---
phase: 186-the-python-floor-before-the-eol
plan: 04
type: execute
wave: 4
depends_on: ["186-03"]
files_modified:
  - .planning/notes/python-floor-decision.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FLOOR-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The reasoning lives in `/workspaces/.planning/notes/python-floor-decision.md`, matching the 27 existing rationale notes and the citation style this milestone already uses (CLAIM-08 cites `notes/catalog-sync-check-retirement.md` the same way). Not the prom wiki: there is no local clone, page creation is an operator-only web-UI step, and no automated wiki guard has existed since 2026-09-02, so a wiki record cannot be gated."
    - "The note is non-vacuous in the only way that matters for FLOOR-03's second sentence — *the next person does not re-derive this*. It carries all four of D-08's required contents as named sections: the decision with its three rejected alternatives, the measured evidence, the standing rule, and the successor backlog item with its date. A note that records only the decision would satisfy FLOOR-03's letter and fail its purpose. (Probe row FLOOR-03/`empty`, resolved explicit.)"
    - "The standing rule is written so it can be APPLIED without re-deriving it: *the floor tracks the version CI runs; when mypy's minimum supported target rises above it, move all four statements together.* That sentence, not a narrative, is what stops the treadmill recurring as a surprise."
    - "A successor backlog item carries **2027-10-31** — Python 3.11's end of life — explicitly in its title or first line, exactly the way 999.27 carried 2026-10-31. That date is the only reason this phase landed ahead of its deadline instead of under it (D-08)."
    - "Every figure quoted in the note is transcribed from `186-RESEARCH.md`, never re-measured and never recomputed: mypy 35 errors in 15 files across 181 checked, byte-identical at 3.10 and 3.11; `mypy.defaults.PYTHON3_VERSION_MIN == (3, 10)` in the pinned mypy 2.3.1; 182 ruff findings, 190 fixes, 3 hand-fixed. Re-measuring here would produce a second, differently-dated set of numbers for the same claim."
    - "All three meta-repo surfaces that co-state the floor are corrected, not just the two D-11 named: `.planning/codebase/STACK.md` (three sites), `.planning/codebase/STRUCTURE.md` (one), and `.planning/codebase/CONVENTIONS.md` (one, doubly falsified — the floor moves AND the sweep makes the union spelling the norm rather than the exception it is described as)."
    - "The phase's residual gap is named rather than left implied: `firestarter/py32_dfu.py` keeps 14 pre-suppressed UP045 sites, so the new lint target has its principal consequence switched off in one whole file — the shape D-09 rejected in the abstract. Recorded, deliberately not tasked (orchestrator ruling 4); the analog note ends with exactly such a section."
    - "The meta repo's `firestarter_app` gitlink is advanced to the app repo's tip once, at the end, after every app-repo commit exists. Nothing is pushed: this project pushes at ship time only, and the release itself is operator-gated."
  prohibitions:
    - "MUST NOT leave the consumer impact of this published-metadata break unrecorded, and MUST NOT record it as \"pip refuses\". Measured: an unpinned `pip install firestarter` on 3.9/3.10 emits NO error and exits 0 — pip silently selects the newest release that still advertises a compatible floor. The quoted refusal fires only on a pinned or exact request. Recording the refusal wording would tell a 3.10 user to expect an error they will never see, while their updates quietly stop arriving. The honest one-liner is that 3.9/3.10 users are pinned to the last release advertising the old floor (C-5)."
  artifacts:
    - path: ".planning/notes/python-floor-decision.md"
      provides: "The FLOOR-03 record: the decision, the three rejected alternatives with their measured grounds, the evidence, the standing rule, the successor item and the named residual gap"
      contains: "2027-10-31"
    - path: ".planning/codebase/STACK.md"
      provides: "The meta-repo stack record with all three floor claims corrected"
      contains: "3.11"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "The Build Environments record with its Python floor corrected"
      contains: "3.11"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "The type-hint conventions section, with the falsified despite-the-floor framing of the union spelling removed"
      contains: "|"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog 999.67, the successor carrying the 2027-10-31 date"
      contains: "999.67"
  key_links:
    - from: "firestarter_app/.planning/codebase/STACK.md"
      to: ".planning/notes/python-floor-decision.md"
      via: "the one-line pointer plan 186-03 added, naming the meta repository and this path; this plan is where that path starts resolving"
      pattern: "python-floor-decision"
    - from: ".planning/notes/python-floor-decision.md"
      to: ".planning/ROADMAP.md"
      via: "the note's successor section names backlog 999.67 and 999.67's body cites the note back, the way 999.26/999.27 and Phase 131 D-13 cite each other"
      pattern: "999.67"
---

<objective>
Record why the floor is 3.11 where the next person to face this will find it, correct the three
meta-repo documents that still state the old one, and file the successor carrying the next date.

Purpose: FLOOR-03's second sentence is the whole requirement — *the treadmill recurs; the decision
should not be re-derived*. This phase exists on schedule rather than as a surprise for exactly one
reason: Phase 131's D-13 filed backlog items carrying the deadline, so the deadline arrived as a
tracked item instead of an EOL announcement. This plan repeats that mechanism for 2027-10-31, and
writes down the rule that makes the next move mechanical rather than a fresh investigation.

This is the phase's last expansion, and the only one outside the app repository. It touches no code.

Output: one rationale note, three corrected meta-repo records, one successor backlog item, and an
advanced gitlink.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md
@.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md
@.planning/phases/186-the-python-floor-before-the-eol/186-PATTERNS.md
@/workspaces/CLAUDE.md

Note on omissions: `.planning/PROJECT.md` and `.planning/STATE.md` are deliberately NOT referenced —
STATE.md is 507 KB with a single ~52,000-character line and inlining it would exhaust the executor's
context. `.planning/ROADMAP.md` IS edited by this plan but must never be `cat`ed whole: it is ~7,100
lines. Slice it with `/usr/bin/grep -n` then `sed -n 'A,Bp'`.

**Repo mechanics.** Every file in this plan lives in the META repository `/workspaces`
(`henols/firestarter_prom`), not in a submodule. Commit in `/workspaces`, on branch
`gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. Do not push — this project pushes at ship
time only. The app repo's own `.planning/codebase/STACK.md` was corrected in plan 186-03 and is NOT
touched again here; do not confuse the two `.planning/codebase/STACK.md` files.

**ROADMAP.md editing rule.** Use the `Edit` tool for scoped replacements only. Do NOT use any
`gsd-tools query roadmap …` verb on this file: those verbs reformat the whole document, and
`roadmap.update-plan-progress` has been observed overwriting an unrelated phase's dependency table by
position. Snapshot the file before editing and diff after, so a clobber is caught rather than committed.
</context>

<failing_direction_convention>
Every `<automated>` command below is a green-direction command with a `<fails_when>` naming an observable
signal. Specific to this plan: its deliverable is prose, and prose has a characteristic failure mode —
a document that exists, reads well, and does not actually contain the thing the requirement asked for.
So the criteria assert named sections, specific dates and specific transcribed figures by grep, not
"the note explains the decision". A note nobody can check is the same defect class this milestone exists
to remove.
</failing_direction_convention>

<!-- planner-discipline-allow: 3.9 -->
<!-- planner-discipline-allow: py39 -->

<tasks>

<task type="auto">
  <name>Task 1: The rationale note — decision, evidence, standing rule, successor, residual gap (D-07, D-08, FLOOR-03)</name>
  <read_first>
    - `/workspaces/.planning/notes/catalog-sync-check-retirement.md` — the WHOLE file (230 lines). This is
      the analog and the freshest instance of the shape, written for Phase 185 of this same milestone.
      Note specifically: the three-key YAML frontmatter closed by `---`, with `title` written as a
      sentence with a subordinate clause rather than a label and `context` naming milestone, phase,
      requirement ID, decision IDs and how the evidence was obtained; an `# H1` repeating the subject with
      its requirement ID in parentheses; `## VERDICT` FIRST, before any evidence, stating the decision in
      bold in its opening sentence; numbered `## N.` sections each mapping to one thing the requirement
      obliged the note to record; verbatim quoted evidence in fenced blocks, introduced as quoted from the
      source; and a closing section naming the residual gap explicitly.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-PATTERNS.md` §
      "`/workspaces/.planning/notes/python-floor-decision.md`" — the measured consistency table across all
      27 existing notes (what is universal, what is a majority, what is a minority) and the recommended
      section skeleton for this note.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 1, § 2 and
      § 7 P-3 — every figure this note quotes, and the pip consumer measurement. Transcribe from here; do
      not re-measure. The note's `date` is this phase's execution date and its figures are pinned to
      mypy 2.3.1 and ruff 0.16.4, which the note must say.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md` D-01, D-02, D-07,
      D-08 and D-12 — the decision, the three rejected alternatives with their grounds, where the record
      lives, what the note must carry, and the release-wording constraint.
  </read_first>
  <files>.planning/notes/python-floor-decision.md</files>
  <action>
    Create `/workspaces/.planning/notes/python-floor-decision.md`. One commit in the meta repo.

    FRONTMATTER, three keys, closed by `---`: a `title` written as a sentence — the floor raised to 3.11,
    why, what it breaks, and the rule for the next move; a `date`; and a `context` naming v1.37 Phase 186,
    FLOOR-01/02/03, the D-01…D-12 decision range, and that the figures were measured in the py3.11
    CI-replica venv. Then an `# H1` naming the subject with `(FLOOR-01)`.

    `## VERDICT` FIRST, before any evidence. Open with the decision in bold: the floor is 3.11, and all
    four statements — `requires-python`, ruff's `target-version`, mypy's `python_version` and the CI pin —
    now name it. Then the grounds in one paragraph. Then an explicit paragraph enumerating what this note
    is required to carry, so a reader can check the note against its own contract, exactly as the analog
    does.

    `## 1. The decision, and the three rejected alternatives.` Each alternative with the measured ground
    that rejected it, not a preference: 3.10 would be end-of-life on the day it landed and would re-fire
    the same backlog item immediately; holding 3.9 is unreachable because `mypy.defaults.PYTHON3_VERSION_MIN`
    is `(3, 10)` under the standing `mypy>=2.1.0,<3` pin, so mypy cannot target it at all; 3.12 would force
    a CI change and drop users on a still-supported runtime. Then why 3.11 specifically: it is the version
    CI already executes, so the floor becomes proven rather than advertised; it is what `tools/catalog/`
    already needs; and it buys about thirteen months rather than seven weeks.

    `## 2. The evidence.` Transcribed from RESEARCH, in fenced blocks, with the producing command shown,
    introduced as quoted from the research pass rather than re-measured. At minimum: the mypy count is
    byte-identical at 3.10 and 3.11 — 35 errors in 15 files, 181 checked — so the watermark neither rose
    nor fell and neither branch of D-05 fired; `PYTHON3_VERSION_MIN == (3, 10)` in mypy 2.3.1; the ruff
    consequence of moving the target (182 findings, 190 fixes, 3 hand-fixed by rule and location); and the
    ordering constraint, which is the single most reusable fact here — the sweep emits 3.11-only code, so
    it must land after the config, and the reverse order produces a false "new type errors" signal.
    State that the figures are pinned to mypy 2.3.1 and ruff 0.16.4 and that a version bump in either
    invalidates them.

    Also record, in this section, what a 3.9/3.10 consumer actually experiences — and record it correctly.
    An unpinned `pip install firestarter` on those interpreters emits no error and exits 0; pip silently
    selects the newest release still advertising a compatible floor. The quoted refusal wording fires only
    on a pinned or exact request. "pip refuses" is the intuitive claim and it is wrong for the default
    case; writing it would tell a 3.10 user to expect an error they will never see while their updates
    quietly stop arriving. Note honestly that this was measured against a local wheel index rather than
    against PyPI, and that the mechanism is index-agnostic.

    `## 3. The standing rule.` One applicable sentence, set apart so it can be found and used without
    reading the rest: *the floor tracks the version CI runs; when mypy's minimum supported target rises
    above it, move all four statements together.* Then name what enforces it — the four-way agreement gate
    at `firestarter_app/tests/test_python_floor_agreement.py`, which fails closed the moment any one of
    the four drifts, including the CI pin. That last clause is the deliberate part: CI silently moving off
    the floor and leaving it untested again is exactly how the divergence this note describes survived
    from 2026-05-27 to now.

    `## 4. The successor: 3.11 EOLs 2027-10-31.` Name backlog 999.67 and the date. Record that this
    phase's existence ahead of its deadline is attributable to Phase 131 D-13 having filed 999.26/999.27
    the same way, and that the successor is filed for that reason rather than as bookkeeping. Note that the
    3.11 EOL date is carried from CONTEXT.md and was not independently re-verified against python.org —
    flag it as such rather than presenting it as measured.

    `## 5. The residual gap, named explicitly.` `firestarter/py32_dfu.py` keeps 14 `Optional[...]`
    annotations, all pre-suppressed, at the line numbers RESEARCH records. Ruff's autofix does not reach
    them and they pre-date this phase, so they were deliberately not swept — but they mean the new lint
    target has its principal consequence switched off in one whole file, which is the shape D-09 rejected
    in the abstract. Say so plainly. The analog note ends with exactly such a section, and it is what makes
    a record trustworthy rather than promotional.

    This file is a `.md` document in the meta repo. The source-comment hard rule does not reach it — prose
    is precisely what belongs here, and that asymmetry is the entire reason D-07 splits the record between
    this note and the app repo's own stack document.
  </action>
  <acceptance_criteria>
    - The file exists at `/workspaces/.planning/notes/python-floor-decision.md` and is at least 80 lines.
    - Its first line is `---` and the frontmatter carries `title:`, `date:` and `context:` keys, with the
      `context:` value naming both `186` and `FLOOR-01`.
    - `/usr/bin/grep -c '^## ' .planning/notes/python-floor-decision.md` prints at least `6` — a VERDICT
      section plus the five numbered sections.
    - `/usr/bin/grep -c '^## VERDICT' .planning/notes/python-floor-decision.md` prints `1`, and its line
      number is lower than that of any other `## ` heading.
    - `/usr/bin/grep -c '2027-10-31' .planning/notes/python-floor-decision.md` prints at least `1`.
    - `/usr/bin/grep -c '999.67' .planning/notes/python-floor-decision.md` prints at least `1`.
    - `/usr/bin/grep -c 'PYTHON3_VERSION_MIN' .planning/notes/python-floor-decision.md` prints at least
      `1`, and `/usr/bin/grep -c '181 source files\|checked 181' …` prints at least `1` — the two
      load-bearing measurements are present as figures, not as summaries of figures.
    - `/usr/bin/grep -c 'py32_dfu' .planning/notes/python-floor-decision.md` prints at least `1` — the
      residual gap is named.
    - `/usr/bin/grep -c 'test_python_floor_agreement' .planning/notes/python-floor-decision.md` prints at
      least `1` — the standing rule names what enforces it.
    - The note contains no sentence asserting that pip refuses an unpinned install on 3.9 or 3.10. It
      states the silent-pin behaviour instead, and flags that the measurement was against a local index.
    - The note states that its figures are pinned to mypy 2.3.1 and ruff 0.16.4.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c '^## ' .planning/notes/python-floor-decision.md</automated>
    <fails_when>the printed count is below `6`, or the command exits 2 because the file does not exist</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n '2027-10-31\|999.67\|PYTHON3_VERSION_MIN\|py32_dfu\|test_python_floor_agreement' .planning/notes/python-floor-decision.md</automated>
    <fails_when>fewer than five distinct search terms appear across the printed lines — the date, the successor id, the load-bearing mypy fact, the residual gap and the enforcing gate must each be present at least once; no output at all (exit 1) means the note is a narrative that records none of the five things D-08 requires</fails_when>
    <automated>cd /workspaces && head -1 .planning/notes/python-floor-decision.md</automated>
    <fails_when>the printed line is not exactly `---` — the frontmatter is missing, which 25 of the 27 existing notes carry and which the analog carries</fails_when>
  </verify>
  <done>A note exists in the established location and shape, opening with the decision, carrying all four of D-08's required contents plus the residual gap, quoting the measured figures rather than re-deriving them, stating the standing rule as one applicable sentence, and describing the consumer impact as it was measured rather than as it was assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three meta-repo documents that still state the old floor (D-11, C-3)</name>
  <read_first>
    - `/workspaces/.planning/codebase/STACK.md:188-210` and `:250-260` — the three floor claims at `:193`
      (Languages → Primary), `:203` (Runtime → Environment, which also names a tested-through range and a
      dev-machine version) and `:256` (Development). Read each in its section: the three are different
      sentence shapes and a blanket replacement would leave at least one ungrammatical.
    - `/workspaces/.planning/codebase/STRUCTURE.md:355-368` — the `## Build Environments` section, the
      `*[unverified in 2026-08-26 scoped remap …]*` caveat at `:360` that heads it, and the
      `- **Python:** 3.9+` line at `:363`.
    - `/workspaces/.planning/codebase/CONVENTIONS.md:164-178` — the whole type-hints block. The claim at
      `:173` is doubly falsified by this phase: the floor moves, AND the sweep makes the union spelling the
      norm rather than the exception the sentence frames it as. The two fenced examples around it show the
      old spellings as house style, which the sweep reversed.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 4, both the
      verified table and the "Surfaces the discussion MISSED" table — the latter is why two of these three
      files are in scope at all: D-11 named only `STACK.md`.
  </read_first>
  <files>.planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/CONVENTIONS.md</files>
  <action>
    One commit in the meta repo. Three files, five sites. These are `.md` records, not source; correcting
    prose is what belongs here and the source-comment hard rule does not reach them.

    `STACK.md` — correct all three floor claims to 3.11. At `:203` the line also states a tested-through
    range and a dev-machine version; the tested-through range should name what is actually tested after
    this phase, and the dev-machine observation is not this phase's to change. Do not invent a new
    tested-through claim: the project's own classifier list after plan 186-01 says 3.11 and 3.12, and CI
    executes 3.11 — say that, and nothing broader.

    `STRUCTURE.md` — correct the one line under `### Python Application`. Leave the
    `*[unverified in 2026-08-26 scoped remap — submodule contents, out of scope]*` caveat that heads the
    section exactly as it is: it is a provenance marker about how that section was produced, not a floor
    claim, and this phase is not the audit that would clear it.

    `CONVENTIONS.md` — the sentence framing the union spelling as appearing "in some places despite" the
    old requirement is falsified in both of its halves. Correct it so it describes what the tree now is:
    after this phase's sweep, the union spelling IS the convention throughout `firestarter/`, with one
    named exception — `py32_dfu.py`, whose sites are individually suppressed. The fenced examples above it
    that show the old spellings as house style should show what the tree now contains; take the replacement
    text from real post-sweep source rather than composing it, and if the surrounding paragraph about which
    names are imported from `typing` no longer holds, correct that too rather than leaving it stale beside
    a corrected neighbour. This document is a description of the codebase; after this phase it describes
    a codebase that no longer exists unless it is updated.

    Then prove the meta repo has no remaining floor claim: scan `.planning/codebase/` for any surviving
    statement of the old version. RESEARCH enumerated exactly five, all above. A sixth hit is a surface
    both the discussion and the research missed and must be dispositioned in the SUMMARY, not waved past.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -rnIE '3\.9|py39' .planning/codebase/` produces NO output and exits 1 — every one of
      the five enumerated sites is corrected and no sixth survives.
    - `/usr/bin/grep -c '3\.11' .planning/codebase/STACK.md` prints at least `3`.
    - `/usr/bin/grep -c '3\.11' .planning/codebase/STRUCTURE.md` prints at least `1`.
    - `/usr/bin/grep -c 'unverified in 2026-08-26 scoped remap' .planning/codebase/STRUCTURE.md` prints
      `1` — the provenance caveat was not swept along with the floor claim.
    - `/usr/bin/grep -c 'py32_dfu' .planning/codebase/CONVENTIONS.md` prints at least `1` — the one named
      exception to the new convention is named where the convention is stated.
    - `git diff --numstat "$BASE_SHA" -- .planning/codebase/` reports changes in exactly three files.
    - The SUMMARY records that two of these three files (`STRUCTURE.md`, `CONVENTIONS.md`) were NOT named
      by D-11 and were found by the research pass, so the widening is visible rather than silent.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -rnIE '3\.9|py39' .planning/codebase/</automated>
    <fails_when>any line is printed. Exit 1 with no output is the PASS signal here: it means no old-floor claim survives anywhere under that directory.</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'unverified in 2026-08-26 scoped remap' .planning/codebase/STRUCTURE.md</automated>
    <fails_when>the printed count is not `1`, or the command exits 1 (count 0), which means the provenance caveat heading that section was deleted along with the floor claim inside it</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'py32_dfu' .planning/codebase/CONVENTIONS.md</automated>
    <fails_when>the printed count is below `1`, or the command exits 1 (count 0) — the corrected convention statement must name the one file that is an exception to it, or it becomes a new overbroad claim</fails_when>
  </verify>
  <done>No statement of the old floor survives anywhere under `.planning/codebase/`, all five enumerated sites are corrected in their own sentence shapes, the type-hint conventions describe the tree the sweep actually produced with its one named exception, the unrelated provenance caveat is intact, and the two files D-11 did not name are recorded as a visible widening.</done>
</task>

<task type="auto">
  <name>Task 3: File the successor carrying 2027-10-31, advance the gitlink, and close the record (D-08, D-12)</name>
  <read_first>
    - `/workspaces/.planning/ROADMAP.md` — slice it, never `cat` it; it is ~7,100 lines. Read
      `### Phase 999.63:` through `### Phase 999.66:` and the `---` separator that follows the last of
      them, using `/usr/bin/grep -n '^### Phase 999\.6'` then `sed -n`. Those four are the freshest stubs
      and carry the house shape: an `### Phase 999.NN:` heading with a sentence-length title and a
      parenthesised `(BACKLOG — filed <date> during <milestone> Phase <N>)` suffix, a `**Goal:**` block,
      then prose giving the evidence and the scope boundary. Also read `### Phase 999.27:` — the stub this
      one succeeds — to see how a deadline-carrying item states its date.
    - `/workspaces/.planning/notes/python-floor-decision.md` — the note Task 1 just wrote, so 999.67's
      body can cite it the way 999.63 cites `ae29f2008-classification-verdict.md`.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md` D-08 and D-12 —
      what the successor must carry, and the release-wording constraint.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 7 P-3 — the
      measured consumer behaviour the release fragment must match.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    One commit in the meta repo for the ROADMAP edit, then the gitlink advance.

    STEP 0 — SNAPSHOT. Copy `.planning/ROADMAP.md` to the scratchpad before editing. After the edit, diff
    the two and confirm the ONLY change is the inserted block. This is not ceremony: a roadmap-mutating
    verb has been observed in this project overwriting an unrelated phase's dependency table by position,
    and the diff is what catches it. Use the `Edit` tool for a scoped insertion; do not use any
    `gsd-tools query roadmap …` verb on this file — those reformat the whole document.

    STEP 1 — FILE 999.67. Insert a new `### Phase 999.67:` block immediately after the last prose line of
    the `### Phase 999.66:` block and BEFORE the `---` separator that precedes the archived v1.20 milestone
    heading. Locate that position by content, not by line number — earlier tasks in this phase may have
    shifted it. 999.66 is the current highest backlog id; confirm that before using 999.67.

    The title must carry **2027-10-31** explicitly, the way 999.27 carried 2026-10-31 — that is D-08's
    requirement and it is the mechanism, not a formatting preference: a date in a title is what made this
    phase land ahead of its deadline instead of under it. The body states the goal (re-run the same
    four-statement move when mypy's minimum supported target next rises, or when 3.11 reaches end of
    life), names the standing rule rather than restating its derivation, cites
    `.planning/notes/python-floor-decision.md` for the full reasoning, and names
    `firestarter_app/tests/test_python_floor_agreement.py` as the gate that will make the divergence
    visible rather than silent this time. Note honestly that the 2027-10-31 date is carried from
    CONTEXT.md and was not independently verified against python.org.

    STEP 2 — RECORD THE RELEASE FRAGMENT. Write a one-line release-note fragment into this plan's SUMMARY
    for the operator to use at the next beta cut, per D-12. It must match what was measured, not what was
    assumed: the fragment says the minimum supported Python is now 3.11 and that users on 3.9 or 3.10 stay
    on the last release advertising the old floor — receiving no error and no further updates — rather
    than saying pip refuses. Do not write it into a release, a tag, a version bump or a changelog: releases
    are operator-gated in this project, D-12 rejected introducing a changelog, and this phase publishes
    nothing.

    STEP 3 — CONFIRM THE POINTER RESOLVES. Plan 186-03 wrote a pointer into the app repo's
    `.planning/codebase/STACK.md` naming this repository and `.planning/notes/python-floor-decision.md`.
    That path now exists. Verify it does, from the meta repo root, and record the verification: a pointer
    to a file that was never created is precisely the class of defect this milestone exists to remove, and
    the two ends of it live in two different repositories, so nothing else checks it.

    STEP 4 — ADVANCE THE GITLINK, LAST. With every app-repo commit from plans 186-01, 186-02 and 186-03
    already made, stage the `firestarter_app` gitlink in the meta repo and commit it. Record the app repo's
    resulting HEAD sha in the SUMMARY. Do this once, at the end, so the gitlink names a tree whose gates
    are all green rather than an intermediate. Do NOT push anything, in either repository.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c '^### Phase 999.67:' .planning/ROADMAP.md` prints `1`, and that heading line also
      contains `2027-10-31`.
    - `/usr/bin/grep -c '^### Phase 999.68:' .planning/ROADMAP.md` prints `0` — 999.67 was the next free
      id and no duplicate or skipped numbering was introduced.
    - The 999.67 block cites both `python-floor-decision.md` and `test_python_floor_agreement.py`.
    - `diff <snapshot> .planning/ROADMAP.md` shows only added lines, all contiguous, all inside the new
      block — no line elsewhere in the document was changed, reordered or reformatted.
    - `test -f .planning/notes/python-floor-decision.md` succeeds — the path the app repo's STACK.md
      pointer names now resolves, and the SUMMARY records that this was checked.
    - `cd /workspaces && git diff --cached --stat` before the final commit shows the `firestarter_app`
      gitlink among the staged entries, and the SUMMARY names the app repo HEAD sha it advanced to.
    - `cd /workspaces/firestarter_app && git status --porcelain` is clean of modified tracked files. In
      the meta repo, capture the commit list first so a failing `git` cannot be swallowed by a pipeline —
      assign `AHEAD=$(git log origin/beta..HEAD --oneline)` on its own, then assert that `$AHEAD` holds at
      least one line while `git status` still reports the branch as unpushed. Nothing left either repository.
    - The SUMMARY carries the one-line release-note fragment, stating the silent-pin behaviour rather than
      a refusal, and states explicitly that no release, tag or version bump was made.
    - The SUMMARY carries the two deliberate deviations from D-10's literal line ranges recorded by plan
      186-01, the three hand-fixed ruff findings named by plan 186-02, and the residual `py32_dfu.py` gap —
      so the phase's full record is assembled in one place at its close.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -n '^### Phase 999.67:' .planning/ROADMAP.md</automated>
    <fails_when>no line is printed (exit 1), or the printed heading does not contain `2027-10-31` — a successor without its date is the bookkeeping version of this item and would not have caused this phase to exist</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'python-floor-decision.md\|test_python_floor_agreement' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is below `2`, or the command exits 1 (count 0) — the successor must cite both the reasoning note and the gate that will surface the next divergence</fails_when>
    <automated>cd /workspaces && test -f .planning/notes/python-floor-decision.md && echo POINTER_RESOLVES</automated>
    <fails_when>the literal line `POINTER_RESOLVES` is not printed, or exit is non-zero — the app repo's stack record names this path and nothing else in either repository checks that it exists</fails_when>
    <automated>cd /workspaces && git diff HEAD --stat -- firestarter_app</automated>
    <fails_when>no output at all, which means the gitlink was not advanced and the meta repo still points at the app tree as it was before this phase. After the commit the equivalent check is `git log -1 --stat -- firestarter_app` naming the submodule.</fails_when>
  </verify>
  <done>Backlog 999.67 carries 2027-10-31 in its title and cites both the note and the gate, the ROADMAP diff contains nothing but the inserted block, the cross-repository pointer resolves, the gitlink names the app tree whose gates are all green, the release fragment is recorded for the operator in the form that matches measurement, and nothing was pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a published-metadata break → a downstream consumer who is never told | The one genuine threat surface of this phase, and the only one that reaches a person outside this repository. It is closed by a record, not by code. |
| a record → the reader who will act on it | A rationale note's only function is to be correct when someone relies on it in a year. A note that quotes a figure wrong, or records an assumed behaviour as a measured one, is worse than no note. |
| a scoped edit → a 7,100-line shared document | `ROADMAP.md` carries every phase in the project's history. A positional or whole-file write here silently damages records this phase has no relationship with. |
| the meta repo's gitlink → which app-repo tree the project claims | Advancing it to the wrong sha makes the meta repo assert a state of the app repo that was never proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-186-04-A | Repudiation (a record that misstates what a consumer will experience) | the note and the release fragment | high | mitigate | The intuitive claim — that pip refuses — is wrong for the default case and was measured wrong. Both the note and the fragment are constrained by a `must_haves` prohibition and by acceptance criteria to state the silent-pin behaviour instead, and to flag that it was measured against a local index rather than PyPI. |
| T-186-04-B | Tampering | `.planning/ROADMAP.md` | high | mitigate | Every roadmap-mutating `gsd-tools` verb is forbidden on this file by instruction; the edit is a scoped insertion located by content; and a before/after snapshot diff must show only contiguous added lines inside the new block. |
| T-186-04-C | Spoofing (a pointer naming a file that does not exist) | the cross-repository record link | medium | mitigate | The two ends live in different git repositories, so no gate in either one checks the pair. Task 3 verifies the target resolves and records the verification, and the plan ordering guarantees the note exists before the check runs. |
| T-186-04-D | Repudiation (a gitlink asserting an unproven tree) | the meta repo's `firestarter_app` submodule entry | medium | mitigate | Advanced once, last, after every app-repo commit exists and after plan 186-03's `CI-REPLICA: PASS`, with the resulting app-repo HEAD sha recorded in the SUMMARY. |
| T-186-04-E | Information disclosure | the note and the backlog stub | low | accept | Both contain version numbers, tool versions, repository-relative paths and a public repository name. No secret, credential or absolute host path is written. |
| T-186-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package, adds no dependency and runs no build. It writes and edits Markdown in the meta repository and advances one submodule pointer. RESEARCH.md records that no Package Legitimacy Audit table is required for this phase and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from the spec-less edge probe and from planning-time measurement. Not silently dropped.

- **FLOOR-03's `empty` probe row is resolved EXPLICITLY in this plan's `must_haves.truths` above.** Its
  generic form asks what the result is for empty or null input; translated honestly for a
  record-keeping requirement, it is the question of whether the record can exist and still be empty of
  the thing that was required — a note that is present, well written, and carries none of D-08's four
  contents. That is why the criteria grep for named sections, a specific date, a specific backlog id and
  two specific figures rather than asserting that the note explains the decision. Phase-wide accounting:
  FLOOR-01's `adjacency`, `empty` and `ordering` rows and FLOOR-03's `encoding` row are resolved
  explicitly in plan 186-03; FLOOR-02's `unclassified` row is flagged unresolved in plan 186-01. Six
  surfaced, six accounted for, none dropped, and no row needed a `verification: backstop` marker.
- **The 2027-10-31 date is INHERITED, not measured.** It is carried from CONTEXT.md D-08 as Python
  3.11's end of life and was not independently verified against python.org during research, which
  recorded it as its lowest-confidence item. Both the note and backlog 999.67 must flag it as carried
  rather than presenting it as measured. If it is wrong, the successor carries a wrong deadline — which
  is a smaller failure than carrying no deadline, but not a nil one.
- **The pip consumer measurement was taken against a local `--find-links` wheel index, not against
  PyPI.** The mechanism (candidate filtering by `Requires-Python`) is index-agnostic, but the claim is
  one step removed from the thing it describes. Recorded in the note as such.
- **`.planning/codebase/` is itself a stale-records surface, and this plan corrects only the floor
  claims in it.** `STRUCTURE.md`'s Build Environments section carries an `unverified` provenance caveat
  from a 2026-08-26 scoped remap; clearing that caveat is a documentation audit, not this phase. The
  caveat is deliberately preserved so a later reader is not misled into thinking the section was
  re-verified when only one line in it was corrected.
- **`CONVENTIONS.md`'s correction is the one edit in this plan with real judgement in it.** The other
  four sites are version-number corrections. This one must describe a convention the sweep created, with
  its one named exception, and it must take its example text from real post-sweep source rather than from
  composition — otherwise the document describes a codebase that does not exist, which is the defect
  class it is being corrected for.
- **No release, no tag, no version bump, no push.** Releases are operator-gated in this project; D-12
  rejected introducing a changelog as a new capability rather than a clarification; and this project
  pushes at ship time only. The release fragment is prose in a SUMMARY for the operator to use at close.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan (NEW paths and NEW symbols; a source-grounding drift pass must not flag them as
missing from prior tree state):
- `/workspaces/.planning/notes/python-floor-decision.md` — the rationale note, with sections
  `## VERDICT`, `## 1. The decision, and the three rejected alternatives`, `## 2. The evidence`,
  `## 3. The standing rule`, `## 4. The successor: 3.11 EOLs 2027-10-31`, and
  `## 5. The residual gap, named explicitly`
- `### Phase 999.67:` — a new backlog stub in `/workspaces/.planning/ROADMAP.md`, carrying 2027-10-31

Modified in place by this plan:
- `/workspaces/.planning/codebase/STACK.md` — three floor claims
- `/workspaces/.planning/codebase/STRUCTURE.md` — one floor claim
- `/workspaces/.planning/codebase/CONVENTIONS.md` — one doubly-falsified convention statement and its
  surrounding examples
- `/workspaces/.planning/ROADMAP.md` — one inserted block, nothing else
- the meta repo's `firestarter_app` submodule gitlink — advanced once, last

Deliberately NOT modified, with the reason on the record:
- `firestarter_app/.planning/codebase/STACK.md` — the APP repo's own record, corrected in plan 186-03;
  it is a different file from the meta repo's same-named one and is not touched again here
- `/workspaces/.planning/codebase/STRUCTURE.md`'s `unverified in 2026-08-26 scoped remap` caveat — a
  provenance marker about how that section was produced, not a floor claim
- anything under `/workspaces/firestarter_app/` or `/workspaces/firestarter/` — this plan is meta-repo
  only, and the firmware repo is untouched by the entire phase
- any release, tag or version file — operator-gated
</artifacts_this_phase_produces>

<verification>
- `.planning/notes/python-floor-decision.md` exists, opens with `---` frontmatter, carries at least six
  `## ` sections with `## VERDICT` first, and names `2027-10-31`, `999.67`, `PYTHON3_VERSION_MIN`,
  `py32_dfu` and `test_python_floor_agreement`.
- The note states the silent-pin consumer behaviour and contains no assertion that pip refuses an
  unpinned install on the old interpreters.
- `/usr/bin/grep -rnIE '3\.9|py39' .planning/codebase/` produces no output and exits 1.
- `.planning/codebase/STRUCTURE.md` still carries its 2026-08-26 provenance caveat.
- `.planning/codebase/CONVENTIONS.md` names `py32_dfu` as the one exception to the new convention.
- `### Phase 999.67:` exists exactly once, its heading contains `2027-10-31`, and its body cites both
  `python-floor-decision.md` and `test_python_floor_agreement.py`.
- The ROADMAP snapshot diff contains only contiguous added lines inside the new block.
- The app repo's STACK.md pointer target resolves from the meta repo root.
- The meta repo's `firestarter_app` gitlink is advanced, and the SUMMARY names the app-repo HEAD sha.
- Neither repository was pushed; no release, tag or version bump exists.
- The SUMMARY assembles the phase's full record: the two D-10 deviations, the three hand-fixed ruff
  findings, the residual `py32_dfu.py` gap, and the release-note fragment.
</verification>

<success_criteria>
The reasoning is recorded where the next person will actually find it — in the meta repo's established
notes location for the reader who has it, and behind a one-line pointer in the app repo's own stack
record for the reader who does not. The record carries all four of the things it was required to carry,
with its figures transcribed rather than re-derived, its one inherited date flagged as inherited, its
consumer claim matching what was measured rather than what was assumed, and its residual gap named
rather than omitted. The successor carries the next deadline in its title, which is the mechanism that
made this phase land ahead of its own. Every meta-repo surface that stated the old floor now states the
current one, including the two the discussion missed. And the phase closes without publishing anything,
because that is the operator's to do.
</success_criteria>

<output>
Create `.planning/phases/186-the-python-floor-before-the-eol/186-04-SUMMARY.md` when done
</output>
