---
phase: 186-the-python-floor-before-the-eol
plan: 02
type: execute
wave: 2
depends_on: ["186-01"]
files_modified:
  - firestarter_app/firestarter/address_parser.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/codec.py
  - firestarter_app/firestarter/config.py
  - firestarter_app/firestarter/diagnostic_report.py
  - firestarter_app/firestarter/eprom_info.py
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/firestarter/firmware.py
  - firestarter_app/firestarter/hardware.py
  - firestarter_app/firestarter/ic_layout.py
  - firestarter_app/firestarter/jp5_gate.py
  - firestarter_app/firestarter/main.py
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py
  - firestarter_app/tests/test_runtime_dependencies.py
  - firestarter_app/tests/test_cap03_ack_layout_parity.py
autonomous: true
requirements: [FLOOR-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The sweep runs with NO `--target-version` flag on any command. The target comes from `[tool.ruff] target-version = \"py311\"` in `pyproject.toml`, which plan 186-01 landed — so the sweep's own output is the proof that the config edit is live rather than nominal (D-09's whole point: a target whose consequences are switched off is a claim-shaped defect)."
    - "The three non-auto-fixable findings are handled INDIVIDUALLY, by hand, and named by rule and location: UP045 at `firestarter/eprom_info.py:97:24`, `:100:36` and `:103:26`, all three in `prepare_detailed_eprom_data`'s signature, all three unfixable because the `Optional[...]` subscript spans lines and encloses a comment. D-09 forbids sweeping them (RESEARCH §2)."
    - "`ruff check` AND `ruff format --check` are BOTH green at the end. CI runs them as two separate steps, and the fix pass alone leaves exactly four files the formatter wants to unwrap — `address_parser.py`, `cli_handlers.py`, `diagnostic_report.py`, `hardware.py` — because shortening an annotation makes a previously-wrapped line fit (RESEARCH §2)."
    - "`ruff check --fix --unsafe-fixes` is never run on this tree, for two separately measured reasons: it produces a format-unstable, inconsistently-suppressed result in `eprom_info.py`, and UP036's unsafe fix DELETES the runtime version guard at `firestarter/main.py:31-35` (RESEARCH §2, §4, P-8)."
    - "The watermark gate still reports `mypy errors: 35 (watermark: 35)` after the sweep. This is only true because `python_version` is ALREADY 3.11: the two UP017 fixes emit `datetime.UTC`, a 3.11 addition, and the same sweep applied at the old target was measured reporting 37 > 35 and FAILING. The dependency on plan 186-01 is a correctness constraint, not a sequencing preference (C-4)."
    - "Six test legs go red from the sweep, all in `tests/test_cap03_ack_layout_parity.py`, all from ONE source-text regex at `:200` pinning `_decode_id_frame`'s old return spelling. The count is predicted from measurement (control `2355 passed, 4 skipped, 0 failed`; with the change `6 failed, 2349 passed`) and one regex edit fixes all six. The gate fails LOUD, not open, which is the good outcome."
    - "No comment is written anywhere. The sweep DELETES five `# noqa: UP006` lines that sat on inner-bracket lines the UP045 fix collapsed away, and deletes `main.py`'s now-unused `typing` import — both are the autofix removing text, which the standing hard rule permits. `firestarter/py32_dfu.py` is untouched: its 14 pre-suppressed UP045 sites are out of scope by orchestrator ruling 4."
  artifacts:
    - path: "firestarter_app/firestarter/eprom_info.py"
      provides: "The three hand-resolved UP045 sites in `prepare_detailed_eprom_data`'s signature, plus the second-order unused-import removal at the top of the file"
      contains: "def prepare_detailed_eprom_data("
    - path: "firestarter_app/firestarter/serial_comm.py"
      provides: "Both `_decode_id_frame` definitions with modernised return annotations — the single source change that reds the six CAP-03 legs"
      contains: "def _decode_id_frame("
    - path: "firestarter_app/tests/test_cap03_ack_layout_parity.py"
      provides: "The repaired definition regex, re-matching the swept signature so the CAP-03 parity extractor has a body to extract from again"
      contains: "_DECODE_ID_FRAME_DEF_RE"
  key_links:
    - from: "firestarter_app/pyproject.toml"
      to: "firestarter_app/firestarter/"
      via: "[tool.ruff] target-version drives which UP rules fire; no command in this plan passes --target-version, so the config file is the only authority"
      pattern: "target-version"
    - from: "firestarter_app/tests/test_cap03_ack_layout_parity.py"
      to: "firestarter_app/firestarter/serial_comm.py"
      via: "_DECODE_ID_FRAME_DEF_RE matches the definition line as source text, so a signature rewrite in the source breaks the test's extractor"
      pattern: "_decode_id_frame"
---

<objective>
Absorb the full lint consequence of the new target, as its own commit, with the three findings ruff
cannot fix handled by hand and named individually — and repair the one existing gate the rewrite reds.

Purpose: D-09 refuses to make `target-version = "py311"` a nominal setting. The alternative considered
and rejected was suppressing UP045 (or all three rules) in `extend-ignore`, which would leave the new
target with its consequences switched off — a claim-shaped defect in a claim-hygiene milestone. So the
182 findings the raise exposes are absorbed rather than silenced, in a commit separate from the config
change so both stay reviewable.

This plan expands horizontally out from plan 186-01's proven slice. It adds no capability and changes no
behaviour: every edit is a type-annotation spelling, and the only non-mechanical judgement in it is the
three-site hand fix and the one regex repair.

Output: a tree that is green on both `ruff check` and `ruff format --check` at the new target, with the
mypy count unmoved at 35, and the CAP-03 parity gate matching the code it guards again.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md
@.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md
@.planning/phases/186-the-python-floor-before-the-eol/186-PATTERNS.md
@/workspaces/CLAUDE.md

Note on omissions: `.planning/PROJECT.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` are
deliberately NOT referenced — see plan 186-01's identical note and the STATE.md size hazard it records.

**Repo mechanics.** Every file here lives in `/workspaces/firestarter_app/`, a separate git repository
tracked as a submodule of `/workspaces`. Commit INSIDE it, on branch
`gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. Do not push. The firmware repo
`/workspaces/firestarter/` is untouched. Wherever RESEARCH.md writes `firestarter/serial_comm.py` it
means `/workspaces/firestarter_app/firestarter/serial_comm.py`.

**Ordering is load-bearing.** This plan's `depends_on: ["186-01"]` encodes a measured correctness
constraint, not a preference: the sweep emits `datetime.UTC`, which is 3.11-only, so running it before
`python_version = "3.11"` lands makes the watermark gate report 37 errors against a ceiling of 35 and
fail — a false "new type errors introduced" signal on a change that introduced none.
</context>

<environment_contract>
Identical to plan 186-01, and it binds every command below:

- Use `/workspaces/firestarter_app/.venv/ci-replica/bin/{python,ruff}` — Python 3.11.16, numpy-free. The
  devcontainer's ambient interpreter is 3.12 and app CI is 3.11-only; measuring in 3.12 has broken beta
  CI before. Never `bash tools/ci_replica_venv.sh --refresh` (its probe would pick the 3.12 today and
  only print an easily-missed `INTERPRETER-DIVERGENCE:` line).
- `/usr/bin/grep`, never bare `grep` — the container's `grep` is ugrep and honours `.gitignore`.
- Every pytest command passes `-o addopts=""`; `[tool.pytest.ini_options] addopts = "-ra -q"` plus a
  command-line `-q` doubles to `-qq` and suppresses the count line.
- The full suite takes roughly 8 minutes. It exceeds a foreground tool timeout — run it in the
  background and collect the result, rather than letting it be killed at 600 s and misread as a failure.
</environment_contract>

<failing_direction_convention>
Every `<automated>` command below is a green-direction command with a `<fails_when>` naming an observable
signal. Note two tool-specific shapes this plan depends on: `ruff check` prints the literal
`All checks passed!` and exits 0 when it finds nothing, so the string is asserted rather than the code
alone; and `ruff format --check` prints `N files would be reformatted` and exits 1 when it is unhappy,
which is the exact CI step the fix pass alone does not satisfy.

The intermediate RED this plan passes through — the six CAP-03 legs, after the sweep and before the
regex repair — is NOT an `<automated>` leg. It is an acceptance criterion requiring its captured output
in the SUMMARY, with the specific failing test names that must appear.
</failing_direction_convention>

<!-- planner-discipline-allow: Optional[ -->
<!-- planner-discipline-allow: Optional\[LogMessage\] -->
<!-- planner-discipline-allow: datetime.timezone.utc -->

<tasks>

<task type="auto">
  <name>Task 1: The sweep — mechanical autofix, three hand fixes named individually, then the formatter (D-09)</name>
  <reversibility rating="costly">Carried from CONTEXT.md D-09, not re-derived. The annotation rewrite touches 15 files across `firestarter/` and `tests/` with roughly 175 insertions and 183 deletions; undoing it means reverting the commit, not editing back by hand. Flagged, not gated — nothing here is a one-way door.</reversibility>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/eprom_info.py:90-115` — `prepare_detailed_eprom_data`'s
      signature in full, and `:1-20` for the `typing` import line. Read the signature before editing it:
      three of its parameters carry a multi-line subscript enclosing a comment, which is exactly why ruff
      classifies those three fixes unsafe, and the shape is not guessable from the finding text.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 2 in full. It
      carries the measured finding split, the per-site table for the three hand fixes with the reason each
      is unfixable, the exact post-fix parameter text, the second-order unused-import finding that only a
      SECOND `--fix` pass catches, the four files the formatter wants to unwrap, and the two separate
      measured reasons `--unsafe-fixes` is the wrong instrument here.
    - `/workspaces/firestarter_app/pyproject.toml` `[tool.ruff]` and `[tool.ruff.lint]` — confirm with
      your own eyes that `target-version` is already `py311` and that `select` is `["E", "F", "I", "UP"]`
      with `extend-ignore = ["E501"]`. Broadening `select` is an explicitly deferred idea and is out of
      scope; if `target-version` is not yet `py311`, plan 186-01 has not landed and this plan must stop.
    - `/workspaces/firestarter_app/.github/workflows/ci.yml:76-92` — the four gate steps this sweep must
      leave green, and specifically that `ruff check` and `ruff format --check` are two separate steps.
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule" — before touching any file under
      `firestarter_app/`. The sweep deletes comments as a side effect, which is permitted; writing one is
      not, at any point, for any reason.
  </read_first>
  <files>firestarter_app/firestarter/address_parser.py, firestarter_app/firestarter/cli_handlers.py, firestarter_app/firestarter/codec.py, firestarter_app/firestarter/config.py, firestarter_app/firestarter/diagnostic_report.py, firestarter_app/firestarter/eprom_info.py, firestarter_app/firestarter/eprom_operations.py, firestarter_app/firestarter/firmware.py, firestarter_app/firestarter/hardware.py, firestarter_app/firestarter/ic_layout.py, firestarter_app/firestarter/jp5_gate.py, firestarter_app/firestarter/main.py, firestarter_app/firestarter/serial_comm.py, firestarter_app/tests/test_dev_gate_reads_no_firmware_source.py, firestarter_app/tests/test_runtime_dependencies.py</files>
  <action>
    Work inside `/workspaces/firestarter_app`. One commit for this whole task — the sweep is a single
    reviewable unit, and splitting it would leave an intermediate commit that is red on `ruff check` for
    three findings.

    STEP 0 — PIN THE TREE AND CONFIRM THE PRECONDITION. Record `git rev-parse HEAD` as `BASE_SHA` and
    `git status --porcelain` (clean apart from the two pre-existing untracked `datasheets/*.pdf`). Then
    read `target-version` out of `pyproject.toml`: it must already be `py311`. If it is not, stop — plan
    186-01 has not landed and running the sweep now produces the false watermark failure described above.

    STEP 1 — THE MECHANICAL PASS. Run `ruff check --fix firestarter/ tests/` with the replica ruff and
    **no `--target-version` flag**. Omitting the flag is deliberate: the target must come from the config
    file, so the command's own output is the evidence that plan 186-01's edit is live. Expect roughly
    `Found 193 errors (190 fixed, 3 remaining).` — ruff iterates, so the fix count exceeds the 179 the
    initial `--statistics` reports. Capture the exact output.

    STEP 2 — THE THREE, BY HAND, NAMED. The three remaining findings are all UP045 and all in
    `firestarter/eprom_info.py`, in `prepare_detailed_eprom_data`'s signature: at `:97:24`
    (`eprom_details`), `:100:36` (`eprom_data_for_programmer`) and `:103:26` (`raw_config_data`). Ruff
    refuses them because each subscript spans lines and encloses a comment, so collapsing it would delete
    that comment. Collapse each of the three parameters onto one line as `dict | None`, dropping the
    now-orphaned inner comments — deletion, which the hard rule permits. Do not add a replacement comment
    and do not preserve the inner text anywhere. RESEARCH § 2 carries the exact resulting three lines.

    Do NOT reach for `--unsafe-fixes` to do this. Measured, it resolves all three but leaves one parameter
    with a different spelling and a different suppression from the other two, and leaves the file failing
    `ruff format --check`. Separately and more seriously, UP036's unsafe fix deletes the runtime version
    guard in `main.py`. `--unsafe-fixes` must not be run on this tree at any point in this phase.

    STEP 3 — THE SECOND PASS. Run `ruff check --fix firestarter/ tests/` again. Expect
    `Found 1 error (1 fixed, 0 remaining).` — a second-order unused-import finding on `typing.Optional`
    at the top of `eprom_info.py`, which becomes unused only once the three hand fixes land, and which
    only a second pass catches.

    STEP 4 — THE FORMATTER. Run `ruff format firestarter/ tests/`. Expect four files reformatted:
    `address_parser.py`, `cli_handlers.py`, `diagnostic_report.py` and `hardware.py`. All four are the
    same shape — shortening an annotation made a previously-wrapped line fit, and the formatter wants to
    unwrap it. This step is not optional cosmetics: CI runs `ruff format --check` as a step of its own,
    and the fix pass alone does not satisfy it.

    STEP 5 — BOTH GATES, THEN THE TYPE-CHECK. `ruff check firestarter/ tests/` must print
    `All checks passed!`; `ruff format --check firestarter/ tests/` must print `179 files already
    formatted`. Then run the watermark gate: it must still report 35 against a ceiling of 35. Two of the
    fixes rewrote `datetime.timezone.utc` to the 3.11-only `datetime.UTC`; the count stays at 35 only
    because `python_version` is already 3.11. If it reports 37, the config edit is not in the tree and
    the commits are in the forbidden order.

    STEP 6 — CHECK THE BLAST RADIUS, AND WHAT IT DID NOT REACH. `git diff --stat` should name 15 files.
    Confirm by name that `firestarter/messages.py` and `firestarter/frame_vectors.py` are NOT among them —
    both are codegen-generated and ID-only, and the two codegen-drift gates plus `beta-release.yml`'s
    normalisation step would diverge if the sweep had reached them. Confirm nothing under `tools/` moved:
    CI's ruff scope is `firestarter/ tests/` only, and `tools/` carries its own unrelated findings that
    are out of scope. Confirm `firestarter/py32_dfu.py` did not move: its 14 pre-suppressed UP045 sites
    pre-date this phase and are recorded, not swept.

    STEP 7 — PREDICT THE RED BEFORE OBSERVING IT. This sweep reds six test legs, all in
    `tests/test_cap03_ack_layout_parity.py`. Write the prediction down first, then run that file and
    record the observed failing test names. Do not repair them here — Task 2 owns the repair, so that the
    cause and the fix are separately legible on the record. If the observed set differs from six, the
    observation wins; record the disagreement rather than absorbing it.
  </action>
  <acceptance_criteria>
    - No command run in this task contains the string `--target-version`, and none contains
      `--unsafe-fixes`. The SUMMARY carries the verbatim command list as evidence.
    - `ruff check firestarter/ tests/` prints exactly `All checks passed!` and exits 0.
    - `ruff format --check firestarter/ tests/` prints `179 files already formatted` and exits 0.
    - `.venv/ci-replica/bin/python tools/check_mypy_watermark.py` prints `mypy errors: 35 (watermark: 35)`
      and `OK: error count at watermark.`, and reports `checked 181 source files`.
    - `/usr/bin/grep -n 'dict | None' firestarter/eprom_info.py` prints at least the three parameter lines
      `eprom_details`, `eprom_data_for_programmer` and `raw_config_data`, each on a single line.
    - `/usr/bin/grep -c 'Optional' firestarter/eprom_info.py` prints `0` — the three hand fixes plus the
      second `--fix` pass removed both the annotations and the now-unused import.
    - `git diff --stat "$BASE_SHA" -- .` names 15 files, and `git diff --name-only "$BASE_SHA" -- .` does
      NOT contain `firestarter/messages.py`, `firestarter/frame_vectors.py`, `firestarter/py32_dfu.py`, or
      any path beginning `tools/` or `.github/`.
    - `git diff "$BASE_SHA" --numstat -- firestarter/py32_dfu.py` produces no output.
    - The comment-line count did not rise in any touched file: for each file in the diff,
      `/usr/bin/grep -c '^[[:space:]]*#'` is less than or equal to its value at `BASE_SHA`.
    - The SUMMARY records the predicted red-leg count of six BEFORE the observed output, then the observed
      failing test names from `tests/test_cap03_ack_layout_parity.py`, and reconciles the two.
    - The SUMMARY names the three hand-fixed findings individually by rule and pristine-tree location —
      UP045 at `firestarter/eprom_info.py:97:24`, `:100:36`, `:103:26` — as D-09 requires.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check firestarter/ tests/</automated>
    <fails_when>the output is not exactly `All checks passed!`; in particular any line of the form `Found N errors.` or any `UP045`/`UP017`/`UP035`/`I001` finding line, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff format --check firestarter/ tests/</automated>
    <fails_when>the output is not `179 files already formatted`; in particular any line of the form `N files would be reformatted`, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python tools/check_mypy_watermark.py</automated>
    <fails_when>the `mypy errors:` line reports any count other than 35 — 37 specifically means the config edit is absent and the commits landed in the forbidden order; or the output lacks the literal line `OK: error count at watermark.`; or `checked N source files` reports N below 120; or exit is non-zero</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'Optional' firestarter/eprom_info.py</automated>
    <fails_when>the printed count is not `0`; the command exits 1 on a zero count, which is the expected signal here, so a printed number greater than zero is the failure</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'noqa: UP036' firestarter/main.py</automated>
    <fails_when>the printed count is not `1`, or the command exits 1 (count 0), which means the sweep removed the suppression that keeps the runtime version guard from being deleted</fails_when>
  </verify>
  <done>Both ruff gates are green at the config-supplied target with no flag overriding it, the three unfixable findings were resolved by hand and named individually, the formatter pass that CI checks separately has run, the mypy count is unmoved at 35, the codegen-generated modules and `tools/` and `py32_dfu.py` were not reached, and the six predicted CAP-03 failures are observed and recorded but not yet repaired.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the one gate the sweep reds — a source-text regex pinning the old return spelling (RESEARCH §2)</name>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_cap03_ack_layout_parity.py:190-300` — `_DECODE_ID_FRAME_DEF_RE`
      at `:198-202` and `_extract_decode_id_frame_body` at `:274-298`, the helper that raises the
      non-vacuity assertion when the regex matches nothing. Read both so you can see that the regex pins
      the definition's full signature as source text, and that the failure mode is a loud raise rather
      than a silent empty result.
    - `/workspaces/firestarter_app/firestarter/serial_comm.py` — both `_decode_id_frame` definitions (near
      `:335` and `:1112` pre-sweep) AS THEY NOW READ after Task 1, so the repaired regex is written against
      observed text rather than a predicted rewrite.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 2, subsection
      "The sweep breaks one existing test — plan for it": the bisect table proving attribution to
      `serial_comm.py` alone, the six affected test names, and the note that five of them are
      `requires_fw`-gated (so they show as skips in GitHub CI) while
      `test_planted_literal_index_is_detected` is not gated and reds in CI regardless.
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule" — the repair is a one-literal regex
      edit; it adds no comment and none may be added to justify it.
  </read_first>
  <files>firestarter_app/tests/test_cap03_ack_layout_parity.py</files>
  <action>
    One commit. One regex literal.

    `_DECODE_ID_FRAME_DEF_RE` matches `_decode_id_frame`'s definition line as source text, and its
    return-type alternative still spells the annotation the way the module was written before Task 1's
    sweep rewrote it. The regex now matches zero definitions, `_extract_decode_id_frame_body` raises its
    non-vacuity assertion, and every leg that depends on having a body to extract fails.

    Update the return-type alternative in that pattern so it matches the swept signature: the union form
    `LogMessage | None`, with `\|` escaped inside the regex and `\s*` tolerance around it, matching the
    surrounding style of the pattern (which already uses `\s*` between every token). Match the new form
    only rather than accepting both spellings: the old form cannot reappear in this tree without ruff
    flagging it as UP045 at the configured target, so an either/or alternative would pin a spelling that
    lint now forbids — a second stale claim in place of the one being removed.

    Change nothing else in this file: not the surrounding assertions, not the extractor, not any other
    pattern. Verify by running the file's twelve legs with the sibling firmware repo present so the five
    `requires_fw`-gated legs actually execute rather than skipping — a repair verified only against the
    ungated leg would leave the other five unproven.

    Then run the whole suite as the closing evidence. The control figure to reconcile against is the
    pristine main tree's `2359 passed`; after this plan the count must be unchanged, because this phase
    has added and removed no test so far. Run it in the background — it takes roughly eight minutes and a
    foreground timeout kill reads exactly like a failure.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'Optional' tests/test_cap03_ack_layout_parity.py` prints `0` — the stale spelling
      is gone from the pattern.
    - `/usr/bin/grep -c 'LogMessage' tests/test_cap03_ack_layout_parity.py` prints at least `1`, and the
      matching line is inside the `_DECODE_ID_FRAME_DEF_RE` definition.
    - `.venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q`
      reports `12 passed` — all twelve, not just the ungated one, with the sibling firmware repo present.
    - `git diff "$BASE_SHA" --numstat -- tests/test_cap03_ack_layout_parity.py` reports 1 insertion and
      1 deletion.
    - The full suite reports `0 failed` and no fewer than 2359 passed.
    - `.venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-report=term-missing
      --cov-fail-under=70` prints `Required test coverage of 70% reached.`
    - The SUMMARY states that the six failures were caused by one source-text regex in one file, names the
      bisect evidence that attributes them to `serial_comm.py` alone, and records that the gate failed
      loud rather than open.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q</automated>
    <fails_when>the tail summary line is not exactly `12 passed` — any `failed`, any `skipped` (which would mean the sibling firmware repo is absent and five legs did not actually run), or a count other than 12; or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check firestarter/ tests/</automated>
    <fails_when>the output is not exactly `All checks passed!`, or non-zero exit — the regex repair must not itself introduce a lint finding in a file inside the swept scope</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q</automated>
    <fails_when>any `FAILED` line; or the tail line reports fewer than 2359 passed; or the tail line is absent entirely, which means the run was killed by a timeout rather than completing — run this in the background, it takes about eight minutes</fails_when>
  </verify>
  <done>All twelve CAP-03 legs pass with the sibling repo present, the full suite is back at its pre-phase count with zero failures, the coverage floor is met, both ruff gates and the watermark gate are green, and the one-line cause of the six failures is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an automated rewriting tool → 15 files of shipped source | The only boundary that matters here. A tool is authorised to rewrite production code in bulk; the question is whether every rewrite is behaviour-preserving and whether anything the tool touched was something it should not have. |
| swept source → the gates that read source AS TEXT | `test_cap03_ack_layout_parity.py` and the codegen-drift gates assert on source text, not behaviour. A rewrite that is semantically identical can still break them, and one did. |
| swept source → the type-checker's error ceiling | Two fixes emit a 3.11-only symbol. Whether that is valid depends entirely on a value in a different file, landed by a different plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-186-02-A | Elevation of privilege (an autofix deleting a safety guard) | `firestarter/main.py:31-35` | high | mitigate | UP036's unsafe fix DELETES the runtime version guard. `--unsafe-fixes` is forbidden by instruction in every step, its absence from the command list is an acceptance criterion, and a grep leg asserts the suppression that holds UP036 off still exists after the sweep. |
| T-186-02-B | Tampering | `firestarter/messages.py`, `firestarter/frame_vectors.py` | high | mitigate | Both are codegen-generated and ID-only; a sweep reaching them would diverge from the two codegen-drift gates and from `beta-release.yml`'s normalisation step. Measured clean at the new target, and asserted by a `git diff --name-only` criterion naming both files explicitly. |
| T-186-02-C | Denial of service (a gate red for the wrong reason) | `tools/check_mypy_watermark.py` in CI | high | mitigate | Two fixes emit `datetime.UTC`, valid only at 3.11+. The `depends_on` edge encodes the ordering, Step 0 re-reads `target-version` before running anything, and the `<fails_when>` names 37 specifically as the signal that the commits landed in the forbidden order. |
| T-186-02-D | Tampering (a rewrite that silently changes behaviour) | 15 shipped modules | medium | mitigate | Every rewrite is an annotation spelling with no runtime effect except the two `datetime.UTC` sites, which are an alias of the same object. Proven by the full suite at its pre-phase count of 2359 with zero failures, plus the 70% coverage floor, rather than by inspection of the diff. |
| T-186-02-E | Repudiation (a source-text gate silently ceasing to assert) | `tests/test_cap03_ack_layout_parity.py` | medium | mitigate | The gate fails LOUD — its extractor raises a non-vacuity assertion when the pattern matches zero definitions, so it could not have quietly passed over an empty set. The repair is verified with the sibling firmware repo present so all twelve legs execute, not just the one that is ungated in CI. |
| T-186-02-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package, adds no dependency and changes no dependency specifier. It runs two already-installed tools over files already in the repository. No Package Legitimacy Audit table is required for this phase and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from planning-time measurement. Not silently dropped.

- **Phase-wide edge-probe accounting.** This plan authors no probe-derived `must_haves.truths`: none of
  the six surfaced rows belongs to it. FLOOR-01's `adjacency`, `empty` and `ordering` rows and FLOOR-03's
  `encoding` row are resolved explicitly in plan 186-03 (they are all properties of the agreement gate);
  FLOOR-03's `empty` row is resolved explicitly in plan 186-04; FLOOR-02's `unclassified` row is flagged
  unresolved in plan 186-01. Six surfaced, six accounted for, none dropped.
- **The figures in this plan are PREDICTIONS carried from measurement, not criteria.** `190 fixed`,
  `3 remaining`, `1 fixed` on the second pass, four files reformatted, 15 files in the diff, six red legs.
  All were measured this session at ruff 0.16.4 in a throwaway worktree. A ruff version bump invalidates
  every one of them. Where an observation disagrees, the observation wins and the disagreement is
  recorded — the acceptance criteria assert the END STATE (both gates green, count 35, 12 legs passing),
  which is what actually matters, rather than the intermediate counts.
- **`firestarter/py32_dfu.py`'s 14 pre-existing UP045 suppressions are deliberately NOT swept.** They
  pre-date this phase and ruff's autofix does not reach them, so they are not D-09's to absorb
  (orchestrator ruling 4). The honest consequence is that the new lint target has its principal effect
  switched off in one whole file — the shape D-09 rejected in the abstract. Recorded here and carried as
  the named residual gap in plan 186-04's rationale note; a task here would be scope creep.
- **Every `# noqa: BLE001` in this repository is inert** because `select` is `["E","F","I","UP"]`. It is
  adjacent to this sweep and deliberately untouched. Broadening `select` is an explicitly deferred idea.
- **Five of the six red legs are `requires_fw`-gated** and would show as skips in GitHub Actions, where no
  sibling firmware repo exists. Only `test_planted_literal_index_is_detected` reds in CI. That is why the
  repair is verified locally with the sibling present: CI alone would prove only one sixth of it.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan: no new files and no new symbols.

Modified in place by this plan:
- 13 modules under `firestarter_app/firestarter/` and 2 test modules under `firestarter_app/tests/` —
  type-annotation spellings only, plus two `datetime` alias rewrites and one import sort
- `firestarter_app/firestarter/eprom_info.py` — additionally, three hand-resolved parameter annotations
  in `prepare_detailed_eprom_data` and the removal of the import they orphaned
- `firestarter_app/tests/test_cap03_ack_layout_parity.py` — one regex literal inside
  `_DECODE_ID_FRAME_DEF_RE`

Deleted by this plan: five `# noqa: UP006` directives that sat on inner-bracket lines the fix collapsed
away, one now-unused `typing` import in `main.py`, and the orphaned inner comments in `eprom_info.py`'s
signature. All are the autofix and the hand fix removing text, which the standing hard rule permits.

Deliberately NOT modified, with the reason on the record:
- `firestarter_app/firestarter/messages.py`, `firestarter_app/firestarter/frame_vectors.py` — codegen
  output; measured clean at the new target, so the drift gates cannot diverge from this change
- `firestarter_app/firestarter/py32_dfu.py` — 14 pre-suppressed UP045 sites, out of scope by ruling 4
- everything under `firestarter_app/tools/` — outside CI's ruff scope entirely, and carrying its own
  unrelated findings that this phase does not adopt
- `firestarter_app/pyproject.toml` — `select` is NOT broadened; that is a deferred idea and its own phase
</artifacts_this_phase_produces>

<verification>
- `ruff check firestarter/ tests/` prints `All checks passed!` with no `--target-version` flag anywhere.
- `ruff format --check firestarter/ tests/` prints `179 files already formatted`.
- `tools/check_mypy_watermark.py` prints `checked 181 source files`, `mypy errors: 35 (watermark: 35)`,
  `OK: error count at watermark.`, exit 0 — on the py3.11 replica.
- `pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q` reports `12 passed`, with the sibling
  firmware repo present so no leg skips.
- The full suite reports `0 failed` and no fewer than 2359 passed.
- `pytest tests/ --cov=firestarter --cov-fail-under=70` prints `Required test coverage of 70% reached.`
- `git diff --name-only "$BASE_SHA"` contains 16 paths across both tasks and none under `tools/`,
  `.github/`, nor `messages.py`, `frame_vectors.py` or `py32_dfu.py`.
- No command in the SUMMARY's command list contains `--unsafe-fixes` or `--target-version`.
- The comment-line count went down or stayed equal in every touched file; it never rose.
</verification>

<success_criteria>
The new lint target has real consequences rather than nominal ones: 182 findings were absorbed, not
silenced, in a commit separate from the config change. The three findings the tool could not fix were
fixed by hand and named individually by rule and location, rather than swept or suppressed. Both ruff
gates are green — including the formatter pass that the fix alone does not satisfy and that CI checks
separately. The one existing gate the rewrite reds was predicted from measurement before it was observed,
attributed to a single line, and repaired in the same wave. The mypy count did not move, which is only
possible because the commits landed in the measured-correct order.
</success_criteria>

<output>
Create `.planning/phases/186-the-python-floor-before-the-eol/186-02-SUMMARY.md` when done
</output>
