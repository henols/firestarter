---
phase: 186-the-python-floor-before-the-eol
plan: 03
type: execute
wave: 3
depends_on: ["186-02"]
files_modified:
  - firestarter_app/tests/test_python_floor_agreement.py
  - firestarter_app/.planning/codebase/STACK.md
autonomous: true
requirements: [FLOOR-01, FLOOR-02, FLOOR-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fail-closed check asserts that all four floor statements name the same version: `project.requires-python` and `tool.ruff.target-version` and `tool.mypy.python_version` from `pyproject.toml`, and every `python-version:` pin in `.github/workflows/*.yml`. Including CI is the deliberate part — it is the only clause that catches CI later moving to a different interpreter and silently leaving the floor untested again, which is the exact mechanism that let this divergence survive from 2026-05-27 to now (D-04)."
    - "The ruff target token carries a VARIABLE-WIDTH minor: `py39` is 3.9 and `py311` is 3.11. The normaliser splits after the first digit — `py`, then major, then the rest as minor — and never at a fixed two-character minor. A fixed-width parse would silently make the gate compare 3.9 against 3.1 and pass on a real divergence. A dedicated unit leg over `py39`, `py310` and `py311` asserts it. (Probe row FLOOR-01/`adjacency`, resolved explicit.)"
    - "The gate refuses to pass over nothing, in three tiers: `pyproject.toml` must exist and parse, the `.github/workflows/` directory must exist, and the pin inventory must hold at least `_WORKFLOW_PIN_FLOOR = 3` entries — a floor RAISED when a fourth workflow gains a pin and never lowered to make a run pass. This repository has a recorded checker (`check_permitted_claims.py`) that resolved to the wrong directory, scanned nothing and exited 0; every path here resolves from `Path(__file__).resolve().parent.parent`, never from cwd. (Probe row FLOOR-01/`empty`, resolved explicit.)"
    - "Workflow files are iterated in `sorted()` order, so a multi-file disagreement report is byte-stable across runs and two files whose pins compare equal are still reported separately, in declared order, rather than merging into one line. Filesystem glob order is not stable and an unsorted report would make the same divergence read differently on two machines. (Probe row FLOOR-01/`ordering`, resolved explicit.)"
    - "Equality is defined AFTER normalisation, never on raw text: `\">=3.11\"`, `\"py311\"`, `\"3.11\"` and `'3.11'` are four spellings of one version. And `requires-python` is a SPECIFIER, not a version — the gate asserts the shape `^>=\\d+\\.\\d+$` and fails closed on anything else, so `\">=3.11,<4\"` or `\"~=3.11\"` cannot slip through a naive prefix strip. (Probe row FLOOR-03/`encoding`, resolved explicit.)"
    - "The gate is a `tests/test_*.py` using stdlib `tomllib` and a line regex for the YAML. It adds NO dependency: neither `pyyaml` nor `tomli` nor `toml` enters this project, and `tomllib` is stdlib by the project's own new floor. `tests/test_runtime_dependencies.py` is the live precedent, whose own docstring says reaching for `toml`/`tomli` would add a dependency in the act of asserting none was added — the identical argument applies to `pyyaml` here."
    - "The gate lives in `tests/` rather than `tools/` because `tests/` is inside `ruff check`, `ruff format --check` and the mypy watermark scope, and `firestarter_app/tools/` is outside every one of them. A claim-hygiene milestone puts its own gate inside the gates. It is also the only one of the three candidate shapes that needs no `ci.yml` edit, so the gate cannot fall out of agreement with a file it reads."
    - "The new file carries ZERO `#` comment lines. Every explanation is a module, function or attribute docstring — the shape `tests/test_runtime_dependencies.py` already uses across 119 lines with no comment in any of them. `/workspaces/CLAUDE.md` § \"Source code comments — hard rule\" is not overridable by this plan, and every other analog in this tree is heavily commented, so their commenting style is explicitly NOT the pattern to copy."
    - "The gate is written 3.11-clean from its first line — builtin generics, no `typing.Optional`, no `from __future__ import annotations` — because it lands inside the swept scope and would otherwise become its own lint finding and count against the mypy watermark."
    - "The app repo's own `.planning/codebase/STACK.md` states the corrected floor AND carries a one-line pointer to the meta-repo rationale note, because the person who next edits `pyproject.toml` works in the app repo and does not have the meta repo's `.planning/notes/` (D-07)."
    - "The whole thing is proven at the floor by the project's designated instrument — `tools/ci_replica_venv.sh` reused, never `--refresh`ed — ending in `CI-REPLICA: PASS` with its `INTERPRETER:` line naming a 3.11, not the devcontainer's 3.12 (success criterion 2)."
  artifacts:
    - path: "firestarter_app/tests/test_python_floor_agreement.py"
      provides: "The four-way fail-closed floor agreement gate, its normalisation helpers, its non-vacuity legs and its planted-file fail-closed legs"
      contains: "_WORKFLOW_PIN_FLOOR"
    - path: "firestarter_app/.planning/codebase/STACK.md"
      provides: "The corrected floor claim for the app-repo reader, plus the pointer to the meta-repo rationale note"
      contains: "python-floor-decision.md"
  key_links:
    - from: "firestarter_app/tests/test_python_floor_agreement.py"
      to: "firestarter_app/pyproject.toml"
      via: "tomllib parse anchored at Path(__file__).resolve().parent.parent, reading requires-python, tool.ruff.target-version and tool.mypy.python_version"
      pattern: "tomllib"
    - from: "firestarter_app/tests/test_python_floor_agreement.py"
      to: "firestarter_app/.github/workflows/"
      via: "a line regex over sorted(glob('*.yml')) extracting every python-version pin, with a >= 3 count floor"
      pattern: "python-version"
    - from: "firestarter_app/.planning/codebase/STACK.md"
      to: ".planning/notes/python-floor-decision.md"
      via: "a one-line prose pointer naming the meta repository and the note path, for a reader who does not have the meta repo checked out"
      pattern: "python-floor-decision"
---

<objective>
Install the check that stops the four floor statements drifting apart again, correct the app repo's own
stack record, and prove the whole phase at the floor with the project's designated py3.11 instrument.

Purpose: after plans 186-01 and 186-02, nothing in `tests/` asserts any of the four floor values —
measured, and precisely the gap D-04 exists to close. The divergence this phase repairs survived from
2026-05-27 to now because no gate could see it: GATE-01 catches mypy *rejecting* a value, not
`requires-python` drifting away from a value mypy happily accepts. Including CI's executed pin as the
fourth clause is what catches the other half of that mechanism — CI silently moving off the floor and
leaving it untested again.

This plan expands out from the proven slice in the last direction that matters: making the property
self-enforcing rather than merely true today. Its shape was settled by the orchestrator against the two
alternatives RESEARCH § 3 costed.

Output: one new test module with zero comments and seven legs, a corrected app-repo stack record with a
pointer to the rationale note, and a `CI-REPLICA: PASS` measured on a 3.11 interpreter.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md
@.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md
@.planning/phases/186-the-python-floor-before-the-eol/186-PATTERNS.md
@/workspaces/CLAUDE.md

Note on omissions: `.planning/PROJECT.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` are
deliberately NOT referenced — see plan 186-01's identical note and the STATE.md size hazard it records.

**Repo mechanics.** Both files here live in `/workspaces/firestarter_app/`, a separate git repository
tracked as a submodule of `/workspaces`. Note that `firestarter_app/.planning/codebase/` is the APP
repo's own planning directory, tracked by the app repo — it is not the meta repo's `.planning/` and must
never be confused with it or removed. Commit INSIDE the app repo, on branch
`gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. Do not push. Plan 186-04 owns every
meta-repo edit and the gitlink advance.
</context>

<environment_contract>
Identical to plans 186-01 and 186-02, and it binds every command below:

- `/workspaces/firestarter_app/.venv/ci-replica/bin/{python,ruff}` — Python 3.11.16, numpy-free. Never
  `--refresh` the replica venv: `resolve_base_python` probes `python3.11` on PATH, then
  `/home/vscode/.local/bin/python3.11`, then bare `python3`; the first two are absent in this container
  today, so a refresh would silently build a 3.12 venv and only print an easily-missed
  `INTERPRETER-DIVERGENCE:` line. The existing venv is already correct.
- `/usr/bin/grep`, never bare `grep` — the container's `grep` is ugrep and honours `.gitignore`.
- Every pytest command passes `-o addopts=""`.
- `bash tools/ci_replica_venv.sh` runs five legs including the full coverage suite and takes well over
  ten minutes. Run it in the BACKGROUND. A foreground timeout kill reads exactly like a failure.
- After any editable install into a fresh venv, print `firestarter.__file__` before trusting a result —
  an editable install does not follow a worktree. The existing replica venv already resolves to
  `/workspaces/firestarter_app/firestarter/__init__.py`.
</environment_contract>

<failing_direction_convention>
Every `<automated>` command below is a green-direction command with a `<fails_when>` naming an observable
signal. Specific to this plan: a brand-new test file is exactly the kind of artefact that can pass while
asserting nothing, so the gate's own non-vacuity is verified two ways — by its named count-floor leg, and
by two planted-file legs that must RAISE. A leg that passes because it found nothing is the failure mode
being designed against, not a theoretical one: this repository has a recorded instance of it.
</failing_direction_convention>

<!-- planner-discipline-allow: from __future__ import annotations -->
<!-- planner-discipline-allow: Optional -->
<!-- planner-discipline-allow: 3.9 -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four-way floor agreement gate — tomllib, a line regex, and three tiers of non-vacuity (D-04, FLOOR-01, FLOOR-02)</name>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_runtime_dependencies.py` — ALL 119 lines, first. This is the
      primary analog and the template: the module-docstring idiom naming the requirement and saying
      "asserted by test rather than by a sentence"; the `tomllib`-over-`toml`/`tomli` justification
      paragraph; the repo-root resolution helper at `:29-35` with the `check_permitted_claims.py`
      wrong-directory defect cited as its reason; the non-vacuity guards at `:51-62`; and the separate,
      explicitly-named vacuity leg at `:112-119`. Note especially that it carries ZERO `#` comments across
      all 119 lines — it is the only analog in this tree that already complies with the standing hard rule.
    - `/workspaces/firestarter_app/tests/test_py32_packaging.py:63-131` and `:203-210` — the module-global
      compiled-regex idiom, the one-helper-shared-by-the-real-leg-and-the-planted-leg structure, and the
      `monkeypatch.setattr` on a module-global path that makes the fail-closed leg possible. Copy that
      STRUCTURE; do not copy its `#` comments and do not copy its `from __future__ import annotations`.
    - `/workspaces/firestarter_app/tests/test_scan_paths_resolve.py:90-110` — the count-floor
      non-vacuity shape (`assert len(...) >= _FLOOR` with a message naming the count, the expectation and
      the likely cause). This is the shape the workflow-pin inventory needs.
    - `/workspaces/firestarter_app/tests/test_revision_constants_parity.py:329`, `:342-357`, `:378-449` —
      the only genuine multi-source agreement gate in this tree: a fail-closed read of source 1, a regex
      extract-and-normalise of source 2, a single normalisation helper isolated for its own testability,
      and errors COLLECTED and reported together rather than short-circuited on the first.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-PATTERNS.md` § "`tests/test_python_floor_agreement.py`"
      in full — the verbatim excerpts, the failure-message house convention, and the explicit table of
      what to copy and what NOT to copy from each analog.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 3 — the
      complete workflow-pin inventory (exactly three, all `'3.11'`, and two further workflow files that
      carry no pin at all and must not be required to), the four spellings table, ruff's closed
      `--target-version` enum, and the specifier-not-a-version warning.
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule" — before writing a single line.
  </read_first>
  <files>firestarter_app/tests/test_python_floor_agreement.py</files>
  <behavior>
    Write the assertions before the implementation they exercise, and run them to see each one fail for
    the right reason before it passes. The seven legs and what each must prove:

    - `test_all_four_floor_statements_name_the_same_version` — the four normalised values are equal. On
      disagreement the message names ALL FOUR values with the file each came from, one per line, so a
      reader sees which one drifted without opening anything.
    - `test_ruff_target_minor_is_not_fixed_width` — `_normalise_ruff_target` maps `py39` to `3.9`,
      `py310` to `3.10` and `py311` to `3.11`. This is the only genuinely subtle bug in the gate: a
      fixed-width minor makes `py311` read as 3.1 and the comparison silently wrong.
    - `test_requires_python_is_a_bare_lower_bound` — `_normalise_requires_python` accepts `>=3.11` and
      RAISES on `>=3.11,<4`, on `~=3.11` and on a bare `3.11`. A specifier is not a version, and a naive
      prefix strip would pass all three.
    - `test_workflow_pin_inventory_is_non_vacuous` — the inventory holds at least `_WORKFLOW_PIN_FLOOR`
      entries. The floor is a module constant, raised when a fourth workflow gains a pin, never lowered.
    - `test_gate_fails_closed_on_a_planted_pyproject_with_no_floor_statements` — with the module-global
      pyproject path monkeypatched at a planted file that has no `[project]` or no `[tool.mypy]`, the
      SHARED helper raises rather than returning an empty mapping that would compare equal to itself.
    - `test_gate_fails_closed_on_an_empty_planted_workflows_dir` — with the module-global workflows path
      monkeypatched at an empty `tmp_path`, the shared helper raises. An absent or emptied directory is
      the `check_permitted_claims.py` failure mode exactly.
    - `test_a_disagreeing_planted_pyproject_names_all_four_values` — a planted pyproject whose three
      values disagree produces an `AssertionError` whose text contains all four source names. This proves
      the report is diagnostic rather than a bare boolean, which is the whole point of collecting the
      disagreements instead of short-circuiting.
  </behavior>
  <action>
    Create `/workspaces/firestarter_app/tests/test_python_floor_agreement.py`. One commit.

    SHAPE. Module-level constants first, then helpers, then the seven legs. `_APP_ROOT` resolves as
    `Path(__file__).resolve().parent.parent` — with `.resolve()`, following the newer analog rather than
    the older one that omits it — and `_PYPROJECT` and `_WORKFLOWS_DIR` derive from it. All three are
    module GLOBALS, not locals inside functions: that is what makes the two planted-file legs possible via
    `monkeypatch.setattr`. `_WORKFLOW_PIN_FLOOR = 3`. One compiled module-level regex for the YAML line,
    matching an optionally-quoted dotted version on a `python-version:` line; one for the
    `requires-python` bare-lower-bound shape.

    HELPERS, each with a docstring stating what it guarantees and what it refuses. `_parsed_pyproject()`
    asserts the file exists, parses it with stdlib `tomllib`, and asserts the three keys it needs are
    present before returning anything. `_workflow_python_versions()` asserts the directory exists, globs
    `*.yml` through `sorted()`, extracts every matching pin, and asserts the count is at least
    `_WORKFLOW_PIN_FLOOR`. Two workflow files legitimately carry no pin at all, so the gate must NOT
    require one per file — only the total floor. `_normalise_ruff_target()` and
    `_normalise_requires_python()` are separate, individually testable functions, isolated exactly as the
    parity gate isolates its own cross-spelling normaliser. `_floor_statements()` returns the four
    normalised values keyed by a human-readable source name, which is what makes the disagreement message
    diagnostic.

    THE ONE HELPER RULE. Each real leg and its planted-file counterpart must call the SAME helper, so the
    fail-closed leg proves the real leg's own code path fails closed — not a parallel one written to fail.
    That is the structural pattern in both analogs and it is the part worth copying most.

    FAILURE MESSAGES, house convention: `assert <cond>, (f"...")` with a parenthesised multi-line message
    that names the offending file, names the offending value with `!r`, and says what would otherwise have
    been vacuously true. Collect multiple disagreements into a list and report them together rather than
    short-circuiting on the first. No custom exception class and no `pytest.fail()` — neither appears
    anywhere in this family.

    HARD CONSTRAINTS, none of them negotiable:
    - ZERO `#` comment lines in the file. Every explanation goes in a module, function or attribute
      docstring. Every analog you are copying from is heavily commented; that commenting is NOT the
      pattern. `/workspaces/CLAUDE.md`'s rule is not overridable by this plan.
    - No new dependency. `tomllib` is stdlib at the project's own new floor; `pyyaml`, `tomli` and `toml`
      must not appear in the file, in `pyproject.toml`, or anywhere else.
    - 3.11-clean from the first line: builtin generics, union syntax, no `typing.Optional`, no
      `from __future__ import annotations`. This file lands inside `ruff check` and the mypy watermark
      scope, so a stale spelling here becomes its own lint finding and counts against the ceiling of 35.
    - `-> None` on every test function, following the majority of this family.

    DOCSTRING CONTENT. The module docstring opens by naming FLOOR-01 and FLOOR-02 and saying the property
    is asserted by test rather than by a sentence. It then carries three paragraphs: why stdlib `tomllib`
    rather than `toml`/`tomli` (and, for the YAML half, a line regex rather than `pyyaml`) — adding a
    dependency in the act of asserting four configuration values agree would be the same defect this
    milestone removes, and after D-01 `tomllib` is stdlib by the project's own declared floor; the
    provenance, which unlike Phase 181's honest "no precedent" DOES have one — cite
    `tests/test_runtime_dependencies.py` and `186-PATTERNS.md` by name; and the honest caveat that no
    existing test in this tree compares a value between `pyproject.toml` and a GitHub workflow, nor TOML
    against YAML, so the structure is copied while the pairing is new. Use ASCII `--` for em-dashes,
    matching the primary analog.
  </action>
  <acceptance_criteria>
    - The file exists at `firestarter_app/tests/test_python_floor_agreement.py` and
      `/usr/bin/grep -c '^[[:space:]]*#' tests/test_python_floor_agreement.py` prints `0`.
    - `/usr/bin/grep -n 'import tomllib' tests/test_python_floor_agreement.py` prints at least one line, and
      `/usr/bin/grep -c 'yaml\|tomli\b\|import toml$' tests/test_python_floor_agreement.py` prints `0`.
    - `/usr/bin/grep -c 'from __future__\|Optional\|typing.Dict' tests/test_python_floor_agreement.py`
      prints `0`.
    - `/usr/bin/grep -n '_WORKFLOW_PIN_FLOOR = 3' tests/test_python_floor_agreement.py` prints at least one
      line, and that line is at module scope rather than inside a function body.
    - `/usr/bin/grep -c 'sorted(' tests/test_python_floor_agreement.py` prints at least `1`, and the
      match is on the workflow glob — a stable iteration order for the report.
    - `/usr/bin/grep -n 'Path(__file__).resolve().parent.parent' tests/test_python_floor_agreement.py`
      prints at least one line, and no more than one — one anchor helper, resolved, not a cwd-relative
      path and not repeated per target.
    - `.venv/ci-replica/bin/python -m pytest tests/test_python_floor_agreement.py -o addopts="" -q`
      reports `7 passed`.
    - The two planted-file legs and the disagreement leg were each observed RED before they were green:
      the SUMMARY carries, for each of the three, the captured failure output from a run made before the
      corresponding guard existed, or the captured output of deliberately weakening the guard once and
      restoring it. A guard that has never been seen to fail proves nothing.
    - `/usr/bin/grep -c 'pyyaml\|tomli' pyproject.toml` prints `0` — the gate added no dependency in the act of
      asserting the configuration agrees.
    - `.venv/ci-replica/bin/ruff check firestarter/ tests/` prints `All checks passed!` and
      `ruff format --check firestarter/ tests/` reports `180 files already formatted` — one more than
      before, because this file joined the scope.
    - `.venv/ci-replica/bin/python tools/check_mypy_watermark.py` reports `mypy errors: 35 (watermark: 35)`
      — the new file contributed no type error and did not consume the ceiling.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_python_floor_agreement.py -o addopts="" -q</automated>
    <fails_when>the tail summary line is not exactly `7 passed` — any `failed`, any `error`, any `skipped`, or a count other than 7; or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '^[[:space:]]*#' tests/test_python_floor_agreement.py</automated>
    <fails_when>the printed count is anything other than `0`; the command exits 1 on a zero count, which is the expected signal here, so any printed number greater than zero is the failure</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check firestarter/ tests/</automated>
    <fails_when>the output is not exactly `All checks passed!`, or non-zero exit — the new file must be clean at the configured target from its first commit</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff format --check firestarter/ tests/</automated>
    <fails_when>the output is not `180 files already formatted`; a count of 179 means the new file is not inside the formatter's scope, and any `N files would be reformatted` line means it is not formatted</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python tools/check_mypy_watermark.py</automated>
    <fails_when>the `mypy errors:` line reports a count other than 35, or the output lacks the literal line `OK: error count at watermark.`, or `checked N source files` reports N below 120, or non-zero exit</fails_when>
  </verify>
  <done>A seven-leg, zero-comment, dependency-free gate asserts the four floor statements agree after normalisation; its two planted-file legs and its disagreement leg have each been observed failing before passing; the ruff and format and watermark gates are all green with the new file inside their scope.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the app repo's own stack record and point it at the rationale note (D-07, D-11, FLOOR-03)</name>
  <read_first>
    - `/workspaces/firestarter_app/.planning/codebase/STACK.md` — the WHOLE file. The three floor claims
      sit at `:7` (under Languages, quoting `requires-python` directly), `:12` (under Runtime, naming both
      a dev-machine runtime and a minimum) and `:57` (under Platform Requirements → Development). Read the
      surrounding sections before editing: each of the three states the floor in a different sentence
      shape and a blanket find-and-replace would produce three ungrammatical lines.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 4, the
      "Verified — CONTEXT.md is correct" table row for this file, which quotes all three lines verbatim as
      they read today.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-CONTEXT.md` D-07 — why this
      file gets the pointer at all: the meta repo's `.planning/notes/` is invisible to the app-repo editor,
      who is exactly the person who next opens `pyproject.toml`.
  </read_first>
  <files>firestarter_app/.planning/codebase/STACK.md</files>
  <action>
    One commit, in the APP repo. Note this is `firestarter_app/.planning/`, the app repo's own tracked
    planning directory — not the meta repo's. Do not touch the meta repo here; plan 186-04 owns it.

    Correct all three floor claims to 3.11. At `:7`, the parenthetical quotes the `pyproject.toml` value
    directly, so both the prose version and the quoted specifier move. At `:12`, the line names a current
    dev-machine runtime alongside the minimum; only the minimum is this phase's to change, and if the
    stated dev runtime is stale that is a separate observation for the SUMMARY, not an edit here. At `:57`
    it is a bare version claim.

    Then add the D-07 pointer: one line stating that the floor decision, its evidence and the standing
    rule for the next move are recorded in the meta repository `henols/firestarter_prom` at
    `.planning/notes/python-floor-decision.md`. Name the repository as well as the path — a reader with
    only the app repo checked out must be able to find it, and a bare relative path would resolve to
    nothing for them. Put it adjacent to the `:7` claim, where someone reading the floor sees it.

    This is a `.md` document, not source, so prose is exactly what belongs here — the source-comment hard
    rule does not reach it. That asymmetry is the whole reason D-07 splits the record across two files.

    The note itself does not exist yet: plan 186-04 creates it, and plan 186-04's acceptance criteria
    include verifying that the path this pointer names now resolves. Do not create it here and do not
    weaken the pointer to a conditional.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c '3\.9' .planning/codebase/STACK.md` prints `0`.
    - `/usr/bin/grep -c '3\.11' .planning/codebase/STACK.md` prints at least `3` — all three claim sites
      moved, not one.
    - `/usr/bin/grep -n 'requires-python = ">=3.11"' .planning/codebase/STACK.md` prints at least one line —
      the quoted specifier at `:7` agrees with the file it quotes.
    - `/usr/bin/grep -n 'python-floor-decision.md' .planning/codebase/STACK.md` prints at least one line, and
      that same line also contains `firestarter_prom`.
    - `git diff --numstat "$BASE_SHA" -- .planning/codebase/STACK.md` shows no more than 5 changed lines —
      three corrections and one added pointer, not a rewrite of the document.
    - The SUMMARY records whether the dev-machine runtime named at `:12` is still accurate, as an
      observation, without editing it.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '3\.11' .planning/codebase/STACK.md</automated>
    <fails_when>the printed count is below `3`, or the command exits 1 (count 0), which means no claim site moved</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -n 'python-floor-decision.md' .planning/codebase/STACK.md</automated>
    <fails_when>no line is printed (exit 1), or the printed line does not also contain `firestarter_prom` — a bare relative path resolves to nothing for a reader who has only the app repo</fails_when>
  </verify>
  <done>All three floor claims in the app repo's stack record name 3.11, the quoted specifier matches the file it quotes, and one line tells an app-repo-only reader which repository and which path carry the reasoning.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the phase at the floor with the designated instrument, not in the devcontainer (success criterion 2)</name>
  <read_first>
    - `/workspaces/firestarter_app/tools/ci_replica_venv.sh` — `:1-50` (why a numpy-free 3.11 interpreter
      is the only trustworthy local measurement, and the explicit list of what this script deliberately
      does NOT mirror), `:111-140` (`resolve_base_python`, which is why `--refresh` is forbidden today,
      and the `INTERPRETER:` / `INTERPRETER-DIVERGENCE:` lines), and `:330-362` (the five-leg summary block
      and the `CI-REPLICA: PASS` / `CI-REPLICA: FAIL (legs:...)` verdict lines).
    - `/workspaces/firestarter_app/.github/workflows/ci.yml:76-92` and `:104-128` — the four gate steps of
      the `ci` job, and the `ci-py32` job, so the SUMMARY can state honestly which CI steps the replica
      covers and which it does not.
    - `/workspaces/.planning/phases/186-the-python-floor-before-the-eol/186-RESEARCH.md` § 6 (the
      verification table, with each command's cwd and its failure signal) and § 7 P-1 (the `--refresh`
      hazard, measured) and P-6b (the full-suite result on the complete change).
  </read_first>
  <files>firestarter_app/tools/ci_replica_venv.sh</files>
  <action>
    NO FILE IS EDITED IN THIS TASK. It runs the project's designated measurement instrument and records
    what it says. `files_modified` names the script because that is what is executed, not changed; if this
    task produces a diff in any tracked file, something went wrong and the diff is the finding.

    Run `bash tools/ci_replica_venv.sh` from `/workspaces/firestarter_app`, WITHOUT `--refresh`. In the
    BACKGROUND: it runs five legs including the full coverage suite and takes well over ten minutes, and a
    foreground timeout kill would be indistinguishable from a failure.

    Read the `INTERPRETER:` line in the summary block FIRST, before the verdict. It must name a 3.11. If
    an `INTERPRETER-DIVERGENCE:` line appears, the measurement was taken on the wrong interpreter and
    success criterion 2 is not met by it, whatever the verdict line says — stop and report rather than
    recording a 3.12 measurement as proof of a 3.11 floor. That line is easy to scroll past, which is
    precisely why it is checked first.

    Then run the residual-claim scan, the one check no gate performs: over TRACKED files only, excluding
    the golden, data, datasheet and image trees, search for any surviving statement of the old floor. Two
    hits are expected and correct — `tools/check_mypy_watermark.py:68` and
    `tests/test_check_mypy_watermark.py:106` — and both are mypy's OWN output quoted as fixture text, not
    a claim about this project's floor. D-11 says leave them, and measurement confirmed D-11 is right
    about both. Any THIRD hit is a surface the phase missed and must be dispositioned, not waved through.

    Record in the SUMMARY, honestly and by name, what the replica covers and what it does not: it
    deliberately does not mirror the two codegen-drift gates, the two catalog-validity steps, the
    `firestarter --help` smoke step, or the `ci-py32` job. Those still need real CI. Stating the boundary
    is what makes this an honest mirror rather than an implied full-CI claim — the script's own header
    holds itself to the same standard.
  </action>
  <acceptance_criteria>
    - The `ci_replica_venv.sh` output's final line is `CI-REPLICA: PASS`, and every one of the five
      `Leg N (...): exit 0` lines reports 0.
    - The summary block's `INTERPRETER:` line names a `3.11` version, and no `INTERPRETER-DIVERGENCE:`
      line appears anywhere in the output.
    - Leg 5's pytest output contains `Required test coverage of 70% reached.`
    - The tracked-file residual scan for the old floor returns exactly two hits, and both are the mypy
      fixture sites named above. Any third hit is reported as a finding and dispositioned in the SUMMARY.
    - `git status --porcelain` in the app repo names no modified tracked file at the end of this task.
    - The SUMMARY states which CI steps the replica does NOT mirror, by name.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh</automated>
    <fails_when>the final line is not `CI-REPLICA: PASS`; or any line of the form `Leg N exit code:` reports a non-zero value; or the output contains `INTERPRETER-DIVERGENCE:`; or the `INTERPRETER:` line does not name a 3.11 — a 3.12 measurement does not satisfy success criterion 2 regardless of the verdict. Run this in the background; it takes well over ten minutes and a foreground timeout kill is not a failure signal.</fails_when>
    <automated>cd /workspaces/firestarter_app && TRACKED=$(git ls-files) && printf '%s\n' "$TRACKED" | /usr/bin/grep -vE '^(tests/golden/|firestarter/data/|datasheets/|images/)' | tr '\n' '\0' | xargs -0 /usr/bin/grep -lIE '3\.9|py39|python3\.9|\(3, 9\)' | sort</automated>
    <fails_when>the printed list is not exactly the two lines `tests/test_check_mypy_watermark.py` and `tools/check_mypy_watermark.py`. Both hold mypy's own output as fixture text, which D-11 says to leave. A third path is a floor surface the phase missed; fewer than two means a deliberate fixture was damaged. Note `xargs` exits non-zero when the final `grep` finds nothing in a batch, so read the printed paths, not the exit code.</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain</automated>
    <fails_when>any line begins with ` M` or `M ` — this task edits nothing, so a modified tracked file means a measurement step wrote where it should only have read. Two untracked `datasheets/*.pdf` entries are pre-existing and expected.</fails_when>
  </verify>
  <done>The whole phase's app-repo change is green on all five replica legs, measured on a 3.11 interpreter with no divergence stamp, the coverage floor is met, exactly two deliberate fixture sites still quote the old version and both are named, nothing was modified by the act of measuring, and the replica's honest boundary is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| four configuration files → one asserted invariant | The new gate is the first thing in this repository that reads across `pyproject.toml` and `.github/workflows/*.yml` together. Everything it claims rests on it having actually read both. |
| a brand-new test → the suite's credibility | A test that passes without asserting anything is worse than no test: it converts an unmonitored property into a falsely-monitored one. This repository has a recorded instance of exactly that. |
| the devcontainer's 3.12 → a claim about a 3.11 floor | Measuring in the wrong interpreter and reporting the result as proof is the failure mode success criterion 2 was written against, and it has broken beta CI before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-186-03-A | Repudiation (a gate that asserts nothing and passes) | `tests/test_python_floor_agreement.py` | high | mitigate | Three tiers of non-vacuity — file/directory existence, parsed-result non-emptiness, and a named `>= _WORKFLOW_PIN_FLOOR` count leg — plus two planted-file legs that must RAISE through the SAME helper the real legs call, plus an acceptance criterion requiring each of the three to have been OBSERVED red before it was green. |
| T-186-03-B | Spoofing (a comparison that silently compares the wrong numbers) | `_normalise_ruff_target` | high | mitigate | Ruff's token carries a variable-width minor; a fixed-width parse makes `py311` read as 3.1 and the four-way comparison then passes on a real divergence while appearing to work. Isolated into its own function with its own unit leg over all three live token shapes. |
| T-186-03-C | Tampering (a specifier smuggling a different constraint past a prefix strip) | `_normalise_requires_python` | medium | mitigate | `requires-python` is a specifier, not a version. The gate asserts the bare-lower-bound shape and RAISES on a comma-joined range, a compatible-release operator or an unprefixed version, each asserted by its own case. |
| T-186-03-D | Tampering | the project's dependency set | medium | mitigate | A four-way config gate is the obvious place to reach for `pyyaml` and `tomli`. Both are forbidden by instruction, absent by acceptance criterion from the new file AND from `pyproject.toml`, and the alternative is stdlib-only with a live in-repo precedent that argues the same point for the same reason. |
| T-186-03-E | Information disclosure | the new test module and the corrected stack record | low | accept | Both contain version numbers, file paths relative to the repository, and a public repository name. No secret, credential or absolute host path is written. |
| T-186-03-F | Denial of service (a false proof of the floor) | `tools/ci_replica_venv.sh` | high | mitigate | The script does not fail on a wrong base interpreter — it prints an easily-missed divergence stamp and carries on. The `INTERPRETER:` line is checked BEFORE the verdict, `--refresh` is forbidden, and a 3.12 measurement is explicitly declared not to satisfy the criterion regardless of the verdict line. |
| T-186-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no dependency and installs nothing new. `ci_replica_venv.sh` leg 1 reinstalls the project's already-pinned test extra into the existing venv; no new distribution enters the project. RESEARCH.md records that no Package Legitimacy Audit table is required for this phase and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from the spec-less edge probe and from planning-time measurement. Not silently dropped.

- **Four of the phase's six probe rows are resolved EXPLICITLY in this plan's `must_haves.truths` above**,
  because all four are properties of the agreement gate rather than of the configuration it guards:
  FLOOR-01/`adjacency` (the variable-width ruff minor), FLOOR-01/`empty` (the three non-vacuity tiers),
  FLOOR-01/`ordering` (the sorted glob and the stable report), and FLOOR-03/`encoding` (equality defined
  after normalisation, and the specifier-shape assertion). The probe's category names are a generic
  classifier and two of these four needed translating to fit; the translations are stated in the truths
  themselves so a reader can judge them rather than take them on trust. Phase-wide: FLOOR-03/`empty` is
  resolved explicitly in plan 186-04, FLOOR-02/`unclassified` is flagged unresolved in plan 186-01. Six
  surfaced, six accounted for, none dropped, and no row needed a `verification: backstop` marker.
- **`_WORKFLOW_PIN_FLOOR = 3` is a floor, and floors only go up.** Three pins exist today —
  `ci.yml:53`, `ci.yml:112`, `beta-release.yml:52` — and `publish.yml` and `release.yml` legitimately
  carry none. If a future workflow adds a pin the constant is raised; it is never lowered to make a run
  pass, because lowering it is how a count-floor stops detecting an emptied inventory.
- **`ci-py32` runs the same interpreter as `ci`** — it is isolated in what it installs (`.[test,py32]`
  versus `.[test]`), not in what Python it runs. So asserting all three pins equal the floor is correct
  today and there is no legitimate-divergence exception to carve out. If a future job genuinely needs a
  different interpreter, this gate is exactly what will force that to be a deliberate, visible decision.
- **The `180 files already formatted` figure is a PREDICTION** — 179 today plus this one new file. If the
  observed number differs, the observation wins and the SUMMARY records why; what matters is that the
  formatter reports zero files needing reformatting.
- **The replica is not full CI, and the SUMMARY must say so by name.** It deliberately does not mirror
  the two codegen-drift gates, the two catalog-validity steps, the `firestarter --help` smoke step, or the
  `ci-py32` job. None of them is affected by this phase's change — the codegen-generated modules were
  measured untouched by the sweep — but claiming a full-CI equivalence the script itself disclaims would
  be a new unproven claim in a milestone about removing them.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan (NEW paths and NEW symbols; a source-grounding drift pass must not flag them as
missing from prior tree state):
- `firestarter_app/tests/test_python_floor_agreement.py`
- module constants in that file: `_APP_ROOT`, `_PYPROJECT`, `_WORKFLOWS_DIR`, `_WORKFLOW_PIN_FLOOR`,
  `_PYTHON_VERSION_RE`, `_REQUIRES_PYTHON_RE`
- helper functions in that file: `_parsed_pyproject`, `_floor_statements`, `_workflow_python_versions`,
  `_normalise_ruff_target`, `_normalise_requires_python`
- test functions in that file: `test_all_four_floor_statements_name_the_same_version`,
  `test_ruff_target_minor_is_not_fixed_width`, `test_requires_python_is_a_bare_lower_bound`,
  `test_workflow_pin_inventory_is_non_vacuous`,
  `test_gate_fails_closed_on_a_planted_pyproject_with_no_floor_statements`,
  `test_gate_fails_closed_on_an_empty_planted_workflows_dir`,
  `test_a_disagreeing_planted_pyproject_names_all_four_values`

Modified in place by this plan:
- `firestarter_app/.planning/codebase/STACK.md` — three floor claims corrected, one pointer line added

Executed but NOT modified by this plan:
- `firestarter_app/tools/ci_replica_venv.sh` — run, never edited, and never with `--refresh`

Deliberately NOT modified, with the reason on the record:
- `firestarter_app/.github/workflows/*.yml` — the gate READS these; editing the file a gate reads, to
  install the gate, is the circularity that ruled out the CI-step placement shape
- `firestarter_app/pyproject.toml` — no dependency is added; that is the point
- `/workspaces/.planning/` — every meta-repo edit belongs to plan 186-04
</artifacts_this_phase_produces>

<verification>
- `pytest tests/test_python_floor_agreement.py -o addopts="" -q` reports `7 passed`.
- The new file contains zero `#` comment lines and no `pyyaml`, `tomli` or `toml` import.
- `pyproject.toml` gained no dependency.
- `ruff check firestarter/ tests/` prints `All checks passed!`; `ruff format --check` reports
  `180 files already formatted`.
- `tools/check_mypy_watermark.py` reports `mypy errors: 35 (watermark: 35)` with `checked 181 source
  files` or more, and `OK: error count at watermark.`
- The app repo's `.planning/codebase/STACK.md` carries no old-floor claim and one pointer line naming
  both `firestarter_prom` and `python-floor-decision.md`.
- `bash tools/ci_replica_venv.sh` ends in `CI-REPLICA: PASS` with an `INTERPRETER:` line naming a 3.11
  and no `INTERPRETER-DIVERGENCE:` line.
- The tracked-file residual scan returns exactly two files, both deliberate mypy fixture sites.
- The SUMMARY carries the observed-RED evidence for the two planted-file legs and the disagreement leg.
</verification>

<success_criteria>
The agreement between the four floor statements is now a property the suite asserts, not a fact that
happens to hold. It is asserted by a gate that cannot pass over an empty scan, that compares versions
rather than spellings, that parses ruff's variable-width token correctly, and whose fail-closed legs have
been seen to fail. It added no dependency in the act of asserting the configuration agrees, it lives
inside every quality gate this milestone is about, and it required no edit to the CI file it reads. The
app-repo reader who next opens `pyproject.toml` finds both the corrected floor and where the reasoning
lives. And the whole change is proven on a 3.11 interpreter, with the boundary of that proof stated
rather than implied.
</success_criteria>

<output>
Create `.planning/phases/186-the-python-floor-before-the-eol/186-03-SUMMARY.md` when done
</output>
