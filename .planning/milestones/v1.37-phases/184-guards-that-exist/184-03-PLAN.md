---
phase: 184-guards-that-exist
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/dispatch-invariant-retirement-verdict.md
autonomous: true
requirements: [CLAIM-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/notes/dispatch-invariant-retirement-verdict.md` exists and states the verdict in its own first bolded sentence: the three-way dispatch invariant is RETIRED OUTRIGHT, with no successor guard and no backlog item (D-05)."
    - "The note records that the verdict was the operator's own call, taken against the orchestrator's recommendation — which was to retire the three-way and backlog the narrower two-way successor — and that a future need re-opens the question from scratch (D-05)."
    - "The note states the grounds, each checkable against source: nothing machine-reads `firestarter/PROTOCOLS.md` in any repository and that file carries no claims-region delimiter, so the document leg bounded nothing; the firmware leg extracted every `0x[0-9A-Fa-f]+` token from whole file text with no comment-awareness, so it was fail-open by construction; only the host leg was structurally checkable, and a one-legged invariant is not an invariant (D-08 item 1)."
    - "The note carries the two-deletion history as evidence legs with their commands and outputs: `39ea3e8` (2026-08-31, app repo) deleted `tests/test_dispatch_mirror.py` because its module-scope `fw_path(\"doc\", \"PROTOCOLS.md\")` aborted the whole app suite at collection once `firestarter/doc/` was deleted, and in the same commit re-pointed the surviving `ScanPathEntry` at `tools/wiki/dispatch_mirror.py (meta repo; relocated by 168-10)`; `5426d7ef` (2026-09-02, meta repo) retired every `tools/wiki/` checker two days later, demolishing the receiving end of a handoff that had just been written. The note says explicitly that NEITHER commit intended to retire the invariant (D-08 item 2)."
    - "The note carries the fail-open regex finding the two `planted_dispatch_*` fixtures encoded, quoted from their own headers, including the fact that `planted_dispatch_comment_only_hex.cpp`'s paired leg asserted GREEN and that the GREEN WAS the finding — so the knowledge survives the fixtures' deletion in `184-04` (D-08 item 3)."
    - "The note records that both fixtures' headers instruct future readers NOT to delete them, and that this instruction is superseded by D-05 and D-07 — their consumer was deleted 2026-08-31 and under D-05 no consumer will ever exist again."
    - "The note carries the three-row drift table with each side's own stated reason, and states in the note's own words that this phase MEASURED the divergence and did NOT verify the reason either side carries — so no reader mistakes the record for a clean bill of health (D-16)."
    - "The note carries a negative record stating that ZERO backlog items were filed on this axis, that no `KNOWN_PROTOCOLS` ↔ `kAllProtocolFamilies` successor guard and no 'unverified drift' item exist, and that this is a deliberate operator choice rather than an oversight (D-06)."
    - "The note records that the same one-row-per-`KNOWN_PROTOCOLS`-entry claim recurs at three further sites inside `test/native/avr/test_dispatch/test_configure_memory.cpp`, that they were seen and deliberately left outside D-10's single clause, and that recording them here is a record and not a filing (D-06, D-10)."
  prohibitions:
    - "MUST NOT file a backlog item, a ROADMAP `999.x` entry, a todo, or a GitHub issue on the CLAIM-03 axis — not a `KNOWN_PROTOCOLS` ↔ `kAllProtocolFamilies` successor-guard item, not an 'unverified drift' item, not a follow-up for the three recurring false mentions. The operator chose the branch that files nothing; an open-question item would quietly reinstate what was closed. Phase 182's 999.55–999.59 and Phase 183's 999.63–999.66 set a pattern that does NOT apply here (D-06)."
    - "MUST NOT design, sketch, propose or scaffold a replacement dispatch guard anywhere — not in the note, not as a code comment, not as a `tools/check_*.py` skeleton. `REQUIREMENTS.md` § Out of Scope bars building it and D-05 answers 'no' to whether it should be built at all."
    - "MUST NOT adjudicate the three divergences. Do not decide whether `0x34` belongs in the firmware table, whether `0x35`/`0x39` belong in `KNOWN_PROTOCOLS`, or which side is wrong. Adjudicating reopens v1.11's DEC-05 and the X88C64P not-implemented decision — protocol-domain work the milestone's Out of Scope bars, and a defect surfaced there would have no room to be fixed in this milestone (D-16)."
  artifacts:
    - path: ".planning/notes/dispatch-invariant-retirement-verdict.md"
      provides: "CLAIM-03's decided-and-recorded verdict, its evidence chain, the fixtures' fail-open finding, the unadjudicated drift table, and the negative record"
      contains: "RETIRED OUTRIGHT"
  key_links:
    - from: ".planning/notes/dispatch-invariant-retirement-verdict.md"
      to: "firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp"
      via: "the note quotes the fixture's own header so the fail-open finding survives the fixture's deletion in 184-04"
      pattern: "0x[0-9A-Fa-f]+"
    - from: ".planning/notes/dispatch-invariant-retirement-verdict.md"
      to: ".planning/REQUIREMENTS.md"
      via: "CLAIM-03's 'decided and recorded' is satisfied by this document; 184-05 ticks the requirement citing it"
      pattern: "CLAIM-03"
---

<objective>
Answer CLAIM-03's question in writing, with its evidence, and make the answer's limits as legible as the
answer.

Purpose: CLAIM-03 says whether the three-way dispatch invariant is worth re-guarding must be decided and
recorded — "retire it" is a valid outcome, "unaddressed" is not. D-05 is the decision: retired outright, the
operator's own call, taken against the orchestrator's recommendation. What is left is the recording, and the
recording carries more than the verdict. It carries the two-deletion history that nobody intended (`39ea3e8`
severed a collection hazard and re-pointed a handoff; `5426d7ef` deleted the receiving end two days later);
the fail-open regex finding that the two orphaned fixtures encoded and that must outlive them; the measured
drift with each side's stated reason and an explicit statement that neither reason was verified; and the
negative record that zero backlog items were filed, deliberately.

This is expansion work in the meta repository, sharing no file with the phase's tracer (`184-01`). It runs
BEFORE `184-04` deletes the fixtures, because the fixtures are the source for one of its sections and a
deletion-first ordering would make that section unverifiable.

Output: `.planning/notes/dispatch-invariant-retirement-verdict.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-guards-that-exist/184-CONTEXT.md
@.planning/phases/184-guards-that-exist/184-PATTERNS.md
@/workspaces/CLAUDE.md

Note on omissions: `.planning/PROJECT.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` are deliberately
not referenced — they are very large and nothing in this plan needs them.
</context>

<failing_direction_convention>
Every `<automated>` command in this plan is a GREEN-direction command. This plan has no RED-first leg.
</failing_direction_convention>

<tasks>

<task type="auto">
  <name>Task 1: The verdict, its grounds, and the two-deletion history nobody intended (D-05, D-08 items 1 and 2)</name>
  <reversibility rating="reversible">A markdown record in `.planning/notes/`. D-05 is itself rated reversible in principle — with the operator's own caveat that after this nothing will be watching, so re-opening depends on someone noticing unaided. Record that caveat in the note; do not soften it.</reversibility>
  <read_first>
    - `/workspaces/.planning/notes/ae29f2008-classification-verdict.md` — the closer of the two templates.
      Take from it: the frontmatter shape (`title` stating the verdict itself, not the topic; `date`;
      `context` naming milestone, phase, requirement ID and how the content was obtained); the `## THE
      VERDICT` shape, where the verdict is bolded first and the epistemic limits of the evidence come
      immediately after, before any detail; and the `## THE EVIDENCE CHAIN` shape of lettered legs, each a
      bolded one-sentence claim followed by a fenced command-and-output block.
    - `/workspaces/.planning/notes/jumper-display-ground-truth.md` — for its measurement-table shape only:
      a source-provenance table then a findings table, each row carrying the reason its own side holds.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` — D-05, D-06, D-08 in full, and
      § Specific Ideas for the drift table draft.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-PATTERNS.md` § "`.planning/notes/<verdict>.md`
      — NEW verdict document (D-08, D-16)" — every quoted pattern, including the Negative-record pattern and
      the explicit "do NOT copy 183's backlog-items section".
    - `/workspaces/firestarter/PROTOCOLS.md` — confirm for yourself that the file carries no claims-region
      delimiter of any kind. This is one of the grounds and the note asserts it.
  </read_first>
  <files>.planning/notes/dispatch-invariant-retirement-verdict.md</files>
  <action>
    Create `.planning/notes/dispatch-invariant-retirement-verdict.md` in the META repository — `.planning/`
    is meta-repo content and is committed in `/workspaces`, never inside a sub-repo. The filename follows
    the house convention: kebab-case, subject-first, ending in the document's kind.

    FRONTMATTER. `title` states the verdict itself, not the topic — the three-way dispatch invariant is
    retired outright, with no successor. `date: 2026-09-11`. `context` names v1.37, Phase 184, CLAIM-03, and
    how the content was obtained (measured in this session against live source and git history, plus the two
    orphaned fixtures read before their deletion in `184-04`).

    SECTION — THE VERDICT. Open with the verdict in bold: the three-way dispatch invariant (dispatch table /
    host tool / firmware agree) is RETIRED OUTRIGHT. No successor guard. No backlog item. A future need
    re-opens the question from scratch. State that this was the operator's own call, taken against the
    orchestrator's recommendation, which was to retire the three-way and backlog the narrower two-way
    successor.

    Immediately after the verdict, and before any detail, state the limits — 183's second-paragraph shape.
    Two things belong here: that the verdict rests on what the legs were measured to do, not on a claim that
    the underlying protocol lists agree; and D-05's own caveat, in the operator's terms, that retirement is
    reversible in principle but nothing will be watching, so re-opening depends on someone noticing unaided,
    which is the very condition this milestone exists to correct. Do not soften that caveat into a
    reassurance.

    SECTION — THE GROUNDS. Three grounds, each checkable against source, and check each one before writing
    it:
    (a) Nothing machine-reads `firestarter/PROTOCOLS.md` in any of the three repositories, and the file
        carries no claims-region delimiter at all — so the "keep its table shape intact" instruction guarded
        a shape no tool read and no marker bounded. The document leg bounded nothing.
    (b) The firmware leg was fail-open by construction: it extracted every `0x[0-9A-Fa-f]+` token from whole
        file text via a bare regex with no comment-awareness, so a comment-only mention of a protocol
        satisfied it exactly as well as a real dispatch case. Detail and quotation belong in Task 2; state
        the conclusion here.
    (c) Only the host leg — `KNOWN_PROTOCOLS`, a Python set literal in `tools/build_db.py` — was
        structurally checkable at all. A three-way invariant with one checkable leg is not an invariant.

    SECTION — THE EVIDENCE CHAIN. Two lettered legs, each a bolded one-sentence claim followed by a fenced
    command-and-output block holding the command you actually ran and its actual output:

    Leg — `39ea3e8`, 2026-08-31, app repo, `fix(168-04): sever the module-scope fw_path collection hazard`.
    It deleted `tests/test_dispatch_mirror.py` because that module's module-scope
    `fw_path("doc", "PROTOCOLS.md")` aborted the WHOLE app suite at collection once `firestarter/doc/` was
    deleted — a collection-time abort, not a test failure. In the same commit it deliberately re-pointed the
    surviving `ScanPathEntry` at `tools/wiki/dispatch_mirror.py (meta repo; relocated by 168-10)`. Evidence:
    `git log -1` for the sha and `git show --stat` scoped to the deleted path, run inside
    `/workspaces/firestarter_app`.

    Leg — `5426d7ef`, 2026-09-02, meta repo, `chore: retire wiki-check.yml and the tools/wiki checkers`. Two
    days later it retired every `tools/wiki/` checker, demolishing the receiving end of a handoff that had
    just been written. Evidence: `git log -1` and `git show --stat` scoped to `tools/wiki/`, run inside
    `/workspaces`.

    Close the section with the sentence that makes the whole record worth having: NEITHER commit intended to
    retire the invariant. Each was a correct local fix; the invariant died in the gap between them, and
    nothing noticed for eleven days.

    Do NOT adjudicate anything in this task. Do NOT propose a successor. Do NOT file anything.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/notes/dispatch-invariant-retirement-verdict.md && wc -l .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>non-zero exit (the file is absent), or the printed line count is below 60 — a record this thin cannot be carrying a verdict, three grounds and two evidence legs with their outputs</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'RETIRED OUTRIGHT' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the verdict is not stated in the form the rest of the phase cites</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '39ea3e8' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the app-repo deletion leg is missing its sha</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '5426d7ef' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the meta-repo retirement leg is missing its sha</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '2026-08-31' .planning/notes/dispatch-invariant-retirement-verdict.md; /usr/bin/grep -c '2026-09-02' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>either printed count is `0` — the two-deletion history has lost one of the two dates that make the eleven-day gap legible</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -ci 'fw_path' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the reason `39ea3e8` deleted the module (a module-scope `fw_path` collection abort) is missing, leaving the deletion looking arbitrary</fails_when>
  </verify>
  <acceptance_criteria>
    - The file exists at `.planning/notes/dispatch-invariant-retirement-verdict.md` in the meta repository
      and is committed there, not inside a sub-repo.
    - Its frontmatter `title` states the verdict, and `context` names v1.37, Phase 184 and CLAIM-03.
    - The first bolded sentence of the body states the invariant is retired outright with no successor guard
      and no backlog item, and names it as the operator's own call against the orchestrator's recommendation.
    - The paragraph immediately after the verdict states the evidence's limits, including D-05's caveat that
      nothing will be watching after this — unsoftened.
    - Three grounds are stated, each checkable against source; ground (a) asserts `PROTOCOLS.md` carries no
      claims-region delimiter, and the SUMMARY records the command used to confirm that.
    - Two evidence legs carry `39ea3e8` and `5426d7ef` with their dates, their subjects, and a fenced block
      holding a command that was actually run and its actual output.
    - The note states that neither commit intended to retire the invariant.
    - No successor guard is proposed anywhere in the file, and no backlog item, todo or issue was created by
      this task — `git status` in the meta repo shows no change to `.planning/ROADMAP.md` or
      `.planning/todos/`.
  </acceptance_criteria>
  <done>CLAIM-03's question has a written answer with its grounds and the history that produced it, and the answer says what it does not know.</done>
</task>

<task type="auto">
  <name>Task 2: Carry the fixtures' fail-open finding, record the drift without adjudicating it, and state what was deliberately not done (D-06, D-07, D-08 items 3 and 4, D-16)</name>
  <reversibility rating="reversible">Further sections in the same markdown record.</reversibility>
  <precondition>`firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` and `planted_dispatch_missing_hex.cpp` both still exist — `184-04` has not yet deleted them, so their headers can be quoted from source rather than from git history.</precondition>
  <read_first>
    - `/workspaces/firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` — the WHOLE file,
      all 85 lines. Its header is the sole source for the fail-open finding, and its body carries a
      byte-faithful copy of `kAllProtocolFamilies` with `flash_intel` rewritten `0x10` to `0xFF`.
    - `/workspaces/firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp` — the WHOLE file, all 95
      lines. Its header names the same regex from the RED side and explains why the fixture never spells the
      real identifier as one contiguous `0x`-prefixed token anywhere, not even in its own header.
      D-07 requires BOTH fixtures be read in full before they are disposed of; this is that read, and it
      happens here rather than in `184-04` so the knowledge is already on the record when the deletion runs.
    - `/workspaces/firestarter_app/tools/build_db.py` — the `KNOWN_PROTOCOLS` set literal and the comment
      block immediately above it, which carries the host side's own stated reasons for `0x34` and against
      `0x35`/`0x39`.
    - `/workspaces/firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp` — the
      `kAllProtocolFamilies` table, to count its rows and read the family strings; and the three
      `KNOWN_PROTOCOLS` mentions other than the one `184-02` excises.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-06, D-07, D-08 item 3, D-16, and
      § Deferred Ideas — which records the two-way successor as a REJECTED option, explicitly not a backlog
      candidate.
  </read_first>
  <files>.planning/notes/dispatch-invariant-retirement-verdict.md</files>
  <action>
    Append three sections to the note created in Task 1.

    SECTION — WHAT THE DELETED FIXTURES PROVED. This is D-08 item 3 and it is the reason `184-04` may delete
    them at all. Quote, from `planted_dispatch_comment_only_hex.cpp`'s own header, the paragraph stating that
    the fixture's paired leg asserted GREEN and that the GREEN WAS the finding: the firmware leg extracted
    every `0x[0-9A-Fa-f]+` token from the WHOLE file text via a bare regex with no comment-awareness, so a
    comment-only mention of `0x10` satisfied the gate exactly as well as a real dispatch case, and the gate
    could not distinguish "a native dispatch test exists for this protocol" from "a comment mentions this
    protocol". Quote alongside it the corresponding passage from `planted_dispatch_missing_hex.cpp`'s header,
    which names the same regex from the RED side and states why that fixture never spells the real identifier
    as a contiguous `0x` token anywhere, not even in its own header.

    Record two further facts in the same section:
    - Both fixture bodies carry a byte-faithful copy of `kAllProtocolFamilies` with `flash_intel` rewritten
      from `0x10` to `0xFF`, at 13 rows — corroborating the 13 figure independently of
      `test_configure_memory.cpp`. State that you counted it.
    - Both headers instruct future readers NOT to delete the fixtures. That instruction is SUPERSEDED by
      D-05 and D-07: their consumer `tests/test_dispatch_mirror.py` was deleted 2026-08-31 by `39ea3e8`, and
      under D-05 no consumer will ever exist again. Say so explicitly, and say that this note is where the
      knowledge the instruction was protecting now lives. A later reader who finds the deletion in git must
      be able to land here.

    SECTION — THE DRIFT, MEASURED AND NOT ADJUDICATED. Carry the four-column table: protocol, presence in
    `KNOWN_PROTOCOLS` (`tools/build_db.py`), presence in `kAllProtocolFamilies`
    (`test/native/avr/test_dispatch/test_configure_memory.cpp`), and the reason that side already carries in
    its own source. Three rows: `0x34` present host-side and absent firmware-side, reason "XICOR X88C64P —
    included as protocol-not-implemented"; `0x35` absent host-side and present firmware-side, reason "NOT
    0x35 or 0x39 — removed by v1.11 DEC-05"; `0x39` the same. State the totals: 12 host-side, 13
    firmware-side. Re-measure both counts yourself before writing them and record the commands in the
    SUMMARY.

    Then the sentence that makes this section honest, in the note's own words, as its own paragraph: this
    phase MEASURED the divergence and did NOT verify the reason either side carries. Neither figure is
    verified by anything. Adjudicating would mean reopening v1.11's DEC-05 and the X88C64P
    not-implemented decision — protocol-domain work the milestone's Out of Scope bars, and a defect surfaced
    there would have no room to be fixed in this milestone. No reader should mistake this table for a clean
    bill of health.

    SECTION — WHAT THIS PHASE DELIBERATELY DID NOT DO. The negative record, 183's shape applied to D-06:
    - ZERO backlog items were filed on this axis. No `KNOWN_PROTOCOLS` ↔ `kAllProtocolFamilies` successor
      guard item. No "unverified drift" item. No todo, no issue.
    - State that the two-way successor WAS considered — both sides are structured and machine-readable, a
      Python set literal and a C array, so it could be checked properly rather than through the
      comment-blind regex that made the deleted leg fail open, and the rule would have been "every
      difference is accounted for" rather than "the lists are equal" — and that the operator explicitly
      declined to file it. It is recorded here as a rejected option so a later reader knows it was
      considered, NOT as a backlog candidate.
    - State that Phase 182's 999.55–999.59 and Phase 183's 999.63–999.66 set a filing pattern that does not
      apply here, and that an "obviously warranted" filing would quietly reinstate what the operator closed.
    - Record that the same one-row-per-`KNOWN_PROTOCOLS`-entry claim recurs at three further sites inside
      `test/native/avr/test_dispatch/test_configure_memory.cpp` beyond the single clause D-10 excises, and
      that one of them cites `build_db.py:89` for a symbol living at line 137. They were seen and
      deliberately left: D-10 names one clause, CONTEXT.md § Deferred flags the line citation as
      explicitly not filed, and D-06 bars a filing on this axis. Recording them here is a record, not a
      filing — that distinction is the whole point of this section.

    Do NOT copy 183's `## Summary of backlog items this verdict generates` section. If an equivalent heading
    is written at all, it states that none were filed and why.

    Commit in the meta repository.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c '0x\[0-9A-Fa-f\]' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the fail-open regex the fixtures encoded is not quoted, so the finding does not survive their deletion</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'planted_dispatch_comment_only_hex.cpp'  .planning/notes/dispatch-invariant-retirement-verdict.md; /usr/bin/grep -c 'planted_dispatch_missing_hex.cpp' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>either printed count is `0` — one of the two fixtures is unnamed, so a reader finding its deletion in git cannot land here</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '0x34' .planning/notes/dispatch-invariant-retirement-verdict.md; /usr/bin/grep -c '0x35' .planning/notes/dispatch-invariant-retirement-verdict.md; /usr/bin/grep -c '0x39' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>any of the three printed counts is `0` — a drift row is missing</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'DEC-05' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the firmware side's own stated reason is missing from the drift table, which is what makes the table a record rather than an accusation</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -ci 'did not verify\|not verified' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the note does not state that this phase measured the divergence without verifying either side's reason, which is D-16's entire requirement</fails_when>
    <automated>cd /workspaces && git status --porcelain -- .planning/ROADMAP.md .planning/todos/</automated>
    <fails_when>any output at all — a backlog item or todo was created, which D-06 forbids on this axis</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^### Phase 999\.' .planning/ROADMAP.md</automated>
    <fails_when>the printed count differs from the count taken at the start of this task — a `999.x` backlog entry was added, which D-06 forbids. Record the pre-task count in the SUMMARY so this comparison is possible</fails_when>
  </verify>
  <acceptance_criteria>
    - The note quotes, from `planted_dispatch_comment_only_hex.cpp`'s own header, the passage stating the
      paired leg asserted GREEN and that the GREEN was the finding, and quotes the corresponding
      `planted_dispatch_missing_hex.cpp` passage naming the same bare `0x[0-9A-Fa-f]+` regex from the RED
      side.
    - The note states that both fixture bodies carry a 13-row byte-faithful `kAllProtocolFamilies` copy with
      `flash_intel` rewritten `0x10` to `0xFF`, and the SUMMARY records the command used to count those rows.
    - The note states that both headers' do-not-delete instruction is superseded by D-05 and D-07, naming
      `39ea3e8` and 2026-08-31 as the consumer's deletion.
    - The drift table has three rows (`0x34`, `0x35`, `0x39`), four columns, each side's own stated reason,
      and the totals 12 host-side and 13 firmware-side — both re-measured in this task, with the commands in
      the SUMMARY.
    - A standalone paragraph states that this phase measured the divergence and did not verify the reason
      either side carries, and names why adjudicating is out of scope.
    - Nothing in the note decides which side is right about `0x34`, `0x35` or `0x39`.
    - A negative-record section states that zero backlog items were filed, that the two-way successor was
      considered and explicitly declined, and that the 182/183 filing pattern does not apply here.
    - The note records the three further `KNOWN_PROTOCOLS` mentions in `test_configure_memory.cpp` as seen
      and deliberately left, and states that recording them is not filing them.
    - `git status --porcelain -- .planning/ROADMAP.md .planning/todos/` is empty, and the `999.x` entry count
      in `ROADMAP.md` is unchanged from the pre-task count recorded in the SUMMARY.
    - No section proposes, sketches or scaffolds a successor guard.
  </acceptance_criteria>
  <done>The knowledge the two fixtures were carrying now lives somewhere that will outlive them, the drift is on the record as measured rather than judged, and the decision not to file anything is itself recorded so nobody helpfully undoes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture headers → the project's memory | The two `planted_dispatch_*` fixtures are the only committed record of the comment-blind regex finding. Deleting them without migrating the knowledge destroys evidence that cost a phase to produce. |
| a measured table → a reader's conclusion | A drift table read as adjudicated becomes a clean bill of health for protocol lists nothing has verified. |
| an operator decision → a later helpful agent | A closed decision with no tracker is one "obviously warranted" backlog filing away from being silently reopened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-03-A | Repudiation | the fixtures' fail-open finding | high | mitigate | The finding is quoted from both fixture headers into the note BEFORE `184-04` deletes them, and `184-04` carries a precondition that the note already contains it. Ordering, not diligence, is what guarantees survival. |
| T-184-03-B | Information disclosure (false assurance) | the drift table | high | mitigate | A standalone paragraph states the divergence was measured and neither side's reason verified, in the note's own words, asserted by a grep for that statement. D-16's requirement is precisely that the record cannot be read as a clean bill of health. |
| T-184-03-C | Elevation of privilege | a backlog filing on the CLAIM-03 axis | high | mitigate | An explicit `must_haves` prohibition, an empty-`git status` gate over `ROADMAP.md` and `.planning/todos/`, and a before/after count of `999.x` entries. The rejected option is recorded in the note as rejected, so the absence cannot be mistaken for an omission. |
| T-184-03-D | Tampering | the verdict's grounds | medium | mitigate | Each of the three grounds is checkable against source and is confirmed in-task before being written; the `PROTOCOLS.md` no-delimiter ground has its confirming command recorded in the SUMMARY. |
| T-184-03-E | Spoofing | the note's own authority | medium | mitigate | The epistemic-limits paragraph runs immediately after the verdict, before any detail, and carries D-05's unsoftened caveat that nothing will be watching. A verdict that overstates its own standing is the failure mode this milestone exists to correct. |
| T-184-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency; it writes one markdown file and runs `git log`, `git show` and `grep`. No Package Legitimacy Audit table is required, and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from the spec-less edge probe and NOT silently dropped.

- **CLAIM-03 / `unclassified` — FLAGGED, left `unresolved`.** The deterministic edge probe returned
  `unclassified` for CLAIM-03 and its probe text reads "unclassified — review manually". It is surfaced here
  rather than auto-resolved, per the fallback protocol, which forbids auto-resolving an `unclassified` row.
  The manual reading: CLAIM-03 is not a data-shape requirement at all — its deliverable is a *decision and a
  record*, so the probe's eight shape categories (empty, adjacency, encoding, ordering, concurrency, …) have
  no purchase on it. That is a reason, not a dismissal. The requirement's real failure mode is not an edge
  case in a computation but a record that overstates what it knows, and that failure mode is covered above
  by T-184-03-B and T-184-03-E and by the D-16 truth in `must_haves`. A verifier that cannot confirm this
  from evidence should return `human_needed`, not a silent pass.
- **Flagged, plan-local:** the three grounds in Task 1 are stated as measured during the phase discussion
  and re-confirmed in-task. Ground (b) — the bare regex with no comment-awareness — is confirmed from the
  fixtures' own headers rather than from the deleted checker's source, which no longer exists in any
  checkout. That is the best evidence available and the note should say which it is.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan:
- `.planning/notes/dispatch-invariant-retirement-verdict.md` — a NEW file. It did not exist before this
  phase; a drift check must not flag it as an unacknowledged symbol.

Deleted by this plan:
- Nothing. This plan is purely additive; the deletions it makes possible happen in `184-04`.

Deliberately NOT created, and this absence is itself an artifact:
- No `999.x` backlog entry, no todo, no GitHub issue on the CLAIM-03 axis (D-06).
- No successor guard, no `tools/check_*.py` skeleton, no design sketch for one (D-05, Out of Scope).
</artifacts_this_phase_produces>

<verification>
- `.planning/notes/dispatch-invariant-retirement-verdict.md` exists in the meta repo, is committed there,
  and is at least 60 lines.
- It contains `RETIRED OUTRIGHT`, `39ea3e8`, `5426d7ef`, `2026-08-31`, `2026-09-02`, `fw_path`,
  `0x[0-9A-Fa-f]`, both fixture filenames, all three of `0x34`/`0x35`/`0x39`, `DEC-05`, and a statement that
  this phase did not verify either side's reason.
- `git status --porcelain -- .planning/ROADMAP.md .planning/todos/` is empty and the `999.x` entry count in
  `ROADMAP.md` is unchanged.
- No successor guard is proposed anywhere in the file.
</verification>

<success_criteria>
CLAIM-03 is answered rather than deferred: the invariant is retired outright, on stated grounds, with the
history that killed it and the finding its orphaned controls encoded both preserved in a document that will
outlive them. The drift is on the record as measured and explicitly unjudged, and the decision to file
nothing is written down where the next helpful agent will read it before undoing it.
</success_criteria>

<output>
Create `.planning/phases/184-guards-that-exist/184-03-SUMMARY.md` when done
</output>
