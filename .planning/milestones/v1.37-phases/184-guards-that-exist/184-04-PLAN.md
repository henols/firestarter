---
phase: 184-guards-that-exist
plan: 04
type: execute
wave: 2
depends_on: ["184-01", "184-03"]
files_modified:
  - firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp
  - firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp
  - firestarter_app/tools/check_no_exists_proxy.py
  - firestarter_app/tests/fixtures/planted_no_exists_proxy.py
autonomous: true
requirements: [CLAIM-02, CLAIM-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` and `planted_dispatch_missing_hex.cpp` no longer exist in the app repository — deleted, not re-pointed, because under D-05 no consumer will ever exist for them to be re-pointed at (D-07, CLAIM-02)."
    - "Both fixtures were read in full BEFORE deletion, and the knowledge their headers carried — the comment-blind `0x[0-9A-Fa-f]+` regex fail-open finding — was already committed to `.planning/notes/dispatch-invariant-retirement-verdict.md` by `184-03` at the moment the deletion ran, checked rather than assumed (D-07, D-08)."
    - "Nothing in the app repository referenced either fixture except the fixtures themselves — verified by a repository-wide search immediately before the deletion, so no consumer was orphaned by it."
    - "All three surviving provenance citations still name `tests/test_dispatch_mirror.py` and now state its disposal: deleted 2026-08-31 in `39ea3e8`. The name is kept because it is the only record of why the compound `not (a.exists() and b.exists())` arm exists in a live checker; stripping it would protect a grep while inviting a future 'why do we guard the compound shape at all?' deletion (D-02)."
    - "The three citations are worded consistently with each other — `tools/check_no_exists_proxy.py` and `tests/fixtures/planted_no_exists_proxy.py` cite one another, so a reader arriving at either lands on the same account (D-02)."
    - "`tools/check_no_exists_proxy.py`'s lint behaviour is unchanged: only its module docstring is edited, and `tests/test_check_no_exists_proxy.py` still passes against the edited fixture — the fixture's three planted subjects and their scope discrimination are untouched (D-02)."
    - "The two deletions clear two of the five app-repo `dispatch_mirror` grep hits; combined with `184-01`'s entry removal, the app repository's only remaining occurrences are the three `tests/test_dispatch_mirror.py` citations D-02 deliberately keeps — a different module, in a different repository's history, each occurrence now naming its own deletion (D-02, D-03, CLAIM-01)."
    - "No comment line was added to `tools/check_no_exists_proxy.py` or `tests/fixtures/planted_no_exists_proxy.py`. Two of the three citation sites are module docstrings, which the project rule does not govern; the third is an EXISTING `#` comment whose wording is repaired in place so a dated fact becomes accurate — repair of an existing line, not authoring new commentary."
    - statement: "The app test suite still collects cleanly after the deletions — no module imported, referenced or globbed either fixture path."
      verification: backstop
  prohibitions:
    - "MUST NOT strip `tests/test_dispatch_mirror.py`'s name from any of the three citations. Deleting the name is the obvious way to make a `dispatch_mirror` grep clean and it is exactly wrong: the name is the only surviving record of why the live lint guards the compound shape, and a future reader without it has no reason not to delete the compound arm. D-02 keeps the name and adds the disposal."
    - "MUST NOT author any new comment into `tools/check_no_exists_proxy.py` or `tests/fixtures/planted_no_exists_proxy.py`, and MUST NOT convert a docstring edit into an added `#` block. `/workspaces/CLAUDE.md` § \"Source code comments — hard rule\" is absolute and is not overridable by this plan, this task, or any skill."
    - "MUST NOT delete either `planted_dispatch_*` fixture until `.planning/notes/dispatch-invariant-retirement-verdict.md` demonstrably contains the fail-open finding. Their headers are the sole source for it, and a deletion-first ordering makes the finding unverifiable rather than merely unwritten."
    - "MUST NOT 'fix' `planted_no_exists_proxy.py` by removing its planted violations or by making the lint stop flagging them. It is a deliberate control: `SIMPLE_ABSENCE_PROXY` and `COMPOUND_ABSENCE_PROXY` are supposed to be flagged and `legitimate_in_function_check` is supposed not to be. Only prose changes here."
  artifacts:
    - path: "firestarter_app/tools/check_no_exists_proxy.py"
      provides: "The live compound-shape lint with its provenance citation made recoverable"
      contains: "39ea3e8"
    - path: "firestarter_app/tests/fixtures/planted_no_exists_proxy.py"
      provides: "The lint's planted control with both of its provenance citations made recoverable"
      contains: "39ea3e8"
  key_links:
    - from: "firestarter_app/tools/check_no_exists_proxy.py"
      to: "firestarter_app/tests/fixtures/planted_no_exists_proxy.py"
      via: "the two files cite each other for the compound-shape provenance; tests/test_check_no_exists_proxy.py runs the lint against the fixture"
      pattern: "test_dispatch_mirror"
    - from: ".planning/notes/dispatch-invariant-retirement-verdict.md"
      to: "firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp"
      via: "the note holds the fixtures' fail-open finding, which is what permits the deletion; the deletion's precondition asserts the note already holds it"
      pattern: "0x[0-9A-Fa-f]+"
---

<objective>
Dispose of two controls whose consumer was deleted eleven days before this phase started, and make three
citations that outlived their subject say so.

Purpose: CLAIM-02 requires the two orphaned `planted_dispatch_*` fixtures to be disposed — deleted, or
re-pointed at a consumer that exists. D-07 chooses deletion, because CLAIM-03's verdict (D-05) means no
consumer will ever exist to re-point them at. Their headers instruct future readers not to delete them, and
that instruction is right about what it was protecting: the fail-open regex finding it guards is real and
valuable. So `184-03` writes that finding into the verdict note FIRST, and this plan's deletion is gated on
the note already holding it.

Separately, CLAIM-01's remaining app-repo work: three provenance citations name `tests/test_dispatch_mirror.py`
— a DIFFERENT module from the deleted meta-repo checker, deleted on a different date by a different commit.
D-02 keeps the name, because it is the only record of why a live lint guards the compound
`not (a.exists() and b.exists())` shape, and adds its disposal so the citation stops being unverifiable.

This is expansion work in the app repository. It runs after `184-01` because both plans commit into the same
sub-repo working tree, and after `184-03` because the note is the precondition for the deletion.

Output: two fewer fixture files, three citations that name their own subject's deletion, and a suite that
still collects.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-guards-that-exist/184-CONTEXT.md
@.planning/phases/184-guards-that-exist/184-PATTERNS.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md

Note on omissions: `.planning/PROJECT.md`, `.planning/ROADMAP.md` and `.planning/STATE.md` are deliberately
not referenced — they are very large and nothing in this plan needs them.
</context>

<failing_direction_convention>
Every `<automated>` command in this plan is a GREEN-direction command. This plan has no RED-first leg; the
RED-then-GREEN discipline lives in `184-01`.
</failing_direction_convention>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two orphaned `planted_dispatch_*` fixtures (D-07, CLAIM-02)</name>
  <reversibility rating="reversible">Both files are recoverable from git, and D-08's verdict note preserves the knowledge separately — which is the reason the deletion is safe at all.</reversibility>
  <precondition>`.planning/notes/dispatch-invariant-retirement-verdict.md` exists in the meta repository and already contains the fail-open finding — check it read-only with a grep for the `0x[0-9A-Fa-f]+` regex and for both fixture filenames before touching either file. If it does not, STOP: `184-03` is incomplete and deleting now destroys the only committed source for that finding.</precondition>
  <read_first>
    - `/workspaces/firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` — the WHOLE file,
      all 85 lines. D-07 requires this read before deletion. Its header carries the fail-open finding; its
      body carries a 13-row `kAllProtocolFamilies` copy with `flash_intel` rewritten `0x10` to `0xFF`.
    - `/workspaces/firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp` — the WHOLE file, all 95
      lines. Its header names the same regex from the RED side and explains why it never spells the real
      identifier as a contiguous `0x` token anywhere.
    - `/workspaces/.planning/notes/dispatch-invariant-retirement-verdict.md` — the section `184-03` wrote
      carrying the fail-open finding. Read it and confirm for yourself that what these two headers say has
      actually landed there, in enough detail that a reader with only the note could reconstruct the
      finding. This is the check the precondition names; do not treat it as a formality.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-05, D-07, D-08 item 3.
  </read_first>
  <files>firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp, firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp</files>
  <action>
    Read both fixtures in full first — that is a hard requirement of D-07 and the `<read_first>` list above,
    not a suggestion. Then confirm the verdict note holds the finding.

    Before deleting, run a repository-wide search for `planted_dispatch` inside
    `/workspaces/firestarter_app`, scoped to tracked files, and confirm the only hits are inside the two
    files themselves. Use `git grep`, which reads the index, or `/usr/bin/grep` by absolute path — the
    devcontainer's default `grep` is ugrep and honours `.gitignore`, so it silently under-scans and would
    report a comfortable zero for files that exist. Record the search output in the SUMMARY; an
    orphan-deletion claim that was never searched for is the same class of unverified assertion this phase
    exists to remove.

    Then `git rm` both files. Do not move them, do not relocate their content into another fixture, and do
    not re-point them at a consumer: CLAIM-02 permits re-pointing, but under D-05 no consumer will ever
    exist.

    Record in the SUMMARY that both headers' do-not-delete instruction was read and is superseded by D-05
    and D-07 — their consumer `tests/test_dispatch_mirror.py` was deleted 2026-08-31 by `39ea3e8`, and the
    knowledge the instruction protected now lives in
    `.planning/notes/dispatch-invariant-retirement-verdict.md`. Name that file in the commit message, so a
    reader who finds the deletion in git history can land on the record in one step.

    Commit inside `/workspaces/firestarter_app`, on
    `gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. Do not commit from the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test ! -e tests/fixtures/planted_dispatch_comment_only_hex.cpp && test ! -e tests/fixtures/planted_dispatch_missing_hex.cpp && echo BOTH_FIXTURES_GONE</automated>
    <fails_when>the word `BOTH_FIXTURES_GONE` is absent from the output, or non-zero exit — at least one fixture still exists on disk</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- 'tests/fixtures/planted_dispatch*'</automated>
    <fails_when>any output at all — a fixture was removed from disk but is still tracked in the git index</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- 'tests/fixtures/planted_no_exists_proxy.py'</automated>
    <fails_when>no output — the surviving fixture was deleted along with the two that were meant to go. This is also the non-vacuity control for the gate above: without it, an empty result there could mean the pathspec matched nothing for an unrelated reason</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -c 'planted_dispatch' -- . ':(exclude).planning'</automated>
    <fails_when>any output at all, or exit code 0 — `git grep` exits 1 with no output when nothing matches, which is the passing direction here; a matching line means a reference to a deleted fixture survived somewhere in the repository</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'planted_dispatch' .planning/notes/dispatch-invariant-retirement-verdict.md</automated>
    <fails_when>the printed count is `0` — the knowledge did not survive the deletion, which is the one outcome this task must not produce</fails_when>
  </verify>
  <acceptance_criteria>
    - Both fixture files are absent from disk and from the git index; `planted_no_exists_proxy.py` is still
      present.
    - The SUMMARY records the pre-deletion `planted_dispatch` search and its output, showing the only hits
      were inside the two files themselves.
    - The SUMMARY states that both files were read in full before deletion, and names at least one concrete
      fact taken from each header — the comment-only GREEN finding from
      `planted_dispatch_comment_only_hex.cpp`, and the never-spell-the-token reasoning from
      `planted_dispatch_missing_hex.cpp`.
    - The SUMMARY records that the precondition check on
      `.planning/notes/dispatch-invariant-retirement-verdict.md` was run before the deletion, with the
      command and its output.
    - The SUMMARY states that both headers' do-not-delete instruction is superseded by D-05 and D-07, naming
      `39ea3e8` and 2026-08-31.
    - The commit message names `.planning/notes/dispatch-invariant-retirement-verdict.md`.
    - Nothing was re-pointed, relocated or re-created: no new file appears under `tests/fixtures/`.
  </acceptance_criteria>
  <done>Two controls whose consumer no longer exists are gone, and the one thing they were really carrying is somewhere it will be found.</done>
</task>

<task type="auto">
  <name>Task 2: Re-date the three surviving provenance citations (D-02, CLAIM-01)</name>
  <reversibility rating="reversible">Prose in two module docstrings and one existing comment.</reversibility>
  <read_first>
    - `/workspaces/firestarter_app/tools/check_no_exists_proxy.py` — the whole module docstring, including
      the two-shape list (`the simple shape` / `the compound shape`) and the `What this must NOT forbid`
      paragraph below it. The citation is in the compound-shape bullet.
    - `/workspaces/firestarter_app/tests/fixtures/planted_no_exists_proxy.py` — the WHOLE file. Two citation
      sites: the numbered item 2 in the module docstring, and the `PLANTED VIOLATION (compound shape)` `#`
      comment above `COMPOUND_ABSENCE_PROXY`. Also read the three planted subjects and the
      `legitimate_in_function_check` function, so you can see exactly what must NOT change.
    - `/workspaces/firestarter_app/tests/test_check_no_exists_proxy.py` — the test that runs the lint against
      the fixture. You are editing prose in both of its inputs; read it to confirm no assertion depends on
      the wording you are changing.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-02 in full, and
      `/workspaces/.planning/phases/184-guards-that-exist/184-PATTERNS.md` § "`firestarter_app/tools/check_no_exists_proxy.py:19` and `tests/fixtures/planted_no_exists_proxy.py:20,41` — re-dated citations (D-02)" — which quotes all three sites verbatim and gives the pattern to apply.
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule" — read it before touching the third site,
      which is a `#` comment.
  </read_first>
  <files>firestarter_app/tools/check_no_exists_proxy.py, firestarter_app/tests/fixtures/planted_no_exists_proxy.py</files>
  <action>
    Edit all three citation sites in one pass, so the wording matches across them — the two files cite each
    other, and a reader who arrives at either must get the same account.

    At each site: KEEP the module name `tests/test_dispatch_mirror.py`, and add its disposal in place —
    state that the module was DELETED on 2026-08-31 in `39ea3e8`. Follow the citation format all three sites
    already use: they each carry a `Phase 123 Plan 08` provenance token, so the disposal reads as another
    provenance fact in the same register, not as an interjection. The house style for "this thing is gone,
    here is where it went" is meta `CLAUDE.md`'s retirement sentence: name, date, sha in backticks.

    The three sites, located by content rather than by line number:
    1. `tools/check_no_exists_proxy.py` — the module docstring bullet beginning `the compound shape:` and
       ending with the `tests/fw_presence.py` rekey. Docstring: the project comment rule does not govern it.
    2. `tests/fixtures/planted_no_exists_proxy.py` — numbered item 2 in the module docstring, the sentence
       ending `used before its own rekey`. Docstring.
    3. `tests/fixtures/planted_no_exists_proxy.py` — the `PLANTED VIOLATION (compound shape):` `#` comment
       above `COMPOUND_ABSENCE_PROXY`, the clause `mirrors the exact shape test_dispatch_mirror.py used
       before its Phase 123 Plan 08 rekey`. This one is an EXISTING comment. Editing its wording in place so
       a dated fact becomes accurate is repair of an existing line — the same category as `184-02`'s clause
       deletion — and is NOT authoring new commentary. Do not add a line to this block, do not add a second
       block, and do not convert it into a docstring.

    Why the name stays, so nobody optimises it away while looking at a `dispatch_mirror` grep: the citation
    is the only record of why the compound arm exists in a live checker. Stripping the name would make the
    grep cleaner and leave a future reader with no answer to "why do we guard the compound shape at all?" —
    which is how the compound arm gets deleted. Leaving it untouched fixes nothing either, because the
    citation stays unverifiable, which is the CLAIM strand's own complaint. Naming it plus its disposal is
    the only option that satisfies both.

    Change NOTHING else in either file. In particular: `check_no_exists_proxy.py`'s AST-scanning behaviour,
    its two detected shapes and its function-body exemption are untouched; and `planted_no_exists_proxy.py`'s
    three planted subjects (`SIMPLE_ABSENCE_PROXY`, `COMPOUND_ABSENCE_PROXY`,
    `legitimate_in_function_check`) and their scope discrimination are untouched. The fixture is a deliberate
    control — its violations are supposed to be flagged.

    Commit inside `/workspaces/firestarter_app`, on the milestone branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'test_dispatch_mirror' tools/check_no_exists_proxy.py; /usr/bin/grep -c 'test_dispatch_mirror' tests/fixtures/planted_no_exists_proxy.py</automated>
    <fails_when>the first printed count is anything other than `1` or the second anything other than `2` — a citation's module name was stripped, which this plan explicitly prohibits, or a citation was duplicated</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '39ea3e8' tools/check_no_exists_proxy.py; /usr/bin/grep -c '39ea3e8' tests/fixtures/planted_no_exists_proxy.py</automated>
    <fails_when>the first printed count is `0`, or the second is below `2` — at least one of the three citations still fails to name the commit that deleted its subject, so it is still unverifiable</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '2026-08-31' tools/check_no_exists_proxy.py; /usr/bin/grep -c '2026-08-31' tests/fixtures/planted_no_exists_proxy.py</automated>
    <fails_when>the first printed count is `0`, or the second is below `2` — a citation carries the sha without the date, so a reader cannot place the deletion relative to `5426d7ef`'s 2026-09-02 retirement</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '^[[:space:]]*#' tests/fixtures/planted_no_exists_proxy.py</automated>
    <fails_when>the printed count is greater than `5`, the count before this task — any increase means a comment line was authored, which the project's hard rule forbids</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_check_no_exists_proxy.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failure — the lint no longer agrees with its own control, meaning a prose edit reached behaviour</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'SIMPLE_ABSENCE_PROXY\|COMPOUND_ABSENCE_PROXY\|legitimate_in_function_check' tests/fixtures/planted_no_exists_proxy.py</automated>
    <fails_when>the printed count is anything other than `6` (three subjects, each named once in the module docstring and once in the code) — one of the fixture's planted subjects or its docstring reference was removed, which would silently weaken the control</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_dispatch_mirror` appears exactly once in `tools/check_no_exists_proxy.py` and exactly twice in
      `tests/fixtures/planted_no_exists_proxy.py` — the name is kept at all three sites.
    - `39ea3e8` and `2026-08-31` each appear at least once in `check_no_exists_proxy.py` and at least twice
      in `planted_no_exists_proxy.py` — every citation names its subject's deletion.
    - The SUMMARY quotes all three edited passages verbatim, so a reviewer can confirm the wording is
      consistent across the two files without opening them.
    - The comment-line count of `tests/fixtures/planted_no_exists_proxy.py` is not greater than 5, its count
      before this task; the third site was repaired in place and no block was added.
    - `tests/test_check_no_exists_proxy.py` passes.
    - The diff for `tools/check_no_exists_proxy.py` touches only its module docstring, and the diff for
      `tests/fixtures/planted_no_exists_proxy.py` touches only its module docstring and the one existing
      `PLANTED VIOLATION (compound shape)` comment. No executable line changed in either file.
  </acceptance_criteria>
  <done>All three citations still name the module that explains why the live lint guards the compound shape, and every one of them now says what became of it.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the app suite still collects, and that the app repo's `dispatch_mirror` residue is exactly what D-02 keeps (CLAIM-01, CLAIM-02, D-03)</name>
  <reversibility rating="reversible">A verification task; it produces evidence, not source change.</reversibility>
  <precondition>Tasks 1 and 2 are committed, so `firestarter_app` is git-porcelain — `tests/test_flash_path_record_sync.py` asserts the whole repository is porcelain and will redden for an unrelated reason otherwise.</precondition>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_flash_path_record_sync.py` — enough to confirm the porcelain
      assertion is what the precondition describes, so the executor understands why the ordering matters
      rather than merely obeying it.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-02 and D-03 — the three citations
      that survive deliberately, and why the literal criterion-1 grep cannot distinguish them from the
      meta-repo checker.
  </read_first>
  <files>(no files modified — this task produces evidence)</files>
  <action>
    Prove three things and record all three in the SUMMARY.

    FIRST — the suite still collects. Run pytest in collect-only mode over the whole `tests/` tree under
    `.venv311`. This is deliberately a COLLECTION check and not a full run: `/dev/ttyACM0` is attached in
    this devcontainer, which reddens the `test_no_programmer_found_*` characterization tests for reasons
    that have nothing to do with this phase, and a full run would therefore prove nothing while looking
    alarming. Collection is what actually catches the realistic failure mode of Task 1 — a module that
    imported, globbed or parametrised over a deleted fixture path. State in the SUMMARY that the full run
    was deliberately not used and why.

    SECOND — the targeted tests pass. Run `tests/test_check_no_exists_proxy.py`, `tests/test_sdp_honesty.py`
    (which also references the no-exists-proxy lint) and `tests/test_scan_paths_resolve.py` together, under
    `.venv311` with `-o addopts=""` so the count line is readable.

    THIRD — enumerate the app repo's remaining `dispatch_mirror` occurrences and check them against D-02's
    list. Run `git grep -n 'dispatch_mirror' -- . ':(exclude).planning'` inside `/workspaces/firestarter_app`
    and paste the full output into the SUMMARY. There must be exactly three hits, all naming
    `tests/test_dispatch_mirror.py`: one in `tools/check_no_exists_proxy.py` and two in
    `tests/fixtures/planted_no_exists_proxy.py`. Zero hits may name `tools/wiki/dispatch_mirror.py`. State
    plainly in the SUMMARY that these three are the citations D-02 deliberately keeps — a different module,
    deleted 2026-08-31 by `39ea3e8`, each occurrence now naming its own deletion — and that this is exactly
    the collision D-03 amends success criterion 1 for. The amendment itself lands in `184-05`; this task
    supplies the evidence it rests on.

    Do not edit any file in this task. If any of the three checks fails, fix the cause in the task that owns
    it rather than here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ --collect-only -q -o addopts=""</automated>
    <fails_when>non-zero exit, or the output contains `error` / `ERROR`, or the collected-count line reports fewer than 2300 tests (2359 were collected at planning time) — a collection abort or a sharply reduced count means a module broke on a deleted fixture path</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_check_no_exists_proxy.py tests/test_sdp_honesty.py tests/test_scan_paths_resolve.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -l 'dispatch_mirror' -- . ':(exclude).planning'</automated>
    <fails_when>the output is anything other than exactly the two lines `tests/fixtures/planted_no_exists_proxy.py` and `tools/check_no_exists_proxy.py` — a third path means a claim survived, and an empty result means one of D-02's deliberate citations was stripped</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — that means `git grep` FOUND the meta-repo checker path, i.e. a live-guard claim survived in the app repository. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain</automated>
    <fails_when>any output at all — the repository is not porcelain, so the first command's collection ran against an uncommitted tree and the SUMMARY's evidence does not describe what was committed</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the collect-only output's collected-test count, and states that a full suite run
      was deliberately not used because `/dev/ttyACM0` is attached and reddens `test_no_programmer_found_*`
      for reasons unrelated to this phase.
    - The SUMMARY records the passing summary line for the three targeted test files.
    - The SUMMARY pastes the full `git grep -n 'dispatch_mirror'` output for the app repo, and it shows
      exactly three hits across exactly two files.
    - Every one of those three hits names `tests/test_dispatch_mirror.py`, and none names
      `tools/wiki/dispatch_mirror.py`.
    - The SUMMARY states in its own words that these three are D-02's deliberate keeps and that they are the
      collision D-03's criterion amendment exists to handle.
    - `git status --porcelain` in `firestarter_app` is empty.
  </acceptance_criteria>
  <done>The app repository names no live guard that does not exist, the three citations that remain say what happened to their subject, and the suite still collects over a tree two fixtures lighter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a fixture header → the project's memory | These two fixtures are the only committed record of a finding that cost a phase to produce; their headers say so and tell readers not to delete them. |
| a provenance citation → a future deletion decision | The citation is the only answer to "why does the live lint guard the compound shape?". Without it, the compound arm looks unmotivated and gets removed. |
| a `dispatch_mirror` grep → a CLAIM-01 verdict | A bare substring collides across two unrelated deletions in two repositories; read literally it either fails on citations that are now correct, or passes on a claim that is not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-04-A | Repudiation | the two deleted fixtures | high | mitigate | The deletion is gated by a `<precondition>` that greps the verdict note for the finding and both fixture filenames, plus a post-deletion gate asserting the note still names them. Ordering (`184-03` before `184-04`), not diligence, is the guarantee. |
| T-184-04-B | Tampering | the three provenance citations | high | mitigate | Exact-count gates pin `test_dispatch_mirror` at 1 and 2 occurrences and require `39ea3e8` and `2026-08-31` alongside each. Stripping the name is called out as an explicit prohibition because it is the intuitive wrong move when staring at a grep. |
| T-184-04-C | Tampering | `planted_no_exists_proxy.py` as a control | high | mitigate | A gate counts its three planted subjects, `tests/test_check_no_exists_proxy.py` must pass, and an acceptance criterion requires the diff to touch no executable line. A fixture quietly "fixed" so the lint stops flagging it is a control that has become a rubber stamp. |
| T-184-04-D | Denial of service (broken suite) | modules referencing a deleted fixture | medium | mitigate | A pre-deletion `git grep` for `planted_dispatch` and a post-deletion `pytest --collect-only` over the whole tree. Collection is the check that actually catches an import-time or glob-time reference. |
| T-184-04-E | Spoofing (false clean scan) | the devcontainer's default `grep` | medium | mitigate | Every evidence search uses `git grep` (index-backed) or `/usr/bin/grep` by absolute path, and the action says why: the default `grep` is ugrep, honours `.gitignore`, and silently under-scans, which would return a comfortable zero for files that exist. |
| T-184-04-F | Information disclosure | deleted fixture content | low | accept | The fixtures hold protocol identifiers and a rewritten `0x10` to `0xFF` copy of a public dispatch table. Nothing sensitive; the content remains in git history by design. |
| T-184-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency; it deletes two files, edits prose in two more, and runs the existing `.venv311`. No Package Legitimacy Audit table is required, and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from the spec-less edge probe and NOT silently dropped.

- **CLAIM-02 / `unclassified` — FLAGGED, left `unresolved`.** The deterministic edge probe returned
  `unclassified` for CLAIM-02 with the probe text "unclassified — review manually". It is surfaced here
  rather than auto-resolved, per the fallback protocol, which forbids auto-resolving an `unclassified` row.
  The manual reading: CLAIM-02's deliverable is the DISPOSAL of two files, so the probe's shape categories
  (empty, adjacency, encoding, ordering, concurrency) have no data structure to apply to. That is a reason,
  not a dismissal. Its real failure mode is disposal that destroys knowledge, which is covered above by
  T-184-04-A and by the `must_haves` truth requiring the finding to be in the note at deletion time. A
  verifier that cannot confirm this from evidence should return `human_needed`, not a silent pass.
- **Flagged, plan-local:** the truth that "the app test suite still collects cleanly after the deletions" is
  a `backstop`-tier marker. Collection proves no module imports or globs the deleted paths; it does NOT
  prove the full suite passes, and this plan deliberately does not run the full suite because
  `/dev/ttyACM0` is attached and reddens the `test_no_programmer_found_*` characterization tests for an
  unrelated reason. A verifier should abstain rather than upgrade collection into a full-suite pass.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan:
- Nothing. No new file, no new symbol. A drift check will find no new identifier introduced here.

Deleted by this plan (a distinct list):
- `firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` — whole file.
- `firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp` — whole file.

Edited in place, prose only:
- `firestarter_app/tools/check_no_exists_proxy.py` — module docstring, the compound-shape bullet's citation.
- `firestarter_app/tests/fixtures/planted_no_exists_proxy.py` — module docstring item 2, and the existing
  `PLANTED VIOLATION (compound shape)` comment.

Kept deliberately, with the reason on the record:
- The module name `tests/test_dispatch_mirror.py` at all three citation sites — the only record of why the
  live lint guards the compound `not (a.exists() and b.exists())` shape.
- `planted_no_exists_proxy.py`'s three planted subjects and their scope discrimination — a deliberate
  control whose violations are supposed to be flagged.
</artifacts_this_phase_produces>

<verification>
- Both `planted_dispatch_*` fixtures are absent from disk and from the git index;
  `planted_no_exists_proxy.py` survives.
- `git grep 'planted_dispatch'` in the app repo returns nothing outside `.planning/`.
- `.planning/notes/dispatch-invariant-retirement-verdict.md` still names both deleted fixtures.
- `test_dispatch_mirror` appears exactly once in `tools/check_no_exists_proxy.py` and exactly twice in
  `tests/fixtures/planted_no_exists_proxy.py`, each accompanied by `39ea3e8` and `2026-08-31`.
- `git grep 'tools/wiki/dispatch_mirror'` in the app repo returns nothing.
- `pytest tests/ --collect-only` exits 0; `tests/test_check_no_exists_proxy.py`,
  `tests/test_sdp_honesty.py` and `tests/test_scan_paths_resolve.py` all pass.
- `git status --porcelain` in `firestarter_app` is empty, and all commits are on the milestone branch.
</verification>

<success_criteria>
CLAIM-02 is disposed by deletion rather than by a note promising someone will look at it, and the one thing
those files were really protecting was written down first, checked before the deletion ran. The app
repository's remaining `dispatch_mirror` occurrences are exactly the three D-02 keeps on purpose, each now
carrying the date and sha that make it verifiable — which is what turns an unverifiable citation into a
record.
</success_criteria>

<output>
Create `.planning/phases/184-guards-that-exist/184-04-SUMMARY.md` when done
</output>
