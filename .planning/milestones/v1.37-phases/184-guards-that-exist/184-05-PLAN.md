---
phase: 184-guards-that-exist
plan: 05
type: execute
wave: 3
depends_on: ["184-01", "184-02", "184-03", "184-04"]
files_modified:
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLAIM-01, CLAIM-02, CLAIM-03, CLAIM-09]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/workspaces/CLAUDE.md`'s claim that of `tools/wiki/` \"only `MIGRATION-TABLE.md` survives there\" is repaired: the file moved to `.planning/v1.35/MIGRATION-TABLE.md` on 2026-09-08 and `tools/wiki/` was removed entirely — `tools/` now holds `catalog/` alone. The retirement date, the sha `5426d7ef` and the bolded \"no automated wiki guard exists now\" all survive the repair (D-04)."
    - "Success criterion 1 in `.planning/ROADMAP.md` § Phase 184 is AMENDED in place, in Phase 183's shape — a bolded amendment clause carrying the reason on the record — to assert that no file in any of the three repositories names `tools/wiki/dispatch_mirror.py`, checked by `git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'` returning nothing in all three (D-03)."
    - "The amendment states why the original bare-substring command could not be used as written: `dispatch_mirror` collides across two unrelated deletions — `tools/wiki/dispatch_mirror.py` (meta, 2026-09-02, `5426d7ef`) and `tests/test_dispatch_mirror.py` (app, 2026-08-31, `39ea3e8`) — and three citations of the SECOND survive by D-02, each now naming its own deletion (D-03)."
    - "`git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'` returns nothing in all three repositories, and each of those three empty results is accompanied by a per-repository positive control proving the search actually scanned the tree rather than silently returning zero (CLAIM-01 edge: empty)."
    - "Every search pattern used to assert criterion 1 is ASCII-only, so byte equality and code-point equality coincide and no Unicode-normalization question arises — the patterns are `tools/wiki/dispatch_mirror`, `dispatch_mirror` and `tools/wiki`, none of which contains a character outside ASCII (CLAIM-01 edge: encoding)."
    - "Every search used as evidence is `git grep` (index-backed) or `/usr/bin/grep` by absolute path, never a bare `grep` — the devcontainer's default `grep` is ugrep, which honours `.gitignore` and silently under-scans, so a bare `grep` would be a gate that fails open."
    - "`.planning/REQUIREMENTS.md` marks CLAIM-01, CLAIM-02, CLAIM-03 and CLAIM-09 complete in both places they are tracked — the `### CLAIM` checkbox list and the `## Traceability` table — each with a one-line note naming where its evidence lives."
    - "Both submodule gitlinks are advanced in the meta repository by name: `git rev-parse HEAD:firestarter` equals `firestarter`'s own HEAD and `git rev-parse HEAD:firestarter_app` equals `firestarter_app`'s own HEAD. Both sub-repos are written in this phase, so both advance."
    - statement: "If this phase were interrupted between plans, every repository is left self-consistent, because each plan commits inside the repository it edits and criterion 1 is asserted across all three only here, after every sub-repo commit has landed (CLAIM-01 edge: concurrency)."
      verification: backstop
  prohibitions:
    - "MUST NOT widen CLAIM-01 into a general retired-checker sweep. D-01 measured it: of the eight files `5426d7ef` deleted (`wiki.py`, `honest01_claims.py`, `honest02_truth.py`, `provenance_footers.py`, `selftest.sh`, `claim-allowlist.json`, `claim-vocabulary.json`, `dispatch_mirror.py`), only `dispatch_mirror.py` was still named outside `.planning/` in any of the three repositories. The other seven are clean, and re-hunting them on the theory that more will turn up is scope this phase does not have."
    - "MUST NOT repair, strip or 'clean up' any `.planning/` citation of a retired `tools/wiki/` checker. The roughly 1,150 such citations are historical-by-intent — they are the retirement evidence, recorded as such in `.planning/todos/completed/migration-table-relocated-out-of-tools.md`. Every criterion-1 search in this plan carries `':(exclude).planning'` for exactly that reason."
    - "MUST NOT rewrite `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` wholesale, and MUST NOT reach for a `roadmap.*` or `requirements.*` tooling verb to make these edits. Those verbs reformat the whole file and have been observed to clobber the phase dependency table and prune unrelated config; `ROADMAP.md` is a ~6,300-line hand-authored file carrying every archived milestone and the whole `999.x` backlog. Use scoped in-place edits only."
    - "MUST NOT file a backlog item on the CLAIM-03 axis while ticking CLAIM-03 complete. The requirement is satisfied by the recorded verdict; D-06 files zero items here, and a 'follow-up' row added at close-out would reinstate exactly what the operator closed."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Meta-repo project instructions whose tools/wiki sentence is true again"
      contains: "MIGRATION-TABLE.md"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 184 with criterion 1 amended in place, with the reason on the record"
      contains: "AMENDED by Phase 184"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLAIM-01, CLAIM-02, CLAIM-03 and CLAIM-09 marked complete with their evidence named"
      contains: "CLAIM-09"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the amended criterion 1 narrows to CLAIM-01's own words — names tools/wiki/dispatch_mirror.py as a live guard — rather than to a bare substring"
      pattern: "tools/wiki/dispatch_mirror"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/notes/dispatch-invariant-retirement-verdict.md"
      via: "CLAIM-03's completion note cites the verdict document as where the decision is recorded"
      pattern: "CLAIM-03"
    - from: "meta repository HEAD"
      to: "firestarter and firestarter_app HEADs"
      via: "both gitlinks are advanced by name in the same close-out commit, because both sub-repos were written in this phase"
      pattern: "HEAD:firestarter"
---

<objective>
Close the phase in the meta repository: repair the last untrue sentence this phase found, amend the success
criterion whose literal command cannot answer the question it asks, and assert the result across all three
repositories with searches that cannot silently return zero.

Purpose: three things that only exist at phase level. D-04 folds in `/workspaces/CLAUDE.md:12`, whose
`tools/wiki/` claim has been false since 2026-09-08 — same category as CLAIM-01, found by the same sweep,
and invisible to criterion 1's grep because it names a directory rather than a checker, so a planner has to
carry it deliberately or it is lost. D-03 amends success criterion 1, because its literal command is a bare
substring that collides across two unrelated deletions in two repositories, and three citations of the
second survive on purpose. And the requirement ledger and both submodule gitlinks close out here, after
every sub-repo commit has landed.

This is the phase's record close-out. It depends on all four earlier plans because it asserts their combined
result.

Output: a true sentence in `CLAUDE.md`, an amended criterion with its reason recorded, four requirements
ticked, and two gitlinks advanced.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/184-guards-that-exist/184-CONTEXT.md
@.planning/phases/184-guards-that-exist/184-PATTERNS.md
@/workspaces/CLAUDE.md

Note on omissions: `.planning/PROJECT.md` and `.planning/STATE.md` are deliberately not referenced — they
are very large and nothing in this plan needs them. `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`
are EDITED by this plan but are deliberately not inlined here either; read the specific sections named in
each task's `<read_first>` with a scoped `sed -n` range instead of loading the whole file.
</context>

<failing_direction_convention>
Every `<automated>` command in this plan is a GREEN-direction command. Two of them use the
`command; test $? -ne 0` shape, where ABSENCE is the passing direction and their `<fails_when>` says so
explicitly. This plan has no RED-first leg; the RED-then-GREEN discipline lives in `184-01`.
</failing_direction_convention>

<tasks>

<task type="auto">
  <name>Task 1: Repair `CLAUDE.md`'s `tools/wiki/` claim, which criterion 1's grep cannot see (D-04)</name>
  <reversibility rating="reversible">One sentence in the meta repo's project instructions.</reversibility>
  <read_first>
    - `/workspaces/CLAUDE.md` — the whole `## Repository Structure` section, and specifically the paragraph
      whose second half retires the `tools/wiki/` checkers. You need all four of its claims in view: that
      the checkers were retired on 2026-09-02, the sha `5426d7ef`, that "only `MIGRATION-TABLE.md` survives
      there", and the bolded "**no automated wiki guard exists now**". Only the third is false.
    - The live filesystem, checked rather than assumed: what `/workspaces/tools/` actually contains, and
      whether `.planning/v1.35/MIGRATION-TABLE.md` exists. Run both before writing the repair.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-04 and D-01.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-PATTERNS.md` § "`/workspaces/CLAUDE.md:12` —
      repair the `tools/wiki/` claim (D-04)" and § "Prose citations state the disposal, not just the name".
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Repair ONLY the clause `only `MIGRATION-TABLE.md` survives there, as a record of the completed
    migration`. Everything else in that paragraph stays: the retirement date `2026-09-02`, the sha
    `5426d7ef` in backticks, and the bolded `**no automated wiki guard exists now**`. The repaired clause
    states what is actually true — the file moved to `.planning/v1.35/MIGRATION-TABLE.md` on 2026-09-08 and
    `tools/wiki/` was removed entirely, with `tools/` now holding `catalog/` alone.

    Verify both facts against the filesystem before writing them, and record the two commands in the
    SUMMARY. Writing a corrected date or a corrected path from CONTEXT.md without checking would reproduce
    the exact defect this requirement exists to remove.

    Use a scoped in-place edit. Do not rewrite the file and do not touch any other section — `CLAUDE.md` is
    this project's binding instruction file and an unrelated reflow in it is a change nobody asked for.

    Note for the record, in the SUMMARY: this repair is invisible to success criterion 1's search because it
    names `tools/wiki/` rather than `dispatch_mirror`. It is in this phase because D-04 folds it in as the
    same category — things the repo says that are not true — found by this phase's own D-01 sweep.

    D-01's fence applies here and is not to be crossed: do NOT sweep for the other seven files `5426d7ef`
    deleted. They were measured during discussion and are already clean outside `.planning/`.

    Commit in the meta repository, on `gsd/v1.37-operator-safety-answered-reports-claim-hygiene`.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c 'survives there' CLAUDE.md</automated>
    <fails_when>the printed count is anything other than `0` — the false clause survived the repair</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'v1.35/MIGRATION-TABLE.md' CLAUDE.md</automated>
    <fails_when>the printed count is `0` — the repair removed the false claim without stating where the file actually went, which is erasure rather than repair</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'no automated wiki guard exists now' CLAUDE.md; /usr/bin/grep -c '5426d7ef' CLAUDE.md; /usr/bin/grep -c '2026-09-02' CLAUDE.md</automated>
    <fails_when>any of the three printed counts is `0` — the repair collaterally removed the bolded absence statement, the retirement sha, or the retirement date, all three of which are true and must survive</fails_when>
    <automated>cd /workspaces && ls tools/</automated>
    <fails_when>the output lists anything other than `catalog` — the repaired sentence's claim about what `tools/` now holds would be false as written, and must be rewritten to match the filesystem rather than the filesystem being blamed</fails_when>
    <automated>cd /workspaces && test -f .planning/v1.35/MIGRATION-TABLE.md && echo MIGRATION_TABLE_AT_CLAIMED_PATH</automated>
    <fails_when>the word `MIGRATION_TABLE_AT_CLAIMED_PATH` is absent, or non-zero exit — the repaired sentence would be pointing at a path that does not exist, replacing one untrue claim with another</fails_when>
    <automated>git diff --stat "$(git merge-base origin/beta HEAD)" -- CLAUDE.md</automated>
    <fails_when>the diffstat reports more than 2 changed lines, or lists any file other than `CLAUDE.md` — this is a one-clause repair and a larger diff means an unrelated reflow landed in the project's instruction file. The merge-base anchor is exact here: `CLAUDE.md` was byte-identical to `origin/beta` at the start of this phase, so everything this range shows is this task's own edit</fails_when>
  </verify>
  <acceptance_criteria>
    - `survives there` appears zero times in `/workspaces/CLAUDE.md`.
    - `v1.35/MIGRATION-TABLE.md`, `2026-09-08`, `5426d7ef`, `2026-09-02` and
      `no automated wiki guard exists now` all appear in the repaired paragraph.
    - The repaired paragraph states that `tools/wiki/` was removed entirely and that `tools/` now holds
      `catalog/` alone.
    - The SUMMARY records the two filesystem checks — `ls tools/` and the existence of
      `.planning/v1.35/MIGRATION-TABLE.md` — with their output, showing the repair was measured rather than
      transcribed.
    - The diff for this commit, taken against the phase merge-base, touches only `CLAUDE.md` and only
      that clause's lines.
    - The SUMMARY notes that this repair is invisible to criterion 1's search, and why it is in this phase.
    - No search was run and no edit was made for the other seven files `5426d7ef` deleted (D-01's fence).
  </acceptance_criteria>
  <done>The project's own instruction file no longer tells its reader that a file survives in a directory that does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Amend success criterion 1 alongside the work, and assert it across all three repositories with searches that cannot fail open (D-03, D-02, CLAIM-01)</name>
  <reversibility rating="reversible">A criterion amendment in a markdown roadmap, with the reason recorded beside it.</reversibility>
  <precondition>`184-01`, `184-02` and `184-04` are all complete and committed inside their own repositories, so the cross-repository assertion below describes committed state rather than a working tree.</precondition>
  <read_first>
    - `/workspaces/.planning/ROADMAP.md` § `### Phase 184: Guards That Exist` — read only this section, with
      a scoped `sed -n` range; the file is ~6,300 lines and carries every archived milestone. You need
      criterion 1 verbatim and the four-criterion structure around it.
    - `/workspaces/.planning/ROADMAP.md` § `### Phase 183:` criterion 2 — the in-repo precedent for an
      amendment: a bolded `**AMENDED by Phase 183 alongside criterion 1 (D-08):**` clause that states what
      the original said, what changed, and why, without deleting the original intent. Read it with a scoped
      range too.
    - `/workspaces/.planning/REQUIREMENTS.md` § `### CLAIM` — CLAIM-01's own words, which are narrower than
      criterion 1's command: it forbids naming `tools/wiki/dispatch_mirror.py` as a LIVE GUARD.
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` D-02 and D-03 in full, plus
      § Canonical References for the Phase 182 criterion-4 precedent (a literal grep answering the wrong
      question).
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    TWO halves. Amend, then assert.

    AMEND. In `.planning/ROADMAP.md` § Phase 184, edit success criterion 1 in place, following Phase 183's
    shape exactly: keep the original criterion text visible, and append a bolded amendment clause —
    `**AMENDED by Phase 184 alongside the code (D-03):**` — carrying the reason on the record. The amendment
    must state all four of:
    1. The operative check: no file in any of the three repositories names `tools/wiki/dispatch_mirror.py`,
       verified by `git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'` returning nothing in
       all three.
    2. Why the original command could not be used as written: `dispatch_mirror` is a bare substring that
       collides across two unrelated deletions — `tools/wiki/dispatch_mirror.py` (meta repo, deleted
       2026-09-02 by `5426d7ef`) and `tests/test_dispatch_mirror.py` (app repo, deleted 2026-08-31 by
       `39ea3e8`).
    3. What survives and why: three citations of the SECOND module remain in the app repo by D-02 — one in
       `tools/check_no_exists_proxy.py`, two in `tests/fixtures/planted_no_exists_proxy.py` — each now
       naming its own deletion, because the module name is the only record of why the live lint guards the
       compound `not (a.exists() and b.exists())` shape.
    4. That CLAIM-01's own words are the narrower and correct target: it forbids naming
       `tools/wiki/dispatch_mirror.py` as a live guard, not any occurrence of the substring.

    Use a scoped in-place edit on the criterion's lines only. Do NOT use a `roadmap.*` tooling verb: those
    reformat the whole file and have been observed to clobber the phase dependency table. Do not touch any
    other phase's section, and do not touch the `999.x` backlog.

    ASSERT. Run, and paste every output into the SUMMARY, the per-repository pair below. Each repository gets
    the exact-pattern search AND a positive control, because an empty result from a search that never
    scanned anything is indistinguishable from an empty result from a clean tree — and the devcontainer's
    default `grep` is ugrep, which honours `.gitignore` and silently under-scans, so every search here is
    `git grep` or `/usr/bin/grep` by absolute path, never a bare `grep`.

    - meta (`/workspaces`): exact pattern returns nothing; control — `tools/wiki` still appears in at least
      one tracked file outside `.planning/` (it is in the repaired `CLAUDE.md` paragraph from Task 1).
    - firmware (`/workspaces/firestarter`): exact pattern returns nothing; control — `tools/wiki` still
      appears in at least one tracked file (it is in `PROTOCOLS.md`'s replacement line from `184-02`).
    - app (`/workspaces/firestarter_app`): exact pattern returns nothing; control — the bare substring
      `dispatch_mirror` still appears in exactly two files, which are D-02's deliberate keeps.

    State in the SUMMARY that every pattern used here is ASCII-only, so byte and code-point equality coincide
    and no normalization question arises.
  </action>
  <verify>
    <automated>cd /workspaces && git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — that means `git grep` FOUND the path, i.e. the meta repo still names the deleted checker outside `.planning/`. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces && git grep -l 'tools/wiki' -- . ':(exclude).planning'</automated>
    <fails_when>no output, or non-zero exit — the positive control for the gate above. An empty result here means the search matched nothing at all outside `.planning/`, so the empty result above proves nothing about the tree. `CLAUDE.md` names `tools/wiki/` after Task 1, so at least one path must print</fails_when>
    <automated>cd /workspaces/firestarter && git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the firmware repo still names the deleted checker. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter && git grep -l 'tools/wiki' -- . ':(exclude).planning'</automated>
    <fails_when>no output, or non-zero exit — the firmware positive control. `PROTOCOLS.md`'s replacement line from `184-02` names `tools/wiki/`, so an empty result means the search is not reading the tree</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the app repo still names the deleted checker. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -l 'dispatch_mirror' -- . ':(exclude).planning'</automated>
    <fails_when>the output is anything other than exactly the two lines `tests/fixtures/planted_no_exists_proxy.py` and `tools/check_no_exists_proxy.py` — the app positive control. An empty result means D-02's deliberate citations were stripped; a third path means a claim survived</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'AMENDED by Phase 184' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is `0` — criterion 1 was asserted but never amended, leaving a criterion on the record whose literal command contradicts the result</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^### Phase 999\.' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is anything other than `62`, the count before this phase — a backlog entry was added or removed while editing the roadmap</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` § Phase 184 criterion 1 carries a bolded `**AMENDED by Phase 184` clause stating
      all four required points: the operative check, the two-deletion collision with both shas and both
      dates, the three surviving D-02 citations and why they survive, and CLAIM-01's narrower wording.
    - `git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'` returns nothing in all three
      repositories, and all three outputs are pasted into the SUMMARY.
    - All three positive controls are pasted into the SUMMARY: `tools/wiki` present in at least one meta
      path, `tools/wiki` present in at least one firmware path, and `dispatch_mirror` present in exactly the
      two app-repo paths D-02 keeps.
    - The SUMMARY names which two app-repo files those are, and states they are D-02's deliberate keeps.
    - The SUMMARY states that every pattern used is ASCII-only.
    - No search used a bare `grep`; every one is `git grep` or `/usr/bin/grep` by absolute path, and the
      SUMMARY says so with the reason.
    - The `999.x` entry count in `ROADMAP.md` is unchanged at 62, and no phase section other than Phase 184
      was edited.
  </acceptance_criteria>
  <done>Criterion 1 now asks the question CLAIM-01 actually asks, the answer is no in all three repositories, and every empty search result is backed by a control proving the search happened.</done>
</task>

<task type="auto">
  <name>Task 3: Close the requirement ledger and advance both submodule gitlinks (CLAIM-01, CLAIM-02, CLAIM-03, CLAIM-09)</name>
  <reversibility rating="reversible">Checkbox and table edits in a tracked markdown file, plus two gitlink pointers that are re-advanceable at any time.</reversibility>
  <precondition>Both sub-repositories have their phase work committed on `gsd/v1.37-operator-safety-answered-reports-claim-hygiene` and are git-porcelain, so the gitlink this task records points at a real, complete commit rather than a mid-task state.</precondition>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` § `### CLAIM` — the four checkbox lines for CLAIM-01, CLAIM-02,
      CLAIM-03 and CLAIM-09, read with a scoped range.
    - `/workspaces/.planning/REQUIREMENTS.md` § `## Traceability` — the four rows for the same IDs, and the
      Phase 182/183 rows above them, which show the house format for a completion note (`Complete — <one
      line naming the evidence>`).
    - `/workspaces/.planning/phases/184-guards-that-exist/184-CONTEXT.md` § Integration Points — the gitlink
      practice: since v1.36 the submodule gitlink is advanced per-phase by name, and both sub-repos are
      written in this phase so both advance.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    TWO halves: the ledger, then the gitlinks.

    THE LEDGER. In `.planning/REQUIREMENTS.md`, mark all four requirements complete in BOTH places they are
    tracked — the `### CLAIM` checkbox list and the `## Traceability` table. Requirements have been observed
    in this project to be ticked in one place and left `Pending` in the other; check both.

    Each traceability row gets a one-line note naming where its evidence lives, in the house format the
    Phase 182 and 183 rows already use:
    - CLAIM-01 — repaired in all three repos; criterion 1 amended by D-03 (`184-05`), three D-02 citations
      kept and re-dated (`184-04`).
    - CLAIM-02 — both fixtures deleted (`184-04`); their fail-open finding preserved in
      `notes/dispatch-invariant-retirement-verdict.md` first (`184-03`).
    - CLAIM-03 — retired outright per D-05; verdict recorded in
      `notes/dispatch-invariant-retirement-verdict.md`; zero backlog items filed, deliberately (D-06).
    - CLAIM-09 — fail-closed check landed (`184-01`), proven RED on the real rotted entry before the entry
      was removed, then on three planted controls.

    Use scoped in-place edits. Do NOT use a `requirements.*` tooling verb — those reformat the whole file.

    Do NOT add a follow-up or open-question row for CLAIM-03 while closing it. D-06 files zero items on that
    axis, and a close-out row is exactly how a closed decision gets quietly reinstated.

    THE GITLINKS. From `/workspaces`, stage both submodule pointers BY NAME — `git add firestarter
    firestarter_app` — and commit them in the meta repository alongside this task's `REQUIREMENTS.md` edit.
    Both sub-repos were written in this phase (firmware in `184-02`, app in `184-01` and `184-04`), so both
    advance; this is the standing per-phase practice since v1.36 and it is an explicit step, not a side
    effect of some other command.

    Before committing, confirm both sub-repos are on the milestone branch and porcelain. A gitlink advanced
    onto a dirty or wrong-branch HEAD records a commit the phase did not produce.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(git rev-parse HEAD:firestarter)" = "$(git -C firestarter rev-parse HEAD)" && test "$(git rev-parse HEAD:firestarter_app)" = "$(git -C firestarter_app rev-parse HEAD)" && echo GITLINKS_ADVANCED</automated>
    <fails_when>the word `GITLINKS_ADVANCED` is absent from the output, or non-zero exit — at least one gitlink still points at a pre-phase commit</fails_when>
    <automated>cd /workspaces && git -C firestarter rev-parse --abbrev-ref HEAD; git -C firestarter_app rev-parse --abbrev-ref HEAD</automated>
    <fails_when>either printed branch name is anything other than `gsd/v1.37-operator-safety-answered-reports-claim-hygiene` — a gitlink was advanced onto a commit from the wrong branch</fails_when>
    <automated>cd /workspaces && git -C firestarter status --porcelain --untracked-files=no; git -C firestarter_app status --porcelain --untracked-files=no</automated>
    <fails_when>any output at all — a sub-repo has uncommitted tracked changes, so the gitlink records a commit that is not the phase's finished state</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^| CLAIM-0[1239] | Phase 184 | Pending' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than `0` — one of the four traceability rows is still marked `Pending`</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^| CLAIM-0[1239] | Phase 184 | Complete' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than `4` — fewer than all four rows were closed, or a row was duplicated</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^- \[ \] \*\*CLAIM-0[1239]\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than `0` — a `### CLAIM` checkbox is still unticked, the second place this project has been observed to leave a requirement half-closed</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^- \[ \] \*\*CLAIM-0[4567]\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than `4` — CLAIM-04 through CLAIM-07 belong to Phase 185 and must still be open; a count below 4 means this task ticked a requirement this phase did not deliver</fails_when>
  </verify>
  <acceptance_criteria>
    - All four of CLAIM-01, CLAIM-02, CLAIM-03 and CLAIM-09 are ticked in the `### CLAIM` checkbox list AND
      marked `Complete` in the `## Traceability` table, each with a one-line evidence note.
    - CLAIM-04 through CLAIM-07 are still unticked — this task closed only what this phase delivered.
    - No follow-up, open-question or successor row was added for CLAIM-03.
    - `git rev-parse HEAD:firestarter` equals `firestarter`'s HEAD, and `git rev-parse HEAD:firestarter_app`
      equals `firestarter_app`'s HEAD; the SUMMARY records all four shas.
    - Both sub-repos are on `gsd/v1.37-operator-safety-answered-reports-claim-hygiene` and porcelain, with
      the checking commands recorded in the SUMMARY.
    - The gitlink advance is its own named step in the commit, not an incidental `git add -A`.
    - The diff for `REQUIREMENTS.md` touches only the four checkbox lines and the four traceability rows —
      no whole-file reformat. Anchor it on this task's own commit sha rather than on the phase merge-base:
      `REQUIREMENTS.md` already carries 187 insertions and 169 deletions relative to `origin/beta` from this
      milestone's own authoring, so a merge-base range here would answer the wrong question.
  </acceptance_criteria>
  <done>Four requirements are closed in both ledgers with their evidence named, and the meta repository points at the exact sub-repo commits this phase produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an empty search result → a CLAIM-01 verdict | The devcontainer's default `grep` is ugrep and honours `.gitignore`; an empty result from a search that never scanned is indistinguishable from a clean tree unless a control runs beside it. |
| a success criterion → a verifier | A criterion whose literal command contradicts the phase's own correct result either fails a clean phase or trains the next reader to ignore criteria. |
| the meta repo's gitlinks → what the phase actually shipped | A gitlink advanced onto a dirty or wrong-branch HEAD records a commit the phase did not produce, and every later audit anchors on it. |
| a close-out edit → a hand-authored 6,300-line roadmap | A whole-file rewrite destroys the dependency table and the `999.x` backlog outside the diff window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-184-05-A | Spoofing (false clean scan) | the criterion-1 searches | high | mitigate | Every repository gets an exact-pattern search AND a positive control, and every search is `git grep` or `/usr/bin/grep` by absolute path. An empty result with no control is explicitly not accepted as evidence. |
| T-184-05-B | Tampering | `.planning/ROADMAP.md` | high | mitigate | Scoped in-place edits only; `roadmap.*` verbs are an explicit prohibition; the `999.x` entry count is pinned at 62 before and after, and an acceptance criterion requires no other phase section to change. |
| T-184-05-C | Repudiation | the amended criterion | high | mitigate | The amendment keeps the original text visible and records the reason beside it, in Phase 183's shape. A criterion silently swapped for an easier one is indistinguishable from a criterion that was met. |
| T-184-05-D | Tampering | the submodule gitlinks | high | mitigate | The advance is a named step; branch and porcelain are asserted on both sub-repos before the commit; all four shas are recorded. |
| T-184-05-E | Elevation of privilege | closing CLAIM-03 | medium | mitigate | An explicit prohibition against adding a follow-up row while ticking it complete, plus the `999.x` count gate from Task 2. D-06's zero-filings rule is easiest to break at close-out, when a "tracked follow-up" looks like diligence. |
| T-184-05-F | Repudiation | the requirement ledger | medium | mitigate | Both places a requirement is tracked are gated separately, and CLAIM-04 through CLAIM-07 are pinned still-open so the close-out cannot overreach into Phase 185's scope. |
| T-184-05-G | Information disclosure | the historical `.planning/` citations | low | accept | Roughly 1,150 `.planning/` citations of retired checkers are preserved deliberately as retirement evidence; every criterion-1 search carries `':(exclude).planning'` and an explicit prohibition against repairing them. |
| T-184-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency; it edits three markdown files and runs `git` and `grep`. No Package Legitimacy Audit table is required, and none is fabricated. |
</threat_model>

<flagged_assumptions>
Surfaced from the spec-less edge probe and NOT silently dropped. This plan owns CLAIM-01's three edges.
`empty` and `encoding` are resolved explicitly into `must_haves.truths` above; `concurrency` is authored as
a `backstop`-tier marker.

- **CLAIM-01 / `concurrency` — resolved BACKSTOP, flagged here so a verifier does not upgrade it.** The
  statement is that an interruption between plans leaves every repository self-consistent, because each plan
  commits inside the repository it edits and the cross-repository assertion runs only here. That reasoning is
  sound but it is NOT exercised — no plan in this phase is actually interrupted to test it. A verifier that
  cannot confirm it with explicit evidence must abstain to `human_needed`, never pass it silently.
- **Flagged, plan-local:** the `999.x` count of 62 was measured at planning time on 2026-09-11. If any other
  workstream files a backlog item between planning and execution, the gate will fire for a correct reason
  that is not this phase's fault. The executor should re-measure at the start of Task 2, record the
  pre-task count in the SUMMARY, and compare against that rather than against 62 if they differ — saying so
  explicitly rather than quietly relaxing the gate.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan:
- Nothing new as a file or symbol. Three existing tracked files are edited in place.

Deleted by this plan (a distinct list):
- `/workspaces/CLAUDE.md`'s clause "only `MIGRATION-TABLE.md` survives there, as a record of the completed
  migration" — replaced by what is actually true, not merely removed.

Edited in place:
- `/workspaces/CLAUDE.md` — one clause in the `## Repository Structure` paragraph.
- `.planning/ROADMAP.md` — success criterion 1 of § Phase 184 only.
- `.planning/REQUIREMENTS.md` — four `### CLAIM` checkboxes and four `## Traceability` rows.

Advanced by this plan:
- The `firestarter` and `firestarter_app` submodule gitlinks in the meta repository, by name.

Deliberately NOT created:
- No `999.x` backlog entry, no follow-up row for CLAIM-03, no sweep for the other seven files `5426d7ef`
  deleted (D-01, D-06).
</artifacts_this_phase_produces>

<verification>
- `survives there` appears zero times in `/workspaces/CLAUDE.md`; `v1.35/MIGRATION-TABLE.md`, `2026-09-08`,
  `5426d7ef`, `2026-09-02` and `no automated wiki guard exists now` all appear.
- `ls tools/` lists `catalog` alone, and `.planning/v1.35/MIGRATION-TABLE.md` exists.
- `.planning/ROADMAP.md` § Phase 184 criterion 1 carries an `AMENDED by Phase 184` clause with the
  two-deletion reason; the `999.x` entry count is unchanged at 62.
- `git grep -n 'tools/wiki/dispatch_mirror' -- . ':(exclude).planning'` returns nothing in all three repos,
  each with its positive control pasted into the SUMMARY.
- All four CLAIM rows in `.planning/REQUIREMENTS.md` read `Complete`, their `### CLAIM` checkboxes are
  ticked, and CLAIM-04 through CLAIM-07 are still open.
- `git rev-parse HEAD:firestarter` and `HEAD:firestarter_app` equal their sub-repos' HEADs; both sub-repos
  are on the milestone branch and porcelain.
</verification>

<success_criteria>
The phase closes with nothing left implicit: the last untrue sentence this phase found is repaired and
measured rather than transcribed; the success criterion that could not answer its own question is amended
in the same phase as the work, with the collision that broke it recorded beside it; every empty search
result carries a control proving the search happened; and the meta repository points at exactly the
sub-repo commits this phase produced.
</success_criteria>

<output>
Create `.planning/phases/184-guards-that-exist/184-05-SUMMARY.md` when done
</output>
