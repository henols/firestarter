---
phase: 183-flash4-erase-refusal-the-ae29f2008-classification
plan: 05
type: execute
wave: 3
depends_on: ["183-04"]
files_modified:
  - firestarter/scripts/check_erase_no_vpp.py
  - firestarter/tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp
  - firestarter/PROTOCOLS.md
  - firestarter/CLAUDE.md
autonomous: true
requirements: [SAFE-08]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/check_erase_no_vpp.py`'s docstring no longer cites a line range that cannot exist, and cites by symbol and enclosing scope so the next source sweep cannot stale it again (D-14 job 1)."
    - "The checker's `proximity, not absence` rationale is updated to record that the copy-source hazard is GONE — the 12 V routine it warned about being copied no longer exists in the tree — while the gate stays armed and functionally unchanged (D-14 job 2)."
    - "The checker still passes for the right reason: `scripts/check_erase_no_vpp.py` exits 0 on the real `eeprom28c_erase_execute` body, and its paired pytest still drives the planted fixture to exit 1 (a real hazard token) rather than exit 2 (fail-closed)."
    - "The identical impossible line citation carried a second and third time — in `tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp` — is repaired by the same rule, and the fixture's violation-path behaviour is unchanged."
    - "`PROTOCOLS.md`'s `0x05` erase-model paragraph no longer claims a capability-gated bulk erase exists and is deliberately unadvertised — there is no bulk erase left to route to (D-12)."
    - "`firestarter/CLAUDE.md`'s `0x0D` warning no longer tells a reader not to reach for a function that does not exist, while the `FLAG_CAN_ERASE` exclusion for algorithm 5 and the reason for it are preserved (D-04)."
    - "A post-deletion cold build of uno, uno328pb and leonardo records the firmware shrink against `183-01`'s pre-deletion figures for the same envs."
    - "`check_size_baseline.py` in DEFAULT mode diverges against the recorded baseline (exit 1, naming the observed-versus-baseline figures), and in `--policy merge05` mode against BASE-01 it passes — so the shrink authors NO new MERGE-05 exemption and CLAIM-05's no-new-exemption clause survives (D-15)."
  prohibitions:
    - "`scripts/baseline/size_baseline.json` MUST NOT be re-recorded or edited by this phase. Re-recording it here would make Phase 185's CLAIM-04 gate green against a baseline this phase wrote, which is the opposite of what that requirement asks."
    - "The checker's non-vacuity anchor MUST NOT be loosened, re-scoped, or exempted while repairing its prose. The prose is what is false; the mechanism is correct and `0x0D`-scoped."
  artifacts:
    - path: "firestarter/scripts/check_erase_no_vpp.py"
      provides: "The 0x0D erase no-VPP gate, with a docstring that describes the tree that exists"
      contains: "eeprom28c_erase_execute"
    - path: "firestarter/PROTOCOLS.md"
      provides: "The 0x05 erase-model paragraph, true after the deletion"
      contains: "0x05"
    - path: "firestarter/CLAUDE.md"
      provides: "The 0x0D algorithm-handler warning, without a pointer to a deleted symbol"
      contains: "FLAG_CAN_ERASE"
  key_links:
    - from: "firestarter/scripts/check_erase_no_vpp.py"
      to: "firestarter/src/proms/eeprom_28c.cpp"
      via: "the brace-matched body scan of eeprom28c_erase_execute, with its two non-vacuity anchors"
      pattern: "eeprom28c_erase_execute"
    - from: "firestarter/tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp"
      to: "firestarter/tests/test_check_erase_no_vpp.py"
      via: "the planted violation the paired pytest drives the checker against, expecting exit 1"
      pattern: "planted_erase_no_vpp_ctrl_write"
---

<objective>
Make the four documents that describe the deleted 12 V erase path stop describing it, and record what the
deletion did to the firmware budget.

Purpose: SAFE-08's blast radius. `check_erase_no_vpp.py` cites `flash_5v_page.cpp` lines 196-231 in a
225-line file — a range that cannot exist — and rests its whole rationale on that function being present
in the tree (D-14). The identical impossible citation appears twice more in a planted test fixture.
`PROTOCOLS.md` states that a capability-gated bulk erase exists and is deliberately unadvertised, which
`183-04` made false. `firestarter/CLAUDE.md` warns readers not to reach for a symbol that is gone.
D-15's Phase 185 dependency rests on a shrink nobody has yet measured.

This is expansion work after `183-04`'s deletion; the phase's tracer slice is `183-03` Task 1.

Output: four repaired documents, a measured shrink, and a recorded observation of exactly which gate the
shrink reddens and which it does not.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-PATTERNS.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair check_erase_no_vpp.py's docstring and the planted fixture's prose (D-14)</name>
  <precondition>`183-04` has landed, so `flash_5v_page_erase_execute` is already absent from the tree and the repaired rationale describes reality rather than a prediction.</precondition>
  <read_first>
    - `/workspaces/firestarter/scripts/check_erase_no_vpp.py` — the module docstring in full. Three
      regions matter: the paragraph beginning `**Proximity, not absence, is the risk.**` with its
      `flash_5v_page.cpp` `lines 196-231` citation; the `**Non-vacuity anchor -- but only on the PASS
      path.**` paragraph, which is `0x0D`-scoped and must be left mechanically intact; and the
      `**No CI leg.**` standing disclosure, which must survive.
    - `/workspaces/firestarter/tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp` — the file-header
      comment carrying the same `flash_5v_page.cpp:196-231` citation, and the in-body
      `PLANTED VIOLATION` comment naming `flash_5v_page_erase_execute`. Note the checker scans comments
      INSIDE the matched function body deliberately, so the in-body comment's hazard-token content is
      load-bearing for the fixture's verdict.
    - `/workspaces/firestarter/tests/test_check_erase_no_vpp.py` — the paired pytest, to see which exit
      code it expects from the planted fixture and which from the clean path.
    - `/workspaces/firestarter/tests/test_boolean_convention_source_contract_v133.py` — the citation
      discipline to adopt: assert and cite on the enclosing function name and the literal, never on a
      line number, because later phases move the file again.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md`
      § C.6 — both D-14 claims confirmed by running the gate, and the confirmation that only the prose is
      false.
  </read_first>
  <files>firestarter/scripts/check_erase_no_vpp.py, firestarter/tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp</files>
  <action>
    JOB 1 — the impossible citation. In `check_erase_no_vpp.py`'s module docstring, remove the
    `flash_5v_page.cpp` line-range citation entirely. Do not replace it with a corrected range: the
    CLAIM-06 lesson, which this repository has now recorded five separate times, is that a line citation
    stales on the next edit. Cite by symbol and enclosing scope, or — where the symbol itself no longer
    exists — describe the shape structurally without naming a file position.

    JOB 2 — the rationale. The paragraph's argument is that proximity, not absence, is the risk: the
    hazardous shape "already exists in this tree today" in a file an executor also edits, so a
    copy-paste mistake is live. After `183-04` that is no longer true — the copy source is gone. Rewrite
    the paragraph to record that the copy-source hazard has been REMOVED from the tree, while stating
    plainly that the gate stays armed: the hazardous SHAPE is still describable and still reachable by
    someone re-deriving it from the AT28C256 datasheet's hardware chip-erase mode, so a body-scoped
    negative scan of `eeprom28c_erase_execute` is still the control. Keep the paragraph's other true
    content: why the scan is body-scoped rather than a whole-file zero-occurrence grep, and why
    `eeprom_28c.cpp` legitimately carries the same tokens in `eeprom28c_check_chip_id`.

    KEEP MECHANICALLY INTACT: the `--function` and `--source` defaults, the brace-matching logic, the
    comment-scanning behaviour, the two non-vacuity anchors, the exit-code taxonomy, and the `No CI leg`
    disclosure. Nothing about the gate's behaviour changes in this task — only the prose that was false.
    This module is a `scripts/` gate, not product source, and its docstring is the artifact under repair;
    do not add any new `#` commentary to it.

    THE FIXTURE, SECOND AND THIRD SITES. In `tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp`, the
    file-header comment carries the identical impossible `flash_5v_page.cpp:196-231` citation and the
    in-body `PLANTED VIOLATION` comment names `flash_5v_page_erase_execute` as the lift source. Remove
    the file path and line range from the header, and re-express both by the structural shape — a
    VPP/VPE control-register write bracketed by `rurp_chip_enable()` / `rurp_chip_disable()` — rather
    than by a symbol that no longer exists. This is a CONTRACTION of false prose, not new commentary.

    CRITICAL CONSTRAINT ON THE FIXTURE EDIT: the checker scans comments inside the matched function body
    on purpose, because at grep distance a comment naming a hazard token is indistinguishable from a
    call. Do not add or remove any hazard-token literal inside the function body — including inside the
    in-body comment — or the fixture's verdict changes and the paired pytest's exit-1 expectation breaks
    for a reason that has nothing to do with this phase. The fixture must still drive the checker to
    exit 1.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_erase_no_vpp.py; echo "rc=$?"</automated>
    <fails_when>the output does not begin `PASS:` or the printed `rc=` is not `0`. `rc=1` means a real hazard token was found in the scanned body; `rc=2` means fail-closed — a missing file, an unresolvable function, unbalanced braces, or a failed non-vacuity anchor. The two codes mean different things and both are failures here</fails_when>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_check_erase_no_vpp.py -q</automated>
    <fails_when>non-zero exit, or the summary reports a failed count above 0, or reports `no tests ran` — the paired pytest is the only thing that proves the planted fixture still drives the checker to exit 1 rather than exit 2</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n '196-231' scripts/check_erase_no_vpp.py tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — that means `grep` FOUND the impossible range still present in one of the two files. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'flash_5v_page' scripts/check_erase_no_vpp.py tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — a reference to the deleted flash4 handler survives in one of the two files. The passing direction here is ABSENCE</fails_when>
  </verify>
  <acceptance_criteria>
    - `196-231` appears in neither `scripts/check_erase_no_vpp.py` nor
      `tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp`.
    - `flash_5v_page` appears in neither file, in any form.
    - The checker's docstring still contains its `No CI leg` disclosure and its non-vacuity-anchor
      paragraph, and still names `eeprom28c_erase_execute` and `delay(AT28C_TEC_MAX_MS)` as the two
      anchors.
    - `python3 scripts/check_erase_no_vpp.py` prints a `PASS:` line and exits 0.
    - `python3 -m pytest tests/test_check_erase_no_vpp.py -q` passes with a non-zero test count.
    - The repaired rationale states that the copy-source has been removed from the tree AND that the gate
      stays armed, naming what it still guards against.
    - `git diff $(git merge-base origin/beta HEAD) -- tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp`
      shows changes only inside comment text — no executable line of the fixture changed.
  </acceptance_criteria>
  <done>Two files stop citing a range that cannot exist and a function that no longer does, the gate still passes for the right reason, and the planted fixture still proves the checker can see a real hazard.</done>
</task>

<task type="auto">
  <name>Task 2: Bring PROTOCOLS.md and firestarter/CLAUDE.md current (D-12, D-04)</name>
  <precondition>`183-04` has landed, so both documents are being corrected against a tree that already lacks the routine they describe.</precondition>
  <read_first>
    - `/workspaces/firestarter/PROTOCOLS.md` — the `0x05` section's **Erase model** paragraph, and the
      **VPP behavior** sentence immediately after it. The erase-model paragraph's final clause claims a
      capability-gated bulk erase exists and is deliberately unadvertised; the VPP sentence
      ("None (5V-only operation)") becomes MORE true after the deletion and needs no edit — it is the
      sentence the record should point at.
    - `/workspaces/firestarter/CLAUDE.md` — the § "Algorithm Handlers" `0x0D` warning, the paragraph
      telling a reader not to "optimise" the erase by reaching for the flash4 12 V path, and the
      `0x05` row of the handler table.
    - `/workspaces/firestarter/src/proms/flash_5v_page.cpp` — as `183-04` left it, so the rewrite
      describes the file that exists.
    - `/workspaces/firestarter_app/firestarter/database.py` — `convert_to_programmer`'s `algo not in (5,)`
      exclusion. The exclusion SURVIVES and its reason must survive with it: the host still clears
      `FLAG_CAN_ERASE` for algorithm 5, and the firmware still refuses (D-04). What changed is that there
      is no longer a 12 V routine waiting behind the flag.
  </read_first>
  <files>firestarter/PROTOCOLS.md, firestarter/CLAUDE.md</files>
  <action>
    Rewrite the two false passages, and only those.

    `PROTOCOLS.md`, the `0x05` **Erase model** paragraph. Its final clause — that the capability-gated
    bulk erase "is unchanged and remains deliberately unadvertised by the host: setting `FLAG_CAN_ERASE`
    for algorithm 5 would route a 12 V bulk erase onto this 5V-only part" — is false after the deletion.
    Replace it with what is true now: the firmware implements no chip-level erase for this protocol at
    all; the host continues to clear `FLAG_CAN_ERASE` for algorithm 5, and the firmware's `eprom_erase`
    continues to refuse, so a standalone erase is refused twice over rather than routed anywhere. Keep
    the paragraph's first half — the implicit per-page auto-erase, and the no-blank-check statement from
    Phase 153 — which remains true. State that the silicon's own software chip-erase sequence is a
    recorded backlog item rather than a firmware capability, and cite the backlog entry `183-02` filed,
    so this paragraph does not read as a claim that the part cannot be erased at all.

    Leave the **VPP behavior** sentence alone.

    `firestarter/CLAUDE.md`, the `0x0D` warning. Its "Do not 'optimise' the erase by reaching for
    `flash_5v_page_erase_execute` in `flash_5v_page.cpp` — that IS the 12 V path" clause points at a
    symbol that no longer exists. Replace the pointer with the hazard itself: the AT28C256 datasheet's
    hardware Chip Erase mode requires 12 V on OE, which is a hardware-damage hazard on a 5 V part, and
    `scripts/check_erase_no_vpp.py` is the gate that enforces its absence from `eeprom28c_erase_execute`.
    PRESERVE the second half of the clause: algorithm 5 keeps its `FLAG_CAN_ERASE` exclusion, and the
    reason it keeps it. D-04 makes that exclusion permanent, and a reader who takes the deletion as a
    reason to set the flag would be undoing this phase.

    Check the `0x05` row of the handler table and the handler-dispatch list for any claim the deletion
    falsified, and correct only what is false.

    Cite by symbol and enclosing scope throughout. Do not introduce a line-number citation into either
    document.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'flash_5v_page_erase_execute' PROTOCOLS.md CLAUDE.md; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the deleted symbol is still named in one of the two documents. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'deliberately unadvertised' PROTOCOLS.md; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the false clause about an unadvertised bulk erase survives. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'FLAG_CAN_ERASE' CLAUDE.md</automated>
    <fails_when>no output and non-zero exit — the `FLAG_CAN_ERASE` exclusion and its reason were deleted along with the false pointer, which would remove the standing D-04 guidance rather than repair it</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'None (5V-only operation)' PROTOCOLS.md</automated>
    <fails_when>no output and non-zero exit — the VPP-behavior sentence, which this task was told to leave alone, was edited or removed</fails_when>
  </verify>
  <acceptance_criteria>
    - `flash_5v_page_erase_execute` appears in neither `PROTOCOLS.md` nor `firestarter/CLAUDE.md`.
    - The phrase `deliberately unadvertised` no longer appears in `PROTOCOLS.md`.
    - `PROTOCOLS.md`'s `0x05` erase-model paragraph still states the implicit per-page auto-erase and the
      Phase 153 no-blank-check fact, and now states that the firmware implements no chip-level erase for
      this protocol and that the host clears `FLAG_CAN_ERASE` while the firmware also refuses.
    - `PROTOCOLS.md`'s `0x05` **VPP behavior** sentence `None (5V-only operation)` is byte-unchanged.
    - `firestarter/CLAUDE.md` still contains `FLAG_CAN_ERASE` and still states why algorithm 5 keeps its
      exclusion.
    - Neither document gained a `file.cpp:NN` style line citation.
    - `git diff $(git merge-base origin/beta HEAD) --stat -- PROTOCOLS.md CLAUDE.md` shows changes to
      exactly these two files and no others.
  </acceptance_criteria>
  <done>Neither document tells a reader about a function that no longer exists, and neither has lost the standing guidance that algorithm 5 keeps its `FLAG_CAN_ERASE` exclusion for a reason the deletion did not remove.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the shrink and record which gate it reddens (D-15)</name>
  <precondition>`183-01` Task 2's pre-deletion per-env cold-build figures are recorded, so the shrink has a denominator; and `183-04` has landed, so the tree being measured is the post-deletion one.</precondition>
  <read_first>
    - `/workspaces/firestarter/scripts/check_size_baseline.py` — the module docstring's two modes and its
      exit-code taxonomy (0 clean, 1 divergence or zero-envs-compared, 2 unparseable log or malformed
      CLI), the `--avr-log ENV=PATH` / `--baseline PATH` / `--policy merge05` argument handling, the four
      `MERGE05_*_EXEMPTION_BYTES` literals, and `_merge05_flash_allowance`'s band arithmetic including
      the `band = 0 if env == "leonardo"` line.
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — `avr_targets`, `native_envs`,
      `meta.deltas_vs_base01` and the leonardo `merge05_clause`. Read it; do NOT edit it.
    - `/workspaces/firestarter/scripts/baseline/size_baseline_base01.json` — the BASE-01 anchor
      `--policy merge05` compares against.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-01-SUMMARY.md`
      — the pre-deletion per-env `flash_used` figures this task subtracts from.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md`
      § A.3 and § C.7 — the measured position against BASE-01 (uno -1872, uno328pb -1874,
      leonardo -1808), the 724 B of named exemptions, and the confirmation that a bare invocation exits 1
      by the never-vacuous guard.
  </read_first>
  <files>(read-only assertions over firestarter/scripts/baseline/size_baseline.json — this task edits no tracked file)</files>
  <action>
    Measure what the deletion cost — negatively — and state precisely which gate that reddens.

    COLD-BUILD ALL THREE TARGETS, post-deletion, using D-16's procedure exactly as `183-01` did:
    `rm -rf .pio/build/<env>` then exactly one `pio run -e <env>`, for `uno`, `uno328pb` and `leonardo`,
    capturing each log. Using the same procedure on both sides is what makes the subtraction honest.

    RECORD THE SHRINK per env: post-deletion `flash_used` minus `183-01`'s pre-deletion `flash_used` for
    THAT env. Also record the post-deletion position against BASE-01, so the MERGE-05 reasoning is
    visible: the tree was already 1872 / 1874 / 1808 bytes below BASE-01 before this phase, with 724 B of
    named exemptions stacked on top and unused.

    RUN THE DEFAULT-MODE GATE against the recorded baseline, supplying the three AVR logs. Expect exit 1
    with divergence lines naming observed versus baseline per env — that is the CORRECT, PREDICTED
    outcome of a byte-identity gate after a firmware change, and it is exactly what D-15 says makes Phase
    185's CLAIM-04 re-record depend on this phase. Distinguish it in the record from exit 1's other
    meaning (zero envs compared) and from exit 2 (unparseable log or malformed CLI): quote the actual
    message.

    RUN THE BAND-MODE GATE against BASE-01, supplying the same three logs with
    `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json`. Expect exit 0. A shrink
    stays inside the allowance with room to spare, so it authors NO new MERGE-05 exemption and CLAIM-05's
    no-new-exemption clause survives. If it does not exit 0, stop and report — that would falsify D-15's
    reasoning and needs adjudication, not a workaround.

    DO NOT EDIT `size_baseline.json`. Re-recording it is Phase 185's CLAIM-04/05 work, and doing it here
    would make that phase's gate green against a baseline this phase wrote.

    CARRY FORWARD THE CASE-COUNT FINDING from `183-04` Task 3: the native case count moved 184 to 185 on
    both native envs, and `check_size_baseline.py` asserts `cases` EXACTLY. So Phase 185's re-record has
    TWO inputs from this phase, not one — the flash shrink and the native case count. D-15's text names
    only the flash shrink. State both in the SUMMARY so `183-06`'s ROADMAP amendment can name both.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/uno && pio run -e uno 2>&1 | tee /tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno.log | grep -E '^(RAM|Flash):'</automated>
    <fails_when>non-zero exit from `pio run`, or no line beginning `Flash:` appears — nothing to read a post-deletion figure from</fails_when>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/uno328pb && pio run -e uno328pb 2>&1 | tee /tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno328pb.log | grep -E '^(RAM|Flash):'</automated>
    <fails_when>non-zero exit from `pio run`, or no line beginning `Flash:` appears</fails_when>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/leonardo && pio run -e leonardo 2>&1 | tee /tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-leonardo.log | grep -E '^(RAM|Flash):'</automated>
    <fails_when>non-zero exit from `pio run`, or no line beginning `Flash:` appears</fails_when>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno.log --avr-log uno328pb=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno328pb.log --avr-log leonardo=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-leonardo.log; echo "rc=$?"</automated>
    <fails_when>the printed `rc=` is `0` (the byte-identity gate did not notice a firmware change, which would mean the deletion did not alter any image), or `rc=2` (a log could not be parsed or the CLI was malformed — a tool failure, not a measurement), or the output contains `no envs compared`. The EXPECTED, passing observation here is `rc=1` with per-env divergence lines naming observed versus baseline figures</fails_when>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno.log --avr-log uno328pb=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-uno328pb.log --avr-log leonardo=/tmp/claude-1000/-workspaces/b9c32896-5382-4936-aad9-490d7be1657f/scratchpad/183-post-leonardo.log; echo "rc=$?"</automated>
    <fails_when>the printed `rc=` is anything other than `0` — a shrink that falls outside MERGE-05's allowance against BASE-01 would falsify D-15's no-new-exemption reasoning and must be adjudicated, not worked around</fails_when>
    <automated>cd /workspaces/firestarter && git status --porcelain --untracked-files=no -- scripts/baseline/</automated>
    <fails_when>any output at all — a baseline file was modified, which is Phase 185's work and not this phase's</fails_when>
  </verify>
  <acceptance_criteria>
    - Three post-deletion cold-build logs exist, one per env, each carrying a `Flash:` line.
    - The SUMMARY records, per env, the post-deletion `flash_used`, the pre-deletion `flash_used` from
      `183-01`, and the shrink as their difference — computed within an env, never across envs.
    - The SUMMARY records the post-deletion position against BASE-01 for all three envs.
    - The default-mode gate run is recorded with its exit code and its actual divergence message, and the
      SUMMARY states explicitly that exit 1 here means DIVERGENCE and not the zero-envs-compared case.
    - The `--policy merge05` run against BASE-01 is recorded as exit 0, and the SUMMARY states that no
      new MERGE-05 exemption was authored, so CLAIM-05's clause survives.
    - The SUMMARY names BOTH inputs to Phase 185's re-record: the flash shrink and the native case count
      moving 184 to 185.
    - `git status --porcelain --untracked-files=no -- scripts/baseline/` produces no output.
  </acceptance_criteria>
  <done>The shrink is measured with the same cold procedure on both sides, the byte-identity gate is observed to diverge for the predicted reason, the band gate is observed to pass, and Phase 185 inherits two named inputs instead of one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate docstring → the next engineer | A checker's rationale is what tells a reader whether a hazard is live; a false rationale mis-directs the next person editing the erase path. |
| planted fixture → the checker's own verdict | The fixture is the only proof the checker can see a real hazard; its in-body comment text is inside the scanned span. |
| build log → `check_size_baseline.py` → the merge decision | Flash figures gate whether a change may land on boards with a real Caterina cliff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-05-A | Tampering | `tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp` | high | mitigate | The checker scans comments inside the matched body deliberately; an edit that adds or removes a hazard-token literal there silently changes the fixture's verdict and can turn the gate's only non-vacuity proof into a pass-by-accident. The action forbids touching hazard-token content, and the paired pytest plus a comments-only diff criterion are the guards. |
| T-183-05-B | Repudiation | `scripts/check_erase_no_vpp.py` docstring | medium | mitigate | A rationale that still claims a live copy-source would be a documented falsehood in a safety gate. The repair is required to state both halves — hazard source removed, gate still armed — so the reader gets the true reason it exists rather than a stale one. |
| T-183-05-C | Denial of service (physical) | `firestarter/CLAUDE.md`'s `FLAG_CAN_ERASE` guidance | high | mitigate | If the repair deletes the exclusion's rationale along with the dead-symbol pointer, a future reader could set `FLAG_CAN_ERASE` for algorithm 5 believing the hazard is gone. An acceptance criterion asserts `FLAG_CAN_ERASE` and its reason survive in that document (D-04). |
| T-183-05-D | Tampering | `scripts/baseline/size_baseline.json` | medium | mitigate | Asserted unmodified by a porcelain check over `scripts/baseline/`. Re-recording it here would make Phase 185's CLAIM-04 gate green against a baseline written by the phase that caused the divergence. |
| T-183-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase. No Package Legitimacy Audit table is required. |
</threat_model>

<verification>
- `python3 scripts/check_erase_no_vpp.py` prints `PASS:` and exits 0; its paired pytest passes.
- `196-231` and `flash_5v_page` appear in neither the checker nor the planted fixture.
- `flash_5v_page_erase_execute` and `deliberately unadvertised` appear in neither `PROTOCOLS.md` nor
  `firestarter/CLAUDE.md`; `FLAG_CAN_ERASE` still appears in `CLAUDE.md`.
- Three post-deletion cold-build logs exist; the default-mode gate exits 1 with a divergence message and
  the `--policy merge05` gate against BASE-01 exits 0.
- `git status --porcelain --untracked-files=no -- scripts/baseline/` is empty.
</verification>

<success_criteria>
Every document in SAFE-08's blast radius describes the tree that exists; the `0x0D` erase gate still
passes for the right reason and still proves it can see a real hazard; and the firmware shrink is measured
with the same cold procedure on both sides, with the gate it reddens and the gate it does not both
observed rather than predicted.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: no new symbol and no new file. Three post-deletion cold-build logs are produced
under the scratch directory as working artifacts.

Deleted by this plan (a distinct list):
- The `flash_5v_page.cpp` `lines 196-231` citation from `scripts/check_erase_no_vpp.py`'s docstring.
- The identical citation from `tests/fixtures/planted_erase_no_vpp_ctrl_write.cpp`'s file header, and the
  `flash_5v_page_erase_execute` name from its in-body provenance comment.
- The `deliberately unadvertised` bulk-erase clause from `PROTOCOLS.md`'s `0x05` erase-model paragraph.
- The `flash_5v_page_erase_execute` pointer from `firestarter/CLAUDE.md`'s `0x0D` warning.

Kept deliberately: `PROTOCOLS.md`'s `0x05` **VPP behavior** sentence, the checker's non-vacuity anchors
and `No CI leg` disclosure, and `firestarter/CLAUDE.md`'s `FLAG_CAN_ERASE` exclusion and its reason.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-05-SUMMARY.md` when done
</output>