---
phase: 183-flash4-erase-refusal-the-ae29f2008-classification
plan: 06
type: execute
wave: 4
depends_on: ["183-01", "183-02", "183-03", "183-04", "183-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SAFE-06, SAFE-07, SAFE-08, SAFE-09]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` § SAFE-06 names the part and states that the cause and the alternative are NOT carried in the CLI — it no longer says the refusal states its cause and its alternative (D-07, D-08)."
    - "`.planning/ROADMAP.md` Phase 183's **Goal** no longer says the refusal `says the reason`; it says the refusal names the part, and the reason is recorded and answered to the reporter rather than printed (D-08)."
    - "`.planning/ROADMAP.md` Phase 183 success criterion 1 says the refusal names the part in place of a bare `Not supported`, with no cause clause and no alternative command (D-08)."
    - "Each amended passage carries a pointer to where the cause and the alternative went — Phase 187's REPLY-03 — so the amendment reads as a relocation rather than as a quiet scope cut (D-09)."
    - "`.planning/ROADMAP.md` Phase 185's `**Depends on:**` line names Phase 183, replacing the current `independent` claim, and names BOTH inputs: the firmware flash shrink and the native test case count moving 184 to 185 (D-15)."
    - "SAFE-06, SAFE-07, SAFE-08 and SAFE-09 are each marked complete in `.planning/REQUIREMENTS.md` only after every plan that carries them has landed, each with a traceability pointer to the artifact that satisfies it."
    - "The phase's four adjudications are recorded in one place: SAFE-07's chosen mechanism and its grounds, SAFE-08's deletion and the option-(a) disposition of the emptied `flash_5v_page_write_init`, SAFE-09's equivalence-based verdict, and D-13's precise unreachability statement."
  prohibitions:
    - "The amended SAFE-06 text MUST NOT quietly narrow the requirement into something the phase happened to build. It must state the operator's decision and name where the cause and the alternative went, so a later reader can see a relocation rather than a dropped promise."
    - "MUST NOT run `roadmap.update-plan-progress` or any other roadmap-mutating CLI verb on this file. That verb overwrites the dependency table positionally and has previously destroyed a phase name and its requirement IDs. Hand edits only."
    - "A requirement checkbox MUST NOT be marked complete while any plan carrying it is unlanded. Multi-plan requirements in this phase are SAFE-06 (plans 03 and 06) and SAFE-08 (plans 04 and 05)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAFE-06 amended to D-07's wording, and SAFE-06..SAFE-09 marked complete with traceability"
      contains: "SAFE-06"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 183's amended Goal and criterion 1, and Phase 185's new dependency on Phase 183"
      contains: "Phase 185"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "SAFE-06's amended wording and Phase 183's amended Goal and criterion 1 must say the same thing"
      pattern: "SAFE-06"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-05-SUMMARY.md"
      via: "Phase 185's dependency on 183 is grounded in the measured shrink and the native case count recorded there"
      pattern: "Phase 185"
---

<objective>
Make three records describe the tool this phase actually shipped, and make Phase 185's dependency line
describe the tree this phase actually left.

Purpose: D-08 is a DELIVERABLE, not a side effect. The operator chose D-07's one-line refusal knowingly,
with the conflict against SAFE-06's own words stated on the record before the choice. Three documents
still carry the superseded wording, and if they are not amended in the same phase as the code, the
records describe a tool that does not exist. D-15 adds a fourth edit: Phase 185 currently claims to be
independent, and this phase's firmware change reddens the byte-identity gate CLAIM-04 re-records.

This plan is the phase's record close-out. It runs last so every amendment can cite a landed artifact
rather than an intention.

Output: an amended SAFE-06, an amended Phase 183 Goal and criterion 1, an amended Phase 185 dependency
line, and one consolidated record of the phase's four adjudications.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md
@/workspaces/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend SAFE-06 and Phase 183's Goal and criterion 1 to D-07's wording (D-08)</name>
  <precondition>`183-03` has landed, so the amended text describes a CLI that exists and can be read out of the shipped `flash4_erase_gate.refusal_text` rather than predicted.</precondition>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` § SAFE-06 — the current text, verbatim. It is in the SAFE
      block near the head of the file; read the surrounding SAFE-05 and SAFE-07 entries too, for the
      register and the checkbox convention.
    - `/workspaces/.planning/ROADMAP.md` Phase 183's section — its `**Goal**:` paragraph and its
      numbered success criteria. Locate it with `grep -n '^### Phase 183' .planning/ROADMAP.md`; do NOT
      read the whole 909 KB file.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md`
      § D-08 — the three-row table naming each document, its current text and its target wording. That
      table is the specification for this task.
    - `/workspaces/firestarter_app/firestarter/flash4_erase_gate.py` — the shipped refusal text, so the
      amended records quote what the tool prints rather than what the plan intended it to print.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Amend three passages by hand, with scoped edits. Do not rewrite either file wholesale, and do not run
    any roadmap-mutating CLI verb — `roadmap.update-plan-progress` overwrites the dependency table by
    positional overwrite and has previously destroyed a phase name and its requirement IDs.

    `.planning/REQUIREMENTS.md` § SAFE-06. Replace the clause "states its cause and its alternative — the
    part self-erases per page during the write, so `erase` is unnecessary rather than unavailable" with
    wording that says: the refusal NAMES THE PART, and the cause and the alternative are deliberately NOT
    carried in the CLI. Keep the "instead of a bare `Not supported`" contrast, which is still the point.
    Add a short trailing note recording that this text was AMENDED by Phase 183 under D-07/D-08, that the
    operator made the choice with the conflict against the original wording stated first, and that the
    cause and the alternative moved to REPLY-03 (D-09). Follow the precedent already set in this file by
    SAFE-01 through SAFE-04, which carry their own amendment-and-evidence trails inline.

    `.planning/ROADMAP.md` Phase 183 **Goal**. Replace "A refusal that is right for a reason says the
    reason" with wording that says the refusal names the part, and that the reason is recorded and
    answered to the reporter rather than printed. Keep the second half of the Goal — the open question
    behind gh#62 getting a recorded answer instead of an assumption — which this phase delivered in
    `.planning/notes/ae29f2008-classification-verdict.md`.

    `.planning/ROADMAP.md` Phase 183 success criterion 1. Replace "names the cause (a page-write part
    self-erases during the write) and the alternative (`write` directly) — not a bare `Not supported`"
    with a criterion that says `firestarter erase` on a flash4 part prints one line naming the part, in
    place of a bare `Not supported`, carrying no cause and no alternative command. Quote the shipped line
    shape so the criterion is checkable against the tool.

    In each of the three amended passages, name Phase 187's REPLY-03 as where the cause and the
    alternative went. Without that pointer the amendment reads as a scope cut rather than as the
    relocation it is.

    Do not touch any other requirement, any other phase section, the Phase 183 plan checklist, or the
    backlog entries `183-02` added.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -n 'self-erases per page during the write' .planning/REQUIREMENTS.md; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the superseded SAFE-06 cause clause is still present. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'says the reason' .planning/ROADMAP.md; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — the superseded Phase 183 Goal clause survives. The passing direction here is ABSENCE</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'REPLY-03' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is `0` — SAFE-06's amendment does not point at where the cause and the alternative went, so it reads as a dropped promise</fails_when>
    <automated>cd /workspaces && git diff --numstat -- .planning/ROADMAP.md</automated>
    <fails_when>the deleted-line count exceeds the added-line count by more than a handful, or the diff touches a line outside the Phase 183 section — a scoped three-line amendment must not remove bulk content from a 909 KB file</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` § SAFE-06 no longer contains the phrase `self-erases per page during the
      write`, and now states that the refusal names the part and that the cause and alternative are not
      carried in the CLI.
    - SAFE-06 carries an inline amendment note naming D-07/D-08, the operator's knowing choice, and
      REPLY-03 as the destination of the cause and the alternative.
    - `.planning/ROADMAP.md` Phase 183's Goal no longer contains `says the reason`.
    - Phase 183 success criterion 1 no longer contains the words `cause` or `alternative` as things the
      CLI prints, and instead states the one-line, part-naming shape.
    - All three amended passages name REPLY-03.
    - `git diff -- .planning/ROADMAP.md` touches only lines inside the Phase 183 section.
    - `git diff -- .planning/REQUIREMENTS.md` touches only the SAFE-06 entry.
  </acceptance_criteria>
  <done>The three documents D-08 names now describe the refusal the operator chose and this phase shipped, and each says where the cause and the alternative went.</done>
</task>

<task type="auto">
  <name>Task 2: Make Phase 185 depend on Phase 183, naming both inputs (D-15)</name>
  <precondition>`183-04` and `183-05` have landed, so the shrink and the native case count are measured facts recorded in their SUMMARYs rather than predictions.</precondition>
  <read_first>
    - `/workspaces/.planning/ROADMAP.md` Phase 185's section — its five success criteria and the
      `**Depends on:** — (independent; CLAIM-09's guard from 184 does not gate this)` line that follows
      them. Locate it with `grep -n '^### Phase 185' .planning/ROADMAP.md`; do not read the whole file.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-05-SUMMARY.md`
      — the measured per-env shrink and the observed default-mode divergence.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-04-SUMMARY.md`
      — the native case count moving 184 to 185 on both native envs, and the note that
      `check_size_baseline.py` asserts `cases` exactly.
    - `/workspaces/.planning/REQUIREMENTS.md` § CLAIM-04 and § CLAIM-05 — what Phase 185 re-records and
      what its no-new-exemption clause claims.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Replace Phase 185's `**Depends on:**` line with one that names Phase 183 as a dependency, by hand,
    with a scoped edit. Do not run `roadmap.update-plan-progress`.

    The new line must carry BOTH inputs, not one. CONTEXT.md's D-15 reasoned only about flash bytes:
    `check_size_baseline.py`'s default mode is a byte-identity gate, so Phase 183's firmware shrink
    reddens it, and CLAIM-04 re-records the baseline against a cold rebuild. This phase found a SECOND
    input that D-15 does not name: `183-04` added one native test case, so both `native` and
    `native_nodevtools` move from 184 cases to 185, and the same script asserts `cases` EXACTLY against
    `size_baseline.json`'s `native_envs` block. A re-record that picks up only the flash figures would
    leave that gate red.

    State both, with their measured values from the two SUMMARYs. Preserve the existing clause about
    CLAIM-09's guard from Phase 184 not gating Phase 185 — that is still true and independent.

    Also state, in one clause, that the shrink authors NO new MERGE-05 exemption — measured in `183-05`
    by a `--policy merge05` run against BASE-01 that exited 0 — so CLAIM-05's no-new-exemption clause
    survives and Phase 185 does not need to author one.

    Do not edit Phase 185's Goal, its success criteria, or its plan checklist. Do not edit any other
    phase section.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -n -A1 '^### Phase 185' .planning/ROADMAP.md | head -5</automated>
    <fails_when>no output and non-zero exit — the Phase 185 heading is gone, which would mean the edit removed or renamed the section instead of amending one line inside it</fails_when>
    <automated>cd /workspaces && awk '/^### Phase 185/,/^### Phase 186/' .planning/ROADMAP.md | /usr/bin/grep -n 'Depends on'</automated>
    <fails_when>no output and non-zero exit — the Phase 185 section has no `Depends on` line at all, meaning the amendment deleted it rather than replacing it</fails_when>
    <automated>cd /workspaces && awk '/^### Phase 185/,/^### Phase 186/' .planning/ROADMAP.md | /usr/bin/grep 'Depends on' | /usr/bin/grep -q 'Phase 183' && echo DEPENDS_ON_183</automated>
    <fails_when>`DEPENDS_ON_183` is absent — Phase 185's dependency line does not name Phase 183, so the record still claims an independence the tree contradicts</fails_when>
    <automated>cd /workspaces && awk '/^### Phase 185/,/^### Phase 186/' .planning/ROADMAP.md | /usr/bin/grep 'Depends on' | /usr/bin/grep -qE '18[45]' && echo BOTH_INPUTS_PRESENT</automated>
    <fails_when>`BOTH_INPUTS_PRESENT` is absent — the dependency line does not carry the native case-count figures, so Phase 185 inherits only the flash half of the problem</fails_when>
  </verify>
  <acceptance_criteria>
    - Phase 185's `**Depends on:**` line names Phase 183.
    - The line states both inputs: the firmware flash shrink and the native case count moving 184 to 185
      on both `native` and `native_nodevtools`.
    - The line states that no new MERGE-05 exemption is authored, citing the `--policy merge05` run
      against BASE-01 that exited 0.
    - The existing clause about CLAIM-09's guard from Phase 184 not gating Phase 185 is preserved.
    - Phase 185's Goal, its five success criteria and its plan checklist are byte-unchanged.
    - `git diff -- .planning/ROADMAP.md` for this task touches only lines inside the Phase 185 section.
  </acceptance_criteria>
  <done>Phase 185 no longer claims to be independent of the phase that reddened its gate, and it inherits both of the reasons rather than one.</done>
</task>

<task type="auto">
  <name>Task 3: Close SAFE-06..SAFE-09 with traceability and one consolidated decision record</name>
  <precondition>Every plan in this phase except this one has landed and produced its SUMMARY, so a completion mark reflects delivered work rather than an intention.</precondition>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` — the SAFE block's checkbox convention and the
      traceability register SAFE-01 through SAFE-05 already use (`Trace:` pointers to SUMMARY files and
      to notes).
    - All five sibling SUMMARYs in
      `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/` — `183-01`
      through `183-05`. These carry the figures, the verdict and the dispositions this task records.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md`
      § D-13 — the exact form the unreachability statement must take.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    First, AUDIT. Check whether any earlier plan's executor already flipped a SAFE-06..SAFE-09 checkbox.
    SAFE-06 spans plans 03 and 06; SAFE-08 spans plans 04 and 05. A checkbox flipped before its last plan
    landed is a premature mark — this project has recorded that failure mode before. If you find one,
    record it in the SUMMARY as a finding, then leave the checkbox in its now-correct state.

    Then mark SAFE-06, SAFE-07, SAFE-08 and SAFE-09 complete, each with a `Trace:` pointer naming the
    artifact that satisfies it:
    - SAFE-06 → `183-03-SUMMARY.md` and the shipped `flash4_erase_gate.py` plus
      `tests/test_flash4_erase_gate.py`.
    - SAFE-07 → `183-01-SUMMARY.md`'s three-row pricing table.
    - SAFE-08 → `183-04-SUMMARY.md` and `183-05-SUMMARY.md`.
    - SAFE-09 → `.planning/notes/ae29f2008-classification-verdict.md` and `183-02-SUMMARY.md`.

    Then record the phase's four adjudications in one place, in this plan's SUMMARY, so a later reader
    does not have to reassemble them from five files:

    1. SAFE-07's chosen mechanism — M3, the host pre-flight policy gate — with the measured M1 per-env
       firmware delta, the two structural zeros, and the grounds: D-02 eliminates M1 and M2 because both
       fire after `Connecting... OK`, and the flash figures did NOT decide it (leonardo had headroom).
    2. SAFE-08's disposition — removed, not kept. Plus the sub-adjudication CONTEXT.md left open: the
       emptied `flash_5v_page_write_init` is KEPT with its init-pointer assignment (option (a)), because
       nulling the pointer segfaults an unguarded call in the same test file this phase repaired, and
       `configure_sram` is the in-tree precedent for a body that does effectively nothing.
    3. SAFE-09's verdict — classification CORRECT, equivalence-based, reporter also correct — and the
       fact that D-21's misclassification machinery was held ready and deliberately not used.
    4. D-13's precise unreachability statement, in its required form: the arm's ASSIGNMENT ran on every
       flash4 INIT via `configure_memory`; it is the FUNCTION POINTER it installed that was never
       invoked, because `eprom_erase` returns at the `FLAG_CAN_ERASE` check before
       `op_execute_simple_operation` is reached. Not "the arm never runs".

    Hand edits only. Do not run a requirements-mutating CLI verb — those reformat the whole file.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -cE '^- \[x\] \*\*SAFE-0[6-9]\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is anything other than `4` — one of SAFE-06 through SAFE-09 is still unchecked, or a checkbox outside that range was altered</fails_when>
    <automated>cd /workspaces && git diff --numstat -- .planning/REQUIREMENTS.md</automated>
    <fails_when>the numstat shows a change count comparable to the file's total line count — a requirements-mutating verb reformatted the whole file instead of a scoped hand edit being applied</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -cE '^- \[ \] \*\*REPLY-0' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is not the pre-edit count — a REPLY requirement was marked complete by this phase, which it has no standing to do; REPLY-03 belongs to Phase 187</fails_when>
  </verify>
  <acceptance_criteria>
    - SAFE-06, SAFE-07, SAFE-08 and SAFE-09 are each marked `[x]` in `.planning/REQUIREMENTS.md`, and each
      carries a `Trace:` pointer to the artifact that satisfies it.
    - No REPLY-* or CLAIM-* requirement checkbox changed state.
    - The SUMMARY records all four adjudications, with SAFE-07's figures, SAFE-08's option-(a) reason,
      SAFE-09's equivalence framing, and D-13's statement in its required form.
    - The SUMMARY records the premature-mark audit result explicitly — either "no checkbox was flipped
      early" or the finding, named.
    - `git diff -- .planning/REQUIREMENTS.md` is a scoped edit, not a whole-file reformat.
  </acceptance_criteria>
  <done>The four requirements are closed against delivered artifacts, and the phase's adjudications are readable in one place instead of spread across five SUMMARYs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` records → the next planner and the operator | These documents are what a later phase plans against; a record that describes a tool that does not exist produces work against a fiction. |
| Phase 183's record edits → Phase 185's gate | An unamended dependency line lets Phase 185 believe its baseline gate is green for reasons this phase invalidated. |
| requirement checkbox → milestone close | A checkbox is what a close audit reads to decide whether a promise was kept. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-06-A | Repudiation | `.planning/REQUIREMENTS.md` § SAFE-06 | high | mitigate | An unamended SAFE-06 would let a later reader believe the CLI states its cause and its alternative when it deliberately does not — and an executor of a future phase could "fix" the CLI to match. The amendment is a required deliverable with its own acceptance criteria, and each amended passage names REPLY-03 so the relocation is visible. |
| T-183-06-B | Tampering | `.planning/ROADMAP.md` | high | mitigate | A whole-file rewrite or a roadmap-mutating CLI verb would clobber the dependency table and the 5,400-line hand-authored content. Hand edits only, asserted by scoped-diff criteria and by a numstat check that a 909 KB file did not lose bulk content. |
| T-183-06-C | Repudiation | SAFE-06..SAFE-09 checkboxes | medium | mitigate | A checkbox marked before its last plan lands makes a milestone close audit read a promise as kept. This task runs last, audits for early marks explicitly, and records the audit result either way. |
| T-183-06-D | Denial of service | Phase 185's CLAIM-04 gate | medium | mitigate | If the dependency line names only the flash shrink, Phase 185 re-records the flash figures and leaves the native case count red, which reads as a new defect rather than as this phase's known consequence. Both inputs are required in the line. |
| T-183-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase. No Package Legitimacy Audit table is required. |
</threat_model>

<verification>
- `self-erases per page during the write` appears nowhere in `.planning/REQUIREMENTS.md`.
- `says the reason` appears nowhere in `.planning/ROADMAP.md`.
- Phase 185's `**Depends on:**` line names Phase 183 and both inputs.
- SAFE-06 through SAFE-09 are `[x]` with `Trace:` pointers; no REPLY or CLAIM checkbox moved.
- Both `.planning/` diffs are scoped, with no bulk deletion from either file.
</verification>

<success_criteria>
Every record this phase touched describes what the phase shipped: SAFE-06 and Phase 183's Goal and
criterion 1 match the one-line refusal the operator chose and say where the cause went; Phase 185 knows
it depends on this phase and why, in both of its senses; and the four requirements are closed against
named artifacts rather than against intent.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan: no new file and no new symbol. The consolidated four-adjudication record lives in
`183-06-SUMMARY.md`.

Deleted by this plan (a distinct list):
- SAFE-06's `states its cause and its alternative` clause and its `self-erases per page during the write`
  explanation, from `.planning/REQUIREMENTS.md`.
- Phase 183's `says the reason` clause, from `.planning/ROADMAP.md`.
- Phase 183 success criterion 1's `names the cause ... and the alternative` clause.
- Phase 185's `— (independent; ...)` dependency claim.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-06-SUMMARY.md` when done
</output>
