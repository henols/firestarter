---
phase: 183-flash4-erase-refusal-the-ae29f2008-classification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/ae29f2008-classification-verdict.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SAFE-09]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A durable, citable record states whether AE29F2008 is classified `algorithm 5` correctly, and states its verdict as EQUIVALENCE-BASED — never as a direct AE29F2008 datasheet reading (D-18)."
    - "The record carries the equivalence chain explicitly: the pinned upstream `infoic.xml` ASD row, the field-for-field-identical upstream WINBOND `W29C020,W29C020C,W29C022` row sharing `chip_id=\"0x0000da45\"`, and the W29C020C datasheet facts that confirm each DB field (D-18, D-19)."
    - "The record states that BOTH the classification and the gh#62 reporter are correct at once — the `0x05` firmware path simply does not implement the chip-erase this silicon supports — and shows the `0x06` (AMD unlock) chip-erase byte sequence matching the datasheet's own six-cycle table (D-19)."
    - "`classify()`'s flash-family branch is re-verified in this session to return `proto_id` unchanged for `{0x05, 0x06, 0x0D, 0x10}`, so `algorithm: 5` is an upstream transcription and there is no decode step in `build_db.py` to correct (D-19, D-21)."
    - "`chip_database.json` is byte-unchanged by this plan and no rule is added to `build_db.py` — D-21's misclassification branch does not fire on this evidence (D-21, activation decision D-6)."
    - "`support_status` for AE29F2008 remains `supported` and is not edited (D-22)."
    - "D-23 is settled in the record as NOT a hardware-live defect on algorithm 5, with the traced reason (`handle->vpp_mv` has no reader in `flash_5v_page.cpp`; the one DB-level VPP gate excludes `configure_flash_5v_page` by name), and the residual display-labelling question is FILED as a backlog item rather than fixed here (D-23)."
    - "The software chip-erase for the `0x05` family is recorded and BACKLOGGED with its template and its boot-block caveat, not built (D-20)."
  prohibitions:
    - "MUST NOT hand-edit `firestarter_app/firestarter/data/chip_database.json`, and MUST NOT add a `build_db.py` rule or run a regeneration on this evidence — the file is generated, a hand edit is silently reverted by the next regeneration, and a rule authored against a correct upstream transcription would corrupt 746 rows to fix nothing."
    - "The SAFE-09 verdict MUST NOT be stated as if a primary AE29F2008 datasheet had been read. Presenting an equivalence-based conclusion as a direct vendor reading is the exact failure gh#62's open question was about."
  artifacts:
    - path: ".planning/notes/ae29f2008-classification-verdict.md"
      provides: "The SAFE-09 recorded verdict and its evidence chain, citable by Phase 187's REPLY-03"
      min_lines: 60
      contains: "equivalence"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog entries 999.63-999.66 filed by this phase"
      contains: "999.63"
  key_links:
    - from: ".planning/notes/ae29f2008-classification-verdict.md"
      to: "firestarter_app/tools/build_db.py"
      via: "classify()'s flash-family pass-through — the reason algorithm 5 is a transcription, not a decision"
      pattern: "classify"
    - from: ".planning/notes/ae29f2008-classification-verdict.md"
      to: "firestarter/include/flash_utils.h"
      via: "FLASH_ERASE's six byte_flip_t cycles, matched against the W29C020C software chip-erase table"
      pattern: "FLASH_ERASE"
---

<objective>
Answer SAFE-09 with a recorded, evidence-grounded verdict, and file the four follow-on items this phase
surfaces but deliberately does not build.

Purpose: gh#62's open question is whether AE29F2008 is classified `algorithm 5` at all. RESEARCH.md
settled it on every leg and the D-19/D-20 branch is taken — so SAFE-09's deliverable is a RECORD, not a
code change. D-21's misclassification branch does not fire: no `build_db.py` rule, no `diff_db.py` run, no
regeneration, no `DECODE-NOTES.md` edit, no `chip_database.json` diff. D-22 keeps `support_status`
untouched. D-23 is settled as a display-labelling question filed at a wider scope than this row.

This plan contains NO implementation task and no source edit in either sub-repo. The phase's tracer slice
is `183-03` Task 1.

Output: `.planning/notes/ae29f2008-classification-verdict.md` — the artifact Phase 187's REPLY-03 cites —
and ROADMAP backlog entries 999.63 through 999.66.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md
@.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md
@/workspaces/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify SAFE-09's load-bearing in-repo evidence in this session</name>
  <read_first>
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md`
      § B in full — the DB row, the upstream `infoic.xml` ASD and WINBOND rows, the W29C020C datasheet
      extracts, the gh#62 transcript, and § B.7's `vpp_mv` call-chain trace.
    - `/workspaces/firestarter_app/tools/build_db.py` — `classify()`, specifically the flash-family
      branch that returns `proto_id` unchanged for `{0x05, 0x06, 0x0D, 0x10}`, and `MINIPRO_XML_URL`'s
      pinned commit near the head of the file.
    - `/workspaces/firestarter_app/firestarter/data/chip_database.json` — vendor `ASD`, `part_number`
      `AE29F2008`, and vendor `WINBOND`, `part_number` `W29C020,W29C020C,W29C022`. Read the two rows, do
      not load the whole file into context by hand — extract them with a short script.
    - `/workspaces/firestarter/include/flash_utils.h` — the `FLASH_ERASE` `byte_flip_t` array.
    - `/workspaces/firestarter_app/tools/check_dispatch.py` — the `configure_flash_5v_page` entry in the
      family VPP-invariant table and the `_DB_CHECKED_VPP_INVARIANTS` scope note immediately above it.
  </read_first>
  <files>(no file is edited by this task — it produces re-verified evidence for Task 2)</files>
  <action>
    Re-derive, in this session, the four in-repo facts the SAFE-09 verdict rests on. RESEARCH.md verified
    all of them, but line citations stale in this repository repeatedly (CLAIM-06, D-14, D-17 are three
    recorded instances), so the record must cite what is true now, by SYMBOL and enclosing scope rather
    than by line number wherever a symbol exists.

    Fact 1 — the DB rows. Extract the `ASD` / `AE29F2008` row and the `WINBOND` /
    `W29C020,W29C020C,W29C022` row from `chip_database.json` and confirm they agree on `algorithm`,
    `chip_id_value`, `size_bytes`, `infoic_page_size_raw` and `pinout`. Confirm `AE29F2008`'s
    `support_status` reads `supported`.

    Fact 2 — the transcription. Confirm `classify()`'s flash-family branch returns `proto_id` unchanged
    for the `{0x05, 0x06, 0x0D, 0x10}` set, so `algorithm: 5` is upstream's `protocol_id="0x05"` carried
    through with no decode step that could be wrong. Record the pinned `MINIPRO_XML_URL` commit sha as
    the provenance anchor. Do NOT re-fetch the 17 MB upstream file — RESEARCH.md fetched it this week,
    recorded its sha256, and quoted both rows verbatim; the pin is a commit, so it cannot drift.

    Fact 3 — the `0x06` explanation. Confirm `FLASH_ERASE` in `flash_utils.h` is the six-cycle
    `{0x5555,0xAA} {0x2AAA,0x55} {0x5555,0x80} {0x5555,0xAA} {0x2AAA,0x55} {0x5555,0x10}` sequence, and
    that `flash_nor_unlock_erase_execute` emits it for a zero address. That is what makes the reporter's
    `erase SST39SF020 --force` at 0.14 s a REAL erase of this silicon rather than a fluke.

    Fact 4 — D-23. Confirm by a tree-wide scan (use `/usr/bin/grep -rn`, NOT the devcontainer's default
    `grep`, which is ugrep and honours `.gitignore`) that `vpp_mv` has no reader in
    `firestarter/src/proms/flash_5v_page.cpp`, and confirm `check_dispatch.py` excludes
    `configure_flash_5v_page` from the DB-level VPP invariant by name with the WP-pin reason recorded
    beside it.

    Record each fact with the command that produced it. Where RESEARCH.md's citation has moved, record
    the new location and note the drift — do not silently copy a line number forward.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "import json;d=json.load(open('firestarter/data/chip_database.json'));rows=[r for v in d.values() if isinstance(v,list) for r in v if r.get('part_number')=='AE29F2008'];assert len(rows)==1,rows;r=rows[0];assert r['programming']['algorithm']==5,r;assert r['programming']['chip_id_value']=='0x0000da45',r;assert r['support_status']=='supported',r;print('AE29F2008_ROW_OK')"</automated>
    <fails_when>`AE29F2008_ROW_OK` is absent, or an AssertionError traceback appears — the row is missing, duplicated, or one of `algorithm`/`chip_id_value`/`support_status` does not read `5`/`0x0000da45`/`supported`</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -n '0x05, 0x06, 0x0D, 0x10' tools/build_db.py</automated>
    <fails_when>no output and non-zero exit — the flash-family set literal has moved or changed shape, so the "returns proto_id unchanged" claim must be re-derived from the current `classify()` body before the record cites it</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n 'vpp_mv' src/proms/flash_5v_page.cpp; test $? -ne 0</automated>
    <fails_when>exit 0 from the `test` — that means `grep` FOUND `vpp_mv` in `flash_5v_page.cpp`, i.e. the field DOES have a reader on algorithm 5 and D-23's new-finding branch fires after all. The passing direction here is the symbol being ABSENT</fails_when>
    <automated>cd /workspaces/firestarter && /usr/bin/grep -n '0x5555, 0x10' include/flash_utils.h</automated>
    <fails_when>no output and non-zero exit — the final cycle of the software chip-erase sequence is not where the record claims, so the datasheet match must be re-derived before it is written down</fails_when>
  </verify>
  <acceptance_criteria>
    - The four facts each have a recorded command and its observed output in the SUMMARY.
    - The AE29F2008 and W29C020C DB rows are shown to agree on `algorithm` (5), `chip_id_value`
      (`0x0000da45`), `size_bytes` (262144), `infoic_page_size_raw` (128) and `pinout`
      (`DIP32_SST39SF040`).
    - `AE29F2008`'s `support_status` is confirmed to read `supported` BEFORE any edit, and no edit is
      made (D-22).
    - The `vpp_mv` scan is run with `/usr/bin/grep`, not the devcontainer default, and that is stated.
    - Any RESEARCH.md citation found to have moved is recorded with its new location and the drift noted.
  </acceptance_criteria>
  <done>The four in-repo facts behind the SAFE-09 verdict are re-derived in this session, each with its command and output, ready to be written into a record that cites current reality rather than week-old line numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Write the AE29F2008 classification verdict record (D-18, D-19, D-22, D-23)</name>
  <precondition>Task 1's four re-verified facts and their commands are recorded, so the note cites evidence derived in this session rather than transcribed from RESEARCH.md.</precondition>
  <read_first>
    - `/workspaces/.planning/notes/jumper-display-ground-truth.md` — Phase 182's durable evidence note.
      This is the shape and register to follow: a standalone, citable record with its measurements and
      their provenance, written so a later phase can cite it without re-deriving it.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md`
      § B.1 through § B.7 — the DB row, the two upstream rows, the datasheet extraction table, the gh#62
      transcript, the `0x06` sequence match and the D-23 trace.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md`
      § "SAFE-09 — the AE29F2008 classification" — D-18 through D-23, and the exact framing D-18 demands.
  </read_first>
  <files>.planning/notes/ae29f2008-classification-verdict.md</files>
  <action>
    Create `.planning/notes/ae29f2008-classification-verdict.md` as the durable SAFE-09 record. It must
    carry, as sections:

    THE VERDICT, stated up front and stated as equivalence-based (D-18): AE29F2008 is classified
    `algorithm 5` CORRECTLY, and the gh#62 reporter is ALSO correct that the chip can be chip-erased.
    Both are true at once — the `0x05` firmware path does not implement the chip-erase this silicon
    supports. State explicitly that no primary AE29F2008 datasheet was retrieved or read, and that the
    conclusion rests on an upstream-declared identity plus a W29C020C datasheet.

    THE EVIDENCE CHAIN, in three legs (D-18): (a) the pinned upstream `infoic.xml` at commit
    `a8efaedc236c1d9718bd28299dfbb99536b010ff` carries ASD's `AE29F2008@DIP32` with
    `protocol_id="0x05"`, and `build_db.py`'s `classify()` returns `proto_id` unchanged for the flash
    family, so the classification is an upstream TRANSCRIPTION with no decode step to be wrong;
    (b) the same upstream file carries WINBOND's `W29C020,W29C020C,W29C022` row as field-for-field
    identical on all thirteen decode-relevant attributes including `chip_id="0x0000da45"` — upstream's
    own database declares the two parts the same die; (c) the W29C020C datasheet independently confirms
    every DB field: 256K x 8, 5 V only with `VPP is not required.`, 128-byte page write, 50 ms fast
    chip-erase, manufacturer code `DA` and device code `45`.

    WHY THE REPORTER'S `--force` ERASE WORKED, and why that is not a licence. `SST39SF020` is
    `algorithm 6`; `flash_nor_unlock_erase_execute` on a zero address emits `FLASH_ERASE`, whose six
    cycles are identical to the W29C020C datasheet's own software chip-erase table. It worked because it
    was the sequence this silicon documents. It was electrically benign because that particular pair
    shares a pinout and a size and `configure_flash_nor_unlock`'s `CMD_ERASE` arm energises no VPP rail —
    a coincidence of this pair, NOT a property of `--force`. Mark this section as the material Phase 187's
    REPLY-03 consumes (D-09).

    D-23, SETTLED. The row's `vpp_mv: 12000` is a faithful decode of upstream `voltages="0x0000"`, whose
    12 V has a real physical referent on this silicon — the A9-based hardware product-ID and
    data-protection modes — and is NOT a programming VPP. It is not live on algorithm 5: no reader in
    `flash_5v_page.cpp`, no register write, no `dev test` assertion, and the one DB-level VPP gate
    excludes `configure_flash_5v_page` by name with the WP-pin reason on the record. The new-finding
    branch does NOT fire. The residual — that the DISPLAY path calls it "VPP" with no carve-out across
    301 rows in three 5 V-only families — is filed as backlog 999.65 by Task 3, not fixed here.

    D-22, RECORDED. `support_status` stays `supported`: the part writes, reads, blank-checks and
    verifies; only the standalone erase is refused, correctly.

    WHAT THIS PHASE DID NOT DO, and why (D-21): no `build_db.py` rule, no `diff_db.py` run, no
    regeneration, no `DECODE-NOTES.md` edit, no `chip_database.json` diff. State that D-21's mechanism
    was held ready and not used because the misclassification branch did not fire — so a later reader
    does not mistake the absence of a DB diff for the question not having been asked.

    Follow the register of `.planning/notes/jumper-display-ground-truth.md`: cite by symbol and enclosing
    scope, mark each claim's provenance, and keep any line number you must use dated to this session.
    Nothing in this task edits a file under `firestarter/` or `firestarter_app/`.
  </action>
  <verify>
    <automated>cd /workspaces && test -s .planning/notes/ae29f2008-classification-verdict.md && /usr/bin/grep -c 'equivalence' .planning/notes/ae29f2008-classification-verdict.md</automated>
    <fails_when>the file is missing or empty, or the count printed is `0` — a verdict that never uses the word "equivalence" has not stated itself as equivalence-based, which D-18 requires</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'a8efaedc236c1d9718bd28299dfbb99536b010ff' .planning/notes/ae29f2008-classification-verdict.md</automated>
    <fails_when>no output and non-zero exit — the record does not name the pinned upstream commit, so its provenance anchor is missing and the classification claim cannot be re-checked by anyone</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain -- firestarter/data/chip_database.json tools/build_db.py tools/DECODE-NOTES.md</automated>
    <fails_when>any output at all — one of the three files D-21 would have touched has been modified, meaning a misclassification-branch edit was made on evidence that says the classification is correct</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/notes/ae29f2008-classification-verdict.md` exists and states the verdict in its first
      section, naming itself as equivalence-based and stating that no primary AE29F2008 datasheet was
      read (D-18).
    - The record names the pinned upstream commit `a8efaedc236c1d9718bd28299dfbb99536b010ff` and the
      shared `chip_id` `0x0000da45` on both the ASD and WINBOND rows.
    - The record states that the classification is correct AND the reporter is correct, in the same
      sentence or adjacent sentences (D-19).
    - The record contains a section marked as REPLY-03 material, stating that the `--force` erase's
      electrical benignity was a coincidence of that chip pair and not a property of `--force` (D-09).
    - The record states D-23's verdict as "not live on algorithm 5" with the four negative legs, and
      points at backlog 999.65 for the residual display question.
    - The record states `support_status` stays `supported` (D-22), and states that D-21's branch did not
      fire and why.
    - `cd /workspaces/firestarter_app && git status --porcelain -- firestarter/data/chip_database.json`
      produces no output.
  </acceptance_criteria>
  <done>SAFE-09 has a durable, citable verdict record with its full equivalence chain, its D-23 resolution and its explicit statement of what was deliberately not changed — and not one byte of generated data moved.</done>
</task>

<task type="auto">
  <name>Task 3: File backlog entries 999.63-999.66 (D-20, D-05, D-23)</name>
  <precondition>The verdict record exists, so the backlog entries can cite it rather than restating its evidence.</precondition>
  <read_first>
    - `/workspaces/.planning/ROADMAP.md` — the backlog section tail. Read the last two existing entries
      (`### Phase 999.61:` and `### Phase 999.62:`) in full to copy their heading shape, their
      `**Goal:**` line, their measured-evidence register and their `BACKLOG — filed <date> during <phase>`
      suffix. Use `grep -n '^### Phase 999\.' .planning/ROADMAP.md | tail` to locate them; do NOT read
      the whole 909 KB file.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-CONTEXT.md`
      § Deferred Ideas — the three deferred items and the shape each was captured in.
    - `/workspaces/.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-RESEARCH.md`
      § B.4 (the boot-block caveat), § B.7 (the 301-row display measurement and the
      "Can be erased: yes" observation) and § "Environment Availability" (the missing `datasheets/` paths
      cited by `PROTOCOLS.md`).
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Append four backlog entries to `.planning/ROADMAP.md`'s backlog section, after the existing
    `### Phase 999.62:` entry, following the 999.55-999.62 convention exactly: a `### Phase 999.NN:`
    heading ending in `(BACKLOG — filed 2026-09-11 during v1.37 Phase 183)`, a `**Goal:**` paragraph, and
    a measured-evidence body citing by symbol and enclosing scope.

    Use HAND EDITS ONLY. Do not run `roadmap.update-plan-progress` or any other roadmap-mutating verb —
    that verb rewrites the dependency table by positional overwrite and has previously destroyed a phase
    name and its requirement IDs. Append with a scoped edit; do not rewrite the file.

    999.63 — Software chip-erase for the `0x05` family (D-20). A NEW FIRMWARE CAPABILITY, excluded from
    v1.37 by the milestone boundary. Template: `eeprom28c_erase_execute`'s AN-0544B six-byte software
    chip erase, 0 B RAM via inline literal writes. Carry the W29C020C datasheet's boot-block caveat
    verbatim in substance: once the boot-block programming lockout feature is activated, the chip-erase
    function is disabled — so any future `0x05` chip-erase must handle the two 8 KB boot blocks. State
    that shipping this REVERSES SAFE-06's refusal and therefore needs an explicit operator scope
    decision, not a quiet follow-on. Cite
    `.planning/notes/ae29f2008-classification-verdict.md`.

    999.64 — Generalize the erase refusal beyond flash4 (D-05's other half). `eprom_erase`'s bare
    `Not supported` also fires for UV-EPROM, SRAM and `0x0D` electrical-type outliers — every part
    without `FLAG_CAN_ERASE`. The design is already done and must be named: reuse `chip_test.py`'s
    existing per-family reason strings so `erase` and `dev test` say the same thing about the same chip.
    Note that this phase's `flash4_erase_gate` deliberately fails OPEN on an unknown part precisely so
    that widening is a decision rather than an accident.

    999.65 — `firestarter info` labels an elevated VPP on 5 V-only parts (D-23 residual). Measured this
    week: 301 rows across three families print a non-zero VPP because the display gates in
    `eprom_info.py` and `ic_layout.py` carry no equivalent of `check_dispatch.py`'s WP-pin carve-out,
    which already records the correct reading. State plainly that this is NOT a `build_db.py` correction
    — the decode is faithful to upstream `voltages` — so activation decision D-6 does not apply. Include
    the adjacent observation that the same `info` output says `Can be erased: yes (electrically
    erasable)` for a part the tool then refuses to erase, because that string is derived from
    `electrical.type` and not from the protocol; that is a second contributor to gh#62 reading as a
    malfunction.

    999.66 — `PROTOCOLS.md` cites datasheet files that exist in neither repository. Its `0x05` section
    points at `datasheets/0x05-FLASH-AMD-STD/*.pdf`; no such directory exists in `firestarter/` or
    `firestarter_app/`. A CLAIM-class finding (a document naming a file that is not there), adjacent to
    CLAIM-09 but outside SAFE-08's scope, reported rather than fixed by this phase.

    Do not renumber, reword or reflow any existing backlog entry.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c '^### Phase 999\.6[3-6]:' .planning/ROADMAP.md | grep -qx '4' && echo FOUR_ENTRIES</automated>
    <fails_when>`FOUR_ENTRIES` is absent — fewer or more than four new `999.63`-`999.66` headings exist, so an entry was dropped, duplicated or misnumbered</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c '^### Phase 999\.' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is anything other than the pre-edit count plus 4 — an existing backlog heading was removed or renumbered by the append</fails_when>
    <automated>cd /workspaces && git diff --stat -- .planning/ROADMAP.md</automated>
    <fails_when>the diffstat shows any deletions (a `-` count above 0) — an append-only edit must delete nothing; deletions mean an existing entry, the Phase 183 section or the dependency table was overwritten</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly four new headings exist, `### Phase 999.63:` through `### Phase 999.66:`, each ending
      `(BACKLOG — filed 2026-09-11 during v1.37 Phase 183)`.
    - `git diff -- .planning/ROADMAP.md` shows zero deleted lines.
    - 999.63 names `eeprom28c_erase_execute` as the template, carries the boot-block lockout caveat, and
      states that shipping it reverses SAFE-06 and needs an explicit operator scope decision.
    - 999.64 names `chip_test.py`'s per-family reason strings as the design and names the fail-open
      choice `flash4_erase_gate` makes.
    - 999.65 states the 301-row measurement, names `eprom_info.py` and `ic_layout.py` as the display
      gates, names `check_dispatch.py` as already recording the correct reading, and states explicitly
      that this is NOT a `build_db.py` correction.
    - 999.66 names `PROTOCOLS.md` and the absent `datasheets/0x05-FLASH-AMD-STD/` path.
    - The Phase 183 and Phase 185 sections of the ROADMAP are byte-unchanged by this task (they belong to
      `183-06`).
  </acceptance_criteria>
  <done>The four follow-on items this phase surfaced are filed as ROADMAP backlog entries, append-only, with their measured evidence and their explicit scope boundaries.</done>
</task>

</tasks>

<flagged_assumptions>
**Probe row surfaced but NOT authored as an acceptance criterion — SAFE-09 / `unclassified`.**

The deterministic edge probe returned `{"requirement_id":"SAFE-09","category":"unclassified",
"status":"unresolved","probe":"unclassified — review manually"}`. Per the spec-less fallback protocol an
`unclassified` row stays `unresolved` and is never auto-resolved with a backstop, so it is surfaced here
rather than silently dropped.

**Why it does not map:** the probe's taxonomy reaches the data/behaviour-shape axis (boundaries,
adjacency, encoding, ordering) of a transformation. SAFE-09 is not a transformation — it is an
investigative requirement whose deliverable is a recorded verdict about a fact in the world. There is no
input whose empty/single/null case could be asked about, and no output whose ordering could be
unspecified. The nearest genuine "shape" question SAFE-09 has is *"what counts as acceptable evidence"*,
and that is not a shape question at all — D-18 answered it by decision, and the answer
(equivalence-based, with the upstream row checked too) is authored into `must_haves.truths` above on its
own merits, not as a resolution of this probe row.

**Standing risk if this assumption is wrong:** if a reviewer believes an edge WAS hiding here, the place
to look is the evidence-admissibility boundary — specifically, whether an upstream-declared identity
between two part numbers is transitive enough to carry a third-party datasheet's claims onto a re-badge.
The record states its chain explicitly so that boundary is visible rather than buried.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream `infoic.xml` → `build_db.py` → `chip_database.json` | An external, third-party database is transcribed into the data that decides how a chip is driven. |
| `chip_database.json` → host → firmware → silicon | A wrong row is a wrong bus config or a wrong voltage on someone's part. |
| `.planning/` record → Phase 187's public reply | This plan's verdict becomes a statement made to an external reporter on GitHub. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-183-02-A | Tampering | `firestarter_app/firestarter/data/chip_database.json` | high | mitigate | A hand edit would be silently reverted by the next `build_db.py` run while the tree claimed otherwise. An acceptance criterion asserts `git status --porcelain` over the file, `build_db.py` and `DECODE-NOTES.md` is empty at task end; the prohibition block states the rule independently. |
| T-183-02-B | Repudiation | the SAFE-09 verdict record | high | mitigate | A verdict presented as a direct vendor datasheet reading, when it is an equivalence inference, would put an unearned claim in front of gh#62's reporter. D-18's framing is enforced by an acceptance criterion (`grep` for `equivalence`) and by the required statement that no primary datasheet was read. |
| T-183-02-C | Information disclosure | ROADMAP backlog entry 999.63 | medium | mitigate | 999.63 describes a capability that, once built, erases a customer's chip. The entry is required to state the boot-block lockout caveat and that shipping it reverses SAFE-06 — so a future implementer cannot pick it up as a routine follow-on. |
| T-183-02-D | Tampering | `.planning/ROADMAP.md` | medium | mitigate | Append-only is asserted by a zero-deletions diffstat criterion, and the roadmap-mutating CLI verb is forbidden in the action text because it overwrites the dependency table positionally. |
| T-183-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase; no Package Legitimacy Audit table is required. |
</threat_model>

<verification>
- `.planning/notes/ae29f2008-classification-verdict.md` exists, states an equivalence-based verdict, names
  the pinned upstream commit and the shared `chip_id`, and carries the REPLY-03 material section.
- Four backlog entries 999.63-999.66 exist; `git diff -- .planning/ROADMAP.md` shows zero deletions.
- `cd /workspaces/firestarter_app && git status --porcelain -- firestarter/data/chip_database.json
  tools/build_db.py tools/DECODE-NOTES.md` is empty.
- Nothing under `firestarter/` or `firestarter_app/` was edited by this plan.
</verification>

<success_criteria>
SAFE-09's question has a recorded, datasheet-grounded, honestly-framed answer that a later phase can cite;
the misclassification machinery was held ready and deliberately not used; and the three things this phase
surfaced but does not build are filed where they will be found.
</success_criteria>

<artifacts_this_phase_produces>
Created by this plan:
- `.planning/notes/ae29f2008-classification-verdict.md` (new record file)
- ROADMAP backlog entries `### Phase 999.63:`, `### Phase 999.64:`, `### Phase 999.65:`,
  `### Phase 999.66:`

Deleted by this plan: nothing. No symbol, no file, no data row.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/183-flash4-erase-refusal-the-ae29f2008-classification/183-02-SUMMARY.md` when done
</output>
