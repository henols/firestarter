---
phase: 185-records-and-checks-that-are-current
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_dev_test_cmd.py
autonomous: true
requirements: [CLAIM-07]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The off-TTY forcing helper is gone from `tests/test_dev_test_cmd.py` along with all 51 of its uses, and the change is BEHAVIOUR-NEUTRAL: `CliRunner` already replaces `sys.stdin` with a non-TTY stream for the duration of `invoke()`, so the state the helper forced was guaranteed by construction (D-05)."
    - "The unwrap is a re-indentation, not a substitution: 46 sites are the sole context manager and need their entire block body dedented by four spaces; 5 sites sit inside a parenthesized multi-context `with` and need only their own line removed. The two populations sum to 51, which is the measured figure — not the source todo's estimate of ~14 (D-05)."
    - "Four of the five multi-context sites are left with a single remaining context manager and collapse to a plain single-CM `with`. A residual single-item `with (X,):` is valid Python but `ruff format` rewrites it, so `ruff format --check firestarter/ tests/` — which app CI runs on Python 3.11 — would go red."
    - "No surviving test name in `firestarter_app/tests/` claims TTY gating: the two names ending in an on-a-TTY / off-a-TTY claim are gone, merged or renamed (D-06, CLAIM-07, success criterion 4)."
    - "The two UV slot-write tests are MERGED, not both kept and not one deleted: once the inert patch is gone they assert the identical thing about the identical input, so one survivor carries the assertion under a name with no TTY claim. The module's collected test count drops by exactly one, from 64 to 63, and that −1 is attributed to the merge rather than to a lost test."
    - "`test_no_prompt_on_a_uv_part_even_on_a_tty` is renamed with its body unchanged: it asserts two `hasattr` structural properties of `cli_handlers` and performs no TTY work of any kind, so its name has always been a misnomer rather than a claim that went stale (D-06)."
    - "`test_non_uv_part_is_still_written_in_full_without_a_prompt` — which CONTEXT.md's D-06 does not name — also loses its direct patch and the 'TTY or not' clause in its docstring, because after the symbol is deleted that clause claims coverage nothing performs. Its name carries no TTY claim, so it is not renamed."
    - "After this plan, `tests/test_dev_test_cmd.py` contains zero references to the deleted TTY-check helper in `cli_handlers` — the module docstring's clause, the helper definition and all three direct patch sites are gone — which is what makes plan 185-04's deletion of that symbol safe."
    - "The class docstring of `TestUVWriteHasNoPrompt`, which narrates the retired UV write prompt in the past tense, is KEPT. It is a historical account of a removed feature, not a live claim about behaviour this module exercises."
  prohibitions:
    - "MUST NOT author any `#` comment line into `firestarter_app/tests/test_dev_test_cmd.py`. The permitted operations on a false docstring clause are deleting it or substituting a name inside the existing prose — never adding a comment that explains the deletion. `/workspaces/CLAUDE.md` § 'Source code comments — hard rule' is absolute and is not overridable by this plan, this task, or any skill. Docstrings are a separate question and may be edited freely."
    - "MUST NOT hand-edit 46 block dedents. A single mis-dedent silently moves an assertion out of or into a block, producing a test that passes vacuously or a `result` referenced before assignment — a failure that a green suite cannot distinguish from success."
  artifacts:
    - path: "firestarter_app/tests/test_dev_test_cmd.py"
      provides: "The unwrapped module: no off-TTY forcing helper, no TTY-claiming test name, a merged UV slot-write test, and a module docstring with the false TTY-gating clause removed"
      contains: "TestUVWriteHasNoPrompt"
  key_links:
    - from: "firestarter_app/tests/test_dev_test_cmd.py"
      to: "firestarter_app/firestarter/cli_handlers.py"
      via: "this plan severs the last test-side dependency on the dead TTY-check helper, which is the precondition for plan 185-04 deleting it"
      pattern: "cli_handlers"
    - from: "firestarter_app/tests/test_dev_test_cmd.py"
      to: "click.testing.CliRunner"
      via: "CliRunner.invoke replaces sys.stdin with a non-TTY stream, which is what makes the removed forcing helper redundant rather than load-bearing"
      pattern: "CliRunner"
---

<objective>
Remove the off-TTY forcing helper and all 51 of its uses from `firestarter_app/tests/test_dev_test_cmd.py`,
merge the redundant pair of UV slot-write tests, and leave no test name claiming TTY gating that the test
does not perform.

Purpose: CLAIM-07's second half. Two tests are named for a TTY distinction they do not make, and 51 context
managers force a state `CliRunner` already guarantees — 51 wrappers doing real work that changes nothing,
which is how a name came to claim coverage that does not exist. The source todo estimated ~14 sites; the
measured figure is 51, and the shapes are not uniform, so this is a scripted transform followed by a
formatter and a counted run, never a `sed` and never 46 hand edits.

This plan runs in parallel with the firmware half of the phase: it shares no file and no dependency with
plans 185-01 and 185-02.

Output: a module with no forcing helper, one fewer test than before (the merge), no TTY-claiming test name,
and zero remaining test-side references to the symbol plan 185-04 deletes.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-records-and-checks-that-are-current/185-CONTEXT.md
@.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md
@.planning/phases/185-records-and-checks-that-are-current/185-PATTERNS.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md

Note on omissions: `.planning/ROADMAP.md`, `.planning/STATE.md` and `.planning/PROJECT.md` are deliberately
not referenced — each is large enough to spend most of the executor's context, and nothing in this plan
needs them.
</context>

<!-- planner-discipline-allow: _off_tty -->
<!-- planner-discipline-allow: _a_tty -->
<!-- planner-discipline-allow: _is_interactive -->

<failing_direction_convention>
Every `<automated>` command here is a GREEN-direction command with a `<fails_when>` naming the observable
that would mean it did nothing or did the wrong thing. Several gates are COUNT gates against a figure
measured on this tree before the task began; each `<fails_when>` names both the expected figure and what a
zero would mean, so a grep that matched nothing cannot read as a pass.
</failing_direction_convention>

<tasks>

<task type="auto">
  <name>Task 1: Unwrap all 51 off-TTY sites by scripted transform, delete the helper, delete the module docstring's false clause (D-05, D-07.3)</name>
  <reversibility rating="costly">CONTEXT.md rates D-05 reversible but costly to redo — the diff touches 51 sites in one 2000+ line file, and re-deriving it means re-running the transform and re-verifying every dedent. Carried through from CONTEXT.md, not re-derived.</reversibility>
  <precondition>`/workspaces/firestarter_app/.venv311/bin/python` exists and reports Python 3.11 — the devcontainer default is 3.12, which has provably masked breakage that reddens app CI, and app CI runs 3.11 only.</precondition>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_dev_test_cmd.py` — the module docstring (lines 1-30, so you
      see the false TTY-gating clause in its surrounding sentence), the helper definition at lines 518-520,
      and the five multi-context sites with ±18 lines of surround at lines 950, 1275, 1302, 1966 and 2183.
      Do not attempt to read all 51 sole-CM sites by hand; the transform enumerates them.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-PATTERNS.md` §
      "`firestarter_app/tests/test_dev_test_cmd.py`" — the four distinct call-site shapes (A: sole CM, 46
      sites, dedent the body; B: first of two, collapses; C: first of three, keep the parenthesized form;
      D: not first, survivor collapses), each excerpted once, plus the verbatim module-docstring clause to
      delete.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md` §B.1 (the
      measured shape counts and the `ruff format` gate), §B.2 (the 64-passed baseline) and §B.4 (the full
      tracked reference table for this file).
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule" and its note that docstrings are a
      separate question — before touching the module docstring.
  </read_first>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <action>
    Work in `/workspaces/firestarter_app` on branch
    `gsd/v1.37-operator-safety-answered-reports-claim-hygiene`.

    STEP 0 — BASELINE THE COUNTS, and record them. Before any edit, capture and record: the collected test
    count for this module (`--collect-only -q -o addopts=""`, measured at 64 on this tree); the number of
    lines containing the helper call (52 — 51 uses plus the `def` line); the number of sole-context-manager
    sites (46); the number of bare multi-context lines (5); and the count of `#` comment lines in the file
    (216). Every count gate below compares against these.

    STEP 1 — WRITE A TRANSFORM, do not hand-edit. Put a throwaway script in the session scratchpad (NOT in
    the repository — `firestarter_app/tools/` sits outside every CI gate and is not a home for one-shot
    scripts). It must handle the four shapes distinctly:

    SHAPE A — the helper is the SOLE context manager, `with <helper>():` on its own line, 46 sites. Delete
    that line and dedent the ENTIRE block body by exactly four spaces. The body ends at the first
    subsequent line whose indentation is less than or equal to the `with` line's own indentation and which
    is not blank. Blank lines inside the body are left blank, not indented.

    SHAPE B — the helper is the first entry in a parenthesized multi-context `with (...)` with exactly ONE
    sibling (lines 950 and 1275, whose siblings are the environment-directory `patch.dict` and the
    submit-report `patch`). Delete the helper's line, then COLLAPSE the construct to a plain single-CM
    `with <sibling>:` — removing the parentheses, the trailing comma and the line break. Do not leave
    `with (X,):`: it is valid Python, but `ruff format` rewrites it and `ruff format --check` then goes red
    in CI.

    SHAPE C — the helper is first in a multi-context `with (...)` with TWO siblings (line 1302, the
    browser-open and subprocess-run patches). Delete only the helper's line. Keep the parenthesized form
    and leave every other line's indentation untouched.

    SHAPE D — the helper is NOT first (line 1966, second of two; line 2183, last after a multi-line
    `patch(...)` call). Delete the helper's line; the single survivor then collapses exactly as in shape B.

    Run the transform, then delete the helper definition itself at lines 518-520 — the `def`, its docstring
    and its `return`.

    STEP 2 — DELETE THE FALSE MODULE-DOCSTRING CLAUSE. The module docstring at lines 8-14 states that
    TTY-gating is controlled by patching a module-level function in `cli_handlers` directly rather than
    `sys.stdin.isatty`, and explains why. That sentence is false today — nothing in this module gates on a
    TTY any more, and plan 185-04 deletes the named function. Under the no-comments rule the permitted
    operation is DELETION of the false clause, not replacement prose. Remove the whole sentence, leaving
    the surrounding paragraph (the hardware-free-proof narrative) reading correctly. Add no `#` comment
    anywhere in this file, and write no replacement explanation of why the clause went — that record
    belongs in this plan's SUMMARY.

    STEP 3 — FORMAT AND PROVE. Run `ruff format firestarter/ tests/` from the py3.11 venv, then
    `ruff format --check` and `ruff check` over the same paths, then the module under pytest. The collected
    and passed count at THIS stage is still 64 — the merge has not happened yet, so a 63 here means a test
    was lost by a bad dedent, not merged.

    STEP 4 — SPOT-CHECK THE DEDENTS. A green suite does not prove a correct dedent: an assertion moved out
    of its block still passes, vacuously. Read back at least six transformed sites chosen across the file
    (one near the top, one near the bottom, and all four of the shape-B/C/D collapses) and confirm the
    assertions that followed the `with` block are still inside the same test function and still at the
    indentation the surrounding code implies. Also run the module with `-x -q` twice and confirm the same
    count both times. Record which sites you read.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_dev_test_cmd.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line does not read `64 passed`. A `63 passed` at this stage means a test was LOST by a bad dedent — the merge that legitimately removes one happens in Task 2, not here. A `0 passed` or a collection error means the transform produced invalid Python</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '_off_tty' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is anything other than `0` — the helper or one of its uses survived. This gate is deliberately unfiltered: in this file a docstring or comment naming the removed helper is itself a stale claim, not an exempt annotation</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'def test_' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is not `64` — the same figure as before this task. This is the non-vacuity control for the gate above: a `0` would mean the grep read nothing at all, so a `0` for the helper would prove nothing</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -cE 'with \([A-Za-z_][A-Za-z0-9_.]*,\):' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is anything other than `0` — a residual single-item parenthesized `with` survived a shape-B or shape-D collapse, and `ruff format --check` will red on it in CI even though the file is valid Python</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, or any line naming a file that would be reformatted — app CI runs this exact command on Python 3.11 and a red here is a red build</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/</automated>
    <fails_when>non-zero exit, or any diagnostic line printed — the transform left an unused import, an undefined name, or an import-order violation</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '^[[:space:]]*#' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is greater than `216`, the count measured on this tree before the task — any increase means a `#` comment line was authored under `firestarter_app/`, which the project's hard rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records all five Step-0 baseline counts and the same five counts after the task, side by
      side.
    - The SUMMARY records the transform script verbatim, states that it lived in the session scratchpad and
      not in the repository, and names how many sites each of the four shapes matched. The shape counts sum
      to 51.
    - The SUMMARY names the six-or-more sites read back in Step 4 by line number, and states for each that
      the following assertions remained inside the same test function.
    - `tests/test_dev_test_cmd.py` contains the string `_off_tty` zero times, and `def test_` exactly 64
      times.
    - No line in the file matches a single-item parenthesized `with (X,):` form.
    - The module docstring no longer contains the TTY-gating sentence, and no replacement explanatory prose
      was added in its place; the surrounding paragraph still reads as a complete sentence.
    - The comment-line count did not increase against 216.
    - `ruff check` and `ruff format --check` over `firestarter/ tests/` are both clean on the py3.11 venv.
    - The module reports `64 passed` — unchanged, which is D-05's behaviour-neutrality proof.
  </acceptance_criteria>
  <done>The forcing helper and all 51 of its uses are gone, the suite passes with its count unchanged, the formatter is clean, and the module docstring no longer describes a mechanism this module does not use.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the redundant UV slot-write pair, rename the two misnamed tests, drop the three direct patches (D-06, CLAIM-07)</name>
  <reversibility rating="reversible">Test names and a merge; recoverable from git, and the merged assertion is preserved verbatim in the survivor.</reversibility>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_dev_test_cmd.py` lines 795-880 — the whole
      `TestUVWriteHasNoPrompt` class as it stands after Task 1: its class docstring, and all FOUR members.
      CONTEXT.md's D-06 names two of them; the fourth also patches the symbol and carries a TTY claim in
      its docstring.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md` §B.3 — the
      per-member table with each member's actual body and its disposition, and the note that the class
      docstring's past-tense narration of the retired prompt is kept.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-CONTEXT.md` § D-06 and
      § Specific Ideas — the verbatim statement of what each of the two named tests actually asserts.
  </read_first>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <action>
    All four changes are inside `TestUVWriteHasNoPrompt`. Keep the class docstring exactly as it is: it
    narrates the retired UV write prompt in the past tense, which is a historical account of a removed
    feature, not a live claim.

    CHANGE 1 — RENAME the structural test. `test_no_prompt_on_a_uv_part_even_on_a_tty` asserts only
    `not hasattr(cli_handlers_mod, "Confirm")` and `not hasattr(cli_handlers_mod, "_default_uv_write_confirm")`.
    It does no TTY work of any kind, so its name has always been a misnomer rather than a claim that went
    stale. Rename it to `test_no_uv_write_prompt_surface_exists`. Body unchanged, docstring unchanged.

    CHANGE 2 — MERGE the redundant pair. `test_uv_part_writes_one_slot_on_a_tty` wraps its `invoke` in a
    direct `patch` of the TTY-check helper with `return_value=True` and then asserts
    `"write-partial" in {s["op"] for s in data["steps"]}`. Its sibling
    `test_uv_part_writes_one_slot_off_a_tty_too` makes the byte-identical assertion about the same chip with
    the same fixture setup. Once the patch is inert there is no difference between them at all.

    Keep ONE test. Name it `test_uv_part_writes_one_slot`. Its body is the sibling's body with the `with`
    wrapper already removed by Task 1 — the operator setup, the app context, the `runner.invoke`, the
    exit-code assertion, the `_load_report` and the `write-partial` membership assertion, unchanged.
    Delete the other member entirely. Give the survivor a docstring stating what it asserts — that a UV part
    receives exactly the partial slot write and the run completes — with no TTY claim in it.

    CHANGE 3 — DROP the last direct patch and its stale clause.
    `test_non_uv_part_is_still_written_in_full_without_a_prompt` also wraps its `invoke` in a direct `patch`
    of the TTY-check helper. D-06 does not name this test, but the patch must go — plan 185-04 deletes the
    symbol it names — and its docstring's "TTY or not" clause becomes a claim nothing tests. Remove the
    `with patch(...)` wrapper, dedent the `invoke` line by four spaces, and delete the "TTY or not" clause
    from the docstring so the sentence reads as the plain statement that every non-UV family is written in
    full, unprompted. The test's NAME carries no TTY claim, so it is not renamed.

    CHANGE 4 — CONFIRM THE FILE IS NOW SYMBOL-FREE. After changes 1-3 the file must contain no reference at
    all to the TTY-check helper in `cli_handlers`: the module docstring's clause went in Task 1, the forcing
    helper went in Task 1, and the three direct patch sites go here. That is what makes plan 185-04's
    deletion of the symbol safe, and it is why this plan must complete before that one starts.

    Then re-run the formatter and the module. The count drops to 63 — exactly one lower, and the −1 is the
    merge. A 62 means a second test was lost.

    Add no `#` comment line anywhere. The rationale for the rename and the merge belongs in the survivor's
    docstring and in this plan's SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_dev_test_cmd.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line does not read `63 passed`. A `64 passed` means the merge did not happen; a `62 passed` or lower means a second test was lost rather than merged</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -nE 'def test_.*_(on|off)_a_tty' -- tests/ ; echo "exit=$?"</automated>
    <fails_when>the trailing line does not read `exit=1`, or any matching test name is printed — a surviving name claims TTY gating that no test in the module performs, which is exactly what success criterion 4 forbids</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '_is_interactive' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is anything other than `0` — a direct patch site or a docstring mention survived, and plan 185-04's deletion of that symbol would then red this module</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'def test_' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is not `63` — the non-vacuity control for the gate above, and the structural confirmation that exactly one definition was removed</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'write-partial' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is `0` — the merged assertion was lost with the deleted member instead of being carried by the survivor</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff format --check firestarter/ tests/ && .venv311/bin/ruff check firestarter/ tests/</automated>
    <fails_when>non-zero exit from either command, or any file named as needing reformatting — the change-3 dedent left the file unformatted or introduced an unused import</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '^[[:space:]]*#' tests/test_dev_test_cmd.py</automated>
    <fails_when>the printed count is greater than `216` — a `#` comment line was authored, which the project's hard rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `TestUVWriteHasNoPrompt` has three members, down from four, and none of their names contains an
      on-a-TTY or off-a-TTY claim.
    - `test_no_uv_write_prompt_surface_exists` exists and its body still contains both `hasattr` assertions
      unchanged.
    - `test_uv_part_writes_one_slot` exists, carries the `write-partial` membership assertion, and its
      docstring makes no TTY claim.
    - `test_non_uv_part_is_still_written_in_full_without_a_prompt` keeps its name, has no `patch` wrapper
      around its `invoke`, and its docstring no longer contains a TTY-or-not clause.
    - The class docstring of `TestUVWriteHasNoPrompt` is byte-unchanged — the SUMMARY states that its
      past-tense narration was kept deliberately.
    - `tests/test_dev_test_cmd.py` contains `_is_interactive` zero times and `def test_` exactly 63 times.
    - The module reports `63 passed`, and the SUMMARY attributes the −1 against Task 1's 64 to the merge by
      naming the deleted member.
    - `ruff check` and `ruff format --check` are clean; the comment-line count did not increase.
    - The SUMMARY quotes the deleted member's assertion and the survivor's assertion side by side, showing
      they were identical — which is what makes this a merge rather than a deletion.
  </acceptance_criteria>
  <done>No test name in the module claims TTY gating, the redundant pair is one test carrying the surviving assertion, and nothing in the file references the symbol plan 185-04 deletes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a test's NAME → a reader's belief about coverage | The boundary CLAIM-07 exists to close. A name claiming a TTY distinction the body does not make is a coverage claim nothing backs. |
| a mechanical transform → 46 block bodies | A mis-dedent relocates an assertion silently; the suite stays green either way, so the transform's correctness cannot be inferred from a passing run. |
| this module → `firestarter/cli_handlers.py` | Plan 185-04 deletes a symbol this module currently patches in three places. The dependency must be severed here, in an earlier wave, or that deletion reds this suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-185-03-A | Spoofing | test names claiming TTY gating | high | mitigate | A `git grep -nE 'def test_.*_(on\|off)_a_tty'` gate expecting no match, plus the rename and merge, plus a `def test_` count gate that proves the grep read a populated file. |
| T-185-03-B | Tampering | 46 block dedents | high | mitigate | Scripted transform rather than hand edits; a count gate holding the module at 64 tests through Task 1; a residual `with (X,):` gate; `ruff format --check`; and an explicit read-back of six-plus transformed sites, because a green suite cannot distinguish a correct dedent from a vacuous one. |
| T-185-03-C | Repudiation | the merge | medium | mitigate | The deleted member's assertion and the survivor's are quoted side by side in the SUMMARY, so "merged" is evidenced rather than asserted; the count is required to be exactly 63, so a silent second loss shows. |
| T-185-03-D | Denial of service (CI red) | `ruff format --check` | medium | mitigate | Named explicitly as a gate in both tasks, run on the py3.11 venv that app CI uses, with the single-item parenthesized `with` form called out as the specific residue that trips it. |
| T-185-03-E | Elevation of privilege (a rule bypassed) | the no-comments rule | medium | mitigate | A `must_haves` prohibition plus a comment-line count gate against the measured pre-task figure of 216 in both tasks. |
| T-185-03-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency; it edits one file already in the repository and runs the existing `.venv311`. No Package Legitimacy Audit table is required and none is fabricated. |
</threat_model>

<flagged_assumptions>
- **CLAIM-07, probe row `unclassified` — UNRESOLVED, flagged.** The deterministic edge probe could not
  classify CLAIM-07 into an edge family, so per protocol it is neither auto-resolved nor auto-dismissed and
  it is surfaced here rather than silently dropped. Read manually, CLAIM-07's real edge is not an
  input-domain one: it is whether a test's NAME and its BODY agree, which has no min/max, no empty case and
  no ordering. The `must_haves.truths` above encode that agreement directly. It is recorded as unresolved
  rather than reclassified, because reclassifying a probe result is the planner substituting its own
  judgement for the probe's.
- **The chosen names are the planner's call, not a measured constraint.** `test_no_uv_write_prompt_surface_exists`
  and `test_uv_part_writes_one_slot` are proposals. The binding property is that no surviving name claims
  TTY gating; a differently-worded name satisfying that property is acceptable, and the SUMMARY should say
  so if one is chosen.
- **The 51-site figure and the four shape counts were measured with `/usr/bin/grep` at research time.** The
  executor re-measures them in Step 0 before transforming. If the counts differ from 46 + 5 = 51, the tree
  has moved since research and the transform must be re-derived rather than forced to match.
- **`test_non_uv_part_is_still_written_in_full_without_a_prompt` is handled here although CONTEXT.md's D-06
  does not name it.** It is the fifth patch site, found during research. Leaving it would either break plan
  185-04's deletion or leave a docstring claiming TTY coverage nothing performs.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan (NEW symbols; a source-grounding drift pass must not flag them as absent from prior
tree state):
- `test_no_uv_write_prompt_surface_exists` — the renamed structural test in `TestUVWriteHasNoPrompt`.
- `test_uv_part_writes_one_slot` — the merged survivor of the redundant UV slot-write pair.

Deleted by this plan (a distinct list):
- the `_off_tty` helper definition in `firestarter_app/tests/test_dev_test_cmd.py` and all 51 of its call
  sites.
- `test_no_prompt_on_a_uv_part_even_on_a_tty` and `test_uv_part_writes_one_slot_on_a_tty` and
  `test_uv_part_writes_one_slot_off_a_tty_too` — as NAMES. Two of the three bodies survive under the new
  names above; one member is removed by the merge.
- the module docstring's TTY-gating sentence (lines 8-14) — deleted, not replaced.
- the "TTY or not" clause from `test_non_uv_part_is_still_written_in_full_without_a_prompt`'s docstring.
- the three direct `patch("firestarter.cli_handlers._is_interactive", …)` call sites.

Created and then discarded inside Task 1 (never committed, never in the repository):
- the one-shot unwrap transform script, written to the session scratchpad.

Kept deliberately, with the reason on the record:
- `TestUVWriteHasNoPrompt`'s class docstring — a past-tense account of the retired UV write prompt, not a
  live claim.
- `firestarter_app/build/lib/firestarter/cli_handlers.py` — a gitignored, untracked stale build artefact
  that still contains the symbol. It is NOT source, it is not deleted here, and it must not be reported as
  a surviving reference.
</artifacts_this_phase_produces>

<verification>
- `tests/test_dev_test_cmd.py` reports `64 passed` after Task 1 and `63 passed` after Task 2.
- The file contains `_off_tty` zero times and `_is_interactive` zero times.
- `def test_` appears 64 times after Task 1 and 63 times after Task 2 — the non-vacuity control for both
  absence gates.
- No single-item parenthesized `with (X,):` form survives.
- `git grep -nE 'def test_.*_(on|off)_a_tty' -- tests/` returns no match.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` are both clean on
  `.venv311`.
- The comment-line count in the file is not greater than 216.
- The SUMMARY carries the transform script, the four shape counts summing to 51, the six-plus read-back
  sites, and the side-by-side quotation of the merged pair's identical assertions.
</verification>

<success_criteria>
The module no longer forces a state `CliRunner` already guarantees, and no test name in it claims a TTY
distinction the test does not make. The 51 sites were unwrapped by a transform whose correctness was
checked by reading transformed code, not inferred from a green suite — because a mis-dedent leaves the
suite green. The redundant pair is one test carrying the surviving assertion, with the −1 attributed to the
merge on the record. Nothing in the file references the symbol plan 185-04 deletes.
</success_criteria>

<output>
Create `.planning/phases/185-records-and-checks-that-are-current/185-03-SUMMARY.md` when done
</output>
