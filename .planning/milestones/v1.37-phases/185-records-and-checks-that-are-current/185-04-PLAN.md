---
phase: 185-records-and-checks-that-are-current
plan: 04
type: execute
wave: 2
depends_on: [185-03]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tools/check_devtest_orchestrator.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
  - firestarter_app/tests/test_numeric_schema_source_scan.py
autonomous: true
requirements: [CLAIM-06, CLAIM-07]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The dead TTY-check helper is gone from `firestarter/cli_handlers.py` — the `def`, its docstring and its `return` — and `git grep` over the app repo returns no tracked match for its name anywhere (CLAIM-07, success criterion 4)."
    - "The symbol had ZERO call sites in live source before deletion: the two real TTY readers are in `submit.py` and `jp5_gate.py`, each behind its own injectable `isatty_fn`, and neither imports it. Neither is disturbed by this plan (D-07)."
    - "All THREE dependents move in the same commit as the deletion: the allow-list entry in `tools/check_devtest_orchestrator.py`, the module-docstring enumeration in the SAME file that also names the symbol in prose, and the subset-vs-equality rationale in `tests/test_check_devtest_orchestrator.py`. Missing the first reds `test_handler_function_names_all_resolve_to_real_callables`; missing the second leaves a docstring naming a symbol that no longer exists, in a file no CI gate reads."
    - "The subset-vs-equality rationale is repaired by NAME SUBSTITUTION inside existing prose, not by authoring a new paragraph: the deleted symbol is swapped for a still-live listed-but-not-body-referenced example. After the deletion, three such examples remain, so the rationale survives with a different live example and needs no invention (D-07.2)."
    - "The subset direction remains CORRECT after the substitution: the replacement example is listed in the allow-list and is called only indirectly, so an equality assertion would still be red on day one — which is the reason the assertion is a subset."
    - "`tests/test_numeric_schema_source_scan.py` contains zero file-plus-line-number citations of any kind, and the repaired sentence names `_AT28C_DIP24_NAMES` with its enclosing scope — a local variable nested inside a `for` loop deep in `main()` — exactly as line 129 of the same file already does (CLAIM-06, D-13, success criterion 3)."
    - "The boundary the citation repair must clear is EXACTLY ZERO, not 'fewer': one surviving file-plus-line citation in that file is a full failure, and the file is measured to hold exactly one today, so the count moves 1 → 0 and nothing else in the file changes (CLAIM-06 edge: boundary)."
    - "No replacement line number is written. The requirement text names one figure, the live tree holds another, and the symbol moved on the same day the requirement was authored — the citation has now staled three times, which is why the symbol-and-scope form is the repair rather than a corrected number (D-13)."
    - "`firestarter_app/build/lib/firestarter/cli_handlers.py` still contains the deleted symbol and is NOT touched, NOT deleted and NOT reported as a surviving reference: it is a gitignored, untracked stale build artefact. `git grep` is the evidence tool precisely because it scans tracked files only, which makes it immune both to that false positive and to this devcontainer's `grep` being ugrep."
    - statement: "The citation repair has no numeric-precision, overflow or rounding contract to state — a docstring citation carries no arithmetic. The nearest real analogue of the probe's question is that no digit sequence follows a `.py` token anywhere in the file after the repair, which the count gate asserts directly."
      verification: backstop
  prohibitions:
    - "MUST NOT author any `#` comment line into `firestarter_app/firestarter/cli_handlers.py`, `tools/check_devtest_orchestrator.py`, `tests/test_check_devtest_orchestrator.py` or `tests/test_numeric_schema_source_scan.py`. The permitted operations on a stale record are deleting the false clause and substituting a name inside existing prose. If a repair cannot be made without authoring replacement commentary, leave it and record the deviation in the SUMMARY. `/workspaces/CLAUDE.md` § 'Source code comments — hard rule' is absolute and is not overridable by this plan, this task, or any skill."
    - "MUST NOT modify `firestarter/submit.py` or `firestarter/jp5_gate.py`. They hold the two REAL TTY readers, each behind its own injectable seam, neither of which imports the deleted symbol. Touching them would turn a dead-symbol removal into a behaviour change, which this phase's boundary excludes."
  artifacts:
    - path: "firestarter_app/firestarter/cli_handlers.py"
      provides: "The module with the dead TTY-check helper removed"
      contains: "_make_sampler"
    - path: "firestarter_app/tools/check_devtest_orchestrator.py"
      provides: "The allow-list and its module-docstring enumeration, both no longer naming the deleted symbol"
      contains: "_HANDLER_FUNCTION_NAMES"
    - path: "firestarter_app/tests/test_check_devtest_orchestrator.py"
      provides: "The subset-vs-equality rationale carrying a still-live example"
      contains: "SUBSET"
    - path: "firestarter_app/tests/test_numeric_schema_source_scan.py"
      provides: "The docstring citing symbol and enclosing scope with no line number"
      contains: "_AT28C_DIP24_NAMES"
  key_links:
    - from: "firestarter_app/tools/check_devtest_orchestrator.py"
      to: "firestarter_app/firestarter/cli_handlers.py"
      via: "_HANDLER_FUNCTION_NAMES asserts every listed name resolves to a real callable in the handler module — the allow-list entry and the symbol must move together"
      pattern: "_HANDLER_FUNCTION_NAMES"
    - from: "firestarter_app/tests/test_check_devtest_orchestrator.py"
      to: "firestarter_app/tools/check_devtest_orchestrator.py"
      via: "the subset assertion compares the referenced-helper set against the allow-list literal, and its docstring names an example from that literal"
      pattern: "check_devtest_orchestrator"
  key_links_note: "tests/test_numeric_schema_source_scan.py names tools/build_db.py only in prose; it AST-parses source text and has no import-time link to it."
---

<objective>
Delete the dead TTY-check helper from `firestarter_app/firestarter/cli_handlers.py` together with all three
of its dependents, and replace the one surviving file-plus-line-number docstring citation with the
symbol-and-scope form the same file already uses correctly.

Purpose: CLAIM-07's first half and CLAIM-06. The helper has had zero call sites in live source since
Phase 181 plan 04 deleted the only one; what kept it alive was an allow-list entry asserting it resolves to
a real callable, a module-docstring enumeration in the same tool, and a test docstring citing it as the
reason an assertion is a subset. Meanwhile a second docstring cites a symbol by file and line number for a
symbol that has moved twice — the citation has now staled three times, and the requirement's own figure
staled within a day of being written. Both are records describing a tree that no longer exists.

This plan runs after 185-03, which severs the last test-side dependency on the symbol. Without that
ordering, deleting it here would red `tests/test_dev_test_cmd.py`.

Output: the symbol gone with no tracked reference anywhere, three dependents repaired by deletion and name
substitution only, and a citation that names a symbol and its scope instead of a line number.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/185-records-and-checks-that-are-current/185-CONTEXT.md
@.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md
@.planning/phases/185-records-and-checks-that-are-current/185-PATTERNS.md
@.planning/phases/185-records-and-checks-that-are-current/185-03-SUMMARY.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md

Note on omissions: `.planning/ROADMAP.md`, `.planning/STATE.md` and `.planning/PROJECT.md` are deliberately
not referenced. `185-03-SUMMARY.md` IS referenced: this plan's safety rests on that plan having removed
every test-side reference to the symbol, and that fact must be confirmed rather than assumed.
</context>

<!-- planner-discipline-allow: _is_interactive -->
<!-- planner-discipline-allow: \.py:[0-9]+ -->

<failing_direction_convention>
Every `<automated>` command here is a GREEN-direction command with a `<fails_when>` naming an observable.
Two of them are absence gates; each is paired with a non-vacuity control in the same `<verify>` block, so a
grep that read nothing cannot be mistaken for a grep that found nothing.
</failing_direction_convention>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead TTY-check helper and all three of its dependents, in one commit (D-07, CLAIM-07)</name>
  <reversibility rating="reversible">A pure deletion of a symbol with zero call sites plus two docstring edits and one allow-list entry; recoverable from git in a single revert. CONTEXT.md rates D-07 reversible.</reversibility>
  <precondition>`firestarter_app/tests/test_dev_test_cmd.py` contains zero references to the symbol — plan 185-03 removed the module-docstring clause, the forcing helper and all three direct patch sites. Verify with `git grep` before deleting anything; if any reference survives, this task's deletion reds that module.</precondition>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py` lines 2320-2340 — the helper definition,
      its docstring and its `return`, plus the function boundaries either side so the deletion takes the
      whole definition and nothing adjacent.
    - `/workspaces/firestarter_app/tools/check_devtest_orchestrator.py` lines 55-80 (the module-docstring
      paragraph that ENUMERATES the allow-list names in prose — this is the site CONTEXT.md's D-07 does not
      name) and lines 140-185 (the `_HANDLER_FUNCTION_NAMES` frozenset literal and `_scan_target_functions`).
    - `/workspaces/firestarter_app/tests/test_check_devtest_orchestrator.py` lines 486-566 (both
      `test_handler_function_names_all_resolve_to_real_callables` and
      `test_handler_function_names_contains_the_uv_scope_helper`, so you know which leg the allow-list edit
      protects and which is unaffected) and lines 565-625 — the whole subset-vs-equality docstring,
      including its two existing in-place supersession sentences, which are the pattern the repair copies.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md` §B.4 (the full
      tracked-reference table with the disposition for each of the ten lines) and §B.5 (the computed
      listed-but-not-body-referenced sets before and after the deletion, which is what makes the
      replacement example a fact rather than a guess).
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-PATTERNS.md` §
      "`firestarter_app/tests/test_check_devtest_orchestrator.py:585`" — the verbatim docstring and the
      statement that the repair is a name substitution inside existing prose.
    - `/workspaces/CLAUDE.md` § "Source code comments — hard rule".
  </read_first>
  <files>firestarter_app/firestarter/cli_handlers.py, firestarter_app/tools/check_devtest_orchestrator.py, firestarter_app/tests/test_check_devtest_orchestrator.py</files>
  <action>
    Work in `/workspaces/firestarter_app` on branch
    `gsd/v1.37-operator-safety-answered-reports-claim-hygiene`. All four edits land in ONE commit — the
    allow-list gate asserts every listed name resolves to a real callable, so splitting the deletion from
    the allow-list edit produces a deliberately red intermediate commit for no gain.

    STEP 0 — ENUMERATE, then baseline. Run `git grep -n` for the symbol across the app repo and record
    every hit with its file and line. Expect exactly four files after plan 185-03: `firestarter/cli_handlers.py`
    (the definition and its own docstring's self-reference), `tools/check_devtest_orchestrator.py` (two
    lines — the docstring enumeration and the allow-list entry), and
    `tests/test_check_devtest_orchestrator.py` (one line — the rationale). If `tests/test_dev_test_cmd.py`
    still appears, STOP: plan 185-03 did not complete and this deletion would red it.

    Use `git grep`, never a bare `grep`. This devcontainer's `grep` is ugrep, which honours `.gitignore` and
    silently under-scans; and `firestarter_app/build/lib/firestarter/cli_handlers.py` is a gitignored,
    untracked stale sdist artefact that still contains the symbol three times. `git grep` scans tracked
    files only and is immune to both traps at once. Do not delete that build artefact, do not chase it, and
    do not report it as a surviving reference.

    Also record the `#` comment-line count of each of the three files before editing: 324, 91 and 98
    respectively.

    STEP 1 — DELETE THE DEFINITION. In `firestarter/cli_handlers.py`, remove the whole helper: the `def`
    line, its docstring, and its `return sys.stdin.isatty()` body. Leave the two blank lines separating the
    surrounding functions in the file's normal shape. Check whether `sys` is still used elsewhere in the
    module before assuming the import can stay or must go — let `ruff check` decide, and do not remove an
    import `ruff` does not flag.

    Do NOT touch `firestarter/submit.py` or `firestarter/jp5_gate.py`. They hold the two real TTY readers,
    each behind its own injectable `isatty_fn`, and neither imports the deleted symbol.

    STEP 2 — REMOVE THE ALLOW-LIST ENTRY. In `tools/check_devtest_orchestrator.py`, delete the symbol's
    entry from the `_HANDLER_FUNCTION_NAMES` frozenset literal, including its trailing comma. That gate
    asserts every listed name resolves to a real callable, so leaving it would red
    `test_handler_function_names_all_resolve_to_real_callables` immediately.

    STEP 3 — REMOVE THE NAME FROM THE SAME FILE'S MODULE DOCSTRING. The module docstring enumerates the
    private co-located helpers the scan narrows to, and the deleted symbol is one of the names in that
    prose list. Delete the name from the list, leaving the surrounding sentence grammatical. CONTEXT.md's
    D-07 does not name this site; it was found during research. Leaving it would end the phase having
    authored a fresh instance of its own defect class — a docstring naming a symbol that no longer exists —
    in a file that `firestarter_app/tools/` places outside every CI gate, with no mypy, no `ruff check` and
    no `ruff format` to catch it.

    STEP 4 — REPAIR THE SUBSET-VS-EQUALITY RATIONALE BY SUBSTITUTION. In
    `tests/test_check_devtest_orchestrator.py`, the docstring justifies a SUBSET assertion by naming one
    allow-listed helper that is legitimately listed but not referenced from the handler's body, and
    observes that an equality assertion would therefore be red on day one.

    Do not author a new paragraph. Substitute the deleted symbol's name for a still-live example, and keep
    the sentence's structure intact. Choose the replacement by MEASUREMENT, not from this plan: compute the
    set difference between the `_HANDLER_FUNCTION_NAMES` literal and the expected-referenced-helpers
    literal as they stand AFTER Step 2, and pick a member of that difference that is a private helper
    called only indirectly. Research computed three such members remain after the deletion; verify that for
    yourself and record the computed set in the SUMMARY. State in the SUMMARY why the substituted example
    keeps the subset direction correct — the replacement must be listed and not body-referenced, or the
    rationale stops explaining the assertion.

    The docstring already carries two in-place supersession sentences naming earlier entries that stopped
    being examples. A third such sentence is permitted and matches the file's own established shape; what
    is not permitted is a `#` comment anywhere in any of these three files.

    STEP 5 — PROVE AND COMMIT. Run the two orchestrator-gate modules, `ruff check`, `ruff format --check`
    and the mypy watermark, then commit all three files together.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git grep -n _is_interactive ; echo "exit=$?"</automated>
    <fails_when>the trailing line does not read `exit=1`, or any file:line is printed — a tracked reference to the deleted symbol survives. `git grep` excludes `build/lib/…` by construction, so a hit here is always a real source reference and never the stale sdist artefact</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -c _make_sampler -- firestarter/cli_handlers.py tools/check_devtest_orchestrator.py</automated>
    <fails_when>fewer than two lines printed, or any printed count is `0` — this is the non-vacuity control for the gate above. `_make_sampler` is the allow-list neighbour that stays; if the search machinery reads nothing here, the absence result above proves nothing</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_check_devtest_orchestrator.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or any test reported as failed. A failure in `test_handler_function_names_all_resolve_to_real_callables` means the allow-list entry was not removed; a failure in the subset leg means the substituted example is not actually listed-but-unreferenced</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_dev_test_cmd.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line does not read `63 passed` — the count plan 185-03 left. A collection error naming the deleted symbol means plan 185-03's severance was incomplete</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/ && .venv311/bin/ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit from either command, or any diagnostic or file name printed — most likely an import left unused by the deletion, or a reflowed docstring. Note `tools/` is NOT in either command's scope, so neither catches anything in `tools/check_devtest_orchestrator.py`</fails_when>
    <automated>cd /workspaces/firestarter_app && ls -l .venv311/bin/mypy && .venv311/bin/python tools/check_mypy_watermark.py</automated>
    <fails_when>the `ls -l` prints no such file — the watermark tool reports OK when mypy is MISSING, so its absence would fake a pass — or the watermark script exits non-zero or reports a count above its recorded watermark</fails_when>
    <automated>cd /workspaces/firestarter_app && for f in firestarter/cli_handlers.py tools/check_devtest_orchestrator.py tests/test_check_devtest_orchestrator.py; do printf '%s %s\n' "$f" "$(/usr/bin/grep -c '^[[:space:]]*#' $f)"; done</automated>
    <fails_when>any printed count exceeds its pre-task figure of 324, 91 and 98 respectively — an increase means a `#` comment line was authored under `firestarter_app/`, which the project's hard rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the Step-0 `git grep` enumeration verbatim, showing the symbol present in exactly
      the three files named, and explicitly showing `tests/test_dev_test_cmd.py` ABSENT from the list.
    - The SUMMARY states that `git grep` was used rather than a bare `grep`, names both devcontainer traps
      it avoids, and records that `build/lib/firestarter/cli_handlers.py` still contains the symbol, is
      untracked and gitignored, and was deliberately not touched.
    - `firestarter/cli_handlers.py` no longer contains the helper's `def`, docstring or `return`.
    - `tools/check_devtest_orchestrator.py` no longer names the symbol in either the `_HANDLER_FUNCTION_NAMES`
      literal or the module-docstring enumeration, and the enumerating sentence still reads grammatically.
    - The SUMMARY records the COMPUTED set difference between the allow-list literal and the
      expected-referenced-helpers literal after the deletion, names the member chosen as the replacement
      example, and states why that member keeps the subset direction correct.
    - The repaired rationale in `tests/test_check_devtest_orchestrator.py` is a name substitution inside the
      existing prose — the SUMMARY shows the before and after of the changed sentence, and no new paragraph
      was authored.
    - `git grep` for the deleted symbol returns no tracked match, and the `_make_sampler` control search
      returns non-zero counts in both files.
    - `tests/test_check_devtest_orchestrator.py` and `tests/test_dev_test_cmd.py` are both green, the latter
      at `63 passed`.
    - `firestarter/submit.py` and `firestarter/jp5_gate.py` do not appear in this task's diff.
    - The three comment-line counts did not increase against 324, 91 and 98.
    - `git show --stat HEAD` shows all three files in one commit.
  </acceptance_criteria>
  <done>The symbol has no tracked reference anywhere in the app repo, all three dependents moved with it in one commit, and both orchestrator-gate legs are green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the file-plus-line-number citation with the symbol-and-scope form the same file already uses (D-13, CLAIM-06)</name>
  <reversibility rating="reversible">A one-sentence docstring edit in a single test module.</reversibility>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_numeric_schema_source_scan.py` lines 33-48 — the defective
      sentence and the clause that follows it, which ALREADY states the scope in prose. Then lines 122-140 —
      the `_top_level_dict_constant_names` helper docstring, which is the correct form to copy: it names the
      symbol and its enclosing scope with no line number at all.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-PATTERNS.md` §
      "`firestarter_app/tests/test_numeric_schema_source_scan.py:40`" — both passages quoted verbatim side
      by side.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-CONTEXT.md` § D-13 — the
      evidence that the line-number form cannot be kept: the symbol has moved twice, and the requirement's
      own figure staled within a day of being authored.
  </read_first>
  <files>firestarter_app/tests/test_numeric_schema_source_scan.py</files>
  <action>
    STEP 1 — LOCATE, do not transcribe. Find the defect by searching the file for a file-plus-line-number
    citation: `/usr/bin/grep -nE "\.py:[0-9]+" tests/test_numeric_schema_source_scan.py`. It is measured to
    return exactly ONE line — the parenthetical attached to `_AT28C_DIP24_NAMES` in the numbered list item
    that explains why scoping to `tree.body` keeps the test from firing on that symbol.

    STEP 2 — CONFIRM THE TRUE SCOPE FROM SOURCE, not from any document. Run
    `/usr/bin/grep -n "_AT28C_DIP24_NAMES" tools/build_db.py` and `/usr/bin/grep -n "^def " tools/build_db.py`
    and read the region. Establish for yourself that it is a set literal (not a dict), a local variable
    nested inside a `for` loop inside `main()`. Record what you found.

    STEP 3 — REPAIR BY REMOVING THE NUMBER, not by correcting it. Replace the parenthetical with the
    symbol-and-scope form the same file already uses in the `_top_level_dict_constant_names` docstring — "a
    local variable nested inside a `for` loop deep in `main()`" — or simply delete the parenthetical
    outright, since the very next clause in the same sentence already states that the set literal is a local
    variable nested inside a `for` loop several indent levels deep. Either form satisfies the requirement;
    choose the one that reads better in place and say which you chose and why.

    Write NO line number. Not the figure currently in the file, not the figure `REQUIREMENTS.md` names, not
    the figure the symbol sits at today. The citation has staled three times and the requirement's own
    figure staled within a day of being written; a corrected number would stale a fourth time on the next
    edit to that file. That is the entire point of D-13.

    STEP 4 — CHANGE NOTHING ELSE in the file. This is a one-site change: the file is measured to hold
    exactly one such citation, so the count moves from one to zero and no other line is touched. Add no
    `#` comment. The comment-line count is 37 before this task and must not increase.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -cE "\.py:[0-9]+" tests/test_numeric_schema_source_scan.py</automated>
    <fails_when>the printed count is anything other than `0` — a file-plus-line-number citation survives, and the boundary CLAIM-06 sets is exactly zero rather than "fewer"</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c "_AT28C_DIP24_NAMES" tests/test_numeric_schema_source_scan.py</automated>
    <fails_when>the printed count is not `2` — the symbol is named twice in this file, once in the repaired sentence and once in the helper docstring. A `0` means the symbol was deleted along with the number, and is also the non-vacuity control proving the gate above read a populated file</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_numeric_schema_source_scan.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or any test reported as failed — the module AST-parses its own source in places, so a malformed docstring edit shows here</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff --stat -- tests/test_numeric_schema_source_scan.py</automated>
    <fails_when>the printed insertions-plus-deletions total exceeds 8 — this is a one-sentence repair, and a larger diff means something beyond the citation was rewritten</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c '^[[:space:]]*#' tests/test_numeric_schema_source_scan.py</automated>
    <fails_when>the printed count is greater than `37` — a `#` comment line was authored, which the project's hard rule forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the Step-1 grep output before the edit, showing exactly one matching line.
    - The SUMMARY records the Step-2 source lookup in `tools/build_db.py` and what it established about the
      symbol's kind and scope — read from source in this session, not carried from any document.
    - The repaired sentence names `_AT28C_DIP24_NAMES` and its enclosing scope, and contains no digits
      following a `.py` token.
    - The SUMMARY states which repair form was chosen — scope parenthetical or outright deletion — and why.
    - The SUMMARY states explicitly that no replacement line number was written, and names the three
      figures that were deliberately not transcribed without reproducing them into the source file.
    - `/usr/bin/grep -cE "\.py:[0-9]+"` over the file returns `0`, and the symbol still appears twice.
    - The module's own tests are green and the diff is a one-sentence change.
    - The comment-line count did not increase against 37.
  </acceptance_criteria>
  <done>The file contains no file-plus-line-number citation, and the surviving sentence names the symbol and the scope that makes the test's behaviour correct.</done>
</task>

<task type="auto">
  <name>Task 3: App-repo gate sweep — the checks CI will run, on the Python version CI uses (CLAIM-06, CLAIM-07)</name>
  <reversibility rating="reversible">A verification pass; it changes no file.</reversibility>
  <read_first>
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` — the actual gate steps and the
      `python-version` they run on, so this sweep runs what CI runs rather than an approximation.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-RESEARCH.md` §
      "Verification Reachability" criterion 4 — the PASS and FAIL signatures for each command, including
      which failure implies which missed edit.
    - `/workspaces/.planning/phases/185-records-and-checks-that-are-current/185-03-SUMMARY.md` — the
      `63 passed` figure this sweep must reproduce.
  </read_first>
  <files>firestarter_app/tests/test_dev_test_cmd.py, firestarter_app/tests/test_check_devtest_orchestrator.py, firestarter_app/tests/test_numeric_schema_source_scan.py</files>
  <action>
    This task modifies nothing. The three files are listed because they are the modules whose green state is
    the property under test.

    Run every app gate on `/workspaces/firestarter_app/.venv311`, never the devcontainer's Python 3.12. App
    CI is Python 3.11 only, and 3.12 has provably masked breakage that reddened `beta` CI here.

    The three modules this phase touched, plus a whole-suite COLLECTION check. Do NOT run the whole suite:
    `/dev/ttyACM0` is currently attached, which reddens the `test_no_programmer_found_*` characterization
    tests for reasons unrelated to this phase (a live port beats the patched empty-comports list), and
    `tests/test_flash_path_record_sync.py` asserts the whole repository is git-porcelain, which it is not
    mid-phase. A `--collect-only` run over `tests/` proves no edit broke an import anywhere in the suite,
    which is the property a whole-suite run would add here.

    Then `ruff check`, `ruff format --check` and the mypy watermark, all scoped exactly as CI scopes them.
    Confirm `.venv311/bin/mypy` exists before trusting the watermark result — that tool reports OK when
    mypy is missing, so its absence fakes a pass.

    Record every command, its exit code and its summary line in the SUMMARY. If a characterization test is
    encountered despite the scoping and it fails for the attached-port reason, record it by name with that
    attribution rather than treating it as a finding of this phase.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_dev_test_cmd.py tests/test_check_devtest_orchestrator.py tests/test_numeric_schema_source_scan.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failure, or the passed count is lower than the sum of the three modules' individual counts (63 for the first, plus the other two unchanged from their pre-phase figures) — a lower total means a test was lost rather than merged</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ --collect-only -q -o addopts=""</automated>
    <fails_when>non-zero exit, or any collection ERROR line — an import broken by the symbol deletion anywhere in the suite surfaces here even though the suite is not run</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python --version</automated>
    <fails_when>the printed version does not begin `Python 3.11` — every gate above must run on the version app CI uses; the devcontainer default is 3.12 and has masked real breakage here before</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/ && .venv311/bin/ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit from either, or any diagnostic or file name printed</fails_when>
    <automated>cd /workspaces/firestarter_app && ls -l .venv311/bin/mypy && .venv311/bin/python tools/check_mypy_watermark.py</automated>
    <fails_when>the `ls -l` reports no such file, or the watermark script exits non-zero or reports an error count above its recorded watermark</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records every command run, its exit code and its summary line verbatim.
    - The SUMMARY records the Python version the gates ran on, and states it is the version app CI uses.
    - The SUMMARY states why the whole suite was not RUN and what was run instead, naming both reasons —
      the attached `/dev/ttyACM0` and the porcelain-asserting module.
    - The collection check over `tests/` reports no ERROR.
    - `ruff check`, `ruff format --check` and the mypy watermark are all recorded clean, with the mypy
      binary's existence confirmed before the watermark result is trusted.
  </acceptance_criteria>
  <done>Every gate app CI runs is green on the Python version app CI uses, and the reason the full suite was collected rather than executed is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an allow-list literal → the symbols it claims exist | `_HANDLER_FUNCTION_NAMES` asserts every listed name resolves to a real callable. The list is what kept a dead symbol alive. |
| `firestarter_app/tools/` → CI | This directory sits outside every CI gate: no mypy, no `ruff check`, no `ruff format`. A stale docstring here is caught by nothing. |
| a docstring citation → a future reader | A file-plus-line citation is read as fact and has staled three times; the requirement's own figure staled within a day. |
| the tracked tree → an untracked build mirror | `build/lib/firestarter/cli_handlers.py` still contains the deleted symbol and looks like a surviving reference to any tool that does not restrict itself to tracked files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-185-04-A | Spoofing | `firestarter_app/tools/check_devtest_orchestrator.py` module docstring | high | mitigate | The docstring enumeration is deleted in the same commit as the allow-list entry and the symbol. Because `tools/` is outside every CI gate, the only control is the explicit `git grep` absence gate in Task 1 and the criterion naming this site by itself. |
| T-185-04-B | Tampering | the subset-vs-equality rationale | high | mitigate | Repaired by name substitution into existing prose; the replacement example is chosen by a computed set difference recorded in the SUMMARY, and the orchestrator-gate module is run so an example that is not actually listed-but-unreferenced fails immediately. |
| T-185-04-C | Repudiation | the "no references remain" claim | high | mitigate | `git grep` is mandated over bare `grep`, with both devcontainer traps named; the absence gate is paired with a `_make_sampler` non-vacuity control so a search that read nothing cannot pass as a search that found nothing. |
| T-185-04-D | Repudiation | the repaired citation | medium | mitigate | The scope is re-established from `tools/build_db.py` source in-session rather than carried from a document, and the repair removes the number rather than correcting it, so the class of staleness is closed instead of reset. |
| T-185-04-E | Denial of service (a gate faking a pass) | the mypy watermark | medium | mitigate | `ls -l .venv311/bin/mypy` runs immediately before the watermark script in the same `<automated>` leg, because the watermark tool reports OK when mypy is absent. |
| T-185-04-F | Elevation of privilege (a behaviour change smuggled into a record repair) | `submit.py`, `jp5_gate.py` | medium | mitigate | An explicit `must_haves` prohibition and an acceptance criterion that neither file appears in the task's diff. |
| T-185-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency; it edits four files already in the repository and runs the existing `.venv311`. No Package Legitimacy Audit table is required and none is fabricated. |
</threat_model>

<flagged_assumptions>
- **CLAIM-06 probe rows: `boundary` is resolved EXPLICITLY; `precision` is resolved as a BACKSTOP.** The
  boundary row becomes the exact-zero citation-count truth above. The precision row — "where can precision
  loss, overflow, or rounding/tie-breaking occur, and what is the exact contract?" — has no genuine referent
  against a docstring-citation requirement: a citation carries no arithmetic, so there is no rounding
  contract to state. It is authored as a `verification: backstop` marker naming the nearest real analogue
  (no digit sequence follows a `.py` token anywhere in the file) rather than dismissed, because the protocol
  forbids auto-dismissal and a wrong dismissal is the silent failure it exists to prevent. If the verifier
  cannot confirm that marker from explicit evidence it should abstain rather than pass it.
- **CLAIM-07's `unclassified` probe row is surfaced in plan 185-03**, which owns the test-name half of that
  requirement. It is not re-surfaced here.
- **The replacement example for the subset rationale is NOT named in this plan.** Research computed three
  candidates, but naming one here would let an executor write it without checking that it is still listed
  and still only indirectly called. The plan requires the set difference to be computed after the deletion
  and recorded; that is the fact the substitution rests on.
- **The `git diff --stat` ceiling of 8 in Task 2 is a heuristic, not a measurement.** It exists to make an
  over-broad rewrite visible. If the chosen repair form legitimately needs a few more lines because the
  sentence rewraps, record the actual figure and the reason rather than forcing the diff to fit.
</flagged_assumptions>

<artifacts_this_phase_produces>
Created by this plan: no new file and no new symbol. Every edit is a deletion or a substitution inside
existing text.

Deleted by this plan (a distinct list — a source-grounding drift pass must not flag these disappearances):
- `_is_interactive` — the function, its docstring and its body, from
  `firestarter_app/firestarter/cli_handlers.py`.
- the `"_is_interactive",` entry from the `_HANDLER_FUNCTION_NAMES` frozenset in
  `firestarter_app/tools/check_devtest_orchestrator.py`.
- the same name from that file's module-docstring enumeration of private co-located helpers.
- the same name from the subset-vs-equality rationale in
  `firestarter_app/tests/test_check_devtest_orchestrator.py`, replaced by a still-live example.
- the file-plus-line-number parenthetical attached to `_AT28C_DIP24_NAMES` in
  `firestarter_app/tests/test_numeric_schema_source_scan.py`.

Kept deliberately, with the reason on the record:
- `firestarter_app/firestarter/submit.py` and `firestarter_app/firestarter/jp5_gate.py` — the two real TTY
  readers, each behind its own injectable seam; untouched.
- `firestarter_app/build/lib/firestarter/cli_handlers.py` — gitignored, untracked stale sdist artefact that
  still contains the deleted symbol. NOT source, NOT deleted, NOT a surviving reference.
- `_AT28C_DIP24_NAMES` itself in `tools/build_db.py` — the citation is repaired; the symbol is not moved,
  renamed or touched.
</artifacts_this_phase_produces>

<verification>
- `git grep` for the deleted symbol over the app repo returns no tracked match, with a non-vacuity control
  proving the search read populated files.
- `tests/test_check_devtest_orchestrator.py`, `tests/test_dev_test_cmd.py` (at `63 passed`) and
  `tests/test_numeric_schema_source_scan.py` are all green on `.venv311`.
- `pytest tests/ --collect-only` over the whole suite reports no ERROR.
- `/usr/bin/grep -cE "\.py:[0-9]+"` over `tests/test_numeric_schema_source_scan.py` returns `0` while
  `_AT28C_DIP24_NAMES` still appears twice.
- `ruff check` and `ruff format --check` over `firestarter/ tests/` are clean; the mypy watermark is clean
  with the mypy binary's existence confirmed first.
- Comment-line counts did not increase in any of the four touched files.
- `submit.py` and `jp5_gate.py` appear in no diff from this plan.
- The SUMMARY carries the `git grep` enumeration, the computed set difference behind the substituted
  example, the before/after of the changed sentence, and the source lookup that re-established the cited
  symbol's scope.
</verification>

<success_criteria>
The dead TTY-check helper is gone and nothing tracked still names it — including the module-docstring
enumeration in a file outside every CI gate, which no requirement named and which would otherwise have left
this phase authoring a fresh instance of its own defect class. Its three dependents moved with it in one
commit, and the subset rationale survives by naming a still-live example rather than by inventing new prose.
The one surviving file-plus-line citation is gone rather than corrected, so the form that has staled three
times cannot stale a fourth.
</success_criteria>

<output>
Create `.planning/phases/185-records-and-checks-that-are-current/185-04-SUMMARY.md` when done
</output>
