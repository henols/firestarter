---
phase: 182-jp5-destructive-operation-gate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_ic_layout.py
  - firestarter_app/firestarter/ic_layout.py
autonomous: true
requirements: [SAFE-05]
commits_land_in: firestarter_app submodule on gsd/v1.37-operator-safety-answered-reports-claim-hygiene — plus one meta commit in /workspaces on the same branch name advancing the firestarter_app gitlink by name
tags: [dead-code-deletion, renderer, jumper-display, python]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "_get_rev2_2_jumper_settings_data and its commented-out call site are deleted, so no code path can render JP5 as an operator-settable Rev 2.2 config header (SAFE-05, D-08)."
    - "JP5 is a solder jumper whose KiCad footprint is a Bridged SolderJumper — bridged by default and unreadable by the tool — so a renderer presenting it as a two-position header was describing something that does not exist."
    - "The live sibling _get_rev2_jumper_settings_data survives and still emits its 2.0 & 2.1 jp4 block, proved by a positive assertion rather than by the deletion's absence."
    - "The deletion is guarded for the first time: a test asserts the symbol is absent from the class, and it was observed RED before the deletion and GREEN after."
    - "The verification grep is scoped so it cannot pass or fail for the wrong reason: a stale gitignored build/lib copy of ic_layout.py exists, and PATH grep in this devcontainer is ugrep which honours .gitignore, so the ROADMAP's literal success-criterion command reads GREEN with one grep and RED with the other for reasons unrelated to the deletion."
    - "firestarter info still prints JP4 = Closed for the eight 8 Mbit parts after this phase, because has_vpp_pin_on_map only tests key presence and the new layout declares vpp-pin [24] — that is the D-09 phase's job, recorded here so a later reader does not read the surviving wrong output as a Phase 182 regression."
  artifacts:
    - firestarter_app/tests/test_ic_layout.py
  key_links:
    - "IcLayout class surface -> hasattr assertion in test_ic_layout.py -> the deleted method"
    - "get_chip_layout output jumpers dict -> the surviving 2.0 & 2.1 jp4 block"
  prohibitions:
    - "No acceptance criterion in this plan may use a bare `grep -rn` over `firestarter_app/`, and none may assert that a comment exists."
  flagged_assumptions:
    - "SAFE-05 (unclassified, unresolved): nothing in firestarter_app/tests/ mentions jumper today, so ic_layout.py's whole jumper derivation is unguarded by construction. This plan adds the first two guards, which cover the deleted symbol and the surviving sibling only — not the derivation's correctness, which is jumper-display-ground-truth.md's confirmed defect 1 and the D-09 phase's scope."
---

<objective>
Delete the dead JP5 renderer, and guard the deletion so it cannot silently come back.

Purpose: `_get_rev2_2_jumper_settings_data` presents `A19_CUT` — a solder jumper the tool cannot read
and which ships bridged — as an operator-settable Rev 2.2 config header. It is `firestarter info`'s
confirmed defect 3 in `.planning/notes/jumper-display-ground-truth.md`, and it is the one requirement
of this phase that is a straight deletion.

Output: two new tests in `tests/test_ic_layout.py` — one observed RED before the deletion — and the
method plus its commented call site removed from `firestarter/ic_layout.py`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md
@.planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md
@.planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md
@.planning/notes/jumper-display-ground-truth.md
@/workspaces/CLAUDE.md
</context>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| test function | `test_the_rev_2_2_jp5_renderer_is_absent_from_the_class` | `firestarter_app/tests/test_ic_layout.py` |
| test function | `test_the_rev_2_jp4_renderer_still_emits_its_jumper_block` | `firestarter_app/tests/test_ic_layout.py` |
| deletion | `IcLayout._get_rev2_2_jumper_settings_data` | `firestarter_app/firestarter/ic_layout.py` |
| deletion | the commented-out call site beneath the two live `output_data["jumpers"].update(...)` calls | `firestarter_app/firestarter/ic_layout.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard the deletion first — one test observed RED, one positive sibling test</name>
  <files>firestarter_app/tests/test_ic_layout.py</files>
  <precondition>The firestarter_app submodule is on branch gsd/v1.37-operator-safety-answered-reports-claim-hygiene; create it off the current gsd/v1.36-dev-test-fidelity HEAD if it does not exist. Never work on beta or main in any repository.</precondition>
  <read_first>
    - firestarter_app/tests/test_ic_layout.py in full (about 312 lines, 9 tests) — how it constructs the layout object, how it obtains a chip's data, and what it asserts on. It covers pin-name display, type labels and erase-capability rows; nothing in it touches the jumper block, so both new tests are the first of their kind here.
    - firestarter_app/firestarter/ic_layout.py lines 165-205 — `_get_rev2_jumper_settings_data` (the live sibling that must SURVIVE byte-unchanged, keyed "2.0 & 2.1" with a "jp4" sub-dict) and `_get_rev2_2_jumper_settings_data` (the near-copy keyed "2.2" with a "jp5" sub-dict, whose parameter is named jp5).
    - firestarter_app/firestarter/ic_layout.py lines 615-660 — the pin-count-keyed jumper derivation, `has_vpp_pin_on_map` set from mere `vpp-pin` key presence, the `jp4_rev2 = 2 if has_vpp_pin_on_map else 1` line at 649, the two live `output_data["jumpers"].update(...)` calls, and the commented-out third call immediately beneath them.
    - .planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md section 3 — the exact excerpt of the surviving sibling and of the commented call, plus the grep-trap note.
    - .planning/notes/jumper-display-ground-truth.md — confirmed defect 3, which is this deletion, and confirmed defect 1 (the pin-count key rather than pin-map identity), which is NOT this phase's.
  </read_first>
  <behavior>
    - `hasattr(IcLayout, "_get_rev2_2_jumper_settings_data")` is False. This test must be observed RED before the deletion, since the method exists today.
    - `get_chip_layout` for a 32-pin chip whose pin map declares a `vpp-pin` returns a `jumpers` dict containing a "2.0 & 2.1" key whose value carries a "jp4" sub-dict with `config_text`, `display`, `pin_text` and `selected_label`.
    - That same `jumpers` dict contains no "2.2" key and no "jp5" key at any depth.
  </behavior>
  <action>
Add the two tests named in the artifacts table to `tests/test_ic_layout.py`, using that file's
existing construction idiom for the layout object and chip data.

Test one, the deletion guard: assert `hasattr` on the `IcLayout` class for the string
`_get_rev2_2_jumper_settings_data` is False. Run it BEFORE the deletion and record the RED output
verbatim in `182-04-SUMMARY.md`. This matters because a test asserting the ABSENCE of the rendered
"2.2"/"jp5" block would be vacuous — the call site is already commented out, so such a test passes
today and would prove nothing. The `hasattr` form is the non-vacuous one: it fails today, for the
right reason.

Test two, the survivor guard: call `get_chip_layout` for a 32-pin chip whose pin map declares a
`vpp-pin`, and assert the returned `jumpers` dict contains the "2.0 & 2.1" key with its "jp4"
sub-dict carrying all four of `config_text`, `display`, `pin_text` and `selected_label`. Then assert
no "2.2" key and no "jp5" key appears anywhere in that dict, at any depth. This is the positive
assertion that fails if Task 2's deletion removes the wrong method — the two methods are near-copies,
so a mis-targeted deletion is a real risk and it must be caught by a test rather than by review.

Do not assert anything about the correctness of the JP4 value itself. `has_vpp_pin_on_map` tests only
key presence, so `firestarter info` will still print JP4 = Closed for a 32-pin part with any declared
`vpp-pin` — including the new `DIP32_27C801` with `vpp-pin [24]`. That is
`jumper-display-ground-truth.md`'s confirmed defect 1 and squarely the D-09 phase's job. Record it in
`182-04-SUMMARY.md` explicitly so a later reader does not read the surviving wrong output as a Phase
182 regression, but do not fix it and do not assert against it here.

Commit inside the submodule and advance the meta gitlink by name.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_ic_layout.py -o addopts="" -q -k "rev_2_2_jp5_renderer_is_absent"; echo "exit=$?"</automated>
    <fails_when>a line reading "exit=0" at this point in the task — the deletion guard MUST be RED before Task 2 runs, because the method still exists; a green result here means the test is vacuous and proves nothing</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_ic_layout.py -o addopts="" -q -k "rev_2_jp4_renderer_still_emits"</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" — the survivor guard must be GREEN before the deletion, so that a later RED unambiguously means the wrong method was deleted</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app grep -n 'def test_the_rev_2_2_jp5_renderer_is_absent_from_the_class' -- tests/test_ic_layout.py` prints one line.
    - `git -C /workspaces/firestarter_app grep -n 'def test_the_rev_2_jp4_renderer_still_emits_its_jumper_block' -- tests/test_ic_layout.py` prints one line.
    - The deletion guard was observed RED and its output is transcribed verbatim into `182-04-SUMMARY.md`.
    - The survivor guard was observed GREEN before the deletion, and its assertion names all four of `config_text`, `display`, `pin_text` and `selected_label`.
    - `git -C /workspaces/firestarter_app diff --name-only HEAD~1` names only `tests/test_ic_layout.py` for this commit — no production file moved in the guard commit.
    - `.venv311/bin/python -m ruff check tests/test_ic_layout.py` exits 0 and `.venv311/bin/python -m ruff format --check tests/test_ic_layout.py` exits 0.
  </acceptance_criteria>
  <done>Two guards exist: one that fails today for the right reason, and one that passes today and would fail if the wrong method were deleted. The RED output is transcribed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the method and its commented call site</name>
  <files>firestarter_app/firestarter/ic_layout.py</files>
  <read_first>
    - firestarter_app/firestarter/ic_layout.py lines 165-205 — read both methods side by side before cutting, so the near-copy is not confused with its live sibling. The one to delete is keyed "2.2" with a "jp5" sub-dict and takes a parameter named jp5; the one to keep is keyed "2.0 & 2.1" with a "jp4" sub-dict and takes jp4.
    - firestarter_app/firestarter/ic_layout.py lines 645-660 — the two live `output_data["jumpers"].update(...)` calls (for the rev1 and rev2 renderers) which must both survive, and the commented-out third call beneath them carrying a `# noqa: E501` suffix, which is the line to delete.
    - firestarter_app/tests/test_ic_layout.py — the two guards from Task 1.
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md section "Success criterion 4 has a grep trap" — the measured reason the ROADMAP's literal criterion command answers the wrong question, and the two tool-independent replacements.
  </read_first>
  <action>
Delete `_get_rev2_2_jumper_settings_data` entirely — the `def` line, its docstring, the
`_select_jumper_label` call, the `jumper_display` list and the returned dict — and delete the
commented-out call site beneath the two live `output_data["jumpers"].update(...)` calls. Outright
deletion with no deprecation shim and no alias, following the Phase 181 precedent.

Keep both live `output_data["jumpers"].update(...)` calls immediately above the deleted comment, and
keep `_get_rev2_jumper_settings_data` byte-unchanged. Do not touch the `jp4_rev2` derivation, the
`has_vpp_pin_on_map` assignment, or anything else in the derivation block: the derivation's
correctness is `jumper-display-ground-truth.md`'s confirmed defect 1 and the D-09 phase's scope, and
widening this deletion into it would be the scope creep CONTEXT's out-of-scope list forbids.

Add no comment in place of the deleted code — `/workspaces/CLAUDE.md`'s no-comments-in-product-source
rule is not overridable, and a tombstone comment is exactly what it forbids. The reason for the
deletion goes in the commit message and in `182-04-SUMMARY.md`.

Verify the deletion with a tool-independent, tracked-files-only grep. Do NOT use the ROADMAP's
literal success-criterion command `grep -rn '_get_rev2_2_jumper_settings_data' firestarter_app/`:
measured this session, a stale copy of the symbol survives at
`firestarter_app/build/lib/firestarter/ic_layout.py`, `build/` is gitignored, and PATH `grep` in this
devcontainer is ugrep which honours `.gitignore` — so that one command reads GREEN with the PATH grep
and RED with `/usr/bin/grep`, both for reasons that have nothing to do with the deletion. Use
`git -C firestarter_app grep` scoped to tracked Python files, or `/usr/bin/grep -rn` with
`--exclude-dir=build --exclude-dir=__pycache__`. Record in `182-04-SUMMARY.md` that the ROADMAP's
criterion 4 was satisfied through the scoped form and why the literal form was not used.

Commit inside the submodule and advance the meta gitlink by name.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git grep -n '_get_rev2_2_jumper_settings_data' -- '*.py'; test $? -ne 0</automated>
    <fails_when>exit 1, meaning git grep printed a surviving occurrence of the symbol in a tracked Python file</fails_when>
    <automated>/usr/bin/grep -rn --exclude-dir=build --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=.venv311 '_get_rev2_2_jumper_settings_data' /workspaces/firestarter_app/; test $? -ne 0</automated>
    <fails_when>exit 1, meaning GNU grep found a surviving occurrence outside the gitignored build and cache directories — this is the second, tool-independent confirmation of ROADMAP success criterion 4</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -n '_get_rev2_jumper_settings_data' -- firestarter/ic_layout.py</automated>
    <fails_when>non-zero exit, meaning the live sibling was deleted too — the wrong method was cut</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_ic_layout.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" — both Task 1 guards must now be green, the deletion guard having flipped from RED</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app grep -n '_get_rev2_2_jumper_settings_data' -- '*.py'` prints nothing (tracked files only, tool-independent).
    - `/usr/bin/grep -rn --exclude-dir=build --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=.venv311 '_get_rev2_2_jumper_settings_data' /workspaces/firestarter_app/` prints nothing.
    - `git -C /workspaces/firestarter_app grep -c 'output_data\["jumpers"\].update' -- firestarter/ic_layout.py` prints exactly 2 — both live calls survive and the commented third is gone.
    - `git -C /workspaces/firestarter_app grep -n 'def _get_rev2_jumper_settings_data' -- firestarter/ic_layout.py` prints one line, and `git -C /workspaces/firestarter_app diff HEAD~2 -- firestarter/ic_layout.py` shows no change inside that method's body.
    - `git -C /workspaces/firestarter_app diff HEAD~2 -- firestarter/ic_layout.py` shows no change to the `has_vpp_pin_on_map` assignment or the `jp4_rev2` derivation.
    - Both Task 1 guards pass, and the whole `tests/test_ic_layout.py` module is green.
    - `.venv311/bin/python -m ruff check firestarter/ic_layout.py` exits 0 and `.venv311/bin/python -m ruff format --check firestarter/ic_layout.py` exits 0.
    - `git -C /workspaces ls-tree HEAD firestarter_app` names a commit reachable from the firestarter_app v1.37 branch tip.
  </acceptance_criteria>
  <reversibility rating="reversible">D-08. A dead-code deletion with no production consumer; a revert restores both regions.</reversibility>
  <done>No code path can render JP5 as an operator-settable config header; the live JP4 renderer is provably untouched; and ROADMAP success criterion 4 is satisfied through a grep scoped so it cannot answer the wrong question.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host renderer -> operator's terminal -> operator's soldering iron | The asset is the operator's belief about what a jumper does; a renderer that names a solder jumper as a settable header can cause a wrong physical action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-17 | Spoofing (of hardware capability) | `_get_rev2_2_jumper_settings_data` rendering `A19_CUT` as a Rev 2.2 two-position config header | medium | mitigate | Delete the method and its call site outright; guard with a `hasattr` assertion observed RED before the deletion |
| T-182-18 | Tampering | The near-copy live sibling `_get_rev2_jumper_settings_data` being deleted by mistake | medium | mitigate | A positive test asserting the surviving "2.0 & 2.1" jp4 block, green before the deletion, plus a diff assertion that its body is unchanged |
| T-182-19 | Repudiation | The ROADMAP's literal criterion-4 grep passing or failing for the wrong reason | high | mitigate | Two independent scoped greps: `git grep` over tracked Python files, and GNU `/usr/bin/grep` with `build` and cache directories excluded. Measured cause recorded in the SUMMARY |
| T-182-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_ic_layout.py -o addopts="" -q` — green with the count line visible, both new guards included.
- `cd /workspaces/firestarter_app && git grep -n '_get_rev2_2_jumper_settings_data' -- '*.py'` prints nothing.
- `/usr/bin/grep -rn --exclude-dir=build --exclude-dir=__pycache__ --exclude-dir=.venv --exclude-dir=.venv311 '_get_rev2_2_jumper_settings_data' /workspaces/firestarter_app/` prints nothing.
- `cd /workspaces/firestarter_app && .venv311/bin/python -m ruff check firestarter/ic_layout.py tests/test_ic_layout.py && .venv311/bin/python -m ruff format --check firestarter/ic_layout.py tests/test_ic_layout.py` — both exit 0.
</verification>

<success_criteria>
- SAFE-05 is met: `_get_rev2_2_jumper_settings_data` and its commented-out call site are deleted.
- ROADMAP success criterion 4 is met, through a grep scoped so it cannot pass or fail for the wrong reason, with the reason for the scoping recorded.
- The deletion is guarded for the first time, and the guard was observed RED before the deletion.
- The surviving JP4 renderer is provably unchanged, and the still-wrong JP4 = Closed output for the eight 8 Mbit parts is recorded as the D-09 phase's job rather than a Phase 182 regression.
</success_criteria>

<output>
Create `.planning/phases/182-jp5-destructive-operation-gate/182-04-SUMMARY.md` when done. It must
record: the verbatim RED output of the deletion guard before the deletion; why an
absence-of-rendered-block test would have been vacuous; why the ROADMAP's literal criterion-4 grep
was replaced and with what; and the fact that `firestarter info` still prints JP4 = Closed for the
eight 8 Mbit parts, which is `jumper-display-ground-truth.md`'s confirmed defect 1 and the D-09
phase's scope.
</output>
