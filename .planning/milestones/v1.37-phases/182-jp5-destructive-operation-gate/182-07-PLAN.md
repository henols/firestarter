---
phase: 182-jp5-destructive-operation-gate
plan: 07
type: execute
wave: 4
depends_on: [182-01, 182-03, 182-05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04]
commits_land_in: meta repo /workspaces on gsd/v1.37-operator-safety-answered-reports-claim-hygiene — this plan touches only .planning/ prose and no submodule, so no gitlink advance is owed
tags: [requirements, backlog, record-keeping, decision-record]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05's question is recorded as asked, answered and resolved, with the trace as the recorded reason and against the operator's stated expectation — a reader can see the question, what answered it, and which way it went (D-05)."
    - "SAFE-02 and SAFE-04 are marked CONFIRMED REQUIRED in REQUIREMENTS.md, not retired: the map fix relocates the hazard onto A19 rather than removing it (D-05)."
    - "SAFE-01's wording reflects that the predicate is achieved by correcting the pin map so the requirement's own words are literally true, rather than by deriving a synthetic predicate around a database that was wrong (D-01)."
    - "CLAIM-07 is left untouched because Option B was taken: the gate carries its own injectable isatty_fn and does not revive _is_interactive, so Phase 185 removes it as planned."
    - "D-15.1 is filed as a backlog item: DIP24_2716 and DIP24_2532 declare vpp-pin [21], unreachable on Rev 0, 2.0 and 2.1, so the tool offers writes it cannot physically perform — now measured rather than suspected (D-15)."
    - "D-15.2 is filed as a backlog item: JP4 in the 24-pin position with a 28- or 32-pin part seated routes VPP onto whatever that part carries at that socket position — the JP5/A19 hazard's mirror image, newly reachable at Rev 2.2 and later (D-15)."
    - "The deferred fail-closed generator assertion is filed as a backlog item: every row's size_bytes must fit the address lines its layout declares. It would have caught the 8 Mbit defect at build time."
    - "The D-09 phase's scope is filed as a backlog item — shield photographs, per-revision jumper tables on the wiki, and the info jumper-block rewrite — and the todo's resolves_phase tag is re-pointed at it, since that phase does not exist yet and Phase 187 must stay last (D-09)."
    - "The A18-on-socket-pin-1 structural remainder is filed as a backlog item: 255 shipped rows on DIP32_SST39SF040 across algorithms 5, 6, 14 and 41, deliberately not gated because the trace established damage capability for the held-boosted-rail EPROM write path only and D-06 forbids gating on inference (D-06)."
    - "The silently-wrong-8-Mbit-read correctness failure is filed if the trace confirmed it: a read above 512 KB on the old map would alias, and a verify against it would pass for the wrong reason. Explicitly not gated per D-06."
    - "Nothing in this plan builds any backlogged item; every one is a stub carrying its goal, its evidence and its test surface."
    - "ROADMAP.md and REQUIREMENTS.md are edited by hand and scoped to the target sections; no gsd requirements or roadmap verb is run, because those reformat the whole file and this ROADMAP is a 6,300-line hand-authored document."
    - statement: "No backlog item filed here silently promotes itself into this milestone's scope: each carries an explicit BACKLOG marker and none appears in a v1.37 phase's requirements list."
      verification: backstop
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "182-05-SUMMARY.md's gh#60 answer -> REQUIREMENTS.md SAFE-03 status -> Phase 187's citation for the reply"
    - "the D-09 backlog item's number -> the todo's resolves_phase frontmatter tag"
  prohibitions:
    - "No gsd requirements or roadmap verb may be run against these files — they reformat the whole file, and this ROADMAP is hand-authored and carries every archived milestone."
    - "No backlogged finding may be built, gated or fixed in this phase; each is recorded as a stub only."
  flagged_assumptions:
    - "SAFE-03 (unclassified, unresolved): assumption A1 may still be unmeasured when this plan runs, if Plan 06's operator probe did not happen. In that case SAFE-03's status must say so — the requirement is met by the trace being recorded with its evidence, and A1 being named as the one inferred link is part of that record rather than a gap in it."
---

<objective>
Close the record: resolve D-05 in writing, correct the two requirements the discussion left open, and
file every finding this phase measured but deliberately did not build.

Purpose: D-05 made the gate conditional on a trace this phase produced, and named the exact
consequence of each branch. The trace came back against the operator's stated expectation, so the
retirement branch does not fire and SAFE-02 and SAFE-04 must be marked confirmed required with the
trace as the reason. Six findings were measured and are owed a backlog entry rather than silence.

Output: corrected SAFE-01, SAFE-02, SAFE-04 and traceability entries in `.planning/REQUIREMENTS.md`;
new backlog stubs in `.planning/ROADMAP.md`; and a re-pointed `resolves_phase` tag on the split todo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md
@.planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md
@.planning/phases/182-jp5-destructive-operation-gate/182-01-SUMMARY.md
@.planning/phases/182-jp5-destructive-operation-gate/182-03-SUMMARY.md
@.planning/phases/182-jp5-destructive-operation-gate/182-05-SUMMARY.md
</context>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| backlog stub | Phase 999.55 — DIP24 unreachable VPP (D-15.1) | `.planning/ROADMAP.md` |
| backlog stub | Phase 999.56 — the mirrored JP4 hazard (D-15.2) | `.planning/ROADMAP.md` |
| backlog stub | Phase 999.57 — fail-closed `size_bytes` fits declared address lines | `.planning/ROADMAP.md` |
| backlog stub | Phase 999.58 — the D-09 phase: shield photographs, per-revision jumper tables, `info` rewrite | `.planning/ROADMAP.md` |
| backlog stub | Phase 999.59 — the A18-on-socket-pin-1 structural remainder | `.planning/ROADMAP.md` |
| status marker | SAFE-02 and SAFE-04 marked CONFIRMED REQUIRED with the D-05 resolution | `.planning/REQUIREMENTS.md` |
| frontmatter edit | `resolves_phase` re-pointed from 182 to the D-09 backlog number | `.planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md` |

<tasks>

<task type="auto">
  <name>Task 1: Record the D-05 resolution and correct SAFE-01, SAFE-02 and SAFE-04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md section SAFE, the SAFE-01 through SAFE-05 bullets, and the Traceability table rows for SAFE-01 through SAFE-05. Read the exact current wording before editing; SAFE-01 currently says the set "is derived from the chip database and its pin maps, not from a list written by hand", and SAFE-02 currently names "JP4 routes VPP to socket pin 1" as part of the explanation the operator is given.
    - .planning/REQUIREMENTS.md CLAIM-07's bullet — read it, confirm it needs no edit under Option B, and record that confirmation rather than editing it.
    - .planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md D-05 in full — it names the exact consequence of each branch, including that the retirement branch would have required the trace as the recorded reason. The confirmation branch owes the same standard of record.
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md section "D-05 recommendation: the gate ships" — the six-link chain, and the paragraph headed "Honest limit on this recommendation" naming A1.
    - .planning/phases/182-jp5-destructive-operation-gate/182-01-SUMMARY.md and 182-05-SUMMARY.md — the as-shipped gate and the as-recorded trace, so the requirement statuses describe what actually landed rather than what was planned.
    - The project memory note that gsd requirements and roadmap verbs reformat the whole file: prefer hand edits.
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` by hand, scoped to the SAFE bullets and the Traceability rows. Do
NOT run any `gsd` requirements verb: they reformat the whole file, and this file is hand-maintained.

SAFE-01's wording. Amend it to say that the predicate is achieved by CORRECTING the pin map so the
requirement's own words are literally true, rather than by deriving a synthetic predicate around a
database that was wrong. Record what made that necessary, in one clause: measured 2026-09-10, no pin
map in `pinouts.json` declared A19 at all, and all eight 1 MB rows sat on `DIP32_STD` which declares
19 address lines and `vpp-pin [1]`. Three synthetic alternatives — an address-line shortfall test, a
size threshold, and the 291-row VPP-on-pin-1 set — were considered and rejected. Keep the
"not from a list written by hand" half of the requirement exactly as it stands; that half was met and
is proved by the synthetic-part injection test.

SAFE-02 and SAFE-04. Mark both CONFIRMED REQUIRED with the D-05 resolution beside them, dated, and
with the trace as the recorded reason. State the resolution in a form a reader can follow without
opening anything else: D-05 made the gate conditional on this phase's SAFE-03 trace and recorded the
operator's expectation that the map fix would supersede it; the trace found the opposite, because on
Rev 2.x the firmware maps logical `CTRL_ADDRESS_LINE_18` onto the same physical bit as
`CTRL_VPP_P1_ENABLE`, and socket pin 1 is bus line 21 which sits inside the address mask — so after
the fix VPP correctly moves to socket pin 24 but A19 lands on the pin-1 line, and every byte written
above 0x7FFFF drives that pin from the 12.75 V programming rail while JP5 is bridged. The map fix
relocates the hazard; it does not remove it. Therefore the retirement branch of D-05 does not fire.

Make that record visible rather than buried. D-05 would have required the trace as the recorded
reason for the retirement; the confirmation branch owes the same standard, and the operator's stated
expectation was the opposite of the outcome. Cite `182-05-SUMMARY.md` and the trace section of
`.planning/notes/jumper-display-ground-truth.md` by name.

Also record, beside SAFE-04, the finding that `--auto` and `--chain` do not exist in the firestarter
CLI — they are GSD execution-mode flags, measured with no counterpart in `firestarter/`. SAFE-04
lists them because a GSD run in those modes invokes the CLI non-interactively, so the clause naming
them is satisfied by the non-TTY refusal rather than by any flag handling. Say it here so nobody goes
looking for CLI options to plumb.

Also record, beside SAFE-04, the Option B choice and its consequence for CLAIM-07: the gate carries
its own injectable `isatty_fn` per the `submit.py` pattern and does not revive
`cli_handlers._is_interactive`, so CLAIM-07 needs no amendment and Phase 185 removes that symbol as
planned. Confirm CLAIM-07's bullet is unedited.

Update the Traceability table rows for SAFE-01 through SAFE-05 to their real status. Mark a
requirement complete only if the work that satisfies it actually landed — check the plan SUMMARYs
rather than assuming, because executors in this project have marked multi-plan requirements complete
prematurely. SAFE-01 spans Plans 01, 02 and 03; SAFE-02 and SAFE-04 span Plan 01; SAFE-03 spans
Plans 05 and 06; SAFE-05 is Plan 04.

If Plan 06's A1 probe did not happen, SAFE-03's status must say so: the requirement is met by the
trace being recorded with its evidence, and A1 being named as the one inferred link is part of that
record rather than a gap in it. Do not mark SAFE-03 complete with A1 silently presented as settled.

Commit in the meta repo on the milestone branch.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -nE 'SAFE-0[24].{0,400}(CONFIRMED REQUIRED|confirmed required)' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — SAFE-02 and SAFE-04 are not marked confirmed required, so the D-05 resolution is not recorded where the requirement lives</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'D-05' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit — the resolution does not cite the decision it resolves, so a reader cannot see that the question was asked</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -nE 'retired|retirement' .planning/REQUIREMENTS.md | /usr/bin/grep -nE 'SAFE-02|SAFE-04' | /usr/bin/grep -v 'does not fire\|not retired\|NOT retired'; test $? -ne 0</automated>
    <fails_when>exit 1, meaning SAFE-02 or SAFE-04 is described as retired somewhere other than a statement that the retirement branch did not fire</fails_when>
    <automated>cd /workspaces && cd /workspaces && git diff HEAD -- .planning/REQUIREMENTS.md > /tmp/182-07-req.diff || { echo "git diff failed"; exit 1; }; /usr/bin/grep -c '^[+-]' /tmp/182-07-req.diff</automated>
    <fails_when>a count above 60 — a hand edit scoped to the SAFE bullets and five traceability rows should be small, and a large count means the file was reformatted rather than edited</fails_when>
    <automated>cd /workspaces && git diff HEAD -- .planning/REQUIREMENTS.md > /tmp/182-07-req.diff || { echo "git diff failed"; exit 1; }; /usr/bin/grep -E '^[+-].*CLAIM-07' /tmp/182-07-req.diff; test $? -ne 0</automated>
    <fails_when>exit 1, meaning CLAIM-07's bullet was edited — Option B was taken, so it must be left for Phase 185</fails_when>
  </verify>
  <acceptance_criteria>
    - SAFE-01's bullet states that the predicate is achieved by correcting the pin map, names the measured reason (no pin map declared A19; all eight 1 MB rows on `DIP32_STD`), and keeps the not-a-hand-written-list clause intact.
    - SAFE-02 and SAFE-04 each carry a dated CONFIRMED REQUIRED marker, the D-05 citation, and the trace as the reason, in a form readable without opening another file.
    - The record states explicitly that the outcome is the opposite of the operator's stated expectation.
    - SAFE-04 carries the finding that `--auto` and `--chain` have no firestarter CLI counterpart and are covered by the non-TTY refusal.
    - SAFE-04 carries the Option B choice and the statement that CLAIM-07 needs no amendment.
    - CLAIM-07's bullet is byte-unchanged.
    - The Traceability rows for SAFE-01 through SAFE-05 reflect what actually landed, checked against the plan SUMMARYs.
    - If A1 was not measured, SAFE-03's row or bullet says so.
    - No `gsd` requirements verb was run — the diff is confined to the SAFE section and the five traceability rows.
  </acceptance_criteria>
  <reversibility rating="reversible">D-05. A record correction; a revert restores the prior wording. The underlying trace is a measurement and does not depend on the record.</reversibility>
  <done>D-05's question is recorded as asked, answered and resolved with the trace as its reason; SAFE-01's wording is corrected; SAFE-02 and SAFE-04 are confirmed required rather than retired; and CLAIM-07 is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: File the five backlog items this phase measured but does not build</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md section Backlog — read two existing stubs in full for the shape, for example Phase 999.49 and Phase 999.51: a `### Phase 999.NN: <one-line title> (BACKLOG — filed <date> during <context>)` heading, a bold Goal line, the evidence in prose with measured figures, a "Chosen fix" or equivalent, and a "Test surface" paragraph saying what would have to be proven. Note the numbering runs to 999.54, so this task starts at 999.55.
    - .planning/ROADMAP.md section for Phase 187, and the sentence stating that Phase 187 runs last because every reply describes what shipped. A new phase inserted into this milestone would have to slot before it — which is why the D-09 work is filed as a backlog stub here rather than inserted as a phase.
    - .planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md D-15 and the Deferred Ideas section, including the wider pin-map audit's two measured classes and the silently-wrong-8-Mbit-read item.
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md Open Question 4, and the Deferred Ideas summary in its user_constraints block.
    - .planning/phases/182-jp5-destructive-operation-gate/182-02-SUMMARY.md — the sibling-parity-test audit result, in case it found more assertions whose firmware side is a literal.
    - .planning/phases/182-jp5-destructive-operation-gate/182-06-SUMMARY.md — in case the D-14 bench reading produced a surprise that needs its own stub.
  </read_first>
  <action>
Append five backlog stubs to `.planning/ROADMAP.md`'s Backlog section, numbered 999.55 through
999.59, each following the shape of the existing stubs: a heading with a BACKLOG marker and the
filing date and context, a bold Goal line, the measured evidence in prose, the chosen fix or the
reason none is chosen yet, and a Test surface paragraph.

Edit by hand, scoped to appending in the Backlog section. Do NOT run any `gsd` roadmap verb: they
reformat the whole file, and one of them is known to clobber the dependency table by positional
overwrite. This ROADMAP is a 6,300-line hand-authored document carrying every archived milestone.

999.55 — DIP24 unreachable VPP (D-15.1). `DIP24_2716` and `DIP24_2532` declare `vpp-pin [21]`. A
24-pin part bottom-aligned in the 32-pin socket puts its pin 21 at socket pin 25, which is exactly
JP4's third pole — added at Rev 2.2. So on Rev 0, Rev 2.0 and Rev 2.1 those maps' declared VPP pin is
physically unreachable and the tool offers writes it cannot perform. Now measured rather than
suspected: cite the VPP-destination table. Note the existing CAP-02 gate already refuses these parts
on pre-Rev-2.2 shields keyed on bus line 11, so state precisely what is left over rather than
re-filing a solved problem.

999.56 — the mirrored hazard (D-15.2). JP4 in the 24-pin position with a 28- or 32-pin part seated
routes VPP onto whatever that part carries at socket pin 25. Structurally the JP5/A19 hazard's mirror
image, newly reachable at Rev 2.2 and later. Record why it is not gated here: the tool can read JP4
no better than it can read JP5, so gating it needs the same D-05 evidence decision this phase made
for JP5, against a trace that has not been done for JP4's positions. Record that this was offered as
a discussion area and not selected, and that recording rather than dropping it is Claude's call made
at the operator's instruction.

999.57 — a fail-closed generator assertion. Every row's `size_bytes` must fit the address lines its
layout declares. It would have caught the 8 Mbit defect at build time. Give the two other measured
classes as evidence that it has real work to do: `AT27C011`, `D27011` and `D27C011` are 128 KB parts
on `DIP28_2764` which supplies 14 address lines, addressing only 16 KB; and 18 rows on
`DIP32_28C512_EEPROM` — including `AT28C010`, `AT28C040`, `AT28MC040`, `CAT28C040` and `WE512K8` —
carry up to 512 KB against 16 address lines. Note the corrected `DIP32_27C801` is the first 1 MB row
that would satisfy such an assertion, so the assertion can now be written without a known failure.

999.58 — the D-09 phase. Shield photographs and per-revision jumper configuration tables published to
the `firestarter_prom` wiki, plus the `firestarter info` jumper-block rewrite: per-pin-map derivation
replacing the pin-count heuristic, corrected JP4 labels, and an explicit "this jumper does not matter
for this chip" line. Record the three things this phase learned that enlarge its job: the tables must
now carry three JP4 states per revision; the Rev 2.2 silkscreen sentence about open for 32 pin ROMs
and closed for 28 pin ROMs must not be reproduced, because it is two-state language on a three-pole
jumper; and `firestarter info` still prints JP4 = Closed for the eight 8 Mbit parts after Phase 182,
because `has_vpp_pin_on_map` tests only key presence and the new layout declares `vpp-pin [24]` —
which is `jumper-display-ground-truth.md`'s confirmed defect 1. Also record that the evidence
photographs are downscaled and their roughly 15 MB originals live outside `.planning/` at
`/workspaces/tmp/`, preserved by no commit, so they need re-exporting at publication resolution
before that directory is cleared. State plainly why this is a backlog stub rather than a phase
inserted into v1.37: Phase 187 must stay last, and inserting a phase is a milestone-scope decision
for the operator rather than a planner's.

999.59 — the A18-on-socket-pin-1 structural remainder. 255 shipped rows on `DIP32_SST39SF040` place
A18 on socket pin 1, across algorithms 5, 6, 14 and 41. They are structurally in the same position
the eight 8 Mbit rows are in, and they are deliberately not gated by Phase 182, because the SAFE-03
trace established damage capability for the held-boosted-rail EPROM write path and D-06 forbids
gating on inference. Record the specific unresolved question so the item is actionable: whether a
protocol-0x05 page write, which asserts the regulator and the drop together with `CTRL_VPE_ENABLE`,
holds a boosted rail while the address drives socket pin 1 — and note that protocol 0x06, measured,
never enables the regulator at all and is therefore genuinely safe. Give the per-algorithm row counts.

Then, conditionally: if Plan 02's sibling-parity audit found further assertions whose firmware side is
a literal rather than a value read from the firmware tree, file those as one more stub. If Plan 06's
`hw_revision` reading showed the detect band moving with JP4 position — contradicting both the
schematic and the PCB — file that as its own stub with both the desk and bench evidence. Both are
conditional; if neither fired, record in `182-07-SUMMARY.md` that they did not.

Build none of these. Every one is a stub.

Commit in the meta repo on the milestone branch.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c '^### Phase 999\.5[5-9]' .planning/ROADMAP.md</automated>
    <fails_when>a count below 5 — fewer than the five required backlog stubs were filed</fails_when>
    <automated>cd /workspaces && for n in 55 56 57 58 59; do /usr/bin/grep -A6 "^### Phase 999.$n:" .planning/ROADMAP.md | /usr/bin/grep -q 'BACKLOG' || { echo "999.$n missing BACKLOG marker"; exit 1; }; done; echo "OK"</automated>
    <fails_when>a line naming a stub as missing its BACKLOG marker, or no line reading "OK" — a stub without the marker can be mistaken for active milestone scope</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -A20 '^### Phase 999.59:' .planning/ROADMAP.md | /usr/bin/grep -c '255'</automated>
    <fails_when>a count of 0 — the 255-row figure that makes 999.59 actionable is missing from the stub</fails_when>
    <automated>cd /workspaces && git diff HEAD -- .planning/ROADMAP.md > /tmp/182-07-roadmap.diff || { echo "git diff failed"; exit 1; }; /usr/bin/grep -c '^-' /tmp/182-07-roadmap.diff</automated>
    <fails_when>a count above 1 — appending five stubs should delete nothing; a deletion count above the diff header line means the file was reformatted or an existing section was overwritten</fails_when>
  </verify>
  <acceptance_criteria>
    - Five new stubs exist, numbered 999.55 through 999.59, each with a BACKLOG marker, a filing date, a bold Goal line, measured evidence and a Test surface paragraph.
    - 999.55 states what CAP-02's existing pre-Rev-2.2 refusal already covers, so it does not re-file a solved problem.
    - 999.56 records why it is not gated here and that recording rather than dropping it was Claude's call at the operator's instruction.
    - 999.57 names all three measured classes: the 8 Mbit rows it would have caught, the three 128 KB parts on `DIP28_2764`, and the 18 rows on `DIP32_28C512_EEPROM`.
    - 999.58 records the three enlargements to the D-09 job, the do-not-reproduce silkscreen sentence, the surviving JP4 = Closed output, the un-preserved photograph originals, and the reason it is a stub rather than an inserted phase.
    - 999.59 carries the 255-row figure, the per-algorithm counts, the protocol-0x05 open question and the measured protocol-0x06 exemption.
    - `git -C /workspaces diff HEAD -- .planning/ROADMAP.md` shows additions only, in the Backlog section, with no deletions.
    - No `gsd` roadmap verb was run.
    - The two conditional stubs are either filed or their non-firing is recorded in `182-07-SUMMARY.md`.
  </acceptance_criteria>
  <reversibility rating="reversible">D-15. Appended stubs; a revert removes them. Nothing is built.</reversibility>
  <done>Every finding this phase measured but deliberately did not build is filed as a stub carrying its goal, its evidence and its test surface, and nothing was built.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the split todo's resolves_phase tag</name>
  <files>.planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md</files>
  <read_first>
    - .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md in full — its frontmatter carrying `resolves_phase: 182`, and its three scope items: fix JP4's copy-paste labels, relabel the Rev-2 block, and delete the dead renderer.
    - .planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md section Folded Todos — the record that this todo was SPLIT, that item 3 is SAFE-05 and lands in Phase 182, that items 1 and 2 move to the D-09 phase, and the 2026-09-10 correction that the rewrite must not reproduce the Rev 2.2 silkscreen's two-state sentence.
    - .planning/phases/182-jp5-destructive-operation-gate/182-04-SUMMARY.md — confirmation that item 3 actually landed.
    - .planning/ROADMAP.md's new 999.58 stub as written in Task 2, for its number.
  </read_first>
  <action>
Re-point the todo's `resolves_phase` frontmatter tag from `182` to the D-09 backlog item's number as
filed in Task 2, since items 1 and 2 belong to that work and the phase does not exist yet.

Record the split inside the todo body, so the file itself explains its own history rather than
relying on a phase CONTEXT nobody will re-read: item 3 landed in Phase 182 as SAFE-05 — state the
date and cite `182-04-SUMMARY.md` — and items 1 and 2 moved to the backlogged D-09 work.

Correct item 2's premise while re-pointing it. It currently says to relabel the Rev-2 block from
"2.0 & 2.1" to "2.0, 2.1, 2.2 & 2.3" on the stated grounds that 2.2 and 2.3 have the same JP4 "plus a
3rd angled pin handled separately". That is now known to be wrong in a way that matters: Rev 2.2's
JP4 is a three-pole selector, the footprint changed at Rev 2.1 to Rev 2.2 rather than 2.2 to 2.3, and
so a single block covering 2.0 through 2.3 cannot describe all of them. Record the correction with
its date and its evidence — Rev 2.2's own pick-and-place CSV and drill file — and leave the
resolution to the D-09 work rather than deciding the new block structure here.

Correct item 1's target text as CONTEXT's 2026-09-10 note requires. The parenthetical "Only for ROMs
with VPP on P1" is still the standard to write to, but the clause beside it on the same silkscreen —
about open for 32 pin ROMs and closed for 28 pin ROMs — is two-state language on a three-pole jumper
and must not be reproduced. On Rev 2.2 and later the existing `28pin` and `32pin` labels are not
merely meaningless, they are wrong: JP4 has a third position those labels cannot express.

Update item 3's stale `file:LINE` citations if they now point at moved or deleted lines — the deleted
method is gone, so a citation to it should be rewritten as a past-tense reference to the commit that
removed it rather than left pointing at nothing. Do not repair the `.planning`-to-`.planning`
citations elsewhere in the file if they are historical by intent.

Do not move the todo out of `pending/`: items 1 and 2 are unresolved.

Commit in the meta repo on the milestone branch.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -n 'resolves_phase' .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md</automated>
    <fails_when>non-zero exit, or a printed line still reading `resolves_phase: 182` — the tag was not re-pointed and the todo still claims a closed phase resolves it</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n '999.58' .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md</automated>
    <fails_when>non-zero exit — the re-pointed tag does not name the backlog item filed in Task 2, so the todo points nowhere</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -nE '2\.0, 2\.1, 2\.2 & 2\.3' .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md | /usr/bin/grep -v 'corrected\|wrong\|2026-09'; test $? -ne 0</automated>
    <fails_when>exit 1, meaning the single-block-covers-2.0-through-2.3 instruction survives outside a dated correction, and the D-09 work would inherit a premise this phase measured false</fails_when>
    <automated>cd /workspaces && test -f .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md</automated>
    <fails_when>non-zero exit — the todo was moved out of pending, but items 1 and 2 are unresolved</fails_when>
  </verify>
  <acceptance_criteria>
    - `resolves_phase` names the 999.58 backlog item rather than 182.
    - The todo body records the split: item 3 landed in Phase 182 as SAFE-05 with its date and a citation to `182-04-SUMMARY.md`, and items 1 and 2 moved to the backlogged D-09 work.
    - Item 2's "2.0, 2.1, 2.2 & 2.3" instruction is corrected or annotated with the reason it is wrong and the evidence, and the new block structure is left for the D-09 work.
    - Item 1's target text records that "Only for ROMs with VPP on P1" is the standard and that the two-state clause beside it must not be reproduced.
    - Any `file:LINE` citation pointing at now-deleted lines is rewritten as a past-tense reference to the removing commit rather than left dangling.
    - The file is still in `.planning/todos/pending/`.
  </acceptance_criteria>
  <reversibility rating="reversible">A frontmatter and prose edit; a revert restores the prior tag.</reversibility>
  <done>The todo points at the backlogged D-09 work rather than a closed phase, explains its own split, and carries corrected premises for both surviving items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured finding -> project record -> a later phase's premise | A finding recorded wrongly, or not recorded at all, becomes a wrong premise a later phase builds on; the JP4 revision attribution is the worked example |
| this phase's scope -> the backlog | The boundary this plan polices: a measured finding must land as a stub, never as silent scope creep and never as silence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-30 | Repudiation | SAFE-02 and SAFE-04 being left ambiguous after D-05's conditional resolved | high | mitigate | Both are marked CONFIRMED REQUIRED with a dated D-05 citation and the trace as the reason, and the grep leg fails when a retirement reading survives outside a did-not-fire statement |
| T-182-31 | Elevation of Privilege | Silently reviving `_is_interactive` and blocking Phase 185's CLAIM-07 | high | mitigate | Option B is recorded beside SAFE-04, CLAIM-07's bullet is asserted byte-unchanged, and the diff leg fails if it moved |
| T-182-32 | Repudiation | A measured finding dropped rather than backlogged | medium | mitigate | Five named stubs with counted evidence, plus two conditional ones whose non-firing must itself be recorded in the SUMMARY |
| T-182-33 | Tampering | A `gsd` requirements or roadmap verb reformatting a hand-authored 6,300-line file, or clobbering the dependency table by positional overwrite | high | mitigate | Hand edits only, scoped to the target sections, with diff-size legs asserting the change is small and deletion-free |
| T-182-34 | Repudiation | A requirement marked complete before the work that satisfies it landed | medium | mitigate | Traceability rows are set from the plan SUMMARYs rather than from intent, and SAFE-03 must state an unmeasured A1 rather than presenting it as settled |
| T-182-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan |
</threat_model>

<verification>
- `cd /workspaces && /usr/bin/grep -c '^### Phase 999\.5[5-9]' .planning/ROADMAP.md` reports at least 5.
- `cd /workspaces && git diff HEAD~3 --stat -- .planning/` shows exactly `REQUIREMENTS.md`, `ROADMAP.md` and the one todo file changed by this plan.
- `cd /workspaces && git diff HEAD~3 -- .planning/ROADMAP.md > /tmp/182-07-roadmap.diff || exit 1; /usr/bin/grep -c '^-' /tmp/182-07-roadmap.diff` reports at most 1 — git's status is checked before the count, so a broken `git diff` cannot read as a clean count.
- `cd /workspaces && /usr/bin/grep -n 'resolves_phase: 182' .planning/todos/pending/fix-jp4-labels-and-rev2-revision-block.md` prints nothing.
- Note PATH `grep` in this devcontainer is ugrep and honours `.gitignore`; every leg above uses `/usr/bin/grep` deliberately.
</verification>

<success_criteria>
- D-05's question is recorded as asked, answered and resolved, with the trace as the reason and the outcome stated as the opposite of the operator's expectation.
- SAFE-01's wording is corrected; SAFE-02 and SAFE-04 are confirmed required rather than retired; CLAIM-07 is untouched under Option B.
- Every finding this phase measured but deliberately did not build is filed as a backlog stub, and nothing is built.
- The split todo points at the backlogged D-09 work and carries corrected premises for both surviving items.
- Both records were edited by hand, scoped, with no `gsd` verb run against them.
</success_criteria>

<output>
Create `.planning/phases/182-jp5-destructive-operation-gate/182-07-SUMMARY.md` when done. It must
record: the D-05 resolution in one paragraph a reader could quote; the exact SAFE-01, SAFE-02 and
SAFE-04 wording as landed; the five backlog numbers with one line each; whether either conditional
stub fired and, if not, that it did not; and the final traceability status of all five SAFE
requirements with the plan that satisfies each.
</output>
