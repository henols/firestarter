---
phase: 182-jp5-destructive-operation-gate
plan: 03
type: execute
wave: 3
depends_on: [182-02]
files_modified:
  - firestarter_app/tools/diff_db.py
  - firestarter_app/firestarter/data/chip_database.json
  - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md
  - firestarter_app/tests/golden/wire_dict_expected_deltas_182.json
  - firestarter_app/tests/test_wire_dict_equivalence.py
autonomous: true
requirements: [SAFE-01]
commits_land_in: firestarter_app submodule on gsd/v1.37-operator-safety-answered-reports-claim-hygiene — plus one meta commit in /workspaces on the same branch name advancing the firestarter_app gitlink by name
tags: [generated-artefact, diff-gate, golden-delta, regeneration, python]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The chip_database.json diff is the output of a build_db.py re-run, never a hand edit (D-04)."
    - "tools/diff_db.py carries a new root-cause rule scoped to the pinout field alone before the regeneration runs, so the regen exits 0 rather than 1 (BLOCK: unexplained diff) — the rule must precede the regen, not follow it (D-04)."
    - "The regeneration changes exactly 8 chip rows and exactly one generated field on each of them, the pinout field, proved by tools/diff_db.py exit 0 with those 8 rows attributed to the new rule."
    - "tests/golden/wire_dict_baseline.json stays byte-unchanged; the wire-level change is recorded as a new named, counted, provenance-carrying delta layer beside it (D-04, milestone D-17)."
    - "The 182 delta layer carries exactly 8 entries, its non-vacuity leg proves each key's golden record does NOT already carry the delta's bus-config, and it has its own capability-to-fail sibling test."
    - "Field-disjointness across the three delta layers holds: 149 touches page-size, 153 touches flags, 182 touches bus-config."
    - "The 182 deltas were generated programmatically from the live capture against the committed golden, never transcribed by hand, because the record-key suffix is a positional index."
    - "The eight rows' wire dicts gain bus line 21 at address-bit index 19 and lose the vpp-pin key entirely, because pin_conversions[32][24] resolves to the ROM_OE sentinel and get_bus_config drops it — so firmware defaults vpp_line to 0xFF and the write asserts CTRL_VPE_ENABLE to socket pin 24, the part's real VPP pin."
    - "test_wire_key_union_is_exactly_nine_keys is unaffected because vpp-pin is nested inside bus-config rather than being a top-level wire key — verified, not assumed."
    - "The chip_database_field_inventory golden is unchanged because no new field is emitted — verified, not assumed."
  artifacts:
    - firestarter_app/tests/golden/wire_dict_expected_deltas_182.json
    - firestarter_app/tools/diff_db.py
    - firestarter_app/firestarter/data/chip_database.json
  key_links:
    - "diff_db rule label -> _RATIONALES entry -> _RULE_FIELD_PATHS scoped to the pinout tuple -> the _classify_diff dispatch arm placed before SRAM_PINOUT -> the priority-order docstring"
    - "build_db.py re-run -> chip_database.json 8 rows -> get_bus_config -> live wire capture -> golden plus the 149, 153 and 182 delta layers"
  prohibitions:
    - "chip_database.json must not be hand-edited, and tests/golden/wire_dict_baseline.json must not be re-captured — a generated artefact's diff is the generator's output, and a golden re-captured to make a diff disappear destroys the evidence it exists to carry."
  flagged_assumptions:
    - "A4 (low risk): support_status and programming.* are expected not to move on the eight rows, because all eight are already `supported` and no rule keys support_status off the pinout for them. tools/diff_db.py is the instrument; treat any movement as an output of the rules and record it, not as a defect to suppress."
---

<objective>
Regenerate `chip_database.json` from the corrected rule, and record the wire-level change as a
reviewable delta layer rather than a re-captured golden.

Purpose: `chip_database.json` is generated. The eight 8 Mbit rows must move from `DIP32_STD` to
`DIP32_27C801` as the output of a `build_db.py` re-run, and the project's own instruments must prove
the diff is exactly those eight rows and exactly one field on each. Two of those instruments need
work first: `tools/diff_db.py` will exit 1 on an unexplained diff without a new root-cause rule, and
`tests/test_wire_dict_equivalence.py` needs a third delta layer or the golden comparison fails.

Output: the new `diff_db.py` rule; the regenerated `chip_database.json` and
`v1.3-COVERAGE-MATRIX.md`; `tests/golden/wire_dict_expected_deltas_182.json`; and the `_DELTAS_182`
layer in the equivalence test.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md
@.planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md
@.planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md
@.planning/phases/182-jp5-destructive-operation-gate/182-02-SUMMARY.md
@/workspaces/CLAUDE.md
</context>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| rule label | `RULE_PHASE182_A19_PINOUT` | `firestarter_app/tools/diff_db.py` (rationale, field-path set, dispatch arm, priority docstring line) |
| golden fixture | `wire_dict_expected_deltas_182.json` | `firestarter_app/tests/golden/wire_dict_expected_deltas_182.json` |
| path constant | `_DELTAS_182` | `firestarter_app/tests/test_wire_dict_equivalence.py` |
| test rename | `test_live_capture_matches_golden_plus_the_149_and_153_deltas` becomes `..._plus_the_149_and_153_and_182_deltas` | `firestarter_app/tests/test_wire_dict_equivalence.py` |
| test function | `test_the_182_delta_layer_is_capable_of_failing` | `firestarter_app/tests/test_wire_dict_equivalence.py` |

<tasks>

<task type="auto">
  <name>Task 1: Add the diff_db root-cause rule — BEFORE the regeneration, not after</name>
  <files>firestarter_app/tools/diff_db.py</files>
  <read_first>
    - firestarter_app/tools/diff_db.py lines 1-50 — the module docstring's exit-code contract (0 all explained, 1 unexplained diff or a missing chip, 2 infrastructure error) and the citation requirement at 42-46.
    - firestarter_app/tools/diff_db.py lines 205-232 — the `RC1_DIP32_27C020` rationale string: phase number plus a one-line label, then indented root-cause prose, with a `[VERIFIED:]` or `[CITED:]` citation. This is the closest analog, being a pinout-only, pinout-value-scoped explanation for a `resolve_pinout_key` change.
    - firestarter_app/tools/diff_db.py lines 374-390 — `_RULE_FIELD_PATHS`, and specifically the `RC1_DIP32_27C020` entry scoped to the single `("pinout",)` tuple.
    - firestarter_app/tools/diff_db.py lines 405-420 — `_PHASE84_RELABEL_PART_NUMBERS` as a `frozenset`, the analog if a part-number scope is also wanted.
    - firestarter_app/tools/diff_db.py lines 440-470 — the priority-order docstring, maintained in lockstep with the ladder, including the `6a. RC1_DIP32_27C020` and `6b. SRAM_PINOUT` lines.
    - firestarter_app/tools/diff_db.py lines 505-545 — the `_classify_diff` dispatch ladder, and specifically the `RC1_DIP32_27C020` arm with its `not algo_diff and not timing_diff and not voltage_diff and not type_diff and not vpp_diff` conjunction plus the `cu_chip.get("pinout") == "DIP32_27C020"` value scope, placed immediately before the generic `SRAM_PINOUT` arm.
    - firestarter_app/tests/test_diff_db_gate.py — the existing gate test, so the new rule does not break it.
    - .planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md section 4 — the three coordinated edits and the reason a tight field set matters (a co-occurring unexpected change escalates rather than being absorbed).
  </read_first>
  <action>
Add a `RULE_PHASE182_A19_PINOUT` rule in the three coordinated places `RC1_DIP32_27C020` occupies,
plus the priority docstring, following its shape exactly.

The rationale string, in `_RATIONALES`: phase number and a one-line label, then indented root-cause
prose stating that `resolve_pinout_key`'s 32-pin arm abandoned infoic's `variant_lo` and substituted a
size threshold, so all eight 1 MB `pm_idx=12` / `proto_id=0x08` / `variant_lo=0x03` rows landed on
`DIP32_STD` — a layout declaring 19 address lines and `vpp-pin: [1]`. State the consequence: the host
asked for VPP on socket pin 1 of a part whose pin 1 is A19. State the fix: the 32-pin arm now forks
on `variant_lo` inside the `proto_id == 0x08` test, routing `0x03` to `DIP32_27C801` (20 address
lines, A19 at pin 1, VPP at pin 24 shared with /OE). State the scope: exactly 8 rows, pinout field
only, no algorithm, timing, voltage, electrical.type or VPP delta. Carry a `[VERIFIED: ...]` citation
naming the infoic commit `a8efaedc`, the eight-row tuple, and this phase's RESEARCH document, in the
style the module header requires.

The field-path entry, in `_RULE_FIELD_PATHS`: scope the label to exactly the single-element set
containing the `("pinout",)` tuple. Keep it that tight — it is what makes a co-occurring unexpected
change escalate to UNEXPLAINED rather than be silently absorbed under this rule.

The dispatch arm, in `_classify_diff`: place it immediately BEFORE the `RC1_DIP32_27C020` arm (both
are pinout-value-scoped, and neither can shadow the other since they test different values, but
keeping the newest first matches the file's ordering habit) and therefore before the generic
`SRAM_PINOUT` arm. Use the same conjunction — `pinout_diff and not algo_diff and not timing_diff and
not voltage_diff and not type_diff and not vpp_diff` — with the value scope
`cu_chip.get("pinout") == "DIP32_27C801"`. Add the part-number scope as well, following the
`_PHASE84_RELABEL_PART_NUMBERS` pattern: a module-level `frozenset` of the eight part numbers
`AM27C080`, `AM27LV080`, `AT27C080`, `M27C801`, `MX27C8000`, `MX27C8000A`, `UPD27C8001`, tested with
`cu_chip.get("part_number") in <that frozenset>`. The double scope is deliberate here: a future
`DIP32_27C801` row that is NOT one of these eight is a new part rather than a Phase 182 relabel, and
it should escalate to UNEXPLAINED so a human looks at it. Note `M27C801` appears twice in the
database, once under SGS-THOMSON and once under ST, which is upstream's own manufacturer duplication
and is structurally normal — the part-number frozenset covers both rows with one entry.

Add a numbered line for the new rule to the priority-order docstring, beside `6a` and `6b`, stating
its position and its value-plus-part-number scope.

Write no comments in `diff_db.py`. The analog arms carry explanatory comment blocks; those are
pre-existing and are not part of the pattern to copy. The rationale string is not a comment — it is a
data field this tool prints to the operator, and it is precisely where this explanation belongs.

Commit inside the submodule. This commit must land BEFORE Task 2 runs the regeneration: without the
rule, `diff_db.py` exits 1 with BLOCK: unexplained diff on all eight rows.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_diff_db_gate.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed" — the existing gate test broke on the new rule</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python tools/diff_db.py; echo "exit=$?"</automated>
    <fails_when>a line reading anything other than "exit=0" — the pre-regeneration database must still diff clean against the pinned baseline, so a non-zero here means the new rule broke classification of an already-explained row rather than adding a new explanation</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -c 'RULE_PHASE182_A19_PINOUT' -- tools/diff_db.py</automated>
    <fails_when>a count below 4 — the label must appear in the rationale dict, the field-path dict, the dispatch arm and the priority docstring</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app grep -n 'RULE_PHASE182_A19_PINOUT' -- tools/diff_db.py` prints at least four lines, one each in `_RATIONALES`, `_RULE_FIELD_PATHS`, `_classify_diff` and the priority-order docstring.
    - The `_RULE_FIELD_PATHS` entry for the label contains exactly one tuple, `("pinout",)`.
    - The dispatch arm's line number is lower than the `RC1_DIP32_27C020` arm's, which is in turn lower than the `SRAM_PINOUT` arm's.
    - `git -C /workspaces/firestarter_app grep -nE 'VERIFIED|CITED' -- tools/diff_db.py` prints a line inside the new rationale string, naming infoic commit `a8efaedc`.
    - The new part-number `frozenset` contains exactly 7 entries — the eight rows share `M27C801` across two manufacturers.
    - `.venv311/bin/python -m ruff check tools/diff_db.py` exits 0 and `.venv311/bin/python -m ruff format --check tools/diff_db.py` exits 0.
  </acceptance_criteria>
  <done>`tools/diff_db.py` carries the new pinout-only, value-and-part-number-scoped rule in all four places, the existing gate test is still green, and the pre-regeneration diff still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate chip_database.json and the coverage matrix, and prove the diff is exactly 8 rows</name>
  <files>firestarter_app/firestarter/data/chip_database.json, firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md</files>
  <precondition>Network access to `gitlab.com` raw content is available: `build_db.py` fetches `infoic.xml` (17.9 MB, pinned to commit `a8efaedc`) at run time from `MINIPRO_XML_URL` and there is no cached copy and no offline mode. If the fetch fails the script exits 1 with the error, and this task cannot proceed.</precondition>
  <read_first>
    - firestarter_app/tools/build_db.py lines 400-420 — `MINIPRO_XML_URL`, the fetch, and the `sys.exit(1)` on failure; plus the `main()` entry point and its output path, so the regeneration is invoked exactly the way the project invokes it rather than by an ad-hoc script.
    - firestarter_app/tools/audit_coverage_matrix.py — its entry point and its output path, and the `DIP32_STD` references it emits.
    - firestarter_app/tests/test_audit_coverage_matrix.py — what it asserts against the golden matrix.
    - firestarter_app/tests/test_chip_database_field_inventory.py and firestarter_app/tests/golden/chip_database_field_inventory.json — the `meta`, `totals`, `levels`, `protocol_chip_counts` and `generator_emitted_chip_entry_keys` sections. No new field is emitted by this phase, so the golden is expected unchanged; verify rather than assume.
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md sections "What the eight rows look like today, and what changes" and "The duplicate M27C801 — not a defect".
    - .planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md section "Generated vs authored" — and its warning that the coverage-matrix regenerator MUTATES the ledger, so this is a deliberate reviewed regeneration and not a sweep.
  </read_first>
  <action>
Commit any outstanding work first, so the tree is clean before a generated artefact moves — a dirty
tree makes a generated diff impossible to review honestly.

Run `tools/build_db.py` through its own documented entry point to regenerate
`firestarter/data/chip_database.json`. Do not hand-edit a single byte of that file: D-04 and the
project's standing rule. A hand edit is silently reverted by the next regeneration and does not fix
the other parts sharing the decode path.

Then run `tools/diff_db.py` and read its output. The expected result is exit 0 with exactly 8 chips
attributed to `RULE_PHASE182_A19_PINOUT` and zero unexplained. If any other row appears, or the
attribution count is not 8, stop: that is the signal RESEARCH pitfall 2 names, and the generator rule
must be corrected in Plan 02's file rather than the diff explained away here.

Then regenerate `tests/golden/v1.3-COVERAGE-MATRIX.md` through `tools/audit_coverage_matrix.py`.
Treat this as a deliberate, reviewed regeneration: read the resulting diff and confirm it moves only
the eight 8 Mbit rows' pinout column, then say so in the commit message. The regenerator is known to
mutate the ledger, so an unreviewed run is how a wider change gets laundered in.

Verify, do not assume, that `tests/golden/chip_database_field_inventory.json` needs no change: this
phase emits no new generated field, so the inventory golden should be untouched. If the field
inventory did move, record what moved and why in `182-03-SUMMARY.md` before deciding anything.

Also record in `182-03-SUMMARY.md`, from the `diff_db.py` output: whether `support_status` or any
`programming.*` field moved on any of the eight rows. Assumption A4 predicts no movement; treat any
movement as an output of the rules rather than a defect, per CONTEXT, and record the figure either
way.

Note for the record, so it is not re-raised: `M27C801` appearing twice, under SGS-THOMSON and under
ST, is upstream's own manufacturer duplication. 66 part numbers appear under more than one
manufacturer in this database and there are no intra-manufacturer duplicates; regeneration preserves
this.

Commit the regenerated artefacts inside the submodule, with the `diff_db.py` output quoted in the
commit message, and advance the meta gitlink by name.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python tools/diff_db.py; echo "exit=$?"</automated>
    <fails_when>a line reading anything other than "exit=0", or output containing "UNEXPLAINED", "BLOCK" or a chip count other than 8 attributed to RULE_PHASE182_A19_PINOUT</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "
import json,collections
db=json.load(open('firestarter/data/chip_database.json'))
c=collections.Counter()
rows=[]
for mfr,parts in db.items():
    if not isinstance(parts,list): continue
    for p in parts:
        c[p.get('pinout')]+=1
        if p.get('pinout')=='DIP32_27C801': rows.append((mfr,p.get('part_number'),p.get('electrical',{}).get('size_bytes'),p.get('support_status')))
assert c['DIP32_27C801']==8, dict(c)
assert c['DIP32_STD']==70, ('DIP32_STD moved unexpectedly', dict(c))
assert all(r[2]==1048576 for r in rows), rows
assert all(r[3]=='supported' for r in rows), rows
print('OK', len(rows), sorted(r[1] for r in rows))"</automated>
    <fails_when>non-zero exit, an AssertionError printing the pinout histogram (DIP32_27C801 is not exactly 8, or DIP32_STD did not drop from 78 to 70), a row whose size_bytes is not 1048576, a row whose support_status is not "supported", or no line beginning "OK"</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff --stat HEAD -- tests/golden/wire_dict_baseline.json</automated>
    <fails_when>any output at all — `wire_dict_baseline.json` must be byte-unchanged by this task (milestone D-17)</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_chip_database_field_inventory.py tests/test_audit_coverage_matrix.py tests/test_diff_db_gate.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `tools/diff_db.py` exits 0, and its output attributes exactly 8 changed chips to `RULE_PHASE182_A19_PINOUT` with zero unexplained and zero missing.
    - `chip_database.json` still holds 746 rows across 59 manufacturers, and the `DIP32_27C801` count is exactly 8 with those eight being the named part numbers.
    - `git -C /workspaces/firestarter_app diff --stat HEAD~1 -- firestarter/data/chip_database.json` shows a change confined to the pinout lines of eight records — read the diff and confirm.
    - `tests/golden/wire_dict_baseline.json` is byte-unchanged.
    - `tests/golden/v1.3-COVERAGE-MATRIX.md`'s diff moves only the eight 8 Mbit rows' pinout column, and the commit message says so.
    - `tests/golden/chip_database_field_inventory.json` is byte-unchanged, or any movement is recorded in `182-03-SUMMARY.md` with its cause.
    - The `diff_db.py` output is quoted in the commit message.
  </acceptance_criteria>
  <reversibility rating="costly">D-04. Reverting means another regeneration and another reviewed coverage-matrix run; any `~/.firestarter/database.json` override a user wrote against `DIP32_27C801` is stranded. Not one-way: the database is generated and the generator is a pure function.</reversibility>
  <done>`chip_database.json` and the coverage matrix are the output of a re-run, `diff_db.py` exits 0 attributing exactly 8 rows to the new rule, and `wire_dict_baseline.json` is byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the 182 wire-dict delta layer beside the preserved golden</name>
  <files>firestarter_app/tests/golden/wire_dict_expected_deltas_182.json, firestarter_app/tests/test_wire_dict_equivalence.py</files>
  <read_first>
    - firestarter_app/tests/golden/wire_dict_expected_deltas_153.json — the second layer's file shape in full: the `deltas` object keyed `MANUFACTURER|PART_NUMBER(S)|<positional index>`, and the mandatory five-key `meta` block (`decision`, `honesty`, `how_to_update`, `phase`, `provenance`).
    - firestarter_app/tests/golden/wire_dict_expected_deltas_149.json — the first layer's `meta` block, whose `provenance` states the deltas were generated programmatically from the live capture against the committed golden and never transcribed by hand, since the record-key suffix is a positional index.
    - firestarter_app/tests/test_wire_dict_equivalence.py in full (about 470 lines). Specifically: the module docstring; `_GOLDEN`, `_DELTAS_149` and `_DELTAS_153` at lines 93-101; `_GOLDEN_PAGE_SIZE_RECORD_KEYS` and the anti-laundering assertion at 108-135 and 220-238; `_capture_wire_dicts` at 137; `_describe_record_diff` at 156; `test_live_capture_matches_golden_plus_the_149_and_153_deltas` at 212-318 with its legs (b) and (d) non-vacuity, (c) and (e) exact-count, (f) field-disjointness at 293-306, and (g) composition at 308-318 which is a SHALLOW `expected[key].update(delta_wire)`; `test_wire_key_union_is_exactly_nine_keys` at 325; and `test_the_153_delta_layer_is_capable_of_failing` at 438-460, which re-uses `_describe_record_diff` rather than a parallel implementation.
    - firestarter_app/tests/golden/wire_dict_baseline.json around lines 437 and 745 — the `AMD|AM27C080|12` and `AM27LV080` records, so the eight record keys are read from the golden rather than guessed.
    - .planning/phases/182-jp5-destructive-operation-gate/182-PATTERNS.md sections 5 and 6 — the file shape, the `meta` semantics to copy, and the leg-for-leg template.
  </read_first>
  <behavior>
    - `wire_dict_expected_deltas_182.json` carries exactly 8 delta entries, each a whole replacement `bus-config` object with 20 bus lines and no `vpp-pin` key.
    - Every 182 delta key exists in the golden.
    - No 182 delta key's golden record already carries the delta's `bus-config` value — the delta would otherwise prove nothing.
    - Composing golden plus the 149, 153 and 182 layers equals the live capture over all 746 chips.
    - Deliberately corrupting one 182 delta makes the composition test fail with a `_describe_record_diff` message naming that record — the capability-to-fail leg.
    - The three delta layers are field-disjoint: 149 touches `page-size`, 153 touches `flags`, 182 touches `bus-config`.
    - `test_wire_key_union_is_exactly_nine_keys` still passes unchanged, because `vpp-pin` is nested inside `bus-config` and is not a top-level wire key.
  </behavior>
  <action>
Write the third delta layer and extend the equivalence test leg for leg from the 153 layer.

Generate `tests/golden/wire_dict_expected_deltas_182.json` PROGRAMMATICALLY from the live capture
against the committed golden — never transcribe it by hand, because the `|<i>` record-key suffix is a
positional index within that manufacturer's list and a hand-written key will silently point at the
wrong record. Use the test module's own `_capture_wire_dicts` helper to produce the live dicts, then
emit, for each of the eight record keys whose `bus-config` differs from the golden's, the WHOLE
replacement `bus-config` object. Supplying the whole object is what removes the `vpp-pin` key,
because the composition at lines 300-305 is a shallow `update` that replaces `bus-config` wholesale
rather than merging into it.

Include the mandatory five-key `meta` block, copying `_149`'s structure and semantics: `decision`
citing milestone D-17 by name (the golden `wire_dict_baseline.json` is preserved byte-unchanged and
this file is the reviewable delta on top of it); `honesty` stating plainly what this layer does and
does not prove; `how_to_update` carrying the same warning the earlier layers carry, that a new delta
must name the chips and the reason in the commit message and that growing the list without a
provenance justification is exactly the re-baselining D-17 exists to prevent, and pointing at the
anti-laundering assertion in the test; `phase` set to `182-jp5-destructive-operation-gate`; and
`provenance` stating that these deltas were generated programmatically from the live capture against
the committed golden, never transcribed, with the positional-index reason.

Then extend `tests/test_wire_dict_equivalence.py`:

Add a `_DELTAS_182` path constant beside `_DELTAS_149` and `_DELTAS_153`, in the same shape.

Rename `test_live_capture_matches_golden_plus_the_149_and_153_deltas` to name all three layers, and
add the 182 legs mirroring the 153 ones: a non-vacuity leg computing `missing_from_golden_182` and
`already_present_182` (keying the second on `recorded[k].get("bus-config") == deltas_182[k].get("bus-config")`,
the `bus-config` analogue of the 153 layer's `flags` test) with an assertion message naming the
offending keys; an exact-count leg asserting `len(deltas_182) == 8`, not "at least"; a third loop in
the composition leg; and updated chip and delta counts in the composition failure message.

Generalise the field-disjointness leg from a pair to a triple. It currently intersects the 149 and 153
key sets and then their per-key field sets; extend it to all three pairwise combinations.
Disjointness holds — 149 touches `page-size`, 153 touches `flags`, 182 touches `bus-config` — so this
leg should pass; if it does not, stop and record why rather than weakening it.

Add `test_the_182_delta_layer_is_capable_of_failing`, mirroring the 153 sibling, and re-use
`_describe_record_diff` — the SAME helper the real comparison test calls, never a parallel
implementation.

Verify, do not assume, that `test_wire_key_union_is_exactly_nine_keys` still passes: `vpp-pin` is
nested inside `bus-config` rather than being a top-level wire key, so the union should be unchanged.

Do NOT touch `tests/golden/wire_dict_baseline.json`. Leg (a) at lines 220-238 is the anti-laundering
assertion that exists precisely to catch a re-capture, and a re-capture would erase Phase 148's
central claim.

Commit inside the submodule and advance the meta gitlink by name.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_wire_dict_equivalence.py -o addopts="" -q</automated>
    <fails_when>non-zero exit, or the summary line reports "0 passed", or the collected count did not grow by at least 1</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "
import json
d=json.load(open('tests/golden/wire_dict_expected_deltas_182.json'))
assert set(d)>={'deltas','meta'}, sorted(d)
assert sorted(d['meta'])==['decision','honesty','how_to_update','phase','provenance'], sorted(d['meta'])
assert len(d['deltas'])==8, sorted(d['deltas'])
for k,v in d['deltas'].items():
    assert set(v)=={'bus-config'}, (k, sorted(v))
    bc=v['bus-config']
    assert 'vpp-pin' not in bc, (k, bc)
    assert len(bc['bus'])==20, (k, bc['bus'])
    assert bc['bus'][19]==21, (k, bc['bus'])
print('OK', sorted(d['deltas']))"</automated>
    <fails_when>non-zero exit, an AssertionError printing the offending record key and its bus-config, a meta block whose five keys do not match exactly, a delta count other than 8, any delta carrying a key other than bus-config, any surviving vpp-pin key, any bus list not 20 long, or no line beginning "OK"</fails_when>
    <automated>cd /workspaces/firestarter_app && git diff --stat HEAD -- tests/golden/wire_dict_baseline.json</automated>
    <fails_when>any output at all — the golden must be byte-unchanged (milestone D-17)</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -n 'def test_the_182_delta_layer_is_capable_of_failing' -- tests/test_wire_dict_equivalence.py</automated>
    <fails_when>non-zero exit — the required capability-to-fail sibling was not added</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/golden/wire_dict_expected_deltas_182.json` exists, carries exactly 8 deltas and a five-key `meta` block whose `decision` names milestone D-17 and whose `provenance` states the deltas were generated programmatically rather than transcribed.
    - Every delta entry's only key is `bus-config`, and every `bus-config` carries a 20-element `bus` with bus line 21 at index 19 and no `vpp-pin` key.
    - `git -C /workspaces/firestarter_app grep -n '_DELTAS_182' -- tests/test_wire_dict_equivalence.py` prints at least three lines: the path constant, the non-vacuity or count legs, and the capability-to-fail sibling.
    - `git -C /workspaces/firestarter_app grep -n 'def test_live_capture_matches_golden_plus_the_149_and_153_and_182_deltas' -- tests/test_wire_dict_equivalence.py` prints one line — the composition test names all three layers.
    - The exact-count leg reads `== 8`, not `>=`.
    - `git -C /workspaces/firestarter_app grep -n '_describe_record_diff' -- tests/test_wire_dict_equivalence.py` shows the 182 capability-to-fail leg calling the shared helper, not a parallel implementation.
    - `tests/golden/wire_dict_baseline.json` is byte-unchanged and the anti-laundering leg still passes.
    - `test_wire_key_union_is_exactly_nine_keys` still passes with no edit to it.
    - `.venv311/bin/python -m ruff check tests/test_wire_dict_equivalence.py` exits 0 and `.venv311/bin/python -m ruff format --check tests/test_wire_dict_equivalence.py` exits 0.
  </acceptance_criteria>
  <done>The wire-level change is recorded as a named, counted, provenance-carrying third delta layer with its own non-vacuity, exact-count and capability-to-fail legs; the golden is byte-unchanged; and the full 746-chip composition matches the live capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream `infoic.xml` over the network -> `build_db.py` -> `chip_database.json` | Untrusted third-party data becomes a safety-relevant shipped artefact |
| generated artefact -> committed golden fixtures | A re-capture here launders evidence of a wrong decode into an approved baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-07 | Tampering | `chip_database.json` and `tests/golden/wire_dict_baseline.json` | high | mitigate | The database diff is a `build_db.py` output with `tools/diff_db.py` exit 0 attributing exactly 8 rows; the golden is byte-unchanged and the change is recorded as a counted delta layer with a non-vacuity leg and an anti-laundering assertion |
| T-182-14 | Tampering | The new `diff_db.py` rule absorbing a co-occurring unexpected change | high | mitigate | The rule's field-path set is exactly the `("pinout",)` tuple and the dispatch arm carries the full `not algo/timing/voltage/type/vpp` conjunction plus a value AND part-number scope, so anything wider escalates to UNEXPLAINED |
| T-182-08 | Spoofing | `MINIPRO_XML_URL` fetch over the network with no cached copy | medium | mitigate | The URL is pinned to git commit `a8efaedc`, which is content-addressed; `diff_db.py` blocks on any diff beyond the explained rows; the fetch dependency is stated as a task precondition |
| T-182-15 | Repudiation | A hand-written positional delta key silently pointing at the wrong record | medium | mitigate | The delta file is generated programmatically from the live capture via the test module's own `_capture_wire_dicts`; the non-vacuity leg asserts every key exists in the golden and does not already carry the delta value |
| T-182-16 | Tampering | The coverage-matrix regenerator mutating the ledger beyond the eight rows | medium | mitigate | Treated as a deliberate reviewed regeneration: the diff is read, confined to the eight rows' pinout column, and that confinement is stated in the commit message |
| T-182-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && .venv311/bin/python tools/diff_db.py; echo "exit=$?"` prints `exit=0` with exactly 8 chips attributed to `RULE_PHASE182_A19_PINOUT`.
- `cd /workspaces/firestarter_app && git status --short` prints nothing, then `.venv311/bin/python -m pytest tests/ -o addopts="" -q` runs the whole app suite green with the count line visible. Commit before this run: an unrelated dirty tree can redden porcelain-sensitive tests.
- `cd /workspaces/firestarter_app && .venv311/bin/python -m ruff check firestarter/ tools/ tests/ && .venv311/bin/python -m ruff format --check firestarter/ tools/ tests/` — both exit 0.
- `cd /workspaces/firestarter_app && .venv311/bin/python tools/check_mypy_watermark.py` — the watermark did not regress.
- Note the devcontainer default interpreter is Python 3.12 while `firestarter_app` CI is Python 3.11 only, and 3.12 has previously masked real CI breakage; every leg above uses `.venv311/bin/python` deliberately.

</verification>
<success_criteria>
- The `chip_database.json` diff is the output of a `build_db.py` re-run, never a hand edit (D-04).
- `tools/diff_db.py` exits 0 attributing exactly 8 changed rows to the new rule, with zero unexplained and zero missing.
- `tests/golden/wire_dict_baseline.json` is byte-unchanged; the wire change lives in a counted, provenance-carrying delta layer (milestone D-17).
- The eight rows' wire dicts carry 20 bus lines with bus line 21 at index 19 and no `vpp-pin`, so firmware defaults `vpp_line` to 0xFF and a write routes VPP to socket pin 24.
- The full app suite is green under Python 3.11.
</success_criteria>

<output>
Create `.planning/phases/182-jp5-destructive-operation-gate/182-03-SUMMARY.md` when done. It must
record: the quoted `diff_db.py` output; whether `support_status` or any `programming.*` field moved on
the eight rows (assumption A4, either way); whether `chip_database_field_inventory.json` moved; the
confirmation that the coverage-matrix regeneration was reviewed and confined to the eight rows; and
the note that the duplicate `M27C801` across SGS-THOMSON and ST is upstream's own manufacturer
duplication and not a defect.
</output>
