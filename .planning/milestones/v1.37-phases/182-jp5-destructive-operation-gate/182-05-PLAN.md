---
phase: 182-jp5-destructive-operation-gate
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/jumper-display-ground-truth.md
  - .planning/v1.7-SHIELD-REVS.md
autonomous: true
requirements: [SAFE-03]
commits_land_in: meta repo /workspaces on gsd/v1.37-operator-safety-answered-reports-claim-hygiene — this plan touches only .planning/ prose and no submodule, so no gitlink advance is owed
tags: [schematic-trace, hardware-record, vpp-routing, record-correction]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SAFE-03's answer is settled from the shield schematic, PCB and gerbers rather than inferred, and it is recorded with per-claim evidence citations (SAFE-03)."
    - "The trace takes the form of a VPP-destination table: revision family by JP4 state, giving which socket pin actually carries VPP (D-11)."
    - "The table lives in .planning/notes/jumper-display-ground-truth.md, whose JP4 row is corrected in place — one home for jumper ground truth (D-12)."
    - "JP4's own record is corrected: pin 1 is JP4's COMMON pole, not a destination, so the standing claim that JP4 routes VPP to socket pin 1 only is wrong; JP4's two selectable poles EXPORT whatever socket pin 1 carries to socket pin 3 (a 28-pin part's pin 1) or socket pin 25 (a 24-pin part's pin 21) (D-10, D-11)."
    - "JP5 is the VPP-to-socket-pin-1 strap: its A side is Q8's collector whose emitter is the VPE rail, its B side is socket pin 1, and its KiCad footprint is a Bridged SolderJumper — bridged by default (D-11)."
    - "The JP4 footprint change is at Rev 2.1 to Rev 2.2, not Rev 2.2 to Rev 2.3 as the project record claims — settled by Rev 2.2's own pick-and-place CSV and its own gerber drill file, both of which sat in the same directory as the gerbers the record cites (D-10, D-13)."
    - "gh#60's open question is answered in the record: writing and erasing energize socket pin 1; reading, verifying, blank-checking and id do not — id puts 12 V on socket pin 26 (A9) instead — cited to firmware file and line, not inferred (SAFE-03)."
    - "The record states the mechanism that makes the map fix insufficient: on Rev 2.x, logical CTRL_ADDRESS_LINE_18 and CTRL_VPP_P1_ENABLE are the same physical bit 0x08, socket pin 1 is bus line 21, and bus line 21 is inside the address mask the firmware drives from the address on every byte."
    - "Assumption A1 is recorded by name as the one unmeasured link in the chain, with the exact probe (VPE at J6 pin 4 with the regulator off) and the consequence if it comes back boosted (read, verify and blank join the damage-capable set)."
    - "jumper-display-ground-truth.md's standing defect 4 (24-pin VPP maps get no JP3/JP4 guidance, unconfirmed) is resolved as a by-product of the table: DIP24_2716 and DIP24_2532 declare vpp-pin [21], which sits at socket pin 25 and is reachable only through JP4's third pole on Rev 2.2 and later (D-11, D-15)."
    - "The R41-couples-to-JP4 claim in v1.7-SHIELD-REVS.md is retracted outright as measured false: R41 sits between Arduino A3 and GND in both schematic and PCB and touches no JP4 pin, so the hw_revision detect band is independent of JP4 position (D-14)."
    - "D-14 is characterize-only: no firmware change lands in this phase, and if the detect band did move with JP4 position that is recorded as a defect rather than fixed here (D-14)."
    - "Section 6's capability column is corrected where the trace settles it: the 24-pin legacy UV-EPROM row is revision-qualified — readable on Rev 0, 2.0 and 2.1, programmable on Rev 2.2 and later — and the capability-set-unchanged note is corrected; other families' rows are left alone, consistent with rejecting the full matrix (D-13)."
    - "One note names the systemic cause: Rev 2.2 has now been inferred from Rev 2.1's shared schematic blob three times with three wrong answers (R41 4k7 versus 10k, JP4's footprint, and the revision at which the footprint changed) while Rev 2.2's own artefacts sat beside it (D-13)."
    - "Cells the desk trace cannot settle are marked PROBE-PENDING with the exact probe named, not silently asserted: Rev 2.0 and 2.1's single JP4 pole destination (A2), the physical orientation of Rev 2.2's three pads as the operator sees them, and Rev 0 and Rev 1's JP3 routing (D-12)."
    - statement: "The corrected records do not contradict each other: jumper-display-ground-truth.md's JP4 row and v1.7-SHIELD-REVS.md's JP4 claims tell the same story about which revision changed the footprint and what JP4's poles reach."
      verification: backstop
  artifacts:
    - .planning/notes/jumper-display-ground-truth.md
    - .planning/v1.7-SHIELD-REVS.md
  key_links:
    - "kicad_sch JP4 symbol at (177.8, 69.85) with its three pole coordinates -> net members at those points -> socket pins 1, 3 and 25"
    - "JP5 A side = Q8 collector; Q8 emitter = VPE rail; Q8 gated by P1_VPP_ENABLE through R25 and Q5 -> socket pin 1"
    - "Rev2.2 top-pos.csv PinHeader_2x02 and Rev2.2 PTH.drl three drills -> the footprint change is at 2.1 to 2.2"
    - "rurp_pinout.h CTRL_ADDRESS_LINE_18_REV2 aliased to CTRL_VPP_P1_ENABLE_REV2 = 0x08 -> rurp_hw_rev_utils.h revision mapping -> both land on physical bit 0x08"
  prohibitions:
    - "No operator-facing text this project writes may reproduce JP4's Rev 2.2 silkscreen sentence about open for 32 pin ROMs and closed for 28 pin ROMs — it is two-state language on a three-pole jumper and is wrong on that board."
    - "No cell in the VPP-destination table may be asserted from an inference where a probe is what would settle it; an unsettled cell is marked PROBE-PENDING with its probe named."
  flagged_assumptions:
    - "A1 (SAFE-03, unresolved): the VPE rail level with CTRL_VPP_REGULATOR_ENABLE clear is inferred from the boost topology, not measured, and no committed artefact states it. It is what makes reading safe rather than merely bit-asserting. Probe: measure VPE at J6 pin 4 with the regulator off. Plan 06 carries it."
    - "A2 (medium risk): Rev 2.0 and Rev 2.1's single JP4 pole is inferred to reach socket pin 3, from the two-pad footprint, the silkscreen and Rev 0's JP3 semantics. No Rev 2.0 or 2.1 schematic or PCB is committed. Mildly discordant evidence: the Rev 2.1 pad pair is vertical, matching Rev 2.2's common-plus-24-pin geometry rather than its common-plus-28-pin geometry. Probe named; cell marked PROBE-PENDING."
    - "A3 (low risk): the bottom-aligned socket offsets — a 28-pin part's pin 1 at socket pin 3 and a 24-pin part's pin 21 at socket pin 25 — are inferred from the standard DIP-in-wider-socket convention plus the GND and VCC anchors. Independently corroborated by the host's own pin_conversions table sharing bus line 15 between [28][1] and [32][3] and bus line 11 between [24][21] and [32][25]."
    - "A6 (low risk): the Rev 2.2 drill file's undrilled fourth grid position means the fourth pad is genuinely unpopulated rather than an NPTH or a same-net tie. Corroborated by the PCB pad extraction showing only three pads with nets."
    - "SAFE-05 (unclassified, unresolved): ic_layout.py's jumper derivation is unguarded by any test; this plan records the ground truth the derivation should be checked against but changes no code. Confirmed defect 1 (pin-count key rather than pin-map identity) stays open for the D-09 phase."
---

<objective>
Write the SAFE-03 trace: which socket pin actually carries VPP, per revision family and per JP4
state, and which operations energize socket pin 1 — settled from artefacts, cited, and recorded where
the next reader will already be looking.

Purpose: gh#60's reporter asked whether the hazard is "just writing, or reading too?" That question
is answerable only from the shield schematic plus the firmware VPP path, and the answer is what the
gate's scope rests on. Two project records are wrong in ways the trace settles, and one of them —
the R41-couples-to-JP4 claim — is the premise of the whole `hw_revision` detect model.

Output: a VPP-destination table and the operations answer in
`.planning/notes/jumper-display-ground-truth.md`; corrected JP4 claims, a retracted R41 claim and one
systemic-cause note in `.planning/v1.7-SHIELD-REVS.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md
@.planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md
@.planning/notes/jumper-display-ground-truth.md
@/workspaces/CLAUDE.md
</context>

## Artifacts this phase produces

| Kind | Name | File |
|---|---|---|
| new section | "VPP destination per revision and JP4 state (Phase 182 trace)" | `.planning/notes/jumper-display-ground-truth.md` |
| new section | "Which operations energize socket pin 1 — the answer to gh#60" | `.planning/notes/jumper-display-ground-truth.md` |
| new section | "Why Rev 2.2 keeps being got wrong" (the D-13 systemic-cause note) | `.planning/v1.7-SHIELD-REVS.md` |
| corrected row | the JP4 row in "What each jumper actually routes" | `.planning/notes/jumper-display-ground-truth.md` |
| resolved item | confirmed defect 4 (24-pin VPP maps get no JP3/JP4 guidance) | `.planning/notes/jumper-display-ground-truth.md` |
| retraction | the "JP4 Caveat" R41-couples-to-JP4 claim | `.planning/v1.7-SHIELD-REVS.md` |

<tasks>

<task type="auto">
  <name>Task 1: The VPP-destination table and the gh#60 answer, in the jumper ground-truth note</name>
  <files>.planning/notes/jumper-display-ground-truth.md</files>
  <read_first>
    - .planning/notes/jumper-display-ground-truth.md in full (about 70 lines). Note its frontmatter, its "Ground-truth sources" table, its "What each jumper actually routes" table whose JP4 row currently claims "VPP to socket pin 1 only" and attributes the footprint change to Rev 2.3, its "Confirmed defects" list items 1 through 5 including item 4 marked unconfirmed, and its "Chip-population impact" table.
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md section "The VPP-destination table (D-11)" in full — how the trace was done, the settled topology table of net members at JP4's three pole coordinates and JP5's two, the PCB pad-to-net extraction, the Rev 2.1 versus Rev 2.2 drill-file counts, the table itself with its PROBE-PENDING cells, the socket-position offset table, and the section "What the trace cannot settle (and the exact probe for each)".
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md section "Which operations energize socket pin 1 (SAFE-03, success criterion 5)" in full — the control-register bit table, the Rev 2 alias at rurp_pinout.h:144, the revision mapping at rurp_hw_rev_utils.h:18-26, the three assert sites with their eprom.cpp line numbers, the sites that do NOT reach socket pin 1, the read-still-enables-the-regulator-but-at-address-zero finding, the write-holds-the-boosted-rail finding, the plainly-stated answer, and the named inferred link A1.
    - .planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md sections Canonical References (the three evidence photographs and their verbatim silkscreen transcriptions) and Specific Ideas (the standard that operator-facing jumper text be checkable against JP4's own "Only for ROMs with VPP on P1" sentence, and the warning that the sentence beside it is wrong).
    - .planning/phases/182-jp5-destructive-operation-gate/evidence/ — confirm the three photograph files are present and name them by path; do not re-derive their silkscreen text, CONTEXT already transcribes it verbatim.
  </read_first>
  <action>
Extend `.planning/notes/jumper-display-ground-truth.md`. This is the one home for jumper ground truth
per D-12, it is where the D-09 phase will already be looking, and Phase 187 will cite it for gh#60.

Correct the JP4 row of "What each jumper actually routes" in place. Two things in it are wrong.
First, "VPP to socket pin 1 only" is wrong: socket pin 1 is JP4's COMMON pole, not a destination, so
JP4 is not a VPP source at all — its two selectable poles EXPORT whatever socket pin 1 carries, to
socket pin 3 (a 28-pin part's pin 1) or socket pin 25 (a 24-pin part's pin 21). Second, "footprint
changes to 3-pole 2x2 selector at Rev 2.3" is wrong: the change is at Rev 2.1 to Rev 2.2. Cite the
decisive artefacts by path — Rev 2.2's own `W27C512Programmer-top-pos.csv` naming
`PinHeader_2x02_P2.54mm_Vertical` against Rev 2.1's `PinHeader_1x02_P2.54mm_Vertical`, and the drill
counts from each revision's own gerber bundle (three component drills for Rev 2.2, two for Rev 2.1).
Add a JP5 row or extend the existing one with the measured topology: A side is Q8's collector, Q8's
emitter is the VPE rail, Q8 is gated by `P1_VPP_ENABLE` through R25 and Q5, B side is socket pin 1,
and the KiCad footprint is a Bridged SolderJumper — bridged by default.

Add a section "VPP destination per revision and JP4 state (Phase 182 trace)". Shape it per CONTEXT's
discretion note so it is readable by the D-09 phase and citable by Phase 187. Use RESEARCH's table as
the source. Columns: revision family, JP4 physical state, which socket pin VPE can reach through Q8
and `P1_VPP_ENABLE`, which socket pins are reachable through the other switches (socket 24 through Q6
and `VPE_ENABLE`, socket 26 through Q7 and `A9_VPP_ENABLE`), and a settled-or-probe-pending column.
Give Rev 0 and Rev 1 their own row carrying a "JP4 and JP5 do not exist on these boards" sentinel and
naming JP3 as Rev 0's equivalent strap, rather than folding them into a Rev 2 row. Mark the three
cells the desk trace cannot settle as PROBE-PENDING with the exact probe named in each: Rev 2.0 and
2.1's single pole destination; the physical orientation of Rev 2.2's three pads as the operator sees
them against the silkscreen legend; and Rev 0 and Rev 1's JP3 routing. Do not assert an inference
where a probe is what would settle it.

Include the socket-position offsets the table depends on, as a small sub-table: a 32-pin part's pin 1
at socket pin 1, a 28-pin part's pin 1 at socket pin 3, a 24-pin part's pin 21 at socket pin 25. Mark
them as inferred from the bottom-aligned convention plus the GND and VCC anchors, and record the
independent corroboration: the host's own `pin_conversions` table already shares bus line 15 between
the 28-pin pin 1 and the 32-pin pin 3, and bus line 11 between the 24-pin pin 21 and the 32-pin
pin 25.

Add a section "Which operations energize socket pin 1 — the answer to gh#60". State the answer
plainly first: writing and erasing; not reading, verifying, blank-checking or `id`. Then the
mechanism, cited to firmware file and line rather than inferred — the three assert sites in
`eprom.cpp`, the Intel-flash sites where socket pin 1 genuinely IS VPP, and the sites that do not
reach pin 1 including `id`'s route to socket pin 26 through Q7. Then the crux that makes the pin-map
fix insufficient on its own: on Rev 2.x, logical `CTRL_ADDRESS_LINE_18` and `CTRL_VPP_P1_ENABLE` are
the same physical bit 0x08, socket pin 1 is bus line 21, bus line 21 is inside the address mask the
firmware rewrites from the address on every byte, and a write holds the boosted route asserted across
every block. So after the fix VPP correctly moves to socket pin 24 but A19 lands on the pin-1 line,
and the operation is safe only if JP5 is cut — which is exactly, and unconditionally, what the
silkscreen says.

Record assumption A1 by name in that section, as the one unmeasured link: the VPE rail level with
`CTRL_VPP_REGULATOR_ENABLE` clear is inferred from the boost topology and no committed artefact
states it. Name the probe (measure `VPE` at `J6` pin 4 with the regulator off) and the consequence if
it comes back boosted (`read`, `verify` and `blank` join the damage-capable set and the gate's scope
widens). Point at Plan 06.

Resolve confirmed defect 4 in place. It currently reads that 24-pin VPP maps get no JP3 or JP4
guidance and is marked unconfirmed. The table settles it: `DIP24_2716` and `DIP24_2532` declare
`vpp-pin [21]`, a 24-pin part bottom-aligned in the 32-pin socket puts its pin 21 at socket pin 25,
and socket pin 25 is exactly JP4's third pole — so on Rev 0, 2.0 and 2.1 those maps' declared VPP pin
is unreachable, and on Rev 2.2 and later it is reachable only with JP4 in the 24-pin position. Mark
it resolved with the date and the evidence, in the file's existing convention.

Record the structural remainder the gate does not cover, so it is not later read as an omission: 255
shipped rows on `DIP32_SST39SF040` place A18 on socket pin 1 (algorithms 5, 6, 14 and 41), and they
are deliberately not gated because the trace established damage capability for the held-boosted-rail
EPROM write path only and D-06 forbids gating on inference. Plan 07 files the backlog item.

Name the three evidence photographs by path, and record CONTEXT's warning verbatim in a form the
D-09 phase cannot miss: JP4's sentence "Only for ROMs with VPP on P1" is the standard for
operator-facing jumper text, but the sentence beside it on the same board is two-state language on a
three-pole jumper and must never be reproduced.

Also record the note CONTEXT makes about the photographs: they are downscaled from roughly 15 MB
originals which live outside `.planning/` at `/workspaces/tmp/` and are preserved by no commit, so
the D-09 phase should re-export them at publication resolution before that directory is cleared.

Commit in the meta repo on the milestone branch. No submodule is touched, so no gitlink advance is
owed.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -c 'PROBE-PENDING' .planning/notes/jumper-display-ground-truth.md</automated>
    <fails_when>a count below 3 — the three cells the desk trace cannot settle (Rev 2.0/2.1 pole destination, Rev 2.2 pad orientation, Rev 0/1 JP3 routing) must each be marked rather than asserted</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'socket pin 25' .planning/notes/jumper-display-ground-truth.md</automated>
    <fails_when>non-zero exit — JP4's third pole destination, which is what resolves confirmed defect 4, is not recorded</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -nE '0x08|CTRL_VPP_P1_ENABLE|CTRL_ADDRESS_LINE_18' .planning/notes/jumper-display-ground-truth.md</automated>
    <fails_when>non-zero exit — the Rev 2 control-bit alias, which is the crux that makes the map fix insufficient on its own, is not recorded</fails_when>
    <automated>/usr/bin/grep -n 'J6' .planning/notes/jumper-display-ground-truth.md</automated>
    <fails_when>non-zero exit — assumption A1's probe location is not named, so the premise the gate's scope rests on is recorded without the measurement that would settle it</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'VPP to socket pin 1 only' .planning/notes/jumper-display-ground-truth.md; test $? -ne 0</automated>
    <fails_when>exit 1, meaning the wrong JP4 claim survives verbatim in the file</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'Open for 32 pin ROMs' .planning/notes/jumper-display-ground-truth.md | /usr/bin/grep -v 'never\|not be reproduced\|must not\|wrong'; test $? -ne 0</automated>
    <fails_when>exit 1, meaning the Rev 2.2 silkscreen sentence appears somewhere other than inside its own do-not-reproduce warning</fails_when>
  </verify>
  <acceptance_criteria>
    - A section titled for the VPP destination per revision and JP4 state exists, with a row for Rev 0 and Rev 1 carrying a "JP4 and JP5 do not exist" sentinel and naming JP3, a row for Rev 2.0 and 2.1, and rows for Rev 2.2 and 2.3 covering all three JP4 states.
    - Every table cell is either settled with an artefact citation naming the file it came from, or marked PROBE-PENDING with its probe stated; no cell is asserted from inference alone.
    - The JP4 row in "What each jumper actually routes" no longer says the footprint changed at Rev 2.3, and names the Rev 2.2 pick-and-place CSV and the two revisions' drill counts.
    - A section answering gh#60 states plainly that writing and erasing energize socket pin 1 and that reading, verifying, blank-checking and `id` do not, and cites `eprom.cpp` assert sites and `rurp_pinout.h` by line.
    - The `id` path's destination is recorded as socket pin 26 through Q7, not socket pin 1.
    - Assumption A1 is recorded by name with the `J6` pin 4 probe and the widened-scope consequence, and points at Plan 06.
    - Confirmed defect 4 is marked resolved with a date and cites `pinouts.json`'s `vpp-pin [21]` on `DIP24_2716` and `DIP24_2532`.
    - The 255-row `DIP32_SST39SF040` A18 structural remainder is recorded as deliberately not gated, with the reason.
    - The three evidence photographs are named by path, and the do-not-reproduce warning about the Rev 2.2 silkscreen sentence appears explicitly.
    - `git -C /workspaces log --oneline -1 -- .planning/notes/jumper-display-ground-truth.md` shows a commit on the v1.37 milestone branch.
  </acceptance_criteria>
  <reversibility rating="reversible">D-11 and D-12. A prose record correction; a revert restores the prior text. The underlying findings are measurements and do not depend on the record.</reversibility>
  <done>The VPP-destination table and the gh#60 operations answer are recorded in the one home for jumper ground truth, every cell is either cited or marked PROBE-PENDING, JP4's row is corrected, defect 4 is resolved, and assumption A1 is named with its probe.</done>
</task>

<task type="auto">
  <name>Task 2: Correct every JP4 claim the trace settles in v1.7-SHIELD-REVS.md, and retract the R41 claim</name>
  <files>.planning/v1.7-SHIELD-REVS.md</files>
  <read_first>
    - .planning/v1.7-SHIELD-REVS.md — read the regions RESEARCH's inventory row 16 names, by content rather than by line number alone, since this is a 57 KB file whose lines move. Specifically: section 1's per-revision provenance rows whose notes attribute "JP4 footprint 1x2 to 2x2 vs Rev2.2" to the Rev 2.3 commit; section 3's per-rev R41 table whose Rev 2.0, 2.1 and 2.2 rows all read "JP4 (P1_VPP_JMP) to R41 to A3 to GND" and whose Rev 2.3 row says the footprint changed from Rev 2.2; section 4's electrical-delta rows for "Rev 2.1 to Rev 2.2" and "Rev 2.2 to Rev 2.3"; section 5's mechanical-delta rows for the same two transitions; section 6's capability rows and the "Capability set unchanged from Rev 2.1/2.2" note on the Rev 2.3 row; section 7's JP4 designator row whose per-rev column reads 1x2, 1x2, 1x2, 2x2; section 8's ASCII topology showing "GND (via JP4 / P1_VPP_JMP)" and the "JP4 Caveat" prose; and section 9's per-rev ADC band table rows that repeat "R41 to GND via JP4".
    - .planning/phases/182-jp5-destructive-operation-gate/182-RESEARCH.md sections "Settled topology" (the Rev 2.1 versus Rev 2.2 drill and pos-CSV evidence, and the finding that the committed kicad_pcb IS the Rev 2.2/2.3 layout), "D-14: the R41 / JP4 coupling claim is FALSE" (R41 pin 1 on the A3 net, pin 2 on GND, roughly 29 mm from JP4 in the PCB, touching no JP4 pin in either artefact), and the "Do not infer Rev 2.2 from Rev 2.1's shared schematic blob" paragraph naming the three wrong answers.
    - .planning/v1.7/upstream-rurp/hardware/Rev2.2/W27C512Programmer-top-pos.csv and .../Rev2.1/W27C512Programmer-top-pos.csv — the decisive JP4 footprint rows, read directly so the correction cites what was read.
    - .planning/phases/182-jp5-destructive-operation-gate/182-CONTEXT.md D-13 and D-14 — the exact scope of what is corrected and what is left alone, and the instruction to use the file's existing inline dated evidence-cited convention (the style in which the R41 error was recorded).
  </read_first>
  <action>
Correct only the JP4 claims the trace settles, using the file's own existing inline dated
evidence-cited convention — the style in which the R41 4k7-versus-10k error was recorded, with the
date, the artefact and what it says. Do not restructure the file and do not regenerate it.

The footprint-revision correction. Everywhere the file attributes the JP4 footprint change to the
Rev 2.2-to-Rev 2.3 transition, correct it to Rev 2.1-to-Rev 2.2 and cite the decisive artefacts:
Rev 2.2's own pick-and-place CSV naming `PinHeader_2x02_P2.54mm_Vertical` where Rev 2.1's names
`PinHeader_1x02_P2.54mm_Vertical`, and each revision's own gerber drill file showing three component
drills for Rev 2.2 against two for Rev 2.1. Note the corollary the trace also settled: the committed
`kicad_pcb` places JP4 at the identical coordinate Rev 2.2's own drill file does, so the committed
PCB IS the Rev 2.2 and 2.3 layout. The affected places are section 1's provenance notes, section 3's
R41 table, section 4's and section 5's transition rows, and section 7's JP4 designator row whose
per-rev column currently reads 1x2, 1x2, 1x2, 2x2 and should read 1x2, 1x2, 2x2, 2x2.

The R41 retraction. The "JP4 Caveat" prose says R41's lower terminal connects to one pin of JP4 such
that the divider works only when JP4 is in a specific config, and it is self-declared untraced
("consult upstream Anders schematic for the exact wiring"). Retract it outright as measured false:
R41's pin 1 is on the net carrying Arduino A3 and its pin 2 is on GND, in both the schematic and the
PCB, and in the PCB it sits roughly 29 mm from JP4 in a different board region. State the
consequence explicitly, because it is the one that mattered: the `hw_revision` detect band is
INDEPENDENT of JP4 position. Then correct the same "via JP4" phrasing wherever it repeats — section
8's ASCII topology line, its surrounding prose, and section 9's per-rev ADC band rows.

Record that D-14 is characterize-only. No firmware change lands in this phase; the milestone touches
firmware at the edges only. Note that the bench half becomes a falsification attempt with a predicted
answer — reading `hw_revision` at each JP4 position should show no change — and point at Plan 06 for
the reading. If the reading did move the band, that is recorded as a defect, not fixed here.

Section 6's capability column. Correct only what the trace settles: revision-qualify the 24-pin
legacy UV-EPROM row as readable on Rev 0, 2.0 and 2.1 but programmable on Rev 2.2 and later, since
only Rev 2.2's third JP4 pole can deliver VPE to socket pin 25 where a 24-pin part's pin 21 sits;
and correct the Rev 2.3 row's "Capability set unchanged from Rev 2.1/2.2" note, which conflates can-read
with can-program. Leave every other family's row alone — that is the full per-pin-map reachability
matrix D-11 rejected, and it is the deferred wider audit under another name.

Add one new short section naming the systemic cause, per D-13: Rev 2.2 has now been inferred from
Rev 2.1's shared schematic blob three times, producing three wrong answers — R41 4k7 versus 10k,
JP4's footprint, and the revision at which the footprint changed — while in every case Rev 2.2's own
artefacts (`Rev2.2/W27C512Programmer-top-pos.csv` and `Rev2.2-gerbers.zip`) sat in the same
directory. State the rule that follows: read Rev 2.2's own artefacts first; Rev 2.2 has no committed
schematic and the shared blob is not evidence about it.

Do not correct any `.planning`-to-`.planning` citation that is historical by intent, and do not
repair line-number citations that this edit itself moves without also re-checking them: after the
edits, re-read any `file:LINE` citation inside the regions you touched and repair it against the new
line numbers, then round-trip one of them to confirm the target still says what the citation claims.

Commit in the meta repo on the milestone branch.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep -n "R41's lower terminal connects to one pin of JP4" .planning/v1.7-SHIELD-REVS.md; test $? -ne 0</automated>
    <fails_when>exit 1, meaning the false R41-couples-to-JP4 claim survives verbatim</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -c 'PinHeader_2x02' .planning/v1.7-SHIELD-REVS.md</automated>
    <fails_when>a count of 0, or a count that did not increase from the pre-task value — the Rev 2.2 pick-and-place evidence was not cited into the corrections</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -nE 'footprint (changed|change).{0,80}Rev ?2\.3' .planning/v1.7-SHIELD-REVS.md | /usr/bin/grep -v 'corrected\|WRONG\|retract\|2026-09'; test $? -ne 0</automated>
    <fails_when>exit 1, meaning a footprint-change-at-Rev-2.3 claim survives outside a dated correction annotation</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -n 'independent of JP4 position' .planning/v1.7-SHIELD-REVS.md</automated>
    <fails_when>non-zero exit — the consequence of the R41 retraction, which is the one that mattered for the detect model, is not stated</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -niE 'systemic|three wrong answers' .planning/v1.7-SHIELD-REVS.md</automated>
    <fails_when>non-zero exit — the D-13 systemic-cause note is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - No surviving claim attributes the JP4 footprint change to the Rev 2.2-to-Rev 2.3 transition, except inside a dated correction annotation that says it was wrong.
    - Section 7's JP4 designator row's per-rev column reads 1x2, 1x2, 2x2, 2x2 across Rev 2.0, 2.1, 2.2 and 2.3.
    - The "JP4 Caveat" section states the R41 claim is retracted as measured false, gives R41's actual endpoints (Arduino A3 and GND), cites both the schematic and the PCB, and states that the `hw_revision` detect band is independent of JP4 position.
    - Every "via JP4" phrasing in section 8's ASCII topology and section 9's ADC band rows is corrected or annotated.
    - Section 6's 24-pin legacy UV-EPROM row is revision-qualified (readable on Rev 0, 2.0 and 2.1; programmable on Rev 2.2 and later), and the Rev 2.3 capability-set-unchanged note is corrected.
    - No other family's capability row was touched — confirm with `git -C /workspaces diff HEAD -- .planning/v1.7-SHIELD-REVS.md` and read the section 6 hunks.
    - A new short section names the systemic cause and lists all three wrong answers by name.
    - Every correction carries a date and names the artefact it was settled from, in the file's existing convention.
    - D-14 is stated as characterize-only, with the predicted bench answer (no change with JP4 position) and a pointer to Plan 06.
    - Any `file:LINE` citation inside a touched region was re-checked against the new line numbers, and at least one was round-tripped to confirm the target still says what the citation claims.
  </acceptance_criteria>
  <reversibility rating="reversible">D-13 and D-14. A prose record correction; a revert restores the prior text. No firmware or host change is made, so nothing downstream depends on it.</reversibility>
  <done>Every JP4 claim the trace settles is corrected in the file's own dated evidence-cited convention, the R41-couples-to-JP4 claim is retracted outright with its consequence for the detect model stated, section 6's 24-pin row is revision-qualified without touching other families, and one note names the systemic cause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project record -> future planner or operator decision | A false record here becomes a wrong belief that a later phase acts on; the R41 claim was the premise of the whole `hw_revision` detect model |
| inferred claim -> asserted claim | The boundary this plan polices: a cell settled by artefact versus a cell settled by inference must not be presented the same way |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-182-20 | Repudiation | An inferred VPP-destination cell presented as settled | high | mitigate | Every cell is either cited to a named artefact or marked PROBE-PENDING with its probe stated; three cells are marked, and the grep leg counts them |
| T-182-21 | Repudiation | The false R41-couples-to-JP4 claim continuing to underwrite the `hw_revision` detect model | high | mitigate | Retract it outright with both artefacts cited, and state the consequence (the detect band is independent of JP4 position) so a later reader cannot re-derive the wrong conclusion |
| T-182-22 | Spoofing (of hardware capability) | Reproducing the Rev 2.2 silkscreen's two-state JP4 sentence in operator-facing text | medium | mitigate | Record it explicitly as must-not-reproduce, and add a grep leg asserting it appears only inside its own warning |
| T-182-23 | Repudiation | Inferring Rev 2.2 from Rev 2.1's shared schematic blob a fourth time | medium | mitigate | One named systemic-cause note listing all three prior wrong answers and stating the rule that Rev 2.2's own artefacts are read first |
| T-182-24 | Tampering | An edit to a 57 KB record staling its own `file:LINE` citations | medium | mitigate | Re-check every `file:LINE` citation inside a touched region against the new line numbers, and round-trip at least one |
| T-182-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan |
</threat_model>

<verification>
- `cd /workspaces && /usr/bin/grep -c 'PROBE-PENDING' .planning/notes/jumper-display-ground-truth.md` reports at least 3.
- `cd /workspaces && /usr/bin/grep -n "R41's lower terminal connects to one pin of JP4" .planning/v1.7-SHIELD-REVS.md` prints nothing.
- `cd /workspaces && git diff --stat HEAD~2 -- .planning/notes/jumper-display-ground-truth.md .planning/v1.7-SHIELD-REVS.md` shows both files changed and no other `.planning/` file touched by this plan.
- Note PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, which silently under-scans; every leg above uses `/usr/bin/grep` deliberately.
</verification>

<success_criteria>
- SAFE-03 is met: which operations the gate covers is settled from the shield schematic, PCB and gerbers plus the firmware VPP path, not inferred, and recorded with evidence.
- ROADMAP success criterion 5 is met: the phase record answers the reporter's own question, citing the schematic and the firmware VPP path.
- The VPP-destination table exists in `.planning/notes/jumper-display-ground-truth.md`, readable by the D-09 phase and citable by Phase 187 (D-11, D-12).
- Every JP4 claim in `.planning/v1.7-SHIELD-REVS.md` that the trace settles is corrected, and the R41 claim is retracted (D-13, D-14).
- Assumption A1 is recorded as the named premise the gate's scope rests on, with its probe.
</success_criteria>

<output>
Create `.planning/phases/182-jp5-destructive-operation-gate/182-05-SUMMARY.md` when done. It must
record: the answer to gh#60 in one paragraph a reporter could read; the list of every claim corrected
in `v1.7-SHIELD-REVS.md` with its old and new value; the three PROBE-PENDING cells and their probes;
assumption A1 with its probe and its consequence; and the statement that D-14 was characterize-only
with no firmware change.
</output>
