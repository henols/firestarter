---
phase: 188-the-tools-directory
plan: 04
type: execute
wave: 4
depends_on: ["188-03"]
commits_in_repo: firestarter_app (submodule; executors commit INSIDE it on the milestone branch)
autonomous: true
requirements: [TOOLS-03]
files_modified:
  - firestarter_app/tests/test_op_registration_parity.py
  - firestarter_app/tests/test_blast_radius_invariance.py
  - firestarter_app/tests/test_chip_test.py
  - firestarter_app/tools/check_devtest_orchestrator.py
  - firestarter_app/tools/check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tools/check_mypy_watermark.py
  - firestarter_app/tools/check_no_community_support_status_write.py
  - firestarter_app/tools/check_no_exists_proxy.py
  - firestarter_app/tools/check_no_log_in_sdp_window.py
  - firestarter_app/tools/check_protection_readability_invariants.py
  - firestarter_app/tools/check_sdp_capability_invariants.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
  - firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tests/test_check_mypy_watermark.py
  - firestarter_app/tests/test_check_no_community_support_status_write.py
  - firestarter_app/tests/test_check_no_exists_proxy.py
  - firestarter_app/tests/test_check_no_log_in_sdp_window.py
  - firestarter_app/tests/test_check_protection_readability.py
  - firestarter_app/tests/test_check_sdp_capability.py
  - firestarter_app/tests/test_lock_status_class_partition.py
  - firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h
  - firestarter_app/tests/fixtures/planted_log_in_window.cpp
  - firestarter_app/tests/fixtures/planted_no_exists_proxy.py
  - firestarter_app/tests/fixtures/planted_permit_by_default.py
  - firestarter_app/tests/fixtures/planted_widenable_allowset.py
  - firestarter_app/tests/fixtures/planted_protection_widenable_tokenset.py
  - firestarter_app/tests/fixtures/planted_protection_permit_by_default.py
  - firestarter_app/.github/workflows/ci.yml
  - firestarter_app/CLAUDE.md

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The nineteen committed report snapshots under tests/fixtures/reports/ are byte-unchanged and still tracked at the end of this plan. They lose their regenerator when plan 188-05 deletes tools/snapshot_report_shapes.py, but they keep a LIVE consumer: the surviving test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree asserts the registered shape-id set equals the set of snapshot filename stems. Deleting or re-bannering them reddens a test D-25 keeps."
    - "tests/test_blast_radius_invariance.py loses exactly its two render_shape sites and keeps everything else: 68 of 106 collected tests survive, and GATE-01, GATE-02, GATE-03, D-07 and D-10 — the v1.36 Phase 174 blast-radius oracle against a silent dedup_fingerprint re-key — remain intact and passing."
    - "Zero files matching the retired gate-family naming pattern remain under firestarter_app/tools/ — the family is retired whole, with no exceptions and no survivor kept back for being load-bearing."
    - "The mypy step and the script it invokes leave CI in the same commit, so host CI is never red between two commits of this phase; nothing replaces the step — not a plain mypy step, not an inline error count."
    - "The 12 V-on-a-part-with-no-VPP-pin hazard description survives its retired guard: the hazard paragraph in firestarter_app/CLAUDE.md stays in full, and only the sentences claiming a live regression guard are removed."
    - "Deleting tests/test_op_registration_parity.py whole does not take a test with it: tests/test_chip_test.py still collects 159 and its one importing test still passes, repaired in the same commit by deriving the two op-vocabulary sets from the surviving firestarter.chip_test product module."
    - "No relocation helper module is created. tests/report_shape_renderer.py and tests/devtest_handler_names.py do not exist at any point in this plan."
  artifacts:
    - path: "firestarter_app/tests/fixtures/reports"
      provides: "The nineteen committed report-shape snapshots — kept, unmodified, because a surviving D-10 closure test asserts against their filename stems"
      contains: "json"
  key_links:
    - from: "firestarter_app/tests/test_blast_radius_invariance.py"
      to: "firestarter_app/tests/fixtures/reports"
      via: "test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree compares the registered shape-id set against the set of snapshot filename stems"
      pattern: "snapshot_stems"
    - from: "firestarter_app/tests/test_chip_test.py"
      to: "firestarter_app/firestarter/chip_test.py"
      via: "the two op-vocabulary sets are derived in-function from the product module, replacing the import of the deleted parity test module"
      pattern: "import firestarter.chip_test as chip_test_mod"
  prohibitions:
    - requirement_id: TOOLS-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT delete or re-banner the nineteen committed snapshots under tests/fixtures/reports/. They keep a live consumer in a test D-25 preserves, so D-08's no-quiet-fragment rule does not reach them. This is the single most likely way this replan goes wrong: the previous draft of this plan re-bannered all nineteen because a relocated renderer would have owned them, and under D-25 nothing is relocated and nothing regenerates them."
    - requirement_id: TOOLS-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT delete the electrical-hazard description in firestarter_app/CLAUDE.md along with the retired guard that referenced it — the paragraph explaining that 12 V on a pin which is not VPP is a hardware-damage path across roughly 23 chips stays in full; only the sentences asserting that a live regression guard enforces it come out."
    - requirement_id: TOOLS-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT create tests/report_shape_renderer.py or tests/devtest_handler_names.py, and MUST NOT create tests/dispatch_model.py or tests/report_claim_patterns.py. Those four modules are the superseded OD-1 relocate basis; D-25 deletes the consuming tests instead."
    - requirement_id: TOOLS-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT delete, skip or xfail tests/test_chip_test.py::test_run_status_is_complete_when_no_step_errored. It does not import any of the four orphaned symbols — it imports two derived constants from a module that does — so under D-25 it is repaired, never deleted. Deleting it would make this plan's collateral one rather than zero."
---

<objective>
Finish the D-25 consumer deletions for the last two orphaned symbols without touching the v1.36
blast-radius oracle or the nineteen snapshots it closes over, then retire the remaining eight gates of
the checker family whole — with their tests, their planted-violation fixtures, the CI step that named
one of them, and the prose in two markdown files that told future readers a guard was live.

Purpose: D-25 says delete the consumers, and it says *surgically*. Whole-module deletion of the eleven
consuming modules would cost 247 collected tests rather than the 90 the operator was priced at, and 68 of
that difference is `tests/test_blast_radius_invariance.py` — the v1.36 Phase 174 hard-ordered, first-and-
alone gate against a silent `dedup_fingerprint` re-key, which was never named at the 188-02 gate. This
plan therefore takes exactly two parametrized tests out of a 106-test module and leaves the oracle
standing. Everything else here is the untouched D-01/D-02/D-13 retirement half: gates and CI steps that
have a same-commit constraint, landing together.

Output: 45 more collected tests gone with zero collateral, the blast-radius oracle and its nineteen
snapshots intact, `firestarter_app/tools/` with no checker family left in it, a CI file whose header no
longer describes a step it does not run, and a suite that is green at both commit boundaries.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/188-the-tools-directory/188-CONTEXT.md
@.planning/phases/188-the-tools-directory/188-RESEARCH.md
@.planning/phases/188-the-tools-directory/188-PATTERNS.md
@.planning/phases/188-the-tools-directory/188-03-SUMMARY.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md

**D-25 is the authority; OD-1 is dead.** Nothing relocates. `render_shape` and the handler allow-list are
deleted with their files (`render_shape` by plan 188-05, the allow-list here). Their consuming tests are
deleted or trimmed here. If a prior artifact tells you to relocate a symbol into the test tier, that is
the superseded basis.

**Plan 188-03 already retired two gates, not one.** It took `check_diagnostic_report_claims.py` and
`check_dispatch.py`, together with the scan-path resolver pair, which the previous draft of this plan
owned. Eight gates remain for this plan. The phase's ROADMAP line for this plan still reads "the nine
remaining gates" — that number was written on the pre-D-25 basis and is stale by one; the authoritative
list is the eight gate paths in this plan's `files_modified`. Verify the starting count by listing rather
than assuming it.

**`/workspaces/CLAUDE.md` "Source code comments — hard rule" binds every edit here.** Delete comments;
never author one. A surviving comment that names a deleted file loses the clause naming it, by deletion
rather than rewriting.

**Commit inside the `firestarter_app` submodule**, on the `v1.37`-slug branch.

**`/usr/bin/grep` or `git grep` only** — the bare devcontainer `grep` is ugrep, honors `.gitignore`, and
under-scans silently. Use `.venv311/bin/python` (Python 3.11.16), never the devcontainer default 3.12.
Pass `-o addopts=""` to pytest whenever a count line is the measurement.

**Re-verify the CI line ranges before editing.** The ranges cited below were confirmed live on
2026-09-12, but a single commit to the repo moves them. Locate each step by its `- name:` text, never by
line number.

**Commit before running the whole suite** — `tests/test_flash_path_record_sync.py` asserts whole-repo
porcelain.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Delete the parity module whole, trim the blast-radius oracle to its two render_shape sites, repair the one collateral</name>
  <precondition>`firestarter_app/.venv311/bin/python` exists and reports Python 3.11.x, and the app suite collects 2307 tests with 0 errors — plan 188-03's measured end state. If the collected count is not 2307, 188-03 did not land as measured and this plan's own figures are unanchored; halt and reconcile rather than proceeding.</precondition>
  <reversibility rating="costly">The seven parity tests and the 38 snapshot-drift and aliasing tests are recoverable from git, but WR-01 — every committed snapshot byte-compared against a fresh regeneration over 19 shape ids — dies with its regenerator when plan 188-05 deletes the snapshot tool, and D-01/D-04 forbid authoring a replacement. Drift in a committed snapshot while that coverage is absent is not retroactively detectable.</reversibility>
  <files>firestarter_app/tests/test_op_registration_parity.py (deleted), firestarter_app/tests/test_blast_radius_invariance.py (trimmed), firestarter_app/tests/test_chip_test.py (repaired)</files>
  <read_first>
    - `firestarter_app/tests/test_blast_radius_invariance.py` **in full** (1071 lines). You are trimming the module that is this milestone's own oracle, and an executor who has not read it whole cannot tell a consuming test from a neighbour. The two sites to delete are `test_committed_snapshot_matches_a_fresh_regeneration` — its `@pytest.mark.parametrize` decorator at line 750, `def` at line 751, body to line 769 — and `test_composing_a_db_diff_never_leaks_onto_a_cached_build_shape` — decorator at line 805, `def` at line 806, body to line 828. Each is parametrized over the 19 registered shape ids, which is why two definitions are 38 collected tests.
    - `firestarter_app/tests/test_blast_radius_invariance.py` lines 871 to 918 in particular — `test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree`, D-10's four-way closure. Read its third assertion: it compares the registered shape-id set against `{p.stem for p in (tests/fixtures/reports).glob("*.json")}`. That test SURVIVES, which is why the nineteen snapshots survive with it.
    - `firestarter_app/tests/fixtures/reports/` — list it and count. Nineteen JSON files before this plan and nineteen after, byte-identical.
    - `firestarter_app/tests/test_op_registration_parity.py` **in full**. Note lines 136 to 140 and line 182: the two constants another module imports are derived by comprehension from `firestarter.chip_test`'s `OP_*` string values, not hand-written. That is what makes the repair in `test_chip_test.py` a two-line derivation rather than a relocation.
    - `firestarter_app/tests/test_chip_test.py` around line 1305 — `test_run_status_is_complete_when_no_step_errored`, whose in-function `from tests.test_op_registration_parity import ...` at line 1309 is the one piece of collateral this plan must repair. Read lines 440, 458 and 586 of the same file too: the module already uses the in-function `import firestarter.chip_test as chip_test_mod` idiom in three places, so the repair follows a pattern the file already carries.
  </read_first>
  <action>
    Pin this plan's diff anchor first: write the current commit sha to `/tmp/188-04-base.sha` with
    `git rev-parse HEAD`. Every numstat leg here diffs against that pinned sha rather than a relative
    `HEAD~N`, which in a repository with parallel sessions names whatever commit landed last. Then record
    the starting collected count; it must read 2307, 0 errors.

    **Delete whole:** `firestarter_app/tests/test_op_registration_parity.py`, 7 collected tests, all 7 of
    them consuming the retired orchestrator gate's handler allow-list. Nothing is relocated: the
    allow-list dies with `tools/check_devtest_orchestrator.py` in Task 2, and
    `tests/devtest_handler_names.py` is not created.

    **Repair the one collateral, in the same commit.** `tests/test_chip_test.py::test_run_status_is_complete_when_no_step_errored`
    imports two derived constants from the module just deleted. That test does not import any of the four
    orphaned symbols, so under D-25 it is repaired, not deleted — collateral is zero. Replace the
    in-function import at line 1309 with an in-function derivation of the same two sets from the
    surviving product module: import `firestarter.chip_test` as a module object, build the set of every
    string-valued attribute whose name begins with the op prefix, and build the multi-word subset as the
    members of that set containing a hyphen. Those are the exact two comprehensions the deleted module
    used at lines 136 to 140 and line 182. Keep the test's three assertions and its docstring unchanged
    apart from the clause naming the deleted module, which is deleted rather than rewritten. Do not
    create a shared helper module for this — the derivation is two expressions and belongs inline, in the
    one test that needs it.

    **Trim the blast-radius oracle to exactly two sites.** In
    `firestarter_app/tests/test_blast_radius_invariance.py`, delete
    `test_committed_snapshot_matches_a_fresh_regeneration` (WR-01) with its parametrize decorator, and
    `test_composing_a_db_diff_never_leaks_onto_a_cached_build_shape` (CR-01's second aliasing path) with
    its parametrize decorator. Delete those two definitions and nothing else. 68 of 106 collected tests
    stay, and every one of GATE-01, GATE-02, GATE-03, D-07 and D-10 stays with them. Leave the module
    docstring, the frozen-hash table, the ladder pins, the shape-id registry, the two planted-mutation
    legs between the deleted pair, `_to_dict_with_db_diff`, and the D-10 four-way closure exactly as they
    are. If removing the two functions leaves an import unused, the linter will name it; remove only what
    the linter names.

    **Do not touch `tests/fixtures/reports/`.** All nineteen snapshots stay, byte-unchanged, and they are
    NOT re-bannered. The previous draft of this plan re-bannered all nineteen because a relocated renderer
    would have owned them; under D-25 nothing is relocated, nothing regenerates them, and the `_generated_by`
    value in each file is a historical record of what produced it rather than a live pointer. They keep a
    live consumer — D-10's closure test asserts their filename stems — so D-08's "do not quietly preserve
    a fragment" does not reach them. Re-bannering them would also mean authoring new content into
    committed data for no consumer, which is exactly what this phase is subtracting.

    **Record WR-01's death for the verdict note.** Snapshot-drift coverage — every committed snapshot
    byte-compared against a fresh regeneration, over 19 shape ids — has no replacement and none is to be
    authored; D-01 and D-04 delete the tool that produced it. Write that in the SUMMARY as a named,
    accepted loss so plan 188-09 can carry it. Do not author a replacement drift check here or anywhere.
  </action>
  <acceptance_criteria>
    - `tests/test_op_registration_parity.py` is untracked; `tests/devtest_handler_names.py` and `tests/report_shape_renderer.py` do not exist.
    - `tests/test_blast_radius_invariance.py` collects exactly 68 tests (was 106), and contains no occurrence of `render_shape` and no occurrence of `snapshot_report_shapes`.
    - `tests/test_blast_radius_invariance.py::test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree` still exists and passes, and so do the frozen-hash, ladder-pin, key-pin and planted-mutation legs.
    - The diff over `tests/test_blast_radius_invariance.py` is deletions only: zero added lines.
    - `tests/fixtures/reports/` still holds exactly 19 tracked JSON files and the diff over that directory is empty — zero files changed, zero lines added, zero deleted.
    - `tests/test_chip_test.py` collects exactly 159 tests, still passes in full, contains no occurrence of `test_op_registration_parity`, and its repaired test derives both op sets from the product module rather than from a test module.
    - The whole app suite collects 2262 tests with 0 errors — down exactly 45 from 188-03's 2307.
    - The whole app suite passes under `.venv311`; both ruff legs on `firestarter/ tests/` are green.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ --co -q -o addopts="" 2>&1 | tail -3</automated>
    <fails_when>the collected count is anything other than 2262, or the output contains `error` or `ERROR`, or no "tests collected" line is printed at all — that last case means `-o addopts=""` was dropped and the measurement is absent rather than passing</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_blast_radius_invariance.py tests/test_chip_test.py --co -q -o addopts="" 2>&1 | tail -2</automated>
    <fails_when>the collected count is anything other than 227 — 265 means the trim did not land, and anything below 227 means the trim took a neighbour or the chip-test repair dropped a test</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest "tests/test_blast_radius_invariance.py::test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree" -o addopts="" -q 2>&1 | tail -2</automated>
    <fails_when>non-zero exit, the output contains `error` or `ERROR`, or the summary does not report exactly `1 passed` — D-10's four-way closure is the test that keeps the nineteen snapshots alive, and if it reddens the snapshots were touched</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tests/fixtures/reports/ > /tmp/188-04-snaps.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-snaps.txt; BASE=$(cat /tmp/188-04-base.sha) && git diff --numstat "$BASE" -- tests/fixtures/reports/ > /tmp/188-04-snapdiff.txt; echo "diff-rc=$?"; wc -l < /tmp/188-04-snapdiff.txt</automated>
    <fails_when>either `git-rc=` or `diff-rc=` is non-zero (a failed command writes an empty file and would print 0, which must not read as a pass), or the first count is anything other than 19 (a snapshot was deleted or added), or the second count is anything other than 0 (a snapshot was modified — the numstat output must be empty, because these files are not touched at all)</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c -e 'render_shape' -e 'snapshot_report_shapes' tests/test_blast_radius_invariance.py; /usr/bin/grep -c 'test_op_registration_parity' tests/test_chip_test.py; echo "grep-rc=$?"</automated>
    <fails_when>either printed count is anything other than 0 — a surviving reference means a function body was only partly removed, or the chip-test repair left the old import in place</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'import firestarter.chip_test as chip_test_mod' tests/test_chip_test.py</automated>
    <fails_when>the printed count is below 4 — the module carried three such in-function imports before this plan, so a count below 4 means the repair did not use the product module and is deriving the op vocabulary from somewhere else</fails_when>
    <automated>cd /workspaces/firestarter_app && BASE=$(cat /tmp/188-04-base.sha) && git diff --numstat "$BASE" -- tests/test_blast_radius_invariance.py > /tmp/188-04-brnumstat.txt; echo "git-rc=$?"; awk '{print "added="$1, "deleted="$2}' /tmp/188-04-brnumstat.txt</automated>
    <fails_when>`git-rc=` is non-zero, or no `added=` line is printed, or `added=` is anything other than 0, or `deleted=` is 0 — a non-zero added count means the oracle was rewritten rather than trimmed, which is the one thing this task must not do</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tests/test_op_registration_parity.py tests/report_shape_renderer.py tests/devtest_handler_names.py tests/dispatch_model.py tests/report_claim_patterns.py > /tmp/188-04-t1-gone.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-t1-gone.txt</automated>
    <fails_when>`git-rc=` is non-zero (a failed listing writes an empty file and would print 0, which must not read as a pass), or the count is anything other than 0 — a deletion target survived, or one of the four forbidden OD-1 relocation helpers was created</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -4 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, any line containing `error` or `ERROR` in the collection summary, a reported passed count other than 2262, `Found` followed by an error count from the linter, or a line reading `files would be reformatted`</fails_when>
  </verify>
  <done>The parity module is gone, the blast-radius oracle keeps 68 of 106 tests with GATE-01/02/03 and D-07/D-10 intact, the nineteen snapshots are untouched and still asserted against, the one collateral test is repaired against the product module, and the suite collects 2262 with 0 errors and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the eight remaining gates, their tests, seven fixtures, the mypy CI step and the guard prose — one commit</name>
  <reversibility rating="costly">D-01 and D-02 together remove ten gates, roughly 3,537 lines of gate tests, and the CI-named type-error floor. Re-adding the floor later means first re-establishing a watermark against whatever the error count has drifted to, which is the work the original gate existed to prevent.</reversibility>
  <files>firestarter_app/tools/check_devtest_orchestrator.py, check_is_memory_cmd_no_ifdef.py, check_mypy_watermark.py, check_no_community_support_status_write.py, check_no_exists_proxy.py, check_no_log_in_sdp_window.py, check_protection_readability_invariants.py, check_sdp_capability_invariants.py (all deleted); firestarter_app/tests/test_check_devtest_orchestrator.py, test_check_is_memory_cmd_no_ifdef.py, test_check_mypy_watermark.py, test_check_no_community_support_status_write.py, test_check_no_exists_proxy.py, test_check_no_log_in_sdp_window.py, test_check_protection_readability.py, test_check_sdp_capability.py (all deleted); firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h, planted_log_in_window.cpp, planted_no_exists_proxy.py, planted_permit_by_default.py, planted_widenable_allowset.py, planted_protection_widenable_tokenset.py, planted_protection_permit_by_default.py (all deleted); firestarter_app/tests/test_lock_status_class_partition.py, firestarter_app/.github/workflows/ci.yml, firestarter_app/CLAUDE.md (edited)</files>
  <read_first>
    - `firestarter_app/.github/workflows/ci.yml` — read lines 1 to 10 and 50 to 100. The header comment near line 3 enumerates the gate steps and names the mypy watermark among them. The mypy step and the two catalog steps that STAY are in the second window. Plan 188-03 changed no workflow file, so these ranges should still hold — but locate each step by its `- name:` text regardless.
    - `firestarter_app/CLAUDE.md` lines 105 to 130. The hazard paragraph begins before line 112, where the sentence about 12 V on pin 1 being a hardware-damage path across roughly 23 chips sits. The `Regression guard:` sentences begin at line 122 and name `tools/check_dispatch.py`, which plan 188-03 already deleted — so that claim has been false since 188-03's second commit, and this task is where it is repaired. Getting the boundary between the hazard paragraph and the guard sentences exactly right is the point of this read.
    - `firestarter_app/tests/test_lock_status_class_partition.py` **in full**. It collects 17 tests. Two of them route a planted fixture through the protection-readability gate's environment-override seam by subprocess, via a helper defined around line 598 and called around lines 618 and 640. Those two and the helper go; the other 15 stay. Read the whole module so you can tell which is which, and read the surviving AST leg's docstring — it names that pair as the thing making it non-decorative, and that clause becomes false.
    - Each of the seven planted-violation fixtures named in this task's file list, before deleting it. A fixture with a surviving consumer stays. Confirm each one's consumers with `git grep -ln` over `tests/ tools/ .github/ pyproject.toml` rather than trusting the list — the research pass found that two of them have extra hits that are docstring mentions rather than loads, and one of them (the protection permit-by-default fixture) is consumed by a test this task itself deletes, which is why it can go only after that deletion.
    - `firestarter_app/tools/check_no_exists_proxy.py`'s default target enumeration — plan 188-03 removed nine entries from it, and seven of the eight gate test files this task deletes are still named in it. Confirm that the gate and those tests leave in the SAME commit, which is what makes that enumeration a non-issue here.
  </read_first>
  <action>
    In one commit, delete the eight remaining gates and the eight test files that exercise them: the
    dev-test orchestrator gate, the ifdef-in-predicate gate, the mypy watermark gate, the
    community-support-status-write gate, the exists-proxy gate, the log-in-SDP-window gate, the
    protection-readability-invariants gate and the SDP-capability-invariants gate. Two of the eight test
    files do not follow the `test_<gate>.py` convention (D-03): the protection-readability and
    SDP-capability tests both drop the `_invariants` suffix. The third exception D-03 names, the dispatch
    gate's test, already left with plan 188-03. Do not assume the naming; confirm each mapping by reading
    the test file's own imports before deleting it.

    The gate and the test files leave together, which matters for one reason worth stating: the
    exists-proxy gate carries a literal enumeration of test modules and fails closed on a missing entry,
    and seven of the eight test files being deleted here are named in it. Because the gate itself is
    deleted in this same commit, that enumeration never gets the chance to fail. If for any reason the
    gate's deletion is separated from the test files', the enumeration must lose those seven names in the
    earlier commit — the same repair plan 188-03 made for its own nine.

    Delete the seven planted-violation fixtures whose only consumers are in that set: the
    ifdef-in-predicate header, the log-in-window source, the exists-proxy module, the permit-by-default
    module, the widenable-allowset module, the protection-widenable-tokenset module, and — after the
    repair below — the protection-permit-by-default module. Keep the unparsable fixture, whose live
    consumer is a `pyproject.toml` rationale, and keep the part-number-delta fixture, which a surviving
    test loads directly.

    Repair `tests/test_lock_status_class_partition.py` in the same commit: delete the subprocess helper
    and the two tests that route a planted fixture through the retired gate, since their subject ceases
    to exist, leaving 15 of 17 collected tests. Delete the clause in the surviving AST test's docstring
    that names that pair as the thing making it non-decorative, because it becomes false. Remove any
    import left unused by the deletion — the linter will name them. Record in the SUMMARY that the
    surviving AST leg is now decorative, as a disclosed cost of D-01, so plan 188-09 can carry it into
    the verdict note.

    Delete the mypy step from `firestarter_app/.github/workflows/ci.yml` — its `- name:` line, its `run:`
    line, and the single blank separator line before the next step, leaving no double blank. Nothing
    replaces it: not a plain mypy invocation, not an inline error count. Amend the header comment at the
    top of the same file, which enumerates the gate steps and names the mypy watermark among them; it
    becomes false, so remove that item from the enumeration. Leave the two catalog steps, both ruff
    steps, the pytest step and the smoke test exactly as they are. The two vector steps are plan 188-05's
    to remove, in its own commit alongside the generator they invoke — do not take them here.

    Edit `firestarter_app/CLAUDE.md`: remove only the sentences beginning `Regression guard:` and the two
    lettered clauses describing what that gate asserted. The hazard paragraph above them — explaining
    that a chip on a pinout with no VPP pin must never route to the EPROM configure path, and naming the
    manufacturers and the seven electrically-erasable exceptions — stays in full. It is the first thing
    an agent reads about this hazard and it outlives its guard. Note for the SUMMARY that this claim has
    been false since plan 188-03 deleted the gate it names, one wave earlier than the previous draft of
    this plan assumed: nothing reddened, because it is prose, but the staleness window is real and plan
    188-09's verdict note quotes the deleted sentences verbatim.

    Do not delete `tools/baseline/dispatch_baseline.json`: it becomes orphaned data with zero consumers,
    but D-14 puts non-script files under the tools directory out of scope. Note it for the verdict note
    instead.
  </action>
  <acceptance_criteria>
    - Zero files matching the gate-family pattern remain tracked under `firestarter_app/tools/`.
    - Zero files matching the gate-test pattern remain tracked under `firestarter_app/tests/`, across all three naming conventions D-03 names.
    - The seven planted-violation fixtures are untracked; `tests/fixtures/planted_unparsable.py` and `tests/fixtures/part_number_delta.json` are still tracked.
    - `firestarter_app/.github/workflows/ci.yml` contains no occurrence of mypy anywhere, including in its header comment, and still contains the catalog validity step, the messages drift gate, both ruff steps, the pytest-with-coverage step and the smoke test.
    - The CI file has no double blank line introduced by the deletion.
    - The two vector CI steps are still present — they belong to plan 188-05 and must not be taken early.
    - `firestarter_app/CLAUDE.md` still contains the hazard sentence naming the hardware-damage path, and contains no sentence asserting that a regression guard enforces it.
    - `tests/test_lock_status_class_partition.py` collects exactly 15 tests (was 17), passes, and leaves no unused import.
    - `tests/fixtures/reports/` still holds exactly 19 tracked JSON files, unchanged since this plan began.
    - The whole app suite collects 2175 tests with 0 errors — down exactly 87 from Task 1's 2262: 85 from the eight gate test modules and 2 from the lock-status repair.
    - The whole app suite passes under `.venv311`; both ruff legs on `firestarter/ tests/` are green.
    - `tools/baseline/dispatch_baseline.json` is still tracked.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && git ls-files -- 'tools/check_*.py' 'tests/test_check_*.py' > /tmp/188-04-gates-gone.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-gates-gone.txt; git ls-files -- 'tools/*.py' > /tmp/188-04-tools.txt; echo "tools-rc=$?"; wc -l < /tmp/188-04-tools.txt</automated>
    <fails_when>either `git-rc=` or `tools-rc=` is non-zero (a failed listing writes an empty file and would print 0, which must not read as a pass), or the first count is anything other than 0 (a gate or a gate test survived), or the second count is 0 (a zero there would mean the whole tools listing failed, so the first zero proved nothing)</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ --co -q -o addopts="" 2>&1 | tail -3</automated>
    <fails_when>the collected count is anything other than 2175, or the output contains `error` or `ERROR`, or no "tests collected" line is printed at all</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/test_lock_status_class_partition.py --co -q -o addopts="" 2>&1 | tail -2</automated>
    <fails_when>the collected count is anything other than 15 — 17 means the two gate-seam tests are still there, and anything below 15 means the repair took a neighbour</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -ci 'mypy' .github/workflows/ci.yml; echo "grep-rc=$?"</automated>
    <fails_when>the printed count is anything other than 0 — a `grep-rc=1` with a printed 0 is the passing shape, and any positive count means the step or its header enumeration entry survived, which D-02 forbids</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c -e 'Catalog validity check' -e 'ruff lint' -e 'ruff format check' -e 'Run pytest with coverage' -e 'Smoke test' -e 'Vector catalog validity check' -e 'Codegen drift gate' .github/workflows/ci.yml</automated>
    <fails_when>the printed count is anything other than 8 — five steps that must survive this phase, plus the two vector steps and the second codegen drift gate that belong to plan 188-05 and must still be present here. Measured at 8 on 2026-09-12; below 8 means a step that must stay was deleted with the ones that go</fails_when>
    <automated>cd /workspaces/firestarter_app && awk 'BEGIN{p=0;c=0} /^[[:space:]]*$/{if(p){c++} p=1; next} {p=0} END{print "doubleblanks="c}' .github/workflows/ci.yml</automated>
    <fails_when>`doubleblanks=` reports anything other than 0, or no `doubleblanks=` line is printed at all</fails_when>
    <automated>cd /workspaces/firestarter_app && /usr/bin/grep -c 'Regression guard' CLAUDE.md; echo "guard-rc=$?"; /usr/bin/grep -c 'hardware-damage path' CLAUDE.md; echo "hazard-rc=$?"</automated>
    <fails_when>the first printed count is anything other than 0 (the false claim survived), or the second is anything other than 1 (the hazard sentence that MUST outlive its retired guard was deleted with it), or `hazard-rc=` is 2 with a printed 0 for the hazard count</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tests/fixtures/planted_ifdef_in_predicate.h tests/fixtures/planted_log_in_window.cpp tests/fixtures/planted_no_exists_proxy.py tests/fixtures/planted_permit_by_default.py tests/fixtures/planted_widenable_allowset.py tests/fixtures/planted_protection_widenable_tokenset.py tests/fixtures/planted_protection_permit_by_default.py > /tmp/188-04-fix-gone.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-fix-gone.txt; git ls-files -- tests/fixtures/planted_unparsable.py tests/fixtures/part_number_delta.json tools/baseline/dispatch_baseline.json > /tmp/188-04-fix-keep.txt; echo "keep-rc=$?"; wc -l < /tmp/188-04-fix-keep.txt</automated>
    <fails_when>either `git-rc=` or `keep-rc=` is non-zero (a failed listing writes an empty file and would print 0, which must not read as a pass), or the first count is anything other than 0 (a planted fixture whose gate is gone survived), or the second is anything other than 3 (a fixture or data file with a live consumer was deleted — the unparsable fixture is cited by a pyproject rationale, the part-number delta is loaded by a surviving test, and the dispatch baseline is data D-14 keeps in scope)</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- tests/fixtures/reports/ > /tmp/188-04-snaps2.txt; echo "git-rc=$?"; wc -l < /tmp/188-04-snaps2.txt; BASE=$(cat /tmp/188-04-base.sha) && git diff --numstat "$BASE" -- tests/fixtures/reports/ > /tmp/188-04-snapdiff2.txt; echo "diff-rc=$?"; wc -l < /tmp/188-04-snapdiff2.txt</automated>
    <fails_when>either `git-rc=` or `diff-rc=` is non-zero, or the first count is anything other than 19, or the second is anything other than 0 — the nineteen snapshots must be byte-unchanged across this whole plan, because D-10's surviving closure test asserts against their filename stems</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest tests/ -o addopts="" -q 2>&1 | tail -4 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <fails_when>non-zero exit, any line containing `error` or `ERROR` in the collection summary, a reported passed count other than 2175, `Found` followed by an error count from the linter, or a line reading `files would be reformatted`</fails_when>
  </verify>
  <done>The checker family is gone from `firestarter_app/tools/`, CI runs no mypy step and no longer says it does, the seven planted fixtures left with their gates, the hazard description outlives its guard, the nineteen snapshots are untouched, and the suite collects 2175 with 0 errors and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the D-25 deletion ledger → the v1.36 blast-radius oracle | `tests/test_blast_radius_invariance.py` is the milestone's hard-ordered, first-and-alone gate against a silent `dedup_fingerprint` re-key; 38 of its 106 tests consume a retired symbol and 68 do not, and the two populations are interleaved in one file |
| the nineteen committed snapshots → the D-10 closure test | the snapshots lose their regenerator but keep a live consumer; they are a set whose membership a surviving assertion compares against the registered shape ids |
| a deleted test module → a surviving test module | `tests/test_chip_test.py` imports two derived constants from the parity module this plan deletes, and no decision, research table or ledger row names that edge |
| chip database record → firmware configure path | the retired dispatch gate was the only automated control stopping a 12 V handler being selected for a part whose pinout has no VPP pin |
| host CI → the published package | the retired mypy watermark was the CI-named type-error floor; after D-02 nothing bounds the error count |
| `firestarter_app/CLAUDE.md` → a future agent | it is the first thing an agent reads about the 12 V hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-12 | Tampering | the nineteen committed snapshots deleted or re-bannered as fragments of a retired tool, reddening a surviving gate | high | mitigate | The plan carries an explicit prohibition; the action says in full why they stay; two verify legs assert both that exactly 19 files are tracked and that the numstat diff over the directory is EMPTY at both commit boundaries; and a third runs D-10's closure test on its own and requires exactly one passing result. The previous draft of this plan re-bannered all nineteen, so this is a live, not theoretical, failure mode. |
| T-188-13 | Tampering | a trim of the blast-radius oracle removing more than the two named `render_shape` sites | high | mitigate | The module's collected count is asserted exactly at 68 of 106, a named-symbol grep is asserted at zero, the numstat leg requires zero ADDED lines so the oracle cannot have been rewritten, and D-10's closure test is run as its own leg. GATE-01, GATE-02, GATE-03 and D-07 are named in the acceptance criteria as survivors. |
| T-188-14 | Repudiation | a whole-module deletion silently breaking a module D-25 keeps | high | mitigate | `tests/test_chip_test.py` imports two derived constants from the parity module. Measured by execution, not inferred: with the parity module removed, that test fails, and with it restored it passes. The repair derives both sets in-function from the surviving product module, the collected count of the repaired module is asserted at 159, and a prohibition forbids deleting the test instead of repairing it. |
| T-188-15 | Repudiation | WR-01's snapshot-drift coverage disappearing without being recorded as a loss | high | mitigate | The action requires the loss to be written into the SUMMARY as a named, accepted loss for plan 188-09's verdict note, and forbids authoring a replacement drift check. D-01 and D-04 delete the tool that produced that coverage; D-23 forbids a successor guard. |
| T-188-16 | Repudiation | the retired dispatch gate's electrical-safety invariant | high | accept | Disclosed, accepted cost of D-01 and D-25: the operator was shown that one of the ten is the CI-named type floor and chose "retire the whole family, no exceptions", and was shown at the 188-02 gate that the six wire-contract suites carry the BLOCKER-2 invariant before answering "Delete the consumers". Recorded by name in plan 188-09's verdict note; D-23 forbids a successor guard for any reason, so no mitigation may be authored here. |
| T-188-17 | Tampering | unbounded type-error drift after the mypy floor leaves CI | high | accept | Disclosed, accepted cost of D-02: the operator was shown that the tree carries roughly 33 mypy errors, that a must-exit-0 step would be red on day one, and that dropping the gate lets the count drift upward unnoticed, and chose "nothing — mypy leaves CI". Recorded in the verdict note; no replacement is authored. |
| T-188-18 | Tampering | host CI red between two commits of this phase | medium | mitigate | D-21(a): the mypy step and the script it invokes are deleted in one commit; verify legs assert zero mypy mentions remain anywhere in the file, that eight named steps are still present including the two vector steps plan 188-05 owns, and that no double blank line was introduced. |
| T-188-SC | Tampering | package-manager installs | low | accept | No package is added and no install task exists; the suite runs against the already-installed editable package in `.venv311`. No package-legitimacy audit is required, and none is claimed. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** nothing. This plan is subtraction and one in-place repair. It creates NO
relocation helper module — `tests/report_shape_renderer.py` and `tests/devtest_handler_names.py` are the
superseded OD-1 basis and must not exist when this phase ends.

**Kept deliberately, against the obvious reading:**

| Kept | Why |
|---|---|
| `firestarter_app/tests/fixtures/reports/` — 19 JSON snapshots, byte-unchanged | a SURVIVING test, `test_shape_ids_frozen_hashes_ladder_pins_and_snapshots_agree`, asserts that the registered shape-id set equals the set of their filename stems. They lose their regenerator in plan 188-05 but keep a live consumer, so D-08's no-quiet-fragment rule does not reach them. Not re-bannered: nothing regenerates them, so the generator name in each file is a historical record rather than a live pointer. |
| `firestarter_app/tests/test_chip_test.py::test_run_status_is_complete_when_no_step_errored` | it imports two derived constants from the deleted parity module but consumes none of the four orphaned symbols; D-25 repairs it rather than deleting it, which is what keeps this plan's collateral at zero |
| 68 of 106 tests in `tests/test_blast_radius_invariance.py` | GATE-01, GATE-02, GATE-03, D-07 and D-10 — the v1.36 Phase 174 oracle against a silent `dedup_fingerprint` re-key |
| `firestarter_app/tools/baseline/dispatch_baseline.json` | orphaned data with zero consumers after this plan; D-14 scopes the phase to scripts, not data |

**Destroyed by this plan (blast radius):**

| Destroyed | Collected tests | Consequence |
|---|---:|---|
| `tests/test_op_registration_parity.py` | 7 | the dev-test op-registration parity proof, whole; all 7 of its tests consumed the retired orchestrator gate's handler allow-list |
| `tests/test_blast_radius_invariance.py::test_committed_snapshot_matches_a_fresh_regeneration` | 19 | WR-01. Snapshot-drift coverage over all 19 shape ids ceases to exist and has NO replacement, by decision, not by oversight |
| `tests/test_blast_radius_invariance.py::test_composing_a_db_diff_never_leaks_onto_a_cached_build_shape` | 19 | CR-01's second aliasing path. CR-01's first aliasing path survives in the same module |
| eight `tools/check_*.py` gates | 0 | the checker family ceases to exist; the SDP-window logging invariant, the exists-proxy invariant, the protection-readability invariant, the SDP-capability invariants, the ifdef-in-predicate invariant, the community-support-status-write invariant, the dev-test orchestrator coverage invariant and the mypy type-error floor all lose their only automated control |
| eight `tests/test_check_*.py` modules | 85 | gate tests, including the planted-violation legs proving each gate had teeth |
| seven planted-violation fixtures | 0 | the known-bad subjects those legs used |
| two gate-seam tests and their subprocess helper in `tests/test_lock_status_class_partition.py` | 2 | the surviving AST leg in that module becomes decorative — it asserts a rule that nothing now proves can fire |
| the mypy CI step and both header clauses naming it | 0 | host CI runs catalog, vector, ruff and pytest only; after plan 188-05 it runs catalog, ruff and pytest |
| the `Regression guard:` sentences in `firestarter_app/CLAUDE.md` | 0 | the hazard paragraph survives; the claim that a live guard enforces it does not |
| **Total** | **132** | 2307 collected before, 2262 after Task 1, 2175 after Task 2 |

## Per-module deletion ledger (this plan's rows — re-measure, do not trust)

| Module | Collected | Consuming | Disposition |
|---|---:|---:|---|
| `tests/test_op_registration_parity.py` | 7 | 7 | delete whole |
| `tests/test_blast_radius_invariance.py` | 106 | 38 | trim the two `render_shape` sites only; 68 stay, GATE-01/02/03 and D-07/D-10 intact |

Both figures re-measured live on 2026-09-12 with
`.venv311/bin/python -m pytest <path> --co -q -o addopts=""`. Re-measure them in this plan's own run
rather than trusting the table — both are inputs to a deletion. The other nine ledger rows belong to plan
188-03 and should already have landed; this plan's precondition asserts their measured end state (2307
collected) rather than assuming it.

**The eleventh module is not in the ledger and must not be deleted.** `tests/test_chip_test.py` (159
collected) imports two derived constants from the parity module. It consumes none of the four orphaned
symbols, so D-25's rule — no test that does not import one of the four is deleted — makes it a repair,
not a twelfth deletion. Measured by execution during planning: with the parity module removed it fails;
with the parity module restored it passes. This edge is named in no decision, no research table and no
ledger row, which is exactly why it is stated here.

## Flagged Planner Assumptions (spec-less probe — unresolved rows)

This phase has no SPEC.md, so every edge-coverage probe row came back `unresolved`. Each is surfaced
here rather than converted into a vacuous truth or dropped.

- **TOOLS-03 / `empty`** — resolved explicitly by truth 3: the gate family's end state is the empty set,
  asserted as a listing count rather than inferred from what was removed, with a second non-empty count
  in the same leg so a failed listing cannot read as a pass.
- **TOOLS-03 / `adjacency`** — resolved explicitly by truth 5 and truth 2. Two boundaries in this plan
  are exactly the "things that touch must separate" case: the hazard paragraph and the guard sentences
  that follow it in the same markdown file, and the 38 consuming tests interleaved with 68 surviving ones
  in a single 1071-line module. One stays, one goes, in both.
- **TOOLS-03 / `ordering`** — resolved explicitly: the mypy CI step and the script it invokes leave in
  one commit; the eight gate test files and the exists-proxy gate that enumerates seven of them leave in
  one commit; the protection-permit-by-default fixture can go only after the test that routes it through
  the gate is deleted, which is why both happen in Task 2 rather than across Tasks 1 and 2.
- **TOOLS-03 / `encoding`** — **flagged, unresolved.** The probe asks whose definition of equality
  applies. This plan takes "the same test" to mean the same pytest node id and measures by collected
  count, which is why two deleted function definitions are 38 collected tests: both are parametrized over
  19 shape ids. That choice is stated rather than proven. If a reviewer prefers definition counts, every
  figure in the ledger moves and the plan must be re-measured; nothing else about it changes.
- **TOOLS-01 / `unclassified — review manually`** — **flagged, not applicable to this plan.** TOOLS-01 is
  dissolved by D-06 with the coverage-matrix tool, which plan 188-05 deletes. No task here touches it.

Two further assumptions, specific to the replan and worth a reviewer's eye:

- **This plan retires EIGHT gates, not nine.** Plan 188-03 now takes two — the diagnostic-claims gate and
  the dispatch gate — because under D-25 it deletes the dispatch gate's consumers and leaving the gate
  file behind would strand it. The phase's ROADMAP line for this plan still reads "the nine remaining
  gates", a number written on the pre-D-25 basis. Eight is the measured figure; the ROADMAP line is stale
  by one and plan 188-09 should correct it when it amends the phase section.
- **The scan-path resolver pair is no longer this plan's to delete.** Plan 188-03 deletes it, because its
  fail-closed leg reddens the moment the dispatch gate disappears and D-13's own planning note says to
  pair the deletion with the tools it indexes. This plan therefore neither deletes nor asserts anything
  about it beyond the phase-wide sweep in plan 188-07.
- **The `Regression guard:` claim in `firestarter_app/CLAUDE.md` is already false when this plan starts.**
  It names `tools/check_dispatch.py`, which plan 188-03 deleted one wave earlier. Nothing reddens — it is
  prose — but the staleness window is real and is stated here rather than discovered. Repairing it is
  still this plan's job, because it is batched with the rest of the D-01 prose work.

## Probe Coverage (this plan's share)

| Requirement | Probe category | Disposition | Where it landed |
|---|---|---|---|
| TOOLS-03 | empty | resolved (explicit) | truth 3 — the gate family's empty end state is asserted as a count, with a non-empty control in the same leg |
| TOOLS-03 | adjacency | resolved (explicit) | truth 5 and truth 2 — the hazard paragraph separates from its guard claim, and 68 surviving tests separate from 38 consuming ones in one file |
| TOOLS-03 | ordering | resolved (explicit) | truth 4 and the Task 2 action — three same-commit constraints named and verified |
| TOOLS-03 | encoding | flagged (unresolved) | stated above: equality is pytest node id, and every count is a collected count |

<verification>
Both tasks' automated legs in order, then two read-backs no regex can do.

First, open `firestarter_app/CLAUDE.md` around the hazard paragraph and confirm by eye that the
manufacturer list and the seven-exception sentence are intact and that the paragraph reads coherently
without the removed guard sentences. A grep can prove a string is present; only a reader can tell whether
the paragraph still says what it meant.

Second, open `tests/test_blast_radius_invariance.py` at both points where a deleted test used to begin,
and confirm that no stray parametrize decorator, blank-line run or dangling docstring fragment was left
behind, and that the tests immediately above and below each cut are intact. This module is the v1.36
oracle; a collected count proves the right number of tests survive, but only a reader can tell whether
the file is still well-formed.
</verification>

<success_criteria>
1. The nineteen committed snapshots are byte-unchanged and still tracked, and D-10's four-way closure test passes on its own.
2. `tests/test_blast_radius_invariance.py` keeps 68 of 106 collected tests with GATE-01, GATE-02, GATE-03, D-07 and D-10 intact, and its diff carries zero added lines.
3. `tests/test_chip_test.py` still collects 159 and passes, repaired against the surviving product module rather than deleted — collateral is zero.
4. Zero gates and zero gate tests remain tracked; seven planted fixtures left with their gates and three named keeps survived.
5. The mypy step and the script it invokes leave in the same commit; nothing replaces it; no mypy mention remains anywhere in the CI file. The two vector steps are untouched and still present for plan 188-05.
6. The 12 V hazard description survives in full; only the guard claim is removed.
7. The suite collects and passes at every commit in this plan — 2262 after Task 1, 2175 after Task 2, 0 errors at both; both ruff legs green.
8. No relocation helper module exists: none of the four OD-1 modules is created.

## Gate markers

<!-- planner-discipline-allow: mypy -->
<!-- planner-discipline-allow: Regression guard -->
<!-- planner-discipline-allow: render_shape -->
<!-- planner-discipline-allow: snapshot_report_shapes -->
<!-- planner-discipline-allow: test_op_registration_parity -->
<!-- planner-discipline-allow: hardware-damage path -->
</success_criteria>

<output>
Create `.planning/phases/188-the-tools-directory/188-04-SUMMARY.md` when done. Record: the eight
gate-to-test mappings as confirmed by reading, not assumed; the collected count after each of the two
commits against 188-03's measured 2307; the `tests/test_chip_test.py` collateral and exactly how it was
repaired, since no decision or research table named it; **WR-01's death — snapshot-drift coverage over 19
shape ids, with no replacement and none to be authored — stated as a named accepted loss** for plan
188-09's verdict note; that the surviving AST leg in the lock-status module is now decorative, as a second
disclosed D-01 cost; that this plan retired eight gates and not the nine the ROADMAP line predicts; and
that `tools/baseline/dispatch_baseline.json` is now orphaned data retained by D-14. Rationale belongs here
and in the commit message — never in a source comment.
</output>




