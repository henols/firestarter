---
phase: 188-the-tools-directory
plan: 02
type: execute
wave: 2
depends_on: ["188-01"]
commits_in_repo: none (decision gate — evidence is written to /tmp, nothing is committed)
autonomous: false
requirements: [TOOLS-03, TOOLS-04]
files_modified: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The operator sees LIVE consumer counts for all four orphaned symbols — re-measured in this plan, not transcribed from a day-old artifact — before any file carrying them is deleted."
    - "The decision is put once, as one confirmation covering all four symbols, and it names the alternative that was never offered: deleting the eight consuming test modules."
    - "No file is created, edited or deleted by this plan; it produces a decision and a measurement, nothing else."
  artifacts:
    - path: "/tmp/188-02-orphan-census.txt"
      provides: "Live per-symbol consumer counts and collected-test counts, captured in this plan's own run"
  key_links:
    - from: "188-02 decision outcome"
      to: "188-03, 188-04, 188-05"
      via: "every plan that deletes a file exporting one of the four symbols is blocked on this gate"
      pattern: "depends_on"
  prohibitions:
    - requirement_id: TOOLS-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT choose the delete-the-consumers option silently — destroying the eight consuming test modules (roughly 105 tests, including six wire-contract modules) was never offered to the operator and is outside this phase's stated scope; it may only be taken if the operator names it at this gate."
---

<objective>
Put the one question this phase cannot answer on its own to the operator, with live numbers, before
a single byte is deleted.

Purpose: four of the files D-01 and D-04 delete are not only gates and tools — they also export
library symbols that **eight surviving test modules import as their oracle**. D-01 says "all ten, no
exceptions"; D-08 says "do not quietly preserve a fragment". Neither decides what happens to a decode
model that surviving tests depend on. The orchestrator has ruled that the model relocates into the
test tier and only the gate half dies (OD-1), and the plans downstream are written that way — but the
ruling is put to the operator here as one confirmation, at the head of the host lane, before any
task deletes those four files.

Output: a live census of the four symbols' consumers, and an operator decision on the record.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/188-the-tools-directory/188-CONTEXT.md
@.planning/phases/188-the-tools-directory/188-RESEARCH.md
@.planning/phases/188-the-tools-directory/188-PATTERNS.md

**Do NOT bare-`cat` `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/PROJECT.md`** —
518 KB / 937 KB / 439 KB, with a single ~52,000-character line in STATE.md. Targeted
`/usr/bin/grep -n` and `sed -n` only.

**Use `/usr/bin/grep` or `git grep`, never the bare devcontainer `grep`.** The devcontainer's `grep`
is ugrep and honors `.gitignore`, so it silently under-scans and any gate built on it fails OPEN.
A naive search for a dotted module path also misses all six `test_val_wire_*` modules — they prepend
the tools directory to `sys.path` and import bare.

**The app suite runs under Python 3.11, not the devcontainer's 3.12.** App CI pins 3.11 only, and
`pyproject.toml`'s `addopts = "-ra -q"` means a second `-q` suppresses the count line — pass
`-o addopts=""` whenever the collected or passed figure is the thing being measured.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the four orphaned symbols' consumers live</name>
  <files>/tmp/188-02-orphan-census.txt (created; deliberately not committed — D-19 keeps sweeps out of the tree)</files>
  <read_first>
    - `.planning/phases/188-the-tools-directory/188-RESEARCH.md` §"Dangling-Reference Sweep" subsections A1 and A2 — the measured table this task re-runs.
    - `firestarter_app/tools/check_dispatch.py` lines 30-60 and 115-145 — `_ALGO_MEM_TYPE`, `_SRAM_PROTOCOLS`, `KNOWN_PROTOCOLS`, and the `dispatch()` signature and docstring. Confirm by reading that `main()` and everything below it is the gate half and the symbols above are the model half.
    - `firestarter_app/tests/test_val_wire_sram.py` lines 23-35 — the `sys.path` prepend and the bare import that a dotted-path search cannot see.
  </read_first>
  <action>
    Capture a live census to `/tmp/188-02-orphan-census.txt` covering exactly four symbols and their
    consumers. Do not edit or delete anything.

    For each symbol, record the importing modules by path and line, and the number of tests each
    contributes, measured with `pytest --co -q -o addopts=""` per module rather than by counting
    `def test_` lines:

    - `dispatch`, `_ALGO_MEM_TYPE`, `_SRAM_PROTOCOLS`, `KNOWN_PROTOCOLS` from the dispatch gate —
      expected consumers: the six `test_val_wire_*` modules (module-level, bare import after a
      `sys.path` prepend), `test_decoder.py` (five in-function imports), `test_build_db_inclusion.py`
      (one in-function import).
    - `render_shape` from the report-shape snapshot tool — expected consumer:
      `test_blast_radius_invariance.py`, at two parametrized sites swept over the registered shape ids.
    - `_HANDLER_FUNCTION_NAMES` from the orchestrator gate — expected consumer:
      `test_op_registration_parity.py`, module-level import plus one locator site.
    - `FORBIDDEN_PATTERNS` from the diagnostic-claims gate — expected consumer:
      `test_parse_devtest_issue.py`, one in-function import.

    Also record, in the same file: the current whole-suite collected count (the baseline the phase
    will be compared against), and the count of tests that would fail to **collect** if the four
    files disappeared unrepaired — collection errors are not test failures, pytest reports ERROR and
    those modules contribute zero results.

    Note in the census file any divergence from RESEARCH.md's figures. RESEARCH.md's measurements
    outrank CONTEXT.md's; a live measurement taken now outranks both.
  </action>
  <acceptance_criteria>
    - `/tmp/188-02-orphan-census.txt` exists and names all four symbols, each with at least one consuming module path and a test count measured by `pytest --co`, not by grep.
    - The census records the whole-suite collected count as a single number.
    - The census explicitly states, for each of the four, whether the live figure matches RESEARCH.md's and by how much it differs if not.
    - `git status --porcelain` in `firestarter_app` shows no change attributable to this task — the census is a measurement, not an edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ --co -q -o addopts="" 2>&1 | tail -3</automated>
    <fails_when>the output contains `error` or `ERROR` (a collection error means the suite is already broken before this phase starts, and the baseline is not trustworthy), or no line matching `tests collected` / `tests/` count appears</fails_when>
    <automated>cd /workspaces/firestarter_app && git grep -n -e 'from check_dispatch import' -e 'import tools.check_devtest_orchestrator' -e 'from tools.snapshot_report_shapes import' -e 'from tools.check_diagnostic_report_claims import' -- tests/ | wc -l</automated>
    <fails_when>the count is 0 — a zero here means the search pattern missed the consumers rather than that none exist, which is the exact fail-open this census exists to prevent</fails_when>
    <automated>cd /workspaces && test -s /tmp/188-02-orphan-census.txt; echo "rc=$?"</automated>
    <fails_when>`rc=1` — an absent or empty census file</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain > /tmp/188-02-st.txt; echo "git-rc=$?"; wc -l < /tmp/188-02-st.txt</automated>
    <fails_when>`git-rc=` is non-zero (a failed status call prints nothing and would read as a pass), or the line count is anything other than 0</fails_when>
  </verify>
  <done>A live, file-backed census of all four orphaned symbols and their consumers exists, and the working tree is untouched.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm the relocation of four orphaned symbols before any file carrying them is deleted</name>
  <files>none — this task decides, it does not edit</files>
  <read_first>
    - `/tmp/188-02-orphan-census.txt` — Task 1's live census. Present its figures, not RESEARCH.md's; if the two diverge, say so in the prompt.
    - `.planning/phases/188-the-tools-directory/188-CONTEXT.md` §D-01 and §D-08 — the two decisions in tension here: "all ten, no exceptions" and "do not quietly preserve a fragment". Neither decides what happens to a decode model that surviving tests depend on, which is why this gate exists.
  </read_first>
  <acceptance_criteria>
    - The prompt names all four symbols and gives a live test count for each, taken from Task 1's census rather than from a prior artifact.
    - The prompt states that the gate half of every one of the four files is deleted either way, so the operator is not choosing whether to retire the gates.
    - The prompt names the alternative — deleting the eight consuming test modules — and its cost.
    - The gate carries `gate="blocking-human"`, so it stops for a human in auto-mode too.
    - The operator's answer is recorded verbatim in the SUMMARY, including the exact resume-signal word.
  </acceptance_criteria>
  <what-built>A live census of four library symbols that surviving tests import from four files this phase deletes. Nothing has been deleted; no file has been edited. The census is at `/tmp/188-02-orphan-census.txt`.</what-built>
  <how-to-verify>
    Four of the files Phase 188 deletes are two things in one file: a gate (the half D-01 and D-04
    retire) and a library symbol that surviving tests import as their oracle. Deleting them whole
    breaks, by live measurement: `dispatch` and its three lookup tables — 44 tests across eight
    modules, six of them the `test_val_wire_*` wire-contract suites; `render_shape` — 38 tests in
    `test_blast_radius_invariance.py`; `_HANDLER_FUNCTION_NAMES` — 7 tests in
    `test_op_registration_parity.py`; `FORBIDDEN_PATTERNS` — 1 test in `test_parse_devtest_issue.py`.
    Forty-five of those break at *collection* time, meaning those modules report nothing at all.

    The plan proceeds on this basis: each symbol moves into a small test-tier helper module under
    `firestarter_app/tests/`, and the gate half of every one of the four files is deleted exactly as
    D-01 and D-04 require. The alternative, which was never put to you, is to delete the eight
    consuming test modules as well — roughly 105 tests including all six wire-contract suites.

    Confirm the relocation, or name the alternative.
  </how-to-verify>
  <resume-signal>Type "relocate" to proceed as planned, or "delete consumers" to destroy the eight consuming test modules instead, or describe a third option.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → operator | a scope decision the artifacts do not settle is crossing to a human; the framing is the only thing the human sees |
| `git` history → the working tree | everything downstream of this gate is an irreversible-by-default subtraction of ~16,500 lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-188-05 | Repudiation | the checkpoint prompt's own numbers | high | mitigate | Task 1 re-measures every figure live with `pytest --co` before the prompt is shown, and records any divergence from RESEARCH.md in the census file. The operator never decides on a transcribed number. |
| T-188-06 | Tampering | auto-mode auto-approving this gate | high | mitigate | The task carries `gate="blocking-human"`, which is never auto-approved in any mode including `--auto` and `--chain`. `autonomous: false` alone has been observed to be insufficient. |
| T-188-07 | Information Disclosure | the census file under `/tmp` | low | accept | Contains only test-module paths and counts from a public repository; written to the session-scoped temp area, never committed. |
| T-188-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; the suite is collected against the already-installed editable package. No package-legitimacy audit is required, and none is claimed. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** `/tmp/188-02-orphan-census.txt` — a measurement, deliberately not committed
(D-19: the dangling-reference and census greps are run *as part of the work*, never staged as a
committed evidence artifact).

**Destroyed by this plan:** nothing. This plan deletes no file, edits no file and commits nothing.

## Flagged Planner Assumptions (spec-less probe — unresolved rows)

The deterministic edge probe returned two `unclassified` rows. Per protocol they stay **unresolved**
and are surfaced here rather than auto-resolved with a backstop marker or silently dropped:

| Requirement | Probe row | Why it stays unresolved |
|---|---|---|
| TOOLS-01 | `unclassified — review manually` | TOOLS-01 asks that `audit_coverage_matrix.py` refuse a repo-escaping default output path. D-06 dissolves the requirement by deleting the tool, so there is no residual behaviour with an edge to probe. The probe could not classify it because the requirement describes a guard on a program that will not exist. Recorded, not converted into a vacuous truth. |
| TOOLS-07 | `unclassified — review manually` | TOOLS-07 asks that `audit_coverage_matrix.py --check` exit 0 or that its staleness be recorded. Same cause: D-06 dissolves it with the tool. Its current literal state was measured for the record before deletion — the command exits 1 printing nothing at all on either fail path — and that measurement is carried into the verdict note by plan 188-09, which is the honest disposition. |

## Probe Coverage (this plan's share)

| Requirement | Probe category | Disposition | Where it landed |
|---|---|---|---|
| TOOLS-01 | unclassified | unresolved → FLAGGED | the table above |
| TOOLS-07 | unclassified | unresolved → FLAGGED | the table above |

<verification>
Task 1's four automated legs, then the checkpoint. The checkpoint's own verification is the
operator's answer; record it verbatim in the SUMMARY, including the exact resume-signal word used.
</verification>

<success_criteria>
1. All four symbols' consumers are counted live, by collection, before the question is asked.
2. The question is asked once, covering all four, naming the alternative that was never offered.
3. The gate stops for a human even under `--auto` / `--chain`.
4. Nothing is deleted, edited or committed by this plan.
</success_criteria>


<output>
Create `.planning/phases/188-the-tools-directory/188-02-SUMMARY.md` when done. Record: the live
census figures, every divergence from RESEARCH.md, the operator's answer verbatim, and — if the
answer was anything other than "relocate" — a HALT, because plans 188-03 through 188-05 are written
for the relocation and must be replanned before they run.
</output>

