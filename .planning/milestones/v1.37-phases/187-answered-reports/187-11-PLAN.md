---
phase: 187-answered-reports
plan: 11
type: execute
wave: 11
depends_on: ["187-10"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/187-11-gh31-operator-approval.txt
  - .planning/phases/187-answered-reports/evidence/187-11-post-transcript.txt
  - .planning/phases/187-answered-reports/evidence/187-11-comment-id.txt
autonomous: false
requirements: [REPLY-02, REPLY-05, REPLY-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The comment posted to gh#31 is byte-identical to `evidence/bodies/187-gh31.md` as approved, proven by a JSON round trip that exits non-zero on mismatch"
    - "gh#31 is still OPEN after this plan, and its label set is exactly the pre-existing set plus `needs:report`"
    - "gh#31 does not carry `fix:released` after this plan, and the posted body states why that label is withheld"
    - "`cause:database` is still present on gh#31 after the label edit, because the pin-map defect it names is real and unfixed"
    - "All five replies this phase owes have been posted, and no pending status line remains in the review document"
  prohibitions:
    - "Must NOT close gh#31; a close requires the reporter's confirmation or a superseding PASS"
    - "Must NOT apply `fix:released` to gh#31; only the harness and reporting fix shipped and the chip defect did not"
    - "Must NOT remove `cause:database` or any other pre-existing label from gh#31; the label change is additive"
    - "Must NOT post any body whose sha256 differs from the one recorded in the approval file"
    - "Must NOT verify the posted comment with `gh api --jq '.body'` piped into `diff`; that form reports a spurious mismatch on a provably identical comment"
    - "Must NOT touch gh#23, gh#28, gh#60, gh#62 or gh#9 in this plan; approval for gh#31 carries to no other issue"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 1 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-11-gh31-operator-approval.txt"
      provides: "the per-issue approval literal, the label decision including the withheld label, and the sha256 of the body as approved"
      contains: "APPROVED-FOR-POST: gh31"
    - path: ".planning/phases/187-answered-reports/evidence/187-11-post-transcript.txt"
      provides: "the command, exit code and returned URL for the post, plus the label edit"
    - path: ".planning/phases/187-answered-reports/evidence/187-11-comment-id.txt"
      provides: "the posted comment id alone on line 1"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-11-gh31-operator-approval.txt"
      to: ".planning/phases/187-answered-reports/evidence/bodies/187-gh31.md"
      via: "a sha256 line binding the approval to the exact bytes posted"
      pattern: "sha256\\("
---

<objective>
Post REPLY-02's m27c1001 reply to gh#31, add `needs:report`, withhold `fix:released` deliberately, and
leave the issue open. This is the last of the five replies.

Purpose: the reporter's INCONCLUSIVE verdict was attributed to their chip rather than to the harness,
and that attribution is what changed. The reply concedes it, keeps the pin-map defect standing as
unfixed with its measured current value, and carries the same caveat about what a passing re-run would
and would not prove.

Output: one posted comment, an additive label change, a deliberately withheld label, an issue still open,
and a review document with no pending status line left.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.claude/skills/devtest-triage/SKILL.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve the exact body to post to gh#31 and confirm the label decision, including the label being withheld</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/bodies/187-gh31.md` in full — the exact bytes being approved
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` — the dispositions table and this issue's status line
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-14, § D-15 and § D-16
    - `.claude/skills/devtest-triage/SKILL.md` — the label taxonomy, for the candidates presented
  </read_first>
  <context>
    D-16 requires a separate blocking checkpoint immediately before each public act. The four approvals
    already given in this phase carry nothing here. This gate approves gh#31 only, and it is the last
    public act of the phase.

    Show the operator four things:

    1. The full verbatim contents of `evidence/bodies/187-gh31.md`, with its sha256 from
       `evidence/187-06-body-hashes.txt`. This is the exact file passed to `--body-file` unmodified.
    2. The label decision: add `needs:report`, withhold `fix:released` for the same reason as gh#28, and
       keep every existing label including `cause:database`, which names a defect that is real and
       unfixed. Confirm that the body states the withholding explicitly.
    3. The two substantive points, so the operator can judge them: that the reply restates the pin-map
       defect with its measured current value — pin 30 mapped as an address line where the datasheet
       gives no connection, because the row still points at the 32-pin 27C020 pin map — and that the fix
       for it belongs in the database generator's decode function, never in the generated JSON.
    4. That the issue is not being closed, and will not be until the reporter confirms or a superseding
       PASS exists.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    If the operator amends any wording, apply it to `evidence/bodies/187-gh31.md` and to the inline copy
    in `187-UPSTREAM-REPLIES.md` first, save a unified diff of the amendment against a scratchpad copy of
    the pre-approval draft beside the body, then recompute the sha256 and write the approval file.

    Write `evidence/187-11-gh31-operator-approval.txt` with the literal `APPROVED-FOR-POST: gh31` alone
    on line 1, then the operator's name and date, then the decision prose explicitly labelled as the
    orchestrator's rendering of a menu selection plus its option description rather than a verbatim
    operator quotation, then the approved label list and the withheld label named as withheld, then a
    single `sha256(evidence/bodies/187-gh31.md) = <hash>` line.
  </context>
  <options>
    <option id="approve">
      <name>Approve as drafted, adding `needs:report` and withholding `fix:released`</name>
      <pros>Completes the set: every reporter this project owes an answer has one, and no issue was closed on our own reading</pros>
      <cons>Public and permanent the moment it posts</cons>
    </option>
    <option id="amend">
      <name>Approve with wording or label amendments</name>
      <pros>The operator's exact wording ships; the amendment's scope is recorded as a diff</pros>
      <cons>The body, its inline copy and its hash must all be updated before the post</cons>
    </option>
    <option id="hold">
      <name>Do not post yet</name>
      <pros>Nothing further is public</pros>
      <cons>REPLY-02 stays half-delivered, with one of its two reporters answered and the other not</cons>
    </option>
  </options>
  <resume-signal>Select: approve, amend (and state the amendment), or hold</resume-signal>
  <acceptance_criteria>
    - `evidence/187-11-gh31-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-POST: gh31`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection plus its option description rather than a verbatim operator quotation.
    - The file carries exactly one `sha256(evidence/bodies/187-gh31.md) = <hash>` line, and that hash equals `sha256sum` of the body on disk at the moment the file was written.
    - The file records the approved label list verbatim.
    - If the operator amended the wording, the amendment was applied to both the body file and its inline copy, a unified diff of it was saved beside the body, and the hash was recomputed after the amendment.
    - Nothing was posted before this file existed.
    - The file names `fix:released` explicitly as withheld, with the reason.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Post to gh#31, prove byte-identity, add `needs:report`, leave it open</name>
  <reversibility rating="one-way">A comment on a public issue is visible to every watcher the instant it posts and is delivered by email; it can be edited but never un-seen.</reversibility>
  <precondition>`evidence/187-11-gh31-operator-approval.txt` exists, its first line is exactly `APPROVED-FOR-POST: gh31`, and its recorded sha256 equals `sha256sum evidence/bodies/187-gh31.md`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-11-post-transcript.txt, .planning/phases/187-answered-reports/evidence/187-11-comment-id.txt, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.3 and § Finding 6.4 (the posting command and the byte-identity trap), § Finding 2 (gh#31's pre-phase comment count and label set)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Posting — the command, and the read-back that does not false-alarm
    - `.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json`
    - `.planning/phases/187-answered-reports/evidence/187-11-gh31-operator-approval.txt`
  </read_first>
  <action>
    Re-assert the gate mechanically: recompute `sha256sum evidence/bodies/187-gh31.md` and confirm it
    equals the hash recorded in the approval file. If it differs, stop.

    Post with `gh issue comment 31 --repo henols/firestarter_prom --body-file evidence/bodies/187-gh31.md`.
    Pass the file; never interpolate the body onto the command line. Capture the returned URL, write the
    comment id alone on line 1 of `evidence/187-11-comment-id.txt`, and write a transcript block into
    `evidence/187-11-post-transcript.txt` with the command, exit code and URL.

    Prove byte-identity by reading the comment back through the API as JSON and comparing the decoded
    body against the file with a python round trip that exits non-zero on mismatch. Do not use
    `gh api --jq '.body'` piped into `diff`. Record both lengths and both hashes in the transcript.

    Add exactly the labels the operator approved with
    `gh issue edit 31 --repo henols/firestarter_prom --add-label "<label>"` per label. Use only
    `--add-label`; `cause:database` and `cause:harness` must both survive the edit. Do not pass
    `fix:released` in any form. Record each command and exit code.

    Do not close the issue. Re-read gh#31 and record its state, stateReason and resulting label set,
    reconciling field by field against the pre-phase set in `evidence/187-05-issue-state-before.json`.

    Flip gh#31's status line in `187-UPSTREAM-REPLIES.md` to a posted state, fill its Posted line with
    the comment URL, and note both the deliberate non-close and the deliberately withheld label. After
    this flip no pending status line remains.

    Touch no other issue.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/187-11-gh31-operator-approval.txt</automated>
    <fails_when>prints anything other than `APPROVED-FOR-POST: gh31`</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh31.md | cut -d' ' -f1 | /usr/bin/grep -qF -f - evidence/187-11-gh31-operator-approval.txt && echo BOUND</automated>
    <fails_when>prints nothing, or non-zero exit — the approval file's hash does not cover the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && gh api "repos/henols/firestarter_prom/issues/comments/$(head -1 evidence/187-11-comment-id.txt)" > /tmp/c187gh31.json && python3 -c "import json,hashlib,sys; b=json.load(open('/tmp/c187gh31.json'))['body'].encode(); d=open('evidence/bodies/187-gh31.md','rb').read(); print('api',len(b),hashlib.sha256(b).hexdigest()); print('disk',len(d),hashlib.sha256(d).hexdigest()); print('BYTE-IDENTICAL' if b==d else 'MISMATCH'); sys.exit(0 if b==d else 1)"</automated>
    <fails_when>prints MISMATCH, or non-zero exit</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN — gh#31 was closed, which REPLY-06 forbids unconditionally</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 3 — the pre-phase count recorded in evidence/187-05-issue-state-before.json was 2, so any value other than that count plus one means a missing or duplicated post</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json labels --jq '[.labels[].name] | sort | join(",")'</automated>
    <fails_when>the output omits `needs:report`, omits `cause:database`, omits `cause:harness`, or contains `fix:released`</fails_when>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 9 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 1 — this phase posts nothing new on gh#9</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 0 — a status line is still pending after the last reply was posted</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && ls evidence/187-*-comment-id.txt | wc -l</automated>
    <fails_when>prints anything other than 5 — one posted comment id per reply is required before the phase can close</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-11-gh31-operator-approval.txt` line 1 is exactly `APPROVED-FOR-POST: gh31`, its sha256 line matches the body on disk, and it names `fix:released` as withheld.
    - The API-read comment body is byte-identical to the file; the round trip prints `BYTE-IDENTICAL` and exits 0.
    - `gh issue view 31 --json state` returns `OPEN`.
    - gh#31's comment count equals its pre-phase count from the before-capture plus exactly one.
    - gh#31's label set equals the pre-phase set plus `needs:report`, retains `cause:database` and `cause:harness`, and does not contain `fix:released`.
    - gh#23 and gh#28 are still OPEN; gh#9 still carries one comment.
    - `187-UPSTREAM-REPLIES.md` has zero pending status lines, and five comment-id files exist in `evidence/`.
  </acceptance_criteria>
  <done>All five replies are posted; every one was approved on disk first and proven byte-identical after; three disputed issues remain open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved body on disk → public issue comment | The moment of publication; visible to every watcher and delivered by email. |
| a label → the reporter's conclusion | `fix:released` here would imply a closure no sentence claims; dropping `cause:database` would erase the record of a defect still unfixed. |
| the last public act → the phase's honesty claim | If any of the three disputed issues is closed here, the milestone's central claim about itself is false. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-56 | Repudiation | posting a body the operator never approved | critical | mitigate | Per-issue approval literal plus sha256 binding as the task's `<precondition>`, re-asserted before the post, with a byte-identity read-back after |
| T-187-57 | Elevation of Privilege | closing gh#31 | critical | mitigate | Closing is an unconditional named prohibition; the post-plan state is asserted `OPEN`, and all three disputed issues are re-asserted `OPEN` here and again in 187-12 |
| T-187-58 | Repudiation | `fix:released` implying a closure | high | mitigate | Applying it is a named prohibition, its absence is an automated label assertion, and the body states the withholding explicitly |
| T-187-59 | Tampering | dropping `cause:database` or `cause:harness` during the edit | high | mitigate | Only `--add-label` is permitted; both labels are asserted present in the resulting set |
| T-187-60 | Tampering | a false mismatch from the naive read-back | high | mitigate | The naive form is a named prohibition; only the JSON round trip is permitted |
| T-187-61 | Elevation of Privilege | auto-mode approving the gate | critical | mitigate | `gate="blocking-human"`, the never-dispatch constraint in plan text, and an explicit mode confirmation at the checkpoint |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*`, `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| the five `evidence/bodies/187-gh*.md` payloads, both link checks, both hash files, `187-UPSTREAM-REPLIES.md` | 187-05, 187-06 |
| the gh#60, gh#62, gh#23 and gh#28 approval files, transcripts, comment-id files, and the window-start file | 187-07 … 187-10 |
| `evidence/187-11-gh31-operator-approval.txt`, `evidence/187-11-post-transcript.txt`, `evidence/187-11-comment-id.txt` | **(this plan)** |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-06 is an `unclassified` spec-less probe row and stays unresolved; the full 11-row accounting is
  in `187-01-PLAN.md`. It is additionally enforced here as a prohibition and by automated state
  assertions on all three disputed issues.
- The expected comment counts in the verify block are the values measured on 2026-09-12. The
  before-capture in `evidence/187-05-issue-state-before.json` is the authoritative baseline; if a
  reporter commented in the interval, the expected value is that recorded count plus one.

<verification>
- The approval file exists with its literal, its label decision including the withheld label, and a hash covering the posted body.
- The posted comment reads back byte-identical.
- gh#31 is OPEN, carries one more comment than the before-capture, gained `needs:report`, retained both cause labels, and does not carry `fix:released`.
- gh#23 and gh#28 are still OPEN; gh#9 is untouched.
- Zero pending status lines remain, and five comment-id files exist.
</verification>

<success_criteria>
Every reporter this project owes an answer has one, each in a comment the operator approved byte for
byte, and not one of the three disputed issues was closed on our own reading.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-11-SUMMARY.md` when done. Record the comment URL, both
hashes from the round trip, the before and after label sets, the withheld label and the reason, and the
confirmation that all three disputed issues remain open.
</output>
