---
phase: 187-answered-reports
plan: 04
type: execute
wave: 4
depends_on: ["187-03"]
files_modified:
  - .planning/phases/187-answered-reports/187-MERGE-RECORD.md
  - .planning/phases/187-answered-reports/evidence/187-04-fw-cut.txt
  - .planning/phases/187-answered-reports/evidence/187-04-fw-version.txt
  - .planning/phases/187-answered-reports/evidence/187-04-operator-approval.txt
autonomous: false
requirements: [REPLY-01, REPLY-02, REPLY-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The firmware pre-release version named in every reply is READ from `gh release list` after `beta-build.yml` completed, and is recorded alone on line 1 of `evidence/187-04-fw-version.txt`"
    - "`src/proms/flash_5v_page.cpp` on the firmware repository's `origin/beta` is identical to the milestone branch's copy after this plan, proving the 52-line deletion landed"
    - "The workflow polled for the firmware cut is `beta-build.yml`; `beta-release.yml` does not exist in the firmware repository"
    - "No `v1.37` tag exists in any of the three repositories after this plan"
    - "`firestarter#54`, a stale external contribution open since 2026-08-22, is untouched: still OPEN and with the same head and base it had before"
  prohibitions:
    - "Must NOT poll, grep for, or otherwise reference `beta-release.yml` in the firmware repository; that filename does not exist there and a leg naming it fails open or wastes a cycle"
    - "Must NOT touch, close, comment on or merge `firestarter#54`; it is an unrelated external contribution"
    - "Must NOT compute, predict or guess the firmware pre-release version; it is read from `gh release list` after the run completes, per D-05"
    - "Must NOT create or push a `v1.37` tag in any repository"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 2 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-04-fw-version.txt"
      provides: "the firmware pre-release version, alone on line 1, for every reply body that names one"
    - path: ".planning/phases/187-answered-reports/evidence/187-04-fw-cut.txt"
      provides: "the pre-merge measurement, the PR number, the timestamped poll lines, the read version, and the `firestarter#54` untouched proof"
      contains: "READ AFTER THE CUT"
    - path: ".planning/phases/187-answered-reports/evidence/187-04-operator-approval.txt"
      provides: "the per-act approval literal for the firmware merge"
      contains: "APPROVED-FOR-MERGE: firmware"
    - path: ".planning/phases/187-answered-reports/187-MERGE-RECORD.md"
      provides: "the firmware rows of sections 1 through 4, completing the merge record's ship half"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-04-fw-version.txt"
      to: "every reply body drafted in 187-06 that asks for a `firestarter fw --install` before a re-run"
      via: "the read firmware version is named beside the install instruction"
      pattern: "3\\.0\\.0b[0-9]+"
---

<objective>
Merge the firmware milestone branch to `beta`, let `beta-build.yml` cut the pre-release, and read the
resulting version rather than predicting it.

Purpose: D-02 requires all three repositories to merge, and the firmware delta is not docs-only — it
carries a 52-line pure deletion in `src/proms/flash_5v_page.cpp` alongside a re-recorded
`size_baseline.json`. Every reply that asks a reporter to run `firestarter fw --install` before a fresh
`dev test` needs a firmware version that exists.

Output: the firmware merge, one published pre-release, the version read from the API, and the merge
record's ship half completed.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg. Export `XDG_CACHE_HOME` to a writable scratch directory
before any `gh run view --log`.

**The firmware repository's beta workflow is `beta-build.yml`, not `beta-release.yml`.** CONTEXT.md's
Integration Points block says `beta-release.yml` fires in both sub-repos; that filename is correct for
the app and wrong for the firmware. The behaviour CONTEXT.md describes is right — a merge to firmware
`beta` does cut a pre-release — only the filename differs. `beta-release.yml` does not exist in
`/workspaces/firestarter/.github/workflows/`, whose contents are `beta-build.yml`, `build.yml` and
`py32f071.yml`.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the firmware seam and record the untouched state of the stale external PR</name>
  <files>.planning/phases/187-answered-reports/evidence/187-04-fw-cut.txt</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Drift from CONTEXT.md, DRIFT-1 (the workflow filename, with the directory listings that prove it)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 1.1 (firmware measured 9 ahead), § Finding 1.2 (`firestarter#54` is a stale external contribution with head `beta` and base `beta`, open since 2026-08-22), § Finding 1.4 (the firmware delta, with the 52-line pure deletion), § Finding 1.5 (what `beta-build.yml` produces)
    - `/workspaces/firestarter/.github/workflows/beta-build.yml`
  </read_first>
  <action>
    Work in `/workspaces/firestarter`. Run `git fetch origin` first.

    Write every measurement verbatim into `evidence/187-04-fw-cut.txt` under a heading
    `PRE-MERGE MEASUREMENT (firmware)`: `git rev-parse origin/beta`, `git rev-parse HEAD`,
    `git rev-parse --abbrev-ref HEAD`, the `^+` and `^-` counts from
    `git cherry origin/beta <branch>`, and `git status --short`.

    Record `ls -1 .github/workflows/` verbatim. This is the DRIFT-1 proof and belongs on the record so
    no later reader re-derives the wrong filename.

    Record the delta that makes this merge a code change rather than a docs merge:
    `git diff --stat origin/beta...HEAD` and
    `git diff --numstat origin/beta...HEAD -- src/proms/flash_5v_page.cpp`. The second is expected to
    read `0	52	src/proms/flash_5v_page.cpp` — zero added, fifty-two deleted.

    Record `gh pr list --repo henols/firestarter --state open --json number,title,headRefName,baseRefName`
    verbatim, and capture `firestarter#54`'s state, head and base as the before-half of an untouched
    proof. Assert that no open PR has this milestone branch as its head. Do not assert "zero open PRs" —
    that phrasing goes red here specifically, because #54 exists and is unrelated.

    Record the pre-cut published state for orientation only, labelled clearly as the version that must
    NOT be named in any reply: `gh release list --repo henols/firestarter --limit 3`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && ls -1 .github/workflows/ | /usr/bin/grep -x 'beta-build.yml' | wc -l</automated>
    <fails_when>prints anything other than 1 — the firmware workflow filename is not what DRIFT-1 measured and every poll leg in Task 3 needs re-deriving</fails_when>
    <automated>cd /workspaces/firestarter && ls -1 .github/workflows/ | /usr/bin/grep -x 'beta-release.yml' | wc -l</automated>
    <fails_when>prints anything other than 0 — if this filename now exists in the firmware repo, DRIFT-1 has been superseded and Task 3 must be re-derived before merging</fails_when>
    <automated>cd /workspaces/firestarter && git fetch origin --quiet && B=$(git rev-parse --abbrev-ref HEAD) && git cherry origin/beta "$B" > /tmp/187-cherry.txt && /usr/bin/grep '^+' /tmp/187-cherry.txt | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — a zero-ahead reading before the merge means the branch or the remote was resolved wrongly</fails_when>
    <automated>cd /workspaces/firestarter && git fetch origin --quiet && BASE=$(git merge-base origin/beta HEAD) && git show "$BASE:src/proms/flash_5v_page.cpp" > /tmp/187-fw-old.cpp && git show HEAD:src/proms/flash_5v_page.cpp > /tmp/187-fw-new.cpp && echo $(( $(wc -l < /tmp/187-fw-old.cpp) - $(wc -l < /tmp/187-fw-new.cpp) ))</automated>
    <fails_when>prints anything other than 52, or non-zero exit — the file on the milestone branch is not exactly 52 lines shorter than on the merge base, so the deletion D-02 names is not in the delta being merged</fails_when>
    <automated>cd /workspaces/firestarter && B=$(git rev-parse --abbrev-ref HEAD) && gh pr list --repo henols/firestarter --state open --json headRefName --jq '.[].headRefName' > /tmp/187-prheads.txt && /usr/bin/grep "$B" /tmp/187-prheads.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a PR for this branch already exists</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-04-fw-cut.txt` contains a `PRE-MERGE MEASUREMENT (firmware)` block with both SHAs, the branch name, both `git cherry` counts, verbatim `git status --short`, the verbatim `ls -1 .github/workflows/` listing, the `git diff --stat` and `--numstat` output, the verbatim open-PR listing including `firestarter#54`'s state/head/base, and the pre-cut `gh release list` labelled as the version not to be named.
    - `beta-build.yml` is present in `.github/workflows/` and `beta-release.yml` is absent.
    - `git diff --numstat origin/beta...HEAD -- src/proms/flash_5v_page.cpp` reads 0 added and 52 deleted.
    - No open PR in `henols/firestarter` has this milestone branch as its head.
  </acceptance_criteria>
  <done>The firmware seam is measured live, the workflow filename is confirmed on the record, and `firestarter#54`'s before-state is captured.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve merging the v1.37 firmware milestone branch to `beta` in `henols/firestarter`, which publishes a pre-release build to GitHub Releases</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/187-04-fw-cut.txt` § PRE-MERGE MEASUREMENT (firmware) — the block shown to the operator verbatim
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-02, § D-05 and § D-16
    - `/workspaces/firestarter/.github/workflows/beta-build.yml`
  </read_first>
  <context>
    This is the third and last merge approval under D-16. Neither the meta approval from Plan 187-02 nor
    the app approval from Plan 187-03 carries over, and approving this carries nothing to any reply
    posting.

    Present the operator with the verbatim `PRE-MERGE MEASUREMENT (firmware)` block from
    `evidence/187-04-fw-cut.txt`, and state these five things:

    1. What fires: `.github/workflows/beta-build.yml` — not `beta-release.yml`, which does not exist in
       this repository — triggers on every push to `beta`. It has no `paths-ignore` filter, deliberately,
       because `FW_VERSION` is compiled in from `include/version.h` and a skipped bump would ship a
       binary that misreports its own version. It publishes a pre-release with `.hex` assets for uno,
       uno328pb and leonardo to GitHub Releases only. There is no PyPI step.
    2. The reversibility rating, in D-02's own words: one-way, for the same publication reason as D-01.
    3. That this is not a docs-only merge: `src/proms/flash_5v_page.cpp` loses 52 lines, zero added. The
       change is behaviour-neutral because the host clears the erase-capability flag for every protocol
       `0x05` part, but it is a code edit and it ships as one.
    4. How long it takes: measured firmware runs of this workflow are 3m31s, 11m48s and 70m21s. The
       polling in Task 3 uses no deadline shorter than 90 minutes.
    5. That `firestarter#54` — an external OLED contribution open since 2026-08-22 with head `beta` and
       base `beta` — exists, is unrelated, and will not be touched.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    On approval, write `evidence/187-04-operator-approval.txt` with the literal
    `APPROVED-FOR-MERGE: firmware` alone on line 1, then the operator's name and date, then the decision
    prose explicitly labelled as the orchestrator's rendering of a menu selection rather than a verbatim
    operator quotation.
  </context>
  <options>
    <option id="merge">
      <name>Open the PR and merge it to `beta`</name>
      <pros>Completes D-02's three-repository merge; gives every re-run ask a firmware version that exists</pros>
      <cons>One-way — a published GitHub pre-release cannot be unpublished</cons>
    </option>
    <option id="hold">
      <name>Open the PR but hold the merge</name>
      <pros>The 9-commit diff, including the 52-line deletion, is reviewable before the cut</pros>
      <cons>The phase blocks; re-run asks would name no firmware version or an old one</cons>
    </option>
    <option id="stop">
      <name>Stop the phase here, with meta and app already merged</name>
      <pros>No firmware publication</pros>
      <cons>Leaves D-02 half-done and the three repositories out of lockstep at the version boundary</cons>
    </option>
  </options>
  <resume-signal>Select: merge, hold, or stop</resume-signal>
  <acceptance_criteria>
    - `evidence/187-04-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-MERGE: firmware`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection rather than a verbatim operator quotation.
    - The SUMMARY records that the operator was shown the workflow filename `beta-build.yml`, the 52-line deletion, and the existence of the unrelated `firestarter#54`.
    - The SUMMARY records the operator's confirmation that the harness is not running under `--auto` or `--chain`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Merge the firmware branch, wait for beta-build.yml, and read the version</name>
  <reversibility rating="one-way">The merge triggers `beta-build.yml`, which publishes a pre-release to GitHub Releases; a published release cannot be unpublished, and D-02 rates it one-way in those terms.</reversibility>
  <precondition>`evidence/187-04-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-MERGE: firmware`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-04-fw-cut.txt, .planning/phases/187-answered-reports/evidence/187-04-fw-version.txt, .planning/phases/187-answered-reports/187-MERGE-RECORD.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 1.6 (the 70-minute firmware run and the polling rule), § Finding 1.7 (`gh release list` is read, never computed), § Finding 1.8 (gitlinks: meta HEAD already points at the sub-repo milestone tips, and the `origin/beta` gitlinks are structurally two commits behind because the CI bump commit does not exist until after the merge)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Common Pitfalls, Pitfall 2 and Pitfall 6
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Reading the cut, never predicting it, and § `187-MERGE-RECORD.md`
  </read_first>
  <action>
    Work in `/workspaces/firestarter`. Push the milestone branch to `origin`, open the pull request with
    `gh pr create --repo henols/firestarter --base beta --head <milestone branch>`, and merge it with
    `gh pr merge <N> --repo henols/firestarter --merge`. Use a true merge commit. Do not route this
    through `/gsd-ship`.

    Poll with
    `gh run list --repo henols/firestarter --workflow beta-build.yml --limit 1 --json status,conclusion,databaseId`
    on a 30-second interval, breaking only when `status` reads `completed`. Use no wall-clock deadline
    shorter than 90 minutes — a measured run of this workflow took 70 minutes because the PlatformIO
    cold cache builds three AVR targets plus an ARM target, and a short timeout reports a false failure
    on a healthy cut. Record each poll line with a UTC timestamp into `evidence/187-04-fw-cut.txt`.

    Do not dispatch the workflow manually with `gh workflow run`; manual dispatch is blocked by the
    auto-mode classifier and is an operator-only act.

    Once the run reports `completed` with conclusion `success`, read the version — never compute it —
    with `gh release list --repo henols/firestarter --limit 3`. Write the newest pre-release tag, and
    nothing else, as the entire first line of `evidence/187-04-fw-version.txt`. Record the full listing
    under a heading `READ AFTER THE CUT` in `evidence/187-04-fw-cut.txt`.

    Prove the deletion landed on `origin/beta` by fetching and diffing `src/proms/flash_5v_page.cpp`
    between `origin/beta` and the milestone branch tip; the diff must be empty.

    Capture the after-half of the `firestarter#54` untouched proof: its state, head, base and comment
    count, reconciled against the before-half from Task 1.

    Record the gitlink position for the milestone close's benefit: meta HEAD's gitlinks already name the
    two sub-repo milestone-branch tips, and after each cut the sub-repo `origin/beta` advances by the
    merge commit plus CI's auto version-bump commit, so meta's `origin/beta` gitlinks are structurally
    two commits behind per repository. This is precedented, not drift, and D-03 says not to schedule a
    second meta PR to fix it unless the operator asks. Record it in `187-MERGE-RECORD.md`; do not act
    on it.

    Fill the firmware rows of `187-MERGE-RECORD.md` sections 1 through 4, completing the ship half of
    the record: the PR, the post-merge `git cherry` reading, the observed cut tag, and the release
    confirmation read directly from the API.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && gh run list --repo henols/firestarter --workflow beta-build.yml --limit 1 --json status --jq '.[0].status'</automated>
    <fails_when>prints anything other than `completed`</fails_when>
    <automated>cd /workspaces/firestarter && gh run list --repo henols/firestarter --workflow beta-build.yml --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
    <fails_when>prints anything other than `success`</fails_when>
    <automated>cd /workspaces/firestarter && git fetch origin --quiet && A=$(git rev-parse origin/beta:src/proms/flash_5v_page.cpp) && B=$(git rev-parse HEAD:src/proms/flash_5v_page.cpp) && [ "$A" = "$B" ] && echo IDENTICAL</automated>
    <fails_when>prints nothing, or non-zero exit — the blob on `beta` differs from the milestone branch's, so the 52-line deletion did not land</fails_when>
    <automated>cd /workspaces && head -1 .planning/phases/187-answered-reports/evidence/187-04-fw-version.txt | /usr/bin/grep -E '^[0-9]+\.[0-9]+\.[0-9]+b[0-9]+$' | wc -l</automated>
    <fails_when>prints anything other than 1 — the version file is missing, or line 1 carries a label, a `v` prefix or a second field</fails_when>
    <automated>cd /workspaces/firestarter && gh release list --repo henols/firestarter --limit 5 > /tmp/187-fwrel.txt && /usr/bin/grep -F "$(head -1 /workspaces/.planning/phases/187-answered-reports/evidence/187-04-fw-version.txt)" /tmp/187-fwrel.txt | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — the recorded version does not appear in the live release list, meaning it was transcribed or computed rather than read</fails_when>
    <automated>cd /workspaces/firestarter && gh pr view 54 --repo henols/firestarter --json state --jq .state</automated>
    <fails_when>prints anything other than `OPEN` — the stale external contribution was closed or merged</fails_when>
    <automated>cd /workspaces/firestarter && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a v1.37 tag exists in the firmware repository, which D-04 forbids</fails_when>
    <automated>cd /workspaces && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a v1.37 tag exists in the meta repository</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a v1.37 tag exists in the app repository</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh pr view <N> --repo henols/firestarter --json state --jq .state` returns `MERGED` with base `beta`.
    - The latest `beta-build.yml` run reports `status: completed` and `conclusion: success`.
    - `git diff --numstat origin/beta HEAD -- src/proms/flash_5v_page.cpp` produces no output.
    - `evidence/187-04-fw-version.txt` line 1 matches a bare PEP 440 pre-release such as `3.0.0b27`, and that exact string appears in `gh release list --repo henols/firestarter`.
    - `gh pr view 54 --repo henols/firestarter --json state,headRefName,baseRefName,comments` matches the before-half captured in Task 1.
    - `git ls-remote --tags origin` matches no `v1.37` tag in any of the three repositories.
    - `187-MERGE-RECORD.md` sections 1 through 4 are complete for all three repositories, and the gitlink position is recorded without being acted on.
  </acceptance_criteria>
  <done>All three repositories are merged to `beta`, both cuts have fired and been read, and the merge record's ship half is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware milestone branch → public `beta` → GitHub Releases | The merge publishes `.hex` binaries a reporter will flash onto their own hardware. |
| this phase's PR → the stale external contribution `firestarter#54` | An unrelated third-party PR shares the repository and the `beta` base ref; an over-broad action could close or merge it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-16 | Denial of Service | the published firmware pre-release | high | mitigate | A blocking-human approval file is the task's `<precondition>`; the gate states the one-way rating and presents the live delta including the 52-line deletion |
| T-187-17 | Tampering | `firestarter#54` | high | mitigate | Its state, head, base and comment count are captured before and reconciled after; touching it is a named prohibition; the PR is created with an explicit `--head` naming the milestone branch |
| T-187-18 | Spoofing | the firmware version named in a reply | high | mitigate | Read from `gh release list` after completion, written bare, cross-checked against the live list; computing it is a named prohibition |
| T-187-19 | Denial of Service | a short poll timeout taking corrective action against a healthy 70-minute cut | medium | mitigate | Polling breaks only on `status == completed` with no deadline under 90 minutes, and every poll line is timestamped into the evidence file |
| T-187-20 | Tampering | `v1.37` tag creation across all three repositories | high | mitigate | Tag absence is asserted here for all three repos at once, and again in 187-12 |
| T-187-21 | Information Disclosure | a leg grepping the firmware repo for `beta-release.yml` | medium | mitigate | Task 1 asserts both the presence of `beta-build.yml` and the absence of `beta-release.yml`, so a superseding change is caught before the merge rather than after |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*` (meta merge, pinned SHA, approval) | 187-02 |
| `evidence/187-03-*` (app cut, app version, approval) | 187-03 |
| `evidence/187-04-fw-cut.txt`, `evidence/187-04-fw-version.txt`, `evidence/187-04-operator-approval.txt` | **(this plan)** |
| `187-MERGE-RECORD.md` sections 1-4 completed for all three repos | **(this plan)**; started 187-02 and 187-03, tail added by 187-12 |
| `187-UPSTREAM-REPLIES.md` | 187-05, 187-06, 187-12 |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md`, `187-gh60.md`, `187-gh62.md` | 187-05, 187-06 |
| `evidence/187-05-issue-state-before.json`, the two draft-link-check files | 187-05, 187-06 |
| one operator-approval file and one post transcript per posted issue | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- The full 11-row spec-less probe accounting is in `187-01-PLAN.md`. REPLY-01 is an `unclassified` row
  that stays unresolved; this plan discharges only the firmware-version precondition its re-run ask
  depends on.
- RESEARCH § Assumptions Log A6 rates the meta-first merge ordering as a recommendation rather than a
  constraint. The constraint it serves — that no permalink may be pinned before the meta merge — is
  verified, and Plan 187-02 already discharged it, so the app-then-firmware ordering used here is free.

<verification>
- The firmware PR is MERGED into `beta`; `beta-build.yml` completed with conclusion `success`.
- The 52-line deletion is present on the firmware `origin/beta`.
- The version on line 1 of `evidence/187-04-fw-version.txt` appears in the live GitHub release list.
- `firestarter#54` is unchanged.
- No `v1.37` tag exists in any of the three repositories.
</verification>

<success_criteria>
All three repositories carry v1.37 on `beta`, both cuts are published and read, and every claim a reply
will make about installable artefacts is backed by a version that exists.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-04-SUMMARY.md` when done. Record the PR number, the
run id and its duration, the read version, the `firestarter#54` before/after reconciliation, the gitlink
position, and the statement that no tag was created in any repository.
</output>
