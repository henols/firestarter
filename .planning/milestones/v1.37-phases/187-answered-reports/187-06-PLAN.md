---
phase: 187-answered-reports
plan: 06
type: execute
wave: 6
depends_on: ["187-05"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/bodies/187-gh23.md
  - .planning/phases/187-answered-reports/evidence/bodies/187-gh28.md
  - .planning/phases/187-answered-reports/evidence/bodies/187-gh31.md
  - .planning/phases/187-answered-reports/evidence/187-06-draft-link-check.txt
  - .planning/phases/187-answered-reports/evidence/187-06-body-hashes.txt
autonomous: true
requirements: [REPLY-01, REPLY-02, REPLY-05, REPLY-06]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "gh#28 and gh#31 each receive their own body file with its own sha256, and each names its own chip; no single body is written once and posted to both issues"
    - "Each of gh#28's and gh#31's bodies names at least one shipped change that bears on that specific issue, and where the chip's own defect has no fix it says the defect is still unfixed rather than letting the reply imply one"
    - { statement: "In gh#28's and gh#31's bodies the D-12 caveat — that a dev test PASS reached through the blank-check skip does not mean the user-facing write command works on the same part — appears before or within the re-run ask, never in a later section the reporter reaches only after the ask", verification: backstop }
    - { statement: "In every body carrying a re-run ask, the REPLY-05 schema_version and dedup_fingerprint sentence sits with the ask — in the same bolded section or immediately adjacent to it — rather than in a section the reporter reaches only after acting on the ask", verification: backstop }
    - "Every app or firmware version named in any of the three bodies is byte-equal to line 1 of `evidence/187-03-app-version.txt` or line 1 of `evidence/187-04-fw-version.txt`, both of which were read from the release API after the cuts rather than computed"
    - "gh#23's body concedes that the reporter's diagnosis — a rig fault reported as a chip verdict — was correct, and separately states that the tool did not and cannot become able to see an unhooked VPP"
    - "All three bodies restate their chip's 2026-08-08 datasheet finding as an unfixed, independent, still-open defect carrying its measured current value"
    - "All three bodies state that the issue stays open pending the reporter's response"
  prohibitions:
    - "Must NOT state or imply that a `w27e257` PASS run exists; the tracker carries two w27e257 issues and both are FAIL, so the general point is conceded without inventing a run"
    - "Must NOT apply or promise the `fix:released` label on gh#28 or gh#31; only the harness fix shipped and the chip defects did not, and the bodies say so explicitly rather than letting a label imply closure"
    - "Must NOT close gh#23, gh#28 or gh#31, and must NOT draft any sentence that reads as a close; a close requires the reporters' confirmation or a superseding PASS"
    - "Must NOT add a report key, an exported field, or any product change disclosing the write-shortcut divergence; the folded todo supplies reply material only and stays pending"
    - "Must NOT imply that editing `chip_database.json` by hand is the fix for any of the three defects; the database is generated and corrections belong in the generator's decode function"
    - "Must NOT post, label or close anything in this plan; every public act is gated in 187-09, 187-10 and 187-11"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/bodies/187-gh23.md"
      provides: "REPLY-01's posting payload"
    - path: ".planning/phases/187-answered-reports/evidence/bodies/187-gh28.md"
      provides: "REPLY-02's posting payload for m27c512"
    - path: ".planning/phases/187-answered-reports/evidence/bodies/187-gh31.md"
      provides: "REPLY-02's posting payload for m27c1001"
    - path: ".planning/phases/187-answered-reports/evidence/187-06-body-hashes.txt"
      provides: "one sha256 line per body, for the three approval gates to bind against"
    - path: ".planning/phases/187-answered-reports/evidence/187-06-draft-link-check.txt"
      provides: "every link in the three drafts, resolved at the pinned SHA"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/bodies/187-gh28.md"
      to: ".planning/phases/187-answered-reports/evidence/187-04-fw-version.txt"
      via: "the firmware version named beside the `firestarter fw --install` step of the re-run ask"
      pattern: "3\\.0\\.0b[0-9]+"
---

<objective>
Draft the three replies to the disputed `dev test` triage threads — gh#23 (w27e257, REPLY-01), gh#28
(m27c512) and gh#31 (m27c1001, both REPLY-02) — to disk.

Purpose: all three reporters answered the 2026-08-08 triage in one or two sentences and two of them
rejected the analysis outright. These replies concede the attribution point in full, keep the datasheet
findings standing as unfixed defects with their measured current values, carry both known caveats into
the re-run asks, and close nothing.

Output: three posting payloads, their link check, their hash record, and three more sections in the
review document.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.planning/todos/pending/2026-09-08-uv-write-shortcut-disclosure-key.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

This plan writes to disk only. It posts nothing, labels nothing and closes nothing. The phase it belongs
to must never be dispatched under `--auto` or `--chain`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft gh#23 — concede the attribution, keep the defect standing, state the limit</name>
  <files>.planning/phases/187-answered-reports/evidence/bodies/187-gh23.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-10 (the three-chip table and the type split), § D-11 (the amendment, its evidence, and the care note on the concession's scope), § D-13 (register), § D-14 (gh#23's label change), § D-17 (the REPLY-05 sentence)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 3 (the measured current values and the alias-key gotcha), § 3.1 (the type split), § Finding 4 (the status-axis trigger, the FAIL title mechanism, the disclosure text, and §4.5's complete tracker search showing no PASS exists)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § the five reply bodies (opening-move, bolded labels, concede-then-hold, and the re-run ask with install block)
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` and `evidence/187-05-body-hashes.txt` (the shape and hash discipline established in 187-05)
    - `.planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt`, `evidence/187-03-app-version.txt`, `evidence/187-04-fw-version.txt`
  </read_first>
  <action>
    Write `evidence/bodies/187-gh23.md` as a bare markdown body — no frontmatter, no title heading, short
    bolded section labels rather than headings. Address AndersBNielsen and resume the 2026-08-09 exchange
    by name.

    Concede in the first two sentences: their diagnosis was correct — a rig fault was reported as a chip
    verdict, and the triage argued against them on that point.

    Then state the limit plainly, because the requirement's original wording would be an overclaim and
    was amended in Plan 187-01 for exactly this reason. Say that the tool did not become able to see an
    unhooked VPP and cannot: the status axis fires only on serial and hardware-operation failures, while
    a mis-wired VPP produces bad data rather than either of those, so a fresh run today would still
    report the write and verify steps as bad and would still file under a FAIL title. Say that
    explicitly rather than leaving the reporter to discover it on a re-run.

    Name what did change, which is the part that answers their actual objection: the misleading
    programming-rail voltage fields were deleted from the report, and a disclosure sentence was added
    stating that those readings measure the regulator rail only and do not show whether the socket is
    connected. Quote the disclosure sentence as shipped.

    Restate the datasheet finding as an unfixed, independent, still-open defect with its measured
    current value: the W27E257 row still carries a programming voltage of 13.5 V where the datasheet
    gives 12 V. Say it is not retracted and not quietly dropped, that no other record tracks it, and that
    when it is fixed the fix belongs in the database generator's decode function and never in the
    generated JSON, which is regenerated wholesale.

    Note the one thing that splits this issue from the other two: W27E257 is an EEPROM, not a UV-EPROM,
    so the UV-slot work of the preceding milestone does not apply here.

    Observe the care note on the concession's scope. The reporter wrote that the second one actually
    passed. Concede the general point — a rig fault was reported as a chip verdict — and do not invent,
    name, link or imply a w27e257 run that passed. The tracker carries exactly two w27e257 issues and
    both are failures; the folded sibling is a blank-check failure, not a pass.

    Name the label change and the reason in the body per D-14: `cause:rig` is being added alongside the
    standing `cause:database`, because the database defect is real and unfixed so that label was never
    wrong, and `needs:report` is being added because the thread is waiting on a fresh run. Never
    reclassify silently.

    Ask for the re-run, and put the REPLY-05 sentence with the ask, in the same bolded section or
    immediately adjacent to it — not in a later section the reporter would reach only after running.
    State that reports are now at schema version 2.0 and that the deduplication fingerprint was
    deliberately re-keyed, so a fresh run will not group with the old one and that non-grouping is
    expected rather than a new defect. Give the install steps naming the exact app version from line 1 of
    `evidence/187-03-app-version.txt` and the exact firmware version from line 1 of
    `evidence/187-04-fw-version.txt`, with `pip install --pre -U firestarter` rather than a plain
    install, because the index's latest stable release is a 2.x host that none of this work is in.

    Say that the issue stays open pending their response. Do not draft any sentence that reads as a
    close.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/bodies/187-gh23.md | /usr/bin/grep -E '^(---|#)' | wc -l</automated>
    <fails_when>prints anything other than 0 — the body opens with frontmatter or a heading instead of the first sentence the reporter reads</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -F "$(head -1 evidence/187-03-app-version.txt)" evidence/bodies/187-gh23.md | wc -l</automated>
    <fails_when>prints 0 — the body does not name the app version that was actually cut</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -F "$(head -1 evidence/187-04-fw-version.txt)" evidence/bodies/187-gh23.md | wc -l</automated>
    <fails_when>prints 0 — the body does not name the firmware version that was actually cut</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep '13' evidence/bodies/187-gh23.md | /usr/bin/grep -i 'v' | wc -l</automated>
    <fails_when>prints 0 — the measured programming-voltage value the defect turns on is absent from the body</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -i 'schema' evidence/bodies/187-gh23.md | wc -l</automated>
    <fails_when>prints 0 — the REPLY-05 sentence is missing from a body that asks for a re-run</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -E 'blob/(beta|main)/' evidence/bodies/187-gh23.md | wc -l</automated>
    <fails_when>prints anything other than 0 — a branch link was used instead of a commit-SHA permalink</fails_when>
    <automated>gh issue list --repo henols/firestarter_prom --state all --search "w27e257" --json number,title --jq '.[] | select(.title | test("PASS")) | .number' | wc -l</automated>
    <fails_when>prints anything other than 0 — a w27e257 PASS issue now exists and the care note on the concession's scope must be re-derived before drafting</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/bodies/187-gh23.md` begins with prose, not with `---` and not with `#`.
    - The body names both cut versions byte-exactly, carries the shipped disclosure sentence as a quotation, and states the W27E257 programming-voltage defect with its measured current value and its correct fix locus.
    - The body contains the REPLY-05 schema-version and re-key statement, and that statement is in the same bolded section as the re-run ask or immediately adjacent to it.
    - The body names `cause:rig` and `needs:report` and gives the reason for each.
    - The body states the issue stays open pending the reporter's response.
    - No body text names, links or implies a w27e257 run that passed, and the live tracker search for a w27e257 PASS still returns nothing.
    - The body contains no `blob/beta/` or `blob/main/` link.
  </acceptance_criteria>
  <done>gh#23's reply concedes the reporter's point in full, states the limit the tool genuinely has, keeps the unfixed defect on the record, and asks for a re-run with both caveats attached.</done>
</task>

<task type="auto">
  <name>Task 2: Draft gh#28 and gh#31 as two separate replies with the write-shortcut caveat</name>
  <files>.planning/phases/187-answered-reports/evidence/bodies/187-gh28.md, .planning/phases/187-answered-reports/evidence/bodies/187-gh31.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-10 (M27C512 and M27C1001 rows, and that both are UV-EPROM so the UV slot work does apply), § D-12 (the gh#28/gh#31 caveat), § D-14 (add `needs:report`, withhold `fix:released` and say so), § D-15 and § Folded Todos
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 3 (the measured values and the alias-key gotcha: these two rows live under comma-joined alias keys and a bare part-number grep finds nothing), § Finding 9.4 (the folded todo's measurement verified in the shipped code), § Finding 2 (each issue's current labels and its last reporter comment)
    - `.planning/todos/pending/2026-09-08-uv-write-shortcut-disclosure-key.md`
    - `.planning/phases/187-answered-reports/evidence/bodies/187-gh23.md` (the shape just established)
  </read_first>
  <action>
    Write two separate bodies, `evidence/bodies/187-gh28.md` and `evidence/bodies/187-gh31.md`. They
    share a shape but they are two replies to two reporters about two different chips on two different
    threads: each names its own chip, its own defect, its own current labels and its own verdict history.
    Do not write one body and post it twice, and do not let either body describe the other chip's defect
    as if it were this one's.

    For both: address the reporter and resume the 2026-08-09 exchange. Concede the attribution point in
    the first two sentences — the objection was about how a failure was attributed and reported, and that
    is what changed. Name the shipped changes that bear on that specific issue. Both parts are UV-EPROMs,
    so the UV-slot work of the preceding milestone does apply to them, unlike gh#23.

    Restate each chip's own 2026-08-08 datasheet finding as an unfixed, independent, still-open defect
    with its measured current value. For gh#28's M27C512 that is the programming supply reading 6.5 V
    against a 5 V operating supply with nothing applying it. For gh#31's M27C1001 that is pin 30 mapped
    as an address line where the datasheet gives no connection, because the row still points at the
    32-pin 27C020 pin map. Say for each that it is not retracted, that no other record tracks it, and
    that the fix belongs in the database generator's decode function rather than in the generated JSON.
    Note in the SUMMARY, not in the body, that both rows live under comma-joined alias keys in the
    generated database, so a verification grep for the bare part number finds nothing.

    Carry D-12's caveat into the re-run ask in both bodies, and place it before or within the ask rather
    than after it. State it in the reporter's terms: the validation harness now gets a non-blank UV part
    through by skipping the blank check, on a path the user-facing write command does not take, so the
    identical firmware still refuses a plain write on the identical part — which means a PASS on a
    re-run does not mean writing works on their chip. Say that plainly rather than letting the verdict
    imply more than it covers. Do not propose, design or promise a report field disclosing this; the
    question of an exported key stays open elsewhere and this phase adds no report key.

    Say explicitly why the `fix:released` label is being withheld: only the harness and reporting fix
    shipped, and the chip defects did not, so a label claiming a released fix would imply a closure that
    has not happened. Name the label actually being added, `needs:report`, and why.

    Ask for the re-run, and put the REPLY-05 sentence with the ask, in the same bolded section or
    immediately adjacent to it. State that reports are now at schema version 2.0 and that the
    deduplication fingerprint was deliberately re-keyed, so a fresh run will not group with the old one
    and that non-grouping is expected rather than a new defect. Give the install steps naming the exact
    app version from line 1 of `evidence/187-03-app-version.txt` and the exact firmware version from
    line 1 of `evidence/187-04-fw-version.txt`, with `pip install --pre -U firestarter`.

    State in both that the issue stays open pending the reporter's response. Draft no sentence that reads
    as a close.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md | cut -d' ' -f1 | sort -u | wc -l</automated>
    <fails_when>prints anything other than 2 — the two bodies are byte-identical, which means one reply was written and duplicated across two issues</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -il 'm27c512' evidence/bodies/187-gh28.md | wc -l</automated>
    <fails_when>prints 0 — gh#28's body does not name its own chip</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -il 'm27c1001' evidence/bodies/187-gh31.md | wc -l</automated>
    <fails_when>prints 0 — gh#31's body does not name its own chip</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -il 'm27c1001' evidence/bodies/187-gh28.md | wc -l</automated>
    <fails_when>prints anything other than 0 — gh#28's body describes the other issue's chip</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -il 'm27c512' evidence/bodies/187-gh31.md | wc -l</automated>
    <fails_when>prints anything other than 0 — gh#31's body describes the other issue's chip</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'fix:released' evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md</automated>
    <fails_when>prints a `:0` count for either file — each body must name the withheld label and say why it is withheld</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -il 'schema' evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md | wc -l</automated>
    <fails_when>prints a number below 2 — a body that asks for a re-run lacks the REPLY-05 statement</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && for F in evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md; do /usr/bin/grep -qF "$(head -1 evidence/187-03-app-version.txt)" "$F" || echo "MISSING-APP $F"; /usr/bin/grep -qF "$(head -1 evidence/187-04-fw-version.txt)" "$F" || echo "MISSING-FW $F"; done | wc -l</automated>
    <fails_when>prints anything other than 0 — a body omits one of the two versions read after the cuts</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -E 'blob/(beta|main)/' evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md | wc -l</automated>
    <fails_when>prints anything other than 0 — a branch link was used instead of a commit-SHA permalink</fails_when>
  </verify>
  <acceptance_criteria>
    - The two bodies have different sha256 values, each names only its own chip, and neither mentions the other's.
    - Each body names both cut versions byte-exactly and carries the REPLY-05 statement in the same bolded section as its re-run ask or immediately adjacent to it.
    - Each body carries the write-shortcut caveat at or before the re-run ask, in reporter-facing terms, and proposes no report field for it.
    - Each body names the withheld `fix:released` label and states why it is withheld, and names `needs:report` and why it is added.
    - Each body restates its own chip's defect with its measured current value and names the generator's decode function as the fix locus.
    - Each body states the issue stays open pending the reporter's response.
    - Neither body contains a `blob/beta/` or `blob/main/` link.
  </acceptance_criteria>
  <done>Two distinct replies exist, each about its own chip, each carrying the caveat that a PASS on a re-run would not mean the user-facing write works.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve links, hash-bind the three bodies, extend the review document</name>
  <files>.planning/phases/187-answered-reports/evidence/187-06-draft-link-check.txt, .planning/phases/187-answered-reports/evidence/187-06-body-hashes.txt, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § `187-UPSTREAM-REPLIES.md` (section order and the per-issue section shape) and § Permalinks
    - `.planning/phases/187-answered-reports/evidence/187-05-draft-link-check.txt` (the format to follow)
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` as created by 187-05
  </read_first>
  <action>
    Extract every URL from the three new bodies and resolve each one into
    `evidence/187-06-draft-link-check.txt`, in the same format Plan 187-05 used: the URL, the method, the
    result, and for each in-repository permalink the pinned SHA it was resolved at and a confirmation
    that the decoded content contains the heading the anchor targets. Confirm every issue cross-link
    resolves and record its current state.

    Write one `sha256(<path>) = <hash>` line per body into `evidence/187-06-body-hashes.txt`, computed
    over the bodies exactly as they stand. These are the values the per-issue approval gates in Plans
    187-09, 187-10 and 187-11 bind against. If a body is amended at its gate, the hash is recomputed
    after the amendment and before the post, never before.

    Extend `187-UPSTREAM-REPLIES.md` with one `## gh#N` section each for 23, 28 and 31, in the same shape
    187-05 used: the body file path, a Posted line left blank, and the body reproduced inline
    byte-identically to its file. Confirm the `## Dispositions` table already covers all five issues with
    the disposition each reply implies, and that the three new status lines still read the pending
    literal.

    Record in the review document, per body, that it carries a re-run ask and therefore requires the
    REPLY-05 statement — the counterpart of the note 187-05 made for the two bodies that carry no ask.

    Post nothing. Change no label. Close nothing.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'sha256(' evidence/187-06-body-hashes.txt</automated>
    <fails_when>prints anything other than 3 — one hash line per drafted body is required</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && for F in evidence/bodies/187-gh23.md evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md; do sha256sum "$F" | cut -d' ' -f1 | /usr/bin/grep -qF -f - evidence/187-06-body-hashes.txt || echo "HASH-MISMATCH $F"; done | wc -l</automated>
    <fails_when>prints anything other than 0 — a recorded hash does not match the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -o 'https://[^) ]*' evidence/bodies/187-gh23.md evidence/bodies/187-gh28.md evidence/bodies/187-gh31.md | sed 's/^[^:]*://' | sort -u | while read -r U; do /usr/bin/grep -qF "$U" evidence/187-06-draft-link-check.txt || echo "UNRESOLVED $U"; done | wc -l</automated>
    <fails_when>prints anything other than 0 — a URL appears in a body but has no entry in the link-check file</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c '^## gh#' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 5 — the review document does not carry one section per replied-to issue</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 5 — a status line was flipped before its issue's own posting plan ran</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && ls evidence/bodies/ | wc -l</automated>
    <fails_when>prints anything other than 5 — the payload set is incomplete or carries a stray file</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-06-body-hashes.txt` contains exactly three `sha256(<path>) = <hash>` lines, each matching `sha256sum` of the body on disk.
    - Every `https://` URL in the three bodies appears in `evidence/187-06-draft-link-check.txt` with its method and result.
    - `187-UPSTREAM-REPLIES.md` contains exactly five `## gh#` sections and exactly five pending status lines.
    - `evidence/bodies/` contains exactly five files.
    - The review document records, per body, whether it carries a re-run ask.
  </acceptance_criteria>
  <done>All five posting payloads exist, are hash-bound, and are mirrored byte-identically in the review document with every status line still pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted body on disk → the public tracker | Three reporters who already rejected one analysis will read these verbatim; a claim that overstates what shipped is the exact failure this milestone exists to correct. |
| a verdict label → a reporter's conclusion | A `fix:released` label on an issue whose chip defect is unfixed would communicate a closure that has not happened, without a sentence ever saying so. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-27 | Repudiation | gh#23's concession scope | high | mitigate | The prohibition names the invented-PASS failure; a live tracker search for a w27e257 PASS is an automated leg; the body concedes the general point without naming a run |
| T-187-28 | Spoofing | a single body posted to two issues | high | mitigate | The two bodies' sha256 values are asserted distinct, and each is asserted to name only its own chip and not the other's |
| T-187-29 | Repudiation | an implied closure via `fix:released` | high | mitigate | The label is a named prohibition and each body must name it and state why it is withheld; the applied label set is re-asserted at each posting gate |
| T-187-30 | Repudiation | a re-run PASS read as proof the write path works | high | mitigate | The D-12 caveat is a `must_haves` truth positioned at or before the ask, and the review document records per body that the ask exists |
| T-187-31 | Spoofing | a predicted version in a re-run instruction | high | mitigate | Both versions are grep-matched byte-exactly against the two cut-version files in every body |
| T-187-32 | Tampering | adding a report key for the write-shortcut divergence | medium | mitigate | Named prohibition; the folded todo supplies reply material only and stays pending |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*`, `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| `evidence/bodies/187-gh60.md`, `187-gh62.md`, `evidence/187-05-*`, `187-UPSTREAM-REPLIES.md` header and first two sections | 187-05 |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md` | **(this plan)** |
| `evidence/187-06-draft-link-check.txt`, `evidence/187-06-body-hashes.txt` | **(this plan)** |
| `187-UPSTREAM-REPLIES.md` sections for gh#23, gh#28, gh#31 | **(this plan)** |
| one operator-approval file and one post transcript per posted issue | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-01 and REPLY-06 are `unclassified` spec-less probe rows and stay unresolved; the full 11-row
  accounting is in `187-01-PLAN.md`. REPLY-06 is additionally enforced as a prohibition here and by
  state assertions in 187-12, but the probe row itself remains unresolved rather than auto-resolved.
- The REPLY-02 and REPLY-05 ordering edges are authored as `verification: backstop` truths because
  "the caveat sits with the ask" is a placement judgement a command cannot decide; the verifier abstains
  rather than passing them silently, and the operator sees the placement at each gate.

<verification>
- Three distinct bodies exist, each naming only its own chip, each with its own sha256.
- Each carries both cut versions byte-exactly, the REPLY-05 statement with its ask, and its caveat at or before the ask.
- Each states the issue stays open; none drafts a close.
- Five payload files and five pending status lines exist.
- Nothing was posted, labelled or closed.
</verification>

<success_criteria>
Three replies exist on disk that concede what the reporters were right about, keep three unfixed defects
on the public record, and ask for a re-run with every caveat the project knows attached to it.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-06-SUMMARY.md` when done. Record the three body hashes,
the versions named, the alias-key note about the generated database, and the label decisions being
carried to the three operator gates.
</output>
