---
phase: 187-answered-reports
plan: 08
type: execute
wave: 8
depends_on: ["187-07"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/187-08-gh62-operator-approval.txt
  - .planning/phases/187-answered-reports/evidence/187-08-post-transcript.txt
  - .planning/phases/187-answered-reports/evidence/187-08-comment-id.txt
autonomous: false
requirements: [REPLY-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The comment posted to gh#62 is byte-identical to `evidence/bodies/187-gh62.md` as approved, proven by a JSON round trip that exits non-zero on mismatch"
    - "`evidence/187-08-gh62-operator-approval.txt` exists, carries the approval literal on line 1, records the operator's decision on the gh#65 and gh#66 acknowledgement wording, and its recorded sha256 equals the sha256 of the body that was actually posted"
    - "gh#62 is still OPEN after this plan, and its label set is exactly the set the operator approved at the gate"
    - "gh#60's state and comment count are unchanged by this plan, and gh#23, gh#28 and gh#31 are untouched"
  prohibitions:
    - "Must NOT close gh#62; its refusal is correct behaviour but the capability the reporter wants is unbuilt, and closing would file a correct refusal as the end of the story"
    - "Must NOT post any body whose sha256 differs from the one recorded in the approval file; an amendment at the gate is applied first and the hash recomputed before the post"
    - "Must NOT verify the posted comment with `gh api --jq '.body'` piped into `diff`; that form reports a spurious mismatch on a provably identical comment"
    - "Must NOT touch gh#23, gh#28, gh#31, gh#60 or gh#9 in this plan; approval for gh#62 carries to no other issue"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 1 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-08-gh62-operator-approval.txt"
      provides: "the per-issue approval literal, the label decision, the acknowledgement-wording decision, and the sha256 of the body as approved"
      contains: "APPROVED-FOR-POST: gh62"
    - path: ".planning/phases/187-answered-reports/evidence/187-08-post-transcript.txt"
      provides: "the command, exit code and returned URL for the post, plus the label edits"
    - path: ".planning/phases/187-answered-reports/evidence/187-08-comment-id.txt"
      provides: "the posted comment id alone on line 1, for the byte-identity round trip"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-08-gh62-operator-approval.txt"
      to: ".planning/phases/187-answered-reports/evidence/bodies/187-gh62.md"
      via: "a sha256 line binding the approval to the exact bytes posted"
      pattern: "sha256\\("
---

<objective>
Post REPLY-03 to gh#62, apply the labels the operator approves, and leave the issue open — each act
gated by a blocking operator checkpoint that approves this issue and no other.

Purpose: the reporter is told they were right, and so is the classification. Both, at once. The reply
carries the erase verdict's four load-bearing claims in a user's register, names the unbuilt capability
and where it is tracked, and cross-links the protocol defect that bites the same part. The issue stays
open because closing it would file a correct refusal as the end of the story.

Output: one posted comment, one label set applied to an issue that had none, and the issue still open.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.claude/skills/devtest-triage/SKILL.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve the exact body to post to gh#62, the exact label set to apply, and the wording of the acknowledgement to the reporter's other open issues</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/bodies/187-gh62.md` in full — the exact bytes being approved
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` — the dispositions table and this issue's status line
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-14, § D-15 and § D-16
    - `.claude/skills/devtest-triage/SKILL.md` — the label taxonomy, for the candidates presented
  </read_first>
  <context>
    D-16 requires a separate blocking checkpoint immediately before each public act. The gh#60 approval
    from Plan 187-07 carries nothing here. This gate approves gh#62 only.

    Show the operator four things, and ask for a decision on each:

    1. The full verbatim contents of `evidence/bodies/187-gh62.md`, with its sha256 from
       `evidence/187-05-body-hashes.txt`. This is the exact file passed to `--body-file` unmodified.
    2. The concrete label set. gh#62 carries zero labels today, so D-14's instruction to label it to match
       its disposition means adding the first labels this issue has ever had. The taxonomy is a `dev test`
       taxonomy and gh#62 is a plain bug report, so no label fits cleanly. Present the two defensible
       candidates and their grounds — `cause:firmware`, on the reading that the missing chip-erase
       capability is a firmware gap, and no label at all, on the reading that the refusal is correct
       behaviour and not a defect — present "no label change" as an equally available answer, and take
       the operator's list verbatim. D-14 forbids silent reclassification by name, so do not choose here.
    3. The acknowledgement of gh#65 and gh#66, and the drift behind it. CONTEXT.md's Specific Ideas block
       asked for a one-line acknowledgement describing them as unanswered; both received a maintainer
       datasheet cross-check on 2026-09-10, so that description is now false. The drafted line says they
       are open, with no timeline promised. Show the operator the drafted line, state the drift plainly,
       and ask whether the line survives at all, survives as drafted, or is reworded. Three of this
       reporter's four issues remain open, which is still true.
    4. That the body links `.planning/notes/ae29f2008-classification-verdict.md` at the one pinned meta
       merge SHA with the anchor copied from the rendered page, names the app version read after the cut,
       names backlog 999.63 with its boot-block caveat, and cross-links gh#68 with its own scope rather
       than the separate page-size defect's.

    Confirm explicitly that gh#62 is not being closed, and why: the refusal is correct, but the software
    chip-erase the reporter actually wants is real work that has not been done.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    If the operator amends any wording, apply it to `evidence/bodies/187-gh62.md` and to the inline copy
    in `187-UPSTREAM-REPLIES.md` first, save a unified diff of the amendment against a scratchpad copy of
    the pre-approval draft beside the body, then recompute the sha256 and write the approval file.

    Write `evidence/187-08-gh62-operator-approval.txt` with the literal `APPROVED-FOR-POST: gh62` alone
    on line 1, then the operator's name and date, then the decision prose explicitly labelled as the
    orchestrator's rendering of a menu selection plus its option description rather than a verbatim
    operator quotation, then the approved label list, then the acknowledgement decision, then a single
    `sha256(evidence/bodies/187-gh62.md) = <hash>` line.
  </context>
  <options>
    <option id="approve">
      <name>Approve as drafted, with the presented label set and the re-worded acknowledgement</name>
      <pros>The reporter gets a direct answer that concedes their point, names the unbuilt capability honestly, and warns them about the related protocol defect</pros>
      <cons>Public and permanent the moment it posts</cons>
    </option>
    <option id="amend">
      <name>Approve with wording, label or acknowledgement amendments</name>
      <pros>The operator's exact wording ships; the amendment's scope is recorded as a diff</pros>
      <cons>The body, its inline copy and its hash must all be updated before the post</cons>
    </option>
    <option id="drop-ack">
      <name>Approve the body with the gh#65 and gh#66 acknowledgement line removed</name>
      <pros>Avoids restating anything about two issues that already have their own answers</pros>
      <cons>The reporter filed four issues in one day and gets no signal that the remaining ones are seen</cons>
    </option>
    <option id="hold">
      <name>Do not post yet</name>
      <pros>Nothing is public</pros>
      <cons>REPLY-03 stays undelivered</cons>
    </option>
  </options>
  <resume-signal>Select: approve, amend (and state the amendment), drop-ack, or hold</resume-signal>
  <acceptance_criteria>
    - `evidence/187-08-gh62-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-POST: gh62`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection plus its option description rather than a verbatim operator quotation.
    - The file carries exactly one `sha256(evidence/bodies/187-gh62.md) = <hash>` line, and that hash equals `sha256sum` of the body on disk at the moment the file was written.
    - The file records the approved label list verbatim.
    - If the operator amended the wording, the amendment was applied to both the body file and its inline copy, a unified diff of it was saved beside the body, and the hash was recomputed after the amendment.
    - Nothing was posted before this file existed.
    - The file records the operator's decision on the gh#65 and gh#66 acknowledgement wording.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Post to gh#62, prove byte-identity, apply the labels, leave it open</name>
  <reversibility rating="one-way">A comment on a public issue is visible to every watcher the instant it posts and is delivered by email; it can be edited but never un-seen.</reversibility>
  <precondition>`evidence/187-08-gh62-operator-approval.txt` exists, its first line is exactly `APPROVED-FOR-POST: gh62`, and its recorded sha256 equals `sha256sum evidence/bodies/187-gh62.md`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-08-post-transcript.txt, .planning/phases/187-answered-reports/evidence/187-08-comment-id.txt, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.3 and § Finding 6.4 (the posting command and the byte-identity trap)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Posting — the command, and the read-back that does not false-alarm
    - `.planning/phases/187-answered-reports/187-07-SUMMARY.md` (the discipline just executed on gh#60)
    - `.planning/phases/187-answered-reports/evidence/187-08-gh62-operator-approval.txt`
  </read_first>
  <action>
    Re-assert the gate mechanically: recompute `sha256sum evidence/bodies/187-gh62.md` and confirm it
    equals the hash recorded in the approval file. If it differs, stop — the body changed after approval.

    Post with `gh issue comment 62 --repo henols/firestarter_prom --body-file evidence/bodies/187-gh62.md`.
    Pass the file; never interpolate the body onto the command line. Capture the returned URL, write the
    comment id from its trailing fragment alone on line 1 of `evidence/187-08-comment-id.txt`, and write
    a transcript block into `evidence/187-08-post-transcript.txt` carrying the command, the exit code and
    the returned URL.

    Prove byte-identity by reading the comment back through the API as JSON and comparing the decoded
    body against the file with a python round trip that exits non-zero on mismatch. Do not use
    `gh api --jq '.body'` piped into `diff`. Record both lengths and both hashes in the transcript.

    Apply exactly the label list the operator approved, with
    `gh issue edit 62 --repo henols/firestarter_prom --add-label "<label>"` per label, or apply none if
    that was the decision. Add nothing the operator did not name. Record each command and exit code.

    Do not close the issue. Re-read gh#62 and record its state, stateReason and resulting label set; the
    state must still be OPEN.

    Flip gh#62's status line in `187-UPSTREAM-REPLIES.md` from the pending literal to a posted state and
    fill its Posted line with the returned comment URL, noting that the issue was deliberately left open.
    Leave the three remaining status lines pending.

    Touch no other issue.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/187-08-gh62-operator-approval.txt</automated>
    <fails_when>prints anything other than `APPROVED-FOR-POST: gh62`</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh62.md | cut -d' ' -f1 | /usr/bin/grep -qF -f - evidence/187-08-gh62-operator-approval.txt && echo BOUND</automated>
    <fails_when>prints nothing, or non-zero exit — the approval file's hash does not cover the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && gh api "repos/henols/firestarter_prom/issues/comments/$(head -1 evidence/187-08-comment-id.txt)" > /tmp/c187gh62.json && python3 -c "import json,hashlib,sys; b=json.load(open('/tmp/c187gh62.json'))['body'].encode(); d=open('evidence/bodies/187-gh62.md','rb').read(); print('api',len(b),hashlib.sha256(b).hexdigest()); print('disk',len(d),hashlib.sha256(d).hexdigest()); print('BYTE-IDENTICAL' if b==d else 'MISMATCH'); sys.exit(0 if b==d else 1)"</automated>
    <fails_when>prints MISMATCH, or non-zero exit</fails_when>
    <automated>gh issue view 62 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN — gh#62 was closed, which D-15 forbids</fails_when>
    <automated>gh issue view 62 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 1 — gh#62 carried zero comments before this plan</fails_when>
    <automated>gh issue view 60 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 1 — a second comment was added to gh#60 by this plan</fails_when>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 3 — gh#23 carried three comments before this phase and this plan must not add one</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 2 — gh#31 carried two comments before this phase and this plan must not add one</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 3 — either gh#62's line was not flipped, or another issue's line was flipped early</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-08-gh62-operator-approval.txt` line 1 is exactly `APPROVED-FOR-POST: gh62`, and its sha256 line matches `sha256sum evidence/bodies/187-gh62.md`.
    - The API-read comment body is byte-identical to the file; the round trip prints `BYTE-IDENTICAL` and exits 0.
    - `gh issue view 62 --json state` returns `OPEN`, and the issue carries exactly one comment.
    - gh#62's label set equals exactly the list recorded in the approval file, which may be empty if that was the decision.
    - gh#60 still carries exactly one comment; gh#23 still carries three; gh#31 still carries two.
    - `187-UPSTREAM-REPLIES.md` has exactly three pending status lines remaining, and gh#62's Posted line carries the comment URL and notes the deliberate non-close.
    - The approval file records the operator's decision on the gh#65 and gh#66 acknowledgement wording.
  </acceptance_criteria>
  <done>REPLY-03 is delivered, provably as the operator approved it; gh#62 is labelled as the operator directed and remains open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved body on disk → public issue comment | The moment of publication; visible to every watcher and delivered by email. |
| issue state → the reporter's understanding | A close would communicate that the story ended, while the capability they want is unbuilt. State is a claim. |
| this issue → the other five | An over-broad invocation could touch an issue this gate never approved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-38 | Repudiation | posting a body the operator never approved | critical | mitigate | Per-issue approval literal plus sha256 binding as the task's `<precondition>`, re-asserted mechanically before the post, with a byte-identity read-back after |
| T-187-39 | Elevation of Privilege | closing gh#62 | high | mitigate | Closing is a named prohibition; the post-plan state is asserted `OPEN` by an automated leg; the gate states the reason for leaving it open |
| T-187-40 | Repudiation | a false claim that gh#65 and gh#66 are unanswered | high | mitigate | Their answered state was re-measured in 187-05, the drafted line says open rather than unanswered, and the drift is put in front of the operator at this gate for an explicit decision |
| T-187-41 | Tampering | a silent reclassification on an issue that had no labels | high | mitigate | Both label candidates and their grounds are presented at the gate with "no change" as an equal option; the applied set is compared against the approval file |
| T-187-42 | Tampering | a false mismatch from the naive read-back | high | mitigate | The naive form is a named prohibition; only the JSON round trip is permitted |
| T-187-43 | Elevation of Privilege | auto-mode approving the gate | critical | mitigate | `gate="blocking-human"`, the never-dispatch constraint stated in plan text, and an explicit mode confirmation at the checkpoint |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*`, `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| the five `evidence/bodies/187-gh*.md` payloads, both link checks, both hash files, `187-UPSTREAM-REPLIES.md` | 187-05, 187-06 |
| the gh#60 approval file, transcript, comment-id and window-start files | 187-07 |
| `evidence/187-08-gh62-operator-approval.txt`, `evidence/187-08-post-transcript.txt`, `evidence/187-08-comment-id.txt` | **(this plan)** |
| the gh#23, gh#28 and gh#31 approval files, transcripts and comment-id files | 187-09, 187-10, 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-03 is an `unclassified` spec-less probe row and stays unresolved; the full 11-row accounting is
  in `187-01-PLAN.md`.
- The label set for gh#62 is `[ASSUMED]` per RESEARCH § Assumptions Log A3 and is resolved by the
  operator at this gate.
- RESEARCH Open Question 2 — whether the gh#65 and gh#66 acknowledgement survives at all given the drift
  — is carried to this gate as an explicit option rather than decided by the plan.

<verification>
- The approval file exists with its literal, its label decision, its acknowledgement decision and a hash covering the posted body.
- The posted comment reads back byte-identical.
- gh#62 is OPEN with exactly one comment and exactly the approved label set.
- gh#60, gh#23 and gh#31 comment counts unchanged.
- Three pending status lines remain in the review document.
</verification>

<success_criteria>
The reporter is told plainly that they were right and so was the classification, is pointed at the
protocol defect that affects the same part, knows where the capability they want is tracked, and their
issue is still open.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-08-SUMMARY.md` when done. Record the comment URL, both
hashes from the round trip, the applied label set, the acknowledgement decision, and the confirmation
that the issue remains open.
</output>
