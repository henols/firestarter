---
phase: 187-answered-reports
plan: 10
type: execute
wave: 10
depends_on: ["187-09"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/187-10-gh28-operator-approval.txt
  - .planning/phases/187-answered-reports/evidence/187-10-post-transcript.txt
  - .planning/phases/187-answered-reports/evidence/187-10-comment-id.txt
autonomous: false
requirements: [REPLY-02, REPLY-05, REPLY-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The comment posted to gh#28 is byte-identical to `evidence/bodies/187-gh28.md` as approved, proven by a JSON round trip that exits non-zero on mismatch"
    - "gh#28 is still OPEN after this plan, and its label set is exactly the pre-existing set plus `needs:report`"
    - "gh#28 does not carry `fix:released` after this plan, and the posted body states why that label is withheld"
    - "The posted body carries the REPLY-05 statement with its re-run ask, so the reporter is told before running that a fresh report will not group with the old one"
  prohibitions:
    - "Must NOT close gh#28; a close requires the reporter's confirmation or a superseding PASS"
    - "Must NOT apply `fix:released` to gh#28; only the harness and reporting fix shipped and the chip defect did not, so that label would imply a closure that has not happened"
    - "Must NOT remove any pre-existing label from gh#28; the label change is additive"
    - "Must NOT post any body whose sha256 differs from the one recorded in the approval file"
    - "Must NOT verify the posted comment with `gh api --jq '.body'` piped into `diff`; that form reports a spurious mismatch on a provably identical comment"
    - "Must NOT touch gh#23, gh#31, gh#60, gh#62 or gh#9 in this plan; approval for gh#28 carries to no other issue"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 1 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-10-gh28-operator-approval.txt"
      provides: "the per-issue approval literal, the label decision including the withheld label, and the sha256 of the body as approved"
      contains: "APPROVED-FOR-POST: gh28"
    - path: ".planning/phases/187-answered-reports/evidence/187-10-post-transcript.txt"
      provides: "the command, exit code and returned URL for the post, plus the label edit"
    - path: ".planning/phases/187-answered-reports/evidence/187-10-comment-id.txt"
      provides: "the posted comment id alone on line 1"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-10-gh28-operator-approval.txt"
      to: ".planning/phases/187-answered-reports/evidence/bodies/187-gh28.md"
      via: "a sha256 line binding the approval to the exact bytes posted"
      pattern: "sha256\\("
---

<objective>
Post REPLY-02's m27c512 reply to gh#28, add `needs:report`, withhold `fix:released` deliberately, and
leave the issue open.

Purpose: the reporter objected to how their failure was attributed, and that is what changed. The reply
concedes it, keeps the chip's own unfixed defect on the record, and carries the caveat that a PASS on a
re-run would not mean the user-facing write command works on their part — because the validation harness
reaches its result by a path that command does not take.

Output: one posted comment, an additive label change, a deliberately withheld label, and an issue still
open.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.claude/skills/devtest-triage/SKILL.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve the exact body to post to gh#28 and confirm the label decision, including the label being withheld</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/bodies/187-gh28.md` in full — the exact bytes being approved
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` — the dispositions table and this issue's status line
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-14, § D-15 and § D-16
    - `.claude/skills/devtest-triage/SKILL.md` — the label taxonomy, for the candidates presented
  </read_first>
  <context>
    D-16 requires a separate blocking checkpoint immediately before each public act. The three approvals
    already given in this phase carry nothing here. This gate approves gh#28 only.

    Show the operator four things:

    1. The full verbatim contents of `evidence/bodies/187-gh28.md`, with its sha256 from
       `evidence/187-06-body-hashes.txt`. This is the exact file passed to `--body-file` unmodified.
    2. The label decision, which D-14 fixes: add `needs:report`, because the thread waits on a fresh run,
       and withhold `fix:released`, because only the harness and reporting fix shipped while the chip
       defect did not. Confirm that the body states the withholding explicitly rather than letting the
       absence of a label speak for itself.
    3. The caveat the reply carries into the re-run ask, so the operator can judge its placement: that
       the validation harness now passes a non-blank part by skipping the blank check on a path the
       user-facing write command does not take, so the identical firmware still refuses a plain write on
       the identical part, and a PASS therefore does not mean writing works on their chip.
    4. That the issue is not being closed, and will not be until the reporter confirms or a superseding
       PASS exists.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    If the operator amends any wording, apply it to `evidence/bodies/187-gh28.md` and to the inline copy
    in `187-UPSTREAM-REPLIES.md` first, save a unified diff of the amendment against a scratchpad copy of
    the pre-approval draft beside the body, then recompute the sha256 and write the approval file.

    Write `evidence/187-10-gh28-operator-approval.txt` with the literal `APPROVED-FOR-POST: gh28` alone
    on line 1, then the operator's name and date, then the decision prose explicitly labelled as the
    orchestrator's rendering of a menu selection plus its option description rather than a verbatim
    operator quotation, then the approved label list and the withheld label named as withheld, then a
    single `sha256(evidence/bodies/187-gh28.md) = <hash>` line.
  </context>
  <options>
    <option id="approve">
      <name>Approve as drafted, adding `needs:report` and withholding `fix:released`</name>
      <pros>The reporter is told what actually changed, what did not, and what a PASS on a re-run would and would not prove</pros>
      <cons>Public and permanent the moment it posts</cons>
    </option>
    <option id="amend">
      <name>Approve with wording or label amendments</name>
      <pros>The operator's exact wording ships; the amendment's scope is recorded as a diff</pros>
      <cons>The body, its inline copy and its hash must all be updated before the post</cons>
    </option>
    <option id="hold">
      <name>Do not post yet</name>
      <pros>Nothing is public</pros>
      <cons>REPLY-02 stays half-delivered, with gh#31 still waiting behind it</cons>
    </option>
  </options>
  <resume-signal>Select: approve, amend (and state the amendment), or hold</resume-signal>
  <acceptance_criteria>
    - `evidence/187-10-gh28-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-POST: gh28`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection plus its option description rather than a verbatim operator quotation.
    - The file carries exactly one `sha256(evidence/bodies/187-gh28.md) = <hash>` line, and that hash equals `sha256sum` of the body on disk at the moment the file was written.
    - The file records the approved label list verbatim.
    - If the operator amended the wording, the amendment was applied to both the body file and its inline copy, a unified diff of it was saved beside the body, and the hash was recomputed after the amendment.
    - Nothing was posted before this file existed.
    - The file names `fix:released` explicitly as withheld, with the reason.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Post to gh#28, prove byte-identity, add `needs:report`, leave it open</name>
  <reversibility rating="one-way">A comment on a public issue is visible to every watcher the instant it posts and is delivered by email; it can be edited but never un-seen.</reversibility>
  <precondition>`evidence/187-10-gh28-operator-approval.txt` exists, its first line is exactly `APPROVED-FOR-POST: gh28`, and its recorded sha256 equals `sha256sum evidence/bodies/187-gh28.md`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-10-post-transcript.txt, .planning/phases/187-answered-reports/evidence/187-10-comment-id.txt, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.3 and § Finding 6.4 (the posting command and the byte-identity trap), § Finding 2 (gh#28's pre-phase comment count and label set)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Posting — the command, and the read-back that does not false-alarm
    - `.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json`
    - `.planning/phases/187-answered-reports/evidence/187-10-gh28-operator-approval.txt`
  </read_first>
  <action>
    Re-assert the gate mechanically: recompute `sha256sum evidence/bodies/187-gh28.md` and confirm it
    equals the hash recorded in the approval file. If it differs, stop.

    Post with `gh issue comment 28 --repo henols/firestarter_prom --body-file evidence/bodies/187-gh28.md`.
    Pass the file; never interpolate the body onto the command line. Capture the returned URL, write the
    comment id alone on line 1 of `evidence/187-10-comment-id.txt`, and write a transcript block into
    `evidence/187-10-post-transcript.txt` with the command, exit code and URL.

    Prove byte-identity by reading the comment back through the API as JSON and comparing the decoded
    body against the file with a python round trip that exits non-zero on mismatch. Do not use
    `gh api --jq '.body'` piped into `diff`. Record both lengths and both hashes in the transcript.

    Add exactly the labels the operator approved with
    `gh issue edit 28 --repo henols/firestarter_prom --add-label "<label>"` per label. Use only
    `--add-label`. Do not pass `fix:released` in any form. Record each command and exit code.

    Do not close the issue. Re-read gh#28 and record its state, stateReason and resulting label set,
    reconciling field by field against the pre-phase set in `evidence/187-05-issue-state-before.json`.

    Flip gh#28's status line in `187-UPSTREAM-REPLIES.md` to a posted state, fill its Posted line with
    the comment URL, and note both the deliberate non-close and the deliberately withheld label. Leave
    the one remaining status line pending.

    Touch no other issue.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/187-10-gh28-operator-approval.txt</automated>
    <fails_when>prints anything other than `APPROVED-FOR-POST: gh28`</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh28.md | cut -d' ' -f1 | /usr/bin/grep -qF -f - evidence/187-10-gh28-operator-approval.txt && echo BOUND</automated>
    <fails_when>prints nothing, or non-zero exit — the approval file's hash does not cover the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && gh api "repos/henols/firestarter_prom/issues/comments/$(head -1 evidence/187-10-comment-id.txt)" > /tmp/c187gh28.json && python3 -c "import json,hashlib,sys; b=json.load(open('/tmp/c187gh28.json'))['body'].encode(); d=open('evidence/bodies/187-gh28.md','rb').read(); print('api',len(b),hashlib.sha256(b).hexdigest()); print('disk',len(d),hashlib.sha256(d).hexdigest()); print('BYTE-IDENTICAL' if b==d else 'MISMATCH'); sys.exit(0 if b==d else 1)"</automated>
    <fails_when>prints MISMATCH, or non-zero exit</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN — gh#28 was closed, which REPLY-06 forbids unconditionally</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 5 — the pre-phase count recorded in evidence/187-05-issue-state-before.json was 4, so any value other than that count plus one means a missing or duplicated post</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json labels --jq '[.labels[].name] | sort | join(",")'</automated>
    <fails_when>the output omits `needs:report`, omits any label present in the pre-phase set, or contains `fix:released`</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 2 — gh#31's pre-phase count; this plan must not comment there</fails_when>
    <automated>gh issue view 9 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 1 — this phase posts nothing new on gh#9</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 1 — either gh#28's line was not flipped, or gh#31's line was flipped early</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-10-gh28-operator-approval.txt` line 1 is exactly `APPROVED-FOR-POST: gh28`, its sha256 line matches the body on disk, and it names `fix:released` as withheld.
    - The API-read comment body is byte-identical to the file; the round trip prints `BYTE-IDENTICAL` and exits 0.
    - `gh issue view 28 --json state` returns `OPEN`.
    - gh#28's comment count equals its pre-phase count from the before-capture plus exactly one.
    - gh#28's label set equals the pre-phase set plus `needs:report`, and does not contain `fix:released`.
    - gh#31 and gh#9 comment counts are unchanged from the before-capture.
    - `187-UPSTREAM-REPLIES.md` has exactly one pending status line remaining.
  </acceptance_criteria>
  <done>The m27c512 reporter has a reply that concedes the attribution point, keeps the defect standing, warns what a PASS would not prove, and does not let a label imply a closure that has not happened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved body on disk → public issue comment | The moment of publication; visible to every watcher and delivered by email. |
| a label → the reporter's conclusion | `fix:released` on an issue whose chip defect is unfixed would communicate a closure no sentence ever claims. |
| a validation PASS → a claim about the product | The harness reaches its verdict by a path the user-facing write command does not take; a PASS read as proof of the write path is a misreading the reply must pre-empt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-50 | Repudiation | posting a body the operator never approved | critical | mitigate | Per-issue approval literal plus sha256 binding as the task's `<precondition>`, re-asserted before the post, with a byte-identity read-back after |
| T-187-51 | Elevation of Privilege | closing gh#28 | critical | mitigate | Closing is an unconditional named prohibition; the post-plan state is asserted `OPEN` and re-asserted in 187-12 |
| T-187-52 | Repudiation | `fix:released` implying a closure | high | mitigate | Applying it is a named prohibition, its absence is an automated label assertion, and the body states the withholding explicitly |
| T-187-53 | Repudiation | a re-run PASS read as proof the write path works | high | mitigate | The caveat is a drafted-in requirement positioned at or before the ask and is re-shown to the operator at this gate for a placement judgement |
| T-187-54 | Tampering | dropping a pre-existing label during the edit | medium | mitigate | Only `--add-label` is permitted; the resulting set is reconciled field by field against the pre-phase capture |
| T-187-55 | Elevation of Privilege | auto-mode approving the gate | critical | mitigate | `gate="blocking-human"`, the never-dispatch constraint in plan text, and an explicit mode confirmation at the checkpoint |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*`, `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| the five `evidence/bodies/187-gh*.md` payloads, both link checks, both hash files, `187-UPSTREAM-REPLIES.md` | 187-05, 187-06 |
| the gh#60, gh#62 and gh#23 approval files, transcripts, comment-id files, and the window-start file | 187-07, 187-08, 187-09 |
| `evidence/187-10-gh28-operator-approval.txt`, `evidence/187-10-post-transcript.txt`, `evidence/187-10-comment-id.txt` | **(this plan)** |
| the gh#31 approval file, transcript and comment-id file | 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-06 is an `unclassified` spec-less probe row and stays unresolved; the full 11-row accounting is
  in `187-01-PLAN.md`. It is additionally enforced here as a prohibition and by an automated state
  assertion.
- The expected comment counts in the verify block are the values measured on 2026-09-12. The
  before-capture in `evidence/187-05-issue-state-before.json` is the authoritative baseline; if a
  reporter commented in the interval, the expected value is that recorded count plus one.

<verification>
- The approval file exists with its literal, its label decision including the withheld label, and a hash covering the posted body.
- The posted comment reads back byte-identical.
- gh#28 is OPEN, carries one more comment than the before-capture, gained `needs:report`, lost nothing, and does not carry `fix:released`.
- gh#31 and gh#9 are untouched.
- One pending status line remains in the review document.
</verification>

<success_criteria>
The m27c512 reporter knows what changed, what did not, and that a passing re-run would not mean the
write command works on their part.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-10-SUMMARY.md` when done. Record the comment URL, both
hashes from the round trip, the before and after label sets, the withheld label and the reason, and the
confirmation that the issue remains open.
</output>
