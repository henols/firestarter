---
phase: 187-answered-reports
plan: 02
type: execute
wave: 2
depends_on: ["187-01"]
files_modified:
  - .planning/phases/187-answered-reports/187-MERGE-RECORD.md
  - .planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt
  - .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt
  - .planning/phases/187-answered-reports/evidence/187-02-operator-approval.txt
autonomous: false
requirements: [REPLY-03, REPLY-04, REPLY-07]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/notes/ae29f2008-classification-verdict.md` exists on `origin/beta` after this plan, having been absent from it before"
    - "`origin/beta`'s `.planning/notes/jumper-display-ground-truth.md` contains the string `energize socket pin 1`, proving it is the full document and not the 76-line stub"
    - "One 40-char commit SHA — the meta merge commit — is recorded alone on the first line of `evidence/187-02-merge-sha.txt`, and it is the single SHA every reply permalink in this phase pins"
    - "The GitHub contents API, queried at that SHA, returns `.planning/notes/jumper-display-ground-truth.md` with the gh#60 answer section present in its decoded body"
    - "No `v1.37` tag exists in `henols/firestarter_prom` after the merge"
  prohibitions:
    - "Must NOT create or push a `v1.37` tag in any repository, and must NOT route this merge through `/gsd-ship` — its close tree contains the only tag push, and `.planning/config.json` sets `git.create_tag: true`, which contradicts D-04"
    - "Must NOT pin any permalink at a SHA at or before `origin/beta`'s pre-merge tip; at such a SHA `jumper-display-ground-truth.md` resolves to a stub lacking the cited section, which is wrong content that looks like it worked rather than an honest 404"
    - "Must NOT push directly to `beta`; GitHub enforces no rules on `beta` in any of the three repositories, so the PR route is a deliberate choice and the only review artefact this merge will have"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 2 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt"
      provides: "the pinned 40-char meta merge SHA, alone on line 1, for every downstream permalink"
    - path: ".planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt"
      provides: "the pre-merge measurement, the PR number, the post-merge git cherry equivalence count, the contents-API permalink proofs, and the two copied anchor slugs"
      contains: "POST-MERGE PERMALINK PROOF"
    - path: ".planning/phases/187-answered-reports/evidence/187-02-operator-approval.txt"
      provides: "the per-act approval literal for the meta merge"
      contains: "APPROVED-FOR-MERGE: meta"
    - path: ".planning/phases/187-answered-reports/187-MERGE-RECORD.md"
      provides: "section 1 (the pull requests) and section 5 (the pinned permalink SHA and anchors)"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt"
      to: "every reply body drafted in 187-05 and 187-06"
      via: "the pinned 40-char meta merge SHA is copied verbatim into every permalink"
      pattern: "blob/[0-9a-f]{40}/"
---

<objective>
Publish the corrected record to the public `beta` branch of `henols/firestarter_prom` and prove, at a
pinned commit SHA, that the two evidence documents the replies will link resolve to the content the
replies cite.

Purpose: this is the phase's tracer — the thinnest path from a working-tree edit all the way to a
publicly-resolvable URL, through every layer the phase modifies on the record side. It is
production-quality and kept: this is the real v1.37 meta merge, not a rehearsal. It is proven before
any expansion task — before the app and firmware merges, and before a single reply body is drafted —
because RESEARCH DRIFT-3 measured that a permalink pinned at or before `origin/beta`'s current tip
resolves to a 76-line stub missing the very section REPLY-04 cites.

The tracer is Task 3 rather than Task 1 for one forced reason: D-16 requires a blocking operator
checkpoint immediately before each public act, and this merge is a public act. Task 1 measures, Task 2
gates, Task 3 traces. Nothing else precedes it, and no expansion follows until its verify block is green.

Output: the meta merge, one pinned SHA, two resolved permalink proofs, two copied anchor slugs.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.planning/notes/v135-close-procedure-under-protection.md
@/workspaces/CLAUDE.md

`.planning/ROADMAP.md` (930 KB) and `.planning/STATE.md` (510 KB) are never bare-read. Use `sed -n`.
Use `/usr/bin/grep` for every evidence leg; the devcontainer's bare `grep` is ugrep and honours
`.gitignore`. Export `XDG_CACHE_HOME` to a writable scratch directory before any `gh run view --log`;
`~/.cache/gh` is unwritable here and the failure presents as an auth error.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** Those modes auto-approve human-verify gates and `autonomous: false` is not self-protecting
against them. If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the meta seam immediately before the gate</name>
  <files>.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 1.1 (the `git cherry` command shape and the measured pre-merge figures), § Finding 1.2 (open-PR state per repo), § Finding 7.1 (the notes inventory on `origin/beta`)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Drift from CONTEXT.md, DRIFT-3 and DRIFT-4
    - `.planning/phases/187-answered-reports/187-01-SUMMARY.md` (what Plan 01 committed)
  </read_first>
  <action>
    Create `.planning/phases/187-answered-reports/evidence/` if it does not exist.

    Re-measure everything and write each result verbatim into `evidence/187-02-meta-merge.txt` under a
    heading `PRE-MERGE MEASUREMENT`. The research figures date from 2026-09-12 and that document's own
    validity note instructs re-measurement before acting regardless.

    Run `git fetch origin` in `/workspaces`, then record `git rev-parse origin/beta`,
    `git rev-parse HEAD`, `git rev-parse --abbrev-ref HEAD`, the count of `^+` lines from
    `git cherry origin/beta <branch>`, and the count of `^-` lines from the same command. Use
    `git cherry`, never `git merge-base --is-ancestor` — v1.30's squashed merge already produced one
    false negative from ancestry checking.

    Record `git status --short` verbatim, and re-run `git diff -- .planning/config.json`. The sub_repos
    prune is re-introduced by `loop render-hooks`, which the plan-phase flow calls, so it may have
    returned since Plan 01. If the diff is non-empty and you did not edit the file, run
    `git checkout -- .planning/config.json` and record that you did.

    Record `gh pr list --repo henols/firestarter_prom --state open --json number,title,headRefName,baseRefName`.
    Assert that no open PR has this milestone branch as its head. Do not assert "zero open PRs" as a
    property — that phrasing goes red later in the phase, because `firestarter#54` is a stale external
    contribution with head `beta` and base `beta` that has been open three weeks and must not be touched.

    Record the pre-merge state of the two documents the replies will link:
    `git ls-tree --name-only origin/beta .planning/notes/ | wc -l`; whether
    `.planning/notes/ae29f2008-classification-verdict.md` exists on `origin/beta`; and the line count of
    `git show origin/beta:.planning/notes/jumper-display-ground-truth.md`. The expected pre-merge
    readings are 26 entries, the verdict file absent, and 76 lines. That is the DRIFT-3 baseline the
    tracer's post-merge proof is measured against, so capture it even though it is the failing state.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep 'PRE-MERGE MEASUREMENT' .planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt | wc -l</automated>
    <fails_when>prints 0</fails_when>
    <automated>cd /workspaces && git diff -- .planning/config.json > /tmp/187-cfg.diff && wc -l < /tmp/187-cfg.diff</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces && git fetch origin --quiet && B=$(git rev-parse --abbrev-ref HEAD) && git cherry origin/beta "$B" > /tmp/187-cherry.txt && /usr/bin/grep '^+' /tmp/187-cherry.txt | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — a zero-ahead reading before the merge means the branch or the remote was resolved wrongly</fails_when>
    <automated>cd /workspaces && B=$(git rev-parse --abbrev-ref HEAD) && gh pr list --repo henols/firestarter_prom --state open --json headRefName --jq '.[].headRefName' > /tmp/187-prheads.txt && /usr/bin/grep "$B" /tmp/187-prheads.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a PR for this branch already exists and would be merged twice</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-02-meta-merge.txt` contains a `PRE-MERGE MEASUREMENT` block carrying both SHAs, the branch name, the `git cherry` `+` and `-` counts, verbatim `git status --short`, the open-PR listing, the `origin/beta` notes-entry count, the presence test for `ae29f2008-classification-verdict.md`, and the line count of `origin/beta`'s `jumper-display-ground-truth.md`.
    - `git diff -- .planning/config.json` produces no output.
    - `git cherry origin/beta <branch>` reports a non-zero count of `^+` lines.
    - No open PR in `henols/firestarter_prom` has this milestone branch as its head.
  </acceptance_criteria>
  <done>The seam is measured against live remotes, the tree is confirmed publishable, and the DRIFT-3 baseline is on the record.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve pushing the v1.37 milestone branch of the meta repository and merging it to the public `beta` branch of `henols/firestarter_prom`</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt` § PRE-MERGE MEASUREMENT — the block shown to the operator verbatim
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-01, § D-02 and § D-16
    - `.planning/notes/v135-close-procedure-under-protection.md`
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Drift from CONTEXT.md, DRIFT-5
  </read_first>
  <context>
    D-01 and D-02 make this phase own the merge and the cut before it speaks, and D-16 requires a
    separate blocking checkpoint immediately before each public act so approval for one act cannot carry
    to another. This is the first of three merge approvals; approving it approves nothing about the app
    merge, the firmware merge, or any reply posting.

    Present the operator with the verbatim `PRE-MERGE MEASUREMENT` block from
    `evidence/187-02-meta-merge.txt`, and state these four things explicitly:

    1. What becomes public: the whole `.planning/` tree at this branch tip, including Plan 01's five
       record repairs and the four new `.planning/notes/` documents. This repository is public.
    2. What this merge does not do: the meta repository has no `.github/workflows/` directory at all, so
       no release CI fires and nothing is published to PyPI or GitHub Releases by this merge.
    3. The reversibility rating, in D-01's own words: one-way. Content merged to a published branch in a
       public repository is world-readable from that moment and cannot be recalled.
    4. GitHub enforces no branch rules on `beta` in any of the three repositories — `Protect main` is the
       only active ruleset — so the PR route is this project's chosen review surface rather than an
       obstacle. No admin bypass is needed and none is used.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    On approval, write `evidence/187-02-operator-approval.txt` with the literal `APPROVED-FOR-MERGE: meta`
    alone on line 1, then the operator's name and date, then the decision prose explicitly labelled as
    the orchestrator's rendering of a menu selection rather than a verbatim operator quotation.
  </context>
  <options>
    <option id="merge">
      <name>Open the PR and merge it to `beta`</name>
      <pros>Unblocks every reply permalink in the phase; publishes the five record repairs; no release CI fires</pros>
      <cons>One-way — the `.planning/` tree at this tip becomes world-readable and cannot be recalled</cons>
    </option>
    <option id="hold">
      <name>Open the PR but hold the merge for review</name>
      <pros>The diff is visible on GitHub before anything lands on `beta`</pros>
      <cons>The branch is already public once pushed; the phase blocks until the merge happens</cons>
    </option>
    <option id="stop">
      <name>Stop the phase here</name>
      <pros>Nothing becomes public</pros>
      <cons>Every reply would describe code nobody can install and link evidence nobody can read — exactly the situation D-01 exists to prevent</cons>
    </option>
  </options>
  <resume-signal>Select: merge, hold, or stop</resume-signal>
  <acceptance_criteria>
    - `evidence/187-02-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-MERGE: meta`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection rather than a verbatim operator quotation.
    - The SUMMARY records that the verbatim `PRE-MERGE MEASUREMENT` block was shown, including both SHAs and the `git cherry` counts.
    - The SUMMARY records the operator's confirmation that the harness is not running under `--auto` or `--chain`.
    - No push, PR or merge happened before this file existed.
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 3: End-to-end — corrected record to resolvable public permalink, one path only</name>
  <reversibility rating="one-way">A merge to a public `beta` branch makes the whole `.planning/` tree world-readable from that instant; there is no recall, and D-01 rates it one-way in those terms.</reversibility>
  <precondition>`evidence/187-02-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-MERGE: meta`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt, .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt, .planning/phases/187-answered-reports/187-MERGE-RECORD.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 7.2 (the permalink URL shape, the `gh api contents?ref=SHA` confirmation, and the anchor caution), § Finding 7.3 (the sequencing consequence), § Finding 6.7 (merging under protection; meta's `origin/beta` tip is a two-parent merge commit)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § `187-MERGE-RECORD.md` (the 152 analog's section list, and that section 5 carries the pinned SHA)
    - `.planning/notes/v135-close-procedure-under-protection.md`
    - `.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt` (the pre-merge baseline this task is measured against)
  </read_first>
  <action>
    Wire one path from the working tree to a publicly-resolvable URL and prove it at the far end. Run the
    whole path; do not stop at an intermediate layer and do not add a second path.

    Push the milestone branch to `origin` in `/workspaces`. Open the pull request with
    `gh pr create --repo henols/firestarter_prom --base beta --head <milestone branch>`, titled for the
    v1.37 planning record. Do not route this through `/gsd-ship`: `ship.md` anchors its audit range on
    `git merge-base beta HEAD` and local `beta` is stale, and the only `git push` in the close tree is a
    tag push, which D-04 forbids.

    Merge with `gh pr merge <N> --repo henols/firestarter_prom --merge`. Use a true merge commit, not a
    squash and not a rebase: meta's `origin/beta` tip is itself a two-parent merge commit, and a squash
    destroys the per-commit correspondence that `git cherry` relies on for the rest of the phase.

    Run `git fetch origin` and capture the merge commit SHA, either from `git rev-parse origin/beta` or
    from `gh pr view <N> --repo henols/firestarter_prom --json mergeCommit`. Write that 40-character hex
    string, and nothing else, as the entire first line of `evidence/187-02-merge-sha.txt`. Downstream
    plans read this file with `cat`, so a label, a prefix or a second field on that line breaks them.
    Also record the SHA, the PR number and the merge timestamp into `evidence/187-02-meta-merge.txt`
    under a heading `MERGE_SHA`.

    Record, as the discretion decision D-13 leaves open, that this single SHA is pinned by every
    permalink in every reply body this phase posts. One SHA is one thing to verify, and the D-09 ledger
    states it once.

    Prove the far end at that SHA with the GitHub contents API rather than with a local git command — a
    local check proves only that your clone holds the object, not that the public URL resolves. Confirm
    `.planning/notes/jumper-display-ground-truth.md` exists at the SHA and that its decoded content
    contains `energize socket pin 1`; confirm `.planning/notes/ae29f2008-classification-verdict.md`
    exists at the SHA. Record both API responses verbatim under a heading `POST-MERGE PERMALINK PROOF`.

    Then open the rendered GitHub page for each of the two documents at the pinned SHA and copy the
    anchor slug GitHub actually emits for the two cited headings: the one reading
    `Which operations energize socket pin 1` followed by the em-dash and the gh#60 reference, and the one
    reading `WHY THE REPORTER'S` followed by the backtick-quoted force flag and the rest of that heading.
    Write both copied anchors into `evidence/187-02-meta-merge.txt`. Do not derive them from the slug
    rule — both headings contain a backtick-quoted token, an em-dash and a `#` inside `gh#60`, and a
    wrong anchor lands the reader silently at the top of a 279-line document instead of at the answer.

    Re-run `git cherry origin/beta <branch>` and record the `^-` count as the post-merge equivalence
    proof required by D-05.

    Start `187-MERGE-RECORD.md` following the 152 analog's section list. Fill section 1 with this PR and
    leave rows for the app and firmware PRs; fill section 5 with the pinned SHA and both copied anchors.
    Sections 2, 3 and 4 are completed by Plans 187-03 and 187-04 after the cuts, and the tail-disclosure
    section by Plan 187-12.
  </action>
  <verify>
    <automated>cd /workspaces && git fetch origin --quiet && git cat-file -e origin/beta:.planning/notes/ae29f2008-classification-verdict.md && echo PRESENT</automated>
    <fails_when>prints nothing, or non-zero exit — the verdict document REPLY-03 links is still absent from `beta`</fails_when>
    <automated>cd /workspaces && git show origin/beta:.planning/notes/jumper-display-ground-truth.md > /tmp/187-jdgt.md && /usr/bin/grep 'energize socket pin 1' /tmp/187-jdgt.md | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — `origin/beta` still carries the 76-line stub that lacks REPLY-04's section</fails_when>
    <automated>cd /workspaces && git show origin/beta:.planning/REQUIREMENTS.md > /tmp/187-req.md && /usr/bin/grep '5511487546' /tmp/187-req.md | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — Plan 01's REPLY-07 repair did not reach `beta`</fails_when>
    <automated>cd /workspaces && head -1 .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt | /usr/bin/grep -E '^[0-9a-f]{40}$' | wc -l</automated>
    <fails_when>prints anything other than 1 — the pinned SHA file is missing, abbreviated, or carries a label on line 1</fails_when>
    <automated>cd /workspaces && gh api "repos/henols/firestarter_prom/contents/.planning/notes/jumper-display-ground-truth.md?ref=$(head -1 .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt)" --jq '.content' > /tmp/187-jdgt.b64 && base64 -d < /tmp/187-jdgt.b64 > /tmp/187-jdgt-api.md && /usr/bin/grep 'energize socket pin 1' /tmp/187-jdgt-api.md | wc -l</automated>
    <fails_when>prints 0, or non-zero exit — the public URL at the pinned SHA does not resolve to the cited section</fails_when>
    <automated>cd /workspaces && gh api "repos/henols/firestarter_prom/contents/.planning/notes/ae29f2008-classification-verdict.md?ref=$(head -1 .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt)" --jq '.path'</automated>
    <fails_when>prints nothing, or non-zero exit — REPLY-03's evidence document does not resolve at the pinned SHA</fails_when>
    <automated>cd /workspaces && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a v1.37 tag exists in the meta repository, which D-04 forbids</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'POST-MERGE PERMALINK PROOF' .planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt | wc -l</automated>
    <fails_when>prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh pr view <N> --repo henols/firestarter_prom --json state --jq .state` returns `MERGED`, and the PR's base was `beta`.
    - `evidence/187-02-merge-sha.txt` line 1 matches `^[0-9a-f]{40}$` and nothing else.
    - The contents API at that SHA returns `.planning/notes/jumper-display-ground-truth.md` whose decoded body contains `energize socket pin 1`, and returns `.planning/notes/ae29f2008-classification-verdict.md` without error.
    - `evidence/187-02-meta-merge.txt` contains a `POST-MERGE PERMALINK PROOF` block with both verbatim API responses, both copied anchor slugs, and the post-merge `git cherry` `^-` count.
    - `git ls-remote --tags origin` in `/workspaces` matches no `v1.37` tag.
    - `187-MERGE-RECORD.md` exists with section 1 naming this PR and section 5 carrying the pinned SHA and both anchors.
  </acceptance_criteria>
  <done>The corrected record is on the public `beta` branch, one SHA is pinned, and both documents the replies will link are proven to resolve at that SHA with the cited sections present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone branch → public `beta` of `henols/firestarter_prom` | The push and merge are the moment private planning content becomes world-readable. Nothing downstream can redact it. |
| local clone → GitHub contents API | A local `git show` proves only what the clone holds; only the API at a pinned ref proves what a reporter's browser will see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-06 | Information Disclosure | the whole `.planning/` tree at the branch tip | high | mitigate | Task 1 re-runs `git status --short` and the `config.json` diff immediately before the gate; Task 2 states verbatim to the operator what becomes public; staging remains explicit-path only from Plan 01 |
| T-187-07 | Spoofing | permalinks in the reply bodies | high | mitigate | The pinned SHA is captured only after the merge, written as a bare 40-char string, and proven at the API before any body is drafted; a pre-merge SHA is a named prohibition |
| T-187-08 | Tampering | `v1.37` tag creation via `git.create_tag: true` | high | mitigate | `/gsd-ship` is not used; PRs are opened directly with `gh pr create --base beta`; tag absence is an automated verify leg in this plan and re-asserted in 187-04 and 187-12 |
| T-187-09 | Repudiation | the merge itself | high | mitigate | A per-act approval file carrying `APPROVED-FOR-MERGE: meta` is written at the gate and asserted as the tracer's `<precondition>`; the PR is the review artefact GitHub does not require on `beta` |
| T-187-10 | Tampering | `.planning/config.json` sub_repos prune re-introduced between plans | medium | mitigate | Task 1 re-checks and reverts, and asserts an empty diff before the gate |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here; the rest are listed so a reader of this plan alone sees
the whole output set.

| Artifact | Produced by |
|---|---|
| `.planning/config.json`, `.planning/VALIDATED-EPROMS.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` repairs | 187-01 |
| `evidence/187-02-meta-merge.txt`, `evidence/187-02-merge-sha.txt`, `evidence/187-02-operator-approval.txt` | **(this plan)** |
| `187-MERGE-RECORD.md` sections 1 and 5 | **(this plan)**; sections 2-4 by 187-03/187-04; tail by 187-12 |
| `evidence/187-03-app-cut.txt`, `evidence/187-04-fw-cut.txt` and their approval files | 187-03, 187-04 |
| `187-UPSTREAM-REPLIES.md` | 187-05, 187-06, 187-12 |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md`, `187-gh60.md`, `187-gh62.md` | 187-05, 187-06 |
| `evidence/187-05-issue-state-before.json`, the two draft-link-check files | 187-05, 187-06 |
| one operator-approval file and one post transcript per posted issue | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-03 and REPLY-04 are `unclassified` spec-less probe rows and stay unresolved; the full 11-row
  accounting is in `187-01-PLAN.md`. What this plan can prove mechanically is only that their evidence
  documents resolve at the pinned SHA, not that the replies carry their substance — that is judged at
  the drafting and approval gates in 187-05, 187-07 and 187-08.
- The two anchor slugs are marked `[ASSUMED]` in RESEARCH § Assumptions Log A4. This plan resolves them
  by copying what GitHub renders rather than deriving them, which is why the copy step is an action
  rather than a computation.

<verification>
- The meta PR is MERGED into `beta` with a true merge commit.
- `evidence/187-02-merge-sha.txt` holds one bare 40-char SHA on line 1.
- The contents API at that SHA returns both linked documents, and the gh#60 answer section is present.
- No `v1.37` tag exists in `henols/firestarter_prom`.
- `187-MERGE-RECORD.md` sections 1 and 5 are filled.
</verification>

<success_criteria>
A reply drafted after this plan can link either evidence document at a commit SHA and be certain the
reader sees the cited section, which was not true before it.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-02-SUMMARY.md` when done. Record the PR number, the
pinned SHA, the pre-merge and post-merge readings side by side, both copied anchor slugs, and the
statement that no tag was created.
</output>
