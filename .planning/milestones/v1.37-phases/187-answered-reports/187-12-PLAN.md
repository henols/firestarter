---
phase: 187-answered-reports
plan: 12
type: execute
wave: 12
depends_on: ["187-11"]
files_modified:
  - .planning/notes/v137-upstream-reply-ledger.md
  - .planning/phases/187-answered-reports/187-MERGE-RECORD.md
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/187-12-issue-state-after.json
  - .planning/phases/187-answered-reports/evidence/187-12-collateral-check.txt
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REPLY-01, REPLY-02, REPLY-03, REPLY-04, REPLY-05, REPLY-06, REPLY-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "gh#23, gh#28 and gh#31 are all OPEN at the end of this phase, and the after-capture proves each carries exactly one more comment than the before-capture recorded"
    - "Exactly five comments were created in `henols/firestarter_prom` between the recorded window start and the end of this phase — one each on gh#60, gh#62, gh#23, gh#28 and gh#31 — and none anywhere else"
    - "gh#9 is unchanged: still OPEN, still pinned, still carrying exactly one comment, and that comment is still the one Phase 173 posted"
    - "No `v1.37` tag exists in any of the three repositories"
    - "`.planning/notes/v137-upstream-reply-ledger.md` records both the gh#9 staleness finding with its comment URL and eight-day window, and a per-issue ledger naming the body posted, its permalink, the versions named, the labels changed, and what was closed versus deliberately left open"
    - "The ledger states explicitly that this phase files zero backlog items, so a later reader does not read the absent section as an omission"
  prohibitions:
    - "Must NOT file any backlog item, todo, successor requirement or guard issue arising from the gh#9 staleness finding; the branch taken is to name it and file nothing, and the ledger says so in those terms"
    - "Must NOT restate the claim that ten files under `.planning/milestones/` mention gh#9; that figure was measured wrong, and repeating it would file a fresh false measurement in the phase that exists to stop doing that. State a count only if re-measured in this plan, or omit it"
    - "Must NOT repair any `.planning/milestones/` citation or either of the two excluded ROADMAP sites; the D-07 fence closed in 187-01 and stays closed"
    - "Must NOT re-pin, unpin or otherwise mutate gh#9; the pinned-issues read is verification only"
    - "Must NOT mark any requirement Complete that this phase did not deliver, and must NOT open a second meta pull request for the phase tail unless the operator asks"
  artifacts:
    - path: ".planning/notes/v137-upstream-reply-ledger.md"
      provides: "the durable record a future reader checks before claiming a reply is owed — the gh#9 staleness finding plus the per-issue reply ledger"
      contains: "5511487546"
    - path: ".planning/phases/187-answered-reports/evidence/187-12-issue-state-after.json"
      provides: "the after-half of the state capture, reconciled field by field against the before-half"
    - path: ".planning/phases/187-answered-reports/evidence/187-12-collateral-check.txt"
      provides: "every comment created in the repository since the window start, proving no collateral post"
    - path: ".planning/phases/187-answered-reports/187-MERGE-RECORD.md"
      provides: "the completed record including the tail-disclosure section"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the REPLY-01 through REPLY-06 traceability rows marked Complete with their comment URLs"
  key_links:
    - from: ".planning/notes/v137-upstream-reply-ledger.md"
      to: "the five posted comment URLs in the evidence transcripts"
      via: "the per-issue ledger table cites each comment URL and the body file it was posted from"
      pattern: "issuecomment-[0-9]+"
---

<objective>
Prove that this phase did exactly what it said and nothing else, then record it where the next reader
will look.

Purpose: the finding that started REPLY-07 is that a requirement was promoted off a record that had been
false for eight days, and nothing caught it. The correction is not a new guard — the operator's decision
is to name it and file nothing — it is a durable record in the place four consecutive phases have put
their verdicts, so the next person to claim a reply is owed checks there first.

Output: the after-state reconciliation, the no-collateral-post proof, the D-09 ledger document, the
completed merge record, and the REPLY traceability rows.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.planning/REQUIREMENTS.md
@/workspaces/CLAUDE.md

`.planning/ROADMAP.md` (930 KB) and `.planning/STATE.md` (510 KB) are never bare-read. Use `sed -n`.
Use `/usr/bin/grep` for every evidence leg.

This plan performs no public act: it reads the API and writes to disk. The phase it closes must never
have been dispatched under `--auto` or `--chain`, and if the SUMMARYs of 187-02, 187-03, 187-04 or
187-07 through 187-11 do not each record an operator approval, say so in this plan's SUMMARY rather than
reporting the phase as clean.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the after-state and prove no collateral act</name>
  <files>.planning/phases/187-answered-reports/evidence/187-12-issue-state-after.json, .planning/phases/187-answered-reports/evidence/187-12-collateral-check.txt</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.5 (the corrected collateral sweep, and why Phase 173's own version of this leg was wrong — it asserted zero comments on every non-target issue, which was never true of a repository where 39 of 53 issues already carried comments), § 2.1 (the pinnedIssues read), § 2.2 (the gh#9 comment round trip)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § No-collateral-post proof and § Before/after issue-state capture
    - `.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json` and `evidence/187-07-window-start.txt`
  </read_first>
  <action>
    Re-read the state of issues 9, 23, 28, 31, 60 and 62 in the same shape the before-capture used, and
    write the result to `evidence/187-12-issue-state-after.json`. Append the GraphQL `pinnedIssues` read.
    Then reconcile the two captures field by field — state, stateReason, label set and comment count per
    issue — and write the reconciliation into the same file as a table, one row per issue, naming every
    difference and stating whether it was an approved act of this phase.

    The expected differences are exactly these: gh#60 moved from OPEN to CLOSED with the approved reason
    and gained its approved labels and one comment; gh#62 stayed OPEN and gained its approved labels and
    one comment; gh#23 stayed OPEN and gained `cause:rig`, `needs:report` and one comment; gh#28 stayed
    OPEN and gained `needs:report` and one comment; gh#31 stayed OPEN and gained `needs:report` and one
    comment; gh#9 changed in no respect at all. Any other difference is an unapproved act and must be
    reported, not normalised away.

    Prove no collateral post. Read the window start from `evidence/187-07-window-start.txt` and list
    every comment created in the repository at or after it, across all issues, with
    `gh api repos/henols/firestarter_prom/issues/comments --paginate` filtered on `created_at`. Write the
    full result to `evidence/187-12-collateral-check.txt`. The expected result is exactly five rows, one
    each on issues 60, 62, 23, 28 and 31. Do not write the leg as an assertion that non-target issues
    carry zero comments — most of this repository's issues already carry comments and that assertion was
    never true here.

    Verify gh#9's pin and comment are intact as a read-only confirmation of D-06: `pinnedIssues` returns
    a total of one naming issue 9 in state OPEN, gh#9 carries exactly one comment, and that comment's id
    is 5511487546 with its created and updated timestamps still equal, meaning it has never been edited.
    Do not call the pin mutation.

    Confirm no `v1.37` tag exists in any of the three repositories, and record the three readings.
  </action>
  <verify>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 60 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than CLOSED</fails_when>
    <automated>gh issue view 62 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 9 --repo henols/firestarter_prom --json state,comments --jq '"\(.state) \(.comments|length)"'</automated>
    <fails_when>prints anything other than `OPEN 1`</fails_when>
    <automated>gh api repos/henols/firestarter_prom/issues/comments/5511487546 --jq '"\(.created_at) \(.updated_at)"' > /tmp/187-gh9ts.txt && awk '{print ($1==$2)?"UNEDITED":"EDITED"}' /tmp/187-gh9ts.txt</automated>
    <fails_when>prints EDITED, or non-zero exit — Phase 173's gh#9 comment was modified by this phase</fails_when>
    <automated>gh api graphql -f query='{repository(owner:"henols",name:"firestarter_prom"){pinnedIssues(first:10){totalCount nodes{issue{number}}}}}' --jq '.data.repository.pinnedIssues.totalCount'</automated>
    <fails_when>prints anything other than 1 — the pin state changed</fails_when>
    <automated>cd /workspaces && gh api repos/henols/firestarter_prom/issues/comments --paginate --jq ".[] | select(.created_at >= \"$(head -1 .planning/phases/187-answered-reports/evidence/187-07-window-start.txt)\") | (.issue_url|split(\"/\")|last)" > /tmp/187-coll.txt && sort /tmp/187-coll.txt | tr '\n' ' '</automated>
    <fails_when>prints anything other than the five issue numbers 23 28 31 60 62 in sorted order, or non-zero exit — an extra number means a collateral comment, a missing one means a reply did not land</fails_when>
    <automated>cd /workspaces && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-remote --tags origin > /tmp/187-tags.txt && /usr/bin/grep 'v1\.37' /tmp/187-tags.txt | wc -l</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-12-issue-state-after.json` contains the after-state of all six issues plus the `pinnedIssues` read, and a reconciliation table naming every difference against the before-capture and whether it was an approved act.
    - `evidence/187-12-collateral-check.txt` lists every comment created at or after the window start, and the set of issue numbers in it is exactly 23, 28, 31, 60 and 62.
    - gh#23, gh#28, gh#31 and gh#62 are OPEN; gh#60 is CLOSED with the approved reason.
    - gh#9 is OPEN with exactly one comment, that comment is id 5511487546, its created and updated timestamps are equal, and `pinnedIssues` returns one node naming issue 9.
    - `git ls-remote --tags origin` matches no `v1.37` tag in any of the three repositories, and all three readings are recorded.
  </acceptance_criteria>
  <done>The phase's public footprint is measured and equals exactly what was approved: five comments, one close, four label changes, and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-09 ledger — the gh#9 finding and the per-issue reply record</name>
  <files>.planning/notes/v137-upstream-reply-ledger.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-06, § D-07, § D-08 and § D-09 (what the document must carry, and the branch that files nothing)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § `.planning/notes/<name>.md` (the three frontmatter keys, the section spine, the numbered-section variant that fits a ledger better than a verdict, and the requirement to state explicitly that zero backlog items are generated)
    - `.planning/notes/python-floor-decision.md` (the numbered-section variant being followed)
    - `.planning/notes/dispatch-invariant-retirement-verdict.md` § the drift-measured-and-not-adjudicated section (the model for the staleness finding)
    - `.planning/phases/187-answered-reports/evidence/187-12-issue-state-after.json` and the five post transcripts
  </read_first>
  <action>
    Create `.planning/notes/v137-upstream-reply-ledger.md` with the three-key YAML frontmatter the
    analogs use — a `title` that states the finding itself rather than a topic, a `date`, and a `context`
    naming the phase, the requirements served and what was measured against what. Follow the
    numbered-section variant rather than the verdict spine, because this document is more ledger than
    verdict.

    Section one carries the gh#9 staleness finding. State what was claimed and where: that the live
    records asserted gh#9 still owed a closing reply. State what was true: Phase 173 posted an
    operator-approved body verbatim as comment id 5511487546 on 2026-09-02, byte-identical to the
    approved body on disk, and then deliberately left the issue open and pinned. State the window: the
    requirement was filed on 2026-09-10, eight days after the claim became false, and nothing caught it.
    State what was repaired and where — the five live sites, by file and by what each said — and state
    what was deliberately not repaired and why: the two ROADMAP sites that describe gh#9 as the pinned
    orientation issue are literally true and configured, and every hit under `.planning/milestones/` is
    historical-by-intent, where repairing a citation would destroy the evidence it records. Do not state
    a count of those historical files unless you re-measure it in this plan; a previously recorded figure
    for it was wrong.

    Section two carries the per-issue reply ledger as a table, one row per issue: the issue number and
    title, the body file it was posted from, the posted comment URL, the app and firmware versions named,
    the labels added and any label deliberately withheld with its reason, and whether the issue was
    closed or deliberately left open. Five rows for 23, 28, 31, 60 and 62, plus a sixth for gh#9 recording
    that this phase posted nothing on it and changed nothing about it.

    Section three records the pinned permalink SHA once — the meta merge commit every reply's links pin —
    with the two anchors used, so a future reader can resolve any cited link without re-deriving anything.

    Section four states the disposition of the finding explicitly, in the operator's own terms: it is
    named and nothing is filed. Say that this phase generates zero backlog items, zero todos and zero
    successor requirements, and that the predecessors' habit of ending with a backlog-items summary is
    honoured here by stating the count is zero rather than by omitting the section. Name the precedent
    phases that took the same branch.

    Section five records the residual gap named explicitly: detecting a mis-wired VPP at the socket is the
    capability gh#23 actually needs and the one thing that would change a fresh run's verdict for that
    failure mode; it is new hardware-side work, not a host fix, and under the same discipline it is
    recorded here and not filed.

    File nothing. Create no backlog entry, no todo and no issue.
  </action>
  <verify>
    <automated>cd /workspaces && head -1 .planning/notes/v137-upstream-reply-ledger.md</automated>
    <fails_when>prints anything other than `---` — the analogs' three-key YAML frontmatter is missing</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -E '^(title|date|context):' .planning/notes/v137-upstream-reply-ledger.md | wc -l</automated>
    <fails_when>prints a number below 3 — one of the three frontmatter keys the analogs all carry is missing</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '5511487546' .planning/notes/v137-upstream-reply-ledger.md | wc -l</automated>
    <fails_when>prints 0 — the staleness finding does not cite the comment that discharged REPLY-07</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -o 'issuecomment-[0-9]*' .planning/notes/v137-upstream-reply-ledger.md | sort -u | wc -l</automated>
    <fails_when>prints a number below 6 — the ledger cites fewer than the five posted comments plus the gh#9 comment</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -F "$(head -1 .planning/phases/187-answered-reports/evidence/187-02-merge-sha.txt)" .planning/notes/v137-upstream-reply-ledger.md | wc -l</automated>
    <fails_when>prints 0 — the pinned permalink SHA is not recorded in the ledger</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -F "$(head -1 .planning/phases/187-answered-reports/evidence/187-03-app-version.txt)" .planning/notes/v137-upstream-reply-ledger.md | wc -l</automated>
    <fails_when>prints 0 — the app version named in the replies is not recorded in the ledger</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -F "$(head -1 .planning/phases/187-answered-reports/evidence/187-04-fw-version.txt)" .planning/notes/v137-upstream-reply-ledger.md | wc -l</automated>
    <fails_when>prints 0 — the firmware version named in the replies is not recorded in the ledger</fails_when>
    <automated>cd /workspaces && git status --porcelain -- .planning/milestones > /tmp/187-ms.txt && wc -l < /tmp/187-ms.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit — the D-07 fence was crossed</fails_when>
    <automated>cd /workspaces && git status --porcelain -- .planning/todos .planning/backlog > /tmp/187-tb.txt && wc -l < /tmp/187-tb.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit — a todo or backlog item was filed, which D-08 forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/notes/v137-upstream-reply-ledger.md` opens with YAML frontmatter carrying `title`, `date` and `context`, and the title states the finding rather than a topic.
    - Section one names what was claimed, what was true, the comment id 5511487546 and its URL, the eight-day window, the five repaired sites and the two categories deliberately not repaired.
    - Section two is a table with six rows — the five replied-to issues and gh#9 — each carrying the body file, the comment URL, the versions named, the labels added, any withheld label with its reason, and the close-or-left-open disposition.
    - Section three records the pinned meta merge SHA and both anchors.
    - Section four states in words that this phase files zero backlog items, zero todos and zero successor requirements, and names the precedent phases that took the same branch.
    - Section five records the mis-wired-VPP detection gap as recorded-not-filed.
    - No count of historical `.planning/milestones/` files is stated unless it was re-measured in this plan.
    - `git status --porcelain` shows no change under `.planning/milestones`, `.planning/todos` or `.planning/backlog`.
  </acceptance_criteria>
  <done>The durable record exists in the place four consecutive phases have put their verdicts, carrying both the finding and the reply ledger, and it files nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the merge record, finalise the review document, and mark the REPLY rows</name>
  <files>.planning/phases/187-answered-reports/187-MERGE-RECORD.md, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § `187-MERGE-RECORD.md` (the 152 analog's section list, including the tail-disclosure section) and § Record repair 5 (the register the traceability Status cell is written in)
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-03 (the phase tail lands on the milestone branch, and a second meta PR is not scheduled unilaterally)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.6 (the measured precedent: v1.35 and v1.36 both did open a second meta PR for the close tail, so do not describe the milestone-branch landing as the project's invariable pattern)
    - `sed -n '208,220p' .planning/REQUIREMENTS.md`
    - the five post transcripts and `evidence/187-12-issue-state-after.json`
  </read_first>
  <action>
    Complete `187-MERGE-RECORD.md`. Confirm sections 1 through 5 are filled for all three repositories,
    then add the tail-disclosure section the 152 analog carries: the commits made to the meta repository
    after its pull request merged — this phase's posting record, the ledger document, the SUMMARYs and
    the verification — which are on the milestone branch and not on `beta`. State that this is D-03's
    accepted outcome. Note, without acting on it, that the two most recent milestones each did open a
    separate operator-initiated pull request for their close tail, so a reader is not told that landing
    the tail on the milestone branch is the project's invariable pattern. Do not schedule or open a
    second meta pull request; D-03 reserves that for the operator to ask for.

    Also record in the same section the gitlink position captured in Plan 187-04: meta's `origin/beta`
    gitlinks are structurally behind each sub-repo's `origin/beta` by the merge commit plus CI's version
    bump, because the bump commit does not exist until after the merge. This is precedented and is not
    drift.

    Finalise `187-UPSTREAM-REPLIES.md`: confirm all five status lines are flipped with their comment
    URLs, confirm each inline body copy is still byte-identical to its file in `evidence/bodies/`, and
    add a closing note recording that every body was approved on disk before posting, naming the five
    approval files.

    Update `.planning/REQUIREMENTS.md` by hand — never through the requirements or roadmap verbs, which
    reformat the whole file. Flip REPLY-01 through REPLY-06 to `[x]` and set their traceability Status
    cells in the register the completed rows use: the disposition, the comment URL, any amendment, and a
    plan-number trace. REPLY-07's row was completed in Plan 187-01 and is not touched. Do not mark
    anything Complete that this phase did not deliver.

    Confirm, and record in this plan's SUMMARY, that every plan in this phase that performed a public act
    has an operator approval file on disk: three merge approvals and five posting approvals, eight in
    total, each with its own approval literal on line 1.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && ls evidence/187-*operator-approval.txt | wc -l</automated>
    <fails_when>prints anything other than 8 — one approval file per public act is required: three merges and five posts</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -l -E '^APPROVED-FOR-(MERGE|POST):' evidence/187-*operator-approval.txt | wc -l</automated>
    <fails_when>prints anything other than 8 — an approval file does not carry its literal on line 1</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -o 'issuecomment-[0-9]*' 187-UPSTREAM-REPLIES.md | sort -u | wc -l</automated>
    <fails_when>prints anything other than 5 — the review document does not carry exactly one posted comment URL per reply</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '\[x\] \*\*REPLY-0[1-7]\*\*' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 7 — a REPLY requirement is still unticked after the phase delivered it</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -E '^\| REPLY-0[1-7] \| Phase 187 \| Complete' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 7 — a REPLY traceability row still reads Pending</fails_when>
    <automated>cd /workspaces && /usr/bin/grep -E '^\| REPLY-0[1-7] \| Phase 187 \| Pending' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c '^## ' 187-MERGE-RECORD.md</automated>
    <fails_when>prints a number below 6 — the merge record is missing sections, including the tail-disclosure section</fails_when>
    <automated>cd /workspaces && git status --porcelain > /tmp/187-st.txt && /usr/bin/grep -v '^??' /tmp/187-st.txt | wc -l</automated>
    <fails_when>prints anything other than 0 after this plan's commits, or non-zero exit — a tracked file is left uncommitted</fails_when>
    <automated>cd /workspaces && git ls-files -- anything.txt tmp/ > /tmp/187-scratch.txt && wc -l < /tmp/187-scratch.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit — operator scratch became tracked at some point in the phase</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly eight `evidence/187-*operator-approval.txt` files exist, and each carries an `APPROVED-FOR-MERGE:` or `APPROVED-FOR-POST:` literal on line 1.
    - `187-UPSTREAM-REPLIES.md` has zero pending status lines and exactly five distinct posted comment URLs, and each inline body copy is byte-identical to its file in `evidence/bodies/`.
    - `187-MERGE-RECORD.md` carries at least six `##` sections including the tail disclosure, records the pinned SHA, both cut versions, and the gitlink position without acting on it.
    - All seven REPLY requirement bullets are `[x]` and all seven traceability rows read `Complete` with a disposition, a trace and, where applicable, a comment URL; none reads `Pending`.
    - `git status --porcelain` shows no uncommitted tracked file, and `anything.txt` and `tmp/` are still untracked.
    - This plan's SUMMARY names all eight approval files and states that no second meta pull request was opened.
  </acceptance_criteria>
  <done>The phase's records are complete and internally consistent: eight approvals, five posted URLs, seven completed requirements, and a merge record that discloses what did not land on `beta`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the phase's actual public footprint → what its records claim | This is the plan where the two are compared. A record that overstates what happened is the exact defect the milestone exists to correct. |
| a finding → a filed item | The operator's branch is to name the finding and file nothing; an executor that helpfully files a guard item overrides a locked decision. |
| the milestone branch → `beta` | The phase tail deliberately does not reach `beta`, and that gap must be disclosed rather than silently left. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-62 | Repudiation | an unapproved public act going unnoticed | high | mitigate | The before/after reconciliation names every difference and whether it was approved, and the collateral sweep lists every comment created in the window across the whole repository |
| T-187-63 | Elevation of Privilege | a disputed issue found closed | critical | mitigate | All three are asserted `OPEN` again here, after the posting plans asserted it individually |
| T-187-64 | Tampering | gh#9 modified or unpinned | high | mitigate | Read-only pin and comment verification, including a created-versus-updated timestamp comparison proving the 2026-09-02 comment was never edited; the pin mutation is a named prohibition |
| T-187-65 | Repudiation | filing a backlog item against a locked decision | high | mitigate | Named prohibition, plus a `git status` leg over the todo and backlog directories, plus an explicit zero-count statement in the ledger |
| T-187-66 | Repudiation | restating a measurement known to be wrong | medium | mitigate | Named prohibition on the historical-file count; the ledger states a count only if re-measured |
| T-187-67 | Tampering | a REPLY requirement marked Complete that was not delivered | medium | mitigate | The traceability legs assert seven Complete rows and zero Pending rows, and each row must carry a trace; the posted comment URLs are independently asserted in the review document |
| T-187-68 | Information Disclosure | operator scratch tracked at any point in the phase | high | mitigate | A final `git ls-files` leg over the scratch paths closes the loop opened in 187-01 |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here. This is the complete inventory for Phase 187.

| Artifact | Produced by |
|---|---|
| `.planning/config.json` (prune reverted), `.planning/VALIDATED-EPROMS.md`, the three `ROADMAP.md` repairs, the `REQUIREMENTS.md` REPLY-07 and REPLY-01 amendments | 187-01 |
| `evidence/187-02-meta-merge.txt`, `evidence/187-02-merge-sha.txt`, `evidence/187-02-operator-approval.txt` | 187-02 |
| `evidence/187-03-app-cut.txt`, `evidence/187-03-app-version.txt`, `evidence/187-03-operator-approval.txt` | 187-03 |
| `evidence/187-04-fw-cut.txt`, `evidence/187-04-fw-version.txt`, `evidence/187-04-operator-approval.txt` | 187-04 |
| `evidence/bodies/187-gh60.md`, `187-gh62.md`, `evidence/187-05-issue-state-before.json`, `evidence/187-05-draft-link-check.txt`, `evidence/187-05-body-hashes.txt` | 187-05 |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md`, `evidence/187-06-draft-link-check.txt`, `evidence/187-06-body-hashes.txt` | 187-06 |
| `187-UPSTREAM-REPLIES.md` | 187-05, 187-06, flipped by 187-07 … 187-11, finalised **(this plan)** |
| `evidence/187-07-window-start.txt` and, per posted issue, an `*-operator-approval.txt`, a `*-post-transcript.txt` and a `*-comment-id.txt` | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | **(this plan)** |
| `.planning/notes/v137-upstream-reply-ledger.md` | **(this plan)** |
| `187-MERGE-RECORD.md` (tail-disclosure section and finalisation) | **(this plan)**; sections 1-5 by 187-02, 187-03, 187-04 |
| `.planning/REQUIREMENTS.md` REPLY-01 … REPLY-06 traceability rows | **(this plan)** |
| One `187-NN-SUMMARY.md` per plan | each plan |

## Flagged planner assumptions carried into this plan

The spec-less probe produced 11 applicable edges. Six classified rows are authored as
`must_haves.truths` on 187-05, 187-06 and the posting plans; five `unclassified` rows stay unresolved and
are surfaced as flagged assumptions. The full accounting table is in `187-01-PLAN.md`. 11 equals 6
authored plus 5 flagged, with no silent drop.

- REPLY-01, REPLY-03, REPLY-04, REPLY-06 and REPLY-07 are the five unresolved rows. This plan verifies
  their observable consequences — an issue's state, a comment's existence and its byte-identity, a
  requirement's traceability row — but the probe rows themselves stay unresolved rather than being
  auto-resolved with a backstop, because an `unclassified` row must never be auto-resolved.
- RESEARCH § Assumptions Log A5 rated the `VALIDATED-EPROMS.md` edit as intended content without a
  line-by-line inspection. The orchestrator dispositioned it as the gh#61 PASS log entry and 187-01
  committed it; if the SUMMARY of 187-01 records anything different, say so here.

<verification>
- The before/after reconciliation accounts for every difference on all six issues.
- Exactly five comments were created in the window, on exactly the five target issues.
- gh#23, gh#28, gh#31 and gh#62 OPEN; gh#60 CLOSED with the approved reason; gh#9 unchanged and pinned.
- No `v1.37` tag in any repository.
- The ledger exists, cites the comment that discharged REPLY-07, records the pinned SHA and both versions, and states a zero backlog count.
- Eight approval files, five posted URLs, seven Complete REPLY rows, zero Pending.
</verification>

<success_criteria>
Phase 187's records say exactly what the phase did, no more: five reporters answered, one issue closed
because its feature shipped, three left open because their reporters have not agreed, one issue touched
not at all, and nothing filed.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-12-SUMMARY.md` when done. Record the reconciliation
table, the collateral sweep result, the eight approval files by name, the ledger path, the pinned SHA,
both versions, and an explicit statement that zero backlog items were filed and no second meta pull
request was opened.
</output>
