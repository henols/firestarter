---
phase: 187-answered-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - .planning/VALIDATED-EPROMS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REPLY-07, REPLY-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/config.json` lists all four configured sub_repos — firestarter, firestarter_app, firestarter_app_py32, firestarter_py32_ci — and `git diff -- .planning/config.json` is empty at the end of this plan"
    - "All five D-07 live sites carry the discharged state, each citing comment id 5511487546, and REPLY-07 is marked Complete in both its requirement bullet and its traceability row"
    - "REPLY-01's requirement text is amended in place with the D-11 measurement as the stated reason, and its original wording is retained rather than deleted"
    - "`.planning/ROADMAP.md` still carries the phrase `stays open as the pinned orientation issue` at both excluded sites, and no file under `.planning/milestones/` is modified by this plan"
    - "No operator scratch path is tracked in any repository: `git ls-files -- anything.txt tmp/` in the meta repo and `git ls-files -- datasheets/` in the app repo both return nothing"
  prohibitions:
    - "Must NOT widen the D-07 record sweep beyond the five named live sites; `ROADMAP.md`'s two `stays open as the pinned orientation issue` sites and every hit under `.planning/milestones/` are never repaired, because those citations are historical-by-intent and repairing them destroys the evidence"
    - "Must NOT file any backlog item, todo, or successor requirement on the verify-upstream-claims-before-promoting axis; this phase's gh#9 finding generates zero backlog items"
    - "Must NOT commit `anything.txt`, `tmp/`, or any untracked datasheet PDF, and must NOT use `git clean -Xdf` to tidy the tree"
    - "Must NOT mark REPLY-01 through REPLY-06 Complete in this plan; only REPLY-07 is discharged here"
  artifacts:
    - path: ".planning/config.json"
      provides: "the restored four-entry sub_repos list"
      contains: "firestarter_py32_ci"
    - path: ".planning/ROADMAP.md"
      provides: "the three repaired ROADMAP sites (the 5409-5411 block, the 5424 checklist line, Phase 187 success criterion 4)"
      contains: "5511487546"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REPLY-07 amended and Complete, REPLY-01 amended per D-11, the REPLY-07 traceability row"
      contains: "5511487546"
    - path: ".planning/VALIDATED-EPROMS.md"
      provides: "the gh#61 AE29F2008 PASS log entry committed"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/milestones/v1.35-phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh9.md"
      via: "REPLY-07's amendment cites the byte-identical approved body that was posted as comment 5511487546"
      pattern: "173-gh9\\.md"
---

<objective>
Make the meta working tree publishable and repair the five live records that falsely claim gh#9 still
owes a closing reply, before any public act exists to carry them.

Purpose: this is the authoring half of the phase's tracer slice. Plan 187-02's tracer publishes this
content to the public `beta` branch and proves it resolves there; that tracer cannot lead the phase
because the tree is currently dirty (RESEARCH DRIFT-4) and because D-16 requires a blocking operator
gate between "content ready" and "content published". Authoring first, publishing second, is the
minimum honest split.

Output: a clean, publishable `.planning/` tree; the five D-07 repairs; the D-11 amendment to REPLY-01.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.planning/REQUIREMENTS.md
@/workspaces/CLAUDE.md

`.planning/ROADMAP.md` is 930 KB and `.planning/STATE.md` is 510 KB. Never bare-read either. Use
`sed -n '<start>,<end>p'` for a window, or `/usr/bin/grep -n` for a heading first. The devcontainer's
bare `grep` is ugrep and honours `.gitignore`; every evidence leg in this plan uses `/usr/bin/grep`.

This phase must never be dispatched under `--auto` or `--chain`. Those modes auto-approve human-verify
gates, and `autonomous: true` on this plan means only that this plan contains no public act — it is not
a statement about the phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the meta and app working trees publishable</name>
  <files>.planning/config.json, .planning/VALIDATED-EPROMS.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Drift from CONTEXT.md, DRIFT-4 (the exact `config.json` diff, the scratch inventory, the `git clean -Xdf` prohibition)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Common Pitfalls, Pitfall 4 and Pitfall 5
    - `.planning/config.json` (the live file, to confirm the prune is still present)
    - `git diff -- .planning/VALIDATED-EPROMS.md` (the +6 lines, to confirm they are the gh#61 AE29F2008 PASS log entry)
  </read_first>
  <action>
    Run `cd /workspaces && git status --short` first and record the output verbatim into the plan's
    SUMMARY, because everything below is conditional on what is actually dirty at execution time.

    Restore the sub_repos prune with `git checkout -- .planning/config.json`. The prune deletes the
    entries `firestarter_app_py32` and `firestarter_py32_ci` from `planning.sub_repos`; it is a known
    `loop render-hooks` side effect, not an edit anyone made, and it must be reverted rather than
    committed. Re-check it immediately before staging anything and again in Plan 187-02 before the PR
    is opened, because the plan-phase flow re-introduces it.

    Stage and commit `.planning/VALIDATED-EPROMS.md` by explicit path. Its uncommitted +6 lines are the
    gh#61 AE29F2008 PASS log entry, they match the existing table shape, and they are intended content
    that belongs in the meta PR — the orchestrator has already dispositioned this, so commit it rather
    than asking.

    Stage and commit the two untracked phase artifacts `187-RESEARCH.md` and `187-PATTERNS.md` under
    `.planning/phases/187-answered-reports/` by explicit path.

    Leave `anything.txt` (a 492-byte operator scratch note at the meta repo root) and `tmp/` (45 MB of
    bench photographs) untracked and unstaged. Leave `firestarter_app/datasheets/MBM27C1001.pdf` and
    `firestarter_app/datasheets/MX27C4000.pdf` untracked — they are gh#65 and gh#66 research material
    for deferred work and must not be swept into the app PR.

    Use `git add <explicit path>` only. Never `git add -A`, never `git add .`, never `git clean -Xdf` —
    `.planning/state.json` and the v1.34 bench `.bin` files are gitignored in this repository and
    `git clean -Xdf` would destroy them.
  </action>
  <verify>
    <automated>cd /workspaces && git diff -- .planning/config.json > /tmp/187-cfg.diff && wc -l < /tmp/187-cfg.diff</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'firestarter_py32_ci' .planning/config.json | wc -l</automated>
    <fails_when>prints 0</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'firestarter_app_py32' .planning/config.json | wc -l</automated>
    <fails_when>prints 0</fails_when>
    <automated>cd /workspaces && git ls-files -- anything.txt tmp/ > /tmp/187-scratch.txt && wc -l < /tmp/187-scratch.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces/firestarter_app && git ls-files -- datasheets/ > /tmp/187-ds.txt && wc -l < /tmp/187-ds.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces && git ls-files -- .planning/VALIDATED-EPROMS.md .planning/phases/187-answered-reports/187-RESEARCH.md .planning/phases/187-answered-reports/187-PATTERNS.md > /tmp/187-tracked.txt && wc -l < /tmp/187-tracked.txt</automated>
    <fails_when>prints anything other than 3, or non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff -- .planning/config.json` produces no output, and both `firestarter_app_py32` and `firestarter_py32_ci` appear in the file.
    - `git ls-files -- anything.txt tmp/` returns nothing in `/workspaces`.
    - `git ls-files -- datasheets/` returns nothing in `/workspaces/firestarter_app`.
    - `.planning/VALIDATED-EPROMS.md`, `187-RESEARCH.md` and `187-PATTERNS.md` are all tracked.
    - The SUMMARY records the verbatim `git status --short` output taken before any staging.
  </acceptance_criteria>
  <done>The meta tree contains only intended, tracked content plus untracked operator scratch; the app tree carries no staged datasheet PDF; the sub_repos prune is reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the five D-07 live sites and amend REPLY-01 per D-11</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-07 (the five-site repair set and the explicit exclusions) and § D-11 (the REPLY-01 amendment and its care note)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 5 (all five citations re-verified, the note on site 1's heading, the note on site 4's wrapped bullet, and §5.2's correction of the exclusion count)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Record repairs 1-2, 3, 4 and 5 (the AMENDED-clause form, with the four elements every analog carries)
    - `sed -n '5405,5430p' .planning/ROADMAP.md` and `sed -n '488,510p' .planning/ROADMAP.md`
    - `sed -n '100,125p' .planning/REQUIREMENTS.md` and `sed -n '208,220p' .planning/REQUIREMENTS.md`
  </read_first>
  <action>
    Edit both files BY HAND. Do not use `gsd-tools` requirements or roadmap verbs — they run a whole-file
    normalizer that reformats far beyond the edit, and `roadmap.update-plan-progress` additionally
    clobbers the dependency table by positional overwrite.

    Apply the AMENDED-clause form the analogs use, and carry all four of its elements in every repair:
    keep or quote the original wording rather than deleting it; open the clause with a bold marker naming
    the phase and the decision, in the shape `**AMENDED by Phase 187 (D-07):**`; state the measured
    reason the original is wrong; say where the substance went instead. Keep every `D-NN` label on a
    single line.

    Site 1 — the `.planning/ROADMAP.md` block currently at 5409-5411. The heading
    `**Absorbed 999.16 / gh#9 remains partially owed:**` is itself part of what is false, so the repair
    covers the heading and the three lines, not line 5411 alone. Replace the owed state with the
    discharged state: Phase 173 posted an operator-approved body verbatim as comment id 5511487546 at
    the URL `https://github.com/henols/firestarter_prom/issues/9#issuecomment-5511487546` on
    2026-09-02, and gh#9 is deliberately left open and pinned as the configured end state.

    Site 2 — `.planning/ROADMAP.md` line 5424, the checklist line whose trailing clause reads
    `gh#9 still owes a closing reply`. Replace that clause with the discharged state and the same
    comment id, in the same AMENDED-with-reason form.

    Site 3 — `.planning/ROADMAP.md` line 502, Phase 187 success criterion 4
    (`4. gh#9 carries a closing reply or is closed as done.`). Amend it in place: the criterion is
    satisfied by the pre-existing comment, gh#9 stays open and pinned, and Phase 187 posts nothing new
    on gh#9 per D-06.

    Site 4 — `.planning/REQUIREMENTS.md` lines 118-119, REPLY-07's two-line wrapped bullet. Flip the
    checkbox to `[x]`, keep the original text intact, and append the AMENDED clause. Copy CLAIM-08's
    shape at lines 144-150 most closely, because REPLY-07's situation is the same kind (a requirement
    already discharged when it was written): state that it was discharged by comment 5511487546 on
    2026-09-02, eight days before REPLY-07 was filed on 2026-09-10; that gh#9 stays open and pinned as
    the deliberate configured end state; cite the byte-identical approved body at
    `.planning/milestones/v1.35-phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh9.md`
    and the evidence at `173-07-SUMMARY.md:115-118`; and name the 183 / 184 / 185 precedent chain by
    phase number exactly as CLAIM-08 does.

    Site 5 — `.planning/REQUIREMENTS.md` line 217, the row `| REPLY-07 | Phase 187 | Pending |`. Set the
    Status cell in the register the other completed rows use, carrying the disposition, the amendment
    and a plan trace: discharged by comment 5511487546 on 2026-09-02 in Phase 173-07, amended under
    D-06 and D-07 by plan 187-01.

    D-11 amendment — `.planning/REQUIREMENTS.md`, REPLY-01's bullet. Leave its checkbox unticked; only
    the wording is amended here, and REPLY-01 is not delivered until its reply is posted. Append an
    AMENDED clause in the same form recording the measurement: `chip_test.py:2599` fires the status axis
    only on `(SerialError, HardwareOperationError)`, while a VPP-not-hooked-up fault raises
    `EpromOperationError` and lands at `chip_test.py:2607` with `verdict=BAD` and `status=COMPLETE`, so
    `submit.py`'s `overall_verdict` still returns `FAIL` and a fresh run still files as
    `[dev test] w27e257 — FAIL`. State that the literal original wording — what a fresh run would now
    show — would be an overclaim, that v1.36 did not and cannot make the tool see an unhooked VPP, and
    that what did change is fault attribution and reporting: Phase 181 deleted `voltage.vpp_mv` and
    `vpe_mv` from the report and Phase 178 added `rail_reading_disclosure`. Name 183 D-08, 184 D-03 and
    185 D-02 as the precedent for amending in the same phase as the work.

    Do not touch `ROADMAP.md`'s two excluded sites — both currently read that gh#9 `stays open as the
    pinned orientation issue`, which is literally true and configured. Do not touch any file under
    `.planning/milestones/`.

    Do not restate D-07's "10 files" figure anywhere. The measured count of `.planning/milestones/`
    files mentioning gh#9 is 33, not 10; the fence is right and only its stated magnitude was wrong.
    State the count only if you re-measure it, or omit it.

    Do not mark REPLY-01 through REPLY-06 Complete. Only REPLY-07 is discharged by this plan.
  </action>
  <verify>
    <automated>cd /workspaces && /usr/bin/grep '5511487546' .planning/ROADMAP.md | wc -l</automated>
    <fails_when>prints 0, 1 or 2 — the three repaired ROADMAP sites must each cite the comment id</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '5511487546' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints 0</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'AMENDED by Phase 187' .planning/ROADMAP.md | wc -l</automated>
    <fails_when>prints 0, 1 or 2</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'AMENDED by Phase 187' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints 0 or 1 — REPLY-07 and REPLY-01 each need their own clause</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '\[x\] \*\*REPLY-07\*\*' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 1</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '^| REPLY-07 | Phase 187 | Complete' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 1</fails_when>
    <automated>cd /workspaces && /usr/bin/grep '\[ \] \*\*REPLY-0[1-6]\*\*' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>prints anything other than 6 — a REPLY-01..06 checkbox was flipped early</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'stays open as the pinned orientation issue' .planning/ROADMAP.md | wc -l</automated>
    <fails_when>prints anything other than 2 — an excluded site was edited</fails_when>
    <automated>cd /workspaces && git status --porcelain -- .planning/milestones > /tmp/187-ms.txt && wc -l < /tmp/187-ms.txt</automated>
    <fails_when>prints anything other than 0, or non-zero exit</fails_when>
    <automated>cd /workspaces && git status --porcelain > /tmp/187-st.txt && /usr/bin/grep -v '^??' /tmp/187-st.txt | wc -l</automated>
    <fails_when>prints anything other than 0 after the plan's commits, or non-zero exit — a tracked file is left uncommitted</fails_when>
  </verify>
  <acceptance_criteria>
    - All three ROADMAP repair sites and both REQUIREMENTS repair sites cite comment id `5511487546`.
    - `AMENDED by Phase 187` appears at least 3 times in `ROADMAP.md` and at least 2 times in `REQUIREMENTS.md`.
    - REPLY-07's bullet is `[x]` and its traceability row reads `Complete` with the comment id and a plan trace.
    - Exactly six `- [ ] **REPLY-0N**` bullets remain (REPLY-01 through REPLY-06), so no other requirement was flipped early.
    - `stays open as the pinned orientation issue` still appears exactly twice in `ROADMAP.md`.
    - `git status --porcelain -- .planning/milestones` is empty.
    - The phrase "10 files" does not appear in anything this plan writes; if a count is stated it is a re-measured one.
  </acceptance_criteria>
  <done>Every live record that claimed gh#9 owes a reply now states it was discharged and cites the comment; the fence held; REPLY-01's wording is honest about what v1.36 could and could not change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → public `beta` branch of `henols/firestarter_prom` | Everything committed here becomes world-readable when Plan 187-02 merges. There is no redaction step downstream. |
| GSD tooling → `.planning/` config | `loop render-hooks` mutates `config.json` without an author; an unreviewed commit publishes that mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-01 | Information Disclosure | `anything.txt`, `tmp/`, `firestarter_app/datasheets/*.pdf` | high | mitigate | Task 1 stages by explicit path only; `git ls-files` legs assert each scratch path stays untracked in both repos; `git add -A` and `git clean -Xdf` are named as forbidden |
| T-187-02 | Tampering | `.planning/config.json` `planning.sub_repos` | high | mitigate | Task 1 reverts with `git checkout --` and asserts an empty diff plus the presence of both pruned entries; Plan 187-02 re-checks immediately before the PR |
| T-187-03 | Repudiation | `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` | medium | mitigate | The AMENDED-clause form keeps the original wording, so the correction is auditable rather than a silent rewrite |
| T-187-04 | Tampering | `.planning/milestones/` historical citations | medium | mitigate | D-07's fence is a prohibition in `must_haves`, and a `git status --porcelain -- .planning/milestones` leg proves nothing under it changed |
| T-187-05 | Information Disclosure | `.planning/state.json`, v1.34 bench `.bin` files | medium | mitigate | `git clean -Xdf` is forbidden by name; removal is by explicit path only |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Phase-wide inventory. The rows marked **(this plan)** are created or modified here; the rest are listed
so a reader of any single plan sees the whole output set.

| Artifact | Produced by |
|---|---|
| `.planning/config.json` (prune reverted) | **(this plan)** |
| `.planning/VALIDATED-EPROMS.md` (gh#61 PASS entry committed) | **(this plan)** |
| `.planning/ROADMAP.md` (three D-07 repairs) | **(this plan)** |
| `.planning/REQUIREMENTS.md` (REPLY-07 amended + Complete, traceability row, REPLY-01 amended) | **(this plan)** |
| `.planning/phases/187-answered-reports/187-MERGE-RECORD.md` | 187-02, 187-03, 187-04, 187-12 |
| `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` | 187-05, 187-06, 187-12 |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md`, `187-gh60.md`, `187-gh62.md` | 187-05, 187-06 |
| `evidence/187-02-meta-merge.txt` (the pinned 40-char SHA) | 187-02 |
| `evidence/187-03-app-cut.txt`, `evidence/187-04-fw-cut.txt` | 187-03, 187-04 |
| `evidence/187-05-issue-state-before.json` | 187-05 |
| `evidence/187-05-draft-link-check.txt`, `evidence/187-06-draft-link-check.txt` | 187-05, 187-06 |
| `evidence/187-07-gh60-operator-approval.txt` and one sibling per posted issue | 187-07 … 187-11 |
| `evidence/187-07-post-transcript.txt` and one sibling per posted issue | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions — spec-less probe, unresolved rows

The spec-less probe produced 11 applicable edges across this phase's requirements: 6 classified rows
(REPLY-02 and REPLY-05, each with adjacency / empty / ordering) are authored into `must_haves.truths`
on the plans that own them (187-05, 187-06, 187-09, 187-10, 187-11), and 5 `unclassified` rows stay
unresolved and are surfaced here rather than auto-resolved or dropped. 11 == 6 authored + 5 flagged.

| Requirement | Probe | Status | Why it is flagged rather than resolved |
|---|---|---|---|
| REPLY-01 | unclassified | unresolved | The probe produced no edge category for it. Its acceptance rests on D-11's amended wording, which is a judgement about honesty of claim, not a mechanical boundary. |
| REPLY-03 | unclassified | unresolved | No edge category. Its acceptance rests on whether the reply carries all four load-bearing claims of the AE29F2008 verdict in substance. |
| REPLY-04 | unclassified | unresolved | No edge category. Its acceptance rests on whether the reply confirms the reporter's own limit claim from the project's schematic record. |
| REPLY-06 | unclassified | unresolved | No edge category. It is a prohibition (no unilateral close), authored as such in `must_haves.prohibitions` on 187-09/10/11 and asserted by state legs in 187-12, but the probe row itself stays unresolved. |
| REPLY-07 | unclassified | unresolved | No edge category. Discharged by a pre-existing comment rather than by new work, so there is no boundary for the probe to classify. |

## Prohibition-probe breadcrumb

Canon security and compliance items (OWASP categories, injection, path traversal, GDPR) were recalled
and dropped per the canon-referral rule — this phase writes no product source, accepts no untrusted
input, and processes no personal data beyond public GitHub handles the reporters chose themselves.
The surviving prohibitions are values and safety items and are authored descriptor-less in
`must_haves.prohibitions`.

<verification>
- `git diff -- .planning/config.json` empty; both pruned sub_repos entries restored.
- All five D-07 sites repaired, each citing comment id 5511487546; both excluded sites untouched.
- REPLY-07 `[x]` and Complete in the traceability table; REPLY-01 amended but still `[ ]`.
- Nothing under `.planning/milestones/` modified.
- `anything.txt`, `tmp/` and both datasheet PDFs still untracked.
- No backlog item, todo or successor requirement filed by this plan.
</verification>

<success_criteria>
The meta tree is publishable by explicit-path staging alone, and no live record in `.planning/` still
claims gh#9 owes a closing reply.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-01-SUMMARY.md` when done. Record the verbatim
pre-staging `git status --short` output, the exact text of each of the six amendments, and the
statement that zero backlog items were filed.
</output>
