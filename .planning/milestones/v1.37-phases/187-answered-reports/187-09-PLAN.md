---
phase: 187-answered-reports
plan: 09
type: execute
wave: 9
depends_on: ["187-08"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/187-09-gh23-operator-approval.txt
  - .planning/phases/187-answered-reports/evidence/187-09-post-transcript.txt
  - .planning/phases/187-answered-reports/evidence/187-09-comment-id.txt
autonomous: false
requirements: [REPLY-01, REPLY-05, REPLY-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The comment posted to gh#23 is byte-identical to `evidence/bodies/187-gh23.md` as approved, proven by a JSON round trip that exits non-zero on mismatch"
    - "gh#23 is still OPEN after this plan, and its label set is exactly the pre-existing set plus the labels the operator approved"
    - "`cause:database` is still present on gh#23 after the label edit, because the database defect it names is real and unfixed"
    - "The posted body carries the REPLY-05 statement, so the reporter is told before running that a fresh report will not group with the old one"
  prohibitions:
    - "Must NOT close gh#23; a close requires the reporter's confirmation or a superseding PASS, and a unilateral close is the failure mode this milestone exists to correct"
    - "Must NOT remove `cause:database` or any other pre-existing label from gh#23; the label change is additive and the body says which label moved and why"
    - "Must NOT post any body whose sha256 differs from the one recorded in the approval file"
    - "Must NOT verify the posted comment with `gh api --jq '.body'` piped into `diff`; that form reports a spurious mismatch on a provably identical comment"
    - "Must NOT touch gh#28, gh#31, gh#60, gh#62 or gh#9 in this plan; approval for gh#23 carries to no other issue"
    - "Must NOT be dispatched under `--auto` or `--chain`; those modes auto-approve the gate in Task 1 and `autonomous: false` is not self-protecting against them"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/187-09-gh23-operator-approval.txt"
      provides: "the per-issue approval literal, the label decision, and the sha256 of the body as approved"
      contains: "APPROVED-FOR-POST: gh23"
    - path: ".planning/phases/187-answered-reports/evidence/187-09-post-transcript.txt"
      provides: "the command, exit code and returned URL for the post, plus the label edits"
    - path: ".planning/phases/187-answered-reports/evidence/187-09-comment-id.txt"
      provides: "the posted comment id alone on line 1"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/187-09-gh23-operator-approval.txt"
      to: ".planning/phases/187-answered-reports/evidence/bodies/187-gh23.md"
      via: "a sha256 line binding the approval to the exact bytes posted"
      pattern: "sha256\\("
---

<objective>
Post REPLY-01 to gh#23, add the labels the operator approves, and leave the issue open.

Purpose: the reporter diagnosed a rig fault being reported as a chip verdict and was argued against. The
reply concedes that in full, states honestly that the tool did not and cannot become able to see an
unhooked VPP, names what did change in fault attribution and reporting, and keeps the unfixed database
defect on the record. The issue stays open unconditionally.

Output: one posted comment, an additive label change, and an issue still open.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.claude/skills/devtest-triage/SKILL.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

**This plan performs a public, irreversible act and must never be dispatched under `--auto` or
`--chain`.** If the harness is in either mode, stop and report rather than proceeding.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Approve the exact body to post to gh#23 and the exact labels to add</decision>
  <read_first>
    - `.planning/phases/187-answered-reports/evidence/bodies/187-gh23.md` in full — the exact bytes being approved
    - `.planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md` — the dispositions table and this issue's status line
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-14, § D-15 and § D-16
    - `.claude/skills/devtest-triage/SKILL.md` — the label taxonomy, for the candidates presented
  </read_first>
  <context>
    D-16 requires a separate blocking checkpoint immediately before each public act. The gh#60 and gh#62
    approvals carry nothing here. This gate approves gh#23 only.

    Show the operator four things:

    1. The full verbatim contents of `evidence/bodies/187-gh23.md`, with its sha256 from
       `evidence/187-06-body-hashes.txt`. This is the exact file passed to `--body-file` unmodified.
    2. The label change, which D-14 fixes rather than leaves open: add `cause:rig` alongside the standing
       `cause:database`, because the programming-voltage defect is real and unfixed so that label was
       never wrong, and add `needs:report` because the thread waits on a fresh run. Both labels exist in
       the repository. State that this is additive and that no existing label is removed, and that the
       body names the change and the reason so the reclassification is not silent.
    3. The two honesty points the body turns on, so the operator can judge them directly: that the reply
       states a fresh run today would still report the write and verify steps as bad and still file under
       a FAIL title, and that the reply concedes the reporter's general point without naming or implying
       a w27e257 run that passed, because the tracker carries no such run.
    4. That the issue is not being closed, and will not be until the reporter confirms or a superseding
       PASS exists.

    Confirm with the operator that the harness is not running under `--auto` or `--chain`.

    If the operator amends any wording, apply it to `evidence/bodies/187-gh23.md` and to the inline copy
    in `187-UPSTREAM-REPLIES.md` first, save a unified diff of the amendment against a scratchpad copy of
    the pre-approval draft beside the body, then recompute the sha256 and write the approval file.

    Write `evidence/187-09-gh23-operator-approval.txt` with the literal `APPROVED-FOR-POST: gh23` alone
    on line 1, then the operator's name and date, then the decision prose explicitly labelled as the
    orchestrator's rendering of a menu selection plus its option description rather than a verbatim
    operator quotation, then the approved label list, then a single
    `sha256(evidence/bodies/187-gh23.md) = <hash>` line.
  </context>
  <options>
    <option id="approve">
      <name>Approve as drafted, adding `cause:rig` and `needs:report`</name>
      <pros>The reporter is told plainly they were right, told exactly what a fresh run would and would not show, and the unfixed defect stays on the public record</pros>
      <cons>Public and permanent the moment it posts</cons>
    </option>
    <option id="amend">
      <name>Approve with wording or label amendments</name>
      <pros>The operator's exact wording ships; the amendment's scope is recorded as a diff</pros>
      <cons>The body, its inline copy and its hash must all be updated before the post</cons>
    </option>
    <option id="hold">
      <name>Do not post yet</name>
      <pros>Nothing is public</pros>
      <cons>REPLY-01 stays undelivered and a reporter who was right stays unanswered</cons>
    </option>
  </options>
  <resume-signal>Select: approve, amend (and state the amendment), or hold</resume-signal>
  <acceptance_criteria>
    - `evidence/187-09-gh23-operator-approval.txt` exists and its first line is exactly `APPROVED-FOR-POST: gh23`.
    - The file carries the operator's name, the date, and decision prose explicitly labelled as the orchestrator's rendering of a menu selection plus its option description rather than a verbatim operator quotation.
    - The file carries exactly one `sha256(evidence/bodies/187-gh23.md) = <hash>` line, and that hash equals `sha256sum` of the body on disk at the moment the file was written.
    - The file records the approved label list verbatim.
    - If the operator amended the wording, the amendment was applied to both the body file and its inline copy, a unified diff of it was saved beside the body, and the hash was recomputed after the amendment.
    - Nothing was posted before this file existed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Post to gh#23, prove byte-identity, add the labels, leave it open</name>
  <reversibility rating="one-way">A comment on a public issue is visible to every watcher the instant it posts and is delivered by email; it can be edited but never un-seen.</reversibility>
  <precondition>`evidence/187-09-gh23-operator-approval.txt` exists, its first line is exactly `APPROVED-FOR-POST: gh23`, and its recorded sha256 equals `sha256sum evidence/bodies/187-gh23.md`</precondition>
  <files>.planning/phases/187-answered-reports/evidence/187-09-post-transcript.txt, .planning/phases/187-answered-reports/evidence/187-09-comment-id.txt, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 6.3 and § Finding 6.4 (the posting command and the byte-identity trap), § Finding 2 (gh#23's pre-phase comment count and label set)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Posting — the command, and the read-back that does not false-alarm
    - `.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json` (the authoritative pre-phase counts)
    - `.planning/phases/187-answered-reports/evidence/187-09-gh23-operator-approval.txt`
  </read_first>
  <action>
    Re-assert the gate mechanically: recompute `sha256sum evidence/bodies/187-gh23.md` and confirm it
    equals the hash recorded in the approval file. If it differs, stop.

    Post with `gh issue comment 23 --repo henols/firestarter_prom --body-file evidence/bodies/187-gh23.md`.
    Pass the file; never interpolate the body onto the command line. Capture the returned URL, write the
    comment id from its trailing fragment alone on line 1 of `evidence/187-09-comment-id.txt`, and write
    a transcript block into `evidence/187-09-post-transcript.txt` with the command, exit code and URL.

    Prove byte-identity by reading the comment back through the API as JSON and comparing the decoded
    body against the file with a python round trip that exits non-zero on mismatch. Do not use
    `gh api --jq '.body'` piped into `diff`. Record both lengths and both hashes in the transcript.

    Add exactly the labels the operator approved with
    `gh issue edit 23 --repo henols/firestarter_prom --add-label "<label>"` per label. Use only
    `--add-label`; do not pass `--remove-label` for anything. `cause:database` must survive the edit.
    Record each command and exit code.

    Do not close the issue. Re-read gh#23 and record its state, stateReason and resulting label set,
    reconciling the label set field by field against the pre-phase set in
    `evidence/187-05-issue-state-before.json`: the result must be the old set plus the approved additions
    and nothing else.

    Flip gh#23's status line in `187-UPSTREAM-REPLIES.md` to a posted state and fill its Posted line with
    the comment URL, noting the issue was deliberately left open. Leave the two remaining status lines
    pending.

    Touch no other issue.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/187-09-gh23-operator-approval.txt</automated>
    <fails_when>prints anything other than `APPROVED-FOR-POST: gh23`</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh23.md | cut -d' ' -f1 | /usr/bin/grep -qF -f - evidence/187-09-gh23-operator-approval.txt && echo BOUND</automated>
    <fails_when>prints nothing, or non-zero exit — the approval file's hash does not cover the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && gh api "repos/henols/firestarter_prom/issues/comments/$(head -1 evidence/187-09-comment-id.txt)" > /tmp/c187gh23.json && python3 -c "import json,hashlib,sys; b=json.load(open('/tmp/c187gh23.json'))['body'].encode(); d=open('evidence/bodies/187-gh23.md','rb').read(); print('api',len(b),hashlib.sha256(b).hexdigest()); print('disk',len(d),hashlib.sha256(d).hexdigest()); print('BYTE-IDENTICAL' if b==d else 'MISMATCH'); sys.exit(0 if b==d else 1)"</automated>
    <fails_when>prints MISMATCH, or non-zero exit</fails_when>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN — gh#23 was closed, which REPLY-06 forbids unconditionally</fails_when>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 4 — the pre-phase count recorded in evidence/187-05-issue-state-before.json was 3, so any value other than that count plus one means a missing or duplicated post</fails_when>
    <automated>gh issue view 23 --repo henols/firestarter_prom --json labels --jq '[.labels[].name] | sort | join(",")'</automated>
    <fails_when>the output omits `cause:database`, omits `cause:rig`, omits `needs:report`, or contains a label absent from the approval file's list combined with the pre-phase set</fails_when>
    <automated>gh issue view 28 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 4 — gh#28's pre-phase count; this plan must not comment there</fails_when>
    <automated>gh issue view 31 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 2 — gh#31's pre-phase count; this plan must not comment there</fails_when>
    <automated>gh issue view 9 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints anything other than 1 — this phase posts nothing new on gh#9</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints anything other than 2 — either gh#23's line was not flipped, or another issue's line was flipped early</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-09-gh23-operator-approval.txt` line 1 is exactly `APPROVED-FOR-POST: gh23`, and its sha256 line matches `sha256sum evidence/bodies/187-gh23.md`.
    - The API-read comment body is byte-identical to the file; the round trip prints `BYTE-IDENTICAL` and exits 0.
    - `gh issue view 23 --json state` returns `OPEN`.
    - gh#23's comment count equals its pre-phase count from the before-capture plus exactly one.
    - gh#23's label set equals the pre-phase set plus exactly the approved additions, with `cause:database` still present.
    - gh#28, gh#31 and gh#9 comment counts are unchanged from the before-capture.
    - `187-UPSTREAM-REPLIES.md` has exactly two pending status lines remaining.
  </acceptance_criteria>
  <done>REPLY-01 is delivered as approved, gh#23 is labelled additively with the reasons stated in the body, and the issue remains open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved body on disk → public issue comment | The moment of publication; visible to every watcher and delivered by email. |
| a label edit → the existing label set | `--remove-label` or a wholesale set operation would silently drop `cause:database`, erasing the record of a defect that is still unfixed. |
| issue state → REPLY-06 | Closing this issue on our own reading is the specific failure this milestone exists to correct. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-44 | Repudiation | posting a body the operator never approved | critical | mitigate | Per-issue approval literal plus sha256 binding as the task's `<precondition>`, re-asserted before the post, with a byte-identity read-back after |
| T-187-45 | Elevation of Privilege | closing gh#23 | critical | mitigate | Closing is a named prohibition and an unconditional one; the post-plan state is asserted `OPEN` by an automated leg and re-asserted in 187-12 |
| T-187-46 | Tampering | dropping `cause:database` during the label edit | high | mitigate | Only `--add-label` is permitted; the resulting set is reconciled field by field against the pre-phase capture and `cause:database` is asserted present |
| T-187-47 | Repudiation | implying a w27e257 run that passed | high | mitigate | Named prohibition in 187-06, re-surfaced at this gate as an explicit honesty point for the operator to judge |
| T-187-48 | Tampering | a false mismatch from the naive read-back | high | mitigate | The naive form is a named prohibition; only the JSON round trip is permitted |
| T-187-49 | Elevation of Privilege | auto-mode approving the gate | critical | mitigate | `gate="blocking-human"`, the never-dispatch constraint in plan text, and an explicit mode confirmation at the checkpoint |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*`, `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| the five `evidence/bodies/187-gh*.md` payloads, both link checks, both hash files, `187-UPSTREAM-REPLIES.md` | 187-05, 187-06 |
| the gh#60 and gh#62 approval files, transcripts, comment-id files, and the window-start file | 187-07, 187-08 |
| `evidence/187-09-gh23-operator-approval.txt`, `evidence/187-09-post-transcript.txt`, `evidence/187-09-comment-id.txt` | **(this plan)** |
| the gh#28 and gh#31 approval files, transcripts and comment-id files | 187-10, 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-01 and REPLY-06 are `unclassified` spec-less probe rows and stay unresolved; the full 11-row
  accounting is in `187-01-PLAN.md`. REPLY-06 is additionally enforced here as a prohibition and by an
  automated state assertion, but the probe row itself remains unresolved rather than auto-resolved.
- The expected comment counts in the verify block are the values measured on 2026-09-12. The
  before-capture in `evidence/187-05-issue-state-before.json` is the authoritative baseline; if a
  reporter commented in the interval, the expected value is that recorded count plus one.

<verification>
- The approval file exists with its literal, its label decision and a hash covering the posted body.
- The posted comment reads back byte-identical.
- gh#23 is OPEN, carries one more comment than the before-capture, and retains `cause:database`.
- gh#28, gh#31 and gh#9 are untouched.
- Two pending status lines remain in the review document.
</verification>

<success_criteria>
The reporter who was told their chip was at fault is told, on the record, that the fault was in the rig
and that the triage argued against them wrongly — and is told exactly what a fresh run would still show.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-09-SUMMARY.md` when done. Record the comment URL, both
hashes from the round trip, the before and after label sets, and the confirmation that the issue remains
open.
</output>
