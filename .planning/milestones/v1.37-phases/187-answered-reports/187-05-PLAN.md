---
phase: 187-answered-reports
plan: 05
type: execute
wave: 5
depends_on: ["187-04"]
files_modified:
  - .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md
  - .planning/phases/187-answered-reports/evidence/bodies/187-gh60.md
  - .planning/phases/187-answered-reports/evidence/bodies/187-gh62.md
  - .planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json
  - .planning/phases/187-answered-reports/evidence/187-05-draft-link-check.txt
  - .planning/phases/187-answered-reports/evidence/187-05-body-hashes.txt
autonomous: true
requirements: [REPLY-03, REPLY-04, REPLY-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`evidence/bodies/187-gh60.md` and `evidence/bodies/187-gh62.md` are each a bare markdown body with no YAML frontmatter, no phase metadata and no top-level title heading, so the first line is the first sentence the reporter reads"
    - "gh#60's body answers the reporter's own question in the words the project's schematic record settled: writing and erasing energize socket pin 1; reading, verifying, blank-checking and `id` do not"
    - "gh#60's body confirms the reporter's own limit claim — the gate warns and refuses and cannot detect the jumper — and quotes the shipped hazard text and the JP5 silkscreen so the operator can check it against the board in front of them"
    - "gh#62's body states both halves of the equivalence verdict at once: the classification is correct, and the reporter is correct that the silicon can be chip-erased"
    - "gh#62's body carries all four load-bearing claims of the AE29F2008 verdict's REPLY-03 section in substance: the erase was real, it used the documented six-cycle chip-erase sequence, it was benign only because that chip pair happens to share a pinout and a voltage class, and identity forgery with `--force` is not a safe workaround in general"
    - "Neither body asks the reporter for a `dev test` re-run, and `187-UPSTREAM-REPLIES.md` records per body whether it carries a re-run ask, so the REPLY-05 sentence is required only where an ask exists"
    - "Every link in both bodies resolves, and `evidence/187-05-draft-link-check.txt` records the resolved status of each one at the pinned meta merge SHA"
    - "Every version string appearing in either body is byte-equal to line 1 of `evidence/187-03-app-version.txt` or line 1 of `evidence/187-04-fw-version.txt`"
  prohibitions:
    - "Must NOT state or imply that gh#65 or gh#66 are unanswered; both received a maintainer datasheet cross-check on 2026-09-10 and remain open, and a body saying otherwise would post a false statement into the public tracker in the phase whose premise is not doing that"
    - "Must NOT post, label, close or otherwise mutate any issue in this plan; this plan writes to disk only and every public act is gated in 187-07 onward"
    - "Must NOT imply that a protocol `0x05` software chip-erase is delivered, scheduled or promised; it is backlog 999.63, unbuilt, and carries the W29C020C boot-block caveat"
    - "Must NOT imply that editing `chip_database.json` by hand is the fix for anything; the database is generated and any correction belongs in the generator's decode function"
    - "Must NOT name any version other than the two read after the cuts, and must NOT promise a timeline for any deferred work"
  artifacts:
    - path: ".planning/phases/187-answered-reports/evidence/bodies/187-gh60.md"
      provides: "REPLY-04's posting payload, passed unmodified to `gh issue comment --body-file`"
    - path: ".planning/phases/187-answered-reports/evidence/bodies/187-gh62.md"
      provides: "REPLY-03's posting payload, passed unmodified to `gh issue comment --body-file`"
    - path: ".planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md"
      provides: "the review document: dispositions table, per-issue status literals, and each body inline byte-identically to its file"
      contains: "PENDING OPERATOR REVIEW"
    - path: ".planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json"
      provides: "the pre-post API state of all six issues plus the pinnedIssues read"
    - path: ".planning/phases/187-answered-reports/evidence/187-05-draft-link-check.txt"
      provides: "every link in both drafts, resolved"
    - path: ".planning/phases/187-answered-reports/evidence/187-05-body-hashes.txt"
      provides: "one sha256 line per drafted body, for the approval gates to bind against"
  key_links:
    - from: ".planning/phases/187-answered-reports/evidence/bodies/187-gh62.md"
      to: ".planning/notes/ae29f2008-classification-verdict.md at the pinned meta merge SHA"
      via: "a commit-SHA permalink with the anchor copied from the rendered page in 187-02"
      pattern: "blob/[0-9a-f]{40}/\\.planning/notes/ae29f2008-classification-verdict\\.md"
    - from: ".planning/phases/187-answered-reports/evidence/bodies/187-gh60.md"
      to: ".planning/notes/jumper-display-ground-truth.md at the pinned meta merge SHA"
      via: "a commit-SHA permalink with the anchor copied from the rendered page in 187-02"
      pattern: "blob/[0-9a-f]{40}/\\.planning/notes/jumper-display-ground-truth\\.md"
---

<objective>
Draft the two replies whose issues carry no prior comment — gh#60 (JP5 hazard, REPLY-04) and gh#62
(AE29F2008 erase, REPLY-03) — to disk, with every link resolved at the pinned SHA and every version read
from the cuts.

Purpose: these are the two issues where the answer is the deliverable rather than a concession. Both
reporters asked a direct question and this milestone has a settled, measured answer to each. Drafting
them first proves the body shape, the permalink mechanics and the version wiring on the two simplest
cases before the three disputed `dev test` threads are drafted in 187-06.

Output: two posting payloads, the review document, the before-state capture, a link check and a hash
record.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/187-answered-reports/187-CONTEXT.md
@.planning/phases/187-answered-reports/187-RESEARCH.md
@.planning/phases/187-answered-reports/187-PATTERNS.md
@.planning/notes/ae29f2008-classification-verdict.md
@.planning/notes/jumper-display-ground-truth.md
@/workspaces/CLAUDE.md

Use `/usr/bin/grep` for every evidence leg.

This plan writes to disk only. It posts nothing, labels nothing and closes nothing. It is nonetheless
part of a phase that must never be dispatched under `--auto` or `--chain`: those modes auto-approve the
human-verify gates in the posting plans downstream, and `autonomous: true` here is a statement about
this plan alone, not about the phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the live pre-post state of all six issues</name>
  <files>.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 2 (the per-issue table and the exact `gh issue view` command shape), § 2.1 (the pinnedIssues read), § 2.3 (gh#68 and the existence of gh#67)
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Drift from CONTEXT.md, DRIFT-2 (gh#65 and gh#66 are answered)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Before/after issue-state capture
  </read_first>
  <action>
    Create `.planning/phases/187-answered-reports/evidence/bodies/` if it does not exist.

    Read the live state of issues 9, 23, 28, 31, 60 and 62 from the API with
    `gh issue view <N> --repo henols/firestarter_prom --json number,title,state,stateReason,labels,comments`
    reduced to number, state, stateReason, label names and comment count, and write the results to
    `evidence/187-05-issue-state-before.json`. Append the GraphQL `pinnedIssues` read for the repository
    to the same file — that leg is D-06's verification only, and this phase never calls `pinIssue`.

    Re-verify the three facts a body could otherwise get wrong, and record each with its command and
    output: that gh#65 and gh#66 each carry a maintainer datasheet cross-check dated 2026-09-10 and are
    still OPEN; that gh#68 is OPEN and names AE29F2008 in its body; and that gh#61 is CLOSED as
    COMPLETED with `chip:validated`. CONTEXT.md's Deferred Ideas block describes gh#65 and gh#66 as
    unanswered and that is no longer true; the acknowledgement in gh#62's body must say they are open,
    never that they are unanswered.

    Record that gh#67 also exists — protocol `0x05` firmware-derived page size smaller than the physical
    page on 9 parts — so that no body describes gh#68's scope as covering the page-size defect too.
    D-12 fences the gh#62 cross-link at gh#68 only; do not widen it.

    Record the exact label sets each issue carries right now. gh#62 carries zero labels, so D-14's
    instruction to label it to match its disposition means adding the first labels this issue has ever
    had rather than moving one.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "import json;print(open('.planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json').read().count('\"state\"'))"</automated>
    <fails_when>prints a number below 6 — fewer than the six target issues were captured</fails_when>
    <automated>cd /workspaces && /usr/bin/grep 'pinnedIssues' .planning/phases/187-answered-reports/evidence/187-05-issue-state-before.json | wc -l</automated>
    <fails_when>prints 0 — the D-06 pin verification was not captured</fails_when>
    <automated>gh issue view 65 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints 0 — gh#65 would then genuinely be unanswered and the DRIFT-2 rewording must be re-derived before drafting</fails_when>
    <automated>gh issue view 66 --repo henols/firestarter_prom --json comments --jq '.comments | length'</automated>
    <fails_when>prints 0 — gh#66 would then genuinely be unanswered and the DRIFT-2 rewording must be re-derived before drafting</fails_when>
    <automated>gh issue view 68 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN — the D-12 cross-link would point at a closed issue</fails_when>
    <automated>gh issue view 62 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
    <automated>gh issue view 60 --repo henols/firestarter_prom --json state --jq .state</automated>
    <fails_when>prints anything other than OPEN</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-05-issue-state-before.json` contains one record each for issues 9, 23, 28, 31, 60 and 62 with state, stateReason, label names and comment count, plus the `pinnedIssues` GraphQL response.
    - The file records, with commands and outputs, that gh#65 and gh#66 each carry at least one comment dated 2026-09-10 and are OPEN; that gh#68 is OPEN and names AE29F2008; that gh#61 is CLOSED/COMPLETED with `chip:validated`; and that gh#67 exists and is a separate defect.
    - The file records gh#62's label set as empty.
  </acceptance_criteria>
  <done>The pre-post state of every issue this phase touches is on disk, and the three facts a body could get wrong are re-measured rather than inherited.</done>
</task>

<task type="auto">
  <name>Task 2: Draft gh#60 and gh#62 bodies and open the review document</name>
  <files>.planning/phases/187-answered-reports/evidence/bodies/187-gh60.md, .planning/phases/187-answered-reports/evidence/bodies/187-gh62.md, .planning/phases/187-answered-reports/187-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-CONTEXT.md` § D-13 (register and linking mechanics), § D-14 (the reply says which label moved and why), § D-15 (gh#60 closes, gh#62 stays open), § Specific Ideas (the four instructions naming gh#60 and gh#62 directly)
    - `.planning/notes/jumper-display-ground-truth.md` § `Which operations energize socket pin 1` — the direct answer to gh#60
    - `.planning/notes/ae29f2008-classification-verdict.md` § the REPLY-03 material section and § the software chip-erase section carrying backlog 999.63 and the boot-block caveat
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 9.1 (the four load-bearing claims, and that every number the reply cites is in the reporter's own paste), § 9.2 (the shipped hazard text and its four call sites), § 9.3 (the one-line refusal format)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § the five reply bodies (bare-body shape, opening-move pattern, bolded-section labels, concede-then-hold) and § `187-UPSTREAM-REPLIES.md`
    - `.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt` (the pinned SHA and the two copied anchors)
    - `.planning/phases/187-answered-reports/evidence/187-03-app-version.txt` and `evidence/187-04-fw-version.txt`
  </read_first>
  <action>
    Write each body as a bare markdown body: no YAML frontmatter, no title heading, no phase metadata.
    The first line is the first sentence the reporter reads. Organise with short bolded section labels
    rather than markdown headings, which render oversized in a GitHub comment. Keep it short and plain
    per D-13: lead with the answer in the first two sentences, state what changed and what to do, and
    link the evidence rather than inlining firmware line numbers.

    Write `evidence/bodies/187-gh60.md` for REPLY-04. It must do six things. Address AstoriaFloyd and
    answer their question in these words: writing and erasing energize socket pin 1; not reading, not
    verifying, not blank-checking, not `id`. Confirm that they are right that the jumper's state cannot
    be checked systematically, and confirm it from the project's own schematic record rather than as an
    opinion — quote the shipped hazard text's own statement that the tool cannot read JP5's current state
    on the attached board, and quote the silkscreen legend about cutting for ROMs with A19 on pin 1 so
    the operator can check the claim against the board in front of them. State what shipped: a warning
    and a refusal on write and erase, with an interactive confirmation as the only escape, in the app
    version on line 1 of `evidence/187-03-app-version.txt`, installable with
    `pip install --pre -U firestarter`. Link `.planning/notes/jumper-display-ground-truth.md` as a
    commit-SHA permalink at the pinned SHA from `evidence/187-02-meta-merge.txt`, using the anchor copied
    from the rendered page in Plan 187-02 — never a `blob/beta/` link, which moves on every merge and
    every CI version bump, and never `main`, which lags `beta`. State the disposition and the label
    change in the body itself per D-14: the issue is being closed as done because the feature they asked
    for shipped in a released version, and name which label moved and why rather than reclassifying
    silently. Do not ask for a `dev test` re-run; this is a feature request being answered, not a
    validation report.

    Write `evidence/bodies/187-gh62.md` for REPLY-03. Address dim20 and tell them plainly, in the first
    two sentences, that they were right: the classification is correct and they are correct that the
    silicon can be chip-erased. The reply must not choose between those; the verdict is equivalence-based
    and says both at once. Then carry, in substance rather than in the maintainer's register, the four
    load-bearing claims of the verdict's REPLY-03 section: that the erase they performed under the other
    identity was real, not a no-op; that it used the documented six-cycle software chip-erase command
    sequence, cycle for cycle identical to the W29C020C datasheet's own command table, and that the
    50 ms internal erase plus serial round trips is consistent with the 0.14 s they observed; that it was
    benign only because that particular chip pair happens to share a pinout and a voltage class and
    because the erase path energises no programming rail, which is a coincidence of that pair and not a
    property of the force flag; and that forging a different identity onto a different real part could
    route a mismatched bus configuration or energise a rail the target part does not expect, so it is not
    a safe workaround in general. Rewrite all four for a user, not for a maintainer reading header line
    numbers. State what changed: the refusal now names the part instead of printing a bare unsupported
    message, in the app version on line 1 of `evidence/187-03-app-version.txt`. Say what they actually
    want and where it stands: a software chip-erase for the protocol `0x05` family is real work that has
    not been done, it is backlogged as 999.63, and the W29C020C datasheet documents a boot-block caveat
    any implementation must handle — once the boot block programming lockout is activated the chip erase
    function is disabled. Promise no timeline. Cross-link gh#68 per D-12, describing its scope accurately
    as partial or unaligned writes on protocol `0x05` silently erasing the rest of the touched page while
    reporting success; do not describe it as covering the separate page-size defect. Note the useful
    context from gh#61, their own PASS report on the same part: read, write and blank-check all pass, and
    only erase is refused. Link `.planning/notes/ae29f2008-classification-verdict.md` as a commit-SHA
    permalink at the pinned SHA with the copied anchor. State the disposition per D-15 and D-14: the
    issue stays open, because closing it would file "we refuse, correctly" as the end of the story while
    the capability they want remains unbuilt — and name the labels being added and why. Add the
    one-line acknowledgement of gh#65 and gh#66 the operator asked for, worded so that it says those two
    issues are open with no timeline promised, never that they are unanswered; flag this rewording
    explicitly in the review document so the operator sees the drift at the gate and can decide whether
    the line survives at all.

    Neither body asks for a `dev test` re-run, so neither is required to carry the REPLY-05
    `schema_version` sentence that D-17 mandates for every reply that does ask. Record that decision
    explicitly per body in the review document, with the reason, so the absence reads as a decision
    rather than an omission.

    Create `187-UPSTREAM-REPLIES.md` following the 173 analog. Open with a header block carrying the
    date, the requirements served, and — because D-16 requires a per-issue gate rather than one
    document-level approval — a separate status line per issue, each starting at the literal
    `PENDING OPERATOR REVIEW`. Add a `## Operator Review` section explaining that each issue's status
    line is flipped only by that issue's own posting plan, and that any verdict recorded there is the
    orchestrator's rendering of a menu selection rather than a verbatim operator quotation. Add a
    `## Dispositions` table with issue, title and disposition for all five issues this phase replies to,
    written before the remaining bodies exist. Add a `## Pre-post state` section pointing at
    `evidence/187-05-issue-state-before.json`. Then add one `## gh#N` section for gh#60 and gh#62, each
    naming its body file, carrying a Posted line left blank for now, and reproducing the body inline
    byte-identically to the file — the standalone file is what gets passed to `--body-file` unmodified,
    so what the operator approves is exactly what gets posted.

    Do not post anything. Do not add or remove a label. Do not close anything.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/bodies/187-gh60.md | /usr/bin/grep -E '^(---|#)' | wc -l</automated>
    <fails_when>prints anything other than 0 — the body opens with frontmatter or a heading instead of the first sentence the reporter reads</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && head -1 evidence/bodies/187-gh62.md | /usr/bin/grep -E '^(---|#)' | wc -l</automated>
    <fails_when>prints anything other than 0</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -E 'blob/[0-9a-f]{40}/' evidence/bodies/187-gh60.md evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints a number below 2 — a body carries no 40-char commit-SHA permalink</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -E 'blob/(beta|main)/' evidence/bodies/187-gh60.md evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints anything other than 0 — a branch link was used, and branch links rot on every merge and every CI version bump</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -F "$(head -1 evidence/187-02-merge-sha.txt)" evidence/bodies/187-gh60.md evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints a number below 2 — a permalink pins a SHA other than the one meta merge commit this phase pinned</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -F "$(head -1 evidence/187-03-app-version.txt)" evidence/bodies/187-gh60.md evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints a number below 2 — a body does not name the app version that was actually cut</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'PENDING OPERATOR REVIEW' 187-UPSTREAM-REPLIES.md</automated>
    <fails_when>prints a number below 5 — the review document does not carry one pending status line per replied-to issue</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep '999.63' evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints 0 — gh#62's body does not name where the capability the reporter wants actually stands</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep 'issues/68' evidence/bodies/187-gh62.md | wc -l</automated>
    <fails_when>prints 0 — the D-12 cross-link to gh#68 is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - Both body files begin with prose, not with `---` and not with `#`.
    - Both bodies contain at least one `blob/<40 hex>/` permalink and zero `blob/beta/` or `blob/main/` links, and every permalink pins the SHA on line 1 of `evidence/187-02-merge-sha.txt`.
    - Both bodies name the exact app version on line 1 of `evidence/187-03-app-version.txt`.
    - gh#60's body contains the phrase naming writing and erasing as the energising operations and naming reading, verifying, blank-checking and `id` as the ones that do not, quotes the shipped hazard text's statement that the tool cannot read JP5's state, and quotes the silkscreen legend.
    - gh#62's body contains `999.63`, a link to `issues/68`, the boot-block caveat, and a statement that the classification is correct and the reporter is correct, both.
    - gh#62's acknowledgement of gh#65 and gh#66 says they are open; the string `unanswered` does not appear anywhere in either body file.
    - `187-UPSTREAM-REPLIES.md` carries at least five `PENDING OPERATOR REVIEW` status lines, a `## Dispositions` table covering all five replied-to issues, and one `## gh#N` section each for 60 and 62 with the body reproduced inline.
    - The review document states, per body, whether it carries a re-run ask and therefore whether the REPLY-05 sentence is required.
  </acceptance_criteria>
  <done>Two posting payloads exist on disk in the exact form they will be posted, with every permalink pinned and every version read rather than predicted.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve every link in both drafts and hash-bind the bodies</name>
  <files>.planning/phases/187-answered-reports/evidence/187-05-draft-link-check.txt, .planning/phases/187-answered-reports/evidence/187-05-body-hashes.txt</files>
  <read_first>
    - `.planning/phases/187-answered-reports/187-RESEARCH.md` § Finding 7.2 (the anchor caution) and § Finding 7.3 (why a pre-merge SHA is worse than a 404)
    - `.planning/phases/187-answered-reports/187-PATTERNS.md` § Permalinks and § `evidence/187-operator-approval.txt` (the hash-binding shape and its generation command)
    - `.planning/phases/187-answered-reports/evidence/187-02-meta-merge.txt` (the two copied anchors)
  </read_first>
  <action>
    Extract every URL from both body files and resolve each one, writing the URL, the method used and the
    result into `evidence/187-05-draft-link-check.txt`. For each permalink into this repository, confirm
    the file exists at the pinned SHA via the GitHub contents API with a `?ref=<SHA>` query, and confirm
    the decoded content contains the heading the anchor points at. For each issue cross-link, confirm the
    issue number resolves and record its current state. A link that resolves at a SHA at or before the
    meta merge's parent is a failure even though it returns 200, because
    `jumper-display-ground-truth.md` exists there as a stub without the cited section — record the pinned
    SHA beside every permalink so that is visible.

    Confirm each anchor against the two slugs copied from the rendered pages in Plan 187-02. Do not
    re-derive a slug from the heading text; both headings contain a backtick-quoted token, an em-dash and
    a `#` inside `gh#60`, and a wrong anchor silently lands the reader at the top of the document.

    Write one `sha256(<path>) = <hash>` line per body file into `evidence/187-05-body-hashes.txt`,
    computed over the bodies exactly as they stand. These are the values the per-issue approval gates in
    Plans 187-07 and 187-08 bind against; if a body is amended at its gate, the hash is recomputed after
    the amendment and before the post, never before.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'sha256(' evidence/187-05-body-hashes.txt</automated>
    <fails_when>prints anything other than 2 — one hash line per drafted body is required</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh60.md | cut -d' ' -f1 | /usr/bin/grep -F -f - evidence/187-05-body-hashes.txt | wc -l</automated>
    <fails_when>prints 0 — the recorded hash does not match the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && sha256sum evidence/bodies/187-gh62.md | cut -d' ' -f1 | /usr/bin/grep -F -f - evidence/187-05-body-hashes.txt | wc -l</automated>
    <fails_when>prints 0 — the recorded hash does not match the body on disk</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -c 'http' evidence/187-05-draft-link-check.txt</automated>
    <fails_when>prints 0 — no link was resolved</fails_when>
    <automated>cd /workspaces/.planning/phases/187-answered-reports && /usr/bin/grep -o 'https://[^) ]*' evidence/bodies/187-gh60.md evidence/bodies/187-gh62.md | sed 's/^[^:]*://' | sort -u | while read -r U; do /usr/bin/grep -qF "$U" evidence/187-05-draft-link-check.txt || echo "UNRESOLVED $U"; done | wc -l</automated>
    <fails_when>prints anything other than 0 — a URL appears in a body but has no entry in the link-check file</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/187-05-draft-link-check.txt` carries one entry per distinct URL in both bodies, each with the method used and the result, and each permalink entry names the pinned SHA it was resolved at.
    - Every `https://` URL present in either body file also appears in the link-check file.
    - Each in-repository permalink was confirmed with `gh api .../contents/<path>?ref=<pinned SHA>` and its decoded content confirmed to contain the heading the anchor targets.
    - Both anchors match the slugs copied from the rendered pages in Plan 187-02, not slugs derived from heading text.
    - `evidence/187-05-body-hashes.txt` contains exactly two `sha256(<path>) = <hash>` lines, and each hash equals `sha256sum` of the body on disk.
  </acceptance_criteria>
  <done>Every link in both drafts is proven to resolve to the content it is cited for, and both bodies are hash-bound for their approval gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drafted body on disk → the public tracker | Everything written here is destined to be posted verbatim; a wrong claim drafted here becomes a wrong public claim two plans later. |
| in-repo evidence document → a reporter's browser | A permalink is a promise about what the reader will see; a pre-merge SHA silently breaks that promise without a visible error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-187-22 | Spoofing | permalinks in both bodies | high | mitigate | Every URL is resolved at the pinned SHA via the contents API and recorded; branch links are grep-forbidden; anchors are copied from rendered pages rather than derived |
| T-187-23 | Repudiation | a false claim about gh#65 and gh#66 | high | mitigate | Their answered state is re-measured in Task 1 with the command and output recorded; the string `unanswered` is asserted absent from both bodies; the rewording is flagged at the gate for the operator to decide |
| T-187-24 | Tampering | a body drifting between draft and post | high | mitigate | One sha256 per body is recorded and is what the approval gates bind against; the standalone file is the exact `--body-file` payload |
| T-187-25 | Information Disclosure | a version named in a body that was predicted rather than read | high | mitigate | Version strings are grep-matched against line 1 of the two cut-version files, which were themselves cross-checked against the live release lists |
| T-187-26 | Repudiation | implying the `0x05` chip-erase is scheduled | medium | mitigate | Named prohibition; the body states 999.63 as unbuilt with the boot-block caveat and promises no timeline |
| T-187-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; there is no supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Rows marked **(this plan)** are created here.

| Artifact | Produced by |
|---|---|
| the four `.planning/` record repairs | 187-01 |
| `evidence/187-02-*`, `evidence/187-03-*`, `evidence/187-04-*` and `187-MERGE-RECORD.md` sections 1-5 | 187-02, 187-03, 187-04 |
| `evidence/bodies/187-gh60.md`, `evidence/bodies/187-gh62.md` | **(this plan)** |
| `187-UPSTREAM-REPLIES.md` (header, dispositions, gh#60 and gh#62 sections) | **(this plan)**; extended by 187-06, flipped by 187-07 … 187-11 |
| `evidence/187-05-issue-state-before.json`, `evidence/187-05-draft-link-check.txt`, `evidence/187-05-body-hashes.txt` | **(this plan)** |
| `evidence/bodies/187-gh23.md`, `187-gh28.md`, `187-gh31.md` and `evidence/187-06-*` | 187-06 |
| one operator-approval file and one post transcript per posted issue | 187-07 … 187-11 |
| `evidence/187-12-issue-state-after.json`, `evidence/187-12-collateral-check.txt` | 187-12 |
| `.planning/notes/v137-upstream-reply-ledger.md` | 187-12 |

## Flagged planner assumptions carried into this plan

- REPLY-03 and REPLY-04 are `unclassified` spec-less probe rows and stay unresolved; the full 11-row
  accounting is in `187-01-PLAN.md`. Whether each body carries its requirement's substance is judged by
  the operator at the gate in 187-07 and 187-08, not by a command here.
- RESEARCH § Assumptions Log A3 rates the specific labels for gh#60 and gh#62 as `[ASSUMED]`, because
  D-14 defers them to "match their disposition" without naming them. This plan does not choose them; it
  records the candidates and their grounds in the review document so the operator decides at the gate.
- RESEARCH § Assumptions Log A2 rates `--reason completed` for gh#60's close as `[ASSUMED]`; D-15 says
  "closes as done" without naming the flag. Carried to the gate in 187-07.
- Open Question 2 in RESEARCH asks whether the dim20 acknowledgement survives at all given DRIFT-2. This
  plan drafts it re-worded and flags it; the operator decides at the gh#62 gate.

<verification>
- Both bodies exist as bare markdown, pin the one meta merge SHA, and name the read app version.
- Every URL in both bodies resolves and is recorded in the link-check file.
- Both bodies are hash-bound in `evidence/187-05-body-hashes.txt`.
- `187-UPSTREAM-REPLIES.md` carries a pending status line per issue and a dispositions table.
- Nothing was posted, labelled or closed.
</verification>

<success_criteria>
Two replies exist on disk in exactly the form they will be posted, with every factual claim traceable to
a measurement in this phase's evidence tree, and nothing public has changed.
</success_criteria>

<output>
Create `.planning/phases/187-answered-reports/187-05-SUMMARY.md` when done. Record the two body hashes,
the pinned SHA used, the two anchors, the versions named, the DRIFT-2 rewording as drafted, and the
label candidates being carried to the operator gate.
</output>
