---
phase: 101-fw-apply-names-in-firmware
plan: 02
type: execute
wave: 2
depends_on: ["101-00", "101-01"]
files_modified:
  - firestarter/CLAUDE.md
files_read_only:
  - firestarter/include/eprom.h
  - firestarter/include/sram.h
  - firestarter/include/flash_type_3.h
  - firestarter/include/flash_type_4.h
  - firestarter/include/eeprom_28c.h
  - firestarter/include/flash_intel.h
  - firestarter/include/not_implemented.h
autonomous: true
requirements: [FW-03, GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "All 7 handler families (eprom/sram/flash4/flash3/eeprom28c/flash_intel/not-implemented) are CONFIRMED conformant to the approved family-name layer — the approved names already equal the existing configure_* function/file names, so FW-03 is a no-op rename (D-01)"
    - "firestarter/CLAUDE.md's dispatch-order list + Algorithm-Handlers table reflect the new PROTO_ tokens (no dangling old prose names), matching the relabeled memory.cpp line-for-line"
    - "The full guard suite is GREEN: pio test -e native 82/82, pio run -e uno + leonardo SUCCESS, check_dispatch.py PASS, diff_db.py identity, constants-parity 6 pass, test_dispatch_mirror.py green, py3.11 ruff/mypy clean"
  artifacts:
    - "firestarter/CLAUDE.md (dispatch-order + Algorithm-Handlers tables synced to PROTO_ tokens)"
    - "101-02-SUMMARY.md documenting the FW-03 conformance assertion (7/7 families, zero renames)"
  key_links:
    - "The conformance assertion binds PROTOCOLS.md Handler-family layer to the actual configure_*/.cpp names (all NONE delta)"
    - "The GATE integration assertion binds all six guard commands green over the Wave-0 + Wave-1 output"
  prohibitions:
    - "No handler function or .cpp file rename (D-01 — a rename would touch 100+ refs and re-open Phase 100; approved names already match)"
    - "No chip_database.json content change (diff_db.py identity — GATE-02)"
    - "No numeric value change to any protocol number or wire/lockstep constant (GATE-02); no PROTO_ block added to constants.py (D-02 — parity test count stays 6)"
    - "No protocol name/token becomes a dispatch or lookup key (GATE-01 — numbers stay authoritative)"
    - "No CLI grammar change; no protocol name/alias accepted as CLI input (GATE-03 — no CLI file touched)"
    - "No change to a handler function signature, extern C frame, or include guard (only an OPTIONAL cross-reference comment is permitted)"
---

<objective>
Confirm FW-03 conformance (the 7 many-to-one handler families already match Phase 100's approved family-name layer — a no-op rename per D-01), sync `firestarter/CLAUDE.md`'s dispatch/handler documentation to the new PROTO_ tokens, and run the full GATE-01/02/03 integration assertion proving no regression across the relabel. This is the phase-closing plan: it depends on Wave-0 (dispatch-mirror green) and Wave-1 (relabeled firmware).

Purpose: FW-03 is satisfied by conformance confirmation, NOT a wholesale rename — the approved family names ARE the existing `configure_*`/`.cpp` names (verified). GATE-01/02/03 are first/primarily enforced in this phase; this plan proves numbers stay the dispatch key, no DB/wire/value change, and no CLI change.
Output: a doc-synced `firestarter/CLAUDE.md`, a documented conformance assertion, and a green full guard suite.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-fw-apply-names-in-firmware/101-CONTEXT.md
@.planning/phases/101-fw-apply-names-in-firmware/101-RESEARCH.md
@.planning/phases/101-fw-apply-names-in-firmware/101-PATTERNS.md
@.planning/phases/101-fw-apply-names-in-firmware/101-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert FW-03 handler-family conformance and sync firestarter/CLAUDE.md to PROTO_ tokens</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    - firestarter/include/eprom.h, sram.h, flash_type_3.h, flash_type_4.h, eeprom_28c.h, flash_intel.h, not_implemented.h (confirm the 7 function signatures)
    - firestarter/CLAUDE.md (its dispatch-order list + "Algorithm Handlers" table — currently carrying OLD prose names like EPROM_STD/FLASH_AMD_STD)
    - 101-PATTERNS.md § "handler families (CONFORMANCE-CONFIRM)" — the 7-row conformance table (approved == current, NONE delta) + the eprom.h representative idiom + the over-reach warning signs; § "firestarter/CLAUDE.md (MOD — doc sync)"
    - 101-RESEARCH.md § "Handler Inventory (FW-03 rename scope)" — approved names == current names, and Pitfall 1 (do not treat FW-03 as a wholesale rename) + Pitfall 6 (CLAUDE.md drift)
    - firestarter/doc/PROTOCOLS.md §0 bucket table + Handler-family layer table (the authoritative name set)
  </read_first>
  <action>
    (a) FW-03 conformance: verify each of the 7 handler families' current `configure_*` function name and `.cpp` file name against PROTOCOLS.md's Handler-family layer table. All 7 match exactly (eprom/configure_eprom/eprom.cpp, sram/configure_sram/sram.cpp, flash4/configure_flash4/flash_type_4.cpp, flash3/configure_flash3/flash_type_3.cpp, eeprom28c/configure_eeprom28c/eeprom_28c.cpp, flash_intel/configure_flash_intel/flash_intel.cpp, not-implemented/configure_not_implemented/not_implemented.cpp) — FW-03 is a NO-OP rename (D-01): assert conformance, do NOT rename any function/file. Do NOT change any function signature, extern-C frame, or include guard. Optionally add ONLY a cross-reference comment naming the PROTO_ tokens each handler serves above its declaration in its `.h` (comment-only, per the PATTERNS eprom.h idiom); if added, keep the signature/guard/extern-C frame byte-stable otherwise. (b) Doc sync: update `firestarter/CLAUDE.md`'s dispatch-order list and "Algorithm Handlers" table so the protocol column uses the new PROTO_ tokens (from PROTOCOLS.md) instead of the old prose names (EPROM_STD, FLASH_AMD_STD, FLASH_EEPROM, FLASH_AMD_ALT, and the rest), keeping the table line-for-line consistent with the relabeled memory.cpp dispatch order. Record the 7/7 conformance result (zero renames) in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd firestarter && grep -q configure_eprom include/eprom.h && grep -q configure_sram include/sram.h && grep -q configure_flash4 include/flash_type_4.h && grep -q configure_flash3 include/flash_type_3.h && grep -q configure_eeprom28c include/eeprom_28c.h && grep -q configure_flash_intel include/flash_intel.h && grep -q configure_not_implemented include/not_implemented.h && pio run -e uno</automated>
  </verify>
  <acceptance_criteria>
    - All 7 handler headers contain their expected `configure_*` symbol (the grep chain above exits 0), confirming zero renames (FW-03 no-op per D-01)
    - `pio run -e uno` exits 0 (any optional cross-reference comments compile cleanly)
    - firestarter/CLAUDE.md's dispatch-order list + "Algorithm Handlers" table contain the PROTO_ tokens (e.g. `PROTO_EPROM_28PIN`, `PROTO_FLASH_5V_PAGE`) and no longer contain the old prose names `EPROM_STD` / `FLASH_AMD_STD` / `FLASH_AMD_ALT` / `FLASH_EEPROM`
    - git diff shows NO change to any handler function signature, `extern "C"` frame, or include guard (only optional `// serves PROTO_...` comment lines, if added)
    - git diff shows NO handler `.cpp`/`.h` file rename
  </acceptance_criteria>
  <done>
    7/7 handler families confirmed conformant (zero renames, FW-03 satisfied per D-01); firestarter/CLAUDE.md dispatch/handler tables synced to PROTO_ tokens; uno build SUCCESS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full GATE-01/02/03 integration assertion — prove no-regression across the relabel</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    - 101-VALIDATION.md § "Host guard commands" + § "Per-Task Verification Map" (101-02-02 row) — the exact green-state for each guard
    - 101-RESEARCH.md § "Guard Machinery" (§1–§7) — exact commands + green-state for native tests, uno/leonardo build, check_dispatch.py, diff_db.py, constants-parity, dispatch-mirror, and the py3.11 ruff/mypy CI gate; § "The py3.12-masks-CI-3.11 Trap"
    - 101-PATTERNS.md § "GATE-02 no-change guards (assert identity, not edit)" — these must stay green with NO edit; constants-parity count stays 6 (D-02)
  </read_first>
  <action>
    Run the full guard suite as the phase-closing GATE-01/02/03 integration assertion (no source edits in this task — it asserts identity over the Wave-0 + Wave-1 output). GATE-01 (numbers stay the dispatch key; dispatch behavior unchanged): `pio test -e native` must be 82/82 and `test_dispatch_mirror.py` green (fixed in Wave 0); the v1.16 golden register traces stay byte-identical. GATE-02 (no DB/wire/value change): `check_dispatch.py` PASS (746 chips, 0 dispatch regressions), `diff_db.py` identity (0 new / 0 removed), constants-parity pytest 6 pass (count MUST stay 6 — no PROTO_ block was added to constants.py per D-02). Host CI gate on the py3.11 target: `ruff check firestarter/ tests/` clean, `ruff format --check firestarter/ tests/` clean, mypy watermark gate. GATE-03 (CLI unchanged): assert by diff scope — no CLI file (argparse/Click grammar) was touched in this phase; no protocol name/alias is accepted as CLI input. `pio run -e uno && pio run -e leonardo` both SUCCESS. If ANY guard is red, STOP and report — a red guard means the relabel changed behavior (it must not). Because there is no python3.11 binary in this devcontainer, follow the established precedent (all CI-scoped commands run under the local 3.12/3.13 and are documented CI-PENDING/structurally-green); note the py3.12-masks-CI-3.11 trap is LOW-exposure here (firmware-only relabel; the only host edit is the Wave-0 test parser + this phase touches no codegen-emitted file). Record every guard's result in the SUMMARY.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e uno && pio run -e leonardo && cd ../firestarter_app && python tools/check_dispatch.py && python tools/diff_db.py && python -m pytest tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` reports 82 test cases, 82 succeeded (GATE-01 — dispatch behavior unchanged)
    - `pio run -e uno` and `pio run -e leonardo` both exit 0 (`[SUCCESS]`)
    - `python tools/check_dispatch.py` prints PASS with 0 dispatch regressions and 0 consistency violations, exit 0 (GATE-02)
    - `python tools/diff_db.py` prints identity (0 new chips, 0 removed), exit 0 (GATE-02 — no chip_database.json change)
    - `pytest tests/test_revision_constants_parity.py -q` reports 6 passed (count stays 6 — no PROTO_ mirror in constants.py, D-02)
    - `pytest tests/test_dispatch_mirror.py -q` passes (Wave-0 fix holds — GATE-01)
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both clean
    - No CLI file changed anywhere in the phase diff (GATE-03 — grammar unchanged, no protocol name/alias as CLI input)
  </acceptance_criteria>
  <done>
    All GATE guards green: native 82/82, uno+leonardo build SUCCESS, check_dispatch PASS, diff_db identity, constants-parity 6 pass, dispatch-mirror green, py3.11 ruff/format clean; GATE-03 holds by diff scope (no CLI touched).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc ↔ tool ↔ firmware | Three-way dispatch bind; the integration assertion proves the relabel did not desync any leg |
| wire → dispatch | `handle->protocol` still selects the correct handler after the relabel (GATE-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-06 | Tampering | chip_database.json / wire value drift hidden behind the relabel | high | mitigate | diff_db.py identity + check_dispatch.py PASS + constants-parity 6 assert no DB/wire/value change (GATE-02) |
| T-101-07 | Spoofing | protocol name/token used as a dispatch or lookup key | high | mitigate | GATE-01 assertion — native dispatch tests + dispatch-mirror guard prove numbers stay the authoritative key; names are labels only |
| T-101-08 | Tampering | CLI grammar accepting a protocol name/alias | medium | mitigate | GATE-03 by diff scope — no CLI file touched in the phase; part-number selection unchanged |
</threat_model>

<verification>
- Full suite: `pio test -e native` (82/82), `pio run -e uno && pio run -e leonardo` (SUCCESS), `check_dispatch.py` (PASS 746), `diff_db.py` (identity), constants-parity (6 pass), `test_dispatch_mirror.py` (green), `ruff check`/`ruff format --check firestarter/ tests/` (clean).
- v1.16 golden register traces byte-identical (relabel is behavior-neutral). Any re-pin must be purely cosmetic with cited rationale — not expected.
- GATE-03 asserted by diff scope: no CLI file touched.
</verification>

<success_criteria>
FW-03 confirmed conformant (7/7 families, zero renames); firestarter/CLAUDE.md synced to PROTO_ tokens; the full GATE-01/02/03 guard suite is green with no DB/wire/value change and no CLI change.
</success_criteria>

<output>
Create `.planning/phases/101-fw-apply-names-in-firmware/101-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**New file:**
- `firestarter/include/proto_constants.h` — the firmware `PROTO_<NAME>` constant home (Plan 01)

**New `#define` tokens (14 total, all in `proto_constants.h`; value == the hex, the label IS the number):**
- `PROTO_FLASH_5V_PAGE` 0x05
- `PROTO_FLASH_NOR_UNLOCK` 0x06
- `PROTO_EPROM_28PIN` 0x07
- `PROTO_EPROM_32PIN` 0x08
- `PROTO_EPROM_24PIN` 0x0B
- `PROTO_EEPROM_PARALLEL` 0x0D
- `PROTO_SRAM_32PIN` 0x0E
- `PROTO_FLASH_INTEL` 0x10
- `PROTO_SRAM_24PIN` 0x27
- `PROTO_SRAM_28PIN` 0x28
- `PROTO_SRAM_32PIN_NVRAM` 0x29
- `PROTO_EEPROM_8051BUS` 0x34
- `PROTO_PHANTOM_0x35` 0x35 (honest phantom, D-04)
- `PROTO_PHANTOM_0x39` 0x39 (honest phantom, D-04)

**Renamed handler symbols:** NONE. FW-03 is a no-op rename — the 7 approved family names (`configure_eprom`/`eprom.cpp`, `configure_sram`/`sram.cpp`, `configure_flash4`/`flash_type_4.cpp`, `configure_flash3`/`flash_type_3.cpp`, `configure_eeprom28c`/`eeprom_28c.cpp`, `configure_flash_intel`/`flash_intel.cpp`, `configure_not_implemented`/`not_implemented.cpp`) already equal the existing symbols (D-01). Only optional `// serves PROTO_...` cross-reference comments may be added.

**NOT produced (by decision):** no `PROTO_` block in `firestarter_app/firestarter/constants.py` (D-02, firmware-only); no tokens for the infeasible 0x11/0x2A/0x2B/0x2C arm (D-04, left raw hex); the `protocol != 0` fail-closed guard stays numeric (D-04).