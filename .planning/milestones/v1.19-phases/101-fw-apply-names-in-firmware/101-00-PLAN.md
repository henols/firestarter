---
phase: 101-fw-apply-names-in-firmware
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - firestarter_app/tests/test_dispatch_mirror.py
autonomous: true
requirements: [GATE-01, FW-03]
must_haves:
  truths:
    - "test_dispatch_mirror.py is GREEN (parse_protocols_md() returns a non-empty 13-row hex→handler map, not {})"
    - "The dispatch-mirror three-way bind (doc ↔ check_dispatch tool ↔ firmware) reconciles against the Phase-100 two-table PROTOCOLS.md layout"
  artifacts:
    - "firestarter_app/tests/test_dispatch_mirror.py (parser re-pointed to the post-Phase-100 table structure)"
  key_links:
    - "parse_protocols_md() joins the §0 bucket table (hex→handler-family) with the Handler-family layer table (family→.cpp) to produce hex→.cpp"
    - "DOC_FILE_TO_FUNC (7 file→func rows) stays unchanged — no handler rename per D-01"
  prohibitions:
    - "No change to DOC_FILE_TO_FUNC values (would imply a handler rename — forbidden by D-01)"
    - "No change to check_dispatch.py, memory.cpp, or chip_database.json in this plan"
    - "No protocol name/token becomes a dispatch or lookup key (GATE-01 — numbers stay authoritative)"
---

<objective>
Reconcile the pre-existing RED dispatch-mirror guard (`firestarter_app/tests/test_dispatch_mirror.py`) so it parses the Phase-100 PROTOCOLS.md two-table layout and returns GREEN. This is the Wave-0 hard predecessor of every GATE-01 green assertion in this phase (D-03): the baseline is NOT clean — Phase 100's PROTOCOLS.md table restructure moved the `.cpp` filename out of the column `_ROW_RE` scans, so `parse_protocols_md()` currently returns `{}` and the guard fails.

Purpose: GATE-01 names this guard explicitly and requires it green. It cannot be honestly claimed green until the parser is fixed. No firmware exists to relabel yet — this plan touches only the host test's doc-side extraction logic.
Output: A green `test_dispatch_mirror.py` whose parser reads the current PROTOCOLS.md structure.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-fw-apply-names-in-firmware/101-CONTEXT.md
@.planning/phases/101-fw-apply-names-in-firmware/101-RESEARCH.md
@.planning/phases/101-fw-apply-names-in-firmware/101-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point parse_protocols_md() to the Phase-100 two-table PROTOCOLS.md layout</name>
  <files>firestarter_app/tests/test_dispatch_mirror.py</files>
  <read_first>
    - firestarter_app/tests/test_dispatch_mirror.py (the full file — the broken `_ROW_RE` at line ~52, `parse_protocols_md()` at lines ~67–80, and `DOC_FILE_TO_FUNC` at lines ~56–64)
    - 101-PATTERNS.md § "firestarter_app/tests/test_dispatch_mirror.py (MOD — Wave 0 guard fix)" — the exact broken parser site, root cause, and the recommended two-table join fix
    - 101-RESEARCH.md § "Guard Machinery §6" — the RED baseline root cause (`_ROW_RE` expected `.cpp` in column 3; it is now the frozen slug)
    - firestarter/doc/PROTOCOLS.md — the §0 bucket table (7 columns: hex | DB chip count | frozen slug | PROTO_ token | display name | handler-family | phantom?) and the "Handler-family layer" table (Handler-family | configure_* | File | Protocols)
  </read_first>
  <action>
    Fix the doc-side extraction in `parse_protocols_md()` so it returns the `{hex_int → handler_filename}` map from the CURRENT PROTOCOLS.md structure. Per D-03 and the PATTERNS two-table join recipe: (1) parse the §0 bucket table rows for `hex → handler-family` — the family word is the FIRST whitespace-delimited token of the `handler-family` column (e.g. `flash4`, `eprom`, `sram`, `flash3`, `eeprom28c`, `flash_intel`, `not-implemented`); (2) parse the "Handler-family layer" table for `family → .cpp` (its rows start with `| <family> |` then a backticked `configure_*()` then a backticked `<file>.cpp`); (3) compose to `hex → .cpp`. Rewrite `_ROW_RE` (and add any second table-row regex needed) to match the new column positions — the phantom rows 0x35/0x39 and the 0x34 row have handler-family `flash4 dispatch arm` / `not-implemented (…)`, so key on the leading family token, and map `not-implemented` → `not_implemented.cpp` to align with the existing `DOC_FILE_TO_FUNC` key. Keep `DOC_FILE_TO_FUNC` (7 file→func rows) UNCHANGED — per D-01 no handler is renamed, so the file→func mappings remain correct. Do NOT touch check_dispatch.py, memory.cpp, or any firmware file. Do NOT alter the `not_implemented` func string (no `configure_` prefix — matches check_dispatch.dispatch()). This fix is scoped to the doc-extraction regex/logic ONLY.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_dispatch_mirror.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python -m pytest tests/test_dispatch_mirror.py -q` exits 0 (was: `AssertionError: parse_protocols_md() returned an empty table`)
    - `parse_protocols_md()` returns a non-empty dict keyed by the protocol hex ints present in the §0 table (includes at least 0x05, 0x06, 0x07, 0x08, 0x0B, 0x0D, 0x0E, 0x10, 0x27, 0x28, 0x29 mapped to the correct `.cpp`)
    - `DOC_FILE_TO_FUNC` in the file is byte-identical to baseline (git diff shows no change to that dict)
    - git diff for this plan touches ONLY firestarter_app/tests/test_dispatch_mirror.py
  </acceptance_criteria>
  <done>
    test_dispatch_mirror.py passes; the parser reads the Phase-100 two-table layout; DOC_FILE_TO_FUNC unchanged; no firmware or check_dispatch.py edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc ↔ tool ↔ firmware | The dispatch-mirror three-way bind; a stale parser silently voids the guard that protects dispatch-order integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-01 | Tampering | test_dispatch_mirror.py parser | medium | mitigate | Fix parser to read the real PROTOCOLS.md structure so the guard actually asserts (a guard returning {} silently passes nothing and masks future dispatch drift) |
| T-101-02 | Repudiation | DOC_FILE_TO_FUNC | low | accept | Left unchanged; no handler rename (D-01), so the 7 mappings remain the authoritative file→func bind |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_dispatch_mirror.py -q` → passes.
- Baseline pre-existing unrelated failure `test_audit_coverage_matrix.py::test_golden_file_matches` is OUT OF SCOPE (v1.3 coverage snapshot, independent test) — do not fix it here (RESEARCH A4).
</verification>

<success_criteria>
test_dispatch_mirror.py is GREEN; parser reads the post-Phase-100 two-table layout; only that one file changed.
</success_criteria>

<output>
Create `.planning/phases/101-fw-apply-names-in-firmware/101-00-SUMMARY.md` when done.
</output>
