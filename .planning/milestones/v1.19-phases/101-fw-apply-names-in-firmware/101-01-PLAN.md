---
phase: 101-fw-apply-names-in-firmware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/include/proto_constants.h
  - firestarter/src/proms/memory.cpp
autonomous: true
requirements: [FW-01, FW-02]
must_haves:
  truths:
    - "Firmware defines a PROTO_<NAME> constant for every protocol number in the 14-token map, each value == the verbatim hex from PROTOCOLS.md (the label IS the number)"
    - "The memory.cpp dispatch chain reads by name (handle->protocol == PROTO_...) instead of raw hex for the relabeled arms, with dispatch ORDER and behavior byte-identical"
    - "The 0x05/0x35/0x39 flash4 arm carries the operator-approved phantom tokens PROTO_PHANTOM_0x35 / PROTO_PHANTOM_0x39 (per D-04)"
    - "pio run -e uno builds SUCCESS and pio test -e native stays 82/82 with zero test edits (relabel is behavior-identity)"
  artifacts:
    - "firestarter/include/proto_constants.h (new — 14 #define PROTO_<NAME> integer-literal tokens, __PROTO_CONSTANTS_H__ include guard)"
    - "firestarter/src/proms/memory.cpp (relabeled dispatch arms + #include \"proto_constants.h\")"
  key_links:
    - "memory.cpp #includes proto_constants.h alongside the existing handler includes"
    - "Each relabeled `if (handle->protocol == PROTO_...)` arm dispatches to the SAME configure_* handler in the SAME order as the raw-hex baseline"
    - "test_configure_memory.cpp (hard-coded hex handles) stays green — proving PROTO_ values == hex"
  prohibitions:
    - "No numeric value change to any protocol number (label IS the number — GATE-02)"
    - "No reordering of dispatch arms (Pitfall 5 / BLOCKER-2 first-match + fail-closed guard order is load-bearing)"
    - "The `if (handle->protocol != 0)` fail-closed guard stays NUMERIC (12V-VPP/BLOCKER-2 mitigation, D-04) — never named"
    - "The 0x11/0x2A/0x2B/0x2C infeasible arm stays RAW HEX — no approved tokens exist (D-04); do not invent names"
    - "The mem_type fallback (TYPE_EPROM etc.) is NOT relabeled — those are mem_type values, not protocol IDs"
    - "No edit to any test file, chip_database.json, or host constants.py (D-02 firmware-only)"
    - "Do NOT 'fix' PROTO_PHANTOM_0x35 / _0x39 to _35 / _39 (operator-approved spelling, D-04)"
---

<objective>
Define the firmware `PROTO_<NAME>` constant home (FW-01) and relabel the raw-hex dispatch chain in `firestarter/src/proms/memory.cpp` to those named constants (FW-02), including the operator-approved honest phantom tokens for the 0x35/0x39 flash4 arm. Every numeric value, dispatch order, and behavior stays identical — the label IS the number.

Purpose: Legibility layer on the unchanged algorithm-first dispatch. No naming is invented here — all 14 tokens are lifted verbatim from Phase 100's PROTOCOLS.md (commit `6e7bd38`). Numbers stay the authoritative dispatch key end to end (GATE-01).
Output: `firestarter/include/proto_constants.h` (new) + a relabeled `memory.cpp`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-fw-apply-names-in-firmware/101-CONTEXT.md
@.planning/phases/101-fw-apply-names-in-firmware/101-RESEARCH.md
@.planning/phases/101-fw-apply-names-in-firmware/101-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create firestarter/include/proto_constants.h with the 14 PROTO_ tokens (FW-01)</name>
  <files>firestarter/include/proto_constants.h</files>
  <read_first>
    - firestarter/include/firestarter.h (lines 1–14 include-guard/license frame; lines 34–68 the `#define NAME 0xNN` constant-block idiom; line 118 `#endif  // __FIRESTARTER_H__`) — the exact style template
    - 101-PATTERNS.md § "firestarter/include/proto_constants.h (NEW)" — the include-guard pattern, the #define-block pattern, the phantom-token caveat, and the verbatim 14-token→value map
    - 101-RESEARCH.md § "Approved Name Set (VERBATIM …)" — the 14-row hex→PROTO_ token map (do NOT paraphrase); § "Constant Home" — new-header recommendation + integer-literal (no-suffix) note; § "Common Pitfalls" Pitfall 4 — phantom identifier legality
    - firestarter/doc/PROTOCOLS.md §0 bucket table — source of truth for the token↔hex map
  </read_first>
  <action>
    Author a new header `firestarter/include/proto_constants.h` following the `firestarter.h` idiom exactly: the MIT license/comment frame, an `#ifndef __PROTO_CONSTANTS_H__` / `#define __PROTO_CONSTANTS_H__` include guard closed with `#endif  // __PROTO_CONSTANTS_H__`, and `#include <stdint.h>` if needed. Emit one bare `#define PROTO_<NAME> 0x<hex>` per the 14-row verbatim map (0x05 PROTO_FLASH_5V_PAGE, 0x06 PROTO_FLASH_NOR_UNLOCK, 0x07 PROTO_EPROM_28PIN, 0x08 PROTO_EPROM_32PIN, 0x0B PROTO_EPROM_24PIN, 0x0D PROTO_EEPROM_PARALLEL, 0x0E PROTO_SRAM_32PIN, 0x10 PROTO_FLASH_INTEL, 0x27 PROTO_SRAM_24PIN, 0x28 PROTO_SRAM_28PIN, 0x29 PROTO_SRAM_32PIN_NVRAM, 0x34 PROTO_EEPROM_8051BUS, 0x35 PROTO_PHANTOM_0x35, 0x39 PROTO_PHANTOM_0x39). Use plain hex integer literals with NO suffix — `handle->protocol` is `uint32_t` and `#define` compares fine. The two phantom identifiers contain `0x` as literal identifier text; keep the operator-approved spelling `PROTO_PHANTOM_0x35` / `PROTO_PHANTOM_0x39` verbatim (D-04) — do NOT rewrite to `_35`/`_39`. Add a brief section comment marking the two phantom lines as honest non-protocols (0 DB chips, dispatch-preserved for forward-compat) so a reader does not mistake them for real algorithms. Emit `PROTO_EEPROM_8051BUS` (0x34) for completeness/cross-reference even though memory.cpp has no 0x34 arm. Do NOT introduce any token for 0x11/0x2A/0x2B/0x2C (no approved names — D-04). Do NOT mirror any of these into host constants.py (D-02).
  </action>
  <verify>
    <automated>grep -c '^#define PROTO_' firestarter/include/proto_constants.h</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#define PROTO_' firestarter/include/proto_constants.h` returns 14
    - File contains `#define PROTO_PHANTOM_0x35 0x35` and `#define PROTO_PHANTOM_0x39 0x39` (exact identifier spelling with `0x` in the name)
    - File contains `#define PROTO_FLASH_5V_PAGE 0x05` and `#define PROTO_EEPROM_8051BUS 0x34`
    - File has an `#ifndef __PROTO_CONSTANTS_H__` include guard
    - `grep -E 'PROTO_(0x11|0x2A|0x2B|0x2C|_11|_2A)' firestarter/include/proto_constants.h` returns nothing (no infeasible-bucket tokens invented)
  </acceptance_criteria>
  <done>
    proto_constants.h exists with exactly 14 PROTO_ integer-literal #defines matching the verbatim map, include-guarded in the firestarter.h style, no infeasible-bucket tokens, no host mirror.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relabel the memory.cpp dispatch chain to named constants, preserving order (FW-02)</name>
  <files>firestarter/src/proms/memory.cpp</files>
  <read_first>
    - firestarter/src/proms/memory.cpp (lines 13–24 the handler-include block; lines 74–137 the dispatch chain — the exact substitution surface)
    - 101-PATTERNS.md § "firestarter/src/proms/memory.cpp (MOD)" — the include-add site, the dispatch-arm substitution surface with per-arm token annotations, the leave-as-raw-hex arms, and the order-preservation invariant
    - 101-RESEARCH.md § "Ground-Truth Dispatch Chain (VERBATIM …)" — the full quoted chain and the per-arm token substitution table; § "Common Pitfalls" Pitfall 5 (do not reorder) + Pitfall 6
    - firestarter/include/proto_constants.h (the tokens created in Task 1)
  </read_first>
  <action>
    Add `#include "proto_constants.h"` into the existing handler-include block (lines ~13–24) of `memory.cpp`. Then relabel the `handle->protocol == 0x..` comparisons token-for-token IN PLACE, preserving the EXACT arm order (0x10 → 0x0D → 0x06 → {0x05/0x35/0x39} → {0x07/0x08/0x0B} → {0x0E/0x27/0x28/0x29} → {0x11/0x2A/0x2B/0x2C} → `!= 0` → mem_type fallback): substitute `0x10`→`PROTO_FLASH_INTEL`, `0x0D`→`PROTO_EEPROM_PARALLEL`, `0x06`→`PROTO_FLASH_NOR_UNLOCK`; the flash4 arm `0x05 || 0x35 || 0x39`→`PROTO_FLASH_5V_PAGE || PROTO_PHANTOM_0x35 || PROTO_PHANTOM_0x39` (this is the arm FW-02 explicitly mandates get the phantom tokens, D-04); the eprom arm `0x07 || 0x08 || 0x0B`→`PROTO_EPROM_28PIN || PROTO_EPROM_32PIN || PROTO_EPROM_24PIN`; the sram arm `0x0E || 0x27 || 0x28 || 0x29`→`PROTO_SRAM_32PIN || PROTO_SRAM_24PIN || PROTO_SRAM_28PIN || PROTO_SRAM_32PIN_NVRAM`. LEAVE the `0x11 || 0x2A || 0x2B || 0x2C` infeasible arm as RAW HEX (no approved tokens — D-04). LEAVE the `if (handle->protocol != 0)` fail-closed guard NUMERIC (BLOCKER-2 / 12V-VPP mitigation — D-04); never name it. Do NOT touch the mem_type fallback (TYPE_EPROM etc. are mem_type, not protocol). Do NOT change any handler function call, the return; statements, or the arm order. Substitute the comparison literals only — this is a pure relabel with byte-identical behavior.
  </action>
  <verify>
    <automated>cd firestarter && pio run -e uno && pio test -e native -f "*test_dispatch*"</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio run -e uno` exits 0 (`[SUCCESS]`) — proves PROTO_PHANTOM_0x35/_0x39 identifiers compile
    - `pio test -e native -f "*test_dispatch*"` — all dispatch cases succeed (behavior-identity; test file unchanged)
    - `grep -c 'handle->protocol == PROTO_' firestarter/src/proms/memory.cpp` returns at least 3 (the single-protocol arms 0x10/0x0D/0x06 now read by name)
    - `grep -c 'PROTO_PHANTOM_0x35\|PROTO_PHANTOM_0x39' firestarter/src/proms/memory.cpp` returns 2 (flash4 arm carries both phantom tokens)
    - `memory.cpp` still contains `handle->protocol != 0` (numeric fail-closed guard preserved) and still contains raw `0x11`/`0x2A`/`0x2B`/`0x2C` in the infeasible arm
    - `memory.cpp` contains `#include "proto_constants.h"`
    - No edit to test_configure_memory.cpp (git diff shows it unchanged)
  </acceptance_criteria>
  <done>
    memory.cpp dispatch reads by name for the relabeled arms with byte-identical order/behavior; phantom tokens on the flash4 arm; infeasible arm + fail-closed guard stay numeric; uno build SUCCESS; dispatch native tests green with no test edit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire → dispatch | `handle->protocol` (uint32_t from the wire JSON) selects a handler in configure_memory(); a value or order change here can route a 5V part to a 12V-VPP handler (BLOCKER-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-03 | Tampering | memory.cpp dispatch arms | critical | mitigate | Relabel token-for-token in place; PROTO_ value MUST equal the hex (compile-time enforced by hard-coded-hex test_configure_memory.cpp staying green); NEVER reorder arms (first-match semantics) |
| T-101-04 | Elevation | `protocol != 0` fail-closed guard (line ~116) | high | mitigate | Preserve exactly and numeric — routes unknown/0x34 to configure_not_implemented, preventing the mem_type 12V fallback; pinned by test_not_implemented |
| T-101-05 | Tampering | silent value change behind a "cosmetic" relabel | high | mitigate | pio test -e native (82/82) + golden frame-vectors + `pio run -e uno` byte-count check prove behavior-identity |
</threat_model>

<verification>
- `cd firestarter && pio run -e uno` → `[SUCCESS]` (byte count ~identical to baseline; `#define` is compile-time).
- `pio test -e native` → 82/82 (run in the Wave/phase gate).
- The v1.16 golden register traces stay byte-identical (relabel does not touch register behavior); if any re-pin is somehow needed it MUST be purely-cosmetic with cited rationale (dispatch order/behavior unchanged) — not expected for a pure relabel.
</verification>

<success_criteria>
proto_constants.h defines all 14 tokens (values == hex); memory.cpp reads by name for the relabeled arms with byte-identical order/behavior; phantom tokens present; fail-closed + infeasible arms numeric; uno build SUCCESS; native dispatch tests green with no test edits.
</success_criteria>

<output>
Create `.planning/phases/101-fw-apply-names-in-firmware/101-01-SUMMARY.md` when done.
</output>
