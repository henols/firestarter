---
phase: 100-name-canonical-protocol-name-set-operator-approval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/doc/PROTOCOLS.md
autonomous: false
requirements: [NAME-01, NAME-02, NAME-03]
must_haves:
  truths:
    - "firestarter/doc/PROTOCOLS.md Canonical bucket set table carries a 3-field entry (PROTO_ token + display name + handler-family) for all 12 DB protocols + 2 phantom rows, with the frozen datasheet slug column retained verbatim."
    - "Every §1.1–§1.12 per-bucket section has its col-2 name line updated to the new PROTO_ token + display name, while the four-facet prose and the §1.10/§1.12 NAME-04 call-outs are preserved verbatim."
    - "0x0E and 0x29 (both 32-pin SRAM) map to two DISTINCT PROTO_ tokens and two distinct display names — the D-05 collision is resolved."
    - "The operator explicitly approved the rendered Canonical bucket set table and the 0x0E/0x29 tiebreak at a blocking gate before the doc was committed as authoritative — no silent auto-approval."
    - "The §3 INV-01..09 matrix is byte-for-byte unchanged (SAFE-02 grep-intact contract): grep -c 'INV-0' PROTOCOLS.md == 34, same as baseline."
  artifacts:
    - "firestarter/doc/PROTOCOLS.md (revised in place — the single authoritative source Phases 101/102/103 cite)"
  key_links:
    - "The revised Region-A Canonical bucket set table IS the operator-approval artifact for the NAME-02 gate."
    - "The frozen datasheets/<hex>-<NAME>/ slug column sitting next to the new name IS the DOC-02 divergence record."
    - "The handler-family column names the existing many-to-one configure_* groupings so Phase 101 FW-03 renames draw from this source."
---

<objective>
Author the single canonical, behavior/datasheet-correct name set for every protocol
number in `chip_database.json` (0x05/06/07/08/0B/0D/0E/10/27/28/29/34) plus the two
phantom IDs (0x35/0x39, flagged non-real), get the operator to explicitly approve it at a
blocking gate (resolving the 0x0E-vs-0x29 collision at approval), and record it in
`firestarter/doc/PROTOCOLS.md` revised in place — the ONE authoritative source Phases
101/102/103 each cite.

This is a NAMING/DECISION phase. The deliverable is a vocabulary + an operator decision
recorded in a doc, NOT executable code. No `chip_database.json` change, no firmware/host
edits, no wire/lockstep-constant change (GATE-01/02/03).

Purpose: Produce the contract every downstream v1.19 phase consumes — Phase 101 reads the
`PROTO_` tokens + handler-family layer, Phase 102 reads the display names, Phase 103
reconciles the facet prose + INV matrix. The authoritative source is
`firestarter/doc/PROTOCOLS.md` revised in place (per D-06 — no new source file; NAME-03
"one identifiable source").
Output: A revised, operator-approved `firestarter/doc/PROTOCOLS.md`.
</objective>

<artifacts_this_phase_produces>
Newly-created symbols/artifacts this phase introduces (source-grounding pass MUST exclude
these from drift/acknowledgement verification — they do not exist upstream yet):

- **`PROTO_<NAME>` C-identifier tokens** — one per protocol, authored as STRINGS in the doc
  only (they are NOT `#define`d anywhere until Phase 101). Illustrative set (final values
  settled at the NAME-02 gate): `PROTO_FLASH_5V_PAGE` (0x05), `PROTO_FLASH_NOR_UNLOCK` (0x06),
  `PROTO_EPROM_28PIN` (0x07), `PROTO_EPROM_32PIN` (0x08), `PROTO_EPROM_24PIN` (0x0B),
  `PROTO_EEPROM_PARALLEL` (0x0D), `PROTO_SRAM_32PIN` (0x0E), `PROTO_FLASH_INTEL` (0x10),
  `PROTO_SRAM_24PIN` (0x27), `PROTO_SRAM_28PIN` (0x28), `PROTO_SRAM_32PIN_LARGE` (0x29,
  the 0x0E tiebreak candidate), `PROTO_EEPROM_X88C64` (0x34), and the two flagged phantom
  tokens `PROTO_PHANTOM_35` / `PROTO_PHANTOM_39` (0x35/0x39). Operator has final say on
  every string at the gate.
- **Short human display names** — one per protocol (field 2), authored in the doc.
- **Handler-family name layer** — a new "handler-family" column (or short companion table)
  in the Canonical bucket set table naming the 7 existing `configure_*` groupings
  (EPROM family = 0x07/08/0B; SRAM family = 0x0E/27/28/29; single-protocol families for
  0x05/0x06/0x0D/0x10; not-implemented for 0x34).
- **Two new phantom rows (0x35/0x39)** in the Canonical bucket set table, flagged non-real.
- **A revised Canonical bucket set table header + intro** describing the new 3-field schema.
- **A Phase-100 footer provenance line** recording the name-set revision + operator-approval
  date.

No new files are created. No `#define`, enum, function, or source symbol is created in this
phase (those are Phase 101). All the above live inside the single revised
`firestarter/doc/PROTOCOLS.md`.
</artifacts_this_phase_produces>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-CONTEXT.md
@.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-RESEARCH.md
@.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the draft 3-field name set + phantom rows + handler-family layer in PROTOCOLS.md (revise-in-place)</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - `firestarter/doc/PROTOCOLS.md` — the CURRENT file being revised. Read fully so you edit
      the real structure, not an assumed one. Note: Region A = the "Canonical bucket set"
      table (currently lines ~22–35, 5 columns: `hex | DB chip count | handler | datasheets
      folder (col 1) | algorithm-axis name (col 2)`); Region B = §1.1–§1.12 per-bucket
      sections, each with a `**Algorithm-axis name (col 2):**` line; Region C = §2 non-protocols
      (§2.1 phantom table lines ~325–328 naming 0x35 `FLASH_EEPROM` / 0x39 `FLASH_EEPROM2`);
      Region D = §3 INV-01..09 matrix + footer.
    - `.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-RESEARCH.md`
      — §Protocol Inventory (the verified 12-protocol + 2-phantom set, chip counts, handler
      files, frozen slugs), §Handler-Family Layer (the 7 configure_* groupings), §Code Examples
      (the illustrative target table shape), §Common Pitfalls (esp. Pitfall 1 scope-creep,
      Pitfall 3 don't-forget-0x34, Pitfall 4 phantom-token spelling).
    - `.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-PATTERNS.md`
      — the Region A/B/C/D revise-in-place shape + exact line ranges + preserve-vs-change rules.
    - `.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-CONTEXT.md`
      — D-01 (3 fields), D-02 (`PROTO_<NAME>` form), D-03 (chip-family/behavior axis, drop
      `AMD`/`QUICK`/`ALT` jargon), D-04 (pin-count primary, voltage → facet prose), D-07 (table
      columns), D-08 (phantom tokens flagged non-real), D-09 (handler-family layer).
    - `firestarter/src/proms/memory.cpp` lines 107–167 — READ-ONLY grounding: the raw-hex
      dispatch chain showing the exact many-to-one groupings (line 122 = 0x05/0x35/0x39 →
      flash4; line 127 = 0x07/0x08/0x0B → eprom; lines 132–133 = 0x0E/0x27/0x28/0x29 → sram)
      the handler-family layer must name. Do NOT edit this file.
    - `firestarter_app/firestarter/ic_layout.py` lines 216–232 (`proto_display`) and 261+
      (`protocol_info_data`) — READ-ONLY grounding: confirm both host maps are MISSING 0x34
      and `protocol_info_data` carries infeasible 0x11, so the draft display-name set MUST
      include 0x34 and MUST NOT introduce a real name for 0x11. Do NOT edit this file.
  </read_first>
  <action>
    Revise `firestarter/doc/PROTOCOLS.md` IN PLACE to carry the draft 3-field canonical name
    set. Touch ONLY the name-axis regions; preserve everything else verbatim.

    Region A — Canonical bucket set table (the primary work surface + the approval artifact):
    Expand the single `algorithm-axis name (col 2)` column into three explicit columns —
    `PROTO_ token`, `display name`, `handler-family` — while KEEPING the existing `hex`,
    `DB chip count`, and `datasheets folder (col 1)` columns verbatim (the slug column is the
    DOC-02 anchor per D-07; never rename or drop it). Target column order:
    `hex | DB chip count | frozen slug (col 1) | PROTO_ token | display name | handler-family | phantom?`.
    Populate all 12 real rows (0x05/06/07/08/0B/0D/0E/10/27/28/29/34) using the chip-family/
    behavior axis (D-03), pin-count-primary (D-04). Proposed draft tokens (mark as PROPOSED —
    operator settles at the gate in Task 2): 0x05→`PROTO_FLASH_5V_PAGE`,
    0x06→`PROTO_FLASH_NOR_UNLOCK`, 0x07→`PROTO_EPROM_28PIN`, 0x08→`PROTO_EPROM_32PIN`,
    0x0B→`PROTO_EPROM_24PIN`, 0x0D→`PROTO_EEPROM_PARALLEL`, 0x0E→`PROTO_SRAM_32PIN`,
    0x10→`PROTO_FLASH_INTEL`, 0x27→`PROTO_SRAM_24PIN`, 0x28→`PROTO_SRAM_28PIN`,
    0x29→`PROTO_SRAM_32PIN_LARGE`, 0x34→`PROTO_EEPROM_X88C64`. For the D-05 collision,
    0x0E and 0x29 are BOTH 32-pin SRAM — give them two DISTINCT tokens in the draft
    (`PROTO_SRAM_32PIN` vs `PROTO_SRAM_32PIN_LARGE`) and mark the 0x29 token with a visible
    "?? — 0x0E/0x29 tiebreak, operator resolves at gate" annotation so it is unmistakable in
    the approval table. Add two flagged phantom rows below the 12 real rows:
    0x35→`PROTO_PHANTOM_35`, 0x39→`PROTO_PHANTOM_39`, with `phantom?` = YES, chip count 0,
    frozen slug `(none)`, handler-family `flash4 dispatch arm`; per D-08 offer both the
    `_35`/`_39` and the `_0x35`/`_0x39` spellings as the operator choice (note it in the row
    or an adjacent line) — do NOT alias phantoms to the 0x05 real family name. The handler-family
    column names the 7 existing groupings from memory.cpp dispatch (D-09): EPROM family for
    0x07/08/0B, SRAM family for 0x0E/27/28/29, plus single-protocol families for 0x05, 0x06,
    0x0D, 0x10, and not-implemented for 0x34. The whole table must render as ONE scannable
    Markdown table (12 real + 2 phantom = 14 rows) so it is a clean yes/no approval artifact.

    Region B — §1.1 through §1.12 per-bucket sections: change ONLY the
    `**Algorithm-axis name (col 2):**` line in each bucket to reflect the new PROTO_ token +
    display name (rename the line label to `**Canonical name (col 2):**` and give it the
    `PROTO_` token + display name). PRESERVE the four-facet prose paragraphs
    (`**Write algorithm:**`/`**Erase model:**`/`**VPP behavior:**`/`**Pin roles:**` with their
    `Citation:` lines) VERBATIM — do NOT rewrite facets (that is Phase 103 / DOC-01). PRESERVE
    the §1.10 FM1608 NAME-04 call-out and the §1.12 X88C64 NAME-04 call-out VERBATIM (carry the
    identity corrections forward, do not re-litigate). Do NOT drop the 12V-VPP hazard signals
    that live in the VPP-behavior facets of 0x07/0x08/0x0B/0x10.

    Region C — §2.1 phantom table: update the two phantom rows so the firmware-name column
    reads the new flagged `PROTO_PHANTOM_35`/`_39` tokens (agreeing with Region A). Leave the
    §2.2 infeasible-bucket table (0x11/0x2A/0x2B/0x2C) UNCHANGED (out of scope — not in the DB).

    Region D — §3 INV-01..09 matrix: PRESERVE UNCHANGED (SAFE-02 grep-intact contract).
    Update ONLY the header intro paragraph (to describe the new 3-field schema + handler-family
    layer, keeping the router links) and add ONE Phase-100 footer provenance line mirroring the
    existing `*Phase 87 — Naming + Documentation Pass | Authored 2026-06-26*` style, noting the
    name-set revision (operator-approval date filled in Task 3).

    Do NOT edit any file other than `firestarter/doc/PROTOCOLS.md`. Do NOT `git mv` or rename
    anything under `datasheets/` (NAME-F1 deferred). Do NOT reconcile the facet prose or INV
    matrix to the new names (that is Phase 103). Leave the draft 0x29 token visibly marked as
    the unresolved D-05 tiebreak for the operator to settle in Task 2.
  </action>
  <verify>
    <automated>cd /workspaces && test $(grep -c 'INV-0' firestarter/doc/PROTOCOLS.md) -eq 34 && grep -q 'PROTO_EPROM_28PIN' firestarter/doc/PROTOCOLS.md && grep -q 'PROTO_PHANTOM_3' firestarter/doc/PROTOCOLS.md && for h in 0x05 0x06 0x07 0x08 0x0B 0x0D 0x0E 0x10 0x27 0x28 0x29 0x34 0x35 0x39; do grep -q "$h" firestarter/doc/PROTOCOLS.md || { echo "MISSING $h"; exit 1; }; done && echo AUTHOR_DRAFT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'INV-0' firestarter/doc/PROTOCOLS.md` returns exactly `34` (INV matrix byte-intact — SAFE-02 grep-intact contract preserved).
    - The Canonical bucket set table contains 14 protocol rows: all 12 real hex ids (0x05, 0x06, 0x07, 0x08, 0x0B, 0x0D, 0x0E, 0x10, 0x27, 0x28, 0x29, 0x34) plus 2 phantom rows (0x35, 0x39 flagged `phantom? = YES`).
    - Every one of the 14 rows has a `PROTO_` token and a display name; the frozen `0x<hex>-<NAME>` slug column is present verbatim next to each real row.
    - 0x0E and 0x29 have two DISTINCT `PROTO_` tokens (e.g. `PROTO_SRAM_32PIN` vs `PROTO_SRAM_32PIN_LARGE`), and the 0x29 token carries a visible tiebreak annotation for the operator.
    - A handler-family column/entry names all 7 groupings (EPROM 0x07/08/0B; SRAM 0x0E/27/28/29; single-protocol 0x05, 0x06, 0x0D, 0x10; not-implemented 0x34).
    - 0x34 has a `PROTO_` token and display name (not omitted — it is in the DB with 1 chip).
    - The §1.1–§1.12 four-facet prose paragraphs and the §1.10/§1.12 NAME-04 call-outs are unchanged from baseline (diff shows changes only on the col-2 name lines, the Canonical bucket set table, the §2.1 phantom rows, the intro, and the footer). `git diff --stat firestarter/doc/PROTOCOLS.md` shows exactly one file changed.
    - No file other than `firestarter/doc/PROTOCOLS.md` is modified; no path under `datasheets/` is renamed or moved.
  </acceptance_criteria>
  <done>The draft 3-field name set (12 real + 2 phantom rows), handler-family layer, phantom tokens, and updated §1.x col-2 name lines exist in PROTOCOLS.md; the INV matrix, facet prose, NAME-04 call-outs, and §2.2 infeasible table are preserved verbatim; the 0x29 token is visibly marked as the unresolved D-05 tiebreak.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING operator approval of the name set + 0x0E/0x29 tiebreak (NAME-02)</name>
  <action>Present the draft Canonical bucket set table (rendered from Task 1) to the operator and BLOCK until they explicitly approve it and resolve the 0x0E-vs-0x29 tiebreak. Capture the operator's decision (final 0x0E token, final 0x29 token, phantom spelling, 0x34 name, any per-protocol edits) so Task 3 can apply it. Do not commit the doc as authoritative and do not proceed to Task 3 until the operator signs off — no silent auto-approval (NAME-02).</action>
  <what-built>
    A draft Canonical bucket set table in `firestarter/doc/PROTOCOLS.md` carrying the 3-field
    canonical name set — a `PROTO_` token + display name + handler-family for all 12 DB
    protocols and the 2 flagged phantom rows (0x35/0x39) — with the frozen datasheet slug
    column retained beside each new name (DOC-02 divergence anchor). The 0x0E-vs-0x29 32-pin
    SRAM name collision is presented as an explicit tiebreak for the operator to resolve, and
    the phantom-token spelling (`PROTO_PHANTOM_35` vs `PROTO_PHANTOM_0x35`) and the 0x34 name
    (`PROTO_EEPROM_X88C64` vs a behavior-axis alternative) are offered as operator choices.
  </what-built>
  <how-to-verify>
    1. Open `firestarter/doc/PROTOCOLS.md` and scroll to the "Canonical bucket set" table
       (top of the doc). Read the 14 rows top-to-bottom as one scannable table.
    2. Confirm each protocol's proposed `PROTO_` token + display name reads correctly for what
       the chip IS and how it programs (chip-family/behavior axis; pin-count-primary), with the
       old minipro jargon (`AMD`, `QUICK`, `ALT`) dropped from the names (it survives only in
       the frozen slug column beside each row).
    3. RESOLVE the 0x0E-vs-0x29 tiebreak: both are 32-pin SRAM. Pick the final distinct name
       for each (draft proposes `PROTO_SRAM_32PIN` for 0x0E battery-backed NVRAM vs
       `PROTO_SRAM_32PIN_LARGE` for 0x29's 512K–1M parts). State the two final tokens.
    4. Decide the phantom-token spelling: `PROTO_PHANTOM_35`/`_39` (numeric) or
       `PROTO_PHANTOM_0x35`/`_0x39` (hex-in-name). Both compile; state your choice.
    5. Confirm (or change) the 0x34 name (`PROTO_EEPROM_X88C64` matches the frozen slug;
       `PROTO_EEPROM_8051BUS` is more behavior-axis-consistent per D-03).
    6. Approve every other proposed token + display name, or list any specific name changes.
  </how-to-verify>
  <resume-signal>Type "approved" (accepting all draft names as-is) OR provide the final 0x0E token, the final 0x29 token, the phantom spelling choice, the 0x34 name, and any other per-protocol name changes. Task 3 applies exactly what you approve. Nothing is committed as authoritative until you sign off here — no silent auto-approval.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize the operator-approved names in PROTOCOLS.md + record approval provenance (NAME-03)</name>
  <!-- planner-region-allow: tiebreak -->
  <!-- planner-region-allow: PROPOSED -->
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - `firestarter/doc/PROTOCOLS.md` — the draft authored in Task 1; read it to apply the
      operator's Task-2 decisions on top of the current state.
    - The operator's approval + tiebreak decision from Task 2 (the resume-signal text): the
      final 0x0E token, final 0x29 token, phantom spelling, 0x34 name, and any per-protocol
      name edits. Apply EXACTLY what was approved — do not re-open naming.
    - `.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-PATTERNS.md`
      — Region D footer pattern (mirror the `*Phase 87 — Naming + Documentation Pass |
      Authored 2026-06-26*` provenance-line style).
  </read_first>
  <action>
    Apply the operator's Task-2 decisions to `firestarter/doc/PROTOCOLS.md`:
    (1) Set the final 0x0E and 0x29 `PROTO_` tokens + display names to the operator's chosen
    tiebreak values (both distinct); REMOVE the "?? tiebreak, operator resolves at gate"
    annotation from the 0x29 row now that it is resolved. (2) Set the phantom tokens
    (0x35/0x39) to the operator's chosen spelling in BOTH the Region-A table and the §2.1
    phantom table, removing the "offer both spellings" note. (3) Set the 0x34 name to the
    operator's choice. (4) Apply any other per-protocol name edits the operator specified.
    (5) Change every "PROPOSED" marker to final — the table is now authoritative. (6) Update
    the corresponding §1.1–§1.12 col-2 `**Canonical name (col 2):**` lines to match the final
    approved tokens/display names (name line ONLY — facet prose still verbatim). (7) Update the
    footer Phase-100 provenance line to record the name-set revision AND the operator-approval
    date (today's date), mirroring the existing footer style.

    If the operator typed only "approved" with no changes, keep the Task-1 draft tokens as
    final, remove the tiebreak/spelling/PROPOSED annotations, and pick the draft defaults
    (0x0E=`PROTO_SRAM_32PIN`, 0x29=`PROTO_SRAM_32PIN_LARGE`, phantom=`PROTO_PHANTOM_35`/`_39`,
    0x34=`PROTO_EEPROM_X88C64`).

    Do NOT touch the §3 INV matrix, the facet prose, the NAME-04 call-outs, the §2.2 infeasible
    table, or any file other than `firestarter/doc/PROTOCOLS.md`. Do NOT rename anything under
    `datasheets/`.
  </action>
  <verify>
    <automated>cd /workspaces && test $(grep -c 'INV-0' firestarter/doc/PROTOCOLS.md) -eq 34 && ! grep -qi 'tiebreak' firestarter/doc/PROTOCOLS.md && ! grep -qi 'PROPOSED' firestarter/doc/PROTOCOLS.md && E=$(grep -oE 'PROTO_SRAM_[A-Z0-9_]*' firestarter/doc/PROTOCOLS.md | grep -c '32PIN') && test "$E" -ge 2 && grep -q '2026-07' firestarter/doc/PROTOCOLS.md && echo FINALIZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'INV-0' firestarter/doc/PROTOCOLS.md` still returns exactly `34` (INV matrix untouched through finalization).
    - No `tiebreak`, `??`, or `PROPOSED` annotation remains anywhere in the doc — every name is final/authoritative.
    - 0x0E and 0x29 map to two distinct final `PROTO_` tokens and two distinct display names (the D-05 collision is resolved, not merely deferred).
    - The phantom tokens (0x35/0x39) use one consistent spelling in BOTH the Region-A table and the §2.1 phantom table.
    - The footer carries a Phase-100 provenance line with the operator-approval date (`grep '2026-07' firestarter/doc/PROTOCOLS.md` hits the new footer line).
    - The §1.1–§1.12 col-2 name lines match the final Region-A tokens/display names; facet prose and NAME-04 call-outs remain verbatim.
    - `git diff --stat` shows exactly one file changed (`firestarter/doc/PROTOCOLS.md`); no path under `datasheets/` renamed.
  </acceptance_criteria>
  <done>PROTOCOLS.md carries the operator-approved final 3-field name set (12 real + 2 phantom), the 0x0E/0x29 tiebreak is resolved with distinct tokens, all draft/tiebreak/PROPOSED markers are gone, the footer records the operator-approval date, and the INV matrix + facet prose are preserved verbatim — the doc is the single authoritative source Phases 101/102/103 cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 100 authors human-readable names in a Markdown doc + records a human decision. No input parsing, no auth, no crypto, no network, no data storage, no runtime code path — no trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-100-01 | Tampering / Elevation | A protocol name silently becomes a dispatch/lookup key, weakening algorithm-first dispatch | low | mitigate | Author `PROTO_<NAME> = 0x..` form only (the label IS the number, D-02); GATE-01 (numbers stay the dispatch key) is enforced in Phases 101–103 that touch code. Phase 100 authors no code and no lookup keyed on strings. |
| T-100-02 | Information disclosure / Repudiation | Mis-naming a hazardous protocol drops the 12V-VPP hazard signal for 0x07/0x08/0x0B/0x10 so a downstream reader under-estimates danger | low | mitigate | D-04 keeps voltage/hazard detail in the facet prose (field 3), which Task 1 preserves VERBATIM from the datasheet-cited §1.x — hazard information survives even though the name is pin-count-primary. Acceptance criteria assert facet prose is unchanged. |
| T-100-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed — this phase runs only text edits + git. No package legitimacy audit applies. |
</threat_model>

<verification>
Phase-level artifact checks (no code produced, so verification is artifact-inspection + the
approval record, per RESEARCH.md §Validation Architecture):

- `grep -c 'INV-0' firestarter/doc/PROTOCOLS.md` == 34 (SAFE-02 grep-intact contract held through the whole phase).
- All 14 protocol rows (12 real + 2 phantom) present in the Canonical bucket set table, each with a PROTO_ token + display name; frozen slug column retained (DOC-02 anchor).
- 0x0E and 0x29 have distinct final tokens (D-05 resolved); 0x34 has a name (Pitfall 3 avoided).
- Operator approval captured at the blocking Task-2 gate before Task 3 finalized the doc (NAME-02 — no silent auto-approval); footer records the approval date.
- Exactly one file changed (`firestarter/doc/PROTOCOLS.md`); no `chip_database.json`/wire/firmware/host change (GATE-01/02/03 held trivially — nothing else touched); no `datasheets/` slug renamed (NAME-F1 deferred).
</verification>

<success_criteria>
- NAME-01: A 3-field canonical entry (`PROTO_` token + display name + datasheet-cited facet prose) exists for every DB protocol (0x05/06/07/08/0B/0D/0E/10/27/28/29/34) + phantoms 0x35/0x39 (flagged), chip-family/behavior axis, pin-count-primary, FM1608/X88C64 corrections carried forward.
- NAME-02: The operator explicitly approved the rendered table at the blocking gate and resolved the 0x0E/0x29 tiebreak — no silent auto-approval.
- NAME-03: The approved set is recorded in one authoritative source (`firestarter/doc/PROTOCOLS.md`, revised in place) with the handler-family layer present and the frozen slug column retained; Phases 101/102/103 cite it by section.
</success_criteria>

<output>
Create `.planning/phases/100-name-canonical-protocol-name-set-operator-approval/100-01-SUMMARY.md` when done.
</output>
