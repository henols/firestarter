---
phase: 104-rename-protocol-header-and-cpp-files-to-descriptive-protocol
plan: 03
type: execute
wave: 3
depends_on: [104-01, 104-02]
files_modified:
  - firestarter/test/native/avr/test_val_flash3
  - firestarter/test/native/avr/test_val_flash4
  - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp
  - firestarter/platformio.ini
  - firestarter/doc/PROTOCOLS.md
  - firestarter/CLAUDE.md
  - firestarter_app/tests/test_dispatch_mirror.py
autonomous: true
requirements: [RENAME-04, RENAME-05]
must_haves:
  truths:
    - "native test-suite dirs are test_val_nor_unlock/ and test_val_5v_page/ (each with its renamed .cpp + host_stubs.cpp); test_val_flash3/4 no longer exist"
    - "platformio.ini test_filter and -I build_flags reference the renamed suite dirs"
    - "PROTOCOLS.md §0 handler-family table, §1.1/§1.2 handler lines, and §3 INV-04/INV-09 suite-path contract reference the new file/function/suite names; INV-04/INV-09 ids remain grep-intact"
    - "test_dispatch_mirror.py DOC_FILE_TO_FUNC maps the new .cpp filenames to the new function names and the doc↔tool↔firmware bind holds"
    - "pio test -e native full suite green; pytest test_dispatch_mirror.py + test_check_dispatch_invariants.py green; pio run -e uno && pio run -e leonardo green"
    - "no surviving flash_type_3/flash_type_4/flash3/flash4/FALSH string in any scoped firmware source/doc/test or host guard file (excluding historical val-results and the TYPE_FLASH_TYPE_3/4 #defines)"
  artifacts:
    - firestarter/test/native/avr/test_val_nor_unlock/test_val_nor_unlock.cpp
    - firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
  key_links:
    - "test_dispatch_mirror.py parses PROTOCOLS.md §0 .cpp filenames (regex) and joins to DOC_FILE_TO_FUNC then to check_dispatch.dispatch() — all three must be internally consistent (Pitfall 1)"
    - "PROTOCOLS.md §3 INV-04→test_val_5v_page/ and INV-09→test_val_nor_unlock/ must match the git-mv'd suite dirs + platformio.ini"
  prohibitions:
    - "MUST NOT change phantom-protocol integers — the make_handle(0x35, ...) / make_handle(0x39, ...) literals in the 5v_page suite stay unchanged (GATE-01)"
    - "MUST NOT change protocol integers (0x05/0x06/0x35/0x39) or any protocols array anywhere"
    - "MUST NOT touch any CLI grammar / cli_handlers source file, and MUST NOT accept any protocol name as CLI input (GATE-03)"
    - "MUST NOT rewrite historical firestarter_app/val-results/ bench artifacts"
---

<objective>
Rename the two native validation test suites (dirs + family-ids + test-fn stems), update the dispatch test, `platformio.ini`, `PROTOCOLS.md` (§0/§1/§3 incl. the SAFE-02 INV suite-path contract), and `firestarter/CLAUDE.md`; then close the doc↔tool↔firmware dispatch-mirror bind and run the full phase gate.

Purpose: RENAME-04 (native suites + family-ids) and RENAME-05 (PROTOCOLS.md + doc prose), plus carrying GATE-01/02/03 non-regression to a fully green end state where the dispatch-mirror test passes.
Output: renamed suite dirs, reconciled docs, and a green full-native + host-mirror + both-board compile gate.

Phase requirements (defined here):
- RENAME-04 — native test suites + family-ids renamed to descriptive stems; phantom-protocol integers (make_handle(0x35/0x39)) unchanged.
- RENAME-05 — PROTOCOLS.md §0/§1/§3 (incl. INV suite-path contract) + firestarter CLAUDE.md + host doc prose reconciled; no surviving flash_type_3/4/flash3/flash4/FALSH string in scoped files.
- Carry GATE-01 (dispatch-mirror + check_dispatch + pio test -e native green), GATE-02 (diff_db identity — constants-parity leg CI-PENDING, no python3.11 in devcontainer; non-blocking), GATE-03 (no CLI grammar / no CLI-source file touched) as non-regression must_haves.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-CONTEXT.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-PATTERNS.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-RESEARCH.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-VALIDATION.md

# Golden analogs (correctly-named siblings — copy their shape):
@firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp
@firestarter/doc/PROTOCOLS.md
@firestarter/CLAUDE.md
@firestarter_app/tests/test_dispatch_mirror.py
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-01-SUMMARY.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Renamed (git mv) native test-suite dirs + files (exclude from drift/orphan checks):
- firestarter/test/native/avr/test_val_nor_unlock/ (was test_val_flash3/) with test_val_nor_unlock.cpp + host_stubs.cpp
- firestarter/test/native/avr/test_val_5v_page/ (was test_val_flash4/) with test_val_5v_page.cpp + host_stubs.cpp
Family-id strings finalized here (Claude's-discretion spelling per CONTEXT): "nor_unlock", "5v_page".
Test-fn stems: test_nor_unlock_* (was test_flash3_*), test_5v_page_* (was test_flash4_*).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: git mv + rename native test suites and the dispatch test</name>
  <files>firestarter/test/native/avr/test_val_flash3, firestarter/test/native/avr/test_val_flash4, firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp, firestarter/platformio.ini</files>
  <read_first>
    - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp (test-fns test_flash3_*_no_vpp L84-120; RUN_TEST list L128-131; configure_flash3 refs in comments/asserts)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (~38 configure_flash4/flash4_/test_flash4 refs incl. make_handle(0x35)/make_handle(0x39) at ~L111,120,131 — phantom INTEGERS stay)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (test_protocol_0x06_dispatches_flash3 L71; 0x05/0x35/0x39_dispatches_flash4 L77-89; FIX-02A comments L165-167 naming configure_flash4/configure_flash3; test_flash4_check_chip_id_* L170-188; RUN_TEST list L203-224)
    - firestarter/platformio.ini (test_filter L91-92; -I build_flags L109-110)
    - firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp (golden suite: dir==filename==test-fn stem; asserts name the function under test)
  </read_first>
  <action>
    From firestarter/ root, git mv the two suite dirs and their files preserving history:
    `git mv test/native/avr/test_val_flash3 test/native/avr/test_val_nor_unlock` then `git mv test/native/avr/test_val_nor_unlock/test_val_flash3.cpp test/native/avr/test_val_nor_unlock/test_val_nor_unlock.cpp`; likewise flash4 → test_val_5v_page with test_val_5v_page.cpp. (host_stubs.cpp keeps its filename; only its comment strings change.)
    In test_val_nor_unlock.cpp: substitute configure_flash3 → configure_flash_nor_unlock in comments/assert messages; rename test-fns test_flash3_* → test_nor_unlock_* and update the RUN_TEST list; update the header-comment references. In its host_stubs.cpp update the "test_val_flash3" comment to "test_val_nor_unlock".
    In test_val_5v_page.cpp: substitute configure_flash4 → configure_flash_5v_page, rename test_flash4_* → test_5v_page_* + flash4_* refs to the 5v_page stem, update RUN_TEST list and header comments. CRITICAL: leave the make_handle(0x35, ...) and make_handle(0x39, ...) phantom-protocol integer literals unchanged (GATE-01). Update its host_stubs.cpp comment to "test_val_5v_page".
    In test_dispatch/test_configure_memory.cpp: rename the dispatch test-fn names test_protocol_0x06_dispatches_flash3 → ..._dispatches_nor_unlock, ..._0x05/0x35/0x39_dispatches_flash4 → ..._dispatches_5v_page; rename test_flash4_check_chip_id_* → test_5v_page_check_chip_id_*; substitute configure_flash3/configure_flash4 in the FIX-02A comment block to the renamed functions; update the RUN_TEST list. Keep the 0x06/0x05/0x35/0x39 integers in fn names as-is (they name the protocol number, which is unchanged).
    In platformio.ini: update test_filter entries (native/avr/test_val_flash3 → test_val_nor_unlock; test_val_flash4 → test_val_5v_page) and the matching -I build_flags entries to the git-mv'd dir names. Leave the test_val_flash_intel entries alone (golden analog).
  </action>
  <verify>
    <automated>cd firestarter && test -d test/native/avr/test_val_nor_unlock && test -d test/native/avr/test_val_5v_page && test -f test/native/avr/test_val_nor_unlock/test_val_nor_unlock.cpp && ! grep -rn 'configure_flash3\|configure_flash4\|test_flash3\|test_flash4\|flash3_\|flash4_' test/native/avr/test_val_nor_unlock test/native/avr/test_val_5v_page test/native/avr/test_dispatch/test_configure_memory.cpp && grep -q 'make_handle(0x35' test/native/avr/test_val_5v_page/test_val_5v_page.cpp && ! grep -n 'test_val_flash3\|test_val_flash4' platformio.ini && echo SUITES_OK</automated>
  </verify>
  <done>Both suites renamed (git R, history preserved) to test_val_nor_unlock/ and test_val_5v_page/ with renamed .cpp; dispatch test + platformio.ini reference the new names; phantom integer literals make_handle(0x35/0x39) intact; no flash3/flash4 identifier survives in the scoped test files.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile PROTOCOLS.md (§0/§1/§3) and firestarter/CLAUDE.md</name>
  <files>firestarter/doc/PROTOCOLS.md, firestarter/CLAUDE.md</files>
  <read_first>
    - firestarter/doc/PROTOCOLS.md (§0 handler-family table L66-67; §1.1 handler L84 + prose; §1.2 handler L104 + prose L110,113; §2.1 phantom prose L356; §3 INV suite-path list L405,407 + INV-04 row L414 + INV-09 row L419 + grep-intact note L398)
    - firestarter/CLAUDE.md (dispatch list L43-44,51-52 naming configure_flash3/4; handler table L77-80 naming flash_type_3.cpp/flash_type_4.cpp)
  </read_first>
  <action>
    In PROTOCOLS.md rewrite every old file/function/family reference to the new names, keeping the numbers and the INV ids intact:
    - §0 handler-family table L66-67: func cell configure_flash4()→configure_flash_5v_page(), .cpp cell flash_type_4.cpp→flash_5v_page.cpp; configure_flash3()→configure_flash_nor_unlock(), flash_type_3.cpp→flash_nor_unlock.cpp. Also update the bare family-label cells (flash4/flash3) to the chosen stems (5v_page / nor_unlock). This §0 table is exactly what test_dispatch_mirror.py's _FAMILY_ROW_RE parses — keep it lockstep with DOC_FILE_TO_FUNC (Task 3).
    - §1.1 L84 Handler line: configure_flash4()→configure_flash_5v_page(), flash_type_4.cpp→flash_5v_page.cpp; §1.2 L104: configure_flash3()→configure_flash_nor_unlock(), flash_type_3.cpp→flash_nor_unlock.cpp; prose L110/L113 configure_flash3()→renamed; §2.1 L356 configure_flash4() dispatch prose→renamed. Where prose says the bare "flash4"/"flash3" page/INV references, use the new stem.
    - §3 INV suite-path contract: L405 INV-04 → test/native/avr/test_val_5v_page/; L407 INV-09 → test/native/avr/test_val_nor_unlock/; INV-04 row L414 owning-handler flash_type_4.cpp→flash_5v_page.cpp and planned-test-fn test_inv04_flash4_*→test_inv04_5v_page_*; INV-09 row L419 flash_type_3.cpp→flash_nor_unlock.cpp and test_inv09_flash3_*→test_inv09_nor_unlock_*. Keep the INV-04 / INV-09 id tokens verbatim (the grep-intact contract at L398). Do NOT change the 0x05/0x06 numbers.
    In firestarter/CLAUDE.md: dispatch list L43-44 configure_flash3()/configure_flash4()→renamed; L51-52 fallback configure_flash3()/configure_flash4()→renamed (the mem_type == TYPE_FLASH_TYPE_3/4 labels stay — those are the #define names, out of scope); handler table L77-80 flash_type_3.cpp/flash_type_4.cpp→flash_nor_unlock.cpp/flash_5v_page.cpp (all four rows incl. the two phantom 0x35/0x39 rows point at flash_5v_page.cpp).
  </action>
  <verify>
    <automated>cd firestarter && ! grep -n 'flash_type_3\|flash_type_4\|configure_flash3\|configure_flash4\|test_val_flash3\|test_val_flash4' doc/PROTOCOLS.md && ! grep -n 'flash_type_3\|flash_type_4\|configure_flash3\|configure_flash4' CLAUDE.md && grep -q 'INV-04' doc/PROTOCOLS.md && grep -q 'INV-09' doc/PROTOCOLS.md && grep -q 'test_val_5v_page' doc/PROTOCOLS.md && grep -q 'test_val_nor_unlock' doc/PROTOCOLS.md && echo DOCS_OK</automated>
  </verify>
  <done>PROTOCOLS.md §0/§1/§3 and firestarter/CLAUDE.md reference the renamed files/functions/suites; INV-04/INV-09 ids grep-intact and point at the new suite dirs; TYPE_FLASH_TYPE_3/4 #define labels and all protocol numbers unchanged; no old flash_type_3/4/configure_flash3/4/test_val_flash3/4 string survives in either doc.</done>
</task>

<task type="auto">
  <name>Task 3: Close the dispatch-mirror bind; run the full phase gate</name>
  <files>firestarter_app/tests/test_dispatch_mirror.py</files>
  <read_first>
    - firestarter_app/tests/test_dispatch_mirror.py (DOC_FILE_TO_FUNC L74-82: flash_type_4.cpp/flash_type_3.cpp keys; golden flash_intel.cpp row L79; the _FAMILY_ROW_RE parse of PROTOCOLS.md §0)
  </read_first>
  <action>
    In test_dispatch_mirror.py update DOC_FILE_TO_FUNC: replace the "flash_type_4.cpp": "configure_flash4" entry with "flash_5v_page.cpp": "configure_flash_5v_page" and "flash_type_3.cpp": "configure_flash3" with "flash_nor_unlock.cpp": "configure_flash_nor_unlock". Both the .cpp-filename KEY (parsed from PROTOCOLS.md §0, updated in Task 2) and the function-name VALUE (returned by check_dispatch.dispatch(), updated in Plan 02) change together, mirroring the golden flash_intel.cpp row. Leave the other rows untouched.
    Then run the FULL phase gate to prove the doc↔tool↔firmware bind and behavior preservation:
    (1) `pio run -t clean` then `pio test -e native` (all native suites — dispatch + renamed val suites — green from the regenerated validation_matrix.h);
    (2) host `pytest firestarter_app/tests/test_dispatch_mirror.py firestarter_app/tests/test_check_dispatch_invariants.py` green;
    (3) `pio run -e uno && pio run -e leonardo` compile green;
    (4) final repo-wide grep smoke — no surviving flash_type_3/flash_type_4/flash3/flash4/FALSH identifier in scoped firmware source/include/test/doc or host guard files (excluding .pio build cache, historical firestarter_app/val-results/, and the intentional TYPE_FLASH_TYPE_3/TYPE_FLASH_TYPE_4 #defines).
    Confirm GATE-03: no CLI grammar / cli_handlers source file was touched by this phase (static confirmation — none is in files_modified). Record the GATE-02 constants-parity leg as CI-PENDING (no python3.11 in devcontainer; this phase changes no constants — non-blocking, per Phase 98/103 precedent).
  </action>
  <verify>
    <automated>cd firestarter_app && grep -q 'flash_5v_page.cpp' tests/test_dispatch_mirror.py && grep -q 'flash_nor_unlock.cpp' tests/test_dispatch_mirror.py && ! grep -n 'flash_type_3\|flash_type_4\|configure_flash3\|configure_flash4' tests/test_dispatch_mirror.py && python -m pytest tests/test_dispatch_mirror.py tests/test_check_dispatch_invariants.py -q && cd /workspaces/firestarter && pio run -t clean >/dev/null 2>&1; pio test -e native && pio run -e uno && pio run -e leonardo && echo GATE_OK</automated>
  </verify>
  <done>DOC_FILE_TO_FUNC maps the new .cpp filenames to the new functions; test_dispatch_mirror.py + test_check_dispatch_invariants.py green; `pio test -e native` full suite green; both boards compile; final grep confirms no old identifier survives in scoped files (excluding val-results + TYPE_FLASH_TYPE_3/4 #defines); GATE-03 held (no CLI file touched); GATE-02 constants-parity recorded CI-PENDING.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware doc ↔ host tool ↔ firmware dispatch | The three-way dispatch-mirror bind (PROTOCOLS.md §0 ↔ DOC_FILE_TO_FUNC ↔ check_dispatch.dispatch()); the sole cross-repo coupling of this rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-05 | Tampering | dispatch-mirror drift — if PROTOCOLS.md §0 filename and DOC_FILE_TO_FUNC key disagree, the join yields None and GATE-01 silently regresses | high | mitigate | Task 2 (PROTOCOLS.md §0) and Task 3 (DOC_FILE_TO_FUNC) change the same filename strings; test_dispatch_mirror.py is run in Task 3 verify to prove the bind holds |
| T-104-06 | Spoofing | phantom-protocol integer literal changed by an over-eager rename, altering dispatch coverage (0x35/0x39) | high | mitigate | Explicit prohibition + verify greps that make_handle(0x35 survives in the 5v_page suite; pio test -e native re-proves the phantom dispatch arms |
| T-104-07 | Tampering | INV-04/INV-09 SAFE-02 traceability ids dropped during §3 rewrite | medium | mitigate | Verify greps assert INV-04 and INV-09 tokens remain present in PROTOCOLS.md after the suite-path rewrite |
</threat_model>

<verification>
- test_dispatch_mirror.py DOC_FILE_TO_FUNC maps new filenames→new functions; suite green.
- `pio test -e native` full green; `pio run -e uno && pio run -e leonardo` green.
- PROTOCOLS.md + CLAUDE.md fully reconciled; INV-04/INV-09 grep-intact; suite dirs renamed.
- Repo-wide grep: no old identifier in scoped files (excluding val-results + TYPE_FLASH_TYPE_3/4 #defines + .pio).
- GATE-01 (dispatch-mirror + native) green; GATE-02 (diff_db identity green, constants-parity CI-PENDING/non-blocking); GATE-03 (no CLI file touched) held.
</verification>

<success_criteria>
The full v1.19 gate battery is green end-to-end with the descriptive names: firmware compiles for both boards, all native suites pass, the host dispatch-mirror bind holds, DB identity is preserved, no CLI grammar changed, and no minipro-heritage flash_type_N / flashN identifier survives in any scoped file.
</success_criteria>

<output>
Create `.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-03-SUMMARY.md` when done.
</output>
