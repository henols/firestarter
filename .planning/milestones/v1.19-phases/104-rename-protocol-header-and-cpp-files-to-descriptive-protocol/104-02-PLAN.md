---
phase: 104-rename-protocol-header-and-cpp-files-to-descriptive-protocol
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - firestarter_app/tools/validation_matrix_spec.json
  - firestarter/test/native/avr/_shared/validation_matrix.h
  - firestarter_app/tools/check_dispatch.py
  - firestarter_app/tests/test_check_dispatch_invariants.py
  - firestarter_app/doc/protocol-id.md
  - firestarter_app/doc/infoic-field-dictionary.md
autonomous: true
requirements: [RENAME-03]
must_haves:
  truths:
    - "check_dispatch.dispatch() returns configure_flash_nor_unlock for 0x06 and configure_flash_5v_page for 0x05, at both the protocol map and the mem_type fallback map"
    - "_FAMILY_VPP_INVARIANTS keys are the renamed function names; test_check_dispatch_invariants expects the renamed set"
    - "validation_matrix_spec.json flash3/flash4 family objects carry descriptive id/handler/suite; validation_matrix.h is REGENERATED (not hand-edited) and its 0x06/0x05 rows carry the descriptive family+handler"
    - "python firestarter_app/tools/check_dispatch.py exits green; diff_db.py reports DB identity (GATE-02)"
  artifacts:
    - firestarter/test/native/avr/_shared/validation_matrix.h
    - firestarter_app/tools/validation_matrix_spec.json
  key_links:
    - "validation_matrix.h is generated from validation_matrix_spec.json via gen_validation_header.py — the spec is the source, the .h is derived and must be regenerated"
    - "check_dispatch.dispatch() function strings must match the firmware function names from Plan 01 (the host mirror of memory.cpp order)"
  prohibitions:
    - "MUST NOT hand-edit validation_matrix.h — it is a generated artifact (DO NOT EDIT header); edit the spec then regenerate"
    - "MUST NOT change any protocols array or protocol integer in the spec (0x05/0x06/0x35/0x39) — numbers stay the dispatch key (GATE-01)"
    - "MUST NOT rewrite historical bench artifacts under firestarter_app/val-results/ (they record what was run at the time)"
    - "MUST NOT change any chip_database.json value (GATE-02) or any CLI grammar / cli_handlers file (GATE-03)"
---

<objective>
Bring the host GATE-01 guard tooling into lockstep with the renamed firmware functions from Plan 01: update the dispatch mirror (`check_dispatch.py`), its invariant test, the validation-matrix spec + host doc tables, and REGENERATE the firmware-shared `validation_matrix.h` from the spec.

Purpose: RENAME-03 — the host dispatch-mirror guard and validation-matrix generator hard-code the `configure_flash3/4` function-name strings; they must move in lockstep or the GATE-01 dispatch-mirror bind fails.
Output: host tooling that returns the descriptive function names; a regenerated `validation_matrix.h` carrying descriptive family+handler for 0x06/0x05.

Phase requirements (defined here):
- RENAME-03 — host GATE-01 guard tooling lockstep: check_dispatch.py (both mirrored maps + VPP-invariant keys), validation_matrix_spec.json family objects + regenerated validation_matrix.h, test_check_dispatch_invariants.py, host doc tables.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-CONTEXT.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-PATTERNS.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-RESEARCH.md
@CLAUDE.md

# Golden analog — the flash_intel family object/row is the descriptive shape to copy:
@firestarter_app/tools/validation_matrix_spec.json
@firestarter_app/tools/check_dispatch.py
@firestarter_app/tools/gen_validation_header.py
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Regenerated (not new) artifact: firestarter/test/native/avr/_shared/validation_matrix.h with rows
`{ 0x06, "nor_unlock", "configure_flash_nor_unlock" }` and `{ 0x05, "5v_page", "configure_flash_5v_page" }`.
New family-id strings introduced here (used by Plan 03 test-suite dirs): "nor_unlock", "5v_page".
New handler strings (from Plan 01): configure_flash_nor_unlock, configure_flash_5v_page.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update check_dispatch.py + invariant test to the renamed function names</name>
  <files>firestarter_app/tools/check_dispatch.py, firestarter_app/tests/test_check_dispatch_invariants.py</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (_FAMILY_VPP_INVARIANTS keys L81-82; dispatch() protocol map returns L140,142; mem_type fallback map L155-156; golden configure_flash_intel entries)
    - firestarter_app/tests/test_check_dispatch_invariants.py (expected-set entries L112-113)
  </read_first>
  <action>
    In check_dispatch.py rename every occurrence of the function-name STRINGS: the _FAMILY_VPP_INVARIANTS keys (configure_flash3 → configure_flash_nor_unlock, configure_flash4 → configure_flash_5v_page, preserving the (0,6000) VPP-window tuples and their explanatory comments); the dispatch() protocol map returns (0x06 → configure_flash_nor_unlock, 0x05 → configure_flash_5v_page); and the mem_type fallback map returns (3 → configure_flash_nor_unlock, 5 → configure_flash_5v_page). Mirror the golden configure_flash_intel style. Do NOT change any protocol/mem_type integer key.
    In test_check_dispatch_invariants.py update the expected function-name set entries (L112-113) to the two renamed strings so the invariant test matches.
    Note: the phantom 0x35/0x39 dispatch is intentionally NOT in check_dispatch.dispatch() (host routes phantoms to not_implemented, DEC-05) — do not add it.
  </action>
  <verify>
    <automated>cd firestarter_app && ! grep -rn 'configure_flash3\|configure_flash4' tools/check_dispatch.py tests/test_check_dispatch_invariants.py && grep -q 'configure_flash_nor_unlock' tools/check_dispatch.py && grep -q 'configure_flash_5v_page' tools/check_dispatch.py && python -m pytest tests/test_check_dispatch_invariants.py -q && echo CHECKDISPATCH_OK</automated>
  </verify>
  <done>check_dispatch.py returns configure_flash_nor_unlock/configure_flash_5v_page at both maps and keys _FAMILY_VPP_INVARIANTS by them; test_check_dispatch_invariants.py passes; no configure_flash3/4 string survives in either file; integer keys unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update validation_matrix_spec.json + regenerate validation_matrix.h</name>
  <files>firestarter_app/tools/validation_matrix_spec.json, firestarter/test/native/avr/_shared/validation_matrix.h</files>
  <read_first>
    - firestarter_app/tools/validation_matrix_spec.json (flash3 object L51-71: id/handler/suite/test_module; flash4 object L72-94: id/handler/protocols_note/suite/test_module; golden flash_intel object L95-116)
    - firestarter_app/tools/gen_validation_header.py (the generator that emits validation_matrix.h)
  </read_first>
  <action>
    In validation_matrix_spec.json edit the two family objects to descriptive form, mirroring the golden flash_intel object:
    - flash3 object: id "flash3" → "nor_unlock"; handler "configure_flash3" → "configure_flash_nor_unlock"; tier1 suite "test_val_flash3" → "test_val_nor_unlock"; test_module "test_val_wire_flash3" → "test_val_wire_nor_unlock".
    - flash4 object: id "flash4" → "5v_page"; handler "configure_flash4" → "configure_flash_5v_page"; suite "test_val_flash4" → "test_val_5v_page"; test_module "test_val_wire_flash4" → "test_val_wire_5v_page"; and update the configure_flash4 references inside the protocols_note prose to configure_flash_5v_page and the test_val_flash4.cpp reference to test_val_5v_page.cpp (keep the note's factual content and the 0x35/0x39 numbers).
    Do NOT change the protocols arrays ([6], [5]) or any integer.
    Then REGENERATE the firmware-shared header: run `python firestarter_app/tools/gen_validation_header.py` (or the invocation documented at the top of validation_matrix.h) so firestarter/test/native/avr/_shared/validation_matrix.h is re-emitted from the spec. Do NOT hand-edit validation_matrix.h (Pitfall 2). Confirm its 0x06/0x05 rows now read the descriptive family+handler and its 0x10 flash_intel row is unchanged.
  </action>
  <verify>
    <automated>cd /workspaces && python firestarter_app/tools/gen_validation_header.py >/dev/null 2>&1; grep -q 'configure_flash_nor_unlock' firestarter/test/native/avr/_shared/validation_matrix.h && grep -q 'configure_flash_5v_page' firestarter/test/native/avr/_shared/validation_matrix.h && ! grep -n 'configure_flash3\|configure_flash4\|"flash3"\|"flash4"' firestarter/test/native/avr/_shared/validation_matrix.h firestarter_app/tools/validation_matrix_spec.json && grep -q 'flash_intel' firestarter/test/native/avr/_shared/validation_matrix.h && echo MATRIX_OK</automated>
  </verify>
  <done>validation_matrix_spec.json flash3/flash4 objects carry descriptive id/handler/suite/test_module and updated protocols_note; validation_matrix.h regenerated (not hand-edited) with descriptive 0x06/0x05 rows; flash_intel row intact; no protocols array / integer changed.</done>
</task>

<task type="auto">
  <name>Task 3: Update host doc tables; confirm DB identity (GATE-02)</name>
  <files>firestarter_app/doc/protocol-id.md, firestarter_app/doc/infoic-field-dictionary.md</files>
  <read_first>
    - firestarter_app/doc/protocol-id.md (rows L17-18: 0x05→configure_flash4, 0x06→configure_flash3)
    - firestarter_app/doc/infoic-field-dictionary.md (rows L79-80: same)
  </read_first>
  <action>
    In both host doc tables rename the handler-name cells: configure_flash4 → configure_flash_5v_page (0x05 row) and configure_flash3 → configure_flash_nor_unlock (0x06 row). Leave the algorithm-id / minipro-label columns and the descriptive prose unchanged. Do NOT rewrite historical bench artifacts under firestarter_app/val-results/ (they record past runs).
    Then run the DB-identity gate to confirm no chip_database.json value drifted: `python firestarter_app/tools/diff_db.py` must report identity (GATE-02). This phase touches no DB values, so identity is expected.
  </action>
  <verify>
    <automated>cd firestarter_app && ! grep -rn 'configure_flash3\|configure_flash4' doc/protocol-id.md doc/infoic-field-dictionary.md && grep -q 'configure_flash_5v_page' doc/protocol-id.md && grep -q 'configure_flash_nor_unlock' doc/protocol-id.md && python tools/diff_db.py && echo HOSTDOC_OK</automated>
  </verify>
  <done>Both host doc tables show the renamed handler names; no configure_flash3/4 survives in host docs; diff_db.py reports DB identity (GATE-02 held); val-results artifacts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware ↔ host (test/tooling) | The dispatch-mirror guard binds the two repos by function-name strings; drift here trips GATE-01 but crosses no wire/runtime boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-03 | Tampering | validation_matrix.h regeneration (hand-edit would clobber on next regen / desync from spec) | medium | mitigate | Edit spec then run gen_validation_header.py; verify asserts the .h carries the descriptive strings and no "flash3"/"flash4"; never hand-edit the generated header |
| T-104-04 | Repudiation | historical val-results bench artifacts could be silently rewritten, losing the record of what was actually run | low | accept | Explicit prohibition: do not touch firestarter_app/val-results/; accepted as they are point-in-time evidence |
</threat_model>

<verification>
- `check_dispatch.py` returns the descriptive function names at both maps; `test_check_dispatch_invariants.py` green.
- `validation_matrix.h` regenerated from spec; carries descriptive 0x06/0x05 rows; no "flash3"/"flash4" strings.
- Host doc tables renamed; no configure_flash3/4 in host docs.
- `python firestarter_app/tools/diff_db.py` reports DB identity (GATE-02).
- No protocol integer / protocols array / chip_database.json / CLI file changed.
</verification>

<success_criteria>
Host guard tooling mirrors the renamed firmware functions; validation_matrix.h is regenerated consistently from the spec; DB identity holds; the dispatch-mirror function-name side is ready for the final bind test in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-02-SUMMARY.md` when done.
</output>
