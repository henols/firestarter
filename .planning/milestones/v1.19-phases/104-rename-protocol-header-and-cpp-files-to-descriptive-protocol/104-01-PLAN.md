---
phase: 104-rename-protocol-header-and-cpp-files-to-descriptive-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/include/flash_type_3.h
  - firestarter/include/flash_type_4.h
  - firestarter/src/proms/flash_type_3.cpp
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/src/proms/memory.cpp
  - firestarter/include/flash_utils.h
  - firestarter/src/proms/flash_utils.cpp
autonomous: true
requirements: [RENAME-01, RENAME-02]
must_haves:
  truths:
    - "firestarter/src/proms/ and include/ contain flash_nor_unlock.{cpp,h} and flash_5v_page.{cpp,h}; the flash_type_3/4 files no longer exist"
    - "memory.cpp dispatches PROTO_FLASH_NOR_UNLOCK (0x06) via configure_flash_nor_unlock() and PROTO_FLASH_5V_PAGE (0x05)+phantoms 0x35/0x39 via configure_flash_5v_page(), at BOTH the protocol chain (~L85-93) and the legacy mem_type fallback (~L130,133)"
    - "pio run -e uno and pio run -e leonardo both compile and link green"
    - "git records the two file-pairs as renames (R status), MIT license headers preserved"
  artifacts:
    - firestarter/include/flash_nor_unlock.h
    - firestarter/include/flash_5v_page.h
    - firestarter/src/proms/flash_nor_unlock.cpp
    - firestarter/src/proms/flash_5v_page.cpp
  key_links:
    - "memory.cpp #include lines resolve to the renamed headers; both dispatch call-site groups call the renamed functions"
    - "header guards are unique, correctly-spelled tokens (__FLASH_NOR_UNLOCK_H__ / __FLASH_5V_PAGE_H__), consistent across #ifndef/#define/#endif"
  prohibitions:
    - "MUST NOT change any protocol integer (0x05, 0x06, 0x35, 0x39) — numbers stay the dispatch key (GATE-01)"
    - "MUST NOT touch the TYPE_FLASH_TYPE_3 / TYPE_FLASH_TYPE_4 #define names (they mirror the wire mem_type int, no host dependency — out of scope)"
    - "MUST NOT change any chip_database.json / constants value (GATE-02) or any CLI grammar / cli_handlers file (GATE-03)"
---

<objective>
Rename the two minipro-heritage firmware handler file-pairs and their public entry-point functions to descriptive protocol-type names, and update every firmware source reference (dispatch includes + both call-site groups + flash_utils comments). Fix the misspelled header guards.

Purpose: Trailing legibility cleanup of the v1.19 Protocol Naming Labels milestone — the two remaining `flash_type_N` names get descriptive names derived from the operator-approved `PROTO_<NAME>` tokens (RENAME-01, RENAME-02).
Output: `flash_nor_unlock.{cpp,h}`, `flash_5v_page.{cpp,h}` with correct guards + renamed functions; a firmware tree that compiles for both boards.

Phase requirements (defined here — not in the closed v1.19 REQUIREMENTS.md):
- RENAME-01 — firmware files renamed (git mv, guards fixed, MIT headers preserved), all #includes + both memory.cpp dispatch call-site groups updated; both boards compile.
- RENAME-02 — firmware public functions renamed configure_flash3/4 → descriptive; no old symbol survives in firmware source.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-CONTEXT.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-PATTERNS.md
@.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-RESEARCH.md
@firestarter/CLAUDE.md
@firestarter/doc/PROTOCOLS.md
@firestarter/include/proto_constants.h

# Golden analog — copy this shape, do not invent structure:
@firestarter/include/flash_intel.h
@firestarter/src/proms/eeprom_28c.cpp
@firestarter/src/proms/memory.cpp
@firestarter/include/flash_type_3.h
@firestarter/include/flash_type_4.h
</context>

<artifacts_this_phase_produces>
New file paths (exclude from drift/orphan checks — they are renames of existing files):
- firestarter/include/flash_nor_unlock.h (was flash_type_3.h)
- firestarter/include/flash_5v_page.h (was flash_type_4.h)
- firestarter/src/proms/flash_nor_unlock.cpp (was flash_type_3.cpp)
- firestarter/src/proms/flash_5v_page.cpp (was flash_type_4.cpp)

New function symbols:
- configure_flash_nor_unlock (was configure_flash3)
- configure_flash_5v_page (was configure_flash4)

Later plans (02, 03) add renamed native-test-suite dirs (test_val_nor_unlock, test_val_5v_page).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: git mv + rename flash_type_3 → flash_nor_unlock (0x06 handler)</name>
  <files>firestarter/include/flash_type_3.h, firestarter/src/proms/flash_type_3.cpp</files>
  <read_first>
    - firestarter/include/flash_type_3.h (current guard L8-9,22 misspelled __FALSH__TYPE_3_H__; decl L16 configure_flash3)
    - firestarter/src/proms/flash_type_3.cpp (self-include L8; public entry configure_flash3 ~L135; static helpers flash3_* L17-131)
    - firestarter/include/flash_intel.h (golden guard/extern-C/MIT-header shape)
  </read_first>
  <action>
    Rename the 0x06 NOR-unlock handler pair with history preserved. From the firestarter/ repo root run `git mv include/flash_type_3.h include/flash_nor_unlock.h` and `git mv src/proms/flash_type_3.cpp src/proms/flash_nor_unlock.cpp` FIRST (before editing content) so git records renames not delete+add (Pitfall 5).
    Then edit flash_nor_unlock.h: preserve the MIT license header block verbatim; rewrite all three guard occurrences (#ifndef, #define, and the trailing #endif comment) to the correct unique token derived from the new basename — fixing the misspelling in one step; rename the public declaration configure_flash3 → configure_flash_nor_unlock.
    Then edit flash_nor_unlock.cpp: preserve the MIT header; change the self-include on L8 to reference the new header basename; rename the public entry point configure_flash3 → configure_flash_nor_unlock. Also rename the file-internal flash3_* static helpers to a flash_nor_unlock_* stem for full consistency (they are file-internal, no cross-repo impact per PATTERNS §internal-helpers) and update their forward-decls + call sites within this .cpp.
    Do NOT change any protocol integer, the TYPE_FLASH_TYPE_3 #define, or any VPP/handler logic — pure identifier rename (GATE-01/behavior preservation). The guard token must be spelled correctly (no FALSH) and match across all three sites (the golden flash_intel.h shows all three identical).
  </action>
  <verify>
    <automated>cd firestarter && git status --porcelain | grep -q '^R.*flash_nor_unlock.h' && git status --porcelain | grep -q '^R.*flash_nor_unlock.cpp' && ! grep -rn 'configure_flash3\|flash_type_3\|__FALSH__TYPE_3' include/flash_nor_unlock.h src/proms/flash_nor_unlock.cpp && grep -q '__FLASH_NOR_UNLOCK_H__' include/flash_nor_unlock.h && echo RENAME3_OK</automated>
  </verify>
  <done>flash_type_3.{cpp,h} renamed to flash_nor_unlock.{cpp,h} (git R status); guard is __FLASH_NOR_UNLOCK_H__ at all three sites; public function is configure_flash_nor_unlock; no configure_flash3/flash_type_3/FALSH string survives in either renamed file; MIT header intact.</done>
</task>

<task type="auto">
  <name>Task 2: git mv + rename flash_type_4 → flash_5v_page (0x05 + phantom handler)</name>
  <files>firestarter/include/flash_type_4.h, firestarter/src/proms/flash_type_4.cpp</files>
  <read_first>
    - firestarter/include/flash_type_4.h (guard L8-9 __FALSH__TYPE_4_H__ but trailing #endif L22 says __FLASH__TYPE_4_H__ — three-way mismatch; decl L16 configure_flash4)
    - firestarter/src/proms/flash_type_4.cpp (self-include L8; public entry configure_flash4 ~L145; static helpers flash4_* L27-39)
    - firestarter/include/flash_intel.h (golden guard/extern-C/MIT-header shape)
  </read_first>
  <action>
    Rename the 0x05 5V-page handler pair with history preserved. From firestarter/ root run `git mv include/flash_type_4.h include/flash_5v_page.h` and `git mv src/proms/flash_type_4.cpp src/proms/flash_5v_page.cpp` FIRST.
    Then edit flash_5v_page.h: preserve the MIT header; rewrite all three guard occurrences to the correct unique token from the new basename — this simultaneously fixes the misspelling AND the three-way mismatch (the current #ifndef/#define use FALSH while the #endif comment differs); rename the declaration configure_flash4 → configure_flash_5v_page.
    Then edit flash_5v_page.cpp: preserve the MIT header; change the self-include on L8 to the new header basename; rename the public entry configure_flash4 → configure_flash_5v_page; rename file-internal flash4_* static helpers to a flash_5v_page_* stem and update their forward-decls + call sites within this .cpp.
    Do NOT change protocol integers (0x05/0x35/0x39), the TYPE_FLASH_TYPE_4 #define, or handler logic. Guard token spelled correctly, consistent across all three sites.
  </action>
  <verify>
    <automated>cd firestarter && git status --porcelain | grep -q '^R.*flash_5v_page.h' && git status --porcelain | grep -q '^R.*flash_5v_page.cpp' && ! grep -rn 'configure_flash4\|flash_type_4\|FALSH' include/flash_5v_page.h src/proms/flash_5v_page.cpp && grep -q '__FLASH_5V_PAGE_H__' include/flash_5v_page.h && echo RENAME4_OK</automated>
  </verify>
  <done>flash_type_4.{cpp,h} renamed to flash_5v_page.{cpp,h} (git R status); guard is __FLASH_5V_PAGE_H__ at all three sites; public function is configure_flash_5v_page; no configure_flash4/flash_type_4/FALSH string survives; MIT header intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update memory.cpp dispatch + flash_utils comments; compile both boards</name>
  <files>firestarter/src/proms/memory.cpp, firestarter/include/flash_utils.h, firestarter/src/proms/flash_utils.cpp</files>
  <read_first>
    - firestarter/src/proms/memory.cpp (includes L14-15; protocol-chain calls L86,91; legacy mem_type fallback calls L130,133 — TWO call-site groups per PATTERNS)
    - firestarter/include/flash_utils.h (comment L66 "Used by flash_type_3 and flash_type_4")
    - firestarter/src/proms/flash_utils.cpp (comment L79 "Used by flash3 and flash4")
  </read_first>
  <action>
    In memory.cpp: update the two #include lines (L14-15) to reference flash_nor_unlock.h and flash_5v_page.h. Rename ALL FOUR call sites: the protocol-guard chain calls (~L86 configure_flash3 → configure_flash_nor_unlock inside the PROTO_FLASH_NOR_UNLOCK arm; ~L91 configure_flash4 → configure_flash_5v_page inside the PROTO_FLASH_5V_PAGE || PROTO_PHANTOM_0x35 || PROTO_PHANTOM_0x39 arm) AND the legacy mem_type fallback calls (~L130 configure_flash3 → configure_flash_nor_unlock; ~L133 configure_flash4 → configure_flash_5v_page). Do NOT touch the TYPE_FLASH_TYPE_3/TYPE_FLASH_TYPE_4 #define references or any protocol integer.
    In flash_utils.h and flash_utils.cpp: update the descriptive-prose comments that name the old handlers so they read in terms of the new handler names (nor-unlock / 5V-page), keeping the comment semantics intact.
    Then run `pio run -t clean` (drops stale .pio/*.o keyed by old source path — Pitfall 3) followed by `pio run -e uno` and `pio run -e leonardo` to prove the renamed symbols link.
  </action>
  <verify>
    <automated>cd firestarter && ! grep -rn 'flash_type_3\|flash_type_4\|configure_flash3\|configure_flash4' src/proms/memory.cpp && ! grep -n 'flash_type_3\|flash_type_4\|flash3\|flash4' include/flash_utils.h src/proms/flash_utils.cpp && pio run -t clean >/dev/null 2>&1; pio run -e uno && pio run -e leonardo && echo BUILD_OK</automated>
  </verify>
  <done>memory.cpp includes + all four call sites reference the renamed headers/functions; TYPE_FLASH_TYPE_3/4 #defines untouched; flash_utils comments updated; `pio run -e uno` and `pio run -e leonardo` both compile and link green after a clean build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Pure firmware identifier rename — no new input, wire, or trust boundary crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-01 | Tampering | header-guard rewrite (guard collision could silently skip a file's contents) | medium | mitigate | Each renamed header gets a unique correctly-spelled guard token; post-rename grep asserts no FALSH survives and each new token is present; both-board compile proves no skipped translation unit |
| T-104-02 | Tampering | VPP-safety invariant drift during rename (5V handler must never assert 12V VPP) | low | accept | No handler logic changes (identifier-only rename); the unchanged test_val VPP-safety suites re-prove it in Plan 03; accepted because rename is mechanical |
</threat_model>

<verification>
- `git status --porcelain` shows both file-pairs as renames (R), not delete+add.
- `grep -rn 'FALSH\|flash_type_3\|flash_type_4' firestarter/src firestarter/include` returns nothing.
- `pio run -t clean && pio run -e uno && pio run -e leonardo` all green.
- No protocol integer, TYPE_FLASH_TYPE_3/4 #define, chip_database.json, constants, or CLI file touched.
</verification>

<success_criteria>
Both firmware boards compile with the renamed files/functions; git records renames with history; guards correct and unique; no old firmware-source symbol survives; wire integers and #define names unchanged.
</success_criteria>

<output>
Create `.planning/phases/104-rename-protocol-header-and-cpp-files-to-descriptive-protocol/104-01-SUMMARY.md` when done.
</output>
