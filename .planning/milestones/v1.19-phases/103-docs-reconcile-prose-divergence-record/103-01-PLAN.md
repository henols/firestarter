---
phase: 103-docs-reconcile-prose-divergence-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/doc/PROTOCOLS.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "All 12 §1.x headings carry the PROTO_ token (not the old FLASH-AMD-STD/EPROM-QUICK/ALT/SRAM-STD slug jargon), and the trailing hex + description stay verbatim."
    - "Every in-doc #1.x fragment link resolves — grep '](#1' PROTOCOLS.md lists 8+ links and each anchor matches a real renamed heading."
    - "Each of the 9 INV rows names its PROTO_ token beside the raw hex in the behavior column, while INV ids + handler-file + native-test-function + suite-path columns stay byte-identical."
    - "A dedicated 'Name <-> Slug Divergence' callout states the three D-04 facts (frozen-slug col is the old-slug<->new-name map; datasheets/ slugs NOT renamed; host ASCII-normalizes dashes)."
    - "The three D-02 locked retentions are untouched: the approved 0x06 name 'Flash — AMD/SST unlock-sequence NOR', every frozen slug string + datasheets/ citation path, and all §2 minipro-provenance prose."
  artifacts:
    - firestarter/doc/PROTOCOLS.md
  key_links:
    - "§3 cross-link anchors (~L410-417) -> renamed §1.x headings (GitHub slugger, regenerated in lockstep)."
    - "§1.x heading text -> its own GitHub auto-anchor (rename changes the anchor)."
    - "The INV matrix's 'Planned native test function name' column strings (SAFE-02 grep contract) must stay byte-identical — these are frozen planned-name strings in the doc, not existing test functions."
---

<objective>
Reconcile the prose face of `firestarter/doc/PROTOCOLS.md` to the operator-approved
Phase-100 name set: rename the twelve §1.x headings to the `PROTO_` token form and
regenerate the eight §3 cross-link anchors in lockstep (D-01), purge residual minipro
bucket-label jargon from facet prose with three explicit locked retentions (D-02),
augment each INV-01..09 row's behavior text with the `PROTO_` token beside the raw hex
while keeping the SAFE-02 grep-contract columns byte-identical (D-03), and add a dedicated
"Name ↔ Slug Divergence" callout recording the frozen-slug map, the NAME-F1 slug freeze,
and the Phase-102 host ASCII-dash deviation (D-04).

Purpose: Make the human reference coherent with the tokens applied in firmware (Phase 101)
and host display (Phase 102), and record the name↔slug divergence in one authoritative place.
Output: One edited Markdown file (`firestarter/doc/PROTOCOLS.md`), reviewable as a single git diff.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-docs-reconcile-prose-divergence-record/103-CONTEXT.md
@.planning/phases/103-docs-reconcile-prose-divergence-record/103-RESEARCH.md
@.planning/phases/103-docs-reconcile-prose-divergence-record/103-PATTERNS.md

# The single work surface — read in full before editing:
@firestarter/doc/PROTOCOLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the 12 §1.x headings to PROTO_ token form + regenerate the 8 §3 cross-link anchors (D-01)</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - firestarter/doc/PROTOCOLS.md — the §0 canonical bucket table (L30-45) is the authoritative token source; §1.x headings (L67, L87, L107, L127, L146, L165, L184, L203, L223, L242, L280, L298); §3 cross-links (L408-417).
    - .planning/phases/103-docs-reconcile-prose-divergence-record/103-PATTERNS.md — the 12-heading token substitution table and the 8-link cross-link block (exact before shapes).
    - .planning/phases/103-docs-reconcile-prose-divergence-record/103-RESEARCH.md — Architecture Pattern 1 (GitHub slugger rules). VERIFIED against this doc's CURRENT working anchors (see verify block): lowercase; keep underscores; drop `#`/`:`/`(`/`)`/`,`/`/` and the dot in `1.N`; spaces->`-`; **em-dash `—` -> `--` (two hyphens)**; **en-dash `–` -> `-` (one hyphen)**; a dropped char between two spaces leaves the surrounding space-hyphens (this is why `1.5 — 0x0B …` slugs to `15----0x0b-…`, and `12–25 V` slugs to `12-25-v`, and `UV-EPROM / EE-EPROM` slugs to `uv-eprom--ee-eprom`).
  </read_first>
  <action>
    In each of the 12 §1.x headings replace ONLY the old slug segment between "0xNN " and the ": " with the exact `PROTO_` token copied verbatim from the §0 canonical bucket table (col PROTO_ token). Keep the leading "### 1.N — 0xNN " prefix and the trailing ": <description>" verbatim. The substitutions: L67 FLASH-AMD-STD->PROTO_FLASH_5V_PAGE; L87 FLASH-AMD-ALT->PROTO_FLASH_NOR_UNLOCK; L107 EPROM-STD->PROTO_EPROM_28PIN; L127 EPROM-QUICK->PROTO_EPROM_32PIN; L146 EPROM-LEGACY->PROTO_EPROM_24PIN; L165 EEPROM-POLL->PROTO_EEPROM_PARALLEL; L184 SRAM-32PIN->PROTO_SRAM_32PIN; L203 FLASH-INTEL->PROTO_FLASH_INTEL; L223 SRAM-24PIN->PROTO_SRAM_24PIN; L242 SRAM-STD->PROTO_SRAM_28PIN; L280 SRAM-512K-1M->PROTO_SRAM_32PIN_NVRAM; L298 EEPROM-X88C64->PROTO_EEPROM_8051BUS. Do NOT invent tokens — copy the exact string from §0.
    Do NOT touch the "**Folder slug (col 1):**" line under each heading (D-02 retention #2 — the frozen slug stays there as the divergence record).
    Then regenerate all 8 §3 cross-links in the "Cross-links to per-bucket sections" block (~L410-417): change the visible link text "(0xNN SLUG)" to "(0xNN PROTO_TOKEN)" AND regenerate each "#..." anchor from the NEW heading text using the GitHub slugger. Slugger rules (proven against this doc's current working anchors): lowercase; keep `_`; drop `#`/`:`/`(`/`)`/`,`/`/` and the dot in "1.N"; spaces->`-`; **em-dash `—` -> `--` (two hyphens)**; en-dash `–` -> `-` (one hyphen). Note the surviving multi-hyphen runs stay: `1.N — 0xNN …` renders `…N----0xnn…` (dot dropped + space + em-dash-as-`--` + space), `UV-EPROM / EE-EPROM` renders `uv-eprom--ee-eprom`, `12–25 V` renders `12-25-v`. The ONLY heading token that changes vs the current anchor is the old-slug segment -> the `PROTO_` token (underscores kept verbatim). Regenerate from the ACTUAL final heading text — the verify block re-derives the golden anchor set from the rendered headings and greps every `](#…)` fragment against it, so a hand-guessed anchor will fail; do NOT hand-guess. Leave the reader-router links at L16/L18/L19 (`#1-real-protocol-buckets` etc.) unchanged — they point at the top-level `## N.` headings, which are NOT renamed.
    Do NOT edit the §0 canonical bucket table or the handler-family table (L22-57) — the dispatch-mirror guard parses those and any change risks GATE-01.
  </action>
  <verify>
    <automated>cd firestarter && grep -c '^### 1\.[0-9]* — 0x[0-9A-Fa-f]* PROTO_' doc/PROTOCOLS.md | grep -qx 12 && echo HEADINGS_OK && python3 -c "
import re, sys, pathlib
t = pathlib.Path('doc/PROTOCOLS.md').read_text()
# Golden anchor set = GitHub slug of every ACTUAL rendered heading in the file.
# Slugger is proven against this doc's current working anchors (em-dash->'--', en-dash->'-').
hs = re.findall(r'^#{1,6} (.+)$', t, re.M)
def slug(h):
    s = h.strip().lower().replace('—', '--').replace('–', '-')
    s = re.sub(r'[^0-9a-z_ -]', '', s)
    return s.replace(' ', '-')
anchors = {slug(h) for h in hs}
frags = re.findall(r'\]\(#([0-9a-z][0-9a-z_-]*)\)', t)
bad = [f for f in frags if f not in anchors]
if bad:
    print('STALE_ANCHORS:', bad); sys.exit(1)
print('ANCHORS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### 1\.[0-9]* — 0x[0-9A-Fa-f]* PROTO_' doc/PROTOCOLS.md` returns exactly 12 (all headings token-form).
    - `grep -E '^### 1\.[0-9]+ — 0x[0-9A-Fa-f]+ (FLASH-AMD|EPROM-STD|EPROM-QUICK|EPROM-LEGACY|EEPROM-POLL|SRAM-STD|SRAM-32PIN:|SRAM-24PIN|SRAM-512K|EEPROM-X88C64|FLASH-INTEL)' doc/PROTOCOLS.md` returns no matches (no old slug jargon left in any heading).
    - Every `](#1...)` fragment link in the doc resolves to a heading whose GitHub slug matches the anchor (the verify loop prints ANCHORS_OK and no STALE_ANCHOR line).
    - `grep -c 'Folder slug (col 1)' doc/PROTOCOLS.md` is unchanged from pre-edit (12) and each still shows its frozen slug string.
    - `git -C firestarter diff --unified=0 doc/PROTOCOLS.md` shows no changed line between the §0 tables (the `| hex |` bucket table and the handler-family table, L22-57).
  </acceptance_criteria>
  <done>All 12 headings are in `PROTO_` token form, the 8 cross-link anchors + link text are regenerated and every `#1.x` fragment resolves, the frozen slug lines and §0 tables are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Augment the 9 INV rows with PROTO_ token beside hex + purge the two prose jargon sentences (D-03, D-02)</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - firestarter/doc/PROTOCOLS.md — the §3 INV matrix (L396-406) and the two prose lines flagged for rephrase: §1.8 pin-roles (L219 "Unlike AMD flash…") and §2.1 (L352 "These are not FLASH-AMD-STD variants…").
    - .planning/phases/103-docs-reconcile-prose-divergence-record/103-PATTERNS.md — the per-row hex->token map and the two prose rephrase examples.
    - firestarter/CLAUDE.md — the PROTO_ token <-> hex table (Algorithm Handlers) to confirm the token for each hex in an INV behavior cell.
  </read_first>
  <action>
    For each of the 9 INV rows (L398-406) prepend the correct `PROTO_` token (with the hex in parens) to the START of the "One-line behavior" column ONLY, per the hex->token map: INV-01/INV-02 (0x0B) -> `PROTO_EPROM_24PIN`; INV-03 (0x08) -> `PROTO_EPROM_32PIN`; INV-04 (flash4 0x05) -> `PROTO_FLASH_5V_PAGE`; INV-05 (0x07) -> `PROTO_EPROM_28PIN`; INV-06 (mentions 0x08/0x0B/0x07 pulse defaults — augment in-prose naturally, e.g. name `PROTO_EPROM_32PIN` (0x08)/`PROTO_EPROM_24PIN` (0x0B)/`PROTO_EPROM_28PIN` (0x07) beside their existing hexes without changing the µs values); INV-07 (0x28) -> `PROTO_SRAM_28PIN`; INV-08 (0x07) -> `PROTO_EPROM_28PIN` (keep the "(dispatch-only scope)" prose and `build_db.py` reference verbatim); INV-09 (0x06) -> `PROTO_FLASH_NOR_UNLOCK`. Illustrative for INV-01: prepend so it reads "`PROTO_EPROM_24PIN` (0x0B) uses `FLAG_VPE_AS_VPP`…". The raw hex STAYS (it is the dispatch key). Keep the other four columns — INV id, Owning handler file, Planned native test function name, Suite path — BYTE-IDENTICAL (SAFE-02 grep contract).
    Then rephrase two prose sentences off the old bucket labels (D-02): (a) §1.8 pin-roles (~L219) "Unlike AMD flash, there is no address-based unlock; commands go directly." -> rephrase to a neutral descriptor that names the unlock-sequence NOR path by its new token, e.g. "Unlike the unlock-sequence NOR path (0x06 / `PROTO_FLASH_NOR_UNLOCK`), there is no address-based unlock; commands go directly."; (b) §2.1 (~L352) "These are not FLASH-AMD-STD variants, EPROM variants, or any other real protocol." -> rephrase off the raw bucket label, e.g. "These are not 5V page-write flash (`PROTO_FLASH_5V_PAGE`) variants, EPROM variants, or any other real protocol."
    Do NOT touch the §2.1 minipro-provenance rows at L349-350 (`IC2_ALG_ITE` ITE-EC explanation) or any §2 phantom/infeasible provenance prose (D-02 retention #3). Do NOT touch the datasheet-accurate "AMD unlock command addresses (0x5555/0x2AAA)" at §1.2 L103 or the "Quick-Pulse Programming" datasheet citation at §1.4 L135 — those are behavior-correct KEEPS, not bucket jargon.
  </action>
  <verify>
    <automated>cd firestarter && for inv in INV-01 INV-02 INV-03 INV-04 INV-05 INV-06 INV-07 INV-08 INV-09; do grep -qE "^\| $inv \| .*PROTO_" doc/PROTOCOLS.md || { echo "MISSING_TOKEN: $inv"; exit 1; }; done && echo INV_TOKENS_OK && grep -qc 'test_inv01_eprom_0x0B_direct_vpe_rail' doc/PROTOCOLS.md && grep -qc 'test_inv09_flash3_sst39sf040_keep_flash_eeprom' doc/PROTOCOLS.md && echo INV_TESTNAMES_INTACT && ! grep -qE 'Unlike AMD flash' doc/PROTOCOLS.md && ! grep -qE 'not FLASH-AMD-STD variants' doc/PROTOCOLS.md && echo PROSE_PURGED && grep -qF 'IC2_ALG_ITE' doc/PROTOCOLS.md && echo PROVENANCE_RETAINED</automated>
  </verify>
  <acceptance_criteria>
    - For every INV id INV-01..INV-09, `grep -E "^\| INV-0N \| .*PROTO_"` matches (each behavior column now names a PROTO_ token). Zero MISSING_TOKEN lines.
    - The "Planned native test function name" column strings are byte-identical pre/post edit — `grep -F 'test_inv04_flash4_256b_page_boundary' doc/PROTOCOLS.md`, `grep -F 'test_inv01_eprom_0x0B_direct_vpe_rail' doc/PROTOCOLS.md`, and `grep -F 'test_inv09_flash3_sst39sf040_keep_flash_eeprom' doc/PROTOCOLS.md` each still match (SAFE-02 grep contract = the doc's frozen planned-test-name strings, NOT test existence — no `test_inv0*` function exists yet; the column is the "Planned native test function name").
    - `git -C firestarter diff doc/PROTOCOLS.md` shows NO change to the "Owning handler file", "Planned native test function name", or "Suite path" columns of any INV row (only the behavior column changed).
    - `grep 'Unlike AMD flash' doc/PROTOCOLS.md` and `grep 'not FLASH-AMD-STD variants' doc/PROTOCOLS.md` both return nothing (both prose sentences rephrased).
    - `grep -F 'IC2_ALG_ITE' doc/PROTOCOLS.md` still matches (§2 provenance retained); `grep -F 'AMD unlock command addresses (0x5555/0x2AAA)' doc/PROTOCOLS.md` still matches (datasheet KEEP intact).
  </acceptance_criteria>
  <done>All 9 INV rows name their PROTO_ token beside the hex with the four grep-contract columns byte-identical, the two bucket-label prose sentences are rephrased, and the §2 provenance + datasheet-accurate KEEPs are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the "Name ↔ Slug Divergence" callout recording the three D-04 facts (DOC-02)</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - firestarter/doc/PROTOCOLS.md — the §0 operator-approved blockquote callout (L24-28) is the in-file style template; the §0 frozen-slug column (col 1, L30-45) the callout points at.
    - .planning/phases/103-docs-reconcile-prose-divergence-record/103-PATTERNS.md — the D-04 callout template + the three facts (a/b/c).
    - firestarter_app/firestarter/ic_layout.py (~L472-484) — the host `_PROTOCOL_DISPLAY_NAME` map that uses ASCII hyphens (fact (c): the punctuation deviation from the em-dash col-2 names).
  </read_first>
  <action>
    Add a short dedicated callout subsection titled "Name ↔ Slug Divergence" (heading level and exact placement are Claude's discretion — near the §0 bucket table it references is preferred). Reuse the blockquote style of the existing §0 operator-approved callout (L24-28). State three facts plainly: (a) the §0 bucket-set table's frozen-slug column (col 1, `datasheets/<hex>-<NAME>/` slugs) IS the canonical old-slug↔new-name map — read it left-to-right to see any old slug's new `PROTO_` token + display name; (b) the `datasheets/` folder slugs are intentionally frozen and NOT renamed (NAME-F1 deferred, avoids folder/provenance churn) — this doc records the divergence, it does not resolve it; (c) the host CLI ASCII-normalizes dashes (em-dash `—` / en-dash `–` → ASCII hyphen `-`) in its display strings (`firestarter_app/firestarter/ic_layout.py` `_PROTOCOL_DISPLAY_NAME`), a documented punctuation deviation from the em-dash PROTOCOLS.md col-2 display names (Phase 102 D-02) — the names are otherwise identical. Do NOT edit the §0 tables themselves — the callout points at the slug column, it does not modify it.
  </action>
  <verify>
    <automated>cd firestarter && grep -qiE 'Name (↔|<->|to) Slug Divergence' doc/PROTOCOLS.md && echo CALLOUT_PRESENT && grep -qF 'NAME-F1' doc/PROTOCOLS.md && grep -qiE 'ASCII' doc/PROTOCOLS.md && grep -qF '_PROTOCOL_DISPLAY_NAME' doc/PROTOCOLS.md && grep -qiE 'Phase[ -]?102' doc/PROTOCOLS.md && echo THREE_FACTS_PRESENT && if git diff --unified=0 doc/PROTOCOLS.md | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -qE '^[+-]\|( 0x[0-9A-Fa-f]+ \| |-----|hex \| DB| eprom | sram | flash| eeprom28c | flash_intel | not-implemented |Handler-family)'; then echo TABLE_TOUCHED_FAIL; exit 1; else echo TABLE_UNTOUCHED; fi</automated>
  </verify>
  <acceptance_criteria>
    - A callout titled "Name ↔ Slug Divergence" (or ASCII "Name <-> Slug Divergence") exists in the doc (grep matches CALLOUT_PRESENT).
    - The callout body references all three facts: the frozen-slug column as the old↔new map, `NAME-F1` (slug freeze deferral), and the host ASCII-dash deviation naming `_PROTOCOL_DISPLAY_NAME` + Phase 102 (grep matches THREE_FACTS_PRESENT).
    - `git diff --unified=0 doc/PROTOCOLS.md` shows NO added/removed line matching ANY §0 table row across the whole region (the `| 0xNN |` bucket rows, the table header/separator, and the handler-family table rows `| eprom |`/`| sram |`/`| flash* |`/`| eeprom28c |`/`| flash_intel |`/`| not-implemented |`) — the callout adds a blockquote subsection, it does not edit either §0 table — verify prints TABLE_UNTOUCHED (mirrors Task 1 acceptance criterion 5's L22–57 changed-line discipline).
  </acceptance_criteria>
  <done>The "Name ↔ Slug Divergence" callout is present, states the three D-04 facts, and the §0 tables are untouched.</done>
</task>

</tasks>

<verification>
Whole-file integrity after all three tasks (run from `firestarter/`):
- `grep -n "](#1" doc/PROTOCOLS.md` — every listed fragment resolves to a real heading (no stale anchor).
- `grep -n "datasheets/0x" doc/PROTOCOLS.md` — every citation path retains its frozen slug (D-02 retention #2; count unchanged from pre-edit).
- `grep -n "Flash — AMD/SST unlock-sequence NOR" doc/PROTOCOLS.md` — exactly the pre-existing hits (§0 L33 + §1.2 col-2 L90), em-dash intact (D-02 retention #1).
- `git diff --stat` shows exactly one changed file: `doc/PROTOCOLS.md`.
- No change to `chip_database.json`, any firmware/host source, or the §0 tables.
</verification>

<success_criteria>
- All 12 §1.x headings are in `PROTO_` token form; all 8 §3 cross-link anchors resolve (DOC-01).
- All 9 INV rows name their token beside the hex; INV ids + test-function names byte-identical (DOC-01 / SAFE-02).
- The two bucket-label prose sentences are rephrased; the three D-02 retentions are verbatim-intact.
- The "Name ↔ Slug Divergence" callout records the three D-04 facts (DOC-02).
- The diff is one file; no §0-table / code / DB change.
</success_criteria>

<output>
Create `.planning/phases/103-docs-reconcile-prose-divergence-record/103-01-SUMMARY.md` when done.
</output>
