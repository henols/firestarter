---
phase: 85-datasheet-acquisition
plan: 02
type: execute
wave: 1
depends_on: ["85-01"]
files_modified:
  - firestarter/datasheets/0x05-FLASH-AMD-STD/W29C020.pdf
  - firestarter/datasheets/0x05-FLASH-AMD-STD/W29C040.pdf
  - firestarter/datasheets/0x06-FLASH-AMD-ALT/SST39SF040.pdf
  - firestarter/datasheets/0x07-EPROM-STD/W27C512.pdf
  - firestarter/datasheets/0x07-EPROM-STD/W27E512.pdf
  - firestarter/datasheets/0x07-EPROM-STD/SST27SF512.pdf
  - firestarter/datasheets/0x07-EPROM-STD/ST-M27C512.pdf
  - firestarter/datasheets/0x08-EPROM-QUICK/W27E040.pdf
  - firestarter/datasheets/0x08-EPROM-QUICK/AM27C020.pdf
  - firestarter/datasheets/0x0B-EPROM-LEGACY/2516_EPROM.pdf
  - firestarter/datasheets/0x28-SRAM-STD/FM1608.pdf
  - firestarter/datasheets/0x0D-EEPROM-POLL/AT28C256.pdf
  - firestarter/datasheets/0x0E-SRAM-32PIN/DS1245Y.pdf
  - firestarter/datasheets/0x10-FLASH-INTEL/Intel-28F010.pdf
  - firestarter/datasheets/0x27-SRAM-24PIN/6116.pdf
  - firestarter/datasheets/0x29-SRAM-512K-1M/DS1250Y.pdf
  - firestarter/datasheets/0x34-EEPROM-X88C64/X88C64.pdf
autonomous: true
requirements: [DSHEET-01, DSHEET-02]
must_haves:
  truths:
    - "Each of the 11 on-hand chips has a committed PDF in its correct bucket folder (per the DB-verified bucket map)"
    - "Each of the 6 no-silicon buckets has ≥1 committed representative PDF"
    - "Every committed PDF is a real PDF (first 4 bytes %PDF), not HTML masquerade"
    - "W27E512 is filed under 0x07 (not 0x08); FM1608 is filed under 0x28"
  artifacts:
    - path: "firestarter/datasheets/0x07-EPROM-STD/W27C512.pdf"
      provides: "On-hand 0x07 EPROM-STD datasheet"
    - path: "firestarter/datasheets/0x05-FLASH-AMD-STD/W29C020.pdf"
      provides: "On-hand 0x05 FLASH-AMD-STD datasheet"
    - path: "firestarter/datasheets/0x34-EEPROM-X88C64/X88C64.pdf"
      provides: "No-silicon 0x34 representative (Xicor data book)"
  key_links:
    - from: "executor curl"
      to: "RESEARCH §URL table verified-200 source URLs"
      via: "lift exact URL per filename"
      pattern: "%PDF"
---

<objective>
Download and commit one datasheet PDF for every dispatched protocol: the 11 on-hand chips across 6 buckets (DSHEET-01) and one representative per the 6 no-silicon buckets (DSHEET-02). Every URL is pre-verified in the RESEARCH §URL table — lift the exact URLs directly; no re-research needed.

Purpose: These PDFs are the verification + rationale source the Phase 86 naming pass and Phase 89 bench ledger cite. The committed file is the durable artifact (URLs rot) — RESEARCH §"Don't Hand-Roll".

Output: 17 PDFs across 12 bucket folders under `firestarter/datasheets/`, all committed to the v1.16 branch, all passing the `%PDF` magic-byte check.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-datasheet-acquisition/85-RESEARCH.md
@.planning/phases/85-datasheet-acquisition/85-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download the 11 on-hand chip datasheets (DSHEET-01)</name>
  <files>firestarter/datasheets/0x05-FLASH-AMD-STD/{W29C020,W29C040}.pdf, firestarter/datasheets/0x06-FLASH-AMD-ALT/SST39SF040.pdf, firestarter/datasheets/0x07-EPROM-STD/{W27C512,W27E512,SST27SF512,ST-M27C512}.pdf, firestarter/datasheets/0x08-EPROM-QUICK/{W27E040,AM27C020}.pdf, firestarter/datasheets/0x0B-EPROM-LEGACY/2516_EPROM.pdf, firestarter/datasheets/0x28-SRAM-STD/FM1608.pdf</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"URL Findings — On-hand chips" table — the verified source URL per filename; §"Common Pitfalls" 1/2/3; §"Environment Availability" bot-walled hosts)
  </read_first>
  <action>
    Create the on-hand bucket folders and download each PDF to its exact target path using `curl -sL -o <target> "<url>"`, lifting the verified-200 URLs from the RESEARCH §"URL Findings — On-hand chips" table. Bucket placement is DB-verified — file W27E512 under `0x07-EPROM-STD/` (NOT 0x08 — it shares the W27C512 silicon entry, algorithm=7; RESEARCH Pitfall 1) and FM1608 under `0x28-SRAM-STD/`.
    Verified primary URLs to use (per RESEARCH table): W29C020 → `http://bitsavers.org/components/winbond/W29C020.PDF`; W29C040 → `https://datasheet.octopart.com/W29C040-90-Winbond-datasheet-181529586.pdf`; SST39SF040 → `https://ww1.microchip.com/downloads/aemDocuments/documents/MPD/ProductDocuments/DataSheets/SST39SF010A-SST39SF020A-SST39SF040-Data-Sheet-DS20005022.pdf`; W27C512 → `http://bitsavers.org/components/winbond/W27C512_64Kx8_EEPROM_199911.pdf`; W27E512 → `https://media.digikey.com/pdf/Data%20Sheets/Winbond%20PDFs/W27C512.pdf` (the W27C512 doc covers both siblings — RESEARCH note 1); ST-M27C512 → `https://media.digikey.com/pdf/data%20sheets/st%20microelectronics%20pdfs/m27c512.pdf`; AM27C020 → `https://web.stanford.edu/class/ee183/datasheets/27c020.pdf` (fallback `https://www.dcddcc.com/blog/e3646a/datasheet.am27c020.pdf`); 2516_EPROM → `https://archive.org/download/2516_EPROM/2516_EPROM.pdf` (mirror `https://downloads.reactivemicro.com/Electronics/ROM/2516%20EPROM.pdf`); FM1608 → `https://www.farnell.com/datasheets/82469.pdf`.
    Two fetch-time-fallback parts: W27E040 (only aggregator leaf at alldatasheet `https://www.alldatasheet.com/datasheet-pdf/view/47657/WINBOND/W27E040.html`) and SST27SF512 (octopart sibling SST27SF256 family doc `https://datasheet.octopart.com/SST27SF256-70-3C-PG-SST-datasheet-7196.pdf` verified-200; exact-leaf at alldatasheet `https://www.alldatasheet.com/datasheet-pdf/pdf/46493/SST/SST27SF512.html`). For each, attempt the exact-part source first; if `head -c4` of the result is not `%PDF` (HTML interstitial), fall back to the verified family/sibling PDF and note the substitution for the README provenance (record the actual URL used + whether it is exact-leaf or family/sibling so Plan 03 can set the substitute flag honestly). Do NOT script downloads from `docs.rs-online.com`, `jameco.com`, or `analog.com` media (bot-walled 403/000 — RESEARCH Pitfall 2). After each download run `head -c4 <file>` and confirm `%PDF`; any file that is `text/html` is a failed fetch — retry the fallback. Record the source URL actually used and the retrieval date for each file (Plan 03 consumes these for the provenance columns).
  </action>
  <verify>
    <automated>cd firestarter && for f in 0x05-FLASH-AMD-STD/W29C020.pdf 0x05-FLASH-AMD-STD/W29C040.pdf 0x06-FLASH-AMD-ALT/SST39SF040.pdf 0x07-EPROM-STD/W27C512.pdf 0x07-EPROM-STD/W27E512.pdf 0x07-EPROM-STD/SST27SF512.pdf 0x07-EPROM-STD/ST-M27C512.pdf 0x08-EPROM-QUICK/W27E040.pdf 0x08-EPROM-QUICK/AM27C020.pdf 0x0B-EPROM-LEGACY/2516_EPROM.pdf 0x28-SRAM-STD/FM1608.pdf; do p="datasheets/$f"; { [ -f "$p" ] && [ "$(stat -c%s "$p")" -gt 1024 ] && head -c4 "$p" | grep -q '%PDF'; } || { echo "BAD: $p"; exit 1; }; done; echo "ALL-11-ON-HAND-OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 target files exist, each >1KB, each with first 4 bytes `%PDF`
    - `datasheets/0x07-EPROM-STD/W27E512.pdf` exists (W27E512 filed under 0x07, NOT under 0x08)
    - `datasheets/0x28-SRAM-STD/FM1608.pdf` exists (FM1608 filed under 0x28)
    - No file is `text/html` (`file --mime-type` reports `application/pdf` for each)
    - The actual source URL + retrieval date used per file is recorded for Plan 03's provenance table
  </acceptance_criteria>
  <done>All 11 on-hand chip datasheets are downloaded into their DB-correct bucket folders, each a verified real PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Download the 6 no-silicon representative datasheets (DSHEET-02)</name>
  <files>firestarter/datasheets/0x0D-EEPROM-POLL/AT28C256.pdf, firestarter/datasheets/0x0E-SRAM-32PIN/DS1245Y.pdf, firestarter/datasheets/0x10-FLASH-INTEL/Intel-28F010.pdf, firestarter/datasheets/0x27-SRAM-24PIN/6116.pdf, firestarter/datasheets/0x29-SRAM-512K-1M/DS1250Y.pdf, firestarter/datasheets/0x34-EEPROM-X88C64/X88C64.pdf</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"URL Findings — No-silicon buckets" table — the D-06 exemplar + verified source URL per bucket; §"Common Pitfalls" 2 bot-walled analog.com)
  </read_first>
  <action>
    Create the 6 no-silicon bucket folders and download one representative PDF each, using the D-06 best-documented-exemplar picks from the RESEARCH §"URL Findings — No-silicon buckets" table (all 6 picks are verified DB members of their protocol_id). Verified URLs to use: 0x0D AT28C256 → `https://ww1.microchip.com/downloads/en/DeviceDoc/doc0006.pdf` (mirror `https://eater.net/datasheets/28c256.pdf`); 0x0E DS1245Y → `https://www.futurlec.com/Datasheet/Dallas/DS1245Y.pdf`; 0x10 Intel-28F010 → `https://www.ardent-tool.com/datasheets/Intel_28F010.pdf`; 0x27 6116 → `http://www.princeton.edu/~mae412/HANDOUTS/Datasheets/6116.pdf` (mirror `https://www.silicon-ark.co.uk/datasheets/hm6116-datasheet-mhs.pdf`); 0x34 X88C64 → `https://www.bitsavers.org/components/xicor/1990_Xicor_Data_Book.pdf` (vendor data-book scan containing X88C64 — note in provenance it is a data book, not a leaflet); 0x29 DS1250Y → analog.com is bot-walled (000 to curl) so do NOT script it — use a mirror or fall back to the verified sibling DS1245Y if no DS1250Y mirror fetches as `%PDF` (RESEARCH note 5 + Assumption A3; if you fall back to DS1245Y silicon, name the file `DS1245Y.pdf` and record it as a representative/substitute for Plan 03). After each download run `head -c4 <file>` and confirm `%PDF`. Record the actual source URL + retrieval date + whether the pick is the exact exemplar or a substitute, for Plan 03's provenance table.
  </action>
  <verify>
    <automated>cd firestarter && for d in 0x0D-EEPROM-POLL 0x0E-SRAM-32PIN 0x10-FLASH-INTEL 0x27-SRAM-24PIN 0x29-SRAM-512K-1M 0x34-EEPROM-X88C64; do n=$(find "datasheets/$d" -maxdepth 1 -name '*.pdf' -size +1k 2>/dev/null | wc -l); [ "$n" -ge 1 ] || { echo "BAD: $d has no >1k PDF"; exit 1; }; for f in datasheets/$d/*.pdf; do head -c4 "$f" | grep -q '%PDF' || { echo "BAD: $f not %PDF"; exit 1; }; done; done; echo "ALL-6-NO-SILICON-OK"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 no-silicon bucket dirs contains ≥1 PDF >1KB with first 4 bytes `%PDF`
    - The 0x29 representative is committed (DS1250Y if a mirror fetched, else DS1245Y recorded as a substitute for Plan 03)
    - No file is `text/html`
    - Each pick's actual source URL + retrieval date + exact-vs-substitute status is recorded for Plan 03
  </acceptance_criteria>
  <done>All 6 no-silicon buckets hold a verified representative PDF; any substitute (e.g. 0x29 fallback) is recorded for honest README flagging.</done>
</task>

<task type="auto">
  <name>Task 3: Commit all 17 datasheets to the firestarter sub-repo</name>
  <files>firestarter/datasheets/**/*.pdf</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"Project Constraints" — commit INSIDE the firestarter submodule; §"Don't Hand-Roll" — commit the file, not the link)
  </read_first>
  <action>
    Commit the 17 downloaded PDFs inside the firestarter sub-repo on the v1.16 branch. Stage only the `datasheets/` PDF paths (e.g. `git add datasheets/0x*/*.pdf` — do NOT `git add -A`; the working tree holds untracked `core.*` dumps and `.pio/` that must stay out). Verify `git diff --cached --name-only` shows only paths under `datasheets/` (SAFE-05 invariant). Commit message: `docs(85): commit 17 protocol datasheets (DSHEET-01/02)`. Then run the Wave-0 check script — note it will still report FAIL/RED at this point only because `datasheets/README.md` does not yet exist (Plan 03); every bucket-folder and %PDF assertion should already pass, so confirm the only remaining FAIL lines reference README/exclusions, not missing/invalid PDFs.
  </action>
  <verify>
    <automated>cd firestarter && git diff --cached --name-only 2>/dev/null | grep -v '^datasheets/' | grep -q . && { echo "SAFE-05-VIOLATION"; exit 1; } || true; git log -1 --name-only | grep -q 'datasheets/' && echo "COMMITTED-DATASHEETS-ONLY"</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && git log -1 --name-only` shows only `datasheets/` paths (17 PDFs)
    - No `core.*` / `.pio/` / source files staged or committed
    - Running `bash datasheets/datasheets-check.sh` shows the PDF/bucket assertions passing; any remaining FAIL references only README/exclusion checks (deferred to Plan 03)
  </acceptance_criteria>
  <done>All 17 datasheets are committed to the firestarter sub-repo on the v1.16 branch, touching only datasheets/ (SAFE-05); the check script's PDF/bucket assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external host → repo file | A downloaded artifact could be HTML/malware masquerading as a PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-SC1 | Tampering / Spoofing | downloaded `.pdf` artifacts | mitigate | every task verifies first 4 bytes are `%PDF` and size >1k before commit; manufacturer/bitsavers/archive.org primaries preferred over aggregators; bot-walled hosts (rs-online/jameco/analog.com) explicitly NOT scripted |
| T-85-NA | N/A | rest of phase | accept | Static-asset phase: no executable surface, no input parsing, no product network surface, no secrets/PII. |
</threat_model>

<verification>
- `bash datasheets/datasheets-check.sh` — every bucket-folder + %PDF assertion passes (README/exclusion FAILs are expected until Plan 03)
- `file --mime-type datasheets/**/*.pdf` reports `application/pdf` for all 17
- `git diff --cached --name-only` shows only `datasheets/` paths
</verification>

<success_criteria>
- 11 on-hand chip PDFs in DB-correct buckets (W27E512→0x07, FM1608→0x28)
- 6 no-silicon representative PDFs (one per bucket)
- All 17 are verified real PDFs and committed to the v1.16 branch, SAFE-05 intact
</success_criteria>

<output>
Create `.planning/phases/85-datasheet-acquisition/85-02-SUMMARY.md` when done — INCLUDE the per-file provenance table (filename ↔ actual source URL ↔ retrieval date ↔ exact/substitute) so Plan 03 can author the README provenance columns without re-fetching.
</output>
