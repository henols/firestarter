---
phase: 85-datasheet-acquisition
plan: 03
type: execute
wave: 2
depends_on: ["85-02"]
files_modified:
  - firestarter/datasheets/README.md
autonomous: true
requirements: [DSHEET-03, SAFE-05]
must_haves:
  truths:
    - "datasheets/README.md indexes every one of the 12 bucket folders with hex ↔ proposed name ↔ handler ↔ filename ↔ on-hand status + provenance"
    - "README names the phantom (0x35, 0x39) and infeasible (0x11, 0x2A, 0x2B, 0x2C) buckets as explicit exclusions with no folder"
    - "README documents the D-02/D-03 substitute / MISSING-UNSOURCED policy even though no row needs it"
    - "bash datasheets/datasheets-check.sh prints datasheets-check: PASS and exits 0"
  artifacts:
    - path: "firestarter/datasheets/README.md"
      provides: "DSHEET-03 index + exclusions + provenance"
      contains: "0x35"
      min_lines: 40
  key_links:
    - from: "firestarter/datasheets/README.md"
      to: "firestarter/datasheets/<bucket>/*.pdf"
      via: "every README filename reference maps to a real committed file"
      pattern: "\\.pdf"
---

<objective>
Author `datasheets/README.md` — the DSHEET-03 index — then run the final phase-gate validation. The README maps every bucket folder to its hex ID, proposed name, firmware handler, datasheet filename(s), on-hand status, and per-file provenance (source URL + retrieval date + substitute flag, D-08), names the phantom/infeasible bucket exclusions, and records the standing D-02/D-03 substitution/gap policy.

Purpose: This is the legible index the Phase 86 naming pass and Phase 89 ledger key off. It is authored AFTER downloads so every filename reference maps to a real committed file.

Output: `firestarter/datasheets/README.md`, committed to the v1.16 branch; `datasheets-check.sh` passes at the phase gate.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-datasheet-acquisition/85-RESEARCH.md
@.planning/phases/85-datasheet-acquisition/85-CONTEXT.md
@.planning/phases/85-datasheet-acquisition/85-02-SUMMARY.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author datasheets/README.md (DSHEET-03 index + exclusions + provenance)</name>
  <files>firestarter/datasheets/README.md</files>
  <read_first>
    - firestarter/CLAUDE.md (§"Algorithm Handlers" table — AUTHORITATIVE source for the hex ↔ name ↔ handler-file columns; more current than research/PROTOCOLS.md)
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"README Index Shape" — required columns + exclusions list; §"Folder / Naming Map" — handler annotations per bucket; §"Common Pitfalls" 3 — 2516 has no committed-DB entry)
    - .planning/phases/85-datasheet-acquisition/85-CONTEXT.md (D-05 proposed names, D-07 substitute flag, D-08 provenance columns, D-02/D-03 policy)
    - .planning/phases/85-datasheet-acquisition/85-02-SUMMARY.md (the per-file source URL + retrieval date + exact/substitute status recorded during downloads)
  </read_first>
  <action>
    Write `firestarter/datasheets/README.md` as the bucket index. Use ONE row per bucket folder (12 rows), with these columns (D-08 + DSHEET-03): `hex` | `proposed name` | `handler (file)` | `datasheet filename(s)` | `on-hand status` | `source URL` | `retrieved` | `substitute?`. Source the `proposed name` from D-05 / research SUMMARY (e.g. FLASH-AMD-STD, FLASH-AMD-ALT, EPROM-STD, EPROM-QUICK, EPROM-LEGACY, EEPROM-POLL, SRAM-32PIN, FLASH-INTEL, SRAM-24PIN, SRAM-STD, SRAM-512K-1M, EEPROM-X88C64). Source the `handler (file)` column from firestarter/CLAUDE.md §"Algorithm Handlers" (0x05→flash_type_4.cpp, 0x06→flash_type_3.cpp, 0x07/0x08/0x0B→eprom.cpp, 0x0D→eeprom_28c.cpp, 0x0E/0x27/0x28/0x29→sram.cpp, 0x10→flash_intel.cpp, 0x34→not_implemented.cpp). `on-hand status` = `on-hand` for the 6 on-hand buckets, `no-silicon (representative)` for the 6 no-silicon buckets. Fill `source URL` / `retrieved` / `substitute?` from the 85-02-SUMMARY provenance table — set `substitute?` to `exact` for genuinely exact datasheets and `representative`/`substitute` (naming the actual part filed vs the one it stands in for, per D-07) for any fetch-time fallback (e.g. the 0x29 DS1250Y→DS1245Y case, the W27E512-shares-W27C512-doc case, or any SST27SF512 family-doc fallback). For shared buckets (0x05, 0x07, 0x08) list all PDFs in the filename column.
    Add an explicit **Exclusions** section naming, with no folder: phantom buckets `0x35` (FLASH_EEPROM — IC2_ALG_ITE is an ITE EC MCU label, not a memory algo, zero DB chips) and `0x39` (FLASH_EEPROM2 — no IC2_ALG constant, zero DB chips); infeasible buckets `0x11` (FWH/LPC-serial), `0x2A`, `0x2B`, `0x2C` (GAL/PLD/PIC — fail-closed on RURP). Each forbidden hex string MUST literally appear in the README (the check script greps for them).
    Add a **Sourcing policy** note documenting D-02 (substitute a compatible/second-source part, flagged in README) and D-03 (a truly-unobtainable datasheet is recorded as a `MISSING`/`UNSOURCED` row with what was tried, and the phase still completes) — even though research found exact sources for all 17 and no row currently invokes either. Add the 2516 honesty note: it is filed under 0x0B but has NO committed-`chip_database.json` entry (it is a v1.15 user-override row) — annotate this in the 0x0B row or a footnote. Do NOT author canonical names, DB corrections, or bench results — those are Phases 86/89 (CONTEXT Deferred Ideas). Do NOT create folders for any excluded bucket.
  </action>
  <verify>
    <automated>cd firestarter && for h in 0x05 0x06 0x07 0x08 0x0B 0x0D 0x0E 0x10 0x27 0x28 0x29 0x34 0x35 0x39 0x11 0x2A 0x2B 0x2C; do grep -q "$h" datasheets/README.md || { echo "README missing $h"; exit 1; }; done; echo "README-COVERS-12-BUCKETS-AND-6-EXCLUSIONS"</automated>
  </verify>
  <acceptance_criteria>
    - `datasheets/README.md` exists, ≥40 lines, with a row for each of the 12 bucket folders
    - README contains the literal strings `0x35`, `0x39`, `0x11`, `0x2A`, `0x2B`, `0x2C` in an exclusions context
    - README has the D-08 columns: hex, proposed name, handler, filename(s), on-hand status, source URL, retrieved, substitute?
    - README documents the D-02/D-03 substitute / MISSING-UNSOURCED policy and the 2516 no-DB-entry note
    - Every `.pdf` filename referenced in README maps to a real committed file (the check script's WARN line is empty)
  </acceptance_criteria>
  <done>datasheets/README.md indexes all 12 buckets with full D-08 provenance, names all 6 excluded buckets, and documents the sourcing policy.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase-gate validation and commit the README</name>
  <files>firestarter/datasheets/README.md</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-VALIDATION.md (§"Validation Sign-Off" — the phase-gate contract: datasheets-check: PASS, exit 0)
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"Reference check script" — what PASS means)
  </read_first>
  <action>
    Run `bash datasheets/datasheets-check.sh` from the firestarter repo root. It must now print `datasheets-check: PASS` and exit 0 (all 12 buckets present with valid PDFs, README present, all 6 exclusions named, no forbidden folders, no dangling README filename references). If it FAILs, fix the README (or a misplaced/invalid PDF) until it passes — do NOT edit the check script to force a pass. Then commit the README inside the firestarter sub-repo on the v1.16 branch: stage only `datasheets/README.md` (explicit path — not `git add -A`). Verify `git diff --cached --name-only` shows only the `datasheets/` path (SAFE-05 invariant). Commit message: `docs(85): datasheets README index + phase-gate PASS (DSHEET-03)`.
  </action>
  <verify>
    <automated>cd firestarter && bash datasheets/datasheets-check.sh | tail -1 | grep -q 'datasheets-check: PASS' && echo "PHASE-GATE-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `bash datasheets/datasheets-check.sh` prints `datasheets-check: PASS` and exits 0
    - The check script was NOT modified to force the pass
    - `cd firestarter && git log -1 --name-only` shows only `datasheets/README.md`
    - `git diff --cached --name-only` (pre-commit) showed only `datasheets/` paths (SAFE-05)
  </acceptance_criteria>
  <done>The phase-gate structural check PASSes (exit 0) and README is committed to the v1.16 branch, touching only datasheets/ (SAFE-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external host → repo file | (inherited from Plan 02) downloaded artifact could masquerade as PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-SC1 | Tampering / Spoofing | committed `.pdf` artifacts | mitigate | the phase-gate `datasheets-check.sh` re-asserts `%PDF` magic + size on every committed PDF before the README is finalized |
| T-85-NA | N/A | rest of phase | accept | Static-asset phase: no executable surface, no input parsing, no product network surface, no secrets/PII. |
</threat_model>

<verification>
- `bash datasheets/datasheets-check.sh` → `datasheets-check: PASS`, exit 0
- README contains all 12 bucket hex IDs + all 6 excluded hex IDs
- `git diff --cached --name-only` shows only `datasheets/` paths
- Manual (operator): spot-open 2–3 PDFs, confirm title-page part matches the README row + substitute flags are honest (per 85-VALIDATION §"Manual-Only Verifications")
</verification>

<success_criteria>
- README.md indexes all 12 buckets with D-08 provenance + names all 6 exclusions + documents D-02/D-03 policy
- Phase-gate `datasheets-check.sh` PASSes
- README committed to v1.16 branch, SAFE-05 intact
</success_criteria>

<output>
Create `.planning/phases/85-datasheet-acquisition/85-03-SUMMARY.md` when done
</output>
