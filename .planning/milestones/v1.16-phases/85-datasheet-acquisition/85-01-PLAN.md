---
phase: 85-datasheet-acquisition
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - firestarter/datasheets/datasheets-check.sh
  - firestarter/datasheets/.gitkeep
autonomous: true
requirements: [SAFE-05]
must_haves:
  truths:
    - "Running bash datasheets/datasheets-check.sh from the firestarter repo root executes and reports a structured PASS/FAIL"
    - "The v1.16-protocol-first-architecture-rebuild branch is the active branch in the firestarter sub-repo"
    - "The only repo change is under datasheets/ (SAFE-05 holds)"
  artifacts:
    - path: "firestarter/datasheets/datasheets-check.sh"
      provides: "Wave-0 structural validation covering DSHEET-01/02/03 + SAFE-05"
      contains: "datasheets-check: PASS"
      min_lines: 30
  key_links:
    - from: "firestarter/datasheets/datasheets-check.sh"
      to: "datasheets/<bucket>/*.pdf"
      via: "find + head -c4 %PDF magic-byte assertion"
      pattern: "%PDF"
---

<objective>
Establish the Phase 85 working tree and author the Nyquist Wave-0 validation artifact BEFORE any PDF is downloaded. Two outcomes: (1) the firestarter sub-repo is on the correct milestone branch so all subsequent `datasheets/` commits land in the right place; (2) `datasheets/datasheets-check.sh` exists and runs, giving every later task a single re-runnable structural gate.

Purpose: The check script is the per-task / per-wave / phase-gate verification for ALL four phase requirements (DSHEET-01/02/03 + SAFE-05). It must exist first so download and README tasks can self-verify. Authoring it does NOT violate SAFE-05 because the script itself is a `datasheets/` artifact (RESEARCH §"Validation Architecture").

Output: `firestarter/datasheets/datasheets-check.sh` (executable), the `datasheets/` directory committed to the `firestarter` sub-repo on branch `v1.16-protocol-first-architecture-rebuild`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-datasheet-acquisition/85-RESEARCH.md
@.planning/phases/85-datasheet-acquisition/85-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork the v1.16 milestone branch in the firestarter sub-repo</name>
  <files>firestarter/ (git branch only — no tracked file edits)</files>
  <read_first>
    - .planning/STATE.md (§"v1.16 Scope Notes" — firmware-only, branch off beta)
    - .planning/ROADMAP.md (§"v1.16" — "Branch: v1.16-protocol-first-architecture-rebuild off beta in firestarter sub-repo")
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"Project Constraints" — datasheets/ lands in the firestarter sub-repo, executors commit INSIDE the submodule)
  </read_first>
  <action>
    Work INSIDE the firestarter sub-repo (`cd firestarter`, not the meta-repo). The sub-repo is currently on `v1.15-bench-validation-of-operator-inventory`. Per the v1.16 branch decision, the milestone branch forks off `beta`. Create and check out `v1.16-protocol-first-architecture-rebuild` from `beta` (fetch `origin/beta` first if `beta` is not present locally). Per memory `project_v115_milestone_branch`, the v1.14 beta cut was deferred — if `beta` lacks code Phase 85 needs, that is irrelevant here because Phase 85 adds only documentation/asset files and touches no firmware source. Do NOT branch in the meta-repo for this — the meta-repo tracks only `.planning/`; the `datasheets/` tree belongs to the firestarter sub-repo. Confirm the active branch name after checkout.
  </action>
  <verify>
    <automated>cd firestarter && test "$(git branch --show-current)" = "v1.16-protocol-first-architecture-rebuild" && echo BRANCH-OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && git branch --show-current` prints exactly `v1.16-protocol-first-architecture-rebuild`
    - The new branch was forked from `beta` (not from the v1.15 branch tip)
    - No meta-repo branch was created for this work
  </acceptance_criteria>
  <done>The firestarter sub-repo is on branch v1.16-protocol-first-architecture-rebuild, forked off beta.</done>
</task>

<task type="auto">
  <name>Task 2: Author datasheets/datasheets-check.sh and the datasheets/ tree root</name>
  <files>firestarter/datasheets/datasheets-check.sh, firestarter/datasheets/.gitkeep</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"Reference check script" — the reference bash implementation to adapt; §"Folder / Naming Map" — the 12 expected_buckets values; §"Validation Architecture" — the four requirements the script covers)
    - .planning/phases/85-datasheet-acquisition/85-VALIDATION.md (§"Wave 0 Requirements" — exact coverage contract)
  </read_first>
  <action>
    Create `firestarter/datasheets/datasheets-check.sh` as a `set -euo pipefail` bash script adapted from the RESEARCH §"Reference check script" reference implementation. It MUST: (1) assert `datasheets/README.md` exists; (2) iterate the 12 expected bucket folder names verbatim from RESEARCH — `0x05-FLASH-AMD-STD 0x06-FLASH-AMD-ALT 0x07-EPROM-STD 0x08-EPROM-QUICK 0x0B-EPROM-LEGACY 0x0D-EEPROM-POLL 0x0E-SRAM-32PIN 0x10-FLASH-INTEL 0x27-SRAM-24PIN 0x28-SRAM-STD 0x29-SRAM-512K-1M 0x34-EEPROM-X88C64` — asserting each is a directory holding ≥1 `*.pdf` larger than 1k whose first 4 bytes are `%PDF`; (3) assert NO folder exists for the forbidden buckets `0x35 0x39 0x11 0x2A 0x2B 0x2C` AND that README mentions each forbidden hex as an exclusion; (4) warn (not fail) for any `*.pdf` filename referenced in README that maps to no real file; (5) print `datasheets-check: PASS` and exit 0 on success, or `datasheets-check: FAIL` and exit 1 on any failure. The script is designed to be run from the firestarter repo root (`DS=datasheets`). Make it executable (`chmod +x`). Because the bucket folders and README do not exist yet, the script is EXPECTED to FAIL at this stage — that is the correct RED state proving the gate works. Also create `firestarter/datasheets/.gitkeep` so the empty `datasheets/` directory is committable before the bucket folders land. Do NOT create the bucket folders or download any PDF in this plan — that is Plan 02.
  </action>
  <verify>
    <automated>cd firestarter && bash datasheets/datasheets-check.sh; rc=$?; test -x datasheets/datasheets-check.sh && test "$rc" -ne 0 && echo "SCRIPT-RUNS-AND-FAILS-AS-EXPECTED (rc=$rc)"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/datasheets/datasheets-check.sh` exists, is executable, and is ≥30 lines
    - Running it from `firestarter/` executes without a bash syntax error and exits non-zero (RED — folders/README not yet present)
    - The script source lists all 12 expected bucket names and all 6 forbidden hex IDs (`grep -c` finds each)
    - The script source contains the literal success string `datasheets-check: PASS`
  </acceptance_criteria>
  <done>datasheets/datasheets-check.sh runs (currently FAIL/RED because no PDFs exist yet) and enforces the 12-bucket + forbidden-exclusion + %PDF-magic contract.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the Wave-0 scaffold to the firestarter sub-repo</name>
  <files>firestarter/datasheets/datasheets-check.sh, firestarter/datasheets/.gitkeep</files>
  <read_first>
    - .planning/phases/85-datasheet-acquisition/85-RESEARCH.md (§"Project Constraints" — commit INSIDE the firestarter submodule on the v1.16 branch)
  </read_first>
  <action>
    Commit ONLY the `datasheets/` additions inside the firestarter sub-repo on the v1.16 branch. Stage exactly `datasheets/datasheets-check.sh` and `datasheets/.gitkeep` (pass explicit file paths — do NOT `git add -A`, since the sub-repo working tree contains untracked `core.*` crash dumps and `.pio/` build artifacts that must stay out of the commit). Verify with `git diff --cached --name-only` that every staged path begins with `datasheets/` (this is the SAFE-05 invariant). Commit message: `docs(85): wave-0 datasheets validation scaffold`.
  </action>
  <verify>
    <automated>cd firestarter && git diff --cached --name-only 2>/dev/null | grep -v '^datasheets/' | grep -q . && echo "SAFE-05-VIOLATION" || echo "SAFE-05-OK (only datasheets/ staged)"</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && git log -1 --name-only` shows only paths under `datasheets/`
    - No `core.*`, `.pio/`, or source-tree files are staged or committed
    - The commit lives on branch `v1.16-protocol-first-architecture-rebuild`
  </acceptance_criteria>
  <done>The Wave-0 scaffold (check script + .gitkeep) is committed inside the firestarter sub-repo, touching only datasheets/ (SAFE-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external host → repo file | A downloaded artifact (Plan 02) could be HTML/malware masquerading as a PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-SC1 | Tampering / Spoofing | downloaded `.pdf` artifacts (Plan 02) | mitigate | `datasheets-check.sh` asserts first 4 bytes are `%PDF` and size >1k for every committed PDF; prefer manufacturer/bitsavers/archive.org primaries (RESEARCH URL table) over aggregators |
| T-85-NA | N/A | rest of phase | accept | Static-asset phase: zero executable product surface, zero input parsing, zero product network surface, no secrets/PII. No further STRIDE threats introduced. |
</threat_model>

<verification>
- `cd firestarter && git branch --show-current` → `v1.16-protocol-first-architecture-rebuild`
- `cd firestarter && bash datasheets/datasheets-check.sh` runs without syntax error (expected RED at this stage)
- `cd firestarter && git diff --cached --name-only` (post-stage) shows only `datasheets/` paths
</verification>

<success_criteria>
- firestarter sub-repo on the v1.16 branch
- `datasheets/datasheets-check.sh` authored, executable, runs, enforces the 12-bucket + forbidden-exclusion + %PDF contract
- Scaffold committed with SAFE-05 invariant verified (only `datasheets/` staged)
</success_criteria>

<output>
Create `.planning/phases/85-datasheet-acquisition/85-01-SUMMARY.md` when done
</output>
