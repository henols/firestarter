---
phase: 90-per-protocol-bench-validation-ledger
plan: 04
type: execute
wave: 3
depends_on:
  - 90-01
  - 90-02
files_modified:
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.json
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.md
  - .planning/v1.16/ledger/bench/BENCH-LOG.md
autonomous: false
requirements:
  - LEDGER-02
user_setup: []

must_haves:
  truths:
    - "The recomposed firmware (submodule a296195) is flashed to the Leonardo + Rev 2.0 oracle"
    - "Each on-hand chip earns a PASS from BOTH a read-SHA regression (N>=3) AND a write-cycle A->B regression"
    - "Each PASS read/write SHA is byte-identical to the v1.15 baseline (D-01)"
    - "Any SHA mismatch is recorded FAIL-INVESTIGATE, never auto-passed (D-03)"
    - "Every PASS row carries oracle leonardo+Rev2.0 and non-empty p90 evidence references (D-09)"
    - "The read regression runs BEFORE the write-cycle per chip (D-01 sequencing)"
  artifacts:
    - path: ".planning/v1.16/ledger/bench/BENCH-LOG.md"
      provides: "Per-task port identity / Rev confirm / command / RC / SHA / SHA-match verdict / operator-authorize note"
      min_lines: 30
    - path: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      provides: "On-hand rows flipped to PASS with non-empty evidence references"
      contains: "leonardo+Rev2.0"
  key_links:
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      to: ".planning/v1.16/ledger/bench/"
      via: "evidence.p90_artifacts paths (D-09 non-empty refs)"
      pattern: "p90_artifacts"
    - from: "bench read/write SHA"
      to: "v1.15 EVIDENCE baseline SHA"
      via: "byte-identical regression match"
      pattern: "matches_v115"
---

<objective>
Bench-prove the 4 on-hand protocols on the recomposed firmware (submodule a296195), flip their
ledger rows from bench-pending to PASS with non-empty evidence references, and record the bench
session in BENCH-LOG.md. This is the load-bearing hardware regression: the recompose changed the
WRITE-path primitives (P3 VPP gate / P4 chip-id / P5 poll-readback), so each chip earns its PASS
from BOTH a non-destructive read-SHA (N>=3) AND a write-cycle A->B (auto-erase) regression, each
asserted byte-identical to the v1.15 baseline (D-01/D-02). Any mismatch is FAIL-INVESTIGATE, never
auto-passed (D-03). Operator gates every live silicon op (D-05); gitlinks stay PINNED at b10 (D-06).

Purpose: LEDGER-02 — each on-hand protocol bench-validated on Leonardo + Rev 2.0; a PASS row
structurally requires oracle leonardo+Rev2.0 + non-empty evidence references (D-09).

Output: PROTOCOL-LEDGER.{json,md} with the 4 on-hand rows PASS + evidence refs; bench/BENCH-LOG.md.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-CONTEXT.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-PATTERNS.md
@.planning/v1.16/ledger/PROTOCOL-LEDGER.json
@.planning/v1.16/ledger/tools/check_ledger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flash the recomposed firmware + generate + sanity-check the A/B images</name>
  <files>.planning/v1.16/ledger/bench/BENCH-LOG.md</files>
  <read_first>
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"Step 0 — Flash the recomposed firmware" lines 192-199 + the version-string caveat; §"Bench Harness + Exact Commands" lines 188-233; the chip sizes line 227 + baseline image-A/B SHAs lines 123-128)
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-PATTERNS.md (§image-generation pattern lines 169-178 — gen_test_image.py seed 1=A / seed 2=B)
    - firestarter_app/tools/gen_test_image.py (the deterministic generator; CLI `<size_bytes> <seed> <out>` prints SHA-256)
    - firestarter/CLAUDE.md (pio build/flash for leonardo; 250000 baud, 1024 B buffer)
  </read_first>
  <action>
    Build + flash (operator-authorizes the flash — Leonardo is EXEMPT from chip-out-before-sideload per D-06, but operator still confirms):
    1. `git -C firestarter rev-parse HEAD` — confirm == a296195 (the recompose build under test; record the hash, the version string still reads 3.0.0b10).
    2. `cd firestarter && pio run -e leonardo` — expect Flash ~87.7% (25136 B / 28672 B); record the actual.
    3. `pio run -t upload -e leonardo` — flash (operator authorizes).
    Generate + SANITY-CHECK the deterministic images for the 4 chips BEFORE touching any silicon (the printed SHA must equal the v1.15 baseline image-A/B SHA — this validates the generator is byte-stable on this host before any write). Sizes: W29C020=262144, SST39SF040=524288, W27C512=65536, FM1608=8192. For each chip: `python tools/gen_test_image.py <SIZE> 1 /tmp/firestarter_bench_p90/<chip>_img_A.bin` then `... 2 ..._img_B.bin`; compare the printed SHA to the baseline image-A/B SHA in RESEARCH lines 123-128. Any mismatch HERE is a generator/host problem, stop before any silicon op.
    Start BENCH-LOG.md with a session header: firmware HEAD a296195, pio flash result, and per-chip image-SHA sanity confirmation. Create the `.planning/v1.16/ledger/bench/` tree.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(git -C firestarter rev-parse HEAD | cut -c1-7)" = "a296195" && test -d .planning/v1.16/ledger/bench && echo FW-IDENTITY-AND-BENCH-TREE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter rev-parse HEAD` reports a296195 (firmware-under-test identity).
    - `pio run -e leonardo` reports a flash figure recorded in BENCH-LOG.md (expected ~25136 B / 87.7%).
    - For all 4 chips, `gen_test_image.py` seed-1 / seed-2 printed SHAs equal the v1.15 baseline image-A/B SHAs (recorded in BENCH-LOG.md before any silicon op).
    - BENCH-LOG.md exists with the session header (firmware HEAD + flash result + image-SHA sanity).
  </acceptance_criteria>
  <done>Recomposed firmware a296195 flashed to the Leonardo; A/B images generated and SHA-sanity-checked against the v1.15 baseline; BENCH-LOG.md session header recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bench regression of the 4 on-hand chips (read N>=3 then write-cycle A->B)</name>
  <files>.planning/v1.16/ledger/bench/BENCH-LOG.md, .planning/v1.16/ledger/bench/&lt;chip&gt;-{read,wcA,wcB}/</files>
  <read_first>
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"Step 1 — Read-SHA regression" lines 201-209; §"Step 2 — Write-cycle A->B" lines 211-233; the v1.15 baseline SHA table lines 123-128 — the byte-exact PASS oracles; the D-01 sequencing nuance line 130; Pitfall 1 FM1608/FRAM lines 368-372; Pitfalls 2/3 VPP + wrong-board instability lines 374-381)
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-PATTERNS.md (§bench evidence lines 165-199 — consistency-check + write-cycle capture + the FM1608 write -b exception; §BENCH-LOG.md per-task block lines 203-215)
    - firestarter_app/firestarter/cli_handlers.py (dev consistency-check ~1049/1102; dev write-cycle ~1139/1163 — the --runs / --output-dir signatures and 0/1/2 exit codes)
  </read_first>
  <what-built>
    Claude drives, over USB passthrough, the per-chip regression on the recomposed firmware. Per chip,
    the READ regression runs FIRST (the write overwrites the contents the read baseline measured — D-01
    sequencing), then the write-cycle A->B. Each captured SHA is compared byte-for-byte to the v1.15
    baseline (RESEARCH table lines 123-128). PASS = byte-identical; any mismatch = FAIL-INVESTIGATE (D-03).
  </what-built>
  <how-to-verify>
    PRECONDITIONS per chip (operator-gated, every task — ttyACM* shuffles on replug; the EEPROM hw byte
    cannot distinguish Rev 2.0/2.2/Rev0):
    1. Operator confirms the controller on the port is the Leonardo (`firestarter fw` / `controller:` identity) and reports the port.
    2. Operator confirms the RURP shield silkscreen reads Rev 2.0 (mandatory oracle — avoids the deferred v1.9 shield-fleet bug + the uno328pb/Rev-2.2 W27C512 instability).
    3. Operator seats ONE chip at a time and authorizes the live op.
    Then, per chip (CLI names: W29C020, SST39SF040, W27C512, FM1608 — confirm via `firestarter list | grep -i`):
    READ FIRST: `firestarter dev consistency-check <CHIP> --runs 3 --output-dir .planning/v1.16/ledger/bench/<chip>-read/` — expect verdict 0 (1 distinct SHA across N=3) AND that SHA == the v1.15 read baseline (W29C020 93ff5287…, SST39SF040 c19c3e07…, W27C512 9376dcd8…, FM1608 2ef1444b…). On any VPP hiccup, reset board + retry; never record N=1 (D-03).
    THEN WRITE-CYCLE A->B (the load-bearing op — exercises the recomposed write primitives):
      - Flash/EPROM chips (W29C020, SST39SF040, W27C512): `firestarter dev write-cycle <CHIP> <img_A.bin> --runs 1 --output-dir .../<chip>-wcA/` then `... <img_B.bin> ... --output-dir .../<chip>-wcB/` — B written over A with NO explicit erase; readback SHA must == the v1.15 write-cycle final SHA (W29C020 47304933…, SST39SF040 a38b13b4…, W27C512 e16b2a5b…).
      - FM1608 (FRAM) ONLY: `dev write-cycle` erase is "Not supported" — use the v1.15 method: `firestarter write FM1608 -b <img_A.bin>` then `write -b <img_B.bin>` then `firestarter dev consistency-check FM1608 --runs 3`; the N=3 SHA must == the v1.15 FM1608 write-cycle final SHA 3c23e7fc…. Record the benign "Empty input" blank-check note (FRAM has no blank concept).
    Negative control (carry the v1.15 pattern): a wrong-file `verify` should exit RC=1 — proves the verify is non-vacuous.
    Record EACH op in BENCH-LOG.md as a per-task block: port identity, Rev 2.0 silkscreen confirm, the exact command, RC, captured SHA, SHA-matches-v115 verdict (PASS / FAIL-INVESTIGATE), and the operator-authorize note. The output-dir per-run captures ARE the D-09 non-empty evidence references (commit at least the per-run SHAs + BENCH-LOG; the 512 KB SST39SF040 binary may be referenced by SHA+log rather than committed — confirm with operator).
    If ANY read or write SHA differs from the v1.15 baseline: record FAIL-INVESTIGATE in BENCH-LOG.md and set that row's verification_status to "FAIL-INVESTIGATE" (a recompose-regression alarm) — do NOT auto-pass, do NOT flip to PASS.
  </how-to-verify>
  <resume-signal>Type "approved" with the 8 SHA verdicts (4 read + 4 write-cycle), or describe any FAIL-INVESTIGATE mismatch.</resume-signal>
  <acceptance_criteria>
    - BENCH-LOG.md has a per-chip block for all 4 chips with port identity, Rev 2.0 confirm, command, RC, captured SHA, SHA-match verdict, and operator-authorize note.
    - Read regression ran BEFORE the write-cycle for each chip (BENCH-LOG ordering shows read-then-write).
    - For each chip the read SHA and the write-cycle final SHA are recorded and compared to the v1.15 baseline.
    - FM1608 used `write -b` (not dev write-cycle) for the A->B; the "Empty input" blank-check note is recorded as benign.
    - Any mismatch is recorded FAIL-INVESTIGATE (not auto-passed).
    - `.planning/v1.16/ledger/bench/<chip>-{read,wcB}/` (or SHA+log refs) exist as the D-09 evidence references.
  </acceptance_criteria>
  <done>All 4 chips have a recorded read-N>=3 + write-cycle A->B regression with a SHA-vs-baseline verdict and non-empty evidence references; mismatches (if any) recorded FAIL-INVESTIGATE.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the on-hand rows to PASS + run the Plan-01 checker (LEDGER-02 gate)</name>
  <files>.planning/v1.16/ledger/PROTOCOL-LEDGER.json, .planning/v1.16/ledger/PROTOCOL-LEDGER.md</files>
  <read_first>
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (the SHA-match verdicts from Task 2 — only verdicts that matched the v1.15 baseline become PASS)
    - .planning/v1.16/ledger/PROTOCOL-LEDGER.json (the bench-pending on-hand rows from Plan 02 — flip these)
    - .planning/v1.16/ledger/tools/check_ledger.py (the D-09 PASS constraint the flipped rows must satisfy)
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"JSON Schema Proposal" lines 285-293 — the PASS-row evidence shape with p90_artifacts + p90_*_sha_matches_v115)
  </read_first>
  <action>
    For each of the 4 on-hand buckets {0x05,0x06,0x07,0x28}: if Task 2's BENCH-LOG verdict was a byte-identical match to the v1.15 baseline for BOTH the read AND the write-cycle, set that row's verification_status to "PASS", set evidence.p90_artifacts to the non-empty list of stored bench artifact paths (e.g. `.planning/v1.16/ledger/bench/<chip>-read/` and `.../<chip>-wcB/` or the BENCH-LOG anchor), and set evidence.p90_read_sha_matches_v115 true + evidence.p90_writecycle_sha_matches_v115 true. The oracle field stays "leonardo+Rev2.0" (D-09). If EITHER op mismatched, set verification_status "FAIL-INVESTIGATE" and the corresponding p90_*_matches_v115 false — do NOT flip to PASS (D-03).
    CRITICAL D-04: still reference the v1.15 cells by KEY only — do NOT copy any baseline SHA into the ledger (the checker no-copy regex will block it). The p90_*_sha_matches_v115 booleans encode the regression verdict; the actual SHA lives only in BENCH-LOG.md and the captured per-run files.
    Re-render the matching rows in PROTOCOL-LEDGER.md (lockstep with the JSON — verification status + evidence refs columns).
    Run the Plan-01 checker against the live upstream files: now that PASS rows exist, the D-09 structural constraint is active. The checker must exit 0 (every PASS row has oracle + non-empty p90_artifacts + both sha-match flags true).
  </action>
  <verify>
    <automated>cd /workspaces && python .planning/v1.16/ledger/tools/check_ledger.py; rc=$?; ! grep -qE "[0-9a-f]{64}" .planning/v1.16/ledger/PROTOCOL-LEDGER.json && test $rc -eq 0 && echo LEDGER-02-CHECKER-PASS</automated>
  </verify>
  <acceptance_criteria>
    - `python .planning/v1.16/ledger/tools/check_ledger.py` exits 0 with PASS rows present (D-09 constraint active and satisfied).
    - Every PASS row in PROTOCOL-LEDGER.json has oracle "leonardo+Rev2.0", non-empty evidence.p90_artifacts, and both p90_*_sha_matches_v115 true.
    - No 64-hex SHA copied into the ledger (D-04 no-copy guard still holds).
    - PROTOCOL-LEDGER.md reflects the flipped statuses + evidence refs (lockstep with JSON).
    - Any FAIL-INVESTIGATE bucket is NOT marked PASS.
  </acceptance_criteria>
  <done>The matched on-hand rows are PASS with non-empty evidence references; the checker exits 0 with the D-09 PASS constraint active — LEDGER-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI → serial (250000 baud) → recomposed firmware → chip in socket | The live silicon boundary; over-voltage and wrong-chip hazards cross here. Operator gates every live op (D-05). |
| bench SHA → v1.15 baseline | The regression-match boundary: byte-identical = behavior-preserving recompose; mismatch = regression alarm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-11 | Tampering (physical) | Over-voltage during a bench write (hardware damage) | mitigate | The firmware vpp_check_window +500 mV gate (verified present in Plan 03) blocks over-voltage; operator gates each live op; Leonardo+Rev2.0 oracle avoids the unstable shield/board paths (Pitfalls 2/3). |
| T-90-12 | Tampering (physical) | Driving a non-supported chip on the wrong board/shield → false-FAIL or damage | mitigate | Operator confirms controller identity + Rev 2.0 silkscreen per task (ttyACM* shuffles; EEPROM hw byte cannot distinguish revs); host resolve_chip guard refuses non-supported chips. |
| T-90-13 | Tampering (process) | A recompose regression silently flipped to PASS | mitigate | D-01 byte-identical regression-match + D-03 never-auto-pass; the checker D-09 constraint blocks a PASS row without non-empty evidence + sha-match flags. |
| T-90-14 | Tampering / drift | Baseline SHA copied into the ledger | mitigate | p90_*_sha_matches_v115 booleans encode the verdict; the checker no-copy regex blocks any 64-hex string (D-04). |
| T-90-15 | Loss of asset | Spending an irreplaceable part on an unstable read path | mitigate | Only the 4 rewritable on-hand chips are bench-cycled; 2516 stays UNVERIFIED (SAFE-04); reset+retry on VPP hiccup, never record N=1. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs (harness reused; SAFE-05). Gitlinks stay PINNED at b10; no beta cut (D-06). |

> The dominant milestone hazard — physical over-voltage during a bench write — is mitigated by the
> firmware VPP +500 mV gate (SAFE-04, verified present in Plan 03) plus the host resolve_chip guard,
> and is exercised only under per-op operator authorization on the pinned Leonardo + Rev 2.0 oracle.
</threat_model>

<verification>
- Firmware HEAD a296195 flashed to Leonardo + Rev 2.0 (operator-confirmed silkscreen + port identity).
- Each on-hand chip: read N>=3 SHA + write-cycle A->B final SHA recorded vs the v1.15 baseline.
- Read runs before write-cycle per chip (D-01 sequencing).
- FM1608 via write -b (FRAM exception); benign "Empty input" note recorded.
- Mismatches recorded FAIL-INVESTIGATE, never auto-passed.
- PASS rows carry oracle + non-empty evidence refs + both sha-match flags; checker exits 0.
- No SHA copied into the ledger (D-04).
</verification>

<success_criteria>
The 4 on-hand protocols are bench-validated on the recomposed firmware (a296195) on Leonardo +
Rev 2.0; each PASS row earns its status from a byte-identical read + write-cycle regression to the
v1.15 baseline, carries oracle leonardo+Rev2.0 + non-empty evidence references, and passes the
Plan-01 checker's D-09 constraint — LEDGER-02 satisfied. Gitlinks remain PINNED at b10; no beta cut.
</success_criteria>

<output>
Create `.planning/phases/90-per-protocol-bench-validation-ledger/90-04-SUMMARY.md` when done.
</output>
