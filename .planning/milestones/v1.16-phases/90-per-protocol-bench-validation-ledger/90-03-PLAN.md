---
phase: 90-per-protocol-bench-validation-ledger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.16/ledger/SAFE-04-VERIFICATION.md
autonomous: true
requirements:
  - SAFE-04
user_setup: []

must_haves:
  truths:
    - "The firmware over-voltage VPP check is present and unmodified at its post-recompose location"
    - "The host resolve_chip support-status guard is present and unmodified"
    - "Both sub-repos show a clean working tree (no source change this phase, D-10)"
    - "2516 retains verification_status UNVERIFIED in the chip database"
    - "The frozen-world gates (native suite + dispatch + DB diff) rerun green"
  artifacts:
    - path: ".planning/v1.16/ledger/SAFE-04-VERIFICATION.md"
      provides: "Recorded SAFE-04 verify-present-only evidence + frozen-world rerun results"
      min_lines: 25
  key_links:
    - from: ".planning/v1.16/ledger/SAFE-04-VERIFICATION.md"
      to: "firestarter/src/proms/primitives.cpp"
      via: "grep of the +500 mV over-voltage gate in vpp_check_window"
      pattern: "vpp_check_window"
    - from: ".planning/v1.16/ledger/SAFE-04-VERIFICATION.md"
      to: "firestarter_app/firestarter/chip_resolver.py"
      via: "grep of the support_status guard in resolve_chip"
      pattern: "support_status"
---

<objective>
Verify the SAFE-04 safety guard chain is PRESENT and UNMODIFIED on the firmware-under-test
(submodule a296195) and record the evidence in `.planning/v1.16/ledger/SAFE-04-VERIFICATION.md`.
This is a verify-present-only plan (D-08/D-10 frozen world): NO source edits. It confirms the
over-voltage VPP check at its POST-recompose location (`primitives.cpp` `vpp_check_window`, NOT
the stale CONTEXT line numbers), the host `resolve_chip` guard, the 2516 UNVERIFIED status, and
reruns the frozen-world gates to prove nothing drifted.

Purpose: SAFE-04 — over-voltage stays blocked at the firmware VPP check; the host guard is never
bypassed; no irreplaceable UV part is written on an unstable read path (2516 stays UNVERIFIED).

Output: SAFE-04-VERIFICATION.md with the grep hits, git-clean confirmations, and gate results.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-CONTEXT.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the SAFE-04 guard chain present + unmodified (grep + git-clean)</name>
  <files>.planning/v1.16/ledger/SAFE-04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"SAFE-04 Verification Targets" lines 396-406 — the corrected POST-recompose locations + the exact grep commands; Pitfall 5 lines 388-390 explaining the CONTEXT line numbers are stale)
    - firestarter/src/proms/primitives.cpp (the over-voltage `+500` mV gate inside vpp_check_window — confirm present)
    - firestarter_app/firestarter/chip_resolver.py (the support_status guard in resolve_chip — confirm present)
    - firestarter_app/firestarter/data/chip_database.json (2516 record — confirm verification_status UNVERIFIED)
  </read_first>
  <action>
    Run the verify-present-only commands (no edits — D-10):
    1. Confirm firmware identity: `git -C firestarter rev-parse HEAD` must report a296195 (the recompose build under test). Record the hash, NOT the version string (which still reads 3.0.0b10).
    2. Over-voltage HIGH check: `grep -n "vpp_mv > (uint32_t)handle->vpp_mv + 500" firestarter/src/proms/primitives.cpp` — confirm the +500 mV threshold (FORCE→WARN / else ERROR) is present inside vpp_check_window (the P3-extracted location). Note it is called by eprom_check_vpp (eprom.cpp) and flash_intel_check_vpp (flash_intel.cpp).
    3. Host guard: `grep -n "support_status" firestarter_app/firestarter/chip_resolver.py` — confirm the resolve_chip support-status guard (refuses non-`supported` chips before any wire dict / serial byte) is present (~line 55).
    4. 2516 status: grep the chip_database.json 2516 record for `verification_status` = UNVERIFIED (no write-graduation this phase, SAFE-04).
    5. Frozen-world clean trees (D-10 — no source change): `git -C firestarter diff --quiet && echo FW-CLEAN` and `git -C firestarter_app diff --quiet && echo HOST-CLEAN`.
    Author SAFE-04-VERIFICATION.md recording: firmware HEAD a296195; each grep hit (file:line + the matched line); the two git-clean results; the 2516 UNVERIFIED confirmation; and a one-line note that these guards moved into shared primitives in P3 but are behavior-identical (threshold + semantics unchanged). Cross-reference the SAFE-04 row this satisfies and link the primitives.cpp / chip_resolver.py targets.
  </action>
  <verify>
    <automated>cd /workspaces && grep -q "vpp_mv > (uint32_t)handle->vpp_mv + 500" firestarter/src/proms/primitives.cpp && grep -q "support_status" firestarter_app/firestarter/chip_resolver.py && git -C firestarter diff --quiet && git -C firestarter_app diff --quiet && echo SAFE-04-GUARDS-PRESENT-AND-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep "vpp_mv > (uint32_t)handle->vpp_mv + 500" firestarter/src/proms/primitives.cpp` matches (over-voltage gate present at post-recompose location).
    - `grep support_status firestarter_app/firestarter/chip_resolver.py` matches (host guard present).
    - `git -C firestarter diff --quiet` and `git -C firestarter_app diff --quiet` both exit 0 (no source change — D-10).
    - The 2516 record in chip_database.json shows `verification_status` UNVERIFIED.
    - SAFE-04-VERIFICATION.md records the firmware HEAD a296195, both grep hits, both git-clean results, and the 2516 status.
  </acceptance_criteria>
  <done>SAFE-04 guard chain is documented present + unmodified at the post-recompose locations; both sub-repos clean; 2516 UNVERIFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Rerun the frozen-world gates (confirm no drift)</name>
  <files>.planning/v1.16/ledger/SAFE-04-VERIFICATION.md</files>
  <read_first>
    - .planning/v1.16/ledger/SAFE-04-VERIFICATION.md (the file from Task 1 — append the gate results)
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"Re-confirm frozen world" lines 431-438 — the rerun-only command set; §"Frozen-world rerun-to-confirm gates" in PATTERNS lines 232-240)
    - firestarter_app/CLAUDE.md ("Tooling gate" — validate host tooling against CI py3.11, NOT devcontainer py3.12)
  </read_first>
  <action>
    Rerun the frozen-world gates (rerun-to-confirm only — change nothing):
    1. `cd firestarter && pio test -e native` — expect the native suite green (was 105/105 at Phase 89 close; record the actual count). If `pio` is unavailable in this session, record the Phase-89-close result (102→105/105) as the carried baseline and note pio absence (per RESEARCH Environment Availability fallback).
    2. `cd firestarter_app && python tools/check_dispatch.py` — expect exit 0, 0 violations.
    3. `python tools/diff_db.py` — expect exit 0, identity diff (0 changed).
    For host tooling, run under the CI py3.11 target where available, not only devcontainer py3.12 (CLAUDE.md). Since this phase changes NO host source, CI risk is minimal — but record which interpreter was used.
    Append the three gate results (command + exit code + summary line) to SAFE-04-VERIFICATION.md, with the conclusion that the frozen world stands (D-10) and the SAFE-04 guards are intact on the firmware-under-test.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python tools/check_dispatch.py >/dev/null 2>&1; cd1=$?; python tools/diff_db.py >/dev/null 2>&1; cd2=$?; test $cd1 -eq 0 && test $cd2 -eq 0 && echo FROZEN-WORLD-GATES-GREEN || echo "check_dispatch=$cd1 diff_db=$cd2"</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/check_dispatch.py` exits 0 (0 violations).
    - `python tools/diff_db.py` exits 0 (identity diff).
    - `pio test -e native` is green (record the count) OR the Phase-89-close carried baseline + pio-absence note is recorded with reason.
    - SAFE-04-VERIFICATION.md records all three gate results + the interpreter used + the frozen-world conclusion.
  </acceptance_criteria>
  <done>The frozen-world gates rerun green (or carried with documented pio absence); SAFE-04-VERIFICATION.md concludes the frozen world stands and the SAFE-04 guards are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI → serial → firmware → chip socket | The SAFE-04 guard chain (host resolve_chip refusal + firmware VPP +500 mV gate) sits across this boundary; this plan verifies both guards present + unmodified, it does not drive silicon. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-07 | Tampering (physical) | Over-voltage on a 5V part (hardware damage) | mitigate | Verify the firmware `vpp_check_window` +500 mV over-voltage gate is present + unmodified at primitives.cpp (post-recompose); SAFE-04. |
| T-90-08 | Tampering (physical) | Driving a non-supported chip (12V on wrong pin) | mitigate | Verify the host resolve_chip support-status guard is present + unmodified (fires before any wire dict / serial byte); SAFE-04. |
| T-90-09 | Loss of asset | Spending the irreplaceable 2516 on an unstable read path | mitigate | Confirm 2516 stays verification_status UNVERIFIED in chip_database.json — not write-graduated this phase (SAFE-04 / D-08). |
| T-90-10 | Tampering (process) | A silent source drift breaking a guard | mitigate | `git diff --quiet` on both sub-repos + the frozen-world gate trio (native suite / check_dispatch / diff_db) confirm no drift (D-10). |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs (verify-present-only; SAFE-05). |

> This plan performs no edits and drives no silicon — it is the SAFE-04 verify-present-only
> evidence. The live over-voltage behavior is exercised under operator gating in Plan 04.
</threat_model>

<verification>
- `grep` confirms the firmware over-voltage gate (primitives.cpp) + host guard (chip_resolver.py).
- `git diff --quiet` clean on both sub-repos (D-10 frozen world).
- 2516 verification_status UNVERIFIED.
- `check_dispatch.py` + `diff_db.py` exit 0; native suite green (or carried with pio-absence note).
</verification>

<success_criteria>
SAFE-04-VERIFICATION.md records the over-voltage VPP check and host resolve_chip guard present +
unmodified at their post-recompose locations on firmware a296195, both sub-repos clean, 2516
UNVERIFIED, and the frozen-world gates green — SAFE-04 satisfied with no source change.
</success_criteria>

<output>
Create `.planning/phases/90-per-protocol-bench-validation-ledger/90-03-SUMMARY.md` when done.
</output>
