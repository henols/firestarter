---
phase: 90-per-protocol-bench-validation-ledger
plan: 02
type: execute
wave: 2
depends_on:
  - 90-01
files_modified:
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.json
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.md
autonomous: true
requirements:
  - LEDGER-01
  - LEDGER-03
user_setup: []

must_haves:
  truths:
    - "PROTOCOL-LEDGER.json contains exactly one row for each of the 12 protocol buckets"
    - "Each row carries proposed name, datasheet citation, primitives used, matrix_family key, and verification status"
    - "The 6 no-silicon buckets (0x0D,0x0E,0x10,0x27,0x29,0x34) carry UNVERIFIED with a datasheet-representative chip"
    - "The 3 open-defect rows (CR-01/W29C040, FUT-06/AM27C020, FUT-03/2516) reproduce documented status verbatim with status_changed=false"
    - "The ledger composes by cross-reference only — it copies no SHA/verdict data from the upstream files"
    - "PROTOCOL-LEDGER.md is the human-readable view of the same rows"
  artifacts:
    - path: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      provides: "Machine-readable 12-bucket cross-reference ledger"
      contains: "matrix_family"
    - path: ".planning/v1.16/ledger/PROTOCOL-LEDGER.md"
      provides: "Human-readable ledger table"
      min_lines: 30
  key_links:
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      to: "firestarter_app/tools/validation_matrix_spec.json"
      via: "matrix_family join key"
      pattern: "\"matrix_family\""
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      to: ".planning/v1.15/bench/EVIDENCE.json"
      via: "evidence_chip join key"
      pattern: "evidence_chip"
---

<objective>
Author the cross-reference ledger — `.planning/v1.16/ledger/PROTOCOL-LEDGER.{json,md}` — with one
row per all 12 protocol buckets. This plan lands every row's static content: proposed name,
handler, datasheet citation, primitives-used, matrix_family / evidence join keys, the milestone
flash-impact note, the 6 full UNVERIFIED rows, and the 3 open-defect carries (verbatim). The four
on-hand bench rows (0x05/0x06/0x07/0x28) are authored in their pre-bench state
(`verification_status: "bench-pending"`, empty `p90_artifacts`) — Plan 04 flips them to PASS after
the silicon regression. This split keeps the doc work independent of the operator-gated bench.

Purpose: LEDGER-01 (per-protocol rows that compose with, not replace, the v1.13 matrix + v1.15
EVIDENCE by key) and LEDGER-03 (6 explicit UNVERIFIED + 3 verbatim defect carries).

Output: PROTOCOL-LEDGER.json + PROTOCOL-LEDGER.md, both passing the Plan-01 checker.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-CONTEXT.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md
@.planning/phases/90-per-protocol-bench-validation-ledger/90-PATTERNS.md
@.planning/v1.16/ledger/tools/check_ledger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author PROTOCOL-LEDGER.json (all 12 bucket rows + open defects)</name>
  <files>.planning/v1.16/ledger/PROTOCOL-LEDGER.json</files>
  <read_first>
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-RESEARCH.md (§"12-Bucket Row Identities" lines 132-151 for handler/file/datasheet-slug/rep-chip per bucket; §"Primitives Used Per Bucket" lines 162-186 for the primitives column; §"Open-Defect Rows" lines 153-161 for the 3 verbatim dispositions; §"JSON Schema Proposal" lines 259-309 for the authoritative shape; §"v1.15 Baseline SHAs" lines 119-130 — READ ONLY to know which chip maps where; do NOT copy any SHA into the ledger)
    - firestarter_app/tools/validation_matrix_spec.json (confirm the exact family ids: eprom[7,8,11], eeprom28c[13], flash3[6], flash4[5], flash_intel[16], sram[14,39,40,41]; 0x34 has NO family → matrix_family null)
    - .planning/v1.15/bench/EVIDENCE.json (confirm cells[].chip spellings W29C020/SST39SF040/W27C512/FM1608 and the op strings "read+blank_check" and "write_A+verify_A → write_B+verify_B" for the evidence join keys)
    - .planning/STATE.md (Deferred Items table — the verbatim CR-01 / FUT-06 / FUT-03 dispositions + source links for the open_defects array)
    - .planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md (the per-primitive flash deltas for milestone_flash_impact: P7 0 / P4 -164 / P3 -402 / P5 +2 / CR01_fix +46 = -518 net; baseline 25654; final 25136 B / 87.7%; firmware HEAD a296195)
  </read_first>
  <action>
    Write PROTOCOL-LEDGER.json per the RESEARCH §"JSON Schema Proposal" shape (field names are D-04 discretion within compose-by-cross-reference). Top-level keys: schema_version 1, generated "2026-06-26", milestone "v1.16", firmware_under_test {submodule_commit "a296195", flash_bytes 25136, flash_pct 87.7, version_string_caveat "reports 3.0.0b10; build is v1.16 recompose"}, oracle "leonardo+Rev2.0", composes_with {v1_13_matrix, v1_15_evidence paths}, milestone_flash_impact {net_delta_bytes -518, baseline_bytes 25654, per_primitive {P7 0, P4 -164, P3 -402, P5 2, CR01_fix 46}}.
    Author `rows[]` with EXACTLY 12 entries, one per bucket {0x05,0x06,0x07,0x08,0x0B,0x0D,0x0E,0x10,0x27,0x28,0x29,0x34}, each with: bucket, proposed_name, handler, handler_file, datasheet_slug, datasheet_citation, matrix_family (the family id, or null for 0x34), matrix_protocols_dec, primitives_used, verification_status, oracle, on_hand_chip (or null), evidence{} block.
    Per RESEARCH §12-Bucket: 0x05 FLASH-AMD-STD/configure_flash4()/flash_type_4.cpp/flash4/[5]/[P4,P5,P7]; 0x06 FLASH-AMD-ALT/configure_flash3()/flash_type_3.cpp/flash3/[6]/[P4,P7]; 0x07 EPROM-STD/configure_eprom()/eprom.cpp/eprom/[7]/[P4,P3]; 0x08 EPROM-QUICK/configure_eprom()/eprom.cpp/eprom/[8]/[P4,P3]; 0x0B EPROM-LEGACY/configure_eprom()/eprom.cpp/eprom/[11]/[P4,P3]; 0x0D EEPROM-POLL/configure_eeprom28c()/eeprom_28c.cpp/eeprom28c/[13]/[P4,P5,P7]; 0x0E SRAM-32PIN/configure_sram()/sram.cpp/sram/[14]/[]; 0x10 FLASH-INTEL/configure_flash_intel()/flash_intel.cpp/flash_intel/[16]/[P4,P3]; 0x27 SRAM-24PIN/configure_sram()/sram.cpp/sram/[39]/[]; 0x28 SRAM-STD/configure_sram()/sram.cpp/sram/[40]/[]; 0x29 SRAM-512K-1M/configure_sram()/sram.cpp/sram/[41]/[]; 0x34 EEPROM-X88C64/configure_not_implemented()/not_implemented.cpp/matrix_family null/[].
    Set verification_status per bucket: the four ON-HAND bench buckets {0x05,0x06,0x07,0x28} get `"bench-pending"` (NOT "PASS" — Plan 04 flips these post-bench), oracle "leonardo+Rev2.0", on_hand_chip set (W29C020/SST39SF040/W27C512/FM1608), evidence{} with v1_15_read_cell{evidence_chip, op "read+blank_check"} + v1_15_writecycle_cell{evidence_chip, op "write_A+verify_A → write_B+verify_B"} + p90_artifacts [] (empty until Plan 04) + p90_read_sha_matches_v115 null + p90_writecycle_sha_matches_v115 null. The six no-silicon buckets {0x0D,0x0E,0x10,0x27,0x29,0x34} get verification_status "UNVERIFIED", reason "no on-hand silicon", on_hand_chip null, and an UNVERIFIED rep-chip + datasheet citation per RESEARCH (0x0D AT28C256, 0x0E DS1245Y, 0x10 Intel-28F010, 0x27 6116, 0x29 DS1245Y, 0x34 X88C64). The 0x08 and 0x0B buckets get verification_status "open-defect-carried" (represented by FUT-06 / FUT-03 in open_defects; on_hand_chip null in the row, defect details live in open_defects per D-07).
    Author `open_defects[]` with the 3 carries (CR-01/W29C040/0x05, FUT-06/AM27C020/0x08, FUT-03/2516/0x0B), each {id, chip, bucket, disposition (VERBATIM from STATE.md Deferred Items — do not re-litigate), source_link (STATE.md anchor + EVIDENCE.json phase84 block), status_changed false}.
    CRITICAL D-04: reference upstream by KEY only — do NOT copy any sha256 / image-SHA / verdict string from EVIDENCE.json. The checker's no-copy regex will fail the build if any 64-hex string appears.
    Footnote (in a comment-free JSON field, e.g. a "notes" key) the FM1608 EVIDENCE "0x40" label as the retired decimal-40 ↔ hex-0x28 conflation.
  </action>
  <verify>
    <automated>cd /workspaces && python -m json.tool .planning/v1.16/ledger/PROTOCOL-LEDGER.json >/dev/null && python -c "import json; d=json.load(open('.planning/v1.16/ledger/PROTOCOL-LEDGER.json')); b={r['bucket'] for r in d['rows']}; assert b=={'0x05','0x06','0x07','0x08','0x0B','0x0D','0x0E','0x10','0x27','0x28','0x29','0x34'}, b; print('12-BUCKETS-OK')" && ! grep -qE "[0-9a-f]{64}" .planning/v1.16/ledger/PROTOCOL-LEDGER.json && echo NO-SHA-COPY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m json.tool PROTOCOL-LEDGER.json` succeeds (well-formed).
    - `rows[]` has exactly 12 entries; their `bucket` set equals the 12-bucket set.
    - `grep -E "[0-9a-f]{64}" PROTOCOL-LEDGER.json` finds NO match (D-04 no-copy guard).
    - The six buckets {0x0D,0x0E,0x10,0x27,0x29,0x34} carry `verification_status: "UNVERIFIED"` with a non-null datasheet_citation.
    - `open_defects[]` has 3 entries (CR-01, FUT-06, FUT-03), each with `status_changed: false`.
    - The four on-hand buckets carry `verification_status: "bench-pending"` (not yet PASS) and oracle "leonardo+Rev2.0".
  </acceptance_criteria>
  <done>PROTOCOL-LEDGER.json holds all 12 rows + 3 open-defect carries, references upstream by key only (no SHA copied), and is well-formed.</done>
</task>

<task type="auto">
  <name>Task 2: Render PROTOCOL-LEDGER.md + run the Plan-01 checker (LEDGER-01/03 gate)</name>
  <files>.planning/v1.16/ledger/PROTOCOL-LEDGER.md</files>
  <read_first>
    - .planning/v1.16/ledger/PROTOCOL-LEDGER.json (the rows authored in Task 1 — the .md mirrors these exactly)
    - .planning/v1.15/bench/EVIDENCE.md (the analog — header block + markdown table mirroring the JSON; lines 1-3 header, table at line 51+)
    - .planning/phases/90-per-protocol-bench-validation-ledger/90-PATTERNS.md (§PROTOCOL-LEDGER.md lines 92-105 — the JSON→markdown-table companion convention)
    - .planning/v1.16/ledger/tools/check_ledger.py (the gate this task must pass — run it)
  </read_first>
  <action>
    Write PROTOCOL-LEDGER.md as the human view of the SAME rows as the .json (keep them in lockstep, exactly like the EVIDENCE.json/.md pair). Header block: title, milestone v1.16, firmware-under-test = submodule a296195 (note version string reports 3.0.0b10 but the build is the v1.16 recompose), oracle leonardo+Rev2.0, generated 2026-06-26, the milestone flash-impact one-line note (-518 B net, 25654→25136 B / 87.7%). Then a markdown table, one row per the 12 buckets, columns: bucket | proposed name | handler (file) | matrix_family | primitives used | on-hand chip | verification status | evidence refs. Add a short "Open Defects (carried verbatim — no status change)" subsection rendering the 3 open_defects with their verbatim disposition + source link. Footnote the FM1608 0x40↔0x28 conflation.
    After both files exist, run the Plan-01 checker against the real ledger (against the live EVIDENCE.json + validation_matrix_spec.json). NOTE: at this stage the on-hand rows are "bench-pending" not "PASS", so the D-09 PASS constraint is vacuously satisfied (no PASS rows yet) — the checker must still exit 0 on LEDGER-01 (join keys resolve, no SHA copy, 12 buckets) and LEDGER-03 (6 UNVERIFIED, defect status_changed false). If the checker rejects "bench-pending" as an invalid enum value, ensure "bench-pending" is in the checker's accepted enum (coordinate via Plan 01's enum {PASS, UNVERIFIED, FAIL-INVESTIGATE, open-defect-carried, bench-pending}).
  </action>
  <verify>
    <automated>cd /workspaces && python .planning/v1.16/ledger/tools/check_ledger.py; rc=$?; test $rc -eq 0 && echo "CHECKER-PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `python .planning/v1.16/ledger/tools/check_ledger.py` exits 0 against the live upstream files.
    - PROTOCOL-LEDGER.md renders all 12 bucket rows + the 3 open-defect carries.
    - The .md and .json describe the same 12 buckets (same proposed names, same verification statuses).
    - The .md open-defects subsection reproduces the verbatim STATE.md dispositions (no status change).
  </acceptance_criteria>
  <done>PROTOCOL-LEDGER.md mirrors the JSON, and the Plan-01 checker exits 0 — LEDGER-01 and LEDGER-03 are structurally satisfied (PASS rows pending Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger JSON/MD ↔ upstream EVIDENCE.json / validation_matrix_spec.json | Cross-reference by key; the ledger is a consumer, the upstream files stay authoritative (D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04 | Tampering / drift | SHA or verdict data duplicated from EVIDENCE.json into the ledger | mitigate | Author keys only; the Plan-01 checker no-copy regex blocks any 64-hex string (verify step). |
| T-90-05 | Repudiation | An open defect silently downgraded/changed | mitigate | open_defects carry verbatim STATE.md dispositions + status_changed=false; checker asserts status_changed==false (LEDGER-03). |
| T-90-06 | Tampering (process) | A bucket row missing or duplicated | mitigate | Checker asserts exactly the 12-bucket set present once. |
| T-90-SC | Tampering | npm/pip/cargo installs | accept | No package installs (doc/JSON authoring only; SAFE-05). |

> Pure documentation/JSON authoring — no network or untrusted-input surface. The physical
> over-voltage hazard is verified in Plan 03 (SAFE-04) and exercised under operator gating in
> Plan 04 (bench).
</threat_model>

<verification>
- `python -m json.tool PROTOCOL-LEDGER.json` succeeds; 12 buckets present.
- No 64-hex SHA copied into the ledger (D-04).
- `python .planning/v1.16/ledger/tools/check_ledger.py` exits 0.
- PROTOCOL-LEDGER.md mirrors the JSON; open defects carried verbatim.
</verification>

<success_criteria>
PROTOCOL-LEDGER.{json,md} record all 12 protocol buckets composing by cross-reference (no SHA
copied) with the 6 no-silicon buckets explicit UNVERIFIED and the 3 open defects carried verbatim
— LEDGER-01 and LEDGER-03 satisfied. The four on-hand rows are staged bench-pending for Plan 04.
</success_criteria>

<output>
Create `.planning/phases/90-per-protocol-bench-validation-ledger/90-02-SUMMARY.md` when done.
</output>
