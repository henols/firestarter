---
phase: 87-naming-documentation-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/doc/PROTOCOLS.md
  - firestarter/.flash-baseline-87.txt
autonomous: true
requirements: [NAME-01, NAME-03, NAME-04, NAME-05, SAFE-06]
must_haves:
  truths:
    - "PROTOCOLS.md names every protocol_id present in chip_database.json on the algorithm axis (col 1 datasheets slug + col 2 descriptive name) with the 4 NAME-01 facets per bucket"
    - "FM1608 (0x28 SRAM_STD) and X88C64 (0x34 EEPROM) corrections are documented with their true infoic.xml identity tuples and the FM1608 0x40 decimal/hex conflation is explicitly retired"
    - "Phantom 0x35/0x39 and infeasible 0x11/0x2A/0x2B/0x2C buckets appear in an 'Honest non-protocols' section, named as non-protocols not buckets-with-behavior"
    - "The INV-01..INV-09 traceability matrix section exists, each row mapping INV id -> behavior -> owning handler -> the exact native test function name AND the matrix-assigned suite path that pins it"
    - "The pre-phase Leonardo flash byte count is captured to firestarter/.flash-baseline-87.txt BEFORE any firmware edit, as the single integer the Plan 04 flash-delta gate (NAME-05/D-10) subtracts against"
  artifacts:
    - path: "firestarter/doc/PROTOCOLS.md"
      provides: "Protocol vocabulary + INV-01..09 traceability matrix (with per-INV suite paths) + Honest non-protocols section"
      min_lines: 200
      contains: "Honest non-protocols"
    - path: "firestarter/.flash-baseline-87.txt"
      provides: "Pre-phase Leonardo flash byte count (single integer) for the Plan 04 flash-delta gate"
      contains: "[0-9]"
  key_links:
    - from: "firestarter/doc/PROTOCOLS.md (INV matrix rows)"
      to: "native test function names (Plan 03) + handler header blocks (Plan 02)"
      via: "stable INV-01..INV-09 ids + per-INV suite paths referenced verbatim in all three"
      pattern: "INV-0[1-9]"
    - from: "firestarter/.flash-baseline-87.txt (pre-phase byte count)"
      to: "Plan 04 Task 2 flash-delta gate (post - pre <= threshold)"
      via: "Plan 04 reads this integer and subtracts the post-phase Leonardo flash bytes against it"
      pattern: "[0-9]"
---

<objective>
Author `firestarter/doc/PROTOCOLS.md` — the single canonical, GitHub-visible protocol
vocabulary for the now-correct Phase-86 DB (D-01). It maps every `protocol_id` present in
`chip_database.json` to its existing `datasheets/<hex>-<NAME>/` folder slug (col 1, unchanged —
D-02) AND a descriptive algorithm-axis name (col 2), with the four NAME-01 facets per bucket
(write algorithm, erase model, VPP behavior, pin roles), datasheet-cited (D-08). It documents the
corrected FM1608 (0x28 SRAM_STD) and X88C64 (0x34 EEPROM) with their true infoic.xml identity
tuples and retires the "FM1608 0x40" decimal-40/hex-0x28 conflation (NAME-04). It carries an
"Honest non-protocols" section for the phantom (0x35/0x39) and infeasible (0x11/0x2A/0x2B/0x2C)
buckets (D-08), and the INV-01..INV-09 traceability matrix section (D-05, NAME-03 doc side). As
Wave 1 (BEFORE any firmware edit lands in Plans 02/03), it also captures the pre-phase Leonardo
flash byte count to `firestarter/.flash-baseline-87.txt` so the Plan 04 flash-delta gate can
actually FAIL on a PROGMEM regression (NAME-05/D-10).

Purpose: This doc canonicalizes the INV-01..09 ids that Plans 02 (handler comments) and 03
(native tests) reference verbatim — it is the source of truth the recompose phases (88/89) grep
for SAFE-02. It also documents the legible-protocol contract (NAME-01/04) the milestone exists to
establish, and pins the zero-flash baseline the whole frozen-world contract is measured against.
Output: `firestarter/doc/PROTOCOLS.md` (new GitHub-visible doc in the firestarter sub-repo) +
`firestarter/.flash-baseline-87.txt` (pre-phase flash byte count).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-naming-documentation-pass/87-CONTEXT.md
@.planning/phases/87-naming-documentation-pass/87-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Capture the pre-phase Leonardo flash baseline (BEFORE any firmware edit)</name>
  <read_first>
    - firestarter/CLAUDE.md (the `pio run -e leonardo` build invocation + native harness notes)
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (zero-flash discipline lines 291-294 — the near-zero delta this baseline anchors)
  </read_first>
  <action>
    BEFORE editing any firmware file (this runs in Wave 1; Plans 02/03 in Wave 2 are the first to touch firmware), build the firmware clean and capture the pre-phase Leonardo flash byte count to `firestarter/.flash-baseline-87.txt` as a single integer with no other text.

    Run `pio run -e leonardo` from the firestarter sub-repo. PlatformIO prints exactly one `Flash:` summary line of the form `Flash: [=======   ]  69.7%  (used 25430 bytes from 32256 bytes)`. Parse the byte count from the `used N bytes` token of THAT one line (do NOT also capture the RAM line, and do NOT match a bare `Program` line — pin a single line so exactly one number is extracted). Write only that integer (e.g. `25430`) to `firestarter/.flash-baseline-87.txt`.

    Exact capture command (cite verbatim in the summary):
    `pio run -e leonardo 2>&1 | grep -E '^Flash:' | grep -oE 'used [0-9]+ bytes' | head -1 | grep -oE '[0-9]+' > .flash-baseline-87.txt`

    Confirm the file holds a single positive integer (sanity: it should be in the ~24000-28000 range for this firmware; the current elf measures 25430 text via avr-size). This integer is the frozen-world zero-flash anchor: Plan 04 Task 2 subtracts the post-phase flash bytes against it and FAILS if the delta exceeds the threshold. Do NOT edit any firmware/DB/datasheet file in this task — only the build + the one-integer write.
  </action>
  <verify>
    <automated>cd firestarter && test -s .flash-baseline-87.txt && grep -qE '^[0-9]+$' .flash-baseline-87.txt && BASE=$(cat .flash-baseline-87.txt) && [ "$BASE" -gt 20000 ] && echo "BASELINE_OK $BASE"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/.flash-baseline-87.txt` exists and contains exactly one positive integer (the pre-phase Leonardo flash byte count, parsed from the single `Flash:` line's `used N bytes` token).
    - The capture ran BEFORE any firmware edit (Wave 1, before Plans 02/03).
    - No firmware/DB/datasheet file modified in this task.
  </acceptance_criteria>
  <done>firestarter/.flash-baseline-87.txt holds the single pre-phase Leonardo flash byte count; verify confirms a positive integer.</done>
</task>

<task type="auto">
  <name>Task 1: Author per-bucket vocabulary + NAME-04 corrections + Honest non-protocols section</name>
  <read_first>
    - firestarter/doc/SHIELD-REVISIONS.md (lines 1-57 — the orientation + reader-router + 2-col wide-table analog to replicate, per PATTERNS lines 62-93)
    - firestarter/datasheets/README.md (the Phase-85 datasheet index; folder slugs feed col 1)
    - firestarter/datasheets/ folder listing (the 12 slugs: 0x05-FLASH-AMD-STD, 0x06-FLASH-AMD-ALT, 0x07-EPROM-STD, 0x08-EPROM-QUICK, 0x0B-EPROM-LEGACY, 0x0D-EEPROM-POLL, 0x0E-SRAM-32PIN, 0x10-FLASH-INTEL, 0x27-SRAM-24PIN, 0x28-SRAM-STD, 0x29-SRAM-512K-1M, 0x34-EEPROM-X88C64)
    - .planning/research/PROTOCOLS.md (the RURP control-register map + per-bucket prose to DISTILL — NOT copy code from; correct its "FM1608 algorithm 40 = 0x28" decimal/hex conflation)
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (12-bucket landscape + erase-scope facets)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (the true identity tuples: FM1608 = type4/proto0x07/variant0x4126 -> 0x28; X88C64 = type1/proto0x34/variant0x3100/flags0x00414200 -> EEPROM)
    - firestarter/CLAUDE.md (the per-protocol name + VPP + file source-of-truth table to distill into columns)
    - firestarter_app/firestarter/data/chip_database.json (authoritative bucket set — re-verify the live programming.algorithm values, do not trust memory)
  </read_first>
  <action>
    First re-enumerate the live bucket set: run `python3` over `firestarter_app/firestarter/data/chip_database.json` reading `programming.algorithm` per chip and confirm the present set is exactly the 12 real buckets (decimal 5,6,7,8,11,13,14,16,39,40,41,52 = hex 0x05,0x06,0x07,0x08,0x0B,0x0D,0x0E,0x10,0x27,0x28,0x29,0x34). If the live set differs from this, document the actual set — the DB is authoritative, not this list.

    Open PROTOCOLS.md with the orientation pattern from SHIELD-REVISIONS.md: a one-paragraph "what this doc is", a "if you want X read section Y" reader-router using the `[section N (label)](#anchor)` heading-anchor form, and a pointer to the deeper sources `.planning/research/PROTOCOLS.md` and `.planning/v1.13-PROTOCOL-ENUMERATION.md`.

    Author ONE section per real protocol_id bucket (D-08). Each section header carries the two-name scheme (D-02): the existing `datasheets/<hex>-<NAME>/` folder slug (col 1, NOT renamed) AND a descriptive algorithm-axis name (col 2). Each section covers the four NAME-01 facets — write algorithm, erase model, VPP behavior, pin roles — each datasheet-cited using the anchored form `datasheets/<slug>/<file>.pdf p.N section` (cite the best available locator per D-discretion). Buckets and handlers per PATTERNS bucket-enumeration table: 0x05->flash_type_4.cpp, 0x06->flash_type_3.cpp, 0x07/0x08/0x0B->eprom.cpp, 0x0D->eeprom_28c.cpp, 0x0E/0x27/0x28/0x29->sram.cpp, 0x10->flash_intel.cpp, 0x34->not_implemented.cpp.

    For the NAME-04 corrections, add explicit call-outs: in the 0x28 section document FM1608 = type4/proto0x07/variant0x4126 -> resolved to SRAM_STD/0x28, and explicitly state the historical "FM1608 0x40" framing is a decimal-40 to hex-0x28 conflation now retired. In the 0x34 section document X88C64 = type1/proto0x34/variant0x3100/flags0x00414200 (flags&0x10==0) -> electrical.type EEPROM, dispatched to not_implemented (PCB-blocked FUT-01 — document only, do NOT plan a handler).

    Add the "Honest non-protocols" section (D-08): list phantom 0x35 and 0x39 (zero DB chips, dispatched-but-dead) and infeasible 0x11, 0x2A, 0x2B, 0x2C (fail-closed, zero DB chips). Name them explicitly as NON-protocols, not buckets with documented behavior.

    Do NOT add a 9-row INV matrix in this task (Task 2 owns it). Do NOT modify any DB record, datasheet folder, or firmware file (D-09 frozen world).
  </action>
  <verify>
    <automated>grep -c "Honest non-protocols" firestarter/doc/PROTOCOLS.md | grep -qv '^0$' && for b in 0x05 0x06 0x07 0x08 0x0B 0x0D 0x0E 0x10 0x27 0x28 0x29 0x34; do grep -q "$b" firestarter/doc/PROTOCOLS.md || { echo "MISSING bucket $b"; exit 1; }; done && for n in 0x35 0x39 0x11 0x2A 0x2B 0x2C; do grep -q "$n" firestarter/doc/PROTOCOLS.md || { echo "MISSING non-protocol $n"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PROTOCOLS.md exists at firestarter/doc/PROTOCOLS.md and opens with orientation + reader-router mirroring SHIELD-REVISIONS.md.
    - All 12 real buckets (0x05,0x06,0x07,0x08,0x0B,0x0D,0x0E,0x10,0x27,0x28,0x29,0x34) each have a section with the two-name scheme header and the 4 NAME-01 facets, each facet datasheet-cited with an anchored locator.
    - The 0x28 section documents FM1608 tuple type4/proto0x07/variant0x4126->0x28 and explicitly retires the decimal-40/hex-0x28 conflation; the 0x34 section documents X88C64 tuple type1/proto0x34/variant0x3100/flags0x00414200->EEPROM (document-only, PCB-blocked).
    - An "Honest non-protocols" section names 0x35, 0x39 (phantom) and 0x11, 0x2A, 0x2B, 0x2C (infeasible) as non-protocols.
    - No firmware file, datasheet folder, or DB record is modified.
  </acceptance_criteria>
  <done>PROTOCOLS.md per-bucket vocabulary + NAME-04 corrections + Honest non-protocols section authored; verify grep passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author the INV-01..INV-09 traceability matrix section + planned test-name + suite-path columns</name>
  <read_first>
    - firestarter/doc/PROTOCOLS.md (the doc being extended by this task — the per-bucket sections from Task 1)
    - .planning/phases/87-naming-documentation-pass/87-CONTEXT.md (D-04 the exact 9 invariants + ordering; D-05 the greppable INV-id contract)
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (Shared Patterns: the INV-01..09 table with owning handlers lines 261-272; the test-function naming convention lines 174-179; the per-suite INV home table lines 163-168)
    - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp (existing test function names e.g. test_eprom_0x0B_write_enables_vpp_regulator — the convention the matrix's planned test-name column must align to)
  </read_first>
  <action>
    Add an INV-01..INV-09 traceability matrix section to PROTOCOLS.md (D-05 — single source of truth, lives inside this doc). Use exactly the D-04 ordering and wording for the 9 invariants: INV-01 0x0B direct-VPE rail (eprom.cpp); INV-02 0x0B shared OE/VPP read-skip (eprom.cpp); INV-03 0x08 P1-as-VPP (eprom.cpp); INV-04 flash4 256B page boundary (flash_type_4.cpp); INV-05 VPP-skip-on-read (eprom.cpp); INV-06 pulse-delay defaults (eprom.cpp); INV-07 FM1608 SRAM->FRAM (sram.cpp); INV-08 WARNING-5 0x07->0x0D override now decode-delivered (eprom.cpp / build path); INV-09 SST39SF040 keep-Flash/EEPROM (flash_type_3.cpp).

    Each matrix row has FIVE columns: INV id | one-line behavior | owning handler file | the EXACT planned native test function name that pins it | the EXACT matrix-assigned suite PATH (`test/native/avr/test_val_<family>/`) that hosts it. Decide the per-INV test function names now using the existing greppable convention (e.g. `test_inv01_eprom_0x0B_direct_vpe_rail`) and the per-suite home table from PATTERNS — these planned names AND their per-INV suite paths are the contract Plan 03 implements verbatim (each INV must land in its matrix-assigned suite path, NOT just anywhere under the native tree) and Plan 02 references. Map INV->suite path per PATTERNS (this exact mapping is the contract Plan 03's verify asserts against):
      - INV-01, INV-02, INV-03, INV-05, INV-06, INV-08 -> `test/native/avr/test_val_eprom/`
      - INV-04 -> `test/native/avr/test_val_flash4/`
      - INV-07 -> `test/native/avr/test_val_sram/`
      - INV-09 -> `test/native/avr/test_val_flash3/`

    Add a short note in the matrix preamble: the INV ids are the SAFE-02 handoff to Phases 88/89 and must survive a recompose grep-intact; one `grep -rn INV-04` must hit this doc row + the native test name + the owning handler header block (the latter two delivered by Plans 03 and 02); each INV's test MUST live in the suite path this matrix assigns it.

    Cross-link the matrix rows to the per-bucket sections from Task 1 using the `[section N (label)](#anchor)` heading-anchor form (PATTERNS line 90).
  </action>
  <verify>
    <automated>for i in 01 02 03 04 05 06 07 08 09; do grep -q "INV-$i" firestarter/doc/PROTOCOLS.md || { echo "MISSING INV-$i"; exit 1; }; done && for s in test_val_eprom test_val_flash4 test_val_sram test_val_flash3; do grep -q "test/native/avr/$s" firestarter/doc/PROTOCOLS.md || { echo "MISSING suite path $s in matrix"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PROTOCOLS.md contains a traceability matrix section with all 9 rows INV-01..INV-09 in the D-04 ordering/wording.
    - Each row carries FIVE columns: INV id, one-line behavior, owning handler file, planned native test function name (matching the existing test_<family>_<protocol>_<behavior> convention with the INV id embedded/greppable), and the matrix-assigned suite PATH (`test/native/avr/test_val_<family>/`).
    - The INV->suite-path mapping matches PATTERNS exactly (eprom: 01/02/03/05/06/08; flash4: 04; sram: 07; flash3: 09) and is stated as the per-INV contract Plan 03 must honor.
    - `grep -q "INV-0[1-9]" firestarter/doc/PROTOCOLS.md` finds all 9 ids; the four suite paths appear; matrix preamble states the SAFE-02 grep-intact + per-INV-suite handoff.
    - No firmware or DB change in this task.
  </acceptance_criteria>
  <done>INV-01..09 matrix section authored inside PROTOCOLS.md with planned greppable test names + per-INV suite paths; all 9 ids and 4 suite paths present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change. No wire/protocol/DB surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | DB/dispatch drift (a doc edit that silently implies or causes a wire/DB change) | mitigate | This plan touches only firestarter/doc/PROTOCOLS.md + the read-only flash-baseline capture file (no firmware/DB files in files_modified); the load-bearing SAFE-04 over-voltage / SAFE-03 DB-frozen regression class is gated by check_dispatch.py 0-violations + empty diff_db.py + the now-failable Leonardo flash-delta gate (anchored on the baseline this plan captures), all run in Plan 04. No new attack surface. |
</threat_model>

<verification>
- The pre-phase Leonardo flash byte count is captured to firestarter/.flash-baseline-87.txt (single integer) BEFORE any firmware edit.
- PROTOCOLS.md enumerates exactly the live-DB bucket set (re-verified from chip_database.json, not memory).
- NAME-04 corrections (FM1608/X88C64) documented with true infoic.xml tuples; 0x40 conflation retired.
- Honest non-protocols section names all 6 phantom/infeasible buckets.
- INV-01..09 matrix present with planned greppable test names + per-INV suite paths; SAFE-02 handoff noted.
- Zero firmware/DB/datasheet-folder modification (frozen world; proven by Plan 04 gates).
</verification>

<success_criteria>
- firestarter/doc/PROTOCOLS.md exists, GitHub-visible, mirroring the SHIELD-REVISIONS.md doc style.
- firestarter/.flash-baseline-87.txt holds the pre-phase Leonardo flash byte count for the Plan 04 flash-delta gate.
- ROADMAP success criteria 1 (vocabulary), 3 (matrix doc side), 4 (NAME-04 + honest non-protocols) satisfied by this doc.
- The canonical INV-01..09 ids + planned test names + per-INV suite paths are set for Plans 02/03 to reference.
</success_criteria>

<output>
Create `.planning/phases/87-naming-documentation-pass/87-01-SUMMARY.md` when done.
</output>
