---
phase: 87-naming-documentation-pass
plan: 03
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp
  - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp
  - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp
  - firestarter/test/native/avr/test_val_sram/test_val_sram.cpp
autonomous: true
requirements: [NAME-03, SAFE-06]
must_haves:
  truths:
    - "Each of the 9 invariants INV-01..INV-09 maps to a live native-test assertion whose function name/docstring carries its INV id, matching the PROTOCOLS.md matrix row verbatim AND landing in that INV's matrix-assigned suite path (not just anywhere under the native tree)"
    - "A minimal targeted native test is added ONLY where an invariant has no existing covering assertion (no golden-trace duplication — that is Phase 88)"
    - "pio test -e native exits 0 with the new assertions registered via RUN_TEST"
  artifacts:
    - path: "firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp"
      provides: "INV-01/02/03/05/06/08 covering assertions named with their INV ids"
      contains: "INV-0"
  key_links:
    - from: "native test function names"
      to: "firestarter/doc/PROTOCOLS.md INV matrix rows + handler header blocks"
      via: "INV-0x id embedded in each test name/docstring, in its matrix-assigned suite path"
      pattern: "INV-0[1-9]"
---

<objective>
Complete the INV-01..INV-09 traceability matrix by ensuring each invariant maps to a LIVE native
test assertion whose function name/docstring carries its INV id (D-05, NAME-03 test side) AND lands
in the suite path the Plan 01 matrix assigns it. Where an invariant already has a covering
assertion in its matrix-assigned test_val_* suite, annotate that assertion's name/docstring with
the INV id; where a cell is empty, add a MINIMAL targeted assertion (D-06 — just enough to pin the
one observable behavior; full per-family golden traces are Phase 88, do NOT duplicate). Extend the
existing Unity + recording-bus harness; do not create a new harness.

Purpose: NAME-03 — closes the third SAFE-02 grep target so `grep -rn INV-0x` hits doc + handler +
test for every invariant, with each INV in its matrix-assigned suite path so a misfiled INV id is
caught — giving the recompose phases (88/89) a machine-traceable, suite-anchored contract.
Output: gap-fill / annotated assertions across the test_val_eprom, test_val_flash3,
test_val_flash4, test_val_sram suites in the firestarter sub-repo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-naming-documentation-pass/87-CONTEXT.md
@.planning/phases/87-naming-documentation-pass/87-PATTERNS.md
@firestarter/doc/PROTOCOLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map/annotate INV-01/02/03/05/06/08 in test_val_eprom and INV-04/07/09 in flash4/sram/flash3</name>
  <read_first>
    - firestarter/doc/PROTOCOLS.md (the INV-01..09 matrix from Plan 01 — the planned test function names this task implements verbatim; the INV->suite-PATH mapping each INV must land in)
    - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp (the richest analog: existing test func names lines 105/119/133/154/175/190; recording-bus assertion mechanics lines 48-100; setUp + make_handle boilerplate lines 54-79; Unity RUN_TEST registration lines 205-220)
    - firestarter/test/native/avr/test_val_eprom/host_stubs.cpp (opt-in flags HOST_STUBS_RECORD_BUS / HOST_STUBS_CUSTOM_HW_REVISION before the shared include, PATTERNS lines 215-220)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (INV-04 256B page boundary home)
    - firestarter/test/native/avr/test_val_sram/test_val_sram.cpp (INV-07 FM1608 SRAM->FRAM home)
    - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp (INV-09 SST39SF040 keep-Flash/EEPROM home)
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (per-suite INV home table lines 163-168; test-naming convention lines 174-179; the uint8_t recording-buffer PITFALL lines 195-198 — assert on 8-bit-fit bits CTRL_VPP_REGULATOR_ENABLE 0x80 / CTRL_VPP_P1_ENABLE 0x08, NOT CTRL_VPP_VPE_DROP_ENABLE 0x100; D-06 minimal-only rule lines 170-172)
  </read_first>
  <action>
    For each invariant, first check whether its MATRIX-ASSIGNED test_val_* suite ALREADY has a covering assertion (e.g. test_val_eprom.cpp lines 105-190 already cover 0x07/0x08/0x0B VPP-enable + read-configure-only behaviors that map onto several INV cells). Where a covering assertion exists in that suite, annotate its function name or docstring/comment with the matching INV id so it is greppable (D-05) — match the planned name from the PROTOCOLS.md matrix verbatim. Where the cell is EMPTY, add a MINIMAL targeted assertion (D-06) IN THAT INV'S MATRIX-ASSIGNED SUITE using the recording-bus mechanics from test_val_eprom.cpp (clear_bus_recording / bus_recording_count / recorded_reg / recorded_data) and the make_handle builder; register it with one RUN_TEST line in that suite's main(). Keep each new test to the single observable behavior — NO full register golden traces (Phase 88).

    Each INV MUST land in the suite PATH the Plan 01 matrix assigns it (a misfiled INV id in the wrong suite is a contract violation and the verify will catch it). INV->suite-path (per PATTERNS + the Plan 01 matrix — exact):
      - INV-01 (0x0B direct-VPE rail), INV-02 (0x0B shared OE/VPP read-skip), INV-03 (0x08 P1-as-VPP), INV-05 (VPP-skip-on-read), INV-06 (pulse-delay defaults), INV-08 (WARNING-5 0x07->0x0D) -> test/native/avr/test_val_eprom/
      - INV-04 (256B page boundary) -> test/native/avr/test_val_flash4/
      - INV-07 (FM1608 SRAM->FRAM) -> test/native/avr/test_val_sram/
      - INV-09 (SST39SF040 keep-Flash/EEPROM) -> test/native/avr/test_val_flash3/

    Honor the uint8_t recording-buffer PITFALL: for VPP detection assert on 8-bit-fit bits (CTRL_VPP_REGULATOR_ENABLE 0x80, CTRL_VPP_P1_ENABLE 0x08), never CTRL_VPP_VPE_DROP_ENABLE 0x100. If a new assertion references new rurp_* symbols, extend the suite's host_stubs.cpp; otherwise reuse it. Change NO handler executable code (frozen world; this plan touches only test files).
  </action>
  <verify>
    <automated>cd firestarter && fail=0; for i in 01 02 03 05 06 08; do grep -rqn "INV-$i" test/native/avr/test_val_eprom/ || { echo "MISSING INV-$i in test_val_eprom/"; fail=1; }; done; grep -rqn "INV-04" test/native/avr/test_val_flash4/ || { echo "MISSING INV-04 in test_val_flash4/"; fail=1; }; grep -rqn "INV-07" test/native/avr/test_val_sram/ || { echo "MISSING INV-07 in test_val_sram/"; fail=1; }; grep -rqn "INV-09" test/native/avr/test_val_flash3/ || { echo "MISSING INV-09 in test_val_flash3/"; fail=1; }; [ "$fail" -eq 0 ] && echo OK || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - All 9 ids INV-01..INV-09 are grep-findable in firestarter/test/native/avr/ AND each lands in its matrix-assigned suite PATH (not just anywhere under the native tree).
    - INV-01/02/03/05/06/08 are in test_val_eprom/; INV-04 in test_val_flash4/; INV-07 in test_val_sram/; INV-09 in test_val_flash3/ — a misfiled INV id in the wrong suite fails the verify.
    - Each new assertion is minimal (single observable behavior, no golden trace) and registered with a RUN_TEST line; function name/docstring matches the PROTOCOLS.md matrix verbatim.
    - VPP assertions use 8-bit-fit bits (0x80 / 0x08), not 0x100; no handler executable code changed.
  </acceptance_criteria>
  <done>Every INV-01..09 maps to a live, greppable native assertion IN ITS MATRIX-ASSIGNED SUITE PATH; new tests minimal.</done>
</task>

<task type="auto">
  <name>Task 2: Run the native suites and confirm green</name>
  <read_first>
    - firestarter/test/native/avr/ (the suites edited in Task 1)
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (invocation line 222: `pio test -e native` all suites, or `-f "*test_val_eprom*"` for one)
    - firestarter/CLAUDE.md (the pio test invocation + native harness notes)
  </read_first>
  <action>
    Run `pio test -e native` from the firestarter sub-repo and confirm exit 0 with all assertions (existing + new INV gap-fill) passing. If a new assertion fails because the asserted behavior differs from the handler's real behavior, the TEST is wrong (frozen world — do NOT change handler code to make a test pass); correct the assertion to pin the actual observable behavior, or re-target the INV id, keeping it minimal. Capture the final Unity PASS/result line into the plan summary (the verify uses `tee` so the result line is captured on both pass and fail).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native 2>&1 | tee /tmp/native87.txt | tail -20; grep -qE "^(OK|FAIL)|[0-9]+ Tests" /tmp/native87.txt; if grep -qiE "FAILED|: FAIL" /tmp/native87.txt; then echo NATIVE_FAIL; exit 1; fi; tail -3 /tmp/native87.txt; echo NATIVE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` exits 0 (all suites green) with the new INV assertions registered and passing.
    - The Unity result line is captured (via tee) into /tmp/native87.txt on both pass and fail, and recorded in the summary.
    - No handler executable code was changed to make a test pass (frozen world preserved).
  </acceptance_criteria>
  <done>pio test -e native exits 0 with the INV gap-fill assertions green; Unity result line captured via tee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Native-test-only change. No wire/protocol/DB surface; no handler executable code touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03 | Tampering | a test edit that masks a real behavior regression, or a handler edit smuggled in to make a test pass | mitigate | This plan's files_modified are test files only; the frozen-world contract (DB/dispatch/over-voltage) is gated by check_dispatch.py 0-violations + empty diff_db.py + Leonardo flash delta in Plan 04. Tests assert the ACTUAL observable behavior (no handler code change to force a pass), and the per-INV suite-path verify catches a misfiled INV id. No new attack surface. |
</threat_model>

<verification>
- All 9 INV ids grep-findable in firestarter/test/native/avr/ in their matrix-assigned suite PATHS (misfile caught).
- New assertions minimal (no golden traces — Phase 88) and registered via RUN_TEST.
- `pio test -e native` exits 0; Unity result line captured via tee; no handler code changed to satisfy a test.
</verification>

<success_criteria>
- ROADMAP success criterion 3 (invariant->native-test traceability matrix; minimal gap-fill where empty) fully satisfied across doc (Plan 01) + tests (this plan), with per-INV suite-path anchoring.
- The third SAFE-02 grep target lands: `grep -rn INV-0x` now hits doc + handler + test for every invariant, each in its assigned suite.
</success_criteria>

<output>
Create `.planning/phases/87-naming-documentation-pass/87-03-SUMMARY.md` when done.
</output>
