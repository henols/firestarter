---
phase: 87-naming-documentation-pass
plan: 04
type: execute
wave: 3
depends_on: ["87-01", "87-02", "87-03"]
files_modified: []
autonomous: true
requirements: [NAME-05, SAFE-03, SAFE-06]
must_haves:
  truths:
    - "check_dispatch.py exits 0 violations against the Phase-86-repinned baseline (SAFE-03)"
    - "diff_db.py exits 0 with an EMPTY diff against the re-pinned baseline — this phase changed no DB records (D-09, NAME-05)"
    - "pio run -e leonardo flash delta = (post - pre) computed against firestarter/.flash-baseline-87.txt is <= 16 bytes — the gate EXITS 1 on a PROGMEM/executable regression, not merely prints the flash line (D-10, NAME-05)"
    - "The full greppability contract holds: grep -rn INV-04 (and every INV-0x) hits >=3 places — the PROTOCOLS.md matrix row, the native test name, and the owning handler header block (SAFE-02 handoff)"
    - "Frozen-host claim is machine-checked: git -C firestarter_app diff --quiet over firestarter/, tools/, tests/ exits 0 (NO host files modified) — the true SAFE-06 frozen-world claim, not masked by the devcontainer py3.12-vs-CI-py3.11 gap"
  artifacts:
    - path: ".planning/phases/87-naming-documentation-pass/87-04-SUMMARY.md"
      provides: "Recorded gate transcripts proving the frozen-world contract"
  key_links:
    - from: "the four hard gates"
      to: "the frozen-world contract (NAME-05 / SAFE-03 / D-09 / D-10 / SAFE-06)"
      via: "check_dispatch 0-violations + diff_db empty + failable Leonardo flash delta vs baseline + INV greppability + frozen-host git-diff"
      pattern: "0 violations|empty"
---

<objective>
Prove the frozen-world contract AFTER the doc (Plan 01) + handler comments (Plan 02) + native
tests (Plan 03) have landed. Run the hard gates read-only and confirm: check_dispatch.py = 0
violations, diff_db.py = empty diff vs the Phase-86-repinned baseline, `pio run -e leonardo` flash
delta vs the Plan 01 baseline is <= 16 bytes (a FAILABLE gate, not a print), the INV-0x
greppability contract holds across doc + handler + test, and the host repo is byte-frozen
(machine-checked via git-diff, the true SAFE-06 claim that the devcontainer py3.12-vs-CI-py3.11 gap
cannot mask). This plan changes NO files — it is the verification wave that ring-fences the contract
(D-09/D-10, NAME-05, SAFE-03, SAFE-06).

Purpose: NAME-05 / SAFE-03 / SAFE-06 — sign off that the naming pass moved nothing electrical and
touched no host code. The gates are the same ones Phase 86 left green; this phase must leave them
green/empty, with the flash-delta gate now able to actually FAIL.
Output: gate transcripts recorded in the plan summary (no code/doc/DB change).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-naming-documentation-pass/87-CONTEXT.md
@.planning/phases/87-naming-documentation-pass/87-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the DB/dispatch frozen-world gates (check_dispatch 0-violations + diff_db empty)</name>
  <read_first>
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (Read-only gates section lines 227-250: exact invocation, exit-code contract, baseline paths — run with default paths, do NOT edit the gates)
    - firestarter_app/tools/check_dispatch.py (module docstring lines 16-21; PASS print lines 491-502 — exit 0 = every chip resolves + no SRAM-on-eprom dispatch)
    - firestarter_app/tools/diff_db.py (module docstring lines 11-22 — exit 0 = empty/all-explained vs re-pinned baseline; exit 1 = unexplained diff; exit 2 = infra error)
    - firestarter_app/tools/baseline/chip_database.baseline.json + dispatch_baseline.json (the 746-chip Phase-86 re-pinned baselines this phase is frozen against)
    - firestarter_app/CLAUDE.md (host env discipline: validate against CI py3.11, not the 3.12 devcontainer)
  </read_first>
  <action>
    From the firestarter_app sub-repo, run the two read-only gates with default baseline paths (do NOT edit them): `python tools/check_dispatch.py` (cite PATTERNS lines 232-236) and `python tools/diff_db.py`. Confirm check_dispatch reports 0 violations (exit 0) and diff_db is an EMPTY identity diff (exit 0 — 0 changed / 0 new / 0 missing) against the Phase-86-repinned baseline. This phase changed no DB records, so any non-empty diff_db output or non-zero exit is a BLOCKER — investigate and remediate (no DB record should have moved; if one did, a non-doc file was wrongly edited). Capture both transcripts verbatim into the plan summary.
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/check_dispatch.py >/tmp/cd.txt 2>&1; CD=$?; python tools/diff_db.py >/tmp/dd.txt 2>&1; DD=$?; echo "check_dispatch exit=$CD"; tail -3 /tmp/cd.txt; echo "diff_db exit=$DD"; tail -5 /tmp/dd.txt; [ "$CD" -eq 0 ] && [ "$DD" -eq 0 ] && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/check_dispatch.py` exits 0 with a PASS line (0 violations) against the 746-chip re-pinned baseline.
    - `python tools/diff_db.py` exits 0 with an empty/identity diff (0 changed, 0 new, 0 missing) against the re-pinned baseline.
    - Neither gate file was edited; both transcripts captured in the summary.
  </acceptance_criteria>
  <done>check_dispatch.py = 0 violations (exit 0) and diff_db.py = empty diff (exit 0); transcripts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run the failable Leonardo flash-delta gate + INV greppability + frozen-host git-diff</name>
  <read_first>
    - .planning/phases/87-naming-documentation-pass/87-PATTERNS.md (zero-flash discipline lines 291-294: pio run -e leonardo near-zero delta; D-10 no PROGMEM; the INV greppability wiring lines 274-276 — one grep -rn INV-04 hits doc + test + handler)
    - firestarter/.flash-baseline-87.txt (the pre-phase Leonardo flash byte count captured by Plan 01 Task 0 — the integer this gate subtracts against)
    - firestarter/doc/PROTOCOLS.md (Plan 01 matrix rows — one INV grep target)
    - firestarter/src/proms/ (Plan 02 handler header blocks — second INV grep target)
    - firestarter/test/native/avr/ (Plan 03 test names — third INV grep target)
    - firestarter_app/CLAUDE.md (note: this phase made NO host-code change — all work lands in the firestarter sub-repo; the true frozen-host claim is "no host file modified", checked by git-diff which the devcontainer py3.12-vs-CI-py3.11 gap cannot mask)
    - firestarter/CLAUDE.md (pio run -e leonardo build invocation)
  </read_first>
  <action>
    FAILABLE flash-delta gate (the load-bearing fix): build firmware with `pio run -e leonardo` from the firestarter sub-repo, parse the post-phase flash byte count from the single `Flash:` line's `used N bytes` token (same parse Plan 01 Task 0 used), read the pre-phase baseline integer from `firestarter/.flash-baseline-87.txt`, compute `DELTA = post - pre`, and EXIT 1 if `DELTA > 16` bytes. Threshold rationale: this phase adds only C comments (`//`, `/* */`), which compile to ZERO flash — a defensible near-zero allowance of 16 bytes absorbs only incidental compiler/alignment noise; any larger delta means a PROGMEM/PSTR string or executable change slipped in (Plan 02 regression) and is a BLOCKER. The gate must be able to FAIL, not merely print the flash line.

    INV greppability contract: for each INV-01..INV-09, `grep -rln INV-NN firestarter/` must hit at least 3 FILES — the PROTOCOLS.md matrix row, the native test name, and the owning handler header block (SAFE-02 handoff). Spot-check INV-04 specifically (the CONTEXT/PATTERNS exemplar) hits >=3 files.

    Frozen-host machine check (replaces the old prose py3.11 criterion): this phase touched no host code, so the TRUE SAFE-06 claim is that NO file under the firestarter_app sub-repo's source/tooling/tests changed. Assert it with git-diff (not a toolchain run, which py3.12 in the devcontainer would mask): `git -C firestarter_app diff --quiet -- firestarter/ tools/ tests/` must exit 0 (zero host modifications). Keep the read-only check_dispatch.py / diff_db.py runs (Task 1) as the legitimate executing host gates. Do NOT hand-normalize generated messages.py (it is not touched this phase anyway). Capture the flash pre/post/delta figures, the INV grep counts, and the frozen-host git-diff result into the summary.
  </action>
  <verify>
    <automated>cd firestarter && PRE=$(cat .flash-baseline-87.txt) && POST=$(pio run -e leonardo 2>&1 | grep -E '^Flash:' | grep -oE 'used [0-9]+ bytes' | head -1 | grep -oE '[0-9]+') && [ -n "$POST" ] && DELTA=$((POST - PRE)); echo "flash pre=$PRE post=$POST delta=$DELTA"; [ "$DELTA" -le 16 ] || { echo "FLASH REGRESSION: delta $DELTA > 16 bytes"; exit 1; }; for i in 01 02 03 04 05 06 07 08 09; do C=$(grep -rln "INV-$i" doc/ src/ test/ 2>/dev/null | wc -l); echo "INV-$i hits $C files"; [ "$C" -ge 3 ] || { echo "INV-$i FAILS >=3 grep contract"; exit 1; }; done; git -C /workspaces/firestarter_app diff --quiet -- firestarter/ tools/ tests/ || { echo "HOST FILES MODIFIED (frozen-world violation)"; git -C /workspaces/firestarter_app diff --stat -- firestarter/ tools/ tests/; exit 1; }; echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pio run -e leonardo` succeeds; the flash delta (post - pre, vs firestarter/.flash-baseline-87.txt) is computed and the gate EXITS 1 if delta > 16 bytes (a failable gate, not a print). On a clean comment-only phase the delta is 0.
    - Every INV-01..INV-09 is grep-findable in >=3 files under firestarter/ (PROTOCOLS.md matrix row + native test name + handler header block); INV-04 spot-checked >=3.
    - Frozen-host machine check passes: `git -C firestarter_app diff --quiet -- firestarter/ tools/ tests/` exits 0 (no host source/tooling/test file modified) — the true SAFE-06 claim, not masked by the devcontainer py3.12-vs-CI-py3.11 gap; messages.py untouched.
    - Flash pre/post/delta + INV grep counts + frozen-host git-diff result recorded in the summary.
  </acceptance_criteria>
  <done>Leonardo flash delta <= 16 bytes vs baseline (gate can fail); all 9 INV ids hit >=3 files; host repo git-diff clean; transcripts recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only verification wave. No files modified (files_modified is empty). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04-SC | Tampering | DB/dispatch drift or over-voltage regression introduced earlier in the phase | mitigate | This plan IS the gate: check_dispatch.py 0-violations (the structural 12V-on-no-VPP-pin / SAFE-04 backstop) + diff_db.py empty (SAFE-03 DB-frozen) + the now-FAILABLE Leonardo flash-delta gate (post-pre vs the Plan 01 baseline, exits 1 on >16B; D-10) prove no electrical/DB drift landed. The relevant safety regression class is exactly DB/dispatch drift, already gated here — no web-style surface exists in a doc+comment+test phase. |
| T-87-04 | Tampering | host supply-chain (no package installs this phase) | accept | No npm/pip/cargo install task in this phase; gates run existing pinned tooling. The frozen-host git-diff check additionally proves no host source/tool/test file moved. Low risk. |
</threat_model>

<verification>
- check_dispatch.py exits 0 (0 violations); diff_db.py exits 0 (empty diff) vs re-pinned baseline.
- pio run -e leonardo flash delta (post - pre vs .flash-baseline-87.txt) <= 16 bytes — gate EXITS 1 on regression.
- All 9 INV-0x ids hit >=3 files (doc + test + handler) — SAFE-02 greppability proven.
- Frozen-host: git -C firestarter_app diff --quiet over firestarter/, tools/, tests/ exits 0; messages.py untouched.
</verification>

<success_criteria>
- ROADMAP success criterion 5 (failable near-zero Leonardo flash delta + empty diff_db) satisfied.
- NAME-05 (dispatch + wire values unchanged) and SAFE-03 (0-violations + empty diff every phase) signed off.
- SAFE-06 (host repo byte-frozen, no lockstep, messages.py untouched) confirmed via git-diff, not a py3.12-maskable toolchain run.
- The frozen-world contract is proven AFTER all doc/comment/test work landed — phase complete.
</success_criteria>

<output>
Create `.planning/phases/87-naming-documentation-pass/87-04-SUMMARY.md` when done.
</output>
