---
phase: 88-golden-traces-dispatch-mirror-guard-was-87
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp
  - firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_write.inc
  - firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_chip_id.inc
  - firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp
  - firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_write.inc
  - firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_chip_id.inc
autonomous: true
requirements: [PRIM-01, SAFE-02]
must_haves:
  truths:
    - "eeprom28c 0x0D write (SDP unlock P7 + DQ7 poll P5) has a committed byte-exact golden trace that passes under pio test -e native"
    - "eeprom28c chip-id (P4) has a committed byte-exact golden trace"
    - "flash_intel 0x10 write (VPP gate P3) has a committed byte-exact golden trace"
    - "flash_intel chip-id (P4) has a committed byte-exact golden trace"
    - "Existing eeprom28c and flash_intel INV/VPP assertions remain green (SAFE-02)"
  artifacts:
    - path: "firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_write.inc"
      provides: "pinned (reg,data) rows for eeprom28c 0x0D write"
      contains: "{ 0x"
    - path: "firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_chip_id.inc"
      provides: "pinned (reg,data) rows for eeprom28c chip-id"
      contains: "{ 0x"
    - path: "firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_write.inc"
      provides: "pinned (reg,data) rows for flash_intel 0x10 write (VPP gate)"
      contains: "{ 0x"
    - path: "firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_chip_id.inc"
      provides: "pinned (reg,data) rows for flash_intel chip-id"
      contains: "{ 0x"
  key_links:
    - from: "test_val_eeprom28c.cpp golden test fns"
      to: "_shared/golden_trace.h assert_trace_eq"
      via: "#include after extern \"C\" recording decls"
      pattern: "assert_trace_eq"
    - from: "test_val_flash_intel.cpp golden test fns"
      to: "_shared/golden_trace.h assert_trace_eq"
      via: "#include after extern \"C\" recording decls"
      pattern: "assert_trace_eq"
---

<objective>
Pin byte-exact register traces for the eeprom28c (0x0D) and flash_intel (0x10) families — each family's write path plus its chip-id (P4) path — reusing the `golden_trace.h` helper from 88-01.

Purpose: eeprom28c exercises the P7 (SDP unlock) and P5 (DQ7 poll/verify) primitive touchpoints; flash_intel exercises the P3 (VPP gate) touchpoint and a P4 chip-id site. These traces are the recompose oracle for those families' Phase-89 extractions (D-03).

Output: four committed `golden_*.inc` fixtures + golden test functions wired into the two suites.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-CONTEXT.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-01-SUMMARY.md
@firestarter/test/native/avr/_shared/golden_trace.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bless + pin eeprom28c 0x0D write + chip-id golden traces (P7 SDP + P5 poll + P4)</name>
  <files>firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp, firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_write.inc, firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_chip_id.inc</files>
  <read_first>
    - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp (whole file — extern "C" recording decls, setUp delay/Serial stubs to reuse, make_handle, existing INV/SDP/poll assertions to keep)
    - firestarter/test/native/avr/test_eeprom28c_chip_id/test_eeprom28c_chip_id.cpp (lines 32-45, 95-110 — scripted-byte mock + reassign-firestarter_get_data-AFTER-configure_memory idiom, Pitfall 3; same family chip-id, exact analog)
    - firestarter/test/native/avr/_shared/golden_trace.h (the assert_trace_eq + GOLDEN_BLESS helper from 88-01)
    - firestarter/src/proms/eeprom_28c.cpp (the 0x0D write/SDP/poll body — confirm which delays the path hits, Pitfall 4)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (sections "Golden write-test functions" and "Golden chip-id (P4) test functions")
  </read_first>
  <behavior>
    - test_golden_eeprom28c_write: CMD_WRITE handle, minimal representative data_size (D-04, sized so the SDP unlock + one program pulse + one DQ7 poll cycle all fire while staying under the 256 cap), configure_memory, clear_bus_recording, drive init then main, assert_trace_eq against golden_eeprom28c_write (or print_trace_inc under GOLDEN_BLESS).
    - test_golden_eeprom28c_chip_id: CMD_CHECK_CHIP_ID handle with non-zero h.chip_id, configure_memory, re-assign h.firestarter_get_data to scripted-byte mock AFTER configure_memory (Pitfall 3), clear_bus_recording, drive main, assert_trace_eq against golden_eeprom28c_chip_id.
  </behavior>
  <action>
    Add `#include "_shared/golden_trace.h"` after the suite's extern "C" recording decls. Add two golden test functions reusing the suite's existing `setUp()`. Size the write `data_size` to a minimal representative input (D-04) that still exercises one SDP unlock, one program pulse, and one DQ7 poll/verify cycle — verify against `eeprom_28c.cpp` that the chosen input fires every branch. `clear_bus_recording()` AFTER `configure_memory`; drive `operation_init` then `operation_main`. For chip-id, re-assign `h.firestarter_get_data` to a scripted-byte mock AFTER `configure_memory` (Pitfall 3) and supply a chip-id byte sequence. Declare `static const golden_entry_t golden_eeprom28c_write[] = { #include "golden_eeprom28c_write.inc" };` (+ `_n`) and the same for chip-id. Bless via a `-D GOLDEN_BLESS` build, redirect `print_trace_inc()` into each `.inc`, remove the flag. Each `.inc` header comment names the producing input + the low-byte caveat (Pitfall 1). Add `RUN_TEST(...)` for both ALONGSIDE existing tests — do not remove any INV/SDP/poll assertion (SAFE-02).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_eeprom28c*" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native -f "*test_val_eeprom28c*"` exits 0.
    - `golden_eeprom28c_write.inc` and `golden_eeprom28c_chip_id.inc` exist and committed; `grep -c "{ 0x" <file>` ≥ 1 each.
    - `grep -n "RUN_TEST(test_golden_eeprom28c" firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp` returns 2 matches.
    - No existing INV/SDP/poll assertion removed (`grep -cn "INV-" test_val_eeprom28c.cpp` not lower than before).
    - `grep -n "low-byte" firestarter/test/native/avr/test_val_eeprom28c/golden_eeprom28c_write.inc` matches.
  </acceptance_criteria>
  <done>eeprom28c 0x0D write + chip-id golden traces pinned, equality-asserted, wired in, suite green with prior tests intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bless + pin flash_intel 0x10 write + chip-id golden traces (P3 VPP gate + P4)</name>
  <files>firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp, firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_write.inc, firestarter/test/native/avr/test_val_flash_intel/golden_flash_intel_chip_id.inc</files>
  <read_first>
    - firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp (whole file — extern "C" recording decls, setUp stubs, P3 VPP-gate trace + custom-voltage stub, existing assertions to keep)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (lines 170-195 — flash_intel chip-id dispatch + CMD_CHECK_CHIP_ID idiom)
    - firestarter/test/native/avr/_shared/golden_trace.h (the helper from 88-01)
    - firestarter/src/proms/flash_intel.cpp (0x10 write VPP-gate body + chip-id; confirm delays hit, Pitfall 4; note over-voltage check at line 65 is VERIFY-ONLY, do not edit)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (sections "Golden write-test functions" and "Golden chip-id (P4) test functions")
  </read_first>
  <behavior>
    - test_golden_flash_intel_write: CMD_WRITE handle with minimal representative data_size (D-04), configure_memory, clear_bus_recording, drive init then main so the VPP-gate (P3) + program sequence is captured, assert_trace_eq against golden_flash_intel_write.
    - test_golden_flash_intel_chip_id: CMD_CHECK_CHIP_ID handle with non-zero h.chip_id, configure_memory, re-assign firestarter_get_data scripted mock AFTER configure_memory (Pitfall 3), clear_bus_recording, drive main, assert_trace_eq against golden_flash_intel_chip_id.
  </behavior>
  <action>
    Add `#include "_shared/golden_trace.h"` after the extern "C" recording decls. Add two golden test functions reusing the suite's existing `setUp()` and its custom-voltage stub pattern so the VPP-gate path runs without aborting (Pitfall 4/6 — assert the register sequence, do NOT re-derive VPP semantics or extend `_DB_CHECKED_VPP_INVARIANTS`). Minimal representative `data_size` under the cap (D-04). `clear_bus_recording()` AFTER `configure_memory`; drive `operation_init` then `operation_main`. Chip-id path re-assigns `firestarter_get_data` AFTER `configure_memory` (Pitfall 3). Declare the two golden arrays via `#include "golden_flash_intel_*.inc"`. Bless each fixture with `-D GOLDEN_BLESS`, redirect `print_trace_inc()`, remove flag. Header-comment each `.inc` with producing input + low-byte caveat. Add `RUN_TEST(...)` for both ALONGSIDE existing tests; remove no existing assertion (SAFE-02). Do not touch `flash_intel.cpp` source.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_flash_intel*" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native -f "*test_val_flash_intel*"` exits 0.
    - `golden_flash_intel_write.inc` and `golden_flash_intel_chip_id.inc` exist and committed; `grep -c "{ 0x" <file>` ≥ 1 each.
    - `grep -n "RUN_TEST(test_golden_flash_intel" firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp` returns 2 matches.
    - `firestarter/src/proms/flash_intel.cpp` is unmodified (no production source diff in this plan).
    - No existing flash_intel assertion removed.
  </acceptance_criteria>
  <done>flash_intel 0x10 write + chip-id golden traces pinned, equality-asserted, wired in, suite green; production source untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware handler → RURP bus | eeprom28c SDP/poll + flash_intel VPP-gate sequences drive control/voltage lines |
| Phase 89 refactor → golden oracle | P7/P5/P3/P4 extractions must keep these traces byte-exact (or be re-blessed under review, D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04 | Tampering (physical) | flash_intel 0x10 VPP-gate (P3) register sequence | mitigate | Byte-exact golden trace pins the VPP-gate register writes; the verify-only over-voltage check at `flash_intel.cpp:65` is confirmed unmodified in 88-05 |
| T-88-05 | Tampering (physical) | eeprom28c SDP unlock (P7) + DQ7 poll (P5) sequence | mitigate | Byte-exact golden trace trips on any SDP/poll register-order drift during Phase-89 extraction |
| T-88-06 | Spoofing (false-VPP) | 5V-family vpp_mv=12000 WP-pin voltage | accept | Golden traces assert register sequences only, not derived VPP semantics (Pitfall 6); `_DB_CHECKED_VPP_INVARIANTS` is not extended |
</threat_model>

<verification>
- `cd firestarter && pio test -e native -f "*test_val_eeprom28c*"` and `*test_val_flash_intel*` each exit 0.
- No production firmware source modified; full-suite + flash gate run in 88-05.
</verification>

<success_criteria>
- eeprom28c (0x0D) write + chip-id and flash_intel (0x10) write + chip-id golden fixtures committed and equality-asserted.
- All prior INV/SDP/poll/VPP assertions remain green (SAFE-02).
</success_criteria>

<output>
Create `.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-02-SUMMARY.md` when done
</output>
