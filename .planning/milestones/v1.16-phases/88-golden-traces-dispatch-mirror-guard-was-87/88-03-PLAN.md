---
phase: 88-golden-traces-dispatch-mirror-guard-was-87
plan: 03
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp
  - firestarter/test/native/avr/test_val_flash3/golden_flash3_write.inc
  - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp
  - firestarter/test/native/avr/test_val_flash4/golden_flash4_write.inc
  - firestarter/test/native/avr/test_val_flash4/golden_flash4_chip_id.inc
autonomous: true
requirements: [PRIM-01, SAFE-02]
must_haves:
  truths:
    - "flash3 0x06 write (AMD/SST unlock-sequence P7) has a committed byte-exact golden trace that passes under pio test -e native"
    - "flash4 0x05 write (256B page, P7 SDP) has a committed byte-exact golden trace sized under the 256-entry cap (D-04)"
    - "flash4 chip-id (P4 via flash_utils) has a committed byte-exact golden trace"
    - "Existing flash3 and flash4 INV assertions (incl. INV-04, INV-09) remain green (SAFE-02)"
  artifacts:
    - path: "firestarter/test/native/avr/test_val_flash3/golden_flash3_write.inc"
      provides: "pinned (reg,data) rows for flash3 0x06 write"
      contains: "{ 0x"
    - path: "firestarter/test/native/avr/test_val_flash4/golden_flash4_write.inc"
      provides: "pinned (reg,data) rows for flash4 0x05 write, minimal page input"
      contains: "{ 0x"
    - path: "firestarter/test/native/avr/test_val_flash4/golden_flash4_chip_id.inc"
      provides: "pinned (reg,data) rows for flash4 chip-id (P4 via flash_utils)"
      contains: "{ 0x"
  key_links:
    - from: "test_val_flash3.cpp golden test fn"
      to: "_shared/golden_trace.h assert_trace_eq"
      via: "#include after extern \"C\" recording decls"
      pattern: "assert_trace_eq"
    - from: "test_val_flash4.cpp golden test fns"
      to: "_shared/golden_trace.h assert_trace_eq"
      via: "#include after extern \"C\" recording decls"
      pattern: "assert_trace_eq"
---

<objective>
Pin byte-exact register traces for the flash3 (0x06) and flash4 (0x05) families — flash3 write (no chip-id; not a P4 site), and flash4 write plus its chip-id (P4 via flash_utils) — reusing the `golden_trace.h` helper from 88-01.

Purpose: flash3 exercises the AMD/SST unlock-sequence (P7) touchpoint and INV-09; flash4 exercises the 256-byte page write (P7 SDP, INV-04) and the flash_utils chip-id (P4) site. These complete the five-family golden-trace coverage (D-03), with flash4 the canonical D-04 minimal-input case (INV-04's 65-byte probe precedent).

Output: three committed `golden_*.inc` fixtures + golden test functions wired into the two suites.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-CONTEXT.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-01-SUMMARY.md
@firestarter/test/native/avr/_shared/golden_trace.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bless + pin flash3 0x06 write golden trace (AMD/SST unlock-sequence P7, INV-09)</name>
  <files>firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp, firestarter/test/native/avr/test_val_flash3/golden_flash3_write.inc</files>
  <read_first>
    - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp (whole file — extern "C" recording decls, setUp stubs, existing INV-09 assertion to keep, make_handle)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (lines 310-342 clear-then-drive idiom; lines 344-359 RUN_TEST list shape)
    - firestarter/test/native/avr/_shared/golden_trace.h (the helper from 88-01)
    - firestarter/src/proms/flash_type_3.cpp (the 0x06 unlock/write body; confirm delays hit, Pitfall 4)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (section "Golden write-test functions"; coverage map: flash3 has NO chip-id)
  </read_first>
  <behavior>
    - test_golden_flash3_write: CMD_WRITE handle, minimal representative data_size (D-04, sized so the AMD/SST unlock sequence + one program pulse fire under the 256 cap), configure_memory, clear_bus_recording, drive init then main, assert_trace_eq against golden_flash3_write (or print_trace_inc under GOLDEN_BLESS).
  </behavior>
  <action>
    Add `#include "_shared/golden_trace.h"` after the extern "C" recording decls. Add one golden write test reusing the suite's existing `setUp()`. flash3 is NOT one of the four P4 chip-id sites — author write only (D-03 coverage map). Minimal representative `data_size` under the cap (D-04). `clear_bus_recording()` AFTER `configure_memory`; drive `operation_init` then `operation_main`. Declare `static const golden_entry_t golden_flash3_write[] = { #include "golden_flash3_write.inc" };` (+ `_n`). Bless with `-D GOLDEN_BLESS`, redirect `print_trace_inc()`, remove flag. Header-comment the `.inc` with producing input + low-byte caveat (Pitfall 1). Add `RUN_TEST(test_golden_flash3_write)` ALONGSIDE the existing INV-09 test; remove nothing (SAFE-02).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_flash3*" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native -f "*test_val_flash3*"` exits 0.
    - `golden_flash3_write.inc` exists and committed; `grep -c "{ 0x" golden_flash3_write.inc` ≥ 1.
    - `grep -n "RUN_TEST(test_golden_flash3_write)" firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp` returns 1 match.
    - Existing INV-09 assertion still present (`grep -n "INV-09" test_val_flash3.cpp` matches).
    - No flash3 chip-id fixture authored (flash3 is not a P4 site): `ls firestarter/test/native/avr/test_val_flash3/golden_flash3_chip_id.inc` returns no such file.
  </acceptance_criteria>
  <done>flash3 0x06 write golden trace pinned, equality-asserted, wired in; INV-09 intact; no spurious chip-id fixture.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bless + pin flash4 0x05 write + chip-id golden traces (256B page P7 / INV-04 sizing, P4 via flash_utils)</name>
  <files>firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp, firestarter/test/native/avr/test_val_flash4/golden_flash4_write.inc, firestarter/test/native/avr/test_val_flash4/golden_flash4_chip_id.inc</files>
  <read_first>
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (lines 79-90 scan helper; lines 310-342 INV-04 65-byte probe + clear-then-drive; lines 344-359 RUN_TEST list; existing INV-04 assertion to keep)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (lines 170-195 — flash4 P4 chip-id via flash_utils, confirms non-NULL operation_main on CMD_CHECK_CHIP_ID)
    - firestarter/test/native/avr/test_eeprom28c_chip_id/test_eeprom28c_chip_id.cpp (lines 42-45, 106-109 — scripted-byte mock + reassign-AFTER-configure_memory idiom, Pitfall 3)
    - firestarter/test/native/avr/_shared/golden_trace.h (the helper from 88-01)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (sections "Golden write-test functions" and "Golden chip-id (P4) test functions"; flash4 specifics)
  </read_first>
  <behavior>
    - test_golden_flash4_write: CMD_WRITE handle with the INV-04 65-byte minimal probe (D-04, NOT a full 256-byte page — that blows the cap), configure_memory, clear_bus_recording, drive init then main, assert_trace_eq against golden_flash4_write.
    - test_golden_flash4_chip_id: CMD_CHECK_CHIP_ID handle with non-zero h.chip_id, configure_memory, re-assign firestarter_get_data scripted mock AFTER configure_memory (Pitfall 3), clear_bus_recording, drive main (flash_util_check_chip_id_execute), assert_trace_eq against golden_flash4_chip_id.
  </behavior>
  <action>
    Add `#include "_shared/golden_trace.h"` after the extern "C" recording decls. Add two golden test functions reusing the suite's existing `setUp()`. For the write, use the 65-byte minimal probe (the INV-04 precedent at lines 310-342) — a full 256-byte page write would exceed the 256-entry recording cap (Pitfall 2 / D-04); the `assert_trace_eq` truncation guard will catch any oversizing. `clear_bus_recording()` AFTER `configure_memory`; drive `operation_init` then `operation_main`. For chip-id, build `CMD_CHECK_CHIP_ID` + non-zero `h.chip_id` and re-assign `h.firestarter_get_data` to a scripted-byte mock AFTER `configure_memory` (Pitfall 3). Declare `static const golden_entry_t golden_flash4_write[]` and `golden_flash4_chip_id[]` via the two `.inc` includes (+ `_n` each). Bless with `-D GOLDEN_BLESS`, redirect `print_trace_inc()` into each `.inc`, remove flag. Header-comment each `.inc` with the producing input (note the 65-byte probe for write) + low-byte caveat. Add `RUN_TEST(...)` for both ALONGSIDE the existing INV-04 test; remove nothing (SAFE-02).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_flash4*" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native -f "*test_val_flash4*"` exits 0.
    - `golden_flash4_write.inc` and `golden_flash4_chip_id.inc` exist and committed; `grep -c "{ 0x" <file>` ≥ 1 each.
    - `grep -n "RUN_TEST(test_golden_flash4" firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp` returns 2 matches.
    - Existing INV-04 assertion still present (`grep -n "INV-04" test_val_flash4.cpp` matches).
    - `golden_flash4_write.inc` header comment names the 65-byte probe input (`grep -n "65" golden_flash4_write.inc` matches).
  </acceptance_criteria>
  <done>flash4 0x05 write (65-byte probe) + chip-id golden traces pinned, equality-asserted, wired in; INV-04 intact; both fixtures under the 256 cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware handler → RURP bus | flash3 AMD/SST unlock + flash4 256B-page SDP sequences drive control lines |
| Phase 89 refactor → golden oracle | P7 SDP dedup + P4 chip-id extraction must keep these traces byte-exact (or be re-blessed under review, D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-07 | Tampering (physical) | flash3 0x06 / flash4 0x05 SDP unlock (P7) register sequence | mitigate | Byte-exact golden trace trips on any SDP/unlock-order drift during Phase-89 P7 dedup |
| T-88-08 | Spoofing (false-green) | flash4 256-byte page exceeding the recording cap | mitigate | 65-byte INV-04 minimal probe (D-04) + assert_trace_eq `bus_recording_count() < 256` guard fail loud on oversizing (Pitfall 2) |
| T-88-09 | Tampering (physical) | flash4 chip-id (P4 via flash_utils) register sequence | mitigate | Byte-exact golden trace pins the P4 compare/report path Phase 89 extracts to a shared primitive |
</threat_model>

<verification>
- `cd firestarter && pio test -e native -f "*test_val_flash3*"` and `*test_val_flash4*` each exit 0.
- No production firmware source modified; full-suite + flash gate run in 88-05.
</verification>

<success_criteria>
- flash3 (0x06) write and flash4 (0x05) write + chip-id golden fixtures committed and equality-asserted.
- flash4 write fixture under the 256-entry cap; all prior INV assertions (INV-04, INV-09) remain green (SAFE-02).
</success_criteria>

<output>
Create `.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-03-SUMMARY.md` when done
</output>
