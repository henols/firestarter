---
phase: 88-golden-traces-dispatch-mirror-guard-was-87
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_dispatch_mirror.py
autonomous: true
requirements: [PRIM-01, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "A host pytest parses the PROTOCOLS.md §0 table into {hex: handler} and binds it to check_dispatch.dispatch() across the FULL dispatch table (D-05/D-06)"
    - "The mirror covers SRAM 0x0E/0x27/0x28/0x29 and 0x34→not_implemented; phantom 0x35/0x39 route to not_implemented (not in §0)"
    - "The mirror makes the firmware leg explicit by asserting the native test_dispatch suite enumerates every §0 protocol"
    - "pytest tests/test_dispatch_mirror.py passes and is ruff-clean against the py3.11 CI target (SAFE-01: dispatch keyed on protocol, WARNING-5 buckets stay green)"
  artifacts:
    - path: "firestarter_app/tests/test_dispatch_mirror.py"
      provides: "three-way dispatch-mirror bind: PROTOCOLS.md §0 ↔ check_dispatch.dispatch() ↔ native test_dispatch enumeration"
      contains: "def test_dispatch_mirror"
  key_links:
    - from: "firestarter_app/tests/test_dispatch_mirror.py"
      to: "firestarter_app/tools/check_dispatch.py dispatch()"
      via: "from tools import check_dispatch (import, never re-implement)"
      pattern: "check_dispatch"
    - from: "firestarter_app/tests/test_dispatch_mirror.py"
      to: "firestarter/doc/PROTOCOLS.md §0 table"
      via: "regex parse of the pipe table (handler in backticks col 3)"
      pattern: "PROTOCOLS"
    - from: "firestarter_app/tests/test_dispatch_mirror.py"
      to: "firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp"
      via: "grep firmware-leg test enumeration for each §0 protocol"
      pattern: "test_configure_memory"
---

<objective>
Author the host-side dispatch-mirror invariant test — the three-way bind that proves the protocol→handler dispatch order agrees across (1) the PROTOCOLS.md §0 table (doc), (2) `check_dispatch.dispatch()` + `_ALGO_MEM_TYPE` (tool), and (3) the native `test_dispatch/test_configure_memory.cpp` arms (firmware leg).

Purpose: SAFE-02's structural backstop. A recompose-time dispatch drift in ANY of the three representations trips this test immediately, so Phase 89 can refactor handler internals while the dispatch order stays pinned (D-05/D-06). This also covers SAFE-01 (dispatch keyed on `handle->protocol`, WARNING-5 buckets) via the existing check_dispatch guards it binds.

Output: `firestarter_app/tests/test_dispatch_mirror.py`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-CONTEXT.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author test_dispatch_mirror.py binding PROTOCOLS.md §0 ↔ check_dispatch.dispatch()</name>
  <files>firestarter_app/tests/test_dispatch_mirror.py</files>
  <read_first>
    - firestarter_app/tests/test_check_dispatch_invariants.py (lines 1-43 — the import seam `from tools.check_dispatch import ...`, `_FA_DIR = Path(__file__).parent.parent` path discipline, docstring style)
    - firestarter_app/tools/check_dispatch.py (lines 36-50 `_ALGO_MEM_TYPE`; lines 117-130 `KNOWN_PROTOCOLS`; lines 133-157 `dispatch(protocol, mem_type)` — import and call, NEVER re-implement)
    - firestarter/doc/PROTOCOLS.md (lines 20-36 — the §0 pipe table: `| hex | DB chip count | handler | datasheets folder | algorithm-axis name |`, handler in backticks col 3; lines 325-328 the phantom 0x35/0x39 §2 note)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (section "firestarter_app/tests/test_dispatch_mirror.py" — exact parse regex, DOC_FILE_TO_FUNC map, and the assertion shape)
  </read_first>
  <behavior>
    - parse_protocols_md(): returns {hex_int: "<handler>.cpp"} for every §0 row (12 rows incl. SRAM 0x0E/0x27/0x28/0x29 and 0x34→not_implemented.cpp).
    - test_dispatch_mirror_doc_matches_tool: for each §0 hex, look up mem_type via `check_dispatch._ALGO_MEM_TYPE.get(hex, 0)`, call `check_dispatch.dispatch(hex, mt)`, assert it equals `DOC_FILE_TO_FUNC[handler_file]`; assertion message names the hex + both sides.
    - test covers the full table (D-06), not just the five recompose families; phantom 0x35/0x39 are NOT in §0 so they are skipped from doc-parse and routed via the not_implemented rule.
  </behavior>
  <action>
    Create `firestarter_app/tests/test_dispatch_mirror.py`. Use the `_FA_DIR = pathlib.Path(__file__).parent.parent` discipline; resolve the doc as `_FA_DIR.parent / "firestarter" / "doc" / "PROTOCOLS.md"` (sub-repo sibling). Import the tool leg via `from tools import check_dispatch` — import `dispatch`, `_ALGO_MEM_TYPE`; NEVER re-implement dispatch (Don't-Hand-Roll). Parse the §0 table with a stdlib `re` regex matching `^\|\s*0x([0-9A-Fa-f]+)\s*\|[^|]*\|\s*` + backtick-wrapped handler `.cpp` filename in column 3 (no markdown lib). Map doc handler-file → dispatch function name with `DOC_FILE_TO_FUNC` (flash_type_4.cpp→configure_flash4, flash_type_3.cpp→configure_flash3, eprom.cpp→configure_eprom, eeprom_28c.cpp→configure_eeprom28c, flash_intel.cpp→configure_flash_intel, sram.cpp→configure_sram, not_implemented.cpp→not_implemented). Assert agreement for every parsed hex. Skip 0x35/0x39 (phantom, not in §0). Avoid f-string-backslash constructs (Pitfall 5). No fenced code in this action; use the PATTERNS analog for exact regex + map.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py -q` exits 0.
    - `grep -n "from tools import check_dispatch" firestarter_app/tests/test_dispatch_mirror.py` matches (tool leg imported, not re-implemented).
    - `grep -n "def parse_protocols_md" firestarter_app/tests/test_dispatch_mirror.py` matches and the test references `check_dispatch.dispatch`.
    - The test asserts agreement for the full table incl. 0x0E/0x27/0x28/0x29 and 0x34 (not just the five families).
    - `cd firestarter_app && ruff check tests/test_dispatch_mirror.py && ruff format --check tests/test_dispatch_mirror.py` exits 0 (py3.11 CI gate, Pitfall 5).
  </acceptance_criteria>
  <done>The doc↔tool bind passes; the full dispatch table is mirror-checked; the file is ruff-clean for CI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the firmware-leg binding — assert native test_dispatch enumerates every §0 protocol</name>
  <files>firestarter_app/tests/test_dispatch_mirror.py</files>
  <read_first>
    - firestarter_app/tests/test_dispatch_mirror.py (the file from Task 1)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (whole file — the per-protocol routing tests `test_protocol_0xNN_*` / flash4 chip-id tests that anchor the firmware arms)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-PATTERNS.md (section "firestarter_app/tests/test_dispatch_mirror.py" — the optional explicit firmware-leg drift-trip)
  </read_first>
  <behavior>
    - test_dispatch_mirror_firmware_leg_enumerates_all_protocols: read test_configure_memory.cpp text, extract the protocol hexes it enumerates (the `0xNN` tokens in its per-protocol test function names / dispatch calls), and assert every §0 protocol that routes to a real handler is enumerated there — so dropping a firmware routing test for a protocol trips this.
  </behavior>
  <action>
    Add a third test function to `test_dispatch_mirror.py` that locates the native anchor at `_FA_DIR.parent / "firestarter" / "test" / "native" / "avr" / "test_dispatch" / "test_configure_memory.cpp"`, reads it, regex-extracts the `0x[0-9A-Fa-f]+` protocol tokens it references, and asserts the set covers every §0 protocol that maps to a non-`not_implemented` handler (use parse_protocols_md() from Task 1 as the source of the §0 set). Assertion message lists any §0 protocol missing a firmware routing test. This makes the firmware leg of the three-way bind explicit and drift-tripping (D-05) without modifying any firmware file. Keep it ruff-clean; pure stdlib `re`/`pathlib`.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py -q` exits 0 with both/all test functions collected (`-q` summary shows ≥ 2 passed).
    - `grep -n "test_configure_memory.cpp" firestarter_app/tests/test_dispatch_mirror.py` matches (firmware-leg anchor referenced).
    - `grep -n "firmware_leg" firestarter_app/tests/test_dispatch_mirror.py` matches.
    - No firmware file modified by this plan (`git -C firestarter status --porcelain test/native/avr/test_dispatch/` shows no change).
    - `cd firestarter_app && ruff check tests/test_dispatch_mirror.py && ruff format --check tests/test_dispatch_mirror.py` exits 0.
  </acceptance_criteria>
  <done>All three dispatch representations (doc, tool, firmware-leg enumeration) are bound in one host pytest; drift in any trips it; firmware unmodified; ruff-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol → handler dispatch | A mis-dispatch routes a 5V part to a 12V-VPP handler (eprom) → over-voltage on a 5V pin |
| three dispatch representations | doc, host tool, and firmware can silently diverge during the recompose |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-10 | Tampering / DoS (physical) | mis-dispatch routes 5V part to configure_eprom (12V VPP) | mitigate | Dispatch-mirror binds doc↔tool↔firmware over the FULL table; BLOCKER-2/WARNING-5/GATE-03 guards in check_dispatch (imported, verified green in 88-05) + dispatch keyed on `handle->protocol` (SAFE-01) |
| T-88-11 | Spoofing (silent drift) | one of three dispatch representations changes alone | mitigate | Three-way bind: doc-parse vs dispatch() (Task 1) + firmware-leg enumeration assertion (Task 2) — any single-representation drift trips the test |
| T-88-12 | Elevation (physical) | phantom 0x35/0x39 reaching a real handler | accept | Not in §0; routed to not_implemented per host rule, matching check_dispatch's KNOWN_PROTOCOLS exclusion |
</threat_model>

<verification>
- `cd firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py -q` exits 0.
- File is ruff-clean / format-clean against the py3.11 CI target.
- No firmware or production host source modified (test-only addition).
</verification>

<success_criteria>
- A single host pytest binds all three dispatch representations (D-05) over the full table (D-06).
- The test passes and is CI-clean (Pitfall 5).
</success_criteria>

<output>
Create `.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-04-SUMMARY.md` when done
</output>
