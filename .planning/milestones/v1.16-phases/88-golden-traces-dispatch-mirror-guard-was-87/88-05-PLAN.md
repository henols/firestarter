---
phase: 88-golden-traces-dispatch-mirror-guard-was-87
plan: 05
type: execute
wave: 2
depends_on: [88-01, 88-02, 88-03, 88-04]
files_modified:
  - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-FROZEN-WORLD.md
autonomous: true
requirements: [SAFE-04, SAFE-01]
must_haves:
  truths:
    - "check_dispatch.py exits 0 violations and diff_db.py reports empty against the Phase-86-repinned baseline — no DB record changed this phase (D-07)"
    - "pio run -e leonardo shows near-zero flash delta vs the 25654 B baseline — all new code compiles only into [env:native] (D-08)"
    - "The firmware over-voltage VPP check is present and unmodified at eprom.cpp:282 and flash_intel.cpp:65 (SC#4 / D-09)"
    - "The host chip_resolver.resolve_chip guard is present and unmodified at chip_resolver.py:55 (SC#4 / SAFE-01 / D-09)"
    - "The full native suite (incl. all new golden traces + existing INV tests) is green"
    - "2516 stays UNVERIFIED — diff_db empty keeps it unmoved (D-09)"
  artifacts:
    - path: ".planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-FROZEN-WORLD.md"
      provides: "captured evidence of all frozen-world gate + safety-posture results"
      contains: "check_dispatch"
  key_links:
    - from: "88-FROZEN-WORLD.md evidence"
      to: "firestarter_app/tools/check_dispatch.py + diff_db.py"
      via: "rerun gates, capture exit 0 / empty"
      pattern: "0 violations|empty"
    - from: "88-FROZEN-WORLD.md evidence"
      to: "firestarter/src/proms/eprom.cpp:282 + flash_intel.cpp:65 + chip_resolver.py:55"
      via: "grep present-and-unmodified"
      pattern: "vpp_mv >|support_status"
---

<objective>
Rerun the frozen-world gates and verify the SC#4 safety posture is unchanged after the golden-trace and dispatch-mirror work landed — proving this phase changed no DB record, no production firmware, no flash size, and no safety guard.

Purpose: SAFE-04 + the milestone safety model. This is the phase's "prove nothing moved" closeout (D-07/D-08/D-09). All gates and greps are VERIFY-ONLY — assert present/unmodified, edit nothing. 2516 stays UNVERIFIED via diff_db-empty.

Output: a captured evidence record `88-FROZEN-WORLD.md` and a green full native suite. (This is the only NON-firmware, NON-host-code plan — it touches only the planning evidence file.)
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-CONTEXT.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-01-SUMMARY.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-02-SUMMARY.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-03-SUMMARY.md
@.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rerun frozen-world gates — full native suite, check_dispatch (0 violations), diff_db (empty), Leonardo flash delta</name>
  <files>.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-FROZEN-WORLD.md</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (lines 1-50 — invocation + the 0-violations gate semantics; do NOT modify)
    - firestarter_app/tools/diff_db.py (lines 1-50 — invocation + empty-diff semantics; do NOT modify)
    - firestarter_app/tools/baseline/chip_database.baseline.json (the Phase-86-repinned 746-chip baseline diff_db compares against — must stay UNCHANGED, D-07)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md (section "Validation Architecture" — exact gate commands + the 25654 B Leonardo flash baseline, D-08)
  </read_first>
  <behavior>
    - Full native suite passes (all five golden suites + existing INV tests + dispatch anchor).
    - check_dispatch.py exits 0 with "0 violations"; diff_db.py reports empty ("0 changed chips").
    - pio run -e leonardo Flash bytes near 25654 (near-zero delta vs baseline) — tests are [env:native]-only.
  </behavior>
  <action>
    Run, in order, and capture each output into `88-FROZEN-WORLD.md`: (1) `cd firestarter && pio test -e native` — full native suite green; (2) `cd firestarter_app && python3 tools/check_dispatch.py` — assert exit 0 + "0 violations"/PASS; (3) `cd firestarter_app && python3 tools/diff_db.py` — assert exit 0 + empty/"0 changed chips"; (4) `cd firestarter && pio run -e leonardo` — capture the `Flash: NNNNN bytes` line and confirm it equals 25654 (or document any delta as near-zero with the cause). Do NOT modify check_dispatch.py, diff_db.py, the baselines, or any firmware/host source — these are VERIFY-ONLY (D-07/D-08). If diff_db is non-empty or flash delta is non-trivial, STOP and report (it indicates an accidental DB/firmware change in a prior plan that must be reverted, not blessed). Record the captured outputs (command + result) in `88-FROZEN-WORLD.md`.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native 2>&1 | tail -5 && cd /workspaces/firestarter_app && python3 tools/check_dispatch.py 2>&1 | tail -3 && python3 tools/diff_db.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` exits 0 (full native suite green, all golden traces + INV tests).
    - `cd firestarter_app && python3 tools/check_dispatch.py` exits 0 and output contains 0 violations / PASS.
    - `cd firestarter_app && python3 tools/diff_db.py` exits 0 and reports empty (e.g. "0 changed chips").
    - `cd firestarter && pio run -e leonardo` succeeds and the captured `Flash:` byte count is 25654 (or a documented near-zero delta with cause).
    - `git -C firestarter_app status --porcelain tools/ tools/baseline/` shows NO change (gates + baselines unmodified, D-07).
    - `88-FROZEN-WORLD.md` records each command + its result.
  </acceptance_criteria>
  <done>All four frozen-world gates pass with no DB/firmware/flash change; evidence captured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify SC#4 safety posture present + unmodified — over-voltage check, resolve_chip guard, 2516 UNVERIFIED</name>
  <files>.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-FROZEN-WORLD.md</files>
  <read_first>
    - firestarter/src/proms/eprom.cpp (line 282 — the over-voltage check; VERIFY-ONLY)
    - firestarter/src/proms/flash_intel.cpp (line 65 — the same over-voltage check; VERIFY-ONLY)
    - firestarter_app/firestarter/chip_resolver.py (line 55 — the support_status host guard; VERIFY-ONLY)
    - .planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-RESEARCH.md (section "Security Domain" — the threat to mitigation map this task verifies + the 2516 UNVERIFIED disposition, D-09)
  </read_first>
  <behavior>
    - The over-voltage VPP check is present at eprom.cpp:282 and flash_intel.cpp:65 and unmodified.
    - The resolve_chip host guard is present at chip_resolver.py:55 and unmodified.
    - 2516 remains UNVERIFIED (diff_db empty from Task 1 keeps it unmoved — no write-graduation).
  </behavior>
  <action>
    Run the safety-posture greps and append results to `88-FROZEN-WORLD.md`: (1) grep the over-voltage pattern `vpp_mv > (uint32_t)handle->vpp_mv + 500` in `firestarter/src/proms/eprom.cpp` and `firestarter/src/proms/flash_intel.cpp` — assert one match in each file at lines 282 / 65; (2) grep the host guard pattern `support_status != "supported"` in `firestarter_app/firestarter/chip_resolver.py` — assert one match at line 55; (3) confirm `git -C firestarter status --porcelain src/proms/eprom.cpp src/proms/flash_intel.cpp` and `git -C firestarter_app status --porcelain firestarter/chip_resolver.py` show NO change (present AND unmodified, D-09); (4) confirm the 2516 stays UNVERIFIED — diff_db empty (from Task 1) means no record moved; grep the DB / verification_status for 2516 to confirm it is not graduated. These are VERIFY-ONLY — edit none of the source files. Record each grep + git-status result in `88-FROZEN-WORLD.md`.
  </action>
  <verify>
    <automated>grep -n 'vpp_mv > (uint32_t)handle->vpp_mv + 500' firestarter/src/proms/eprom.cpp firestarter/src/proms/flash_intel.cpp; grep -n 'support_status != "supported"' firestarter_app/firestarter/chip_resolver.py</automated>
  </verify>
  <acceptance_criteria>
    - grep of the over-voltage pattern matches at `eprom.cpp:282` and `flash_intel.cpp:65` (one each).
    - grep of the support_status pattern matches at `chip_resolver.py:55`.
    - `git -C firestarter status --porcelain src/proms/eprom.cpp src/proms/flash_intel.cpp` shows NO change; `git -C firestarter_app status --porcelain firestarter/chip_resolver.py` shows NO change (present AND unmodified, D-09).
    - 2516 remains UNVERIFIED (diff_db empty from Task 1; the 2516 verification_status is not graduated to supported/write-proven).
    - `88-FROZEN-WORLD.md` records each grep + git-status result.
  </acceptance_criteria>
  <done>The over-voltage check and resolve_chip guard are present and unmodified; 2516 stays UNVERIFIED; evidence captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware VPP gate | over-voltage beyond the chip's vpp_mv setpoint can damage hardware |
| host resolve_chip | non-supported chips must be refused before any wire byte |
| DB baseline | a silent DB record move could write-graduate an unverified part (2516) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-13 | Tampering (physical) | over-voltage VPP beyond setpoint | mitigate | Verify `eprom.cpp:282` / `flash_intel.cpp:65` over-voltage check present + unmodified (errors when vpp_mv > setpoint + 500 unless FLAG_FORCE); SC#4 |
| T-88-14 | Elevation (physical) | non-supported chip reaching a real handler | mitigate | Verify `chip_resolver.resolve_chip` guard present + unmodified at `chip_resolver.py:55` (raises for any non-supported support_status before any wire byte) — authoritative host guard, SAFE-01 |
| T-88-15 | DoS (physical) | writing an irreplaceable UV part on unstable read path (2516) | mitigate | diff_db empty (Task 1) keeps the 2516 unmoved; it stays UNVERIFIED, not write-graduated (D-09) |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (88-RESEARCH Package Legitimacy Audit: N/A — in-repo test code + existing stdlib tools only) |
</threat_model>

<verification>
- Full native suite green; check_dispatch 0 violations; diff_db empty; Leonardo flash near 25654 B.
- Over-voltage check + resolve_chip guard present and unmodified; 2516 UNVERIFIED.
- No source file (firmware or host) modified by this plan — only the evidence record.
</verification>

<success_criteria>
- All four ROADMAP success criteria's frozen-world half is proven: 0-violations / empty-diff / near-zero flash / safety guards present + unmodified (SAFE-04 / D-07/D-08/D-09).
- 2516 stays UNVERIFIED.
</success_criteria>

<output>
Create `.planning/phases/88-golden-traces-dispatch-mirror-guard-was-87/88-05-SUMMARY.md` when done
</output>
