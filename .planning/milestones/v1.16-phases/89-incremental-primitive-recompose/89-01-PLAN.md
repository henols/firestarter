---
phase: 89-incremental-primitive-recompose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/include/flash_utils.h
  - firestarter/src/proms/eeprom_28c.cpp
autonomous: true
requirements: [PRIM-02, PRIM-06, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "The duplicate FLASH_ENABLE_WRITE_PROTECTION const table is removed from flash_utils.h"
    - "The duplicate EEPROM_SDP_DISABLE table is removed from eeprom_28c.cpp; its one caller redirects to FLASH_DISABLE_WRITE_PROTECTION"
    - "test_val_eeprom28c + test_val_flash3 + test_val_flash4 golden traces stay byte-identical (zero-diff)"
    - "Leonardo flash does not rise more than +16 B vs the 25654 B Phase-88 baseline (D-01 per-step gate)"
    - "check_dispatch.py exits 0 violations and diff_db.py is empty (no DB/dispatch change)"
    - "All 9 INV-01..09 ids still hit >=3 files; the WARNING-5 structural guards survive"
  artifacts:
    - path: "firestarter/include/flash_utils.h"
      provides: "Single canonical FLASH_ENABLE_WRITE / FLASH_DISABLE_WRITE_PROTECTION tables; dead duplicate deleted"
      contains: "FLASH_DISABLE_WRITE_PROTECTION"
    - path: "firestarter/src/proms/eeprom_28c.cpp"
      provides: "SDP-disable call redirected to the shared flash_utils table"
      contains: "FLASH_DISABLE_WRITE_PROTECTION"
  key_links:
    - from: "firestarter/src/proms/eeprom_28c.cpp"
      to: "firestarter/include/flash_utils.h"
      via: "flash_execute_command(FLASH_DISABLE_WRITE_PROTECTION)"
      pattern: "FLASH_DISABLE_WRITE_PROTECTION"
---

<objective>
Extract P7 — the SDP / const-table dedup warm-up (PRIM-02). Delete the dead, callerless duplicate
`FLASH_ENABLE_WRITE_PROTECTION` table from `flash_utils.h`, and delete the byte-identical local
`EEPROM_SDP_DISABLE` table from `eeprom_28c.cpp`, redirecting its single caller to the existing
`FLASH_DISABLE_WRITE_PROTECTION`. This is delete-not-merge: the tables are verified byte-identical,
so the golden traces must stay zero-diff. P7 is the lowest-risk extraction and front-loads the flash
headroom that P4/P3/P5 spend down (D-09 order: P7 first).

Purpose: First extraction step in the P7→P4→P3→P5 chain; removes dead duplicated PROGMEM data and
establishes the per-step refactor-under-test gate cadence for the phase.
Output: One atomic commit (P7) in the `firestarter` sub-repo; a logged Leonardo flash delta.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/89-incremental-primitive-recompose/89-CONTEXT.md
@.planning/phases/89-incremental-primitive-recompose/89-RESEARCH.md
@.planning/phases/89-incremental-primitive-recompose/89-PATTERNS.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead FLASH_ENABLE_WRITE_PROTECTION + redirect EEPROM_SDP_DISABLE</name>
  <files>firestarter/include/flash_utils.h, firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/include/flash_utils.h:42-60 — the three const tables: FLASH_ENABLE_WRITE (42-46), the dead duplicate FLASH_ENABLE_WRITE_PROTECTION (48-52, byte-identical to FLASH_ENABLE_WRITE), FLASH_DISABLE_WRITE_PROTECTION (53-60).
    - firestarter/src/proms/eeprom_28c.cpp:47-54 — local EEPROM_SDP_DISABLE table (byte-identical to FLASH_DISABLE_WRITE_PROTECTION); line 130 — the single caller `flash_execute_command(EEPROM_SDP_DISABLE)`; confirm `#include "flash_utils.h"` near the top is already present (table is in scope).
    - firestarter/test/native/avr/test_val_eeprom28c/ — golden write trace fixture (golden_eeprom28c_write.inc) that gates this edit; test_val_flash3/ + test_val_flash4/ goldens use FLASH_ENABLE_WRITE and must also stay green.
    - 89-PATTERNS.md §P7 (lines 262-287) and 89-RESEARCH.md Pitfall 1 (lines 239-244) — the verified byte-identity claims + the delete-not-merge instruction.
  </read_first>
  <action>
    First RE-VERIFY the two byte-identity / dead-code claims at execution time (Assumption A1):
    run `grep -rn FLASH_ENABLE_WRITE_PROTECTION firestarter/src/ firestarter/include/` and confirm
    the ONLY hit is the definition in flash_utils.h (no callers). If a caller exists, redirect it to
    `FLASH_ENABLE_WRITE` (byte-identical) instead of deleting — do not strand a reference.
    Confirm `EEPROM_SDP_DISABLE` in eeprom_28c.cpp is byte-identical to `FLASH_DISABLE_WRITE_PROTECTION`
    in flash_utils.h (both the 6-write `{0x5555,0x80}...{0x5555,0x20}` sequence per PATTERNS §P7).

    Edit 1 (flash_utils.h): delete the `FLASH_ENABLE_WRITE_PROTECTION` const table block (lines ~48-52).
    Leave `FLASH_ENABLE_WRITE` and `FLASH_DISABLE_WRITE_PROTECTION` untouched.

    Edit 2 (eeprom_28c.cpp): delete the local `EEPROM_SDP_DISABLE` const table (lines ~47-54), and change
    its single caller (line ~130) from `flash_execute_command(EEPROM_SDP_DISABLE)` to
    `flash_execute_command(FLASH_DISABLE_WRITE_PROTECTION)`. eeprom_28c.cpp already includes flash_utils.h.

    Do NOT touch any other table, any wire/constant value, any dispatch, or any handler logic (SAFE-06/D-09).
    This is delete + one-line redirect only. Commit as a single atomic P7 commit inside the firestarter
    submodule on the v1.16 branch. If after deletion the eeprom28c golden trace is NOT zero-diff, the tables
    were not identical — STOP, inspect, and per D-02 revert P7 cleanly and document the deferral (do NOT
    re-bless to mask a diff).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_eeprom28c*" -f "*test_val_flash3*" -f "*test_val_flash4*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn FLASH_ENABLE_WRITE_PROTECTION firestarter/src/ firestarter/include/` returns ZERO matches (table deleted, no dangling reference).
    - `grep -rn EEPROM_SDP_DISABLE firestarter/src/ firestarter/include/` returns ZERO matches (local table deleted + caller redirected).
    - `grep -c 'FLASH_DISABLE_WRITE_PROTECTION' firestarter/src/proms/eeprom_28c.cpp` >= 1 (caller now uses the shared table).
    - `cd firestarter && pio test -e native -f "*test_val_eeprom28c*" -f "*test_val_flash3*" -f "*test_val_flash4*"` — all suites green, golden traces byte-identical (zero-diff). A trace diff is a STOP-and-inspect (D-04), not an auto-rebless.
  </acceptance_criteria>
  <done>The dead FLASH_ENABLE_WRITE_PROTECTION table is gone; eeprom_28c.cpp uses the shared FLASH_DISABLE_WRITE_PROTECTION; eeprom28c/flash3/flash4 golden traces stay byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Per-step P7 gate — full native suite + flash delta + frozen-world host gates + INV grep</name>
  <files>firestarter/.flash-baseline-87.txt (read-only)</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 284-323) — the exact Phase 87-04 `DELTA<=16` flash-delta recipe (parse `Flash: ... used N bytes`), the per-step oracle rerun commands, and the read-only host-gate commands.
    - firestarter/.flash-baseline-87.txt — the 25654 B baseline.
    - firestarter_app/tools/check_dispatch.py + firestarter_app/tools/diff_db.py — the frozen-world gates (run read-only; py-version-insensitive pure-stdlib).
    - firestarter/doc/PROTOCOLS.md §3 — the INV-01..09 traceability matrix (the SAFE-02 greppability targets).
  </read_first>
  <action>
    Run the full per-step gate for P7 and record the result in the SUMMARY (this gate is identical for
    every primitive plan in this phase):
    1. Full native suite: `cd firestarter && pio test -e native` — ALL suites green (byte-exact golden + INV asserts; D-07/SAFE-02). Note: `test_flash_intel_vpp` + `test_eeprom28c_chip_id` are pre-existing suite-level flaky (excluded from test_filter); a teardown SIGABRT there is NOT a P7 regression (RESEARCH Pitfall 6) — rely on the in-filter `test_val_*` suites.
    2. Flash delta (D-01): `cd firestarter && pio run -e leonardo`, parse the `Flash: ... used N bytes` line, compute `POST - 25654`, assert `<= 16`. Log the delta (P7 may legitimately be ~0 or negative; it cannot fail the +16 gate — Open Question 1 / Assumption A2).
    3. Host frozen-world gates (D-05/SAFE-03): `cd firestarter_app && python tools/check_dispatch.py` (expect exit 0, "0 dispatch regressions, 0 consistency violations") and `python tools/diff_db.py` (expect exit 0, "0 changed / 0 new / 0 missing").
    4. SAFE-06 host-frozen check: `git -C firestarter_app diff --quiet` (exit 0 — no host source change; gates are read-only).
    5. INV greppability (SAFE-02): for each INV-01..INV-09, `grep -rln "INV-0N" firestarter/doc/ firestarter/src/ firestarter/test/` still hits >= 3 files.
    Record the measured pre/post flash bytes + delta in the step ledger for the phase-close PRIM-06 report (89-05).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e leonardo 2>&1 | grep -oE 'Flash:.*used [0-9]+ bytes'</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` — full suite green (in-filter suites; flaky-suite teardown ignored per RESEARCH Pitfall 6).
    - `pio run -e leonardo` Flash bytes parsed; `(POST - 25654) <= 16` (D-01 per-step gate). Delta logged.
    - `cd firestarter_app && python tools/check_dispatch.py` exits 0; `python tools/diff_db.py` exits 0.
    - `git -C firestarter_app diff --quiet` exits 0 (SAFE-06 host untouched).
    - Each of INV-01..INV-09 still resolves to >= 3 files via `grep -rln` across doc/ src/ test/ (SAFE-02).
  </acceptance_criteria>
  <done>Full native suite green, flash delta <= +16 logged, both host gates exit 0, host repo untouched, all 9 INV ids still >= 3 files — P7 commit is kept; or, on any gate failure, P7 is reverted cleanly and deferred with a documented FUT/CR reason (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON command → firmware handler | protocol_id flows from DB → wire → handler; dispatch must stay frozen |
| firmware logic → RURP control register | VPP/regulator bits drive physical voltage onto the socket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/DoS | 12V/25V VPP regulator on a 5V part | mitigate | P7 touches ONLY const data tables (SDP byte sequences), never regulator routing; check_dispatch.py SRAM-never-reaches-eprom guard + protocol-keyed routing (D-06) re-run and stay 0-violations. |
| T-89-02 | Tampering | Over-voltage past VPP window | accept (untouched) | P7 does not touch eprom.cpp:282 / flash_intel.cpp:65 over-voltage check; verified unmodified at phase close (89-05). |
| T-89-03 | Elevation | Host support-status guard bypass | accept (untouched) | No host source change (SAFE-06); `git -C firestarter_app diff --quiet` proves it; chip_resolver.resolve_chip unchanged. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (SAFE-05); only new artifact across the phase is primitives.{cpp,h} (89-02). N/A here. |
</threat_model>

<verification>
- `cd firestarter && pio test -e native` — full suite green (byte-exact golden traces unchanged).
- `pio run -e leonardo` flash delta <= +16 B vs 25654 (D-01).
- `check_dispatch.py` exit 0 + `diff_db.py` exit 0 (D-05/SAFE-03).
- `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
- INV-01..09 each >= 3 files (SAFE-02).
</verification>

<success_criteria>
P7 deduplication committed (or cleanly deferred per D-02): both duplicate tables removed, single caller
redirected, all golden traces byte-identical, flash delta <= +16 B logged, frozen-world gates green,
host repo untouched. (ROADMAP SC#1.)
</success_criteria>

<output>
Create `.planning/phases/89-incremental-primitive-recompose/89-01-SUMMARY.md` when done. Record the
measured pre/post Leonardo flash bytes + delta for the phase-close PRIM-06 report.
</output>
