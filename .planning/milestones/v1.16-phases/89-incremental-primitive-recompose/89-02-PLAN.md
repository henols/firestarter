---
phase: 89-incremental-primitive-recompose
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - firestarter/include/primitives.h
  - firestarter/src/proms/primitives.cpp
  - firestarter/src/proms/eprom.cpp
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/src/proms/flash_intel.cpp
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/src/proms/flash_utils.cpp
autonomous: true
requirements: [PRIM-03, PRIM-06, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "A new firestarter/include/primitives.h + firestarter/src/proms/primitives.cpp module exists (D-03)"
    - "A shared chip_id_report primitive handles compare + _b[4] pack + FORCE downgrade for all four call sites (eprom, flash_intel, eeprom28c, flash4 via flash_utils)"
    - "Each handler keeps its protocol-specific chip-id READ mechanism local; only the report tail is shared"
    - "test_val_eprom + test_val_flash_intel + test_val_eeprom28c + test_val_flash4 chip-id golden traces stay byte-identical"
    - "Leonardo flash does not rise more than +16 B vs the prior (P7) step (D-01); cumulative trend toward net-decrease"
    - "check_dispatch.py exits 0 violations, diff_db.py is empty, host repo untouched; all 9 INV ids >= 3 files"
  artifacts:
    - path: "firestarter/include/primitives.h"
      provides: "Declarations-only header for cross-family primitives (eprom.h-style guard + extern C)"
      contains: "chip_id_report"
    - path: "firestarter/src/proms/primitives.cpp"
      provides: "chip_id_report shared report-tail body (compare + _b[4] + FORCE downgrade)"
      contains: "chip_id_report"
  key_links:
    - from: "firestarter/src/proms/eprom.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "chip_id_report(handle, read_id) after protocol-specific read"
      pattern: "chip_id_report"
    - from: "firestarter/src/proms/flash_intel.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "chip_id_report(handle, read_id)"
      pattern: "chip_id_report"
---

<objective>
Extract P4 — the chip-ID compare/report primitive (PRIM-03). Create the NEW dedicated primitives
module (`firestarter/include/primitives.h` + `firestarter/src/proms/primitives.cpp`, D-03) and lift the
already-byte-identical report tail (`if (chip_id != handle->chip_id) { pack _b[4]; FORCE?WARN:ERROR }`)
out of all four call sites into a shared `chip_id_report` primitive. The proven precedent is
`flash_util_check_chip_id_execute` (flash_utils.cpp:110-126) which already shares this tail across
flash3/flash4 — P4 generalizes it to also serve eprom, eeprom28c, and flash_intel. Each handler keeps
its protocol-specific chip-id READ (A9-12V vs command-register 0x90 vs AMD unlock) local.

Purpose: Second extraction (P7→P4 order, D-09); the clean win that establishes the new module the
remaining cross-family primitives (P3, P5) will also live in.
Output: New module + one atomic P4 commit in the firestarter sub-repo; logged flash delta.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/89-incremental-primitive-recompose/89-CONTEXT.md
@.planning/phases/89-incremental-primitive-recompose/89-RESEARCH.md
@.planning/phases/89-incremental-primitive-recompose/89-PATTERNS.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create primitives.{h,cpp} module + chip_id_report; rewire all four P4 call sites</name>
  <files>firestarter/include/primitives.h, firestarter/src/proms/primitives.cpp, firestarter/src/proms/eprom.cpp, firestarter/src/proms/eeprom_28c.cpp, firestarter/src/proms/flash_intel.cpp, firestarter/src/proms/flash_type_4.cpp, firestarter/src/proms/flash_utils.cpp</files>
  <behavior>
    - The four chip-id golden traces (eprom, flash_intel, eeprom28c, flash4) are byte-identical before and after — the recorded (reg,data) sequence and the MSG frame `_b[4]` bytes (MSB-first: read_id hi, read_id lo, expected hi, expected lo) are unchanged.
    - On chip_id mismatch with FLAG_FORCE set: LOG_WARN_ID_BYTES(MSG_WARN_CHIP_ID_MISMATCH, _b, 4) + RESPONSE_CODE_WARNING. Without FORCE: LOG_ERROR_ID_BYTES(MSG_ERR_CHIP_ID_MISMATCH, _b, 4) + RESPONSE_CODE_ERROR.
    - On chip_id match: no log frame, response_code unchanged.
    - The eeprom28c `mem_size < 64` underflow guard (eeprom_28c.cpp:82-91) stays in eeprom_28c.cpp, before the read — NOT moved into the primitive (V5 input-validation; SAFE).
  </behavior>
  <read_first>
    - firestarter/include/eprom.h:8-21 — the header guard + `extern "C"` declarations-only skeleton to COPY (89-PATTERNS.md lines 33-68: copy eprom.h, NOT flash_utils.h's in-header table-definition style).
    - firestarter/src/proms/flash_utils.cpp:8-28 (banner convention to copy) and :102-126 — `flash_util_get_chip_id` (the protocol read) + `flash_util_check_chip_id_execute` (the byte-identical report tail = THE precedent for chip_id_report).
    - firestarter/include/flash_utils.h:62-69 — the function-declaration convention (firestarter_handle_t* handle first arg, void/scalar return via response_code).
    - Four call sites + their protocol-specific reads (89-PATTERNS.md table lines 123-130):
      eprom report tail eprom.cpp:356-369, read eprom.cpp:249-260, plus the `error_code` param wrinkle eprom_internal_check_chip_id eprom.cpp:353-370 + caller eprom_generic_init eprom.cpp:343-351;
      eeprom28c report tail eeprom_28c.cpp:100-115, read eeprom_28c.cpp:92-99, underflow guard eeprom_28c.cpp:82-91;
      flash_intel report tail flash_intel.cpp:218-231, read flash_intel.cpp:214-217;
      flash4 already delegates flash_type_4.cpp:143-145 -> flash_utils.cpp.
    - 89-RESEARCH.md Pattern 2 (lines 181-202) + Assumption A3 (line 341) — the suggested `void chip_id_report(firestarter_handle_t* handle, uint16_t read_id)` signature and the `error_code`-param redundancy warning (do NOT collapse eprom's keying onto FLAG_FORCE without confirming the golden trace).
    - firestarter/platformio.ini:129-130 — `build_src_filter = +<proms/>` (new TU auto-compiled, no ini change needed; RESEARCH Pattern 1).
    - firestarter/test/native/avr/test_val_{eprom,eeprom28c,flash_intel,flash4}/ — the chip-id golden fixtures (`*_chip_id.inc`) that gate this edit. flash3 has NO chip-id site (Phase 88 D-03) — do not add one.
  </read_first>
  <action>
    Create `firestarter/include/primitives.h`: copy the `include/eprom.h` guard + `extern "C"` skeleton
    (declarations only — do NOT define function bodies in the header), `#include "firestarter.h"`, and
    declare `void chip_id_report(firestarter_handle_t* handle, uint16_t read_id);`. Use guard `__PRIMITIVES_H__`.

    Create `firestarter/src/proms/primitives.cpp`: copy the `flash_utils.cpp:8-28` banner style (name the
    callers eprom + eeprom28c + flash_intel + flash4; note the D-06 keying-on-handle->protocol boundary and
    that NO regulator control lives here). Implement `chip_id_report` by lifting the report tail verbatim
    from flash_utils.cpp:112-125: compare `read_id != handle->chip_id`, pack `_b[4]` MSB-first
    (read_id hi/lo, handle->chip_id hi/lo), FORCE?WARN:ERROR via `is_flag_set(FLAG_FORCE)`. Status via
    `handle->response_code` only.

    Rewire the four call sites so the caller performs its protocol-specific read, then calls
    `chip_id_report(handle, read_id)`:
    - flash_utils.cpp: rewrite `flash_util_check_chip_id_execute` to call `chip_id_report(handle, flash_util_get_chip_id(handle))` (flash4 inherits via its existing delegation flash_type_4.cpp:143-145 — no flash4 edit beyond confirming the trace).
    - eprom.cpp: keep the A9-12V read local; replace the inline report tail with `chip_id_report(handle, read_id)`. PRESERVE the `error_code` param semantics — per Assumption A3, eprom_generic_init passes FORCE?WARNING:ERROR which equals the shared tail, BUT eprom_check_chip_id_execute may pass ERROR unconditionally; if collapsing onto the FLAG_FORCE tail changes any chip-id trace, keep the eprom-local keying and only share the pack+frame, or do not extract eprom's site (D-02). Verify the golden trace before collapsing.
    - eeprom_28c.cpp: keep the `mem_size < 64` underflow guard + the A9-12V read with `mfr_addr = mem_size-64` local; replace the report tail with `chip_id_report(handle, read_id)`.
    - flash_intel.cpp: keep the command-register read (set_data 0x90 autoselect, read, 0xFF exit) local; replace the report tail with `chip_id_report(handle, read_id)`.

    No platformio.ini change (build_src_filter globs proms/). No wire/constant/dispatch/DB change (SAFE-06/D-09).
    Commit as one atomic P4 commit inside the firestarter submodule. If any of the four chip-id golden traces
    will not zero-diff, per D-02 either keep that one call site in pre-extraction form (share the rest) or
    revert P4 and document the deferral — do NOT re-bless to mask a diff (D-04).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_eprom*" -f "*test_val_flash_intel*" -f "*test_val_eeprom28c*" -f "*test_val_flash4*"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/include/primitives.h` exists, uses the `__PRIMITIVES_H__` guard + `extern "C"`, and declares `chip_id_report` (functions declared, not defined — `grep -c '^[[:space:]]*void chip_id_report' firestarter/include/primitives.h` >= 1; no function body brace in the header).
    - `firestarter/src/proms/primitives.cpp` exists and defines `chip_id_report` (`grep -c 'void chip_id_report' firestarter/src/proms/primitives.cpp` >= 1).
    - `grep -rc 'chip_id_report' firestarter/src/proms/` shows the report tail is called from >= 3 handler/util TUs (flash_utils + eprom + eeprom28c + flash_intel), not duplicated inline.
    - `cd firestarter && pio test -e native -f "*test_val_eprom*" -f "*test_val_flash_intel*" -f "*test_val_eeprom28c*" -f "*test_val_flash4*"` — all chip-id golden traces byte-identical (zero-diff). A diff is STOP-and-inspect (D-04).
    - The eeprom28c `mem_size < 64` guard and each handler's protocol-specific read remain in their original handler file (`grep -c 'mem_size' firestarter/src/proms/eeprom_28c.cpp` unchanged for the guard).
  </acceptance_criteria>
  <done>The new primitives module exists; chip_id_report is shared across the four P4 sites; each handler's read mechanism + the eeprom28c underflow guard stay local; all four chip-id golden traces are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Per-step P4 gate — full native suite + flash delta + frozen-world host gates + INV grep</name>
  <files>firestarter/.flash-baseline-87.txt (read-only)</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 284-323) — the Phase 87-04 `DELTA<=16` flash recipe + per-step oracle + read-only host-gate commands.
    - firestarter/.flash-baseline-87.txt (25654) and the P7 step's logged post-flash (the prior-step reference for the per-step delta).
    - firestarter_app/tools/check_dispatch.py + diff_db.py; firestarter/doc/PROTOCOLS.md §3 (INV matrix).
  </read_first>
  <action>
    Run the identical per-step gate (same as 89-01 Task 2):
    1. `cd firestarter && pio test -e native` — ALL in-filter suites green (D-07/SAFE-02); ignore the two known-flaky excluded suites (RESEARCH Pitfall 6).
    2. Flash delta (D-01): `pio run -e leonardo`, parse `Flash: ... used N bytes`, assert `(POST - PRIOR_STEP_POST) <= 16` where PRIOR_STEP_POST is P7's logged post-flash. Adding the first call site of a shared function can cost a few bytes — the +16 tolerance absorbs that; biggest-saver P7 front-loaded the headroom. Log pre/post/delta.
    3. `cd firestarter_app && python tools/check_dispatch.py` (exit 0) + `python tools/diff_db.py` (exit 0) (D-05/SAFE-03).
    4. `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
    5. INV-01..09 each still >= 3 files via `grep -rln` over doc/ src/ test/ (SAFE-02).
    Record pre/post flash for the 89-05 PRIM-06 report.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e leonardo 2>&1 | grep -oE 'Flash:.*used [0-9]+ bytes'</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` — full suite green (in-filter).
    - `(POST - prior-step-post) <= 16` (D-01 per-step gate); delta logged.
    - `check_dispatch.py` exit 0; `diff_db.py` exit 0.
    - `git -C firestarter_app diff --quiet` exit 0.
    - Each INV-01..09 >= 3 files (SAFE-02).
  </acceptance_criteria>
  <done>Full native suite green, flash delta <= +16 logged, both host gates exit 0, host repo untouched, 9 INV ids >= 3 files — P4 commit kept; or P4 (or its hardest call site) deferred cleanly per D-02 with a documented reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON command → firmware handler | protocol_id → wire → handler; dispatch frozen |
| firmware logic → RURP control register | chip-id read drives A9-12V on some protocols |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/DoS | 12V VPP regulator on a 5V part | mitigate | P4 shares only the report tail; the protocol-specific chip-id read (incl. A9-12V regulator enable) stays handler-local + protocol-keyed (D-06); check_dispatch.py 0-violations re-run. |
| T-89-02 | Tampering | Over-voltage past VPP window | accept (untouched) | P4 does not touch the VPP window check (eprom.cpp:282 / flash_intel.cpp:65). |
| T-89-03 | Elevation | Host support-status guard bypass | accept (untouched) | No host source change (SAFE-06); `git -C firestarter_app diff --quiet` proves it. |
| T-89-V5 | Tampering | eeprom28c mem_size underflow (mfr_addr = mem_size-64) | mitigate | The `mem_size < 64` underflow guard (eeprom_28c.cpp:82-91) stays handler-local, NOT moved into chip_id_report (RESEARCH Security Domain V5). |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (SAFE-05); the only new artifact (primitives.{cpp,h}) is first-party C++, not a third-party dependency. |
</threat_model>

<verification>
- `cd firestarter && pio test -e native` — full suite green (all four chip-id golden traces byte-identical).
- `pio run -e leonardo` flash delta <= +16 B vs the P7 step (D-01).
- `check_dispatch.py` exit 0 + `diff_db.py` exit 0 (D-05/SAFE-03).
- `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
- INV-01..09 each >= 3 files (SAFE-02).
</verification>

<success_criteria>
P4 chip_id_report extracted (or cleanly deferred per D-02): new primitives module created; shared report
tail serves all four call sites; handler-local reads + the eeprom28c underflow guard preserved; all four
chip-id golden traces byte-identical; flash delta <= +16 logged; frozen-world gates green; host untouched.
(ROADMAP SC#2.)
</success_criteria>

<output>
Create `.planning/phases/89-incremental-primitive-recompose/89-02-SUMMARY.md` when done. Record the
measured pre/post Leonardo flash bytes + delta for the phase-close PRIM-06 report.
</output>
