---
phase: 89-incremental-primitive-recompose
plan: 05
type: execute
wave: 5
depends_on: ["89-04"]
files_modified:
  - .planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md
autonomous: true
requirements: [PRIM-06, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "The phase-cumulative final Leonardo flash is strictly below the 25654 B Phase-88 baseline (D-01 net-decrease)"
    - "The achieved final flash percentage is recorded (PRIM-06)"
    - "A step ledger records the pre/post bytes + delta for each of P7/P4/P3/P5 (incl. any D-02-deferred primitive and its reason)"
    - "Final full-suite gate: pio test -e native green, check_dispatch.py 0 violations, diff_db.py empty, git -C firestarter_app diff --quiet, all 9 INV ids >= 3 files"
    - "SAFE-04 posture verified: over-voltage VPP check present+unmodified, chip_resolver.resolve_chip guard never bypassed, 2516 stays UNVERIFIED"
  artifacts:
    - path: ".planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md"
      provides: "Per-step flash ledger + final % + per-primitive disposition (committed/deferred)"
      contains: "Final flash"
  key_links:
    - from: ".planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md"
      to: "firestarter/.flash-baseline-87.txt"
      via: "final bytes vs 25654 baseline net-decrease assertion"
      pattern: "25654"
---

<objective>
Close Phase 89 (PRIM-06): assert the phase-cumulative net-decrease, record the achieved final flash
percentage, consolidate the per-step flash ledger from P7/P4/P3/P5, and run the final frozen-world +
SAFE-04 safety-posture gate. This plan owns NO firmware change — it measures, reports, and verifies the
whole-phase outcome of the four extraction plans (89-01..89-04), capturing the wins per D-02 even if a
primitive (most likely P3) was deferred.

Purpose: Final wave; the PRIM-06 measurement/report deliverable + the phase-level frozen-world and
electrical-safety verification before `/gsd-verify-work`.
Output: `89-FLASH-LEDGER.md` with the step ledger, final %, and per-primitive disposition.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/89-incremental-primitive-recompose/89-CONTEXT.md
@.planning/phases/89-incremental-primitive-recompose/89-RESEARCH.md
@.planning/phases/89-incremental-primitive-recompose/89-VALIDATION.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final flash measure + net-decrease assertion + step ledger (PRIM-06)</name>
  <files>.planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 284-298) — the Phase 87-04 flash recipe + the phase-close net-decrease assertion (`[ "$FINAL" -lt 25654 ] || exit 1`).
    - firestarter/.flash-baseline-87.txt (25654 B baseline).
    - The four prior SUMMARYs (89-01..89-04) — each recorded its pre/post Leonardo flash bytes + delta + commit/defer disposition; consolidate them into the ledger.
  </read_first>
  <action>
    Measure the final Leonardo flash with HEAD at the last committed extraction:
    `cd firestarter && pio run -e leonardo`, parse the `Flash: NN.N% (used N bytes from 28672 bytes)` line.
    Compute the final % and the cumulative delta vs 25654 B.

    Author `.planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md` with:
    - A step table: one row per primitive (P7, P4, P3, P5) with pre-bytes, post-bytes, per-step delta,
      and disposition (committed | deferred-D-02 with reason), pulled from the four SUMMARYs.
    - The baseline (25654 B / 89.5%), the final bytes, the final % (PRIM-06), and the cumulative delta.
    - The net-decrease assertion result: `final_bytes < 25654` (D-01 phase-close gate). If a primitive was
      deferred (D-02), the phase still ships the clean wins — note which primitives landed and confirm the
      cumulative result is still a net-decrease (the committed primitives must net below 25654; if even the
      committed set does not net-decrease, flag it explicitly in the ledger as the PRIM-06 honest outcome).

    Do NOT touch any firmware file (this plan is measurement + reporting only).
  </action>
  <verify>
    <automated>cd firestarter && pio run -e leonardo 2>&1 | grep -oE 'Flash:.*used [0-9]+ bytes' && test -f .planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `pio run -e leonardo` final Flash bytes parsed; `89-FLASH-LEDGER.md` records baseline 25654, final bytes, final % (PRIM-06), and per-step deltas for P7/P4/P3/P5.
    - The ledger asserts `final_bytes < 25654` (D-01 net-decrease) for the committed set, OR explicitly flags the honest outcome if the committed primitives do not net-decrease.
    - Each of P7/P4/P3/P5 has a disposition row (committed | deferred-D-02 + reason).
    - `grep -c 'Final flash' .planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md` >= 1.
  </acceptance_criteria>
  <done>The final Leonardo flash + % is measured and recorded; the per-step ledger consolidates P7/P4/P3/P5 deltas + dispositions; the phase-cumulative net-decrease is asserted (or the honest outcome flagged). (ROADMAP SC#5, PRIM-06.)</done>
</task>

<task type="auto">
  <name>Task 2: Final frozen-world gate + SAFE-04 safety-posture verification</name>
  <files>.planning/phases/89-incremental-primitive-recompose/89-FLASH-LEDGER.md</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 300-323) — full native suite + read-only host gate commands.
    - 89-CONTEXT.md D-08 + 89-VALIDATION.md Threat References (T-89-01..03) — the SAFE-04 posture targets.
    - firestarter_app/tools/check_dispatch.py + diff_db.py; firestarter/doc/PROTOCOLS.md §3 (INV matrix).
    - firestarter_app/firestarter/chip_resolver.py:55 — the resolve_chip host guard (must be unbypassed/unchanged).
    - firestarter/src/proms/eprom.cpp (~282) + flash_intel.cpp (~65) — the over-voltage VPP HIGH check.
  </read_first>
  <action>
    Run the whole-phase final gate (mirrors Phase 88-05's frozen-world + SC#4 posture verification):
    1. Full native suite: `cd firestarter && pio test -e native` — ALL in-filter suites green (D-07/SAFE-02); ignore the two known-flaky excluded suites (RESEARCH Pitfall 6).
    2. Host frozen-world gates (D-05/SAFE-03): `cd firestarter_app && python tools/check_dispatch.py` (exit 0, "0 dispatch regressions, 0 consistency violations") + `python tools/diff_db.py` (exit 0, "0 changed / 0 new / 0 missing").
    3. SAFE-06: `git -C firestarter_app diff --quiet` exit 0 (host source untouched).
    4. INV greppability (SAFE-02): each INV-01..INV-09 still hits >= 3 files via `grep -rln "INV-0N" firestarter/doc/ firestarter/src/ firestarter/test/`.
    5. SAFE-04 posture (D-08, T-89-02/03):
       - over-voltage HIGH check `vpp_mv > ... + 500` present + unmodified at eprom.cpp (~282 region) and flash_intel.cpp (~65 region) — threshold + FORCE/ERROR semantics byte-identical.
       - `chip_resolver.resolve_chip` guard at chip_resolver.py:55 present + unchanged (no host edit; covered by step 3).
       - 2516 stays `UNVERIFIED` (verification_status=UNVERIFIED + support_status=supported; no write-graduation this phase).
    Append the gate results + SAFE-04 confirmation to `89-FLASH-LEDGER.md`.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` — full suite green (in-filter).
    - `cd firestarter_app && python tools/check_dispatch.py` exit 0; `python tools/diff_db.py` exit 0.
    - `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
    - Each INV-01..INV-09 >= 3 files (SAFE-02).
    - Over-voltage HIGH check present + byte-identical at eprom.cpp + flash_intel.cpp (D-08); `grep -n 'resolve_chip' firestarter_app/firestarter/chip_resolver.py` shows the guard present (and unchanged via step 3); 2516 still UNVERIFIED.
  </acceptance_criteria>
  <done>Full native suite green, both host gates exit 0, host repo untouched, all 9 INV ids >= 3 files, SAFE-04 posture confirmed (over-voltage check + host guard intact, 2516 UNVERIFIED) — phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON command → firmware handler | protocol_id → wire → handler; dispatch frozen (verified empty diff_db + 0-violation dispatch) |
| firmware logic → RURP control register | VPP/regulator bits — over-voltage check verified present at phase close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/DoS | 12V/25V VPP regulator on a 5V part | mitigate | Final check_dispatch.py 0-violations (SRAM-never-reaches-eprom) + protocol-keyed routing (D-06) confirmed across the whole recompose. |
| T-89-02 | Tampering | Over-voltage past VPP window | mitigate | Final verification that the `vpp_mv > set+500` HIGH check is present + byte-identical at eprom.cpp:282 / flash_intel.cpp:65 (D-08). |
| T-89-03 | Elevation | Host support-status guard bypass | mitigate | `chip_resolver.resolve_chip` guard verified present + unchanged; `git -C firestarter_app diff --quiet` proves no host edit (SAFE-06); 2516 stays UNVERIFIED. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in the phase (SAFE-05); the only new artifact (primitives.{cpp,h}) is first-party C++. |
</threat_model>

<verification>
- `cd firestarter && pio run -e leonardo` final bytes recorded; `final < 25654` net-decrease (D-01/PRIM-06).
- `cd firestarter && pio test -e native` — full suite green.
- `check_dispatch.py` exit 0 + `diff_db.py` exit 0 (D-05/SAFE-03).
- `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
- INV-01..09 each >= 3 files (SAFE-02).
- Over-voltage check + resolve_chip guard present/unmodified; 2516 UNVERIFIED (SAFE-04/D-08).
</verification>

<success_criteria>
Phase closed: final Leonardo flash + achieved % recorded with phase-cumulative net-decrease asserted
(PRIM-06); per-step ledger with each primitive's committed/deferred disposition; full native suite green;
frozen-world gates exit 0; host untouched; all 9 INV ids >= 3 files; SAFE-04 electrical-safety posture
intact. (ROADMAP SC#5.)
</success_criteria>

<output>
Create `.planning/phases/89-incremental-primitive-recompose/89-05-SUMMARY.md` when done, alongside the
`89-FLASH-LEDGER.md` artifact (the PRIM-06 deliverable).
</output>
