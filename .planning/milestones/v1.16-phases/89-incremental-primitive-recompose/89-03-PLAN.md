---
phase: 89-incremental-primitive-recompose
plan: 03
type: execute
wave: 3
depends_on: ["89-02"]
files_modified:
  - firestarter/include/primitives.h
  - firestarter/src/proms/primitives.cpp
  - firestarter/src/proms/eprom.cpp
  - firestarter/src/proms/flash_intel.cpp
autonomous: true
requirements: [PRIM-04, PRIM-06, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "A shared vpp_check_window primitive handles the HIGH/LOW window-compare + _b[8] byte-packing + FORCE downgrade"
    - "eprom_check_vpp and flash_intel_check_vpp each keep their own regulator-routing assertion, REV0 guard, and trailing clear local, then call the shared check"
    - "The over-voltage HIGH check (vpp_mv > set+500) threshold and FORCE/ERROR semantics are byte-identical (D-08 unchanged)"
    - "Behavior keys on handle->protocol, never electrical.type (D-06); regulator routing is NOT moved into the shared primitive"
    - "test_val_eprom (0x07/08/0B) + test_val_flash_intel golden traces AND the INV-01/INV-03 bit-level asserts stay green (full suite)"
    - "OR, if zero-diff is not achievable, P3 is deferred per D-02 with a documented FUT/CR reason and both handlers left in pre-extraction form"
  artifacts:
    - path: "firestarter/src/proms/primitives.cpp"
      provides: "vpp_check_window shared read+window+_b[8]+FORCE body (no regulator control)"
      contains: "vpp_check_window"
    - path: "firestarter/include/primitives.h"
      provides: "vpp_check_window declaration"
      contains: "vpp_check_window"
  key_links:
    - from: "firestarter/src/proms/eprom.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "vpp_check_window(handle) called between its own regulator enable and trailing clear"
      pattern: "vpp_check_window"
    - from: "firestarter/src/proms/flash_intel.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "vpp_check_window(handle) called after caller-held regulator, no trailing clear"
      pattern: "vpp_check_window"
---

<objective>
Extract P3 — the VPP-gate primitive (PRIM-04). This is the HIGHEST deferral risk in the phase (D-02).
The window-compare body is byte-identical between `eprom_check_vpp` (eprom.cpp:262-325) and
`flash_intel_check_vpp` (flash_intel.cpp:52-108): read voltage, HIGH check (`vpp_mv > set+500`,
the SAFE-04/D-08 over-voltage guard), LOW check (`< set*95/100`), identical `_b[8]` packing, identical
FORCE downgrade. Extract ONLY that verbatim middle into a shared `vpp_check_window(handle)`. The three
divergent axes — regulator enable, the REV0 guard, the trailing regulator clear — MUST stay handler-local.
Behavior keys on `handle->protocol`, never `electrical.type` (D-06). If the golden traces or the
INV-01/INV-03 bit guards will not zero-diff, DEFER P3 (D-02) rather than re-bless.

Purpose: Third extraction (P7→P4→P3 order, D-09); the biggest-saving but riskiest primitive, adjacent
to the over-voltage check and owning the regulator-keying surface.
Output: One atomic P3 commit OR a documented D-02 deferral; logged flash delta.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/89-incremental-primitive-recompose/89-CONTEXT.md
@.planning/phases/89-incremental-primitive-recompose/89-RESEARCH.md
@.planning/phases/89-incremental-primitive-recompose/89-PATTERNS.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract vpp_check_window shared body; keep regulator routing + REV0 guard + clear handler-local</name>
  <files>firestarter/include/primitives.h, firestarter/src/proms/primitives.cpp, firestarter/src/proms/eprom.cpp, firestarter/src/proms/flash_intel.cpp</files>
  <behavior>
    - eprom 0x07/0x08/0x0B write golden traces AND flash_intel write golden trace are byte-identical before/after: same (reg,data) order, same `_b[8]` MSB-first packing, same MSG_WARN_VPP_HIGH / MSG_ERR_VPP_HIGH / MSG_WARN_VPP_LOW frames.
    - HIGH branch (`vpp_mv > (uint32_t)handle->vpp_mv + 500`): FORCE -> WARN+RESPONSE_CODE_WARNING, else ERROR+RESPONSE_CODE_ERROR. LOW branch (`vpp_mv < (uint32_t)handle->vpp_mv * 95 / 100`): always WARN+WARNING. In-window: no frame. (Threshold + semantics byte-identical = D-08.)
    - INV-01 (0x0B direct-VPE rail) and INV-03 (0x08 P1-as-VPP) bit-level asserts in test_val_eprom stay green — these guard the 0x100 VPE_DROP bit that is LOW-BYTE-invisible in the golden trace (RESEARCH Pitfall 5).
    - The regulator enable/clear bits 0x80/0x100/P1 are issued by each HANDLER, not by vpp_check_window — verified by trace position relative to the read.
  </behavior>
  <read_first>
    - 89-PATTERNS.md §P3 (lines 144-201) — the shared middle (eprom.cpp:280-323), the three handler-local divergence axes table (lines 176-180), the protocol-keying idiom to replicate (eprom.cpp:271 / :198), the deferral trigger.
    - firestarter/src/proms/eprom.cpp:262-325 — `eprom_check_vpp`: REV0 guard 264-270, regulator enable 271-279 (the `handle->protocol == 0x0B || is_flag_set(FLAG_VPE_AS_VPP)` keying), shared window body 280-323, trailing clear 324 (`set_control_register(REGULATOR|VPE_DROP, 0)`), over-voltage HIGH check at 282 (D-08).
    - firestarter/src/proms/flash_intel.cpp:52-108 — `flash_intel_check_vpp`: REV0 guard 54-60, NO regulator toggle (caller flash_intel_write_init:133 set REGULATOR|P1 + delay(500)), shared body 63-106 (over-voltage HIGH check at 65, D-08), NO trailing clear (caller holds VPP, comment at 107).
    - 89-RESEARCH.md Pitfall 2 (lines 246-256), Pitfall 5 (lines 271-273 — 0x100 bit invisible, INV asserts are the complementary guard), Open Question 2 (lines 352-355 — keep REV0 guard handler-local in the first cut), ## Security Domain (lines 404-425 — P3 is the single highest-stakes invariant).
    - firestarter/test/native/avr/test_val_eprom/ + test_val_flash_intel/ — the write golden fixtures + INV-01/INV-03 bit asserts that gate this edit. (Use test_val_flash_intel, NOT the flaky test_flash_intel_vpp — RESEARCH Pitfall 6.)
  </read_first>
  <action>
    Add to `primitives.h`: declare `void vpp_check_window(firestarter_handle_t* handle);`.

    Add to `primitives.cpp`: implement `vpp_check_window` by lifting ONLY the verbatim-identical middle
    from eprom.cpp:280-323 (== flash_intel.cpp:63-106): `delay(100)` if it is part of the shared body
    (verify against the trace — eprom has it; flash_intel's caller already delayed — if delay placement
    diverges, keep delay handler-local), `rurp_read_voltage_mv()`, the HIGH check `> vpp_mv + 500` (D-08
    threshold UNCHANGED), the LOW check `< vpp_mv * 95 / 100`, the identical `_b[8]` MSB-first pack, and the
    FORCE?WARN:ERROR frames (MSG_WARN_VPP_HIGH / MSG_ERR_VPP_HIGH / MSG_WARN_VPP_LOW). Status via
    handle->response_code only. NO regulator control, NO REV0 guard, NO trailing clear inside this function
    (RESEARCH anti-pattern: moving the clear in would strand 12V on flash_intel or break eprom's trace).

    Rewire eprom_check_vpp: keep the REV0 early-return guard local (Open Q2 — first cut keeps it local;
    do NOT fold it in unless the trace stays zero-diff AND it saves flash); keep the protocol-keyed
    regulator enable + delay (the `0x0B || FLAG_VPE_AS_VPP -> REGULATOR` else `REGULATOR|VPE_DROP` branch,
    D-06) BEFORE the call; call `vpp_check_window(handle)`; keep the trailing
    `set_control_register(REGULATOR|VPE_DROP, 0)` clear AFTER the call.

    Rewire flash_intel_check_vpp: keep the REV0 guard local; the caller already enabled the regulator
    (do NOT add one); call `vpp_check_window(handle)`; add NO trailing clear (caller holds VPP).

    No wire/constant/dispatch/DB change (SAFE-06/D-09). Commit as one atomic P3 commit.

    D-02 DEFERRAL PATH (explicit, not a hard failure): if either `test_val_eprom` (0x07/08/0B write) or
    `test_val_flash_intel` write golden trace will not zero-diff, OR the INV-01/INV-03 bit asserts go red,
    revert P3 cleanly (leave both handlers in pre-extraction duplicated form), document the deferral as a
    FUT/CR-style row in the SUMMARY with the specific trace/assert that diverged, and CONTINUE to P5 (89-04).
    Do NOT re-bless to mask a P3 diff (D-04) — this is exactly where the oracle matters most.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_eprom*" -f "*test_val_flash_intel*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vpp_check_window' firestarter/include/primitives.h` >= 1 (declared) and `grep -c 'void vpp_check_window' firestarter/src/proms/primitives.cpp` >= 1 (defined).
    - `grep -c 'vpp_check_window' firestarter/src/proms/eprom.cpp` >= 1 and `grep -c 'vpp_check_window' firestarter/src/proms/flash_intel.cpp` >= 1 (both handlers call the shared check).
    - The regulator-routing keying stays in the handlers: `grep -c 'protocol == 0x0B' firestarter/src/proms/eprom.cpp` >= 1 (the D-06 keying NOT moved into primitives.cpp); `grep -c 'REGULATOR' firestarter/src/proms/primitives.cpp` == 0 for the vpp_check_window body (no regulator control inside the shared primitive).
    - `cd firestarter && pio test -e native -f "*test_val_eprom*" -f "*test_val_flash_intel*"` — write golden traces byte-identical AND INV-01/INV-03 bit asserts green (zero-diff). On any diff: D-02 deferral (revert + documented FUT/CR), NOT re-bless.
  </acceptance_criteria>
  <done>vpp_check_window is shared by both VPP handlers with all regulator routing / REV0 guard / clear kept handler-local and protocol-keyed; eprom + flash_intel write traces byte-identical and INV-01/INV-03 green — OR P3 cleanly deferred per D-02 with a documented reason.</done>
</task>

<task type="auto">
  <name>Task 2: Per-step P3 gate — full native suite + flash delta + frozen-world host gates + INV grep + D-08 posture</name>
  <files>firestarter/.flash-baseline-87.txt (read-only)</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 284-323) — the flash recipe + per-step oracle + read-only host gates.
    - firestarter/.flash-baseline-87.txt (25654) and the P4 step's logged post-flash (prior-step reference).
    - 89-CONTEXT.md D-08 + 89-VALIDATION.md T-89-02 — the over-voltage check (eprom.cpp:282, flash_intel.cpp:65) MUST be present + unmodified after P3.
    - firestarter_app/tools/check_dispatch.py + diff_db.py; firestarter/doc/PROTOCOLS.md §3 (INV matrix).
  </read_first>
  <action>
    Run the identical per-step gate, plus the D-08 safety-posture spot-check that is most load-bearing here
    (P3 is the only step touching the VPP region):
    1. `cd firestarter && pio test -e native` — ALL in-filter suites green (CRITICAL: run the FULL suite, not just the golden tests — the 0x100 VPE_DROP bit is LOW-BYTE-invisible in golden traces, so INV-01/INV-03 bit asserts are the complementary guard; RESEARCH Pitfall 5). Ignore the two known-flaky excluded suites (Pitfall 6).
    2. Flash delta (D-01): `pio run -e leonardo`, parse Flash bytes, assert `(POST - prior-step-post) <= 16`. P3 is the biggest saver — expect a net DECREASE here (this is where the cumulative net-decrease is earned). Log pre/post/delta.
    3. `cd firestarter_app && python tools/check_dispatch.py` (exit 0) + `python tools/diff_db.py` (exit 0) (D-05/SAFE-03).
    4. `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
    5. INV-01..09 each >= 3 files via `grep -rln` (SAFE-02).
    6. D-08 posture: confirm the over-voltage HIGH check `vpp_mv > ... + 500` is still present at eprom.cpp (~282 region) and flash_intel.cpp (~65 region) with the threshold + FORCE/ERROR semantics byte-identical (`grep -n 'vpp_mv' firestarter/src/proms/eprom.cpp firestarter/src/proms/flash_intel.cpp` — the +500 HIGH compare must remain, whether in the handler or the shared body).
    Record pre/post flash for the 89-05 PRIM-06 report.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e leonardo 2>&1 | grep -oE 'Flash:.*used [0-9]+ bytes'</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` — full suite green (in-filter), including INV-01/INV-03 bit asserts (D-07/SAFE-02).
    - `(POST - prior-step-post) <= 16` (D-01); delta logged (expected net-decrease).
    - `check_dispatch.py` exit 0; `diff_db.py` exit 0.
    - `git -C firestarter_app diff --quiet` exit 0.
    - Each INV-01..09 >= 3 files (SAFE-02).
    - Over-voltage `vpp_mv > ... + 500` HIGH check present + byte-identical threshold/semantics (D-08).
  </acceptance_criteria>
  <done>Full native suite green incl. INV-01/INV-03, flash delta <= +16 (net-decrease) logged, host gates exit 0, host untouched, 9 INV ids >= 3 files, over-voltage check intact — P3 commit kept; or P3 deferred cleanly per D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON command → firmware handler | protocol_id → wire → handler; protocol-keyed routing |
| firmware logic → RURP control register | regulator/VPP bits drive 12-25V onto the socket (highest-stakes boundary this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/DoS | 12V/25V VPP regulator on a 5V part | mitigate | P3 extracts ONLY the read+window+pack+FORCE body; ALL regulator routing + REV0 guard + trailing clear stay handler-local + protocol-keyed (D-06, `handle->protocol == 0x0B || FLAG_VPE_AS_VPP`); WARNING-5 structural guards (novpp_in_eprom/eeprom28c_in_eprom) preserved; check_dispatch.py SRAM-never-reaches-eprom 0-violations re-run. |
| T-89-02 | Tampering | Over-voltage past VPP window | mitigate | The HIGH check `vpp_mv > set+500` threshold + FORCE/ERROR semantics stay byte-identical (D-08); golden trace + INV-01/INV-03 bit asserts (the 0x100 VPE_DROP bit is LOW-BYTE-invisible, so INV asserts are the complementary guard — full suite re-run) both verify it. |
| T-89-03 | Elevation | Host support-status guard bypass | accept (untouched) | No host source change (SAFE-06); `git -C firestarter_app diff --quiet` proves it. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (SAFE-05); primitives.{cpp,h} is first-party. |

**Highest-stakes invariant (RESEARCH Security Domain):** if P3 cannot keep BOTH the golden trace AND the
INV-01/INV-03 bit guards green at zero-diff, defer it (D-02) — do not re-bless.
</threat_model>

<verification>
- `cd firestarter && pio test -e native` — full suite green (write traces byte-identical + INV-01/INV-03 green).
- `pio run -e leonardo` flash delta <= +16 B vs the P4 step (D-01; expected net-decrease).
- `check_dispatch.py` exit 0 + `diff_db.py` exit 0 (D-05/SAFE-03).
- `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
- Over-voltage HIGH check present + byte-identical (D-08).
- INV-01..09 each >= 3 files (SAFE-02).
</verification>

<success_criteria>
P3 vpp_check_window extracted (or cleanly deferred per D-02 with a documented FUT/CR reason): shared
window body owns no regulator control; both handlers keep regulator routing / REV0 guard / clear local and
protocol-keyed; over-voltage threshold byte-identical (D-08); eprom + flash_intel write traces and
INV-01/INV-03 green; flash delta <= +16 (net-decrease) logged; frozen-world gates green; host untouched.
(ROADMAP SC#3.)
</success_criteria>

<output>
Create `.planning/phases/89-incremental-primitive-recompose/89-03-SUMMARY.md` when done. Record the
measured pre/post Leonardo flash bytes + delta for the phase-close PRIM-06 report. If P3 is deferred,
record the specific diverging trace/assert and the FUT/CR row.
</output>
