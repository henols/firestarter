---
phase: 89-incremental-primitive-recompose
plan: 04
type: execute
wave: 4
depends_on: ["89-03"]
files_modified:
  - firestarter/include/primitives.h
  - firestarter/src/proms/primitives.cpp
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/src/proms/eprom.cpp
autonomous: true
requirements: [PRIM-05, PRIM-06, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "A shared poll_readback primitive is used by eeprom28c_wait_for_write and flash4_wait_for_page_write (the two single-address polls)"
    - "The iteration cap (2000 vs 1024) and the per-site error frame (MSG_ERR_EEPROM_TIMEOUT vs MSG_ERR_FL4_VERIFY_TIMEOUT, different _b[] byte order) are parameterized — never reordered"
    - "The eprom verify_and_update_mask whole-buffer bitmask updater and all outer retry/page/erase loops stay untouched"
    - "test_val_eeprom28c + test_val_flash4 (+ test_val_eprom) golden traces stay byte-identical"
    - "OR, if zero-diff is not achievable, P5 (or eprom's site) is deferred per D-02 with a documented reason"
    - "Leonardo flash delta <= +16 vs the prior step; phase-cumulative net-decrease below 25654; check_dispatch/diff_db green; 9 INV ids >= 3 files"
  artifacts:
    - path: "firestarter/src/proms/primitives.cpp"
      provides: "poll_readback bounded single-address poll kernel (iteration-cap + error-frame parameterized)"
      contains: "poll_readback"
    - path: "firestarter/include/primitives.h"
      provides: "poll_readback declaration"
      contains: "poll_readback"
  key_links:
    - from: "firestarter/src/proms/eeprom_28c.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "poll_readback(...) with cap 2000 + MSG_ERR_EEPROM_TIMEOUT frame"
      pattern: "poll_readback"
    - from: "firestarter/src/proms/flash_type_4.cpp"
      to: "firestarter/src/proms/primitives.cpp"
      via: "poll_readback(...) with cap 1024 + MSG_ERR_FL4_VERIFY_TIMEOUT frame"
      pattern: "poll_readback"
---

<objective>
Extract P5 — the poll/readback verify primitive (PRIM-05). This is the SECOND-highest deferral risk
(D-02). The three candidate sites are NOT the same shape: `eeprom28c_wait_for_write` (eeprom_28c.cpp:156,
cap 2000) and `flash4_wait_for_page_write` (flash_type_4.cpp:119, cap 1024) share a bounded single-address
poll kernel, but with different iteration caps and different error frames (different MSG id AND different
`_b[]` byte order). `verify_and_update_mask` (eprom.cpp:182) is a structurally different whole-buffer
bitmask updater — do NOT unify it. Extract a `poll_readback` shared by the two single-address sites with
the iteration cap + error frame parameterized; leave every outer retry/page/erase loop intact.

Purpose: Fourth and final extraction (P7→P4→P3→P5 order, D-09); narrow shared kernel, subtle trace risk.
Output: One atomic P5 commit OR a documented D-02 deferral; logged flash delta.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/89-incremental-primitive-recompose/89-CONTEXT.md
@.planning/phases/89-incremental-primitive-recompose/89-RESEARCH.md
@.planning/phases/89-incremental-primitive-recompose/89-PATTERNS.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract poll_readback for the two single-address polls; leave eprom bitmask + outer loops intact</name>
  <files>firestarter/include/primitives.h, firestarter/src/proms/primitives.cpp, firestarter/src/proms/eeprom_28c.cpp, firestarter/src/proms/flash_type_4.cpp, firestarter/src/proms/eprom.cpp</files>
  <behavior>
    - eeprom28c write golden trace AND flash4 write golden trace are byte-identical before/after: same poll count, same get_data(addr) sequence, same return-on-match, and (on timeout) the same MSG frame with the SAME `_b[]` byte order per site.
    - eeprom28c timeout frame: MSG_ERR_EEPROM_TIMEOUT, `_b[5]` = {addr>>16, addr>>8, addr, expected, observed}. flash4 timeout frame: MSG_ERR_FL4_VERIFY_TIMEOUT, `_b[5]` = {expected, addr>>16, addr>>8, addr, observed} (DIFFERENT order — must be a parameter, never normalized).
    - eeprom28c iteration cap = 2000; flash4 iteration cap = 1024 (parameters).
    - eprom's verify_and_update_mask whole-buffer bitmask path and the eprom_write_execute outer retry/pulse-delay-ramp loop are byte-unchanged (NOT routed through poll_readback unless a tiny read-compare leaf trivially zero-diffs).
  </behavior>
  <read_first>
    - 89-PATTERNS.md §P5 (lines 205-258) — the three site shapes: site 1 eeprom_28c.cpp:156-176 (cap 2000), site 2 flash_type_4.cpp:119-141 (cap 1024, different `_b[]` order), site 3 eprom.cpp:182-194 (whole-buffer bitmask, returns count, no timeout/error frame — DO NOT unify), the recommended cut, and the deferral trigger.
    - 89-RESEARCH.md Pitfall 3 (lines 258-265 — the three sites are not the same loop; error frames + caps MUST be parameters) and the Anti-Patterns (lines 204-208 — sharing P5's whole loop changes the trace).
    - firestarter/src/proms/eeprom_28c.cpp:156-176 — eeprom28c_wait_for_write (the single caller context: eeprom28c_write_execute outer loop stays intact).
    - firestarter/src/proms/flash_type_4.cpp:119-141 — flash4_wait_for_page_write (the page-write outer loop stays intact).
    - firestarter/src/proms/eprom.cpp:182-194 (verify_and_update_mask) + the outer retry loop eprom_write_execute:216-232 — both stay UNTOUCHED.
    - firestarter/test/native/avr/test_val_eeprom28c/ + test_val_flash4/ (+ test_val_eprom/) — the write golden fixtures that gate this edit.
  </read_first>
  <action>
    Add to `primitives.h`: declare a `poll_readback` whose signature parameterizes the iteration cap AND
    the error frame so neither site's trace is normalized. Discretion on exact signature (D-03); a workable
    shape: `bool poll_readback(firestarter_handle_t* handle, uint32_t address, uint8_t expected, uint16_t max_iters, uint16_t timeout_msg_id, const uint8_t* err_frame_order)` — OR keep it minimal and pass the error-frame construction back to the caller (return a bool + let each handler emit its own frame on false). Choose the shape that keeps BOTH golden traces zero-diff with the least shared surface (RESEARCH "share only the inner read-compare leaf" is an acceptable fallback).

    Add to `primitives.cpp`: implement the bounded single-address poll kernel verbatim from the two sites:
    `for (j = 0; j < max_iters; j++) { delayMicroseconds(10); observed = handle->firestarter_get_data(handle, address); if (observed == expected) return true; }`. The timeout/error-frame emission must reproduce each site's EXACT `_b[]` byte order and MSG id — if that cannot be expressed cleanly as a parameter, emit the frame in the caller (return false + observed) rather than reorder bytes.

    Rewire eeprom28c_wait_for_write -> poll_readback with cap 2000 + MSG_ERR_EEPROM_TIMEOUT (addr-first `_b[5]`).
    Rewire flash4_wait_for_page_write -> poll_readback with cap 1024 + MSG_ERR_FL4_VERIFY_TIMEOUT (expected-first `_b[5]`).
    Leave verify_and_update_mask (eprom.cpp:182) and the eprom_write_execute outer retry/pulse-delay loop
    UNTOUCHED — eprom's site is a different algorithm (whole-buffer bitmask, returns a count). Only consider
    sharing eprom's site if a tiny read-compare leaf trivially zero-diffs; otherwise leave it duplicated.

    No wire/constant/dispatch/DB change (SAFE-06/D-09). Commit as one atomic P5 commit.

    D-02 DEFERRAL PATH: if `test_val_eeprom28c` or `test_val_flash4` write trace (poll count, element order,
    or the timeout frame) will not zero-diff, revert P5 cleanly (leave both polls in pre-extraction form),
    document the deferral as a FUT/CR row in the SUMMARY, and CONTINUE to phase close (89-05). Do NOT re-bless
    to mask a P5 diff (D-04). Sharing only eeprom28c+flash4 (NOT eprom) is the expected scope.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_eeprom28c*" -f "*test_val_flash4*" -f "*test_val_eprom*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'poll_readback' firestarter/include/primitives.h` >= 1 (declared) and `grep -c 'poll_readback' firestarter/src/proms/primitives.cpp` >= 1 (defined).
    - `grep -c 'poll_readback' firestarter/src/proms/eeprom_28c.cpp` >= 1 and `grep -c 'poll_readback' firestarter/src/proms/flash_type_4.cpp` >= 1 (both single-address polls use the shared kernel).
    - eprom's whole-buffer path is untouched: `grep -c 'verify_and_update_mask' firestarter/src/proms/eprom.cpp` unchanged; `grep -c 'poll_readback' firestarter/src/proms/eprom.cpp` == 0 unless a trivially-zero-diff leaf share was made (default: 0).
    - Both error-frame byte orders preserved: the eeprom28c frame is addr-first and the flash4 frame is expected-first (verify in the diff — the `_b[]` packing per site is unchanged).
    - `cd firestarter && pio test -e native -f "*test_val_eeprom28c*" -f "*test_val_flash4*" -f "*test_val_eprom*"` — golden traces byte-identical (zero-diff). On any diff: D-02 deferral, NOT re-bless.
  </acceptance_criteria>
  <done>poll_readback is shared by the two single-address polls with cap + error-frame parameterized (byte order per site preserved); eprom's bitmask path + all outer loops untouched; eeprom28c + flash4 traces byte-identical — OR P5 cleanly deferred per D-02 with a documented reason.</done>
</task>

<task type="auto">
  <name>Task 2: Per-step P5 gate — full native suite + flash delta + frozen-world host gates + INV grep</name>
  <files>firestarter/.flash-baseline-87.txt (read-only)</files>
  <read_first>
    - 89-RESEARCH.md ## Code Examples (lines 284-323) — the flash recipe + per-step oracle + read-only host gates.
    - firestarter/.flash-baseline-87.txt (25654) and the prior step's logged post-flash.
    - firestarter_app/tools/check_dispatch.py + diff_db.py; firestarter/doc/PROTOCOLS.md §3 (INV matrix).
  </read_first>
  <action>
    Run the identical per-step gate (same as 89-01/02/03 Task 2):
    1. `cd firestarter && pio test -e native` — ALL in-filter suites green (D-07/SAFE-02); ignore the two known-flaky excluded suites (RESEARCH Pitfall 6).
    2. Flash delta (D-01): `pio run -e leonardo`, parse Flash bytes, assert `(POST - prior-step-post) <= 16`. Log pre/post/delta.
    3. `cd firestarter_app && python tools/check_dispatch.py` (exit 0) + `python tools/diff_db.py` (exit 0) (D-05/SAFE-03).
    4. `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
    5. INV-01..09 each >= 3 files via `grep -rln` (SAFE-02).
    Record pre/post flash for the 89-05 PRIM-06 report.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e leonardo 2>&1 | grep -oE 'Flash:.*used [0-9]+ bytes'</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && pio test -e native` — full suite green (in-filter).
    - `(POST - prior-step-post) <= 16` (D-01); delta logged.
    - `check_dispatch.py` exit 0; `diff_db.py` exit 0.
    - `git -C firestarter_app diff --quiet` exit 0.
    - Each INV-01..09 >= 3 files (SAFE-02).
  </acceptance_criteria>
  <done>Full native suite green, flash delta <= +16 logged, host gates exit 0, host untouched, 9 INV ids >= 3 files — P5 commit kept; or P5 deferred cleanly per D-02 with a documented reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON command → firmware handler | protocol_id → wire → handler; dispatch frozen |
| firmware logic → RURP control register | poll reads data bus; no VPP/regulator change in P5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering/DoS | 12V/25V VPP regulator on a 5V part | accept (untouched) | P5 touches only readback poll loops; no regulator/VPP routing; protocol-keyed routing (D-06) + WARNING-5 guards preserved; check_dispatch.py 0-violations re-run. |
| T-89-02 | Tampering | Over-voltage past VPP window | accept (untouched) | P5 does not touch the VPP window check; verified unmodified at phase close (89-05). |
| T-89-03 | Elevation | Host support-status guard bypass | accept (untouched) | No host source change (SAFE-06); `git -C firestarter_app diff --quiet` proves it. |
| T-89-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (SAFE-05); primitives.{cpp,h} is first-party. |
</threat_model>

<verification>
- `cd firestarter && pio test -e native` — full suite green (eeprom28c + flash4 + eprom traces byte-identical).
- `pio run -e leonardo` flash delta <= +16 B vs the P3 step (D-01).
- `check_dispatch.py` exit 0 + `diff_db.py` exit 0 (D-05/SAFE-03).
- `git -C firestarter_app diff --quiet` exit 0 (SAFE-06).
- INV-01..09 each >= 3 files (SAFE-02).
</verification>

<success_criteria>
P5 poll_readback extracted (or cleanly deferred per D-02): shared bounded single-address poll used by
eeprom28c + flash4 with iteration cap + error frame parameterized (byte order per site preserved); eprom's
whole-buffer bitmask + all outer retry/page/erase loops untouched; eeprom28c + flash4 traces byte-identical;
flash delta <= +16 logged; frozen-world gates green; host untouched. (ROADMAP SC#4.)
</success_criteria>

<output>
Create `.planning/phases/89-incremental-primitive-recompose/89-04-SUMMARY.md` when done. Record the
measured pre/post Leonardo flash bytes + delta for the phase-close PRIM-06 report. If P5 is deferred,
record the diverging trace and the FUT/CR row.
</output>
