---
phase: 86-variant-decode-correct-db-regen
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - firestarter_app/tools/build_db.py
  - firestarter_app/tools/diff_db.py
  - firestarter_app/firestarter/data/chip_database.json
autonomous: true
requirements: [VAR-02, VAR-03, VAR-04]
must_haves:
  truths:
    - "build_db.py classifies electrical.type/algorithm/pinout from a single principled classify(type, proto, pm_idx, flags, pinout) — Rule 1, Rule 2 (WARNING-5), Rule 3 are deleted"
    - "The regenerated chip_database.json resolves FM1608 -> algorithm 0x28 (40) and X88C64 -> electrical.type EEPROM via the general decode, no special-case"
    - "diff_db.py classifies every changed row vs the OLD baseline by a cited variant-decode rule (exit 0) BEFORE any baseline re-pin"
    - "check_dispatch.py exits 0 violations on the regenerated DB"
  artifacts:
    - path: "firestarter_app/tools/build_db.py"
      provides: "principled classify() replacing the Rule 1/2/3 override stack"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "regenerated correct DB (~744 chips)"
    - path: "firestarter_app/tools/diff_db.py"
      provides: "VARIANT_DECODE rule label(s) explaining the regen diff"
  key_links:
    - from: "firestarter_app/tools/build_db.py"
      to: "resolve_pinout_key"
      via: "classify() consumes the already-resolved pinout_key; resolve_pinout_key is UNCHANGED"
      pattern: "resolve_pinout_key"
    - from: "firestarter_app/tools/diff_db.py"
      to: "firestarter_app/tools/baseline/chip_database.baseline.json"
      via: "classified diff vs OLD baseline, exit 0"
      pattern: "VARIANT_DECODE"
---

<objective>
Replace the build_db.py Rule 1/2/3 override stack with a single principled `classify(type, proto, pm_idx, flags, pinout)` function (VAR-02), regenerate `chip_database.json` so FM1608 and X88C64 fall out correctly from the general decode (VAR-03), extend `diff_db.py` with a cited VARIANT_DECODE rule label so every changed row is explained against the OLD baseline (VAR-03), and prove `check_dispatch.py` 0 violations (VAR-04). The baseline re-pin is deliberately NOT in this plan — it is Plan 03, after the diff has been reviewed.

Purpose: VAR-02 (override-stack collapse), VAR-03 (correct decode + explained diff), VAR-04 (structural safety gate green). Honors the research reframing: the variant HIGH byte is documented (Plan 01) but NOT wired into classification — classify() keys on type/proto/pm_idx/flags.
Output: rewritten build_db.py, regenerated chip_database.json, extended diff_db.py, a captured explained-diff transcript.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md
@.planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md
@.planning/phases/86-variant-decode-correct-db-regen/86-01-SUMMARY.md
@firestarter_app/tools/DECODE-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Rule 1/2/3 with a principled classify() and regenerate the DB</name>
  <read_first>
    - firestarter_app/tools/build_db.py (the FULL classifier region: Pass-1 flags-based _etype ~L499-506; Rule 1 DIP24_2816 ~L508-524; Rule 2 WARNING-5 ~L526-571; Rule 3 type=4 SRAM ~L573-605; Pass-2 protocol-aware _etype ~L607-643; the Phase-84 FM1608->FRAM relabel ~L645-663; resolve_pinout_key ~L173-270; the inclusion gates Site A/Site B/AT28C arm + NMOS Site C — these are NOT Rule 1/2/3 and must stay)
    - firestarter_app/tools/DECODE-NOTES.md (Plan-01 decode dictionary; the pinned minipro SHA; the type/proto/pm_idx/flags classification keys)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Decode → Classification Mapping" table mapping each deleted rule to its replacement signal; §"Recommended classifier shape" pseudocode; §Pitfall 2 two-pass ordering, §Pitfall 3 do-not-touch resolve_pinout_key, §Pitfall 5 keep Phase-84 relabel + inclusion gates)
    - firestarter_app/firestarter/data/chip_database.json (current FM1608/X88C64/W27C512 records — the values the rewrite must reproduce or correct)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-06 full deletion no residual override; D-08 check_dispatch is the backstop)
  </read_first>
  <action>
    In `firestarter_app/tools/build_db.py`, define a single `classify(type_int, proto_id, pm_idx, flags, pinout_key, mem_size)` function returning `(etype, algorithm)`, structurally equivalent to the current Pass-1/Rules/Pass-2 sequence but with NO override stack. Order the arms per RESEARCH §"Recommended classifier shape": (1) SRAM/FRAM/NVRAM class — `type_int == 4 or proto_id in {0x0E,0x27,0x28,0x29}` -> SRAM with algorithm `0x28` when proto_id in {0x07,0x08,0x0B} else proto_id (this subsumes Rule 3); (2) 5V-EEPROM pinout clusters — `pinout_key in {"DIP24_2816","DIP28_28C64","DIP28_28C256"}` or (`DIP28_2764` and `flags & 0x10`) -> EEPROM / algorithm `0x0D` (subsumes Rule 1 + Rule 2; the {18,19,20} pm_idx clusters resolve to these pinout keys, pm_idx=22 keeps its VPP-pin EPROM cluster); (3) EPROM-family `proto_id in {0x07,0x08,0x0B}` -> EEPROM if `flags & 0x10` else UV-EPROM, algorithm proto_id; (4) Flash families `proto_id in {0x05,0x06,0x0D,0x10}` -> Flash/EEPROM, algorithm proto_id; (4b) X88C64 fix: `proto_id == 0x34` -> EEPROM, algorithm proto_id (display-only, see DECODE-NOTES.md); (5) default -> UV-EPROM, algorithm proto_id.
    Replace the inline Pass-1/Rule-1/Rule-2/Rule-3/Pass-2 blocks with a single `classify(...)` call AFTER `resolve_pinout_key` returns. For the type=4 28-pin SRAM case, preserve Rule 3's pinout/size_label re-route INSIDE classify (or immediately around it) so FM1608 still gets `DIP28_JEDEC_SRAM_8K` (8K) / `DIP28_28C256` (16K+) — RESEARCH Pitfall 5. KEEP UNCHANGED: `resolve_pinout_key` (variant LOW byte), Site A (0x34 PNI gate), Site B (24-pin EEPROM adapter-required + proto demotion), the AT28C named arm, NMOS Site C VPP correction, the Phase-84 FM1608->FRAM cosmetic relabel, and the SRAM vcc-normalization. Per D-06 leave NO residual post-classify override. Then run `python tools/build_db.py` to regenerate `firestarter/data/chip_database.json`. If DECODE-NOTES.md recorded a decision to pin `MINIPRO_XML_URL` to the SHA, apply that URL change here. Do NOT inline implementation code into this plan — write the function in build_db.py.
  </action>
  <verify>
    <automated>cd firestarter_app && grep -vE '^\s*#' tools/build_db.py | grep -Eqi "Rule 1:|Rule 2 WARNING-5|Rule 3:" && echo "RULES-STILL-PRESENT-FAIL" || (python tools/build_db.py >/dev/null 2>&1 && python -c "import json;d=json.load(open('firestarter/data/chip_database.json'));fm=[c for m in d.values() for c in m if 'FM1608' in {a.split('@')[0].strip() for a in c['part_number'].split(',')}][0];x=[c for m in d.values() for c in m if {'X88C64','X88C64P'} & {a.split('@')[0].strip() for a in c['part_number'].split(',')}][0];assert fm['programming']['algorithm']==40 and fm['pinout']=='DIP28_JEDEC_SRAM_8K',fm;assert x['electrical']['type']=='EEPROM' and x['support_status']=='protocol-not-implemented',x;print('CLASSIFY-OK')")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*#' firestarter_app/tools/build_db.py | grep -Ei "Rule 1:|Rule 2 WARNING-5|Rule 3:"` returns NOTHING (the three override blocks are removed from active code — VAR-02).
    - A `classify(` function is defined in build_db.py and is the sole classifier path called after `resolve_pinout_key`.
    - `resolve_pinout_key` is unchanged (verified via git diff — variant LOW byte logic untouched, RESEARCH Pitfall 3).
    - Regenerated `chip_database.json`: FM1608 has `programming.algorithm == 40` AND `pinout == "DIP28_JEDEC_SRAM_8K"`; X88C64 has `electrical.type == "EEPROM"` AND `support_status == "protocol-not-implemented"` (VAR-03).
    - W27C512 keeps `algorithm 0x07` + `electrical.type EEPROM` (flags&0x10 still wins on pm_idx=22 — RESEARCH Pitfall 2).
  </acceptance_criteria>
  <done>build_db.py has no Rule 1/2/3 marker comments in active (non-comment) code; a `classify(...)` function is the sole classifier; the regenerated DB has FM1608 algorithm==40 + pinout DIP28_JEDEC_SRAM_8K and X88C64 electrical.type==EEPROM + support_status protocol-not-implemented. Gate prints CLASSIFY-OK.</done>
</task>

<task type="auto">
  <name>Task 2: Extend diff_db.py with the VARIANT_DECODE rule label + capture the explained diff</name>
  <read_first>
    - firestarter_app/tools/diff_db.py (the `_RATIONALES` dict, `_RULE_FIELD_PATHS` dict, `_classify_diff` priority chain, the exit-code contract 0/1/2 — the existing RULE_ALGO/SRAM_PINOUT/BUG_A_ETYPE/RULE_PHASE84_RELABEL labels are the model)
    - firestarter_app/tools/DECODE-NOTES.md (the pinned SHA + the variant-decode facts to cite in the new rationale)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Code Examples" → diff_db.py rule-label extension snippet for VARIANT_DECODE; §Pitfall 4 — diff must exit 0 vs the OLD baseline with every row labeled BEFORE re-pin)
    - firestarter_app/tools/baseline/chip_database.baseline.json (the OLD baseline the diff runs against this plan — re-pin is Plan 03)
  </read_first>
  <action>
    Add a `VARIANT_DECODE` entry to `_RATIONALES` in `firestarter_app/tools/diff_db.py` citing the variant-decode consolidation (Rule 1/2/3 replaced by `classify(type,proto,pm_idx,flags,pinout)`), with the `[VERIFIED: minipro database.c#L1918 @ <PINNED_SHA> — uint8_t algo_number = (uint8_t)(device->variant >> 8)]` citation (use the SHA recorded in DECODE-NOTES.md) and `[VERIFIED: minipro.h#L70 MP_SRAM=0x04]`. Add `_RULE_FIELD_PATHS["VARIANT_DECODE"] = {("programming","algorithm"),("pinout",),("electrical","type")}`. Wire VARIANT_DECODE into `_classify_diff`'s priority chain so the regen rows (algorithm/pinout/electrical.type deltas attributable to the override-stack collapse + the X88C64 EEPROM type fix) are explained; preserve the existing precedence rules (RULE_ALGO primary on algo_diff; keep RULE_PHASE84_RELABEL before BUG_A_ETYPE). If a changed row is already cleanly explained by an existing label (e.g. RULE_ALGO for an algo flip), prefer the existing label — only route rows the existing labels cannot fully explain into VARIANT_DECODE. Then run `python tools/diff_db.py` against the OLD baseline and capture the full transcript to `firestarter_app/tools/variant-decode-diff.txt`; it MUST exit 0 (every changed row explained, no missing chips). If any row is unexplained, fix the label coverage (NOT the baseline) until exit 0.
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/diff_db.py | tee tools/variant-decode-diff.txt | grep -q "PASS: all" && echo DIFF-EXPLAINED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/diff_db.py` has a `VARIANT_DECODE` key in `_RATIONALES` with a `[VERIFIED: minipro database.c#L1918 @ <SHA>]` citation and a `_RULE_FIELD_PATHS["VARIANT_DECODE"]` entry covering algorithm/pinout/electrical.type.
    - `python tools/diff_db.py` exits 0 against the OLD `tools/baseline/chip_database.baseline.json` (every changed row attributed to a cited rule; 0 unexplained; 0 missing).
    - The transcript `tools/variant-decode-diff.txt` ends with `PASS: all`.
    - The baseline files are NOT modified by this task (re-pin is Plan 03).
  </acceptance_criteria>
  <done>diff_db.py has a VARIANT_DECODE rationale + field-path set + priority-chain wiring; `python tools/diff_db.py` exits 0 vs the OLD baseline with every changed row attributed to a cited rule; the transcript is saved to tools/variant-decode-diff.txt. Gate prints DIFF-EXPLAINED-OK. (No baseline re-pin in this plan.)</done>
</task>

<task type="auto">
  <name>Task 3: Prove check_dispatch.py 0 violations + EVIDENCE-11 stability on the regenerated DB (VAR-04)</name>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (the structural guards: novpp_in_eprom GATE-03, eeprom28c_in_eprom WARNING-5 type-keyed, sram_in_eprom BLOCKER-2, the D-10 assertions; this file is UNCHANGED — it is the D-08 backstop, just run it)
    - firestarter_app/tests/test_variant_decode_evidence_stability.py (the Plan-01 D-09 oracle — now run against the regenerated DB)
    - firestarter_app/tests/test_build_db_inclusion.py (the Plan-01 FM1608/X88C64 assertions — X88C64 should now be GREEN)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"D-09 EVIDENCE-11 Risk Table" — expected stable wire values; §"Safety Domain" — the hazard table)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-08 0-violations backstop; D-09 wire-stability-or-flag)
  </read_first>
  <action>
    Run `python tools/check_dispatch.py` on the regenerated DB; it MUST print 0 violations / 0 regressions (VAR-04 / D-08). Do NOT modify check_dispatch.py — if it reports a violation, the regenerated DB or classify() is wrong; fix the classifier (Task 1), not the gate. Run `python -m pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py -q` against the regenerated DB. The X88C64 assertion must now be GREEN. The EVIDENCE-11 test must pass (all 11 chips' algorithm/vpp_mv/pinout unchanged vs the OLD baseline, 2516 still absent). If ANY EVIDENCE chip's wire value moved, do NOT silently accept it: record the moved chip + its old/new algorithm/vpp_mv/pinout in the SUMMARY and create a `checkpoint:human-verify` flag for Leonardo + RURP Rev 2.0 re-bench before Phase 90 (D-09). Per the Risk Table, the expected outcome is zero moves (FM1608 stays 0x28/FRAM/DIP28_JEDEC_SRAM_8K; all 27512-family stays 0x07/12000/DIP28_27512; etc.).
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/check_dispatch.py | grep -Eq "0 dispatch regressions; 0 consistency violations" && python -m pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py -q 2>&1 | grep -Eq "passed" && echo VAR04-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/check_dispatch.py` prints `0 dispatch regressions; 0 consistency violations` on the regenerated DB (VAR-04 / D-08; check_dispatch.py NOT modified).
    - `test_x88c64_electrical_type_eeprom` now PASSES (gap from Plan 01 closed).
    - `test_variant_decode_evidence_stability.py` PASSES against the regenerated DB — all 10 in-DB EVIDENCE chips' algorithm/vpp_mv/pinout match the OLD baseline; 2516 still absent.
    - If any EVIDENCE chip moved, the SUMMARY records old/new wire values AND a Leonardo+Rev2.0 re-bench flag (D-09) — never silently accepted.
  </acceptance_criteria>
  <done>check_dispatch.py prints 0 dispatch regressions / 0 consistency violations on the regenerated DB; the FM1608/X88C64 inclusion tests pass; the EVIDENCE-11 stability test passes (or any moved chip is explicitly flagged for re-bench in the SUMMARY). Gate prints VAR04-GATE-OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| infoic.xml decode -> chip_database.json -> wire `algorithm`/`vpp_mv`/`pinout` | A wrong decode here is a physical-hardware-damage path: routing a 5V chip to `configure_eprom` asserts 12V VPP on a socket pin that is an address/WE line on the resident part. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering (physical) | build_db.py classify() — deleting Rule 2 (WARNING-5) could reopen the 12V-on-a-5V-pin hazard | mitigate | `check_dispatch.py` 0-violations is the structural backstop (Task 3): `novpp_in_eprom` (type-string-independent GATE-03) + `eeprom28c_in_eprom` (DIP28_2764 type-keyed) prove no 5V-EEPROM routes to configure_eprom even with the special-case gone (D-08). |
| T-86-02 | Tampering (silent regression) | the 11 v1.15 EVIDENCE bench chips' wire values silently move | mitigate | `test_variant_decode_evidence_stability.py` (Plan 01, run Task 3) asserts algorithm/vpp_mv/pinout vs the OLD baseline; any move is flagged for Leonardo+Rev2.0 re-bench (D-09), never silently accepted. |
| T-86-03 | Tampering (irreplaceable part) | the 2516 (v1.15 ANOMALY, unstable read path) gets wire values introduced/moved | mitigate | SAFE-04: the EVIDENCE-stability test asserts 2516 stays ABSENT from the generated DB (it is a user-override only); the decode rewrite touches no write path and no host guard. |
| T-86-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages introduced (SAFE-05); no install tasks in this phase. |
</threat_model>

<verification>
- `python tools/build_db.py` regenerates ~744 chips with no Rule 1/2/3 active code.
- `python tools/diff_db.py` exits 0 vs the OLD baseline; every changed row attributed to a cited rule (transcript captured).
- `python tools/check_dispatch.py` prints 0 dispatch regressions / 0 consistency violations.
- `pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py` green.
- Baseline files NOT modified in this plan (re-pin is Plan 03).
</verification>

<success_criteria>
VAR-02 (Rule 1/2/3 removed, single principled classify()), VAR-03 (FM1608->0x28 + X88C64->EEPROM via general decode; every diff row explained vs OLD baseline), and VAR-04 (check_dispatch 0 violations; EVIDENCE-11 stable or flagged) are met. The DB is correct and the diff is fully explained — the baseline is still the OLD one, ready for the Plan-03 re-pin.
</success_criteria>

<output>
Create `.planning/phases/86-variant-decode-correct-db-regen/86-02-SUMMARY.md` when done. Record: the deleted rule blocks, the classify() arm order, the chip count, the diff transcript path, the check_dispatch PASS line, and explicitly whether any EVIDENCE chip moved (and its re-bench flag if so).
</output>
