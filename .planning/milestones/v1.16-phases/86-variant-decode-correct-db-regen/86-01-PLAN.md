---
phase: 86-variant-decode-correct-db-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/DECODE-NOTES.md
  - firestarter_app/tests/test_build_db_inclusion.py
  - firestarter_app/tests/test_variant_decode_evidence_stability.py
autonomous: true
requirements: [VAR-01]
must_haves:
  truths:
    - "The variant high byte's true meaning (minipro T56/T76 algo_number, NOT a classification axis) is documented and source-cited"
    - "The infoic.xml fetch is pinned to a recorded commit SHA so the regen + baseline re-pin are reproducible"
    - "A failing test asserts FM1608 -> algorithm 0x28 (40) and X88C64 -> electrical.type EEPROM (RED before Plan 02 rewrites the classifier)"
    - "A failing/asserting EVIDENCE-stability test loads the upstream-decoded v1.15 bench chips and compares algorithm/vpp_mv/pinout against the OLD baseline"
  artifacts:
    - path: "firestarter_app/tools/DECODE-NOTES.md"
      provides: "VAR-01 variant-field decode dictionary, high-byte census, pinned SHA, honest gaps"
      contains: "algo_number"
    - path: "firestarter_app/tests/test_build_db_inclusion.py"
      provides: "FM1608 0x28 + X88C64 EEPROM classification assertions"
    - path: "firestarter_app/tests/test_variant_decode_evidence_stability.py"
      provides: "D-09 EVIDENCE wire-stability data-driven test (upstream-decoded chips)"
  key_links:
    - from: "firestarter_app/tools/DECODE-NOTES.md"
      to: "minipro src/database.c#L1918"
      via: "cited pinned-SHA permalink for variant>>8 = algo_number"
      pattern: "database.c#L1918"
---

<objective>
Establish the VAR-01 documentation foundation and the Wave-0 test scaffolds that the Plan-02 classifier rewrite must satisfy. This plan writes NO production decode logic — it (a) documents the variant field in full (low + high byte) grounded in minipro source, pinning the upstream infoic.xml commit SHA for reproducibility, and (b) adds the failing/asserting tests that pin the success criteria (FM1608 -> 0x28, X88C64 -> EEPROM, EVIDENCE wire stability) BEFORE the classifier changes, so Plan 02 is a refactor-under-test.

Purpose: VAR-01 (decode documented in full + honest gaps) and the test oracle for VAR-03/VAR-04 must exist before any classifier change, per the deep-work + Nyquist rules.
Output: `firestarter_app/tools/DECODE-NOTES.md`; new assertions in `test_build_db_inclusion.py`; new `test_variant_decode_evidence_stability.py`.

Note on 2516 (VAR-05 cross-plan boundary): 2516 is one of the 11 v1.15 EVIDENCE chips, but it is ABSENT from upstream infoic.xml. Per the D-10/D-11 operator directive it is introduced first-class via a non-upstream supplement in Plan 86-04 (which runs AFTER Plan 02 regen, BEFORE the Plan-03 re-pin). This plan therefore does NOT assert anything about 2516's presence/absence — it scopes the EVIDENCE-stability oracle to the upstream-decoded EVIDENCE chips and defers all 2516 handling to 86-04.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md
@.planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author DECODE-NOTES.md (VAR-01 variant decode dictionary + pinned SHA)</name>
  <read_first>
    - firestarter_app/tools/build_db.py (the decode being documented: resolve_pinout_key variant_lo logic ~L193-270, the Rule 1/2/3 blocks ~L508-605, MINIPRO_XML_URL L11)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Variant Field Semantics — The Decisive Finding", the full high-byte census table, §"Reproducibility caveat", §"Honest Gaps"/Open Questions)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-04 decode both bytes, D-05 honest gap never guessed + provenance discretion)
    - firestarter/datasheets/README.md (Phase 85 provenance columns — the model for how datasheet grounding is recorded)
  </read_first>
  <action>
    Create `firestarter_app/tools/DECODE-NOTES.md` documenting the infoic.xml `variant` field in full per VAR-01. Sections required:
    (1) LOW byte (`variant & 0xFF`): document that it is the pinout-family sub-discriminator already consumed by `resolve_pinout_key` — cite the specific variant_lo values it switches on (0x01/0x10/0x11) and that it is NOT changing this phase.
    (2) HIGH byte (`variant >> 8`): document the source-grounded truth that it is minipro's T56/T76 `algo_number` (a per-protocol FPGA algorithm-file selector), NOT an electrical-type/algorithm/pinout classifier. Cite `minipro src/database.c#L1918` with a pinned-SHA permalink. Reproduce the full high-byte census table from RESEARCH.md (variant-hi -> count -> co-occurring (type,proto) -> algo_table family) and the two collision cells (0x41 FRAM-vs-EEPROM separated by `type`; 0x31 27512-vs-X88C64 separated by `proto`). State explicitly that classification keys on `type`/`proto_id`/`pm_idx`/`flags`, NOT on the high byte.
    (3) Pinned reproducibility: fetch the current upstream `infoic.xml` and record the exact minipro `master` commit SHA used for this regen (resolve `master` to its commit SHA via the GitLab API or `git ls-remote https://gitlab.com/DavidGriffith/minipro.git master`). Record the SHA in DECODE-NOTES.md and state the build_db.py provenance decision (per D-05 discretion: pin `MINIPRO_XML_URL` to that SHA OR record it as the regen provenance). NOTE: do not change build_db.py here — record the decision; Plan 02 applies any URL pin.
    (4) X88C64 fix rationale: `proto_id==0x34` (XICOR NovRAM) -> `electrical.type=EEPROM`; display-only (protocol-not-implemented, non-dispatchable). Cite the live tuple type=1/proto=0x34/variant=0x3100/flags=0x00414200, `flags & 0x10 == 0` (why the flags rule misses it today).
    (5) Honest gaps: list any high-byte value or chip that no minipro source or committed datasheet resolves; mark it documented-gap, never guessed (D-05). Also note here (one line) that 2516/2532 are physically-real chips ABSENT from infoic.xml — they are NOT a decode gap but are added via the Plan-86-04 non-upstream supplement (VAR-05); cross-reference it so the decode-notes reader is not misled.
    Use the existing `[VERIFIED: minipro database.c#Lxxx @ <SHA>]` citation idiom verbatim.
  </action>
  <verify>
    <automated>cd firestarter_app && grep -q "database.c#L1918" tools/DECODE-NOTES.md && grep -q "algo_number" tools/DECODE-NOTES.md && grep -Eq "0x34|X88C64" tools/DECODE-NOTES.md && grep -Eq "[0-9a-f]{8,}" tools/DECODE-NOTES.md && echo VAR01-NOTES-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "database.c#L1918" firestarter_app/tools/DECODE-NOTES.md` returns a hit (high-byte = algo_number source grounding).
    - `grep "algo_number" firestarter_app/tools/DECODE-NOTES.md` returns a hit AND the doc states the high byte is NOT a classification axis.
    - DECODE-NOTES.md contains a resolved minipro commit SHA (hex, >=8 chars) for the infoic.xml fetch.
    - DECODE-NOTES.md documents X88C64 (0x34 -> EEPROM, display-only), lists honest gaps (or states there are none), and cross-references the Plan-86-04 supplement for 2516/2532.
    - `firestarter_app/tools/build_db.py` is unchanged by this task (verified via git diff).
  </acceptance_criteria>
  <done>DECODE-NOTES.md exists, documents both variant bytes (low = pinout discriminator, high = algo_number/NOT a classifier), reproduces the high-byte census, records a pinned minipro commit SHA, documents the X88C64 0x34->EEPROM rationale, lists honest gaps, and cross-references 86-04 for 2516/2532. Grep gate prints VAR01-NOTES-OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FM1608 + X88C64 classification assertions to test_build_db_inclusion.py</name>
  <read_first>
    - firestarter_app/tests/test_build_db_inclusion.py (existing structure: `_load_db`, `_all_chips`, `_aliases`, FIRESTARTER_DB_FILE seam, taxonomy strings — DO NOT change wording of locked taxonomy)
    - firestarter_app/firestarter/data/chip_database.json (current FM1608 record: RAMTRON, algorithm 40, pinout DIP28_JEDEC_SRAM_8K, electrical.type FRAM; current X88C64 record: proto 0x34, electrical.type UV-EPROM today)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Validation Architecture" → Test Map rows for VAR-03; §"X88C64 EEPROM-type fix"; §Pitfall 5 FM1608 must stay FRAM + DIP28_JEDEC_SRAM_8K)
  </read_first>
  <behavior>
    - FM1608 record (manufacturer RAMTRON): `programming.algorithm == 40` (0x28 SRAM_STD) AND `electrical.type == "FRAM"` (the Phase-84 cosmetic relabel survives) AND `pinout == "DIP28_JEDEC_SRAM_8K"`.
    - X88C64 record (manufacturer XICOR, alias X88C64 / X88C64P): `electrical.type == "EEPROM"` AND `support_status == "protocol-not-implemented"` (display-only fix; dispatch unchanged).
  </behavior>
  <action>
    Add a new test class (e.g. `TestVariantDecodeClassification`) to `tests/test_build_db_inclusion.py` using the existing `_load_db`/`_all_chips`/`_aliases` helpers and the `FIRESTARTER_DB_FILE` seam. Add `test_fm1608_resolves_sram_std` asserting FM1608's algorithm==40, electrical.type=="FRAM", pinout=="DIP28_JEDEC_SRAM_8K". Add `test_x88c64_electrical_type_eeprom` asserting the 0x34 X88C64 chip has electrical.type=="EEPROM" and support_status=="protocol-not-implemented". These assertions describe the POST-Plan-02 state and are expected to be RED for X88C64 against the current DB (X88C64 is UV-EPROM today) and GREEN for FM1608 (already 0x28/FRAM) — record the RED expectation for X88C64 in the SUMMARY. Do not regenerate the DB in this plan.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_build_db_inclusion.py -k "fm1608 or x88c64" -q 2>&1 | grep -Eq "x88c64|X88C64|passed|failed" && echo TEST-SCAFFOLD-ADDED</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_build_db_inclusion.py -k "fm1608 or x88c64"` collects exactly the two new tests.
    - `test_fm1608_resolves_sram_std` PASSES against the current DB (FM1608 is already algorithm 40 / FRAM / DIP28_JEDEC_SRAM_8K).
    - `test_x88c64_electrical_type_eeprom` is RED against the current DB (X88C64 is electrical.type UV-EPROM today) — the gap Plan 02 closes; the RED expectation is recorded in the SUMMARY.
    - No locked taxonomy string wording is changed; `chip_database.json` is not regenerated in this plan.
  </acceptance_criteria>
  <done>Two new assertions exist and are collected by pytest; FM1608 assertion passes against the current DB, X88C64 assertion is RED (documents the gap Plan 02 closes). Grep gate prints TEST-SCAFFOLD-ADDED.</done>
</task>

<task type="auto">
  <name>Task 3: Create EVIDENCE wire-stability test (D-09 / VAR-04 oracle, upstream-decoded chips)</name>
  <read_first>
    - .planning/v1.15/bench/EVIDENCE.json (the 11 bench chips — `cells` list; chip names: W27C512, W27E512, SST27SF512, W27E040, SST39SF040, W29C020, W29C040, FM1608, ST M27C512, AM27C020, 2516 — note 2516 is the ONLY upstream-absent one)
    - firestarter_app/tools/baseline/chip_database.baseline.json (the OLD baseline these chips' algorithm/vpp_mv/pinout are compared against — this is the pre-regen reference)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"D-09 EVIDENCE-11 Risk Table" — per-chip expected wire values + the 2516 caveat: not present under bare "2516")
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-09: wire values must not silently move OR are flagged for re-bench; D-10/D-11: 2516 added via the 86-04 supplement, NOT here; SAFE-04: 2516 unstable read path / wire values must not silently move)
  </read_first>
  <action>
    Create `firestarter_app/tests/test_variant_decode_evidence_stability.py`. Load the EVIDENCE chip identifiers from EVIDENCE.json `cells` at test time (sourced, not hand-copied). Scope the stability assertions to the UPSTREAM-DECODED EVIDENCE chips — i.e. every EVIDENCE chip EXCEPT 2516 (the 10 chips that come from the infoic.xml decode). For each such chip present in the GENERATED `chip_database.json` (resolve by alias-set membership, reusing the `_aliases` idiom), assert its `programming.algorithm`, `electrical.vpp_mv`, and `pinout` equal the values in `tools/baseline/chip_database.baseline.json` (the OLD baseline) — this is the D-09 no-silent-move gate. Explicitly EXCLUDE 2516 from this test (its stability is owned by Plan 86-04, which introduces it via the non-upstream supplement and asserts its UNVERIFIED status + wire-value stability there). Add a short module docstring stating that 2516 stability is deferred to 86-04 (so a future reader does not think it was forgotten). The test reads BOTH the current DB and the OLD baseline via path constants (use the `FIRESTARTER_DB_FILE` / `FIRESTARTER_BASELINE_FILE` env seams so it can run pre- and post-regen). Design the test so that against OLD-vs-OLD it passes trivially; after Plan 02 regen it asserts each upstream-decoded EVIDENCE chip is unchanged OR (if any moved) fails loudly and surfaces the moved chip for the Plan-02/03 re-bench flag (D-09).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_variant_decode_evidence_stability.py -q 2>&1 | grep -Eq "passed|failed" && echo EVIDENCE-TEST-ADDED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_variant_decode_evidence_stability.py` loads the EVIDENCE chip identifiers sourced from `.planning/v1.15/bench/EVIDENCE.json`.
    - For each of the 10 upstream-decoded EVIDENCE chips (all EXCEPT 2516) it asserts `programming.algorithm`, `electrical.vpp_mv`, and `pinout` against `tools/baseline/chip_database.baseline.json`.
    - The test does NOT assert anything about 2516 presence/absence; the module docstring states 2516 stability is owned by Plan 86-04.
    - It uses the `FIRESTARTER_DB_FILE` / `FIRESTARTER_BASELINE_FILE` env seams and PASSES against the current (pre-regen) DB.
  </acceptance_criteria>
  <done>`test_variant_decode_evidence_stability.py` exists, loads the EVIDENCE chips from EVIDENCE.json, asserts algorithm/vpp_mv/pinout vs the OLD baseline for the 10 upstream-decoded chips, excludes 2516 (deferred to 86-04 with a docstring note), and is collected + green against the current (pre-regen) DB. Grep gate prints EVIDENCE-TEST-ADDED.</done>
</task>

</tasks>

<verification>
- `grep "database.c#L1918" firestarter_app/tools/DECODE-NOTES.md` returns a hit (VAR-01 high-byte grounding).
- A pinned minipro commit SHA is recorded in DECODE-NOTES.md.
- `pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py` collects the new tests (FM1608 green, X88C64 RED expected, EVIDENCE green vs OLD baseline).
- No production decode logic changed in this plan (build_db.py untouched).
- This plan makes NO assertion about 2516 (owned by 86-04).
</verification>

<success_criteria>
VAR-01's documentation half is satisfied (variant decoded in full, high byte documented as algo_number with census + honest gaps, fetch SHA pinned). The Plan-02 classifier rewrite has a complete test oracle: FM1608/X88C64 classification assertions + the D-09 EVIDENCE wire-stability test (upstream-decoded chips), all keyed off the OLD baseline. 2516 handling is cleanly deferred to 86-04.
</success_criteria>

<output>
Create `.planning/phases/86-variant-decode-correct-db-regen/86-01-SUMMARY.md` when done. Record the pinned minipro SHA, the X88C64 RED-assertion expectation, the 2516-deferred-to-86-04 decision, and confirm build_db.py was NOT modified.
</output>
