---
phase: 86-variant-decode-correct-db-regen
plan: 03
subsystem: build-tooling
tags: [diff_db, check_dispatch, baseline-repin, chip-database, dispatch-baseline, variant-decode, host-only, phase-gate]

# Dependency graph
requires:
  - phase: 86-variant-decode-correct-db-regen
    plan: 02
    provides: "classify()-based correct DB regen (744 chips); diff_db VARIANT_DECODE label exit 0; check_dispatch 0 violations; EVIDENCE-11 wire-stable"
  - phase: 86-variant-decode-correct-db-regen
    plan: 04
    provides: "2516 + 2532 non-upstream supplement (extra_chips.json) merged post-decode (DB 744->746); diff_db EXTRA_CHIPS_SUPPLEMENT exit 0; check_dispatch 0 violations; 2516 UNVERIFIED + wire-stable"
provides:
  - "tools/baseline/chip_database.baseline.json re-pinned byte-identical to the correct 746-chip DB (744 upstream decode + 2516/2532 supplement) — the load-bearing diff_db identity gate reference going forward"
  - "tools/baseline/dispatch_baseline.json re-pinned (746 chips) via the check_dispatch.py inline-capture derivation; meta provenance cites Phase 86 + minipro SHA a8efaedc + the VAR-05 supplement; 28C-EEPROM family now captured as configure_eeprom28c (0x0D); X88C64P keeps 0x34->not_implemented"
  - "diff_db.py now produces an IDENTITY diff (0 changed / 0 new / 0 missing, exit 0) — D-07 closed; the gate surfaces any future drift as a real change"
  - "Full Phase 86 gate proven green against the CI py3.11 target: diff_db identity + check_dispatch 0 violations + 686 host tests + 77.69% coverage + ruff/format/mypy-watermark"
affects: [87-naming-pass, 88-golden-traces, 89-recompose, 90-bench-ledger]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Re-pin-last sequencing (D-07 / RESEARCH Pitfall 4): baselines re-pinned only AFTER the Plan-02 + Plan-04 classified diff was reviewed and exited 0 — re-pinning earlier would hide the very changes the gate exists to surface"
    - "dispatch_baseline.json regenerated by faithfully mirroring check_dispatch.py's own per-chip derivation (proto -> _ALGO_MEM_TYPE | etype-fallback mem_type; dispatch() handler) — there is no committed generator, so the {meta, chips} shape was reproduced field-exact (manufacturer/part/algorithm/algorithm_id/mem_type/resolved_handler) from the 746-chip DB"
    - "Provenance-vs-gate split honored: chip_database.baseline.json is the load-bearing diff_db gate (a test consumes it); dispatch_baseline.json is provenance-only (no test reads it) — re-pinned for D-07 consistency, not correctness-load-bearing"

key-files:
  created: []
  modified:
    - firestarter_app/tools/baseline/chip_database.baseline.json
    - firestarter_app/tools/baseline/dispatch_baseline.json

key-decisions:
  - "dispatch_baseline.json regenerated (not hand-edited): no committed generator exists for it, so a faithful reproduction script mirroring check_dispatch.py main()'s exact mem_type derivation + dispatch() was used to capture the 746-chip state; meta.note updated to cite Phase 86 + minipro SHA a8efaedc + the VAR-05 supplement and to record that it supersedes the Phase 66 Plan 04 capture"
  - "The 66 dispatch_baseline changes are the legitimate variant-decode consolidation (28C-EEPROM family configure_eprom 0x07/0x0B -> configure_eeprom28c 0x0D) + the 14 prior-ERROR/None AT28C04/16 chips now resolving to configure_eeprom28c, plus the 2 supplement chips added — exactly the diff_db-explained set; X88C64P stays not_implemented (mem_type derivation None->1 is the only X88C64 movement, a benign provenance-capture effect of its new EEPROM etype)"
  - "chip_database.baseline.json re-pinned via byte-identical cp of firestarter/data/chip_database.json — diff -q reports identical; the diff_db gate is now an empty identity diff"

patterns-established:
  - "Identity-diff baseline gate: after a sanctioned DB rewrite is fully explained, re-pin makes diff_db.py an identity check (0 changed) so phases 87-90 inherit a clean DB-frozen baseline; any subsequent unsanctioned DB edit re-trips the gate"

requirements-completed: [VAR-03, VAR-04, SAFE-04]

# Metrics
duration: 17min
completed: 2026-06-25
---

# Phase 86 Plan 03: Baseline Re-Pin to the Correct 746-Chip DB Summary

**Re-pinned both diff_db baselines (`chip_database.baseline.json` byte-identical + `dispatch_baseline.json` regenerated) to the classify()-corrected 746-chip DB including the 2516/2532 non-upstream supplement — strictly LAST after the Plan-02 + Plan-04 diff was reviewed PASS-all (D-07 / RESEARCH Pitfall 4) — turning diff_db.py into an empty identity diff and proving the full Phase 86 gate green against the CI py3.11 target (686 tests, 77.69% coverage, ruff/format/mypy-watermark all clean, check_dispatch 0 violations).**

## Performance

- **Duration:** ~17 min
- **Completed:** 2026-06-25
- **Tasks:** 2
- **Files modified:** 2 (both baseline files) inside the `firestarter_app` submodule

## Accomplishments

- **VAR-03 — Baselines re-pinned to the correct DB (re-pin sequenced LAST).** Verified the hard precondition first: `tools/variant-decode-diff.txt` ends with `PASS: all 72 changed chips explained (2 new chips confirmed; 0 chips removed)` AND contains the cited non-upstream-supplement section listing 2516 + 2532 (lines 148-171). Only then re-pinned. `tools/baseline/chip_database.baseline.json` is now a byte-identical copy of `firestarter/data/chip_database.json` (`diff -q` reports identical; 746 chips incl. 2516 + 2532). `tools/baseline/dispatch_baseline.json` regenerated to 746 chips via a faithful mirror of `check_dispatch.py`'s own per-chip derivation; `meta` provenance updated to cite Phase 86 (Plan 86-03), the pinned minipro infoic.xml SHA `a8efaedc236c1d9718bd28299dfbb99536b010ff`, and the VAR-05 supplement (supersedes the Phase 66 Plan 04 744-chip capture). Frozen files (`build_db.py`, `diff_db.py`, `extra_chips.json`, `chip_database.json`, `pinouts.json`) untouched — `git diff` showed only the two baseline files. Gate: `REPIN-OK`.
- **VAR-04 — Identity gate green against the re-pinned baseline.** `python tools/diff_db.py` now reports `746 chips (baseline) == 746 chips (current)`, `0 CHANGED / 0 NEW / 0 MISSING`, `PASS: all 0 changed chips explained`, exit 0 — the empty identity diff (D-07 closed). `pytest tests/test_diff_db_gate.py` passes (4 tests; the subprocess identity-diff gate is GREEN). `python tools/check_dispatch.py` prints `0 dispatch regressions; 0 consistency violations` against the re-pinned state (746 chips, 736 supported, 10 confirmed non-dispatchable).
- **SAFE-04 — Full phase gate green (py3.11 CI target).** No write path / host guard / 2516 wire values touched (this plan only copies/regenerates two baseline JSONs). VAR-03/04/05 suite (`test_build_db_inclusion.py`, `test_variant_decode_evidence_stability.py`, `test_extra_chips_supplement.py`, `test_check_dispatch_invariants.py`) = 42 passed. Full submodule suite = **686 passed** (29 snapshots passed). Coverage = **77.69%** (>= 70% floor). Toolchain (CI-exact scope `firestarter/ tests/`): `ruff check` clean, `ruff format --check` clean (75 files), mypy watermark gate `35 == 35` exit 0 (mypy 2.1.0 confirmed actually present — not the MISSING-prints-OK trap). `messages.py` and `codec.py` unchanged (no codegen-drift concern; this plan touches no Python source).

## Task Commits

Committed atomically inside the `firestarter_app` submodule on branch `v1.16-protocol-first-architecture-rebuild`:

1. **Task 1: re-pin both baselines to the correct 746-chip DB (LAST — after diff reviewed)** — `dd541f6` (feat)
2. **Task 2: full phase gate (diff identity / dispatch / EVIDENCE / supplement / py3.11 toolchain)** — verification-only, no file changes to commit (the baselines committed in Task 1 are the only artifacts; the gate proves them).

**Plan metadata:** committed via the meta-repo final docs commit.

## Files Created/Modified

- `firestarter_app/tools/baseline/chip_database.baseline.json` (modified) — re-pinned byte-identical to the 746-chip correct DB (load-bearing diff_db gate reference).
- `firestarter_app/tools/baseline/dispatch_baseline.json` (modified) — regenerated 746-chip dispatch provenance capture; meta cites Phase 86 + SHA a8efaedc + VAR-05 supplement.

## Decisions Made

- **dispatch_baseline.json regenerated by mirroring check_dispatch.py, not hand-edited.** `check_dispatch.py` has no committed generator for this file (grep finds no writer; RESEARCH A2 anticipated this). A reproduction script mirrored `main()`'s exact derivation — `proto = chip.programming.algorithm`; `mt = _ALGO_MEM_TYPE[proto]` when proto is a known algorithm else the `electrical.type` heuristic (Flash->2 / SRAM->4 / default 1); `handler = dispatch(proto, mt)` — and emitted the field-exact `{manufacturer, part, algorithm, algorithm_id, mem_type, resolved_handler}` shape for all 746 chips, plus an updated `meta` block. Verified the only deltas vs the OLD 744-chip baseline are the 66 variant-decode consolidation rows (28C-EEPROM -> configure_eeprom28c 0x0D), the 14 previously-ERROR/None AT28C04/16 chips now reaching configure_eeprom28c, X88C64P's benign mem_type None->1 (new EEPROM etype; handler stays not_implemented), and the 2 added supplement chips — i.e. exactly the diff_db-explained set. No removed chips.
- **chip_database.baseline.json re-pinned via byte-identical copy** so the diff_db gate becomes a true identity check. This is the load-bearing gate (a test consumes it); dispatch_baseline.json is provenance-only.

## Deviations from Plan

None - plan executed exactly as written. The re-pin was sequenced last (after both 86-02 and 86-04), the precondition diff review passed, and all gates were green on the first run.

## Issues Encountered

None during planned work. Toolchain discipline notes resolved cleanly:
- **mypy MISSING trap checked:** mypy is absent from every `python3 -m mypy` import path but present as a standalone binary at `/home/vscode/.local/bin/mypy` (2.1.0); the watermark gate invokes `mypy` via subprocess (PATH-resolved) and exits 0 with `35 == 35` — confirmed it genuinely ran (the script exits 2 on a missing/broken mypy, so the OK is real).
- **py3.11 vs 3.12 divergence:** the devcontainer is py3.12; CI is py3.11. This plan changed only two JSON files (zero Python), so no version-sensitive ruff/format/codegen divergence is possible. CI lints `firestarter/ tests/` only — both clean.

## Out-of-Scope Discoveries (NOT fixed — pre-existing)

- **4 pre-existing ruff errors in `tools/` (NOT touched by this phase).** `ruff check tools/ firestarter/ tests/` reports 4 errors in `tools/audit_coverage_matrix.py`, `tools/catalog/codegen.py`, `tools/catalog/codegen_vectors.py` (and `ruff format --check` would reformat `tools/catalog/codegen.py`, `tools/catalog/codegen_vectors.py`, `tools/check_mypy_watermark.py`). Confirmed present at `dd541f6~1` (the commit before this plan). These are exactly the pre-existing errors 86-02's SUMMARY flagged. **They do NOT block CI** — `ci.yml` lints `firestarter/ tests/` only (NOT `tools/`). This plan touched zero Python files; left untouched per the scope boundary.
- **Pre-existing stray submodule working-tree changes** (`.gitignore` modified; untracked `.coverage`, `SECURITY.md`, `write_test_port.sh`) left untouched per submodule_commit_protocol — only the two baseline files this plan touched were committed.

## Next Phase Readiness

- **Phase 86 is gate-green and ready for `/gsd-verify-work`.** This is the LAST plan of Phase 86. The DB is now correct (variant-decoded, Rule 1/2/3 deleted in 86-02) and supplemented (2516/2532 in 86-04), and both baselines are re-pinned to it with the diff_db gate at identity (0 changed). The recompose phases (88-89) inherit a clean DB-frozen baseline against which their "diff_db empty every phase" precondition holds.
- 2516 stays UNVERIFIED + wire-stable (SAFE-04); host guards unchanged; no write path / VPP value moved.
- No blockers; no re-bench flags.

## Self-Check: PASSED

- FOUND: firestarter_app/tools/baseline/chip_database.baseline.json (746 chips; byte-identical to chip_database.json; 2516 + 2532 present)
- FOUND: firestarter_app/tools/baseline/dispatch_baseline.json (746 chips; meta cites Phase 86 + SHA a8efaedc + supplement)
- FOUND commit: dd541f6 (Task 1 — re-pin both baselines)
- VERIFIED: diff_db.py identity diff exit 0 (0 changed / 0 new / 0 missing)
- VERIFIED: check_dispatch.py 0 dispatch regressions; 0 consistency violations
- VERIFIED: 686 host tests passed; 77.69% coverage; ruff/format/mypy-watermark clean (CI py3.11 scope)

---
*Phase: 86-variant-decode-correct-db-regen*
*Completed: 2026-06-25*
