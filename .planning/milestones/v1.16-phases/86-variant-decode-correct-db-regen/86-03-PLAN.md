---
phase: 86-variant-decode-correct-db-regen
plan: 03
type: execute
wave: 4
depends_on: ["86-02", "86-04"]
files_modified:
  - firestarter_app/tools/baseline/chip_database.baseline.json
  - firestarter_app/tools/baseline/dispatch_baseline.json
autonomous: true
requirements: [VAR-03, VAR-04, SAFE-04]
must_haves:
  truths:
    - "chip_database.baseline.json + dispatch_baseline.json are re-pinned to the new correct DB (incl. the 2516/2532 supplement) — and ONLY after the Plan-02 + Plan-04 diff was reviewed and exit 0"
    - "test_diff_db_gate.py passes (identity diff: current DB == re-pinned baseline)"
    - "the full host toolchain (ruff / ruff format --check / mypy-watermark / pytest) passes against the CI py3.11 target"
    - "check_dispatch.py 0 violations holds against the re-pinned baseline (incl. the supplement chips)"
  artifacts:
    - path: "firestarter_app/tools/baseline/chip_database.baseline.json"
      provides: "re-pinned diff_db gate reference (new correct DB incl. supplement)"
    - path: "firestarter_app/tools/baseline/dispatch_baseline.json"
      provides: "re-pinned dispatch provenance capture"
  key_links:
    - from: "firestarter_app/tools/baseline/chip_database.baseline.json"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "byte-identical copy (re-pin); test_diff_db_gate identity diff passes"
      pattern: "PASS: all"
---

<objective>
Re-pin both baselines to the new correct DB (now including the 2516/2532 non-upstream supplement from 86-04) and run the full phase gate. This is the LAST step by design (RESEARCH §Pitfall 4 + the deep-work sequencing constraint): re-pinning before the Plan-02 + Plan-04 diff was reviewed would hide the very changes the gate exists to surface. This plan now depends on BOTH 86-02 (classify regen) AND 86-04 (supplement merge) so the re-pin captures the complete correct DB. After re-pin, prove the identity diff passes, check_dispatch stays at 0 violations, and the host toolchain is green against the CI py3.11 target (NOT the 3.12 devcontainer).

Purpose: VAR-03 completion (baselines re-pinned to the correct DB), VAR-04 (gate green against the re-pinned baseline), SAFE-04 (no write path / host guard touched; 2516 UNVERIFIED + wire values intact; toolchain green per SAFE-06 discipline).
Output: re-pinned `chip_database.baseline.json` + `dispatch_baseline.json`; full green gate transcript.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md
@.planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md
@.planning/phases/86-variant-decode-correct-db-regen/86-02-SUMMARY.md
@.planning/phases/86-variant-decode-correct-db-regen/86-04-SUMMARY.md
@firestarter_app/tools/variant-decode-diff.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-pin both baselines to the new correct DB (LAST — after diff reviewed)</name>
  <read_first>
    - firestarter_app/tools/variant-decode-diff.txt (the explained-diff transcript from Plan 02 + extended by Plan 04 — confirm it ended PASS: all and now includes the 2516/2532 supplement rows before re-pinning; this is the review gate)
    - .planning/phases/86-variant-decode-correct-db-regen/86-02-SUMMARY.md (confirm Plan 02 reached check_dispatch 0 violations + EVIDENCE stability)
    - .planning/phases/86-variant-decode-correct-db-regen/86-04-SUMMARY.md (confirm Plan 04 merged 2516/2532, check_dispatch 0 violations, 2516 UNVERIFIED + wire-stable)
    - firestarter_app/tools/baseline/chip_database.baseline.json (the OLD baseline being replaced — the diff_db gate reference)
    - firestarter_app/tools/baseline/dispatch_baseline.json (structure: {meta, chips}; meta.generated_by="check_dispatch.py inline capture"; confirmed NO test reads it — provenance-only per RESEARCH A2)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-07: re-pin both baselines AFTER every diff row is explained; D-11 extends D-07 to the supplement rows)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§Pitfall 4 re-pin sequencing; §"Code Examples" → re-pin via cp + dispatch_baseline regeneration; Open Q2 — chip_database.baseline.json is the load-bearing gate, dispatch_baseline is provenance)
  </read_first>
  <action>
    Precondition (HARD): verify `firestarter_app/tools/variant-decode-diff.txt` ends with `PASS: all` AND includes the 2516/2532 supplement rows, and that BOTH 86-02-SUMMARY.md and 86-04-SUMMARY.md confirm check_dispatch 0 violations. Only then re-pin.
    Re-pin `chip_database.baseline.json`: copy `firestarter/data/chip_database.json` (now containing the classify() decode + the 2516/2532 supplement) -> `tools/baseline/chip_database.baseline.json` (byte-identical; this is the load-bearing diff_db gate reference per Open Q2).
    Re-pin `dispatch_baseline.json`: regenerate it via the same inline-capture path that produced it (`meta.generated_by="check_dispatch.py inline capture"`; if no committed generator exists, reproduce the existing `{meta, chips}` shape from the regenerated DB and update `meta` provenance to cite Phase 86 + the pinned minipro SHA + the VAR-05 supplement). No test consumes dispatch_baseline.json (grep finds no reader) — re-pin for D-07 consistency but treat chip_database.baseline.json as the gate.
    Do NOT modify build_db.py / diff_db.py / extra_chips.json / chip_database.json / pinouts.json in this plan — they are frozen from Plans 02 + 04; this plan only re-pins baselines.
  </action>
  <verify>
    <automated>cd firestarter_app && grep -q "PASS: all" tools/variant-decode-diff.txt && diff -q firestarter/data/chip_database.json tools/baseline/chip_database.baseline.json && python -c "import json;json.load(open('tools/baseline/dispatch_baseline.json'))" && python -c "import json;d=json.load(open('tools/baseline/chip_database.baseline.json'));al=[a.split('@')[0].strip() for m in d.values() for c in m for a in c['part_number'].split(',')];assert '2516' in al and '2532' in al,'supplement not in re-pinned baseline';print('REPIN-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tools/variant-decode-diff.txt` ends with `PASS: all` AND includes the 2516/2532 supplement rows (the re-pin precondition — diff reviewed first).
    - `diff -q firestarter/data/chip_database.json tools/baseline/chip_database.baseline.json` reports the files identical (byte-exact re-pin).
    - The re-pinned baseline contains the 2516 + 2532 supplement records.
    - `dispatch_baseline.json` is valid JSON with `{meta, chips}` shape and `meta` provenance updated to cite Phase 86 + the pinned minipro SHA + the VAR-05 supplement.
    - `build_db.py`, `diff_db.py`, `extra_chips.json`, `pinouts.json`, and `chip_database.json` are unchanged by this task (git diff shows only the two baseline files).
  </acceptance_criteria>
  <done>chip_database.baseline.json is byte-identical to the regenerated chip_database.json (incl. 2516/2532); dispatch_baseline.json is valid JSON re-pinned with updated provenance; the re-pin happened only after the diff transcript showed PASS: all with the supplement rows. Gate prints REPIN-OK.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase gate — diff identity, dispatch, EVIDENCE, supplement, and py3.11 toolchain</name>
  <read_first>
    - firestarter_app/tests/test_diff_db_gate.py (the identity-diff gate: runs diff_db.py in a subprocess, asserts exit 0 + "PASS: all" — now current DB == re-pinned baseline so the diff is empty/trivially-explained)
    - firestarter_app/tests/test_extra_chips_supplement.py (the Plan-04 supplement test — now run against the re-pinned state)
    - firestarter_app/CLAUDE.md (the tooling gate: ruff check + ruff format --check + mypy strict-on-8-modules + pytest --cov-fail-under=70; build_db.py NOT in the mypy strict set per RESEARCH §Standard Stack)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Validation Architecture" → Sampling Rate "Phase gate"; §Standard Stack — Python 3.11 is the CI target, 3.12 is the devcontainer; mypy watermark via tools/check_mypy_watermark.py)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (SAFE-06 carry-over: validate against py3.11 not 3.12; generated messages.py never hand-normalized — this phase does not regenerate messages.py)
  </read_first>
  <action>
    Run the full phase gate against the re-pinned baseline. (1) `python -m pytest tests/test_diff_db_gate.py -q` — identity diff must pass (current DB == re-pinned baseline). (2) `python tools/check_dispatch.py` — must still print 0 violations against the re-pinned state (incl. 2516/2532). (3) `python -m pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py tests/test_extra_chips_supplement.py tests/test_check_dispatch_invariants.py -q` — VAR-03/VAR-04/VAR-05 assertions green. (4) `python -m pytest tests/ --cov=firestarter --cov-fail-under=70 -q` — full suite + coverage floor. (5) Toolchain: `ruff check tools/ firestarter/ tests/` and `ruff format --check tools/ firestarter/ tests/` and the mypy watermark check (`python tools/check_mypy_watermark.py` or the project's documented mypy invocation). CRITICAL: validate against the CI py3.11 target semantics, NOT the 3.12 devcontainer — if a py3.11/3.12 ruff or format divergence is possible, run them the way ci.yml runs them (per the devcontainer-py312-masks-CI note). This phase does NOT touch codec.py / messages.py, so no codegen-drift concern applies; confirm by asserting messages.py is unchanged in `git status`.
    If any gate is RED: this is a Plan-02 or Plan-04 logic defect surfaced late — record it and return for revision rather than mutating the baseline to make a gate pass.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_diff_db_gate.py tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py tests/test_extra_chips_supplement.py tests/test_check_dispatch_invariants.py -q 2>&1 | grep -Eq "passed" && python tools/check_dispatch.py | grep -Eq "0 dispatch regressions; 0 consistency violations" && ruff check tools/ firestarter/ tests/ && ruff format --check tools/ firestarter/ tests/ && echo PHASE-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_diff_db_gate.py` PASSES (identity diff: current DB == re-pinned baseline, "PASS: all").
    - `python tools/check_dispatch.py` prints `0 dispatch regressions; 0 consistency violations` against the re-pinned state (incl. 2516/2532).
    - `pytest tests/test_build_db_inclusion.py tests/test_variant_decode_evidence_stability.py tests/test_extra_chips_supplement.py tests/test_check_dispatch_invariants.py` all green.
    - `pytest tests/ --cov=firestarter --cov-fail-under=70` PASSES (full suite + 70% coverage floor).
    - `ruff check` + `ruff format --check` clean and mypy watermark green, validated against the CI py3.11 target; `git status` shows `firestarter/codec.py` and any generated `messages.py` unchanged.
  </acceptance_criteria>
  <done>test_diff_db_gate identity diff passes; check_dispatch.py 0 violations against the re-pinned baseline (incl. supplement); FM1608/X88C64/EVIDENCE/supplement/dispatch-invariant tests green; full pytest suite + 70% coverage floor green; ruff check + ruff format --check clean (py3.11-valid); mypy watermark green; messages.py unchanged. Gate prints PHASE-GATE-OK.</done>
</task>

</tasks>

<verification>
- `diff -q firestarter/data/chip_database.json tools/baseline/chip_database.baseline.json` reports identical (re-pin done; incl. 2516/2532).
- `pytest tests/test_diff_db_gate.py` passes (identity diff vs re-pinned baseline).
- `python tools/check_dispatch.py` prints 0 dispatch regressions / 0 consistency violations.
- `pytest tests/ --cov-fail-under=70` green; `ruff check` + `ruff format --check` clean against py3.11; mypy watermark green.
- Re-pin happened strictly AFTER the Plan-02 + Plan-04 diff review (variant-decode-diff.txt = PASS: all with supplement rows).
</verification>

<success_criteria>
VAR-03 is fully met (baselines re-pinned to the correct DB incl. the supplement, identity gate green), VAR-04 holds against the re-pinned baseline, and SAFE-04 is satisfied (no write path / host guard / 2516 wire values touched; 2516 UNVERIFIED preserved; toolchain green against the CI py3.11 target per SAFE-06). Phase 86 deliverables are gate-green and ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/86-variant-decode-correct-db-regen/86-03-SUMMARY.md` when done. Record the re-pin confirmation (byte-identical, incl. supplement), the full gate transcript (diff identity / check_dispatch / pytest / ruff / mypy), and confirm the re-pin was sequenced last (after both 86-02 and 86-04).
</output>
