---
phase: 86-variant-decode-correct-db-regen
plan: 04
type: execute
wave: 3
depends_on: ["86-02"]
files_modified:
  - firestarter_app/tools/extra_chips.json
  - firestarter_app/tools/build_db.py
  - firestarter_app/tools/diff_db.py
  - firestarter_app/firestarter/data/pinouts.json
  - firestarter_app/firestarter/data/chip_database.json
  - firestarter_app/tests/test_extra_chips_supplement.py
autonomous: true
requirements: [VAR-05, SAFE-04]
must_haves:
  truths:
    - "2516 and 2532 ship first-class in chip_database.json via a curated non-upstream supplement (tools/extra_chips.json) merged by build_db.py AFTER the infoic.xml decode"
    - "Each supplement field cites a datasheet; rows are clearly fenced as non-upstream"
    - "Supplement rows pass check_dispatch.py (0 violations) — 24-pin VPP-pin safety holds for these UV parts"
    - "diff_db.py explains 2516 + 2532 as cited non-upstream-supplement rows (exit 0) BEFORE the baseline re-pin"
    - "2516 retains its SAFE-04 UNVERIFIED status — resolvable but not write-graduated; its guards/wire values are not silently moved"
  artifacts:
    - path: "firestarter_app/tools/extra_chips.json"
      provides: "curated provenance-cited non-upstream chip supplement (2516, 2532)"
      contains: "2516"
    - path: "firestarter_app/tools/build_db.py"
      provides: "post-decode merge of extra_chips.json into chip_database.json"
    - path: "firestarter_app/firestarter/data/pinouts.json"
      provides: "DIP24_2532 pinout entry if 2532 is non-JEDEC vs DIP24_2732"
  key_links:
    - from: "firestarter_app/tools/build_db.py"
      to: "firestarter_app/tools/extra_chips.json"
      via: "merge supplement into complete_db AFTER the infoic.xml decode loop, before write"
      pattern: "extra_chips"
    - from: "firestarter_app/tools/diff_db.py"
      to: "firestarter_app/tools/baseline/chip_database.baseline.json"
      via: "EXTRA_CHIPS_SUPPLEMENT rule explains 2516/2532 as new non-upstream rows"
      pattern: "SUPPLEMENT"
---

<objective>
Ship the two upstream-absent 24-pin oddballs (2516, 2532) first-class in `chip_database.json` via a curated, provenance-cited non-upstream supplement (`tools/extra_chips.json`) that `build_db.py` merges AFTER the infoic.xml decode (VAR-05 / D-10). Prove the supplement rows pass the same gates as decoded chips — `check_dispatch.py` 0 violations (D-11), `diff_db.py` explains them as cited non-upstream-supplement rows (D-07 extended) — and that 2516 keeps its SAFE-04 UNVERIFIED status (resolvable, NOT write-graduated; guards/wire values unmoved).

Sequencing: runs AFTER 86-02 (the regenerated DB + classify() exist) and BEFORE 86-03 (baseline re-pin). The hard invariant holds — baseline re-pin is LAST (86-03, Wave 4).

Purpose: VAR-05 (non-upstream supplement) + SAFE-04 (2516 UNVERIFIED, wire-stable). This is NOT a return of the deleted Rule 1/2/3 — those patched chips already in infoic.xml; this adds chips with NO upstream record at all (D-10).
Output: `tools/extra_chips.json`, the build_db.py post-decode merge, a DIP24_2532 pinout entry if needed, an extended diff_db.py supplement rule, a supplement test, the regenerated DB with 2516+2532.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md
@.planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md
@.planning/phases/86-variant-decode-correct-db-regen/86-02-SUMMARY.md
@firestarter_app/tools/DECODE-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify/add the 2532 pinout + author tools/extra_chips.json (provenance-cited)</name>
  <read_first>
    - firestarter_app/firestarter/data/pinouts.json (existing 24-pin entries: DIP24_2716 [vpp-pin 21, vcc 24, gnd 12], DIP24_2732 [vpp-pin 20=oe, A11 on pin 21], DIP24_6116, DIP24_2816 — the schema/shape a new DIP24_2532 must follow)
    - firestarter/datasheets/0x0B-EPROM-LEGACY/2516_EPROM.pdf (the committed 2516 datasheet — Phase 85; the cited source for the 2516 supplement row)
    - firestarter/datasheets/README.md (datasheet index + provenance-column convention; where a 2532 datasheet would be filed if acquired under D-05)
    - .planning/v1.15/DECODE-AUDIT.md (the v1.15 2516 user-override wire values: algorithm 0x0B, pinout DIP24_2716, UV-EPROM, vpp_mv 25000, size_bytes 2048 — the wire values 2516 must NOT silently move from; 2516 read ANOMALY / UNVERIFIED status)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-10 supplement mechanism; D-11 gates + 2516 UNVERIFIED + each field cites a datasheet; Claude's Discretion on supplement file format)
  </read_first>
  <action>
    First resolve the 2532 pinout: the TI 2532 is a 4KB 24-pin UV-EPROM whose pin assignment differs from the JEDEC 2732 (notably the VPP / OE / A11 pin roles on pins 18/20/21 differ). Compare the 2532 datasheet pinout against the existing `DIP24_2732` entry in `pinouts.json`. If they differ in ANY pin role (vpp-pin, oe-pin, ce-pin, address-bus-pins, vcc/gnd), add a new `DIP24_2532` entry to `pinouts.json` modeling the 2532's true pin map (mirror the DIP24_2716/DIP24_2732 schema: vcc-pin, gnd-pin, vpp-pin, ce-pin, oe-pin, address-bus-pins, data-bus-pins, static-high-pins, VCC_READ/VCC_PROG). If they are byte-identical, reuse DIP24_2732 and record that finding. If no committed 2532 datasheet exists, acquire one (operator-authorized D-05) and file it under the appropriate `datasheets/<bucket>/` folder + README provenance, OR if unobtainable record an honest gap and model the pinout from a cited secondary source — never guess silently.
    Then author `firestarter_app/tools/extra_chips.json`: a curated supplement with one record each for 2516 and 2532, in the SAME schema build_db.py emits per chip (manufacturer-keyed; each chip a dict with part_number, support_status, electrical{type,size_bytes,pin_count,vpp,vpp_mv,vcc,vdd}, programming{algorithm,pulse_duration,chip_id_check,chip_id_value}, pinout). Each record carries an explicit non-upstream provenance marker (e.g. a `"source": "non-upstream-supplement"` field and a `"datasheet": "<path>"` citation per the field-cites-a-datasheet rule, D-11). 2516 row: algorithm 0x0B, pinout DIP24_2716, electrical.type UV-EPROM, vpp_mv 25000, size_bytes 2048 (matching the v1.15 wire values exactly — do NOT move them), support_status reflecting its UNVERIFIED/non-write-graduated status (resolvable for read/info but not a write-proven graduation; keep its host-guard posture). 2532 row: 4KB UV-EPROM, the resolved pinout (DIP24_2532 or DIP24_2732), algorithm + VPP from the datasheet. Do NOT inline implementation code into this plan — author the JSON file and pinout entry directly.
  </action>
  <verify>
    <automated>cd firestarter_app && python -c "import json;e=json.load(open('tools/extra_chips.json'));pn=[c['part_number'] for m in e.values() for c in m];assert any('2516' in p for p in pn) and any('2532' in p for p in pn),pn;po=json.load(open('firestarter/data/pinouts.json'));s=[c.get('pinout') for m in e.values() for c in m];assert all(x in po for x in s),('pinout missing',s);print('SUPPLEMENT-AUTHORED-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/extra_chips.json` exists with a 2516 record AND a 2532 record, each carrying a non-upstream provenance marker and a datasheet citation field (D-11).
    - The 2516 record's wire values match the v1.15 user-override exactly: algorithm 0x0B, pinout DIP24_2716, electrical.type UV-EPROM, vpp_mv 25000, size_bytes 2048 (SAFE-04 — not silently moved).
    - The 2516 record's support_status reflects UNVERIFIED/non-write-graduated (resolvable but not a write-proven graduation).
    - Either a `DIP24_2532` pinout entry was added to `pinouts.json` (if 2532 differs from DIP24_2732) OR the SUMMARY records that 2532 reuses DIP24_2732 with the datasheet-verified justification.
    - Every `pinout` referenced by an extra_chips.json record exists in `pinouts.json`.
  </acceptance_criteria>
  <done>tools/extra_chips.json carries provenance-cited 2516 + 2532 records; the 2532 pinout is resolved (new DIP24_2532 entry or justified DIP24_2732 reuse); 2516 wire values match v1.15 and its status is UNVERIFIED. Gate prints SUPPLEMENT-AUTHORED-OK.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the supplement in build_db.py (post-decode) + regenerate the DB</name>
  <read_first>
    - firestarter_app/tools/build_db.py (the main() decode loop that builds `complete_db` per manufacturer ~L297-776, and the `json.dump(complete_db, ...)` write ~L777-778 — the merge must land AFTER the loop and BEFORE the write; the classify() from Plan 02 is already in place)
    - firestarter_app/tools/extra_chips.json (the supplement authored in Task 1 — the schema to merge)
    - firestarter_app/tools/DECODE-NOTES.md (the pinned SHA + provenance conventions; add a note that the supplement is merged post-decode)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-10: merge AFTER the infoic.xml decode, ship first-class in chip_database.json; NOT a return of Rule 1/2/3)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"Runtime State Inventory" — chip_database.json is the regenerated artifact; the user-override layer ~/.firestarter is a SEPARATE concern, not this)
  </read_first>
  <action>
    In `firestarter_app/tools/build_db.py`, after the per-manufacturer decode loop completes `complete_db` and BEFORE `json.dump(...)`, load `tools/extra_chips.json` and merge its manufacturer-keyed records into `complete_db` (append to the existing manufacturer list if the key exists, else create it). Keep the merge minimal and clearly commented as the VAR-05 non-upstream supplement (cite D-10). Preserve total chip count reporting so the supplement count is visible (e.g. print the upstream count + supplement count). Do NOT route the supplement through classify() or resolve_pinout_key — these chips arrive fully-specified from extra_chips.json (that is the point of a curated supplement; they have no infoic.xml fields to decode). Then run `python tools/build_db.py` to regenerate `firestarter/data/chip_database.json` so it now contains the 2516 + 2532 records. Confirm the supplement records are byte-faithful to extra_chips.json (the merge must not mutate their wire values).
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/build_db.py >/dev/null 2>&1 && python -c "import json;d=json.load(open('firestarter/data/chip_database.json'));al=[a.split('@')[0].strip() for m in d.values() for c in m for a in c['part_number'].split(',')];assert '2516' in al and '2532' in al,'2516/2532 not merged';c2516=[c for m in d.values() for c in m if '2516' in {a.split('@')[0].strip() for a in c['part_number'].split(',')}][0];assert c2516['programming']['algorithm']==11 and c2516['pinout']=='DIP24_2716' and c2516['electrical']['vpp_mv']==25000,c2516;print('MERGE-OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `build_db.py` loads `tools/extra_chips.json` and merges it into `complete_db` AFTER the decode loop and BEFORE the JSON write, with a comment citing VAR-05 / D-10.
    - The regenerated `chip_database.json` contains 2516 and 2532 records (resolved by alias-set membership).
    - The 2516 record in the regenerated DB has algorithm 0x0B (11), pinout DIP24_2716, vpp_mv 25000 — byte-faithful to extra_chips.json (no mutation by the merge).
    - The supplement records are NOT passed through classify()/resolve_pinout_key (they arrive fully-specified).
    - build_db.py prints/records the supplement count distinctly from the upstream decode count.
  </acceptance_criteria>
  <done>build_db.py merges extra_chips.json post-decode; the regenerated DB contains 2516 + 2532 with byte-faithful wire values; the supplement count is reported. Gate prints MERGE-OK.</done>
</task>

<task type="auto">
  <name>Task 3: Extend diff_db.py supplement rule + prove check_dispatch + supplement test (D-11 / SAFE-04)</name>
  <read_first>
    - firestarter_app/tools/diff_db.py (the `_RATIONALES`, `_RULE_FIELD_PATHS`, `_classify_diff` priority chain, and the NEW-chips reporting path ~L525-543 — new chips (2516/2532) appear as `new_chips`; the gate currently expects new chips to be "Rule 1 unblock". The supplement chips are a NEW category of new-chip that must be explained, not flagged)
    - firestarter_app/tools/check_dispatch.py (the structural guards: novpp_in_eprom GATE-03 needs DIP24_2716/DIP24_2532 to HAVE a vpp-pin; the D-10 assertions; UNCHANGED — just run it)
    - firestarter_app/firestarter/data/pinouts.json (confirm DIP24_2716 and the 2532 pinout both expose a vpp-pin so the 24-pin UV parts pass the structural VPP guard)
    - .planning/phases/86-variant-decode-correct-db-regen/86-CONTEXT.md (D-11: supplement rows pass check_dispatch 0 violations + diff_db shows them as cited non-upstream-supplement rows; SAFE-04: 2516 guards/wire values not silently moved)
    - .planning/phases/86-variant-decode-correct-db-regen/86-RESEARCH.md (§"diff_db.py re-pin mechanics"; the new-chips reporting + exit-code contract)
  </read_first>
  <action>
    Extend `firestarter_app/tools/diff_db.py` so 2516 + 2532 are explained as cited non-upstream-supplement NEW chips rather than tripping the "expected Rule 1 unblock" WARN or any unexplained-diff path. Add an `EXTRA_CHIPS_SUPPLEMENT` (or `SUPPLEMENT`) rationale to `_RATIONALES` citing VAR-05 / D-10 and the datasheet provenance, and update the new-chips handling so a new chip whose record carries the non-upstream `source` marker (matching extra_chips.json) is reported under the supplement label (its presence is the explanation). Keep the exit-code contract: this runs against the OLD baseline this plan (re-pin is 86-03), so 2516/2532 are NEW chips — they must be reported and accepted (not block exit 0) because they are cited supplement rows. Run `python tools/diff_db.py | tee tools/variant-decode-diff.txt` (this OVERWRITES/extends the Plan-02 transcript to now include the supplement rows) — it MUST still exit 0 with every changed/new row explained. Run `python tools/check_dispatch.py` — it MUST print 0 violations / 0 regressions (D-11; the 24-pin UV parts sit on a pinout WITH a vpp-pin so the structural guard is satisfied). Create `firestarter_app/tests/test_extra_chips_supplement.py` asserting: (a) 2516 + 2532 present in the generated DB with their cited wire values; (b) 2516 retains UNVERIFIED/non-write-graduated support_status and DIP24_2716/0x0B/25000mV unchanged (SAFE-04); (c) every supplement record carries a datasheet citation field; (d) check_dispatch invariants hold (or reference the gate). If check_dispatch reports a violation on a supplement chip, fix extra_chips.json (Task 1) — never weaken the gate.
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/diff_db.py | tee tools/variant-decode-diff.txt | grep -q "PASS: all" && python tools/check_dispatch.py | grep -Eq "0 dispatch regressions; 0 consistency violations" && python -m pytest tests/test_extra_chips_supplement.py -q 2>&1 | grep -Eq "passed" && echo VAR05-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tools/diff_db.py` has an `EXTRA_CHIPS_SUPPLEMENT`/`SUPPLEMENT` rationale citing VAR-05 / D-10 + datasheet provenance; 2516 + 2532 are reported as cited supplement rows (not WARN/unexplained).
    - `python tools/diff_db.py` exits 0 against the OLD baseline with every changed/new row explained; the transcript `tools/variant-decode-diff.txt` ends with `PASS: all` and now includes the supplement rows.
    - `python tools/check_dispatch.py` prints `0 dispatch regressions; 0 consistency violations` with the supplement chips present (D-11; check_dispatch.py NOT modified).
    - `tests/test_extra_chips_supplement.py` passes: 2516 + 2532 present with cited wire values; 2516 UNVERIFIED + DIP24_2716/0x0B/25000mV unchanged (SAFE-04); each supplement record cites a datasheet.
  </acceptance_criteria>
  <done>diff_db.py explains 2516/2532 as cited non-upstream-supplement rows (exit 0); check_dispatch.py 0 violations with the supplement present; the supplement test passes and pins 2516 UNVERIFIED + wire-stable. Gate prints VAR05-GATE-OK. (No baseline re-pin in this plan — that is 86-03.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated extra_chips.json -> chip_database.json -> wire `algorithm`/`vpp_mv`/`pinout` | A wrong supplement row is a physical-hardware-damage path for these 24-pin UV parts: 25V VPP must land on the chip's real VPP pin (pin 21 on the 2516 DIP24_2716 layout), not an address line. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-01 | Tampering (physical) | a supplement row routes a 24-pin UV part to configure_eprom on a pinout with no/mismatched VPP pin (25V on wrong pin) | mitigate | `check_dispatch.py` 0-violations (Task 3): the structural `novpp_in_eprom` GATE-03 requires the resolved pinout (DIP24_2716 / DIP24_2532) to expose a real vpp-pin; Task 1 verifies the 2532 pinout's vpp-pin before shipping the row (D-11). |
| T-86-04-02 | Tampering (silent regression) | the 2516 supplement silently moves its v1.15 wire values OR upgrades it past UNVERIFIED | mitigate | `test_extra_chips_supplement.py` pins 2516 to algorithm 0x0B / DIP24_2716 / 25000mV and asserts its UNVERIFIED/non-write-graduated status (SAFE-04); the row is byte-faithful from extra_chips.json. |
| T-86-04-03 | Spoofing (provenance) | a supplement row ships without a cited source / datasheet (a guessed chip) | mitigate | Task 1 + Task 3 require each supplement field to carry a datasheet citation + non-upstream `source` marker; the supplement test asserts the citation field is present (D-05/D-11; honest-gap-not-guessed). |
| T-86-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new packages (SAFE-05); the only new artifacts are extra_chips.json + a pinout entry + a test. |
</threat_model>

<verification>
- `python tools/build_db.py` regenerates the DB including 2516 + 2532 from the supplement (merged post-decode).
- `python tools/diff_db.py` exits 0 vs the OLD baseline; 2516/2532 reported as cited non-upstream-supplement rows (transcript captured).
- `python tools/check_dispatch.py` prints 0 dispatch regressions / 0 consistency violations with the supplement present.
- `pytest tests/test_extra_chips_supplement.py` green; 2516 UNVERIFIED + wire-stable.
- Baseline files NOT modified in this plan (re-pin is 86-03, Wave 4).
</verification>

<success_criteria>
VAR-05 is met (2516 + 2532 ship first-class via the curated provenance-cited non-upstream supplement merged post-decode; rows pass check_dispatch 0 violations and are explained by diff_db as cited supplement rows) and SAFE-04 holds (2516 retains UNVERIFIED/non-write-graduated status, wire values unmoved, host guards intact). The DB now carries the oddballs and the diff is fully explained — ready for the 86-03 re-pin.
</success_criteria>

<output>
Create `.planning/phases/86-variant-decode-correct-db-regen/86-04-SUMMARY.md` when done. Record: the extra_chips.json schema, the 2532 pinout decision (new DIP24_2532 vs DIP24_2732 reuse) + datasheet provenance, the merge site in build_db.py, the diff_db supplement label, the check_dispatch PASS line, and confirmation that 2516 is UNVERIFIED + wire-stable.
</output>
