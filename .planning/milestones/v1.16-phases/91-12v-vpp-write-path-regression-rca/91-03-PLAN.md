---
phase: 91-12v-vpp-write-path-regression-rca
plan: 03
type: execute
wave: 3
depends_on: ["91-02"]
files_modified:
  - .planning/v1.16/ledger/rca/91-RCA.md
  - .planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt
autonomous: true
requirements: [FIX-91]
must_haves:
  truths:
    - "If the Decision Gate left fw-vs-host ambiguous, the cheap host-axis backup A/B (v1.15 host 98b3a92, no reflash) is run to disambiguate"
    - "The fix indicated by the Plan-02 decision gate is applied (code fix, timing/timeout adjustment, OR documented accepted deferral)"
    - "The SST39SF040 write+verify is bench-confirmed byte-identical to the v1.15 baseline a38b13b4… (write -b A → verify → write -b B → verify → consistency-check --runs 3)"
    - "The recompose firmware (a296195) is restored to the board afterward and re-confirmed if the fix lives in the recompose"
    - "The firmware VPP over-voltage guard and BLOCKER-2 invariant are NOT weakened by the fix (SAFE-04 verified present + unmodified-or-stronger)"
  artifacts:
    - path: ".planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt"
      provides: "Post-fix SST39SF040 write+verify+consistency-check SHAs — the FIX-91 acceptance evidence"
      contains: "a38b13b4"
    - path: ".planning/v1.16/ledger/rca/91-RCA.md"
      provides: "Fix description + the working-write confirmation + board-restore record"
      contains: "Fix Applied"
  key_links:
    - from: ".planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt"
      to: "v1.15 baseline a38b13b4…"
      via: "byte-identity gate (the silicon truth oracle)"
      pattern: "a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b"
    - from: "the fix"
      to: "firmware VPP guard / BLOCKER-2 invariant"
      via: "SAFE-04 present-and-unweakened check"
      pattern: "vpp_check_window|500"
---

<objective>
Apply the fix indicated by the Plan-02 decision gate and CONFIRM the SST39SF040 writes clean on
the bench — the phase's headline deliverable (FIX-91). If the gate left fw-vs-host ambiguous,
first run the cheap host-axis backup A/B (no reflash). Then apply the fix for the indicated
branch and drive to a working write whose final SHA is byte-identical to the v1.15 baseline
a38b13b4…. Restore the recompose firmware to the board afterward (so the board is left on the
milestone firmware) and re-confirm the fix holds there if it lives in the recompose.

Purpose: This is FIX-91's must-prove acceptance criterion: a WORKING SST39SF040 write proven on
silicon. 91-RESEARCH's likely branch is environmental/timing/slow-path, so the candidate fixes
are weighted toward a host write-timeout increase for the flash3 slow path, a firmware
program/poll timing adjustment, or — if a genuine code cause was found — the targeted code fix.
If the evidence shows an accepted-deferral cause (a marginal individual chip with no code
defect), document it as an accepted deferral but STILL drive to a working write if at all
achievable. NEVER weaken the VPP guard or BLOCKER-2 to "make it pass" (SAFE-04 / T-91-VPP).

Output: an optional host-axis A/B record, the applied fix (committed inside the sub-repo on the
v1.16 branch — gitlinks stay PINNED at b10), the post-fix SST39SF040 SHA evidence == a38b13b4…,
and the board-restore record.

ALL tasks autonomous: every op is on the seated SST39SF040 the operator authorized.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md
@.planning/v1.16/ledger/rca/91-RCA.md
@.planning/v1.16/ledger/bench/BENCH-LOG.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: (Conditional) host-axis backup A/B + apply the indicated fix</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md, firestarter/src/proms/*.cpp OR firestarter_app/firestarter/*.py (only if a code fix is indicated)</files>
  <read_first>
    - .planning/v1.16/ledger/rca/91-RCA.md (## Decision Gate — the branch this task acts on)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Fix Decision Tree, §Pattern 1 host axis, §Code Examples host-axis A/B, §Pitfall 3 slow-path timeout)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md (D-06 gitlinks pinned; fix commits land INSIDE the sub-repo on the v1.16 branch)
    - firestarter/src/proms/flash_type_3.cpp + firestarter/src/proms/primitives.cpp (if a firmware timing/poll fix is indicated)
    - memory reference_firestarter_app_python_test_env (use /usr/local python; `pip install -e '.[test]'`)
  </read_first>
  <action>
    (A) CONDITIONAL host-axis A/B — run ONLY if the Plan-02 decision gate left fw-vs-host
    ambiguous (e.g. b10 failed identically, so the question is now host-or-environmental). With the
    recompose fw on the board (cheap — no reflash), `git -C firestarter_app worktree add /tmp/fsa-b8
    98b3a92 && cd /tmp/fsa-b8 && pip install -e .` (v1.15 host), re-run the SST39SF040 `write -b`
    cycle, then restore the v1.16 host (`cd /workspaces/firestarter_app && pip install -e .`).
    Record the outcome in 91-RCA.md (v1.15 host PASSES ⇒ host regression; FAILS ⇒ host innocent →
    environmental).
    (B) APPLY THE FIX for the gate's branch:
      - environmental/slow-path ⇒ raise the host per-operation write timeout for the flash3 slow
        path (the 524288 B SST39SF040 is a ~240s write; a ~177s host window clips it — Pitfall 3).
        Locate the timeout in firestarter_app and widen it for flash3-class sizes; commit in the host
        sub-repo on the v1.16 branch.
      - firmware timing ⇒ a targeted program/poll timing adjustment in flash_type_3.cpp /
        primitives.cpp; commit in the firmware sub-repo on the v1.16 branch.
      - genuine code cause ⇒ the targeted code fix the RCA identified.
      - accepted deferral (marginal chip, no code defect) ⇒ document the disposition in 91-RCA.md;
        still attempt the working write in Task 2 (e.g. via the timeout widening if that is the
        operative lever).
    CONSTRAINTS: do NOT weaken the VPP over-voltage guard (`vpp_check_window` +500mV) or the
    BLOCKER-2 no-12V-to-no-VPP-pin invariant (SAFE-04 / T-91-VPP). Do NOT bump meta gitlinks
    (D-06). If a firmware fix lands, rebuild + reflash will happen in Task 2. Record the chosen
    branch + the exact change (file:line, commit SHA) in 91-RCA.md under "## Fix Applied".
  </action>
  <verify>
    <automated>grep -q "Fix Applied" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - If fw-vs-host was ambiguous, the host-axis A/B (v1.15 host 98b3a92) was run and its outcome recorded; the v1.16 host was restored afterward.
    - 91-RCA.md "## Fix Applied" names the chosen branch and the exact change (file:line + commit SHA, or an explicit accepted-deferral disposition).
    - Any code fix is committed INSIDE the relevant sub-repo on branch v1.16-protocol-first-architecture-rebuild; meta gitlinks are NOT bumped (D-06).
    - The VPP guard (`vpp_check_window` +500mV) and BLOCKER-2 invariant are present + not weakened (`grep` shows the guard intact; SAFE-04).
  </acceptance_criteria>
  <done>The fix branch is chosen, the host axis is disambiguated if needed, and the fix (or accepted-deferral disposition) is applied + recorded without weakening any safety guard.</done>
</task>

<task type="auto">
  <name>Task 2: CONFIRM SST39SF040 working write — SHA byte-identical to a38b13b4… (the FIX-91 gate)</name>
  <files>.planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt, .planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/v1.16/ledger/rca/91-RCA.md (## Fix Applied — the fix to validate)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md (write-cycle method = `write -b` direct path; PASS = SHA byte-identity)
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (Image-SHA Sanity — image B == a38b13b4…)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-VALIDATION.md (Manual-Only Verifications — the FIX-91 silicon gate)
    - firestarter/CLAUDE.md (build/flash: `pio run -e leonardo`, `pio run -t upload -e leonardo`)
  </read_first>
  <action>
    If the fix was firmware-side, rebuild + reflash from the appropriate checkout: from
    /workspaces/firestarter `pio run -e leonardo && pio run -t upload -e leonardo` (Leonardo EXEMPT
    from chip-out; SST39SF040 stays seated). Confirm controller identity (`firestarter fw` →
    leonardo) and the on-board image (flash byte count). Then run the v1.15-faithful write-cycle on
    SST39SF040: `firestarter write -b SST39SF040 /tmp/firestarter_bench_p90/SST39SF040_img_A.bin`
    → `verify A` → `write -b SST39SF040 .../img_B.bin` → `verify B` → `firestarter dev
    consistency-check SST39SF040 --runs 3 --output-dir firestarter-runs/SST39SF040-fix/`. The
    PASS gate (FIX-91 must-prove): the write-cycle-final / consistency-check SHA == the v1.15
    baseline a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b. Negative control:
    `verify SST39SF040 img_A` → RC=1 (verify non-vacuous). Commit the post-fix SHAs to
    bench/SST39SF040-fix/SHA256SUMS.txt (per-dir SHA convention, no multi-MB binaries). Record the
    PASS in 91-RCA.md under "## SST39SF040 Working-Write Confirmation". If the write still cannot
    reach a38b13b4… after a reasonable fix attempt, record the residual mechanism + the
    accepted-deferral disposition honestly (do NOT fabricate a pass), but exhaust the indicated
    lever (e.g. timeout widening) first.
  </action>
  <verify>
    <automated>grep -q a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b .planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt &amp;&amp; grep -q "Working-Write Confirmation" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - The SST39SF040 write-cycle ran via `write -b A → verify → write -b B → verify → dev consistency-check --runs 3`.
    - bench/SST39SF040-fix/SHA256SUMS.txt contains the post-fix final SHA == a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b (the v1.15 baseline).
    - The negative control `verify SST39SF040 img_A` returns RC=1 (verify non-vacuous).
    - 91-RCA.md "## SST39SF040 Working-Write Confirmation" records the PASS (or, if unreachable, an honest residual-mechanism + accepted-deferral disposition after exhausting the indicated lever).
  </acceptance_criteria>
  <done>The SST39SF040 write+verify is bench-confirmed byte-identical to a38b13b4… (FIX-91 must-prove gate met), or an honest accepted-deferral disposition is recorded after exhausting the fix lever.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the recompose firmware + SAFE-04 re-verify (leave the board on milestone fw)</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md (leave the board on the recompose; gitlinks pinned at b10)
    - .planning/v1.16/ledger/rca/91-RCA.md (## Fix Applied — whether the fix lives in the recompose)
    - firestarter/CLAUDE.md (handler table — the guard sites to re-verify)
    - .planning/STATE.md (Decisions — Phase 90-03 SAFE-04 guard sites: primitives.cpp:106 +500mV; chip_resolver.py:55 support-status guard)
  </read_first>
  <action>
    Restore the milestone firmware to the board: from /workspaces/firestarter (HEAD a296195, or
    the recompose-plus-fix HEAD if a firmware fix landed there) `pio run -e leonardo && pio run -t
    upload -e leonardo`, so the board is left on the milestone firmware — NOT the b10 worktree
    image. Confirm `firestarter fw` leonardo + the recompose flash byte count. If the fix lives in
    the recompose, re-confirm the SST39SF040 write still passes a38b13b4… on the restored image
    (one `write -b B → verify B` is sufficient as a re-confirmation). SAFE-04 re-verify: grep the
    VPP over-voltage guard (`vpp_check_window` / +500mV window in primitives.cpp) and the
    chip_resolver support-status guard are PRESENT + unmodified-or-stronger; record present-only
    evidence in 91-RCA.md under "## Board Restore + SAFE-04". Clean up the b10 worktree:
    `git -C firestarter worktree remove /tmp/fs-b10` (and the host worktree if used). Do NOT bump
    meta gitlinks (D-06).
  </action>
  <verify>
    <automated>grep -q "Board Restore" .planning/v1.16/ledger/rca/91-RCA.md &amp;&amp; grep -qi "vpp_check_window\|500" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - The board is left on the milestone firmware (recompose a296195, or recompose+fix) — confirmed by `firestarter fw` + flash byte count, recorded in 91-RCA.md.
    - If the fix lives in the recompose, the SST39SF040 write was re-confirmed to pass a38b13b4… on the restored image.
    - 91-RCA.md "## Board Restore + SAFE-04" records present-only evidence that the VPP guard + support-status guard are intact (SAFE-04).
    - The /tmp/fs-b10 worktree is removed; meta gitlinks are NOT bumped (D-06).
  </acceptance_criteria>
  <done>The board is restored to the milestone firmware with the working SST39SF040 write re-confirmed, SAFE-04 re-verified intact, and the b10 worktree cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fix → firmware/host source | A fix may modify code; it must NOT weaken the VPP guard or BLOCKER-2 |
| host PC → Leonardo (reflash) | Rebuild+reflash with chip seated — operator-authorized; Leonardo EXEMPT from chip-out |
| firmware → 12V/VPP rail → socket | The program pulse routes voltage; the over-voltage guard is the safety control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-VPP | Tampering / hardware damage | firmware VPP over-voltage guard (+500mV window) + BLOCKER-2 no-12V-to-no-VPP-pin invariant | mitigate | The fix MUST NOT weaken the guard or invariant; Task 3 re-verifies both present + unmodified-or-stronger and records present-only evidence (SAFE-04). A "fix" that disables a safety check to force a pass is explicitly forbidden. |
| T-91-ID | Tampering | firmware identity confusion across reflash/restore | mitigate | `firestarter fw` + flash byte count confirmed before each silicon op and after the restore. |
</threat_model>

<verification>
- bench/SST39SF040-fix/SHA256SUMS.txt contains a38b13b4… (the FIX-91 silicon gate) OR an honest accepted-deferral is recorded.
- Negative control verify(img_A) RC=1.
- The board is left on the milestone firmware; /tmp/fs-b10 worktree removed.
- 91-RCA.md has Fix Applied + Working-Write Confirmation + Board Restore + SAFE-04 sections.
- VPP guard + support-status guard present + unweakened; meta gitlinks NOT bumped (D-06).
</verification>

<success_criteria>
FIX-91 met: the SST39SF040 write+verify is bench-confirmed byte-identical to the v1.15 baseline
a38b13b4… (or an honest accepted-deferral after exhausting the indicated lever), the board is
left on the milestone firmware, and no safety guard was weakened.
</success_criteria>

<output>
Create `.planning/phases/91-12v-vpp-write-path-regression-rca/91-03-SUMMARY.md` when done.
</output>
