---
phase: 91-12v-vpp-write-path-regression-rca
plan: 04
type: execute
wave: 4
depends_on: ["91-03"]
files_modified:
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.json
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.md
  - .planning/v1.16/ledger/bench/BENCH-LOG.md
  - .planning/v1.16/ledger/rca/91-RCA.md
  - .planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md
autonomous: false
requirements: [FIX-91]
must_haves:
  truths:
    - "The 0x06 SST39SF040 PROTOCOL-LEDGER row is dispositioned (graduated to PASS if the write now matches a38b13b4…, OR carried as a named defect with the RCA attribution)"
    - "check_ledger.py exits 0 (RC=0) with the updated ledger"
    - "The 0x07 W27C512 row is dispositioned as carried-pending-operator with the documented RCA attribution"
    - "A ready-to-run W27C512 operator bench checklist exists for return (the chip swap is operator-only — bench re-validation DEFERRED)"
    - "BENCH-LOG.md is updated with the A/B results + new SHAs"
  artifacts:
    - path: ".planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md"
      provides: "Ready-to-run W27C512 (0x07) bench re-validation checklist for operator return"
      min_lines: 25
    - path: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      provides: "Updated 0x06/0x07 rows with disposition + evidence references"
      contains: "0x06"
  key_links:
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      to: "check_ledger.py"
      via: "self-consistency gate (RC=0)"
      pattern: "PROTOCOL-LEDGER"
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.md"
      to: ".planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt"
      via: "0x06 PASS evidence reference"
      pattern: "SST39SF040-fix"
---

<objective>
Close out the RCA: disposition the 0x06 and 0x07 PROTOCOL-LEDGER rows, update BENCH-LOG.md with
the A/B + fix results, and author the ready-to-run W27C512 operator bench checklist for return.
The 0x06 row graduates to PASS if Plan 03 proved a working write (FIX-91 / LEDGER-02 for 0x06);
the 0x07 row is carried-pending-operator with the documented RCA attribution because its bench
re-validation requires an operator chip swap (DEFERRED).

Purpose: FIX-91's disposition + checklist deliverables. This is where the milestone ledger is
made honest: graduate what we proved, carry what we deferred, and leave a turnkey checklist so
the operator can finish 0x07 in one bench session without re-deriving the procedure.

Output: updated PROTOCOL-LEDGER.{json,md} (checker RC=0), updated BENCH-LOG.md, the W27C512
operator checklist, and a final RCA wrap-up in 91-RCA.md.

This plan is `autonomous: false`: the ONLY operator-blocked item in the entire phase — the
W27C512 bench re-validation (chip swap) — is captured here as a deferred operator-action task.
All other tasks (ledger disposition, BENCH-LOG update, checklist authoring) are autonomous.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md
@.planning/v1.16/ledger/rca/91-RCA.md
@.planning/v1.16/ledger/bench/BENCH-LOG.md
@.planning/v1.16/ledger/PROTOCOL-LEDGER.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disposition the 0x06/0x07 ledger rows + update BENCH-LOG (checker RC=0)</name>
  <files>.planning/v1.16/ledger/PROTOCOL-LEDGER.json, .planning/v1.16/ledger/PROTOCOL-LEDGER.md, .planning/v1.16/ledger/bench/BENCH-LOG.md</files>
  <read_first>
    - .planning/v1.16/ledger/PROTOCOL-LEDGER.md + PROTOCOL-LEDGER.json (current 0x06/0x07 FAIL-INVESTIGATE rows + the row schema / valid statuses)
    - .planning/v1.16/ledger/tools/check_ledger.py (the 0/1/2 exit-code contract + D-04 no-SHA-copy guard + LEDGER-02 PASS constraint: oracle leonardo+Rev2.0 + non-empty evidence refs)
    - .planning/v1.16/ledger/rca/91-RCA.md (Decision Gate + Fix Applied + Working-Write Confirmation — the disposition inputs)
    - .planning/v1.16/ledger/bench/SST39SF040-fix/SHA256SUMS.txt (the 0x06 PASS evidence, if achieved)
    - .planning/STATE.md (Decisions Phase 90-01/90-02 — checker contract, D-04 no-copy, _VALID_STATUSES)
  </read_first>
  <action>
    Update PROTOCOL-LEDGER.json + .md in lockstep:
    (a) 0x06 SST39SF040 — if Plan 03 confirmed the write == a38b13b4…, graduate the row to PASS
    with oracle `leonardo+Rev2.0` and evidence refs pointing at bench/SST39SF040-fix/ (satisfies
    LEDGER-02's structural PASS constraint + D-09). If Plan 03 recorded an accepted deferral,
    carry 0x06 as a named defect with the RCA attribution + the residual-mechanism note. Honor
    D-04: reference SHAs by evidence path, do NOT copy the SHA into the ledger row (the checker
    guards this).
    (b) 0x07 W27C512 — set the row to carried-pending-operator (a status the checker accepts; if a
    new status string is needed, mirror the Phase-90 pattern of adding it to _VALID_STATUSES in
    check_ledger.py as a 1-line change, with the addition justified) with the documented RCA
    attribution (FLAG_CAN_ERASE auto-erase pulse under-performing on this board / the Plan-02
    both-symptom explanation) and a pointer to the operator checklist (Task 2).
    (c) Update BENCH-LOG.md with the A/B results + new SHAs (recompose vs b10 outcomes from Plan 02,
    the post-fix SST39SF040 SHA from Plan 03), appended as a "## Phase 91 A/B + Fix Results"
    section — do NOT rewrite the Phase-90 history.
    Run `python3 .planning/v1.16/ledger/tools/check_ledger.py` and confirm RC=0.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; python3 .planning/v1.16/ledger/tools/check_ledger.py; test $? -eq 0 &amp;&amp; grep -q "Phase 91" .planning/v1.16/ledger/bench/BENCH-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The 0x06 row is either PASS (oracle leonardo+Rev2.0 + non-empty evidence refs to bench/SST39SF040-fix/) OR a named defect with RCA attribution — matching the Plan-03 outcome.
    - The 0x07 row is carried-pending-operator with the documented RCA attribution + a pointer to the operator checklist.
    - `python3 .planning/v1.16/ledger/tools/check_ledger.py` exits 0 (RC=0); D-04 no-SHA-copy guard holds.
    - PROTOCOL-LEDGER.json and .md are in lockstep; BENCH-LOG.md has a "## Phase 91 A/B + Fix Results" section with the new SHAs (Phase-90 history not rewritten).
  </acceptance_criteria>
  <done>Both ledger rows are dispositioned per the RCA outcome, the checker passes RC=0, and BENCH-LOG records the Phase-91 A/B + fix results.</done>
</task>

<task type="auto">
  <name>Task 2: Author the ready-to-run W27C512 operator bench checklist + RCA wrap-up</name>
  <files>.planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md, .planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (Chip 2 W27C512 block — exact failure: bad bytes:921 @0x000000, clean 12.0V rail, image B e16b2a5b…)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Pitfall 4 W27C512 bad-bytes = erase-rail/chip-state; §Code Examples vpp/erase-rail-hold diagnostics; §W27C512 Operator Checklist)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md (0x07 bench DEFERRED; write-cycle = `write -b`; controller identity per op)
    - memory reference_w27c512_bench_write_erase_gotcha + reference_v114_bench_erase_rail_and_test_artifact (erase-rail hold `dev reg 0 0 0x86 -f`; bad-bytes is chip-state not transport)
  </read_first>
  <action>
    Author .planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md as a turnkey single-session
    bench procedure for operator return: (1) preconditions — verify controller identity
    (`firestarter fw` → leonardo on /dev/ttyACM0), confirm Rev 2.0 silkscreen, seat W27C512 ONLY,
    confirm milestone firmware on the board (flash byte count); (2) the RCA hypothesis to test —
    FLAG_CAN_ERASE auto-erase pulse under-performing / VPP rail under load at write-start (the
    bad-bytes-~921-@0x0 mechanism); (3) the diagnostics — capture loaded VPP with `timeout -s INT 20
    stdbuf -oL firestarter vpp` during a write attempt, optionally hold the erase rail with
    `firestarter dev reg 0 0 0x86 -f` for a DMM reading; (4) the write-cycle — v1.15 `write -b A →
    verify → write -b B → verify → dev consistency-check --runs 3`; PASS gate = final SHA ==
    e16b2a5b26d99440a8e596963faa0f2d64fff4e1dd9682b93b2f8f1ddc326ab5 (W27C512 image B v1.15
    baseline); (5) the apply-the-91-fix step if the Plan-03 fix is a candidate for 0x07 too; (6) the
    disposition step — graduate the 0x07 ledger row to PASS on success or update the carried-defect
    note. Then append a "## Phase 91 Wrap-Up" section to 91-RCA.md summarizing: cause attribution
    (RCA-91), the SST39SF040 outcome (FIX-91), the 0x07 deferral, and the LEDGER-02 status for 0x06.
  </action>
  <verify>
    <automated>test -s .planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md &amp;&amp; grep -q e16b2a5b26d99440a8e596963faa0f2d64fff4e1dd9682b93b2f8f1ddc326ab5 .planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md &amp;&amp; grep -q "Wrap-Up" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - W27C512-OPERATOR-CHECKLIST.md is non-empty (≥25 lines) and contains the W27C512 image B PASS gate SHA e16b2a5b26d99440a8e596963faa0f2d64fff4e1dd9682b93b2f8f1ddc326ab5.
    - The checklist covers: controller-identity precondition, the RCA hypothesis to test, the loaded-VPP / erase-rail-hold diagnostics, the `write -b` write-cycle, the PASS gate, and the ledger-disposition step.
    - 91-RCA.md "## Phase 91 Wrap-Up" summarizes the cause attribution (RCA-91), the SST39SF040 outcome (FIX-91), the 0x07 deferral, and the LEDGER-02 status for 0x06.
  </acceptance_criteria>
  <done>A turnkey W27C512 operator bench checklist exists for return and the RCA is wrapped up in 91-RCA.md.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [DEFERRED — OPERATOR ONLY] W27C512 (0x07) bench re-validation</name>
  <what-built>
    The RCA analysis, fix design, ledger disposition (carried-pending-operator), and the
    ready-to-run W27C512-OPERATOR-CHECKLIST.md are all authored autonomously (Tasks 1–2). The
    only remaining item is the live W27C512 bench re-validation, which requires a chip swap —
    an operator-only physical action. The seated chip this session is SST39SF040; swapping in
    W27C512 cannot be automated.
  </what-built>
  <how-to-verify>
    DEFERRED to operator return. When ready, follow
    .planning/v1.16/ledger/rca/W27C512-OPERATOR-CHECKLIST.md end-to-end:
    1. Verify controller identity (`firestarter fw` → leonardo on /dev/ttyACM0) + Rev 2.0 silkscreen.
    2. Seat W27C512 ONLY; confirm milestone firmware on the board (flash byte count).
    3. Run the loaded-VPP / erase-rail diagnostics from the checklist.
    4. Run the `write -b A → verify → write -b B → verify → dev consistency-check --runs 3` cycle.
    5. PASS gate = final SHA == e16b2a5b26d99440a8e596963faa0f2d64fff4e1dd9682b93b2f8f1ddc326ab5.
    6. Update the 0x07 PROTOCOL-LEDGER row (graduate to PASS or update the carried-defect note);
       confirm `check_ledger.py` RC=0.
  </how-to-verify>
  <resume-signal>This task stays open/deferred until the operator returns and runs the checklist; it does NOT block phase completion for the SST39SF040 (0x06) deliverable. Mark resolved only after the operator records the W27C512 bench outcome.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger disposition → milestone truth | The ledger must honestly reflect proven vs deferred; no auto-pass of unproven rows (D-03) |
| operator W27C512 bench (deferred) → 12V/VPP rail | The deferred bench op routes 12V; the checklist keeps the VPP guard intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-VPP | Tampering / hardware damage | firmware VPP over-voltage guard + BLOCKER-2 invariant (relevant to the deferred W27C512 bench) | mitigate | The operator checklist runs the standard `write -b` path with the guard intact; it does NOT instruct weakening any safety check. SAFE-04 unchanged by this documentation-only plan. |
| T-91-LEDGER | Repudiation | PROTOCOL-LEDGER honesty | mitigate | 0x06 graduated only on a real a38b13b4… match; 0x07 carried-pending-operator (never auto-passed, D-03); `check_ledger.py` RC=0 enforces self-consistency + D-04 no-SHA-copy. |
</threat_model>

<verification>
- `python3 .planning/v1.16/ledger/tools/check_ledger.py` exits 0 (RC=0).
- PROTOCOL-LEDGER.{json,md} 0x06/0x07 rows dispositioned per the RCA outcome; lockstep.
- BENCH-LOG.md has the Phase-91 A/B + fix results section.
- W27C512-OPERATOR-CHECKLIST.md exists (≥25 lines) with the e16b2a5b… PASS gate.
- 91-RCA.md has the Phase 91 Wrap-Up section.
- W27C512 bench re-validation is the ONLY operator-deferred item (Task 3, checkpoint:human-action).
</verification>

<success_criteria>
The 0x06/0x07 ledger rows are honestly dispositioned (checker RC=0), BENCH-LOG records the
Phase-91 results, and a turnkey W27C512 operator checklist is left for return — closing FIX-91's
disposition + checklist deliverables, with the W27C512 live bench as the sole deferred item.
</success_criteria>

<output>
Create `.planning/phases/91-12v-vpp-write-path-regression-rca/91-04-SUMMARY.md` when done.
</output>
