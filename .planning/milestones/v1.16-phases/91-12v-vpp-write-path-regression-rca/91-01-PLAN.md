---
phase: 91-12v-vpp-write-path-regression-rca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.16/ledger/rca/91-RCA.md
  - .planning/v1.16/ledger/rca/diff-forensics.txt
autonomous: true
requirements: [RCA-91]
must_haves:
  truths:
    - "The recompose write-path diff (a1953c2..a296195) is confirmed comment-only on flash3 (0x06) and measurement-only on eprom.cpp (0x07) — no behavioral write-path delta"
    - "The flash3 0x06 write bus sequence is byte-identical pre/post recompose (native golden trace PASSES)"
    - "The eprom 0x07 write+chip-id bus sequence is byte-identical pre/post recompose (native golden trace PASSES)"
    - "The b10 baseline firmware image is built and staged for the bench A/B without disturbing the recompose checkout"
    - "The A/B image SHAs (image A seed 1, image B seed 2) are regenerated and confirmed byte-identical to the v1.15 baseline before any silicon op"
  artifacts:
    - path: ".planning/v1.16/ledger/rca/91-RCA.md"
      provides: "RCA working document: diff-forensics verdict, native-test confirmation, A/B hypothesis tree, prep status"
      min_lines: 40
    - path: ".planning/v1.16/ledger/rca/diff-forensics.txt"
      provides: "Captured git-diff output for the 0x06/0x07 write path (the primary RCA artifact)"
  key_links:
    - from: ".planning/v1.16/ledger/rca/91-RCA.md"
      to: "git diff a1953c2..a296195"
      via: "diff-forensics.txt capture"
      pattern: "a1953c2"
    - from: "pio test -e native"
      to: "test/native/avr/test_val_flash3 + test_val_eprom"
      via: "native golden trace run"
      pattern: "test_val_(flash3|eprom)"
---

<objective>
Establish the no-hardware analytic foundation of the A/B-first decision tree: confirm the
diff-forensics claim (the Phase-89 recompose introduced ZERO behavioral write-path delta on
either failing chip), prove it with the existing native Unity golden traces, regenerate +
verify the A/B images, and build the b10 baseline firmware in an isolated worktree so the
bench A/B in Plan 02 can run immediately.

Purpose: The CONTEXT prior is "recompose regressed the write path." 91-RESEARCH proves that
prior is contradicted by the diffs. This plan converts that research claim into committed,
reproducible evidence (captured diff + green native traces) and pre-stages everything the
bench A/B needs, so Wave 2 is pure silicon. This is the PRIMARY branch of the decision tree:
"recompose is innocent; the failure is environmental/pre-existing."

Output: `91-RCA.md` working document (forensics verdict + A/B hypothesis tree + prep status),
captured `diff-forensics.txt`, green native traces, a staged `/tmp/fs-b10` b10 build, and
re-verified A/B images. NO firmware/host source is modified (analysis + prep only).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md
@.planning/v1.16/ledger/bench/BENCH-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture + verify the 0x06/0x07 write-path diff forensics (recompose innocence, static)</name>
  <files>.planning/v1.16/ledger/rca/diff-forensics.txt, .planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Root-Cause Analysis, §Sources Primary — the exact hunks this task must reproduce)
    - firestarter/src/proms/flash_type_3.cpp (0x06 handler — claim: comment-only delta)
    - firestarter/src/proms/eprom.cpp (0x07 handler — claim: only eprom_check_vpp + chip-id extracted; eprom_write_execute untouched)
    - firestarter/src/proms/primitives.cpp + firestarter/include/primitives.h (P3/P4/P5 bodies — claim: byte-identical to originals)
    - firestarter/CLAUDE.md (handler/VPP table: 0x06 = None/5V, 0x07 = 13V via VPE_DROP)
  </read_first>
  <action>
    Operating inside the firestarter sub-repo at /workspaces/firestarter (HEAD a296195, branch
    v1.16-protocol-first-architecture-rebuild — confirm with `git rev-parse HEAD`, do NOT move it).
    Capture `git diff a1953c2..a296195 -- src/proms/eprom.cpp src/proms/flash_type_3.cpp
    src/proms/flash_type_4.cpp src/proms/primitives.cpp include/primitives.h
    src/proms/flash_utils.cpp include/flash_utils.h` into
    .planning/v1.16/ledger/rca/diff-forensics.txt (create the rca/ dir). Also capture the host
    DB parity check: for SST39SF040 and W27C512, `git -C firestarter_app show 98b3a92:firestarter/data/chip_database.json`
    vs `e46549f:...` extracted by part_number, and record whether the algorithm/vpp_mv/pulse/size/flags
    fields are byte-identical (per RESEARCH they are). Classify each changed file: comment-only,
    measurement-only-extraction, or behavioral. Write the verdict into 91-RCA.md under a
    "## Diff Forensics" section, explicitly NAMING and EXONERATING P3 vpp_check_window (body
    byte-identical; flash3 has no P3) so the RCA formally closes the "revert the recompose"
    hypothesis. Do NOT modify any source file. Do NOT use a fenced diff inside 91-RCA.md — cite
    file:line ranges and point to diff-forensics.txt for the raw hunks.
  </action>
  <verify>
    <automated>test -s .planning/v1.16/ledger/rca/diff-forensics.txt &amp;&amp; grep -q "Diff Forensics" .planning/v1.16/ledger/rca/91-RCA.md &amp;&amp; grep -qi "vpp_check_window" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - diff-forensics.txt exists and is non-empty (contains the captured `git diff a1953c2..a296195` hunks for the named files).
    - 91-RCA.md "## Diff Forensics" section classifies flash_type_3.cpp as comment-only and eprom.cpp as check_vpp+chip-id extraction only (eprom_write_execute UNTOUCHED).
    - 91-RCA.md records that the SST39SF040 + W27C512 chip_database.json wire params are byte-identical 98b3a92 vs e46549f.
    - 91-RCA.md explicitly exonerates P3 vpp_check_window (body byte-identical; flash3 has no P3).
    - No file under firestarter/src or firestarter/include is modified (`git -C firestarter status --porcelain src include` empty).
  </acceptance_criteria>
  <done>The diff is captured and the recompose-innocence verdict for both 0x06 and 0x07 write paths is committed in 91-RCA.md with the raw hunks in diff-forensics.txt.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the bus sequence is unchanged via native golden traces (no board)</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - firestarter/CLAUDE.md (## Native (Host) Test Environment — `pio test -e native` invocation)
    - firestarter/test/native/avr/test_val_flash3/ (0x06 write golden trace, 12-entry, Phase 88-03)
    - firestarter/test/native/avr/test_val_eprom/ (0x07 write+chip-id golden traces, Phase 88)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-VALIDATION.md (Per-Task Verification Map rows 91-01-02/03)
  </read_first>
  <action>
    From /workspaces/firestarter run `pio test -e native -f "*test_val_flash3*"` and
    `pio test -e native -f "*test_val_eprom*"`. These golden traces were pinned at Phase 88 on
    the PRE-recompose handlers and re-run green at Phase 89 on a296195 — re-running them now
    re-confirms the recompose preserves the exact 0x06 write bus sequence (12-entry) and the
    0x07 write+chip-id sequence. Record the PASS counts + any WARNING/severity-fork note (per
    memory reference_golden_trace_misses_severity_fork: golden traces with a matching id can
    miss the WARNING-vs-ERROR fork — note this as a known coverage caveat, it is analysis-only
    here, not a fix). Append a "## Native Trace Confirmation" section to 91-RCA.md with the exact
    commands, PASS counts, and the conclusion: the bus sequence is behavior-preserving, so any
    bench failure must be rail/timing/chip-state on real silicon, not a bus-sequence code change.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter &amp;&amp; pio test -e native -f "*test_val_flash3*" &amp;&amp; pio test -e native -f "*test_val_eprom*"</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "*test_val_flash3*"` PASSES (exit 0).
    - `pio test -e native -f "*test_val_eprom*"` PASSES (exit 0).
    - 91-RCA.md "## Native Trace Confirmation" records both commands, PASS counts, and the severity-fork coverage caveat.
    - The conclusion (bus sequence preserved → bench failure is rail/timing/chip-state, not a bus-sequence change) is stated in 91-RCA.md.
  </acceptance_criteria>
  <done>Both native suites pass and 91-RCA.md documents that the recompose preserves the 0x06/0x07 write bus sequence at the unit level.</done>
</task>

<task type="auto">
  <name>Task 3: Stage A/B images + build the b10 baseline firmware in an isolated worktree</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Pattern 2 worktree b10 build, §Pattern 3 firmware identity, §Code Examples)
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (Image-SHA Sanity table — image A/B SHAs per chip; the SST39SF040 image B gate a38b13b4…)
    - firestarter/CLAUDE.md (build commands: `pio run -e leonardo`)
    - firestarter_app/tools/gen_test_image.py (deterministic generator: seed 1 = image A, seed 2 = image B)
  </read_first>
  <action>
    (a) Confirm the staged images at /tmp/firestarter_bench_p90/SST39SF040_img_{A,B}.bin exist
    and their SHAs match the BENCH-LOG Image-SHA-Sanity table (image B == a38b13b4…). If absent,
    regenerate via `python3 firestarter_app/tools/gen_test_image.py 524288 1 .../SST39SF040_img_A.bin`
    and seed 2 for image B, then re-verify the SHAs. (b) Build the b10 baseline WITHOUT moving the
    recompose HEAD: `git worktree add /tmp/fs-b10 a1953c2` (detached at tag 3.0.0b10), then
    `cd /tmp/fs-b10 && pio run -e leonardo`. Record the flash byte count — expect ~25654 B (b10),
    distinct from the recompose 25136 B — this is the ONLY reliable in-band identity discriminator
    because `firestarter fw` reports 3.0.0b10 for BOTH. Do NOT flash the board in this task (no
    hardware here). If the worktree build fails under the current toolchain (Assumption A5), fall
    back to a throwaway clone at a1953c2 and record the deviation. Append a "## A/B Prep" section to
    91-RCA.md: image SHA confirmation, b10 flash byte count, recompose flash byte count (25136 B
    from BENCH-LOG), and the identity-disambiguation rule (flash size, not version string).
  </action>
  <verify>
    <automated>sha256sum /tmp/firestarter_bench_p90/SST39SF040_img_B.bin | grep -q a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b &amp;&amp; test -f /tmp/fs-b10/.pio/build/leonardo/firmware.hex &amp;&amp; grep -q "A/B Prep" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - `sha256sum /tmp/firestarter_bench_p90/SST39SF040_img_B.bin` == `a38b13b4d285756c1f385a75d0cdf89f72720764c21fd933ced75ebdd970b96b` (the FIX-91 gate image).
    - A b10 firmware image exists at /tmp/fs-b10/.pio/build/leonardo/ (or a documented throwaway-clone fallback) with its flash byte count recorded (~25654 B).
    - The recompose HEAD is still a296195 (`git -C firestarter rev-parse HEAD` == a296195…); the worktree did not move it.
    - 91-RCA.md "## A/B Prep" records both flash byte counts and the flash-size-not-version-string identity rule.
  </acceptance_criteria>
  <done>A/B images are SHA-verified, the b10 baseline image is built in /tmp/fs-b10 with the recompose HEAD untouched, and the identity-disambiguation rule is documented — Wave 2 can run pure silicon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis → firmware source | This plan reads + diffs firmware but MUST NOT modify it (analysis + prep only) |
| host PC → bench hardware | No hardware op in this plan; the b10 build is staged, not flashed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-VPP | Tampering | firmware VPP over-voltage guard (`primitives.cpp` +500mV window) + BLOCKER-2 no-12V-to-no-VPP-pin invariant | mitigate | This plan does NOT modify firmware; the guard stays PRESENT + UNMODIFIED (SAFE-04). Verified by `git -C firestarter status` clean on src/include. |
| T-91-ID | Tampering | firmware identity confusion (version string 3.0.0b10 identical for recompose + b10) | mitigate | Disambiguate by flash byte count (25136 vs 25654 B), recorded in 91-RCA.md; never trust the version string alone. |
</threat_model>

<verification>
- `pio test -e native -f "*test_val_flash3*"` and `*test_val_eprom*` both PASS.
- diff-forensics.txt non-empty; 91-RCA.md has Diff Forensics + Native Trace Confirmation + A/B Prep sections.
- SST39SF040 image B SHA == a38b13b4… ; b10 image built; recompose HEAD still a296195.
- `git -C firestarter status --porcelain src include` is empty (no source modified, SAFE-04).
</verification>

<success_criteria>
The recompose-innocence verdict is committed with reproducible evidence (captured diff + green
native traces), the A/B images are SHA-verified, and the b10 baseline is staged — Wave 2 bench
A/B can execute with zero remaining prep.
</success_criteria>

<output>
Create `.planning/phases/91-12v-vpp-write-path-regression-rca/91-01-SUMMARY.md` when done.
</output>
