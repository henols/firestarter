---
phase: 91-12v-vpp-write-path-regression-rca
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - .planning/v1.16/ledger/rca/91-RCA.md
  - .planning/v1.16/ledger/rca/ebca6266-forensic.txt
  - .planning/v1.16/ledger/bench/SST39SF040-ab/SHA256SUMS.txt
autonomous: true
requirements: [RCA-91]
must_haves:
  truths:
    - "The deterministic wrong content ebca6266… currently on the SST39SF040 is byte-compared against image B and classified (partial-program / transform / noise) — Open Q1 resolved"
    - "The SST39SF040 write failure is reproduced on the CURRENT recompose firmware (a296195) with a loaded-rail VPP capture taken during the write (not just idle)"
    - "The b10 baseline firmware is flashed (disambiguated by flash byte count) and the SST39SF040 write-cycle is re-run — THE decisive A/B experiment"
    - "The cause is classified at the decision gate: recompose-fw / host / pre-existing-environmental"
  artifacts:
    - path: ".planning/v1.16/ledger/rca/ebca6266-forensic.txt"
      provides: "Byte-comparison of the on-chip ebca6266 capture vs image B (divergence map + classification)"
    - path: ".planning/v1.16/ledger/rca/91-RCA.md"
      provides: "Mechanism explanation for both symptoms + the A/B decision-gate verdict"
      contains: "Decision Gate"
    - path: ".planning/v1.16/ledger/bench/SST39SF040-ab/SHA256SUMS.txt"
      provides: "Committed bench A/B SHA evidence (recompose vs b10 outcomes; no large binaries)"
  key_links:
    - from: ".planning/v1.16/ledger/rca/91-RCA.md"
      to: "firestarter fw + flash byte count"
      via: "firmware identity confirmation per silicon op"
      pattern: "25136|25654"
    - from: "bench A/B"
      to: "v1.15 baseline a38b13b4…"
      via: "write-cycle final SHA comparison"
      pattern: "a38b13b4"
---

<objective>
Execute the decisive A/B experiment on the seated SST39SF040 (0x06) on Leonardo + Rev 2.0.
Resolve Open Q1 (what is the ebca6266 wrong content?), reproduce the failure on the recompose
firmware with a LOADED-rail VPP capture, then flash the b10 baseline (Plan 01's worktree image)
and re-run the identical write-cycle. The result classifies the cause at a decision gate:
recompose-fw vs host vs pre-existing-environmental — the input to the Plan 03 fix.

Purpose: This is the load-bearing experiment of the whole phase. 91-RESEARCH rates the
recompose INNOCENT with HIGH confidence (no write-path delta), so this plan is structured so
b10-also-fails is the PRIMARY expected branch, not an edge case. Either way the plan produces a
firm, evidence-backed attribution and the mechanism for both symptoms (RCA-91).

Output: ebca6266-forensic.txt (Open Q1), a loaded-rail VPP capture, the recompose-vs-b10 write
outcomes, committed SHA evidence, and a "## Decision Gate" verdict in 91-RCA.md.

ALL tasks here are autonomous: the operator authorized reflash-with-chip-seated and all
SST39SF040 ops; the Leonardo is EXEMPT from chip-out-before-sideload. The chip currently holds
the wrong ebca6266 content — overwriting it during this A/B is expected and authorized.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md
@.planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md
@.planning/v1.16/ledger/bench/BENCH-LOG.md
@.planning/v1.16/ledger/rca/91-RCA.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ebca6266 content forensic — re-read the chip (non-destructive) + byte-compare vs image B</name>
  <files>.planning/v1.16/ledger/rca/ebca6266-forensic.txt, .planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Open Questions Q1, §Assumptions Log A2 — structured corruption discriminates timing/rail from addressing/buffer)
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (Chip 4 SST39SF040 block — ebca6266…3533e0b full SHA, image B a38b13b4…)
    - firestarter/CLAUDE.md (handler table 0x06 = None/5V; controller identity check `firestarter fw`)
    - memory reference_verify_port_identity_each_task (verify controller identity per task)
  </read_first>
  <action>
    FIRST confirm controller identity: `firestarter fw` → must report leonardo, and confirm the
    board currently runs the recompose (flash byte count proxy — the recompose was on the board at
    Phase 90 end). The SST39SF040 read path is byte-identical to v1.15 (proven), so re-reading is
    safe and non-destructive — do this BEFORE any write that would overwrite ebca6266. Read the
    chip: `firestarter read SST39SF040 /tmp/fs-91/SST39SF040_ebca6266.bin`. Verify its SHA ==
    ebca626663a78613a7572b792e00de11472aaefa84b41539ec5f5cfae3533e0b (the Phase-90 capture; if it
    differs, record that the chip state drifted and proceed with the actual content). Byte-compare
    against image B (/tmp/firestarter_bench_p90/SST39SF040_img_B.bin) with a small python3
    one-off: report first-divergence offset, % matching bytes, longest matching prefix, and whether
    the wrong content is a partial-program (first-N-correct), a constant fill, or a transform
    (e.g. fixed byte-offset/swap). Write the divergence map + classification to ebca6266-forensic.txt
    and summarize in 91-RCA.md under "## ebca6266 Content Forensic (Open Q1)". This discriminates
    A2: a partial/prefix-correct pattern ⇒ timing/rail under-program; a structured transform ⇒
    addressing/buffer fault.
  </action>
  <verify>
    <automated>test -s .planning/v1.16/ledger/rca/ebca6266-forensic.txt &amp;&amp; grep -q "Content Forensic" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter fw` confirmed leonardo before the read (controller identity verified).
    - The chip was re-read to /tmp/fs-91/SST39SF040_ebca6266.bin BEFORE any overwrite.
    - ebca6266-forensic.txt records first-divergence offset, % matching, and a classification (partial-program / constant-fill / transform / noise) vs image B a38b13b4….
    - 91-RCA.md "## ebca6266 Content Forensic (Open Q1)" states which A2 branch the structure supports (timing/rail vs addressing/buffer).
  </acceptance_criteria>
  <done>Open Q1 is resolved: the ebca6266 wrong content is captured, byte-compared to image B, and classified, with the mechanism implication recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the failure on the recompose with a LOADED-rail VPP capture, then run the b10 A/B</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md, .planning/v1.16/ledger/bench/SST39SF040-ab/SHA256SUMS.txt</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§A/B Procedure, §Pattern 2/3, §Pitfall 2 idle-vs-loaded rail, §Pitfall 3 flash3 slow-path timeout, §Code Examples)
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-CONTEXT.md (write-cycle method = `write -b` direct path; NOT `dev write-cycle`)
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (Chip 4 block — exact failure strings: write A `Operation timed out`, write B 177s + wrong content)
    - memory reference_v114_bench_erase_rail_and_test_artifact (vpp continuous-monitor capture idiom; `timeout -s INT 20 stdbuf -oL firestarter vpp`)
  </read_first>
  <action>
    (A) RECOMPOSE LEG (current fw a296195 on board — confirm `firestarter fw` leonardo + the
    recompose flash byte count if disambiguable): reproduce per BENCH-LOG using the v1.15 `write -b`
    direct path: `firestarter write -b SST39SF040 /tmp/firestarter_bench_p90/SST39SF040_img_A.bin`
    → `firestarter verify SST39SF040 .../img_A.bin` → same for img_B. Capture the loaded VPP rail
    during/around the write with a backgrounded `timeout -s INT 25 stdbuf -oL firestarter vpp`
    (measure-only, safe with chip seated; idle 12.0V was measured at Phase 90 but loaded was NOT —
    Pitfall 2). Record exact RC + timing + failure strings + the loaded-rail trace. Expect a repro
    of write-A timeout + write-B wrong content (or, if it now passes, record that and skip to the
    decision gate).
    (B) b10 LEG (THE experiment): flash the Plan-01 b10 image from /tmp/fs-b10:
    `cd /tmp/fs-b10 && pio run -t upload -e leonardo` (Leonardo EXEMPT from chip-out; SST39SF040
    stays seated). Confirm the swap by FLASH BYTE COUNT (b10 ~25654 B vs recompose 25136 B —
    `firestarter fw` says 3.0.0b10 for both, so byte count is the discriminator) plus the avrdude
    "bytes written" line. Re-run the IDENTICAL `write -b A → verify → write -b B → verify` cycle.
    Capture the loaded VPP rail again. Record both legs' final SHAs into
    bench/SST39SF040-ab/SHA256SUMS.txt (per-dir SHA convention — no multi-MB binaries committed).
    Use `-f` only if the VPP-high init guard blocks a write AND only as the operator standing
    directive allows; NEVER weaken the firmware guard itself (SAFE-04 / T-91-VPP).
  </action>
  <verify>
    <automated>test -s .planning/v1.16/ledger/bench/SST39SF040-ab/SHA256SUMS.txt &amp;&amp; grep -qiE "25654|b10" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - The recompose-leg write-cycle was run via `write -b` (NOT `dev write-cycle`) with exact RC/timing/failure strings recorded in 91-RCA.md.
    - A LOADED-rail `firestarter vpp` trace was captured during a write attempt on at least one leg (idle-vs-loaded distinction, Pitfall 2).
    - The b10 image was flashed and confirmed by flash byte count (~25654 B) + avrdude bytes-written, NOT by version string alone.
    - The b10-leg write-cycle final SHA is recorded and compared against the v1.15 baseline a38b13b4… in bench/SST39SF040-ab/SHA256SUMS.txt.
    - No firmware source was modified during the A/B (`git -C firestarter status --porcelain src include` empty); the VPP guard stayed intact.
  </acceptance_criteria>
  <done>Both A/B legs (recompose + b10) are run on the seated SST39SF040 with loaded-rail captures, and the final SHAs are committed as bench evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Decision gate — classify the cause + explain BOTH symptoms (RCA-91)</name>
  <files>.planning/v1.16/ledger/rca/91-RCA.md</files>
  <read_first>
    - .planning/phases/91-12v-vpp-write-path-regression-rca/91-RESEARCH.md (§Pattern 1 axes + outcomes, §State of the Art, §Pitfall 3/4 both-symptom mechanism, §Assumptions A1/A2/A3)
    - .planning/v1.16/ledger/rca/ebca6266-forensic.txt (Task 1 output)
    - .planning/v1.16/ledger/bench/SST39SF040-ab/SHA256SUMS.txt (Task 2 A/B outcomes)
    - .planning/v1.16/ledger/bench/BENCH-LOG.md (W27C512 0x07 block — bad-bytes-@0x0 symptom, for the both-symptom explanation)
  </read_first>
  <action>
    Combine the Task-1 content forensic + the Task-2 A/B outcomes into a "## Decision Gate"
    section in 91-RCA.md that classifies the cause as exactly one of: (1) recompose-fw — b10
    PASSES where recompose FAILS (LOW prior per RESEARCH; if observed, the regression IS firmware
    via a path the static diff missed, e.g. codegen/layout-sensitive timing); (2) host — handled
    in Plan 03 via the cheap host-axis backup A/B if still ambiguous; (3) pre-existing /
    environmental — b10 ALSO FAILS identically (HIGH prior; recompose exonerated; cause is
    rail-under-load / flash3 slow-path × host timeout window / chip wear). Then write a "## Both
    Symptoms Explained" section satisfying RCA-91 Success Criterion 2: SST39SF040 write-A timeout +
    deterministic-wrong write-B as ONE under-volt/under-time program mechanism (tie to the Task-1
    classification), AND W27C512 bad-bytes-~921-@0x0 at a clean idle rail as the FLAG_CAN_ERASE
    auto-erase pulse under-performing on this board (analysis-only; 0x07 bench deferred). State
    explicitly why a P3-only explanation does NOT cover both (flash3/0x06 uses P4/P7, no P3).
    Name the indicated fix branch for Plan 03 to consume.
  </action>
  <verify>
    <automated>grep -q "Decision Gate" .planning/v1.16/ledger/rca/91-RCA.md &amp;&amp; grep -q "Both Symptoms" .planning/v1.16/ledger/rca/91-RCA.md</automated>
  </verify>
  <acceptance_criteria>
    - 91-RCA.md "## Decision Gate" classifies the cause as exactly one of recompose-fw / host / pre-existing-environmental, justified by the A/B outcomes + content forensic.
    - 91-RCA.md "## Both Symptoms Explained" covers BOTH the SST39SF040 timeout+wrong-content mechanism AND the W27C512 bad-bytes-@0x0 mechanism (RCA-91 SC2).
    - The writeup states why a P3-only explanation fails to cover both chips.
    - The indicated fix branch for Plan 03 is named explicitly.
  </acceptance_criteria>
  <done>RCA-91 is satisfied: the cause is attributed via the controlled A/B and both distinct symptoms are explained, with the fix branch handed to Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host PC → Leonardo (sideload) | Reflash with chip seated — operator-authorized; Leonardo EXEMPT from chip-out |
| firmware → 12V/VPP rail → socket | The program pulse routes voltage; the VPP over-voltage guard is the safety control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-VPP | Tampering / hardware damage | firmware VPP over-voltage guard (+500mV window) + BLOCKER-2 no-12V-to-no-VPP-pin invariant | mitigate | No firmware source modified during the A/B (verified by `git -C firestarter status` clean on src/include); `-f` used only per operator standing directive, never weakening the guard itself (SAFE-04). NOTE: 0x06/flash3 is 5V-only and never enables the VPP regulator (BLOCKER-2) — the actual hardware-damage surface here is minimal, but the guard stays untouched regardless. |
| T-91-ID | Tampering | firmware identity confusion across the b10/recompose flash swap | mitigate | Each silicon op is preceded by `firestarter fw` + flash byte count (25136 vs 25654 B) + avrdude bytes-written; never trust the 3.0.0b10 version string alone. |
</threat_model>

<verification>
- ebca6266-forensic.txt non-empty with a divergence classification vs image B.
- A loaded-rail VPP trace captured; recompose + b10 write-cycles run via `write -b`.
- bench/SST39SF040-ab/SHA256SUMS.txt records both legs' final SHAs vs a38b13b4….
- 91-RCA.md has Content Forensic + Decision Gate + Both Symptoms Explained sections.
- `git -C firestarter status --porcelain src include` empty (no firmware modified; SAFE-04).
</verification>

<success_criteria>
The decisive A/B is run on real silicon, the cause is classified at a decision gate, both
symptoms are mechanistically explained, and Open Q1 is resolved — RCA-91 satisfied and the
fix branch is handed to Plan 03.
</success_criteria>

<output>
Create `.planning/phases/91-12v-vpp-write-path-regression-rca/91-02-SUMMARY.md` when done.
</output>
