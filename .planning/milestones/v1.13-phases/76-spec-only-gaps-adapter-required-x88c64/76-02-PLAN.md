---
phase: 76-spec-only-gaps-adapter-required-x88c64
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/doc/AT28C04-ADAPTER.md
  - .planning/AT28C04-ADAPTER.md
  - .planning/X88C64-FEASIBILITY.md
autonomous: true
requirements: [GAP-01, GAP-02]
must_haves:
  truths:
    - "A DIP24→DIP32 adapter pin-map spec exists in BOTH layers (operator-facing firestarter/doc/ + meta .planning/), lockstep-consistent, mirroring SHIELD-REVISIONS"
    - "The adapter spec pin table maps all 24 DIP24 chip pins to DIP32 socket pins against DIP32_28C512_EEPROM (NOT DIP32_STD), including the critical /WE chip-pin-21 → socket-pin-30 reroute"
    - "An X88C64 feasibility verdict doc exists with the datasheet-sourced protocol (8051 multiplexed ALE/WR/RD bus, byte/page write, timing) and a MEDIUM feasibility verdict"
    - "The X88C64 verdict corrects the prior wrong wording: parallel DIP24 (not serial-parallel hybrid) and NO STORE/RECALL pins (that belongs to the X2210/X2212 NovRAM family) — reconciling the ROADMAP/CONTEXT STORE/RECALL wording as a documented correction"
    - "No firmware handler is committed; nothing graduates to supported"
  artifacts:
    - path: "firestarter/doc/AT28C04-ADAPTER.md"
      provides: "Operator-facing DIP24→DIP32 adapter pin-map spec (D-04 layer 1)"
      min_lines: 30
      contains: "DIP32_28C512_EEPROM"
    - path: ".planning/AT28C04-ADAPTER.md"
      provides: "Meta investigation-canonical adapter derivation (D-04 layer 2)"
      min_lines: 40
      contains: "DIP32_28C512_EEPROM"
    - path: ".planning/X88C64-FEASIBILITY.md"
      provides: "X88C64 feasibility verdict + protocol write-up (D-01, GAP-02)"
      min_lines: 40
      contains: "STORE/RECALL"
  key_links:
    - from: "firestarter/doc/AT28C04-ADAPTER.md"
      to: ".planning/AT28C04-ADAPTER.md"
      via: "cross-reference to the meta derivation doc (SHIELD-REVISIONS lockstep pattern)"
      pattern: "AT28C04-ADAPTER"
    - from: ".planning/X88C64-FEASIBILITY.md"
      to: "firestarter_app DB X88C64 reason string (plan 76-01)"
      via: "the DB reason summarizes this verdict's feasible-candidate classification"
      pattern: "feasible-candidate"
---

<objective>
Deliver the documentation half of both spec-only gaps — no code, no firmware, no graduation:

- **GAP-01 (D-04):** Author the DIP24→DIP32 adapter pin-map as a TWO-LAYER spec doc kept in lockstep,
  mirroring the SHIELD-REVISIONS precedent: an operator-facing copy in `firestarter/doc/AT28C04-ADAPTER.md`
  (someone physically builds the adapter) and a canonical investigation copy in `.planning/AT28C04-ADAPTER.md`
  (full derivation, citations, future-graduation steps). The pin table maps every DIP24 chip pin to its
  DIP32 socket pin against the `DIP32_28C512_EEPROM` layout (the layout `configure_eeprom28c` uses — NOT
  `DIP32_STD`), with the critical /WE reroute (chip pin 21 → socket pin 30) called out.
- **GAP-02 (D-01):** Author `.planning/X88C64-FEASIBILITY.md` — the datasheet-sourced feasibility verdict +
  write protocol (8051 multiplexed ALE/WR/RD bus, byte/page write up to 32 bytes, ~EEPROM-class timing,
  toggle-bit polling). Classify X88C64 as a documented MEDIUM feasible-candidate. Correct the prior wrong
  description: the chip is parallel DIP24 (NOT a "serial-parallel hybrid") and has NO STORE/RECALL pins
  (those belong to the older X2210/X2212 NovRAM family) — reconcile the ROADMAP/CONTEXT "STORE/RECALL"
  wording as a documented finding. NO 0x34 firmware handler is committed (D-01).

Purpose: capture the spec/feasibility knowledge so a future milestone can build the physical adapter and
the X88C64 handler without re-deriving anything — while keeping v1.13's "nothing newly supported" invariant.

Output: 3 new spec docs (2 adapter layers + 1 feasibility verdict). No code or DB changes in this plan.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-CONTEXT.md
@.planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-RESEARCH.md
@.planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-PATTERNS.md
@firestarter/doc/SHIELD-REVISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the DIP24→DIP32 pin mapping against pinouts.json, then author the two-layer adapter spec (GAP-01, D-04)</name>
  <read_first>
    - firestarter_app/firestarter/data/pinouts.json (the DIP24_2816 and DIP32_28C512_EEPROM entries — the `pins` object: address-bus-pins, data-bus-pins, ce-pin, oe-pin, rw-pin, vcc-pin, gnd-pin — GROUND TRUTH for the mapping)
    - .planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-RESEARCH.md (the full 24-row adapter pin-map table; the /WE chip-pin-21 → socket-pin-30 key insight; Pitfall 5: must use DIP32_28C512_EEPROM not DIP32_STD; the safety note that neither pinout has a vpp-pin)
    - .planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-PATTERNS.md (the operator-facing + meta doc section layouts; the cross-reference idiom)
    - firestarter/doc/SHIELD-REVISIONS.md (the operator-facing two-layer doc structure to mirror: opening paragraph, "read §N if you…" routing, cross-ref to the meta doc)
  </read_first>
  <action>
    FIRST re-verify the pin mapping against the `pins` object of pinouts.json (RESEARCH derivation is
    MEDIUM-confidence). Confirm address A0..A10 = DIP24 pins [8,7,6,5,4,3,2,1,23,22,19] → DIP32 pins
    [12,11,10,9,8,7,6,5,27,26,23]; data D0..D7 = DIP24 [9,10,11,13,14,15,16,17] → DIP32 [13,14,15,17,18,19,20,21];
    CE 18→22; OE 20→24; WE 21→30; VCC 24→32; GND 12→16. Confirm neither pinout has a vpp-pin (5V-only, no
    high-voltage hazard). Resolve any mismatch in favor of pinouts.json (ground truth) and note it.

    THEN author both layers (D-04, lockstep):
    - `firestarter/doc/AT28C04-ADAPTER.md` (operator-facing): opening paragraph + "read §N if…" routing +
      cross-ref to `.planning/AT28C04-ADAPTER.md`; §1 Overview (which 9 chips; firmware handler
      configure_eeprom28c / protocol 0x0D; adapter-required until physical adapter + golden round-trip);
      §2 Adapter Pin Table (24 rows DIP24 chip pin → DIP32 socket pin against DIP32_28C512_EEPROM, plus the
      unconnected DIP32 socket pins listed separately); §3 Safety Notes (the /WE pin-21→pin-30 key reroute,
      what goes wrong without the adapter — socket pin 21 = D7 in the DIP32 layout, the 5V-only no-VPP
      safety point, the AT28C04 NC-pin difference for A9/A10).
    - `.planning/AT28C04-ADAPTER.md` (meta investigation-canonical): the fuller structure from 76-PATTERNS.md
      §"meta adapter spec" — Summary, §1 Scope (all 9 chips), §2 Pinout Sources (pinouts.json DIP24_2816 +
      DIP32_28C512_EEPROM ground truth + AT28C16 datasheet cross-check), §3 Adapter Pin Table (same 24 rows,
      with per-pin source citations), §4 Key Re-route narrative, §5 Safety Analysis, §6 Future Graduation
      Steps (physical adapter + golden write+read-back round-trip + test coverage).
    Keep the two pin tables byte-consistent (lockstep). Per GAP-01 (D-04). Do NOT encode the pin remap in
    build_db.py (that is plan 76-01's named arm, which deliberately does NOT encode the remap).
  </action>
  <verify>
    <automated>test -f firestarter/doc/AT28C04-ADAPTER.md && test -f .planning/AT28C04-ADAPTER.md && grep -q "DIP32_28C512_EEPROM" firestarter/doc/AT28C04-ADAPTER.md && grep -q "DIP32_28C512_EEPROM" .planning/AT28C04-ADAPTER.md && grep -qE "21.*30|30.*WE" firestarter/doc/AT28C04-ADAPTER.md && ! grep -q "DIP32_STD" firestarter/doc/AT28C04-ADAPTER.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Pin-map re-verified against pinouts.json: WE = DIP24 pin 21 → DIP32 pin 30; CE 18→22; OE 20→24; VCC 24→32; GND 12→16; address/data arrays match the verified mapping. Any deviation from RESEARCH resolved in favor of pinouts.json and noted in the meta doc.
    - `test -f firestarter/doc/AT28C04-ADAPTER.md` and `test -f .planning/AT28C04-ADAPTER.md` both pass.
    - Both docs reference `DIP32_28C512_EEPROM`; NEITHER references `DIP32_STD` (Pitfall 5 — wrong layout).
    - Both docs contain the /WE chip-pin-21 → socket-pin-30 reroute and a 24-row pin table; the two pin tables are consistent (lockstep).
    - The operator-facing doc cross-references the meta `.planning/AT28C04-ADAPTER.md` (SHIELD-REVISIONS pattern).
    - The meta doc has a §"Future Graduation Steps" stating graduation needs a physical adapter + golden write+read-back round-trip (out of v1.13 scope).
  </acceptance_criteria>
  <done>Two-layer adapter spec authored and lockstep-consistent; pin map verified against pinouts.json; uses DIP32_28C512_EEPROM; /WE reroute documented; graduation deferred.</done>
</task>

<task type="auto">
  <name>Task 2: Author the X88C64 feasibility verdict + protocol doc (GAP-02, D-01)</name>
  <read_first>
    - .planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-RESEARCH.md (GAP-02 Technical Facts: device identity, the 24-pin interface table, the critical-finding 8051 multiplexed-bus architecture, write protocol ALE/WR + page-write 32B + toggle-bit polling, the RURP feasibility verdict MEDIUM, Assumptions A4/A5, Open Questions on ALE routing + tWC)
    - .planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-PATTERNS.md (the §"X88C64-FEASIBILITY.md" section layout — Summary, §1 Device Identity, §2 Interface Architecture, §3 Write Protocol, §4 RURP Feasibility Assessment table, §5 What is Needed for a Handler, §6 Assumptions Log, §7 Sources)
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (the X88C64 0x34 row + the support_status↔feasibility taxonomy — cite the row by protocol_id)
    - .planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-CONTEXT.md (D-01 + the Pitfall-3 STORE/RECALL correction; reconcile the ROADMAP/CONTEXT STORE/RECALL wording as a documented finding)
  </read_first>
  <action>
    Author `.planning/X88C64-FEASIBILITY.md` following the 76-PATTERNS.md section layout. State the MEDIUM
    feasibility verdict (documented feasible-candidate, handler deferred). Document the corrected findings:
    the X88C64P is a parallel DIP24 5V EEPROM with an 8051 multiplexed address/data bus (ALE/WR/RD), NOT a
    "serial-parallel hybrid"; it has NO STORE/RECALL pins — STORE/RECALL belongs to the older X2210/X2212
    NovRAM family (cite the bitsavers 1985 Xicor Data Book), and the ROADMAP/CONTEXT "STORE/RECALL" wording
    is reconciled here as a documented correction (not a scope change). Document the write protocol
    (ALE-latch → /WR-strobe, page write up to 32 bytes, toggle-bit polling, WC write-abort, block-protect,
    ~EEPROM-class timing). Include §4 the RURP feasibility table (socket/voltage compatible; bus-protocol
    non-trivial; ALE routing = open question), §5 what a future handler needs (ALE control-bit investigation
    in rurp_pinout.h, firmware ALE/WR/RD sequence, bench verification), §6 assumptions log (A4 tWC
    LOW-confidence, A5 no-STORE/RECALL HIGH-confidence), §7 sources (datasheet citations + bitsavers). Cite
    the 0x34 row of v1.13-PROTOCOL-ENUMERATION.md by protocol_id. Per GAP-02 (D-01): do NOT commit a 0x34
    firmware handler; do NOT change the chip's support_status (the DB reword is plan 76-01's job). This doc is
    the canonical home of the verdict that plan 76-01's DB reason string summarizes.
  </action>
  <verify>
    <automated>test -f .planning/X88C64-FEASIBILITY.md && grep -q "STORE/RECALL" .planning/X88C64-FEASIBILITY.md && grep -qi "feasible-candidate" .planning/X88C64-FEASIBILITY.md && grep -qi "multiplexed" .planning/X88C64-FEASIBILITY.md && ! grep -qi "serial-parallel hybrid" .planning/X88C64-FEASIBILITY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/X88C64-FEASIBILITY.md` passes.
    - Doc states a MEDIUM feasibility verdict and the word "feasible-candidate".
    - Doc documents the 8051 "multiplexed" bus (ALE/WR/RD) and the byte/page write protocol (up to 32 bytes).
    - Doc explicitly corrects the prior wording: it states X88C64 has NO STORE/RECALL pins (those are X2210/X2212) and does NOT contain "serial-parallel hybrid".
    - Doc states no 0x34 firmware handler is committed this phase (D-01) and lists what a future handler needs.
    - Doc cites the X88C64 0x34 row of `.planning/v1.13-PROTOCOL-ENUMERATION.md`.
  </acceptance_criteria>
  <done>X88C64 feasibility verdict + protocol doc authored; corrected findings (parallel DIP24, no STORE/RECALL, multiplexed bus) documented; handler explicitly deferred (D-01); no DB/firmware change in this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec docs → future hardware/firmware work | The adapter pin-map and feasibility verdict are consumed by a future milestone that physically builds the adapter / writes the 0x34 handler. An incorrect pin map could route a signal to the wrong socket line; an over-optimistic verdict could authorize an unsafe handler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-D1 | Tampering / Information disclosure | adapter pin-map table | mitigate | The pin map is re-verified against pinouts.json ground truth (the authoritative RURP routing) before sign-off (Task 1); the safety note documents that neither pinout carries a vpp-pin (5V-only, no high-voltage hazard) so a mis-wired adapter cannot route 12V to a chip pin. Spec only — no chip is graduated, so no live programming path is enabled this phase. |
| T-76-D2 | Repudiation / over-trust | X88C64 feasibility verdict | accept | The verdict is explicitly MEDIUM with an open ALE-routing question and a LOW-confidence tWC assumption logged; D-01 forbids committing a handler this phase, so the doc cannot authorize an unsafe write path on its own. The next milestone must do bench verification before any handler ships. Accepted: documentation-only, fail-closed at the firmware not_implemented guard until a handler is deliberately built. |
| T-76-D3 | Elevation of Privilege | "nothing newly supported" invariant | mitigate | This plan touches only doc files (no DB, no firmware) — it cannot graduate any chip. The invariant is additionally enforced by plan 76-01's green check_dispatch/diff_db gates. |
</threat_model>

<verification>
- `test -f firestarter/doc/AT28C04-ADAPTER.md && test -f .planning/AT28C04-ADAPTER.md && test -f .planning/X88C64-FEASIBILITY.md` — all three docs exist.
- Adapter docs use DIP32_28C512_EEPROM (not DIP32_STD) and document the /WE pin-21→pin-30 reroute; pin tables lockstep-consistent.
- X88C64 doc states MEDIUM feasible-candidate, multiplexed ALE/WR/RD bus, NO STORE/RECALL, no "serial-parallel hybrid"; cites PROTOCOL-ENUMERATION 0x34 row.
- No edits under firestarter/src/ (no firmware handler); no DB changes in this plan.
</verification>

<success_criteria>
- Two-layer DIP24→DIP32 adapter spec authored, lockstep, pin map verified against pinouts.json, /WE reroute documented, graduation deferred (GAP-01, D-04).
- X88C64 feasibility verdict + protocol documented, prior wording corrected (parallel DIP24, no STORE/RECALL), handler deferred (GAP-02, D-01).
- Nothing graduates to supported; no firmware committed.
</success_criteria>

<output>
Create `.planning/phases/76-spec-only-gaps-adapter-required-x88c64/76-02-SUMMARY.md` when done.
</output>
