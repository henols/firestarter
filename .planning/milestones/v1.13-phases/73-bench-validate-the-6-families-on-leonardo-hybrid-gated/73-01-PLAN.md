---
phase: 73-bench-validate-the-6-families-on-leonardo-hybrid-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/val-results/eeprom28c/validation-matrix.json
  - firestarter_app/val-results/eeprom28c/validation-matrix.md
  - firestarter_app/val-results/flash4/validation-matrix.json
  - firestarter_app/val-results/flash4/validation-matrix.md
  - firestarter_app/val-results/flash_intel/validation-matrix.json
  - firestarter_app/val-results/flash_intel/validation-matrix.md
  - ~/.firestarter/config.json
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05, VAL-06]
must_haves:
  truths:
    - "All six families have GREEN Tier-1 native register-sequence cells (re-confirmed, not rebuilt)"
    - "All six families have GREEN Tier-2 host wire round-trip cells (re-confirmed, not rebuilt)"
    - "The three chipless families (eeprom28c, flash4, flash_intel) have a recorded Tier-3 SKIP-deferred cell with reason 'no chip on hand'"
    - "The live R1/R2 precondition is armed so it actually fires for the Wave-2 Tier-3 runs (r1 persisted ≈ 270000)"
  artifacts:
    - path: "firestarter_app/val-results/eeprom28c/validation-matrix.json"
      provides: "Tier-3 SKIP-deferred cell for eeprom28c (VAL-02)"
      contains: "SKIP-deferred"
    - path: "firestarter_app/val-results/flash4/validation-matrix.json"
      provides: "Tier-3 SKIP-deferred cell for flash4 (VAL-04)"
      contains: "SKIP-deferred"
    - path: "firestarter_app/val-results/flash_intel/validation-matrix.json"
      provides: "Tier-3 SKIP-deferred cell for flash_intel (VAL-05)"
      contains: "SKIP-deferred"
  key_links:
    - from: "firestarter config -r1 270000"
      to: "~/.firestarter/config.json"
      via: "config_manager persists r1"
      pattern: "\"r1\""
---

<objective>
Establish the software-tier baseline and the bench precondition for Phase 73, and record the
three chipless families as Tier-3 SKIP-deferred — all without any chip in the socket.

This plan does three flash-free things, all per D-15 (reuse, not rebuild):
1. Re-confirm every family's Tier-1 native + Tier-2 host wire cells are GREEN (they were stood up
   GREEN in Phase 71; this is a confirm-don't-rebuild step satisfying ROADMAP success criterion 1
   for all of VAL-01..VAL-06).
2. Arm the live R1/R2 precondition by persisting `r1 = 270000` into local config so the
   `dev validate-family` r1 gate actually fires for the Wave-2 Tier-3 runs (closes the
   RESEARCH-flagged R1 precondition gap — Pitfall 2).
3. Emit Tier-3 SKIP-deferred cells for the three chipless families (eeprom28c/VAL-02,
   flash4/VAL-04, flash_intel/VAL-05) per D-02/D-13, each into its own output dir.

Purpose: Front-load all flash-free, chip-less work so the Wave-2 bench plans only do the
chip-in-socket Tier-3 HIL runs. Establishes that the standing bench precondition is real
(not silently skipped) before any write cycle.
Output: Software-tier GREEN confirmation log; persisted r1 precondition; three SKIP-deferred
matrix artifacts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md
@.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm all six families' Tier-1 native + Tier-2 host wire cells GREEN</name>
  <files>(no source edits — confirmation only; results recorded in SUMMARY)</files>
  <read_first>
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md §"Validation Architecture" (the Tier-1/Tier-2 run commands and the Phase-requirements→test map)
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md D-15 (reuse-not-rebuild) and D-07 carry-forward
  </read_first>
  <action>
    Re-confirm the software tiers are GREEN for ALL six families (VAL-01..VAL-06), per D-15 — do
    NOT rebuild or modify any test. Run Tier-1 native: from firestarter/ run the native env
    filtered to the six validation suites: `pio test -e native -f "test_val_*"` (covers
    test_val_eprom, test_val_eeprom28c, test_val_flash3, test_val_flash4, test_val_flash_intel,
    test_val_sram). Run Tier-2 host wire: from firestarter_app/ run `pytest tests/test_val_wire_*.py -v`
    (the six wire round-trip suites). Capture the pass counts per suite. If any suite is RED, STOP
    and record it — a RED software cell is a blocker that predates the bench and must be surfaced,
    not bench-papered. RESEARCH confirmed all six Tier-1 + all Tier-2 (26 tests) PASSED live on
    2026-06-17, so GREEN is expected; this task proves it still holds at execution time.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "test_val_*" 2>&1 | tail -20</automated>
    <automated>cd /workspaces/firestarter_app && pytest tests/test_val_wire_*.py -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "test_val_*"` reports all six val suites PASSED (test_val_eprom,
      test_val_eeprom28c, test_val_flash3, test_val_flash4, test_val_flash_intel, test_val_sram).
    - `pytest tests/test_val_wire_*.py` reports 0 failures across the six wire suites.
    - SUMMARY records the per-suite pass counts as the Tier-1/Tier-2 GREEN evidence for
      VAL-01..VAL-06 ROADMAP success criterion 1.
  </acceptance_criteria>
  <done>All six families' Tier-1 native and Tier-2 host wire cells confirmed GREEN; counts recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the live R1/R2 precondition (persist r1 ≈ 270000) and confirm Leonardo port identity</name>
  <files>~/.firestarter/config.json</files>
  <read_first>
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md §"The Non-Vacuous PASS Oracle" → "R1 precondition practical notes" and Pitfall 2
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md D-11 (standard precondition only) and D-14 (uno328pb N/A)
  </read_first>
  <action>
    Per D-11, the pre-write gate for this phase is verify-port + live R1/R2 readback (`r1 ≈ 270000`)
    + the Tier-1 recording-stub VPP assertions (already proven GREEN in Task 1). This task ARMS the
    R1 gate so it fires for the Wave-2 Tier-3 runs — RESEARCH Pitfall 2 shows
    `config_manager.get_value("r1")` reads `~/.firestarter/config.json`, which currently has no `r1`
    key, so the gate is silently skipped. First verify controller identity per D-03/Pitfall 4:
    `firestarter -p /dev/ttyACM0 fw` MUST show `controller: leonardo` (ACM numbers shuffle on
    replug — never assume). Then read live calibration with `firestarter -p /dev/ttyACM0 config`
    and confirm R1 reads ≈ 270000 (within ±25%, i.e. [202500, 337500]) and R2 ≈ 44000. Then persist
    r1 into local config so the gate is armed: `firestarter -p /dev/ttyACM0 config -r1 270000`.
    Re-read `~/.firestarter/config.json` and confirm an `r1` key now exists. If the live R1 readback
    is OUT of band, STOP — a 999.1 calibration confounder must not be papered over (per the standing
    bench precondition + D-14); record the out-of-band value and halt rather than proceed to writes.
  </action>
  <verify>
    <automated>firestarter -p /dev/ttyACM0 fw 2>&1 | grep -i "controller.*leonardo"</automated>
    <automated>grep -q '"r1"' ~/.firestarter/config.json && echo "r1 armed"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter -p /dev/ttyACM0 fw` output contains `controller: leonardo`.
    - `firestarter -p /dev/ttyACM0 config` shows R1 within [202500, 337500] (≈270000) and R2 ≈ 44000;
      the readback values are recorded in SUMMARY.
    - `~/.firestarter/config.json` contains an `r1` key after persistence (the gate is armed for
      Wave-2 runs).
    - SUMMARY records the live R1/R2 readback as the precondition evidence and the persisted r1.
  </acceptance_criteria>
  <done>Leonardo identity confirmed on /dev/ttyACM0; live R1≈270000/R2≈44000 readback recorded; r1 persisted into local config so the Wave-2 precondition gate fires.</done>
</task>

<task type="auto">
  <name>Task 3: Emit Tier-3 SKIP-deferred cells for the three chipless families (VAL-02, VAL-04, VAL-05)</name>
  <files>firestarter_app/val-results/eeprom28c/, firestarter_app/val-results/flash4/, firestarter_app/val-results/flash_intel/</files>
  <read_first>
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md §"The dev validate-family Invocation Contract" (SKIP-deferred path) and §"Validation Matrix Spec + Results Schema" (SKIP-deferred cell schema)
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md D-02 (which families are SKIP-deferred) and D-13 (partial coverage is closeable and explicit)
  </read_first>
  <action>
    Per D-02/D-13, record the three families with no chip on hand as Tier-3 SKIP-deferred — explicit
    per-cell, never silent. Use the existing runner's SKIP-deferred auto-path (D-06/D-15): invoking
    `dev validate-family <family>` with NO `--board/--chip/--source` triggers
    `_emit_skip_deferred_artifact()` and exits 0. Run, each into its own output-dir (disjoint files
    so this plan owns them cleanly): from firestarter_app/ run
    `firestarter dev validate-family eeprom28c --output-dir val-results/eeprom28c` (VAL-02),
    `firestarter dev validate-family flash4 --output-dir val-results/flash4` (VAL-04), and
    `firestarter dev validate-family flash_intel --output-dir val-results/flash_intel` (VAL-05).
    Do NOT pass a port/board/chip. After each, open the emitted `validation-matrix.json` and confirm
    the cell `verdict` is `"SKIP-deferred"`. The runner's default reason is recorded automatically;
    note in SUMMARY that the SKIP reason for all three is "no chip on hand" per D-02. These three
    families' software cells are already GREEN (Task 1), so per D-13 they close as Tier-1/Tier-2
    GREEN + Tier-3 SKIP-deferred.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && firestarter dev validate-family eeprom28c --output-dir val-results/eeprom28c && firestarter dev validate-family flash4 --output-dir val-results/flash4 && firestarter dev validate-family flash_intel --output-dir val-results/flash_intel</automated>
    <automated>cd /workspaces/firestarter_app && grep -l 'SKIP-deferred' val-results/eeprom28c/validation-matrix.json val-results/flash4/validation-matrix.json val-results/flash_intel/validation-matrix.json | wc -l | grep -q 3 && echo "3 SKIP-deferred artifacts"</automated>
  </verify>
  <acceptance_criteria>
    - Three artifacts exist: val-results/{eeprom28c,flash4,flash_intel}/validation-matrix.json.
    - Each contains a Tier-3 cell with `"verdict": "SKIP-deferred"` for its family.
    - The companion validation-matrix.md exists in each output dir.
    - SUMMARY records each cell as VAL-02 / VAL-04 / VAL-05 SKIP-deferred with reason "no chip on
      hand" (D-02) and notes these three families are now closed (GREEN software + SKIP-deferred
      Tier-3) per D-13.
  </acceptance_criteria>
  <done>eeprom28c/flash4/flash_intel each have a recorded Tier-3 SKIP-deferred cell with reason; the three chipless families are closed at partial coverage per D-13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devcontainer → /dev/ttyACM0 | Claude-driven CLI commands cross to physical Leonardo; wrong port = wrong board |
| live calibration → matrix verdict | a stale/wrong R1 (999.1) could let a future Tier-3 write be misread as an algorithm result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Spoofing | /dev/ttyACM0 identity | mitigate | Task 2 runs `firestarter fw` and requires `controller: leonardo` before any config write (Pitfall 4; ACM numbers shuffle on replug) |
| T-73-02 | Tampering | r1 precondition silently skipped | mitigate | Task 2 persists r1 into local config so the `dev validate-family` gate actually fires for Wave-2 runs (closes Pitfall 2 / RESEARCH R1 gap) |
| T-73-03 | Repudiation | false GREEN on a RED software cell | mitigate | Task 1 STOPS and records any RED suite rather than proceeding — a RED software cell predates the bench and is surfaced, not papered |
| T-73-04 | Information disclosure | SKIP recorded silently as omission | mitigate | Task 3 emits an explicit per-cell SKIP-deferred verdict (D-13) — partial coverage is recorded, never silent |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH §Package Legitimacy Audit: none); zero production flash |
</threat_model>

<verification>
- All six Tier-1 native + Tier-2 host wire suites GREEN (Task 1).
- Leonardo identity confirmed + live R1≈270000/R2≈44000 + r1 persisted (Task 2).
- Three SKIP-deferred Tier-3 artifacts emitted with explicit reason (Task 3).
- Zero production firmware flash added (no `pio run` source changes; D-15).
</verification>

<success_criteria>
- VAL-01..VAL-06 software half (Tier-1 + Tier-2) confirmed GREEN (ROADMAP SC#1).
- VAL-02 / VAL-04 / VAL-05 Tier-3 SKIP-deferred recorded (ROADMAP SC#2, D-13).
- R1/R2 precondition armed and recorded (ROADMAP SC#3 enabling condition).
</success_criteria>

<output>
Create `.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-01-SUMMARY.md` when done
</output>
