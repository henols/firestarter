---
phase: 73-bench-validate-the-6-families-on-leonardo-hybrid-gated
plan: 03
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - firestarter_app/val-results/flash3/validation-matrix.json
  - firestarter_app/val-results/flash3/validation-matrix.md
  - firestarter_app/val-results/flash3/am29f040-source.bin
  - firestarter_app/val-results/flash3/am29f040-wrongfile.bin
autonomous: false
requirements: [VAL-03]
must_haves:
  truths:
    - "AM29F040 (Flash AMD, configure_flash3 0x06, 512KB, 5V no-VPP) has a recorded Tier-3 Leonardo verdict (PASS or FAIL — both valid per D-12)"
    - "The Tier-3 cell records an independent post-write full read + SHA compare on Leonardo (authoritative pass_type)"
    - "A passing negative control is recorded: verify against a wrong file FAILS"
    - "Sector/chip erase behavior is exercised or recorded advisory per the flash3 cycle (VAL-03 algorithm surface, Claude's discretion)"
  artifacts:
    - path: "firestarter_app/val-results/flash3/validation-matrix.json"
      provides: "Tier-3 flash3 cell with verdict + pass_type:authoritative + evidence_sha (VAL-03)"
      contains: "\"family\": \"flash3\""
  key_links:
    - from: "dev validate-family flash3 --board leonardo --chip AM29F040"
      to: "validation-matrix.json flash3 Tier-3 cell"
      via: "write_cycle_eprom (erase_eprom → flash3_erase_execute) return code mapped to verdict"
      pattern: "\"pass_type\": \"authoritative\""
---

<objective>
Run the Tier-3 HIL cell for the flash3 family (VAL-03) on Leonardo with the on-hand AM29F040,
recording a real verdict (PASS or FAIL) plus a passing negative control.

AM29F040 is the Flash AMD family representative (configure_flash3, 0x06, 512KB, 5V-only — no VPP
preconditions). This plan invokes the existing `dev validate-family` runner (D-15,
reuse-not-rebuild) and records the emitted cell as VAL-03 Tier-3 evidence. The write_cycle_eprom
flow calls erase_eprom (→ flash3_erase_execute, sector/chip erase) BEFORE write, so the flash3
erase surface is exercised as part of the cycle (Claude's discretion per CONTEXT — exercised here,
not deferred advisory). Per D-12, a FAIL is a valid outcome routing to Phase 74.

Purpose: Real bench evidence for the largest Flash family on the only valid-PASS board (Leonardo,
D-14). Output: a Tier-3 flash3 cell (verdict + authoritative pass_type + evidence_sha) + a recorded
passing negative control.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md
@.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Operator swaps to the AM29F040 chip on the Rev 2.0 Leonardo</name>
  <action>Operator physical action only (Claude cannot swap a chip). Remove any prior chip, seat the AM29F040, and confirm the shield revision. See how-to-verify for the exact steps.</action>
  <what-built>Wave-1 (73-01) armed the precondition and confirmed Leonardo + Rev 2.0. This checkpoint asks the operator to swap to the AM29F040 chip for the Tier-3 flash3 run.</what-built>
  <how-to-verify>
    1. Remove any chip currently in the socket (e.g. the W27C512 from 73-02 if run first).
    2. Confirm the shield is still Rev 2.0 (D-03 — ASK if unsure).
    3. Insert the AM29F040 (Flash AMD, 32-pin/PLCC-in-adapter as applicable) into the socket,
       correct orientation. AM29F040 is 5V-only — no VPP concern.
    4. Reply "inserted" (and confirm the shield rev) when the AM29F040 is seated.
  </how-to-verify>
  <resume-signal>Type "inserted" and the shield rev, or describe any issue</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Verify Leonardo identity + live R1, then run the AM29F040 Tier-3 flash3 cell with negative control</name>
  <files>firestarter_app/val-results/flash3/</files>
  <read_first>
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-RESEARCH.md §"Per-family canonical invocations" (VAL-03 flash3 command), §"AM29F040 specific gotchas", §"The Non-Vacuous PASS Oracle" → "Negative control bench procedure", and Pitfalls 4, 5
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-CONTEXT.md D-11 (precondition), D-12 (FAIL is valid), D-14 (Leonardo authoritative), and Claude's-Discretion bullet on AM29F040 sector-vs-chip erase
  </read_first>
  <action>
    Re-verify port identity at task start (Pitfall 4): `firestarter -p /dev/ttyACM0 fw` MUST show
    `controller: leonardo`; `firestarter -p /dev/ttyACM0 hw` MUST show Rev 2.0. Confirm armed live
    R1 with `firestarter -p /dev/ttyACM0 config` (R1 ~ 270000); STOP if out of band. Create an exact
    524288-byte source image (AM29F040 is 512KB — RESEARCH gotcha) with a non-trivial deterministic
    pattern at val-results/flash3/am29f040-source.bin. Run the existing runner (D-15):
    `firestarter -p /dev/ttyACM0 dev validate-family flash3 --board leonardo --chip AM29F040 --source
    val-results/flash3/am29f040-source.bin --output-dir val-results/flash3`. The runner's
    write_cycle_eprom calls erase_eprom → flash3_erase_execute (sector/chip erase) before write, so
    the flash3 erase surface is exercised as part of this cell (VAL-03 algorithm surface). Record the
    exit code (0=PASS, 1=FAIL, 2=hw-error). Then PASSING NEGATIVE CONTROL (Pitfall 5): create a
    distinct val-results/flash3/am29f040-wrongfile.bin (different 512KB pattern) and run
    `firestarter -p /dev/ttyACM0 verify AM29F040 val-results/flash3/am29f040-wrongfile.bin` — it MUST
    exit non-zero. If the runner exits 2 (hw-error per Assumption A3, flash3 standalone erase),
    record the failure detail and note it as a Phase-74 candidate (per D-12 a recorded outcome still
    closes the cell; surface the erase-path detail rather than silently retrying). Per D-12 the
    recorded verdict (PASS or FAIL) closes VAL-03 Tier-3.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "import json; c=json.load(open('val-results/flash3/validation-matrix.json')); cell=[x for x in c['cells'] if x['family']=='flash3' and x['board']=='leonardo']; assert cell and cell[0]['verdict'] in ('PASS','FAIL','SKIP-deferred'), cell; print('flash3 Tier-3 cell:', cell[0]['verdict'], cell[0].get('pass_type'))"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter fw` showed `controller: leonardo` and `hw` showed Rev 2.0 at task start.
    - Live R1 readback within [202500, 337500] recorded.
    - val-results/flash3/validation-matrix.json contains a flash3 Tier-3 leonardo cell with a
      recorded `verdict`; if PASS/FAIL then `pass_type` = "authoritative", `evidence_sha` non-null,
      `retry_count` recorded. (If hw-error → SKIP-deferred, the erase-path detail is recorded as a
      Phase-74 candidate.)
    - The negative control is recorded: `verify AM29F040 <wrong-file>` exited non-zero.
    - SUMMARY records the AM29F040 verdict as VAL-03 Tier-3 evidence, notes whether sector/chip
      erase fired in the cycle, and routes any FAIL/hw-error to Phase 74 per D-12.
  </acceptance_criteria>
  <done>AM29F040 has a recorded Tier-3 verdict on Leonardo + a passing negative control; VAL-03 Tier-3 closed per D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devcontainer → /dev/ttyACM0 → AM29F040 | Claude drives a 5V flash erase+write to a seated chip; wrong board corrupts the verdict |
| readback SHA → recorded verdict | a vacuous PASS would be untrustworthy evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-09 | Spoofing | wrong board on /dev/ttyACM0 | mitigate | Task 1 re-verifies `controller: leonardo` + Rev 2.0 before any write (Pitfall 4) |
| T-73-10 | Repudiation | vacuous PASS (verify can't fail) | mitigate | Task 1 records a passing negative control (wrong-file verify FAILS) per Pitfall 5 |
| T-73-11 | Tampering | flash3 erase hw-error logged as algorithm bug | mitigate | Task 1 records an erase hw-error (Assumption A3) as a Phase-74 candidate with detail, not as a silent retry |
| T-73-12 | Information disclosure | partial coverage silent | mitigate | The flash3 cell is explicitly recorded (PASS/FAIL/SKIP-deferred) — never silent (D-13) |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; zero production flash (D-15); AM29F040 is 5V no-VPP |
</threat_model>

<verification>
- flash3 Tier-3 leonardo cell recorded with a verdict.
- Passing negative control recorded (wrong-file verify FAILS).
- Live R1 precondition confirmed in-band.
</verification>

<success_criteria>
- VAL-03 Tier-3 closed on Leonardo per D-12.
- ROADMAP SC#2 + SC#3 satisfied for the flash3 family; sector/chip erase surface exercised or
  recorded.
</success_criteria>

<output>
Create `.planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-03-SUMMARY.md` when done
</output>
