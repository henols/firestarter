---
phase: 74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md
  - firestarter/CLAUDE.md
  - firestarter_app/firestarter/database.py
  - firestarter_app/firestarter/ic_layout.py
autonomous: true
requirements: [FIX-01, FIX-03]
must_haves:
  truths:
    - "FIX-01 is recorded closed not-needed with citations to the VAL-06 PASS evidence (no SRAM code change)."
    - "0x39 and 0x35 are described as 0-DB-chip phantom/future-proofed entries consistently across firmware CLAUDE.md and host database.py + ic_layout.py."
    - "No wire/messages.toml change is introduced (comments + closure doc only)."
    - "check_dispatch.py and diff_db.py stay green after the host comment edits."
  artifacts:
    - path: ".planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md"
      provides: "FIX-01 closed-not-needed disposition citing val06-perbyte-verdict.txt + validation-matrix.json + Phase 73 SC#4"
      contains: "table-stakes-PASS"
    - path: "firestarter/CLAUDE.md"
      provides: "Reconciled 0x05/0x35/0x39 dispatch-table row stating 0x35+0x39 = 0 DB chips"
      contains: "0x39"
  key_links:
    - from: "firestarter/CLAUDE.md"
      to: "firestarter_app/firestarter/database.py"
      via: "consistent 0x39/0x35 = 0 DB chips phantom framing"
      pattern: "0x39"
---

<objective>
Close FIX-01 with recorded evidence (no SRAM code change) and reconcile the firmware↔host story for protocols 0x39 and 0x35 so both repos describe them identically as 0-DB-chip phantom / future-proofed dispatch entries (FIX-03 doc reconciliation).

Purpose: Phase 73 VAL-06 proved `configure_sram` already writes correctly (table-stakes-PASS), so FIX-01 is a documentation closure, not an implementation. FIX-03's "2 current 0x39 DB chips" premise is false (Phase 72 GAP-5: 0 DB chips); the real debt is aligning the inconsistent comments across the two repos.
Output: A FIX-01 closure doc plus reconciled comments in `firestarter/CLAUDE.md`, `database.py`, and `ic_layout.py`. No firmware source change, no wire/messages.toml change.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md
@.planning/v1.13-PROTOCOL-ENUMERATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record FIX-01 closed not-needed with VAL-06 evidence (FIX-01)</name>
  <files>.planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md</files>
  <read_first>
    - firestarter_app/val-results/sram/val06-perbyte-verdict.txt (the authoritative VAL-06 = table-stakes-PASS verdict + Phase-74 FIX-01 Disposition section)
    - firestarter_app/val-results/sram/validation-matrix.json (verdict=PASS, pass_type=authoritative, retry_count=2)
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-VERIFICATION.md (SC#4: FIX-01 closed not-needed)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md (FIX-01 decision block)
  </read_first>
  <action>Create `74-FIX01-CLOSURE.md` documenting that FIX-01 is CLOSED NOT-NEEDED per the requirement's "IF VAL-06 shows it already works" branch. Cite by relative path: `firestarter_app/val-results/sram/val06-perbyte-verdict.txt` (verdict line "VAL-06 = table-stakes-PASS", FM1608 two-pattern A/B, N=2, zero mismatches, D-09 hard gate SATISFIED), `firestarter_app/val-results/sram/validation-matrix.json`, and Phase 73 VERIFICATION SC#4. State explicitly that `configure_sram` is NOT a silent no-op (generic `memory_write_execute` path persists data) and that NO firmware change is made in this phase. Do NOT plan or describe an SRAM read/write rewrite. Reference REQUIREMENTS.md FIX-01 and Phase 72 GAP-2.</action>
  <verify>
    <automated>test -f .planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md && grep -q "table-stakes-PASS" .planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md && grep -q "NOT-NEEDED\|not-needed\|not needed" .planning/phases/74-per-family-correctness-fixes-flash-gated/74-FIX01-CLOSURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `74-FIX01-CLOSURE.md` exists and contains the string "table-stakes-PASS".
    - The doc cites all three evidence sources by relative path (val06-perbyte-verdict.txt, validation-matrix.json, Phase 73 SC#4).
    - The doc asserts FIX-01 = closed not-needed and that no firmware/SRAM code change is made.
    - No SRAM source file (`firestarter/src/proms/sram.cpp`) is modified by this task (`cd firestarter && git status --porcelain src/proms/sram.cpp` returns empty).
  </acceptance_criteria>
  <done>FIX-01 closure doc on disk, citing VAL-06 PASS evidence, asserting no-code-change disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 0x39/0x35 phantom comments across firmware CLAUDE.md and host (FIX-03)</name>
  <files>firestarter/CLAUDE.md, firestarter_app/firestarter/database.py, firestarter_app/firestarter/ic_layout.py</files>
  <read_first>
    - firestarter/CLAUDE.md (dispatch table row 4: "protocol ∈ {0x05, 0x35, 0x39} → configure_flash4() — page-write flash (0x39 future-proofed, no chips in current DB)" and the Algorithm Handlers table 0x35/0x39 rows)
    - firestarter_app/firestarter/database.py (lines ~50-63: the protocol→type map and the 0x35/0x39 "removed in Phase 57 (DEC-05)" comment)
    - firestarter_app/firestarter/ic_layout.py (lines ~220-230: proto_display map and the 0x35/0x39 "removed in Phase 57 (DEC-05)" comment)
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (GAP-5: memory.cpp:89 comment is accurate; 0 DB chips on 0x39 and 0x35)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md (FIX-03 target reconciliation section)
  </read_first>
  <action>Update comments only (NO behavioral / wire change) so the two repos tell one consistent story for 0x39 AND its sibling 0x35: both are 0-DB-chip phantom entries that firmware dispatches to `configure_flash4` by analogy (forward-compatibility), while the host's `KNOWN_PROTOCOLS` allowlist excludes them so the host routes them to not_implemented. In `firestarter/CLAUDE.md`, edit the dispatch-table row-4 note (and the Algorithm Handlers 0x35/0x39 rows) to state symmetrically that 0x05 has DB chips while 0x35 and 0x39 = 0 DB chips (phantom; future-proofed dispatch preserved). In `firestarter_app/firestarter/database.py` (~line 60) and `ic_layout.py` (~line 228), extend the existing "removed in Phase 57 (DEC-05)" comments with a cross-reference noting firmware still dispatches 0x39/0x35 to configure_flash4 for forward-compat while host routes them to not_implemented (excluded from KNOWN_PROTOCOLS). Do NOT change `memory.cpp` (its dispatch and comment are accurate per Phase 72 GAP-5). Do NOT add or remove protocols from any allowlist or map. Do NOT touch `messages.toml` (confirm: no new wire message is required — this is comments, not protocol behavior).</action>
  <verify>
    <automated>grep -ql "0x39" firestarter/CLAUDE.md && grep -ql "0x39" firestarter_app/firestarter/database.py && grep -ql "0x39" firestarter_app/firestarter/ic_layout.py && grep -qi "phantom\|future-proof\|0 DB chips\|no chips\|not.implemented\|forward-compat" firestarter/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY cites Phase 72 GAP-5 (`v1.13-PROTOCOL-ENUMERATION.md`) AND host source verification (`build_db.py` KNOWN_PROTOCOLS allowlist, `database.py` ~line 60, `ic_layout.py` ~line 228) confirming **0 DB chips on 0x39 and 0x35**, resolving the REQUIREMENTS.md "2 current 0x39 DB chips" premise as FALSE — FIX-03 closed as not-needed with evidence (the comment reconciliation IS the resolution; no code/DB change), mirroring FIX-01's closure-with-evidence pattern.
    - A grep for `0x39` across `firestarter/CLAUDE.md`, `firestarter_app/firestarter/database.py`, `firestarter_app/firestarter/ic_layout.py` shows all three describe 0x39 (and 0x35) as 0-DB-chip phantom / future-proofed entries (consistent framing across both repos).
    - `firestarter/src/proms/memory.cpp` is NOT modified (`cd firestarter && git status --porcelain src/proms/memory.cpp` is empty).
    - No `messages.toml` (meta-repo or sub-repo) is modified by this plan (`git status --porcelain` shows no `messages.toml` path).
    - Non-regression: `cd firestarter_app && python3 tools/check_dispatch.py` exits 0 AND `python3 tools/diff_db.py` reports 0 changed chips (comments must not alter the generated DB or dispatch).
  </acceptance_criteria>
  <done>0x39/0x35 comments are consistent across firmware CLAUDE.md and host source; check_dispatch.py + diff_db.py green; no wire/DB change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan changes documentation comments and adds a closure doc only — no code path, no serial/wire surface, no hardware register access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-DOC-01 | Tampering (doc drift) | 0x39/0x35 comments across two repos | mitigate | Acceptance grep asserts cross-repo consistency; check_dispatch.py + diff_db.py confirm no DB/dispatch behavior changed. |

FIX-01 (closure doc) and FIX-03 (comment reconciliation) have no security or hardware surface — no VPP path, no wire message, no register write.
</threat_model>

<verification>
- `cd firestarter_app && python3 tools/check_dispatch.py` exits 0 (744 chips, 0 regressions).
- `cd firestarter_app && python3 tools/diff_db.py` reports 0 changed chips.
- `74-FIX01-CLOSURE.md` exists and cites VAL-06 = table-stakes-PASS.
- Grep confirms 0x39/0x35 phantom framing is consistent across the three files.
- No `messages.toml` and no firmware source (`memory.cpp`, `sram.cpp`) modified.
</verification>

<success_criteria>
- FIX-01 is recorded closed not-needed with VAL-06 evidence citations (ROADMAP SC#1 "closed as not-needed" branch).
- 0x39/0x35 comments reconciled and consistent across firmware CLAUDE.md + host database.py + ic_layout.py (ROADMAP SC#3 corrected-comment intent; "2-chip" premise documented as superseded by Phase 72 GAP-5).
- check_dispatch.py + diff_db.py stay green; no wire/messages.toml change.
</success_criteria>

<output>
Create `.planning/phases/74-per-family-correctness-fixes-flash-gated/74-01-SUMMARY.md` when done.
</output>
