---
phase: 74
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp
  - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp
autonomous: true
requirements: [FIX-02]
must_haves:
  truths:
    - "configure_flash4 handles CMD_CHECK_CHIP_ID and sets a non-NULL operation_main (mirroring flash3)."
    - "flash4_write_execute emits the FLASH_ENABLE_WRITE SDP 3-byte unlock at each page start before loading page bytes."
    - "flash4_write_execute commits pages at the correct (W29C040 256-byte) boundary, with a no-regression argument for the other 26 flash4 0x05 chips."
    - "flash4_write_execute NEVER sets CTRL_VPP_REGULATOR_ENABLE or CTRL_VPP_P1_ENABLE (operation-phase VPP-safety, proven by recording-stub test)."
    - "pio run -e leonardo links successfully and the post-fix flash-% is recorded."
    - "All native suites stay green; check_dispatch.py + diff_db.py unchanged."
  artifacts:
    - path: "firestarter/src/proms/flash_type_4.cpp"
      provides: "CMD_CHECK_CHIP_ID dispatch + flash4_check_chip_id_execute + SDP/page-size write fix"
      contains: "CMD_CHECK_CHIP_ID"
    - path: "firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp"
      provides: "FIX-02A RED→GREEN dispatch test asserting non-NULL operation_main for CMD_CHECK_CHIP_ID on 0x05/0x35/0x39"
      contains: "CMD_CHECK_CHIP_ID"
    - path: "firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp"
      provides: "FIX-02B operation-phase SDP-emission recording test + operation-phase VPP-bit assertion"
      contains: "flash4_write_execute"
  key_links:
    - from: "firestarter/src/proms/flash_type_4.cpp"
      to: "firestarter/include/flash_utils.h"
      via: "flash_execute_command(FLASH_ENABLE_WRITE) and FLASH_ENABLE_ID/FLASH_DISABLE_ID reuse"
      pattern: "FLASH_ENABLE_WRITE|FLASH_ENABLE_ID"
    - from: "firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp"
      to: "flash4_write_execute"
      via: "recording-stub side-effect assertions"
      pattern: "recorded_reg|recorded_data"
---

<objective>
Fix both flash4 defects in `flash_type_4.cpp`, software-first RED→GREEN: (A) add the missing `case CMD_CHECK_CHIP_ID:` mirroring flash3, and (B) fix the W29C040 write algorithm — add the SDP 3-byte unlock (`FLASH_ENABLE_WRITE`) at each page start and correct the page-commit boundary from the wrong 64-byte `#define` to the W29C040's 256-byte page — while proving the write path never enables the VPP regulator and never regresses the other flash4 0x05 chips.

Purpose: Phase 73 VAL-04 recorded a genuine W29C040 write FAIL (exit code 2 + standalone `write -b` timeout, negative control passed). Research traced it to (1) missing SDP unlock — the W29C040 ships with Software Data Protection enabled, so unlocked page loads are silently rejected — and (2) `#define PAGE_SIZE 64` polling at the wrong boundary. Defect A is a small dispatch mirror that fixes chip-ID verification for the whole flash4 family.
Output: Patched `flash_type_4.cpp`, extended native tests (dispatch suite + val_flash4 suite), a green `pio run -e leonardo` build with the post-fix flash-% recorded, and green non-regression gates.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-VALIDATION.md
@firestarter/src/proms/flash_type_4.cpp
@firestarter/src/proms/flash_type_3.cpp
@firestarter/include/flash_utils.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX-02A — RED→GREEN CMD_CHECK_CHIP_ID dispatch case in configure_flash4</name>
  <files>firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp, firestarter/src/proms/flash_type_4.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_type_3.cpp (configure_flash3 CMD_CHECK_CHIP_ID case at ~line 46, flash3_check_chip_id_execute ~line 131, flash3_get_chip_id ~line 149 — the exact mirror source)
    - firestarter/src/proms/flash_type_4.cpp (configure_flash4 switch ~line 26-40 — the missing case; forward-declare the new fn near the existing forward declarations ~line 21-24)
    - firestarter/include/flash_utils.h (FLASH_ENABLE_ID / FLASH_DISABLE_ID sequences to reuse via flash_execute_command)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (make_handle helper ~line 58, existing 0x05/0x35/0x39 dispatch tests ~line 77-93, RUN_TEST registrations ~line 171)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md (FIX-02A section + Option A inline-mirror recommendation)
  </read_first>
  <behavior>
    - RED: a new test `test_flash4_check_chip_id_0x05_sets_operation` builds a handle via make_handle(0x05, 0, CMD_CHECK_CHIP_ID), calls configure_memory, and asserts firestarter_operation_main != NULL AND response_code != RESPONSE_CODE_ERROR. Add sibling tests for 0x35 and 0x39. These FAIL today (no case → operation_main stays NULL).
    - GREEN: after adding the case + flash4_check_chip_id_execute, all three new tests pass and the 15 existing dispatch tests stay green.
  </behavior>
  <action>First write the failing tests in `test_configure_memory.cpp`: add `test_flash4_check_chip_id_0x05_sets_operation` (and 0x35, 0x39 variants) using the existing `make_handle(protocol, 0, CMD_CHECK_CHIP_ID)` pattern, asserting `TEST_ASSERT_NOT_NULL(h.firestarter_operation_main)` and `TEST_ASSERT_NOT_EQUAL(RESPONSE_CODE_ERROR, h.response_code)`; register them with RUN_TEST. Confirm RED via `pio test -e native -f "*test_dispatch*"`. Then in `flash_type_4.cpp` add `case CMD_CHECK_CHIP_ID:` to `configure_flash4` setting `firestarter_operation_init = NULL` and `firestarter_operation_main = flash4_check_chip_id_execute`, and add a new `flash4_check_chip_id_execute` + `flash4_get_chip_id` mirroring `flash3_check_chip_id_execute` / `flash3_get_chip_id` exactly (reuse `flash_execute_command(FLASH_ENABLE_ID)` → read 0x0000/0x0001 → `flash_execute_command(FLASH_DISABLE_ID)`; same FLAG_FORCE warning-vs-error handling, same MSG_*_CHIP_ID_MISMATCH log IDs). Use Option A (inline mirror in flash_type_4.cpp) per research — do NOT refactor flash3 into a shared util this phase. Forward-declare the new function near the existing forward decls.</action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_dispatch*"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/src/proms/flash_type_4.cpp` contains `case CMD_CHECK_CHIP_ID:` and a `flash4_check_chip_id_execute` definition.
    - `grep -c "test_flash4_check_chip_id" firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp` >= 3 (0x05, 0x35, 0x39).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` passes ALL tests (new + the pre-existing 15), zero failures.
    - The new tests were RED before the source edit (the executor must confirm the RED run in the SUMMARY).
  </acceptance_criteria>
  <done>CMD_CHECK_CHIP_ID dispatches to a non-NULL operation_main on 0x05/0x35/0x39; dispatch suite fully green; chip-id mirror is inline in flash_type_4.cpp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FIX-02B — RED→GREEN SDP unlock + correct page boundary in flash4_write_execute (VPP-safe)</name>
  <files>firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp, firestarter/src/proms/flash_type_4.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_type_4.cpp (PAGE_SIZE #define ~line 19; flash4_write_execute ~line 61-76 — the bare per-byte set_data loop missing SDP; flash4_wait_for_page_write ~line 78-100 — DQ7 poll, keep as-is)
    - firestarter/src/proms/flash_type_3.cpp (flash3_write_execute ~line 96-106 — shows flash_execute_command(FLASH_ENABLE_WRITE) usage; note flash3 calls it PER-BYTE, flash4 must call it PER-PAGE-START — see Pitfall 2)
    - firestarter/include/flash_utils.h (FLASH_ENABLE_WRITE = {0x5555,0xAA},{0x2AAA,0x55},{0x5555,0xA0} — the SDP sequence; flash_execute_command macro)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (make_handle ~line 57, assert_no_vpp_in_recording helper ~line 71-82, recording API externs ~line 42-45, RUN_TEST registrations ~line 144)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (HOST_STUBS_RECORD_BUS recording API: clear_bus_recording/bus_recording_count/recorded_reg/recorded_data; how firestarter_set_data side-effects are recorded)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md (W29C040 Datasheet Analysis, "Concrete target write algorithm", Pitfalls 1-4, Architecture Pattern 1 operation-phase VPP test + Pattern 2 SDP-at-page-boundary)
  </read_first>
  <behavior>
    - RED test 1 (SDP emission): a recording-stub test invokes flash4_write_execute on a small data buffer (≥1 page) for protocol 0x05 and asserts the recorded bus shows the FLASH_ENABLE_WRITE 3-byte sequence (writes 0xAA→0x5555, 0x55→0x2AAA, 0xA0→0x5555) at the start of each page, before the page's data bytes. FAILS today (no SDP emitted).
    - RED test 2 (operation-phase VPP-safety): the same write-execute invocation asserts assert_no_vpp_in_recording — flash4_write_execute must NEVER set CTRL_VPP_REGULATOR_ENABLE (0x80) or CTRL_VPP_P1_ENABLE (0x08) in any CONTROL_REGISTER write. (This passes for the bare loop today but MUST keep passing after the SDP fix, since flash_util_byte_flipping only sets CTRL_READ_WRITE, never VPP.)
    - GREEN: after the source fix, both tests pass and the 6 existing configure-phase VPP tests stay green.
  </behavior>
  <action>First add the two failing tests to `test_val_flash4.cpp` (extend the existing recording-stub suite — the HOST_STUBS_RECORD_BUS API is already wired there). Test 1 asserts the SDP 3-byte sequence appears at each page start in the recorded bus; Test 2 reuses `assert_no_vpp_in_recording` against the operation phase (build a handle, configure_memory, fill a small data_buffer, call `h.firestarter_operation_main(&h)` to drive flash4_write_execute). Register with RUN_TEST and confirm RED. Then fix `flash4_write_execute` in `flash_type_4.cpp`: (1) call `flash_execute_command(FLASH_ENABLE_WRITE)` at each page start (`address % page_size == 0` OR first byte `i == 0`) and immediately load the page bytes back-to-back with no intervening delay (Pitfall 1); call SDP per-page-start, NOT per-byte (Pitfall 2); (2) commit/poll at the correct page boundary using `((address + 1) % page_size) == 0` or last byte, calling the unchanged `flash4_wait_for_page_write`.

RESOLVE THE PAGE_SIZE NO-REGRESSION DESIGN CONCERN (research OQ#1) — implement option (a) data-driven page size: derive a runtime `page_size` from `handle->mem_size` rather than the compile-time `#define PAGE_SIZE 64`. Apply this exact data-driven derivation: if `handle->mem_size` ≤ 65536 then page size 64; else if `handle->mem_size` ≤ 262144 then 128; else 256. Worked examples to encode/verify: W29C040 (mem_size 524288, 512K) → 256; AT29C256 (mem_size 32768, 32K) → 64; SST29EE010 (mem_size 131072, 128K) → 128. NO-REGRESSION ARGUMENT (must be stated in the SUMMARY and is an acceptance criterion): the 27 flash4 0x05 chips in the DB span 32KB–512KB; deriving page_size from mem_size gives each chip its native (or conservative) page size so the SDP+poll cadence matches the chip's real page buffer — a fixed `256` would over-run smaller chips' page buffers (the design hazard), and the old fixed `64` polled mid-page on the W29C040 (the original bug). Data-driven sizing fixes W29C040 without changing the effective behavior for the smaller chips whose native page is ≤ their derived size. Keep the change minimal to protect the flash budget. Do NOT enable any VPP bit. Do NOT touch flash4_erase_execute (its latent VPP dead-code path is documented as tech debt in the threat model — out of scope here).</action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_val_flash4*"</automated>
  </verify>
  <acceptance_criteria>
    - `flash_type_4.cpp` `flash4_write_execute` calls `flash_execute_command(FLASH_ENABLE_WRITE)` at page start (grep shows `FLASH_ENABLE_WRITE` in the write path) and derives page size from `handle->mem_size` (the literal `#define PAGE_SIZE 64` is removed or replaced by the data-driven derivation).
    - The exact thresholds (mem_size ≤ 65536 → 64, ≤ 262144 → 128, else 256) are visible in `flash_type_4.cpp` as an inline define, comment, or derivation.
    - A code review confirms no intervening `Serial`, `delay()`, or complex operation occurs between the `FLASH_ENABLE_WRITE` (SDP unlock) emission and the first `handle->firestarter_set_data` call in the same page-load iteration — preventing the ~150µs SDP inactivity timeout from aborting the page load (RESEARCH Pitfall 1) — and the review finding is recorded in the SUMMARY.
    - The SUMMARY states the page-size no-regression argument explicitly (why data-driven sizing cannot regress the other 26 flash4 0x05 chips).
    - `cd firestarter && pio test -e native -f "*test_val_flash4*"` passes ALL tests: the 2 new (SDP emission + operation-phase VPP-safety) plus the 6 pre-existing configure-phase VPP tests, zero failures.
    - Both new tests were RED before the source edit (executor confirms RED run in SUMMARY).
    - `flash4_erase_execute` is NOT modified (`cd firestarter && git diff src/proms/flash_type_4.cpp` shows no change inside flash4_erase_execute).
  </acceptance_criteria>
  <done>W29C040 write path emits SDP at page boundaries and commits at the data-driven (256B for W29C040) boundary, proven VPP-safe by the operation-phase recording test; val_flash4 suite fully green; page-size no-regression argument recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Non-regression gate + Leonardo flash-ceiling measurement [BLOCKING]</name>
  <files>firestarter/src/proms/flash_type_4.cpp</files>
  <read_first>
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md (Flash budget note: baseline 88.9% = 25482/28672; FIX-02A+B expected +~100-130 bytes → ~89.3-89.8%; mitigation guidance if over-budget)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md (flash-ceiling constraint; mitigation = share unlock util / collapse duplicate command-sequence code)
    - firestarter/src/proms/flash_type_4.cpp (final state after Tasks 1-2)
  </read_first>
  <action>Run the full non-regression and flash-budget gate after Tasks 1-2. Run the complete native suite (`pio test -e native`), the host dispatch/DB gates (`check_dispatch.py`, `diff_db.py`), and the Leonardo link build (`pio run -e leonardo`). Record the actual post-fix Leonardo flash-% (bytes used / 28672) in the SUMMARY. The link MUST succeed — this is BLOCKING. Apply the conditional flash-% mitigation threshold: if post-fix Leonardo flash-% exceeds 89.5% (or leaves < 50 bytes headroom), the research Option B mitigation (refactor the duplicate AMD command sequences so flash4 shares flash3's unlock util) MUST be applied and re-measured, recording BOTH the original and the mitigated flash-% in the SUMMARY; if flash-% ≤ 89.5%, the SUMMARY notes the mitigation was not needed. (Baseline is 88.9% per research.) Confirm NO `messages.toml` change was introduced by FIX-02 (handler logic only).</action>
  <verify>
    <automated>cd firestarter && pio test -e native && pio run -e leonardo && cd ../firestarter_app && python3 tools/check_dispatch.py && python3 tools/diff_db.py</automated>
  </verify>
  <acceptance_criteria>
    - [BLOCKING] `cd firestarter && pio run -e leonardo` links successfully (exit 0).
    - The post-fix Leonardo flash-% (Program: used/maximum, percentage) is recorded verbatim in the SUMMARY.
    - `cd firestarter && pio test -e native` passes all suites (full 77+ tests, zero failures).
    - `cd firestarter_app && python3 tools/check_dispatch.py` exits 0 and `python3 tools/diff_db.py` reports 0 changed chips.
    - No `messages.toml` modified (`git status --porcelain` shows no messages.toml in the diff across meta + sub-repos).
    - Conditional mitigation: if post-fix flash-% > 89.5% (or < 50 bytes headroom), the Option B shared-unlock-util mitigation was applied and BOTH original and mitigated flash-% recorded; if ≤ 89.5%, the SUMMARY notes mitigation was not needed.
  </acceptance_criteria>
  <done>Leonardo build links, flash-% recorded, full native suite + host gates green, no wire change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware → seated flash chip (socket bus) | flash4_write_execute drives address/data/control register lines to a physical 5V flash part; an incorrect CONTROL_REGISTER write could apply 12V (VPP boost) to a 5V-only chip. |
| host JSON command → handle->cmd dispatch | configure_flash4 selects the operation function from handle->cmd; a mis-wired case could route a chip-id check to a write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-VPP | Tampering / Hardware-Damage | flash4_write_execute (operation phase) | mitigate | Tier-1 operation-phase recording-stub assertion (Task 2, test 2) proves flash4_write_execute NEVER sets CTRL_VPP_REGULATOR_ENABLE (0x80) or CTRL_VPP_P1_ENABLE (0x08); the SDP path (flash_util_byte_flipping) only sets CTRL_READ_WRITE. Configure-phase VPP-safety (6 existing tests) retained. The seated-write VPP control is the chip-OUT multimeter dry-run gated in Plan 03 (Tier-3). |
| T-74-VPP-LATENT | Tampering / Hardware-Damage (residual) | flash4_erase_execute (asserts CTRL_VPP_REGULATOR_ENABLE) | accept | Documented residual tech debt: flash4_erase_execute asserts the 12V boost regulator, but it is dead code — no flash4 DB chip sets FLAG_CAN_ERASE (host only sets it from info-flags&0x10; all 27 flash4 chips have info-flags 0), so the erase path is never reached for any current chip. NOT fixed this phase (out of scope per CONTEXT; erase = Phase 75). Named here per threat-model requirement; flash4_erase_execute is asserted unmodified by Task 2 acceptance. |
| T-74-DISPATCH | Spoofing/Tampering | configure_flash4 CMD_CHECK_CHIP_ID case | mitigate | Task 1 dispatch test asserts the new case sets the correct non-NULL operation_main; the chip-id mirror uses the same FLASH_ENABLE_ID/DISABLE_ID read-only sequence as flash3 (no VPP, no write). |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (firmware C++ + existing native test framework only). No new dependency. |
</threat_model>

<verification>
- `cd firestarter && pio test -e native` — full native suite green (dispatch + val_flash4 + all others).
- `cd firestarter && pio run -e leonardo` — links; flash-% recorded (BLOCKING).
- `cd firestarter_app && python3 tools/check_dispatch.py && python3 tools/diff_db.py` — green, 0 changed chips.
- Source greps confirm CMD_CHECK_CHIP_ID case + FLASH_ENABLE_WRITE in write path + data-driven page size.
- No messages.toml change.
</verification>

<success_criteria>
- FIX-02A: configure_flash4 handles CMD_CHECK_CHIP_ID (mirror of flash3), proven by native test (ROADMAP SC#2).
- FIX-02B: W29C040 write algorithm fixed (SDP unlock + correct page boundary), proven by Tier-1 SDP-emission + operation-phase VPP-safety recording tests; no other flash4 chip regresses (data-driven page size + green non-regression gates) (ROADMAP SC#2 + SC#4 VPP/flash-ceiling).
- Leonardo links under the flash ceiling with the actual flash-% recorded; no wire/messages.toml change (ROADMAP SC#4).
- flash4_write_execute proven to never enable VPP (T-74-VPP control green).
</success_criteria>

<output>
Create `.planning/phases/74-per-family-correctness-fixes-flash-gated/74-02-SUMMARY.md` when done. Record the post-fix Leonardo flash-% and the page-size no-regression argument.
</output>
