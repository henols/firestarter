---
phase: 74
plan: 03
type: execute
wave: 2
depends_on: ["74-02"]
files_modified:
  - firestarter_app/val-results/flash4/validation-matrix.json
  - firestarter_app/val-results/flash4/validation-matrix.md
autonomous: false
requirements: [FIX-02]
user_setup:
  - service: bench-hardware
    why: "Tier-3 W29C040 write+read-back requires physical Leonardo + Rev 2.0 shield + seated W29C040; software cannot assert real-silicon behavior."
must_haves:
  truths:
    - "The fixed firmware is flashed to the Leonardo before the re-bench (pio run -t upload -e leonardo)."
    - "Before any seated W29C040 write, a chip-OUT VPP multimeter dry-run confirms the socket VPP pin stays ~5V (never 12V+)."
    - "W29C040 write+read-back on Leonardo produces a readback whose SHA256 equals the source bin SHA256 (VAL-04 flips FAIL→PASS)."
    - "Bench precondition honored: controller:leonardo identity confirmed, R1≈270000 readback in-band, retry-on-timeout, Leonardo only (never uno328pb)."
  artifacts:
    - path: "firestarter_app/val-results/flash4/validation-matrix.json"
      provides: "Updated flash4 verdict = PASS with retry_count + readback SHA evidence"
      contains: "W29C040"
  key_links:
    - from: "firestarter (Leonardo, fixed firmware)"
      to: "seated W29C040"
      via: "dev validate-family flash4 write+read-back"
      pattern: "validate-family"
---

<objective>
Re-bench the W29C040 write+read-back on Leonardo with the FIX-02B firmware flashed, flipping the Phase 73 VAL-04 FAIL to PASS — gated by a chip-OUT VPP multimeter dry-run (T-74-VPP hardware control) and the standing bench precondition.

Purpose: Tier-1 recording-stub tests (Plan 02) prove the SDP/page-size fix is correct in software and VPP-safe by side-effect, but only a real W29C040 on Leonardo proves the write algorithm actually programs the chip. This is the Tier-3 evidence cell that closes FIX-02 Defect B.
Output: Updated `validation-matrix.json` / `.md` recording the W29C040 PASS (readback SHA == source SHA), with the VPP dry-run and bench-precondition evidence captured in the SUMMARY.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-VALIDATION.md
@.planning/phases/74-per-family-correctness-fixes-flash-gated/74-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flash fixed firmware to Leonardo + verify port identity and bench precondition</name>
  <files>(no source files — operational task; output captured in SUMMARY)</files>
  <read_first>
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-02-SUMMARY.md (confirms the FIX-02B firmware is built and the flash-% recorded)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-CONTEXT.md (bench precondition: live R1≈270000 readback, retry-on-timeout, Leonardo ONLY, verify controller: identity per port at task start)
    - firestarter_app/val-results/sram/val06-perbyte-verdict.txt (reference for the precondition format: controller/shield/R1 gate)
  </read_first>
  <action>With the W29C040 chip OUT of the socket, flash the FIX-02 firmware to the Leonardo via `cd firestarter && pio run -t upload -e leonardo`. Then confirm the bench precondition on the target port: query the controller identity and assert `controller: leonardo` (NEVER uno328pb — uno328pb is forbidden for program/write per the standing precondition), confirm a live R1 readback ≈ 270000 (within the [202500, 337500] band), and note retry-on-timeout is the standing policy. Record port (e.g. /dev/ttyACM0), controller identity, R1 readback, and firmware version in the SUMMARY. Do NOT seat the chip yet.</action>
  <verify>
    <human-check>Operator confirms: firmware uploaded to Leonardo; controller identity = leonardo on the chosen port; R1 readback ≈ 270000 in-band; chip still OUT.</human-check>
  </verify>
  <acceptance_criteria>
    - FIX-02 firmware uploaded to the Leonardo (upload exit 0).
    - Port controller identity confirmed `leonardo` (recorded in SUMMARY); uno328pb explicitly NOT used.
    - R1 readback ≈ 270000 recorded and in-band [202500, 337500].
    - Chip is OUT at end of this task (gates the VPP dry-run next).
  </acceptance_criteria>
  <done>Fixed firmware on Leonardo; port identity + R1 precondition recorded; chip out, ready for VPP dry-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Chip-OUT VPP multimeter dry-run (T-74-VPP hardware safety gate)</name>
  <files>(no source files — hardware safety checkpoint; output captured in SUMMARY)</files>
  <action>Operator-run hardware VPP-safety control before any seated W29C040 write. With the chip OUT, place a multimeter on the socket VPP pin, drive the flash4 write path against the empty socket, and confirm the socket VPP pin never rises above ~5V. See how-to-verify for the exact steps; this checkpoint must be approved before Task 3 seats the chip.</action>
  <what-built>FIX-02B write path (SDP + data-driven page size) flashed to Leonardo; Tier-1 tests already prove flash4_write_execute sets no VPP register bits. This checkpoint is the hardware VPP-safety control (T-74-VPP) before any seated W29C040 write.</what-built>
  <how-to-verify>
    1. Keep the W29C040 chip OUT of the socket.
    2. Place a multimeter on the socket VPP pin (per the Rev 2.0 shield pinout).
    3. Drive the flash4 write path against the empty socket (e.g. attempt a short `firestarter -p /dev/ttyACM0 write W29C040 val-results/flash4/w29c040-source.bin -b` or the dev validate-family write phase) and observe the VPP pin voltage during the write attempt.
    4. Confirm the socket VPP pin stays at ~5V and NEVER rises to 12V+ at any point in the write sequence.
    5. If 12V+ is observed at any moment: STOP — do not seat the chip; report the VPP excursion as a blocking hardware-safety failure.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the socket VPP pin stayed ~5V (never 12V+) throughout the chip-OUT write dry-run.</human-check>
  </verify>
  <done>Chip-OUT VPP dry-run approved — socket VPP pin confirmed ~5V (never 12V+); safe to seat the W29C040.</done>
  <resume-signal>Type "approved" once the chip-OUT VPP dry-run confirms ~5V (never 12V+) on the socket VPP pin; or describe the VPP excursion observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Seated W29C040 write+read-back re-bench on Leonardo (VAL-04 FAIL→PASS)</name>
  <files>firestarter_app/val-results/flash4/validation-matrix.json, firestarter_app/val-results/flash4/validation-matrix.md</files>
  <read_first>
    - firestarter_app/val-results/flash4/validation-matrix.json (current verdict=FAIL — the cell to flip)
    - firestarter_app/val-results/flash4/w29c040-source.bin (the source bin; readback SHA must equal this file's SHA)
    - .planning/phases/74-per-family-correctness-fixes-flash-gated/74-RESEARCH.md (Pitfall 5 blank-check gating: use `-b` to skip blank check since the flash4 erase path is dead code; Test Map FIX-02B bench command)
    - .planning/phases/73-bench-validate-the-6-families-on-leonardo-hybrid-gated/73-03-SUMMARY.md (the original W29C040 FAIL details: exit 2, write -b timeout, negative control passed)
  </read_first>
  <action>Only after the VPP dry-run checkpoint is approved: seat the W29C040 and run the Tier-3 re-bench on Leonardo. Use `firestarter -p /dev/ttyACM0 dev validate-family flash4 --board leonardo --chip W29C040 --source val-results/flash4/w29c040-source.bin --output-dir val-results/flash4` (or, if blank-check gating blocks it per Pitfall 5, first `firestarter -p /dev/ttyACM0 write W29C040 val-results/flash4/w29c040-source.bin -b` then `read` + SHA compare). Retry on timeout per the standing precondition; never trust N=1 — record the retry_count. Compute the readback SHA256 and compare it to the source bin SHA256. Handle BOTH outcomes definitively: (PASS) if readback SHA256 == source SHA256, update `validation-matrix.json` and `validation-matrix.md` W29C040 verdict to PASS and record retry_count (FAIL→PASS confirmed), capturing exit codes, source SHA, readback SHA in the SUMMARY; (FAIL) if readback SHA256 != source SHA256, record a definitive FAIL with the captured error message, exit code, DQ7/poll state if available, and chip state in the SUMMARY, update `validation-matrix.json` (and `.md`) W29C040 verdict to FAIL with the failure reason, and do NOT retry ad-hoc or silently SKIP — return the failure evidence for plan revision.</action>
  <verify>
    <human-check>Operator confirms the seated W29C040 write+read-back completed and the readback SHA256 equals the source bin SHA256 (VAL-04 = PASS); retry_count recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - W29C040 write+read-back run on Leonardo (NOT uno328pb), with retry-on-timeout applied and retry_count recorded.
    - PASS path: readback SHA256 == source bin (`w29c040-source.bin`) SHA256, recorded in the SUMMARY; `validation-matrix.json` (+ `.md`) W29C040 verdict = PASS with retry_count.
    - FAIL path: if readback SHA256 != source SHA256, a definitive FAIL is recorded with the captured error message, exit code, DQ7/poll state (if available), and chip state in the SUMMARY; `validation-matrix.json` (+ `.md`) verdict = FAIL with the failure reason; NO ad-hoc retry, NO silent SKIP — failure evidence returned for plan revision.
    - The chip-OUT VPP dry-run checkpoint was approved before this task ran.
  </acceptance_criteria>
  <done>W29C040 write+read-back matches source SHA on Leonardo (PASS) — or a definitive FAIL with full failure evidence is recorded; flash4 validation matrix updated accordingly; FIX-02 Defect B Tier-3 evidence captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware → seated W29C040 (socket VPP/data/control lines) | Driving real programming voltages and the SDP write sequence against a physical 5V-only chip; 12V on the VPP socket pin would damage the chip. |
| host CLI → Leonardo serial (250000 baud) | The validate-family / write command stream; wrong board (uno328pb) is bench-unstable and forbidden for program/write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-VPP | Tampering / Hardware-Damage | seated W29C040 write on Leonardo | mitigate | BLOCKING chip-OUT VPP multimeter dry-run (checkpoint task) confirms the socket VPP pin stays ~5V before the chip is ever seated; Tier-1 operation-phase recording test (Plan 02) already proves flash4_write_execute sets no VPP register bits. Both gates must pass before the seated write. |
| T-74-BOARD | Availability/Integrity | wrong controller (uno328pb) | mitigate | Task 1 asserts `controller: leonardo` per port; uno328pb explicitly forbidden for program/write (bench-unstable on W27C512-class reads, browns out on program). |
| T-74-VPP-LATENT | Tampering (residual) | flash4_erase_execute VPP dead-code | accept | Re-bench uses the WRITE path (`-b` skips blank-check/erase); the erase path is not invoked. Latent hazard documented in Plan 02 threat model; out of scope (Phase 75). |

This plan reuses existing firmware + on-disk source bin; no package-manager installs (no T-74-SC surface).
</threat_model>

<verification>
- Firmware uploaded to Leonardo; controller=leonardo and R1≈270000 confirmed (Task 1).
- Chip-OUT VPP dry-run confirms ~5V never 12V+ (blocking checkpoint).
- W29C040 readback SHA256 == source SHA256; validation-matrix.json verdict=PASS (Task 3) — or definitive FAIL with full evidence.
- retry_count recorded; Leonardo only.
</verification>

<success_criteria>
- W29C040 write+read-back PASSES on Leonardo (VAL-04 FAIL→PASS), closing FIX-02 Defect B with Tier-3 evidence (ROADMAP SC#2 re-bench + SC#4 chip-OUT VPP multimeter dry-run before any seated write) — or a definitive FAIL with full failure evidence is captured for plan revision.
- VPP hardware-safety control (T-74-VPP) green: dry-run confirmed ~5V before seating.
- Bench precondition honored (Leonardo only, controller identity verified, R1 in-band, retry-on-timeout).
</success_criteria>

<output>
Create `.planning/phases/74-per-family-correctness-fixes-flash-gated/74-03-SUMMARY.md` when done. Record port, controller identity, R1 readback, VPP dry-run result, retry_count, source SHA, readback SHA, and the final flash4 verdict.
</output>
