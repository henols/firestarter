---
phase: 72-re-research-the-protocol-landscape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.13-PROTOCOL-ENUMERATION.md
autonomous: true
requirements: [RSCH-01]
must_haves:
  truths:
    - "The enumeration document exists and assigns every in-scope protocol_id exactly one of feasible-and-implemented / feasible-gap / infeasible"
    - "The 12-row in-scope table records handler, DB chip count, and a 'v1.12 claim' holds-vs-overstated judgment per row"
    - "The erase-scope open question (A1) and the 0x2B identity open question are resolved by an executor code-trace, not asserted"
  artifacts:
    - path: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      provides: "Per-protocol verdict table + v1.12 claim reconciliation (SC#1)"
      contains: "## In-Scope Protocol Enumeration"
      min_lines: 60
  key_links:
    - from: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      to: "firestarter/src/proms/memory.cpp"
      via: "handler dispatch citation per row"
      pattern: "memory\\.cpp"
    - from: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "DB chip count per protocol_id"
      pattern: "chip_database"
---

<objective>
Build the spine of the v1.13 protocol re-enumeration: the per-protocol verdict table, the verdict taxonomy, and the explicit v1.12 "feasible set complete" holds-vs-overstated reconciliation. This is SC#1 of Phase 72.

Purpose: Produce a citable, row-by-row feasibility artifact that downstream phases 73-76 reference by protocol_id row, replacing v1.12's informal implicit "feasible set complete" claim with an evidence-grounded judgment.
Output: `.planning/v1.13-PROTOCOL-ENUMERATION.md` containing the header, the v1.12 claim review, and the 12-row in-scope enumeration table with one verdict + one v1.12-claim judgment + a resolvable evidence citation per row.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md
@.planning/phases/72-re-research-the-protocol-landscape/72-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the two open-question code-traces (erase scope A1, 0x2B identity)</name>
  <files>.planning/v1.13-PROTOCOL-ENUMERATION.md</files>
  <read_first>
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (artifact being created — does not yet exist; this task creates its first section)
    - .planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md (§Open Questions; §Gap Item 1; §Q5 0x2B note)
    - firestarter_app/firestarter/eprom_operations.py (erase_eprom flag handling)
    - firestarter_app/firestarter/cli_handlers.py (the `erase` CLI command path; _build_op_flags)
    - firestarter_app/firestarter/database.py (convert_to_programmer FLAG_CAN_ERASE derivation ~lines 590-598)
    - firestarter/src/proms/eprom.cpp (eprom_internal_erase ~274-288; eprom_erase_execute; eprom_write_init FLAG_CAN_ERASE gating)
    - firestarter_app/doc/protocol-id.md (Excluded/Infeasible table — confirm 0x2B absence)
  </read_first>
  <action>
    Trace, without hardware, whether a standalone `firestarter erase W27C512` already drives the firmware erase electricals for 0x07 EE-EPROMs. Determine the EXACT gap by reading erase_eprom in eprom_operations.py and the _build_op_flags call in the `erase` CLI path: confirm whether FLAG_CAN_ERASE is (a) needed by firmware eprom_erase_execute (which per research calls eprom_internal_erase unconditionally) or (b) only relevant to the write auto-erase path. Record the finding as either "standalone erase already electrically functional; gap is write auto-erase FLAG_CAN_ERASE routing" or "standalone erase command does not reach erase electricals" with the file:line evidence that decides it. Separately, identify the 0x2B IC2_ALG identity (GAL20 / other PLD) — if the minipro database.h source is not present in-repo, record 0x2B as "GAL/PLD family, exact IC2_ALG constant unconfirmed — protocol-id.md lacks a 0x2B row" and note that the firmware dispatch arm at memory.cpp:107-110 includes it. Write both findings into a new "## Open-Question Resolutions" section at the top of `.planning/v1.13-PROTOCOL-ENUMERATION.md` (create the file). Do NOT specify any implementation; record verdict-relevant facts only.
  </action>
  <verify>
    <automated>test -f .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "erase" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "0x2B" .planning/v1.13-PROTOCOL-ENUMERATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.13-PROTOCOL-ENUMERATION.md` exists with an "## Open-Question Resolutions" section.
    - The erase-scope resolution cites a concrete file:line (eprom_operations.py and/or cli_handlers.py and/or eprom.cpp) that decides whether standalone erase already works — not an unbacked assertion. Maps to 72-VALIDATION.md SC#2 (erase row).
    - The 0x2B resolution names the family (GAL/PLD) and explicitly notes the protocol-id.md doc gap, citing memory.cpp:107-110. Maps to 72-VALIDATION.md SC#3 (anti-feature row, 0x2B).
    - No implementation detail, register sequence, or firmware change is specified anywhere in the section.
  </acceptance_criteria>
  <done>Both open questions are answered with resolvable file:line evidence in the new document's Open-Question Resolutions section; no implementation specified.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 12-row in-scope enumeration table + verdict taxonomy + v1.12 claim review</name>
  <files>.planning/v1.13-PROTOCOL-ENUMERATION.md</files>
  <read_first>
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (artifact being extended — created in Task 1)
    - .planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md (§Q1 inventory; §Q2 taxonomy; §Q3 v1.12 claim review; §Q6 format)
    - firestarter/src/proms/memory.cpp (configure_memory dispatch arms — the source-of-truth handler per protocol_id)
    - firestarter/src/proms/sram.cpp (configure_sram near-no-op evidence for the 0x0E/27/28/29 rows)
    - firestarter_app/firestarter/data/chip_database.json (per-algorithm chip counts + support_status distribution)
    - firestarter_app/tools/build_db.py (KNOWN_PROTOCOLS host set; PROTOCOL_MAP)
    - firestarter_app/tools/check_dispatch.py (dispatch mirror; note 0x35/0x39 host omission)
    - firestarter_app/doc/protocol-id.md (IC2_ALG names per protocol_id)
    - .planning/ROADMAP.md (the v1.12 milestone "Framework + honest reporting only / no new chip became programmable" scope note to quote)
  </read_first>
  <action>
    Add "## Verdict Taxonomy" (the three buckets: feasible-and-implemented / feasible-gap / infeasible, with the orthogonality note vs. the per-chip support_status axis), "## v1.12 Claim Review" (quote the ROADMAP.md v1.12 scope note verbatim, then state holds-vs-overstated), and "## In-Scope Protocol Enumeration" (the 12-row table) to the document. Enumerate exactly these in-scope protocol_ids: 0x05, 0x06, 0x07, 0x08, 0x0B, 0x0D, 0x0E, 0x10, 0x27, 0x28, 0x29, 0x34, 0x35, 0x39 (the research lists 12 "in-scope rows" — collapse the SRAM family or flash4 family rows per the research §Q1 table so the table has at least 12 verdict-bearing rows covering this set). Columns: protocol_id | IC2_ALG name | Firestarter label | firmware handler | DB chips (supported/total) | feasibility verdict | v1.12 claim (holds/overstated) | revision | evidence citation (file:line or named datasheet). For each row, derive the handler from the memory.cpp dispatch arm, the chip count by counting programming.algorithm in chip_database.json, and the verdict per research §Q1. Mark SRAM rows "feasible-and-implemented (behavior deferred to Phase 73 VAL-06)" per research §Gap Item 2 / Open Questions #3. Mark 0x34 "feasible-gap" (overturn v1.12 implicit infeasibility). Every verdict cell MUST carry a resolvable citation; vague assertions are not acceptable. Do NOT write the gap-item index, anti-feature block, or ceiling section here — those are Plan 72-02.
  </action>
  <verify>
    <automated>grep -cE "feasible-and-implemented|feasible-gap|infeasible" .planning/v1.13-PROTOCOL-ENUMERATION.md | awk '{ if ($1>=12) print "OK"; else { print "FAIL: only "$1" verdict rows"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - "## In-Scope Protocol Enumeration" table has ≥ 12 rows, each carrying exactly one of feasible-and-implemented / feasible-gap / infeasible (grep count ≥ 12). Maps to 72-VALIDATION.md SC#1.
    - No protocol_id appears in two enumeration rows (verify by eye against the listed set 0x05..0x39).
    - Each row's "evidence citation" cell contains a resolvable file:line (e.g. `memory.cpp:74-119`) OR a named datasheet — confirm by spot-reading 3 cited locations.
    - "## v1.12 Claim Review" quotes the ROADMAP.md v1.12 scope note and states, for SRAM / erase / X88C64, where the claim was overstated, each backed by a code-state pointer (sram.cpp no-op, FLAG_CAN_ERASE routing, protocol-not-implemented X88C64). Maps to 72-VALIDATION.md SC#1.
    - "## Verdict Taxonomy" present with the support_status-vs-verdict orthogonality note.
  </acceptance_criteria>
  <done>The document contains a ≥12-row verdict table with resolvable per-row citations, the three-bucket taxonomy, and a v1.12 holds-vs-overstated review backed by code-state pointers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enumeration verdict → downstream firmware scope | A mis-classification in this document seeds a downstream phase (73-76) decision about what to implement/flash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | Enumeration verdict rows | mitigate | Every feasible verdict must cite a resolvable file:line or named datasheet (Task 2 acceptance criteria); reviewer cross-checks ≥3 rows per 72-VALIDATION.md. A row asserting feasibility without a backing citation is rejected. |
| T-72-02 | Repudiation | v1.12 "holds vs overstated" judgment | accept | Documentation-only; the judgment is auditable against quoted ROADMAP.md text and cited code-state. No runtime impact. |

No code is created or modified in this plan; no external input is processed. No HIGH-severity code threats apply (per 72-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- SC#1: `grep -cE "feasible-and-implemented|feasible-gap|infeasible" .planning/v1.13-PROTOCOL-ENUMERATION.md` returns ≥ 12.
- Reviewer cross-checks ≥ 3 enumeration rows against memory.cpp dispatch + chip_database.json counts.
- Open-question resolutions cite resolvable file:line evidence (no assertions).
</verification>

<success_criteria>
- The enumeration document exists with the in-scope verdict table (≥12 verdict rows), the verdict taxonomy, the v1.12 claim review, and the two open-question resolutions — all with resolvable citations.
</success_criteria>

<output>
Create `.planning/phases/72-re-research-the-protocol-landscape/72-01-SUMMARY.md` when done.
</output>
