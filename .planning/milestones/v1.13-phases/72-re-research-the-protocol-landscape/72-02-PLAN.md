---
phase: 72-re-research-the-protocol-landscape
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - .planning/v1.13-PROTOCOL-ENUMERATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RSCH-01]
must_haves:
  truths:
    - "The five named gap items (erase 0x07, configure_sram no-op, X88C64 0x34, flash4 chip-id, stale 0x39 comment) are each marked in-scope or deferred with rationale and routed to a downstream phase"
    - "Anti-features 0x11, 0x2A, 0x2B, 0x2C and the 25V NMOS class are re-confirmed fail-closed with cited firmware + host code locations"
    - "RURP_VPP_CEILING_MV = 22000 is confirmed present and unchanged at build_db.py with a file:line citation; the document does not recommend relaxing it"
  artifacts:
    - path: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      provides: "Gap-item index (SC#2) + anti-feature block + ceiling constraint (SC#3), completing the enumeration artifact"
      contains: "## Gap Item Index"
      min_lines: 120
  key_links:
    - from: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      to: "firestarter_app/tools/build_db.py"
      via: "RURP_VPP_CEILING_MV ceiling citation"
      pattern: "RURP_VPP_CEILING_MV"
    - from: ".planning/v1.13-PROTOCOL-ENUMERATION.md"
      to: "firestarter/src/proms/memory.cpp"
      via: "anti-feature configure_not_implemented dispatch arm citation"
      pattern: "configure_not_implemented"
---

<objective>
Complete the enumeration artifact: resolve the five named RURP-feasible gap items with in-scope/deferred verdicts + downstream-phase routing (SC#2), re-confirm the anti-features fail-closed with cited code locations and confirm the VPP ceiling is unchanged (SC#3), then commit the artifact and tick RSCH-01.

Purpose: Turn the verdict table (Plan 72-01) into the complete gate document phases 73-76 cite for scope. The security-relevant invariant of this phase — that no fail-closed anti-feature and no VPP ceiling is recommended for relaxation — is satisfied here.
Output: The finished `.planning/v1.13-PROTOCOL-ENUMERATION.md` (gap index + anti-feature block + ceiling section + sources), committed; REQUIREMENTS.md RSCH-01 ticked.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md
@.planning/phases/72-re-research-the-protocol-landscape/72-VALIDATION.md
@.planning/v1.13-PROTOCOL-ENUMERATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Gap Item Index for the five named gap items</name>
  <files>.planning/v1.13-PROTOCOL-ENUMERATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (artifact being extended — has table + open-question resolutions from Plan 72-01)
    - .planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md (§Q4 the five gap items, one subsection each)
    - firestarter/src/proms/eprom.cpp (eprom_internal_erase electricals — Gap Item 1)
    - firestarter/src/proms/sram.cpp (configure_sram near-no-op — Gap Item 2)
    - firestarter_app/firestarter/data/chip_database.json (X88C64P support_status / unsupported_reason — Gap Item 3)
    - firestarter/src/proms/flash_type_4.cpp (missing CMD_CHECK_CHIP_ID — Gap Item 4)
    - firestarter/src/proms/flash_type_3.cpp (flash3_check_chip_id_execute — the mirror precedent for Gap Item 4)
    - firestarter/src/proms/memory.cpp (0x39 future-proofed dispatch arm — Gap Item 5)
    - firestarter_app/doc/protocol-id.md (0x39 phantom/INFOIC2PLUS-unreachable note)
  </read_first>
  <action>
    Add a "## Gap Item Index" section with one entry per the five named gap items, each cross-referenced to its enumeration-table row and routed to a downstream phase: (1) erase path 0x07 — use the open-question resolution from Plan 72-01; verdict feasible-gap; route to Phase 75 (ERASE-01); cite eprom_internal_erase + W27C512 14V-erase-rail datasheet. (2) configure_sram no-op — feasible-and-implemented (behavior deferred); route to Phase 73 VAL-06 then Phase 74 FIX-01 if confirmed; cite sram.cpp:15-17 plus the generic memory_write_execute path. (3) X88C64 0x34 — feasible-gap, re-classified from implicit-infeasible to feasible candidate; route to Phase 76 GAP pending datasheet; cite chip_database.json X88C64P support_status and note the "serial-parallel hybrid" reason string is a correction candidate (it is a parallel 24-pin 5V DIP). (4) flash4 chip-id — feasible-gap (FIX-02, minor); route to Phase 74; cite flash_type_4.cpp absence of CMD_CHECK_CHIP_ID vs flash_type_3.cpp:46-48 precedent. (5) stale 0x39 comment — feasible-and-implemented (comment-correctness only, FIX-03); route to Phase 74; cite memory.cpp:89 future-proofed arm plus protocol-id.md phantom note. Each entry MUST state in-scope OR deferred with rationale. Do NOT specify register sequences or implementation; record verdict, missing-piece, and downstream phase only (per 72-RESEARCH.md Pitfall 4).
  </action>
  <verify>
    <automated>grep -qE "erase" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "configure_sram" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "X88C64" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qiE "flash4|CMD_CHECK_CHIP_ID" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "0x39" .planning/v1.13-PROTOCOL-ENUMERATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - "## Gap Item Index" present; all five named items (erase / configure_sram / X88C64 / flash4 / 0x39) appear and each is grep-discoverable. Maps to 72-VALIDATION.md SC#2.
    - Each of the five entries states exactly one of in-scope OR deferred, with a rationale and a named downstream phase (73/74/75/76).
    - Each entry cites a resolvable file:line or named datasheet (eprom_internal_erase, sram.cpp:15-17, chip_database.json X88C64P, flash_type_4.cpp/flash_type_3.cpp:46-48, memory.cpp:89). Confirm by spot-reading 2 citations.
    - No implementation detail / register sequence is specified for any gap item (per Pitfall 4).
  </acceptance_criteria>
  <done>All five named gap items are recorded with a single in-scope/deferred verdict, rationale, downstream-phase route, and a resolvable citation each — no implementation specified.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Anti-Feature block + Ceiling Constraint section</name>
  <files>.planning/v1.13-PROTOCOL-ENUMERATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (artifact being extended)
    - .planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md (§Q5 anti-feature re-confirmation; the RURP_VPP_CEILING_MV location note)
    - firestarter/src/proms/memory.cpp (the infeasible dispatch arm at lines 107-110: 0x11, 0x2A, 0x2B, 0x2C; the protocol!=0 fallback)
    - firestarter/src/proms/not_implemented.cpp (configure_not_implemented effect)
    - firestarter/include/messages.h (MSG_ERR_PROTOCOL_NOT_IMPLEMENTED = 0xBB ~line 96)
    - firestarter_app/firestarter/chip_resolver.py (host guard: resolve_chip refuses non-supported chips)
    - firestarter_app/tools/build_db.py (RURP_VPP_CEILING_MV = 22000 ~line 117; NMOS_TRUE_VPP_MV M2732A=21000 distinction)
    - firestarter_app/tools/check_dispatch.py (_FAMILY_VPP_INVARIANTS configure_eprom (0,22000) ~line 79)
    - firestarter_app/firestarter/data/chip_database.json (the 4 vpp-exceeds-max 25V NMOS chips)
  </read_first>
  <action>
    Add a "## Anti-Feature Block" section with one row per anti-feature ID: 0x11 (IC2_ALG_FWH — LPC 4-wire serial + 3.3V, not parallel), 0x2A (IC2_ALG_GAL16), 0x2B (GAL/PLD, doc-gap flagged from Plan 72-01), 0x2C (IC2_ALG_GAL22) — each citing the firmware fail-closed location (memory.cpp:107-110 → configure_not_implemented → MSG_ERR_PROTOCOL_NOT_IMPLEMENTED=0xBB at messages.h) AND the host guard (chip_resolver.py resolve_chip). Add a 25V NMOS row citing the host vpp-exceeds-max guard and the 4 affected chips, and note the M2732A=21V (supported) vs M2732=25V (infeasible) distinction. Then add a "## Ceiling Constraint" section confirming RURP_VPP_CEILING_MV = 22000 with the build_db.py file:line citation and the check_dispatch.py invariant; state explicitly that the enumeration does NOT recommend relaxing it and that there is no firmware-runtime ceiling enforcement (DB/host enforce it). Add the 0x2B row to the document's notes as a protocol-id.md doc-gap to close in this artifact. This section satisfies SC#3.
  </action>
  <verify>
    <automated>grep -q "RURP_VPP_CEILING_MV = 22000" firestarter_app/tools/build_db.py && grep -qE "0x11" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "0x2A" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "0x2B" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "0x2C" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "vpp-exceeds-max|25V|NMOS" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "RURP_VPP_CEILING_MV.*22000|22000.*RURP_VPP_CEILING_MV|22000 mV" .planning/v1.13-PROTOCOL-ENUMERATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - "## Anti-Feature Block" lists 0x11, 0x2A, 0x2B, 0x2C and a 25V NMOS row; each cites a firmware location (memory.cpp:107-110 / not_implemented.cpp / messages.h 0xBB) and/or the host guard (chip_resolver.py). Maps to 72-VALIDATION.md SC#3.
    - The 0xBB / MSG_ERR_PROTOCOL_NOT_IMPLEMENTED fail-closed effect is cited; `grep -c "configure_not_implemented" firestarter/src/proms/memory.cpp` returns ≥ 1 (verified live: 2).
    - "## Ceiling Constraint" cites RURP_VPP_CEILING_MV = 22000 at build_db.py with line and states the ceiling is NOT relaxed; live grep `grep -n "RURP_VPP_CEILING_MV = 22000" firestarter_app/tools/build_db.py` exits 0.
    - The M2732A (21V, supported) vs M2732 (25V, vpp-exceeds-max) distinction is recorded.
    - The document does not contain any recommendation to relax a fail-closed anti-feature or the ceiling (reviewer confirms — security invariant T-72-03).
  </acceptance_criteria>
  <done>Anti-features 0x11/0x2A/0x2B/0x2C + the 25V NMOS class are re-confirmed fail-closed with cited firmware + host locations, and the 22000 mV ceiling is cited as unchanged with no relaxation recommended.</done>
</task>

<task type="auto">
  <name>Task 3: Add Sources section, finalize header, commit artifact, tick RSCH-01</name>
  <files>.planning/v1.13-PROTOCOL-ENUMERATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/v1.13-PROTOCOL-ENUMERATION.md (artifact being finalized — has all body sections)
    - .planning/phases/72-re-research-the-protocol-landscape/72-RESEARCH.md (§Q6 format/header; §Sources)
    - .planning/REQUIREMENTS.md (RSCH-01 checkbox + status table row)
  </read_first>
  <action>
    Add the document header block per 72-RESEARCH.md §Q6 (Date, Status: COMMITTED (gate for Phases 73-76), Supersedes: v1.12 implicit feasible-set claim, RURP ceiling line with the build_db.py:117 citation) if not already present, and a "## Sources" section listing the file:line citations used across the document (memory.cpp, sram.cpp, eprom.cpp, flash_type_4.cpp, not_implemented.cpp, messages.h, build_db.py, check_dispatch.py, chip_resolver.py, chip_database.json, protocol-id.md, W27C512 datasheet). Verify the whole document is internally consistent (every verdict row's citation resolves; the five gap items reconcile to their table rows). Then update .planning/REQUIREMENTS.md: flip RSCH-01 from `[ ]` to `[x]` and set its status-table row to Complete. Commit both files with message `docs(phase-72): commit v1.13 protocol re-enumeration (RSCH-01)` scoped to `.planning/v1.13-PROTOCOL-ENUMERATION.md` and `.planning/REQUIREMENTS.md` only — do NOT stage sub-repo gitlinks or unrelated working-tree changes.
  </action>
  <verify>
    <automated>grep -qiE "COMMITTED|gate for Phases 73" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "## Sources" .planning/v1.13-PROTOCOL-ENUMERATION.md && grep -qE "^\- \[x\] \*\*RSCH-01\*\*" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Document header records Status COMMITTED + Supersedes v1.12 claim + the ceiling citation. Maps to 72-VALIDATION.md SC#1/SC#3.
    - "## Sources" lists the cited source files (≥ 8 distinct file references).
    - REQUIREMENTS.md RSCH-01 is `[x]` and its status-table row reads Complete (grep anchor passes).
    - The commit contains ONLY the enumeration doc + REQUIREMENTS.md (no sub-repo gitlink bump, no stray working-tree files) — verify via `git show --stat HEAD`.
  </acceptance_criteria>
  <done>The enumeration artifact is finalized with header + Sources, internally consistent, committed alongside a ticked RSCH-01, with no out-of-scope files in the commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enumeration verdict → downstream firmware scope | A mis-classification of an infeasible/anti-feature protocol as feasible could seed an unsafe downstream firmware change |
| document recommendation → VPP ceiling / fail-closed guards | The artifact could, if wrong, recommend relaxing a fail-closed guard or the 22V ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | Gap-item verdicts | mitigate | Each gap entry cites a resolvable file:line/datasheet and routes to a named downstream phase (Task 1 acceptance); no implementation is specified, so a wrong verdict cannot silently become firmware. |
| T-72-03 | Elevation of Privilege | Anti-feature + ceiling re-confirmation | mitigate | SC#3 requires the anti-features stay fail-closed with cited code locations and the 22000 mV ceiling cited as unchanged; Task 2 acceptance forbids any relaxation recommendation. Live grep `RURP_VPP_CEILING_MV = 22000` must exit 0; `configure_not_implemented` arms must remain present. |
| T-72-04 | Tampering | Commit scope | mitigate | Task 3 commits ONLY the enumeration doc + REQUIREMENTS.md, preventing an accidental sub-repo gitlink bump (per CLAUDE.md meta-repo discipline). |

No code is created or modified in this plan; the enumeration is documentation. No HIGH-severity code threats apply (per 72-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- SC#2: all five named gap items grep-present with in-scope/deferred verdicts and downstream routes.
- SC#3: `grep -n "RURP_VPP_CEILING_MV = 22000" firestarter_app/tools/build_db.py` exits 0; anti-feature rows 0x11/0x2A/0x2B/0x2C + 25V NMOS present with cited locations; `grep -c "configure_not_implemented" firestarter/src/proms/memory.cpp` ≥ 1.
- RSCH-01 ticked in REQUIREMENTS.md; commit scoped to the two intended files.
</verification>

<success_criteria>
- The enumeration artifact is complete (table + open questions + gap index + anti-feature block + ceiling + sources), internally consistent, committed, with RSCH-01 marked complete — and recommends NO relaxation of any fail-closed anti-feature or the VPP ceiling.
</success_criteria>

<output>
Create `.planning/phases/72-re-research-the-protocol-landscape/72-02-SUMMARY.md` when done.
</output>
