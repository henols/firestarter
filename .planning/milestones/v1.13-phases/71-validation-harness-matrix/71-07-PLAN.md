---
phase: 71-validation-harness-matrix
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_validate_oracle.py
autonomous: true
gap_closure: true
requirements: [HARN-03]
user_setup: []

must_haves:
  truths:
    - "A verdict_int==0 PASS verdict is driven by write_cycle_eprom's real return code (its internal source-vs-readback SHA compare), NOT by a source==source self-comparison"
    - "_classify_sha_result is proven correct by a test that supplies DISTINCT source_sha and readback_sha (one match case, one mismatch case)"
    - "Leonardo PASS stays authoritative; non-Leonardo stays advisory; uno328pb stays hard N/A; write_cycle_eprom==1 still maps to FAIL"
  artifacts:
    - path: "firestarter_app/firestarter/cli_handlers.py"
      provides: "verdict_int==0 branch mapping board-class verdict WITHOUT a guaranteed-PASS self-comparison call"
      contains: "dev_validate_family"
    - path: "firestarter_app/tests/test_validate_oracle.py"
      provides: "Distinct-hash comparator proof + non-vacuous verdict_int==0 PASS-path coverage"
  key_links:
    - from: "firestarter_app/firestarter/cli_handlers.py dev_validate_family verdict_int==0 branch"
      to: "write_cycle_eprom return code"
      via: "board-class verdict mapping (no source==source compare)"
      pattern: "verdict_int == 0"
---

<objective>
Close GAP-1 (HARN-03 / SC#3): the `dev validate-family` hardware PASS verdict is currently vacuous. At `cli_handlers.py:1556-1562` the `verdict_int == 0` branch calls `_classify_sha_result(evidence_sha, evidence_sha, board)` — the source-image SHA compared to itself — so the oracle always yields PASS regardless of what was read back.

Locked operator decision (FINAL — do not propose alternatives): TRUST `write_cycle_eprom`'s return code directly. `write_cycle_eprom` (eprom_operations.py:774) already computes a real `source_sha = hashlib.sha256(Path(source_image_path).read_bytes()).hexdigest()` and (per its docstring, eprom_operations.py:756-766) returns 0 ONLY when every read-back matched that SHA, 1 on mismatch, 2 on hw-error. PREMISE CONFIRMED during planning — the real readback compare lives inside write_cycle_eprom; the caller must stop adding a vacuous self-compare and instead map verdict_int==0 to the board-authoritative verdict directly.

Do NOT change `write_cycle_eprom`'s signature/return contract (operator explicitly rejected surfacing the readback SHA). `_classify_sha_result` remains in the module as a correct comparator and MUST be proven non-dead by a test supplying distinct hashes.

Purpose: make the SC#3 "non-vacuous PASS oracle" truth real for the hardware path so a green Tier-3 cell cannot be a self-match artifact.
Output: edited cli_handlers.py verdict_int==0 branch + updated/added tests in test_validate_oracle.py.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/71-validation-harness-matrix/71-VERIFICATION.md
@.planning/phases/71-validation-harness-matrix/71-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: De-vacuum the verdict_int==0 branch in dev_validate_family</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - firestarter_app/firestarter/cli_handlers.py (dev_validate_family ~1446-1586; the verdict_int==0 self-comparison call site at 1558-1564; _classify_sha_result at 1393-1414; _AUTHORITATIVE_PASS_BOARD at 1277; the dead _EVIDENCE_SHA_SOFTWARE_SENTINEL at 1263-1265; the datetime.utcnow() at 1358)
    - firestarter_app/firestarter/eprom_operations.py (write_cycle_eprom 747-766 — CONFIRM the 0=PASS/1=FAIL/2=hw-error contract and the real internal source_sha compare at line 774 before relying on it)
  </read_first>
  <behavior>
    - verdict_int==0 on board=="leonardo" → cell_verdict "PASS" (authoritative), with NO call of the form _classify_sha_result(evidence_sha, evidence_sha, ...)
    - verdict_int==0 on a non-Leonardo, non-uno328pb board (e.g. "uno") → cell_verdict "PASS" but classified advisory (the advisory-vs-authoritative distinction _classify_sha_result provided MUST be preserved)
    - verdict_int==1 → cell_verdict "FAIL" (unchanged)
    - verdict_int==2 → cell_verdict "SKIP-deferred" (unchanged)
    - uno328pb path unchanged (still hard N/A, handled earlier at ~1485-1502)
  </behavior>
  <action>
    In dev_validate_family, replace the verdict_int==0 branch (currently calling `_classify_sha_result(evidence_sha or "", evidence_sha or "", board)`) so it derives the verdict from write_cycle_eprom's return code directly. Because verdict_int==0 already means write_cycle_eprom's real internal readback-vs-source SHA compare passed, map it to "PASS" without any SHA self-comparison. Preserve the board-class semantics that _classify_sha_result encoded: Leonardo is authoritative, every other board (uno328pb already excluded earlier) is advisory. The verdict string for a passing cell is "PASS" on all board classes (matching the prior behavior where a match yielded "PASS" for both authoritative and advisory paths); capture the authoritative/advisory distinction in the cell record where it is observable. Add a `pass_type` field to the verdict_int==0 cell dict, set to "authoritative" when `board == _AUTHORITATIVE_PASS_BOARD` else "advisory", so the board-class verdict mapping is not lost. The result MUST NOT contain a `_classify_sha_result(evidence_sha, evidence_sha, ...)` call (source compared to itself). Do not call _classify_sha_result at this site at all unless you pass it a genuinely distinct readback signal — which the contract does not provide — so the cleanest form is a direct map: pass_type = "authoritative" if board==_AUTHORITATIVE_PASS_BOARD else "advisory"; cell_verdict = "PASS". Keep evidence_sha derived from the source image as the cell's evidence_sha field (it documents WHICH image was written — that is its legitimate role; it is no longer used as a fake comparison input). Leave verdict_int==1 → "FAIL" and verdict_int==2 → "SKIP-deferred" untouched. While editing this file: (WR-01, in-file/cheap) replace `datetime.datetime.utcnow()` at line 1358 with `datetime.datetime.now(datetime.timezone.utc)`; (IN-03, in-file/cheap) remove the unused `_EVIDENCE_SHA_SOFTWARE_SENTINEL` definition at 1263-1265 — confirm via grep it has no other reference before deleting. Do NOT touch write_cycle_eprom or its signature. Honor the firestarter_app tooling gate: keep ruff check + ruff format --check clean and mypy-strict clean for cli_handlers.py (target py3.9/3.11, not the devcontainer's 3.12).
  </action>
  <verify>
    <automated>cd firestarter_app && python -c "import re,sys; s=open('firestarter/cli_handlers.py').read(); sys.exit(1 if re.search(r'_classify_sha_result\(\s*evidence_sha', s) else 0)" && grep -c "_EVIDENCE_SHA_SOFTWARE_SENTINEL" firestarter/cli_handlers.py | grep -qx 0 && grep -q "datetime.now(datetime.timezone.utc)" firestarter/cli_handlers.py && python -m mypy firestarter/cli_handlers.py --strict</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/firestarter/cli_handlers.py` no longer contains any `_classify_sha_result(evidence_sha, ...)` self-comparison call (regex `_classify_sha_result\(\s*evidence_sha` matches nothing).
    - The verdict_int==0 cell dict carries a `pass_type` key equal to "authoritative" when board=="leonardo" else "advisory".
    - `_EVIDENCE_SHA_SOFTWARE_SENTINEL` no longer appears in cli_handlers.py (grep count == 0).
    - `datetime.now(datetime.timezone.utc)` is present; `datetime.utcnow()` no longer appears.
    - `cd firestarter_app && ruff check firestarter/cli_handlers.py` exits 0 and `ruff format --check firestarter/cli_handlers.py` exits 0.
    - `cd firestarter_app && python -m mypy firestarter/cli_handlers.py --strict` exits 0 (cli_handlers.py is on the strict-mypy module list per firestarter_app/CLAUDE.md).
    - write_cycle_eprom signature/return contract is unchanged (eprom_operations.py untouched).
  </acceptance_criteria>
  <done>The hardware PASS path is driven by write_cycle_eprom's real return code; no source==source self-comparison remains; board-class (authoritative/advisory) verdict mapping is preserved on the cell; WR-01 and IN-03 cleaned in-file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the oracle is non-vacuous and the comparator is exercised with distinct hashes</name>
  <files>firestarter_app/tests/test_validate_oracle.py</files>
  <read_first>
    - firestarter_app/tests/test_validate_oracle.py (existing 18 tests; especially test_write_cycle_pass_on_leonardo_is_authoritative at ~381 which currently passes through the vacuous path, and test_negative_control_write_cycle_returns_fail at ~95)
    - firestarter_app/firestarter/cli_handlers.py (the edited verdict_int==0 branch + _classify_sha_result)
  </read_first>
  <behavior>
    - Comparator match case: _classify_sha_result(readback_sha=A, source_sha=A, board="leonardo") → verdict "PASS", pass_type "authoritative" (A is a real, non-source-aliased value)
    - Comparator mismatch case: _classify_sha_result(readback_sha=B, source_sha=A, board="leonardo") with A != B → verdict "FAIL", pass_type "authoritative"
    - verdict_int==0 PASS path (CLI invoke, write_cycle_eprom mocked to 0, board=leonardo): exit 0, cell verdict "PASS", cell pass_type "authoritative", and write_cycle_eprom called exactly once (the PASS is sourced from the return code, not a self-compare)
    - verdict_int==0 advisory path (board="uno"): cell pass_type "advisory"
    - Negative control retained: write_cycle_eprom mocked to 1 → exit 1, cell verdict "FAIL"
  </behavior>
  <action>
    Update test_write_cycle_pass_on_leonardo_is_authoritative so it no longer relies on (or asserts) the vacuous self-comparison path: assert that with write_cycle_eprom mocked to return 0 on board=leonardo, the emitted cell verdict is "PASS" AND its pass_type is "authoritative" AND `operator.write_cycle_eprom.assert_called_once()` holds (the PASS is driven by the real return code). Add a sibling test for board="uno" asserting the emitted cell pass_type is "advisory" while verdict stays "PASS". Add/keep two direct comparator tests on _classify_sha_result using DISTINCT, non-aliased hashes — one match case (readback_sha == source_sha computed from genuinely different byte sources that happen to hash equal is unnecessary; just pass the SAME literal hex string for a match, but the point is both arguments are SUPPLIED distinctly at the call site, not one variable passed twice) and one mismatch case (sha256(b"a") vs sha256(b"b")) — asserting verdict PASS/FAIL respectively. The mismatch test (sha A vs sha B → FAIL on leonardo) is the load-bearing proof the comparator can fail and is therefore not dead/vacuous; the existing test_classify_sha_mismatch_is_fail_on_leonardo at ~69 already covers this — confirm it stays and references it in the comment as the comparator-non-dead proof. Keep test_negative_control_write_cycle_returns_fail unchanged. Honor ruff/mypy gate.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_validate_oracle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python -m pytest tests/test_validate_oracle.py` exits 0.
    - A test asserts the verdict_int==0 leonardo PASS path emits cell verdict "PASS" with pass_type "authoritative" AND `write_cycle_eprom.assert_called_once()` (PASS sourced from the return code).
    - A test asserts the verdict_int==0 board="uno" path emits pass_type "advisory".
    - A test exercises `_classify_sha_result` with a mismatch (two distinct SHA-256 hex digests) on board="leonardo" and asserts verdict "FAIL" (comparator proven non-dead).
    - The negative control (write_cycle_eprom==1 → exit 1 / FAIL cell) still passes.
    - `cd firestarter_app && ruff check tests/test_validate_oracle.py` exits 0.
  </acceptance_criteria>
  <done>The verdict_int==0 PASS path is proven to come from write_cycle_eprom's return code (not a self-compare); _classify_sha_result is proven correct on distinct match/mismatch hashes; negative control retained.</done>
</task>

</tasks>

<verification>
- `cd firestarter_app && python -m pytest tests/test_validate_oracle.py` exits 0.
- `cd firestarter_app && ruff check firestarter/cli_handlers.py tests/test_validate_oracle.py` exits 0; `ruff format --check firestarter/cli_handlers.py tests/test_validate_oracle.py` exits 0.
- No `_classify_sha_result(evidence_sha, evidence_sha, ...)` self-comparison remains in cli_handlers.py.
- write_cycle_eprom signature/return contract unchanged (eprom_operations.py not in files_modified).
- Zero production firmware flash change (host-Python only).
</verification>

<success_criteria>
SC#3 / HARN-03 is met for the hardware path: a verdict_int==0 PASS is authoritative because write_cycle_eprom's own source-vs-readback SHA compare returned 0 — not because the runner compared the source image to itself. The comparator _classify_sha_result is retained and proven non-vacuous via a distinct-hash mismatch test. Leonardo-authoritative / advisory-on-other-boards semantics preserved; uno328pb N/A and the verdict_int==1 FAIL negative control unchanged.
</success_criteria>

<output>
Create `.planning/phases/71-validation-harness-matrix/71-07-SUMMARY.md` when done.
</output>
