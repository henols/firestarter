---
phase: 71-validation-harness-matrix
plan: 05
type: execute
wave: 2
depends_on: ["71-02"]
files_modified:
  - firestarter_app/tests/test_val_wire_eprom.py
  - firestarter_app/tests/test_val_wire_eeprom28c.py
  - firestarter_app/tests/test_val_wire_flash3.py
  - firestarter_app/tests/test_val_wire_flash4.py
  - firestarter_app/tests/test_val_wire_flash_intel.py
  - firestarter_app/tests/test_val_wire_sram.py
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "For each of the 6 families, the host wire dict built for its representative chip carries the correct algorithm field and dispatches to the correct handler via dispatch() — with no serial port"
    - "The representative chip per family is sourced from the authored validation_matrix_spec.json (single source of truth), not hardcoded ad hoc"
    - "The Tier-2 tests reuse make_comm/fake_serial and EpromDatabase.convert_to_programmer — no re-implementation of wire construction or dispatch"
  artifacts:
    - path: "firestarter_app/tests/test_val_wire_eprom.py"
      provides: "Tier-2 host wire round-trip for configure_eprom family"
    - path: "firestarter_app/tests/test_val_wire_sram.py"
      provides: "Tier-2 host wire round-trip for configure_sram family (asserts SRAM protocol routes to configure_sram, never configure_eprom)"
  key_links:
    - from: "test_val_wire_*.py"
      to: "tools.check_dispatch.dispatch + EpromDatabase.convert_to_programmer"
      via: "build wire dict for rep_chip, assert algorithm field + dispatch() handler"
      pattern: "dispatch\\("
    - from: "test_val_wire_*.py rep_chip"
      to: "validation_matrix_spec.json families[].rep_chip"
      via: "reads the authored spec for the representative chip name"
      pattern: "validation_matrix_spec"
---

<objective>
Create the 6 Tier-2 host pytest wire round-trip suites (HARN-01), one per family, each proving —
WITHOUT a serial port, via `make_comm`/`fake_serial` — that the host wire dict for that family's
representative chip carries the correct `algorithm` field and reaches the correct firmware handler
through the SAME `dispatch()` logic already exercised by `check_dispatch.py`. Reuse, do not
re-implement (D-10): `EpromDatabase.convert_to_programmer()` builds the wire dict, `dispatch()`
maps it.

Purpose: Tier-2 validates the host→wire half of each family's path without hardware, completing
(with Plan 04) all 6 families' software cells GREEN per D-07.
Output: 6 test files reading the authored spec for representative chips; all green; coverage held.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/71-validation-harness-matrix/71-CONTEXT.md
@.planning/phases/71-validation-harness-matrix/71-PATTERNS.md
@.planning/phases/71-validation-harness-matrix/71-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier-2 wire round-trip suites for the EPROM + EEPROM28c + flash families</name>
  <files>firestarter_app/tests/test_val_wire_eprom.py, firestarter_app/tests/test_val_wire_eeprom28c.py, firestarter_app/tests/test_val_wire_flash3.py, firestarter_app/tests/test_val_wire_flash4.py, firestarter_app/tests/test_val_wire_flash_intel.py</files>
  <behavior>
    - For eprom rep_chip: wire dict algorithm == 7 (or 8/11) AND dispatch(algorithm, type) == "configure_eprom"
    - For eeprom28c rep_chip (AT28C256): algorithm == 13 AND dispatch(...) == "configure_eeprom28c"
    - For flash3 rep_chip: algorithm == 6 AND dispatch(...) == "configure_flash3"
    - For flash4 rep_chip: algorithm in {5,53,57} AND dispatch(...) == "configure_flash4"
    - For flash_intel rep_chip: algorithm == 16 AND dispatch(...) == "configure_flash_intel"
    - Each representative chip name is read from validation_matrix_spec.json, not hardcoded
  </behavior>
  <read_first>
    - firestarter_app/tests/conftest.py (make_comm / fake_serial fixture API, lines ~65-151)
    - firestarter_app/tools/check_dispatch.py (the dispatch() function signature + return values to assert against)
    - firestarter_app/firestarter/database.py (EpromDatabase.get_eprom + convert_to_programmer — the wire-dict builder to reuse)
    - firestarter_app/tools/validation_matrix_spec.json (post-Plan-02 — the rep_chip per family)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"test_val_wire_eprom.py (and 5 sibling files)" (exact import + fixture + assertion pattern; make_comm is a factory call)
    - .planning/phases/71-validation-harness-matrix/71-RESEARCH.md §"Tier-2 Host Wire Round-Trip" (the round-trip pattern; Don't-Hand-Roll table — use convert_to_programmer + dispatch, not manual JSON)
  </read_first>
  <action>
    Create 5 Tier-2 test files (test_val_wire_eprom, _eeprom28c, _flash3, _flash4, _flash_intel).
    Each loads its representative chip name from `tools/validation_matrix_spec.json` (single source of
    truth — do NOT hardcode chip names ad hoc), looks it up via `EpromDatabase.get_eprom`, builds the
    wire dict via `convert_to_programmer`, and asserts (a) the `algorithm` field equals the expected
    protocol for that family and (b) `dispatch(algorithm, type)` returns the expected `configure_*`
    handler. Accept the `make_comm`/`fake_serial` fixtures as parameters to confirm the no-serial-port
    posture (per the analog pattern; the fixtures are auto-discovered). Reuse the production converter
    and the existing `dispatch()` — do NOT construct wire JSON by hand or mirror dispatch logic
    (Don't-Hand-Roll). Fully ruff-clean / type-safe where applicable.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && pytest tests/test_val_wire_eprom.py tests/test_val_wire_eeprom28c.py tests/test_val_wire_flash3.py tests/test_val_wire_flash4.py tests/test_val_wire_flash_intel.py -x -q && ruff check tests/test_val_wire_*.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_val_wire_{eprom,eeprom28c,flash3,flash4,flash_intel}.py -x` exits 0.
    - Each test asserts both the wire `algorithm` field AND `dispatch(algorithm, type) == "configure_<family>"`.
    - Representative chip names are read from validation_matrix_spec.json (grep shows the spec path referenced), not string-literal chip names scattered in each file.
    - Wire dicts are built via `EpromDatabase.convert_to_programmer` and dispatch via `tools.check_dispatch.dispatch` (no hand-rolled JSON / dispatch).
    - `ruff check tests/test_val_wire_*.py` + `ruff format --check` exit 0.
  </acceptance_criteria>
  <done>Five family Tier-2 wire round-trips prove correct algorithm + handler routing without a serial port.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tier-2 wire round-trip for SRAM (must route to configure_sram, never configure_eprom)</name>
  <files>firestarter_app/tests/test_val_wire_sram.py</files>
  <behavior>
    - For sram rep_chip: wire dict algorithm in {14,39,40,41} (0x0E/0x27/0x28/0x29) AND dispatch(...) == "configure_sram"
    - The SRAM rep chip never dispatches to configure_eprom (the 12V-VPP-on-5V-part hazard)
    - The rep_chip is read from validation_matrix_spec.json
  </behavior>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (_SRAM_PROTOCOLS + dispatch() SRAM branch)
    - firestarter_app/tools/validation_matrix_spec.json (the sram family rep_chip)
    - firestarter/CLAUDE.md §"Protocol Dispatch" (the BLOCKER-2 invariant: SRAM mem_type=1 chips MUST NOT reach configure_eprom)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"test_val_wire_eprom.py (and 5 sibling files)" (the sibling pattern to mirror)
  </read_first>
  <action>
    Create test_val_wire_sram.py mirroring the Task-1 sibling pattern but with the safety-critical
    assertion that the SRAM representative chip's wire dict dispatches to `configure_sram` AND
    explicitly NOT to `configure_eprom` (the documented 12V-VPP-on-a-5V-part hazard, BLOCKER-2).
    Read the rep_chip from the authored spec. Reuse convert_to_programmer + dispatch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && pytest tests/test_val_wire_sram.py -x -q && ruff check tests/test_val_wire_sram.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_val_wire_sram.py -x` exits 0.
    - The test asserts `dispatch(algorithm, type) == "configure_sram"` AND `dispatch(...) != "configure_eprom"` for the SRAM rep chip.
    - rep_chip is read from validation_matrix_spec.json.
    - `ruff check` + `ruff format --check` exit 0; `cd firestarter_app && pytest tests/ --cov-fail-under=70` stays green.
  </acceptance_criteria>
  <done>The SRAM Tier-2 wire test proves SRAM routes to configure_sram and never to the VPP-asserting configure_eprom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host DB lookup → wire dict → dispatch | The host-side path from chip name to firmware handler, validated with no serial port |
| SRAM family → handler | SRAM must never reach configure_eprom (12V on a 5V part) — the BLOCKER-2 hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-SRAM-EPROM | Tampering / Damage | SRAM rep chip routing to configure_eprom | mitigate | test_val_wire_sram asserts dispatch() == configure_sram AND != configure_eprom (BLOCKER-2) |
| T-71-WIREDRIFT | Tampering | hand-rolled wire dict diverging from production converter | mitigate | Tests reuse EpromDatabase.convert_to_programmer + the production dispatch() — no mirror to drift (Don't-Hand-Roll) |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses pytest + existing fixtures (D-10). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `pytest tests/test_val_wire_*.py -x` exits 0 (all 6 family wire round-trips).
- SRAM test asserts configure_sram and not configure_eprom.
- `ruff check` + `ruff format --check` on the 6 files exit 0.
- `cd firestarter_app && pytest tests/ --cov-fail-under=70` stays green.
</verification>

<success_criteria>
Six Tier-2 host wire round-trip suites prove each family's representative chip (sourced from the
authored matrix spec) builds the correct `algorithm` field and dispatches to the correct handler
with no serial port, reusing the production converter + dispatch — completing the host software
half of the harness (HARN-01 Tier-2, D-07).
</success_criteria>

<output>
Create `.planning/phases/71-validation-harness-matrix/71-05-SUMMARY.md` when done
</output>
