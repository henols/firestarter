---
phase: 71-validation-harness-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/test/native/avr/_shared/host_stubs_common.inc
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "Native test suites can opt in to a register-write recording buffer via #define HOST_STUBS_RECORD_BUS"
    - "Existing native suites (test_dispatch, test_cobs_*, test_read_timing) compile and pass unchanged with the flag off"
    - "Production pio run -e uno / -e leonardo flash byte-count is unchanged (test-only file)"
  artifacts:
    - path: "firestarter/test/native/avr/_shared/host_stubs_common.inc"
      provides: "Define-guarded recording bus buffer extending the shared stub in place (D-04, WR-06 single-edit-point)"
      contains: "HOST_STUBS_RECORD_BUS"
  key_links:
    - from: "host_stubs_common.inc #ifdef HOST_STUBS_RECORD_BUS branch"
      to: "rurp_write_to_register"
      via: "recording version replaces the no-op when the flag is defined; #else keeps today's no-op"
      pattern: "HOST_STUBS_RECORD_BUS"
---

<objective>
Extend the SHARED native-test stub `host_stubs_common.inc` in place with a define-guarded
register-write recording buffer (per D-04), so a later per-family Tier-1 suite can prove a
firmware handler's behavior by side-effect (the sequence of `rurp_write_to_register` calls),
not merely by operation-pointer presence.

Purpose: This is the highest-novelty, lowest-blast-radius foundation of the HARN-01 Tier-1
harness. Getting the define-guard contract right BEFORE any family suite is built on top of it
prevents the linker-error pitfall (RESEARCH §Pitfall 1) and preserves the WR-06 consolidation
(no second forked stub `.inc`).
Output: One modified test-only `.inc` file; existing suites still green; zero production flash.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/71-validation-harness-matrix/71-CONTEXT.md
@.planning/phases/71-validation-harness-matrix/71-PATTERNS.md
@.planning/phases/71-validation-harness-matrix/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the define-guarded recording buffer to host_stubs_common.inc</name>
  <files>firestarter/test/native/avr/_shared/host_stubs_common.inc</files>
  <read_first>
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (the file being modified; read its full current contents incl. the WR-06 header comment lines 36-39 anticipating recording, the current no-op rurp_write_to_register, and the two existing opt-OUT guard patterns HOST_STUBS_CUSTOM_VOLTAGE_MV / HOST_STUBS_CUSTOM_HW_REVISION)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"host_stubs_common.inc (MODIFIED)" (exact replacement block + opt-IN-vs-opt-OUT rationale)
    - .planning/phases/71-validation-harness-matrix/71-RESEARCH.md §"Recording Bus Stub (HARN-01 / D-04)" (which rurp_* functions to intercept; the per-family observable table)
  </read_first>
  <action>
    Replace the current no-op `rurp_write_to_register` in `host_stubs_common.inc` with the
    define-guarded version from 71-PATTERNS.md: an `#ifdef HOST_STUBS_RECORD_BUS` branch that
    declares a fixed-size recording buffer (`HOST_STUBS_MAX_RECORDING 256`, a
    `bus_record_entry_t { uint8_t reg; uint8_t data; }` array, and a count), records each
    `(reg, data)` pair on `rurp_write_to_register`, and exposes the C-linkage recording API
    `clear_bus_recording()`, `bus_recording_count()`, `recorded_reg(int)`, `recorded_data(int)`;
    plus an `#else` branch that preserves today's exact no-op (`(void)reg; (void)data;`).
    The recording version must bound-check against `HOST_STUBS_MAX_RECORDING` (mitigates threat
    T-71-02 buffer overflow). This is the D-04 opt-IN (inverse of the existing opt-OUT
    HOST_STUBS_CUSTOM_* guards). Do NOT create a second stub file — single edit point per WR-06.
    Add the Phase 71 / HARN-01 / D-04 banner comment from PATTERNS so a future reader knows
    existing suites MUST NOT define the flag. No other rurp_* stub is modified in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c "HOST_STUBS_RECORD_BUS" test/native/avr/_shared/host_stubs_common.inc | grep -qv '^0$' && grep -q "bus_recording_count" test/native/avr/_shared/host_stubs_common.inc && grep -q "(void)reg; (void)data;" test/native/avr/_shared/host_stubs_common.inc && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '#ifdef HOST_STUBS_RECORD_BUS' test/native/avr/_shared/host_stubs_common.inc` matches (opt-IN guard present).
    - The four recording API symbols `clear_bus_recording`, `bus_recording_count`, `recorded_reg`, `recorded_data` are each declared `extern "C"` inside the `#ifdef` branch.
    - The `#else` branch contains the byte-identical original no-op body (`(void)reg; (void)data;`).
    - A `HOST_STUBS_MAX_RECORDING` bound is enforced before each store (no unbounded write).
    - No new file is created under `test/native/avr/_shared/` (single-edit-point WR-06 preserved).
  </acceptance_criteria>
  <done>The shared stub carries a define-guarded recording buffer; flag-off path is byte-equivalent to the previous no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Prove flag-off regression — existing native suites compile and pass unchanged</name>
  <files>firestarter/test/native/avr/_shared/host_stubs_common.inc</files>
  <read_first>
    - firestarter/platformio.ini §[env:native] (the test_filter allowlist lines 80-88 — the existing suites that MUST stay green)
    - .planning/phases/71-validation-harness-matrix/71-RESEARCH.md §"Pitfall 1: Define-guard ordering" (the undefined-reference symptom to guard against)
  </read_first>
  <action>
    Run the existing native suite battery to prove the recording extension is a true no-op when
    `HOST_STUBS_RECORD_BUS` is undefined: the existing suites (test_dispatch, test_cobs_data_frame,
    test_cobs_cmd_frame, test_read_timing, test_messages, test_data_input, test_not_implemented,
    test_frame_vectors) must compile and pass exactly as before. None of them define the flag, so
    none must reference the recording API symbols. If a linker "undefined reference to
    clear_bus_recording / bus_recording_count" surfaces, the guard ordering in Task 1 is wrong —
    fix the `#ifdef`/`#else` boundary so the recording API exists ONLY inside the `#ifdef` branch.
    Also confirm production flash is untouched by building `pio run -e uno` and `pio run -e leonardo`
    and confirming the byte count is unchanged vs the pre-edit baseline (the file lives under
    test/ and is PIO-excluded from production src_filter — the build proves it).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_dispatch*" && pio test -e native -f "*test_cobs*" && pio test -e native -f "*test_read_timing*"</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "*test_dispatch*"` exits 0 (existing dispatch suite green with flag off).
    - `pio test -e native -f "*test_cobs*"` and `pio test -e native -f "*test_read_timing*"` exit 0.
    - No linker "undefined reference to (clear_bus_recording|bus_recording_count|recorded_reg|recorded_data)" appears in any existing-suite build log.
    - `pio run -e uno` and `pio run -e leonardo` both succeed; reported flash byte-count is unchanged from before this plan (delta == 0).
  </acceptance_criteria>
  <done>All pre-existing native suites pass with HOST_STUBS_RECORD_BUS undefined; production flash byte-count delta is zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test build → production build | Test-only `.inc` must never link into `pio run -e uno/leonardo`; flash must be unchanged |
| recording buffer writes | Bounded array in native test process; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-02 | Denial of Service | recording buffer in host_stubs_common.inc | mitigate | Cap at HOST_STUBS_MAX_RECORDING=256 with a bound check before each store; firmware configure_* makes <20 register writes per init so no overflow is reachable (RESEARCH §Security Domain) |
| T-71-FLASH | Tampering | production firmware flash size | mitigate | File is under test/ (PIO-excluded from src_filter); Task 2 builds -e uno + -e leonardo and asserts byte-count delta == 0 |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages; reuses present PlatformIO/Unity/ArduinoFake substrate (D-10). No package legitimacy checkpoint needed. |
</threat_model>

<verification>
- `pio test -e native -f "*test_dispatch*"` exits 0 (flag-off regression baseline).
- `grep "HOST_STUBS_RECORD_BUS" test/native/avr/_shared/host_stubs_common.inc` finds the guard.
- `pio run -e uno && pio run -e leonardo` succeed with unchanged flash byte-count.
</verification>

<success_criteria>
The shared native stub gains a define-guarded recording bus buffer; existing suites are
byte-behaviorally unchanged with the flag off; zero production flash added. The recording API
(clear/count/recorded_reg/recorded_data) is available to any suite that opts in via
`#define HOST_STUBS_RECORD_BUS`.
</success_criteria>

<output>
Create `.planning/phases/71-validation-harness-matrix/71-01-SUMMARY.md` when done
</output>
