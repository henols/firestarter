---
phase: 71-validation-harness-matrix
plan: 04
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp
  - firestarter/test/native/avr/test_val_eprom/host_stubs.cpp
  - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp
  - firestarter/test/native/avr/test_val_eeprom28c/host_stubs.cpp
  - firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp
  - firestarter/test/native/avr/test_val_flash3/host_stubs.cpp
  - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp
  - firestarter/test/native/avr/test_val_flash4/host_stubs.cpp
  - firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp
  - firestarter/test/native/avr/test_val_flash_intel/host_stubs.cpp
  - firestarter/test/native/avr/test_val_sram/test_val_sram.cpp
  - firestarter/test/native/avr/test_val_sram/host_stubs.cpp
  - firestarter/platformio.ini
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "Six Tier-1 native Unity suites (one per family) prove handler behavior by recorded register-write side-effect"
    - "VPP families (eprom, flash_intel) prove a VPP-enabling CTL bit fires on write and a negative-control read proves it does NOT fire"
    - "Non-VPP families (eeprom28c, flash3, flash4) prove NO VPP-enable CTL bit ever fires"
    - "The SRAM suite asserts bus_recording_count() == 0 — documenting the current no-op state (VAL-06 resolution deferred to Phase 73)"
    - "All 6 suites are added to the positive test_filter allowlist and run under pio test -e native; production flash byte-count unchanged"
  artifacts:
    - path: "firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp"
      provides: "Tier-1 recording suite for configure_eprom 0x07/0x08/0x0B"
    - path: "firestarter/platformio.ini"
      provides: "6 new test_val_* entries in test_filter allowlist + build_flags -I includes"
      contains: "test_val_eprom"
  key_links:
    - from: "test_val_*/host_stubs.cpp"
      to: "host_stubs_common.inc recording buffer"
      via: "#define HOST_STUBS_RECORD_BUS before the shared include"
      pattern: "HOST_STUBS_RECORD_BUS"
    - from: "test_val_*/test_val_*.cpp"
      to: "configure_memory dispatch + recorded CTL register sequence"
      via: "configure_memory(&h) then iterate recorded_reg/recorded_data"
      pattern: "bus_recording_count"
---

<objective>
Stand up the 6 Tier-1 native Unity per-family suites (HARN-01), each driven by the recording bus
stub (Plan 01) and consuming the generated family header (Plan 02). Each suite proves the family's
handler behavior BY SIDE-EFFECT — the recorded `rurp_write_to_register` sequence — including the
in-tier negative control (a read-path that must NOT enable VPP), per D-08's "verify can fail"
posture. Register all 6 suites in the positive `test_filter` allowlist so they actually run.

Purpose: This delivers all 6 families' Tier-1 cells GREEN per D-07, the flash-free software half of
the harness. Provable-by-side-effect catches a handler that is wired but electrically wrong.
Output: 6 suite directories (test + host_stubs each), platformio.ini updated, all green under
`pio test -e native`, zero production flash.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/71-validation-harness-matrix/71-CONTEXT.md
@.planning/phases/71-validation-harness-matrix/71-PATTERNS.md
@.planning/phases/71-validation-harness-matrix/71-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the VPP + negative-control families (eprom, flash_intel) and the non-VPP families (eeprom28c, flash3, flash4)</name>
  <files>firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp, firestarter/test/native/avr/test_val_eprom/host_stubs.cpp, firestarter/test/native/avr/test_val_flash_intel/test_val_flash_intel.cpp, firestarter/test/native/avr/test_val_flash_intel/host_stubs.cpp, firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp, firestarter/test/native/avr/test_val_eeprom28c/host_stubs.cpp, firestarter/test/native/avr/test_val_flash3/test_val_flash3.cpp, firestarter/test/native/avr/test_val_flash3/host_stubs.cpp, firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp, firestarter/test/native/avr/test_val_flash4/host_stubs.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (the authoritative native-suite pattern: imports, setUp with ArduinoFake Serial mocks, make_handle helper, main()/UNITY_BEGIN structure)
    - firestarter/test/native/avr/test_flash_intel_vpp/host_stubs.cpp (the host_stubs.cpp opt-IN/opt-OUT include pattern + mock voltage shape for the flash_intel suite)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (post-Plan-01 — the recording API symbols the suites call)
    - firestarter/src/proms/eprom.cpp (which exact CTL bit configure_eprom asserts on write: CTRL_VPP_VPE_DROP_ENABLE for 0x07/0x08, direct 12-18V for 0x0B — derive the assertion target from the real handler, NOT a guessed constant)
    - firestarter/src/proms/flash_intel.cpp (configure_flash_intel CTRL_VPP_P1_ENABLE 0x08 path)
    - firestarter/src/proms/eeprom_28c.cpp and firestarter/src/proms/flash_type_3.cpp and firestarter/src/proms/flash_type_4.cpp (confirm these 5V handlers never set a VPP-enable CTL bit)
    - firestarter/include/rurp_pinout.h (CTRL_VPP_REGULATOR_ENABLE 0x80, CTRL_VPP_VPE_DROP_ENABLE, CTRL_VPP_P1_ENABLE 0x08, CTRL_VPP_A9_ENABLE, CTRL_VPE_ENABLE — the bit constants to test against)
    - firestarter/CLAUDE.md §"Algorithm Handlers" + §"Constants" (canonical per-family VPP mechanism)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"test_val_eprom/test_val_eprom.cpp" + §"test_val_eprom/host_stubs.cpp" (exact suite + stub patterns, the SRAM override note)
  </read_first>
  <action>
    Create 5 suite directories under `firestarter/test/native/avr/`: test_val_eprom (0x07/0x08/0x0B),
    test_val_flash_intel (0x10), test_val_eeprom28c (0x0D), test_val_flash3 (0x06),
    test_val_flash4 (0x05/0x35/0x39). Each gets a `host_stubs.cpp` that `#define HOST_STUBS_RECORD_BUS`
    BEFORE `#include "../_shared/host_stubs_common.inc"` (Pitfall 1 — order matters), plus for
    test_val_flash_intel a mock `rurp_read_voltage_mv()` returning a valid 12V via the
    HOST_STUBS_CUSTOM_VOLTAGE_MV opt-OUT (so the 12V P1 path is reachable). Each `test_val_*.cpp`
    mirrors test_configure_memory.cpp (imports, setUp with the ArduinoFake Serial mocks + a
    clear_bus_recording() call, the make_handle helper, main()/UNITY_BEGIN/UNITY_END). For the VPP
    families: a POSITIVE test running configure_memory with CMD_WRITE and asserting the
    family-appropriate VPP-enable CTL bit appears in the recorded sequence (derive the exact bit
    from the handler source you read — eprom uses CTRL_VPP_VPE_DROP_ENABLE, flash_intel uses
    CTRL_VPP_P1_ENABLE), AND a NEGATIVE-CONTROL test running CMD_READ asserting that VPP-enable bit
    is NEVER set in any CTL write (proves verify-can-fail in-tier per D-08). For the non-VPP families
    (eeprom28c/flash3/flash4): assert that across the full recorded sequence NO CTL write ever sets a
    VPP-enable bit (CTRL_VPP_REGULATOR_ENABLE / CTRL_VPP_P1_ENABLE / CTRL_VPP_VPE_DROP_ENABLE) for any
    command — proving 5V-only operation. Cover each protocol ID in the family via RUN_TEST variants.
    Do NOT modify production firmware sources; tests only observe via the recording buffer.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_val_eprom*" && pio test -e native -f "*test_val_flash_intel*" && pio test -e native -f "*test_val_eeprom28c*" && pio test -e native -f "*test_val_flash3*" && pio test -e native -f "*test_val_flash4*"</automated>
  </verify>
  <acceptance_criteria>
    - All five suites are GREEN under `pio test -e native` (added to test_filter in Task 3 first, or run with the suite present and filter applied).
    - test_val_eprom asserts a VPP-enable CTL bit fires on CMD_WRITE AND a negative-control CMD_READ test asserts that bit never fires.
    - test_val_flash_intel asserts CTRL_VPP_P1_ENABLE fires on write with mock 12V present, and read does not.
    - test_val_eeprom28c / test_val_flash3 / test_val_flash4 each assert NO VPP-enable CTL bit is ever recorded (5V-only proof).
    - Each suite's host_stubs.cpp `#define HOST_STUBS_RECORD_BUS` precedes the shared include (no undefined-reference linker error).
    - The assertion VPP-bit constant for each family matches the bit the real handler source actually sets (verified by reading the handler .cpp, not guessed).
  </acceptance_criteria>
  <done>Five Tier-1 family suites prove VPP/non-VPP behavior by recorded side-effect, with in-tier negative controls for the VPP families.</done>
</task>

<task type="auto">
  <name>Task 2: Create the SRAM suite asserting the documented no-op (zero recorded writes)</name>
  <files>firestarter/test/native/avr/test_val_sram/test_val_sram.cpp, firestarter/test/native/avr/test_val_sram/host_stubs.cpp</files>
  <read_first>
    - firestarter/src/proms/sram.cpp (lines 15-17 — confirm configure_sram is the LOG_DEBUG_ID_SUB one-liner with NO function-pointer wiring)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"SRAM-specific override" (assert bus_recording_count() == 0 with the Phase-74-will-change comment)
    - .planning/phases/71-validation-harness-matrix/71-RESEARCH.md §"Pitfall 2" + §"Open Questions Q1" (SRAM no-op records zero writes; assert current state, NOT desired Phase-74 state)
    - firestarter/test/native/avr/test_val_eprom/host_stubs.cpp (the recording opt-IN include pattern from Task 1 to mirror)
  </read_first>
  <action>
    Create test_val_sram (0x0E/0x27/0x28/0x29). host_stubs.cpp uses the same `#define
    HOST_STUBS_RECORD_BUS` opt-IN include. The test asserts that after `configure_memory` runs for an
    SRAM protocol, `bus_recording_count() == 0` (the handler issues no register writes) AND
    `h.response_code != RESPONSE_CODE_ERROR` — documenting the CURRENT no-op state. Add a clear code
    comment that this GREEN test pins the no-op baseline and that Phase 74 FIX-01 will flip it to
    RED→GREEN once VAL-06 (Phase 73) resolves whether SRAM should write. Do NOT write an assertion
    requiring SRAM to perform writes (Pitfall 2 — that would be RED and is out of scope here). Cover
    each SRAM protocol ID via RUN_TEST variants.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_val_sram*"</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "*test_val_sram*"` exits 0.
    - The suite asserts `bus_recording_count() == 0` after configure_memory for an SRAM protocol (no-op documented), not a non-zero write count.
    - A comment marks this as the Phase-71 baseline that Phase 74 FIX-01 will revisit (VAL-06 deferred to Phase 73).
    - host_stubs.cpp defines HOST_STUBS_RECORD_BUS before the shared include.
  </acceptance_criteria>
  <done>The SRAM Tier-1 suite GREEN-asserts the current no-op state with a deferral comment, per D-07 / Pitfall 2.</done>
</task>

<task type="auto">
  <name>Task 3: Register all 6 suites in platformio.ini and prove the full native battery is green</name>
  <files>firestarter/platformio.ini</files>
  <read_first>
    - firestarter/platformio.ini lines 80-101 (the positive test_filter allowlist + build_flags -I include list — the exact lines to extend)
    - .planning/phases/71-validation-harness-matrix/71-PATTERNS.md §"platformio.ini (MODIFIED)" (exact entries to add)
    - .planning/phases/71-validation-harness-matrix/71-RESEARCH.md §"Pitfall 3" (positive allowlist — a suite not listed silently does not run)
  </read_first>
  <action>
    Add all 6 new suite directories to the `[env:native]` `test_filter` allowlist
    (native/avr/test_val_eprom, test_val_eeprom28c, test_val_flash3, test_val_flash4,
    test_val_flash_intel, test_val_sram) AND add the matching `-I test/native/avr/test_val_<family>`
    entries to `build_flags`. This is the load-bearing step — PIO uses a positive allowlist
    (Pitfall 3), so an unlisted suite silently does not run. Then run the FULL native battery to
    prove (a) all 6 new suites run and pass, (b) every pre-existing suite still passes (the recording
    stub flag-off no-op holds), and (c) production flash is unchanged.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c "test_val_" platformio.ini | awk '$1>=12{exit 0} {exit 1}' && pio test -e native && pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - All 6 `native/avr/test_val_*` entries are present in `test_filter` AND 6 matching `-I` entries in `build_flags` (≥12 `test_val_` occurrences total in platformio.ini).
    - `pio test -e native` (full battery, no filter) exits 0 — all new + all pre-existing suites pass.
    - `pio run -e uno` and `pio run -e leonardo` succeed with production flash byte-count unchanged vs the pre-plan baseline (delta == 0).
  </acceptance_criteria>
  <done>All 6 Tier-1 suites are wired into the allowlist and run green alongside the existing battery; zero production flash added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native test build → production build | All suites live under test/; production flash must be unchanged |
| handler dispatch → recorded CTL sequence | Test observes VPP-enable bits via the recording buffer — the side-effect oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-VACUOUS | Spoofing | a Tier-1 "PASS" that cannot actually fail | mitigate | VPP families include a negative-control read test asserting the VPP bit is NOT set; non-VPP families assert the bit is NEVER set — both can go RED if a handler regresses (D-08 verify-can-fail) |
| T-71-WIRED-WRONG | Tampering | a handler wired but electrically wrong (enables VPP it must not) | mitigate | Side-effect recording asserts the exact CTL bit sequence, not just op-pointer presence; non-VPP families fail if any VPP-enable bit appears |
| T-71-SRAM-FALSE | Repudiation | asserting SRAM writes when it is a no-op (false RED) | mitigate | SRAM suite asserts bus_recording_count()==0 documenting current no-op; VAL-06 resolution explicitly deferred to Phase 73 (Pitfall 2) |
| T-71-FLASH | Tampering | production flash inflation from test files | mitigate | Files under test/ (src_filter-excluded); Task 3 builds -e uno/-e leonardo and asserts byte-count delta == 0 |
| T-71-SC | Tampering | npm/pip/cargo installs | accept | No package installs; reuses PlatformIO/Unity/ArduinoFake (D-10). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `pio test -e native` (full) exits 0: 6 new suites + all pre-existing suites pass.
- platformio.ini lists all 6 test_val_* in both test_filter and build_flags.
- `pio run -e uno && pio run -e leonardo` succeed with unchanged flash byte-count.
</verification>

<success_criteria>
Six Tier-1 native suites prove each family's VPP/non-VPP behavior by recorded register-write
side-effect, with in-tier negative controls for VPP families and a documented zero-write no-op
for SRAM; all 6 are in the positive test_filter allowlist and pass under `pio test -e native`;
zero production flash added (HARN-01 Tier-1, D-07).
</success_criteria>

<output>
Create `.planning/phases/71-validation-harness-matrix/71-04-SUMMARY.md` when done
</output>
