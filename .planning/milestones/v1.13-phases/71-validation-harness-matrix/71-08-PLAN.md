---
phase: 71-validation-harness-matrix
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/validation_matrix_spec.json
  - firestarter/test/native/avr/_shared/validation_matrix.h
  - firestarter_app/tests/test_gen_validation_header.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [HARN-04]
user_setup: []

must_haves:
  truths:
    - "validation_matrix_spec.json flash4 protocols == [5], aligning the declared matrix with check_dispatch.py's KNOWN_PROTOCOLS / dispatch() reality"
    - "The committed validation_matrix.h is byte-identical to a fresh gen_validation_header.py run after the spec trim (drift gate green)"
    - "REQUIREMENTS.md HARN-04 is checked [x] and its coverage-table row reads Complete; HARN-03 stays [x] / Complete"
    - "A durable rationale records WHY the host matrix lists only [5] while firmware + native cover {0x05,0x35,0x39} (so CR-02 is not re-discovered as a regression)"
  artifacts:
    - path: "firestarter_app/tools/validation_matrix_spec.json"
      provides: "flash4 protocols trimmed to [5] with embedded rationale"
      contains: "\"protocols\": [5]"
    - path: "firestarter/test/native/avr/_shared/validation_matrix.h"
      provides: "Regenerated 11-row header (flash4 0x35/0x39 rows removed)"
      contains: "VAL_FAMILY_COUNT 11"
    - path: "firestarter_app/tests/test_gen_validation_header.py"
      provides: "Drift gate row-count assertion updated 13 -> 11"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HARN-04 checkbox + coverage row marked Complete"
  key_links:
    - from: "firestarter_app/tools/validation_matrix_spec.json"
      to: "firestarter/test/native/avr/_shared/validation_matrix.h"
      via: "gen_validation_header.py codegen (byte-identical drift gate)"
      pattern: "VAL_FAMILY_COUNT"
---

<objective>
Close GAP-2 (HARN-04 / SC#4): the host dispatch mirror diverges from the authored spec. `validation_matrix_spec.json` declares flash4 `"protocols": [5, 53, 57]` (0x05/0x35/0x39) but `check_dispatch.py::dispatch()` only maps 0x05 -> configure_flash4 (0x35/0x39 fall through to "not_implemented"). Zero DB chips use 0x35/0x39 so the host never dispatches them.

Locked operator decision (FINAL — do not propose alternatives): TRIM THE SPEC to the host. Change flash4 `"protocols": [5, 53, 57]` -> `"protocols": [5]`. Do NOT modify check_dispatch.py's dispatch()/KNOWN_PROTOCOLS. Regenerate validation_matrix.h and keep the drift gate green. Check HARN-04 in REQUIREMENTS.md.

TIER-CONSISTENCY (resolved, durable): the firmware (memory.cpp:89; firmware CLAUDE.md source-of-truth) genuinely dispatches {0x05,0x35,0x39} -> configure_flash4, and the Tier-1 native suite test_val_flash4.cpp hard-codes passing 0x35/0x39 firmware tests. KEEP those native tests — they prove real firmware behavior, cost nothing, and call configure_memory directly (they do NOT iterate VAL_FAMILIES, so trimming the generated header does NOT break them — confirmed during planning). The matrix tracks host-DISPATCHABLE protocols (KNOWN_PROTOCOLS); 0x35/0x39 have zero chips in the DB so the host never dispatches them. This rationale is recorded in the spec so a future reader does not re-flag CR-02 as a regression.

Purpose: remove the cross-tier contradiction so SC#4 / HARN-04 is genuinely satisfied, without creating a new hollow claim.
Output: trimmed spec (with rationale) + regenerated 11-row header + updated drift-gate row count + REQUIREMENTS.md HARN-04 marked Complete.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/71-validation-harness-matrix/71-VERIFICATION.md
@.planning/phases/71-validation-harness-matrix/71-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim flash4 protocols to [5] with a durable rationale, regenerate the header, update the drift gate</name>
  <files>firestarter_app/tools/validation_matrix_spec.json, firestarter/test/native/avr/_shared/validation_matrix.h, firestarter_app/tests/test_gen_validation_header.py</files>
  <read_first>
    - firestarter_app/tools/validation_matrix_spec.json (the flash4 entry at lines 72-93; `"protocols": [5, 53, 57]` at line 75)
    - firestarter_app/tools/gen_validation_header.py (emit_cpp_header 101-134 — one row per sorted protocol per family; validate_spec 57-94 requires protocols be a non-empty list of ints, so [5] is valid; both functions ignore any extra family keys)
    - firestarter_app/tests/test_gen_validation_header.py (test_committed_header_has_13_rows at 56-65 hard-codes 13; test_codegen_produces_byte_identical_output at 68-108; the 6-handler parametrize at 155-171 — all 6 handler names incl. configure_flash4 must still be present after the trim)
    - firestarter/test/native/avr/_shared/validation_matrix.h (current 13-row header; flash4 rows 0x05/0x35/0x39 at lines 22-24; VAL_FAMILY_COUNT 13 at line 8)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (confirm it hard-codes make_handle(0x35,...) / make_handle(0x39,...) and calls configure_memory directly — it does NOT read VAL_FAMILIES, so the header trim does not break it; do NOT delete these native tests)
  </read_first>
  <action>
    Edit `firestarter_app/tools/validation_matrix_spec.json`: in the flash4 family entry, change `"protocols": [5, 53, 57]` to `"protocols": [5]`. Add a durable rationale as a sibling string field on the flash4 entry (JSON has no comment syntax), key `"protocols_note"`, documenting: the matrix tracks host-dispatchable protocols (check_dispatch.py KNOWN_PROTOCOLS / dispatch()); 0x35 (53) and 0x39 (57) ALSO dispatch to configure_flash4 in firmware (memory.cpp) and ARE covered by the Tier-1 native suite test_val_flash4.cpp, but have zero chips in chip_database.json so the host never dispatches them; they are intentionally omitted here to keep the host matrix consistent with the host dispatch mirror (CR-02 resolution, not a regression). Confirm by reading validate_spec and emit_cpp_header that the extra `protocols_note` key is ignored (validate_spec only requires id/handler/protocols and that protocols is a non-empty int list; emit_cpp_header reads only protocols/id/handler). Regenerate the committed header: `cd firestarter_app && python tools/gen_validation_header.py` — this rewrites `firestarter/test/native/avr/_shared/validation_matrix.h` so flash4 contributes only the 0x05 row and VAL_FAMILY_COUNT becomes 11. Update the drift gate `firestarter_app/tests/test_gen_validation_header.py`: in test_committed_header_has_13_rows rename to reflect 11, update the docstring, and change the `== 13` assertion and its failure message to `== 11` (removed one protocol x ... 13->11). Do NOT modify check_dispatch.py, dispatch(), or KNOWN_PROTOCOLS. Do NOT delete test_val_flash4.cpp's 0x35/0x39 tests. Honor the firestarter_app tooling gate (ruff check + ruff format --check on the edited test file; target py3.9/3.11).
  </action>
  <verify>
    <automated>cd firestarter_app && python -c "import json; p=[f for f in json.load(open('tools/validation_matrix_spec.json'))['families'] if f['id']=='flash4'][0]['protocols']; assert p==[5], p" && python tools/gen_validation_header.py && python -m pytest tests/test_gen_validation_header.py -q && grep -q "VAL_FAMILY_COUNT 11" ../firestarter/test/native/avr/_shared/validation_matrix.h</automated>
  </verify>
  <acceptance_criteria>
    - `validation_matrix_spec.json` flash4 `protocols` == `[5]` (the Python one-liner assertion exits 0).
    - The flash4 entry carries a `protocols_note` string documenting the firmware/native vs host-matrix distinction (CR-02 rationale durable).
    - `cd firestarter_app && python tools/gen_validation_header.py` exits 0 and the committed `firestarter/test/native/avr/_shared/validation_matrix.h` contains `#define VAL_FAMILY_COUNT 11` and no `0x35`/`0x39` flash4 rows.
    - `cd firestarter_app && python -m pytest tests/test_gen_validation_header.py` exits 0 (drift gate byte-identical; row-count test now asserts 11; all 6 handler names still present).
    - `firestarter_app/tools/check_dispatch.py` is unchanged (not in files_modified); `test_val_flash4.cpp` 0x35/0x39 tests are unchanged.
    - `cd firestarter_app && ruff check tests/test_gen_validation_header.py` exits 0 and `ruff format --check tests/test_gen_validation_header.py` exits 0.
  </acceptance_criteria>
  <done>Spec flash4 protocols trimmed to [5] with durable rationale; header regenerated to 11 rows byte-identically; drift gate updated to 11 and green; check_dispatch.py and the native firmware tests untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Mark HARN-04 Complete (and confirm HARN-03) in REQUIREMENTS.md, then re-run the HARN-04 gate</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 18: `- [ ] **HARN-04**:`; line 76 coverage row `| HARN-04 | Phase 71 | Pending |`; line 17 HARN-03 `- [x]`; line 75 `| HARN-03 | Phase 71 | Complete |`)
    - firestarter_app/tools/check_dispatch.py (the gate that must still exit 0 on the clean DB — unchanged by this plan)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`: change the HARN-04 checkbox at line 18 from `- [ ] **HARN-04**:` to `- [x] **HARN-04**:` (leave the requirement text unchanged). Change the coverage-table row at line 76 from `| HARN-04 | Phase 71 | Pending |` to `| HARN-04 | Phase 71 | Complete |`. Confirm (do not change unless drifted) that HARN-03 stays `- [x]` at line 17 and its coverage row at line 75 reads `Complete` — GAP-1 (plan 07) closes HARN-03's substance; this plan only verifies the checkbox state is consistent. Do not alter any other requirement rows. Then re-run the HARN-04 host gate to prove it still passes on the clean DB after the spec trim (the gate reads check_dispatch.py, which is unchanged): `cd firestarter_app && python tools/check_dispatch.py` must print the PASS line and exit 0.
  </action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*HARN-04\*\*" .planning/REQUIREMENTS.md && grep -q "| HARN-04 | Phase 71 | Complete |" .planning/REQUIREMENTS.md && grep -q "^- \[x\] \*\*HARN-03\*\*" .planning/REQUIREMENTS.md && cd firestarter_app && python tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line 18 reads `- [x] **HARN-04**:` and the coverage-table row reads `| HARN-04 | Phase 71 | Complete |`.
    - HARN-03 remains `- [x]` with coverage row `Complete` (unchanged).
    - `cd firestarter_app && python tools/check_dispatch.py` prints the `PASS: all ... chips scanned` line and exits 0 (the HARN-04 dispatch-invariant gate still green after the spec trim).
    - No other REQUIREMENTS.md rows changed.
  </acceptance_criteria>
  <done>HARN-04 is marked Complete in both the checkbox and the coverage table; HARN-03 confirmed Complete; the check_dispatch.py HARN-04 gate exits 0 on the clean DB.</done>
</task>

</tasks>

<verification>
- `cd firestarter_app && python -m pytest tests/test_gen_validation_header.py` exits 0 (drift gate byte-identical at 11 rows).
- `validation_matrix_spec.json` flash4 protocols == [5]; `validation_matrix.h` has `VAL_FAMILY_COUNT 11`; no 0x35/0x39 flash4 rows.
- `cd firestarter_app && python tools/check_dispatch.py` exits 0 (unchanged dispatch mirror; gate green).
- REQUIREMENTS.md HARN-04 == `[x]` / Complete; HARN-03 stays `[x]` / Complete.
- check_dispatch.py / dispatch() / KNOWN_PROTOCOLS unchanged; test_val_flash4.cpp 0x35/0x39 tests retained.
- Zero production firmware flash change (spec JSON + a test-only generated header + a test + REQUIREMENTS.md only).
</verification>

<success_criteria>
SC#4 / HARN-04 is met: the authored matrix no longer claims flash4 protocols (0x35/0x39) that the host dispatch mirror does not route, removing the cross-tier contradiction. The trim is regenerated into the committed header byte-identically (drift gate green at 11 rows), the firmware-truth native tests are retained, and the firmware-vs-host distinction is documented durably so CR-02 cannot be re-discovered as a regression. REQUIREMENTS.md HARN-04 is Complete; check_dispatch.py is unchanged and its gate still passes.
</success_criteria>

<output>
Create `.planning/phases/71-validation-harness-matrix/71-08-SUMMARY.md` when done.
</output>
