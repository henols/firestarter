---
phase: 77-erase-write-path-graduation-0x07-ee-eproms
plan: 03
type: execute
wave: 2
depends_on: ["77-01"]
files_modified: []
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "check_dispatch.py full-DB VPP-safety gate passes after the convert_to_programmer edit (744 chips, 0 violations)"
    - "FLAG_CAN_ERASE = 0x02 parity between constants.py and firestarter.h is preserved (untouched this phase)"
    - "All 8 target EE-EPROMs are support_status: supported and resolve_chip does NOT refuse them — SAFE-01 is N/A-no-refusal here (D-05)"
    - "The full host test suite passes with coverage >= 70%"
  artifacts:
    - path: ".planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-03-SUMMARY.md"
      provides: "Recorded post-edit gate evidence (check_dispatch output, parity test result, resolve_chip non-refusal, SAFE-01 N/A rationale)"
  key_links:
    - from: "convert_to_programmer edit (Plan 01)"
      to: "check_dispatch.py VPP-safety gate"
      via: "post-edit full-DB scan stays at 0 violations"
      pattern: "0 violations|0 non_supported_dispatchable"
---

<objective>
Satisfy the SAFE-01/02/03 graduation discipline as software gates AFTER the canonical
`FLAG_CAN_ERASE` edit (Plan 01) has landed. This is gate-verification-only — NO code edits.
Record the post-edit evidence: `check_dispatch.py` stays green (SAFE-02), FLAG_CAN_ERASE constant
parity holds (SAFE-03), and the 8 target chips remain `supported` with no `resolve_chip` refusal to
remove (SAFE-01 = N/A-no-refusal per D-05).

Purpose: SAFE-02 (full-DB VPP-safety gate green post-edit), SAFE-03 (firmware↔host FLAG_CAN_ERASE
parity preserved — this phase touches neither constant, so parity is trivially held), SAFE-01 (D-05:
explicitly document there is NO host-guard refusal to drop for these already-`supported` chips, so
downstream agents do not fabricate a refusal-removal task). These gates run AFTER the edit, never
before (RESEARCH Pitfall 6).

Output: 77-03-SUMMARY.md recording all three gate artifacts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md
@firestarter_app/tools/check_dispatch.py
@firestarter_app/firestarter/constants.py
@firestarter_app/firestarter/chip_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (SAFE-02): Run check_dispatch.py full-DB VPP-safety gate AFTER the edit</name>
  <files>(no files modified — gate run + evidence capture)</files>
  <read_first>
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (SAFE-02 section: pre-edit baseline = PASS 744 chips / 730 supported / 14 non-dispatchable / 0 violations; Pitfall 6: capture AFTER the edit, not before)
    - firestarter_app/tools/check_dispatch.py (the gate; understand its PASS line format)
  </read_first>
  <action>
    Confirm Plan 01's `convert_to_programmer` edit is present on disk (grep `electrical-type` in the
    flag block of `database.py`). Then run `python3 tools/check_dispatch.py` from `firestarter_app/`.
    The expected output is the unchanged baseline: 744 chips scanned, 730 supported, 14 confirmed
    non-dispatchable, 0 non_supported_dispatchable, 0 dispatch regressions, 0 consistency violations.
    This phase edits only the `FLAG_CAN_ERASE` flag derivation (not `algorithm`, `vpp_mv`,
    `support_status`, `electrical.type` storage, or `_FAMILY_VPP_INVARIANTS`), so the gate must remain
    green. Paste the verbatim PASS line into the SUMMARY as the SAFE-02 artifact (this is the
    post-edit capture, satisfying Pitfall 6).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -q 'electrical-type' firestarter/database.py && python3 tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `database.py` contains the `electrical-type` derivation (Plan 01 landed)
    - `python3 tools/check_dispatch.py` exits 0 and prints `0 non_supported_dispatchable; 0 dispatch regressions; 0 consistency violations`
    - The verbatim PASS line (744 chips) is recorded in 77-03-SUMMARY.md
  </acceptance_criteria>
  <done>check_dispatch.py PASS captured post-edit (744 chips, 0 violations); recorded as SAFE-02 artifact.</done>
</task>

<task type="auto">
  <name>Task 2 (SAFE-03 + SAFE-01): Confirm FLAG_CAN_ERASE parity and document SAFE-01 N/A-no-refusal</name>
  <files>(no files modified — gate run + evidence capture)</files>
  <read_first>
    - firestarter_app/firestarter/constants.py line ~80 (`FLAG_CAN_ERASE = 0x02`)
    - firestarter/include/firestarter.h line ~60 (`#define FLAG_CAN_ERASE 0x02`)
    - firestarter_app/tests/test_revision_constants_parity.py (`test_flag_values_match_firmware` — the parity test)
    - firestarter_app/firestarter/chip_resolver.py (`resolve_chip` — confirm no refusal for the 8 chips)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (SAFE-01: all 8 chips RESOLVED, no refusal — D-05 N/A-no-refusal; SAFE-03: parity preserved, neither constant touched)
  </read_first>
  <action>
    SAFE-03: Run `pytest tests/test_revision_constants_parity.py -v` and confirm
    `test_flag_values_match_firmware` is green — `FLAG_CAN_ERASE = 0x02` in `constants.py` and
    `0x02` in `firestarter.h` match. This phase does NOT edit either constant (Plan 01 reads the flag,
    does not redefine it), so parity is trivially preserved. Do NOT plan or perform any constants edit.
    SAFE-01 (D-05): Run `pytest tests/test_chip_resolver.py -v` to confirm `resolve_chip` does not
    refuse the 8 target EE-EPROMs (all `support_status: supported`). In the SUMMARY, document
    explicitly that SAFE-01 is N/A-no-refusal for Phase 77 — there is NO `chip_resolver.resolve_chip`
    host-guard refusal to remove (unlike Phases 78-80). The evidence-gated "FINAL step" the SAFE
    discipline protects in this phase is the `FLAG_CAN_ERASE` wiring itself (Plan 01), which lands
    before the bench proof (Plan 04). State this so a downstream verifier does not hunt for a
    nonexistent refusal-removal.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_revision_constants_parity.py tests/test_chip_resolver.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_revision_constants_parity.py::test_flag_values_match_firmware` exits 0
    - `pytest tests/test_chip_resolver.py` exits 0 (no refusal for the 8 supported EE-EPROMs)
    - 77-03-SUMMARY.md states SAFE-01 = N/A-no-refusal (D-05) with the rationale that the chips are already `supported`
    - 77-03-SUMMARY.md confirms FLAG_CAN_ERASE = 0x02 parity (constants.py == firestarter.h), no constant edited
  </acceptance_criteria>
  <done>Parity test green (FLAG_CAN_ERASE 0x02 both sides); resolve_chip non-refusal confirmed; SAFE-01 N/A-no-refusal documented per D-05.</done>
</task>

<task type="auto">
  <name>Task 3: Full host suite + coverage gate post-edit</name>
  <files>(no files modified — gate run)</files>
  <read_first>
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (Validation Architecture: full suite `pytest --cov --cov-fail-under=70`; py3.12 masks CI py3.9/3.11 — validate ruff against target)
    - firestarter_app/CLAUDE.md (tooling gate: ruff + ruff format + mypy strict-8 + pytest cov>=70; database.py NOT in strict-8)
  </read_first>
  <action>
    Run the full host gate from `firestarter_app/`: `ruff check . && ruff format --check .`,
    then `python3 -m pytest --cov --cov-fail-under=70`. Confirm the suite is green (including the
    3 Plan-01 tests and the Plan-02 D-07 test) and coverage stays at or above 70%. Note: `database.py`
    is NOT in the strict-mypy-8 module list, so the edit site is not subject to strict mypy overrides;
    a plain `mypy firestarter/database.py` is informational only. Record the pass/coverage figure in
    the SUMMARY as the wave-merge gate evidence.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check . && ruff format --check . && python3 -m pytest --cov --cov-fail-under=70</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check .` and `ruff format --check .` both exit 0
    - `python3 -m pytest --cov --cov-fail-under=70` exits 0 (suite green, coverage >= 70%)
    - Coverage figure recorded in 77-03-SUMMARY.md
  </acceptance_criteria>
  <done>Full suite green; coverage >= 70%; ruff clean across the package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host DB → wire dispatch | `check_dispatch.py` simulates the full-DB dispatch and asserts no chip routes VPP above its family invariant |
| host constants ↔ firmware constants | `FLAG_CAN_ERASE` value must match across `constants.py` and `firestarter.h` or the wire flag means different things on each side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-VPP | Elevation of Privilege / Damage | full-DB VPP routing | mitigate | `check_dispatch.py` re-run AFTER the edit stays at 0 violations — confirms the flag-derivation change routes no chip's VPP above its family invariant |
| T-77-SCOPE | Tampering | FLAG_CAN_ERASE constant value | mitigate | Parity test confirms `0x02` on both sides; this phase touches neither constant, so the wire flag's meaning is unchanged firmware-side |
</threat_model>

<verification>
- `check_dispatch.py` PASS post-edit (744 chips, 0 violations) — SAFE-02
- `test_revision_constants_parity.py::test_flag_values_match_firmware` green — SAFE-03
- `test_chip_resolver.py` green; SAFE-01 documented N/A-no-refusal (D-05)
- Full suite + cov>=70 green; ruff clean
</verification>

<success_criteria>
- SAFE-02: check_dispatch.py full-DB VPP gate green post-edit, output recorded
- SAFE-03: FLAG_CAN_ERASE 0x02 parity preserved, parity test green, no constant edited
- SAFE-01: N/A-no-refusal documented (D-05) — chips already supported, no refusal removed
- Full host suite green, coverage >= 70%
</success_criteria>

<output>
Create `.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-03-SUMMARY.md` when done.
</output>
