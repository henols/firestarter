---
phase: 77-erase-write-path-graduation-0x07-ee-eproms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/database.py
  - firestarter_app/tests/test_database_conversion.py
autonomous: true
requirements: [ERASE-01]
must_haves:
  truths:
    - "convert_to_programmer derives FLAG_CAN_ERASE from electrical-type (canonical), not the synthetic info-flags round-trip"
    - "All 8 0x07 EE-EPROMs (W27C512 etc.) carry FLAG_CAN_ERASE in wire JSON"
    - "Genuine UV-EPROMs (M27C512) do NOT carry FLAG_CAN_ERASE"
    - "Flash/EEPROM parts (AT28C256, 0x0D) carry FLAG_CAN_ERASE (inert on firmware 0x0D path per D-03)"
  artifacts:
    - path: "firestarter_app/firestarter/database.py"
      provides: "convert_to_programmer reading electrical-type for FLAG_CAN_ERASE (D-01/D-02)"
      contains: "electrical-type"
    - path: "firestarter_app/tests/test_database_conversion.py"
      provides: "3 wire-level FLAG_CAN_ERASE tests (EEPROM set / UV-EPROM clear / Flash-EEPROM set)"
      contains: "FLAG_CAN_ERASE"
  key_links:
    - from: "firestarter_app/firestarter/database.py convert_to_programmer"
      to: "wire JSON flags field"
      via: "electrical-type membership in ('EEPROM','Flash/EEPROM')"
      pattern: "electrical-type.*EEPROM"
---

<objective>
Make the `FLAG_CAN_ERASE` wire-flag derivation in `convert_to_programmer` explicit and
canonical — read `electrical-type` directly instead of the fragile synthetic `info-flags & 0x10`
round-trip injected by `_map_data`. Lock the canonical derivation with three wire-level unit tests
(written first, RED, per TDD) so the EEPROM-set / UV-EPROM-clear / Flash-EEPROM-set behavior cannot
silently regress under any future `_map_data` refactor.

Purpose: Implements ERASE-01 per D-01 (canonical erase-capability ground truth = infoic.xml
`flags & 0x10` → `electrical.type`) and D-02 (set for `electrical.type in {"EEPROM","Flash/EEPROM"}`).
Per RESEARCH RF-01, the flag is ALREADY set on the wire today for all 8 target chips via the
synthetic indirection — so this is a zero-behavioral-delta canonicality improvement, and the tests
are the thing that LOCKS the derivation, not a gap fill.

Output: Edited `convert_to_programmer` + 3 new tests in `test_database_conversion.py`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-PATTERNS.md
@firestarter_app/firestarter/database.py
@firestarter_app/tests/test_database_conversion.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0, RED): Add 3 wire-level FLAG_CAN_ERASE tests to test_database_conversion.py</name>
  <files>firestarter_app/tests/test_database_conversion.py</files>
  <read_first>
    - firestarter_app/tests/test_database_conversion.py (existing convert_to_programmer tests + module `db` fixture lines 10-13; existing AT28C256 0x0D test)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-PATTERNS.md (the "add 3 tests" section — exact test shapes)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (Pitfall 2 + Pitfall 3: assert wire output, include UV-EPROM negative control)
  </read_first>
  <behavior>
    - Test 1 `test_convert_w27c512_flag_can_erase`: `convert_to_programmer(get_eprom("W27C512"))["flags"] & FLAG_CAN_ERASE` is truthy (EEPROM/0x07 → flag set).
    - Test 2 `test_convert_uv_eprom_no_flag_can_erase`: `convert_to_programmer(get_eprom("M27C512"))["flags"] & FLAG_CAN_ERASE == 0` (UV-EPROM negative control → flag clear).
    - Test 3 `test_convert_at28c256_flash_eeprom_flag_can_erase`: `convert_to_programmer(get_eprom("AT28C256"))["flags"] & FLAG_CAN_ERASE` is truthy (Flash/EEPROM/0x0D → flag set; inert on firmware path per D-03).
  </behavior>
  <action>
    Add import `from firestarter.constants import FLAG_CAN_ERASE` alongside the existing
    `from firestarter.database import EpromDatabase`. Append the three named tests above, each
    reusing the existing module-scoped `db` fixture (do NOT re-declare it), placed before the
    `# --- Additional EpromDatabase surface` section comment so they group with the other
    `convert_to_programmer` tests. Each test follows the established shape: `full = db.get_eprom(NAME)`,
    `assert full is not None`, `out = db.convert_to_programmer(full)`, then the flag assertion.
    Assert on the wire output `out["flags"]` — NOT on `info-flags` (that path is already covered by
    TestErasableFlag in test_eprom_database.py; do not duplicate it). These tests are written FIRST
    and will FAIL or PASS depending on the current synthetic path — record the RED/observed state in
    the SUMMARY before Task 2 edits the source.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_database_conversion.py -k "flag_can_erase" -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_database_conversion.py` imports `FLAG_CAN_ERASE` from `firestarter.constants`
    - Three new test functions named exactly `test_convert_w27c512_flag_can_erase`, `test_convert_uv_eprom_no_flag_can_erase`, `test_convert_at28c256_flash_eeprom_flag_can_erase` exist
    - `pytest tests/test_database_conversion.py -k flag_can_erase` collects exactly 3 tests
  </acceptance_criteria>
  <done>3 wire-level FLAG_CAN_ERASE tests added and collected; pre-edit observed state recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1, GREEN): Switch convert_to_programmer to canonical electrical-type derivation</name>
  <files>firestarter_app/firestarter/database.py</files>
  <read_first>
    - firestarter_app/firestarter/database.py lines 592-600 (the edit site — current `info-flags & 0x10` check) and `_map_data` line ~434 (the analog that already keys off `electrical.type in ("EEPROM","Flash/EEPROM")`)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-PATTERNS.md (exact replacement block + key constraints: key is hyphenated `electrical-type`, FLAG_CAN_ERASE already imported)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (RF-01 zero-delta confirmation; D-03 inert-on-0x0D firmware confirmation)
  </read_first>
  <action>
    In `convert_to_programmer` (lines 592-600), replace the `simple_flags` derivation so the
    condition reads `full_eprom_data.get("electrical-type", "") in ("EEPROM", "Flash/EEPROM")`
    instead of `full_eprom_data.get("info-flags", 0) & 0x00000010`. Keep `simple_flags |= FLAG_CAN_ERASE`
    and `programmer_data["flags"] = simple_flags` unchanged. The key is the hyphenated `electrical-type`
    (carried through by `_map_data`), NOT `type`. `FLAG_CAN_ERASE` is already imported — no import change.
    Replace the stale comment block with a comment recording the canonical derivation AND the D-03
    firmware confirmation: that `configure_eeprom28c` (the 0x0D path) never calls `is_flag_set(FLAG_CAN_ERASE)`
    (confirmed by reading `firestarter/src/proms/eeprom_28c.cpp` — uses only FLAG_FORCE/FLAG_SKIP_BLANK_CHECK),
    so setting the flag on Flash/EEPROM parts is inert and safe (this comment is the D-03 in-code assertion;
    no firmware edit). Cite D-01/D-02. Behavioral delta is zero for all chips per RF-01. Do NOT add fenced
    code; this is a single-condition swap. Default `""` (A1) degrades safely: a missing key yields flag-clear,
    identical to the old path.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_database_conversion.py -v && ruff check firestarter/database.py && ruff format --check firestarter/database.py</automated>
  </verify>
  <acceptance_criteria>
    - `database.py convert_to_programmer` reads `electrical-type` (grep `electrical-type` in the convert_to_programmer flag block returns a hit; `info-flags & 0x00000010` no longer present in that block)
    - In-code comment records D-03 firmware-inert confirmation for the 0x0D `configure_eeprom28c` path
    - `pytest tests/test_database_conversion.py::test_convert_w27c512_flag_can_erase` exits 0
    - `pytest tests/test_database_conversion.py::test_convert_uv_eprom_no_flag_can_erase` exits 0
    - `pytest tests/test_database_conversion.py::test_convert_at28c256_flash_eeprom_flag_can_erase` exits 0
    - `ruff check firestarter/database.py` and `ruff format --check firestarter/database.py` both exit 0
  </acceptance_criteria>
  <done>convert_to_programmer derives FLAG_CAN_ERASE from canonical electrical-type; all 3 unit tests green; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host DB → wire JSON | `convert_to_programmer` assembles the `flags` field sent to firmware; an incorrect flag here changes firmware erase behavior |
| host → firmware (serial) | the `flags` field crosses into firmware `eprom_write_init`; FLAG_CAN_ERASE there triggers the high-voltage erase rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-SCOPE | Tampering | `convert_to_programmer` flag derivation | mitigate | Derivation keyed to canonical `electrical-type` mask (`{"EEPROM","Flash/EEPROM"}`); unit tests lock EEPROM-set + UV-EPROM-clear so the flag cannot bleed to a non-erasable family that would trigger an unexpected erase rail |
| T-77-VPP | Information Disclosure / Damage | flag reaching firmware 0x0D path | accept | Flag is firmware-confirmed inert on the 0x0D `configure_eeprom28c` path (D-03: never reads FLAG_CAN_ERASE); setting it on Flash/EEPROM parts routes no extra voltage. No code change required; documented in-code |
</threat_model>

<verification>
- `pytest tests/test_database_conversion.py -v` — all tests including the 3 new ones green
- `ruff check firestarter/database.py && ruff format --check firestarter/database.py` clean (validate against py3.9/3.11 semantics; py3.12 masks CI)
- No change to `algorithm`, `vpp_mv`, `support_status`, or `electrical.type` storage — wire output is byte-identical to pre-edit for all chips (RF-01 zero-delta)
</verification>

<success_criteria>
- `convert_to_programmer` sets FLAG_CAN_ERASE from `electrical-type ∈ {"EEPROM","Flash/EEPROM"}` (SC#1)
- W27C512 + 7 sibling 0x07 EE-EPROMs carry the flag; M27C512 (UV-EPROM) does not (SC#1)
- D-03 firmware-inert confirmation recorded as an in-code comment
- 3 new unit tests green; ruff clean
</success_criteria>

<output>
Create `.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-01-SUMMARY.md` when done.
</output>
