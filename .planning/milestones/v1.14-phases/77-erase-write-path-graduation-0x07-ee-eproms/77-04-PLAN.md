---
phase: 77-erase-write-path-graduation-0x07-ee-eproms
plan: 04
type: execute
wave: 3
depends_on: ["77-01", "77-02", "77-03"]
files_modified: []
autonomous: false
requirements: [ERASE-02]
must_haves:
  truths:
    - "On Leonardo with a real non-blank W27C512, a single default `firestarter write` (no `-b`) drives write→auto-erase→program→verify to clean completion"
    - "An independent post-write full read SHA-matches the source file (non-vacuous)"
    - "A wrong-file verify exits non-zero (negative control proves the verify is real)"
    - "The 14V erase-rail VPP is measured chip-OUT under the 22V ceiling and the value is recorded (SC#3)"
  artifacts:
    - path: ".planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-04-SUMMARY.md"
      provides: "Recorded bench evidence: shield rev, controller port identity, r1 readback, measured VPP, write log, SHA match, negative-control exit code"
  key_links:
    - from: "host FLAG_CAN_ERASE in wire JSON (Plan 01)"
      to: "firmware eprom_internal_erase on Leonardo"
      via: "default write of non-blank W27C512 triggers auto-erase before program"
      pattern: "write.*W27C512"
---

<objective>
Bench-prove the full write→auto-erase→program→verify cycle on Leonardo with a real non-blank
W27C512 (ERASE-02 / SC#2 / SC#3). This is the milestone's FIRST hardware graduation and the
evidence-gated FINAL step the SAFE discipline protects — it runs AFTER the canonical wiring (Plan 01)
and all software gates (Plan 03) are on record. Per D-04 a SINGLE cycle is sufficient here (N>=5 NOT
required). Includes the mandatory chip-OUT 14V erase-rail VPP multimeter dry-run, an independent
post-write SHA-match read, and a non-vacuous wrong-file negative control.

Purpose: ERASE-02 — confirm the auto-erase default-write path works end-to-end on real silicon, and
close SC#2 + SC#3. This is the graduation evidence that justifies the Plan-01 wiring.

Output: 77-04-SUMMARY.md with the full bench record.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md
@.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Standing bench precondition — shield rev, port identity, r1 reconcile</name>
  <files>(no source files — hardware bench precondition; state captured in 77-04-SUMMARY.md)</files>
  <read_first>
    - .planning/ROADMAP.md §v1.14 "Standing bench precondition"
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md (canonical_refs: standing bench precondition)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (Environment Availability: Leonardo only; uno328pb N/A; shield rev must be ASKed)
  </read_first>
  <action>Establish the trustworthy bench state before any high-voltage step. Confirm the mounted board is the Leonardo (the only trustworthy write/verify board; uno328pb is N/A for program/write; Leonardo is exempt from chip-OUT-before-sideload). ASK the operator which silkscreen shield rev (Rev 2.2 / Rev 2.0 / Modified Rev 0 — the EEPROM hw_revision byte cannot distinguish them) is mounted and record it. Run `firestarter --port <port> version` to verify controller port identity and record the /dev/ttyACM* port (port numbers shuffle across replug). Reconcile live R1/R2 (`r1 ≈ 270000`) and record the readback (a wrong r1 makes the later VPP measurement meaningless). See how-to-verify for exact steps; approval gates Task 2.</action>
  <what-built>
    Plan 01 wired the canonical FLAG_CAN_ERASE derivation; the firmware `eprom_write_init` guard
    already honors it. This step establishes the trustworthy bench state before any high-voltage step.
  </what-built>
  <how-to-verify>
    Operator-driven (Claude can drive serial via USB passthrough but must ASK and confirm physical state):
    1. Confirm the board mounted is the **Leonardo** (the ONLY trustworthy write/verify board — v1.9 read bug; uno328pb is N/A for program/write). Leonardo is exempt from the chip-OUT-before-sideload rule.
    2. ASK the operator which **silkscreen shield revision** is mounted (Rev 2.2 / Rev 2.0 / Modified Rev 0 — the EEPROM hw_revision byte cannot distinguish them). Record the answer.
    3. Verify `controller:` port identity for the Leonardo (run `firestarter --port <port> version`); record the resolved /dev/ttyACM* port. Port numbers shuffle across replug — do not trust a stale mapping.
    4. Reconcile live R1/R2: confirm `r1 ≈ 270000` (the VPP-calibration value); record the readback.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms board=Leonardo, states the silkscreen shield rev, gives the resolved port, and the `r1 ≈ 270000` readback is recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - Mounted board confirmed = Leonardo; port identity recorded
    - Silkscreen shield rev recorded (operator-confirmed)
    - `r1 ≈ 270000` readback recorded
  </acceptance_criteria>
  <done>Board=Leonardo confirmed; shield rev + port identity + r1≈270000 recorded in the SUMMARY.</done>
  <resume-signal>Operator confirms board=Leonardo, states shield rev, and the r1 readback is recorded — type "preconditions confirmed".</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 (SC#3): Chip-OUT 14V erase-rail VPP multimeter dry-run</name>
  <files>(no source files — hardware VPP-safety checkpoint; measurement captured in 77-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (eprom_internal_erase: VPP regulator without drop resistor → ~14V VPE rail; 14V well under RURP_VPP_CEILING_MV=22000; Pitfall 4: must record the measurement)
    - .planning/ROADMAP.md §SC#3 (14V erase-rail setpoint confirmed under ceiling, chip-OUT dry-run first)
  </read_first>
  <action>DAMAGE-SAFETY gate (T-77-VPP) before seating the chip. With the W27C512 OUT of the ZIF socket, raise the erase rail via the CLI (`firestarter --port <port> vpp W27C512` or the equivalent erase-rail command) and measure the socket VPP pin with a multimeter. Record the measured VPP (expect ~14V on a well-calibrated Rev 2.2 shield; may differ on Rev 2.0 / Modified Rev 0). The measured value MUST be below the 22V ceiling (RURP_VPP_CEILING_MV). If it exceeds the ceiling or is wildly off ~14V, STOP — do not seat the chip; flag a shield/calibration issue. See how-to-verify for exact steps; approval gates Task 3.</action>
  <what-built>
    The firmware erase path (`eprom_internal_erase`) routes the unregulated VPE/VPP boost rail to A9+VPE,
    producing the ~14V the W27C512 datasheet specifies for erase — higher than the 12V program VPP.
  </what-built>
  <how-to-verify>
    Chip MUST be out of the socket:
    1. Remove the W27C512 from the ZIF socket (chip-OUT). Confirm socket is empty.
    2. Raise the erase rail via the CLI and measure the socket VPP pin with a multimeter.
    3. Record the measured VPP. Expect ~14V; MUST be below the 22V ceiling (RURP_VPP_CEILING_MV).
    4. If the measured VPP is above the ceiling or wildly off the ~14V expectation, STOP — do not seat the chip; flag a shield/calibration issue.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the W27C512 was OUT of the socket and records the measured erase-rail VPP, which is below the 22V ceiling.</human-check>
  </verify>
  <acceptance_criteria>
    - W27C512 confirmed OUT of socket during measurement
    - Measured erase-rail VPP recorded in the SUMMARY
    - Measured VPP < 22V ceiling (SC#3 confirmed)
  </acceptance_criteria>
  <done>Chip-OUT 14V erase-rail VPP measured under the 22V ceiling and recorded (SC#3); safe to seat the chip.</done>
  <resume-signal>Operator records the measured VPP under the ceiling with chip OUT — type "VPP measured <value>" or report the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3 (SC#2): Seated write→auto-erase→program→verify + SHA match + negative control</name>
  <files>(no source files — hardware bench cycle; logs/digests captured in 77-04-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (firestarter CLI bench sequence: write no-`-b`, read+sha256sum, wrong-file verify; D-04 single cycle; D-07 no-`-b` clean-completion live proof)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-CONTEXT.md (D-04 bench acceptance rigor)
  </read_first>
  <action>Re-seat the W27C512 starting NON-BLANK (so the auto-erase is actually exercised, not a no-op on an already-blank part). On the Leonardo at the recorded port: (1) Default write with NO `-b` flag: `firestarter --port <port> write W27C512 <source.bin>` — the cycle must run write→auto-erase→program→verify to clean completion with NO 0xA4 / empty-input error (live D-07 proof the no-`-b` path completes clean); capture the log. (2) Independent post-write read: `firestarter --port <port> read W27C512 <readback.bin>`, then `sha256sum <source.bin> <readback.bin>` — digests MUST match. (3) Non-vacuous NEGATIVE control: `firestarter --port <port> verify W27C512 <wrong.bin>` against a different file — MUST exit non-zero. Record the write log tail, both SHA digests (matching), and the negative-control exit code.</action>
  <what-built>
    A default `firestarter write` (no `-b`) of a non-blank W27C512 now auto-erases before programming
    because FLAG_CAN_ERASE is carried on the wire (Plan 01) and the firmware guard honors it. This step
    proves the full cycle on real silicon and doubles as the live no-`-b` clean-completion proof for D-07.
  </what-built>
  <how-to-verify>
    Re-seat the W27C512 (start NON-BLANK), then on the Leonardo at the recorded port:
    1. Default write (NO `-b` flag): `firestarter --port <port> write W27C512 <source.bin>`. Must run write→auto-erase→program→verify to clean completion with NO 0xA4 / empty-input error. Capture the log.
    2. Independent read: `firestarter --port <port> read W27C512 <readback.bin>`, then `sha256sum <source.bin> <readback.bin>` — digests MUST match.
    3. NEGATIVE control: `firestarter --port <port> verify W27C512 <wrong.bin>` — MUST exit non-zero.
    Record: the write log tail, both SHA digests (matching), and the negative-control exit code (non-zero).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the no-`-b` write completed clean (no 0xA4), `sha256sum source.bin readback.bin` matched, and the wrong-file verify exited non-zero — all recorded in the SUMMARY.</human-check>
  </verify>
  <acceptance_criteria>
    - Default no-`-b` write of a non-blank W27C512 completes clean (no 0xA4 / empty-input) — live D-07 proof
    - `sha256sum source.bin readback.bin` shows identical digests (SC#2 SHA match)
    - Wrong-file `verify` exits non-zero (non-vacuous negative control)
    - Write log tail, both digests, and the negative-control exit code recorded in 77-04-SUMMARY.md
  </acceptance_criteria>
  <done>Full write→auto-erase→program→verify cycle proven clean on Leonardo; SHA match + non-vacuous negative control recorded (SC#2, ERASE-02).</done>
  <resume-signal>Operator confirms: clean write, SHA match, and wrong-file verify exited non-zero — type "bench cycle proven" or report the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host write command → firmware high-voltage erase rail | the FLAG_CAN_ERASE flag triggers `eprom_internal_erase`, routing the ~14V unregulated VPE/VPP rail to the socket; a wrong rail or chip-present-during-dry-run risks chip/board damage |
| post-write read → verify oracle | the verify board must be trustworthy; Leonardo is the only board not corrupted by the v1.9 read bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-VPP | Damage (hardware) | erase-rail VPP routing | mitigate | Chip-OUT 14V VPP multimeter dry-run BEFORE seating (Task 2); measured value must be under the 22V ceiling; r1≈270000 reconcile (Task 1) makes the measurement meaningful |
| T-77-FALSEPASS | Spoofing (false PASS) | verify oracle board | mitigate | Leonardo-only (v1.9 read bug corrupts other boards' oracle); independent post-write SHA-match read; non-vacuous wrong-file negative control must exit non-zero |
| T-77-A4 | Denial of Service | no-`-b` auto-erase write path | mitigate | The seated write (Task 3) is the live proof the no-`-b` path completes clean with no 0xA4 desync (complements the Plan-02 regression test) |
</threat_model>

<verification>
- Bench cycle clean (write→auto-erase→program→verify, no 0xA4) on Leonardo with non-blank W27C512
- source/readback SHA-256 digests match (SC#2)
- wrong-file verify exits non-zero (non-vacuous)
- erase-rail VPP measured chip-OUT, under 22V ceiling, recorded (SC#3)
- shield rev, port identity, r1≈270000 recorded (standing precondition)
</verification>

<success_criteria>
- SC#2: single default write of a non-blank W27C512 drives the full cycle clean; independent read SHA-matches source; wrong-file verify exits non-zero
- SC#3: 14V erase-rail VPP confirmed under ceiling via chip-OUT dry-run, value recorded
- ERASE-02 graduation evidence on record (the FINAL step of the SAFE discipline for this phase)
</success_criteria>

<output>
Create `.planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-04-SUMMARY.md` when done.
</output>
