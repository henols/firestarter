---
phase: 79
plan: 02
type: execute
wave: 2
depends_on: [79-01]
files_modified:
  - firestarter_app/tools/build_db.py
  - firestarter_app/tools/check_dispatch.py
  - firestarter_app/firestarter/data/chip_database.json
  - firestarter_app/tests/test_build_db_inclusion.py
  - firestarter_app/tests/test_chip_resolver.py
  - firestarter_app/tests/test_cli_handlers.py
  - firestarter_app/tests/test_check_dispatch_invariants.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [NMOS-02]
tags: [nmos, vpp, 25v, ceiling, build-db, check-dispatch, db-regen, tests, ruff]

must_haves:
  truths:
    - "RURP_VPP_CEILING_MV is 25000 in build_db.py and _FAMILY_VPP_INVARIANTS['configure_eprom'] is (0, 25000) in check_dispatch.py — both raised in the SAME commit"
    - "After DB regen, the 4 NMOS chips (INTEL M2716, INTEL M2732 combined entry, SGS-THOMSON ETC2716, ST M2716) re-classify off vpp-exceeds-max to support_status=supported with vpp_mv=25000 and algorithm=11 (0x0B)"
    - "ZERO chips with support_status=vpp-exceeds-max remain in chip_database.json; M2732A (21V) standalone entries are untouched and still supported"
    - "check_dispatch.py exits 0 (0 violations) at the new ceiling and >25V chips would still fail-closed (FUT-02 preserved)"
    - "The full test suite is green within this wave (the ~7 vpp-exceeds-max-exemplar tests are updated + new non-vacuous positive tests added) and ruff check/format pass at --target-version py39"
    - "REQUIREMENTS.md FUT-03 root-cause text is corrected from PCB-feedback-resistor framing to manual-shield-potentiometer framing (D-01)"
  artifacts:
    - path: "firestarter_app/tools/build_db.py"
      provides: "RURP_VPP_CEILING_MV = 25000"
      contains: "RURP_VPP_CEILING_MV = 25000"
    - path: "firestarter_app/tools/check_dispatch.py"
      provides: "_FAMILY_VPP_INVARIANTS['configure_eprom'] = (0, 25000)"
      contains: "(0, 25000)"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "4 NMOS chips reclassified supported; zero vpp-exceeds-max chips"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FUT-03 corrected to manual-potentiometer root cause"
  key_links:
    - from: "build_db.py RURP_VPP_CEILING_MV=25000 (strict-greater compare)"
      to: "chip_database.json support_status=supported for the 4 NMOS chips"
      via: "python3 tools/build_db.py regen (NMOS_TRUE_VPP_MV 25000 > 25000 is False)"
      pattern: "RURP_VPP_CEILING_MV = 25000"
    - from: "check_dispatch.py configure_eprom invariant (0, 25000)"
      to: "0 violations at new ceiling, FUT-02 >25V fail-closed preserved"
      via: "python3 tools/check_dispatch.py"
      pattern: "configure_eprom.*25000"
---

<objective>
Raise the host-side VPP ceiling from 22000 to 25000 mV, regenerate the chip database (which re-classifies the 4 NMOS chips off `vpp-exceeds-max`), update the ~7 tests that used those chips as the sole `vpp-exceeds-max` exemplar, add non-vacuous positive tests for the new ceiling, and correct the REQUIREMENTS.md FUT-03 root-cause text. Host-only (firestarter_app); no firmware change, no version bump, no lockstep (per RESEARCH Q7 — neither constant lives in constants.py/firestarter.h).

Purpose: This implements NMOS-02. The 22V ceiling was a conservative software limit; 25V is the new ceiling. The two ceiling constants must move together (build_db.py + check_dispatch.py) to keep the CI invariant semantically consistent, and the test suite must not go red across the wave boundary.

Output: edited build_db.py + check_dispatch.py, regenerated chip_database.json, updated + new tests, corrected REQUIREMENTS.md FUT-03.

GATE — OPERATOR OVERRIDE (2026-06-23, CONTEXT D-07 supersedes D-05): The original gate required a CLEARED ≥25V 79-01 verdict. The corrected 79-01 re-run measured ~15–19V (NOT CLEARED), and the operator has authorized **best-effort graduation with NO hardware change, ever**: proceed with this plan REGARDLESS of the NOT-CLEARED verdict. Graduation is no longer gated on ≥25V or on a bench SHA-match. The chips graduate to `supported`; a real write uses the existing 0x0B/direct-VPE path where the firmware warns-and-proceeds on under-voltage (no damage, may not verify — best-effort, user opts in). 79-03 is demoted to informational bench validation. This authorization is the sole reason this plan runs on a NOT-CLEARED gate.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-CONTEXT.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-RESEARCH.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raise the ceiling (both constants) + DB regen + safety gate</name>
  <files>firestarter_app/tools/build_db.py, firestarter_app/tools/check_dispatch.py, firestarter_app/firestarter/data/chip_database.json</files>
  <read_first>
    - firestarter_app/tools/build_db.py:107-117 (NMOS_TRUE_VPP_MV + RURP_VPP_CEILING_MV) and the Site C classification block (~645-671: `_nmos_vpp_mv > RURP_VPP_CEILING_MV` strict-greater demotion)
    - firestarter_app/tools/check_dispatch.py:78-93 (_FAMILY_VPP_INVARIANTS + _DB_CHECKED_VPP_INVARIANTS scope)
    - firestarter_app/firestarter/chip_resolver.py:54-57 (host guard self-clears when support_status flips to supported via DB regen — NO guard edit)
    - .planning/phases/79-25v-nmos-ceiling-raise/79-RESEARCH.md § "Edit Sites" + § Pitfalls 3/5/6 (both constants together; regen BEFORE gate; combined INTEL entry only)
    - firestarter_app/CLAUDE.md (chip_database.json is generated — never hand-edit; ruff --target-version py39 gate)
    - NOTE on DB shape: chip_database.json is a dict keyed by manufacturer, each value a LIST of chip entries (e.g. d["INTEL"] is a list; each entry has part_number, support_status, programming.algorithm, electrical, pinout). Traverse manufacturer→list→entry; do NOT assume a flat dict.
  </read_first>
  <behavior>
    - After edit: `from tools.build_db import RURP_VPP_CEILING_MV; assert RURP_VPP_CEILING_MV == 25000`
    - After edit: `_FAMILY_VPP_INVARIANTS["configure_eprom"] == (0, 25000)` in check_dispatch.py
    - After regen: the 4 NMOS chips (INTEL `M2716,M2716M`; INTEL `2732,2732A,M2732,M2732A`; SGS-THOMSON `ETC2716,M2716`; ST `ETC2716,M2716`) have support_status=supported, vpp_mv=25000, programming.algorithm=11 (0x0B), no unsupported_reason
    - After regen: zero chips in chip_database.json have support_status="vpp-exceeds-max" (verified by Task 2's test_zero_vpp_exceeds_max_chips_remain)
    - After regen: M2732A standalone entries (SGS-THOMSON/ST) unchanged — still supported at vpp_mv=21000
    - `python3 tools/check_dispatch.py` exits 0 with 0 violations (expect ~734 supported / ~10 non-dispatchable)
  </behavior>
  <action>
    In firestarter_app/tools/build_db.py:117 change `RURP_VPP_CEILING_MV = 22000` to `RURP_VPP_CEILING_MV = 25000`. In firestarter_app/tools/check_dispatch.py:79 change the `_FAMILY_VPP_INVARIANTS["configure_eprom"]` tuple from `(0, 22000)` to `(0, 25000)` and update its inline comment to note the Phase 79 raise. Both edits in the SAME commit (Pitfall 3). Then regenerate the DB: `cd firestarter_app && python3 tools/build_db.py`. Then run `python3 tools/diff_db.py` and confirm exactly the 4 INTEL/SGS-THOMSON/ST NMOS entries changed (support_status vpp-exceeds-max→supported, algorithm 0→11, unsupported_reason removed) — the M2732A standalone entries must NOT appear in the diff (Pitfall 6). Run `python3 tools/check_dispatch.py` AFTER regen (Pitfall 5) and confirm exit 0 / 0 violations. The strict-greater compare means a 25V chip at exactly 25000 stays supported; >25V chips (none exist today) stay fail-closed (FUT-02). Do NOT hand-edit chip_database.json or chip_resolver.py — the guard self-clears from the regen. After edits run `ruff check --target-version py39 . && ruff format --check --target-version py39 .` from firestarter_app/.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -c "from tools.build_db import RURP_VPP_CEILING_MV; assert RURP_VPP_CEILING_MV == 25000" && python3 tools/check_dispatch.py && ruff check --target-version py39 . && ruff format --check --target-version py39 .</automated>
  </verify>
  <done>RURP_VPP_CEILING_MV=25000 + check_dispatch invariant (0,25000); DB regenerated with 4 NMOS chips supported and (per Task 2's gate) zero vpp-exceeds-max chips; check_dispatch.py exits 0; ruff clean at py39.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update vpp-exceeds-max-exemplar tests + add non-vacuous positive tests</name>
  <files>firestarter_app/tests/test_build_db_inclusion.py, firestarter_app/tests/test_chip_resolver.py, firestarter_app/tests/test_cli_handlers.py, firestarter_app/tests/test_check_dispatch_invariants.py</files>
  <read_first>
    - .planning/phases/79-25v-nmos-ceiling-raise/79-RESEARCH.md § "Test Updates Required" (the 7-test table) + § "New tests to add" + § "Tests NOT broken"
    - .planning/phases/79-25v-nmos-ceiling-raise/79-VALIDATION.md § Wave 0 Requirements
    - firestarter_app/tests/test_build_db_inclusion.py (TestNmosVppCorrection, test_vpp_exceeds_max_reason_*)
    - firestarter_app/tests/test_chip_resolver.py (the two M2716 vpp-exceeds-max tests)
    - firestarter_app/tests/test_cli_handlers.py (the three M2716 vpp-exceeds-max / refusal tests)
    - firestarter_app/tests/test_check_dispatch_invariants.py:~153 (the synthetic non-violation fixture test)
  </read_first>
  <behavior>
    - test_build_db_inclusion.py: `test_nmos_vpp_exceeds_max` rewritten/renamed to `test_nmos_graduated_to_supported` asserting all 4 NMOS entries are supported with vpp_mv=25000; NEW `test_zero_vpp_exceeds_max_chips_remain` asserts no chip in the DB has support_status=vpp-exceeds-max (traversing manufacturer→list→entry); `test_vpp_exceeds_max_reason_*` re-anchored (M2716 no longer has unsupported_reason — assert 0 vpp-exceeds-max chips / keep FUT-02 contract as synthetic invariant)
    - test_chip_resolver.py: the two tests using M2716 as vpp-exceeds-max re-anchored to a still-non-supported exemplar (X88C64P protocol-not-implemented, or AT28C16 adapter-required); NEW positive `test_resolve_chip_nmos_graduated_resolves` asserts resolve_chip("M2716") succeeds (no exception)
    - test_cli_handlers.py: the three M2716 vpp-exceeds-max/refusal tests re-anchored to X88C64P or AT28C16
    - test_check_dispatch_invariants.py: NEW `test_configure_eprom_with_25v_vpp_is_not_a_violation` proving vpp_mv=25000 does NOT violate the new (0,25000) range (non-vacuous positive test for the new ceiling)
    - test_nmos_m2732a_supported and test_info_vpp_exceeds_max_no_crash and the vpp_mv=12000 fixture test remain green unchanged
  </behavior>
  <action>
    Apply the 7-test re-anchoring from RESEARCH § "Test Updates Required": rename/invert `test_nmos_vpp_exceeds_max`→`test_nmos_graduated_to_supported`; re-anchor every test that used M2716/M2732 as the `vpp-exceeds-max` exemplar to a chip that STAYS non-supported — use `X88C64P` (protocol-not-implemented) or `AT28C16` (adapter-required) per the table. Add the three NEW non-vacuous tests: `test_zero_vpp_exceeds_max_chips_remain` (DB invariant — traverse manufacturer→list→entry), `test_resolve_chip_nmos_graduated_resolves` (positive: M2716 resolves), `test_configure_eprom_with_25v_vpp_is_not_a_violation` (proves 25000 passes the new range — the positive control for the ceiling raise, NOT a vacuous re-assert of 12000). Do NOT use M2716/M2732 as a `vpp-exceeds-max` exemplar anywhere (that category is now empty). Run the full suite + ruff at py39 within this wave so the suite never goes red across the boundary.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_build_db_inclusion.py::test_zero_vpp_exceeds_max_chips_remain tests/test_chip_resolver.py::test_resolve_chip_nmos_graduated_resolves tests/test_check_dispatch_invariants.py::test_configure_eprom_with_25v_vpp_is_not_a_violation -q && python3 -m pytest tests/test_build_db_inclusion.py tests/test_chip_resolver.py tests/test_cli_handlers.py tests/test_check_dispatch_invariants.py -q && python3 -m pytest --cov --cov-fail-under=70 -q && ruff check --target-version py39 . && ruff format --check --target-version py39 .</automated>
  </verify>
  <done>All 7 affected tests re-anchored to X88C64P/AT28C16; 3 new non-vacuous tests (incl. test_zero_vpp_exceeds_max_chips_remain) added and passing; full suite green with cov>=70; ruff clean at py39.</done>
</task>

<task type="auto">
  <name>Task 3: Correct REQUIREMENTS.md FUT-03 root-cause text (D-01 doc fix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:48 (FUT-03 current text — PCB-feedback-resistor framing)
    - .planning/phases/79-25v-nmos-ceiling-raise/79-CONTEXT.md D-01 (the VPP setpoint is a MANUAL shield potentiometer, not PCB feedback resistors; R1/R2 only scale the ADC readback) + the Deferred Ideas note flagging this doc fix
  </read_first>
  <acceptance_criteria>
    - REQUIREMENTS.md FUT-03 no longer claims the AP3012 output is "physically set by PCB feedback resistors" or that unblocking "requires a PCB feedback-resistor change".
    - FUT-03 states the FINAL understanding per CONTEXT D-07 (supersedes D-01 + D-05): the VPP boost output is set by a MANUAL potentiometer on the shield (R1/R2 only scale the ADC readback); even at MAX pot the direct VPE rail (the 0x0B path) reaches only ~15–19V on this shield — NOT 25V — so "crank the pot" alone does not reach 25V either.
    - FUT-03 records the operator decision: NO hardware change ever; the 4 NMOS chips are graduated BEST-EFFORT — they resolve and attempt a write on the direct-VPE rail where firmware warns-and-proceeds on under-voltage (no damage; may not verify). The ≥25V hard pre-gate is retired.
    - The 79-01 evidence reference and the superseding-note (prior NOT-CLEARED measured the wrong/dropped rail; corrected re-run still ~15–19V) are reflected; the corrected dry-run uses the direct-VPE path (`dev reg 0 0 0x86 -f`), not `firestarter vpp`.
    - This is a doc-only edit; the capability change (best-effort graduation) is implemented by Tasks 1–2.
  </acceptance_criteria>
  <action>
    Rewrite the FUT-03 root-cause sentence(s) in .planning/REQUIREMENTS.md:48 per CONTEXT.md D-07 (final understanding). Replace the "PCB feedback-resistor" framing entirely. State: the VPP boost output is set by an operator-cranked MANUAL potentiometer on the shield (NOT PCB feedback resistors, NOT firmware-controlled); R1/R2 only scale the ADC readback; the corrected 79-01 re-run measured the DIRECT VPE path (the 0x0B rail, `dev reg 0 0 0x86 -f`, NOT `firestarter vpp`'s dropped ~13V path) at MAX pot and read only ~15–19V — so even cranking the pot does not reach 25V on this shield. Record the operator decision (D-07): no hardware change ever; the 4 NMOS chips graduate BEST-EFFORT — they resolve and attempt a write on the direct-VPE rail, with the firmware warning-and-proceeding on under-voltage (no damage; the write may not verify; the user opts in). The prior NOT-CLEARED verdict (and its PCB-resistor framing) is superseded. Keep it concise — a root-cause + decision correction, not a new requirement. MUST still contain the word "potentiometer" and MUST NOT contain "requires a ... PCB feedback-resistor change".
  </action>
  <verify>
    <automated>grep -v '^#' .planning/REQUIREMENTS.md | grep -A2 'FUT-03' | grep -iq 'potentiometer' && ! { grep -A3 'FUT-03' .planning/REQUIREMENTS.md | grep -iq 'requires a .*PCB feedback-resistor change'; }</automated>
  </verify>
  <done>FUT-03 root cause corrected to manual-potentiometer framing; PCB-feedback-resistor root-cause language removed; doc-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_db.py ceiling constant → generated chip_database.json | Raising the ceiling re-classifies chips; a mistake could graduate a chip that should stay fail-closed |
| check_dispatch.py invariant → CI safety gate | The invariant is the guard that catches a future >25V chip routed to configure_eprom; it must move in lockstep with build_db.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-CEIL | Elevation (over-broad graduation) | RURP_VPP_CEILING_MV raise | mitigate | Strict-greater compare keeps >25V chips fail-closed (FUT-02); diff_db.py confirms ONLY the 4 intended entries changed; M2732A untouched |
| T-79-GATE | Tampering (stale/desynced invariant) | check_dispatch.py vs build_db.py ceiling | mitigate | Both constants raised in the SAME commit; check_dispatch.py run AFTER regen exits 0 with 0 violations |
| T-79-HANDEDIT | Tampering (hand-edited DB / guard) | chip_database.json, chip_resolver.py | mitigate | Graduation is driven ONLY by `python3 tools/build_db.py` regen; host guard self-clears; no hand-edit of support_status or the guard (RESEARCH Q5) |
| T-79-VACUOUS | Repudiation (false test confidence) | new ceiling tests | mitigate | New tests are non-vacuous: positive control proves vpp_mv=25000 passes the new range, resolve_chip("M2716") succeeds, and test_zero_vpp_exceeds_max_chips_remain fails on the pre-edit DB — not a re-assert of the old 12000 fixture |
| T-79-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `python3 -c "from tools.build_db import RURP_VPP_CEILING_MV; assert RURP_VPP_CEILING_MV == 25000"` passes.
- `python3 tools/check_dispatch.py` exits 0 with 0 violations after DB regen.
- `test_zero_vpp_exceeds_max_chips_remain` passes (zero vpp-exceeds-max chips in the DB).
- Full test suite green (cov>=70); ruff check + ruff format --check pass at --target-version py39.
- REQUIREMENTS.md FUT-03 corrected to manual-potentiometer root cause.
</verification>

<success_criteria>
- NMOS-02 complete: ceiling raised in both files, DB regenerated, 4 NMOS chips supported at vpp_mv=25000 / algorithm 0x0B, zero vpp-exceeds-max chips, M2732A untouched.
- check_dispatch.py green; FUT-02 (>25V fail-closed) preserved.
- Tests re-anchored + non-vacuous positive tests added; suite green; ruff clean at py39.
- FUT-03 doc corrected.
</success_criteria>

<output>
Create `.planning/phases/79-25v-nmos-ceiling-raise/79-02-SUMMARY.md` when done.
</output>
