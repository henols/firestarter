---
phase: 79
plan: 03
type: execute
wave: 3
depends_on: [79-01, 79-02]
files_modified: []
autonomous: false
requirements: [NMOS-03]
tags: [nmos, vpp, 25v, graduation, leonardo, bench, write-verify, sha, negative-control, parity]

must_haves:
  truths:
    - "resolve_chip('M2716') succeeds (no ChipNotImplementedError) after the 79-02 DB regen — the host guard self-cleared from support_status=supported (no guard edit)"
    - "firmware<->host constants parity (FLAG_*/protocol) stays green — no constants.py/firestarter.h touched this phase (SAFE-03)"
    - "A real NMOS chip is written on a Leonardo (NOT uno328pb, NOT Rev 0 shield) and an independent read-back SHA matches the written image"
    - "A non-vacuous negative control proves verify is real: verifying against a WRONG file exits non-zero"
    - "Live R1/R2 reconcile and silkscreen shield rev are on record for the bench proof"
  artifacts:
    - path: ".planning/phases/79-25v-nmos-ceiling-raise/79-03-SUMMARY.md"
      provides: "NMOS-03 graduation evidence: resolve_chip success, parity green, Leonardo write+read-back SHA match, negative control, R1/R2 + shield rev"
  key_links:
    - from: "chip_database.json support_status=supported (79-02 regen)"
      to: "resolve_chip('M2716') returns a programmer config (no refusal)"
      via: "chip_resolver.resolve_chip host-guard self-clear"
      pattern: "resolve_chip"
    - from: "Leonardo write of a known image"
      to: "independent read-back SHA match (+ wrong-file negative control non-zero exit)"
      via: "firestarter write then read + sha256 compare on Leonardo"
      pattern: "sha256"
---

<objective>
Graduate the 4 NMOS chips by proving the end-to-end programming path on real hardware: confirm the host guard self-cleared (resolve_chip succeeds), confirm firmware<->host parity is untouched (SAFE-03), then perform a Leonardo write + independent read-back SHA-match on a physical NMOS chip with a non-vacuous negative control. Reuses the Phase 77 graduation pattern (DB-regen-driven support flip + Leonardo write/verify SHA-match as the final gate).

Purpose: This implements NMOS-03 and is the FINAL graduation step. The DB regen in 79-02 already flipped support_status to supported and self-cleared the host guard (chip_resolver.py:54-57) — no guard edit. The bench proof is the safety-critical gate: it confirms the 25V rail (CLEARED in 79-01) actually programs a real chip, and the negative control proves the verify oracle is real (guards against false-PASS, the dominant v1.13 failure class). Leonardo is the ONLY trustworthy program/verify board (uno328pb brownout 999.2; Rev 0 read bug).

Output: 79-03-SUMMARY.md recording the graduation evidence.

GATE: Depends on 79-01 CLEARED (>= 25V) and 79-02 complete. Does not run otherwise.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-CONTEXT.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-01-SUMMARY.md
@.planning/phases/79-25v-nmos-ceiling-raise/79-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm host-guard self-clear + firmware/host parity (SAFE-03)</name>
  <files>(none — verification only)</files>
  <read_first>
    - firestarter_app/firestarter/chip_resolver.py:40-63 (host guard: support_status != supported raises ChipNotImplementedError; self-clears once 79-02 regen set supported)
    - firestarter_app/firestarter/data/chip_database.json (post-79-02: the 4 NMOS chips supported)
    - firestarter_app/tests/test_revision_constants_parity.py (FLAG_*/protocol parity — must stay green; no constants.py/firestarter.h touched this phase per RESEARCH Q7)
    - .planning/phases/77-erase-write-path-graduation-0x07-ee-eproms/77-RESEARCH.md (Phase 77 graduation pattern — guard self-clear + SHA-match final gate)
  </read_first>
  <acceptance_criteria>
    - `resolve_chip("M2716", EpromDatabase(skip_local_override=True))` returns a programmer config WITHOUT raising ChipNotImplementedError (run live).
    - The NEW 79-02 positive test `test_resolve_chip_nmos_graduated_resolves` passes.
    - `pytest tests/test_revision_constants_parity.py` is green (SAFE-03 — no FLAG_*/protocol constant touched).
    - No edit to chip_resolver.py or constants.py/firestarter.h was made (guard self-cleared from the DB).
  </acceptance_criteria>
  <action>
    Confirm the graduation surfaced through the host without any guard edit. Run a live resolve_chip on each graduated alias (at minimum M2716, plus the chip physically on hand) using `EpromDatabase(skip_local_override=True)` and assert no ChipNotImplementedError. Run `pytest tests/test_revision_constants_parity.py -q` and confirm green (SAFE-03: this phase touched neither constants.py nor firestarter.h, so parity is unaffected — RESEARCH Q7). Record both in the summary. Do NOT edit chip_resolver.py — the guard self-clears from the 79-02 DB regen (RESEARCH Q5).
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -c "from firestarter.chip_resolver import resolve_chip; from firestarter.database import EpromDatabase; resolve_chip('M2716', EpromDatabase(skip_local_override=True)); print('resolve OK')" && python3 -m pytest tests/test_chip_resolver.py::test_resolve_chip_nmos_graduated_resolves tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <done>resolve_chip('M2716') succeeds; parity test green; no guard/constants edit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Leonardo write + read-back SHA match + negative control (T-79-BENCH)</name>
  <read_first>
    - .planning/STATE.md ("Standing bench precondition" — Leonardo only; uno328pb N/A; Rev 0 excluded; ASK shield rev; live R1/R2; verify controller identity per task)
    - .planning/phases/79-25v-nmos-ceiling-raise/79-01-SUMMARY.md (the CLEARED >=25V direct-VPE verdict that authorizes this write)
    - .planning/phases/79-25v-nmos-ceiling-raise/79-CONTEXT.md (D-05: chip must actually receive its rated 25V; D-06 graduation sequencing)
    - firestarter/src/proms/eprom.cpp:209-272 (eprom_check_vpp will block over-voltage / warn under-voltage at write time — runtime backstop)
    - the Phase 77 graduation bench pattern (write known image → independent read-back → sha256 compare; wrong-file negative control)
  </read_first>
  <what-built>
    The graduation bench proof on real hardware. With a physical NMOS chip (M2716, M2732, or ETC2716 — operator's choice from inventory) seated on a Leonardo with a Rev 2.2/Rev 2.0 shield (NOT Rev 0, NOT uno328pb), the VPP pot at the CLEARED >=25V setting from 79-01, write a known image, then do an INDEPENDENT read-back and confirm a SHA-256 match. Then run the non-vacuous negative control: verify the chip against a WRONG/altered file and confirm it exits NON-ZERO (proves the verify oracle is real, not rubber-stamping). Record live R1/R2 reconcile and the silkscreen shield rev. Confirm controller port identity at task start (ports shuffle across replug).
  </what-built>
  <how-to-verify>
    1. Confirm controller identity: `firestarter -p <port> fw` shows leonardo; record port. Record live R1/R2 via `firestarter -p <port> config`. ASK + record silkscreen shield rev (Rev 2.2/2.0, NOT Rev 0).
    2. Confirm the VPP pot is at the CLEARED >=25V position from 79-01.
    3. Seat the chosen NMOS chip on the Leonardo.
    4. Write a known image: `firestarter -p <port> write <CHIP> <image.bin>` (use `-b` if the chip is non-blank). Confirm no over/under-voltage ERROR from eprom_check_vpp.
    5. Independent read-back: `firestarter -p <port> read <CHIP> <readback.bin>`; `sha256sum image.bin readback.bin` — hashes MUST match (sized to the chip image).
    6. Negative control: verify against a WRONG file (e.g. a flipped-byte copy) — `firestarter -p <port> verify <CHIP> <wrong.bin>` MUST exit non-zero.
    7. Record all evidence in 79-03-SUMMARY.md.
  </how-to-verify>
  <acceptance_criteria>
    - Controller identity (leonardo + port + fw), live R1/R2, and silkscreen shield rev (Rev 2.2/2.0, NOT Rev 0) recorded.
    - Write completed with NO over/under-voltage ERROR from firmware eprom_check_vpp (rail adequate, confirming the 79-01 >=25V CLEARED gate).
    - Independent read-back SHA-256 matches the written image (sized to chip).
    - Negative control: verify against a wrong file exits NON-ZERO (verify oracle proven real).
    - Chip alias used (M2716/M2732/ETC2716) recorded.
    - All evidence captured in 79-03-SUMMARY.md.
  </acceptance_criteria>
  <action>
    On a Leonardo (uno328pb is N/A — 999.2 brownout; Rev 0 excluded) with the VPP pot at the 79-01 CLEARED >=25V position, write a known image to a physical NMOS chip (M2716/M2732/ETC2716, operator's choice), do an independent read-back, and confirm `sha256sum` match. Then run the non-vacuous negative control: `verify` against a deliberately-wrong file must exit non-zero. Record controller identity, live R1/R2 reconcile, and silkscreen shield rev. If the firmware returns a VPP over/under-voltage ERROR at write time, STOP — that contradicts the 79-01 CLEARED gate and the graduation cannot be claimed. This is the FINAL graduation gate (Phase 77 pattern); a clean SHA-match + a failing negative control are both required.
  </action>
  <verify>
    <human-check>Operator reports: chip used, write completed without VPP ERROR, read-back SHA matches, and the wrong-file verify exited non-zero; R1/R2 + shield rev recorded.</human-check>
  </verify>
  <resume-signal>Type the chip alias, the SHA-match result, and the negative-control exit status, or report a blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 25V VPP rail → real NMOS chip socket | The write applies the (now >=25V) rail to a physical chip; an inadequate rail under-drives the write, an over-voltage rail damages the chip |
| read-back oracle → SHA claim | A false-PASS (untrustworthy verify board) would graduate a chip on a hollow proof — the dominant v1.13 failure class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-BENCH | Tampering (false-PASS graduation) | Leonardo write + read-back verify | mitigate | Independent read-back SHA-256 match + a non-vacuous wrong-file negative control (must exit non-zero); Leonardo only (uno328pb N/A 999.2, Rev 0 excluded) |
| T-79-UNDER2 | Denial (graduate on inadequate rail) | 25V rail at write time | mitigate | Firmware eprom_check_vpp over/under-voltage check at write time is the runtime backstop; a write ERROR contradicts the 79-01 CLEARED gate and STOPS the graduation (D-05) |
| T-79-SAFE03 | Tampering (silent wire-constant drift) | constants.py / firestarter.h parity | mitigate | No FLAG_*/protocol constant touched this phase; parity test green (RESEARCH Q7) |
| T-79-GUARD | Elevation (hand-flipped support_status) | chip_resolver host guard | mitigate | Guard self-clears ONLY from the 79-02 DB regen; live resolve_chip confirms; no guard/DB hand-edit |
</threat_model>

<verification>
- `resolve_chip('M2716')` succeeds live; `test_resolve_chip_nmos_graduated_resolves` and the parity test green.
- Leonardo write of a known image to a real NMOS chip completes without a firmware VPP ERROR.
- Independent read-back SHA-256 matches the written image.
- Wrong-file verify exits non-zero (negative control).
- Live R1/R2 + silkscreen shield rev recorded.
</verification>

<success_criteria>
- NMOS-03 complete: 4 NMOS chips graduated to supported (host guard self-cleared, parity green), proven by a Leonardo write + independent read-back SHA-match on a real NMOS chip with a passing non-vacuous negative control.
- All bench evidence (chip, SHA, negative control, R1/R2, shield rev) recorded.
</success_criteria>

<output>
Create `.planning/phases/79-25v-nmos-ceiling-raise/79-03-SUMMARY.md` when done.
</output>
