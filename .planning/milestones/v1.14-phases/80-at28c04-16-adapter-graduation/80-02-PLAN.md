---
phase: 80
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - firestarter_app/tests/test_chip_resolver.py
  - firestarter_app/tests/test_build_db_inclusion.py
  - firestarter_app/tests/test_cli_handlers.py
autonomous: true
requirements: [ADPT-02]
must_haves:
  truths:
    - "Positive graduation tests exist for AT28C16 and AT28C04: resolve_chip succeeds and returns algorithm 0x0D (RED before the build_db.py edit lands in Plan 03)"
    - "A DB-supported assertion exists: AT28C16 has support_status='supported'"
    - "The existing adapter-required assertions are inverted/retargeted so the suite reflects post-graduation behavior (the inverted tests are RED until Plan 03 regenerates the DB)"
    - "The pre-edit RED/observed state of every changed test is recorded in the SUMMARY"
  artifacts:
    - path: "firestarter_app/tests/test_chip_resolver.py"
      provides: "Inverted adapter-required test + 2 positive graduation tests (AT28C16, AT28C04)"
      contains: "at28c16"
    - path: "firestarter_app/tests/test_build_db_inclusion.py"
      provides: "Retargeted TestAdapterRequired24Pin + positive test_at28c16_is_supported; removed the two breaking TestUnsupportedReasonStrings adapter-doc tests"
      contains: "supported"
    - path: "firestarter_app/tests/test_cli_handlers.py"
      provides: "Retargeted info/read adapter-status tests for post-graduation behavior"
      contains: "AT28C16"
  key_links:
    - from: "graduation tests (this plan, RED)"
      to: "build_db.py named-arm removal + DB regen (Plan 03, GREEN)"
      via: "tests assert support_status='supported' and resolve_chip success — go green only after the DB flips"
      pattern: "support_status.*supported"
---

<objective>
Write/invert the AT28C04/AT28C16 graduation tests RED FIRST, before any `build_db.py` edit. Add
positive assertions (resolve_chip succeeds for AT28C16/AT28C04, AT28C16 is `supported` in the DB) and
invert/retarget the existing `adapter-required` assertions so the test suite encodes the
post-graduation contract. Do NOT touch `build_db.py` or regenerate the DB in this plan — that lands in
Plan 03 (the GREEN step), which keeps the suite from going red across a wave boundary.

Purpose: ADPT-02 (test-first half) — lock the graduation behavior as executable tests before the
source edit, mirroring the Phase 77 RED→GREEN discipline (77-01 wrote 3 wire-level tests RED before
77-01 Task 2 edited the source). Per 80-VALIDATION.md Wave 0, these tests are the thing that locks the
graduation; they will be RED until Plan 03 regenerates the DB in the SAME wave.

Output: Edited `test_chip_resolver.py`, `test_build_db_inclusion.py`, `test_cli_handlers.py` with the
pre-edit RED/observed state recorded in 80-02-SUMMARY.md.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md
@.planning/phases/80-at28c04-16-adapter-graduation/80-VALIDATION.md
@firestarter_app/tests/test_chip_resolver.py
@firestarter_app/tests/test_build_db_inclusion.py
@firestarter_app/tests/test_cli_handlers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0, RED): Positive graduation tests + inverted resolve_chip test in test_chip_resolver.py</name>
  <files>firestarter_app/tests/test_chip_resolver.py</files>
  <read_first>
    - firestarter_app/tests/test_chip_resolver.py lines 77–86 (the existing `test_resolve_chip_adapter_required_raises_not_implemented(db)` — the test to INVERT; note the `db` fixture and the `resolve_chip(name, db=db)` call shape)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md (§Q3 — chip_resolver self-heals via support_status; §Q4 — the test inversion; §Validation Architecture)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-VALIDATION.md (Wave 0 Requirements — test_chip_resolver additions/inversion)
  </read_first>
  <behavior>
    - `test_resolve_chip_at28c16_supported_resolves`: `resolve_chip("AT28C16", db=db)` returns a dict (no raise) whose `algorithm == 0x0D` and which has a valid `memory-size`.
    - `test_resolve_chip_at28c04_supported_resolves`: `resolve_chip("AT28C04", db=db)` returns a dict with `algorithm == 0x0D` and a valid `memory-size` (512B-class part).
    - Inverted `test_resolve_chip_adapter_required_raises_not_implemented`: was "AT28C04 must raise ChipNotImplementedError"; convert to a positive assertion that `resolve_chip("AT28C04", db=db)` SUCCEEDS (no raise) — rename to reflect graduation (e.g. `test_resolve_chip_at28c04_no_longer_refused`), since the chip is supported post-graduation.
  </behavior>
  <action>
    Add the two positive graduation tests above, reusing the existing module `db` fixture (do NOT
    re-declare it) and the established `resolve_chip(name, db=db)` call shape. Invert the existing
    `test_resolve_chip_adapter_required_raises_not_implemented` (lines 77–86): change it from asserting
    `pytest.raises(ChipNotImplementedError)` to asserting a successful resolve, and rename it to a
    graduation-positive name. Resolve the correct algorithm-field key by reading how the existing
    passing tests in this file read the resolved dict (the wire key may be `algorithm` or a constant
    name — match the file's existing convention; do NOT guess). These tests are written FIRST and will
    be RED until Plan 03 regenerates the DB with support_status='supported' for these chips — record
    the RED/observed state in the SUMMARY. Do NOT edit build_db.py or regenerate the DB here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check --target-version py39 tests/test_chip_resolver.py && ruff format --check --target-version py39 tests/test_chip_resolver.py && python3 -m pytest tests/test_chip_resolver.py -k "at28c" -v --no-header || true</automated>
  </verify>
  <acceptance_criteria>
    - Two new positive tests (`...at28c16_supported_resolves`, `...at28c04_supported_resolves`) collected
    - `test_resolve_chip_adapter_required_raises_not_implemented` inverted to a graduation-positive assertion (renamed)
    - ruff check + ruff format --check (py39 target) clean on the file
    - Pre-edit RED/observed state recorded in 80-02-SUMMARY.md (these are EXPECTED to fail until Plan 03)
  </acceptance_criteria>
  <done>Positive resolve_chip graduation tests added + the adapter-required raise test inverted; ruff clean; RED state recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 0, RED): DB-supported + adapter-required retarget in test_build_db_inclusion.py and test_cli_handlers.py</name>
  <files>firestarter_app/tests/test_build_db_inclusion.py, firestarter_app/tests/test_cli_handlers.py</files>
  <read_first>
    - firestarter_app/tests/test_build_db_inclusion.py lines 93–150 (`TestAdapterRequired24Pin` — asserts SOME chip is adapter-required and AT28C family is among them; Pitfall 3: zero adapter-required chips remain post-graduation, so `assert adapter_chips` will fail) and lines ~396–480 (`TestUnsupportedReasonStrings.test_adapter_required_reason_starts_with_adapter_required` + `test_at28c16_named_arm_reason_mentions_adapter_doc` — both break post-graduation)
    - firestarter_app/tests/test_cli_handlers.py lines 169–195 (`test_info_adapter_required_no_crash` — stays valid: info exits 0), 809–835 (`test_info_adapter_required_shows_status` — breaks: no adapter notice post-graduation), 871–890 (`test_read_adapter_required_status_refusal` — breaks: AT28C16 no longer refused for "adapter")
    - .planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md (§Q4 full test list; §Pitfall 3 — TestAdapterRequired24Pin leaves no adapter-required chips)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    - `test_build_db_inclusion.py`: `TestAdapterRequired24Pin` retargeted — instead of asserting AT28C chips ARE adapter-required, assert AT28C16 is NOT adapter-required (positive graduation). Add `test_at28c16_is_supported` asserting `support_status == "supported"` for AT28C16 in the DB.
    - `test_build_db_inclusion.py`: the two `TestUnsupportedReasonStrings` adapter-doc tests (starts-with "adapter required:" / mentions AT28C04-ADAPTER.md) are REMOVED (they assert the now-deleted named-arm reason).
    - `test_cli_handlers.py`: `test_info_adapter_required_shows_status` retargeted — `info AT28C16` exits 0 and now shows `supported` (no "adapter" status notice). `test_read_adapter_required_status_refusal` retargeted — `read AT28C16` no longer exits 1 with an "adapter" reason (it reaches the no-port / connection path instead, i.e. NOT a support-status refusal). Leave `test_info_adapter_required_no_crash` (exit 0) as-is — still valid.
  </behavior>
  <action>
    Retarget the tests per the behavior block. For `TestAdapterRequired24Pin`: if AT28C are the only
    adapter-required chips (confirm by reading the class body + the DB), the `assert adapter_chips`
    premise is now false — repurpose the class to a positive graduation assertion (AT28C16 NOT
    adapter-required) rather than leaving a vacuous/failing assert; add `test_at28c16_is_supported`.
    REMOVE the two breaking `TestUnsupportedReasonStrings` adapter-doc tests entirely (they pin the
    deleted named-arm reason string). For `test_cli_handlers.py`: retarget the two breaking tests to
    the post-graduation contract — `info AT28C16` exits 0 showing supported; `read AT28C16` is no
    longer a support-status "adapter" refusal (assert it does NOT exit 1 with the adapter reason; it
    may exit non-zero for an unrelated no-port reason — match how other supported-chip read tests in
    this file assert the no-port path, do NOT invent a message). These edits are RED until Plan 03
    regenerates the DB — record the pre-edit observed state in the SUMMARY. Do NOT edit build_db.py or
    regenerate the DB here. ruff py39 target on both files.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check --target-version py39 tests/test_build_db_inclusion.py tests/test_cli_handlers.py && ruff format --check --target-version py39 tests/test_build_db_inclusion.py tests/test_cli_handlers.py && python3 -m pytest tests/test_build_db_inclusion.py -k "supported or adapter" -v --no-header || true</automated>
  </verify>
  <acceptance_criteria>
    - `TestAdapterRequired24Pin` retargeted to a positive graduation assertion (AT28C16 NOT adapter-required); `test_at28c16_is_supported` added
    - The two `TestUnsupportedReasonStrings` adapter-doc tests removed
    - `test_info_adapter_required_shows_status` + `test_read_adapter_required_status_refusal` retargeted to post-graduation behavior; `test_info_adapter_required_no_crash` left intact
    - ruff check + ruff format --check (py39 target) clean on both files
    - Pre-edit RED/observed state recorded in 80-02-SUMMARY.md
  </acceptance_criteria>
  <done>DB-supported + retargeted adapter tests in place across both files; breaking named-arm-reason tests removed; ruff clean; RED state recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite ↔ DB graduation contract | the graduation tests are the executable specification of the post-graduation contract; if they are vacuous or wrong-keyed, a broken graduation could pass silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-VACUOUS | Spoofing (false PASS) | retargeted/inverted graduation tests | mitigate | Positive resolve_chip tests assert a concrete `algorithm == 0x0D` + valid memory-size (not just "no raise"); the DB-supported test asserts the literal `support_status == "supported"`; tests are written RED first so the GREEN transition in Plan 03 is observed, not assumed |
| T-80-WAVERED | Tampering | suite redness across the wave boundary | accept | Tests are RED in this plan by design; Plan 03 (same wave) regenerates the DB and fixes all breakage so the suite is green at wave merge — the RED window never crosses a wave boundary |
</threat_model>

<verification>
- Two positive resolve_chip graduation tests + inverted adapter-required test in test_chip_resolver.py
- DB-supported + retargeted adapter tests in test_build_db_inclusion.py; two breaking reason-string tests removed
- Retargeted info/read tests in test_cli_handlers.py
- ruff check + ruff format --check (py39 target) clean on all three files
- Pre-edit RED/observed state recorded in 80-02-SUMMARY.md (RED is expected until Plan 03)
</verification>

<success_criteria>
- ADPT-02 (test-first half): graduation contract encoded as executable tests, RED before the source edit
- All adapter-required assertions inverted/retargeted/removed so the suite reflects post-graduation behavior
- ruff py39 clean; no build_db.py edit and no DB regeneration in this plan
</success_criteria>

<output>
Create `.planning/phases/80-at28c04-16-adapter-graduation/80-02-SUMMARY.md` when done.
</output>
