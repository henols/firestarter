---
phase: 80
plan: 03
type: execute
wave: 2
depends_on: ["80-02"]
files_modified:
  - firestarter_app/tools/build_db.py
  - firestarter_app/firestarter/data/chip_database.json
autonomous: true
requirements: [ADPT-02, SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "The _AT28C_DIP24_NAMES named arm (incl. the _chip_aliases construction used only by it) is deleted from build_db.py"
    - "chip_database.json is regenerated via `python3 tools/build_db.py` (NOT hand-edited); the 9 AT28C entries are now support_status='supported', proto_id=0x0D, pinout DIP24_2816"
    - "chip_resolver.resolve_chip self-heals — NO edit to chip_resolver.py (the support_status guard is data-driven)"
    - "check_dispatch.py passes (SAFE-02) AFTER regen with 0 violations; configure_eeprom28c stays excluded from _DB_CHECKED_VPP_INVARIANTS — NO check_dispatch.py edit required (Open Validation Note resolved)"
    - "All Plan-02 graduation tests go GREEN and the full suite passes with coverage >= 70% in the SAME wave (no red across the wave boundary)"
    - "SAFE-03 holds trivially: no constants.py / firestarter.h touched (no lockstep bump)"
  artifacts:
    - path: "firestarter_app/tools/build_db.py"
      provides: "build_db.py with the AT28C named arm removed (the 9 chips fall through to supported)"
      contains: "Named rule arm"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "Regenerated DB with the 9 AT28C entries as support_status='supported'"
      contains: "AT28C16"
    - path: ".planning/phases/80-at28c04-16-adapter-graduation/80-03-SUMMARY.md"
      provides: "Post-regen evidence: build_db diff, check_dispatch PASS line, full-suite + coverage figure, SAFE-01/02/03 dispositions, Open Validation Note resolution"
  key_links:
    - from: "build_db.py named-arm removal"
      to: "chip_database.json 9 AT28C entries"
      via: "python3 tools/build_db.py regeneration flips support_status adapter-required → supported"
      pattern: "support_status.*supported"
---

<objective>
GREEN step for the graduation: DELETE the `_AT28C_DIP24_NAMES` named arm in `build_db.py`, regenerate
`chip_database.json` via `python3 tools/build_db.py` (do NOT hand-edit the DB), and confirm the 9 AT28C
chips fall through to `support_status='supported'` (proto_id stays 0x0D, pinout DIP24_2816). Then run
all the SAFE gates AFTER the edit — `check_dispatch.py` (SAFE-02), constant parity (SAFE-03), the full
suite + coverage — and fix any remaining test breakage so the suite is green at wave merge. This is the
SAME wave as Plan 02's RED tests, so the suite never goes red across a wave boundary.

Purpose: ADPT-02 (source half) + SAFE-01/02/03. `chip_resolver.resolve_chip` self-heals (its guard is
support_status-driven — NO edit). The Open Validation Note is RESOLVED: `configure_eeprom28c` is
excluded from `_DB_CHECKED_VPP_INVARIANTS = frozenset({"configure_flash_intel"})` (check_dispatch.py),
so NO `(0,6000)→(0,13000)` invariant edit is needed — the §Critical research note is stale; the gate
passes unmodified. SAFE-01 in THIS phase IS an active guard-removal (unlike Phase 77's N/A): removing
the named arm flips support_status, which retires the `chip_resolver` `adapter-required` refusal as the
SOURCE-side final step — but the chips are only truly graduated after the Plan 04 bench proof.

Output: edited `build_db.py`, regenerated `chip_database.json`, and 80-03-SUMMARY.md with the gate
evidence.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md
@.planning/phases/80-at28c04-16-adapter-graduation/80-VALIDATION.md
@firestarter_app/tools/build_db.py
@firestarter_app/tools/check_dispatch.py
@firestarter_app/firestarter/chip_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (ADPT-02 GREEN): Delete the _AT28C_DIP24_NAMES named arm + regenerate the DB</name>
  <files>firestarter_app/tools/build_db.py, firestarter_app/firestarter/data/chip_database.json</files>
  <read_first>
    - firestarter_app/tools/build_db.py lines 416–459 (the `# Named rule arm: AT28C04/AT28C16 family (D-03, Phase 76)` comment block through the `_unsupported_reason = (...)` assignment, including the `_AT28C_DIP24_NAMES` set, the `_chip_aliases = {...}` construction, and the `if _chip_aliases & _AT28C_DIP24_NAMES:` block)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md (§Q2 — exact deletion; §Pitfall 1 — Site B does NOT fire for 0x0D chips, do NOT add a Site B exemption; §Pitfall 5 — verify AT28C04 mem_size=512)
  </read_first>
  <action>
    Delete the entire named-arm block in `build_db.py`: the `# Named rule arm: AT28C04/AT28C16 family
    (D-03, Phase 76)` comment, the `_AT28C_DIP24_NAMES` set, the `_chip_aliases = {...}` construction,
    and the `if _chip_aliases & _AT28C_DIP24_NAMES: _support_status=... _unsupported_reason=...` block.
    Confirm by grep that `_chip_aliases` has NO other use downstream (it does not — it is used only by
    this arm; verify before deleting). Do NOT touch Site B (lines ~391–414) — the AT28C chips arrive
    with proto_id=0x0D, which is NOT in Site B's {0x07,0x08,0x0B}, so Site B never fires for them and
    proto_id stays 0x0D (do NOT add a Site B exemption). After deletion the 9 chips fall through with
    the loop's default `support_status='supported'`. Regenerate the DB: `python3 tools/build_db.py`
    (do NOT hand-edit chip_database.json). Then confirm the regenerated DB shows the 9 AT28C entries as
    `support_status='supported'`, `algorithm=0x0D`, `pinout=DIP24_2816`, and AT28C04 `memory-size` /
    mem_size = 512 (not 2048). ruff py39 target on build_db.py.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ! grep -q '_AT28C_DIP24_NAMES' tools/build_db.py && python3 tools/build_db.py && ruff check --target-version py39 tools/build_db.py && ruff format --check --target-version py39 tools/build_db.py && python3 -c "import json; db=json.load(open('firestarter/data/chip_database.json')); at=[c for m in (db if isinstance(db,list) else db.values()) for c in (m if isinstance(m,list) else [m]) if 'AT28C16' in str(c.get('part_number','')) ]; assert at and all(c.get('support_status')=='supported' for c in at), at; print('AT28C16 supported:', len(at))"</automated>
  </verify>
  <acceptance_criteria>
    - `_AT28C_DIP24_NAMES` no longer present in build_db.py (grep returns nothing)
    - `_chip_aliases` confirmed to have no other downstream use before removal
    - Site B untouched (no new exemption added)
    - `python3 tools/build_db.py` regenerates chip_database.json successfully
    - The 9 AT28C entries are support_status='supported', proto_id=0x0D, pinout DIP24_2816; AT28C04 mem_size=512
    - ruff check + ruff format --check (py39 target) clean on build_db.py
  </acceptance_criteria>
  <done>Named arm deleted, DB regenerated; 9 AT28C chips now supported with proto_id 0x0D; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2 (SAFE-01/02/03 + ADPT-02 verify): SAFE gates green + all Plan-02 tests pass in the same wave</name>
  <files>(no source files modified — gate runs + evidence capture; minor test touch-ups only if a Plan-02 test needs a key/path correction surfaced by the regen)</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py lines 78–93 (the `_FAMILY_VPP_INVARIANTS` + `_DB_CHECKED_VPP_INVARIANTS = frozenset({"configure_flash_intel"})` — confirm configure_eeprom28c is EXCLUDED, so no invariant edit is needed; resolves the Open Validation Note)
    - firestarter_app/firestarter/chip_resolver.py lines 50–58 (the support_status guard — self-heals; confirm NO edit)
    - firestarter_app/firestarter/constants.py + firestarter/include/firestarter.h (FLAG_* / protocol constants — confirm UNTOUCHED for SAFE-03)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-RESEARCH.md (§Critical: vpp_mv invariant ALREADY HANDLED; §Pitfall 2 — do NOT expect check_dispatch to fail; SAFE-02/03 dispositions)
    - .planning/phases/80-at28c04-16-adapter-graduation/80-VALIDATION.md (Open Validation Notes — resolve the (0,6000)→(0,13000) question by direct read)
  </read_first>
  <action>
    Run the SAFE gates AFTER the Task-1 regen (never before — Pitfall 6 / Phase 77 discipline):
    (1) SAFE-02: `python3 tools/check_dispatch.py` — must exit 0 with 0 violations. RESOLVE the Open
    Validation Note IN THE SUMMARY: read check_dispatch.py:78–93 and state that `configure_eeprom28c`
    is NOT in `_DB_CHECKED_VPP_INVARIANTS` (only `configure_flash_intel` is), so the `(0,6000)`
    family-invariant value is fired ONLY via synthetic fixture and needs NO change; the research
    §Critical `(0,6000)→(0,13000)` note is STALE — record this conclusion explicitly so no downstream
    agent edits check_dispatch.py. If — contrary to this analysis — check_dispatch.py actually reports
    a NEW violation for an AT28C chip, that signals a real DB-level invariant gap: in that case (and
    only then) land the minimal invariant edit in check_dispatch.py in the SAME commit as the regen
    (semantic sync) and document the deviation. (2) SAFE-03: run the constants parity test
    (`pytest tests/test_revision_constants_parity.py -v`) and confirm green; confirm constants.py and
    firestarter.h are untouched (no lockstep bump). (3) ADPT-02 verify + wave-merge gate: run the full
    suite with coverage — `ruff check --target-version py39 . && ruff format --check --target-version
    py39 . && python3 -m pytest --cov --cov-fail-under=70`. ALL Plan-02 graduation tests must now be
    GREEN (the DB flipped to supported). If a Plan-02 test still fails due to a key/path mismatch
    surfaced only post-regen (e.g. the resolved-dict algorithm key name), make the minimal correction
    to that test here so the suite is green at wave merge — but do NOT weaken an assertion to mask a
    real graduation failure. SAFE-01: document that the source-side guard removal (named-arm deletion →
    support_status flip → chip_resolver refusal retired) is on record here, but the chips are TRULY
    graduated only after the Plan 04 bench proof (the evidence-gated FINAL step).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_dispatch.py && python3 -m pytest tests/test_revision_constants_parity.py -v && ruff check --target-version py39 . && ruff format --check --target-version py39 . && python3 -m pytest --cov --cov-fail-under=70</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/check_dispatch.py` exits 0 with 0 violations (SAFE-02); Open Validation Note resolved in the SUMMARY (configure_eeprom28c excluded from _DB_CHECKED_VPP_INVARIANTS; no check_dispatch edit; §Critical note stale)
    - `test_revision_constants_parity.py` green; constants.py + firestarter.h confirmed untouched (SAFE-03)
    - chip_resolver.py confirmed UNCHANGED (self-heal)
    - Full suite green, coverage >= 70%; ALL Plan-02 graduation tests GREEN
    - ruff check + ruff format --check (py39 target) clean across the package
    - SAFE-01 source-side guard-removal documented (truly graduated only after Plan 04 bench proof)
  </acceptance_criteria>
  <done>check_dispatch green (SAFE-02), parity green (SAFE-03), full suite + cov>=70 green with all graduation tests GREEN; Open Validation Note + SAFE-01/02/03 recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_db.py rule → generated DB → wire dispatch | removing the named arm flips support_status; if it accidentally also changed proto_id or pinout, a chip could route to the wrong handler |
| host DB → full-DB VPP dispatch | `check_dispatch.py` asserts no chip routes VPP above its family invariant after the graduation |
| host constants ↔ firmware constants | FLAG_* / protocol parity must hold; this phase touches neither (SAFE-03 trivial) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-DISPATCH | Elevation of Privilege / Damage | regenerated DB dispatch for the 9 AT28C chips | mitigate | Task 1 asserts proto_id stays 0x0D + pinout DIP24_2816 post-regen (no demotion, no remap); Site B untouched; `check_dispatch.py` (SAFE-02) re-run AFTER regen confirms 0 violations — the 0x0D handler is VPP-free so no elevated rail is reachable |
| T-80-INVARIANT | Tampering | check_dispatch _FAMILY_VPP_INVARIANTS | accept | configure_eeprom28c is excluded from `_DB_CHECKED_VPP_INVARIANTS`; the `(0,6000)` value fires only via synthetic fixture; no DB-level check fires for the real AT28C chips (75 supported 0x0D chips already pass with vpp_mv=12000) — no edit needed; conclusion recorded so it is not silently changed |
| T-80-SCOPE | Tampering | constants.py / firestarter.h parity | mitigate | Parity test confirms FLAG_*/protocol values unchanged; this phase edits neither constant, so no lockstep bump and the wire flag meaning is unchanged firmware-side |
</threat_model>

<verification>
- `_AT28C_DIP24_NAMES` removed from build_db.py; Site B untouched; ruff py39 clean
- chip_database.json regenerated (not hand-edited); 9 AT28C entries supported / 0x0D / DIP24_2816; AT28C04 mem_size=512
- chip_resolver.py UNCHANGED (self-heal)
- check_dispatch.py 0 violations post-regen (SAFE-02); Open Validation Note resolved (no edit needed)
- parity test green; constants.py + firestarter.h untouched (SAFE-03)
- full suite green + coverage >= 70%; all Plan-02 graduation tests GREEN
</verification>

<success_criteria>
- ADPT-02 (source): named arm deleted, DB regenerated, 9 chips supported via 0x0D; chip_resolver self-heals
- SAFE-02: check_dispatch.py full-DB VPP gate green post-regen, output recorded; Open Validation Note resolved
- SAFE-03: FLAG_* / protocol parity preserved, parity test green, no constant edited, no lockstep bump
- SAFE-01: source-side guard removal on record (truly graduated only after the Plan 04 bench proof)
- Full suite green, coverage >= 70%, no red across the wave boundary
</success_criteria>

<output>
Create `.planning/phases/80-at28c04-16-adapter-graduation/80-03-SUMMARY.md` when done.
</output>
