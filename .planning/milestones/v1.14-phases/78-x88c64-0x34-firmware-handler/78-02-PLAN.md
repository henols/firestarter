---
phase: 78-x88c64-0x34-firmware-handler
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - firestarter/src/proms/eeprom_x88c64.cpp
  - firestarter/include/eeprom_x88c64.h
  - firestarter/src/proms/memory.cpp
  - firestarter/test/native/avr/test_val_x88c64/test_val_x88c64.cpp
  - firestarter/test/native/avr/test_val_x88c64/host_stubs.cpp
  - firestarter_app/firestarter/data/pinouts.json
  - .planning/X88C64-FEASIBILITY.md
autonomous: true
requirements: [XIC-02, XIC-03, XIC-04]
must_haves:
  truths:
    - "CONTINGENT: this plan executes handler tasks ONLY IF Plan 01's A6 verdict is FREE-BIT-FOUND; on PCB-BLOCKED it records the deferral and STOPS with zero code changes (D-02)"
    - "PROCEED-PATH: configure_x88c64 implements protocol 0x34 (ALE-latch address phase, /WR strobe, page write <=32B, I/O6 toggle-bit poll, NO SDP unlock) in firestarter/src/proms/eeprom_x88c64.cpp + header (XIC-02, D-05)"
    - "PROCEED-PATH: the 0x34 dispatch arm is registered in memory.cpp BETWEEN the named-infeasibility block (after line 111 return) and the generic `protocol != 0` guard (line 116) — never after it (XIC-02)"
    - "PROCEED-PATH: a Tier-1 native recording-stub suite test_val_x88c64 asserts the ALE/WR register sequence and that NO VPP-enable bit (CTRL_VPP_REGULATOR_ENABLE / CTRL_VPP_P1_ENABLE) is set during configure (XIC-02, hardware-independent)"
    - "PROCEED-PATH: pio run -e leonardo Flash <= ~90% measured and recorded as a gate, D-06 optimize-first applied if over (XIC-03)"
    - "BOTH PATHS: X88C64 stays support_status protocol-not-implemented and host-refused; chip_resolver host-guard NOT removed; check_dispatch.py green; constants.py <-> firestarter.h parity green (SAFE-01/02/03, XIC-04 deferral)"
  artifacts:
    - path: "firestarter/src/proms/eeprom_x88c64.cpp"
      provides: "configure_x88c64 + x88c64_write_init + x88c64_write_execute + x88c64_wait_for_write (PROCEED-PATH only)"
      contains: "configure_x88c64"
    - path: "firestarter/include/eeprom_x88c64.h"
      provides: "void configure_x88c64(firestarter_handle_t*) declaration (PROCEED-PATH only)"
      contains: "configure_x88c64"
    - path: "firestarter/test/native/avr/test_val_x88c64/test_val_x88c64.cpp"
      provides: "Tier-1 recording-stub register-sequence + no-VPP assertions (PROCEED-PATH only)"
      contains: "0x34"
  key_links:
    - from: "firestarter/src/proms/memory.cpp configure_memory 0x34 arm"
      to: "configure_x88c64 in eeprom_x88c64.cpp"
      via: "if (handle->protocol == 0x34) before the generic protocol != 0 guard"
      pattern: "protocol == 0x34"
    - from: "test_val_x88c64.cpp"
      to: "recording-stub API (clear_bus_recording/bus_recording_count/recorded_reg/recorded_data)"
      via: "#define HOST_STUBS_RECORD_BUS in host_stubs.cpp"
      pattern: "HOST_STUBS_RECORD_BUS"
---

<objective>
CONTINGENT handler-write branch (Branch B). This plan activates the
`configure_x88c64` firmware handler, its native recording-stub test, the
`DIP24_X88C64` pinout, and the Leonardo flash gate — but ONLY IF Plan 01's A6
verdict was FREE-BIT-FOUND. Because RESEARCH is HIGH-confidence PCB-blocked
(D-02), the most likely outcome is that the FIRST task records the deferral and
STOPS this plan with zero code changes. Make the conditional explicit and safe.

Purpose: bank the hardware-independent firmware + native-test work IF (and only
if) the ALE routing is genuinely feasible, while never faking a graduation —
X88C64 stays host-refused on BOTH paths until a physical chip + adapter exist
(D-04/D-05). "No blind handler."

Output (proceed-path): `eeprom_x88c64.cpp` + header, a `memory.cpp` 0x34 dispatch
arm, a `test_val_x88c64` native suite, a `DIP24_X88C64` pinout entry, and a
recorded `pio run -e leonardo` flash measurement. Output (defer-path): a recorded
"Branch A — ALE PCB-blocked, no handler code; graduation deferred FUT-01" note
and nothing else.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/X88C64-FEASIBILITY.md
@.planning/phases/78-x88c64-0x34-firmware-handler/78-CONTEXT.md
@.planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md
@.planning/phases/78-x88c64-0x34-firmware-handler/78-01-SUMMARY.md
@firestarter/src/proms/eeprom_28c.cpp
@firestarter/include/eeprom_28c.h
@firestarter/src/proms/memory.cpp
@firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp
@firestarter/test/native/avr/test_val_flash4/host_stubs.cpp
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1 [BLOCKING]: Read the A6 verdict and branch — proceed or no-op</name>
  <files>.planning/X88C64-FEASIBILITY.md</files>
  <read_first>
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-01-SUMMARY.md (the recorded A6 verdict outcome from Plan 01)
    - .planning/X88C64-FEASIBILITY.md (the `A6 VERDICT:` line written by Plan 01)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-CONTEXT.md (D-02 deferral bar; D-05 handler-write branch conditions)
  </read_first>
  <action>
    Read the `A6 VERDICT:` line in `X88C64-FEASIBILITY.md` (cross-check the Plan 01
    SUMMARY). Branch deterministically:
    (a) If the verdict is `FREE-BIT-FOUND: 0xNN` — PROCEED. Record in this plan's
    SUMMARY which free `CTRL_*` bit was found and that Tasks 2-5 will run. Continue
    to Task 2.
    (b) If the verdict is `PCB-BLOCKED` (the RESEARCH-expected outcome) — STOP. Do
    NOT write any handler code. Append a note to `X88C64-FEASIBILITY.md` reading
    exactly `Branch A — ALE PCB-blocked, no handler code; graduation deferred
    FUT-01.` and end the plan here. Tasks 2-5 are SKIPPED. The phase still closes
    cleanly (XIC-02/03 are vacuously satisfied as "not-applicable on the PCB-blocked
    branch"; XIC-04 stays the Plan 01 deferral). Do NOT remove the host-guard, do
    NOT flip support_status, do NOT touch constants on either branch.
    Per D-02: never adopt a speculative reuse of a busy line — only an explicit
    FREE-BIT-FOUND verdict authorizes the proceed-path.
  </action>
  <verify>
    <automated>grep -Eq "A6 VERDICT: (PCB-BLOCKED|FREE-BIT-FOUND)" /workspaces/.planning/X88C64-FEASIBILITY.md && echo VERDICT_GATE_READ</automated>
  </verify>
  <acceptance_criteria>
    - The executor reads `A6 VERDICT:` and records the branch decision (PROCEED or STOP) in the plan SUMMARY.
    - On PCB-BLOCKED: `X88C64-FEASIBILITY.md` contains the literal string `Branch A — ALE PCB-blocked, no handler code; graduation deferred FUT-01.` and NO files under `firestarter/src`, `firestarter/include`, `firestarter/test`, or `firestarter_app/firestarter/data/pinouts.json` are modified.
    - On FREE-BIT-FOUND: the SUMMARY names the free `CTRL_*` bit and confirms Tasks 2-5 will run.
    - On BOTH paths: `firestarter_app/firestarter/chip_resolver.py` and `chip_database.json` X88C64P `support_status` are unchanged.
  </acceptance_criteria>
  <done>The branch decision is made and recorded; on PCB-BLOCKED the deferral note is written and the plan stops cleanly with zero code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [PROCEED-PATH ONLY]: Implement configure_x88c64 handler + header</name>
  <files>firestarter/src/proms/eeprom_x88c64.cpp, firestarter/include/eeprom_x88c64.h</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp (the closest analog — configure_eeprom28c structure, eeprom28c_write_init, eeprom28c_write_execute page loop, eeprom28c_wait_for_write toggle-bit poll, PAGE_SIZE 64, the SDP-disable sequence to OMIT)
    - firestarter/include/eeprom_28c.h (the header shape to mirror for eeprom_x88c64.h)
    - .planning/X88C64-FEASIBILITY.md (§3 write protocol — ALE HIGH->LOW latch, /WR strobe, page <=32B, I/O6 toggle poll, WC LOW during write, NO STORE/RECALL; the A6 free-bit value to use for ALE)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"eeprom_28c.cpp — Closest Analog Analysis" key-differences table; §"X88C64 Write Protocol — Mapped to Firmware Sequencing" 5-phase sequence; §"Common Pitfalls" Pitfall 3 I/O6 two-read toggle check)
    - firestarter/include/rurp_pinout.h (the free CTRL_* bit identified by Plan 01 for ALE; the CTRL_READ_WRITE bit for /WR direction)
    - firestarter/include/memory_utils.h and firestarter/include/flash_utils.h (shareable helpers per D-06 — mem_util_set_address, page-boundary math, mem_util_blank_check)
  </read_first>
  <behavior>
    - configure_x88c64(handle): on CMD_WRITE sets firestarter_operation_init = x88c64_write_init and firestarter_operation_main = x88c64_write_execute; on CMD_BLANK_CHECK sets firestarter_operation_main = mem_util_blank_check (shared); pulse_delay = 0; does NOT error (response_code != RESPONSE_CODE_ERROR).
    - No CONTROL_REGISTER write during configure sets CTRL_VPP_REGULATOR_ENABLE (0x80) or CTRL_VPP_P1_ENABLE (0x08) — X88C64 is 5V single-supply.
    - x88c64_write_execute walks data_buffer in <=32-byte pages: for each byte, ALE-latch the address (upper A8-A12 on dedicated pins, lower A0-A7 on A/D bus, pulse ALE HIGH->LOW), place data on the A/D bus, strobe /WR LOW->HIGH; at each page boundary ((address+1) % 32 == 0) or last byte, call x88c64_wait_for_write.
    - x88c64_wait_for_write polls completion via I/O6 TOGGLE (Pitfall 3): read twice, complete when (read1 ^ read2) & 0x40 == 0; ~20ms ceiling (2000 * 10us) mirroring eeprom28c_wait_for_write; NO `observed == expected` data-match.
    - NO SDP unlock sequence (X88C64 has none).
  </behavior>
  <action>
    Create `firestarter/include/eeprom_x88c64.h` mirroring `eeprom_28c.h` (extern "C"
    guard, `void configure_x88c64(firestarter_handle_t* handle);`). Create
    `firestarter/src/proms/eeprom_x88c64.cpp` modeled on `eeprom_28c.cpp`:
    `configure_x88c64`, `x88c64_write_init`, `x88c64_write_execute`,
    static `x88c64_wait_for_write`. Use `#define PAGE_SIZE 32`. Drive ALE via the
    free `CTRL_*` bit identified by Plan 01 (HIGH then LOW to latch the address).
    Implement the I/O6 two-read toggle poll per Pitfall 3 with a code comment citing
    the X88C64 datasheet. Reuse `mem_util_set_address` for upper address pins and
    `mem_util_blank_check` for CMD_BLANK_CHECK; reuse the eeprom_28c page-boundary
    pattern (PAGE_SIZE 32). Do NOT set any VPP bit. Do NOT implement STORE/RECALL.
    Do NOT add an A9-12V chip-ID routine (DB entry has chip_id_check false — D-06
    flash saver). If a new protocol/FLAG constant is introduced, mirror it lockstep
    in `firestarter/include/firestarter.h` AND `firestarter_app/firestarter/constants.py`
    and keep parity tests green (SAFE-03) — the 0x34 literal in the dispatch arm
    needs no new constant.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run -e native 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/include/eeprom_x88c64.h` declares `void configure_x88c64(firestarter_handle_t*)` inside an extern "C" guard.
    - `firestarter/src/proms/eeprom_x88c64.cpp` defines `configure_x88c64`, `x88c64_write_init`, `x88c64_write_execute`, and a static `x88c64_wait_for_write` with `#define PAGE_SIZE 32`.
    - The wait function uses a two-read I/O6 toggle check `(read1 ^ read2) & 0x40 == 0`, NOT `observed == expected` (source inspection).
    - No `CTRL_VPP_REGULATOR_ENABLE` or `CTRL_VPP_P1_ENABLE` reference appears in the configure path.
    - `pio run -e native` compiles the new TU without errors.
    - If any FLAG_*/protocol constant was added: `constants.py` and `firestarter.h` changed in lockstep and `pytest` parity test green.
  </acceptance_criteria>
  <done>configure_x88c64 and its write/poll functions exist, compile under [env:native], use the I/O6 toggle poll, set no VPP bits, and implement no STORE/RECALL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 [PROCEED-PATH ONLY]: Register the 0x34 dispatch arm + add the Tier-1 recording-stub test suite</name>
  <files>firestarter/src/proms/memory.cpp, firestarter/test/native/avr/test_val_x88c64/test_val_x88c64.cpp, firestarter/test/native/avr/test_val_x88c64/host_stubs.cpp</files>
  <read_first>
    - firestarter/src/proms/memory.cpp (lines 99-119 — insert the 0x34 arm AFTER line 111 `return;` closing the named-infeasibility block and BEFORE the line 113 generic-guard comment / line 116 `if (handle->protocol != 0)`; add the `#include "eeprom_x88c64.h"`)
    - firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (the recording-stub exemplar: make_handle, configure_memory assertions, assert_no_vpp_in_recording over CONTROL_REGISTER writes, Unity RUN_TEST structure)
    - firestarter/test/native/avr/test_val_flash4/host_stubs.cpp (the `#define HOST_STUBS_RECORD_BUS` + `#include "../_shared/host_stubs_common.inc"` activation pattern)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (the recording API: clear_bus_recording, bus_recording_count, recorded_reg, recorded_data; note recorded_data is truncated to uint8_t — only 8-bit-fit VPP bits are checkable)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"Tier-1 Native Recording-Stub Test Pattern" — exact assertions; §"Dispatch Insertion Point"; Pitfall 1 dead-code-after-guard; Pitfall 5 do NOT add 0x34 to check_dispatch.py KNOWN_PROTOCOLS)
  </read_first>
  <behavior>
    - configure_memory with protocol 0x34, CMD_WRITE routes to configure_x88c64 and does NOT set RESPONSE_CODE_ERROR (an unknown protocol would have shown ERROR before this arm).
    - During the configure phase, no CONTROL_REGISTER recording entry has CTRL_VPP_REGULATOR_ENABLE (0x80) or CTRL_VPP_P1_ENABLE (0x08) set (assert_no_vpp pattern).
    - After firestarter_operation_init + a single-byte firestarter_operation_main with cleared recording, the recorded register sequence contains the ALE latch strobe(s) and the /WR strobe in the expected order (LSB/MSB/CONTROL address writes, then data-bus write, then write strobe).
  </behavior>
  <action>
    In `firestarter/src/proms/memory.cpp`: add `#include "eeprom_x88c64.h"` with the
    other proms includes, and insert the dispatch arm
    `if (handle->protocol == 0x34) { configure_x88c64(handle); return; }`
    immediately after the named-infeasibility block's `return;` (line 111) and BEFORE
    the generic `if (handle->protocol != 0)` guard (line 116). Per Pitfall 1, placing
    it after the guard makes it dead code — verify it sits before. Create
    `firestarter/test/native/avr/test_val_x88c64/host_stubs.cpp` with
    `#define HOST_STUBS_RECORD_BUS` then `#include "../_shared/host_stubs_common.inc"`
    (mirror test_val_flash4). Create
    `firestarter/test/native/avr/test_val_x88c64/test_val_x88c64.cpp` modeled on
    `test_val_flash4.cpp`: (1) a dispatch test asserting protocol 0x34 CMD_WRITE does
    NOT yield RESPONSE_CODE_ERROR; (2) a no-VPP test over CONTROL_REGISTER recordings
    using TEST_ASSERT_BITS_LOW for CTRL_VPP_REGULATOR_ENABLE and CTRL_VPP_P1_ENABLE;
    (3) a write-sequence test that clears recording after init, drives one byte, and
    asserts the ALE/WR strobe order. The `[env:native]` config auto-discovers the new
    suite — no platformio.ini change. Do NOT add 0x34 to check_dispatch.py
    KNOWN_PROTOCOLS (Pitfall 5).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_val_x88c64*" 2>&1 | tail -8 && pio test -e native -f "*test_dispatch*" 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "protocol == 0x34" firestarter/src/proms/memory.cpp` returns a line numerically BEFORE the line matching `if (handle->protocol != 0)` (arm precedes the generic guard — Pitfall 1).
    - `firestarter/src/proms/memory.cpp` includes `eeprom_x88c64.h`.
    - `pio test -e native -f "*test_val_x88c64*"` exits 0 with the dispatch, no-VPP, and write-sequence tests passing.
    - `pio test -e native -f "*test_dispatch*"` still exits 0 (no regression).
    - `firestarter_app/tools/check_dispatch.py` KNOWN_PROTOCOLS does NOT include 0x34 (unchanged).
  </acceptance_criteria>
  <done>The 0x34 arm routes to configure_x88c64 before the generic guard; the test_val_x88c64 native suite passes (dispatch + no-VPP + ALE/WR order); test_dispatch unaffected.</done>
</task>

<task type="auto">
  <name>Task 4 [PROCEED-PATH ONLY]: Add the DIP24_X88C64 pinout entry (A7)</name>
  <files>firestarter_app/firestarter/data/pinouts.json</files>
  <read_first>
    - firestarter_app/firestarter/data/pinouts.json (the existing DIP24_6116 entry — structure of vcc-pin/gnd-pin/ce-pin/oe-pin/rw-pin/address-bus-pins/data-bus-pins; mirror the JSON shape)
    - .planning/X88C64-FEASIBILITY.md (§2 DIP24 pin table — authoritative pin assignments: VCC=24, VSS=12, /CE=16, /RD=18, A8-A12=21/20/19/17/2, A/D0-7=7-15, WC=5, ALE=22, /WR=23)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"Pinout Strategy: DIP24_6116 vs DIP24_X88C64 (A7 Research)" — recommended DIP24_X88C64 pin map; A7 = dedicated entry chosen because /CE, /RD, WC are structurally incompatible with DIP24_6116)
    - firestarter_app/firestarter/data/chip_database.json (the X88C64P entry — currently pinout DIP24_6116; do NOT change support_status; this task only adds the new pinout to pinouts.json, NOT re-points the chip until graduation per D-04/D-05)
  </read_first>
  <action>
    A7 DECISION (planner's call, confirmed): create a DEDICATED `DIP24_X88C64` pinout
    entry rather than reusing `DIP24_6116` — RESEARCH confirms /CE (16 vs 18), /RD
    (18 vs 20), and WC (pin 5, unrepresented in 6116) are structurally incompatible.
    Add a `DIP24_X88C64` object to `firestarter_app/firestarter/data/pinouts.json`
    mirroring the existing entry shape with: vcc-pin [24], gnd-pin [12], ce-pin [16],
    oe-pin [18], the dedicated upper-address pins [21,20,19,17,2] (A8,A9,A11,A10,A12),
    the multiplexed A/D bus [7,8,9,10,11,13,14,15], rw-pin/WC [5], and document
    ALE [22] and /WR [23] (the custom 0x34 handler sequences the multiplexed A/D
    bus in firmware regardless of static pin mapping). Do NOT re-point the X88C64P
    `chip_database.json` entry to the new pinout and do NOT change its
    `support_status` — that is the graduation step, hardware-blocked this phase
    (D-04/D-05). Validate the host change against CI: `ruff check --target-version
    py39` and confirm JSON is well-formed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -c "import json,glob; p=glob.glob('firestarter/**/pinouts.json',recursive=True)[0]; d=json.load(open(p)); assert 'DIP24_X88C64' in d, 'missing DIP24_X88C64'; print('PINOUT_OK',p)" && python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pinouts.json` parses as valid JSON and contains a `DIP24_X88C64` key (the python assert prints `PINOUT_OK`).
    - The new entry maps vcc-pin [24], gnd-pin [12], ce-pin [16], oe-pin [18], rw-pin/WC [5] per the §2 pin table.
    - The X88C64P entry in `chip_database.json` is UNCHANGED — still `support_status: protocol-not-implemented`, still pinout `DIP24_6116` (graduation deferred — D-04/D-05).
    - `pytest` from `firestarter_app/` exits 0; `ruff check --target-version py39` clean.
  </acceptance_criteria>
  <done>A dedicated DIP24_X88C64 pinout entry exists in pinouts.json; the X88C64P DB entry and its support_status are untouched; host tests + ruff (py39) green.</done>
</task>

<task type="auto">
  <name>Task 5 [PROCEED-PATH ONLY]: Measure the Leonardo flash gate and record the XIC-04 deferral</name>
  <files>.planning/X88C64-FEASIBILITY.md</files>
  <read_first>
    - firestarter/platformio.ini (§[env:leonardo] lines 57-67 — the build target; 28672-byte flash)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"Flash Budget" — baseline 89.5% / 25654 of 28672; ~90% gate = 25805 bytes; D-06 optimize-first levers: share helpers, PROGMEM, no chip-ID routine, dead-code trim)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-CONTEXT.md (D-06 optimize-first-then-report; SAFE-01/02/03)
    - .planning/X88C64-FEASIBILITY.md (the graduation-pending-hardware note from Plan 01 — extend with the banked-handler status)
  </read_first>
  <action>
    Run `pio run -e leonardo` and read the `Flash: [...] N%` line. Record the
    measured percentage and byte count in the plan SUMMARY and in
    `X88C64-FEASIBILITY.md` as the XIC-03 gate result. If Flash > ~90% (25805 bytes),
    apply D-06 optimize-first levers in order — share eeprom_28c page-boundary
    helpers, move any new error strings to PROGMEM, confirm no A9-12V chip-ID routine
    was added, dead-code trim — then re-measure. Only if still over after a
    reasonable optimization pass, STOP and escalate the optimize-vs-accept-vs-defer
    call to the operator (do NOT silently exceed the gate). Then record the XIC-04
    closeout: extend the `X88C64-FEASIBILITY.md` graduation-pending-hardware note to
    state the firmware handler + native test are BANKED but X88C64 stays
    `protocol-not-implemented` and host-refused (D-04/D-05) — the SC#4 graduation
    flip (support_status -> supported + chip_resolver host-guard removal) waits for a
    physical X88C64P + DIP24->DIP32 adapter + an N>=5 Leonardo write/read-back
    SHA-match (FUT-01). Confirm SAFE-02 (`python tools/check_dispatch.py` green) and
    SAFE-03 (constants.py <-> firestarter.h parity green) hold after all changes.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run -e leonardo 2>&1 | grep -E "Flash:" && cd /workspaces/firestarter_app && python tools/check_dispatch.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pio run -e leonardo` succeeds and the `Flash:` line is recorded in the SUMMARY and X88C64-FEASIBILITY.md; the percentage is <= ~90% OR an operator escalation is documented (XIC-03).
    - `X88C64-FEASIBILITY.md` records that the handler is banked but X88C64 stays `protocol-not-implemented`/host-refused, with the SC#4 graduation tracked under FUT-01 (XIC-04 deferral-with-evidence).
    - `python tools/check_dispatch.py` from `firestarter_app/` exits 0 / prints its PASS line (SAFE-02).
    - If a FLAG_*/protocol constant was touched: `constants.py` <-> `firestarter.h` parity test green (SAFE-03).
    - `chip_resolver.resolve_chip` host-guard is NOT removed; `chip_database.json` X88C64P `support_status` unchanged (SAFE-01).
  </acceptance_criteria>
  <done>The Leonardo flash gate is measured and recorded <= ~90% (or escalated per D-06); XIC-04 is recorded as hardware-deferred with the banked-handler status; check_dispatch + parity green; host-guard intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI -> chip dispatch | `resolve_chip` host-guard refuses `protocol-not-implemented` chips before any wire command — kept active on BOTH branches this phase |
| firmware dispatch -> hardware bus | `configure_memory` 0x34 arm routes to `configure_x88c64` (proceed-path) which sets NO VPP bits; otherwise the generic guard returns 0xBB |
| native test stub -> recording buffer | recording-stub asserts register-write sequence with zero real hardware (XIC-02 hardware-independent proof) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering/Damage | 0x34 reaching a 12V VPP handler | mitigate | 0x34 arm routes to `configure_x88c64` (5V, no VPP bit) inserted BEFORE the generic guard (Pitfall 1); Tier-1 no-VPP recording-stub test (Task 3) asserts CTRL_VPP_REGULATOR_ENABLE/CTRL_VPP_P1_ENABLE never set; `check_dispatch.py` SAFE-02 gate green |
| T-78-02 | Tampering | Blind handler writing to unverified hardware | mitigate | D-04/D-05: no graduation without bench SHA-match; X88C64 stays host-refused on BOTH branches; `chip_resolver` host-guard NOT removed; `support_status` unchanged (Task 4/5 acceptance) |
| T-78-03 | Tampering | Control-register bit pollution (ALE via a busy bit) | mitigate | D-02 deferral bar enforced at Task 1 [BLOCKING] gate: only an explicit FREE-BIT-FOUND verdict authorizes driving ALE; speculative reuse of a busy line is prohibited and forces the no-op defer-path |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | No package installs introduced; RESEARCH §Standard Stack confirms no new third-party deps (PlatformIO/Unity/ArduinoFake/pytest/ruff all pre-installed); `pip install -e '.[test]'` only restores the existing toolchain |
</threat_model>

<verification>
- Defer-path: `grep -q "Branch A — ALE PCB-blocked" .planning/X88C64-FEASIBILITY.md` and no firmware/pinout files changed.
- Proceed-path: `pio test -e native -f "*test_val_x88c64*"` and `pio test -e native -f "*test_dispatch*"` both exit 0.
- Proceed-path: `pio run -e leonardo` Flash% <= ~90% (or operator escalation recorded).
- Proceed-path: 0x34 arm precedes the generic `protocol != 0` guard in memory.cpp.
- BOTH paths: `python tools/check_dispatch.py` green; `chip_resolver` host-guard present; X88C64P `support_status` = `protocol-not-implemented`; constants parity green.
</verification>

<success_criteria>
- XIC-02 satisfied (proceed-path): configure_x88c64 implements 0x34 (ALE/WR, page <=32B, I/O6 toggle poll, no SDP, no STORE/RECALL) registered before the generic guard; native recording-stub test green.
- XIC-03 satisfied (proceed-path): Leonardo flash measured <= ~90% and recorded (D-06 optimize-first if needed).
- XIC-04 satisfied (both paths): graduation recorded as hardware-deferred (FUT-01); chip stays refused.
- SAFE-01/02/03 hold: host-guard-removal-last (not performed), check_dispatch green, constants parity green.
- Defer-path (RESEARCH-expected): clean no-op with the Branch A note and zero code changes.
</success_criteria>

<output>
Create `.planning/phases/78-x88c64-0x34-firmware-handler/78-02-SUMMARY.md` when done.
State explicitly which branch executed (PROCEED or DEFER) and the A6 verdict that
drove it. On the proceed-path, record the measured Leonardo Flash%.
</output>
