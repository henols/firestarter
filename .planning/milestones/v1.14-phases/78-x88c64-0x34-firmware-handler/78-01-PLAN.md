---
phase: 78-x88c64-0x34-firmware-handler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/X88C64-FEASIBILITY.md
autonomous: true
requirements: [XIC-01, XIC-04]
must_haves:
  truths:
    - "The A6 ALE-routing verdict (PCB-blocked OR free-bit-found) is recorded in X88C64-FEASIBILITY.md with concrete line-cited trace evidence from rurp_pinout.h, rurp_register_utils.h, and rurp_shield.h (XIC-01)"
    - "A FUT-01 future-unblock spec is written stating exactly what a future milestone needs to drive ALE — PCB mod / new shield-rev control bit / dedicated ALE GPIO — plus a TODO to check the Leonardo ATmega32u4 GPIO-to-socket map (D-03)"
    - "The SC#4 graduation is recorded as hardware-deferred (FUT-01) because there is no physical X88C64P chip or DIP24->DIP32 adapter; X88C64 stays protocol-not-implemented and host-refused (D-04, XIC-04)"
    - "SAFE invariants confirmed intact: chip_resolver host-guard NOT removed; check_dispatch.py green; constants.py <-> firestarter.h parity untouched (SAFE-01/02/03)"
  artifacts:
    - path: ".planning/X88C64-FEASIBILITY.md"
      provides: "A6 verdict section + FUT-01 future-unblock spec + graduation-pending-hardware note"
      contains: "A6 VERDICT:"
  key_links:
    - from: ".planning/X88C64-FEASIBILITY.md A6 verdict"
      to: "firestarter/include/rurp_pinout.h CTRL_* bit map"
      via: "line-cited trace evidence (every 0x01..0x80 bit accounted for; 0x100 needs 9-bit register)"
      pattern: "rurp_pinout.h"
---

<objective>
Resolve the gating ALE-routing question (Assumption A6, XIC-01) by a thorough
software/schematic trace by Claude (D-01) — NOT an operator bench trace — of the
RURP control-register bit map and the 74HC573 latch-strobe architecture. Decide:
is there a genuinely free `CTRL_*` bit, or a zero-risk reuse of a provably-idle
control line, that could drive the 8051 ALE? Record the verdict with line-cited
evidence in `X88C64-FEASIBILITY.md`, write the FUT-01 future-unblock spec (D-03),
and record the SC#4 graduation as hardware-deferred (D-04, XIC-04). This is the
FIRST plan and it gates everything downstream.

Purpose: "No blind handler." The trace decides whether Plan 02 (handler write)
activates at all. A documented PCB-block deferral is a clean, acceptable
completion — not a failure. RESEARCH expects PCB-BLOCKED at HIGH confidence.

Output: An updated `.planning/X88C64-FEASIBILITY.md` carrying the A6 verdict, the
FUT-01 spec, and the graduation-pending-hardware note. ZERO code changes on the
expected (PCB-blocked) branch.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/X88C64-FEASIBILITY.md
@.planning/phases/78-x88c64-0x34-firmware-handler/78-CONTEXT.md
@.planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace the control-register allocation and latch-strobe architecture; record the A6 verdict</name>
  <files>.planning/X88C64-FEASIBILITY.md</files>
  <read_first>
    - firestarter/include/rurp_pinout.h (lines 71-129 — the CTRL_* bit map, both the `#ifndef HARDWARE_REVISION` 8-bit layout and the `#else` wide layout, plus the per-rev REV1/REV2 variants and the `CTRL_ADDRESS_LINE_13 = 0x20` reserved comment)
    - firestarter/include/rurp_register_utils.h (lines 24-88 — `rurp_internal_write_to_register`: how data goes onto the shared 8-bit data bus via `rurp_write_data_buffer(data)` then a single strobe pulse; confirm `data` is uint8_t so 0x100 cannot be transmitted)
    - firestarter/include/rurp_shield.h (lines 53-57 — the LEAST_SIGNIFICANT_BYTE / MOST_SIGNIFICANT_BYTE / OUTPUT_ENABLE / CONTROL_REGISTER / CHIP_ENABLE strobe constants; confirm no free/unconnected strobe exists for an ALE line)
    - .planning/X88C64-FEASIBILITY.md (§2 DIP24 pin table — ALE is pin 22; §4 RURP feasibility; §6 Assumption A6 LOW confidence — the row this task closes)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"ALE-Routing Trace: A6 Verdict" — the source-verified trace tables; reuse, do not re-derive from scratch)
  </read_first>
  <action>
    Trace every CTRL_* bit position in `rurp_pinout.h` and classify each as
    allocated or free, for BOTH the 8-bit (`#ifndef HARDWARE_REVISION`) layout and
    the wide (`#else`) layout (production builds use `-D HARDWARE_REVISION`, per
    platformio.ini). For each bit 0x01..0x80, name its assigned CTRL_* function and
    cite the rurp_pinout.h line. Establish whether the `0x100`
    `CTRL_VPP_VPE_DROP_ENABLE` bit (wide layout only) is transmissible — confirm
    from `rurp_register_utils.h` that `rurp_internal_write_to_register` pushes data
    through `rurp_write_data_buffer(data)` where `data` is a `uint8_t`, so any value
    > 0xFF is truncated and the 0x100 bit cannot physically reach the 74HC573.
    Cross-check the 74HC573 strobe inventory in `rurp_shield.h` — confirm there is
    NO unconnected strobe line available to drive ALE without a PCB change. Apply
    the D-02 deferral bar: a verdict of FEASIBLE requires EITHER a genuinely free
    `CTRL_*` bit OR a zero-risk reuse of a control line provably idle during the
    X88C64 write window. Per D-02, speculative creative multiplexing of a busy line
    (e.g. pulsing `CTRL_VPP_REGULATOR_ENABLE` for a 5V chip, which would produce an
    undamped VPP spike at the socket) is PROHIBITED and does NOT count as feasible.
    Write a new "## A6 ALE-Routing Verdict (Phase 78)" section into
    `X88C64-FEASIBILITY.md` whose first content line begins with the exact string
    `A6 VERDICT:` followed by either `PCB-BLOCKED` or `FREE-BIT-FOUND: 0xNN`. Include
    the line-cited evidence table. The expected landing (RESEARCH HIGH confidence) is
    PCB-BLOCKED. Do NOT presume the verdict in the prose — let the trace decide; if a
    genuinely free bit IS found, record it as FREE-BIT-FOUND with the bit value and
    the trace justification.
  </action>
  <verify>
    <automated>grep -q "A6 VERDICT:" /workspaces/.planning/X88C64-FEASIBILITY.md && echo VERDICT_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `X88C64-FEASIBILITY.md` contains a line beginning with `A6 VERDICT:` followed by `PCB-BLOCKED` or `FREE-BIT-FOUND: 0x` (grep exits 0).
    - The verdict section cites specific `rurp_pinout.h` line numbers for the CTRL_* bit allocation and explicitly addresses the 0x100 / uint8_t-truncation argument with a `rurp_register_utils.h` citation.
    - The verdict section cites `rurp_shield.h` strobe constants confirming no free ALE strobe.
    - If PCB-BLOCKED: the section explicitly states that no speculative reuse of a busy bit was adopted (D-02 bar honored).
    - No source files under `firestarter/` are modified by this task (`git -C /workspaces/firestarter status --porcelain` shows no changes).
  </acceptance_criteria>
  <done>The A6 verdict is recorded in X88C64-FEASIBILITY.md with concrete line-cited trace evidence from all three header files; no firmware code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the FUT-01 future-unblock spec and the SC#4 graduation-pending-hardware note</name>
  <files>.planning/X88C64-FEASIBILITY.md</files>
  <read_first>
    - .planning/X88C64-FEASIBILITY.md (the A6 verdict section just written in Task 1; §5 "What is Needed for a Future Handler")
    - .planning/REQUIREMENTS.md (FUT-01 — X88C64 graduation if PCB-blocked; XIC-04 — graduation + N>=5 SHA-match; §"Out of Scope")
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-RESEARCH.md (§"Open Questions" item 2 — Leonardo ATmega32u4 GPIO-to-socket map TODO; §"Hardware-Gated Validation (XIC-04)" — what the bench needs)
    - .planning/phases/78-x88c64-0x34-firmware-handler/78-CONTEXT.md (D-03 deferral deliverable; D-04 no physical chip/adapter)
  </read_first>
  <action>
    Append to the A6 verdict section (or a directly-following subsection) of
    `X88C64-FEASIBILITY.md`: (a) a FUT-01 future-unblock spec per D-03 enumerating
    what a future milestone needs to drive ALE — ONE of: a new shield revision
    (>= Rev 2.4) adding a 9th control bit via a second 74HC573 control latch + a
    dedicated Arduino GPIO strobe; a dedicated Arduino GPIO routed directly to the
    X88C64 ALE pin (DIP socket pin 22) bypassing the 74HC573; or a documented (but
    NOT recommended) idle-window analysis — and per RESEARCH Open-Question 2, a
    `TODO:` line to check the Leonardo ATmega32u4 GPIO-to-RURP-socket map for a
    direct-drive ALE path. (b) a "Graduation pending hardware" note recording that
    SC#4 / XIC-04 is hardware-blocked this phase (D-04: no physical X88C64P chip and
    no DIP24->DIP32 adapter), that X88C64 therefore stays
    `support_status: protocol-not-implemented` and host-refused, and that the
    eventual bench graduation (N>=5 write + read-back SHA-match + non-vacuous
    negative control, Leonardo only, chip-OUT VPP dry-run / ASK shield rev /
    `r1 ~= 270000` / verify `controller:` port identity first) is tracked under
    FUT-01. Confirm in the note that the SAFE invariants hold trivially on this
    plan: the `chip_resolver.resolve_chip` host-guard is NOT removed, no DB entry
    changes, and no `constants.py`/`firestarter.h` parity is touched (SAFE-01/02/03).
  </action>
  <verify>
    <automated>grep -q "FUT-01" /workspaces/.planning/X88C64-FEASIBILITY.md && grep -qi "graduation pending hardware" /workspaces/.planning/X88C64-FEASIBILITY.md && echo FUT01_AND_GRADUATION_NOTE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `X88C64-FEASIBILITY.md` contains a `FUT-01` future-unblock spec listing at least the new-shield-rev option and the dedicated-GPIO option (grep `FUT-01` exits 0).
    - The spec contains a `TODO:` line referencing the Leonardo ATmega32u4 GPIO-to-socket map (RESEARCH Open-Question 2).
    - `X88C64-FEASIBILITY.md` contains a "graduation pending hardware" note stating X88C64 stays `protocol-not-implemented` and host-refused, citing D-04 (grep case-insensitive exits 0).
    - The note explicitly states the `chip_resolver` host-guard is NOT removed this phase (SAFE-01).
    - No files outside `.planning/X88C64-FEASIBILITY.md` are modified (no DB, constants, or firmware changes).
  </acceptance_criteria>
  <done>FUT-01 spec and graduation-pending-hardware note are recorded; XIC-04 is satisfied as deferral-with-evidence; SAFE invariants confirmed intact with no code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI -> chip dispatch | `resolve_chip` host-guard refuses `protocol-not-implemented` chips before any wire command is built — the wrong-VPP-to-wrong-pin damage barrier |
| firmware dispatch -> hardware bus | `configure_memory` routes a non-zero unknown protocol to `configure_not_implemented` (0xBB) with zero hardware side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering/Damage | 0x34 routing reaching a 12V VPP handler | mitigate | X88C64 stays `protocol-not-implemented`; no dispatch arm added in this plan; `check_dispatch.py` SAFE-02 gate remains green (no DB change) |
| T-78-02 | Tampering | Blind handler writing to unverified hardware | mitigate | No handler code written this plan; graduation recorded as hardware-deferred (D-04); chip stays host-refused; host-guard NOT removed (SAFE-01) |
| T-78-03 | Tampering | Control-register bit pollution (ALE via a busy bit) | mitigate | D-02 deferral bar enforced in Task 1: speculative reuse of a busy line (e.g. `CTRL_VPP_REGULATOR_ENABLE` undamped-spike path) is prohibited; verdict either finds a genuinely free bit or records PCB-BLOCKED |
| T-78-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; documentation-only changes to a `.planning` artifact |
</threat_model>

<verification>
- `grep "A6 VERDICT:" .planning/X88C64-FEASIBILITY.md` returns a verdict line.
- `grep "FUT-01" .planning/X88C64-FEASIBILITY.md` and `grep -i "graduation pending hardware" .planning/X88C64-FEASIBILITY.md` both return matches.
- `git -C /workspaces/firestarter status --porcelain` shows no firmware changes.
- `git -C /workspaces/firestarter_app status --porcelain` shows no host changes (chip_database.json, constants.py, chip_resolver.py all untouched).
- Sanity: the verdict cites line numbers from rurp_pinout.h, rurp_register_utils.h, and rurp_shield.h.
</verification>

<success_criteria>
- XIC-01 satisfied: A6 verdict recorded with line-cited trace evidence (PCB-BLOCKED expected, FREE-BIT-FOUND permitted).
- XIC-04 satisfied as deferral-with-evidence: graduation recorded as hardware-blocked (D-04/FUT-01); chip stays refused.
- SAFE-01/02/03 hold trivially: host-guard intact, check_dispatch unchanged, no constant parity touched.
- ZERO code changes on the expected PCB-blocked branch.
</success_criteria>

<output>
Create `.planning/phases/78-x88c64-0x34-firmware-handler/78-01-SUMMARY.md` when done.
Record the A6 verdict outcome (PCB-BLOCKED or FREE-BIT-FOUND) explicitly in the
SUMMARY — Plan 02's leading gate task reads this verdict to decide whether to
proceed or no-op.
</output>
