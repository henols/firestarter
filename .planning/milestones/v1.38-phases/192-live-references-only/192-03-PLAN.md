---
phase: 192-live-references-only
plan: 03
type: execute
wave: 2
depends_on: [192-02]
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt
autonomous: true
requirements: [SWEEP-01, SWEEP-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/codebase/CONVENTIONS.md`, `TESTING.md` and `CONCERNS.md` describe the meta repository as it is today and name no workflow file and no wiki-tooling directory that does not exist, closing the two documents D-03 found stale that neither SWEEP-03 nor the ROADMAP's criterion 4 had named."
    - "All seven `.planning/codebase/` documents read 0 under the boundary-aware bare-slug sweep and 0 under the retired-CI token set, each reading paired in the same transcript with a control over the same file that returns non-zero — so criterion 1 for these documents, criterion 3 for `STACK.md` and criterion 4 for the four named documents are proved against the post-remap tree rather than against the mapper's intent (D-06)."
    - "The scope of SWEEP-03 as amended and the scope actually swept are the same seven files, and that set is exactly what `.planning/codebase/` contains: no document is claimed by two scopes and none falls outside. The original requirement named one document, the ROADMAP's criterion 4 named four more, and `CONCERNS.md` and `CONVENTIONS.md` were named by neither; their union is the whole directory."
    - "No regenerated document came back empty or short of its preserved history: each of the seven is longer than 20 lines, and every preserved-history anchor captured before the remap is still present in the document it came from."
    - "The gate's verdict is order-independent: it is a per-document count taken with an identical command form for each of the seven, and the transcript lists the documents in a fixed sorted order so two runs are byte-comparable."
    - "The preserved 2026-05-08 host-app sections of `CONVENTIONS.md` and `TESTING.md` and the `Submodule Findings (2026-05-08)` section of `CONCERNS.md` survive the remap with their wording and their not-re-verified caveats intact (D-05)."
    - "Each of the three documents this plan owns carries a provenance stamp true of the run that wrote it: `last_mapped_commit` and `last_mapped_at` from this remap, and `mapped_paths` naming D-04's scope verbatim (D-03)."
    - "`.planning/milestones/` is untouched by this plan: a scoped `git status --porcelain` over that path prints no file after every task."
  artifacts:
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Meta-repository coding and authoring conventions, remapped"
      contains: "mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md"
    - path: ".planning/codebase/TESTING.md"
      provides: "What is tested in the mapped scope, remapped, with the sole-workflow claim gone"
      contains: "mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Tech debt and fragile areas for the mapped scope, remapped, with the six retired-CI findings gone"
      contains: "mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md"
    - path: ".planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt"
      provides: "D-06's post-remap gate: a per-document bare-slug reading, retired-CI reading, control reading and preserved-anchor reading for all seven documents, and the criterion-3 and criterion-4 verdicts derived from them"
      contains: "documents_gated: 7"
  key_links:
    - from: ".planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt"
      to: ".planning/codebase/CONVENTIONS.md, TESTING.md, CONCERNS.md"
      via: "preserved-history anchors captured pre-remap and re-asserted per document after it"
      pattern: "2026-05-08"
    - from: ".planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt"
      to: "the ROADMAP's success criteria 1, 3 and 4"
      via: "per-document readings named against the criterion each one settles"
      pattern: "criterion_"
  prohibitions:
    - requirement_id: SWEEP-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT let a regenerated document assert currency it does not have. A preserved section keeps its not-re-verified caveat, and no section may claim the submodule working trees or `tools/` were scanned when the path scope excluded them."
    - requirement_id: SWEEP-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT accept the mapper's output on intent. Every criterion this plan settles is settled by a reading taken against the post-remap files, and every zero reading is paired with a control over the same file that returns non-zero."
    - requirement_id: SWEEP-01
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT transcribe a credential, token or key value read from `.devcontainer/`, `.vscode/` or `.claude/` into a regenerated document. Name the variable, never its value."
    - requirement_id: SWEEP-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit, repair or rewrite anything under `.planning/milestones/`, and MUST NOT run any tool that rewrites archived records, in order to make a reading come out clean."
---

<objective>
Finish the remap with the three documents no requirement had named, then gate all seven against the
post-remap tree.

`CONCERNS.md` carried nine retired-CI references and `CONVENTIONS.md` two; neither was named by
SWEEP-03 nor by the ROADMAP's criterion 4, and shipping a known-stale remainder is what D-03 declined.
This plan remaps them alongside `TESTING.md`, and then runs D-06's gate: the boundary-aware sweep and
the retired-CI token check over all seven documents, each zero paired with a control, because the
mapper's intent is not evidence.

Purpose: ROADMAP criteria 1, 3 and 4 for `.planning/codebase/`, proved rather than assumed.
Output: `CONVENTIONS.md`, `TESTING.md` and `CONCERNS.md` regenerated, stamped and swept;
`evidence/192-03-codebase-gate.txt` written.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-live-references-only/192-CONTEXT.md
@.planning/phases/192-live-references-only/192-PATTERNS.md
@.planning/phases/192-live-references-only/192-SCOUT.md
@.planning/phases/192-live-references-only/192-02-SUMMARY.md
@.claude/gsd-core/workflows/map-codebase.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
command whose evidence depends on seeing every file uses the absolute `/usr/bin/grep`, and every file
set is enumerated with `git ls-files`, never with a shell glob.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 carries a trailing
`|| true` and is judged on its printed number, not its exit status.

There is no nested subagent dispatch available to a GSD executor in this devcontainer, so
`map-codebase.md`'s `detect_runtime_capabilities` step routes to `sequential_mapping`.

`.planning/config.json` is dirty in the working tree before this plan starts, from a pre-existing
GSD-verb defect that pruned two `planning.sub_repos` entries. It is not this phase's work and must not
be staged or committed here.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: Remap the quality pair</name>
  <reversibility rating="costly">D-03's remap rewrites content far beyond the rename; undoing it means restoring the documents from git and re-applying the corrections by hand.</reversibility>
  <precondition>`evidence/192-02-preserved-history.txt` exists and holds a `pre_remap_commit:` SHA together with the verbatim preserved-history anchors of `CONVENTIONS.md` and `TESTING.md`, captured by plan 02 before any document was rewritten.</precondition>
  <read_first>
    - /workspaces/.planning/codebase/CONVENTIONS.md — a file being rewritten; read it whole, including the `Host-app conventions (submodule — preserved from 2026-05-08)` section that must survive
    - /workspaces/.planning/codebase/TESTING.md — a file being rewritten; read it whole, including the `Host-app integration scripts (submodule — preserved from 2026-05-08)` section that must survive
    - /workspaces/.planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt — the anchors captured before the remap began
    - /workspaces/.claude/gsd-core/workflows/map-codebase.md — the `sequential_mapping` Pass 3 brief and `scan_for_secrets`
    - /workspaces/.planning/phases/192-live-references-only/192-CONTEXT.md — D-03, D-04, D-05, D-06
  </read_first>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <action>
    Run Pass 3, Quality Focus, of `map-codebase.md`'s `sequential_mapping` step at D-04's scope,
    verbatim: `.claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md`. Enumerate the
    scope with `git ls-files` over those prefixes, not with a shell glob.

    Rewrite the meta-repository halves of `CONVENTIONS.md` and `TESTING.md`. Keep verbatim, per D-05:
    `CONVENTIONS.md`'s `# Host-app conventions (submodule — preserved from 2026-05-08)` section through
    the end of the file, and `TESTING.md`'s
    `## Host-app integration scripts (submodule — preserved from 2026-05-08)` section through the end of
    the file, each with its own italicised caveat paragraph. Extend each preserved section's existing
    unverified marker additively so it names this remap's date as well as the earlier one; remove no
    existing wording.

    Three claims are stale today and must not survive in any form. `CONVENTIONS.md` cites a
    catalog-authority workflow as the documented reference for an orphaned-gitlink checkout convention;
    `TESTING.md` states that the same workflow is the sole workflow in this repository and describes its
    triggers. The meta repository has no `.github/workflows/` directory at all — `.github/` holds
    `CONTRIBUTING.md` and an `ISSUE_TEMPLATE/` directory of four files, and nothing else. Remove those
    claims outright rather than rewriting them, and name none of the three retired workflow files and
    not the removed wiki-tooling directory anywhere, not even to record that they are gone; that belongs
    in this plan's SUMMARY.md and in the commit message.

    `TESTING.md`'s meta-repository half must say plainly what is true in the mapped scope: there is no
    workflow directory and therefore no automated check of any kind in it; the only executable artefacts
    in scope are the devcontainer provisioning scripts. Say nothing about `tools/` — it is outside the
    scope by decision (D-04), so `tools/catalog/` is equally out of frame.

    Where an untracked-but-present path inside a scoped prefix is load-bearing for the development
    environment — the GSD install under `.claude/gsd-core/` and the agent definitions under
    `.claude/agents/` are both gitignored — describe it and say plainly that it is untracked.

    Stamp the frontmatter of both documents with this run's `last_mapped_commit`, `last_mapped_at` from
    `node .claude/gsd-core/bin/gsd-tools.cjs query current-timestamp`, and D-04's `mapped_paths` string
    unchanged. Set each `**Analysis Date:**` line so the meta-repository half names today and the
    host-app half still names 2026-05-08.

    Run `map-codebase.md`'s `scan_for_secrets` pattern over both documents before committing.
  </action>
  <acceptance_criteria>
    - The boundary-aware bare-slug count is 0 in both `CONVENTIONS.md` and `TESTING.md`, and the underscore-extended control count over each of the same two files is non-zero
    - The retired-CI token set `catalog-sync-check`, `wiki-check`, `wiki-publish`, `tools/wiki` matches no line in either document
    - Both documents carry this run's `last_mapped_commit` and `last_mapped_at`, not the pre-remap stamp, and `mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md` unchanged
    - `CONVENTIONS.md` still carries its `# Host-app conventions (submodule — preserved from 2026-05-08)` heading and `TESTING.md` still carries its `## Host-app integration scripts (submodule — preserved from 2026-05-08)` heading
    - The secret-pattern scan matches neither document
    - Both documents are longer than 20 lines
    - A scoped `git status --porcelain -- .planning/milestones/` prints no path and exits 0
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/.planning/codebase/CONVENTIONS.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/.planning/codebase/TESTING.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cE 'firestarter_(fw|app|prom)' /workspaces/.planning/codebase/CONVENTIONS.md</automated>
    <fails_when>prints 0, or exits non-zero — the control matched nothing, so the preceding zero is vacuous</fails_when>
    <automated>/usr/bin/grep -cE 'firestarter_(fw|app|prom)' /workspaces/.planning/codebase/TESTING.md</automated>
    <fails_when>prints 0, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -lE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' /workspaces/.planning/codebase/CONVENTIONS.md /workspaces/.planning/codebase/TESTING.md | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cF '# Host-app conventions (submodule — preserved from 2026-05-08)' /workspaces/.planning/codebase/CONVENTIONS.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero — the preserved section was dropped</fails_when>
    <automated>/usr/bin/grep -cF '## Host-app integration scripts (submodule — preserved from 2026-05-08)' /workspaces/.planning/codebase/TESTING.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF 'mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md' /workspaces/.planning/codebase/CONVENTIONS.md /workspaces/.planning/codebase/TESTING.md</automated>
    <fails_when>either per-file count printed is not 1, or the command exits non-zero</fails_when>
    <automated>/usr/bin/grep -clF 'last_mapped_commit: 3e2f7d89' /workspaces/.planning/codebase/CONVENTIONS.md /workspaces/.planning/codebase/TESTING.md | wc -l</automated>
    <fails_when>prints any number other than 0 — a document still carries the pre-remap provenance stamp</fails_when>
    <automated>/usr/bin/grep -clE '^last_mapped_commit: [0-9a-f]{7,40}$' /workspaces/.planning/codebase/CONVENTIONS.md /workspaces/.planning/codebase/TESTING.md | wc -l</automated>
    <fails_when>prints any number other than 2</fails_when>
    <automated>/usr/bin/grep -lE '(sk-[a-zA-Z0-9]{20,}|ghp_[a-zA-Z0-9]{36}|gho_[a-zA-Z0-9]{36}|glpat-[a-zA-Z0-9_-]+|AKIA[A-Z0-9]{16}|xox[baprs]-[a-zA-Z0-9-]+|-----BEGIN.*PRIVATE KEY)' /workspaces/.planning/codebase/CONVENTIONS.md /workspaces/.planning/codebase/TESTING.md | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>wc -l < /workspaces/.planning/codebase/CONVENTIONS.md</automated>
    <fails_when>prints a number below 20</fails_when>
    <automated>wc -l < /workspaces/.planning/codebase/TESTING.md</automated>
    <fails_when>prints a number below 20</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
  </verify>
  <done>`CONVENTIONS.md` and `TESTING.md` describe the conventions and the test surface of the repository that exists, name no workflow that does not, keep their preserved 2026-05-08 host-app sections with their caveats, and carry a provenance stamp true of this run. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Remap the concerns document</name>
  <reversibility rating="costly">Six of `CONCERNS.md`'s findings are removed outright because their subject no longer exists; restoring them means recovering the pre-remap file from git.</reversibility>
  <precondition>`evidence/192-02-preserved-history.txt` exists and holds the verbatim anchors of `CONCERNS.md`'s `Submodule Findings (2026-05-08)` section, captured before any document was rewritten.</precondition>
  <read_first>
    - /workspaces/.planning/codebase/CONCERNS.md — the file being rewritten; read it whole, including the six findings that cite a retired workflow and the `# Submodule Findings (2026-05-08)` section that must survive
    - /workspaces/.planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt — the anchors captured before the remap began
    - /workspaces/.claude/gsd-core/workflows/map-codebase.md — the `sequential_mapping` Pass 4 brief and `scan_for_secrets`
    - /workspaces/.planning/phases/192-live-references-only/192-SCOUT.md — the already-derived stale-line inventory for this document; do not re-derive it
  </read_first>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
    Run Pass 4, Concerns Focus, of `map-codebase.md`'s `sequential_mapping` step at D-04's scope,
    verbatim: `.claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md`.

    Rewrite the meta-repository half — the section that runs from the
    `# Meta-Repo / Dev-Environment / CI Concerns` heading down to the
    `# Submodule Findings (2026-05-08)` heading. Keep the `# Submodule Findings (2026-05-08)` section
    and the file's closing audit line verbatim, per D-05, extending that section's existing unverified
    marker additively so it names this remap's date as well as the earlier one.

    Six findings in the meta half are about a catalog-authority workflow that the repository does not
    have: a tech-debt entry about its never-passing runs and its branch pinning, a files line naming it,
    a second files line pairing it with `.gitmodules`, a third naming specific line ranges in it, a
    fragile-area entry citing it as the reference for the orphaned-gitlink failure mode, and a
    test-coverage-gap entry about assertions in it that never ran to completion. Remove all six
    outright. The subject of a concern that no longer exists is not a concern, and naming the file even
    to say it was removed reintroduces exactly the staleness this phase is closing; the removal is
    recorded in this plan's SUMMARY.md and in the commit message instead.

    What replaces them is one honest, present-tense observation rather than six: in the mapped scope the
    meta repository has no `.github/workflows/` directory and therefore no automated gate of any kind,
    so nothing in this repository is checked by CI. State it as a fact about what exists, naming no
    removed file. Keep every other meta-half finding that is still true of the mapped scope, and say
    nothing about `tools/` — it is outside the scope by decision (D-04).

    Stamp the frontmatter with this run's `last_mapped_commit`, `last_mapped_at` from
    `node .claude/gsd-core/bin/gsd-tools.cjs query current-timestamp`, and D-04's `mapped_paths` string
    unchanged. Set the `**Analysis Date:**` line so the meta half names today and the
    `**Prior full audit:**` line still names 2026-05-08.

    Run `map-codebase.md`'s `scan_for_secrets` pattern over the document before committing.
  </action>
  <acceptance_criteria>
    - The boundary-aware bare-slug count is 0 in `CONCERNS.md`, and a control reading over the same file is non-zero
    - The retired-CI token set `catalog-sync-check`, `wiki-check`, `wiki-publish`, `tools/wiki` matches no line in the document
    - The document carries this run's `last_mapped_commit` and `last_mapped_at`, not the pre-remap stamp, and `mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md` unchanged
    - The document still carries its `# Submodule Findings (2026-05-08)` heading and its `**Prior full audit:** 2026-05-08` line
    - The document records that the meta repository has no workflow directory in the mapped scope, so the absence of CI is stated rather than left as the reader's inference
    - The secret-pattern scan matches nothing
    - The document is longer than 20 lines
    - A scoped `git status --porcelain -- .planning/milestones/` prints no path and exits 0
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/.planning/codebase/CONCERNS.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cE 'firestarter_(fw|app|prom)|firestarter/|firestarter_app/' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints 0, or exits non-zero — the control matched nothing, so the preceding zero is vacuous</fails_when>
    <automated>/usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' /workspaces/.planning/codebase/CONCERNS.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cF '# Submodule Findings (2026-05-08)' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero — the preserved section was dropped</fails_when>
    <automated>/usr/bin/grep -cF '**Prior full audit:** 2026-05-08' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF '.github/workflows/' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints 0, or exits non-zero — the absence of any CI in the mapped scope is not stated, leaving it as the reader's inference</fails_when>
    <automated>/usr/bin/grep -cF 'mapped_paths: .claude,.devcontainer,.github,.gitignore,.gitmodules,.vscode,CLAUDE.md' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF 'last_mapped_commit: 3e2f7d89' /workspaces/.planning/codebase/CONCERNS.md || true</automated>
    <fails_when>prints any number other than 0 — the document still carries the pre-remap provenance stamp</fails_when>
    <automated>/usr/bin/grep -cE '^last_mapped_commit: [0-9a-f]{7,40}$' /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -lE '(sk-[a-zA-Z0-9]{20,}|ghp_[a-zA-Z0-9]{36}|gho_[a-zA-Z0-9]{36}|glpat-[a-zA-Z0-9_-]+|AKIA[A-Z0-9]{16}|xox[baprs]-[a-zA-Z0-9-]+|-----BEGIN.*PRIVATE KEY)' /workspaces/.planning/codebase/CONCERNS.md | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>wc -l < /workspaces/.planning/codebase/CONCERNS.md</automated>
    <fails_when>prints a number below 20</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
  </verify>
  <done>`CONCERNS.md`'s six findings about a workflow the repository does not have are gone, replaced by one present-tense statement that the mapped scope has no CI at all; the preserved 2026-05-08 submodule findings survive with their caveat; the provenance stamp is true of this run. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Gate all seven documents against the post-remap tree</name>
  <read_first>
    - /workspaces/.planning/phases/192-live-references-only/192-CONTEXT.md — D-06, which makes this reading the gate rather than the mapper's intent
    - /workspaces/.planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt — the pre-remap anchors every document is re-checked against
    - /workspaces/.planning/ROADMAP.md — the four success criteria this transcript reports against, in the `Phase 192: Live References Only` section
    - /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt — the paired-reading transcript shape
  </read_first>
  <files>.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</files>
  <action>
    Write `evidence/192-03-codebase-gate.txt`: D-06's gate, taken against the files as they now stand
    rather than against what the remap was asked to do.

    Enumerate the documents with `git ls-files -- .planning/codebase`, which is exactly seven paths
    today, and process them in sorted order so two runs of the gate are byte-comparable. Record
    `documents_gated:` followed by that count, then one block per document, in sorted order, each
    carrying four labelled readings taken with `/usr/bin/grep` and a copy-pasteable `command:` line:
    - `bare_slug:` the boundary-aware count for `henols/firestarter($|[^_A-Za-z0-9])`, expected 0
    - `control_extended_slug:` the count for the underscore-extended slugs over the same file, which
      must be non-zero, because a zero paired with a zero means the pattern matched nothing and is
      indistinguishable from a clean file
    - `retired_ci_tokens:` the count for the token set `catalog-sync-check`, `wiki-check`,
      `wiki-publish`, `tools/wiki`, expected 0
    - `preserved_anchor:` the document's preserved-history anchor line from
      `192-02-preserved-history.txt`, quoted, with `present` or `ABSENT`

    Close with the criterion verdicts, each on its own line keyed as shown, each citing the readings it
    rests on: `criterion_1:` no live tracked `.planning/codebase/` document addresses the bare slug;
    `criterion_3:` `STACK.md` describes no catalog-sync workflow checking out the sub-repos;
    `criterion_4:` `STRUCTURE.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md` and `TESTING.md` agree with the
    renamed reality. Add a short scope paragraph recording, in plain words, that the union of SWEEP-03's
    one named document and the ROADMAP's four is five, that `CONCERNS.md` and `CONVENTIONS.md` were
    named by neither, and that the seven gated here are exactly what `.planning/codebase/` contains — so
    no document is claimed by two scopes and none falls outside. Record too that `tools/` was outside the
    remap scope by decision, so the previously-stale wiki-tooling claim died by omission rather than by
    correction.

    If any document reads non-zero for the bare slug or for the retired-CI token set, hand-correct it
    only if this plan owns it — `CONVENTIONS.md`, `TESTING.md` or `CONCERNS.md`. A survivor in one of
    plan 02's four documents is a finding, not this task's repair: record it in the transcript, halt,
    and surface it, because plan 02 owns those files and a cross-plan edit would break the file
    ownership the wave structure depends on.
  </action>
  <acceptance_criteria>
    - `git ls-files -- .planning/codebase` lists exactly 7 paths, and the transcript's `documents_gated:` line records 7
    - The transcript carries seven `bare_slug:` lines, seven `control_extended_slug:` lines, seven `retired_ci_tokens:` lines and seven `preserved_anchor:` lines
    - Every `bare_slug:` value is 0 and every `retired_ci_tokens:` value is 0
    - Every `control_extended_slug:` value is a non-zero integer, so no zero above it is vacuous
    - No `preserved_anchor:` line is marked `ABSENT`
    - The transcript carries a `criterion_1:`, a `criterion_3:` and a `criterion_4:` verdict line
    - A live re-reading of the boundary-aware bare-slug count over all seven documents, taken independently of the transcript, returns no matching file
    - All seven documents are longer than 20 lines
    - A scoped `git status --porcelain -- .planning/milestones/` prints no path and exits 0
  </acceptance_criteria>
  <verify>
    <automated>git -C /workspaces ls-files -- .planning/codebase > "${TMPDIR:-/tmp}/192-codebase-docs.txt"; echo "lsfiles_rc=$?"; wc -l < "${TMPDIR:-/tmp}/192-codebase-docs.txt"</automated>
    <fails_when>the rc line is not `lsfiles_rc=0`, or the printed count is not 7</fails_when>
    <automated>/usr/bin/grep -cE '^documents_gated: 7$' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^bare_slug: 0$' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 7, or exits non-zero — a document is missing a reading, or one of them is not 0</fails_when>
    <automated>/usr/bin/grep -cE '^retired_ci_tokens: 0$' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 7, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^control_extended_slug: [1-9][0-9]*$' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 7, or exits non-zero — a control read 0, which makes the bare-slug zero beside it vacuous</fails_when>
    <automated>/usr/bin/grep -cE '^preserved_anchor:' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 7, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cF 'ABSENT' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt || true</automated>
    <fails_when>prints any number other than 0 — a preserved-history anchor captured before the remap is gone from the document it came from</fails_when>
    <automated>/usr/bin/grep -cE '^criterion_(1|3|4):' /workspaces/.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt</automated>
    <fails_when>prints any number other than 3, or exits non-zero</fails_when>
    <automated>while IFS= read -r f; do /usr/bin/grep -lE 'henols/firestarter($|[^_A-Za-z0-9])' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-codebase-docs.txt" | wc -l</automated>
    <fails_when>prints any number other than 0 — a live re-reading found a bare-slug survivor the transcript does not report</fails_when>
    <automated>while IFS= read -r f; do /usr/bin/grep -lE 'firestarter_(fw|app|prom)' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-codebase-docs.txt" | wc -l</automated>
    <fails_when>prints any number other than 7 — the live control did not match every document, so the live zero above it is vacuous</fails_when>
    <automated>while IFS= read -r f; do /usr/bin/grep -lE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-codebase-docs.txt" | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>while IFS= read -r f; do wc -l < "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-codebase-docs.txt" | sort -n | head -1</automated>
    <fails_when>prints a number below 20 — at least one document came back effectively empty</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>All seven `.planning/codebase/` documents are gated against the post-remap tree with a paired control beside every zero, every preserved-history anchor is confirmed present, and the transcript states the criterion 1, 3 and 4 verdicts together with the scope reasoning that makes the seven-document set the right one. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → tracked record | The remap writes documents this plan does not control line by line; D-06's gate is the boundary check |
| scoped source files → regenerated document | `.devcontainer/`, `.vscode/` and `.claude/` configuration is read and summarised into tracked documents |
| plan 03 → plan 02's four documents | A read-only boundary: this plan gates them but must not edit them, or the wave's exclusive file ownership breaks |
| executor → `.planning/milestones/` | An integrity boundary, not a write surface (D-5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-192-02 | Tampering | The remap's unreviewed output landing in tracked documents | high | mitigate | Task 3 re-reads all seven documents live and independently of the transcript, with a control leg that must match all seven |
| T-192-03 | Information disclosure | Credentials in `.devcontainer/`, `.vscode/` or `.claude/` transcribed into a regenerated document | medium | mitigate | `scan_for_secrets` pattern run per document as a verify leg; the actions instruct naming variables, never values |
| T-192-04 | Repudiation | The gate transcript certifying a cleanliness it never measured | high | mitigate | Every zero in the transcript has a paired `control_extended_slug:` line that must be non-zero, and Task 3 re-takes both readings live |
| T-192-10 | Tampering | Silent loss of the preserved 2026-05-08 history | medium | mitigate | `preserved_anchor:` line per document checked against the pre-remap capture, with any `ABSENT` marking failing the task |
| T-192-11 | Tampering | A cross-plan edit into plan 02's four documents breaking exclusive file ownership | medium | mitigate | Task 3 repairs only the three documents this plan owns; a survivor elsewhere halts and is surfaced rather than silently fixed |
| T-192-01 | Tampering | `.planning/milestones/` archived records | high | mitigate | No `milestones/` path in `files_modified`; a scoped porcelain leg whose own exit status is echoed ends every task |
| T-192-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install; the package-legitimacy gate has no subject. Accepted on the basis of zero install tasks, re-checked if one is added |
</threat_model>

## Artifacts this phase produces

New files created by this plan (excluded from drift verification — they do not exist before it runs):

- `.planning/phases/192-live-references-only/evidence/192-03-codebase-gate.txt` — D-06's post-remap gate transcript covering all seven documents, with the criterion 1, 3 and 4 verdicts
- `.planning/phases/192-live-references-only/192-03-SUMMARY.md` — this plan's summary

Existing files rewritten in place (not new symbols): `.planning/codebase/CONVENTIONS.md`,
`.planning/codebase/TESTING.md`, `.planning/codebase/CONCERNS.md`.

<verification>
- All seven `.planning/codebase/` documents read 0 for the bare slug and 0 for the retired-CI token set, live and in the transcript
- Every zero is paired with a control that reads non-zero over the same file
- Every preserved-history anchor captured before the remap is still present
- All three documents this plan owns carry a `last_mapped_commit` from this run and none carries the pre-remap value
- A scoped `git status --porcelain -- .planning/milestones/` prints no path
</verification>

<success_criteria>
`.planning/codebase/` is internally consistent in one pass: all seven documents describe the repository
that exists, none names a workflow or a tooling directory that was retired, none addresses the firmware
repository by its old name, and the transcript that says so carries a live control beside every zero
and a stated verdict against ROADMAP criteria 1, 3 and 4.
</success_criteria>

<output>
Create `.planning/phases/192-live-references-only/192-03-SUMMARY.md` when done.
</output>
