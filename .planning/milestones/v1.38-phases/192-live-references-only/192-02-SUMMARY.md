---
phase: 192-live-references-only
plan: 02
subsystem: docs
tags: [meta-repo, codebase-mapping, gsd-map-codebase, github, evidence]

# Dependency graph
requires:
  - phase: 189-free-the-name
    provides: "henols/firestarter_fw as the firmware repository's live GitHub identity, and .gitmodules already resolving it"
provides:
  - "STACK.md and INTEGRATIONS.md remapped at D-04's recorded scope, with true provenance stamps"
  - "ARCHITECTURE.md and STRUCTURE.md remapped at D-04's recorded scope, with the retired-CI section, row and anti-pattern entries removed outright"
  - "The one bare-slug survivor inside a preserved section (INTEGRATIONS.md's GitHub releases endpoint) corrected, with the exception disclosed in that section's own caveat"
  - "evidence/192-02-preserved-history.txt — pre-remap anchors for all seven codebase documents, captured before any of them was rewritten"
affects: [192-03]

# Actuals (#2632)
actuals:
  tokens: 12857
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Real /gsd-map-codebase remap over hand-patching, gated post-remap by the same boundary-aware sweep used to write the code (D-03/D-06) — the mapper's intent is never trusted as proof"
    - "Preserved-history capture before rewrite: verbatim anchor lines for every affected document, quoted into an evidence file keyed to a named pre-remap SHA, so a dropped section is a diff rather than a memory exercise"

key-files:
  created:
    - .planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt
  modified:
    - .planning/codebase/STACK.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/STRUCTURE.md

key-decisions:
  - "Followed D-03/D-04/D-05/D-06 verbatim: a real remap at the recorded scope, not a fifth hand-patch; preserved 2026-05-08 submodule sections kept verbatim with their not-re-verified markers extended additively to also name this run's date; the post-remap boundary-aware sweep is what proves criterion 1, not the mapper's intent."
  - "Went beyond the minimum slug/CI fix where the scan surfaced other stale facts inside the same scope (GSD version 1.6.1→1.13.0, tracked-skill-file count 7→9, agent/hook/command/reference/template counts, post-create.sh's new GSD-install step, .devcontainer/README.md) — a real remap re-derives what it can see, not just what the plan named."
  - "Removed the two anti-pattern entries that cited catalog-sync-check.yml as their reference outright, rather than rewriting them to cite something else, per the plan's explicit instruction not to rewrite CI sections."
  - "wiki/ and tools/ were dropped from STRUCTURE.md's tracked-artifact list and top-level tree: wiki/ does not exist at all (0 tracked files), and tools/ is out of scope by D-04's accepted narrowing — described nowhere in either document, including in the prescriptive 'Where to Add New Meta-Repo Code' section."

patterns-established:
  - "Pattern 1: a not-re-verified marker is extended additively across remaps (`[unverified in 2026-08-26 and 2026-09-14 scoped remaps]`), never replaced — each remap's own date joins the list rather than erasing the prior one."
  - "Pattern 2: a correction inside a section labelled 'carried forward verbatim' is disclosed in that section's own caveat sentence, not silently made — INTEGRATIONS.md's Part 2 now names the one documented exception."

requirements-completed: [SWEEP-01, SWEEP-03]

coverage:
  - id: D1
    description: "STACK.md and INTEGRATIONS.md regenerated by a real remap at D-04's scope: firmware submodule remote corrected to firestarter_fw, retired CI section removed, provenance stamp true of this run, preserved Part 2 sections intact"
    requirement: "SWEEP-01"
    verification:
      - kind: other
        ref: "boundary-aware bare-slug count 0 paired with non-zero underscore-extended control over both files; retired-CI token set absent; last_mapped_commit/last_mapped_at from this run; Part 2 headings and not-re-verified caveats present"
        status: pass
    human_judgment: false
  - id: D2
    description: "The one bare-slug survivor inside INTEGRATIONS.md's preserved Part 2 (GitHub releases endpoint) corrected to firestarter_fw, with the exception disclosed in that section's own caveat sentence"
    requirement: "SWEEP-01"
    verification:
      - kind: other
        ref: "grep -cF 'https://api.github.com/repos/henols/firestarter_fw/releases/latest' (1); grep -cF 'documented exception' (1); grep -cF 'carried forward verbatim' (1)"
        status: pass
    human_judgment: false
  - id: D3
    description: "ARCHITECTURE.md and STRUCTURE.md regenerated: CI component row, 'CI: catalog authority' section, CI entry-point line, and the two anti-pattern entries citing catalog-sync-check.yml removed outright; STRUCTURE.md's tree no longer lists .github/workflows/ or tools/wiki/ as tracked"
    requirement: "SWEEP-03"
    verification:
      - kind: other
        ref: "retired-CI token set (catalog-sync-check|wiki-check|wiki-publish|tools/wiki) matches 0 lines across both files; boundary-aware bare-slug count 0 paired with non-zero control; preserved Part 1 (ARCHITECTURE.md) and Python/Arduino sections (STRUCTURE.md) still present with extended not-re-verified markers"
        status: pass
    human_judgment: false
  - id: D4
    description: "evidence/192-02-preserved-history.txt captures the pre-remap anchors of all seven .planning/codebase/ documents against a named pre_remap_commit SHA, before any document was rewritten"
    requirement: "SWEEP-03"
    verification:
      - kind: other
        ref: "grep -cE '^pre_remap_commit: [0-9a-f]{40}$' → 1; grep -oE for the seven document names, sort -u, wc -l → 7"
        status: pass
    human_judgment: false

duration: 45min
completed: 2026-09-14
status: complete
---

# Phase 192 Plan 02: Live References Only Summary

**Regenerated STACK.md, INTEGRATIONS.md, ARCHITECTURE.md and STRUCTURE.md via a real `/gsd-map-codebase` remap at D-04's recorded scope, correcting the firmware submodule slug and removing every trace of the retired catalog-sync-check.yml CI workflow, while keeping the 2026-05-08 submodule history verbatim.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-09-14 (session start)
- **Completed:** 2026-09-14T05:59:00Z (approx.)
- **Tasks:** 2
- **Files modified:** 4 (`.planning/codebase/{STACK,INTEGRATIONS,ARCHITECTURE,STRUCTURE}.md`); 1 new evidence file

## Accomplishments

- Captured `evidence/192-02-preserved-history.txt`: the pre-remap `pre_remap_commit` SHA plus verbatim preserved-history anchor lines for all seven `.planning/codebase/` documents (not just the four this plan owns), so plan 03 inherits a capture taken before any of them was rewritten.
- Remapped `STACK.md` and `INTEGRATIONS.md`'s meta-repository sections: the firmware submodule remote now reads `git@github.com:henols/firestarter_fw.git` (name/path unchanged, still `firestarter`); the retired `catalog-sync-check.yml` CI section is gone entirely; GSD version, tracked-skill-file count, and hook/agent counts updated to current values. Both documents' `Part 2 — Submodule …` sections survive verbatim with their not-re-verified caveats intact.
- Corrected the one bare-slug survivor sitting inside a preserved section: `INTEGRATIONS.md`'s `Part 2 — Submodule integrations` GitHub releases endpoint now reads `https://api.github.com/repos/henols/firestarter_fw/releases/latest`, with the section's own caveat extended in place to name this as the one documented exception to "carried forward verbatim."
- Remapped `ARCHITECTURE.md` and `STRUCTURE.md`: removed the CI component-table row, the "CI: catalog authority" data-flow section, the CI entry-point line, and the two anti-pattern entries that cited `catalog-sync-check.yml` as their reference (removed outright per the plan, not rewritten to cite something else). `STRUCTURE.md`'s top-level tree no longer lists `.github/workflows/` (the three retired files) or `tools/wiki/` as tracked; `.github/` now shows what it actually holds — `CONTRIBUTING.md` plus a four-file `ISSUE_TEMPLATE/`. `tools/` is named nowhere in either document, including in the prescriptive "Where to Add New Meta-Repo Code" section, per D-04's accepted narrowing.
- Both documents' preserved 2026-05-08 sections (`ARCHITECTURE.md`'s Part 1; `STRUCTURE.md`'s Python Application and Arduino Firmware sections) survive verbatim, with their not-re-verified markers extended additively (`[unverified in 2026-08-26 and 2026-09-14 scoped remaps]`) rather than replaced.

## Task Commits

Each task was committed atomically:

1. **Task 1: Capture the preserved history, then remap the tech pair** - `52c0cc0d` (feat)
2. **Task 2: Remap the architecture pair** - `a48d31c1` (feat)

_No plan-metadata commit was made — per this plan's orchestrator constraints, ROADMAP.md, STATE.md's bulk-write verbs and config.json are owned centrally and were left untouched by this executor. This SUMMARY.md is committed separately by the orchestrator's own flow._

## Files Created/Modified

- `.planning/codebase/STACK.md` - Remapped: firmware remote corrected, retired CI section removed, GSD/tooling counts refreshed, Part 2 preserved verbatim
- `.planning/codebase/INTEGRATIONS.md` - Remapped: firmware remote and releases endpoint corrected (with disclosed exception), retired CI references removed, Part 2 preserved with the one documented correction
- `.planning/codebase/ARCHITECTURE.md` - Remapped: CI row/section/entry-point/anti-patterns removed, firmware remote corrected, GSD/tooling counts refreshed, Part 1 preserved verbatim
- `.planning/codebase/STRUCTURE.md` - Remapped: `.github/workflows/` and `tools/wiki/` dropped from the tree, `.github/` shown as it is, firmware remote corrected, Python/Arduino sections preserved verbatim
- `.planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt` - New: pre-remap preserved-history anchors for all seven codebase documents

## Decisions Made

- Followed D-03 (real remap, not a fifth hand-patch), D-04 (recorded scope, `tools/` excluded), D-05 (preserved history re-attached verbatim) and D-06 (boundary-aware sweep as the gate) exactly as recorded in `192-CONTEXT.md`.
- Where the in-scope scan surfaced facts stale beyond the slug and the retired workflow — GSD version, managed-file/migration counts, tracked-skill-file list, hook/agent/command/reference/template counts, the new `.devcontainer/README.md` and the new GSD-install step in `post-create.sh` — those were corrected too, since a genuine remap re-derives everything it can see in scope rather than patching only the two named facts.
- The two anti-pattern entries in `ARCHITECTURE.md` that cited `catalog-sync-check.yml` were removed outright rather than rewritten with a different reference, matching the plan's instruction to remove CI-referencing content rather than rewrite it.

## Deviations from Plan

None - plan executed exactly as written. The additional stale-fact corrections above (GSD version, tooling counts, etc.) are within the plan's own instruction to "rewrite the meta-repository sections from that scan," not additions beyond it.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Ready for plan 03 (SWEEP-03 requirements amendment naming all seven documents, plus `CONCERNS.md`/`CONVENTIONS.md`/`TESTING.md` remap and the sub-repo re-verification).
- `evidence/192-02-preserved-history.txt` gives plan 03 the pre-remap anchors for `CONCERNS.md`, `CONVENTIONS.md` and `TESTING.md` (the three documents it still owns) without needing to re-derive them.
- `git status --porcelain -- .planning/milestones/` reported 0 lines after every task — the D-5 integrity boundary held throughout.
- `.planning/STATE.md`, `.planning/config.json`, `tools/catalog/messages.toml`, `anything.txt` and `tmp/` were left dirty/untracked exactly as found, per this plan's orchestrator constraints.

## Self-Check: PASSED

- `.planning/codebase/STACK.md`, `.planning/codebase/INTEGRATIONS.md`, `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/STRUCTURE.md`, `.planning/phases/192-live-references-only/evidence/192-02-preserved-history.txt` all confirmed present with `[ -f ]`.
- Both task commits (`52c0cc0d`, `a48d31c1`) confirmed in `git log --oneline`.
- All acceptance criteria for both tasks re-verified passing at SUMMARY time: boundary-aware bare-slug count 0 in all four documents, each paired with a non-zero underscore-extended control; retired-CI token set absent from all four; `last_mapped_commit`/`last_mapped_at` from this run in all four, pre-remap stamp absent; Part 2/Part 1 preserved headings and closing lines present in all four; `INTEGRATIONS.md`'s corrected endpoint and disclosed exception present; secret-pattern scan clean across all four; all four longer than 20 lines; `.planning/milestones/` clean; branch `gsd/v1.38-repository-rename-activated-2026-09-13` throughout.
- Plan-level `<verification>` block re-run in full immediately before writing this SUMMARY: all six bullets confirmed passing (see the commands and results recorded above).

---
*Phase: 192-live-references-only*
*Completed: 2026-09-14*
