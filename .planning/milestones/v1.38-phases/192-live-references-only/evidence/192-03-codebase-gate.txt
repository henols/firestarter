D-06 POST-REMAP GATE: ALL SEVEN .planning/codebase/ DOCUMENTS
=================================================================
capture_date: 2026-09-14T05:31:06Z
capture_commit: 3496be22f811ace4cd390d40498038be1c34a7cb
documents_gated: 7

Purpose: prove ROADMAP criteria 1, 3 and 4 against the post-remap tree as it
now stands, not against what plan 02 and this plan were asked to do (D-06).
Every zero below is paired with a control that must be non-zero over the
same file, because a zero bare-slug reading paired with a zero control would
be a failed gate, not a pass — it would mean the pattern itself matched
nothing, indistinguishable from a clean file. Readings taken with
`/usr/bin/grep` (PATH `grep` in this devcontainer is ugrep and silently
honours `.gitignore`) over the file list from `git ls-files -- .planning/codebase`,
sorted, so two runs of this gate are byte-comparable.

Enumeration command:
  git ls-files -- .planning/codebase | sort

Per-document readings — four labelled readings each, sorted document order:
-------------------------------------------------------------------------

ARCHITECTURE.md
---------------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/ARCHITECTURE.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/ARCHITECTURE.md
control_extended_slug: 15
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/ARCHITECTURE.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '# Part 2 — Meta-Repo Architecture (agent tooling & dev environment)' .planning/codebase/ARCHITECTURE.md
preserved_anchor: "# Part 2 — Meta-Repo Architecture (agent tooling & dev environment)" present

CONCERNS.md
-----------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/CONCERNS.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/CONCERNS.md
control_extended_slug: 32
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/CONCERNS.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '# Submodule Findings (2026-05-08)' .planning/codebase/CONCERNS.md
preserved_anchor: "# Submodule Findings (2026-05-08)" present

CONVENTIONS.md
--------------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/CONVENTIONS.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/CONVENTIONS.md
control_extended_slug: 7
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/CONVENTIONS.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '# Host-app conventions (submodule — preserved from 2026-05-08)' .planning/codebase/CONVENTIONS.md
preserved_anchor: "# Host-app conventions (submodule — preserved from 2026-05-08)" present

INTEGRATIONS.md
---------------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/INTEGRATIONS.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/INTEGRATIONS.md
control_extended_slug: 5
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/INTEGRATIONS.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '# Part 2 — Submodule integrations (from 2026-05-08 analysis; not re-verified this run)' .planning/codebase/INTEGRATIONS.md
preserved_anchor: "# Part 2 — Submodule integrations (from 2026-05-08 analysis; not re-verified this run)" present

STACK.md
--------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/STACK.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/STACK.md
control_extended_slug: 10
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/STACK.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '# Part 2 — Submodule stacks (from 2026-05-08 analysis; not re-verified this run)' .planning/codebase/STACK.md
preserved_anchor: "# Part 2 — Submodule stacks (from 2026-05-08 analysis; not re-verified this run)" present

STRUCTURE.md
------------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/STRUCTURE.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/STRUCTURE.md
control_extended_slug: 18
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/STRUCTURE.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '*Submodule structure analysis: 2026-05-08*' .planning/codebase/STRUCTURE.md
preserved_anchor: "*Submodule structure analysis: 2026-05-08*" present

TESTING.md
----------
bare_slug command: /usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' .planning/codebase/TESTING.md
bare_slug: 0
control_extended_slug command: /usr/bin/grep -cE 'firestarter_(fw|app|prom)' .planning/codebase/TESTING.md
control_extended_slug: 9
retired_ci_tokens command: /usr/bin/grep -cE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' .planning/codebase/TESTING.md
retired_ci_tokens: 0
preserved_anchor command: /usr/bin/grep -cF '## Host-app integration scripts (submodule — preserved from 2026-05-08)' .planning/codebase/TESTING.md
preserved_anchor: "## Host-app integration scripts (submodule — preserved from 2026-05-08)" present

Summary table
-------------
document            bare_slug  control_extended_slug  retired_ci_tokens  preserved_anchor
ARCHITECTURE.md      0          15                      0                  present
CONCERNS.md           0          32                      0                  present
CONVENTIONS.md        0          7                       0                  present
INTEGRATIONS.md       0          5                       0                  present
STACK.md              0          10                      0                  present
STRUCTURE.md          0          18                      0                  present
TESTING.md            0          9                       0                  present

Independent live re-reading (not derived from the table above)
----------------------------------------------------------------
command: git ls-files -- .planning/codebase | sort > /tmp/192-codebase-docs.txt
         while IFS= read -r f; do /usr/bin/grep -lE 'henols/firestarter($|[^_A-Za-z0-9])' "$f"; done < /tmp/192-codebase-docs.txt | wc -l
result: 0 (no live bare-slug survivor across all seven documents)

command: while IFS= read -r f; do /usr/bin/grep -lE 'firestarter_(fw|app|prom)' "$f"; done < /tmp/192-codebase-docs.txt | wc -l
result: 7 (every document matches the control — the zero above is not vacuous)

command: while IFS= read -r f; do /usr/bin/grep -lE 'catalog-sync-check|wiki-check|wiki-publish|tools/wiki' "$f"; done < /tmp/192-codebase-docs.txt | wc -l
result: 0

Criterion verdicts
------------------
criterion_1: No live tracked `.planning/codebase/` document addresses the bare slug
  `henols/firestarter` unqualified. All seven bare_slug readings above are 0, each
  paired with a non-zero control (5–32), and the independent live re-reading taken in
  this same task, over the same file list but not derived from the per-document table,
  confirms zero survivors. `.gitmodules` itself (D-04's scope) already resolves
  `firestarter_fw`, so the mapper read the corrected remote rather than a redirect.

criterion_3: `STACK.md` describes no catalog-sync workflow checking out the sub-repos.
  `STACK.md`'s retired_ci_tokens reading is 0; the workflow's frontmatter/prose does not
  appear anywhere in the document (confirmed by the same reading that gates all seven).
  `STACK.md` was remapped in plan 02 (192-02-SUMMARY.md), which removed the retired CI
  section outright, and this task's independent re-reading re-confirms it stayed removed.

criterion_4: `STRUCTURE.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md` and `TESTING.md` agree
  with the renamed reality. All four read 0 for both the bare slug and the retired-CI
  token set, each paired with a non-zero control, and each still carries its preserved
  2026-05-08 section verbatim (anchors above, all present). `STRUCTURE.md`'s tree no
  longer lists `.github/workflows/` or `tools/wiki/` as tracked (192-02-SUMMARY.md);
  `TESTING.md`'s meta-repository half (this plan) now states plainly that no
  `.github/workflows/` directory exists and therefore no automated check of any kind
  runs in this repo.

Scope reasoning
---------------
SWEEP-03 as originally written named one document (`STACK.md`). The ROADMAP's own
criterion 4 named four more (`STRUCTURE.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md`,
`TESTING.md`). The union of those two sources is five documents. `CONCERNS.md` and
`CONVENTIONS.md` were named by neither SWEEP-03 nor criterion 4 — both were nonetheless
stale (CONCERNS.md carried nine retired-CI references, CONVENTIONS.md two; 192-CONTEXT.md
D-03), and D-07 hand-amended SWEEP-03 in REQUIREMENTS.md to name all seven so the
traceability table stays honest. The seven documents gated in this transcript are exactly
what `git ls-files -- .planning/codebase` returns today — no document is claimed by two
scopes (STACK.md is plan 02's alone; CONVENTIONS.md/TESTING.md/CONCERNS.md are this plan's
alone) and none of the seven falls outside this gate.

`tools/` was outside the D-04 remap scope by decision, not by oversight
(192-CONTEXT.md D-04's "accepted narrowing"). `STRUCTURE.md`'s previously-stale
`tools/wiki/` claim therefore died by omission when plan 02 regenerated the document at
the recorded scope, rather than by a targeted correction — `tools/wiki/` does not appear
anywhere in any of the seven documents (retired_ci_tokens readings above), and none of the
seven documents describes `tools/catalog/` as part of its scanned scope. This is recorded
here, not discovered by a later reader, per D-04's explicit instruction.

Preserved-history integrity
----------------------------
Every anchor quoted above is one of the anchors captured in
evidence/192-02-preserved-history.txt before either the plan 02 or the plan 03 remap
rewrote any of the seven documents. Every one of the seven `preserved_anchor:` readings
above reads `present` — every
preserved 2026-05-08 section survived both remaps with its wording and its
not-re-verified caveat intact (192-CONTEXT.md D-05). CONVENTIONS.md, TESTING.md and
CONCERNS.md additionally extended their not-re-verified markers additively this run
(`[unverified in 2026-08-26 and 2026-09-14 scoped remaps]` / equivalent), naming both
remap dates rather than replacing the earlier one, matching the pattern plan 02 already
established for STACK.md, INTEGRATIONS.md, ARCHITECTURE.md and STRUCTURE.md.

Cross-plan ownership boundary
-------------------------------
This task re-read all seven documents live but repaired none of plan 02's four
(`STACK.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md`, `STRUCTURE.md`) — none of the four
needed repair, since every reading over them (above) already came back clean. Had any of
plan 02's documents read non-zero for the bare slug or the retired-CI token set, this task
would have recorded the finding and halted rather than editing a file plan 02 owns, per the
wave's exclusive file-ownership boundary (T-192-11).

Milestones integrity
---------------------
command: git status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"
result: (no output before the rc line); milestones_status_rc=0

Branch identity
----------------
command: git branch --show-current
result: gsd/v1.38-repository-rename-activated-2026-09-13
