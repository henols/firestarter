---
phase: 192-live-references-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/v1.4-e2e-verify.sh
  - .planning/v1.4-RELEASE-PROCEDURES.md
  - .planning/phases/192-live-references-only/evidence/192-slug-sweep.txt
  - .planning/phases/192-live-references-only/evidence/192-01-enumeration.txt
autonomous: true
requirements: [SWEEP-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README.md's repository table names `firestarter_fw` and links to https://github.com/henols/firestarter_fw, so a reader who clicks it reaches the firmware repository directly rather than through the rename redirect."
    - "All 13 bare-slug sites in the two runnable v1.4 artefacts address `henols/firestarter_fw`, including `.planning/v1.4-RELEASE-PROCEDURES.md` line 325, whose `gh release delete` invocation is the one stale-and-destructive site named by D-02."
    - "The sweep discriminates at the slug boundary: the pattern `henols/firestarter($|[^_A-Za-z0-9])` matches the bare slug only when it is followed by a non-identifier character or end of line, so it matches none of `henols/firestarter_fw`, `henols/firestarter_app`, `henols/firestarter_prom`; the same transcript records a control reading over the identical file set that returns a non-zero count for the underscore-extended slugs."
    - "Every bare-slug site sat beside an underscore-extended twin and every twin is byte-identical after the edit: README.md lines 1, 17, 28 and 35 still carry 4 `_app`/`_prom` references, `.planning/v1.4-e2e-verify.sh` lines 198, 200 and 201 still carry 3 `_app` references, and `.planning/v1.4-RELEASE-PROCEDURES.md` lines 39, 176, 191, 199 and 244 still carry 5."
    - "No zero is accepted alone. Every zero bare-slug reading recorded by this plan is paired with a control reading taken with the same command form over the same file set that returns non-zero; a zero paired with a zero is recorded as a failed gate, not as a clean tree."
    - "The enumeration is order-stable: matches are emitted in `git ls-files` order with `path:line` ascending, and two consecutive runs of the enumeration command produce byte-identical output."
    - "Every count states its unit. `/usr/bin/grep -c` counts matching LINES; the 1 / 7 / 6 occurrence claims are additionally taken with `/usr/bin/grep -o` piped to `wc -l`, and the two readings agree because each site sits on its own line."
    - statement: "Re-running this plan on an already-edited tree changes nothing: the replacement is boundary-anchored so a second pass cannot extend `henols/firestarter_fw` again, and the plan's three edited files have disjoint ownership from every other plan in wave 1, so a parallel or interrupted run cannot leave a half-applied edit in a file another plan is writing."
      verification: backstop
    - "The two runnable v1.4 artefacts carry no staleness banner, no added comment and no annotation — only their repository URLs changed (D-02)."
    - "`.planning/milestones/` is untouched by this plan: `git status --porcelain -- .planning/milestones/` reports 0 lines after every task."
  artifacts:
    - path: ".planning/phases/192-live-references-only/evidence/192-slug-sweep.txt"
      provides: "The phase's primary boundary-aware sweep transcript, in the 190-slug-sweep.txt shape: header, pattern-justification paragraph, paired readings, interpretation sentence, and the deliberately-not-covered section"
      contains: "reading_1_total:"
    - path: ".planning/phases/192-live-references-only/evidence/192-01-enumeration.txt"
      provides: "D-01's one-time enumeration of every remaining tracked bare-slug match in the meta repository, with the edited subset marked"
      contains: "edited_subset:"
    - path: "README.md"
      provides: "The meta repository's front-door repository table, repointed"
      contains: "henols/firestarter_fw"
    - path: ".planning/v1.4-e2e-verify.sh"
      provides: "The runnable v1.4 end-to-end verification script, repointed at all 7 sites"
      contains: "henols/firestarter_fw"
    - path: ".planning/v1.4-RELEASE-PROCEDURES.md"
      provides: "The runnable v1.4 release procedure, repointed at all 6 sites including the destructive release-delete line"
      contains: "henols/firestarter_fw"
  key_links:
    - from: "README.md"
      to: "https://github.com/henols/firestarter_fw"
      via: "markdown link in the `## The repositories` table, row 27"
      pattern: "henols/firestarter_fw"
    - from: ".planning/v1.4-e2e-verify.sh"
      to: "the firmware repository's GitHub releases"
      via: "gh release view / gh api invocations carrying -R henols/firestarter_fw"
      pattern: "-R henols/firestarter_fw"
    - from: ".planning/phases/192-live-references-only/evidence/192-slug-sweep.txt"
      to: "the boundary-aware sweep command"
      via: "a copy-pasteable `command:` line recording the exact /usr/bin/grep form used"
      pattern: "usr/bin/grep"
  prohibitions:
    - requirement_id: SWEEP-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT edit, repair or rewrite a historical record in order to make a sweep read clean — nothing under `.planning/milestones/`, no `.planning/phases/*` record, and not `.planning/PROJECT.md`, `.planning/notes/`, `.planning/seeds/` or `.planning/todos/`, even where the bare slug appears there. Repairing a record destroys the evidence of what the repository was called when the record was written."
    - requirement_id: SWEEP-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT report a zero or empty reading as proof without a paired non-vacuity control that reads non-zero using the same command form over the same file set."
    - requirement_id: SWEEP-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT imply that the v1.4 release procedure or the v1.4 end-to-end script was re-validated by this work. Only their repository URLs were repointed; their currency is untested and unclaimed."
    - requirement_id: SWEEP-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT add a comment, annotation, banner, phase reference or requirement citation to any edited file, and MUST NOT make the existence of such a comment an acceptance criterion."
---

<objective>
Repoint every meta-repository reference a reader could act on today, and record the blast radius of
the allowlist that decided which references those are.

This plan owns the three files named by D-01's allowlist outside `.planning/codebase/`: the meta
`README.md` and the two runnable v1.4 artefacts of D-02. It also produces the phase's primary sweep
transcript and D-01's one-time enumeration of everything the allowlist deliberately leaves alone.

Purpose: SWEEP-01's live-reference clause for the meta repository, and the recorded decision that
makes the un-edited remainder a choice rather than an executor's silence.
Output: `README.md`, `.planning/v1.4-e2e-verify.sh` and `.planning/v1.4-RELEASE-PROCEDURES.md`
repointed at 14 sites; `evidence/192-slug-sweep.txt` and `evidence/192-01-enumeration.txt` written.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/192-live-references-only/192-CONTEXT.md
@.planning/phases/192-live-references-only/192-PATTERNS.md
@.planning/phases/192-live-references-only/192-SCOUT.md
@.planning/ROADMAP.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
command in this plan whose evidence depends on seeing every file uses the absolute `/usr/bin/grep`.
File sets are enumerated with `git ls-files`, never with a shell glob.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 is written with a
trailing `|| true` and is judged on its printed number, not on its exit status.

`.planning/config.json` is dirty in the working tree before this plan starts — a GSD-verb defect pruned
two `planning.sub_repos` entries. That is pre-existing, is not this phase's work, and must not be staged
or committed by any task here.
</environment_notes>

<tasks>

<task type="tracer">
  <name>Task 1: One live reference, repointed and proved end-to-end</name>
  <reversibility rating="reversible">A one-line markdown edit in an untracked-by-any-consumer front-door README; reverting is a single line.</reversibility>
  <read_first>
    - /workspaces/README.md — the file being modified; read the whole `## The repositories` table so the row shape is copied, not guessed
    - /workspaces/.planning/phases/192-live-references-only/192-CONTEXT.md — D-01 (the allowlist rule) and D-02
    - /workspaces/.planning/phases/192-live-references-only/192-PATTERNS.md — the transcript shape and the sweep command form
    - /workspaces/.planning/phases/190-endpoints-that-do-not-depend-on-a-redirect/evidence/190-slug-sweep.txt — the transcript this one copies
  </read_first>
  <files>README.md, .planning/phases/192-live-references-only/evidence/192-slug-sweep.txt</files>
  <action>
    This is the phase's tracer: it drives one real reference through every layer the phase uses —
    the boundary-aware instrument, the edit, the paired-control proof, the evidence transcript and the
    commit — so that the instrument is proven before the remaining sites are expanded onto it.

    Create `/workspaces/.planning/phases/192-live-references-only/evidence/` with `mkdir -p`.

    Replace line 27 of `/workspaces/README.md` in full. The table's column header is literally
    `Repository` and row 28 labels its row with the repository's name, so both the link label and the
    URL move. The line becomes exactly, with no leading or trailing whitespace change:
    `| [firestarter_fw](https://github.com/henols/firestarter_fw) | The AVR firmware that runs on the Arduino and drives the chip |`
    Change nothing else in the file. Lines 1, 17, 28 and 35 carry `henols/firestarter_app` and
    `henols/firestarter_prom` references that are correct as written and must stay byte-identical, and
    the file must still be 35 lines. Add no comment, no banner and no note (D-01, D-02).

    Write `evidence/192-slug-sweep.txt` following the 190-slug-sweep.txt structure, in this order:
    (a) a title line naming it the phase-192 boundary-aware bare-slug sweep and citing D-01;
    (b) `capture_date:` an ISO-8601 UTC timestamp, and `capture_commit:` the meta-repo HEAD sha
    captured with `git -C /workspaces rev-parse HEAD` before the edit;
    (c) a pattern-justification paragraph stating why the naive sweep is vacuous — `henols/firestarter`
    is a substring of `henols/firestarter_fw`, so a line-filtering negative grep under-reports — and
    stating that the pattern below requires the bare slug be followed by a non-identifier character or
    end of line, and that `/usr/bin/grep` is used because PATH `grep` here is ugrep and honours
    `.gitignore`;
    (d) `Reading 1: the bare-slug sweep over README.md` with a copy-pasteable `command:` line, the
    matching lines, and `reading_1_total:` followed by the count;
    (e) `Reading 2: the non-vacuity positive control` over the same file with the underscore-extended
    slugs, a `command:` line, and `reading_2_control_total:` followed by the count;
    (f) the interpretation sentence, worded so a reader cannot miss it: a zero bare-slug reading paired
    with a zero control is a failed gate, not a pass, because it is indistinguishable from a command
    that matched nothing;
    (g) `Reading 3: the endpoint resolves without a redirect` recording the live
    `gh api repos/henols/firestarter_fw --jq '.full_name'` output;
    (h) a closing section headed exactly
    `What this sweep deliberately does NOT cover` listing, per D-01 and D-5: everything under
    `.planning/milestones/`; the record files under `.planning/phases/`, `.planning/notes/`,
    `.planning/seeds/`, `.planning/todos/`, `.planning/debug/` and `.planning/v1.3x/`; and
    `.planning/PROJECT.md`, naming its line 1396 live actions-run link and its line 69 sentence about
    claiming the bare slug as the two cases that show why the record test is the right cut.

    Commit README.md and the transcript together on the current branch.
  </action>
  <acceptance_criteria>
    - `sed -n '27p' /workspaces/README.md` prints exactly `| [firestarter_fw](https://github.com/henols/firestarter_fw) | The AVR firmware that runs on the Arduino and drives the chip |`
    - The boundary-aware bare-slug count over README.md is 0, and the underscore-extended control count over the same file is 5 (lines 1, 17, 27, 28, 35)
    - The untouched-neighbours control over README.md lines 1, 17, 28 and 35 still returns 4
    - README.md is still 35 lines
    - `evidence/192-slug-sweep.txt` carries exactly one `capture_commit:` line, one `reading_1_total:` line, one `reading_2_control_total:` line whose value is a non-zero integer, and one line containing `What this sweep deliberately does NOT cover`
    - `gh api repos/henols/firestarter_fw --jq '.full_name'` prints `henols/firestarter_fw`, proving the repointed URL is the repository's own name and not a redirect target
    - `git status --porcelain -- .planning/milestones/` reports 0 lines
    - Work is on branch `gsd/v1.38-repository-rename-activated-2026-09-13`
  </acceptance_criteria>
  <verify>
    <automated>sed -n '27p' /workspaces/README.md</automated>
    <fails_when>the printed line is anything other than `| [firestarter_fw](https://github.com/henols/firestarter_fw) | The AVR firmware that runs on the Arduino and drives the chip |`</fails_when>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/README.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cE 'henols/firestarter_(app|prom|fw)' /workspaces/README.md</automated>
    <fails_when>prints any number other than 5, or exits non-zero — a 0 here would mean the control pattern itself matched nothing, which makes the preceding zero vacuous</fails_when>
    <automated>awk 'NR==1||NR==17||NR==28||NR==35' /workspaces/README.md | /usr/bin/grep -cE 'henols/firestarter_(app|prom)'</automated>
    <fails_when>prints any number other than 4, or exits non-zero</fails_when>
    <automated>wc -l < /workspaces/README.md</automated>
    <fails_when>prints any number other than 35</fails_when>
    <automated>/usr/bin/grep -cE '^reading_2_control_total: [1-9][0-9]*$' /workspaces/.planning/phases/192-live-references-only/evidence/192-slug-sweep.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero — a missing or zero-valued control line means the transcript records an unproven zero</fails_when>
    <automated>/usr/bin/grep -cF 'What this sweep deliberately does NOT cover' /workspaces/.planning/phases/192-live-references-only/evidence/192-slug-sweep.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>gh api repos/henols/firestarter_fw --jq '.full_name'</automated>
    <fails_when>prints anything other than `henols/firestarter_fw`, or exits non-zero</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>README.md's repository table links to the firmware repository by its real name, the link target is proved live to be the repository's own full name rather than a redirect, and the transcript that proves it carries a non-vacuity control and its own scope boundary. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: The 13 runnable-artefact sites</name>
  <reversibility rating="reversible">13 string edits in two untested planning artefacts with no consumer; reverting is `git checkout` of two files (D-02).</reversibility>
  <read_first>
    - /workspaces/.planning/v1.4-e2e-verify.sh — the file being modified; read the surrounding shell so the `_app` twin lines are recognised
    - /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md — the file being modified, in particular the troubleshooting table row at line 325
    - /workspaces/.planning/phases/192-live-references-only/192-CONTEXT.md — D-02, which settles both the scope and the no-banner call
  </read_first>
  <files>.planning/v1.4-e2e-verify.sh, .planning/v1.4-RELEASE-PROCEDURES.md</files>
  <action>
    Repoint all 13 bare-slug sites per D-02. The sites are already measured and must not be re-derived:
    `.planning/v1.4-e2e-verify.sh` lines 142, 157, 184, 186, 205, 207 and 208; and
    `.planning/v1.4-RELEASE-PROCEDURES.md` lines 40, 177, 192, 200, 249 and 325. Each file carries
    exactly one occurrence per listed line.

    Apply the replacement with a boundary-anchored substitution, run once per file:
    `sed -i -E 's|henols/firestarter\b|henols/firestarter_fw|g' FILE`
    The `\b` is load-bearing and is equivalent to the phase's stated boundary pattern: `_` is a word
    constituent, so there is no word boundary between `firestarter` and the `_app` / `_prom` / `_fw`
    suffixes and those references cannot be touched. The same property makes the substitution
    idempotent — a second run finds no boundary after the newly written `firestarter_fw` and changes
    nothing.

    Leave the twin lines byte-identical: `.planning/v1.4-e2e-verify.sh` lines 198, 200 and 201, and
    `.planning/v1.4-RELEASE-PROCEDURES.md` lines 39, 176, 191, 199 and 244. Both files keep their
    current length — 285 and 360 lines.

    Add nothing else. Per D-02 there is no staleness banner and no header: a currency claim is not
    something this work measures, and asserting one would be false. Add no comment and no annotation of
    any kind, and do not record the requirement or decision identifier in either file — that belongs in
    this plan's SUMMARY.md and in the commit message.
  </action>
  <acceptance_criteria>
    - The boundary-aware bare-slug count is 0 in both files
    - `henols/firestarter_fw` appears on 7 lines of `.planning/v1.4-e2e-verify.sh` and on 6 lines of `.planning/v1.4-RELEASE-PROCEDURES.md`, matching the pre-edit bare-slug line counts exactly
    - The untouched-neighbours control returns 3 for `.planning/v1.4-e2e-verify.sh` lines 198/200/201 and 5 for `.planning/v1.4-RELEASE-PROCEDURES.md` lines 39/176/191/199/244
    - Line 325 of `.planning/v1.4-RELEASE-PROCEDURES.md` — the destructive `gh release delete` site — names `firestarter_fw`
    - Both files keep their length: 285 and 360 lines
    - Neither file gained a banner, header or comment: the occurrence count of `henols/firestarter_fw` taken with `/usr/bin/grep -o` equals the line count, so no site was duplicated or annotated
    - `git status --porcelain -- .planning/milestones/` reports 0 lines
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/.planning/v1.4-e2e-verify.sh || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cE 'henols/firestarter($|[^_A-Za-z0-9])' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md || true</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>/usr/bin/grep -cF 'henols/firestarter_fw' /workspaces/.planning/v1.4-e2e-verify.sh</automated>
    <fails_when>prints any number other than 7, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -oF 'henols/firestarter_fw' /workspaces/.planning/v1.4-e2e-verify.sh | wc -l</automated>
    <fails_when>prints any number other than 7 — a higher number means a site was duplicated or annotated rather than replaced in place</fails_when>
    <automated>/usr/bin/grep -cF 'henols/firestarter_fw' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md</automated>
    <fails_when>prints any number other than 6, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -oF 'henols/firestarter_fw' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md | wc -l</automated>
    <fails_when>prints any number other than 6</fails_when>
    <automated>awk 'NR==198||NR==200||NR==201' /workspaces/.planning/v1.4-e2e-verify.sh | /usr/bin/grep -cF 'henols/firestarter_app'</automated>
    <fails_when>prints any number other than 3, or exits non-zero — the twins were disturbed</fails_when>
    <automated>awk 'NR==39||NR==176||NR==191||NR==199||NR==244' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md | /usr/bin/grep -cF 'henols/firestarter_app'</automated>
    <fails_when>prints any number other than 5, or exits non-zero</fails_when>
    <automated>sed -n '325p' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md | /usr/bin/grep -cF 'gh release delete'</automated>
    <fails_when>prints any number other than 1, or exits non-zero — line 325 is no longer the release-delete row, so the line addressing drifted</fails_when>
    <automated>sed -n '325p' /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md | /usr/bin/grep -cF 'henols/firestarter_fw'</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>wc -l < /workspaces/.planning/v1.4-e2e-verify.sh</automated>
    <fails_when>prints any number other than 285</fails_when>
    <automated>wc -l < /workspaces/.planning/v1.4-RELEASE-PROCEDURES.md</automated>
    <fails_when>prints any number other than 360</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
  </verify>
  <done>Both runnable artefacts address the firmware repository by its real name at every site, the destructive release-delete line included; their `henols/firestarter_app` twins are untouched and neither file gained a banner or a comment. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Enumerate the remainder the allowlist leaves alone</name>
  <read_first>
    - /workspaces/.planning/phases/192-live-references-only/192-CONTEXT.md — D-01, which requires this enumeration once, in phase evidence
    - /workspaces/.planning/phases/192-live-references-only/evidence/192-slug-sweep.txt — the transcript written in Task 1; this file is its companion and must use the same command form
    - /workspaces/.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt — the enumeration shape
  </read_first>
  <files>.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</files>
  <action>
    Write `evidence/192-01-enumeration.txt`: the complete, one-time enumeration D-01 requires, so the
    allowlist's blast radius is a recorded decision rather than an executor's silence.

    Take the enumeration in two steps so that no fallible `git` sits in a non-final pipeline stage,
    where its exit status would be swallowed by the last stage and a broken scan would read as clean.
    First materialise the file list, checking its status:
    `git -C /workspaces ls-files -z > "${TMPDIR:-/tmp}/192-tracked.z"; echo "lsfiles_z_rc=$?"`
    Then drive the NUL-delimited loop from that file, so a path containing whitespace cannot truncate
    the scan, forcing the filename prefix because a single-file grep omits it:
    `while IFS= read -r -d '' f; do /usr/bin/grep -HnE 'henols/firestarter($|[^_A-Za-z0-9])' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-tracked.z"`
    Paths therefore appear absolute in the output; record them exactly as emitted. Record, each on its
    own line and each with the exact key shown:
    - `tracked_files_visited:` the number of files the loop iterated over, taken as the line count of
      `git -C /workspaces ls-files` written to a file and counted separately, so a silently truncated
      scan is visible as a mismatch
    - `reading_1_total:` the number of matching lines the loop emitted
    - `reading_2_control_total:` the same loop with the pattern `henols/firestarter_prom`, whose
      non-zero result is what makes the readings above non-vacuous
    - `edited_subset:` the count of matching files this phase edits, followed by the list — `README.md`,
      `.planning/v1.4-e2e-verify.sh`, `.planning/v1.4-RELEASE-PROCEDURES.md`, and the seven
      `.planning/codebase/` documents owned by plans 02 and 03
    Then the full match list verbatim, in the loop's own order, each entry prefixed `EDITED ` when its
    path is in the edited subset and `RECORD ` otherwise.

    Close with a section headed exactly
    `What this enumeration deliberately does NOT cover`, stating the D-01 rule in one sentence — edit
    only what a reader could act on today; everything else under `.planning/` is a record — and naming
    the excluded classes: `.planning/milestones/` under D-5, the record files under `.planning/phases/`,
    `.planning/notes/`, `.planning/seeds/`, `.planning/todos/`, `.planning/debug/` and
    `.planning/v1.3x/`, and `.planning/PROJECT.md`. Note that the total will keep growing while this
    phase runs, because this phase's own planning records name the old slug, and that this is expected
    rather than a regression.

    Take the reading after Tasks 1 and 2 are committed so the edited files already read clean.
  </action>
  <acceptance_criteria>
    - The transcript carries exactly one `tracked_files_visited:` line, one `reading_1_total:` line, one `reading_2_control_total:` line and one `edited_subset:` line
    - `reading_2_control_total:` carries a non-zero integer, so the enumeration's own instrument is proved to match something
    - `tracked_files_visited:` equals the live tracked-file count, taken by writing `git ls-files` output to a file and counting that file, proving the NUL-delimited loop visited every tracked file rather than stopping early
    - Re-running the enumeration command twice in a row produces byte-identical output, so the recorded list is reproducible and its ordering is stable
    - The live enumeration line count equals the recorded `reading_1_total:` value
    - Every listed match is prefixed either `EDITED ` or `RECORD `, so no match is left unclassified
    - The transcript carries the section heading `What this enumeration deliberately does NOT cover`
    - `git status --porcelain -- .planning/milestones/` reports 0 lines
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -cE '^tracked_files_visited: [0-9]+$' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^reading_1_total: [0-9]+$' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^reading_2_control_total: [1-9][0-9]*$' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero — a missing line or a zero control means the enumeration proves nothing</fails_when>
    <automated>/usr/bin/grep -cE '^edited_subset: [0-9]+' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>git -C /workspaces ls-files > "${TMPDIR:-/tmp}/192-tracked.txt"; echo "lsfiles_rc=$?"; wc -l < "${TMPDIR:-/tmp}/192-tracked.txt"</automated>
    <fails_when>the rc line is not `lsfiles_rc=0`, or the printed count differs from the `tracked_files_visited:` value recorded in the transcript</fails_when>
    <automated>git -C /workspaces ls-files -z > "${TMPDIR:-/tmp}/192-tracked.z"; echo "lsfiles_z_rc=$?"</automated>
    <fails_when>the rc line is not `lsfiles_z_rc=0`</fails_when>
    <automated>while IFS= read -r -d '' f; do /usr/bin/grep -HnE 'henols/firestarter($|[^_A-Za-z0-9])' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-tracked.z" | wc -l</automated>
    <fails_when>prints a number that differs from the `reading_1_total:` value recorded in the transcript</fails_when>
    <automated>while IFS= read -r -d '' f; do /usr/bin/grep -HnE 'henols/firestarter($|[^_A-Za-z0-9])' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-tracked.z" | md5sum; while IFS= read -r -d '' f; do /usr/bin/grep -HnE 'henols/firestarter($|[^_A-Za-z0-9])' "/workspaces/$f"; done < "${TMPDIR:-/tmp}/192-tracked.z" | md5sum</automated>
    <fails_when>the two printed checksums differ, which means the enumeration is not order-stable and the recorded list is not reproducible</fails_when>
    <automated>/usr/bin/grep -cE '^(EDITED|RECORD) ' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints 0, exits non-zero, or prints a number smaller than the recorded `reading_1_total:` value — an unclassified match was dropped from the list</fails_when>
    <automated>/usr/bin/grep -cF 'What this enumeration deliberately does NOT cover' /workspaces/.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/milestones/; echo "milestones_status_rc=$?"</automated>
    <fails_when>any path is printed before the rc line, or the rc line is not `milestones_status_rc=0`</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>Every remaining tracked bare-slug match in the meta repository is listed once, classified EDITED or RECORD, with the instrument proved non-vacuous and the exclusion rule stated in the transcript itself. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → meta-repo working tree | The only write surface in this plan; three tracked files plus two evidence transcripts |
| executor → `.planning/milestones/` | An integrity boundary, not a write surface: 3440 tracked files that must remain byte-identical (D-5) |
| executor → GitHub REST API (`gh api`) | Read-only network read used as the end-to-end proof that the repointed URL is not a redirect |
| recorded evidence → future readers | A transcript is an assertion about a measurement; a vacuous reading crossing this boundary becomes a false claim of cleanliness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-192-01 | Tampering | `.planning/milestones/` archived records | high | mitigate | No task lists a `milestones/` path in `files_modified`; every task ends with a scoped `git status --porcelain -- .planning/milestones/` leg whose own exit status is echoed and which must print no paths |
| T-192-04 | Repudiation | Zero-count evidence legs in `192-slug-sweep.txt` and `192-01-enumeration.txt` | high | mitigate | Every zero is paired with a control leg that must read non-zero; the transcript states in prose that a zero/zero pair is a failed gate |
| T-192-05 | Tampering | `henols/firestarter_app` and `_prom` neighbour lines in the three edited files | medium | mitigate | Boundary-anchored `\b` substitution plus `awk` untouched-neighbours control legs pinned to the measured neighbour line numbers (4 / 3 / 5) |
| T-192-07 | Tampering | The `git ls-files` enumeration loop | medium | mitigate | NUL-delimited `ls-files -z` with `read -r -d ''`, plus a `tracked_files_visited:` control compared against a separately captured tracked-file count so a truncated scan is visible |
| T-192-08 | Spoofing | The repointed README URL | medium | mitigate | The end-to-end leg asserts `gh api repos/henols/firestarter_fw` returns that exact `full_name`, which a redirect target would not |
| T-192-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install; no third-party package is added, so the package-legitimacy gate has no subject. Accepted on the basis of zero install tasks, re-checked if a task is added |
</threat_model>

## Artifacts this phase produces

New files created by this plan (excluded from drift verification — they do not exist before it runs):

- `.planning/phases/192-live-references-only/evidence/192-slug-sweep.txt` — the phase's primary boundary-aware sweep transcript
- `.planning/phases/192-live-references-only/evidence/192-01-enumeration.txt` — D-01's one-time enumeration of the full match list with the edited subset marked
- `.planning/phases/192-live-references-only/evidence/` — the phase evidence directory itself
- `.planning/phases/192-live-references-only/192-01-SUMMARY.md` — this plan's summary

Existing files modified (not new symbols): `README.md`, `.planning/v1.4-e2e-verify.sh`,
`.planning/v1.4-RELEASE-PROCEDURES.md`.

<verification>
- The boundary-aware bare-slug count is 0 across all three edited files, each paired with a control reading that is non-zero over the same file set
- `gh api repos/henols/firestarter_fw --jq '.full_name'` returns `henols/firestarter_fw`
- The enumeration is reproducible: two consecutive runs of the enumeration command produce identical checksums
- `git status --porcelain -- .planning/milestones/` reports 0 lines
- `git branch --show-current` reports the milestone branch throughout
</verification>

<success_criteria>
Every meta-repository reference this plan owns — one in `README.md`, seven in
`.planning/v1.4-e2e-verify.sh`, six in `.planning/v1.4-RELEASE-PROCEDURES.md` — addresses
`henols/firestarter_fw`; the neighbouring `_app` and `_prom` references are byte-identical; the two
evidence transcripts each carry a non-zero control beside every zero; and the un-edited remainder of the
meta repository is enumerated once and classified.
</success_criteria>

<output>
Create `.planning/phases/192-live-references-only/192-01-SUMMARY.md` when done.
</output>
