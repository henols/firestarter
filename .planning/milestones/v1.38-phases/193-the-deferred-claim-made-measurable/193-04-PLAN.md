---
phase: 193-the-deferred-claim-made-measurable
plan: 04
type: execute
wave: 2
depends_on: ["193-03"]
files_modified:
  - .planning/notes/gitmodules-archaeology-trap.md
autonomous: true
requirements: [GATE-03]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The `.gitmodules` history trap has a dedicated note that a reader reaches from `CLAUDE.md`, carrying both workarounds and the transcripts that prove them were executed (D-15, D-16)."
    - "Each workaround is written as an ordered procedure of literal commands with the consequence of each step stated inline, and each cites the committed transcript that recorded its execution, by filename and by the reading within it."
    - "Where the `.gitmodules` value and the `.git/config` override AGREE, nothing observable distinguishes them and the override is invisible; where they DISAGREE, `.git/config` wins. The note states both halves, because a reader who sees two matching values cannot tell which one was in force."
    - "The note carries the `git submodule sync` hazard as a first-class warning, not a footnote: `sync` re-reads `.gitmodules` at the current HEAD and clobbers BOTH the `.git/config` override AND the child's own `origin` remote, so the repair needs two commands and a one-command repair silently half-works. The note names the neighbouring documented procedure that instructs `git submodule sync --recursive` as routine hygiene, which is what makes the hazard reachable."
    - "Nothing in the note claims a breakage that has not happened. It states that the old firmware slug still redirects today, that a plain `git submodule update --init` at a pre-rename ref therefore still succeeds, and that the failure the workarounds prevent arms only when the freed slug is claimed."
    - "The projected post-claim failure — a submodule update resolving the old URL to the META repository and cloning the parent into its own child directory — is presented as a reasoned projection, labelled as unverified and unverifiable while the redirect is live, and never as an observation."
    - "Every citation in the note is by path and section heading or by filename, never by line number, and every figure names the command that produced it."
  artifacts:
    - path: ".planning/notes/gitmodules-archaeology-trap.md"
      provides: "The GATE-03 note: the trap, both executed workarounds as ordered procedures, the sync hazard and its two-command repair, the banked transcripts, and the honest limits"
      contains: "## Honest limits"
  key_links:
    - from: ".planning/notes/gitmodules-archaeology-trap.md"
      to: ".planning/phases/193-the-deferred-claim-made-measurable/evidence/"
      via: "a banked-evidence section naming each transcript file and the reading inside it that supports each claim"
      pattern: "193-gate-03-"
    - from: ".planning/notes/gitmodules-archaeology-trap.md"
      to: ".planning/notes/999.9-repo-rename-impact-analysis.md"
      via: "citation by path and section heading of the analysis that first named the trap and prescribed the two workarounds"
      pattern: "999.9-repo-rename-impact-analysis.md"
  prohibitions:
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present the projected post-claim failure as something observed. It is unverifiable while the redirect is live, and the note must label it a projection rather than let a later reader inherit it as a measurement."
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT present a workaround this phase invented as one of the two the prior analysis already prescribes. A supplementary idiom may appear only if it is explicitly labelled as a convenience beside the two, never as a replacement for either."
    - requirement_id: GATE-03
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT document a workaround while omitting the neighbouring documented procedure that silently undoes it. A workaround a routine step reverses without warning is not a workaround."
    - requirement_id: GATE-03
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT cite a source file, note or procedure by line number. Line-numbered citations in this project have already gone stale; cite by section heading or by content."
---

<objective>
Write the note `CLAUDE.md` will point at: the `.gitmodules` history trap, both workarounds as ordered
procedures backed by the transcripts plan 193-03 executed, the hazard that silently undoes one of
them, and an explicit statement of what is projection rather than observation.

D-15 puts GATE-03's mechanism in a dedicated note rather than in `CLAUDE.md` itself, because its
reader — someone checking out an old tag or bisecting firmware history — is not the reader of the
close-procedure rules. D-16 makes the workarounds executions rather than arguments, and forbids
claiming a breakage that has not happened.

Purpose: GATE-03's "documented with a workaround demonstrated for both cases", and ROADMAP
criterion 4.
Output: `.planning/notes/gitmodules-archaeology-trap.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
evidence-bearing command in this plan uses the absolute `/usr/bin/grep`.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 carries a
trailing `|| true` and is judged on its printed number, not on its exit status.

Notes in `.planning/notes/` carry **no YAML frontmatter** — bold key/value lines only. Do not add a
frontmatter block to this file.

This plan writes prose only. It runs no `git` mutation and produces no new reading; every figure it
states must already exist in a transcript committed by plan 193-03.
</environment_notes>

<artifacts_this_phase_produces>
Symbols created by phase 193. Newly-created names are not drift against any existing source.

- `tools/adoption/` and `tools/adoption/pypi_version_share.sh` — new; no arguments, no CLI flags.
  Stdout keys `window:`, `fixed_downloads_ge_2_0_9:`, `at_risk_downloads_2_0_7:`,
  `fixed_share_pct:`, `threshold:`, `TRIGGER:`, banner `WHAT THIS DOES NOT MEASURE`; exit codes
  0, 1, 2.
- `.planning/notes/gitmodules-archaeology-trap.md` — **new note, created by this plan.** Its
  headings: `## The trap`, `## Workaround A — an existing clone`,
  `## Workaround B — a fresh clone at a pre-rename ref`, `## A one-shot variant, supplementary`,
  `## The sync hazard`, `## Banked evidence — the executed transcripts`, `## Honest limits`. Its
  bold key lines: `**Date:**`, `**Raised during:**`, `**Status:**`, `**Method:**`.
- `.planning/phases/193-the-deferred-claim-made-measurable/evidence/` — new directory holding
  `193-gate-01-instrument-run.txt`, `193-gate-03-fresh-clone.txt`,
  `193-gate-03-existing-clone.txt`, `193-gate-03-submodule-sync-hazard.txt` and
  `193-disposition.md`.
- `.planning/phases/193-the-deferred-claim-made-measurable/COVERAGE.md` — planner-authored.
- `CLAUDE.md` gains one bullet and one paragraph (plan 193-05). No new headings.
- `.planning/seeds/SEED-claim-firestarter-slug.md` keeps its four frontmatter keys; no new key.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The trap and both workarounds, as ordered procedures</name>
  <reversibility rating="reversible">One new note with a single citing consumer added in a later plan; reverting is deleting a file.</reversibility>
  <read_first>
    - /workspaces/.planning/notes/v135-close-procedure-under-protection.md — the primary analog: the note `CLAUDE.md` already cites, for its header key lines, its `##` heading style and its transcript-then-prose pattern
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — the archaeology-trap section, which prescribes the two workarounds, and the `**Method:**` header key and ordered-procedure shape
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-fresh-clone.txt — the executed fresh-clone transcript this section cites
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-existing-clone.txt — the executed existing-clone transcript this section cites
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-2, §F-3, §F-5 and §Code Examples 4, 5 and 7
  </read_first>
  <files>.planning/notes/gitmodules-archaeology-trap.md</files>
  <action>
    Create `.planning/notes/gitmodules-archaeology-trap.md`. No YAML frontmatter — the sibling notes
    use bold key lines only.

    **Header.** A `#` title naming the trap, then the four bold key lines in the shape of the two
    sibling notes: `**Date:**` the execution date; `**Raised during:**` the requirement and milestone
    context in the form the siblings use; `**Status:**` a one-line statement that the trap cannot be
    solved and is documented with two executed workarounds; and `**Method:**` in the 999.9 form —
    direct execution in disposable clones on the stated date, with every figure reproducible from the
    command noted beside it.

    **`## The trap`.** State the mechanism: `.gitmodules` records a submodule URL per commit, so
    fixing it on the live branches does not fix history. Checking out an old tag, bisecting firmware
    history or reading any pre-rename commit resurrects the old URL from that commit. Name the
    concrete ref a reader will actually hit — `v1.35`, whose `.gitmodules` still declares the old
    firmware slug — and state that this project does a great deal of archaeology, so the trap will be
    met. State plainly, in the same section, that **the trap does not bite today**: the old slug
    still redirects, and a plain `git submodule update --init` at a pre-rename ref succeeds, fetching
    through the redirect. The workarounds exist for the state that arrives when the freed slug is
    claimed. Bold the load-bearing negative, in the house style — the single clause a skimming reader
    must not miss.

    **`## Workaround A — an existing clone`.** The case where a clone already exists and travels back
    to a pre-rename ref. Write it as a numbered ordered procedure of literal commands with the
    consequence of each stated inline, in the shape of the 999.9 note's ordered procedure: set
    `git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git` in `.git/config`,
    then check out the old ref, then `git submodule update --init`. State the mechanism that makes it
    work: when `.gitmodules` and `.git/config` **disagree**, `.git/config` wins; when they **agree**,
    nothing observable distinguishes them and the override is invisible — which is why a reader who
    sees two matching values cannot tell which one was in force, and why the transcript's
    discriminating reading is the one taken at the pre-rename ref. Cite
    `evidence/193-gate-03-existing-clone.txt` by filename and name the reading that shows the
    divergence.

    **`## Workaround B — a fresh clone at a pre-rename ref`.** The case where there is no clone yet.
    Ordered procedure: `git clone --no-recurse-submodules`, `git checkout <pre-rename ref>`,
    `git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git`, then
    `git submodule update --init --depth 1 firestarter`. State that **the order is load-bearing**:
    `git submodule init` preserves an existing `submodule.<name>.url` rather than re-copying it from
    `.gitmodules`, so the override must be set before the update, and setting it afterwards would
    already have cloned from the wrong URL. State why the ref to demonstrate against is the published
    `v1.35` tag and not a milestone-branch commit: local milestone-branch shas are unreachable from a
    fresh clone, so a procedure written against one cannot be followed by the person who needs it.
    Cite `evidence/193-gate-03-fresh-clone.txt` by filename, and name its second reading as the one
    recording today's un-worked-around success.

    **`## A one-shot variant, supplementary`.** A short section carrying the non-persisting idiom
    `git -c submodule.firestarter.url=… submodule update --init --depth 1 firestarter`, which checks
    out the right repository and leaves `.git/config` unset. Label it unambiguously as a convenience
    for a single archaeology checkout, **beside** the two workarounds above and not a replacement for
    either — the two above are the ones the prior analysis prescribes and the ones this phase
    executed.

    Every citation in the file is by path and section heading or by filename. No line numbers: a
    line-numbered citation in this project has already gone stale, and the project's own house idiom
    is to cite by content.

    Commit the note on the current branch.
  </action>
  <acceptance_criteria>
    - The file exists, has no YAML frontmatter, and its first line is a `#` title
    - It carries the four bold key lines `**Date:**`, `**Raised during:**`, `**Status:**` and `**Method:**`
    - It carries the headings `## The trap`, `## Workaround A — an existing clone`, `## Workaround B — a fresh clone at a pre-rename ref` and `## A one-shot variant, supplementary`
    - `## The trap` states that a plain submodule update at a pre-rename ref succeeds today because the old slug still redirects
    - Workaround A states both halves of the precedence rule — `.git/config` wins on disagreement, and is invisible on agreement
    - Workaround B states that the override must be set before `git submodule update --init`, and why the published tag is used rather than a milestone-branch commit
    - The supplementary section labels the one-shot idiom as a convenience beside the two workarounds, not a replacement
    - Both transcripts are cited by filename
    - No citation anywhere in the file carries a `path:NN` or `path:NN-MM` line reference
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c -E '^## (The trap|Workaround A — an existing clone|Workaround B — a fresh clone at a pre-rename ref|A one-shot variant, supplementary)$' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints any number other than 4, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c -E '^\*\*(Date|Raised during|Status|Method):\*\*' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints any number other than 4, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c '^---$' /workspaces/.planning/notes/gitmodules-archaeology-trap.md || true</automated>
    <fails_when>prints a number of 2 or more — the sibling notes carry no YAML frontmatter and a parser-visible block here would break the convention `CLAUDE.md` cites into</fails_when>
    <automated>/usr/bin/grep -c -F -e '193-gate-03-existing-clone.txt' -e '193-gate-03-fresh-clone.txt' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints a number below 2, or exits non-zero — a workaround whose transcript is not named cannot be checked by the reader who needs it</fails_when>
    <automated>/usr/bin/grep -c -F -e 'git clone --no-recurse-submodules' -e 'git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git' -e 'git submodule update --init' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints a number below 3, or exits non-zero — the ordered procedures must carry literal commands, not descriptions of commands</fails_when>
    <automated>/usr/bin/grep -cE '\.(md|py|sh|cpp|h):[0-9]+' /workspaces/.planning/notes/gitmodules-archaeology-trap.md || true</automated>
    <fails_when>prints any number other than 0 — a line-numbered citation is the staleness this project's house idiom exists to avoid</fails_when>
  </verify>
  <done>A reader who lands on the note from `CLAUDE.md` can follow either workaround as a literal, ordered command sequence, knows which ref to use and why, knows that the order of the override and the update is load-bearing, and is told plainly that the failure these prevent has not happened yet.</done>
</task>

<task type="auto">
  <name>Task 2: The hazard, the banked evidence, and what is projection</name>
  <read_first>
    - /workspaces/.planning/notes/gitmodules-archaeology-trap.md — the file being extended; read what task 1 wrote so the register and heading style continue
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/evidence/193-gate-03-submodule-sync-hazard.txt — the executed reproduction and repair this section reports
    - /workspaces/.planning/notes/v135-close-procedure-under-protection.md — the transcript-then-prose pattern and the `## Banked evidence — …` heading form
    - /workspaces/.planning/notes/999.9-repo-rename-impact-analysis.md — its `## Honest limits` heading, its ordered procedure whose step instructing `git submodule sync --recursive` makes the hazard reachable, and its projected post-claim failure description
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-4, §F-5 and Assumption A3
  </read_first>
  <files>.planning/notes/gitmodules-archaeology-trap.md</files>
  <action>
    Extend the note with the three sections that make it safe to follow.

    **`## The sync hazard`.** Open with the statement of consequence: running `git submodule sync`
    while HEAD is at a pre-rename ref **silently destroys** the workaround. Embed the captured
    before-and-after readings from `evidence/193-gate-03-submodule-sync-hazard.txt` as a fenced block
    in the v1.35-note pattern, then follow immediately with prose identifying which lines are the
    discriminators and why the others prove nothing: `sync` re-reads `.gitmodules` at the current
    HEAD, so it overwrites `.git/config`'s `submodule.firestarter.url` **and** rewrites the child's
    own `origin` remote, and a reader who checks only the first of those two would conclude a
    one-command repair worked when it half did. Give the repair as an ordered pair of literal
    commands — the `git config submodule.firestarter.url` set and the
    `git -C firestarter remote set-url origin` set — and state that re-running `sync` at the same
    HEAD immediately re-clobbers both, so this is a repair and not a fix; the durable answer at a
    pre-rename ref is not to run `sync` there. Name the neighbouring documented procedure that
    instructs `git submodule sync --recursive` as routine hygiene, citing it by note path and section
    heading, and say that this is what makes the hazard reachable by someone doing everything right.

    **`## Banked evidence — the executed transcripts`.** One entry per transcript, naming the file and
    saying in one line what it proves and which reading carries the proof:
    `193-gate-03-fresh-clone.txt`, `193-gate-03-existing-clone.txt` and
    `193-gate-03-submodule-sync-hazard.txt`. State that each carries its own capture date and the
    commands that produced it, so a later reader re-runs rather than trusts.

    **`## Honest limits`.** Three statements, each labelled for what it is:
    (a) **The trap does not bite today.** The old firmware slug still redirects; a plain
    `git submodule update --init` at a pre-rename ref succeeds. The workarounds are for the state
    that arrives when the freed slug is claimed, and that state has not arrived.
    (b) **The post-claim failure shape is a projection, not an observation.** The prior analysis
    reasons that after the claim, `git submodule update --init` resolves the old URL to the meta
    repository and clones the parent into its own child directory. That is unverified and unverifiable
    while the redirect is live, and it is deliberately not tested, because testing it would require
    performing the destructive act this milestone declines to perform. Present it as a projection and
    label it so.
    (c) **History cannot be repaired.** Fixing `.gitmodules` on the live branches does not change what
    any past commit records, and rewriting history across a repository with published tags and two
    submodules is not on the table. The trap is documented and worked around, not solved.

    Every claim in these three sections must already exist in a committed transcript or be labelled a
    projection. Do not introduce a new figure here.

    Commit the note on the current branch.
  </action>
  <acceptance_criteria>
    - The file carries the headings `## The sync hazard`, `## Banked evidence — the executed transcripts` and `## Honest limits`
    - `## The sync hazard` embeds a fenced block of captured output followed by prose naming the discriminating lines
    - Both repair commands appear as literal commands, and the note states that re-running `sync` re-clobbers both
    - The note names the neighbouring procedure that instructs `git submodule sync --recursive`, cited by note path and section heading
    - All three transcript filenames appear in the banked-evidence section
    - `## Honest limits` states that the trap does not bite today, labels the post-claim failure shape a projection, and states that history cannot be repaired
    - The word describing the post-claim failure is `projection` or `projected`, not `observed` or `measured`
    - No citation anywhere in the file carries a `path:NN` line reference, and the note still has no YAML frontmatter
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c -E '^## (The sync hazard|Banked evidence — the executed transcripts|Honest limits)$' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints any number other than 3, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c -F -e '193-gate-03-fresh-clone.txt' -e '193-gate-03-existing-clone.txt' -e '193-gate-03-submodule-sync-hazard.txt' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints a number below 3, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -c -F -e 'git config submodule.firestarter.url git@github.com:henols/firestarter_fw.git' -e 'git -C firestarter remote set-url origin git@github.com:henols/firestarter_fw.git' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints a number below 2, or exits non-zero — a repair missing either command leaves the clone half-repaired</fails_when>
    <automated>/usr/bin/grep -cF 'git submodule sync --recursive' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints 0, or exits non-zero — the routine step that undoes the workaround must be named, or the warning has no referent</fails_when>
    <automated>awk '/^## Honest limits$/,0' /workspaces/.planning/notes/gitmodules-archaeology-trap.md | /usr/bin/grep -ciE 'projection|projected'</automated>
    <fails_when>prints 0, or exits non-zero — an unlabelled post-claim description is inherited by the next reader as a measurement</fails_when>
    <automated>awk '/^## Honest limits$/,0' /workspaces/.planning/notes/gitmodules-archaeology-trap.md | /usr/bin/grep -ciE 'observed (breakage|failure)|we measured the post-claim' || true</automated>
    <fails_when>prints any number other than 0 — the post-claim failure has not been observed and must not be written as if it had</fails_when>
    <automated>/usr/bin/grep -c '^```' /workspaces/.planning/notes/gitmodules-archaeology-trap.md</automated>
    <fails_when>prints an odd number, or a number below 2, or exits non-zero — an odd count means an unclosed fence, and fewer than two means no captured output was embedded</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>The note warns about the routine command that undoes its own advice, points at the three committed transcripts by name, and separates what was executed from what is reasoned — so nobody inherits the projected failure as a measured one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the note → a future operator recovering a broken clone | The note is the only instruction that reader has, and a wrong step costs them a working checkout |
| the note → a future agent reading it as fact | A projection written as an observation propagates as a measurement |
| the note → the transcripts it cites | A claim whose evidence file does not carry it is an unbacked assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-40 | Tampering | a reader's clone following `## Workaround A` or `## Workaround B` | high | mitigate | The hazard section names `git submodule sync` as the command that silently clobbers both the config override and the child's `origin`, gives the two-command repair literally, and states that re-running `sync` re-clobbers. Verified by a grep requiring both repair commands and the routine command that causes the damage. |
| T-193-41 | Repudiation | the projected post-claim failure | medium | mitigate | `## Honest limits` labels it a projection and states it is unverifiable while the redirect is live. A grep requires a projection word in that section and forbids observation phrasing. |
| T-193-42 | Spoofing | claims not backed by a transcript | medium | mitigate | Every figure in the note must already exist in a transcript committed by plan 193-03; the banked-evidence section names all three files, and this plan introduces no new reading. |
| T-193-43 | Information disclosure | the note's embedded output block | low | accept | The embedded readings are repository URLs and git's own status lines, already public. Accepted because nothing confidential exists in the captured material and the transcripts it quotes were themselves screened. |
| T-193-SC | Tampering | npm/pip/cargo installs | high | mitigate | No plan in this phase runs a package-manager install. 193-RESEARCH.md records the Package Legitimacy Audit as not applicable; this plan writes one markdown file. |
</threat_model>

<verification>
- Both workarounds appear as ordered procedures of literal commands, each citing the transcript that recorded its execution.
- The sync hazard, its two damaged locations and its two-command repair are all present.
- The post-claim failure is labelled a projection and no section describes it as observed.
- No line-numbered citation appears anywhere in the file.
</verification>

<success_criteria>
`.planning/notes/gitmodules-archaeology-trap.md` exists, carries the seven named headings, cites
all three transcripts by filename, states both halves of the config-versus-gitmodules precedence
rule, warns about `git submodule sync` with a two-command repair, and separates execution from
projection under `## Honest limits`.
</success_criteria>

<output>
Create `.planning/phases/193-the-deferred-claim-made-measurable/193-04-SUMMARY.md` when done.
Record which claims came from which transcript, and the rationale this plan deliberately keeps out
of the note.
</output>
