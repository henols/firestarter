---
phase: 193-the-deferred-claim-made-measurable
plan: 02
type: execute
wave: 2
depends_on: ["193-01"]
files_modified:
  - .planning/seeds/SEED-claim-firestarter-slug.md
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 17000
  raw_tokens: 34000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "The seed's `trigger_condition:` frontmatter states a threshold and an observation window rather than 'once adoption has moved' — it carries D-09's numbers: a fixed share of `(>= 2.0.9) / ((>= 2.0.9) + 2.0.7)` at or above 90%, AND 2.0.7 drawing 10 or fewer pip and uv downloads, both measured over a rolling 90-day window on the stable channel (D-12)."
    - "The rewritten frontmatter still parses. All four keys survive — `title`, `trigger_condition`, `planted_date`, `status` — and `status` still reads `dormant`, so the seed stays visible to the audit scanner that reads it and to the `/gsd-new-milestone` glob that surfaces it. The `trigger_condition` scalar does not begin with a backtick, which is a YAML reserved indicator that makes the whole block unparseable and silently drops every key."
    - "`planted_date: 2026-09-13` is unchanged — it records when the seed was planted, not when it was edited."
    - "The seed names the instrument that evaluates the trigger by path, so a reader a year from now runs a committed command instead of re-deriving a query (D-01, D-03)."
    - "One reading suffices (D-10) and the trigger never fires the act (D-11): the seed states that a single 90-day reading is the evidence, that a human decision stands between the reading and the rename, and that 2027-09-13 is a date on which the PREMISE is re-examined — not a date on which anything fires. The existing sentence 'Gate on that stable having shipped and having displaced 2.0.7 — not on a calendar date' is preserved intact, and the review point is added as a separate clause beside it rather than replacing it."
    - "The tense is corrected throughout (D-12): the stable carrying the `firestarter_fw` URLs HAS shipped — it is 2.0.9, published 2026-09-13 — and no sentence in the seed still places that event in the future. 2.0.8 never reached PyPI, so the seed names 2.0.9 and does not name 2.0.8 as the cut point."
    - "No checklist leg claims a satisfaction a reader can falsify with one `git show`. The `.gitmodules` leg is scoped to the ref where it is true: `origin/main` carries `firestarter_fw` and `origin/beta` still carries the old URL until this milestone merges, and the leg says so by naming the ref."
    - "The seed's `## Known residual, accepted` section and the instrument's caveat agree rather than contradict: users who never upgrade are unreachable by any sequencing, and no threshold reduces that residual to zero."
  artifacts:
    - path: ".planning/seeds/SEED-claim-firestarter-slug.md"
      provides: "The dormant seed carrying the destructive act, with an evaluable trigger, a named instrument, a re-examination date and a ref-scoped readiness checklist"
      contains: "trigger_condition:"
  key_links:
    - from: ".planning/seeds/SEED-claim-firestarter-slug.md"
      to: "tools/adoption/pypi_version_share.sh"
      via: "the seed names the committed instrument by path as the way its trigger is evaluated"
      pattern: "tools/adoption/pypi_version_share.sh"
    - from: ".planning/seeds/SEED-claim-firestarter-slug.md"
      to: "GSD's frontmatter parser and seed scanner"
      via: "a parse-safe trigger_condition scalar keeping status: dormant readable"
      pattern: "status: dormant"
  prohibitions:
    - requirement_id: GATE-01
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT state or imply that a met trigger authorises the rename. The trigger is a measurement; the act stays operator-gated, and the re-examination date re-opens the premise rather than firing anything."
    - requirement_id: GATE-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT assert a readiness leg as satisfied on a ref where a reader can falsify it with one command. A leg that is true on one branch and false on another must name the branch."
    - requirement_id: GATE-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT soften the accepted residual. The seed already records that users who never upgrade are unreachable by any sequencing and that the claim permanently breaks `fw` for them; the trigger's arrival does not change that and no rewritten sentence may suggest it does."
---

<objective>
Turn the seed's trigger from a judgement call into a number someone can evaluate, and correct the
tense and the version references it was planted with the day before the stable shipped.

This plan implements D-12 (the `trigger_condition:` frontmatter carries D-09's numbers; the prose
sections are brought into agreement; the 2.0.7 to 2.0.9 corrections are applied throughout), D-09
(the numbers themselves), D-10 (one 90-day reading, not two consecutive) and D-11 (a re-examination
date, never an auto-fire).

It depends on 193-01 because the seed names the committed instrument by path, and a dangling
citation in a dormant record is exactly the kind of stale reference this milestone spent Phase 192
removing.

Purpose: ROADMAP criterion 1 — "the seed's trigger names a threshold and an observation window
rather than 'once adoption has moved'".
Output: `.planning/seeds/SEED-claim-firestarter-slug.md`, rewritten in frontmatter and in two prose
sections, still parsing and still dormant.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md
@.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md
</context>

<environment_notes>
PATH `grep` in this devcontainer is ugrep and honours `.gitignore`, so it silently under-scans. Every
evidence-bearing command in this plan uses the absolute `/usr/bin/grep`.

`/usr/bin/grep -c` exits 1 when the count is 0, so a leg that legitimately expects 0 carries a
trailing `|| true` and is judged on its printed number, not on its exit status.

`node` resolves at `/usr/local/share/nvm/current/bin/node` in this container. Use that absolute path
in the parse assertion rather than relying on PATH.

The three published refs and what their `.gitmodules` firmware URL currently says, verified by the
orchestrator on 2026-09-14 with `git show <ref>:.gitmodules`: `origin/main` and the local milestone
branch name `firestarter_fw`; `origin/beta` and the `v1.35` tag still name the old slug. Nothing in
this plan changes that — the seed must describe it accurately.
</environment_notes>

<artifacts_this_phase_produces>
Symbols created by phase 193. Newly-created names are not drift against any existing source.

- `tools/adoption/` and `tools/adoption/pypi_version_share.sh` — new; the script takes no arguments
  and defines no CLI flags. Its stdout keys are `window:`, `fixed_downloads_ge_2_0_9:`,
  `at_risk_downloads_2_0_7:`, `fixed_share_pct:`, `threshold:`, `TRIGGER:` and the banner
  `WHAT THIS DOES NOT MEASURE`; its exit codes are 0, 1 and 2.
- `.planning/notes/gitmodules-archaeology-trap.md` — new note (plan 193-04).
- `.planning/phases/193-the-deferred-claim-made-measurable/evidence/` — new directory holding
  `193-gate-01-instrument-run.txt`, `193-gate-03-fresh-clone.txt`, `193-gate-03-existing-clone.txt`,
  `193-gate-03-submodule-sync-hazard.txt` and `193-disposition.md`.
- `.planning/phases/193-the-deferred-claim-made-measurable/COVERAGE.md` — planner-authored.
- `CLAUDE.md` gains one bullet in `## Milestone close and branch protection` and one paragraph in
  `## Repository Structure` (plan 193-05). No new headings.
- `.planning/seeds/SEED-claim-firestarter-slug.md` keeps its four existing frontmatter keys and
  its six existing body headings. **No new frontmatter key and no new heading is introduced by this
  plan** — only the values and the prose beneath them change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The trigger becomes a number, and the frontmatter is proved to still parse</name>
  <reversibility rating="reversible">Frontmatter in one dormant seed file with no runtime consumer; reverting is `git checkout` of one file (D-12 records the same rating).</reversibility>
  <read_first>
    - /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md — the file being modified; read all 53 lines, including the four frontmatter keys and their order
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-7 for the parser's five-shape table and the verified folded-scalar form, and §Common Pitfalls 6
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md — D-08, D-09, D-10, D-11 and D-12
    - /workspaces/.planning/seeds/phase-gate-expiry-discipline.md — the sibling seed whose expiry-discipline question D-11 answers for this gate
  </read_first>
  <files>.planning/seeds/SEED-claim-firestarter-slug.md</files>
  <action>
    Rewrite the `trigger_condition:` value and prove the block still parses.

    **Shape.** Keep all four keys and their existing order: `title`, `trigger_condition`,
    `planted_date`, `status`. Leave `title`, `planted_date` and `status` byte-identical —
    `planted_date: 2026-09-13` records when the seed was planted, not when it was edited, and
    `status: dormant` is what the audit scanner reads.

    **The value.** Write `trigger_condition:` as a folded block scalar — the key, a space, then `>-`,
    then the value on continuation lines indented two spaces. The folded form is verified to parse
    and to round-trip the value with newlines folded to spaces. The value must **begin with a word**,
    not with a backtick: a value whose first character is a backtick makes the entire frontmatter
    block unparseable, the parser returns an empty object, every key including `status` is lost, and
    the seed silently disappears from the audit because the scanner's failure direction is open.
    Mid-string backticks are verified safe, and an embedded colon-space is repaired by the parser, so
    the only constraint is the first character.

    **The content**, carrying D-09's numbers so no later agent re-derives them: a fixed share of
    `(>= 2.0.9) / ((>= 2.0.9) + 2.0.7)` at or above 90%, AND `2.0.7` drawing 10 or fewer `pip` plus
    `uv` downloads, both measured over a rolling 90-day window on the stable channel. It must also
    carry, in the same scalar: that a single reading suffices (D-10); that 2027-09-13 is a
    re-examination date for the premise and never an auto-fire (D-11); and the path
    `tools/adoption/pypi_version_share.sh` as the instrument that produces the reading.

    **Then prove the parse.** Run GSD's own `extractFrontmatter` from
    `.claude/gsd-core/bin/lib/frontmatter.cjs` against the edited file and confirm four keys come
    back and `status` reads `dormant`. A rewrite that silently unhooks the seed from its consumer
    would defeat this phase; do not assume the parse, assert it.

    Commit the seed on the current branch.
  </action>
  <acceptance_criteria>
    - `extractFrontmatter` over the edited file returns exactly 4 keys and `status` reads `dormant`
    - The returned `trigger_condition` string is non-empty and its first character is not a backtick
    - The returned `trigger_condition` string contains `90`, `10`, `90-day`, `2.0.9`, `2.0.7`, `2027-09-13` and `tools/adoption/pypi_version_share.sh`
    - The frontmatter block is still delimited by two `---` lines and holds no fifth key
    - `planted_date: 2026-09-13` and `status: dormant` appear verbatim and unchanged
    - The file still carries its six body headings and its trailing `Full analysis:` line
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/share/nvm/current/bin/node -e 'const{extractFrontmatter}=require("/workspaces/.claude/gsd-core/bin/lib/frontmatter.cjs");const fm=extractFrontmatter(require("fs").readFileSync("/workspaces/.planning/seeds/SEED-claim-firestarter-slug.md","utf8"));const t=String(fm.trigger_condition||"");console.log("keys="+Object.keys(fm).length+" status="+fm.status+" trigger_len="+t.length+" first_is_backtick="+(t.charAt(0)==="`"))'</automated>
    <fails_when>the printed line is not exactly `keys=4 status=dormant trigger_len=<n> first_is_backtick=false` with `<n>` greater than 0 — `keys=0` means the whole block became unparseable, `status=undefined` means the audit scanner can no longer see the seed</fails_when>
    <automated>/usr/local/share/nvm/current/bin/node -e 'const{extractFrontmatter}=require("/workspaces/.claude/gsd-core/bin/lib/frontmatter.cjs");const t=String(extractFrontmatter(require("fs").readFileSync("/workspaces/.planning/seeds/SEED-claim-firestarter-slug.md","utf8")).trigger_condition||"");const need=["90","10","90-day","2.0.9","2.0.7","2027-09-13","tools/adoption/pypi_version_share.sh"];console.log(need.map(function(n){return n+"="+(t.indexOf(n)>=0)}).join(" "))'</automated>
    <fails_when>any printed pair ends in `=false`, or the command exits non-zero — a `false` means D-09's stated number, the window, the instrument path or the review date did not survive into the parsed value</fails_when>
    <automated>/usr/bin/grep -c -E '^(planted_date: 2026-09-13|status: dormant)$' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints any number other than 2, or exits non-zero</fails_when>
    <automated>/usr/bin/grep -cE '^trigger_condition: *`' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md || true</automated>
    <fails_when>prints any number other than 0 — a value whose first character is a backtick makes the whole block unparseable and drops every key</fails_when>
    <automated>/usr/bin/grep -c '^## ' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints any number other than 5, or exits non-zero — the body's five second-level headings must survive the rewrite</fails_when>
    <automated>/usr/bin/grep -cF 'Full analysis: `.planning/notes/999.9-repo-rename-impact-analysis.md`.' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero</fails_when>
  </verify>
  <done>The seed's trigger is a threshold and an observation window that someone can evaluate with one committed command, and GSD's own parser confirms the seed is still readable and still dormant.</done>
</task>

<task type="auto">
  <name>Task 2: The prose catches up with what actually shipped</name>
  <read_first>
    - /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md — the file being modified; read the rewritten frontmatter from task 1 and all five body sections
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-CONTEXT.md — D-02's caveat framing, D-09's rationale and baseline, D-11's review point, D-12's correction pass
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-RESEARCH.md — §F-2's published-ref table and its side finding on the third checklist leg, and §Open Questions 2
    - /workspaces/.planning/phases/193-the-deferred-claim-made-measurable/193-PATTERNS.md — the seed's prose conventions: short paragraphs, bold on the operative word only, the trailing unheaded `Full analysis:` line
  </read_first>
  <files>.planning/seeds/SEED-claim-firestarter-slug.md</files>
  <action>
    Bring the body into agreement with the frontmatter and with what has actually shipped. Keep the
    five existing `##` headings, their order, and the trailing unheaded `Full analysis:` line. Hold
    the file's prose conventions: short paragraphs, bold reserved for the operative word, backticks
    around every path, branch, command and version token.

    **`## Why the trigger is what it is`** — rewrite this section. It currently puts the stable's
    arrival in the future tense, which was true when the seed was planted on 2026-09-13 and is not
    true now. The corrected content:
    - The stable carrying the `firestarter_fw` URLs **has** shipped. It is **2.0.9**, published to
      PyPI on 2026-09-13. Name 2.0.9, and do not name 2.0.8 as the cut point — 2.0.8 never reached
      PyPI.
    - `pip install firestarter` served **2.0.7** from 2026-01-13 to 2026-09-13, so 2.0.7 — and not a
      band of everything below it — is the at-risk population the trigger keys on (D-07). State
      plainly that the pre-2.0.7 tail is automation rather than stranded humans and is outside the
      instrument's reach: by quarter it runs 398 / 254 / 320 / 265 / 52 from 2025-Q3 to 2026-Q3,
      holding at 250 to 400 even in quarters before 2.0.7 existed.
    - Why the threshold is a share **AND** an absolute floor (D-08): a ratio says nothing about how
      many people are behind it, and the thing that breaks is a count of humans.
    - Why the window is 90 days and not 30 (D-09): monthly stable-channel totals show scraper spikes
      — 342 in January and 312 in May against a 34 to 75 baseline — and a 30-day window is swamped by
      one scraper pass.
    - Why these values (D-09): the 2.0.6 to 2.0.7 transition is the natural experiment. Share crossed
      90% in about six weeks and then plateaued against a thin tail that never reached zero, so this
      threshold would have fired roughly three months after that cut.
    - The baseline at authoring time, stated plainly so a reader knows how far away this is
      (D-09): on 2026-09-14, over the 90-day window, pip plus uv, stable channel — fixed **17**,
      2.0.7 **116**, older-stable tail **52**, fixed share **12.8%**. Nowhere near firing.
    - The instrument, by path: `tools/adoption/pypi_version_share.sh`. One reading over the 90-day
      window is the evidence; a second consecutive reading is not required, because the window
      already damps the spikes named above and overlapping 90-day windows make "consecutive"
      ambiguous (D-10).
    - What the reading cannot establish, in the strong form and not a weaker paraphrase: installed
      base is not observable, download share is a proxy, a download of a stranded version today is a
      **new acquisition** of an old version, and a user who installed 2.0.7 and never reinstalls
      generates zero downloads. The threshold therefore certifies that new acquisition of stranded
      versions has effectively stopped — a necessary condition, never a sufficient one.
    - **Keep the existing sentence** that ends the section — the one stating the gate is on that
      stable having shipped and having displaced 2.0.7 rather than on a calendar date — **verbatim
      and intact**. Add D-11's review point as a separate clause beside it: if the trigger has not
      fired by **2027-09-13**, twelve months from 2.0.9's publish, the **premise** is re-examined.
      That date never fires the act; it re-opens the question of whether the threshold was right,
      whether the front-door problem was solved another way, or whether 2.0.7 acquisition is
      structural.

    **`## Do not fire this while any of these is untrue`** — keep the three legs and their order,
    and correct their claims.
    - Leg 1 (the three `FIRESTARTER_*_URL` constants on both `beta` and `main`): now satisfied. Say
      so.
    - Leg 2 (a stable carrying the `main` fix published to PyPI): now satisfied, and it is 2.0.9.
      Say so, and drop the future tense.
    - Leg 3 (`.gitmodules` and `git submodule sync --recursive`): **scope it to the ref where it is
      true.** `origin/main` carries `firestarter_fw`; `origin/beta` still carries the old URL and
      will until the v1.38 milestone merges. Name both states. Do not write that all three legs are
      satisfied without qualification — a reader can falsify that with one `git show
      origin/beta:.gitmodules`, and shipping a false checklist leg inside the artefact that exists to
      make a deferred claim honest would be self-defeating.
    Add one sentence, immediately after the three legs, stating that a met trigger is a measurement
    and not an authorisation: a human decides, and this seed does not fire itself.

    **`## Known residual, accepted`** — leave its substance intact. It already records that users who
    never upgrade are unreachable by any sequencing and that the claim permanently breaks `fw` for
    them. Confirm it does not contradict the caveat language added above; if it does, reconcile in
    favour of the stronger statement.

    Leave `## Why it is separable` and `## Carry this rule forward when it fires` as they are, except
    to correct any version reference the corrections above make stale.

    Add no comment, no banner and no process citation. Commit the seed on the current branch.
  </action>
  <acceptance_criteria>
    - `## Why the trigger is what it is` names `2.0.9` and states that the stable has shipped, in the past tense
    - The section carries the baseline figures 17, 116, 52 and 12.8, and the window length 90
    - The section carries the re-examination date `2027-09-13` and states that the date re-examines the premise rather than firing the act
    - The sentence stating the gate is on displacement and not on a calendar date survives verbatim
    - The section names `tools/adoption/pypi_version_share.sh`
    - The caveat language carries `necessary condition, never a sufficient one` and the phrase `new acquisition`
    - `## Do not fire this while any of these is untrue` still holds exactly three bullet legs, and the `.gitmodules` leg names both `origin/main` and `origin/beta`
    - No sentence in the file places the stable's publication in the future, and `2.0.8` appears nowhere
    - The file's five `##` headings and the trailing `Full analysis:` line are unchanged
    - The frontmatter still parses to four keys with `status` reading `dormant` after this task's edits
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -c -F -e '2.0.9' -e '2027-09-13' -e 'tools/adoption/pypi_version_share.sh' -e '12.8' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints a number below 4, or exits non-zero — a shortfall means the shipped stable, the review date, the instrument path or the authoring baseline is absent</fails_when>
    <automated>/usr/bin/grep -cF 'not on a calendar date' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero — D-11's review point must be added beside this sentence, never in place of it</fails_when>
    <automated>/usr/bin/grep -cF 'necessary condition, never a sufficient one' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md</automated>
    <fails_when>prints any number other than 1, or exits non-zero — a weaker paraphrase is a regression against ROADMAP criterion 2</fails_when>
    <automated>awk '/^## Do not fire this while any of these is untrue$/,/^## Carry this rule forward/' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md | /usr/bin/grep -c -F -e 'origin/main' -e 'origin/beta'</automated>
    <fails_when>prints a number below 2, or exits non-zero — the `.gitmodules` leg must name the ref on which it is true and the ref on which it is not</fails_when>
    <automated>/usr/bin/grep -cF '2.0.8' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md || true</automated>
    <fails_when>prints any number other than 0 — 2.0.8 never reached PyPI and naming it as the cut point would send a reader to a version that does not exist</fails_when>
    <automated>/usr/bin/grep -cE 'will (ship|be published)|has not yet shipped|once a stable ships' /workspaces/.planning/seeds/SEED-claim-firestarter-slug.md || true</automated>
    <fails_when>prints any number other than 0 — a future-tense construction about the stable's arrival is the staleness this task exists to remove</fails_when>
    <automated>/usr/local/share/nvm/current/bin/node -e 'const{extractFrontmatter}=require("/workspaces/.claude/gsd-core/bin/lib/frontmatter.cjs");const fm=extractFrontmatter(require("fs").readFileSync("/workspaces/.planning/seeds/SEED-claim-firestarter-slug.md","utf8"));console.log("keys="+Object.keys(fm).length+" status="+fm.status)'</automated>
    <fails_when>the printed line is not exactly `keys=4 status=dormant` — the body edits must not have disturbed the block the audit scanner reads</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13`</fails_when>
  </verify>
  <done>The seed reads as a record of what is true on 2026-09-14: the stable has shipped, the trigger is a number with a window and a named instrument, the review point re-opens the premise rather than firing the act, and the readiness checklist states which ref each leg is true on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed frontmatter → GSD's frontmatter parser and seed scanner | A malformed scalar crosses here and is swallowed rather than rejected |
| seed prose → a future operator about to perform a destructive, irreversible act | The record is the only thing standing between a reading and a rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-193-20 | Denial of service | `trigger_condition:` scalar in `.planning/seeds/SEED-claim-firestarter-slug.md` | high | mitigate | A value beginning with a backtick makes the whole block unparseable, drops `status: dormant`, and the scanner's fail-open direction removes the seed from the audit with no error. Mitigated by a folded scalar starting with a word, and asserted after the edit by running GSD's own `extractFrontmatter` and checking four keys and `status`. |
| T-193-21 | Elevation of privilege | the trigger as read by a future agent | high | mitigate | A met trigger could be read as an authorisation to rename. The seed states in the checklist section that the reading is a measurement and that a human decides; the re-examination date is stated as re-opening the premise, never as firing anything. |
| T-193-22 | Repudiation | the readiness checklist | medium | mitigate | A leg asserted as satisfied on all refs is falsifiable on `origin/beta` today. The leg names the ref on which it is true and the ref on which it is not, and a verify leg greps both ref names inside that section. |
| T-193-23 | Information disclosure | the seed file | low | accept | The seed records repository slugs, version numbers and public download counts. Nothing in it is secret and nothing is added that could be; accepted because there is no confidential datum in scope. |
| T-193-SC | Tampering | npm/pip/cargo installs | high | mitigate | No plan in this phase runs a package-manager install. 193-RESEARCH.md records the Package Legitimacy Audit as not applicable; this plan edits one markdown file and adds no dependency. |
</threat_model>

<verification>
- GSD's own parser reads four keys and `status: dormant` from the edited file.
- The trigger value carries D-09's threshold, its window, the review date and the instrument's path.
- The readiness checklist names the refs its legs are true on.
- No sentence places the stable's publication in the future, and 2.0.8 is named nowhere.
</verification>

<success_criteria>
`.planning/seeds/SEED-claim-firestarter-slug.md` states a threshold and an observation window that a
reader can evaluate with one committed command, still parses, is still dormant, and makes no claim a
reader can falsify with a single `git show`.
</success_criteria>

<output>
Create `.planning/phases/193-the-deferred-claim-made-measurable/193-02-SUMMARY.md` when done.
Record the parse assertion's output and the rationale this plan deliberately keeps out of the seed.
</output>
