---
phase: 189-free-the-name
plan: 02
type: execute
wave: 2
depends_on: ["189-01"]
files_modified:
  - .gitmodules
  - .planning/phases/189-free-the-name/fresh-clone-fixture.sh
  - .planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt
  - .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt
autonomous: true
requirements: [RENAME-02, RENAME-03]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 only the URL changes: .gitmodules still carries the section [submodule \"firestarter\"] and the line path = firestarter, and the firestarter_app section is byte-unchanged."
    - "Per D-02 the SSH transport is retained: the new value is git@github.com:henols/firestarter_fw.git, not an HTTPS URL and not a relative URL."
    - "Per D-03 all three observables carry the new URL, not just the file: .gitmodules, the superproject's local submodule.firestarter.url, and the firestarter submodule's own remote.origin.url — the latter two propagated by git submodule sync --recursive."
    - "Per D-04 the RENAME-03 demonstration clones the local meta repository over file:// while the submodule leg resolves against the real GitHub remotes, so it needs no push and does not breach the standing operator-gated-push rule."
    - "Per D-06 the demonstration is a re-runnable fixture committed under the phase directory, not a one-off transcript: Phase 193's GATE-03 can extend it for its two history-trap clone cases instead of reinventing it."
    - "Per D-05 this demonstration runs while the gitlinks still point at the pushed beta tips 10ec1b0e24d98f04c3b4aa076b9d84231ac5da04 and f926e36262c368633b50245a225478ef36aed885; the firmware gitlink is advanced only afterwards, in 189-03."
    - "The fixture prints the resolved submodule URLs on labelled lines before its verdict, so the evidence distinguishes resolving firestarter_fw directly from following a redirect — an exit code alone cannot."
    - "A missing .gitmodules section or key is a failure, not an empty pass: git config --file .gitmodules submodule.firestarter.url exits non-zero when the section or key is absent, and no verification leg substitutes a default for a missing reading."
    - "The committed .gitmodules change is exactly one line: the diff against the previous commit is one insertion and one deletion, the TAB indentation git submodule set-url writes is preserved, and no whitespace drift is introduced."
  prohibitions:
    - "MUST NOT rename the .gitmodules section [submodule \"firestarter\"] or the firestarter/ submodule path — either would invalidate the documented submodule.firestarter.url workaround and require surgery on .git/modules/firestarter."
    - "MUST NOT change the submodule transport to HTTPS or to a relative URL while editing the line; that was considered and declined in discussion."
    - "MUST NOT satisfy the fresh-clone demonstration from a local object cache, a --reference clone or a pre-initialised submodule directory — the submodule leg must reach the real remote, or the evidence proves nothing about the new name."
    - "MUST NOT record a RENAME-03 transcript that was not produced by an actual run of the committed fixture."
    - "MUST NOT sweep or rewrite the archived firmware-slug references under .planning/milestones/ — they are historical-by-intent evidence."
  artifacts:
    - path: ".gitmodules"
      provides: "The firmware submodule URL, repointed at the renamed repository"
      contains: "firestarter_fw"
    - path: ".planning/phases/189-free-the-name/fresh-clone-fixture.sh"
      provides: "Re-runnable RENAME-03 demonstration: clones the meta repository over file://, initialises both submodules against the real remotes, prints every resolved URL, then asserts"
      min_lines: 80
    - path: ".planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt"
      provides: "Captured fixture run including the meta_commit pin the run was taken against"
      contains: "meta_commit:"
    - path: ".planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt"
      provides: "The three D-03 observables read from the existing clone"
      contains: "git@github.com:henols/firestarter_fw.git"
  key_links:
    - from: ".gitmodules"
      to: "the superproject's local git config key submodule.firestarter.url"
      via: "git submodule sync --recursive, which is what propagates the file into the clone"
      pattern: "firestarter_fw"
    - from: "the superproject's local git config"
      to: "firestarter/.git remote.origin.url"
      via: "the --recursive arm of the same sync"
      pattern: "firestarter_fw"
    - from: ".planning/phases/189-free-the-name/fresh-clone-fixture.sh"
      to: "git@github.com:henols/firestarter_fw.git"
      via: "a clone of the meta repository over file:// whose submodule leg resolves against the real remote"
      pattern: "firestarter_fw"
---

<objective>
Repoint the firmware submodule at its new name and prove, by running it, that a clone taken fresh
from the milestone tip resolves both submodules from the recorded URL rather than through GitHub's
rename redirect.

This plan carries the phase's tracer. Task 1 is a single vertical slice that runs the whole path end
to end — renamed repository, `.gitmodules`, the superproject's own config, the submodule's own
remote, and finally a real clone that initialises both submodules and prints what it resolved. It is
production work, not a probe: the fixture it writes is the committed, re-runnable artifact D-06 asks
for, and Phase 193's GATE-03 extends it rather than reinventing it.

Purpose: RENAME-02 and RENAME-03 are the two halves of "no redirect in the path" — the existing
clone and the fresh one. Both are observable, and this plan makes both observed.
Output: a one-line `.gitmodules` change on the milestone branch, a fixture under the phase directory,
and two evidence transcripts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/189-free-the-name/189-CONTEXT.md
@.planning/phases/189-free-the-name/189-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Deliberately NOT `@`-included: `.planning/STATE.md`, `.planning/ROADMAP.md` and
`.planning/PROJECT.md` — all three are large and everything needed is quoted in `189-CONTEXT.md`.

State measured on 2026-09-13 and not to be re-measured — cite it:

- `/workspaces/.gitmodules` holds two sections, TAB-indented, with absolute SSH URLs. The only line
  that changes is the firmware section's `url` line.
- The superproject's local config carries `submodule.firestarter.url` and
  `submodule.firestarter.active`; `firestarter/`'s own `remote.origin.url` still names the old slug.
  Both must change, and `git submodule sync --recursive` is what changes them.
- `firestarter_app` is already `git@github.com:henols/firestarter_app.git` and is unaffected.
- The meta repository's own `remote.origin.url` is HTTPS while the submodules are SSH. That
  asymmetry is pre-existing and out of scope — do not "fix" it.
- Gitlinks at the v1.38 fork, set by `05bd22a5` onto the pushed `beta` tips:
  `firestarter` at `10ec1b0e24d98f04c3b4aa076b9d84231ac5da04`,
  `firestarter_app` at `f926e36262c368633b50245a225478ef36aed885`. The fixture's submodule leg
  depends on those commits being fetchable, which is precisely why D-05 puts this plan before the
  gitlink advance in `189-03`.

Two devcontainer hazards that decide how verification legs are written here:

- The default `grep` on this machine is **ugrep**, and it honours `.gitignore`, so it silently
  under-scans. Any leg whose evidence depends on seeing every file uses `/usr/bin/grep` explicitly,
  or `git grep` for a tracked-file-only sweep.
- Never let a scratch clone live under `/workspaces`. A later `git clean -Xdf` aimed at this tree
  destroys ignored GSD state, and a clone parked inside the repository would be collateral.

Follow the repository's standing commit-attribution convention for every commit this plan makes.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — repoint the firmware submodule URL and prove a fresh clone resolves it</name>
  <precondition>`git ls-remote git@github.com:henols/firestarter_fw.git HEAD` returns a ref line — the SSH key on this machine can reach the renamed firmware repository over the transport the submodule uses.</precondition>
  <reversibility rating="costly">Running the demonstration before the gitlink advance (D-05) is what makes the transcript non-vacuous; recovering the claim after an advance-first ordering means re-winding the gitlink and regenerating the evidence.</reversibility>
  <files>.gitmodules, .planning/phases/189-free-the-name/fresh-clone-fixture.sh, .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt</files>
  <read_first>
    - `/workspaces/.gitmodules` — the file being modified; see its exact current content before touching it.
    - `.planning/phases/189-free-the-name/189-CONTEXT.md` — D-01, D-02, D-03, D-04, D-05, D-06 and the
      `<specifics>` requirement that the demonstration prints resolved URLs rather than merely exiting 0.
    - `.planning/phases/189-free-the-name/189-PATTERNS.md` — the evidence-script skeleton and the
      bare-slug discriminator; both are the house conventions this fixture must follow.
    - `/workspaces/.planning/milestones/v1.4-phases/15-versioning-locked-step-coordination-foundation/lockstep-dryrun-fixture.sh`
      — the primary analog: header block, `SCRIPT_DIR` derivation, precondition loop, labelled-value
      emission before the verdict, single verdict token with an explicit exit.
    - `/workspaces/.planning/v1.34/tools/run_gates.sh` — the argument loop with a fail-closed default
      branch and the documented exit-code table.
    - `/workspaces/tools/catalog/sync_to_subrepos.sh` — the only in-repo `mktemp` plus `trap ... EXIT`
      precedent, and its idempotence/requirements header lines.
  </read_first>
  <action>
Do the repoint first, then write the fixture, then run it. All three are one slice: the fixture
cannot prove anything until the URL it is meant to resolve is committed.

**Repoint (D-03).** Run `git submodule set-url firestarter git@github.com:henols/firestarter_fw.git`
from `/workspaces`, then `git submodule sync --recursive`. `set-url` rewrites `.gitmodules` alone;
the `sync` is what propagates the value into the superproject's local config and into the
submodule's own `remote.origin.url`. Per D-01 the section name and the `path = firestarter` line are
untouched, and per D-02 the transport stays SSH — only the slug changes. `set-url` writes the same
TAB indentation the file already uses, so there should be no whitespace drift. Commit `.gitmodules`
on its own, on `v1.38-repository-rename`, with plain `git -C /workspaces commit`; a commit helper
that scrapes the roadmap for a milestone name has been observed switching branches in this
repository, so confirm `git -C /workspaces branch --show-current` afterwards.

**Fixture (D-04, D-06).** Write `.planning/phases/189-free-the-name/fresh-clone-fixture.sh`
following the evidence-script skeleton in `189-PATTERNS.md`: `#!/usr/bin/env bash`,
`set -euo pipefail`, `SCRIPT_DIR` derived from `BASH_SOURCE`, and the meta root derived from
`SCRIPT_DIR` three levels up rather than hardcoded — the hardcoded-absolute-root habit in
`check-migration.sh` is called out there as the one leg not to copy. It is a `.planning/` script,
not product source, so it carries the full comment header the house style uses.

Its contract, which `189-03` depends on:

- Environment: `META_REF` defaults to `v1.38-repository-rename`; `KEEP_CLONE` defaults to `0` and
  when `1` leaves the scratch tree for inspection; `SCRATCH_DIR` defaults to a fresh `mktemp -d`.
  The scratch tree lives outside `/workspaces` and is removed by an `EXIT` trap by explicit path.
- Arguments: `--help` prints the usage block and exits 0; any other argument prints usage to stderr
  and exits 2, in the fail-closed style of `run_gates.sh`.
- Preconditions, checked before any work and each reporting to stderr: `git` is available, the
  derived meta root is a git repository, and `META_REF` resolves inside it.
- The clone is the one D-04 names: a single `git clone` of `file://` plus the meta root, with
  `--branch "$META_REF"` and `--recurse-submodules`, into the scratch tree. No `--reference`, no
  shared object store, no pre-seeded submodule directory — the submodule leg must reach the real
  remote or the run proves nothing.
- Before the verdict it prints one labelled value per line: `meta_source:`, `meta_ref:`,
  `meta_commit:` (the cloned HEAD sha), `gitmodules_url(firestarter):`,
  `config_url(firestarter):`, `remote_origin(firestarter):`, `remote_origin(firestarter_app):`,
  `firestarter_head:` and `firestarter_app_head:`. The three URL lines are read from inside the
  scratch clone, not from `/workspaces`.
- Assertions, bail-on-first in the `check-migration.sh` style, each failure naming what was expected,
  what the pre-state looks like and what to do next: (1) both submodule working trees are populated
  and `git submodule status` in the clone shows no uninitialised entry; (2) the firmware URL contains
  `firestarter_fw`; (3) the firmware URL does not match the bare-slug discriminator documented in
  `189-PATTERNS.md`; (4) the host URL still names `firestarter_app`.
- Verdict and exit codes, documented in the header: `0` with the token `FRESH CLONE OK`; `1` for a
  failed assertion or a clone failure of any other cause; `2` for bad usage; and `3` for the one
  named, expected case — the superproject cloned but the recorded firmware gitlink commit is not
  present on the remote. Detect case `3` by inspecting the captured clone output for git's
  missing-object wording and reporting the gitlink sha it could not fetch, with the guidance that
  this is expected between this phase's gitlink advance and the milestone push. Exit `3` must be
  reachable code, not a documented intention; `189-03` is where it is first exercised.
- The header also states: what it proves (RENAME-03), `Usage:`, the exit-code table, the dependency
  list, the bail-on-first failure style, that it is re-runnable, and that Phase 193's GATE-03 can
  extend it for its two history-trap clone cases rather than reinventing it.

**Run (D-05).** Run the fixture once with no arguments and capture its full output to
`.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt`. The run must happen now,
while the gitlinks still point at the pushed `beta` tips; `189-03` advances the firmware gitlink only
after this evidence exists. Commit the fixture and the transcript.
  </action>
  <verify>
    <automated>git -C /workspaces config --file .gitmodules submodule.firestarter.url</automated>
    <fails_when>prints anything other than `git@github.com:henols/firestarter_fw.git`, or exits non-zero because the section or the key is absent</fails_when>
    <automated>git -C /workspaces config --file .gitmodules submodule.firestarter.path</automated>
    <fails_when>prints anything other than `firestarter` — the path must not have been renamed</fails_when>
    <automated>git -C /workspaces config --local submodule.firestarter.url</automated>
    <fails_when>prints anything other than `git@github.com:henols/firestarter_fw.git`, or exits non-zero, either of which means the sync did not propagate into the superproject</fails_when>
    <automated>git -C /workspaces/firestarter remote get-url origin</automated>
    <fails_when>prints anything other than `git@github.com:henols/firestarter_fw.git` — the `--recursive` arm of the sync did not reach the submodule</fails_when>
    <automated>bash .planning/phases/189-free-the-name/fresh-clone-fixture.sh</automated>
    <fails_when>exits non-zero, or its output does not contain the token `FRESH CLONE OK`, or it contains no line beginning `remote_origin(firestarter):`</fails_when>
    <automated>/usr/bin/grep -c 'remote_origin(firestarter): git@github.com:henols/firestarter_fw.git' .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt</automated>
    <fails_when>prints `0`, or exits non-zero because the transcript is absent — the captured run must show the URL actually resolved inside the scratch clone, not merely that the run exited 0</fails_when>
    <automated>/usr/bin/grep -c '^meta_commit: ' .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt</automated>
    <fails_when>prints anything other than `1` — without the pinned meta commit a later reader cannot tell which tip the demonstration was taken against</fails_when>
    <automated>/usr/bin/grep -lE 'henols/firestarter([^_a-zA-Z0-9]|$)' /workspaces/.gitmodules | wc -l</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>/usr/bin/grep -lE 'henols/firestarter_app' /workspaces/.gitmodules | wc -l</automated>
    <fails_when>prints `0` — this is the non-vacuity control for the leg above; if it prints 0 the sweep matched nothing because the file or the pattern machinery is wrong, not because the file is clean</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename`</fails_when>
  </verify>
  <acceptance_criteria>
    - `.gitmodules` contains `url = git@github.com:henols/firestarter_fw.git` under
      `[submodule "firestarter"]`, still contains `path = firestarter`, still carries the section name
      `[submodule "firestarter"]`, and the `firestarter_app` section is unchanged.
    - `git -C /workspaces show --numstat --format= HEAD -- .gitmodules` prints exactly one line reading
      one insertion, one deletion, `.gitmodules` — the change is one line, with no whitespace drift.
    - The TAB indentation is preserved:
      `awk '/^\turl = git@github\.com:henols\/firestarter_fw\.git$/ {n++} END {print n+0}' /workspaces/.gitmodules`
      prints `1`.
    - All three D-03 observables read `git@github.com:henols/firestarter_fw.git`: the `.gitmodules`
      value, the superproject's local `submodule.firestarter.url`, and `firestarter/`'s
      `remote.origin.url`.
    - `.planning/phases/189-free-the-name/fresh-clone-fixture.sh` exists, is committed, begins
      `#!/usr/bin/env bash`, sets `set -euo pipefail`, derives its meta root from `SCRIPT_DIR` with no
      hardcoded `/workspaces` path, documents exit codes 0, 1, 2 and 3 in its header, and rejects an
      unrecognised argument with exit 2.
    - `bash .planning/phases/189-free-the-name/fresh-clone-fixture.sh --nonsense` exits 2 and prints a
      usage line to stderr.
    - The committed transcript contains labelled lines for `meta_commit:`,
      `gitmodules_url(firestarter):`, `config_url(firestarter):`, `remote_origin(firestarter):` and
      `remote_origin(firestarter_app):`, and the firmware lines name `firestarter_fw`.
    - `git -C /workspaces diff --name-only HEAD -- firestarter` is empty at the end of this task: the
      gitlink has not been advanced here (D-05).
  </acceptance_criteria>
  <done>
A clone taken fresh from the milestone tip initialises both submodules from the recorded URLs, the
firmware one naming `firestarter_fw`, and the run that proved it is committed as a re-runnable
fixture plus its captured output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the existing-clone observables for RENAME-02</name>
  <files>.planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt</files>
  <read_first>
    - `.planning/phases/189-free-the-name/189-CONTEXT.md` — D-03's statement that the superproject
      config and the submodule's own remote are **the** observable, not just the file.
    - `/workspaces/.gitmodules` — the committed post-repoint state.
    - `.planning/phases/189-free-the-name/189-PATTERNS.md` — the three-row observable table.
  </read_first>
  <action>
Write `.planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt` recording, each on
its own labelled line and each read read-only from this already-existing clone: the capture date;
the `.gitmodules` value of `submodule.firestarter.url`; the superproject's local
`submodule.firestarter.url`; `firestarter/`'s `remote.origin.url`; and, as the untouched control,
the `.gitmodules` value of `submodule.firestarter_app.url`.

Add two short statements a verifier can check rather than take on trust. First, per D-03 the file
alone would not satisfy RENAME-02: `git submodule set-url` rewrites `.gitmodules` only, and it is
`git submodule sync --recursive` that reaches the other two, which is why three readings are
recorded and not one. Second, per D-08 the ROADMAP's "on both `beta` and `main`" is satisfied here by
the milestone branch plus the `main` pull request in `189-04`, with the `beta` half explicitly
close-carried — it lands when the milestone merges, which is the standard route for every change in
this project, and merging to `beta` early is not an option because it fires a pre-release cut in both
sub-repositories and publishes the host one.

Record the section-and-path integrity readings too, so the "URL only" claim is evidenced rather than
asserted: the section name present in the file and the `path` value.

Then commit the transcript.
  </action>
  <verify>
    <automated>/usr/bin/grep -c 'git@github.com:henols/firestarter_fw.git' .planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt</automated>
    <fails_when>prints a count below 3 — fewer than three readings carry the new URL, so at least one of the D-03 observables is missing or stale</fails_when>
    <automated>/usr/bin/grep -c 'firestarter_app' .planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt</automated>
    <fails_when>prints `0` — the untouched control reading is absent, so the transcript cannot show that the host submodule was left alone</fails_when>
    <automated>/usr/bin/grep -c 'close-carried' .planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt</automated>
    <fails_when>prints `0` — the D-08 disposition of the `beta` half is unrecorded, and a verifier would read the milestone branch as an incomplete requirement</fails_when>
    <automated>/usr/bin/grep -lE 'henols/firestarter([^_a-zA-Z0-9]|$)' /workspaces/.gitmodules | wc -l</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>/usr/bin/grep -lE 'henols/firestarter_app' /workspaces/.gitmodules | wc -l</automated>
    <fails_when>prints `0` — non-vacuity control for the sweep above</fails_when>
  </verify>
  <acceptance_criteria>
    - The transcript records five labelled readings plus a capture date, and the three firmware
      readings all print `git@github.com:henols/firestarter_fw.git`.
    - The transcript states why three readings are recorded rather than one, naming
      `git submodule sync --recursive` as the mechanism that reaches the latter two (D-03).
    - The transcript states that the `beta` half of ROADMAP criterion 2 is close-carried per D-08, and
      why an early merge to `beta` is not an option.
    - The transcript records the `[submodule "firestarter"]` section name and the `path = firestarter`
      value, evidencing the URL-only scope (D-01).
    - `git -C /workspaces log -1 --name-only` lists the transcript.
  </acceptance_criteria>
  <done>
RENAME-02's existing-clone half is evidenced by three recorded observables rather than by the file
alone, and the branch disposition a verifier needs in order not to misread the milestone branch is
written down.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.gitmodules` -> every future clone | The URL recorded here is where every fresh clone fetches firmware source from; it is untrusted input to everyone who clones. |
| scratch clone -> github.com over SSH | The fixture's submodule leg crosses the network using this machine's SSH key. |
| local meta repository -> `file://` clone | The fixture reads committed refs from the working repository through a file URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-189-05 | Tampering | the `.gitmodules` firmware URL | high | mitigate | The value is written by `git submodule set-url` to one exact SSH URL and then asserted from three independent readings plus a real clone that prints what it resolved; a one-line diff assertion and a TAB-preservation assertion bound the change so nothing else can ride along in the same commit. |
| T-189-06 | Spoofing | the fixture's proof of resolution | high | mitigate | The fixture is forbidden from using `--reference`, a shared object store or a pre-seeded submodule directory, and it prints the resolved URLs read from inside the scratch clone; a transcript that was not produced by a run is a named prohibition. |
| T-189-07 | Information disclosure | committed evidence transcripts | low | mitigate | The fixture emits only URLs, refs and commit shas; it never prints environment, SSH agent state or credentials, and the scratch tree is removed on exit. |
| T-189-08 | Denial of service | a scratch clone left inside `/workspaces` | low | mitigate | The scratch tree is created by `mktemp -d` outside the repository and removed by an `EXIT` trap by explicit path, so a later `git clean -Xdf` aimed at this tree cannot take it and it cannot itself pollute the tree. |
| T-189-09 | Repudiation | which tip the demonstration was taken against | medium | mitigate | The transcript pins `meta_commit:`, so the claim is attached to a specific commit rather than to "the branch as it was at some point". |
| T-189-SC | Tampering | package-manager installs | low | accept | This plan installs no packages; the fixture depends only on `bash`, `git` and `mktemp`, all already present. |
</threat_model>

## Spec-less probe coverage

No SPEC exists for this phase; the deterministic edge probe surfaced **6 rows** across
RENAME-01/02/03. This plan owns three of them:

- **RENAME-02 / empty** — answered explicitly in `must_haves.truths`: a missing `.gitmodules` section
  or key makes `git config --file` exit non-zero, and no verification leg substitutes a default for a
  missing reading.
- **RENAME-02 / encoding** — answered explicitly in `must_haves.truths`: the committed change is one
  insertion and one deletion, and the TAB indentation `git submodule set-url` writes is preserved
  byte-for-byte.
- **RENAME-03 / unclassified** — stays **`unresolved`**, and is carried here as a flagged planner
  assumption rather than auto-resolved:

> **Flagged assumption (RENAME-03 / unclassified, unresolved).** The probe could not classify this
> row. The planner's reading, stated so a reviewer can reject it: RENAME-03 is demonstrated against
> the **local** meta repository over `file://`, with only the submodule leg reaching real GitHub
> (D-04). The phase does **not** demonstrate a clone taken from `origin`, because the milestone
> branch is unpushed until ship time and pushing it early would breach the standing operator-gated
> rule. If RENAME-03 is intended to mean "a third party clones from GitHub and it works", this phase
> does not prove that and cannot until the milestone ships; what it proves is that the URLs recorded
> at the milestone tip resolve, which is the checkable half.

The remaining three rows (RENAME-01 / empty, encoding and concurrency) are authored in `189-01`.

**Arithmetic: 6 probe-surfaced rows == 5 authored into `must_haves` + 1 surfaced as a flagged
assumption.** No row was dropped.

## Artifacts this phase produces

| Artifact | Created by | Notes |
|---|---|---|
| `.planning/phases/189-free-the-name/evidence/189-rename-01-identity.txt` | 189-01 | RENAME-01 identity transcript |
| `.planning/phases/189-free-the-name/fresh-clone-fixture.sh` | 189-02 | Re-runnable RENAME-03 fixture; env vars `META_REF`, `KEEP_CLONE`, `SCRATCH_DIR`; exit codes 0/1/2/3; labelled output keys `meta_source:`, `meta_ref:`, `meta_commit:`, `gitmodules_url(firestarter):`, `config_url(firestarter):`, `remote_origin(firestarter):`, `remote_origin(firestarter_app):`, `firestarter_head:`, `firestarter_app_head:`; verdict token `FRESH CLONE OK` |
| `.planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt` | 189-02 | The three D-03 observables for an existing clone |
| `.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt` | 189-02 | Captured fixture run, including the `meta_commit:` pin |
| `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` | 189-03 | Firmware-repository bare-slug sweep with its positive control |
| `.planning/phases/189-free-the-name/evidence/189-rename-02-branch-disposition.md` | 189-04 | RENAME-02 branch disposition |
| `.planning/phases/189-free-the-name/COVERAGE.md` | planning | Reasoned "no external API integration" declaration |
| meta branch `v1.38-gitmodules-main` | 189-04 | A git ref, not a file |

## Verification

- Re-running the fixture with the recorded pin reproduces the verdict while the gitlink is
  unadvanced:
  `META_REF=v1.38-repository-rename bash .planning/phases/189-free-the-name/fresh-clone-fixture.sh`.
- After `189-03` advances the gitlink and until the milestone is pushed, the same invocation is
  expected to exit `3` with the named "gitlink commit not present on the remote" diagnosis. That is
  the fixture reporting an accurate fact about an unpushed commit, not a regression in the URL, and
  `189-03` asserts it explicitly so the distinction is proved rather than assumed.
- The `beta` half of ROADMAP criterion 2 is close-carried (D-08) and is not expected to be true
  mid-milestone.

## Success criteria

- ROADMAP criterion 2's existing-clone half is satisfied and evidenced across three observables.
- ROADMAP criterion 3 is satisfied by running a clone, not by reading a file, and the run is
  reproducible from a committed fixture.

<output>
Create `.planning/phases/189-free-the-name/189-02-SUMMARY.md` when done.
</output>
