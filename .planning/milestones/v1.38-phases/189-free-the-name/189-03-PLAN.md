---
phase: 189-free-the-name
plan: 03
type: execute
wave: 3
depends_on: ["189-02"]
files_modified:
  - firestarter/README.md
  - firestarter/tests/meta_presence.py
  - firestarter
  - .planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt
  - .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt
autonomous: true
requirements: [RENAME-01, RENAME-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-09 Phase 189 owns BOTH of the firmware repository's live slug references outright — firestarter/README.md:47 and firestarter/tests/meta_presence.py:22 — so neither falls through the gap between ROADMAP criterion 4 and Phase 192's SWEEP-01."
    - "The firmware README's Releases link addresses henols/firestarter_fw, and README lines 1, 7, 75, 80 and 81 are byte-unchanged because they address henols/firestarter_app and henols/firestarter_prom, which keep their names."
    - "The meta_presence.py change is one token inside an existing docstring and nothing else: no comment, no annotation and no provenance edit accompanies it, per the repository's non-overridable no-comments-in-product-source rule."
    - "Per D-10 no regression guard is added inside the firmware repository: cleanliness is proved by a sweep at verification time, and a standing repo-wide check belongs to Phase 192's SWEEP-01/02, which has to build the discriminating pattern anyway."
    - "The firmware repository holds zero live tracked references to the bare slug after this plan, and the sweep proving it is paired with a positive control so a zero cannot be produced by a broken pattern or an empty scan."
    - "Per D-05 the firmware gitlink is advanced only after 189-02's fresh-clone demonstration has been captured, so the demonstration was taken against a commit a third party could actually fetch."
    - "Executors commit inside the submodule on the milestone branch v1.38-repository-rename and the meta repository re-pins the gitlink, which is the established per-phase pattern in this project since v1.36."
    - "Once the gitlink points at an unpushed firmware commit, the fixture reports that specific fact with exit code 3 and names the sha it could not fetch, rather than failing in a way indistinguishable from a wrong URL."
  prohibitions:
    - "MUST NOT add any comment, annotation, phase reference or requirement citation to firestarter/README.md or firestarter/tests/meta_presence.py — the repository's no-comments-in-product-source rule is not overridable by a plan, and rationale belongs in the SUMMARY or the commit message."
    - "MUST NOT strip or reword the existing GSD provenance already present in the meta_presence.py docstring — that is a separate standing todo, and widening this phase into a source-hygiene sweep is out of scope."
    - "MUST NOT touch README lines 1, 7, 75, 80 or 81, nor the backtick-quoted submodule directory name on meta_presence.py line 16 — those name repositories and paths that keep their names."
    - "MUST NOT add a source-scanning CI gate or any committed regression guard inside the firmware repository; firmware CI is native, native_nodevtools and the python test suite only, and source-scanning gates in this project have a history of failing open after renames."
    - "MUST NOT advance the firmware gitlink before 189-02's demonstration transcript exists — the ordering is what makes that evidence non-vacuous."
  artifacts:
    - path: "firestarter/README.md"
      provides: "The firmware repository's own Releases link, addressing the new name"
      contains: "henols/firestarter_fw/releases"
    - path: "firestarter/tests/meta_presence.py"
      provides: "The module docstring's isolated-checkout description, addressing the new name"
      contains: "henols/firestarter_fw"
    - path: ".planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt"
      provides: "Firmware-repository-wide bare-slug sweep with its non-vacuity control"
      contains: "0"
  key_links:
    - from: "firestarter/README.md line 47"
      to: "https://github.com/henols/firestarter_fw/releases"
      via: "the Releases link a user follows to download firestarter_{board}.hex"
      pattern: "firestarter_fw/releases"
    - from: "the meta repository's firestarter gitlink"
      to: "the firmware commit carrying these two edits"
      via: "git add firestarter in the superproject, after 189-02's demonstration"
      pattern: "160000 commit"
---

<objective>
Give the firmware repository its own new name in the two places it still says the old one, and only
then re-pin the meta repository's gitlink at the commit that carries them.

Per D-09 this phase owns both references outright — `firestarter/README.md:47`, which is literally
ROADMAP criterion 4's "release links", and `firestarter/tests/meta_presence.py:22`, which is named by
neither criterion 4 nor Phase 192's SWEEP-01 and would otherwise fall through both. Per D-05 the
gitlink advance comes last: `189-02` has already demonstrated a fresh clone against gitlinks pointing
at pushed `beta` tips, and advancing first would have produced a transcript proving the clone
plumbing rather than proving that `firestarter_fw` resolves.

Purpose: criterion 4 and the D-05 ordering are the two things this phase must finish after the proof,
not before it.
Output: one commit inside the firmware submodule, one sweep transcript, and one gitlink advance in the
meta repository.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/189-free-the-name/189-CONTEXT.md
@.planning/phases/189-free-the-name/189-PATTERNS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Deliberately NOT `@`-included: `.planning/STATE.md`, `.planning/ROADMAP.md` and
`.planning/PROJECT.md` — large, and everything needed is quoted in `189-CONTEXT.md`.

**The hard rule that shapes both edits.** `CLAUDE.md` section "Source code comments — hard rule"
forbids writing comments into anything under `firestarter/` and `firestarter_app/`, and states that
it is not overridable by a plan, task, skill or subagent instruction. Both files here are product
source. The edits are a slug swap and nothing more: no annotation, no citation, no explanatory note.
Rationale goes in the SUMMARY or the commit message. The docstring in `meta_presence.py` already
carries GSD provenance — leave it exactly as it is; stripping it belongs to the standing todo
`2026-08-27-strip-gsd-provenance-comments-from-source.md`, and folding that in would widen a
three-requirement infrastructure phase into a source-hygiene one.

State measured on 2026-09-13 and not to be re-measured. The firmware repository holds exactly two
live tracked references to the bare slug:

| File | Line | Current text | Verdict |
|---|---|---|---|
| `firestarter/README.md` | 1 | `henols/firestarter_app` logo raw URL | unchanged |
| `firestarter/README.md` | 7 | `henols/firestarter_prom` | unchanged |
| `firestarter/README.md` | **47** | `[Releases](https://github.com/henols/firestarter/releases)` | **change the slug** |
| `firestarter/README.md` | 75 | `henols/firestarter_prom/wiki/Contributing` | unchanged |
| `firestarter/README.md` | 80 | `henols/firestarter_prom/wiki` | unchanged |
| `firestarter/README.md` | 81 | `henols/firestarter_prom/wiki/Breaking-Changes` | unchanged |
| `firestarter/tests/meta_presence.py` | **22** | `` `actions/checkout` of `henols/firestarter` in isolation, that parent `` | **change the slug** |

Line 16 of `meta_presence.py` carries a backtick-quoted `firestarter` naming the **submodule
directory**, which D-01 keeps. It must not be touched, and it carries no owner prefix so it is not a
slug reference at all.

Both files are tracked in the `firestarter` submodule, not in the meta repository — verify with
`git -C firestarter ls-files`, never with a bare `git ls-files` at the meta root.

All three repositories are already on `v1.38-repository-rename`. Executors commit inside the
submodule on that branch and the meta repository re-pins the gitlink; that has been the norm since
v1.36.

The default `grep` here is ugrep and honours `.gitignore`. Every sweep leg below uses `git grep`
(tracked files only, which is what the measurement used) or `/usr/bin/grep` explicitly.

Follow the repository's standing commit-attribution convention for every commit this plan makes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the firmware repository's own two slug references</name>
  <files>firestarter/README.md, firestarter/tests/meta_presence.py</files>
  <read_first>
    - `firestarter/README.md` — the file being modified; read lines 40–50 for the surrounding context
      of the Releases link and confirm which other lines carry an owner-prefixed slug.
    - `firestarter/tests/meta_presence.py` — the file being modified; read lines 15–27 so the docstring's
      direction-warning paragraph is understood before one token inside it is changed.
    - `CLAUDE.md` section "Source code comments — hard rule" — the constraint on what may be added.
    - `.planning/phases/189-free-the-name/189-PATTERNS.md` section 2 — the per-line verdict table and
      the bare-slug discriminator.
  </read_first>
  <action>
Make exactly two one-line edits inside the `firestarter` submodule, on branch
`v1.38-repository-rename`.

In `firestarter/README.md` line 47, the Releases link currently addresses
`https://github.com/henols/firestarter/releases`. Repoint it at
`https://github.com/henols/firestarter_fw/releases`. The link wording beyond the slug is yours to
adjust if it reads better, but nothing else on the line and nothing on any other line changes — in
particular lines 1, 7, 75, 80 and 81 address `henols/firestarter_app` and `henols/firestarter_prom`,
which keep their names under milestone decision D-1.

In `firestarter/tests/meta_presence.py` line 22, the module docstring describes an
`actions/checkout` of `henols/firestarter` in isolation. Change that one slug to
`henols/firestarter_fw`. Nothing else on line 22 or on any other line changes. Line 16's
backtick-quoted `firestarter` is the submodule directory name, which D-01 keeps, and it carries no
owner prefix.

Add nothing to either file. No comment, no annotation, no phase or requirement citation, no note
explaining the change — `CLAUDE.md`'s no-comments rule covers everything under `firestarter/` and is
not overridable here. The existing provenance lines in the docstring stay exactly as they are.

Commit both files together inside the submodule with plain `git -C /workspaces/firestarter commit`,
and confirm the submodule is still on `v1.38-repository-rename` afterwards. Do not touch the meta
repository's gitlink in this task — that is Task 3, and D-05 puts it after `189-02`'s demonstration.
  </action>
  <verify>
    <automated>git -C /workspaces/firestarter show --numstat --format= HEAD</automated>
    <fails_when>prints anything other than exactly two lines reading one insertion and one deletion for `README.md` and for `tests/meta_presence.py` — more files, or a count other than 1/1 on either, means the edit was not surgical</fails_when>
    <automated>sed -n '47p' /workspaces/firestarter/README.md | /usr/bin/grep -c 'henols/firestarter_fw/releases'</automated>
    <fails_when>prints `0` — line 47 does not carry the repointed Releases link</fails_when>
    <automated>sed -n '22p' /workspaces/firestarter/tests/meta_presence.py | /usr/bin/grep -c 'henols/firestarter_fw'</automated>
    <fails_when>prints `0` — line 22 does not carry the repointed slug</fails_when>
    <automated>awk 'NR==1||NR==7||NR==75||NR==80||NR==81' /workspaces/firestarter/README.md | /usr/bin/grep -c 'henols/firestarter_prom\|henols/firestarter_app'</automated>
    <fails_when>prints anything other than `5` — one of the five must-not-change lines lost or changed its slug</fails_when>
    <automated>git -C /workspaces/firestarter branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename`</fails_when>
    <automated>git -C /workspaces diff --name-only HEAD -- firestarter | wc -l</automated>
    <fails_when>prints `0` — the submodule commit exists but the superproject does not see the gitlink as moved, which means the commit did not land inside the submodule at all</fails_when>
  </verify>
  <acceptance_criteria>
    - `firestarter/README.md` line 47 addresses `https://github.com/henols/firestarter_fw/releases`.
    - `firestarter/tests/meta_presence.py` line 22 addresses `henols/firestarter_fw`.
    - The submodule commit changes exactly two files, one line added and one line removed in each.
    - `git -C /workspaces/firestarter diff HEAD~1 HEAD` adds no line that is a comment or an annotation
      in either file; the only added lines are the two repointed lines.
    - The five untouched README lines still address `henols/firestarter_app` or
      `henols/firestarter_prom`.
    - The submodule HEAD is on `v1.38-repository-rename`, and the meta repository's working tree shows
      the `firestarter` gitlink as moved but **not** committed.
  </acceptance_criteria>
  <done>
The firmware repository addresses its own new name in both places it still said the old one, in one
surgical commit that adds no commentary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep the firmware repository for the bare slug, with a control</name>
  <files>.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt</files>
  <read_first>
    - `.planning/phases/189-free-the-name/189-PATTERNS.md` — the bare-slug discriminator section,
      including why a plain search for the slug matches all six README lines and is wrong.
    - `.planning/phases/189-free-the-name/189-CONTEXT.md` — D-10, and the measured statement that the
      firmware repository held exactly two live tracked references before this plan.
  </read_first>
  <action>
Sweep the whole `firestarter` submodule for live tracked references to the bare slug, using the
discriminator documented in `189-PATTERNS.md` and a tracked-file-only search rather than the
devcontainer's default `grep`, which is ugrep and honours `.gitignore`.

Record the result to `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` with
three labelled readings: the capture date; the count of tracked files still matching the bare-slug
discriminator, which must be zero; and — as the non-vacuity control — the count of tracked files
matching `henols/firestarter_prom`, which must be non-zero because those references legitimately
remain. Without the control, a zero cannot be distinguished from a search that matched nothing
because it ran against the wrong tree or with a broken pattern.

Also record the pre-state for contrast: before this phase there were exactly two matches,
`README.md:47` and `tests/meta_presence.py:22`.

Per D-10, stop there. Do not add a committed regression guard, a lint rule, a workflow or any
source-scanning check inside the firmware repository. Cleanliness here is proved by this sweep at
verification time; a standing repo-wide check is Phase 192's SWEEP-01/02, which has to build a
discriminating pattern regardless, and source-scanning gates in this project have a documented habit
of failing open after a rename.

Commit the transcript in the meta repository.
  </action>
  <verify>
    <automated>git -C /workspaces/firestarter grep -lE 'henols/firestarter([^_a-zA-Z0-9]|$)' -- . | wc -l</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>git -C /workspaces/firestarter grep -lE 'henols/firestarter_prom' -- . | wc -l</automated>
    <fails_when>prints `0` — the non-vacuity control for the leg above; a zero here means the sweep matched nothing because the tree or the pattern machinery is wrong, not because the repository is clean</fails_when>
    <automated>/usr/bin/grep -c 'firestarter_prom' .planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt</automated>
    <fails_when>prints `0` — the transcript records no control reading, so its zero is unfalsifiable</fails_when>
    <automated>git -C /workspaces/firestarter ls-files '*.yml' '*.yaml' | xargs -r git -C /workspaces/firestarter grep -lE 'henols/firestarter' -- | wc -l</automated>
    <fails_when>prints anything other than `0` — a workflow file matching any firmware slug would mean a source-scanning guard was added inside the firmware repository, which D-10 forbids</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` exists, is committed,
      and records a capture date, a zero count for the bare-slug discriminator and a non-zero control
      count.
    - The transcript states the pre-state — two matches, named by file and line — so the delta is
      readable without re-running anything.
    - No new file was added inside the `firestarter` submodule by this task, and no workflow file in
      that repository references any firmware slug.
  </acceptance_criteria>
  <done>
The firmware repository holds zero live tracked references to the bare slug, proved by a sweep whose
zero is falsifiable, and no guard was committed to keep it that way.
  </done>
</task>

<task type="auto">
  <name>Task 3: Advance the firmware gitlink, and prove the fixture names the consequence</name>
  <reversibility rating="costly">D-05: the gitlink advance must follow 189-02's demonstration, because advancing first retargets the gitlink at an unpushed commit and yields a transcript that proves the clone plumbing rather than proving that firestarter_fw resolves; recovering the claim would mean re-winding the gitlink and regenerating the evidence.</reversibility>
  <files>firestarter</files>
  <read_first>
    - `.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt` — the demonstration
      that must already exist before this task runs; note its `meta_commit:` pin.
    - `.planning/phases/189-free-the-name/fresh-clone-fixture.sh` — the fixture's documented exit-code
      table, in particular the case-3 branch.
    - `.planning/phases/189-free-the-name/189-CONTEXT.md` — D-05 and the established-patterns note that
      the gitlink is advanced per phase.
  </read_first>
  <action>
Confirm first that `189-02`'s demonstration transcript exists and carries a `meta_commit:` line. If
it does not, stop: advancing the gitlink now would make that evidence unobtainable without rewinding
(D-05).

Then advance the meta repository's `firestarter` gitlink to the firmware commit created in Task 1 —
stage the `firestarter` path in the superproject and commit it on `v1.38-repository-rename` with
plain `git -C /workspaces commit`. This is the ordinary per-phase gitlink advance this project has
done since v1.36; the commit it points at is unpushed, which is ordinary unpushed phase work.

Then run the fixture once more, with no arguments, and record what it does. With the gitlink now
naming a commit that is not on `git@github.com:henols/firestarter_fw.git`, the fixture is expected to
exit `3` and to say which sha it could not fetch — not to exit `1`, which would mean an assertion
about the URL failed. Append that run's verdict line and exit code to
`.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt` under a clearly labelled
"after the gitlink advance" heading, so the transcript carries both readings and a later reader
cannot mistake the second for a regression in the URL. This is the first time the case-3 branch is
reachable at all, so treat this run as the proof that the branch works rather than as a formality.

Commit the gitlink advance and the appended transcript.
  </action>
  <verify>
    <automated>git -C /workspaces ls-tree HEAD firestarter</automated>
    <fails_when>prints the fork-point sha `10ec1b0e24d98f04c3b4aa076b9d84231ac5da04`, which means the gitlink was never advanced, or prints no `160000 commit` line at all</fails_when>
    <automated>test "$(git -C /workspaces rev-parse HEAD:firestarter)" = "$(git -C /workspaces/firestarter rev-parse HEAD)"; echo "match=$?"</automated>
    <fails_when>prints anything other than `match=0` — the committed gitlink does not name the submodule's current HEAD</fails_when>
    <automated>git -C /workspaces status --porcelain -- firestarter | wc -l</automated>
    <fails_when>prints anything other than `0` — the gitlink move is still uncommitted in the superproject</fails_when>
    <automated>bash .planning/phases/189-free-the-name/fresh-clone-fixture.sh; echo "exit=$?"</automated>
    <fails_when>prints anything other than `exit=3`; `exit=0` would mean the advanced gitlink is somehow already fetchable, `exit=1` would mean the fixture failed a URL assertion rather than reporting the unpushed gitlink, and `exit=2` would mean the invocation itself was rejected</fails_when>
    <automated>/usr/bin/grep -c 'after the gitlink advance' .planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt</automated>
    <fails_when>prints `0` — the second reading was not appended, so the transcript no longer explains why a later re-run of the fixture does not exit 0</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename`</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces rev-parse HEAD:firestarter` equals `git -C /workspaces/firestarter rev-parse HEAD`,
      and is not `10ec1b0e24d98f04c3b4aa076b9d84231ac5da04`.
    - `git -C /workspaces status --porcelain -- firestarter` is empty: the advance is committed, not
      merely staged.
    - The demonstration transcript from `189-02` existed, carrying a `meta_commit:` line, **before**
      this task advanced anything — recorded in the SUMMARY as the D-05 ordering check.
    - Re-running the fixture with no arguments exits `3`, and its stderr names the firmware gitlink sha
      it could not fetch rather than reporting a URL assertion failure.
    - The transcript carries both readings under distinct headings, the second labelled "after the
      gitlink advance".
  </acceptance_criteria>
  <done>
The meta repository pins the firmware commit carrying criterion 4's edits, the demonstration that
preceded it is intact and pinned to its own meta commit, and the fixture's expected post-advance
behaviour is a named, proved exit code rather than an unexplained failure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta repository -> `firestarter` submodule | The superproject commits a pointer into a repository with its own history and its own branch state. |
| firmware README -> a user's browser | The Releases link is the path a user follows to download and flash a `.hex`; a wrong target sends them somewhere the project does not control. |
| plan text -> product source | A plan instruction is the usual vector by which process commentary leaks into shipped source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-189-10 | Spoofing | the firmware README Releases link | medium | mitigate | The target is stated as one exact URL and asserted by a line-scoped check on line 47, with the five must-not-change lines asserted separately so an over-broad edit cannot pass. |
| T-189-11 | Tampering | product source picking up process commentary | medium | mitigate | The plan states the no-comments rule as non-overridable, adds it as a prohibition, and makes "the diff adds only the two repointed lines" an acceptance criterion — no criterion anywhere asserts that a comment exists. |
| T-189-12 | Denial of service | the meta gitlink pointing at an unpushed commit | medium | mitigate | D-05 orders the advance after the demonstration, and the fixture's exit code 3 names the unpushed sha, so the state is reported accurately rather than read as a broken URL. This is accepted, bounded phase state that resolves when the milestone is pushed. |
| T-189-13 | Repudiation | a sweep result that cannot be falsified | medium | mitigate | Every zero-expectation sweep leg is paired with a positive control over the same tree with the same machinery, so a zero produced by an empty or misdirected scan is detected. |
| T-189-SC | Tampering | package-manager installs | low | accept | This plan installs no packages; it edits two tracked text files and moves a gitlink. |
</threat_model>

## Spec-less probe coverage

No SPEC exists for this phase; the deterministic edge probe surfaced **6 rows** across
RENAME-01/02/03. This plan authors none of them directly — the three RENAME-01 rows are in `189-01`
and the two RENAME-02 rows plus the flagged RENAME-03 row are in `189-02`. The encoding row's
discipline is nevertheless honoured here: every slug assertion uses the exact-string bare-slug
discriminator from `189-PATTERNS.md`, never a prefix match, which is what keeps `firestarter_app` and
`firestarter_prom` out of the sweep.

**Arithmetic: 6 probe-surfaced rows == 5 authored into `must_haves` + 1 surfaced as a flagged
assumption.** No row was dropped.

## Artifacts this phase produces

| Artifact | Created by | Notes |
|---|---|---|
| `.planning/phases/189-free-the-name/evidence/189-rename-01-identity.txt` | 189-01 | RENAME-01 identity transcript |
| `.planning/phases/189-free-the-name/fresh-clone-fixture.sh` | 189-02 | Re-runnable RENAME-03 fixture; env vars `META_REF`, `KEEP_CLONE`, `SCRATCH_DIR`; exit codes 0/1/2/3 |
| `.planning/phases/189-free-the-name/evidence/189-rename-02-observables.txt` | 189-02 | The three D-03 observables for an existing clone |
| `.planning/phases/189-free-the-name/evidence/189-rename-03-fresh-clone.txt` | 189-02, appended by 189-03 | Captured fixture run plus the post-advance reading |
| `.planning/phases/189-free-the-name/evidence/189-firmware-slug-sweep.txt` | 189-03 | Firmware-repository bare-slug sweep with its positive control |
| `.planning/phases/189-free-the-name/evidence/189-rename-02-branch-disposition.md` | 189-04 | RENAME-02 branch disposition |
| `.planning/phases/189-free-the-name/COVERAGE.md` | planning | Reasoned "no external API integration" declaration |
| meta branch `v1.38-gitmodules-main` | 189-04 | A git ref, not a file |

## Verification

- The firmware repository holds zero live tracked bare-slug references, with a control proving the
  sweep ran.
- The gitlink names the commit carrying those edits, and the advance happened after `189-02`'s
  transcript existed.
- Nothing was added to the firmware repository beyond the two repointed lines: no guard, no workflow,
  no comment.

## Success criteria

- ROADMAP criterion 4 is satisfied: the firmware repository's own README and release links address
  the new name.
- D-09's boundary holds: Phase 192 will find the firmware repository already clean and will only need
  to re-verify it.
- D-05's ordering holds and is evidenced by the order of the two readings in the RENAME-03 transcript.

<output>
Create `.planning/phases/189-free-the-name/189-03-SUMMARY.md` when done.
</output>
