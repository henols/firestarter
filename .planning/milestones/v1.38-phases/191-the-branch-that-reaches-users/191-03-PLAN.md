---
phase: 191-the-branch-that-reaches-users
plan: 03
type: execute
wave: 3
depends_on: ["191-02"]
files_modified:
  - .planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt
  - .planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt
autonomous: false
requirements: [URL-02, STABLE-01]
user_setup:
  - service: github
    why: "firestarter_app's main is protected by ruleset 22046179 with current_user_can_bypass: never, so a pull request is the only route, and every push in this project is operator-gated (D-7). The agent prepared the branch in 191-02; the operator pushes it, opens the pull request and merges it."
    dashboard_config:
      - task: "Push branch v1.38-url-02-main from the firestarter_app submodule, open a pull request against henols/firestarter_app main, and merge it"
        location: "GitHub -> henols/firestarter_app -> Pull requests"
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-06 this is Gate 1 of two operator gates, and the agent observes between them rather than assuming. The operator pushes, opens and merges; the agent then reads the release.yml run conclusion LIVE and branches on what it actually sees."
    - "Per 189-04, which is binding, the merged state of a protected branch is verified by reading the file back from the GitHub contents API at ref=main, never from the local clone and never from the operator's report. A local branch proves only what was prepared, not what landed."
    - "Every zero-count reading is accompanied by a positive control taken over the same content in the same run, so an absence is proved rather than assumed - a zero can otherwise come from an empty response rather than from a clean branch."
    - "Per D-04 the predicted outcome is that release.yml fails: merging fires it because paths-ignore does not cover firestarter/**, its update_version.py step bumps the patch version, and stefanzweifel/git-auto-commit-action@v5 then pushes directly to main using the default GITHUB_TOKEN, which cannot bypass ruleset 22046179's pull_request rule whose only bypass actor is DeployKey. The push is rejected, the job aborts, and the Release step never runs."
    - "Per D-04 the prediction is recorded BEFORE the merge precisely so that the run's failure reads as a confirmed measurement rather than as an incident. The run URL and conclusion belong in the phase record."
    - "Per D-06 the unexpected-success branch is real plan content, not an aside. If release.yml completes, the bot cut 2.0.10 rather than 2.0.9, because update_version.py bumps on top of the 2.0.9 the pull request carries. In that case the hand-cut in 191-04 is skipped and the real version is read back and recorded."
    - "This has never been exercised under protection: release.yml's last run before this phase was 2026-08-07 on e4112ef, 25 days before ruleset 22046179 was created on 2026-09-01. Whatever the run does is therefore new information."
    - "Per D-10 Phase 191 owns origin/main's slug references outright; the post-merge read-back here is what lets Phase 192 re-verify origin/main as a git ref rather than editing it or opening a second pull request against a protected branch."
    - "gh run view --log requires XDG_CACHE_HOME to be set in this container because ~/.cache/gh is unwritable, and the resulting error misdiagnoses as an auth failure. gh run list and gh api are unaffected."
  prohibitions:
    - "MUST NOT push, open a pull request, merge, tag, create a release or dispatch a workflow. The operator performs every one of these; the agent prepares and verifies. main is protected in all three repositories and current_user_can_bypass is never."
    - "MUST NOT record the predicted release.yml failure as the observed outcome without reading the run. The prediction's evidential value comes entirely from it having been written down before the merge and then checked against reality; asserting it instead of reading it converts a measurement into a guess and destroys the record."
    - "MUST NOT verify the merge against the local clone, the prepared branch, or the operator's message. Only a read from the GitHub contents API at ref=main says what actually landed."
    - "MUST NOT be executed with --auto or --chain. In this project those flags auto-approve human-verify gates, and autonomous: false alone does not protect a gate. This plan reaches a protected default branch that every default pip install resolves from."
  artifacts:
    - path: ".planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt"
      provides: "The live contents-API read-back of all three files at ref=main after the merge, with the boundary-aware zero-count sweep and its non-vacuity positive control, plus the pull request number and merge commit sha"
      contains: "ref=main"
    - path: ".planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt"
      provides: "The observed release.yml run: its id, URL, event, conclusion and which branch of D-06 it selected, recorded next to the prediction it tests"
      contains: "conclusion"
  key_links:
    - from: "firestarter_app branch v1.38-url-02-main"
      to: "henols/firestarter_app main"
      via: "an operator-pushed, operator-opened, operator-merged pull request - the only route past ruleset 22046179"
      pattern: "firestarter_fw"
    - from: "the merge commit on main"
      to: "the version that will be cut in 191-04"
      via: "the release.yml run conclusion, read live, which selects the hand-cut branch or the bot-cut branch of D-06"
      pattern: "conclusion"
---

> **Outward-facing. Do not execute this plan with `--auto` or `--chain`.** Those flags auto-approve
> human-verify gates in this project, and `autonomous: false` alone does **not** protect a gate. This
> plan lands a change on `firestarter_app`'s protected default branch — the branch every default
> `pip install firestarter` resolves from. Both checkpoints below are `blocking-human` and must be
> reached with a person present.

<objective>
Get URL-02 onto `origin/main` through the only route branch protection allows, then find out what the
release pipeline actually does — rather than assuming D-04 was right.

Gate 1 is the operator's: push the branch 191-02 prepared, open the pull request, merge it. Then the
agent reads back all three files from the live GitHub contents API at `ref=main` — never from this
clone, never from the operator's report — and reads the `release.yml` run conclusion live, taking
whichever branch of D-06 the observation selects.

Purpose: the prediction in D-04 is only evidence because it was written down before the merge. This
plan is where it meets reality, and both outcomes are planned for.
Output: a merged protected branch, a live read-back transcript, and a recorded run observation that
tells 191-04 which version to cut.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-the-branch-that-reaches-users/191-CONTEXT.md
@.planning/phases/191-the-branch-that-reaches-users/191-PATTERNS.md
@.planning/notes/v135-close-procedure-under-protection.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Deliberately NOT `@`-included: `.planning/STATE.md` (~125k tokens, one 52k-char line),
`.planning/ROADMAP.md`, `.planning/PROJECT.md`.

**Measured at planning time, cite rather than re-measure.** `gh run list -R henols/firestarter_app
--workflow release.yml` returns exactly **two** runs, both `success`, the most recent
`2026-08-07T18:00:25Z` on `e4112ef` — 25 days before ruleset `22046179` was created on 2026-09-01.
`release.yml`'s `paths-ignore` covers `**.md`, `**.sh`, `.gitignore`, `docs/**`, `images/**`,
`.github/**`, `.vscode/**`, `tools/**` — **not** `firestarter/**`. Its `Release` step is
`softprops/action-gh-release@v2` with `make_latest: true` and **no** `target_commitish`.
`gh api "repos/henols/firestarter_app/contents/firestarter/__init__.py?ref=main"` with
`-H "Accept: application/vnd.github.raw"` currently returns `__version__ = "2.0.8"` and is the exact
read-back surface this plan uses.

**Container caveat.** `gh run view --log` needs `XDG_CACHE_HOME` exported (for example
`/tmp/ghcache`) because `~/.cache/gh` is unwritable here, and the failure misdiagnoses as an auth
error. `gh run list`, `gh run view --json` and `gh api` are unaffected.

Follow the repository's standing commit-attribution convention for every commit this plan makes.
</context>

<flagged_assumptions>
**Unresolved edge-probe rows carried by this plan.** URL-02 (`unclassified`) — see 191-02's note; the
"three constants" reading is settled by D-01 as a decision, not derived by the probe. STABLE-01
(`unclassified`) — the requirement assumes a cut from `main` reaches PyPI; D-04 and D-05 record that
the automatic pipeline is predicted to fail in two places, so the assumption being carried is that the
manual route in 191-04 succeeds. Neither is silently resolved. The full four-row disposition table
lives in `191-01-PLAN.md`.
</flagged_assumptions>

<!-- planner-discipline-allow: henols/firestarter([^_a-zA-Z0-9]|$) -->

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator pushes v1.38-url-02-main, opens the pull request and merges it</name>
  <reversibility rating="costly">Per D-03: undoing a merge to a protected default branch needs a second pull request through the same protection, and any `pip install` taken from a release cut in the interval already carries the change. Reversible in principle, expensive in practice.</reversibility>
  <precondition>Branch `v1.38-url-02-main` exists in `/workspaces/firestarter_app` with exactly one commit over three files against `origin/main` (191-02 Task 1 verified this); the operator has push rights on `henols/firestarter_app`.</precondition>
  <read_first>
    - `.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt` —
      the readings taken in 191-02, so the resume message can state exactly what is being merged.
    - `.planning/notes/v135-close-procedure-under-protection.md` — the pull-request-only route into a
      protected `main` and the banked evidence that the route works.
    - `.planning/phases/189-free-the-name/189-04-SUMMARY.md` — the binding precedent for how this
      handoff is shaped and what the agent does after it.
  </read_first>
  <action>
Stop here. `firestarter_app`'s `main` is protected by ruleset `22046179` with
`current_user_can_bypass: never`, so a pull request is the only route, and every push in this project
is operator-gated (D-7). The agent has prepared the branch and verified its diff three ways; the push,
the pull request and the merge are the operator's.

Before handing over, restate the prediction that this merge is about to test, so it is on the record
as a prediction rather than a hindsight explanation: per D-04, merging is expected to fire
`release.yml`, whose auto-commit step will be rejected by the ruleset, aborting the job before the
`Release` step — no tag, no GitHub release, nothing on PyPI.
  </action>
  <instructions>
Branch `v1.38-url-02-main` is prepared locally in the `firestarter_app` submodule at
`/workspaces/firestarter_app`. Its single commit changes three files against `origin/main`:

- `firestarter/constants.py` — the firmware release endpoint now addresses `henols/firestarter_fw`
- `README.md` — the firmware link now addresses `henols/firestarter_fw`
- `firestarter/__init__.py` — `__version__` `2.0.8` → `2.0.9`

I have verified the diff is exactly those three files at one insertion and one deletion each, that a
boundary-aware sweep of the branch finds no remaining unqualified firmware-slug reference, that the
`henols/firestarter_app` control still reads non-zero, and that no comment line was added.

Please:

1. Push the branch from `/workspaces/firestarter_app`.
2. Open a pull request against `henols/firestarter_app` `main`.
3. Merge it.

Do not merge anything to `beta` — that fires a pre-release cut in both sub-repositories and publishes
the host one; this project's close carries `beta`.

**What I expect to happen next, recorded now so the observation means something:** the merge should
fire `release.yml`, and I expect it to **fail** — its auto-commit step pushes directly to `main` with
the default `GITHUB_TOKEN`, which cannot bypass the `pull_request` rule, so the job should abort
before it ever creates a tag or a release. If instead it succeeds, the bot will have cut **2.0.10**
rather than 2.0.9, because it bumps on top of the version the pull request carries. Either way I will
read the run rather than assume, and I will tell you which of the two we are in.

Tell me the pull request number and, once merged, the merge commit sha.
  </instructions>
  <verification>
After you resume I will read all three files back from GitHub rather than from this clone — the
contents API at `repos/henols/firestarter_app/contents/<path>` with `ref=main` — and assert the
repointed endpoint, the repointed README link and `2.0.9`, each paired with a positive control so a
zero-count is proved to be a real absence. Then I will read the `release.yml` run conclusion live and
record which branch of D-06 we are on.
  </verification>
  <acceptance_criteria>
    - No command run by the agent pushed a branch, created a pull request, merged one, created a tag,
      published a release or dispatched a workflow.
    - The checkpoint carries `gate="blocking-human"`, so an unattended run stops here rather than
      auto-approving an outward-facing step.
    - The handoff message states the D-04 prediction in advance, in words, including the 2.0.10
      consequence of the unexpected-success branch.
    - The resume message records the pull request number and the merge commit sha.
  </acceptance_criteria>
  <resume-signal>Reply with the pull request number and merge sha, for example "merged, app#NN, sha abc1234", or describe what stopped you.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Read all three files back from the live contents API at ref=main and record the read-back</name>
  <files>.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt</files>
  <read_first>
    - `.planning/phases/189-free-the-name/189-04-SUMMARY.md` — the verbatim pattern: "The merged state
      of a protected branch is verified by reading the file back from the GitHub contents API at the
      target ref, never from the local clone, since a local branch proves only what was prepared, not
      what landed."
    - `.planning/phases/191-the-branch-that-reaches-users/191-PATTERNS.md` § "Post-merge verification"
      — the four concrete `gh api` commands adapted for `firestarter_app`.
    - `.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt` —
      the prepared-branch readings, so the merged readings can be compared against them.
  </read_first>
  <action>
Read each of the three files back from GitHub at `ref=main`, requesting the raw representation, and
assert against the response body rather than against anything local:

- `firestarter/constants.py` must contain
  `https://api.github.com/repos/henols/firestarter_fw/releases/latest` exactly once.
- `README.md` must contain `https://github.com/henols/firestarter_fw`, and must still contain the
  `henols/firestarter_app` badge URL — that second reading is the positive control proving the
  response carried real content.
- `firestarter/__init__.py` must read `2.0.9` — unless the `release.yml` bot succeeded and bumped it,
  which Task 3 determines; if Task 3 finds a successful run, re-read this file and record the value
  the bot left, naming it as such rather than treating it as a failure of this task.

Pair every zero-expectation reading with a control over the same response in the same run. A zero
bare-slug count next to a zero control is a failed gate, not a pass.

Also read the pull request object itself: assert `merged` is true and record `merge_commit_sha` and
the pull request number from the API rather than from the operator's message.

Write everything into
`.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt` with the
capture timestamp, the exact commands used, their raw output, and one line per assertion naming what
was asserted and what was read. Commit it in the meta repository on the milestone branch.
  </action>
  <verify>
    <automated>gh api "repos/henols/firestarter_app/contents/firestarter/constants.py?ref=main" -H "Accept: application/vnd.github.raw" | /usr/bin/grep -c 'https://api.github.com/repos/henols/firestarter_fw/releases/latest'</automated>
    <fails_when>prints anything other than `1`, or the command exits non-zero (for example `gh: Not Found (HTTP 404)`) — the merged default branch does not carry the repointed endpoint</fails_when>
    <automated>gh api "repos/henols/firestarter_app/contents/firestarter/constants.py?ref=main" -H "Accept: application/vnd.github.raw" | /usr/bin/grep -cE 'henols/firestarter([^_a-zA-Z0-9]|$)' || true</automated>
    <fails_when>prints anything other than `0` — a reference to the unqualified firmware slug survived onto the merged default branch</fails_when>
    <automated>gh api "repos/henols/firestarter_app/contents/README.md?ref=main" -H "Accept: application/vnd.github.raw" | /usr/bin/grep -c 'https://github.com/henols/firestarter_fw'</automated>
    <fails_when>prints `0` — the merged README still sends readers to the unqualified slug</fails_when>
    <automated>gh api "repos/henols/firestarter_app/contents/README.md?ref=main" -H "Accept: application/vnd.github.raw" | /usr/bin/grep -cE 'henols/firestarter([^_a-zA-Z0-9]|$)' || true</automated>
    <fails_when>prints anything other than `0` — the README still addresses the unqualified slug somewhere</fails_when>
    <automated>gh api "repos/henols/firestarter_app/contents/README.md?ref=main" -H "Accept: application/vnd.github.raw" | /usr/bin/grep -c 'henols/firestarter_app'</automated>
    <fails_when>prints `0` — the non-vacuity control for the two README legs above; a zero here means the API returned no usable body, not that the README is clean</fails_when>
    <automated>gh api "repos/henols/firestarter_app/contents/firestarter/__init__.py?ref=main" -H "Accept: application/vnd.github.raw"</automated>
    <fails_when>prints `__version__ = "2.0.8"`, or exits non-zero — the version bump did not land. A value of `2.0.10` is NOT a failure of this leg: it means the `release.yml` bot succeeded, which Task 3 determines and records</fails_when>
    <automated>/usr/bin/grep -c 'ref=main' /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt</automated>
    <fails_when>prints `0`, or exits non-zero because the record is absent — the transcript does not show the readings were taken at the merged ref rather than from the local clone</fails_when>
    <automated>/usr/bin/grep -cE 'merge_commit_sha|merged' /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt</automated>
    <fails_when>prints `0` — the pull request's merged state and merge sha were not read from the API and recorded</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/phases/191-the-branch-that-reaches-users .planning/config.json | wc -l</automated>
    <fails_when>prints anything other than `0` — the evidence file is uncommitted, or `.planning/config.json` was pruned by a state-writing verb and the prune is about to be committed</fails_when>
  </verify>
  <acceptance_criteria>
    - All three files were read from `repos/henols/firestarter_app/contents/<path>?ref=main` with the
      raw accept header; the transcript records the exact commands and their raw output.
    - `constants.py` at `ref=main` contains the repointed endpoint exactly once and no boundary-aware
      unqualified-slug match.
    - `README.md` at `ref=main` contains the repointed firmware link, no boundary-aware
      unqualified-slug match, and at least one `henols/firestarter_app` match as the non-vacuity
      control.
    - `firestarter/__init__.py` at `ref=main` reads `2.0.9`, or reads `2.0.10` with Task 3 recording a
      successful `release.yml` run as the cause.
    - The pull request object was read from the API and shows `merged: true`; its number and
      `merge_commit_sha` are recorded from the API response, not from the operator's message.
    - The evidence file is committed on the milestone branch and `.planning/config.json` is unmodified.
  </acceptance_criteria>
  <done>
What actually landed on the protected default branch is on the record, read from GitHub, with every
zero proved non-vacuous by a sibling control.
  </done>
</task>

<task type="auto">
  <name>Task 3: Read the release.yml run live and take whichever branch of D-06 it selects</name>
  <files>.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt</files>
  <read_first>
    - `.planning/phases/191-the-branch-that-reaches-users/191-CONTEXT.md` — D-04 in full (the
      mechanism and the prediction) and D-06 in full (the two branches and what each one does next).
    - `firestarter_app/.github/workflows/release.yml` **on `origin/main`** via `git show` — the
      `paths-ignore` list, the `update_version.py` step, the `git-auto-commit-action` push and the
      `softprops/action-gh-release@v2` step with no `target_commitish`.
    - `.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt` — the
      merge commit sha, so the run is matched to this merge rather than to any other push.
  </read_first>
  <action>
List the `release.yml` runs live for `henols/firestarter_app` and find the one whose head sha is the
merge commit recorded in Task 2. Two runs existed before this phase, both `success`, the most recent
on 2026-08-07; anything newer is this merge's run. If no run has appeared yet, wait and re-check
before concluding — `paths-ignore` does not cover `firestarter/**`, so a run is expected.

Record, in
`.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt`: the
capture timestamp, the run id, the run URL, the event, the head sha, the conclusion, and the D-04
prediction quoted next to it so the two can be compared by a reader.

Then branch on what was actually read — do not hard-code the predicted outcome as the only path:

**Branch A — the run failed (D-04's prediction confirmed).** Identify the step that failed. If step
logs are needed, export `XDG_CACHE_HOME` (for example to `/tmp/ghcache`) before `gh run view --log`,
because `~/.cache/gh` is unwritable here and the resulting error misdiagnoses as an auth failure.
Record that no tag and no GitHub release were created — check both, do not infer. Then write into the
same file the **exact commands the operator will run in 191-04**, filled in with the real merge sha:
the tag creation and push, the GitHub release creation for tag `2.0.9` targeting that sha, and the
`publish.yml` `workflow_dispatch` with its `tag` input set to `2.0.9`. Do not run any of them.

**Branch B — the run succeeded (D-04's prediction falsified).** Read back which version the bot
actually cut, from the releases API rather than from the run summary. Expect `2.0.10`, because
`update_version.py` bumps on top of the `2.0.9` the pull request carried; record whatever it really
is. Record the tag, the release, and the commit the tag points at — noting that the `Release` step
carries no `target_commitish`, so the tag's commit may not be the bumped one. Then check whether the
`release: published` event reached `publish.yml` this time, by listing that workflow's runs and
reading the event of any new one; per D-05 it has never fired in 8 of 8 runs. Record the finding
either way. In this branch the hand-cut in 191-04 is **skipped**; write that conclusion into the file
explicitly, along with the real version, so 191-04 acts on the observation rather than on the
prediction.

Whichever branch was taken, state it in one unambiguous line near the top of the file, so 191-04 and
the phase record read the same conclusion. Commit the file in the meta repository on the milestone
branch.

Do not push, do not tag, do not create a release, do not dispatch a workflow. Those are 191-04's
operator gate.
  </action>
  <verify>
    <automated>gh run list -R henols/firestarter_app --workflow release.yml --limit 5</automated>
    <fails_when>exits non-zero, or lists no run newer than the `2026-08-07` run that existed before this phase — the merge did not fire the workflow, which contradicts the measured `paths-ignore` list and means the run being recorded is not this merge's</fails_when>
    <automated>/usr/bin/grep -cE 'conclusion' /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt</automated>
    <fails_when>prints `0`, or exits non-zero because the record is absent — the run's conclusion was not recorded, so the phase has a prediction with nothing to test it against</fails_when>
    <automated>/usr/bin/grep -cE 'https://github.com/henols/firestarter_app/actions/runs/[0-9]+' /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt</automated>
    <fails_when>prints `0` — the run URL is missing, so a later reader cannot re-open the run this phase's central prediction was tested against</fails_when>
    <automated>/usr/bin/grep -ciE 'branch a|branch b' /workspaces/.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt</automated>
    <fails_when>prints `0` — the record does not state which branch of D-06 was selected, leaving 191-04 to act on the prediction instead of the observation</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/phases/191-the-branch-that-reaches-users .planning/config.json | wc -l</automated>
    <fails_when>prints anything other than `0` — the run record is uncommitted, or `.planning/config.json` was pruned and the prune is about to be committed</fails_when>
  </verify>
  <acceptance_criteria>
    - The run recorded is matched to the merge commit sha read in Task 2, not merely to "the newest
      run".
    - The record carries the run id, the run URL, the event, the head sha and the conclusion, all read
      live, with the D-04 prediction quoted alongside for comparison.
    - Exactly one of the two branches is stated in one unambiguous line near the top of the file.
    - If branch A: the failing step is named; the absence of a tag and of a GitHub release is
      **checked** rather than inferred; and the three exact operator commands for 191-04 are written
      out with the real merge sha substituted.
    - If branch B: the version the bot actually cut is read from the releases API and recorded; the
      commit the tag points at is recorded; whether `publish.yml` received a `release` event is
      checked and recorded; and the file states explicitly that the hand-cut is skipped.
    - No tag, release, push or workflow dispatch was performed by the agent.
  </acceptance_criteria>
  <done>
The pipeline's real behaviour under branch protection is measured and written down, and 191-04 knows
which version to cut — or that there is nothing left to cut.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new — exclude from drift verification):

- `.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-merged-main.txt` (new file)
- `.planning/phases/191-the-branch-that-reaches-users/evidence/191-stable-01-release-run.txt` (new file)
- The "Branch A" / "Branch B" labels used in the run record are introduced by this plan and name
  D-06's two observation outcomes.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → protected default branch | A human performs the only act that can write `main`; the ruleset, not the agent, is the control. |
| GitHub API → phase record | Remote state is read and becomes the phase's evidence; the local clone is deliberately not trusted for this. |
| merged `main` → GitHub Actions | A merge triggers an automated workflow that holds `contents: write` and a release token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-191-03-01 | Elevation of privilege | `release.yml`'s `git-auto-commit-action` pushing directly to `main` with `contents: write` | high | mitigate | This phase does not grant, widen or work around that privilege. The ruleset rejects the push, which is the correct outcome; the collision is filed as a backlog item in 191-02 rather than resolved by loosening protection. No ruleset change and no bypass actor is added. |
| T-191-03-02 | Repudiation | verifying the merge from the local clone or the operator's report | high | mitigate | Every assertion is taken from the GitHub contents API at `ref=main` and from the pull request object, with the commands and raw output recorded. A local branch proves only what was prepared. |
| T-191-03-03 | Spoofing | a zero-count reading produced by an empty API response | medium | mitigate | Every zero-expectation reading is paired with a positive control over the same response in the same run; a zero next to a zero control is treated as a failed gate. |
| T-191-03-04 | Tampering | recording the predicted failure instead of the observed one | high | mitigate | The prediction is stated in the handoff before the merge and quoted next to the observation afterwards; both D-06 branches are written as plan content and the record must name which one was selected. |
| T-191-03-05 | Information disclosure | run logs pasted into the phase record | low | accept | `release.yml` handles `GITHUB_TOKEN` and `PERSONAL_ACCESS_TOKEN` through Actions, which masks secrets in logs. Only the failing step name and the job conclusion are recorded, not raw environment dumps. |
</threat_model>

<verification>
- All three files read back from the live contents API at `ref=main` carry the repointed values, with
  a non-vacuity control alongside every zero-count.
- The pull request object shows `merged: true`; its number and merge sha come from the API.
- The `release.yml` run for that merge sha is recorded with id, URL, event and conclusion, next to the
  D-04 prediction.
- Exactly one D-06 branch is stated, and the branch-specific follow-through is present.
- Nothing was pushed, tagged, released or dispatched by the agent.
</verification>

<success_criteria>
URL-02 is on the branch that reaches users, proved by reading GitHub rather than this clone, and the
release pipeline's real behaviour under protection is measured rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/191-the-branch-that-reaches-users/191-03-SUMMARY.md` when done.
</output>
