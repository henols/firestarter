---
phase: 191-the-branch-that-reaches-users
plan: 02
type: execute
wave: 2
depends_on: ["191-01"]
files_modified:
  - firestarter_app/firestarter/constants.py
  - firestarter_app/README.md
  - firestarter_app/firestarter/__init__.py
  - .planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md
  - .planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md
  - .planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt
autonomous: true
requirements: [URL-02]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 origin/main carries exactly ONE firmware-release constant, FIRESTARTER_RELEASE_URL at firestarter/constants.py:9, and repointing it fully meets URL-02's intent because firmware.py:107 is its only consumer and main has no other network call to the firmware repository. FIRESTARTER_RELEASES_URL and FIRESTARTER_RELEASE_BY_TAG_URL are beta-only surface introduced after main diverged and are NOT backported: module-level constants no code on main imports would ship dead surface to every default install and enlarge the diff on a 951-commit-stale protected branch."
    - "Per D-01, URL-02's own text reads the same three constants. It is recorded satisfied under the explicit reading one constant present, one constant repointed, zero code paths on a redirect. It is not marked partial and the requirement text is not rewritten."
    - "Per D-03 the change is ONE pull request over exactly THREE files: firestarter/constants.py, README.md and firestarter/__init__.py. The first two are the complete boundary-aware sweep of origin/main - git grep -nE 'henols/firestarter([^_a-zA-Z0-9]|$)' origin/main returns exactly README.md:17 and firestarter/constants.py:9, re-verified live at planning time. There is no fourth file."
    - "The boundary-aware pattern exists because the unqualified slug is a substring of firestarter_fw; a naive pattern would report the repointed branch as still dirty and a naive replacement would produce firestarter_fw_fw."
    - "constants.py lines 8-10 are a parenthesised assignment with the URL literal alone on line 9, so an exact-match edit targets line 9's string rather than the assignment statement."
    - "Per D-04 the branch carries __version__ 2.0.8 -> 2.0.9 so the merged commit is already correct for a hand-cut release. README.md sits inside release.yml's paths-ignore, but constants.py does not, so folding the README in changes nothing about whether the cut fires."
    - "Per D-03 and 189-04, which is the binding procedural precedent, the change is prepared in a scratch worktree off origin/main created OUTSIDE /workspaces, committed there with plain git commit, and the worktree is then removed - the branch survives, waiting for the operator. origin/main is never checked out in the primary working tree."
    - "The agent never pushes, opens a pull request or merges. Every outward-facing step in this project is operator-gated (D-7) and this phase's gates are 191-03 and 191-04."
    - "Per CLAUDE.md's non-overridable rule, no comment is added to constants.py or __init__.py. The rationale goes in the commit message and in SUMMARY.md; a reader of the pip package has no .planning/ and never will."
    - "Per D-05 the two pipeline defects are FILED, not fixed. beta-release.yml on beta already carries the proven fix - a pypi job with uses ./.github/workflows/publish.yml, with tag, secrets inherit, plus target_commitish on the release - and each backlog item names it. Porting it here would be inert, because D-04 declined to fix the push blocker so the job that would call publish.yml still never runs; half a fix that cannot execute is worse evidence than a filed defect."
    - "Per D-05 the measured basis of both items: publish.yml has run 8 times in this repository's entire history, every one event=workflow_dispatch and zero release events ever, and the consequence is already on disk - GitHub release 2.0.8 exists from 2026-08-07 while PyPI's latest stable is 2.0.7. release.yml's git-auto-commit-action pushes directly to main with the default GITHUB_TOKEN, which cannot bypass the pull_request rule of ruleset 22046179 whose only bypass actor is DeployKey."
    - "Per D-02 no regression guard is added on main: origin/main has no tests/ directory, no [test] extra in pyproject.toml and no ci.yml, so a pin test would have nowhere to live and nothing to run it. The resulting gap is real and is named in the phase record (191-05), not papered over."
    - "Per D-10 this phase owns origin/main's slug references outright and Phase 192 re-verifies origin/main as a git ref without editing it. Without D-10 SWEEP-01's both sub-repo READMEs line falls through both phases, because Phase 192 sweeps the milestone branch where origin/main's README is invisible."
    - "No commit in this plan lands on the milestone branch of firestarter_app, so the meta repository's firestarter_app gitlink is NOT advanced by this phase. The app-repository commits live on a scratch branch off origin/main; the fixture, transcripts, evidence and backlog items live in the meta repository under .planning/."
  prohibitions:
    - "MUST NOT push any branch, open any pull request, merge anything, create a tag, publish a release, or dispatch a workflow. The agent prepares; the operator performs. This is 189-04's binding precedent and D-7's standing posture, and main is protected in all three repositories with current_user_can_bypass set to never."
    - "MUST NOT carry any change beyond the three named files onto the main-based branch. origin/main is 951 commits behind beta and reaches every default install; anything riding along is an unreviewed regression on the branch that reaches users. In particular do not backport the two beta-only constants, the click CLI, cli_handlers.py, submit.py, a tests/ directory or a ci.yml."
    - "MUST NOT add a comment to any file under firestarter_app/. The rule is not overridable by a plan, a task, a skill or a subagent instruction, and a version bump plus a URL swap is exactly the kind of change an executor reflexively annotates with a phase or decision citation."
    - "MUST NOT edit, port or otherwise modify any .github/workflows/ file in any repository. beta-release.yml is read for the pattern the two backlog items must name, and nothing more."
  artifacts:
    - path: ".planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md"
      provides: "The filed defect for release.yml's git-auto-commit-action push being rejected by the Protect main ruleset, naming beta-release.yml as the proven pattern"
      contains: "beta-release.yml"
    - path: ".planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md"
      provides: "The filed defect for publish.yml's release: published trigger never having fired in 8 of 8 runs, naming beta-release.yml's pypi job as the proven pattern"
      contains: "beta-release.yml"
    - path: ".planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt"
      provides: "The prepared-branch readings: the three-file numstat against origin/main, the repointed constant, the version bump, the zero bare-slug sweep and its non-vacuity positive control"
      contains: "firestarter_fw"
  key_links:
    - from: "firestarter_app branch v1.38-url-02-main"
      to: "https://api.github.com/repos/henols/firestarter_fw/releases/latest"
      via: "FIRESTARTER_RELEASE_URL at firestarter/constants.py line 9, whose only consumer is firmware.py:107"
      pattern: "firestarter_fw/releases/latest"
    - from: "the prepared branch's firestarter/__init__.py"
      to: "the hand-cut 2.0.9 release the operator makes in 191-04"
      via: "__version__ carried in the merged commit so the tag and the sdist agree without a bot bump"
      pattern: "2.0.9"
---

<objective>
Prepare, locally and only locally, the one commit that repoints the branch every default `pip install`
lands on — and file the two pipeline defects that will stop that commit reaching PyPI on its own.

Three files, one commit, on a branch forked from `origin/main` in a scratch worktree outside
`/workspaces`. Nothing is pushed. Per D-01 the branch carries **one** release constant, not three,
and the two missing names are deliberately not backported. Per D-05 the `release.yml` and
`publish.yml` defects are written down with their measurements and the known-good fix named, and
neither is repaired here.

Purpose: this is URL-02's whole substance, and it is the only deliberate write to a protected `main`
in the milestone. It is prepared *after* 191-01 proved the instrument that will measure it, so the
outward-facing act in 191-03 is taken against a measurement already shown capable of failing.
Output: a local branch waiting for the operator, two backlog items, and a readings transcript.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/191-the-branch-that-reaches-users/191-CONTEXT.md
@.planning/phases/191-the-branch-that-reaches-users/191-PATTERNS.md
@.planning/notes/v135-close-procedure-under-protection.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

Deliberately NOT `@`-included: `.planning/STATE.md` (~125k tokens, one 52k-char line),
`.planning/ROADMAP.md`, `.planning/PROJECT.md`. Everything needed is quoted in `191-CONTEXT.md`.

**Branch-protection facts this plan is built on, measured live and not to be re-measured.** Ruleset
`22046179` "Protect main", `enforcement: active`, scope `~DEFAULT_BRANCH`, and `firestarter_app`'s
default branch **is** `main`. Rules: `deletion`, `non_fast_forward`, `pull_request`. Bypass actors:
`DeployKey` only. `current_user_can_bypass: never` — no person can bypass it, the operator included.
A pull request is the only route, and this project's close targets `beta`, not `main`.

**The exact current lines on `origin/main`, re-verified at planning time.** `firestarter/constants.py`
lines 8–10 are a parenthesised assignment whose URL literal sits alone on line 9. `README.md:17` is
the firmware link sentence. `firestarter/__init__.py` is a single line carrying `2.0.8`.
`README.md:11` separately contains a `raw.githubusercontent.com/henols/firestarter_app/...` badge URL
— that is the **app** repository, correctly named, and it is the positive control proving a
boundary-aware sweep is reading real content rather than an empty file.

**Where the work happens.** The three source files are inside the git submodule
`/workspaces/firestarter_app`, and the commit lands **inside that submodule** — but on a scratch
branch off `origin/main`, **not** on the milestone branch. Therefore the meta repository's
`firestarter_app` gitlink is **not** advanced by this phase. The two backlog items are meta-repository
files and are committed on the milestone branch there.

**Pre-existing worktrees.** `git -C /workspaces/firestarter_app worktree list` already shows **three**
entries — the primary tree plus two v1.34 bench arms under `/workspaces/.v1.34-arms/`. Do not assert
a count of one; assert that no worktree entry names this plan's branch after cleanup.

Follow the repository's standing commit-attribution convention for every commit this plan makes.
</context>

<flagged_assumptions>
**Unresolved edge-probe row carried by this plan (URL-02, `unclassified`).** The deterministic probe
could not classify URL-02, and the requirement's own text says "the same three constants" while
`origin/main` carries **one**. D-01 settles the reading — one constant present, one constant
repointed, zero code paths on a redirect — but that reading is a decision recorded in `CONTEXT.md`,
not a property the probe derived. It is surfaced here rather than silently resolved. Do not mark
URL-02 partial and do not rewrite the requirement; D-01 is explicit on both points. The full
four-row disposition table lives in `191-01-PLAN.md`.
</flagged_assumptions>

<!-- planner-discipline-allow: henols/firestarter([^_a-zA-Z0-9]|$) -->

<tasks>

<task type="auto">
  <name>Task 1: Prepare branch v1.38-url-02-main — three files, one commit, off origin/main</name>
  <files>
    firestarter_app/firestarter/constants.py
    firestarter_app/README.md
    firestarter_app/firestarter/__init__.py
    .planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt
  </files>
  <read_first>
    - `.planning/phases/189-free-the-name/189-04-PLAN.md` Task 1 and
      `.planning/phases/189-free-the-name/189-04-SUMMARY.md` — the **binding procedural precedent**:
      scratch worktree off `origin/main` kept outside `/workspaces`, plain `git commit` there,
      `git worktree remove` after, branch survives, agent never pushes/opens/merges.
    - `.planning/phases/191-the-branch-that-reaches-users/191-PATTERNS.md` §3–5 — the exact current
      text of all three lines, quoted so the edits are exact-match rather than retyped, and the
      counting-idiom verify legs.
    - The three files as they exist **on `origin/main`**, read with
      `git -C /workspaces/firestarter_app show origin/main:<path>` — never from the milestone-branch
      working copy, which is 951 commits ahead and a different codebase.
    - `CLAUDE.md` § "Source code comments — hard rule" and § "Milestone close and branch protection".
  </read_first>
  <action>
Fetch from `origin` in the submodule — a fetch, never a push. Then create a scratch worktree at a
path produced by `mktemp -d`, which resolves outside `/workspaces` so a later `git clean -Xdf` aimed
at the meta tree cannot take it, using
`git -C /workspaces/firestarter_app worktree add <scratch-path> -b v1.38-url-02-main origin/main`.
Do not check out `main` in the primary working tree: it is on `v1.38-repository-rename` and switching
it across 951 commits would be pointless churn.

In that worktree make exactly three edits and nothing else:

1. `firestarter/constants.py` — replace the URL string on line 9 (the sole content of the
   parenthesised assignment's middle line) with
   `"https://api.github.com/repos/henols/firestarter_fw/releases/latest"`. Preserve the four-space
   indentation and the surrounding parentheses; the assignment statement itself is untouched. Per
   D-01 do **not** add `FIRESTARTER_RELEASES_URL` or `FIRESTARTER_RELEASE_BY_TAG_URL` — they are
   `beta`-only surface, no code on `main` imports them, and adding them would ship dead module-level
   surface to every default install.
2. `README.md` — on line 17, change the firmware link target to
   `https://github.com/henols/firestarter_fw`. D-03 permits rewording beyond the slug; the minimal
   change is preferred. Leave line 11's `raw.githubusercontent.com/henols/firestarter_app/...` badge
   alone — that is the app repository and it is correctly named.
3. `firestarter/__init__.py` — the whole file is one line; change the version string from `2.0.8` to
   `2.0.9`, per D-04, so the merged commit is already correct for the hand-cut release.

Add **no comment** to any of these files. `CLAUDE.md`'s rule is not overridable by this plan: no
phase citation, no decision citation, no explanatory block. The rationale belongs in the commit
message and in `SUMMARY.md`.

Commit in the worktree with plain `git commit` — one commit, all three files. Write a commit message
that states what changed and why (the firmware repository was renamed in Phase 189; `main` is the
branch the default `pip install` lands on; the version is bumped so the release can be cut by hand
because the automatic pipeline is blocked). Then remove the worktree with
`git -C /workspaces/firestarter_app worktree remove <scratch-path>`. The branch survives the
worktree's removal — that is the point: the commit is prepared locally and waits for the operator.

Capture the readings into
`.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt`: the
`git diff --numstat origin/main v1.38-url-02-main` output, the repointed constant line, the README
link line, the version line, the boundary-aware bare-slug sweep over the whole branch reading zero,
and the `henols/firestarter_app` positive control reading non-zero in the same run — a zero is only
evidence next to a sibling reading that is not zero. Commit that evidence file in the **meta**
repository on the milestone branch.

Do not push it, do not open a pull request, and do not merge anything. That is 191-03, and it is the
operator's.
  </action>
  <verify>
    <automated>git -C /workspaces/firestarter_app rev-parse --verify v1.38-url-02-main</automated>
    <fails_when>exits non-zero with `unknown revision` — the branch was never created</fails_when>
    <automated>git -C /workspaces/firestarter_app diff --numstat origin/main v1.38-url-02-main | wc -l</automated>
    <fails_when>prints anything other than `3` — a fourth file rode along onto the branch that reaches every default install, or one of the three is missing</fails_when>
    <automated>git -C /workspaces/firestarter_app diff --numstat origin/main v1.38-url-02-main | /usr/bin/grep -cE '^1[[:space:]]+1[[:space:]]'</automated>
    <fails_when>prints anything other than `3` — one of the three files changed by more than a single line, which on a 951-commit-stale protected branch is an unreviewed regression</fails_when>
    <automated>git -C /workspaces/firestarter_app show v1.38-url-02-main:firestarter/constants.py | /usr/bin/grep -c 'https://api.github.com/repos/henols/firestarter_fw/releases/latest'</automated>
    <fails_when>prints anything other than `1` — the constant is not repointed, or was duplicated</fails_when>
    <automated>git -C /workspaces/firestarter_app show v1.38-url-02-main:firestarter/__init__.py</automated>
    <fails_when>prints anything other than a single line reading `__version__ = "2.0.9"` — the bump is missing or the file gained content</fails_when>
    <automated>git -C /workspaces/firestarter_app grep -lE 'henols/firestarter([^_a-zA-Z0-9]|$)' v1.38-url-02-main | wc -l</automated>
    <fails_when>prints anything other than `0` — a tracked file on the prepared branch still addresses the unqualified slug</fails_when>
    <automated>git -C /workspaces/firestarter_app grep -lE 'henols/firestarter_app' v1.38-url-02-main | wc -l</automated>
    <fails_when>prints `0` — the non-vacuity control for the leg above; a zero here means the sweep pipeline delivered no content, not that the branch is clean</fails_when>
    <automated>git -C /workspaces/firestarter_app diff origin/main v1.38-url-02-main -- firestarter/constants.py firestarter/__init__.py | /usr/bin/grep -cE '^\+[[:space:]]*#' || true</automated>
    <fails_when>prints anything other than `0` — a comment line was added to product source, which the project rule forbids without exception</fails_when>
    <automated>git -C /workspaces/firestarter_app worktree list | /usr/bin/grep -c 'v1.38-url-02-main' || true</automated>
    <fails_when>prints anything other than `0` — the scratch worktree was not removed</fails_when>
    <automated>git -C /workspaces/firestarter_app branch --show-current</automated>
    <fails_when>prints anything other than `v1.38-repository-rename` — the submodule's primary working tree was switched away from the milestone branch</fails_when>
    <automated>git -C /workspaces branch --show-current</automated>
    <fails_when>prints anything other than `gsd/v1.38-repository-rename-activated-2026-09-13` — the meta working tree was switched away from the milestone branch</fails_when>
    <automated>git -C /workspaces diff --name-only HEAD -- firestarter_app | wc -l</automated>
    <fails_when>prints anything other than `0` — the `firestarter_app` gitlink was advanced, which this phase must not do because none of its app commits land on the milestone branch</fails_when>
  </verify>
  <acceptance_criteria>
    - Branch `v1.38-url-02-main` exists in the `firestarter_app` repository, forked from `origin/main`,
      with exactly one commit whose diff against `origin/main` is three files at one insertion and one
      deletion each.
    - That branch's `firestarter/constants.py` contains
      `https://api.github.com/repos/henols/firestarter_fw/releases/latest` exactly once, still inside
      the parenthesised `FIRESTARTER_RELEASE_URL` assignment, and defines no other `FIRESTARTER_*_URL`
      name.
    - That branch's `README.md` line 17 links to `https://github.com/henols/firestarter_fw`, and
      line 11's `henols/firestarter_app` badge URL is unchanged.
    - That branch's `firestarter/__init__.py` is the single line `__version__ = "2.0.9"`.
    - The diff adds no line beginning with a hash character in either Python file.
    - `git grep` for the boundary-aware unqualified pattern over the branch returns zero files, and
      the `henols/firestarter_app` control in the same run returns at least one.
    - No worktree entry names `v1.38-url-02-main`; both primary working trees are still on their
      milestone branches; the `firestarter_app` gitlink is unchanged.
    - No command in this task pushed, opened a pull request, merged, tagged, released or dispatched.
  </acceptance_criteria>
  <done>
The change that reaches the default install exists as one local commit on its own branch, verified
three ways, and nothing outward-facing has happened.
  </done>
</task>

<task type="auto">
  <name>Task 2: File the two pipeline defects as backlog items naming beta-release.yml as the proven fix</name>
  <files>
    .planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md
    .planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md
  </files>
  <read_first>
    - `.planning/todos/pending/2026-08-30-sync-to-subrepos-self-diff-asserts-nothing.md` — the
      defect-shaped template: frontmatter keys, the `path (line range — what it is)` form of the
      `files:` list, and the convention of citing the CORRECT instance alongside the defect.
    - `.planning/todos/pending/2026-09-13-close-six-stale-claims-wr01-wr06.md` — the newer variant
      that adds `resolves_phase: unassigned` and a `source:` key.
    - `firestarter_app/.github/workflows/release.yml` **on `origin/main`** (via `git show`) — the
      `paths-ignore` list, the `update_version.py` step and the `git-auto-commit-action` push.
    - `firestarter_app/.github/workflows/publish.yml` **on `origin/main`** (via `git show`) — the
      `workflow_dispatch` input named `tag`, and the comment block that already documents the
      suppressed `release.published` event.
    - `firestarter_app/.github/workflows/beta-release.yml` **on `origin/beta`** (via `git show`) — the
      `pypi` job that both items must name as the proven pattern. **Read only. Never edited, never
      ported** (D-05).
  </read_first>
  <action>
Write two separate backlog items under `.planning/todos/pending/`, both dated `2026-09-13`, both with
`area: tooling`, `resolves_phase: unassigned`, and
`source: .planning/phases/191-the-branch-that-reaches-users/191-CONTEXT.md (D-04 / D-05)`. Body shape
per the template: `## Problem`, then what is measured, then why it matters, then the known-good fix,
then disposition. Follow the analog's convention of listing the defect **and** the correct instance
in `files:`, each with a short parenthetical saying what it is.

**Item 1 — `2026-09-13-release-yml-autocommit-vs-ruleset.md`.** `release.yml` runs on push to `main`
with a `paths-ignore` list that covers `**.md`, `**.sh`, `.gitignore`, `docs/**`, `images/**`,
`.github/**`, `.vscode/**` and `tools/**` but **not** `firestarter/**`, so a merge touching
`firestarter/constants.py` fires it. Its `update_version.py` step bumps the patch version and
`stefanzweifel/git-auto-commit-action@v5` then pushes directly to `main` using the default
`GITHUB_TOKEN` — its `PERSONAL_ACCESS_TOKEN` env line is commented out. Ruleset `22046179`
"Protect main", created 2026-09-01, scope `~DEFAULT_BRANCH`, enforces `pull_request` and lists
`DeployKey` as its only bypass actor, with `current_user_can_bypass: never`; a `GITHUB_TOKEN` cannot
bypass a `pull_request` rule. The push is therefore rejected, the job aborts, and the `Release` step
never runs — no tag, no GitHub release, nothing on PyPI. State that this has never been exercised
under protection: `release.yml`'s last run before this phase was `2026-08-07` on `e4112ef`, which
predates the ruleset by 25 days. Name the fix direction as a credential or settings change, or a
redesign that stops the workflow pushing at all, and name `beta-release.yml` on `beta` as the
repository's own proven release pattern. Record that Phase 191 deliberately did not fix it: a rename
milestone is not where the stable release flow gets redesigned.

**Item 2 — `2026-09-13-publish-yml-release-published-never-fires.md`.** `publish.yml` declares both
`release: [published]` and a `workflow_dispatch` taking a `tag` input, and it has run **8 times in
this repository's entire history, every one `event=workflow_dispatch`. Zero `release` events, ever.**
The `release: published` trigger has literally never fired. The consequence is already on disk:
GitHub release `2.0.8` exists (2026-08-07) while PyPI's latest stable is `2.0.7` — 2.0.8 was cut and
never published, and nothing reported an error. Name the known-good fix precisely: `beta-release.yml`
on `beta` carries a `pypi` job (`uses: ./.github/workflows/publish.yml`, `with: tag:`,
`secrets: inherit`) added because "the `release.published` event does NOT reach publish.yml… the
failure was silent — GitHub reached b17 while PyPI stopped at b15", and it also passes
`target_commitish` so the tag lands on the bumped commit. `main`'s `release.yml` has neither. State
why it was not ported in Phase 191: D-04 declined to fix the push blocker, so the job that would call
`publish.yml` still never runs — half a fix that cannot execute is worse evidence than a filed defect.

Optionally file, or record as a `SUMMARY.md` observation, the third measured `main`-branch defect
that D-09 predicts: `_compare_versions` splits on dots and calls `int()` on each part, so a
`3.0.0bNN` firmware string raises `ValueError`, is caught, logs "Could not parse version strings for
comparison" and returns `False` — a stable CLI therefore reports every beta-flashed board as out of
date and offers a downgrade. `beta` already uses PEP 440 comparison. Which of the two dispositions is
used is Claude's discretion; whichever is chosen must actually appear somewhere in the phase record.

Commit both files in the meta repository on the milestone branch. Do not run any state-writing GSD
verb; if one is run, check `git status -- .planning/config.json` afterwards and restore
`planning.sub_repos` to its four entries rather than committing the prune.
  </action>
  <verify>
    <automated>ls /workspaces/.planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md /workspaces/.planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md</automated>
    <fails_when>exits non-zero with `No such file or directory` — one or both backlog items were not filed</fails_when>
    <automated>/usr/bin/grep -c 'beta-release.yml' /workspaces/.planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md</automated>
    <fails_when>prints `0` — D-05 requires each item to name the repository's own proven pattern, and an item that omits it hands the next reader a defect with no route to a fix</fails_when>
    <automated>/usr/bin/grep -c 'beta-release.yml' /workspaces/.planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md</automated>
    <fails_when>prints `0` — same reason</fails_when>
    <automated>/usr/bin/grep -c 'workflow_dispatch' /workspaces/.planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md</automated>
    <fails_when>prints `0` — the item does not record the measurement that all 8 runs were manual dispatches, which is the whole evidential basis of the defect</fails_when>
    <automated>/usr/bin/grep -c '22046179' /workspaces/.planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md</automated>
    <fails_when>prints `0` — the item does not name the ruleset that rejects the push, so a later reader cannot check whether the collision still exists</fails_when>
    <automated>/usr/bin/grep -cE '^resolves_phase: unassigned$' /workspaces/.planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md /workspaces/.planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md | /usr/bin/grep -c ':1$'</automated>
    <fails_when>prints anything other than `2` — one or both items are missing the frontmatter key that marks a freshly filed, unassigned defect</fails_when>
    <automated>git -C /workspaces/firestarter_app status --porcelain -- .github | wc -l</automated>
    <fails_when>prints anything other than `0` — a workflow file was edited, which D-05 forbids: the defects are filed, not fixed</fails_when>
    <automated>git -C /workspaces status --porcelain -- .planning/todos/pending .planning/config.json | wc -l</automated>
    <fails_when>prints anything other than `0` — the backlog items are uncommitted, or `.planning/config.json` was pruned by a state-writing verb and the prune is about to be committed</fails_when>
  </verify>
  <acceptance_criteria>
    - Both files exist in `.planning/todos/pending/` with the `YYYY-MM-DD-kebab-summary.md` name form,
      valid frontmatter carrying `created`, `title`, `area`, `resolves_phase: unassigned`, `source:`
      and a `files:` list, and a body beginning `## Problem`.
    - Item 1 names ruleset `22046179`, the `git-auto-commit-action` push, the default `GITHUB_TOKEN`,
      the `DeployKey`-only bypass, and the `2026-08-07` last run predating the ruleset.
    - Item 2 states 8 of 8 runs were `workflow_dispatch` and zero were `release` events, and names the
      on-disk consequence: GitHub `2.0.8` present, PyPI stable `2.0.7`.
    - Both items name `beta-release.yml` as the proven pattern, and item 2 names its `pypi` job with
      `secrets: inherit` and the `target_commitish` detail.
    - Both items state the disposition — filed, not fixed in Phase 191 — with the reason.
    - No file under any `.github/workflows/` directory in any repository was modified.
    - `.planning/config.json` is unmodified and both items are committed on the milestone branch.
  </acceptance_criteria>
  <done>
Both pipeline defects are written down with their measurements, their known-good fix and their
reason for deferral, so the next stable cut meets them instead of rediscovering them.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new — exclude from drift verification):

- Git branch `v1.38-url-02-main` in the `firestarter_app` repository (new local branch, off
  `origin/main`, never pushed by the agent)
- `.planning/todos/pending/2026-09-13-release-yml-autocommit-vs-ruleset.md` (new file)
- `.planning/todos/pending/2026-09-13-publish-yml-release-published-never-fires.md` (new file)
- `.planning/phases/191-the-branch-that-reaches-users/evidence/191-url-02-prepared-branch.txt` (new file)

Modified (not new): `firestarter_app/firestarter/constants.py`, `firestarter_app/README.md`,
`firestarter_app/firestarter/__init__.py` — all on the new branch only, never on the milestone branch.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local prepared branch → protected `main` | A change crosses into the branch every default `pip install` resolves from; the crossing is operator-performed and reviewed, never agent-performed. |
| `origin/main` → scratch worktree | Remote content is materialised on local disk outside the repository tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-191-02-01 | Tampering | the prepared branch's diff against `origin/main` | high | mitigate | The diff is asserted to be exactly three files at 1 insertion / 1 deletion each, with a boundary-aware slug sweep and a non-vacuity positive control. Anything riding along onto a 951-commit-stale branch that reaches every default install would be an unreviewed change; the numstat gate makes it impossible to land silently. |
| T-191-02-02 | Elevation of privilege | agent acting on a protected branch | high | mitigate | The agent has no push, PR, merge, tag, release or dispatch step anywhere in this plan; the acceptance criteria assert none was performed, and the operator gates are separate plans behind `blocking-human` checkpoints. `current_user_can_bypass: never` means the protection holds even for the operator. |
| T-191-02-03 | Repudiation | the version the release is cut from | medium | mitigate | `__version__` is carried in the merged commit itself rather than being applied by a bot after the fact, so the tag, the sdist and the commit agree and the published artefact is traceable to one reviewed diff. |
| T-191-02-04 | Information disclosure | backlog items describing a live branch-protection weakness | low | accept | Both items describe a workflow that *fails closed* (a rejected push, an unfired trigger); neither discloses a credential, a token or an exploitable bypass. The repository is public and the workflow files themselves already are. |
| T-191-02-05 | Denial of service | leaving the scratch worktree registered | low | mitigate | The worktree is removed and its absence is asserted by name rather than by count, because the `firestarter_app` repository legitimately carries two pre-existing v1.34 bench worktrees. |
</threat_model>

<verification>
- `v1.38-url-02-main` exists off `origin/main` with one commit over exactly three files, 1/1 each.
- The repointed constant, the README link and the version string all read as specified.
- Boundary-aware slug sweep over the branch reads zero, with a non-zero `henols/firestarter_app`
  control in the same run.
- No comment line added to either Python file; no workflow file touched anywhere.
- No worktree named `v1.38-url-02-main` remains; both primary trees are on their milestone branches;
  the `firestarter_app` gitlink is unchanged.
- Both backlog items exist, name `beta-release.yml`, and carry their measurements.
- Nothing was pushed, opened, merged, tagged, released or dispatched.
</verification>

<success_criteria>
URL-02's change exists as one reviewable commit waiting for the operator, the two reasons it will not
reach PyPI on its own are written down with their proven fix, and no outward-facing act has occurred.
</success_criteria>

<output>
Create `.planning/phases/191-the-branch-that-reaches-users/191-02-SUMMARY.md` when done.
</output>
