Three steps are structurally NA by construction, not a rig or chip fault -- verbatim reasons from DERIVE-PLAN.json: id -- "no chip-id in DB entry"; blank-check -- "blank-check not applicable to FRAM (volatile/byte-rewritable, no factory-blank state)"; erase -- "FLAG_CAN_ERASE not set for this chip". None of the three appears in this row's divergence_verdict.

Family-label conflation (v1.15 record vs. this row's schema), stated once: the v1.15 EVIDENCE.md record labels this part's family "0x40 (SRAM_STD / FRAM)" -- 0x40 is the DECIMAL algorithm (40) written as though it were hexadecimal. The chip schema here renders the TRUE hexadecimal form of algorithm 40, which is 0x28 (confirmed: this run's own report shows protocol: "40" in auto_capture, i.e. decimal 40 = hex 0x28). The v1.16 ledger (.planning/v1.16/ledger/PROTOCOL-LEDGER.md:31) already caught and retired this exact conflation ("NAME-04 conflation, retired in PROTOCOLS.md Section 1.10"). The two strings differ for a naming reason, not a behavioural one -- this is NOT a divergence.

Declared operating voltage, resolved in advance: Wave 0's bench/cells/CHIP/FM1608-VCC.md established from source that this part's database-declared vcc_mv: 3300 is decorative -- never transmitted to the firmware (absent from convert_to_programmer()'s wire dict), no VCC control path exists on any shield revision (no setter in firmware source), and the field is byte-identical on both arms (never a v1.33 finding; root-caused to a pre-existing build_db.py ordering interaction, filed as a 162-10 backlog item, chip_database.json NOT edited here). Socket VCC: not measured -- no VCC control path exists in firmware and the shield's rail is fixed; v1.34 makes no electrical claim.

The predicted register-cache-elision byte-0 write defect (see verdict_file for the full comparison) did NOT manifest this run -- verify succeeded on both alternating-pattern write/verify cycles, no byte-0 mismatch. Recorded as a data point on the open todo, not as a fix or closure.

read_divergence is `not measured` and read_consistency_followup is `not applicable`, for the same systematic, already-filed reason recorded on positions 1-3 (diagnostic_report.py never exports steps[].divergence) -- not a per-position anomaly. write/verify fingerprint = "indeterminate" on both steps (verdict OK on both) -- consistent with the same ambiguous-bucket shape already observed on positions 1-3, kept as recorded detail per the operator ruling, not grounds for a divergence verdict on its own.

dedup_query_outcome for this run: scraped from the console log (see row's own field), not asserted here.
