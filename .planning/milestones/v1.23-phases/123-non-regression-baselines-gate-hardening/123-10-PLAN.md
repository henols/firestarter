---
phase: 123-non-regression-baselines-gate-hardening
plan: 10
type: execute
wave: 1
depends_on: []
repo: meta (/workspaces, .planning/ only)
files_modified:
  - .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py
  - .planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py
  - .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_control.md
  - .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_control_second.md
  - .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_avr_bench_control.md
  - .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/planted_py32_overclaim.md
  - .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/planted_missing_caveat.md
autonomous: true
requirements: [BASE-07, BASE-08]
requirement_closure: "None. Do NOT tick BASE-07 or BASE-08 here — both are closed only in 123-11."
must_haves:
  truths:
    - "A forbidden phrase fires only when a py32 token is in the same 3-line window, so v1.23's many legitimate AVR sentences about genuinely bench-validated targets do not trip the gate (D-16)"
    - "Both directions are proven by committed fixtures: a py32 overclaim that fires AND a legitimate AVR bench sentence that does not"
    - "An explicitly empty target list exits non-zero and never falls back to the real defaults — proven through the env seam, which is exactly how criterion 5 words it"
    - "With zero of the named v1.23 closing artifacts existing, the gate reports UNARMED and exits 0; once any one exists, every named target must exist, so a half-written close is a hard failure (D-15)"
    - "The never-vacuous guard runs BEFORE the missing-target guard, hardening v1.22's latent ordering wart rather than replicating it"
  artifacts:
    - path: ".planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py"
      provides: "The v1.23 claim gate: 8-phrase table, required PY32F071 caveat, D-16 proximity scoping, D-15 all-or-nothing arming, fail-closed and never-vacuous guards"
      contains: "FORBIDDEN_PATTERNS"
      min_lines: 200
    - path: ".planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_avr_bench_control.md"
      provides: "D-16's negative direction — the fixture with no v1.22 analogue, proving proximity scoping rather than merely asserting it"
      contains: "bench-validated"
  key_links:
    - from: ".planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py"
      to: "130-LEDGER.md and the other named v1.23 closing artifacts"
      via: "an explicit non-pattern default target list, armed all-or-nothing, recording the scan contract seven phases before anyone writes them"
      pattern: "130-"
---

<objective>
Copy v1.22's `check_permitted_claims.py` into this phase directory and adapt it to v1.23: the eight-row
forbidden-phrase table from the Validation Ceiling, the required caveat *"no PY32F071 hardware exists"*,
D-16's proximity scoping, and D-15's all-or-nothing arming over the v1.23 closing artifacts named now —
plus the five fixtures and the paired pytest that prove all of it.

Purpose: v1.23 is a milestone whose whole honesty problem is claiming things about silicon that does not
exist. Research C-5 proved an overclaim of exactly this shape already reached a *locked* project decision
once. This gate catches that class mechanically, in the phase's own outward-facing prose, before it
reaches a stranger. Recording the scan contract seven phases early is what makes a half-written close a
hard failure rather than a quiet omission.

Output: one checker, one pytest, five fixtures — all in the meta repo's Phase 123 directory.

**Copy first; adapt only what the requirement forces.** The env-seam contract, the two guards, the
anti-skip PASS line and the precedence pin are subtle and already correct. Only the phrase table, the
target list, the caveat, the arming and the scoping change.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`), already on branch `gsd/v1.23-py32f071-integration`. Only files under
`.planning/phases/123-non-regression-baselines-gate-hardening/` are written. No sub-repo file is touched,
so this plan is independent of the branch creation in 123-01 and runs in parallel with it.

@.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md
@.planning/phases/123-non-regression-baselines-gate-hardening/123-VALIDATION.md
</context>

<artifacts_this_phase_produces>
See `123-01-PLAN.md` §"Artifacts this phase produces" for the full phase-wide list.

Introduced by THIS plan:

| Symbol / path | Kind | Consumed by |
|---|---|---|
| `.planning/phases/123-…/check_permitted_claims.py` | checker CLI | 123-11 (runs it over `123-NONREGRESSION.md`), Phase 130 CLOSE-02/CLOSE-04 |
| `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PROSE`, `REQUIRED_CAVEAT_PATTERN`, `PY32_TOKEN_RE`, `PROXIMITY_WINDOW`, `_DEFAULT_TARGETS`, `scan_text`, `resolve_targets`, `main` | Python names | Phase 130 |
| `FIRESTARTER_CLAIMSCAN_TARGETS` | env seam (no default, reused name) | this plan's pytest, 123-11 |
| 5 fixtures under `fixtures/` | fixtures | this plan's pytest |
| The named v1.23 closing-artifact target list | **contract Phase 130 must satisfy or amend in the same commit** | Phase 130 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Copy and adapt check_permitted_claims.py — v1.23 phrase table, caveat, arming, proximity scoping</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` in full (223 lines) — the file being copied. Note especially the no-default env seam at line 68, the `is not None` precedence at line 147, the two guards at lines 164-181, the bucketed printer at 152-157, the anti-skip PASS line at 212-217, and the explicit-non-claim docstring paragraph at lines 25-30.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"BASE-07: Copying v1.22's `check_permitted_claims.py`" (lines 804-925) — the verbatim env-seam contract, the two guards and their ordering wart with the recommended hoist, the change table, the research-supplied eight-row phrase table cross-checked against REQUIREMENTS §Validation Ceiling, the D-16 proximity design with its line-scoped recommendation, and the inherited negated-phrasing comment to carry forward.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 8-22) — the source of the forbidden-claim set; cross-check the eight patterns against it and record that both sources agree.
    - `.planning/ROADMAP.md` §"Phase 130" (Goal, Requirements, Success Criteria) and `.planning/REQUIREMENTS.md` lines 102-105 (CLOSE-01…CLOSE-04) — the basis for the named default target list.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md` §"Meta-repo `check_permitted_claims.py` (v1.23)" (lines 700-757).
  </read_first>
  <action>
    Copy v1.22's checker into this phase directory and change only what the requirement forces.

    **Keep verbatim, in structure and in comment:** the module-top `_HERE` constant; the no-default env
    seam `FIRESTARTER_CLAIMSCAN_TARGETS = os.environ.get("FIRESTARTER_CLAIMSCAN_TARGETS")` with its full
    explanatory comment about absent-versus-present-but-empty; `resolve_targets`'s three-level precedence
    with the `is not None` check (a truthiness check would make an explicitly empty value fall back to the
    real defaults, which is precisely the vacuous pass criterion 5 forbids); the bucketed FAIL printer with
    its 20-row cap; the anti-skip PASS line naming every scanned file; the `Exit codes:` docstring block;
    and the explicit-non-claim paragraph, rewritten for v1.23 — a green run is the mechanizable half only
    and cannot detect an implied overclaim, a misleading omission or wrong tone.

    **Reuse the env-var name** `FIRESTARTER_CLAIMSCAN_TARGETS`. The two checkers live in different phase
    directories and never coexist in one process. Record RESEARCH assumption A3 in a comment: if a future
    run ever scanned both phases at once, one seam would aim both, and the fix then is to suffix the name.

    **Hoist the never-vacuous guard above the missing-target guard.** v1.22 runs the missing-target check
    first, which is vacuously satisfied by an empty list, with the zero-target guard catching it after. The
    behaviour is correct but the ordering is fragile. Observable behaviour is unchanged and v1.22's own
    test asserting the specific never-vacuous message still holds. Note the hoist in a comment as a
    deliberate hardening, not a style change.

    **`_DEFAULT_TARGETS` — the v1.23 closing artifacts, named now (D-15).** An explicit, non-pattern list,
    never a glob and never a tree walk, of `_HERE`-relative paths pointing at the Phase 130 directory:
    `130-LEDGER.md` (CLOSE-02's honesty ledger), `130-DECISION.md` (CLOSE-04's release-decision artifact,
    the ROADMAP writes it as `13X-DECISION.md`), and the two release-notes artifacts
    `130-RELEASE-NOTES-fw.md` and `130-RELEASE-NOTES-app.md` following v1.22's five-artifact precedent —
    release notes are the outward-facing prose most likely to carry an overclaim to a stranger, which is
    the entire lesson of C-5. Carry v1.22's comment explaining why this list must never become a wildcard:
    the `fixtures/` directory deliberately contains violating text and must stay unreachable from the
    default set. Record in the docstring, where Phase 130 will read it, that Phase 130 must either produce
    exactly these four names or amend this list **in the same commit** that renames one — D-15's
    all-or-nothing arming makes a renamed artifact a hard failure, and that coupling is the point.

    **Arming (D-15).** Count how many of `_DEFAULT_TARGETS` exist. Zero exist ⇒ print an `UNARMED:` notice
    naming Phase 130 and return 0. At least one exists ⇒ armed, and then **every** named target must exist
    or the run fails naming the missing ones. Arming applies only to the default target set; when targets
    come from argv or the env seam the caller has named them explicitly and the ordinary fail-closed guard
    applies. State that distinction in a comment — it is the difference between "the close has not started"
    and "the close is half written".

    **`FORBIDDEN_PATTERNS` — eight labelled, case-insensitive entries**, per the research table cross-checked
    against the Validation Ceiling: `runs-on-py32`, `works-end-to-end`, `silicon-verified`,
    `bench-validated`, `hardware-validated`, `flashed-a-py32`, `closed-loop-vpp`, `pin-map-correct`. Carry
    v1.22's commenting discipline: record which entries are deliberately narrowed to an object and which
    are deliberately broad, and why. Record that both sources — research and REQUIREMENTS §Validation
    Ceiling — agree on all eight, with `flashed-a-py32` being research's narrowing of the end-to-end claim.

    **`REQUIRED_CAVEAT_PROSE` and `REQUIRED_CAVEAT_PATTERN`** — the canonical sentence fragment
    *"no PY32F071 hardware exists"* with a whitespace-tolerant pattern. Carry forward v1.22's recorded
    interaction comment, sharpened for D-16: an honest negated phrasing such as *"nothing about the
    PY32F071 is silicon-verified"* contains both a py32 token and a forbidden phrase, so it fires. The
    correct response is to reword the artifact using the canonical caveat, **not** to narrow the pattern
    set to dodge the alarm. The canonical caveat exists precisely so authors have an approved way to say
    this.

    **D-16 proximity scoping — line-scoped, 3-line window.** Add `PY32_TOKEN_RE` matching `PY32F071`,
    `py32` or `PY32` case-insensitively, and `PROXIMITY_WINDOW = 1` meaning one line either side. Change
    `scan_text` — and only `scan_text`, keeping the pure-function-separate-from-I/O split — so a forbidden
    match on line *i* is reported only if any of lines *i-1*, *i*, *i+1* carries a py32 token. Do **not**
    attempt sentence segmentation: markdown tables, bullet lists and code blocks have no reliable sentence
    terminators, and a naive split mangles version numbers like `v1.23`, filenames like
    `check_permitted_claims.py` and decimals. Record that reasoning in a comment, because "why not
    sentences" is the first question a future reader will have.

    Explain in the docstring why proximity scoping is necessary rather than a weakening: v1.23's artifacts
    are largely a non-regression story about AVR targets that genuinely **are** bench-validated from
    earlier milestones, so an unconditional literal match on *"bench-validated"* would fire on every one of
    those true sentences and the gate would be disabled within a week.

    Keep `scan_text` returning `(forbidden_hits, caveat_present)`; extend each hit tuple to carry the line
    number so the FAIL bucket can name it. Entry point unchanged.
  </action>
  <acceptance_criteria>
    - The file exists in the Phase 123 directory and defines `FORBIDDEN_PATTERNS` with exactly **8** entries whose labels are `runs-on-py32`, `works-end-to-end`, `silicon-verified`, `bench-validated`, `hardware-validated`, `flashed-a-py32`, `closed-loop-vpp`, `pin-map-correct`
    - `REQUIRED_CAVEAT_PROSE` is `no PY32F071 hardware exists` and `REQUIRED_CAVEAT_PATTERN` matches it whitespace-tolerantly and case-insensitively
    - `PY32_TOKEN_RE` and `PROXIMITY_WINDOW` exist, and `PROXIMITY_WINDOW` is `1`
    - `_DEFAULT_TARGETS` has exactly **4** entries, all `_HERE`-relative paths naming `130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, and the list is built with no glob and no tree walk
    - `FIRESTARTER_CLAIMSCAN_TARGETS` is read at module scope with `os.environ.get` and **no** default, and `resolve_targets` checks it with `is not None`
    - In `main`, the zero-targets guard appears at a lower line number than the missing-target guard
    - Running the checker with no arguments and no env seam exits **0** and prints `UNARMED:` naming Phase 130 — none of the four v1.23 closing artifacts exists yet
    - The docstring contains an `Exit codes:` block, the explicit-non-claim paragraph, the Phase-130 amend-in-the-same-commit coupling statement, the A3 env-var-name note, and the "why not sentence segmentation" reasoning
    - The module imports only `os`, `re` and `sys`
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && python3 -c "
import re,sys
src=open('check_permitted_claims.py').read()
import ast;ast.parse(src)
ns={}
exec(compile(src.replace('if __name__','if False and __name__'),'c','exec'),ns)
labels=[l for l,_ in ns['FORBIDDEN_PATTERNS']]
assert len(labels)==8, labels
for l in ('runs-on-py32','works-end-to-end','silicon-verified','bench-validated','hardware-validated','flashed-a-py32','closed-loop-vpp','pin-map-correct'): assert l in labels, l
assert ns['REQUIRED_CAVEAT_PROSE']=='no PY32F071 hardware exists', ns['REQUIRED_CAVEAT_PROSE']
assert ns['PROXIMITY_WINDOW']==1
assert len(ns['_DEFAULT_TARGETS'])==4, ns['_DEFAULT_TARGETS']
for n in ('130-LEDGER.md','130-DECISION.md','130-RELEASE-NOTES-fw.md','130-RELEASE-NOTES-app.md'):
    assert any(t.endswith(n) for t in ns['_DEFAULT_TARGETS']), n
assert src.index('if not targets') < src.index('missing = ['), 'never-vacuous guard must be hoisted above the missing-target guard'
for s in ('Exit codes:','FIRESTARTER_CLAIMSCAN_TARGETS','is not None','PY32_TOKEN_RE'): assert s in src, s
print('CLAIMGATE_SRC_OK')" && python3 check_permitted_claims.py > /tmp/cg0.txt 2>&1 && grep -q '^UNARMED:' /tmp/cg0.txt && grep -q '130' /tmp/cg0.txt && echo CLAIMGATE_OK</automated>
  </verify>
  <done>The v1.23 claim gate carries the eight-row phrase table, the PY32F071 caveat, line-scoped proximity, all-or-nothing arming over four named Phase-130 artifacts, and the hoisted never-vacuous guard — and reports UNARMED today because none of those artifacts exists yet.</done>
</task>

<task type="auto">
  <name>Task 2: Author the five fixtures, including D-16's negative-direction control that has no v1.22 analogue</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_control.md, .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_control_second.md, .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/clean_avr_bench_control.md, .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/planted_py32_overclaim.md, .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/planted_missing_caveat.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/` — all four files; the shape being copied and the naming convention (`clean_*` controls, `planted_*` violations).
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Both-directions fixture (D-16, mandatory)" (lines 908-918) — the four-row fixture table and the statement that the second row is the whole point of D-16 and has no v1.22 analogue.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 8-22) — the permitted and forbidden claim wording the fixtures should echo so they read as realistic artifacts rather than word lists.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — written in Task 1; the caveat sentence and the proximity window the fixtures must be authored against.
  </read_first>
  <action>
    Five markdown fixtures in a new `fixtures/` subdirectory of this phase directory. They must be
    unreachable from `_DEFAULT_TARGETS`, which names only `130-*.md` files one directory up — that is the
    fixture-placement discipline v1.22's comment spells out, and it is why the default list must never
    become a glob.

    Write each as a realistic short artifact excerpt, not a bare list of trigger phrases: a fixture that
    reads like real prose is what proves the gate works on real prose.

    1. `clean_control.md` — a short, honest v1.23-style passage: the target builds clean, the native and
       host suites pass at their recorded case and suite counts, the DFU sequence is exercised against
       device descriptors and mocks. It must carry the canonical caveat *"no PY32F071 hardware exists"*
       and trip nothing. Expect exit 0.
    2. `clean_control_second.md` — a second clean artifact so the anti-skip PASS-line test can assert **two**
       basenames appear, matching v1.22's test 6.
    3. `clean_avr_bench_control.md` — **D-16's negative direction, the fixture with no v1.22 analogue.** It
       contains a sentence such as *"the Leonardo target remains bench-validated from v1.15"* — a true
       statement about AVR silicon that genuinely was bench-validated — and must NOT fire.
       ⚠ **The authoring trap:** this file must also carry the required caveat, and the caveat contains the
       token `PY32F071`. With a 3-line proximity window, a caveat line within one line of the
       bench-validated line would put a py32 token in the window and fire the gate. Separate them by at
       least **two** blank or intervening lines, and state that requirement in a comment inside the fixture
       so a future editor does not reflow the file and silently break the test. Add a second AVR sentence
       using a different forbidden phrase (`hardware-validated`, for instance) so the fixture proves the
       scoping for more than one pattern.
    4. `planted_py32_overclaim.md` — contains, on one line, a claim such as *"the PY32F071 target is
       bench-validated"*: a forbidden phrase co-occurring with a py32 token. It must carry the required
       caveat too, so the failure is attributable to the forbidden phrase alone and not to a missing
       caveat. Expect exit non-zero with the `bench-validated` label named.
    5. `planted_missing_caveat.md` — clean prose with no forbidden phrase and **no** canonical caveat.
       Expect exit non-zero with the missing-caveat message.

    Every fixture opens with a one-line marker stating it is deliberately-authored fixture input for
    `check_permitted_claims.py` and is not a project artifact — the same discipline the host repo's
    `planted_*` fixtures follow.
  </action>
  <acceptance_criteria>
    - All five fixtures exist under `fixtures/` and are tracked (`git ls-files` lists them)
    - Each of `clean_control.md`, `clean_control_second.md`, `clean_avr_bench_control.md` and `planted_py32_overclaim.md` contains the exact caveat phrase `no PY32F071 hardware exists`; `planted_missing_caveat.md` does not
    - `clean_avr_bench_control.md` contains `bench-validated` and, in the 3-line window around every forbidden-phrase occurrence, contains no `py32`/`PY32` token — verified by running the gate against it and getting exit 0
    - `clean_avr_bench_control.md` carries an in-file comment warning that reflowing the file can put the caveat inside the proximity window and break the test
    - `planted_py32_overclaim.md` has a forbidden phrase and a py32 token on the same line
    - No fixture path is reachable from `_DEFAULT_TARGETS` — every default target is a `130-*.md` in the parent directory
    - Running the gate through the env seam gives exit 0 for the three clean controls and non-zero for the two planted fixtures
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && for f in clean_control clean_control_second clean_avr_bench_control planted_py32_overclaim; do grep -q 'no PY32F071 hardware exists' fixtures/$f.md || exit 1; done && ! grep -q 'no PY32F071 hardware exists' fixtures/planted_missing_caveat.md && grep -q 'bench-validated' fixtures/clean_avr_bench_control.md && for f in clean_control clean_control_second clean_avr_bench_control; do FIRESTARTER_CLAIMSCAN_TARGETS="fixtures/$f.md" python3 check_permitted_claims.py > /dev/null 2>&1 || exit 1; done && for f in planted_py32_overclaim planted_missing_caveat; do FIRESTARTER_CLAIMSCAN_TARGETS="fixtures/$f.md" python3 check_permitted_claims.py > /dev/null 2>&1 && exit 1; done; git -C /workspaces ls-files .planning/phases/123-non-regression-baselines-gate-hardening/fixtures/ | wc -l | grep -q 5 && echo CLAIM_FIXTURES_OK</automated>
  </verify>
  <done>Five committed fixtures prove both directions of D-16: a py32 overclaim fires, a legitimate AVR bench-validated sentence does not, the missing caveat fires, and the two clean controls pass — with the proximity-window reflow trap documented inside the fixture that depends on it.</done>
</task>

<task type="auto">
  <name>Task 3: Write test_check_permitted_claims.py — nine subprocess proofs including both D-16 directions and D-15 arming</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py` in full (204 lines, 7 tests) — the canonical shape: the module docstring's coverage enumeration and never-import contract, `_run_scanner`'s list argv with the `targets is None` branch reaching the genuinely-absent path, the assert-both-exit-and-substring rule, the never-vacuous test asserting the specific message, the anti-skip PASS-line test and the precedence pin.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md` §"`tests/test_check_*.py`" (lines 379-511).
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-VALIDATION.md` lines 68-72 — the five BASE-07 rows this module must satisfy.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — written in Task 1.
  </read_first>
  <action>
    Copy v1.22's test module into this phase directory and extend it. Keep `_run_scanner` verbatim in
    structure: a list argv, `cwd` set to the phase directory, and the `targets is None` branch that pops
    the variable from the child's environment so the genuinely-absent path is reachable. Never import the
    scanner.

    The module docstring enumerates the coverage list and states the anti-hollow contract: a checker with
    no negative-fixture test is this project's v1.12 hollow-GATE-03 failure mode — a declared-empty
    detector that could never fail. Every planted test invokes the checker as a real subprocess against a
    committed fixture.

    Tests, one named function each:
    1. **Clean pass through the seam** — `clean_control.md` exits 0 with a `PASS:` line.
    2. **Planted py32 overclaim fires** — exit non-zero, `FAIL:` present, and the `bench-validated` label
       named. Assert the label, not just the exit code.
    3. **D-16 negative direction** — `clean_avr_bench_control.md` exits **0** despite containing
       `bench-validated`, because no py32 token is in the proximity window. This is the test that makes
       D-16 proven rather than asserted, and it has no v1.22 analogue.
    4. **Proximity is real, not accidental** — copy `clean_avr_bench_control.md` into `tmp_path`, insert a
       line containing a py32 token immediately adjacent to the bench-validated line, and assert the run now
       FAILS. Without this, test 3 could pass because the pattern never matches at all rather than because
       scoping suppressed it.
    5. **Missing caveat fires** — `planted_missing_caveat.md` exits non-zero with the missing-caveat message.
    6. **Never-vacuous on an explicitly empty target list** — the seam set to the empty string exits
       non-zero, prints no `PASS:`, and prints the specific never-vacuous message. Assert the message, not
       merely the exit code: with the default targets not existing yet, a silent fall-back would ALSO exit
       non-zero, but for the wrong reason — this assertion isolates the correct failure mode from that
       coincidental one, exactly as v1.22's test 5 docstring explains.
    7. **Fail-closed on a missing target** — a nonexistent path through the seam exits non-zero naming the
       path.
    8. **D-15 arming, both directions** — with zero of the four named closing artifacts existing (today's
       state), the default run exits 0 with `UNARMED:`. Then create exactly one of them in `tmp_path`-like
       isolation and assert the run becomes armed and FAILS naming the three still-missing targets. Do this
       without creating a real `130-*.md` in the Phase 130 directory: drive it through a copy of the
       checker's target resolution using the seam, or by pointing the checker at a temp directory — and
       state in the test docstring which mechanism was used and why the real phase directory was left
       untouched.
    9. **Precedence pin** — the env seam points at `planted_py32_overclaim.md` while positional argv names
       `clean_control.md`; the run must PASS, pinning the documented precedence against a future silent
       inversion.
    10. **Anti-skip PASS line** — scanning both clean controls prints a `PASS:` naming both basenames.

    Every failing case asserts BOTH the non-zero exit AND a distinctive substring.
  </action>
  <acceptance_criteria>
    - `cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 -m pytest test_check_permitted_claims.py -q` reports **10 passed, 0 failed, 0 skipped**
    - The module never imports `check_permitted_claims`; every invocation goes through `subprocess.run` with a list argv
    - Test 3 asserts exit 0 on a fixture that demonstrably contains `bench-validated`
    - Test 4 asserts the same content FAILS once a py32 token is adjacent — proving suppression rather than non-matching
    - Test 6 asserts the specific never-vacuous message string, not merely a non-zero exit
    - Test 8 asserts both the UNARMED exit 0 and the armed-but-incomplete failure, and does not create any `130-*.md` file inside the Phase 130 directory
    - `git -C /workspaces status --porcelain .planning/phases/130-*` shows nothing — no Phase 130 artifact was created as a side effect
    - The module contains no `monkeypatch.setenv`, no `pytest.skip` and no `pytest.mark.skipif`
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -2 && test "$(grep -c 'import check_permitted_claims' test_check_permitted_claims.py)" = "0" && test "$(grep -c 'monkeypatch.setenv' test_check_permitted_claims.py)" = "0" && test "$(grep -c 'pytest.skip' test_check_permitted_claims.py)" = "0" && grep -q 'subprocess.run' test_check_permitted_claims.py && test -z "$(ls -d /workspaces/.planning/phases/130-* 2>/dev/null)" && echo CLAIM_TESTS_OK</automated>
  </verify>
  <done>Ten subprocess tests prove both D-16 directions (suppression AND that suppression is real), the D-15 arming transition, the never-vacuous and fail-closed guards by their specific messages, the documented precedence, and the anti-skip PASS line — without creating any Phase 130 artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase prose → public artifact | Phase 130's outputs reach strangers; C-5 proved an overclaim of this shape already reached a locked decision |
| env seam → scan targets | A fully overridable target list means CI could be pointed at a clean fixture |
| forbidden-phrase pattern → legitimate AVR prose | An unconditional match would fire on true sentences and the gate would be disabled within a week |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-10-01 | Repudiation | an overclaim about nonexistent silicon reaching a public artifact | critical | mitigate | Eight-pattern table distilled from the Validation Ceiling, cross-checked against two sources, with a committed planted fixture proving the gate fires |
| T-123-10-02 | Repudiation | a gate disabled because it fires on true AVR sentences | high | mitigate | D-16 line-scoped 3-line proximity window, with BOTH directions proven — the negative-direction fixture and the adjacency test that proves suppression rather than non-matching |
| T-123-10-03 | Repudiation | an emptied target list silently falling back to defaults and passing | high | mitigate | No-default seam, `is not None` precedence, never-vacuous guard hoisted first, and a test asserting the specific message rather than the exit code |
| T-123-10-04 | Repudiation | a half-written close passing because only some artifacts exist | high | mitigate | D-15 all-or-nothing arming over four explicitly named targets, with the amend-in-the-same-commit coupling documented where Phase 130 reads it |
| T-123-10-05 | Tampering | the default target list becoming a glob and scanning the fixtures | high | mitigate | Explicit non-pattern list asserted to have exactly 4 entries; v1.22's rationale comment carried forward; fixtures placed one directory down, unreachable from the defaults |
| T-123-10-06 | Tampering | a test creating a real Phase 130 artifact as a side effect | medium | mitigate | Test 8 is asserted not to create any `130-*.md`; `git status` over the Phase 130 path is asserted empty |
| T-123-10-07 | Repudiation | a green claim-scan reported as satisfying the honesty criterion | medium | mitigate | The explicit-non-claim docstring paragraph is required and asserted; it states a green run is the mechanizable half only |
| T-123-10-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs; `os`, `re`, `sys` and `pytest` only |
</threat_model>

<verification>
1. `cd .planning/phases/123-…/ && python3 -m pytest test_check_permitted_claims.py -q` reports **10 passed, 0 skipped**.
2. `python3 check_permitted_claims.py` with no arguments exits 0 with `UNARMED:` naming Phase 130 — none of the four closing artifacts exists yet.
3. Three clean controls exit 0; both planted fixtures exit non-zero with their distinctive labels.
4. The D-16 negative direction passes AND the adjacency variant fails, so suppression is proven rather than assumed.
5. No `130-*.md` file and no Phase 130 directory was created; `git -C /workspaces status --porcelain` shows only this plan's intended files.
6. Meta repo HEAD still `gsd/v1.23-py32f071-integration`.

**Requirement ticking:** this plan ticks **nothing**. BASE-07 and BASE-08 are closed only in 123-11.
</verification>

<success_criteria>
- The v1.23 claim gate exists seven phases before the artifacts it scans, with the scan contract recorded and coupled to Phase 130's naming.
- Proximity scoping is proven in both directions, so the gate is both effective on py32 overclaims and quiet on the milestone's many true AVR statements.
- The empty-target-list property criterion 5 names is proven through the env seam, exactly as criterion 5 words it.
</success_criteria>

<output>
Create `.planning/phases/123-non-regression-baselines-gate-hardening/123-10-SUMMARY.md` when done.
Record: the eight pattern labels with their regexes verbatim; the four named `_DEFAULT_TARGETS` and the
Phase-130 amend coupling; the `UNARMED:` line the default run prints today; the observed exit code and
distinctive substring for each fixture; the mechanism test 8 used to drive the arming transition without
creating a Phase 130 artifact; and an explicit statement that a green claim-scan is the mechanizable half
only and never by itself satisfies the milestone's honesty criterion.
</output>
