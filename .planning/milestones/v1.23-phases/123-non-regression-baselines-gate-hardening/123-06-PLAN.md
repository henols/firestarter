---
phase: 123-non-regression-baselines-gate-hardening
plan: 06
type: execute
wave: 6
depends_on: ["123-05"]
repo: firestarter
files_modified:
  - firestarter/tests/test_checker_convention.py
autonomous: true
requirements: [BASE-08]
requirement_closure: "None. Do NOT tick BASE-08 here — it is closed only in 123-11."
must_haves:
  truths:
    - "Every `firestarter/scripts/check_*.py` has a paired `tests/test_check_*.py` and at least one `tests/fixtures/planted_*` fixture named after its stem — asserted mechanically, with a hardcoded floor so a zero-match glob fails instead of passing vacuously (D-08)"
    - "The meta-test is scoped to `firestarter/scripts/`, which by D-06's home choice is exactly the v1.23 checker set, so it needs no grandfather allow-list and is green on arrival"
    - "The 3 pre-existing non-conforming host checkers are named in the docstring as deliberately out of scope, so the finding is recorded rather than lost or silently blessed"
    - "The firmware side of the phase is verified as a whole: both native envs at 141 cases / 17 suites all PASSED, all three AVR clean builds matching the recorded baseline, and every new gate exercised against the real tree"
  artifacts:
    - path: "firestarter/tests/test_checker_convention.py"
      provides: "BASE-08's convention-derived meta-test with a hardcoded floor, scoped to the firmware scripts directory"
      contains: "FLOOR"
      min_lines: 90
  key_links:
    - from: "firestarter/tests/test_checker_convention.py"
      to: "firestarter/scripts/"
      via: "a glob over check_*.py whose result count is asserted >= a hardcoded floor before any per-checker assertion runs"
      pattern: "check_\\*\\.py"
---

<objective>
Write `firestarter/tests/test_checker_convention.py` — BASE-08's convention-derived meta-test — and then
verify the whole firmware half of Phase 123 against the real tree: both native environments at their
recorded counts, all three AVR clean builds against the recorded baseline, and every new gate exercised.

Purpose: BASE-08 says *every checker introduced in this milestone* ships with a committed planted-violation
fixture and a pytest proving the non-zero exit. Four such checkers now exist in `firestarter/scripts/`.
D-08 makes the filesystem convention the single source of truth with no registry file, and a hardcoded
floor so a zero-match glob fails instead of passing vacuously. D-06's home choice does double duty here:
`firestarter/scripts/` contained no Python checker before this phase, so that directory *is* exactly the
v1.23 set and the meta-test needs no allow-list.

Output: one meta-test module, and the firmware-side verification record for 123-11 to cite.

**Scope the glob to `firestarter/scripts/`.** A repo-wide version goes RED on arrival: measured, only 4 of
the 7 checkers in `firestarter_app/tools/` conform, and `check_mypy_watermark.py` has no test at all.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **firmware** (`/workspaces/firestarter`), branch `v1.23-py32f071-integration`. Task 2 runs commands
in the firmware repo only; it writes no file.

@.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md
@.planning/phases/123-non-regression-baselines-gate-hardening/123-VALIDATION.md
</context>

<artifacts_this_phase_produces>
See `123-01-PLAN.md` §"Artifacts this phase produces" for the full phase-wide list.

Introduced by THIS plan:

| Symbol / path | Kind | Consumed by |
|---|---|---|
| `firestarter/tests/test_checker_convention.py` | meta-test | 123-11; every later milestone adding a firmware checker |
| `CHECKER_GLOB`, `FLOOR`, `FIXTURE_FLOOR` | Python names | the floor contract a future phase must raise when it adds a checker |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_checker_convention.py — glob, pairing assertions, hardcoded floors</name>
  <files>firestarter/tests/test_checker_convention.py</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"BASE-08: The Convention Is Not Universal (Correction C-6)" (lines 929-983) — the measured 4-of-7 table naming each violator, the three options with Option A recommended, the scoping mechanism (i) that makes `firestarter/scripts/` exactly the v1.23 set, the floor-count guidance, and the note that `firestarter/tests/fixtures/` did not exist before this phase.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Common Pitfalls" → Pitfall 8 (lines 1449-1455) — the warning sign is a meta-test globbing `**/check_*.py` or `firestarter_app/tools/`.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md` §"No Analog Found" row for `test_checker_convention.py` (line 835) — there is no analog; the shape comes from D-08 directly.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md` §"Firmware-repo test modules" (lines 514-591).
    - `/workspaces/firestarter/scripts/` and `/workspaces/firestarter/tests/fixtures/` as they now stand — the actual filenames the floors must match.
  </read_first>
  <action>
    Create `firestarter/tests/test_checker_convention.py`, mirroring `test_update_version.py`'s provenance
    header (Phase 123 Plan 06; Requirements BASE-08; Decisions covered D-06, D-08) and obeying the
    no-conftest house rule. Stdlib and `pytest` only.

    **Scope.** `CHECKER_GLOB` globs `check_*.py` in `firestarter/scripts/` only, resolved from
    `Path(__file__).resolve().parent.parent / "scripts"`. Never glob recursively, never reach into
    `firestarter_app/tools/`. State in the docstring that this scope is not an arbitrary narrowing: D-06
    put every v1.23 firmware checker in this one directory, which contained no Python checker before this
    phase, so the glob is exactly the "introduced in this milestone" set BASE-08 names — with no registry
    file, which D-08 forbids, and no grandfather allow-list, which would be a silencer.

    **Record the out-of-scope finding.** In the docstring, name all three non-conforming host checkers
    measured in research — `check_dispatch.py` (its test is named `test_check_dispatch_invariants.py`),
    `check_sdp_capability_invariants.py` (its test is named `test_check_sdp_capability.py`), and
    `check_mypy_watermark.py` (which has **no test at all**) — and state that they are deliberately out of
    scope for this meta-test because they predate v1.23, with `check_mypy_watermark.py`'s missing test
    noted as a genuine gap rather than a blessed one. That keeps the finding from being lost without
    expanding the phase.

    **Floors.** `FLOOR` is a hardcoded integer equal to the number of `check_*.py` files actually shipped
    into `firestarter/scripts/` by this phase — count them at authoring time; it is 4 if
    `check_size_baseline.py`, `check_build_warnings.py`, `check_cmake_manifest.py` and
    `check_orphan_provisional.py` are all present. `FIXTURE_FLOOR` is a hardcoded integer equal to the
    number of `planted_*` entries actually present in `firestarter/tests/fixtures/`. Assert the glob
    results are `>= FLOOR` before any per-checker assertion runs — a zero-match glob must fail, not pass
    vacuously, and an accidental deletion must fail too. Carry a comment stating that a later phase adding
    a checker raises the floor deliberately in the same commit, and that lowering a floor is never the
    right response to a red gate.

    Tests, one named function each, enumerated in the module docstring:
    1. **The glob is non-vacuous** — at least `FLOOR` checkers are found, asserted with a message naming
       the observed count and the floor.
    2. **Every checker has a paired test module** — for each `scripts/check_<stem>.py`, assert
       `tests/test_check_<stem>.py` exists. Report every missing pair in one assertion message rather than
       failing on the first, so a reader sees the whole gap at once.
    3. **Every checker has at least one planted fixture named after its stem** — for each
       `check_<stem>.py`, assert at least one entry in `tests/fixtures/` matching `planted_<stem>*`. Count
       directories as well as files, because two of this phase's fixtures are trees.
    4. **The fixture directory is non-vacuous** — the total `planted_*` count is `>= FIXTURE_FLOOR`.
    5. **Each paired test module actually invokes its checker** — assert the test module's text names its
       checker's filename. A pairing that exists but tests something else would satisfy the filename
       convention while proving nothing, which is the hollow shape this whole requirement targets.
    6. **Each paired test module asserts a non-zero exit somewhere** — assert its text contains an
       assertion on a non-zero return code. This is deliberately a coarse textual check; state in the
       docstring that it is a convention guard, not a semantic proof, and that the semantic proof is each
       module's own planted-fixture test.
    7. **The scope really is firmware-only** — assert that the resolved glob directory ends in
       `firestarter/scripts` and that no path in the result set contains `firestarter_app`.

    Every assertion carries a got-vs-expected message. Add no config file to the firmware repo.
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python3 -m pytest tests/test_checker_convention.py -q` reports **7 passed, 0 failed, 0 skipped**
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` reports **48 passed, 0 skipped** (8 + 7 + 10 + 8 + 8 + 7)
    - The module defines `FLOOR` and `FIXTURE_FLOOR` as integer literals, and `FLOOR` equals the actual count of `firestarter/scripts/check_*.py` (4)
    - `grep -c 'firestarter_app' firestarter/tests/test_checker_convention.py` counts only the docstring mentions of the three out-of-scope checkers, and no glob or path expression reaches that directory
    - The module contains no recursive glob pattern and no `rglob` call
    - The docstring names all three of `check_dispatch.py`, `check_sdp_capability_invariants.py` and `check_mypy_watermark.py` as deliberately out of scope, and states that `check_mypy_watermark.py` has no test at all
    - Temporarily renaming one checker's paired test module makes the suite FAIL (verified once, then reverted) — the meta-test is proven able to fail
    - `ls firestarter/conftest.py firestarter/tests/conftest.py firestarter/pytest.ini` finds none of them
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_checker_convention.py -q 2>&1 | tail -2 && python3 -m pytest tests/ -q 2>&1 | grep -qE '48 passed' && test "$(ls scripts/check_*.py | wc -l)" = "4" && grep -qE 'FLOOR *= *[0-9]+' tests/test_checker_convention.py && grep -qE 'FIXTURE_FLOOR *= *[0-9]+' tests/test_checker_convention.py && test "$(grep -c 'rglob' tests/test_checker_convention.py)" = "0" && grep -q 'check_mypy_watermark.py' tests/test_checker_convention.py && mv tests/test_check_orphan_provisional.py /tmp/tcop.py && ! python3 -m pytest tests/test_checker_convention.py -q > /dev/null 2>&1; RC=$?; mv /tmp/tcop.py tests/test_check_orphan_provisional.py; test $RC -eq 0 && python3 -m pytest tests/ -q 2>&1 | grep -qE '48 passed' && echo CONVENTION_OK</automated>
  </verify>
  <done>The meta-test enforces the checker↔test↔fixture convention over exactly the v1.23 firmware checker set with hardcoded non-vacuous floors, records the three out-of-scope pre-existing violations without blessing them, is proven able to fail by a temporary rename, and the firmware suite is at 48 passed / 0 skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the firmware half of the phase against the real tree and record the counts</name>
  <files>(no file writes — verification run only, in /workspaces/firestarter)</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-VALIDATION.md` §"Sampling Rate" (lines 30-40) — the per-wave and pre-verify sampling contract this task discharges for the firmware side.
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — the recorded figures every command below is compared against.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Corrections" C-4 (line 202) — neither firmware workflow fires on the v1.23 branch, so this local run is the only evidence during the milestone and must be recorded rather than assumed.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md` — the measured figures from the branch point, for comparison, AND §"Fork Point SHAs": read the field named exactly `fork_point_firmware` (a line matching `^fork_point_firmware: [0-9a-f]{40}$`). That SHA is the left side of step 7's cumulative no-firmware-code-moves check. Do not substitute `beta`, `origin/beta`, `HEAD~1` or a `merge-base` recomputation: `beta` can move under you, and this phase's proof must be anchored to the exact commit the milestone branch was cut from.
  </read_first>
  <action>
    Run the firmware-side verification set from `/workspaces/firestarter`, recording every command and its
    verbatim result for 123-11's evidence artifact. Do not paraphrase a count; record the observed integer.

    1. `python3 -m pytest tests/ -q` — expect **48 passed, 0 skipped**. Assert the count, not "tests pass":
       a suite that stops being collected also reports green.
    2. `pio test -e native` and `pio test -e native_nodevtools` — expect **141 test cases: 141 succeeded**
       and 17 SUMMARY rows all PASSED, on each. Record both.
    3. `pio run -t clean -e uno && pio run -e uno`, and the same for `uno328pb` and `leonardo` — then feed
       each build's output to `python3 scripts/check_size_baseline.py --avr-log <env>=<log>` and to
       `python3 scripts/check_build_warnings.py --log <env>=<log>`. Expect exit 0 from both for all three
       envs. This is the first time the comparator runs against a **fresh** build rather than a committed
       capture, and it is the proof that the recorded baseline still describes the tree after this phase's
       commits. Any divergence is a finding to report, not a number to update.
    4. `python3 scripts/check_size_baseline.py --native-log native=<fresh native log>` and the same for
       `native_nodevtools` — exit 0, confirming cases, suites and all-PASSED.
    5. `python3 scripts/check_cmake_manifest.py` and `python3 scripts/check_orphan_provisional.py` on the
       real tree — expect exit 0 with `UNARMED:` from both, each naming `platform/py32f071` and Phase 124.
       Record the exact lines; 123-11 quotes them, and Phase 124 will assert they change to armed.
    6. `git status --porcelain` — expect empty. Confirm no build artifact or generated file was left behind
       and that `.pio/` remains gitignored.
    7. The **cumulative** no-firmware-code-moves check. Read `fork_point_firmware` out of
       `123-01-SUMMARY.md`, assert it is non-empty and an ancestor of HEAD, then diff the whole phase's
       committed history against it:

           FORK=$(grep -oE '^fork_point_firmware: [0-9a-f]{40}$' \
             /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md \
             | cut -d' ' -f2)
           test -n "$FORK"
           git merge-base --is-ancestor "$FORK" HEAD
           git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test

       Expect the last command to print nothing. **The ref argument is the whole point.** A `--stat` diff
       with no ref compares the working tree against HEAD, and by the time this task runs, waves 1-5 are
       already committed — so the ref-less form could only ever catch an uncommitted stray edit in this
       very task and is structurally blind to anything an earlier plan committed. The `<fork>..HEAD` range
       is what actually proves binding constraint 1: no firmware production source, build config, workflow
       or native test suite was touched by ANY plan in this phase. The `test -n "$FORK"` guard is
       load-bearing — with `FORK` empty the range collapses to `HEAD..HEAD` and passes on any tree
       whatsoever, which is the same vacuity in a new costume.

       Also run `git status --porcelain -- src include platformio.ini .github test` and expect empty. That
       is the index-inclusive immediate check for this task's own edits; a path-scoped `--stat` diff would
       miss an already-staged one.

    State explicitly in the SUMMARY, so 123-11 can carry it forward without re-deriving: neither
    `build.yml` (which triggers on `main` only) nor `beta-build.yml` (on `beta` only) fires on the v1.23
    milestone branch, so these checkers are **local-run-only** for the duration of the milestone and become
    CI-live when the branch merges toward `beta` in Phase 130. Do not describe them as continuously
    CI-covered from Phase 123 onward.

    Also record, for Phase 124's planner: the two native envs agreed again on this run (or did not, with
    the observed values), so MERGE-06 remains satisfiable exactly as worded.

    Change no file. If any command fails, STOP and report rather than adjusting a baseline or a floor.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest tests/ -q` reports exactly **48 passed, 0 skipped**, and the observed integer is recorded
    - Both `pio test` runs report **141 test cases: 141 succeeded**, each with 17 SUMMARY rows and zero FAILED/ERRORED, and both counts are recorded
    - All three fresh AVR builds pass `check_size_baseline.py` at exit 0, and the three observed flash/RAM pairs equal the values in `scripts/baseline/size_baseline.json`
    - All three fresh AVR builds pass `check_build_warnings.py` at exit 0 with a macro-redefinition count of 0
    - Both fresh native runs pass `check_size_baseline.py` at exit 0
    - `check_cmake_manifest.py` and `check_orphan_provisional.py` each exit 0 with an `UNARMED:` line, and both lines are recorded verbatim
    - `git status --porcelain` is empty; `git status --porcelain -- src include platformio.ini .github test` is empty; and the cumulative `git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test` produces **empty output**, with `$FORK` read from 123-01-SUMMARY's `fork_point_firmware` field, asserted non-empty, and asserted an ancestor of HEAD
    - The SUMMARY states that neither firmware workflow fires on this branch and that the checkers are local-run-only until Phase 130
    - No file in the firmware repo was created, modified or deleted by this task
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | grep -qE '48 passed' && pio test -e native 2>&1 | tee /tmp/n1.log | grep -qE '141 test cases: +141 succeeded' && pio test -e native_nodevtools 2>&1 | tee /tmp/n2.log | grep -qE '141 test cases: +141 succeeded' && for e in uno uno328pb leonardo; do pio run -t clean -e $e > /dev/null 2>&1; pio run -e $e > /tmp/b_$e.log 2>&1 || exit 1; python3 scripts/check_size_baseline.py --avr-log $e=/tmp/b_$e.log || exit 1; python3 scripts/check_build_warnings.py --log $e=/tmp/b_$e.log || exit 1; done && python3 scripts/check_size_baseline.py --native-log native=/tmp/n1.log && python3 scripts/check_size_baseline.py --native-log native_nodevtools=/tmp/n2.log && python3 scripts/check_cmake_manifest.py | grep -q '^UNARMED:' && python3 scripts/check_orphan_provisional.py | grep -q '^UNARMED:' && test -z "$(git status --porcelain)" && FORK=$(grep -oE '^fork_point_firmware: [0-9a-f]{40}$' /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md | cut -d' ' -f2) && test -n "$FORK" && git -C /workspaces/firestarter merge-base --is-ancestor "$FORK" HEAD && test -z "$(git -C /workspaces/firestarter diff --stat "$FORK"..HEAD -- src include platformio.ini .github test)" && test -z "$(git -C /workspaces/firestarter status --porcelain -- src include platformio.ini .github test)" && echo FIRMWARE_VERIFIED_OK</automated>
  </verify>
  <done>The firmware half of Phase 123 is verified against a fresh build rather than a committed capture: 48 script tests, 141/17 on both native envs, all three AVR targets matching the recorded baseline through both new gates, both armed-on-arrival gates correctly UNARMED, and structural proof that no production source, build config, workflow or native suite changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem convention → requirement satisfaction | BASE-08 is discharged by a naming convention; a meta-test that globs the wrong scope either passes vacuously or fails on unrelated debt |
| committed capture → live tree | Every gate up to now was proven against a committed log; this plan is the first time they meet a fresh build |
| local run → claimed coverage | Neither firmware workflow fires on this branch, so overstating CI coverage would be an unearned claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-06-01 | Repudiation | a zero-match glob passing vacuously | high | mitigate | `FLOOR` and `FIXTURE_FLOOR` hardcoded and asserted before any per-checker assertion; the meta-test is proven able to fail by a temporary rename |
| T-123-06-02 | Repudiation | a repo-wide meta-test red on arrival, then weakened until meaningless | high | mitigate | Scope pinned to `firestarter/scripts/`; the three pre-existing host violations are named in the docstring as out of scope rather than allow-listed |
| T-123-06-03 | Repudiation | a paired test module that exists but tests something else | medium | mitigate | Two coarse textual assertions — the test names its checker's filename, and asserts a non-zero exit somewhere — with the docstring stating plainly that these are convention guards, not semantic proofs |
| T-123-06-04 | Repudiation | claiming continuous CI coverage the branch does not have | medium | mitigate | The SUMMARY must state that neither workflow fires on the milestone branch and that the gates are local-run-only until Phase 130 |
| T-123-06-05 | Tampering | a fresh build silently diverging from the recorded baseline and the baseline being updated to match | high | mitigate | Any divergence is an explicit STOP-and-report; the plan forbids adjusting a baseline or a floor in response to a red gate |
| T-123-06-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs; stdlib, `pytest` and already-present `pio` only |
</threat_model>

<verification>
1. `cd firestarter && python3 -m pytest tests/ -q` reports **48 passed, 0 skipped**.
2. Both native envs at 141 cases / 17 suites / all PASSED, on a fresh run.
3. All three AVR clean builds pass both new gates against the recorded baseline.
4. Both coarse-key gates report `UNARMED:` on the real tree, with their lines recorded verbatim.
5. The meta-test is proven able to fail and restored afterwards; `git status --porcelain` empty.
6. The cumulative `git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test` is empty, with `$FORK` = 123-01-SUMMARY's `fork_point_firmware`, asserted non-empty and an ancestor of HEAD — binding constraint 1 holds structurally across every commit waves 1-6 made, not merely in the working tree. `git status --porcelain -- src include platformio.ini .github test` is also empty.
7. HEAD still `v1.23-py32f071-integration`.

**Requirement ticking:** this plan ticks **nothing**. BASE-08 is closed only in 123-11.
</verification>

<success_criteria>
- BASE-08 is enforced by a mechanism, not by review discipline, over exactly the set BASE-08 names.
- Every firmware gate this phase created has met a live build and passed.
- The two gates that must be UNARMED today are UNARMED, with their exact output recorded so Phase 124 can assert the transition.
- The phase's "no firmware code moves" boundary is proven structurally over the phase's committed history — a `<fork_point_firmware>..HEAD` range, not a working-tree comparison that every already-committed wave would slip past.
</success_criteria>

<output>
Create `.planning/phases/123-non-regression-baselines-gate-hardening/123-06-SUMMARY.md` when done.
Record: the `FLOOR` and `FIXTURE_FLOOR` values as shipped; the verbatim UNARMED lines from both coarse-key
gates; the fresh-build flash/RAM figures for all three AVR envs alongside the recorded baseline values;
both native `{cases, suites}` pairs from the fresh run; the firmware suite count (expected 48); the
`fork_point_firmware` SHA used and the verbatim cumulative `<fork>..HEAD` diff invocation with its empty
output, as the proof that no production source changed anywhere in the phase's history; and the explicit statement that
neither firmware workflow fires on the v1.23 branch, so these gates are local-run-only until Phase 130.
</output>
