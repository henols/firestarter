---
phase: 123-non-regression-baselines-gate-hardening
plan: 11
type: execute
wave: 7
depends_on: ["123-06", "123-09", "123-10"]
repo: meta (/workspaces, .planning/ only)
files_modified:
  - .planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03, BASE-04, BASE-05, BASE-06, BASE-07, BASE-08]
requirement_closure: "This plan is the ONLY place BASE-01…BASE-08 are ticked Complete, and only after every row of the evidence table is recorded green from a run performed in this plan."
must_haves:
  truths:
    - "One committed artifact records, per row, the command, the expected result and the verbatim observed result for every gate this phase created and every pre-existing gate it must not have broken (D-05)"
    - "The artifact states plainly that neither firmware workflow fires on the v1.23 branch and that host CI has no firmware sibling, so all of this is LOCAL evidence — not CI coverage"
    - "The 360 native macro-redefinition warnings appear with their cause and an explicit not-a-regression statement, so a Phase-124 reader cannot mistake the number for damage"
    - "The MERGE-06 finding is handed forward affirmatively: both native envs agree at 141/17, so MERGE-06 is satisfiable as worded and no amendment should be requested"
    - "All eight BASE requirements are ticked Complete in REQUIREMENTS.md and the ROADMAP Phase 123 entry reflects the shipped plan set — and nothing else in either file is touched"
  artifacts:
    - path: ".planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md"
      provides: "D-05's recorded verbatim evidence, in the 122-NONREGRESSION.md shape: command | expected | observed, per row, re-executed in this session"
      contains: "observed"
      min_lines: 120
  key_links:
    - from: ".planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md"
      to: "firestarter/scripts/baseline/size_baseline.json"
      via: "every AVR and native figure in the artifact is quoted from the committed baseline and re-verified against a live run in this plan"
      pattern: "size_baseline"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md"
      via: "each BASE tick is justified by a named row in the evidence artifact, never by a SUMMARY assertion alone"
      pattern: "BASE-0"
---

<objective>
Execute the full cross-repo gate sweep one final time against the finished trees, write
`123-NONREGRESSION.md` in the `122-NONREGRESSION.md` shape, and close BASE-01…BASE-08 in REQUIREMENTS.md.

Purpose: D-05 discharges MERGE-07's *"the gates ran, never skipped"* with a **local run plus recorded
verbatim evidence** rather than a cross-repo CI leg — because GitHub Actions cannot check out above the
workspace, and during v1.23 the matching firmware commit lives on an unpushed milestone branch, so a leg
would score against `beta`, the wrong tree. Producing that artifact is a real deliverable of this phase,
not a side effect. Phase 124 reads it instead of recomputing.

Output: one evidence artifact, eight requirement ticks, one ROADMAP entry update.

**This plan pushes nothing, publishes nothing and comments nowhere public.** Phase 130 owns every
outward-facing action. No `git push`, no `gh` invocation, no release.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`), branch `gsd/v1.23-py32f071-integration`. Commands run in all three repos;
files are written only in the meta repo's `.planning/`. No sub-repo file is modified.

@.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md
@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md
</context>

<artifacts_this_phase_produces>
See `123-01-PLAN.md` §"Artifacts this phase produces" for the full phase-wide list.

Introduced by THIS plan:

| Symbol / path | Kind | Consumed by |
|---|---|---|
| `.planning/phases/123-…/123-NONREGRESSION.md` | evidence artifact | Phase 124 MERGE-05/06/07, Phase 130 CLOSE-02 |
| BASE-01…BASE-08 marked Complete in `.planning/REQUIREMENTS.md` | requirement state | `/gsd-verify-work`, Phase 130 |
| ROADMAP Phase 123 `**Plans:**` entry | roadmap state | `/gsd-progress` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every gate row against the finished trees and capture verbatim output</name>
  <files>(no file writes — measurement run only, across all three repos)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` §2 and §3 — the command-expected-observed table shape and the eleven-row cross-repo gate table this sweep re-executes.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-06-SUMMARY.md` and `123-09-SUMMARY.md` — the firmware-side and host-side counts already recorded; this task re-runs rather than copies them, and reports any difference as a finding.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-10-SUMMARY.md` — the claim gate's `UNARMED:` line and fixture results.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` Corrections C-4 and C-8 (lines 202, 206) — neither firmware workflow fires on this branch, and the eleven-row table represents nine gates while D-11's inventory is a superset.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md` §"Fork Point SHAs" — read the field named exactly `fork_point_firmware` (a line matching `^fork_point_firmware: [0-9a-f]{40}$`). That SHA is the left side of row 6's cumulative check, which is THE phase-wide no-firmware-code-moves proof. Do not substitute `beta`, `origin/beta`, `HEAD~1` or a `merge-base` recomputation: `beta` can move under you, and this claim must be anchored to the exact commit the milestone branch was cut from. If the field is missing or malformed, STOP — the phase's single most load-bearing claim cannot be discharged without it, and no substitute is acceptable.
  </read_first>
  <action>
    Re-execute everything below in this session, against the trees as they now stand. Do **not** copy a
    result from a prior plan's SUMMARY; where a prior SUMMARY made a claim, re-check it and say so,
    including any case where the live tree disagrees.

    **Firmware repo (`/workspaces/firestarter`):**
    1. `python3 -m pytest tests/ -q` — expect 48 passed, 0 skipped.
    2. `pio test -e native` and `pio test -e native_nodevtools` — expect 141 cases / 141 succeeded / 17
       SUMMARY rows all PASSED, on each. Record both, and record explicitly whether the two agree.
    3. Clean-build all three AVR envs and run both new gates against each fresh log:
       `check_size_baseline.py --avr-log <env>=<log>` and `check_build_warnings.py --log <env>=<log>`.
       Record the observed flash/RAM pairs alongside the recorded baseline values.
    4. `check_size_baseline.py --native-log …` for both native envs.
    5. `check_cmake_manifest.py` and `check_orphan_provisional.py` — record their `UNARMED:` lines verbatim.
    6. **The phase-wide no-firmware-code-moves proof.** This is the single most load-bearing claim the
       phase makes, so it is measured against the phase's committed history, not against the working tree:

           FORK=$(grep -oE '^fork_point_firmware: [0-9a-f]{40}$' \
             /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md \
             | cut -d' ' -f2)
           test -n "$FORK"
           git merge-base --is-ancestor "$FORK" HEAD
           git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test

       The last command must print nothing. Record `$FORK`, the full invocation and the empty output
       verbatim; Task 2's artifact quotes them.

       **Why the ref argument is mandatory here.** At phase end HEAD already contains every commit from
       waves 1-6. A `--stat` diff with no ref compares the working tree against HEAD, so it can only see an
       uncommitted stray edit made in this very task — it is structurally incapable of detecting that an
       earlier plan modified and committed `src/` or `include/`. Using that form to justify "no firmware
       code moved" would assert the claim without verifying it, which is precisely the failure class this
       phase exists to eliminate and a direct breach of the ROADMAP's own structural-verification
       discipline. The `test -n "$FORK"` guard is equally mandatory: with `FORK` empty the range collapses
       to `HEAD..HEAD` and passes on any tree at all.

       Then, separately, run `git status --porcelain` (expect empty for the repo overall) and
       `git status --porcelain -- src include platformio.ini .github test` (expect empty) as the
       index-inclusive immediate check.

    **Host repo (`/workspaces/firestarter_app`):**
    7. The seven proxy modules with `-rs -q` — expect 49 passed, **0 skipped**.
    8. The eleven-row cross-repo gate set exactly as `122-NONREGRESSION.md` §3 lists it. Record each
       command and result. Row 5 (`gen_sdp_bus_config.py`) is an **idempotence** check: assert the firmware
       tree afterwards matches its recorded pre-run `git status --porcelain` baseline, not that it is empty
       — the untracked-directory dirt in that repo is pre-existing, and reading it as generator damage
       would be a false failure.
    9. `python3 tools/check_no_exists_proxy.py`, plus
       `pytest tests/test_fw_presence.py tests/test_scan_paths_resolve.py tests/test_skip_census.py -q`.
    10. `python3 -m pytest tests/ -q` — expect 1158 passed, 0 skipped.
    11. `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`,
        `python tools/check_mypy_watermark.py` — record the mypy count against the watermark.

    **Meta repo:**
    12. `python3 -m pytest test_check_permitted_claims.py -q` in this phase directory — expect 10 passed.
    13. `python3 check_permitted_claims.py` with no arguments — expect exit 0 and `UNARMED:`.
    14. Run the claim gate over `123-NONREGRESSION.md` itself through the env seam once Task 2 has written
        it. That file is **not** one of the four default targets — it is an internal gate-result artifact,
        not outward-facing prose — so this is a courtesy check, not a contract. If it flags a phrase,
        reword the artifact; never weaken the pattern set.

    **Meta repo, continued — guard the command shape itself against regression:**
    15. The vacuous `--stat`-with-no-ref shape is easy to reintroduce with a well-meaning edit, so assert
        it is absent from the phase's own plan set. Run, and expect the count to be exactly `0`:

            grep -rlE -e 'diff --stat +--[^a-zA-Z-]' -e 'diff --stat +--$' \
              /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/ \
              --include='*-PLAN.md' | wc -l

        Both patterns require the `--stat` flag to be followed only by whitespace and then a bare `--`
        pathspec separator — the exact ref-less shape. The `[^a-zA-Z-]` class is what keeps a legitimate
        long flag such as `--cached` from tripping the guard. Neither pattern matches the corrected
        `"$FORK"..HEAD --` form, because a quote character sits where the pattern demands whitespace; and
        neither matches this criterion's own text, because a `+` quantifier sits there instead — both
        properties were verified against a fixture table of good and bad shapes at planning time and must
        be re-verified if either pattern is ever edited. If the count is non-zero,
        the named file has regressed to a working-tree-only comparison: fix the file, do not weaken the
        pattern. Record the observed `0` in the SUMMARY as an integer, not as "passes".

    **Branch state, all three repos:** record `git rev-parse --abbrev-ref HEAD` and the HEAD SHA for each.
    Confirm the two sub-repos are still on `v1.23-py32f071-integration` and the meta repo on
    `gsd/v1.23-py32f071-integration` — a `gsd-tools query commit` has been observed silently switching the
    meta checkout mid-phase.

    If any row fails, STOP and report. Do not adjust a baseline, a watermark, a floor or an allow-list to
    make a row green; that inversion is the failure mode this entire phase exists to prevent.
  </action>
  <acceptance_criteria>
    - Every one of the rows above is executed in this session and its verbatim output captured
    - Firmware: 48 script tests passed / 0 skipped; both native envs at 141 cases / 17 suites / all PASSED; all three AVR fresh builds pass both gates; both coarse-key gates report `UNARMED:`
    - Firmware, **the phase-wide claim**: the cumulative `git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test` produces **empty output**, where `$FORK` is read from `123-01-SUMMARY.md`'s `fork_point_firmware` field, asserted non-empty (`test -n "$FORK"`) and asserted an ancestor of HEAD. `git status --porcelain -- src include platformio.ini .github test` is separately empty. A ref-less `--stat` diff is NOT acceptable evidence for this criterion: with waves 1-6 already committed it compares only the working tree against HEAD and cannot see what those waves changed
    - The phase's plan set contains **zero** occurrences of the ref-less `--stat` shape: `grep -rlE -e 'diff --stat +--[^a-zA-Z-]' -e 'diff --stat +--$' .planning/phases/123-non-regression-baselines-gate-hardening/ --include='*-PLAN.md' | wc -l` outputs exactly `0`, and the observed integer is recorded (a known-zero count, not a "passes")
    - Host: 49 passed / **0 skipped** across the seven proxy modules; all eleven cross-repo gate rows pass; 1158 passed / 0 skipped overall; ruff, ruff-format and mypy-watermark all green
    - Meta: 10 claim-gate tests pass; the default claim-gate run exits 0 with `UNARMED:`
    - Branch names and HEAD SHAs recorded for all three repos, with the two sub-repos on `v1.23-py32f071-integration`
    - Row 5's idempotence is asserted against the recorded working-tree baseline, not against blanket emptiness
    - No `git push` and no `gh` invocation appears anywhere in the executed command list
    - No file was created, modified or deleted by this task in any repo
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | grep -qE '48 passed' && pio test -e native 2>&1 | grep -qE '141 test cases: +141 succeeded' && pio test -e native_nodevtools 2>&1 | grep -qE '141 test cases: +141 succeeded' && python3 scripts/check_cmake_manifest.py | grep -q '^UNARMED:' && python3 scripts/check_orphan_provisional.py | grep -q '^UNARMED:' && FORK=$(grep -oE '^fork_point_firmware: [0-9a-f]{40}$' /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md | cut -d' ' -f2) && test -n "$FORK" && git -C /workspaces/firestarter merge-base --is-ancestor "$FORK" HEAD && test -z "$(git -C /workspaces/firestarter diff --stat "$FORK"..HEAD -- src include platformio.ini .github test)" && test -z "$(git -C /workspaces/firestarter status --porcelain -- src include platformio.ini .github test)" && test "$(grep -rlE -e 'diff --stat +--[^a-zA-Z-]' -e 'diff --stat +--$' /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/ --include='*-PLAN.md' | wc -l)" = "0" && test "$(git rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration" && cd /workspaces/firestarter_app && python3 -m pytest tests/ -q 2>&1 | grep -qE '1158 passed' && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_dispatch.py && python3 tools/check_devtest_orchestrator.py && python3 tools/check_no_exists_proxy.py && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/check_mypy_watermark.py && test "$(git rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration" && cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -2 && python3 check_permitted_claims.py | grep -q '^UNARMED:' && test "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "gsd/v1.23-py32f071-integration" && echo FULL_SWEEP_OK</automated>
  </verify>
  <done>Every gate this phase created and every pre-existing cross-repo gate has been re-executed against the finished trees in this session, with verbatim output captured, counts asserted as integers, branch state confirmed in all three repos, and no file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write 123-NONREGRESSION.md — the D-05 recorded evidence artifact</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` in full — the exact-match analog: the header block pinning base and HEAD commits for all three repos, the re-execution pledge, §1's "the claim, as precise statements" style, the command/expected/observed tables, the known-and-explained-conditions section, and the validation-ceiling section.
    - The verbatim outputs captured in Task 1.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 8-22) — quoted verbatim in the artifact's ceiling section.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Corrections to CONTEXT.md and ROADMAP" (lines 192-217) — the nine corrections; the artifact records which ones this phase acted on and how.
  </read_first>
  <action>
    Write `123-NONREGRESSION.md` in the meta phase directory, mirroring `122-NONREGRESSION.md`'s shape.

    **Header block:** the firmware and host branch names and HEAD SHAs at this sweep; the `beta` base SHA
    each branch forked from; the meta branch and HEAD; and the re-execution pledge in substance — every row
    below was executed in this session against the trees as they now stand, nothing is copied from a prior
    plan's SUMMARY, and where a prior artifact made a claim this document re-checks it and says so.

    Sections, in this order:

    1. **The claim, as precise statements.** Each individually checkable against a named row: the recorded
       baseline exists and is machine-readable; the FW-absent proxy is split and a present-repo-missing-target
       is a hard failure; the skip census fails on a firmware-absent reason while the marker exists; four new
       checkers exist, three of them armed-on-arrival and correctly UNARMED today; every checker introduced
       this milestone ships a committed planted fixture and a pytest proving the non-zero exit; and no
       firmware production source, build config, workflow or native suite was touched.

    2. **The BASE-01 baseline, as recorded and as re-verified.** The six AVR figures and both native pairs,
       each shown as recorded-in-JSON versus observed-on-a-fresh-build. State the ROADMAP cross-check result
       (five reproduced byte-exact; uno328pb RAM supplied for the first time at 1579 B).

    3. **The gate table** — one row per gate: command, expected, observed. Cover every new checker, every
       new test module, the eleven pre-existing cross-repo rows, both full suites, both native envs and the
       host gate trio. Mark clearly which rows belong to MERGE-07's **nine-gate** set and which belong to
       D-11's larger inventory — C-8 reconciled these as different sets and Phase 124 must not conflate
       them.

    4. **What is UNARMED today and what arms it.** The verbatim `UNARMED:` lines from
       `check_cmake_manifest.py`, `check_orphan_provisional.py` and `check_permitted_claims.py`, each with
       the condition that arms it: `platform/py32f071/` appearing (Phase 124) for the first two, any one of
       the four named `130-*.md` artifacts appearing (Phase 130) for the third. State that no manual
       arm-flip exists and that a rename inside the port cannot disarm the first two.

    5. **Known and explained conditions**, each with its cause and why it is not this phase's work:
       - **Neither firmware workflow fires on the v1.23 branch.** `build.yml` triggers on `main` only,
         `beta-build.yml` on `beta` only. These gates are **local-run-only** for the duration of the
         milestone and become CI-live when the branch merges toward `beta` in Phase 130. Everything in this
         document is local evidence per D-05, not CI coverage.
       - **Host `ci.yml` performs a single checkout with no firmware sibling.** That is why D-05 chose local
         evidence over a cross-repo CI leg, and why the standalone-CI skip introduced by `81fa53c` remains
         honest.
       - **The 360 macro-redefinition warnings on each native env** — 8 macros × 45 translation units from
         each suite's own `avr/pgmspace.h` shim being redefined by ArduinoFake's. Characterised pre-existing
         debt measured on `beta` before any v1.23 work. **Not a regression and not damage.** The
         zero-tolerance rule is scoped to the three AVR envs, where the count is genuinely zero and is
         therefore spelled as exact equality; the native envs carry a watermark so any *new* warning fails.
         RESEARCH's Option B (deduplicating the shims across 17 suite directories) was considered and
         rejected as a breach of this phase's no-firmware-code-moves boundary.
       - **`check_uno_ram.sh` was deleted, and it was already red.** Floor 545 B versus measured 475 B free
         on `uno`, referenced by neither workflow. Its parse and its three-way exit taxonomy survive in
         `check_size_baseline.py`, which is strictly stronger — flash as well as RAM, three AVR envs rather
         than one, both native envs, and a recorded measurement rather than a hand-maintained floor.
       - **The D-14 pio-framing gap is closed**, by `captured_native_warnings_excerpt.log`. State any
         residual limitation honestly rather than declaring the gap fully retired if it is not.
       - **The three pre-existing non-conforming host checkers** (`check_dispatch.py`,
         `check_sdp_capability_invariants.py`, `check_mypy_watermark.py` — the last with no test at all) are
         deliberately out of the BASE-08 meta-test's scope, which is `firestarter/scripts/` only.
         `check_mypy_watermark.py`'s missing test is a genuine gap, recorded rather than blessed.
       - The named pre-existing working-tree dirt in each repo.

    6. **Hand-off to Phase 124.** Affirmatively: both native envs agree at 141 cases / 17 suites, so
       **MERGE-06 is satisfiable exactly as worded and no amendment should be requested** — D-04's feared
       live risk did not materialise. The `PY32_EXCLUDED` comment format is committed and Phase 124
       populates it; quote the format verbatim. `check_cmake_manifest.py` will report exactly the
       `flash_type_3.cpp`/`flash_type_4.cpp` pair as violations the moment the port lands, which is
       MERGE-02's precise first firing. `check_orphan_provisional.py` will fire on
       `RURP_PY32F071_PINMAP_PROVISIONAL`, which is the mechanism forcing MERGE-04 to wire the guard.

    7. **The validation ceiling.** Quote REQUIREMENTS.md's permitted and forbidden claim lists verbatim,
       then confirm line by line that nothing in this document asserts a forbidden claim. Cite measured
       figures only.

    8. **Deliberately not taken.** No firmware source moved, merged, cherry-picked or renamed. No
       `platformio.ini` edit — so the stale `[env:native_nodevtools]` "16-entry list" comments (three
       occurrences; the list is 17) stay unfixed and remain a deferred idea. No workflow edit. No
       cross-repo CI leg. No push, no `gh`, no release, no tag, no gitlink bump.

    Then run the claim gate over this file through the env seam as the courtesy check described in Task 1
    row 14. If it flags a phrase, reword this artifact — never weaken the pattern set. State plainly in the
    artifact that a green claim-scan is the mechanizable half only.
  </action>
  <acceptance_criteria>
    - `123-NONREGRESSION.md` exists, is at least 120 lines, and contains all eight numbered sections
    - Its header pins both sub-repo branch names and HEAD SHAs, both `beta` base SHAs, and the meta branch and HEAD
    - Every gate row records a command, an expected value and a verbatim observed value; no row says only "passes"
    - The artifact contains the literal figures `141`, `17`, `1158`, `48`, `49`, `23932`, `1573`, `23976`, `1579`, `26072`, `2014` and `360`
    - The artifact contains the strings `not a regression`, `pre-existing`, `local evidence` and `MERGE-06`
    - The artifact states that neither firmware workflow fires on the v1.23 branch and that host `ci.yml` has no firmware sibling
    - The artifact records the `check_uno_ram.sh` deletion with both the 545 and 475 figures
    - The artifact quotes the `PY32_EXCLUDED` comment format verbatim and names `flash_type_3.cpp`/`flash_type_4.cpp` as the gate's first firing
    - The artifact quotes REQUIREMENTS.md's permitted and forbidden claim lists verbatim
    - `FIRESTARTER_CLAIMSCAN_TARGETS=<abs path to 123-NONREGRESSION.md> python3 check_permitted_claims.py` exits **0**
    - The artifact states that a green claim-scan is the mechanizable half only
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && test "$(wc -l < 123-NONREGRESSION.md)" -ge 120 && for s in 141 17 1158 48 49 23932 1573 23976 1579 26072 2014 360 'not a regression' 'pre-existing' 'local evidence' 'MERGE-06' 'PY32_EXCLUDED' 'flash_type_3' '545' '475'; do grep -q "$s" 123-NONREGRESSION.md || { echo "MISSING: $s"; exit 1; }; done && FIRESTARTER_CLAIMSCAN_TARGETS="$PWD/123-NONREGRESSION.md" python3 check_permitted_claims.py && echo NONREGRESSION_ARTIFACT_OK</automated>
  </verify>
  <done>`123-NONREGRESSION.md` records the whole phase as command/expected/observed rows re-executed in this session, correctly qualifies the evidence as local rather than CI, records every known-and-explained condition with its cause, hands Phase 124 the MERGE-06 and MERGE-02/04 findings affirmatively, and passes its own claim gate.</done>
</task>

<task type="auto">
  <name>Task 3: Tick BASE-01…BASE-08 in REQUIREMENTS.md and update the ROADMAP Phase 123 entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 26-40 (the BASE checklist) and lines 155-173 (the Traceability table with its `BASE-01 … BASE-08 | Phase 123 | Pending` row and the coverage counts).
    - `.planning/ROADMAP.md` lines 1994 and 2005-2019 — the Phase 123 summary line and the Phase Details block with its `**Plans**: TBD` placeholder.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md` — written in Task 2; each tick must be justifiable by a named row in it.
    - The eleven plan SUMMARY files in this phase directory — the record of what each plan actually delivered.
  </read_first>
  <action>
    **Tick each BASE requirement individually, and only against evidence.** For each of BASE-01…BASE-08,
    change its checklist box from unchecked to checked in `.planning/REQUIREMENTS.md`, and name — in this
    plan's SUMMARY, not in REQUIREMENTS.md — the specific row of `123-NONREGRESSION.md` that justifies it.
    A tick justified only by a plan SUMMARY's assertion is not acceptable: executors on this project have
    marked multi-plan requirements Complete prematurely four times in one phase, and the evidence artifact
    exists precisely so a tick has a citable row.

    Update the Traceability table row `BASE-01 … BASE-08 | Phase 123 | Pending` to `Complete`. Do not
    change any other row, and do not change the coverage counts — 47 total, 47 mapped, 0 unmapped are
    unaffected by this phase.

    **ROADMAP.md — scoped edits only.** Use `Edit` with narrow, unique anchors. Never rewrite the file:
    a whole-file write destroys every phase entry outside the diff window, and this file carries the entire
    project's roadmap.
    - The Phase 123 Details block already carries the eleven-plan checklist, written at planning time.
      Tick each `- [ ] 123-NN-PLAN.md` box to `- [x]` **only** for plans whose SUMMARY exists and whose
      verification passed. Do not reword the objective text beside a box, and do not add or remove a row —
      an eleven-row list that becomes a ten-row list is exactly the silent scope loss this phase's whole
      discipline is about.
    - Leave the Phase 123 Goal, Depends on, Requirements, Success Criteria and the `**Plans**:` header line
      byte-unchanged — they were correct and this plan is not the place to reword them.
    - Change nothing in the Phase 124-130 entries, nothing in the milestone preamble, and nothing in any
      earlier milestone's section.

    **Verify the scoping mechanically.** After editing, confirm with `git diff HEAD -- .planning/ROADMAP.md`
    that the changes in `ROADMAP.md` are confined to the Phase 123 Details block and that no line belonging
    to another phase changed. Confirm `git diff HEAD -- .planning/REQUIREMENTS.md` touches only the eight
    BASE checkboxes and the one Traceability row. Pass the explicit `HEAD` ref in both cases: a bare
    `git diff <path>` compares the working tree against the index only, so an edit you have already staged
    becomes invisible to the very check meant to bound it. For the untouched-file assertion use
    `git status --porcelain -- .planning/STATE.md` and require empty output, for the same reason.

    Do **not** update `.planning/STATE.md` in this plan — the orchestrator's own state handling owns that
    file, and a hand-edit here has previously produced counters that disagree with the frontmatter. Record
    in the SUMMARY what STATE.md will need (phase 123 complete, 11 plans) so the operator can verify it
    rather than assume it.

    No push, no `gh`, no tag, no gitlink bump.
  </action>
  <acceptance_criteria>
    - All eight BASE checkboxes in `.planning/REQUIREMENTS.md` are checked, and no other checkbox in the file changed state
    - The Traceability row for BASE-01…BASE-08 reads `Complete`; the coverage counts still read 47 total / 47 mapped / 0 unmapped
    - `git -C /workspaces diff --stat HEAD -- .planning/REQUIREMENTS.md` shows changes only on the nine expected lines (the explicit `HEAD` ref makes the comparison index-inclusive, so an already-staged edit is visible)
    - `.planning/ROADMAP.md`'s Phase 123 Details block still lists exactly **11** plan rows, and all eleven boxes read `- [x]`; no objective text beside a box changed
    - `git -C /workspaces diff HEAD -- .planning/ROADMAP.md` shows no changed line belonging to Phase 124, 125, 126, 127, 128, 129 or 130, nor to any earlier milestone section
    - The Phase 123 Goal, Depends on, Requirements and Success Criteria lines are byte-unchanged
    - `.planning/STATE.md` is not modified by this plan
    - No `git push`, no `gh` invocation, no tag and no gitlink change
    - This plan's SUMMARY names, per requirement, the `123-NONREGRESSION.md` row that justifies its tick
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -cE '^- \[x\] \*\*BASE-0[1-8]\*\*' .planning/REQUIREMENTS.md)" = "8" && test "$(grep -cE '^- \[ \] \*\*BASE-0' .planning/REQUIREMENTS.md)" = "0" && grep -qE 'BASE-01 … BASE-08 \| Phase 123 \| Complete' .planning/REQUIREMENTS.md && grep -q '47' .planning/REQUIREMENTS.md && test "$(git diff --numstat HEAD -- .planning/ROADMAP.md | awk '{print $1+$2}')" -le 30 && test -z "$(git status --porcelain -- .planning/STATE.md)" && test "$(git diff HEAD -- .planning/ROADMAP.md | grep -cE '^[+-].*Phase 12[4-9]:|^[+-].*Phase 130:')" = "0" && echo TICKS_OK</automated>
  </verify>
  <done>All eight BASE requirements are ticked with a citable evidence row each, the Traceability row reads Complete, the ROADMAP Phase 123 entry lists the eleven shipped plans, no other phase entry or milestone section changed, and STATE.md was left to the orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan SUMMARY claim → requirement tick | A tick justified by an assertion rather than a citable evidence row is how requirements get marked Complete prematurely |
| local gate run → claimed coverage | Neither sub-repo's CI exercises these gates on this branch; overstating that is an unearned claim |
| scoped edit → whole-file rewrite | ROADMAP.md carries the entire project roadmap; a whole-file write destroys everything outside the diff window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-11-01 | Repudiation | a requirement ticked on an assertion rather than evidence | high | mitigate | Every tick must cite a named row of `123-NONREGRESSION.md`; the artifact is written before the ticks and its rows are re-executed in this session rather than copied |
| T-123-11-02 | Repudiation | reporting local gate results as continuous CI coverage | high | mitigate | The artifact must state that `build.yml` fires on `main` only, `beta-build.yml` on `beta` only, and host `ci.yml` has no sibling checkout — all asserted by grep |
| T-123-11-03 | Repudiation | the 360 warnings later read as damage | medium | mitigate | Recorded in the artifact with cause, pre-existence and an explicit not-a-regression statement, asserted by grep |
| T-123-11-04 | Tampering | a whole-file ROADMAP write destroying other phases | critical | mitigate | Scoped `Edit` only, with a `git diff HEAD -- <path>` assertion (explicit ref, so staged edits are visible) that no Phase 124-130 line and no earlier-milestone line changed, and a total changed-line ceiling |
| T-123-11-05 | Tampering | a green row obtained by adjusting a baseline or allow-list | critical | mitigate | The plan forbids adjusting any baseline, watermark, floor or allow-list to make a row green; a failing row is an explicit STOP-and-report |
| T-123-11-06 | Elevation of Privilege | an outward-facing action taken in a phase that owns none | critical | mitigate | No `git push`, no `gh`, no tag, no gitlink bump anywhere in this plan; asserted by the absence of those commands from the executed list |
| T-123-11-07 | Tampering | a gsd commit silently switching the meta checkout mid-plan | medium | mitigate | Branch name and HEAD asserted in all three repos in Task 1 and again after the final commit |
| T-123-11-08 | Repudiation | the no-firmware-code-moves claim discharged by a ref-less `--stat` diff, which at phase end compares only the working tree against a HEAD that already contains all seven waves | critical | mitigate | The claim is measured as a cumulative `<fork_point_firmware>..HEAD` range, with `$FORK` read from 123-01's SUMMARY, asserted non-empty and asserted an ancestor of HEAD; and a grep over the phase's own `*-PLAN.md` set asserts zero occurrences of the ref-less shape so it cannot be reintroduced |
| T-123-11-SC | Tampering | npm/pip/cargo installs | low | accept | Zero installs across the entire phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
1. Every gate row re-executed in this session with verbatim output captured; firmware 48 / 141-17 ×2 / three AVR envs clean; host 49-0-skipped / 1158 / gate trio green; meta 10 claim-gate tests.
2. `123-NONREGRESSION.md` ≥ 120 lines, all eight sections present, every required figure and string present, and it passes its own claim gate.
3. All eight BASE checkboxes ticked; Traceability row Complete; coverage counts unchanged.
4. ROADMAP Phase 123 lists eleven plans; no other phase entry changed; STATE.md untouched.
5. In the firmware repo, the cumulative `git diff --stat "$FORK"..HEAD -- src include platformio.ini .github test` is empty, with `$FORK` = 123-01-SUMMARY's `fork_point_firmware`, asserted non-empty and an ancestor of HEAD — the no-firmware-code-moves boundary proven against every commit the phase made, not against a working tree that all seven waves have already been folded into. `git status --porcelain -- src include platformio.ini .github test` separately empty.
5a. The ref-less `--stat` shape occurs zero times across the phase's `*-PLAN.md` files, so the vacuous form cannot return by a later edit.
6. All three repos on their expected branches after the final commit; nothing pushed.

**Requirement ticking:** this plan ticks **BASE-01 through BASE-08**, and it is the only plan in the phase that ticks anything.
</verification>

<success_criteria>
- MERGE-07's "the gates ran, never skipped" is discharged by a recorded local run, correctly qualified, rather than by a CI leg that would have scored against the wrong tree.
- Phase 124 can read every number and every finding it needs from one committed artifact without recomputing or remembering.
- Every BASE requirement's Complete state is traceable to a specific executed command and its observed output.
- The phase closes having moved no firmware code, pushed nothing and published nothing — and "moved no firmware code" is discharged by a cumulative `<fork_point_firmware>..HEAD` diff over `src include platformio.ini .github test` that covers every commit waves 1-7 made, with the fork point read from 123-01's SUMMARY and asserted non-empty and an ancestor of HEAD. It is explicitly NOT discharged by a working-tree comparison, which at phase end sees none of those commits and would assert the phase's central claim without verifying it.
</success_criteria>

<output>
Create `.planning/phases/123-non-regression-baselines-gate-hardening/123-11-SUMMARY.md` when done.
Record: the per-requirement tick justification, naming the `123-NONREGRESSION.md` row for each of
BASE-01…BASE-08; the full observed-versus-expected table in condensed form; the `fork_point_firmware` SHA
used, the verbatim cumulative `<fork>..HEAD` invocation and its empty output, and the observed integer `0`
from the ref-less-shape grep over the phase's plan set; the three repos' branch names
and HEAD SHAs at phase end; what `.planning/STATE.md` will need to say (phase 123 complete, 11 plans) so
the operator can verify rather than assume; and an explicit statement that this phase pushed nothing,
published nothing and commented nowhere public.
</output>
