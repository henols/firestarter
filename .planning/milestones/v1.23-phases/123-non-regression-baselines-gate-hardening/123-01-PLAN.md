---
phase: 123-non-regression-baselines-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
repo: firestarter (plus a branch-creation task in firestarter_app)
files_modified:
  - firestarter/scripts/baseline/size_baseline.json
  - firestarter/tests/fixtures/README.md
  - firestarter/tests/fixtures/captured_build_uno.log
  - firestarter/tests/fixtures/captured_build_uno328pb.log
  - firestarter/tests/fixtures/captured_build_leonardo.log
  - firestarter/tests/fixtures/captured_test_native_summary.log
  - firestarter/tests/fixtures/captured_test_native_nodevtools_summary.log
  - firestarter/tests/fixtures/captured_native_warnings_excerpt.log
autonomous: true
requirements: [BASE-01, BASE-06]
requirement_closure: "None. BASE-01's comparator is 123-02 and BASE-06's gate is 123-03. Do NOT tick any requirement in this plan — every BASE id is closed in 123-11 and nowhere else."
must_haves:
  truths:
    - "Both sub-repos are on branch `v1.23-py32f071-integration`, forked off their own `beta` at the `origin/beta` tip (D-03 precondition)"
    - "A committed JSON records flash AND RAM for uno, uno328pb and leonardo, re-measured on a clean build in this session (D-01, D-02, D-03)"
    - "The same JSON records cases AND suites for both native envs separately, plus the measured `envs_agree` fact and its consequence for Phase 124's MERGE-06 (D-04)"
    - "The same JSON records macro-redefinition counts per env and marks the native 360 as characterised pre-existing debt, not damage (D-13, operator-locked Option A)"
    - "Verbatim captured build/test output is committed, so plans 123-02 and 123-03 parse real tool output rather than a paraphrase, with no `pio` shell-out inside pytest"
  artifacts:
    - path: "firestarter/scripts/baseline/size_baseline.json"
      provides: "BASE-01 recorded truth: 6 AVR numbers, 2 native pairs, warning watermarks, full provenance meta block"
      contains: "avr_targets"
      min_lines: 40
    - path: "firestarter/tests/fixtures/captured_build_uno.log"
      provides: "verbatim pio size-report output the BASE-01 parser is fixture-tested against"
      contains: "used 23932 bytes from 32256 bytes"
    - path: "firestarter/tests/fixtures/captured_native_warnings_excerpt.log"
      provides: "verbatim pio-framed macro-redefinition diagnostic, closing D-14's recorded open framing gap"
      contains: "redefined"
    - path: "firestarter/tests/fixtures/README.md"
      provides: "the captured_/planted_/clean_ naming discipline and the committed-tree-vs-runtime-materialisation rule every fixture in this phase follows"
      contains: "git ls-files"
      min_lines: 25
  key_links:
    - from: "firestarter/scripts/baseline/size_baseline.json"
      to: "firestarter/scripts/check_size_baseline.py"
      via: "the 123-02 comparator loads this file as its recorded truth via FIRESTARTER_SIZE_BASELINE"
      pattern: "size_baseline\\.json"
    - from: "firestarter/scripts/baseline/size_baseline.json"
      to: "firestarter/scripts/check_build_warnings.py"
      via: "the 123-03 warning gate reads this same JSON's warnings block for the AVR zero rule and the native watermark (D-13)"
      pattern: "warnings"
---

<objective>
Create the v1.23 milestone branch in both sub-repos, re-measure every BASE-01 number on a clean build
per D-03, commit the raw tool output verbatim as fixtures, and write the machine-readable baseline JSON
per D-01/D-02 that Phases 124-130 read instead of remembering.

Purpose: this is the recorded number every later phase is judged against. D-03 requires re-measurement
rather than citing the ROADMAP; D-04 requires the two native envs recorded separately plus an explicit
agreement fact; D-13 plus the operator's post-research decision require the warning watermark stored in
this same file, with the AVR rule spelled as exact equality and the native 360 recorded as characterised
pre-existing debt.

Output: two milestone branches, seven committed fixtures, one baseline JSON.

**No checker, no test module and no firmware source is written by this plan.** Those are 123-02 … 123-06.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repos: **both sub-repos** for Task 1 (`/workspaces/firestarter`, `/workspaces/firestarter_app`);
**firmware only** for Tasks 2 and 3. Firmware commits land inside the `firestarter` submodule on its
`v1.23-py32f071-integration` branch. No meta-repo file is written by this plan.

@.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md
@.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md
</context>

<source_coverage_audit>
## Multi-Source Coverage Audit — plan-set level (covers all 11 plans of Phase 123)

**GOAL** — ROADMAP Phase 123 goal + its five success criteria

| Item | Plan | Status |
|------|------|--------|
| Goal: every gate and baseline exists AND is proven able to fail on a planted violation, before a firmware file moves | 123-01 … 123-11 | COVERED |
| Criterion 1 — committed baseline: flash + RAM ×3 AVR, native case **and** suite counts | 123-01, 123-02 | COVERED |
| Criterion 2 — split proxy keyed on `../firestarter/.git`; missing target ⇒ hard failure; skip census; both proven against a committed fixture | 123-07, 123-08, 123-09 | COVERED |
| Criterion 3 — CMake drift gate non-zero on a mismatched path, zero on a `PY32_EXCLUDED` omission | 123-04 | COVERED |
| Criterion 4 — orphan-provisional non-zero on a zero-consumer flag; warning gate non-zero on one macro redefinition | 123-05, 123-03 | COVERED |
| Criterion 5 — claim gate non-zero on an empty target list and on a forbidden phrase; every new checker ships a planted fixture + pytest | 123-10, 123-06 | COVERED |

**REQ** — REQUIREMENTS.md BASE-01 … BASE-08

| Item | Plan | Status |
|------|------|--------|
| BASE-01 | 123-01, 123-02, 123-11 | COVERED |
| BASE-02 | 123-07, 123-08, 123-11 | COVERED |
| BASE-03 | 123-09, 123-11 | COVERED |
| BASE-04 | 123-04, 123-11 | COVERED |
| BASE-05 | 123-05, 123-11 | COVERED |
| BASE-06 | 123-01, 123-03, 123-11 | COVERED |
| BASE-07 | 123-10, 123-11 | COVERED |
| BASE-08 | 123-02,03,04,05,06,07,09,10, 123-11 | COVERED |

**RESEARCH** — 123-RESEARCH.md corrections, plan-changing findings, Wave-0 gaps

| Item | Plan | Status |
|------|------|--------|
| C-1 native envs agree 141/17 ⇒ MERGE-06 satisfiable, no amendment to request | 123-01, 123-11 | COVERED |
| C-2 warnings 0 on AVR / 360 on native ⇒ operator-locked Option A | 123-01, 123-03 | COVERED |
| C-3 git refuses any `.git` path component at exit 0 ⇒ Mechanism 1 fixture | 123-07 | COVERED |
| C-4 neither firmware workflow fires on the v1.23 branch ⇒ never claim CI-live | 123-11 | COVERED |
| C-5 `pytest tests/ -v` runs before `pio run` ⇒ host `g++`, never avr-g++ | 123-03 | COVERED |
| C-6 D-08's convention holds for only 4 of 7 host checkers ⇒ scope the meta-test to `firestarter/scripts/` | 123-06 | COVERED |
| C-7 24 decorator legs + 1 inline guard across 7 modules (not 27) | 123-08 | COVERED |
| C-8 the 7-module inventory is a superset of MERGE-07's nine gates | 123-08, 123-11 | COVERED |
| C-9 both sub-repos level with `origin/beta`, no v1.23 branch ⇒ create it first | 123-01 | COVERED |
| `PY32_SDK_SOURCES` structurally exempt (FetchContent), not a `PY32_EXCLUDED` entry | 123-04 | COVERED |
| `PY32_EXCLUDED` comment format is a Phase-123 deliverable | 123-04 | COVERED |
| `check_uno_ram.sh` is currently RED (floor 545 vs measured 475 free) | 123-01, 123-02 | COVERED |
| Hoist the never-vacuous guard above the missing-target guard | 123-10 | COVERED |
| v1.23 phrase table (8 rows) + caveat "no PY32F071 hardware exists" | 123-10 | COVERED |
| D-16 line-scoped 3-line proximity window (never sentence segmentation) | 123-10 | COVERED |
| Wave-0 gap: `firestarter/tests/fixtures/` absent | 123-01 | COVERED |
| Wave-0 gap: no Python checker in `firestarter/scripts/` | 123-02 | COVERED |
| Wave-0 gap: committed captured-pio-output fixtures | 123-01 | COVERED |
| Wave-0 gap: `firestarter_app/tests/fixtures/fake_firestarter/` | 123-07 | COVERED |
| Wave-0 gap: shared FW-presence helper | 123-07 | COVERED |
| Wave-0 gap: D-11 central scan-path inventory | 123-08 | COVERED |
| Wave-0 gap: meta `fixtures/` incl. the D-16 clean-AVR control | 123-10 | COVERED |

**CONTEXT** — D-01 … D-16

| Item | Plan | Status |
|------|------|--------|
| D-01 JSON + comparator, not a prose table | 123-01, 123-02 | COVERED |
| D-02 firmware-repo home; supersedes `check_uno_ram.sh` | 123-01, 123-02 | COVERED |
| D-03 re-measure everything, with provenance | 123-01 | COVERED |
| D-04 per-env `{cases, suites}` + recorded agreement fact | 123-01 | COVERED |
| D-05 local run + verbatim evidence artifact, no CI leg | 123-11 | COVERED |
| D-06 scan-target-follows-home | 123-02,03,04,05,06,10 | COVERED |
| D-07 coarse-key arming, no manual arm-flip | 123-04, 123-05 | COVERED |
| D-08 convention-derived meta-test with a hardcoded floor | 123-06 | COVERED |
| D-09 all 7 modules rekeyed + recurrence lint | 123-08, 123-09 | COVERED |
| D-10 skip-reason allow-list, no pinned skip count | 123-09 | COVERED |
| D-11 centralised scan-path inventory + one resolving test | 123-08 | COVERED |
| D-12 committed fake sibling behind an env seam | 123-07 | COVERED |
| D-13 parse build output; AVR zero + native watermark in the same JSON | 123-01, 123-03 | COVERED |
| D-14 compiled `.cpp` fixture + the recorded pio-framing gap | 123-01, 123-03 | COVERED |
| D-15 all-or-nothing arming over the named closing artifacts | 123-10 | COVERED |
| D-16 proximity scoping + both-directions fixture | 123-10 | COVERED |

**Deferred, correctly absent from every plan:** the cross-repo CI leg · ARM flash/RAM baseline
(FUT-ARMSIZE) · backlog 999.15 · `correct-v128-py32-roadmap-prior-art` (Phase 130 / CLOSE-03). The stale
`[env:native_nodevtools]` "16-entry list" comment stays unfixed because no plan edits `platformio.ini`
(binding constraint 1).

**Result: 0 MISSING. No phase split required.**
</source_coverage_audit>

<!-- planner-discipline-allow: warning: -->
<!-- The AVR captures are redirected `pio run` output, never written from this plan's text, so the
     zero-match assertion on that literal cannot be self-invalidated by naming it here. -->

<artifacts_this_phase_produces>
**Firmware repo `/workspaces/firestarter`, branch `v1.23-py32f071-integration` — new:**
`scripts/baseline/size_baseline.json` · `scripts/check_size_baseline.py` · `scripts/check_build_warnings.py` ·
`scripts/check_cmake_manifest.py` · `scripts/check_orphan_provisional.py` ·
`tests/test_check_size_baseline.py` · `tests/test_check_build_warnings.py` ·
`tests/test_check_cmake_manifest.py` · `tests/test_check_orphan_provisional.py` ·
`tests/test_checker_convention.py` · `tests/fixtures/` (README + 6 `captured_*` logs + 3 `planted_*` logs
+ `planted_build_warnings_macro_redef.cpp` + `clean_build_warnings_no_redef.cpp` + 5 fixture trees).
**Firmware repo — deleted:** `scripts/check_uno_ram.sh` (superseded by the baseline comparator, D-02).

**Host repo `/workspaces/firestarter_app`, branch `v1.23-py32f071-integration` — new:**
`tests/fw_presence.py` · `tests/scan_paths.py` · `tests/test_fw_presence.py` ·
`tests/test_scan_paths_resolve.py` · `tests/test_skip_census.py` · `tools/check_no_exists_proxy.py` ·
`tests/test_check_no_exists_proxy.py` · `tests/fixtures/planted_no_exists_proxy.py` ·
`tests/fixtures/fake_firestarter/` (committed deliberately-incomplete sibling tree + README).
**Host repo — modified:** the 7 proxy-carrying test modules.

**Meta repo `.planning/phases/123-non-regression-baselines-gate-hardening/` — new:**
`check_permitted_claims.py` · `test_check_permitted_claims.py` · `fixtures/` (5 markdown files) ·
`123-NONREGRESSION.md`.

**Symbols this phase introduces (the downstream naming contract):**
env seams `FIRESTARTER_SIZE_BASELINE`, `FIRESTARTER_MANIFEST_ROOT`, `FIRESTARTER_PROVISIONAL_ROOT`,
`FIRESTARTER_FW_ROOT`, `FIRESTARTER_PROXY_LINT_TARGETS`, `FIRESTARTER_CLAIMSCAN_TARGETS` ·
JSON keys `meta`, `avr_targets`, `native_envs`, `envs_agree`, `envs_agree_note`, `warnings`,
`flash_used`, `flash_total`, `flash_free`, `ram_used`, `ram_total`, `ram_free`, `cases`, `succeeded`,
`suites`, `all_passed`, `macro_redefinition`, `total`, `total_watermark`, `policy`, `avr_rule`,
`native_rule` · Python names `SIZE_RE`, `CASES_RE`, `SUITE_RE`, `MACRO_REDEF_RE`, `ParseError`,
`parse_sizes`, `parse_native`, `parse_warnings`, `FW_REPO_PRESENT`, `FW_ABSENT_REASON`, `requires_fw`,
`fw_path`, `MissingScanTargetError`, `CROSS_REPO_TEST_PATHS`, `CROSS_REPO_TOOL_RESOLVERS`,
`ALLOWED_SKIP_REASONS`, `ARMED` · CMake comment key `PY32_EXCLUDED`.

**Deliberately NOT produced:** no firmware `src/`/`include/` edit · no `platformio.ini` edit · no merge,
cherry-pick or rename of any firmware source · no workflow edit · no `git push` · no `gh` invocation ·
no release, tag or gitlink bump · no write into either py32 worktree.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the v1.23 milestone branch in both sub-repos, forked off beta</name>
  <files>(no file writes — git branch state only, in /workspaces/firestarter and /workspaces/firestarter_app)</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Branch Creation Required Before Any File Is Written" (lines 1060-1101) — the measured branch state of both repos, the two named hazards, and the explicit "re-verify at execute time rather than trusting this reading" instruction.
    - `.planning/ROADMAP.md` line 1986 (Branch model) — sub-repos fork off `beta` per standing policy.
    - `.planning/STATE.md` §"Operator Next Steps" — the 2026-07-30 `gsd-tools query commit` branch-switch footgun.
  </read_first>
  <action>
    Re-verify, do not assume, the branch state RESEARCH measured. In each of `/workspaces/firestarter`
    and `/workspaces/firestarter_app`: run `git fetch origin`, then
    `git rev-list --left-right --count beta...origin/beta`. If local `beta` is behind, fast-forward it
    with `git merge --ff-only origin/beta` FIRST — a milestone branch forked one commit shy of what
    shipped is the recorded local-beta-lags-origin hazard, and every measurement in Task 2 would be
    against the wrong tree.

    Then create and check out `v1.23-py32f071-integration` off `beta` in BOTH sub-repos. Use exactly that
    branch name; it matches the `v1.22-…` / `v1.21-…` convention already present in both repos' branch
    lists. If a branch of that name already exists, STOP and report rather than reusing it.

    Do NOT push anything (binding constraint 10). Do NOT touch `/workspaces/firestarter_py32_ci` or
    `/workspaces/firestarter_app_py32` — those two gitignored worktrees are read-only reference material
    for plans 123-04 and 123-05.

    After creating the branches, and after EVERY later `gsd-tools query commit` in this phase, verify
    `git rev-parse --abbrev-ref HEAD` in all three repos: the recorded footgun is that a stale
    `current_milestone` plus `branching_strategy: milestone` can silently switch the checkout onto a
    different milestone branch and commit there, reverting gitlinks as a side effect.

    **Record the fork point under an exact, greppable field name — this is a hard contract.**
    Immediately after creating each branch, while it still has zero commits, capture
    `git -C /workspaces/firestarter rev-parse HEAD` and `git -C /workspaces/firestarter_app rev-parse HEAD`
    and write both into this plan's SUMMARY under a section headed exactly `## Fork Point SHAs`, as two
    lines at column 0, each in exactly this shape — field name, one colon, one space, 40 lowercase hex
    characters, nothing else on the line and no surrounding backticks, bullet or bold:

        fork_point_firmware: <40 lowercase hex characters>
        fork_point_host: <40 lowercase hex characters>

    Plans 123-02, 123-03, 123-04, 123-05, 123-06 and 123-11 each grep `123-01-SUMMARY.md` for
    `^fork_point_firmware: [0-9a-f]{40}$` and use the captured SHA as the LEFT side of a cumulative
    `<fork>..HEAD` diff — that cumulative range is the phase's only real proof that no firmware
    production source moved, because by the time those plans run, every earlier plan's changes are
    already committed and a working-tree-only diff can no longer see them. A different spelling, a
    bullet or bold prefix, backticks around the value, or a short SHA breaks every one of those gates.
    They are written to fail closed on an empty capture (`test -n "$FORK"` before the diff), so a
    misspelling stops the phase rather than passing vacuously — but do not rely on that: get the shape
    right the first time.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs exactly `v1.23-py32f071-integration`
    - `git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` outputs exactly `v1.23-py32f071-integration`
    - `git -C /workspaces/firestarter merge-base --is-ancestor origin/beta HEAD` exits 0 (the branch contains the `origin/beta` tip)
    - `git -C /workspaces/firestarter_app merge-base --is-ancestor origin/beta HEAD` exits 0
    - `git -C /workspaces/firestarter log --oneline origin/beta..HEAD` is empty (a pure fork point, no commits yet), and the same in the host repo
    - `git rev-parse --abbrev-ref HEAD` in `/workspaces` still outputs `gsd/v1.23-py32f071-integration`
    - The SUMMARY records the observed `git rev-list --left-right --count beta...origin/beta` value for each repo, and both branch tip SHAs
    - The SUMMARY contains a section headed exactly `## Fork Point SHAs` carrying one line matching `^fork_point_firmware: [0-9a-f]{40}$` and one matching `^fork_point_host: [0-9a-f]{40}$`, verified by running those two greps against the written file and asserting each returns exactly one line
    - Each captured SHA equals its repo's `git rev-parse HEAD` at branch-creation time, and `git -C <repo> merge-base --is-ancestor <sha> HEAD` exits 0
    - Neither `git push` nor any `gh` command appears in the executed command list
  </acceptance_criteria>
  <verify>
    <automated>test "$(git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration" && test "$(git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration" && git -C /workspaces/firestarter merge-base --is-ancestor origin/beta HEAD && git -C /workspaces/firestarter_app merge-base --is-ancestor origin/beta HEAD && test "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "gsd/v1.23-py32f071-integration" && echo BRANCHES_OK</automated>
  </verify>
  <done>Both sub-repos are on `v1.23-py32f071-integration`, each containing its own `origin/beta` tip, with no commits yet, the meta repo still on its own milestone branch, and no outward-facing action taken.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure all six AVR figures and both native pairs, committing verbatim output as fixtures</name>
  <files>firestarter/tests/fixtures/README.md, firestarter/tests/fixtures/captured_build_uno.log, firestarter/tests/fixtures/captured_build_uno328pb.log, firestarter/tests/fixtures/captured_build_leonardo.log, firestarter/tests/fixtures/captured_test_native_summary.log, firestarter/tests/fixtures/captured_test_native_nodevtools_summary.log, firestarter/tests/fixtures/captured_native_warnings_excerpt.log</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Measured Baseline (BASE-01)" (lines 221-300) — the six AVR figures, both native pairs, and the toolchain provenance table.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Parse Targets" (lines 303-370) — the verbatim `RAM:` / `Flash:` and `N test cases:` formats, and the load-bearing fact that the suite count is printed nowhere as a number and must be derived by counting SUMMARY rows.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"BASE-06: Warning Inventory" (lines 372-410) — the 8-macro × 45-TU decomposition and the verbatim `pgm_read_ptr` diagnostic sample with its paired `note:` line.
    - `/workspaces/firestarter/tests/test_update_version.py` lines 68-73 — the `Path(__file__).resolve().parent / "<dir>"` idiom `fixtures/` sits beside `golden/` using.
    - `/workspaces/firestarter/scripts/check_uno_ram.sh` lines 30-58 — the existing `pio run` invocation and `^RAM:` parse whose shape the new parser inherits.
  </read_first>
  <action>
    Create `firestarter/tests/fixtures/`. It does not exist; `firestarter/tests/` currently holds only
    `__init__.py`, `golden/` and `test_update_version.py`.

    Measure in `/workspaces/firestarter`, capturing combined stdout+stderr:
    `pio run -t clean -e uno && pio run -e uno`, then the same for `uno328pb` and `leonardo`, then
    `pio test -e native` and `pio test -e native_nodevtools`. No board is attached and none is needed.

    Commit these captures:
    - `captured_build_uno.log`, `captured_build_uno328pb.log`, `captured_build_leonardo.log` — the FULL
      `pio run -e <env>` output, verbatim and unedited: no trimming, no reflow, no ANSI stripping, no
      hand-edited number. Each carries both the `RAM:` and `Flash:` size lines plus the surrounding pio
      framing, so it serves BASE-01's parser and BASE-06's zero-warning assertion alike.
    - `captured_test_native_summary.log` and `captured_test_native_nodevtools_summary.log` — the verbatim
      tail block of each `pio test` run: the whole `SUMMARY` table (every `native/avr/<suite>` row with
      its PASSED/FAILED/ERRORED status) plus the `================ N test cases: N succeeded …` line. Do
      NOT commit the full native logs; each carries 360 warnings with multi-line source excerpts. Record
      in the SUMMARY exactly which line the capture begins at, so the truncation is a stated fact rather
      than an invisible edit.
    - `captured_native_warnings_excerpt.log` — a verbatim contiguous window of roughly a dozen lines from
      the real `pio test -e native` output around ONE of the 360 macro-redefinition diagnostics,
      including pio's own surrounding framing (the `Compiling .pio/build/...` line above it), the
      `warning: "<macro>" redefined` line, its `#define` echo, and the paired
      `note: this is the location of the previous definition` line pointing at the suite's own
      `test/native/avr/<suite>/avr/pgmspace.h` shim. This is the fixture that closes the gap D-14 flags
      as open: it proves the parser survives pio's framing, which a bare `g++` invocation cannot.

    Count total `warning:` lines and macro-redefinition warnings per env with a filtered pipeline, and
    record BOTH the command and its per-env output for Task 3's meta block. Expected per RESEARCH:
    0 / 0 / 0 on the three AVR envs, 360 / 360 on the two native envs.

    Write `firestarter/tests/fixtures/README.md` recording the discipline every fixture in this phase
    follows: files prefixed `captured_` are verbatim tool output committed unedited; files prefixed
    `planted_` are deliberate violations, each derived from a named `captured_` file by a single stated
    edit; files prefixed `clean_` are controls that must pass. State that `firestarter/tests/` is
    invisible to PlatformIO — pio globs `test/`, `src/` and `lib/`, and both native envs' `build_src_filter`
    is `+<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c> +<operation_utils.cpp>`, naming no path
    under `tests/` — so a `.cpp` fixture here cannot reach any build and `platformio.ini` stays untouched.
    State that fixture presence in this directory is verified with `git ls-files`, never with `git add`'s
    exit code, because git reports exit 0 while staging nothing on a path component it refuses. State
    that the per-file inventory is enforced mechanically by `tests/test_checker_convention.py` (plan
    123-06) rather than duplicated as prose here, so this README cannot drift.

    **Stop condition (D-03).** If any measured figure differs from the RESEARCH-recorded value, STOP and
    surface it rather than editing an acceptance criterion. The tree is byte-identical to `beta` at
    `5c9160a`, so a difference means the toolchain moved, not that the firmware changed. Measured wins —
    but the plan must be amended with the discrepancy recorded, not quietly re-baselined.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter ls-files tests/fixtures/` lists all 7 files (verified with `git ls-files`, never with `git add`'s exit code)
    - `grep -c 'used 23932 bytes from 32256 bytes' firestarter/tests/fixtures/captured_build_uno.log` outputs `1`, and `grep -c 'used 1573 bytes from 2048 bytes'` on the same file outputs `1`
    - `grep -c 'used 23976 bytes from 32384 bytes' firestarter/tests/fixtures/captured_build_uno328pb.log` outputs `1`, and `grep -c 'used 1579 bytes from 2048 bytes'` on the same file outputs `1`
    - `grep -c 'used 26072 bytes from 28672 bytes' firestarter/tests/fixtures/captured_build_leonardo.log` outputs `1`, and `grep -c 'used 2014 bytes from 2560 bytes'` on the same file outputs `1`
    - Each of the three build captures contains exactly one line matching `^RAM:` and exactly one matching `^Flash:`
    - `grep -cE '^=+ +141 test cases: +141 succeeded'` outputs `1` on both native captures
    - `grep -cE 'native/avr/\S+ +(PASSED|FAILED|ERRORED)'` outputs `17` on both native captures, and `grep -cE 'native/avr/\S+ +(FAILED|ERRORED)'` outputs `0` on both
    - `grep -cE 'warning: *"[^"]+" +redefined' firestarter/tests/fixtures/captured_native_warnings_excerpt.log` is `>= 1`
    - `grep -c 'Compiling' firestarter/tests/fixtures/captured_native_warnings_excerpt.log` is `>= 1`, and the file also contains `pgmspace.h` and `this is the location of the previous definition`
    - `grep -cE 'warning:' firestarter/tests/fixtures/captured_build_uno.log` outputs `0`, and likewise for the uno328pb and leonardo captures
    - `firestarter/tests/fixtures/README.md` is at least 25 lines and contains the literal strings `git ls-files`, `captured_`, `planted_` and `clean_`
    - `git -C /workspaces/firestarter status --porcelain -- src include platformio.ini .github test` is empty — no production source, build config, workflow or native suite was touched OR staged by this task. `status --porcelain` is used rather than a path-scoped `--stat` diff because the diff form is blind to an already-staged edit; and the cumulative `<fork>..HEAD` form every later plan uses is deliberately NOT used here, because this plan IS the fork point (the branch has no commits yet, so that range would be empty and would prove nothing)
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -c 'used 23932 bytes from 32256 bytes' tests/fixtures/captured_build_uno.log)" = 1 && test "$(grep -c 'used 1573 bytes from 2048 bytes' tests/fixtures/captured_build_uno.log)" = 1 && test "$(grep -c 'used 23976 bytes from 32384 bytes' tests/fixtures/captured_build_uno328pb.log)" = 1 && test "$(grep -c 'used 1579 bytes from 2048 bytes' tests/fixtures/captured_build_uno328pb.log)" = 1 && test "$(grep -c 'used 26072 bytes from 28672 bytes' tests/fixtures/captured_build_leonardo.log)" = 1 && test "$(grep -c 'used 2014 bytes from 2560 bytes' tests/fixtures/captured_build_leonardo.log)" = 1 && for e in summary nodevtools_summary; do test "$(grep -cE 'native/avr/[^ ]+ +(PASSED|FAILED|ERRORED)' tests/fixtures/captured_test_native_$e.log)" = 17 && test "$(grep -cE 'native/avr/[^ ]+ +(FAILED|ERRORED)' tests/fixtures/captured_test_native_$e.log)" = 0 || exit 1; done && test "$(grep -cE 'warning:' tests/fixtures/captured_build_uno.log)" = 0 && grep -q 'git ls-files' tests/fixtures/README.md && test "$(git ls-files tests/fixtures/ | wc -l)" = 7 && test -z "$(git status --porcelain -- src include platformio.ini .github test)" && echo CAPTURES_OK</automated>
  </verify>
  <done>Seven fixtures are tracked by `git ls-files`, every recorded AVR figure appears verbatim in its own capture, both native captures show 17 PASSED suites and 141/141 cases, the warning excerpt carries pio's framing and the `pgmspace.h` cause, and no production source or build config changed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the BASE-01 baseline JSON with full provenance, per-env native pairs, and the warning watermarks</name>
  <files>firestarter/scripts/baseline/size_baseline.json</files>
  <read_first>
    - `/workspaces/firestarter_app/tools/baseline/dispatch_baseline.json` lines 1-8 — the `{"meta": {...}, "<data>": …}` convention, and note how its `note` field states explicitly whether anything consumes the file.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-PATTERNS.md` §"Analog C — baseline JSON layout" (lines 358-375).
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"Provenance / meta block" (lines 226-238) — the exact toolchain versions to record.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-RESEARCH.md` §"What this means for BASE-06 — three viable shapes, one recommended" (lines 412-432) and §"State of the Art" line 1553 (the `check_uno_ram.sh` 545-vs-475 finding).
  </read_first>
  <action>
    Create `firestarter/scripts/baseline/` and write `size_baseline.json` — D-01 (machine-readable JSON,
    not a prose table) and D-02 (it lives in the firmware repo because the build lives here).

    Top-level keys — these names are the contract Phases 124-130 read, so they are fixed here:
    `meta`, `avr_targets`, `native_envs`, `envs_agree`, `envs_agree_note`, `warnings`.

    `meta` records, per D-03: `generated` (ISO date), `phase` (`"123"`), `generated_by`
    (`"123-01-PLAN.md Task 2 measurement"`), `firmware_tree_sha` (branch HEAD at measurement, from
    `git rev-parse HEAD`), `host_app_tree_sha`, `platformio_core` (`"6.1.19"`), `platform_atmelavr`
    (`"5.2.0"`), `toolchain_atmelavr` (`"1.70300.191015"`), `avr_gcc` (`"7.3.0"`),
    `framework_arduino_avr` (`"5.3.0"`), `framework_arduino_avr_minicore` (`"3.1.2"`),
    `roadmap_cross_check`, `supersedes`, `consumed_by`, `note`.

    `roadmap_cross_check` states affirmatively that five of six ROADMAP figures reproduced byte-exact
    (Leonardo 26072/2014, Uno 23932/1573, uno328pb flash 23976) and that the sixth — uno328pb RAM,
    1579 B — had never been recorded anywhere before this phase. If Task 2 found ANY disagreement, record
    it here explicitly per D-03, with both numbers and the measured one marked authoritative.

    `supersedes` names `firestarter/scripts/check_uno_ram.sh` and records the honest fact that its
    hardcoded floor of 545 B free RAM is ABOVE the measured 475 B free on `uno`, so that script is red
    today and is referenced by neither workflow. This baseline replaces a stale gate, not a green one.
    Plan 123-02 removes the script.

    `consumed_by` names `firestarter/scripts/check_size_baseline.py` and
    `firestarter/scripts/check_build_warnings.py`, and states that this file IS load-bearing — the
    contrast with `dispatch_baseline.json`, whose own note records it as provenance-only, is the reason
    that field exists.

    `avr_targets` carries one object per env key `uno`, `uno328pb`, `leonardo`, each with integer
    `flash_used`, `flash_total`, `flash_free`, `ram_used`, `ram_total`, `ram_free`, taken from Task 2's
    captures.

    `native_envs` carries one object per env key `native` and `native_nodevtools`, each with `cases`,
    `succeeded`, `suites`, `all_passed`. Both get their own pair per D-04's letter even though they agree.
    Alongside, `envs_agree` is the boolean D-04 asked for, and `envs_agree_note` explains WHY they agree
    (no test file references `DEV_TOOLS` or `CMD_DEV_*`, so no suite compiles a conditional case; both
    `test_filter` blocks carry 17 identical entries) and states the consequence for Phase 124: MERGE-06 is
    satisfiable exactly as worded and needs no amendment — D-04's feared live risk did not materialise,
    and no amendment should be requested.

    `warnings` carries `avr` (per-env `macro_redefinition` and `total`, all 0) and `native` (per-env
    `macro_redefinition` and `total_watermark`, both 360), plus a `policy` object encoding the operator's
    post-research decision: `avr_rule` is the string `"== 0"` — spelled as exact equality, not a
    less-than-or-equal form, because the AVR envs are measured genuinely clean and exact equality is
    strictly stronger — and `native_rule` is the string `"<= total_watermark"`.

    `warnings.note` must say, in prose that survives being read in isolation seven phases from now: the
    360 on each native env is a **characterised pre-existing** count, present on `beta` at `5c9160a`
    before any v1.23 work, caused by each suite's own `test/native/avr/<suite>/avr/pgmspace.h` host shim
    defining 8 macros (`PSTR`, `memcpy_P`, `pgm_read_byte`, `pgm_read_dword`, `pgm_read_ptr`,
    `pgm_read_word`, `strcpy_P`, `strlen_P`) which ArduinoFake's own `pgmspace.h` then redefines across
    45 translation units — 8 × 45 = 360. It is **not a regression and not damage**. Deduplicating those
    shims would touch 17 suite directories, breaching this phase's "no firmware code moves" boundary;
    that remediation was considered (RESEARCH Option B) and rejected by the operator. Record the counting
    command verbatim so a later reader can reproduce the 360 rather than trust it.
  </action>
  <acceptance_criteria>
    - `python3 -c "import json;print(sorted(json.load(open('firestarter/scripts/baseline/size_baseline.json'))))"` outputs exactly `['avr_targets', 'envs_agree', 'envs_agree_note', 'meta', 'native_envs', 'warnings']`
    - `d['avr_targets']['uno'] == {'flash_used':23932,'flash_total':32256,'flash_free':8324,'ram_used':1573,'ram_total':2048,'ram_free':475}`
    - `d['avr_targets']['uno328pb']['flash_used'] == 23976` and `d['avr_targets']['uno328pb']['ram_used'] == 1579`
    - `d['avr_targets']['leonardo'] == {'flash_used':26072,'flash_total':28672,'flash_free':2600,'ram_used':2014,'ram_total':2560,'ram_free':546}`
    - `d['native_envs']['native'] == d['native_envs']['native_nodevtools'] == {'cases':141,'succeeded':141,'suites':17,'all_passed':True}` and `d['envs_agree'] is True`
    - `d['warnings']['avr'][e]['macro_redefinition'] == 0` for each of `uno`, `uno328pb`, `leonardo`
    - `d['warnings']['native'][e]['total_watermark'] == 360` for each of `native`, `native_nodevtools`
    - `d['warnings']['policy']['avr_rule'] == '== 0'` and `d['warnings']['policy']['native_rule'] == '<= total_watermark'`
    - `d['warnings']['note']` contains all four substrings `characterised`, `pre-existing`, `pgmspace`, `not a regression`
    - `d['meta']['supersedes']` contains the substrings `check_uno_ram.sh` and `475`
    - `d['meta']['consumed_by']` contains the substring `check_size_baseline.py`
    - `d['envs_agree_note']` contains the substring `MERGE-06`
    - `d['meta']['firmware_tree_sha']` is a 40-character hex string equal to `git -C /workspaces/firestarter rev-parse HEAD` at measurement time
    - `git -C /workspaces/firestarter ls-files scripts/baseline/size_baseline.json` outputs the path
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import json
d=json.load(open('scripts/baseline/size_baseline.json'))
assert sorted(d)==['avr_targets','envs_agree','envs_agree_note','meta','native_envs','warnings'], sorted(d)
assert d['avr_targets']['uno']=={'flash_used':23932,'flash_total':32256,'flash_free':8324,'ram_used':1573,'ram_total':2048,'ram_free':475}
assert d['avr_targets']['uno328pb']['ram_used']==1579 and d['avr_targets']['uno328pb']['flash_used']==23976
assert d['avr_targets']['leonardo']=={'flash_used':26072,'flash_total':28672,'flash_free':2600,'ram_used':2014,'ram_total':2560,'ram_free':546}
n={'cases':141,'succeeded':141,'suites':17,'all_passed':True}
assert d['native_envs']['native']==n and d['native_envs']['native_nodevtools']==n and d['envs_agree'] is True
assert all(d['warnings']['avr'][e]['macro_redefinition']==0 for e in ('uno','uno328pb','leonardo'))
assert all(d['warnings']['native'][e]['total_watermark']==360 for e in ('native','native_nodevtools'))
assert d['warnings']['policy']['avr_rule']=='== 0' and d['warnings']['policy']['native_rule']=='<= total_watermark'
for s in ('characterised','pre-existing','pgmspace','not a regression'): assert s in d['warnings']['note'], s
assert 'check_uno_ram.sh' in d['meta']['supersedes'] and '475' in d['meta']['supersedes']
assert 'check_size_baseline.py' in d['meta']['consumed_by']
assert 'MERGE-06' in d['envs_agree_note']
assert len(d['meta']['firmware_tree_sha'])==40
print('BASELINE_JSON_OK')" && git ls-files scripts/baseline/size_baseline.json | grep -q size_baseline.json</automated>
  </verify>
  <done>`size_baseline.json` exists and is tracked, carries all six AVR figures, both native pairs, `envs_agree: true` with its reason and its MERGE-06 consequence, the AVR-exact-zero / native-360 warning policy, and a note that cannot be misread as recording damage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tool → recorded baseline | pio's stdout is parsed and frozen into a file seven later phases trust without recomputing |
| planning record → measured tree | ROADMAP figures are a cross-check only; a silent mismatch would encode a wrong truth for the whole milestone |
| git index → committed fixture | a fixture that appears staged but is not tracked behaves differently on a fresh clone |
| `beta` tip → milestone branch | a branch cut off a stale base mis-bases every measurement and every later commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-123-01-01 | Repudiation | `size_baseline.json` records a remembered rather than measured number | high | mitigate | D-03 re-measurement on clean builds; `meta.firmware_tree_sha` plus full toolchain provenance; explicit stop-and-report condition on any disagreement |
| T-123-01-02 | Tampering | a fixture appears committed but is absent from the index | high | mitigate | every fixture verified with `git ls-files`, never with `git add`'s exit code (measured: `git add` reports 0 while staging nothing on a refused path) |
| T-123-01-03 | Information Disclosure | the native 360 is later read as damage and triggers a wrong bisect | medium | mitigate | `warnings.note` states the cause (8 pgmspace macros × 45 TUs), pre-existence at `5c9160a`, and that Option B remediation was considered and rejected |
| T-123-01-04 | Elevation of Privilege | branch creation lands on the wrong base or the wrong repo, poisoning every later commit | high | mitigate | `merge-base --is-ancestor origin/beta HEAD` asserted per repo; `rev-parse --abbrev-ref HEAD` re-checked in all three repos after every gsd commit |
| T-123-01-05 | Tampering | a capture is hand-edited between measurement and commit | high | mitigate | captures are redirected tool output; every recorded figure is asserted to appear verbatim in its own capture, so a transcription error fails the gate |
| T-123-01-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. RESEARCH §"Package Legitimacy Audit" records the whole phase as not-applicable: every tool is stdlib, `pytest`, or already-present `pio`/`g++`/`git`. No `[ASSUMED]` or `[SUS]` package exists to gate. |
</threat_model>

<verification>
- `git -C firestarter rev-parse --abbrev-ref HEAD` and the same in `firestarter_app` both output `v1.23-py32f071-integration`; `/workspaces` still on `gsd/v1.23-py32f071-integration`
- **After the SUMMARY is written**, both fork-point fields are present and well-formed — this is the last chance to catch a malformed capture before six downstream plans depend on it:
  `test "$(grep -cE '^fork_point_firmware: [0-9a-f]{40}$' .planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md)" = "1"` and the same with `fork_point_host`, and `grep -q '^## Fork Point SHAs$'` on the same file
- `cd firestarter && python3 -m pytest tests/ -q` still reports **8 passed, 0 failed, 0 skipped** — this plan adds fixtures and a JSON, no test modules, so the pre-existing count must be unchanged
- `git -C firestarter ls-files tests/fixtures/ scripts/baseline/` lists all 8 new files
- `git -C firestarter status --porcelain` empty after the commit
- No `git push` and no `gh` invocation in this plan's command history; both py32 worktrees unwritten

**Requirement ticking:** this plan ticks **nothing**. BASE-01 and BASE-06 are closed only in 123-11.
</verification>

<success_criteria>
- Both sub-repos on `v1.23-py32f071-integration`, forked off their own `origin/beta` tip, nothing pushed
- 7 committed fixtures + 1 committed JSON, all confirmed present via `git ls-files`
- Every one of the six AVR figures and both native `{141, 17}` pairs present in the JSON and independently reproducible from a committed capture
- The 360 native warning count recorded with its cause, its pre-existence and an explicit "not a regression" statement
- Firmware `pytest tests/ -q` still 8 passed, 0 skipped
</success_criteria>

<output>
Create `.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md` when done.
Record **first**, in a section headed exactly `## Fork Point SHAs`, the two contract lines
`fork_point_firmware: <40-hex>` and `fork_point_host: <40-hex>` at column 0, unadorned — six later plans
grep this file for them and their cumulative no-firmware-code-moves proof is inert without them.
Then record: both branch tip SHAs and both `rev-list --left-right` counts; the six AVR figures; both native
`{cases, suites}` pairs and the agreement fact; the verbatim warning-counting command with its per-env
output; the line at which each native capture was truncated; the firmware `pytest tests/ -q` count
(expected 8) so 123-06 and 123-11 can do the count arithmetic without re-deriving it; and any
disagreement with the research cross-check figures, stated as a disagreement rather than normalised away.
</output>
