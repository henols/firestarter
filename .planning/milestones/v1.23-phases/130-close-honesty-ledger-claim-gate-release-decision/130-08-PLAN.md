---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 08
type: execute
wave: 2
depends_on: ["130-02"]
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "D-05 lands for STATE.md: it is edited in place, not given new correction blocks, because it is a working file that already carries a partial ⚠ RESEARCH CORRECTIONS block and would otherwise become block sprawl."
    - "RESEARCH found no stale assertion in STATE.md: :90, :302, :309 and :794 are all correct or correctly labeled. The in-place edits are additive, so this plan records that finding rather than manufacturing corrections."
    - "The two historically-correct 2992 B statements in the Phase 119 decision log (:699, :700) carry inline recordscan:history markers with stated reasons, per RESEARCH C-7."
    - "The Milestone Context section records the CLOSE-03 outcome so a reader of STATE.md is not left with the retired py32 slots as live plans."
    - "check_record_corrections.py reports zero unlabeled hits for STATE.md."
  artifacts:
    - .planning/STATE.md
  key_links:
    - "STATE.md ## Milestone Context (v1.23) -> the ROADMAP retirement line plans 130-04 and 130-05 wrote"
    - "each history-exempt decision-log line -> an inline recordscan:history marker carrying a stated reason"
  prohibitions:
    - "Do NOT delete or rewrite the Phase 119 decision-log lines carrying 2992 B. They were accurate when written — 2992 = 28672 − 25680 was the pre-Phase-119 headroom that Phase 119's +392 B was judged against — and RESEARCH C-7 establishes R-10 needs no substantive correction."
    - "Do NOT add new ⚠ CORRECTION blocks to STATE.md. D-05 assigns it in-place editing precisely to avoid block sprawl; PROJECT.md and ROADMAP.md are the files that get blocks."
    - "Do NOT edit STATE.md's YAML frontmatter. current_phase, current_phase_name, status, stopped_at, last_activity_desc and progress.percent are the orchestrator's to set at phase end, and this project's state tooling is known to under-write and re-clobber them."
    - "Do NOT use a whole-file Write on STATE.md. It is over 780 lines; use scoped Edit operations, and re-read the file immediately before each edit because the orchestrator may have written to it between waves."
    - "Do NOT manufacture a correction for a site RESEARCH measured as already correct. Record the finding instead."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Carry the research corrections into `STATE.md` under D-05's in-place treatment, and mark the
historically-correct statements so the CLOSE-01 checker can tell a preserved record from a stale claim.

The honest finding, which shapes this plan more than CONTEXT's framing does: research grepped every
needle across this file and found **no stale assertion**. `:90`'s C-1 row, `:302`'s
`⚠ RESEARCH CORRECTIONS` bullet, `:309`'s stale-prior-art warning and `:794`'s
`write_checksums.cmake` record are all either correct or correctly labeled. The only two `2992 B`
occurrences outside the labeled bullet are Phase 119 decision-log lines that were **accurate when
written**.

So this plan's real work is three things, not a sweep: record that finding, mark the two history lines,
and bring the Milestone Context section up to date so a reader is not left believing the two py32
ROADMAP slots are live plans.

Purpose: leave the working file honest and checker-green without inventing corrections or destroying
records.
Output: in-place Milestone Context updates, two history markers, and a recorded no-stale-assertion
finding.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-02-SUMMARY.md
</context>

## Artifacts this phase produces

Changed by **this** plan: `.planning/STATE.md`, body sections only — never its YAML frontmatter. No new
file.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, and
the two `usb_cdc.c` `#define` values.

<tasks>

<task type="auto">
  <name>Task 1: Mark the two Phase 119 decision-log 2992 B lines history-exempt with stated reasons</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 695-705 — the Phase 119 decision-log bullets. Line 699 records the sixth
      PROJECT.md correction block including *"LOCK-06's 3348B superseded by live 2992B"*; line 700 records
      *"measured against the live 2992 B phase-base headroom (28672-25680)"*.
    - `.planning/STATE.md` line 302 — the `⚠ RESEARCH CORRECTIONS` bullet, which legitimately quotes
      `2992 B` as *"not 2992 B"* and is already exempt by the line-label path. It must **not** get a
      redundant marker.
    - `.planning/STATE.md` lines 85-95 — the C-1 correction table row, for how this file already records
      a research correction, so the style matches.
    - `check_record_corrections.py` as written by plan 130-02 — the inline marker grammar, and the rule
      that a marker with no stated reason does **not** exempt.
    - `130-RESEARCH.md` §"C-7" and the R-10 row of the work list.
    - `130-02-SUMMARY.md` — the reconciliation table filtered to `STATE.md` rows is this task's
      authoritative worklist.
  </read_first>
  <action>
Re-read `STATE.md` immediately before editing. The orchestrator writes to this file between waves and
this project's state tooling is known to under-write and re-clobber fields, so line numbers recorded at
plan time may have moved. Locate the two decision-log lines by their text, not by line number.

Append an inline `recordscan:history` marker to each of the two lines. The stated reason must say, in
substance, that `2992 B` was the **pre-Phase-119** Leonardo headroom (28672 − 25680), that it is exactly
the figure Phase 119's +392 B was judged against, and that the line is therefore preserved as an
accurate decision-log record rather than corrected. Reference `130-RESEARCH.md` C-7 so a future auditor
can find the reasoning.

The marker is an HTML comment and is invisible in the rendered file. The prose of both lines stays
byte-unchanged apart from the appended comment — do not reflow, do not re-word, do not renumber.

Do **not** add a marker to line 302. It is already exempt by the line-label path, and a redundant
marker makes the exemption set harder to audit.

Do not touch the YAML frontmatter.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py --explain | grep -E 'leonardo-headroom-2992|unlabeled|inline-history' | head -20; FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py --explain`
      reports exactly two records with verdict `inline-history` for the `leonardo-headroom-2992` label,
      and exactly one with verdict `line-label` (line 302).
    - The same `--explain` run reports zero records with verdict `unlabeled`.
    - `git -C /workspaces diff -- .planning/STATE.md` shows, on each of the two decision-log lines, only
      an added HTML comment — no change to any other text on those lines.
    - Stripping either marker on a `/tmp` copy of `STATE.md` makes
      `FIRESTARTER_RECORDSCAN_TARGETS=<copy> python3 check_record_corrections.py` exit non-zero and name
      the `leonardo-headroom-2992` label. Record the observed output, then discard the copy.
    - `git -C /workspaces diff -- .planning/STATE.md` shows no hunk inside the YAML frontmatter block
      (the region between the first two `---` lines).
    - Line 302's bullet carries no `recordscan:` marker.
  </acceptance_criteria>
  <done>The two accurate decision-log records are preserved and exempt with stated reasons, the exemption is proven live on the real file, and no redundant marker was added.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Milestone Context section in place and record the no-stale-assertion finding</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` lines 296-338 — the `## Milestone Context (v1.23)` and `## Roadmap Summary
      (v1.23)` sections in full. Note the existing bullets at 302, 303, 309, 313 and 314 and their style:
      bold lead-in, then substance, one bullet per fact.
    - `.planning/STATE.md` lines 84-117 — the Phase 129 research-corrections table, for the correction
      style this file already uses.
    - `.planning/ROADMAP.md` as left by plans 130-04 and 130-05 — the new `✅ v1.23 PY32F071 Integration`
      entry, the retirement line, the relocated `v1.28 Binary Command Protocol` entry, and the retired
      `999.23`/`999.24` stubs. The Milestone Context update must describe what is actually there now.
    - `130-RESEARCH.md` §"Summary of the actual CLOSE-01 work list", the `STATE.md` bullet — the finding
      that no stale assertion exists in this file.
    - `130-02-SUMMARY.md` — the machine list, so the finding is confirmed against the checker rather than
      inherited from research prose.
  </read_first>
  <action>
Edit in place, additively, in the style the file already uses. Three additions and one correction.

**(1) Record the CLOSE-03 outcome.** Add a bullet to `## Milestone Context (v1.23)` stating that the
ROADMAP slot renumber landed in Phase 130: the `## Milestones` list now carries a real
`✅ v1.23 PY32F071 Integration — Phases 123–130` entry where it previously ran straight from v1.22 to
`v1.23 Binary Command Protocol`; Binary Command Protocol is now **v1.28** and sits after v1.27 in
version order; both py32 slots are retired into v1.23 behind one dated retirement line; the v1.29 number
is vacant; `v1.30` keeps its number by its own instruction; and backlog stubs `999.23`/`999.24` are
retired as delivered. Name plans 130-04 and 130-05 as the owners.

**(2) Correct the stale-prior-art bullet at `:309`.** It warns that the v1.28 ROADMAP entry's prior-art
paragraph is stale. That entry no longer exists. Rewrite the bullet in place to record that the
paragraph was **removed with its entry** in Phase 130, that the owning todo
`correct-v128-py32-roadmap-prior-art` moved to `todos/completed/`, and that the historical text survives
in git history only. Preserve the substance of what was believed — the bullet is the trail — but stop it
warning about a paragraph a reader cannot find.

**(3) Record the no-stale-assertion finding.** Add a bullet stating that CLOSE-01's committed checker
`check_record_corrections.py` was run against this file and found **zero** unlabeled needle hits, that
`:90`, `:302`, `:309` and `:794` were each measured as correct or correctly labeled, and that the two
Phase 119 decision-log `2992 B` lines are exempt as history with the reason recorded on the line. Cite
the checker path and the exact command so the claim is re-runnable, and confirm the finding against the
machine list in `130-02-SUMMARY.md` rather than inheriting it from research prose. If the machine list
disagrees with research on any site, record the disagreement and act on the machine list.

**(4) Record the D-11 firmware change and the ARM-pass status.** Add a bullet stating that the py32 USB
descriptor now presents pid.codes `1209:0001` instead of Puya Semiconductor's registered `0x36B7`/
`0xFFFF` (D-11, plan 130-03), that the `[SHARED:S4]` record pair was updated in lockstep and the 41-leg
sync gate re-run locally because it runs in no CI leg, and that the ship gate in §5(c) is unchanged with
the tension carried as an owned residual (D-17). Do not write that the warning is required by
pid.codes' terms — the terms say the source *should* warn.

Do not touch the YAML frontmatter. Re-read the file immediately before each edit.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py; echo "exit=$?"; cd /workspaces/.planning && python3 -c "
s=open('STATE.md').read()
for k in ('v1.23 PY32F071 Integration','v1.28 Binary Command Protocol','999.23','1209:0001','check_record_corrections.py','todos/completed'):
    assert k in s, k
assert 'required by pid.codes' not in s
print('OK milestone context updated')"</automated>
  </verify>
  <acceptance_criteria>
    - `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py`
      exits `0` and prints a `PASS:` line naming `STATE.md`.
    - `## Milestone Context (v1.23)` contains a bullet naming the CLOSE-03 outcome with all six facts
      (the v1.23 entry, the v1.28 renumber, the retirement line, the vacant v1.29, v1.30 unchanged, the
      retired stubs) and naming plans 130-04 and 130-05.
    - The `:309` bullet no longer warns about a paragraph that exists; it records the removal, the todo
      move to `todos/completed/`, and that the text survives in git history.
    - A bullet records the checker finding with the exact re-runnable command and the four measured sites
      `:90`, `:302`, `:309`, `:794`.
    - A bullet records D-11's descriptor change, the lockstep record edit, the locally re-run 41-leg gate,
      and D-17's unchanged ship gate.
    - `grep -c 'required by pid.codes' .planning/STATE.md` returns `0`.
    - `git -C /workspaces diff -- .planning/STATE.md` shows no hunk inside the YAML frontmatter block.
    - `git -C /workspaces diff --stat -- .planning/STATE.md` shows exactly one file changed; no new
      `⚠ CORRECTION` block heading was added (D-05 assigns blocks to PROJECT.md and ROADMAP.md, not
      here).
  </acceptance_criteria>
  <done>STATE.md describes the record as it now stands, the stale-prior-art warning points at a real state, and the no-stale-assertion finding is recorded with a re-runnable command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `STATE.md` -> the next session's agent | This is the working file every resumed session reads first. A warning about a paragraph that no longer exists sends a reader looking for it. |
| this plan -> the orchestrator's own writes | GSD state tooling writes this file between waves and is known to under-write and re-clobber fields. An unscoped edit here can lose orchestrator state. |
| a history marker -> a future auditor | An exemption with no stated reason is a silent forgiveness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-33 | Tampering | `STATE.md` YAML frontmatter | high | mitigate | `must_haves.prohibitions` forbid touching it; an acceptance criterion asserts no diff hunk inside the frontmatter block. The orchestrator owns those fields. |
| T-130-34 | Tampering | the Phase 119 decision-log records | high | mitigate | Markers are appended HTML comments; a criterion requires the prose byte-unchanged, and the strip-the-marker demonstration proves the exemption is live rather than accidental. |
| T-130-35 | Repudiation | an invented correction | medium | mitigate | Research measured no stale assertion in this file. The plan records the finding and confirms it against plan 130-02's machine list, and `must_haves.prohibitions` forbid manufacturing a correction for an already-correct site. |
| T-130-36 | Repudiation / false assurance | the `:309` stale-prior-art warning | medium | mitigate | Rewritten in place to record the removal and the todo move, so the trail survives without misdirecting a reader. |
| T-130-37 | Denial of service (self-inflicted) | `STATE.md` outside the edited sections | high | mitigate | Scoped `Edit` only, with a mandated re-read immediately before each edit because a concurrent orchestrator write is expected. |
| T-130-38 | Information disclosure | pid.codes terms wording | low | mitigate | A criterion greps the forbidden `required by pid.codes` phrasing at zero, per RESEARCH C-6. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package and runs only `python3`, `git` and `grep`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/STATE.md python3 check_record_corrections.py` → exit 0, `PASS:` naming `STATE.md`.
- `--explain` on that target → zero `unlabeled`, exactly two `inline-history`, one `line-label`.
- `git diff -- .planning/STATE.md` → no hunk in the YAML frontmatter block.
- `grep -c 'required by pid.codes' .planning/STATE.md` → `0`.
- The marker-strip demonstration on a `/tmp` copy → non-zero exit naming the label.
</verification>

<success_criteria>
- D-05's in-place treatment is applied; no new correction block was added to this file.
- The two historically-correct decision-log lines are exempt with stated reasons, proven live.
- The Milestone Context describes the ROADMAP as it now stands, including the retired stubs and the vacant v1.29.
- The no-stale-assertion finding is recorded with a re-runnable command and confirmed against the machine list.
- STATE.md is green under the CLOSE-01 checker; its frontmatter is untouched.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-08-SUMMARY.md` when done.
</output>
