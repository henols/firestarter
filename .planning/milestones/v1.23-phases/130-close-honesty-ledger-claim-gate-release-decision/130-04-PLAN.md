---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [CLOSE-03]

must_haves:
  truths:
    - "D-13 lands: ROADMAP.md's ## Milestones list gains a real ✅ v1.23 PY32F071 Integration — Phases 123–130 entry where it previously ran straight from v1.22 to v1.23 Binary Command Protocol."
    - "D-13 lands: the v1.28 PY32F071 Port and v1.29 PY32F071 USB Firmware Install entries collapse into ONE dated retirement line pointing at the v1.23 detail section, and the stale prior-art paragraph disappears with the entry it lived in."
    - "D-14 lands: Binary Command Protocol is renumbered v1.23 → v1.28 and MOVED to after v1.27, preserving the list's strict version ordering; its stale sequence sentence is annotated; v1.30 keeps its number; v1.29 is left vacant and explained by the retirement line."
    - "D-16 is proven: the four v1.24–v1.27 entries are byte-unchanged, evidenced by SHA-256 captured before the edit and re-hashed after, plus a path-scoped git diff, all recorded for plan 130-16."
    - "CONSTRAINT 11 is honoured: this collapse runs BEFORE any CLOSE-01 ROADMAP sweep, so no ⚠ CORRECTION block is written into a line that is then deleted."
  artifacts:
    - .planning/ROADMAP.md
  key_links:
    - "the new v1.23 SHIPPED entry -> the v1.23 detail section heading '## v1.23 — PY32F071 Integration' further down the file"
    - "the retirement line -> the two collapsed py32 slots' historical text, preserved in git history only"
    - "the four v1.24–v1.27 entry SHA-256 values -> plan 130-16's 130-NONREGRESSION.md D-16 record"
  prohibitions:
    - "Do NOT change one byte of the v1.24, v1.25, v1.26 or v1.27 entries. Their pre-edit SHA-256 values are 4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b (v1.24), 4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f (v1.25), 733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9 (v1.26) and bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52 (v1.27), measured at plan time on 2026-08-02."
    - "Do NOT build a committed checker for the byte-unchanged claim. D-16 records the reason: those four entries SHOULD change when v1.24 is scoped, so a standing gate would either ship pre-obsolete or block a legitimate edit forever. This is the one place BASE-08's ships-with-a-fixture discipline is deliberately not applied."
    - "Do NOT compact the v1.30 SDP slot into the freed v1.29. That entry's own text says to compact at activation, not now (D-14), and /gsd-new-milestone settles its number anyway."
    - "Do NOT delete the two py32 entries outright without a retirement line. That leaves the 999.23/999.24 backlog stubs pointing into a void (D-13 rejected option)."
    - "Do NOT keep either py32 entry marked RETIRED with its body intact. The owning todo explicitly warns against it: a scoping pass reads the body regardless of the marker (D-13 rejected option)."
    - "Do NOT edit ROADMAP.md's backlog stubs, its dated review-history paragraphs, ROADMAP.md:2414 or ROADMAP.md:2468 in this plan. Those belong to plans 130-05 and 130-06, which run in later waves because they touch the same file."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Land the ROADMAP slot renumber and the stale v1.28 prior-art correction in one change, and capture the
one-shot proof that the four untouched entries really are untouched.

This runs **before** any CLOSE-01 ROADMAP sweep. RESEARCH C-11 established that as an eleventh hard
sequencing constraint CONTEXT's ten do not contain: lines 33 and 34 carry R-1, R-5, R-8, R-9, R-11 and
R-14 verbatim, so running CLOSE-01 first would write `⚠ CORRECTION` blocks into two lines this plan
then deletes, while running this plan first discharges the great majority of CLOSE-01's ROADMAP work by
deletion, with the historical text preserved in git as D-13 intends.

Two asymmetries research found that D-13's symmetric framing hides (C-12): line 34 already carries a
`⚠ SUPERSEDED — this slot is RETIRED into v1.23 ... Marker added 2026-07-31; content left otherwise
untouched for Phase 130 to retire` marker, and **line 33 carries no marker at all** — its stale
prior-art paragraph, the exact thing the owning todo was filed about and the thing
`/gsd-new-milestone` reads, is currently unwarned. The two lines' diffs will not look alike, and no
assertion may claim both slots carried a supersession marker.

Purpose: stop the propagation hazard at its source, before the next milestone's scoping pass reads it.
Output: a corrected `## Milestones` list and the D-16 before/after hash record.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
@.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md
</context>

## Artifacts this phase produces

Changed by **this** plan: `.planning/ROADMAP.md` — the `## Milestones` list only (currently lines
27-35). No new file. The D-16 hash record is written into `130-04-SUMMARY.md` and carried into
`130-NONREGRESSION.md` by plan 130-16.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, and
the two `usb_cdc.c` `#define` values.

<tasks>

<task type="auto">
  <name>Task 1: Capture the D-16 before-state — SHA-256 of each v1.24–v1.27 entry, keyed by version token not by line number</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-04-SUMMARY.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 1-36 — the whole `## Milestones` list. Note that entries are
      **one physical line each**, that line 28 is the `⬜ v1.23 Binary Command Protocol` entry, 29-32
      are v1.24 through v1.27, 33 and 34 are the two py32 slots, and 35 is v1.30.
    - `130-RESEARCH.md` §"CLOSE-03's Subject Matter — exact current lines" — the per-line role table
      and the verbatim stale prior-art paragraph.
    - `130-PATTERNS.md` §"No Analog Found" — D-16 deliberately has no checker analog; the closest shape
      analog for a hash-based before/after record is `129-NONREGRESSION.md` §"ARM byte-identity row".
  </read_first>
  <action>
Capture the before-state that makes D-16's claim falsifiable. The edit in Task 2 changes the file's
line count and moves one entry, so **line numbers shift** and a line-number-keyed proof would be
worthless. Key the proof on the entry's version token instead.

For each of the four version tokens `v1.24`, `v1.25`, `v1.26`, `v1.27`: locate the single line in the
`## Milestones` list whose bold entry title begins with that token, and record the line's full
SHA-256 together with its current line number and its byte length. Do the same for the three lines
this plan does change (`v1.23 Binary Command Protocol`, `v1.28 PY32F071 Port`,
`v1.29 PY32F071 USB Firmware Install`) and for `v1.30`, so the SUMMARY shows which hashes are expected
to move and which are not.

The four expected before-hashes, measured at plan time on 2026-08-02, are:
`v1.24` = `4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b`,
`v1.25` = `4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f`,
`v1.26` = `733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9`,
`v1.27` = `bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52`.
The `v1.30` before-hash is `ec99fc7fcb15d22eae4abe12c1dd50ef1685687522fd35e95f03d13a3ed5b064`; expect
it to **change**, because plan 130-05 corrects its back-reference — note that explicitly so a later
reader does not read the v1.30 change as a D-16 violation.

If a measured before-hash does not match the value above, stop and record the drift before editing
anything: it means the file moved between planning and execution, and the recorded value, not the
measurement, is what needs re-deriving.

Also record the exact `git diff` invocation Task 3 will use, and the current total line count of
`ROADMAP.md` (2476 at plan time), so the after-state comparison has a stated baseline.

Do not edit `ROADMAP.md` in this task.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
import hashlib,re,sys
want={'v1.24':'4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b','v1.25':'4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f','v1.26':'733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9','v1.27':'bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52'}
lines=open('ROADMAP.md').read().split('\n')
got={}
for i,l in enumerate(lines[:40]):
    m=re.match(r'^- \S+ \*\*(v1\.\d+)\b',l)
    if m and m.group(1) in want: got[m.group(1)]=(i+1,hashlib.sha256((l+'\n').encode()).hexdigest())
bad=[k for k in want if k not in got or got[k][1]!=want[k]]
print('measured:',got)
sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0`: all four measured SHA-256 values equal the four recorded values,
      confirming the file has not moved since planning.
    - `130-04-SUMMARY.md` records, in a table, one row per version token for v1.23-BCP, v1.24, v1.25,
      v1.26, v1.27, v1.28-py32, v1.29-py32 and v1.30, each with: current line number, byte length,
      SHA-256, and an expected-to-change column reading `no` for v1.24–v1.27 and `yes` for the others.
    - `130-04-SUMMARY.md` states the current `wc -l` of `.planning/ROADMAP.md` as the after-comparison
      baseline.
    - `130-04-SUMMARY.md` states in one sentence why the proof is keyed on version token rather than
      line number.
    - `130-04-SUMMARY.md` names v1.30's expected hash change and attributes it to plan 130-05, so it is
      not read as a D-16 violation.
    - `git -C /workspaces diff --stat -- .planning/ROADMAP.md` is empty at the end of this task.
  </acceptance_criteria>
  <done>The before-state is measured, matches the planned values, and is keyed so it survives the line-number shift the next task causes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Milestones list — v1.23 gains its SHIPPED entry, BCP moves to v1.28, the two py32 slots collapse into one retirement line</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 1-36 — the list. Read every one of lines 27-35 in full; they are
      long. Also read the shape of an existing shipped entry (line 26 `✅ v1.21`, line 27 `✅ v1.22`)
      because the new v1.23 entry must match that shape: status glyph, bold title, em dash, phase
      range, a parenthetical carrying the ship date and the cut, then the substance, then a full-detail
      pointer.
    - `.planning/ROADMAP.md` around line 1993 — the `## v1.23 — PY32F071 Integration (PLANNING)`
      detail-section heading the new entry must point at.
    - `.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md` — its five numbered corrections
      plus the header note recording that v1.23 research re-verified all five and found a sixth
      (A-6/R-8, `PORTING.md` absent from the live branch). The retirement line must not reintroduce any
      of the five.
    - `130-RESEARCH.md` §"CLOSE-03's Subject Matter" and §"C-12" — the exact current lines and the
      marker asymmetry between line 33 and line 34.
    - `.planning/REQUIREMENTS.md` §"Close" and §"Validation Ceiling" — what may and may not be claimed
      about v1.23 in a shipped-entry summary.
  </read_first>
  <action>
Rewrite only the `## Milestones` list region. Four changes, in one commit.

**(1) Line 28 becomes v1.23's real shipped entry.** Replace the `⬜ v1.23 Binary Command Protocol`
entry in place with `✅ **v1.23 PY32F071 Integration** — Phases 123–130`, followed by a parenthetical
and body in the same shape as the v1.21 and v1.22 entries above it. Its substance must stay inside the
Validation Ceiling: the milestone is software-only, no PY32F071 PCB exists, nothing in it has run on
this silicon. Name what it delivered — the atomic port-stack landing, the VPP seam, flash-persistent
config, the host DFU installer, the release-asset fold, the flash-path and PCB record, and this close
— plus the requirement tally by category, and a full-detail pointer to the
`## v1.23 — PY32F071 Integration` detail section. Do **not** state the cut tag as a literal: at the
time this line is written the cut has not happened, so write the tag as a placeholder the closing plan
fills, or omit the tag and let plan 130-16 add the observed value. CONSTRAINT 5 forbids any predicted
beta tag value in anything intended to be authoritative before the tag is read from `gh release list`.

**(2) Binary Command Protocol is renumbered and moved.** Its entry becomes
`⬜ **v1.28 Binary Command Protocol**` and is **relocated to immediately after the v1.27 entry**, so
the list keeps its strict version ordering v1.15…v1.30 rather than breaking it for the first time
(D-14). Carry the entry's body across unchanged in substance. Add one short annotation to its
*"Sequence ahead of v1.24 (also breaking)"* sentence recording that the **number** is bookkeeping
while the **sequence** claim is the substance — the project's own stated convention, already quoted
verbatim in two other entries. Do not renumber the v1.24 reference inside that sentence; the sequence
claim is about v1.24 and stays true.

**(3) The two py32 slots collapse into one dated retirement line.** Delete the `v1.28 PY32F071 Port`
entry and the `v1.29 PY32F071 USB Firmware Install` entry and replace both with a single line,
positioned where they were (that is, after the relocated v1.28 Binary Command Protocol entry and
before v1.30). The retirement line must:
  - be dated, and state that both slots were absorbed into **v1.23 PY32F071 Integration, Phases
    123–130**;
  - point at the `## v1.23 — PY32F071 Integration` detail section for what actually shipped;
  - state that the full historical text of both entries is preserved in git history and that the
    v1.28 slot's *"Prior art — verified 2026-07-27"* paragraph was **stale** — it claimed the work was
    not in flight citing PR #46 closed-unmerged, and pointed a scoping pass at
    `feature/py32f071-toolchain` @ `2c2ed10` — which is why it was removed with its entry rather than
    left behind a marker;
  - record the asymmetry C-12 found: the v1.29 slot already carried a `⚠ SUPERSEDED` marker added
    2026-07-31 while the v1.28 slot carried **no marker at all**, so the two are not being treated
    symmetrically because they were not in the same state;
  - state that the **v1.29 number is now vacant**, and that this is deliberate rather than an
    oversight;
  - name the owning todo `correct-v128-py32-roadmap-prior-art` as discharged here.
  The retirement line must not restate any of the todo's five wrong claims, and must not cite
  `PORTING.md` as an existing specification (A-6/R-8: it lives only on the two closed PRs).

**(4) v1.30 stays v1.30 and is otherwise untouched in this plan.** Do not compact it into the freed
v1.29. Its back-reference sentence — *"the v1.29 slot immediately above is still occupied by the py32
USB-install entry until v1.23's Phase 130 retires it"* — is now false, but correcting it belongs to
plan 130-05 together with the backlog stubs. Leave it for that plan so the two ROADMAP edits stay in
separate, reviewable commits.

Use scoped `Edit` operations, never a whole-file `Write`. A whole-file write on `ROADMAP.md` would put
every entry outside the diff window at risk, and this file is 2476 lines.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
import re,sys
lines=open('ROADMAP.md').read().split('\n')
head=[l for l in lines[:45] if re.match(r'^- \S+ \*\*v1\.\d+',l)]
toks=[re.match(r'^- \S+ \*\*(v1\.\d+)',l).group(1) for l in head]
print('order:',toks)
assert 'v1.23' in toks
i23=toks.index('v1.23')
assert head[i23].startswith('- ✅ **v1.23 PY32F071 Integration**'), head[i23][:80]
nums=[float(t[1:]) for t in toks]
assert nums==sorted(nums), ('version order broken',nums)
body='\n'.join(lines[:45])
assert 'v1.23 Binary Command Protocol' not in body
assert 'v1.28 Binary Command Protocol' in body
assert '2c2ed10' not in body, 'stale prior-art SHA still in the Milestones list'
assert '603 additions' not in body
print('OK')" && grep -c 'v1.29' ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## Milestones` list's version tokens read in strictly ascending numeric order from v1.0
      through v1.30, with no token appearing twice.
    - Exactly one list entry begins `- ✅ **v1.23 PY32F071 Integration**` and it names `Phases 123–130`.
    - No list entry contains the string `v1.23 Binary Command Protocol`; exactly one contains
      `v1.28 Binary Command Protocol`, and it sits immediately after the v1.27 entry.
    - Exactly one retirement line replaces the two former py32 slots; it is dated, names Phases 123–130,
      points at the v1.23 detail section, states the v1.29 number is now vacant, and names the todo
      `correct-v128-py32-roadmap-prior-art`.
    - Within the `## Milestones` list region, `grep -c '2c2ed10'` and `grep -c '603 additions'` both
      return `0` — the stale prior-art paragraph is gone with its entry. (Elsewhere in the file those
      strings legitimately survive in the backlog and review-history regions; plan 130-05 owns those.)
    - `grep -c 'PORTING.md' ` restricted to the `## Milestones` list region returns `0`.
    - The v1.24, v1.25, v1.26 and v1.27 entry lines still hash to the four values recorded in Task 1.
    - The v1.30 entry still begins `- ⬜ **v1.30 SDP Surface Retirement & Behavioral Lock Proof**` and
      still contains its `the v1.29 slot immediately above` sentence — plan 130-05 corrects it, not this
      plan.
    - `git -C /workspaces diff -U0 -- .planning/ROADMAP.md` shows changed hunks only in the line range
      that was 27-35 before the edit; no hunk touches any line beyond the `## Milestones` list.
  </acceptance_criteria>
  <done>The list carries a real v1.23 shipped entry, BCP sits at v1.28 in version order, the two py32 slots are one dated retirement line, and the stale prior-art paragraph is gone from the list.</done>
</task>

<task type="auto">
  <name>Task 3: Re-hash the four v1.24–v1.27 entries and record the path-scoped diff as D-16's one-shot proof</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-04-SUMMARY.md</files>
  <read_first>
    - `130-04-SUMMARY.md` as written by Task 1 — the before-hash table and the stated `git diff`
      invocation.
    - `.planning/ROADMAP.md` as edited by Task 2.
    - `130-RESEARCH.md` §"Phase Requirements → Test Map", the CLOSE-03 byte-unchanged row — the proof is
      `sha256sum` of each entry before and after plus `git diff -U0 -- .planning/ROADMAP.md`, recorded
      in `130-NONREGRESSION.md`.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` from
      §"ARM byte-identity row" onward — the shape of a hash-based before/after record in this project.
  </read_first>
  <action>
Close the D-16 proof and record it in a form plan 130-16 can lift into `130-NONREGRESSION.md`
verbatim.

Re-locate each of the four entries by version token — their line numbers have moved — and re-hash each
line. Compare against Task 1's recorded before-hashes. All four must be equal. Record the after line
numbers alongside, so a reader can see the lines moved while their content did not; that combination is
the whole point of keying on the token.

Run the path-scoped diff `git -C /workspaces diff -U0 -- .planning/ROADMAP.md` and record its output.
Then state, explicitly, that no hunk in that output touches any of the four entries, and cite the
after-hash equality as the mechanical evidence for that statement rather than relying on a human read
of the diff.

Record D-16's stated reason that **no committed checker exists** for this claim, in the record's own
voice and not as a footnote: *"these four entries never change"* is false as a standing invariant —
those entries **should** change when v1.24 is scoped — so a permanent gate would either ship
pre-obsolete or block a legitimate edit forever. Note that this is the one place in the milestone where
BASE-08's ships-with-a-fixture discipline is deliberately not applied, and that D-16 records the reason
so a later reader does not read the absence as an oversight.

Finally, record the two changes a reader will see in the diff that are **not** D-16 violations: the
v1.30 entry is untouched by this plan and will change in plan 130-05, and the total line count drops
because two entries became one.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
import hashlib,re,sys
want={'v1.24':'4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b','v1.25':'4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f','v1.26':'733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9','v1.27':'bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52'}
lines=open('ROADMAP.md').read().split('\n')
got={}
for i,l in enumerate(lines[:45]):
    m=re.match(r'^- \S+ \*\*(v1\.\d+)\b',l)
    if m and m.group(1) in want: got[m.group(1)]=(i+1,hashlib.sha256((l+'\n').encode()).hexdigest())
bad=[k for k in want if k not in got or got[k][1]!=want[k]]
for k in sorted(want): print(k,'line',got.get(k,('?','?'))[0],'MATCH' if k not in bad else 'DRIFT')
sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0`: all four after-hashes equal the four before-hashes recorded in Task
      1, so the four entries are byte-unchanged.
    - `130-04-SUMMARY.md` records a before/after table with, per token, the before line number, the
      after line number, the single shared SHA-256, and a `byte-unchanged` verdict.
    - `130-04-SUMMARY.md` reproduces the output of `git -C /workspaces diff -U0 -- .planning/ROADMAP.md`
      and states that no hunk overlaps the four entries, citing the hash equality as the evidence.
    - `130-04-SUMMARY.md` records D-16's reason for building no checker, in full, including that this is
      the milestone's single deliberate exception to BASE-08.
    - `130-04-SUMMARY.md` names the two non-violations a reader will see in the diff: the untouched
      v1.30 back-reference deferred to plan 130-05, and the line-count drop from collapsing two entries
      into one.
    - `ls .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/` lists no new checker
      script for this claim — D-16 forbids one.
  </acceptance_criteria>
  <done>D-16's one-shot proof is complete and recorded in a form plan 130-16 can lift verbatim, with the reason no checker exists recorded alongside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ROADMAP.md` `## Milestones` -> `/gsd-new-milestone` scope seeding | The next milestone's scope is seeded by reading these entries. A stale prior-art paragraph crossing this boundary becomes work nobody re-verified. |
| a renumber -> every by-number cross-reference in the tree | Renumbering a slot silently breaks pointers held in backlog stubs, other entries and notes. |
| an editing agent -> 2476 lines of record it is not reading | A whole-file write, or an unscoped edit, can destroy entries outside the diff window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-17 | Tampering | the v1.24–v1.27 entries | high | mitigate | Four named before-hashes are pinned in `must_haves.prohibitions` and re-verified in Task 3; the proof is keyed on version token so it survives the line shift. A drift halts the plan before the edit. |
| T-130-18 | Repudiation / false assurance | the stale prior-art paragraph | high | mitigate | The paragraph is removed with the entry it lives in, not left behind a marker — the option the owning todo explicitly warns against, since a scoping pass reads the body regardless of the marker. Task 2's criteria grep the list region for `2c2ed10` and `603 additions` at zero. |
| T-130-19 | Denial of service (self-inflicted) | `ROADMAP.md` outside the list | high | mitigate | Scoped `Edit` only, never a whole-file `Write`; an acceptance criterion requires `git diff -U0` hunks to stay inside the former line range 27-35. |
| T-130-20 | Tampering | broken by-number cross-references | medium | mitigate | The renumber is deliberately confined to this plan and the pointer repairs to plan 130-05, which runs in the next wave against the same file, so no wave has two writers. v1.30's now-false back-reference is named here and owned there rather than silently left. |
| T-130-21 | Repudiation | a symmetric claim about two asymmetric slots | medium | mitigate | C-12: the retirement line records that only the v1.29 slot carried a supersession marker; no assertion in this plan claims both did. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package and runs only `python3`, `git` and `grep`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- The `## Milestones` version tokens are in strictly ascending order with no duplicate.
- Exactly one `✅ **v1.23 PY32F071 Integration**` entry naming Phases 123–130.
- `v1.28 Binary Command Protocol` present, immediately after v1.27; `v1.23 Binary Command Protocol` absent.
- One dated retirement line replacing the two py32 slots; `2c2ed10` and `603 additions` absent from the list region.
- All four v1.24–v1.27 entry hashes equal their recorded before-values.
- `git diff -U0 -- .planning/ROADMAP.md` hunks confined to the former list range.
- `cd .planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py --explain` re-run and its record count for `third-stack-2c2ed10` compared against plan 130-02's baseline, to show the deletion moved the needle count in the expected direction.
</verification>

<success_criteria>
- D-13, D-14 and D-16 are landed and proven.
- CONSTRAINT 11 is honoured: this plan precedes every CLOSE-01 ROADMAP edit.
- The propagation hazard is removed at source; the owning todo's subject no longer exists in the list.
- The four untouched entries are proven byte-unchanged by hash, not by eye.
- No checker was built for the one-shot claim, and the reason is recorded.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-04-SUMMARY.md` when done.
</output>