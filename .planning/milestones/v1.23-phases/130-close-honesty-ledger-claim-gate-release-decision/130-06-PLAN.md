---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 06
type: execute
wave: 3
depends_on: ["130-02", "130-05"]
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "RESEARCH C-9's third live no-VTOR site is closed: ROADMAP.md:2414, Phase 129's criterion 3, no longer asserts a vector-relocation implication for a part with no VTOR. It was recorded AMENDED in 129-NONREGRESSION.md but the ROADMAP line itself was never touched."
    - "RESEARCH C-8 is closed: ROADMAP.md:2468 — Phase 130's own criterion 1, which quotes three of the CLOSE-01 checker's needles because it defines them — carries an inline recordscan:allow marker with a stated reason, so the checker can go green on an honest tree without its phrase table being weakened."
    - "Every remaining unlabeled needle hit in ROADMAP.md is closed, by correction where the statement is a false current fact and by a reasoned marker where it is a preserved record."
    - "check_record_corrections.py runs green in DEFAULT mode across all five planning files after this plan, so CLOSE-01 is mechanically proven."
    - "D-16 still holds: the four v1.24–v1.27 entries are byte-unchanged against the SHA-256 values plan 130-04 recorded."
  artifacts:
    - .planning/ROADMAP.md
  key_links:
    - "ROADMAP.md:2414 -> 129-RESEARCH.md C-1 and 129-NONREGRESSION.md §4 Criterion 3 (AMENDED)"
    - "ROADMAP.md:2468 -> an inline recordscan:allow marker, the C-8 self-reference exemption made explicit on one named line"
    - "the default-mode checker run -> CLOSE-01's mechanical proof, cited by plan 130-16"
  prohibitions:
    - "Do NOT weaken, narrow or delete a needle in check_record_corrections.py to make ROADMAP.md green. RESEARCH C-8 names that as the fail-open outcome BASE-08 exists to prevent."
    - "Do NOT region-exempt the whole success-criteria block. The C-8 exemption is an inline marker with a stated reason on one named line, so a future auditor sees exactly which line is forgiven and why."
    - "Do NOT change one byte of the v1.24, v1.25, v1.26 or v1.27 entries. Their SHA-256 values are recorded in plan 130-04 and re-verified here."
    - "Do NOT re-edit the ## Milestones list, the backlog stubs, or the dated review-history paragraphs. Plans 130-04 and 130-05 own those; this plan owns only the CLOSE-01 prose sites."
    - "Do NOT rewrite Phase 129's criterion 3 out of existence. It is a delivered phase's success criterion; the correction states the true fact and preserves what was asserted, matching how 129-NONREGRESSION.md recorded it AMENDED."
    - "Do NOT use a whole-file Write on ROADMAP.md. It is roughly 2470 lines; use scoped Edit operations."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Close CLOSE-01's remaining ROADMAP sites — the ones plans 130-04 and 130-05 did not discharge by
deletion — and land the C-8 self-reference exemption that lets the checker go green on an honest tree.

Two sites carry the weight:

- **`ROADMAP.md:2414`** is Phase 129's criterion 3 and reads *"a stated vector-relocation implication for
  a part with no VTOR"*, bare and unlabeled. `STATE.md:90`'s own C-1 row enumerates the sites and names
  this one; `129-NONREGRESSION.md` recorded criterion 3 **AMENDED**, but the ROADMAP line itself was never
  touched. D-06's framing — *"two VTOR clauses", "widens the file list by one"* — would lead a planner to
  fix only the `REQUIREMENTS.md` pair and leave a delivered phase's criterion asserting a disproven fact.

- **`ROADMAP.md:2468`** is **Phase 130's own criterion 1** and reads *"verified by grepping for each
  specific superseded figure/claim (e.g. "2992 B", py32 buffer "1024", "27 commits behind")"*. A
  phrase-table checker fires on **three** of its own needles in that one line, because the line is where
  the needles come from. Without an explicit exemption the checker can never pass, and the tempting
  response — narrowing the table — is precisely the fail-open outcome BASE-08 exists to prevent.

This is the last plan before the closing artifacts, so it is also where the **default-mode** checker run
must go green across all five planning files. Every plan before it verified one file with the env seam;
this one proves the whole set.

Purpose: finish CLOSE-01 and make its proof a single re-runnable command.
Output: a corrected criterion 3, one reasoned inline marker on criterion 1, any residual sites closed, and
a green default-mode run.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-02-SUMMARY.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md
</context>

## Artifacts this phase produces

Changed by **this** plan: `.planning/ROADMAP.md` — the Phase 129 and Phase 130 success-criteria regions
and any residual CLOSE-01 prose site the checker still flags. No new file.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, and
the two `usb_cdc.c` `#define` values.

<tasks>

<task type="auto">
  <name>Task 1: Correct Phase 129's criterion 3 and add the C-8 self-reference marker to Phase 130's criterion 1</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 2405-2420 — Phase 129's five success criteria. Criterion 3 is the
      flash-budget one carrying *"a stated vector-relocation implication for a part with no VTOR"*.
    - `.planning/ROADMAP.md` lines 2461-2476 — Phase 130's block. Criterion 1 quotes `"2992 B"`, py32
      buffer `"1024"` and `"27 commits behind"`; criterion 4 names a `13X-DECISION.md` artifact.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` §4
      Criterion 3 — the AMENDED record whose account this correction closes out. Read it so the ROADMAP
      correction agrees with it rather than inventing a second account.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md`
      §"Corrections to CONTEXT.md" C-1 — the evidence.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` §1.6 and §4(d) — the corrected migration-cost statement.
    - `check_record_corrections.py` — the exact inline marker grammar, including that a marker with no
      stated reason does not exempt.
    - `130-02-SUMMARY.md` and `130-05-SUMMARY.md` — the machine worklist, filtered to `ROADMAP.md` rows
      still open after plans 130-04 and 130-05. Act on that list.
  </read_first>
  <action>
Two edits, both scoped, both on named lines.

**(1) Correct Phase 129's criterion 3.** Replace the *"for a part with no VTOR"* clause with the corrected
fact and preserve what was asserted. The corrected substance, from `129-RESEARCH.md` C-1 and the record's
§1.6/§4(d): the PY32F071 **has** a VTOR (`__VTOR_PRESENT 1` in the pinned SDK's CMSIS header, and the
firmware already writes `SCB->VTOR` at every boot), so the vector-table move is cheap and the **fleet
re-flash** is the real migration cost. Add a short bracketed note recording that this criterion was
recorded **AMENDED** in `129-NONREGRESSION.md` §4 Criterion 3 and that the ROADMAP line itself is
corrected here by v1.23 Phase 130 CLOSE-01 — which is the gap C-9 found: the amendment was recorded and
the line was not touched.

Keep the criterion's other substance — that it cites the actual reserved addresses and sizes from Phase
126's linker symbols rather than a pre-Phase-126 estimate — unchanged. A delivered phase's criterion is
being corrected, not rewritten out of existence.

**(2) Add the C-8 self-reference marker to Phase 130's criterion 1.** Append an inline
`recordscan:allow` marker to that single line. The stated reason must say, in substance: this line is the
**definition** of the CLOSE-01 checker's needle table, so the three figures it quotes are needles rather
than claims; exempting the line by an explicit marker with a stated reason is the alternative to
narrowing the phrase table, which would be the fail-open outcome BASE-08 exists to prevent. Reference
`130-RESEARCH.md` C-8.

Mark **only that line**. Do not region-exempt the criteria block: a marker on one named line is auditable,
a region rule silently forgives whatever else lands in it.

**(3) Close any residual site the machine list still shows.** For each remaining `unlabeled` record in
`ROADMAP.md`, apply the disposition recorded in `130-02-SUMMARY.md`: correct the prose where the statement
is a false current fact, or add a reasoned marker where it is a preserved record. Do not touch the
`## Milestones` list, the backlog stubs, or the dated review-history paragraphs — plans 130-04 and 130-05
own those, and a site there means one of those plans left work behind, which is a finding to record rather
than to fix here.

Note in passing, without editing it: criterion 4 names the artifact `13X-DECISION.md`. That was a
placeholder in the roadmap's phase template; the real artifact is `130-DECISION.md` and plan 130-13
creates it. Record the observation in the SUMMARY so plan 130-16's criterion-discharge section can address
the naming rather than leaving a reader to wonder whether a differently-named file was expected.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
s=open('ROADMAP.md').read()
lines=s.split('\n')
c3=[l for l in lines if 'vector-relocation implication' in l]
assert len(c3)==1, c3
assert 'no VTOR' not in c3[0], c3[0][:160]
c1=[l for l in lines if 'grepping for each specific superseded' in l]
assert len(c1)==1, c1
assert 'recordscan:allow' in c1[0], c1[0][-160:]
print('OK criterion 3 corrected, criterion 1 marked')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one line contains `vector-relocation implication`, and it no longer contains `no VTOR`; it
      states the corrected fact naming `__VTOR_PRESENT` or `SCB->VTOR`, names the fleet re-flash as the
      real migration cost, and cites `129-NONREGRESSION.md` §4 Criterion 3.
    - Exactly one line contains `grepping for each specific superseded`, and it carries a
      `recordscan:allow` marker with a stated reason referencing `130-RESEARCH.md` C-8.
    - `git -C /workspaces diff -- .planning/ROADMAP.md` shows the criterion-1 line's only change is the
      appended HTML comment — the criterion's own text is byte-unchanged.
    - No `recordscan:` marker appears on any Phase 130 criterion line other than criterion 1, and no
      region-level exemption construct was added.
    - `git -C /workspaces diff -U0 -- .planning/ROADMAP.md` shows no hunk in the `## Milestones` list
      region, none in the `999.2x` backlog stub regions, and none at the dated review-pass paragraphs.
    - `130-06-SUMMARY.md` records the `13X-DECISION.md` placeholder observation and hands it to plan
      130-16.
    - `python3 -c "import hashlib,re; ..."` re-verification of the four v1.24–v1.27 entry SHA-256 values
      against plan 130-04's record passes; record the four MATCH verdicts in the SUMMARY.
  </acceptance_criteria>
  <done>The last live no-VTOR prose site is corrected, the self-reference exemption is a single reasoned marker rather than a region rule, and the ROADMAP's own edits stayed inside this plan's scope.</done>
</task>

<task type="auto">
  <name>Task 2: Prove CLOSE-01 mechanically — a green default-mode run across all five planning files</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-06-SUMMARY.md</files>
  <read_first>
    - `check_record_corrections.py` and `test_check_record_corrections.py` — the default target set and
      the fixture suite.
    - `130-02-SUMMARY.md` — the expected RED-to-GREEN transition and the exit-code prediction: `1` at plan
      130-02, `0` after the last of plans 130-06..130-10.
    - `130-05-SUMMARY.md`, `130-07-SUMMARY.md`, `130-08-SUMMARY.md`, `130-09-SUMMARY.md`,
      `130-10-SUMMARY.md` — the per-file green results, so this task can state which plan closed which
      file rather than asserting a collective green.
  </read_first>
  <action>
Run the checker in **default mode** — no argv, no env seam — so it resolves its own five default targets,
and record the result. This is the run that proves CLOSE-01: one command, five files, exit 0.

Record in `130-06-SUMMARY.md`:
1. The exact command and its exit code, and the full `PASS:` line naming all five scanned files.
2. The `--explain` per-verdict tally, so a reader sees how many hits are exempt and by which mechanism —
   a green run with zero total hits would mean the needles stopped matching, which is a different and
   worse outcome than a green run with many exempt hits.
3. A per-file attribution table: which plan closed which file, citing that plan's SUMMARY.
4. The fixture suite's result, re-run here, so the green default run is accompanied by live proof that the
   checker can still fail.
5. The exit-code transition against plan 130-02's prediction: RED at 130-02, GREEN here. If the prediction
   was wrong in either direction, record why.

If the default-mode run is **not** green, do not weaken a needle and do not add a blanket exemption. Read
the remaining records, apply the recorded disposition for each, and if a record falls outside this plan's
scope — a `## Milestones`, backlog-stub or review-history site — record it as a finding that plan 130-04 or
130-05 left work behind, and close it here with a scoped edit while naming the plan that should have.

Also re-run the sibling claim gate's suite and the claim gate itself, and record both: the claim gate must
still print `UNARMED:` and exit 0 at this point, because none of the four contracted artifacts exists yet.
That is the correct pre-artifact state now that plan 130-01 repointed it, and recording it here gives plan
130-11 a baseline to show the transition from.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && echo "=== default mode ===" && python3 check_record_corrections.py; echo "record_exit=$?"; echo "=== tally ===" && python3 check_record_corrections.py --explain | tail -12; echo "=== fixtures ===" && python3 -m pytest test_check_record_corrections.py -q 2>&1 | tail -2; echo "=== claim gate ===" && cd ../123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py; echo "claim_exit=$?"; python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cd .planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py`
      exits `0` and prints a single `PASS:` line naming all five default targets.
    - `--explain` reports **zero** records with verdict `unlabeled` and a **non-zero** total record count —
      a green run with zero total hits is a failure of this criterion, because it would mean the needles
      stopped matching rather than being exempt.
    - The `--explain` tally shows at least one record for each of the verdicts `block`, `inline-history` and
      `inline-allow`, proving all three exemption paths are exercised by the real tree.
    - `python3 -m pytest test_check_record_corrections.py -q` reports `0 failed` — the checker is still
      able to fail, proven live alongside the green run.
    - `cd ../123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py` exits `0` and
      prints `UNARMED:`, and `python3 -m pytest test_check_permitted_claims.py -q` reports `11 passed`.
      Both are recorded as plan 130-11's baseline.
    - `130-06-SUMMARY.md` carries the per-file attribution table with five rows, each citing the closing
      plan's SUMMARY.
    - `130-06-SUMMARY.md` records the exit-code transition against plan 130-02's prediction and explains any
      divergence.
    - The four v1.24–v1.27 SHA-256 values still match plan 130-04's record.
  </acceptance_criteria>
  <done>CLOSE-01 has a single re-runnable green command over five files, accompanied by live proof the checker can still fail and by an attribution table showing which plan closed which file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the needle table -> the line that defines it | `ROADMAP.md:2468` is both the definition and a match. The exemption granted there is the checker making a statement about itself. |
| a green gate -> a reader's confidence | A gate that goes green because its needles stopped matching is indistinguishable, at the exit code, from one that goes green because the record is honest. |
| this plan -> plans 130-04 and 130-05's regions | Three plans write the same 2470-line file in three consecutive waves. An out-of-scope edit here silently overwrites a prior plan's reviewed work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-50 | Repudiation / false assurance | the needle table under pressure to go green | critical | mitigate | Weakening a needle is prohibited in `must_haves.prohibitions`; the green criterion requires a **non-zero total** record count and at least one record per exemption verdict, so a green-by-non-matching outcome fails the criterion. The fixture suite is re-run alongside the green run as live proof the checker can still fail. |
| T-130-51 | Spoofing | `ROADMAP.md:2414`'s disproven fact | high | mitigate | Corrected in prose with the amendment account cited; this is C-9's third site, the one D-06's framing would have left behind. |
| T-130-52 | Tampering | the self-reference exemption's blast radius | high | mitigate | One inline marker with a stated reason on one named line; region-level exemption is prohibited, and a criterion asserts no other Phase 130 criterion line carries a marker. |
| T-130-53 | Denial of service (self-inflicted) | plans 130-04 and 130-05's regions | high | mitigate | Scoped `Edit` only; a `git diff -U0` criterion forbids hunks in the `## Milestones` list, the backlog stubs and the review-history paragraphs. A site found there is recorded as a finding naming the plan that should have closed it. |
| T-130-54 | Tampering | the v1.24–v1.27 entries | high | mitigate | Re-verified against plan 130-04's four SHA-256 values as a criterion of both tasks. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package and runs only `python3`, `git`, `pytest` and `grep`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `python3 check_record_corrections.py` in default mode → exit 0, one `PASS:` line naming five files.
- `--explain` → zero `unlabeled`, non-zero total, at least one each of `block`, `inline-history`, `inline-allow`.
- `python3 -m pytest test_check_record_corrections.py -q` → `0 failed`.
- `python3 check_permitted_claims.py` → exit 0 with `UNARMED:`; `python3 -m pytest test_check_permitted_claims.py -q` → `11 passed`.
- Exactly one `vector-relocation implication` line, without `no VTOR`; exactly one `grepping for each specific superseded` line, carrying a reasoned `recordscan:allow` marker.
- `git diff -U0 -- .planning/ROADMAP.md` → no hunk in the list, stub or review-history regions.
- The four v1.24–v1.27 hashes match plan 130-04's record.
</verification>

<success_criteria>
- C-9's third live no-VTOR site and C-8's self-reference trap are both closed, the latter by an auditable single-line marker.
- CLOSE-01 is mechanically proven by one default-mode command over five files.
- The green run is accompanied by evidence it is green for the right reason: a non-zero exempt-hit count and a passing fixture suite.
- The claim gate's pre-artifact `UNARMED:` state is recorded as plan 130-11's baseline.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-06-SUMMARY.md` when done.
</output>
