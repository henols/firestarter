---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 13
type: execute
wave: 6
depends_on: ["130-12"]
files_modified:
  - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md
autonomous: true
requirements: [CLOSE-04]

must_haves:
  truths:
    - "CONSTRAINT 1 and CLOSE-04's literal text are satisfied: 130-DECISION.md is committed before any push to beta, and this file's own commit timestamp is the evidence that the decision preceded the act. v1.21's close skipped exactly this step and auto-cut a stray 3.0.0b12 which is still public."
    - "D-01 is recorded with all three dispositions: ACCEPT chosen, AVOID declined, CLEANUP declined, each with its reason, following 122-DECISION.md's three-option table."
    - "Every pre-flight figure is re-measured live in this session and carries an AGREES or DRIFTED verdict against 130-RESEARCH.md's recorded value — nothing is copied from RESEARCH."
    - "CONSTRAINT 9 is satisfied: every gate is green on the exact tree that will be merged, and the measured suite states are recorded as gates rather than as assumptions (RESEARCH C-13)."
    - "The accepted sequence names, for each step, its owning plan and the CONTEXT constraint it satisfies — and states which steps are the operator's."
    - "D-17's ship-gate reasoning and research assumption A3 are both recorded explicitly rather than left implicit."
    - "The claim gate's default-mode run goes GREEN when this file lands: all four contracted artifacts now exist."
    - "An explicit no-mutation section records that nothing was pushed, merged, tagged, dispatched or published by this plan or this file."
  artifacts:
    - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md
  key_links:
    - "130-DECISION.md's commit timestamp -> the evidence that the decision preceded the push (CLOSE-04, CONSTRAINT 1)"
    - "the pre-flight suite states -> the app's beta-release.yml blocking steps, which gate the cut before the version bump (RESEARCH C-13)"
    - "the accepted sequence -> plan 130-14's operator procedure, which is where the privileged commands live"
  prohibitions:
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload. This plan is read-only against every remote. Its whole value is that it precedes the act."
    - "Do NOT put a 3.0.0b15 literal in any command written for verbatim execution. The tag is derivable but must be READ from gh release list after the cut (CONSTRAINT 5, RESEARCH C-15). Use an <observed tag> placeholder in every command, the way 122-DECISION.md's step 5 does."
    - "Do NOT copy a figure from 130-RESEARCH.md's live-state table without re-measuring it. That table is stamped 2026-08-02 and explicitly valid only until branch tips or tag ceilings move."
    - "Do NOT invent a merge-conflict probe or an --ours superset proof. Both sub-repos measured 0 behind origin/beta, so 122-DECISION.md's items 7 and 8 have no subject here — record them as no-ops rather than manufacturing an artifact."
    - "Do NOT pin the meta gitlinks unchanged. D-04 ASSERTS the gitlinks match the milestone-branch tips and re-bumps only if this phase's own commits moved a tip, which honours this milestone's in-phase gitlink practice (Phases 125/128/129) rather than v1.22's pinned model."
    - "Do NOT iterate .planning/config.json's planning.sub_repos. It lists FOUR repos and all four directories exist; two are py32 scratch worktrees that are not deliverables. Iterate firestarter and firestarter_app explicitly by name."
    - "Do NOT read firestarter_app's pre-existing working-tree dirt as this phase's damage. A modified .gitignore plus untracked .coverage, .planning/config.json, SECURITY.md and write_test_port.sh predate this phase, and meta shows a modified firestarter_app gitlink. The firestarter tree was clean at plan time."
    - "Do NOT state or imply the ship gate in §5(c) is satisfied, amended or resolved (D-17)."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
---

<objective>
Record the `beta`-push decision, with its pre-flight evidence, **before any push** — the literal text of
CLOSE-04, and the step v1.21's close skipped, auto-cutting a stray `3.0.0b12` that is still public.

This is the fourth and last of the contracted closing artifacts, so its landing is also the moment the claim
gate goes fully armed and must pass. And it is the last plan before the operator acts, so it is where every
gate must be green **on the exact tree that gets merged** (CONSTRAINT 9).

RESEARCH C-13 makes the app's pre-flight suite a gate rather than a note: `beta-release.yml` runs
`pip install -e .[test]`, the catalog validity check, the `messages.py` codegen drift gate and **`pytest tests/
-v`** — all blocking, all **before** the version bump and the release step. A RED there means **no app b15 at
all**, which breaks D-01's lockstep pairing *after* the firmware half has already published. That asymmetry is
the single worst outcome available in this phase, so the pre-flight state is measured and recorded here, the way
`122-DECISION.md` records branch tips.

Purpose: make the decision an act of record that precedes the act, and give the operator a measured basis for
it.
Output: `130-DECISION.md`, and a green default-mode claim gate.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-LEDGER.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
</context>

## Artifacts this phase produces

Created by **this** plan: `130-DECISION.md` — the fourth and final contracted artifact.

Already produced: `130-LEDGER.md`, `130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`,
`check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, the two `usb_cdc.c` `#define`
values. Still to come: `130-HANDOFF.md` (130-14), `130-CHANNELS.md` (130-15), `130-NONREGRESSION.md` (130-16).

<tasks>

<task type="auto">
  <name>Task 1: Re-measure every pre-flight figure live and run every gate on the tree that will be merged</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` — lines 1-80
      and 226-275. The `**Purpose:**` paragraph at 3-6 is the load-bearing decision-precedes-act framing; lines
      12-19 plus the item-1 table are the AGREES/DRIFTED pattern; the numbered pre-flight sections 1-10 are the
      shape to follow; the explicit `### No mutation occurred` section is mandatory.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md`
      §"Live State Re-Verification (2026-08-02, this session)" — the full table of recorded values to verdict
      against, including the four new rows research added.
    - `130-RESEARCH.md` §"C-13" and §"C-14" — the app's blocking pre-cut steps with their measured values, and
      the `paths-ignore` analysis showing both pushes will trigger (112 non-ignored changed files in firmware,
      32 in the app).
    - `130-RESEARCH.md` §"C-15" — why b15 is deterministically derivable and why CONSTRAINT 5 nonetheless
      stands.
    - `130-PATTERNS.md` §`130-DECISION.md` — the skeleton heading list, the verbatim purpose paragraph, the
      AGREES/DRIFTED table shape, and the notes that items 7 and 8 have no subject here and that item 9's
      meaning changes because D-04 asserts rather than pins.
    - `firestarter_app/.github/workflows/beta-release.yml` and `publish.yml` — the blocking step order and the
      required `tag` input.
    - `firestarter/.github/workflows/beta-build.yml` and `py32f071.yml` — the `paths-ignore` sets, the
      `continue-on-error` call site, and the LOUD gate's unconditional `push: branches: [beta]`.
  </read_first>
  <action>
Write the pre-flight half of `130-DECISION.md`. Every number in it is measured in this session with the command
shown, and carries an **AGREES** or **⚠ DRIFTED** verdict against `130-RESEARCH.md`'s recorded value. Nothing
is copied.

Open with the purpose paragraph, in the shape `122-DECISION.md:3-6` uses: CLOSE-04's text is literal, the
accept/avoid/cleanup decision must be made and recorded **before any push**, v1.21's close skipped exactly this
step and auto-cut a stray `3.0.0b12`, and this artifact's own commit timestamp is the evidence that the decision
preceded the act — nothing is pushed, merged, or published by this plan or this file.

Then a statement of what commands were run: fetch-only plus read-only inspection. Name them.

**Numbered pre-flight sections.** Follow `122-DECISION.md`'s numbering, adapting where this phase differs:
1. **Branch tips** — `firestarter` and `firestarter_app` HEAD SHAs and branch names. Recorded values:
   `v1.23-py32f071-integration` in both, `5a89ee7` and `cc9452f`. Both are expected to have **moved** because
   plan 130-03 commits inside `firestarter`; record the movement and its cause rather than flagging it as drift
   in the worrying sense.
2. **`origin/beta` tips** after `git fetch origin` in each repo. Recorded: `5c9160a` and `e7d3ee8`.
3. **Ahead/behind counts.** Recorded: 0 behind / 83 ahead and 0 behind / 37 ahead. Zero behind in both is what
   makes the outbound merge clean and is why there is no inbound catch-up to plan.
4. **Local `beta` versus `origin/beta`.** Research measured 0/0 in both. Record it, and record the standing
   hazard: after CI's version-bump auto-commit, local `beta` is one commit behind the remote, so any later
   local operation touching `beta` must fetch first.
5. **Version strings on both sides.**
6. **The highest `3.0.0b*` tag in both repos, and the absence of the next one.** Recorded ceiling: `3.0.0b14` in
   both. State that the auto-increment target is derivable — `is_beta_mode()` returns true on a push to `beta`,
   `compute_beta_version` falls back to a git tag scan of `3.0.0b*` and takes max plus one, and `checkout` uses
   `fetch-depth: 0` so all tags are present — **and** that CONSTRAINT 5 still stands, because a concurrent cut,
   an out-of-band tag or a rehearsal-tag collision would change it. Do not write the derived literal into any
   command.
7. **No-op: merge-conflict probe.** Both repos are 0 behind, so `122-DECISION.md`'s `merge-tree` probe has no
   subject. Record as a no-op with the reason.
8. **No-op: `--ours` superset proof.** Same reason. Record as a no-op.
9. **Meta gitlink assertion (D-04).** `git ls-tree HEAD` for both submodule paths, compared against the working
   tips. D-04 **asserts** they match and re-bumps only if this phase's own commits moved a tip; plan 130-03 did
   move the firmware tip, so record the expected bump and name plan 130-16 as its owner. Do **not** pin.
10. **Working-tree dirt, all three repos**, via `git status --porcelain`. Record `firestarter_app`'s
    pre-existing dirt explicitly — modified `.gitignore`, untracked `.coverage`, `.planning/config.json`,
    `SECURITY.md`, `write_test_port.sh` — and that meta shows a modified `firestarter_app` gitlink, so a later
    cleanliness assertion does not read pre-existing dirt as this phase's damage. Note that the firmware
    sync-gate module has a `test_dirty_tree_is_detected` leg.
11. **`paths-ignore` will not suppress either cut.** Measure the non-ignored changed file counts against
    `origin/beta` for both repos (research measured 112 and 32) and record that
    `firestarter/.github/workflows/py32f071.yml` carries `push: branches: [beta]` with **no** paths filter, so
    the LOUD ARM gate always fires. Worth recording because a documentation-only close would silently cut
    nothing while looking successful.
12. **The gates, run on this exact tree (CONSTRAINT 9).** All of the following, each with its measured result:
    `firestarter` `python3 -m pytest tests/ -q` (recorded 221 passed); `firestarter` `pio test -e native` at the
    BASE-01 case and suite counts; `firestarter`
    `FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q` (41 legs);
    `firestarter_app` `python3 -m pytest tests/ -q` (recorded 1303 passed); `firestarter_app`
    `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check`; `firestarter_app`
    `git diff --exit-code firestarter/messages.py`; the CLOSE-01 checker in default mode; its fixture suite; the
    claim gate in default mode; and the claim gate's suite. Record the app suite state as the **gate** C-13 says
    it is, and note the environment difference: local runs are on Python 3.12 while CI pins 3.11 (research
    assumption A2, MEDIUM risk). If any gate is RED, stop and record it — a RED here means the operator must not
    push.
  </action>
  <verify>
    <automated>cd /workspaces && git -C firestarter fetch origin --quiet; git -C firestarter_app fetch origin --quiet; echo "fw HEAD=$(git -C firestarter rev-parse --short HEAD) branch=$(git -C firestarter rev-parse --abbrev-ref HEAD)"; echo "fw beta=$(git -C firestarter rev-parse --short origin/beta) behind_ahead=$(git -C firestarter rev-list --left-right --count origin/beta...HEAD)"; echo "app HEAD=$(git -C firestarter_app rev-parse --short HEAD) branch=$(git -C firestarter_app rev-parse --abbrev-ref HEAD)"; echo "app beta=$(git -C firestarter_app rev-parse --short origin/beta) behind_ahead=$(git -C firestarter_app rev-list --left-right --count origin/beta...HEAD)"; echo "tag ceilings:"; git -C firestarter tag --list '3.0.0b*' | sort -V | tail -1; git -C firestarter_app tag --list '3.0.0b*' | sort -V | tail -1; echo "gitlinks:"; git ls-tree HEAD firestarter firestarter_app; echo "=== gates ==="; (cd firestarter && python3 -m pytest tests/ -q 2>&1 | tail -1); (cd firestarter && FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1); (cd firestarter_app && python3 -m pytest tests/ -q 2>&1 | tail -1); (cd .planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py; echo "record_exit=$?")</automated>
  </verify>
  <acceptance_criteria>
    - `130-DECISION.md` carries the purpose paragraph stating that the decision precedes the act, naming v1.21's
      stray `3.0.0b12`, and stating that nothing is pushed, merged or published by this plan or this file.
    - Twelve numbered pre-flight sections exist, each with a measured value, the command that produced it, and an
      **AGREES** or **⚠ DRIFTED** verdict against `130-RESEARCH.md`'s recorded value.
    - Sections 7 and 8 are recorded as **no-ops** with the zero-behind reason, not as manufactured artifacts.
    - Section 9 records the gitlink comparison and states that D-04 asserts rather than pins, naming plan 130-16
      as the owner of any bump this phase's own commits require.
    - Section 10 lists `firestarter_app`'s pre-existing dirt item by item and states it predates this phase.
    - Section 11 records both non-ignored changed-file counts and the LOUD gate's unfiltered `push` trigger.
    - Section 12 records every one of the ten named gates with its measured result. Any RED is recorded together
      with an explicit statement that the operator must not push until it is green.
    - `grep -c '3.0.0b15' 130-DECISION.md` returns `0`.
    - No command in the file that is written for verbatim execution contains a `3.0.0b` literal; every one uses an
      `<observed tag>` placeholder.
    - `git -C /workspaces log --oneline -1` after this task shows a commit touching only `130-DECISION.md` — and
      `git -C firestarter rev-parse origin/beta` still equals the value recorded in section 2, proving no push
      occurred.
  </acceptance_criteria>
  <done>Every pre-flight figure is re-measured with a verdict, every gate is run on the tree that will be merged, and the no-ops are recorded as no-ops.</done>
</task>

<task type="auto">
  <name>Task 2: Record the decision itself, the accepted sequence, D-17's reasoning, and the no-mutation section</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` lines 226-275 —
      the three-option accept/avoid/cleanup table with its verbatim CHOSEN/DECLINED dispositions, and the
      accepted-sequence numbered list where each item reads `**<action>** (plan 122-0N) — constraint N`.
    - `130-DECISION.md` as written by Task 1 — the pre-flight half this task completes.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` §5(b), §5(c) and §5(e) — the ship gate verbatim, the
      private-testing-id characterisation, and the LOW-confidence allocation assessment. These are the inputs to
      D-17's recorded reasoning.
    - `130-RESEARCH.md` §"Open Questions" item 1 and §"Assumptions Log" A3 — the two judgment calls this task
      records.
    - `130-RESEARCH.md` §"Security Domain" — the three privileged acts and why they are gated structurally.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-LEDGER.md` — the negative-space
      row for the §5(c) tension, which this file must be consistent with and expand on.
  </read_first>
  <action>
Write the decision half.

**The three options, all recorded.** A table with `Option | Disposition | Why`, following
`122-DECISION.md:238-243`:
- **ACCEPT — CHOSEN.** Both repos carry `on: push: branches: [beta]` with git-tag-scan auto-increment and both
  sit at a `3.0.0b14` ceiling, so the outbound `--no-ff` merge push cuts the next beta by itself. This is the
  first publication of `firestarter_py32f071.hex` as a real release asset, which is the only thing that makes
  the 21 already-landed host capabilities reachable; REL-01…04's present evidence is a rehearsal draft that has
  since been deleted. The two halves are lockstep — a firmware asset with no published host that can install it
  is inert, and vice versa. Risk is bounded: no PY32F071 board exists anywhere, and the py32 board is beta-only
  by construction.
- **AVOID — DECLINED.** A recorded no-push leaves the milestone's one user-visible deliverable permanently
  unexercised and makes the two required release-notes artifacts notes for nothing. State explicitly that no
  workflow trigger is edited by this phase and `paths-ignore` is not weakened in either repo.
- **CLEANUP — DECLINED.** The stray `3.0.0b12` prereleases stay public; declined at v1.22 D-05 because b12 has
  been public since 2026-07-27 and may already be installed, and cleaning it up would be an operator-driven
  outward-facing act, not close work. State explicitly that no force push, no history rewrite and no deletion of
  any published artifact occurs in this phase.
- Also record the fourth option that was considered and declined at D-01: **accept gated on a fresh
  `rehearsal=true` dispatch**, declined because run `30722352902` (SHA `7a0a375`, `conclusion: success`) already
  proved the asset publishes and is still readable, and re-deriving it costs an operator round-trip plus a draft
  cleanup.

**The accepted sequence**, numbered, each item naming its owning plan and the CONTEXT constraint it satisfies,
and each marked **agent** or **operator**:
1. This decision recorded and committed — plan 130-13, CONSTRAINT 1. Agent.
2. The `1209:0001` descriptor edit, its `[SHARED:S4]` §5(a)+§5(d) lockstep body update in both copies, and an
   ARM pass — plan 130-03, CONSTRAINT 2. Agent. Record the ARM pass's outcome as Task 1's section 12 measured it,
   including the LOUD-gate fallback if the local build could not complete.
3. `130-LEDGER.md` written before either release-notes draft — plan 130-11, CONSTRAINT 3. Agent.
4. Both bodies hand-written as committed drafts — plan 130-12, and the **blocking operator wording review**,
   CONSTRAINT 4. **Operator**, and it precedes step 5.
5. The outbound `--no-ff` merge of the milestone branch into `beta` and the push, in both sub-repos — plan
   130-14's written procedure. **Operator.** State plainly that no plan in this phase contains the command.
6. CI cuts the beta in both repos. Automatic. Record that the firmware `beta-build.yml` ARM steps are
   `continue-on-error: true` by design, so a green run is not evidence the py32 asset shipped, and that the LOUD
   `py32f071.yml` gate fires unconditionally and says what broke.
7. The observed cut tag read from `gh release list` in both repos — plan 130-15, CONSTRAINT 5. Agent, read-only.
   Never computed.
8. A manual `workflow_dispatch` of `firestarter_app`'s `publish.yml` with `tag=<observed tag>` — plan 130-14's
   procedure. **Operator**, CONSTRAINT 6. Record why it is manual: `publish.yml`'s own in-file comment states
   that when a release is created by another workflow using a PAT lacking `workflow` scope, GitHub suppresses the
   `release.published` event — and `3.0.0b12`'s absence from PyPI corroborates it. Six of thirteen published app
   betas never reached PyPI. Record that the `tag` input flows into `ref:`, so the value must be the observed tag
   and never an untrusted or computed one.
9. `firestarter_py32f071.hex` asserted present among the real b15 release's assets — plan 130-15, CONSTRAINT 8,
   D-03. Agent, read-only.
10. Both channels verified public, PyPI resolution checked from a clean temp env — plan 130-15, CONSTRAINT 7.
    Agent.
11. Both release bodies posted after the D-02 review — plan 130-14's procedure. **Operator.**
12. The closing sweep, the gitlink assertion and bump, and the CLOSE-01…04 ticks — plan 130-16, CONSTRAINT 10.
    Agent. State that plan 130-16 is the **only** plan permitted to tick a CLOSE id.
13. Out of scope for this phase: the `v1.23` annotated tag and any merge toward `main`, both of which stay with
    `/gsd-complete-milestone` (D-04); any stable release; deleting the stray b12 prereleases.

**Facts the sequence depends on.** A short list, each measured in Task 1: both repos 0 behind `origin/beta`; the
tag ceiling `3.0.0b14` in both; both pushes will trigger despite `paths-ignore`; the app's blocking pre-cut
steps are green on this tree.

**`## The §5(c) ship-gate residual (D-17)`.** Record the reasoning in full, because leaving it implicit is
precisely the shape D-11's third rejected option was rejected for. State: the gate reads that no board ships and
no release advertises a USB identity until a PID allocated under VID `0x1209` exists; `1209:0001` is the
registry's documented private-testing product id and is **not** an allocated PID; §5(c) says outright that it is
deliberately a condition rather than a warning **so a future reader can fail it**; therefore it is left
**byte-unchanged**, because amending a ship gate so that the act about to be taken clears it destroys the
mechanism — the same fail-open move BASE-08 exists to prevent. Then record why this phase does not read a
caveated disclosure of an explicitly non-allocated test id as *advertising an identity*, and state plainly that a
future reader may disagree, which the gate deliberately permits. Also record **research assumption A3** as a
judgment call: publishing a `.hex` carrying `1209:0001` is taken not to violate the registry's
must-not-be-used-on-a-redistributed-device clause because no device is redistributed. Note that the operator may
overrule either judgment at the D-02 review.

**`### No mutation occurred`.** Explicit and last. Name every class of act that did not happen: no push, no merge
into `beta`, no tag, no workflow dispatch, no release created, edited or deleted, no upload, no force push, no
history rewrite, no deletion of any published artifact. Record the read-only and fetch-only commands that were
run, and record `origin/beta`'s tip in both repos as unchanged from Task 1's section 2.

Finish with a `## Summary of what this artifact proves` section in `122-DECISION.md`'s shape.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 -c "
s=open('130-DECISION.md').read()
for k in ('ACCEPT','AVOID','CLEANUP','CHOSEN','DECLINED','No mutation occurred','D-17','A3','30722352902','publish.yml','<observed tag>'):
    assert k in s, k
assert '3.0.0b15' not in s, 'tag literal present'
import re
for m in re.finditer(r'3\.0\.0b1[0-9]', s):
    pass
print('OK decision structure')" && cd ../123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py; echo "claim_default_exit=$?"; python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py`
      **in default mode** exits `0` and prints a `PASS:` line naming all four contracted artifacts. This is the
      first fully-armed green run and it is CLOSE-02's mechanizable proof. Record the output verbatim.
    - `python3 -m pytest test_check_permitted_claims.py -q` reports `11 passed` with all four artifacts on disk —
      the final confirmation that plan 130-01's differential side-effect guard was the right fix.
    - The three-option table exists with `ACCEPT` marked `CHOSEN` and both `AVOID` and `CLEANUP` marked
      `DECLINED`, each with its stated reason. The fourth considered option, a fresh rehearsal dispatch, is
      recorded as declined with run `30722352902` cited.
    - The accepted sequence has thirteen numbered items; every one names an owning plan and, where applicable, the
      CONTEXT constraint number; every one is marked **agent** or **operator**.
    - Every operator step states explicitly that no plan in this phase contains the command.
    - The `§5(c)` residual section states the gate is left byte-unchanged, gives the destroys-the-mechanism
      reasoning, records why a caveated disclosure is not read as advertising, states that a future reader may
      disagree, and records research assumption A3.
    - `grep -ci 'ship gate.*\(satisfied\|amended\|resolved\)' 130-DECISION.md` returns `0`.
    - A `### No mutation occurred` section names all nine classes of act that did not happen and records
      `origin/beta`'s unchanged tip in both repos.
    - `grep -c '3.0.0b15' 130-DECISION.md` returns `0`, and every command written for verbatim execution uses an
      `<observed tag>` placeholder.
    - `git -C firestarter rev-parse origin/beta` and `git -C firestarter_app rev-parse origin/beta` are unchanged
      from Task 1's recorded values — no push happened.
    - `130-DECISION.md` is committed. `git -C /workspaces log --format=%cI -1 -- .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md`
      returns a timestamp, and that timestamp is recorded in `130-13-SUMMARY.md` as CLOSE-04's
      decision-preceded-the-act evidence.
  </acceptance_criteria>
  <done>The decision exists as a committed artifact with a timestamp that precedes any push, all four options are on the record, the sequence names its owners, D-17's and A3's judgments are explicit, and the claim gate is green at four of four.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a recorded decision -> the act it governs | CLOSE-04's whole mechanism is that the artifact's commit timestamp precedes the push. If the artifact lands after, it proves nothing; v1.21's close is the worked example. |
| a research figure -> a decision made on it | `130-RESEARCH.md`'s live-state table is stamped and explicitly perishable: branch tips and tag ceilings move the moment anyone pushes. |
| the app pre-cut suite -> the lockstep pair | A RED in the app suite means no app b15 **after** the firmware half published. The failure is asymmetric and the firmware asset is already public when it bites. |
| a computed tag -> `publish.yml`'s `ref:` | The `tag` input flows into a checkout ref. A value that was computed rather than observed is a checkout-of-arbitrary-ref vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-68 | Repudiation | the decision-precedes-act property | critical | mitigate | The artifact is committed by this plan, before plan 130-14 is reachable and before any operator step; a criterion records the commit timestamp and asserts `origin/beta` is unchanged in both repos. |
| T-130-69 | Elevation of privilege | the push, the dispatch, the release post | critical | mitigate | Structurally gated: this plan contains no privileged command. Every privileged step in the recorded sequence is marked **operator** and states that no plan contains the command. |
| T-130-70 | Tampering | an untrusted or computed `tag` reaching `publish.yml`'s `ref:` | high | mitigate | Every command written for verbatim execution uses an `<observed tag>` placeholder; a criterion greps `3.0.0b15` at zero. CONSTRAINT 5 is recorded as standing even though b15 is derivable. |
| T-130-71 | Repudiation / false assurance | a green CI tick read as a shipped asset | high | mitigate | The sequence records that the firmware ARM steps are `continue-on-error: true`, so green and shipped are independent, and hands the asset-presence assertion to plan 130-15 against the real release (D-03). |
| T-130-72 | Denial of service | an asymmetric lockstep break | high | mitigate | RESEARCH C-13: the app's blocking pre-cut steps are measured on this tree and recorded as a gate, with the Python 3.12-local versus 3.11-CI difference recorded as assumption A2. A RED stops the push. |
| T-130-73 | Repudiation | inherited rather than measured pre-flight figures | high | mitigate | Every one of twelve sections carries an AGREES/DRIFTED verdict against the recorded value, and `must_haves.prohibitions` forbid copying without re-measuring. |
| T-130-74 | Repudiation | the §5(c) judgment left implicit | high | mitigate | D-17's reasoning and research assumption A3 are both recorded in full, with an explicit statement that a future reader may decide differently and that the operator may overrule at the D-02 review. |
| T-130-75 | Spoofing | scratch worktrees mistaken for deliverables | medium | mitigate | `must_haves.prohibitions` forbid iterating `config.json`'s four-entry `sub_repos`; the two real sub-repos are named explicitly in every command. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package. It runs `git fetch`/read-only `git`, `pytest`, `python3` and no `gh` write. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `python3 check_permitted_claims.py` default mode → exit **0**, `PASS:` naming all four contracted artifacts.
- `python3 -m pytest test_check_permitted_claims.py -q` → `11 passed`.
- `python3 check_record_corrections.py` default mode → exit 0.
- `firestarter` `pytest tests/ -q` → 221 passed; `pio test -e native` → BASE-01 counts; sync gate → 41 passed.
- `firestarter_app` `pytest tests/ -q` → collected total recorded; codegen check and `messages.py` diff gate clean.
- Twelve pre-flight sections with AGREES/DRIFTED verdicts; sections 7 and 8 recorded as no-ops.
- Three-option table with one CHOSEN and two DECLINED, plus the rehearsal option declined with its run cited.
- `grep -c '3.0.0b15' 130-DECISION.md` → `0`.
- `git -C firestarter rev-parse origin/beta` and the app equivalent unchanged; a `### No mutation occurred` section present.
</verification>

<success_criteria>
- CLOSE-04's decision-before-push requirement is satisfied by a committed artifact with a recorded timestamp.
- All four options are on the record with reasons; the accepted sequence names owners and constraints.
- Every gate is green on the exact tree that will be merged, with the app suite recorded as a gate.
- D-17's and A3's judgments are explicit and overrulable.
- The claim gate is green at four of four; the arming mechanism has now been observed in all three states.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-13-SUMMARY.md` when done.
</output>
