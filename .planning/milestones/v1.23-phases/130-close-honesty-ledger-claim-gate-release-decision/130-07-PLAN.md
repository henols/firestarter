---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 07
type: execute
wave: 2
depends_on: ["130-02"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "D-05 lands for PROJECT.md: corrections arrive as labeled ⚠ CORRECTION blocks, in the same register as v1.22's eight, because /gsd-new-milestone reads this file to seed scope and a block warns a future reader where an in-place edit would not."
    - "R-2 is corrected in prose at PROJECT.md:59: the py32 DATA_BUFFER_SIZE is 512, not 1024 — a false current fact sitting inside a ⚠-labeled block about a different subject, which label-awareness would otherwise skip."
    - "RESEARCH C-10 is closed: PROJECT.md:836's footer, which names v1.29 as the next milestone and carries R-11's superseded head SHA 311eacf, is corrected. It is the tree's highest-risk stale site because it is a start-here-next instruction and /gsd-new-milestone reads this file."
    - "RESEARCH C-7 is honoured: the four historically-correct 2992 B statements are preserved and marked history-exempt with a stated reason, not deleted."
    - "check_record_corrections.py reports zero unlabeled hits for PROJECT.md."
  artifacts:
    - .planning/PROJECT.md
  key_links:
    - "PROJECT.md ⚠ CORRECTION block -> the v1.22 eight-block register precedent it matches"
    - "PROJECT.md:836 footer -> the v1.23 close, not the retired v1.29 slot"
    - "each history-exempt line -> an inline recordscan:history marker carrying a stated reason"
  prohibitions:
    - "Do NOT delete or rewrite the historically-correct 2992 B statements. PROJECT.md:32's v1.22 archive line and PROJECT.md:163's v1.22 decision-register line were true when written: 2992 = 28672 − 25680 was the pre-Phase-119 headroom, which is exactly what Phase 119's +392 B was judged against. RESEARCH C-7 established R-10 needs no substantive correction; a sweep that removes stale 2992s would delete accurate records."
    - "Do NOT convert PROJECT.md to in-place edits. D-05 assigns it labeled ⚠ CORRECTION blocks; in-place editing destroys the what-did-we-once-believe trail."
    - "Do NOT add an exemption marker to PROJECT.md:59. It is a false current fact, not a preserved historical one, and the prose must change."
    - "Do NOT use a whole-file Write on PROJECT.md. It is over 830 lines; use scoped Edit operations."
    - "Do NOT weaken or delete a needle in check_record_corrections.py to make this file green."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Carry the research corrections into `PROJECT.md`, the file `/gsd-new-milestone` reads to seed the next
milestone's scope.

Two sites research named that CONTEXT's framing does not:
- **C-10** — `PROJECT.md:836`'s footer says *"**Next milestone: v1.29 PY32F071 USB Firmware Install
  (host-side)** — implementation already exists and is green on `firestarter_app` branch
  `feature/py32f071-fw-install` @ `311eacf` … Start with `/gsd-new-milestone`."* It carries R-11's
  superseded head SHA (correct value `4ee64a1`) and points a future reader at a slot plan 130-04
  retired. It is a dated footer, not a labeled correction block, and it is the single highest-risk
  stale site in the tree precisely because it is a start-here-next instruction.
- **C-7** — of the three `2992` hits in this file, `:71` sits inside the labeled
  `⚠ RESEARCH CORRECTIONS` block as an explicit *"not 2992 B"*, while `:32` and `:163` are
  **historically correct** v1.22 archive and decision-register statements. R-10 needs no substantive
  correction here. What it needs is for the history to be marked as history so a checker can tell the
  two apart.

Purpose: stop the propagation hazard at the file the next milestone actually reads, without deleting
records that were true when written.
Output: a labeled `⚠ CORRECTION` block, a corrected `:59` and `:836`, and history markers on the
statements that stay.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-02-SUMMARY.md
</context>

## Artifacts this phase produces

Changed by **this** plan: `.planning/PROJECT.md`. No new file.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, and
the two `usb_cdc.c` `#define` values.

<tasks>

<task type="auto">
  <name>Task 1: Correct PROJECT.md:59's false current fact and add the labeled ⚠ CORRECTION block</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 28-100 — the whole `## Current Milestone: v1.23` region. Note the
      block structure: bold `**⚠ ...**` openers at 55, 61, 63, 65 and 67, each followed by a blank line
      and then numbered body items; the `⚠ RESEARCH CORRECTIONS` block at 67 runs items 1-7 at 69-75.
      Line 59 is item 3 of the block opened at 55, and it asserts
      `DATA_BUFFER_SIZE = 1024` as a current fact.
    - `.planning/PROJECT.md` line 75 — item 7 of the research-corrections block, which already states the
      corrected 512, so the new correction must agree with it rather than contradict it.
    - `.planning/PROJECT.md` lines 155-170 — the v1.22 decision register, for the eight-block
      `⚠ CORRECTION` register shape D-05 names as the precedent. Copy that shape.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-02-SUMMARY.md` — the
      machine-derived reconciliation table, filtered to `PROJECT.md` rows, is this task's authoritative
      worklist. Act on it, not on memory.
    - `130-RESEARCH.md` §"The R-1…R-18 Work List" rows R-2, R-10, R-11, R-14, R-15, and §"C-7", §"C-10".
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" — what may be claimed about the milestone in a
      correction block.
  </read_first>
  <action>
Two edits, both scoped.

**(1) Correct `:59` in prose.** Its claim that the py32 firmware identity is
`DATA_BUFFER_SIZE = 1024` is a false **current** fact. The correct value is **512** — recorded in
`REQUIREMENTS.md` §"Out of Scope" as a deliberate non-change, because the value is wire-visible via
v1.10 CAP-01 so a bump is a behaviour change needing its own justification. Rewrite that clause to
state 512, cite R-2 and the out-of-scope row, and preserve the superseded figure in the same sentence
as what was previously believed. This site must be corrected in prose rather than exempted: it sits
inside a `⚠`-labeled block about a *different* subject (the stale prior-art paragraph), so
label-awareness would skip it and a reader would carry away a wrong number.

Do not touch line 75, which already carries the corrected 512; make the two agree.

**(2) Add a `⚠ CORRECTION` block for the v1.23 close.** Append a new labeled block in the
`## Current Milestone: v1.23` region, matching the register shape the v1.22 blocks use (bold `**⚠
CORRECTION`, a date, a subject sentence, then numbered items). It must record, one numbered item each:
  - **R-2** — py32 `DATA_BUFFER_SIZE` is 512, not 1024, and why the value is deliberately not bumped.
  - **R-11** — the host branch head is `4ee64a1`, not `311eacf`; the footer at the end of this file
    carried the superseded SHA and is corrected by this same plan.
  - **R-15** — both halves moved: the no-`push`-trigger half is fixed in code (`py32f071.yml` now
    carries `push: branches: [beta]`, MERGE-03), and the toolchain-absent half is **itself false** —
    the ARM toolchain installs and works in this devcontainer, which is D-07's subject in
    `REQUIREMENTS.md` and is narrowed there by plan 130-10. State the surviving rule: local ARM builds
    support **delta and byte-identity** claims only; every absolute ARM size claim cites a CI run URL
    plus commit SHA.
  - **R-10** — no substantive correction is needed. `2992 B` was the pre-Phase-119 headroom
    (28672 − 25680), which is exactly what Phase 119's +392 B was judged against, so this file's
    archive and decision-register mentions of it are **historically correct** and are preserved. The
    live post-merge figures are the ones this milestone measured. Cite `124-NONREGRESSION.md` §F4d.
  - **A-5** — the operator-visible AVR flash-constraint decision the research spine assigns to Phase
    130 is **already discharged at Phase 124**, with the operator restatement *"Leonardo flash must not
    grow; Uno-class growth ≤ 64 B, recorded"* in `REQUIREMENTS.md` Operator Decision 4, MERGE-05
    encoding it, and `124-NONREGRESSION.md` §F4d's independent 328PB build closing the single-source
    gap. Record it as discharged with the citation, not as fresh work.
  - **The two py32 ROADMAP slots** are retired into v1.23 by CLOSE-03 (plans 130-04 and 130-05), so any
    reference in this file to a `v1.28 PY32F071 Port` or `v1.29 PY32F071 USB Firmware Install` slot as
    a live plan is superseded.

Every claim in the block must sit inside the Validation Ceiling. Nothing in it may say the firmware
runs on a PY32F071, that the install works end to end, or that anything is bench-validated,
hardware-validated or silicon-verified.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
s=open('PROJECT.md').read()
import re
assert 'DATA_BUFFER_SIZE = 1024' not in s, 'stale py32 buffer claim survives'
assert '4ee64a1' in s
assert 'F4d' in s or '124-NONREGRESSION' in s
print('OK prose corrected')" && cd phases/130-close-honesty-ledger-claim-gate-release-decision && FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/PROJECT.md python3 check_record_corrections.py --explain | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DATA_BUFFER_SIZE = 1024' .planning/PROJECT.md` returns `0`, and the corrected clause
      states `512` and cites R-2.
    - A new `⚠ CORRECTION` block exists in the `## Current Milestone: v1.23` region carrying one numbered
      item for each of R-2, R-11, R-15, R-10, A-5 and the retired py32 slots — six items, each naming its
      identifier.
    - The A-5 item cites `124-NONREGRESSION.md` §F4d and the operator restatement, and describes A-5 as
      discharged at Phase 124 rather than as work done here.
    - The R-10 item states no substantive correction is needed and gives the 28672 − 25680 arithmetic.
    - `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/PROJECT.md python3 check_record_corrections.py`
      no longer reports an `unlabeled` hit for the `py32-buffer-1024` label.
    - `python3 check_record_corrections.py --explain` restricted to this file prints at least one record
      with verdict `block`, proving the new labeled block is recognised by the block model rather than
      only looking labeled to a human.
    - No sentence in the new block asserts that the firmware runs on a PY32F071, that the install works
      end to end, or that anything is bench-validated, hardware-validated or silicon-verified.
    - `git -C /workspaces diff --stat -- .planning/PROJECT.md` shows exactly one file changed.
  </acceptance_criteria>
  <done>The false buffer figure is gone, and the corrections arrive as a labeled block the checker's block model actually recognises.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the PROJECT.md:836 footer and mark the historically-correct statements history-exempt</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 830-840 — the footer stack. Line 836 is the 2026-07-30 v1.22-close
      footer carrying `**Next milestone: v1.29 PY32F071 USB Firmware Install (host-side)**`, `311eacf`,
      the cross-repo release-asset-naming blocker, and `Start with /gsd-new-milestone`. Read the
      surrounding footers so the correction matches the file's own footer-stack convention of retaining
      prior footers below.
    - `.planning/PROJECT.md` line 32 — the v1.22 archive line carrying `(119, +392 B against 2992 B
      headroom)`.
    - `.planning/PROJECT.md` line 163 — the v1.22 decision-register line carrying `the live Leonardo
      figure is 25680/28672, leaving 2992 B`.
    - `check_record_corrections.py` — the exact inline marker grammar written by plan 130-02, including
      that a marker with no stated reason does **not** exempt.
    - `130-RESEARCH.md` §"C-7" and §"C-10", and the R-11 row of the work list.
    - `.planning/ROADMAP.md` as left by plans 130-04 and 130-05 — the retirement line and the v1.23
      shipped entry the corrected footer must point at instead of the retired slot.
  </read_first>
  <action>
Two edits, both scoped.

**(1) Correct the `:836` footer.** It is a dated footer, so preserve it as a dated record while
disarming its instruction. Keep the footer's own text as the v1.22-close record it is, and append a
bracketed supersession note stating:
  - the next-milestone claim is superseded: v1.23 became **PY32F071 Integration** (Phases 123–130), and
    the two py32 slots it names were retired into v1.23 by CLOSE-03;
  - `311eacf` is superseded by `4ee64a1` (R-11);
  - the cross-repo release-asset-naming blocker it describes is **closed** — REL-02 landed the py32
    `.hex` as a release asset matched by a glob, and REL-01 put the ARM build after the version bump.
    Cite the requirement ids, not a claim that the install works;
  - the next milestone after v1.23 is the **v1.30** entry, per its own operator-dated queueing, and
    `/gsd-new-milestone` settles its number.
Do not delete the footer, and do not rewrite the footer stack's ordering convention.

**(2) Mark `:32` and `:163` history-exempt.** Each gets an inline `recordscan:history` marker carrying a
stated reason — the marker grammar plan 130-02 defined requires a reason, and a bare marker does not
exempt. The reason must say, in substance, that `2992 B` was the **pre-Phase-119** Leonardo headroom
(28672 − 25680) and that the statement was accurate when written, so it is preserved as a record rather
than corrected. The markers are HTML comments and are invisible in the rendered file, which is why they
are markers rather than prose; the prose stays exactly as written.

Do **not** add a marker to `:71`. It already sits inside the labeled `⚠ RESEARCH CORRECTIONS` block and
states *"not 2992 B"*, so the block model already exempts it, and adding a redundant marker would make
the exemption harder to audit.

After both edits, re-run the checker against this file alone and confirm zero `unlabeled` records.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/PROJECT.md python3 check_record_corrections.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/PROJECT.md python3 check_record_corrections.py`
      exits `0` and prints a `PASS:` line naming `PROJECT.md`.
    - `--explain` on the same target reports zero records with verdict `unlabeled`, and reports at least
      two records with verdict `inline-history`.
    - The `311eacf` occurrence in the footer region is accompanied, on the same line or in the appended
      note, by `4ee64a1`; `grep -c '4ee64a1' .planning/PROJECT.md` returns at least 2 (the correction
      block from Task 1 and the footer note).
    - `grep -c 'Next milestone: v1.29' .planning/PROJECT.md` still returns 1 — the dated footer is
      preserved — and the appended supersession note sits adjacent to it naming v1.23 PY32F071
      Integration and the v1.30 entry.
    - The prose of lines 32 and 163 is byte-unchanged apart from the appended HTML-comment marker.
      Demonstrate with `git diff` showing the only added text on those lines is the comment.
    - Stripping the marker from either line makes the checker FAIL: demonstrate on a `/tmp` copy of
      `PROJECT.md` with the marker removed, record the observed non-zero exit and the named label, then
      discard the copy. This is the suppression-is-real proof applied to the real file.
    - `git -C /workspaces diff --stat -- .planning/PROJECT.md` shows exactly one file changed, and no
      hunk outside the `## Current Milestone: v1.23` region, line 32, line 163 and the footer stack.
  </acceptance_criteria>
  <done>The start-here-next instruction is disarmed without destroying the dated footer, the two historically-correct statements are exempt with stated reasons, and PROJECT.md is green under the checker with the exemption proven live on the real file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `PROJECT.md` -> `/gsd-new-milestone` scope seeding | This file is read to seed the next milestone. A start-here-next instruction crossing this boundary becomes work nobody re-verified, against a branch SHA that moved. |
| a false current fact inside a labeled block | Label-awareness is a deliberate blind spot; a wrong number sitting inside a block about a different subject is invisible to the gate and visible to a reader. |
| a history marker -> a future auditor | An exemption is only auditable if it carries a reason. A bare marker is a silent forgiveness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-28 | Repudiation / false assurance | `PROJECT.md:836`'s footer | critical | mitigate | Task 2 appends a supersession note naming the corrected SHA, the retired slots and the closed cross-repo blocker, while preserving the dated footer. RESEARCH C-10 rates this the tree's highest-risk stale site. |
| T-130-29 | Spoofing | `PROJECT.md:59`'s buffer figure | high | mitigate | Corrected in prose, deliberately not exempted, because it is a false current fact inside a block about a different subject; an acceptance criterion greps the stale form at zero. |
| T-130-30 | Tampering | the historically-correct 2992 statements | high | mitigate | `must_haves.prohibitions` forbid deleting them; the markers carry stated reasons and a criterion requires the prose byte-unchanged apart from the comment. |
| T-130-31 | Repudiation | a marker that forgives without saying why | medium | mitigate | Plan 130-02's grammar rejects a reasonless marker, and a criterion proves the exemption is live by stripping the marker on a `/tmp` copy and observing the FAIL. |
| T-130-32 | Denial of service (self-inflicted) | `PROJECT.md` outside the edited regions | high | mitigate | Scoped `Edit` only, never a whole-file `Write`; a criterion pins hunks to the milestone region, lines 32 and 163, and the footer stack. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package and runs only `python3`, `git` and `grep`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `FIRESTARTER_RECORDSCAN_TARGETS=/workspaces/.planning/PROJECT.md python3 check_record_corrections.py` → exit 0, `PASS:` naming `PROJECT.md`.
- `--explain` on that target → zero `unlabeled`, at least one `block`, at least two `inline-history`.
- `grep -c 'DATA_BUFFER_SIZE = 1024' .planning/PROJECT.md` → `0`.
- The marker-strip demonstration on a `/tmp` copy → non-zero exit with the label named.
- `git diff --stat -- .planning/PROJECT.md` → one file, hunks confined to the named regions.

</verification>

<success_criteria>
- D-05's labeled-block treatment is applied to PROJECT.md with six identified correction items.
- R-2 and R-11 are corrected in prose; C-10's footer instruction is disarmed additively.
- C-7 is honoured: the historically-correct statements survive, exempt with stated reasons, and the exemption is proven live on the real file.
- A-5 is recorded as discharged at Phase 124 with its citation, not replanned.
- PROJECT.md is green under the CLOSE-01 checker.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-07-SUMMARY.md` when done.
</output>
