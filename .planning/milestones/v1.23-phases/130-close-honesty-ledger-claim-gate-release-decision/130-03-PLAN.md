---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/platform/py32f071/src/usb_cdc.c
  - firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md
  - .planning/v1.23-FLASH-PATH-DECISION.md
autonomous: true
requirements: [CLOSE-02]

must_haves:
  truths:
    - "D-11 lands: the py32 USB device descriptor presents pid.codes 1209:0001 instead of Puya Semiconductor's registered 0x36B7/0xFFFF, before the b15 cut publishes the image."
    - "The not-universally-unique source warning sits BELOW the FIRESTARTER_USB_PID #endif, so the record's citations of line 20 and line 24 stay true in both copies (RESEARCH C-4 item 3)."
    - "Section 5's [SHARED:S4] body is byte-identical across .planning/v1.23-FLASH-PATH-DECISION.md and firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md after the 5(a) and 5(d) rewrites, proven by the 41-leg sync gate."
    - "D-17 holds: section 5(c)'s ship gate and test_flash_path_record_sync.py's _L2_SHIP_GATE constant are byte-unchanged, and the tension is carried as an owned residual rather than resolved by amendment."
    - "An ARM pass precedes the outbound merge (CONSTRAINT 2), recorded as a DELTA claim only: the target still configures and compiles, and the object-level change is confined to usb_cdc.c."
    - "Every _S4_NEEDLES token still appears in section 5, including 0x36B7, 0xFFFF and Puya Semiconductor, which survive as the recorded superseded pair with its provenance."
  artifacts:
    - firestarter/platform/py32f071/src/usb_cdc.c
    - firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md
    - .planning/v1.23-FLASH-PATH-DECISION.md
  key_links:
    - "usb_cdc.c FIRESTARTER_USB_VID/FIRESTARTER_USB_PID -> USB_DEVICE_DESCRIPTOR_INIT at lines 35-36 -> firestarter_cdc_descriptor"
    - "section 5(a) prose -> the literal line numbers 20 and 24 in usb_cdc.c, cited in BOTH record copies"
    - "section 5 body -> _extract_shared_section exact string equality across both copies (no whitespace normalisation)"
  prohibitions:
    - "Do NOT edit section 5(c). D-17 locks it byte-unchanged: it states outright that it is deliberately a condition rather than a warning, so a future reader can fail it, and amending a ship gate so the act about to be taken clears it destroys the mechanism."
    - "Do NOT edit firestarter/tests/test_flash_path_record_sync.py, and in particular not the _L2_SHIP_GATE constant at lines 345-348. It is only in play if 5(c) moves, and 5(c) does not move."
    - "Do NOT insert anything above usb_cdc.c line 19. Inserting above the #ifndef guards shifts the defines off lines 20 and 24 and silently falsifies section 5(a)'s citations in BOTH record copies (RESEARCH C-4 item 3, risk 8)."
    - "Do NOT write that the source warning is required by pid.codes' terms. The terms say the source should warn, not must (RESEARCH C-6). Upgrading a registry's SHOULD to a MUST is itself an overclaim in the one phase whose purpose is not overclaiming."
    - "Do NOT remove 0x36B7, 0xFFFF, Puya Semiconductor, usbd_cdc_if.c, pycdc.inf, 0x0448, py32_dfu.py or the 0xFE/0x01 interface-class pair from section 5. All are _S4_NEEDLES and their absence fails the sync gate."
    - "Do NOT amend REQUIREMENTS.md PCB-04. Its unedited-this-phase clause is Phase-129-scoped and stays true (D-11); the reversal is recorded in 130-LEDGER.md and 130-NONREGRESSION.md instead."
    - "Do NOT claim byte-identity of the ARM image. The descriptor bytes change by design, so byte-identity is the wrong criterion here; the permitted claim is a confined delta."
    - "Do NOT claim any absolute ARM flash or RAM figure from a local build. Per D-07 a local build supports delta and byte-identity claims only; absolute sizes need a CI run URL plus SHA."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them. PCB-04 is already Complete and is not re-touched."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Land the one firmware source change this close makes, and its lockstep record update, before anything
is published.

D-01 publishes an image whose USB device descriptor currently presents `0x36B7`/`0xFFFF` — **Puya
Semiconductor's registered vendor identity**, copied verbatim from the pinned SDK's CDC example — and
everyone who flashes that image inherits it. D-11 replaces it with pid.codes' documented
private-testing id `1209:0001` plus a source warning, and the release body states it.

Two consequences a plan must carry, both from CONTEXT D-11 and both widened by research:
- The change is a real firmware code change and needs an ARM pass before the merge (CONSTRAINT 2).
- Section 5 of the flash-path record is `[SHARED:S4]` under a 41-leg body-only sync gate comparing by
  **exact string equality**, so the body edit must land character-identically in both copies.
  Research C-4 widened the footprint: **5(d) also becomes false** — it currently asserts
  `usb_cdc.c` is not edited and that editing the descriptor *follows* the allocation, which D-11
  inverts.

D-17 (operator-locked during planning) settles the ship-gate tension: 5(c) stays byte-unchanged and
the tension is carried as an owned residual, because amending a ship gate so that the act about to be
taken clears it destroys the mechanism.

Purpose: the published image and the published record must not disagree, and the record must not be
quietly bent to fit the act.
Output: two changed `#define` values, one source warning, a rewritten 5(a) and 5(d) in both copies,
a green 41-leg sync gate, and a recorded local ARM delta.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
@firestarter/CLAUDE.md
</context>

## Artifacts this phase produces

Created or changed by **this** plan:
- `firestarter/platform/py32f071/src/usb_cdc.c` — two changed `#define` values,
  `FIRESTARTER_USB_VID` → `0x1209U` (line 20) and `FIRESTARTER_USB_PID` → `0x0001U` (line 24), plus a
  new provenance-and-warning comment block below the second `#endif`.
- `.planning/v1.23-FLASH-PATH-DECISION.md` — section 5(a) and 5(d) bodies rewritten inside
  `[SHARED:S4]`.
- `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` — the same two sub-paragraphs, byte-identical.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`.

<tasks>

<task type="auto">
  <name>Task 1: Change the two USB descriptor #define values and add the source warning below line 25</name>
  <files>firestarter/platform/py32f071/src/usb_cdc.c</files>
  <read_first>
    - `firestarter/platform/py32f071/src/usb_cdc.c` — read lines 1-45. Lines 19-21 are the
      `FIRESTARTER_USB_VID` `#ifndef` / `#define 0x36B7U` / `#endif` triple; 23-25 are the
      `FIRESTARTER_USB_PID` `#ifndef` / `#define 0xFFFFU` / `#endif` triple; 35-36 consume both inside
      the `USB_DEVICE_DESCRIPTOR_INIT` call that builds `firestarter_cdc_descriptor`.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` section 5 in full — 5(a) at line 196 cites *line 20*
      and *line 24* verbatim; 5(b) at 198 quotes pid.codes' terms and argues for the interim id;
      5(c) at 200-204 is the ship gate; 5(d) at 206; 5(e) rates allocation confidence.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md`
      §`usb_cdc.c` — lines 1-27 quoted verbatim, and the instruction that the comment goes below the
      `#endif` at line 25, never above line 19.
    - `130-RESEARCH.md` §"C-4", §"C-6", §"D-11's Firmware Change — verified on disk".
    - `firestarter/CLAUDE.md` — the five `[SHARED:S*]` keys, the three-places convention, and the
      statement that the sync-gate module runs in no CI leg so re-running it locally is an obligation.
  </read_first>
  <action>
Before writing anything, confirm the firmware sub-repo is on the milestone branch:
`git -C firestarter rev-parse --abbrev-ref HEAD` must be `v1.23-py32f071-integration`. The
branch-base check has been a real trap twice in this project. This plan's firmware commit is made
**inside** the `firestarter` submodule, on that branch.

Change exactly two values, in place, without moving any line:
- line 20: the `FIRESTARTER_USB_VID` value becomes `0x1209U` (was `0x36B7U`).
- line 24: the `FIRESTARTER_USB_PID` value becomes `0x0001U` (was `0xFFFFU`).

Both remain wrapped in their existing `#ifndef` / `#endif` guards, so this is a value change only —
no structural change, and `USB_DEVICE_DESCRIPTOR_INIT` at lines 35-36 needs no edit. Line 20 and line
24 must still be the lines carrying the two `#define`s after the edit, because both record copies
cite those numbers.

Add a comment block **directly below the `#endif` on line 25** — never above line 19 — using the
`/* ... */` block style. It must state, in this order and in the file's own voice:
- what the pair is: pid.codes' documented private-testing product id `1209:0001` under VID `0x1209`,
  which is **not** an allocated PID.
- the warning itself: the PID is not universally unique and must not be relied on outside a test
  environment. Word the obligation as pid.codes' terms *asking* that source referencing the id carry
  this warning, and that this firmware does — never as a requirement. RESEARCH C-6 established the
  terms say *should*, not *must*, and this milestone does not upgrade a registry's SHOULD to a MUST.
- what it replaced and why: the previous pair `0x36B7`/`0xFFFF` is registered to Puya Semiconductor
  and was copied verbatim from the pinned SDK's own CDC example, so the board presented another
  company's registered vendor identity on a product they did not make.
- where the decision lives: `platform/py32f071/FLASH-PATH-AND-PCB.md` section 5 (and its meta twin
  `.planning/v1.23-FLASH-PATH-DECISION.md` section 5), including that the ship gate in 5(c) stands
  unchanged — no board ships and no release advertises a USB identity until a PID allocated under VID
  `0x1209` exists — and that Phase 130 D-11 reverses Phase 129 D-06, which declined this edit only
  because that phase was docs-only with no cut planned.

Do not touch any other line of the file. Do not add or remove an include. Do not change
`FIRESTARTER_CDC_IN_EP`, `FIRESTARTER_USB_RX_CAPACITY`, `FIRESTARTER_USB_TIMEOUT_MS`,
`FIRESTARTER_USB_CONFIG_SIZE` or anything in the descriptor construction.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
p='platform/py32f071/src/usb_cdc.c'
L=open(p).read().split('\n')
assert 'FIRESTARTER_USB_VID' in L[19] and '0x1209U' in L[19], repr(L[19])
assert 'FIRESTARTER_USB_PID' in L[23] and '0x0001U' in L[23], repr(L[23])
assert L[18].strip().startswith('#ifndef'), repr(L[18])
assert L[22].strip().startswith('#ifndef'), repr(L[22])
assert '0x36B7' not in L[19] and '0xFFFF' not in L[23]
body='\n'.join(L)
assert 'pid.codes' in body and '1209:0001' in body
assert 'Puya Semiconductor' in body
print('OK descriptor pair swapped')"</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '20p;24p' firestarter/platform/py32f071/src/usb_cdc.c` prints exactly two lines: the
      `FIRESTARTER_USB_VID` define carrying `0x1209U`, and the `FIRESTARTER_USB_PID` define carrying
      `0x0001U`.
    - `sed -n '19p;23p' firestarter/platform/py32f071/src/usb_cdc.c` still prints the two `#ifndef`
      guard lines — nothing was inserted above line 19.
    - `grep -c '0x36B7' firestarter/platform/py32f071/src/usb_cdc.c` counts only occurrences inside
      the new comment block; `grep -n '0x36B7U' firestarter/platform/py32f071/src/usb_cdc.c` returns
      no line that is an active `#define` (i.e. no match on a line beginning `#define`).
    - `grep -vE '^\s*(\*|/\*|//)' firestarter/platform/py32f071/src/usb_cdc.c | grep -c '0xFFFFU'`
      returns `0` — the superseded PID value survives only as comment text, never as code.
    - `cd firestarter && git diff --stat -- platform/py32f071/src/usb_cdc.c` shows exactly one file
      changed and the hunks confined to lines 19-30.
    - `cd firestarter && python3 -m pytest tests/ -q` reports `221 passed` (the recorded local figure)
      with `0 failed`.
    - `cd firestarter && pio test -e native` completes with all cases passing at the BASE-01 recorded
      counts. `usb_cdc.c` is a py32-only translation unit and must not appear in any native build log.
    - `cd firestarter && git rev-parse --abbrev-ref HEAD` prints `v1.23-py32f071-integration`, and the
      commit for this task exists in the `firestarter` submodule, not in the meta repo.
  </acceptance_criteria>
  <done>The descriptor presents the pid.codes interim id; the warning is present and worded as an ask, not a requirement; lines 20 and 24 still carry the two defines; the AVR and native suites are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite section 5(a) and 5(d) identically in both [SHARED:S4] copies and re-run the 41-leg sync gate</name>
  <files>.planning/v1.23-FLASH-PATH-DECISION.md, firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md</files>
  <read_first>
    - `.planning/v1.23-FLASH-PATH-DECISION.md` — read section 5 in full. The `[SHARED:S4]` heading is
      at line 194 (`## 5. USB vendor and product identity [SHARED:S4]`); 5(a) is line 196; 5(b) 198;
      5(c) 200-204; 5(d) 206; the shared-verbatim closing sentence 214; the next `## ` heading (section
      6, socket empty) at 216 bounds the span.
    - `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` — the firmware twin. Its `[SHARED:S4]`
      heading is at line 104 (no `5. ` prefix — the heading is excluded from the compared body, which
      is why the differing prefix is legal); 5(a) 106; 5(c) 110-114; 5(d) 116; closing sentence 124;
      next `## ` at 126.
    - `firestarter/tests/test_flash_path_record_sync.py` — `_extract_shared_section` at lines 167-213
      (span-scoped between the marked `## ` heading and the next `## `, heading excluded, trailing
      blank lines stripped, otherwise **exact string equality**, duplicate marker refuses to guess,
      missing marker yields a non-vacuity failure); `_S4_NEEDLES` at line 434; `_L2_SHIP_GATE` at
      345-348.
    - `130-PATTERNS.md` §`[SHARED:S4]` lockstep pair — 5(a) and 5(d) quoted verbatim from both copies
      so a character-identical replacement can be specified, plus the section-marker semantics table.
    - `130-RESEARCH.md` §"C-4" and §"C-5", and this plan's `<objective>` for D-17's resolution.
  </read_first>
  <action>
Edit the same two sub-paragraphs in both copies so that, after the edit, the two section-5 bodies are
character-identical. `_extract_shared_section` performs no whitespace normalisation, so a single
trailing space or a differing line wrap fails the gate. Author the replacement text once and apply the
identical text to both files.

**5(a) — "What the descriptor currently presents, and where it came from."** Rewrite so it describes
the post-D-11 state and preserves the provenance record:
- `platform/py32f071/src/usb_cdc.c` now defines `FIRESTARTER_USB_VID 0x1209U` at **line 20** and
  `FIRESTARTER_USB_PID 0x0001U` at **line 24**, consumed inside the `USB_DEVICE_DESCRIPTOR_INIT` call
  that builds `firestarter_cdc_descriptor`. Keep the literal line numbers 20 and 24 — Task 1 preserved
  them, and both copies cite them.
- `0x1209` is the pid.codes community vendor id and `1209:0001` is that registry's documented
  private-testing product id — **not** an allocated PID. Keep the existing `[CITED: ...]` sourcing
  tags in the form the record already uses.
- What it replaced, retained as the record of what the board previously presented: `0x36B7`/`0xFFFF`,
  with `0x36B7` registered to Puya Semiconductor (Shanghai) Co., Ltd., copied verbatim from the pinned
  SDK's own USB CDC example `Projects/PY32F071-STK/.../usbd_cdc_if.c` lines 9-10, whose companion
  Windows driver INF `pycdc.inf` lines 28 and 31 matched it, both at
  `GIT_TAG 0ed2f4b4d3391eccfd4491006a30295fd78e32c2`. Retain the sentence a reader cannot misread —
  that until this change the board presented another company's registered vendor identity, the silicon
  vendor's, on a product they did not make — in the past tense, and retain the observation that every
  PY32 project starting from Puya's CDC example without changing the descriptor presents the same
  pair.
- One new sentence: the values are no longer undocumented in this tree — `usb_cdc.c` now carries a
  provenance-and-warning comment directly below the two guards. Word the pid.codes obligation as the
  terms *asking* that source referencing `1209:0001` warn that the PID is not universally unique, and
  that this firmware does. Never as a requirement (RESEARCH C-6).

**5(d) — "What this phase does and does not change."** This paragraph currently asserts the opposite
of what D-11 does and needs substantive rework, not a tweak. Rewrite so it states:
- `usb_cdc.c` **is** edited, in v1.23 Phase 130 under D-11, and that this **reverses Phase 129 D-06**,
  which declined the same edit only because that phase was documentation-only with no cut planned.
  Record the reversal *as* a reversal with its constraint named — publishing an image is the new fact.
  Note that PCB-04's "`usb_cdc.c` itself stays unedited **this phase**" clause is Phase-129-scoped and
  remains true as written; `REQUIREMENTS.md` is not amended for it.
- The ordering claim is inverted from what this paragraph previously said: an interim non-allocated id
  lands **before** any allocation, precisely so a published image does not present a third party's
  identity while the allocation waits on a schematic (5(e) rates that confidence LOW).
- The ship gate in 5(c) is **unchanged and still binds**: no board ships and no release advertises a
  USB identity until a PID allocated under VID `0x1209` exists. State plainly that whether a caveated
  disclosure of an explicitly non-allocated test id counts as *advertising a USB identity* is a
  judgment a future reader may decide differently — which 5(c) deliberately permits — and that the
  reasoning is recorded in `.planning/phases/130-.../130-DECISION.md` and carried as a negative-space
  row in `130-LEDGER.md` rather than resolved by amending the gate.
- Retain, unchanged in substance, the host-side facts already in the paragraph: `py32_dfu.py`'s
  `find_dfu_interfaces` discovers DFU devices by **interface class** `0xFE/0x01` rather than by vendor
  and product id, so PCB-04 is never read as a host bug; `0x0448` is a bootloader-parameter-table
  **device** id (Puya UM1504 Table 1-1), not a USB product id, which confirms the `REQUIREMENTS.md`
  out-of-scope note; and the datasheet-versus-UM1504 authority sentence about USB DFU.
- One new sentence recording that the descriptor change needs an ARM pass, that Task 3 of this plan
  ran one locally, and that a local build supports a **delta** claim only — never an absolute size,
  which requires a CI run URL plus SHA (D-07).

Leave 5(b), 5(c), 5(e) and the closing shared-verbatim sentence byte-unchanged in both copies. Leave
`firestarter/tests/test_flash_path_record_sync.py` entirely untouched.

The meta copy is committed in the meta repo; the firmware copy is committed inside the `firestarter`
submodule on `v1.23-py32f071-integration`. Both edits belong to the same logical change — commit them
in immediate succession and say so in the commit bodies, since the gate that binds them runs in no CI
leg.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter && FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q`
      reports `41 passed` and `0 failed` — the same leg count as before the edit, so no leg was
      skipped or lost.
    - `python3 -m pytest tests/test_flash_path_record_sync.py -q -k "shared_sections_match"` passes
      for every one of the five `[SHARED:S*]` keys, S4 included.
    - `python3 -m pytest tests/test_flash_path_record_sync.py -q -k "vid_pid_decision_and_ship_gate"`
      passes for both the `meta` and `fw` parametrisations — this is the `_L2_SHIP_GATE` byte-exact
      leg, and it passing proves 5(c) was not touched.
    - `cd firestarter && git diff -- tests/test_flash_path_record_sync.py` is empty.
    - For each of the twelve `_S4_NEEDLES` tokens, `grep -c` inside the extracted section-5 body of
      **both** files returns at least 1. Demonstrate by running the sync-gate needle leg rather than by
      hand-grepping, and record the leg name in the SUMMARY.
    - The two extracted section-5 bodies are byte-identical: capture each with a short `python3 -c`
      using the test module's own `_extract_shared_section`, `sha256sum` both, and record the two equal
      hashes in the SUMMARY. Do **not** assert an empty `git diff` between the files — they differ
      outside section 5 by design.
    - The literal `1209:0001` and the literal `0x36B7` both appear inside the extracted section-5 body
      of both files.
    - `git -C /workspaces diff --stat -- .planning/v1.23-FLASH-PATH-DECISION.md` shows hunks confined
      to the 5(a) and 5(d) regions; no hunk overlaps lines 200-204.
  </acceptance_criteria>
  <done>Both copies carry the rewritten 5(a) and 5(d) character-identically; the 41-leg gate is green at the same leg count; 5(c) and `_L2_SHIP_GATE` are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Run a local ARM pass and record a confined-delta claim</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-03-SUMMARY.md</files>
  <read_first>
    - `firestarter/.github/actions/build-py32f071/action.yml` — the single place holding the ARM
      toolchain install plus the configure and build invocations. The configure is
      `cmake -S platform/py32f071 -B build/py32f071 -G Ninja -DCMAKE_BUILD_TYPE=Release`; the build is
      `cmake --build build/py32f071`; the image is the single
      `build/py32f071/firestarter_*.hex`; the SDK arrives via `FetchContent` into
      `build/py32f071/_deps/py32f071_sdk-src`.
    - `firestarter/.github/workflows/py32f071.yml` — the LOUD gate with `push: branches: [beta]` and
      no paths filter, and no `continue-on-error`. This is what closes the gap if a local build cannot
      be completed.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" — the absolute-versus-delta rule, and D-07's
      narrowing (plan 130-10 amends the clause; the rule it lands on is the one this task obeys).
    - `130-RESEARCH.md` §"Environment Availability" — recorded `arm-none-eabi-gcc`/`cmake`/`ninja` as
      **absent but installable**. Measured at plan time on 2026-08-02 they are all **present**
      (`arm-none-eabi-gcc` 14.2.1, `cmake` 4.4.0, `ninja` 1.13.0), so that row has DRIFTED in the
      helpful direction. Record the drift.
  </read_first>
  <action>
Produce an ARM pass that supports a **delta** claim about D-11's edit, satisfying CONSTRAINT 2's
"an ARM pass precedes the outbound merge" without overclaiming.

Sequence:
1. Stash or otherwise set aside the `usb_cdc.c` change so the pre-edit source is on disk, configure
   and build the ARM target with the two commands the composite action uses, and record: the exit
   codes, `arm-none-eabi-size` on the produced `.elf`, and the object file list with sizes for the
   `usb_cdc.c` object. Do this **without** committing anything in that state.
2. Restore the edit, rebuild incrementally, and record the same three things again.
3. Compute and record the delta. The expected and permitted claim is: the ARM target still configures
   and compiles cleanly, exactly one translation unit's object changed, and that object is
   `usb_cdc.c`'s. Report the `.text`/`.data`/`.bss` delta as measured.

Do **not** claim byte-identity of the image — the descriptor bytes change by design, so byte-identity
is the wrong criterion here and asserting it would be false. Do **not** report any absolute ARM flash
or RAM figure as a milestone figure; local absolute sizes are not comparable with CI's (measured
`text=27260` local against `text=27344` CI), which is exactly why D-07 narrows the ceiling to deltas
and byte-identity.

If the `FetchContent` SDK fetch cannot complete (no network, or the upstream is unreachable), do not
fake a build and do not weaken the claim. Record instead: the exact command run, the exact failure
output, and that the ARM pass for D-11 is therefore **owed to the LOUD `py32f071.yml` gate**, which
fires unconditionally on the operator's push to `beta` because it carries no paths filter and no
`continue-on-error`. State it as a named, dated gap with its closer, and carry the same statement into
`130-DECISION.md`'s pre-flight section and `130-NONREGRESSION.md`, so the operator sees it before
pushing rather than after.

Record all of the above in `130-03-SUMMARY.md`, including the drift of RESEARCH's
Environment-Availability row (toolchain recorded absent, measured present) with the three measured
version strings.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && cmake -S platform/py32f071 -B build/py32f071 -G Ninja -DCMAKE_BUILD_TYPE=Release > /tmp/claude-1000/arm-configure.log 2>&1; echo "configure_exit=$?"; cmake --build build/py32f071 > /tmp/claude-1000/arm-build.log 2>&1; echo "build_exit=$?"; ls build/py32f071/firestarter_*.hex 2>/dev/null && arm-none-eabi-size build/py32f071/firestarter_py32f071.elf 2>/dev/null; tail -5 /tmp/claude-1000/arm-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `130-03-SUMMARY.md` records both configure exit codes and both build exit codes, with the
      post-edit pair being `0` and `0`, **or** records the exact failure output plus the named
      LOUD-gate closer if the SDK fetch could not complete.
    - On a completed build, `ls build/py32f071/firestarter_*.hex` lists exactly one file, and its name
      equals `firestarter_py32f071.hex` — the same name `asset_candidates("py32f071")[0]` resolves
      (REL-04's binding).
    - The recorded object-level comparison names exactly one changed object, and its source is
      `usb_cdc.c`. If any other object changed, the SUMMARY explains why before the plan is considered
      done.
    - `130-03-SUMMARY.md` contains no absolute ARM flash or RAM figure presented as a milestone figure;
      every ARM number it carries is labelled as a local delta or as an input to a delta.
    - `130-03-SUMMARY.md` records the three measured tool version strings for `arm-none-eabi-gcc`,
      `cmake` and `ninja`, and states that RESEARCH's Environment-Availability row recorded them absent
      and they measured present.
    - `cd firestarter && git status --porcelain` shows no `build/` artifacts staged or committed — the
      build directory is not a deliverable. Confirm `build/` is ignored, and if it is not, do not add
      it to the commit.
    - `cd firestarter && python3 -m pytest tests/ -q` still reports `221 passed` after the ARM work.
  </acceptance_criteria>
  <done>An ARM pass exists for D-11 before the merge, recorded as a confined-delta claim with no absolute-size overclaim, or recorded as a named gap with the LOUD gate as its closer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published `.hex` -> every host that enumerates it | The descriptor bytes in the published image become the USB identity of every board anyone flashes. This is the one boundary in the milestone that reaches third-party hardware. |
| meta record copy <-> firmware record copy | The two `[SHARED:S4]` bodies must agree byte-for-byte; the gate that binds them runs in no CI leg, so the boundary is enforced only by a local run. |
| ship gate prose -> the act it governs | Section 5(c) is a condition a future reader can fail. An edit to it by the party taking the governed act is a self-clearing gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-11 | Spoofing | the py32 USB device descriptor | critical | mitigate | Task 1 replaces Puya Semiconductor's registered `0x36B7`/`0xFFFF` with pid.codes `1209:0001` before the cut, and adds an in-source warning that the id is not universally unique. Acceptance criteria assert the superseded values survive only as comment text, never as an active `#define`. |
| T-130-12 | Tampering | section 5(c), the ship gate | critical | mitigate | D-17 locks 5(c) and `_L2_SHIP_GATE` byte-unchanged; the `vid_pid_decision_and_ship_gate` leg passing for both parametrisations is the mechanical proof, and an acceptance criterion requires an empty diff on the test module. |
| T-130-13 | Repudiation | the lockstep record pair | high | mitigate | The 41-leg gate is re-run and its leg count pinned at 41; the two extracted bodies are hashed and the equal hashes recorded, rather than asserting an empty inter-file diff which would be false outside section 5. |
| T-130-14 | Repudiation / false assurance | the ARM pass | high | mitigate | Task 3 forbids a byte-identity claim (wrong criterion once descriptor bytes change) and forbids absolute local sizes (D-07); an unfinishable build is recorded as a named gap with the LOUD `py32f071.yml` gate as its closer, surfaced in `130-DECISION.md` before the push. |
| T-130-15 | Information disclosure | pid.codes terms wording | medium | mitigate | RESEARCH C-6: the terms say the source *should* warn. The `must_haves.prohibitions` forbid writing that the warning is required, in the source comment and in both record copies. |
| T-130-16 | Elevation of privilege | none | low | accept | This plan runs no privileged command: no push, no merge, no tag, no workflow dispatch, no release edit. The firmware commit stays local on the milestone branch. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package. `arm-none-eabi-gcc` 14.2.1, `cmake` 4.4.0 and `ninja` 1.13.0 were measured present at plan time; the CMake `FetchContent` SDK is already pinned in-tree at `GIT_TAG 0ed2f4b4d3391eccfd4491006a30295fd78e32c2`, which is a pinned commit rather than a floating package version, so the Package Legitimacy Gate has no `[ASSUMED]`/`[SUS]` subject. If the pin ever has to change, that is a separate decision needing its own checkpoint. |
</threat_model>

<verification>
- `cd firestarter && FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q` → `41 passed`.
- `cd firestarter && python3 -m pytest tests/ -q` → `221 passed`.
- `cd firestarter && pio test -e native` → BASE-01 recorded case and suite counts.
- `sed -n '20p;24p' firestarter/platform/py32f071/src/usb_cdc.c` → the two defines carrying `0x1209U` and `0x0001U`.
- `cd firestarter && git diff -- tests/test_flash_path_record_sync.py` → empty.
- The two extracted `[SHARED:S4]` bodies hash equal.
- ARM configure and build exit 0, or the failure plus its named closer is recorded.
</verification>

<success_criteria>
- The descriptor no longer presents a third party's registered vendor identity.
- The source warning is present and worded as an ask, per C-6.
- Lines 20 and 24 still carry the two defines, so both record copies' citations stay true.
- Section 5(a) and 5(d) are rewritten identically in both copies; the 41-leg gate is green at 41.
- Section 5(c) and `_L2_SHIP_GATE` are provably untouched, per D-17.
- An ARM pass is recorded as a confined-delta claim, or its absence is recorded as a named gap with the LOUD gate as closer.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-03-SUMMARY.md` when done.
</output>