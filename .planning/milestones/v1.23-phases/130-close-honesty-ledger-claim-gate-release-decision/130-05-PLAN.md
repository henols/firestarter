---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 05
type: execute
wave: 2
depends_on: ["130-04"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/todos/pending/correct-v128-py32-roadmap-prior-art.md
  - .planning/todos/completed/correct-v128-py32-roadmap-prior-art.md
autonomous: true
requirements: [CLOSE-03]

must_haves:
  truths:
    - "D-15 lands: backlog stubs 999.23 and 999.24 retire as shipped-into-v1.23, marked with the phases that delivered them, the way 999.4–999.7 retired when promoted."
    - "D-15 lands: the → v1.28 leads/follows pointers in both stubs, broken by plan 130-04's renumber, are corrected rather than left pointing at Binary Command Protocol."
    - "D-15 lands: the v1.30 entry's 'the v1.29 slot immediately above' back-reference is corrected to name the retirement line."
    - "D-15's 999.25 half is verified before an edit is planned, and recorded as a no-op if it has no subject — the phrase occurs exactly once in the file, at the v1.30 entry."
    - "ROADMAP.md:1883's 'scope v1.28 from that document' instruction carries an inline supersession note, because it is an instruction rather than a record; the other dated review-pass paragraphs are recorded history-exempt."
    - "The owning todo correct-v128-py32-roadmap-prior-art moves to .planning/todos/completed/."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/todos/completed/correct-v128-py32-roadmap-prior-art.md
  key_links:
    - "999.23 and 999.24 dispositions -> the retirement line plan 130-04 wrote in the ## Milestones list"
    - "the v1.30 back-reference -> the retirement line, not the deleted v1.29 entry"
    - "ROADMAP.md:1883's instruction -> an inline supersession note naming Phase 130 as its superseder"
  prohibitions:
    - "Do NOT change one byte of the v1.24, v1.25, v1.26 or v1.27 entries. Their SHA-256 values, recorded in plan 130-04, are re-verified by this plan's final task."
    - "Do NOT touch backlog stub 999.22 (→ v1.27) or 999.25 (→ v1.30) beyond what verification shows is needed. D-15 states both are untouched, and 999.25's heading pointer → v1.30, NEXT after v1.23 stays true."
    - "Do NOT rewrite the dated review-pass paragraphs at ROADMAP.md:1747, :1877, :1879 and :1887. Rewriting a dated review record is the same error D-05 avoids for the branch-state note; they are history-exempt and the ruling is recorded."
    - "Do NOT compact the v1.30 slot into the freed v1.29 number. D-14 defers that to activation."
    - "Do NOT edit ROADMAP.md:2414 or ROADMAP.md:2468 in this plan. Those are CLOSE-01 sites owned by plan 130-06, which runs in the next wave against the same file."
    - "Do NOT invent a 999.25 edit to satisfy D-15's wording. If verification shows no subject, record the no-op as a no-op."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Repair everything plan 130-04's renumber broke, and retire the two backlog stubs whose work shipped in
Phases 123–130.

The pointers are not cosmetic. `999.23` and `999.24` both carry `→ v1.28, leads` / `→ v1.28, follows`
in their headings and `provisional v1.28 PY32F071 Port` in their dispositions. After plan 130-04, v1.28
**is** Binary Command Protocol, so those pointers now aim at the wrong milestone — and the renumber is
what broke them, which is why fixing them is in scope by consequence (D-15). Both stubs are also still
marked `⏫ QUEUED` for work that is done, so the backlog's open-item count overstates.

Two research findings shape this plan beyond D-15's text:
- **D-15's 999.25 half may be a no-op.** The phrase *"the v1.29 slot immediately above"* occurs
  **exactly once** in the whole file, at the v1.30 entry. 999.25's stub carries only
  `→ v1.30, NEXT after v1.23`, which stays true. Verify before planning an edit; record a no-op as a
  no-op rather than inventing one.
- **`ROADMAP.md:1883` is not named by D-15 and needs a ruling.** It is a dated third-review-pass
  paragraph carrying both the `999.23 + 999.24 → v1.28 PY32F071 Port` pairing and the stale prior-art,
  including *"scope v1.28 from that document"*. That last clause is an **instruction**, not a record —
  a future reader following it would scope from a document A-6/R-8 proved does not exist on the live
  branch. Treat the dated paragraphs as history-exempt but give that one clause an inline supersession
  note.

Purpose: leave no pointer aiming at a slot that no longer means what it meant, and no queued stub for
delivered work.
Output: retired stubs, corrected back-references, one inline supersession note, a recorded 999.25
verdict, and the todo moved to completed.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-04-SUMMARY.md
@.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md
</context>

## Artifacts this phase produces

Changed by **this** plan: `.planning/ROADMAP.md` (backlog stub region and the v1.30 entry only), and
`.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md` moved to
`.planning/todos/completed/`.

Also produced by the phase, for orientation: `130-LEDGER.md`, `130-DECISION.md`,
`130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`, `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md`, `check_record_corrections.py`, `test_check_record_corrections.py`, `fixtures/`, and
the two `usb_cdc.c` `#define` values.

<tasks>

<task type="auto">
  <name>Task 1: Verify D-15's actual subject set before editing, and record the 999.25 verdict</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 1720-1770 — the `999.23` heading and disposition, the `999.24`
      heading and disposition, and the `999.25` heading. Read each in full; the dispositions are long
      single lines.
    - `.planning/ROADMAP.md` lines 1875-1892 — the five dated review-pass paragraphs, including
      `:1883`'s third-pass paragraph carrying the `999.23 + 999.24 → v1.28 PY32F071 Port` pairing and
      the `scope v1.28 from that document` instruction.
    - `.planning/ROADMAP.md` the `## Milestones` list as plan 130-04 left it — the retirement line the
      corrected back-references must name.
    - `130-RESEARCH.md` §"Backlog stubs and back-references (D-15)" — the per-site table with D-15
      actions, and the `D-15 nuance` paragraph establishing the phrase occurs once.
    - `130-RESEARCH.md` §"Open Questions" items 2 and 3 — the recommended rulings this plan records.
  </read_first>
  <action>
Measure the subject set before touching anything, so no edit is invented and no site is missed.

Record, in `130-05-SUMMARY.md`:
1. Every line number carrying the phrase `the v1.29 slot immediately above`, with its containing
   entry or stub. Expected: exactly one, in the v1.30 entry. If the count is one, record explicitly
   that **D-15's 999.25 half has no subject and is a no-op**, and that recording it as a no-op is
   preferable to inventing an edit. If the count is more than one, list every site and treat each.
2. Every line number in the `999.23` and `999.24` regions carrying a `v1.28` pointer or the words
   `leads` / `follows` / `provisional`, so the pointer repairs are enumerated rather than guessed.
3. The `999.25` heading text verbatim, together with the verdict that its `→ v1.30, NEXT after v1.23`
   pointer stays true and needs no change.
4. The five dated review-pass paragraph line numbers, and the ruling: they are **history-exempt**,
   because rewriting a dated, signed review record is the same error D-05 avoids for the branch-state
   note, and criterion 1's own escape clause covers a labeled correction **or history** block. Record
   the single exception: `:1883`'s `scope v1.28 from that document` clause is an instruction rather
   than a record and gets an inline supersession note in Task 2.
5. Whether `999.22`'s region contains any `v1.28` pointer. D-15 says 999.22 is untouched; verify that
   is true rather than assumed, and record the result.

Do not edit `ROADMAP.md` in this task.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && echo "v1.29-slot-phrase count:" && grep -c 'the v1.29 slot immediately above' ROADMAP.md && echo "999.2x v1.28 pointers:" && grep -n 'v1\.28' ROADMAP.md | sed -n '1,40p' | cut -c1-100 && python3 -c "
s=open('.planning/../.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md').read() if False else open('phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md').read()
for k in ('999.23','999.24','999.25','999.22','1883','history-exempt'):
    assert k in s, k
print('OK summary covers all five measurement items')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'the v1.29 slot immediately above' .planning/ROADMAP.md` returns `1`, and
      `130-05-SUMMARY.md` records that count together with the explicit no-op verdict for D-15's
      999.25 half.
    - `130-05-SUMMARY.md` lists every `999.23` and `999.24` line number needing a pointer repair, with
      the current text and the intended corrected target, before any edit is made.
    - `130-05-SUMMARY.md` quotes the `999.25` heading verbatim and states its pointer stays true.
    - `130-05-SUMMARY.md` names the five dated review-pass line numbers, records them history-exempt
      with the D-05 reasoning, and names `:1883`'s instruction clause as the single exception.
    - `130-05-SUMMARY.md` records a measured verdict for `999.22` rather than repeating D-15's
      assertion.
    - `git -C /workspaces diff --stat -- .planning/ROADMAP.md` is empty at the end of this task.
  </acceptance_criteria>
  <done>Every D-15 subject is measured and enumerated, the 999.25 no-op is recorded as a no-op, and the history-exemption ruling with its single exception is on the record before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: Retire 999.23 and 999.24, correct the broken pointers and the v1.30 back-reference, and add the one inline supersession note</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `130-05-SUMMARY.md` as written by Task 1 — the enumerated subject set this task acts on.
    - `.planning/ROADMAP.md` lines 1720-1770 and 1875-1892, plus the `## Milestones` retirement line.
    - `.planning/ROADMAP.md` — find how `999.4`–`999.7` were marked when they retired on promotion
      (D-15 cites them as the precedent) and copy that marking shape rather than inventing one.
    - `130-RESEARCH.md` §"Backlog stubs and back-references (D-15)".
  </read_first>
  <action>
Make four kinds of edit, all with scoped `Edit` operations and never a whole-file `Write`.

**(1) Retire the two stubs.** For `999.23` and `999.24`, change the heading's disposition glyph and
trailing pointer so the stub reads as **delivered**, not queued: replace the `⏫ QUEUED 2026-07-27 →
v1.28, leads` / `→ v1.28, follows` form with a retired form naming the milestone and phases that
delivered it — v1.23 PY32F071 Integration, Phases 123–130 — dated with this phase's date. Match the
marking shape `999.4`–`999.7` used when they retired on promotion. Keep the issue references
(`gh#16`, `gh#17`) intact; they are still the origin.

**(2) Correct both dispositions.** In `999.23`'s disposition, the `provisional v1.28 PY32F071 Port`
slot name is now wrong twice over — the slot is retired and v1.28 is Binary Command Protocol. Replace
the slot-name references with a pointer to the `## Milestones` retirement line and the v1.23 detail
section. In the same disposition, correct the three stale claims it still carries: PR #46
closed-unmerged as the live state (PR #48 was the live attempt), `2c2ed10` with `603 additions across
8 files` as the surviving prior art, and `PORTING.md` (195 lines) as an existing specification — A-6
and R-8 established it exists only on the two closed PRs and its layout does not match what #48
built. Do the same for `999.24`'s disposition, which carries `follows 999.23 inside the provisional
v1.28 PY32F071 Port milestone slot`.

**(3) Correct the v1.30 back-reference.** Its sentence *"the v1.29 slot immediately above is still
occupied by the py32 USB-install entry until v1.23's Phase 130 retires it"* is now false — the
retirement has landed. Rewrite it to state that the retirement landed in v1.23 Phase 130, that the
v1.29 number is vacant, and that this entry's own instruction to compact at activation rather than now
stands unchanged. Do **not** renumber this entry.

**(4) Add the single inline supersession note at `:1883`.** Append, to that dated third-review-pass
paragraph, a short bracketed supersession note stating that its `scope v1.28 from that document`
instruction is superseded by v1.23 Phase 130: the two py32 slots retired into v1.23, and the document
it names does not exist on the live branch (A-6/R-8). Keep the paragraph's own dated text intact — the
note is additive, so the review record is preserved and the instruction is disarmed. Add **no** note
to `:1747`, `:1877`, `:1879` or `:1887`; they are records rather than instructions and Task 1 recorded
them history-exempt.

If plan 130-02's checker flags any line in the regions this task edits, prefer the correction over an
exemption marker. An exemption marker is for a statement that was true when written and is preserved
deliberately; a broken pointer is neither.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
s=open('ROADMAP.md').read()
lines=s.split('\n')
h23=[l for l in lines if l.startswith('### Phase 999.23')]
h24=[l for l in lines if l.startswith('### Phase 999.24')]
h25=[l for l in lines if l.startswith('### Phase 999.25')]
assert len(h23)==1 and len(h24)==1 and len(h25)==1
for h in h23+h24:
    assert '⏫ QUEUED' not in h, h
    assert 'v1.28' not in h, h
    assert '123' in h and '130' in h, h
assert '→ v1.30, NEXT after v1.23' in h25[0], h25[0]
assert 'the v1.29 slot immediately above is still occupied' not in s
print('OK stubs retired, pointers repaired')" && grep -c 'v1\.28 PY32F071 Port' ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `### Phase 999.23` heading and one `### Phase 999.24` heading exist; neither contains
      `⏫ QUEUED` or a `v1.28` pointer, and each names the delivering phase range `123`–`130`.
    - `### Phase 999.25`'s heading still contains `→ v1.30, NEXT after v1.23` unchanged.
    - `grep -c 'the v1.29 slot immediately above is still occupied' .planning/ROADMAP.md` returns `0`,
      and the v1.30 entry now names the retirement.
    - Inside the `999.23` and `999.24` disposition regions,
      `grep -vE '^\s*(<!--|>)' | grep -c '2c2ed10'` returns `0`, and `grep -c 'PORTING.md'` restricted
      to those two regions returns `0` — the three stale claims are gone from the stubs.
    - The `:1883` paragraph still contains its original dated text **and** now contains a supersession
      note naming v1.23 Phase 130. The four other dated review-pass paragraphs are byte-unchanged —
      demonstrate with `git diff -U0` showing no hunk at their line numbers.
    - `grep -c 'v1.28 PY32F071 Port' .planning/ROADMAP.md` is strictly lower than the value plan 130-04
      left, and every surviving occurrence is inside a dated review-history paragraph or a supersession
      note. Record the before and after counts.
    - `git -C /workspaces diff -U0 -- .planning/ROADMAP.md` shows no hunk in the `## Milestones` list
      region other than the v1.30 entry.
    - `git -C /workspaces diff -U0 -- .planning/ROADMAP.md` shows no hunk at line 2414 or line 2468 —
      those are plan 130-06's sites.
  </acceptance_criteria>
  <done>Both stubs read as delivered, no pointer aims at the renumbered v1.28, the v1.30 back-reference names the retirement, and the one instruction clause is disarmed additively.</done>
</task>

<task type="auto">
  <name>Task 3: Move the owning todo to completed and re-verify the four untouched entries</name>
  <files>.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md, .planning/todos/completed/correct-v128-py32-roadmap-prior-art.md, .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md</files>
  <read_first>
    - `.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md` — the full file: frontmatter with
      `status: pending` and `resolves_phase: 130`, the header note recording that v1.23 research
      re-verified all five corrections and found a sixth, the five numbered corrections, and the closing
      note about doing this before v1.28 is activated.
    - `.planning/todos/completed/` — check whether the directory exists and what naming and frontmatter
      convention completed todos in this project use, so this one matches rather than inventing a shape.
    - `130-04-SUMMARY.md` — the four recorded v1.24–v1.27 SHA-256 values.
    - `.planning/ROADMAP.md` as edited by Task 2.
  </read_first>
  <action>
Close the todo and re-prove the untouched entries.

Move `correct-v128-py32-roadmap-prior-art.md` from `.planning/todos/pending/` to
`.planning/todos/completed/` with `git mv` so the history follows the file. Update its frontmatter
`status` from `pending` to the value this project's completed todos use, and append a short closing
note recording: the date, that CLOSE-03 discharged it in v1.23 Phase 130 across plans 130-04 and
130-05, which of its five numbered corrections were discharged **by deletion** (the whole v1.28 entry
went, taking items 1, 2 and 3's subject with it) versus **by correction** (the 999.23/999.24
dispositions), and that its sixth correction — the A-6/R-8 finding that `PORTING.md` exists only on the
two closed PRs — is discharged in the stub dispositions. Do not rewrite the todo's body; the record of
what was once believed is the point.

Then re-run the D-16 check: re-locate the v1.24, v1.25, v1.26 and v1.27 entries by version token and
re-hash each. All four must still equal plan 130-04's recorded values. Record the result in
`130-05-SUMMARY.md`, and record that the v1.30 entry's hash **did** change in this plan, attributing it
to Task 2's back-reference correction — so plan 130-16's D-16 record can state the v1.30 change as
expected rather than as a violation.

Finally, re-run plan 130-02's checker in `--explain` mode and record the change in the record count for
each needle label against plan 130-02's baseline, so the effect of two ROADMAP plans on the CLOSE-01
worklist is measured rather than assumed.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && test -f todos/completed/correct-v128-py32-roadmap-prior-art.md && test ! -f todos/pending/correct-v128-py32-roadmap-prior-art.md && python3 -c "
import hashlib,re,sys
want={'v1.24':'4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b','v1.25':'4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f','v1.26':'733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9','v1.27':'bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52'}
lines=open('ROADMAP.md').read().split('\n')
got={}
for i,l in enumerate(lines[:45]):
    m=re.match(r'^- \S+ \*\*(v1\.\d+)\b',l)
    if m and m.group(1) in want: got[m.group(1)]=hashlib.sha256((l+'\n').encode()).hexdigest()
bad=[k for k in want if got.get(k)!=want[k]]
print({k:('MATCH' if k not in bad else 'DRIFT') for k in sorted(want)})
sys.exit(1 if bad else 0)" && cd phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py --explain | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/completed/correct-v128-py32-roadmap-prior-art.md` exists and
      `.planning/todos/pending/correct-v128-py32-roadmap-prior-art.md` does not.
    - `git -C /workspaces log --follow --oneline -- .planning/todos/completed/correct-v128-py32-roadmap-prior-art.md`
      shows the file's pre-move history, proving `git mv` was used rather than a delete-and-create.
    - The moved file's frontmatter `status` is no longer `pending`, and it carries a dated closing note
      naming CLOSE-03, plans 130-04 and 130-05, and the by-deletion versus by-correction split across its
      five numbered items plus the sixth A-6/R-8 finding.
    - The moved file's original body text is byte-unchanged below the appended note — demonstrate with
      `git diff -M` showing only frontmatter and appended lines.
    - The four v1.24–v1.27 hashes still equal plan 130-04's recorded values; the verify command exits `0`.
    - `130-05-SUMMARY.md` records the v1.30 hash change with its cause, explicitly labelled expected.
    - `130-05-SUMMARY.md` records the per-label `--explain` record-count delta against plan 130-02's
      baseline, with at least the labels `third-stack-2c2ed10`, `porting-md-dual-slot`,
      `portability-macros-provides` and `host-44-unit-tests` named.
  </acceptance_criteria>
  <done>The todo is closed with its history intact and its discharge accounted item by item; the four untouched entries are re-proven; the worklist delta is measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a backlog stub -> `/gsd-review-backlog` and `/gsd-new-milestone` | Both read stub headings and dispositions. A stub still marked queued for delivered work inflates the open-item count and can be promoted a second time. |
| a by-number pointer -> the slot it names | After a renumber, an unrepaired pointer aims at a different milestone's content with no error anywhere. |
| a dated review record -> a future reader following its instructions | A record and an instruction look identical in markdown. Preserving one while disarming the other is the distinction this plan draws. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-22 | Spoofing | the `→ v1.28` stub pointers | high | mitigate | Task 1 enumerates every pointer site by measurement before Task 2 edits; Task 2's criteria assert no `v1.28` pointer survives in either stub heading. |
| T-130-23 | Repudiation / false assurance | `ROADMAP.md:1883`'s instruction | high | mitigate | An additive inline supersession note disarms the instruction without rewriting the dated record, and the four sibling paragraphs are pinned byte-unchanged by a `git diff -U0` criterion. |
| T-130-24 | Tampering | an invented 999.25 edit | medium | mitigate | Task 1 measures the phrase count and records a no-op verdict; `must_haves.prohibitions` forbid inventing an edit to satisfy D-15's wording. The `999.25` heading pointer is asserted byte-present. |
| T-130-25 | Tampering | the v1.24–v1.27 entries | high | mitigate | Task 3 re-verifies all four SHA-256 values against plan 130-04's record; a `git diff -U0` criterion pins hunks out of the list region except the v1.30 entry. |
| T-130-26 | Repudiation | the todo's discharge account | medium | mitigate | `git mv` preserves history; the closing note splits the five items into discharged-by-deletion and discharged-by-correction rather than asserting a blanket close. |
| T-130-27 | Denial of service (self-inflicted) | `ROADMAP.md` outside the edited regions | high | mitigate | Scoped `Edit` only; criteria forbid hunks at lines 2414 and 2468 (plan 130-06's sites) and outside the list region except v1.30. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package and runs only `python3`, `git` and `grep`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- Exactly one `999.23` and one `999.24` heading, neither queued, neither carrying a `v1.28` pointer, both naming Phases 123–130.
- `999.25`'s heading pointer byte-unchanged; the `the v1.29 slot immediately above is still occupied` sentence gone.
- `:1883` carries an additive supersession note; `:1747`, `:1877`, `:1879`, `:1887` byte-unchanged.
- The four v1.24–v1.27 hashes equal plan 130-04's values.
- The todo lives in `todos/completed/` with its history followed.
- `python3 check_record_corrections.py --explain` re-run; per-label counts recorded against the 130-02 baseline.
</verification>

<success_criteria>
- D-15 is landed, including its 999.25 half recorded as a measured no-op.
- No pointer in the file aims at the renumbered v1.28 slot as if it were the py32 port.
- Research Open Question 2 is settled and recorded: dated review paragraphs are history-exempt, with one instruction clause disarmed additively.
- The owning todo is closed with an item-by-item discharge account.
- The four untouched entries are re-proven byte-unchanged.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-05-SUMMARY.md` when done.
</output>
