---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 16
type: execute
wave: 9
depends_on: ["130-15"]
files_modified:
  - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [CLOSE-01, CLOSE-02, CLOSE-03, CLOSE-04]

must_haves:
  truths:
    - "CONSTRAINT 10 is satisfied: this is the ONLY plan in the phase that ticks a requirement id, and it ticks exactly CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04. Every other plan states in its own body that it ticks none."
    - "Every row in 130-NONREGRESSION.md is re-executed in THIS session against the tree as it now stands — nothing is copied from any of the fifteen prior plans' SUMMARY files, and where a prior SUMMARY made a claim it is re-checked independently."
    - "D-16's one-shot before/after SHA-256 proof for the four v1.24–v1.27 entries is recorded, together with the recorded reason no committed checker exists — the milestone's single deliberate exception to BASE-08."
    - "D-07's toolchain reproduction recipe lives here, not in the Validation Ceiling, because a claims-policy statement should not become a how-to."
    - "RESEARCH C-17's A-5 discharge is recorded as discharged at Phase 124 with 124-NONREGRESSION.md §F4d cited, not as fresh work."
    - "Every one of D-01 through D-17 has a coverage row stating where it landed and what evidence discharges it."
    - "D-04 lands: the meta gitlinks are ASSERTED to match the milestone-branch tips and re-bumped only where this phase's own commits moved a tip — not pinned."
    - "The four ROADMAP success criteria are each discharged with named evidence, including the criterion-4 naming discrepancy between 13X-DECISION.md and 130-DECISION.md."
  artifacts:
    - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-NONREGRESSION.md
  key_links:
    - "the CLOSE-01..04 ticks -> 130-NONREGRESSION.md's criterion-discharge section, which names the evidence per id"
    - "the meta gitlinks -> the two sub-repo milestone-branch tips (D-04 asserts, does not pin)"
    - "the observed cut tag -> 130-CHANNELS.md, which read it from gh release list; this plan fills the placeholders left in 130-LEDGER.md and the ROADMAP v1.23 entry"
  prohibitions:
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload. The v1.23 annotated tag and any merge toward main stay with /gsd-complete-milestone (D-04). Read-only gh and git only."
    - "Do NOT copy a result from a prior plan's SUMMARY. Re-execute. Where a prior SUMMARY claimed an exit code, a test count or a parsed literal, re-check it independently against the live tree and say so."
    - "Do NOT change one byte of the v1.24, v1.25, v1.26 or v1.27 ROADMAP entries. Their SHA-256 values are recorded in plan 130-04 and are re-verified here as D-16's after-state."
    - "Do NOT build a committed checker for the v1.24–v1.27 byte-unchanged claim. D-16 forbids it and requires the reason recorded so the absence is not read as an oversight."
    - "Do NOT tick any requirement id other than CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04. BASE, MERGE, VPP, CFG, HOST, REL and PCB are already Complete and are not re-touched."
    - "Do NOT iterate .planning/config.json's planning.sub_repos. Four entries exist and all four directories exist; two are py32 scratch worktrees. Name firestarter and firestarter_app explicitly."
    - "Do NOT read firestarter_app's pre-existing working-tree dirt as this phase's damage."
    - "Do NOT state or imply the §5(c) ship gate is satisfied, amended or resolved (D-17), and do NOT write that the pid.codes source warning is required by pid.codes' terms (RESEARCH C-6)."
    - "Do NOT claim the published image runs, boots or installs, and do NOT report a green claim-gate run as satisfying the honesty criterion — it is the mechanizable half only."
---

<objective>
Close the phase: re-execute every gate in this session, record the sweep, discharge the four success criteria, assert
the gitlinks, and tick the four CLOSE requirement ids.

This is the **only** plan permitted to change a requirement checkbox (CONSTRAINT 10). Executors marked multi-plan
requirements Complete prematurely four times in Phase 116, which is why every one of the other fifteen plans states in
its own body that it ticks nothing, and why this plan re-reads `REQUIREMENTS.md` before and after.

Three things live only here, named nowhere else in the phase:
- **D-07's toolchain reproduction recipe** — the ceiling states the claims policy; the how-to belongs here.
- **D-16's one-shot before/after SHA-256 proof** for the four v1.24–v1.27 entries, plus the recorded reason that no
  committed checker exists: *"these four entries never change"* is false as a standing invariant, so a permanent gate
  would either ship pre-obsolete or block a legitimate edit forever. It is the milestone's single deliberate
  exception to BASE-08.
- **RESEARCH C-17's A-5 discharge** — the operator-visible flash-constraint decision the research spine assigns to
  this phase is already satisfied at Phase 124, and recording it as discharged with its citation is the correct
  treatment rather than planning fresh work or leaving a spine deliverable silently unaddressed.

Purpose: leave a record a stranger can audit, and tick nothing that is not proven.
Output: `130-NONREGRESSION.md`, the four ticks, the gitlink assertion, and the filled tag placeholders.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-LEDGER.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CHANNELS.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
</context>

## Artifacts this phase produces

Created by **this** plan: `130-NONREGRESSION.md`.

The phase's full artifact set, for the record: `130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md`,
`130-RELEASE-NOTES-app.md` (the four contracted, scanned artifacts); `130-NONREGRESSION.md`, `130-HANDOFF.md`,
`130-CHANNELS.md` (three unscanned, per v1.22's precedent); `check_record_corrections.py`,
`test_check_record_corrections.py` and six files under `fixtures/`; new symbols in `check_permitted_claims.py`
(`_PHASE_130_DIRNAME`, `_CONTRACTED_ARTIFACT_NAMES`, a repointed `_DEFAULT_TARGETS`) and in
`test_check_permitted_claims.py` (`_snapshot_contracted_artifacts`, `_assert_no_new_contracted_artifacts`,
`test_side_effect_guard_fires_on_a_new_contracted_artifact`); and the two `usb_cdc.c` `#define` values
(`FIRESTARTER_USB_VID` → `0x1209U` at line 20, `FIRESTARTER_USB_PID` → `0x0001U` at line 24).

## Multi-source coverage audit

Every source item, with the plan that covers it. No item is MISSING.

| Source | Item | Covered by |
|---|---|---|
| GOAL | The planning record carries every research correction | 130-02, 130-06, 130-07, 130-08, 130-09, 130-10 |
| GOAL | An honesty ledger pairs each permitted claim with its non-claim | 130-11 |
| GOAL | The ROADMAP slot renumber lands | 130-04, 130-05 |
| GOAL | Pushing `beta` is a deliberate, recorded decision | 130-13, 130-14, 130-15 |
| REQ | CLOSE-01 | 130-02, 130-06, 130-07, 130-08, 130-09, 130-10, 130-16 |
| REQ | CLOSE-02 | 130-01, 130-03, 130-11, 130-12, 130-16 |
| REQ | CLOSE-03 | 130-04, 130-05, 130-16 |
| REQ | CLOSE-04 | 130-13, 130-14, 130-15, 130-16 |
| RESEARCH | C-1 decision parse regression | 130-01 Task 3 |
| RESEARCH | C-2 `_DEFAULT_TARGETS` repoint | 130-01 Task 1 |
| RESEARCH | C-3 side-effect guard | 130-01 Task 2 |
| RESEARCH | C-4 §5(a)+§5(d) footprint, comment below line 25 | 130-03 |
| RESEARCH | C-5 / D-17 ship-gate residual | 130-03, 130-11, 130-13 |
| RESEARCH | C-6 pid.codes SHOULD not MUST | 130-03, 130-08, 130-11, 130-12 |
| RESEARCH | C-7 R-10 already discharged; history-awareness | 130-02, 130-07, 130-08 |
| RESEARCH | C-8 self-reference exemption | 130-02, 130-06 |
| RESEARCH | C-9 three live no-VTOR sites | 130-06, 130-10 |
| RESEARCH | C-10 `PROJECT.md:836` footer | 130-07 |
| RESEARCH | C-11 CLOSE-03 before CLOSE-01's ROADMAP sweep | wave order: 130-04 (w1), 130-05 (w2), 130-06 (w3) |
| RESEARCH | C-12 line 33 carries no marker | 130-04 |
| RESEARCH | C-13 app pre-cut suite is a gate | 130-13 |
| RESEARCH | C-14 `paths-ignore` will not suppress | 130-13, 130-14 |
| RESEARCH | C-15 b15 derivable but read | 130-13, 130-14, 130-15 |
| RESEARCH | C-16 §Future Requirements is `:109-128`, eight deferrals | 130-11 |
| RESEARCH | C-17 A-5 discharged at Phase 124 | 130-07, 130-16 |
| RESEARCH | C-18 gh#20 / gh#18 post-import | 130-11, 130-12, 130-15 |
| RESEARCH | Open Q2 review-history exemption + `:1883` note | 130-05 |
| RESEARCH | Open Q3 999.25 no-op verified | 130-05 |
| CONTEXT | D-01 | 130-13, 130-14 |
| CONTEXT | D-02 | 130-12 |
| CONTEXT | D-03 | 130-15 |
| CONTEXT | D-04 | 130-16 |
| CONTEXT | D-05 | 130-07, 130-08, 130-09 |
| CONTEXT | D-06 | 130-10 |
| CONTEXT | D-07 | 130-10, 130-16 |
| CONTEXT | D-08 | 130-02 |
| CONTEXT | D-09 | 130-11 |
| CONTEXT | D-10 | 130-11 |
| CONTEXT | D-11 | 130-03 |
| CONTEXT | D-12 | 130-11 |
| CONTEXT | D-13 | 130-04 |
| CONTEXT | D-14 | 130-04 |
| CONTEXT | D-15 | 130-05 |
| CONTEXT | D-16 | 130-04, 130-16 |
| OPERATOR | D-17 | 130-03, 130-11, 130-13 |

Exclusions, not gaps: everything in CONTEXT `<deferred>` — the stray b12 cleanup, a fresh rehearsal dispatch,
compacting v1.30 into v1.29, a committed checker for the byte-unchanged claim, blocking the py32 asset, the pid.codes
PR, the eight FUT deferrals, Phase 129's two open hardware questions, F-10's part-selection consequence, the v1.30
outward-facing debt, and backlog stubs 999.22 and 999.25.

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every gate in this session and write the sweep record</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md` lines 1-40 plus its heading list — the opening
      shape to copy: the header block with per-repo branch and HEAD SHA, the blockquote ceiling paragraph, and the
      **re-execution pledge** stating that every row was executed in this session against the trees as they now
      stand, with nothing copied from prior plans' SUMMARY files.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` — its per-repo
      sub-sections under the locally-provable heading, its `### Criterion N` structure, its `## Decision coverage`
      section, its `## Claim ceiling` and `## Sweep Summary`, and its ARM byte-identity row as the shape for a
      hash-based before/after record.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CHANNELS.md` — the observed cut tag
      and the self-verifying-facts section, so this sweep lifts them rather than re-deriving.
    - `130-DECISION.md` — the twelve pre-flight sections; this sweep re-measures them on the post-push tree.
    - `130-03-SUMMARY.md` — the ARM pass result or its recorded gap, and the measured tool versions.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" as plan 130-10 narrowed it — the claims policy this sweep
      obeys.
  </read_first>
  <action>
Write the sweep, re-executing everything. Copy `128-NONREGRESSION.md`'s opening shape exactly: the header block
naming each repo's branch and HEAD SHA at this sweep plus the meta branch; the blockquote ceiling paragraph stating
that no PY32F071 hardware exists, that nothing in this milestone has ever run on this silicon and nothing in it can,
and that everything recorded is about **publication** — that a file with a particular name carrying a particular
version string became a downloadable release asset, and nothing about the published image running, booting or
installing; then the **re-execution pledge**, stating that every row below was executed in this session against the
trees exactly as they now stand and that where a prior SUMMARY claimed an exit code, a test count or a parsed
literal, this document re-checked it independently.

**Section — the claim, as precise statements.** One numbered sentence per thing this phase claims, at the strength
the evidence gives it.

**Section — locally provable, executed now.** Per-repo sub-sections. Re-run and tabulate, each with its command and
its measured result:
- `firestarter`: `python3 -m pytest tests/ -q`; `pio test -e native` at the BASE-01 case **and** suite counts;
  `FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q` at 41 legs, and
  note in the row that this module runs in **no CI leg** so the local run is the enforcement — and do not imply CI
  coverage.
- `firestarter_app`: `python3 -m pytest tests/ -q`; `python3 tools/catalog/codegen.py --catalog
  tools/catalog/messages.toml --check`; `git diff --exit-code firestarter/messages.py`. Record that CI pins Python
  3.11 while these runs are on 3.12 (research assumption A2).
- meta `.planning/` checkers: `check_record_corrections.py` in default mode and its suite;
  `check_permitted_claims.py` in default mode and its suite. Record the CLOSE-01 checker's `--explain` per-verdict
  tally, so the green is shown to be green for the right reason rather than because the needles stopped matching.
- the decision parser against `130-CONTEXT.md`, expecting a parseable outcome with 16 ids.
- `usb_cdc.c` lines 20 and 24 carrying the two new values, and lines 19 and 23 still being the `#ifndef` guards.

**Section — D-16's one-shot proof.** Re-locate the four v1.24–v1.27 ROADMAP entries by version token, re-hash each,
and compare against plan 130-04's recorded before-values. Record the before line numbers, the after line numbers and
the single shared hash per entry. Then record the path-scoped `git diff -U0 -- .planning/ROADMAP.md` output over the
phase's full range and state that no hunk touches the four, citing the hash equality as the evidence. Record D-16's
reason that **no committed checker exists**, in full, and that this is the milestone's single deliberate exception to
BASE-08. Record the two changes a reader will see that are **not** violations: the v1.30 back-reference correction
(plan 130-05) and the line-count drop from collapsing two entries into one (plan 130-04).

**Section — D-07's toolchain reproduction recipe.** The exact commands to reproduce a local ARM build, drawn from
`firestarter/.github/actions/build-py32f071/action.yml`: the configure `cmake -S platform/py32f071 -B
build/py32f071 -G Ninja -DCMAKE_BUILD_TYPE=Release` and the build `cmake --build build/py32f071`, plus the tool
versions measured. Record the standing rule the recipe operates under: a local build supports **delta** and
byte-identity claims only, never an absolute size, because local and CI compilers differ — `text=27260` local
against `text=27344` CI. Record the D-11-specific note that byte-identity is the **wrong** criterion for that change
because the descriptor bytes change by design, so the permitted claim is a confined delta. Also record that
RESEARCH's Environment-Availability row listed the toolchain absent and it measured **present** at plan time, with
the three version strings.

**Section — A-5, discharged at Phase 124.** Record RESEARCH C-17: the operator-visible flash-constraint decision the
research spine assigns to Phase 130 is already satisfied. Cite `REQUIREMENTS.md` Operator Decision 4's restatement
*"Leonardo flash must not grow; Uno-class growth ≤ 64 B, recorded"*, MERGE-05 as its encoding, and
`124-NONREGRESSION.md` §F4d's independent 328PB build closing SUMMARY:345's single-source gap. Record it as
discharged with the citation, explicitly not as fresh work.

**Section — recorded rulings and no-ops.** Research Open Question 2's history-exemption ruling for the dated
review-pass paragraphs plus the one inline supersession note at `ROADMAP.md:1883`; Open Question 3's verified
999.25 no-op; `122-DECISION.md`'s items 7 and 8 recorded as no-ops because both repos measured 0 behind; and the six
R-Ns with zero live occurrences (R-4, R-12, R-13, R-16, R-17, R-18) recorded as **discharged with evidence** rather
than given hollow checker legs, plus R-10 recorded as needing no substantive correction per C-7.

**Section — what this phase does NOT claim.** The published image running, booting or installing. Any silicon
behaviour. That the §5(c) ship gate is satisfied, amended or resolved. That a green claim-gate run satisfies the
honesty criterion — it is the mechanizable half only, and D-02's operator review is the other half. That the
community inbox is clear. That the pid.codes source warning is required rather than asked for.
  </action>
  <verify>
    <automated>cd /workspaces && echo "=== fw ===" && (cd firestarter && python3 -m pytest tests/ -q 2>&1 | tail -1; FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1; sed -n '19,25p' platform/py32f071/src/usb_cdc.c) && echo "=== app ===" && (cd firestarter_app && python3 -m pytest tests/ -q 2>&1 | tail -1; python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check; git diff --exit-code firestarter/messages.py && echo "codegen_clean") && echo "=== checkers ===" && (cd .planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py; echo "record_exit=$?"; python3 -m pytest test_check_record_corrections.py -q 2>&1 | tail -1) && (cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py; echo "claim_exit=$?"; python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -1) && echo "=== D-16 ===" && python3 -c "
import hashlib,re,sys
want={'v1.24':'4b83c9e1c980355ee5f75985419c9e2f3c99b4f4dc9d69e282bdf61b21b4630b','v1.25':'4bc536d5783de3cf1a21ba7b60260063141fcb3620338b5643b66b1456cfa61f','v1.26':'733b81ddcc45308bcc4c20d643e67cc09263315a0585f80cb0058a8c7f9dddf9','v1.27':'bb8cc73acdbe977f8d95a3550e17b2f4bd7ea21d91167359c17484bd8d1d3f52'}
lines=open('.planning/ROADMAP.md').read().split('\n')
got={}
for i,l in enumerate(lines[:45]):
    m=re.match(r'^- \S+ \*\*(v1\.\d+)\b',l)
    if m and m.group(1) in want: got[m.group(1)]=(i+1,hashlib.sha256((l+'\n').encode()).hexdigest())
bad=[k for k in want if got.get(k,('',''))[1]!=want[k]]
for k in sorted(want): print(k,'line',got.get(k,('?',))[0],'MATCH' if k not in bad else 'DRIFT')
sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `130-NONREGRESSION.md` opens with the header block naming both sub-repo branches with their HEAD SHAs at this
      sweep plus the meta branch, the ceiling blockquote, and the re-execution pledge naming the fifteen prior plans
      whose SUMMARY results were **not** copied.
    - Every gate row carries its command and its measured result: `firestarter` `pytest tests/`, `pio test -e native`
      at the BASE-01 counts, the 41-leg sync gate; `firestarter_app` `pytest tests/` plus both codegen gates; both
      `.planning/` checkers in default mode plus both suites; the decision parser at 16 ids; and the `usb_cdc.c`
      line-20/line-24 assertion.
    - The sync-gate row states that the module runs in no CI leg and does **not** imply CI coverage.
    - The `firestarter_app` rows record that CI pins Python 3.11 while these runs are on 3.12.
    - The CLOSE-01 checker's `--explain` per-verdict tally is recorded, with a non-zero total and zero `unlabeled`.
    - D-16's proof records the four before/after hashes with before and after line numbers, the path-scoped diff, the
      no-checker reason in full, and the two non-violations. The verify command's four MATCH verdicts pass.
    - The D-07 recipe section carries the exact configure and build commands, the three measured tool versions, the
      delta-and-byte-identity-only rule with the `text=27260` versus `text=27344` figures, the note that
      byte-identity is the wrong criterion for D-11's change, and the drift of RESEARCH's Environment-Availability
      row.
    - The A-5 section cites Operator Decision 4's restatement, MERGE-05 and `124-NONREGRESSION.md` §F4d, and
      describes A-5 as discharged at Phase 124.
    - The rulings-and-no-ops section names Open Question 2's ruling with the `:1883` note, Open Question 3's verified
      no-op, `122-DECISION.md` items 7 and 8 as no-ops, and R-4, R-12, R-13, R-16, R-17, R-18 and R-10 with their
      dispositions.
    - The does-NOT-claim section names all seven items listed in the action.
    - `grep -ci 'ship gate.*\(satisfied\|amended\|resolved\)' 130-NONREGRESSION.md` returns `0`, and
      `grep -c 'required by pid.codes' 130-NONREGRESSION.md` returns `0`.
  </acceptance_criteria>
  <done>Every gate is re-executed in this session and recorded with its command; D-16, D-07 and A-5 each have their dedicated section; the non-claims are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-01…D-17 coverage, discharge the four criteria, assert the gitlinks and fill the tag placeholders</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-NONREGRESSION.md, .planning/ROADMAP.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `130-NONREGRESSION.md` as written by Task 1.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CONTEXT.md` §`<decisions>` — all
      sixteen decisions, and this plan set's `must_haves` for D-17.
    - `.planning/ROADMAP.md` Phase 130's four success criteria at lines 2461-2476 as plan 130-06 left them,
      including criterion 4's `13X-DECISION.md` naming and criterion 1's `recordscan:allow` marker.
    - `.planning/ROADMAP.md`'s `## Milestones` v1.23 entry as plan 130-04 wrote it — the tag placeholder to fill.
    - `130-LEDGER.md`'s identity header — the published-cut-tag placeholder to fill.
    - `130-CHANNELS.md` — the observed cut tag and the self-verifying facts.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` §"Decision coverage" —
      the coverage-table shape to copy.
    - `.planning/PROJECT.md` and `.planning/STATE.md` — where a phase-complete entry goes, matching how Phases 128
      and 129 recorded theirs.
  </read_first>
  <action>
Complete the record and fill what only the observed tag can fill.

**Decision coverage, D-01 through D-17.** One row per decision: the id, a one-line restatement, the plan that landed
it, and the evidence that discharges it. Seventeen rows — D-01…D-16 from `130-CONTEXT.md` plus **D-17**, the
operator's locked ship-gate decision delivered during planning rather than in CONTEXT, which is why the decision
parser correctly reports 16 ids for that file and not 17. State that accounting explicitly so a reader does not read
16 as a missing decision.

**Criterion discharge.** One `### Criterion N` sub-section per ROADMAP success criterion:
1. All of R-1…R-18 corrected, with zero remaining occurrences outside a labeled correction or history block — cite
   the default-mode `check_record_corrections.py` run, its `--explain` tally, and the per-file attribution from plan
   130-06. Name the six R-Ns with no live subject as discharged with evidence, and R-10 as needing no substantive
   correction per C-7.
2. The honesty ledger pairing each permitted claim with its non-claim, covering the four named minimum items — cite
   `130-LEDGER.md`'s tier sections and the four items by name.
3. The renumber landing in the same change as the prior-art correction, with v1.24–v1.27 byte-unchanged — cite plans
   130-04 and 130-05 and Task 1's D-16 proof.
4. The decision artifact committed before any push, the tag read not computed, and PyPI verified directly — cite
   `130-DECISION.md`'s commit timestamp against `origin/beta`'s move, `130-CHANNELS.md`'s quoted `gh release list`
   line, and the clean-venv `pip download`. **Address the naming discrepancy**: the criterion names a
   `13X-DECISION.md` artifact, which was a placeholder in the roadmap's phase template; the real artifact is
   `130-DECISION.md`. Record that plainly rather than leaving a reader to wonder whether a differently-named file was
   expected.

**Gitlink assertion (D-04).** Run `git ls-tree HEAD firestarter firestarter_app` in the meta repo and compare each
against that sub-repo's milestone-branch tip. D-04 **asserts** they match and re-bumps only where this phase's own
commits moved a tip — plan 130-03 moved the firmware tip, so a bump is expected there. Record the before and after
gitlink values, name the plan whose commits moved the tip, and stage the bump. Do **not** pin them unchanged; this
milestone bumps gitlinks in-phase (Phases 125/128/129), the opposite of v1.22's pinned model. Record that the
`v1.23` annotated tag and any merge toward `main` stay with `/gsd-complete-milestone`.

**Fill the tag placeholders.** Using the observed tag from `130-CHANNELS.md` — read, never computed — fill:
- `130-LEDGER.md`'s identity-header published-cut-tag field, marking it **observed, not predicted**, and citing the
  `gh release list` line it came from;
- the `## Milestones` `✅ v1.23 PY32F071 Integration` entry's cut-tag placeholder, plus the channel facts:
  the firmware prerelease carrying the four `.hex` assets including `firestarter_py32f071.hex`, PyPI carrying the
  host app, and no stable release.
After editing `130-LEDGER.md`, re-run `check_permitted_claims.py` in default mode: it must still exit 0 across all
four artifacts.

**Record the phase in PROJECT.md and STATE.md.** Add a phase-complete entry to each, matching how Phases 128 and 129
recorded theirs: what shipped, the requirement tally, the observed cut tag, the channel verification, and the three
carried residuals (D-17's ship-gate tension, the ARM pass's status, and the non-empty inbox). Use scoped `Edit`
operations. Do not touch `STATE.md`'s YAML frontmatter — the orchestrator owns those fields — and re-read both files
immediately before editing.

Finish `130-NONREGRESSION.md` with a `## Claim ceiling` section and a `## Sweep Summary`, both in
`129-NONREGRESSION.md`'s shape.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
s=open('.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-NONREGRESSION.md').read()
import re
ids=set(re.findall(r'\bD-(\d\d)\b', s))
missing=[f'D-{i:02d}' for i in range(1,18) if f'{i:02d}' not in ids]
assert not missing, ('missing decision coverage rows', missing)
for k in ('Criterion 1','Criterion 2','Criterion 3','Criterion 4','13X-DECISION','ls-tree','F4d','Claim ceiling','Sweep Summary'):
    assert k in s, k
print('OK coverage + criteria + gitlink + ceiling')" && git ls-tree HEAD firestarter firestarter_app && (cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py; echo "claim_exit=$?")</automated>
  </verify>
  <acceptance_criteria>
    - `130-NONREGRESSION.md` contains a coverage row for every one of D-01 through **D-17**; the verify command fails
      if any is absent. The D-17 row states it was delivered during planning rather than in `130-CONTEXT.md`, which
      is why the decision parser correctly reports 16 ids for that file.
    - Four `### Criterion N` sub-sections exist, each citing named evidence rather than asserting discharge.
    - Criterion 4's sub-section addresses the `13X-DECISION.md` versus `130-DECISION.md` naming discrepancy
      explicitly.
    - `git ls-tree HEAD firestarter firestarter_app` output is recorded before and after; each gitlink equals its
      sub-repo's milestone-branch tip after the bump, and the plan whose commits moved a tip is named.
    - `130-LEDGER.md`'s published-cut-tag field carries the observed tag, marked observed and not predicted, with the
      `gh release list` line cited. `check_permitted_claims.py` in default mode still exits `0` after that edit.
    - The `## Milestones` `✅ v1.23 PY32F071 Integration` entry carries the observed cut tag and the channel facts,
      and the four v1.24–v1.27 hashes still match plan 130-04's record after this edit.
    - `.planning/PROJECT.md` and `.planning/STATE.md` each carry a phase-130-complete entry naming the requirement
      tally, the observed cut tag, the channel verification and the three carried residuals.
    - `git -C /workspaces diff -- .planning/STATE.md` shows no hunk inside the YAML frontmatter block.
    - `130-NONREGRESSION.md` ends with a `## Claim ceiling` section and a `## Sweep Summary`.
  </acceptance_criteria>
  <done>Every decision including D-17 has a coverage row, all four criteria are discharged with named evidence, the gitlinks are asserted and bumped where this phase moved a tip, and the observed tag is filled everywhere a placeholder waited.</done>
</task>

<task type="auto">
  <name>Task 3: Tick CLOSE-01…CLOSE-04 — the only requirement change in the phase</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Close" lines 100-106 — the four CLOSE requirement lines, and the Traceability
      table's `CLOSE-01 … CLOSE-04 | Phase 130 | Pending` row at line 168.
    - `130-NONREGRESSION.md` as completed in Tasks 1 and 2 — the criterion-discharge sections are the evidence each
      tick cites.
    - `.planning/REQUIREMENTS.md` as plan 130-10 left it — confirm plan 130-10 changed prose only and left all four
      checkboxes unchecked, which is the Phase-116 guard holding.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` — how Phase 129's
      closing plan phrased its own tick evidence, including the honest amendment qualifiers on PCB-03 and PCB-04.
  </read_first>
  <action>
Before touching anything, verify the guard held: read `REQUIREMENTS.md` and confirm all four CLOSE lines still begin
unchecked and that no other requirement's state changed during this phase. Record the pre-tick state. If any CLOSE id
is already ticked, that is a Phase-116-class failure by an earlier plan — record it as a finding, identify the plan
from `git log -p` on the file, and say so rather than quietly accepting it.

Then tick exactly the four: CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04. For each, append a short evidence clause in the
style the already-Complete requirements use — a pointer to `130-NONREGRESSION.md`'s criterion section plus the one
qualifier that matters:
- **CLOSE-01** — cite the default-mode `check_record_corrections.py` run and the per-file attribution. Qualify: six
  R-Ns had no live occurrence and are recorded as discharged with evidence rather than given checker legs, and R-10
  needed no substantive correction because its remaining occurrences are historically accurate.
- **CLOSE-02** — cite `130-LEDGER.md`'s tier sections and the four named minimum-coverage items. Qualify: the claim
  gate's green run is the **mechanizable half only**; D-02's operator wording review is the other half and it
  preceded posting.
- **CLOSE-03** — cite plans 130-04 and 130-05 and D-16's hash proof. Qualify: the v1.29 number is left vacant by
  design, v1.30 keeps its number by its own instruction, and D-15's 999.25 half was verified to have no subject and
  is recorded as a no-op.
- **CLOSE-04** — cite `130-DECISION.md`'s commit timestamp, `130-CHANNELS.md`'s quoted observed tag, the py32
  asset assertion on the real release, and the clean-venv PyPI check. Qualify: the merge, the push, the manual
  `publish.yml` dispatch and the release-body posting were **operator acts**, structurally excluded from every plan
  in this phase; and the `v1.23` tag and any merge toward `main` stay with `/gsd-complete-milestone`.

Also update the Traceability table's `CLOSE-01 … CLOSE-04 | Phase 130` row from `Pending` to Complete, in the same
form the other rows use, citing `130-NONREGRESSION.md`.

Do not touch any other requirement's checkbox or status. Do not amend PCB-04. Do not add a new requirement.
  </action>
  <verify>
    <automated>cd /workspaces/.planning && python3 -c "
s=open('REQUIREMENTS.md').read()
lines=s.split('\n')
for rid in ('CLOSE-01','CLOSE-02','CLOSE-03','CLOSE-04'):
    m=[l for l in lines if rid in l and l.lstrip().startswith('- [')]
    assert m, rid
    assert m[0].lstrip().startswith('- [x]'), (rid,'NOT TICKED')
    assert '130-NONREGRESSION' in m[0], (rid,'no evidence citation')
# every other requirement must remain [x] as before; count unchecked
unchecked=[l for l in lines if l.lstrip().startswith('- [ ] **')]
assert not unchecked, ('unexpected unchecked requirement', unchecked[:3])
trace=[l for l in lines if 'CLOSE-01' in l and '|' in l]
assert trace and 'Pending' not in trace[0], ('traceability row still Pending', trace)
print('OK four ticked, none left unchecked, traceability updated')"</automated>
  </verify>
  <acceptance_criteria>
    - The pre-tick state is recorded: all four CLOSE lines were unchecked before this task, and no other requirement's
      state changed during the phase. If a CLOSE id was already ticked, the finding is recorded with the offending
      plan identified from `git log -p`.
    - All four CLOSE requirement lines now begin ticked, and each carries an evidence clause citing
      `130-NONREGRESSION.md` plus its named qualifier.
    - No requirement line in `REQUIREMENTS.md` remains unchecked — the verify command fails otherwise.
    - The Traceability table's `CLOSE-01 … CLOSE-04 | Phase 130` row no longer reads `Pending` and cites
      `130-NONREGRESSION.md`.
    - `git -C /workspaces diff -- .planning/REQUIREMENTS.md` shows changes confined to the four CLOSE lines and the
      one Traceability row; no hunk touches PCB-04, and no other checkbox character changed.
    - Running the mechanical scan from plan 130-14 across all sixteen `130-*-PLAN.md` files still returns an empty
      violation list for `git push`, `git merge`, `git tag`, `gh workflow run`, `gh release create|edit|delete` and
      `twine upload`. Record the result as the phase's final structural-gate proof.
    - `git -C firestarter rev-parse origin/beta` and `git -C firestarter_app rev-parse origin/beta` are recorded, and
      no push, merge or tag was executed by this plan.
    - `cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py` still
      exits `0`, and `python3 -m pytest test_check_permitted_claims.py -q` still reports `11 passed`, on the final
      tree.
  </acceptance_criteria>
  <done>The four CLOSE ids are ticked with cited evidence and honest qualifiers, nothing else moved, and the structural gate is proven one last time across the whole plan set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a requirement checkbox -> the project's own account of what shipped | A tick is the durable claim. Once set, later readers treat it as settled, so it must cite evidence and carry its qualifiers. |
| a prior plan's SUMMARY -> this sweep's rows | A SUMMARY is a claim by an agent about a run it performed. Copying it forward compounds any error; re-executing does not. |
| the meta gitlinks -> the sub-repo tips they name | An unasserted gitlink drifts silently; v1.22 learned that and had to correct it out of band. |
| this plan -> every other requirement in the file | This is the only plan opening `REQUIREMENTS.md` with permission to change state, so a stray edit here is unguarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-90 | Elevation of privilege | premature or over-broad requirement ticking | critical | mitigate | CONSTRAINT 10: this is the only plan permitted to tick, and it ticks exactly four. Task 3 records the pre-tick state, identifies any earlier violator from `git log -p`, and a criterion confines the diff to the four lines plus one Traceability row with no other checkbox change. |
| T-130-91 | Repudiation | copied rather than re-executed results | critical | mitigate | The re-execution pledge is mandatory in the opening block and names the fifteen prior plans whose SUMMARY results were not copied; every gate row carries its own command and measured result. |
| T-130-92 | Repudiation / false assurance | a green gate read as more than it is | high | mitigate | The CLOSE-01 checker's `--explain` tally is recorded with a non-zero total, so green-by-non-matching is distinguishable; the CLOSE-02 tick carries the mechanizable-half-only qualifier; the sync-gate row states it runs in no CI leg. |
| T-130-93 | Tampering | the meta gitlinks | high | mitigate | D-04 asserts and bumps where this phase's own commits moved a tip, with before and after values recorded and the moving plan named; pinning is explicitly rejected. |
| T-130-94 | Elevation of privilege | the tag and the merge toward `main` | high | mitigate | Both are recorded as staying with `/gsd-complete-milestone`; `must_haves.prohibitions` forbid `git tag` and every remote write, and the final structural-gate scan across all sixteen plans is an acceptance criterion. |
| T-130-95 | Spoofing | a computed tag written into the durable record | high | mitigate | The tag filled into `130-LEDGER.md` and the ROADMAP entry comes from `130-CHANNELS.md`'s quoted `gh release list` line and is marked observed, not predicted. |
| T-130-96 | Tampering | the v1.24–v1.27 entries during the final ROADMAP edit | medium | mitigate | The four hashes are re-verified after Task 2's `## Milestones` edit as an acceptance criterion. |
| T-130-97 | Tampering | `STATE.md`'s orchestrator-owned frontmatter | medium | mitigate | A criterion asserts no diff hunk inside the frontmatter block; scoped `Edit` with a mandated re-read. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package. It runs `python3`, `pytest`, `pio test -e native`, read-only `git` and read-only `gh`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `firestarter`: `pytest tests/ -q`, `pio test -e native` at BASE-01 counts, and the 41-leg sync gate — all green, all re-run here.
- `firestarter_app`: `pytest tests/ -q` plus both codegen gates — green, with the 3.11-versus-3.12 note recorded.
- `check_record_corrections.py` default mode → exit 0 with a non-zero `--explain` total and zero `unlabeled`; its suite `0 failed`.
- `check_permitted_claims.py` default mode → exit 0 across all four contracted artifacts; its suite `11 passed`.
- The decision parser against `130-CONTEXT.md` → parseable, 16 ids.
- D-16: the four v1.24–v1.27 hashes MATCH plan 130-04's record, re-checked after Task 2's ROADMAP edit.
- Coverage rows present for all seventeen of D-01…D-17; four `### Criterion N` sub-sections with named evidence.
- `git ls-tree HEAD firestarter firestarter_app` recorded before and after; each gitlink equals its sub-repo tip.
- All four CLOSE ids ticked with citations; no other requirement state changed; Traceability row updated.
- The mechanical structural-gate scan across all sixteen plans → empty violation list.
</verification>

<success_criteria>
- Every gate is re-executed in this session and recorded with its command and result; nothing is copied forward.
- D-16, D-07 and A-5 each have their dedicated section, with D-16's no-checker reason recorded in full.
- All seventeen decisions have coverage rows, with D-17's out-of-CONTEXT provenance stated.
- All four ROADMAP criteria are discharged with named evidence, including criterion 4's artifact-naming discrepancy.
- The gitlinks are asserted and bumped where this phase moved a tip; the tag and the `main` merge stay with `/gsd-complete-milestone`.
- CLOSE-01…CLOSE-04 are ticked with cited evidence and honest qualifiers, and nothing else in `REQUIREMENTS.md` moved.
- The structural gate is proven one final time across all sixteen plans.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-16-SUMMARY.md` when done.
</output>
