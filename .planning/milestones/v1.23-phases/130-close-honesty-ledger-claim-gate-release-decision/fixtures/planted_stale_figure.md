<!-- fixture for check_record_corrections.py test suite: never scanned as a real planning record -->
<!-- plants R-10's flash-headroom needle on an ordinary unlabeled prose line, with no other needle present, so the FAIL is attributable to exactly one label. NOTE: this comment deliberately avoids repeating the planted figure itself, so the descriptive comment cannot accidentally re-trigger the needle it is describing. -->

# Fixture: planted stale figure

This is ordinary prose describing the milestone's flash budget. The Leonardo
target has 2992 B of flash headroom remaining, a figure that predates a later
phase's own delta and should have been corrected in place or explicitly
marked as historically accurate. Nothing else on this page carries any of
the checker's other eleven needles.
