<!-- fixture for check_record_corrections.py test suite: never scanned as a real planning record -->
<!-- plants R-10's flash-headroom needle inside a block whose opener is a neutral bold lead-in the checker does NOT recognise as a label -- the mislabeled-block trap: the author believed this paragraph was flagged as an exemption, and it is not, so the gate must still FAIL. NOTE: this comment deliberately avoids repeating the planted figure itself. -->

# Fixture: mislabeled block

**Note:** the paragraph below describes a stale headroom figure that the
author believed was already marked as a correction.

1. The Leonardo target has 2992 B of flash headroom remaining, and this
   numbered item sits directly beneath the bold lead-in above, exactly the
   shape a real `⚠ CORRECTION` block would use -- except the lead-in above
   is a plain `**Note:**`, not one of the recognised label tokens.
