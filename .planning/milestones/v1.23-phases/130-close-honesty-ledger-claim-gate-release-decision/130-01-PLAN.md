---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py
  - .planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py
autonomous: true
requirements: [CLOSE-02]

must_haves:
  truths:
    - "A default-mode run of check_permitted_claims.py resolves its four targets inside the Phase 130 directory, not the Phase 123 directory (RESEARCH C-2)."
    - "test_check_permitted_claims.py reports 10 passed / 0 failed, with the D-15 arming leg green (RESEARCH C-3 pre-existing RED closed)."
    - "The side-effect guard survives the four contracted artifacts legitimately existing in the real Phase 130 directory, and is proven still able to fire by a dedicated reachability test."
    - "130-CONTEXT.md still parses: extractDecisions returns outcome != could-not-parse with 16 ids D-01..D-16 (RESEARCH C-1 regression re-assert)."
    - "D-15 all-or-nothing arming still applies only to the default target set, so a three-of-four close is still a hard failure."
  artifacts:
    - .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py
    - .planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py
  key_links:
    - "_DEFAULT_TARGETS -> the four 130-*.md names inside .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/"
    - "the side-effect guard -> the four contracted names only, compared before/after, never as an absolute-absence assertion"
  prohibitions:
    - "Do NOT resolve targets via explicit argv or FIRESTARTER_CLAIMSCAN_TARGETS instead of repointing _DEFAULT_TARGETS: arming applies only to the default set, so D-15's all-or-nothing guarantee would be lost (RESEARCH C-2 resolution (b), rejected)."
    - "Do NOT delete or weaken the side-effect guard at test_check_permitted_claims.py lines 299-304. Fix the locator only, with a RED-preserving proof (Phase 129 lesson)."
    - "Do NOT narrow FORBIDDEN_PATTERNS, PY32_TOKEN_RE, PROXIMITY_WINDOW or REQUIRED_CAVEAT_PATTERN. This plan changes target resolution and one test locator, nothing about what counts as a violation."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked in .planning/REQUIREMENTS.md; only plan 130-16 may tick them."
    - "Do NOT run git push, git merge into beta, git tag, gh workflow run, gh release create/edit/delete, or twine upload."
---

<objective>
Repair the two broken mechanisms in the milestone's only outward-facing overclaim gate, before any
closing artifact is written.

RESEARCH C-2: `check_permitted_claims.py:74` sets `_HERE` to **Phase 123's** directory and joins the
four `130-*` artifact names onto it. If Phase 130 writes its artifacts where CONTEXT says to write
them and the scanner runs in default mode, it prints `UNARMED:` and exits **0** — a green run that
scanned nothing.

RESEARCH C-3: the paired pytest suite is **already RED** (`1 failed, 9 passed`) because the
side-effect guard globs `130-*.md` in the Phase 130 directory, which now holds `130-CONTEXT.md`. The
failure predates this phase and gets worse once the real artifacts land.

Purpose: make the claim gate able to see the artifacts it was contracted to scan, and make its own
suite green and *provably* green, so every later plan's claim-gate assertion means something.
Output: a repointed `_DEFAULT_TARGETS`, an updated module docstring, a differential side-effect guard
with a dedicated reachability test, and a re-asserted decision parse.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md
@.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py
@.planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py
</context>

## Artifacts this phase produces

New files (across the whole phase, for orientation — this plan creates none of them):
`130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`,
`130-NONREGRESSION.md`, `130-HANDOFF.md`, `130-CHANNELS.md`, `check_record_corrections.py`,
`test_check_record_corrections.py`, `fixtures/` (six fixture files).

New symbols created by **this** plan:
- `_DEFAULT_TARGETS` in `check_permitted_claims.py` — repointed at the Phase 130 directory (existing
  name, new resolved values).
- `_PHASE_130_DIRNAME` — new module-level string constant in `check_permitted_claims.py` naming
  `130-close-honesty-ledger-claim-gate-release-decision`, so the directory name appears exactly once.
- `_CONTRACTED_ARTIFACT_NAMES` — new module-level tuple in `check_permitted_claims.py` holding the
  four basenames, consumed by `_DEFAULT_TARGETS` and importable by the test.
- `_snapshot_contracted_artifacts(directory)` and `_assert_no_new_contracted_artifacts(directory,
  before)` — new helpers in `test_check_permitted_claims.py`.
- `test_side_effect_guard_fires_on_a_new_contracted_artifact` — new test proving the guard is
  reachable.

Modified: the two files named in `files_modified`. No `.hex`, no firmware, no wire change.

<tasks>

<task type="auto">
  <name>Task 1: Repoint _DEFAULT_TARGETS at the Phase 130 directory and update the docstring's resolved paths</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — read
      the whole file. Lines 1-66 are the module docstring (including the
      `**Phase 130 coupling (load-bearing):**` paragraph that sanctions this amendment), 68-91 are
      `_HERE` + `_DEFAULT_TARGETS`, 93-104 the env seam, 202-215 `resolve_targets`, 226-291 `main()`'s
      never-vacuous guard and the D-15 arming branch, 322-332 the PASS line.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md` §"C-2"
      — the reproduced `UNARMED:` + `exit=0` transcript and why resolution (b) is rejected.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-PATTERNS.md`
      §`check_record_corrections.py` — the verbatim quote of lines 68-91 including the anti-wildcard
      warning comment that must survive the edit.
  </read_first>
  <action>
Add two module-level constants directly above `_DEFAULT_TARGETS`, keeping `_HERE` exactly as it is
(it is still correct for the PASS line's `os.path.relpath(s, _HERE)` and for locating `fixtures/`):

- `_PHASE_130_DIRNAME` = the literal directory name
  `130-close-honesty-ledger-claim-gate-release-decision`, so that name appears exactly once in the
  module.
- `_CONTRACTED_ARTIFACT_NAMES` = a tuple of the four basenames in this exact order:
  `130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md`, `130-RELEASE-NOTES-app.md`.
  Order matters — `test_d15_arming_both_directions` asserts the three still-missing names appear in
  the FAIL output, and the PASS line names files in resolution order.

Rebuild `_DEFAULT_TARGETS` as the four names joined onto
`os.path.normpath(os.path.join(_HERE, os.pardir, _PHASE_130_DIRNAME))`. Use `os.pardir`, not a
literal `..`, and normalise, so the printed paths in FAIL/UNARMED messages are readable rather than
carrying an embedded parent-directory hop.

Preserve verbatim, unchanged, the existing comment block above `_DEFAULT_TARGETS` (the "NEVER
pattern-based and NEVER discovered by walking a directory tree" / `fixtures/` poisoning warning) and
extend it with two sentences recording: (1) that the list is now resolved into the sibling Phase 130
directory rather than `_HERE`, because `_HERE` is the Phase 123 directory and the artifacts live in
Phase 130's — the defect RESEARCH C-2 reproduced as `UNARMED:` + exit 0; (2) that the arming
guarantee is why the fix is a repoint and not a switch to `argv` / `FIRESTARTER_CLAIMSCAN_TARGETS` —
`main()`'s `used_defaults` branch means arming applies only to the default set.

In the module docstring, amend the `**Phase 130 coupling (load-bearing):**` paragraph so it states
where the four names now resolve (the sibling `130-close-...` directory) and keeps its existing rule
that a rename or addition must be reflected here in the same commit. Add one sentence recording that
this relocation is itself that sanctioned same-commit amendment, taken in Phase 130 plan 130-01
ahead of the artifacts so the gate is armed *before* the first artifact lands rather than after.

Change nothing else: `FORBIDDEN_PATTERNS`, `PY32_TOKEN_RE`, `PROXIMITY_WINDOW`,
`REQUIRED_CAVEAT_PROSE`, `REQUIRED_CAVEAT_PATTERN`, `resolve_targets`, `scan_text`, `_print_bucket`
and every branch of `main()` stay byte-identical. Keep the env-var name
`FIRESTARTER_CLAIMSCAN_TARGETS` as-is; RESEARCH assumption A3's suffixing advice applies to the
*new* checker in plan 130-02, not to this one.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py; test $? -eq 0 && python3 -c "
import re,sys
src=open('check_permitted_claims.py').read()
assert '130-close-honesty-ledger-claim-gate-release-decision' in src
assert src.count('130-close-honesty-ledger-claim-gate-release-decision') == 1, 'dirname must appear exactly once'
assert 'os.pardir' in src
for n in ('FORBIDDEN_PATTERNS','PY32_TOKEN_RE','PROXIMITY_WINDOW','REQUIRED_CAVEAT_PATTERN','used_defaults'):
    assert n in src, n
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import check_permitted_claims as m; print(m._DEFAULT_TARGETS)"` run from the Phase
      123 directory prints four absolute paths, every one of them beginning
      `/workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/` and
      ending `130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md`,
      `130-RELEASE-NOTES-app.md` in that order.
    - None of the four printed paths contains the substring `123-non-regression-baselines-gate-hardening`.
    - No printed path contains `..` (the `os.path.normpath` requirement).
    - `python3 check_permitted_claims.py` exits **0** and prints a line beginning `UNARMED:` — zero of
      the four artifacts exist yet, which is the correct pre-artifact state now that the directory is
      right.
    - `python3 -c "import check_permitted_claims as m; print(len(m.FORBIDDEN_PATTERNS))"` prints `8`.
    - `git -C /workspaces diff --stat -- .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py`
      shows exactly one file changed, and `git diff` on that file touches only the module docstring
      region and the constants region — no hunk inside `scan_text`, `resolve_targets`,
      `_print_bucket`, or `main`.
    - The literal string `FIRESTARTER_CLAIMSCAN_TARGETS` still appears in the file (the env seam is
      not renamed here).
  </acceptance_criteria>
  <done>Default-mode resolution lands in the Phase 130 directory; the run is a correct `UNARMED:` exit 0 rather than a mis-targeted one; the violation-detection logic is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Make the side-effect guard differential and prove it can still fire</name>
  <files>.planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/test_check_permitted_claims.py` —
      read the whole file. Lines 1-38 are the docstring's numbered `Coverage:` list, 40-69 the
      `_run_scanner` subprocess harness, 232-304 `test_d15_arming_both_directions` (the RED leg,
      whose guard is at 299-304), 312-349 tests 9 and 10.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` as
      edited by Task 1 — the test imports nothing from it today, but
      `_CONTRACTED_ARTIFACT_NAMES` is now the single source of the four basenames.
    - `130-RESEARCH.md` §"C-3" and §"Things That Would Break a Plan" item 2 — the guard's purpose and
      the Phase 129 locator lesson.
    - `130-PATTERNS.md` §`test_check_record_corrections.py` — the verbatim guard quote and the
      "suppression is real, not accidental" pattern this task mirrors.
  </read_first>
  <action>
The guard's intent is "this *test* must not create a Phase 130 artifact as a side effect". Its
current form asserts absolute absence, which was already false (`130-CONTEXT.md` matched the
`130-*.md` glob) and will be false again and permanently once `130-LEDGER.md` and its three siblings
legitimately land. Narrowing the glob alone therefore fixes today's RED and re-plants the same RED
three plans later. Convert it to a **differential** guard instead.

Add two module-level helpers near the top, below `_SCANNER`:

- `_snapshot_contracted_artifacts(directory)` — returns a frozenset of the members of the four
  contracted basenames that currently exist as files in `directory`. Import the four names from the
  scanner module (`_CONTRACTED_ARTIFACT_NAMES`) rather than re-typing them, so a future rename
  cannot desynchronise the test from the gate. Return an empty frozenset when `directory` does not
  exist.
- `_assert_no_new_contracted_artifacts(directory, before)` — re-snapshots and raises `AssertionError`
  when the after-set is not equal to the before-set, with a message naming the added basenames and
  stating that the test must not create a real contracted artifact as a side effect.

Rewrite the guard at the end of `test_d15_arming_both_directions` to: snapshot the real Phase 130
directory into a local `before` at the **top** of the test body (before `shutil.copy`), then call
`_assert_no_new_contracted_artifacts` with that snapshot as the final statement. Locate the real
directory as `_HERE.parent / "130-close-honesty-ledger-claim-gate-release-decision"` exactly as
today. Keep the surrounding comment and extend it to record why the assertion is differential rather
than absolute: the four artifacts are this phase's own deliverables and will exist in that directory
by design, so an absolute-absence assertion is self-invalidating.

Add one new test, `test_side_effect_guard_fires_on_a_new_contracted_artifact`, taking `tmp_path`. It
snapshots an empty `tmp_path`, writes a file named `130-LEDGER.md` into it, and asserts that calling
`_assert_no_new_contracted_artifacts(tmp_path, before)` raises `AssertionError` with `130-LEDGER.md`
in the message — using `pytest.raises`. This is the RED-preserving proof: it demonstrates the guard
is reachable and fires for the right reason, without ever writing into the real phase directory. Add
`import pytest` if the module does not already import it.

Extend the module docstring's numbered `Coverage:` list with an entry for the new test, stating the
direction it proves (the guard is live, not vacuous), and amend the existing entry for test 8 to say
its guard is differential.

Do not touch `_run_scanner`, tests 1-7, 9 or 10, and do not change any scanner behaviour.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening && python3 -m pytest test_check_permitted_claims.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest test_check_permitted_claims.py -q` reports `11 passed` (the 10 pre-existing
      tests plus the new reachability test) and `0 failed`. It reported `1 failed, 9 passed` before
      this task.
    - `python3 -m pytest test_check_permitted_claims.py::test_d15_arming_both_directions -q` reports
      `1 passed`.
    - `python3 -m pytest test_check_permitted_claims.py::test_side_effect_guard_fires_on_a_new_contracted_artifact -q`
      reports `1 passed`.
    - Reachability is proven positively, not by absence: temporarily inverting
      `_assert_no_new_contracted_artifacts` to `return` unconditionally makes
      `test_side_effect_guard_fires_on_a_new_contracted_artifact` FAIL. Demonstrate this by running
      the single test against the mutated helper, record the observed failure text in the SUMMARY,
      then restore the helper and re-run the full suite to `11 passed`.
    - After the whole suite has run, `ls .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/`
      lists no file named `130-LEDGER.md`, `130-DECISION.md`, `130-RELEASE-NOTES-fw.md` or
      `130-RELEASE-NOTES-app.md` — the suite created no real artifact.
    - `python3 -c "import test_check_permitted_claims as t; print(sorted(t._snapshot_contracted_artifacts.__doc__ is not None, ))"` is not required, but
      `grep -c '_CONTRACTED_ARTIFACT_NAMES' test_check_permitted_claims.py` returns a value of at
      least 1 — the four names are imported from the scanner, not re-typed.
  </acceptance_criteria>
  <done>The suite is green at 11 passed; the guard survives the artifacts legitimately existing; a dedicated test proves the guard fires when something really does add a contracted artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-assert the 130-CONTEXT.md decision parse as a regression check</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CONTEXT.md`
      §`<decisions>` — the sixteen `- **D-NN: ...**` bullets whose labels were rewrapped
      pre-planning at commit `0f9a709`.
    - `.claude/gsd-core/bin/lib/decisions.cjs` — the three accepted bullet grammars (`bulletColonRe`,
      `bulletEmDashRe`, `bulletTitledColonRe`) and the parse-miss guard that returns
      `could-not-parse` when any bullet is unparseable.
    - `130-RESEARCH.md` §"C-1" and `130-VALIDATION.md` §"Wave 0 Requirements" — C-1 is recorded as
      landed pre-planning; this task is the regression re-assert, not the fix.
  </read_first>
  <action>
Run the decision extractor against `130-CONTEXT.md` and record the measured result. Invoke
`extractDecisions` from `.claude/gsd-core/bin/lib/decisions.cjs` on the file's text via `node -e`,
printing the returned `outcome` and the extracted id list. Expected: `outcome` is not
`could-not-parse`, the count is `16`, and the ids are exactly `D-01` through `D-16` with no gaps.

Do not edit `130-CONTEXT.md`. C-1 was discharged pre-planning at `0f9a709`; this task exists so that
a later edit to that file — for example a reader adding a note — cannot silently drop a decision
from the §13a coverage gate's view without a recorded check having failed. Record the measured
outcome, count and id list verbatim in `130-01-SUMMARY.md`, and record that D-17 (the operator's
locked ship-gate decision, delivered during planning rather than in `130-CONTEXT.md`) is
deliberately **not** in the extractor's id set and is carried in the plan set's `must_haves`
instead — so `16`, not `17`, is the correct expected count for this file.
  </action>
  <verify>
    <automated>cd /workspaces && node -e 'const fs=require("fs");const {extractDecisions}=require("./.claude/gsd-core/bin/lib/decisions.cjs");const r=extractDecisions(fs.readFileSync(".planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CONTEXT.md","utf8"));const ids=(r.decisions||[]).map(d=>d.id);if(r.outcome==="could-not-parse"){console.error("FAIL outcome="+r.outcome);process.exit(1)}if(ids.length!==16){console.error("FAIL count="+ids.length);process.exit(1)}const want=Array.from({length:16},(_,i)=>"D-"+String(i+1).padStart(2,"0"));if(ids.join(",")!==want.join(",")){console.error("FAIL ids="+ids.join(","));process.exit(1)}console.log("PASS outcome="+r.outcome+" count=16 ids="+ids.join(","))'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits `0` and prints a line beginning `PASS outcome=` whose `count=16`.
    - The printed id list is exactly `D-01,D-02,D-03,D-04,D-05,D-06,D-07,D-08,D-09,D-10,D-11,D-12,D-13,D-14,D-15,D-16`.
    - `git -C /workspaces diff --stat -- .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-CONTEXT.md`
      is empty — the file is not edited by this task.
    - `130-01-SUMMARY.md` contains the measured `outcome`, the count `16`, the full id list, and the
      one-sentence statement that D-17 is carried in `must_haves` rather than in `130-CONTEXT.md`.
  </acceptance_criteria>
  <done>The parse is measured, green, and recorded, with the D-17 accounting stated so a later reader does not read `16` as a missing decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> outward-facing release body | The claim gate is the only mechanical check between this phase's prose and a stranger reading a public release. Everything crossing it is authored by an agent. |
| checker source -> checker verdict | The gate's own target-resolution code decides what is scanned; a defect there is invisible in the verdict. |
| test suite -> the phase's own deliverables | The suite writes files; a bad locator could write into or falsely fail on the phase's deliverable set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-01 | Repudiation / false assurance | `check_permitted_claims.py` `_DEFAULT_TARGETS` | critical | mitigate | Task 1 repoints resolution into the Phase 130 directory and asserts none of the four resolved paths contains `123-non-regression-baselines-gate-hardening`; the `UNARMED:` exit-0 state is retained deliberately and is only correct because the directory is now right. |
| T-130-02 | Tampering | the D-15 arming guarantee | high | mitigate | Resolution (b) — switching to `argv`/env targets — is prohibited in `must_haves.prohibitions`; Task 1's acceptance criteria pin `used_defaults` still present and the diff confined to docstring + constants. |
| T-130-03 | Repudiation | the side-effect guard | high | mitigate | Task 2 makes the guard differential and adds a reachability test; the acceptance criteria require a positive mutation demonstration, not merely a green suite (Phase 129's unreachable-leg lesson). |
| T-130-04 | Denial of service (self-inflicted) | the phase's own deliverable directory | medium | mitigate | The reachability proof runs entirely in `tmp_path`; an acceptance criterion re-lists the real directory after the suite to confirm no contracted artifact was created. |
| T-130-05 | Information disclosure | none | low | accept | This plan touches no secret, no token and no network. `gh` is not invoked. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package. `python3`, `pytest` and `node` are already present (RESEARCH §Environment Availability), so the Package Legitimacy Gate has no subject here; no `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py` → exit 0, `UNARMED:` line, no path naming the 123 directory.
- `cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 -m pytest test_check_permitted_claims.py -q` → `11 passed`.
- The decision-parse node command → `PASS outcome=... count=16`.
- `git -C /workspaces status --porcelain .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/` shows no new `130-LEDGER.md` / `130-DECISION.md` / `130-RELEASE-NOTES-fw.md` / `130-RELEASE-NOTES-app.md`.
</verification>

<success_criteria>
- Default-mode target resolution lands in the Phase 130 directory; C-2 closed.
- The paired suite is green at 11 passed; C-3 closed, and closed in a way that survives the four artifacts landing.
- The guard is proven reachable by a dedicated test plus a recorded mutation demonstration.
- The decision parse is measured at 16/16 and recorded, with the D-17 accounting stated.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-01-SUMMARY.md` when done.
</output>
