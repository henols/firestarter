---
phase: 130-close-honesty-ledger-claim-gate-release-decision
plan: 14
type: execute
wave: 7
depends_on: ["130-13"]
files_modified:
  - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-HANDOFF.md
autonomous: false
requirements: [CLOSE-04]

must_haves:
  truths:
    - "The privileged acts are gated STRUCTURALLY: this plan writes the operator procedure and contains no command that can push, merge, tag, dispatch, post or upload. A fully auto-approved execution of this plan cannot publish anything."
    - "130-DECISION.md is verified committed before this plan's procedure becomes reachable (CONSTRAINT 1), by reading git log rather than by assumption."
    - "The live-state table is RE-MEASURED immediately before hand-off, not inherited: branch tips, origin/beta tips, ahead/behind counts and tag ceilings all move the moment anyone pushes."
    - "Every command in the procedure that carries a tag uses an <observed tag> placeholder read from gh release list; no 3.0.0b15 literal appears anywhere in it (CONSTRAINT 5)."
    - "The procedure's step 1 is D-02's blocking wording review, so it precedes any body reaching a public release (CONSTRAINT 4)."
    - "The procedure records the manual publish.yml workflow_dispatch as CONSTRAINT 6 requires, with its reason, and warns that the tag input flows into ref:."
  artifacts:
    - .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-HANDOFF.md
  key_links:
    - "130-HANDOFF.md -> the operator's hands; it is the only place in the phase where the privileged commands are written down, and they are written as a procedure, not as a task"
    - "the re-measured live state -> the merge the operator is about to perform, not RESEARCH's stamped table"
    - "the observed tag -> plan 130-15's verification, which reads it from gh release list rather than computing it"
  prohibitions:
    - "No task in this plan may RUN git push, git merge into beta, git tag, gh workflow run, gh release create, gh release edit, gh release delete, or twine upload — not as a dry run, not behind a checkpoint, not with any flag. These are operator acts. --auto and --chain auto-approve human-verify checkpoints and autonomous:false does not protect an outward-facing gate, so the gate is that the commands are absent."
    - "Do NOT put a 3.0.0b15 literal, or any computed 3.0.0b tag, into the procedure. Every tag position is an <observed tag> placeholder the operator fills from gh release list after the cut (CONSTRAINT 5, RESEARCH C-15)."
    - "Do NOT inherit 130-RESEARCH.md's or 130-DECISION.md's live-state figures. Re-measure. RESEARCH's table is explicitly valid only until branch tips or tag ceilings move, and 130-DECISION.md's were measured one wave earlier."
    - "Do NOT iterate .planning/config.json's planning.sub_repos. It lists four repos and all four directories exist; two are py32 scratch worktrees. Name firestarter and firestarter_app explicitly."
    - "Do NOT instruct the operator to weaken paths-ignore, to remove continue-on-error, or to edit any workflow trigger. No workflow file is edited by this phase."
    - "Do NOT instruct the operator to force push, rewrite history, or delete any published artifact — including the stray 3.0.0b12 prereleases, which stay public by v1.22 D-05."
    - "Do NOT instruct the operator to create the v1.23 tag or to merge toward main. Both stay with /gsd-complete-milestone (D-04)."
    - "This plan ticks NO requirement ids. CLOSE-01, CLOSE-02, CLOSE-03 and CLOSE-04 stay unchecked; only plan 130-16 may tick them."
---

<objective>
Write the operator hand-off dossier, re-measure the live state one last time, and stop.

This is where the phase's structural gating is realised. The ROADMAP's own Ordering note says it, CONTEXT's
Established Patterns say it, and research's break-a-plan list says it: **`--auto` and `--chain` auto-approve
human-verify checkpoints, and `autonomous: false` does not protect an outward-facing gate.** So the protection
cannot be a checkpoint type or a flag. It is that the privileged commands are **not in any task, in any plan, in
this phase**. They exist here only as written instructions for a human.

The dossier's other job is to stop the operator acting on stale numbers. `130-RESEARCH.md`'s live-state table is
stamped 2026-08-02 and every figure agreed when it was written — and branch tips and tag ceilings move the moment
anyone pushes. `130-DECISION.md` re-measured them one wave ago, and plans have committed since. So this plan
measures them again, immediately before hand-off, and the dossier carries the fresh numbers.

Purpose: give the operator everything needed to act, in order, with fresh figures, and make it impossible for the
agent to act instead.
Output: `130-HANDOFF.md`, and a hard stop.

**Requirement ticking:** this plan ticks NO requirement ids.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-LEDGER.md
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-RESEARCH.md
</context>

## Artifacts this phase produces

Created by **this** plan: `130-HANDOFF.md` — deliberately **not** a scanned artifact. v1.22's precedent covers
adding an unscanned artifact: it shipped `122-CHANNELS.md`, `122-CUT.md` and `122-DELIVERY.md` outside its scanned
set, so `check_permitted_claims.py`'s `_DEFAULT_TARGETS` does **not** grow and needs no amendment. Only a rename
of, or an addition to, the scanned set requires the same-commit amendment.

Already produced: all four contracted artifacts, `check_record_corrections.py`,
`test_check_record_corrections.py`, `fixtures/`, the two `usb_cdc.c` `#define` values. Still to come:
`130-CHANNELS.md` (130-15), `130-NONREGRESSION.md` (130-16).

<tasks>

<task type="auto">
  <name>Task 1: Verify the CONSTRAINT 1 precondition and re-measure the live state immediately before hand-off</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-HANDOFF.md</files>
  <read_first>
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md` — the twelve
      pre-flight sections and their recorded values. This task's measurements are compared against them, and any
      movement is expected and must be attributed.
    - `130-RESEARCH.md` §"Live State Re-Verification" and its Metadata `Valid until` note — the table is valid
      for roughly seven days for live state and says to re-verify immediately before the merge.
    - `130-RESEARCH.md` §"C-14" — the `paths-ignore` sets and the measured non-ignored changed-file counts.
    - `firestarter/CLAUDE.md` and `firestarter_app/CLAUDE.md` — the lockstep obligations and the note that the
      `[SHARED:S*]` sync-gate module runs in no CI leg.
  </read_first>
  <action>
Verify the precondition, then measure.

**Precondition (CONSTRAINT 1).** Read `git log --oneline -- .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md`
and confirm the artifact is committed, capturing the commit SHA and the committer timestamp. If it is not
committed, **stop** — the procedure this plan writes must not be reachable, because CLOSE-04's text is literal and
v1.21's close skipped exactly this step. Record the timestamp; plan 130-16 cites it as the
decision-preceded-the-act evidence.

**Re-measure, do not inherit.** For `firestarter` and `firestarter_app` **named explicitly** — never by iterating
`config.json`'s four-entry `sub_repos`, two of which are py32 scratch worktrees:
- `git fetch origin` in each, then the branch name and HEAD SHA;
- `origin/beta`'s tip SHA;
- the ahead/behind counts against `origin/beta`;
- local `beta` versus `origin/beta`;
- the highest `3.0.0b*` tag, via `git tag --list '3.0.0b*' | sort -V | tail -1`;
- the count of non-ignored changed files against `origin/beta`, so the dossier can state that both pushes will
  trigger rather than silently cutting nothing;
- `git status --porcelain` in all three repos, with `firestarter_app`'s pre-existing dirt named item by item so
  the operator does not read it as this phase's damage;
- `git ls-tree HEAD firestarter firestarter_app` in the meta repo, compared against the two working tips (D-04).

Also re-run, and record, the gates that bind the tree being merged: `firestarter` `pytest tests/ -q`,
`pio test -e native`, and `FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q`;
`firestarter_app` `pytest tests/ -q` plus its two codegen gates; both `.planning/` checker suites; and both
checkers in default mode. Commits have landed since `130-DECISION.md` measured them, so these are not the same
measurements.

Tabulate every figure against `130-DECISION.md`'s recorded value with an **AGREES** or **MOVED** verdict, and
attribute every movement to the plan that caused it. A movement is expected; an unattributable one is a finding.

Write all of this as the dossier's opening section. Read-only and fetch-only throughout: no push, no merge, no
tag, no dispatch, no release write.
  </action>
  <verify>
    <automated>cd /workspaces && echo "=== CONSTRAINT 1 ===" && git log --format='%H %cI %s' -1 -- .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-DECISION.md && git -C firestarter fetch origin --quiet; git -C firestarter_app fetch origin --quiet; for r in firestarter firestarter_app; do echo "--- $r ---"; echo "branch=$(git -C $r rev-parse --abbrev-ref HEAD) head=$(git -C $r rev-parse --short HEAD)"; echo "origin/beta=$(git -C $r rev-parse --short origin/beta)"; echo "behind_ahead=$(git -C $r rev-list --left-right --count origin/beta...HEAD)"; echo "tag_ceiling=$(git -C $r tag --list '3.0.0b*' | sort -V | tail -1)"; echo "nonignored_changed=$(git -C $r diff --name-only origin/beta...HEAD | grep -vE '\.(md|sh)$|^\.gitignore$|^docs/|^documents/|^images/|^\.vscode/' | wc -l)"; done; echo "=== gitlinks ==="; git ls-tree HEAD firestarter firestarter_app; echo "=== gates ==="; (cd firestarter && FIRESTARTER_META_ROOT=/workspaces python3 -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1); (cd .planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 check_record_corrections.py >/dev/null; echo "record_exit=$?"); (cd .planning/phases/123-non-regression-baselines-gate-hardening && python3 check_permitted_claims.py >/dev/null; echo "claim_exit=$?")</automated>
  </verify>
  <acceptance_criteria>
    - `git log --format='%H %cI' -1 -- <130-DECISION.md>` returns a commit SHA and timestamp; both are recorded in
      `130-HANDOFF.md`. If the file is uncommitted, the plan stops and records the stop.
    - `130-HANDOFF.md` opens with a table of every re-measured figure carrying an **AGREES** or **MOVED** verdict
      against `130-DECISION.md`'s value, with every MOVED attributed to a named plan.
    - The measured tag ceiling is recorded as read output, not as a derived value, and appears nowhere as an
      instruction to use a literal.
    - The non-ignored changed-file counts for both repos are recorded, with an explicit statement that both pushes
      will therefore trigger.
    - `firestarter_app`'s working-tree dirt is listed item by item with a statement that it predates this phase.
    - The gitlink comparison is recorded, with any needed bump attributed to plan 130-16 (D-04 asserts, does not
      pin).
    - Every re-run gate's result is recorded; the claim gate exits `0` in default mode with all four artifacts
      present, and the CLOSE-01 checker exits `0`.
    - `git -C firestarter rev-parse origin/beta` equals the value this task measured at its start — the fetch
      changed nothing on the remote.
    - `git -C /workspaces log --oneline -3` shows no commit from this task other than the one touching
      `130-HANDOFF.md`.
  </acceptance_criteria>
  <done>CONSTRAINT 1's precondition is verified by reading git rather than assuming, and the dossier carries figures measured minutes before the operator acts.</done>
</task>

<task type="auto">
  <name>Task 2: Write the operator procedure — as instructions, never as executable tasks</name>
  <files>.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-HANDOFF.md</files>
  <read_first>
    - `130-DECISION.md`'s accepted sequence — the thirteen numbered items with their owners and constraints. The
      procedure is the operator subset of that sequence, expanded into runnable form.
    - `firestarter/.github/workflows/beta-build.yml` — the `push: branches: [beta]` trigger, the `paths-ignore`
      set, `workflow_dispatch`'s `beta_version` and permanent `rehearsal` inputs, the ARM step's
      `continue-on-error: true` at the call site, and the missing-image report keyed on `steps.arm.outcome`.
    - `firestarter/.github/workflows/py32f071.yml` — the LOUD gate: `push: branches: [beta]` with no paths filter
      and no `continue-on-error`.
    - `firestarter_app/.github/workflows/beta-release.yml` — the blocking pre-release steps and the absence of a
      `files:` step, so app releases carry zero assets.
    - `firestarter_app/.github/workflows/publish.yml` — `on: release: types: [published]` plus
      `workflow_dispatch` with a **required** `tag` string, `ref: ${{ github.event.inputs.tag || github.ref }}`,
      and the in-file comment recording that a PAT lacking `workflow` scope suppresses the `release.published`
      event.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md` and `122-DELIVERY.md`
      — how the previous close documented the operator half. Match their level of detail.
    - `130-RELEASE-NOTES-fw.md` and `130-RELEASE-NOTES-app.md` — the two bodies the operator reviews and then
      posts.
  </read_first>
  <action>
Write the procedure as a numbered list of instructions **for a human**, in a section clearly headed as the operator
procedure. Every command appears as text to be read and run by the operator. **No task in this plan executes any
of them**, and the dossier must say so in its own words at the top of the section: the commands are here because
this is the only place in the phase they are written down, and they are not in any task because `--auto` and
`--chain` auto-approve checkpoints and an `autonomous:` flag protects nothing.

The steps, in this order:

1. **The D-02 blocking wording review.** Read `130-RELEASE-NOTES-fw.md` and `130-RELEASE-NOTES-app.md` in full.
   State what to look for beyond the scanner: an implied overclaim, a misleading omission, wrong tone. Name the
   four specific questions — does the firmware headline read as "it works" to a skimmer; does either body leave a
   reader thinking a board exists; is the USB identity statement clear that `1209:0001` is a test id and not an
   allocation; does the app body's beta-only section read as a design rather than an apology. State that this step
   **precedes** step 3 and that the operator may overrule D-17's ship-gate judgment or research assumption A3
   here.
2. **Confirm the pre-flight state.** Point at Task 1's table and at `130-DECISION.md`. State the go/no-go rule: if
   any gate in that table is RED, do not push. Name the app suite explicitly as the one whose RED breaks the
   lockstep pair asymmetrically, after the firmware half has already published.
3. **The outbound merge and push, in both sub-repos.** Give the exact `--no-ff` merge and push commands for
   `firestarter` and `firestarter_app`, naming each repo explicitly. State the order — either order works because
   the two cuts are independent, but recommend firmware first so the asset exists when the app's PyPI publish is
   dispatched. Record what happens next: both `beta-build.yml`/`beta-release.yml` fire, `update_version.py` runs
   with an empty `BETA_VERSION` on a `beta` push so `is_beta_mode()` takes the beta path and the tag scan
   auto-increments, and `py32f071.yml` fires unconditionally as the LOUD ARM gate.
4. **Wait for CI, then read the observed tag.** Give the read-only `gh release list --repo henols/firestarter
   --limit 3` and `gh release list --repo henols/firestarter_app --limit 3` commands. State plainly that the tag is
   **read, never computed**, and that no step in this procedure may substitute a derived literal.
5. **Check the ARM gate's outcome.** Give the read-only `gh run list` and `gh run view` commands for both
   workflows. State that `beta-build.yml`'s ARM step is contained by `continue-on-error: true`, so a green run is
   **not** evidence the py32 asset shipped, and that `py32f071.yml` is the workflow that says what broke. Point at
   plan 130-15 for the asset-presence assertion against the real release (D-03).
6. **Dispatch `publish.yml` manually for PyPI.** Give the `gh workflow run publish.yml --repo
   henols/firestarter_app -f tag=<observed tag>` form with the placeholder. State why it is manual: a release
   created by another workflow using a PAT lacking `workflow` scope suppresses `release.published`, six of thirteen
   published app betas never reached PyPI, and `3.0.0b12`'s absence from PyPI corroborates it. Warn that the `tag`
   input flows into the checkout `ref:`, so it must be the tag observed in step 4 and nothing else.
7. **Post both release bodies**, after the step-1 review. Name the read-only commands for inspecting the current
   bodies and state that the posting itself is the operator's act.
8. **Hand back to plan 130-15** for the channel verification: the observed tag, the py32 asset presence assertion,
   and the PyPI resolution check from a clean temp env. State that plan 130-15 will refuse to proceed unless a
   release tag strictly newer than the recorded ceiling exists in both repos, so an accidentally auto-approved
   execution fails closed rather than claiming a cut that did not happen.

Then a short section headed as what this procedure deliberately does **not** include: creating the `v1.23`
annotated tag or merging toward `main`, both of which stay with `/gsd-complete-milestone` (D-04); any stable
release; deleting the stray `3.0.0b12` prereleases; editing any workflow trigger; weakening `paths-ignore`;
removing `continue-on-error`; any force push or history rewrite.

Close with a one-paragraph statement of what the operator is being asked to accept, drawn from `130-DECISION.md`'s
ACCEPT row: the merge **is** the cut, in both repos, and the risk is bounded because no PY32F071 board exists
anywhere and the py32 board is beta-only by construction.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision && python3 -c "
s=open('130-HANDOFF.md').read()
assert '<observed tag>' in s, 'tag placeholder missing'
assert '3.0.0b15' not in s, 'tag literal present'
for k in ('publish.yml','continue-on-error','py32f071.yml','--no-ff','D-02','130-15'):
    assert k in s, k
print('OK handoff procedure')" && echo "=== assert no privileged command is an executable task in this plan ===" && python3 - <<'PY'
import re,sys
p='130-14-PLAN.md'
s=open(p).read()
tasks=re.findall(r'<automated>(.*?)</automated>', s, re.S)
bad=[]
for t in tasks:
    for pat in ('git push','git tag','gh workflow run','gh release create','gh release edit','gh release delete','twine upload'):
        if pat in t: bad.append((pat,t[:80]))
    if re.search(r'git\s+(-C\s+\S+\s+)?merge\b', t): bad.append(('git merge',t[:80]))
print('violations:',bad)
sys.exit(1 if bad else 0)
PY</automated>
    <human-check>
**This is the phase's hand-off point.** After this plan commits, the remaining outward-facing work is the
operator's: the D-02 wording review, the two `--no-ff` merges and pushes, the manual `publish.yml` dispatch, and
posting the two release bodies. `130-HANDOFF.md` is the procedure.

Plan 130-15 verifies the result afterwards and opens with a fail-closed precondition, so if execution continues
without the operator having acted, it stops with a clear message rather than claiming a cut that did not happen.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `130-HANDOFF.md` contains an operator-procedure section with eight numbered steps in the order specified,
      and step 1 is the D-02 wording review.
    - The section opens with an explicit statement that the commands are written here for a human and are in no
      task, and that the reason is that `--auto`/`--chain` auto-approve checkpoints and an `autonomous:` flag
      protects nothing.
    - `grep -c '<observed tag>' 130-HANDOFF.md` returns at least 2, and `grep -c '3.0.0b15' 130-HANDOFF.md`
      returns `0`.
    - Every tag-bearing command in the procedure uses the placeholder; none contains a `3.0.0b` literal.
    - The procedure names `firestarter` and `firestarter_app` explicitly in every repo-scoped command; it contains
      no loop over `config.json`'s `sub_repos`.
    - Step 5 states that a green `beta-build.yml` run is not evidence the py32 asset shipped, naming
      `continue-on-error: true`, and names `py32f071.yml` as the LOUD gate.
    - Step 6 gives the manual dispatch with its reason and warns that the `tag` input flows into `ref:`.
    - A deliberately-excluded section names the `v1.23` tag, any merge toward `main`, any stable release, deleting
      the stray b12 prereleases, editing a workflow trigger, weakening `paths-ignore`, removing
      `continue-on-error`, and any force push or history rewrite.
    - **The structural gate holds mechanically:** the inline Python check in this task's verify block scans every
      `<automated>` block in `130-14-PLAN.md` for `git push`, `git merge`, `git tag`, `gh workflow run`,
      `gh release create|edit|delete` and `twine upload`, and exits `0` with an empty violation list. Run the same
      check across all sixteen `130-*-PLAN.md` files and record the result — the expected violation list is empty
      for every plan.
    - `git -C firestarter rev-parse origin/beta` and `git -C firestarter_app rev-parse origin/beta` are unchanged
      from Task 1's measurements. Nothing was pushed by this plan.
    - `gh release list --repo henols/firestarter --limit 3` and the app equivalent still show `3.0.0b14` as newest.
  </acceptance_criteria>
  <done>The operator has a complete, fresh, ordered procedure; the agent has no way to execute any part of it; and the absence of the privileged commands is proven mechanically across all sixteen plans rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> the public world | The push, the CI dispatch and the release-body post are the three privileged acts of the milestone. Everything on the far side is irreversible and public. |
| a checkpoint -> an actual gate | `--auto`/`--chain` auto-approve human-verify checkpoints. A checkpoint in front of a privileged command is decoration; the gate has to be the command's absence. |
| a stamped figure -> the act taken on it | Branch tips and tag ceilings move on any push. A procedure carrying inherited numbers can instruct a merge against a tip that no longer exists. |
| a computed tag -> `publish.yml`'s checkout `ref:` | The `tag` input flows into `ref:`. A computed or guessed value is a checkout-of-arbitrary-ref vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-76 | Elevation of privilege | the push, the dispatch, the release post | critical | mitigate | Structural: no privileged command exists in any task of this plan, so a fully auto-approved execution cannot publish. Task 2's verify block **mechanically scans** every `<automated>` block in this plan — and, as an acceptance criterion, across all sixteen plans — for the seven forbidden command forms and requires an empty violation list. |
| T-130-77 | Tampering | an untrusted `tag` reaching `publish.yml`'s `ref:` | critical | mitigate | Every tag position in the procedure is an `<observed tag>` placeholder read from `gh release list`; criteria grep the literal at zero and require the placeholder present. Step 6 carries the warning explicitly. |
| T-130-78 | Repudiation | acting on stale live state | high | mitigate | Task 1 re-measures every figure with an AGREES/MOVED verdict against `130-DECISION.md`, attributing every movement; the dossier carries the fresh numbers, and RESEARCH's stamped table is explicitly not inherited. |
| T-130-79 | Repudiation / false assurance | a green CI tick read as a shipped asset | high | mitigate | Step 5 states the containment and hands the asset assertion to plan 130-15 against the real release (D-03). |
| T-130-80 | Denial of service | an accidental stable release or a deleted artifact | high | mitigate | A deliberately-excluded section names stable release, the `v1.23` tag, the merge toward `main`, workflow-trigger edits, `paths-ignore` weakening, `continue-on-error` removal, force push, history rewrite and deleting the stray b12 as out of scope. |
| T-130-81 | Spoofing | scratch worktrees mistaken for deliverables | medium | mitigate | Both repos are named explicitly in every command; a criterion forbids a loop over `config.json`'s four-entry `sub_repos`. |
| T-130-82 | Denial of service (self-inflicted) | continuing without the operator | medium | mitigate | Plan 130-15 opens with a fail-closed precondition requiring a release tag strictly newer than the recorded ceiling in both repos, so auto-continuation fails visibly rather than claiming a phantom cut. |
| T-130-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs **no** package. It runs `git fetch`, read-only `git`, `pytest`, `python3` and read-only `gh release list`/`gh run list`/`gh run view`. The Package Legitimacy Gate has no subject. |
</threat_model>

<verification>
- `git log --format='%H %cI' -1 -- <130-DECISION.md>` returns a commit — CONSTRAINT 1's precondition, read not assumed.
- The re-measured live-state table with AGREES/MOVED verdicts and attributions.
- `grep -c '<observed tag>' 130-HANDOFF.md` ≥ 2; `grep -c '3.0.0b15' 130-HANDOFF.md` = `0`.
- The inline scan of every `<automated>` block across all sixteen `130-*-PLAN.md` files → empty violation list.
- `git -C firestarter rev-parse origin/beta` and the app equivalent unchanged; `gh release list` still shows `3.0.0b14` newest in both repos.
- Both `.planning/` checkers green in default mode on the tree being handed off.
</verification>

<success_criteria>
- The operator procedure is complete, ordered, and carries figures measured minutes earlier.
- The D-02 review is step 1, ahead of the merge (CONSTRAINT 4).
- No plan in the phase contains a privileged command, and that is proven by a mechanical scan rather than asserted.
- Every tag position is a placeholder; nothing is computed into a command.
- The out-of-scope list is explicit, including everything that stays with `/gsd-complete-milestone`.
- No requirement id is ticked by this plan.
</success_criteria>

<output>
Create `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/130-14-SUMMARY.md` when done.
</output>
