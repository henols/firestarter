---
phase: 125-vpp-control-seam
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - firestarter/tests/test_vpp_seam_manual_on_every_board.py
autonomous: true
requirements: [VPP-02]

must_haves:
  truths:
    - "A single parametrized pytest asserts, in one run, that `rurp_set_vpp_target_mv()` returns `MANUAL_ADJUSTMENT_REQUIRED` and `rurp_vpp_control_mode()` returns `MANUAL` on all four board macro-sets — Uno, Leonardo, uno328pb, py32f071 — each compiled AND run, never a single-board spot check (D-01, ROADMAP Criterion 2)"
    - "The harness is proven non-vacuous by a forced-capability leg compiling `src/rurp_vpp.cpp` with the capability macro forced to 1, which must produce a non-zero compiler exit carrying that source file's own `#error` message (D-03 as corrected by RESEARCH C-4 — the header's guard alone exits 0, measured)"
    - "The header's `#else` arm has its own leg: a compile with neither `__AVR__` nor an explicit capability macro must fail with the header's own `#error` message (D-08)"
    - "The four board macro-sets are hardcoded literals plus one drift leg anchored on values that are literally present in the real build files — `[env:<name>]` plus `board = <value>` for AVR, and the two ARM defines — never on framework-supplied board macros, which appear nowhere in `platformio.ini` (D-04 as corrected by RESEARCH C-6, which would fail on arrival)"
    - "`src/rurp_vpp.cpp` is asserted dependency-free, turning D-02's standing constraint into something a future edit cannot quietly break"
    - "A missing host compiler FAILS the suite and can never be skipped, and the module's own source is scanned to keep that contract from being edited away — a skip at exit 0 is how this project's gates failed OPEN before (A-7)"
  artifacts:
    - firestarter/tests/test_vpp_seam_manual_on_every_board.py
  key_links:
    - "The return value crosses the process boundary on **stdout**, never the exit code. `RURP_VPP_MANUAL_ADJUSTMENT_REQUIRED` is 1, which is also the exit code of a compile failure, a link failure and a crash — returning it would make the correct answer indistinguishable from every failure mode (125-PATTERNS Trap 2)"
    - "This module must live under `tests/`, never as a `scripts/check_*.py`. RESEARCH C-11 planted one and measured the cost: four extra artifacts plus `FLOOR 5→6` and `FIXTURE_FLOOR 10→11`. The `tests/` shape costs zero because `test_checker_convention.py`'s glob is non-recursive over `scripts/` only. Never lower either floor"
    - "There is no `conftest.py`, `pytest.ini`, `pyproject.toml`, `setup.cfg` or `tox.ini` anywhere in the firmware repo — a recorded house rule, not an omission. Resolve every path at module scope. Do not add one"
---

<objective>
Author the committed proof for VPP-02: one pytest module that drives a host compiler to build and
**run** the seam once per board macro-set, plus the two legs that prove the capability macro is
genuinely consulted rather than decorative, plus the drift, dependency-freedom and no-skip legs
that keep the whole thing honest.

Purpose: ROADMAP Criterion 2 requires a single parametrized test asserting the refusal across
every board macro-set in one run. D-01 fixes the mechanism as a pytest under `firestarter/tests/`
driving host `g++` — not a fourth PlatformIO env (four `extern "C"` copies in one binary would need
macro-renaming gymnastics and would still be one native compile) and not static assertions in the
real target builds (that would put a new assertion into all four production builds whose flash
impact this phase would then have to defend). D-03 and D-08 give each `#error` arm its own leg, so
the guard is proven able to fire rather than assumed to be.

Output: `firestarter/tests/test_vpp_seam_manual_on_every_board.py`, seven test functions collecting
ten cases, taking `pytest tests/ -q` from 72 to 82.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/125-vpp-control-seam/125-CONTEXT.md
@.planning/phases/125-vpp-control-seam/125-RESEARCH.md
@.planning/phases/125-vpp-control-seam/125-PATTERNS.md
@.planning/phases/125-vpp-control-seam/125-VALIDATION.md
@.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md

**Read order and precedence.** `125-CONTEXT.md`'s decision **ids** D-01…D-16 are binding; several
of their **mechanisms** were measured wrong and replaced by RESEARCH C-1…C-18. Where the two
disagree, **RESEARCH.md wins**. For this plan the replacements that matter are C-4 (a second
`#error` in the source file is required, and D-03's forced leg must compile the `.cpp`, not merely
include the header) and C-6 (D-04's named drift anchors are absent from `platformio.ini` and the
leg as worded fails on arrival).

**Repo layout.** The module lives in the firmware submodule at
`/workspaces/firestarter/tests/test_vpp_seam_manual_on_every_board.py`, branch
`v1.23-py32f071-integration`. Commits land inside that submodule; this plan's SUMMARY commits in
the meta repo. Never write into `firestarter_py32_ci/` or `firestarter_app_py32/`.

**Requirement ticking scope for this plan: NONE.** VPP-02 appears in `requirements` frontmatter
because this plan is where its proof is authored, but VPP-02 is discharged across several plans and
only Plan 125-06 may tick it. Do not tick VPP-01, VPP-02 or VPP-03 in `.planning/REQUIREMENTS.md`
from this plan.

**No CI oracle.** RESEARCH C-7 measured that `pytest tests/ -v` appears only in `build.yml`
(push/PR to `main`) and `beta-build.yml` (push to `beta`); `py32f071.yml` has no pytest step. This
module therefore executes in **zero** CI legs on this branch. The analog
`tests/test_pinmap_guard_fires.py` carries a docstring sentence asserting that both firmware CI
workflows run the suite — true of those two workflows, but neither fires on this branch. **Do not
copy that sentence.** Say plainly that the local run is the evidence.

**Claim ceiling.** No PY32F071 hardware exists. The py32f071 leg proves a host-compiled macro-set
resolves to manual control; it says nothing about silicon. AVR manual VPP control is **permanent**
(D-05), never "for now".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four board legs plus the two guard legs</name>
  <files>firestarter/tests/test_vpp_seam_manual_on_every_board.py</files>
  <read_first>
    - `firestarter/tests/test_pinmap_guard_fires.py` — **read all 235 lines.** This is the direct
      precedent D-01 names. Five of its six structural features transfer: the module docstring
      shape (MIT banner, phase/plan line, `Requirements:` / `Decisions covered:` lines, a
      defect-class paragraph, the no-conftest note, a numbered `Coverage:` list with one entry per
      test), the module-scope path resolution, the fail-closed compiler resolver, the fresh
      temporary translation unit written into `tmp_path`, the list-argv subprocess convention, and
      the assertion-message discipline that dumps both streams on every failure.
    - `firestarter/include/rurp_vpp.h` and `firestarter/src/rurp_vpp.cpp` — as Plan 125-01 landed
      them. Read the two real `#error` messages; do not hardcode either one a second time.
    - `.planning/phases/125-vpp-control-seam/125-PATTERNS.md` §`tests/test_vpp_seam_manual_on_every_board.py`
      — the nine numbered patterns, the verbatim shim, the leg inventory table with each leg's
      required outcome, and **Trap 2** on the process boundary.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §"Pattern 3: The compile-and-run
      variant" — the four concrete differences from the preprocess-only analog and the measured
      prototype output for all six legs.
    - `.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md` — the five hand-proved behaviours
      with their observed exit codes and stdout, so this module is written against a seam already
      known to behave.
  </read_first>
  <behavior>
    - Leg `uno`: defines `__AVR__`, `ARDUINO_AVR_UNO`, `RURP_BOARD_NAME="uno"`, `SERIAL_ON_IO` →
      compile exit 0, run exit 0, stdout `mode=0 result=1`, zero bytes of warning output.
    - Leg `leonardo`: defines `__AVR__`, `ARDUINO_AVR_LEONARDO`, `RURP_BOARD_NAME="leonardo"`,
      `DATA_BUFFER_SIZE=1024` → same outcome.
    - Leg `uno328pb`: defines `__AVR__`, `ARDUINO_AVR_ATmega328PB`, `RURP_BOARD_NAME="uno328pb"`,
      `SERIAL_ON_IO` → same outcome.
    - Leg `py32f071`: defines `RURP_PLATFORM_PY32F071=1`, `RURP_HAS_VPP_DAC=0`,
      `RURP_BOARD_NAME="py32f071"` → same outcome.
    - Forced-capability leg: defines `__AVR__` and the capability macro forced to 1, compiling
      `src/rurp_vpp.cpp` → compile exit non-zero, and the message read out of `src/rurp_vpp.cpp`
      appears in stderr.
    - Unset-and-non-AVR leg: no defines at all → compile exit non-zero, and the message read out of
      `include/rurp_vpp.h` appears in stderr.
  </behavior>
  <action>
    Create `firestarter/tests/test_vpp_seam_manual_on_every_board.py`. Stdlib plus pytest only. No
    `conftest.py` — resolve every path at module scope, deriving the directory of this file and its
    parent as the repository root, then `include/` for the compiler's include path, and the two seam
    paths. Note the include path is `include/`, not `include/boards/` as in the analog.

    **Module docstring.** Follow the analog's shape exactly: the MIT banner, then a
    `Phase 125 Plan 02 — ...` line, then `Requirements: VPP-02` and
    `Decisions covered: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08` lines, then a defect-class
    paragraph, then the no-conftest note, then a numbered `Coverage:` list with one entry per test
    function naming the mechanical outcome it asserts. Two things the defect-class paragraph must
    say, both corrections research measured:
      - the capability macro's two consumers are the header's `!defined` test and the source file's
        value test, and this module is what proves each can fire — the direct antidote to the hollow
        guard Phase 124 D-14 had to restructure, where a `#define` sat thirty lines above the `#if`
        that tested it;
      - **what the four board legs actually prove** (C-6, Pitfall 8): the seam consults exactly two
        macros, and nothing in it distinguishes Uno from Leonardo from uno328pb, so the four legs
        prove *uniformity across one compiler-supplied AVR fact plus one explicit ARM declaration* —
        not four independent per-board facts. The real AVR-cross-compiler resolution is discharged
        by the three `pio run` builds Plan 125-04 performs. Say this; do not let a reader infer more.
    Also state plainly that this module executes in **no** CI leg on this branch (C-7), so the local
    run is the evidence. Do not carry over the analog's claim that both firmware CI workflows run
    the suite.

    **Helpers.** Copy the analog's fail-closed compiler resolver almost unchanged: resolve the
    compiler from the `CXX` environment variable falling back to `g++`, via `shutil.which`, and
    assert it is not None with a message stating that this must FAIL the suite and can never be
    silently bypassed. Revise only its docstring's CI sentence per C-7. Add a helper that writes the
    temporary translation unit into `tmp_path` — never a committed fixture translation unit, which
    is also what D-03 declined — containing exactly the measured shim: it includes `"rurp_vpp.h"`
    and `<cstdio>`, and its `main` prints the control mode and the result of calling
    `rurp_set_vpp_target_mv(12000, 200, 50)` in the exact form `mode=%d result=%d`, each cast to
    `int`, then calls `rurp_disable_vpp_control()` and returns 0 unconditionally. Add a compile
    helper that takes a list of macro definitions and returns the `CompletedProcess`: list argv
    only, never a shell, `capture_output=True, text=True`; drop the analog's `-E`; emit a real binary
    into `tmp_path`; pass **both** the temporary translation unit and the production
    `src/rurp_vpp.cpp` in one invocation; and add `-std=gnu++17 -Wall -Wextra` plus the `include/`
    path. Because no shell is invoked, a definition whose value contains double quotes needs no
    extra escaping — pass it as the literal Python string. Add a run helper that executes the built
    binary as a second subprocess, list argv, capturing output. Add **two** separate helpers that
    read the expected `#error` message back out of a named file at test time with a regex matching
    an `#error` directive followed by a quoted string, rather than hardcoding either message a
    second time where it could silently desync. Each helper is scoped to its own file and asserts
    that its file yields exactly one such directive — do not reuse an "exactly one" assertion across
    both files, because the header carries one and the source carries a different one.

    **Leg 1 — the four board macro-sets, as one parametrized test.** ROADMAP Criterion 2 asks for a
    *single parametrized* test across every board macro-set in one run, so use one test function
    parametrized over the four macro-sets with the board names as ids, not four separate functions.
    Each case: compile with that macro-set, assert compile exit 0, assert the compiler emitted zero
    bytes on stderr, run the binary, assert run exit 0, and parse the two values out of stdout —
    asserting the control mode equals the manual enumerator's value 0 and the result equals the
    manual-adjustment-required enumerator's value 1. **Parse from stdout; never use the process exit
    code to carry the value** (Trap 2): the required result value is 1, which is also the exit code
    of a compile failure, a link failure and a crash, so returning it would make the correct answer
    indistinguishable from every failure mode. Every assertion message must state what was expected
    and dump both streams, as the analog does at its lines 131–138 — never a bare comparison.

    **Leg 2 — the forced-capability leg (D-03, mechanism per C-4).** Compile with `__AVR__` defined
    and the capability macro forced to the value 1, compiling `src/rurp_vpp.cpp`. Assert a non-zero
    compiler exit and assert the message read out of `src/rurp_vpp.cpp` is present in stderr. The
    docstring must record why this leg compiles the source file rather than only including the
    header: research measured that with only the header's guard present, forcing the macro to 1
    exits **0**, so a header-only leg would pass vacuously and the whole "the macro is genuinely
    consulted" argument would evaporate. This is the leg that makes the harness non-vacuous.

    **Leg 3 — the unset-and-non-AVR leg (D-08).** Compile with no definitions at all. Assert a
    non-zero compiler exit and assert the message read out of `include/rurp_vpp.h` is present in
    stderr. The docstring should note this is the arm every native translation unit would reach if
    the seam header were ever included from `include/rurp_shield.h`, which research measured takes
    `pio test -e native` to 17 suites with 0 succeeded — so this leg is also the standing
    explanation for why that include does not exist.

    Commit in the firmware submodule with a message naming the plan, e.g.
    `test(125-02): prove the VPP seam refuses on every board macro-set`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_vpp_seam_manual_on_every_board.py` exists and `python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py -q` reports exactly `6 passed` at this task's end — four parametrized board cases plus the two guard legs.
    - The four board cases come from **one** parametrized test function, not four functions, and their parametrize ids are `uno`, `leonardo`, `uno328pb`, `py32f071`. Verified by `python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py --collect-only -q`, whose output names all four ids under a single function.
    - Each board case asserts three separate things: compile returncode equals 0, run returncode equals 0, and the two values parsed from run stdout equal 0 (mode) and 1 (result). The mode and result are read from stdout; no assertion anywhere in the module compares a run process's returncode against the seam's result value.
    - Each board case also asserts the compiler produced zero bytes on stderr under `-Wall -Wextra`.
    - The forced-capability leg passes the capability macro with the value 1 **and** compiles `src/rurp_vpp.cpp`; it asserts a non-zero compile returncode and asserts the expected substring came from a read of `src/rurp_vpp.cpp`, not from a literal in the test module.
    - The unset-and-non-AVR leg passes no `-D` arguments at all; it asserts a non-zero compile returncode and asserts the expected substring came from a read of `include/rurp_vpp.h`.
    - The module contains two distinct expected-message helpers, one per file, each asserting exactly one matching directive in its own file. Neither `#error` message text appears as a literal string in the test module.
    - Every `subprocess.run` call in the module passes a list argv and sets `capture_output=True, text=True`; no call passes `shell=True` and no call passes a single composed command string.
    - Every assertion in the module has a failure message that includes both the captured stdout and the captured stderr, or — for the pure file-read assertions — the offending file's path and content.
    - No temporary translation unit or built binary is committed: `git status --porcelain` after the commit is 0 lines and the commit's changed-path list contains exactly `tests/test_vpp_seam_manual_on_every_board.py`.
    - The module docstring contains a numbered `Coverage:` list, a `Requirements: VPP-02` line, a `Decisions covered:` line naming D-01, D-02, D-03, D-04, D-05, D-06, D-07 and D-08, the no-conftest note, the bounded claim about what the four legs prove (uniformity across one AVR fact plus one ARM declaration, not four independent per-board facts), and an explicit statement that no CI leg on this branch executes this module.
    - The module docstring does **not** assert that any firmware CI workflow runs this suite.
    - `python3 -m pytest tests/ -q` reports `78 passed` at this task's end (72 pre-existing plus this task's 6).
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>One parametrized test proves the refusal on all four board macro-sets by compiling and running, and each of the seam's two `#error` arms has its own leg proving it fires with its own file's message.</done>
</task>

<task type="auto">
  <name>Task 2: The drift, build-supplies-the-macro, dependency-freedom and no-skip legs</name>
  <files>firestarter/tests/test_vpp_seam_manual_on_every_board.py</files>
  <read_first>
    - `firestarter/tests/test_pinmap_guard_fires.py` lines 184–234 — coverage 4 (the
      dependency-freedom leg), coverage 5 (the board header does not define what it tests), and
      coverage 6 (the self-enforcement leg, including the string-concatenation trick that stops the
      leg tripping on its own source). Copy coverage 6 whole, adapting only the names.
    - `firestarter/platformio.ini` — the three AVR env sections and their `board = ` lines. Read
      them; do not recall them.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the `target_compile_definitions` block as
      Plan 125-01 left it, including the new capability entry.
    - `firestarter/include/boards/py32f071_rurp_shield.h` — confirm by reading that it contains no
      top-level definition of the capability macro, which is the property the new regression leg
      pins.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-6 — the table of what is literally
      present in `platformio.ini` versus what D-04 named, and why a leg anchored on the framework
      board macros fails on arrival.
    - `.planning/phases/125-vpp-control-seam/125-PATTERNS.md` §"Drift-leg anchors" — the four-row
      anchor table.
  </read_first>
  <action>
    Extend the same module with four more test functions. Add a `Coverage:` entry for each and give
    each function a docstring repeating its coverage number, as the analog does.

    **Leg 4 — the drift leg (D-04, anchors per C-6).** One test function asserting, by plain file
    reads, that each board's real build config still literally contains the anchors the hardcoded
    macro-sets are keyed to. For the three AVR boards the anchors in `platformio.ini` are the
    section header `[env:uno]` together with `board = uno`; `[env:uno328pb]` together with
    `board = ATmega328PB`; and `[env:leonardo]` together with `board = leonardo`. For ARM the
    anchors in `platform/py32f071/CMakeLists.txt`'s `target_compile_definitions` are
    `RURP_PLATFORM_PY32F071=1` and the board-name define whose value is the quoted string
    `py32f071`. **Do not anchor on the framework-supplied board macros** — research measured that
    those come from the Arduino framework and the board JSON and appear nowhere in `platformio.ini`,
    so a leg keyed to them fails the moment it is written, and the tempting repair is to weaken it
    into something vacuous. Record that reasoning in the function's docstring. Note the board-name
    define is a literal only for uno328pb; the other two AVR envs interpolate it from the board
    value, which is why the drift anchors are the env header and the board line rather than that
    define. Do **not** parse either build file into a structure — `tests/scan_paths.py`'s stated
    principle is deliberately explicit, never derived, and the two build systems have incompatible
    define syntaxes, so a derived version is two parsers. Substring presence is the whole mechanism.
    The failure message for each anchor must name the file, the missing anchor, and the fact that
    the hardcoded macro-set in this module has drifted from the real build config.

    Separately, note in the docstring that the macro definitions the board legs pass to the compiler
    deliberately include the framework board macros as part of each board's macro-set — that is
    correct and required by the leg inventory — so this drift leg is about what the *build files*
    contain, not about what the compile legs pass. A file-wide search of this module for those
    framework macro names will and should find them.

    **Leg 5 — the build supplies what the header only tests (D-07).** One test function asserting
    two complementary facts: `platform/py32f071/CMakeLists.txt` contains the capability macro
    declared with the value 0 inside its `target_compile_definitions`, and
    `include/boards/py32f071_rurp_shield.h` contains no top-level definition of the capability
    macro. The second half is the direct analog of the precedent's coverage 5 and is the regression
    guard against the hollow shape returning: a definition in the board header would make the
    header's own guard permanently unfirable, which is exactly the defect Phase 124 Plan 09 had to
    repair. The docstring must state the ARM value is 0 because no hardware exists, that the closed
    branch which chose 1 is out of scope by requirement, and that AVR manual control is permanent
    rather than provisional (D-05).

    **Leg 6 — the seam source is dependency-free (D-02).** One test function asserting that
    `src/rurp_vpp.cpp`'s only include directive names the seam header. Model it on the precedent's
    coverage 4 but invert the shape: the analog asserts *no* include directive exists in a fragment
    header, whereas here exactly one is expected and any second one is the violation. Collect every
    include directive in the file with a multiline regex and assert the collected list has length 1
    and that its single entry names the seam header. The failure message must state that D-02 is a
    standing constraint, not a local convenience — it is what lets this harness need zero stub
    scaffolding, and a later phase wanting configuration or hardware access must add the dependency
    deliberately rather than by drift.

    **Leg 7 — the compiler is required, not optional (the self-enforcement meta leg).** Copy the
    precedent's coverage 6 whole. It reads this module's own source and asserts that neither of the
    two skip constructs appears anywhere in it, with both needle strings built by concatenation
    rather than written verbatim so the leg's own assertion text does not trip its own check. The
    literal needles must appear nowhere in the file, including inside failure messages. This leg is
    what keeps the fail-closed contract from being edited away: an absence-proxy skip reporting
    success at exit 0 is precisely the failure class this milestone's Phase 123 removed.

    Finally, assert the module's own total: after this task the module collects exactly ten cases
    from seven functions, and `pytest tests/ -q` reports 82. Record both numbers in the SUMMARY as
    counts, never as "tests pass" — a suite that stops being collected also reports green.

    Commit in the firmware submodule, e.g.
    `test(125-02): add the VPP seam drift, dependency-freedom and no-skip legs`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py -q 2>&1 | tail -3 && python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py --collect-only -q 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py -q` reports exactly `10 passed`.
    - `python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py --collect-only -q` reports exactly 10 collected cases originating from exactly 7 test functions.
    - `python3 -m pytest tests/ -q` reports exactly `82 passed`.
    - The drift leg asserts all eight AVR anchors — three env section headers and three board lines make six; state the exact set asserted — plus the two ARM anchors, and every assertion is a substring presence check against a file read. No assertion in the drift leg parses either build file into a data structure.
    - The drift leg's assertions reference only the anchors named in the action. `platformio.ini` is never searched for a framework-supplied board macro, and the drift leg's failure messages name no such macro.
    - The build-supplies leg asserts the capability macro appears with the value 0 in `platform/py32f071/CMakeLists.txt`'s `target_compile_definitions`, and separately asserts `include/boards/py32f071_rurp_shield.h` contains no top-level definition of that macro.
    - The dependency-freedom leg collects include directives from `src/rurp_vpp.cpp` and asserts the collected list has length exactly 1 naming the seam header. Verified to be a real assertion by confirming it fails when pointed at any other source file in `src/` — record which file was used for that confirmation and its observed failure message.
    - The self-enforcement leg builds both needle strings by concatenation, and a search of the whole module for either literal needle returns zero hits — including inside every failure message. Confirm by running the leg and by an independent search whose command constructs the needles the same way the leg does.
    - Every new function has a docstring naming its coverage number, and the module's `Coverage:` list has exactly seven numbered entries matching the seven functions in order.
    - `git status --porcelain` in `/workspaces/firestarter` is 0 lines after the commit, and this task's commit changes exactly one path.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>The harness is complete at ten cases from seven functions: the hardcoding is kept honest by a drift leg anchored on values that are actually present, the build-supplies-the-macro contract is pinned in both directions, D-02 is a tested constraint, and the fail-closed contract is self-enforcing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host compiler subprocess → test verdict | The whole proof rests on correctly distinguishing a compile failure from a run result |
| the module's hardcoded macro-sets → the real build files | Hardcoding is honest only while the drift leg holds |
| tool absence → test outcome | A skip at exit 0 is how this project's gates failed OPEN before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-08 | Repudiation | the seam's result value carried as a process exit code | high | mitigate | The value crosses on stdout and is parsed; run returncode is asserted to be 0 separately. An acceptance criterion forbids any comparison of a run returncode against the result value |
| T-125-09 | Repudiation | a forced-capability leg that passes because the header cannot reject the value 1 | high | mitigate | The leg compiles `src/rurp_vpp.cpp`, whose own `#error` is the one that fires, and asserts the expected substring read from that file (C-4, measured) |
| T-125-10 | Tampering | a missing host compiler silently skipped, turning the whole module green-by-absence | critical | mitigate | Fail-closed resolver asserting rather than skipping, plus the self-enforcement leg scanning this module's own source with concatenation-built needles |
| T-125-11 | Tampering | the hardcoded macro-sets drifting away from the real build configs | medium | mitigate | The drift leg, anchored on values research measured to be literally present (C-6), never on framework-supplied macros that would make it fail on arrival and invite a vacuous repair |
| T-125-12 | Tampering | the capability macro re-acquiring a definition in the board header, making the guard unfirable | high | mitigate | Leg 5's second half is the direct regression guard against Phase 124's hollow-guard shape returning |
| T-125-13 | Repudiation | overclaiming that the four legs are four independent per-board verifications | medium | mitigate | The docstring states the bound explicitly: uniformity across one compiler-supplied AVR fact plus one explicit ARM declaration. Plan 125-06 repeats it in the evidence artifact |
| T-125-14 | Elevation of privilege | landing this gate as a `scripts/check_*.py` and then lowering a convention floor to quiet the meta-test | medium | mitigate | The module lives under `tests/`, where the convention glob does not reach (C-11, proven by planting one). Never lower `FLOOR` or `FIXTURE_FLOOR` |

**Package legitimacy:** not applicable — stdlib and pytest only, both already present. No `T-125-SC`
row is owed.
</threat_model>

<verification>
1. `python3 -m pytest tests/test_vpp_seam_manual_on_every_board.py -q` reports 10 passed from 7 functions, with the four board cases coming from one parametrized function.
2. `python3 -m pytest tests/ -q` reports 82 passed.
3. `tests/test_checker_convention.py` is still green with no floor changed: confirm `FLOOR` is still 5 and `FIXTURE_FLOOR` still 10 in that file, and that no new `scripts/check_*.py` was added.
4. No committed fixture translation unit or binary; the module's only artifacts are written into `tmp_path`.
5. Both `#error` messages are read from their own files at test time; neither appears as a literal in the module.
</verification>

<success_criteria>
- VPP-02's proof exists as one parametrized pytest asserting the refusal across all four board macro-sets in one run, compiled and run (D-01, ROADMAP Criterion 2).
- The capability macro is proven genuinely consulted by two dedicated legs, one per `#error` arm, each asserting its own file's message (D-03 via C-4, D-08).
- The hardcoding is kept honest by a drift leg that can actually fail (D-04 via C-6), and the build-supplies-the-macro contract is pinned in both directions (D-07).
- The fail-closed contract is self-enforcing and the module incurs zero convention debt.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

The seam symbols this module exercises — `rurp_vpp_control_mode_t`, `rurp_vpp_result_t`,
`RURP_VPP_CONTROL_MANUAL`, `RURP_VPP_CONTROL_DAC`, `RURP_VPP_OK`,
`RURP_VPP_MANUAL_ADJUSTMENT_REQUIRED`, `RURP_HAS_VPP_DAC`, `rurp_vpp_control_mode()`,
`rurp_set_vpp_target_mv()`, `rurp_disable_vpp_control()` — are **all newly created by this phase**
in Plan 125-01. They have no prior definition on this branch; a drift-verification pass must not
treat them as pre-existing identifiers.

| Symbol / path | Kind |
|---|---|
| `firestarter/tests/test_vpp_seam_manual_on_every_board.py` | new file, 7 test functions / 10 collected cases |
| the recorded suite counts 10 (module) and 82 (`tests/`) | evidence data consumed by `125-NONREGRESSION.md` |

<output>
Create `.planning/phases/125-vpp-control-seam/125-02-SUMMARY.md` when done. Record: the module's
collected case count and function count as observed from `--collect-only`; the four parametrize ids;
the verbatim outcome of each of the six compile/run legs including exit codes, stdout and warning
byte counts; both `#error` messages as read out of their files at test time; the exact anchor set the
drift leg asserts; the file used to confirm the dependency-freedom leg can fail and its observed
failure message; the confirmed-unchanged `FLOOR` and `FIXTURE_FLOOR` values; and the `pytest tests/`
count before (72) and after (82).
</output>
