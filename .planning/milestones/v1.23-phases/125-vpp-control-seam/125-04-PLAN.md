---
phase: 125-vpp-control-seam
plan: 04
type: execute
wave: 3
depends_on: ["125-01", "125-02", "125-03"]
files_modified:
  - firestarter/scripts/baseline/size_baseline.json
autonomous: true
requirements: [VPP-03]

must_haves:
  truths:
    - "AVR flash AND RAM are measured — not assumed, not recalled — for all three AVR targets on the tree carrying the seam, each from a cleaned build directory and a single build invocation (ROADMAP Criterion 4, D-15)"
    - "The measured figures are RECORDED, not gated. No new comparator, no tolerance band, no new policy flag is added by this phase — the operator took that decision against the recommendation and it is implemented as chosen (D-15)"
    - "The zero-cost claim is non-vacuous in both directions: the seam's object file provably exists in each of the three build directories, AND the three seam symbols are provably absent from the linked image while five unrelated pre-existing VPP symbols remain, so the symbol query is provably not matching nothing by accident (RESEARCH C-5, Pitfall 2)"
    - "The already-armed strict comparator is run and its exit code recorded. It is strict equality on four fields per env, proven strict by a two-byte perturbation, and never vacuous (D-16, RESEARCH C-8)"
    - "The elimination mechanism is stated correctly: link-time optimisation is in the real flag set and no upstream document named it, so the seam's object is a slim LTO object and both LTO and section garbage collection contribute. Do not attribute the zero to section GC alone (RESEARCH C-5)"
    - "D-16's re-baseline path is evaluated and its disposition recorded explicitly — taken with a reason, or not taken with the reason it must not be taken pre-emptively"
  artifacts: []
  key_links:
    - "Every measurement must follow a removal of that env's build directory and then a SINGLE build invocation with an extended timeout. A default two-minute timeout truncates the toolchain build mid-compile and silently contaminates the figure — a trap this project already recorded once in Phase 124"
    - "The frozen Phase-124 reference baseline is a separate file reached only by an explicit baseline argument. No re-baseline in this phase may touch it (D-16)"
---

<!-- planner-discipline-allow: rurp_vpp_control_mode -->
<!-- planner-discipline-allow: rurp_set_vpp_target_mv -->
<!-- planner-discipline-allow: rurp_disable_vpp_control -->

<objective>
Measure the AVR cost of the seam on all three targets, non-vacuously, and record it. Then run the
already-armed strict comparator and record its verdict, taking D-16's re-baseline path only if it
actually fires.

Purpose: ROADMAP Criterion 4 requires the AVR flash delta to be **measured against the Phase-124
baseline and recorded, not assumed to be zero** — for all three targets, flash and RAM. Research
measured 0 B and 0 B on all three, non-vacuously, but assumption A4 is explicit that those figures
must not be carried forward as this phase's evidence: re-measure on the real files. The three cold
builds are also the only real-cross-compiler proof that the seam resolves under `avr-gcc`; a
preprocessor error in either new file would fail them.

D-15 is a deliberate operator exception to this project's standing exit-code preference: the figures
are **recorded, not gated**. Implement it as chosen. Do not add a comparator leg of your own — the
pressure is already covered, because the pre-existing strict comparator is armed and a nonzero delta
goes red whether or not this phase gates on it.

Output: recorded flash and RAM for uno, uno328pb and leonardo; the two-directional non-vacuity proof;
the comparator's exit code; the warnings gate's exit code; and D-16's recorded disposition.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/125-vpp-control-seam/125-CONTEXT.md
@.planning/phases/125-vpp-control-seam/125-RESEARCH.md
@.planning/phases/125-vpp-control-seam/125-VALIDATION.md
@.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-02-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-03-SUMMARY.md

**Read order and precedence.** `125-CONTEXT.md`'s decision ids bind; RESEARCH C-1…C-18 replace
several mechanisms. For this plan the governing corrections are C-5 (the figures, the real flag set
including link-time optimisation, and the two-directional non-vacuity proof), C-8 (the comparator is
strict equality, armed, proven strict, and never vacuous; the environment seam is read once at
import so an in-process patch is ineffective and a subprocess with the child's environment is
required) and C-14 (the warnings gate has no ARM awareness at all — its universe is the three AVR
envs and two of the three native envs).

**Repo layout.** All builds and gates run in the firmware submodule `/workspaces/firestarter`,
branch `v1.23-py32f071-integration`. This plan is expected to modify **no** firmware file; the one
path in `files_modified` is reachable only if D-16's contingency actually fires, and then only in its
own commit. This plan's SUMMARY commits in the meta repo. Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`.

**Requirement ticking scope for this plan: NONE.** VPP-03 appears in `requirements` frontmatter
because this plan produces Criterion 4's measurement, but VPP-03 is discharged across several plans
and only Plan 125-06 may tick it. Do not tick VPP-01, VPP-02 or VPP-03 in
`.planning/REQUIREMENTS.md` from this plan.

**Claim ceiling.** No PY32F071 hardware exists. ARM flash and RAM are **not measurable here** — the
ARM toolchain, CMake and the build generator are all absent — so this plan measures AVR only and must
state ARM size as not measured. A local build is never evidence about ARM. AVR manual VPP control is
**permanent** (D-05), never "for now".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three cold AVR builds, and the two-directional non-vacuity proof</name>
  <files>(no files modified — measurement only; build outputs under `.pio/` are untracked)</files>
  <read_first>
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-5 in full — the real compiler and
      linker flag sets read out of the platform builder, the three measured figures, the object-file
      sizes, the symbol-query result, the five unrelated pre-existing VPP symbol names, and the note
      that no AVR env declares a source filter so all of `src/` compiles.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §"Runtime State Inventory" — the
      clean-then-single-invocation-with-extended-timeout discipline and the recorded trap it exists
      to avoid.
    - `firestarter/scripts/baseline/size_baseline.json` — the `avr_targets` block: the three recorded
      flash and RAM figures this measurement is compared against, plus `meta.note`'s description of
      the exact measurement sequence Phase 124 used. Reproduce that sequence.
    - `firestarter/platformio.ini` — confirm by reading that none of the three AVR env sections
      declares a source filter, which is why the new translation unit compiles without any build-file
      edit.
    - `.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md` — the recorded firmware `HEAD` and the
      confirmation that the seam's object file appeared after the single smoke build.
  </read_first>
  <action>
    Measure, do not cite. Research's figures are a cross-check, not this phase's evidence
    (assumption A4).

    For each of `uno`, `uno328pb` and `leonardo`, in this exact order per env: remove that env's
    build directory under `.pio/build/`, then run **one** `pio run -e <env>` invocation, capturing
    the full build log to a file, with a timeout of at least 540000 ms. One invocation per env, three
    separate cold builds. Do not batch the three into a single command whose timeout covers all of
    them, and do not reuse a warm build directory: a default two-minute timeout truncates the
    toolchain build mid-compile and silently contaminates the figure, which is a trap Phase 124
    recorded, and a warm directory measures something other than what is claimed.

    From each build log record the flash-used and RAM-used figures as the tool printed them, together
    with the totals, and state the delta against the corresponding figure in
    `scripts/baseline/size_baseline.json`. Record both flash and RAM for all three targets — Criterion
    4 and D-15 both say flash **and** RAM.

    Then prove the number is not vacuous, in both directions. Pitfall 2 is that a build-source-filter
    typo produces exactly the same zero as an honest elimination, so the figure alone proves nothing:
      - **Positive direction — the translation unit really compiled.** Confirm
        `.pio/build/<env>/src/rurp_vpp.cpp.o` exists for all three envs and record each one's byte
        size. Research observed 4520, 4520 and 4508 bytes; record what you observe.
      - **Negative direction — the symbols really vanished.** For each env, run the AVR symbol lister
        over that env's linked image and count matches for the three seam symbol names. The expected
        count is zero. In the **same** command or an adjacent one over the same image, count the
        unrelated pre-existing symbols whose names contain the VPP token — research found five
        (`eprom_check_vpp`, `get_vpp_mv`, `key_vpp_mv`, and two link-time-optimisation clones of
        `using_p1_as_vpp`) — and record that count as non-zero. A zero-versus-zero pair would mean the
        query was matching nothing at all; a zero-versus-five pair is what makes the absence a fact.
    Record the exact image path used for each env, as the build produced it, rather than assuming a
    filename pattern.

    State the elimination mechanism correctly. Link-time optimisation is present in the real compiler
    and linker flag sets — read out of the platform builder by research, and named by no upstream
    document in this project's record — so the seam's object is a slim link-time-optimisation object
    and its bodies never reach a real code section. Both link-time optimisation and section garbage
    collection contribute. Do not attribute the zero to section garbage collection alone, and do not
    describe it as expected: it is measured.

    Record explicitly, as a checked fact rather than an omission, that ARM flash and RAM are **not
    measured** and are not measurable in this environment. Criterion 4 binds AVR only, and no ARM size
    baseline exists.

    Modify no file in this task. There is nothing to commit in the firmware repo.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && ls -l .pio/build/uno/src/rurp_vpp.cpp.o .pio/build/uno328pb/src/rurp_vpp.cpp.o .pio/build/leonardo/src/rurp_vpp.cpp.o && for e in uno uno328pb leonardo; do echo "== $e =="; ELF=$(ls .pio/build/$e/*.elf); echo "image=$ELF"; echo -n "seam-symbols="; avr-nm "$ELF" | grep -cE 'rurp_vpp_control_mode|rurp_set_vpp_target_mv|rurp_disable_vpp_control' || true; echo -n "unrelated-vpp-symbols="; avr-nm "$ELF" | grep -c vpp || true; done && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three envs was measured from a removed build directory followed by exactly one `pio run` invocation; the SUMMARY states the removal command, the single invocation, and the timeout value used for each of the three.
    - Flash-used, flash-total, RAM-used and RAM-total are recorded for all three envs as the tool printed them, each alongside the corresponding figure from `scripts/baseline/size_baseline.json` and the resulting delta.
    - `.pio/build/uno/src/rurp_vpp.cpp.o`, `.pio/build/uno328pb/src/rurp_vpp.cpp.o` and `.pio/build/leonardo/src/rurp_vpp.cpp.o` all exist and each byte size is recorded.
    - For each of the three envs, the count of the three seam symbol names in that env's linked image is recorded as `0`, and the count of unrelated pre-existing symbols containing the VPP token in the **same** image is recorded as a non-zero number. Both counts are recorded per env, and the SUMMARY states in words that the zero-versus-non-zero pair is what makes the absence a fact rather than an artifact of a query matching nothing.
    - The exact linked-image path used for each env is recorded as observed from the build directory, not assumed from a naming pattern.
    - The SUMMARY names link-time optimisation as a contributor alongside section garbage collection, and does not attribute the zero to section garbage collection alone.
    - The SUMMARY states that ARM flash and RAM are not measured and not measurable here, as a checked fact, and contains no ARM size figure.
    - `git status --porcelain` in `/workspaces/firestarter` is 0 lines at the end of this task; no firmware file was modified and nothing was committed in the firmware repo.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>Criterion 4 is a measurement on the real files, on all three targets, with the zero corroborated positively by the object file's presence and negatively by a symbol query proven not to be matching nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Run the already-armed gates and record D-16's disposition</name>
  <files>firestarter/scripts/baseline/size_baseline.json (only if D-16's contingency actually fires)</files>
  <read_first>
    - `firestarter/scripts/check_size_baseline.py` — the module docstring's two modes and exit
      taxonomy, and the comparison function's field list. Its default mode is strict equality on
      flash-used, flash-total, RAM-used and RAM-total per env, with no band and no tolerance.
    - `firestarter/scripts/check_build_warnings.py` — the module docstring's usage block and its
      recognised arguments, and the two environment tuples at roughly lines 86–87. Note the third
      native env is absent from its native tuple despite being recorded in the baseline, so it must
      be passed explicitly if it is examined at all.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-8 — the comparator proven strict by a
      two-byte perturbation, its never-vacuous guard, the environment seam being read once at import,
      and the confirmation that the frozen Phase-124 reference is a genuinely separate file reached
      only by an explicit baseline argument.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` D-15 and D-16 verbatim, and
      `<specifics>`'s paragraph recording that record-without-gating is a real, intentional exception
      the planner must not quietly restore.
    - `.planning/phases/124-firmware-integration-merge/124-10-PLAN.md` — the shape a legitimate
      re-baseline commit took, if and only if D-16's contingency fires.
  </read_first>
  <action>
    Run the two gates that already exist and are already armed. **Add nothing.** D-15 forbids a new
    comparator, a tolerance band or a new policy flag; the planner must not "complete" Criterion 4 by
    inventing one, and neither may the executor.

    Run `python3 scripts/check_size_baseline.py` in its **default** mode — no policy argument, no
    baseline override, so it reads the live baseline through its documented environment seam — passing
    each of Task 1's three build logs with the repeated per-env log argument the script's usage block
    documents. Record the full output line and the exit code. Expect exit 0 with a PASS line naming
    all three envs and their four figures each. Because the environment seam is read once at import,
    never attempt to redirect it in-process; if a different baseline is ever needed, pass the explicit
    baseline argument in the child invocation instead.

    Run `python3 scripts/check_build_warnings.py` against the same three build logs using the repeated
    per-env log argument its usage block documents. Record the output and the exit code. This proves
    the new translation unit introduced no macro-redefinition warning on any AVR target, which is the
    zero-watermark property Phase 123 established. Note the script has no ARM awareness at all, so it
    says nothing about the ARM target — state that rather than letting a green line be read as
    broader than it is.

    **Then record D-16's disposition explicitly, in one of two branches.**

    Branch A — the comparator exited 0 (the expected outcome). Record that D-16's re-baseline path was
    evaluated and deliberately **not** taken, and state why taking it pre-emptively would be wrong: a
    re-baseline with no delta to explain would rewrite the recorded reference for no reason and
    destroy the very comparison Criterion 4 rests on. Confirm the live baseline file is byte-identical
    to its pre-task state by re-hashing it, and confirm the frozen Phase-124 reference is likewise
    untouched at `b940c91655600a57ad7ef67cba723943af929daf`. Commit nothing in the firmware repo.

    Branch B — the comparator exited non-zero. Then, in this order: record the exact FAIL line and the
    per-field deltas it names; determine and record the **cause** of the delta from the build logs and
    the object/symbol evidence in Task 1, not by guessing; then re-baseline only the live
    `scripts/baseline/size_baseline.json` in its **own** commit whose message states which bytes moved
    and why they are legitimate. Never widen a tolerance, never re-baseline silently, and never touch
    the frozen Phase-124 reference — it is a separate file reached only by an explicit baseline
    argument, and it remains that phase's judged comparison point. Re-run the comparator after the
    re-baseline and record the new exit code. If the cause cannot be established from evidence, STOP
    and report rather than re-baselining an unexplained delta.

    In either branch, record that this phase added **no** new gate, band or flag, and that the
    pressure is nonetheless covered because the pre-existing comparator is armed and strict.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-uno.log --avr-log uno328pb=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-uno328pb.log --avr-log leonardo=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-leonardo.log; echo "size-exit=$?"; python3 scripts/check_build_warnings.py --log uno=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-uno.log --log uno328pb=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-uno328pb.log --log leonardo=/tmp/claude-1000/-workspaces/757f8ba4-f485-4f1e-be1f-4df7bbc031aa/scratchpad/125-leonardo.log; echo "warn-exit=$?"; git hash-object scripts/baseline/size_baseline.json scripts/baseline/size_baseline_base01.json</automated>
  </verify>
  <acceptance_criteria>
    - `check_size_baseline.py` was invoked in default mode with no policy argument and no baseline override, with all three per-env log arguments; its full output line and exit code are recorded.
    - `check_build_warnings.py` was invoked with the same three logs; its output and exit code are recorded, together with an explicit statement that it has no ARM awareness and says nothing about the ARM target.
    - Task 1's three build-log file paths are recorded, and the two gate invocations reference those exact paths — the verify command's example paths are illustrative and must be replaced with the real ones.
    - Exactly one of D-16's two branches is recorded as taken, with its reasoning. If Branch A: the statement that a pre-emptive re-baseline would be wrong is present, and both baseline files are re-hashed and shown unchanged, with the frozen reference matching `b940c91655600a57ad7ef67cba723943af929daf`.
    - If Branch B: the FAIL line and per-field deltas are quoted, the cause is stated as derived from evidence rather than guessed, the re-baseline touches only `scripts/baseline/size_baseline.json` in a commit whose message states which bytes moved and why, `size_baseline_base01.json` still hashes to `b940c91655600a57ad7ef67cba723943af929daf`, and the post-re-baseline comparator exit code is recorded.
    - No new comparator, tolerance band, policy flag or gate script was added anywhere. The set of files under `firestarter/scripts/` is unchanged from Plan 125-01's recorded state; state the count and confirm it.
    - The SUMMARY states plainly that this phase records the figures without gating on them (D-15), and that the pre-existing strict comparator is nonetheless armed.
    - `python3 -m pytest tests/ -q` still reports `86 passed`.
    - `git status --porcelain` in `/workspaces/firestarter` is 0 lines at the end of the task.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>Both already-armed gates ran against the measured tree and their exit codes are recorded, D-16's contingency has an explicit recorded disposition, and no new gate, band or flag was invented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a build measurement → a cost claim | An honest zero and a vacuous zero are numerically identical |
| the recorded baseline → the measured tree | The comparator is the only thing standing between the two |
| a legitimate re-baseline → a silently moved reference | Re-baselining is how a real regression gets absorbed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-22 | Repudiation | a zero flash delta that proves nothing | high | mitigate | Two-directional non-vacuity: the object file's presence in each build directory, plus a symbol count of zero measured against a non-zero count of unrelated pre-existing symbols in the same image |
| T-125-23 | Repudiation | a figure contaminated by a truncated or warm build | high | mitigate | Build directory removed, one invocation per env, timeout at least 540000 ms, all three recorded per env. The trap is a recorded Phase-124 finding, not a hypothetical |
| T-125-24 | Tampering | a real regression absorbed by a silent re-baseline or a widened tolerance | critical | mitigate | D-16's two branches are both explicit and recorded; a re-baseline requires an evidence-derived cause and its own commit stating which bytes moved; an unexplained delta is a STOP |
| T-125-25 | Tampering | the frozen Phase-124 reference being re-baselined | high | mitigate | It is a separate file reached only by an explicit baseline argument; its blob SHA is re-asserted in this plan's acceptance criteria in both branches |
| T-125-26 | Elevation of privilege | the planner or executor "completing" Criterion 4 by adding a gate the operator declined | medium | mitigate | D-15 is implemented as chosen: record without gating. An acceptance criterion asserts the script set under `scripts/` is unchanged |
| T-125-27 | Repudiation | a green warnings gate read as covering the ARM target | medium | mitigate | The gate's environment universe is three AVR envs and two native envs and it has no ARM awareness; the SUMMARY must say so alongside the green line |
| T-125-28 | Repudiation | an ARM size figure inferred from a local build | high | mitigate | ARM size recorded as not measured and not measurable here; the ARM toolchain is absent. Plan 125-05 obtains the only admissible ARM evidence |

**Package legitimacy:** not applicable — no package is installed or upgraded. No `T-125-SC` row is
owed.
</threat_model>

<verification>
1. Three cold builds, one invocation each, extended timeout, with the removal command recorded per env.
2. Flash and RAM recorded for all three envs with deltas against the live baseline.
3. Object file present in all three build directories, byte sizes recorded.
4. Seam-symbol count zero and unrelated-VPP-symbol count non-zero, per env, from the same image.
5. `check_size_baseline.py` default mode run against the three logs; exit code recorded.
6. `check_build_warnings.py` run against the three logs; exit code recorded; its ARM blindness stated.
7. D-16's disposition recorded in exactly one branch; both baseline files re-hashed.
8. Nothing added under `firestarter/scripts/`; `pytest tests/ -q` still 86.
</verification>

<success_criteria>
- Criterion 4 is measured on all three AVR targets, flash and RAM, on the real files rather than cited from research (A4).
- The zero is non-vacuous in both directions, and the elimination mechanism is stated correctly including link-time optimisation.
- The armed comparator's verdict is recorded and D-16's contingency has an explicit disposition.
- D-15's record-without-gating exception is implemented as chosen, with no new gate, band or flag.
- ARM size is stated as not measured, with no local build cited as ARM evidence.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

`rurp_vpp_control_mode_t`, `rurp_vpp_result_t`, `RURP_VPP_CONTROL_MANUAL`, `RURP_VPP_CONTROL_DAC`,
`RURP_VPP_OK`, `RURP_VPP_MANUAL_ADJUSTMENT_REQUIRED`, `RURP_HAS_VPP_DAC`,
`rurp_vpp_control_mode()`, `rurp_set_vpp_target_mv()` and `rurp_disable_vpp_control()` are **all
newly created by this phase** in Plan 125-01 (`include/rurp_vpp.h`, `src/rurp_vpp.cpp`). This plan
proves those three function symbols are absent from the linked AVR images — their absence is the
measured outcome, not a drift finding. A drift-verification pass must not treat any of these names as
pre-existing identifiers.

| Symbol / path | Kind |
|---|---|
| the three per-env flash and RAM figures with their deltas | evidence data consumed by `125-NONREGRESSION.md` |
| the three object-file byte sizes | non-vacuity evidence (positive direction) |
| the per-env seam-symbol count 0 against a non-zero unrelated-VPP-symbol count | non-vacuity evidence (negative direction) |
| the comparator and warnings-gate exit codes | evidence data |
| D-16's recorded disposition | decision record |

<output>
Create `.planning/phases/125-vpp-control-seam/125-04-SUMMARY.md` when done. Record: per env, the
removal command, the single build invocation with its timeout, the four size figures with their
deltas against the live baseline, the object-file byte size, the linked-image path, the seam-symbol
count and the unrelated-VPP-symbol count; the three build-log paths; both gates' full output and exit
codes; D-16's branch and its reasoning; both baseline files' blob SHAs; the statement that ARM size is
not measured; and confirmation that nothing was added under `firestarter/scripts/`.
</output>
