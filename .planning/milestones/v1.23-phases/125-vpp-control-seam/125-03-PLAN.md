---
phase: 125-vpp-control-seam
plan: 03
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - firestarter/tests/test_pr45_non_ancestry.py
autonomous: true
requirements: [VPP-01]

must_haves:
  truths:
    - "None of PR #45's ten commits is an ancestor of the integration branch's `HEAD`, proven by an exit code rather than by prose (ROADMAP Criterion 1, VPP-01)"
    - "The reachability test is scoped to `HEAD` only. A repository-wide scope gets the wrong answer because `origin/feature/common-vpp-calibration` is a fetched local ref, so all ten commits are reachable somewhere in the object graph (RESEARCH C-2, measured both ways)"
    - "An unresolvable or unfetched commit object is a loud tool error, never a silent not-an-ancestor pass: research measured that the ancestry query exits 128 on a bogus reference, which a naive negated check would read as clean (RESEARCH C-2)"
    - "The check is never vacuous: the commit list is asserted to be the complete ten, distinct, and each object asserted to resolve locally before any ancestry question is asked"
    - "The two new seam files' blob SHAs differ from PR #45's corresponding blobs, so VPP-01's hand-authored claim is content-checked and not only ancestry-checked"
    - "This lands as a pytest under `tests/`, not as a `scripts/check_*.py` — research planted one and measured the cost at four extra artifacts plus two floor bumps (RESEARCH C-11). Never lower a floor"
  artifacts:
    - firestarter/tests/test_pr45_non_ancestry.py
  key_links:
    - "The ROADMAP's named mechanism for Criterion 1 is wrong in two independent ways: it searches commit *messages* rather than commit objects, which returns zero rows today and would still return zero after a cherry-pick with a rewritten message — the exact evasion the criterion exists to catch — and any repository-wide reachability scope finds all ten commits because PR #45's ref is fetched. Both were measured (C-2)"
    - "A missing `git` binary must FAIL, never skip. An absence-proxy skip reporting success at exit 0 is the failure class this milestone's Phase 123 removed (A-7)"
---

<objective>
Author the committed, exit-code-producing proof for ROADMAP Criterion 1: no commit from PR #45
(`feature/common-vpp-calibration`) is an ancestor of the firmware integration branch, and the two
new seam files are not copies of PR #45's blobs.

Purpose: VPP-01 requires the seam be hand-authored with nothing cherry-picked from PR #45, whose ten
commits reach into three of the files VPP-03 pins byte-identical. That overlap is the whole reason
cherry-picking is forbidden. The record's prescribed mechanism cannot fail, so this plan replaces it
with the shape research verified command by command — object-existence first, then a `HEAD`-scoped
ancestry query, with an explicit non-vacuity count and explicit handling of the tool-error exit.

Output: `firestarter/tests/test_pr45_non_ancestry.py`, four test functions, taking `pytest tests/ -q`
from 82 to 86.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/125-vpp-control-seam/125-CONTEXT.md
@.planning/phases/125-vpp-control-seam/125-RESEARCH.md
@.planning/phases/125-vpp-control-seam/125-PATTERNS.md
@.planning/phases/125-vpp-control-seam/125-VALIDATION.md
@.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md

**Read order and precedence.** `125-CONTEXT.md`'s decision ids are binding; several mechanisms were
measured wrong and replaced by RESEARCH C-1…C-18. For this plan the governing corrections are C-2
(the mechanism), C-3 (the complete verified ten-commit set, the branch's six-file footprint, and the
two blob SHAs) and C-11 (the shape: a pytest, not a checker script). Where CONTEXT and RESEARCH
disagree, RESEARCH wins.

**Repo layout.** The module lives in the firmware submodule at
`/workspaces/firestarter/tests/test_pr45_non_ancestry.py`, branch `v1.23-py32f071-integration`.
Commits land inside that submodule; this plan's SUMMARY commits in the meta repo. Never write into
`firestarter_py32_ci/` or `firestarter_app_py32/`.

**Requirement ticking scope for this plan: NONE.** VPP-01 appears in `requirements` frontmatter
because this plan authors half of its proof, but VPP-01 is discharged across several plans and only
Plan 125-06 may tick it. Do not tick VPP-01, VPP-02 or VPP-03 in `.planning/REQUIREMENTS.md` from
this plan.

**Claim ceiling.** No PY32F071 hardware exists. Nothing in this plan touches or claims anything
about silicon. PR #47 (`feature/py32f071-full-support`, closed, Out of Scope by requirement) does
contain a py32 VPP DAC implementation — mention it only as the reason the seam's forced-capability
message is scoped to this branch, never as prior art to restore.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The non-vacuity guard and the HEAD-scoped ancestry proof</name>
  <files>firestarter/tests/test_pr45_non_ancestry.py</files>
  <read_first>
    - `firestarter/tests/test_golden_trace_identity.py` lines 1–80 — the house style for a pytest
      whose subprocess is `git`: the module docstring's statement that it imports no checker
      machinery and asserts directly against the live tree via list-form argv invoked without a
      shell, plus the module-scope path resolution. Also read its own self-enforcement leg for the
      missing-`git` case.
    - `firestarter/tests/test_pinmap_guard_fires.py` — the fail-closed resolver skeleton, the
      assertion-message discipline that dumps both streams, and the numbered `Coverage:` docstring
      convention.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-2 — the two independent ways the
      record's mechanism fails, the correct command shape verified command by command, the measured
      tool-error exit for a fabricated reference, and the fresh-clone caveat.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-3 — the complete ten-commit set in
      chronological order, the merge base, the branch tip, the six-file footprint, and the two blob
      SHAs for the optional divergence leg.
    - `.planning/phases/125-vpp-control-seam/125-PATTERNS.md` §`tests/test_pr45_non_ancestry.py` —
      the helper shape, the whole verified test body, and the three non-negotiables.
    - `.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md` — the recorded firmware `HEAD` this
      module's ancestry queries are scoped to.
  </read_first>
  <behavior>
    - The commit list is exactly ten entries, all distinct: `04fd9b3`, `fc0b2c7`, `86f351a`,
      `768580f`, `05f4a77`, `b964ee6`, `9134f2a`, `d285b83`, `71278d0`, `a47228d` — chronological
      order, matching `origin/beta..origin/feature/common-vpp-calibration`.
    - For each of the ten, the commit-object existence query exits 0 (all ten objects resolve
      locally today, because PR #45's ref is fetched).
    - For each of the ten, the `HEAD`-scoped ancestry query exits 1, meaning not an ancestor. Any
      exit of 0 is a violation naming the offending commits. Any exit other than 0 or 1 is a loud
      tool error, never treated as clean.
    - The number of commits actually examined equals ten; a short or empty list cannot pass.
  </behavior>
  <action>
    Create `firestarter/tests/test_pr45_non_ancestry.py`. Stdlib plus pytest only, no `conftest.py`,
    every path resolved at module scope from this file's directory and its parent.

    **Module docstring.** MIT banner, then a `Phase 125 Plan 03 — ...` line, then
    `Requirements: VPP-01` and a `Decisions covered:` line naming the Criterion-1 discretion default
    and D-09/D-10 (the API-surface decisions that make content divergence from PR #45 substantial),
    then a defect-class paragraph, then the no-conftest note, then a numbered `Coverage:` list. The
    defect-class paragraph must record, in prose a reader can act on, both halves of why the record's
    prescribed mechanism was replaced:
      - it searches commit **messages** rather than commit objects, so it returns zero rows today,
        would return zero rows forever, and would still return zero rows after a cherry-pick whose
        message was rewritten — which is precisely the evasion Criterion 1 exists to catch;
      - any reachability question asked against the whole object graph gets the wrong answer, because
        `origin/feature/common-vpp-calibration` is a fetched local remote-tracking ref: research
        measured all ten commits reachable that way and none of them reachable from `HEAD`. Every
        reachability question in this module therefore names `HEAD` as its scope.
    Also state that this module asserts against the live tree via `git` subprocesses in list form,
    invoked without a shell, and that it imports no checker machinery.

    **Helpers.** A fail-closed `git` resolver: resolve the binary via `shutil.which`, assert it is
    not None with a message stating this must FAIL the suite and can never be silently bypassed. A
    thin subprocess helper that runs `git` with an explicit repository directory argument pointing at
    the resolved repository root, taking the remaining arguments as a list and returning the
    `CompletedProcess` with output captured as text. List argv only, never a shell, never a composed
    command string.

    **The commit tuple.** Define the ten abbreviated SHAs as a module-level tuple in chronological
    order, with a comment recording that this is exactly the set
    `origin/beta..origin/feature/common-vpp-calibration` enumerates, that the merge base is
    `a1953c22862ac3fb1e0111985946644a568aee36`, and that the branch tip is
    `a47228d862b9b53e6d936d1d0993bee9fc74940e`.

    **Coverage 1 — the never-vacuous guard.** One test function asserting three things before any
    ancestry question is asked anywhere in the module: the tuple has exactly ten entries; the ten
    entries are distinct; and for each entry the commit-object existence query — the `cat-file`
    subcommand with the existence flag, on the SHA suffixed with the commit-peel syntax — exits 0.
    A non-zero exit here is the tool-error class: the message must name
    `origin/feature/common-vpp-calibration` as the ref to fetch and state explicitly that an absent
    object must **not** read as "not an ancestor". Research recorded this as a real fresh-clone
    hazard: the objects resolve here only because the ref is fetched.

    **Coverage 2 — the ancestry proof.** One test function that, for each of the ten, runs the
    `merge-base` subcommand with the ancestry flag against `HEAD`, and classifies the exit code
    into exactly three buckets: 0 means the commit **is** an ancestor and is collected as a
    violation; 1 means clean; anything else is a loud failure quoting the captured error output. Do
    not write the check as a negated single call — research measured a fabricated reference producing
    exit 128 with a fatal message, which a naive negation reads as clean. Count the commits actually
    examined and assert that count equals ten, so a loop that silently skipped entries cannot pass.
    Finally assert the collected violation list is empty, with a failure message naming every
    offending commit. The docstring must state that a green run here means: ten commits examined,
    each object present, each not reachable from `HEAD`.

    Commit in the firmware submodule, e.g.
    `test(125-03): prove no PR #45 commit is an ancestor of the integration branch`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_pr45_non_ancestry.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pr45_non_ancestry.py` exists and `python3 -m pytest tests/test_pr45_non_ancestry.py -q` reports exactly `2 passed` at this task's end.
    - The module-level commit tuple contains exactly the ten abbreviated SHAs listed in the behaviour block, in that order, and the SUMMARY quotes the tuple as authored.
    - Coverage 1 asserts the tuple length equals 10, asserts the ten entries are distinct, and asserts an exit code of 0 from the object-existence query for each of the ten. Its failure message names `origin/feature/common-vpp-calibration` and states that an absent object must not read as not-an-ancestor.
    - Coverage 2 classifies the ancestry query's exit code into three buckets and fails loudly on any code other than 0 or 1, quoting the captured error stream. Verified to be a real distinction, not decoration: run the ancestry query by hand against a fabricated 40-character SHA and record the observed exit code and message in the SUMMARY.
    - Coverage 2 asserts an examined-count equal to 10 in addition to asserting the violation list is empty.
    - Every reachability query in the module names `HEAD` as its scope. The only `git` subcommands the module invokes are the object-existence, ancestry and object-hash subcommands; state the exact set in the SUMMARY, derived by reading every call site.
    - No `git` invocation in the module is repository-graph-wide in scope, and no invocation searches commit messages. Confirm by enumerating every argv list the module builds and recording the enumeration in the SUMMARY.
    - Every `subprocess.run` call passes a list argv with `capture_output=True, text=True`; none passes `shell=True`.
    - Every assertion has a failure message stating what was expected and quoting the relevant captured output.
    - No new file was created under `firestarter/scripts/`, and `tests/test_checker_convention.py`'s `FLOOR` is still 5 and `FIXTURE_FLOOR` still 10 — quote both values as read from the file.
    - `python3 -m pytest tests/ -q` reports `84 passed` at this task's end (82 from Plans 125-01/02 plus this task's 2).
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>Criterion 1 is an exit code that can actually fail: ten commits, each proven to exist locally and each proven unreachable from `HEAD`, with the tool-error exit handled explicitly and a count assertion that forecloses a vacuous pass.</done>
</task>

<task type="auto">
  <name>Task 2: The blob-divergence leg and the fail-closed self-enforcement leg</name>
  <files>firestarter/tests/test_pr45_non_ancestry.py</files>
  <read_first>
    - `firestarter/tests/test_golden_trace_identity.py` — its blob-SHA pinning legs (the house style
      for comparing a live file's hash against a recorded value) and its `git`-required
      self-enforcement leg with its own self-avoidance note.
    - `firestarter/tests/test_pinmap_guard_fires.py` lines 215–234 — the self-enforcement leg and the
      string-concatenation trick that stops it tripping on its own source.
    - `firestarter/include/rurp_vpp.h` and `firestarter/src/rurp_vpp.cpp` — as Plan 125-01 landed
      them; their current worktree hashes are what this leg compares.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-3 — the two PR #45 blob SHAs, and
      the note that D-09/D-10 already guarantee substantial divergence so this leg is cheap
      corroboration rather than the primary proof.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-18 — the sentence VPP-01 should
      cite, which this leg's docstring records.
  </read_first>
  <action>
    Extend the same module with two more test functions, adding a `Coverage:` entry for each and a
    docstring on each repeating its coverage number.

    **Coverage 3 — content divergence from PR #45's blobs.** Define a module-level mapping from the
    two seam paths to PR #45's corresponding blob SHAs: `include/rurp_vpp.h` maps to
    `c982173813b38ec745b59d6e02817f2504d6c6b4` and `src/rurp_vpp.cpp` maps to
    `fcbe009dffcd46139802f8779865a1d7aa331880`. One test function asserting, for each of the two,
    that the live worktree file exists and that its object hash — obtained with the object-hash
    subcommand — is **not** equal to PR #45's recorded value. Assert the mapping has exactly two
    entries and that both paths resolve, so an emptied mapping or a typo'd path cannot pass
    vacuously. The docstring must state why this leg exists on top of the ancestry proof: ancestry
    catches a cherry-pick, content divergence catches a copy-paste that left no commit behind. It
    must also record RESEARCH C-18's finding as the sentence VPP-01 cites — `768580f` adds the
    calibration fields to the configuration struct *before* PR #45's version bump, so cherry-picking
    it alone would change the struct layout while the version string stayed literally `VER06`, a
    silent schema change with no migration signal and strictly worse than the visible bump the record
    already warns about. That is the failure mode a well-meaning "just take the types" shortcut would
    produce, and it is why the answer is take nothing.

    Record in the SUMMARY the two observed live hashes alongside PR #45's two, so a reader can see
    the inequality rather than take it on trust.

    **Coverage 4 — the tool is required, not optional.** One test function combining the two
    protections the analogs keep separate, or two if that reads better: assert that resolving the
    `git` binary is fail-closed by calling the resolver directly, and scan this module's own source
    to assert neither skip construct appears anywhere in it. Build both needle strings by
    concatenation rather than writing them verbatim, so the leg's own assertion text does not trip
    its own check; the literal needles must appear nowhere in the file, including inside failure
    messages. Copy the analog's self-avoidance docstring note. This is what keeps the fail-closed
    contract from being edited away: a skip that reports success at exit 0 is the absence-proxy
    failure class this milestone's Phase 123 removed, and it is why every gate in this milestone
    fails rather than skips when its tool is missing.

    After this task the module collects exactly four cases from four functions and `pytest tests/ -q`
    reports 86. Record both as counts, never as "tests pass".

    Commit in the firmware submodule, e.g.
    `test(125-03): assert the seam diverges from PR #45's blobs and the gate cannot skip`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_pr45_non_ancestry.py -q 2>&1 | tail -3 && python3 -m pytest tests/test_pr45_non_ancestry.py --collect-only -q 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_pr45_non_ancestry.py -q` reports exactly `4 passed`, from exactly 4 collected cases and 4 test functions per `--collect-only -q`.
    - `python3 -m pytest tests/ -q` reports exactly `86 passed`.
    - The blob mapping has exactly two entries, keyed on `include/rurp_vpp.h` and `src/rurp_vpp.cpp`, with the two PR #45 SHAs exactly as given in the action. The leg asserts the mapping length is 2 and asserts each path exists before comparing.
    - The SUMMARY records the two live worktree hashes and the two PR #45 hashes side by side, and states the inequality for each as an observed fact.
    - The blob-divergence leg's docstring records C-18's finding — the calibration fields landing before the version bump, and why that makes a partial cherry-pick strictly worse than the visible bump.
    - The self-enforcement leg builds both needle strings by concatenation, and an independent search of the whole module for either literal needle returns zero hits, including inside every failure message. Record the search command and its zero result.
    - The `git`-resolver assertion is exercised (called) by at least one test rather than only defined.
    - `tests/test_checker_convention.py` is still green with `FLOOR` = 5 and `FIXTURE_FLOOR` = 10 unchanged, and no file was added under `firestarter/scripts/`.
    - `git status --porcelain` in `/workspaces/firestarter` is 0 lines after the commit; this task's commit changes exactly one path.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>VPP-01's proof is both ancestry-based and content-based, the whole module fails rather than skips when `git` is absent, and the contract is self-enforcing against a future edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `git` object graph → the ancestry verdict | The scope of the query is the whole correctness of Criterion 1 |
| an absent or unfetched object → the ancestry verdict | The measured exit-128 path, which a naive negation reads as clean |
| PR #45's design → this phase's files | Read as a reference, take nothing |
| tool absence → test outcome | A skip at exit 0 is how this project's gates failed OPEN before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-15 | Repudiation | a Criterion-1 check that cannot fail | high | mitigate | The message-searching mechanism is replaced by an object-existence query plus a `HEAD`-scoped ancestry query; an acceptance criterion enumerates every argv the module builds so the scope is auditable |
| T-125-16 | Spoofing | a mistyped or unfetched SHA reading as clean | high | mitigate | Coverage 1 asserts every object resolves before any ancestry question is asked, and Coverage 2 fails loudly on any exit other than 0 or 1. The fabricated-SHA behaviour is re-observed by hand and recorded |
| T-125-17 | Repudiation | a vacuous pass from a shortened or emptied commit list | high | mitigate | Length-equals-ten and distinctness assertions, plus an examined-count assertion inside the loop |
| T-125-18 | Tampering | a copy-paste of PR #45's files leaving no commit to detect | medium | mitigate | Coverage 3's blob-SHA inequality on both seam files, with a length assertion so the mapping cannot be emptied |
| T-125-19 | Tampering | a silent schema change smuggled in by a partial cherry-pick | high | mitigate | Nothing is cherry-picked at all; C-18's specific mechanism is recorded in the leg's docstring so the reasoning survives the phase. VPP-03's blob pin in Plans 125-01 and 125-06 is the complementary guard |
| T-125-20 | Tampering | a missing `git` binary silently skipped | critical | mitigate | Fail-closed resolver exercised by a test, plus the self-enforcement source scan with concatenation-built needles |
| T-125-21 | Elevation of privilege | landing this as a checker script and then lowering a convention floor | medium | mitigate | The module lives under `tests/`, outside the convention glob's non-recursive `scripts/` scope (C-11, proven by planting one). Both floors are re-read and quoted unchanged |

**Package legitimacy:** not applicable — stdlib and pytest only. No `T-125-SC` row is owed.
</threat_model>

<verification>
1. `python3 -m pytest tests/test_pr45_non_ancestry.py -q` reports 4 passed from 4 functions.
2. `python3 -m pytest tests/ -q` reports 86 passed.
3. Every argv the module builds is enumerated in the SUMMARY; every reachability query names `HEAD`; the invoked subcommand set is exactly the three named.
4. The fabricated-SHA tool-error exit is re-observed by hand and recorded, proving the three-bucket classification is a real distinction.
5. Both live seam blob hashes are recorded alongside PR #45's, showing the inequality.
6. `FLOOR` = 5 and `FIXTURE_FLOOR` = 10 quoted unchanged; nothing added under `scripts/`.
</verification>

<success_criteria>
- ROADMAP Criterion 1 is discharged by an exit code that can actually fail, replacing a mechanism that could not (C-2).
- The check is non-vacuous in three independent ways: list length, object existence, and examined count.
- Content divergence from PR #45 is asserted as well as ancestry, so a copy-paste leaves no gap.
- The module incurs zero convention debt and cannot be turned into a skip.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

`rurp_vpp_control_mode_t`, `rurp_vpp_result_t`, `RURP_VPP_CONTROL_MANUAL`, `RURP_VPP_CONTROL_DAC`,
`RURP_VPP_OK`, `RURP_VPP_MANUAL_ADJUSTMENT_REQUIRED`, `RURP_HAS_VPP_DAC`,
`rurp_vpp_control_mode()`, `rurp_set_vpp_target_mv()` and `rurp_disable_vpp_control()` are **all
newly created by this phase** in Plan 125-01, and `include/rurp_vpp.h` / `src/rurp_vpp.cpp` are new
files. A drift-verification pass must not treat any of them as pre-existing identifiers.

| Symbol / path | Kind |
|---|---|
| `firestarter/tests/test_pr45_non_ancestry.py` | new file, 4 test functions / 4 collected cases |
| the recorded live blob SHAs of the two seam files | evidence data consumed by `125-NONREGRESSION.md` |
| the recorded suite count 86 (`tests/`) | evidence data |

<output>
Create `.planning/phases/125-vpp-control-seam/125-03-SUMMARY.md` when done. Record: the ten-commit
tuple as authored; the observed existence-query exit code for each of the ten; the observed
ancestry-query exit code for each of the ten with the not-an-ancestor verdict stated; the examined
count; the hand-observed fabricated-SHA exit code and message; the enumeration of every argv the
module builds and the exact subcommand set; the two live seam blob hashes beside PR #45's two; the
zero-hit needle search command and result; the unchanged `FLOOR` and `FIXTURE_FLOOR` values; and the
`pytest tests/` count before (82) and after (86).
</output>
