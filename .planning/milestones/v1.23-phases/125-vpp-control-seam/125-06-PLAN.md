---
phase: 125-vpp-control-seam
plan: 06
type: execute
wave: 5
depends_on: ["125-05"]
files_modified:
  - .planning/phases/125-vpp-control-seam/125-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VPP-01, VPP-02, VPP-03]

must_haves:
  truths:
    - "Every row in `125-NONREGRESSION.md` was executed in the closing session against the trees exactly as they then stand — nothing is copied from any of this phase's five prior plans' SUMMARY files, and where a prior SUMMARY made a claim it is re-checked independently and said so"
    - "Criterion 3's primary proof is the blob-SHA re-hash of the three pinned files plus the literal `CONFIG_VERSION` string; `git status --porcelain` empty is a post-commit corroboration only, and every porcelain row names the FIRMWARE repo explicitly (RESEARCH C-15)"
    - "No untouched-ness claim anywhere in the artifact rests on a path-scoped diff of any kind, and none rests on a diff piped through a filter — Phase 124's verification recorded a live case where the summary trailer survived exactly such a filter and the pipeline reported empty when it was not"
    - "The claim gate is invoked with the artifact named EXPLICITLY through its target environment variable, because the gate's default target set is four Phase-130 files and would otherwise scan nothing of this phase's (RESEARCH C-16); and the artifact carries the canonical caveat sentence verbatim"
    - "The artifact bounds what the four board legs prove — uniformity across one compiler-supplied AVR fact plus one explicit ARM declaration, not four independent per-board facts (RESEARCH C-6) — and states plainly that no CI leg on this branch executes the new harness (RESEARCH C-7)"
    - "VPP-01, VPP-02 and VPP-03 are each ticked in `.planning/REQUIREMENTS.md` against a row re-executed in this session, and this is the ONLY plan in the phase permitted to tick any of them"
  artifacts:
    - .planning/phases/125-vpp-control-seam/125-NONREGRESSION.md
  key_links:
    - "This plan writes META-repo artifacts only. It must commit nothing in the firmware repo, because Plan 125-05's cited ARM run head SHA is the firmware branch tip and a further firmware commit would make the citation describe an earlier tree"
    - "Research's own first claim-gate run failed with seven self-inflicted matches, six of them in a paragraph that quoted the forbidden phrases in order to claim it had avoided them. Reference the ceiling's items by LABEL, never by phrase. The prescribed remedy is to reword the artifact, never to narrow the patterns"
---

<objective>
Close the phase: re-execute every verification row against the live trees, write
`125-NONREGRESSION.md` as the phase's evidence artifact, prove it carries no forbidden claim by
running the real gate with the target named explicitly, and tick VPP-01, VPP-02 and VPP-03.

Purpose: this phase's whole premise is that a capability seam landed and **nothing else moved**. That
is only a claim until the pins are re-hashed, the counts re-measured, the gates re-run and the whole
thing written down in one place a reader can audit. The evidence artifact is where D-15's measured
figures, D-13's ARM run URL and head SHA, Criterion 3's blob pins and the deliberate absence of an
`#include` in `include/rurp_shield.h` all land, in the command / expected / observed row shape
Phases 123 and 124 established.

Output: `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md`, a green claim-gate run naming it
explicitly, and three ticked requirement checkboxes.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-vpp-control-seam/125-CONTEXT.md
@.planning/phases/125-vpp-control-seam/125-RESEARCH.md
@.planning/phases/125-vpp-control-seam/125-PATTERNS.md
@.planning/phases/125-vpp-control-seam/125-VALIDATION.md
@.planning/phases/125-vpp-control-seam/125-01-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-02-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-03-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-04-SUMMARY.md
@.planning/phases/125-vpp-control-seam/125-05-SUMMARY.md
@.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md
@.planning/phases/124-firmware-integration-merge/124-VERIFICATION.md

**Requirement ticking scope for this plan: VPP-01, VPP-02 and VPP-03 — all three, and this plan only.**
Plans 125-01 through 125-05 were each told explicitly not to tick any of them. Each tick must cite the
row re-executed in this session that discharges it, not a prior plan's SUMMARY prose.

**This plan writes meta-repo artifacts only.** Do not commit anything in `/workspaces/firestarter`.
Plan 125-05's ARM evidence cites the firmware branch tip as its head SHA; a further firmware commit
would make that citation describe an earlier tree. If a firmware change turns out to be needed, that
is a finding to report, not something to slip in here.

**Claim ceiling and the gate that enforces it.** No PY32F071 hardware exists. The artifact must carry
the canonical caveat sentence verbatim, and must reference the ceiling's forbidden items by **label**
rather than by phrase — research's own first gate run failed with seven self-inflicted matches, six of
them in a paragraph that quoted the phrases in order to claim it had avoided them, and one in a table
column where a label spelled with a hyphen matched its own pattern. The prescribed remedy is to reword
the artifact, never to narrow the patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every verification row against the live trees</name>
  <files>(no files modified — measurement and evidence capture only)</files>
  <read_first>
    - `.planning/phases/125-vpp-control-seam/125-VALIDATION.md` §"Per-Task Verification Map" — the row
      table this task re-executes. Every row.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-15 (blob re-hash primary, porcelain
      post-commit only, firmware repo named explicitly), §C-5 (the measurement hygiene and the
      two-directional non-vacuity pair), §C-8 (the comparator's strictness and its environment seam
      read once at import), §C-9 (the host parity gate parses only one firmware header, with the
      function and line numbers), §C-10 (no host scan-path inventory entry is expected, as a checked
      fact), §C-7 (no CI leg on this branch runs the firmware pytest suite).
    - `.planning/phases/124-firmware-integration-merge/124-VERIFICATION.md` — the live informational
      finding about a diff summary piped through a filter reporting empty when it was not. Read it
      before writing any untouched-ness proof.
    - All five prior `125-0*-SUMMARY.md` files — for what each *claimed*, so this task can re-check
      each claim independently rather than inherit it.
    - `firestarter_app/tests/scan_paths.py` — the cross-repo path inventory, so the "no entry
      expected" row is a checked fact rather than an omission.
  </read_first>
  <action>
    Re-execute, in this session, against the trees exactly as they now stand. Nothing in this task may
    be copied from a prior plan's SUMMARY. Where a prior SUMMARY made a claim — a gate's exit code, a
    figure, a PASS line, a count — re-check it independently and record both the prior claim and this
    session's observation, saying which source each came from.

    **Criterion 3 — the pin.** For each of `src/boards/rurp_common.cpp`, `include/rurp_types.h` and
    `src/rurp_config_utils.cpp`: record the worktree object hash and the object hash recorded in the
    committed tree at `HEAD`, and state both equal the pre-phase value Plan 125-01 recorded. Do the
    same for the four files this phase must not have touched at all — `include/rurp_shield.h`,
    `platformio.ini`, `include/messages.h` and `scripts/baseline/size_baseline_base01.json`. Then
    record the `CONFIG_VERSION` line verbatim with its line number and state it reads as the literal
    `VER06`. **Never prove untouched-ness with a path-scoped diff of any kind**, and never with a diff
    summary piped through a filter — Phase 124's verification recorded a live case where the
    `N file changed` trailer survived exactly such a filter and the pipeline reported empty when it was
    not. Object hashes are the whole mechanism.

    **VPP-01.** Run the non-ancestry module and record its case count and outcome. Then, independently
    of the module, re-run the object-existence and `HEAD`-scoped ancestry queries for all ten PR #45
    commits by hand and record the ten exit codes, so the artifact does not rest solely on the module
    it also ships. Record the two seam files' live object hashes beside PR #45's two recorded blob
    SHAs, showing the inequality. Record the sentence VPP-01 cites for "cherry-pick nothing": the
    commit that adds the calibration fields to the configuration struct lands *before* PR #45's version
    bump, so cherry-picking it alone would change the struct layout while the version string stayed
    literally `VER06` — a silent schema change with no migration signal, strictly worse than the visible
    bump the record already warns about.

    **VPP-02.** Run the seam harness module and record its case count, its function count from a
    collect-only run, and the verbatim result. Record the **verbatim local pytest output** for the whole
    firmware `tests/` directory. Then state plainly, as a checked fact, that **no CI leg on this branch
    executes either new module**: the firmware pytest step appears only in the workflow that triggers on
    the default branch and the workflow that triggers on `beta`, and the ARM workflow has no pytest step
    at all. There is no CI oracle to fall back on, so this local run is the evidence. Do not claim CI
    coverage this branch does not have.

    Also bound the claim: the seam consults exactly two macros and nothing in it distinguishes Uno from
    Leonardo from uno328pb, so the four board legs prove uniformity across one compiler-supplied AVR
    fact plus one explicit ARM declaration — **not** four independent per-board facts. The real
    AVR-cross-compiler resolution is discharged by the three AVR builds below, which a preprocessor
    error in either new file would have failed.

    **VPP-03 / Criterion 4.** Re-measure all three AVR targets: per env, remove that env's build
    directory, then a **single** build invocation with a timeout of at least 540000 ms, capturing the
    log. Record flash-used, flash-total, RAM-used and RAM-total per env with the delta against the live
    baseline. Re-prove the non-vacuity pair per env: the seam's object file exists in that env's build
    directory with its byte size recorded, and the count of the three seam symbol names in that env's
    linked image is zero while the count of unrelated pre-existing symbols containing the VPP token in
    the same image is non-zero. Record link-time optimisation as a contributor alongside section garbage
    collection — the seam's object is a slim link-time-optimisation object — rather than attributing the
    zero to section garbage collection alone. Then run the strict comparator in its default mode against
    the three logs and record its exit code and full output line, and run the warnings gate against the
    same three logs and record its exit code, noting it has no ARM awareness at all so its green line
    says nothing about the ARM target.

    **Non-regression rows.** `pio test -e native` and `pio test -e native_nodevtools`, each with an
    extended timeout, recorded as **counts**: exactly 141 test cases, 141 succeeded, 17 suites, all
    passed. `pio test -e native_pinmap_provisional` at 10 cases / 1 suite. The ARM manifest gate at 24
    enforced sources, exit 0. The whole firmware `tests/` suite at its exact count. The convention
    meta-test's two floors quoted unchanged at 5 and 10, with the statement that neither was lowered.
    Then the host repo: run the full `firestarter_app` suite and record the exact passed count and the
    skipped count, and run the revision-constants parity module and record its exact count. Record, as
    a checked fact with the function and line numbers research established, that the parity module parses
    only one firmware header — not `include/rurp_shield.h`, which appears in that module only in
    docstrings — so this phase's new header is inert to it. Record, also as a checked fact rather than an
    omission, that the cross-repo path inventory contains none of this phase's new or edited paths, so
    **no inventory entry is expected**.

    Every porcelain statement anywhere in this task must name the **firmware** repo explicitly.
    `/workspaces/firestarter_app` has a legitimately non-empty porcelain, so an unqualified row reads as
    a failure it is not.

    Modify no file and commit nothing in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git hash-object src/boards/rurp_common.cpp include/rurp_types.h src/rurp_config_utils.cpp include/rurp_shield.h platformio.ini include/messages.h scripts/baseline/size_baseline_base01.json && git rev-parse HEAD:src/boards/rurp_common.cpp HEAD:include/rurp_types.h HEAD:src/rurp_config_utils.cpp && grep -n 'define CONFIG_VERSION' include/rurp_shield.h && python3 scripts/check_cmake_manifest.py && python3 -m pytest tests/ -q 2>&1 | tail -2 && grep -n 'FLOOR\s*=\|FIXTURE_FLOOR\s*=' tests/test_checker_convention.py && echo "fw-porcelain-lines=$(git status --porcelain | wc -l)"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains an explicit re-execution statement: every row was executed in this session against the trees as they now stand, and nothing was copied from any of the five prior plans' SUMMARY files.
    - For each of the three pinned files, both the worktree object hash and the `HEAD`-tree object hash are recorded and both equal the pre-phase value; the values are `5de1c8a1494200d8b2db210c3fd9d2d577a19b2b`, `d3fe5203a91527bdb7b20a33843c81065e21c613` and `6705fd46e07a2d359d161dc2e7728cb4e45f89c7`.
    - For each of the four must-not-touch files, the object hash is recorded and equals the pre-phase value; `include/rurp_shield.h` is `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, `platformio.ini` is `f4e720ba75a8c618cc23bac045ab65084d41a0a4`, `include/messages.h` is `dc7dbfc6b7ad3d767f7dad1ecbe13a53ca1eb346`, `size_baseline_base01.json` is `b940c91655600a57ad7ef67cba723943af929daf`.
    - The `CONFIG_VERSION` line is quoted verbatim with its line number and reads as the literal `VER06`.
    - No untouched-ness evidence anywhere in this task is a diff of any kind. Every such row is an object-hash comparison. State this explicitly.
    - All ten PR #45 commits are re-checked by hand, independently of the pytest module, with the object-existence exit code and the `HEAD`-scoped ancestry exit code recorded for each, and the ten not-an-ancestor verdicts stated.
    - The two seam files' live object hashes are recorded beside PR #45's two, and the inequality is stated for each.
    - Both new pytest modules are run and their case counts recorded — 10 for the seam harness (from 7 functions per a collect-only run) and 4 for the non-ancestry module — together with the verbatim whole-suite pytest output at its exact count.
    - The SUMMARY states plainly that no CI leg on this branch executes either new module, names why (the firmware pytest step's two host workflows and the ARM workflow's lack of one), and does not claim CI coverage.
    - The four-board claim is bounded in words: uniformity across one compiler-supplied AVR fact plus one explicit ARM declaration, not four independent per-board facts.
    - All three AVR targets are re-measured from a removed build directory with a single invocation each and a recorded timeout; four size figures per env with deltas; object-file byte size per env; seam-symbol count 0 per env against a non-zero unrelated-VPP-symbol count in the same image.
    - Link-time optimisation is named as a contributor alongside section garbage collection.
    - The strict comparator's exit code and full output line are recorded, and the warnings gate's exit code is recorded with the statement that it has no ARM awareness.
    - `pio test -e native` and `-e native_nodevtools` are each recorded as exactly 141 cases / 141 succeeded / 17 suites / all passed; `native_pinmap_provisional` as 10 cases / 1 suite. Recorded as counts, never as "tests pass".
    - The ARM manifest gate exits 0 at 24 enforced sources.
    - The convention meta-test's `FLOOR` and `FIXTURE_FLOOR` are quoted as 5 and 10 with an explicit statement that neither was lowered.
    - The full `firestarter_app` suite's exact passed count and skipped count are recorded, and the revision-constants parity module's exact count is recorded.
    - The parity module's single parsed firmware header is named with the function and line numbers, and the statement that this phase's new header is inert to it is recorded as a checked fact.
    - The absence of any cross-repo path-inventory entry for this phase's paths is recorded as a checked fact, not as an omission.
    - Every porcelain statement names the firmware repo explicitly.
    - `git status --porcelain` in `/workspaces/firestarter` is 0 lines at the end of the task and nothing was committed in the firmware repo.
  </acceptance_criteria>
  <done>Every verification row has a this-session observation with its command and its expected value, obtained independently of what the prior plans claimed, with Criterion 3 proved by object hashes and never by a diff.</done>
</task>

<task type="auto">
  <name>Task 2: Write 125-NONREGRESSION.md and prove it carries no forbidden claim</name>
  <files>.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` — the structure to reuse
      verbatim: the header block naming each repo's branch and full head SHA, the re-execution pledge,
      §1 "The claim, as precise statements" with numbered claims each naming its evidence, §2 "The
      baseline, as recorded and as re-verified" with recorded-versus-observed two-column tables, then
      per-criterion sections. Note its honesty pattern: it names the one figure that legitimately
      changed and explains why.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — read
      its default target set, its target environment variable and the separator it splits on, its
      pattern table with the proximity window, and the required-caveat pattern. Read the comment block
      near the patterns about self-matching artifacts and its prescribed remedy.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-16 — the default target set being four
      Phase-130 files, the present-but-empty-means-zero-targets behaviour, the label-versus-phrase
      technique, and the account of research's own first failing run.
    - `.planning/phases/125-vpp-control-seam/125-RESEARCH.md` §C-1 (the measured native collapse and the
      operator's Option A, which this artifact records as a deliberate decision), §C-17 (the closed
      branch that chose the opposite capability value, and why one sentence about it belongs here),
      §C-6 and §Pitfall 8 (what the four legs prove).
    - `.planning/phases/125-vpp-control-seam/125-PATTERNS.md` §`125-NONREGRESSION.md` — the
      Phase-125-specific content list this artifact must carry.
    - Task 1's recorded observations — the sole source for every row.
  </read_first>
  <action>
    Write `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md`, reusing Phase 124's structure.

    **Header block.** Name the firmware branch and its full head SHA, the host branch and its full head
    SHA, and the meta branch. Then the **re-execution pledge**: every row below was executed in this
    session against the trees exactly as they now stand, nothing is copied from any of this phase's
    five prior plans' SUMMARY files, and where a prior SUMMARY made a claim this document re-checked it
    independently and says so.

    **The canonical caveat, verbatim.** The document must contain the sentence stating that no PY32F071
    hardware exists, spelled exactly as the gate's required-caveat pattern expects — the words "no",
    "PY32F071", "hardware", "exists" in that order with single spaces. Place it in the header block so
    a reader meets it first.

    **§1 The claim, as precise statements.** Numbered claims, each naming its evidence row. At minimum:
    the seam is two hand-authored files taking nothing from PR #45; the capability macro's value comes
    from the compiler on AVR and from the ARM build for py32f071, never from the header; both `#error`
    arms are proven able to fire, each by its own leg against its own file's message; the refusal holds
    on all four board macro-sets compiled and run; the three pinned files and four must-not-touch files
    are byte-identical by object hash; `CONFIG_VERSION` is still the literal `VER06`; the AVR cost is
    measured on all three targets, flash and RAM, non-vacuously; and the ARM target configures and
    builds, cited by run URL plus head SHA.

    **§2 The baseline, as recorded and as re-verified.** Recorded-versus-observed tables for the three
    AVR targets' flash and RAM, and for the three native envs' case and suite counts.

    **Per-criterion sections**, each in the command / expected / observed row shape, carrying:
      - Criterion 1: the ten commits with both exit codes each, the examined count, the two blob
        inequalities, and the sentence VPP-01 cites for taking nothing.
      - Criterion 2: the harness's case and function counts, the verbatim local pytest output, the
        bounded statement of what the four legs prove, and the plain statement that no CI leg on this
        branch executes the harness.
      - Criterion 3: the seven object-hash rows and the `CONFIG_VERSION` row, with an explicit sentence
        that object hashes are the primary proof, that a porcelain row is corroboration only and names
        the firmware repo, and that no row is a diff of any kind — with a pointer to Phase 124's live
        informational finding about a filtered diff summary reporting empty when it was not.
      - Criterion 4: the three targets' figures with deltas, the object-file presence per env, the
        symbol counts per env with their non-zero counterpart, link-time optimisation named alongside
        section garbage collection, the measurement hygiene actually used, and the comparator's and
        warnings gate's exit codes. State that these figures are **recorded, not gated** by this phase
        (D-15's deliberate operator exception, taken against the recommendation), while noting the
        pre-existing comparator is armed and strict — and record D-16's disposition as Plan 125-04
        determined it.
      - Non-regression: the ARM manifest gate at 24, the two pinned native envs at 141/17, the third at
        10/1, the whole firmware suite count, the two unchanged convention floors, the host suite's
        passed and skipped counts, the parity module's count, the checked fact that the parity module
        parses only one firmware header, and the checked fact that no cross-repo path-inventory entry is
        expected.

    **Three Phase-125-specific decisions this artifact must record as decisions, with their reasons.**
      1. `include/rurp_shield.h` carries **no** `#include` of the seam header, and that absence is
         deliberate — the operator's Option A. Record the measured reason: adding that one line takes
         the native environment from 141 cases with 141 succeeded to 17 suites with 0 succeeded, all
         errored, because that header has 46 include sites of which 14 are native host-stub translation
         units, the host compiler defines no AVR predicate, and no native environment declares the
         capability macro. Record that Option B — keeping the include and declaring the macro in two
         native environments — was also measured green and was declined because it contradicts the
         reason D-06 chose the compiler-supplied predicate and puts a build-flag edit inside the phase
         whose premise is that nothing else moved.
      2. The AVR fact is **permanent**, not provisional (D-05, operator). Write it that way; do not hedge
         it as "for now" or "pending hardware".
      3. The py32 declaration is `0` while a closed branch chose the opposite value. One sentence: that
         branch is out of scope by requirement, its tip is not an ancestor, and there is no hardware, so
         `0` is the only defensible value here — recorded so a later reader does not treat that branch's
         choice as prior art to restore.

    **Then run the claim gate, for real.** Invoke `check_permitted_claims.py` with this artifact's
    absolute path named through the gate's target environment variable — the gate's default target set
    is four Phase-130 files and would otherwise scan nothing of this phase's, and a present-but-empty
    value means zero targets rather than falling back to the defaults. Record the exact invocation and
    the exit code.

    **Reference the ceiling's forbidden items by LABEL, never by phrase.** Research's own first run
    failed with seven self-inflicted matches, six of them in a paragraph that quoted the phrases in
    order to claim it had avoided them, and one in a table column where a label spelled with a hyphen
    matched its own pattern. Expect to reword; the prescribed remedy is to reword the artifact, **never**
    to narrow the patterns or edit the gate. If the gate reports a match, fix the prose and re-run until
    it exits 0, recording each rewording. Note in passing that the seam source's own `#error` message may
    legitimately be quoted in this artifact — verify that by running the gate, not by reasoning about the
    patterns.

    Commit in the **meta** repo only, e.g.
    `docs(125-06): write 125-NONREGRESSION.md, the phase's re-executed evidence artifact`.
  </action>
  <verify>
    <automated>cd /workspaces && FIRESTARTER_CLAIMSCAN_TARGETS="/workspaces/.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md" python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py; echo "claimscan-exit=$?"; grep -c 'no PY32F071 hardware exists' .planning/phases/125-vpp-control-seam/125-NONREGRESSION.md; cd /workspaces/firestarter && echo "fw-porcelain-lines=$(git status --porcelain | wc -l)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md` exists and follows Phase 124's structure: a header block naming each repo's branch and full head SHA, the re-execution pledge, §1 numbered claims each naming its evidence, §2 recorded-versus-observed tables, then per-criterion sections in command / expected / observed row shape.
    - The re-execution pledge is present and explicitly states that nothing is copied from any of this phase's five prior plans' SUMMARY files.
    - The gate's required caveat sentence about PY32F071 hardware appears verbatim at least once; a positive count of that sentence in the file returns at least 1.
    - `check_permitted_claims.py` was invoked with this artifact's absolute path named through the gate's target environment variable, and the invocation and exit code are recorded. The final recorded exit code is 0.
    - The gate's target environment variable was set to a non-empty value naming the artifact; the SUMMARY states that the gate's default target set is four Phase-130 files and that a present-but-empty value would mean zero targets rather than a fall-back.
    - The forbidden items are referenced by label rather than by phrase anywhere the artifact discusses the ceiling. If the gate reported any match, each rewording is recorded, and neither the gate's pattern table nor its target logic was edited.
    - Criterion 3's section states explicitly that object hashes are the primary proof, that any porcelain row is corroboration only and names the firmware repo, and that no row is a diff of any kind; it points at Phase 124's live informational finding about a filtered diff summary reporting empty when it was not.
    - Criterion 4's section states that the figures are recorded and not gated by this phase, names that as the operator's deliberate exception taken against the recommendation, notes the pre-existing comparator is nonetheless armed and strict, and records D-16's disposition as Plan 125-04 determined it.
    - Criterion 2's section carries the verbatim local pytest output, the bounded statement of what the four board legs prove, and the plain statement that no CI leg on this branch executes either new module.
    - The three Phase-125-specific decisions are each recorded as a decision with its reason: the deliberate absence of an include in `include/rurp_shield.h` with the measured 141-cases-to-17-suites-zero-succeeded reason and the declined Option B; the AVR fact stated as permanent with no provisional hedge anywhere in the document; and the one sentence about the closed branch that chose the opposite capability value.
    - The document contains no ARM size figure and states ARM flash and RAM as not measured; every ARM statement cites the run URL plus head SHA from Plan 125-05.
    - The commit is in the **meta** repo only. `git status --porcelain` in `/workspaces/firestarter` is 0 lines and the firmware HEAD is unchanged from Plan 125-05's recorded value.
    - No requirement checkbox has been ticked yet at the end of this task — that is Task 3.
  </acceptance_criteria>
  <done>The phase's evidence artifact exists in the established row shape, every row sourced from this session's re-execution, and the real claim gate exits 0 against it with the target named explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Tick VPP-01, VPP-02 and VPP-03, and record the post-commit corroboration</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"VPP Control Seam" — the three checkbox lines to tick, verbatim as
      they stand, plus §"Validation Ceiling" one more time before writing anything.
    - `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md` as written in Task 2 — each tick
      must cite a row in it.
    - `.planning/ROADMAP.md` §"Phase 125: VPP Control Seam" — the four success criteria, so each tick
      can be mapped to the criteria it discharges.
  </read_first>
  <action>
    Tick exactly three checkboxes in `.planning/REQUIREMENTS.md` — VPP-01, VPP-02 and VPP-03 — and no
    others. This is the only plan in the phase permitted to tick any of them; Plans 125-01 through
    125-05 were each told explicitly not to. Change only the checkbox character on those three lines;
    do not reword the requirement text, and do not touch any other requirement in the file.

    For each tick, record in the SUMMARY the specific row or rows in `125-NONREGRESSION.md` that
    discharge it, and the ROADMAP success criterion each maps to. Read each requirement's **prose** and
    satisfy yourself the whole of it is discharged before ticking — not the abbreviation, not a plan
    title, the prose. VPP-01 covers hand-authorship AND non-ancestry; VPP-02 covers the return value on
    every board macro-set asserted by a test across each macro-set; VPP-03 covers three files untouched
    AND the version string AND the AVR flash delta measured rather than asserted. If any clause of any
    of the three is not discharged by a re-executed row, do **not** tick it — report the gap instead.

    Then record the **post-commit corroboration** for Criterion 3, which is only meaningful after the
    last code commit and only for the firmware repo: `git status --porcelain` in
    `/workspaces/firestarter` is 0 lines. Name the firmware repo explicitly in the row. Note in the
    SUMMARY that `/workspaces/firestarter_app`'s porcelain is legitimately non-empty for unrelated
    reasons, so the row is scoped deliberately rather than selectively. Also re-assert that the firmware
    HEAD still equals the head SHA Plan 125-05's ARM run cites, so the ARM evidence still describes the
    branch tip.

    Commit in the **meta** repo only, e.g.
    `docs(125-06): tick VPP-01..03 against re-executed rows and close the phase`. If `125-NONREGRESSION.md`
    needed a corroboration row appended, include that in the same commit.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n 'VPP-01\|VPP-02\|VPP-03' .planning/REQUIREMENTS.md && echo "unticked-vpp=$(grep -c '^- \[ \] \*\*VPP-0' .planning/REQUIREMENTS.md)" && echo "ticked-vpp=$(grep -c '^- \[x\] \*\*VPP-0' .planning/REQUIREMENTS.md)" && cd /workspaces/firestarter && echo "fw-porcelain-lines=$(git status --porcelain | wc -l)" && git rev-parse HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Exactly three VPP checkboxes are ticked in `.planning/REQUIREMENTS.md`: the count of ticked VPP lines is 3 and the count of unticked VPP lines is 0.
    - No requirement outside the VPP group changed state. Confirm by recording the ticked/unticked counts for every requirement group in the file before and after, and by the commit's diff touching only the three VPP lines.
    - The requirement text on all three lines is byte-identical to before except for the checkbox character.
    - Each of the three ticks is justified in the SUMMARY by naming the specific `125-NONREGRESSION.md` row or rows that discharge it and the ROADMAP success criterion it maps to; each justification addresses **every** clause of the requirement's prose, not just its headline.
    - `git status --porcelain` in `/workspaces/firestarter` is recorded as 0 lines, with the firmware repo named explicitly and a note that the host repo's non-empty porcelain is unrelated and expected.
    - The firmware `HEAD` is re-asserted equal to the head SHA Plan 125-05's ARM run cites, so the ARM evidence still describes the branch tip.
    - The commit is in the meta repo only; nothing was committed in `/workspaces/firestarter`.
  </acceptance_criteria>
  <done>All three VPP requirements are ticked, each against a row re-executed in this session rather than against a prior plan's prose, and Criterion 3's post-commit corroboration is recorded scoped to the firmware repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a prior plan's SUMMARY → this phase's evidence | Copying a claim forward is how an unverified statement becomes a fact |
| the artifact → an outside reader | This document is what a stranger reads; an overclaim here is the one that escapes |
| the claim gate's default target set → this phase's artifact | The gate scans nothing of this phase's unless the target is named |
| a requirement checkbox → the milestone record | A tick is the strongest claim in the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-125-37 | Repudiation | evidence copied from a prior plan's SUMMARY rather than re-executed | high | mitigate | The re-execution pledge plus an acceptance criterion requiring both the prior claim and this session's observation to be recorded with their sources |
| T-125-38 | Repudiation | an untouched-ness claim proved with a diff that passes vacuously | high | mitigate | Object hashes in both the worktree and the `HEAD` tree; porcelain as corroboration only; an explicit criterion that no row is a diff of any kind, pointing at Phase 124's live finding |
| T-125-39 | Repudiation | a forbidden claim entering the artifact and reaching a stranger | critical | mitigate | The real gate run with the target named explicitly through its environment variable, iterated to exit 0, with the ceiling's items referenced by label rather than by phrase — the technique research had to apply to its own document |
| T-125-40 | Tampering | narrowing the gate's patterns or editing its target logic to make the artifact pass | critical | mitigate | An acceptance criterion forbids both; the prescribed remedy is rewording the artifact, and each rewording is recorded |
| T-125-41 | Repudiation | a green gate run that scanned nothing | high | mitigate | The target is named explicitly and the SUMMARY records that a present-but-empty value means zero targets rather than a fall-back to the four Phase-130 defaults |
| T-125-42 | Elevation of privilege | ticking a requirement whose prose is only partly discharged | high | mitigate | Each tick must address every clause of the requirement's prose against a named re-executed row; an undischarged clause is a reported gap, never a tick |
| T-125-43 | Repudiation | overclaiming that the four board legs are four independent per-board verifications | medium | mitigate | The bound is stated in the artifact and in the harness module's own docstring; the real AVR-compiler resolution is attributed to the three AVR builds instead |
| T-125-44 | Spoofing | an ARM citation that no longer describes the branch tip | high | mitigate | This plan commits nothing in the firmware repo, and Task 3 re-asserts the firmware `HEAD` equals the cited run's head SHA |
| T-125-45 | Repudiation | claiming CI coverage this branch does not have | medium | mitigate | The artifact states plainly that no CI leg on this branch executes either new module, and carries the verbatim local pytest output as the evidence instead |

**Package legitimacy:** not applicable — no package is installed. No `T-125-SC` row is owed.
</threat_model>

<verification>
1. Every row in `125-NONREGRESSION.md` traces to a this-session command with its expected and observed values; the re-execution pledge is present.
2. Criterion 3 is proved by object hashes in both the worktree and the `HEAD` tree, for three pinned plus four must-not-touch files, with `CONFIG_VERSION` quoted; no row is a diff.
3. Criterion 4's figures are recorded with their non-vacuity pair per env; the comparator and warnings gate exit codes are recorded; D-16's disposition is stated.
4. Both pinned native envs recorded at 141 cases / 17 suites; the manifest gate at 24; both convention floors unchanged at 5 and 10.
5. The host suite's passed and skipped counts and the parity module's count are recorded; the parity module's single parsed firmware header is named; the no-inventory-entry fact is recorded as checked.
6. `check_permitted_claims.py` exits 0 with the artifact named explicitly; the caveat sentence is present verbatim.
7. Exactly three VPP checkboxes ticked, no other requirement changed, each tick justified against a named row for every clause of its prose.
8. Nothing committed in `/workspaces/firestarter`; its porcelain is 0 lines; its `HEAD` still equals the cited ARM run's head SHA.
</verification>

<success_criteria>
- The phase's claim — a capability seam landed and nothing else moved — is a set of re-executed rows in one auditable document, not a set of assertions.
- Criterion 3 is proved by object hashes and never by a diff; Criterion 4 by measurement with a two-directional non-vacuity pair; Criterion 1 by exit codes; Criterion 2 by a parametrized compile-and-run harness whose scope is honestly bounded.
- The artifact passes the real claim gate with its target named explicitly, and carries the canonical caveat verbatim.
- VPP-01, VPP-02 and VPP-03 are ticked by this plan and only this plan, each against a re-executed row covering every clause of its prose.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

`rurp_vpp_control_mode_t`, `rurp_vpp_result_t`, `RURP_VPP_CONTROL_MANUAL`, `RURP_VPP_CONTROL_DAC`,
`RURP_VPP_OK`, `RURP_VPP_MANUAL_ADJUSTMENT_REQUIRED`, `RURP_HAS_VPP_DAC`,
`rurp_vpp_control_mode()`, `rurp_set_vpp_target_mv()` and `rurp_disable_vpp_control()` are **all newly
created by this phase** in Plan 125-01, together with the new files `firestarter/include/rurp_vpp.h`,
`firestarter/src/rurp_vpp.cpp`, `firestarter/tests/test_vpp_seam_manual_on_every_board.py` and
`firestarter/tests/test_pr45_non_ancestry.py`. A drift-verification pass must not treat any of them as
pre-existing identifiers.

| Symbol / path | Kind |
|---|---|
| `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md` | new file (meta repo) — the phase's evidence artifact |
| the three ticked VPP requirement checkboxes | milestone record (meta repo) |
| the recorded claim-gate invocation and exit code | evidence datum |

<output>
Create `.planning/phases/125-vpp-control-seam/125-06-SUMMARY.md` when done. Record: the re-execution
pledge; every row's command, expected value and this-session observation, with the prior plan's claim
beside it where one existed; the seven object hashes in both the worktree and the `HEAD` tree; the
`CONFIG_VERSION` line; the ten by-hand PR #45 exit-code pairs; the two blob inequalities; the two
modules' case counts and the verbatim whole-suite pytest output; the three AVR targets' figures with
their non-vacuity pairs; both gates' exit codes and D-16's disposition; the three native envs' counts;
the manifest gate's count; the two unchanged convention floors; the host suite's passed and skipped
counts and the parity module's count; the claim-gate invocation, exit code and any rewordings applied;
the three ticks with their per-clause justifications and the criteria they map to; and the post-commit
firmware porcelain and `HEAD` re-assertion.
</output>
