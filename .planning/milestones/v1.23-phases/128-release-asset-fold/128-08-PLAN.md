---
phase: 128-release-asset-fold
plan: 08
type: execute
wave: 5
depends_on: ["128-04", "128-07"]
files_modified:
  - firestarter/platform/py32f071/README.md
autonomous: true
requirements: [REL-02, REL-03]

must_haves:
  truths:
    - "MISMATCH-1 (128-PATTERNS, HIGH): `platform/py32f071/README.md` has **no** `## Release integration` section today. Its only headings are `# Firestarter on PY32F071`, `## Implemented`, `## Provisional example pin map`, `## Build`, `## Hardware validation still required`. D-15 is therefore **add-the-section-in-corrected-form**, not edit-in-place; the section exists only inside the unmerged commit `ad47c3b`."
    - "D-15: the section's release-file entry is the GLOB `build/py32f071/firestarter_*.hex`, matching the shipped `beta-build.yml` `files:` entry exactly. `ad47c3b` supplied the literal directly under prose arguing for a glob (MISMATCH-3) — that slip is corrected here."
    - "F-1 / MISMATCH-2: the glob-versus-literal *justification* is rewritten, not carried. The action makes no such distinction; the soft-absence property comes from the unmatched-files input's `false` default. And `ad47c3b`'s claim that the rename means the existing release glob needs no new pattern is false — a second `files:` entry was required and has now landed."
    - "D-05: the README states the `continue-on-error` removal trigger as a recorded decision rather than `ad47c3b`'s suggestion — it comes off when the target is validated on real silicon; no PCB exists, so it is unreachable this milestone and the flag stays."
    - "D-13: `build.yml` (stable / `main`) is out of scope, recorded with its graduation trigger — fold into `build.yml` when `py32f071` leaves the host's `BETA_ONLY_BOARDS`. A stable release carrying a py32 asset would advertise an image the stable CLI exits 2 on."
    - "D-14: the hyphen-to-underscore rename is verified grep-consistent across CMakeLists, both workflow files, the composite action and the docs — the property `ad47c3b`'s commit message claimed but never built."
    - "Claim ceiling: the section describes publication only. Nothing in it says the image runs, boots or installs; no PY32F071 PCB exists."
  artifacts:
    - firestarter/platform/py32f071/README.md
  key_links:
    - "README `## Release integration` -> the shipped `beta-build.yml` `files:` block (must match exactly)"
    - "README's recorded triggers -> D-05's `continue-on-error` removal and D-13's `build.yml` graduation"
---

<objective>
Add a `## Release integration` section to `platform/py32f071/README.md` in corrected form,
and prove the hyphen-to-underscore rename is consistent across every file in the firmware
repo that names the emitted image.

Purpose: `ad47c3b` wrote this section, and it contains three defects — a literal supplied
under prose arguing for a glob, a false justification for that argument, and a false claim
that the existing release glob needed no new pattern. The section is worth having; the
defects are not. This is also the only place the two standing triggers (D-05's
`continue-on-error` removal and D-13's `build.yml` graduation) become documented decisions
rather than someone's memory.

**Scope boundary — this plan does NOT close REL-02 or REL-03.** It documents the shipped
mechanism and closes the D-14 consistency check. Both requirements still need Plan 128-10's
observed run evidence. Do not tick REL-02 or REL-03 in REQUIREMENTS.md from this plan.

Output: one modified README, plus a repo-wide consistency proof recorded in the SUMMARY.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-04-SUMMARY.md
@.planning/phases/128-release-asset-fold/128-07-SUMMARY.md

**Repo:** `/workspaces/firestarter`, branch `v1.23-py32f071-integration`. Commit inside the
sub-repo.

**This is the last firmware commit of the phase.** Plan 128-09's app-side test asserts the
firmware working tree is clean (`_git_porcelain(FW_ROOT) == ""`, research finding F-16), so
every firmware edit must be committed before that plan runs. D-19's ordering is encoded in
the wave graph, not left to prose.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `files:` entry:** `build/py32f071/firestarter_*.hex`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (rehearsal mode only)

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`,
`firestarter_py32f071_build_diagnostics`

**New README section (this plan):** `## Release integration` in
`platform/py32f071/README.md`, inserted between the existing `## Build` and
`## Hardware validation still required` headings

<tasks>

<task type="auto">
  <name>Task 1: Add the Release integration section in corrected form</name>
  <read_first>
    - `/workspaces/firestarter/platform/py32f071/README.md` — the whole file (67 lines). Its headings are at lines 1, 5, 24, 55 and 65. **There is no `## Release integration` heading** — do not go looking for one to edit; this task ADDS it. Line 63 (`Generated outputs are ELF, BIN, HEX, linker map, size report, and SHA-256 checksums.`) is the one existing line the new material replaces.
    - `git -C /workspaces/firestarter show ad47c3b -- platform/py32f071/README.md` — the unmerged prose. Reuse its structure and its correct paragraphs; three specific claims in it must be rewritten (see the action).
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` as shipped after Plan 128-07 — the `Release` step's `files:` block, the `rehearsal` input, the `arm` call site's `continue-on-error`, and the AVR gate step. **The README must describe what actually shipped, not what `ad47c3b` proposed.**
    - `/workspaces/firestarter/.github/workflows/py32f071.yml` as shipped after Plan 128-04 — the composite call and the single-image artifact upload.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-PATTERNS.md` MISMATCH-1, MISMATCH-2 and MISMATCH-3 (the third quotes the exact slip verbatim).
    - `/workspaces/firestarter_app/firestarter/channel.py` and the host's `BETA_ONLY_BOARDS` definition — read enough to state D-13's graduation trigger accurately.
  </read_first>
  <files>firestarter/platform/py32f071/README.md</files>
  <action>
Replace line 63 and insert a new `## Release integration` section between `## Build` and
`## Hardware validation still required`.

First, replace line 63's outputs sentence with `ad47c3b`'s corrected version: the generated
outputs are ELF, BIN, HEX, linker map and size report, all under `build/py32f071/`; only the
`.hex` install image is ever published, the rest being dev-environment artefacts reproducible
with the two commands above, which CI does not upload. Then keep `ad47c3b`'s
underscore-is-load-bearing paragraph — the host installer resolves release assets by
`firestarter_<board>.hex`, where `<board>` is the `RURP_BOARD_NAME` the firmware reports over
the wire — but **delete its final sentence**, the one claiming this means `beta-build.yml`'s
existing release glob needs no new pattern. That is false (R-16, MISMATCH-2) and a second
`files:` entry was required.

Then write `## Release integration`, describing what actually shipped:

1. **Why the build lives in `beta-build.yml`.** Keep `ad47c3b`'s reasoning verbatim in
   substance: `beta-build.yml` runs `.github/scripts/update_version.py`, which rewrites
   `include/version.h` and auto-commits it *before* building; an image built in any other job
   compiles a stale `VERSION` string, and the host's whole update decision is a comparison of
   that string against the release tag. Add the fact `ad47c3b` could not know: the build is
   reached through a **composite action** at `.github/actions/build-py32f071/`, called by both
   workflows, because a composite action's steps run in the calling job — a reusable
   `workflow_call` workflow would be a separate job with a separate checkout and would break
   that ordering outright.
2. **The release file list, as shipped.** Show the two-entry `files:` block exactly as it
   appears in `beta-build.yml` — `.pio/build/**/firestarter_*.hex` and
   `build/py32f071/firestarter_*.hex`. Two entries because PlatformIO writes under
   `.pio/build/` and CMake writes under `build/py32f071/`, and one glob cannot cover both
   trees. The second entry is a glob, not a literal (D-15 / MISMATCH-3).
3. **Why an absent py32 image does not fail the release, correctly explained.** Do **not**
   write that the action warns on an unmatched glob but fails on a missing literal. That
   claim appears in `ad47c3b`, in the milestone research, and in the requirement's own text,
   and it is false: the action globs every entry, literal or glob alike, and the only knob is
   the unmatched-files failure input, whose default is false and which this workflow
   deliberately does not set. Write the correct mechanism, and note that a glob is still
   preferred here for rename-resilience and for reading consistently with the AVR entry — a
   style argument, not a failure-mode argument.
4. **The two builds and the removal trigger (D-05).** State the split as a decision:
   `py32f071.yml` is the LOUD gate on `beta` with no containment; `beta-build.yml`'s call
   site carries `continue-on-error: true` and exists only to produce the asset. State the
   removal trigger — the flag comes off when the target is validated on real silicon — and
   state plainly that no PY32F071 PCB exists, so it is unreachable today and the flag stays
   deliberately. Replace `ad47c3b`'s "consider `continue-on-error`" suggestion with this
   recorded decision.
5. **The AVR-assets gate.** One sentence: `scripts/check_release_assets.py` runs
   unconditionally before the release step and fails the build if any AVR hex named by
   `scripts/baseline/size_baseline.json`'s `avr_targets` keys is missing or empty. It never
   requires the py32 image.
6. **The rehearsal switch.** One short paragraph: `beta-build.yml` carries a permanent
   `rehearsal` boolean dispatch input that publishes a draft under a `rehearsal-<run_id>` tag
   instead of a real pre-release. Note that a rehearsal dispatch must always supply
   `beta_version` explicitly, or `update_version.py` takes the stable path.
7. **Stable channel, out of scope (D-13).** One paragraph: `build.yml` (stable / `main`) is
   deliberately untouched, because `py32f071` is in the host's `BETA_ONLY_BOARDS` and a stable
   release carrying a py32 asset would advertise an image the stable CLI exits 2 on. Record
   the graduation trigger: fold into `build.yml` when the board leaves `BETA_ONLY_BOARDS`.

End the section with the claim ceiling, stated in one sentence: everything above describes
**publication**; nothing here says the published image runs, boots or installs, because no
PY32F071 PCB exists.

<!-- planner-discipline-allow: fail_on_unmatched_files -->
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import pathlib,re
t=pathlib.Path('platform/py32f071/README.md').read_text()
assert '## Release integration' in t
assert 'build/py32f071/firestarter_*.hex' in t
assert 'build/py32f071/firestarter_py32f071.hex' not in t, 'the R-16 literal slip is still present'
for bad in ['fails on a missing literal','warns on an unmatched glob','needs no new pattern','consider \`continue-on-error\`']:
    assert bad not in t, bad
for good in ['BETA_ONLY_BOARDS','real silicon','composite action','update_version.py','check_release_assets.py','rehearsal','beta_version']:
    assert good in t, good
h=[l for l in t.splitlines() if l.startswith('## ')]
assert h.index('## Release integration')==h.index('## Build')+1, h
assert 'firestarter-py32f071' not in t
print('OK', h)
"</automated>
  </verify>
  <acceptance_criteria>
    - `platform/py32f071/README.md` contains a `## Release integration` heading, positioned immediately after `## Build` and before `## Hardware validation still required`.
    - The section contains the glob `build/py32f071/firestarter_*.hex` and does NOT contain the literal `build/py32f071/firestarter_py32f071.hex`.
    - The file contains none of these phrases: `fails on a missing literal`, `warns on an unmatched glob`, `needs no new pattern`, `consider \`continue-on-error\``.
    - The file contains all of: `BETA_ONLY_BOARDS`, `real silicon`, `composite action`, `workflow_call`, `update_version.py`, `check_release_assets.py`, `avr_targets`, `rehearsal`, `beta_version`.
    - The file contains a sentence stating no PY32F071 PCB exists and that nothing in the section claims the image runs, boots or installs.
    - The file contains zero occurrences of the hyphenated `firestarter-py32f071`.
    - The pre-existing `## Implemented`, `## Provisional example pin map`, `## Build` and `## Hardware validation still required` sections are otherwise unchanged: `git diff` touches only line 63's region and the inserted section.
  </acceptance_criteria>
  <done>The README gains the section `ad47c3b` intended, with its literal corrected to a glob, its glob rationale replaced by the real mechanism, its `continue-on-error` suggestion promoted to a recorded decision with a trigger, D-13's graduation trigger written down, and the claim ceiling stated.</done>
</task>

<task type="auto">
  <name>Task 2: Prove README-to-workflow parity and repo-wide rename consistency</name>
  <read_first>
    - The README as edited by Task 1.
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` — the shipped `files:` block.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-03-SUMMARY.md` and `128-04-SUMMARY.md` — the recorded hyphenated-occurrence baseline and its closure.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` § D-14 — the instruction to verify the rename is grep-consistent across CMakeLists, both workflow files, the composite action and the docs, and the note that `ad47c3b`'s own message claimed grep-verification but was never built.
  </read_first>
  <files>firestarter/platform/py32f071/README.md</files>
  <action>
Verification and record-keeping; edit the README only if a parity check fails.

**Parity.** Extract the two `files:` entries from the shipped `beta-build.yml` by parsing the
YAML, and extract the release-file entries the README shows in its `## Release integration`
section by parsing the fenced block. Assert the two sets are equal, with a non-vacuity guard
on each parse first — assert each extraction yielded exactly two non-empty entries before
comparing, so a README rewrite that breaks the fence, or a YAML change that empties the
block, fails loudly rather than passing on two empty lists (A-7, Pitfall 8). D-15's whole
instruction is that the README entry and the shipped entry match; this makes that
mechanical rather than a promise.

**Rename consistency (D-14).** Run one grep for the hyphenated `firestarter-py32f071` across
the entire firmware working tree — CMakeLists, both workflow files, the composite action,
`platform/py32f071/README.md`, and everything else tracked — and assert zero occurrences.
Then run the positive counterpart: for each of the five files D-14 names, record the count of
the underscored form present. `ad47c3b`'s commit message claimed the rename was
grep-verified but never built; this is the check that makes the claim real, and its output
belongs in the SUMMARY as a table.

**Claim discipline.** Record in the SUMMARY, verbatim: no ARM build was performed locally —
no ARM toolchain exists in this devcontainer — so nothing in this plan or the README claims
the published image runs, boots or installs. The permitted claim is exactly one sentence
wide: the asset publishes. Note that every ARM figure this milestone cites carries a CI run
URL plus a commit SHA.

**Firmware-tree closure (D-19 / F-16).** This is the last firmware commit of the phase.
After committing, assert `git -C /workspaces/firestarter status --porcelain` is empty and
record the resulting HEAD SHA in the SUMMARY — Plan 128-09's app-side test asserts that same
tree is clean, and Plan 128-10 cites the SHA.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib,re
wf=yaml.safe_load(pathlib.Path('.github/workflows/beta-build.yml').read_text())
rel=[s for s in wf['jobs']['build']['steps'] if s.get('name')=='Release'][0]
yml=[l.strip() for l in rel['with']['files'].strip().splitlines() if l.strip()]
assert len(yml)==2 and all(yml), yml
t=pathlib.Path('platform/py32f071/README.md').read_text()
sec=t.split('## Release integration',1)[1]
doc=sorted(set(re.findall(r'(?m)^\s*((?:\.pio/build|build/py32f071)/\S*firestarter_\S*\.hex)\s*\$', sec)))
assert len(doc)==2 and all(doc), doc
assert sorted(yml)==doc, (sorted(yml), doc)
print('PARITY OK', doc)
" && test "$(grep -r 'firestarter-py32f071' --exclude-dir=.git . | wc -l)" -eq 0 && test -z "$(git status --porcelain)" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - The parity script prints `PARITY OK` with the two-element list, and fails if either parse yields fewer than two non-empty entries.
    - `grep -r 'firestarter-py32f071' --exclude-dir=.git /workspaces/firestarter` returns zero lines.
    - The SUMMARY carries a table of underscored-form occurrence counts for `platform/py32f071/CMakeLists.txt`, `.github/workflows/beta-build.yml`, `.github/workflows/py32f071.yml`, `.github/actions/build-py32f071/action.yml` and `platform/py32f071/README.md`.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green; `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` unchanged.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing, and the HEAD SHA is recorded in the SUMMARY.
    - The SUMMARY carries the verbatim non-claim sentence about no local ARM build and the one-sentence-wide claim ceiling.
  </acceptance_criteria>
  <done>The README's release-file entries provably equal the shipped ones, the rename is grep-clean across the whole firmware tree, the firmware working tree is clean with a recorded HEAD SHA, and the claim ceiling is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future contributor's behaviour | A wrong explanation beside correct code is the mechanism by which a corrected defect gets reintroduced |
| documented file list -> shipped file list | A README that drifts from the workflow silently teaches the wrong thing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-18 | Tampering | a future contributor setting the unmatched-files failure input, having read the README's old rationale | medium | mitigate | the false rationale is deleted and the corrected mechanism written in its place; the invariant itself is enforced by Plan 128-01's comment-stripped test |
| T-128-20 | Repudiation | the `continue-on-error` flag persisting with no recorded reason, so it reads as an oversight rather than a decision | low | mitigate | D-05's removal trigger is stated in the README as a decision, alongside the fact that no PCB exists so the trigger is unreachable this milestone |
| T-128-21 | Spoofing | a stable release advertising a py32 asset the stable CLI exits 2 on | medium | mitigate | D-13 keeps `build.yml` out of scope and the README records the graduation trigger (`BETA_ONLY_BOARDS`); Task 2 asserts `build.yml` was never touched by this phase |
| T-128-22 | Repudiation | documentation claiming or implying the published image runs | high | mitigate | the section ends with an explicit non-claim; an acceptance criterion asserts that sentence is present, and the SUMMARY repeats it for Phase 130's CLOSE-02 ledger |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). This plan edits one Markdown file. |
</threat_model>

<verification>
- `## Release integration` exists, immediately after `## Build`, and contains the glob rather than the literal.
- The four forbidden phrases are absent and the nine required substrings are present.
- README release-file entries equal the shipped `beta-build.yml` `files:` entries, proven with a non-vacuity guard on each parse.
- Zero hyphenated `firestarter-py32f071` occurrences anywhere in the firmware working tree.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green; `pio test -e native` and `pio test -e native_nodevtools` unchanged.
- `git -C /workspaces/firestarter status --porcelain` empty; HEAD SHA recorded — this is the precondition Plan 128-09 depends on (F-16).
- No local ARM build was possible. Nothing here claims the image runs, boots or installs.
</verification>

<success_criteria>
- The README carries a `## Release integration` section describing what shipped, with the glob, the corrected mechanism, D-05's removal trigger, D-13's graduation trigger, the rehearsal switch and the `beta_version` warning.
- The section ends with an explicit non-claim about the image running.
- README and workflow release-file entries are proven equal.
- The hyphen-to-underscore rename is grep-clean repo-wide across all five files D-14 names.
- The firmware working tree is clean, closing D-19's firmware half.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-08-SUMMARY.md` when done.
</output>
