---
phase: 128-release-asset-fold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/tests/fixtures/clean_release_assets_all_three/README.md
  - firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/uno/firestarter_uno.hex
  - firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/uno328pb/firestarter_uno328pb.hex
  - firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/leonardo/firestarter_leonardo.hex
  - firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/README.md
  - firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/pio_build/uno/firestarter_uno.hex
  - firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/pio_build/leonardo/firestarter_leonardo.hex
  - firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/README.md
  - firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/uno/firestarter_uno.hex
  - firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/uno328pb/firestarter_uno328pb.hex
  - firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/leonardo/firestarter_leonardo.hex
  - firestarter/scripts/check_release_assets.py
  - firestarter/tests/test_check_release_assets.py
  - firestarter/tests/test_checker_convention.py
autonomous: true
requirements: [REL-03, REL-02]

must_haves:
  truths:
    - "D-11: `scripts/check_release_assets.py` exists in the repo's established checker shape and exits non-zero on a planted missing or zero-byte AVR hex — proven by a paired pytest that invokes it as a real subprocess against committed fixtures, not by assertion."
    - "D-12: the required AVR set is derived from `scripts/baseline/size_baseline.json`'s `avr_targets` keys, never from three hardcoded filenames, and an empty key set exits 1 rather than printing PASS."
    - "REL-03 (checker slice only): the AVR-assets-present gate is provable locally without CI and tolerates the py32 image's absence — the clean control fixture contains no py32 image and the checker still exits 0."
    - "REL-02 (invariant slice only): `beta-build.yml` never *sets* the release action's unmatched-files failure input as a YAML key; the paired test asserts this over comment-stripped source with a non-vacuity guard."
    - "F-6: no fixture directory is named `.pio` — the build root is reached through the new `FIRESTARTER_PIO_BUILD_ROOT` seam and every fixture path is proven tracked with `git ls-files`, never with `git add`'s exit code."
    - "F-11: `test_checker_convention.py`'s `FLOOR` and `FIXTURE_FLOOR` rise in the SAME commit that adds the checker, and the pre-existing `FIXTURE_FLOOR` drift (10 recorded vs 13 actual) is corrected."
  artifacts:
    - firestarter/scripts/check_release_assets.py
    - firestarter/tests/test_check_release_assets.py
    - firestarter/tests/fixtures/clean_release_assets_all_three/
    - firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/
    - firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/
  key_links:
    - "`FIRESTARTER_SIZE_BASELINE` seam → `avr_targets` keys → the required `<build_root>/<key>/firestarter_<key>.hex` set"
    - "`FIRESTARTER_PIO_BUILD_ROOT` seam → the committed fixture trees (this is the only reason the fixtures are committable at all)"
    - "`tests/test_checker_convention.py` FLOOR/FIXTURE_FLOOR → the new checker/test/fixture triple"
---

<objective>
Land the D-11/D-12 AVR-assets-present gate as a complete, locally-provable triple:
three committed fixture trees, `scripts/check_release_assets.py`, its mandatory paired
pytest, and the `test_checker_convention.py` floor bump — all in the firmware repo on
branch `v1.23-py32f071-integration`.

Purpose: REL-03's own wording is *"demonstrably fails"*. Three inline `test -s` lines in
the release YAML would be unprovable outside a `workflow_dispatch`. A checker with a
planted fixture is an exit code anyone can run in one second. This plan produces the gate;
Plan 128-07 wires its call site into `beta-build.yml`.

**Scope boundary — this plan does NOT close REL-03 or REL-02.** It closes only the
*locally-provable checker* slice of REL-03 and the *`fail_on_unmatched_files` invariant*
slice of REL-02. REL-03 additionally needs the `beta-build.yml` call site (128-07), the
LOUD/soft split (128-04), and run B's observed evidence (128-10). REL-02 additionally
needs the `files:` block (128-07) and run A's asset list (128-10). Do not tick REL-02 or
REL-03 in REQUIREMENTS.md from this plan.

Output: a sixth firmware checker, its paired test module, three fixture trees, and raised
convention floors.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-VALIDATION.md

**Repo:** all work lands in `/workspaces/firestarter` (the firmware sub-repo), on branch
`v1.23-py32f071-integration`. Commit **inside that sub-repo**, not in the meta-repo. The
meta-repo tracks only `.planning/` and `.claude/`.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:**
- `scripts/check_release_assets.py`
- `tests/test_check_release_assets.py`
- `tests/fixtures/clean_release_assets_all_three/` (+ `README.md`, `pio_build/…`)
- `tests/fixtures/planted_release_assets_missing_uno328pb/` (+ `README.md`, `pio_build/…`)
- `tests/fixtures/planted_release_assets_zero_byte_leonardo/` (+ `README.md`, `pio_build/…`)
- `.github/actions/` (new directory) and `.github/actions/build-py32f071/action.yml`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT` (the existing `FIRESTARTER_SIZE_BASELINE` is reused, not new)

**New CLI flags:** `--build-root <path>` and `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`'s `workflow_dispatch`

**New step ids in `beta-build.yml`:** `mode`, `arm` (the existing `version` and `release_target` ids are unchanged)

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (rehearsal mode only)

**New fixture directory component:** `pio_build/` — deliberately **not** `.pio/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`, `firestarter_py32f071_build_diagnostics`

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto">
  <name>Task 1: Commit the three release-assets fixture trees under non-dotted build roots</name>
  <read_first>
    - `/workspaces/firestarter/tests/fixtures/README.md` — the whole file. Lines 8-16 define the `captured_*` / `planted_*` / `clean_*` naming contract; lines 27-34 are the `git ls-files`-not-`git add` rule this task exists to honour.
    - `/workspaces/firestarter/tests/fixtures/planted_cmake_manifest_missing_source/README.md` — the only *directory-tree* planted fixture in the repo and the exact derivation-statement shape to copy.
    - `/workspaces/firestarter/.gitignore` — line 1 is the bare pattern `.pio`, which matches at **any depth**. This is why the fixture build root is named `pio_build/`.
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — confirm the `avr_targets` keys are exactly `uno`, `uno328pb`, `leonardo`; the fixture layout mirrors them.
  </read_first>
  <files>
firestarter/tests/fixtures/clean_release_assets_all_three/README.md
firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/uno/firestarter_uno.hex
firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/uno328pb/firestarter_uno328pb.hex
firestarter/tests/fixtures/clean_release_assets_all_three/pio_build/leonardo/firestarter_leonardo.hex
firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/README.md
firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/pio_build/uno/firestarter_uno.hex
firestarter/tests/fixtures/planted_release_assets_missing_uno328pb/pio_build/leonardo/firestarter_leonardo.hex
firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/README.md
firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/uno/firestarter_uno.hex
firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/uno328pb/firestarter_uno328pb.hex
firestarter/tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/leonardo/firestarter_leonardo.hex
  </files>
  <action>
Create three fixture trees under `/workspaces/firestarter/tests/fixtures/`. Every build
root directory is named `pio_build` — never `.pio` — because `.gitignore` line 1 is the
bare pattern that matches at any depth and `git add` on an ignored path exits 0 while
staging nothing (research finding F-6, Pitfall 2).

**`clean_release_assets_all_three/pio_build/`** — the control that must pass. One
subdirectory per `avr_targets` key: `uno/firestarter_uno.hex`,
`uno328pb/firestarter_uno328pb.hex`, `leonardo/firestarter_leonardo.hex`. Each hex file is
a minimal but syntactically real Intel-HEX stub: one data record followed by the EOF
record `:00000001FF`. Deliberately no py32 image anywhere in this tree — the checker must
exit 0 without one (D-11's rejection of a do-everything script; REL-03's tolerance).

**`planted_release_assets_missing_uno328pb/pio_build/`** — derived from the clean tree by
exactly one edit: `uno328pb/firestarter_uno328pb.hex` is absent, and because git cannot
track an empty directory the `uno328pb/` directory is absent with it. `uno/` and
`leonardo/` are byte-identical to the clean tree.

**`planted_release_assets_zero_byte_leonardo/pio_build/`** — derived from the clean tree by
exactly one edit: `leonardo/firestarter_leonardo.hex` is truncated to **zero bytes**. All
three directories exist. This case is load-bearing, not belt-and-braces: the release
action's own `paths()` filter accepts any path where `statSync(p).isFile()` is true, so a
zero-byte hex would be uploaded as a real asset (research finding F-1, secondary
observation).

Each fixture directory gets a `README.md` stating, in the shape
`tests/fixtures/planted_cmake_manifest_missing_source/README.md` uses: the fixture's
purpose, its derivation as *"= `clean_release_assets_all_three/` with `<the one edit>`"*,
and the note that the `.hex` payloads are truncated presence/size stand-ins — the checker
reads only `Path.exists()` and `stat().st_size`, never file content, so a truncated stub
is faithful to everything the checker actually inspects. Also state in each README why the
directory is `pio_build/` and not `.pio/`.

Stage and commit inside `/workspaces/firestarter` on branch `v1.23-py32f071-integration`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git ls-files tests/fixtures/clean_release_assets_all_three tests/fixtures/planted_release_assets_missing_uno328pb tests/fixtures/planted_release_assets_zero_byte_leonardo | wc -l | grep -qx 11 && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files tests/fixtures/clean_release_assets_all_three` lists exactly 4 paths (`README.md` + three `.hex`).
    - `git ls-files tests/fixtures/planted_release_assets_missing_uno328pb` lists exactly 3 paths, and none of them contains the substring `uno328pb/`.
    - `git ls-files tests/fixtures/planted_release_assets_zero_byte_leonardo` lists exactly 4 paths.
    - `git check-ignore -v tests/fixtures/clean_release_assets_all_three/pio_build/uno/firestarter_uno.hex` exits non-zero (no ignore rule matches).
    - `find tests/fixtures/clean_release_assets_all_three tests/fixtures/planted_release_assets_missing_uno328pb tests/fixtures/planted_release_assets_zero_byte_leonardo -name '.pio' | wc -l` prints `0`.
    - `stat -c %s tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build/leonardo/firestarter_leonardo.hex` prints `0`; the same command on the clean tree's leonardo hex prints a value greater than `0`.
    - `grep -rl 'py32' tests/fixtures/clean_release_assets_all_three/pio_build | wc -l` prints `0`.
    - Each of the three fixture `README.md` files contains the literal substring `clean_release_assets_all_three` (the derivation statement) — for the clean fixture's own README this is its directory name in the purpose line.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` is green (adding orphan fixtures must not disturb `test_checker_convention.py`, whose floors are `>=` assertions).
  </acceptance_criteria>
  <done>Eleven fixture paths are tracked in the index, the zero-byte plant is genuinely zero bytes, no directory component is `.pio`, and the existing firmware pytest suite is still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add check_release_assets.py, its paired pytest, and the raised convention floors in ONE commit</name>
  <read_first>
    - `/workspaces/firestarter/scripts/check_size_baseline.py` — the structural analog. Read lines 1-79 (the six-block docstring: title/phase, mode description, `Exit codes:` taxonomy, `Anti-hollow contract:`, `Non-claim:`, `Usage:`), lines 85-110 (repo-root resolution + the `FIRESTARTER_SIZE_BASELINE` env seam with a committed default), lines 310-326 (`_print_pass` / `_print_fail`), lines 329-386 (the manual argv parser and its no-argparse justification), lines 395-403 (the never-vacuous guard placed BEFORE the loop), and lines 486-487 (the `sys.exit(main(sys.argv[1:]))` entrypoint).
    - `/workspaces/firestarter/tests/test_check_size_baseline.py` — lines 1-105 (the four-block module docstring, including the verbatim no-`conftest.py` house-rule note), lines 113-135 (path constants and the `_run_checker` subprocess harness with `env_overrides`), lines 191-209 (asserting the *literal* exit code, not just non-zero), lines 245-264 (the seam-precedence test template).
    - `/workspaces/firestarter/tests/test_checker_convention.py` — the module docstring block at lines 53-66 (which enumerates the five checkers by name and states each floor's provenance) and the floor constants at lines 121-124.
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — the `avr_targets` object this checker derives its required set from.
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` — the whole file (103 lines). The paired test greps it; the test must know exactly what it is reading.
  </read_first>
  <files>
firestarter/scripts/check_release_assets.py
firestarter/tests/test_check_release_assets.py
firestarter/tests/test_checker_convention.py
  </files>
  <behavior>
    - Clean control tree → exit 0, stdout starts with `PASS:` and names all three envs.
    - Planted missing-hex tree → exit 1, stdout contains `FAIL:` and names `uno328pb` and the expected path; stdout never contains `PASS:`.
    - Planted zero-byte tree → exit 1, stdout contains `FAIL:` and names `leonardo` and the observed size `0`.
    - Baseline whose `avr_targets` is an empty object → exit 1 (never-vacuous guard), stdout never contains `PASS:`.
    - Baseline that is not valid JSON, or has no `avr_targets` key, or whose `avr_targets` is not an object → exit 2.
    - Unknown argv flag, or a flag missing its value → exit 2.
    - `FIRESTARTER_PIO_BUILD_ROOT` set in the child environment to the planted tree flips a would-be-clean invocation to exit 1 (seam precedence).
    - `FIRESTARTER_SIZE_BASELINE` set in the child environment to a tampered baseline carrying a fourth `avr_targets` key flips the clean tree to exit 1 (seam precedence).
    - The clean tree contains no py32 image and still exits 0 (REL-03 tolerance).
    - `beta-build.yml`, with comment lines removed, contains no line setting the release action's unmatched-files failure input as a YAML key.
  </behavior>
  <action>
Write `/workspaces/firestarter/scripts/check_release_assets.py`, `/workspaces/firestarter/tests/test_check_release_assets.py`, and the
`test_checker_convention.py` floor bump, then commit **all three together**.
`test_checker_convention.py`'s own docstring (lines 53-66) mandates that both floors rise
in the SAME commit that adds the checker; splitting them across commits leaves an
intermediate red suite.

**Checker (`scripts/check_release_assets.py`).** Stdlib only (`json`, `os`, `sys`,
`pathlib.Path`). No argparse — the house convention is a hand-rolled argv parser with its
own docstring justifying the absence, raising `SystemExit(2)` on any malformed form.
Reproduce all six docstring blocks from `check_size_baseline.py:1-79`, naming Phase 128,
Plan 01, requirements REL-03 and REL-02, decisions D-11 and D-12, and the
`Anti-hollow contract:` paragraph naming `tests/test_check_release_assets.py` and the
`planted_release_assets_*` / `clean_release_assets_all_three` fixtures.

Resolve `REPO_ROOT = Path(__file__).resolve().parent.parent`. Two env seams, both read
with committed defaults and both documented as *set in the CHILD environment*:
`FIRESTARTER_SIZE_BASELINE` (reused, defaulting to
`REPO_ROOT / "scripts" / "baseline" / "size_baseline.json"`) and the new
`FIRESTARTER_PIO_BUILD_ROOT` (defaulting to `REPO_ROOT / ".pio" / "build"`). Each is
overridable by an argv flag: `--baseline <path>` and `--build-root <path>`, argv winning
over env, env winning over the default. The new seam exists because a fixture tree cannot
contain a `.pio` directory (F-6) — say so in the seam's comment.

Logic, in this order. Load the baseline JSON; any read/parse failure, a top-level value
that is not an object, a missing `avr_targets` key, or an `avr_targets` value that is not
an object → print `FAIL:` naming the file and the specific format problem, return **2**.
Take `sorted(avr_targets.keys())`. If that list is empty → print a `FAIL:` naming the
never-vacuous guard, in the wording shape of `check_size_baseline.py:395-403` (*a gate that
requires nothing must not print PASS*), return **1**; this guard runs BEFORE the per-env
loop. For each key `k`, require `<build_root>/<k>/firestarter_<k>.hex` to exist and have
`stat().st_size > 0`; collect one failure line per violation naming the env, the absolute
expected path, and the observation (`missing` or `0 bytes`). Any failures → `_print_fail`
in the two-part house shape, return **1**. Otherwise `_print_pass` listing every env
compared and the resolved build root, return **0**.

The checker must **never** mention, require, or look for the py32 image. Its absence is
REL-03's whole point and D-11 rejected a do-everything script precisely so this file has
no severity logic. Entrypoint: `if __name__ == "__main__": sys.exit(main(sys.argv[1:]))`.

**Paired test (`tests/test_check_release_assets.py`).** Copy
`test_check_size_baseline.py`'s four-block docstring shape, including the verbatim
no-`conftest.py` note at its lines 102-104. Module constants `_HERE`, `_REPO_ROOT`,
`_CHECKER = _REPO_ROOT / "scripts" / "check_release_assets.py"`, `_FIXTURES`,
`_BASELINE`, and `_BETA_BUILD = _REPO_ROOT / ".github" / "workflows" / "beta-build.yml"`.
Reproduce `_run_checker(argv=None, env_overrides=None)` exactly: list-form
`subprocess.run([sys.executable, str(_CHECKER), *argv], cwd=str(_REPO_ROOT), capture_output=True, text=True, env={**os.environ, **(env_overrides or {})})`
— never `shell=True`, never an in-process import. Every assert carries an f-string message
echoing `stdout` and `stderr`. The literal `returncode != 0` must appear at least once
(convention test 6) and the literal filename `check_release_assets.py` must appear
(convention test 5).

Ten numbered `Coverage:` entries, one test function each, matching the `<behavior>` list
above. Two of them need specific care:

*The seam-precedence pair.* One test proves `FIRESTARTER_PIO_BUILD_ROOT` is genuinely read
(point it at `planted_release_assets_missing_uno328pb/pio_build` via `env_overrides` and
assert the run flips to exit 1). One proves `FIRESTARTER_SIZE_BASELINE` is genuinely read
(load the real baseline, add a fourth key such as `mega2560` with the same value shape,
write it to `tmp_path`, point the seam at it, and assert the clean tree now exits 1 naming
that fourth env). Without both, the seam could be embedded in the script and nothing would
notice.

*The unmatched-files-input invariant test.* Read `_BETA_BUILD` as text. Build the list of
lines whose first non-whitespace character is **not** `#`. Non-vacuity first: assert that
list is non-empty and that some surviving line contains `softprops/action-gh-release@v2`
— a file that failed to load, or a wholesale rewrite, must fail this test rather than pass
it vacuously. Then assert no surviving line matches the regex `fail_on_unmatched_files\s*:`.
The comment-stripping is **required**, not cosmetic: Plan 128-07 adds a comment on the
`Release` step that names this input by name in order to explain why it is deliberately
unset, and a naive whole-file grep would make the explanation self-invalidating. State
that reason in the test's docstring.

<!-- planner-discipline-allow: fail_on_unmatched_files -->

**Convention floors (`tests/test_checker_convention.py`).** Raise `FLOOR` from `5` to `6`.
Recount `planted_*` entries after Task 1 landed — do not predict — with
`ls -d tests/fixtures/planted_* | wc -l`; the derivation is 13 pre-existing entries plus
this phase's two, so the expected value is **15**, but use the counted value if it differs.
Raise `FIXTURE_FLOOR` from `10` to that number. Update the docstring block at lines 53-66
so it names `check_release_assets.py` as the sixth checker and records that
`FIXTURE_FLOOR` was carrying a pre-existing drift (10 recorded against 13 actual, left
behind by Phases 124 and 126) which this commit corrects — so the change reads as a
deliberate correction rather than an unrelated edit. Never lower a floor.

Commit all three files together inside `/workspaces/firestarter` on
`v1.23-py32f071-integration`, and note the drift correction in the commit message.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_check_release_assets.py tests/test_checker_convention.py -q && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python3 -m pytest tests/test_check_release_assets.py -q` reports 10 passed, 0 skipped, 0 failed.
    - `cd /workspaces/firestarter && python3 -m pytest tests/test_checker_convention.py -q` reports 7 passed.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` is green with no skips introduced by this plan.
    - `cd /workspaces/firestarter && python3 scripts/check_release_assets.py --build-root tests/fixtures/clean_release_assets_all_three/pio_build; echo $?` prints `0` and the stdout begins with `PASS:`.
    - `cd /workspaces/firestarter && python3 scripts/check_release_assets.py --build-root tests/fixtures/planted_release_assets_missing_uno328pb/pio_build; echo $?` prints `1`, stdout contains `FAIL:` and the substring `uno328pb`, and stdout does not contain `PASS:`.
    - `cd /workspaces/firestarter && python3 scripts/check_release_assets.py --build-root tests/fixtures/planted_release_assets_zero_byte_leonardo/pio_build; echo $?` prints `1` and stdout contains `leonardo`.
    - `cd /workspaces/firestarter && python3 scripts/check_release_assets.py --nonexistent-flag; echo $?` prints `2`.
    - `grep -c 'FLOOR = 6' tests/test_checker_convention.py` is at least 1, and `grep -Ec '^FIXTURE_FLOOR = (1[1-9]|[2-9][0-9])' tests/test_checker_convention.py` is 1 (the floor rose above 10).
    - `grep -c 'check_release_assets.py' tests/test_check_release_assets.py` is at least 1 and `grep -c 'returncode != 0' tests/test_check_release_assets.py` is at least 1 (convention tests 5 and 6).
    - `grep -c 'argparse' scripts/check_release_assets.py` counts only the house-convention justification text, and `grep -c '^import argparse\|^from argparse' scripts/check_release_assets.py` is `0`.
    - Comment-stripped grep over the checker source finds `FIRESTARTER_PIO_BUILD_ROOT` and `FIRESTARTER_SIZE_BASELINE`, and finds no occurrence of `py32` outside the docstring's REL-03-tolerance explanation.
  </acceptance_criteria>
  <done>Six checkers ship with six paired tests and a raised, recounted fixture floor; the new checker's ten behaviours are proven by subprocess against committed fixtures; the whole firmware pytest suite is green in a single commit.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the triple is committed, not merely on disk</name>
  <read_first>
    - `/workspaces/firestarter/tests/fixtures/README.md` lines 27-34 — the `git ls-files`-never-`git add` rule.
    - The commit(s) produced by Tasks 1 and 2 (`git -C /workspaces/firestarter log --stat -3`).
  </read_first>
  <files>firestarter/tests/fixtures/README.md</files>
  <action>
Verification-only, plus one documentation edit. Confirm every new path is in the git
index, not merely on disk — F-6's failure mode is that everything passes locally while
the commit contains no fixture at all.

Run `git -C /workspaces/firestarter ls-files tests/fixtures/ scripts/check_release_assets.py tests/test_check_release_assets.py`
and confirm all 13 new paths appear. Run `git -C /workspaces/firestarter status --porcelain`
and confirm it is empty. Then append one row to `tests/fixtures/README.md`'s existing
inventory (or add it in whatever form that file lists fixtures) naming the three new
`release_assets` fixture trees, their owning checker, and the one-line reason the build
root is `pio_build/` rather than `.pio/`. Keep the edit scoped — do not rewrite the file.

If any path is missing from the index, the fixture is ignored: fix the path, do not
`git add -f`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(git ls-files tests/fixtures/clean_release_assets_all_three tests/fixtures/planted_release_assets_missing_uno328pb tests/fixtures/planted_release_assets_zero_byte_leonardo scripts/check_release_assets.py tests/test_check_release_assets.py | wc -l)" -eq 13 && test -z "$(git status --porcelain)" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files` over the five new path roots returns exactly 13 paths.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing.
    - `tests/fixtures/README.md` contains the substrings `release_assets` and `pio_build`.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
    - `git -C /workspaces/firestarter branch --show-current` prints `v1.23-py32f071-integration`.
  </acceptance_criteria>
  <done>All 13 new paths are tracked, the firmware working tree is clean, and the fixtures README names the new trees and why their build root is not dotted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release job → published assets | Anything the release step uploads is public and permanent; a missing or zero-byte asset is a silent integrity failure |
| committed fixture → git index | A fixture that is ignored rather than committed makes a local green run meaningless for everyone else |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-04 | Tampering | `beta-build.yml` release step publishing an incomplete asset set | high | mitigate | `scripts/check_release_assets.py` derives the required set from `size_baseline.json` `avr_targets` and exits 1 on any missing or zero-byte hex; proven by two planted fixtures |
| T-128-09 | Tampering | a zero-byte hex uploaded as a real asset (`statSync().isFile()` accepts it) | medium | mitigate | the `st_size > 0` check plus the dedicated `planted_release_assets_zero_byte_leonardo` fixture |
| T-128-10 | Repudiation | a green local suite over a fixture tree that is not in the index | medium | mitigate | Task 3's `git ls-files` proof; `git add`'s exit code is never used as evidence |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs **zero** packages in either ecosystem (128-RESEARCH § Standard Stack: no Package Legitimacy Audit applies). The checker is Python stdlib only. |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — green.
- `cd /workspaces/firestarter && python3 -m pytest tests/test_check_release_assets.py -q -rs` — 10 passed, no skips.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` — unchanged by this plan; a regression here means something outside this plan broke.
- `git -C /workspaces/firestarter status --porcelain` — empty (a precondition for Plan 128-09's app-side test, per F-16).
</verification>

<success_criteria>
- Six `scripts/check_*.py` files exist, each with a paired test module and at least one `planted_*` fixture.
- `FLOOR` is 6 and `FIXTURE_FLOOR` is the recounted post-phase value (>= 15), both raised in the same commit as the checker.
- The checker exits 0 on the clean tree, 1 on both planted trees, 1 on an empty `avr_targets`, and 2 on an unparseable baseline or malformed argv.
- The `fail_on_unmatched_files` invariant test passes over comment-stripped `beta-build.yml` source with its non-vacuity guard satisfied.
- All 13 new paths are in the git index and the firmware working tree is clean.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-01-SUMMARY.md` when done.
</output>
