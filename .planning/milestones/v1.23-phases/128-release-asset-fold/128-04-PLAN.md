---
phase: 128-release-asset-fold
plan: 04
type: execute
wave: 2
depends_on: ["128-02", "128-03"]
files_modified:
  - firestarter/.github/workflows/py32f071.yml
autonomous: true
requirements: [REL-03, REL-04]

must_haves:
  truths:
    - "D-05: `py32f071.yml` stays the LOUD gate — no `continue-on-error` anywhere in it, red and staying red when ARM breaks on `beta`. This pairing is what makes the SOFT copy's containment in `beta-build.yml` defensible; neither half is sound alone."
    - "D-05: the `continue-on-error` removal trigger is recorded in-file — it comes off when the target is validated on real silicon. No PCB exists, so it is unreachable this milestone and the flag stays; recording the trigger makes its persistence read as a decision rather than an oversight."
    - "D-06: this workflow's toolchain/configure/build steps are REPLACED by a single `uses: ./.github/actions/build-py32f071` call. The cmake invocation now exists in exactly one place."
    - "D-14: every remaining hyphenated `firestarter-py32f071` literal in this file is renamed to the underscore form — the size step, the non-empty image check, the artifact names and the artifact path list."
    - "D-16: the single-file `actions/upload-artifact` of the install image is KEPT, so a PR build stays downloadable — the only way to obtain an image for a future board bring-up before a release exists. The ELF/map/logs/checksum uploads that `ad47c3b` removes stay removed."
    - "Phase 124 MERGE-03's in-file comment named Phase 128 as the resolver of the double-ARM-build question. It is UPDATED to state the D-05 resolution, not deleted and not left pointing at an open question."
    - "REL-03 (loud-half slice only) and REL-04 (workflow-rename slice only) are advanced here, not closed."
  artifacts:
    - firestarter/.github/workflows/py32f071.yml
  key_links:
    - "`py32f071.yml` -> `./.github/actions/build-py32f071` (the LOUD call site, no `continue-on-error`)"
    - "MERGE-03's `push: branches: [beta]` trigger -> preserved verbatim; a cherry-pick of `ad47c3b` would have reverted it"
    - "the composite action's `tool-versions.txt` / `configure.log` / `build.log` -> this workflow's `if: failure()` diagnostics upload"
---

<objective>
Convert `py32f071.yml` from a workflow that inlines the ARM build into one that calls the
composite action, rename every remaining hyphenated literal in it, collapse its artifact
upload to the single install image, and update MERGE-03's comment to record the resolution
it was written to await.

Purpose: D-06 removes the duplicated cmake invocation; D-05 keeps this workflow as the LOUD
half of the split. The soft `continue-on-error` that Plan 128-05 adds to the release job is
only defensible because a separate workflow exists whose entire purpose is to *not* hide the
same failure. That is the property this plan preserves, and it is the property Phase 124
D-10 explicitly deferred to Phase 128 to resolve.

**Scope boundary — this plan does NOT close REL-03 or REL-04.** It advances only the
loud-gate half of REL-03 (the pairing that makes containment defensible) and the
workflow-literal-rename half of REL-04. Neither may be ticked in REQUIREMENTS.md from this
plan; REL-03 additionally needs 128-01, 128-07 and run B, and REL-04 additionally needs
128-06 and 128-09.

Output: one rewritten workflow, no behaviour change to its trigger set.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-02-SUMMARY.md
@.planning/phases/128-release-asset-fold/128-03-SUMMARY.md

**Repo:** `/workspaces/firestarter`, branch `v1.23-py32f071-integration`. Commit inside the
sub-repo.

**No ARM toolchain exists locally.** Nothing in this plan can be built or measured here.
Every claim about what this workflow produces is discharged by a CI run URL + commit SHA.
Nothing may claim the resulting image runs, boots or installs; no PY32F071 PCB exists.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New step id in `py32f071.yml` (this plan):** `arm` (the composite call site)

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (rehearsal mode only)

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml` (this plan):** `firestarter_py32f071`
and `firestarter_py32f071_build_diagnostics`. Historical Actions artifacts from pre-rename
runs keep the old names in GitHub's storage; nothing reads them and they expire on the
repo's retention policy — do not attempt to rename them.

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto">
  <name>Task 1: Replace the inlined build steps with the composite call and update MERGE-03's comment</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/py32f071.yml` — the whole file (124 lines, no trailing newline). Lines 4-8 are MERGE-03's comment naming Phase 128 as the resolver. Lines 24-26 are the checkout. Lines 28-60 are the four steps that move into the composite action.
    - `/workspaces/firestarter/.github/actions/build-py32f071/action.yml` — the composite action landed by Plan 128-02, including its `outputs:` block. This file's call site must match what it declares.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` Pattern 2 (both first-use traps) and the D-05 decision text.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-PATTERNS.md` section 7 — the exact verbatim step text that moved, and the MERGE-03 comment block quoted in full.
  </read_first>
  <files>firestarter/.github/workflows/py32f071.yml</files>
  <action>
Delete the four inlined build steps (`Install GNU Arm toolchain and build tools`,
`Record tool versions`, `Configure`, `Build`, currently lines 28-60) and replace them with a
single step:

    - name: Build PY32F071 firmware
      id: arm
      uses: ./.github/actions/build-py32f071

Add NO `continue-on-error` here. That key belongs on the `beta-build.yml` call site only
(D-05, Pattern 2 TRAP 2). Add a comment above the call recording exactly that: this is the
LOUD half of the split, it must go red and stay red when the ARM build breaks on `beta`, and
the containment in `beta-build.yml` is only defensible because this workflow exists to not
hide the same failure. In the same comment, record D-05's removal trigger for the *other*
side's flag: `continue-on-error` comes off `beta-build.yml`'s call site when the target is
validated on real silicon; no PCB exists, so it is unreachable this milestone and the flag
stays. Recording the trigger is what makes its persistence read as a decision.

Keep the checkout step exactly as it is. A composite action runs in the calling job on the
same workspace, so it needs no checkout of its own.

Update the trigger comment at lines 4-8. Do NOT delete it and do NOT leave it pointing at an
open question. Rewrite it so it states: MERGE-03's `push: branches: [beta]` was implemented
literally, with no paths filter, so any change anywhere that breaks the ARM configure is
caught on `beta`; the double-ARM-build question it recorded for Phase 128 is RESOLVED by
D-05 — both builds stay, with distinct roles, this one LOUD and `beta-build.yml`'s SOFT.
The `push`, `pull_request` and `workflow_dispatch` trigger blocks themselves change in no
way; MERGE-03's trigger is preserved verbatim, which is precisely why `ad47c3b` was
re-applied by hand rather than cherry-picked.

<!-- planner-discipline-allow: continue-on-error -->
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
t=pathlib.Path('.github/workflows/py32f071.yml').read_text()
d=yaml.safe_load(t)
steps=d['jobs']['build']['steps']
arm=[s for s in steps if s.get('id')=='arm']
assert len(arm)==1, arm
assert arm[0].get('uses')=='./.github/actions/build-py32f071', arm[0]
assert 'continue-on-error' not in arm[0]
assert all('continue-on-error' not in s for s in steps)
trig=d[True] if True in d else d['on']
assert trig['push']['branches']==['beta'], trig
assert 'workflow_dispatch' in trig and 'pull_request' in trig
assert not any('apt-get' in (s.get('run') or '') for s in steps)
assert not any('cmake -S' in (s.get('run') or '') or 'cmake \\\\' in (s.get('run') or '') for s in steps)
print('OK', len(steps), 'steps')
"</automated>
  </verify>
  <acceptance_criteria>
    - `jobs.build.steps` contains exactly one step with `id: arm`, whose `uses` is the literal `./.github/actions/build-py32f071`, and that step has no `continue-on-error` key.
    - No step anywhere in the file carries a `continue-on-error` key, and a comment-stripped grep of the file for `continue-on-error` matches nothing.
    - No step's `run` body contains `apt-get` or a `cmake -S` / `cmake --build` invocation — those moved into the composite action.
    - The `on:` block still declares `push.branches == ['beta']` with no `paths` filter, a `pull_request` block with the original five path entries, and `workflow_dispatch`.
    - The header comment contains the substrings `MERGE-03`, `D-05`, `RESOLVED`, `LOUD` and `real silicon`, and no longer contains the phrase `is recorded for Phase 128 to resolve`.
    - `python3 -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/py32f071.yml').read_text())"` exits 0.
  </acceptance_criteria>
  <done>The workflow calls the composite action instead of inlining the build, carries no containment of its own, keeps MERGE-03's trigger byte-for-byte, and its header comment records the D-05 resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Rename every remaining hyphenated literal and collapse the artifact upload to the install image</name>
  <read_first>
    - The `py32f071.yml` produced by Task 1.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-PATTERNS.md` section 7, the paragraph enumerating every hyphenated line: `:66` diagnostics artifact name, `:79` the size step's `.elf`, `:86-95` the non-empty checks and `sha256sum` block (7 occurrences), `:100-103` the manifest block, `:113` artifact name, `:116-120` the artifact path list.
    - `git -C /workspaces/firestarter show ad47c3b -- .github/workflows/py32f071.yml` — specifically what it deletes (`Record tool versions` is now in the composite, so ignore that part) and what its `Report firmware size`, `Verify the install image exists and is non-empty` and single-file upload steps look like.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-03-SUMMARY.md` — the hyphenated-occurrence inventory Plan 128-03 Task 2 recorded. This task closes exactly that list.
  </read_first>
  <files>firestarter/.github/workflows/py32f071.yml</files>
  <action>
Rewrite the post-build half of the workflow. Keep it in this order:

1. **`Upload failed build diagnostics`** — keep the step, keep `if: failure()`, keep
   `if-no-files-found: warn` and the five-entry `path:` list (`configure.log`, `build.log`,
   `tool-versions.txt`, `build/py32f071/CMakeCache.txt`, `build/py32f071/build.ninja`). Those
   three logs are produced by the composite action's `tee` calls in the same workspace, so
   they resolve unchanged. Rename the artifact `name:` to
   `firestarter_py32f071_build_diagnostics`.
2. **`Report firmware size`** — replace the `tee size.txt` form with the direct
   `arm-none-eabi-size build/py32f071/firestarter_py32f071.elf`, per `ad47c3b`: size goes to
   the job log, which is worth seeing on every run, and is worth nothing as an artifact.
   Add `ad47c3b`'s one-line comment saying exactly that.
3. **`Verify the install image exists and is non-empty`** — replace the whole
   `Create and verify firmware checksums` step with `working-directory: build/py32f071`,
   `set -euxo pipefail`, and a single `test -s firestarter_py32f071.hex`. Per `ad47c3b`, the
   non-empty check stays as a build gate; the `sha256sum` generation and verification, and
   the `.elf`/`.bin` non-empty checks, go.
4. **Delete `Create artifact checksum manifest`** entirely (the `SHA256SUMS` step) — D-16
   keeps only the install image; the ELF/map/logs/checksum uploads `ad47c3b` removes stay
   removed.
5. **`Upload firmware install image`** — collapse the multi-file `Upload firmware artifacts`
   step to a single-file `actions/upload-artifact@v4` of
   `build/py32f071/firestarter_py32f071.hex`, artifact `name: firestarter_py32f071`,
   `if-no-files-found: error`. Keep D-16's rationale as a comment: a PR build stays
   downloadable, which is the only way to obtain an image for a future board bring-up before
   a release exists, and a single-file artifact unzips to exactly the image and nothing else.

After the edits, no hyphenated `firestarter-py32f071` literal may remain anywhere in the
file, including inside comments — unlike the CMakeLists case there is no reason for this
file to name the old form at all. Verify with a plain grep as well as a comment-stripped one.

Do not add a release step to this workflow. Release assets come from `beta-build.yml` only
(REL-01's ordering constraint), and `build.yml` is out of scope (D-13).

<!-- planner-discipline-allow: firestarter-py32f071 -->
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -c 'firestarter-py32f071' .github/workflows/py32f071.yml)" -eq 0 && python3 -c "
import yaml,pathlib
d=yaml.safe_load(pathlib.Path('.github/workflows/py32f071.yml').read_text())
steps=d['jobs']['build']['steps']
names=[s.get('name') for s in steps]
ups=[s for s in steps if str(s.get('uses','')).startswith('actions/upload-artifact')]
assert len(ups)==2, names
diag=[u for u in ups if u['with']['name']=='firestarter_py32f071_build_diagnostics']
img=[u for u in ups if u['with']['name']=='firestarter_py32f071']
assert len(diag)==1 and len(img)==1, [u['with']['name'] for u in ups]
assert diag[0].get('if')=='failure()'
p=img[0]['with']['path'].strip()
assert p=='build/py32f071/firestarter_py32f071.hex', repr(p)
assert img[0]['with']['if-no-files-found']=='error'
assert not any('SHA256SUMS' in (s.get('run') or '') for s in steps)
assert not any('sha256sum' in (s.get('run') or '') for s in steps)
assert not any('release' in (s.get('name') or '').lower() for s in steps)
print('OK', names)
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'firestarter-py32f071' .github/workflows/py32f071.yml` prints `0` (plain grep, comments included).
    - Exactly two `actions/upload-artifact@v4` steps remain: one named `firestarter_py32f071_build_diagnostics` guarded by `if: failure()`, and one named `firestarter_py32f071` whose `path` is the single scalar `build/py32f071/firestarter_py32f071.hex` with `if-no-files-found: error`.
    - No step's `run` body contains `sha256sum` or `SHA256SUMS`; the `Create artifact checksum manifest` step is gone.
    - Exactly one step runs `test -s firestarter_py32f071.hex` under `working-directory: build/py32f071`, and no step tests the `.elf` or `.bin` for non-emptiness.
    - The size step runs `arm-none-eabi-size build/py32f071/firestarter_py32f071.elf` and does not tee to a file.
    - No step name contains the word `release` in any case, and the file contains no `softprops/action-gh-release` reference.
    - The file parses as YAML and `jobs.build.steps` length is 6.
  </acceptance_criteria>
  <done>Every literal in the workflow uses the underscored basename, the diagnostics upload survives, the install image is the only published artifact, and the checksum/manifest machinery is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the loud/soft invariant and the rename inventory are closed for this file</name>
  <read_first>
    - The `py32f071.yml` produced by Tasks 1 and 2.
    - `/workspaces/firestarter/.github/actions/build-py32f071/action.yml` — confirm the call site's expectations match the action's declared `outputs` and that the action itself still carries no `continue-on-error`.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-03-SUMMARY.md` — the recorded hyphenated-occurrence inventory.
  </read_first>
  <files>firestarter/.github/workflows/py32f071.yml</files>
  <action>
Verification and record-keeping only; no further edits unless a check fails.

Assert the D-05 invariant mechanically across both files at once, by **parsing the YAML**
rather than by grepping raw text: neither `.github/workflows/py32f071.yml` nor
`.github/actions/build-py32f071/action.yml` declares a containment key on any step, at any
nesting level. A parse-based check is the right instrument here precisely because Task 1
asks for a comment that *names* that key in order to explain why it is absent — a raw grep
would make the explanation self-invalidating, which is the same trap Plan 128-01's
comment-stripped invariant test avoids. `runs.steps[*].continue-on-error` IS supported
inside composite actions, which is exactly why this is easy to get wrong and why the check
is worth writing down.

<!-- planner-discipline-allow: continue-on-error -->

Close Plan 128-03's inventory: run the comment-stripped repo-wide grep for the hyphenated
basename across `/workspaces/firestarter` again and confirm the surviving count is now zero
outside `.planning/`-style prose (there is none in this repo). Record the before/after counts
in the SUMMARY, citing 128-03's recorded baseline.

Record in the SUMMARY, explicitly and in these terms: this plan changed only CI
configuration. No ARM build ran locally — no ARM toolchain exists in this devcontainer — so
this workflow's behaviour after the rewrite is unproven until a CI run exists, and the first
such observation will be cited by run URL + commit SHA in `128-NONREGRESSION.md`. Nothing
here claims the image runs, boots or installs.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
KEY='continue-'+'on-error'
def steps_of(p):
    d=yaml.safe_load(pathlib.Path(p).read_text())
    return d['jobs']['build']['steps'] if 'jobs' in d else d['runs']['steps']
for p in ['.github/workflows/py32f071.yml','.github/actions/build-py32f071/action.yml']:
    ss=steps_of(p)
    bad=[s.get('name') or s.get('id') for s in ss if KEY in s]
    assert not bad, (p, bad)
print('LOUD/SOFT INVARIANT OK')
" && test "$(grep -rl 'firestarter-py32f071' --include='*.yml' --include='*.txt' --include='*.md' --include='*.cmake' . 2>/dev/null | wc -l)" -eq 0 && python3 -m pytest tests/ -q && git status --porcelain | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The YAML-parse check prints `LOUD/SOFT INVARIANT OK`: no step in either file declares a containment key. The check parses rather than greps, so the explanatory comment Task 1 adds does not invalidate it.
    - `grep -rl 'firestarter-py32f071' --include='*.yml' --include='*.txt' --include='*.md' --include='*.cmake' /workspaces/firestarter` returns nothing.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing.
    - The SUMMARY records the before/after hyphenated-occurrence counts, citing 128-03's baseline, and carries the explicit statement that no ARM build was performed locally and the permitted claim is that the asset publishes — not that the image runs.
  </acceptance_criteria>
  <done>Neither the LOUD workflow nor the composite action can contain a failure, the hyphenated basename is extinct in the firmware tree, and the CI-only claim ceiling is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pull request from a fork -> `py32f071.yml` | A `pull_request` trigger runs untrusted branch content; this workflow holds no write permission and must keep `pull_request`, never `pull_request_target` |
| composite action -> calling job | The action runs with the calling job's permissions and workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-11 | Tampering | `continue-on-error` reaching the LOUD gate, directly or by inheritance from the composite | high | mitigate | Task 3's cross-file assertion that neither the workflow nor the action declares the key; the comment above the call site states the prohibition and D-05's removal trigger |
| T-128-07 | Elevation | a privileged workflow triggered from an untrusted ref | medium | accept | Unchanged posture: `pull_request` (never `pull_request_target`), path-filtered, and this job declares no `permissions:` block, so it inherits the read-only default. This plan adds no permission. |
| T-128-14 | Information disclosure | build logs and CMake cache uploaded on failure | low | accept | Pre-existing `if: failure()` diagnostics upload, retained unchanged apart from its artifact name. The repo is public and the logs contain no secret. |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). The apt line moved into the composite action and is covered by T-128-06 there. |
</threat_model>

<verification>
- `py32f071.yml` parses as YAML; `jobs.build.steps` is 6 steps; one is the composite call with `id: arm`.
- Neither `py32f071.yml` nor `action.yml` contains `continue-on-error`.
- Zero hyphenated `firestarter-py32f071` occurrences remain anywhere in `/workspaces/firestarter`.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green; `pio test -e native` and `pio test -e native_nodevtools` unchanged by this plan.
- `git -C /workspaces/firestarter status --porcelain` empty.
- CI-only: this workflow's post-rewrite behaviour is unproven until a run exists. The first observation is cited by run URL + commit SHA in `128-NONREGRESSION.md` (Plan 128-10). No task here pushes or dispatches anything.
</verification>

<success_criteria>
- The ARM toolchain/configure/build invocation exists in exactly one place in the repo, and `py32f071.yml` reaches it through `uses: ./.github/actions/build-py32f071`.
- `py32f071.yml` remains the LOUD gate: no containment key anywhere, MERGE-03's trigger preserved verbatim, and its header comment records the D-05 resolution plus the removal trigger for the soft flag.
- Every literal in the file uses the underscored basename; the hyphenated form is extinct repo-wide.
- Exactly one publishable artifact remains — the single install image (D-16) — with the diagnostics upload retained on failure.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-04-SUMMARY.md` when done.
</output>
