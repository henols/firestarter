---
phase: 128-release-asset-fold
plan: 10
type: execute
wave: 7
depends_on: ["128-09"]
files_modified:
  - .planning/phases/128-release-asset-fold/128-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [REL-01, REL-02, REL-03, REL-04]

must_haves:
  truths:
    - "D-04: the push and BOTH dispatches are outward-facing operator actions. **No task in this plan runs `git push` or `gh workflow run`** — not as a command, not as a suggestion inside an action, not in a verify block. The structural separation IS the gate; `--auto`/`--chain` auto-approve human-verify checkpoints regardless of `autonomous: false`, so a checkpoint alone would not protect this."
    - "D-01: the rehearsal is dispatched from a THROWAWAY branch forked off the firmware milestone branch, with `rehearsal=true`, producing a draft release. F-3 (RESOLVED): a draft release creates no git tag. Verified this session: pushing a branch that is neither `beta` nor `main` fires nothing in the firmware repo."
    - "F-2 (BLOCKING procedure requirement): every rehearsal dispatch MUST supply `beta_version` explicitly — `3.0.0b99` is the recommended value. `is_beta_mode()` needs `--beta`, `GITHUB_REF == refs/heads/beta`, or a non-empty `BETA_VERSION`; a throwaway-branch dispatch with the input blank satisfies none and takes the STABLE path, rewriting `include/version.h` from `3.0.0b14` to `3.0.1`."
    - "D-02: two dispatches off the same branch. Run A healthy. Run B after a commit that breaks CMake configure via a RENAMED SOURCE PATH — reproducing C-1, the failure this exact target actually had in Phase 124, rather than a synthetic one. The cascade is the proof: configure fails, contained; build fails, contained; the glob matches nothing; the three AVR assets still publish."
    - "D-03: the draft carries `rehearsal-<run_id>`, so an accidentally published draft cannot masquerade as `3.0.0b15`."
    - "D-18: `128-NONREGRESSION.md` records both run URLs + commit SHAs, run A's asset list, run B's asset list (three AVR, no py32), the resolved SDK SHA, and the EXPLICIT non-claim that nothing here says the published image runs. Phase 130's CLOSE-02 ledger cites it."
    - "F-7: the hazard D-01 guards against has already fired once — run 30199560282 dispatched `beta-build.yml` from the non-default branch `v1.21-community-chip-validation-command` and published the real public prerelease `3.0.0b11` with a real tag. This belongs in the artifact as the justification for the input's permanence."
    - "F-8: the cross-repo binding is enforced by a local run and developer discipline, NOT by app CI. Claiming CI enforcement would be false."
    - "Claim ceiling: no PY32F071 PCB exists. The permitted claim is exactly one sentence wide — *the asset publishes*. Every ARM figure cites a CI run URL plus a commit SHA; no local measurement exists or is acceptable."
  artifacts:
    - .planning/phases/128-release-asset-fold/128-NONREGRESSION.md
  key_links:
    - "run A's `gh release view --json assets` output -> REL-02's Criterion 2 evidence"
    - "run B's asset list (three AVR, no py32) + the `::warning::` annotation -> REL-03's Criterion 3 and D-07's observed firing"
    - "run A's step summary (resolved SDK SHA, resolved rehearsal boolean, the version string found in the image) -> REL-04 and REL-01"
    - "`128-NONREGRESSION.md` -> Phase 130 CLOSE-02's honesty ledger"
---

<objective>
Produce Phase 128's evidence artifact and close the four requirements. Everything provable
locally is executed and recorded in this session; everything CI-only is discharged by two
operator-gated rehearsal dispatches and cited by run URL plus commit SHA.

Purpose: Criteria 2 and 3 both require `beta-build.yml` to actually run, and it is the
workflow that cuts real public beta prereleases. D-01's rehearsal shape exists so that
requirement can be met with no public footprint — one Actions run, one deletable draft, one
version-bump auto-commit on a deletable branch, and no git tag.

**Scope note — this is the ONLY plan permitted to tick REL-01, REL-02, REL-03 and REL-04 in
`.planning/REQUIREMENTS.md`,** and only after every prior plan's slice is verified. Plans
128-01 through 128-09 each closed a portion and each states so; none of them may tick.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-VALIDATION.md
@.planning/phases/128-release-asset-fold/128-08-SUMMARY.md
@.planning/phases/128-release-asset-fold/128-09-SUMMARY.md

**Repo:** `128-NONREGRESSION.md` and `REQUIREMENTS.md` are **meta-repo** files. Commit in
`/workspaces` on `gsd/v1.23-py32f071-integration`. The two sub-repos are read-only for this
plan and must already be committed and clean.

**D-04, restated as a hard constraint on this plan's tasks:** no task may execute `git push`
or `gh workflow run`. Read-only `gh` calls that inspect an already-created run
(`gh run view`, `gh release view`, `gh api` GETs) are permitted in Task 3 **after** the
operator reports the runs exist.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo (this plan):**
`.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `files:` entry:** `build/py32f071/firestarter_*.hex`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (rehearsal mode only)

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`,
`firestarter_py32f071_build_diagnostics`

**New README section:** `## Release integration` in `platform/py32f071/README.md`

**Transient, operator-created, deletable:** one throwaway firmware branch, two Actions runs,
one draft release named `rehearsal-<run_id>`, and one version-bump auto-commit on that
branch. None of these is a Phase 128 deliverable; all are deleted after the evidence is
recorded.

<tasks>

<task type="auto">
  <name>Task 1: Write 128-NONREGRESSION.md with every locally-provable row executed in-session</name>
  <read_first>
    - `/workspaces/.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md` lines 1-30 — the house shape: title, branch/HEAD block for all three repos, the hoisted no-hardware non-claim block, the re-execution pledge, then `## 1. The claim, as precise statements` and numbered sections of `| # | Mechanism | Command | Expected | Observed |` tables.
    - `/workspaces/.planning/ROADMAP.md` lines 2357-2371 — Phase 128's four success criteria verbatim. Each gets its own section.
    - All nine prior SUMMARYs for this phase — but **do not copy their recorded results**. The house pledge is that every row in this artifact was executed in THIS session.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — F-2, F-3, F-7, F-8, and the Environment Availability table (the PY32F071-board row, whose fallback is "None. No fallback exists").
  </read_first>
  <files>.planning/phases/128-release-asset-fold/128-NONREGRESSION.md</files>
  <action>
Create `128-NONREGRESSION.md` in the house shape, with every locally-provable row executed
now, in this session, and every CI-only row written as an explicit `PENDING — operator
dispatch` placeholder that Task 3 replaces.

**Header block.** Title `# Phase 128 Non-Regression Sweep — closing plan (128-10)`; the
written date; the firmware branch and HEAD SHA; the host branch and HEAD SHA; the meta
branch. Then, hoisted above section 1 and unmissable:

> **No PY32F071 hardware exists.** Nothing in this milestone has ever run on this silicon,
> and nothing in it can. Everything below is about **publication**: that a file with a
> particular name, carrying a particular version string, becomes a downloadable release
> asset. Nothing here says the published image runs, boots, or installs. The permitted claim
> is exactly one sentence wide.

Then the re-execution pledge in the 127 wording: every row below was executed in this
session; nothing is copied from any prior plan's SUMMARY.

**§1 — The claim, as precise statements.** Four numbered statements, one per ROADMAP success
criterion, each written as what is actually being asserted rather than as the criterion's
prose. Include the ceiling as a fifth, negative statement.

**§2 — Locally provable, executed now.** A table with the five columns. Rows, each with the
exact command and its observed output:
- The REL-01 step-order reading: parse `beta-build.yml` and print the indices of the
  `version` step, the `git-auto-commit-action` step, the `arm` step and the `Release` step,
  showing they are strictly increasing. This is Criterion 1's stated method, discharged
  mechanically.
- The `files:` block's two entries, parsed from YAML.
- The absence of any `fail_on_unmatched_files` YAML key over comment-stripped source, with
  the non-vacuity guard's result.
- `python3 scripts/check_release_assets.py --build-root <clean fixture>` → exit 0, and the
  same against both planted fixtures → exit 1, with the stdout excerpt naming the env.
- `python3 -m pytest tests/ -q` in the firmware repo — counts.
- `pio test -e native` and `pio test -e native_nodevtools` — case and suite counts, compared
  against the recorded Phase 123 baseline.
- `python3 -m pytest tests/test_checker_convention.py -q` — 7 passed with the raised floors,
  plus the recounted `planted_*` total.
- The `GIT_TAG` parse expression run against the real `platform/py32f071/CMakeLists.txt` →
  the 40-hex SHA.
- `python3 -m pytest tests/test_py32_asset_name_host.py -q -rs` in the host repo — the
  observed pass count and the empty skip report.
- `python3 -m pytest tests/test_skip_census.py -q` and `python3 -m pytest tests/ -q` in the
  host repo — counts.
- `git status --porcelain` in both sub-repos — empty.
- The repo-wide grep for the hyphenated basename — zero.

**§3 — CI-only, PENDING operator dispatch.** A table with the same columns and every
`Observed` cell reading `PENDING — operator dispatch`. Rows: run A URL + commit SHA; run A's
asset list from the API; run A's resolved SDK SHA from the step summary; run A's resolved
rehearsal boolean; run A's REL-01 image-carries-the-version PASS line; run A's confirmation
that the draft created no git tag; run B URL + commit SHA; run B's asset list (expected:
exactly three AVR hexes, no py32); run B's `::warning::` annotation and step-summary line
proving D-07 fired.

**§4 — The operator dispatch procedure.** Write it as a numbered, copy-pasteable procedure
for the operator, with every value spelled out. It must include, at minimum:
1. Fork a throwaway branch off the firmware milestone branch and push it. Note, with the
   verification behind it, that pushing a branch that is neither `beta` nor `main` fires
   nothing in this repo — `beta-build.yml` is `push: [beta]` plus dispatch, `py32f071.yml` is
   `push: [beta]` plus PR plus dispatch, `build.yml` is `push: [main]` plus PR.
2. Run A: dispatch `beta-build.yml` against that ref with `rehearsal=true` **and
   `beta_version=3.0.0b99`**. State F-2's consequence in the procedure itself, not in a
   footnote: with `beta_version` blank the run takes the STABLE path and rewrites
   `include/version.h` to `3.0.1`, and the evidence would then record a stable string as
   proof of a beta fold. `3.0.0b99` is unmistakable, passes the version regex, and cannot
   collide with a real `b15`. Give the grep anchor for the wrong path: the version step logs
   a non-`bNN` string.
3. **Flag A4 to the operator before the first dispatch:** GitHub resolves the workflow
   definition, including the new `rehearsal` input, from the dispatched ref. Precedent run
   30199560282 proves dispatch from a non-default branch works, but that run's workflow had
   no *new* input. If the dispatch errors with an unexpected-input message, that is a loud,
   immediately diagnosable failure, not a silent one — stop and report rather than working
   around it.
4. Collect run A's evidence: the run URL, the head commit SHA, and
   `gh release view <rehearsal tag> --json assets`. Record asset **names and sizes** from the
   API, not download URLs — a draft's asset URLs are `untagged-<hash>` placeholders and are
   not stable citations (F-3). Read the resolved SDK source path off the run log and confirm
   it matches the measured `build/py32f071/_deps/py32f071_sdk-src`.
5. Run B: on the same throwaway branch, commit a break that makes **CMake configure** fail —
   a renamed source path in `platform/py32f071/CMakeLists.txt`'s source list, reproducing
   C-1, the failure this exact target actually had in Phase 124. Dispatch again with the same
   two inputs. Collect the run URL, the head SHA, the asset list, and a screenshot-free
   textual confirmation that the `::warning::` annotation and the step-summary line appeared.
6. Cleanup: delete the draft release and delete the throwaway branch. Confirm no tag named
   `rehearsal-*` exists and that `3.0.0b99` exists neither as a tag nor as a release.
7. State the two failure signatures to watch for, so a wrong result is recognised rather than
   absorbed: a run B that is green, publishes three AVR assets, and has NO `::warning::` and
   no summary line is a passing REL-03 and a **failing** D-07 simultaneously; and a `draft`
   that resolves true on anything other than a rehearsal dispatch would silently stop
   publishing real betas.

**§5 — What this phase does NOT claim.** The non-claim block, in full, for Phase 130's
CLOSE-02 ledger:
- The published image has never been executed. No PY32F071 PCB exists. The asset publishes;
  that is the whole claim.
- The cross-repo filename binding is enforced by a local run and by developer discipline, not
  by app CI — neither app CI workflow checks out the firmware sibling, so `@requires_fw`
  skips there and only the unmarked RED demonstrations run (F-8).
- The `continue-on-error` on the ARM call site is deliberate and permanent for now, with a
  recorded removal trigger that is unreachable this milestone (D-05).
- No ARM number anywhere in this phase was measured locally; there is no ARM toolchain in the
  devcontainer, and every ARM figure cites a CI run URL plus a commit SHA.

**§6 — Precedent and prior art.** Record F-7 in full: on 2026-07-26, run 30199560282
dispatched this workflow from the non-default branch `v1.21-community-chip-validation-command`
and published the real, public prerelease `3.0.0b11` with a real tag
(`refs/tags/3.0.0b11` → `0fd7992187467f6d245bc106786253f497ea0ecc`). D-01's draft mode is not
theoretical caution; the exact accident it prevents has already happened once from precisely
this route. That is the justification for the `rehearsal` input's permanence (D-03), and it
belongs in Phase 130's honesty ledger alongside the two stray prereleases already recorded.

Commit the artifact in `/workspaces` on `gsd/v1.23-py32f071-integration`. Do not tick any
requirement yet.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/128-release-asset-fold/128-NONREGRESSION.md && python3 -c "
import pathlib
t=pathlib.Path('.planning/phases/128-release-asset-fold/128-NONREGRESSION.md').read_text()
for s in ['No PY32F071 hardware exists','3.0.0b99','30199560282','3.0.0b11','_deps/py32f071_sdk-src','rehearsal','the asset publishes','PENDING']:
    assert s in t, s
for h in ['## 1.','## 2.','## 3.','## 4.','## 5.','## 6.']:
    assert h in t, h
print('OK')
" && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `128-NONREGRESSION.md` exists with six numbered sections and the hoisted no-hardware block above section 1.
    - Section 2's every `Observed` cell contains a real value produced by a command run in this session; no cell reads `PENDING` in section 2.
    - Section 3's every `Observed` cell reads `PENDING — operator dispatch`.
    - Section 4 contains the literal `beta_version=3.0.0b99`, the literal `rehearsal=true`, and a sentence stating the stable-path consequence of leaving `beta_version` blank.
    - Section 4 contains the A4 warning about the new input being resolved from the dispatched ref, and the two failure signatures from step 7.
    - Section 5 contains all four non-claims, including the F-8 CI ceiling stated as a negative.
    - Section 6 contains `30199560282`, `3.0.0b11` and `0fd7992187467f6d245bc106786253f497ea0ecc`.
    - The file contains no occurrence of `git push` or `gh workflow run` **outside section 4's operator procedure** — those are instructions to a human, never commands this plan executes.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` are consistent with plans 128-08 and 128-09 having committed (firmware empty).
    - `.planning/REQUIREMENTS.md` is unmodified by this task.
  </acceptance_criteria>
  <done>The evidence artifact exists with every locally-provable row executed in-session, every CI-only row explicitly pending, and a copy-pasteable operator procedure that cannot omit `beta_version`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (CHECKPOINT — operator only): push the throwaway branch and run the two rehearsal dispatches</name>
  <read_first>
    - `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md` § 4 — the numbered operator procedure written by Task 1. It is the authoritative version of the summary below; if the two ever disagree, § 4 wins.
    - `.planning/phases/128-release-asset-fold/128-RESEARCH.md` § F-2 (why `beta_version` must be supplied), § F-3 (why a draft creates no tag), § F-7 (the precedent run), and Assumption A4 (the new input resolved from the dispatched ref).
  </read_first>
  <files>(none — no file is written by this task)</files>
  <action>
**This task is not executed by an agent. It exists so that the push and both dispatches are
performed by the operator and by no one else (D-04).**

The agent's only actions here are: present the `<what-built>` and `<how-to-verify>` blocks
below, stop, and wait. It must not run `git push`, must not run `gh workflow run`, must not
create a branch on a remote, and must not "helpfully" prepare the dispatch by any other
route. `autonomous: false` alone does not protect this — `--auto` and `--chain`
auto-approve human-verify checkpoints regardless — so the protection is that no executable
command exists in this plan at all. If this checkpoint is auto-approved without the operator
having supplied run URLs, Task 3 must record section 3 as `UNDISCHARGED`; it must never
invent, infer or extrapolate a run URL, a commit SHA or an asset list.
  </action>
  <verify>
    <human-check>The operator returns two run URLs, two commit SHAs, two asset lists and the resolved SDK SHA — or explicitly defers.</human-check>
  </verify>
  <acceptance_criteria>
    - No `git push` and no `gh workflow run` was executed by the agent during this task.
    - Either the operator supplied both run URLs (matching `https://github.com/henols/firestarter/actions/runs/<digits>`) with their head commit SHAs and asset lists, or the deferral is explicit and recorded.
    - Nothing was written to any file by this task.
  </acceptance_criteria>
  <done>The operator has either supplied the two rehearsal runs' evidence or explicitly deferred it; no outward-facing command was issued by an agent.</done>
  <what-built>
Everything that can be built has been. The firmware repo carries the composite action, the
renamed CMake outputs, the rewritten `py32f071.yml`, the folded `beta-build.yml` with its
`rehearsal` input and four gates, the AVR-assets checker with its fixtures and raised
convention floors, and the corrected README section. The host repo carries the cross-repo
three-way filename binding. `128-NONREGRESSION.md` §2 is fully populated from this session's
local runs; §3 is entirely `PENDING`.

**What remains is outward-facing and is yours alone (D-04).** No task in this plan has run,
or will run, `git push` or `gh workflow run`. `beta-build.yml` is the workflow that cuts real
public beta prereleases, and it has already published one by accident from exactly this route
(run 30199560282 → the real public `3.0.0b11` with a real tag, 2026-07-26).
  </what-built>
  <how-to-verify>
Follow `128-NONREGRESSION.md` §4 exactly. In summary:

1. From `/workspaces/firestarter` on `v1.23-py32f071-integration`, create a throwaway branch
   and push it. A branch that is neither `beta` nor `main` fires no workflow in this repo.
2. **Run A (healthy).** Dispatch `beta-build.yml` against the throwaway ref with
   `rehearsal=true` **and `beta_version=3.0.0b99`**. The second input is not optional: with it
   blank the run takes `update_version.py`'s stable path and rewrites `include/version.h` to
   `3.0.1`. If the dispatch is rejected with an unexpected-input error, **stop and report** —
   that is assumption A4 failing loudly, and it needs a decision rather than a workaround.
3. Record run A's URL and head commit SHA. Then `gh release view <the rehearsal-… tag> --json assets`
   and record asset **names and sizes** (a draft's download URLs are `untagged-…` placeholders,
   not citable). Confirm from the run's step summary: the resolved rehearsal boolean is true,
   the SDK SHA line is present and equals the pinned `GIT_TAG`, and the REL-01 PASS line names
   `3.0.0b99:py32f071`. Confirm no tag was created.
4. **Run B (planted break).** On the same throwaway branch, commit a rename of a source path
   inside `platform/py32f071/CMakeLists.txt`'s source list so CMake **configure** fails —
   reproducing C-1. Dispatch again with the same two inputs.
5. Record run B's URL and head SHA. Confirm: the ARM steps failed and were contained; the run
   is green; the release carries **exactly three AVR hexes and no py32 asset**; and the run
   page shows the `::warning::` annotation plus the step-summary line
   *"PY32F071 image not produced — this release carries no py32f071 asset."*
   A green run B with three AVR assets and **no** warning is a passing REL-03 and a failing
   D-07 at the same time — report that rather than accepting it.
6. Delete the draft release and the throwaway branch. Confirm no `rehearsal-*` tag exists and
   that `3.0.0b99` exists neither as a tag nor as a release.

Then paste back: both run URLs, both head commit SHAs, both asset lists, the resolved SDK
SHA, the resolved rehearsal boolean, and whether the D-07 warning appeared on run B.
  </how-to-verify>
  <resume-signal>Paste the two run URLs, the two commit SHAs, both asset lists and the SDK SHA — or describe what failed. Type "skip" only if you are deliberately deferring the dispatch evidence, in which case Task 3 must record §3 as UNDISCHARGED and Criteria 2 and 3 must not be ticked.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the returned evidence, verify the four claims, and tick REL-01..REL-04</name>
  <read_first>
    - `128-NONREGRESSION.md` as written in Task 1, specifically §3's PENDING rows.
    - The operator's returned run URLs, commit SHAs and asset lists from the checkpoint.
    - `/workspaces/.planning/ROADMAP.md` lines 2362-2367 — the four success criteria, to confirm each is discharged by a specific recorded row rather than by an overall impression.
    - `/workspaces/.planning/REQUIREMENTS.md` lines 85-88 — REL-01…REL-04, read as prose, not as a paraphrase.
  </read_first>
  <files>
.planning/phases/128-release-asset-fold/128-NONREGRESSION.md
.planning/REQUIREMENTS.md
  </files>
  <action>
Replace every `PENDING — operator dispatch` cell in §3 with the operator-supplied value, and
re-verify each one read-only where possible. Read-only `gh` inspection of runs that already
exist is permitted and expected here — `gh run view <id>`, `gh api` GETs, and
`gh release view --json assets`. **Do not dispatch anything and do not push anything.**

For every ARM figure recorded, the cell must carry both a run URL and a commit SHA. A figure
without both is not admissible; there is no ARM toolchain in this devcontainer, so a local
measurement is never available and never acceptable.

Then add a `## 7. Criterion discharge` section mapping each of the four ROADMAP success
criteria to the specific §2 or §3 row that discharges it:
- Criterion 1 → the §2 step-index reading **and** the §3 run A REL-01 PASS line naming the
  version string found in the published image. Note that the mechanical form was added
  alongside the criterion's stated YAML read, not instead of it.
- Criterion 2 → the §3 run A asset list showing `firestarter_py32f071.hex` present, cited by
  run URL and SHA.
- Criterion 3 → the §3 run B asset list showing exactly three AVR hexes and no py32 asset,
  plus the §2 planted-fixture exit codes proving the assets gate demonstrably fails.
- Criterion 4 → the §3 run A resolved SDK SHA equal to the pinned `GIT_TAG`, plus the §3 run
  A filename-equality PASS line, plus the §2 host-side three-way binding pass count.

Then, and only then, tick REL-01, REL-02, REL-03 and REL-04 in `.planning/REQUIREMENTS.md`.
**This is the only plan permitted to tick them.** Before each tick, confirm the requirement's
full prose is satisfied — not just the slice one plan closed. If the operator answered "skip"
at the checkpoint, tick only what is genuinely discharged, mark §3 `UNDISCHARGED`, and leave
Criteria 2 and 3 and their requirements unticked with the reason recorded. Do not
tick-with-a-caveat; either a requirement is discharged or it is not.

Finally, re-read §5's non-claim block and confirm nothing added in this task contradicts it.
Commit both files in `/workspaces` on `gsd/v1.23-py32f071-integration`.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib,re
t=pathlib.Path('.planning/phases/128-release-asset-fold/128-NONREGRESSION.md').read_text()
assert '## 7.' in t, 'criterion discharge section missing'
sec3=t.split('## 3.',1)[1].split('## 4.',1)[0]
if 'UNDISCHARGED' not in sec3:
    assert 'PENDING' not in sec3, 'section 3 still has PENDING cells'
    urls=re.findall(r'https://github\.com/henols/firestarter/actions/runs/\d+', t)
    shas=re.findall(r'\b[0-9a-f]{40}\b', t)
    assert len(set(urls))>=2, urls
    assert len(set(shas))>=2, 'need run head SHAs plus the SDK SHA'
for s in ['No PY32F071 hardware exists','the asset publishes']:
    assert s in t, s
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - §3 contains no `PENDING` cell, or the whole section is explicitly marked `UNDISCHARGED` with the operator's stated reason.
    - When discharged: at least two distinct `https://github.com/henols/firestarter/actions/runs/<id>` URLs appear, and every ARM figure's row carries both a run URL and a 40-hex commit SHA.
    - Run B's recorded asset list contains exactly `firestarter_uno.hex`, `firestarter_uno328pb.hex` and `firestarter_leonardo.hex`, and does not contain `firestarter_py32f071.hex`.
    - Run A's recorded asset list contains all four, including `firestarter_py32f071.hex`.
    - The recorded resolved SDK SHA equals `0ed2f4b4d3391eccfd4491006a30295fd78e32c2`, the value parsed from the shipped `CMakeLists.txt`.
    - A `## 7. Criterion discharge` section maps each of the four ROADMAP criteria to a named row.
    - `.planning/REQUIREMENTS.md`: REL-01…REL-04 are ticked only where genuinely discharged; `git diff` on that file touches only those four lines.
    - No command executed by this task is `git push` or `gh workflow run`; the plan's git history shows no push from this session's agent.
    - §5's non-claim block is unchanged and nothing added contradicts it — in particular, no row states or implies the published image runs, boots or installs.
  </acceptance_criteria>
  <done>Every CI-only row carries a run URL and a commit SHA, each ROADMAP criterion maps to a named piece of evidence, and REL-01..REL-04 are ticked exactly to the extent they are discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator action -> public GitHub state | A dispatch of this workflow can create a real public prerelease and a real tag; both are effectively permanent |
| throwaway branch -> milestone branch | A version-bump auto-commit lands on the dispatched ref; merging it would carry a wrong version string |
| evidence artifact -> Phase 130's honesty ledger | Anything overstated here becomes an overstated public claim later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-02 | Repudiation | a rehearsal dispatch publishing a real public prerelease and a real tag | high | mitigate | `rehearsal=true` sets `draft: true`, and a draft creates no tag (F-3); `tag_name` is overridden to `rehearsal-<run_id>` (D-03); and no task in this plan can dispatch anything (D-04's structural gate). **Already materialised once from this exact route** — run 30199560282 published `3.0.0b11`. |
| T-128-26 | Tampering | a rehearsal dispatch with `beta_version` blank writing a STABLE version into `include/version.h`, and that regression travelling if the branch is merged | high | mitigate | the procedure names `beta_version=3.0.0b99` in every dispatch instruction; the wrong path is observable in the version step's log and in the REL-01 PASS line; cleanup requires deleting the branch, never merging it (F-2, Pitfall 3) |
| T-128-27 | Repudiation | recording an ARM figure with no run URL or SHA, i.e. an unfalsifiable claim | high | mitigate | every §3 cell must carry both; an acceptance criterion asserts at least two distinct run URLs and per-row SHAs; no local ARM measurement exists or is acceptable |
| T-128-22 | Repudiation | the artifact claiming or implying the published image runs | high | mitigate | §5's non-claim block is hoisted above §1 in condensed form and stated in full in §5; Task 3 re-reads it and asserts nothing added contradicts it. Phase 130's CLOSE-02 cites this block. |
| T-128-08 | Repudiation | run B green with three AVR assets and no `::warning::` — a passing REL-03 and a failing D-07 at once, easily absorbed as success | medium | mitigate | the checkpoint names that exact signature and instructs the operator to report it rather than accept it |
| T-128-28 | Elevation | an agent performing the outward-facing action itself, bypassing the operator gate | high | mitigate | D-04's structural separation: the push and both dispatches exist only as text inside a `checkpoint:human-action` and inside §4's procedure. `autonomous: false` alone would not suffice, because `--auto`/`--chain` auto-approve checkpoints — the protection is that no executable task carries the command. |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). This plan writes two Markdown/planning files. |
</threat_model>

<verification>
- `128-NONREGRESSION.md` exists with sections 1-7, the hoisted no-hardware block, and the re-execution pledge.
- §2's rows were all executed in this session; §3's rows each carry a run URL plus a commit SHA, or the section is explicitly UNDISCHARGED.
- Run A's asset list contains `firestarter_py32f071.hex`; run B's contains exactly the three AVR hexes and no py32 asset.
- The recorded SDK SHA equals the pinned `GIT_TAG` parsed from the shipped `CMakeLists.txt`.
- Both sub-repo working trees clean; the throwaway branch and the draft release deleted; no `rehearsal-*` tag and no `3.0.0b99` tag or release exists.
- `.planning/REQUIREMENTS.md` REL-01…REL-04 ticked only where discharged.
- No `git push` and no `gh workflow run` was executed by any task in this plan.
</verification>

<success_criteria>
- The phase's evidence artifact exists in the house shape and is citable by Phase 130's CLOSE-02 ledger.
- Every ARM figure carries a CI run URL plus a commit SHA; no ARM claim rests on a local measurement.
- The four ROADMAP success criteria each map to a named row of evidence.
- The rehearsal left no public footprint: no tag, no published release, no surviving branch.
- The non-claim block is explicit and unqualified — the asset publishes; nothing here says the image runs.
- REL-01…REL-04 are ticked exactly to the extent they are discharged, by the only plan permitted to tick them.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-10-SUMMARY.md` when done.
</output>
