---
phase: 128-release-asset-fold
plan: 06
type: execute
wave: 3
depends_on: ["128-05"]
files_modified:
  - firestarter/.github/workflows/beta-build.yml
autonomous: true
requirements: [REL-01, REL-04]

must_haves:
  truths:
    - "D-08(a): the workflow performs a real string-equality check of the emitted basename against a literal, with a comment citing `asset_candidates()` and labelling itself explicitly as a TRANSCRIPTION — the firmware repo cannot import the host package, so the binding lives app-side (D-08(b), Plan 128-09). The two must never be blurred."
    - "D-10: the resolved SDK commit SHA is LOGGED to `$GITHUB_STEP_SUMMARY` and ASSERTED string-equal to the 40-hex `GIT_TAG` read out of `platform/py32f071/CMakeLists.txt`, with a non-vacuity guard on BOTH sides — two empty strings compare equal (A-7)."
    - "F-15: D-10's rationale is corrected in the shipped comment. Nothing caches `build/py32f071/_deps` (`actions/cache` covers only `~/.cache/pip` and `~/.platformio/.cache`), so the cache-substitution scenario cannot occur. The assertion catches a `GIT_TAG` edit that does not match what was fetched, an upstream force-push behind a moved ref, and a future contributor adding an SDK cache without re-checking the pin."
    - "F-9 / REL-01: the PUBLISHED image is asserted to contain the bumped version string. `FW_VERSION` is `VERSION \":\" RURP_BOARD_NAME`, and on ARM `PSTR()` is a no-op and `PROGMEM` is empty, so the concatenated literal is plain ASCII in `.rodata`. This is the only REL-01 evidence that survives a future step reorder, which a YAML-order read does not."
    - "F-2: the REL-01 assertion compares against `steps.version.outputs.version`, NEVER against `tag_name` — in rehearsal mode D-03 overrides `tag_name` by design, so a tag-equality assertion would be red on purpose."
    - "All three steps are guarded `if: steps.arm.outcome == 'success'` so a contained ARM failure skips them silently rather than failing the release (REL-03)."
  artifacts:
    - firestarter/.github/workflows/beta-build.yml
  key_links:
    - "`steps.arm.outputs.hex_path` -> the D-08(a) basename equality"
    - "`steps.arm.outputs.sdk_sha` -> the D-10 equality against `CMakeLists.txt`'s `GIT_TAG`"
    - "`steps.version.outputs.version` -> the F-9 `strings` assertion over the objcopy'd image"
---

<objective>
Add the three success-path assertions to `beta-build.yml`, immediately after D-07's report
step: the emitted-filename equality (D-08(a)), the SDK-pin equality (D-10), and the
image-carries-the-bumped-version check (F-9). All three are exit codes, not log lines.

Purpose: four of this phase's claims become mechanical. Criterion 1's stated method is
*"verified by reading the YAML step order"* — satisfiable, and it rots the moment someone
reorders the job. The `strings`-over-the-published-image form asserts the property on the
bytes that actually ship, costs six lines, and uses tools the composite action already
installed. Criterion 4 names *"a mechanical string-equality check in the workflow, not a
human read of the release page"* — that is D-08(a) and D-10.

**Scope boundary — this plan does NOT close REL-01 or REL-04.** REL-01 additionally needs
the ordering that Plan 128-05 landed and run A's observation (128-10). REL-04 additionally
needs the CMake rename (128-03) and the cross-repo three-way binding (128-09), which is the
half that actually binds this step's literal to the host function. Do not tick REL-01 or
REL-04 in REQUIREMENTS.md from this plan.

Output: one modified workflow — three new steps. The `Release` step is still not touched;
Plan 128-07 owns it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-05-SUMMARY.md

**Repo:** `/workspaces/firestarter`, branch `v1.23-py32f071-integration`. Commit inside the
sub-repo.

**No task may run `git push` or `gh workflow run`** (D-04). **No ARM toolchain exists
locally** — `arm-none-eabi-objcopy` and the ARM build are CI-only. Every assertion added
here is unproven until rehearsal run A observes it; that observation is Plan 128-10's, cited
by run URL + commit SHA. Nothing may claim the image runs, boots or installs.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm` (existing `version`, `release_target` unchanged)

**New step-level env var names (this plan):** `EXPECTED_VERSION`, `HEX_PATH`, `SDK_SHA`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (Plan 128-07)

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`,
`firestarter_py32f071_build_diagnostics`

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto">
  <name>Task 1: Assert the emitted asset filename equals the transcribed literal</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` as edited by Plan 128-05 — the `arm` step and the `Report a missing PY32F071 image` step it follows.
    - `/workspaces/firestarter/.github/actions/build-py32f071/action.yml` — the `hex_path` output and the glob-count guard that produced it.
    - `/workspaces/firestarter_app/firestarter/firmware.py` lines 116-132 — `asset_candidates()`. READ-ONLY. Frozen. This is the source of the literal being transcribed.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` Code Examples, the D-08(a) block — including the note on `${#MATCHES[@]}` and why an unmatched bash glob would otherwise produce a confusing mismatch message rather than a clear "no image" one.
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
Insert a step named `Assert the emitted asset filename (REL-04)` immediately after
`Report a missing PY32F071 image`, guarded `if: steps.arm.outcome == 'success'`.

Pass the composite action's output into shell through an `env:` mapping —
`HEX_PATH: ${{ steps.arm.outputs.hex_path }}` — never by interpolating the expression into
the `run:` body (Security V5). In the body: `set -euo pipefail`; set
`EXPECTED=firestarter_py32f071.hex`; assert `"$HEX_PATH"` is non-empty and fail with a
message naming the composite action if it is not — an empty output means the action's glob
guard did not run, and a comparison against an empty basename is the A-7 shape; take
`ACTUAL=$(basename "$HEX_PATH")`; assert `"$ACTUAL" = "$EXPECTED"` and on failure print a
message naming both strings and exit 1; on success print a `PASS:` line naming the basename
and append it to `$GITHUB_STEP_SUMMARY`.

The comment above the step is load-bearing and must say all of this. Label the step
**deliberately a TRANSCRIPTION**: the firmware repo cannot import the host package, so the
literal is checked against the real function by `firestarter_app`'s own test (D-08(b), Plan
128-09), which is where the binding actually lives. Cite the source of the literal as
`firestarter_app/firestarter/firmware.py:116-132`, `asset_candidates("py32f071")[0]`. Do not
imply the workflow imports or evaluates that function — D-08 explicitly forbids blurring the
transcription and the binding.

Also add a `# REL-04` marker in the comment so a grep for the requirement id finds this step.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
t=pathlib.Path('.github/workflows/beta-build.yml').read_text()
d=yaml.safe_load(t)
steps=d['jobs']['build']['steps']
s=[x for x in steps if x.get('name','').startswith('Assert the emitted asset filename')]
assert len(s)==1, [x.get('name') for x in steps]
st=s[0]
assert st['if'].strip()==\"steps.arm.outcome == 'success'\", st['if']
assert st['env']['HEX_PATH']=='\${{ steps.arm.outputs.hex_path }}', st['env']
b=st['run']
assert '\${{' not in b, 'expression interpolated into run body (V5)'
assert 'set -euo pipefail' in b
assert 'firestarter_py32f071.hex' in b
assert 'basename' in b and 'exit 1' in b
i_rep=[i for i,x in enumerate(steps) if x.get('name')=='Report a missing PY32F071 image'][0]
assert steps.index(st)==i_rep+1
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one step's name starts with `Assert the emitted asset filename`, and it sits immediately after the report step.
    - Its `if` is exactly `steps.arm.outcome == 'success'`.
    - It maps `HEX_PATH` in `env:` from `steps.arm.outputs.hex_path`, and its `run:` body contains no `${{` at all.
    - The `run:` body sets `set -euo pipefail`, contains the literal `firestarter_py32f071.hex`, contains a non-empty guard on `HEX_PATH` that exits non-zero, uses `basename`, and exits 1 on mismatch.
    - The comment block contains the substrings `REL-04`, `D-08`, `TRANSCRIPTION` (any case), `asset_candidates` and `firmware.py`.
    - `git -C /workspaces/firestarter diff HEAD~1 -- .github/workflows/beta-build.yml` shows insertions only.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
  </acceptance_criteria>
  <done>The release job fails with an exit code if the emitted basename is not the transcribed literal, and the file records that the real binding lives in the host repo.</done>
</task>

<task type="auto">
  <name>Task 2: Assert and log the resolved SDK commit SHA against the declared GIT_TAG</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` as edited by Task 1.
    - `/workspaces/firestarter/platform/py32f071/CMakeLists.txt` lines 10-25 — the `FetchContent_Declare` block, `GIT_TAG 0ed2f4b4d3391eccfd4491006a30295fd78e32c2` at line 16, and `GIT_SHALLOW FALSE` (a full clone, so `.git` exists in the fetched tree and `rev-parse HEAD` returns the pinned SHA from a detached HEAD).
    - `/workspaces/firestarter/.github/actions/build-py32f071/action.yml` — the `sdk_sha` output, including the non-fatal empty-value path and its `::warning::`.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — F-5 (the measured FetchContent path and the two required guards), F-15 (the corrected rationale: nothing caches `_deps`), and the D-10 code example.
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` lines 33-38 (the `actions/cache@v4` block) — read it so the corrected rationale you write is accurate about what is and is not cached.
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
Insert a step named `Assert and log the resolved SDK commit SHA (REL-04)` immediately after
Task 1's step, guarded `if: steps.arm.outcome == 'success'`.

Pass `SDK_SHA: ${{ steps.arm.outputs.sdk_sha }}` through `env:`. In the body:
`set -euo pipefail`; parse `PINNED` out of `platform/py32f071/CMakeLists.txt` with a `sed`
expression anchored on a `GIT_TAG` line followed by exactly 40 hex characters; then run
**both** non-vacuity guards before comparing — assert `"$PINNED"` matches a 40-hex pattern
and exit **2** with a message saying no 40-hex `GIT_TAG` parsed, and assert `"$SDK_SHA"`
matches a 40-hex pattern and exit **2** with a message saying the build resolved no SHA
(this is the branch that catches the composite action's non-fatal empty output when the
FetchContent layout changed). Only then compare the two strings and exit **1** on mismatch,
with a message naming both. On success, append a line to `$GITHUB_STEP_SUMMARY` naming the
resolved SHA and stating the pin was honoured.

The exit-code split is deliberate and matches the house taxonomy: 2 is a tool/format
failure, 1 is the substantive violation. Two empty strings compare equal, so the guards are
what make the assertion mean anything (A-7, Pitfall 8).

The comment must state D-10 and the **corrected** rationale (F-15). Do not write that this
catches a cache or mirror serving something else — nothing caches `build/py32f071/_deps`;
`actions/cache@v4` at lines 33-38 covers only `~/.cache/pip` and `~/.platformio/.cache`, so
the SDK is cloned fresh on every run and the cache scenario cannot occur. Write the reasons
that do apply: it catches a `GIT_TAG` edit that does not match what was actually fetched, an
upstream force-push behind a moved ref, and a future contributor adding an SDK cache without
re-checking the pin. Also record that the SDK is *already* pinned — this is not a new pin,
it is a per-release proof that the existing pin was honoured. Cite the measured FetchContent
source path and CI run 30676982030 as its provenance, in one line, so a reader knows the
path was observed rather than guessed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib,re
t=pathlib.Path('.github/workflows/beta-build.yml').read_text()
d=yaml.safe_load(t)
steps=d['jobs']['build']['steps']
s=[x for x in steps if x.get('name','').startswith('Assert and log the resolved SDK')]
assert len(s)==1, [x.get('name') for x in steps]
st=s[0]
assert st['if'].strip()==\"steps.arm.outcome == 'success'\"
assert st['env']['SDK_SHA']=='\${{ steps.arm.outputs.sdk_sha }}', st['env']
b=st['run']
assert '\${{' not in b
assert 'GIT_TAG' in b and 'platform/py32f071/CMakeLists.txt' in b
assert b.count('exit 2')>=2, 'both non-vacuity guards must exit 2'
assert 'exit 1' in b
assert re.search(r'\{40\}', b), 'missing 40-hex guard'
assert 'GITHUB_STEP_SUMMARY' in b
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one step's name starts with `Assert and log the resolved SDK`, guarded `if: steps.arm.outcome == 'success'`, sitting immediately after Task 1's step.
    - It maps `SDK_SHA` in `env:` from `steps.arm.outputs.sdk_sha`; its `run:` body contains no `${{`.
    - The `run:` body parses `GIT_TAG` from `platform/py32f071/CMakeLists.txt`, contains a `{40}` hex quantifier, exits `2` on each of the two non-vacuity failures (at least two `exit 2` occurrences), exits `1` on mismatch, and appends the resolved SHA to `$GITHUB_STEP_SUMMARY`.
    - Running the step's `sed`/parse expression against the real `platform/py32f071/CMakeLists.txt` locally yields exactly `0ed2f4b4d3391eccfd4491006a30295fd78e32c2` — the parse is proven against the real file even though the ARM half is CI-only.
    - The comment block contains `D-10`, `F-15`, `30676982030`, and does NOT contain the word `cache` used as this assertion's justification (the corrected rationale explicitly states nothing caches `_deps`).
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
  </acceptance_criteria>
  <done>Every release proves the declared SDK pin was the tree that was actually fetched, with both sides guarded against the empty-string comparison, and the shipped comment carries the corrected rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the built image carries the bumped VERSION string (REL-01, mechanically)</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` as edited by Tasks 1 and 2, plus lines 68-72 (`Generate release version`, `id: version`, and the `BETA_VERSION` env pattern).
    - `/workspaces/firestarter/include/version.h` line 11, `/workspaces/firestarter/include/firestarter.h` line 54 (`FW_VERSION` is `VERSION ":" RURP_BOARD_NAME`), and `/workspaces/firestarter/include/rurp_platform_compat.h` lines 23-32 (on ARM, `PROGMEM` is empty and `PSTR(v)` is `(v)`, so the concatenated literal lands in `.rodata` as ordinary bytes).
    - `/workspaces/firestarter/platform/py32f071/CMakeLists.txt` line 139 (`RURP_BOARD_NAME="py32f071"`) and lines 36-38 of its `FIRESTARTER_COMMON_SOURCES` list — `src/firestarter.cpp` and `src/hardware_operations.cpp` both reference `FW_VERSION`, which is why the literal is emitted at all.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — F-9, Pattern 1, Assumption A2 and Assumption A6.
    - `/workspaces/firestarter/.github/scripts/update_version.py` lines 176-194 — how `version` is written to `$GITHUB_OUTPUT`, and why the assertion targets that output rather than the tag.
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
Insert a step named `Assert the py32 image carries the bumped VERSION (REL-01)` immediately
after Task 2's step, guarded `if: steps.arm.outcome == 'success'`.

Pass two values through `env:`: `EXPECTED_VERSION: ${{ steps.version.outputs.version }}` and
`HEX_PATH: ${{ steps.arm.outputs.hex_path }}`. In the body: `set -euo pipefail`; assert
`"$EXPECTED_VERSION"` is non-empty and exit 2 if not — an empty version would make the
search string a bare `:py32f071` and the assertion nearly vacuous; build
`EXPECTED="$EXPECTED_VERSION:py32f071"`; convert the built image back to a flat binary with
`arm-none-eabi-objcopy -I ihex -O binary "$HEX_PATH" "$RUNNER_TEMP/py32.bin"`; then
`strings "$RUNNER_TEMP/py32.bin" | grep -Fq -- "$EXPECTED"` and exit 1 with a message naming
the expected string if it is not found; on success append a `PASS:` line naming the string to
`$GITHUB_STEP_SUMMARY`.

Use `grep -Fq`, **not** `grep -Fqx`. Assumption A2 records that `-x` (whole-line equality)
was considered and deliberately not used: `.rodata` packing is unobserved on this target, and
a too-strict match would fail rehearsal run A for a reason that has nothing to do with the
property being asserted. Put that reasoning in the comment so nobody "tightens" it later
without knowing why. `objcopy -I ihex -O binary` needs no `--gap-fill` or `--pad-to`
(Assumption A6): the sparse output still contains the literal bytes.

The comment must also state, in these terms: `include/firestarter.h` defines `FW_VERSION` as
`VERSION ":" RURP_BOARD_NAME`; on ARM `include/rurp_platform_compat.h` makes `PSTR()` a no-op
and `PROGMEM` empty, so the concatenated string is plain ASCII in `.rodata`; asserting it in
the image proves the image was built AFTER the `update_version.py` auto-commit — a property a
YAML step-order read cannot keep proving once someone reorders the job. And a hard note:
compare against `steps.version.outputs.version`, **never** against `tag_name`, because in
rehearsal mode D-03 overrides `tag_name` by design and a tag-equality assertion would be red
on purpose (F-2).

This assertion is added *alongside* Criterion 1's YAML step-order read, not instead of it.
Plan 128-05 already landed the ordering; Plan 128-10 records the step-index reading as
evidence too.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
t=pathlib.Path('.github/workflows/beta-build.yml').read_text()
d=yaml.safe_load(t)
steps=d['jobs']['build']['steps']
s=[x for x in steps if x.get('name','').startswith('Assert the py32 image carries')]
assert len(s)==1, [x.get('name') for x in steps]
st=s[0]
assert st['if'].strip()==\"steps.arm.outcome == 'success'\"
e=st['env']
assert e['EXPECTED_VERSION']=='\${{ steps.version.outputs.version }}', e
assert e['HEX_PATH']=='\${{ steps.arm.outputs.hex_path }}', e
b=st['run']
assert '\${{' not in b
assert 'arm-none-eabi-objcopy -I ihex -O binary' in b
assert 'strings' in b and 'grep -Fq' in b and 'grep -Fqx' not in b
assert ':py32f071' in b
assert 'tag_name' not in b
assert 'exit 1' in b and 'exit 2' in b
i=[j for j,x in enumerate(steps) if x.get('name','').startswith('Assert and log the resolved SDK')][0]
assert steps.index(st)==i+1
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one step's name starts with `Assert the py32 image carries`, guarded `if: steps.arm.outcome == 'success'`, immediately after Task 2's step.
    - Its `env:` maps `EXPECTED_VERSION` from `steps.version.outputs.version` and `HEX_PATH` from `steps.arm.outputs.hex_path`; its `run:` body contains no `${{`.
    - The `run:` body contains `arm-none-eabi-objcopy -I ihex -O binary`, `strings`, `grep -Fq`, and the literal `:py32f071`; it does NOT contain `grep -Fqx`, `--gap-fill`, `--pad-to`, or the string `tag_name`.
    - The body exits 2 on an empty `EXPECTED_VERSION` and exits 1 when the string is not found.
    - The comment block contains `REL-01`, `F-9`, `A2`, `rodata`, `PSTR`, and the sentence fragment stating the comparison is against the step output and never against the tag.
    - The four `if: steps.arm.outcome == 'success'` guarded steps (Tasks 1-3 plus none other) all sit strictly between the report step and the `Release` step, and `Release` itself is still unmodified from its pre-phase form.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green; `git -C /workspaces/firestarter status --porcelain` empty.
  </acceptance_criteria>
  <done>REL-01 is an exit code over the shipped bytes rather than a reading of step order, keyed on the version step's output so rehearsal mode cannot make it red by design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream SDK repo -> fetched build tree | Third-party source arrives at build time via `FetchContent`; a moved ref substitutes content silently |
| build outputs -> published release assets | Anything asserted here is what a stranger downloads and flashes |
| workflow expressions -> shell | Step outputs reach `run:` bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-03 | Tampering | SDK source substitution behind a moved `GIT_TAG` ref, or a `GIT_TAG` edit that does not match what was fetched | medium | mitigate | Task 2's string equality between the fetched tree's `rev-parse HEAD` and the declared 40-hex `GIT_TAG`, with a 40-hex non-vacuity guard on both sides and exit 2 for the format failure |
| T-128-12 | Spoofing | an emitted image whose name no host lookup matches | medium | mitigate | Task 1's basename equality against the transcribed literal, guarded against an empty `hex_path`; permanently bound to the host function by Plan 128-09 |
| T-128-16 | Tampering | a stale-`VERSION` image published under a fresh tag, so the host's update decision compares the wrong string | high | mitigate | Task 3's `strings` assertion over the objcopy'd published image, compared against `steps.version.outputs.version` — evidence on the shipped bytes that survives a step reorder |
| T-128-17 | Repudiation | an assertion that passes because both sides parsed empty | medium | mitigate | every one of the three steps guards its inputs for non-emptiness (and 40-hex where applicable) BEFORE comparing, exiting 2 for the parse failure and 1 for the violation |
| T-128-01 | Tampering / Elevation | expression interpolated into a `run:` body | high | mitigate | all three steps pass values through `env:` only; each acceptance criterion asserts the `run:` body contains no `${{` |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). `arm-none-eabi-objcopy` and `strings` come from the apt line already covered by T-128-06. |
</threat_model>

<verification>
- `beta-build.yml` parses as YAML; three new steps exist, all guarded `if: steps.arm.outcome == 'success'`, all between the report step and `Release`.
- No `run:` body in any of the three contains `${{`.
- The `GIT_TAG` parse expression, run locally against the real `platform/py32f071/CMakeLists.txt`, yields the pinned 40-hex SHA — the one half of this plan that IS locally provable.
- `grep -Fqx` and `tag_name` appear nowhere in the REL-01 step.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green, including Plan 128-01's comment-stripped invariant test.
- `git -C /workspaces/firestarter status --porcelain` empty.
- CI-only: the ARM half of all three assertions is unproven until rehearsal run A. Plan 128-10 records the observation with a run URL + commit SHA. Nothing here claims the image runs.
</verification>

<success_criteria>
- Three exit-code assertions run on the ARM success path: emitted basename equals the transcribed literal; resolved SDK SHA equals the declared 40-hex `GIT_TAG`; the objcopy'd published image contains the bumped version string.
- Each has a non-vacuity guard that runs before any comparison, with exit 2 for a parse/format failure and exit 1 for the substantive violation.
- The REL-01 assertion targets `steps.version.outputs.version`, never `tag_name`.
- The SDK comment carries F-15's corrected rationale, not the cache one.
- The filename step is explicitly labelled a transcription, with the binding attributed to the host repo.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-06-SUMMARY.md` when done.
</output>
