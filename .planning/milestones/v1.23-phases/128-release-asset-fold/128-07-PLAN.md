---
phase: 128-release-asset-fold
plan: 07
type: execute
wave: 4
depends_on: ["128-01", "128-06"]
files_modified:
  - firestarter/.github/workflows/beta-build.yml
autonomous: true
requirements: [REL-02, REL-03]

must_haves:
  truths:
    - "REL-03 / D-11: `scripts/check_release_assets.py` is called as a step that runs UNCONDITIONALLY and immediately before `Release`, so a missing or zero-byte AVR hex fails the build before anything is published. It carries no `if:` guard — the whole point is that it still runs when the ARM build was contained."
    - "REL-02 / D-15: `files:` becomes a block list with two entries — `.pio/build/**/firestarter_*.hex` (PlatformIO's tree, unchanged) and `build/py32f071/firestarter_*.hex` (CMake's tree, a GLOB not a literal). One glob cannot cover both trees."
    - "F-1 (CORRECTION): REL-02's stated rationale is FALSE. `softprops/action-gh-release` runs `glob.sync()` on EVERY `files:` entry, literal or glob alike, and only fails when `fail_on_unmatched_files: true`, whose default is false. The soft-absence property REL-03 depends on comes from that default, not from choosing a glob. The shipped comment must state the corrected mechanism and must NOT repeat the glob-versus-literal folklore."
    - "F-1 / Pitfall 1: the real REL-03 invariant is that the unmatched-files failure input is never SET on this step. Its absence is pinned by a comment naming REL-03, and enforced by Plan 128-01's comment-stripped test, so a future contributor adding it 'for stricter CI' sees why it is forbidden."
    - "D-01: `draft:` resolves from `steps.mode.outputs.rehearsal`, so a rehearsal dispatch produces a draft release. F-3 (RESOLVED): a draft release creates NO git tag — publishing is what triggers tag-ref creation, and publishing never happens."
    - "D-03: in rehearsal mode `tag_name` is overridden to `rehearsal-${{ github.run_id }}` so an accidentally-published draft cannot masquerade as the next real beta. On a real `beta` push it resolves to `steps.version.outputs.version`, unchanged."
    - "D-13: `build.yml` (stable / `main`) is untouched, deliberately. Recorded with its graduation trigger in Plan 128-08's README section."
  artifacts:
    - firestarter/.github/workflows/beta-build.yml
  key_links:
    - "`scripts/check_release_assets.py` (Plan 128-01) -> this step is its only production call site"
    - "`steps.mode.outputs.rehearsal` (Plan 128-05) -> `draft:` and `tag_name:`"
    - "`build/py32f071/firestarter_*.hex` -> the second `files:` entry -> the published release asset"
---

<objective>
Close the release path: call the AVR-assets gate immediately before `Release`, convert
`files:` to a two-entry block list so the CMake tree is actually reachable, pin the
unmatched-files omission with a comment carrying the corrected mechanism, and wire
`draft:` and `tag_name:` to the rehearsal switch.

Purpose: this is the step where REL-02 and REL-03 become real. Until `files:` gains a
second entry the py32 image is built and asserted and then simply never published — the
shipped glob is rooted at PlatformIO's `.pio/build/`, and CMake writes to `build/py32f071/`.
And until the assets gate runs before publication, "a broken ARM build still publishes all
three AVR assets" is an assumption rather than a gate.

**Scope boundary — this plan does NOT close REL-02 or REL-03.** It lands the publication
mechanism and the gate's call site. REL-02 additionally needs run A's observed asset list
(128-10); REL-03 additionally needs the checker itself (128-01), the LOUD gate (128-04) and
run B's observed cascade (128-10). Do not tick REL-02 or REL-03 in REQUIREMENTS.md from
this plan.

Output: one modified workflow. This is the last change to `beta-build.yml` in this phase.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-01-SUMMARY.md
@.planning/phases/128-release-asset-fold/128-06-SUMMARY.md

**Repo:** `/workspaces/firestarter`, branch `v1.23-py32f071-integration`. Commit inside the
sub-repo.

**No task may run `git push` or `gh workflow run`** (D-04). The dispatches are Plan 128-10's
and are operator-gated. **No ARM toolchain exists locally.** Every claim about what this
workflow publishes is discharged by a CI run URL + commit SHA. Nothing may claim the
published image runs, boots or installs — the permitted claim is exactly one sentence wide:
*the asset publishes.*
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `files:` entry (this plan):** `build/py32f071/firestarter_*.hex` — a second entry
alongside the pre-existing `.pio/build/**/firestarter_*.hex`, which becomes a block list

**New `tag_name` form (this plan):** `rehearsal-${{ github.run_id }}`, used in rehearsal
mode only

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`,
`firestarter_py32f071_build_diagnostics`

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto">
  <name>Task 1: Call the AVR-assets gate unconditionally, immediately before Release</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` as edited by Plans 128-05 and 128-06 — in particular the `Resolve release target SHA` step (id `release_target`) and the `Release` step that follows it.
    - `/workspaces/firestarter/scripts/check_release_assets.py` — its module docstring's `Usage:` block and its two env seams. Confirm the committed defaults (`scripts/baseline/size_baseline.json` and `<repo>/.pio/build`) are exactly what CI produces, so the call site needs no flags.
    - `/workspaces/firestarter/tests/test_check_release_assets.py` — the exit taxonomy the call site relies on (0 clean, 1 violation, 2 tool/format failure).
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — D-11, D-12, and the system architecture diagram's placement of this step between the assertions and `Resolve release target SHA`/`Release`.
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
Insert a step named `Assert all AVR release assets are present (REL-03)` immediately before
the `Release` step — that is, after `Resolve release target SHA`.

Its `run:` is a single invocation: `python3 scripts/check_release_assets.py`. No flags. The
checker's committed defaults already resolve to `scripts/baseline/size_baseline.json` and
`<repo>/.pio/build`, which is exactly the tree `pio run` wrote earlier in this same job; the
`--baseline` and `--build-root` flags and their env seams exist for the paired tests and for
local use, not for this call site. Say that in the comment, so nobody adds redundant flags
later.

**The step carries no `if:` guard at all.** That is the requirement, not an omission: the
gate must still run when `steps.arm.outcome` is `failure`, because "a broken ARM build still
publishes all three AVR assets" is only provable if the AVR assertion runs in exactly that
case. Write that reason into the comment.

Comment the step with REL-03 and D-11/D-12: the required set is derived from
`scripts/baseline/size_baseline.json`'s `avr_targets` keys, so a fourth AVR target updates
this gate for free rather than silently not being required; the checker never mentions or
requires the py32 image, because its absence must be tolerated (which is why D-11 rejected a
single do-everything checker that would have had to carry internal severity logic); and the
script is provable locally against committed planted fixtures, which is what
*"demonstrably fails"* in REL-03's own wording asks for.

Do not add this call to `build.yml`. D-13 puts the stable workflow out of scope with a
recorded graduation trigger, landed in Plan 128-08's README section.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
d=yaml.safe_load(pathlib.Path('.github/workflows/beta-build.yml').read_text())
steps=d['jobs']['build']['steps']
g=[x for x in steps if 'check_release_assets.py' in (x.get('run') or '')]
assert len(g)==1, [x.get('name') for x in steps]
st=g[0]
assert 'if' not in st, 'the AVR gate must be unconditional (REL-03)'
i_rel=[i for i,x in enumerate(steps) if x.get('name')=='Release'][0]
assert steps.index(st)==i_rel-1, (steps.index(st), i_rel)
assert '--build-root' not in st['run'] and '--baseline' not in st['run']
print('OK')
" && python3 scripts/check_release_assets.py --build-root tests/fixtures/clean_release_assets_all_three/pio_build</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one step's `run` invokes `scripts/check_release_assets.py`, and its index in `jobs.build.steps` is exactly one less than the index of the step named `Release`.
    - That step has **no** `if:` key.
    - Its `run` body passes no `--build-root` and no `--baseline` flag.
    - Its comment block contains `REL-03`, `D-11`, `D-12`, `avr_targets`, and a sentence stating the step is deliberately unguarded so it runs when the ARM build was contained.
    - `build.yml` is unmodified: `git -C /workspaces/firestarter diff HEAD~1 --name-only` lists only `.github/workflows/beta-build.yml`.
    - `cd /workspaces/firestarter && python3 scripts/check_release_assets.py --build-root tests/fixtures/clean_release_assets_all_three/pio_build` exits 0 (the call site's script is the one already proven by Plan 128-01).
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
  </acceptance_criteria>
  <done>A missing or zero-byte AVR hex fails the release job before anything is published, and it does so on exactly the path where the ARM build was contained.</done>
</task>

<task type="auto">
  <name>Task 2: Convert files: to a two-entry block list and pin the unmatched-files omission</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` — the `Release` step. `files:` is currently the single-line scalar `.pio/build/**/firestarter_*.hex` (originally line 92, verified by 128-PATTERNS). The `env:` block below it already carries a long provenance comment (`Phase 20 E2E-05`), which is the precedent for pinning an omission with a comment.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — F-1 in full (the primary-source read of `src/util.ts` `unmatchedPatterns()`, `src/run.ts`'s warn-or-throw, and the `false` default), Pitfall 1, and the Anti-Patterns entry forbidding the glob-versus-literal rationale.
    - `/workspaces/firestarter/tests/test_check_release_assets.py` — the comment-stripped invariant test. **Read it before writing the comment**: the test drops lines whose first non-whitespace character is `#`, so a comment naming the input is safe and a YAML key setting it is not. A whole-file grep would have made the explanation self-invalidating.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-PATTERNS.md` MISMATCH-2 and MISMATCH-3 — the two places the false rationale is written down, both of which Plan 128-08 corrects in the README.
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
Convert the `Release` step's `files:` from a single-line scalar to a `|` block list with
exactly two entries, in this order:

    .pio/build/**/firestarter_*.hex
    build/py32f071/firestarter_*.hex

The second entry is a **glob**, matching D-15's correction: `ad47c3b`'s README supplied the
literal `build/py32f071/firestarter_py32f071.hex` directly under prose arguing for a glob
(MISMATCH-3). Use the glob, and make the README match it in Plan 128-08.

Write the comment above `files:` stating the reason two entries are needed: PlatformIO
writes under `.pio/build/` and CMake writes under `build/py32f071/`, so one glob cannot
cover both trees. This is the corrected form of the claim `ad47c3b`'s commit message and
README both got wrong (R-16 / MISMATCH-2).

Then write the comment pinning the omission. This is the most important comment in the file
and its content is constrained:

- State that the release action's unmatched-files failure input is deliberately **UNSET**,
  that its default is false, and that REL-03 requires a broken ARM build to leave the py32
  asset simply ABSENT rather than failing the release — so setting it true would violate
  REL-03 directly. Name REL-03 in the comment so a grep for the id finds it.
- Record the correction on the record: the action makes **no** glob-versus-literal
  distinction. `src/util.ts`'s `unmatchedPatterns()` globs every entry, literal or glob
  alike, and `src/run.ts` warns-or-throws purely on that input. **Do not repeat the false
  rationale** in this comment or anywhere else — REL-02's own text and `ad47c3b`'s README
  both assert that the action warns on an unmatched glob but fails on a missing literal, and
  it is not true of this action at any version in its current source. Shipping a corrected
  code line beside a wrong explanation is the Phase 122 C-5 shape.
- State that the glob above is chosen for rename-resilience and for reading consistently
  with the AVR entry, not for failure semantics.

Change nothing else in the `Release` step in this task. The `env:`/`GITHUB_TOKEN` block and
its Phase 20 E2E-05 comment stay exactly as they are; the job already holds
`permissions: contents: write` at lines 25-26 and this plan adds no permission.

<!-- planner-discipline-allow: fail_on_unmatched_files -->
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib,re
p=pathlib.Path('.github/workflows/beta-build.yml')
t=p.read_text()
d=yaml.safe_load(t)
rel=[s for s in d['jobs']['build']['steps'] if s.get('name')=='Release'][0]
files=[l.strip() for l in rel['with']['files'].strip().splitlines() if l.strip()]
assert files==['.pio/build/**/firestarter_*.hex','build/py32f071/firestarter_*.hex'], files
assert 'fail_on_unmatched_files' not in rel['with'], rel['with']
live=[l for l in t.splitlines() if l.strip() and not l.strip().startswith('#')]
assert not any(re.search(r'fail_on_unmatched_files\s*:', l) for l in live), 'key is SET, not merely mentioned'
assert any('softprops/action-gh-release@v2' in l for l in live), 'non-vacuity: release action not found'
assert 'firestarter_py32f071.hex' not in rel['with']['files']
print('OK', files)
" && python3 -m pytest tests/test_check_release_assets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The `Release` step's `files:` parses to exactly the two-element list `['.pio/build/**/firestarter_*.hex', 'build/py32f071/firestarter_*.hex']`, in that order, and contains no literal `firestarter_py32f071.hex`.
    - The `Release` step's `with:` mapping has no `fail_on_unmatched_files` key.
    - Over comment-stripped `beta-build.yml` source, no line matches `fail_on_unmatched_files\s*:`, and some line contains `softprops/action-gh-release@v2` (the non-vacuity guard).
    - The comment above `files:` contains `REL-03`, `.pio/build/`, `build/py32f071/`, `default`, `util.ts` and `run.ts`, and contains neither the phrase `fails on a missing literal` nor `warns on an unmatched glob`.
    - The `env:` block and its `Phase 20 E2E-05` comment are byte-identical to their pre-plan form.
    - `jobs.build.permissions` is still exactly `{contents: write}` — no permission added.
    - `cd /workspaces/firestarter && python3 -m pytest tests/test_check_release_assets.py -q` green (10 passed) — the invariant test must survive the comment that names the input.
  </acceptance_criteria>
  <done>The CMake tree is inside the publication glob, and the omission that makes REL-03 possible is documented with the corrected mechanism instead of the folklore.</done>
</task>

<task type="auto">
  <name>Task 3: Wire draft: and tag_name: to the rehearsal switch</name>
  <read_first>
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` — the `mode` step landed by Plan 128-05 (its output name is `rehearsal`), and the `Release` step's current `tag_name: ${{ steps.version.outputs.version }}`.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` — D-01, D-03, F-3 (RESOLVED: `finalizeRelease()` returns early when `input_draft` is true, the action's own `untagged-...` comment, and issue #722 — a draft release creates no git tag), F-7 (the precedent: run 30199560282 dispatched this workflow from a non-default branch WITHOUT draft mode and published the real public prerelease `3.0.0b11` with a real tag), and Assumption A1.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-05-SUMMARY.md` — the exact resolved-value semantics of `steps.mode.outputs.rehearsal` (the string `true` or the string `false`).
  </read_first>
  <files>firestarter/.github/workflows/beta-build.yml</files>
  <action>
In the `Release` step's `with:` mapping:

Add `draft:` resolving from the normalised switch — `${{ steps.mode.outputs.rehearsal == 'true' }}`.
Do **not** read `inputs.rehearsal` directly here: `workflow_dispatch` inputs are absent on a
`push` event, and Plan 128-05's `mode` step exists precisely so exactly one place decides
what the value is and echoes it to the step summary of every run (Assumption A1). A
`draft: true` leaking onto a real `beta` push would silently stop publishing betas.

Replace `tag_name:` with the rehearsal-aware form:
`${{ steps.mode.outputs.rehearsal == 'true' && format('rehearsal-{0}', github.run_id) || steps.version.outputs.version }}`.
`github.run_id` is integer-valued and GitHub-supplied, so it needs no sanitisation.

Comment both lines together with D-01, D-03 and F-3. State: in rehearsal mode this publishes
a **draft**, and a draft release creates **no git tag** — the action's `finalizeRelease()`
returns the release untouched when draft is set, publishing is what triggers GitHub's
tag-ref creation, and it never happens (verified from the action's source and corroborated
by its own `untagged-...` asset-URL comment and issue #722). State D-03's reason for the tag
override: the version bump still runs and still compiles into the image, so REL-01 stays
proven by the rehearsal; what changes is only the name the draft carries, so an accidentally
published draft cannot masquerade as the next real beta. And record F-7's precedent in one
sentence — this workflow has already been dispatched from a non-default milestone branch
once, on 2026-07-26 (run 30199560282), and because draft mode did not exist it published the
real public prerelease `3.0.0b11` with a real tag. The `rehearsal` input's permanence (D-03)
is justified by that, not by hypothetical caution.

Add one further note in the same comment: a rehearsal dispatch must always supply
`beta_version` explicitly. `update_version.py`'s `is_beta_mode()` requires `--beta`, a
`GITHUB_REF` of `refs/heads/beta`, or a non-empty `BETA_VERSION`; a throwaway-branch dispatch
with the input blank satisfies none and takes the **stable** path, rewriting
`include/version.h` to a stable patch bump (F-2). The dispatch procedure itself belongs to
Plan 128-10, but the warning belongs next to the switch a reader will be looking at.

This is the last change to `beta-build.yml` in this phase. After it, re-read the complete
`Release` step once and confirm it reads coherently top to bottom.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import yaml,pathlib
t=pathlib.Path('.github/workflows/beta-build.yml').read_text()
d=yaml.safe_load(t)
rel=[s for s in d['jobs']['build']['steps'] if s.get('name')=='Release'][0]
w=rel['with']
assert w['draft'].strip()==\"\${{ steps.mode.outputs.rehearsal == 'true' }}\", w['draft']
tn=w['tag_name']
assert 'steps.mode.outputs.rehearsal' in tn and \"format('rehearsal-{0}', github.run_id)\" in tn and 'steps.version.outputs.version' in tn, tn
assert 'inputs.rehearsal' not in tn and 'inputs.rehearsal' not in w['draft']
assert w['prerelease'] is True and w['make_latest'] is False
assert 'fail_on_unmatched_files' not in w
print('OK')
" && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `Release.with.draft` is exactly the expression `${{ steps.mode.outputs.rehearsal == 'true' }}`.
    - `Release.with.tag_name` references `steps.mode.outputs.rehearsal`, `format('rehearsal-{0}', github.run_id)` and `steps.version.outputs.version`, and references `inputs.rehearsal` nowhere.
    - Neither `draft` nor `tag_name` reads `github.event.inputs.rehearsal` or `inputs.rehearsal` directly — both go through the `mode` step's output.
    - `prerelease` is still the boolean `true` and `make_latest` still the boolean `false`.
    - The comment block contains `D-01`, `D-03`, `F-3`, `30199560282`, `3.0.0b11`, and the sentence requiring `beta_version` on every rehearsal dispatch.
    - The whole file still contains zero occurrences of `steps.arm.conclusion` and zero occurrences of `always()`.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green; `git -C /workspaces/firestarter status --porcelain` empty.
  </acceptance_criteria>
  <done>A rehearsal dispatch produces an unmistakably named draft that creates no tag, a real beta push is unchanged, and the file records both the resolved mechanism and the precedent that makes it necessary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release job (`contents: write`) -> public releases and tags | This step is where a permanent, public artifact is created |
| build outputs -> published assets | Anything matched by `files:` becomes downloadable by strangers |
| operator dispatch -> release visibility | The rehearsal switch decides whether the output is a deletable draft or a real public prerelease |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-02 | Repudiation | a rehearsal dispatch publishing a real public prerelease and a real tag | high | mitigate | `draft:` from the normalised switch (F-3: a draft creates no tag) plus D-03's `rehearsal-<run_id>` tag override plus D-04's structural operator gate. **This threat has already materialised once in this repo from this exact route** (run 30199560282 published `3.0.0b11`), which is why the mitigation is permanent rather than a rehearsal-only scaffold. |
| T-128-04 | Tampering | a broken build silently shipping an incomplete release | high | mitigate | Task 1's unconditional `check_release_assets.py` gate immediately before `Release` |
| T-128-18 | Tampering | `fail_on_unmatched_files: true` added later "for stricter CI", converting the contained ARM case into a hard release failure | medium | mitigate | the omission is pinned by a comment naming REL-03 and the corrected mechanism, and enforced by Plan 128-01's comment-stripped test which fails closed |
| T-128-15 | Tampering | `draft: true` leaking onto a real `beta` push, silently stopping beta publication | high | mitigate | resolution goes through the single `mode` step (`default: false`, boolean-typed), echoed to `$GITHUB_STEP_SUMMARY` on every run, and confirmed by observation on run A |
| T-128-19 | Tampering | a zero-byte hex published as a real asset (`statSync().isFile()` accepts it) | medium | mitigate | the assets gate's `st_size > 0` check, proven by the zero-byte planted fixture |
| T-128-05 | Tampering | third-party actions pinned to mutable `@vN` major tags | medium | accept | Pre-existing repo-wide posture. SHA-pinning every action is a real improvement AND a real behaviour change; it is not in REL-01…REL-04 and this plan adds no new third-party action. Recorded as deferred. |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). |
</threat_model>

<verification>
- `beta-build.yml` parses as YAML; `files:` is the two-entry list in the stated order.
- No `fail_on_unmatched_files` key is set on the `Release` step, asserted over comment-stripped source with a non-vacuity guard on the release action's presence.
- The AVR gate step index is exactly one less than the `Release` step index and carries no `if:`.
- `draft:` and `tag_name:` both resolve through `steps.mode.outputs.rehearsal`, never through `inputs.rehearsal`.
- `jobs.build.permissions` is unchanged (`contents: write` only).
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green, including the invariant test in `tests/test_check_release_assets.py`.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` unchanged by this plan.
- `git -C /workspaces/firestarter status --porcelain` empty.
- CI-only: what this step actually publishes is unproven until rehearsal run A. Plan 128-10 records the observed asset list from the API, cited with a run URL + commit SHA. The permitted claim is that the asset publishes — not that the image runs.
</verification>

<success_criteria>
- The AVR-assets gate runs unconditionally, immediately before `Release`.
- `files:` is a two-entry block list whose second entry is a glob reaching `build/py32f071/`.
- The unmatched-files omission is pinned by a comment carrying F-1's corrected mechanism, and the false glob-versus-literal rationale appears nowhere in the file.
- `draft:` and `tag_name:` resolve from the single normalised rehearsal switch; a real `beta` push is behaviourally unchanged.
- No permission is added and no third-party action is introduced or re-pinned.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-07-SUMMARY.md` when done.
</output>
