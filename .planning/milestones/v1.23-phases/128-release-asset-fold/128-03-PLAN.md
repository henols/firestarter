---
phase: 128-release-asset-fold
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/platform/py32f071/CMakeLists.txt
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "D-14: the four hyphen-to-underscore renames from unmerged commit `ad47c3b` (`TARGET_NAME`, the `-Wl,-Map=` link option, `BIN_FILE`, `HEX_FILE`) are re-applied **by hand** with `ad47c3b` cited in the commit message — never cherry-picked, because that commit's tree predates Phase 124's `push: branches: [beta]` trigger and its workflow shape is superseded by D-06's composite action."
    - "MISMATCH-2 (128-PATTERNS): `ad47c3b`'s own explanatory comment asserts the rename means `beta-build.yml`'s release glob needs no new pattern. That is FALSE (R-16) — the shipped glob is rooted at `.pio/build/` and CMake writes to `build/py32f071/`. The re-applied comment must state the corrected fact, that a SECOND `files:` entry is required, not carry the original sentence."
    - "REL-04 (emitted-name slice only): the CMake build emits `firestarter_py32f071.hex`, which is exactly `asset_candidates(\"py32f071\")[0]` as computed live from `firestarter_app/firestarter/firmware.py`."
  artifacts:
    - firestarter/platform/py32f071/CMakeLists.txt
  key_links:
    - "`TARGET_NAME` -> the ELF name -> `$<TARGET_FILE:${TARGET_NAME}>` in the objcopy custom command -> `BIN_FILE` / `HEX_FILE`"
    - "`HEX_FILE` basename -> the composite action's `hex_path` glob -> `beta-build.yml`'s D-08(a) literal -> `asset_candidates(\"py32f071\")[0]` (bound by Plan 128-09)"
---

<objective>
Re-apply, by hand, the hyphen-to-underscore rename in `platform/py32f071/CMakeLists.txt` —
all four sites — and rewrite the explanatory comment `ad47c3b` shipped with it, because
that comment carries a false claim about the release glob.

Purpose: the host installer resolves release assets by the exact name
`firestarter_<board>.hex` where `<board>` is the `RURP_BOARD_NAME` the firmware reports
(`py32f071`). Until this rename lands, the CMake build emits a hyphenated name that no
release glob and no host lookup will ever match. Phase 124 D-07 deliberately withheld
`ad47c3b` for this phase.

**Scope boundary — this plan does NOT close REL-04.** It closes only the *emitted CMake
filename* slice. REL-04 additionally needs the workflow-side renames (128-04), the
in-workflow string-equality and SDK-SHA assertions (128-06), the cross-repo three-way
binding (128-09) and run A evidence (128-10). Do not tick REL-04 in REQUIREMENTS.md from
this plan.

Output: one modified file, four renamed literals, one corrected comment.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md

**Repo:** `/workspaces/firestarter`, branch `v1.23-py32f071-integration`. Commit inside the
sub-repo, not in the meta-repo.

**No ARM toolchain exists in this devcontainer.** This rename cannot be built or measured
locally. Its effect is discharged by CI — first by Plan 128-04's `py32f071.yml` run and then
by Plan 128-10's run A, each cited by run URL + commit SHA. Nothing in this plan may claim
the resulting image runs, boots or installs; no PY32F071 PCB exists.
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/`:** `tests/test_py32_asset_name_host.py`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT`

**New CLI flags:** `--build-root <path>`, `--baseline <path>` on `check_release_assets.py`

**New workflow input:** `rehearsal` (`type: boolean`, `default: false`) on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}` (rehearsal mode only)

**New fixture directory component:** `pio_build/`

**Renamed build outputs (this plan):** `firestarter_py32f071.{elf,bin,hex,map}` — the
underscore form is new to the tree. The hyphenated form it replaces has never appeared in
any published release asset (verified live against `3.0.0b13` and `3.0.0b14`, both of which
carry exactly three AVR hexes and no py32 asset), so there is no historical asset to migrate.

**Renamed Actions artifact names in `py32f071.yml`:** `firestarter_py32f071`,
`firestarter_py32f071_build_diagnostics`

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto">
  <name>Task 1: Rename the four CMake output literals and rewrite the explanatory comment</name>
  <read_first>
    - `/workspaces/firestarter/platform/py32f071/CMakeLists.txt` — read the whole file. The four sites are line 25 (`set(TARGET_NAME ...)`), line 173 (the `-Wl,-Map=` link option string), line 184 (`set(BIN_FILE ...)`) and line 185 (`set(HEX_FILE ...)`). Note also line 139 (`RURP_BOARD_NAME="py32f071"`) and lines 188-196 (the `add_custom_command` consuming `BIN_FILE`/`HEX_FILE` through `$<TARGET_FILE:${TARGET_NAME}>`), which need no edit but must still resolve.
    - The unmerged commit: `git -C /workspaces/firestarter show ad47c3b -- platform/py32f071/CMakeLists.txt`. Read its six-line comment. Its LAST sentence is the one to rewrite.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-PATTERNS.md` MISMATCH-2, and its CONFIRMED row recording that `beta-build.yml:92` is a single-line scalar `files: .pio/build/**/firestarter_*.hex`.
    - `/workspaces/firestarter_app/firestarter/firmware.py` lines 116-132 — `asset_candidates()`. READ-ONLY. Frozen this phase.
  </read_first>
  <files>firestarter/platform/py32f071/CMakeLists.txt</files>
  <action>
Apply four scoped edits to `platform/py32f071/CMakeLists.txt`, changing the separator in the
emitted-output basename from a hyphen to an underscore at each site:

1. Line 25 — the `TARGET_NAME` value, ending `.elf`.
2. Line 173 — the linker map path inside the `-Wl,-Map=...,--cref` string, ending `.map`.
3. Line 184 — `BIN_FILE`, ending `.bin`.
4. Line 185 — `HEX_FILE`, ending `.hex`.

Change nothing else. `RURP_BOARD_NAME="py32f071"` at line 139 already carries the board token
and stays exactly as it is. The `add_custom_command` block resolves through the variables and
needs no edit. The `GIT_TAG` SDK pin at line 16 is not this plan's business.

Above the `TARGET_NAME` line, place the explanatory comment. Re-apply `ad47c3b`'s reasoning
intact for its first five sentences: the underscore is load-bearing because the host
installer resolves release assets by `firestarter_<board>.hex`, where `<board>` is the
`RURP_BOARD_NAME` the firmware reports over the wire; the AVR targets get that name from
`name_firmware.py`, and a CMake build has to spell it out.

Then REPLACE `ad47c3b`'s final sentence. Its claim — that this keeps `beta-build.yml`'s
release glob working with no new pattern — is false (R-16, 128-PATTERNS MISMATCH-2): the
shipped glob is rooted at PlatformIO's `.pio/build/` tree while CMake writes to
`build/py32f071/`, so one glob cannot cover both trees and a SECOND `files:` entry is
required (Plan 128-07 adds it). Write that corrected fact. Cite Phase 128 Plan 03 and
`ad47c3b` in the comment, per the house style in which every comment names the decision,
requirement or phase that produced the line. Shipping a corrected code line beside a wrong
explanation is exactly the defect caught in Phase 122 (C-5).

Commit inside `/workspaces/firestarter` on `v1.23-py32f071-integration`. The commit message
must cite `ad47c3b` as the origin of the rename and state explicitly that it was re-applied
by hand rather than cherry-picked, because `ad47c3b`'s tree predates Phase 124's MERGE-03
`push: branches: [beta]` trigger and a cherry-pick would silently revert it.

<!-- planner-discipline-allow: firestarter-py32f071 -->
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -v '^[[:space:]]*#' platform/py32f071/CMakeLists.txt > /tmp/cml.nc && test "$(grep -c 'firestarter-py32f071' /tmp/cml.nc)" -eq 0 && test "$(grep -c 'firestarter_py32f071' /tmp/cml.nc)" -eq 4 && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - With comment lines stripped, `platform/py32f071/CMakeLists.txt` contains zero occurrences of the hyphenated basename and exactly four occurrences of the underscored basename.
    - `grep -n 'firestarter_py32f071' platform/py32f071/CMakeLists.txt` shows one match each on the `TARGET_NAME` line (`.elf`), the `-Wl,-Map=` line (`.map`), the `BIN_FILE` line (`.bin`) and the `HEX_FILE` line (`.hex`).
    - `grep -c 'RURP_BOARD_NAME="py32f071"' platform/py32f071/CMakeLists.txt` is still `1`.
    - `grep -c 'GIT_TAG 0ed2f4b4d3391eccfd4491006a30295fd78e32c2' platform/py32f071/CMakeLists.txt` is still `1`.
    - The comment block above `TARGET_NAME` contains the substrings `ad47c3b`, `.pio/build/`, `build/py32f071/` and `second`, and does NOT contain the phrase `no new pattern`.
    - `git -C /workspaces/firestarter show --stat HEAD` lists `platform/py32f071/CMakeLists.txt` as the only changed file.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` is green — in particular the CMake-manifest checker's paired test must not regress.
  </acceptance_criteria>
  <done>All four emitted-output literals use the underscore form, the SDK pin and board name are untouched, and the shipped comment states the corrected two-entry-glob fact rather than `ad47c3b`'s false one.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the emitted name equals the frozen host contract, non-vacuously</name>
  <read_first>
    - The edited `/workspaces/firestarter/platform/py32f071/CMakeLists.txt`.
    - `/workspaces/firestarter_app/firestarter/firmware.py` lines 106-132 — `flash_method()` and `asset_candidates()`. READ-ONLY.
    - `/workspaces/firestarter/scripts/check_cmake_manifest.py` lines 1-60 — to confirm the manifest checker's expectations are unaffected by an output-name change.
  </read_first>
  <files>firestarter/platform/py32f071/CMakeLists.txt</files>
  <action>
Verification-only. Make no further source edits unless a check fails.

Extract the `HEX_FILE` basename from `platform/py32f071/CMakeLists.txt` with a regex, and
independently compute `asset_candidates("py32f071")[0]` by running Python inside
`/workspaces/firestarter_app`. Before comparing, assert each string is non-empty and matches
the pattern for a lowercase underscored firestarter hex name — two empty strings compare
equal, and a rename that makes a regex miss must fail loudly rather than pass vacuously
(research finding A-7, Pitfall 8). Then assert the two are equal.

Next, establish the rename baseline the later sweep closes against. Run a comment-stripped
repo-wide grep for the hyphenated basename across `/workspaces/firestarter` and record every
surviving occurrence with its file and line. At this point in the phase the surviving
occurrences are expected to be confined to `.github/workflows/py32f071.yml` — that file is
Plan 128-04's work and its edit would collide across waves, so do NOT touch it here. Record
the exact count and file list in the SUMMARY so Plan 128-04 and Plan 128-08's consistency
sweep have a measured baseline rather than an assumption.

Record both extracted strings and the surviving-occurrence inventory in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 - <<'PY'
import re, pathlib
from firestarter.firmware import asset_candidates
cml = pathlib.Path('/workspaces/firestarter/platform/py32f071/CMakeLists.txt').read_text()
m = re.search(r'set\(HEX_FILE[^)]*?/(firestarter_[a-z0-9_]+\.hex)"', cml)
assert m, 'HEX_FILE basename did not parse -- non-vacuity guard (A-7)'
from_cmake = m.group(1)
from_host = asset_candidates('py32f071')[0]
pat = re.compile(r'^firestarter_[a-z0-9_]+\.hex$')
assert pat.match(from_cmake), from_cmake
assert pat.match(from_host), from_host
assert from_cmake == from_host, (from_cmake, from_host)
print('EQUAL', from_cmake)
PY</automated>
  </verify>
  <acceptance_criteria>
    - The verify script prints `EQUAL firestarter_py32f071.hex` and exits 0.
    - The script fails (non-zero) if the `HEX_FILE` regex finds no match — the non-vacuity guard is present and is asserted before any equality comparison.
    - A comment-stripped repo-wide grep for the hyphenated basename over `/workspaces/firestarter` returns occurrences ONLY in `.github/workflows/py32f071.yml`; the exact count and line numbers are recorded in the SUMMARY.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing (no accidental edits to another plan's files).
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
  </acceptance_criteria>
  <done>The CMake-emitted hex basename and the host's frozen `asset_candidates()` value are proven equal by a guard that cannot pass on two empty strings, and the remaining hyphenated occurrences are inventoried for the later sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware build output name -> host asset lookup | A cross-repo string contract with no compiler enforcing it; a divergence is only observable at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-12 | Spoofing | an emitted image whose name no release glob or host lookup matches, so `fw --install` silently resolves nothing | medium | mitigate | the four-site rename plus Task 2's non-vacuous equality against the live `asset_candidates("py32f071")[0]`; permanently bound by Plan 128-09's three-way cross-repo test |
| T-128-13 | Repudiation | a corrected code line shipped beside `ad47c3b`'s incorrect explanation | medium | mitigate | the final sentence is rewritten, and an acceptance criterion asserts the false phrase is absent while the corrected substrings are present |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). This plan edits one CMake file and installs nothing. |
</threat_model>

<verification>
- Comment-stripped `platform/py32f071/CMakeLists.txt`: 0 hyphenated, 4 underscored occurrences.
- The CMake-emitted hex basename equals `asset_candidates("py32f071")[0]`, proven with a per-parse non-vacuity guard.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` green.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` unchanged by this plan.
- `git -C /workspaces/firestarter status --porcelain` empty after the commit (a precondition for Plan 128-09, per F-16).
- No ARM build is possible locally. The rename's compile effect is discharged in CI by Plan 128-04's run and Plan 128-10's run A, each cited by run URL + commit SHA.
</verification>

<success_criteria>
- `TARGET_NAME`, the linker map path, `BIN_FILE` and `HEX_FILE` all use the underscored basename; nothing else in the file changed.
- The explanatory comment carries `ad47c3b`'s valid reasoning and the corrected two-`files:`-entry fact, and no longer asserts the release glob needs no new pattern.
- The emitted hex basename is proven equal to the frozen host contract by a non-vacuous check.
- The remaining hyphenated occurrences in the repo are inventoried and confined to `py32f071.yml`, which Plan 128-04 owns.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-03-SUMMARY.md` when done.
</output>
