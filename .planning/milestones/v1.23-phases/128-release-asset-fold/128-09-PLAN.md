---
phase: 128-release-asset-fold
plan: 09
type: execute
wave: 6
depends_on: ["128-08"]
files_modified:
  - firestarter_app/tests/test_py32_asset_name_host.py
autonomous: true
requirements: [REL-04]

must_haves:
  truths:
    - "D-08(b): the binding that actually connects the two repos lives in `firestarter_app`. The firmware-side check (Plan 128-06 Task 1) is a transcription; the firmware repo cannot import the host package. This module is the binding, and it is the exact sibling of the test Phase 127 landed for the flash map."
    - "D-09: a THREE-way equality — the name parsed from `firestarter/platform/py32f071/CMakeLists.txt` (what is actually emitted), the literal parsed from `firestarter/.github/workflows/beta-build.yml` (the independent transcription), and `asset_candidates(\"py32f071\")[0]` — with a SEPARATE non-vacuity assertion per parse, so a rename that makes a regex miss fails loudly instead of passing on two empty strings."
    - "D-09 / A-7: the module binds through `tests/fw_presence.py`'s `@requires_fw` and resolves every firmware path through `fw_path()`, never a hand-built relative path. `fw_path` raises `MissingScanTargetError` when the repo is present but the file is not, so a Phase-129 rename becomes a hard failure, never a silent skip."
    - "D-09 (RESOLVED, do not re-derive): `FW_ABSENT_REASON` is already entry 1 in `ALLOWED_SKIP_REASONS` and is imported rather than re-typed. **No new skip-census entry is needed** — confirm by RUNNING `tests/test_skip_census.py`, not by reading it."
    - "D-19: this is the SECOND and last commit of the phase. Every firmware commit landed first, and F-16 makes that a hard precondition — the planted-mutation test asserts `_git_porcelain(FW_ROOT) == \"\"`, so a dirty firmware tree makes this module red for a reason that has nothing to do with filenames."
    - "F-8 (ceiling, must be stated honestly): neither app CI workflow checks out the firmware sibling, so every `@requires_fw` leg SKIPS in app CI. The binding is enforced by a local run and by developer discipline. Claiming CI enforcement would be false, and `128-NONREGRESSION.md` must say so."
    - "REL-04 (binding slice only): this closes the cross-repo half. The in-workflow assertions (128-06) and run A's observation (128-10) are the other halves."
  artifacts:
    - firestarter_app/tests/test_py32_asset_name_host.py
  key_links:
    - "`fw_path('platform','py32f071','CMakeLists.txt')` -> the emitted-name parse"
    - "`fw_path('.github','workflows','beta-build.yml')` -> the transcription parse"
    - "`firestarter.firmware.asset_candidates('py32f071')[0]` -> the host contract (READ-ONLY, FROZEN)"
    - "`tests/fw_presence.py` `@requires_fw` / `FW_ROOT` / `MissingScanTargetError` -> the fail-closed cross-repo seam"
---

<objective>
Add the single app-repo commit of Phase 128: a cross-repo test module that proves the name
the firmware actually emits, the literal the firmware workflow asserts against, and the name
the host resolves are all the same string — and that the check cannot pass by parsing
nothing.

Purpose: the firmware repo cannot import the host package, so its filename check is a
transcription of a value it has no way to verify. The failure mode Criterion 4 exists to
catch — the host renames the asset while the firmware keeps emitting the old name, or the
reverse — is only detectable from this side.

**Scope boundary — this plan does NOT close REL-04.** It closes the cross-repo binding
slice. REL-04 additionally needs the CMake rename (128-03), the workflow renames (128-04),
the in-workflow string-equality and SDK-SHA assertions (128-06), and run A's observed
evidence (128-10). Do not tick REL-04 in REQUIREMENTS.md from this plan.

Output: one new test module in `firestarter_app`, and the phase's second and final commit.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-release-asset-fold/128-RESEARCH.md
@.planning/phases/128-release-asset-fold/128-PATTERNS.md
@.planning/phases/128-release-asset-fold/128-08-SUMMARY.md

**Repo:** this plan's only file lives in `/workspaces/firestarter_app` (the host sub-repo),
on branch `v1.23-py32f071-integration`. Commit **inside that sub-repo**. `/workspaces/firestarter`
is READ-ONLY for this plan.

**Hard precondition (D-19 / F-16):** `git -C /workspaces/firestarter status --porcelain` must
be empty before this module is run or committed. Plan 128-08 recorded the firmware HEAD SHA
for exactly this reason. If the firmware tree is dirty, stop — do not attribute the resulting
red to a filename mismatch (Pitfall 7).
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 128**. A source-grounding or drift-verification
pass must treat these as new, never as a rename of something that already exists.

**New files — firmware repo `firestarter/`:** `.github/actions/build-py32f071/action.yml`
(and the new `.github/actions/` directory), `scripts/check_release_assets.py`,
`tests/test_check_release_assets.py`, `tests/fixtures/clean_release_assets_all_three/`,
`tests/fixtures/planted_release_assets_missing_uno328pb/`,
`tests/fixtures/planted_release_assets_zero_byte_leonardo/`

**New files — host repo `firestarter_app/` (this plan):**
`tests/test_py32_asset_name_host.py`

**New module-level symbols in that file:** `_CMAKELISTS`, `_BETA_BUILD`,
`_parse_emitted_hex_name`, `_parse_workflow_literal`, `_assert_non_vacuous_name`,
`_git_hash_object`, `_git_porcelain`, `TestPy32AssetNameParity`,
`TestPy32AssetNameFailsClosedOnBadInput`

**New files — meta repo:** `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md`

**New env-var seam name:** `FIRESTARTER_PIO_BUILD_ROOT` (firmware side)

**New workflow input:** `rehearsal` on `beta-build.yml`

**New step ids in `beta-build.yml`:** `mode`, `arm`

**New composite-action outputs:** `hex_path`, `sdk_sha`

**New `files:` entry:** `build/py32f071/firestarter_*.hex`

**New `tag_name` form:** `rehearsal-${{ github.run_id }}`

**New fixture directory component:** `pio_build/`

**Renamed build outputs:** `firestarter_py32f071.{elf,bin,hex,map}` (underscore form)

**New README section:** `## Release integration` in `platform/py32f071/README.md`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the module scaffold — seam imports, two guarded parsers, fail-closed git helpers</name>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_py32_flash_map_host.py` — the structural analog, read in full. Lines 39-51 (imports), line 151 (the module-scope `fw_path` constant), lines 136-150 (the A-7 header comment justifying `fw_path` over a hand-built path), lines 190-200 (`_assert_non_vacuous`, whose message must contain the phrase `vacuously true` because the RED test greps for it), lines 216-244 (`_git_hash_object` / `_git_porcelain`, fail-closed via `shutil.which("git")` + `assert ... is not None`).
    - `/workspaces/firestarter_app/tests/fw_presence.py` — the whole module. Lines 77-102 define `FW_ROOT`, `FW_REPO_MARKER`, `FW_REPO_PRESENT`, `FW_ABSENT_REASON` and `requires_fw`; lines 105-140 define `MissingScanTargetError` and `fw_path`. **Everything binds at import**, so `monkeypatch.setenv("FIRESTARTER_FW_ROOT", ...)` has no effect; and `FW_ROOT` is the SIBLING repo, never the in-repo `firestarter/` package (the name-collision trap).
    - `/workspaces/firestarter_app/firestarter/firmware.py` lines 106-132 — `flash_method()` and `asset_candidates()`. READ-ONLY. FROZEN this phase.
    - `/workspaces/firestarter/platform/py32f071/CMakeLists.txt` — the shipped `HEX_FILE` line (post-Plan-128-03), the exact text the CMake parser must match.
    - `/workspaces/firestarter/.github/workflows/beta-build.yml` — the shipped `Assert the emitted asset filename (REL-04)` step (post-Plan-128-06), the exact text the workflow parser must match. Note there are TWO `EXPECTED=` assignments in the file after Plan 128-06; only one of them assigns a `firestarter_*.hex` name.
    - `/workspaces/firestarter_app/tests/conftest.py` — confirm nothing here needs changing; the firmware side has no `conftest.py` by house rule, and this module resolves its own paths.
  </read_first>
  <files>firestarter_app/tests/test_py32_asset_name_host.py</files>
  <behavior>
    - `_parse_emitted_hex_name(text)` returns the `firestarter_*.hex` basename from CMake `HEX_FILE` source text, or an empty string when there is no match.
    - `_parse_workflow_literal(text)` returns the transcribed hex literal from workflow source text when exactly one candidate matches, and an empty string when zero match.
    - `_parse_workflow_literal` raises rather than guessing when more than one distinct candidate matches.
    - `_assert_non_vacuous_name(value, source)` raises `AssertionError` whose message contains the phrase `vacuously true` when `value` is empty or does not match `^firestarter_[a-z0-9_]+\.hex$`.
    - `_git_hash_object` and `_git_porcelain` fail the suite (never skip) when `git` is not on PATH.
  </behavior>
  <action>
Create `/workspaces/firestarter_app/tests/test_py32_asset_name_host.py`.

**Module docstring.** Copy the shape of `test_py32_flash_map_host.py`'s: MIT header, a
`Phase 128 Plan 09 — D-08(b) and D-09` title line, `Requirements: REL-04 (cross-repo binding
slice only — the in-workflow assertions and the rehearsal-run evidence are separate)`, a
`Decisions covered:` list naming D-08, D-09 and D-19, a numbered `Coverage:` list with one
line per test, and an explicit statement of the F-8 ceiling: neither app CI workflow checks
out the firmware sibling, so every `@requires_fw` leg SKIPS in CI; this binding is enforced
by a local run and by developer discipline, and claiming CI enforcement would be false.

**Imports.** `from __future__ import annotations`; `re`, `shutil`, `subprocess`, `sys`,
`pathlib.Path`; `pytest`; `from firestarter.firmware import asset_candidates`;
`from tests.fw_presence import FW_ROOT, fw_path, requires_fw`. Import the seam — never
re-derive presence detection with a local `Path(...).exists()` proxy.

**Module-scope path constants**, each resolved through `fw_path()`:
`_CMAKELISTS = fw_path("platform", "py32f071", "CMakeLists.txt")` and
`_BETA_BUILD = fw_path(".github", "workflows", "beta-build.yml")`. Above them, reproduce the
A-7 header comment with these two targets named: a hand-built relative path out of `tests/`
is deliberately never constructed; `fw_path` raises `MissingScanTargetError` when the sibling
repo is present but the file is not, so a later rename becomes a hard failure at call time
rather than a silent skip; and `@requires_fw` is the ONLY skip marker this module uses,
firing only when the sibling repo itself is genuinely absent.

**Two parsers, one per source.**
`_parse_emitted_hex_name(text)` — a regex over CMake source anchored on the `HEX_FILE`
assignment, capturing the basename. Return `""` on no match; do not raise here, so the RED
tests can exercise the guard separately.
`_parse_workflow_literal(text)` — a regex over workflow source capturing a
`firestarter_<something>.hex` token assigned to the transcription step's `EXPECTED` shell
variable. Collect **all** matches. If the set of distinct matches has more than one element,
raise `AssertionError` naming them — a workflow restructure that introduces a second
candidate must fail loudly rather than have the parser silently pick the first. Return `""`
when there are zero matches.

**One shared non-vacuity guard, called once per parse.**
`_assert_non_vacuous_name(value, source)` asserts `value` is non-empty **and** matches
`^firestarter_[a-z0-9_]+\.hex$`. Its failure message must state that a parse that found
nothing (or captured whitespace) would make every downstream comparison **vacuously true**,
name the source, and cite research finding A-7. The exact phrase `vacuously true` is
load-bearing: the RED tests match on it.

**Fail-closed git helpers.** Copy `_git_hash_object(path)` and `_git_porcelain(root)`
verbatim in shape from the 127 module: resolve `git` with `shutil.which("git")` and
`assert git_bin is not None` with a message saying a missing `git` must FAIL the suite and
never be silently skipped; run list-form `subprocess.run([...], capture_output=True,
text=True, check=True)`; return the stripped stdout.

Write no test functions in this task. The parsers and guards land first so Task 2's tests
have something to exercise; run them once by hand against the real files to confirm each
returns the expected non-empty value before proceeding.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import re,pathlib,importlib.util,sys
sys.path.insert(0,'.')
m=importlib.import_module('tests.test_py32_asset_name_host')
c=m._parse_emitted_hex_name(pathlib.Path(str(m._CMAKELISTS)).read_text())
w=m._parse_workflow_literal(pathlib.Path(str(m._BETA_BUILD)).read_text())
assert c=='firestarter_py32f071.hex', c
assert w=='firestarter_py32f071.hex', w
m._assert_non_vacuous_name(c,'cmake'); m._assert_non_vacuous_name(w,'workflow')
try:
    m._assert_non_vacuous_name('','synthetic'); raise SystemExit('guard did not fire')
except AssertionError as e:
    assert 'vacuously true' in str(e), str(e)
assert m._git_porcelain(m.FW_ROOT)=='', 'firmware tree is dirty -- D-19/F-16 precondition'
print('OK', c, w)
"</automated>
  </verify>
  <acceptance_criteria>
    - `_parse_emitted_hex_name` over the real `platform/py32f071/CMakeLists.txt` returns exactly `firestarter_py32f071.hex`.
    - `_parse_workflow_literal` over the real `beta-build.yml` returns exactly `firestarter_py32f071.hex` — one match, not two (the file's second `EXPECTED=` assignment must not be captured).
    - `_assert_non_vacuous_name("", "synthetic")` raises `AssertionError` whose message contains the exact phrase `vacuously true`.
    - `_assert_non_vacuous_name("firestarter_py32f071.hex ", "synthetic")` (trailing space) also raises — the shape regex rejects a whitespace capture.
    - The module imports `FW_ROOT`, `fw_path` and `requires_fw` from `tests.fw_presence`, and contains no `Path(...).exists()` presence proxy and no module-local `pytest.mark.skipif`.
    - `_CMAKELISTS` and `_BETA_BUILD` are both built with `fw_path(...)`; the module contains no `".."` path segment.
    - `_git_hash_object` and `_git_porcelain` each contain `shutil.which("git")` and an `assert ... is not None`.
    - `_git_porcelain(FW_ROOT)` returns the empty string — the D-19 / F-16 precondition holds before any test is written.
  </acceptance_criteria>
  <done>The module's seam bindings, both guarded parsers and both fail-closed git helpers exist and are proven against the real shipped firmware files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the three-way parity class and the fail-closed RED demonstrations</name>
  <read_first>
    - The module scaffold from Task 1.
    - `/workspaces/firestarter_app/tests/test_py32_flash_map_host.py` lines 245-321 (`class TestLinkerScriptParity`, every method carrying `@requires_fw` and calling the non-vacuity guard BEFORE any comparison, with coverage item 1 running first and doing nothing else) and lines 324-389 (`class TestLinkerScriptParityFailsClosedOnBadInput`, the RED demonstrations, and the planted-mutation test at 336-380 with its blob-SHA and porcelain assertions).
    - `/workspaces/firestarter_app/tests/test_skip_census.py` line 92 (the `FW_ABSENT_REASON` import) and line 117 (its presence in `ALLOWED_SKIP_REASONS`). Confirm; do not add an entry.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-RESEARCH.md` Pattern 4 (all seven structural elements), F-16, Pitfall 6, Pitfall 7 and Pitfall 8.
  </read_first>
  <files>firestarter_app/tests/test_py32_asset_name_host.py</files>
  <behavior>
    - Parsing the real CMakeLists is non-vacuous (a dedicated test that runs first and compares nothing).
    - Parsing the real workflow is non-vacuous (a second dedicated test, D-09's "one per parse").
    - The CMake-emitted name equals the workflow literal.
    - The workflow literal equals `asset_candidates("py32f071")[0]`.
    - All three names are equal in one explicit assertion, each guarded before comparison.
    - A planted, mutated copy of the real CMakeLists (a different hex basename) makes the parity check FAIL, and the real file's blob SHA and the firmware tree's porcelain status are unchanged afterwards.
    - The CMake parser over text with no `HEX_FILE` returns empty, and the guard raises with `vacuously true`.
    - The workflow parser over text with no candidate returns empty, and the guard raises with `vacuously true`.
    - The workflow parser raises when fed text containing two distinct candidates.
  </behavior>
  <action>
Add two classes.

**`class TestPy32AssetNameParity`** — D-09's live legs. Every method carries `@requires_fw`.
Every method re-reads and re-parses (no caching across tests) and calls the non-vacuity guard
before comparing anything.

- Coverage 1, `test_cmake_parse_is_non_vacuous` — runs first, parses the real CMakeLists,
  calls the guard, compares nothing.
- Coverage 2, `test_workflow_parse_is_non_vacuous` — the same for the workflow. **Two
  separate tests, because D-09 requires one non-vacuity assertion per parse** and these are
  two independent regexes; a single shared guard would leave one parse unproven.
- Coverage 3, `test_cmake_emitted_name_matches_workflow_literal`.
- Coverage 4, `test_workflow_literal_matches_asset_candidates` — compares against
  `asset_candidates("py32f071")[0]`, and additionally asserts `asset_candidates("py32f071")`
  has length 2 with the `.hex` first, so a reordering of the host's preference list is caught
  rather than silently changing which name is bound.
- Coverage 5, `test_all_three_names_are_equal` — the explicit three-way, with all three
  guards run first.
- Coverage 6, `test_planted_mutated_cmake_name_is_detected` — the planted-mutation RED. Capture
  `real_path = _CMAKELISTS` BEFORE any monkeypatch; take `before_blob = _git_hash_object(real_path)`;
  read the real text; produce `mutated_text` by replacing the hex basename with a deliberately
  different one; assert `mutated_text != real_text` with a message saying the replacement
  target was not found if it did not change; write the plant under `tmp_path`;
  `monkeypatch.setattr(sys.modules[__name__], "_CMAKELISTS", planted_path)`; assert the
  three-way comparison now fails; then assert `_git_hash_object(real_path) == before_blob`
  with a message saying the plant must only ever be written under `tmp_path`, and assert
  `_git_porcelain(FW_ROOT) == ""` with a message saying the firmware repo is a read-only input
  to this phase.

  **Deliberate, stated deviation from the 127 analog:** put this test in the `@requires_fw`
  class, not in the no-marker RED class where the 127 module puts its equivalent. It reads
  the real firmware file and hashes it inside `FW_ROOT`, so without the sibling repo it is a
  hard error rather than an honest skip — and F-8 records that the sibling is always absent
  in app CI. Marking it is the accurate classification and it adds no new skip reason, since
  `FW_ABSENT_REASON` is already allow-listed. Write that reasoning into the test's docstring,
  and record it in the SUMMARY as a flagged surprise, together with the observation that the
  127 module's equivalent test carries no marker and may therefore behave differently in app
  CI — a finding for Phase 130 to note, not something to fix here.

**`class TestPy32AssetNameFailsClosedOnBadInput`** — the pure RED demonstrations. **None
carries `@requires_fw`.** These need no firmware sibling and are therefore the only legs of
this module that actually run in app CI (F-8).

- Coverage 7 — the CMake parser over synthetic text with no `HEX_FILE` returns `""`, and
  `pytest.raises(AssertionError, match="vacuously true")` around the guard.
- Coverage 8 — the same for the workflow parser.
- Coverage 9 — the workflow parser over synthetic text containing two *distinct* candidate
  names raises, naming both. This is what stops the parser from silently picking one when
  the workflow is restructured.
- Coverage 10 — the shape guard rejects a captured value with surrounding whitespace and one
  with an uppercase segment, proving `^firestarter_[a-z0-9_]+\.hex$` is doing work beyond a
  bare non-empty check (Pitfall 8: a guard that merely checks `is not None` is itself vacuous).

Every assertion carries an f-string message naming the values compared. Do not add an entry
to `ALLOWED_SKIP_REASONS` — D-09 resolved that (127 D-14): `FW_ABSENT_REASON` is already
entry 1 and is imported, not re-typed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_py32_asset_name_host.py -q -rs 2>&1 | tee /tmp/p128_09.txt && ! grep -q 'firestarter firmware checkout absent' /tmp/p128_09.txt && ! grep -qE '^[0-9]+ skipped| [0-9]+ skipped' /tmp/p128_09.txt && grep -qE '10 passed' /tmp/p128_09.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter_app && python3 -m pytest tests/test_py32_asset_name_host.py -q -rs` reports **10 passed, 0 skipped, 0 failed**. A green run that actually skipped is the A-7 shape and does not count (Pitfall 6).
    - The `-rs` skip report contains no line matching `firestarter firmware checkout absent`.
    - Exactly six test methods carry `@requires_fw` and exactly four do not; the four unmarked ones are all in `TestPy32AssetNameFailsClosedOnBadInput`.
    - Two distinct tests exist whose sole purpose is a non-vacuity assertion, one per parse (D-09's per-parse requirement, asserted by reading the coverage list and the method names).
    - `test_planted_mutated_cmake_name_is_detected` asserts the real file's blob SHA is unchanged AND `_git_porcelain(FW_ROOT) == ""`, and carries `@requires_fw` with the deviation reasoning in its docstring.
    - `git -C /workspaces/firestarter status --porcelain` is still empty after the pytest run — the planted copy never touched the source of truth.
    - `grep -c 'ALLOWED_SKIP_REASONS' tests/test_py32_asset_name_host.py` is `0`; `tests/test_skip_census.py` is unmodified (`git diff --name-only` does not list it).
    - The module contains no `firestarter-py32f071` (hyphenated) literal except, if present at all, inside a planted synthetic string in a RED test — and if so, that occurrence is inside `tmp_path`-bound text only.
  </acceptance_criteria>
  <done>Ten tests pass with zero skips locally: six `@requires_fw` parity legs proving the three names are one string, and four unmarked RED demonstrations proving the check cannot pass on an empty parse.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full app suite, confirm the skip census by running it, and land the phase's second commit</name>
  <read_first>
    - The completed module.
    - `/workspaces/firestarter_app/tests/test_skip_census.py` — read it, then run it. D-09 says confirm by RUNNING, not by reading.
    - `/workspaces/.planning/phases/128-release-asset-fold/128-08-SUMMARY.md` — the recorded firmware HEAD SHA, which this commit's message cites as the tree it was validated against.
    - `/workspaces/firestarter_app/pyproject.toml` `[tool.pytest.ini_options]` — so the full-suite command matches CI's.
  </read_first>
  <files>firestarter_app/tests/test_py32_asset_name_host.py</files>
  <action>
Verification and commit; no further module edits unless a check fails.

Run, in this order, and record each result in the SUMMARY:

1. `git -C /workspaces/firestarter status --porcelain` — must be empty. Record the firmware
   HEAD SHA and confirm it matches the one Plan 128-08 recorded.
2. `python3 -m pytest tests/test_py32_asset_name_host.py -q -rs` in
   `/workspaces/firestarter_app` — record the passed count and confirm the skip report is
   empty. **Record the observed pass count, not the exit code**; a green exit with skips is
   exactly the A-7 shape this milestone exists to remove.
3. `python3 -m pytest tests/test_skip_census.py -q` — must be green with no new entry. This
   is D-09's "confirm by running" instruction. If it goes red, the correct response is to
   understand why, not to add an entry.
4. `python3 -m pytest tests/ -q` — the full host suite. Record the counts. Note any
   pre-existing failure that reproduces on a clean tree without this module, and attribute it
   explicitly rather than absorbing it.
5. The repo's gate commands as CI runs them (`ruff check`, `ruff format --check`, and the
   mypy watermark tool) scoped to the new file at minimum. The devcontainer's Python is
   newer than CI's, so scope the run rather than trusting a global pass.

Then commit inside `/workspaces/firestarter_app` on `v1.23-py32f071-integration`. This is
D-19's second and final commit: the firmware commits landed first because this module parses
firmware files and asserts the firmware tree is clean. Cite that ordering and the firmware
HEAD SHA in the commit message.

Record in the SUMMARY, plainly and without hedging: **the cross-repo binding is enforced by a
local run and by developer discipline, not by app CI.** Neither app CI workflow checks out
the firmware sibling, so `@requires_fw` skips there and only the four RED demonstrations run.
`128-NONREGRESSION.md` must repeat this. Also record that nothing in this plan says anything
about whether the published image runs — this module compares filenames.
  </action>
  <verify>
    <automated>test -z "$(git -C /workspaces/firestarter status --porcelain)" && cd /workspaces/firestarter_app && python3 -m pytest tests/test_py32_asset_name_host.py tests/test_skip_census.py -q -rs 2>&1 | tee /tmp/p128_09b.txt && ! grep -q 'firestarter firmware checkout absent' /tmp/p128_09b.txt && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter status --porcelain` is empty and the HEAD SHA matches Plan 128-08's recorded value.
    - `python3 -m pytest tests/test_py32_asset_name_host.py -q -rs` shows 10 passed and an empty skip report; the observed pass count is recorded in the SUMMARY.
    - `python3 -m pytest tests/test_skip_census.py -q` is green, and `tests/test_skip_census.py` appears in no diff for this phase.
    - `python3 -m pytest tests/ -q` counts are recorded; any failure is reproduced on a clean tree without this module and attributed, or the suite is green.
    - `ruff check tests/test_py32_asset_name_host.py` and `ruff format --check tests/test_py32_asset_name_host.py` both exit 0.
    - The commit exists on `v1.23-py32f071-integration` in `/workspaces/firestarter_app`, touches exactly one file, and its message cites D-19's ordering and the firmware HEAD SHA.
    - The SUMMARY states the F-8 ceiling verbatim: the binding is enforced locally and by discipline, not by app CI.
  </acceptance_criteria>
  <done>The phase's second and final commit lands in the host repo, with the binding observed PASS-not-SKIP locally, the skip census confirmed by running it, and the CI ceiling recorded honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware repo -> host test | The host test reads files from a sibling repository it does not own; a rename there changes this test's inputs |
| host contract -> firmware transcription | A string duplicated across two repositories with no compiler enforcing agreement |
| test process -> firmware working tree | The planted-mutation test must never write into the read-only sibling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-12 | Spoofing | the host renames the asset while the firmware keeps emitting the old name (or the reverse), so `fw --install` resolves nothing | medium | mitigate | the three-way equality binds CMake output, the workflow transcription and `asset_candidates()` in one assertion, with a non-vacuity guard per parse |
| T-128-17 | Repudiation | the gate passing because every parse returned an empty string | medium | mitigate | one guard per parse, run before any comparison, asserting non-empty AND a shape regex; four unmarked RED tests prove each guard actually fires |
| T-128-23 | Repudiation | a green run that actually SKIPPED, recorded as proof (the A-7 shape, measured in this milestone: a firmware rename once flipped five gate legs PASS-to-SKIP at exit 0 with a false reason) | high | mitigate | the local run is asserted PASS-not-SKIP with `-rs`, the observed pass count is recorded rather than the exit code, and the F-8 CI ceiling is stated in both the SUMMARY and `128-NONREGRESSION.md` |
| T-128-24 | Tampering | the planted mutation writing into the read-only firmware tree | medium | mitigate | the plant is written under `tmp_path` and reached by monkeypatching the module's own constant; the real file's blob SHA and `FW_ROOT`'s porcelain status are asserted unchanged afterwards |
| T-128-25 | Tampering | a `MissingScanTargetError` downgraded to a skip by a hand-built path or a local presence proxy | medium | mitigate | every firmware path goes through `fw_path()`; `@requires_fw` is the only skip marker; an acceptance criterion asserts no `.exists()` proxy and no module-local `skipif` |
| T-128-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in either ecosystem this phase (128-RESEARCH, Standard Stack). This plan adds one stdlib+pytest test module; no dependency is added to `pyproject.toml`. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_py32_asset_name_host.py -q -rs` in `/workspaces/firestarter_app`: 10 passed, 0 skipped, skip report empty of the FW-absent reason.
- `python3 -m pytest tests/test_skip_census.py -q`: green, no new entry, file unmodified.
- `python3 -m pytest tests/ -q`: full host suite counts recorded.
- `ruff check` and `ruff format --check` clean on the new module.
- `git -C /workspaces/firestarter status --porcelain` empty before and after the run — the read-only sibling is provably untouched.
- **Ceiling, stated:** app CI does not check out the firmware sibling, so six of these ten tests skip there. The binding is enforced by this local run and by developer discipline. Nothing here says anything about whether the published image runs, boots or installs.
</verification>

<success_criteria>
- One new test module in `firestarter_app`, structurally a sibling of the Phase 127 cross-repo gate.
- A three-way equality across CMake output, workflow transcription and `asset_candidates("py32f071")[0]`, with a separate non-vacuity assertion per parse.
- Four unmarked RED demonstrations proving both guards fire and the workflow parser refuses to guess between two candidates.
- Observed 10 passed / 0 skipped locally, with the count recorded rather than the exit code.
- No new `ALLOWED_SKIP_REASONS` entry; the census confirmed by running it.
- D-19 satisfied: this is the second and final commit, landed after a clean firmware tree at a recorded SHA.
</success_criteria>

<output>
Create `.planning/phases/128-release-asset-fold/128-09-SUMMARY.md` when done.
</output>
