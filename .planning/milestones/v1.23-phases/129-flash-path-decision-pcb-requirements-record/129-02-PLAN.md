---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - firestarter/tests/test_flash_path_record_sync.py
autonomous: true
requirements: [PCB-01, PCB-02, PCB-03, PCB-04, PCB-05]

must_haves:
  truths:
    - "Every content assertion that will judge the two records exists BEFORE either record is written, and is observed RED on arrival."
    - "The RED takes the form of MissingScanTargetError under a present meta repo — the hard-failure half of the split — never a skip and never a vacuous pass."
    - "Each shared section has its own non-vacuity leg, per copy, so partial progress is visible and no comparison can pass on two empty strings."
    - "The planted-mutation leg mutates only a tmp_path copy, and proves it by re-hashing the real file's blob afterwards."
  artifacts:
    - firestarter/tests/test_flash_path_record_sync.py
  key_links:
    - "_meta_doc() routes through meta_presence.meta_path('.planning', 'v1.23-FLASH-PATH-DECISION.md') so a missing record is a hard error, never a skip."
    - "Every content gate is parametrized over the copy it reads ('meta' / 'fw' / 'readme'), so the RED ledger discharges one plan at a time."
    - "The exact gated literals L1 (non-retirement), L2 (ship gate) and L3 (socket instruction) are defined here and reproduced verbatim by plans 129-03, 129-05 and 129-06."
---

<objective>
Add the D-03 gate's **parity and content half** to `firestarter/tests/test_flash_path_record_sync.py`,
while neither record exists yet. These 31 legs are RED-by-construction on arrival, and that RED is
the phase's central piece of evidence: Phase 123's doctrine is that a gate authored after the
content it judges can only bless what already happened.

Purpose: pin the exact wording the two records must carry — the three literal sentences, the
checklist row shape, the reserved-address needles, the bootloader-figure proximity rule — before
anyone writes prose. A document whose exact wording is load-bearing cannot be gated by criteria
invented afterwards to fit it.

Output: the module grows from 10 legs to 41; the suite goes from `190 passed` to
`31 failed, 190 passed`, with the failure list recorded verbatim as this plan's deliverable.

**This plan may NOT mark any requirement Complete.** PCB-01…PCB-05 appear in `requirements`
because these legs gate all five, but only the closing plan `129-09` may tick them in
`.planning/REQUIREMENTS.md`. Do not edit `REQUIREMENTS.md` or `ROADMAP.md`.

**Repo scope (D-04):** the firmware repo only. `firestarter_app` is READ-ONLY — cite it as an
analog, never edit it, and leave its unrelated uncommitted local changes alone.

**Standing rule:** no task in this plan runs `git push` or `gh workflow run`.

<!-- planner-discipline-allow: no VTOR -->
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the full phase-wide list of new files,
new symbols and new document anchors. Nothing in that list exists in the tree before Phase 129.

**New symbols added by THIS plan** (all inside `firestarter/tests/test_flash_path_record_sync.py`):
`_meta_doc()` · `_fw_doc_text()` · `_readme_text()` · `_linker_text()` · `_seed_text()` ·
`_copy_text(copy_id)` · `_frontmatter(text)` · `_checklist_rows(section_body)` ·
`_L1_NON_RETIREMENT` · `_L2_SHIP_GATE` · `_L3_SOCKET_EMPTY` · `_S1_NEEDLES` · `_S2_NEEDLES` ·
`_S2_UNDECIDED_NEEDLES` · `_S3_NEEDLES` · `_S3_FIGURE_RE` · `_S3_COST_TOKENS` · `_S4_NEEDLES` ·
`_S5_NEEDLES` · `_LINKER_NEEDLES` · `_LINKER_FORBIDDEN_RE` · class `TestFlashPathRecordSync` with
`test_meta_extract_is_non_vacuous`, `test_fw_extract_is_non_vacuous`, `test_shared_sections_match`,
`test_three_tiers_and_non_retirement`, `test_pcb_checklist_rows_are_wellformed`,
`test_flash_budget_cites_reserved_map`, `test_bootloader_figure_carries_its_cost`,
`test_vid_pid_decision_and_ship_gate`, `test_socket_empty_instruction_present`,
`test_linker_comment_cross_references_record`, `test_seed_status_is_no_longer_dormant`,
`test_planted_mutation_of_the_real_subset_is_detected`.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-VALIDATION.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the gated literals, needle sets and accessors</name>
  <files>firestarter/tests/test_flash_path_record_sync.py</files>
  <read_first>
    - `firestarter/tests/test_flash_path_record_sync.py` — the module as `129-01` left it: the docstring, `_SHARED_KEYS`, `_extract_shared_section`, `_shared_sections`, `_assert_non_vacuous`, `_git_hash_object`, `_git_porcelain`, and the fail-closed class.
    - `firestarter/tests/test_config_storage_design_vendored.py` lines 76–94 — the needle-tuple convention: module-level constants named for the requirement that demands them.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §F-1 (the reserved flash map), §F-3 (the sector-quantised budget), §F-5/F-9/F-10/F-11 (the PCB facts), §F-6 (pid.codes and `1209:0001`), §F-13 (why the socket warning is stronger here), §C-1, §C-2, §C-4.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-VALIDATION.md` §"Per-Task Verification Map" — the requirement-level contract each leg lands against.
  </read_first>
  <behavior>
    - `_copy_text("meta")` routes through `meta_path(".planning", "v1.23-FLASH-PATH-DECISION.md")`, so a missing record under a present meta repo raises `MissingScanTargetError`.
    - `_copy_text("fw")` asserts `_FW_DOC.exists()` with a message naming the resolved absolute path, then returns its text — a missing subset is an AssertionError, never a skip.
    - `_frontmatter(text)` returns an ordered mapping of the YAML block between the first two `---` lines, and raises `AssertionError` containing `refusing to guess` when the opening `---` is not line 1 or the closing `---` is absent.
    - `_checklist_rows(body)` returns a list of `(row_id, title, why_line, breaks_line)` tuples, raising `AssertionError` when a row is not followed by both of its two required lines.
  </behavior>
  <action>
Extend the module in place. Do not rewrite the fail-closed half, and do not add a second
extractor — every new leg goes through the existing `_extract_shared_section` /
`_shared_sections` / `_assert_non_vacuous` trio (the single-helper rule, PATTERNS §3a).

Update the module docstring's `Coverage:` block to list the new tests too, and add a paragraph
titled `Expected-RED ledger on arrival` stating that all 31 legs added by Plan 02 are RED when
committed, that the RED for `meta`-side legs is `MissingScanTargetError` (not a skip), and naming
which later plan discharges each group: `129-03` → S1; `129-07` → the linker leg; `129-04` → S2
and S3; `129-05` → S4 and S5; `129-06` → every `fw` and `readme` parametrisation, all five
`shared_sections_match` legs and the planted-mutation leg; `129-08` → the seed leg.

**Three exact literals**, as module constants. Every character matters — these are what plans
129-03/05/06 must reproduce verbatim. Use U+2014 EM DASH (`—`), never a double hyphen, wherever an
em dash appears:

- `_L1_NON_RETIREMENT` = `Landing the factory USB DFU path in v1.23 does not retire the self-flash bootloader seed.`
- `_L2_SHIP_GATE` = `Ship gate: no PY32F071 board ships, and no release advertises a USB identity, until a PID allocated under VID 0x1209 exists.`
- `_L3_SOCKET_EMPTY` = `Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.`

Each constant carries a comment naming the requirement it discharges (PCB-01, PCB-04, PCB-05
respectively) and stating that the assertion is a plain substring test, so the records may wrap
the sentence in `**` bold markers without breaking it.

**Needle tuples**, one module constant per shared section, each with a comment naming its source
finding:

- `_S1_NEEDLES` (PCB-01, RESEARCH §"Three-Tier Flash Path"): `self-flash bootloader`, `CDC`,
  `COBS`, `factory USB DFU`, `SWD`, `intended primary`, `maintainer/manufacturing recovery`,
  `last resort`.
- `_S2_NEEDLES` (PCB-02, F-5/F-8/F-9/F-10/F-11): `PF8`, `nBOOT1`, `PA13`, `PA14`, `nRST`, `PB0`,
  `PB7`, `LQFP64`, `CSP64`, `QFN64`, `LQFP48`, `QFN48`, `QFN56`, `QFN32`, `HSE`, `PA4`, `ADC`,
  `PA11`, `PA12`, `1.5 kΩ`.
- `_S2_UNDECIDED_NEEDLES` (CONTEXT §Specifics "the record should state its own edges"): `socket`,
  `ZIF`, `connector`, `power budget`.
- `_S3_NEEDLES` (PCB-03, F-1/F-3/C-1/C-4): `0x08000000`, `0x0801DFFF`, `0x0801E000`, `0x0801E100`,
  `0x08020000`, `120K`, `8K`, `256`, `8192`, `Sector 15`, `__config_page_size`,
  `__config_slot_a_start`, `__config_slot_b_start`, `__config_region_end`, `24 KiB`, `3 sectors`,
  `14.6 KiB`, `27,372`, `192 B`, `__VTOR_PRESENT`, `SCB->VTOR`.
- `_S3_FIGURE_RE` — a compiled regex matching the bootloader reservation figure in either of its
  two written forms: a `24` followed by optional whitespace and `KiB`, or a `3` followed by
  whitespace, an optional `whole`, whitespace and `sectors`. Case-insensitive.
- `_S3_COST_TOKENS` (D-10) — `ORIGIN`, `migration`, `re-flash`. At least one must appear within a
  two-line window either side of every `_S3_FIGURE_RE` match.
- `_S4_NEEDLES` (PCB-04, C-2/F-6/F-7/F-12/F-17): `0x1209`, `1209:0001`, `pid.codes`, `0x36B7`,
  `0xFFFF`, `Puya Semiconductor`, `usbd_cdc_if.c`, `pycdc.inf`,
  `0ed2f4b4d3391eccfd4491006a30295fd78e32c2`, `0x0448`, `py32_dfu.py`, `0xFE/0x01`.
- `_S5_NEEDLES` (PCB-05, F-13): `provisional`, `RURP_PY32F071_PINMAP_PROVISIONAL`, `direction`,
  `BOOT0`, `three board revisions`.
- `_LINKER_NEEDLES` (D-11, C-1): `FLASH-PATH-AND-PCB.md`, `v1.23-FLASH-PATH-DECISION.md`,
  `__VTOR_PRESENT`, `SCB->VTOR`, and the region line `BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0`.
- `_LINKER_FORBIDDEN_RE` — a case-insensitive compiled regex for the two-word clause the C-1
  correction removes: the word `no`, one or more whitespace characters, then `VTOR`. Build it from
  the pattern string `r"no\s+VTOR"`. Its comment must record that this clause is factually false
  (the part declares `__VTOR_PRESENT 1` and the compiled `SystemInit` writes `SCB->VTOR` at every
  boot, RESEARCH C-1) and that the record — not this repo's `REQUIREMENTS.md` or `ROADMAP.md` —
  is where the correction is stated, because Phase 130's CLOSE-01 sweep owns that prose.

**Accessors**, each with a one-line docstring:

- `_meta_doc()` → `meta_path(".planning", "v1.23-FLASH-PATH-DECISION.md")`. Never a string
  concatenation onto `META_ROOT`; the point of routing through `meta_path` is that a missing
  target under a present meta repo raises `MissingScanTargetError` instead of skipping.
- `_fw_doc_text()`, `_readme_text()`, `_linker_text()` — assert `.exists()` on `_FW_DOC`, on
  `_FW_REPO_ROOT / "platform" / "py32f071" / "README.md"` and on `_LINKER` respectively, with a
  message naming the resolved absolute path and stating that a missing target must fail the suite,
  never be skipped; then return `.read_text()`.
- `_seed_text()` → reads `meta_path(".planning", "seeds", "py32f071-no-external-tool-fw-install.md")`.
- `_copy_text(copy_id)` — dispatches `"meta"` → `_meta_doc().read_text()`, `"fw"` →
  `_fw_doc_text()`, `"readme"` → `_readme_text()`; any other id is an `AssertionError`.
- `_frontmatter(text)` — splits the YAML block delimited by the first two lines equal to `---`,
  requiring the opening delimiter on line 1; returns a dict of the top-level `key: value` pairs in
  source order. On a malformed block, raise `AssertionError` containing `refusing to guess`.
- `_checklist_rows(body)` — finds every line matching a row header of the form
  `- [ ] **R<digit> — <title>` and, for each, requires the next two non-blank lines to begin
  (after exactly two leading spaces) with `- *Why:*` and `- *Breaks if omitted:*` in that order,
  each followed by at least twenty characters of text. Returns the parsed tuples; raises
  `AssertionError` naming the offending row id when the shape is violated. Its docstring cites
  D-16 (checkbox + one line of rationale + one line of what breaks) and says the shape exists so
  Phase 130's CLOSE-02 honesty ledger can cite specific rows.
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python -c "import ast,sys; ast.parse(open('tests/test_flash_path_record_sync.py').read())"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; assert m._L1_NON_RETIREMENT == 'Landing the factory USB DFU path in v1.23 does not retire the self-flash bootloader seed.'"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; assert m._L2_SHIP_GATE == 'Ship gate: no PY32F071 board ships, and no release advertises a USB identity, until a PID allocated under VID 0x1209 exists.'"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; assert '—' in m._L3_SOCKET_EMPTY and m._L3_SOCKET_EMPTY.endswith('the PROM socket must be empty.')"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; assert m._LINKER_FORBIDDEN_RE.search('on a part with no  VTOR') is not None and m._LINKER_FORBIDDEN_RE.search('__VTOR_PRESENT 1') is None"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; assert len(m._S3_NEEDLES) >= 21 and len(m._S2_NEEDLES) >= 20 and len(m._S4_NEEDLES) >= 12"` exits 0.
    - `python -c "from tests import test_flash_path_record_sync as m; import pytest; m._meta_doc()"` raises `MissingScanTargetError` (the record does not exist yet) — confirm the exception type by name in the traceback, not merely a non-zero exit.
    - `grep -c '_extract_section' tests/test_flash_path_record_sync.py` returns `0` — no second extractor was introduced.
    - The ten fail-closed legs from `129-01` still pass: `python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSyncFailsClosed -q` reports `10 passed`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python -c "
from tests import test_flash_path_record_sync as m
from tests.meta_presence import MissingScanTargetError
assert m._L1_NON_RETIREMENT.startswith('Landing the factory USB DFU path')
assert m._L2_SHIP_GATE.startswith('Ship gate: no PY32F071 board ships')
assert '—' in m._L3_SOCKET_EMPTY
assert m._LINKER_FORBIDDEN_RE.search('a part with no  VTOR')
assert not m._LINKER_FORBIDDEN_RE.search('__VTOR_PRESENT 1')
try:
    m._meta_doc(); raise SystemExit('expected MissingScanTargetError')
except MissingScanTargetError:
    pass
" && python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSyncFailsClosed -q 2>&1 | tail -1 | grep -q '^10 passed'</automated>
  </verify>
  <done>The three exact literals, eight needle sets, the forbidden-clause regex and every accessor exist; `_meta_doc()` hard-fails on the not-yet-written record; the fail-closed half is still 10/10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TestFlashPathRecordSync — 31 parity and content legs, RED on arrival</name>
  <files>firestarter/tests/test_flash_path_record_sync.py</files>
  <read_first>
    - `firestarter/tests/test_flash_path_record_sync.py` — the constants and accessors from Task 1.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md` §3b — the two-class split, the per-parse non-vacuity rule, and the full planted-mutation ceremony including the `assert mutated_text != real_text` line PATTERNS calls the most-often-dropped one.
    - `firestarter_app/tests/test_py32_asset_name_host.py` lines 195–198 and 265–332 (READ-ONLY, D-04) — the class docstrings and the mutation ceremony in full.
  </read_first>
  <behavior>
    - Every leg re-reads and re-parses its inputs; nothing is cached across tests.
    - `_assert_non_vacuous` is called on every parse before any comparison, in its own test.
    - The planted-mutation leg writes only under `tmp_path`, monkeypatches the module's own `_FW_DOC` constant, and afterwards asserts the real file's blob SHA is unchanged and the firmware working tree is clean.
  </behavior>
  <action>
Add `class TestFlashPathRecordSync` to the module. Its docstring: the live legs; every method
carries `@requires_meta`; every method re-reads and re-parses (no caching) and calls the
non-vacuity guard before comparing anything; and the explicit non-closure sentence — this class
gates PCB-01…PCB-05 mechanically but does **NOT** close any of them, and no requirement is marked
complete in `REQUIREMENTS.md` from this plan (the Phase 116 4× premature-tick guard, in its most
copyable form, PATTERNS §S4).

Twelve test functions, 31 collected legs:

1. `test_meta_extract_is_non_vacuous` — parametrized over `_SHARED_KEYS` (5 legs). Extract the
   key from the meta copy and pass it to `_assert_non_vacuous`. No comparison in this test.
2. `test_fw_extract_is_non_vacuous` — parametrized over `_SHARED_KEYS` (5 legs). Same, for the
   firmware subset. A separate test per parse is what F-14 mode 2 requires; do not merge these
   two into one loop.
3. `test_shared_sections_match` — parametrized over `_SHARED_KEYS` (5 legs). Assert non-vacuity of
   BOTH parses first, then assert the two bodies are equal. On failure the message must name the
   key, the first differing line number, and both differing lines — a bare `assert a == b` on two
   multi-hundred-line strings is unactionable.
4. `test_three_tiers_and_non_retirement` — parametrized over `("meta", "fw")` (2 legs). Extract
   `S1`; assert non-vacuity; assert every `_S1_NEEDLES` entry is present; assert
   `_L1_NON_RETIREMENT` is a substring. PCB-01.
5. `test_pcb_checklist_rows_are_wellformed` — parametrized over `("meta", "fw")` (2 legs).
   Extract `S2`; assert non-vacuity; call `_checklist_rows` and assert exactly seven rows with ids
   `R1`…`R7`, each appearing exactly once and in ascending order; assert every `_S2_NEEDLES` entry
   is present in the section; assert the section contains a line beginning with
   `### Deliberately undecided` and that all four `_S2_UNDECIDED_NEEDLES` appear inside that
   subsection's span (not merely somewhere in `S2`). PCB-02 / D-14 / D-16 / F-10.
6. `test_flash_budget_cites_reserved_map` — parametrized over `("meta", "fw")` (2 legs). Extract
   `S3`; assert non-vacuity; assert every `_S3_NEEDLES` entry is present, reporting the full list
   of missing needles rather than the first. PCB-03 / F-1 / F-3 / C-1 / C-4.
7. `test_bootloader_figure_carries_its_cost` — parametrized over `("meta", "fw")` (2 legs). D-10's
   proximity gate. Split `S3` into lines; for every line matching `_S3_FIGURE_RE`, require at
   least one `_S3_COST_TOKENS` entry within the window of two lines either side. Assert at least
   one match exists first, so the gate can never pass because the figure is absent — that is the
   vacuous shape A-7 measured. Failure message names the offending line number and its text.
8. `test_vid_pid_decision_and_ship_gate` — parametrized over `("meta", "fw")` (2 legs). Extract
   `S4`; assert non-vacuity; assert every `_S4_NEEDLES` entry is present; assert `_L2_SHIP_GATE`
   is a substring. PCB-04 / C-2 / F-6 / F-7.
9. `test_socket_empty_instruction_present` — parametrized over `("meta", "fw", "readme")` (3
   legs). For `meta` and `fw`, extract `S5`, assert non-vacuity, assert `_L3_SOCKET_EMPTY` is a
   substring and every `_S5_NEEDLES` entry is present. For `readme`, assert `_L3_SOCKET_EMPTY` is
   a substring of the whole README text and that the README also contains the string
   `FLASH-PATH-AND-PCB.md` — the README carries the instruction and a pointer, not a fourth copy
   of the reasoning. PCB-05.
10. `test_linker_comment_cross_references_record` (1 leg). Read `_linker_text()`; assert every
    `_LINKER_NEEDLES` entry is present; assert `_LINKER_FORBIDDEN_RE.search(...)` is `None`, with
    a failure message that names the matched span and cites RESEARCH C-1 as the reason the clause
    is false. Also assert the `BOOTLOADER` comment block itself is non-empty by extracting the
    lines from the `MEMORY` opening brace to the `BOOTLOADER (rx)` line and passing that span to
    `_assert_non_vacuous` — a file-wide substring search that silently matched nothing would be
    the vacuous shape. D-11 / C-1.
11. `test_seed_status_is_no_longer_dormant` (1 leg). Read `_seed_text()`; call `_frontmatter`;
    assert its key set is exactly `title`, `trigger_condition`, `planted_date`, `status` in that
    order — the seed format is a fixed four-field schema and D-17 must work within it, not extend
    it (PATTERNS §6); assert the `status` value, lowercased and stripped, is not the single word
    the ten seeds currently all carry (build that comparison word at runtime from the characters
    of the seed's own historical value rather than embedding it as a literal in the assertion, or
    read the expected-forbidden value from the module constant defined for it); assert the body
    contains the relative markdown link target `../v1.23-FLASH-PATH-DECISION.md`; assert the body
    contains `FUT-N05`. D-17 / D-18.
12. `test_planted_mutation_of_the_real_subset_is_detected` (1 leg). The full PATTERNS §3b
    ceremony, in this order: capture `_FW_DOC` into a local BEFORE any monkeypatch; hash it with
    `_git_hash_object`; read its real text; produce a mutated copy by replacing the first
    occurrence of `24 KiB` inside its `S3` body with `8 KiB`; assert the mutated text differs from
    the real text, with the message that the replacement target string was not found and the
    record's wording may have changed; write the mutated text under `tmp_path`;
    `monkeypatch.setattr(sys.modules[__name__], "_FW_DOC", planted_path)`; assert
    `_extract_shared_section` of `S3` from the planted copy differs from the meta copy's `S3`;
    then assert `_git_hash_object` of the captured real path is unchanged, and
    `_git_porcelain(_FW_REPO_ROOT)` is empty. F-14 mode 1 against the real artifact.

Every leg carries `@requires_meta`. No leg writes outside `tmp_path`.

After adding the class, run the suite and capture the failure list verbatim. Do NOT attempt to
make any of the 31 legs pass in this plan — writing the records is what discharges them, and the
recorded RED is this plan's deliverable.

<!-- planner-discipline-allow: no VTOR -->
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py --collect-only -q | tail -1` reports `41 tests collected`.
    - `python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSyncFailsClosed -q` reports `10 passed`.
    - `python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSync -q 2>&1 | tail -1` reports `31 failed` and reports no `passed` and no `skipped` count.
    - `python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSync -q 2>&1 | grep -c MissingScanTargetError` returns at least `20` — the meta-side RED is the hard-failure half of the split, not a skip.
    - `python -m pytest tests/ -q 2>&1 | tail -1` reports `31 failed, 190 passed`.
    - The verbatim list of the 31 failing node ids is recorded in this plan's SUMMARY, grouped by the plan that will discharge each group.
    - `git -C /workspaces/firestarter status --porcelain` lists only `tests/test_flash_path_record_sync.py` as modified — no fixture escaped `tmp_path`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py --collect-only -q 2>&1 | tail -1 | grep -q '^41 tests collected' && python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSyncFailsClosed -q 2>&1 | tail -1 | grep -q '^10 passed' && python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSync -q 2>&1 | tail -1 | grep -q '^31 failed' && python -m pytest tests/test_flash_path_record_sync.py::TestFlashPathRecordSync -q 2>&1 | grep -c MissingScanTargetError | awk '$1>=20{exit 0} {exit 1}'</automated>
  </verify>
  <done>41 legs collected; the 10 fail-closed legs pass; the 31 new legs fail with `MissingScanTargetError` on the meta side; the failure list is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the parity half, RED and labelled as such</name>
  <files>firestarter/tests/test_flash_path_record_sync.py</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7.
  </read_first>
  <action>
Commit inside the firmware submodule, on `v1.23-py32f071-integration`. Verify the branch name
immediately before and immediately after.

Commit message subject: `test(129-02): flash-path record parity and content gates, RED before
either record exists`. The body must state, in plain terms, that the suite is deliberately RED at
this commit; give the exact counts (`31 failed, 190 passed`); name the discharging plans per
group; and cite Phase 123's doctrine that a gate authored after the content it judges can only
bless what already happened. Anyone bisecting must be able to tell this RED apart from a
regression by reading the commit message alone.

Do not bump the meta gitlink (plan `129-09` owns the single in-phase bump, D-05). No task runs
`git push` or `gh workflow run`.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter log -1 --format=%s` starts with `test(129-02):`.
    - `git -C /workspaces/firestarter log -1 --format=%b` contains the literal `31 failed, 190 passed` and the literal `129-06`.
    - `git -C /workspaces/firestarter show --stat --format= HEAD` lists exactly one file: `tests/test_flash_path_record_sync.py`.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes after the commit.
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs `v1.23-py32f071-integration`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && git log -1 --format=%s | grep -q '^test(129-02):' && git log -1 --format=%b | grep -q '31 failed, 190 passed' && test "$(git show --stat --format= HEAD | grep -c 'tests/test_flash_path_record_sync.py')" = "1" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The RED parity half is committed with its RED state explained in the commit body, the tree is clean, and no outward-facing command ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta repo → firmware repo | The gate reads a document in another repository over a path that does not exist in every checkout. |
| record prose → future reader | A durable record is trusted by a schematic author who will not re-derive its claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-02 | Tampering (undetected) | the 31 content and parity legs | high | mitigate | One non-vacuity assertion per parse in its own test; the proximity gate asserts at least one figure match exists before checking adjacency; every needle failure reports the full missing list. |
| T-129-06 | Tampering | subset/authoritative divergence | high | mitigate | Five per-key `test_shared_sections_match` legs plus a planted-mutation leg against the real subset, with blob-SHA-unchanged and porcelain-clean assertions afterwards. |
| T-129-03 | Repudiation / Information disclosure | the record's wording | high | mitigate | The gate pins the three exact sentences and the reserved-address needles before prose is written, so wording cannot be relaxed to fit a draft. |
| T-129-04 | Elevation of Privilege | any task in this plan | critical | mitigate | Structural separation: no `git push`, no `gh workflow run`, no PR filing. Not a checkpoint — `--auto`/`--chain` auto-approve those. |
| T-129-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan and no dependency is added to any manifest. RESEARCH §"Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase. |
</threat_model>

<verification>
- `python -m pytest tests/test_flash_path_record_sync.py --collect-only -q` → `41 tests collected`.
- `python -m pytest tests/ -q` → `31 failed, 190 passed`, zero skipped.
- The 31 failing node ids are listed verbatim in the SUMMARY, grouped by discharging plan.
- `git -C /workspaces/firestarter status --porcelain` → empty; branch unchanged.
</verification>

<success_criteria>
- The gate module carries all 41 legs, with the parity/content class RED by construction.
- Every meta-side RED is a `MissingScanTargetError`, proving the present-repo-missing-target path fires rather than skipping.
- The three exact literals and eight needle sets are fixed in code before any record prose exists.
- No requirement ticked; `REQUIREMENTS.md` and `ROADMAP.md` untouched.
- No `git push`, no `gh workflow run`, no edit under `firestarter_app/`.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-02-SUMMARY.md` when done.
Record verbatim: the collected count, the two pytest summary lines, the full 31-node RED ledger
grouped by discharging plan, and the commit SHA.
</output>
