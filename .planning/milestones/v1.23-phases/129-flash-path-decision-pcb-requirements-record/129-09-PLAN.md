---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 09
type: execute
wave: 9
depends_on: ["129-08"]
files_modified:
  - .planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - firestarter
autonomous: true
requirements: [PCB-01, PCB-02, PCB-03, PCB-04, PCB-05]

must_haves:
  truths:
    - "Every claim this phase makes was re-executed in the closing session against the live trees, not accepted from any prior plan's SUMMARY."
    - "129-NONREGRESSION.md exists, passes the milestone claim gate when scanned directly, and does not trip that gate by quoting its own forbidden phrases."
    - "ROADMAP criterion 3 is recorded as AMENDED, with the reason, the corrected statement and the owner of the outstanding prose correction all named."
    - "All eighteen decisions D-01…D-18 are individually accounted for, including the two the operator escalated during planning and the four research corrections."
    - "PCB-01…PCB-05 are ticked in REQUIREMENTS.md by this plan and by no other, each against a row re-executed in this session."
    - "The meta gitlink for firestarter is bumped in-phase to the firmware tip this phase produced (D-05)."
  artifacts:
    - .planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md
  key_links:
    - "The gitlink bump is asserted equal to the firmware HEAD recorded by 129-07, so the meta record and the submodule cannot disagree."
    - "check_permitted_claims.py is run with explicit argv against all three durable artifacts, so its arming logic and its default-target list are bypassed and nothing is scanned vacuously."
    - "The D-13 byte-identity sequence is re-run on the FINAL tree, not inherited from 129-07's SUMMARY."
---

<objective>
Close the phase: re-execute every row in this session, write `129-NONREGRESSION.md`, tick
PCB-01…PCB-05, and bump the meta gitlink.

Purpose: this is the only plan permitted to tick a requirement, and the only artifact a later reader
or Phase 130's honesty ledger will trust without re-deriving. Its value comes entirely from the
re-execution: every exit code, count, digest and literal is checked against the live trees as they
now stand, and where a prior plan's SUMMARY made a claim, this document re-checks it independently
and says so.

Output: one evidence artifact, five ticked requirements, an updated ROADMAP plan list, and one meta
commit carrying the gitlink bump.

**This IS the closing plan.** It is the only plan in Phase 129 permitted to mark PCB-01, PCB-02,
PCB-03, PCB-04 and PCB-05 Complete in `.planning/REQUIREMENTS.md`. It may tick **only** those five
ids and no others.

**Repo scope (D-04):** the meta repo, plus the gitlink pointing at `firestarter`. No file inside
either submodule is edited. `firestarter_app` is untouched and its gitlink is **not** bumped — it
carries unrelated uncommitted local changes that must be left alone.

**Standing rule:** no task in this plan runs `git push` or `gh workflow run`. D-13 forbids a CI run
this phase; the ARM evidence is the local sequence, re-run here.

<!-- planner-discipline-allow: no VTOR -->
<!-- planner-discipline-allow: dormant -->
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the phase-wide list of new files, new
symbols and new document anchors — all of them created by Phase 129 and absent from the tree before
it.

**Created by THIS plan:**
`.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md`, carrying
the heading anchors `## 1. The claim, as precise statements`,
`## 2. Locally provable, executed now` (with per-repo subsections
`### Firmware repo`, `### Meta repo`, `### ARM byte-identity row`),
`## 3. Deliberately empty — no CI dispatch, no operator gate`,
`## 4. Success criteria`, `## 5. Decision coverage — D-01…D-18`,
`## 6. Precedent and prior art`, `## 7. What this phase does NOT claim`,
`## 8. Deviations recorded during planning and execution`, `## Claim ceiling`, `## Sweep Summary`.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.23-FLASH-PATH-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every row in this session, including the D-13 sequence on the final tree</name>
  <files>(no files written; evidence captured for Task 2)</files>
  <read_first>
    - All eight prior SUMMARY files in this phase directory (`129-01-SUMMARY.md` … `129-08-SUMMARY.md`) — read every claim they make, so each can be re-checked independently rather than copied.
    - `.planning/ROADMAP.md` §"Phase 129" — the five success criteria verbatim; they are quoted in Task 2 and must be transcribed from the file, not from memory.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — its argv handling, so it is invoked with explicit targets rather than relying on its default-target arming logic.
    - `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md` and `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md` — the two analogs whose shapes Task 2 combines.
  </read_first>
  <action>
Execute every check below **in this session**, capture each command and its full output verbatim, and
note for each whether it confirms or contradicts a prior SUMMARY's claim. Nothing in Task 2 may be
copied from a SUMMARY without an independent re-check recorded here.

**Repository state.** Branch name and HEAD for the meta repo and for `firestarter`; `git status
--porcelain` for both (both must be empty at the start of this plan). Confirm the firmware HEAD
equals the value `129-07`'s SUMMARY recorded. Do **not** gate on `firestarter_app`'s porcelain — it
carries unrelated local changes and is read-only this phase (D-04); record that fact instead.

**Firmware suite.** `python -m pytest tests/ -q -rs` from `/workspaces/firestarter`. Expected
`221 passed`, zero failed, zero skipped. Record the exact summary line. Then
`python -m pytest tests/test_flash_path_record_sync.py -v` and record that all 41 legs pass, listing
the ten fail-closed leg names explicitly — those are the legs that make the gate a gate rather than
a comment, and a reader must be able to see them by name.

**Fail-closed re-demonstration.** Re-run the subprocess leg that points `FIRESTARTER_META_ROOT` at
an empty directory and confirm from its captured output that the skip reason names the resolved
absent marker path. Re-run the present-root-missing-target leg and confirm the exception type is
`MissingScanTargetError`. These two are the phase's answer to the four fail-open gate breakages this
milestone has already paid for; re-confirm them rather than citing them.

**Record structure.** For `.planning/v1.23-FLASH-PATH-DECISION.md`: the count of `## ` headings, the
five `[SHARED:Sn]` marker lines, the exactly-seven checklist row headers with their seven `*Why:*`
and seven `*Breaks if omitted:*` lines, the presence of `### Deliberately undecided` with its four
named items, and the three exact literals (the non-retirement sentence, the ship gate, the socket
instruction) each present exactly once. For
`firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md`: the five marker lines, zero numbered `## `
headings, and its own `## Claim ceiling`.

**Negative greps.** On both records: zero matches for the no-VTOR-workaround terms
(`MEM_MODE`, trampoline, RAM vector copy), and zero matches for the claim gate's validation-claim and
silicon-claim phrase shapes. On the linker script: zero matches for the two-word clause C-1 removed,
and exactly one match for the unchanged `BOOTLOADER` region line.

**Claim gate, with explicit targets.** Run
`python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py`
with all three durable artifacts named on the command line: the meta record, the firmware subset,
and — after Task 2 has written it — `129-NONREGRESSION.md`. Explicit argv bypasses the checker's
default-target arming logic, so a missing file is a hard failure rather than an `UNARMED` exit 0.
Record the exit code and the full `PASS:` output. In this task, run it against the two records; Task
2 re-runs it against all three.

**D-13, re-run on the final tree.** Do not inherit `129-07`'s digests. In the scratch directory
outside both working trees: rebuild, capture SHA-256 of the `.bin` and `.hex` and the
`arm-none-eabi-size` row, then re-apply and revert a throwaway comment change to the linker script
**inside the scratch copy's dependency chain only** — that is, make the change in the real file,
rebuild, capture, then restore the file exactly and confirm `git status --porcelain` is empty again.
Confirm from the build log that the link step re-ran on each build, and that the digests are
identical across the pair. If the working tree is not clean after the restore, stop and report; a
dirty tree at the closing plan is a real problem, not a rounding error.
Record the toolchain versions again — the delta claim's terms include them — and record, as its own
sentence, that these absolute figures may not be compared against any CI figure and that a
byte-identical image is not evidence that the image runs.

**ROADMAP criteria.** Transcribe all five Phase 129 success criteria from `.planning/ROADMAP.md`
verbatim into the capture, so Task 2 quotes the file rather than a paraphrase.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -q` from `/workspaces/firestarter` reports `221 passed` with no `failed` and no `skipped` count, and the line is captured verbatim.
    - `python -m pytest tests/test_flash_path_record_sync.py -v` reports 41 passed, and the ten fail-closed leg names are captured.
    - The subprocess absent-meta leg's captured output contains the absolute path of the resolved marker; the missing-target leg's captured output names `MissingScanTargetError`.
    - `grep -cE '^- \[ \] \*\*R[1-7] — ' .planning/v1.23-FLASH-PATH-DECISION.md` returns `7`; the `*Why:*` and `*Breaks if omitted:*` counts are `7` each.
    - Each of the three exact literals appears exactly once in the meta record and exactly once in the subset.
    - `grep -ciE 'MEM_MODE|trampoline|RAM vector copy'` returns `0` on both records.
    - `grep -ciE 'no[[:space:]]+VTOR' firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` returns `0`; `grep -cF 'BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0'` on that file returns `1`.
    - The claim gate run against the two records exits 0 and prints `PASS:` naming both files.
    - The D-13 re-run produces identical digests across the pair, both build logs contain a `Linking` line naming `firestarter_py32f071.elf`, and `git -C /workspaces/firestarter status --porcelain` is empty afterwards.
    - Every re-checked claim is annotated in the capture as confirming or contradicting the SUMMARY that first made it; contradictions, if any, are listed explicitly rather than smoothed over.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^221 passed' && python -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1 | grep -q '^41 passed' && test -z "$(git status --porcelain)" && cd /workspaces && test "$(grep -cE '^- \[ \] \*\*R[1-7] — ' .planning/v1.23-FLASH-PATH-DECISION.md)" = "7" && test "$(grep -ciE 'no[[:space:]]+VTOR' firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld)" = "0" && python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md | grep -q '^PASS:'</automated>
  </verify>
  <done>Every row is re-executed in this session with its output captured verbatim, the D-13 sequence is re-run on the final tree with the tree left clean, and each re-checked claim is annotated against the SUMMARY that first made it.</done>
</task>

<task type="auto">
  <name>Task 2: Write 129-NONREGRESSION.md and scan it with the claim gate</name>
  <files>.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/128-release-asset-fold/128-NONREGRESSION.md` lines 1–25 and its full heading tree — the header block, the ceiling paragraph, the re-execution pledge, and the eight-section shape.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md` — its per-repo gate-table subsections and its `## Success criteria` section with the `(AMENDED — read this carefully)` heading suffix.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md` §7 — the two analogs, the intentional structural deviation for this phase, and the Phase 125 self-reference trap.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — the required caveat pattern and the one-line proximity window, so this document satisfies the gate rather than tripping it.
    - Task 1's captured output — the sole source for every figure in this document.
  </read_first>
  <action>
Write `129-NONREGRESSION.md`. Every number, exit code, digest and literal comes from Task 1's
capture, not from any prior SUMMARY.

**Header block**, following the 128 analog: `# Phase 129 Non-Regression Sweep — closing plan
(129-09)`; the written date; the firmware branch and its HEAD at this sweep; the meta branch; and an
explicit line stating that `firestarter_app` was **not** touched this phase (D-04), that it carries
unrelated uncommitted local changes, and that its gitlink is deliberately not bumped.

**The ceiling block**, as a blockquote: state that **no PY32F071 hardware exists**, that nothing in
this milestone has run on this silicon and nothing in it can, and then state precisely what this
phase's claim *is*: a written decision record, a mechanically enforced parity between its two
copies, and a comment-only firmware edit proven to change no emitted byte. Say in one sentence that
none of that says the image runs, boots or installs.

**The re-execution pledge**, in the 128 analog's words adapted: every row in §2 was executed in this
session, against the trees exactly as they now stand, and nothing is copied from any of this phase's
eight prior plans' SUMMARY files; where a prior SUMMARY made a claim, this document re-checked it
independently and says so.

**`## 1. The claim, as precise statements`** — a short numbered list, each item a sentence that is
either true or false and checkable: the record exists at a named path with named sections; the
subset's five shared bodies are byte-identical to the authoritative record's; the gate module is at
41 of 41 with ten fail-closed legs; the linker comment names both record layers and no longer
carries the corrected clause; the comment-only edit changed no emitted byte across two local builds
with a confirmed relink; the seed's status reflects its fired trigger; and the milestone claim gate
passes on all three durable artifacts.

**`## 2. Locally provable, executed now`** — with the 126 analog's per-repo subsections:
`### Firmware repo (/workspaces/firestarter)` — a table of command, expected, observed for the suite
run, the gate module run, the two fail-closed re-demonstrations, the subset structure greps, the
linker greps, and the two linker-parsing modules.
`### Meta repo (/workspaces)` — the record structure counts, the row-shape counts, the three exact
literals, the negative greps, the seed frontmatter checks, and the claim-gate run.
`### ARM byte-identity row (executed locally in this session)` — the toolchain versions, the resolved
SDK commit, the scratch build path, both digest pairs, the size rows, the `Linking` log lines, the
recompiled-object count, and the two honesty constraints as their own sentences.

**`## 3. Deliberately empty — no CI dispatch, no operator gate`** — state the intentional structural
deviation from the 128 analog: that analog's §3 and §4 held CI-only evidence and an operator
dispatch procedure, and this phase has neither, because D-13 forbids a CI run and the only firmware
edits are a new document and a comment. Say that the byte-identity evidence therefore moved *into*
§2 as a locally executed row, and that this is a deviation recorded on purpose rather than an
omission.

**`## 4. Success criteria`** — one subsection per ROADMAP criterion, quoting the criterion verbatim
from `.planning/ROADMAP.md`, then the discharge.
`### Criterion 3 — … (AMENDED — read this carefully)`: this criterion asks for a vector-relocation
implication stated for a part that lacks a vector-table offset register. Satisfying its literal
wording would require writing something false. Record: what the criterion asks; why it cannot be met
as written, with the two in-tree proofs named; what the record states instead, and where; that the
operator was consulted during planning and chose to correct the record and the linker comment while
leaving the requirement and ROADMAP prose to Phase 130's CLOSE-01 sweep; and that the substantive
intent of the criterion — a stated migration implication attached to the bootloader region — is
discharged in full by the record's flash-budget section.
`### Criterion 4`: note the partial amendment — the criterion's premise describes the placeholder as
undocumented, and the record establishes its exact upstream provenance instead; and the criterion's
verb *replaces* is satisfied by a recorded decision plus a tracked obligation, not by a code change,
because D-06 keeps `usb_cdc.c` untouched. Name the obligation and its owner.
The other three criteria are discharged as written; say so and point at the evidence rows.

**`## 5. Decision coverage — D-01…D-18`** — a table with one row per decision: id, one-line
statement, where it was discharged (plan and artifact), and how it was verified. Add three further
rows for the operator escalations and corrections that arrived during planning: the VTOR correction
and its chosen disposition; the vendor-identity correction and its chosen disposition, including the
interim identity; and the local-toolchain correction that upgraded the D-13 evidence from an
argument to an executed proof. Add one row for the size-figure correction that supplied the
reservation number.

**`## 6. Precedent and prior art`** — the two-layered document pattern and its sub-repo subset; the
milestone-prefixed decision-doc convention and the deliberate absence of an ADR numbering scheme;
the presence-probe pattern this phase mirrored from the host repository; the planted-fixture
doctrine; and the premature-tick guard, noting that eight of this phase's nine plans were told
explicitly not to tick a requirement.

**`## 7. What this phase does NOT claim`** — the explicit non-claims, one per line: no claim about
how the silicon behaves; no claim that the pin assignments match any board; no claim that the
bootloader reservation figure is anything but an estimate with a stated method; no claim that a
vendor identity has been allocated; no claim that the sync gate runs in CI on this branch; no claim
that the byte-identical image runs; and no claim that any absolute size figure here is comparable to
a CI figure. Phase 130's CLOSE-02 ledger consumes this section directly, so pair each non-claim with
the permitted claim it bounds.

**`## 8. Deviations recorded during planning and execution`** — at minimum: the fully serial wave
structure and why (deterministic expected counts across nine plans that all write to two shared
documents and one shared gate module); the fifth shared section beyond the four D-03 enumerates, and
why PCB-01 required it; the wholesale replacement of the linker comment block rather than a
clause-level edit, and why; the gate module committed RED before either record existed, and why; the
three discretion resolutions the operator delegated (the socket instruction's placement and
strength, the per-claim sourcing discipline, and the sync-gate mechanism), each with its resolution
and its rationale; and every deviation the executors recorded in their own SUMMARY files.

**`## Claim ceiling`** — the required caveat phrase in the form the gate matches, and a
defer-by-reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling". **Do not reproduce any
forbidden phrase, and do not reproduce the checker's phrase-table label strings** — three of those
labels are themselves matches for their own patterns, so quoting them inside a compliance paragraph
is exactly the Phase 125 self-reference trap that made all six of that phase's SUMMARY files trip
the gate. Refer to the table by its count and by the checker's file path, and describe the phrases
by category rather than by text.

**`## Sweep Summary`** — the one-paragraph verdict, plus a line giving the final counts: firmware
suite, gate module, records, claim-gate exit codes.

Then run the claim gate against all three durable artifacts with explicit argv, including this new
document, and record the full output. It must exit 0 with a `PASS:` line naming all three files. If
it fails on this document, fix this document — never the checker, and never by narrowing a pattern.
  </action>
  <acceptance_criteria>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` exists; line 1 is exactly `# Phase 129 Non-Regression Sweep — closing plan (129-09)`.
    - It contains all eleven required heading anchors listed in this plan's "Artifacts this phase produces".
    - `grep -ciF 'no PY32F071 hardware exists' 129-NONREGRESSION.md` returns at least `1`.
    - `grep -c 'AMENDED' 129-NONREGRESSION.md` returns at least `1`, and the `### Criterion 3` heading carries the `(AMENDED — read this carefully)` suffix.
    - §5's decision table contains all eighteen ids `D-01` through `D-18`, each exactly once as a row key: `grep -cE '^\| \*\*D-[0-1][0-9]\*\*' 129-NONREGRESSION.md` returns `18`.
    - §8 names all three delegated discretion areas and the serial-wave rationale.
    - It states explicitly that `firestarter_app` was not touched and that its gitlink is not bumped.
    - `grep -ciE 'bench[- ]validated|hardware[- ]validated|silicon[- ]verified|works[- ]end[- ]to[- ]end|pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' 129-NONREGRESSION.md` returns `0` — the self-reference trap is avoided.
    - `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` with all three artifact paths on the command line exits 0 and prints a `PASS:` line naming all three.
    - Every figure in §2 and §3 is traceable to Task 1's capture; no figure is copied from a prior SUMMARY without an accompanying re-check note.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && N=.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md && head -1 "$N" | grep -qF '# Phase 129 Non-Regression Sweep — closing plan (129-09)' && grep -qiF 'no PY32F071 hardware exists' "$N" && grep -q 'AMENDED' "$N" && test "$(grep -cE '^\| \*\*D-[0-1][0-9]\*\*' "$N")" = "18" && test "$(grep -ciE 'bench[- ]validated|hardware[- ]validated|silicon[- ]verified|works[- ]end[- ]to[- ]end|pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' "$N")" = "0" && python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md "$N" | grep -q '^PASS:'</automated>
  </verify>
  <done>`129-NONREGRESSION.md` exists with all eleven sections, criterion 3 recorded as AMENDED with its reason and owner, all eighteen decisions covered plus the four corrections, all three delegated discretion resolutions recorded, and the claim gate passing on all three durable artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Tick PCB-01…PCB-05, update the ROADMAP plan list, bump the gitlink, commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, firestarter</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Flash-Path Decision & PCB Requirements" lines 94–98 — the five unticked rows, and the style of an adjacent already-ticked section (for example the `REL-01`…`REL-04` rows) so each tick carries the same kind of evidence citation.
    - `.planning/ROADMAP.md` §"Phase 129" — its `**Plans**:` line and its plan checklist, and how a completed phase's entry is formatted elsewhere in the same file.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` as Task 2 wrote it — every tick's citation points into it by section.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-07-SUMMARY.md` — the firmware HEAD the gitlink must be bumped to.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7.
  </read_first>
  <action>
**Tick the five requirements.** In `.planning/REQUIREMENTS.md`, change each of PCB-01, PCB-02,
PCB-03, PCB-04 and PCB-05 from unticked to ticked, and extend each row with an evidence clause in
the style the adjacent completed sections use: what discharges it, where, and — where the
requirement's own wording was overtaken by a correction — a short honest qualifier.
Specifically: PCB-03's clause must state that the record carries the corrected register fact and
that PCB-03's own wording is superseded, with Phase 130 CLOSE-01 named as the owner of the prose
amendment. PCB-04's clause must state that the decision and the tracked obligation discharge it
while `usb_cdc.c` stays unedited per D-06, and that the placeholder's provenance is now recorded
rather than merely called undocumented. Each of the five must cite a section of
`129-NONREGRESSION.md`.
Tick **only** these five ids. Do not touch any other requirement row, and do not touch
§"Future Requirements", §"Out of Scope" or §"Validation Ceiling" — the Validation Ceiling's
toolchain wording is CLOSE-01's, and amending it here would take a correction out of the ledger's
sight.

**Update the ROADMAP.** In `.planning/ROADMAP.md` §"Phase 129", set the `**Plans**:` line to the
completed form showing all nine plans complete, and tick each of the nine plan checkboxes in the
phase's plan list. Change nothing in any other phase entry — Phase 130's entry in particular is
CLOSE-01/CLOSE-03 territory and must be byte-unchanged by this plan.

**Bump the meta gitlink (D-05).** Stage the `firestarter` submodule pointer so the meta repo records
the firmware tip this phase produced. Assert the staged gitlink SHA equals the firmware HEAD
recorded by `129-07`. Do **not** stage `firestarter_app` — it is untouched, read-only, and carries
unrelated local changes (D-04).

**Commit.** Verify `git -C /workspaces rev-parse --abbrev-ref HEAD` is
`gsd/v1.23-py32f071-integration` before and after. Stage exactly: the new `129-NONREGRESSION.md`,
`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, the eight prior SUMMARY files and this plan's
own SUMMARY if not already committed, and the `firestarter` gitlink. Commit subject:
`docs(129): close phase — flash-path and PCB record, sync gate, PCB-01..05`. Body: the five ticked
ids; the criterion-3 amendment in one sentence with CLOSE-01 named; the final counts (firmware suite
221 passed, gate module 41 of 41); the claim-gate exit code; the gitlink SHA; and an explicit line
stating that no CI workflow was dispatched, nothing was pushed, and no pid.codes request was filed.

No `git push`, no `gh workflow run`. Leave the scratch build directory outside the working trees;
it is not committed and does not need cleaning up, but note in the SUMMARY whether it was removed.
  </action>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] \*\*PCB-0[1-5]\*\*' .planning/REQUIREMENTS.md` returns `5`, and `grep -cE '^- \[ \] \*\*PCB-0[1-5]\*\*' .planning/REQUIREMENTS.md` returns `0`.
    - Each of the five ticked rows contains the literal `129-NONREGRESSION.md`.
    - The PCB-03 row contains the literal `CLOSE-01`; the PCB-04 row contains the literals `D-06` and `usb_cdc.c`.
    - No other requirement row changed state: `git -C /workspaces diff HEAD -- .planning/REQUIREMENTS.md | grep -E '^[+-]- \[' | grep -cvE 'PCB-0[1-5]'` returns `0`.
    - `git -C /workspaces diff HEAD -- .planning/ROADMAP.md | grep -E '^[+-]' | grep -c 'Phase 130'` returns `0` — Phase 130's entry is untouched.
    - All nine Phase 129 plan checkboxes in `.planning/ROADMAP.md` are ticked, and the `**Plans**:` line reports nine of nine complete.
    - `git -C /workspaces diff --cached --submodule=short -- firestarter` shows the new gitlink SHA, and that SHA string-equals the firmware HEAD recorded in `129-07`'s SUMMARY.
    - `git -C /workspaces diff --cached --name-only | grep -c firestarter_app` returns `0`.
    - `git -C /workspaces log -1 --format=%s` starts with `docs(129):`, and its body contains the literals `221 passed`, `41`, `CLOSE-01`, and a sentence stating nothing was pushed and no workflow was dispatched.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` outputs `gsd/v1.23-py32f071-integration`; `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs `v1.23-py32f071-integration`; `git -C /workspaces/firestarter status --porcelain` outputs zero bytes.
    - `cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1` still reports `221 passed` after the commit.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -cE '^- \[x\] \*\*PCB-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = "5" && test "$(grep -cE '^- \[ \] \*\*PCB-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = "0" && test "$(git show HEAD --format= --name-only | grep -c firestarter_app)" = "0" && git log -1 --format=%s | grep -q '^docs(129):' && git log -1 --format=%b | grep -q '221 passed' && test "$(git rev-parse --abbrev-ref HEAD)" = "gsd/v1.23-py32f071-integration" && test -z "$(git -C /workspaces/firestarter status --porcelain)" && cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^221 passed'</automated>
  </verify>
  <done>PCB-01…PCB-05 are ticked with evidence citations and honest qualifiers, the ROADMAP shows nine of nine plans complete with Phase 130's entry untouched, the meta gitlink matches the firmware tip, and one closing commit landed on the milestone branch with nothing pushed and no workflow dispatched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SUMMARY prose → closing artifact | A closing document that copies a prior SUMMARY's claim inherits its errors and launders them as verified. |
| closing artifact → Phase 130 / a stranger | `129-NONREGRESSION.md` is what a later reader and the honesty ledger trust without re-deriving. |
| meta gitlink → submodule tip | A gitlink that disagrees with the recorded firmware HEAD silently detaches the planning record from the code. |
| agent → CI / remote / registry | Pushing, dispatching or filing crosses out of the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-16 | Repudiation | `129-NONREGRESSION.md`'s figures | high | mitigate | Every row re-executed in the closing session with its output captured; each re-checked claim annotated as confirming or contradicting the SUMMARY that first made it; contradictions listed rather than smoothed over. |
| T-129-17 | Tampering (undetected) | the claim gate applied to the closing artifact | high | mitigate | The checker is invoked with explicit argv naming all three artifacts, bypassing its arming logic so a missing file is a hard failure; the document avoids reproducing any forbidden phrase or phrase-table label, three of which match their own patterns (the Phase 125 self-reference trap). |
| T-129-18 | Repudiation | ticking a requirement whose own wording was overtaken | high | mitigate | PCB-03's and PCB-04's ticks carry explicit honest qualifiers naming the correction, the superseded wording and Phase 130 CLOSE-01 as the owner of the prose amendment; criterion 3 is recorded as AMENDED with its reason. |
| T-129-19 | Tampering | the meta gitlink | medium | mitigate | The staged gitlink SHA is asserted string-equal to the firmware HEAD recorded by `129-07`, and `firestarter_app` is asserted absent from the staged set. |
| T-129-04 | Elevation of Privilege | pushing, dispatching CI, or filing the registry request | critical | mitigate | Structural separation, not a checkpoint: no task runs `git push` or `gh workflow run`, no agent files the pid.codes request, and the commit body states so explicitly. `--auto`/`--chain` auto-approve human-verify gates, so a checkpoint would not hold. |
| T-129-SC | Tampering | apt / pip toolchain installs | medium | accept | The D-13 re-run may reuse the already-installed Debian-archive and PyPI toolchain; no dependency is added to any repository manifest and the pinned SDK `GIT_TAG` is asserted unchanged. RESEARCH §"Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` from `/workspaces/firestarter` → `221 passed`, zero failed, zero skipped, before and after the commit.
- `python -m pytest tests/test_flash_path_record_sync.py -q` → `41 passed`.
- `check_permitted_claims.py` with all three artifacts on argv → exit 0, `PASS:` naming three files.
- D-13 sequence re-run on the final tree → identical digests, `Linking` confirmed, tree clean afterwards.
- `grep -cE '^- \[x\] \*\*PCB-0[1-5]\*\*' .planning/REQUIREMENTS.md` → `5`; no other requirement row changed.
- ROADMAP diff touches no other phase entry; the staged gitlink equals `129-07`'s firmware HEAD.
- Both branch names re-checked; `firestarter_app` absent from every staged set.
</verification>

<success_criteria>
- Every claim in `129-NONREGRESSION.md` was re-executed in the closing session, with contradictions against prior SUMMARYs listed explicitly if any exist.
- Criterion 3 is recorded as AMENDED with its reason, the corrected statement, and CLOSE-01 named as owner of the outstanding prose amendment; criterion 4's partial amendment is recorded too.
- All eighteen decisions plus the four research corrections and both operator escalations are individually accounted for.
- All three delegated discretion areas are recorded with their resolutions and rationale.
- PCB-01…PCB-05 are ticked by this plan and by no other, each citing a section of the evidence artifact.
- The meta gitlink is bumped in-phase to the firmware tip; `firestarter_app` is untouched and unbumped.
- Nothing was pushed, no workflow was dispatched, and no pid.codes request was filed.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-09-SUMMARY.md` when done.
Record verbatim: every command and output from Task 1's capture that is not already transcribed into
`129-NONREGRESSION.md`, the claim-gate output for all three artifacts, the five ticked ids, the
staged gitlink SHA, and the closing commit SHA. State explicitly that no push, no workflow dispatch
and no registry request occurred.
</output>
