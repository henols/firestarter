---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 06
type: execute
wave: 6
depends_on: ["129-05"]
files_modified:
  - firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md
  - firestarter/platform/py32f071/README.md
  - firestarter/CLAUDE.md
autonomous: true
requirements: [PCB-01, PCB-02, PCB-03, PCB-04, PCB-05]

must_haves:
  truths:
    - "A schematic author working only in the firmware repository finds the checklist, the flash budget, the USB identity decision, the three-tier framing and the socket instruction without leaving that repository (D-01)."
    - "The five shared sections are byte-identical to the authoritative record, by construction rather than by transcription."
    - "The subset names its authoritative parent and states the sync obligation, and the same obligation is stated in firestarter/CLAUDE.md, keyed on the same five markers (PATTERNS S6)."
    - "The socket-empty instruction is reachable from platform/py32f071/README.md, the text a bring-up reader already opens, without creating a fourth uncontrolled copy of the reasoning."
  artifacts:
    - firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md
  key_links:
    - "The subset's bodies are produced by the gate module's own _extract_shared_section helper, so byte-identity is a property of construction, not of proofreading."
    - "README §\"Hardware validation still required\" carries the one-sentence instruction plus a pointer to FLASH-PATH-AND-PCB.md — the instruction is gated in three places, the reasoning in two."
    - "firestarter/CLAUDE.md's new section is the human half of D-03; the machine half is tests/test_flash_path_record_sync.py, and the note says so explicitly."
---

<objective>
Land the firmware-repo subset (D-01/D-03) and the two pointers that make it findable: the
socket-empty instruction in `platform/py32f071/README.md`, and the sync obligation in
`firestarter/CLAUDE.md`.

Purpose: the meta record alone fails PCB-02's and PCB-05's real audience — a schematic author, and
someone about to install firmware, both of whom work in the firmware repository and would never
open `.planning/`. This plan is where the two-layer design pays off, and where 18 of the gate's 20
remaining RED legs go green.

Output: `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` with the five shared sections
byte-identical to the authoritative record, plus two pointer edits.

**This plan may NOT mark any requirement Complete.** All five PCB ids are listed in `requirements`
because the subset carries all five shared sections, but only `129-09` may tick them in
`.planning/REQUIREMENTS.md`. Do not edit `REQUIREMENTS.md` or `ROADMAP.md`.

**Repo scope (D-04):** the firmware repo only in this plan's writes. `firestarter_app` is
READ-ONLY and must not appear in any staged change.

**Standing rule:** no task in this plan runs `git push` or `gh workflow run`.
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the phase-wide list.

**Created by THIS plan:** `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md`, carrying the
heading anchors `## The three-tier flash path [SHARED:S1]`,
`## PCB requirements before the first schematic [SHARED:S2]`,
`## Flash budget, as actually reserved [SHARED:S3]`,
`## USB vendor and product identity [SHARED:S4]`,
`## Socket empty before any PY32F071 firmware install [SHARED:S5]`, and `## Claim ceiling`.
**Also created:** the README subsection anchor `### Socket empty before any firmware install`, and
the `firestarter/CLAUDE.md` section anchor `### PY32F071 Flash-Path and PCB Documentation`.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/v1.23-FLASH-PATH-DECISION.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the subset by extraction, not transcription</name>
  <files>firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md</files>
  <read_first>
    - `firestarter/platform/py32f071/CONFIG-STORAGE.md` — the whole file: its opening two lines (`# <Title> — Design Record` then `**Phase N Plan NN. Requirements: X, Y.**`), its unnumbered topic-section heading style, and its closing `## Claim ceiling` that defers by reference. This is the exact shape and the exact directory, established one phase ago.
    - `firestarter/doc/SHIELD-REVISIONS.md` — its opening reader-routing paragraph and its closing reciprocal pointer naming the meta-repo parent by path and by section numbers. This is the v1.7 subset precedent D-01 cites.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` — the authoritative record in full, especially its `**Shared sections:**` header line and its `## Claim ceiling`.
    - `firestarter/tests/test_flash_path_record_sync.py` — `_extract_shared_section`, `_SHARED_KEYS` and `_FW_DOC`. The subset is built with that helper, so byte-identity is guaranteed by construction.
  </read_first>
  <action>
Create `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md`.

**Build the five shared bodies programmatically, not by retyping.** From `/workspaces/firestarter`,
run a throwaway Python snippet — in the session only, written under the scratchpad directory and
never committed — that imports `_extract_shared_section` from
`tests.test_flash_path_record_sync`, reads
`/workspaces/.planning/v1.23-FLASH-PATH-DECISION.md`, and emits each of the five bodies verbatim.
Assert inside the snippet that each extraction is non-empty before writing anything; a silently
empty extraction would produce a subset that passes parity vacuously, which is exactly the shape
A-7 measured. Assemble the document from those five bodies plus the subset-only wrapper below.
Retyping any shared body by hand is forbidden — the sync gate would catch it, but only after the
fact, and a whitespace difference is an expensive way to learn that.

**The subset-only wrapper**, in this order:

1. Title line: `# PY32F071 Flash Path & PCB Requirements — Design Record`.
2. The requirements line, matching `CONFIG-STORAGE.md`'s form exactly:
   `**Phase 129 Plan 06. Requirements: PCB-01, PCB-02, PCB-03, PCB-04, PCB-05.**`
3. A `**Subset layer.**` paragraph naming the authoritative parent by path —
   `.planning/v1.23-FLASH-PATH-DECISION.md` in the Firestarter meta-repo — listing the five shared
   markers with their titles, stating that the five sections below are kept byte-identical with
   that record, and naming the enforcement mechanism as `tests/test_flash_path_record_sync.py`
   rather than lockstep discipline alone. State what the parent additionally carries and this copy
   does not: the sourced silicon substrate, the revision note recording three corrections, the
   rejected-route survey, the tracked obligations and the open questions. Give their section
   numbers, following the v1.7 precedent of naming shared and non-shared sections by number.
4. A short reader-routing block, in the shape `firestarter/doc/SHIELD-REVISIONS.md` opens with:
   *starting a schematic* → the checklist and the flash budget; *about to install firmware on a
   board* → the socket section, first; *implementing the self-flash bootloader* → the three-tier
   section and the flash budget, then the parent's open questions.
5. The five shared sections, each introduced by an **unnumbered** heading carrying its marker last
   on the line — `## The three-tier flash path [SHARED:S1]`,
   `## PCB requirements before the first schematic [SHARED:S2]`,
   `## Flash budget, as actually reserved [SHARED:S3]`,
   `## USB vendor and product identity [SHARED:S4]`,
   `## Socket empty before any PY32F071 firmware install [SHARED:S5]` — followed by the extracted
   body, unaltered. The heading text differs from the parent's only in dropping the leading section
   number; the gate compares bodies, not heading lines, which is why the numbering may differ.
6. A closing `## Claim ceiling` section. It is **not** a shared section and carries no marker.
   Write it fresh in `CONFIG-STORAGE.md`'s shape: state that **no PY32F071 hardware exists** as a
   physical board and that nothing here is a claim about behaviour observed on that silicon; state
   where the figures came from; state that the ARM toolchain is not installed by default in the
   development environment but is installable, and that a local build supports **delta** claims
   only, never an absolute-size comparison against a CI figure nor any claim that the image runs;
   and defer the full permitted/forbidden claim list to `.planning/REQUIREMENTS.md`
   §"Validation Ceiling" **by reference rather than restating its wording**. Do **not** copy
   `CONFIG-STORAGE.md`'s sentence asserting the ARM toolchain is absent from this environment — it
   is contradicted by this phase's own local build, and Phase 130's CLOSE-01 sweep owns correcting
   the upstream wording.

Use U+2014 EM DASH throughout the wrapper, matching the extracted bodies.
  </action>
  <acceptance_criteria>
    - `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` exists; line 1 is exactly `# PY32F071 Flash Path & PCB Requirements — Design Record`; line 3 is exactly `**Phase 129 Plan 06. Requirements: PCB-01, PCB-02, PCB-03, PCB-04, PCB-05.**`.
    - `grep -cE '^## .*\[SHARED:S[1-5]\]$' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns exactly `5`, one per key.
    - `grep -cE '^## [0-9]' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns `0` — the subset uses unnumbered topic headings.
    - The file contains the literal `.planning/v1.23-FLASH-PATH-DECISION.md` and the literal `tests/test_flash_path_record_sync.py`.
    - `grep -c '^## Claim ceiling$' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns `1`, and that heading carries no `[SHARED:` marker.
    - `grep -ciF 'no PY32F071 hardware exists' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns at least `1`.
    - `grep -ciE 'are absent from this (dev)?(container|environment)' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns `0`.
    - The subset contains none of the meta-only content: `grep -ciE 'Candidate survey|Consequences and tracked obligations|Revision Note' firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` returns `0`.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_shared_sections_match" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_fw_extract_is_non_vacuous" -q` reports `10 passed`.
    - No file under `firestarter_app/` appears in `git -C /workspaces/firestarter status --porcelain` (it is a different repository) and no throwaway script was written inside either repository working tree.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && F=platform/py32f071/FLASH-PATH-AND-PCB.md && test "$(grep -cE '^## .*\[SHARED:S[1-5]\]$' $F)" = "5" && test "$(grep -cE '^## [0-9]' $F)" = "0" && test "$(grep -c '^## Claim ceiling$' $F)" = "1" && grep -qiF 'no PY32F071 hardware exists' $F && test "$(grep -ciE 'Candidate survey|Consequences and tracked obligations|Revision Note' $F)" = "0" && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_shared_sections_match" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_fw_extract_is_non_vacuous" -q 2>&1 | tail -1 | grep -q '^10 passed'</automated>
  </verify>
  <done>The subset exists with five byte-identical shared bodies produced by extraction, its own unnumbered headings, a reciprocal pointer to the authoritative parent, and its own claim ceiling; all ten parity and firmware-side non-vacuity legs pass.</done>
</task>

<task type="auto">
  <name>Task 2: The two pointers — README socket instruction and the CLAUDE.md sync obligation</name>
  <files>firestarter/platform/py32f071/README.md, firestarter/CLAUDE.md</files>
  <read_first>
    - `firestarter/platform/py32f071/README.md` in full — particularly §"Provisional example pin map" and §"Hardware validation still required", the section the new subsection appends to.
    - `firestarter/CLAUDE.md` §"Hardware Revision Documentation" — the exact analog sentence for a subset-clone sync obligation, including how it names the shared sections and cites the phase that established the rule.
    - `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` §"Socket empty before any PY32F071 firmware install" as Task 1 wrote it — the README carries the instruction sentence and a pointer, never a second copy of the reasoning.
    - `firestarter/tests/test_flash_path_record_sync.py` — the `readme` parametrisation of `test_socket_empty_instruction_present`.
  </read_first>
  <action>
**README.** Append a new subsection to `platform/py32f071/README.md`, immediately after the
existing §"Hardware validation still required" paragraph, with the heading line exactly
`### Socket empty before any firmware install`.

Its body is three sentences and no more — this is a pointer, not a fourth copy of the reasoning:
1. The instruction, **verbatim**, matching the gate's `_L3_SOCKET_EMPTY` constant character for
   character, optionally wrapped in `**` bold markers:
   `Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.`
2. One sentence giving the short reason: the pin map in this document is provisional, so a signal
   may be assigned the wrong direction; and a DFU install runs the factory bootloader, during which
   no Firestarter GPIO initialisation executes at all. Phrase this so the words `pin map` are never
   immediately followed, with or without an intervening `is`, by `correct`, `verified` or
   `validated`.
3. One sentence pointing at the full reasoning: `FLASH-PATH-AND-PCB.md`
   §"Socket empty before any PY32F071 firmware install", in the same directory.

Add nothing else to the README. In particular do not restate the checklist, the flash map or the
USB identity decision there — the subset owns those, and an ungated fourth copy is precisely the
drift the sync gate exists to prevent.

**CLAUDE.md.** Add a new section to `firestarter/CLAUDE.md`, immediately after
§"Hardware Revision Documentation", with the heading line exactly
`### PY32F071 Flash-Path and PCB Documentation`. Mirror the analog section's sentence structure:

- State that `platform/py32f071/FLASH-PATH-AND-PCB.md` is a subset clone of the Firestarter
  meta-repo decision record at `.planning/v1.23-FLASH-PATH-DECISION.md`.
- Name the five shared sections by their markers and titles: `[SHARED:S1]` the three-tier flash
  path, `[SHARED:S2]` the PCB checklist, `[SHARED:S3]` the flash budget, `[SHARED:S4]` the USB
  vendor and product identity, `[SHARED:S5]` the socket-empty instruction. This is the third of the
  three places the same five keys are named, matching the v1.7 precedent; a reader must be able to
  find the contract from the sub-repo, from the meta record, or from this file.
- State the obligation in the analog's imperative form: if any of those sections changes in the
  meta-repo, update the sub-repo doc in the **same change** — and state that unlike the v1.7
  precedent this one is enforced mechanically by `tests/test_flash_path_record_sync.py`, not by
  lockstep discipline alone, so a divergence is a test failure rather than a latent inconsistency.
- Add one sentence recording the honest ceiling: that test module runs in **no** CI leg on this
  branch, so the enforcement is a local-run obligation for anyone editing either copy. Do not imply
  CI coverage.
- Add one sentence naming the new environment seam `FIRESTARTER_META_ROOT`, alongside the existing
  `FIRESTARTER_FW_ROOT` and `FIRESTARTER_SIZE_BASELINE`: it overrides the resolved meta-repo **root
  only**, never the marker name, and it binds at import so it must be set in a child process rather
  than monkeypatched. This repository has no central environment-variable inventory — every existing
  seam is documented in its own module docstring — so this sentence is the one place a reader who is
  not already inside `tests/meta_presence.py` can discover it.
- Cite the origin as Phase 129 / v1.23, matching how the analog cites Phase 35 / v1.7.
  </action>
  <acceptance_criteria>
    - `grep -c '^### Socket empty before any firmware install$' firestarter/platform/py32f071/README.md` returns `1`.
    - `grep -cF 'Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.' firestarter/platform/py32f071/README.md` returns `1`.
    - `grep -cF 'FLASH-PATH-AND-PCB.md' firestarter/platform/py32f071/README.md` returns at least `1`.
    - `grep -ciE 'pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' firestarter/platform/py32f071/README.md` returns `0`.
    - The README diff adds no more than 8 lines: `git -C /workspaces/firestarter diff --numstat -- platform/py32f071/README.md` shows an insertion count of at most `8` and a deletion count of `0`.
    - `grep -c '^### PY32F071 Flash-Path and PCB Documentation$' firestarter/CLAUDE.md` returns `1`.
    - `firestarter/CLAUDE.md` contains all five literals `[SHARED:S1]` … `[SHARED:S5]`, the literal `.planning/v1.23-FLASH-PATH-DECISION.md`, the literal `tests/test_flash_path_record_sync.py`, and the literal `Phase 129`.
    - `firestarter/CLAUDE.md` contains a sentence stating the module runs in no CI leg on this branch: `grep -ciE 'no CI leg' firestarter/CLAUDE.md` returns at least `1`.
    - The CLAUDE.md diff has a deletion count of `0`: `git -C /workspaces/firestarter diff --numstat -- CLAUDE.md` shows deletions `0`.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_socket_empty_instruction_present" -q` reports `3 passed`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -c '^### Socket empty before any firmware install$' platform/py32f071/README.md)" = "1" && test "$(grep -cF 'Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.' platform/py32f071/README.md)" = "1" && test "$(grep -ciE 'pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' platform/py32f071/README.md)" = "0" && test "$(grep -c '^### PY32F071 Flash-Path and PCB Documentation$' CLAUDE.md)" = "1" && for s in '[SHARED:S1]' '[SHARED:S5]' '.planning/v1.23-FLASH-PATH-DECISION.md' 'tests/test_flash_path_record_sync.py' 'Phase 129'; do grep -qF "$s" CLAUDE.md || { echo "MISSING: $s"; exit 1; }; done && test "$(git diff --numstat -- CLAUDE.md | cut -f2)" = "0" && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_socket_empty_instruction_present" -q 2>&1 | tail -1 | grep -q '^3 passed'</automated>
  </verify>
  <done>The README carries the verbatim instruction plus a pointer in at most eight added lines, and `firestarter/CLAUDE.md` states the sync obligation keyed on the same five markers with its enforcement and its CI ceiling named honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Commit, then run the full gate including the planted-mutation leg</name>
  <files>firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md, firestarter/platform/py32f071/README.md, firestarter/CLAUDE.md</files>
  <read_first>
    - `firestarter/tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_planted_mutation_of_the_real_subset_is_detected` — its final two assertions require the real subset's blob SHA to be unchanged and the firmware working tree to be clean, so the commit must land **before** the full-module run.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7.
  </read_first>
  <action>
Commit inside the firmware submodule on `v1.23-py32f071-integration`, verifying the branch before
and after. Stage exactly the three files: `platform/py32f071/FLASH-PATH-AND-PCB.md`,
`platform/py32f071/README.md`, `CLAUDE.md`. Commit subject:
`docs(129-06): PY32F071 flash-path and PCB subset record, with its README and CLAUDE.md pointers`.
Body: name D-01, D-03, PCB-01 through PCB-05, state that the five shared bodies were produced by
extraction rather than transcription, state that no requirement is ticked by this plan, and record
the RED ledger moving from 20 to 2.

Only after the commit, run the full gate module. The planted-mutation leg asserts the real
subset's blob SHA is unchanged and `git status --porcelain` is empty afterwards, so it can only
pass against a committed, clean tree — running it earlier would produce a false RED and teach the
wrong lesson.

Do not bump the meta gitlink (`129-09` owns the single in-phase bump, D-05). No `git push`, no
`gh workflow run`.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter log -1 --format=%s` starts with `docs(129-06):`.
    - `git -C /workspaces/firestarter show --stat --format= HEAD` lists exactly three files: `CLAUDE.md`, `platform/py32f071/FLASH-PATH-AND-PCB.md`, `platform/py32f071/README.md`.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes after the commit.
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs `v1.23-py32f071-integration`.
    - `cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1` reports `2 failed, 39 passed`.
    - The two remaining failures are exactly `test_linker_comment_cross_references_record` and `test_seed_status_is_no_longer_dormant`, named verbatim in the SUMMARY.
    - `cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1` reports `2 failed, 219 passed`.
    - `python3 /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py /workspaces/firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` exits 0 and prints a `PASS:` line.
    - `git -C /workspaces status --porcelain` shows the `firestarter` gitlink as modified and nothing else staged — the bump itself is deferred to `129-09`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && git log -1 --format=%s | grep -q '^docs(129-06):' && test "$(git show --stat --format= HEAD | grep -cE 'CLAUDE.md|FLASH-PATH-AND-PCB.md|README.md')" = "3" && test -z "$(git status --porcelain)" && python -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1 | grep -q '^2 failed, 39 passed' && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^2 failed, 219 passed' && python3 /workspaces/.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py /workspaces/firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md | grep -q '^PASS:'</automated>
  </verify>
  <done>Three files committed in one firmware commit; the full gate module is at 39/41 with exactly the linker and seed legs remaining; the subset passes the milestone claim gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta record → firmware subset | Two copies of the same text, maintained in different repositories on different review paths. |
| subset → schematic author | The subset is the copy that will actually be read while a board is designed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-06 | Tampering | subset/authoritative divergence | high | mitigate | Bodies produced by extraction with a non-empty assertion before writing; five per-key parity legs plus a planted-mutation leg run against the committed tree; the obligation is stated in three places keyed on the same markers. |
| T-129-11 | Tampering | an uncontrolled fourth copy of the reasoning in README.md | medium | mitigate | The README carries the one gated instruction sentence plus a pointer, in at most eight added lines, with the addition count asserted; the reasoning lives only in the two gated copies. |
| T-129-03 | Repudiation / Information disclosure | the subset's claim ceiling | high | mitigate | Its own `## Claim ceiling` carries the required caveat, states delta-only for local build figures, and defers the forbidden-phrase list by reference; verified by running the milestone claim gate against the file with an explicit target. |
| T-129-04 | Elevation of Privilege | any task in this plan | critical | mitigate | Structural separation: no `git push`, no `gh workflow run`, no PR filing. |
| T-129-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and adds no dependency to any manifest; RESEARCH §"Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` from `/workspaces/firestarter` → `2 failed, 219 passed`, with the two failures named.
- `check_permitted_claims.py` against the subset → exit 0, `PASS:`.
- README insertion count ≤ 8, deletion count 0; CLAUDE.md deletion count 0.
- Firmware tree clean after the commit; branch re-checked.
</verification>

<success_criteria>
- The subset exists with five byte-identical shared bodies, built by extraction.
- Eighteen gate legs discharged; the RED ledger drops from 20 to exactly 2.
- The socket instruction is reachable from the README a bring-up reader already opens, without a fourth uncontrolled copy of the reasoning.
- The sync obligation is stated in the subset, in `firestarter/CLAUDE.md` and in the authoritative record, keyed on the same five markers.
- No requirement ticked; `REQUIREMENTS.md`/`ROADMAP.md` untouched; nothing under `firestarter_app/` staged.
- No `git push`, no `gh workflow run`.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-06-SUMMARY.md` when done.
Record verbatim: the extraction command used, the pytest summary line before and after, the two
remaining failing node ids, the claim-gate output, and the firmware commit SHA.
</output>
