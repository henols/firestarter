---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 07
type: execute
wave: 7
depends_on: ["129-06"]
files_modified:
  - firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld
autonomous: true
requirements: [PCB-03]

must_haves:
  truths:
    - "The linker script's BOOTLOADER comment names the record's actual filename, so the cross-reference closes in both directions (D-11)."
    - "The comment no longer asserts the part lacks a vector-table offset register — the one known-false statement in a comment that explicitly instructs future readers is removed in the edit D-11 already opens (C-1 / OP-1)."
    - "BOOTLOADER keeps ORIGIN 0x08000000 and LENGTH = 0; giving it a real length stays rejected (D-11)."
    - "The edit changes no emitted byte, proven by identical SHA-256 digests of the .bin and .hex across two local builds of the same tree, with the link step demonstrably re-run (D-13)."
    - "The build evidence is a delta claim only: no absolute size figure from a local build is compared against a CI figure, and a byte-identical image is never presented as evidence that the image runs."
  artifacts:
    - firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld
  key_links:
    - "LINK_DEPENDS binds the target to the linker script, so a comment-only edit forces a relink — which is what makes the byte-identity comparison non-vacuous rather than a no-op."
    - "The build directory lives outside both repository working trees, so `git status --porcelain` stays empty and the D-13 evidence cannot be confused with a dirty tree."
    - "tests/test_py32_flash_map.py and tests/test_flash_geometry_recorded_before_linker.py both parse this file and must stay green across the edit."
---

<objective>
Land D-11's comment-only cross-reference in
`firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld`, correct the one false clause in that
comment per C-1, and discharge D-13 with a locally executed byte-identity proof.

Purpose: the existing `BOOTLOADER` comment is a direct instruction to this phase — it tells a
future reader to consult Phase 129's record before giving the region a size, without naming it.
D-11 closes that reference. And because the comment currently attributes the migration cost to the
absence of a register the part actually has, leaving it in place would preserve a known-false
statement inside the very comment that instructs future readers, which is the worst available
outcome (RESEARCH C-1's recommended disposition, confirmed by the operator).

Output: one comment-only firmware commit, plus the evidence that it changed no emitted byte.

**This plan may NOT mark any requirement Complete.** PCB-03 appears in `requirements` because this
edit is part of its evidence, but only `129-09` may tick it. Do not edit `REQUIREMENTS.md` or
`ROADMAP.md` — Phase 130's CLOSE-01 sweep owns PCB-03's and ROADMAP criterion 3's prose, and this
plan's job is to leave that sweep a working cross-reference and a corrected comment, not to
perform it.

**D-13 is a hard constraint:** no operator-gated ARM CI run this phase. **No task runs `git push`
or `gh workflow run`.** The evidence is local.

**Repo scope (D-04):** the firmware repo only.

<!-- planner-discipline-allow: no VTOR -->
<!-- planner-discipline-allow: BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0 -->
<!-- planner-discipline-allow: ORIGIN = -->

Note on the allowlist markers above: Task 2's acceptance criteria negative-grep the **diff** of the
linker script for added or removed lines matching `ORIGIN =`, `PROVIDE(`, `ASSERT(`, `_estack` or
`_Min_` — the point being that the edit is comment-only and touches no region line, symbol or guard.
Task 2's action necessarily quotes the unchanged `BOOTLOADER` region line so the executor knows to
leave it byte-identical. There is no echo hazard: the line is already in the file and is not being
rewritten, so it can appear in neither the added nor the removed side of the diff.
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the phase-wide list.

**Modified by THIS plan:** `firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` — the
`BOOTLOADER` region's preceding comment block only. No `MEMORY` region line, no `PROVIDE`, no
`ASSERT`, and no symbol changes.

**Produced as evidence, outside both working trees:** a scratch CMake build directory and four
capture files (`before.txt`, `after.txt`, `before-build.log`, `after-build.log`) whose contents are
transcribed into this plan's SUMMARY and then into `129-NONREGRESSION.md`. None of them is
committed.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolchain preflight and the baseline build, hashed before any edit</name>
  <files>(no files written inside either repository)</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §C-3 and §"Standard Stack" — the exact install command set, the two extra packages the CI composite action does **not** install, and the executed configure/build sequence with its recorded figures.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the target name, the `BIN_FILE` and `HEX_FILE` output names, and the `LINK_DEPENDS` property that binds the target to the linker script.
    - `firestarter/.gitignore` — confirm the repository does **not** ignore a `build/` directory, which is why the scratch build directory must live outside the working tree.
  </read_first>
  <action>
Establish the toolchain, then capture a baseline before touching anything.

**Preflight.** Check whether `arm-none-eabi-gcc`, `arm-none-eabi-size`, `cmake` and `ninja` are all
already on PATH and report versions. If any is missing, install with
`sudo apt-get install -y --no-install-recommends gcc-arm-none-eabi binutils-arm-none-eabi
libnewlib-arm-none-eabi libstdc++-arm-none-eabi-newlib` followed by `pip install cmake ninja`.
The last two apt packages are load-bearing and are **not** in the CI composite action's install
list: Debian's `gcc-arm-none-eabi` does not pull newlib, so without them every C++ translation unit
fails on a missing standard header. Record the resolved versions verbatim — they are part of the
delta claim's terms.

**Scratch build directory, outside both working trees.** Use
`${TMPDIR:-/tmp}/firestarter-py32f071-d13`. The firmware repository does **not** gitignore a
`build/` directory, so a build inside the working tree would dirty it and confuse the D-13
evidence with an unrelated change. Create it fresh (remove any prior contents) so the configure
step is a clean one.

**Configure and build the baseline.** From `/workspaces/firestarter`, configure with the Ninja
generator, `Release` build type, source directory `platform/py32f071`, binary directory the scratch
path. `FetchContent` will clone the pinned SDK at the recorded `GIT_TAG`; record the resolved SDK
commit from the configure output and assert it equals
`0ed2f4b4d3391eccfd4491006a30295fd78e32c2`. Then build, teeing the full output to
`before-build.log` in the scratch directory. Record the object count reported by the build.

**Capture the baseline artifacts.** Write to `before.txt` in the scratch directory: the SHA-256
digests of `firestarter_py32f071.bin` and `firestarter_py32f071.hex`, and the `arm-none-eabi-size`
output for `firestarter_py32f071.elf`. Transcribe all of it into the SUMMARY.

**State the two honesty constraints now, before any comparison exists.** (a) These absolute figures
are from a *local* build and may be compared only against another local build of the same tree with
the same toolchain — the local and CI compilers differ and have produced different absolute sizes
for identical source, so no absolute figure here may be set beside a CI figure. The D-13 claim is a
**delta** claim, and a delta is unaffected by that difference. (b) A byte-identical image proves the
emitted output is unchanged; it proves nothing about whether the image runs, and no PY32F071
hardware exists on which to find out.

Confirm `git -C /workspaces/firestarter status --porcelain` is still empty after the build.
  </action>
  <acceptance_criteria>
    - `arm-none-eabi-gcc --version`, `arm-none-eabi-size --version`, `cmake --version` and `ninja --version` all succeed, and their version strings are recorded verbatim in the SUMMARY.
    - The scratch build directory is outside `/workspaces/firestarter` and outside `/workspaces/.planning`: its resolved absolute path does not start with either.
    - The configure step exits 0 and the resolved SDK commit recorded in the output equals `0ed2f4b4d3391eccfd4491006a30295fd78e32c2`.
    - `cmake --build` exits 0 and `before-build.log` contains a line naming `firestarter_py32f071.elf` together with the word `Linking`.
    - `before.txt` exists and contains two SHA-256 digests plus one `arm-none-eabi-size` data row, all transcribed into the SUMMARY.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes.
    - The SUMMARY states both honesty constraints (delta-only; byte-identity is not a behavioural claim) in its own words.
  </acceptance_criteria>
  <verify>
    <automated>B="${TMPDIR:-/tmp}/firestarter-py32f071-d13" && test -f "$B/before.txt" && test "$(grep -c . "$B/before.txt")" -ge 3 && grep -q 'firestarter_py32f071.elf' "$B/before-build.log" && grep -q 'Linking' "$B/before-build.log" && case "$B" in /workspaces/firestarter*|/workspaces/.planning*) exit 1;; esac && test -z "$(git -C /workspaces/firestarter status --porcelain)"</automated>
  </verify>
  <done>Toolchain versions recorded, a clean out-of-tree Release build produced at the pinned SDK commit, baseline digests and size row captured, and both honesty constraints stated before any comparison exists.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the BOOTLOADER comment block — cross-reference in, false clause out</name>
  <files>firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld</files>
  <read_first>
    - `firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` in full — the file is 60-odd lines; read all of it. Note the two comment conventions it uses: every comment names the decision that owns it, and cross-references take the form `see <path> §"<Heading>"` (as the file's own opening comment does for the geometry record).
    - `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` — confirm the exact heading text of its flash-budget section, so the cross-reference names a section that actually exists.
    - `firestarter/tests/test_flash_path_record_sync.py` — `_LINKER_NEEDLES` and `_LINKER_FORBIDDEN_RE` as `129-02` fixed them.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` §1.6 Correction 1 — the corrected framing this comment now carries in condensed form.
  </read_first>
  <action>
**Replace the entire comment block that immediately precedes the `BOOTLOADER` region line** — the
block-comment spanning from its opening delimiter down to its closing delimiter — with new text.
Replacing the whole block, rather than editing individual clauses, is deliberate: it is how the
false clause leaves the file without anyone having to reproduce it in an instruction or a commit
message.

Everything else in the file is untouched. The region line itself keeps its exact text
`BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0`. No `FLASH`, `CONFIG` or `RAM` region changes;
no `PROVIDE` changes; no `ASSERT` changes; no whitespace changes outside the replaced block.

The new block, preserving the file's four-space indentation and its ` * ` continuation style, must
say the following, in this order:

1. **Ownership and the closed cross-reference.** Keep the existing `D-13` attribution and the
   `NAMED SEAM ONLY, ZERO LENGTH` characterisation. Add the reference D-11 asks for, in the file's
   own `see <path> §"<Heading>"` form: the subset record at
   `platform/py32f071/FLASH-PATH-AND-PCB.md`, naming its flash-budget section heading exactly as
   that file spells it; and the authoritative layer at `.planning/v1.23-FLASH-PATH-DECISION.md` in
   the Firestarter meta-repo. Keep the `PCB-03/FUT-N05` attribution so the requirement trail
   survives.
2. **The instruction, unchanged in force.** Retain the imperative that this must be read before the
   region is given a size, and retain the contrast with the `CONFIG` region: `CONFIG` sits at the
   top of flash and grows downward without moving anything, whereas giving `BOOTLOADER` a non-zero
   length moves the application's `ORIGIN`, which is a flash-map migration rather than a resize.
3. **The corrected cost.** State it as the fleet migration: every already-flashed unit needs a full
   re-flash over DFU or SWD instead of an in-place update — precisely the recovery paths a
   self-flash bootloader exists to avoid.
4. **The correction, attributed.** One short parenthetical recording that this comment previously
   attributed the cost to the absence of a vector-table offset register, that the attribution was
   wrong, and how that was established: the part's CMSIS device header declares `__VTOR_PRESENT`
   and the compiled `SystemInit` writes `SCB->VTOR` at every boot, so the vector table moving is
   the cheap half and the one-time fleet migration is the expensive half. Cite Phase 129 and
   `129-RESEARCH.md` C-1 so a reader can find the evidence.
5. **The standard, retained verbatim in force.** Phase 129 records the bootloader budget as an
   intent with that cost attached, never as a number that looks already paid for.

Do not add a `LENGTH` value, and do not soften the rejection: giving `BOOTLOADER` a real length
remains out of scope for this milestone.
  </action>
  <acceptance_criteria>
    - `grep -cF 'BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0' firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` returns `1`.
    - `grep -ciE 'no[[:space:]]+VTOR' firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` returns `0`.
    - The file contains all of: `FLASH-PATH-AND-PCB.md`, `v1.23-FLASH-PATH-DECISION.md`, `__VTOR_PRESENT`, `SCB->VTOR`, `PCB-03`, `FUT-N05`, `re-flash`, `already paid for`.
    - `git -C /workspaces/firestarter diff --numstat -- platform/py32f071/linker/PY32F071xB_FLASH.ld` shows changes confined to that one file, and `git -C /workspaces/firestarter diff -U0 -- platform/py32f071/linker/PY32F071xB_FLASH.ld | grep -cE '^[+-][^+-]' ` counts only lines that are inside the replaced comment block — no added or removed line contains `ORIGIN =`, `PROVIDE(`, or `ASSERT(`.
    - `git -C /workspaces/firestarter diff -- platform/py32f071/linker/PY32F071xB_FLASH.ld | grep -E '^[+-]' | grep -cE 'ORIGIN =|PROVIDE\(|ASSERT\(|_estack|_Min_' ` returns `0`.
    - `cd /workspaces/firestarter && python -m pytest tests/test_py32_flash_map.py tests/test_flash_geometry_recorded_before_linker.py -q` passes with the same counts recorded in `129-01`'s baseline for those two modules.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && L=platform/py32f071/linker/PY32F071xB_FLASH.ld && test "$(grep -cF 'BOOTLOADER (rx) : ORIGIN = 0x08000000, LENGTH = 0' $L)" = "1" && test "$(grep -ciE 'no[[:space:]]+VTOR' $L)" = "0" && for s in 'FLASH-PATH-AND-PCB.md' 'v1.23-FLASH-PATH-DECISION.md' '__VTOR_PRESENT' 'SCB->VTOR' 'PCB-03' 'FUT-N05' 're-flash' 'already paid for'; do grep -qF "$s" $L || { echo "MISSING: $s"; exit 1; }; done && test "$(git diff -- $L | grep -E '^[+-]' | grep -cE 'ORIGIN =|PROVIDE\(|ASSERT\(|_estack|_Min_')" = "0" && python -m pytest tests/test_py32_flash_map.py tests/test_flash_geometry_recorded_before_linker.py -q 2>&1 | tail -1 | grep -qv 'failed'</automated>
  </verify>
  <done>The BOOTLOADER comment names both record layers, states the corrected fleet-migration cost with its attribution, retains the "never as a number that looks already paid for" standard, and no longer carries the false clause; the region line, every symbol and both structural guards are byte-unchanged, and both linker-parsing test modules still pass.</done>
</task>

<task type="auto">
  <name>Task 3: The D-13 byte-identity proof, then commit</name>
  <files>firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Architecture Patterns" Pattern 3 and §"Common Pitfalls" Pitfall 4 — the command sequence, and why the relink must be confirmed or the comparison is vacuous.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the `LINK_DEPENDS` property, which is what guarantees the relink actually happens.
  </read_first>
  <action>
**Rebuild in the same scratch directory without reconfiguring.** Do not delete it and do not re-run
`cmake -S`: the point is an incremental build in which the *only* changed input is the linker
script, so the only work is the link step and the objcopy steps that depend on it. Tee the output
to `after-build.log`.

**Confirm the relink actually happened** before comparing anything. `after-build.log` must contain a
line naming `firestarter_py32f071.elf` together with the word `Linking`. If it does not, the
comparison is vacuous — a no-op rebuild producing identical hashes proves nothing — and the task
must stop and report rather than record a false proof. Also record how many objects were
recompiled; the expected answer is zero, and that zero is itself part of the evidence that no
translation unit was added or altered.

**Capture and compare.** Write `after.txt` in the same format as `before.txt`: SHA-256 of the
`.bin`, SHA-256 of the `.hex`, and the `arm-none-eabi-size` row for the `.elf`. Then diff
`before.txt` against `after.txt`; it must be empty. Transcribe both files and the diff result into
the SUMMARY.

Restate the claim precisely, in the SUMMARY, in exactly these terms: the comment-only edit to the
linker script changed no emitted byte, established by two local builds of the same tree with the
same toolchain whose `.bin` and `.hex` digests are identical and whose size rows are identical,
with the link step confirmed to have re-run. Then restate both ceilings: the absolute figures are
delta-comparable only and must never be set beside a CI figure, and a byte-identical image says
nothing about whether the image runs.

**Then run the gate and the full suite**, and commit inside the firmware submodule on
`v1.23-py32f071-integration`, verifying the branch before and after. Stage exactly
`platform/py32f071/linker/PY32F071xB_FLASH.ld`. Commit subject:
`docs(129-07): linker BOOTLOADER comment cites the flash-path record and drops the corrected clause`.
Body: name D-11, D-13, C-1 and PCB-03; state that the edit is comment-only; give both SHA-256
digests and state they are unchanged across the rebuild; state that the link step re-ran; state
that no requirement is ticked by this plan; and state that no CI run and no push was performed.

Leave the scratch build directory in place until `129-09` has transcribed its contents into
`129-NONREGRESSION.md`. Do not bump the meta gitlink (`129-09` owns the single bump, D-05).
No `git push`, no `gh workflow run`.
  </action>
  <acceptance_criteria>
    - `after-build.log` contains a line naming `firestarter_py32f071.elf` together with the word `Linking`.
    - `diff before.txt after.txt` in the scratch directory produces no output and exits 0.
    - The two SHA-256 digests and the `arm-none-eabi-size` row are transcribed identically for before and after in the SUMMARY.
    - The number of recompiled objects in `after-build.log` is zero, and that is stated in the SUMMARY.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_linker_comment_cross_references_record" -q` reports `1 passed`.
    - `cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1` reports `1 failed, 220 passed`, the single remaining failure being `test_seed_status_is_no_longer_dormant`.
    - `git -C /workspaces/firestarter log -1 --format=%s` starts with `docs(129-07):`, and `git -C /workspaces/firestarter show --stat --format= HEAD` lists exactly one file, the linker script.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes after the commit, and `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs `v1.23-py32f071-integration`.
    - No CI workflow was dispatched and nothing was pushed: the SUMMARY states this explicitly, and the session transcript contains no `git push` or `gh workflow run` invocation.
  </acceptance_criteria>
  <verify>
    <automated>B="${TMPDIR:-/tmp}/firestarter-py32f071-d13" && grep -q 'firestarter_py32f071.elf' "$B/after-build.log" && grep -q 'Linking' "$B/after-build.log" && diff -q "$B/before.txt" "$B/after.txt" && cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_linker_comment_cross_references_record" -q 2>&1 | tail -1 | grep -q '^1 passed' && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^1 failed, 220 passed' && git log -1 --format=%s | grep -q '^docs(129-07):' && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The comment-only edit is proven to change no emitted byte by two local builds with a confirmed relink and identical digests; the linker gate leg passes; one gate leg remains; the commit is comment-only and no CI run or push occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| linker comment → future implementer | The comment explicitly instructs a future reader before a flash-map change; a false statement there is acted on. |
| local build evidence → milestone claims | A local build's figures could be mistaken for CI figures, or for behavioural evidence. |
| agent → CI / remote | Dispatching a workflow or pushing a branch crosses out of the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-12 | Repudiation | the `BOOTLOADER` comment's stated reason | high | mitigate | The whole comment block is replaced, so the false clause leaves the file; the replacement states the corrected cost, names the two in-tree proofs, and cites the phase and research finding that established it. A negative grep asserts the clause is gone. |
| T-129-07 | Information disclosure | the local ARM build's absolute figures | medium | mitigate | The claim is scoped to a delta between two local builds of the same tree with the same toolchain; toolchain versions are recorded as part of the claim's terms; the SUMMARY states explicitly that no absolute figure may be set beside a CI figure, and that byte-identity is not a behavioural claim. |
| T-129-13 | Tampering (undetected) | a vacuous byte-identity proof | high | mitigate | The relink is confirmed from the build log before any comparison is recorded, and the recompiled-object count of zero is recorded too; `LINK_DEPENDS` is what makes the relink happen, and a missing `Linking` line stops the task rather than producing a false proof. |
| T-129-04 | Elevation of Privilege | dispatching CI or pushing | critical | mitigate | Structural separation, not a checkpoint: D-13 forbids a CI run this phase and no task runs `git push` or `gh workflow run`. `--auto`/`--chain` auto-approve human-verify gates, so a checkpoint would not hold. |
| T-129-SC | Tampering | apt / pip toolchain installs | medium | accept | The four apt packages come from Debian's official archive and are the same family the CI composite action installs; `cmake` and `ninja` come from PyPI. **No dependency is added to any repository manifest** — `platform/py32f071/CMakeLists.txt`, `platformio.ini` and every requirements file are unchanged, and the pinned SDK `GIT_TAG` is asserted equal to its recorded value. RESEARCH §"Package Legitimacy Audit" records the phase as not-applicable with zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `diff before.txt after.txt` → empty, exit 0; both digests identical; size row identical.
- `after-build.log` contains a `Linking` line naming `firestarter_py32f071.elf`; zero objects recompiled.
- `grep -ciE 'no[[:space:]]+VTOR'` on the linker script → `0`.
- The linker-script diff contains no line matching `ORIGIN =`, `PROVIDE(`, `ASSERT(`, `_estack` or `_Min_`.
- `python -m pytest tests/ -q` → `1 failed, 220 passed`.
- Firmware tree clean after the commit; branch re-checked; no push, no workflow dispatch.
</verification>

<success_criteria>
- The `BOOTLOADER` comment names both record layers and no longer carries the false clause; the region line and every symbol are byte-unchanged.
- The comment-only edit is proven to change no emitted byte, with the relink confirmed.
- Both honesty ceilings are stated: delta-only, and not a behavioural claim.
- `tests/test_py32_flash_map.py` and `tests/test_flash_geometry_recorded_before_linker.py` still pass.
- One gate leg discharged; exactly one remains.
- No requirement ticked; no CI run; no `git push`; no `gh workflow run`; nothing under `firestarter_app/` touched.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-07-SUMMARY.md` when done.
Record verbatim: the four toolchain version strings, the resolved SDK commit, the scratch build
path, `before.txt` and `after.txt` in full, the `Linking` log line, the recompiled-object count,
the pytest summary line, and the firmware commit SHA. Leave the scratch directory in place for
`129-09`.
</output>
