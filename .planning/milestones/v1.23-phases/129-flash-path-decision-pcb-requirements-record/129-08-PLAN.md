---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 08
type: execute
wave: 8
depends_on: ["129-07"]
files_modified:
  - .planning/seeds/py32f071-no-external-tool-fw-install.md
autonomous: true
requirements: [PCB-01]

must_haves:
  truths:
    - "The seed's status reflects reality: its trigger has already fired, the factory-DFU runner-up landed in v1.23, and the self-flash primary route did not (D-17)."
    - "The seed remains live for FUT-N05 — landing the DFU path does not retire it, and the seed says so in its own words (PCB-01)."
    - "The seed points at the new record as canonical for the flash-path decision, by relative markdown link (D-18)."
    - "The seed's four-field frontmatter schema is unchanged: no new key is invented to carry the update."
    - "The seed's superseded bootloader-size sentence is flagged as superseded by the record, and its stale milestone-slot reference is flagged as owned by Phase 130 CLOSE-03 rather than silently rewritten."
  artifacts:
    - .planning/seeds/py32f071-no-external-tool-fw-install.md
  key_links:
    - "The body link target `../v1.23-FLASH-PATH-DECISION.md` resolves from `.planning/seeds/` to the authoritative record, using the same relative-link form the seed already uses for its branch-state note."
    - "test_seed_status_is_no_longer_dormant is the last of the gate's 31 legs; this plan takes the module to 41/41."
---

<objective>
Update `.planning/seeds/py32f071-no-external-tool-fw-install.md` to reflect what actually happened
(D-17) and to point at the new record as canonical (D-18).

Purpose: the seed's trigger condition — *the PY32F071 port milestone is activated, or the first
PCB/schematic is specified, whichever comes first* — fired when the port was activated as v1.23,
while its status still reads as unfired. That gap is what PCB-01's "does not retire the seed" points
at concretely: a reader who opens the seed today cannot tell that half of it shipped, or that the
half that shipped is the runner-up rather than the decision.

Output: one meta commit editing one file, taking the gate module to 41/41 and the firmware suite to
fully green.

**This plan may NOT mark any requirement Complete.** PCB-01 appears in `requirements` because the
seed disposition is part of its evidence, but only `129-09` may tick it. Do not edit
`REQUIREMENTS.md` or `ROADMAP.md`.

**Repo scope (D-04):** the meta repo only, and within it exactly one file.

**Standing rule:** no task in this plan runs `git push` or `gh workflow run`.

<!-- planner-discipline-allow: dormant -->
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the phase-wide list.

**Modified by THIS plan:** `.planning/seeds/py32f071-no-external-tool-fw-install.md` — the
frontmatter `status:` value, and additions to its existing §"Status: the USB DFU half is
implemented (2026-07-28)" section. No new frontmatter key; no new top-level section.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/v1.23-FLASH-PATH-DECISION.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the seed's status, within its existing schema</name>
  <files>.planning/seeds/py32f071-no-external-tool-fw-install.md</files>
  <read_first>
    - `.planning/seeds/py32f071-no-external-tool-fw-install.md` in full — the four-field frontmatter, the decision body, the rejected-route table, the five PCB requirements, its §"Status" section and its §"Open questions".
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md` §6 — the frontmatter is a fixed four-field schema (`title`, `trigger_condition`, `planted_date`, `status`); adding a field is a change to the seed format itself and must be resisted, with prose in the body preferred over a novel key.
    - The other files under `.planning/seeds/` — confirm by inspection that the four-field schema is the house convention before choosing a new `status` value, and that no tooling parses the value as an enumeration.
    - `firestarter/tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_seed_status_is_no_longer_dormant` — the exact assertions: key set equal and ordered, `status` value changed, body link target present, `FUT-N05` present.
    - `.planning/v1.23-FLASH-PATH-DECISION.md` §2 and §4 — what the seed now points at, and which single sentence of the seed §4 supersedes.
  </read_first>
  <action>
**Frontmatter.** Change only the `status:` value. Keep `title`, `trigger_condition` and
`planted_date` byte-identical, keep all four keys in their current order, and **add no new key** —
no fired-date, no pointer key. The new value must be a short hyphenated phrase plus a clause,
readable as prose on one line, conveying that the seed is partially realised: the factory-USB-DFU
runner-up landed in v1.23 while the self-flash primary route did not, and the seed stays live for
FUT-N05. Keep it on a single line so the block stays parseable by the simple key-value split the
gate uses.

Do **not** alter `trigger_condition`. Its text names the milestone by its originally-queued slot,
and rewriting it would erase the evidence that the trigger fired — the point of this update is that
the recorded condition was met, not that the condition was wrong.

**Body — extend the existing status section, do not add a new top-level one.** Under
§"Status: the USB DFU half is implemented (2026-07-28)", append a short dated block whose first line
is a bolded sub-note naming Phase 129 and the current date. It must say, in this order:

1. **The trigger fired.** The port milestone was activated as v1.23, so the first of the trigger's
   two disjuncts has been met. The second — a specified PCB or schematic — has **not**: the board is
   still paper, which is exactly why the PCB consequences were recorded now rather than later.
2. **What landed and what did not.** The factory-USB-DFU route — the *runner-up* in this seed's own
   table, not the decision — shipped in v1.23. The self-flash bootloader over the existing CDC and
   COBS transport, this seed's primary route, did not. State that landing the runner-up **does not
   retire this seed**, in the seed's own existing terms.
3. **Where the decision now lives.** A relative markdown link to
   `../v1.23-FLASH-PATH-DECISION.md`, using the same link form the seed already uses for its
   branch-state note. Say that the record is **canonical** for the flash-path decision, that it is
   the only document citing the flash addresses Phase 126 actually reserved, and that it carries the
   PCB checklist this seed's own five items fed into. Name `FUT-N05` as the requirement that owns
   the remaining work.
4. **One number this seed no longer gets to assert.** The record supersedes this seed's estimate
   that the bootloader fits in the first few KB of flash: measured against real objects, the
   components such a bootloader must carry already total substantially more, and the record's
   flash-budget section holds the replacement figure with its migration cost attached. Point at that
   section by name. Scope the supersession to that one number and say so — the rest of the seed
   stands.
5. **One stale reference, flagged rather than rewritten.** The paragraph above this note refers to
   the work as queued under its original milestone slot. That reference is stale, and correcting it
   belongs to **Phase 130 CLOSE-03**, which owns the slot renumber together with the stale
   prior-art correction. Flag it here so a reader is not misled, and do not rewrite it in this
   phase — two phases editing the same claim in different directions is how a correction gets lost.
6. **One open question this seed can now close by reference.** Its PCB item about confirming the
   contiguous data bus against the final package and pin multiplexing is discharged by the record's
   PCB checklist, which names the packages that cannot carry the bus at all. Point at the row.

Leave the seed's §"Open questions" section otherwise untouched — its remaining entries are still
open, and two of them are restated as open questions in the record with a cross-reference.
  </action>
  <acceptance_criteria>
    - The frontmatter block still has exactly four keys in this order: `title`, `trigger_condition`, `planted_date`, `status`. `grep -cE '^(title|trigger_condition|planted_date|status):' .planning/seeds/py32f071-no-external-tool-fw-install.md` returns `4`.
    - `git -C /workspaces diff -- .planning/seeds/py32f071-no-external-tool-fw-install.md | grep -E '^[+-]' | grep -cE '^[+-](title|trigger_condition|planted_date):'` returns `0` — only the `status` line changed in the frontmatter.
    - `sed -n '5p' .planning/seeds/py32f071-no-external-tool-fw-install.md | grep -qv 'dormant'` succeeds — the status value no longer carries the word every other seed carries.
    - The status value is a single line: the frontmatter's closing `---` is still on line 6.
    - The body contains the literal `../v1.23-FLASH-PATH-DECISION.md`, the literal `FUT-N05`, the literal `CLOSE-03`, and the literal `v1.23`.
    - The body contains a sentence naming the record as canonical and a sentence scoping the supersession to the bootloader-size number only.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_seed_status_is_no_longer_dormant" -q` reports `1 passed`.
    - `cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1` reports `221 passed` with no `failed` and no `skipped` count.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes — no firmware file was touched.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && S=.planning/seeds/py32f071-no-external-tool-fw-install.md && test "$(grep -cE '^(title|trigger_condition|planted_date|status):' $S)" = "4" && test "$(sed -n '6p' $S)" = "---" && test "$(git diff -- $S | grep -E '^[+-]' | grep -cE '^[+-](title|trigger_condition|planted_date):')" = "0" && for t in '../v1.23-FLASH-PATH-DECISION.md' 'FUT-N05' 'CLOSE-03'; do grep -qF "$t" $S || { echo "MISSING: $t"; exit 1; }; done && cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^221 passed' && test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The seed's status reflects reality within its unchanged four-field schema, its body records what landed and what did not, points at the record as canonical, scopes the one superseded number, flags the stale slot reference to CLOSE-03, and closes its own data-bus open item by reference — and the firmware suite is fully green at 221.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the seed update in the meta repo</name>
  <files>.planning/seeds/py32f071-no-external-tool-fw-install.md</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7.
  </read_first>
  <action>
Verify `git -C /workspaces rev-parse --abbrev-ref HEAD` is `gsd/v1.23-py32f071-integration` before
and after. Stage exactly `.planning/seeds/py32f071-no-external-tool-fw-install.md`. Commit subject:
`docs(129-08): seed status reflects the fired trigger and points at the canonical flash-path record`.
Body: name D-17, D-18 and PCB-01; state that the four-field schema is unchanged; state that the
stale milestone-slot reference is flagged for CLOSE-03 rather than rewritten here; record that the
firmware gate module is now 41/41 and the firmware suite 221 passed; state that no requirement is
ticked by this plan.

Do not stage `REQUIREMENTS.md`, `ROADMAP.md`, or any submodule gitlink — `129-09` owns the single
in-phase gitlink bump (D-05). No `git push`, no `gh workflow run`.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces log -1 --format=%s` starts with `docs(129-08):`.
    - `git -C /workspaces show --stat --format= HEAD` lists exactly one file, `.planning/seeds/py32f071-no-external-tool-fw-install.md`.
    - `git -C /workspaces diff --name-only HEAD~1 HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` outputs nothing.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` outputs `gsd/v1.23-py32f071-integration`.
    - `git -C /workspaces/firestarter rev-parse HEAD` equals the value `129-07`'s SUMMARY recorded — the firmware repo did not move in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git log -1 --format=%s | grep -q '^docs(129-08):' && test "$(git show --stat --format= HEAD | grep -c 'py32f071-no-external-tool-fw-install.md')" = "1" && test -z "$(git diff --name-only HEAD~1 HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md)" && test "$(git rev-parse --abbrev-ref HEAD)" = "gsd/v1.23-py32f071-integration"</automated>
  </verify>
  <done>One meta commit on the milestone branch containing only the seed; `REQUIREMENTS.md`, `ROADMAP.md` and the firmware repo all unmoved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed prose → future milestone planner | `/gsd-new-milestone` and future scoping read seeds to seed scope, so a stale seed misdirects planning. |
| seed prose → future reader | A reader who opens the seed alone must be able to tell what shipped and what did not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-14 | Repudiation | the seed's status and its superseded bootloader-size number | medium | mitigate | The status value states what landed and what did not; the superseded number is flagged in place with a pointer to the replacement figure and its cost; the supersession is explicitly scoped to that one number so the rest of the seed is not quietly devalued. |
| T-129-15 | Tampering | the seed frontmatter schema | low | mitigate | The four-field schema is asserted unchanged and no new key is added; the update is carried in body prose, matching the convention every other seed follows. `trigger_condition` is asserted byte-unchanged so the evidence that the trigger fired is not erased. |
| T-129-08 | Repudiation | the stale milestone-slot reference | low | transfer | Flagged in place with Phase 130 CLOSE-03 named as its owner, rather than rewritten here — two phases editing the same claim in different directions is how a correction gets lost. |
| T-129-04 | Elevation of Privilege | any task in this plan | critical | mitigate | Structural separation: no `git push`, no `gh workflow run`, no PR filing. |
| T-129-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and adds no dependency to any manifest; RESEARCH §"Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` from `/workspaces/firestarter` → `221 passed`, zero failed, zero skipped.
- Frontmatter key count `4`, closing delimiter still on line 6, only the `status` line changed.
- Body contains the relative link target, `FUT-N05` and `CLOSE-03`.
- One meta commit, one file; firmware HEAD unchanged from `129-07`.
</verification>

<success_criteria>
- The seed's status reflects the fired trigger and the partial realisation, within its unchanged four-field schema.
- The seed points at the record as canonical and names FUT-N05 as the owner of the remaining work.
- The one superseded number is flagged and scoped; the stale slot reference is flagged and transferred to CLOSE-03.
- The gate module reaches 41/41 and the firmware suite 221 passed.
- No requirement ticked; `REQUIREMENTS.md`/`ROADMAP.md` untouched; no firmware file touched.
- No `git push`, no `gh workflow run`.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-08-SUMMARY.md` when done.
Record verbatim: the old and new `status` values, the pytest summary line, and the meta commit SHA.
</output>
