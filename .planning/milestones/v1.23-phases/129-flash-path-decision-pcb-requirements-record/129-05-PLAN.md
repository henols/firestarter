---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 05
type: execute
wave: 5
depends_on: ["129-04"]
files_modified:
  - .planning/v1.23-FLASH-PATH-DECISION.md
autonomous: true
requirements: [PCB-04, PCB-05]

must_haves:
  truths:
    - "The record names a specific USB VID/PID decision — pid.codes VID 0x1209, interim 1209:0001, target 1209:<allocated> — with its sourcing basis (PCB-04 / D-07 / OP-2)."
    - "The record states the placeholder's real provenance: 0x36B7 is allocated to Puya Semiconductor and the exact pair is copied verbatim from the pinned SDK's own CDC example, with file path, line numbers and the pinned SDK SHA (C-2 / OP-2)."
    - "The ship gate is a checkable condition, present verbatim, not an adjective (D-09)."
    - "usb_cdc.c is NOT edited: PCB-04 is satisfied by a recorded decision plus a tracked obligation (D-06)."
    - "The socket-empty instruction is present with the reason it is stronger here than the comparable warning elsewhere — the provisional map's direction hazard, and the fact that no Firestarter code runs during a DFU install (PCB-05 / F-13)."
    - "The record states its own edges: what is not yet decidable is named, and the pid.codes queue figures are stated as measured-on-a-date rather than as current (F-7)."
  artifacts:
    - .planning/v1.23-FLASH-PATH-DECISION.md
  key_links:
    - "§5 records that host discovery is by interface class 0xFE/0x01, not VID/PID, so nobody reads PCB-04 as a host bug."
    - "§8 is where the operator follow-up, the CLOSE-01 prose sweep and the CLOSE-02 ledger inputs are tracked — nothing outward-facing is performed by an agent."
    - "## Claim ceiling defers to REQUIREMENTS.md §\"Validation Ceiling\" by reference rather than restating its wording, avoiding the Phase 125 self-reference trap."
---

<objective>
Finish the authoritative record: §5 the USB vendor and product identity decision (PCB-04), §6 the
socket-empty instruction (PCB-05), §7 the rejected-route survey, §8 the tracked obligations, §9 the
open questions, and the closing claim ceiling.

Purpose: PCB-04's "liability the moment a board ships" becomes a condition a future reader can
fail, and PCB-05's instruction lands with the specific reason it is stronger here. §7–§9 are the
meta-only half that makes this the authoritative layer rather than a longer copy of the subset.

Output: the record is complete; four more of the gate's 24 RED legs go green.

**This plan may NOT mark any requirement Complete.** Only `129-09` may tick PCB-01…PCB-05. Do not
edit `REQUIREMENTS.md` or `ROADMAP.md`.

**Repo scope (D-04):** the meta repo only. `firestarter_app` is READ-ONLY — cite `py32_dfu.py` as
evidence, never edit it.

**Standing rule and D-08:** no task in this plan runs `git push` or `gh workflow run`, and **no
agent files the pid.codes pull request.** That request is an outward-facing action under the
operator's name; this plan records it as an obligation and stops there. Structural separation is
the control — `--auto`/`--chain` auto-approve human-verify gates, so a checkpoint would not hold.
</objective>

## Artifacts this phase produces

See `129-01-PLAN.md` §"Artifacts this phase produces" for the phase-wide list.

**Created by THIS plan:** the heading anchors
`## 5. USB vendor and product identity [SHARED:S4]`,
`## 6. Socket empty before any PY32F071 firmware install [SHARED:S5]`,
`## 7. Candidate survey — the rejected routes`,
`## 8. Consequences and tracked obligations`,
`## 9. Open questions`, and `## Claim ceiling`.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/v1.23-FLASH-PATH-DECISION.md
@.planning/REQUIREMENTS.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §5 — USB vendor and product identity [SHARED:S4]</name>
  <files>.planning/v1.23-FLASH-PATH-DECISION.md</files>
  <read_first>
    - `firestarter/platform/py32f071/src/usb_cdc.c` lines 19–36 — the two `#define`s and their use in the descriptor initialiser, so the record cites the exact lines. **Read only; D-06 forbids editing this file.**
    - `firestarter_app/firestarter/py32_dfu.py` around line 441 — `find_dfu_interfaces`'s docstring stating discovery is by interface class, not VID/PID. **READ-ONLY, D-04.**
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §C-2, §F-6, §F-7, §F-12, §F-17 — the provenance, the pid.codes process and its prerequisites, the measured queue figures and their volatility note, and the `0x0448` clarification.
    - `.planning/REQUIREMENTS.md` §"Out of Scope" — the `0x0448` row, which this section confirms rather than contradicts.
    - `firestarter/tests/test_flash_path_record_sync.py` — `_L2_SHIP_GATE` and `_S4_NEEDLES` exactly as `129-02` fixed them.
  </read_first>
  <action>
Append `## 5. USB vendor and product identity [SHARED:S4]`, marker last on the heading line.

Write it in six parts.

**(a) What the descriptor currently presents, and where it came from.** State that
`platform/py32f071/src/usb_cdc.c` defines the vendor id `0x36B7` and product id `0xFFFF` at lines
20 and 24 and consumes them in the device-descriptor initialiser. Then the provenance: the vendor
id is registered to Puya Semiconductor (Shanghai) Co., Ltd. `[CITED: the-sz.com USB ID database,
accessed 2026-08-02 — single-source for the allocation holder]`, and the exact pair is copied
verbatim from the pinned SDK's own USB CDC example at
`Projects/PY32F071-STK/Applications/USB_Device/USBD_Virtual_COM_Port/Src/usbd_cdc_if.c` lines 9–10,
whose companion Windows driver INF `pycdc.inf` lines 28 and 31 matches it, both at
`GIT_TAG 0ed2f4b4d3391eccfd4491006a30295fd78e32c2` `[VERIFIED: pinned SDK blobs]`. State the
consequence in one sentence a reader cannot misread: the board does not squat an empty slot — it
presents **another company's registered vendor identity**, specifically the silicon vendor's, on a
product they did not make. Add that every PY32 project starting from Puya's CDC example without
changing the descriptor presents the same pair, so a collision between two such devices on one
host is a common failure mode rather than a hypothetical one. Note that the values are undocumented
*in this tree* — no comment says where they came from — but are fully traceable upstream, and that
this record is where that traceability now lives.

**(b) The decision.** pid.codes, vendor id `0x1209` — the established free registry for
open-source hardware. The **target** identity is an allocated `1209:<pid>`. The **interim**
identity is `1209:0001`, the registry's documented private-testing product id, whose own terms
permit exactly this use and forbid shipping. Adopt its warning obligation in this record's own
words: the interim id is not universally unique and must not be used on any device that is
redistributed, sold or manufactured, and any source or configuration referencing it must say so.
State plainly why the interim id is strictly better than the status quo: it replaces another
company's registered identity with one explicitly sanctioned for this use, and it does not weaken
the ship gate, because the test id's own terms forbid shipping.

**(c) The ship gate.** On its own line and **verbatim**, character for character, matching the
gate's `_L2_SHIP_GATE` constant:
`Ship gate: no PY32F071 board ships, and no release advertises a USB identity, until a PID allocated under VID 0x1209 exists.`
It may be wrapped in `**` bold markers; nothing else may be inserted inside it. Follow it with one
sentence saying this is deliberately a condition rather than a warning, so a future reader can fail
it.

**(d) What this phase does and does not change.** `usb_cdc.c` is **not** edited (D-06): PCB-04 is
satisfied by a recorded decision plus a tracked obligation, which is what keeps this phase free of
an ARM rebuild. Editing the descriptor follows the allocation, which follows an operator-filed
public pull request, and it will need an ARM build to stay honest. State the host-side fact
explicitly so PCB-04 is never read as a host bug: `firestarter_app/firestarter/py32_dfu.py`
discovers DFU devices by **interface class** `0xFE/0x01`, not by vendor and product id, because the
identity the Puya bootloader presents is not confirmed. Also record, from the same family of
confusions, that `0x0448` is a bootloader-parameter table **device** id sitting beside a separate
bootloader id column — it is not a USB product id, and this record confirms `REQUIREMENTS.md`
§"Out of Scope" is correct on that point. One further line: the datasheet §2.3 describes the boot
loader as downloading through the USART interface and does not mention USB, while UM1504 documents
USB DFU for this part — UM1504 is the authority for the USB DFU capability, and a reader who checks
the datasheet first should not conclude the DFU path is imaginary.

**(e) How the allocation is obtained, and what it depends on.** Summarise the process in three
sentences: fork the registry repository, add an organisation page and a product-id page carrying a
named open-source license, open a pull request; ids are allocated in the order pull requests are
submitted. Then the two facts CONTEXT.md does not account for.
*Sequencing:* the registry's own instructions require a publicly available repository containing
modifiable PCB design files as well as source, and say to hold off requesting an id until the
project meets them — so the request may not be fileable until a schematic exists, putting the
allocation downstream of the very board the ship gate protects. The registry's FAQ is softer,
saying a project need not have shipped; the two are in mild tension and are resolved case by case,
so confidence that a firmware-only request would be accepted is **LOW**
`[ASSUMED — the howto and the FAQ disagree; the operator can resolve it by asking]`.
*Latency:* state the queue figures **as measured on a date, never as current** —
`[VERIFIED: GitHub API, measured 2026-08-02]` 64 open pull requests, the oldest opened 2026-01-20,
and no merge to the registry's default branch since 2026-04-29 — immediately followed by an
explicit sentence that these are volatile figures that must be re-read before being relied on.
Conclude with the practical consequence: treat "file the pull request" and "receive the id" as two
separately tracked events, and file early.

**(f) Who does what.** The phase decides the route; it does not request the id. The allocation is a
public pull request filed under the operator's name — **no agent files it** (D-08). It is tracked
in §8.

Throughout, phrase the provisional-pin-map and validation language so that the words `pin map`
are never immediately followed, with or without an intervening `is`, by `correct`, `verified` or
`validated`; and avoid the hyphenated or spaced forms of `bench validated`, `hardware validated`,
`silicon verified` and `works end to end`. These are the phrases
`.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` fails on
within one line of a `py32` token, and `129-09` scans this file with that checker.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 5\. USB vendor and product identity \[SHARED:S4\]$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - `grep -cF 'Ship gate: no PY32F071 board ships, and no release advertises a USB identity, until a PID allocated under VID 0x1209 exists.' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - The §5 span contains every `_S4_NEEDLES` entry: `0x1209`, `1209:0001`, `pid.codes`, `0x36B7`, `0xFFFF`, `Puya Semiconductor`, `usbd_cdc_if.c`, `pycdc.inf`, `0ed2f4b4d3391eccfd4491006a30295fd78e32c2`, `0x0448`, `py32_dfu.py`, `0xFE/0x01`.
    - The §5 span contains the literals `measured 2026-08-02`, `2026-04-29`, `UM1504`, and `no agent files it` (case-insensitive for the last).
    - The §5 span contains the literal `usb_cdc.c` together with the words `not` and `edited` in the same sentence, and `git -C /workspaces/firestarter status --porcelain` outputs zero bytes — the file really is untouched.
    - `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md` exits non-zero ONLY because the claim ceiling is not written yet; after Task 4 it must exit 0. Record the exit code and message at this point in the SUMMARY.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_meta_extract_is_non_vacuous[S4]" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_vid_pid_decision_and_ship_gate[meta]" -q` reports `2 passed`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/v1.23-FLASH-PATH-DECISION.md && test "$(grep -cF 'Ship gate: no PY32F071 board ships, and no release advertises a USB identity, until a PID allocated under VID 0x1209 exists.' $D)" = "1" && for s in '0x1209' '1209:0001' 'pid.codes' '0x36B7' '0xFFFF' 'Puya Semiconductor' 'usbd_cdc_if.c' 'pycdc.inf' '0x0448' 'py32_dfu.py' '0xFE/0x01' 'measured 2026-08-02' '2026-04-29' 'UM1504'; do grep -qF "$s" $D || { echo "MISSING: $s"; exit 1; }; done && test -z "$(git -C /workspaces/firestarter status --porcelain)" && cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_meta_extract_is_non_vacuous[S4]" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_vid_pid_decision_and_ship_gate[meta]" -q 2>&1 | tail -1 | grep -q '^2 passed'</automated>
  </verify>
  <done>§5 records the corrected provenance, the pid.codes decision with its interim identity, the verbatim ship gate, the sequencing constraint, the date-stamped queue figures and the no-agent-files-it rule — with `usb_cdc.c` demonstrably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: §6 — Socket empty before any PY32F071 firmware install [SHARED:S5]</name>
  <files>.planning/v1.23-FLASH-PATH-DECISION.md</files>
  <read_first>
    - `firestarter/platform/py32f071/README.md` §"Provisional example pin map" and §"Hardware validation still required" — the provisional declaration, the `RURP_PY32F071_PINMAP_PROVISIONAL` marker, and the nearest existing text this instruction anchors to.
    - `firestarter/include/boards/py32f071_rurp_shield.h` — the provisional marker in its actual definition site.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §F-12 and §F-13 — the confirmed premises and the four-part reason the warning is stronger here.
    - `firestarter/tests/test_flash_path_record_sync.py` — `_L3_SOCKET_EMPTY` and `_S5_NEEDLES` as `129-02` fixed them.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` `FORBIDDEN_PATTERNS` — the eight patterns and the one-line proximity window, so this section's provisional-map language does not trip the gate `129-09` runs.
  </read_first>
  <action>
Append `## 6. Socket empty before any PY32F071 firmware install [SHARED:S5]`, marker last.

Open with the instruction itself, on its own line and **verbatim**, matching the gate's
`_L3_SOCKET_EMPTY` constant character for character, using U+2014 EM DASH for both dashes:
`Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.`
It may be wrapped in `**` bold markers; nothing else may be inserted inside it.

Then, under a short lead-in saying why this instruction is stronger here than the comparable
warning elsewhere in this project, four numbered reasons:

1. **The map is provisional by declaration, not by omission.** The board header defines
   `RURP_PY32F071_PINMAP_PROVISIONAL` as a marker and the README states that every assignment
   other than the ADC channel is a placeholder chosen for a simple contiguous bus. Say that
   plainly and without using the phrases the claim gate forbids — describe the map as provisional
   and unconfirmed against any board, rather than asserting anything about its correctness.
2. **The specific hazard is direction, not just identity.** A provisional map can assign a signal
   the wrong **direction**. A pin the real board wires to a PROM output, configured by the firmware
   as a push-pull output, creates a driver fight through the PROM's output stage — which destroys
   parts silently, on first power-up. This is qualitatively different from the AVR comparable,
   where the mapping has accumulated three board revisions of bench measurement since v1.0.
3. **The startup levels are asserted but unmeasured.** The README lists safe active-low `/CE` and
   `/OE` startup levels among what is implemented, and then, three sections later, lists validating
   those same startup levels among what is still required. Record the tension rather than picking
   a side: the code intends safety, and nothing has measured it `[UNVERIFIED-UNTIL-SILICON]`.
4. **A DFU install is the acute case.** Installing over DFU power-cycles and re-enumerates the
   board with `BOOT0` strapped high — a state in which the application's GPIO initialisation never
   runs at all. Whatever the factory bootloader leaves the pins in is what a seated chip sees.
   Neither the application's startup levels nor any Firestarter code is in control during a DFU
   install, which is precisely when a user is most likely to have a chip in the socket from the
   previous session.

Close with a short paragraph on placement — where this instruction is repeated and why: verbatim
in the firmware subset (`platform/py32f071/FLASH-PATH-AND-PCB.md`), and as a pointer plus the same
one-sentence instruction in `platform/py32f071/README.md` §"Hardware validation still required",
which is the nearest existing text a bring-up reader already opens. State that propagating it into
the host installer documentation, and any installer-time prompt, is **out of scope this phase**
because the host repository is untouched (D-04), and that it is tracked as an obligation in §8.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 6\. Socket empty before any PY32F071 firmware install \[SHARED:S5\]$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - `grep -cF 'Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - The §6 span contains every `_S5_NEEDLES` entry: `provisional`, `RURP_PY32F071_PINMAP_PROVISIONAL`, `direction`, `BOOT0`, `three board revisions`.
    - `grep -ciE 'pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' .planning/v1.23-FLASH-PATH-DECISION.md` returns `0`.
    - `grep -ciE 'bench[- ]validated|hardware[- ]validated|silicon[- ]verified|works[- ]end[- ]to[- ]end' .planning/v1.23-FLASH-PATH-DECISION.md` returns `0`.
    - `cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_meta_extract_is_non_vacuous[S5]" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_socket_empty_instruction_present[meta]" -q` reports `2 passed`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/v1.23-FLASH-PATH-DECISION.md && test "$(grep -cF 'Before any PY32F071 firmware install — DFU, SWD or otherwise — the PROM socket must be empty.' $D)" = "1" && for s in provisional RURP_PY32F071_PINMAP_PROVISIONAL direction BOOT0 'three board revisions'; do grep -qF "$s" $D || { echo "MISSING: $s"; exit 1; }; done && test "$(grep -ciE 'pin[[:space:]]+map[[:space:]]+(is[[:space:]]+)?(correct|verified|validated)' $D)" = "0" && test "$(grep -ciE 'bench[- ]validated|hardware[- ]validated|silicon[- ]verified|works[- ]end[- ]to[- ]end' $D)" = "0" && cd /workspaces/firestarter && python -m pytest "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_meta_extract_is_non_vacuous[S5]" "tests/test_flash_path_record_sync.py::TestFlashPathRecordSync::test_socket_empty_instruction_present[meta]" -q 2>&1 | tail -1 | grep -q '^2 passed'</automated>
  </verify>
  <done>§6 carries the verbatim instruction and all four reasons it is stronger here, without tripping any forbidden claim phrase.</done>
</task>

<task type="auto">
  <name>Task 3: §7 rejected routes, §8 tracked obligations, §9 open questions</name>
  <files>.planning/v1.23-FLASH-PATH-DECISION.md</files>
  <read_first>
    - `.planning/seeds/py32f071-no-external-tool-fw-install.md` §"Why every factory-bootloader route was rejected" and §"Open questions" — the four-row table verbatim and the seed's own remaining unknowns.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §F-6's rejected-route material, §"Open Questions" 1–5, and §"Architectural Responsibility Map".
    - `.planning/REQUIREMENTS.md` §"Future Requirements" — FUT-N04, FUT-N05, FUT-N06 verbatim.
    - `.planning/v1.9-COBS-DECISION.md` §4 `Candidate Survey` and its `Comparative Verdict Table` — the established shape for a per-option survey.
  </read_first>
  <action>
These three sections are **meta only** — they are what makes this the authoritative layer. They
carry no `[SHARED:Sn]` marker and must not be copied into the subset.

**`## 7. Candidate survey — the rejected routes`.** Two subsections.
*7.1 Install routes.* Reproduce the seed's four-row table — the vendor Windows-only tool, the
external `dfu-util` binary, a vendored Python DFU client over libusb, and the UART ISP tool — each
with the real cost that rejected it, and add the note that the third row was accepted as the
runner-up at operator request and is what shipped in v1.23. Each row must state a real cost, not a
strawman; a reader must be able to see why someone might reasonably have chosen it.
*7.2 USB identity routes.* Four options with their costs: keeping `36B7:FFFF` (rejected — it
presents another company's registered identity, §5(a)); a USB-IF vendor id (rejected — the
registry's own front page quotes 6000 USD, for a project with no board); a silicon-vendor product-id
program (several vendors run one, but none is applicable to a Puya part, which is itself the
finding); and pid.codes (chosen). State pid.codes' honest costs plainly rather than only its
benefits: it is not supported by, endorsed by or associated with the USB-IF; it confers no
certification and no logo rights; and its own FAQ acknowledges an explicitly contested legal
position. Close with a comparative verdict table.

**`## 8. Consequences and tracked obligations`.** A table with columns *Obligation*, *Owner*,
*Trigger*, *Notes*, listing at minimum:
- File the pid.codes pull request under the operator's name — **operator**, not any agent (D-08);
  triggered by §5(e)'s sequencing question being resolved; note that filing and allocation are two
  separately tracked events.
- Edit `platform/py32f071/src/usb_cdc.c` to the allocated identity — a later phase or milestone;
  triggered by the allocation landing; note it will need an ARM build to stay honest.
- Amend `REQUIREMENTS.md` PCB-03, `ROADMAP.md` Phase 129 criterion 3, and `FUT-N04`'s deferral
  reason to match §1.6's Correction 1 — **Phase 130 CLOSE-01**; note that this phase deliberately
  did not amend them, so the correction stays visible.
- Narrow `REQUIREMENTS.md` §"Validation Ceiling"'s statement about the ARM toolchain from
  *absent from this environment* to a delta-claims-only formulation — **Phase 130 CLOSE-01**;
  reference §"Claim ceiling" below for the interim wording this record uses.
- Cite §1.6's three corrections and §3's rows R1–R7 in the honesty ledger — **Phase 130 CLOSE-02**.
- Propagate §6 into `firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md`, and consider an
  installer-time prompt — Phase 130 or a later host phase; out of scope here because the host
  repository is untouched (D-04).
- Build the self-flash bootloader — **FUT-N05**, its own milestone; §4 holds its budget.
- Publish a raw-binary release asset — **FUT-N06**; triggered by FUT-N05 needing it.
- The seed at `.planning/seeds/py32f071-no-external-tool-fw-install.md` remains live for FUT-N05,
  with its status updated by this phase to reflect that its trigger has already fired (D-17); this
  record is canonical for the flash-path decision and the seed points at it (D-18).

**`## 9. Open questions`.** Five entries, each with *Known*, *Unclear* and *What would answer it*:
1. `nBOOT1`'s factory default, and whether a bad option-byte write can strand a board without SWD.
   Answerable from the reference manual's option-byte chapter, which this project already cites but
   which was not obtainable in this session. Note that this is the strongest justification for §3
   row R2.
2. Whether the USB PHY provides an internal software-controlled D+ pull-up, or a discrete 1.5 kΩ
   resistor to 3V3 is required. Answerable from the reference manual's USB chapter. Do not guess.
3. **Reboot into the bootloader: a protocol command, or strap-only?** Recorded as an open question
   with its board cost stated on **both** sides, not decided (D-15). Strap-only means the BOOT0
   jumper must stay reachable forever — §3 row R1 assumes exactly that. A protocol command could
   relax the requirement to a manufacturing-only test point, but only once it exists and is
   trusted; until then, relaxing R1 on the strength of software that has not been written would be
   the expensive kind of mistake this record exists to prevent. FUT-N04 owns the software half, and
   §1.6's Correction 1 notes that FUT-N04's stated deferral reason is wrong even if its deferral
   may still be right.
4. Whether a pid.codes request can be filed before a schematic exists (§5(e)). Cheaply resolvable
   by the operator asking the registry directly.
5. Whether the factory bootloader enumerates as something a standard DFU host can drive, and what
   identity it actually presents — the seed's own open question, answerable with one enumeration
   listing once silicon exists.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 7\. Candidate survey — the rejected routes$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - `grep -c '^## 8\. Consequences and tracked obligations$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - `grep -c '^## 9\. Open questions$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`.
    - None of `## 7`, `## 8` or `## 9` carries a `[SHARED:` marker: `grep -cE '^## [789]\..*\[SHARED:' .planning/v1.23-FLASH-PATH-DECISION.md` returns `0`.
    - §7 names all four install routes and all four identity routes, and contains the literals `6000` and `USB-IF`.
    - §8 contains the literals `CLOSE-01`, `CLOSE-02`, `FUT-N04`, `FUT-N05`, `FUT-N06`, `operator`, `usb_cdc.c`, and `PY32F071-FIRMWARE-INSTALL.md`.
    - §9 contains exactly five numbered entries and the literals `nBOOT1`, `1.5 kΩ`, `D-15`, `R1`, `R2`.
    - `grep -ciE 'MEM_MODE|trampoline|RAM vector copy' .planning/v1.23-FLASH-PATH-DECISION.md` still returns `0`.
    - `cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -1` reports `20 failed, 21 passed` — the four legs from Tasks 1 and 2 are the only new passes; §7–§9 add no gated content.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/v1.23-FLASH-PATH-DECISION.md && test "$(grep -c '^## 7\. Candidate survey — the rejected routes$' $D)" = "1" && test "$(grep -c '^## 8\. Consequences and tracked obligations$' $D)" = "1" && test "$(grep -c '^## 9\. Open questions$' $D)" = "1" && test "$(grep -cE '^## [789]\..*\[SHARED:' $D)" = "0" && for s in 'USB-IF' 'CLOSE-01' 'CLOSE-02' 'FUT-N04' 'FUT-N05' 'FUT-N06' 'usb_cdc.c' 'PY32F071-FIRMWARE-INSTALL.md' 'D-15'; do grep -qF "$s" $D || { echo "MISSING: $s"; exit 1; }; done && test "$(grep -ciE 'MEM_MODE|trampoline|RAM vector copy' $D)" = "0"</automated>
  </verify>
  <done>The three meta-only sections exist, carry no shared marker, name every tracked obligation with its owner, and record all five open questions including D-15's two-sided board cost.</done>
</task>

<task type="auto">
  <name>Task 4: The claim ceiling, the claim-gate run, and the commit</name>
  <files>.planning/v1.23-FLASH-PATH-DECISION.md</files>
  <read_first>
    - `firestarter/platform/py32f071/CONFIG-STORAGE.md` §"Claim ceiling" — the shape to copy: a blanket ceiling that defers to `REQUIREMENTS.md` §"Validation Ceiling" **by reference rather than restating its wording**. Its third sentence, about the ARM toolchain being absent from this environment, must **not** be copied — it is contradicted by this phase's own local build (RESEARCH C-3) and by D-13.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" — read it, then defer to it; do not reproduce its forbidden-phrase list in this document (the Phase 125 self-reference trap).
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — the required caveat pattern and the eight forbidden patterns, so the ceiling satisfies the checker rather than tripping it.
  </read_first>
  <action>
Append a final `## Claim ceiling` section. Four sentences, in this order:

1. The blanket statement, which must contain the caveat phrase the milestone's claim gate requires
   in the form it matches: state that **no PY32F071 hardware exists** as a physical board, and that
   nothing recorded in this document is a claim about behaviour observed on that silicon.
2. Where every figure above came from: quoted from a published reference document, corroborated
   from the pinned SDK's own sources, read from a file already in this tree, or measured in a local
   build performed for this phase.
3. The toolchain sentence, **reworded** from the `CONFIG-STORAGE.md` original: the ARM toolchain is
   not installed by default in this development environment but is installable from the same
   packages CI uses; a local build may support **delta** claims only — same tree, same toolchain,
   two builds — and never an absolute-size comparison against a CI figure, nor any claim that the
   image runs. Add one clause noting that `REQUIREMENTS.md` §"Validation Ceiling" still carries the
   older, now-inaccurate wording and that Phase 130's CLOSE-01 sweep owns narrowing it (§8).
4. The defer-by-reference sentence: the full list of claims this milestone may and may not make is
   recorded once, in `.planning/REQUIREMENTS.md` §"Validation Ceiling", and this document defers to
   that list **by reference rather than restating its wording**. Do not reproduce any forbidden
   phrase here — quoting them inside a compliance paragraph is what trips the gate when the file is
   scanned directly, which is the trap Phase 125 fell into.

Then run the claim gate against the finished record with an explicit target, and record the output
verbatim:
`python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md`
It must exit 0 and print a `PASS:` line naming the file. If it exits 1, fix the record — never the
checker, and never by narrowing a pattern.

Then commit in the meta repo on `gsd/v1.23-py32f071-integration`, verifying the branch before and
after. Stage exactly `.planning/v1.23-FLASH-PATH-DECISION.md`. Commit subject:
`docs(129-05): USB identity decision, socket-empty instruction, rejected routes and open questions`.
Body: name PCB-04, PCB-05, D-06, D-07, D-08, D-09, D-15, D-18, C-2, F-6 and F-7; state that no
requirement is ticked; state that no agent filed the pid.codes request; record the RED ledger
moving from 24 to 20 and the claim-gate exit code.

No `git push`, no `gh workflow run`.
  </action>
  <acceptance_criteria>
    - `grep -c '^## Claim ceiling$' .planning/v1.23-FLASH-PATH-DECISION.md` returns `1`, and it is the last `## ` heading in the file.
    - `grep -ciF 'no PY32F071 hardware exists' .planning/v1.23-FLASH-PATH-DECISION.md` returns at least `1`.
    - The ceiling contains the literals `delta`, `local`, `CLOSE-01`, and `by reference`.
    - The ceiling does NOT contain the phrase asserting the toolchain is absent from this environment: `grep -ciE 'are absent from this (dev)?(container|environment)' .planning/v1.23-FLASH-PATH-DECISION.md` returns `0`.
    - `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md` exits 0 and prints a line beginning `PASS:`.
    - `cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1` reports `20 failed, 201 passed`.
    - `git -C /workspaces log -1 --format=%s` starts with `docs(129-05):`; `git -C /workspaces show --stat --format= HEAD` lists exactly one file; `git -C /workspaces diff --name-only HEAD~1 HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` outputs nothing.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes and its HEAD is unchanged from `129-02`'s recorded value.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md | grep -q '^PASS:' && grep -qiF 'no PY32F071 hardware exists' .planning/v1.23-FLASH-PATH-DECISION.md && test "$(grep -ciE 'are absent from this (dev)?(container|environment)' .planning/v1.23-FLASH-PATH-DECISION.md)" = "0" && git log -1 --format=%s | grep -q '^docs(129-05):' && test -z "$(git diff --name-only HEAD~1 HEAD -- .planning/REQUIREMENTS.md .planning/ROADMAP.md)" && cd /workspaces/firestarter && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^20 failed, 201 passed'</automated>
  </verify>
  <done>The record is complete and passes the milestone claim gate with an explicit target; the RED ledger stands at 20; one meta commit landed with only the record staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → pid.codes / GitHub | Filing a public pull request under the operator's identity crosses out of the project. |
| descriptor → USB host | The device presents a vendor identity to every host it is plugged into. |
| record prose → future reader | A durable record's claims are trusted without re-derivation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-01 | Spoofing | the USB device descriptor's vendor identity | high | mitigate | §5 records the corrected provenance, names `1209:0001` as the interim identity whose own terms forbid shipping, and states the ship gate verbatim as a condition a reader can fail. `usb_cdc.c` is not edited this phase (D-06), so the mitigation is a recorded decision plus a tracked obligation in §8. |
| T-129-10 | Spoofing / Denial of Service | product-id collision with every unmodified Puya CDC example | medium | mitigate | Same as T-129-01; §5 states the collision is common rather than hypothetical. |
| T-129-04 | Elevation of Privilege | filing the pid.codes pull request | critical | mitigate | Structural separation, not a checkpoint: no agent files it, no task runs `git push` or `gh workflow run`. §8 records it as an operator obligation with its trigger. |
| T-129-03 | Repudiation / Information disclosure | the record's claims and the volatile queue figures | high | mitigate | Per-claim confidence tags; queue figures stated as measured-on-a-date with an explicit volatility sentence; a `## Claim ceiling` that defers by reference rather than restating forbidden phrases; verified by running the milestone claim gate against the file with an explicit target. |
| T-129-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing and adds no dependency to any manifest; RESEARCH §"Package Legitimacy Audit" records zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase. |
</threat_model>

<verification>
- `check_permitted_claims.py .planning/v1.23-FLASH-PATH-DECISION.md` → exit 0, `PASS:` line.
- `python -m pytest tests/ -q` from `/workspaces/firestarter` → `20 failed, 201 passed`.
- The three forbidden-phrase greps and the no-VTOR-workaround grep all return `0`.
- `git -C /workspaces/firestarter status --porcelain` → empty; firmware HEAD unchanged.
</verification>

<success_criteria>
- The authoritative record is complete: §1 through §9 plus the claim ceiling.
- PCB-04's decision, sourcing basis and verbatim ship gate are recorded; PCB-05's instruction and its four-part reason are recorded.
- Four more gate legs discharged; the RED ledger drops from 24 to exactly 20.
- No pid.codes request filed, no `git push`, no `gh workflow run`, no edit under `firestarter/` or `firestarter_app/`.
- No requirement ticked; `REQUIREMENTS.md` and `ROADMAP.md` untouched.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-05-SUMMARY.md` when done.
Record verbatim: the claim-gate command and its full output, the pytest summary line before and
after, the four discharged node ids, and the meta commit SHA.
</output>
