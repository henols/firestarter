---
phase: 129-flash-path-decision-pcb-requirements-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/tests/meta_presence.py
  - firestarter/tests/test_flash_path_record_sync.py
autonomous: true
requirements: [PCB-01, PCB-02, PCB-03, PCB-04, PCB-05]

must_haves:
  truths:
    - "A meta-repo presence probe exists in the firmware repo whose absent-repo claim names the exact marker path it resolved, so the claim is auditable rather than silent."
    - "A missing scan target under a PRESENT meta repo raises MissingScanTargetError; it is never downgraded to pytest.skip."
    - "One module-level shared-section extractor drives every assertion in the gate module — positive legs and planted-violation legs alike."
    - "A parse that finds nothing raises an AssertionError naming the phrase `vacuously true`; it never compares equal to another empty parse."
    - "All five of RESEARCH F-14's fail-open modes are demonstrated by committed fixtures that need no real record file to exist."
    - "`python -m pytest tests/ -q` reports 180 passed plus this module's own legs, with zero skipped in this devcontainer."
  artifacts:
    - firestarter/tests/meta_presence.py
    - firestarter/tests/test_flash_path_record_sync.py
  key_links:
    - "meta_presence.META_MARKER resolves to `<meta root>/.git`, which is a real directory for the superproject — probed with .exists(), never .is_dir()."
    - "`from tests.meta_presence import ...` resolves under both `python -m pytest tests/` and the `pytest` console script from the firmware repo root (proved at plan time; no conftest.py added)."
    - "_extract_shared_section() keys on `[SHARED:Sn]` markers on `## ` heading lines — the stable key both records and firestarter/CLAUDE.md name."
---

<objective>
Build the D-03 sync gate's **fail-closed half** before either record exists: the meta-repo
presence helper, the single shared-section extractor every later assertion will use, and
committed fixtures for all five of RESEARCH F-14's fail-open modes.

Purpose: this milestone has measured cross-repo source-scanning gates failing OPEN four times
(Phase 117), and A-7 measured five legs flipping PASS→SKIP at exit 0 with a false reason. Phase
123's doctrine is that a gate written after the content it judges can only bless what already
happened. So the machinery and its RED proofs land first, in a state where `pytest tests/` stays
fully green.

Output: `firestarter/tests/meta_presence.py` and the fail-closed half of
`firestarter/tests/test_flash_path_record_sync.py`, both green on commit.

**This plan may NOT mark any requirement Complete.** PCB-01…PCB-05 are listed in this plan's
`requirements` field because this module's assertions will eventually gate all five — but only
the closing plan `129-09` may tick them in `.planning/REQUIREMENTS.md`. Do not edit
`REQUIREMENTS.md` or `ROADMAP.md` in this plan (the Phase 116 4× premature-tick guard, restated
in Phases 125 and 126).

**Repo scope (D-04):** the firmware repo only. `firestarter_app` is READ-ONLY this phase — it may
be cited as an analog, never edited, and it currently carries unrelated uncommitted local changes
that must not be touched or cleaned.

**Standing rule:** no task in this plan runs `git push` or `gh workflow run`.
</objective>

## Artifacts this phase produces

Every symbol below is **created by Phase 129** and therefore does not exist in the tree yet. Any
source-grounding or drift check must treat these as new, not as unresolved references.

**New files (whole phase):**
- `.planning/v1.23-FLASH-PATH-DECISION.md` — authoritative meta record (plans 129-03/04/05)
- `firestarter/platform/py32f071/FLASH-PATH-AND-PCB.md` — firmware subset (plan 129-06)
- `firestarter/tests/meta_presence.py` — **this plan**
- `firestarter/tests/test_flash_path_record_sync.py` — **this plan** + plan 129-02
- `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-NONREGRESSION.md` — plan 129-09

**Modified files (whole phase):**
- `firestarter/platform/py32f071/linker/PY32F071xB_FLASH.ld` (comment only — plan 129-07)
- `firestarter/platform/py32f071/README.md` (plan 129-06)
- `firestarter/CLAUDE.md` (plan 129-06)
- `.planning/seeds/py32f071-no-external-tool-fw-install.md` (plan 129-08)
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, meta gitlink for `firestarter` (plan 129-09)

**New symbols in `firestarter/tests/meta_presence.py` (this plan):**
`_FW_REPO_ROOT` · `_DEFAULT_META_ROOT` · `META_ROOT` · `META_MARKER` · `META_PRESENT` ·
`META_ABSENT_REASON` · `requires_meta` · `MissingScanTargetError` · `meta_path(*parts)`

**New symbols in `firestarter/tests/test_flash_path_record_sync.py` (this plan):**
`_HERE` · `_FW_REPO_ROOT` · `_FW_DOC` · `_META_DOC_REL` · `_LINKER` · `_SEED_REL` ·
`_SHARED_KEYS` · `_SHARED_MARKER_RE` · `_extract_shared_section(text, key)` ·
`_shared_sections(text)` · `_assert_non_vacuous(value, source)` · `_git_hash_object(path)` ·
`_git_porcelain(path)` · `_run_gate_in_subprocess(env_overrides)` · `_synthetic_record(keys, bodies)` ·
class `TestFlashPathRecordSyncFailsClosed` with `test_absent_meta_root_skip_is_auditable_not_silent`,
`test_absent_meta_claim_can_never_be_false`, `test_present_root_with_missing_target_raises_not_skips`,
`test_marker_name_is_not_overridable`, `test_empty_extraction_is_not_a_vacuous_pass`,
`test_renamed_marker_yields_a_refusal_not_a_guess`, `test_duplicate_marker_refuses_to_guess`,
`test_planted_divergence_in_synthetic_copies_is_detected`, `test_dirty_tree_is_detected`,
`test_git_binary_is_required_not_optional`

**New symbols added by plan 129-02 (same module):** class `TestFlashPathRecordSync` with
`test_meta_extract_is_non_vacuous`, `test_fw_extract_is_non_vacuous`, `test_shared_sections_match`,
`test_three_tiers_and_non_retirement`, `test_pcb_checklist_rows_are_wellformed`,
`test_flash_budget_cites_reserved_map`, `test_bootloader_figure_carries_its_cost`,
`test_vid_pid_decision_and_ship_gate`, `test_socket_empty_instruction_present`,
`test_linker_comment_cross_references_record`, `test_seed_status_is_no_longer_dormant`,
`test_planted_mutation_of_the_real_subset_is_detected`, plus `_meta_doc()`.

**Document anchors the gate keys on (created by plans 129-03…129-06):**
`[SHARED:S1]` (three-tier flash path) · `[SHARED:S2]` (PCB checklist) · `[SHARED:S3]` (flash
budget) · `[SHARED:S4]` (USB VID/PID) · `[SHARED:S5]` (socket-empty instruction) — each appearing
exactly once as a suffix on a `## ` heading line in **each** of the two records.

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-CONTEXT.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md
@.planning/phases/129-flash-path-decision-pcb-requirements-record/129-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight pin — branches, clean trees, suite baseline</name>
  <files>(no files written)</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7 (`gsd-tools query commit` can switch branches on a stale milestone regex)
  </read_first>
  <action>
Record the starting state so every later claim in this phase is anchored.

Run and capture verbatim: `git -C /workspaces rev-parse --abbrev-ref HEAD` (expect
`gsd/v1.23-py32f071-integration`); `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD`
(expect `v1.23-py32f071-integration`); `git -C /workspaces/firestarter rev-parse HEAD` (expect
`7a0a375de7e71ed3e9108b9531fffb59d8d95cd8`); `git -C /workspaces/firestarter status --porcelain`
(expect zero lines).

Then capture the pytest baseline: `cd /workspaces/firestarter && python -m pytest tests/ -q -rs`
and record the summary line (expected `180 passed`, zero skipped).

Do NOT run `git -C /workspaces/firestarter_app status --porcelain` as a gate — that repo carries
unrelated uncommitted local changes (D-04) and is read-only this phase. Do not touch or clean them.

If the firmware HEAD is not `7a0a375de7e71ed3e9108b9531fffb59d8d95cd8`, or either branch differs,
STOP and report rather than proceeding — every non-regression claim in `129-09` is anchored to
these values.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` outputs exactly `gsd/v1.23-py32f071-integration`.
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs exactly `v1.23-py32f071-integration`.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes.
    - `python -m pytest tests/ -q` (run from `/workspaces/firestarter`) reports `180 passed` and reports no `skipped` count.
    - The four captured values are quoted verbatim in this plan's SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration" && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^180 passed'</automated>
  </verify>
  <done>Both branches confirmed, firmware tree clean at `7a0a375`, suite baseline recorded as `180 passed / 0 skipped`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: firestarter/tests/meta_presence.py — the auditable presence probe</name>
  <files>firestarter/tests/meta_presence.py</files>
  <read_first>
    - `firestarter_app/tests/fw_presence.py` — the analog, in full (READ-ONLY, D-04). All four load-bearing parts must be mirrored: the env seam + unrenameable marker (lines 65–102), `MissingScanTargetError` (105–114), the accessor `fw_path` (117–140), and the import-time-binding warning in the docstring (35–45).
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md` §4 — the verbatim excerpts and the exact 129 substitutions.
    - `firestarter/tests/test_config_storage_design_vendored.py` lines 1–35 and 68–74 — the firmware repo's module-docstring house style and the no-conftest path idiom.
  </read_first>
  <behavior>
    - Given `FIRESTARTER_META_ROOT` unset, `META_ROOT` resolves to the parent directory of the firmware repo root (i.e. `/workspaces`), because the firmware repo is a **submodule of** the meta repo, not a sibling of it.
    - Given `FIRESTARTER_META_ROOT` set to a directory containing no `.git`, `META_PRESENT` is False and `META_ABSENT_REASON` contains the resolved absolute marker path.
    - Given a present meta root and a relative path that does not exist under it, `meta_path(...)` raises `MissingScanTargetError` whose message contains the resolved absolute path, the marker that proved presence, and the instruction to update the path rather than delete the gate.
    - Given an ABSENT meta root, `meta_path(...)` returns the path WITHOUT raising (callers are behind `requires_meta`; raising would turn an honest skip into a collection-time error).
    - `META_MARKER.name` is `.git` and no environment variable can change it.
  </behavior>
  <action>
Create `firestarter/tests/meta_presence.py`, mirroring `firestarter_app/tests/fw_presence.py`
part for part with these substitutions.

Module docstring: the repo's standard four-line MIT header (`Project Name: Firestarter` /
`Copyright (c) 2024 Henrik Olsson` / blank / `Permission is hereby granted under MIT license.`),
then `Phase 129 Plan 01 -- D-03's meta-repo presence probe for the flash-path record sync gate.`,
then `Requirements: PCB-01, PCB-02, PCB-03, PCB-04, PCB-05 (presence-probe slice only -- this
module does NOT close any of them, and no requirement is marked complete in REQUIREMENTS.md from
this plan).`, then `Decisions covered: D-03, D-04`.

The docstring must additionally carry four paragraphs, all reproduced in substance from the
analog:

(a) **Direction warning, unique to this module.** State that the relationship here is
*parent*, not sibling: `firestarter` is a **submodule of** the meta repo, so the meta root is
`Path(__file__).resolve().parent.parent.parent` — one level further up than
`fw_presence.py`'s sibling arithmetic. State that under `actions/checkout` of `henols/firestarter`
that parent directory is the runner work directory and `.planning/` does not exist there at all
(RESEARCH F-14).

(b) **`.git` is probed with `.exists()`, never `.is_dir()`.** A submodule checkout stores `.git`
as a *file*; the superproject's is a *directory*. Here the marker being probed is the
superproject's, so it is normally a directory — but the code must not depend on that, because the
same probe runs against `tmp_path` fixtures in `test_flash_path_record_sync.py`.

(c) **Import-time binding.** Reproduce the analog's warning: `META_ROOT`, `META_MARKER`,
`META_PRESENT`, `META_ABSENT_REASON` and `requires_meta` are all evaluated once, at import
(`pytest.mark.skipif` binds at collection), so `monkeypatch.setenv` has **no effect**. A test
needing a different root must invoke pytest in a **subprocess** with `FIRESTARTER_META_ROOT` set
in the child environment.

(d) **CI coverage, stated honestly.** Copy `test_config_storage_design_vendored.py` lines 15–20
verbatim in form: this module executes in NO CI leg on this branch — `pytest tests/ -v` runs only
in `build.yml` (push/PR to `main`) and `beta-build.yml` (push to `beta`), neither of which fires on
this firmware milestone branch, and `py32f071.yml` has no pytest step at all. The local run
recorded in this phase's evidence artifact is the only evidence this module's assertions were
ever exercised. Never imply CI coverage.

Body, stdlib + pytest only (no third-party import, nothing under `.pio/`):

- `_FW_REPO_ROOT = Path(__file__).resolve().parent.parent`
- `_DEFAULT_META_ROOT = _FW_REPO_ROOT.parent`
- `META_ROOT: Path = Path(os.environ.get("FIRESTARTER_META_ROOT", str(_DEFAULT_META_ROOT)))` —
  this is the module's ONLY read of `os.environ`. The seam overrides the **root only**; the
  marker name is deliberately not overridable, with a comment saying so in the analog's words
  ("one more knob that can be set wrong in a real run").
- `META_MARKER: Path = META_ROOT / ".git"`
- `META_PRESENT: bool = META_MARKER.exists()`
- `META_ABSENT_REASON: str` — ONE canonical reason string shared by every caller, of the form
  `meta repo checkout absent (no <resolved absolute marker path> marker)`. The resolved path must
  be interpolated so the claim is auditable by a reader.
- `requires_meta = pytest.mark.skipif(not META_PRESENT, reason=META_ABSENT_REASON)`
- `class MissingScanTargetError(Exception)` with the analog's docstring adapted: raised when the
  meta repo IS present but a named path under it is not; this is the hard-failure half of the
  split, and it must never be silently downgraded to a skip, because that is exactly the
  fail-open behaviour A-7 measured.
- `def meta_path(*parts: str) -> Path` — joins onto `META_ROOT`; if `META_PRESENT` and the
  resolved path does not exist, raises `MissingScanTargetError` naming the resolved absolute
  path, the marker that proved the repo present, and the instruction to update the path (or the
  cross-repo scan-path inventory) rather than removing or bypassing the gate; if the repo is
  absent, returns the path without raising.

Do NOT create a `conftest.py`, `pytest.ini`, `pyproject.toml`, `setup.cfg` or `tox.ini` — the
firmware repo deliberately has none anywhere, and per-module path resolution is the recorded
house rule (PATTERNS S3).
  </action>
  <acceptance_criteria>
    - `firestarter/tests/meta_presence.py` exists and imports cleanly: `cd /workspaces/firestarter && python -c "from tests.meta_presence import META_ROOT, META_MARKER, META_PRESENT, META_ABSENT_REASON, requires_meta, MissingScanTargetError, meta_path"` exits 0.
    - `python -c "from tests import meta_presence as m; print(m.META_ROOT)"` prints exactly `/workspaces`.
    - `python -c "from tests import meta_presence as m; assert m.META_MARKER.name == '.git'"` exits 0.
    - `python -c "from tests import meta_presence as m; assert m.META_PRESENT is True"` exits 0.
    - `python -c "from tests import meta_presence as m; assert str(m.META_MARKER) in m.META_ABSENT_REASON"` exits 0 — the absent-claim names the marker it resolved.
    - `grep -c 'os.environ' tests/meta_presence.py` returns exactly `1`, and `grep -c 'FIRESTARTER_META_ROOT' tests/meta_presence.py` returns at least `1`; `grep -c 'FIRESTARTER_META_MARKER' tests/meta_presence.py` returns `0`.
    - `grep -c 'is_dir()' tests/meta_presence.py` returns `0`.
    - The docstring contains the literal substrings `Requirements: PCB-01, PCB-02, PCB-03, PCB-04, PCB-05`, `does NOT close`, `Decisions covered: D-03, D-04`, `NO CI leg on this branch`, and `subprocess`.
    - `ls tests/conftest.py pytest.ini pyproject.toml setup.cfg tox.ini` reports every one of the five as absent.
    - `python -m pytest tests/ -q` still reports `180 passed` (a new non-test module must not change the collected count).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python -c "from tests import meta_presence as m; assert str(m.META_ROOT)=='/workspaces'; assert m.META_MARKER.name=='.git'; assert m.META_PRESENT is True; assert str(m.META_MARKER) in m.META_ABSENT_REASON" && test "$(grep -c 'os.environ' tests/meta_presence.py)" = "1" && test "$(grep -c 'FIRESTARTER_META_MARKER' tests/meta_presence.py)" = "0" && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^180 passed'</automated>
  </verify>
  <done>`meta_presence.py` exists, resolves `/workspaces` as the meta root, keys presence on an unrenameable `.git` marker, names that marker in its absent-reason string, raises rather than skips on a missing target under a present root, and leaves the suite at 180 passed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_flash_path_record_sync.py — extractor, helpers, and all five F-14 fail-open fixtures</name>
  <files>firestarter/tests/test_flash_path_record_sync.py</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-PATTERNS.md` §3, §3a, §3b and §S1 — the module docstring shape, the `_extract_section` helper to reuse rather than rewrite, the non-vacuity guard whose `vacuously true` phrase is load-bearing, the parser-that-refuses-to-guess shape, the git helpers, the planted-mutation ceremony, and the two-class split.
    - `firestarter/tests/test_config_storage_design_vendored.py` — the same-repo module in full: docstring, numbered `Coverage:` block, needle-tuple convention, single-helper rule.
    - `firestarter_app/tests/test_py32_asset_name_host.py` (READ-ONLY, D-04) — the assertion architecture: `_assert_non_vacuous_name`, the multi-candidate refusal, `_git_hash_object`/`_git_porcelain`, the planted-mutation ceremony, the two-class split.
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §F-14 — the five failure modes each fixture must demonstrate.
  </read_first>
  <behavior>
    - `_extract_shared_section(text, "S3")` returns the body between the unique `## ` heading line carrying `[SHARED:S3]` and the next `## ` heading line (exclusive), with the heading line itself excluded and trailing blank lines stripped.
    - It returns `None` when no `## ` heading line carries the marker (a renamed marker is a refusal, not a silent empty match).
    - It raises `AssertionError` containing `refusing to guess` when two or more `## ` heading lines carry the same marker.
    - `_assert_non_vacuous(None, src)` and `_assert_non_vacuous("   ", src)` each raise `AssertionError` whose message contains `vacuously true`.
    - Two synthetic records with identical `[SHARED:Sn]` bodies compare equal; mutating one body makes them compare unequal.
    - Running the module in a subprocess with `FIRESTARTER_META_ROOT` set to an empty directory exits 0, reports skips, and the skip reason names the resolved absent marker path — an auditable skip, never a silent one.
    - `meta_path("__definitely_not_a_real_file__.md")` raises `MissingScanTargetError` under the present real meta root.
  </behavior>
  <action>
Create `firestarter/tests/test_flash_path_record_sync.py` containing ONLY the fail-closed half.
Plan `129-02` adds the parity/content class to this same module; do not write it here.

**Module docstring.** Repo MIT header, then `Phase 129 Plan 01 (fail-closed half) + Plan 02
(parity half) -- D-03's fail-closed sync gate over the two copies of the v1.23 flash-path and PCB
requirements record.` Then `Requirements: PCB-01, PCB-02, PCB-03, PCB-04, PCB-05 (mechanical
content-gate slice only -- this module does NOT close any of them, and no requirement is marked
complete in REQUIREMENTS.md from this plan).` Then `Decisions covered: D-01, D-03, D-04, D-11,
D-16, D-17`.

Then, in prose:

- **The shared-section contract.** Name the two copies (`.planning/v1.23-FLASH-PATH-DECISION.md`
  in the meta repo, authoritative; `platform/py32f071/FLASH-PATH-AND-PCB.md` in this repo,
  subset) and the five stable keys `S1` three-tier flash path, `S2` PCB checklist, `S3` flash
  budget, `S4` USB VID/PID, `S5` socket-empty instruction. State that the key lives as a
  `[SHARED:Sn]` suffix on a `## ` heading line in both copies, that only the **body** is compared
  (the meta copy numbers its headings and the subset does not), and that the same five keys are
  named in `firestarter/CLAUDE.md` so the human instruction and the machine gate cannot drift
  apart (v1.7 precedent, PATTERNS S6).
- **CI coverage, honestly.** Copy `test_config_storage_design_vendored.py` lines 15–20 verbatim in
  form: NO CI leg on this branch, `py32f071.yml` has no pytest step, the local run recorded in
  this phase's evidence artifact is the only evidence.
- **Single-helper rule.** Every test — positive legs and planted-violation legs alike — goes
  through the one module-level `_extract_shared_section` / `_shared_sections` pair, so the RED
  demonstrations exercise the same checking code the positive legs do. Two parallel
  implementations drift and the RED then proves nothing.
- **The RED-first disposition.** State that this module was committed before either record
  existed, that Plan 02's parity legs were therefore RED-by-construction on arrival, and that the
  RED took the form of `MissingScanTargetError` — the hard-failure half of the split — not a skip.
- A numbered `Coverage:` block, one line per test function, naming the function and the F-14 mode
  or requirement it discharges.

**Path constants** (the `_HERE`-relative idiom, PATTERNS S3): `_HERE`, `_FW_REPO_ROOT = _HERE.parent`,
`_FW_DOC = _FW_REPO_ROOT / "platform" / "py32f071" / "FLASH-PATH-AND-PCB.md"`,
`_META_DOC_REL = "v1.23-FLASH-PATH-DECISION.md"`,
`_LINKER = _FW_REPO_ROOT / "platform" / "py32f071" / "linker" / "PY32F071xB_FLASH.ld"`,
`_SEED_REL = "seeds/py32f071-no-external-tool-fw-install.md"`.
Note in a comment that `_META_DOC_REL` and `_SEED_REL` are resolved through
`meta_presence.meta_path(".planning", ...)`, never by string concatenation, so a missing target
under a present meta repo raises instead of skipping.

Import the helper as `from tests.meta_presence import META_ABSENT_REASON, META_MARKER,
META_PRESENT, META_ROOT, MissingScanTargetError, meta_path, requires_meta`. This sibling-package
import was proved to resolve at plan time under both `python -m pytest tests/ -v` and the bare
`pytest tests/ -v` console script from the firmware repo root; add a comment recording that it is
the **first** sibling-helper import in this repo's test suite and that it works because
`tests/__init__.py` exists and pytest's prepend import mode inserts the repo root on `sys.path`.
Do not add a `conftest.py`.

**Module-level constants:** `_SHARED_KEYS = ("S1", "S2", "S3", "S4", "S5")` and
`_SHARED_MARKER_RE` — a compiled regex matching a full line that starts with `## ` and ends with
`[SHARED:<key>]` (allow trailing whitespace). Build the per-key pattern from the key so a typo
cannot silently produce a pattern that matches nothing.

**Helpers, one implementation each:**

- `_extract_shared_section(text, key)` — adapt `test_config_storage_design_vendored.py`'s
  `_extract_section` (lines 104–125), keeping its docstring's reasoning about why span-scoping
  matters. Differences: candidate lines are located by the per-key marker regex, not a heading
  prefix; if more than one candidate line matches, raise `AssertionError` naming the count, the
  key and the matched lines, with the literal phrase `refusing to guess`; the returned body
  EXCLUDES the heading line and has trailing blank lines stripped; the span ends at the next line
  starting with `## `; return `None` if no candidate matches. Note in the docstring that returning
  `None` on a renamed marker is F-14 mode 4 and is exactly why the non-vacuity assertion is a
  separate test per parse.
- `_shared_sections(text)` — returns a dict mapping each key in `_SHARED_KEYS` to
  `_extract_shared_section(text, key)`. No filtering, no defaults: a missing key maps to `None`
  so the non-vacuity legs can see it.
- `_assert_non_vacuous(value, source)` — mirror `_assert_non_vacuous_name` (PATTERNS §3b): run
  BEFORE any comparison; assert the value is not `None` and `value.strip()` is non-empty; the
  message must contain the literal phrase `vacuously true`, name `source`, and cite research
  finding A-7. Note in the docstring that the phrase is load-bearing because the RED tests match
  on it.
- `_git_hash_object(path)` and `_git_porcelain(path)` — copy PATTERNS §3b lines 162–192 exactly in
  shape, including `shutil.which("git")` asserted non-`None` with the message that a missing
  binary must FAIL the suite, never be silently skipped.
- `_run_gate_in_subprocess(env_overrides, node_id=None)` — runs
  `[sys.executable, "-m", "pytest", <this module's path>, "-q", "-rs"]` with `cwd=_FW_REPO_ROOT`
  and `os.environ | env_overrides`, capturing text output; returns the `CompletedProcess`. State
  in its docstring that a subprocess is mandatory because `meta_presence`'s names bind at import
  and `pytest.mark.skipif` binds at collection, so `monkeypatch.setenv` has no effect. Guard
  against infinite recursion by refusing to run when the environment variable
  `FIRESTARTER_129_GATE_CHILD` is already set, and set that variable in the child.
- `_synthetic_record(bodies)` — builds a minimal markdown document string with a title line and,
  for each key in `_SHARED_KEYS`, a `## ` heading line ending in the marker followed by that
  key's body from the `bodies` mapping. Used by every synthetic fixture so no fixture hand-rolls
  document text.

**`class TestFlashPathRecordSyncFailsClosed`** — docstring: the pure RED demonstrations; none
carries `@requires_meta`; none needs either real record to exist; these are the legs that make
this gate a gate rather than a comment (RESEARCH §"Don't Hand-Roll" key insight). Ten tests:

1. `test_absent_meta_root_skip_is_auditable_not_silent` — F-14 mode 3. Create an empty
   `tmp_path` subdirectory (no `.git`); call `_run_gate_in_subprocess` with
   `FIRESTARTER_META_ROOT` pointed at it. Assert `returncode == 0`; assert the output contains
   the string `skipped`; assert the output contains the absolute path of the child's resolved
   marker (`<tmp dir>/.git`), proving the skip reason names what it resolved rather than making an
   unauditable claim; assert the output does NOT contain `failed`.
2. `test_absent_meta_claim_can_never_be_false` — the census assertion, in-process: if
   `META_MARKER.exists()` then `META_PRESENT` must be `True`; and `str(META_MARKER)` must be a
   substring of `META_ABSENT_REASON`. A skip claiming the meta checkout is absent while its
   marker exists is the A-7 shape and must be impossible by construction.
3. `test_present_root_with_missing_target_raises_not_skips` — F-14 mode 3's hard half. Under the
   real present meta root, `pytest.raises(MissingScanTargetError)` around
   `meta_path(".planning", "__definitely_not_a_real_file__.md")`; assert the message contains the
   resolved absolute path and the substring `update`. Use a name that will never exist so this
   leg is stable across every later wave.
4. `test_marker_name_is_not_overridable` — read `tests/meta_presence.py`'s own source; assert
   `os.environ` appears exactly once, that `FIRESTARTER_META_ROOT` appears, and that no other
   `FIRESTARTER_META_` identifier appears. The seam overrides the root only.
5. `test_empty_extraction_is_not_a_vacuous_pass` — F-14 mode 2. Build a synthetic document with
   no markers at all; assert `_shared_sections` maps every key to `None`; then assert
   `pytest.raises(AssertionError, match="vacuously true")` for `_assert_non_vacuous(None, ...)`
   and again for `_assert_non_vacuous("   \n", ...)`.
6. `test_renamed_marker_yields_a_refusal_not_a_guess` — F-14 mode 4. Build a synthetic document
   whose `S3` marker text is mutated to `[SHARED:S3x]`; assert `_extract_shared_section(text,
   "S3")` is `None` while `S1`, `S2`, `S4`, `S5` all extract non-empty; then assert the
   non-vacuity guard raises on the `S3` result. Also assert the mutation actually changed the
   text (the `assert mutated != original` line PATTERNS calls the most-often-dropped one).
7. `test_duplicate_marker_refuses_to_guess` — build a synthetic document with two `## ` heading
   lines both ending in `[SHARED:S2]`; assert `pytest.raises(AssertionError, match="refusing to
   guess")`.
8. `test_planted_divergence_in_synthetic_copies_is_detected` — F-14 mode 1. Build two synthetic
   documents with identical bodies; assert `_shared_sections` of each are equal. Then mutate the
   `S3` body of one copy (assert the mutation changed the text) and assert the two now compare
   unequal, and that the inequality is located in `S3` specifically, not merely somewhere.
9. `test_dirty_tree_is_detected` — F-14 mode 5. Build a throwaway git repo under `tmp_path`
   (`git init`, `git -c user.email=… -c user.name=… commit --allow-empty`), write an untracked
   file, and assert `_git_porcelain(tmp_repo)` is non-empty; then assert
   `_git_porcelain(_FW_REPO_ROOT)` is a `str` (calling it against the real repo must not raise).
   Do not assert the real repo is clean here — that assertion belongs to legs that write to
   `tmp_path`, and a mid-plan working tree is legitimately dirty.
10. `test_git_binary_is_required_not_optional` — assert `shutil.which("git")` is not `None`, and
    read this module's own source asserting the `shutil.which("git")` call is followed by an
    `assert`, so a future edit cannot convert it into a skip.

Every synthetic document is written under `tmp_path` only. No test in this class writes to any
path under `_FW_REPO_ROOT` or `META_ROOT`.

Stdlib and pytest only. No third-party import, no PlatformIO import, nothing under `.pio/` — a
dependency on `.pio/libdeps/` passes on a warm tree and fails on a clean checkout, which is the
fail-open shape this milestone has already paid for once (A-7).
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py -v` exits 0 with exactly 10 tests reported, all passed, zero skipped.
    - `python -m pytest tests/ -q` reports `190 passed` (the pre-existing 180 plus this module's 10) and no `skipped` count.
    - `grep -c 'vacuously true' tests/test_flash_path_record_sync.py` returns at least `2` (the helper message and the RED match).
    - `grep -c 'refusing to guess' tests/test_flash_path_record_sync.py` returns at least `2`.
    - `grep -c 'conftest' tests/test_flash_path_record_sync.py` may be non-zero (the house-rule note) but `ls tests/conftest.py` still reports the file absent.
    - `grep -cE '^(import|from) ' tests/test_flash_path_record_sync.py` resolves only to stdlib modules, `pytest`, and `tests.meta_presence`: `grep -E '^(import|from) ' tests/test_flash_path_record_sync.py` shows no module outside {`os`, `re`, `shutil`, `subprocess`, `sys`, `pathlib`, `pytest`, `tests.meta_presence`}.
    - `grep -c '\.pio' tests/test_flash_path_record_sync.py` returns `0`.
    - The docstring contains the literal substrings `does NOT close`, `NO CI leg on this branch`, `[SHARED:S1]`, `[SHARED:S5]`, and `Decisions covered: D-01, D-03, D-04, D-11, D-16, D-17`.
    - `git -C /workspaces/firestarter status --porcelain` lists only `tests/meta_presence.py` and `tests/test_flash_path_record_sync.py` as additions before the commit — no fixture escaped `tmp_path`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py -q -rs 2>&1 | tail -1 | grep -q '^10 passed' && python -m pytest tests/ -q 2>&1 | tail -1 | grep -q '^190 passed' && test "$(grep -c '\.pio' tests/test_flash_path_record_sync.py)" = "0"</automated>
  </verify>
  <done>The gate module's fail-closed half is green at 10/10, the whole suite is at 190 passed / 0 skipped, all five F-14 modes have a committed fixture, and no fixture wrote outside `tmp_path`.</done>
</task>

<task type="auto">
  <name>Task 4: Commit both files inside the firmware submodule</name>
  <files>firestarter/tests/meta_presence.py, firestarter/tests/test_flash_path_record_sync.py</files>
  <read_first>
    - `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-RESEARCH.md` §"Common Pitfalls" Pitfall 7 — check the branch before and after any commit step.
  </read_first>
  <action>
Commit **inside** the firmware submodule, on branch `v1.23-py32f071-integration`. Do not bump the
meta gitlink here — D-05 bumps gitlinks in-phase, and plan `129-09` owns that single bump for the
whole phase.

Verify `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` is
`v1.23-py32f071-integration` immediately before and immediately after the commit.

Stage exactly `tests/meta_presence.py` and `tests/test_flash_path_record_sync.py`. Commit message:
`test(129-01): meta-repo presence probe and the fail-closed half of the flash-path record sync gate`
with a body naming D-03, the five F-14 modes, and the recorded RED-first disposition.

No task in this plan runs `git push` or `gh workflow run`.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter log -1 --format=%s` starts with `test(129-01):`.
    - `git -C /workspaces/firestarter show --stat --format= HEAD` lists exactly two files, both under `tests/`.
    - `git -C /workspaces/firestarter status --porcelain` outputs zero bytes after the commit.
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` outputs `v1.23-py32f071-integration` after the commit.
    - `git -C /workspaces/firestarter log --oneline -1 --all --grep='gh workflow run'` returns nothing, and the session transcript contains no `git push` or `gh workflow run` invocation.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && git log -1 --format=%s | grep -q '^test(129-01):' && test "$(git show --stat --format= HEAD | grep -c 'tests/')" = "2" && test -z "$(git status --porcelain)" && test "$(git rev-parse --abbrev-ref HEAD)" = "v1.23-py32f071-integration"</automated>
  </verify>
  <done>Both new files are committed inside the firmware submodule on `v1.23-py32f071-integration`, the tree is clean, and no outward-facing command was executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta repo → firmware repo | The gate reads a document that lives in a different repository, reachable only through a filesystem path that does not exist in every checkout. |
| agent → outward-facing services | `git push`, `gh workflow run` and PR filing cross out of the project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-02 | Tampering (undetected) | `tests/test_flash_path_record_sync.py` | high | mitigate | Per-parse non-vacuity assertion with the load-bearing `vacuously true` phrase; single-helper rule so the RED legs exercise the same code as the positive legs; ten committed fixtures covering all five F-14 modes. |
| T-129-06 | Tampering | cross-repo parity of the two record copies | high | mitigate | Presence keyed on `<meta root>/.git`, a marker no in-repo rename can move; `MissingScanTargetError` on a present-root missing target; the absent-reason string names the resolved marker so the claim is auditable. |
| T-129-04 | Elevation of Privilege | any task in this plan | critical | mitigate | Structural separation, not a checkpoint: no task runs `git push` or `gh workflow run`. `--auto`/`--chain` auto-approve human-verify gates, so a checkpoint would not hold. |
| T-129-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. RESEARCH §"Package Legitimacy Audit" records the phase as not-applicable: no dependency is added to `platform/py32f071/CMakeLists.txt`, `platformio.ini` or any requirements file; zero `[ASSUMED]`, `[SUS]` or `[SLOP]` packages. The module imports stdlib and `pytest` only. |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python -m pytest tests/test_flash_path_record_sync.py -v` → 10 passed, 0 skipped.
- `cd /workspaces/firestarter && python -m pytest tests/ -q -rs` → `190 passed`, no skips.
- `git -C /workspaces/firestarter status --porcelain` → empty.
- `git -C /workspaces/firestarter log -1 --format='%s %H'` recorded verbatim in the SUMMARY for `129-09` to cite.
</verification>

<success_criteria>
- `firestarter/tests/meta_presence.py` exists, resolves `/workspaces` as the meta root, and names the resolved marker in its absent-reason string.
- `firestarter/tests/test_flash_path_record_sync.py` exists with exactly the ten fail-closed legs, all green.
- All five RESEARCH F-14 fail-open modes have a committed fixture that needs neither record to exist.
- The suite is at 190 passed / 0 skipped; the pre-existing 180 are untouched.
- No requirement is marked Complete; `REQUIREMENTS.md` and `ROADMAP.md` are unmodified by this plan.
- No `git push`, no `gh workflow run`, no edit under `firestarter_app/`.
</success_criteria>

<output>
Create `.planning/phases/129-flash-path-decision-pcb-requirements-record/129-01-SUMMARY.md` when done.
Record verbatim: both branch names, the firmware HEAD before and after, the two pytest summary
lines (180 baseline and 190 after), and the commit SHA — `129-09` re-checks each independently.
</output>
