---
phase: 127-host-dfu-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/py32_dfu.py
  - firestarter_app/firestarter/channel.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/firmware.py
  - firestarter_app/tests/test_py32_dfu.py
  - firestarter_app/pyproject.toml
  - firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md
  - firestarter_app/CLAUDE.md
autonomous: true
requirements: [HOST-01]

must_haves:
  truths:
    - "The landing is a **real merge commit** whose parent SHAs include `4ee64a14a8933b60896c8b168bb1c7e34d788fa4` (D-16). ROADMAP Criterion 1 requires this literally; a squash or a rebase would fail it. This is deliberately the **opposite** of Phase 124 D-05, and D-16 records why"
    - "**Zero fixups are scheduled** (C-1, MEASURED). Research performed a real `git merge --no-ff 4ee64a1` in a scratch worktree in a correct sibling layout and got 1216 collected / 0 failed with all eight `ci.yml` gate steps green and coverage 81.35%. D-16's 'plan for fixups, do not assume none' caution was sound when written and is now discharged by measurement — no fixup task, no fixup budget, no expected red commit"
    - "The verification run happens in `/workspaces/firestarter_app` **in the sibling layout**. `tests/test_sdp_bus_config_drift.py:22` and `tests/test_gen_validation_header.py:21` hardcode `_REPO_ROOT / \"firestarter_app\"` as a literal directory name — a wrongly-named working directory produced 6 spurious failures in research's first probe. Criterion 1's phrase *in the sibling layout* is mechanical, not decorative"
    - "The pre-merge baseline in the **real** checkout is **1158 collected · 1158 passed · 0 skipped · 0 failed** (re-measured 2026-08-01 in `/workspaces/firestarter_app`). The post-merge expectation is therefore **1216 collected · 1216 passed · 0 skipped · 0 failed** — research's `1213 passed / 3 skipped` was a scratch-worktree artifact (the meta-repo `.planning/` was unreachable from the probe path) and does **not** apply here"
    - "D-17's accepted deviation is recorded **in the code**, at `flash_method()` in `firestarter/firmware.py`, so a reader learns it without opening `.planning/`: the branch shipped a `flash_method()` router rather than the prescribed flasher-strategy extraction, and `_install_with_avrdude` is untouched — an **accepted deviation, not a defect to fix**"
    - "`asset_candidates()` is **not touched**. Phase 128's Criterion 4 asserts against `asset_candidates(\"py32f071\")[0]`; changing it changes Phase 128's contract"
  prohibitions:
    - statement: "No `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "The merge and every commit are local. Plan 127-11 is the only place an outward-facing action happens, and there it is the operator's, not an agent's"
    - statement: "No squash, no rebase, no `merge --squash`, no `merge --no-commit`"
      status: required
      verification: "`git log -1 --format=%P` on the merge commit must print two SHAs, the second string-containing `4ee64a1`"
    - statement: "No fixup commit is authored speculatively, and no test is modified to make the merge pass"
      status: required
      verification: "C-1 measured zero required fixups. If a failure genuinely appears, it is recorded verbatim and carried as a blocker — never papered over by editing a test"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08 (the Phase-116 4x premature-tick guard)"
    - statement: "`firestarter/firmware.py`'s `asset_candidates()` body is unchanged"
      status: required
      verification: "Phase 128 Criterion 4 depends on it. Only the D-17 comment at `flash_method()` is added to this file by this plan"
    - statement: "Nothing is written into the `firestarter/` (firmware) repo"
      status: required
      verification: "This phase writes only in `firestarter_app`. `git -C /workspaces/firestarter status --porcelain` unchanged throughout"
  artifacts:
    - "A merge commit on `v1.23-py32f071-integration` in `firestarter_app` with `4ee64a14a8933b60896c8b168bb1c7e34d788fa4` among its parents"
    - "`firestarter/py32_dfu.py` (832 L), `firestarter/channel.py` (81 L), `tests/test_py32_dfu.py` (654 L, 58 tests), `doc/PY32F071-FIRMWARE-INSTALL.md` (273 L) present on the branch"
    - "A separate commit adding the D-17 accepted-deviation comment at `flash_method()`"
  key_links:
    - "Every other plan in this phase reads or edits a file that **does not exist until this merge lands**. This is the phase's only Wave 1 plan and every other plan depends on it, directly or transitively."
    - "The merge must not disturb `pyproject.toml`'s `[tool.ruff] extend-exclude = [\"tests/golden\", \"tests/fixtures\"]`. `4ee64a1` predates that block by 87 commits, so a naive `git checkout --theirs` on that file would delete it and take `ruff format --check` red. Research measured the real merge keeping it; confirm it survived."
---

<objective>
Land `feature/py32f071-fw-install` @ `4ee64a1` on the app milestone branch as a **real merge commit**,
verify the merged tree against every gate `ci.yml` runs, and record D-17's accepted deviation in the
code at `flash_method()`.

Purpose: eight files and 2125 lines of py32 DFU host code exist only on an unmerged branch that is 87
commits behind. Nothing else in Phase 127 can be written, read or tested until they are on the branch.
ROADMAP Criterion 1 requires the merge commit's parent SHAs to include `4ee64a1` **literally**, which
forbids the squash Phase 124 used.

Output: a merge commit with `4ee64a1` as a parent; a recorded post-merge measurement (collected count,
pass/skip/fail, coverage, and all eight `ci.yml` gate steps); and a second commit carrying the D-17
comment.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md

**Repo.** All work is in `/workspaces/firestarter_app` on branch `v1.23-py32f071-integration`. Commit
**inside that submodule**, not in the meta-repo. `/workspaces/firestarter` (firmware) is a **read-only
input** to this phase and must not be modified. The meta-repo is on `gsd/v1.23-py32f071-integration`.

**CONTEXT.md carries inline `CORRECTED by 127-RESEARCH.md §C-N` notes inside several locked decisions.
Where a note and its surrounding decision text disagree, the note wins.** For this plan that means
**C-1**: the merge is measured clean and **no fixups are scheduled**. D-16's contingency wording stands
as history; nothing is planned against it.

**Sibling layout is mechanical.** Run every verification command from `/workspaces/firestarter_app`
(a directory literally *named* `firestarter_app`) with `/workspaces/firestarter` present as its
sibling. Two test modules hardcode that name.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** Landing this branch proves the code is on the branch and the suite passes. It proves
**nothing** about a PY32F071: no PCB exists, no install has ever run against silicon, and HOST-03's
ceiling is literally *asserted against a mock*. Do not write any sentence claiming the install works.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the pre-merge baseline, then land 4ee64a1 as a real merge commit</name>
  <files>firestarter_app/ (merge commit — 8 files, +2125/-33)</files>
  <read_first>
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Q1 in full — the merge measurement, the
      eight `ci.yml` gate rows with their exact commands and expected outputs, the three-skip explanation,
      and the wrongly-named-directory trap that produced 6 spurious failures.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-16** including its `CORRECTED by
      127-RESEARCH.md §C-1` note — read the note as the live instruction and the surrounding paragraph as
      the superseded caution.
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` — the eight gate steps in order, with their
      exact commands. This task re-runs all of them locally on the merged tree.
    - `/workspaces/firestarter_app/pyproject.toml` on `HEAD` — specifically `[tool.ruff]`'s
      `extend-exclude` list and `[tool.pytest.ini_options]`'s `addopts`. `4ee64a1` predates
      `extend-exclude`; confirm it survives the merge.
  </read_first>
  <action>
    Record the pre-merge state, perform the merge, and verify the merged tree. No fixup work is
    scheduled and none is expected (C-1, MEASURED).

    **Pin first, in `/workspaces/firestarter_app`.** Record: the branch name (must be
    `v1.23-py32f071-integration`); the full 40-character `HEAD`; `git status --porcelain` (five known
    pre-existing lines: a modified `.gitignore`, and untracked `.coverage`, `.planning/config.json`,
    `SECURITY.md`, `write_test_port.sh` — record them as pre-existing, do not clean them); confirmation
    that the working directory basename is `firestarter_app` and that `../firestarter/.git` exists;
    confirmation that no `/dev/ttyACM*` or `/dev/ttyUSB*` device is attached (a live board turns
    `test_no_programmer_found_read` / `_erase` red and would confound the baseline); and the exact
    pre-merge collected count from `python3 -m pytest --collect-only -q -o addopts=` — **expected 1158**.

    **Then merge.** `git merge --no-ff 4ee64a1` with a message naming the phase, HOST-01 and D-16, e.g.
    `merge(127-01): land feature/py32f071-fw-install @ 4ee64a1 (HOST-01, D-16)`. Do **not** use
    `--squash`, `--no-commit`, or a rebase — all three destroy the parent-SHA property ROADMAP Criterion
    1 requires literally.

    **Then verify the merged tree**, recording every value verbatim:
      - `git log -1 --format=%P` on the merge commit prints **two** parents, the second being
        `4ee64a14a8933b60896c8b168bb1c7e34d788fa4`;
      - `git diff --stat` of the merge shows **8 files, +2125/-33**;
      - the four new/changed paths exist with their expected sizes: `firestarter/py32_dfu.py` 832 lines,
        `firestarter/channel.py` 81 lines, `tests/test_py32_dfu.py` 654 lines,
        `doc/PY32F071-FIRMWARE-INSTALL.md` 273 lines;
      - `pyproject.toml` still carries `[tool.ruff]`'s `extend-exclude` entries for `tests/golden` and
        `tests/fixtures`, **and** has gained the `[py32]` extra pinned at the branch's original
        `pyusb>=1.2.1` (Plan 127-02 raises it — do not raise it here);
      - collected count is **1216** (1158 + 58 from `tests/test_py32_dfu.py`);
      - the full suite is **1216 passed, 0 skipped, 0 failed**. Note explicitly that research's
        `1213 passed / 3 skipped` was a scratch-worktree artifact: the two allow-listed meta-repo skips
        (`meta-repo ledger not available at`, `EVIDENCE.json not found at`) fire only when `.planning/`
        is unreachable, which is not the case in `/workspaces/firestarter_app`. **0 skipped is the
        correct expectation here**;
      - coverage with `--cov=firestarter --cov-fail-under=70` passes (research measured 81.35%);
      - all eight `ci.yml` gate steps run locally and pass: catalog validity, `messages.py` codegen drift
        + `git diff --exit-code`, vector catalog validity, `frame_vectors.py` codegen drift +
        `git diff --exit-code`, `ruff check firestarter/ tests/`, `ruff format --check firestarter/
        tests/`, `python tools/check_mypy_watermark.py` (expect 1 error against watermark 35), and the
        `firestarter --help` smoke test.

    **Known non-findings, do not "fix".** `ruff check .` over the whole tree reports 4 diagnostics under
    `tools/` (`audit_coverage_matrix.py`, both `tools/catalog/codegen*.py`). CI lints only
    `firestarter/ tests/`, the diagnostics are identical pre- and post-merge, and they are local
    ruff-version drift, not merge damage. Do not schedule or perform a cleanup for them.

    **If anything is red**, record the verbatim failure and stop — do not edit a test, do not author a
    speculative fixup. C-1 measured zero required fixups; a genuine deviation from that measurement is
    a finding worth surfacing, not something to absorb.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git rev-parse --abbrev-ref HEAD && git log -1 --format=%P | tee /dev/stderr | grep -q 4ee64a14a8933b60896c8b168bb1c7e34d788fa4 && echo "PARENT_OK" && test "$(basename "$PWD")" = "firestarter_app" && test -e ../firestarter/.git && echo "SIBLING_OK" && wc -l firestarter/py32_dfu.py firestarter/channel.py tests/test_py32_dfu.py doc/PY32F071-FIRMWARE-INSTALL.md && grep -c 'extend-exclude' pyproject.toml && python3 -m pytest --collect-only -q -o addopts= 2>&1 | tail -1 && python3 -m pytest -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration` in `/workspaces/firestarter_app` before and after the merge.
    - The pre-merge collected count is recorded as **1158**, and the five pre-existing `git status --porcelain` lines are recorded as pre-existing rather than cleaned.
    - The working directory basename is recorded as `firestarter_app` and `../firestarter/.git` is recorded as present; no `/dev/ttyACM*` or `/dev/ttyUSB*` device is attached.
    - `git log -1 --format=%P` on the merge commit prints exactly two SHAs and the output contains `4ee64a14a8933b60896c8b168bb1c7e34d788fa4`.
    - The merge diffstat is recorded as 8 files, +2125/-33.
    - `wc -l` gives 832 / 81 / 654 / 273 for `firestarter/py32_dfu.py`, `firestarter/channel.py`, `tests/test_py32_dfu.py`, `doc/PY32F071-FIRMWARE-INSTALL.md`.
    - `grep -c 'extend-exclude' pyproject.toml` returns 1, and `grep -c 'pyusb>=1.2.1' pyproject.toml` returns 1 (unchanged by this plan).
    - `python3 -m pytest --collect-only -q -o addopts=` reports **1216 tests collected**.
    - `python3 -m pytest -q --no-cov -o addopts=` reports **1216 passed, 0 skipped, 0 failed**, and the SUMMARY states why 0 skipped (not 3) is correct in this checkout.
    - `python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70` exits 0, and the coverage percentage is recorded.
    - All eight `ci.yml` gate commands are run verbatim and each exit code is recorded as 0; both `git diff --exit-code` codegen-drift gates are clean; `check_mypy_watermark.py` prints an error count below the watermark 35.
    - **No fixup commit was authored and no test file was edited** to make the merge pass; the SUMMARY states this explicitly.
    - `git -C /workspaces/firestarter status --porcelain` is unchanged (the firmware repo was not written to).
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>`4ee64a1` is a parent of a real merge commit on the app milestone branch, the merged tree is 1216/1216 green in the sibling layout with all eight CI gates passing locally, and every figure is on the record with its command.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-17's accepted deviation at flash_method() in a commit of its own</name>
  <files>firestarter_app/firestarter/firmware.py</files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/firmware.py` (post-merge) lines 76-140 — `FLASH_METHOD_AVRDUDE` /
      `FLASH_METHOD_DFU`, `_BOARD_FLASH_METHODS`, `_PORTLESS_FLASH_METHODS`, `flash_method()` and
      `asset_candidates()`. Read `asset_candidates()`'s docstring for the tone to imitate: it explains
      *why* the shape is what it is, which is exactly what D-17 asks for.
    - `/workspaces/firestarter_app/firestarter/firmware.py` around `_install_with_avrdude` and the router
      branch inside `install_firmware` — the code D-17 freezes. Read enough to describe it accurately;
      change nothing there.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-17** and the `<deferred>` §"Reviewed
      Todos (not folded)" entry for `avrdude-mcu-detection-fallback`.
    - `.planning/REQUIREMENTS.md` HOST-01's prose — the phrase *accepted deviation … rather than a defect
      to fix* is the requirement's own wording and should be reflected.
  </read_first>
  <action>
    Add a comment block immediately above `def flash_method(` in `firestarter/firmware.py` recording the
    accepted deviation, and commit it **separately** from the merge so it is reviewable on its own.

    The comment must state, in the file's existing explanatory tone (imitate `asset_candidates()`'s
    docstring, which explains rationale rather than restating code):
      - that this router is a **deliberate, accepted deviation** from the flasher-strategy extraction the
        milestone plan prescribed, recorded under **HOST-01 / D-17** — not a defect awaiting cleanup;
      - that `_install_with_avrdude` is **untouched** and rewriting it is explicitly **out of scope**;
      - that the pending todo `avrdude-mcu-detection-fallback` was reviewed during Phase 127 and
        deliberately **not** folded in, precisely because it targets the frozen function;
      - a pointer to `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md` as the phase evidence
        artifact carrying the same record (Plan 127-12 writes it).

    Keep it a comment block, not a docstring change — `flash_method()`'s one-line docstring stays as it
    is. Do not touch `asset_candidates()`, `_BOARD_FLASH_METHODS`, `_PORTLESS_FLASH_METHODS`, or
    `_install_with_avrdude`.

    A source-scan gate proving this comment exists is added by Plan 127-02; this task only writes it.
    Choose the wording so a stable, greppable anchor phrase is present — use the exact phrase
    `accepted deviation` and the exact token `D-17` in the block, because that is what 127-02 will scan
    for.

    Commit with a message naming HOST-01 and D-17.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -n -B2 -A20 'def flash_method' firestarter/firmware.py && test "$(grep -c 'accepted deviation' firestarter/firmware.py)" -ge 1 && test "$(grep -c 'D-17' firestarter/firmware.py)" -ge 1 && test "$(grep -c 'avrdude-mcu-detection-fallback' firestarter/firmware.py)" -ge 1 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python3 -m pytest tests/test_firmware_install.py tests/test_py32_dfu.py -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - A comment block sits immediately above `def flash_method(` in `firestarter/firmware.py`.
    - `grep -c 'accepted deviation' firestarter/firmware.py` returns at least 1, and the phrase is inside that block.
    - `grep -c 'D-17' firestarter/firmware.py` returns at least 1; `grep -c 'HOST-01' firestarter/firmware.py` returns at least 1.
    - `grep -c 'avrdude-mcu-detection-fallback' firestarter/firmware.py` returns at least 1.
    - The block names `_install_with_avrdude` as untouched and out of scope, and points at `127-NONREGRESSION.md`.
    - `git diff` for this commit touches **only** `firestarter/firmware.py`, and within it only added comment lines — no executable line changed. `git diff -U0 <merge>..HEAD -- firestarter/firmware.py | grep -c '^+[^+]'` counts only comment lines.
    - `asset_candidates()`'s body is byte-identical to its post-merge state (Phase 128 Criterion 4 depends on it).
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `python3 -m pytest tests/test_firmware_install.py tests/test_py32_dfu.py -q` passes with no failures.
    - The commit is separate from the merge commit and its message names HOST-01 and D-17.
  </acceptance_criteria>
  <done>A reader of `firestarter/firmware.py` learns the flasher-strategy deviation is accepted, and why `_install_with_avrdude` and the `avrdude-mcu-detection-fallback` todo are deliberately untouched, without opening `.planning/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unmerged branch → milestone branch | 87 commits of drift cross here; textual cleanliness is not semantic cleanliness |
| a green local suite → a claim about the phase | The suite is only meaningful in the sibling layout; elsewhere it produces 6 spurious failures |
| merged code → a claim about silicon | No PCB exists; nothing here says the install works |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-01-01 | Tampering | the merge strategy | **high** | mitigate | `--no-ff` only; `git log -1 --format=%P` asserted to contain `4ee64a14a8933b60896c8b168bb1c7e34d788fa4`. `--squash`, `--no-commit` and rebase are prohibited outright |
| T-127-01-02 | Repudiation | a suite run in a wrongly-named directory | **high** | mitigate | Working-directory basename asserted equal to `firestarter_app` and `../firestarter/.git` asserted present before any count is recorded |
| T-127-01-03 | Repudiation | absorbing a real failure by editing a test | high | mitigate | Prohibited. A failure is recorded verbatim and carried as a blocker; C-1's zero-fixup measurement makes any failure a finding |
| T-127-01-04 | Tampering | the merge silently dropping `extend-exclude` from `pyproject.toml` | medium | mitigate | `grep -c 'extend-exclude' pyproject.toml` asserted; `ruff format --check` run on the merged tree |
| T-127-01-05 | Tampering | an incidental edit to `asset_candidates()` | **high** | mitigate | Phase 128 Criterion 4 asserts against it. Acceptance criteria require the body byte-identical; the D-17 commit's diff is comment-only |
| T-127-01-06 | Repudiation | a live board confounding the baseline | medium | mitigate | `/dev/ttyACM*` and `/dev/ttyUSB*` absence asserted before the run; a board attached turns `test_no_programmer_found_*` red for reasons unrelated to this phase |
| T-127-01-07 | Elevation of privilege | an agent performing an outward-facing push | high | mitigate | No task runs `git push` or `gh workflow run`. Plan 127-11 carries the only outward-facing action and it is the operator's |
| T-127-01-08 | Repudiation | a green merge read as a claim about a PY32F071 | high | mitigate | Claim-ceiling paragraph required in the SUMMARY: the code is on the branch and the suite passes; no PCB exists and no install has run |
| T-127-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package is installed by this plan.** `pyusb` must NOT be installed into the shared devcontainer environment — the pyusb-absent tests characterise it. The only package in phase scope, `pyusb` 1.3.1, carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit and is a pre-existing dependency of the branch; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. Branch, `HEAD`, porcelain, sibling layout, absent serial devices and the 1158 pre-merge collected count are all recorded before the merge.
2. The merge is a real merge commit; `git log -1 --format=%P` contains `4ee64a14a8933b60896c8b168bb1c7e34d788fa4`.
3. The merge diffstat is 8 files, +2125/-33, and the four headline files exist at 832 / 81 / 654 / 273 lines.
4. Post-merge collected is 1216; the run is 1216 passed, 0 skipped, 0 failed, with the 0-vs-3 skip discrepancy explained.
5. Coverage passes `--cov-fail-under=70`, and the percentage is recorded.
6. All eight `ci.yml` gate commands pass locally, each recorded with its exit code.
7. `pyproject.toml` retains `extend-exclude` and still carries the branch's original `pyusb>=1.2.1`.
8. The D-17 comment exists at `flash_method()`, is comment-only, names `accepted deviation`, `D-17`, `HOST-01`, `_install_with_avrdude` and `avrdude-mcu-detection-fallback`, and is in its own commit.
9. `asset_candidates()` is unchanged; the firmware repo is unchanged; no requirement was ticked.
10. The four `tools/` ruff diagnostics are recorded as pre-existing non-findings, with no cleanup performed.
</verification>

<success_criteria>
- The py32 DFU host code is on the milestone branch via a merge commit whose parents include `4ee64a1`, satisfying ROADMAP Criterion 1's literal requirement.
- The merged tree is measured, not assumed: 1216/1216 green in the sibling layout with all eight CI gates passing locally, every figure recorded with the command that produced it.
- C-1's zero-fixup prediction is confirmed or contradicted on the record — never quietly absorbed.
- HOST-01's accepted deviation is legible from the source, not only from the planning record.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._upload_block()`, `Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-01-SUMMARY.md` when done
</output>
