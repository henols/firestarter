---
phase: 127-host-dfu-installer
plan: 10
type: execute
wave: 6
depends_on: ["127-09"]
files_modified:
  - firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md
  - firestarter_app/tests/test_py32_packaging.py
autonomous: true
requirements: [HOST-07, HOST-03]

must_haves:
  truths:
    - "`doc/PY32F071-FIRMWARE-INSTALL.md` (273 lines) is updated **only for facts this phase changed** (D-15): the 120K/8K map as actually reserved, the readback-verification step and its three non-`VERIFIED` outcomes, and the `[py32]` extra's raised floor. **Clean split: 127 documents what it built, 129 documents what it decides**"
    - "**Phase 129 keeps** the three-tier flash-path decision, `BOOTLOADER` sizing, VID/PID, BOOT0/nBOOT1 strapping, SWD pads, the socket-empty safety line, and the explicit statement that landing DFU does **not** retire the self-flash seed. Editing any of those here would write down positions Phase 129 has not decided, leaving 129 editing prose it did not author"
    - "A full reconciliation pass was **rejected**, and so was a *current as of Phase 127* staleness header — a doc that contradicts the linker script **and admits it** is worse for a reader than either fixing it or not shipping it"
    - "The doc's flash-envelope figure is currently **wrong**: step 5 of §3 says an image outside `0x08000000`–`0x08020000` (128 KiB) is rejected, which is the pre-D-13 bound. After Plan 127-05 the guard refuses at the application-region end instead. This is the single most load-bearing correction in the file"
    - "The doc's §5 already tells the operator to *verify by reading the image back before trusting it* on the plain-DFU path. HOST-03 turns that advice into a mechanism on the DfuSe path and records why it cannot be one on the plain path — the doc must now say which is which"
    - "The corrected figures are held in place by a **parity gate**: a test asserting the doc's stated application-region end string-matches `f\"0x{py32_dfu.APP_REGION_END:08X}\"`. A doc figure nobody scans is a doc figure that rots the moment Phase 129 moves the map"
    - "**This plan does not discharge HOST-03 or HOST-07.** `requirements` lists them for traceability — the doc records the readback outcomes HOST-03 built and the floor HOST-07 raised. Plans 127-09 and 127-02 are the evidence; Plan 127-12 must cite those, not this one"
  prohibitions:
    - statement: "No `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "Every commit this plan makes is local. Plan 127-11 is the only place an outward-facing action happens, and there it is the operator's, not an agent's. `--auto`/`--chain` auto-approve human-verify checkpoints, so the structural absence of the command is the gate"
    - statement: "No edit to §1's three-tier flash-path framing, §2c's own-bootloader section, the VID/PID discussion, the BOOT0/nBOOT1 strapping instructions, the SWD fallback, or the socket-empty safety line"
      status: required
      verification: "All of those are Phase 129's, per D-15. `git diff` for this plan must be confined to the Dependencies subsection, §3's steps, and the §5 verification sentence"
    - statement: "No *current as of Phase 127* staleness header is added"
      status: required
      verification: "Explicitly rejected in D-15. Either the figure is corrected or it is not touched"
    - statement: "No sentence claims the install works, that the DFU sequence is correct on silicon, or that any figure was observed on a PY32F071"
      status: required
      verification: "The doc already opens with *Status: unverified against silicon*; every added sentence must be consistent with that header, and the readback text must say it is asserted against a mock"
    - statement: "The statement that landing DFU does not retire the self-flash seed is neither added nor removed here"
      status: required
      verification: "It is Phase 129's to write. The existing §1 sentence pointing at the seed stays exactly as it is"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts:
    - "`firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md` — three scoped corrections"
    - "`firestarter_app/tests/test_py32_packaging.py` — the doc-vs-constant parity gate"
  key_links:
    - "Phase 129 will give `BOOTLOADER` a non-zero length, which moves the application's ORIGIN and therefore both bounds of the figure this plan writes. The parity gate is what turns that into a red test rather than a silently stale doc."
    - "Plan 127-05 wrote `APP_REGION_END`; Plan 127-09 wrote the three non-`VERIFIED` outcomes; Plan 127-02 raised the floor. This plan states all three in operator-facing prose and is the last place they must agree."
---

<objective>
Update `doc/PY32F071-FIRMWARE-INSTALL.md` for the three facts this phase changed — the reserved flash
map, the readback-verification step and its outcomes, and the `[py32]` floor — and hold the flash-map
figure in place with a parity gate against the host constant.

Purpose: the doc currently tells an operator that an image outside a 128 KiB window is rejected. After
this phase the guard refuses at 120 KiB, because the top 8 KiB is the config region Phase 126 reserved.
A doc that contradicts the code on a safety bound is worse than no doc. And the readback HOST-03 added
changes what a successful install means, which is the first thing an operator reads about.

Output: three scoped corrections, nothing Phase 129 owns touched, and a test that fails if the doc's
figure and the host constant ever disagree.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md
@.planning/phases/127-host-dfu-installer/127-02-SUMMARY.md
@.planning/phases/127-host-dfu-installer/127-05-SUMMARY.md
@.planning/phases/127-host-dfu-installer/127-09-SUMMARY.md

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`; commit **inside** that
submodule; run from a directory literally named `firestarter_app` with `/workspaces/firestarter` as a
sibling.

**Scope discipline is the whole point of D-15.** This doc is 273 lines and most of it belongs to Phase
129. Three subsections change. Everything else — including things that look stale — stays.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** The doc's own header already says *Status: unverified against silicon*. Every
sentence added must be consistent with that, and the readback text must say plainly that it is asserted
against a mock.
</context>

<!-- Comment-text discipline: Task 1's gate negative-greps the superseded hex bound (count must reach 0).
     That literal has to be nameable in <read_first> so the executor can find the line it replaces, but it
     must not be written back into doc/PY32F071-FIRMWARE-INSTALL.md. The status-header phrase is gated at
     exactly 1, not 0, so it is not a negative grep and needs no allowance. -->
<!-- planner-discipline-allow: 0x08020000 -->

<tasks>

<task type="auto">
  <name>Task 1: Three scoped corrections — the reserved map, the readback step, the raised floor</name>
  <files>firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md</files>
  <read_first>
    - `/workspaces/firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md` in full, all 273 lines. In
      particular: the *Status: unverified against silicon* header; §0 *Availability — pre-release builds
      only*; §1's `flash_method()` discussion and its **Dependencies** subsection with the
      `pip install 'firestarter[py32]'` block and the libusb / WinUSB paragraph; §2a/§2b/§2c; §3's
      seven-step *What the host does during an install* list, especially **step 5**'s
      `0x08000000`–`0x08020000` (128 KiB) figure and **step 6**'s DfuSe / plain fork; §4's command
      reference; and §5's numbered first-bench-session list, whose item 2 already says the plain-DFU load
      address should be checked *by reading the image back before trusting it*.
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` after Plans 127-05 and 127-09 — the four
      flash-map constants and their comment, `VerifyResult`'s four members, `_verify_readback`'s two skip
      reasons and their exact strings, and the mismatch message shape.
    - `/workspaces/firestarter_app/firestarter/firmware.py` after Plan 127-09 — the *written but NOT
      verified* completion line, which is what an operator will actually see.
    - `/workspaces/firestarter_app/pyproject.toml` after Plan 127-02 — the `[py32]` extra's floor.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-15** and `<deferred>` — the explicit
      list of what Phase 129 keeps.
  </read_first>
  <action>
    Make exactly three scoped edits. Change nothing else in the file.

    **(a) The reserved flash map — §3 step 5.** Replace the 128 KiB figure with the application region
    the firmware's linker script actually reserves: the accepted span now ends at the application-region
    end, and the top 8 KiB above it is the config storage Phase 126 reserved (Sector 15). Write the
    figure so it matches `firestarter/py32_dfu.py`'s constants exactly, in the same uppercase-hex
    `0x0801E000` form the parity gate in Task 2 will look for. Say **why** the bound is 120 KiB rather
    than the part's 128 KiB — the config region is reserved, and a write reaching into it is refused
    before a byte is sent. State that the refusal is deliberately non-overridable: there is no force flag.
    Do **not** discuss `BOOTLOADER` sizing or the vector-relocation implication — that is Phase 129's.

    **(b) The readback-verification step — §3.** Add a step between the transfer step and the
    tolerate-the-reset step, describing verification in operator terms: after the download and **before**
    the device is told to leave DFU mode, the host reads the image back over `DFU_UPLOAD` and compares
    the **full payload byte-for-byte**. Then name the three non-verified outcomes and what each means:
      - the device reports it cannot upload (`bitCanUpload = 0`) — verification is skipped and the install
        reports **written but NOT verified**;
      - the device speaks plain DFU 1.1 rather than DfuSe — verification is skipped with the reason
        **load address not under host control**, because the host does not choose where the image lands
        and therefore has no address to read back from;
      - the bytes differ — this is a **hard failure**: the install exits non-zero naming the first
        differing offset and the expected and actual bytes, and the device is deliberately **not** told to
        manifest the image.
    State explicitly that this is asserted against a fake USB device and has never run against a
    PY32F071 — consistent with the file's own status header (quoted in `<read_first>`).

    **Do not repeat the status header's phrase verbatim anywhere in the new text.** The doc carries that
    phrase **exactly once** today, at line 7 — MEASURED 2026-08-01 against blob
    `556fd18859df0bc8e5aa0dacba51132c5599126b`, the 273-line content the 127-01 merge lands, which no
    plan touches before this one. The gate below asserts the count is **still exactly 1** after your edit,
    re-deriving that 1 from the pinned blob rather than from `HEAD~1`, so a second copy of the header's
    exact wording turns it red even though no staleness header was added — which is what the criterion
    exists to catch. Write the mock-only ceiling in your own words instead; *never run against a
    PY32F071* and *asserted against a fake USB device* are the load-bearing claims, and neither collides
    with the header.

    Also update §5's item 2: the sentence advising the operator to verify by reading the image back is
    now partly automated, and the doc should say the DfuSe path does it for you while the plain path
    still cannot. Keep the surrounding bench-session steps as they are.

    Update §3's closing sentence — currently *Every step above is asserted in `tests/test_py32_dfu.py`
    against a fake USB device* — so it still reads true after the new step, and so the *currently the only
    check that the sequence is well-formed* clause remains accurate.

    **(c) The `[py32]` floor — §1 Dependencies.** State the pinned range the extra now declares and, in
    one clause, why the upper bound exists: a future pyusb major could reorder `ctrl_transfer`'s
    parameters, which every call site passes positionally. Leave the libusb / WinUSB paragraph and the
    self-flash-seed pointer exactly as they are — the wording of the WinUSB caveat is deliberately
    untouched by this phase.

    **Touch nothing else.** No edit to §0, §2a, §2b, §2c, §4, the VID/PID discussion in §5 item 1, the
    SWD fallback, or the closing recovery paragraph. Add no staleness header.

    Commit with a message naming D-15.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && D=doc/PY32F071-FIRMWARE-INSTALL.md && B=556fd18859df0bc8e5aa0dacba51132c5599126b && test -f "$D" && git cat-file -e "$B" && test "$(git cat-file blob "$B" | wc -l)" -eq 273 && test "$(grep -c '^## 3\. What the host does during an install' "$D")" -eq 1 && test "$(grep -c '^## 5\. First bench session' "$D")" -eq 1 && test "$(grep -c '0x0801E000' "$D")" -ge 1 && test "$(grep -c '0x08020000' "$D")" -eq 0 && test "$(grep -c 'DFU_UPLOAD' "$D")" -ge 1 && test "$(grep -c 'written but NOT verified' "$D")" -ge 1 && test "$(grep -c 'load address not under host control' "$D")" -ge 1 && test "$(grep -c 'bitCanUpload' "$D")" -ge 1 && test "$(grep -c 'pyusb>=1.3.1,<2' "$D")" -ge 1 && test "$(git cat-file blob "$B" | grep -c 'unverified against silicon')" -eq 1 && test "$(grep -c 'unverified against silicon' "$D")" -eq 1 && echo "STALENESS_HEADER_OK pre=1 post=1" && test "$(grep -cE '^##+ ' "$D")" -eq 10 && test "$(git cat-file blob "$B" | grep -E '^##+ ' | md5sum)" = "$(grep -E '^##+ ' "$D" | md5sum)" && echo "HEADINGS_IDENTICAL_OK" && test "$(git cat-file blob "$B" | sed -n '/needs a libusb backend/,/in the planning repo\./p' | md5sum)" = "$(sed -n '/needs a libusb backend/,/in the planning repo\./p' "$D" | md5sum)" && echo "WINUSB_PARA_IDENTICAL_OK" && git --no-pager diff --stat -- "$D" && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '0x0801E000' doc/PY32F071-FIRMWARE-INSTALL.md` returns at least 1, and the accepted span in §3 step 5 ends there. The superseded upper bound is **gone**: `grep -c '0x08020000'` returns **exactly 0** (pre-edit value **1**, at §3 step 5 — MEASURED 2026-08-01 against blob `556fd18859df0bc8e5aa0dacba51132c5599126b`). Prose may still say *128 KiB* when explaining why the bound is 120 KiB; only the hex literal must go.
    - §3 step 5 explains that the region above it is the reserved config storage and that the refusal is non-overridable; it does not discuss `BOOTLOADER` sizing or vector relocation.
    - A verification step exists in §3 between the transfer step and the tolerate-the-reset step, describing a full byte-for-byte readback that happens **before** the device leaves DFU mode.
    - All three non-verified outcomes are named, with `bitCanUpload`, `load address not under host control` and `written but NOT verified` each appearing in the file.
    - The mismatch outcome is described as a hard, non-zero-exit failure that names the first differing offset and does not manifest the image.
    - The doc states that verification is asserted against a fake USB device and has never run against a PY32F071.
    - §5 item 2 distinguishes the DfuSe path (now automated) from the plain path (still not verifiable by the host).
    - §3's closing sentence still reads true after the added step.
    - `grep -c 'pyusb>=1.3.1,<2' doc/PY32F071-FIRMWARE-INSTALL.md` returns at least 1 and the upper bound's reason is given in one clause.
    - The libusb / WinUSB paragraph and the self-flash-seed pointer are byte-identical to their previous text, asserted rather than asserted-in-prose: the six lines from `needs a libusb backend` through `in the planning repo.` must md5-match the same range extracted from the pinned pre-edit blob `556fd18859df0bc8e5aa0dacba51132c5599126b`. MEASURED 2026-08-01: 6 lines, md5 `9e50a7c4ac7dd0849e00c20d9b4ace9f`. The range is delimited by content, not line numbers, so the correction to §1 Dependencies above it does not move it.
    - No section heading is added, removed or renamed: the doc's `^##`-and-deeper heading list is **byte-identical** to its pre-edit state. Asserted by comparing `grep -E '^##+ ' | md5sum` between the pinned pre-edit blob `556fd18859df0bc8e5aa0dacba51132c5599126b` and the edited file, with the count pinned at **10** so two empty extractions cannot match each other. MEASURED 2026-08-01: 10 headings, list md5 `5d90f081b03aa702bf81b191cded4aa8`. This replaces the previous `git diff HEAD~1 … | grep -cE '^[-+](##|###) '` form, which was both unstable (a `HEAD~1` anchor moves with every commit the wave makes) and inert (`grep -c` exits 1 on its own desired answer of 0, killing the rest of the `&&` chain). Every changed hunk must still fall inside §1 Dependencies, §3, or §5 item 2 — confirmed by reading the `git diff --stat` the block prints.
    - No staleness header was added: `grep -c 'unverified against silicon' doc/PY32F071-FIRMWARE-INSTALL.md` returns **exactly 1**. The pre-edit value is also **1** (the file's own status header at line 7) — MEASURED 2026-08-01 with `git cat-file blob 556fd18859df0bc8e5aa0dacba51132c5599126b | grep -c 'unverified against silicon'` against the 273-line blob the 127-01 merge of `4ee64a1` lands, which no plan touches before this one. The automated block re-derives that 1 from the pinned blob and compares against the post-edit count, so the gate is self-contained: it never consults `HEAD~1` and is unaffected by how many commits this wave has made. **Non-vacuity (research finding A-7):** the same command also requires the doc to exist, the pinned blob to be 273 lines, and the `## 3.` and `## 5.` headings to each match exactly once — so a renamed or truncated file goes red instead of reading as "count unchanged".
    - The full suite is still green with 0 failures and 0 skips.
  </acceptance_criteria>
  <done>An operator reading the doc learns the real 120 KiB bound and why, learns that a successful install is now verified — or told plainly that it was not — and finds nothing Phase 129 has yet to decide.</done>
</task>

<task type="auto">
  <name>Task 2: A parity gate so the doc's flash-map figure cannot outlive the constant</name>
  <files>firestarter_app/tests/test_py32_packaging.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_py32_packaging.py` as Plan 127-02 left it — the module
      docstring, `_APP_DIR`, `_PYPROJECT`, `_FIRMWARE_PY`, the non-vacuity idiom, and the monkeypatched
      fail-closed RED shape it already uses twice.
    - `/workspaces/firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md` after Task 1 — the exact rendering of
      the address in §3 step 5, so the gate matches what was written.
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` — `APP_REGION_END` and `FLASH_BASE`.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` `<deferred>` — the Phase 129 re-check of
      D-13's constant, which this gate is designed to surface.
  </read_first>
  <action>
    Add a documentation-parity gate to `tests/test_py32_packaging.py` (the phase's textual-gates module),
    rather than creating a fourth scanning module.

    Add a module-level `_INSTALL_DOC = _APP_DIR / "doc" / "PY32F071-FIRMWARE-INSTALL.md"` and a small
    helper that reads it and returns its text, so the RED test can monkeypatch the path.

    Tests:
      1. **Non-vacuity.** The doc exists, is non-empty, and contains the §3 heading text, with a failure
         message saying the downstream assertions would otherwise be vacuously true.
      2. **The address parity.** The doc contains the application-region end formatted from the host
         constant — build the expected string with `f"0x{py32_dfu.APP_REGION_END:08X}"` rather than
         writing the address literal, so the gate follows the constant automatically when Phase 129 moves
         the map. Failure message must say that the doc figure and the host constant have diverged and
         name both.
      3. **The base parity.** The doc likewise contains `f"0x{py32_dfu.FLASH_BASE:08X}"`.
      4. **The readback outcomes are documented.** The doc contains all three of `bitCanUpload`,
         `load address not under host control` and `written but NOT verified` — so a future edit cannot
         quietly drop the honest half of the install's outcome vocabulary.
      5. **The floor parity.** The requirement string the `[py32]` extra declares (already extracted by
         this module's existing `pyproject.toml` helper) also appears in the doc — one comparison, both
         sources, so the two cannot drift.
      6. **Fail-closed RED.** Monkeypatch `_INSTALL_DOC` at a `tmp_path` file lacking the address and
         assert the parity helper raises `AssertionError`, in the same shape this module's two existing
         RED demonstrations use.

    Update the module docstring to record that it now carries three gate families — packaging, the D-17
    deviation record, and this documentation parity — and why they live together.

    Commit with a message naming D-15 and D-13.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_py32_packaging.py -v --no-cov -o addopts= 2>&1 | tail -18 && python3 -c "
from firestarter import py32_dfu as m
doc = open('doc/PY32F071-FIRMWARE-INSTALL.md').read()
for s in (f'0x{m.APP_REGION_END:08X}', f'0x{m.FLASH_BASE:08X}', 'bitCanUpload', 'load address not under host control', 'written but NOT verified'):
    assert s in doc, s
print('DOC_PARITY_OK')
" && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && python3 -m pytest tests/test_skip_census.py -q --no-cov -o addopts= 2>&1 | tail -2 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_py32_packaging.py` gains `_INSTALL_DOC` and a monkeypatchable doc-reading helper.
    - All six tests exist and pass, including the non-vacuity guard and the fail-closed RED.
    - The address expectations are **built from** `py32_dfu.APP_REGION_END` and `py32_dfu.FLASH_BASE` with `f"0x{...:08X}"`, not written as literals — so the gate follows the constant when Phase 129 moves the map.
    - The three readback-outcome strings are asserted present in the doc.
    - The `[py32]` requirement string is compared between `pyproject.toml` and the doc in a single test.
    - The RED demonstration genuinely raises `AssertionError` against a planted `tmp_path` doc, proven by `pytest.raises`.
    - The module docstring records all three gate families and why they live in one module.
    - `python3 -m pytest tests/test_skip_census.py -q` passes — no new skip reason.
    - The full suite is green with 0 failures and 0 skips; the new collected total is recorded in the SUMMARY as an observation only.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
  </acceptance_criteria>
  <done>The doc's safety figure and the code's constant cannot disagree without a red test, and the honest outcome vocabulary the install now uses cannot be quietly edited out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented safety bound → enforced safety bound | A doc stating a wider window than the guard misleads on exactly the point that matters |
| this phase's facts → Phase 129's decisions | Writing 129's positions here leaves 129 editing prose it did not author |
| an operator reading "success" → what was actually proven | The install's outcome vocabulary is the operator's only signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-10-01 | **Repudiation** | a documented 128 KiB window contradicting a 120 KiB guard | **high** | mitigate | §3 step 5 corrected to the application-region end, and a parity gate builds its expectation from `APP_REGION_END` so the doc follows the constant |
| T-127-10-02 | Repudiation | an operator reading a bare *success* for an unverified write | high | mitigate | All three non-verified outcomes documented by name, including the *written but NOT verified* wording the completion line actually prints, and asserted present by the gate |
| T-127-10-03 | Repudiation | doc prose read as a silicon claim | **high** | mitigate | The added verification text states it is asserted against a fake device and has never run against a PY32F071; the file's *unverified against silicon* header is preserved and its occurrence count asserted |
| T-127-10-04 | Tampering | scope creep into Phase 129's sections | medium | mitigate | The diff is asserted to add or remove no section heading and to fall only within §1 Dependencies, §3 and §5 item 2; the libusb/WinUSB paragraph and the seed pointer are asserted byte-identical |
| T-127-10-05 | Repudiation | a doc figure silently rotting when Phase 129 moves the map | medium | mitigate | The parity gate is derived from the constant, not from a literal, so a Phase-129 map move turns it red rather than stale |
| T-127-10-06 | Repudiation | a vacuous doc scan passing on an unreadable file | medium | mitigate | Non-vacuity guard plus a monkeypatched planted-file RED demonstration |
| T-127-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install is performed and no dependency is added. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. §3 step 5 states the application-region end, explains the reserved region above it and the non-overridable refusal, and says nothing about `BOOTLOADER` sizing.
2. A verification step exists in §3 with the full byte-for-byte readback described as happening before the device leaves DFU mode.
3. All three non-verified outcomes are documented by name, and the mismatch is described as a hard non-zero-exit failure that does not manifest.
4. The doc states the verification is mock-only and has never run against a PY32F071.
5. §5 item 2 distinguishes the automated DfuSe path from the still-unverifiable plain path.
6. §1 Dependencies states the pinned pyusb range and the reason for the upper bound; the libusb/WinUSB paragraph and the seed pointer are unchanged.
7. The diff adds and removes no section heading and stays inside the three permitted regions; no staleness header exists.
8. The parity gate builds its expectations from `APP_REGION_END` and `FLASH_BASE`, and passes.
9. The three outcome strings and the `[py32]` requirement string are asserted present in the doc.
10. The fail-closed RED raises against a planted file; the full suite, ruff and ruff-format all pass.
</verification>

<success_criteria>
- The install doc no longer overstates the safe write window, and an operator learns why the bound is 120 KiB rather than the part's 128 KiB.
- The three ways an install can complete without verification are named in the doc using the same words the tool prints.
- The doc and the code cannot disagree on the flash map without a red test, including when Phase 129 moves it.
- Everything Phase 129 owns is exactly as Phase 129 will find it.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-10-SUMMARY.md` when done
</output>
