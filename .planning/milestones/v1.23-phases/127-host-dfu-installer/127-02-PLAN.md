---
phase: 127-host-dfu-installer
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - firestarter_app/pyproject.toml
  - firestarter_app/tests/test_py32_packaging.py
autonomous: true
requirements: [HOST-07, HOST-01]

must_haves:
  truths:
    - "The `[py32]` extra pins **`pyusb>=1.3.1,<2`** (D-19), raised from the branch's `pyusb>=1.2.1`. Zero cost: research measured 1.3.1 as the current release with `Requires-Python: >=3.9.0`, satisfiable on the project's `requires-python = \">=3.9\"` floor"
    - "The floor is proven by an **exit code**, not by a human reading `pyproject.toml` — a textual gate in the primary test leg, matching the standing operator preference recorded in `123-CONTEXT.md`"
    - "The gate parses **no TOML**. `tomllib` is stdlib only from py3.11 and the project floor is py3.9; `tomli` is not a dependency and this phase adds none. The scan is a regex over the file text, scoped to the `py32 = [` block — the same source-scanning idiom `tests/test_revision_constants_parity.py` already uses for `#define` extraction"
    - "The gate carries a **non-vacuity assertion**: it fails loudly if the `py32 = [` block is not found at all, rather than passing on an empty match set. This is research finding **A-7** applied — a scan that finds nothing must never read as a pass"
    - "D-17's accepted-deviation comment at `flash_method()` (written by Plan 127-01 Task 2) is held in place by a source scan, so a later refactor cannot silently delete the only in-code record of HOST-01's deviation"
  prohibitions:
    - statement: "No `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "Every commit this plan makes is local. Plan 127-11 is the only place an outward-facing action happens, and there it is the operator's, not an agent's. `--auto`/`--chain` auto-approve human-verify checkpoints, so the structural absence of the command is the gate"
    - statement: "No `tomllib`, `tomli`, `toml` or any other TOML parser is imported"
      status: required
      verification: "`tomllib` is py3.11+; the project floor is py3.9 and `ruff target-version = \"py39\"` / `mypy python_version = \"3.9\"` enforce it statically. `grep -c 'import toml' tests/test_py32_packaging.py` returns 0"
    - statement: "No new dependency is added to any extra, and no existing pin other than the `[py32]` floor is changed"
      status: required
      verification: "`git diff pyproject.toml` for this plan touches only the `pyusb` requirement string"
    - statement: "`pyusb` is not installed into the shared devcontainer environment"
      status: required
      verification: "The pyusb-**absent** tests (Plan 127-07) characterise that environment. Rehearsal, if any, happens in an isolated venv"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts:
    - "`firestarter_app/pyproject.toml` — `[project.optional-dependencies]` `py32 = [\"pyusb>=1.3.1,<2\"]`"
    - "`firestarter_app/tests/test_py32_packaging.py` — the packaging floor gate and the D-17 record gate"
  key_links:
    - "Plan 127-06's `ci-py32` job installs `.[test,py32]`; the resolver honours whatever floor this plan writes. A wrong floor here is only observable in the operator-dispatched CI run at Plan 127-11, which is why the local textual gate matters."
    - "Plan 127-10 documents the raised floor in `doc/PY32F071-FIRMWARE-INSTALL.md` §Dependencies. If the floor written here and the floor documented there disagree, one of the two is wrong; 127-10 reads this file."
---

<objective>
Raise the `[py32]` extra's `pyusb` floor to `>=1.3.1,<2` and hold both it and D-17's accepted-deviation
comment in place with exit-code gates that run in the primary test leg.

Purpose: HOST-07 is a one-line packaging change whose only failure mode is silent reversion — nothing in
the repo would notice. HOST-01's deviation record has the same property: a comment nobody scans for is a
comment a refactor deletes. Both become checkable.

Output: the raised floor; `tests/test_py32_packaging.py` with a non-vacuous textual gate over the
`py32 = [` block and a source scan over `firestarter/firmware.py`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md
@.planning/phases/127-host-dfu-installer/127-01-SUMMARY.md

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`; commit **inside** that
submodule. Run every command from a directory literally named `firestarter_app` with
`/workspaces/firestarter` as its sibling.

**Python floor.** The devcontainer is py3.12; CI targets py3.11; the declared floor is py3.9 and is
enforced statically by `ruff target-version = "py39"` and `mypy python_version = "3.9"`. **`tomllib` and
`StrEnum` are py3.11+ and must not be used.** New test modules should carry
`from __future__ import annotations` if they use PEP-604 annotations.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** Raising a version floor proves a resolver constraint, nothing about a PY32F071.
</context>

<!-- Comment-text discipline: the old requirement string must be named in the action so the executor
     knows exactly what it is replacing, and it is also the subject of a negative grep over
     pyproject.toml. The negative grep targets a config file this plan edits, not a file whose content
     is derived from the action text, so the literal is allowed to appear here. -->
<!-- planner-discipline-allow: pyusb>=1.2.1 -->

<tasks>

<task type="auto">
  <name>Task 1: Raise the [py32] extra to pyusb&gt;=1.3.1,&lt;2</name>
  <files>firestarter_app/pyproject.toml</files>
  <read_first>
    - `/workspaces/firestarter_app/pyproject.toml` (post-merge) — the whole
      `[project.optional-dependencies]` block: `dev`, the new `py32` block with its two explanatory
      comment lines, and `test`. Also read `requires-python` and `[tool.ruff]`'s `target-version` and
      `extend-exclude`.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-19** and
      `127-RESEARCH.md` §Q3 "D-19 on the py39 floor — resolvable [MEASURED]".
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Package Legitimacy Audit — `pyusb` 1.3.1
      carries an **OK** verdict and is a pre-existing dependency of the branch, so no legitimacy
      checkpoint is owed.
  </read_first>
  <action>
    In `firestarter_app/pyproject.toml`, change the `[py32]` extra's single requirement string from
    `pyusb>=1.2.1` to `pyusb>=1.3.1,<2`.

    Keep the two existing explanatory comment lines above the `py32 = [` block (they explain why the
    extra is optional — that pyusb pulls in a libusb backend AVR users never touch). Extend them with one
    short line recording **why** the floor moved and why the upper bound exists: 1.3.1 is the release
    whose `ctrl_transfer` signature Plan 127-06's API-surface test pins, and `<2` refuses a future major
    that could reorder those parameters. Cite **HOST-07 / D-19**.

    Write that line about the **new** bounds only. Do **not** restate the superseded requirement string
    anywhere in the file — not in the comment, not as a struck-through note — because this task's gate
    asserts the superseded string occurs zero times in `pyproject.toml`, and a comment quoting it is
    textually indistinguishable from an unfinished edit.

    Change nothing else in the file — not `requires-python`, not `[tool.ruff]`'s `extend-exclude`, not
    the `dev` or `test` extras, not `[tool.pytest.ini_options]`. Do **not** add pyusb to `test`; the
    whole point of the two-leg design (D-02) is that the primary leg never installs it.

    Do **not** run `pip install` in the shared devcontainer environment. If a resolution rehearsal is
    wanted, do it in a throwaway venv and delete it; the authoritative check is the operator-dispatched
    `ci-py32` run at Plan 127-11.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -n -B6 -A3 'py32 = \[' pyproject.toml && test "$(grep -c 'pyusb>=1.3.1,<2' pyproject.toml)" -eq 1 && test "$(grep -c 'pyusb>=1.2.1' pyproject.toml)" -eq 0 && test "$(grep -c 'requires-python = ">=3.9"' pyproject.toml)" -eq 1 && test "$(grep -c 'extend-exclude' pyproject.toml)" -eq 1 && python3 -c "import sys; sys.exit(0)" && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pyusb>=1.3.1,<2' pyproject.toml` returns exactly 1.
    - `grep -c 'pyusb>=1.2.1' pyproject.toml` returns 0.
    - `grep -c 'requires-python = ">=3.9"' pyproject.toml` returns 1 (unchanged).
    - `grep -c 'extend-exclude' pyproject.toml` returns 1 (unchanged).
    - The `test` extra does **not** contain `pyusb`: `grep -A10 '^test = \[' pyproject.toml | grep -c pyusb` returns 0.
    - The comment block above `py32 = [` names HOST-07 and D-19 and states why the upper bound `<2` exists.
    - `git diff pyproject.toml` for this task shows changes confined to the `py32` block (the requirement string plus added comment lines).
    - `python3 -m pytest tests/ -q` still reports 1216 passed, 0 failed.
    - No `pip install` was run against the shared devcontainer environment; the SUMMARY states this.
  </acceptance_criteria>
  <done>The `[py32]` extra pins `pyusb>=1.3.1,<2`, the reason for both bounds is recorded beside it, and nothing else in `pyproject.toml` moved.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_py32_packaging.py — non-vacuous floor gate plus the D-17 record gate</name>
  <files>firestarter_app/tests/test_py32_packaging.py</files>
  <read_first>
    - `/workspaces/firestarter_app/pyproject.toml` after Task 1 — the exact text the regex must match,
      including the block's indentation and the trailing comma.
    - `/workspaces/firestarter_app/tests/test_revision_constants_parity.py` lines 1-80 and its
      `#define`-extraction helper — the repo's nearest source-scanning idiom, and the module-docstring
      convention (MIT header, then a paragraph naming the phase, plan and decision ID, then *why* this
      mechanism over the rejected one).
    - `/workspaces/firestarter_app/tests/test_revision_constants_parity.py` around
      `test_gate_fails_closed_on_an_unreadable_header_path` — the fail-closed RED demonstration shape to
      imitate: monkeypatch the module-level path constant to a nonexistent file and assert the gate
      raises rather than passes.
    - `/workspaces/firestarter_app/firestarter/firmware.py` — the D-17 comment block Plan 127-01 Task 2
      wrote above `def flash_method(`, so the scan targets the phrases actually present.
    - `/workspaces/firestarter/tests/test_py32_flash_map.py` around its non-vacuity assertions (the
      *"would be vacuously true"* message shape) — the wording convention for a non-vacuity failure.
  </read_first>
  <action>
    Create `tests/test_py32_packaging.py` carrying two independent textual gates. Open it with the MIT
    header block and a docstring naming Phase 127, Plan 127-02, HOST-07 / D-19 and HOST-01 / D-17, and
    stating why a **regex scan** is used rather than a TOML parse: `tomllib` is py3.11+ and the project
    floor is py3.9, and `tomli` is not a dependency — the repo already scans source text for exactly
    this class of gate. Name those two parsers as **bare module names** in that sentence, as written
    here; do not render either as an import statement, because this task's gate counts zero occurrences
    of an import naming a TOML parser anywhere in the file and a docstring example would trip it.

    Module-level constants: `_APP_DIR = Path(__file__).parent.parent`; `_PYPROJECT = _APP_DIR /
    "pyproject.toml"`; `_FIRMWARE_PY = _APP_DIR / "firestarter" / "firmware.py"`. Write the expected floor
    **independently as a literal** — `_EXPECTED_PYUSB_SPEC = "pyusb>=1.3.1,<2"` — with a citing comment
    naming D-19 and the measured facts (1.3.1 is the current release; `Requires-Python: >=3.9.0`; `<2`
    refuses a major that could reorder `ctrl_transfer`'s parameters). Never derive the expectation from
    the file under test.

    Provide one helper, `_py32_extra_requirements(text)`, that extracts the `py32 = [ ... ]` block from
    the `pyproject.toml` text with a regex spanning to the closing bracket and returns the list of
    quoted requirement strings inside it. It must return an empty result — not raise — when the block is
    absent, so the non-vacuity assertion is the thing that reports the failure.

    Tests to write:
      1. **Non-vacuity.** `_py32_extra_requirements` over the real `pyproject.toml` returns a non-empty
         list. Failure message must say the `py32 = [` block was not found and that every downstream
         assertion would be vacuously true.
      2. **The floor.** The extracted requirement list equals exactly `[_EXPECTED_PYUSB_SPEC]` — one
         requirement, string-equal. This is what makes a silent revert to `pyusb>=1.2.1`, a dropped upper
         bound, or an added second requirement all fail.
      3. **pyusb is absent from the primary install path.** The `test` extra's requirement list contains
         no entry whose lowercase form starts with `pyusb`. This is D-02's two-leg design as an
         assertion: if `pyusb` ever migrates into `test`, the entire pyusb-absent proof set (Plan 127-07)
         becomes vacuous without any test going red on its own.
      4. **Fail-closed RED demonstration.** With `monkeypatch`, point the module's `_PYPROJECT` constant
         at a `tmp_path` file containing no `py32` block, and assert the non-vacuity check raises
         `AssertionError`. Structure the gate so this is possible — put the assertion in a helper the
         test can call directly, in the shape `test_revision_constants_parity.py` uses.
      5. **D-17's record.** `firestarter/firmware.py` contains the phrases `accepted deviation`, `D-17`,
         `HOST-01`, `_install_with_avrdude` and `avrdude-mcu-detection-fallback`, **and** the
         `accepted deviation` phrase occurs within the 25 lines preceding `def flash_method(` — so the
         record cannot drift away from the function it describes. Include a non-vacuity assertion that
         `def flash_method(` was located at all.
      6. **Fail-closed RED for the D-17 gate.** Same monkeypatch shape: point `_FIRMWARE_PY` at a
         `tmp_path` file lacking the phrases and assert the helper reports a violation.

    Add `from __future__ import annotations` at the top if any PEP-604 annotation is used. No skip
    marker of any kind: this module runs identically in both CI legs and must add **no** entry to
    `ALLOWED_SKIP_REASONS` in `tests/test_skip_census.py`.

    Commit with a message naming HOST-07, D-19 and D-17.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(grep -c 'import toml' tests/test_py32_packaging.py)" -eq 0 && test "$(grep -c 'importorskip\|skipif\|pytest.skip' tests/test_py32_packaging.py)" -eq 0 && python3 -m pytest tests/test_py32_packaging.py -v --no-cov -o addopts= 2>&1 | tail -12 && python3 -m pytest tests/test_skip_census.py -q --no-cov -o addopts= 2>&1 | tail -2 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_py32_packaging.py` exists, opens with the MIT header and a docstring naming Phase 127, Plan 127-02, HOST-07/D-19 and HOST-01/D-17, and states why a regex scan replaces a TOML parse.
    - `grep -c 'import toml' tests/test_py32_packaging.py` returns 0; no `tomllib`, `tomli` or `toml` import exists.
    - `grep -c 'importorskip\|skipif\|pytest.skip' tests/test_py32_packaging.py` returns 0 — the module carries no skip marker and adds no `ALLOWED_SKIP_REASONS` entry.
    - `_EXPECTED_PYUSB_SPEC` is written as an independent literal with a citing comment naming D-19; it is not read from `pyproject.toml`.
    - All six tests exist and pass: non-vacuity, exact floor equality, `pyusb` absent from `test`, the packaging fail-closed RED, the D-17 phrase-and-proximity scan, and the D-17 fail-closed RED.
    - Both RED demonstrations genuinely fail the gate when pointed at a planted file — proven by the tests passing under `pytest.raises(AssertionError)` rather than by inspection.
    - `python3 -m pytest tests/test_skip_census.py -q` passes — no new skip reason was introduced.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - The full suite reports **1222 passed, 0 skipped, 0 failed** (1216 + 6), and the count is recorded in the SUMMARY as an observation, not asserted anywhere in code (D-04 — recorded, never gated).
  </acceptance_criteria>
  <done>A silent revert of the pyusb floor, a migration of pyusb into the primary `test` extra, or a deletion of D-17's in-code record each turn the suite red — and both gates are proven able to fail against a planted file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared dependency floor → resolved wheel | Only CI resolves for real; the local gate checks the declaration, never the resolution |
| a scan that finds nothing → a pass | The A-7 failure shape; non-vacuity assertions are the control |
| an in-code decision record → a refactor | A comment nobody scans for is a comment that silently disappears |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-02-01 | Tampering | supply chain — the `pyusb` floor | **high** | mitigate | Exact string equality against an independently-written literal `pyusb>=1.3.1,<2`; a dropped upper bound, a lowered floor and an added second requirement all fail. Upper bound `<2` refuses a major that could reorder `ctrl_transfer` parameters |
| T-127-02-02 | Repudiation | a vacuous scan passing on zero matches | **high** | mitigate | Non-vacuity assertion before every comparison, plus a monkeypatched planted-file RED demonstration for each of the two gates (research finding A-7) |
| T-127-02-03 | Tampering | `pyusb` migrating into the `test` extra | medium | mitigate | Asserted absent from `test`. Its presence there would make Plan 127-07's entire pyusb-absent proof set vacuous with nothing going red |
| T-127-02-04 | Repudiation | D-17's deviation record drifting away from `flash_method()` | medium | mitigate | Proximity assertion — the `accepted deviation` phrase must occur within 25 lines above `def flash_method(`, not merely somewhere in the file |
| T-127-02-05 | Tampering | a py3.11-only construct breaking the py39 floor | medium | mitigate | No TOML parser imported; `ruff target-version = "py39"` and the mypy watermark enforce it statically, and both are run in the verify command |
| T-127-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install is performed. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit and is a pre-existing branch dependency — no `[ASSUMED]`/`[SUS]` entry exists, so no legitimacy checkpoint is owed. Installing `pyusb` into the shared devcontainer is prohibited: the pyusb-absent tests characterise that environment |
</threat_model>

<verification>
1. The `[py32]` extra reads `pyusb>=1.3.1,<2` and nothing else in `pyproject.toml` moved.
2. The comment beside it records why both bounds exist and cites HOST-07 / D-19.
3. `tests/test_py32_packaging.py` parses no TOML and carries no skip marker.
4. The floor gate asserts exact equality against an independently-written literal.
5. `pyusb` is asserted absent from the `test` extra.
6. Both gates are proven able to fail against a planted file, via `pytest.raises`.
7. The D-17 scan checks five phrases and their proximity to `def flash_method(`, with a non-vacuity guard.
8. `tests/test_skip_census.py` passes — no fifth allow-list entry.
9. `ruff check`, `ruff format --check` and the full suite all pass; the new collected total is recorded, not asserted.
</verification>

<success_criteria>
- HOST-07 is discharged by an exit code rather than by a reader, and a silent revert cannot survive the suite.
- HOST-01's in-code deviation record is held in place by a gate that is itself proven able to fail.
- Neither gate can pass vacuously, and no TOML parser or skip reason was introduced to achieve it.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._upload_block()`, `Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-02-SUMMARY.md` when done
</output>
