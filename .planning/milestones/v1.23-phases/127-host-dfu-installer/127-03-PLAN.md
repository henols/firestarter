---
phase: 127-host-dfu-installer
plan: 03
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - firestarter_app/tests/test_dfu_opcode_anchors.py
autonomous: true
requirements: [HOST-06]

must_haves:
  truths:
    - "HOST-06 is **purely ADDITIVE** (C-2, MEASURED). D-18's instruction that *the existing self-referential assertions in `tests/test_py32_dfu.py` are removed or converted* is **superseded**: a targeted scan returns **no matches** — every use of `DFUSE_ERASE_PAGE`, `DFUSE_SET_ADDRESS`, `DFUSE_VERSION` and `FLASH_BASE` in that file is a *label inside a sequencing assertion*, i.e. exactly the assertions D-18's own next sentence orders kept. Following the struck wording literally would damage 58 working tests to satisfy an instruction its source never gave"
    - "One new module writes the DFU literals **independently**, with a citing comment naming the external source (UM1504 for the DfuSe extensions, USB DFU 1.1 §3 for the request codes and §4.1.3 for the functional descriptor), and asserts the module's constants equal them. Never import-and-compare; never derive the expectation from the thing under test. Same discipline as 126 D-05's CRC32 known-answer vector"
    - "The module also **holds C-2's property forward**: a test asserts that `tests/test_py32_dfu.py` contains no assertion comparing a DFU/DFUSE/FLASH constant directly to a numeric literal, so research finding 7's source==source oracle cannot be re-introduced there later"
    - "**Residual A1 is real and must be carried, not hidden.** The specs are external documents that research did **not** fetch. The anchored values are currently *consistent with the module*, which is weaker than *independent of it*. The module records that limitation in its own docstring and Plan 127-12 carries it into the honesty ledger"
  prohibitions:
    - statement: "No `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "Every commit this plan makes is local. Plan 127-11 is the only place an outward-facing action happens, and there it is the operator's, not an agent's. `--auto`/`--chain` auto-approve human-verify checkpoints, so the structural absence of the command is the gate"
    - statement: "Nothing in `tests/test_py32_dfu.py` is deleted, converted, or otherwise modified by this plan"
      status: required
      verification: "`files_modified` names only the new module. The blob SHA of `tests/test_py32_dfu.py` must be identical before and after this plan, and is recorded in the SUMMARY"
    - statement: "The anchored constants are not imported from `firestarter.py32_dfu` to build the expectation"
      status: required
      verification: "The module writes each expected value as its own literal with a citing comment; the module under test is imported only to supply the *observed* value"
    - statement: "The module adds no skip marker and no `ALLOWED_SKIP_REASONS` entry"
      status: required
      verification: "It is a pure module-level constant comparison and runs identically in both CI legs"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts:
    - "`firestarter_app/tests/test_dfu_opcode_anchors.py`"
  key_links:
    - "Research finding 7 (`research/SUMMARY.md:217`) prescribes only *anchor the literals to UM1504 / DFU 1.1 in one test* — it never says remove. C-2 measures that the removal target does not exist. This plan is where the discrepancy between CONTEXT's D-18 and its own source is resolved in favour of the source."
    - "`_DFU_BIT_CAN_UPLOAD` (bit 1 of `bmAttributes`, DFU 1.1 §4.1.3) is anchored here but **created** by Plan 127-09. Until 127-09 lands, that one anchor has no counterpart to compare against — so this plan anchors the mask as a module-level literal only, and 127-09 adds the equality assertion. Do not assert against a symbol that does not exist yet."
---

<objective>
Add one test module that writes the DFU and DfuSe opcode literals independently from the specifications
and asserts `firestarter/py32_dfu.py`'s constants equal them — and that holds the same property over
`tests/test_py32_dfu.py` going forward.

Purpose: research finding 7 identified the opcode block as a source==source oracle — the module's own
constants asserted against themselves prove nothing. HOST-06 replaces that with an independent oracle,
the same shape 126 D-05 used for its CRC32 known-answer vector.

Output: `tests/test_dfu_opcode_anchors.py`, purely additive, with residual A1 recorded in its docstring.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md
@.planning/phases/127-host-dfu-installer/127-01-SUMMARY.md

**Read D-18 with its correction applied.** `127-CONTEXT.md` D-18 carries an inline
`CORRECTED by 127-RESEARCH.md §C-2` note. **The note wins.** The original wording is preserved as the
superseded claim, not as live instruction. HOST-06 is additive: create one module, delete and convert
nothing.

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`; commit **inside** that
submodule; run from a directory literally named `firestarter_app` with `/workspaces/firestarter` as a
sibling.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** An opcode anchor proves the host constant matches a written-down value. It proves
nothing about a PY32F071 bootloader's behaviour; no PCB exists.
</context>

<!-- Comment-text discipline: the symbol below must be named in the action so the executor knows which
     assertion NOT to write (it does not exist until Plan 127-09), and the same name is the subject of a
     negative grep proving no such assertion was written. The literal is allowed to appear here. -->
<!-- planner-discipline-allow: _DFU_BIT_CAN_UPLOAD -->

<tasks>

<task type="auto">
  <name>Task 1: Record the C-2 evidence and pin tests/test_py32_dfu.py's blob SHA</name>
  <files>(no files modified — read-only capture into this plan's SUMMARY)</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_py32_dfu.py` — the import block at the top and every use of
      `DFUSE_ERASE_PAGE`, `DFUSE_SET_ADDRESS`, `DFUSE_VERSION` and `FLASH_BASE`. Confirm first-hand that
      each is a **label inside a sequencing assertion** and not a comparison against a numeric literal.
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §C-2 in full, including the scan command
      it used and its `NONE FOUND` result.
    - `.planning/research/SUMMARY.md` finding 7 — the source it cites, which prescribes anchoring and
      never prescribes removal.
  </read_first>
  <action>
    Re-derive C-2 first-hand rather than inheriting it, and pin the file this plan must not touch.

    Record:
      - the blob SHA of `tests/test_py32_dfu.py` (`git rev-parse HEAD:tests/test_py32_dfu.py`), its line
        count (654) and its collected test count (58);
      - the result of re-running C-2's scan for any assertion comparing a `DFU_*`, `DFUSE_*` or `FLASH_*`
        constant against a numeric literal in that file — **expected: no matches**;
      - every line in that file that mentions one of those four constants, with a one-word classification
        of each (`sequencing` / `import` / `other`), so the "labels inside sequencing assertions" claim is
        evidenced rather than asserted;
      - a plain statement that D-18's removal instruction is superseded by C-2 and that this plan deletes
        and converts nothing.

    If — contrary to C-2 — a genuine source==source assertion **is** found, do not remove it silently.
    Record it verbatim, note that it contradicts the measurement, and carry it as a finding for Plan
    127-12; converting it would then be a deliberate, recorded act rather than an unexamined execution of
    superseded wording.

    Attempt one read-only lookup of the USB DFU 1.1 §3 request-code table and UM1504's DfuSe command
    values, and record the outcome either way — obtained (with the source) or not obtainable. This is
    research assumption **A1**; it is the single genuine gap in HOST-06's premise, and whichever way it
    resolves must reach Plan 127-12's honesty ledger.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git rev-parse HEAD:tests/test_py32_dfu.py && wc -l tests/test_py32_dfu.py && python3 -m pytest tests/test_py32_dfu.py --collect-only -q -o addopts= 2>&1 | tail -1 && grep -nE "(DFU|DFUSE|FLASH)_[A-Z_]+" tests/test_py32_dfu.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The blob SHA of `tests/test_py32_dfu.py` is recorded as a 40-character hex string.
    - `wc -l tests/test_py32_dfu.py` is recorded as 654; the collected count for that module is recorded as 58.
    - C-2's scan is re-run first-hand and its result recorded verbatim; the expected outcome is no matches.
    - Every line mentioning `DFU_*`, `DFUSE_*` or `FLASH_*` in that file is listed with a `sequencing` / `import` / `other` classification.
    - The SUMMARY states plainly that D-18's removal instruction is superseded by C-2 and that this plan deletes and converts nothing.
    - The A1 spec-lookup attempt is recorded with its outcome — obtained (naming the source) or not obtainable — and phrased so Plan 127-12 can lift it into the honesty ledger.
    - No file was modified by this task.
  </acceptance_criteria>
  <done>C-2 is re-derived rather than inherited, the file HOST-06 must not touch is pinned by blob SHA, and A1's status is on the record before any anchor is written.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_dfu_opcode_anchors.py — independent UM1504 / DFU 1.1 anchors</name>
  <files>firestarter_app/tests/test_dfu_opcode_anchors.py</files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` lines 41-116 — the full wire-constant block:
      the DFU 1.1 request codes at :51-57, `_DFU_FUNCTIONAL_DESCRIPTOR` at :93, the DfuSe commands at
      :96-98, `DFUSE_VERSION` at :100, and `FLASH_BASE` / `FLASH_SIZE` at :107-108. These are the
      *observed* values; do not copy them into the expectation.
    - `/workspaces/firestarter/tests/test_config_storage_dualslot.py` lines 27-28, 107-111 and 573-588 —
      Phase 126 D-05's independent CRC32 known-answer vector: the constant block with its explanatory
      comment, the assertion shape with its formatted failure message, and the docstring paragraph
      explaining *why* an independent vector exists. This is the discipline precedent CONTEXT names.
    - `.planning/phases/127-host-dfu-installer/127-PATTERNS.md` §`tests/test_dfu_opcode_anchors.py` — the
      constant table with each value's anchor line, and the C-2 constraint restated as load-bearing.
    - `/workspaces/firestarter_app/tests/test_skip_census.py` lines 1-90 — the test-module docstring
      convention: MIT header, then a paragraph naming the phase, plan and decision ID, then *why* this
      mechanism was chosen over the rejected one.
    - This plan's Task 1 record — the A1 outcome, which the docstring must reflect honestly.
  </read_first>
  <action>
    Create `tests/test_dfu_opcode_anchors.py`. Nothing else is touched.

    **Docstring.** MIT header, then a paragraph naming Phase 127, Plan 127-03, HOST-06 / D-18 and
    research finding 7; then *why* an independent oracle exists (the module's constants asserted against
    themselves prove nothing — the class v1.13 removed); then the **C-2 note**: HOST-06 is additive and
    `tests/test_py32_dfu.py`'s sequencing assertions are genuinely independent and stay; then **A1**
    stated plainly with whatever Task 1 established — if the specs were not obtained first-hand, say so:
    the values are currently consistent-with-the-module rather than independently sourced, and that is a
    recorded residual, not a silent one.

    **Independent constant block.** One module-level constant per anchored value, each with a citing
    comment naming the exact specification and section. Write every value as its own literal:
      - USB DFU 1.1 §3 request codes: `DFU_DETACH` 0, `DFU_DNLOAD` 1, `DFU_UPLOAD` 2, `DFU_GETSTATUS` 3,
        `DFU_CLRSTATUS` 4, `DFU_GETSTATE` 5, `DFU_ABORT` 6.
      - USB DFU 1.1 §4.1.3: the DFU **functional descriptor** `bDescriptorType` 0x21, and `bitCanUpload`
        as **bit 1** of `bmAttributes` (mask 0x02).
      - Puya/ST **UM1504** DfuSe extensions: `DFUSE_SET_ADDRESS` 0x21, `DFUSE_ERASE_PAGE` 0x41,
        `DFUSE_READ_UNPROTECT` 0x92, and the DfuSe `bcdDFUVersion` 0x011A.
      - The PY32F071xB flash origin `FLASH_BASE` 0x08000000.
    Group them so the two 0x21 values are visibly distinct constants with different meanings — one is a
    descriptor type, the other a DfuSe command — because their numeric collision is the kind of thing a
    careless refactor conflates.

    **Assertions.** One test per group (request codes, functional descriptor, DfuSe commands, DfuSe
    version, flash origin), each importing the observed value from `firestarter.py32_dfu` and comparing
    it against the local literal, with a failure message in the 126 D-05 shape: name the spec, the
    expected value formatted as hex, and the observed value formatted as hex.

    Do **not** assert against `_DFU_BIT_CAN_UPLOAD` — that symbol is created by Plan 127-09 and does not
    exist yet. Define the mask here as a literal with its citation and leave a short comment saying Plan
    127-09 adds the equality assertion once the production constant exists.

    Refer to that production constant **by description** in the comment — *the module's `bitCanUpload`
    mask constant* — and do not write its name. This task's gate asserts the name occurs zero times in
    this file, precisely because its presence would mean an assertion against a symbol that does not yet
    exist; Plan 127-09 is what introduces both the constant and its first mention here.

    **Forward-holding test.** One further test reads `tests/test_py32_dfu.py` as text and asserts it
    contains **no** assertion comparing a `DFU_*`, `DFUSE_*` or `FLASH_*` name directly against a numeric
    literal — C-2's measured property, held forward so finding 7's oracle cannot be re-introduced there.
    Include a non-vacuity guard: the file must be readable and must contain at least one occurrence of
    those constant names at all, otherwise the check would be vacuously true. Failure message must name
    the offending line and point at this module as the place to add an anchor instead.

    No skip marker. `from __future__ import annotations` if PEP-604 annotations are used.

    Commit with a message naming HOST-06, D-18 and C-2.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(git rev-parse HEAD:tests/test_py32_dfu.py)" = "$(git rev-parse HEAD~1:tests/test_py32_dfu.py)" && echo "TEST_PY32_DFU_UNCHANGED" && test "$(grep -c 'importorskip\|skipif\|pytest.skip' tests/test_dfu_opcode_anchors.py)" -eq 0 && test "$(grep -c '_DFU_BIT_CAN_UPLOAD' tests/test_dfu_opcode_anchors.py)" -eq 0 && grep -c 'UM1504' tests/test_dfu_opcode_anchors.py && grep -c 'DFU 1.1' tests/test_dfu_opcode_anchors.py && python3 -m pytest tests/test_dfu_opcode_anchors.py -v --no-cov -o addopts= 2>&1 | tail -12 && python3 -m pytest tests/test_py32_dfu.py -q --no-cov -o addopts= 2>&1 | tail -1 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dfu_opcode_anchors.py` exists; its docstring names Phase 127, Plan 127-03, HOST-06, D-18, finding 7, the C-2 additivity note, and A1's status as Task 1 established it.
    - Every anchored value is written as its own module-level literal with a citing comment naming the specification and section; none is imported from `firestarter.py32_dfu` to build the expectation.
    - All twelve values are anchored: seven request codes, the functional-descriptor type, three DfuSe commands, the DfuSe `bcdDFUVersion`, plus `FLASH_BASE` and the `bitCanUpload` mask.
    - The two 0x21 values are separate constants with distinct citing comments (descriptor type vs DfuSe command).
    - `grep -c '_DFU_BIT_CAN_UPLOAD' tests/test_dfu_opcode_anchors.py` returns 0 — no assertion against a symbol Plan 127-09 has not yet created.
    - `grep -c 'UM1504'` and `grep -c 'DFU 1.1'` each return at least 1.
    - `grep -c 'importorskip\|skipif\|pytest.skip' tests/test_dfu_opcode_anchors.py` returns 0, and `python3 -m pytest tests/test_skip_census.py -q` passes.
    - The forward-holding test passes and carries a non-vacuity guard; its failure message names the offending line and directs the reader to add an anchor here.
    - `tests/test_py32_dfu.py`'s blob SHA is **identical** to the value Task 1 recorded, and `python3 -m pytest tests/test_py32_dfu.py -q` still reports 58 passed.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - The full-suite collected total is recorded in the SUMMARY as an observation only — no assertion pins it (D-04).
  </acceptance_criteria>
  <done>Every DFU and DfuSe literal in `firestarter/py32_dfu.py` is compared against a value written independently from the specification with a citing comment, the source==source oracle cannot return to `tests/test_py32_dfu.py`, and 58 working tests are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a specification → a value in a test file | The citation is a comment, not a fetched document; A1 is the residual |
| the module under test → the test's expectation | Importing the expectation collapses the oracle; the boundary must not be crossed |
| a superseded instruction → an executed edit | D-18's removal wording would damage 58 working tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-03-01 | Repudiation | a source==source opcode oracle | **high** | mitigate | Every expected value written as an independent literal with a citing comment; a forward-holding test prevents the oracle returning to `tests/test_py32_dfu.py` |
| T-127-03-02 | Tampering | executing D-18's superseded removal instruction | **high** | mitigate | C-2 re-derived first-hand in Task 1; `tests/test_py32_dfu.py`'s blob SHA asserted identical before and after the plan |
| T-127-03-03 | Repudiation | a citing comment read as a fetched specification | **medium** | accept | The specs are external documents research did not fetch (A1). Accepted with the residual recorded in the module docstring and carried to Plan 127-12's honesty ledger — the risk is a wrong constant, whose blast radius is confined to a mock-only path |
| T-127-03-04 | Repudiation | a vacuously-true forward-holding scan | medium | mitigate | Non-vacuity guard: the file must be readable and must contain at least one occurrence of the constant names before the absence assertion is evaluated |
| T-127-03-05 | Tampering | conflating the two 0x21 constants | low | mitigate | Anchored as separate constants with distinct citing comments; a refactor merging them fails one of the two assertions |
| T-127-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install is performed and no dependency is added. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. C-2 is re-derived first-hand and its scan result recorded verbatim.
2. `tests/test_py32_dfu.py`'s blob SHA is identical before and after this plan, and it still collects 58 tests.
3. Twelve values are anchored, each as an independent literal with a specification-and-section citation.
4. The two 0x21 constants are distinct with distinct citations.
5. No assertion references `_DFU_BIT_CAN_UPLOAD`, which Plan 127-09 creates.
6. The forward-holding test passes, carries a non-vacuity guard, and names the remedy in its failure message.
7. No skip marker exists and `tests/test_skip_census.py` passes.
8. `ruff check`, `ruff format --check` and the full suite pass; the collected total is recorded, not asserted.
9. A1's status is recorded in both the module docstring and the SUMMARY, in a form Plan 127-12 can lift.
</verification>

<success_criteria>
- HOST-06 is satisfied by an oracle genuinely outside the module under test, in the same discipline as 126 D-05's CRC32 vector.
- The 58 existing DFU sequencing tests are untouched — C-2 applied rather than D-18's struck wording executed.
- The one honest weakness (A1: the specs were not fetched) is written down where a reader meets it, not omitted.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._upload_block()`, `Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-03-SUMMARY.md` when done
</output>
