---
phase: 127-host-dfu-installer
plan: 07
type: execute
wave: 3
depends_on: ["127-05", "127-06"]
files_modified:
  - firestarter_app/firestarter/py32_dfu.py
  - firestarter_app/tests/test_py32_pyusb_absent.py
autonomous: true
requirements: [HOST-05]

must_haves:
  truths:
    - "The `# pragma: no cover` removed is the one on `except ImportError` inside `_require_usb()`, at **`py32_dfu.py:375`** (C-3 — CONTEXT's `:374` is `import usb.util`). It excludes **two** statements, 375 and 376, where 376 is the first line of the multi-line `raise PyusbMissingError(...)`"
    - "Removing it **cannot lower coverage**. `_require_usb()` is already reached by the existing suite: the `try` and `import usb.core` execute and the import **raises** in a pyusb-absent environment, so the handler runs but is unmeasured. Removing the pragma adds two covered statements in the primary leg"
    - "**Two other pragmas at `:659-660` and `:665-666`** (the `_dev` / `_index` guards) are explicitly **out of scope**. Only the `_require_usb()` one is HOST-05's. `grep -c 'pragma: no cover'` in `py32_dfu.py` goes from 3 to 2, not to 0"
    - "HOST-05 is **two tests, deliberately** (D-06). A subprocess contributes nothing to the parent's `--cov-fail-under=70` run, so an **in-process** test monkeypatches `sys.modules` to make `_require_usb()` raise and covers the de-pragma'd lines; D-05's **subprocess** test separately proves the CLI survives *real* absence. Each mechanism does the job it is actually good at"
    - "The message assertions are `pip install 'firestarter[py32]'`, `libusb` and **`WinUSB`** (C-4, MEASURED). The Windows driver-installer utility CONTEXT's D-06 names appears **nowhere** in the module; asserting it would be red on day one. Do not add the word to the message either — D-15 scopes this phase to facts it changed"
    - "*Genuine* absence is produced by a **subprocess with a `sys.meta_path` import blocker** whose `find_spec` **raises** `ModuleNotFoundError` for `usb` and `usb.*`, installed before the CLI is imported, with `sys.modules` purged of `usb*` afterwards. Returning `None` from `find_spec` only defers to the next finder and would let a real pyusb through. This runs identically in **both** CI legs, so it needs **no** skip marker and **no** new `ALLOWED_SKIP_REASONS` entry"
    - "In-process `sys.modules` monkeypatching alone was **rejected** for the CLI half: it *simulates* absence, and an eager top-level `import usb` anywhere would already have succeeded before the fixture ran — precisely the regression HOST-05 exists to catch. The sharpest assertion the subprocess makes is that after the CLI has run, `usb` is **still absent from the child's `sys.modules`**"
    - "`fw --list` is deterministic offline: `list_releases` catches `requests.RequestException` and returns an empty list, after which the CLI prints its header row and exits **0** (verified by reading the code path). The child stubs the HTTP call so the test needs no network and no 10-second timeout, while still running the full command path"
  prohibitions:
    - statement: "No `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "Every commit this plan makes is local. Plan 127-11 is the only place an outward-facing action happens, and there it is the operator's, not an agent's. `--auto`/`--chain` auto-approve human-verify checkpoints, so the structural absence of the command is the gate"
    - statement: "The two `# pragma: no cover` markers on the `_dev` / `_index` guards are not touched"
      status: required
      verification: "Only the `_require_usb()` pragma is in HOST-05's scope. `grep -c 'pragma: no cover' firestarter/py32_dfu.py` must equal 2 after this plan, not 0"
    - statement: "No assertion references the Windows driver-installer utility named in D-06's original wording"
      status: required
      verification: "C-4 measured that word absent from the file. Assert the three strings that exist: `pip install 'firestarter[py32]'`, `libusb`, `WinUSB`"
    - statement: "The `PyusbMissingError` message text is not changed"
      status: required
      verification: "D-15 scopes this phase to facts it changed; a wording edit is not one of them. The test adapts to the message, not the reverse"
    - statement: "The blocker's `find_spec` raises rather than returning `None`"
      status: required
      verification: "Returning `None` defers to the next finder and lets a genuinely installed pyusb through, silently making the `ci-py32` leg's copy of this test vacuous"
    - statement: "`pyusb` is not installed into the shared devcontainer environment"
      status: required
      verification: "These tests characterise that environment. The blocker is what makes them meaningful where pyusb IS installed"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts:
    - "`firestarter/py32_dfu.py` with the `_require_usb()` pragma removed"
    - "`firestarter_app/tests/test_py32_pyusb_absent.py`"
  key_links:
    - "The subprocess blocker was proven **with pyusb genuinely installed** in an isolated venv, not merely under ambient absence — that is the only run that tests the mechanism rather than the environment. The same module must therefore behave identically in the `ci-py32` leg, where pyusb is present."
    - "Coverage is the reason D-06 splits into two tests. If the in-process test is dropped, the de-pragma'd lines go uncovered in the `ci-py32` leg and — once the pragma is gone — the primary leg's `--cov-fail-under=70` loses two statements it was silently getting for free."
---

<objective>
Remove `PyusbMissingError`'s `# pragma: no cover`, cover the code path it was hiding with an in-process
test, and prove separately — in a subprocess where `usb` is genuinely unreachable — that `fw --list` and
`fw --help` both exit 0 with their expected output.

Purpose: the `PyusbMissingError` branch is what **every plain-install user hits**, and it carried a
pragma while being a three-line monkeypatch to test. And nothing today proves the CLI survives pyusb's
absence: a single eager top-level `import usb` anywhere in the import graph would break `fw --help` for
every AVR user, and no test would notice.

Output: the pragma gone with its two statements covered, and `tests/test_py32_pyusb_absent.py` carrying
both mechanisms.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md
@.planning/phases/127-host-dfu-installer/127-05-SUMMARY.md
@.planning/phases/127-host-dfu-installer/127-06-SUMMARY.md

**Read D-06 with its corrections applied.** `127-CONTEXT.md` D-06 carries two inline
`CORRECTED by 127-RESEARCH.md` notes — **C-3** (the pragma is at `:375`, excluding 375 and 376; the
`:659-660` / `:665-666` pragmas are out of scope) and **C-4** (the message names `WinUSB`, and the
driver-installer tool D-06 mentions appears nowhere in the file). **The notes win.**

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`; commit **inside** that
submodule; run from a directory literally named `firestarter_app` with `/workspaces/firestarter` as a
sibling.

**Both legs.** This module carries no skip marker and must behave identically whether pyusb is installed
or not. In the `ci-py32` leg pyusb **is** installed — that is exactly the condition the blocker exists
to handle, and the condition under which research proved it works.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** Proving the CLI survives pyusb's absence proves an import-graph property. It proves
nothing about a PY32F071; no PCB exists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the _require_usb() pragma and cover its two statements in-process</name>
  <files>firestarter_app/firestarter/py32_dfu.py, firestarter_app/tests/test_py32_pyusb_absent.py</files>
  <read_first>
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` — `_require_usb()` in full (the `try`, both
      `import usb.core` / `import usb.util` lines, the `except ImportError as exc:` line carrying the
      pragma, and the whole multi-line `raise PyusbMissingError(...)` body with its three substrings).
      Also locate the two **other** pragmas on the `_dev` and `_index` property guards, and confirm by
      reading that they are unrelated to HOST-05.
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` — the exception hierarchy (`DfuError`,
      `PyusbMissingError`, `DfuDeviceNotFoundError`, `DfuProtocolError`, `ImageError`).
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §C-3 in full — the coverage JSON report
      showing `excluded_lines [375, 376]`, `executed_lines [370, 372, 373, 386]` and the argument that
      removing the pragma adds two covered statements rather than losing coverage.
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §C-4 — the verbatim message body and the
      three substrings that are actually present.
    - `/workspaces/firestarter_app/tests/test_skip_census.py` lines 1-90 — the module-docstring
      convention for the new test module.
  </read_first>
  <action>
    Delete the `# pragma: no cover` comment from the `except ImportError as exc:` line inside
    `_require_usb()`, leaving the `except` clause and the whole `raise PyusbMissingError(...)` body
    unchanged. Change nothing else in `firestarter/py32_dfu.py` — the `_dev` and `_index` pragmas stay,
    the message text stays, and no other line moves.

    Then create `tests/test_py32_pyusb_absent.py` and land its **in-process half**.

    **Docstring.** MIT header; then a paragraph naming Phase 127, Plan 127-07, HOST-05 / D-05 / D-06;
    then *why two mechanisms*: a subprocess contributes nothing to the parent's coverage run, so the
    de-pragma'd lines need an in-process test, while only a genuine subprocess absence can prove the CLI
    import graph is clean; then C-4's note that the assertions target `WinUSB` and not the
    driver-installer utility CONTEXT names, because that word appears nowhere in the module.

    **In-process test(s).** Using `monkeypatch.setitem(sys.modules, ...)` to set both `"usb"` and
    `"usb.core"` to `None` — which makes `import usb.core` raise `ImportError: import of usb.core halted;
    None in sys.modules`, verified behaviour — assert:
      1. `py32_dfu._require_usb()` raises `PyusbMissingError` specifically (not a bare `ImportError`,
         not `DfuError` generically — assert the concrete subclass).
      2. The raised message contains all three of `pip install 'firestarter[py32]'`, `libusb` and
         `WinUSB`. Write those three as independent literals in the test with a comment citing C-4 and
         the measured message body.
      3. The exception **chains** the original `ImportError` — `exc.__cause__` is an `ImportError`. This
         is what proves the `from exc` survived, and it is the diagnostic a user's traceback depends on.
      4. `PyusbMissingError` is a subclass of `DfuError`, so `_install_with_dfu`'s `except DfuError`
         still catches it and converts it to `FirmwareOperationError`. A one-line structural assertion
         that keeps the error-to-exit-code chain honest.
      5. The pragma is gone: read `firestarter/py32_dfu.py` as text, locate the `except ImportError`
         line inside `_require_usb()`, and assert it carries no coverage-exclusion comment — while
         asserting the file still carries exactly **two** such comments in total, so the test also fails
         if someone deletes the `_dev` / `_index` pragmas that are out of scope. Include a non-vacuity
         guard that `_require_usb` was located in the source at all.

    Run the coverage gate afterwards and record the `py32_dfu.py` coverage percentage and the file's
    `Missing` line list before and after, to show the two statements moved from excluded to covered.

    Commit with a message naming HOST-05, D-06, C-3 and C-4.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(grep -c 'pragma: no cover' firestarter/py32_dfu.py)" -eq 2 && grep -n -A12 'def _require_usb' firestarter/py32_dfu.py && python3 -m pytest tests/test_py32_pyusb_absent.py -v --no-cov -o addopts= 2>&1 | tail -14 && python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 -o addopts= 2>&1 | grep -E 'py32_dfu|TOTAL|failed|passed' | tail -6 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/check_mypy_watermark.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pragma: no cover' firestarter/py32_dfu.py` returns exactly **2** — the `_require_usb()` one removed, the `_dev` and `_index` ones untouched.
    - The `except ImportError as exc:` line inside `_require_usb()` carries no coverage-exclusion comment; the `raise PyusbMissingError(...)` body is byte-identical to its pre-task text.
    - `git diff firestarter/py32_dfu.py` for this task shows a single changed line.
    - The in-process test asserts `PyusbMissingError` specifically, all three message substrings written as independent literals with a C-4 citation, `__cause__` being an `ImportError`, and `PyusbMissingError` being a `DfuError` subclass.
    - The source-scan test asserts the pragma's absence from the `_require_usb()` handler **and** the file's total count of two, behind a non-vacuity guard that `_require_usb` was located.
    - `python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70` exits 0, and the `py32_dfu.py` coverage figure plus its `Missing` list are recorded before and after in the SUMMARY.
    - The full suite is green with 0 failures and 0 skips.
    - `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/` and `python tools/check_mypy_watermark.py` all pass.
  </acceptance_criteria>
  <done>The branch every plain-install user hits is measured rather than excluded, its message and its exception chaining are pinned, and the two out-of-scope pragmas are demonstrably still in place.</done>
</task>

<task type="auto">
  <name>Task 2: The subprocess meta_path blocker — fw --list and fw --help under genuine pyusb absence</name>
  <files>firestarter_app/tests/test_py32_pyusb_absent.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_skip_census.py` lines 199-245 — the subprocess harness to
      copy: `functools.lru_cache(maxsize=1)`, `[sys.executable, "-m", ...]`, `cwd=str(_APP_DIR)`,
      `capture_output=True, text=True`, an explicit `timeout=`, and the *prove the argument took effect*
      pre-assertion. Also its docstring's explanation of why in-process re-running cannot work when
      bindings are frozen at import — cite it rather than re-arguing it.
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Q4 — the exact blocker class that was
      measured working **with pyusb genuinely installed**, the requirement that `find_spec` raise rather
      than return `None`, the requirement to purge `sys.modules` of `usb*` after installing it, and the
      measured result that `fw --help` and `fw --list` both exit 0 with `usb in sys.modules: []`.
    - `/workspaces/firestarter_app/firestarter/cli_handlers.py` — the `fw` command's `if list_releases:`
      branch: it calls `app.firmware_manager.list_releases(...)`, prints a header row whose columns are
      `Version`, `Channel`, `Published` and `Asset URL`, then `sys.exit(0)`.
    - `/workspaces/firestarter_app/firestarter/firmware.py` — `list_releases` and `_fetch_all_releases`:
      `requests.RequestException` is caught and an empty list returned, which is why `fw --list` exits 0
      offline. This is the seam the child stubs so the test needs no network.
    - `/workspaces/firestarter_app/firestarter/__init__.py` — the single `__version__` line. The real
      version is a pre-release, so the py32 surface **is** enabled in this test's children; that makes the
      proof stronger, not weaker.
  </read_first>
  <action>
    Add the subprocess half to `tests/test_py32_pyusb_absent.py`.

    **The harness.** A module-scope `_APP_DIR = Path(__file__).parent.parent`. A
    `functools.lru_cache`-wrapped `_run_blocked_cli(argv: tuple[str, ...])` that runs
    `[sys.executable, "-c", program]` with `cwd=str(_APP_DIR)`, `capture_output=True, text=True,
    timeout=120`, and returns a frozen dataclass carrying the invocation's exit code, its combined
    output, and the child's post-run list of `usb*` entries in `sys.modules`.

    **The child program**, in this load-bearing order:
      1. install a `sys.meta_path` finder at position 0 whose `find_spec` **raises**
         `ModuleNotFoundError` (with `name=` set) for `usb` and any `usb.` submodule, and returns `None`
         for everything else;
      2. delete every existing `usb*` entry from `sys.modules` — required in the `ci-py32` leg where
         pyusb is genuinely installed and may already be imported;
      3. assert **in the child** that `import usb` now raises `ModuleNotFoundError`, so a broken blocker
         is a child failure rather than a silently passing test — this is the analog's *prove the
         argument took effect* pre-assertion;
      4. stub the HTTP seam so no network call is made: patch `requests.get` on
         `firestarter.firmware`'s imported `requests` module to raise `requests.RequestException`, which
         `list_releases` already handles by returning an empty list. Do this **after** step 3 and
         **before** importing the CLI;
      5. import the CLI, invoke it with `click.testing.CliRunner` on the requested argv;
      6. print a single JSON line carrying the exit code, the output, and the list of `usb*` keys still
         in `sys.modules`.

    The parent parses the **last** stdout line as JSON and surfaces a non-zero child return code as an
    explicit failure naming the child's stderr — never as a skipped or silently-passing test.

    **Tests:**
      1. `fw --help` exits **0**, and its output contains `--board` and `--usb-id` (the real version is a
         pre-release, so the py32 surface is present — a CLI carrying py32-only options still works with
         pyusb unreachable, which is the stronger claim).
      2. `fw --list` exits **0**, and its output contains the header row's column labels `Version`,
         `Channel`, `Published` and `Asset URL`. Assert nothing about the number of rows.
      3. **Nothing imported `usb`.** After either invocation, the child's `usb*` `sys.modules` list is
         **empty**. This is the sharpest assertion in the module: an eager top-level `import usb`
         anywhere in the import graph would have had to raise, and a lazily-guarded one leaves no trace.
      4. `firestarter --help` (the top-level group, not the `fw` subcommand) exits 0 under the same
         blocker — the smoke path `ci.yml` already runs, now proven pyusb-independent.
      5. The **operator-facing** absence path: `fw --dfu-probe` under the blocker exits non-zero and its
         output contains all three of `pip install 'firestarter[py32]'`, `libusb` and `WinUSB` — proving
         the `PyusbMissingError` message reaches the CLI surface through
         `DfuError` → `FirmwareOperationError` → `ClickException`, not only the library API. Assert the
         three substrings, **not** an exact exit code.

    No skip marker; no `ALLOWED_SKIP_REASONS` entry. The module must run identically in both legs — run
    it once in the devcontainer and once in a **throwaway** `.[test,py32]` venv, and record both results,
    because a pass under ambient absence proves nothing about the mechanism.

    Commit with a message naming HOST-05 and D-05.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(grep -c 'importorskip\|skipif\|pytest.skip' tests/test_py32_pyusb_absent.py)" -eq 0 && grep -c 'meta_path' tests/test_py32_pyusb_absent.py && grep -c 'lru_cache' tests/test_py32_pyusb_absent.py && grep -c 'timeout=' tests/test_py32_pyusb_absent.py && python3 -m pytest tests/test_py32_pyusb_absent.py -v --no-cov -o addopts= 2>&1 | tail -18 && echo "--- pyusb-PRESENT rehearsal in a THROWAWAY venv (the run that tests the mechanism) ---" && V=$(mktemp -d) && python3 -m venv "$V" && "$V/bin/pip" -q install -e ".[test,py32]" && "$V/bin/python" -m pytest tests/test_py32_pyusb_absent.py -v --no-cov -o addopts= 2>&1 | tail -18; rm -rf "$V"; cd /workspaces/firestarter_app && python3 -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('usb') is None else 1)" && echo "DEVCONTAINER_STILL_PYUSB_ABSENT" && python3 -m pytest tests/test_skip_census.py -q --no-cov -o addopts= 2>&1 | tail -2 && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - The blocker's `find_spec` **raises** `ModuleNotFoundError` for `usb` and `usb.*` and returns `None` otherwise; `sys.modules` is purged of `usb*` after it is installed.
    - The child asserts `import usb` raises **before** the CLI is imported, and the parent surfaces a non-zero child return code as an explicit failure naming the child's stderr.
    - The HTTP seam is stubbed in the child so no network request is made; the test's wall-clock time is recorded and is well under the harness timeout.
    - `fw --help` exits 0 and its output contains `--board` and `--usb-id`.
    - `fw --list` exits 0 and its output contains `Version`, `Channel`, `Published` and `Asset URL`; no assertion is made about the number of rows.
    - The child's post-run `usb*` `sys.modules` list is **empty** for every invocation.
    - `firestarter --help` exits 0 under the blocker.
    - `fw --dfu-probe` under the blocker exits non-zero and its output contains all three of `pip install 'firestarter[py32]'`, `libusb` and `WinUSB`; no exact exit code is asserted.
    - The module passes **both** in the devcontainer (pyusb absent) and in a throwaway `.[test,py32]` venv (pyusb present), and both results are recorded — the second is the run that tests the mechanism rather than the environment.
    - After the rehearsal, `importlib.util.find_spec("usb")` in the devcontainer still returns `None`.
    - `grep -c 'importorskip\|skipif\|pytest.skip' tests/test_py32_pyusb_absent.py` returns 0 and `python3 -m pytest tests/test_skip_census.py -q` passes.
    - The full suite is green with 0 failures and 0 skips; `ruff check` and `ruff format --check` both exit 0.
  </acceptance_criteria>
  <done>`fw --list`, `fw --help` and `firestarter --help` are proven to exit 0 with `usb` genuinely unreachable and never imported, and the operator-facing install hint is proven to reach the CLI surface — in an environment where pyusb is installed, so the mechanism is what was tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional dependency → the whole CLI's import graph | One eager `import usb` breaks `fw --help` for every AVR user |
| simulated absence → genuine absence | An in-process poke runs after imports have already succeeded |
| a pragma → an untested branch | The excluded lines are the ones every plain-install user reaches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-07-01 | **Denial of service** | an eager `import usb` breaking the CLI for AVR users | **high** | mitigate | Subprocess `sys.meta_path` blocker whose `find_spec` raises; `fw --help`, `fw --list` and `firestarter --help` asserted exit 0; the child's post-run `usb*` `sys.modules` list asserted empty |
| T-127-07-02 | Repudiation | a test that passes only because the environment happens to lack pyusb | **high** | mitigate | The module is run in a throwaway `.[test,py32]` venv as well, and the child asserts `import usb` raises before proceeding — a broken blocker is a child failure |
| T-127-07-03 | Repudiation | an untested error branch every plain-install user hits | high | mitigate | Pragma removed and the two statements covered in-process, with the exception type, all three message substrings, the `__cause__` chain and the `DfuError` subclass relationship all asserted |
| T-127-07-04 | Tampering | removing the out-of-scope `_dev` / `_index` pragmas | medium | mitigate | The source scan asserts the file's total pragma count is exactly 2, so deleting either of the other two fails |
| T-127-07-05 | Repudiation | asserting a message string that does not exist | medium | mitigate | C-4 measured the driver-installer utility's name absent from the file; the three asserted substrings are the measured ones, written as independent literals with a citation |
| T-127-07-06 | Denial of service | a network call making the test slow or flaky | medium | mitigate | The HTTP seam is stubbed in the child; `list_releases` already returns an empty list on `RequestException`, so `fw --list` still exits 0 through its real code path |
| T-127-07-07 | Repudiation | a blocker that defers instead of blocking | high | mitigate | `find_spec` must raise; returning `None` defers to the next finder. Asserted by the child's own pre-check and by the pyusb-present rehearsal |
| T-127-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | The only install is `pip install -e ".[test,py32]"` inside a **throwaway venv** for the mechanism rehearsal, deleted afterwards; installing into the shared devcontainer is prohibited. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
1. Exactly one line changed in `firestarter/py32_dfu.py`; the file's `pragma: no cover` count is 2.
2. The in-process test asserts the concrete exception type, three measured message substrings, the `__cause__` chain and the `DfuError` subclass relationship.
3. The source-scan test asserts the pragma's absence from `_require_usb()` and the file's total of two, behind a non-vacuity guard.
4. Coverage passes `--cov-fail-under=70`, with `py32_dfu.py`'s figure and `Missing` list recorded before and after.
5. The blocker raises from `find_spec`, purges `sys.modules`, and the child pre-asserts that `import usb` fails.
6. `fw --help`, `fw --list` and `firestarter --help` each exit 0 with their expected output and an empty `usb*` `sys.modules` list.
7. `fw --dfu-probe` surfaces all three message substrings at the CLI, with no exact exit code asserted.
8. The module passes in both the pyusb-absent devcontainer and a throwaway pyusb-present venv, with both recorded.
9. The devcontainer is still pyusb-absent afterwards; `ALLOWED_SKIP_REASONS` still has four entries.
10. `ruff check`, `ruff format --check`, the mypy watermark and the full suite all pass.
</verification>

<success_criteria>
- The `PyusbMissingError` path is measured instead of excluded, and its message, chaining and type are pinned against the code that actually exists.
- The CLI is proven to work with `usb` genuinely unreachable — by a mechanism validated where pyusb **is** installed, not by an environment that happens to lack it.
- Nothing imports `usb` eagerly, and that is asserted rather than assumed.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._upload_block()`, `Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-07-SUMMARY.md` when done
</output>
