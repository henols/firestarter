---
phase: 127-host-dfu-installer
plan: 06
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - firestarter_app/tests/conftest.py
  - firestarter_app/tests/test_pyusb_gating.py
  - firestarter_app/tests/test_pyusb_api_surface.py
  - firestarter_app/.github/workflows/ci.yml
autonomous: true
requirements: [HOST-04]

must_haves:
  truths:
    - "**The optional-dependency gating mechanism is decided and measured: a conditional `collect_ignore` in `tests/conftest.py`, keyed on `importlib.util.find_spec(\"usb\") is None`.** This was the one genuinely undecided mechanism in the phase — no existing test in this repo is gated on an optional dependency — and it was settled by a direct pytest probe, not by reasoning"
    - "Probe result 1: with `usb` **absent**, a bare `python -m pytest -q` does **not collect** the gated module and exits **0**. That is Criterion 1's run and the primary CI leg — green, 0 failures, and **no skip reason**, so `tests/test_skip_census.py`'s allow-list needs no fifth entry"
    - "Probe result 2: with `usb` **present**, the same bare run **does** collect and run the module. The gate inverts correctly rather than staying off"
    - "Probe result 3 — the property that makes this fail-**closed**: `collect_ignore` does **not** suppress a path named explicitly on the command line. With `usb` absent, `pytest tests/test_pyusb_api_surface.py` raises a **collection error and exits 2**, never a silent skip. So the `ci-py32` job, which names the file explicitly, cannot pass by quietly running nothing: a failed `pip install .[test,py32]` surfaces as exit 2, a renamed module as exit 4, and a wrongly-armed ignore as exit 5"
    - "Rejected: `pytest.importorskip(\"usb\")` — it emits a skip reason absent from `ALLOWED_SKIP_REASONS`, which `tests/test_skip_census.py` would flag, and a skip in the `ci-py32` leg would be a **silent green** in the one leg whose entire purpose is to run the module. Rejected: `--ignore=` in `addopts` — `--ignore` suppresses explicitly-named paths too, so `ci-py32` would need an `addopts` override and a developer naming the file directly would get a false *no tests ran*. Rejected: a directory outside `testpaths` — it would fall out of `ruff check firestarter/ tests/`. Rejected: a marker plus `-m 'not pyusb'` in `addopts` — it forces every `import usb` into function bodies and changes deselection semantics for every other invocation in the repo"
    - "The `.[test,py32]` leg is a **separate `ci-py32` job** (D-02), not a matrix dimension: checkout → setup-python 3.11 → `pip install -e .[test,py32]` → run **only** the pyusb-API-surface tests. Unambiguously *distinct from the existing `.[test]`-only leg* (Criterion 2's words), roughly a minute, and isolated so a pyusb or libusb break cannot take down the primary gate. Per the discretion default it runs **no** ruff, mypy, coverage or codegen-drift steps — the primary job already gates all of those over the whole tree"
    - "`workflow_dispatch:` is added to `ci.yml`'s `on:` block and **nothing else** (D-01). The milestone branch is **not** added to `push:` — that would be a literal that rots and would make agent pushes CI-triggering, weakening the gate the last three phases relied on"
    - "`usb.core.find(find_all=True)` is called **for real**, asserting it either enumerates or raises `usb.core.NoBackendError` **explicitly** — never a bare `pass`, which would be vacuous. Two measured facts shape it: it **enumerates** in this devcontainer (8 devices), and `NoBackendError` **subclasses `ValueError`**, so a broad `except ValueError` would silently make the test vacuous. A runner may enumerate **zero** devices without raising, so a non-zero device count must never be asserted"
    - "`ctrl_transfer`'s argument order is pinned against a literal list written independently in the test — `self, bmRequestType, bRequest, wValue, wIndex, data_or_wLength` — because the real pyusb 1.3.1 signature and the in-repo fake have **already drifted** (the fake names its 5th parameter `data` and has no `timeout`), and it works only because all production call-sites pass positionally"
  prohibitions:
    - statement: "No task runs `git push`, `gh workflow run`, `gh pr` or any other outward-facing command"
      status: required
      verification: "This plan writes the workflow; Plan 127-11 obtains the evidence, and there the push and dispatch are the **operator's**. `--auto`/`--chain` auto-approve human-verify checkpoints, so the structural absence of the command is the gate"
    - statement: "The milestone branch is not added to `ci.yml`'s `push:` trigger"
      status: required
      verification: "`workflow_dispatch:` only. `grep -c 'v1.23-py32f071-integration' .github/workflows/ci.yml` returns 0"
    - statement: "`pyusb` is not installed into the shared devcontainer environment"
      status: required
      verification: "The pyusb-**absent** tests (Plan 127-07) characterise that environment; installing it would invert the conditional `collect_ignore` locally and change what the primary suite means. Any rehearsal uses a throwaway venv that is deleted afterwards"
    - statement: "No new entry is added to `ALLOWED_SKIP_REASONS`"
      status: required
      verification: "The mechanism produces a non-collection, not a skip. `tests/test_skip_census.py` must pass unchanged"
    - statement: "No `except ValueError`, `except Exception` or bare `except` wraps the `usb.core.find` call"
      status: required
      verification: "`NoBackendError` subclasses `ValueError`; a broad catch is the *never a bare pass* failure in a different costume"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts:
    - "`firestarter_app/tests/conftest.py` — a conditional `collect_ignore`"
    - "`firestarter_app/tests/test_pyusb_gating.py` — primary-leg guards for the mechanism"
    - "`firestarter_app/tests/test_pyusb_api_surface.py` — the `ci-py32`-only real-pyusb module"
    - "`firestarter_app/.github/workflows/ci.yml` — `workflow_dispatch:` and the `ci-py32` job"
  key_links:
    - "Plan 127-08 aligns `_FakeUsbDevice.ctrl_transfer`'s signature with the real pyusb one (C-6) and adds the fake-vs-real comparison test to `tests/test_pyusb_api_surface.py`. **This plan must not write that comparison** — it would be red until 127-08 lands. This plan pins the real signature only."
    - "Plan 127-11 is where the operator pushes and dispatches. The workflow this plan writes must be on the branch before that happens, and Plan 127-11 records the run URL."
    - "Criterion 2's actual claim is that the **leg exists and is green in CI**. The tests are runnable locally in a venv, but a local rehearsal is preparation, not the evidence."
---

<objective>
Add the `.[test,py32]` CI leg and the test module that genuinely imports pyusb and exercises its real
API surface — and settle, with a measured mechanism, how that module stays out of the primary
pyusb-absent leg without emitting a skip.

Purpose: the real pyusb API surface is exercised nowhere — not in CI (`pip install -e .[test]`, never
`.[test,py32]`) and not locally. `usb.core.find` and `ctrl_transfer`'s argument order are unverified,
and the in-repo fake has already drifted from the real signature. A leg that catches that break in about
three seconds is the whole fix.

Output: a conditional `collect_ignore` with primary-leg guards proving it is armed and non-vacuous;
`tests/test_pyusb_api_surface.py`; and `ci.yml` carrying `workflow_dispatch:` and a `ci-py32` job.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-PATTERNS.md
@.planning/phases/127-host-dfu-installer/127-01-SUMMARY.md

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`; commit **inside** that
submodule; run from a directory literally named `firestarter_app` with `/workspaces/firestarter` as a
sibling.

**Devcontainer vs CI.** Local is py3.12 with ruff 0.16.0; `ci.yml` pins py3.11 and resolves
`ruff>=0.15.14`. `ci.yml` never tests py3.9 despite `requires-python = ">=3.9"`, so the py39 floor is
enforced only statically by `ruff target-version = "py39"` and `mypy python_version = "3.9"`. **No
`tomllib`, no `StrEnum`.** Treat every local run as rehearsal; the operator-dispatched CI run at Plan
127-11 is authoritative.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** Enumerating USB devices on a runner proves pyusb imports and its API is shaped as
expected. It proves nothing about a PY32F071 — no PCB exists, and no DFU device is present in CI.
</context>

<!-- Comment-text discipline: both literals below must be named in their actions — the fake's class name
     so the executor knows which comparison to defer to Plan 127-08, and the branch name so the executor
     knows exactly which literal must NOT be added to the workflow's push trigger. Each is also the
     subject of a negative grep over a file whose content is not derived from the action text. Both are
     allowed to appear here. -->
<!-- planner-discipline-allow: _FakeUsbDevice -->
<!-- planner-discipline-allow: v1.23-py32f071-integration -->

<tasks>

<task type="auto">
  <name>Task 1: The conditional collect_ignore mechanism, with primary-leg guards proving it is armed</name>
  <files>firestarter_app/tests/conftest.py, firestarter_app/tests/test_pyusb_gating.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/conftest.py` in full — the MIT header, the module docstring
      listing what the file exposes, `MAGIC_PREAMBLE_REF`, `_ref_crc8_ccitt`, `build_frame`,
      `_FakeSerial`, and the `fake_serial` / `make_comm` fixtures. The new `collect_ignore` goes at module
      scope near the top, and the docstring's "it exposes" list must be extended.
    - `/workspaces/firestarter_app/pyproject.toml` `[tool.pytest.ini_options]` — `testpaths = ["tests"]`
      and `addopts = "-ra -q"`. Neither is changed by this plan.
    - `/workspaces/firestarter_app/tests/test_skip_census.py` lines 110-141 — `ALLOWED_SKIP_REASONS` and
      the deliberateness rule for adding a fifth entry. This mechanism deliberately produces a
      **non-collection**, not a skip, so no entry is owed.
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` — every `ctrl_transfer(` call site (there are
      five today, in `_detach`, `_get_status`, `_clear_status`, `_abort` and `_dnload`), for the AST scan
      this task writes.
    - `.planning/phases/127-host-dfu-installer/127-PATTERNS.md` §"No Analog Found" — the statement that
      no test in this repo is gated on an optional dependency, and the two honest options it names.
  </read_first>
  <action>
    Land the gating mechanism and the guards that prove it cannot rot silently.

    **`tests/conftest.py`.** At module scope, near the top and above the existing reference constants,
    add a `collect_ignore` list populated conditionally: if `importlib.util.find_spec("usb")` is `None`,
    append `"test_pyusb_api_surface.py"` (a path relative to this conftest's directory, which is how
    pytest interprets `collect_ignore`). Wrap the `find_spec` call so an `ImportError` or `ValueError`
    from a broken installation is treated as **absent** rather than propagating out of conftest import —
    a conftest that raises takes the entire suite down.

    Above it, write a comment block recording, for the next reader: that this is Phase 127 / Plan 127-06 /
    **HOST-04 / D-02**; that the module is gated on an **optional dependency**, the first such gate in
    this repo; that `collect_ignore` is used specifically because it produces a **non-collection** rather
    than a skip, so `ALLOWED_SKIP_REASONS` needs no fifth entry; and — the fail-closed property —
    that `collect_ignore` does **not** suppress a path named explicitly on the command line, so the
    `ci-py32` job naming the file directly gets a hard collection error if pyusb is missing rather than a
    quiet pass. Name the two rejected alternatives (`pytest.importorskip`, `--ignore=` in `addopts`) in
    one sentence each so the choice is not re-litigated.

    Extend the conftest docstring's "it exposes" list with a line for `collect_ignore`.

    **`tests/test_pyusb_gating.py`** — a new module that always runs, in both legs. MIT header, docstring
    naming Phase 127, Plan 127-06, HOST-04 / D-02 / D-03 and stating that its job is to keep the gating
    mechanism honest: a conditional collection rule is invisible when it misfires, so it needs its own
    gate. Tests:
      1. **The gated module exists.** `tests/test_pyusb_api_surface.py` is present on disk. Its failure
         message must say that a rename requires updating both `tests/conftest.py`'s `collect_ignore` and
         the `ci-py32` job's pytest argument.
      2. **Every `collect_ignore` entry names a real file.** Import `tests.conftest`, read its
         `collect_ignore`, and assert each entry resolves to an existing path under `tests/`. This is what
         stops a stale entry from silently disarming nothing.
      3. **The gate is armed correctly for this environment.** Assert the biconditional: the gated
         module's name is in `collect_ignore` **if and only if** `importlib.util.find_spec("usb")` is
         `None`. This is a real behavioural assertion in whichever leg it runs, and it inverts correctly
         between them.
      4. **The condition is keyed on what it claims to be keyed on.** A source scan of
         `tests/conftest.py` asserting the gating block mentions `find_spec` and `usb`, behind a
         non-vacuity guard that the file was read and the `collect_ignore` assignment was located at all.
      5. **No production `ctrl_transfer` call-site passes its 5th argument by keyword** (C-6's second
         strengthening). Parse `firestarter/py32_dfu.py` with `ast`, collect every `ast.Call` whose
         callee attribute is `ctrl_transfer`, and assert each has **at least 5 positional args and zero
         keywords**. Include a non-vacuity assertion that at least 5 such call sites were found — record
         the count in the message, and use a floor rather than an exact number because Plan 127-09 adds
         UPLOAD call sites. This lives here, not in the gated module, precisely because it needs no pyusb
         and should run in **every** suite execution.

    Commit with a message naming HOST-04 and D-02.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -n -B12 -A10 'collect_ignore' tests/conftest.py && python3 -c "
import importlib.util, sys
sys.path.insert(0, '.')
import tests.conftest as c
absent = importlib.util.find_spec('usb') is None
print('usb absent:', absent, '| collect_ignore:', c.collect_ignore)
assert ('test_pyusb_api_surface.py' in c.collect_ignore) == absent
print('GATE_ARMED_CORRECTLY')
" && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && python3 -m pytest tests/test_pyusb_gating.py -v --no-cov -o addopts= 2>&1 | tail -12 && python3 -m pytest tests/test_skip_census.py -q --no-cov -o addopts= 2>&1 | tail -2 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `collect_ignore` at module scope, populated only when `importlib.util.find_spec("usb")` is `None`, with the `find_spec` call guarded so a broken install cannot raise out of conftest import.
    - The comment block above it names Phase 127, Plan 127-06, HOST-04, D-02, the non-collection-not-skip rationale, the fail-closed explicit-path property, and both rejected alternatives.
    - The conftest docstring's "it exposes" list mentions `collect_ignore`.
    - `tests/test_pyusb_gating.py` exists and its five tests pass in the current (pyusb-absent) environment.
    - The biconditional guard passes: `"test_pyusb_api_surface.py" in collect_ignore` is exactly `importlib.util.find_spec("usb") is None`.
    - The `collect_ignore`-entries-exist guard passes and its failure message names both `tests/conftest.py` and the `ci-py32` pytest argument as the two places a rename must be reflected.
    - The `ast` call-site scan finds at least 5 `ctrl_transfer` calls, asserts each has ≥5 positional args and 0 keywords, and carries a non-vacuity assertion recording the count.
    - `python3 -m pytest tests/ -q` is green with **0 failures and 0 skips**, and `tests/test_pyusb_api_surface.py` does not appear in the collected set (it does not exist yet at this task; the guard for its existence lands with Task 2 — sequence the two tasks so the suite is green at each commit, or land both in one commit and say so).
    - `python3 -m pytest tests/test_skip_census.py -q` passes — `ALLOWED_SKIP_REASONS` still has exactly four entries.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
  </acceptance_criteria>
  <done>The optional-dependency gate is armed, keyed on the thing it claims to be keyed on, proven to invert with the environment, and unable to rot into a stale entry that silently ignores nothing.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_pyusb_api_surface.py — real usb.core.find and a pinned ctrl_transfer signature</name>
  <files>firestarter_app/tests/test_pyusb_api_surface.py</files>
  <read_first>
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Q3 in full — the measured
      `usb.core.find(find_all=True)` result (enumerates, 8 devices), the real pyusb 1.3.1 `ctrl_transfer`
      signature, the table of what each of the five production call-sites passes positionally, and the two
      facts the test author needs (`NoBackendError` subclasses `ValueError`; a runner may enumerate zero).
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Common Pitfalls 6 and 7 — the broad-catch
      trap and the non-zero-device-count trap.
    - `/workspaces/firestarter_app/firestarter/py32_dfu.py` — the five `ctrl_transfer` call sites, and
      `_OUT` / `_IN` (`0x21` / `0xA1`), so the test knows what "passes positionally" means concretely.
    - `/workspaces/firestarter_app/tests/test_skip_census.py` lines 1-90 — the module-docstring
      convention.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-03** with its
      `REFINED by 127-RESEARCH.md §C-6 + §Q3` note.
  </read_first>
  <action>
    Create `tests/test_pyusb_api_surface.py`. This module runs **only** where pyusb is installed — the
    `ci-py32` leg — by virtue of Task 1's `collect_ignore`. It may therefore `import usb.core` at module
    top level.

    **Docstring.** MIT header; then a paragraph naming Phase 127, Plan 127-06, HOST-04 / D-03 and
    research finding 8 (the real pyusb API surface is exercised nowhere); then how the module is gated —
    `tests/conftest.py`'s conditional `collect_ignore`, chosen because it produces a non-collection rather
    than a skip — and that the `ci-py32` job names this file explicitly, which is why a missing pyusb
    surfaces as a collection error rather than a quiet pass; then the claim ceiling: enumerating devices
    proves pyusb's API shape, not anything about a PY32F071.

    **Independent expectation.** A module-level literal
    `_CTRL_TRANSFER_PARAMS = ["self", "bmRequestType", "bRequest", "wValue", "wIndex",
    "data_or_wLength"]` with a comment naming pyusb 1.3.1 as the measured source and stating that all
    production call-sites pass these positionally, which is exactly why a rename or reorder would be
    silent without this pin.

    **Tests:**
      1. **pyusb genuinely imports**, and its version is recorded via `importlib.metadata.version("pyusb")`
         in the assertion message. Assert the resolved version satisfies the declared floor by comparing
         its first two components as integers against 1 and 3 — do **not** compare version strings
         lexically.
      2. **`usb.core.find(find_all=True)` for real.** Initialise an `outcome` sentinel to `None`; call it
         inside a `try` whose **only** `except` clause is `usb.core.NoBackendError` — no `ValueError`, no
         `Exception`, no bare `except`. On success set `outcome` to the enumerated branch and materialise
         the generator into a list; on `NoBackendError` set it to the no-backend branch. Then assert
         `outcome is not None` and that it is one of the two named branches, and record which branch was
         taken in the assertion message. **Assert nothing about the device count** — zero is legitimate on
         a runner. If the enumerated branch was taken, assert the result is a list.
      3. **`NoBackendError` is a `ValueError`.** Assert the subclass relationship explicitly, with a
         docstring saying this is why test 2 catches it first and narrowly — Pitfall 6 recorded as a live
         assertion rather than a comment. Word that docstring so it never spells out a broad handler
         clause: the gate below counts **zero** occurrences of a handler naming `ValueError` or
         `Exception` anywhere in this file, and a docstring example is textually indistinguishable from
         a real one. Say *a broad catch would swallow it* rather than illustrating the code shape.
      4. **`ctrl_transfer`'s parameter order is pinned.** Read
         `inspect.signature(usb.core.Device.ctrl_transfer)` and assert the first six parameter names, in
         order, equal `_CTRL_TRANSFER_PARAMS`. Failure message must print the observed list beside the
         expected one and name a pyusb upgrade as the likely cause.
      5. **`timeout` is available and optional.** Assert a `timeout` parameter exists in the real
         signature and has a default — recording that the in-repo fake lacks it, which Plan 127-08
         reconciles.

    **Do not** write any assertion comparing `_FakeUsbDevice.ctrl_transfer` against the real signature —
    the fake still names its 5th parameter `data` and Plan 127-08 aligns it. Leave a short comment saying
    Plan 127-08 adds that comparison to this module.

    In that comment, refer to the fake **by description** — *the in-repo fake USB device in
    `tests/test_py32_dfu.py`* — and do not write its class name. This task's gate asserts the class name
    occurs zero times in this file, which is exactly what proves the comparison was deferred rather than
    written; Plan 127-08 Task 3 is what introduces the name here.

    No skip marker of any kind — the module is gated by collection, not by a marker.

    Commit with a message naming HOST-04 and D-03.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test -f tests/test_pyusb_api_surface.py && test "$(grep -c 'importorskip\|skipif\|pytest.skip' tests/test_pyusb_api_surface.py)" -eq 0 && test "$(grep -cE 'except (ValueError|Exception)\b|except:' tests/test_pyusb_api_surface.py)" -eq 0 && test "$(grep -c '_FakeUsbDevice' tests/test_pyusb_api_surface.py)" -eq 0 && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && python3 -m pytest tests/test_pyusb_gating.py -v --no-cov -o addopts= 2>&1 | tail -10 && echo "--- pyusb-present rehearsal in a THROWAWAY venv ---" && V=$(mktemp -d) && python3 -m venv "$V" && "$V/bin/pip" -q install -e ".[test,py32]" && "$V/bin/python" -m pytest tests/test_pyusb_api_surface.py -v --no-cov -o addopts= 2>&1 | tail -14; rm -rf "$V"; cd /workspaces/firestarter_app && python3 -c "import importlib.util,sys; sys.exit(0 if importlib.util.find_spec('usb') is None else 1)" && echo "DEVCONTAINER_STILL_PYUSB_ABSENT" && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pyusb_api_surface.py` exists with the MIT header and a docstring naming Phase 127, Plan 127-06, HOST-04/D-03, finding 8, the `collect_ignore` gating and its fail-closed explicit-path property, and the claim ceiling.
    - `_CTRL_TRANSFER_PARAMS` is a module-level literal with a comment naming pyusb 1.3.1 as the measured source; it is not derived from `inspect` at import.
    - `grep -cE 'except (ValueError|Exception)\b|except:' tests/test_pyusb_api_surface.py` returns **0** — the only `except` clause names `usb.core.NoBackendError`.
    - `grep -c 'importorskip\|skipif\|pytest.skip' tests/test_pyusb_api_surface.py` returns 0.
    - `grep -c '_FakeUsbDevice' tests/test_pyusb_api_surface.py` returns 0 — the fake-vs-real comparison is Plan 127-08's, and a comment says so.
    - The `usb.core.find` test uses an `outcome` sentinel initialised to `None`, asserts it is not `None`, records which branch was taken, and asserts **nothing** about device count.
    - All five tests pass in a **throwaway** venv installed with `.[test,py32]`, and the rehearsal venv is deleted afterwards.
    - After the rehearsal, `importlib.util.find_spec("usb")` in the devcontainer still returns `None` — pyusb was **not** installed into the shared environment.
    - In the devcontainer (pyusb absent), `python3 -m pytest tests/ -q` is green with 0 failures and 0 skips, and `tests/test_pyusb_api_surface.py` is **not** in the collected set.
    - `tests/test_pyusb_gating.py`'s gated-module-exists guard now passes.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0 (the module is inside `tests/` and is therefore lint-gated even though it is not collected here).
  </acceptance_criteria>
  <done>A test genuinely imports pyusb, calls `usb.core.find` for real with an explicit either/or outcome, and pins `ctrl_transfer`'s parameter order against an independently written list — and it runs nowhere that pyusb is absent, without emitting a skip.</done>
</task>

<task type="auto">
  <name>Task 3: ci.yml — workflow_dispatch and the separate ci-py32 job</name>
  <files>firestarter_app/.github/workflows/ci.yml, firestarter_app/tests/test_pyusb_gating.py</files>
  <read_first>
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` in full — the three phase-and-decision header
      comment lines, the `on:` block with its `push: branches: [main]` + `paths-ignore` and its
      `pull_request:` arm, and the single `ci:` job's step sequence: checkout, setup-python 3.11, the four
      codegen/catalog steps, `pip install -e .[test]`, ruff, ruff format, the mypy watermark, pytest with
      coverage, and the smoke test.
    - `/workspaces/firestarter_app/.github/workflows/beta-release.yml`, `release.yml` and `publish.yml` —
      read only their `on:` blocks, to re-confirm first-hand that pushing `v1.23-py32f071-integration`
      fires none of them (`beta-release.yml` is `beta`-only plus dispatch; `release.yml` and `ci.yml`'s
      `push` are `main`-only; `publish.yml` is `release: published` plus dispatch). Record the finding —
      it is the reason D-01 is safe, and Plan 127-11 depends on it.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` **D-01** and **D-02**, including D-01's
      list of rejected alternatives (a draft PR; adding the branch name to `push:`).
    - `/workspaces/firestarter_app/tests/test_pyusb_gating.py` as Tasks 1-2 left it.
  </read_first>
  <action>
    Add the dispatch trigger and the second leg, then hold the workflow's reference to the gated module
    with a test.

    **Trigger.** Add `workflow_dispatch:` to `ci.yml`'s `on:` block. Add **nothing else** — in particular
    do **not** add `v1.23-py32f071-integration`, or any branch, to `push:`. A branch literal rots, and it
    would make agent pushes CI-triggering, weakening the structural gate Phases 124, 125 and 126 relied
    on.

    **The `ci-py32` job.** A second job alongside `ci:`, not a matrix dimension on it. Steps, in order:
      - `actions/checkout@v4`;
      - `actions/setup-python@v5` pinned to `'3.11'`, matching the primary job so a failure is
        attributable to pyusb rather than to a Python version difference;
      - `pip install -e .[test,py32]`;
      - a step that proves pyusb is genuinely importable and records its resolved version, printing
        `importlib.metadata.version("pyusb")` — so the log carries the version the run actually resolved,
        which is what Plan 127-11 records as evidence;
      - `pytest tests/test_pyusb_api_surface.py -q`, naming the file explicitly.
    Per the discretion default, add **no** ruff, ruff-format, mypy, coverage or codegen-drift steps: the
    primary `ci` job already gates all of those over the whole tree and duplicating them adds nothing.

    Follow the file's existing header-comment convention — add one line naming **Phase 127 (D-01/D-02)**
    and summarising both changes. Keep that line to the two changes themselves: it must **not** name the
    milestone branch, because the gate below asserts the branch literal occurs zero times anywhere in
    the workflow file — a comment mentioning it is indistinguishable, to a text scan, from a trigger
    entry. In the job body, add a short comment recording the three fail-closed
    exit codes the explicit-path invocation gives: a failed extra install surfaces as a collection error,
    a renamed module as pytest's file-not-found, and a wrongly-armed `collect_ignore` as pytest's
    nothing-collected — so the job cannot pass by running zero tests.

    **The workflow-reference guard.** Append one test to `tests/test_pyusb_gating.py`: read
    `.github/workflows/ci.yml` as text and assert it contains the literal path
    `tests/test_pyusb_api_surface.py`, that a job named `ci-py32` is declared, and that the string
    `.[test,py32]` appears. Behind a non-vacuity guard that the workflow file was read and is non-empty.
    Its failure message must say that renaming the gated module requires updating three places:
    `tests/conftest.py`'s `collect_ignore`, this workflow, and the module itself.

    **Run nothing outward-facing.** Do not `git push`, do not `gh workflow run`. Plan 127-11 is where the
    operator does both.

    Commit with a message naming HOST-04, D-01 and D-02.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -n -A4 '^on:' .github/workflows/ci.yml && test "$(grep -c 'workflow_dispatch:' .github/workflows/ci.yml)" -eq 1 && test "$(grep -c 'v1.23-py32f071-integration' .github/workflows/ci.yml)" -eq 0 && test "$(grep -c 'ci-py32:' .github/workflows/ci.yml)" -eq 1 && test "$(grep -c 'test,py32' .github/workflows/ci.yml)" -ge 1 && test "$(grep -c 'tests/test_pyusb_api_surface.py' .github/workflows/ci.yml)" -eq 1 && python3 -c "
import sys
try:
    import yaml
except ImportError:
    print('PyYAML absent - structural check skipped, textual greps stand'); sys.exit(0)
d = yaml.safe_load(open('.github/workflows/ci.yml'))
trig = d.get(True, d.get('on'))
assert 'workflow_dispatch' in trig, trig
assert set(trig['push']['branches']) == {'main'}, trig['push']
assert 'ci-py32' in d['jobs'] and 'ci' in d['jobs'], list(d['jobs'])
steps = d['jobs']['ci-py32']['steps']
names = [s.get('name','') for s in steps]
runs = ' '.join(str(s.get('run','')) for s in steps)
assert '.[test,py32]' in runs, runs
assert 'tests/test_pyusb_api_surface.py' in runs, runs
assert 'ruff' not in runs and 'mypy' not in runs and 'cov-fail-under' not in runs, runs
print('CI_YML_STRUCTURE_OK', names)
" && python3 -m pytest tests/test_pyusb_gating.py -v --no-cov -o addopts= 2>&1 | tail -12 && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflow_dispatch:' .github/workflows/ci.yml` returns exactly 1 and it is inside the `on:` block.
    - `grep -c 'v1.23-py32f071-integration' .github/workflows/ci.yml` returns **0**; `push:` still lists only `main`.
    - A job named `ci-py32` exists alongside `ci`, with checkout, `setup-python` pinned to `'3.11'`, `pip install -e .[test,py32]`, a pyusb-import-and-version step, and `pytest tests/test_pyusb_api_surface.py -q`.
    - The `ci-py32` job contains **no** ruff, ruff-format, mypy, coverage or codegen-drift step.
    - `grep -c 'tests/test_pyusb_api_surface.py' .github/workflows/ci.yml` returns exactly 1.
    - A header comment line naming Phase 127 (D-01/D-02) is present in the file's existing convention, and an in-job comment records the three fail-closed exit codes.
    - The workflow-reference guard test passes and its failure message names all three places a rename must be reflected.
    - The `on:` blocks of `beta-release.yml`, `release.yml` and `publish.yml` are re-read first-hand and recorded in the SUMMARY, with the explicit conclusion that pushing this milestone branch fires none of them.
    - `python3 -m pytest tests/ -q` is green with 0 failures and 0 skips.
    - **No task ran `git push` or `gh workflow run`**, and the SUMMARY states this explicitly.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
  </acceptance_criteria>
  <done>`ci.yml` can be dispatched by hand and carries a second, isolated leg that installs `.[test,py32]` and runs the real-pyusb module by name — with no branch literal added to `push:` and no outward-facing command executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional dependency present → optional dependency absent | The conditional collection rule; invisible when it misfires |
| a CI job exiting 0 → tests having actually run | Zero-collected is the silent-green shape this job must not have |
| a workflow trigger → an unintended release | Adding a branch to `push:` would make agent pushes CI-triggering |
| a local rehearsal → CI evidence | Devcontainer py3.12 vs CI py3.11; only CI is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-06-01 | **Repudiation** | the `ci-py32` job passing while collecting nothing | **high** | mitigate | The job names the file explicitly, and `collect_ignore` does not suppress explicit paths (measured) — a missing extra is exit 2, a renamed module exit 4, a wrongly-armed ignore exit 5. A separate step proves pyusb imports and prints its resolved version |
| T-127-06-02 | Repudiation | a vacuous `usb.core.find` test | **high** | mitigate | Sentinel-based either/or outcome, only `usb.core.NoBackendError` caught, `except ValueError`/`Exception`/bare prohibited and grep-asserted, no device-count assertion |
| T-127-06-03 | Repudiation | a stale `collect_ignore` entry silently ignoring nothing | high | mitigate | A primary-leg guard asserts every entry resolves to an existing file, plus a biconditional armed-correctly assertion and a source scan proving the condition is keyed on `find_spec("usb")` |
| T-127-06-04 | **Elevation of privilege** | a branch literal in `push:` making agent pushes CI-triggering | **high** | mitigate | `workflow_dispatch:` only; `grep -c 'v1.23-py32f071-integration'` asserted 0 and `push:` asserted to list only `main`. The other three workflows' triggers re-read first-hand |
| T-127-06-05 | Tampering | supply chain — pyusb resolved at install time in CI | medium | mitigate | Floor and upper bound `>=1.3.1,<2` (Plan 127-02); the job prints the resolved version into the log, and the signature pin fails on a parameter rename or reorder |
| T-127-06-06 | Tampering | installing pyusb into the shared devcontainer | medium | mitigate | Prohibited. Rehearsal happens in a throwaway venv that is deleted; the acceptance criteria re-assert `find_spec("usb") is None` afterwards |
| T-127-06-07 | Repudiation | a renamed module silently dropping the leg | medium | mitigate | Three-place rename guard: `collect_ignore` entry existence, module existence, and the workflow's textual reference to the path |
| T-127-06-08 | Elevation of privilege | an agent performing the push or dispatch | high | mitigate | No task runs either command; Plan 127-11 carries the operator gate, and the structural absence is the control because `--auto`/`--chain` auto-approve human-verify checkpoints |
| T-127-06-SC | Tampering | npm/pip/cargo installs | **high** | mitigate | The only install is `pip install -e ".[test,py32]"` inside a **throwaway venv** for rehearsal, plus the same command inside the CI job. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit — long-established, high-download, `github.com/pyusb/pyusb`, and a pre-existing dependency of the merged branch. No `[ASSUMED]` or `[SUS]` package is introduced, so no blocking legitimacy checkpoint is owed |
</threat_model>

<verification>
1. `collect_ignore` exists in `tests/conftest.py`, is keyed on `find_spec("usb")`, is guarded against a raising `find_spec`, and is documented with its rationale and both rejected alternatives.
2. The armed-correctly biconditional, the entries-exist guard and the condition source scan all pass in the pyusb-absent environment.
3. The `ast` call-site scan finds ≥5 `ctrl_transfer` calls, each with ≥5 positional args and 0 keywords, behind a non-vacuity assertion.
4. `tests/test_pyusb_api_surface.py` exists, catches only `usb.core.NoBackendError`, asserts no device count, and pins the six-name parameter order against an independent literal.
5. The module passes in a throwaway `.[test,py32]` venv; the devcontainer is still pyusb-absent afterwards.
6. In the devcontainer the full suite is green with 0 failures and 0 skips, and the gated module is not collected.
7. `ci.yml` gains `workflow_dispatch:` and only that; `push:` still lists only `main`; no branch literal was added.
8. `ci-py32` exists as a separate job with the five steps and no lint/type/coverage/codegen step.
9. The workflow-reference guard passes and names all three rename sites.
10. `ALLOWED_SKIP_REASONS` still has four entries; no task ran `git push` or `gh workflow run`.
</verification>

<success_criteria>
- The one genuinely undecided mechanism in the phase is decided, documented with its rejected alternatives, and proven to behave correctly in **both** legs rather than argued.
- A CI leg exists that installs `.[test,py32]` and genuinely imports pyusb, exercising `usb.core.find` and `ctrl_transfer` rather than a mock of the module.
- The leg cannot pass by running nothing, and the primary leg stays green with 0 failures and 0 skips and no new allow-listed skip reason.
- The workflow gains only a manual trigger — no branch literal, no automatic firing, no outward-facing command executed by any task.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._upload_block()`, `Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-06-SUMMARY.md` when done
</output>
