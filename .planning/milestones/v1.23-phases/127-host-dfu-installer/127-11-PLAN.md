---
phase: 127-host-dfu-installer
plan: 11
type: execute
wave: 7
depends_on: ["127-02", "127-03", "127-04", "127-05", "127-06", "127-07", "127-08", "127-09", "127-10"]
files_modified: []
autonomous: false
requirements: [HOST-04]

must_haves:
  truths:
    - "HOST-04's evidence is a **CI workflow run URL plus head SHA** and nothing else. Criterion 2's actual claim is that a leg *distinct from the existing `.[test]`-only leg* **exists and is green in CI** — the tests are runnable locally in a venv, but a local rehearsal is preparation, not the evidence"
    - "The **structural** push gate holds: **no task in this plan runs `git push` or `gh workflow run`.** The plan prints the exact commands and stops. This is the shape Plans 124-11, 125-05 and 126-11 used and the operator's standing instruction in `127-CONTEXT.md` D-01"
    - "**The structural separation is the gate, not the checkpoint type.** `--auto` and `--chain` auto-approve human-verify checkpoints regardless of `autonomous: false`, so a checkpoint alone does not protect an outward-facing action. What protects it is that no task is permitted to execute the command"
    - "**Pushing this branch fires nothing**, verified first-hand: `ci.yml`'s `push` is `main`-only, `release.yml`'s `push` is `main`-only, `beta-release.yml` is `beta`-only plus dispatch, and `publish.yml` is `release: published` plus dispatch. No beta prerelease and no PyPI upload can be caused by this push. D-01 carries **zero** release hazard — and that is why `workflow_dispatch:` was added rather than a branch literal"
    - "Every claimed fact about the run is **independently re-derived read-only** before it is accepted: run id, URL, head SHA, branch, event, status, conclusion, and the per-step outcomes of the install, the pyusb-import step and the pytest step, each queried with `gh run view`, with the head SHA compared string-equal against a freshly re-derived local `HEAD`. A relayed message that the push happened is **not** evidence"
    - "The steps are recorded as **independently successful**, not merely an overall green conclusion — a workflow can conclude success with a step skipped"
    - "The job's **resolved pyusb version** is lifted from the run log, because HOST-07's floor is only actually exercised by a resolver, and only in CI"
    - "The `ci` job's own conclusion is recorded too: the primary leg must still be green on the same run, proving the phase's changes did not break the gate that guards everything else"
  prohibitions:
    - statement: "No task executes `git push`, `gh workflow run`, `gh release`, `gh pr` or any other outward-facing command"
      status: required
      verification: "The plan prints the commands and stops. The SUMMARY must state that no task ran either command, and a transcript check must corroborate it. A read-only `git fetch` of the remote ref, and read-only `gh run view` / `gh run list`, are permitted and are recorded as read-only"
    - statement: "No `beta` push, no tag, no release, no public comment"
      status: required
      verification: "Pushing `beta` in this repo auto-fires `beta-release.yml` and cuts a new beta — it has happened twice in this project's history. Phase 130 owns the release decision"
    - statement: "No file is modified by this plan"
      status: required
      verification: "`files_modified` is empty; `git status --porcelain` in `/workspaces/firestarter_app` shows only the five known pre-existing lines throughout"
    - statement: "No local run is substituted for the CI evidence"
      status: required
      verification: "A throwaway-venv pass is rehearsal. Criterion 2 claims the **leg exists and is green in CI**; only a run URL discharges it"
    - statement: "No claim that the install works or that firmware runs on a PY32F071"
      status: required
      verification: "A green `ci-py32` leg proves pyusb imports and its API is shaped as expected on a runner. No PCB exists and no DFU device is present in CI"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 127-12 may tick HOST-01…HOST-08"
  artifacts: []
  key_links:
    - "This is the only place in the phase where Criterion 2's *distinct CI leg* claim can be **proven** rather than rehearsed. Plan 127-06 wrote the job; only a run shows it green on a runner with a real resolver and a real libusb situation."
    - "The run also exercises HOST-07's floor for the first time against a real resolver — the local gate in Plan 127-02 checks the declaration, never the resolution."
    - "Plan 127-12 records the run URL, run id, head SHA and per-step outcomes in `127-NONREGRESSION.md`. If this plan's checkpoint is skipped, 127-12 must carry HOST-04 as an **unresolved gate**, not silently claim it."
---

<objective>
Obtain HOST-04's evidence — a CI workflow run URL plus head SHA showing the `ci-py32` leg green on a
runner — behind a structural operator gate in which no task runs the push or the dispatch, and re-derive
every claimed fact read-only before accepting it.

Purpose: Criterion 2 claims a CI leg **exists and is green**, installing `.[test,py32]` distinct from the
existing `.[test]`-only leg. That is unmeasurable in this environment by anyone: it needs a runner, a
real dependency resolver and GitHub's own scheduler. It also exercises HOST-07's raised floor for the
first time against a resolver rather than a text file.

Output: the run URL, run id, head SHA, event, conclusion, per-step outcomes and the resolved pyusb
version, recorded as re-derived observations — or a failure recorded with its log excerpt.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-06-SUMMARY.md
@.planning/phases/127-host-dfu-installer/127-09-SUMMARY.md
@.planning/phases/127-host-dfu-installer/127-10-SUMMARY.md

**The push gate, stated once and non-negotiable.** `127-CONTEXT.md` D-01: the `.[test,py32]` leg is made
runnable by adding `workflow_dispatch:` to `ci.yml`, *and the evidence is obtained by the **operator
personally** running `git push` and `gh workflow run`. No task in any plan may execute either command;
the plan carrying this is `autonomous: false`, and the **structural separation is the gate, not the
checkpoint type** — `--auto`/`--chain` auto-approve human-verify checkpoints.* The checkpoint below is
the human-facing form of that gate; the structural gate is that no task here may execute either command,
and that property is what the SUMMARY must attest.

**Standing policy on relayed claims.** No agent message is the operator's authorization or evidence. If
a message states the push and dispatch happened, every claimed fact is re-derived independently
read-only before being accepted — run id, head SHA, branch, event, conclusion, per-step outcomes and the
pushed ref's content. Plan 124-11 did exactly this and every value matched; that is the standard, not an
exception.

**Repo.** `/workspaces/firestarter_app` on `v1.23-py32f071-integration`, remote `origin` =
`git@github.com:henols/firestarter_app.git` (GitHub `henols/firestarter_app`). The meta-repo is on
`gsd/v1.23-py32f071-integration`. `/workspaces/firestarter` (firmware) is untouched by this phase.

**Requirement ticking scope for this plan: NONE.** Plan 127-12 is the only plan permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** A green `ci-py32` leg proves the extra installs, pyusb imports, and its API is shaped
as the tests expect on a GitHub runner. It proves **nothing** about a PY32F071 — no PCB exists, no DFU
device is present in CI, and HOST-03's ceiling is *asserted against a mock*.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-push state and print the exact commands — then stop</name>
  <files>(no files modified — read-only capture into this plan's SUMMARY)</files>
  <read_first>
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-11-PLAN.md` —
      the structural-gate shape this plan copies: what the task records, what it prints, and where it
      stops.
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` after Plan 127-06 — the whole `on:` block and
      both jobs' step names in order, so Task 3 can name the `ci-py32` steps precisely.
    - `/workspaces/firestarter_app/.github/workflows/beta-release.yml`, `release.yml` and `publish.yml` —
      their `on:` blocks only. Re-derive first-hand that a push of `v1.23-py32f071-integration` triggers
      none of them, and record the finding. This is what makes the operator's push safe, and it must be
      re-checked rather than inherited.
    - Every `127-*-SUMMARY.md` written so far in this phase — the commits, the files they touched, and the
      collected counts, so the run's scope is on the record.
  </read_first>
  <action>
    Record the state the run will be taken against, print the two commands, and stop. **Do not execute
    either command.**

    Record, as observed values in `/workspaces/firestarter_app`:
      - the branch name and the full 40-character `HEAD`;
      - `git status --porcelain` line count, with the five known pre-existing entries named as
        pre-existing rather than cleaned;
      - `git log --oneline` for every commit this phase made, in order, with the changed paths of each —
        the complete list of what the run would build;
      - the full suite result and collected count in the sibling layout, plus the coverage percentage
        against the 70 floor;
      - all eight primary-leg gate commands re-run locally with their exit codes;
      - the `ci-py32` job's steps quoted verbatim from `ci.yml`, and the `on:` block quoted verbatim;
      - the throwaway-venv rehearsal result for `tests/test_pyusb_api_surface.py` and for the **full suite
        under pyusb-present** — the latter is C-8's measurement re-taken after this phase's changes, and
        Plan 127-12 records it as the grounding for D-02's accepted cost;
      - whether `origin/v1.23-py32f071-integration` exists and, if so, the SHA it points at. A read-only
        `git fetch` of that ref is permitted and is **not** a push.

    Then **print**, without executing, the two commands the operator will run, fully expanded with no
    placeholders:
      - the push of the app milestone branch to `origin` (`henols/firestarter_app`), with the
        set-upstream flag since the branch does not yet exist on the remote;
      - the `gh workflow run` dispatch of `ci.yml` on `v1.23-py32f071-integration` in
        `henols/firestarter_app`.
    Alongside them, print the read-only follow-up that yields the run id — a `gh run list` scoped to that
    workflow and branch — and state plainly that Task 3 will re-derive every fact independently regardless
    of what is reported back.

    **Print the safety finding beside the commands**, in one short paragraph: which workflows exist, what
    each triggers on, and the conclusion that this push fires nothing. The operator should be able to
    satisfy themselves of that without leaving the SUMMARY.

    Name the failure modes in advance so Task 3's failure branch is not improvised:
      - **the extra fails to resolve** — the `.[test,py32]` install step red. Most likely cause is the
        `>=1.3.1,<2` bound against the runner's index state; recovery is to record the resolver output
        verbatim and revisit D-19 in a new plan, never to loosen the bound reflexively;
      - **`import usb` succeeds but `usb.core.find` raises `NoBackendError`** — libusb absent on the
        runner. This is **not** a failure: the test accepts that branch explicitly by design (research
        assumption A4). If it appears, record which branch the log shows;
      - **pytest exits 5 (nothing collected)** — the `collect_ignore` misfired or the module was renamed.
        Recovery is Plan 127-06's three-place rename guard, which should already have caught it locally;
      - **the primary `ci` job goes red** — that is a phase regression, not a pyusb problem, and must be
        recorded as such with the failing step named.

    **This task ends here.** It runs neither command. The SUMMARY must state that explicitly.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && echo "porcelain=$(git status --porcelain | wc -l)" && sed -n '/^on:/,/^jobs:/p' .github/workflows/ci.yml && grep -n -A20 'ci-py32:' .github/workflows/ci.yml && python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1 && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/check_mypy_watermark.py && git ls-remote --heads origin v1.23-py32f071-integration; echo "lsremote_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The branch is `v1.23-py32f071-integration` and the full 40-character `HEAD` is recorded.
    - `git status --porcelain` is recorded with the five known entries named as pre-existing; nothing was cleaned.
    - Every commit this phase made is listed in order with its changed paths.
    - The full suite result, collected count and coverage percentage are recorded, measured in `/workspaces/firestarter_app` with the `firestarter` sibling present.
    - All eight primary-leg gate commands are run locally and each exit code is recorded as 0.
    - The `ci-py32` job's steps and `ci.yml`'s `on:` block are quoted verbatim.
    - The throwaway-venv rehearsal results are recorded for both the API-surface module and the **full suite under pyusb-present**, and the venv is deleted; the devcontainer is confirmed still pyusb-absent afterwards.
    - The `on:` blocks of all four workflows are re-derived first-hand and the conclusion that this push fires nothing is stated in one paragraph beside the commands.
    - Both outward-facing commands are printed fully expanded with no placeholders, and the read-only run-id follow-up is printed beside them.
    - All four named failure modes are recorded with their recovery paths, including the explicit statement that a `NoBackendError` branch is a designed-for outcome and not a failure.
    - **No task ran `git push` or `gh workflow run`**, and the SUMMARY states this explicitly. Any `git fetch` or `git ls-remote` performed is recorded as read-only.
    - No file was modified; `git rev-parse --abbrev-ref HEAD` is unchanged at the end of the task.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>The exact pre-push state is on the record, the two outward-facing commands are printed rather than executed, the push's safety is re-derived rather than inherited, and every anticipated failure carries a named recovery.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: OPERATOR GATE — push the app milestone branch and dispatch Host CI</name>
  <read_first>
    - This plan's Task 1 output in `127-11-SUMMARY.md` — the two printed commands, the recorded pre-push
      state, the workflow-trigger safety paragraph, and the four anticipated failure modes.
    - `/workspaces/firestarter_app/.github/workflows/ci.yml`'s `on:` block as Task 1 recorded it: `push`
      is scoped to `main` and the new `workflow_dispatch:` is what makes a manual run possible. Pushing
      `v1.23-py32f071-integration` triggers nothing on its own and **cannot** cut a beta prerelease or a
      PyPI upload. Satisfy yourself of that before running the push.
  </read_first>
  <action>
    **No agent action. This task exists to stop.** Task 1 printed the two outward-facing commands fully
    expanded; the operator runs them. No task in this plan is permitted to execute `git push` or
    `gh workflow run`, and that structural absence — not this checkpoint's type — is the gate, because
    `--auto` and `--chain` auto-approve human-verify checkpoints regardless of `autonomous: false`.

    Execution resumes at Task 3 on a **datum** — a run id or run URL — not on an acknowledgement, because
    Task 3's entire job is to query that run and re-derive every fact about it read-only.
  </action>
  <what-built>
    Phase 127's host changes are complete and every local gate is green: the `4ee64a1` merge with D-17's
    deviation record; the `[py32]` floor at `pyusb>=1.3.1,<2`; the independent DFU opcode anchors; the
    shared py32-only option refusal closing the `--usb-id`-accepted-on-stable gap plus both-directions
    channel gating; the flash-envelope tightened to the reserved application region with a fail-closed
    cross-repo linker-script gate; the `PyusbMissingError` pragma removed and covered, with the CLI proven
    to work under genuine pyusb absence; the `_finish()` hoist and the `DFU_UPLOAD` readback with its four
    `VerifyResult` outcomes; and the scoped install-doc update.

    One requirement is **CI-only and cannot be verified in this environment by anyone**: HOST-04's claim
    that a leg *distinct from the existing `.[test]`-only leg* exists and is **green in CI**. That needs a
    runner, a real dependency resolver and GitHub's scheduler. It also exercises HOST-07's raised floor
    against a resolver for the first time.
  </what-built>
  <how-to-verify>
    1. Read the two commands Task 1 printed in `127-11-SUMMARY.md`, and the safety paragraph beside them.
       Confirm for yourself that `ci.yml`'s and `release.yml`'s `push` arms are `main`-only,
       `beta-release.yml`'s is `beta`-only, and `publish.yml` triggers on a published release — so this
       push fires nothing.
    2. Run the printed `git push` of `v1.23-py32f071-integration` to `origin` in
       `/workspaces/firestarter_app`.
    3. Run the printed `gh workflow run` dispatch of `ci.yml` on that branch.
    4. Run the printed read-only follow-up to obtain the run id, and wait for the run to finish.
    5. Report back the **run id or run URL**. Nothing else is needed — every other fact will be
       re-derived. Do not summarise the outcome; the datum is enough.
  </how-to-verify>
  <resume-signal>Reply with the run id or run URL (e.g. `31234567890` or the full Actions URL). If you chose not to run it, say so and Task 3 will record HOST-04 as an unresolved gate rather than claiming it.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-derive every fact about the run read-only, and record it or record its absence</name>
  <files>(no files modified — read-only capture into this plan's SUMMARY)</files>
  <read_first>
    - This plan's Task 1 record — the pre-push `HEAD`, the `ci-py32` step names in order, and the four
      anticipated failure modes with their recoveries.
    - `.planning/phases/124-firmware-integration-merge/124-11-SUMMARY.md` — the re-derivation discipline
      this task copies: every claimed field queried independently rather than accepted on report.
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` — the step names, so each can be named
      precisely when its conclusion is recorded.
  </read_first>
  <action>
    Query the run **read-only** and re-derive every field. Accept nothing on report.

    Using `gh run view` against `henols/firestarter_app`, record:
      - the run id and the full run URL;
      - the workflow name, the `event` (must be `workflow_dispatch`), the `headBranch` (must be
        `v1.23-py32f071-integration`), the `headSha`, the `status` and the `conclusion`;
      - the **per-job** conclusions for **both** `ci` and `ci-py32`. The primary `ci` job's conclusion is
        as important as the new one: it proves this phase did not break the gate guarding everything else;
      - the **per-step** conclusions inside `ci-py32`, each named: checkout, setup-python, the
        `.[test,py32]` install, the pyusb-import-and-version step, and the pytest step. A workflow can
        conclude success with a step skipped, so an overall green is not sufficient;
      - the **resolved pyusb version** printed by the import step, lifted from the log;
      - the **collected/passed count** the pytest step reports for `tests/test_pyusb_api_surface.py`, and
        whether it was non-zero — pytest exits 5 on nothing collected, so a green step with zero tests is
        impossible, but the number belongs on the record anyway;
      - which branch the `usb.core.find` test took on the runner — enumerated, or `NoBackendError` — since
        research assumption A4 predicted either is acceptable and only a real run resolves it.

    Then compare `headSha` **string-equal** against a freshly re-derived local `HEAD` in
    `/workspaces/firestarter_app`, recording `git rev-parse --abbrev-ref HEAD` immediately before and
    after the comparison. Also verify the **pushed ref's content**: after a read-only `git fetch`, read
    `.github/workflows/ci.yml` from `origin/v1.23-py32f071-integration` and confirm the `ci-py32` job and
    the `workflow_dispatch:` trigger are present on the ref that was actually built — the run builds what
    was pushed, which is not automatically what is on disk.

    **If the run failed**, do not retry blindly. Record the failing job and step by name with a verbatim
    log excerpt, match it against Task 1's four anticipated failure modes, and carry it as an unresolved
    gate to Plan 127-12 with the named recovery. In particular: a `NoBackendError` branch is **not** a
    failure, and a resolver failure must not be answered by loosening `>=1.3.1,<2` inside this plan.

    **If the checkpoint was skipped**, record that plainly: HOST-04's CI half is an **unresolved gate**,
    Plan 127-12 must carry it as such, and no local rehearsal may be substituted for it.

    Close with the claim ceiling in one paragraph: a green `ci-py32` leg proves the extra installs, pyusb
    imports and its API is shaped as the tests expect on a runner. It proves nothing about a PY32F071 —
    no PCB exists and no DFU device is present in CI.

    **No file is modified by this task**, and no outward-facing command is run: `gh run view`,
    `gh run list` and `git fetch` are read-only.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && git fetch origin 2>&1 | tail -2; git show origin/v1.23-py32f071-integration:.github/workflows/ci.yml 2>/dev/null | grep -c 'ci-py32:'; echo "porcelain=$(git status --porcelain | wc -l)"; git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - The run id and full run URL are recorded, together with the workflow name, `event` = `workflow_dispatch`, `headBranch` = `v1.23-py32f071-integration`, `headSha`, `status` and `conclusion` — each queried read-only, none accepted on report.
    - **Both** jobs' conclusions are recorded by name: `ci` and `ci-py32`.
    - Every `ci-py32` step is recorded by name with its individual conclusion; any skipped step is recorded with a statement of whether the skip is expected.
    - The resolved `pyusb` version is lifted from the run log and recorded.
    - The pytest step's collected/passed count for `tests/test_pyusb_api_surface.py` is recorded and is non-zero.
    - Which branch the `usb.core.find` test took on the runner — enumerated or `NoBackendError` — is recorded, resolving research assumption A4 for the first time.
    - `headSha` is compared **string-equal** against a freshly re-derived local `HEAD`, with the branch recorded immediately before and after the comparison.
    - The pushed ref is verified: after a read-only `git fetch`, `origin/v1.23-py32f071-integration`'s `ci.yml` is confirmed to contain the `ci-py32` job and the `workflow_dispatch:` trigger.
    - A failure is recorded with the failing job and step named and a verbatim log excerpt, matched against Task 1's four anticipated modes, and carried to Plan 127-12 as an unresolved gate — with no blind retry and no loosening of the `pyusb` bound inside this plan.
    - A skipped checkpoint is recorded plainly as an unresolved gate, with an explicit statement that no local rehearsal substitutes for it.
    - The claim-ceiling paragraph is present.
    - No file was modified; no task ran `git push` or `gh workflow run`, and the SUMMARY states this explicitly.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>HOST-04's CI half is either on the record as a re-derived run URL with per-step conclusions and a string-equal head SHA, or on the record as an explicitly unresolved gate — never as an inherited claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a relayed claim → an accepted fact | No agent message is evidence; every field is re-derived read-only |
| the local tree → the pushed ref | The run builds what was pushed, which is not automatically what is on disk |
| an overall `success` → per-step success | A workflow can conclude success with a step skipped |
| an autonomous flag → an outward-facing action | `--auto` and `--chain` auto-approve human-verify checkpoints regardless of `autonomous: false` |
| a branch push → a release | Pushing `beta` in this repo cuts a prerelease; this branch must fire nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-11-01 | **Elevation of privilege** | an agent performing an outward-facing push or dispatch | **high** | mitigate | **Structural gate:** no task in this plan is permitted to run `git push` or `gh workflow run`; the plan prints them and stops (the 124-11 / 125-05 / 126-11 shape). The checkpoint is the human-facing form, but the structural separation is the control — an auto-approval cannot cause a push |
| T-127-11-02 | **Repudiation** | accepting a relayed run result as evidence | **high** | mitigate | Every field re-derived read-only in-session via `gh run view`; `headSha` compared string-equal against a freshly re-derived local `HEAD`; the pushed ref's `ci.yml` read from `origin` and checked for the job and the trigger |
| T-127-11-03 | Repudiation | an overall green conclusion hiding a skipped step | **high** | mitigate | Both jobs and every `ci-py32` step recorded by name with individual conclusions; any skipped step recorded with a statement of whether the skip is expected |
| T-127-11-04 | Repudiation | a green job that collected zero tests | high | mitigate | The pytest step's collected count is recorded and asserted non-zero; the explicit-path invocation exits 5 on nothing collected, so this is belt and braces |
| T-127-11-05 | Spoofing | a head SHA compared against a stale or switched branch | medium | mitigate | `git rev-parse --abbrev-ref HEAD` recorded immediately before and after the comparison; `gsd-tools query commit` has been observed moving HEAD live in this project |
| T-127-11-06 | **Elevation of privilege** | a push cutting an unintended beta or PyPI upload | **high** | mitigate | All four workflows' `on:` blocks re-derived first-hand in Task 1 and printed beside the commands; `beta` push, tags and releases prohibited outright. Phase 130 owns the release decision |
| T-127-11-07 | Denial of service | a blind retry masking a real resolver failure | medium | mitigate | Four failure modes named in advance with recoveries; a failure is recorded with its log excerpt and carried as an unresolved gate. Loosening the `>=1.3.1,<2` bound inside this plan is prohibited |
| T-127-11-08 | Repudiation | substituting a local venv run for CI evidence | **high** | mitigate | Prohibited. Criterion 2 claims the leg exists and is green **in CI**; only a run URL discharges it, and a skipped checkpoint is recorded as unresolved |
| T-127-11-09 | Repudiation | a green run read as a claim about silicon | high | mitigate | A one-paragraph ceiling statement is required: the extra installs and pyusb's API is as expected on a runner; no PCB exists and no DFU device is present in CI |
| T-127-11-SC | Tampering | npm/pip/cargo installs | high | mitigate | No task installs anything locally. The `.[test,py32]` install happens **on the runner**, and its resolved `pyusb` version is lifted from the log into the record. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit and is a pre-existing dependency of the merged branch; no `[ASSUMED]`/`[SUS]` package exists, so no blocking legitimacy checkpoint is owed |
</threat_model>

<verification>
1. The pre-push state is recorded: branch, `HEAD`, porcelain with its five pre-existing entries, this phase's commits with changed paths, the suite and coverage figures, and all eight primary-leg gates.
2. `ci.yml`'s `on:` block and the `ci-py32` steps are quoted verbatim, and all four workflows' triggers are re-derived with the conclusion that this push fires nothing.
3. Both outward-facing commands are printed fully expanded and **neither was executed by any task**.
4. All four anticipated failure modes are recorded with recovery paths, including that a `NoBackendError` branch is a designed-for outcome.
5. The run's id, URL, head SHA, branch, event, status and conclusion are re-derived read-only in-session.
6. Both jobs' conclusions and every `ci-py32` step's conclusion are recorded by name.
7. The resolved `pyusb` version and the pytest step's non-zero collected count are lifted from the log.
8. `headSha` is string-equal to a freshly re-derived local `HEAD`, with the branch recorded before and after.
9. The pushed ref's `ci.yml` is confirmed to carry the `ci-py32` job and the `workflow_dispatch:` trigger.
10. A failure — or a skipped checkpoint — is recorded as an unresolved gate carried to Plan 127-12, with no local substitute; the ceiling paragraph is present; no file was modified and no requirement was ticked.
</verification>

<success_criteria>
- The phase's one CI-only requirement is observed by the only instrument that can observe it — a workflow run — with both jobs and every `ci-py32` step independently recorded.
- Every fact about that run is re-derived read-only rather than accepted on report, and the pushed ref's content is verified against what was built.
- The structural push gate held: no task ran `git push` or `gh workflow run`, attested in the SUMMARY.
- What a green run proves, and what it does not, are stated in the same breath.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-11-SUMMARY.md` when done
</output>