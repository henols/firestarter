---
phase: 127-host-dfu-installer
plan: 12
type: execute
wave: 8
depends_on: ["127-11"]
files_modified:
  - .planning/phases/127-host-dfu-installer/127-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03, HOST-04, HOST-05, HOST-06, HOST-07, HOST-08]

must_haves:
  truths:
    - "**Every row is re-executed in this session against the live trees**, not accepted from a prior plan's SUMMARY. A figure quoted from a SUMMARY is a claim; a figure produced by a command run now is evidence"
    - "**This is the only plan permitted to tick `HOST-01…HOST-08`** in `.planning/REQUIREMENTS.md` — the Phase-116 premature-tick guard, where executors marked multi-plan requirements Complete four times in one phase. Every other plan in this phase was told explicitly not to"
    - "Each tick cites the plan that **discharges** the requirement, not merely a plan that mentions it. Plans 127-05 and 127-10 list `HOST-03` in their `requirements` for traceability and **explicitly do not discharge it**; 127-08 and 127-09 do. The ledger must not confuse the two"
    - "The verification run happens in `/workspaces/firestarter_app` **in the sibling layout** — a directory literally named `firestarter_app` with `firestarter` beside it. A wrongly-named working directory produces **6 spurious failures** in `test_sdp_bus_config_drift.py` and `test_gen_validation_header.py`, which hardcode the name"
    - "D-04's *exact collected-test count* is discharged as a **recorded** number with the verbatim `pytest --collect-only` trailer and run summary. **No assertion pins the integer** — Phase 123 D-10 rejected a pinned count for measured flakiness and `test_skip_census.py::test_no_pinned_skip_count` enforces that rejection. This is a **deliberate, reasoned exception** to the standing operator preference for an exit code over a human reading output, and the artifact must say so rather than let it look like a lapse"
    - "The measured baselines this artifact records: **1158** collected pre-merge (re-measured 2026-08-01 in the real checkout, **1158 passed / 0 skipped / 0 failed** — research's *1213 passed / 3 skipped* was a scratch-worktree artifact and does not apply here); **1216** post-merge; and the phase's final total, re-measured at evidence time"
    - "**C-8's measurement is recorded, not merely asserted:** the full suite run under **pyusb-present** returned the same outcome as under pyusb-absent, which converts D-02's *accepted cost* from an assertion into a measurement. Plan 127-11 Task 1 re-took it after this phase's changes; that figure is what the artifact carries"
    - "**C-1's zero-fixup measurement is recorded with its reproduction command**, so a reader can re-run it rather than trust it"
    - "**The mock-only ceiling on HOST-03 is produced in a form Phase 130's CLOSE-02 can cite verbatim.** CLOSE-02's honesty ledger names it explicitly. If this artifact does not carry it as a quotable non-claim, Phase 130 has nothing to cite"
    - "**Research assumption A1 is carried forward honestly**: the UM1504 and USB DFU 1.1 opcode values anchored by HOST-06 are external documents research did not fetch. Whatever Plan 127-03 Task 1 established about them goes in the artifact as a residual, not as a resolved item"
  prohibitions:
    - statement: "No requirement is ticked against a claim inherited from a SUMMARY rather than a command re-run in this session"
      status: required
      verification: "Each of the eight rows names the command, its exit code and its output, produced now"
    - statement: "No `git push`, `gh workflow run`, tag, release or public comment"
      status: required
      verification: "Plan 127-11 carried the only outward-facing action and it was the operator's. Phase 130 owns the release decision, and pushing `beta` cuts a prerelease"
    - statement: "No claim that the install works, that firmware runs on a PY32F071, or that any DFU behaviour is bench, hardware or silicon validated"
      status: required
      verification: "The permitted ceiling is: the target builds, the suites pass, the DFU sequence is exercised against descriptors and mocks. HOST-03's ceiling is literally *asserted against a mock*"
    - statement: "No source file in `firestarter_app` or `firestarter` is modified by this plan"
      status: required
      verification: "`files_modified` names only meta-repo planning artifacts. Both sub-repos' `git status --porcelain` must be unchanged apart from the app repo's five known pre-existing lines"
    - statement: "`asset_candidates()` is confirmed unchanged rather than assumed unchanged"
      status: required
      verification: "Phase 128's Criterion 4 asserts against `asset_candidates(\"py32f071\")[0]`. The sweep must call it and record the returned list"
    - statement: "If Plan 127-11's checkpoint was skipped or its run failed, HOST-04 is recorded as an **unresolved gate** and is NOT ticked"
      status: required
      verification: "Criterion 2 claims a leg green **in CI**. A local rehearsal is not a substitute, and a partial tick is worse than an open one"
  artifacts:
    - "`.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`"
    - "`.planning/REQUIREMENTS.md` with HOST-01…HOST-08 resolved"
  key_links:
    - "Phase 128 depends on this phase for the `asset_candidates()` filename contract. The sweep must record `asset_candidates(\"py32f071\")` explicitly so Phase 128 can cite a measured value rather than an assumption."
    - "Phase 129 will move the flash map when `BOOTLOADER` gets a non-zero length. The artifact must name `tests/test_py32_flash_map_host.py` as the gate that will go red when it does, and point at `127-CONTEXT.md` `<deferred>`."
    - "Phase 130's CLOSE-02 honesty ledger cites *the mock-only ceiling on HOST-03*. Section 7 of this artifact is where that non-claim must be quotable."
---

<objective>
Re-execute every gate and every figure this phase claims, write `127-NONREGRESSION.md` as the phase's
evidence artifact, and resolve `HOST-01…HOST-08` in `.planning/REQUIREMENTS.md` — each against a row
produced in this session.

Purpose: eleven plans produced claims. A closing sweep exists because a claim in a SUMMARY is a claim,
and because the four-times-in-one-phase premature-tick failure this project has already had is prevented
structurally: exactly one plan may tick, and only against rows it re-ran itself.

Output: the evidence artifact, eight resolved requirement rows, and the phase's non-claims written in a
form Phase 130 can cite verbatim.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/127-host-dfu-installer/127-CONTEXT.md
@.planning/phases/127-host-dfu-installer/127-RESEARCH.md
@.planning/phases/127-host-dfu-installer/127-VALIDATION.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md

**Repo.** The artifact and the requirement ticks are **meta-repo** files (`/workspaces`, branch
`gsd/v1.23-py32f071-integration`). Every measurement is taken in `/workspaces/firestarter_app` on
`v1.23-py32f071-integration`, in the sibling layout. `/workspaces/firestarter` is read-only throughout.

**The self-reference trap, and how to avoid it.** Phase 125's six plan SUMMARYs each trip the
claim-ceiling checker when scanned directly, because they quote the forbidden phrases inside their own
compliance paragraphs — `125-RESEARCH.md` C-16 documents exactly this. `125-NONREGRESSION.md` avoids it
correctly. Write the ceiling section by **reference** to `.planning/REQUIREMENTS.md` §"Validation
Ceiling" and by stating what is *not* claimed in your own words; do **not** reproduce the checker's
forbidden-phrase list.

**Requirement ticking scope: THIS PLAN ONLY.** No other plan in Phase 127 was permitted to tick
HOST-01…HOST-08.

**Claim ceiling.** The permitted ceiling for this whole milestone: the target builds clean, the suites
pass, the DFU sequence is exercised against device descriptors and mocks. Nothing here says the install
works or that firmware runs on a PY32F071. No PCB exists.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every gate and every figure in the sibling layout</name>
  <files>(no files modified — read-only sweep captured into this plan's SUMMARY)</files>
  <read_first>
    - Every `.planning/phases/127-host-dfu-installer/127-*-SUMMARY.md` from Plans 127-01 through 127-11 —
      the commits, the files touched, the figures claimed, and any finding or unresolved gate carried
      forward. Read them to know **what to re-run**, not to inherit their numbers.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md`
      — its §3 gate table's column shape (command / expected / observed) and its per-repo sectioning.
    - `.planning/ROADMAP.md` §"Phase 127" — the five success criteria verbatim; each needs its own
      re-executed evidence row.
    - `.planning/REQUIREMENTS.md` HOST-01…HOST-08 prose and §"Validation Ceiling" — read the requirement
      text itself, not a plan's paraphrase of it.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — its
      argument shape and its fail-closed-on-empty-target-list behaviour, so the claim gate is invoked with
      an explicit target rather than an empty list.
  </read_first>
  <action>
    Re-run everything this phase claims, in `/workspaces/firestarter_app`, and record each row as
    command / expected / observed.

    **Layout precondition, first.** Confirm the working directory basename is literally `firestarter_app`
    and `../firestarter/.git` exists, and confirm no `/dev/ttyACM*` or `/dev/ttyUSB*` device is attached.
    Record both. A wrongly-named directory yields 6 spurious failures; an attached board turns
    `test_no_programmer_found_read` / `_erase` red for reasons unrelated to this phase.

    **Host repo rows:**
      - `git log -1 --format=%P` on the phase's merge commit contains
        `4ee64a14a8933b60896c8b168bb1c7e34d788fa4` — HOST-01's structural claim, re-derived;
      - the full suite, with the **verbatim** `pytest --collect-only` trailer and the verbatim run summary
        line. Record the collected count, passed, skipped and failed;
      - coverage against the 70 floor, with the total percentage and `py32_dfu.py`'s own figure;
      - all eight primary-leg gate commands from `ci.yml`, each with its exit code: both catalog-validity
        checks, both codegen-drift gates with their `git diff --exit-code`, `ruff check`,
        `ruff format --check`, the mypy watermark, and the `firestarter --help` smoke test;
      - each new test module run individually with its per-module count:
        `test_py32_packaging.py`, `test_dfu_opcode_anchors.py`, `test_py32_channel_gating.py`,
        `test_py32_flash_map_host.py`, `test_pyusb_gating.py`, `test_py32_pyusb_absent.py`, and
        `test_py32_dfu.py`;
      - `tests/test_skip_census.py` green, and `ALLOWED_SKIP_REASONS` confirmed still at **four** entries —
        the phase added none;
      - `tests/test_pyusb_api_surface.py` confirmed **not collected** in the pyusb-absent environment, and
        `importlib.util.find_spec("usb")` confirmed still `None` in the devcontainer;
      - `asset_candidates("py32f071")` called and its returned list recorded — Phase 128's contract,
        confirmed rather than assumed;
      - the four flash-map constants read from the module and compared against the linker script read live
        from `/workspaces/firestarter`, with both sides quoted;
      - `grep -c 'pragma: no cover' firestarter/py32_dfu.py` recorded as **2**, and
        `grep -c 'self._finish(' firestarter/py32_dfu.py` recorded as **1**;
      - `grep -c 'no such option' firestarter/cli_handlers.py` recorded as **1** — D-08's one-code-path
        property;
      - the pyusb-**present** full-suite result from Plan 127-11 Task 1's throwaway venv, re-taken here if
        it was not taken after the last commit. This is **C-8**, and it is what grounds D-02's accepted
        cost.

    **Firmware repo row:** `git -C /workspaces/firestarter status --porcelain` line count and the branch
    name — this phase wrote nothing there, and that must be shown rather than asserted.

    **Meta repo rows:** the meta branch; and the claim gate run with an **explicit target**, since it
    fails closed on an empty target list.

    **CI row:** the run URL, run id, head SHA, event, both job conclusions and the `ci-py32` per-step
    conclusions **as Plan 127-11 Task 3 re-derived them**. Re-query the run read-only to confirm the
    values still hold. If Plan 127-11's checkpoint was skipped or its run failed, record HOST-04's CI half
    as an **unresolved gate** here and carry it into Task 3 unticked.

    Do not write the artifact yet — this task produces the measurements Task 2 formats.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(basename "$PWD")" = firestarter_app && test -e ../firestarter/.git && ls /dev/ttyACM* /dev/ttyUSB* 2>&1 | head -2; python3 -m pytest tests/ --collect-only -q -o addopts= 2>&1 | tail -1; python3 -m pytest tests/ -q --no-cov -o addopts= 2>&1 | tail -1; python3 -m pytest tests/ --cov=firestarter --cov-fail-under=70 -o addopts= 2>&1 | grep -E 'TOTAL|passed|failed' | tail -2; ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/check_mypy_watermark.py; python3 -c "from firestarter.firmware import asset_candidates; print(asset_candidates('py32f071'))"; echo "pragma=$(grep -c 'pragma: no cover' firestarter/py32_dfu.py) finish=$(grep -c 'self._finish(' firestarter/py32_dfu.py) refusal=$(grep -c 'no such option' firestarter/cli_handlers.py)"; git -C /workspaces/firestarter status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The layout precondition is recorded: basename `firestarter_app`, `../firestarter/.git` present, no serial device attached.
    - `git log -1 --format=%P` on the merge commit is re-run and its output contains `4ee64a14a8933b60896c8b168bb1c7e34d788fa4`.
    - The verbatim `pytest --collect-only` trailer and the verbatim run summary are captured; collected, passed, skipped and failed are each recorded.
    - Coverage passes the 70 floor; the total and `py32_dfu.py`'s figure are recorded.
    - All eight primary-leg gate commands are re-run with exit codes recorded; both codegen-drift `git diff --exit-code` gates are clean.
    - Each of the seven named test modules is run individually with its count recorded.
    - `tests/test_skip_census.py` passes and `ALLOWED_SKIP_REASONS` is confirmed at four entries.
    - `tests/test_pyusb_api_surface.py` is confirmed not collected, and `find_spec("usb")` in the devcontainer is confirmed `None`.
    - `asset_candidates("py32f071")` is called and its returned list recorded as `["firestarter_py32f071.hex", "firestarter_py32f071.bin"]`.
    - The four host flash-map constants and the linker script's `MEMORY` lines are both quoted and compared.
    - `grep -c 'pragma: no cover' firestarter/py32_dfu.py` is 2; `grep -c 'self._finish('` is 1; `grep -c 'no such option' firestarter/cli_handlers.py` is 1.
    - The pyusb-present full-suite result (C-8) is recorded, taken after the phase's last commit.
    - `git -C /workspaces/firestarter status --porcelain` is recorded as unchanged, with the branch named.
    - The claim gate is run with an explicit target and its exit code recorded.
    - The CI run's fields are re-queried read-only and confirmed, or HOST-04's CI half is recorded as an unresolved gate.
    - No file was modified by this task and no outward-facing command was run.
  </acceptance_criteria>
  <done>Every figure this phase will claim exists as a command, an expected value and an observed value produced in this session — with nothing inherited from a SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Write 127-NONREGRESSION.md</name>
  <files>.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md`
      in full — the section structure to reuse: the claim as precise statements; the baseline as recorded
      and as re-verified; the gate table per repo; one subsection per success criterion quoting the
      ROADMAP verbatim; decision coverage for every D-01…D-19; informational findings carried forward;
      the claim ceiling; and the sweep summary. Phase 127 also has nineteen decisions, so 126's shape is
      the nearest neighbour.
    - This plan's Task 1 record — every measured row.
    - `.planning/ROADMAP.md` §"Phase 127" — the five criteria, to quote verbatim.
    - `.planning/phases/127-host-dfu-installer/127-CONTEXT.md` — all nineteen decisions **and** their
      inline `CORRECTED by` notes, so the decision-coverage section records what was actually implemented
      rather than what the superseded wording said.
    - `.planning/phases/127-host-dfu-installer/127-RESEARCH.md` §Corrections and §Assumptions Log — C-1
      through C-8 and A1 through A5, each of which needs a disposition in the artifact.
  </read_first>
  <action>
    Write `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md` using Phase 126's section
    structure.

    **1. The claim, as precise statements.** What this phase does and does not assert, in short
    declarative sentences.

    **2. The baseline, as recorded and as re-verified.** The pre-merge **1158 collected / 1158 passed /
    0 skipped / 0 failed** figure with the date and the checkout it was taken in; the post-merge **1216**;
    and the phase's final total from Task 1, with the verbatim `--collect-only` trailer and run summary.
    State plainly that **no assertion pins any of these integers** (D-04), that this is a deliberate
    exception to the standing preference for an exit code over a human reading output, and why:
    Phase 123 D-10 rejected a pinned count for measured flakiness and `test_skip_census.py` enforces that
    rejection. Also state that research's *1213 passed / 3 skipped* was a scratch-worktree artifact — the
    two meta-repo skips fire only when `.planning/` is unreachable — so **0 skipped is correct here**.

    **3. The gate table.** Command / expected / observed, sectioned by repo: host repo (gates, modules,
    grep counts, constants), firmware repo (untouched), meta repo (claim gate), and the CI row (the
    operator-dispatched run, its URL, both job conclusions and the `ci-py32` per-step conclusions).

    **4. Success criteria.** One subsection per ROADMAP criterion, quoting it **verbatim**, then the rows
    that discharge it and the plan that produced each. Criterion 1 needs the merge-parent check, the
    `--usb-id` refusal and the sibling-layout suite result. Criterion 2 needs the CI run. Criterion 3
    needs the pragma count, the in-process coverage and the subprocess CLI proofs. Criterion 4 needs the
    four `VerifyResult` outcomes and the opcode anchors. Criterion 5 needs the floor and the both-ways
    gating with the import-time assertion.

    **5. Decision coverage — all nineteen, D-01…D-19.** For each: what was implemented, by which plan, and
    — where a `CORRECTED by` note applies — what the superseded wording said and what was done instead.
    D-06, D-12, D-16 and D-18 all carry corrections; D-02 and D-03 carry refinements. Record D-13 and
    D-14 as the deliberate in-scope addition with **no HOST id**.

    **6. Informational findings carried forward.** At minimum: **C-1**'s zero-fixup merge measurement with
    its reproduction command; **C-8**'s pyusb-present full-suite result grounding D-02's accepted cost;
    **A1** — the UM1504 / DFU 1.1 opcode values were **not fetched** by research, so HOST-06's anchors are
    currently consistent-with-the-module rather than independently sourced, with whatever Plan 127-03
    Task 1 established; **A4**'s resolution — which branch the runner's `usb.core.find` actually took;
    **A5** converted from inferred to measured by Plan 127-08's before/after `device.calls` comparison;
    **D-17**'s accepted deviation and the `avrdude-mcu-detection-fallback` todo reviewed and not folded;
    the **sibling-layout** requirement and its 6-failure trap; the four pre-existing `tools/` ruff
    diagnostics recorded as non-findings; and the Phase-129 tripwire in
    `tests/test_py32_flash_map_host.py` that will go red when `BOOTLOADER` gets a length.

    **7. Claim ceiling.** State what is **not** claimed in your own words and by reference to
    `.planning/REQUIREMENTS.md` §"Validation Ceiling". **Do not reproduce the checker's forbidden-phrase
    list** — Phase 125's SUMMARYs tripped the gate by doing exactly that. This section must contain the
    **mock-only ceiling on HOST-03** as a short, self-contained, quotable paragraph, because Phase 130's
    CLOSE-02 honesty ledger cites it by name. Also record the two adjacent non-claims: no evidence exists
    that any DFU tool has ever driven a PY32, so the DfuSe-versus-plain fork is untested against reality
    and one of its two branches has never been the right one; and *success* on this path currently means
    the transfer completed and — where verification was possible — the readback matched, nothing more.

    **Sweep summary.** Pass / passed-with-findings / fail, with the count of rows re-executed.

    Then run the claim gate against the finished artifact with an explicit target and record its exit
    code. If it trips, fix the artifact's wording rather than the gate.
  </action>
  <verify>
    <automated>cd /workspaces && A=.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md; test -f "$A"; grep -c '^## ' "$A"; grep -c 'HOST-0' "$A"; for d in 01 05 09 13 17 19; do printf "D-%s:%s " "$d" "$(grep -c "D-$d" "$A")"; done; echo; python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py "$A"; echo "claimgate=$?"; git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md` exists and carries all eight top-level sections in Phase 126's order.
    - §2 records the pre-merge 1158/1158/0/0 figure with its date and checkout, the post-merge 1216, and the phase's final total with the verbatim `--collect-only` trailer and run summary — and states that no assertion pins any of them, why that is a deliberate exception, and why 0 skipped rather than 3 is correct here.
    - §3's gate table has command / expected / observed columns and is sectioned into host repo, firmware repo, meta repo and the CI row.
    - §4 has one subsection per ROADMAP criterion, each quoting the criterion **verbatim** and naming the rows and plans that discharge it.
    - §5 covers **all nineteen** decisions D-01…D-19; every decision carrying a `CORRECTED by` note records both the superseded wording and what was implemented instead; D-13 and D-14 are recorded as the deliberate in-scope addition with no HOST id.
    - §6 carries at minimum: C-1 with its reproduction command, C-8's pyusb-present result, A1's unresolved spec-sourcing residual, A4's resolution from the runner, A5 converted to measured, D-17's deviation and the unfolded todo, the sibling-layout trap, the four `tools/` ruff non-findings, and the Phase-129 tripwire.
    - §7 contains a short, self-contained, **quotable** paragraph stating the mock-only ceiling on HOST-03, plus the DfuSe-versus-plain non-claim and the meaning-of-success non-claim — written in the author's own words and by reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling", **without** reproducing the checker's forbidden-phrase list.
    - The claim gate is run against the finished artifact with an explicit target and exits 0; if it tripped, the artifact's wording was fixed rather than the gate.
    - No source file in either sub-repo was modified.
  </acceptance_criteria>
  <done>The phase's evidence exists as one artifact a reader can re-run, every decision including the four corrected ones is accounted for, and the non-claims Phase 130 must cite are written where 130 can quote them.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve HOST-01…HOST-08 in REQUIREMENTS.md, each against a re-executed row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — HOST-01…HOST-08's checkbox lines verbatim, the summary table row
      `HOST-01 … HOST-08 | Phase 127 | Pending`, and §"Validation Ceiling".
    - This plan's Task 1 record and the finished `127-NONREGRESSION.md` — the rows each tick will cite.
    - `.planning/phases/127-host-dfu-installer/127-05-PLAN.md` and `127-10-PLAN.md` frontmatter — both list
      `HOST-03` for traceability and both state explicitly that they **do not discharge** it. Do not cite
      either as HOST-03 evidence.
    - `.planning/STATE.md` — the current position block, so the SUMMARY can describe what changes without
      guessing. Note that `current_phase`, `current_phase_name` and `last_activity_desc` are hand-edited
      fields; if STATE.md is updated at all, diff rather than infer.
  </read_first>
  <action>
    Tick each of `HOST-01` … `HOST-08` in `.planning/REQUIREMENTS.md`, and update the summary-table row
    from `Pending` to its resolved state.

    For **each** requirement, record in this plan's SUMMARY: the requirement's own prose (read from
    `REQUIREMENTS.md`, not from a plan's paraphrase), the **command re-run in this session** that
    discharges it, its observed output, and the plan that produced the work:
      - **HOST-01** — the merge-parent check plus the D-17 record scan; Plans 127-01 and 127-02.
      - **HOST-02** — the simulated-stable `--usb-id` refusal at exit 2 with the exact message, plus the
        one-code-path count; Plan 127-04.
      - **HOST-03** — the four `VerifyResult` outcomes, the ordering assertion and the mismatch hard
        failure; Plans 127-08 and 127-09. **Do not cite 127-05 or 127-10** — they carry `HOST-03` for
        traceability only and say so in their own objectives.
      - **HOST-04** — the operator-dispatched CI run URL with both job conclusions and the `ci-py32`
        per-step conclusions; Plans 127-06 and 127-11. **If Plan 127-11's checkpoint was skipped or its
        run failed, leave HOST-04 unticked** and record it as an unresolved gate in both the artifact and
        the summary table. A partial tick is worse than an open one.
      - **HOST-05** — the pragma count of 2, the in-process coverage of the de-pragma'd lines, and the
        subprocess CLI exit-0 proofs; Plan 127-07.
      - **HOST-06** — `tests/test_dfu_opcode_anchors.py` green, with A1's residual named beside it so the
        tick is not read as stronger than it is; Plans 127-03 and 127-09.
      - **HOST-07** — the `pyusb>=1.3.1,<2` textual gate plus the resolved version lifted from the CI log;
        Plans 127-02 and 127-11.
      - **HOST-08** — both simulated channels' board-choice lists, the `--help` visibility both ways, and
        the import-time-by-construction test; Plan 127-04.

    Use a scoped edit — replace only the eight checkbox lines and the one summary-table row. Do **not**
    rewrite the file.

    Then state the phase's disposition in the SUMMARY — passed, or passed-with-findings naming each
    finding — and list what is carried forward, explicitly including: A1's spec-sourcing residual; the
    Phase-129 re-check of the flash-map constant; the `avrdude-mcu-detection-fallback` todo, unfolded;
    Phase 128's dependency on the recorded `asset_candidates("py32f071")` value; and the mock-only ceiling
    Phase 130's CLOSE-02 will cite.

    Do not push, do not tag, do not touch `beta`.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n 'HOST-0' .planning/REQUIREMENTS.md | head -20; git -C /workspaces/firestarter_app status --porcelain | wc -l; git -C /workspaces/firestarter status --porcelain | wc -l; git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Each of HOST-01…HOST-08 is either ticked in `.planning/REQUIREMENTS.md` or explicitly left unticked with a recorded reason; no requirement is ticked against a claim inherited from a SUMMARY.
    - The summary-table row for HOST-01…HOST-08 is updated from `Pending` to its resolved state.
    - For each requirement the SUMMARY records: the requirement's own prose, the command re-run **in this session**, its observed output, and the producing plan.
    - HOST-03's evidence cites Plans 127-08 and 127-09 only; Plans 127-05 and 127-10 are **not** cited as HOST-03 evidence.
    - HOST-04 is ticked only if Plan 127-11's run URL exists and both jobs concluded successfully; otherwise it is left unticked and recorded as an unresolved gate in both the artifact and the summary table.
    - HOST-06's tick is accompanied by A1's residual, so the tick is not read as stronger than it is.
    - The edit is scoped: `git diff .planning/REQUIREMENTS.md` touches only the eight checkbox lines and the one summary-table row.
    - `git -C /workspaces/firestarter_app status --porcelain` shows only the five known pre-existing lines; `git -C /workspaces/firestarter status --porcelain` is unchanged.
    - The phase's disposition is stated, and the carried-forward list names A1, the Phase-129 flash-map re-check, the unfolded `avrdude-mcu-detection-fallback` todo, Phase 128's dependency on the recorded `asset_candidates("py32f071")` value, and the mock-only ceiling for Phase 130's CLOSE-02.
    - No push, no tag, no `beta` touch, no release, no public comment.
  </acceptance_criteria>
  <done>All eight requirement IDs are resolved against rows produced in this session, with any unresolved gate left visibly open rather than partially ticked, and everything the next three phases need is named where they will look for it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a SUMMARY's claim → an accepted fact | Eleven plans produced claims; only re-execution makes them evidence |
| a requirement mentioned → a requirement discharged | Two plans list HOST-03 for traceability and discharge nothing |
| a green sweep → a claim about silicon | The ceiling must be written where the next phase will quote it |
| an artifact's own prose → a claim-gate violation | Phase 125's SUMMARYs tripped the gate by quoting its forbidden list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-12-01 | **Repudiation** | ticking a requirement against an inherited claim | **high** | mitigate | Every row re-executed in this session with command, expected and observed recorded; ticks cite the re-run, not a SUMMARY |
| T-127-12-02 | **Repudiation** | ticking HOST-03 against a plan that does not discharge it | **high** | mitigate | Plans 127-05 and 127-10 declare in their own objectives that they do not discharge HOST-03; the tick cites 127-08 and 127-09 only |
| T-127-12-03 | Repudiation | ticking HOST-04 without the CI run | **high** | mitigate | HOST-04 is ticked only against a run URL with both job conclusions; a skipped checkpoint leaves it open and recorded as an unresolved gate |
| T-127-12-04 | Repudiation | a sweep run in a wrongly-named directory | high | mitigate | The layout precondition is asserted before any figure is recorded; a wrong basename produces 6 spurious failures |
| T-127-12-05 | Repudiation | a pinned collected count that goes red on every later addition | medium | accept | Deliberate: D-04 records the count without asserting it, and `test_skip_census.py::test_no_pinned_skip_count` enforces the rejection of a pinned count. The artifact states this is a reasoned exception to the exit-code preference, not a lapse |
| T-127-12-06 | Repudiation | the artifact tripping the claim gate on its own compliance prose | medium | mitigate | The ceiling section is written by reference to `REQUIREMENTS.md` §"Validation Ceiling" and in the author's own words; the checker's forbidden-phrase list is not reproduced. The gate is run against the finished artifact with an explicit target |
| T-127-12-07 | **Repudiation** | a mock-only result read as a silicon claim by a later phase | **high** | mitigate | §7 carries the HOST-03 ceiling as a short quotable paragraph plus the DfuSe-fork and meaning-of-success non-claims, because Phase 130's CLOSE-02 cites them by name |
| T-127-12-08 | Elevation of privilege | a push, tag or `beta` touch at phase close | **high** | mitigate | Prohibited outright. Pushing `beta` cuts a prerelease; Phase 130 owns the release decision |
| T-127-12-09 | Tampering | a whole-file rewrite of `REQUIREMENTS.md` | medium | mitigate | Scoped edit only — eight checkbox lines and one table row; the diff is asserted to touch nothing else |
| T-127-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install is performed. `pyusb` 1.3.1 carries an **OK** verdict in `127-RESEARCH.md` §Package Legitimacy Audit and is a pre-existing dependency; no `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is owed. The runner's resolved version is recorded in §3's CI row |
</threat_model>

<verification>
1. The layout precondition and the absence of serial devices are recorded before any figure.
2. Every host-repo, firmware-repo, meta-repo and CI row is re-executed in this session with command, expected and observed.
3. `asset_candidates("py32f071")` is called and recorded — Phase 128's contract confirmed, not assumed.
4. The three grep counts (pragma 2, `self._finish(` 1, refusal 1) are recorded.
5. `ALLOWED_SKIP_REASONS` is confirmed at four entries and the gated module is confirmed not collected.
6. `127-NONREGRESSION.md` carries all eight sections, all nineteen decisions with their corrections, and the six informational findings.
7. §4 quotes each ROADMAP criterion verbatim and names the rows and plans discharging it.
8. §7 carries the quotable mock-only ceiling for Phase 130 and does not reproduce the checker's phrase list; the claim gate exits 0 against the artifact with an explicit target.
9. All eight requirement IDs are resolved, each against a re-run command, with HOST-03 cited to 127-08/127-09 and HOST-04 gated on the run URL.
10. Both sub-repos are unmodified; no push, tag, release or `beta` touch occurred.
</verification>

<success_criteria>
- Phase 127 closes on evidence produced in the closing session, not on eleven plans' word for it.
- Exactly one plan ticked requirements, each against a named re-executed row, with any unresolved gate left visibly open.
- The phase's non-claims — above all the mock-only ceiling on HOST-03 — exist as quotable prose the milestone's honesty ledger can cite without paraphrase.
- Phases 128, 129 and 130 each find the fact they depend on written down where they will look for it.
</success_criteria>

## Artifacts this phase produces

Symbols, files and jobs **created by Phase 127**. Newly-created names are not drift.

**`firestarter/py32_dfu.py`** — `APP_REGION_SIZE`, `APP_REGION_END`, `CONFIG_REGION_SIZE`,
`_DFU_BIT_CAN_UPLOAD`, `class VerifyResult(enum.Enum)` with members `VERIFIED`, `SKIPPED_NO_UPLOAD`,
`SKIPPED_PLAIN_DFU`, `MISMATCH`; `Py32DfuFlasher.verify_result`, `Py32DfuFlasher.verify_reason`,
`Py32DfuFlasher._read_back()`, `Py32DfuFlasher._verify_readback()`.

**`firestarter/cli_handlers.py`** — `_reject_py32_only_option(name, given)`.

**`firestarter/firmware.py`** — the D-17 accepted-deviation comment at `flash_method()`; verify-aware
logging inside `_install_with_dfu` (no new public symbol).

**`tests/`** — `test_dfu_opcode_anchors.py`, `test_py32_flash_map_host.py`, `test_py32_pyusb_absent.py`,
`test_py32_channel_gating.py`, `test_pyusb_api_surface.py`, `test_pyusb_gating.py`,
`test_py32_packaging.py`; `collect_ignore` in `tests/conftest.py`; `_FakeUsbDevice.upload_image`,
`_FakeUsbDevice.upload_block_size`, `_FakeUsbDevice.uploads()` and the `attributes=` parameter of
`_interface()` in `tests/test_py32_dfu.py`.

**`.github/workflows/ci.yml`** — the `workflow_dispatch:` trigger and the `ci-py32` job.

**`pyproject.toml`** — the `[py32]` extra raised to `pyusb>=1.3.1,<2`.

**`.planning/`** — `.planning/phases/127-host-dfu-installer/127-NONREGRESSION.md`.

<output>
Create `.planning/phases/127-host-dfu-installer/127-12-SUMMARY.md` when done
</output>