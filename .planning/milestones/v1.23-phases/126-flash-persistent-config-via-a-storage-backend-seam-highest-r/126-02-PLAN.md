---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/tests/test_config_storage_eeprom_regression.py
autonomous: true
requirements: [CFG-04]

must_haves:
  truths:
    - "D-04: the CFG-04 regression test is authored against the **pre-refactor** `src/rurp_config_utils.cpp` (blob `6705fd46e07a2d359d161dc2e7728cb4e45f89c7`), proven green, and its own blob SHA recorded — so it observed the behaviour it claims to match before the split existed"
    - "CFG-04: the test asserts the `EEPROM.get`/`put` access is at offset **48** with a length equal to `sizeof(rurp_configuration_t)`, recorded symbolically — never as an integer literal, because host `long` is 8 bytes and the same struct measures 32 / 15 / 20 bytes on host / AVR / ARM (C-6) and `g++ -m32` is unavailable here"
    - "D-01: the harness is a pytest module under `firestarter/tests/` driving host `g++` by subprocess, in Phase 125's shape — not a new PIO `test/native/` suite (which would move the pinned 141/17 counts inside the one phase whose premise is that nothing else moved) and not a new `scripts/check_*.py`"
    - "C-12: the fake `EEPROM.h` is **hand-written fresh into `tmp_path` on every call**, never a committed fixture header and never a dependency on `.pio/libdeps/native/ArduinoFake/src/EEPROM.h` — a gitignored build artifact that would make the test pass warm and fail on a clean checkout"
    - "The fake records every `(idx, sizeof(T))` pair, and the test asserts on that recording — which is simultaneously the CFG-04 assertion and the non-vacuity hook (at least one call recorded)"
    - "C-12: the fake mirrors the real Arduino template signatures at `framework-arduino-avr/libraries/EEPROM/src/EEPROM.h:130-142`, including `put`'s per-byte `update()` semantics, so the moved code's typed call keeps emitting an identical access"
    - "The Discretion default is proven to survive contact: the test names BOTH `src/rurp_config_utils.cpp` and `src/boards/rurp_config_storage_eeprom.cpp` from the start, filters the list to existing files at collection time, and asserts non-vacuity two ways — at least one path resolved AND at least one fake call recorded"
    - "Each of the four assertions the module makes is proven able to FAIL, by mutating a copy in `tmp_path` (wrong offset, wrong length, no call, missing include) — never by editing the committed source"
  prohibitions:
    - statement: "The test module never calls a pytest skip nor a conditional-skip marker"
      status: required
      verification: "The analog's self-enforcing leg (`tests/test_vpp_seam_manual_on_every_board.py:447-468`) is copied verbatim including its concatenation trick, and passes. A skip on a missing compiler is the fail-open shape A-7 measured — the compiler resolver must fail-closed with an assertion instead"
    - statement: "`src/rurp_config_utils.cpp` is NOT modified by this plan"
      status: required
      verification: "`git hash-object src/rurp_config_utils.cpp` still returns `6705fd46e07a2d359d161dc2e7728cb4e45f89c7` at the end of this plan; the file does not appear in this plan's commit"
    - statement: "`include/rurp_shield.h` is NOT modified (D-09) and `include/rurp_types.h` is NOT modified (CFG-07)"
      status: required
      verification: "`git hash-object` returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46` and `d3fe5203a91527bdb7b20a33843c81065e21c613` respectively"
    - statement: "No test in this module asserts a literal record size, a literal field offset other than the EEPROM address 48, or a literal `EEPROM.get` length"
      status: required
      verification: "Length assertions are expressed against `sizeof(rurp_configuration_t)` evaluated by the compiled binary; the only integer literal in a size or length position is absent. The offset 48 is an EEPROM address, not a size, and is asserted as a literal deliberately (CFG-04 names it)"
    - statement: "No new `scripts/check_*.py` is created"
      status: required
      verification: "`ls scripts/check_*.py | wc -l` is still 5; `tests/test_checker_convention.py` is unmodified"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 126-12 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` unchanged"
  artifacts:
    - firestarter/tests/test_config_storage_eeprom_regression.py
  key_links:
    - "This plan's ENTIRE value is its ordering. A test written after the refactor can only prove the new code is self-consistent — it never observed the behaviour it claims to match. The recorded blob SHA of this test file is the artefact Plan 126-03 re-hashes; if it is not recorded here, D-04's proof is unsatisfiable and Criterion 3 cannot be retro-fitted."
    - "The test must compile a target whose path is stable across the refactor, or the empty-diff proof breaks. Naming both paths and filtering to existing files at collection time is legitimate **because D-08 locks the post-refactor path in advance**. If it cannot be made to work, the documented fallback is ONE named, justified line change in Plan 126-03 with both blob SHAs recorded — never a silent edit, and never a skip."
    - "`git diff --stat` on the test file is CORROBORATION ONLY. `124-VERIFICATION.md` records a live finding where exactly that pipeline reported \"(empty)\" while a `1 file changed, 25 insertions(+)` trailer survived the grep. The blob-SHA re-hash is the primary proof."
---

<objective>
Author CFG-04's AVR regression test against the untouched, pre-refactor
`src/rurp_config_utils.cpp`, prove it green, prove each of its assertions able to fail, and record
its blob SHA — the artefact Plan 126-03's split is judged against.

Purpose: ROADMAP Criterion 3 asks for behaviour "identical to the pre-refactor code — proven by an
empty `git diff` on the test file itself". D-04 discharges that as **two commits plus a blob-SHA
re-hash**: the test is written first against the original file and proven green here; the split
lands in a later commit; the proof is that the recorded SHA re-hashes identical and the test is
still green. This plan is the first half, and it is the half that cannot be done later.

Output: `firestarter/tests/test_config_storage_eeprom_regression.py`, its recorded blob SHA, and the
observed `(offset, length)` access pair the pre-refactor code emits.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-VALIDATION.md

**Read order and precedence.** `126-CONTEXT.md` records the locked decisions; `126-RESEARCH.md`
files 14 corrections and **wins on verified facts**. The three relevant here are **C-6** (three
different record sizes across three compilers; `-m32` unavailable — never assert a literal size),
**C-12** (ArduinoFake's `EEPROM.h` exists only under the gitignored `.pio/libdeps/`, so the fake
must be hand-written; positively confirmed by pattern-mapping that no hand-written compile-shim
header exists anywhere under `tests/` to copy), and **Open Question 5** (whether the test can name
both TU paths from the start — recommendation: filter to existing files at collection time, assert
non-vacuity two ways, never skip).

**Repo layout.** Every path in this plan lives in the firmware submodule `/workspaces/firestarter`,
branch `v1.23-py32f071-integration`. Commits land inside that submodule; only the SUMMARY commits in
the meta repo. `firestarter_app` is out of scope (D-12). **Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`** (RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check
`git rev-parse --abbrev-ref HEAD` in both repos (RESEARCH Pitfall 7).

**Requirement ticking scope for this plan: NONE.** CFG-04 spans this plan (the test authored
pre-refactor), Plan 126-03 (the split and the re-hash proof) and Plan 126-04 (the AVR flash/RAM
measurement). Do not tick CFG-01…CFG-07. Plan 126-12 is the only plan permitted to tick any of them.

**Claim ceiling.** No PY32F071 PCB exists. This plan makes no ARM claim at all — it is entirely
AVR-side behaviour compiled on the host. State the ceiling by reference to `.planning/REQUIREMENTS.md`
§"Validation Ceiling"; do not reproduce its forbidden-phrase list.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the pre-refactor CFG-04 regression test with a hand-written fake EEPROM.h</name>
  <files>firestarter/tests/test_config_storage_eeprom_regression.py</files>
  <read_first>
    - `firestarter/src/rurp_config_utils.cpp` — **read all 40 lines.** This is the file under test in
      its pre-refactor state. The two lines that will cross the seam are `:24` (`EEPROM.get(CONFIG_START, *config)`)
      and `:29` (`EEPROM.put(CONFIG_START, *config)`); `#define CONFIG_START 48` is at `:11`; the
      write-back at `:38`. Nothing else in the file is EEPROM-aware.
    - `firestarter/tests/test_vpp_seam_manual_on_every_board.py` — read the module docstring
      (1–76), the self-contained path block (78–93), `_resolve_compiler` (136–155, **copy verbatim**
      — it fails closed with an assertion, never a skip), `_compile` (179–191, list argv, never a
      shell, `-std=gnu++17 -Wall -Wextra -I include`), `_write_shim_tu` (158–176, the
      write-fresh-into-`tmp_path` idiom the fake `EEPROM.h` must follow), `_run_binary` (194–201,
      **and its docstring explaining why the result crosses on stdout and never as the exit code**),
      the zero-warning assertion (250–254) and the no-skip leg (447–468).
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §`tests/test_config_storage_*.py` and §"No Analog Found" row 3 — the positively-confirmed fact
      that there is no existing fake header to copy, and that `_write_shim_tu`'s pattern is what the
      fake should follow.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §C-12 (the real Arduino template signatures the fake must mirror, and `put`'s per-byte
      `update()` semantics), §C-6 (the three record sizes; what this forbids and requires), §C-11
      (`rurp_shield.h:11-12` opens `extern "C"`, so the config functions already have C linkage and
      `rurp_shield.h:17` already supplies `<string.h>`).
    - `firestarter/include/rurp_shield.h` — read the `CONFIG_VERSION`, `VALUE_R1`, `VALUE_R2` lines
      (46, 49, 50) and the four config declarations (61, 150–152). The test compiles against this
      header; do not modify it.
    - `firestarter/include/rurp_types.h` — all 26 lines. It includes only `<stdint.h>` and compiles
      standalone under host `g++` (verified), which is what makes this harness possible.
  </read_first>
  <behavior>
    Observable behaviour this module must assert, all of it about the **pre-refactor** code:
    - Compiling `src/rurp_config_utils.cpp` with host `g++`, a fresh fake `EEPROM.h` on the include
      path, and a fresh driver `main` in `tmp_path`, links and runs cleanly with zero bytes of
      `-Wall -Wextra` warning output.
    - Calling `rurp_load_config()` produces exactly one recorded `get` access, at index **48**, with
      a length equal to `sizeof(rurp_configuration_t)` as the compiled binary reports it.
    - Calling `rurp_save_config(cfg)` produces exactly one recorded `put` access, at index **48**,
      with the same length.
    - `rurp_validate_config()` on a struct whose `version` does not match `CONFIG_VERSION` writes the
      defaults back and triggers a `put` — the D-14 write-back at `:38`, which must behave identically
      after the split.
    - The number of recorded accesses is greater than zero (non-vacuity), and the number of resolved
      source paths is greater than zero (the second non-vacuity leg).
    Each of the four assertions must be shown to FAIL on a mutated copy in `tmp_path`.
  </behavior>
  <action>
    Create `firestarter/tests/test_config_storage_eeprom_regression.py`. It lives in `tests/`
    (PIO-invisible) — never under `test/`, which is globbed into builds. It is a `tests/test_*.py`
    module, not a `scripts/check_*.py`: those floors cost four artifacts plus two bumps and this
    costs zero.

    Open with the house module-docstring convention: MIT header, `Phase 126 Plan 02 -- <what>`,
    `Requirements: CFG-04`, `Decisions covered: D-01, D-04, D-06, D-07, D-08`, a rationale block
    stating that this module is authored against the pre-refactor file **on purpose** and that its
    blob SHA is recorded so Plan 126-03 can re-hash it, an explicit statement that it **executes in
    NO CI leg on this branch** (`pytest tests/` runs only in `build.yml` on `main` and
    `beta-build.yml` on `beta`; `py32f071.yml` has no pytest step), the "self-contained path
    resolution, NOT in conftest.py" note, and a numbered `Coverage:` list.

    Imports: stdlib plus `pytest` only. **No path under `.pio/` may appear anywhere in this module.**
    ArduinoFake does ship an `EEPROM.h`, but only under `.pio/libdeps/<env>/`, which is a gitignored
    artifact that exists solely after a native build — depending on it makes the test pass on a warm
    tree and fail on a clean checkout.

    Resolve paths self-contained at module level: `_HERE`, `_REPO_ROOT`, `_INCLUDE = _REPO_ROOT / "include"`,
    and a **tuple of candidate source paths** naming both `src/rurp_config_utils.cpp` and
    `src/boards/rurp_config_storage_eeprom.cpp`. Filter that tuple to files that exist **at
    collection time** and compile whichever resolved. Today only the first exists; after Plan 126-03
    both do. This is legitimate because D-08 locks the post-refactor path in advance, and it is what
    makes the file's blob SHA stable across the refactor — which is the whole point.

    Copy `_resolve_compiler` verbatim from the analog. It resolves `$CXX` falling back to `g++` and
    **asserts** rather than skipping; a skip on a missing compiler is the fail-open shape A-7
    measured five gate legs flipping through.

    Write a `_write_fake_eeprom_header(tmp_path)` helper that writes a minimal fake `EEPROM.h`
    **fresh into `tmp_path` on every call** — never a committed fixture header. The fake must:
      - mirror the real Arduino template signatures from
        `framework-arduino-avr/libraries/EEPROM/src/EEPROM.h:130-142` — a `get` template returning
        `T&` and a `put` template returning `const T&`, both taking an `int idx`;
      - record, for each call, the pair `(idx, sizeof(T))` and whether it was a get or a put, into a
        file-scope vector the driver `main` can print;
      - reproduce `put`'s per-byte `update()` semantics (read the byte, write only if it differs) so
        that the moved code's typed call keeps emitting an identical access — a byte-loop
        reimplementation in the backend would be arguably equivalent but would not be a *move*;
      - expose a global `EEPROM` object of the fake class, matching the real library's usage shape;
      - provide backing storage large enough to cover index 48 plus `sizeof(rurp_configuration_t)`.

    Write a `_write_driver_tu(tmp_path)` helper that writes a fresh driver `main` into `tmp_path`
    which: constructs a `rurp_configuration_t`; calls `rurp_load_config()`; calls
    `rurp_save_config(...)`; calls `rurp_validate_config(...)` once with a deliberately mismatched
    `version` so the write-back fires; then prints one machine-parsable line per recorded access in a
    fixed form carrying the operation, the index and the length, plus one summary line carrying
    `sizeof(rurp_configuration_t)` and the total access count. **The result crosses on stdout, never
    as the process exit code** — 1 is also the exit code of a compile failure, a link failure and a
    crash, so returning a result through it would make the correct answer indistinguishable from
    every failure mode. Return 0 from `main` unconditionally.

    Write a `_compile(compiler, sources, include_dirs, output_path)` helper in the analog's shape:
    list argv only, never a shell; always `-std=gnu++17 -Wall -Wextra`; `-I` for the repo `include`
    directory **and** for `tmp_path` so the fake `EEPROM.h` shadows nothing real; then the sources
    and `-o`.

    Write the test functions:
      - one that compiles and runs the pre-refactor TU and asserts `compile_result.stderr == ""` —
        zero bytes of warning output under `-Wall -Wextra`, not merely exit 0 (the analog's standard);
      - one that parses stdout and asserts the `get` access is at index `48` with a length equal to
        the `sizeof` the same binary reported. **Compare against the binary's own reported `sizeof`,
        never against a Python integer literal** — host `long` is 8 bytes so the value is 32 here, 15
        under `avr-g++` and 20 on ARM (C-6);
      - one that asserts the `put` access is at index `48` with the same length;
      - one that asserts the validate write-back produced a `put` when the version did not match;
      - one non-vacuity function asserting both that at least one candidate source path resolved and
        that at least one access was recorded — the two-way check the Discretion default requires;
      - one that records, as an assertion on the module's own text, that no path under `.pio/`
        appears in this file;
      - the analog's self-enforcing no-skip leg, copied verbatim including its concatenation trick.

    Assertion messages carry full context in the analog's style: the argv, the observed stdout and
    the observed stderr, so a failure is diagnosable from the pytest output alone.

    Run `python3 -m pytest tests/test_config_storage_eeprom_regression.py -v`. Every function must
    pass. Record the observed `sizeof` the binary printed (expect 32 on this host), the observed
    access list, and the function count. Then run `python3 -m pytest tests/ -q` and record the new
    total.

    Commit the single new file with a message of the form
    `test(126-02): assert the pre-refactor EEPROM access at offset 48 with sizeof(rurp_configuration_t)`.
    Then re-check the branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_config_storage_eeprom_regression.py -v 2>&1 | tail -25 && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "cfgutils=$(git hash-object src/rurp_config_utils.cpp)" && echo "shield=$(git hash-object include/rurp_shield.h)" && echo "types=$(git hash-object include/rurp_types.h)" && echo "checkers=$(ls scripts/check_*.py | wc -l)" && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/tests/test_config_storage_eeprom_regression.py` exists under `tests/`, not `test/`.
    - The module docstring carries the MIT header, the `Phase 126 Plan 02` attribution, `Requirements: CFG-04`, a `Decisions covered:` line naming D-01, D-04, D-06, D-07 and D-08, the explicit no-CI-leg statement, the self-contained-path note, and a numbered `Coverage:` list with one entry per function.
    - The module imports only stdlib and `pytest`, and contains no reference to any path under `.pio/`. A dedicated test function asserts that property against the module's own text and passes.
    - The candidate-source tuple names both `src/rurp_config_utils.cpp` and `src/boards/rurp_config_storage_eeprom.cpp`, and is filtered to existing files at collection time. Today the resolved count is 1; the module records it.
    - `_resolve_compiler` is the analog's verbatim fail-closed form: it asserts on a missing compiler and never skips.
    - The fake `EEPROM.h` is written fresh into `tmp_path` by a helper on every call. No `.h` file is added under `tests/` by this plan (`find tests -name '*.h'` still returns the same four pre-existing files).
    - The fake exposes `get` and `put` templates matching the real library's signature shape, records `(operation, idx, sizeof(T))` per call, and implements `put` with per-byte update semantics.
    - The driver `main` prints the recorded accesses and `sizeof(rurp_configuration_t)` on **stdout** and returns 0 unconditionally; no test infers a result from the process exit code.
    - `_compile` uses list argv with `-std=gnu++17 -Wall -Wextra` and `-I` for both the repo `include` directory and `tmp_path`.
    - A test asserts `compile_result.stderr == ""` — zero bytes of warning output, not merely a zero exit code.
    - The `get` access is asserted at index `48` with a length compared against the value the compiled binary itself reported for `sizeof(rurp_configuration_t)`. No Python integer literal appears in any length or size comparison.
    - The `put` access is asserted at index `48` with the same length.
    - A test asserts the validate write-back produced a `put` when the struct's `version` did not match `CONFIG_VERSION`.
    - The non-vacuity function asserts both that at least one candidate path resolved and that at least one access was recorded, and passes.
    - The self-enforcing no-skip leg is present, uses the concatenation trick, and passes.
    - `python3 -m pytest tests/test_config_storage_eeprom_regression.py -v` reports every function passed; the observed `sizeof` value (expected 32 on this host) and the observed access list are recorded.
    - `python3 -m pytest tests/ -q` reports the Plan 126-01 total plus this module's function count, and both numbers are recorded.
    - `git hash-object src/rurp_config_utils.cpp` still returns `6705fd46e07a2d359d161dc2e7728cb4e45f89c7`; `include/rurp_shield.h` still returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`; `include/rurp_types.h` still returns `d3fe5203a91527bdb7b20a33843c81065e21c613`.
    - `ls scripts/check_*.py | wc -l` is still `5`.
    - `git show --stat HEAD` lists exactly one path, and `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>CFG-04's regression test exists, was authored against the untouched pre-refactor file, is green, and asserts the (48, `sizeof(rurp_configuration_t)`) access pair symbolically with a hand-written fake that records at least one call.</done>
</task>

<task type="auto">
  <name>Task 2: Prove each assertion able to fail, then record the D-04 evidence ledger</name>
  <files>(no files modified — mutation demonstrations run entirely in tmp_path; evidence recorded into this plan's SUMMARY)</files>
  <read_first>
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §"Pattern 3: Prove the move before making it" (lines 838–857) — the exact two-commit recipe,
      the `git hash-object` recording step, and the explicit note that the path-scoped
      `git diff --stat` is **corroboration only**.
    - `.planning/phases/124-firmware-integration-merge/124-VERIFICATION.md` — the live finding where a
      `git diff --stat | grep -v` pipeline was reported as "(empty)" while a
      `1 file changed, 25 insertions(+)` trailer survived the grep because the trailer does not
      contain the grepped filename. Read this before writing any untouched-ness statement.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` §C-15 — blob-SHA re-hash primary,
      `git status --porcelain` scoped by repo name as corroboration, **never** a path-scoped
      `git diff` alone.
    - `firestarter/tests/test_pinmap_guard_fires.py` — the in-tree precedent for a
      must-fail/planted-violation leg, if a shape reference is wanted for the mutation harness.
  </read_first>
  <action>
    A gate that passes without observing anything is the failure mode this project has unwound twice
    (Phases 118 and 124). This task proves the module from Task 1 is not one, then records the D-04
    evidence Plan 126-03 will re-check.

    **Mutation demonstrations — four, all in `tmp_path`, never touching the committed source.** For
    each, copy `src/rurp_config_utils.cpp` into a scratch directory outside the repo (or `tmp_path`
    via a one-off script), apply the single mutation, point the same compile-and-run helpers at the
    mutated copy, and record the observed failure — the assertion that fired and its message:
      1. **Wrong offset.** Change the `CONFIG_START` value from 48 to any other value. The offset
         assertions must fail, and the recorded failure message must name both the expected 48 and
         the observed value.
      2. **Wrong length.** Replace the typed `EEPROM.get(CONFIG_START, *config)` call with a call
         whose template argument is a narrower type (for instance a `uint8_t` at the same index), so
         the recorded `sizeof(T)` differs. The length assertion must fail.
      3. **No access at all.** Delete the `EEPROM.get` call. The get assertion and the non-vacuity
         assertion must both fail — and the recorded output must show that the non-vacuity leg is
         what catches a silently-absent access, which is the load-bearing assertion here (the
         absent-chip false-green lesson: an exit-code-only test would have reported green).
      4. **Missing fake header.** Compile without `-I tmp_path` so the fake `EEPROM.h` is
         unreachable. The compile must fail and the test must report a compile failure with the
         compiler's stderr — never a skip, and never a pass.
    Record all four as a table: mutation, expected outcome, observed outcome, the assertion that
    fired. Then re-run the unmutated module and confirm it is green again, and confirm
    `git hash-object src/rurp_config_utils.cpp` is unchanged — proving no mutation leaked into the
    repository.

    **The D-04 evidence ledger.** Record, as observed values:
      - `git hash-object tests/test_config_storage_eeprom_regression.py` — the recorded SHA. **This
        is the number Plan 126-03 re-hashes.** Print it in full, 40 characters, and state
        explicitly that Plan 126-03 must compare against exactly this string.
      - `git hash-object src/rurp_config_utils.cpp` — `6705fd46e07a2d359d161dc2e7728cb4e45f89c7`,
        the pre-refactor anchor.
      - the commit SHA from Task 1, with its single changed path.
      - the observed `(operation, index, length)` access list and the observed
        `sizeof(rurp_configuration_t)`.
      - the pre-refactor pytest result for this module: function names and pass count.
    State the D-04 proof protocol Plan 126-03 must execute, so it is written down in one place: the
    recorded test-file SHA **re-hashes identical** after the split, and the test is **still green**
    against the post-refactor tree. A path-scoped `git diff --stat` is corroboration only and its
    output must be quoted in full rather than summarised as "(empty)".

    Also record the documented fallback, so nobody invents one under pressure: if the both-paths
    approach cannot survive the refactor, the fallback is **one named, justified line change** to
    this test file in Plan 126-03, with **both** blob SHAs recorded and the justification stated —
    never a silent edit, and never a `pytest.skip`.

    Do not modify any file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && echo "recorded-test-sha=$(git hash-object tests/test_config_storage_eeprom_regression.py)" && echo "prerefactor-anchor=$(git hash-object src/rurp_config_utils.cpp)" && python3 -m pytest tests/test_config_storage_eeprom_regression.py -q 2>&1 | tail -2 && echo "fw-porcelain=$(git status --porcelain | wc -l)" && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - All four mutation demonstrations are recorded as a table with mutation, expected outcome, observed outcome and the assertion that fired. Each demonstration produced a genuine test failure (or, for the missing-header case, a genuine compile failure surfaced as a failure) — not a skip and not a pass.
    - The no-access mutation's row explicitly identifies the non-vacuity assertion as the one that catches a silently-absent access.
    - Every mutation ran on a copy outside the repository or in `tmp_path`. `git hash-object src/rurp_config_utils.cpp` returns `6705fd46e07a2d359d161dc2e7728cb4e45f89c7` after the demonstrations, and `git status --porcelain` in `/workspaces/firestarter` is 0 lines.
    - The unmutated module is re-run after the demonstrations and reported green with its full function list.
    - The recorded blob SHA of `tests/test_config_storage_eeprom_regression.py` is written out in full 40 characters, with an explicit instruction that Plan 126-03 compares against exactly that string.
    - The pre-refactor anchor SHA of `src/rurp_config_utils.cpp` is recorded as `6705fd46e07a2d359d161dc2e7728cb4e45f89c7`.
    - The Task 1 commit SHA and its single changed path are recorded.
    - The observed access list and the observed `sizeof(rurp_configuration_t)` are recorded as values, not as descriptions.
    - The D-04 proof protocol Plan 126-03 must execute is written out: recorded SHA re-hashes identical, test still green, `git diff --stat` quoted in full as corroboration only with `124-VERIFICATION.md`'s trailer finding cited.
    - The documented fallback is recorded: one named, justified line change with both blob SHAs — never a silent edit, never a skip.
    - No file was modified by this task.
    - `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
  </acceptance_criteria>
  <done>Every assertion in the CFG-04 regression test is demonstrated able to fail on a mutated copy, no mutation reached the repository, and the recorded blob SHA plus the exact proof protocol Plan 126-03 must run are written down in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-refactor behaviour → post-refactor behaviour | The only crossing CFG-04 exists to police; the test is the sole witness and it must have observed the *before* state |
| a gitignored build artifact → a committed test's dependency graph | `.pio/libdeps/native/ArduinoFake/src/EEPROM.h` exists on a warm tree only; depending on it is a fail-open dependency (C-12) |
| host compiler width → the on-storage record layout | Host `long` is 8 bytes; the target's is 4. A literal size assertion is wrong on two of three compilers (C-6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-02-01 | Repudiation | a regression test written after the refactor | high | mitigate | D-04's ordering: authored here against blob `6705fd46…`, proven green, SHA recorded. Plan 126-03 re-hashes it. A post-refactor test could only prove the new code self-consistent |
| T-126-02-02 | Denial of service | a test that passes with no access recorded | high | mitigate | The two-way non-vacuity leg (≥1 path resolved AND ≥1 fake call recorded), plus mutation demonstration 3 which proves the leg is what catches an absent access. Exit-code-only tests lie in this project's history |
| T-126-02-03 | Tampering | a fake header dependency that resolves only on a warm tree | high | mitigate | The fake is hand-written fresh into `tmp_path` on every call and the module asserts against its own text that no `.pio/` path appears (C-12) |
| T-126-02-04 | Tampering | a hardcoded record size silently masking a layout change | medium | mitigate | Length assertions compare against the compiled binary's own reported `sizeof`; no integer literal in any size position (C-6). Only the EEPROM address 48 is a literal, and CFG-04 names it |
| T-126-02-05 | Repudiation | a skip standing in for a proof | high | mitigate | `_resolve_compiler` asserts rather than skips; the self-enforcing leg proves the module contains no skip call or conditional-skip marker. A-7 measured five gate legs flipping PASS→SKIP at exit 0 with a false reason |
| T-126-02-06 | Tampering | a mutation leaking into the repository | medium | mitigate | Every mutation runs on a copy outside the repo or in `tmp_path`; the pre-refactor blob SHA is re-hashed and the firmware porcelain re-counted after the demonstrations |
| T-126-02-07 | Repudiation | an "untouched" claim resting on a path-scoped diff | medium | mitigate | Blob-SHA re-hash is primary; `git diff --stat` output is quoted in full, citing `124-VERIFICATION.md`'s surviving-trailer finding |

**Package legitimacy:** not applicable. No `npm`, `pip`, `cargo` or PlatformIO `lib_deps` entry is
added; `pytest` 9.1.1 and `g++` 14.2.0 are already present. No `T-126-SC` row is owed by this plan.
</threat_model>

<verification>
1. `tests/test_config_storage_eeprom_regression.py` exists, is stdlib+pytest only, references no `.pio/` path, and resolves its paths self-contained with no `conftest.py`.
2. Every named function passes; `compile_result.stderr` is zero bytes under `-Wall -Wextra`.
3. The get and put accesses are asserted at index 48 with a length compared against the binary's own reported `sizeof(rurp_configuration_t)`; no integer literal appears in any size or length comparison.
4. The validate write-back is asserted to produce a put on a version mismatch.
5. The two-way non-vacuity leg passes, and the self-enforcing no-skip leg passes.
6. All four mutation demonstrations produced genuine failures, and no mutation reached the repository (pre-refactor blob SHA unchanged, porcelain 0 lines).
7. The recorded test-file blob SHA is written out in full, with the instruction that Plan 126-03 compares against exactly that string, and the D-04 proof protocol plus its documented fallback are recorded.
8. `src/rurp_config_utils.cpp`, `include/rurp_shield.h` and `include/rurp_types.h` are all still at their pre-phase blob SHAs.
9. `ls scripts/check_*.py | wc -l` is still 5; both repos are still on their milestone branches.
10. No requirement checkbox was ticked.
</verification>

<success_criteria>
- CFG-04's regression test observed the pre-refactor behaviour: it asserts `EEPROM.get`/`put` at offset 48 with `sizeof(rurp_configuration_t)` against the untouched `src/rurp_config_utils.cpp`, and it is green.
- The test's blob SHA is recorded, making D-04's two-commit proof executable by Plan 126-03 and Criterion 3 satisfiable rather than retro-fitted.
- The test is not hollow: four mutation demonstrations show each assertion able to fail, and the non-vacuity leg is shown to be what catches a silently-absent access.
- Nothing depends on `.pio/`, nothing skips, and nothing asserts a literal record size.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{cpp,h}` | new files (HAL-free core) | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07) and
the `# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in
`firestarter/platform/py32f071/CMakeLists.txt` (Plan 126-03, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-02-SUMMARY.md`
when done. Record: the new module's full function list with pass results; the observed
`(operation, index, length)` access list and the observed host `sizeof(rurp_configuration_t)`; the
zero-byte warning observation; the resolved candidate-path count (1 today); the four mutation
demonstrations as a table with the assertion that fired in each; the confirmation that no mutation
reached the repository (pre-refactor blob SHA re-hashed, porcelain 0 lines); **the recorded 40-char
blob SHA of `tests/test_config_storage_eeprom_regression.py`, called out as the value Plan 126-03
must compare against**; the pre-refactor anchor SHA of `src/rurp_config_utils.cpp`; the D-04 proof
protocol and its documented fallback; the `pytest tests/` total before and after; the commit SHA with
its single changed path; and the branch re-check result in both repos.
</output>
