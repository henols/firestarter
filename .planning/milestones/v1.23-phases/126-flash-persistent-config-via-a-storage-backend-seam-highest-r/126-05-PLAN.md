---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 05
type: execute
wave: 3
depends_on: ["126-03"]
files_modified:
  - firestarter/tests/test_config_storage_seam_shape.py
autonomous: true
requirements: [CFG-03]

must_haves:
  truths:
    - "CFG-03 / D-06: a committed gate asserts `include/rurp_config_storage.h` declares exactly **two** functions, both returning `bool`, with the D-06 signatures — and that it declares no enum, no capability macro and no third function"
    - "C-11: the gate asserts the header wraps its declarations in an `#ifdef __cplusplus` / `extern \"C\"` pair and that its `<stdbool.h>` and `<stddef.h>` includes appear **before** that wrapper opens — a mangled-vs-unmangled mismatch would surface only as an ARM link failure, which this environment cannot build"
    - "D-09: the gate asserts `include/rurp_shield.h` does **not** include the seam header, and that the seam header is not reachable from `rurp_shield.h` by any transitive include path in the tree — the measured consequence of one such line is `pio test -e native` going from 141 cases / 141 succeeded to 17 suites / 0 succeeded, because that header reaches 46 TUs including 14 native `host_stubs.cpp` files"
    - "D-09: the includer census is expressed so it is stable across Plan 126-08 and never vacuous — every translation unit including the seam header is in the sanctioned set of three, every sanctioned path that **exists in the tree** does include it, and the census count is at least 2"
    - "D-07: the gate asserts the four public config declarations are still in `include/rurp_shield.h` (not moved into the seam header), and that `CONFIG_VERSION` there still reads the literal `VER06`"
    - "D-07: the gate asserts `CONFIG_START` is defined in the AVR backend translation unit and is absent from `src/rurp_config_utils.cpp` — it is an EEPROM address and belongs below the seam"
    - "D-08: the gate asserts `src/rurp_config_utils.cpp` contains no platform conditional, so \"per-platform backend\" is structurally true rather than true only because the file says so"
    - "D-01: the gate is a `tests/test_*.py` pytest module, executes in ZERO CI legs on this branch, and costs no `scripts/check_*.py` floor bumps"
    - "Every assertion the module makes is demonstrated able to FAIL, on copies in `tmp_path`, never by editing committed source"
  prohibitions:
    - statement: "The gate does NOT pin `include/rurp_shield.h` by blob SHA"
      status: required
      verification: "A committed test that hardcodes a blob SHA of a shared header would break the first time a later milestone legitimately edits it. The blob-SHA pin for `rurp_shield.h` is a plan-level proof recorded in this phase's SUMMARY files (and in `126-NONREGRESSION.md`); the committed gate uses textual anchors that actually exist, the Phase-125 drift-leg discipline"
    - statement: "The gate never calls a pytest skip nor a conditional-skip marker"
      status: required
      verification: "The analog's self-enforcing leg (`tests/test_vpp_seam_manual_on_every_board.py:447-468`) is copied verbatim including its concatenation trick, and passes"
    - statement: "The gate depends on no path under `.pio/`"
      status: required
      verification: "A dedicated function asserts the property against the module's own text. A `.pio/libdeps` dependency passes warm and fails on a clean checkout (C-12), the fail-open shape A-7 measured"
    - statement: "No production source file is modified by this plan"
      status: required
      verification: "The only path in `files_modified` is the new test module; `git show --stat` for this plan's commit lists exactly one path"
    - statement: "No new `scripts/check_*.py` is created"
      status: required
      verification: "`ls scripts/check_*.py | wc -l` is still 5; `tests/test_checker_convention.py` is unmodified"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 126-12 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` unchanged"
  artifacts:
    - firestarter/tests/test_config_storage_seam_shape.py
  key_links:
    - "The includer census is the mechanical form of D-09, which is Phase 125's C-1 lesson applied before the fact. It must be written so that it holds at three points in time — now (2 includers), after Plan 126-08 (3 includers), and in any later milestone that adds a fourth platform (which would have to add its path to the sanctioned set deliberately). An `== 3` assertion authored now would be red until Plan 126-08 lands; an `== 2` assertion would go red when it does. Both are wrong."
    - "The `extern \"C\"` assertion is the only local detector for a failure mode that otherwise appears solely as an ARM link error. `arm-none-eabi-gcc` is absent from this environment, so a mangled-vs-unmangled mismatch would reach the gated CI run hours later. This is the same class of defect as C-3's missing flash driver."
---

<objective>
Turn CFG-03's shape requirements into an exit code: the seam header declares exactly two `bool`
functions with C linkage, it is included by exactly the sanctioned translation units and never by
`include/rurp_shield.h`, the four public config declarations stay where they were, and the policy
layer carries no platform conditional and no EEPROM address.

Purpose: Criterion 3's structural half. Plan 126-03 landed the split and proved its behaviour with the
CFG-04 regression test; nothing yet stops a later edit from adding a third seam function, moving a
declaration, dropping the `extern "C"` wrapper, or adding the one `#include` line that D-09 exists to
forbid. This plan makes each of those an exit code, locally, before the gated ARM CI run is the only
thing that would catch them.

Output: `firestarter/tests/test_config_storage_seam_shape.py`, green, with every assertion
demonstrated able to fail.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-03-SUMMARY.md

**Read order and precedence.** `126-CONTEXT.md` records D-01…D-19; `126-RESEARCH.md` files 14
corrections and **wins on verified facts**. For this plan the load-bearing ones are **C-11** (the
config API's C linkage comes from `rurp_shield.h:11-12`, so the seam header must match), **C-13** (the
native counts cannot move from this phase's edits), and **C-14** (seven consumers of the config API,
all above the seam, none of which changes). `126-PATTERNS.md` §"Golden-trace and header-blast-radius
discipline" and §`tests/test_config_storage_*.py` give the enforcement idioms.

**Repo layout.** Every path lives in the firmware submodule `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commits land inside that submodule; only the SUMMARY commits in the meta
repo. `firestarter_app` is out of scope (D-12). **Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`** (RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check
`git rev-parse --abbrev-ref HEAD` in both repos (RESEARCH Pitfall 7).

**Requirement ticking scope for this plan: NONE.** CFG-03 spans Plan 126-03 (the split) and this plan
(the gate). Do not tick CFG-01…CFG-07. Plan 126-12 is the only plan permitted to tick any of them.

**Claim ceiling.** No PY32F071 PCB exists; `arm-none-eabi-gcc`, `cmake` and `ninja` are absent here.
This gate reads source text and compiles nothing for ARM; it must not state or imply any ARM build or
runtime claim. State the ceiling by reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/test_config_storage_seam_shape.py — the CFG-03 structural gate</name>
  <files>firestarter/tests/test_config_storage_seam_shape.py</files>
  <read_first>
    - `firestarter/include/rurp_config_storage.h` — the file the gate asserts against, as Plan 126-03
      actually wrote it. Read it; do not gate against the plan's description of it.
    - `firestarter/src/rurp_config_utils.cpp` — post-split. Confirm what it now includes and that
      `CONFIG_START` is gone.
    - `firestarter/src/boards/rurp_config_storage_eeprom.cpp` — post-split. Confirm `CONFIG_START 48`
      is here and the guard placement.
    - `firestarter/include/rurp_shield.h` — the `CONFIG_VERSION` line (46), `VALUE_R1`/`VALUE_R2`
      (49–50), the four config declarations (61 and 150–152), and the `extern "C"` open/close at
      11–12 / 161–162. **All five line numbers verified at pattern-map time.**
    - `firestarter/tests/test_vpp_seam_manual_on_every_board.py` — the module docstring convention
      (1–76), self-contained path resolution (78–93), the **textual drift-gate idiom** (333–386, plain
      `read_text()` plus substring/regex presence with a full-context failure message), the
      **negative-form assertion** at 412, and the self-enforcing no-skip leg (447–468) — copy that leg
      verbatim including its concatenation trick.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §"Golden-trace and header-blast-radius discipline" — the 46-TU / 14-`host_stubs.cpp` blast radius
      that makes D-09's census load-bearing, and the negative-regex plus includer-census shape it
      recommends.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-03-SUMMARY.md`
      — the recorded includer census from the split commit (two translation units, named), so this
      gate's initial expectation matches an observed value rather than an assumption.
  </read_first>
  <behavior>
    Test functions this module must contain, each named for what it proves:
    - `test_seam_header_declares_exactly_two_functions` — a declaration census over the header finds
      exactly two function prototypes, both returning `bool`, named `rurp_config_storage_load` and
      `rurp_config_storage_save`, with parameter lists `(void*, size_t)` and `(const void*, size_t)`.
      Also asserts no `enum`, no `typedef` and no object-like macro other than the include guard.
    - `test_seam_header_has_c_linkage_with_includes_outside_the_wrapper` — the `#ifdef __cplusplus` /
      `extern "C" {` open and its matching close both exist, both declarations lie between them, and
      the `<stdbool.h>` and `<stddef.h>` includes appear at a line index **before** the wrapper opens.
    - `test_seam_header_is_not_included_by_rurp_shield_h` — `include/rurp_shield.h` contains no
      include directive naming the seam header, and no header it includes does either (a transitive
      walk bounded to headers inside this repository). The failure message must carry the measured
      consequence so the reason survives in the diagnostic.
    - `test_seam_header_includers_are_exactly_the_sanctioned_set` — the census. Walk `src/`,
      `include/`, `platform/`, `test/` and `lib/` for files containing an include of the seam header.
      Assert: (a) every hit is in the sanctioned set of three paths — `src/rurp_config_utils.cpp`,
      `src/boards/rurp_config_storage_eeprom.cpp`, `platform/py32f071/src/config_storage_flash.cpp`;
      (b) every sanctioned path that **exists in the tree** is among the hits; (c) the hit count is
      **at least 2** (non-vacuity). This shape is stable before and after Plan 126-08 and is never
      vacuous.
    - `test_public_config_declarations_stay_in_rurp_shield_h` — all four of `rurp_get_config`,
      `rurp_load_config`, `rurp_save_config` and `rurp_validate_config` are declared in
      `include/rurp_shield.h`, and none of them is declared in the seam header.
    - `test_config_version_literal_is_unchanged` — `include/rurp_shield.h` defines `CONFIG_VERSION` as
      the literal `VER06`.
    - `test_config_start_lives_below_the_seam` — `CONFIG_START` is defined in
      `src/boards/rurp_config_storage_eeprom.cpp` with the value 48, and no definition of it appears in
      `src/rurp_config_utils.cpp`.
    - `test_policy_layer_has_no_platform_conditional` — `src/rurp_config_utils.cpp` contains no
      preprocessor conditional of any kind, so the per-platform split is structural rather than
      declared.
    - `test_helper_reports_violations_on_planted_copies` — the **RED demonstration**: a set of
      planted copies built in `tmp_path` (a header with a third declaration; a header whose includes
      sit inside the `extern "C"` block; a `rurp_shield.h` copy carrying the forbidden include; an
      unsanctioned includer) each fed to the same module-level helpers the positive tests call, each
      producing a non-empty violation list.
    - `test_module_has_no_pio_libdeps_dependency` — the module's own text contains no path under
      `.pio/`.
    - `test_compiler_is_required_not_optional` — the analog's self-enforcing no-skip leg, adapted: this
      module invokes no compiler, so the leg asserts only the absence of a skip call and a
      conditional-skip marker.
  </behavior>
  <action>
    Create `firestarter/tests/test_config_storage_seam_shape.py` under `tests/` — never under `test/`,
    which is globbed into builds. It is a `tests/test_*.py` module and **not** a new
    `scripts/check_*.py`: those floors cost four artifacts plus bumps to
    `tests/test_checker_convention.py`'s `FLOOR` and `FIXTURE_FLOOR`, and a pytest module costs zero.

    Open with the house module-docstring convention: MIT header, `Phase 126 Plan 05 -- <what>`,
    `Requirements: CFG-03`, `Decisions covered: D-01, D-06, D-07, D-08, D-09`, a rationale block, the
    explicit statement that this module **executes in NO CI leg on this branch** (`pytest tests/` runs
    only in `build.yml` on `main` and `beta-build.yml` on `beta`; `py32f071.yml` has no pytest step),
    the "self-contained path resolution, NOT in conftest.py" note, and a numbered `Coverage:` list
    mapping each function to its decision or requirement.

    Imports: stdlib plus `pytest` only. Resolve every path self-contained at module level from
    `Path(__file__).resolve().parent`, following the analog. Declare the sanctioned includer set as a
    module-level tuple of three repo-relative paths, with a comment stating that
    `platform/py32f071/src/config_storage_flash.cpp` arrives in Plan 126-08 and that the census is
    deliberately written to be correct both before and after that — every hit must be sanctioned, every
    sanctioned path that exists must be a hit, and the count must be at least 2.

    Factor every check into **module-level helpers taking a path or text and returning a list of
    violations**, so the positive tests and the planted-copy test exercise the same code. A parallel
    second implementation inside the RED test would prove nothing about the gate.

    Use the analog's textual-gate idiom throughout: plain `read_text()` plus substring or regex
    presence, deliberately not parsed into a structure, with assertion messages carrying the path, the
    missing or offending item, and a short quote of the surrounding text. For the negative assertions
    use the analog's negative-regex form at line 412 as the shape reference.

    **Do not hardcode any blob SHA in this module.** A committed gate that pins a shared header by hash
    breaks the first time a later milestone legitimately edits it. The blob-SHA pin for
    `include/rurp_shield.h` is a plan-level proof recorded in this phase's SUMMARY files; this gate uses
    textual anchors that actually exist — the Phase-125 drift-leg discipline.

    Write the twelve functions listed in the behaviour block. For the transitive-reachability walk in
    `test_seam_header_is_not_included_by_rurp_shield_h`, bound the walk to headers inside this
    repository (resolve include names against `include/` and the including file's own directory) and
    record the number of headers visited in the assertion message, so a walk that visited nothing
    cannot pass silently.

    Do not use a skip call or a conditional-skip marker. Copy the analog's self-enforcing leg,
    including the concatenation trick.

    Run `python3 -m pytest tests/test_config_storage_seam_shape.py -v` and record every function name
    with its result plus the observed census (the hit list and the count). Then run
    `python3 -m pytest tests/ -q` and record the new total. Commit the single new file with a message
    of the form `test(126-05): gate the config storage seam's shape, linkage and includer census`, then
    re-check the branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_config_storage_seam_shape.py -v 2>&1 | tail -25 && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "checkers=$(ls scripts/check_*.py | wc -l)" && git show --stat --format=%H HEAD | head -5 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/tests/test_config_storage_seam_shape.py` exists under `tests/`, not `test/`.
    - The module docstring carries the MIT header, the `Phase 126 Plan 05` attribution, `Requirements: CFG-03`, a `Decisions covered:` line naming D-01, D-06, D-07, D-08 and D-09, the explicit no-CI-leg statement, the self-contained-path note, and a numbered `Coverage:` list with one entry per function.
    - The module imports only stdlib and `pytest`, and `test_module_has_no_pio_libdeps_dependency` passes.
    - All twelve functions named in the behaviour block exist with those names and `pytest -v` reports every one passed.
    - The declaration census finds exactly two prototypes, both `bool`-returning, with the D-06 names and parameter lists, and asserts the absence of any enum, typedef or non-guard object-like macro.
    - The linkage test asserts both the `#ifdef __cplusplus` / `extern "C" {` open and its close exist, that both declarations lie between them, and that the `<stdbool.h>` and `<stddef.h>` includes appear at a line index strictly before the wrapper opens.
    - The `rurp_shield.h` test asserts no direct include of the seam header and performs a repo-bounded transitive walk; its assertion message records the number of headers visited, and that number is greater than zero.
    - The includer census asserts all three properties — every hit sanctioned, every existing sanctioned path a hit, count ≥ 2 — and the observed hit list and count are recorded. Today the count is 2 and the two paths are `src/rurp_config_utils.cpp` and `src/boards/rurp_config_storage_eeprom.cpp`.
    - The four public config declarations are asserted present in `include/rurp_shield.h` and absent from the seam header.
    - `CONFIG_VERSION` is asserted to be the literal `VER06`.
    - `CONFIG_START` is asserted defined as 48 in the AVR backend and absent from `src/rurp_config_utils.cpp`.
    - `src/rurp_config_utils.cpp` is asserted to contain no preprocessor conditional.
    - `test_helper_reports_violations_on_planted_copies` builds its planted copies in `tmp_path`, feeds them to the same module-level helpers the positive tests call, and asserts a non-empty violation list for each. No committed file is mutated: the blob SHAs of `include/rurp_config_storage.h`, `include/rurp_shield.h` and `src/rurp_config_utils.cpp` are unchanged before and after the run, and this is recorded.
    - No blob SHA literal appears anywhere in the module.
    - The self-enforcing no-skip leg is present with the concatenation trick and passes.
    - `python3 -m pytest tests/ -q` reports the Plan 126-04 total plus this module's function count, and both numbers are recorded.
    - `ls scripts/check_*.py | wc -l` is still `5`; `tests/test_checker_convention.py` is unmodified.
    - `git show --stat HEAD` lists exactly one path; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>CFG-03's structural requirements are an exit code: two declarations with C linkage, includes outside the wrapper, a census that is stable across Plan 126-08 and never vacuous, the four public declarations still in place, `CONFIG_START` below the seam, and no platform conditional in the policy layer.</done>
</task>

<task type="auto">
  <name>Task 2: Prove every assertion able to fail, and record the D-09 blob pin at plan level</name>
  <files>(no files modified — mutation demonstrations run in tmp_path; evidence recorded into this plan's SUMMARY)</files>
  <read_first>
    - `firestarter/tests/test_pinmap_guard_fires.py` — the in-tree precedent for a planted-violation /
      must-fail leg, including its preprocess-only compile form if a shape reference is wanted.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §"Golden-trace and header-blast-radius discipline" — the blast-radius numbers to quote in the
      recorded demonstration for the forbidden-include mutation.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` §C-15 — why the blob-SHA pin belongs in a
      plan-level record rather than in a committed test.
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` §D-14 — "a guard that supplies
      the answer it tests is structurally dead". Read it before writing the demonstrations, so no
      demonstration is constructed by feeding the helper its own expected output.
  </read_first>
  <action>
    A gate that passes without observing anything is the failure mode this project has unwound twice
    (Phases 118 and 124). Task 1's planted-copy test covers four cases in-module; this task extends the
    demonstration to **every** assertion the module makes and records the results as evidence.

    For each of the following mutations, build the mutated copy in `tmp_path` or a scratch directory
    outside the repository, feed it to the same module-level helper the corresponding positive test
    calls, and record the observed violation message. Never edit a committed file.
      1. **Third declaration** — add a function prototype to a copy of the seam header. The declaration
         census must report a violation naming the extra prototype.
      2. **Wrong return type** — change one declaration to return `void`. The census must report it.
      3. **Includes inside the wrapper** — move `<stddef.h>` inside the `extern "C"` block in a copy.
         The linkage test's helper must report a violation. Record why this matters locally: a
         mangled-vs-unmangled mismatch would otherwise appear only as an ARM link failure, and
         `arm-none-eabi-gcc` is absent from this environment.
      4. **Missing `extern "C"` wrapper** — delete the wrapper from a copy. The linkage helper must
         report it.
      5. **The forbidden include** — add an include of the seam header to a copy of
         `include/rurp_shield.h`. The reachability helper must report it, and the recorded message must
         carry the measured consequence: 46 including translation units, 14 of them native
         `host_stubs.cpp` files, and a measured collapse from 141 cases / 141 succeeded to 17 suites /
         0 succeeded.
      6. **Transitive forbidden include** — add the include to a copy of an intermediate header that
         `rurp_shield.h` includes. The reachability walk must still report it, proving the walk is not
         depth-1 only.
      7. **An unsanctioned includer** — add a file outside the sanctioned set that includes the seam
         header. The census helper must report it.
      8. **A sanctioned path that stops including it** — remove the include from a copy of
         `src/boards/rurp_config_storage_eeprom.cpp`. The census helper must report the missing hit,
         proving property (b) is enforced and not just property (a).
      9. **An empty census** — point the census helper at a tree with no hits. It must report the
         non-vacuity violation, proving property (c).
      10. **`CONFIG_START` above the seam** — add the definition to a copy of
          `src/rurp_config_utils.cpp`. The helper must report it.
      11. **A platform conditional in the policy layer** — add one to a copy of
          `src/rurp_config_utils.cpp`. The helper must report it.
      12. **A changed `CONFIG_VERSION` literal** — change it in a copy of `include/rurp_shield.h`. The
          helper must report it.
    Record all twelve as a table: mutation, helper exercised, expected violation, observed violation
    message. Then re-run the unmutated module and confirm it is green, and confirm the blob SHAs of
    `include/rurp_config_storage.h`, `include/rurp_shield.h`, `src/rurp_config_utils.cpp` and
    `src/boards/rurp_config_storage_eeprom.cpp` are unchanged — proving no mutation leaked into the
    repository.

    **Record the D-09 blob pin at plan level, not in the test.** State, as observed values:
    `git hash-object include/rurp_shield.h` returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`
    (unchanged since before the phase), and `git log --oneline -1 -- include/rurp_shield.h` shows no
    commit from this phase. State explicitly why this pin lives here rather than in the committed
    gate: a test hardcoding a shared header's hash would break the first time a later milestone
    legitimately edits it, so the committed gate uses textual anchors and the exact-bytes claim is a
    phase-scoped record that `126-NONREGRESSION.md` will re-execute.

    Also re-run and record the two pinned native envs at exactly 141 cases / 141 succeeded / 17 suites
    and `python3 -m pytest tests/ -q` at its current total, so this plan does not leave the suite in an
    unknown state.

    Do not modify any file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_config_storage_seam_shape.py -q 2>&1 | tail -2 && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "shield=$(git hash-object include/rurp_shield.h)" && echo "seam=$(git hash-object include/rurp_config_storage.h)" && echo "policy=$(git hash-object src/rurp_config_utils.cpp)" && echo "avrbackend=$(git hash-object src/boards/rurp_config_storage_eeprom.cpp)" && git log --oneline -1 -- include/rurp_shield.h && echo "fw-porcelain=$(git status --porcelain | wc -l)" && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - All twelve mutation demonstrations are recorded as a table with mutation, helper exercised, expected violation and the observed violation message. Every one produced a genuine violation — not a pass, not a skip.
    - Demonstration 5's recorded message carries the measured blast radius: 46 including translation units, 14 native `host_stubs.cpp` files, and the 141 cases / 141 succeeded → 17 suites / 0 succeeded collapse.
    - Demonstration 6 shows the reachability walk is not depth-1 only, by reporting a violation planted in an intermediate header.
    - Demonstrations 8 and 9 show census properties (b) and (c) are separately enforced, not implied by (a).
    - No demonstration was constructed by feeding a helper its own expected output; each mutation is an independent input.
    - Every mutation ran in `tmp_path` or outside the repository. The blob SHAs of `include/rurp_config_storage.h`, `include/rurp_shield.h`, `src/rurp_config_utils.cpp` and `src/boards/rurp_config_storage_eeprom.cpp` are recorded unchanged after the demonstrations, and `git status --porcelain` for `/workspaces/firestarter` is 0 lines.
    - The unmutated module is re-run and recorded green with its full function list.
    - The D-09 blob pin is recorded at plan level: `include/rurp_shield.h` at `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, `git log --oneline -1` on that path showing no commit from this phase, and an explicit statement of why the pin lives in the plan record rather than in the committed gate.
    - Both pinned native envs are recorded at exactly 141 cases / 141 succeeded / 17 suites, with the extended-timeout discipline stated, and `pytest tests/ -q` is recorded at its current total.
    - No file was modified by this task; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>Every assertion in the CFG-03 gate is demonstrated able to fail on an independent mutated input, the census's three properties are each shown separately enforced, no mutation reached the repository, and the exact-bytes claim about `include/rurp_shield.h` is recorded at plan level where it belongs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the seam header → the translation units that can reach it | D-09's constraint. `include/rurp_shield.h` reaches 46 TUs including 14 native `host_stubs.cpp` files; one include line there was measured to collapse the native suites |
| C++ name mangling → the ARM linker | The `extern "C"` wrapper. A mismatch is invisible locally because no ARM toolchain exists here, and would surface only in the gated CI run |
| the policy layer → platform-specific code | A single `#ifdef` would make "per-platform backend" true only because the file says so, and would put the EEPROM library back in py32's translation unit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-05-01 | Denial of service | an include of the seam header reaching `include/rurp_shield.h` | critical | mitigate | A direct-include assertion plus a repo-bounded transitive reachability walk whose assertion message records the headers visited; demonstrations 5 and 6 prove both depths fire. Measured consequence quoted in the diagnostic |
| T-126-05-02 | Tampering | a third seam function or a widened signature added later | high | mitigate | The declaration census asserts exactly two `bool` prototypes with the D-06 signatures and the absence of enums, typedefs and non-guard macros; demonstrations 1 and 2 prove it fires |
| T-126-05-03 | Repudiation | a linkage defect detectable only by an ARM link | high | mitigate | The `extern "C"` wrapper and the include-ordering assertion are the only local detectors; demonstrations 3 and 4 prove both fire. Same class as C-3's missing flash driver — a CI-only failure converted into a local one |
| T-126-05-04 | Denial of service | a census assertion that is red before Plan 126-08 or after it | high | mitigate | The three-property formulation (all hits sanctioned, all existing sanctioned paths hit, count ≥ 2) is stable at both points in time; demonstrations 8 and 9 prove properties (b) and (c) are separately enforced |
| T-126-05-05 | Denial of service | a committed gate pinning a shared header by blob SHA | medium | mitigate | Prohibited outright; the gate uses textual anchors that exist (the Phase-125 drift-leg discipline) and the exact-bytes claim is recorded at plan level and re-executed by `126-NONREGRESSION.md` |
| T-126-05-06 | Tampering | a silent schema or version change | high | mitigate | `CONFIG_VERSION` asserted as the literal `VER06` and demonstrated able to fail (demonstration 12); the four public declarations asserted still in `rurp_shield.h` |
| T-126-05-07 | Elevation of privilege | a platform conditional reintroduced into the policy layer | medium | mitigate | An assertion that `src/rurp_config_utils.cpp` contains no preprocessor conditional, demonstrated able to fail (demonstration 11) |
| T-126-05-08 | Repudiation | a gate whose RED demonstration re-implements the check | medium | mitigate | Every check is a module-level helper the positive tests and the planted-copy test both call; the acceptance criteria forbid a parallel second implementation and forbid feeding a helper its own expected output (Phase 124 D-14) |

**Package legitimacy:** not applicable. No `npm`, `pip`, `cargo` or PlatformIO `lib_deps` entry is
added. No `T-126-SC` row is owed by this plan.
</threat_model>

<verification>
1. `tests/test_config_storage_seam_shape.py` exists with all twelve named functions passing, stdlib+pytest only, self-contained path resolution, no `conftest.py`, no `.pio/` reference, no blob SHA literal, no skip.
2. The declaration census asserts exactly two `bool` prototypes with the D-06 signatures and the absence of enums, typedefs and non-guard macros.
3. The linkage test asserts the `extern "C"` wrapper exists, both declarations lie inside it, and both includes precede it.
4. `include/rurp_shield.h` is asserted not to include the seam header directly or transitively, with the headers-visited count recorded and non-zero.
5. The includer census enforces all three properties and its observed hit list (2 today, both named) is recorded.
6. The four public declarations, the `VER06` literal, `CONFIG_START`'s location and the absence of a platform conditional are each asserted and each demonstrated able to fail.
7. All twelve mutation demonstrations produced genuine violations from independent inputs; no committed file was mutated; four blob SHAs re-asserted unchanged.
8. The D-09 blob pin for `include/rurp_shield.h` is recorded at plan level with the reason it is not in the committed gate.
9. `pytest tests/` at the Plan 126-04 total plus this module's functions; both pinned native envs at 141/17; five checkers unchanged.
10. One path in the commit; both repos on their milestone branches; no requirement ticked.
</verification>

<success_criteria>
- CFG-03 (structural half): the seam's shape, C linkage, include ordering, includer set and the location of everything that stayed above or moved below it are all exit codes rather than prose.
- D-09 is enforced mechanically at two depths, with the measured consequence carried in the diagnostic so the reason survives the next reader.
- The census is correct both before and after Plan 126-08 lands the third includer, and cannot pass on an empty tree.
- Every assertion is demonstrated able to fail on an independent input, using the same helpers the positive tests call.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{cpp,h}` | new files (HAL-free core) | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07) and
the `# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in
`firestarter/platform/py32f071/CMakeLists.txt` (Plan 126-03, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-05-SUMMARY.md`
when done. Record: the module's full function list with results; the observed includer census (hit
list and count) and the sanctioned set; the headers-visited count from the reachability walk; the
twelve mutation demonstrations as a table with the helper exercised and the observed violation message
each; the four blob SHAs re-asserted after the demonstrations; the plan-level D-09 pin
(`include/rurp_shield.h` at `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, no phase commit on that path)
with the stated reason it is not in the committed gate; the `pytest tests/` total before and after; the
two native env counts; the checker count (5, unchanged); the commit SHA with its single changed path;
and the branch re-check in both repos.
</output>
