---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 03
type: execute
wave: 2
depends_on: ["126-01", "126-02"]
files_modified:
  - firestarter/include/rurp_config_storage.h
  - firestarter/src/rurp_config_utils.cpp
  - firestarter/src/boards/rurp_config_storage_eeprom.cpp
  - firestarter/platform/py32f071/CMakeLists.txt
  - firestarter/scripts/check_cmake_manifest.py
autonomous: true
requirements: [CFG-03, CFG-04]

must_haves:
  truths:
    - "CFG-03 / D-06: `include/rurp_config_storage.h` declares exactly two `bool`-returning functions over a byte blob — `rurp_config_storage_load(void*, size_t)` and `rurp_config_storage_save(const void*, size_t)` — and nothing else"
    - "C-11: the seam header wraps its declarations in the same `#ifdef __cplusplus` / `extern \"C\"` pair the config API already has via `rurp_shield.h:11-12`, and puts its `<stdbool.h>` / `<stddef.h>` includes BEFORE the `extern \"C\"` open (the `include/rurp_vpp.h:58` vs `:70` ordering), not inside it as RESEARCH §Pattern 1's sketch does"
    - "CFG-03 / D-07: all four public functions and the `rurp_config` global stay in `src/rurp_config_utils.cpp`; only the two byte-blob calls cross the seam. `#define CONFIG_START 48` is an EEPROM address, is meaningless on py32, and moves **into** the AVR backend TU"
    - "CFG-04 / D-08: the AVR backend is `src/boards/rurp_config_storage_eeprom.cpp`, a **pure move** — `CONFIG_START 48` from `:11`, the typed `EEPROM.get` from `:24` and the typed `EEPROM.put` from `:29`, returning `true` unconditionally, so the emitted access is byte-identical rather than merely equivalent"
    - "D-14: `rurp_validate_config`'s write-back at the current `:38` is untouched — one validate policy on both platforms, which is what CFG-03 keeps common and what makes PR #48's drifted copy deletable rather than reconcilable"
    - "D-08, **one of its four manifest edits, deliberately**: `platform/py32f071/CMakeLists.txt` gains exactly one reasoned `# PY32_EXCLUDED:` line, for the new EEPROM backend, taking the exclusion set from 5 lines to **6** and leaving the enforced-source count at **24**. The other three edits — retiring the `src/rurp_config_utils.cpp` exclusion, naming that file in `FIRESTARTER_COMMON_SOURCES`, and C-3's flash-driver entry — move to Plan 126-08, which lands them in the same commit that **deletes** `platform/py32f071/src/config.cpp`"
    - "**Why the manifest split (a planning correction to D-08's grouping, not to D-08 itself):** `platform/py32f071/src/config.cpp` currently defines all four public config functions. Naming `src/rurp_config_utils.cpp` in the ARM manifest here — while `config.cpp` still exists — would give the ARM link **two definitions of each of the four functions**. Nothing local can detect that (`arm-none-eabi-gcc`, `cmake` and `ninja` are absent; `py32f071.yml` does not fire on a push to this branch), so it would have surfaced only in Plan 126-11's gated CI run, several plans later. Deferring the promotion to the deletion commit removes the broken window entirely rather than documenting it"
    - "The five edits in this plan land in ONE commit: `check_cmake_manifest.py`'s reverse check makes a present-but-unnamed `src/*.cpp` exit 1 and its forward check makes a named-but-absent path exit 1, and `tests/test_check_cmake_manifest.py::test_armed_and_passing_on_the_real_tree` asserts the REAL tree — so the new backend TU and its exclusion line have no green intermediate state"
    - "D-04: after the split, the blob SHA recorded by Plan 126-02 for `tests/test_config_storage_eeprom_regression.py` **re-hashes identical** and the test is **still green** — the primary proof; the path-scoped `git diff --stat` is quoted in full as corroboration only"
    - "C-13: both pinned native envs still report exactly **141 test cases / 17 suites**, because all three native `build_src_filter` lines name `boards/rurp_serial_utils.cpp` by exact path and compile neither `src/rurp_config_utils.cpp` nor the new backend"
    - "The checker's own module docstring is updated in the same commit, because it enumerates the `PY32_EXCLUDED` set and that set grows from 5 lines to 6 here — leaving it stale would make the checker document a set that no longer exists. The docstring must also record that the `src/rurp_config_utils.cpp` entry's retirement is deferred to the commit deleting `platform/py32f071/src/config.cpp`, and why"
  prohibitions:
    - statement: "Neither pinned native environment may move off **141 test cases / 17 suites** — after this commit, `pio test -e native` and `pio test -e native_nodevtools` must each still report exactly those two numbers"
      status: required
      verification: "Run both envs cold, each with an extended timeout of at least 540000 ms, and assert **both numbers explicitly** — the case count and the suite count — never \"tests pass\": a suite that stops being collected also reports green. C-13 records why they structurally cannot move from this phase's edits (all three native `build_src_filter` lines, `platformio.ini:163/:252/:290`, name `boards/rurp_serial_utils.cpp` by exact path and compile neither `src/rurp_config_utils.cpp` nor the new `src/boards/rurp_config_storage_eeprom.cpp`), but the counts are **measured here, not inferred**. This is the milestone-wide invariant BASE-01 and MERGE-06 both cite, and Phase 125's C-1 measured the failure signature: 141 cases / 141 succeeded collapsing to 17 suites / 0 succeeded"
    - statement: "`include/rurp_shield.h` is NOT modified (D-09)"
      status: required
      verification: "`git hash-object include/rurp_shield.h` still returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, and the file is absent from this commit's changed-path list. Measured consequence of one `#include` line in that header: `pio test -e native` goes from 141 cases / 141 succeeded to 17 suites / 0 succeeded, because it reaches 46 TUs including 14 native `host_stubs.cpp` files (125-RESEARCH C-1)"
    - statement: "The seam header is included by exactly three translation units — the policy layer and the two backends — and by nothing else"
      status: required
      verification: "An includer census over `src/`, `include/`, `platform/` and `test/` finds the policy TU plus the AVR backend today (the ARM backend's seam implementation arrives in Plan 126-08); the count is recorded and Plan 126-05 gates it"
    - statement: "`rurp_configuration_t` and `CONFIG_VERSION \"VER06\"` are unchanged (CFG-07)"
      status: required
      verification: "`git hash-object include/rurp_types.h` returns `d3fe5203a91527bdb7b20a33843c81065e21c613`; the `CONFIG_VERSION` line at `include/rurp_shield.h:46` still reads the literal `VER06`"
    - statement: "No `#ifdef __AVR__` (or any platform conditional) is introduced inside `src/rurp_config_utils.cpp`"
      status: required
      verification: "D-08 rejected it outright: it would make \"per-platform backend\" true only because the file says so, and would leave py32 sharing a translation unit with the EEPROM library. The policy TU's include list after the split is `rurp_shield.h` plus the seam header, and nothing else"
    - statement: "`platformio.ini` is NOT modified"
      status: required
      verification: "`git hash-object platformio.ini` returns `f4e720ba75a8c618cc23bac045ab65084d41a0a4`. The three AVR envs carry no `build_src_filter`, so `src/` is compiled wholesale and the new backend TU is picked up automatically; the three native filters are at `:163`, `:252`, `:290` and must not be widened"
    - statement: "`platform/py32f071/linker/PY32F071xB_FLASH.ld` is NOT modified by this plan"
      status: required
      verification: "`git hash-object` returns `b32b5824c8e27492551db5c2b1d413f74f05b6f3`; the linker edit is Plan 126-06's, and it must remain a descendant of Plan 126-01's `CONFIG-STORAGE.md` commit"
    - statement: "`scripts/baseline/size_baseline.json` and `size_baseline_base01.json` are NOT modified by this plan"
      status: required
      verification: "Both blob SHAs unchanged (`9cc5204b…` and `b940c916…`). Plan 126-04 owns the measurement and any re-baseline; this plan claims no flash or RAM figure at all"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 126-12 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` unchanged"
  artifacts:
    - firestarter/include/rurp_config_storage.h
    - firestarter/src/boards/rurp_config_storage_eeprom.cpp
  key_links:
    - "This is the only commit in v1.23 that edits a file compiled into all three AVR targets. Phase 125 VPP-03 pinned `src/rurp_config_utils.cpp` byte-identical precisely so that any regression here is attributable to this phase alone. That pin is spent by this commit — which is why Plan 126-04 measures the AVR flash and RAM delta IMMEDIATELY afterwards, before any ARM work."
    - "The typed `EEPROM.get`/`put` call must be kept. Both are templates whose length comes from `sizeof(T)`, so the cast to `rurp_configuration_t*` is what makes the moved code emit the identical `(48, sizeof(rurp_configuration_t))` access CFG-04 asserts. A byte-loop over `len` would be arguably equivalent but is not a move, and would have to reproduce `put`'s per-byte `update()` semantics by hand."
    - "The AVR backend returns `true` unconditionally on purpose. The signature is new; the behaviour is byte-identical, because EEPROM always yields bytes and `rurp_validate_config` decides whether they are usable. That is exactly what CFG-04 requires, and it is why D-06 rejected a richer status enum."
---

<objective>
Split configuration persistence into a common policy layer plus a two-function per-platform
byte-blob backend, move the AVR EEPROM code to its own translation unit as a pure move, and land the
the one ARM-manifest exclusion the new translation unit forces — all in **one** commit. Then discharge D-04's
proof: the regression test's recorded blob SHA re-hashes identical and the test is still green.

Purpose: CFG-03 requires the split; CFG-04 requires the AVR side to be a pure move with byte-identical
behaviour. The five edits are atomic by construction — `check_cmake_manifest.py`'s reverse check makes
the new `src/boards/*.cpp` an exit-1 violation the moment it exists unnamed, its forward check makes a
named-but-absent path an exit-1 violation, and `tests/test_check_cmake_manifest.py::test_armed_and_passing_on_the_real_tree`
asserts the real tree passes, so `pytest tests/` would go red between any two of them.

Output: `include/rurp_config_storage.h`, `src/boards/rurp_config_storage_eeprom.cpp`, a policy-only
`src/rurp_config_utils.cpp`, one new reasoned exclusion line plus a docstring update to the ARM manifest
and its checker, in one firmware commit — and the D-04 re-hash proof recorded.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-VALIDATION.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-02-SUMMARY.md

**Read order and precedence.** `126-CONTEXT.md` records the locked decisions D-01…D-19;
`126-RESEARCH.md` files 14 corrections and **wins on verified facts**. For this plan the load-bearing
corrections are **C-3** (D-08's "three edits" is really four — the fourth,
`py32f071_hal_flash.c` into `PY32_SDK_SOURCES`, belongs to Plan 126-08 and **must not be done here**,
because nothing calls `HAL_FLASH_` yet), **C-11** (C linkage and include ordering) and **C-13** (the
native counts cannot move). `126-PATTERNS.md` gives the verbatim analog excerpt for each new file —
read the excerpt it points at rather than inventing a shape.

**`126-02-SUMMARY.md` carries the recorded blob SHA of `tests/test_config_storage_eeprom_regression.py`.**
Task 2 of this plan compares against exactly that 40-character string. Read it from the SUMMARY; do
not recompute it from a description.

**Repo layout.** Every path lives in the firmware submodule `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commits land inside that submodule; only the SUMMARY commits in the meta
repo. `firestarter_app` is out of scope (D-12). **Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`** (RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check
`git rev-parse --abbrev-ref HEAD` in both repos (RESEARCH Pitfall 7).

**Requirement ticking scope for this plan: NONE.** CFG-03 is completed only when Plan 126-05's
seam-shape gate lands; CFG-04 only when Plan 126-04's measurement lands. Do not tick CFG-01…CFG-07.
Plan 126-12 is the only plan permitted to tick any of them.

**Claim ceiling.** No PY32F071 PCB exists, and `arm-none-eabi-gcc`, `cmake` and `ninja` are absent
here — so this plan makes **no ARM claim whatsoever**, not even that the manifest edit compiles. The
ARM evidence for this phase is a CI workflow run URL plus head SHA, gated in Plan 126-11. This plan
also claims **no AVR flash or RAM figure**; Plan 126-04 owns that measurement. State the ceiling by
reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The atomic split — seam header, policy-only TU, AVR backend, three manifest edits, checker docstring, ONE commit</name>
  <files>firestarter/include/rurp_config_storage.h, firestarter/src/rurp_config_utils.cpp, firestarter/src/boards/rurp_config_storage_eeprom.cpp, firestarter/platform/py32f071/CMakeLists.txt, firestarter/scripts/check_cmake_manifest.py</files>
  <read_first>
    - `firestarter/src/rurp_config_utils.cpp` — **read all 40 lines.** The pre-refactor reference.
      `#define CONFIG_START 48` at `:11`; the `rurp_config` global at `:15`; `rurp_get_config` at
      `:18`; `rurp_load_config` at `:22` with `EEPROM.get(CONFIG_START, *config)` at `:24`;
      `rurp_save_config` at `:28` with `EEPROM.put(CONFIG_START, *config)` at `:29`;
      `rurp_validate_config` at `:32` with the write-back `rurp_save_config(config)` at `:38`.
    - `firestarter/include/rurp_vpp.h` — **read all 89 lines.** This is the exact-role analog for the
      new seam header, authored one phase earlier under the same D-09-ancestor constraint. Copy: the
      long ALL-CAPS-labelled rationale block; the includes placed **before** the `extern "C"` open
      (`:58` vs `:70`); the `#ifdef __cplusplus` / `extern "C" {` … `}` pair (`:70-72`, `:88-90`);
      the FIRE-PROOF section naming the `tests/` module that proves its constraints; bare prototypes
      with `(void)` on no-arg functions.
    - `firestarter/src/boards/rurp_common.cpp` lines 1–12 — the AVR-only-TU-in-`src/boards/` analog
      D-08 cites by name: the five-line MIT banner, the include set, and the three-board
      `#if defined(ARDUINO_AVR_UNO) || defined(ARDUINO_AVR_ATmega328PB) || defined(ARDUINO_AVR_LEONARDO)`
      guard. Also read `firestarter/src/boards/uno_rurp_shield.cpp:8` and
      `leonardo_rurp_shield.cpp:9` — both place the guard **before** the includes, which is the safer
      shape for a translation unit that includes the EEPROM library.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the `# PY32_EXCLUDED:` block at `:30-34`
      (five lines; the `src/rurp_config_utils.cpp` entry at `:34` carries its own "WILL NEED
      REVISITING in Phase 126" note), the `set(FIRESTARTER_COMMON_SOURCES …)` block at `:35-53`
      (17 entries, `"${REPOSITORY_ROOT}/…"`-quoted), and `set(PY32_PLATFORM_SOURCES …)` at `:55-63`
      (7 entries, bare relative paths). **Three incompatible path idioms live in this file; do not mix
      them.**
    - `firestarter/scripts/check_cmake_manifest.py` — **read the module docstring in full**, plus
      `parse_manifest`, `resolve_paths`, `enumerate_tree_sources` and `main`. The docstring at
      roughly `:43-78` documents the mandatory `# PY32_EXCLUDED: <path> -- <reason>` format (the
      reason segment is mandatory — a path with no reason is itself a violation) and the 0/1/2 exit
      taxonomy, and it **names the current five-line exclusion set including the
      `src/rurp_config_utils.cpp` entry**.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §"Pattern 1" (the seam header sketch — but prefer the analog's include ordering over the
      sketch's, per PATTERNS), §"Code Examples" → "The AVR backend (CFG-04, pure move)" and the
      planner note beneath it about keeping the typed call, §C-11, §C-13.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §`include/rurp_config_storage.h`, §`src/rurp_config_utils.cpp`,
      §`src/boards/rurp_config_storage_eeprom.cpp`, §`platform/py32f071/CMakeLists.txt` and
      §"The `# PY32_EXCLUDED:` contract and its reverse check".
  </read_first>
  <behavior>
    Behaviour that must hold after this commit, each verifiable by an existing or Plan-126-02 test:
    - `tests/test_config_storage_eeprom_regression.py` — unchanged file, still green: the `get` and
      `put` accesses are still at index 48 with a length equal to `sizeof(rurp_configuration_t)`, and
      the validate write-back still fires on a version mismatch. Its candidate-path resolution now
      finds **2** paths instead of 1.
    - `python3 scripts/check_cmake_manifest.py` — exit 0, **24** enforced sources (unchanged), 14
      exempt, and **6** allow-listed omissions: the five that exist today plus
      `src/boards/rurp_config_storage_eeprom.cpp`. `src/rurp_config_utils.cpp` is still among them —
      its retirement is Plan 126-08's, in the commit that deletes
      `platform/py32f071/src/config.cpp`.
    - `python3 -m pytest tests/ -q` — the Plan 126-02 total, unchanged (this task adds no test).
    - `pio test -e native` and `pio test -e native_nodevtools` — exactly **141 test cases / 141
      succeeded across 17 suites** each.
    - `pio run -e uno`, `-e uno328pb`, `-e leonardo` — each exits 0 and produces an object file for
      the new backend TU. **No flash or RAM figure is claimed here.**
  </behavior>
  <action>
    Author five edits and commit them as **ONE** firmware commit. Do not commit any subset; there is
    no green intermediate state.

    **Edit 1 — create `include/rurp_config_storage.h`.** Follow `include/rurp_vpp.h`'s shape. Use the
    `#ifndef __RURP_CONFIG_STORAGE_H__` / `#define` include-guard form (matching `rurp_types.h` and
    `rurp_shield.h`, the two headers on the other side of this seam) rather than `#pragma once`; both
    idioms are live in `include/` and either is defensible, but the guarded form matches the files
    this header sits between. Then one block comment with ALL-CAPS section labels carrying at minimum:
      - an attribution line: Phase 126, requirements CFG-03/CFG-04, decisions D-06/D-07/D-08/D-09;
      - a WHY EXACTLY TWO FUNCTIONS section recording D-06: the seam is two `bool`-returning
        functions over a byte blob, so the py32 backend can report *"no valid record"* honestly
        instead of returning zeroed bytes and relying on a version-string accident. Record that a
        `void`/`void` seam and a richer status enum were both considered and rejected — the enum
        because no consumer exists for it, and D-15 declines to distinguish blank from corrupt;
      - a WHAT STAYS ABOVE THE SEAM section recording D-07: all four public config functions
        (`rurp_get_config`, `rurp_load_config`, `rurp_save_config`, `rurp_validate_config`) and the
        `rurp_config` global stay in the common policy layer, and their declarations stay where they
        already are in `rurp_shield.h`. `CONFIG_START` is an EEPROM **address**, is meaningless on
        py32, and therefore lives below the seam;
      - a WHY `rurp_shield.h` IS NOT TOUCHED section recording D-09 with its measured reason: one
        `#include` line in that header was measured to take `pio test -e native` from 141 cases / 141
        succeeded to 17 suites / 0 succeeded, because it reaches 46 translation units including 14
        native `host_stubs.cpp` files. This header is included by exactly three translation units —
        the policy layer and the two backends;
      - an AVR BEHAVIOUR IS UNCHANGED section: the AVR implementation returns `true` unconditionally
        after the typed EEPROM read, and policy calls `rurp_validate_config` either way — the
        signature is new, the behaviour is byte-identical, which is what CFG-04 requires;
      - a FIRE-PROOF section naming `tests/test_config_storage_seam_shape.py` (Plan 126-05) as the
        module that gates this header's shape, and `tests/test_config_storage_eeprom_regression.py`
        (Plan 126-02) as the module that pins the AVR access.
    Then the includes — `<stdbool.h>` and `<stddef.h>` — placed **before** the `extern "C"` open, per
    the analog's ordering. `bool` needs `<stdbool.h>` when the header is preprocessed as C and
    `size_t` needs `<stddef.h>`; RESEARCH §Pattern 1's sketch puts `<stddef.h>` inside the
    `extern "C"` block and omits `<stdbool.h>` — do not copy the sketch there.
    Then the `#ifdef __cplusplus` / `extern "C" {` open (C-11: `rurp_shield.h:11-12` already opens
    one, so all four config declarations have C linkage; a mismatch here would be a
    mangled-vs-unmangled link failure appearing only on the ARM target), the two declarations
    `bool rurp_config_storage_load(void* blob, size_t len);` and
    `bool rurp_config_storage_save(const void* blob, size_t len);` each with a short doc comment
    stating the return contract, then the matching close. **Declare nothing else** — no status enum,
    no capability macro, no third function.

    **Edit 2 — reduce `src/rurp_config_utils.cpp` to policy only.** Remove `#include <EEPROM.h>` and
    `#define CONFIG_START 48`. Add `#include "rurp_config_storage.h"`. Replace the `EEPROM.get` line
    at `:24` with a call to `rurp_config_storage_load(config, sizeof(*config))` whose `bool` result is
    explicitly discarded with a cast to `void` and a one-line comment saying why:
    `rurp_validate_config` decides usability, so the load result is deliberately not branched on —
    which is what keeps one validate policy on both platforms (D-14). Replace the `EEPROM.put` line at
    `:29` with `rurp_config_storage_save(config, sizeof(*config))`, likewise cast to `void`. Leave
    everything else byte-identical: the MIT banner, the `rurp_config` global, `rurp_get_config`,
    the whole of `rurp_validate_config` including the write-back call, and the `#include "rurp_shield.h"`
    line (which is what supplies `<string.h>` for `strcmp`/`strcpy`, per `rurp_shield.h:17` — the
    policy TU needs no new include for those). Introduce **no** platform conditional of any kind.

    **Edit 3 — create `src/boards/rurp_config_storage_eeprom.cpp`.** A **move**, not a rewrite. Open
    with the five-line MIT banner copied byte-for-byte from `src/boards/rurp_common.cpp`. Place the
    three-board guard `#if defined(ARDUINO_AVR_UNO) || defined(ARDUINO_AVR_ATmega328PB) || defined(ARDUINO_AVR_LEONARDO)`
    **before** the includes (the `uno_rurp_shield.cpp` / `leonardo_rurp_shield.cpp` placement, which is
    the safer shape for a TU that includes the EEPROM library) with its `#endif` at end of file.
    Include `"rurp_config_storage.h"`, `"rurp_shield.h"` and `<EEPROM.h>`. Carry `#define CONFIG_START 48`
    across from `rurp_config_utils.cpp:11` with a comment stating it was moved verbatim, that it is an
    EEPROM address, and that this is precisely why it belongs below the seam (D-07). Then the two
    functions:
      - `rurp_config_storage_load` casts `blob` to `rurp_configuration_t*` and calls
        `EEPROM.get(CONFIG_START, *that)`, then returns `true`, with a comment stating that returning
        `true` unconditionally is byte-identical to the pre-refactor behaviour because EEPROM always
        yields bytes and `rurp_validate_config` decides whether they are usable;
      - `rurp_config_storage_save` casts `blob` to `const rurp_configuration_t*` and calls
        `EEPROM.put(CONFIG_START, *that)`, then returns `true`.
    Cast the unused `len` parameter to `void` in both, so `-Wall -Wextra` stays silent.
    **Keep the typed call.** `EEPROM.get`/`put` are templates whose length comes from `sizeof(T)`, so
    the cast is what makes the moved code emit the identical `(48, sizeof(rurp_configuration_t))`
    access CFG-04 asserts. A byte-loop over `len` would be arguably equivalent but is not a move, and
    would have to reproduce `put`'s per-byte `update()` semantics by hand.

    **Edit 4 — `platform/py32f071/CMakeLists.txt`, exactly ONE change.** Add a new exclusion line in
    the mandatory format, with a non-empty reason:
    `# PY32_EXCLUDED: src/boards/rurp_config_storage_eeprom.cpp -- AVR EEPROM backend, no ARM analogue`.
    A path with no `--` reason segment is itself a violation. This takes the `PY32_EXCLUDED` set from 5
    lines to **6** and leaves the enforced-source count at **24**.

    **Do NOT retire the `src/rurp_config_utils.cpp` exclusion here, and do NOT add that file to
    `FIRESTARTER_COMMON_SOURCES` here.** This is a deliberate correction to how D-08 grouped its edits,
    and the reason is a defect it would otherwise create: `platform/py32f071/src/config.cpp` still
    defines all four public config functions (`rurp_get_config`, `rurp_load_config`, `rurp_save_config`,
    `rurp_validate_config`). Promoting the policy translation unit into the ARM build while that file
    still exists gives the ARM link **two definitions of each of those four functions**. Nothing in this
    environment can detect it — `arm-none-eabi-gcc`, `cmake` and `ninja` are absent, and
    `py32f071.yml` fires on a push to `beta` and on pull requests, not on a push to this branch — so it
    would surface only in Plan 126-11's gated CI run, several plans later, with a broken window in
    between. Plan 126-08 lands the retirement, the promotion and C-3's flash-driver entry in the **same
    commit that deletes `config.cpp`**, which removes the window rather than documenting it. D-08's four
    edits all still happen; only their grouping changes, and this plan's SUMMARY must record the
    correction and its reason.

    **Do NOT touch `PY32_SDK_SOURCES`.** C-3's flash-driver entry belongs to Plan 126-08, the plan that
    introduces the first `HAL_FLASH_` call; here it would be a manifest entry with no consumer.
    **Do NOT touch `PY32_PLATFORM_SOURCES`.** The `src/config.cpp` deletion is Plan 126-08's.

    **Edit 5 — `scripts/check_cmake_manifest.py`, docstring only.** Its module docstring enumerates the
    current five-line `PY32_EXCLUDED` set. Update that enumeration to the new six-line set so the
    checker does not document a set that no longer exists, and record in the docstring that the
    `src/rurp_config_utils.cpp` entry's retirement is deferred to the commit that deletes
    `platform/py32f071/src/config.cpp`, with the one-line reason (a duplicate-definition ARM link
    otherwise). **Change no code, no regex, no constant, no exit code** — docstring text only.

    **Prove the atomicity claim in both directions before committing**, on the staged tree, and record
    the observations: with the new backend TU present but its exclusion line absent, run
    `python3 scripts/check_cmake_manifest.py` and record exit 1 with the "present in tree, not named
    … and not covered by a reasoned PY32_EXCLUDED entry" violation; then with the exclusion present
    but its reason segment removed, record exit 1 with the missing-reason violation. Restore the
    correct state and record exit 0. Do these as scratch manipulations that are reverted, never as
    committed intermediate states.

    Commit all five edits together with a message of the form
    `refactor(126-03): split config persistence into a common policy layer and a per-platform byte-blob backend`.
    Then re-check `git rev-parse --abbrev-ref HEAD`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_cmake_manifest.py && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "shield=$(git hash-object include/rurp_shield.h)" && echo "types=$(git hash-object include/rurp_types.h)" && echo "pio-ini=$(git hash-object platformio.ini)" && echo "ld=$(git hash-object platform/py32f071/linker/PY32F071xB_FLASH.ld)" && grep -n 'define CONFIG_VERSION' include/rurp_shield.h && grep -c 'PY32_EXCLUDED' platform/py32f071/CMakeLists.txt && git show --stat --format=%H HEAD | head -10 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `include/rurp_config_storage.h` exists, uses the `__RURP_CONFIG_STORAGE_H__` include-guard form, and declares exactly two functions: `rurp_config_storage_load(void*, size_t)` and `rurp_config_storage_save(const void*, size_t)`, both returning `bool`. No third declaration, no enum, no macro other than the include guard.
    - `<stdbool.h>` and `<stddef.h>` are included **before** the `extern "C"` open, and the `#ifdef __cplusplus` / `extern "C" {` … `}` pair wraps both declarations.
    - The header's comment block records D-06's two-function rationale, D-07's above/below split with `CONFIG_START` named as an EEPROM address, D-09 with its measured 141/141 → 17-suites/0 consequence, the unconditional-`true` AVR behaviour statement, and a FIRE-PROOF section naming both `tests/test_config_storage_seam_shape.py` and `tests/test_config_storage_eeprom_regression.py`.
    - `src/rurp_config_utils.cpp` no longer includes the EEPROM library and no longer defines `CONFIG_START`; it includes `rurp_shield.h` and `rurp_config_storage.h` and nothing else. It contains no platform conditional of any kind.
    - `src/rurp_config_utils.cpp` still defines the `rurp_config` global and all four public functions, and `rurp_validate_config`'s body — including its write-back call — is byte-identical to the pre-refactor text apart from nothing. A side-by-side of the pre- and post-refactor `rurp_validate_config` is recorded showing zero differences.
    - Both seam calls in the policy layer discard their `bool` result with an explicit `void` cast and a one-line comment saying `rurp_validate_config` decides usability.
    - `src/boards/rurp_config_storage_eeprom.cpp` exists, carries the five-line MIT banner byte-identical to `src/boards/rurp_common.cpp`'s, places the three-board `#if defined(...)` guard before its includes with a matching `#endif` at end of file, defines `CONFIG_START` as `48`, and calls `EEPROM.get` / `EEPROM.put` through a cast to `rurp_configuration_t*` — not a byte loop.
    - Both backend functions return `true` unconditionally and cast the unused `len` to `void`.
    - `platform/py32f071/CMakeLists.txt`: the `PY32_EXCLUDED` line count is **6**; the new line names `src/boards/rurp_config_storage_eeprom.cpp` with a non-empty reason after `--`; the `src/rurp_config_utils.cpp` exclusion is **still present**; `FIRESTARTER_COMMON_SOURCES` still has 17 entries and does not name `src/rurp_config_utils.cpp`.
    - `PY32_SDK_SOURCES` still has 14 entries and `PY32_PLATFORM_SOURCES` still has 7 — neither is touched by this plan.
    - `python3 scripts/check_cmake_manifest.py` exits 0, its PASS line reports **24** enforced sources and 14 exempt entries, and its allow-listed omission list contains **six** paths including both `src/boards/rurp_config_storage_eeprom.cpp` and `src/rurp_config_utils.cpp`.
    - The SUMMARY records the deliberate manifest-grouping correction and its reason: promoting `src/rurp_config_utils.cpp` into the ARM build while `platform/py32f071/src/config.cpp` still exists would give the ARM link two definitions of each of the four public config functions, undetectable in this environment and surfacing only in Plan 126-11's gated CI run. Plan 126-08 lands the retirement, the promotion and C-3's entry in the same commit as the deletion.
    - `scripts/check_cmake_manifest.py`'s module docstring enumerates the new six-line exclusion set and records that the `src/rurp_config_utils.cpp` entry's retirement is deferred to the `config.cpp` deletion commit, with its one-line reason. A diff of that file shows changes confined to docstring text — no regex, constant, function body or exit code changed.
    - The two atomicity demonstrations are recorded: exit 1 with the uncovered-tree-source violation when the exclusion line is absent, and exit 1 with the missing-reason violation when the reason segment is removed. Both were scratch manipulations, reverted; neither was committed.
    - `python3 -m pytest tests/ -q` reports the same total as at the end of Plan 126-02 — this task adds no test module.
    - `pio test -e native` reports exactly `141 test cases: 141 succeeded` across 17 suites and exits 0; `pio test -e native_nodevtools` reports the same. Both are recorded as counts with the suite count stated, never as "tests pass". Each run uses an extended timeout of at least 540000 ms — a default two-minute timeout truncates the toolchain build mid-compile and silently contaminates the result.
    - `pio run -e uno`, `pio run -e uno328pb` and `pio run -e leonardo` each exit 0, and `.pio/build/<env>/src/boards/rurp_config_storage_eeprom.cpp.o` exists for each. **No flash or RAM figure is claimed in this plan.**
    - `git hash-object include/rurp_shield.h` returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`; `include/rurp_types.h` returns `d3fe5203a91527bdb7b20a33843c81065e21c613`; `platformio.ini` returns `f4e720ba75a8c618cc23bac045ab65084d41a0a4`; `platform/py32f071/linker/PY32F071xB_FLASH.ld` returns `b32b5824c8e27492551db5c2b1d413f74f05b6f3`; `scripts/baseline/size_baseline.json` returns `9cc5204bb437735d77523e62512c1d2cadfc668f`; `scripts/baseline/size_baseline_base01.json` returns `b940c91655600a57ad7ef67cba723943af929daf`.
    - The `CONFIG_VERSION` line at `include/rurp_shield.h:46` still reads the literal `VER06`.
    - An includer census over `src/`, `include/`, `platform/` and `test/` reports exactly two translation units including `rurp_config_storage.h` today — the policy TU and the AVR backend — and the count plus the two paths are recorded. `include/rurp_shield.h` is not among them.
    - All five edits are in **one** firmware commit; `git show --stat` for that commit lists exactly five paths.
    - `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration` after the commit, and the meta repo is still on `gsd/v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>Configuration persistence is split into a common policy layer plus a two-function per-platform byte-blob backend, the AVR side is a pure move keeping the typed EEPROM call, the ARM manifest allow-lists the new backend at 24 enforced sources and 6 exclusions with the policy-TU promotion deliberately deferred to the `config.cpp` deletion commit, and both pinned native environments still report 141 cases across 17 suites — all in one commit with `include/rurp_shield.h` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Discharge D-04 — the recorded test blob SHA re-hashes identical and the test is still green</name>
  <files>(no files modified — evidence capture into this plan's SUMMARY)</files>
  <read_first>
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-02-SUMMARY.md`
      — the recorded 40-character blob SHA of `tests/test_config_storage_eeprom_regression.py`, the
      recorded pre-refactor anchor SHA of `src/rurp_config_utils.cpp`, the recorded access list and
      `sizeof`, and the D-04 proof protocol written out there. **Compare against exactly those
      strings.**
    - `.planning/phases/124-firmware-integration-merge/124-VERIFICATION.md` — the live finding where a
      `git diff --stat | grep -v` pipeline was reported as "(empty)" while a
      `1 file changed, 25 insertions(+)` trailer survived the grep. Read it before writing this
      task's corroboration row.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` §C-15 — blob-SHA re-hash primary;
      `git status --porcelain` scoped by repo name as corroboration; never a path-scoped `git diff`
      alone.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §"Pattern 3" (lines 838–857) — the exact recipe, and §C-14 (the seven consumers of the config
      API, none of which changes — the structural argument that byte-identical behaviour is
      achievable and that no golden trace can move).
  </read_first>
  <action>
    ROADMAP Criterion 3 asks for behaviour "identical to the pre-refactor code — proven by an empty
    `git diff` on the test file itself". D-04 discharges that as a blob-SHA re-hash plus a still-green
    run, because a path-scoped `git diff` has passed vacuously in this repository on a wrong path
    before. This task executes the proof and records it.

    **Primary proof.** Run `git hash-object tests/test_config_storage_eeprom_regression.py` and
    compare, as a string equality producing an exit code, against the value recorded in
    `126-02-SUMMARY.md`. Record both strings and the verdict. Then run
    `python3 -m pytest tests/test_config_storage_eeprom_regression.py -v` and record every function
    name with its result — all must pass. Record the module's now-resolved candidate-path count,
    which must be **2** (both `src/rurp_config_utils.cpp` and
    `src/boards/rurp_config_storage_eeprom.cpp` exist), and the observed access list and `sizeof`,
    and state whether they match the values Plan 126-02 recorded pre-refactor. **A change in the
    observed access pair is a CFG-04 failure, not a curiosity** — report it as a STOP finding.

    If the recorded SHA does **not** re-hash identical, the documented fallback applies and nothing
    else: record both blob SHAs, state the single line that changed and why it was unavoidable, and
    justify it against D-08's locked post-refactor path. Never a silent edit; never a skip; never a
    widened assertion.

    **Corroboration only.** Run `git diff --stat -- tests/test_config_storage_eeprom_regression.py`
    against the pre-refactor commit and **quote its output in full**, including any trailer line. Do
    not summarise it as "(empty)". Label the row explicitly as corroboration, citing
    `124-VERIFICATION.md`'s surviving-trailer finding as the reason the blob hash is primary.
    Additionally record `git status --porcelain` for `/workspaces/firestarter`, naming the repository
    explicitly — the host repo has five known pre-existing porcelain lines and an unqualified row
    would read as a failure it is not.

    **Behaviour-preservation corroboration (C-14).** Record that all seven consumers of the config
    API sit above the seam and none of them changed: `src/firestarter.cpp:40`, `:103`, `:109`;
    `src/boards/rurp_common.cpp:53`; `include/rurp_hw_rev_utils.h:95`, `:101`;
    `src/hardware_operations.cpp:106`, `:119`; and
    `platform/py32f071/src/py32f071_rurp_shield.cpp:297`. Verify each by reading the line, and record
    that none appears in Task 1's commit. This is the structural argument that no golden register
    trace can move.

    **Golden traces and native counts.** Run `python3 -m pytest tests/test_golden_trace_identity.py -q`
    and record the count; golden register traces are compared **per-array** for
    `_shared/sdp_expected.h` since Phase 119, so a whole-file blob shorthand is not the check.
    Re-assert the native counts from Task 1 if they were not captured there.

    Do not modify any file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && echo "test-sha-now=$(git hash-object tests/test_config_storage_eeprom_regression.py)" && python3 -m pytest tests/test_config_storage_eeprom_regression.py -v 2>&1 | tail -20 && python3 -m pytest tests/test_golden_trace_identity.py -q 2>&1 | tail -2 && echo "fw-porcelain=$(git status --porcelain | wc -l)" && python3 scripts/check_cmake_manifest.py && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git hash-object tests/test_config_storage_eeprom_regression.py` is recorded in full and compared as a string equality against the 40-character value recorded in `126-02-SUMMARY.md`; both strings and the verdict are recorded. If they differ, the documented fallback is applied and both SHAs plus the single justified line change are recorded — no silent edit and no skip.
    - `python3 -m pytest tests/test_config_storage_eeprom_regression.py -v` reports every function passed, with the function names listed.
    - The module's resolved candidate-path count is recorded as `2`, and both resolved paths are named.
    - The observed `(operation, index, length)` access list and the observed `sizeof(rurp_configuration_t)` are recorded and compared against Plan 126-02's pre-refactor values; any difference is reported as a STOP finding rather than absorbed.
    - `git diff --stat -- tests/test_config_storage_eeprom_regression.py` output is quoted in full, including any trailer line, and the row is explicitly labelled corroboration-only with `124-VERIFICATION.md`'s finding cited.
    - `git status --porcelain` is recorded for `/workspaces/firestarter` by name, as 0 lines, with the host repo's five known pre-existing lines separately noted as pre-existing.
    - All seven config-API consumer sites are verified by reading the line, recorded with their file and line number, and shown absent from Task 1's commit changed-path list.
    - `python3 -m pytest tests/test_golden_trace_identity.py -q` passes and its count is recorded; the per-array comparison basis for `_shared/sdp_expected.h` is stated.
    - `pio test -e native` and `pio test -e native_nodevtools` are recorded at exactly 141 cases / 141 succeeded / 17 suites each (re-run here if not captured in Task 1), with the extended-timeout discipline stated.
    - `python3 scripts/check_cmake_manifest.py` exits 0 at 24 enforced sources with 6 allow-listed omissions.
    - No file was modified by this task; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>D-04's two-commit proof is discharged as an exit code: the regression test's blob SHA re-hashes identical after the split, the test is still green with the same observed access pair, the path-scoped diff is quoted in full and labelled corroboration only, and all seven config-API consumers are shown unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| common policy → per-platform backend | The new seam. Two `bool` functions over a byte blob are the only thing that varies by platform (D-06) |
| the seam header → the translation units that can reach it | D-09's constraint: exactly three includers, and `rurp_shield.h` (46 include sites, 14 of them native `host_stubs.cpp`) must not be one of them |
| a new `src/*.cpp` → the ARM source manifest | The reverse-omission check makes this crossing mandatory and atomic |
| this commit → all three AVR targets | The only commit in v1.23 editing a file compiled into every AVR target; Phase 125's pin is spent here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-03-01 | Denial of service | a new `#include` reaching `include/rurp_shield.h` | critical | mitigate | D-09: the seam header is included by exactly three TUs and `rurp_shield.h` is not one. Its blob SHA is re-asserted after the commit and the includer census is recorded. Measured failure signature: 141 cases / 141 succeeded → 17 suites / 0 succeeded (125-RESEARCH C-1) |
| T-126-03-02 | Tampering | a behaviour change disguised as a move | high | mitigate | CFG-04's regression test was authored pre-refactor (Plan 126-02) and is re-run here; the typed `EEPROM.get`/`put` call is mandated so the emitted `(48, sizeof(rurp_configuration_t))` access is identical rather than equivalent; `rurp_validate_config` is diffed line-for-line against its pre-refactor text |
| T-126-03-03 | Repudiation | an "unchanged" claim resting on a path-scoped diff | high | mitigate | Blob-SHA re-hash is primary; the `git diff --stat` output is quoted in full and labelled corroboration, citing `124-VERIFICATION.md`'s surviving-trailer finding |
| T-126-03-04 | Tampering | a silent config-schema change | high | mitigate | CFG-07: `include/rurp_types.h`'s blob SHA re-asserted and `CONFIG_VERSION` re-read as the literal `VER06`. No field is added and no version bumped |
| T-126-03-05 | Spoofing | a manifest that names a path the checker never resolves | medium | mitigate | The new-TU-plus-exclusion pair is proven atomic in both directions by scratch demonstrations recording exit 1 for an uncovered tree source and exit 1 for a missing reason segment, then exit 0 for the correct state |
| T-126-03-06 | Repudiation | a checker whose docstring documents a set that no longer exists | medium | mitigate | Edit 5 updates the enumeration to six lines in the same commit and records that the `src/rurp_config_utils.cpp` retirement is deferred to the `config.cpp` deletion commit with its reason; the diff is confined to docstring text |
| T-126-03-07 | Elevation of privilege | a platform conditional inside the policy file | medium | mitigate | D-08 rejected `#ifdef __AVR__` in `src/rurp_config_utils.cpp` outright; the acceptance criteria require the policy TU's include list to be exactly two headers and forbid any platform conditional. Otherwise py32 would share a translation unit with the EEPROM library |
| T-126-03-08 | Denial of service | an unattributable AVR flash delta | high | mitigate | This plan claims no size figure; Plan 126-04 measures immediately afterwards and before any ARM work, which is why the ROADMAP made the internal ordering load-bearing. `size_baseline.json` and the frozen `size_baseline_base01.json` are both re-asserted unchanged here |
| T-126-03-09 | Spoofing | a commit landing on the wrong branch | medium | mitigate | `git rev-parse --abbrev-ref HEAD` re-checked in both repos after the commit (RESEARCH Pitfall 7, observed live 2026-07-30) |

**Package legitimacy:** not applicable. No `npm`, `pip`, `cargo` or PlatformIO `lib_deps` entry is
added. `<EEPROM.h>` is the AVR framework's bundled library, already used by the pre-refactor file. No
`T-126-SC` row is owed by this plan.
</threat_model>

<verification>
1. `include/rurp_config_storage.h` exists with exactly two `bool` declarations, includes before the `extern "C"` open, and a comment block carrying D-06, D-07, D-09 and the fire-proof section.
2. `src/rurp_config_utils.cpp` is policy-only: no EEPROM include, no `CONFIG_START`, no platform conditional, `rurp_validate_config` byte-identical.
3. `src/boards/rurp_config_storage_eeprom.cpp` is a pure move with the typed EEPROM call, `CONFIG_START 48`, the three-board guard before the includes, and both functions returning `true`.
4. `check_cmake_manifest.py` exits 0 at 24 enforced sources / 14 exempt / 6 allow-listed (the new backend added, `src/rurp_config_utils.cpp` deliberately retained); the two atomicity demonstrations are recorded; the docstring diff is text-only; the manifest-grouping correction and its duplicate-definition reason are recorded.
5. `pio test -e native` and `-e native_nodevtools` each report 141 cases / 141 succeeded / 17 suites; all three AVR builds exit 0 with the new object file present and no size figure claimed.
6. D-04 is discharged: the recorded test blob SHA re-hashes identical, the test is green, the resolved path count is 2, the observed access pair matches Plan 126-02's, and the `git diff --stat` is quoted in full as corroboration.
7. All seven config-API consumers are unchanged and absent from the commit; golden traces pass per-array.
8. Every must-not-touch blob SHA is re-asserted; `CONFIG_VERSION` still reads `VER06`.
9. All five edits are in one commit; both repos are still on their milestone branches.
10. No requirement checkbox was ticked.
</verification>

<success_criteria>
- CFG-03: `src/rurp_config_utils.cpp` is a common policy layer and a two-function byte-blob backend exists per platform, with the AVR one landed and the seam declared in a header included by exactly three translation units.
- CFG-04 (behaviour half): the AVR EEPROM path is a pure move, proven by a regression test that observed the pre-refactor behaviour and whose blob SHA re-hashes identical after the split.
- Nothing else moved: `include/rurp_shield.h`, `include/rurp_types.h`, `platformio.ini`, the linker script and both size-baseline files are all at their pre-phase blob SHAs, and both pinned native environments still report 141 cases across 17 suites.
- The manifest is legal at 24 enforced sources with 6 reasoned exclusions, the atomicity of the five-edit commit is demonstrated in both directions rather than asserted, and the deferral of the policy-TU promotion to the `config.cpp` deletion commit is recorded with the duplicate-definition ARM link it avoids.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{cpp,h}` | new files (HAL-free core) | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07) and
the `# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in
`firestarter/platform/py32f071/CMakeLists.txt` (this plan, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-03-SUMMARY.md`
when done. Record: the single commit SHA with its five changed paths; the seam header's declaration
count and include ordering as observed; the pre/post side-by-side of `rurp_validate_config` showing
zero differences; the policy TU's post-split include list; the backend's `CONFIG_START` value and
the typed-call form; the manifest gate's enforced count (24, unchanged), exempt count (14), and the
before/after allow-listed omission lists (5 → 6); the deliberate manifest-grouping correction with its
duplicate-definition reason; the two atomicity demonstrations with their exit
codes and violation messages; the docstring-only diff confirmation; both pinned native envs at
141/17 with the extended-timeout note; the three AVR build exit codes and object-file paths with an
explicit statement that no size figure is claimed; the includer census (two TUs today, named); every
must-not-touch blob SHA re-asserted; the `CONFIG_VERSION` line; the D-04 re-hash verdict with both
SHA strings; the regression test's function list, resolved-path count (2) and observed access pair
compared against Plan 126-02's; the full `git diff --stat` output labelled corroboration; the seven
config-API consumer sites verified by line; the golden-trace count; and the branch re-check in both
repos.
</output>
