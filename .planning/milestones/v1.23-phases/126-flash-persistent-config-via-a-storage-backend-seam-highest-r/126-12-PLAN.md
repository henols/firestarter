---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 12
type: execute
wave: 9
depends_on: ["126-11"]
files_modified:
  - .planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, CFG-06, CFG-07]

must_haves:
  truths:
    - "Every row in `126-NONREGRESSION.md` is **re-executed in this closing session** against the live trees, never transcribed from an earlier plan's SUMMARY — the discipline `124-NONREGRESSION.md` and `125-NONREGRESSION.md` both followed"
    - "The artifact follows the `123-` / `124-` / `125-NONREGRESSION.md` command / expected / observed row shape, so the four documents are comparable side by side"
    - "The nine cross-repo source-scanning gates (eleven rows, H1–H9b) are shown to have **RUN**, not skipped — this phase moved firmware files, which is exactly the condition the fail-open proxy was blind to (A-7 measured five legs flipping PASS→SKIP at exit 0 with a false \"firmware absent\" reason)"
    - "The skip census is asserted: no skip reason may mention firmware absence while `../firestarter/.git` exists, and the host suite's skipped count is recorded"
    - "Counts are asserted, never \"tests pass\": both pinned native envs at exactly **141 cases / 17 suites**, `native_pinmap_provisional` at 10/1, the manifest gate at **26** enforced sources with 15 exempt and 5 allow-listed, and the `pytest tests/` total with its per-module breakdown"
    - "All five ROADMAP success criteria are addressed row by row, including the two procedural ones: Criterion 2's commit ordering re-proven with `git rev-list --is-ancestor`, and Criterion 3's two-commit blob-SHA re-hash re-executed"
    - "Criterion 4's six distinctly named test functions are listed **by name** with individual results, plus D-05's seventh CRC anchor"
    - "Criterion 5's deletion is re-verified by **absence from the tree**, and Criterion 1's SUPERSEDED block is re-checked"
    - "All 19 decisions D-01…D-19 are accounted for in a coverage table with where each was implemented and how it was verified — including the three that were **amended** (D-08's four edits with their split grouping, D-10/D-11 refined by D-18, and D-16 superseded by C-2)"
    - "The claim-ceiling gate is run with its targets **named explicitly** — the Phase-125 C-16 self-reference trap means a compliance paragraph quoting the forbidden phrases trips the checker, and all six of Phase 125's SUMMARY files did exactly that"
    - "CFG-01…CFG-07 are ticked in `.planning/REQUIREMENTS.md` **only here**, each against a specific re-executed row rather than against a prior plan's claim"
  prohibitions:
    - statement: "Neither pinned native environment may move off **141 test cases / 17 suites** — the closing re-execution must observe `pio test -e native` and `pio test -e native_nodevtools` each reporting exactly those two numbers"
      status: required
      verification: "Both envs re-run **cold in this closing session**, each with an extended timeout of at least 540000 ms, with **both numbers asserted explicitly** — the case count and the suite count — never \"tests pass\", and never transcribed from an earlier plan's SUMMARY. `native_pinmap_provisional` is re-asserted alongside them at 10 cases / 1 suite. C-13 records why the counts structurally cannot move from this phase's edits; the closing plan **measures rather than infers**, because this is the milestone-wide invariant BASE-01 and MERGE-06 both cite and Phase 125's C-1 measured its collapse signature (141 cases / 141 succeeded → 17 suites / 0 succeeded). A moved count is a STOP finding, not a figure to re-baseline"
    - statement: "No row is transcribed from an earlier SUMMARY"
      status: required
      verification: "Every command in the table is re-run in this session and its observed value recorded from that run. `124-VERIFICATION.md` and `125-VERIFICATION.md` both set this standard; a transcribed row is not evidence"
    - statement: "No claim of CI coverage for the new pytest modules"
      status: required
      verification: "`pytest tests/` runs only in `build.yml` (push/PR to `main`) and `beta-build.yml` (push to `beta`); `py32f071.yml` has no pytest step. **Zero legs on this branch** (D-01). The artifact records the local run's verbatim output as the discharge and says so"
    - statement: "No ARM build, silicon, DFU-preservation or first-boot-timing claim"
      status: required
      verification: "ARM evidence is Plan 126-11's run URL plus head SHA, cited as such. D-14's first-boot cost is recorded as **not measured**. Config surviving a DFU install is the *intended* behaviour and an explicit non-claim. No PY32F071 PCB exists"
    - statement: "No `git push`, `gh workflow run`, tag, release, `beta` push or public comment"
      status: required
      verification: "Phase 130 owns the release decision; pushing `beta` auto-fires CI and cuts a new beta. This plan's outward-facing surface is zero"
    - statement: "`126-NONREGRESSION.md` does not reproduce the Validation Ceiling's forbidden-phrase list verbatim"
      status: required
      verification: "The Phase-125 C-16 self-reference trap: all six `125-0N-SUMMARY.md` files tripped the claim gate by quoting the forbidden phrases inside their own compliance paragraphs, while `125-NONREGRESSION.md` avoided it correctly. State the ceiling by reference"
    - statement: "`scripts/baseline/size_baseline_base01.json` is not touched"
      status: required
      verification: "`git hash-object` returns `b940c91655600a57ad7ef67cba723943af929daf` — Phase 124's frozen MERGE-05 reference, recorded immutable in `meta.supersedes`"
    - statement: "No firmware source file is modified by this plan"
      status: required
      verification: "`files_modified` names only meta-repo paths; `git status --porcelain` for `/workspaces/firestarter` is 0 lines throughout"
  artifacts:
    - .planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md
  key_links:
    - "This is the **only** plan permitted to tick CFG-01…CFG-07. Executors on this project marked multi-plan requirements Complete from a single plan four times in Phase 116, and every plan in this phase carries an explicit instruction not to. Each tick must cite the specific row in `126-NONREGRESSION.md` that was re-executed here."
    - "The nine-gate sweep is not a formality this phase. A-7 measured five host gate legs flipping PASS→SKIP at exit 0 with a false \"firmware absent\" reason after a single firmware rename — and this phase moved firmware files, deleted one and created five. Assert the gates **ran**; a green host suite with an elevated skip count is the failure signature."
    - "If Plan 126-11 recorded an unresolved ARM gate, this plan records it as an unresolved gate in the artifact and in the phase's carry-forward — it does **not** work around it, substitute a local build, or tick a requirement that depends on it. CFG-06's linker-symbol half is local and verifiable; the ARM link is not."
---

<objective>
Close the phase honestly: re-execute every gate and count against the live trees in this session, write
`126-NONREGRESSION.md` in the established row shape, account for all 19 decisions and all five success
criteria, run the claim gate with named targets, and tick CFG-01…CFG-07 — each against a row re-executed
here rather than against a prior plan's claim.

Purpose: this phase edited the one file compiled into all three AVR targets, moved and deleted firmware
files that host gates scan by source text, and introduced two ARM-only constructs no local tool can
build. Every one of those is a condition under which this project has previously shipped a green report
that was not true. The closing plan's whole value is that it re-runs rather than re-reads.

Output: `.planning/phases/126-…/126-NONREGRESSION.md`; CFG-01…CFG-07 ticked in
`.planning/REQUIREMENTS.md`; the ROADMAP's Phase 126 plan list and count finalised.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-VALIDATION.md

**Read every `126-0N-SUMMARY.md` and `126-1N-SUMMARY.md` in this phase directory** — but read them to
learn *what to re-execute*, not to copy results. Every observed value in `126-NONREGRESSION.md` must come
from a command run in this session.

**Repo layout.** Firmware is `/workspaces/firestarter` on `v1.23-py32f071-integration`; the host repo is
`/workspaces/firestarter_app` on the same branch name; meta is `/workspaces` on
`gsd/v1.23-py32f071-integration`. **Never write into `firestarter_py32_ci/` or `firestarter_app_py32/`**
(RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check `git rev-parse --abbrev-ref HEAD` in
all three (RESEARCH Pitfall 7).

**Claim ceiling.** No PY32F071 PCB exists. ARM evidence is Plan 126-11's run URL plus head SHA. State the
ceiling **by reference** to `.planning/REQUIREMENTS.md` §"Validation Ceiling" and do **not** reproduce
its forbidden-phrase list — that is the Phase-125 C-16 self-reference trap that tripped all six of that
phase's SUMMARY files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-execute every firmware row and the eleven cross-repo gate rows, in this session</name>
  <files>(no files modified — measurement and re-execution only)</files>
  <read_first>
    - `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md` — the row-shape template this phase
      reuses, and its handling of a deliberately-violated claim as a reasoned exception.
    - `.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` §3 — the **eleven-row**
      cross-repo table (H1–H9b) representing MERGE-07's **nine gates**, with the exact commands and the
      note that three gates contribute both a checker invocation row and a paired-pytest row. Also the
      hygiene rows H10–H16 and the ARM rows.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-09-SUMMARY.md` — the C-8
      distinction: the eleven-row table represents nine gates, and `tests/test_gen_validation_header.py`
      contributes paths to the larger scan-path inventory but is **absent** from that table. Do not
      conflate the two populations.
    - `.planning/phases/126-…/126-04-SUMMARY.md` — the AVR figures and the disposition arm taken, so
      this session's re-measurement has a comparison point.
    - `.planning/phases/126-…/126-11-SUMMARY.md` — the ARM run URL, head SHA and per-step outcomes, or
      the unresolved-gate record.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 8–22) — read it so the ceiling can be
      stated by reference rather than reproduced.
  </read_first>
  <action>
    Re-execute, in this session, every row below. Record the command, the expected value and the
    **observed** value for each. A transcribed row is not evidence.

    **Firmware repo (`/workspaces/firestarter`).**
      - `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, and `git status --porcelain` line count,
        with the repository named explicitly in the porcelain row.
      - `python3 -m pytest tests/ -q` — record the total **and** a per-module breakdown for the six
        modules this phase added or extended, so a silently uncollected module is visible.
      - `python3 -m pytest tests/test_config_storage_design_vendored.py -v`,
        `…eeprom_regression.py -v`, `…seam_shape.py -v`,
        `tests/test_flash_geometry_recorded_before_linker.py -v`, `tests/test_py32_flash_map.py -v`,
        `tests/test_config_storage_dualslot.py -v`, `tests/test_config_schema_pinned.py -v` — each with
        its full function list and individual results. **Criterion 4's six names plus the CRC anchor must
        appear individually.**
      - `pio test -e native`, `pio test -e native_nodevtools`, `pio test -e native_pinmap_provisional`,
        each cold and with an extended timeout of at least 540000 ms. Assert **counts**: 141 cases /
        141 succeeded / 17 suites for the first two, 10/10/1 for the third.
      - `pio run -t clean -e <env>` then `pio run -e <env>` for uno, uno328pb and leonardo — one
        uninterrupted invocation each — then `python3 scripts/check_size_baseline.py` in **both** modes,
        naming each: `compare_avr` (strict) against `scripts/baseline/size_baseline.json`, and
        `compare_avr_policy_merge05` (A-5 band) against `scripts/baseline/size_baseline_base01.json`.
        Record flash **and** RAM, used and total, per env, and compare against Plan 126-04's figures.
      - `python3 scripts/check_build_warnings.py --log <env>=<log>` for all three AVR logs and for the
        three native logs, with exit codes and verbatim lines.
      - `python3 scripts/check_cmake_manifest.py` — expect exit 0, **26** enforced sources, **15**
        exempt, **5** allow-listed omissions; record the PASS line verbatim.
      - `python3 scripts/check_orphan_provisional.py` and `python3 scripts/check_landing_range.py` —
        exit codes and lines.
      - `python3 -m pytest tests/test_golden_trace_identity.py -q` — golden register traces, compared
        **per-array** for `_shared/sdp_expected.h` since Phase 119.
      - `git hash-object` for `include/rurp_types.h`, `include/rurp_shield.h`, `platformio.ini`,
        `include/messages.h` and `scripts/baseline/size_baseline_base01.json` — all five must equal their
        pre-phase values (`d3fe5203…`, `602fe6f3…`, `f4e720ba…`, `dc7dbfc6…`, `b940c916…`).
      - `test ! -e platform/py32f071/src/config.cpp` — Criterion 5's absence proof.
      - `git rev-list --is-ancestor <CONFIG-STORAGE.md add commit> <linker tip commit>` — Criterion 2's
        ordering, re-derived here with both SHAs re-resolved in this session.
      - `git hash-object tests/test_config_storage_eeprom_regression.py` compared against the SHA Plan
        126-02 recorded — Criterion 3's blob re-hash, re-executed.
      - `grep -c hal_flash platform/py32f071/CMakeLists.txt` — C-3's entry present.

    **Host repo (`/workspaces/firestarter_app`) — the eleven-row MERGE-07 set.** Run from a directory
    literally named `firestarter_app` with the `firestarter` sibling present. Re-run all eleven rows
    H1–H9b using `124-NONREGRESSION.md` §3's commands, and record for each whether it **RAN** and its
    result. **Assert the gates ran, not merely that they passed.** Then:
      - the skip census: run the host suite with `-rs` and record the skipped count and every skip
        reason. **Fail the row if any skip reason mentions firmware absence while
        `../firestarter/.git` exists** — A-7 measured five legs flipping PASS→SKIP at exit 0 with exactly
        that false reason after a single firmware rename, and this phase moved, deleted and created
        firmware files;
      - `python3 -m pytest tests/` full total, compared against Phase 124's recorded 1158, with any
        difference recorded **with its cause**. Note the known environment artifact: with a live board
        attached on `/dev/ttyACM*`, the no-programmer-found characterisation tests go red — record the
        attached-device state so a red there is attributable rather than mysterious;
      - `python3 tools/check_no_exists_proxy.py`, `ruff check`, `ruff format --check` and
        `python tools/check_mypy_watermark.py` as the hygiene rows;
      - the host repo's `git status --porcelain`, recorded **by repository name**, with its five known
        pre-existing lines (` M .gitignore`, and untracked `.coverage`, `.planning/config.json`,
        `SECURITY.md`, `write_test_port.sh`) noted as pre-existing rather than as findings.
    Record the C-8 distinction explicitly: the eleven rows represent **nine gates**, because three gates
    contribute both a checker invocation and a paired-pytest row, and `tests/test_gen_validation_header.py`
    is outside this table though it is inside the larger scan-path inventory.

    **Meta repo.** `python3 -m pytest test_check_permitted_claims.py` in
    `.planning/phases/123-non-regression-baselines-gate-hardening/` — record the count.

    **ARM row.** Re-query Plan 126-11's run read-only with `gh run view` and record the id, URL, head
    SHA, event, conclusion and the Configure and Build step conclusions; re-derive
    `git -C /workspaces/firestarter rev-parse HEAD` in this session and compare string-equal. If Plan
    126-11 recorded an unresolved gate, record it here as unresolved — do not substitute anything.

    Do not modify any file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git rev-parse --abbrev-ref HEAD && python3 -m pytest tests/ -q 2>&1 | tail -2 && python3 scripts/check_cmake_manifest.py && python3 scripts/check_orphan_provisional.py && python3 -m pytest tests/test_golden_trace_identity.py -q 2>&1 | tail -2 && test ! -e platform/py32f071/src/config.cpp && echo "config.cpp ABSENT" && git hash-object include/rurp_types.h include/rurp_shield.h platformio.ini include/messages.h scripts/baseline/size_baseline_base01.json && echo "fw-porcelain=$(git status --porcelain | wc -l)"</automated>
  </verify>
  <acceptance_criteria>
    - Every row listed in the action is recorded with its command, expected value and **observed** value from a run performed in this session. No observed value is transcribed from an earlier SUMMARY, and the artifact says so.
    - The `pytest tests/` total is recorded with a per-module breakdown covering all seven modules this phase added or extended, so a silently uncollected module would be visible.
    - Criterion 4's six named functions plus D-05's CRC anchor are each listed by name with an individual result.
    - `pio test -e native` and `-e native_nodevtools` are each recorded at exactly 141 cases / 141 succeeded / 17 suites, and `native_pinmap_provisional` at 10/10/1, each cold with the extended-timeout discipline stated.
    - The three AVR envs are re-measured cold, with flash **and** RAM, used and total, per env, compared against Plan 126-04's figures; both comparators are named with their baseline files and their exit codes recorded.
    - The warning gate is recorded for all three AVR logs and all three native logs.
    - `check_cmake_manifest.py` exits 0 with 26 enforced / 15 exempt / 5 allow-listed, PASS line verbatim; `check_orphan_provisional.py` and `check_landing_range.py` exit codes recorded; golden traces pass with the per-array basis stated.
    - All five must-not-touch blob SHAs equal their pre-phase values, and a mismatch would be a STOP finding.
    - `platform/py32f071/src/config.cpp` is confirmed absent by path existence.
    - Criterion 2's `git rev-list --is-ancestor` is re-derived in this session with both SHAs re-resolved, and exits 0.
    - Criterion 3's blob re-hash is re-executed and compared against Plan 126-02's recorded SHA, with both strings recorded.
    - `grep -c hal_flash platform/py32f071/CMakeLists.txt` returns at least 1.
    - All eleven host rows H1–H9b are recorded as having **RUN**, each with its result, from a directory literally named `firestarter_app` with the `firestarter` sibling present.
    - The skip census is recorded: the host suite's skipped count under `-rs` and every skip reason. No skip reason mentions firmware absence while `../firestarter/.git` exists; if one does, it is recorded as a **failure** of that row.
    - The host suite total is recorded and compared against Phase 124's 1158, with any difference explained. The attached-device state on `/dev/ttyACM*` is recorded so a characterisation-test red is attributable.
    - The four hygiene rows are recorded, and the host repo's porcelain is recorded **by repository name** with its five known pre-existing lines noted as pre-existing.
    - The C-8 distinction is stated: eleven rows represent nine gates, and `tests/test_gen_validation_header.py` is outside this table.
    - The meta-repo claim-checker pytest count is recorded.
    - The ARM row is re-queried read-only with the run id, URL, head SHA, event, conclusion and the named Configure and Build conclusions, and the head SHA compared string-equal against a freshly re-derived firmware `HEAD` — or the unresolved gate is recorded with no substitute.
    - No file was modified; all three repositories are on their milestone branches.
    - No requirement checkbox was ticked in this task.
  </acceptance_criteria>
  <done>Every gate, count and figure this phase is judged against has been re-executed against the live trees in this session, the eleven cross-repo rows are shown to have run rather than skipped, and the two procedural criteria are re-proven rather than re-read.</done>
</task>

<task type="auto">
  <name>Task 2: Write 126-NONREGRESSION.md, with the decision-coverage and criteria tables and the claim gate</name>
  <files>.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/125-vpp-control-seam/125-NONREGRESSION.md` — the full document, for structure,
      section order and tone. This artifact is its sibling.
    - `.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` — the summary-claims section
      at the top, the baseline table, the gate table split by repository, and the handling of
      informational findings carried forward.
    - `.planning/phases/126-…/126-CONTEXT.md` §decisions — all nineteen, so the coverage table is
      complete and the three amendments are described accurately.
    - `.planning/ROADMAP.md` §"Phase 126" lines 2197–2212 — the five success criteria, verbatim.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` — read its
      `resolve_targets(argv)` handling so the invocation names its targets explicitly.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" — read it; state the ceiling **by reference**.
  </read_first>
  <action>
    Write `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md`
    from Task 1's observations, in `125-NONREGRESSION.md`'s structure. Sections:

    **1. Summary claims.** A short numbered list of what this phase establishes, each pointing at the row
    that establishes it. Include, explicitly, the claims this phase does **not** make: no ARM silicon
    claim; config surviving a real DFU install is the *intended* behaviour and unverifiable without a
    board; D-14's first-boot flash-write cost is **not measured**; the new pytest modules run in **zero
    CI legs on this branch** and are discharged by the local run recorded here.

    **2. The baseline, as recorded and as re-verified.** The AVR flash/RAM table (recorded vs observed,
    all three envs, used and total), the native counts table (recorded vs observed, all three envs,
    cases and suites), and the warning counts. State that all figures come from fresh cold builds in this
    session.

    **3. The gate table — command, expected, observed.** Split by repository: firmware rows, host rows
    (the eleven-row MERGE-07 set plus the hygiene rows), meta row, ARM row. Reproduce the C-8 note that
    the eleven rows represent nine gates. Every observed value from Task 1.

    **4. Success criteria, one subsection each.** For each of the five ROADMAP criteria, quote it, then
    give the rows that discharge it:
      - **Criterion 1** — the vendored design: blob `4b1a441` cited by SHA, PRs #46/#47 named, the
        SUPERSEDED block covering all seven module names, and the gate that enforces it.
      - **Criterion 2** — the commit-ordering constraint: both SHAs and the `git rev-list --is-ancestor`
        exit code re-derived in this session, plus the non-vacuity leg's after-the-record commit count.
      - **Criterion 3** — the split plus the two-commit proof: the recorded test blob SHA re-hashed
        identical, the test still green, the path-scoped `git diff --stat` quoted in full and labelled
        corroboration only, with `124-VERIFICATION.md`'s surviving-trailer finding cited.
      - **Criterion 4** — the six distinctly named functions listed **by name** with individual results,
        plus D-05's seventh CRC anchor, plus a statement that no aggregate pass/fail stands in for any of
        them.
      - **Criterion 5** — the reserved map with its addresses, the two slots in different page erase
        units, the four `PROVIDE`d symbols, the host `FLASH_BASE`/`FLASH_SIZE` consistency recorded as an
        asymmetry that is correct rather than drift, `rurp_configuration_t` and `CONFIG_VERSION`
        unchanged, and `config.cpp` verified absent from the tree.

    **5. Decision coverage — all nineteen.** A table with one row per decision D-01…D-19: the decision in
    one line, where it was implemented (plan and file), and how it was verified (the row or test that
    proves it). Three rows need extra care and must say so:
      - **D-08** — four manifest edits, deliberately split across two commits so the ARM link never had
        two definitions of the four public config functions. Record the grouping correction.
      - **D-10 / D-11 / D-18** — D-18 refines D-10's shrink quantum to one whole 8 KiB sector; D-10's
        top-of-flash placement, the shrunk `LENGTH`, the second `MEMORY` region and the `PROVIDE`d
        symbols are untouched.
      - **D-16** — superseded by RESEARCH C-2. Its literal wording ("program the header/CRC word LAST")
        is **not implementable** on this part; record the reference-manual citation and the corrected
        shape actually implemented. State plainly that the phase implemented the amendment, not the
        literal decision.
    Also record **D-19** and **D-18** as the two decisions locked from a research escalation after the
    main discuss pass, and confirm each was implemented as locked.

    **6. Informational findings carried forward.** Anything observed that is true but non-blocking —
    including, if applicable, an unresolved ARM gate from Plan 126-11, the 7680 B of deliberate flash
    slack in the reserved sector, the C-9 note that the host's 2048-byte fallback erase grid matches
    neither the page nor the sector (Phase 127's to act on if it ever fires), the deferred
    `FUT-ARMSIZE` item, and the correction that `126-CONTEXT.md`'s "2600 B" leonardo headroom is stale
    against the live 2656 B.

    **7. Claim ceiling.** State by **reference** to `.planning/REQUIREMENTS.md` §"Validation Ceiling".
    **Do not reproduce its forbidden-phrase list** — all six of Phase 125's SUMMARY files tripped the
    claim checker by quoting those phrases inside their own compliance paragraphs (125-RESEARCH C-16),
    while `125-NONREGRESSION.md` avoided it correctly.

    Then run the claim gate with its targets **named explicitly**:
    `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py`
    against `126-NONREGRESSION.md`, `firestarter/platform/py32f071/CONFIG-STORAGE.md` and every
    `126-*-SUMMARY.md` in this phase directory. Record the invocation verbatim and the exit code. If a
    SUMMARY trips it on the self-reference trap, record that as an informational finding in the same
    shape Phase 125 did — the artifact itself must be clean.

    Commit the artifact in the meta repo with a message of the form
    `docs(126-12): record the phase 126 non-regression evidence, decision coverage and criteria`, then
    re-check `git rev-parse --abbrev-ref HEAD` in all three repositories.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md && grep -c 'D-19\|D-18\|D-16' .planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md && python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-NONREGRESSION.md; echo "claimgate=$?" && git rev-parse --abbrev-ref HEAD && git -C firestarter rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `126-NONREGRESSION.md` exists in the phase directory with all seven sections in the order given, following `125-NONREGRESSION.md`'s command / expected / observed row shape.
    - Section 1 lists both the claims established and the explicit non-claims: no ARM silicon claim, DFU preservation as intended-but-unverifiable, D-14's first-boot cost as **not measured**, and zero CI legs for the new pytest modules on this branch.
    - Section 2 gives recorded-versus-observed AVR flash and RAM for all three envs (used and total) and native cases and suites for all three envs, all from fresh cold builds in this session.
    - Section 3's gate table covers the firmware rows, the eleven host rows H1–H9b with each shown to have **RUN**, the hygiene rows, the meta row and the ARM row — with the C-8 note that eleven rows represent nine gates.
    - Section 4 has one subsection per ROADMAP criterion, each quoting the criterion and citing the rows that discharge it. Criterion 2 carries both SHAs and the ancestry exit code re-derived in this session. Criterion 3 carries both blob SHA strings and the full `git diff --stat` output labelled corroboration only. Criterion 4 lists the six function names individually plus the CRC anchor and states that no aggregate stands in for any of them. Criterion 5 carries the reserved addresses, the four symbols, the host-contract asymmetry marked correct-not-drift, the unchanged schema and version, and the absence proof.
    - Section 5's decision table has **all nineteen** rows D-01…D-19, each with where it was implemented and how it was verified. D-08's split grouping, D-18's refinement of D-10/D-11 and D-16's supersession by C-2 are each described as amendments, with D-16's reference-manual citation present and an explicit statement that the amendment — not the literal decision — was implemented. D-18 and D-19 are noted as the two escalation-locked decisions and each confirmed implemented as locked.
    - Section 6 records the informational findings, including any unresolved ARM gate, the 7680 B slack, the C-9 fallback-grid note for Phase 127, `FUT-ARMSIZE`, and the stale 2600 B correction.
    - Section 7 states the ceiling **by reference** and does not reproduce the forbidden-phrase list.
    - The claim gate is run with all targets named explicitly — the artifact, `CONFIG-STORAGE.md`, and every `126-*-SUMMARY.md` — with the invocation recorded verbatim and the exit codes recorded. The artifact itself exits 0; any SUMMARY that trips the self-reference trap is recorded as an informational finding.
    - The artifact is committed in the meta repo, `git show --stat` lists exactly one path, and all three repositories are still on their milestone branches.
    - `git status --porcelain` for `/workspaces/firestarter` is 0 lines.
    - No requirement checkbox was ticked in this task.
  </acceptance_criteria>
  <done>`126-NONREGRESSION.md` records every gate, count and figure as re-executed in this session, discharges all five success criteria row by row including the two procedural ones, accounts for all nineteen decisions with the three amendments described as amendments, and passes the claim gate with its targets named explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Tick CFG-01…CFG-07 against re-executed rows, and finalise the ROADMAP entry</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 62–68 — CFG-01…CFG-07 verbatim, and line 164's coverage row
      `CFG-01 … CFG-07 | Phase 126 | Pending`.
    - `.planning/ROADMAP.md` §"Phase 126" lines 2197–2212 — the `**Plans**: TBD` line and the absent plan
      list, both of which this task finalises.
    - `.planning/phases/126-…/126-NONREGRESSION.md` — the artifact just written; every tick cites a row
      in it.
    - `.planning/phases/125-vpp-control-seam/125-06-SUMMARY.md` — how the previous phase's closing plan
      worded its ticks and its ROADMAP finalisation, for consistency.
  </read_first>
  <action>
    **Tick CFG-01…CFG-07 in `.planning/REQUIREMENTS.md`.** This is the only plan permitted to do so.
    For each of the seven, cite in the SUMMARY the **specific row in `126-NONREGRESSION.md`** that
    discharges it — a row re-executed in this session, never a prior plan's claim. Executors on this
    project marked multi-plan requirements Complete from a single plan four times in Phase 116, and every
    other plan in this phase carries an explicit instruction not to tick; this is where that instruction
    is paid off.

    Before ticking, confirm each requirement is genuinely discharged, and **read the requirement prose
    rather than trusting a plan's ticking scope**:
      - **CFG-01** — the vendored design with the closed-PR origin cited and the superseded parts marked.
      - **CFG-02** — the page/erase-unit size read from the reference manual and recorded **before** the
        linker edit, proven by ancestry.
      - **CFG-03** — the split by concern, policy common, a two-function byte-blob backend per platform.
      - **CFG-04** — the AVR pure move with the offset-48 / `sizeof(rurp_configuration_t)` assertion and
        byte-identical behaviour, plus the measured AVR delta.
      - **CFG-05** — the dual-slot CRC32 backend covered by the six named cases.
      - **CFG-06** — two config pages in different erase units, exposed as linker symbols, with the host's
        `FLASH_BASE`/`FLASH_SIZE` kept consistent.
      - **CFG-07** — the schema and `CONFIG_VERSION` unchanged, and PR #48's `config.cpp` deleted.
    **If Plan 126-11 recorded an unresolved ARM gate**, examine whether it blocks any of the seven. The
    linker-symbol half of CFG-06 and the algorithm half of CFG-05 are locally verifiable and are not
    blocked by it; if anything genuinely is, record that requirement as **not ticked** with the reason,
    rather than ticking it against evidence that does not exist.

    Update `.planning/REQUIREMENTS.md`'s coverage row for `CFG-01 … CFG-07` from `Pending` to the
    completed state, matching the file's existing convention for a finished category.

    **Finalise `.planning/ROADMAP.md`'s Phase 126 entry**, using scoped `Edit` calls only — never a
    whole-file `Write`, which would destroy every phase entry outside the diff window:
      - replace `**Plans**: TBD` with the final count;
      - add the plan list in the wave-grouped shape Phase 125's entry uses, one line per plan with a
        one-line objective, with each checkbox ticked;
      - add a short note recording the three decision amendments (D-08's split grouping, D-18's
        refinement of D-10/D-11, and D-16's supersession by RESEARCH C-2 with its reference-manual
        reason) so the ROADMAP reader is not misled by the criteria's original wording, in the same shape
        Phase 125's entry uses for its two falsified mechanisms.

    Then update `.planning/STATE.md`'s position and last-activity lines for the phase close. Note the
    known tooling gap: `gsd state.planned-phase` under-writes STATE.md — the frontmatter `status`,
    `stopped_at` and `progress.total_plans` fields stay stale — so hand-correct them and record that they
    were hand-corrected.

    Commit the three meta-repo files with a message of the form
    `docs(126-12): tick CFG-01..CFG-07 and finalise the phase 126 roadmap entry`, then re-check
    `git rev-parse --abbrev-ref HEAD` in all three repositories — `gsd-tools query commit` has been
    observed moving HEAD to another branch, and an unanchored ROADMAP-prose regex is the root cause, so a
    ROADMAP edit is exactly when to re-check.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n 'CFG-0' .planning/REQUIREMENTS.md | head -12 && grep -n 'Phase 126' .planning/ROADMAP.md | head -5 && sed -n '/### Phase 126/,/### Phase 127/p' .planning/ROADMAP.md | grep -n 'Plans\|126-' | head -20 && git status --porcelain | head -10 && git rev-parse --abbrev-ref HEAD && git -C firestarter rev-parse --abbrev-ref HEAD && git -C firestarter_app rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - All seven of CFG-01…CFG-07 are ticked in `.planning/REQUIREMENTS.md`, and the SUMMARY cites for each one the specific `126-NONREGRESSION.md` row — re-executed in this session — that discharges it.
    - Each requirement's prose was read before ticking, and the SUMMARY states for each what was actually delivered against what the requirement asks.
    - If Plan 126-11 recorded an unresolved ARM gate, its effect on each of the seven is assessed explicitly, and any requirement genuinely blocked by it is left **unticked** with the reason recorded rather than ticked against absent evidence.
    - The `CFG-01 … CFG-07 | Phase 126` coverage row is updated from `Pending` to the completed state, matching the file's existing convention.
    - `.planning/ROADMAP.md`'s Phase 126 entry has `**Plans**: TBD` replaced with the final count, and carries a wave-grouped plan list with one ticked line per plan and a one-line objective each.
    - The ROADMAP entry carries a note recording the three decision amendments — D-08's split grouping, D-18's refinement of D-10/D-11, and D-16's supersession by RESEARCH C-2 with its reference-manual reason — in the same shape Phase 125's entry uses.
    - Every ROADMAP change was made with scoped `Edit` calls; no whole-file `Write` was used on `ROADMAP.md` or `REQUIREMENTS.md`, and the diff touches only the Phase 126 section and the CFG coverage row.
    - `.planning/STATE.md` is updated for the phase close, with the frontmatter `status`, `stopped_at` and `progress.total_plans` fields hand-corrected and that hand-correction recorded.
    - The three meta-repo files are committed together; `git show --stat` lists exactly those paths.
    - `git rev-parse --abbrev-ref HEAD` is re-checked after the commit and reports `gsd/v1.23-py32f071-integration` in meta and `v1.23-py32f071-integration` in both sub-repos.
    - `git status --porcelain` for `/workspaces/firestarter` is 0 lines; the host repo's five known pre-existing lines are unchanged and noted as pre-existing.
    - No `git push`, `gh workflow run`, tag, release or public comment was performed.
  </acceptance_criteria>
  <done>All seven CFG requirements are ticked against rows re-executed in this closing session, the ROADMAP's Phase 126 entry is finalised with its plan list and a note recording the three decision amendments, and STATE.md is hand-corrected past the known tooling gap — with every repository verified still on its milestone branch afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an earlier SUMMARY's claim → a closing artifact's observation | The transcription boundary; this project's closing plans re-execute rather than re-read |
| a firmware file move → a host gate's scan target | Six host modules key "repo absent" on a single file's existence, and this phase moved, deleted and created firmware files |
| a plan's ticking scope → a requirement's prose | A requirement can be ticked against a plan's narrower reading of it |
| a ROADMAP edit → the branch HEAD | An unanchored ROADMAP-prose regex has been observed moving HEAD to another branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-12-01 | **Repudiation** | a closing artifact assembled from prior claims | **high** | mitigate | Every row re-executed in this session, and the artifact states it. `124-VERIFICATION.md` and `125-VERIFICATION.md` both set this standard |
| T-126-12-02 | **Denial of service** | a cross-repo gate silently skipping after a firmware file moved | **high** | mitigate | All eleven rows shown to have **RUN**, plus a skip census that fails on any skip reason mentioning firmware absence while `../firestarter/.git` exists. A-7 measured five legs flipping PASS→SKIP at exit 0 with exactly that false reason |
| T-126-12-03 | **Repudiation** | a requirement ticked beyond what it asks | **high** | mitigate | Each requirement's prose is read before ticking and each tick cites a specific re-executed row. Only this plan may tick; every other plan in the phase carries the counter-instruction (the Phase-116 4× guard) |
| T-126-12-04 | Repudiation | an unresolved ARM gate ticked away | high | mitigate | If Plan 126-11 recorded one, its effect on each requirement is assessed explicitly and anything genuinely blocked is left unticked with the reason recorded — no local substitute is permitted |
| T-126-12-05 | Repudiation | a green report resting on "tests pass" | medium | mitigate | Counts asserted throughout: 141/17 on both pinned native envs, 26 enforced sources, per-module pytest breakdown, the six criterion-4 functions listed by name |
| T-126-12-06 | Repudiation | the claim gate tripped by the artifact's own compliance text | medium | mitigate | The ceiling is stated by reference; the gate is run with targets named explicitly; a SUMMARY tripping the self-reference trap is recorded as informational, the shape Phase 125 used |
| T-126-12-07 | **Tampering** | a whole-file `Write` on `ROADMAP.md` destroying other phases | **high** | mitigate | Scoped `Edit` calls only, with the diff confined to the Phase 126 section and the CFG coverage row, and the constraint stated in the action and the acceptance criteria |
| T-126-12-08 | Spoofing | a commit landing on the wrong branch after a ROADMAP edit | high | mitigate | `git rev-parse --abbrev-ref HEAD` re-checked in all three repositories after the commit; the unanchored ROADMAP-prose regex is the documented root cause, so a ROADMAP edit is precisely when to check |
| T-126-12-09 | Repudiation | STATE.md left stale by a known tooling gap | medium | mitigate | The frontmatter `status`, `stopped_at` and `progress.total_plans` fields are hand-corrected and the hand-correction is recorded |
| T-126-12-10 | Elevation of privilege | an unintended release | high | mitigate | No `git push`, `gh workflow run`, tag, release or public comment. Pushing `beta` auto-fires CI and cuts a new beta; Phase 130 owns the release decision |

**Package legitimacy:** not applicable. This plan installs nothing and adds no dependency. No
`T-126-SC` row is owed.
</threat_model>

<verification>
1. Every firmware, host, meta and ARM row re-executed in this session with command, expected and observed recorded; nothing transcribed.
2. Both pinned native envs at 141 cases / 17 suites; `native_pinmap_provisional` at 10/1; the manifest gate at 26 enforced / 15 exempt / 5 allow-listed; the `pytest tests/` total with a per-module breakdown.
3. Three cold AVR builds with flash and RAM per env, both comparators named with their baseline files.
4. All eleven host rows shown to have RUN, the skip census clean, the host total compared against 1158 with any difference explained, and the attached-device state recorded.
5. `126-NONREGRESSION.md` written with all seven sections, discharging all five criteria row by row including the two procedural ones.
6. The decision table covers all nineteen ids, with D-08, D-18 and D-16's amendments described as amendments and D-16's supersession carrying its reference-manual citation.
7. The claim gate run with targets named explicitly; the artifact exits 0; any SUMMARY self-reference trip recorded as informational.
8. CFG-01…CFG-07 ticked against specific re-executed rows, with any ARM-blocked requirement left unticked and explained.
9. The ROADMAP entry finalised with scoped edits only, carrying the plan list and the amendment note; STATE.md hand-corrected.
10. All three repositories on their milestone branches; firmware porcelain 0 lines; no outward-facing action performed.
</verification>

<success_criteria>
- CFG-01…CFG-07 are each ticked against a row re-executed in this closing session, with any requirement genuinely blocked by an unresolved ARM gate left unticked and explained rather than ticked against absent evidence.
- `126-NONREGRESSION.md` is a re-execution record, not a summary of summaries, and it discharges all five ROADMAP criteria — including the two that constrain commit ordering and proof method — row by row.
- All nineteen decisions are accounted for, with the three that were amended described as amendments rather than silently followed, so the next reader is not misled by the original wording.
- The nine cross-repo gates are shown to have run, and the phase's non-claims — silicon, DFU preservation, first-boot timing, CI coverage for the new modules — are stated as plainly as its claims.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions (or the A-7 fallback symbol pair) | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{h,cpp}` | new files (HAL-free core) | Plan 126-07 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `test_manifest_names_the_flash_driver` and five sibling functions | new test functions in `tests/test_py32_flash_map.py` | Plan 126-08 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07) and the
`# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in `firestarter/platform/py32f071/CMakeLists.txt`
(Plan 126-08, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-12-SUMMARY.md`
when done. Record: every re-executed row with its command, expected and observed value, grouped by
repository; the per-module `pytest tests/` breakdown; all three native env counts and the three AVR
flash/RAM figures with both comparators named; the eleven host rows shown to have RUN plus the skip
census and the host suite total against 1158 with any difference explained and the attached-device state;
the meta claim-checker count; the ARM row re-queried or its unresolved-gate record; the five criteria
each with the rows that discharge it, including both procedural ones re-proven; the nineteen-row
decision-coverage table with the three amendments described as amendments; the claim-gate invocation
verbatim with its targets and exit codes; the seven requirement ticks each citing its row, and any
requirement deliberately left unticked with the reason; the ROADMAP and STATE.md edits with the
hand-correction noted; the commit SHAs with their changed paths; and the branch re-check in all three
repositories.
</output>
