---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 08
type: execute
wave: 6
depends_on: ["126-07"]
files_modified:
  - firestarter/platform/py32f071/src/config_storage_flash.cpp
  - firestarter/platform/py32f071/src/config.cpp
  - firestarter/platform/py32f071/CMakeLists.txt
  - firestarter/scripts/check_cmake_manifest.py
  - firestarter/tests/test_py32_flash_map.py
autonomous: true
requirements: [CFG-05, CFG-06, CFG-07]

must_haves:
  truths:
    - "CFG-05 / D-02: `platform/py32f071/src/config_storage_flash.cpp` supplies the three real flash primitives and implements the two seam functions on top of the HAL-free core, so the ARM target and the host suite exercise the same algorithm from the same source file"
    - "C-4: the primitives call **only** `HAL_FLASH_Unlock` / `HAL_FLASH_Erase` / `HAL_FLASH_Program` / `HAL_FLASH_Lock`. The §4.2.3.6 flash timing registers are configured **inside** those entry points (`py32f071_hal_flash.c:416` and `:605`), and RM V0.2 §4.2.3.6 states the operation *\"will fail\"* without them — so a hand-rolled `FLASH->CR` sequence would compile, review cleanly and fail on silicon nobody can debug"
    - "V4: a HAL return other than `HAL_OK` yields `save() == false`. RM V0.2 §4.5.3 / §4.2.3.3: if the flash write-protection option byte covers the config pages, a page erase is **silently skipped** and `WRPERR` is set — success must never be reported for an operation that did not happen"
    - "C-2: `HAL_FLASH_Program` is handed a full 64-word staging buffer built by the core, never a pointer to the record — it reads 64 words regardless of the caller's object size"
    - "CFG-06 / D-11: slot addresses come from the linker symbols `__config_slot_a_start`, `__config_slot_b_start` and `__config_page_size`, declared `extern` here — the linker is the single source of the flash map, not a duplicated constant"
    - "CFG-07: `platform/py32f071/src/config.cpp` is **deleted**, verified by its absence from the tree. Its four drift points are recorded before deletion: a private static instead of the shared `rurp_config` global, a second drifted `rurp_validate_config` with a `memset` and an extra `r2 == 0` condition the AVR version does not have, **no write-back call at all**, and a `rurp_save_config()` that assigns to a static and **persists nothing**"
    - "D-08's remaining three manifest edits land in the **same commit as the deletion**: the `# PY32_EXCLUDED: src/rurp_config_utils.cpp` line is retired, `\"${REPOSITORY_ROOT}/src/rurp_config_utils.cpp\"` joins `FIRESTARTER_COMMON_SOURCES`, and C-3's `py32f071_hal_flash.c` joins `PY32_SDK_SOURCES` — so the ARM target never has two definitions of the four public config functions"
    - "C-3: `py32f071_hal_flash.c` is genuinely absent from the ARM build today (`grep -c hal_flash platform/py32f071/CMakeLists.txt` returns 0, confirmed independently). `py32f071_hal_conf.h:10` already enables the module and `:53` already includes the header, so everything **compiles** and the first `HAL_FLASH_` call fails at **link**. `check_cmake_manifest.py` is structurally exempt from checking `PY32_SDK_SOURCES`, so nothing in this repository detects it — `tests/test_py32_flash_map.py::test_manifest_names_the_flash_driver` converts that CI-only failure into a local one"
    - "The manifest lands at **26** enforced sources (18 common + 8 platform), **15** exempt SDK entries and **5** allow-listed omissions"
    - "D-14: with `config.cpp` gone, py32 uses the one common policy — `rurp_validate_config`'s write-back fires on a virgin part exactly as it does on AVR. The first-boot flash erase+program cost is recorded as **not measured**, never as acceptable"
  prohibitions:
    - statement: "No raw flash register access anywhere"
      status: required
      verification: "The glue TU names only the four `HAL_FLASH_*` entry points. A `FLASH->CR` or `FLASH->KEYR` sequence would skip the timing-register configuration the reference manual requires and fail only on silicon (C-4)"
    - statement: "`config.cpp`'s policy is deleted, not reconciled"
      status: required
      verification: "CFG-07 and D-07: all four public functions stay in the common policy layer. A merged or partially-kept `config.cpp` would leave two validate policies, which is the drift CFG-07 exists to remove"
    - statement: "The deletion and the three manifest edits are ONE commit"
      status: required
      verification: "`PY32_PLATFORM_SOURCES` is an ENFORCED list, so a dangling `src/config.cpp` entry is exit 1; and promoting `src/rurp_config_utils.cpp` before the deletion would give the ARM link two definitions of each of the four public config functions — undetectable in this environment. There is no green intermediate state in either direction"
    - statement: "`rurp_configuration_t`, `CONFIG_VERSION \"VER06\"` and `include/rurp_shield.h` are unchanged"
      status: required
      verification: "`git hash-object include/rurp_types.h` returns `d3fe5203a91527bdb7b20a33843c81065e21c613`; `include/rurp_shield.h` returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`; the `CONFIG_VERSION` line still reads the literal `VER06` (CFG-07, D-09)"
    - statement: "No ARM build, link or silicon claim is made"
      status: required
      verification: "`arm-none-eabi-gcc`, `cmake` and `ninja` are absent here, so C-3's fix can only be **proven** by the gated CI run in Plan 126-11. This plan lands the local detector and says exactly that"
    - statement: "`platformio.ini` and the linker script are NOT modified"
      status: required
      verification: "`git hash-object platformio.ini` returns `f4e720ba75a8c618cc23bac045ab65084d41a0a4`; the linker script's blob SHA is the one Plan 126-06 recorded after its edit"
    - statement: "No new `scripts/check_*.py` is created"
      status: required
      verification: "`ls scripts/check_*.py | wc -l` is still 5; only the existing checker's docstring changes, and `tests/test_checker_convention.py` is unmodified"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 126-12 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` unchanged"
  artifacts:
    - firestarter/platform/py32f071/src/config_storage_flash.cpp
  key_links:
    - "C-3 is the one defect in this phase that **no local tooling can catch**. `check_cmake_manifest.py`'s own docstring states `PY32_SDK_SOURCES` is structurally exempt from resolution — a property of FetchContent — and its reverse check walks only `src/`. `cmake`, `ninja` and `arm-none-eabi-gcc` are all absent. Without `test_manifest_names_the_flash_driver`, the first sign of a missing flash driver would be `undefined reference to HAL_FLASH_Unlock` in Plan 126-11's gated CI run, hours after the code was written."
    - "This commit closes the only window in the phase where the ARM target could have had duplicate symbol definitions. Plan 126-03 deliberately deferred the policy-TU promotion here for exactly that reason; the SUMMARY must record that the window was never opened."
    - "`platform/py32f071/src/config.cpp` must be read verbatim and its four drift points recorded **before** it is deleted, because after deletion the evidence that CFG-07 removed real drift — rather than an equivalent implementation — exists only in this SUMMARY."
---

<objective>
Land the PY32 flash primitives on top of the HAL-free core, delete PR #48's drifted `config.cpp`, and
close the manifest: retire the policy-TU exclusion, promote that file into the ARM build, and add the
flash driver C-3 found missing — all in one commit, followed by the local detector that turns C-3's
CI-only link failure into a `pytest` failure here.

Purpose: CFG-05 needs real primitives behind the injected table; CFG-07 needs `config.cpp` — whose
`rurp_save_config()` persists nothing — deleted rather than reconciled; and C-3 found that
`py32f071_hal_flash.c` is not in the ARM source list at all, so the first `HAL_FLASH_` call would fail
at link in a workflow run nobody can trigger locally. All of it belongs in one commit because the
manifest's forward check makes a dangling entry exit 1, and because promoting the policy translation
unit any earlier would give the ARM link two definitions of each of the four public config functions.

Output: `platform/py32f071/src/config_storage_flash.cpp`; `platform/py32f071/src/config.cpp` deleted;
`platform/py32f071/CMakeLists.txt` at 26 enforced sources, 15 exempt entries and 5 exclusions; the
checker docstring updated; and `test_manifest_names_the_flash_driver` added to
`tests/test_py32_flash_map.py`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-06-SUMMARY.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-07-SUMMARY.md

**Read order and precedence.** `126-CONTEXT.md` records D-01…D-19; `126-RESEARCH.md` **wins on verified
facts**. The three that shape this plan are **C-3** (the missing flash driver — D-08's "three edits" is
four, and the fourth is CI-detectable only), **C-4** (route everything through the HAL because the
timing registers live inside its entry points) and **C-2** (one whole-page program primitive; hand it a
full 64-word buffer). `126-PATTERNS.md` §`platform/py32f071/src/config_storage_flash.cpp` gives the
exact analog (`timing.cpp`) and the platform-guard `#error` pattern that belongs **here** and not in the
core.

**Repo layout.** Every path lives in the firmware submodule `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commits land inside that submodule; only the SUMMARY commits in the meta
repo. `firestarter_app` is out of scope (D-12). **Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`** (RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check
`git rev-parse --abbrev-ref HEAD` in both repos (RESEARCH Pitfall 7).

**Requirement ticking scope for this plan: NONE.** CFG-05 spans Plans 126-07, this one and 126-09;
CFG-06 spans Plan 126-06 and this one; CFG-07 spans Plan 126-03, this one and Plan 126-10's gate. Do not
tick CFG-01…CFG-07. Plan 126-12 is the only plan permitted to tick any of them.

**Claim ceiling.** No PY32F071 PCB exists, and `arm-none-eabi-gcc`, `cmake` and `ninja` are absent — so
this plan **cannot prove the ARM target links**, which is precisely the failure mode C-3 describes.
Land the local detector, state plainly that the authoritative proof is Plan 126-11's gated CI run, and
make no claim about silicon behaviour, first-boot timing or DFU preservation. State the ceiling by
reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The HAL glue, the config.cpp deletion and the manifest close — ONE commit</name>
  <files>firestarter/platform/py32f071/src/config_storage_flash.cpp, firestarter/platform/py32f071/src/config.cpp, firestarter/platform/py32f071/CMakeLists.txt, firestarter/scripts/check_cmake_manifest.py</files>
  <read_first>
    - `firestarter/platform/py32f071/src/config.cpp` — **read all 47 lines verbatim before deleting
      it.** The four drift points, with their verified line numbers: a private `configuration` static in
      an unnamed namespace instead of the shared `rurp_config` global (`:5-7`); a second, drifted
      `rurp_validate_config` at `:15-30` whose condition adds `|| value->r2 == 0` and whose body starts
      with a `memset` — neither of which the AVR version has; **no `rurp_save_config()` write-back call
      at all**, where `src/rurp_config_utils.cpp:38` has one; and `rurp_save_config` at `:38-47`, which
      validates then assigns to the static and **persists nothing**. Record all four in the SUMMARY
      before deleting, because afterwards this record is the only evidence CFG-07 removed real drift
      rather than an equivalent implementation.
    - `firestarter/platform/py32f071/src/config_storage_dualslot.h` — the primitive-table contract and
      the two core entry points this glue wires up.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §"Code Examples" → "The HAL primitive layer (C-2, C-4, C-8)" (lines 1143–1188) — the erase,
      program and read primitives with their exact HAL calls and the comments explaining why. Treat it
      as the specification; it cannot be compiled here.
    - Same file, §C-3 in full (the four manifest edits, the exact `PY32_SDK_SOURCES` path to add, and
      why the checker cannot catch the omission), §C-4 in full (including the already-satisfied HSI
      prerequisite at `platform/py32f071/src/main.cpp:25-27` and the warning that a future clock
      refactor turning HSI off would silently break config persistence), and §"Pitfall 3".
    - Same file, §"Security Domain" → the V4 write-protection row: RM §4.5.3 / §4.2.3.3, a page erase
      silently skipped with `WRPERR` set, and the requirement that a non-`HAL_OK` return yields
      `save() == false`.
    - `firestarter/platform/py32f071/src/timing.cpp` lines 1–20 — the analog: a single board-header
      include pulls the whole HAL in, file-local state lives in an unnamed `namespace { }`, `extern "C"`
      on definitions, Allman braces.
    - `firestarter/platform/py32f071/src/py32f071_rurp_shield.cpp` lines 1–10 — the platform-guard
      `#error` pattern (`#if !defined(RURP_PLATFORM_PY32F071)`). Copy it into the glue TU; it must
      **never** go into the core, which the host harness compiles.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the `PY32_EXCLUDED` block (now 6 lines), the
      `FIRESTARTER_COMMON_SOURCES` `set()` (17 entries, `${REPOSITORY_ROOT}`-rooted and quoted), the
      `PY32_PLATFORM_SOURCES` `set()` (8 entries after Plan 126-07, bare relative paths) and the
      `PY32_SDK_SOURCES` `set()` (14 entries, `${PY32_SDK_ROOT}`-rooted and quoted). **Three
      incompatible path idioms; do not mix them.**
    - `firestarter/scripts/check_cmake_manifest.py` — the docstring's exclusion enumeration (updated by
      Plan 126-03 to six lines), and the `EXEMPT_LISTS` comment explaining why `PY32_SDK_SOURCES` is
      structurally exempt.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-06-SUMMARY.md`
      — the four `PROVIDE`d linker symbol names and whether the A-7 fallback was taken, because the glue
      declares them `extern`.
  </read_first>
  <behavior>
    Behaviour that must hold after this commit:
    - `python3 scripts/check_cmake_manifest.py` exits 0 with **26** enforced sources, **15** exempt
      `PY32_SDK_SOURCES` entries and **5** allow-listed omissions —
      `src/boards/leonardo_rurp_shield.cpp`, `src/boards/rurp_common.cpp`,
      `src/boards/rurp_config_storage_eeprom.cpp`, `src/boards/uno_rurp_shield.cpp`,
      `src/dev_tools.cpp`. `src/rurp_config_utils.cpp` is no longer among them.
    - `platform/py32f071/src/config.cpp` does not exist in the tree.
    - `python3 -m pytest tests/ -q` reports the Plan 126-07 total, unchanged.
    - `tests/test_config_storage_seam_shape.py` still passes, and its includer census now reports **3**
      hits — the policy TU, the AVR backend and the new glue TU.
    - `pio test -e native` and `pio test -e native_nodevtools` each report exactly 141 cases / 141
      succeeded across 17 suites.
    - All three AVR builds still succeed with the flash and RAM figures Plan 126-04 recorded — this
      commit touches no AVR-compiled file, so any movement is a finding.
  </behavior>
  <action>
    Author four changes and commit them as **ONE** firmware commit. They are atomic in two independent
    directions: `PY32_PLATFORM_SOURCES` is an ENFORCED list, so deleting `config.cpp` without removing
    its entry is exit 1; and promoting `src/rurp_config_utils.cpp` into the ARM build before the
    deletion would give the ARM link two definitions of each of the four public config functions —
    undetectable here, and only visible in Plan 126-11's gated run.

    **Change 1 — create `platform/py32f071/src/config_storage_flash.cpp`.** Follow `timing.cpp`'s
    conventions: Allman braces, file-local state in an unnamed `namespace { }`, `extern "C"` on the
    definitions. Contents:
      - the five-line MIT banner, then a comment naming the phase, requirement CFG-05, decisions
        D-02/D-06/D-11, and stating in one sentence that this is the **only** file that knows the HAL
        exists;
      - the platform guard copied from `py32f071_rurp_shield.cpp`'s pattern:
        `#if !defined(RURP_PLATFORM_PY32F071)` with an `#error` naming this file. It belongs here
        because this TU must never be compiled by the host harness — and explicitly **not** in
        `config_storage_dualslot.cpp`, which the harness does compile;
      - includes: the board header that pulls the HAL in, `"config_storage_dualslot.h"`, and
        `"rurp_config_storage.h"` — this TU **implements the seam**, so it is the third and final
        sanctioned includer of the seam header (Plan 126-05's census expects exactly this);
      - `extern` declarations for the linker symbols `__config_slot_a_start`, `__config_slot_b_start`
        and `__config_page_size`, with C linkage, and a `slot_addr(slot)` helper returning the address
        of slot 0 or slot 1 by taking the address of the corresponding symbol. Comment that the linker
        script is the single source of the flash map (D-11) and that a rename on either side is an ARM
        link failure this environment cannot detect;
      - `hal_read` — `memcpy` from the slot address, because flash is memory-mapped for reads; returns
        true;
      - `hal_erase_page` — build a `FLASH_EraseInitTypeDef` with `TypeErase = FLASH_TYPEERASE_PAGEERASE`
        (the 256 B unit, RM V0.2 §4.2.3.3), `PageAddress = slot_addr(slot)` and `NbPages = 1`; call
        `HAL_FLASH_Unlock` and return false if it is not `HAL_OK`; call `HAL_FLASH_Erase`; call
        `HAL_FLASH_Lock`; return whether the erase returned `HAL_OK`. Comment, citing RM V0.2 §4.2.3.6,
        that `HAL_FLASH_Erase` internally runs the timing-sequence configuration
        (`py32f071_hal_flash.c:416`) and that without those registers the operation *"will fail"* —
        which is exactly why `FLASH->CR` is not poked directly. Comment, citing RM §4.5.3 / §4.2.3.3,
        that if the write-protection option byte covers these pages the erase is **silently skipped**
        with `WRPERR` set, so a non-`HAL_OK` return must propagate as failure and never be reported as
        success;
      - `hal_program_page` — `HAL_FLASH_Unlock`, then
        `HAL_FLASH_Program(FLASH_TYPEPROGRAM_PAGE, slot_addr(slot), <the 64-word buffer>)`, then
        `HAL_FLASH_Lock`, returning whether the program returned `HAL_OK`. Comment (C-2) that
        `FLASH_Program_Page` writes 64 words unconditionally from the pointer it is given, so the caller
        **must** hand over a full 64-word buffer — the core does — and (C-8) that the page must already
        be erased because RM §4.2.3.2 requires reading out a non-blank page's 64 words first and this
        HAL does not do it;
      - a file-local `rurp_flash_primitives_t` instance wiring the three functions and a null context;
      - the two seam functions, `extern "C"`: `rurp_config_storage_load` delegating to
        `rurp_dualslot_load` with that table, and `rurp_config_storage_save` delegating to
        `rurp_dualslot_save`. These are the py32 implementations of the D-06 contract;
      - a closing comment recording C-4's already-satisfied prerequisite: RM §4.2.3 requires HSI on for
        program and erase, and `platform/py32f071/src/main.cpp:25-27` already turns it on — so a future
        clock refactor that turns HSI off would silently break config persistence.

    **Change 2 — delete `platform/py32f071/src/config.cpp`.** Record its four drift points in the
    SUMMARY **first** (see `<read_first>`). Delete the file with `git rm`. Do not merge, port or keep
    any part of it: CFG-07 and D-07 put all four public functions in the common policy layer, and
    keeping any of this file would leave two validate policies.

    **Change 3 — `platform/py32f071/CMakeLists.txt`, four edits.**
      (a) Delete the `# PY32_EXCLUDED: src/rurp_config_utils.cpp -- …` line. Its own comment asked for
          exactly this, and the window Plan 126-03 avoided is now closed by change 2 landing in the same
          commit. Exclusions go 6 → **5**.
      (b) Add `"${REPOSITORY_ROOT}/src/rurp_config_utils.cpp"` to `FIRESTARTER_COMMON_SOURCES`, taking
          it 17 → **18**. Use the `${REPOSITORY_ROOT}/` prefix exactly — it is what makes the string
          match the checker's tree-relative comparison. Place it among the top-level `src/*.cpp`
          entries, before the `src/proms/` group.
      (c) In `PY32_PLATFORM_SOURCES`, remove `src/config.cpp` and add `src/config_storage_flash.cpp`,
          as a **bare relative path**. The list stays at **8** entries.
      (d) **C-3's fourth edit:** add
          `"${PY32_SDK_ROOT}/Drivers/PY32F071_HAL_Driver/Src/py32f071_hal_flash.c"` to
          `PY32_SDK_SOURCES`, taking it 14 → **15**. Use the `${PY32_SDK_ROOT}` idiom exactly. This is
          the edit nothing in this repository can verify: `check_cmake_manifest.py`'s docstring states
          `PY32_SDK_SOURCES` is structurally exempt from resolution because `PY32_SDK_ROOT` resolves
          only after a networked `cmake` configure, and `cmake` is absent here. Treat it as a named
          checklist item and confirm it by `grep -c hal_flash platform/py32f071/CMakeLists.txt`
          returning at least 1 — which is exactly the assertion Task 2 commits.

    **Change 4 — `scripts/check_cmake_manifest.py`, docstring only.** Update the exclusion enumeration
    from six lines back to five, remove the deferral note Plan 126-03 added (its condition is now
    satisfied), and record in one line that `src/rurp_config_utils.cpp` is now a common source compiled
    by the ARM target. **Change no code, no regex, no constant, no exit code** — docstring text only.

    **Prove the atomicity in both directions before committing**, as scratch manipulations that are
    reverted and never committed: with `config.cpp` deleted but its `PY32_PLATFORM_SOURCES` entry still
    present, run `python3 scripts/check_cmake_manifest.py` and record exit 1 with the not-found
    violation; with `config_storage_flash.cpp` created but unnamed in `PY32_PLATFORM_SOURCES`, record
    that the checker still exits 0 — and record that observation explicitly as the reason edit (c) is a
    deliberate act rather than something the gate enforces (the reverse check walks only `src/`).
    Restore the correct state and record exit 0 at 26 enforced sources.

    Commit all four changes together with a message of the form
    `feat(126-08): add the PY32 flash primitives, delete PR #48's non-persisting config.cpp, and close the ARM manifest`.
    Then re-check the branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test ! -e platform/py32f071/src/config.cpp && echo "config.cpp ABSENT" && python3 scripts/check_cmake_manifest.py && echo "hal_flash=$(grep -c hal_flash platform/py32f071/CMakeLists.txt)" && echo "excl=$(grep -c PY32_EXCLUDED platform/py32f071/CMakeLists.txt)" && python3 -m pytest tests/ -q 2>&1 | tail -2 && python3 -m pytest tests/test_config_storage_seam_shape.py -q 2>&1 | tail -2 && echo "shield=$(git hash-object include/rurp_shield.h)" && echo "types=$(git hash-object include/rurp_types.h)" && echo "pio-ini=$(git hash-object platformio.ini)" && git show --stat --format=%H HEAD | head -8 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `platform/py32f071/src/config.cpp`'s four drift points are recorded in the SUMMARY **before** the deletion, each with its verified line range: the private static (`:5-7`), the drifted `rurp_validate_config` with its extra `r2 == 0` condition and `memset` (`:15-30`), the absent write-back, and the `rurp_save_config` that assigns to a static and persists nothing (`:38-47`).
    - `test ! -e platform/py32f071/src/config.cpp` succeeds — CFG-07's deletion verified by absence from the tree, not by a diff.
    - `platform/py32f071/src/config_storage_flash.cpp` exists, uses Allman braces, keeps file-local state in an unnamed `namespace { }`, and gives the two seam functions `extern "C"` linkage.
    - It carries the `#if !defined(RURP_PLATFORM_PY32F071)` guard with an `#error` naming the file, and that guard does **not** appear in `config_storage_dualslot.cpp`.
    - It includes the board header, `"config_storage_dualslot.h"` and `"rurp_config_storage.h"` — making it the third sanctioned includer of the seam header.
    - It declares `__config_slot_a_start`, `__config_slot_b_start` and `__config_page_size` as `extern` with C linkage, and derives slot addresses from them rather than from any duplicated constant. Their names match the values Plan 126-06 recorded, including whether the A-7 fallback was taken.
    - `hal_erase_page` uses `FLASH_TYPEERASE_PAGEERASE`, `NbPages = 1`, unlocks, erases, locks, and returns false unless the erase returned `HAL_OK`. Its comments cite RM V0.2 §4.2.3.6 (timing registers inside the HAL entry points) and RM §4.5.3 / §4.2.3.3 (a write-protected page erase silently skipped with `WRPERR` set, so a non-`HAL_OK` return must never be reported as success).
    - `hal_program_page` calls `HAL_FLASH_Program` with `FLASH_TYPEPROGRAM_PAGE` and the 64-word buffer, and its comments cite C-2 (64 words written unconditionally from the given pointer) and C-8 (the page must already be erased).
    - `hal_read` uses `memcpy` from the memory-mapped slot address.
    - **No `FLASH->CR`, `FLASH->KEYR` or any other direct flash-register access appears in the file** — only `HAL_FLASH_Unlock`, `HAL_FLASH_Erase`, `HAL_FLASH_Program` and `HAL_FLASH_Lock`.
    - A closing comment records C-4's satisfied HSI prerequisite at `platform/py32f071/src/main.cpp:25-27` and the warning that a future clock refactor turning HSI off would silently break config persistence.
    - `platform/py32f071/CMakeLists.txt`: the `PY32_EXCLUDED` line count is **5** and no line names `src/rurp_config_utils.cpp`; `FIRESTARTER_COMMON_SOURCES` has **18** entries including the literal `"${REPOSITORY_ROOT}/src/rurp_config_utils.cpp"`; `PY32_PLATFORM_SOURCES` has **8** entries, contains `src/config_storage_flash.cpp` and does not contain `src/config.cpp`; `PY32_SDK_SOURCES` has **15** entries including the `${PY32_SDK_ROOT}`-rooted `py32f071_hal_flash.c` path.
    - `grep -c hal_flash platform/py32f071/CMakeLists.txt` returns at least 1 (it returned 0 before this commit, confirmed independently at pattern-map time).
    - `python3 scripts/check_cmake_manifest.py` exits 0 and its PASS line reports **26** enforced sources, **15** exempt entries, and exactly the five allow-listed omissions listed in the behaviour block.
    - `scripts/check_cmake_manifest.py`'s docstring enumerates five exclusion lines again, the Plan 126-03 deferral note is removed, and a one-line record notes that `src/rurp_config_utils.cpp` is now an ARM-compiled common source. The diff is confined to docstring text.
    - Both atomicity demonstrations are recorded: exit 1 with the not-found violation for a dangling `src/config.cpp` entry, and exit 0 for an unnamed new `platform/` TU — the latter recorded explicitly as the reason edit (c) is a deliberate act the gate does not enforce. Both were scratch manipulations, reverted, never committed.
    - `python3 -m pytest tests/ -q` reports the Plan 126-07 total, unchanged. `tests/test_config_storage_seam_shape.py` passes and its includer census now reports **3** hits, all three named.
    - `pio test -e native` and `pio test -e native_nodevtools` each report exactly `141 test cases: 141 succeeded` across 17 suites, with the extended-timeout discipline (≥ 540000 ms) stated.
    - The three AVR builds are re-run and their flash and RAM figures are compared against Plan 126-04's recorded values; any movement is reported as a finding, since this commit touches no AVR-compiled file.
    - `git hash-object include/rurp_shield.h` returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`; `include/rurp_types.h` returns `d3fe5203a91527bdb7b20a33843c81065e21c613`; `platformio.ini` returns `f4e720ba75a8c618cc23bac045ab65084d41a0a4`; the linker script matches the SHA Plan 126-06 recorded.
    - `git show --stat HEAD` lists exactly four paths, one of them a deletion.
    - `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - The SUMMARY records explicitly that the ARM link is **not** proven here and that Plan 126-11's gated CI run is the authoritative evidence, and makes no claim about silicon, first-boot timing or DFU preservation.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>The py32 backend is complete and honest: three HAL-routed primitives with the write-protection and erase-first failure modes handled, the seam implemented on the shared core, PR #48's non-persisting `config.cpp` deleted and verified absent, and the manifest closed at 26 enforced sources with the flash driver C-3 found missing now named — all in one commit that never opened the duplicate-definition window.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn C-3's CI-only link failure into a local pytest failure</name>
  <files>firestarter/tests/test_py32_flash_map.py</files>
  <read_first>
    - `firestarter/tests/test_py32_flash_map.py` — the module Plan 126-06 created, including its
      docstring note that this plan extends it. **Extend that module; do not create a second one.**
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §C-3 in full and §"Pitfall 3" — the failure mode (`undefined reference to HAL_FLASH_Unlock` on a
      gated push, hours after the code was written), why nothing local detects it, and the recommended
      assertion shape: the manifest must name the flash driver **whenever the tree contains a
      `HAL_FLASH_` call**.
    - `firestarter/tests/test_vpp_seam_manual_on_every_board.py` lines 333–386 and 412 — the textual
      drift-gate and negative-form idioms this assertion follows.
    - `firestarter/scripts/check_cmake_manifest.py` — the `EXEMPT_LISTS` comment, so the new test's
      docstring can state accurately why the existing checker cannot cover this.
  </read_first>
  <behavior>
    Functions this task adds to the existing module:
    - `test_manifest_names_the_flash_driver` — the implication with a **proven antecedent**: scan
      `platform/py32f071/src/` for any call to a `HAL_FLASH_` entry point; assert the scan found at
      least one (the antecedent is real, not vacuous); then assert
      `platform/py32f071/CMakeLists.txt` names `py32f071_hal_flash.c`. Record the number of files
      scanned and the number of call sites found in the assertion message.
    - `test_the_flash_glue_uses_only_hal_entry_points` — the glue TU calls only `HAL_FLASH_Unlock`,
      `HAL_FLASH_Erase`, `HAL_FLASH_Program` and `HAL_FLASH_Lock`, and contains no direct flash-register
      access (C-4).
    - `test_the_dualslot_core_stays_hal_free` — `config_storage_dualslot.cpp`'s include set is exactly
      its local header plus C standard headers, and it contains no HAL call and no platform `#error`
      guard, so the host harness can compile it.
    - `test_pr48_config_cpp_is_absent_from_the_tree` — CFG-07's deletion, verified by absence.
    - `test_slot_addresses_come_from_linker_symbols` — the glue TU declares the three linker symbols
      `extern` and does not duplicate any config address as a literal constant.
    - `test_manifest_helper_reports_violations_on_planted_copies` — the **RED demonstration**: planted
      copies in `tmp_path` (a manifest with the flash-driver entry removed while a `HAL_FLASH_` call
      exists; a glue TU with a direct register access; a core TU with a HAL include) each fed to the
      same module-level helpers, each producing a non-empty violation list.
  </behavior>
  <action>
    Extend `firestarter/tests/test_py32_flash_map.py` with the six functions in the behaviour block.
    Update its module docstring: add `CFG-05` and `CFG-07` to the `Requirements:` line, add C-3, C-4 and
    D-02 to the `Decisions covered:` line, extend the numbered `Coverage:` list, and replace the "Plan
    126-08 extends this module" note with a record that it did.

    Add a short rationale paragraph to the docstring stating precisely why
    `test_manifest_names_the_flash_driver` exists: `platform/py32f071/include/py32f071_hal_conf.h:10`
    already enables the flash module and `:53` already includes its header, so every `HAL_FLASH_` call
    **compiles**; the failure is at **link**; `check_cmake_manifest.py`'s own docstring declares
    `PY32_SDK_SOURCES` structurally exempt from resolution because `PY32_SDK_ROOT` resolves only after a
    networked `cmake` configure; and `cmake`, `ninja` and `arm-none-eabi-gcc` are all absent from this
    environment. Without this assertion the first sign of the defect would be a gated CI run.

    Factor each new check into a module-level helper returning violations, so the planted-copy test
    exercises the same code. Follow the module's existing textual-gate style.

    **The antecedent must be proven, not assumed.** `test_manifest_names_the_flash_driver` must first
    establish that at least one `HAL_FLASH_` call exists in the platform sources and fail if the scan
    finds none — an implication with an unproven antecedent is exactly the vacuous shape this project
    has had to unwind twice. Record the scanned-file count and the call-site count in the message.

    For the planted-copy test, build each variant in `tmp_path` and feed it to the same helpers. Never
    mutate a committed file; record the blob SHAs of `platform/py32f071/CMakeLists.txt`,
    `platform/py32f071/src/config_storage_flash.cpp` and
    `platform/py32f071/src/config_storage_dualslot.cpp` as unchanged after the run.

    Do not add a skip call or a conditional-skip marker; the module's existing self-enforcing leg must
    still pass with the new functions present.

    Run `python3 -m pytest tests/test_py32_flash_map.py -v` and record every function name with its
    result plus the scanned-file and call-site counts. Then run `python3 -m pytest tests/ -q` and record
    the new total. Commit the single modified file with a message of the form
    `test(126-08): assert the ARM manifest names the flash driver wherever a HAL_FLASH_ call exists`,
    then re-check the branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_py32_flash_map.py -v 2>&1 | tail -30 && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "hal_flash=$(grep -c hal_flash platform/py32f071/CMakeLists.txt)" && echo "checkers=$(ls scripts/check_*.py | wc -l)" && git show --stat --format=%H HEAD | head -5 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_py32_flash_map.py` gains all six functions named in the behaviour block, with those exact names, and `pytest -v` reports every function in the module — old and new — as passed.
    - The module docstring's `Requirements:` line now names CFG-05, CFG-06 and CFG-07; its `Decisions covered:` line adds C-3, C-4 and D-02; the `Coverage:` list is extended; and the "Plan 126-08 extends this module" note is replaced by a record that it did.
    - The docstring carries the rationale paragraph naming `py32f071_hal_conf.h:10` and `:53`, the compile-versus-link distinction, `check_cmake_manifest.py`'s structural exemption of `PY32_SDK_SOURCES`, and the absence of `cmake`, `ninja` and `arm-none-eabi-gcc` from this environment.
    - `test_manifest_names_the_flash_driver` proves its antecedent first: it fails if the `HAL_FLASH_` scan finds zero call sites, and its assertion message records both the scanned-file count and the call-site count. Both observed numbers are recorded in the SUMMARY, and the call-site count is greater than zero.
    - `test_the_flash_glue_uses_only_hal_entry_points` passes and its helper reports a violation on a planted copy carrying a direct flash-register access.
    - `test_the_dualslot_core_stays_hal_free` asserts the core's include set is exactly the local header plus C standard headers, with no HAL call and no platform `#error` guard, and its helper reports a violation on a planted copy carrying a HAL include.
    - `test_pr48_config_cpp_is_absent_from_the_tree` asserts the path does not exist.
    - `test_slot_addresses_come_from_linker_symbols` asserts the three symbols are declared `extern` in the glue TU and that no config address appears there as a duplicated literal constant.
    - `test_manifest_helper_reports_violations_on_planted_copies` builds all three planted variants in `tmp_path`, feeds them to the same module-level helpers the positive tests call, and observes a non-empty violation list for each.
    - The blob SHAs of `platform/py32f071/CMakeLists.txt`, `platform/py32f071/src/config_storage_flash.cpp` and `platform/py32f071/src/config_storage_dualslot.cpp` are recorded unchanged after the planted-copy run, and `git status --porcelain` for `/workspaces/firestarter` is 0 lines.
    - The module's self-enforcing no-skip leg still passes with the new functions present, and no blob SHA literal was introduced.
    - `python3 -m pytest tests/ -q` reports the Task 1 total plus six, and both numbers are recorded.
    - `ls scripts/check_*.py | wc -l` is still `5`; `git show --stat HEAD` lists exactly one path; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - The SUMMARY states explicitly that this assertion **detects** the C-3 defect locally but does not **prove** the ARM target links, and names Plan 126-11's gated CI run as the authoritative evidence.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>The one defect in this phase that no local tooling could catch now has a local detector: a proven-antecedent implication asserting the ARM manifest names the flash driver wherever a `HAL_FLASH_` call exists, alongside assertions that the glue routes only through the HAL, the core stays HAL-free, PR #48's `config.cpp` is gone, and the slot addresses come from the linker — each demonstrated able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the HAL → the flash controller | The §4.2.3.6 timing registers are configured inside the HAL entry points; bypassing them produces code that compiles, reviews cleanly and fails on silicon |
| the write-protection option byte → an erase that reports success | RM §4.5.3 / §4.2.3.3: a protected page erase is silently skipped with `WRPERR` set |
| the linker script → the glue TU | Three `extern` symbols carry the whole flash map; a rename on either side is an ARM-only link failure |
| the source manifest → the ARM linker | `PY32_SDK_SOURCES` is structurally exempt from every local check, so an omission here is invisible until a gated CI run |
| PR #48's `config.cpp` → the shipped policy | A second, drifted `rurp_validate_config` and a `rurp_save_config` that persists nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-08-01 | **Denial of service** | `py32f071_hal_flash.c` absent from `PY32_SDK_SOURCES` | **high** | mitigate | Edit (d) adds it, and `test_manifest_names_the_flash_driver` asserts the manifest names it wherever a `HAL_FLASH_` call exists, with the antecedent proven. Confirmed independently that `grep -c hal_flash` returned 0 before this commit. The authoritative proof remains Plan 126-11's gated CI run |
| T-126-08-02 | **Tampering** | a hand-rolled `FLASH->CR` sequence | **high** | mitigate | Only the four `HAL_FLASH_*` entry points are called, asserted by `test_the_flash_glue_uses_only_hal_entry_points` and demonstrated able to fail. RM §4.2.3.6: without the timing registers the operation *"will fail"*, and nothing local could detect a hand-rolled version |
| T-126-08-03 | **Spoofing** | reporting a write-protected erase as success | **high** | mitigate | A non-`HAL_OK` return from `HAL_FLASH_Unlock` or `HAL_FLASH_Erase` propagates as `false` from `save()`, with the RM §4.5.3 / §4.2.3.3 citation beside it. V4's only real access-control surface on this part |
| T-126-08-04 | **Information disclosure** | programming adjacent RAM into readable flash | high | mitigate | `HAL_FLASH_Program` is handed the core's full 64-word staging buffer, never a record pointer (C-2). The buffer is built and `0xFF`-filled in Plan 126-07's core |
| T-126-08-05 | Tampering | a duplicated flash address diverging from the linker map | medium | mitigate | Slot addresses derive from `__config_slot_a_start` / `__config_slot_b_start` / `__config_page_size`; `test_slot_addresses_come_from_linker_symbols` asserts no duplicated literal address appears in the glue |
| T-126-08-06 | **Repudiation** | a `rurp_save_config()` that persists nothing shipping to a user | **high** | mitigate | CFG-07: `config.cpp` deleted, verified by absence, with its four drift points recorded in the SUMMARY **before** deletion so the evidence survives the file |
| T-126-08-07 | Denial of service | two definitions of each public config function at ARM link | high | mitigate | The promotion of `src/rurp_config_utils.cpp` and the deletion of `config.cpp` are in the same commit. Plan 126-03 deferred the promotion for exactly this reason; the SUMMARY records the window was never opened |
| T-126-08-08 | Denial of service | a dangling manifest entry after a deletion | medium | mitigate | Demonstrated: exit 1 with the not-found violation. The complementary asymmetry — an unnamed new `platform/` TU still exiting 0 — is also demonstrated and recorded, so nobody later assumes the gate covers it |
| T-126-08-09 | Repudiation | the platform guard placed where it breaks the host harness | medium | mitigate | The `#error` guard is required in the glue TU and forbidden in the core; `test_the_dualslot_core_stays_hal_free` asserts the core has neither a HAL include nor a guard |
| T-126-08-10 | Repudiation | an implication that passes because its antecedent is false | high | mitigate | `test_manifest_names_the_flash_driver` fails when the `HAL_FLASH_` scan finds zero call sites, and records the scanned-file and call-site counts in its message |
| T-126-08-11 | Spoofing | a commit landing on the wrong branch | medium | mitigate | `git rev-parse --abbrev-ref HEAD` re-checked in both repos after every commit (RESEARCH Pitfall 7) |

**Package legitimacy:** edit (d) names a source file **inside the already-pinned** Puya SDK
(`OpenPuya/PY32F071_Firmware` @ `0ed2f4b4d3391eccfd4491006a30295fd78e32c2`, `GIT_SHALLOW FALSE`). No new
fetch source, no new pin, no `npm`/`pip`/`cargo` install, and no PlatformIO `lib_deps` entry is added.
No `T-126-SC` row is owed.
</threat_model>

<verification>
1. `config.cpp`'s four drift points recorded with line ranges, then the file deleted and verified absent from the tree.
2. `config_storage_flash.cpp` exists with the platform guard, the three seam-header/board-header/core includes, three `extern` linker symbols, and three primitives calling only the four `HAL_FLASH_*` entry points — with the §4.2.3.6, §4.5.3/§4.2.3.3, C-2 and C-8 citations present and no direct register access.
3. The two seam functions delegate to the core's entry points through a file-local primitive table.
4. The manifest is at 5 exclusions, 18 common sources, 8 platform sources and 15 SDK entries; `grep -c hal_flash` returns at least 1; the gate exits 0 at 26 enforced sources with the five expected allow-listed omissions.
5. The checker docstring is back to five exclusion lines with the deferral note removed; the diff is docstring-only.
6. Both atomicity demonstrations recorded, including the explicit observation that an unnamed new `platform/` TU still exits 0.
7. `tests/test_py32_flash_map.py` gains six functions, all passing, with the flash-driver assertion's antecedent proven and its scanned-file and call-site counts recorded; three planted copies each produce violations; no committed file mutated.
8. The seam-shape census now reports 3 includers; `pytest tests/` grew by six; both pinned native envs at 141 cases / 17 suites; the three AVR figures unchanged against Plan 126-04's record.
9. `include/rurp_shield.h`, `include/rurp_types.h` and `platformio.ini` at their pre-phase blob SHAs; `CONFIG_VERSION` still `VER06`.
10. Two commits (four paths, then one); both repos on their milestone branches; no requirement ticked; the ARM link explicitly **not** claimed proven.
</verification>

<success_criteria>
- CFG-05 (primitives half): three real flash primitives behind the injected table, routed exclusively through the HAL, with the write-protection and erase-first failure modes handled rather than assumed.
- CFG-06 (manifest half): C-3's missing flash driver is named in `PY32_SDK_SOURCES` and a local `pytest` assertion with a proven antecedent detects its absence, converting a CI-only link failure into a local one.
- CFG-07 (deletion half): PR #48's `config.cpp` — including a `rurp_save_config()` that persists nothing — is deleted and verified by absence, with its four drift points recorded before the file disappeared.
- The duplicate-definition ARM link window was never opened: the deletion and the policy-TU promotion are the same commit.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions (or the A-7 fallback symbol pair) | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{h,cpp}` | new files (HAL-free core) | Plan 126-07 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `test_manifest_names_the_flash_driver` and five sibling functions | new test functions in `tests/test_py32_flash_map.py` | Plan 126-08 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (this plan, CFG-07) and the
`# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in `firestarter/platform/py32f071/CMakeLists.txt`
(this plan, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-08-SUMMARY.md`
when done. Record: `config.cpp`'s four drift points with their line ranges, captured before deletion,
and the absence check afterwards; the glue TU's include set, platform guard, three `extern` linker
symbols and the four HAL entry points it calls, with the confirmation that no direct register access
appears; the four manifest edits with before/after counts (exclusions 6 → 5, common 17 → 18, platform
8 → 8 with one swap, SDK 14 → 15) and the gate's PASS line at 26 enforced sources with its five
allow-listed omissions; `grep -c hal_flash` before (0, from the pattern-map record) and after; the
docstring-only diff confirmation; both atomicity demonstrations including the explicit
unnamed-`platform/`-TU-still-exits-0 observation; the six new test functions with results and the
proven antecedent's scanned-file and call-site counts; the three planted-copy demonstrations; the
seam-shape census at 3 includers, named; `pytest tests/` before and after; both native env counts; the
three AVR figures compared against Plan 126-04's record; every must-not-touch blob SHA; the two commit
SHAs with their changed paths; the explicit statement that the ARM link is not proven here and that
Plan 126-11's gated CI run is the authoritative evidence; and the branch re-check in both repos.
</output>
