---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/platform/py32f071/CONFIG-STORAGE.md
  - firestarter/tests/test_config_storage_design_vendored.py
autonomous: true
requirements: [CFG-01, CFG-02]

must_haves:
  truths:
    - "D-01: the new gate is a pytest module under `firestarter/tests/`, not a new PIO `test/native/` suite and not a new `scripts/check_*.py` — and the plan states that it runs in ZERO CI legs on this branch rather than claiming coverage the branch lacks"
    - "CFG-01: `platform/py32f071/CONFIG-STORAGE.md` vendors the in-scope §\"Configuration storage\" subset of blob `4b1a441`, cites that blob by its SHA, names both closed-PR homes (`feature/py32f071-toolchain`/PR #46 and `feature/py32f071-full-support`/PR #47), and carries an explicit SUPERSEDED block mapping all seven superseded module names to what PR #48 actually built"
    - "CFG-02: the doc carries a `## Flash geometry` section recording page = 256 B and sector = 8192 B with the Puya RM V0.2 §4.1 / §4.2.1 / Table 4-1 citation AND the pinned-SDK corroboration at `py32f071xB.h:575-581`, and it lands in its OWN commit which precedes every commit this phase makes to the linker script"
    - "D-19: `CONFIG_MAGIC = 0x52555250` (ASCII `RURP`) is recorded as a **this-milestone choice, explicitly NOT vendored** — blob `4b1a441` specifies the field but supplies no value, so calling the constant vendored would be the Phase-122 C-5 overclaim shape"
    - "D-16 is recorded as AMENDED by RESEARCH C-2, with the RM V0.2 §4.2.3.2 citation and `IS_FLASH_TYPEPROGRAM`'s single accepted value as the reason its literal wording (\"program the header/CRC word LAST\") is not implementable on this part; the corrected shape is written down, never silently substituted"
    - "D-18 amends D-10's shrink quantum to one whole 8 KiB sector: the recorded map is `FLASH` 120K, `CONFIG` `0x0801E000` + 8K, slot A `0x0801E000` (page 480), slot B `0x0801E100` (page 481) — and the amendment is visible as an amendment, with D-10's untouched elements named"
    - "D-13's bootloader seam carries its honest cost statement: a top-of-flash CONFIG region grows downward without moving anything, but giving `BOOTLOADER` a non-zero length MOVES the application `ORIGIN` and is a flash-map MIGRATION, not a resize"
    - "D-12: the host contract is RECORDED here (firmware-only phase) — `FLASH_BASE` `0x08000000` unchanged, `FLASH_SIZE` deliberately stays the physical 131072 because it is a refusal envelope and not an erase bound, and the asymmetry against the linker's 120K app region is correct rather than drift (C-10)"
    - "D-14's first-boot flash-write cost is recorded as **not measured**, with RESEARCH C-7's bus-stall/interrupt-masking mechanism cited — never as *acceptable* and never as a number"
    - "The pre-phase pin is recorded from the live tree before any file moves: eleven blob SHAs, `check_cmake_manifest.py` at 24 enforced sources, `pytest tests/` at 86, both pinned native envs at 141 cases / 17 suites, and the three AVR flash/RAM figures"
  prohibitions:
    - statement: "`include/rurp_shield.h` is NOT modified by this plan (D-09)"
      status: required
      verification: "`git hash-object include/rurp_shield.h` still returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, and the file does not appear in any commit this plan makes"
    - statement: "No new `scripts/check_*.py` is created — the CFG-01 gate is a `tests/test_*.py` module"
      status: required
      verification: "`ls scripts/check_*.py | wc -l` is 5, unchanged; `tests/test_checker_convention.py`'s `FLOOR` and `FIXTURE_FLOOR` are not edited"
    - statement: "`CONFIG-STORAGE.md` does not reproduce the Validation Ceiling's forbidden-claim phrases verbatim"
      status: required
      verification: "The claim-ceiling section states the ceiling by reference to `.planning/REQUIREMENTS.md` §\"Validation Ceiling\"; `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` run with `firestarter/platform/py32f071/CONFIG-STORAGE.md` named as an explicit target exits 0"
    - statement: "CRC32 is never described as a security, authentication or tamper-resistance primitive"
      status: required
      verification: "`CONFIG-STORAGE.md` carries an explicit paragraph stating CRC32 detects accidental corruption only and that anyone able to write flash can recompute it"
    - statement: "No commit in this plan edits `platform/py32f071/linker/PY32F071xB_FLASH.ld`"
      status: required
      verification: "`git hash-object platform/py32f071/linker/PY32F071xB_FLASH.ld` still returns `b32b5824c8e27492551db5c2b1d413f74f05b6f3` at the end of this plan"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "`git diff` on `.planning/REQUIREMENTS.md` is empty for this plan's commits; only Plan 126-11 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` are unchanged by this plan"
  artifacts:
    - firestarter/platform/py32f071/CONFIG-STORAGE.md
    - firestarter/tests/test_config_storage_design_vendored.py
  key_links:
    - "The `CONFIG-STORAGE.md` commit is the CFG-02 ordering anchor for the whole phase. Plan 126-06 edits the linker script and wraps `git rev-list --is-ancestor` around this commit. If the doc lands in the same commit as anything else, or later than a linker edit, Criterion 2 is unsatisfiable and cannot be retro-fitted."
    - "`platform/py32f071/PORTING.md` must never be cited again as a live path — it exists on no live branch. The doc's own text must say so, because PROJECT.md and STATE.md both cite it and a later reader will look for it."
    - "The CFG-01 gate must carry a planted-violation RED demonstration: a copy of the doc in `tmp_path` with the blob SHA removed, fed to the same module-level helper, must be reported as a violation. Phases 118 and 124 both shipped gates that passed without observing anything and had to be unwound."
---

<objective>
Record the numbers this phase is judged against, then land the vendored flash-config design as its
own commit — the ordering anchor every later linker edit is measured against — and gate its
required content mechanically.

Purpose: CFG-01 makes this phase partly *design* work. The document PROJECT.md and STATE.md both
cite as its specification (`platform/py32f071/PORTING.md`) exists on no live branch; it survives
only as blob `4b1a441` on closed PRs #46/#47, and its module layout was superseded by what PR #48
actually built. CFG-02 additionally requires the PY32F071xB flash page/erase-unit size to be read
from the Puya reference manual and recorded in a commit that **precedes** any linker-script edit —
because two plausible wrong answers (the PY32F030/F003 "128 B page / 4 KiB sector" figures, and
the pinned SDK's own stale HAL header comment) sit in the first two places a hurried reader would
look.

Output: `firestarter/platform/py32f071/CONFIG-STORAGE.md` in one commit of its own;
`firestarter/tests/test_config_storage_design_vendored.py` in a second commit; and the pre-phase
pin recorded in this plan's SUMMARY.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-VALIDATION.md

**Read order and precedence.** `126-CONTEXT.md` records the operator's locked decisions D-01…D-19.
Research then measured several of their mechanisms and filed 14 corrections C-1…C-14. The decision
**ids stay binding; three mechanisms are replaced** — D-08's "three edits" becomes four (C-3),
D-10/D-11's shrink quantum is refined by D-18 (from C-5), and D-16's "program the header/CRC word
LAST" is **not implementable** (C-2). Where CONTEXT and RESEARCH disagree, **RESEARCH wins on
verified facts** — CONTEXT's own banner says so. `126-PATTERNS.md` gives the analog file and the
verbatim excerpt for every new file; read the excerpt it points at rather than inventing a shape.

**Path correction, verified at pattern-map time:** the linker script is at
`platform/py32f071/linker/PY32F071xB_FLASH.ld`. The ROADMAP and CONTEXT prose imply
`platform/py32f071/PY32F071xB_FLASH.ld`; **that path does not exist.**

**Repo layout.** `/workspaces` is a meta/planning repo tracking only `.planning/` and `.claude/`.
Every code and doc path in this plan lives in the firmware submodule `/workspaces/firestarter`,
branch `v1.23-py32f071-integration`, tip `2b5e8c875bb04d728b5e08d16cc2d29e0d43c1d7`. Code commits
land **inside that submodule** on that branch; only this plan's SUMMARY commits in the meta repo.
`/workspaces/firestarter_app` is out of scope this phase (D-12 — Phase 127 owns the host half).
**Never write into `firestarter_py32_ci/` or `firestarter_app_py32/`** — gitignored worktrees of
the same two repos, never gitlinked (RESEARCH Pitfall 8).

**After every `gsd-tools query commit`, re-check `git rev-parse --abbrev-ref HEAD` in BOTH repos.**
An unanchored `##…vX.Y` ROADMAP-prose regex has been observed moving HEAD to another branch
(RESEARCH Pitfall 7, seen live 2026-07-30). Expect `v1.23-py32f071-integration` in the firmware
submodule and `gsd/v1.23-py32f071-integration` in the meta repo.

**Requirement ticking scope for this plan: NONE.** CFG-01 and CFG-02 appear in this plan's
`requirements` frontmatter because its tasks contribute the evidence for them, but CFG-02 is
completed only when Plan 126-06 lands the linker edit the ordering is measured against. Do **not**
tick CFG-01…CFG-07 in `.planning/REQUIREMENTS.md` from this plan. Plan 126-12 is the only plan
permitted to tick any of them. Executors on this project marked multi-plan requirements Complete
from a single plan four times in Phase 116; this is that guard.

**Claim ceiling, non-negotiable.** No PY32F071 PCB exists. Nothing written here may claim, or
imply, that the firmware runs on a PY32F071, that a DFU install works end to end, or that any
behaviour is bench/hardware/silicon validated. ARM flash and RAM are unmeasurable locally by
anyone. State the ceiling **by reference** to `.planning/REQUIREMENTS.md` §"Validation Ceiling" —
do **not** reproduce its forbidden-phrase list verbatim in `CONFIG-STORAGE.md`, because
`check_permitted_claims.py` scans for those phrases as literals and every one of Phase 125's six
SUMMARY files tripped it exactly that way (125-RESEARCH C-16, the self-reference trap).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-phase pin — eleven blob SHAs, gate counts, native counts, AVR figures</name>
  <files>(no files modified — read-only evidence capture into this plan's SUMMARY)</files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` — the `avr_targets` and `native_envs`
      blocks, plus `meta.note` (the measurement procedure and its recorded trap) and
      `meta.supersedes` (which names `size_baseline_base01.json` immutable). Read the file; do not
      recall the figures.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §"AVR size measurement (strict equality, armed)" — the live figures table and the two
      comparators. **`scripts/check_size_baseline.py` has TWO comparators reading DIFFERENT
      baseline files**: `compare_avr` (strict equality, live `size_baseline.json`) and
      `compare_avr_policy_merge05` (A-5 band, frozen `size_baseline_base01.json`). Any measurement
      row must name which one it invoked.
    - `.planning/phases/124-firmware-integration-merge/124-VERIFICATION.md` — the live finding on a
      `git diff --stat | grep -v` pipeline reported as "(empty)" when a trailer survived the grep.
      Read it before writing any untouched-ness proof.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` §C-15 — why a blob-SHA re-hash is the
      primary "untouched" proof and a path-scoped `git diff` is corroboration only.
  </read_first>
  <action>
    Record the numbers this phase is judged against, before anything moves. A gate written
    afterwards can only bless what already happened.

    In `/workspaces/firestarter`, record as observed values: the current branch name, the full
    40-character `HEAD`, and the `git status --porcelain` line count. **Name the repository
    explicitly in every porcelain statement.** `/workspaces/firestarter_app` has a legitimately
    non-empty porcelain right now (` M .gitignore`, plus untracked `.coverage`, `.planning/config.json`,
    `SECURITY.md`, `write_test_port.sh`), so an unqualified porcelain row reads as a failure it is
    not.

    Record the `git hash-object` blob SHA of each of these eleven paths and state for each whether
    it matches the expected value below. The first five are files CFG-07 or D-09 forbid this phase
    from changing at all; the next four are files later plans in this phase deliberately change,
    pinned here so each change is attributable; the last two are the size-baseline pair.
      - `include/rurp_types.h` → expect `d3fe5203a91527bdb7b20a33843c81065e21c613` (CFG-07: `rurp_configuration_t` unchanged)
      - `include/rurp_shield.h` → expect `602fe6f326a042ab71efd111e4dfcf3a6e41dd46` (D-09: not touched, and `CONFIG_VERSION "VER06"` lives here)
      - `platformio.ini` → expect `f4e720ba75a8c618cc23bac045ab65084d41a0a4`
      - `include/messages.h` → expect `dc7dbfc6b7ad3d767f7dad1ecbe13a53ca1eb346` (codegen-generated from the meta repo's canonical `messages.toml`; a hand edit here is always wrong)
      - `scripts/baseline/size_baseline_base01.json` → expect `b940c91655600a57ad7ef67cba723943af929daf` (Phase 124's frozen MERGE-05 reference; never re-baselined by this phase)
      - `src/rurp_config_utils.cpp` → expect `6705fd46e07a2d359d161dc2e7728cb4e45f89c7` (**this is the D-04 pre-refactor anchor** — Plan 126-02's regression test is written against exactly this blob)
      - `platform/py32f071/CMakeLists.txt` → expect `4ba33365050fa73faeceb9ebcd61578dede04b83`
      - `platform/py32f071/linker/PY32F071xB_FLASH.ld` → expect `b32b5824c8e27492551db5c2b1d413f74f05b6f3`
      - `platform/py32f071/src/config.cpp` → expect `78ebf0eabcba5adca40f517e05606cd941902801` (deleted by Plan 126-08)
      - `scripts/baseline/size_baseline.json` → expect `9cc5204bb437735d77523e62512c1d2cadfc668f`
      - `scripts/check_cmake_manifest.py` → expect `ec56b5e163950a9e7a3c1fb43df89d574647c215` (its module docstring is edited by Plan 126-03)
    A mismatch on any of the eleven is a STOP-and-report finding, not something to work around: it
    would mean the tree is not the tree research and pattern-mapping measured.

    Record the `CONFIG_VERSION` line verbatim with its line number from `include/rurp_shield.h` (it
    is at line 46). It must read as the literal `VER06` and must still read that way at the end of
    the phase (CFG-07).

    Record `sizeof(rurp_configuration_t)` as measured on this host — compile a throwaway
    translation unit in a scratch directory outside the repo that includes `rurp_types.h` and
    prints the value. Expect **32** on this host, because host `long` is 8 bytes. Record alongside
    it that AVR measures **15** and ARM is computed at **20** (RESEARCH C-6, table). **The only
    property any test in this phase may assert about size is the relational one** —
    `sizeof(StoredConfiguration) <= 256` — because `g++ -m32` is unavailable here (no multilib) and
    a literal size assertion is wrong on two of the three compilers.

    Record the current state of the gates and suites this plan must not move:
      - `python3 scripts/check_cmake_manifest.py` — record the exit code and the enforced-source
        count, the exempt count, and the allow-listed omission list from its PASS line. Expect exit
        0, **24 enforced sources**, **14** `PY32_SDK_SOURCES` exempt, and 5 allow-listed omissions
        (`src/boards/leonardo_rurp_shield.cpp`, `src/boards/rurp_common.cpp`,
        `src/boards/uno_rurp_shield.cpp`, `src/dev_tools.cpp`, `src/rurp_config_utils.cpp`).
      - `python3 -m pytest tests/ -q` — record the passed count. Expect **86**.
      - `ls scripts/check_*.py | wc -l` — expect **5**. This phase adds none (Phase 125 C-11
        measured a new checker at four artifacts plus two floor bumps in
        `tests/test_checker_convention.py`).
      - the `native_envs` block of `scripts/baseline/size_baseline.json` — record `native` and
        `native_nodevtools` at **141 cases / 17 suites**, and `native_pinmap_provisional` at 10
        cases / 1 suite. These are the *recorded* numbers; later plans measure against them.
      - the `avr_targets` block — record flash used/total and RAM used/total for all three: uno
        23954/32256 flash and 1573/2048 RAM; uno328pb 24004/32384 and 1579/2048; leonardo
        26016/28672 and 2014/2560. Note that leonardo's free flash is **2656 B**, not the 2600 B
        `126-CONTEXT.md` states — CONTEXT is stale here and RESEARCH is right.

    Do not modify any file in this task. Do not create `CONFIG-STORAGE.md` here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git rev-parse --abbrev-ref HEAD && git rev-parse HEAD && echo "fw-porcelain-lines=$(git status --porcelain | wc -l)" && git hash-object include/rurp_types.h include/rurp_shield.h platformio.ini include/messages.h scripts/baseline/size_baseline_base01.json src/rurp_config_utils.cpp platform/py32f071/CMakeLists.txt platform/py32f071/linker/PY32F071xB_FLASH.ld platform/py32f071/src/config.cpp scripts/baseline/size_baseline.json scripts/check_cmake_manifest.py && grep -n 'define CONFIG_VERSION' include/rurp_shield.h && echo "checkers=$(ls scripts/check_*.py | wc -l)" && python3 scripts/check_cmake_manifest.py && python3 -m pytest tests/ -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records `branch = v1.23-py32f071-integration` and the full 40-character firmware `HEAD` as observed values, and states the meta repo's branch as `gsd/v1.23-py32f071-integration`.
    - `git status --porcelain` in `/workspaces/firestarter` is recorded as exactly 0 lines, and the row names the firmware repo explicitly rather than saying "the repo". The host repo's five known pre-existing porcelain lines are recorded as pre-existing and NOT treated as a finding.
    - All eleven blob SHAs are recorded, each stated as matching or not matching the expected value listed in the action. Any mismatch is reported as a STOP finding and the plan halts.
    - The `CONFIG_VERSION` line is quoted verbatim with its line number and reads as the literal `VER06`.
    - `sizeof(rurp_configuration_t)` measured on this host is recorded as `32`, with the AVR `15` and computed ARM `20` recorded beside it and an explicit statement that no test in this phase may assert a literal size or offset.
    - `python3 scripts/check_cmake_manifest.py` exits 0; the enforced-source count is recorded as `24`, the exempt count as `14`, and the five allow-listed omissions are listed.
    - `python3 -m pytest tests/ -q` reports `86 passed` and the number 86 is recorded.
    - `ls scripts/check_*.py | wc -l` is recorded as `5`.
    - The recorded native counts are `native` 141/17, `native_nodevtools` 141/17, `native_pinmap_provisional` 10/1, sourced from `scripts/baseline/size_baseline.json` and stated as recorded-baseline values rather than as measurements taken here.
    - The three AVR flash/RAM figures are recorded with both used and total, and leonardo's free flash is recorded as `2656` with an explicit note that `126-CONTEXT.md`'s "2600 B" is stale.
    - Which comparator each later measurement row will invoke is stated: `compare_avr` (strict equality) against `scripts/baseline/size_baseline.json`, and `compare_avr_policy_merge05` (A-5 band) against `scripts/baseline/size_baseline_base01.json`. The two are named as different gates with different pass sets.
    - No file was modified: `git status --porcelain` in `/workspaces/firestarter` is still 0 lines at the end of the task.
  </acceptance_criteria>
  <done>Every number, hash and figure this phase is judged against is recorded from the live tree before any file moves, with the firmware repo named explicitly in every porcelain statement and the two size comparators distinguished by name and baseline file.</done>
</task>

<task type="auto">
  <name>Task 2: Author platform/py32f071/CONFIG-STORAGE.md — its own commit, the CFG-02 ordering anchor</name>
  <files>firestarter/platform/py32f071/CONFIG-STORAGE.md</files>
  <read_first>
    - Blob `4b1a441` itself: run `git cat-file -p 4b1a441` from `/workspaces/firestarter` and read
      all 195 lines. **This is the source being vendored.** Verified readable from the local clone
      2026-07-31. Read it; do not work from RESEARCH's excerpt alone — you must be able to name
      which sections are in scope, superseded and out of scope.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §"Code Examples" → "The vendored in-scope design (CFG-01)" and "The CFG-02 geometry record"
      (lines 1020–1090). These give the exact in-scope subset, the SUPERSEDED mapping, and the
      geometry table with its citations.
    - Same file, §C-1 (the geometry and the two wrong numbers), §C-2 (why D-16's literal wording is
      not implementable), §C-5 (why the reservation is sector-aligned), §C-7 (the two RM quotes on
      reset during program/erase and on the bus stalling), §C-8 (erase before program), §C-9 (the
      host's 2048 fallback erase grid), §C-10 (the host's `FLASH_BASE`/`FLASH_SIZE` are already
      right).
    - Same file, §"Security Domain" (lines 1329–1356) — the V4/V5/V6 rows. V6's row is the source
      of this document's mandatory "CRC32 is not a security primitive" paragraph.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md`
      §decisions — read D-10, D-11, D-12, D-13, D-14, D-16, D-17, D-18, D-19 in full. D-18 and
      D-19 are the newest and least-echoed (locked from a research escalation in commit `20a59c9`).
    - `firestarter/platform/py32f071/README.md` — the only existing doc under `platform/py32f071/`;
      match its heading depth, table style and tone. There is no in-tree vendored-design document
      to copy, so this is the nearest house-style reference.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 8–22) — read it so the ceiling can
      be stated **by reference**. Do not copy its forbidden-phrase list into the new document.
  </read_first>
  <action>
    Create `firestarter/platform/py32f071/CONFIG-STORAGE.md` and commit it **alone**. This commit
    is the CFG-02 ordering anchor: Plan 126-06 wraps `git rev-list --is-ancestor` around it, so it
    must exist as a commit that precedes every commit this phase makes to
    `platform/py32f071/linker/PY32F071xB_FLASH.ld`. Do not fold any other file into this commit and
    do not touch the linker script in this plan at all.

    The document must contain these sections, in this order, with this content:

    **Opening statement.** What the document is: the in-scope subset of the flash-config design,
    vendored onto the milestone branch because its cited source is stranded on closed PRs. Cite
    blob **`4b1a441`** by SHA. Name both closed-PR homes: `feature/py32f071-toolchain` (PR #46) and
    `feature/py32f071-full-support` (PR #47), both **closed**. State plainly that
    `platform/py32f071/PORTING.md` **does not exist on any live branch** and must never be cited as
    a live path again — PROJECT.md and STATE.md both cite it, so a later reader will go looking.

    **`## Configuration storage (vendored, in scope)`.** The blob's three sentences and its
    `StoredConfiguration` struct, verbatim: `uint32_t magic; uint16_t version; uint16_t length;
    rurp_configuration_t configuration; uint32_t sequence; uint32_t crc32;`. Include the blob's
    acceptance item *"Flash-backed configuration survives interrupted writes."* Record D-17's
    warning explicitly so nobody later "reconciles" them: the wrapper's `version` (u16) is **not**
    `CONFIG_VERSION` (the `char[6]` `"VER06"` inside `rurp_configuration_t`), and
    `rurp_configuration_t` is embedded **byte-for-byte**, which is what makes CFG-07's "schema
    unchanged" structurally true rather than merely asserted. State that `version` is written as
    `1` and that no reader branches on it yet, and that no version-dispatch branch is to be added.

    **`## SUPERSEDED by PR #48's actual module layout`.** A two-column mapping table. The blob's
    entire §"PY32F071 backend modules" tree is superseded. Name all seven module names the CFG-01
    gate will look for — `storage.cpp`, `gpio.cpp`, `board.cpp`, `adc.cpp`, `dac.cpp`,
    `py32f071_board.h`, `py32f071_pins.h` — plus `usb.cpp`, and map them to what PR #48 actually
    built: `py32f071_rurp_shield.cpp`, `timing.cpp`, `usb_cdc.c`, `platform_compat.cpp`,
    `main.cpp`, and `config.cpp` (noting that `config.cpp` is **deleted by this phase** per
    CFG-07). Mark each row superseded rather than silently following it.

    **`## Out of scope`.** The blob's §"ADC measurement" steps 5–6, its §"DAC VPP control" in full,
    and its acceptance items covering calibration, closed-loop DAC and real hardware. Route them to
    **FUT-VPP** and **FUT-CAL** (the queued calibration milestone). State why the full 195-line
    document was not restored under its own name: 4 of its 15 acceptance items are out of scope and
    restoring it whole would re-strand out-of-scope prescriptions in-tree as if they were the
    contract.

    **`## Flash geometry` (CFG-02).** Source of record: **Puya, *PY32F07X Series Reference Manual*,
    V0.2** — §4.1 "Key features" (p.34), §4.2.1 "Flash structure" (p.34), **Table 4-1 "Flash
    structure and boundary addresses"** (p.34). A table with: page size (smallest erase + program
    unit) = **256 bytes**; sector size = **8192 bytes (8 KBytes)**; main flash = 128 KBytes,
    `0x08000000`–`0x0801FFFF` = 16 sectors / 512 pages; Sector 15 = pages 480–511,
    `0x0801E000`–`0x0801FFFF`; program granularity = one full page as 64 × 32-bit words, with
    non-32-bit writes raising a hard fault (RM §4.2.3.2); erase granularities = page (256 B),
    sector (8 KB), mass. Corroborate byte-for-byte against the pinned SDK
    `OpenPuya/PY32F071_Firmware` @ `0ed2f4b4d3391eccfd4491006a30295fd78e32c2` (the exact `GIT_TAG`
    in `platform/py32f071/CMakeLists.txt:16`),
    `Drivers/CMSIS/Device/PY32F071/Include/py32f071xB.h:575-581`: `FLASH_PAGE_SIZE 0x00000100U`,
    `FLASH_SECTOR_SIZE 0x00002000U`, `FLASH_BASE 0x08000000UL`, `FLASH_END 0x0801FFFFUL`.
    Add an explicit **do-not-use** note naming two traps: the pinned SDK's own
    `py32f071_hal_flash.h:268` comment claiming a 128-byte program unit (stale, carried over from
    the smaller part, contradicted by the `while(index<64U)` loop directly beneath it), and the
    widely circulated "128 B page / 4 KiB sector" figures, which are PY32F030/F003 values.

    **`## Reserved flash map` (CFG-06, D-10/D-11/D-13, quantum per D-18).** A table:
    `BOOTLOADER` at `0x08000000`, length **0**; `FLASH` (app) at `0x08000000`, length **120K**
    (`0x1E000`), shrunk from 128K so `.text`/`.rodata` physically cannot reach config; `CONFIG` at
    `0x0801E000`, length **8K** = Sector 15, pages 480–511; `__config_slot_a_start` = `0x0801E000`
    (page 480); `__config_slot_b_start` = `0x0801E100` (page 481, a **different page erase unit**);
    `__config_page_size` = `256`.
    State the D-18 amendment **as an amendment**: D-10 mandated top-of-flash placement, a shrunk
    `LENGTH`, a second `MEMORY` region and `PROVIDE`d symbols (D-11), and all of that is untouched;
    D-18 changes only the *quantum* of the shrink from "two erase units" to one whole 8 KiB sector,
    because a 512 B reservation at `0x0801FE00` leaves the app region ending at `0x0801FDFF` —
    inside Sector 15 — so a sector-granular DFU erase of the app's final block would be exactly the
    block holding config. Record the accepted cost: **7680 B of the 8192 B is deliberate slack**
    (6.25% of flash), claimable later by FUT-N05 or by additional slots **without moving any
    address**.
    Record D-13's cost honestly and in full: a top-of-flash CONFIG region grows *downward* without
    moving anything, but the `BOOTLOADER` placeholder does **not** have that property — giving it a
    non-zero length **moves the application's `ORIGIN`**, which is a flash-map **migration**, not a
    resize, and changes the vector-table address of every previously flashed unit. State that Phase
    129 must record the bootloader budget as an *intent with that cost attached*, never as a number
    that looks already paid for.

    **`## Amendment to D-16 — the commit step, corrected` (RESEARCH C-2).** State that D-16's
    locked wording — *erase the inactive slot → program the record body → program the header/CRC
    word LAST* — has a step that **cannot be executed on this part**, and cite why:
    `py32f071_hal_flash.h:631`'s `IS_FLASH_TYPEPROGRAM` accepts exactly one value
    (`FLASH_TYPEPROGRAM_PAGE`), `FLASH_Program_Page` writes 64 `uint32` words unconditionally, and
    RM V0.2 §4.2.3.2 states any non-32-bit write raises a hard fault. There is no primitive that
    writes one word. Record the corrected shape: **erase the inactive slot → build the whole
    256-byte page in a 4-byte-aligned staging buffer → one page program whose completion IS the
    commit.** The property D-16 exists to protect survives intact: the active slot is never
    touched, so any interruption leaves the previous record loadable, and a page interrupted
    mid-burst fails CRC on the next load and is rejected. Note that the alternative that preserves
    D-16's literal wording (two pages per slot, body page then commit page) was considered and
    rejected: it adds a failure mode CRC already covers. **This is an explicit amendment, not a
    reinterpretation** — the phrase "program the header/CRC word LAST" must not appear anywhere in
    this phase's implementation as an instruction.

    **`## CONFIG_MAGIC` (D-19).** The value is **`0x52555250`** — the ASCII four-CC `'R','U','R','P'`,
    tying the record to the shield name already used throughout the firmware and readable as `RURP`
    in a hex dump. It is defined **once**, in the HAL-free core's local header. Record it as a
    **this-milestone choice, explicitly NOT vendored**: blob `4b1a441` specifies the `magic` *field*
    but supplies no value, so describing the constant as vendored would be exactly the overclaim
    Phase 122's C-5 had to correct. State the two hard constraints it satisfies: it is neither
    `0xFFFFFFFF` (which erased NOR flash reads as, so a blank slot would look valid and collapse
    D-15's blank test into a CRC-collision inference) nor `0x00000000`.

    **`## Validation order for a record read from flash` (V5).** Every byte read from slot A or B is
    untrusted input — it may be blank, garbage from a partial write, or left over from an unrelated
    firmware image. Record the mandatory order: `magic`, then `length` **bounds-checked against the
    caller's buffer**, then `crc32` — each validated before any is used. State plainly that a
    record whose `length` exceeds the caller's buffer is **rejected before any copy**, that the copy
    is bounded by `min(length, len)`, and that **the CRC does not save you**: the bound check must
    be ordered before the copy, and anyone who can write flash can produce a matching CRC. Name the
    target: Cortex-M0+, 16 KiB SRAM, no MPU configured.

    **`## CRC32 is not a security primitive` (V6).** One explicit paragraph: CRC32 detects
    accidental corruption only. It provides no tamper resistance and no authentication — anyone able
    to write flash can recompute it. State that "CRC-protected" must never drift into
    "authenticated" in any artifact this milestone writes.

    **`## Erase before program is mandatory` (C-8).** RM V0.2 §4.2.3.2's programming sequence, step
    2, requires reading out the 64 words of a page that already holds data. `FLASH_Program_Page`
    does **not** perform that read-out, so the HAL is only correct on a **blank** page. Record that
    this makes "erase the inactive slot first" a hard correctness requirement rather than a design
    preference, and that the dual-slot test suite asserts a program on a non-erased page as a
    **test failure**, not a silent overwrite.

    **`## Write protection` (V4).** RM V0.2 §4.5.3 / §4.2.3.3: if the flash write-protection option
    byte covers the config pages, a page erase is **silently skipped** and `WRPERR` is set. Record
    the requirement: a HAL return other than `HAL_OK` must yield `save() == false`; the backend must
    never report success it did not achieve.

    **`## Reset and interrupt behaviour` (C-7).** Quote RM V0.2 §4.2.3 twice, verbatim: *"If a reset
    occurs during Flash program and erase operations, the contents of the Flash memory are not
    protected."* and the passage stating that any attempt to read flash during a program or erase
    stalls the bus, so code and data fetches cannot be made while those operations are in progress.
    Record the consequence: combined with the HAL's interrupt masking across the 64-word burst, no
    interrupt is serviced for the duration of an erase or a program, USB CDC included — which is in
    direct tension with the blob's own architectural requirement that USB must keep running. Record
    the prerequisite already satisfied: RM §4.2.3 requires HSI on for program and erase, and
    `platform/py32f071/src/main.cpp:25-27` already turns it on — note that a future clock refactor
    turning HSI off would silently break config persistence. Then record **D-14's cost as a
    non-claim**: policy is unchanged on both platforms, so `rurp_validate_config`'s existing
    write-back fires on a virgin part exactly as it does on AVR, which means a flash erase+program
    during startup. With no PCB, that cost is **not measured**. Write it as *not measured* — never
    as *acceptable*, and never as a number.

    **`## Host contract` (D-12, C-9, C-10).** This phase is firmware-only; Phase 127 owns the host
    half and runs in parallel. Record: `FLASH_BASE` stays `0x08000000` and `FLASH_SIZE` stays the
    **physical 131072** — because `FLASH_SIZE` is a *refusal envelope* (`py32_dfu.py:648`), not an
    erase bound, and shrinking it would make the host unable to flash the part it describes. State
    the asymmetry explicitly and mark it **correct, not drift**: the linker's `FLASH` `LENGTH`
    becomes 120K while the host's `FLASH_SIZE` stays 128K, because one is an app-region bound and
    the other a physical refusal envelope. Record that the host's DFU erase is payload-length-scoped
    (`py32_dfu.py:748-750`), which is what makes top-of-flash placement preserve config for free on
    an install whose image does not reach the top — and carry that as the *intended* behaviour, an
    explicit **non-claim** until a board exists. Record C-9: the host's
    `DEFAULT_ERASE_PAGE_SIZE = 2048` fallback matches neither the 256 B page nor the 8192 B sector;
    it fires only when the device publishes no DfuSe layout; **do not edit the host here**. Record
    the reserved config base `0x0801E000` as the named constant Phase 127 must agree with. Record
    Phase 127's two obligations: `firestarter_app/tests/scan_paths.py`'s `CROSS_REPO_TEST_PATHS`
    contains **no** entry for the linker script or any config file today, so a host test reading
    firmware source text must add one; and per BASE-02 it must key repo presence on
    `../firestarter/.git` via `tests/fw_presence.py`, never on a single scanned file.

    **`## Claim ceiling`.** State by **reference** to `.planning/REQUIREMENTS.md` §"Validation
    Ceiling" that no PY32F071 PCB exists, that no statement in this document is a claim about
    silicon behaviour, and that ARM build evidence is a CI workflow run URL plus head SHA and never
    a local build (`arm-none-eabi-gcc`, `cmake` and `ninja` are absent from this environment). **Do
    not reproduce the forbidden-phrase list verbatim** — `check_permitted_claims.py` scans for those
    phrases as literals, and all six of Phase 125's SUMMARY files tripped it by quoting them inside
    their own compliance paragraphs.

    Before committing, run the claim gate against this file with the target named explicitly:
    `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py`
    with `firestarter/platform/py32f071/CONFIG-STORAGE.md` as its target. It must exit 0. Then
    commit the single file in the firmware submodule with a message of the form
    `docs(126-01): vendor the in-scope flash-config design and record the PY32F071xB flash geometry`,
    and afterwards confirm `git rev-parse --abbrev-ref HEAD` is still `v1.23-py32f071-integration`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -f platform/py32f071/CONFIG-STORAGE.md && grep -c '4b1a441' platform/py32f071/CONFIG-STORAGE.md && grep -c '0x52555250' platform/py32f071/CONFIG-STORAGE.md && grep -c '0x0801E000' platform/py32f071/CONFIG-STORAGE.md && grep -c '0ed2f4b4d3391eccfd4491006a30295fd78e32c2' platform/py32f071/CONFIG-STORAGE.md && echo "ld-blob=$(git hash-object platform/py32f071/linker/PY32F071xB_FLASH.ld)" && echo "shield-blob=$(git hash-object include/rurp_shield.h)" && git show --stat --format=%H HEAD | head -5 && git rev-parse --abbrev-ref HEAD && python3 -m pytest tests/ -q 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `platform/py32f071/CONFIG-STORAGE.md` exists and contains the literal string `4b1a441` at least once, as a blob SHA citation.
    - The document names both `feature/py32f071-toolchain` and `feature/py32f071-full-support` and identifies PR #46 and PR #47 as closed.
    - The document states that `platform/py32f071/PORTING.md` does not exist on any live branch.
    - The vendored `StoredConfiguration` field list appears with all six fields in the blob's order: `magic`, `version`, `length`, `configuration`, `sequence`, `crc32`.
    - A SUPERSEDED section names all seven superseded module identifiers — `storage.cpp`, `gpio.cpp`, `board.cpp`, `adc.cpp`, `dac.cpp`, `py32f071_board.h`, `py32f071_pins.h` — and maps them to PR #48's actual modules. `config.cpp` is marked as deleted by this phase.
    - An out-of-scope section routes the DAC-VPP and calibration material to FUT-VPP and FUT-CAL.
    - A `## Flash geometry` section records page = 256 bytes and sector = 8192 bytes, cites *PY32F07X Series Reference Manual* V0.2 with §4.1, §4.2.1 and Table 4-1, and cites the pinned SDK commit `0ed2f4b4d3391eccfd4491006a30295fd78e32c2` with the `py32f071xB.h` line range.
    - The geometry section carries an explicit do-not-use note covering both the stale HAL header comment and the PY32F030/F003 figures.
    - A reserved-flash-map section records `FLASH` at 120K, `CONFIG` at `0x0801E000` with length 8K, slot A at `0x0801E000`, slot B at `0x0801E100`, `__config_page_size` = 256, and `BOOTLOADER` at `0x08000000` with length 0.
    - The D-18 amendment is written as an amendment: D-10's untouched elements are named, only the quantum changes, and the 7680 B of deliberate slack is recorded.
    - D-13's cost statement is present and says in plain terms that giving `BOOTLOADER` a non-zero length moves the application `ORIGIN` and is a migration rather than a resize.
    - An amendment section states that D-16's literal wording is not implementable, cites RM V0.2 §4.2.3.2 and `IS_FLASH_TYPEPROGRAM`, and records the corrected shape in which the completion of the single page program is the commit.
    - A `CONFIG_MAGIC` section records the value `0x52555250`, describes it as ASCII `RURP`, and states explicitly that it is a this-milestone choice and NOT vendored.
    - A validation-order section records `magic` then bounds-checked `length` then `crc32`, states that a record whose `length` exceeds the caller's buffer is rejected before any copy, and states that the CRC does not substitute for the bound check.
    - A section states that CRC32 detects accidental corruption only, provides no tamper resistance, and must never be described as authentication.
    - Sections record erase-before-program as a hard correctness requirement (C-8) and the write-protection failure mode (V4) with the requirement that a non-`HAL_OK` return yields `save() == false`.
    - A reset/interrupt section quotes RM V0.2 §4.2.3 on unprotected flash contents during a reset and on the bus stalling, and records D-14's first-boot cost as **not measured** — the words "acceptable" and any numeric latency figure are absent from that statement.
    - A host-contract section records `FLASH_BASE` `0x08000000`, `FLASH_SIZE` 131072, the 120K-vs-128K asymmetry marked correct-not-drift, the payload-scoped erase, the 2048 fallback note, and Phase 127's two obligations.
    - A claim-ceiling section states the ceiling by reference to `.planning/REQUIREMENTS.md` §"Validation Ceiling" and does not reproduce its forbidden-phrase list.
    - `python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py` run with `firestarter/platform/py32f071/CONFIG-STORAGE.md` named as an explicit target exits 0, and the invocation with its target is recorded verbatim.
    - `git show --stat HEAD` for this commit lists exactly one path: `platform/py32f071/CONFIG-STORAGE.md`.
    - `git hash-object platform/py32f071/linker/PY32F071xB_FLASH.ld` still returns `b32b5824c8e27492551db5c2b1d413f74f05b6f3`, and `git hash-object include/rurp_shield.h` still returns `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`.
    - `git rev-parse --abbrev-ref HEAD` in `/workspaces/firestarter` is `v1.23-py32f071-integration` after the commit.
    - `python3 -m pytest tests/ -q` still reports `86 passed` — this task adds no test module.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>The in-scope flash-config design is vendored on the milestone branch as a single-file commit that cites blob `4b1a441` by SHA, marks every superseded part superseded, records the 256 B / 8192 B geometry from the reference manual with an independent SDK corroboration, and stands as the ordering anchor every later linker edit is measured against.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the vendored design mechanically — tests/test_config_storage_design_vendored.py</name>
  <files>firestarter/tests/test_config_storage_design_vendored.py</files>
  <read_first>
    - `firestarter/tests/test_vpp_seam_manual_on_every_board.py` — read the module docstring
      (lines 1–76), the self-contained path-resolution block (lines 78–93), the textual drift-gate
      idiom (lines 333–386) and the self-enforcing no-skip leg (lines 447–468). **Copy the no-skip
      leg verbatim, including its concatenation trick** — the two needle strings are built by
      concatenation so the test's own assertion text does not trip its own check.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md`
      §`tests/test_config_storage_*.py` — the exact idioms, and the confirmed fact that **no
      `conftest.py`, `pytest.ini`, `pyproject.toml`, `setup.cfg` or `tox.ini` exists anywhere in
      this repo**. Self-contained per-module path resolution is the house rule, recorded as a
      decision rather than an omission.
    - `firestarter/platform/py32f071/CONFIG-STORAGE.md` — the file just written; the gate asserts
      against its actual content.
  </read_first>
  <behavior>
    Test functions this module must contain, each named for what it proves:
    - `test_design_doc_cites_the_vendored_blob_by_sha` — the doc contains `4b1a441` and names both
      closed-PR branches and PR numbers.
    - `test_design_doc_marks_every_superseded_module` — all seven superseded module identifiers are
      present AND each appears inside the SUPERSEDED section, not merely somewhere in the file.
    - `test_design_doc_records_the_flash_geometry_with_its_citation` — 256 and 8192 both present,
      the RM V0.2 section identifiers present, and the pinned SDK commit `0ed2f4b4…` present.
    - `test_design_doc_records_config_magic_as_not_vendored` — `0x52555250` present, and the
      sentence marking it a this-milestone rather than vendored choice present.
    - `test_design_doc_records_the_d16_amendment` — the amendment section exists and cites RM
      §4.2.3.2.
    - `test_design_doc_refuses_to_call_crc32_a_security_primitive` — the explicit
      not-a-security-primitive statement is present.
    - `test_design_doc_records_the_reserved_map_addresses` — `0x0801E000`, `0x0801E100`, `120K`,
      `8K` and `256` all present.
    - `test_helper_reports_a_violation_on_a_planted_copy` — the **RED demonstration**: a copy of the
      doc in `tmp_path` with the blob SHA citation stripped is fed to the same module-level helper
      the positive tests use, and the helper reports a violation. This is what makes the gate
      non-vacuous.
    - `test_compiler_is_required_not_optional` — the analog's self-enforcing no-skip leg, adapted:
      this module invokes no compiler, so the leg asserts only that the module contains no skip call
      and no conditional-skip marker.
  </behavior>
  <action>
    Create `firestarter/tests/test_config_storage_design_vendored.py`. It lives in `tests/`, which
    is PIO-invisible — **never** under `test/`, which is globbed into builds. It is a
    `tests/test_*.py` module and **not** a new `scripts/check_*.py`: Phase 125's C-11 measured a new
    checker at four artifacts plus bumps to `tests/test_checker_convention.py`'s `FLOOR = 5` and
    `FIXTURE_FLOOR = 10`, and those floors are scoped to `scripts/check_*.py` so a pytest module
    costs zero bumps.

    Open with the house module-docstring convention, uniform across `tests/`: the MIT header, then
    `Phase 126 Plan 01 -- <what>`, `Requirements: CFG-01, CFG-02`, `Decisions covered: D-01, D-13,
    D-16, D-17, D-18, D-19`, a rationale block, an explicit statement that this module **executes in
    NO CI leg on this branch** (`pytest tests/` runs only in `build.yml` on `main` and
    `beta-build.yml` on `beta`; `py32f071.yml` has no pytest step), the "self-contained path
    resolution, NOT in conftest.py" note, and a numbered `Coverage:` list mapping each test function
    to its requirement or decision.

    Imports: stdlib plus `pytest` only. No third-party import, no PlatformIO import, nothing under
    `.pio/` — a dependency on `.pio/libdeps/` passes on a warm tree and fails on a clean checkout,
    which is the fail-open shape this milestone has already paid for once.

    Resolve paths self-contained at module level, following the analog: `_HERE = Path(__file__).resolve().parent`,
    `_REPO_ROOT = _HERE.parent`, then the doc path `_REPO_ROOT / "platform" / "py32f071" / "CONFIG-STORAGE.md"`.

    Factor the checking logic into **one module-level helper** that takes a path (or the text) and
    returns a list of violations, so the positive tests and the planted-violation test exercise the
    same code. This is what makes the RED demonstration meaningful rather than a second, parallel
    implementation.

    Write the nine test functions listed in this task's behaviour block. For the superseded-module
    test, locate the SUPERSEDED section by heading and assert the seven identifiers appear **within
    that section's span**, not merely somewhere in the file — a bare file-wide substring test would
    pass on a document that mentioned the names while silently following them, which is precisely
    what CFG-01 forbids. Assertion messages must carry full context (the path, the missing item, and
    a short quote of the surrounding text) in the analog's style.

    For the planted-violation test, copy the real document into `tmp_path`, remove the blob-SHA
    citation from the copy, feed the copy to the helper, and assert the returned violation list is
    non-empty and names the missing citation. Never mutate the committed document.

    Do **not** use a skip call or a conditional-skip marker anywhere. Copy the analog's
    self-enforcing no-skip leg, including the concatenation trick that keeps the leg from tripping
    its own check.

    Run `python3 -m pytest tests/test_config_storage_design_vendored.py -v` and record the function
    names and the count. Then run the full `python3 -m pytest tests/ -q` and record the new total
    (86 plus the number of functions added). Commit the single new file with a message of the form
    `test(126-01): gate the vendored flash-config design and its recorded geometry`, then confirm
    the branch is still `v1.23-py32f071-integration`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_config_storage_design_vendored.py -v 2>&1 | tail -20 && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "checkers=$(ls scripts/check_*.py | wc -l)" && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/tests/test_config_storage_design_vendored.py` exists under `tests/`, not under `test/`.
    - The module's docstring carries the MIT header, the `Phase 126 Plan 01` attribution line, a `Requirements:` line naming CFG-01 and CFG-02, a `Decisions covered:` line naming D-01, D-13, D-16, D-17, D-18 and D-19, the explicit statement that the module executes in NO CI leg on this branch, the self-contained-path-resolution note, and a numbered `Coverage:` list with one entry per test function.
    - The module imports only stdlib and `pytest`. It references no path under `.pio/` and imports no PlatformIO module.
    - Path resolution is at module level from `Path(__file__).resolve().parent`, with no `conftest.py` created.
    - All nine test functions named in the behaviour block exist with those names, and `pytest tests/test_config_storage_design_vendored.py -v` reports every one of them as passed.
    - The superseded-module test locates the SUPERSEDED section by heading and asserts the seven identifiers within that section's span; a file-wide substring search alone is not accepted.
    - `test_helper_reports_a_violation_on_a_planted_copy` copies the real document into `tmp_path`, strips the blob-SHA citation from the copy, feeds it to the same module-level helper the positive tests call, and asserts a non-empty violation list naming the missing citation. The committed document is not mutated: `git hash-object platform/py32f071/CONFIG-STORAGE.md` is unchanged before and after the test run, and this is recorded.
    - The module contains no skip call and no conditional-skip marker, and carries the analog's self-enforcing leg proving it — with the two needle strings built by concatenation so the leg does not trip its own check.
    - `python3 -m pytest tests/ -q` reports `86` plus the number of functions this module adds, and both numbers are recorded.
    - `ls scripts/check_*.py | wc -l` is still `5`; `tests/test_checker_convention.py` is not modified.
    - `git show --stat HEAD` for this commit lists exactly one path.
    - `git rev-parse --abbrev-ref HEAD` in `/workspaces/firestarter` is `v1.23-py32f071-integration` after the commit.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>CFG-01's required content is enforced by an exit code rather than by prose, the gate is proven able to fail on a planted copy, and it costs zero checker-floor bumps because it is a `tests/test_*.py` module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closed-PR blob → the live milestone branch | A design contract crosses from an unmerged, partly-superseded document into a document later phases will cite as authoritative |
| the reference manual → the linker script | The one number in this phase that cannot be derived from the tree crosses here; two wrong values sit in the two most reachable places |
| this document → Phase 129's PCB record | PCB-03 quotes the flash budget "as actually reserved"; a wrong or unmarked figure here becomes a hardware decision later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-01-01 | Tampering | the recorded flash page size | high | mitigate | Two independent sources cited in the doc (RM V0.2 §4.1/§4.2.1/Table 4-1 and the pinned SDK part header at a full 40-char commit), plus an explicit do-not-use note naming both wrong values a reader would otherwise find (C-1). The gate asserts both 256 and 8192 with their citations present |
| T-126-01-02 | Repudiation | a design doc that silently follows superseded prescriptions | high | mitigate | CFG-01's SUPERSEDED block is enforced section-scoped by `test_design_doc_marks_every_superseded_module`, so a document that merely mentions the seven module names without marking them fails |
| T-126-01-03 | Repudiation | an overclaim that `CONFIG_MAGIC` was vendored | medium | mitigate | D-19's value is recorded as a this-milestone choice with the reason (the blob specifies the field, not the value), and the gate asserts that sentence is present — the Phase-122 C-5 shape, caught before it reaches a stranger |
| T-126-01-04 | Repudiation | a locked decision silently reinterpreted | high | mitigate | D-16's amendment is written as an explicit amendment citing RM §4.2.3.2 and `IS_FLASH_TYPEPROGRAM`, and gated. D-18 is written as an amendment to D-10's quantum with D-10's untouched elements named |
| T-126-01-05 | Denial of service | a gate that passes without observing anything | high | mitigate | `test_helper_reports_a_violation_on_a_planted_copy` feeds a stripped copy in `tmp_path` to the same helper and requires a violation. Phases 118 and 124 each shipped a hollow gate that had to be unwound |
| T-126-01-06 | Information disclosure | describing CRC32 as tamper resistance | medium | mitigate | An explicit V6 paragraph states CRC32 detects accidental corruption only and that anyone able to write flash can recompute it; the gate asserts the paragraph exists |
| T-126-01-07 | Tampering | the claim gate tripped by the document's own compliance text | medium | mitigate | The ceiling is stated **by reference** to `.planning/REQUIREMENTS.md` §"Validation Ceiling" rather than by reproducing its phrase list — the exact self-reference trap all six Phase-125 SUMMARY files fell into (125-RESEARCH C-16) |
| T-126-01-08 | Spoofing | a commit landing on the wrong branch | medium | mitigate | `git rev-parse --abbrev-ref HEAD` re-checked in both repos after every commit — an unanchored ROADMAP-prose regex was observed moving HEAD live on 2026-07-30 (RESEARCH Pitfall 7) |

**Package legitimacy:** not applicable. This plan adds no `npm`, `pip`, `cargo` or PlatformIO
`lib_deps` entry. `pytest` 9.1.1 and `g++` 14.2.0 are already present; `pypdf` was used for research
only and must not appear in any manifest. No `T-126-SC` row is owed by this plan.
</threat_model>

<verification>
1. The pre-phase pin is recorded: eleven blob SHAs with match verdicts, the `CONFIG_VERSION` line verbatim, firmware `HEAD`, a zero-line porcelain named to the firmware repo, the host repo's five known pre-existing lines recorded as pre-existing, the manifest gate at 24 enforced sources, `pytest tests/` at 86, five checkers, both pinned native envs at 141/17, and the three AVR flash/RAM figures with leonardo's 2656 B free flash.
2. `platform/py32f071/CONFIG-STORAGE.md` exists, cites blob `4b1a441` by SHA, names PRs #46/#47 and their closed branches, marks all seven superseded modules, routes DAC-VPP and calibration to FUT-VPP/FUT-CAL, records the 256 B / 8192 B geometry with both citations and both do-not-use traps, records the reserved map and the D-18 amendment, records D-13's migration cost, records D-16's amendment with the RM citation, records `CONFIG_MAGIC 0x52555250` as not vendored, records the V5 validation order, the V6 CRC32 statement, the C-8 erase-before-program requirement, the V4 write-protection failure mode, the C-7 quotes with D-14's not-measured cost, and the D-12 host contract.
3. That document landed in a commit of its own — `git show --stat` lists exactly one path — and no commit in this plan touched the linker script or `include/rurp_shield.h` (both blob SHAs unchanged).
4. `check_permitted_claims.py` exits 0 with `firestarter/platform/py32f071/CONFIG-STORAGE.md` named as an explicit target, and the invocation is recorded verbatim.
5. `tests/test_config_storage_design_vendored.py` exists with all nine named functions passing, including the planted-violation RED demonstration in `tmp_path`, and it contains no skip call or conditional-skip marker.
6. `pytest tests/ -q` moved from 86 to 86 plus the number of new functions; `ls scripts/check_*.py | wc -l` is still 5.
7. Both repos are still on their milestone branches after every commit.
8. No requirement checkbox was ticked.
</verification>

<success_criteria>
- CFG-01: the in-scope flash-config design is vendored on the milestone branch, citing blob `4b1a441` by SHA and its closed-PR homes, with every part superseded by PR #48's actual module layout explicitly marked superseded rather than silently followed — enforced by a section-scoped gate, not by prose.
- CFG-02 (first half): the PY32F071xB page and erase-unit sizes are recorded from the Puya reference manual with an independent SDK corroboration, in a commit that exists and precedes every later linker-script commit this phase makes. The second half — the ordering proven as an exit code — is Plan 126-06's.
- The three amended decisions are visible as amendments: D-08's fourth edit is flagged for Plan 126-08, D-18 refines D-10's quantum with D-10's untouched elements named, and D-16's literal wording is recorded as not implementable with the reference-manual reason.
- The new gate costs zero checker-floor bumps and is proven able to fail.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**. A drift-verification pass that
treats these as pre-existing identifiers will flag them incorrectly; none has a prior definition on
this branch in either repository.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file (vendored design + geometry record) | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file (CFG-01 gate) | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file (CFG-04 regression) | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` | function (seam declaration + two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `rurp_config_storage_save` | function (seam declaration + two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file (CFG-03 gate) | Plan 126-05 |
| `__config_page_size` | linker symbol (`PROVIDE`d) | Plan 126-06 |
| `__config_slot_a_start` | linker symbol (`PROVIDE`d) | Plan 126-06 |
| `__config_slot_b_start` | linker symbol (`PROVIDE`d) | Plan 126-06 |
| `__config_region_end` | linker symbol (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` | linker `MEMORY` region | Plan 126-06 |
| `BOOTLOADER` | linker `MEMORY` region (zero length, D-13 seam) | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file (CFG-02 ordering gate) | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file (CFG-06 gate) | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.cpp` | new file (HAL-free core) | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.h` | new file (local header) | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `StoredConfiguration` | struct type (vendored per D-17) | Plan 126-07 |
| `CONFIG_MAGIC` | macro / constant `0x52555250` (D-19) | Plan 126-07 |
| `rurp_flash_primitives_t` | struct type (injected primitive table) | Plan 126-07 |
| `rurp_dualslot_load` | function (HAL-free core entry point) | Plan 126-07 |
| `rurp_dualslot_save` | function (HAL-free core entry point) | Plan 126-07 |
| `rurp_config_crc32` | function (bitwise reflected CRC-32, poly `0xEDB88320`) | Plan 126-07 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (CFG-05, six named fns + KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file (CFG-07 gate) | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo, evidence artifact) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07),
and the `# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in
`firestarter/platform/py32f071/CMakeLists.txt` (Plan 126-03, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-01-SUMMARY.md`
when done. Record: the eleven pre-phase blob SHAs with their match/mismatch verdicts; the
`CONFIG_VERSION` line verbatim with its line number; the measured host `sizeof(rurp_configuration_t)`
with the AVR and computed ARM values beside it; the firmware `HEAD` and the zero-line porcelain named
to the firmware repo, plus the host repo's five known pre-existing lines recorded as pre-existing;
the manifest gate's enforced/exempt/allow-listed figures (24 / 14 / 5); the `pytest tests/` count
before and after; the checker count (5, unchanged); the recorded native and AVR figures with
leonardo's 2656 B free flash and the note that CONTEXT's 2600 B is stale; the two size comparators
named with their baseline files; the `CONFIG-STORAGE.md` commit SHA with its single changed path; the
verbatim `check_permitted_claims.py` invocation with its explicit target and its exit code; the nine
gate-function names with their pass results; the planted-violation demonstration's observed violation
message; and the branch re-check result after every commit in both repos.
</output>
