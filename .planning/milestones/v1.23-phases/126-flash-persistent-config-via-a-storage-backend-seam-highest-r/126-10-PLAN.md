---
phase: 126-flash-persistent-config-via-a-storage-backend-seam-highest-r
plan: 10
type: execute
wave: 7
depends_on: ["126-08"]
files_modified:
  - firestarter/tests/test_config_schema_pinned.py
autonomous: true
requirements: [CFG-07]

must_haves:
  truths:
    - "CFG-07 (schema half): a committed gate asserts `rurp_configuration_t` still has exactly the four fields `char version[6]`, `long r1`, `long r2`, `uint8_t hardware_revision`, in that order, and that `CONFIG_VERSION` in `include/rurp_shield.h` is still the literal `VER06`"
    - "CFG-07 (deletion half): the gate asserts `platform/py32f071/src/config.cpp` is **absent from the tree** — Criterion 5's \"verified by its absence\", a path-existence check rather than a diff"
    - "D-17: the gate asserts the vendored `StoredConfiguration` embeds `rurp_configuration_t` **as a whole member**, which is what makes \"schema unchanged\" structurally true rather than merely asserted, and that the wrapper's `version` is a `uint16_t` distinct from the `char[6]` `CONFIG_VERSION`"
    - "D-07: the gate asserts all four public config functions are declared in `include/rurp_shield.h` and defined in `src/rurp_config_utils.cpp`, and that no second definition of any of them exists anywhere under `platform/` — the drift CFG-07 removed must not come back"
    - "C-14: the gate asserts the seven config-API consumer sites still call only the four public functions, so a later change cannot quietly route a consumer around the policy layer"
    - "`VALUE_R1 270000` and `VALUE_R2 44000` at `include/rurp_shield.h:49-50` are asserted unchanged, because `rurp_validate_config`'s write-back writes them and a silent change would alter every defaulting board's stored record without a version signal"
    - "D-01: the gate is a `tests/test_*.py` pytest module, executes in ZERO CI legs on this branch, and costs no `scripts/check_*.py` floor bumps"
    - "Every assertion is demonstrated able to FAIL on planted copies in `tmp_path`, never by editing committed source"
  prohibitions:
    - statement: "The gate does NOT pin `include/rurp_types.h` or `include/rurp_shield.h` by blob SHA"
      status: required
      verification: "A committed test hardcoding a shared header's hash breaks the first time a later milestone legitimately edits it. The blob-SHA pins are plan-level proofs recorded in this phase's SUMMARY files and re-executed by `126-NONREGRESSION.md`; the committed gate uses textual anchors that actually exist — the Phase-125 drift-leg discipline"
    - statement: "The gate never calls a pytest skip nor a conditional-skip marker"
      status: required
      verification: "The analog's self-enforcing leg (`tests/test_vpp_seam_manual_on_every_board.py:447-468`) is copied verbatim with its concatenation trick"
    - statement: "The gate depends on no path under `.pio/`"
      status: required
      verification: "A dedicated function asserts it against the module's own text (C-12)"
    - statement: "No production source file is modified"
      status: required
      verification: "`git show --stat` for this plan's commit lists exactly one path"
    - statement: "No new `scripts/check_*.py` is created"
      status: required
      verification: "`ls scripts/check_*.py | wc -l` is still 5; `tests/test_checker_convention.py` is unmodified"
    - statement: "No requirement checkbox in `.planning/REQUIREMENTS.md` is ticked by this plan"
      status: required
      verification: "Only Plan 126-12 may tick CFG-01…CFG-07"
    - statement: "The two gitignored py32 worktrees are not written to"
      status: required
      verification: "`git -C /workspaces/firestarter_py32_ci status --porcelain` and `git -C /workspaces/firestarter_app_py32 status --porcelain` unchanged"
  artifacts:
    - firestarter/tests/test_config_schema_pinned.py
  key_links:
    - "Criterion 5 asks for the deletion to be *verified by its absence from the tree*. A `git diff` showing a removal is not the same claim — it can be satisfied by a wrong path, which is the vacuous shape `124-VERIFICATION.md` recorded live. A path-existence assertion is the only form that means what the criterion says."
    - "The schema pin has to outlive this phase. `rurp_configuration_t` is the struct PR #45's `768580f` adds calibration fields to **before** its `CONFIG_VERSION` bump — a silent layout change with the version string still reading `VER06`. A future cherry-pick of that shape is exactly what this gate exists to catch, which is why it asserts the field list and the version literal together."
    - "The no-second-definition assertion is what keeps CFG-07 closed. PR #48's `config.cpp` was a second, drifted copy of the policy; the gate must make a reappearance of any of the four functions under `platform/` an exit code, not something a reviewer has to notice."
---

<objective>
Pin the configuration schema and the deletion as exit codes: the four-field struct in order, the
`VER06` literal, the two default resistance values, the vendored wrapper embedding the struct whole,
the four public functions defined exactly once, the seven consumers still routed through them, and
PR #48's `config.cpp` absent from the tree.

Purpose: CFG-07 has two halves and Criterion 5 states both. The schema half must survive this phase and
the next: `rurp_configuration_t` is precisely the struct a cherry-pick from the closed calibration PR
would grow fields on *before* bumping the version string, producing a silent layout change on every
board that already stores a record. The deletion half must be verified the way the criterion words it —
by absence from the tree, not by a diff that can pass on a wrong path.

Output: `firestarter/tests/test_config_schema_pinned.py`, green, with every assertion demonstrated able
to fail.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-CONTEXT.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-PATTERNS.md
@.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-08-SUMMARY.md

**Read order and precedence.** `126-CONTEXT.md` records D-01…D-19; `126-RESEARCH.md` **wins on verified
facts**. The relevant ones here are **C-14** (the seven consumer sites, all above the seam, none of
which changes) and **C-6** (three record sizes across three compilers — so this gate asserts the field
**list and order**, never a byte size).

**Repo layout.** Every path lives in the firmware submodule `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commits land inside that submodule; only the SUMMARY commits in the meta
repo. `firestarter_app` is out of scope (D-12). **Never write into `firestarter_py32_ci/` or
`firestarter_app_py32/`** (RESEARCH Pitfall 8). After every `gsd-tools query commit`, re-check
`git rev-parse --abbrev-ref HEAD` in both repos (RESEARCH Pitfall 7).

**Requirement ticking scope for this plan: NONE.** CFG-07 spans Plan 126-03 (schema untouched through
the split), Plan 126-08 (the deletion) and this plan (the gate). Do not tick CFG-01…CFG-07. Plan 126-12
is the only plan permitted to tick any of them.

**Claim ceiling.** No PY32F071 PCB exists. This gate reads source text and compiles nothing; it must
not state or imply any build or runtime claim. State the ceiling by reference to
`.planning/REQUIREMENTS.md` §"Validation Ceiling".
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/test_config_schema_pinned.py — the CFG-07 gate</name>
  <files>firestarter/tests/test_config_schema_pinned.py</files>
  <read_first>
    - `firestarter/include/rurp_types.h` — all 26 lines. `rurp_configuration_t` is at `:19-24` with the
      four fields `char version[6]`, `long r1`, `long r2`, `uint8_t hardware_revision`. **Verified
      byte-for-byte at pattern-map time.**
    - `firestarter/include/rurp_shield.h` — `CONFIG_VERSION "VER06"` at `:46`, `VALUE_R1 270000` at
      `:49`, `VALUE_R2 44000` at `:50`, and the four config declarations at `:61` and `:150-152`. **All
      verified.**
    - `firestarter/src/rurp_config_utils.cpp` — post-split; the four definitions and the `rurp_config`
      global.
    - `firestarter/platform/py32f071/src/config_storage_dualslot.h` — the `StoredConfiguration`
      definition, so the gate can assert `rurp_configuration_t` is embedded as a whole member and the
      wrapper's `version` is a distinct `uint16_t`.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-RESEARCH.md`
      §C-14 — the seven consumer sites with their file and line numbers: `src/firestarter.cpp:40`,
      `:103`, `:109`; `src/boards/rurp_common.cpp:53`; `include/rurp_hw_rev_utils.h:95`, `:101`;
      `src/hardware_operations.cpp:106`, `:119`; `platform/py32f071/src/py32f071_rurp_shield.cpp:297`.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-08-SUMMARY.md`
      — `config.cpp`'s four recorded drift points, captured before deletion. The gate's docstring should
      name them so a reader understands what the absence assertion is protecting.
    - `firestarter/tests/test_vpp_seam_manual_on_every_board.py` — the module docstring convention
      (1–76), self-contained path resolution (78–93), the textual drift-gate idiom (333–386), the
      negative-form assertion (412) and the self-enforcing no-skip leg (447–468).
  </read_first>
  <behavior>
    Functions this module must contain:
    - `test_rurp_configuration_t_has_exactly_the_four_pinned_fields` — the struct's member list is
      exactly `version` (a `char` array of 6), `r1` (`long`), `r2` (`long`), `hardware_revision`
      (`uint8_t`), **in that order**, with no fifth member.
    - `test_config_version_literal_is_ver06` — `CONFIG_VERSION` in `include/rurp_shield.h` is the
      literal `VER06`.
    - `test_default_resistance_values_are_unchanged` — `VALUE_R1` is 270000 and `VALUE_R2` is 44000.
    - `test_stored_configuration_embeds_the_struct_whole` — `StoredConfiguration` contains
      `rurp_configuration_t` as a single named member, and its own `version` member is a `uint16_t`
      distinct from `CONFIG_VERSION`.
    - `test_pr48_config_cpp_is_absent_from_the_tree` — the path does not exist. Criterion 5's
      verified-by-absence.
    - `test_the_four_public_functions_are_defined_exactly_once` — each of `rurp_get_config`,
      `rurp_load_config`, `rurp_save_config` and `rurp_validate_config` has exactly one definition in
      the tree, in `src/rurp_config_utils.cpp`, and **no definition anywhere under `platform/`**.
    - `test_the_four_public_functions_are_declared_in_rurp_shield_h` — all four declarations are present
      there.
    - `test_the_seven_consumers_call_only_the_public_api` — each of the seven C-14 sites is located by
      file and by the function it calls, all seven are found, and the count is asserted equal to seven
      so a silently removed consumer is a finding rather than a silent pass.
    - `test_helper_reports_violations_on_planted_copies` — the **RED demonstration**: planted copies in
      `tmp_path` (a fifth struct member; a reordered field list; a changed `CONFIG_VERSION` literal; a
      changed `VALUE_R1`; a second definition of `rurp_validate_config` under `platform/`; a
      `StoredConfiguration` with the struct's fields inlined instead of embedded) each fed to the same
      module-level helpers, each producing a non-empty violation list.
    - `test_module_has_no_pio_libdeps_dependency` and the self-enforcing no-skip leg.
  </behavior>
  <action>
    Create `firestarter/tests/test_config_schema_pinned.py` under `tests/` — never `test/`, and not a
    new `scripts/check_*.py`.

    Open with the house module-docstring convention: MIT header, `Phase 126 Plan 10 -- <what>`,
    `Requirements: CFG-07`, `Decisions covered: D-01, D-07, D-17`, a rationale block, the explicit
    statement that this module **executes in NO CI leg on this branch**, the self-contained-path note,
    and a numbered `Coverage:` list.

    Add two paragraphs to the docstring that carry the reasoning forward, because the next reader will
    not have this phase's context:
      - **What the absence assertion protects.** Name PR #48's deleted `platform/py32f071/src/config.cpp`
        and its four recorded drift points: a private static instead of the shared `rurp_config` global;
        a second `rurp_validate_config` whose condition added `|| r2 == 0` and whose body began with a
        `memset`, neither of which the common policy has; **no write-back call at all**, where the
        common policy has one; and a `rurp_save_config()` that validated, assigned to a static and
        **persisted nothing**. State that Criterion 5 asks for the deletion to be verified by absence
        from the tree, which a diff cannot do — a path-scoped diff can pass on a wrong path, the shape
        `124-VERIFICATION.md` recorded live.
      - **Why the schema pin outlives this phase.** `rurp_configuration_t` is the struct the closed
        calibration PR's `768580f` adds fields to **before** its `CONFIG_VERSION` bump, producing a
        silent layout change while the version string still reads `VER06` — strictly worse than a
        visible bump, because every board already storing a record would misparse it with no migration
        signal. That is why the field list and the version literal are asserted together.

    Imports: stdlib plus `pytest` only. Resolve paths self-contained at module level. Factor every check
    into module-level helpers returning violations, so the planted-copy test exercises the same code.

    Use the analog's textual-gate idiom throughout: plain `read_text()` plus regex extraction of the
    struct body, the macro definitions and the function definitions, with assertion messages carrying
    the path, the observed text and the expectation.

    **Assert the field list and its order, never a byte size or an offset.** Host `long` is 8 bytes and
    the target's is 4, so the struct measures 32 / 15 / 20 bytes on host / AVR / ARM and `g++ -m32` is
    unavailable here (C-6). A size assertion would be wrong on two of the three compilers.

    **Do not hardcode any blob SHA.** A committed gate pinning a shared header by hash breaks the first
    time a later milestone legitimately edits it; the blob-SHA pins for `include/rurp_types.h` and
    `include/rurp_shield.h` are plan-level proofs recorded in this phase's SUMMARY files and re-executed
    by `126-NONREGRESSION.md`.

    For `test_the_four_public_functions_are_defined_exactly_once`, scan `src/`, `platform/` and `lib/`
    for a **definition** (not a declaration) of each of the four names, assert exactly one hit each, and
    assert that hit is in `src/rurp_config_utils.cpp`. The no-definition-under-`platform/` half is what
    keeps CFG-07 closed against a reappearance of the drift.

    For `test_the_seven_consumers_call_only_the_public_api`, locate each of the seven C-14 sites by file
    and by the called function, assert all seven are found, and assert the total equals seven — so a
    consumer that is silently removed or rerouted is a finding rather than a silent pass. Record the
    seven observed locations in the assertion message.

    For the planted-copy test, build each variant in `tmp_path` and feed it to the same helpers. Never
    mutate a committed file; record the blob SHAs of `include/rurp_types.h`, `include/rurp_shield.h` and
    `src/rurp_config_utils.cpp` as unchanged after the run.

    Do not use a skip call or a conditional-skip marker; copy the analog's self-enforcing leg with its
    concatenation trick. Add the no-`.pio/` self-check.

    Run `python3 -m pytest tests/test_config_schema_pinned.py -v` and record every function name with
    its result plus the observed struct field list, the observed macro values and the seven consumer
    locations. Then run `python3 -m pytest tests/ -q` and record the new total. Commit the single new
    file with a message of the form
    `test(126-10): pin the config schema, the VER06 literal and PR #48's config.cpp absence`, then
    re-check the branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_config_schema_pinned.py -v 2>&1 | tail -25 && test ! -e platform/py32f071/src/config.cpp && echo "config.cpp ABSENT" && python3 -m pytest tests/ -q 2>&1 | tail -2 && echo "types=$(git hash-object include/rurp_types.h)" && echo "shield=$(git hash-object include/rurp_shield.h)" && echo "checkers=$(ls scripts/check_*.py | wc -l)" && git show --stat --format=%H HEAD | head -5 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/tests/test_config_schema_pinned.py` exists under `tests/`, imports only stdlib and `pytest`, references no path under `.pio/`, and resolves its paths self-contained with no `conftest.py`.
    - The module docstring carries the MIT header, the `Phase 126 Plan 10` attribution, `Requirements: CFG-07`, a `Decisions covered:` line naming D-01, D-07 and D-17, the no-CI-leg statement, the self-contained-path note, a numbered `Coverage:` list, and **both** required reasoning paragraphs — the four recorded drift points of the deleted `config.cpp`, and why the schema pin outlives this phase.
    - All ten functions named in the behaviour block exist and `pytest -v` reports every one passed.
    - The struct assertion checks the four field names, their types and their **order**, and rejects a fifth member. It contains no byte-size or offset literal.
    - `CONFIG_VERSION` is asserted as the literal `VER06`; `VALUE_R1` as 270000; `VALUE_R2` as 44000.
    - `StoredConfiguration` is asserted to embed `rurp_configuration_t` as a single named member, with its own `version` member a `uint16_t` distinct from `CONFIG_VERSION`.
    - `test_pr48_config_cpp_is_absent_from_the_tree` is a path-existence assertion, not a diff.
    - Each of the four public functions is asserted to have exactly one definition, located in `src/rurp_config_utils.cpp`, with no definition anywhere under `platform/`.
    - All four declarations are asserted present in `include/rurp_shield.h`.
    - The seven C-14 consumer sites are each located and asserted found, the total is asserted equal to seven, and the seven observed locations are recorded.
    - `test_helper_reports_violations_on_planted_copies` builds all six planted variants in `tmp_path`, feeds them to the same module-level helpers the positive tests call, and observes a non-empty violation list for each. No committed file is mutated: the blob SHAs of `include/rurp_types.h`, `include/rurp_shield.h` and `src/rurp_config_utils.cpp` are recorded unchanged after the run.
    - No blob SHA literal appears anywhere in the module.
    - The self-enforcing no-skip leg and the no-`.pio/` self-check are present and pass.
    - `python3 -m pytest tests/ -q` reports the Plan 126-09 total plus this module's function count, and both numbers are recorded.
    - `ls scripts/check_*.py | wc -l` is still `5`; `git show --stat HEAD` lists exactly one path; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>CFG-07 is an exit code in both halves: the four-field struct in order with the `VER06` literal and both default resistances pinned, the vendored wrapper embedding the struct whole, the four public functions defined exactly once and never under `platform/`, the seven consumers still routed through them, and PR #48's `config.cpp` verified absent from the tree — each demonstrated able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Record the CFG-07 evidence ledger at plan level</name>
  <files>(no files modified — evidence capture into this plan's SUMMARY)</files>
  <read_first>
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-01-SUMMARY.md`
      — the pre-phase blob SHAs of `include/rurp_types.h` and `include/rurp_shield.h`, and the
      `CONFIG_VERSION` line recorded verbatim with its line number.
    - `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-08-SUMMARY.md`
      — `config.cpp`'s four drift points captured before deletion, and the commit SHA that deleted it.
    - `.planning/phases/125-vpp-control-seam/125-CONTEXT.md` §C-15 — blob-SHA re-hash primary,
      `git status --porcelain` scoped by repo name as corroboration, never a path-scoped `git diff`.
  </read_first>
  <action>
    The committed gate uses textual anchors so it survives future milestones. The exact-bytes claim is a
    phase-scoped record, and this task writes it down where `126-NONREGRESSION.md` will re-execute it.

    Record, as observed values:
      - `git hash-object include/rurp_types.h` — must still equal
        `d3fe5203a91527bdb7b20a33843c81065e21c613`, the pre-phase value. CFG-07 forbids any change to
        `rurp_configuration_t`, and this is the strongest available proof.
      - `git hash-object include/rurp_shield.h` — must still equal
        `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`, the pre-phase value. D-09 forbids touching it.
      - `git log --oneline -- include/rurp_types.h include/rurp_shield.h` scoped to this phase's commit
        range — must be empty. Record the range used.
      - The `CONFIG_VERSION` line quoted verbatim with its line number, still reading the literal
        `VER06`.
      - `test ! -e platform/py32f071/src/config.cpp` — recorded as the absence proof, together with the
        commit SHA from Plan 126-08 that deleted it and the four drift points that SUMMARY captured
        before deletion. State explicitly that the drift record exists only in that SUMMARY now, and
        that this ledger carries it forward.
      - `git status --porcelain` for `/workspaces/firestarter`, **naming the repository**, as 0 lines.
        The host repo's five known pre-existing lines are noted separately as pre-existing, not as a
        finding.

    Then record the phase-level suite state so this plan does not leave it unknown: `pytest tests/ -q`
    at its current total, both pinned native envs at exactly 141 cases / 141 succeeded / 17 suites with
    the extended-timeout discipline, and `python3 scripts/check_cmake_manifest.py` at exit 0 with 26
    enforced sources.

    Finally, state the division of labour explicitly so the verifier does not look for the wrong proof
    in the wrong place: the **committed gate** uses textual anchors and will keep working across future
    milestones; the **exact-bytes claim** is this phase's record and is re-executed by
    `126-NONREGRESSION.md`; and a path-scoped `git diff` is corroboration only, never the primary proof
    (`124-VERIFICATION.md` records that shape passing vacuously on a wrong path).

    Do not modify any file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && echo "types=$(git hash-object include/rurp_types.h)" && echo "shield=$(git hash-object include/rurp_shield.h)" && grep -n 'define CONFIG_VERSION' include/rurp_shield.h && test ! -e platform/py32f071/src/config.cpp && echo "config.cpp ABSENT" && echo "fw-porcelain=$(git status --porcelain | wc -l)" && python3 -m pytest tests/ -q 2>&1 | tail -2 && python3 scripts/check_cmake_manifest.py && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git hash-object include/rurp_types.h` is recorded and equals `d3fe5203a91527bdb7b20a33843c81065e21c613`; a mismatch is reported as a STOP finding.
    - `git hash-object include/rurp_shield.h` is recorded and equals `602fe6f326a042ab71efd111e4dfcf3a6e41dd46`; a mismatch is reported as a STOP finding.
    - The phase-scoped `git log` over both header paths is recorded as empty, with the commit range used stated explicitly.
    - The `CONFIG_VERSION` line is quoted verbatim with its line number and reads the literal `VER06`.
    - The absence of `platform/py32f071/src/config.cpp` is recorded as a path-existence result, alongside Plan 126-08's deletion commit SHA and the four drift points carried forward from that SUMMARY.
    - `git status --porcelain` is recorded for `/workspaces/firestarter` by name as 0 lines, with the host repo's five known pre-existing lines noted separately as pre-existing.
    - `pytest tests/ -q` is recorded at its current total; both pinned native envs at 141 cases / 141 succeeded / 17 suites with the extended-timeout discipline stated; `check_cmake_manifest.py` at exit 0 with 26 enforced sources.
    - The division of labour is stated: textual anchors in the committed gate, exact bytes in this ledger and in `126-NONREGRESSION.md`, path-scoped diffs as corroboration only with `124-VERIFICATION.md` cited.
    - No file was modified by this task; `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`.
    - No requirement checkbox in `.planning/REQUIREMENTS.md` was ticked.
  </acceptance_criteria>
  <done>CFG-07's exact-bytes claim is recorded where it belongs — both header blob SHAs unchanged since before the phase, no phase commit on either path, the `VER06` literal quoted, and PR #48's `config.cpp` absent with its drift record carried forward — with the committed gate's textual-anchor role stated so neither proof is mistaken for the other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the config schema → every board already storing a record | A silent field addition changes the on-EEPROM layout with the version string unchanged, and no migration signal reaches the host |
| the common policy → a per-platform reimplementation | PR #48's `config.cpp` was exactly that; the gate must make a reappearance an exit code |
| a deletion claim → the tree | Criterion 5 asks for absence; a diff can be satisfied on a wrong path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-10-01 | **Tampering** | a silent `rurp_configuration_t` layout change | **high** | mitigate | The gate asserts the four field names, types and order together with the `VER06` literal — the exact shape PR #45's `768580f` would produce (fields added before the version bump), which the docstring names so the next reader understands what is being caught |
| T-126-10-02 | **Repudiation** | a second, drifted policy reappearing under `platform/` | **high** | mitigate | `test_the_four_public_functions_are_defined_exactly_once` asserts one definition each, located in the common policy TU, with none under `platform/`; demonstrated able to fail on a planted second definition |
| T-126-10-03 | Repudiation | a deletion claim satisfiable on a wrong path | medium | mitigate | A path-existence assertion, which is what Criterion 5 words. `124-VERIFICATION.md` records a path-scoped diff pipeline reported as "(empty)" while a trailer survived the grep |
| T-126-10-04 | Tampering | a default resistance value changed without a version signal | medium | mitigate | `VALUE_R1` and `VALUE_R2` asserted at 270000 and 44000; `rurp_validate_config`'s write-back writes them, so a silent change would alter every defaulting board's stored record |
| T-126-10-05 | Tampering | the vendored wrapper inlining the struct's fields | medium | mitigate | `test_stored_configuration_embeds_the_struct_whole` — embedding as a single member is what makes "schema unchanged" structural rather than asserted (D-17) |
| T-126-10-06 | Repudiation | a consumer silently rerouted around the policy layer | medium | mitigate | The seven C-14 sites are located individually and the total asserted equal to seven, so a removal is a finding rather than a silent pass |
| T-126-10-07 | Denial of service | a committed gate pinning a shared header by hash | medium | mitigate | Prohibited; the gate uses textual anchors and the exact-bytes claim is a plan-level record re-executed by `126-NONREGRESSION.md` |
| T-126-10-08 | Repudiation | a size assertion wrong on two of three compilers | medium | mitigate | The gate asserts the field list and order only; host `long` is 8 bytes, the target's is 4, and `g++ -m32` is unavailable (C-6) |
| T-126-10-09 | Spoofing | a commit landing on the wrong branch | medium | mitigate | `git rev-parse --abbrev-ref HEAD` re-checked in both repos after the commit (RESEARCH Pitfall 7) |

**Package legitimacy:** not applicable. No `npm`, `pip`, `cargo` or PlatformIO `lib_deps` entry is
added. No `T-126-SC` row is owed.
</threat_model>

<verification>
1. `tests/test_config_schema_pinned.py` exists with all ten named functions passing, stdlib+pytest only, self-contained paths, no `.pio/` reference, no blob SHA literal, no skip.
2. The struct's four fields, their types and their order are asserted with no size or offset literal; a fifth member is rejected.
3. `CONFIG_VERSION` is `VER06`; `VALUE_R1` is 270000; `VALUE_R2` is 44000.
4. `StoredConfiguration` embeds `rurp_configuration_t` whole, with its own `uint16_t version` distinct.
5. `platform/py32f071/src/config.cpp` is asserted absent by path existence.
6. The four public functions have exactly one definition each, in the common policy TU, with none under `platform/`; all four declarations are in `include/rurp_shield.h`.
7. The seven C-14 consumer sites are found and counted, with their locations recorded.
8. Six planted copies each produce violations from the same helpers; no committed file mutated; three blob SHAs re-asserted.
9. The plan-level ledger records both header blob SHAs at their pre-phase values, an empty phase-scoped log over both paths, the `VER06` line verbatim, the absence proof with Plan 126-08's deletion commit and the four drift points, and the firmware porcelain at 0 lines named to the repository.
10. `pytest tests/` grew by the module's function count; both native envs at 141/17; manifest at 26 enforced sources; one commit, one path; both repos on their milestone branches; no requirement ticked.
</verification>

<success_criteria>
- CFG-07 is enforced as an exit code in both halves — the schema and version pinned by field list and literal, and the deletion verified by absence from the tree exactly as Criterion 5 words it.
- The drift PR #48 introduced cannot come back unnoticed: a second definition of any public config function under `platform/` is a test failure.
- The gate survives future milestones because it uses textual anchors rather than blob hashes, and the exact-bytes claim is recorded separately where it belongs.
- Every assertion is demonstrated able to fail on an independent planted copy.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **newly created by this phase**; none has a prior definition on this
branch in either repository. A drift-verification pass treating them as pre-existing will flag them
incorrectly.

| Symbol / path | Kind | Created by |
|---|---|---|
| `firestarter/platform/py32f071/CONFIG-STORAGE.md` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_design_vendored.py` | new file | Plan 126-01 |
| `firestarter/tests/test_config_storage_eeprom_regression.py` | new file | Plan 126-02 |
| `firestarter/include/rurp_config_storage.h` | new file (the seam header) | Plan 126-03 |
| `rurp_config_storage_load` / `rurp_config_storage_save` | seam functions (one declaration, two implementations) | Plan 126-03 (AVR), Plan 126-08 (ARM) |
| `firestarter/src/boards/rurp_config_storage_eeprom.cpp` | new file (AVR backend, pure move) | Plan 126-03 |
| `firestarter/tests/test_config_storage_seam_shape.py` | new file | Plan 126-05 |
| `__config_page_size` / `__config_slot_a_start` / `__config_slot_b_start` / `__config_region_end` | linker symbols (`PROVIDE`d) | Plan 126-06 |
| `CONFIG` / `BOOTLOADER` | linker `MEMORY` regions (or the A-7 fallback symbol pair) | Plan 126-06 |
| `firestarter/tests/test_flash_geometry_recorded_before_linker.py` | new file | Plan 126-06 |
| `firestarter/tests/test_py32_flash_map.py` | new file | Plan 126-06, extended by Plan 126-08 |
| `firestarter/platform/py32f071/src/config_storage_dualslot.{h,cpp}` | new files (HAL-free core) | Plan 126-07 |
| `StoredConfiguration` / `CONFIG_MAGIC` / `rurp_flash_primitives_t` | struct type / constant `0x52555250` / struct type | Plan 126-07 |
| `rurp_dualslot_load` / `rurp_dualslot_save` / `rurp_config_crc32` | functions | Plan 126-07 |
| `firestarter/platform/py32f071/src/config_storage_flash.cpp` | new file (HAL glue) | Plan 126-08 |
| `test_manifest_names_the_flash_driver` and five sibling functions | new test functions in `tests/test_py32_flash_map.py` | Plan 126-08 |
| `firestarter/tests/test_config_storage_dualslot.py` | new file (six named fns + CRC KAT) | Plan 126-09 |
| `firestarter/tests/test_config_schema_pinned.py` | new file | Plan 126-10 |
| `.planning/phases/126-…/126-NONREGRESSION.md` | new file (meta repo) | Plan 126-12 |

**Deleted by this phase:** `firestarter/platform/py32f071/src/config.cpp` (Plan 126-08, CFG-07) and the
`# PY32_EXCLUDED: src/rurp_config_utils.cpp` line in `firestarter/platform/py32f071/CMakeLists.txt`
(Plan 126-08, D-08).

<output>
Create `.planning/phases/126-flash-persistent-config-via-a-storage-backend-seam-highest-r/126-10-SUMMARY.md`
when done. Record: the module's ten function names with results; the observed struct field list in
order; the observed `CONFIG_VERSION`, `VALUE_R1` and `VALUE_R2` values; the `StoredConfiguration`
embedding observation; the seven consumer locations; the definition census for the four public
functions; the six planted-copy demonstrations with their observed violations; the three blob SHAs
unchanged after the planted-copy run; the plan-level ledger (both header blob SHAs, the empty
phase-scoped log with its commit range, the `VER06` line verbatim, the absence proof with Plan 126-08's
deletion commit SHA and the four drift points, the firmware porcelain named to the repository); the
stated division of labour between textual anchors and exact bytes; the `pytest tests/` total before and
after; both native env counts; the manifest gate at 26 enforced sources; the commit SHA with its single
changed path; and the branch re-check in both repos.
</output>
