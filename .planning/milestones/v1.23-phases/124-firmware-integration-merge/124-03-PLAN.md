---
phase: 124-firmware-integration-merge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/tests/test_golden_trace_identity.py
  - firestarter/tests/golden/sdp_expected_inventory.json
autonomous: true
requirements: [MERGE-06]

must_haves:
  truths:
    - "The golden-trace fixture test/native/avr/_shared/sdp_expected.h is pinned by BOTH its git blob SHA and a per-array name+entry-count inventory, so a deleted array whose consuming assertions vanish with it cannot pass silently (MERGE-06)"
    - "The inventory check runs in `pytest tests/`, which both firmware CI workflows already execute, and fails closed when `git` is unavailable"
    - "The pin is authored before the landing and holds unchanged across it — the blob is byte-identical before and after the merge (measured)"
  artifacts:
    - firestarter/tests/test_golden_trace_identity.py
    - firestarter/tests/golden/sdp_expected_inventory.json
  key_links:
    - "Nine arrays with the entry counts 54/54/54/54/54/30/30/30/33 are the recorded inventory; a whole-file blob match alone would not catch an array deletion accompanied by a matching test-suite edit"
    - "The recorded blob SHA dd1ba1cce60d8aa8934e8c067ed82ad85cfd3b83 must be RE-DERIVED at execute time with `git rev-parse HEAD:<path>`, never copied from a document"
---

<objective>
Give MERGE-06's second half — *"the golden register traces are byte-identical (per-array for
`_shared/sdp_expected.h`)"* — a mechanical proof, authored before the landing.

Purpose: the 141/17 case-and-suite count already covers "the two consuming suites pass". It does **not**
cover an array being deleted together with the assertions that consume it: the counts stay 141/17 and
the suite reports green. RESEARCH §MERGE-06 states the requirement is discharged by three checks, and
only one of them (the suites passing) exists today. This plan adds the other two: whole-file blob
identity, and a per-array name + entry-count inventory.

Output: a pytest under `firestarter/tests/` (PIO-invisible, and executed by both firmware CI workflows
via `pytest tests/ -v`) plus a committed inventory JSON.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-PATTERNS.md

Repo layout: all edits land in the **`firestarter` submodule** at `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commit inside the submodule.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive and commit the per-array inventory</name>
  <files>firestarter/tests/golden/sdp_expected_inventory.json</files>
  <read_first>
    - `firestarter/test/native/avr/_shared/sdp_expected.h` — read the array declarations. RESEARCH
      records nine `static const sdp_strobe_t` arrays at lines 144, 195, 227, 253, 286, 337, 356, 372,
      405; grep handle: `grep -n "static const sdp_strobe_t" test/native/avr/_shared/sdp_expected.h`.
    - `firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md` around lines 521 and 837 — the
      blob-loop idiom and the `| path | base | head | unchanged? |` result-table shape this project
      already uses for exactly this kind of evidence.
    - `firestarter/tests/golden/` — look at what is already there so the new file follows the directory's
      existing conventions.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §MERGE-06 — the recorded nine
      names and counts, and the three-check decomposition.
  </read_first>
  <action>
    Derive the inventory **from the file**, never by transcribing RESEARCH's table. Write a one-shot
    derivation (a short inline `python3 -c` or a scratch script under
    `/tmp/claude-1000/.../scratchpad`, not committed) that, for each
    `static const sdp_strobe_t <NAME>[] = { ... };` declaration, records the array name and the number of
    brace-delimited entries in its initialiser, then emit
    `firestarter/tests/golden/sdp_expected_inventory.json` with this shape:

      {
        "meta": {
          "source": "test/native/avr/_shared/sdp_expected.h",
          "recorded_by": "Phase 124 Plan 03",
          "requirement": "MERGE-06",
          "blob_sha": "<the 40-hex output of `git rev-parse HEAD:test/native/avr/_shared/sdp_expected.h` at record time>",
          "recorded_at_head": "<the 40-hex `git rev-parse HEAD` at record time>",
          "why_two_checks": "<one sentence: a whole-file blob match alone cannot distinguish 'unchanged' from 'array deleted together with its consuming assertions'; the per-array inventory is what makes a deletion visible>",
          "how_to_update": "<one sentence: if this file legitimately changes, re-derive the inventory AND state in the commit message which array changed and why — never edit this JSON to match a surprise>"
        },
        "arrays": [ { "name": "...", "entries": N }, ... ]
      }

    Cross-check the derived result against RESEARCH's recorded table (9 arrays; entry counts
    54,54,54,54,54,30,30,30,33) and **report any disagreement in the SUMMARY as a finding** rather than
    silently adopting either side.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "import json;d=json.load(open('tests/golden/sdp_expected_inventory.json'));print(len(d['arrays']), sum(a['entries'] for a in d['arrays']), d['meta']['blob_sha'])"</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints `9` as the array count and the observed entry-sum; both integers are
      recorded in the SUMMARY.
    - `meta.blob_sha` equals the live output of
      `git rev-parse HEAD:test/native/avr/_shared/sdp_expected.h` (re-derived at execute time, not copied
      from any document). Record the observed SHA in the SUMMARY and state explicitly whether it matches
      RESEARCH's recorded `dd1ba1cce60d8aa8934e8c067ed82ad85cfd3b83`.
    - Every entry in `arrays` has a non-empty `name` and an integer `entries` >= 1.
    - `meta` contains non-empty `why_two_checks` and `how_to_update` strings.
  </acceptance_criteria>
  <done>A committed, machine-readable inventory of the golden trace exists, derived from the file and cross-checked against the research record.</done>
</task>

<task type="auto">
  <name>Task 2: Write the identity pytest, fail-closed on a missing git</name>
  <files>firestarter/tests/test_golden_trace_identity.py</files>
  <read_first>
    - `firestarter/tests/test_check_build_warnings.py` lines 114-167 — the `_HERE/_REPO_ROOT/_FIXTURES`
      path triple and, critically, `_resolve_compiler()`'s **fail-closed, never-skipped** external
      dependency idiom (128-145). Copy that shape for `git`, including the reasoning comment: a missing
      external tool must FAIL, never `pytest.skip`, because skipping recreates the BASE-02/BASE-03
      absence-proxy failure class Phase 123 removed.
    - `firestarter/tests/test_check_orphan_provisional.py` lines 1-90 — the module-docstring shape (MIT
      header, `Phase N Plan NN`, `Requirements:`, `Decisions covered:`, numbered `Coverage:` list, and the
      verbatim "Self-contained path resolution below -- NOT in conftest.py" house-rule note).
  </read_first>
  <action>
    Create `firestarter/tests/test_golden_trace_identity.py`. Stdlib + pytest only, no third-party deps,
    no `conftest.py`.

    Module docstring: MIT header; `Phase 124 Plan 03 — MERGE-06's per-array golden-trace identity pin`;
    `Requirements: MERGE-06`; a paragraph stating the defect class this closes (an array deleted together
    with its consuming `TEST_ASSERT` calls keeps the suite at 141 cases / 17 suites and reports green — a
    count assertion cannot see it); the "Self-contained path resolution" house-rule note verbatim; and a
    numbered `Coverage:` list.

    Module-level helpers:
      - `_resolve_git()` — `shutil.which(os.environ.get("GIT", "git"))`, asserted non-None with a message
        stating this must FAIL the suite and must never be skipped. No `pytest.skip` anywhere in the module.
      - `_git(*args)` — list-argv `subprocess.run` with `cwd=_REPO_ROOT`, `capture_output=True`,
        `text=True`; assert returncode 0 with a message reproducing stderr.
      - `_parse_arrays(text)` — re-derive the `(name, entries)` pairs from the header text with the same
        rule Task 1 used. Keep the parser in the test, not in a shared module: the point is that the
        inventory and the file are compared by two independent readings.

    Tests (one per numbered Coverage item):
      1. `test_blob_sha_matches_the_recorded_inventory` — `git rev-parse HEAD:test/native/avr/_shared/sdp_expected.h`
         equals `meta.blob_sha`. The assertion message must name both SHAs and tell the reader that a
         legitimate change requires re-deriving the inventory and saying which array changed and why.
      2. `test_array_names_match_the_recorded_inventory` — the ordered list of names parsed from the file
         equals the ordered list in the JSON. Assertion message prints both lists.
      3. `test_array_entry_counts_match_the_recorded_inventory` — per-array entry counts match; the
         message names the **first** diverging array and both counts (positional, never a bare
         "lists differ"; mirrors `sdp_expected.h`'s own `sdp_first_divergence` discipline).
      4. `test_inventory_is_non_vacuous` — the JSON has >= 9 arrays and every `entries` >= 1; an empty or
         truncated inventory must FAIL, not pass. Name the guard in the message.
      5. `test_consuming_suites_still_include_the_fixture` — assert
         `test/native/avr/test_sdp_harness/test_sdp_harness.cpp` and
         `test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp` each contain the literal
         `_shared/sdp_expected.h` include. This is the link that makes the fixture load-bearing; if both
         consumers stopped including it, the blob could stay identical while nothing used it.
      6. `test_git_is_required_not_optional` — assert this module's own source contains no `pytest.skip`
         and no `@pytest.mark.skipif`, i.e. the fail-closed contract is self-enforcing.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_golden_trace_identity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_golden_trace_identity.py -q` reports **6 passed, 0 skipped, 0 failed**.
    - `python3 -m pytest tests/test_golden_trace_identity.py -q -rs` shows **zero** `SKIPPED` lines.
    - `grep -c 'pytest.skip\|mark.skipif' tests/test_golden_trace_identity.py` returns **0**.
    - `grep -c 'shell=True' tests/test_golden_trace_identity.py` returns **0**.
    - Negative control, run manually and recorded in the SUMMARY: copy the header to a `tmp_path`, delete
      one array, and confirm `_parse_arrays` on the mutated text yields 8 names — proving the parser
      genuinely counts rather than returning a constant. (This is a one-shot demonstration; it does not
      need to be a committed test, but its observed output must be quoted in the SUMMARY.)
    - `python3 -m pytest tests/ -q` reports **0 failed**; record the new total.
  </acceptance_criteria>
  <done>MERGE-06's per-array clause is a mechanical assertion that runs in both firmware CI workflows and cannot be satisfied by a green-but-hollow suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden trace fixture -> the "byte-identical traces" claim | The only evidence AVR bus behaviour is unchanged across this landing |
| test suite count -> coverage claim | 141/17 is invariant under a deletion that removes both an array and its assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-09 | Repudiation | array deleted with its consuming assertions, suite still green | high | mitigate | Per-array name + entry-count inventory compared by an independent parser in the test |
| T-124-10 | Tampering | the inventory JSON edited to match a surprise instead of investigating it | medium | mitigate | `meta.how_to_update` records the required behaviour; assertion messages restate it; the blob SHA makes any file edit visible in the same run |
| T-124-11 | Repudiation | `git` absent in some environment turning the pin into a silent skip | high | mitigate | Fail-closed `_resolve_git()`; grep-asserted absence of `pytest.skip`; a self-checking test for that property |
| T-124-12 | Repudiation | both consumers stop including the fixture, leaving it inert but identical | medium | mitigate | Coverage 5 asserts the include is present in both consuming suites |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && python3 -m pytest tests/ -q -rs` -> 0 failed, 0 skipped; record the total.
2. `git rev-parse HEAD:test/native/avr/_shared/sdp_expected.h` -> record the observed blob SHA.
3. `git -C /workspaces/firestarter status --porcelain` names only this plan's two paths.
</verification>

<success_criteria>
- MERGE-06's per-array clause is discharged by an exit code, not by prose.
- The pin holds on the pre-landing tree and is expected to hold unchanged across the landing (Plan 124-04 re-runs it as part of the post-landing sweep).
- The check fails closed when `git` is missing, in the same shape Phase 123 established for `g++`.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| `firestarter/tests/golden/sdp_expected_inventory.json` | new recorded inventory (9 arrays, blob SHA, provenance) |
| `firestarter/tests/test_golden_trace_identity.py` | new pytest module (6 cases) |
| `_resolve_git()`, `_git()`, `_parse_arrays()` | new test-module helpers |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-03-SUMMARY.md` when done. Record: the derived
array names and entry counts, the observed blob SHA and whether it matches RESEARCH's recorded value, the
negative-control output, and the new `pytest tests/ -q` total.
</output>
