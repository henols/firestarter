---
phase: 124-firmware-integration-merge
plan: 07
type: execute
wave: 3
depends_on: ["124-04"]
files_modified:
  - firestarter/platform/py32f071/cmake/write_checksums.cmake
  - firestarter/platform/py32f071/src/main.cpp
autonomous: true
requirements: [MERGE-08]

must_haves:
  truths:
    - "platform/py32f071/cmake/write_checksums.cmake is deleted, with its zero-consumer status re-proven against the landed tree rather than accepted from the research record (D-01)"
    - "main.cpp passes the one-wait-state flash-latency constant at 48 MHz, not the raw ACR bit-mask that resolves to two wait states (D-04)"
    - "A compile-time regression guard makes reintroducing the ACR mask a build failure, with an honest comment stating why the preferred clock-derived assertion is not achievable (C-5)"
    - "The write-up frames the latency setting as over-conservative but safe, and as a deliberate 2026-07-21 workaround that was never reverted — not as a typo and not as a correctness bug (C-6)"
  artifacts:
    - firestarter/platform/py32f071/src/main.cpp
  key_links:
    - "Neither change is verifiable locally — arm-none-eabi-gcc, cmake and ninja are absent. The only mechanical check is Plan 124-11's CI run URL + SHA; a local pio run is not evidence about ARM"
    - "The static_assert operands must both be SDK compile-time macros; RCC_HSICALIBRATION_24MHz dereferences a factory-trim address at runtime and can never appear in one"
---

<objective>
Close the two remaining MERGE-08 in-branch defects: the orphaned `write_checksums.cmake` and the
flash-latency constant.

Purpose: MERGE-08 names three defects. Plan 124-06 closed the `DEV_TOOLS` one. This plan closes the other
two, each as its own attributable change after the landing (D-03's ordering), and each written up honestly
against the research corrections — `FLASH_ACR_LATENCY_1` is **not** a typo (C-6) and D-04's preferred
compile-time proof shape is **not achievable** (C-5).

Output: one deletion, one constant change, and one compile-time regression guard that can only be
evaluated by CI.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-CONTEXT.md
@.planning/phases/124-firmware-integration-merge/124-04-SUMMARY.md

Repo layout: edits land in the **`firestarter` submodule** at `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commit inside the submodule.

Hard environment constraint governing this plan: `arm-none-eabi-gcc`, `cmake` and `ninja` are **absent**
from this devcontainer. Nothing in this plan can be compiled here. Every ARM claim in the SUMMARY must be
written as "to be evidenced by Plan 124-11's CI run URL + commit SHA", never as a local result.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-prove zero consumers, then delete write_checksums.cmake (D-01)</name>
  <files>firestarter/platform/py32f071/cmake/write_checksums.cmake</files>
  <read_first>
    - `firestarter/platform/py32f071/cmake/write_checksums.cmake` — the whole file, so the deletion is made
      knowing what capability is being removed.
    - `firestarter/platform/py32f071/CMakeLists.txt` — confirm by reading that nothing `include()`s or
      otherwise references it.
    - `firestarter/.github/workflows/py32f071.yml` around lines 75-101 — the inline `sha256sum` steps that
      already produce the checksum artifact, which is why wiring the orphan up now would create a
      build-output contract Phase 128 immediately rewrites.
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` D-01 and
      `124-RESEARCH.md` correction C-15.
  </read_first>
  <action>
    Re-prove the zero-consumer claim against the **landed** tree, not against the research record and not
    against the source branch. Run a repo-wide search for the basename and for the stem across the whole
    firmware working tree and, separately, across `/workspaces/firestarter_app` — a host-side reference
    would be exactly the kind of cross-repo coupling this project has been bitten by. Record both search
    commands and their verbatim output.

    If and only if both searches return zero code references, `git rm` the file.

    Record in the commit message: that it had zero consumers on the landed tree (with the search commands
    quoted), that the checksum capability already exists inline in `py32f071.yml`, that Phase 128's
    `ad47c3b` stops publishing checksums as debug output so wiring this up now would create a contract
    Phase 128 immediately rewrites, and that if image integrity is wanted by Phase 127's DFU installer it
    returns **with a consumer attached**.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git grep -n write_checksums -- . ; echo "fw_hits=$?" ; cd /workspaces/firestarter_app && git grep -n write_checksums -- . ; echo "app_hits=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Both `git grep` searches return **no matches** (grep exit status 1) before the deletion; both commands and their outputs are recorded verbatim.
    - `test ! -e platform/py32f071/cmake/write_checksums.cmake` succeeds after the deletion.
    - `git ls-tree HEAD platform/py32f071/cmake/` lists `arm-none-eabi.cmake` and does **not** list `write_checksums.cmake`; the listing is recorded.
    - `python3 scripts/check_cmake_manifest.py` still exits **0** after the deletion (the deleted file is not a source-list entry, but the gate must be re-run rather than assumed unaffected).
    - The commit message contains all four recorded justifications named in the action.
  </acceptance_criteria>
  <done>The orphan is gone, its zero-consumer status was re-proven against the tree it is being deleted from, and the reason it is not being wired up instead is in the history.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the flash-latency constant and add a compile-time regression guard (D-04)</name>
  <files>firestarter/platform/py32f071/src/main.cpp</files>
  <read_first>
    - `firestarter/platform/py32f071/src/main.cpp` — the clock-configuration block around line 48, and the
      oscillator/PLL setup above it (`RCC_HSICALIBRATION_24MHz`, `RCC_PLL_MUL2`,
      `AHBCLKDivider = RCC_SYSCLK_DIV1`).
    - `firestarter/platform/py32f071/include/py32f071_hal_conf.h` — confirm `HAL_FLASH_MODULE_ENABLED` is
      defined and `py32f071_hal_flash.h` is included on the LANDED tree, because that is what makes the
      corrected constant in scope. This was restored by commit `d76910c` three minutes after the workaround
      commit `91c6e45`, and the workaround was never reverted.
    - `firestarter/platform/py32f071/CMakeLists.txt` — the pinned SDK `GIT_TAG` (a full SHA, not a tag or
      branch), which the new comment must cite by SHA.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §D-04 — the SDK header excerpt
      showing the three latency macros and their wait-state meanings, the reason the preferred proof shape
      is unachievable (C-5), and the severity framing (C-6).
  </read_first>
  <action>
    Change the flash-latency argument passed to the clock-configuration call so it names the SDK's
    one-wait-state latency constant rather than the raw ACR bit-mask that numerically equals the
    two-wait-state constant. Change nothing else in the clock setup — the configured 48 MHz is correct and
    is not in scope.

    Add a compile-time regression guard immediately beside the call: a `static_assert` whose two operands
    are both SDK compile-time macros, asserting that the one-wait-state constant is not equal to the ACR
    bit-mask, with a failure message stating that the ACR mask is the two-wait-state value and must not be
    reintroduced. This is a real guard because both operands are integral SDK macros; it is the honest
    maximum available.

    Add a comment above it recording, in this order:
      - what the two SDK macros mean, citing the SDK header path and the pinned SDK commit SHA read from
        `CMakeLists.txt`;
      - why the preferred proof is not achievable (C-5): the configured clock is not exposed as a
        compile-time constant, because the HSI calibration macro dereferences a factory-trim address at
        runtime and the PLL multiplier macro is a register-field value rather than a multiplier, so no
        `static_assert` can tie the chosen latency to the configured clock;
      - the honest severity framing (C-6): more wait states than required is functionally **safe**; this is
        an over-conservative setting, not a correctness fault. It arrived as a deliberate 2026-07-21
        workaround made while the flash HAL module was not enabled, and was never reverted once the module
        was restored three minutes later. Name both commits by SHA.
      - that this guard can only be evaluated by the ARM toolchain in CI, since no local ARM compiler
        exists.

    Write the SUMMARY's claim about this change in the permitted form: the constant is corrected and the
    guard is present in source; whether it compiles is evidenced only by Plan 124-11's CI run URL and head
    SHA. Do not write, in any artifact, that the change was verified, built, or validated locally.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -n 'FLASH_' platform/py32f071/src/main.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'FLASH_ACR_LATENCY_1' platform/py32f071/src/main.cpp` returns **1**, and reading that single hit confirms it is inside the `static_assert` regression guard — never inside the clock-configuration call argument. The line is quoted in the SUMMARY.
    - `grep -c 'FLASH_LATENCY_1' platform/py32f071/src/main.cpp` returns **>= 2** (the call argument plus the guard operand), and reading the hits confirms the call argument is the corrected constant.
    - `grep -c 'static_assert' platform/py32f071/src/main.cpp` returns **>= 1**.
    - The comment block cites the pinned SDK commit SHA read live from `platform/py32f071/CMakeLists.txt` (not transcribed from a planning document), names both the workaround commit and the module-restoring commit by SHA, and contains the word `safe` in the severity framing sentence. Verified by reading and quoting the block.
    - Nothing else in the clock setup changed: `git diff HEAD -- platform/py32f071/src/main.cpp` shows one changed argument plus added comment and assert lines; the diff is quoted in the SUMMARY.
    - The SUMMARY contains no statement that this change was built, compiled, verified or validated locally, and instead names Plan 124-11's CI run as the sole evidence route.
  </acceptance_criteria>
  <done>The latency constant is correct, reintroducing the wrong one is a build failure, and the write-up says exactly what is and is not known.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned SDK -> the build | The SDK is fetched at configure time; every claim about its macros depends on the pin being immutable |
| source-level assertion -> actual behaviour | No ARM toolchain exists here, so a guard's correctness is unproven until CI runs it |
| deleted build helper -> downstream consumers | A deletion is only safe if the zero-consumer claim holds on the tree being edited |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-26 | Tampering | supply chain — the fetched SDK changing under a mutable ref | high | mitigate | Re-read `GIT_TAG` from `CMakeLists.txt` and confirm it is a full 40-hex SHA, not a tag or branch; cite that SHA in the new comment |
| T-124-27 | Repudiation | the ACR-mask workaround silently returning in a later edit | medium | mitigate | `static_assert` regression guard with both operands compile-time SDK macros; grep criteria pin the mask to exactly one occurrence, inside the guard |
| T-124-28 | Repudiation | claiming a local build proved the ARM change | high | mitigate | Acceptance criterion forbids any local-verification statement in the SUMMARY and routes evidence to Plan 124-11's run URL + SHA |
| T-124-29 | Denial of service | deleting a build helper that something still calls | medium | mitigate | Zero-consumer re-proven across BOTH repos on the landed tree before the deletion; manifest gate re-run afterwards |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && git ls-tree HEAD platform/py32f071/cmake/` -> `write_checksums.cmake` absent.
2. `grep -n 'FLASH_' platform/py32f071/src/main.cpp` -> corrected call argument plus the guard.
3. `python3 scripts/check_cmake_manifest.py` -> exit 0.
4. `git -C /workspaces/firestarter status --porcelain` -> empty after commit.
5. ARM compilation is NOT verified here and must not be claimed — Plan 124-11 supplies the run URL and SHA.
</verification>

<success_criteria>
- Two of MERGE-08's three defects are closed in their own attributable commits.
- The latency write-up matches what research measured: a deliberate, never-reverted workaround producing an over-conservative but safe setting, not a typo and not a correctness bug.
- The unachievable proof shape is recorded as unachievable, with the reason, rather than quietly dropped.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| deletion of `firestarter/platform/py32f071/cmake/write_checksums.cmake` | removed file |
| corrected flash-latency argument in `platform/py32f071/src/main.cpp` | changed constant |
| `static_assert` flash-latency regression guard + cited comment block in the same file | new compile-time guard |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-07-SUMMARY.md` when done. Record: both
zero-consumer search commands and their verbatim output, the `git ls-tree` listing after deletion, the
quoted diff of `main.cpp`, the pinned SDK SHA as read from `CMakeLists.txt`, and an explicit statement that
ARM compilation of both changes is pending Plan 124-11's CI evidence.
</output>
