---
phase: 124-firmware-integration-merge
plan: 04
type: execute
wave: 2
depends_on: ["124-01", "124-02", "124-03"]
files_modified:
  - firestarter/.github/workflows/py32f071.yml
  - firestarter/include/avr/pgmspace.h
  - firestarter/include/boards/py32f071_rurp_shield.h
  - firestarter/include/rurp_platform.h
  - firestarter/include/rurp_platform_compat.h
  - firestarter/include/rurp_serial_utils.h
  - firestarter/include/rurp_shield.h
  - firestarter/platform/py32f071/
autonomous: true
requirements: [MERGE-01, MERGE-05, MERGE-06]

must_haves:
  truths:
    - "platform/py32f071/ and the two portability headers exist on v1.23-py32f071-integration as ONE squashed commit whose tree is byte-identical to a true merge's tree (MERGE-01, D-05)"
    - "check_landing_range.py exits 0 over the whole fork..HEAD range after the landing (MERGE-01, D-06)"
    - "780a3fb's content is present in include/rurp_platform_compat.h even though 780a3fb is not an ancestor of HEAD after a squash (MERGE-01)"
    - "Both native envs report exactly 141 cases / 17 suites on the landed tree (MERGE-06)"
    - "AVR flash and RAM on the landed tree sit inside MERGE-05's band, asserted by --policy merge05 against the frozen BASE-01 (MERGE-05)"
    - "ad47c3b / feature/py32f071-release-assets is NOT landed (D-07)"
  artifacts:
    - firestarter/platform/py32f071/CMakeLists.txt
    - firestarter/include/rurp_platform_compat.h
    - firestarter/include/boards/py32f071_rurp_shield.h
    - firestarter/.github/workflows/py32f071.yml
  key_links:
    - "The squash-vs-true-merge tree equality must be RE-COMPUTED at execute time in a scratch clone; RESEARCH's recorded 693ffdf is valid only while neither tip moves"
    - "The landing commit contains ONLY the squash. D-02's conversion, D-01's deletion and D-04's fix are separate later commits (D-03) — that ordering is what makes any flash/RAM delta attributable"
---

<objective>
Land `agent/portability-macros` plus the `agent/py32f071-toolchain` stack onto
`v1.23-py32f071-integration` as **one squashed commit** (D-05), then measure the landed tree against the
Phase-123 baseline.

Purpose: MERGE-01 forbids any commit on the integration branch where the portability half is present and
the py32 stack is not. RESEARCH measured that a true `git merge` produces **exactly five** such commits
(`52d6c1f`, `adb133a`, `b253092`, `c0c6695`, `532997c`) because `agent/portability-macros` is an ancestor
of the toolchain branch, while `git merge --squash` produces **zero** — and the squash's tree is
byte-identical to the true merge's. That is the entire basis of D-05.

Output: one squashed landing commit, plus the recorded post-landing measurements every later plan in this
phase is judged against.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-CONTEXT.md
@.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md

Repo layout: this plan commits inside the **`firestarter` submodule** at `/workspaces/firestarter`, on
branch `v1.23-py32f071-integration`. The meta repo's gitlink is NOT bumped by this plan. Never write into
`/workspaces/firestarter_py32_ci/`.

Correction that governs this plan: **C-8** — "72 commits behind `beta`" applies to the py32 *source*
branches. The integration branch is **14 ahead / 0 behind** `origin/beta` (re-verified this session). No
rebase and no catch-up merge is needed or permitted here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the landing preconditions and the squash-equals-merge tree in a scratch clone</name>
  <files>(no repo files modified — scratch clone only)</files>
  <read_first>
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` sections "Git topology", "The
      landing, executed in a scratch clone", "D-06's Criterion-1 range check", and the Metadata block's
      "Valid until" note: every SHA and tree hash there is tip-dependent and must be recomputed.
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` D-05 and D-07.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md` lines 46-47 — the
      recorded fork points.
  </read_first>
  <action>
    Work in a scratch clone under the session scratchpad directory so nothing here can dirty the real
    submodule: `git clone --shared --no-checkout /workspaces/firestarter SCRATCH/fw`, then
    `git -C SCRATCH/fw checkout v1.23-py32f071-integration`.

    Re-observe every value below and record each in the SUMMARY. Accept none of RESEARCH's numbers
    without re-observing it.

    1. In `/workspaces/firestarter`: `git rev-parse --abbrev-ref HEAD` is `v1.23-py32f071-integration`
       and `git status --porcelain` is empty.
    2. `git rev-list --count origin/beta..HEAD` and `git rev-list --count HEAD..origin/beta` — expect 14
       and 0. If the second is non-zero, STOP and report: the C-8 reading no longer holds and the landing
       shape must be re-decided before proceeding.
    3. `git merge-base --is-ancestor origin/agent/portability-macros origin/agent/py32f071-toolchain`
       exits 0 — the fact that forces D-05's squash.
    4. `git merge-base --is-ancestor 780a3fb origin/agent/py32f071-toolchain` exits 0. Record
       `git rev-parse origin/agent/py32f071-toolchain` (RESEARCH observed e5abb514; it may have moved).
    5. In the scratch clone, build BOTH landings on throwaway branches and compare their tree SHAs:
       a squash branch (`git merge --squash origin/agent/py32f071-toolchain` then `git commit`) and a
       true-merge branch (`git merge --no-ff origin/agent/py32f071-toolchain`). Compare
       `git rev-parse BRANCH^{tree}` for each; they must be equal.
    6. Run this phase's Criterion-1 gate against each throwaway branch and record the counts. Invoke it
       with the two env seams pointed at the scratch clone and the recorded fork point
       `5c9160a34b665878b05403ab014b959926feb6bf`. The squash branch must give 0 violations; the true
       merge must give a non-zero count. **If the true merge gives 0, STOP and report** — the
       discriminating evidence behind D-05 would have evaporated and the decision needs re-confirming,
       never silent continuation.
    7. Record the merge surface:
       `git diff --name-status $(git merge-base HEAD origin/agent/py32f071-toolchain) origin/agent/py32f071-toolchain`.
       Expect 22 paths — 20 added, 2 modified (`include/rurp_serial_utils.h`, `include/rurp_shield.h`).
       Any other shape is a STOP-and-report.
    8. Record `git rev-parse origin/feature/py32f071-release-assets` for the SUMMARY, and confirm that
       branch is not being merged (D-07).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git merge-base --is-ancestor origin/agent/portability-macros origin/agent/py32f071-toolchain && git rev-list --count HEAD..origin/beta</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count HEAD..origin/beta` prints exactly `0`, and `git rev-list --count origin/beta..HEAD` prints `14`. Both recorded.
    - The squash-branch tree SHA and the true-merge-branch tree SHA are string-equal; both recorded verbatim.
    - The Criterion-1 gate exits `0` on the squash branch and `1` on the true-merge branch, with a violation count of at least 1 and the violating commit SHAs recorded verbatim.
    - The merge surface is exactly 22 paths, 20 added and 2 modified; the full name-status list is recorded.
    - `git status --porcelain` in `/workspaces/firestarter` is empty before any merge command touches it.
  </acceptance_criteria>
  <done>Every load-bearing fact D-05 rests on is re-observed against the live tips, and the tree-equality claim is recomputed rather than quoted.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the squashed landing commit</name>
  <files>firestarter/.github/workflows/py32f071.yml, firestarter/include/avr/pgmspace.h, firestarter/include/boards/py32f071_rurp_shield.h, firestarter/include/rurp_platform.h, firestarter/include/rurp_platform_compat.h, firestarter/include/rurp_serial_utils.h, firestarter/include/rurp_shield.h, firestarter/platform/py32f071/</files>
  <read_first>
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` D-05 (what the commit message must
      record), D-03 (what must NOT be in this commit), D-07 (what must not be landed at all).
    - The values recorded by Task 1 in this same session.
  </read_first>
  <action>
    In `/workspaces/firestarter`, on `v1.23-py32f071-integration`, run
    `git merge --squash origin/agent/py32f071-toolchain`, then exactly one `git commit`.

    Nothing else goes into this commit. Per D-03 the `#if DEV_TOOLS` conversion is a separate later commit
    (Plan 124-06) because that content is not on the branch being landed. Per D-01 and D-04 the
    `write_checksums.cmake` deletion and the flash-latency fix are separate later commits (Plan 124-07).
    That ordering is forced by MERGE-01 and it is what makes any flash or RAM delta attributable to a
    specific commit rather than to "the merge".

    Commit message. Subject line:
    `feat(124-04): land portability-macros + py32f071 toolchain stack (squashed)`.
    Body, each item on its own line and each value re-observed in Task 1 rather than copied from a
    planning document:
      - the `origin/agent/py32f071-toolchain` tip SHA;
      - the `origin/agent/portability-macros` tip SHA;
      - the merge-base SHA;
      - the 22-path surface counts (20 added, 2 modified);
      - the squash tree SHA and the true-merge tree SHA, with an explicit statement that they are equal;
      - `780a3fb` named explicitly, stated to be included **by content and not by parentage** because a
        squash keeps no upstream parentage, together with the program-memory helper identifiers it
        contributes. Derive those identifiers mechanically from
        `git show 780a3fb -- include/rurp_platform_compat.h`; do not transcribe a list from a document;
      - one sentence saying why a squash and not a merge, citing the violation count Task 1 step 6 measured
        for the true merge;
      - one sentence stating that `ad47c3b` / `feature/py32f071-release-assets` is deliberately NOT landed
        and belongs to Phase 128 (D-07).
    Recorded discretion choice from CONTEXT: the message carries the tip SHAs plus `780a3fb`, not the full
    52-commit shortlog.

    After committing, assert `git status --porcelain` is empty, and assert `git rev-parse HEAD^{tree}`
    equals the scratch clone's squash-branch tree SHA from Task 1.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_landing_range.py</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse HEAD^{tree}` equals the Task 1 squash-branch tree SHA, which equals the true-merge tree SHA. All three strings recorded.
    - `python3 scripts/check_landing_range.py` exits `0`; its PASS line names the observed commits-scanned integer, a `carrying` integer of at least 1, and `0 violations`. The verbatim PASS line is recorded.
    - `git merge-base --is-ancestor 780a3fb HEAD` exits **non-zero** — confirming a squash, not a merge — while every identifier `780a3fb` adds to `include/rurp_platform_compat.h` is present in that file at HEAD. Both facts asserted and recorded together, because the pair is what "included by content, not by parentage" means.
    - `git merge-base --is-ancestor ad47c3b HEAD` exits **non-zero** (D-07).
    - `git log --oneline -1` shows exactly one new commit; `git rev-list --count origin/beta..HEAD` is 15.
    - `test -f platform/py32f071/CMakeLists.txt`, `test -f include/rurp_platform_compat.h`, `test -f include/avr/pgmspace.h`, `test -f .github/workflows/py32f071.yml` all succeed.
    - `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>The port exists on the integration branch as one atomic landing whose history shape is proven clean by an exit code and whose tree is proven equal to a true merge's.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the landed tree — native counts, AVR sizes, golden traces, and the newly armed gates</name>
  <files>(no repo files modified — measurement and evidence capture only)</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md` sections 2 and 3
      — the exact command shapes and the "assert counts, never tests pass" row format this measurement
      must reproduce.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` "Post-landing measurements" table
      (the figures to compare against) and sections W-2 and W-4 (the two gates that are EXPECTED to be red
      here — a red result in this task is a planned finding, not a failure of this plan).
    - `firestarter/scripts/check_size_baseline.py` usage block, as extended by Plan 124-02.
  </read_first>
  <action>
    Measure the landed tree and capture every log under the session scratchpad (nothing captured here is
    committed; Plan 124-10 owns fixture re-capture).

    Clean AVR builds, one env at a time: `pio run -t clean -e ENV` then `pio run -e ENV` for `uno`,
    `uno328pb`, `leonardo`. Native: remove `.pio/build/native` and `.pio/build/native_nodevtools` first so
    both runs are COLD, then `pio test -e native` and `pio test -e native_nodevtools`. Record for each
    native env, from its own log, both the cold total warning count (`grep -cE 'warning:'`) and the
    macro-redefinition count (`grep -cE 'warning: *"[^"]+" +redefined'`). Then immediately re-run each
    native env WARM and record the same two counts again. The cold-versus-warm pair is the W-2 datum and
    both halves must be recorded — RESEARCH measured 456 cold / 360 warm pre-merge and 1166 cold / 998
    warm merged, and the BASE-01 record of 360 is a warm-cache number whose `meta.note` claims otherwise
    (correction C-13).

    Then run these gates and record each exit code and verbatim output line:
      - `check_size_baseline.py --native-log native=LOG --native-log native_nodevtools=LOG` — expect exit
        0 with `cases=141,suites=17` for both (MERGE-06).
      - `check_size_baseline.py --avr-log ...` for all three envs, DEFAULT mode — expect **exit 1**. This
        is W-1 as designed: the default is byte-identity against BASE-01 and the landing moves flash on all
        three targets. Record the three diverging lines verbatim.
      - `check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json`
        with all three `--avr-log` arguments — expect **exit 0** (MERGE-05).
      - `check_build_warnings.py --log` for the three AVR envs — expect exit 0, macro_redefinition 0.
      - `check_build_warnings.py --log` for the two native envs — expect **exit 1** with an
        `exceeds recorded=360` message. Planned red; W-2 resolves it in Plan 124-10.
      - `check_cmake_manifest.py` — newly armed by the landing; expect **exit 1** with 9 violations, not
        the 2 that `123-NONREGRESSION.md` predicted (W-4). Record all 9 verbatim.
      - `check_orphan_provisional.py` — newly armed; expect **exit 1** with 1 violation naming
        `RURP_PY32F071_PINMAP_PROVISIONAL`. Record verbatim.
      - `python3 -m pytest tests/ -q` from `/workspaces/firestarter` — expect the two Phase-123 `UNARMED:`
        tests to FAIL (W-3, planned). Record the exact failed/passed integers and the two failing node ids.
      - `python3 -m pytest tests/test_golden_trace_identity.py -q` — expect **0 failed**: the golden-trace
        blob is unchanged across the merge, so Plan 124-03's pin must hold. If it does not hold, STOP and
        report — that would mean the landing touched a golden trace, which no requirement licenses.
    Every one of the expected-red results above must be reported in the SUMMARY as a **planned red with a
    named owning plan**, never as a surprise and never as something to "fix while here".
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_golden_trace_identity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both native envs report exactly `141` cases and `17` suites, all PASSED, and `check_size_baseline.py --native-log` exits 0 for both (MERGE-06 half one).
    - `check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json` with all three AVR logs exits `0`, and its PASS line carries the three observed deltas (MERGE-05).
    - The three observed AVR flash figures and three RAM figures are recorded; each is compared explicitly to RESEARCH's measured 26016/2014, 23954/1573, 24004/1579 and any disagreement is reported as a finding rather than reconciled silently.
    - `pytest tests/test_golden_trace_identity.py -q` reports `0 failed` (MERGE-06 half two).
    - The four planned-red results are each recorded with exit code, verbatim output, and owning plan: default-mode `check_size_baseline.py` (W-1 -> 124-10); native `check_build_warnings.py` (W-2 -> 124-10); `check_cmake_manifest.py` at 9 violations (W-4 -> 124-05); `check_orphan_provisional.py` at 1 violation (W-5 -> 124-08); plus the two failing `UNARMED:` pytests (W-3 -> 124-05 and 124-08).
    - The cold and warm native warning counts are recorded as four distinct integers (two envs x two build states), with the build-state recipe used for each stated in one sentence.
    - `git status --porcelain` is empty — no measurement wrote into the tree.
  </acceptance_criteria>
  <done>The landed tree's measurements exist as recorded numbers, the two requirements that are satisfiable at this point are satisfied by exit codes, and every gate that is red is red for a named, owned, pre-declared reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream branch tips -> the integration branch | 52 upstream commits of unreviewed content enter the milestone branch in one act |
| "the merge had no conflicts" -> a quality claim | ROADMAP forbids this conflation explicitly; git cannot see the rename defect |
| git history shape -> MERGE-01's claim | A merge instead of a squash silently reintroduces five forbidden commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-13 | Repudiation | a true merge landing instead of a squash | critical | mitigate | `check_landing_range.py` run on both shapes in a scratch clone before the real landing, and again on the real HEAD after it |
| T-124-14 | Tampering | a tip moving between research and execution, invalidating the recorded tree SHA | high | mitigate | Every SHA re-observed in Task 1; the tree-equality comparison recomputed rather than quoted; STOP-and-report clauses on each divergence |
| T-124-15 | Tampering | `ad47c3b` landing by accident and pre-empting Phase 128's asset contract | medium | mitigate | `git merge-base --is-ancestor ad47c3b HEAD` asserted non-zero |
| T-124-16 | Repudiation | a red gate being quietly repaired inside the landing commit | high | accept-with-control | The four planned reds are enumerated in advance with owning plans; the acceptance criteria require them to be RECORDED, and D-03 forbids any other content in this commit |
| T-124-17 | Tampering | a golden register trace changing under cover of a 22-file merge | high | mitigate | Plan 124-03's blob + per-array pin re-run in Task 3 with a STOP-and-report on failure |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && python3 scripts/check_landing_range.py` -> exit 0 with a self-describing PASS line.
2. `pio test -e native` and `pio test -e native_nodevtools` -> 141 cases / 17 suites each, all PASSED.
3. `python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json` with three `--avr-log` args -> exit 0.
4. `python3 -m pytest tests/test_golden_trace_identity.py -q` -> 0 failed.
5. `git -C /workspaces/firestarter status --porcelain` -> empty.
</verification>

<success_criteria>
- The landing is one commit, its tree equals a true merge's, and the Criterion-1 gate passes over the full range.
- `780a3fb` is proven present by content while proven absent by parentage.
- MERGE-06's counts and MERGE-05's band both hold on the landed tree, asserted as exit codes.
- Every gate that goes red does so for a pre-declared reason with a named owning plan, and none is repaired here.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| `firestarter/platform/py32f071/` (15 files incl. `CMakeLists.txt`, `cmake/write_checksums.cmake`, `src/main.cpp`, `linker/PY32F071xB_FLASH.ld`) | landed directory |
| `firestarter/include/rurp_platform_compat.h`, `include/avr/pgmspace.h`, `include/rurp_platform.h` | landed headers |
| `firestarter/include/boards/py32f071_rurp_shield.h` | landed board header (carries the hollow guard 124-09 repairs) |
| `firestarter/.github/workflows/py32f071.yml` | landed workflow (124-05 adds its `push` trigger) |
| The squashed landing commit and its recorded message body | git history artifact |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-04-SUMMARY.md` when done. Record: the two tree
SHAs, the landing commit SHA, the verbatim Criterion-1 PASS line, the true-merge violation SHAs, the
22-path surface, the six AVR figures, both native count pairs, the four cold/warm native warning integers,
and every planned-red gate with its verbatim output and owning plan.
</output>
