---
phase: 124-firmware-integration-merge
plan: 05
type: execute
wave: 3
depends_on: ["124-04"]
files_modified:
  - firestarter/platform/py32f071/CMakeLists.txt
  - firestarter/.github/workflows/py32f071.yml
  - firestarter/tests/test_check_cmake_manifest.py
autonomous: true
requirements: [MERGE-02, MERGE-03]

must_haves:
  truths:
    - "platform/py32f071/CMakeLists.txt names flash_nor_unlock.cpp and flash_5v_page.cpp, not flash_type_3.cpp / flash_type_4.cpp (MERGE-02)"
    - "check_cmake_manifest.py goes from 9 violations to 0 on the real tree — two by the rename, seven by D-15's reasoned PY32_EXCLUDED allow-list (W-4, D-15)"
    - "py32f071.yml carries push: branches: [beta] verified by reading the workflow file (MERGE-03, D-10)"
    - "The Phase-123 pytest that asserts UNARMED: on the real tree is inverted to assert the armed PASS shape, and its module Coverage list says so (W-3)"
  artifacts:
    - firestarter/platform/py32f071/CMakeLists.txt
    - firestarter/.github/workflows/py32f071.yml
    - firestarter/tests/test_check_cmake_manifest.py
  key_links:
    - "Every PY32_EXCLUDED line needs its mandatory reason segment; an entry with a path and no reason is itself a violation because an allow-list without required reasons degrades into a silencer"
    - "The rename must be verified against the tree, not against a document — the two target filenames must be confirmed to exist under src/proms/ before the manifest is edited to name them"
---

<objective>
Repair the defect git cannot see. `platform/py32f071/CMakeLists.txt` names two source files that v1.19
Phase 104 renamed, so the merge is textually perfect and the ARM target fails at CMake configure time.
Add the `push: branches: [beta]` trigger that would have caught it, and close out the CMake-manifest
gate's first armed run.

Purpose: this is MERGE-02's textual half plus MERGE-03 in full. RESEARCH measured the first armed run of
`check_cmake_manifest.py` at **9** violations, not the 2 that `123-NONREGRESSION.md` predicted (W-4):
two are the rename defect; five are exactly D-15's `PY32_EXCLUDED` set; two more
(`flash_5v_page.cpp`, `flash_nor_unlock.cpp`) disappear the moment the rename lands. The plan expects 9
and drives it to 0.

Output: a configurable ARM source manifest, the `beta` push trigger, and a green armed manifest gate with
its paired pytest inverted out of its Phase-123 `UNARMED:` state.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-PATTERNS.md
@.planning/phases/124-firmware-integration-merge/124-CONTEXT.md
@.planning/phases/124-firmware-integration-merge/124-04-SUMMARY.md

Repo layout: all edits land in the **`firestarter` submodule** at `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commit inside the submodule.

Line numbers that are CORRECT (correction C-11): the rename defect is at
`platform/py32f071/CMakeLists.txt` lines **40-41**; `DATA_BUFFER_SIZE=512` is at line **107**;
`target_compile_definitions` spans lines **99-108**. PROJECT.md's "46-47" and "113" are wrong.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the 9 armed violations, fix the rename, and write D-15's PY32_EXCLUDED allow-list</name>
  <files>firestarter/platform/py32f071/CMakeLists.txt</files>
  <read_first>
    - `firestarter/scripts/check_cmake_manifest.py` — **read the module docstring in full**. It prescribes
      the five `PY32_EXCLUDED` lines verbatim, defines the comment format
      `# PY32_EXCLUDED: <path> -- <reason>` with the reason segment MANDATORY, names
      `ENFORCED_LISTS = {"FIRESTARTER_COMMON_SOURCES", "PY32_PLATFORM_SOURCES"}`, records that
      `PY32_SDK_SOURCES` is structurally exempt (FetchContent), and documents the 0/1/2 exit taxonomy
      including "an unrecognised `set()` list name is exit 2".
    - `firestarter/tests/fixtures/clean_cmake_manifest_excluded/platform/py32f071/CMakeLists.txt` — a
      working, gate-passing manifest already in-tree; read it for the exact `set()` spelling and a
      correctly formatted `PY32_EXCLUDED` line the gate accepts today.
    - `firestarter/tests/fixtures/planted_cmake_manifest_excluded_no_reason/` — the negative control
      (path present, no reason segment).
    - `firestarter/platform/py32f071/CMakeLists.txt` lines 25-60 and 95-115.
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` D-15 (the five lines and the one
      amended reason) and D-02 (the mechanism the amended reason must describe).
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §W-4 — the verbatim 9-violation
      output to compare against.
  </read_first>
  <action>
    First, record the armed gate's starting state: run `python3 scripts/check_cmake_manifest.py` and copy
    its output verbatim into the SUMMARY. Expect exit 1 and 9 violations. If the count is not 9, record
    the actual count and the actual lines and reconcile against RESEARCH's list before editing anything —
    do not proceed on a different violation set without saying so.

    Then, before editing the manifest, confirm against the tree that the two replacement filenames exist:
    `ls src/proms/flash_nor_unlock.cpp src/proms/flash_5v_page.cpp`. The manifest must be edited to name
    files that are actually there, verified by listing them, not by trusting the rename record.

    Edit `FIRESTARTER_COMMON_SOURCES` lines 40-41: replace `src/proms/flash_type_3.cpp` with
    `src/proms/flash_nor_unlock.cpp` and `src/proms/flash_type_4.cpp` with `src/proms/flash_5v_page.cpp`,
    preserving the `${REPOSITORY_ROOT}/` prefix and the surrounding quoting exactly.

    Then add the `PY32_EXCLUDED` allow-list. Use the checker's own prescribed format,
    `# PY32_EXCLUDED: <path> -- <reason>`, one line per entry, placed as a comment block immediately above
    the `set(FIRESTARTER_COMMON_SOURCES` block so a reader meets the deliberate omissions before the list.
    Five entries, the reasons taken from the checker docstring's prescribed set with exactly one amendment
    per D-15:
      - `src/boards/uno_rurp_shield.cpp` — AVR board impl, no ARM analogue
      - `src/boards/leonardo_rurp_shield.cpp` — AVR board impl, no ARM analogue
      - `src/boards/rurp_common.cpp` — AVR-specific common
      - `src/dev_tools.cpp` — **amended per D-15**: the reason must describe D-02's uniform value-semantics
        mechanism (no ARM dev-tools translation unit; `DEV_TOOLS` resolves to 0 through the shared default),
        NOT the old "deliberately off by omission" wording. Note in the SUMMARY that Plan 124-06 lands that
        shared default in the same wave, so this reason describes end-of-wave state.
      - `src/rurp_config_utils.cpp` — Phase 126 per-platform config-backend split; the entry keeps its
        stated caveat that this exclusion **will need revisiting in Phase 126** and is not permanent.
    Keep each reason on the comment line the checker parses; a multi-line reason must be shaped so the
    gate still reads a reason segment on the `PY32_EXCLUDED:` line itself.

    Also add, inside `target_compile_definitions` (lines 99-108), a comment recording MERGE-08's explicit
    ARM `DEV_TOOLS`-off decision: `DEV_TOOLS` is deliberately not defined for the ARM target, so the shared
    value-semantics default resolves it to 0 — an explicit commented decision rather than an accident of
    the defines. Do not add a `DEV_TOOLS` define.

    Re-run the gate and record the final output verbatim.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_cmake_manifest.py</automated>
  </verify>
  <acceptance_criteria>
    - The pre-edit run is recorded verbatim, with its exit code and its violation count; the count observed is stated explicitly (RESEARCH measured 9).
    - `ls src/proms/flash_nor_unlock.cpp src/proms/flash_5v_page.cpp` succeeds before the manifest edit; the observed listing is recorded.
    - `grep -c 'flash_type_3\|flash_type_4' platform/py32f071/CMakeLists.txt` returns **0**.
    - `grep -c 'flash_nor_unlock.cpp' platform/py32f071/CMakeLists.txt` returns **1** and `grep -c 'flash_5v_page.cpp' platform/py32f071/CMakeLists.txt` returns **1**.
    - `grep -c 'PY32_EXCLUDED:' platform/py32f071/CMakeLists.txt` returns **5**.
    - `python3 scripts/check_cmake_manifest.py` exits **0** and its PASS line names the resolved source count and the exempt count; the verbatim PASS line is recorded.
    - `grep -c 'DEV_TOOLS' platform/py32f071/CMakeLists.txt` returns **>= 2** (the exclusion reason plus the `target_compile_definitions` comment), and none of those hits is a `-D DEV_TOOLS` or a `DEV_TOOLS=` define — verified by reading the lines, not by the count alone.
    - `grep -n 'DATA_BUFFER_SIZE=512' platform/py32f071/CMakeLists.txt` still resolves (the landing's value is untouched by this plan; it is a Phase 127/128 concern per the deferred list).
  </acceptance_criteria>
  <done>The ARM source manifest names files that exist, its five deliberate omissions each carry a mandatory reason, and the armed drift gate is green from a recorded starting state of 9 violations.</done>
</task>

<task type="auto">
  <name>Task 2: Add MERGE-03's push trigger to py32f071.yml</name>
  <files>firestarter/.github/workflows/py32f071.yml</files>
  <read_first>
    - `firestarter/.github/workflows/py32f071.yml` lines 1-30 — the existing `on:` block
      (`pull_request:` with a `paths:` filter, plus `workflow_dispatch:`).
    - `firestarter/.github/workflows/beta-build.yml` — its `on:` block, to confirm what already fires on
      a `beta` push and that this plan does not touch it.
    - `.planning/phases/124-firmware-integration-merge/124-CONTEXT.md` D-10 — MERGE-03 is implemented
      **literally as specified**; the consequence that Phase 128 will later build ARM inside
      `beta-build.yml`, giving two ARM builds on a `beta` push, is recorded for Phase 128 to resolve and
      is deliberately NOT pre-solved here.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §"Pushing the milestone branch
      fires nothing" — the trigger table that proves this addition cannot fire on the milestone branch.
  </read_first>
  <action>
    Add a `push:` trigger to the `on:` block of `.github/workflows/py32f071.yml`, scoped to
    `branches: [beta]` and nothing else. Do not add a `paths:` filter to the push arm — MERGE-03's whole
    point is that a change anywhere that breaks the ARM configure gets caught on `beta`, and the existing
    `pull_request` `paths:` filter is what made the rename defect invisible.

    Add a one-line YAML comment directly above the new trigger recording D-10: implemented literally per
    MERGE-03; the double-ARM-build question created by Phase 128's `beta-build.yml` fold is recorded for
    Phase 128 to resolve, not pre-solved here.

    Change nothing else in the file. Do not touch `beta-build.yml` or `build.yml`.

    Verify the YAML still parses, and re-read the resulting `on:` block to confirm the `pull_request` and
    `workflow_dispatch` arms are intact.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "import sys;print(open('.github/workflows/py32f071.yml').read().split('jobs:')[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Reading the emitted `on:` block shows three arms: `push:` with `branches: [beta]`, the original `pull_request:` with its `paths:` list unchanged, and `workflow_dispatch:`. The block is quoted verbatim in the SUMMARY.
    - The file parses as YAML: `python3 -c "import yaml,sys;yaml.safe_load(open('.github/workflows/py32f071.yml'))"` exits 0. If PyYAML is unavailable in this environment, record that fact and substitute a structural read of the `on:` block plus a `gh workflow view` confirmation once Plan 124-11 pushes — never skip silently.
    - `git diff --name-only HEAD` for this task lists **only** `.github/workflows/py32f071.yml`; `beta-build.yml` and `build.yml` are absent from that list.
    - The observed branch `v1.23-py32f071-integration` matches **no** `push:` branch filter in any of the three workflows — re-confirm by reading all three `on:` blocks and recording the result, since this is the safety argument Plan 124-11's push depends on (C-14).
  </acceptance_criteria>
  <done>MERGE-03 is implemented literally, its known Phase-128 consequence is recorded rather than pre-solved, and the milestone branch is re-confirmed to match no push trigger.</done>
</task>

<task type="auto">
  <name>Task 3: Invert the expiring Phase-123 CMake-manifest pytest (W-3, half one)</name>
  <files>firestarter/tests/test_check_cmake_manifest.py</files>
  <read_first>
    - `firestarter/tests/test_check_cmake_manifest.py` — the whole file, and specifically lines **87-103**
      (`test_unarmed_on_the_real_tree_with_no_seam_override`) and lines **179-193**
      (`test_reasoned_omission_passes_and_is_named`, the armed-state analog to invert toward, which
      asserts `PASS:` plus the allow-listed path named on the PASS line).
    - `.planning/phases/124-firmware-integration-merge/124-PATTERNS.md` §6 — the exact assertion-by-line
      table for this rewrite and the instruction that everything else in the file stays, including
      `test_unarmed_on_clean_unarmed_tree_fixture` which points the seam at `clean_unarmed_tree/` and must
      NOT change.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §W-3 — why a fixed, armed gate
      never prints the unarmed line, so this test does not recover on its own.
  </read_first>
  <action>
    Rewrite exactly one test in this module. Rename
    `test_unarmed_on_the_real_tree_with_no_seam_override` to `test_armed_and_passing_on_the_real_tree`
    and invert its assertions:
      - keep `result.returncode == 0`;
      - replace the unarmed-prefix assertion with an assertion that stdout contains `PASS:`;
      - keep the assertion that `platform/py32f071` is named in stdout, but first VERIFY by running the
        gate that the armed PASS line genuinely names it. If it does not, drop that assertion rather than
        weakening the PASS assertion to accommodate it, and record the decision in the SUMMARY.
      - rewrite the docstring: it currently says the unarmed state "must stay true until Phase 124 lands
        the port". Replace with a statement that Phase 124 landed the port, that this test now pins the
        ARMED, PASSING state, and that a regression to the unarmed line would mean
        `platform/py32f071/` had disappeared and must fail here.
      - update the module docstring's numbered `Coverage:` item 1 to match the new name and meaning.

    Change nothing else. `test_unarmed_on_clean_unarmed_tree_fixture` must stay byte-identical: it points
    the seam at `clean_unarmed_tree/`, which never arms, and it is what proves the arming decision follows
    the supplied root rather than the process cwd.

    Then run the whole firmware pytest suite and record the counts. Exactly one of the two Phase-123
    expiring tests is repaired here; the other
    (`tests/test_check_orphan_provisional.py::test_unarmed_on_the_real_tree_with_no_seam_override`) stays
    RED until Plan 124-08 wires the provisional-macro consumer. Record that remaining red explicitly, with
    its owning plan named.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_check_cmake_manifest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_check_cmake_manifest.py -q` reports **0 failed**; the collected count is unchanged from before this task (recorded as two integers).
    - `grep -c 'UNARMED' tests/test_check_cmake_manifest.py` returns a value that is strictly less than the pre-edit value, and every remaining hit is inside `test_unarmed_on_clean_unarmed_tree_fixture` or a docstring describing it — verified by reading the hits, not by the count alone.
    - `grep -c 'test_armed_and_passing_on_the_real_tree' tests/test_check_cmake_manifest.py` returns **>= 2** (the definition plus the Coverage list entry).
    - `git diff HEAD -- tests/test_check_cmake_manifest.py` touches exactly one test function plus the module docstring's Coverage item 1 — confirmed by reading the diff hunks, and the diff is quoted in the SUMMARY.
    - `python3 -m pytest tests/ -q` reports exactly **1 failed** and names `test_check_orphan_provisional.py::test_unarmed_on_the_real_tree_with_no_seam_override` as the sole failure, owned by Plan 124-08. Record the passed integer.
  </acceptance_criteria>
  <done>Half of W-3 is closed at the point where its gate turns green, so the repair is attributable to the fix rather than to a bulk test edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMake source manifest -> the ARM build | A path that does not resolve fails at configure time, which no local tool in this devcontainer can observe |
| `PY32_EXCLUDED` allow-list -> the drift gate | An allow-list without mandatory reasons degrades into a silencer that hides real rename damage |
| CI trigger coverage -> regression detection | The rename defect survived because `py32f071.yml` had no `push` trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-18 | Repudiation | an exclusion added to silence a real rename break | high | mitigate | The checker's mandatory `-- <reason>` segment; exactly five entries asserted; each reason reviewed against D-15's prescribed text |
| T-124-19 | Tampering | the manifest edited to name files that still do not exist | high | mitigate | `ls src/proms/flash_nor_unlock.cpp src/proms/flash_5v_page.cpp` asserted BEFORE the edit, and the gate re-run after |
| T-124-20 | Elevation of privilege | a workflow trigger change accidentally firing a release | critical | mitigate | Scope is `push: branches: [beta]` only; all three `on:` blocks re-read and recorded; `beta-build.yml` and `build.yml` asserted untouched |
| T-124-21 | Repudiation | a test edited to accommodate a gate rather than a gate fixed | high | mitigate | The PASS assertion is fixed first and the `platform/py32f071` naming assertion is verified against real output before being kept |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && python3 scripts/check_cmake_manifest.py` -> exit 0 with a PASS line naming the resolved and exempt counts.
2. Read `.github/workflows/py32f071.yml`'s `on:` block -> three arms, `push` scoped to `beta`.
3. `python3 -m pytest tests/ -q` -> exactly 1 failed (the orphan-provisional expiring test, owned by 124-08).
4. `git -C /workspaces/firestarter status --porcelain` -> empty after commit.
</verification>

<success_criteria>
- MERGE-02's textual half is closed: the manifest names the real filenames and the drift gate is green.
- MERGE-03 is closed: `push: branches: [beta]` is present and verified by reading the file.
- W-4 is closed from a recorded starting state of 9 violations, not the 2 the prior phase predicted.
- W-3's CMake half is closed at the same commit that greens its gate.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| Five `# PY32_EXCLUDED: <path> -- <reason>` comment lines in `platform/py32f071/CMakeLists.txt` | new allow-list entries |
| `DEV_TOOLS`-off decision comment inside `target_compile_definitions` | new commented decision (MERGE-08) |
| `push: branches: [beta]` arm in `.github/workflows/py32f071.yml` | new CI trigger |
| `test_armed_and_passing_on_the_real_tree` in `tests/test_check_cmake_manifest.py` | renamed + inverted test |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-05-SUMMARY.md` when done. Record: the verbatim
9-violation pre-edit output, the verbatim PASS output, the five exclusion lines as written, the emitted
`on:` block, the pytest counts before and after, and the single remaining red with its owning plan.
</output>
