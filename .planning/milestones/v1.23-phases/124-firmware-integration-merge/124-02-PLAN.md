---
phase: 124-firmware-integration-merge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/scripts/check_size_baseline.py
  - firestarter/scripts/baseline/size_baseline_base01.json
  - firestarter/tests/test_check_size_baseline.py
  - firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log
  - firestarter/tests/fixtures/planted_size_baseline_policy_leonardo_growth.log
  - firestarter/tests/fixtures/planted_size_baseline_policy_ram_moved.log
autonomous: true
requirements: [MERGE-05]

must_haves:
  truths:
    - "MERGE-05's rule (Leonardo flash must not grow; Uno-class flash growth <= 64 B; RAM unchanged) is an exit code, not a human comparison of two files (W-1)"
    - "The band comparator FAILS on Leonardo +1 B, on Uno-class +65 B, and on any RAM move — three separately planted logs, three separately named tests"
    - "BASE-01's recorded numbers are frozen byte-identically in a second committed file so Plan 124-10's re-baseline cannot silently move MERGE-05's reference point"
    - "The default (no --policy) strict-equality behaviour is unchanged, so every Phase-123 test arm still discriminates"
  artifacts:
    - firestarter/scripts/baseline/size_baseline_base01.json
    - firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log
    - firestarter/tests/fixtures/planted_size_baseline_policy_leonardo_growth.log
    - firestarter/tests/fixtures/planted_size_baseline_policy_ram_moved.log
  key_links:
    - "--policy merge05 must compose with --baseline so the one-shot MERGE-05 assertion reads the FROZEN BASE-01 file, never whatever size_baseline.json says after Plan 124-10"
    - "MERGE05_UNO_CLASS_FLASH_BAND = 64 is the single place the band lives; the failure string must print the computed delta, not just a verdict"
---

<objective>
Close work item **W-1**: `check_size_baseline.py`'s `compare_avr()` is strict equality and exits 1 on
exactly the deltas MERGE-05 *permits* (Leonardo -56 B, Uno +22 B, uno328pb +28 B — measured). Its own
docstring claims to turn MERGE-05 into an exit code; the implementation does not. Today MERGE-05 has
**no** exit code at all.

Purpose: RESEARCH §W-1 recommends option (c) — a one-shot policy assertion for MERGE-05 **plus** a
re-baseline for the ongoing byte-identity gate. This plan lands half (a): the `--policy merge05` band
comparator, authored **before** the landing so it is proven able to fail against an unmoved tree. Half
(b), the re-baseline, is Plan 124-10 — deliberately separate so the gate that judges the landing is not
written in the same commit as the numbers it judges.

Purpose (second): freeze BASE-01. Plan 124-10 rewrites `scripts/baseline/size_baseline.json` to the
post-landing figures so the DEFAULT run of both gates is correct on the landed tree (the operator's
"cannot be silently forgotten" tie-breaker). That rewrite must not move MERGE-05's reference point, so
BASE-01 is copied byte-identically to `size_baseline_base01.json` here, while the tree is still
unlanded, with its blob SHA recorded.

Output: a `--policy merge05` mode with three planted-violation proofs, and an immutable BASE-01 copy.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-CONTEXT.md

Repo layout: all edits land in the **`firestarter` submodule** at `/workspaces/firestarter`, branch
`v1.23-py32f071-integration`. Commit inside the submodule.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze BASE-01 byte-identically and record its blob SHA</name>
  <files>firestarter/scripts/baseline/size_baseline_base01.json</files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` — the whole file, so the copy is understood to be
      byte-identical rather than "a copy with a note added".
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §W-2 and correction C-13 — the
      recorded `meta.note` claims every figure came from a clean build; that is FALSE for the two native
      warning figures (360 is a warm-cache number; the same tree measures 456 cold).
  </read_first>
  <action>
    Copy `firestarter/scripts/baseline/size_baseline.json` to
    `firestarter/scripts/baseline/size_baseline_base01.json` **byte-identically**. Add nothing, reformat
    nothing, correct nothing — including its `meta.note`, which correction C-13 shows is wrong about the
    two native warning figures. That correction belongs in the NEW baseline Plan 124-10 writes and in
    `124-NONREGRESSION.md`, never retro-fitted into a frozen historical record.

    Prove byte-identity mechanically with `git hash-object` on both paths and record the shared blob SHA
    in the SUMMARY. Do not prove it with a path-scoped `git diff`, which passes vacuously on a wrong path.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(git hash-object scripts/baseline/size_baseline.json)" = "$(git hash-object scripts/baseline/size_baseline_base01.json)" && echo IDENTICAL</automated>
  </verify>
  <acceptance_criteria>
    - `git hash-object` on both paths prints the **same 40-hex blob SHA**, recorded verbatim in the SUMMARY.
    - `python3 -c "import json,sys;a=json.load(open('scripts/baseline/size_baseline.json'));b=json.load(open('scripts/baseline/size_baseline_base01.json'));sys.exit(0 if a==b else 1)"` exits **0**.
    - `wc -c` on the two files prints the **same byte count**.
  </acceptance_criteria>
  <done>BASE-01 exists as an immutable second file whose byte-identity to the current default is proven by a shared blob SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Add --policy merge05 band mode to check_size_baseline.py</name>
  <files>firestarter/scripts/check_size_baseline.py</files>
  <read_first>
    - `firestarter/scripts/check_size_baseline.py` — the WHOLE file. Specifically: the module docstring's
      MERGE-05 claim at lines 3-5, `compare_avr()` at 148-174, `compare_native()` at 179-201,
      `_print_pass`/`_print_fail` at 220-238, `_parse_argv` at 239-278, and `main` from 282. The manual
      argv parser is a recorded house convention — extend it, never swap in argparse.
    - `.planning/REQUIREMENTS.md:47` — MERGE-05's exact wording.
    - `.planning/REQUIREMENTS.md` §"Operator Decisions Locked at Definition" item 4 — why the band is
      64 B rather than zero growth.
    - `.planning/ROADMAP.md` Phase 124 Success Criterion 4 — "Leonardo flash not growing, Uno-class
      flash growth <= 64 B, and RAM unchanged on Uno/Leonardo".
  </read_first>
  <action>
    Extend `_parse_argv` with one new flag, `--policy VALUE`. The only recognised VALUE is `merge05`; any
    other value is a CLI usage error raising `SystemExit(2)` with the existing message shape. Return it
    as a fifth tuple element and update `main`'s unpack.

    Add module constant `MERGE05_UNO_CLASS_FLASH_BAND = 64` with a comment citing
    `.planning/REQUIREMENTS.md:47` and the locked operator decision item 4.

    Add `compare_avr_policy_merge05(env, parsed, baseline)` beside `compare_avr`, returning the same
    list-of-failure-strings shape. Rules:
      - `leonardo`: `flash_used <= rec["flash_used"]` (may shrink, must not grow).
      - `uno` and `uno328pb`: `flash_used <= rec["flash_used"] + MERGE05_UNO_CLASS_FLASH_BAND`.
      - all three: `ram_used == rec["ram_used"]`. Comment that MERGE-05's text binds RAM on Uno/Leonardo
        only and that equality is enforced on uno328pb too because it is measured equal — deliberately
        stronger than the requirement.
      - all three: `flash_total == rec["flash_total"]` and `ram_total == rec["ram_total"]`, reusing
        `compare_avr`'s existing "board or framework moved" message text.
    Every failure string names the env, the baseline figure, the observed figure and the **computed
    delta**, in this shape:
    `uno: flash_used baseline=23932 observed=23997 delta=+65 exceeds MERGE-05 uno-class band of 64 B`.
    Every pass entry likewise carries the delta so the PASS line reports a measurement rather than a
    verdict, in this shape: `uno(flash=23954/32256[+22<=64],ram=1573/2048[=])`.

    Wire into `main`: `policy == "merge05"` routes AVR envs through `compare_avr_policy_merge05`;
    `policy is None` keeps the existing strict-equality path unchanged in behaviour and in output text.
    `--policy` must not alter native comparison in either mode — MERGE-06 is already an exit code and is
    not being loosened.

    Rewrite the module docstring's opening two paragraphs: state plainly that the default mode is strict
    byte-identity against the live baseline and that `--policy merge05` is the band mode which is
    MERGE-05's exit code; name `scripts/baseline/size_baseline_base01.json` as the frozen BASE-01 record
    and `scripts/baseline/size_baseline.json` as the LIVE default that Phase 124 Plan 10 re-baselines;
    extend the Exit-codes block; and add a `Non-claim:` sentence — *a green `--policy merge05` run proves
    the deltas are inside the band the requirement licenses; it proves nothing about whether the deltas
    are desirable, and nothing about the log having come from a clean build (only `--rebuild` guarantees
    that).* Extend Usage with the canonical MERGE-05 invocation naming the frozen baseline.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=tests/fixtures/captured_build_uno.log && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=tests/fixtures/captured_build_uno.log && echo BOTH_MODES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Default mode against `tests/fixtures/captured_build_uno.log` exits **0** and its PASS line is
      textually identical to the pre-change output (capture both before and after; record the two strings
      in the SUMMARY and assert equality).
    - `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=tests/fixtures/captured_build_uno.log`
      exits **0** and its PASS entry contains the literal substring `[+0<=64]`.
    - `python3 scripts/check_size_baseline.py --policy bogus --avr-log uno=tests/fixtures/captured_build_uno.log`
      exits exactly **2** with the message on **stderr**.
    - `python3 scripts/check_size_baseline.py --policy merge05` with no logs and no `--rebuild` exits **1**
      (the never-vacuous guard is not bypassed by the new flag).
    - `grep -c 'MERGE05_UNO_CLASS_FLASH_BAND' scripts/check_size_baseline.py` returns **>= 2** (constant
      definition plus at least one use); `grep -c '64' scripts/check_size_baseline.py` must not be used as
      the check — the band literal appears exactly once, in the constant.
    - `grep -c 'import argparse' scripts/check_size_baseline.py` returns **0**.
    - The docstring contains a line beginning `Non-claim:` and names `size_baseline_base01.json`.
  </acceptance_criteria>
  <done>MERGE-05 has an exit code; the default strict-equality mode is provably unchanged; the band lives in exactly one constant.</done>
</task>

<task type="auto">
  <name>Task 3: Plant three band violations and extend the paired pytest</name>
  <files>firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log, firestarter/tests/fixtures/planted_size_baseline_policy_leonardo_growth.log, firestarter/tests/fixtures/planted_size_baseline_policy_ram_moved.log, firestarter/tests/test_check_size_baseline.py</files>
  <read_first>
    - `firestarter/tests/test_check_size_baseline.py` — the WHOLE file: the module docstring's numbered
      `Coverage:` list and its "Fixture provenance" block (which records exactly how each planted log was
      derived from which captured log), `_run_checker` at ~81, and the existing planted arms at 133-201.
      New arms must extend both the Coverage list and the provenance block, in the same prose shape.
    - `firestarter/tests/fixtures/captured_build_uno.log` and `captured_build_leonardo.log` — the two
      source captures the new planted logs are derived from; look at the exact `RAM:` / `Flash:` report
      lines the parser matches (`SIZE_RE` in the checker, anchored at column 0).
    - `firestarter/tests/fixtures/README.md` — the fixture-directory contract.
  </read_first>
  <action>
    Create three planted logs, each a copy of a captured log with **exactly one number changed**, and
    record the derivation in the test module's provenance block:
      - `planted_size_baseline_policy_uno_over_band.log` = `captured_build_uno.log` with the `Flash:`
        `used` figure raised from 23932 to **23997** (+65, one byte outside the band).
      - `planted_size_baseline_policy_leonardo_growth.log` = `captured_build_leonardo.log` with the
        `Flash:` `used` figure raised from 26072 to **26073** (+1; Leonardo must not grow at all).
      - `planted_size_baseline_policy_ram_moved.log` = `captured_build_uno.log` with the `RAM:` `used`
        figure changed from 1573 to **1574** (+1; RAM equality holds under the band mode too).
    Do not change the `[bar]`/percentage columns — the parser deliberately ignores them, and leaving them
    stale is honest fixture provenance.

    Add four tests to `tests/test_check_size_baseline.py`, each with an f-string message reproducing both
    `stdout` and `stderr`, each invoking the checker as a real subprocess with list argv:
      - `test_policy_merge05_permits_the_measured_landing_deltas` — synthesise the three post-landing AVR
        figures **in a tmp_path copy** of the captured logs (Leonardo 26016, Uno 23954, uno328pb 24004,
        RAM unchanged), run `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json`, and
        assert exit **0**. Docstring must state these are RESEARCH's measured post-landing figures and
        that this test is the pre-landing proof that the band mode will pass on them.
      - `test_policy_merge05_fires_on_uno_class_over_band` — the +65 log -> `result.returncode != 0`, and
        `"delta=+65"` and `"band of 64"` both present in stdout.
      - `test_policy_merge05_fires_on_leonardo_growth` — the +1 log -> `result.returncode != 0`, and
        `"leonardo"` plus `"delta=+1"` present in stdout.
      - `test_policy_merge05_fires_on_ram_move` — the RAM log -> `result.returncode != 0`, and
        `"ram_used"` present in stdout.
    Add one regression test that pins the unchanged default:
      - `test_default_mode_is_unchanged_by_the_new_flag` — run the three `captured_build_*.log` files with
        no `--policy` and assert exit 0 and that stdout does **not** contain the substring `<=64`.

    Extend the module docstring's numbered `Coverage:` list with the five new items and the provenance
    block with the three new derivations.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_check_size_baseline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_check_size_baseline.py -q` reports **0 failed**; the collected count
      is the previous count **+ 5** (record both integers in the SUMMARY).
    - `python3 -m pytest tests/ -q` reports **0 failed**; record the new total.
    - `ls tests/fixtures/ | grep -c '^planted_size_baseline_policy_'` returns **3**.
    - `grep -c 'returncode != 0' tests/test_check_size_baseline.py` returns **>= 4**.
    - `grep -c 'shell=True' tests/test_check_size_baseline.py` returns **0**.
    - `python3 -m pytest tests/test_checker_convention.py -q` reports **0 failed** (the new fixtures raise
      the `planted_*` count, and `FIXTURE_FLOOR` is a `>=` assertion, so this must stay green without
      further edits).
  </acceptance_criteria>
  <done>The band comparator is proven able to fail on three distinct, separately named violations and proven to pass on the exact post-landing figures the landing will produce.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded baseline JSON -> the size claim | Every AVR size statement this milestone makes is judged against a file that a later phase may rewrite |
| gate docstring -> gate behaviour | A docstring that over-claims (as this one did) is worse than no docstring: it makes a red gate look like a bug rather than a gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-05 | Tampering | Plan 124-10's re-baseline silently moving MERGE-05's reference point | high | mitigate | BASE-01 frozen byte-identically as `size_baseline_base01.json` with its blob SHA recorded before anything moves; `--policy merge05` is invoked against the frozen file |
| T-124-06 | Repudiation | a band comparator that passes everything | high | mitigate | Three planted logs, three separately named tests, each asserting the computed delta appears in stdout |
| T-124-07 | Repudiation | `--policy merge05` bypassing the never-vacuous guard | medium | mitigate | Acceptance criterion asserts `--policy merge05` with no logs still exits 1 |
| T-124-08 | Tampering | the new flag silently changing default-mode output | medium | mitigate | Before/after PASS-line string equality asserted for the default path |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && python3 -m pytest tests/ -q` -> 0 failed; record the total.
2. `python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log leonardo=tests/fixtures/captured_build_leonardo.log` -> exit 0.
3. `git -C /workspaces/firestarter status --porcelain` names only this plan's six paths.
4. No file under `src/`, `include/`, `test/`, `platformio.ini` or `.github/` is touched by this plan —
   assert with `git diff --name-only HEAD` and read the list, not with a path-scoped `git diff`.
</verification>

<success_criteria>
- MERGE-05 has an exit code for the first time in this milestone.
- The band is proven to fail at +65 B Uno-class, +1 B Leonardo, and +1 B RAM.
- The band is proven to pass on the exact post-landing figures, before the landing happens.
- BASE-01 is frozen with a recorded blob SHA.
- Default strict-equality behaviour is textually unchanged.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| `firestarter/scripts/baseline/size_baseline_base01.json` | new frozen baseline record |
| `--policy merge05` argv flag | new CLI surface on `check_size_baseline.py` |
| `MERGE05_UNO_CLASS_FLASH_BAND` | new module constant |
| `compare_avr_policy_merge05()` | new function |
| `planted_size_baseline_policy_uno_over_band.log` | new planted fixture |
| `planted_size_baseline_policy_leonardo_growth.log` | new planted fixture |
| `planted_size_baseline_policy_ram_moved.log` | new planted fixture |
| 5 new test functions in `tests/test_check_size_baseline.py` | new coverage |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-02-SUMMARY.md` when done. Record: the shared
BASE-01 blob SHA, the before/after default-mode PASS strings, the three planted deltas, the new
`pytest tests/ -q` total, and the exact canonical MERGE-05 invocation Plan 124-10 must run.
</output>
