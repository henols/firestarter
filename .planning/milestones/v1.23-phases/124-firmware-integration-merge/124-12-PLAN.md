---
phase: 124-firmware-integration-merge
plan: 12
type: execute
wave: 8
depends_on: ["124-10", "124-11"]
files_modified:
  - .planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03, MERGE-04, MERGE-05, MERGE-06, MERGE-07, MERGE-08]

must_haves:
  truths:
    - "All nine cross-repo source-scanning gates are shown to RUN — never skip — and pass, executed from a directory literally named firestarter_app with a merged firestarter sibling (MERGE-07)"
    - "124-NONREGRESSION.md exists in 123-NONREGRESSION.md's shape, every row re-executed in the closing session against the trees as they then stand, never copied from a prior plan's SUMMARY (D-16)"
    - "Its section 8 carries as REASONED EXCEPTIONS the two Phase-123 bullets this phase deliberately violates: a baseline and a watermark WERE adjusted, and a push and gh invocations DID occur"
    - "Every MERGE-01..MERGE-08 requirement is ticked citing the specific row that justifies it, and any that cannot be justified stays unticked with the reason stated"
  artifacts:
    - .planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md
  key_links:
    - "The MERGE-07 sweep must run from /workspaces/firestarter_app and the firmware pytest from /workspaces/firestarter — both suites carry literal-directory-name assertions and a scratch clone with a different name produces a false failure"
    - "A skip is a failure here: BASE-03's census fails the suite if any skip reason claims the firmware checkout is absent while ../firestarter/.git exists, and MERGE-07's wording is 'shown to run, not skip'"
    - "The ARM row's evidence is Plan 124-11's run URL + head SHA, re-queried read-only in this session rather than transcribed"
---

<objective>
Run the full cross-repo gate sweep and write the phase's recorded-evidence artifact.

Purpose: MERGE-07 requires the nine cross-repo source-scanning gates to be shown to **run** — not skip —
and pass, in a directory literally named `firestarter_app` with a merged `firestarter` sibling. D-16
requires that evidence to be a `124-NONREGRESSION.md` in `123-NONREGRESSION.md`'s shape, with every row
re-executed against the trees as they then stand rather than copied from earlier plans' SUMMARY files.

Purpose (second): `123-NONREGRESSION.md` section 8 makes two claims this phase deliberately violates —
that no baseline, watermark, floor or allow-list was adjusted to make a row green, and that no push and no
`gh` invocation occurred. Both are false of Phase 124 by design. Silently dropping them would let the new
artifact inherit a claim it cannot support; they must be carried as explicit, reasoned exceptions.

Output: `124-NONREGRESSION.md`, the MERGE-01..MERGE-08 requirement ticks, and the meta-repo commit.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-firmware-integration-merge/124-RESEARCH.md
@.planning/phases/124-firmware-integration-merge/124-PATTERNS.md
@.planning/phases/124-firmware-integration-merge/124-CONTEXT.md
@.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md

Repo layout: this plan writes into the **meta repo** at `/workspaces` (`.planning/`). It executes commands
in both sub-repos but must modify neither. Pre-existing `firestarter_app` dirt that is NOT this phase's
doing and must not be cleaned up: modified `.gitignore`; untracked `.coverage`, `.planning/config.json`,
`SECURITY.md`, `write_test_port.sh`.

After any `gsd-tools query commit`, check `git rev-parse --abbrev-ref HEAD` — a stale `current_milestone`
under the milestone branching strategy has silently switched branches and reverted gitlinks in this project
before.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the full cross-repo sweep, firmware then host</name>
  <files>(no files modified — sweep execution only)</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md` sections 2 and 3
      — the firmware F-row and host H-row tables, their exact command shapes, and the recorded expected
      values this sweep supersedes or reproduces.
    - `.planning/phases/124-firmware-integration-merge/124-RESEARCH.md` §MERGE-07 — the eleven-row
      post-landing table, including row H5's note that the generator idempotence check writes into the
      firmware tree and was deferred to execute time, and the recorded fact that no gate's scan path is
      invalidated by the landing.
    - `firestarter_app/tests/scan_paths.py` and `tests/fw_presence.py` — the cross-repo scan-path inventory
      and the presence probe.
    - `firestarter/tests/test_checker_convention.py` `test_scope_is_firmware_only` — it asserts the resolved
      scripts path ends in a firmware-named directory, which is why the firmware pytest must run from
      `/workspaces/firestarter` and not from any scratch clone.
  </read_first>
  <action>
    Run every row in this session, in place, in the real `/workspaces` layout. Record for each row: the exact
    command, the expected value, the observed value, and the exit code. Where a tool prints a PASS line,
    quote it verbatim.

    Firmware rows, from `/workspaces/firestarter`:
      - the firmware pytest suite with `-q`, recording passed and skipped integers;
      - both pinned native envs, recording cases, suites and all-passed;
      - the third native env from Plan 124-08, recording its cases and suites;
      - all three AVR clean builds, recording flash and RAM;
      - `check_size_baseline.py` in default mode against the new live baseline, for all three AVR envs;
      - `check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json` for
        all three AVR envs — the MERGE-05 row;
      - `check_size_baseline.py --native-log` for both pinned envs — the MERGE-06 count row;
      - `check_build_warnings.py --log` for all three AVR envs and all three native envs;
      - `check_cmake_manifest.py` — now ARMED and PASSING, which is the inversion of Phase 123's unarmed row;
      - `check_orphan_provisional.py` — likewise armed and passing, naming both provisional macros;
      - `check_landing_range.py` — the MERGE-01 row;
      - `pytest tests/test_golden_trace_identity.py` and `tests/test_pinmap_guard_fires.py` individually,
        since they carry MERGE-06's per-array clause and MERGE-04's fire-proof.

    Host rows, from `/workspaces/firestarter_app` — a directory literally named `firestarter_app` with the
    merged `firestarter` sibling:
      - the nine cross-repo source-scanning gates, presented as the same eleven rows Phase 123 used (three
        gates contribute both a checker invocation row and a paired-pytest row). Reproduce Phase 123's row
        ids so the two documents are comparable side by side;
      - row H5's generator idempotence check, which was deferred at research time because it writes into the
        firmware tree: run it, and prove idempotence by `git -C /workspaces/firestarter status --porcelain`
        being **empty before and empty after**, recording both observations. If it is non-empty after,
        record the diff and treat it as a finding;
      - the full host suite with `-rs`, recording the passed integer and asserting **zero** SKIPPED lines. A
        skip claiming the firmware checkout is absent while the sibling marker exists is a hard failure by
        BASE-03's census;
      - `check_no_exists_proxy.py`, `ruff check`, `ruff format --check`, and the mypy watermark check, so the
        host repo's own hygiene rows are comparable to Phase 123's.

    ARM row: re-query Plan 124-11's run with read-only `gh` in this session and record the id, head SHA,
    conclusion, event and URL freshly. Do not transcribe them from Plan 124-11's SUMMARY — D-16's
    re-execution pledge covers this row too, and a re-query is its re-execution.

    Meta row: run the milestone's claim scanner over this phase's artifacts as a courtesy check, the same way
    Phase 123 did, and record its exit code and output.

    Modify nothing in either sub-repo. Confirm at the end that `git -C /workspaces/firestarter status
    --porcelain` is empty and that `git -C /workspaces/firestarter_app status --porcelain` matches the
    pre-existing dirt list named in this plan's context, item for item.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -q -rs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The host suite reports **0 failed** and **0 skipped**, with zero `SKIPPED` lines under `-rs`; the passed integer is recorded and compared to Phase 123's recorded 1158.
    - All eleven host cross-repo rows PASS with exit code 0; each PASS line is quoted.
    - The firmware pytest suite reports **0 failed**; the total is recorded and stated as superseding `123-NONREGRESSION.md` row F1's recorded 48.
    - Both pinned native envs report exactly **141 cases / 17 suites**, all PASSED.
    - `check_cmake_manifest.py` and `check_orphan_provisional.py` both exit **0** with `PASS:` lines — the inversion of Phase 123's `UNARMED:` rows is recorded explicitly as such.
    - `check_landing_range.py` exits **0** with its commits-scanned, carrying and violations integers recorded.
    - Row H5's idempotence is proven by an empty `git status --porcelain` in the firmware repo both before and after the generator run; both observations recorded.
    - The ARM run's five fields are re-queried in this session, and the head SHA is compared to the live firmware HEAD with the result stated.
    - `git -C /workspaces/firestarter status --porcelain` is empty; `git -C /workspaces/firestarter_app status --porcelain` matches the named pre-existing dirt exactly.
  </acceptance_criteria>
  <done>Every gate this phase is judged by has been executed in this session, in the real layout, with its observed value recorded — and none reported a skip.</done>
</task>

<task type="auto">
  <name>Task 2: Write 124-NONREGRESSION.md with its section 8 exceptions stated, not dropped</name>
  <files>.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-NONREGRESSION.md` — the WHOLE file
      (343 lines). Reproduce its section structure: header with branches, HEAD SHAs and recorded fork points;
      the re-execution pledge; section 1 the claim as precise statements; section 2 the baseline as recorded
      and as re-verified; section 3 the gate table with command / expected / observed and bolded observed
      figures; section 4 what is armed and what it fired on; section 5 known and explained conditions;
      section 6 the phase-wide range proof; section 7 the validation ceiling; section 8 deliberately not
      taken; and the closing sweep summary table.
    - `.planning/phases/124-firmware-integration-merge/124-PATTERNS.md` §10 — the section-by-section mapping
      and the explicit instruction that Phase 124's section 8 must rewrite two bullets as reasoned exceptions
      rather than silently dropping them.
    - `.planning/REQUIREMENTS.md:14` — the forbidden-claim list, to be cited **by location** rather than
      reproduced verbatim, exactly as `123-NONREGRESSION.md` section 7 explains and for the same reason: the
      claim scanner matches a phrase's shape regardless of quotation or negation context, by design.
    - `.planning/phases/123-non-regression-baselines-gate-hardening/123-01-SUMMARY.md` lines 46-47 — the
      recorded fork points to cite in the header and to use in the section 6 range proof.
  </read_first>
  <action>
    Write `.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` from Task 1's observations.
    Nothing may be copied from any of this phase's eleven prior SUMMARY files; where a prior SUMMARY made a
    claim, this document re-checked it in Task 1 and says so, and any disagreement is reported as a finding
    rather than reconciled.

    Section 4 inverts Phase 123's: instead of "what is UNARMED today and what arms it", record **what armed
    and what it fired on** — the CMake manifest gate's nine violations on arrival against the two Phase 123
    predicted, the orphan-provisional gate's single violation, the native warning gate's unpredicted firing,
    and the two Phase-123 pytests that expired. For each, state which plan closed it.

    Section 6 is the phase-wide range proof. Use the cumulative `fork_point_firmware..HEAD` range with the
    fork point read from the recorded field, asserted non-empty and asserted an ancestor of HEAD **before**
    any diff runs. Do not prove anything "untouched" with a path-scoped `git diff` — it passes vacuously on a
    wrong path. Where something must be shown unchanged, use `git status --porcelain` empty or a literal blob
    SHA. Phase 124 moved firmware code by design, so this section's job is the opposite of Phase 123's: it
    must enumerate what moved and show that everything outside that set did not.

    Section 8 must carry both violated bullets as **explicit, reasoned exceptions**, each stating what was
    changed, why the requirement licensed it, and what preserves the prior record:
      - a baseline and two watermarks WERE adjusted. Name `scripts/baseline/size_baseline.json` as
        re-baselined by Plan 124-10; name `scripts/baseline/size_baseline_base01.json` as the frozen BASE-01
        with its blob SHA; state that MERGE-05's band assertion runs against the frozen file, so no bar was
        lowered to make MERGE-05 green — the band was always the requirement's own wording and the previous
        gate simply did not implement it (work item W-1); and state that the native watermark was **raised**
        with a cold measurement, with the warm-versus-cold correction to the prior record recorded in the
        JSON itself (work item W-2). Give the numbers.
      - a push and `gh` invocations DID occur. Name Plan 124-11, state that the push was the firmware
        milestone branch and not `beta`, that no tag, release, gitlink bump or public comment was made, that
        the safety argument was re-proven by reading all three workflow `on:` blocks, and that the action sat
        behind a structural operator gate the chain could not wave through (D-09).
    Then re-assert, unchanged and honestly, the bullets that DO still hold for this phase — no cross-repo CI
    leg was added, no release was cut, `beta` was not pushed, and Phase 130 still owns every remaining
    outward-facing action.

    Section 7 cites the forbidden list by location and states line by line that every result in the document
    has a software artifact as its subject — a git blob or ref identity, a size report, an exit code, a
    checker's own PASS line, a source read, or a CI run's reported conclusion — and never a silicon
    observation, because no PY32F071 hardware exists to observe. State plainly that a green claim scan is the
    mechanizable half only.

    Finally, run the milestone claim scanner over the finished file and record its exit code and output in
    the document, as Phase 123 did.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/phases/123-non-regression-baselines-gate-hardening/check_permitted_claims.py .planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and contains all nine structural sections plus the closing sweep summary table, in `123-NONREGRESSION.md`'s order.
    - The header records the firmware branch and HEAD SHA, the host branch and HEAD SHA, the two recorded fork points, and the meta branch and HEAD — all observed in this session.
    - The re-execution pledge is present and is true: state explicitly that every row was executed in Task 1 of this session.
    - Section 3's table carries every row from Task 1 with command, expected and observed, and every observed figure is a number, never "passed".
    - Section 8 contains the two reasoned exceptions with their numbers, and separately re-asserts the bullets that still hold. A reader can tell which of Phase 123's claims Phase 124 inherits and which it deliberately does not.
    - Section 6's range proof reads the fork point from the recorded field, asserts it non-empty, and asserts it an ancestor before diffing; no path-scoped `git diff` is used as an untouched-proof anywhere in the document.
    - The claim scanner exits **0** against the finished file, and its exit code and output are recorded inside the document.
    - `grep -c 'diff --stat' .planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` — every hit, if any, carries an explicit ref range; a ref-less form is forbidden (the exact defect a plan checker flagged as a blocker in Phase 123).
  </acceptance_criteria>
  <done>The phase has a recorded-evidence artifact in the established shape whose every row was executed in the closing session, and which states the two prior claims it cannot inherit rather than quietly dropping them.</done>
</task>

<task type="auto">
  <name>Task 3: Tick MERGE-01..MERGE-08 against the evidence, and commit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 43-50 — the eight MERGE requirement statements verbatim. Read the
      **prose**, not a plan's summary of it: this project has recorded an incident where a plan's ticking
      scope diverged from the requirement text.
    - `.planning/REQUIREMENTS.md` §Traceability — the requirement-to-phase table whose Phase 124 row moves
      from Pending.
    - `.planning/ROADMAP.md` Phase 124 Success Criteria 1-5 — the criteria the ticks must also satisfy.
    - `124-NONREGRESSION.md` as written in Task 2 — the source of each citation.
  </read_first>
  <action>
    For each of MERGE-01 through MERGE-08, read the requirement prose and decide, one at a time, whether the
    evidence in `124-NONREGRESSION.md` satisfies **every clause** of it. Tick it only if so, and record
    alongside each tick the specific section-3 row id or section-4 entry that justifies it.

    Pay particular attention to the multi-clause requirements:
      - MERGE-01 has three clauses: the atomic landing, the inclusion of `780a3fb`, and the absence of any
        commit carrying the portability half alone.
      - MERGE-02 has two: the manifest naming the renamed files, and the ARM target reaching a successful
        CMake configure. The ROADMAP criterion additionally requires a successful **build**, cited by run URL
        and SHA.
      - MERGE-04 has two: the refusal of every PROM-energising operation, and the guard being restructured so
        its error is provably able to fire.
      - MERGE-05 has three: Leonardo not growing, Uno-class growth within the band and recorded, and flash
        **and RAM** recorded for all three targets.
      - MERGE-06 has two: the case **and suite** counts on both envs, and per-array golden-trace identity.
      - MERGE-08 has three named defects.
    If any clause of any requirement is unsatisfied — most plausibly MERGE-02's, if the ARM run failed or the
    dispatch was rejected — leave that requirement **unticked** and record the reason and the owning gap in
    both `REQUIREMENTS.md`'s Traceability notes and `124-NONREGRESSION.md`. Do not tick on the strength of a
    plan having run.

    Update the Traceability table's Phase 124 row to match what was actually ticked.

    Commit the meta-repo changes with the `gsd-tools query commit` helper, scoping the commit to this plan's
    two files. Immediately afterwards, run `git rev-parse --abbrev-ref HEAD` and confirm the branch is still
    `gsd/v1.23-py32f071-integration`; this project has had a stale milestone value silently switch branches
    and revert gitlinks during a commit. If the branch changed, stop and report before doing anything else.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n 'MERGE-0' .planning/REQUIREMENTS.md | head -20 && git rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <acceptance_criteria>
    - Each of MERGE-01..MERGE-08 is either ticked with a cited justifying row id, or left unticked with a recorded reason. Every one of the eight is accounted for explicitly; none is left ambiguous.
    - The multi-clause requirements listed in the action each have a per-clause justification recorded, not one blanket citation.
    - The Traceability table's Phase 124 row reflects the actual tick state.
    - `git rev-parse --abbrev-ref HEAD` prints `gsd/v1.23-py32f071-integration` after the commit; the observed value is recorded.
    - `git -C /workspaces status --porcelain` shows no unexpected staged or modified paths beyond this plan's two files and the pre-existing submodule dirt.
    - `git -C /workspaces/firestarter status --porcelain` is empty and `git -C /workspaces/firestarter_app status --porcelain` matches the named pre-existing dirt — this plan modified neither sub-repo.
  </acceptance_criteria>
  <done>The milestone's requirement ledger reflects what the evidence actually supports, clause by clause, and the meta commit landed on the branch it was supposed to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate execution -> the non-regression claim | The whole phase's acceptance rests on gates running rather than skipping |
| recorded evidence -> future phases and the milestone close | Phases 125-130 and the honesty ledger cite this document |
| a prior phase's claims -> this phase's artifact | Reusing a template silently reuses its assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-124-51 | Repudiation | a gate leg flipping to SKIP at exit 0 with a false firmware-absent reason | critical | mitigate | Full host suite with `-rs` and zero SKIPPED lines asserted; run from the literally-named directory with the merged sibling; BASE-03's census active |
| T-124-52 | Repudiation | inheriting Phase 123's "no bar was lowered" and "no push occurred" claims while both are false here | critical | mitigate | Section 8 rewrites both as reasoned exceptions with numbers and names; the still-true bullets are re-asserted separately |
| T-124-53 | Repudiation | copying a row from a prior SUMMARY instead of re-executing it | high | mitigate | The re-execution pledge, plus Task 1 executing every row including a fresh read-only re-query of the ARM run |
| T-124-54 | Repudiation | ticking a requirement whose second clause is unmet | high | mitigate | Per-clause justification required for the six multi-clause requirements; an unsatisfied clause leaves the requirement unticked with a recorded reason |
| T-124-55 | Repudiation | a forbidden claim entering the closing artifact | high | mitigate | The milestone claim scanner run over the finished file with its exit code recorded inside it; the forbidden list cited by location rather than reproduced |
| T-124-56 | Tampering | a commit helper silently switching branches and reverting gitlinks | high | mitigate | Branch re-checked immediately after the commit with a stop-and-report on any change |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q -rs` -> 0 failed, 0 skipped.
2. `cd /workspaces/firestarter && python3 -m pytest tests/ -q` -> 0 failed.
3. Both pinned native envs -> 141 cases / 17 suites each.
4. `check_landing_range.py`, `check_cmake_manifest.py`, `check_orphan_provisional.py` -> exit 0 each.
5. The claim scanner over `124-NONREGRESSION.md` -> exit 0.
6. `git rev-parse --abbrev-ref HEAD` in `/workspaces` -> `gsd/v1.23-py32f071-integration`.
7. Both sub-repos' `git status --porcelain` unchanged by this plan.
</verification>

<success_criteria>
- MERGE-07 is discharged by nine gates shown to run and pass, with zero skips, in the literally-named layout.
- `124-NONREGRESSION.md` exists in the established shape, re-executed rather than assembled from SUMMARY files.
- The two Phase-123 claims this phase violates are carried as reasoned exceptions with their numbers.
- Every MERGE requirement is ticked against a cited row or left unticked with a stated reason — clause by clause, never on the strength of a plan having run.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind |
|---|---|
| `.planning/phases/124-firmware-integration-merge/124-NONREGRESSION.md` | new recorded-evidence artifact (9 sections + sweep summary) |
| MERGE-01..MERGE-08 tick state and Traceability row in `.planning/REQUIREMENTS.md` | updated requirement ledger |

<output>
Create `.planning/phases/124-firmware-integration-merge/124-12-SUMMARY.md` when done. Record: the full sweep
results row by row; the host passed/skipped integers against Phase 123's 1158/0; the firmware pytest total
against 48; both native env counts; the ARM run's re-queried five fields; the claim scanner's exit code; the
per-requirement tick decisions with their cited rows; and the branch name observed after the commit.
</output>
