---
phase: 08-convert-state-machine-prefix-call-sites-ok-init-main-end
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - firestarter/include/logging_id.h
  - firestarter/src/hardware_operations.cpp
  - firestarter/src/firestarter.cpp
  - firestarter/src/eprom_operations.cpp
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/tests/test_decoder.py
autonomous: true
requirements:
  - LMIG-03
must_haves:
  truths:
    - "logging_id.h exports the LOG_OK_ID_U8_U8 (P-02 helper) + LOG_OK_ID_BYTES_U32_U32_U8 (P-03 helper) + LOG_OK_ID_U8_U8_ASTR (P-04 composite) emit macros"
    - "hardware_operations.cpp send_ack_const(\"Ready\") → LOG_OK_ID(MSG_OK_READY) (line ~44)"
    - "hardware_operations.cpp VPP/VPE log_data_format split into LOG_DATA_ID_U16_U16(MSG_DATA_VPP_VOLTAGE, voltage_mv, vcc_mv) or LOG_DATA_ID_U16_U16(MSG_DATA_VPE_VOLTAGE, voltage_mv, vcc_mv) depending on handle->cmd (lines ~67-69)"
    - "hardware_operations.cpp fw_get_version (line ~80) — keeps text path per LFW-05 + P-01 (MSG_OK_FW_VERSION stays wire_format='text'); the bootstrap probe still parses 'FW:' as text"
    - "hardware_operations.cpp hw_get_version (lines ~85-91) → LOG_OK_ID_U8_U8(MSG_OK_REV, physical, effective_or_0xFF_sentinel) per P-02"
    - "hardware_operations.cpp hw_get_config (lines ~94-104) → composite LOG_ID_BYTES emit packing u32 r1 + u32 r2 + u8 override (9 wire bytes) for MSG_OK_CFG per P-03"
    - "firestarter.cpp send_ack_format(PARSE_RESPONSE, ...) at lines ~148-154 → LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE, hw_rev, cmd, FW_VERSION) per P-04 — single emit replaces both #ifdef branches; hw_rev = 0xFF when HARDWARE_REVISION is not defined"
    - "eprom_operations.cpp (firmware side) — chip-read streaming wraps each chunk in MSG_DATA_CHUNK (W-04). Either a new emit helper in rurp_serial_utils.cpp or a widened _firestarter_emit_frame variant carries the chunk body up to 512 bytes (Uno) / 1024 bytes (Leonardo) using the u16 len field from Plan 02"
    - "Host eprom_operations.py (firestarter_app side) — chip-read receive loop decodes MSG_DATA_CHUNK frames and extracts the params[0] bytes payload as raw chip data; the legacy 'DATA:' text-prefix read path is removed for the chunk stream (the bootstrap MSG_DATA_SENDING zero-param ack still arrives as an id_frame from Plan 04)"
    - "serial_comm.py — if needed, _format_message handles MSG_DATA_CHUNK specifically (do not stringify the bytes payload; return a short summary like '<chunk: N bytes>' for log display)"
    - "New test_decoder.py end-to-end MSG_DATA_CHUNK roundtrip test (existing Wave-0 gap test from Plan 02 + a new test asserting the host eprom_operations.py reader can consume a sequence of chunked frames)"
    - "All AVR builds + native test_messages + native test_dispatch + host test_decoder.py suites pass after every commit"
    - "End-to-end: a host pytest that mocks the firmware + simulated chip + drives a read operation produces a byte-identical output file vs the pre-Phase-8 baseline (Success Criterion 3 in ROADMAP)"
  artifacts:
    - path: "firestarter/include/logging_id.h"
      provides: "LOG_OK_ID_U8_U8 + LOG_OK_ID_BYTES_U32_U32_U8 (or equivalent) + LOG_OK_ID_U8_U8_ASTR composites"
      contains: "LOG_OK_ID_U8_U8_ASTR"
    - path: "firestarter/src/hardware_operations.cpp"
      provides: "Ready + VPP/VPE + hw_get_version + hw_get_config converted to ID frames"
      contains: "LOG_DATA_ID_U16_U16(MSG_DATA_VPP_VOLTAGE"
    - path: "firestarter/src/firestarter.cpp"
      provides: "PARSE_RESPONSE composite FW_HANDSHAKE conversion (P-04)"
      contains: "LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE"
    - path: "firestarter/src/eprom_operations.cpp"
      provides: "MSG_DATA_CHUNK chip-byte streaming wrapper"
      contains: "MSG_DATA_CHUNK"
    - path: "firestarter_app/firestarter/eprom_operations.py"
      provides: "host chip-read receive loop decodes MSG_DATA_CHUNK frames"
      contains: "MSG_DATA_CHUNK"
  key_links:
    - from: "firestarter/src/firestarter.cpp"
      to: "firestarter_app/firestarter/serial_comm.py"
      via: "MSG_OK_FW_HANDSHAKE composite frame round-trip (P-04)"
      pattern: "MSG_OK_FW_HANDSHAKE"
    - from: "firestarter/src/eprom_operations.cpp"
      to: "firestarter_app/firestarter/eprom_operations.py"
      via: "MSG_DATA_CHUNK streaming wire format (W-04)"
      pattern: "MSG_DATA_CHUNK"
---

<objective>
Wave 5 — convert the COMPOSITE / FIXED-SHAPE state-machine ack call-sites that need new helper macros and host parser awareness: the 5 hardware_operations.cpp call-site groups (Ready ack, VPP/VPE voltage split, fw_get_version stays text, hw_get_version P-02, hw_get_config P-03), the firestarter.cpp PARSE_RESPONSE composite (P-04 FW_HANDSHAKE), AND the MSG_DATA_CHUNK chip-byte streaming wrapper (W-04) on both firmware and host sides.

Why this is its own plan (not folded into Plan 04): hardware_operations.cpp + firestarter.cpp are large files with multiple distinct conversion sites each needing different helper macros (LOG_OK_ID_U8_U8 for P-02; a 9-byte composite for P-03; the LOG_OK_ID_U8_U8_ASTR for P-04). The MSG_DATA_CHUNK conversion crosses both sub-repos and requires a host-side eprom_operations.py rewrite — Pitfall 5 in RESEARCH explicitly flags this as the largest semantic shift in Phase 8.

Out of scope (deferred):
- `_check_response` log_info/log_data strip + `response_msg` field deletion + `copy_to_buffer` macro deletion → Plan 06
- debug() sweep → Plan 07
- Phase-close measurement → Plan 08

Purpose: after this plan, every state-machine ack and every DATA emit is an ID frame end-to-end (firmware emit + host decode). The Phase 8 success criterion #2 ("firestarter write -e W27C512 runs end-to-end with INIT/MAIN/END rendered from ID-frame decoding alone") becomes true here.
Output: 2 firmware files updated (hardware_operations.cpp, firestarter.cpp) + the MSG_DATA_CHUNK firmware+host pair + a new emit helper or sibling function in rurp_serial_utils.cpp if the existing function's uint8_t param_count can't carry 512-byte chunks + host eprom_operations.py rewrite + new MSG_DATA_CHUNK roundtrip test on the host side.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-CONTEXT.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-04-SUMMARY.md

<interfaces>
Helper macros required (added to logging_id.h in Task 1 of this plan):
- LOG_OK_ID_U8_U8(id, p1, p2) — composite emit for P-02 (MSG_OK_REV). Packs two u8s into a 2-byte stack buffer; calls rurp_log_id(id, _b, 2).
- LOG_OK_ID_BYTES_U32_U32_U8(id, r1, r2, ovr) — composite emit for P-03 (MSG_OK_CFG). Packs r1 (u32 big-endian, 4 bytes) + r2 (u32 BE, 4 bytes) + ovr (u8, 1 byte) into a 9-byte stack buffer; calls rurp_log_id(id, _b, 9). Alternative shape: call-site uses LOG_ID_BYTES with an inline-built 9-byte buffer (no new macro) — pick whichever the executor finds cleaner; if no new macro is needed, omit it from logging_id.h additions.
- LOG_OK_ID_U8_U8_ASTR(id, p1, p2, str_ptr) — composite emit for P-04 (MSG_OK_FW_HANDSHAKE). Packs u8 p1 + u8 p2 + u8 ascii_str length-prefix + N ascii_str data bytes into a 3 + N stack buffer (N <= 32, clamped via strnlen-style cap); calls rurp_log_id(id, _b, 3 + N).

ascii_str wire encoding (catalog convention from Phase 6 / Plan 01 for P-04):
- 1-byte u8 length prefix followed by N data bytes (no NUL terminator on the wire).
- N capped at 32 in the firmware composer (FW_VERSION strings are short — e.g. "2.0.11-dev" is 10 bytes).

hardware_operations.cpp existing call-sites (from RESEARCH §"Key Files (Phase 8 modification targets — verified existence)" + CONTEXT canonical-refs):
- Line ~44: `send_ack_const("Ready")` → `LOG_OK_ID(MSG_OK_READY)` — zero-param OK ack.
- Lines ~60-76: VPP/VPE path — currently `const char* type = (handle->cmd == CMD_READ_VPE) ? "VPE" : "VPP"; log_data_format("%s: %u.%uV, Internal VCC: %u.%uV", type, ...)`. Split into two ID frames per CONTEXT: when handle->cmd == CMD_READ_VPE, emit LOG_DATA_ID_U16_U16(MSG_DATA_VPE_VOLTAGE, voltage_mv, vcc_mv); else emit LOG_DATA_ID_U16_U16(MSG_DATA_VPP_VOLTAGE, voltage_mv, vcc_mv). The host-side _format_message (Plan 03) renders the decimal split.
- Line ~79-81: fw_get_version — `send_ack_const(FW_VERSION)`. Per RESEARCH Open Question 3 + LFW-05 + P-01 conclusion: KEEP THIS AS TEXT (do not convert). The MSG_OK_FW_VERSION catalog entry stays wire_format = "text" and this call-site emits via send_ack_const which expands to the text-frame path. The host's _probe_port bootstrap still parses "FW: ..." as text.
- Lines ~85-91: hw_get_version — `send_ack_format("Rev%d%s", rurp_get_physical_hardware_revision(), revStr)`. Convert to P-02: compute effective from rurp_config->hardware_revision (use 0xFF sentinel when hardware_revision >= 0xFF or when no override is active — read the current revStr building logic to map the existing "no override" case to the 0xFF sentinel). Emit LOG_OK_ID_U8_U8(MSG_OK_REV, (uint8_t)physical, effective).
- Lines ~94-104: hw_get_config — `send_ack_format("R1: %ld, R2: %ld%s", rurp_config->r1, rurp_config->r2, revStr)`. Convert to P-03: pack r1 (u32 BE) + r2 (u32 BE) + override byte (0xFF or hw_revision) into a 9-byte buffer and emit via LOG_OK_ID_BYTES_U32_U32_U8(MSG_OK_CFG, r1, r2, override) — or inline the same shape via LOG_ID_BYTES(MSG_OK_CFG, _cfg, 9).

firestarter.cpp PARSE_RESPONSE composite (P-04):
- Lines ~148-154: `#ifdef HARDWARE_REVISION` → `send_ack_format("FW: " FW_VERSION ", HW: Rev%d, Cmd: 0x%02x", rurp_get_hardware_revision(), handle->cmd);` `#else` `send_ack_format("FW: " FW_VERSION ", Cmd: 0x%02x", handle->cmd);` `#endif`. Convert to a single LOG_OK_ID_U8_U8_ASTR emit per RESEARCH §"P-04 FW_HANDSHAKE conversion (two-line firmware pattern)". Compute hw_rev = (uint8_t)rurp_get_hardware_revision() when HARDWARE_REVISION is defined, else hw_rev = 0xFF. Emit LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE, hw_rev, (uint8_t)handle->cmd, FW_VERSION). Delete the `#define PARSE_RESPONSE ...` lines along with the converted call-sites.

MSG_DATA_CHUNK wire shape:
- Frame: MAGIC (4 B) | len_u16 BE (2 B) | id = MSG_DATA_CHUNK (1 B) | params (1 byte for ascii_str-like length-prefix bytes? — NO. params for MSG_DATA_CHUNK = the raw chip-byte payload directly. The frame_len field already carries the body size.) | crc (1 B) | 0x0A anchor (1 B).
- params shape in messages.toml = `[{type="bytes"}]` (Plan 01 declared this). The host's _decode_param for type "bytes" reads ALL remaining bytes of the body after the id (and before the crc) as the bytes payload. Verify the existing _decode_param "bytes" branch in serial_comm.py handles this (Phase 6 likely already did).

Host chip-read receive loop (firestarter_app/firestarter/eprom_operations.py):
- Pitfall 5 in RESEARCH explicitly warns this code path was not fully inspected during research. Executor MUST `cat firestarter_app/firestarter/eprom_operations.py` and locate the chip-read loop (probably named `read_chip` or `chip_read` or similar). The existing loop:
  1. Calls firestarter command "read" with chip params.
  2. Reads a "DATA: <size>" prefix line OR a direct binary block based on how the firmware framed it.
  3. Reads `data_size` raw bytes off the wire.
  4. Appends to output file.
- Phase 8 target: instead of reading raw bytes after a DATA: prefix, the loop receives a sequence of MSG_DATA_CHUNK id_frames. Each frame's `Response` carries the raw chunk bytes (as params[0] or a dedicated `.payload` attribute on Response, depending on how Plan 03 wired the parser). The loop appends each chunk's payload to the output file and continues until an END id_frame (MSG_END_DONE) arrives.
- The loop uses serial_comm's existing get_response/_get_response/get_data API (verify the existing call sequence; do not re-implement the parser — reuse the Plan 03 + Phase 6 infrastructure).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LOG_OK_ID_U8_U8 + LOG_OK_ID_U8_U8_ASTR composites (and optionally LOG_OK_ID_BYTES_U32_U32_U8) to logging_id.h</name>
  <files>firestarter/include/logging_id.h</files>
  <read_first>
    - firestarter/include/logging_id.h (post-Plan-04 — the LOG_OK_ID/INIT/MAIN/END/DATA families exist; this task ADDS the composite emitters)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"New composite pattern for LOG_OK_ID_U8_U8_ASTR" (the exact stack-buffer + strlen pattern)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Code Examples" §"P-04 FW_HANDSHAKE conversion"
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    Step 1 — confirm string.h availability. The composite ascii_str macro uses strlen + memcpy. Verify `#include <string.h>` is already in logging_id.h or transitively pulled in by an existing include. If absent and the existing macros do not already require it, add `#include <string.h>` near the top of logging_id.h.

    Step 2 — add LOG_OK_ID_U8_U8(id, p1, p2) macro:
    Insert before the closing `#endif` of logging_id.h:
    A `do { ... } while (0)` wrapping a local `uint8_t _b[2] = { (uint8_t)(p1), (uint8_t)(p2) }; rurp_log_id((id), _b, 2);`.

    Step 3 — add LOG_OK_ID_U8_U8_ASTR(id, p1, p2, str_ptr) macro:
    Stack buffer sized 2 + 1 + 32 = 35 bytes. Use the exact composite pattern in 08-PATTERNS.md: cast str_ptr to const char*, compute uint8_t _slen = (uint8_t)strlen(_s) capped at 32 (use `if (_slen > 32) _slen = 32;` or `_slen = (_slen > 32) ? 32 : _slen;` before the memcpy), set `_b[0] = (uint8_t)(p1); _b[1] = (uint8_t)(p2); _b[2] = _slen; memcpy(_b + 3, _s, _slen);` then `rurp_log_id((id), _b, (uint8_t)(3 + _slen));`.

    Step 4 — (optional) LOG_OK_ID_BYTES_U32_U32_U8(id, r1, r2, ovr):
    If the call-site in hardware_operations.cpp Task 2 prefers an inline-9-byte LOG_ID_BYTES emit (no new macro), SKIP this step. Otherwise, add the do-while wrapper that packs r1 (4 bytes big-endian) + r2 (4 bytes BE) + ovr (1 byte) into a 9-byte stack buffer and calls rurp_log_id(id, _b, 9). The executor's choice — both shapes are equivalent.

    Build verification: `cd firestarter && pio run -e uno --target clean && pio run -e uno && pio run -e leonardo --target clean && pio run -e leonardo && pio test -e native -f "*test_messages*"`. All green.

    Commit subject inside firestarter: `feat(logging_id.h): add LOG_OK_ID_U8_U8 + LOG_OK_ID_U8_U8_ASTR composites (P-02, P-04)`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom/firestarter &amp;&amp; grep -q "LOG_OK_ID_U8_U8\b\|LOG_OK_ID_U8_U8(" include/logging_id.h &amp;&amp; grep -q "LOG_OK_ID_U8_U8_ASTR" include/logging_id.h &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native -f "*test_messages*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "LOG_OK_ID_U8_U8(" firestarter/include/logging_id.h` exits 0.
    - `grep -q "LOG_OK_ID_U8_U8_ASTR" firestarter/include/logging_id.h` exits 0.
    - Both AVR builds green; native test_messages green.
    - firestarter commit landed with subject starting `feat(logging_id.h): add LOG_OK_ID_U8_U8`.
  </acceptance_criteria>
  <done>
    Composite helper macros for P-02 + P-04 present in logging_id.h. Both AVR builds + native suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert hardware_operations.cpp call-sites — Ready ack, VPP/VPE split, hw_get_version (P-02), hw_get_config (P-03). fw_get_version stays text per LFW-05.</name>
  <files>firestarter/src/hardware_operations.cpp</files>
  <read_first>
    - firestarter/src/hardware_operations.cpp (full file — locate every send_ack_const / send_ack_format / log_data_format call-site; verify exact line numbers for the 5 conversion groups; pay close attention to the revStr building logic around lines 85-104 — it gates the override sentinel)
    - firestarter/include/messages.h (confirm MSG_OK_READY (0x01), MSG_OK_REV (0x04, post-P-02 shape), MSG_OK_CFG (0x05, post-P-03 shape), MSG_DATA_VPP_VOLTAGE, MSG_DATA_VPE_VOLTAGE all present)
    - firestarter/include/logging_id.h (post-Task-1 — LOG_OK_ID_U8_U8 + LOG_OK_ID_U8_U8_ASTR + LOG_DATA_ID_U16_U16 from Plan 04 all present)
    - firestarter/include/firestarter.h (rurp_config struct definition — confirm `hardware_revision` field exists and the 0xFF sentinel convention is honored elsewhere)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter/src/hardware_operations.cpp" (all 4 site patterns)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Open Questions" §3 (P-01 fw_get_version stays text)
  </read_first>
  <action>
    Site 1 — line ~44 Ready ack:
    Replace `send_ack_const("Ready");` with `LOG_OK_ID(MSG_OK_READY);`. Zero-param OK ack; no response_code change.

    Site 2 — lines ~60-76 VPP/VPE voltage split:
    Locate the existing `const char* type = (handle->cmd == CMD_READ_VPE) ? "VPE" : "VPP";` block and the subsequent `log_data_format("%s: %u.%uV, Internal VCC: %u.%uV", type, voltage_mv/1000, ...)` call. Replace the entire block with a conditional emit:
    - If `handle->cmd == CMD_READ_VPE`: `LOG_DATA_ID_U16_U16(MSG_DATA_VPE_VOLTAGE, voltage_mv, vcc_mv);`
    - Else: `LOG_DATA_ID_U16_U16(MSG_DATA_VPP_VOLTAGE, voltage_mv, vcc_mv);`
    Drop the local `type` string variable (it's no longer needed) and the format-string arithmetic (host renders the decimal split per Plan 03's _format_message — wait, actually the catalog format string from Plan 01 already handles this via the format-string positional args). Verify the renderer produces the same human output as the current `log_data_format`; this is a wire-format win (4 bytes vs ~25 bytes per emit) with no operator-visible change.

    Site 3 — line ~79-81 fw_get_version:
    LEAVE UNCHANGED. Per LFW-05 + P-01 conclusion in RESEARCH Open Question 3, MSG_OK_FW_VERSION stays wire_format = "text". The `send_ack_const(FW_VERSION)` (or equivalent) call-site continues to emit text. Add an inline comment above the call-site explaining: `// Phase 8 / P-01 / LFW-05: MSG_OK_FW_VERSION stays text-emitted to preserve the host's _probe_port bootstrap path.`

    Site 4 — lines ~85-91 hw_get_version (P-02):
    Locate the current `send_ack_format("Rev%d%s", rurp_get_physical_hardware_revision(), revStr)` (or similar — verify exact signature). The `revStr` is built earlier in the function — typically empty when no override, or `", Override HW: RevN"` when override is active. Convert to P-02:
    ```
    uint8_t physical = (uint8_t)rurp_get_physical_hardware_revision();
    uint8_t effective = (uint8_t)rurp_config->hardware_revision;
    if (effective >= 0xFF || effective == physical) {
        effective = 0xFF;  // P-02 sentinel: no override
    }
    LOG_OK_ID_U8_U8(MSG_OK_REV, physical, effective);
    ```
    Delete the `revStr` building block IF it's only used by this call-site (verify by grep — if revStr is also used by Site 5 below, keep building it until Site 5 is also converted, then remove). After conversion, the host's `_format_message` (Plan 03) renders the with/without-override text from the catalog.

    Site 5 — lines ~94-104 hw_get_config (P-03):
    Locate `send_ack_format("R1: %ld, R2: %ld%s", rurp_config->r1, rurp_config->r2, revStr)`. Convert to P-03 with the 9-byte composite buffer:
    ```
    uint8_t override = (rurp_config->hardware_revision < 0xFF) ? (uint8_t)rurp_config->hardware_revision : 0xFF;
    uint8_t _cfg[9];
    _cfg[0] = (uint8_t)((rurp_config->r1 >> 24) & 0xFF);
    _cfg[1] = (uint8_t)((rurp_config->r1 >> 16) & 0xFF);
    _cfg[2] = (uint8_t)((rurp_config->r1 >>  8) & 0xFF);
    _cfg[3] = (uint8_t)((rurp_config->r1      ) & 0xFF);
    _cfg[4] = (uint8_t)((rurp_config->r2 >> 24) & 0xFF);
    _cfg[5] = (uint8_t)((rurp_config->r2 >> 16) & 0xFF);
    _cfg[6] = (uint8_t)((rurp_config->r2 >>  8) & 0xFF);
    _cfg[7] = (uint8_t)((rurp_config->r2      ) & 0xFF);
    _cfg[8] = override;
    LOG_ID_BYTES(MSG_OK_CFG, _cfg, 9);
    ```
    If Task 1 added a LOG_OK_ID_BYTES_U32_U32_U8 macro, use that instead — the executor's choice. Now delete the `revStr` building block — neither Site 4 nor Site 5 needs it any more.

    Verify includes: hardware_operations.cpp must `#include "logging_id.h"` and `#include "messages.h"`. Add if missing.

    Build verification: `cd firestarter && pio run -e uno && pio run -e leonardo && pio test -e native`. All green. Host pytest: `cd firestarter_app && python -m pytest tests/test_decoder.py -v` — Plan 03's P-02/P-03 tests will now match what the firmware emits (in the simulator harness sense — actual hardware verification happens in Plan 08).

    Commit subject: `feat(hardware_operations): convert Ready + VPP/VPE + hw_get_version + hw_get_config to ID frames (P-02, P-03, W-01, W-03); fw_get_version stays text per LFW-05`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom/firestarter &amp;&amp; grep -c "send_ack_const(\"Ready\"" src/hardware_operations.cpp | awk '{ if ($1 == 0) exit 0; else exit 1 }' &amp;&amp; grep -c "log_data_format" src/hardware_operations.cpp | awk '{ if ($1 == 0) exit 0; else exit 1 }' &amp;&amp; grep -q "LOG_OK_ID(MSG_OK_READY)" src/hardware_operations.cpp &amp;&amp; grep -q "LOG_DATA_ID_U16_U16(MSG_DATA_VPP_VOLTAGE\|LOG_DATA_ID_U16_U16(MSG_DATA_VPE_VOLTAGE" src/hardware_operations.cpp &amp;&amp; grep -q "LOG_OK_ID_U8_U8(MSG_OK_REV" src/hardware_operations.cpp &amp;&amp; grep -q "MSG_OK_CFG" src/hardware_operations.cpp &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'send_ack_const("Ready"' firestarter/src/hardware_operations.cpp` returns 0.
    - `grep -c "log_data_format" firestarter/src/hardware_operations.cpp` returns 0 (VPP/VPE site converted).
    - `grep -c "send_ack_format" firestarter/src/hardware_operations.cpp` returns 0 (hw_get_version + hw_get_config converted).
    - `grep -q "LOG_OK_ID(MSG_OK_READY)" firestarter/src/hardware_operations.cpp` exits 0.
    - `grep -E "LOG_DATA_ID_U16_U16\\(MSG_DATA_(VPP|VPE)_VOLTAGE" firestarter/src/hardware_operations.cpp | wc -l` returns 2 (both branches present).
    - `grep -q "LOG_OK_ID_U8_U8(MSG_OK_REV" firestarter/src/hardware_operations.cpp` exits 0.
    - `grep -E "LOG_ID_BYTES\\(MSG_OK_CFG|LOG_OK_ID_BYTES_U32_U32_U8\\(MSG_OK_CFG" firestarter/src/hardware_operations.cpp | wc -l` returns 1.
    - fw_get_version site preserved as text: grep `grep -q "FW_VERSION" firestarter/src/hardware_operations.cpp` exits 0 AND the surrounding emit is still send_ack_const or equivalent text-format helper.
    - Both AVR builds + native suite green; host test_decoder.py still green (no host change in this task).
    - firestarter commit landed with subject starting `feat(hardware_operations): convert Ready`.
  </acceptance_criteria>
  <done>
    hardware_operations.cpp's 4 conversion-group sites (Ready, VPP/VPE, hw_get_version P-02, hw_get_config P-03) now emit ID frames; fw_get_version preserved as text per LFW-05. revStr building block deleted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Convert firestarter.cpp PARSE_RESPONSE composite to LOG_OK_ID_U8_U8_ASTR (P-04 FW_HANDSHAKE)</name>
  <files>firestarter/src/firestarter.cpp</files>
  <read_first>
    - firestarter/src/firestarter.cpp (locate lines ~148-154 — the #ifdef HARDWARE_REVISION branch with PARSE_RESPONSE macro definition and send_ack_format call; also locate the response_msg clear sites at lines 67 + 168 — these stay until Plan 06)
    - firestarter/include/logging_id.h (post-Task-1 — LOG_OK_ID_U8_U8_ASTR present)
    - firestarter/include/messages.h (confirm MSG_OK_FW_HANDSHAKE (0x06) is now wire_format = id_frame per Plan 01)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter/src/firestarter.cpp"
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Common Pitfalls" §P6 (P-04 send_ack_format references response_msg — must convert BEFORE Plan 06 deletes the field) and §"Code Examples" §"P-04 FW_HANDSHAKE conversion"
  </read_first>
  <action>
    Step 1 — replace the two-branch send_ack_format block at lines ~148-154 with a single ID-frame emit. The exact replacement (from 08-PATTERNS.md):
    ```
    {
        uint8_t hw_rev;
    #ifdef HARDWARE_REVISION
        hw_rev = (uint8_t)rurp_get_hardware_revision();
    #else
        hw_rev = 0xFF;  // P-04 sentinel: no hardware revision override
    #endif
        LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE, hw_rev, (uint8_t)handle->cmd, FW_VERSION);
    }
    ```
    Delete the `#define PARSE_RESPONSE "FW: " FW_VERSION ", HW: Rev%d, Cmd: 0x%02x"` and `#define PARSE_RESPONSE "FW: " FW_VERSION ", Cmd: 0x%02x"` lines along with the conversion (they have no other callers). The `{ ... }` braced block isolates the hw_rev local so it doesn't leak into the surrounding function scope.

    Step 2 — leave the two `handle->response_msg[0] = '\0';` clear sites at lines 67 + 168 ALONE. Plan 06 deletes them with the field. After this task, the response_msg field is still written to by some Phase 7 / Plan 04 / non-Phase-8 paths (e.g. the `_check_response` OK + DATA branches still log via `log_info(handle->response_msg)` / `log_data(handle->response_msg)` until Plan 06).

    Step 3 — verify includes (firestarter.cpp must include logging_id.h + messages.h, possibly via firestarter.h transitively). Add if missing.

    Build verification: `cd firestarter && pio run -e uno && pio run -e leonardo && pio test -e native`. All green. Host: `cd firestarter_app && python -m pytest tests/test_decoder.py -v` — Plan 03's P-04 with/without HW_REVISION tests confirm the host now correctly decodes the firmware's emitted frames.

    Commit subject: `feat(firestarter): convert PARSE_RESPONSE composite to LOG_OK_ID_U8_U8_ASTR (P-04)`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom/firestarter &amp;&amp; grep -c "PARSE_RESPONSE\|send_ack_format" src/firestarter.cpp | awk '{ if ($1 == 0) exit 0; else exit 1 }' &amp;&amp; grep -q "LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE" src/firestarter.cpp &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PARSE_RESPONSE" firestarter/src/firestarter.cpp` returns 0.
    - `grep -c "send_ack_format" firestarter/src/firestarter.cpp` returns 0.
    - `grep -q "LOG_OK_ID_U8_U8_ASTR(MSG_OK_FW_HANDSHAKE, hw_rev, (uint8_t)handle->cmd, FW_VERSION)" firestarter/src/firestarter.cpp` exits 0 (or equivalent with minor whitespace differences).
    - `grep -c "response_msg\[0\] = '\\\\0';" firestarter/src/firestarter.cpp` returns 2 (line 67 + line 168 clear sites still present — Plan 06 deletes them).
    - Both AVR builds + native suite green.
    - firestarter commit landed.
  </acceptance_criteria>
  <done>
    firestarter.cpp PARSE_RESPONSE composite converted to single ID-frame emit. response_msg clear sites preserved for Plan 06 deletion.
  </done>
</task>

<task type="auto">
  <name>Task 4: MSG_DATA_CHUNK chip-byte streaming — firmware emit + host receive loop + roundtrip test</name>
  <files>
    firestarter/src/boards/rurp_serial_utils.cpp
    firestarter/src/eprom_operations.cpp
    firestarter_app/firestarter/eprom_operations.py
    firestarter_app/firestarter/serial_comm.py
    firestarter_app/tests/test_decoder.py
  </files>
  <read_first>
    - firestarter/src/eprom_operations.cpp (locate the chip-read path that follows the LOG_DATA_ID(MSG_DATA_SENDING) emit + the rurp_communication_write(handle->data_buffer, handle->data_size) call from Plan 04 Task 3; this is the streaming site)
    - firestarter/src/boards/rurp_serial_utils.cpp (post-Plan-02 — the u16 len widening landed; need to verify whether _firestarter_emit_frame's uint8_t param_count parameter prevents emitting a 512-byte chunk. If yes, add a sibling function `_firestarter_emit_frame_wide` with `uint16_t param_count` that mirrors the existing function but supports the larger param range)
    - firestarter_app/firestarter/eprom_operations.py (the host chip-read receive loop — locate the function that drives a chip read; trace through how it consumes serial responses post-Phase-7)
    - firestarter_app/firestarter/serial_comm.py (post-Plan-03 — _decode_id_frame for type "bytes" reads all remaining body bytes as the payload; verify the Response object carries this in a usable form)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Common Pitfalls" §P5 (DATA: prefix removal breaks chip-read host loop) — this is the largest unknown in Phase 8 by RESEARCH's own admission
    - firestarter_app/CLAUDE.md
  </read_first>
  <action>
    Step 1 — firmware-side MSG_DATA_CHUNK emit.

    Decision A: if `_firestarter_emit_frame` already accepts uint8_t param_count and that's the hard cap (255 bytes max), introduce a sibling `_firestarter_emit_frame_wide(uint8_t id, const uint8_t* params, uint16_t param_count)` in `rurp_serial_utils.cpp`. Reuse all the same magic-preamble + len_u16 + crc + anchor code; only the param-count parameter type changes (uint16_t) and the loop bound. Export it via the same header (rurp_serial_utils.h) so eprom_operations.cpp can call it.

    Decision B (alternative — simpler if executor confirms safe): widen the existing `_firestarter_emit_frame` signature from `uint8_t param_count` to `uint16_t param_count`. Update every existing caller (mostly the LOG_ID_* primitives in logging_id.h — verify by grep). The LOG_ID_BYTES primitive already takes a count parameter; widen its forwarding type as well. This is the cleanest diff but touches more files.

    Pick whichever shape adds fewer files to the diff. Document the choice in the commit body.

    In `eprom_operations.cpp`, replace the `rurp_communication_write(handle->data_buffer, handle->data_size)` call (the one from Plan 04 Task 3 that streams chip bytes after MSG_DATA_SENDING) with an MSG_DATA_CHUNK emit:
    ```
    // Wrap the raw chip-byte chunk in an MSG_DATA_CHUNK frame (W-04).
    rurp_log_id_wide(MSG_DATA_CHUNK, handle->data_buffer, handle->data_size);
    // OR if widening _firestarter_emit_frame:
    rurp_log_id_bytes(MSG_DATA_CHUNK, handle->data_buffer, handle->data_size);  // adjust to whichever helper name is canonical
    ```
    The MSG_DATA_SENDING zero-param ack emitted BEFORE this in Plan 04 Task 3 may now be redundant — the host can detect chunk arrival via the MSG_DATA_CHUNK frame itself. Decide: either KEEP the MSG_DATA_SENDING ack as a per-chunk-batch starter (one emit, then N chunks) OR remove it. Recommended: keep it once per operation (not per chunk) — only emit MSG_DATA_SENDING before the FIRST chunk of a multi-chunk read. Verify how many places MSG_DATA_SENDING was being emitted before Phase 8.

    Step 2 — host-side eprom_operations.py chip-read loop rewrite.

    Open `firestarter_app/firestarter/eprom_operations.py`. Locate the chip-read function. The existing flow is roughly:
    - Send the `read` command.
    - For each chunk: wait for "DATA:" prefix or equivalent; read `data_size` raw bytes; append to output file.
    - Wait for END/MAIN done ack.

    New flow:
    - Send the `read` command.
    - For each response from `serial_comm.get_response()`:
      - If `response.type == "DATA"` and the response carries a chunk payload (decoded MSG_DATA_CHUNK frame), extract the payload bytes (from response.params or response.payload — match whatever Plan 03's parser produced) and append to the output file.
      - If `response.type == "MAIN"` or `"END"` and the response's catalog id is MSG_END_DONE, exit the loop.
      - If `response.type == "ERROR"`, surface the error.
    - Close the output file.

    Step 3 — serial_comm.py adjustments (if needed).

    The MSG_DATA_CHUNK frame's params shape is `[{type="bytes"}]`. After decode, `response.params[0]` should be a `bytes` object containing the chunk. If the existing Response dataclass doesn't expose raw params (only the rendered text message), add a `payload: bytes | None = None` field to Response and populate it in `_decode_id_frame` specifically for `msg_id == MSG_DATA_CHUNK`. This is the minimal additive change to expose binary payloads to callers without breaking the existing text-message API.

    Also, in `_format_message`: for `msg_id == MSG_DATA_CHUNK` return a short summary string like `f"<chunk: {len(payload)} bytes>"` so log lines don't try to print 512 binary bytes.

    Step 4 — host-side test (new MSG_DATA_CHUNK roundtrip).

    Add a new test to `firestarter_app/tests/test_decoder.py` (or a new test file `tests/test_chunk_stream.py` if the existing test_decoder.py is getting crowded — executor's choice):
    - `test_data_chunk_payload_exposed_via_response_payload_field` — build a frame for MSG_DATA_CHUNK with a 256-byte payload; feed via fake_serial; drive one response; assert `response.payload == <256-byte payload>` (the new payload field on Response).
    - `test_chip_read_loop_concatenates_multiple_chunks` — mock the firmware (or use the existing simulator harness) to emit 3 chunks of 256 bytes each followed by an MSG_END_DONE. Drive the eprom_operations.py read loop. Assert the output file contains the concatenated 768 bytes in order.

    Step 5 — full-suite verification.

    `cd firestarter && pio run -e uno && pio run -e leonardo && pio test -e native` — all green.
    `cd firestarter_app && python -m pytest tests/ -v` — all green (the new chunk tests + all Plan 02 + 03 tests still pass).

    Step 6 — commits. Two commits:
    - firestarter sub-repo: `feat(eprom_operations): wrap chip-byte stream in MSG_DATA_CHUNK frames (W-04)`. Include rurp_serial_utils.cpp + eprom_operations.cpp (firmware-side atomic).
    - firestarter_app sub-repo: `feat(host/eprom_operations): decode MSG_DATA_CHUNK stream + expose payload field on Response (W-04)`. Include eprom_operations.py + serial_comm.py + tests/test_decoder.py.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom &amp;&amp; cd firestarter &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native &amp;&amp; cd .. &amp;&amp; cd firestarter_app &amp;&amp; python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "MSG_DATA_CHUNK" firestarter/src/eprom_operations.cpp` exits 0 (firmware emit landed).
    - `grep -q "MSG_DATA_CHUNK" firestarter_app/firestarter/eprom_operations.py` exits 0 (host receiver landed).
    - The host Response dataclass has a `payload` field (or equivalent) exposing raw bytes for MSG_DATA_CHUNK — verify by `grep -q "payload" firestarter_app/firestarter/serial_comm.py`.
    - New tests `test_data_chunk_payload_exposed_via_response_payload_field` and `test_chip_read_loop_concatenates_multiple_chunks` exist and pass: `grep -q "test_data_chunk_payload_exposed_via_response_payload_field" firestarter_app/tests/test_decoder.py firestarter_app/tests/test_chunk_stream.py 2>/dev/null` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo && pio test -e native` all exit 0.
    - `cd firestarter_app && python -m pytest tests/ -v` exits 0 with no regressions.
    - Two paired commits land — one in each sub-repo — with subjects starting `feat(eprom_operations): wrap chip-byte stream` and `feat(host/eprom_operations): decode MSG_DATA_CHUNK stream`.
  </acceptance_criteria>
  <done>
    Chip-read streaming is end-to-end ID-frame: firmware wraps each chunk in MSG_DATA_CHUNK; host loop decodes the chunks via the Response.payload field; new tests prove the roundtrip. The Phase 8 W-04 chip-stream conversion is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firmware MSG_DATA_CHUNK emit → host eprom_operations.py decode | Each chunk is up to 512 bytes (Uno) / 1024 bytes (Leonardo). The u16 len + CRC8 catches truncation; the host's pyserial read with timeout catches missing bytes. |
| Firmware FW_HANDSHAKE composite ID frame → host probe | New shape (u8 hw + u8 cmd + ascii_str fw) means a malformed handshake (e.g. ascii_str length-prefix overflow) could panic the decoder. WR-04 guard in _decode_param already raises ValueError on overflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | MSG_DATA_CHUNK len field claims 65535 bytes but the firmware only sends 512 | mitigate | Host read timeout (pyserial default + serial_comm guard) re-syncs on next preamble; truncated body fails CRC and is discarded. |
| T-08-05-02 | Tampering | ascii_str length-prefix in FW_HANDSHAKE claims 255 bytes but frame only carries 10 | mitigate | WR-04 guard in _decode_param raises ValueError if end > len(buf); already in place from Phase 6. |
| T-08-05-03 | DoS | A 1024-byte chunk doubles the SRAM working-set during transmit on Leonardo | accept | DATA_BUFFER_SIZE was already at 1024 in Phase 1 baseline; Phase 6 lowered it to 512 (TEMP). RESEARCH Open Question 1 notes this; restoration is out of Phase 8 scope. |
| T-08-05-04 | Information disclosure | MSG_OK_FW_HANDSHAKE emits FW_VERSION over the wire | accept | FW_VERSION was already on the wire in the legacy text format; no new exposure. |
| T-08-05-05 | Tampering | Sentinel byte 0xFF mistaken for a legitimate hardware revision | accept | rurp_config->hardware_revision is operator-installed via EEPROM; 0xFF would mean "revision 255" which is not produced by any real hardware. The collision is non-issue. |
</threat_model>

<verification>
- hardware_operations.cpp: zero hits for send_ack_const("Ready"), log_data_format, send_ack_format. FW_VERSION text emit preserved at line ~80.
- firestarter.cpp: zero hits for PARSE_RESPONSE or send_ack_format.
- eprom_operations.cpp (firmware): MSG_DATA_CHUNK emit landed; rurp_communication_write for raw bytes wrapped.
- eprom_operations.py (host): chip-read loop decodes MSG_DATA_CHUNK Response.payload; output file matches per-chunk concatenation.
- serial_comm.py: Response.payload field exists; _format_message handles MSG_DATA_CHUNK with a summary string.
- Both AVR builds + native suites + host pytest all green.
- Plan 03's Wave-0 gap tests for P-02/P-03/P-04 still pass (now against the actual firmware emits, not just hand-rolled test frames).
</verification>

<success_criteria>
- After this plan, every firmware emit except MSG_OK_FW_VERSION (LFW-05 bootstrap) is an ID frame.
- Chip-read streaming is fully wrapped in MSG_DATA_CHUNK; no more raw-byte gap between log_data and rurp_communication_write.
- Host chip-read loop in eprom_operations.py is rewritten to consume MSG_DATA_CHUNK frames.
- Success Criterion 3 (`firestarter read -e W27C512 -o out.bin produces byte-identical binary vs pre-Phase-8 baseline`) is automatable via a host pytest (manual hardware verification is Plan 08's job).
- Both AVR builds green; both sub-repo test suites green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-05-SUMMARY.md` with: per-task commit SHAs in both sub-repos, Decision A vs B choice for _firestarter_emit_frame widening (sibling function vs uint16_t signature widen), the captured Uno + Leonardo Flash + SRAM usage after this plan, count of state-machine call-sites converted vs preserved (FW_VERSION preserved), and a one-line "every state-machine ack is now an ID frame except the LFW-05 bootstrap" assertion.
</output>
</content>
