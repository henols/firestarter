---
phase: 08-convert-state-machine-prefix-call-sites-ok-init-main-end
plan: 08
type: execute
wave: 8
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md
autonomous: false
requirements:
  - LMIG-03
must_haves:
  truths:
    - "SC#1: only the LFW-05 bootstrap MSG_OK_FW_VERSION line remains as text in the host parser surface; INIT/MAIN/END are decoded by _decode_id_frame only — verified by re-running the firmware-simulator harness OR by the Plan 03 host parser tests"
    - "SC#2: `firestarter write -e W27C512` runs end-to-end on Uno + Leonardo with INIT/MAIN/END acks rendered from ID-frame decoding alone; operator-visible CLI output is indistinguishable from the pre-Phase-8 baseline (functional parity)"
    - "SC#3: `firestarter read -e W27C512 -o out.bin` against the simulator (and on real hardware if available) produces a byte-identical binary file vs the pre-Phase-8 baseline; the DATA: text prefix remains in code but is unused in production paths now (every chip-read flow uses MSG_DATA_CHUNK from Plan 05)"
    - "SC#4: Both AVR builds compile; Uno + Leonardo Flash usage measurably smaller than the Phase 7 close baseline (Leonardo 27,026 / Uno 24,838 per 07-FLASH-MEASUREMENT.md). The exact percentages are recorded in 08-MEASUREMENT.md"
    - "SRAM usage on both boards is ~96 bytes lower than the Plan 05 baseline (R-01 response_msg deletion win)"
    - "Both boards' write and read operations have been verified on real hardware (Uno + Leonardo paired control per project memory)"
    - "Phase 8's 08-MEASUREMENT.md artifact is committed with: Phase 7 close baseline reference, post-Phase-8 measurements, per-board Flash + SRAM deltas, end-to-end functional verification transcripts, and the SRAM win attributed to R-01"
  artifacts:
    - path: ".planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md"
      provides: "phase-close measurement artifact mirroring 07-FLASH-MEASUREMENT.md shape + SRAM delta from R-01"
      min_lines: 50
  key_links:
    - from: ".planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md"
      to: ".planning/phases/07-convert-error-warn-info-call-sites/07-FLASH-MEASUREMENT.md"
      via: "byte-delta computation (Leonardo + Uno Flash size deltas vs Phase 7 close)"
      pattern: "27,026\\|27026\\|24,838\\|24838"
---

<objective>
Phase 8 close — prove all four ROADMAP success criteria hold and record the Flash + SRAM deltas vs the Phase 7 close baseline (Leonardo 27,026 / 28,672 = 94.3%; Uno 24,838 / 32,256 = 77.0%) into a phase-close measurement artifact mirroring Phase 7's `07-FLASH-MEASUREMENT.md`.

This plan is NOT autonomous because SC#2 + SC#3 require hardware-in-the-loop verification on both boards (Uno + Leonardo paired control per project memory: "Always mirror Uno tests on Leonardo"). The automated portion (SC#1 grep gate, SC#4 builds, host pytest, native test suite) runs first in Task 1; the manual hardware verification runs in Task 2 as a checkpoint:human-verify.

Purpose: gates entry to `/gsd-verify-work` for Phase 8. After this plan, the phase ships and Phase 9 (delete legacy log macros + final Leonardo flash measurement) can begin.
Output: 08-MEASUREMENT.md artifact + console proof of all four success criteria, with hardware-confirmation transcripts.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-CONTEXT.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-FLASH-MEASUREMENT.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-02-SUMMARY.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-05-SUMMARY.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-06-SUMMARY.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-07-SUMMARY.md

<interfaces>
Phase 7 close baseline (from 07-FLASH-MEASUREMENT.md):
- Leonardo: 27,026 / 28,672 bytes Flash used (94.3%), 1,646 bytes free.
- Uno: 24,838 / 32,256 bytes Flash used (77.0%), 7,418 bytes free.

Expected direction for Phase 8 close:
- Flash: DOWN (debug strings stripped in Plan 07; response_msg buffer code paths simplified; legacy log_*_const/log_*_format macros still in place from Phase 9's scope but their callers are mostly gone, so per-call PROGMEM strings vanish).
- SRAM: DOWN by approximately 96 bytes per the R-01 deletion (Plan 06).
- If either metric grows vs Phase 7 close, surface to the operator for diagnosis — this is a phase-close gate.

Phase 8 ROADMAP success criteria (verbatim from ROADMAP.md):
1. SC#1: "Only line-prefix-matched messages remaining in the host parser are the `DATA:` binary read-payload stream marker and the bootstrap `OK: FW: ...` version handshake — every other `OK:` / `INIT:` / `MAIN:` / `END:` ack arrives as an ID-encoded frame."
2. SC#2: "firestarter write -e W27C512 runs end-to-end and reaches normal completion with the host correctly rendering state-machine progress (INIT phase, MAIN data-transfer phase, END acknowledgement) from ID-frame decoding alone."
3. SC#3: "DATA: binary read-payload stream still works unchanged — firestarter read -e W27C512 -o out.bin against the simulator produces a byte-identical binary file vs the pre-Phase-8 baseline." (Note: Phase 8 actually CHANGES this to MSG_DATA_CHUNK frames; the byte-identity check is on the output FILE contents, not the wire format. Plan 05 + the W-03/W-04 decisions mean the wire is now id_frame-only, but the file contents are still the chip bytes.)
4. SC#4: "Both boards compile, with firmware binary size again measurably smaller than the Phase 7 baseline."

Pre-Phase-8 chip-read baseline file:
- A known-good `out.bin` from Phase 7 close (created when SC#2 was last verified on Phase 7). If the operator does not have one, capture a baseline from a pre-Phase-8 git checkout BEFORE running the Phase 8 read test — explained in Task 2 instructions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC#1 grep gate (host) + SC#4 dual-board build + host + native test suite + write 08-MEASUREMENT.md</name>
  <files>.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/07-convert-error-warn-info-call-sites/07-FLASH-MEASUREMENT.md (template — Phase 8 mirrors the shape: header, per-board Flash sections, computed deltas, SC verification sections, anchor-for-Plan-9 table)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-CONTEXT.md (phase summary for the artifact header)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md (Per-Task Verification Map row `8-99-*` phase-close)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-06-SUMMARY.md (R-01 SRAM delta numbers)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-07-SUMMARY.md (Plan 07 Flash delta numbers)
    - firestarter/CLAUDE.md (build invocations)
    - firestarter_app/CLAUDE.md
  </read_first>
  <action>
    PART A — SC#1 grep gate (host parser surface check).

    The Phase 8 SC#1 says only `DATA:` (binary read-payload stream marker, NOT to be confused with the MSG_DATA_CHUNK wrapping done in Plan 05 — DATA: as a TEXT line prefix is removed by Plan 05 too) and the bootstrap `OK: FW: ...` version handshake remain as text-prefix-matched lines. Verify by running:

    ```
    cd firestarter_app
    grep -nE '^[[:space:]]*[\"\']?(MAIN|INIT|END)[\"\']?' firestarter/serial_comm.py | grep -v "#" | grep -v "INIT_DONE\|MAIN_DONE\|END_DONE\|INIT[A-Z]\|MAIN[A-Z]\|END[A-Z]"
    ```
    Expected: zero hits (Plan 03 removed MAIN/INIT/END from EXPECTED_PREFIXES and STATE_MACHINE_PREFIXES). Any hits OTHER than catalog-id-name imports (MSG_INIT_DONE, MSG_MAIN_DONE, MSG_END_DONE — those are fine) signal a missed parser site.

    Save the grep output (empty or not) to `/tmp/ph8-08-sc1-grep.txt`.

    PART B — host pytest regression.

    `cd firestarter_app && python -m pytest tests/ -v 2>&1 | tee /tmp/ph8-08-pytest.txt`. All tests must pass (Plan 02's 14+, Plan 03's 9 new + 1 updated, Plan 05's MSG_DATA_CHUNK roundtrip tests, every baseline). Save the pytest summary line.

    PART C — SC#4 dual-board build with Flash + SRAM capture.

    Clean rebuild for accurate measurements:
    ```
    cd firestarter
    pio run -e uno --target clean
    pio run -e leonardo --target clean
    pio run -e uno 2>&1 | tee /tmp/ph8-08-uno-build.txt
    pio run -e leonardo 2>&1 | tee /tmp/ph8-08-leo-build.txt
    pio test -e native 2>&1 | tee /tmp/ph8-08-native-tests.txt
    ```

    Extract Flash + RAM usage from each build output. The `pio run` final report includes lines like:
    - `RAM:   [=====     ]  XX.X% (used XXXX bytes from XXXX bytes)`
    - `Flash: [========  ]  YY.Y% (used YYYY bytes from YYYY bytes)`

    `grep -E "RAM:|Flash:" /tmp/ph8-08-uno-build.txt` and the same for Leonardo.

    All three commands (uno build, leo build, native tests) MUST exit 0.

    PART D — write 08-MEASUREMENT.md.

    Create `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md` mirroring 07-FLASH-MEASUREMENT.md's structure. Required sections:

    1. **Header** — `# Phase 8 — Flash + SRAM Budget Measurement` + measurement date + boards (Uno + Leonardo) + Phase 7 close baseline reference + repo SHAs (firestarter + firestarter_app).

    2. **Leonardo** section:
       - Full `pio run -e leonardo` Flash + RAM lines.
       - Computed Flash delta vs Phase 7 close: `27,026 - <new_used> = <delta> bytes` and `<delta>/28,672 = <pct>` percentage points.
       - Computed RAM delta vs Plan 05 (or pre-R-01 baseline if Plan 05 SUMMARY captured it): expected ~96 bytes lower.
       - Interpretation: was Flash direction downward (SC#4 PASS) or did it grow (surface)?

    3. **Uno** section:
       - Full `pio run -e uno` Flash + RAM lines.
       - Computed deltas as above (Uno baseline: 24,838 Flash / pre-R-01 SRAM).

    4. **SC#1 Host Parser Surface** section:
       - The /tmp/ph8-08-sc1-grep.txt output (zero hits expected).
       - Confirmation that EXPECTED_PREFIXES no longer contains MAIN/INIT/END (grep firestarter_app/firestarter/serial_comm.py).
       - Confirmation that the bootstrap `OK: FW: ...` text path is preserved (LFW-05) — grep for the MSG_OK_FW_VERSION text-decode site.

    5. **SC#2 Manual Verification Plan** section — REFERENCE Task 2 below; do NOT mark SC#2 as PASS in this artifact until Task 2 completes (operator pairs Uno + Leonardo).

    6. **SC#3 Manual Verification Plan** section — REFERENCE Task 2 below; the byte-identity diff of out.bin against a pre-Phase-8 baseline file is the load-bearing check.

    7. **SC#4 Build Status** section: confirm both boards' builds succeeded AND Flash usage is < Phase 7 close baseline for both boards. Native suite green. Host pytest summary line included.

    8. **SRAM win (R-01) attribution** section: tie the SRAM delta to the response_msg deletion in Plan 06. Reference 08-06-SUMMARY.md.

    9. **Anchor for Plan 9** section: append a new Phase 8 close row to the table from 07-FLASH-MEASUREMENT.md so Phase 9's milestone-close measurement can cite v1.1 close → Phase 6 close → Phase 7 close → Phase 8 close → Phase 9 close in a continuous ladder.

    Commit subject inside meta-repo: `docs(phase-08): write 08-MEASUREMENT.md — SC#1/SC#4 verified, SC#2/SC#3 hardware pending`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom &amp;&amp; cd firestarter &amp;&amp; pio run -e uno --target clean &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo --target clean &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native &amp;&amp; cd .. &amp;&amp; cd firestarter_app &amp;&amp; python -m pytest tests/ -v &amp;&amp; test -f /workspaces/firestarter_prom/.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - SC#1 grep `/tmp/ph8-08-sc1-grep.txt` is empty (zero non-import, non-comment hits for MAIN/INIT/END as line prefixes in serial_comm.py).
    - `cd firestarter_app && python -m pytest tests/ -v` exits 0 with all tests passing (Plans 02 + 03 + 05 new tests + baseline).
    - `cd firestarter && pio run -e uno` exits 0; Flash usage strictly LESS than 24,838 bytes (Phase 7 Uno close baseline). If equal or larger, surface to operator before proceeding to Task 2.
    - `cd firestarter && pio run -e leonardo` exits 0; Flash usage strictly LESS than 27,026 bytes (Phase 7 Leonardo close baseline).
    - `cd firestarter && pio test -e native` exits 0; test_messages + test_dispatch suites all green.
    - `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-MEASUREMENT.md` exists with all nine sections from PART D, at least 50 lines.
    - The "Anchor for Plan 9" table includes the new Phase 8 close row alongside v1.1 close, Phase 6 close, Phase 7 close.
    - SC#2 + SC#3 sections explicitly reference Task 2 as the pending verification (not marked PASS yet).
    - Meta-repo commit landed with subject starting `docs(phase-08): write 08-MEASUREMENT.md`.
  </acceptance_criteria>
  <done>
    SC#1 + SC#4 verified PASS via automation. 08-MEASUREMENT.md artifact written with Flash + SRAM deltas. SC#2 + SC#3 pending Task 2 hardware verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC#2 + SC#3 hardware verification on Uno + Leonardo (write + read end-to-end with byte-identity diff)</name>
  <what-built>
    All Phase 8 conversions landed:
    - Plan 01: catalog + codegen + sync (Wave 1).
    - Plan 02: wire-format len u8→u16 (Wave 2).
    - Plan 03: host parser surgical strip + sentinel rendering (Wave 3).
    - Plan 04: simple populate-sites converted (Wave 4).
    - Plan 05: composite P-02/P-03/P-04 + VPP/VPE + MSG_DATA_CHUNK (Wave 5).
    - Plan 06: _check_response strip + response_msg deletion + copy_to_buffer macro deletion (Wave 6).
    - Plan 07: debug() sweep to MSG_DEBUG + sub_id channel (Wave 7).
    - Plan 08 Task 1: SC#1 + SC#4 automated gates passed; 08-MEASUREMENT.md drafted.

    What remains: hardware-in-the-loop verification that:
    - SC#2: `firestarter write -e W27C512` runs end-to-end on Uno + Leonardo with the host correctly rendering INIT/MAIN/END from ID-frame decoding (no text-prefix matching remaining for those acks). Operator confirms the CLI output looks functionally identical to the pre-Phase-8 baseline.
    - SC#3: `firestarter read -e W27C512 -o /tmp/ph8-{uno,leo}-out.bin` against a known chip produces a byte-identical binary file vs the pre-Phase-8 baseline file (operator captures the baseline from a pre-Phase-8 git checkout before running Phase 8's read test — see Step 0 below).

    Per project memory ("Always mirror Uno tests on Leonardo"), this verification is performed on BOTH boards. The operator does NOT need to confirm per-cycle chip-removal ("IC-removal autonomy granted" from project memory) — flash + test on both boards without asking.
  </what-built>
  <how-to-verify>
    Step 0 — Baseline capture (do this ONCE before flashing Phase 8 firmware, only if a pre-Phase-8 baseline /tmp file doesn't already exist from Phase 7).

    Required baseline files (SC#3 byte-identity check):
    - `/tmp/ph8-pre-phase8-uno-baseline.bin` — a chip-read from a known W27C512 chip captured against the pre-Phase-8 firmware.
    - `/tmp/ph8-pre-phase8-leo-baseline.bin` — same chip read on Leonardo.

    If those files don't exist, capture them now:
    ```
    cd firestarter
    git stash             # or git checkout <pre-Phase-8 SHA> -- src/ include/
    pio run -t upload -e uno
    cd ../firestarter_app
    firestarter read -e W27C512 -o /tmp/ph8-pre-phase8-uno-baseline.bin
    # Repeat for Leonardo:
    cd ../firestarter
    pio run -t upload -e leonardo
    cd ../firestarter_app
    firestarter read -e W27C512 -o /tmp/ph8-pre-phase8-leo-baseline.bin
    # Restore Phase 8 firmware:
    cd ../firestarter
    git stash pop         # or git checkout HEAD -- src/ include/
    ```

    Step 1 — Flash Phase 8 firmware to Uno:
    ```
    cd firestarter && pio run -t upload -e uno
    ```

    Step 2 — SC#2 write end-to-end (Uno):
    Choose a known-good binary file for W27C512 (a chip from the v1.0 / v1.1 hardware test corpus). From firestarter_app/:
    ```
    FIRESTARTER_DEV_ALLOW_PRE_V12=1 firestarter write -e W27C512 -i <known.bin> 2>&1 | tee /tmp/ph8-08-uno-write.txt
    ```
    Note: FIRESTARTER_DEV_ALLOW_PRE_V12=1 is required because Phase 8's wire-format major bump (W-04 u16 len) means the firmware reports a new wire-format major; the host's pre-v1.2 guard would otherwise reject the firmware. Phase 9 bumps the firmware version proper; for Phase 8 the env var is the workaround.

    Confirm:
    - The CLI output shows INIT / MAIN / END acks WITHOUT a literal `INIT:`, `MAIN:`, or `END:` text prefix (those are rendered by the catalog format string now).
    - No raw `0x...` binary appears in the CLI output (would indicate decoder failure).
    - The write completes with a success message.
    - The bootstrap `OK: FW: ...` text line at command start is still present (LFW-05 exemption preserved).

    Step 3 — SC#3 read + byte-identity (Uno):
    ```
    FIRESTARTER_DEV_ALLOW_PRE_V12=1 firestarter read -e W27C512 -o /tmp/ph8-08-uno-out.bin 2>&1 | tee /tmp/ph8-08-uno-read.txt
    diff /tmp/ph8-08-uno-out.bin /tmp/ph8-pre-phase8-uno-baseline.bin
    echo "Uno SC#3 diff exit: $?"
    ```

    The `diff` MUST exit 0 (byte-identical). Any non-zero exit indicates SC#3 failure — surface to operator with the byte-difference range.

    Step 4 — Repeat Steps 1-3 on Leonardo:
    ```
    cd firestarter && pio run -t upload -e leonardo
    cd ../firestarter_app
    FIRESTARTER_DEV_ALLOW_PRE_V12=1 firestarter write -e W27C512 -i <known.bin> 2>&1 | tee /tmp/ph8-08-leo-write.txt
    FIRESTARTER_DEV_ALLOW_PRE_V12=1 firestarter read -e W27C512 -o /tmp/ph8-08-leo-out.bin 2>&1 | tee /tmp/ph8-08-leo-read.txt
    diff /tmp/ph8-08-leo-out.bin /tmp/ph8-pre-phase8-leo-baseline.bin
    echo "Leonardo SC#3 diff exit: $?"
    ```

    Both Uno and Leonardo SC#3 diffs MUST exit 0.

    HARDWARE-SOCKET CAVEAT (project memory): if the Leonardo readback looks corrupted, suspect the Leonardo shield socket FIRST (not the firmware) — wonky socket is a known issue. Re-seat the chip or swap to a different known-good chip before declaring a regression. If the Uno reads cleanly with the same chip + the Leonardo doesn't, the Leonardo socket is the suspect.

    Step 5 — Append SC#2 + SC#3 outcome to `08-MEASUREMENT.md` under their pending sections. Include:
    - Date + board + chip used.
    - Sample of the CLI output showing INIT/MAIN/END acks without text prefixes (proves SC#2).
    - The `diff` exit status (0) and the file sizes (proves SC#3).
    - Note any operator observations (e.g. "wonky Leonardo socket — re-seated chip once, then clean").
    - Re-commit 08-MEASUREMENT.md with subject `docs(phase-08): mark SC#2 + SC#3 PASS on Uno + Leonardo`.
  </how-to-verify>
  <resume-signal>
    Type "approved" with a one-line summary describing the hardware outcome (e.g. "SC#2 + SC#3 verified PASS on Uno + Leonardo with W27C512; both reads byte-identical to pre-Phase-8 baseline; 08-MEASUREMENT.md updated") OR describe the failure mode if either board's verification fails (e.g. "Leonardo wonky socket persisted across two re-seats; need a different shield" — operator decides whether to defer or block phase close).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-Phase-8 baseline vs Phase 8 firmware behavior | The byte-identity check on the chip read (SC#3) is the load-bearing integrity gate for the W-04 wire-format bump and the MSG_DATA_CHUNK streaming wrapper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | MSG_DATA_CHUNK frame corruption silently corrupts the output file | mitigate | SC#3 byte-identity diff against the pre-Phase-8 baseline catches any divergence; CRC8 over [id, params] catches in-frame corruption; pyserial timeout catches truncation. |
| T-08-08-02 | DoS | Phase 8 firmware fails to bootstrap because of the LFW-05 text path | mitigate | Plan 05 explicitly preserved fw_get_version as text-emit; SC#2 confirms the bootstrap text path still works. |
| T-08-08-03 | Information disclosure | The Leonardo shield socket issue produces an apparent regression | accept | Project memory documents this; Task 2 instructions tell the operator to suspect the socket first. |
</threat_model>

<verification>
- SC#1: zero MAIN/INIT/END line-prefix-matched entries in serial_comm.py (only the bootstrap MSG_OK_FW_VERSION text path remains).
- SC#2: hardware-verified on Uno + Leonardo — write end-to-end completes, INIT/MAIN/END render via catalog format strings.
- SC#3: hardware-verified on Uno + Leonardo — read produces byte-identical out.bin vs pre-Phase-8 baseline.
- SC#4: Uno + Leonardo Flash usage strictly less than Phase 7 close baseline (24,838 / 27,026 bytes); deltas documented in 08-MEASUREMENT.md.
- SRAM: ~96 bytes lower on both boards (R-01 win); delta documented.
- 08-MEASUREMENT.md committed with all four SC sections marked PASS.
</verification>

<success_criteria>
- All four Phase 8 ROADMAP success criteria verified PASS.
- 08-MEASUREMENT.md artifact present + comprehensive + ready to anchor Phase 9's milestone-close measurement.
- Phase 8 closes cleanly; STATE.md can roll forward to Phase 9 with a clean handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-08-SUMMARY.md` summarizing the four SC outcomes + a one-line "ready for /gsd-verify-work" assertion.
</output>
</content>
