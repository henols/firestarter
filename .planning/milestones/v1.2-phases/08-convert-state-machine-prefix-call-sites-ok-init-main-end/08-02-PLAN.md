---
phase: 08-convert-state-machine-prefix-call-sites-ok-init-main-end
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - firestarter/src/boards/rurp_serial_utils.cpp
  - firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/tests/conftest.py
  - firestarter_app/tests/test_decoder.py
autonomous: true
requirements:
  - LMIG-03
must_haves:
  truths:
    - "Firmware _firestarter_emit_frame writes the len field as 2 bytes big-endian (MSB then LSB) for every emitted frame"
    - "Firmware param_count guard raised from 253 to 65533 (u16 max minus 2 bytes for id+crc)"
    - "Host _read_and_parse_lines reads exactly 2 bytes for frame_len after a magic-preamble match and decodes as u16 big-endian"
    - "Host conftest.build_frame() emits the len field as 2 bytes big-endian; every existing test that calls build_frame() continues to pass against the new wire format"
    - "Native test_rurp_log_id.cpp byte-offset assertions are shifted +1 for every captured[] index >= 5 (id moves from captured[5] to captured[6], etc.); len now spans captured[4..5]"
    - "Native test_oversize_param_count_rejected updated for the new 65533 guard threshold (not 253)"
    - "Native test for an MSG_DATA_CHUNK body > 253 bytes (the byte count u8-len could not have carried) confirms u16-only path is exercised end-to-end (W-04 Wave 0 gap)"
    - "test_decoder.py existing 12 tests still pass after build_frame switches to u16 len"
    - "Firmware and host wire-format change land in a SINGLE atomic commit per side (firmware commit + host commit, paired in time; never separate firmware-only or host-only commits — see W-04 anti-pattern)"
    - "Both AVR builds (Uno + Leonardo) compile cleanly after the firmware edit"
  artifacts:
    - path: "firestarter/src/boards/rurp_serial_utils.cpp"
      provides: "u16 big-endian len write in _firestarter_emit_frame"
      contains: "len_u16"
    - path: "firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp"
      provides: "byte-offset assertions for u16 len + 65533-cap guard"
      contains: "captured[6]"
    - path: "firestarter_app/firestarter/serial_comm.py"
      provides: "u16 big-endian frame_len read in _read_and_parse_lines"
      contains: "struct.unpack"
    - path: "firestarter_app/tests/conftest.py"
      provides: "build_frame helper emitting u16 len"
      contains: "struct.pack"
    - path: "firestarter_app/tests/test_decoder.py"
      provides: "new MSG_DATA_CHUNK >253B body test exercising u16-only path"
      contains: "MSG_DATA_CHUNK"
  key_links:
    - from: "firestarter/src/boards/rurp_serial_utils.cpp"
      to: "firestarter_app/firestarter/serial_comm.py"
      via: "wire format — u16 big-endian len field"
      pattern: "struct.unpack_from\\(\">H\""
    - from: "firestarter_app/tests/conftest.py"
      to: "firestarter_app/tests/test_decoder.py"
      via: "build_frame helper (single point of truth for frame assembly in host tests)"
      pattern: "build_frame\\("
---

<objective>
Wave 2 — widen the wire-format `len` field from u8 to u16 (big-endian, MSB-first to match all existing big-endian params) so chip-read chunks up to 1020 bytes can fit in a single `MSG_DATA_CHUNK` frame. This is the W-04 atomic firmware+host+test commit group per CONTEXT and RESEARCH. Four files change together: `_firestarter_emit_frame` (firmware emit), `_read_and_parse_lines` (host decode), `test_rurp_log_id.cpp` (native byte-offset assertions), `conftest.build_frame` (host test fixture). Add the W-04 Wave-0 gap test: a `MSG_DATA_CHUNK` body > 253 bytes that proves the u16-only path is exercised (a u8 len could not have carried such a frame at all).

Purpose: every Phase 8 wire byte after this plan flows through a u16-len frame. This is the wire-format major bump that gates all downstream populate-site conversions (Plans 04+05) and the host parser surgical removal (Plan 03). Splitting the firmware and host changes across commits creates an immediate silent desync — keep them tightly paired.
Output: Firmware emits 2-byte len; host reads 2-byte len; all 12+ existing decoder tests still pass; a new MSG_DATA_CHUNK >253B test proves the u16-only path; native test_messages suite shifted to the new byte offsets and still green.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-CONTEXT.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-01-SUMMARY.md

<interfaces>
Current firmware emit shape (rurp_serial_utils.cpp ~line 156-200):
- void _firestarter_emit_frame(uint8_t id, const uint8_t* params, uint8_t param_count)
- Guards: `if (param_count > 253) return;`
- Writes: MAGIC_PREAMBLE (4 bytes) → len (1 byte = 1 + param_count + 1) → id (1 byte) → params (N bytes) → crc (1 byte) → 0x0A anchor
- CRC8 covers [id, params]; len NOT in CRC.

Phase 8 firmware emit shape (W-04):
- Same signature for param_count (uint8_t) — callers never pass more than 253 today; the guard widens for FUTURE MSG_DATA_CHUNK callers that pass via LOG_ID_BYTES with a uint16_t or uint32_t count, but the public API width to the typical caller is unchanged.
- IF the MSG_DATA_CHUNK call-site needs to emit >255 bytes, introduce an internal sibling function (or widen the existing function's param_count parameter to uint16_t — pick the minimum-diff option after reading the actual call-sites; deferred to executor judgment in this task).
- New guard: `if (param_count > 65533) return;` (65535 minus 1 for id and 1 for crc).
- New len write: u16 big-endian, MSB then LSB.

Current host decode shape (serial_comm.py _read_and_parse_lines ~lines 451-464):
- After MAGIC_PREAMBLE matches, reads `self.connection.read(1)` → frame_len = single byte.
- Then reads `self.connection.read(frame_len)` → body (id + params + crc).
- Final 0x0A anchor byte read separately.

Phase 8 host decode shape (W-04):
- After MAGIC_PREAMBLE matches, reads `self.connection.read(2)` → unpack as u16 big-endian via `struct.unpack_from(">H", ...)`.
- Then reads `self.connection.read(frame_len)` — unchanged.
- 0x0A anchor — unchanged.

test_rurp_log_id.cpp current byte-offset map (u8 len at captured[4]):
- captured[0..3] = magic preamble (0xDE 0xAD 0xBE 0xEF — or whatever the project's preamble is; do not hard-code; read from the test fixture).
- captured[4] = len (u8)
- captured[5] = id
- captured[6..N-1] = params (N = param_count)
- captured[N] = crc
- captured[N+1] = 0x0A

Phase 8 test_rurp_log_id.cpp byte-offset map (u16 len at captured[4..5]):
- captured[0..3] = magic preamble
- captured[4] = len MSB (= 0x00 for any param_count <= 0xFF, which all unit tests use)
- captured[5] = len LSB
- captured[6] = id
- captured[7..N] = params
- captured[N+1] = crc
- captured[N+2] = 0x0A

conftest.build_frame() current shape (firestarter_app/tests/conftest.py ~line 52-61):
- `length = len(body) + 1` (body = id + params; +1 for crc).
- `return MAGIC_PREAMBLE_REF + bytes([length]) + body + bytes([crc, 0x0A])`

Phase 8 build_frame() shape:
- `length = len(body) + 1`
- `return MAGIC_PREAMBLE_REF + struct.pack(">H", length) + body + bytes([crc, 0x0A])`
- Module-level `import struct` if not already present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update test_rurp_log_id.cpp byte-offset assertions + conftest.build_frame for u16 len (RED step — tests reflect new wire format)</name>
  <files>
    firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp
    firestarter_app/tests/conftest.py
    firestarter_app/tests/test_decoder.py
  </files>
  <read_first>
    - firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp (full file — read every TEST_ASSERT line; build the complete map of `captured[N]` references to update)
    - firestarter_app/tests/conftest.py (full file — focus on the build_frame helper around line 52-61 + any other place that hand-builds a frame)
    - firestarter_app/tests/test_decoder.py (full file — confirm every test uses build_frame; if any test hand-rolls bytes, list them so they can be updated too)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Common Pitfalls" §P2 (test_messages byte-offset shift) + §P3 (conftest.py build_frame encodes old u8 len)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp" + §"firestarter_app/tests/conftest.py"
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md (Wave 0 gap rows for `test_rurp_log_id.cpp` + `conftest.py` build_frame + MSG_DATA_CHUNK >253B test)
  </read_first>
  <behavior>
    - Native test `test_zero_param_frame` asserts captured.size() == 9 (was 8); len MSB=0x00 at captured[4], len LSB=0x02 at captured[5], id at captured[6], crc at captured[7], 0x0A anchor at captured[8].
    - Native test `test_u32_param_frame` asserts captured.size() == 13 (was 12); len MSB=0x00, len LSB=0x06, id at [6], params at [7..10], crc at [11], anchor at [12].
    - Native test `test_multi_param_frame` and any other captured[N] reference shifts by +1 for every index >= 5; param_count and offsets stay otherwise identical.
    - Native test `test_oversize_param_count_rejected` updated: the rejected boundary changes from 253 to 65533. The pass-vs-reject boundary case at param_count = 253 should now SUCCEED (was the largest accepted; still accepted under u16). A new test case at param_count = 65534 must REJECT.
    - Host `build_frame()` emits u16 big-endian len (2 bytes) immediately after the magic preamble; all 12+ existing test_decoder.py tests pass without per-test changes (the helper does the work).
    - New host test `test_data_chunk_body_over_253_bytes_decodes` builds a MSG_DATA_CHUNK frame with a 512-byte payload (firmware DATA_BUFFER_SIZE), passes it through the decoder, asserts the decoded response carries the full 512 bytes (W-04 Wave 0 gap from VALIDATION).
    - New host test `test_data_chunk_body_511_bytes_at_old_u8_limit` builds a frame whose body length (id + params + crc) = 254 bytes — one byte over the old u8 len cap — and asserts successful decode (proves the path could NOT have worked under u8 len).
  </behavior>
  <action>
    Part A — `test_rurp_log_id.cpp`: open the file. Locate every TEST_ASSERT line that references `captured[N]` for N >= 4. Update according to the byte-offset map in the `<interfaces>` block:
    - `captured.size()` assertions: increase each expected size by 1.
    - `captured[4]` becomes the len MSB; assert it equals `0x00` for any frame whose body fits in a single u8 (which is every existing test).
    - `captured[5]` becomes the len LSB; assert it equals the previously-asserted u8 len value.
    - `captured[N]` for every N >= 5 becomes `captured[N+1]` with the same expected byte value.

    Update `test_oversize_param_count_rejected`: change the rejection boundary from param_count = 254 to param_count = 65534 (assert that the emit is rejected — captured.size() stays at 0). Keep the param_count = 253 success case but ALSO add a param_count = 65533 success-boundary case (the new largest accepted value). For the 65533 case, only assert captured.size() is non-zero and the first 4 bytes match the magic preamble (don't try to assert every byte of a 65k buffer — the goal is proving the guard threshold moved, not byte-level correctness on a giant frame).

    Part B — `conftest.py` build_frame: add `import struct` to the module imports if absent. Change the body assembly so the length field is two bytes big-endian: assemble `MAGIC_PREAMBLE_REF + struct.pack(">H", length) + body + bytes([crc, 0x0A])`. Length still = `len(body) + 1` (id + params + crc).

    Part C — `test_decoder.py` MSG_DATA_CHUNK over-253B test (Wave 0 gap from VALIDATION):
    - Add `from firestarter.messages import MSG_DATA_CHUNK` to imports (the messages.py codegen output from Plan 01 must include this — verify before relying on it).
    - Add `def test_data_chunk_body_over_253_bytes_decodes(self, fake_serial, make_comm):` to the existing TestIdFrameDecoder class. Body: build a 512-byte deterministic payload (e.g. `bytes(range(256)) + bytes(range(256))`); call `build_frame(MSG_DATA_CHUNK, payload)`; feed to fake_serial; drive one response; assert the response is non-None, its type is "DATA", and its message (or a `params` field — match the existing decoder API surface) carries all 512 payload bytes. The test should FAIL before the firmware widening + host widening lands in Task 2, but should PASS after Task 2.
    - Add `def test_data_chunk_body_254_bytes_at_old_u8_limit(self, fake_serial, make_comm):` — same pattern with a 252-byte payload (id + params + crc = 254 = one byte past old u8 max of 253 inclusive). Confirms the u16 path handles the edge case.

    Part D — pre-Task-2 verification (RED):
    - Run `cd firestarter_app && python -m pytest tests/test_decoder.py -v 2>&1 | tee /tmp/ph8-02-red-host.txt`. With the firmware+host emit/decode still on u8 len AND the conftest now emitting u16 len, the existing tests SHOULD FAIL (frame_len reads as the high byte of u16 = 0x00). Expected output: most tests fail with a decode-error or zero-frame-length warning. This is the RED step; do NOT commit yet.
    - DO NOT run `pio test -e native -f "*test_messages*"` until Task 2 lands the firmware emit change — the test file would not compile if it references new offset constants that the emit function does not yet honor. Wait until Task 2.

    Part E — defer commit. Do NOT commit the test changes alone. The atomic-commit constraint (W-04) means firmware + host + tests land together. Stage everything for the Task 2 single-commit-per-sub-repo flow.
  </action>
  <verify>
    <automated>test -f firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp &amp;&amp; grep -q "captured\[6\]" firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp &amp;&amp; grep -q "65533\|65534" firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp &amp;&amp; grep -q "struct.pack" firestarter_app/tests/conftest.py &amp;&amp; grep -q "test_data_chunk_body_over_253_bytes_decodes\|test_data_chunk_body_254_bytes_at_old_u8_limit" firestarter_app/tests/test_decoder.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "captured\[6\]" firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp` returns at least 1 (id offset shifted).
    - `grep -q "65533" firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp` exits 0 (new guard threshold reflected).
    - `grep -q "struct.pack" firestarter_app/tests/conftest.py` exits 0; `grep -q "import struct" firestarter_app/tests/conftest.py` exits 0.
    - `grep -q "test_data_chunk_body_over_253_bytes_decodes" firestarter_app/tests/test_decoder.py` exits 0.
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` FAILS at least one test (RED state — firmware/host still on u8 path). Output captured to `/tmp/ph8-02-red-host.txt`. This is the expected pre-Task-2 state.
    - No commit landed yet (intentional — atomicity with Task 2).
  </acceptance_criteria>
  <done>
    Test files updated for the new u16 wire format. Host test_decoder.py is in RED state (expected). Test changes staged but not committed; Task 2 commits everything together.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen _firestarter_emit_frame to u16 len + widen _read_and_parse_lines to read 2 bytes (GREEN step — atomic firmware+host commit)</name>
  <files>
    firestarter/src/boards/rurp_serial_utils.cpp
    firestarter_app/firestarter/serial_comm.py
  </files>
  <read_first>
    - firestarter/src/boards/rurp_serial_utils.cpp (focus lines 156-200 — full _firestarter_emit_frame body; note the existing param_count > 253 guard and the single SERIAL_PORT.write(len) call)
    - firestarter_app/firestarter/serial_comm.py (focus lines 391-512 — _read_and_parse_lines; the `len_bytes = self.connection.read(1)` site is the single read to change. Also verify `import struct` at module top — if absent, add it.)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Code Examples" §"Wire-format `len` widening" + §"Host `len` widening" + §"Common Pitfalls" §P3 (build_frame coordination)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter/src/boards/rurp_serial_utils.cpp" + §"firestarter_app/firestarter/serial_comm.py" §"Site 3"
    - firestarter/CLAUDE.md
    - firestarter_app/CLAUDE.md
  </read_first>
  <behavior>
    - _firestarter_emit_frame writes len as two bytes (MSB first, then LSB). For every call where param_count <= 253 the wire is byte-identical to the old format except for one extra leading 0x00 byte before the old len byte.
    - The param_count guard threshold rises from 253 to 65533. param_count is uint8_t at the API surface, so the practical guard still hits at 254 for typical callers; the new threshold matters only for MSG_DATA_CHUNK paths added in Plan 05.
    - Host read advances by exactly 2 bytes after MAGIC_PREAMBLE matches and decodes them as a u16 big-endian integer. The subsequent body read of frame_len bytes is unchanged.
    - All 12+ existing test_decoder.py tests now PASS (GREEN) because conftest.build_frame() and serial_comm now agree on u16 len.
    - New test_data_chunk_body_over_253_bytes_decodes PASSES — the 512-byte payload roundtrips end-to-end.
    - Native `test_messages` Unity suite passes with the updated byte-offset assertions (running on the freshly-built native binary that now emits u16 len).
  </behavior>
  <action>
    Part A — firmware widen in `firestarter/src/boards/rurp_serial_utils.cpp` (RESEARCH §Code Examples Wire-format len widening):
    - Locate the param_count guard: replace `if (param_count > 253) return;` with `if (param_count > 65533) return;`.
    - Locate the line `uint8_t len = (uint8_t)(1 + param_count + 1);` and `SERIAL_PORT.write(len);`. Replace with `uint16_t len_u16 = (uint16_t)(1 + param_count + 1);` then `SERIAL_PORT.write((uint8_t)(len_u16 >> 8));` followed by `SERIAL_PORT.write((uint8_t)(len_u16 & 0xFF));`. Keep all surrounding code (CRC accumulation, id write, params loop, crc write, 0x0A anchor write) UNCHANGED.
    - Do NOT change the function signature (`param_count` stays `uint8_t`). The widened guard is forward-looking; existing callers cannot trigger the >253 path until Plan 05 adds the MSG_DATA_CHUNK call-site that bypasses param_count via LOG_ID_BYTES with a uint16_t internal accumulator (Plan 05 will introduce a sibling `_firestarter_emit_frame_wide` or widen this signature; either choice is fine, Plan 05 owns it).

    Part B — host widen in `firestarter_app/firestarter/serial_comm.py` (RESEARCH §Host len widening):
    - At module top, confirm `import struct` is present. If not, add it.
    - In `_read_and_parse_lines` locate the block immediately after the MAGIC_PREAMBLE match where `len_bytes = self.connection.read(1)` is called. Replace `read(1)` with `read(2)`. Change the existing `if not len_bytes` guard to `if len(len_bytes) < 2: logger.warning("Magic preamble seen but length bytes not received — re-syncing."); continue` (or whatever exact phrasing matches the existing log style — preserve the existing severity and message style).
    - Replace `frame_len = len_bytes[0]` with `frame_len = struct.unpack_from(">H", len_bytes)[0]`.
    - Do NOT change `_decode_id_frame` signature; it still receives `frame_len: int` and a `body: bytes` — the widening only affects how `frame_len` is computed by the caller.

    Part C — verification (GREEN):
    1. Host tests: `cd firestarter_app && python -m pytest tests/test_decoder.py -v 2>&1 | tee /tmp/ph8-02-green-host.txt`. ALL tests must pass, including the new `test_data_chunk_body_over_253_bytes_decodes` and `test_data_chunk_body_254_bytes_at_old_u8_limit`.
    2. Firmware native: `cd firestarter && pio test -e native -f "*test_messages*" 2>&1 | tee /tmp/ph8-02-green-native.txt`. All test_rurp_log_id.cpp assertions pass with the updated byte-offsets.
    3. Firmware AVR builds: `cd firestarter && pio run -e uno --target clean && pio run -e uno && pio run -e leonardo --target clean && pio run -e leonardo`. Both must exit 0. (Capture build output for the SRAM/flash baseline — Plan 08 uses this as the pre-conversion reference.)

    Part D — commit. Two commits land — one in firestarter, one in firestarter_app:
    - In `firestarter/`: subject `feat(wire): widen frame len field from u8 to u16 big-endian (W-04)`. Include `src/boards/rurp_serial_utils.cpp` and `test/native/avr/test_messages/test_rurp_log_id.cpp` in the same commit (firmware-side atomicity).
    - In `firestarter_app/`: subject `feat(wire): widen frame len field from u8 to u16 big-endian (W-04)`. Include `firestarter/serial_comm.py`, `tests/conftest.py`, and `tests/test_decoder.py` in the same commit (host-side atomicity).
    - Both commits land in the same wall-clock minute to keep the firmware+host pair tight; document the pair-up in commit bodies (e.g. firmware commit body says "Host pair: firestarter_app SHA <to-fill>"; the host commit references the firmware SHA back).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom &amp;&amp; cd firestarter_app &amp;&amp; python -m pytest tests/test_decoder.py -v &amp;&amp; cd .. &amp;&amp; cd firestarter &amp;&amp; pio test -e native -f "*test_messages*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0 with ALL tests passing (12 baseline + 2 new = 14+).
    - `cd firestarter && pio test -e native -f "*test_messages*"` exits 0; test_rurp_log_id.cpp suite reports all assertions green.
    - `cd firestarter && pio run -e uno` exits 0; `cd firestarter && pio run -e leonardo` exits 0. Both Flash usage values captured to /tmp for Plan 08 reference.
    - `grep -q "len_u16" firestarter/src/boards/rurp_serial_utils.cpp` exits 0 (widening landed).
    - `grep -q 'struct.unpack_from(">H"' firestarter_app/firestarter/serial_comm.py` exits 0 (host read uses u16 big-endian).
    - `grep -q "65533" firestarter/src/boards/rurp_serial_utils.cpp` exits 0 (new guard threshold landed).
    - Two commits land — one in each sub-repo — with subjects starting `feat(wire): widen frame len field`. Firmware commit body and host commit body cross-reference each other's SHAs (atomicity documentation).
  </acceptance_criteria>
  <done>
    Wire-format major bump in flight. Firmware emits u16 len. Host reads u16 len. All existing tests green (including the new W-04 Wave-0 gap tests). Both AVR builds clean. Two paired commits with cross-references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firmware emit → host decode (UART) | u16 len now spans 2 bytes; corruption of either byte misaligns the body read. Existing CRC8 over [id, params] catches any body corruption but does NOT protect the len field itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | Adversarial frame claiming 65535-byte body | mitigate | pyserial read timeout caps the host's read window; if the body never arrives, `_read_and_parse_lines` continues and the magic-preamble loop re-syncs on the next preamble. |
| T-08-02-02 | DoS | Firmware emits truncated u16 len (only 1 byte arrives) | mitigate | Host guard `if len(len_bytes) < 2: continue` logs a warning and re-syncs on the next preamble. No infinite-loop risk. |
| T-08-02-03 | Tampering | Mid-frame byte loss aliases as new magic preamble | accept | Existing 4-byte magic preamble + CRC8 design (Phase 6) already mitigates; W-04 widening does not change that property. |
</threat_model>

<verification>
- Host test_decoder.py: 14+ tests pass (12 baseline + 2 new u16-required tests for MSG_DATA_CHUNK).
- Native test_messages: all assertions pass with shifted byte offsets.
- Both AVR builds (Uno + Leonardo) compile.
- Two paired commits in the two sub-repos with cross-reference SHAs.
- Firmware emit guard threshold is 65533; host frame_len decoded as u16 big-endian.
</verification>

<success_criteria>
- Wire-format `len` is u16 big-endian end-to-end after this plan.
- Firmware + host change atomically (no commit-window gap where a half-upgraded firmware can talk to a half-upgraded host).
- The Wave-0 gap from VALIDATION.md (MSG_DATA_CHUNK body > 253 bytes) is exercised by a passing test.
- Downstream plans (03/04/05) can now build their populate-site conversions on top of a u16-len wire.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-02-SUMMARY.md` with: the firmware commit SHA, the host commit SHA, the Uno + Leonardo Flash usage before/after this plan (captured from the build output), and a one-line "wire-format major bump landed" assertion.
</output>
</content>
