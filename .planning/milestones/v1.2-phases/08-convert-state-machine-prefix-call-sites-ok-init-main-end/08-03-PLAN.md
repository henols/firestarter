---
phase: 08-convert-state-machine-prefix-call-sites-ok-init-main-end
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/tests/test_decoder.py
autonomous: true
requirements:
  - LMIG-03
must_haves:
  truths:
    - "EXPECTED_PREFIXES no longer contains 'MAIN', 'INIT', or 'END' — the host parser does not line-prefix-match those acks any more"
    - "STATE_MACHINE_PREFIXES is removed or set to an empty list"
    - "_log_rurp_feedback no longer rewrites response.message to 'Done' for state-machine prefixes (catalog format string handles rendering for ID frames)"
    - "PREFIX_REGEX (recompiled from EXPECTED_PREFIXES at module load) no longer matches 'MAIN'/'INIT'/'END' line prefixes"
    - "expect_ack() routes incoming ID frames by catalog severity band — verified by a new ID-frame OK round-trip test (existing prefix-line OK path still works because OK stays in EXPECTED_PREFIXES until Plan 04 lands the conversion)"
    - "_format_message handles the P-02/P-03/P-04 sentinel-byte rendering rules (0xFF in effective/override/hw → render without override clause)"
    - "Wave-0 decoder tests for MSG_INIT_DONE / MSG_MAIN_DONE / MSG_END_DONE ID-frame decode pass (new tests, were failing before this plan because the catalog severity labels weren't wired through the routing yet)"
    - "Wave-0 decoder tests for P-02 (MSG_OK_REV u8+u8), P-03 (MSG_OK_CFG u32+u32+u8), P-04 (MSG_OK_FW_HANDSHAKE u8+u8+ascii_str) pass with 0xFF sentinel rendering"
    - "test_wire_format_text_catalog_id_rejected_as_id_frame updated to only assert rejection for 0x03 (MSG_OK_FW_VERSION) — 0x06 (MSG_OK_FW_HANDSHAKE) is now id_frame per P-04 and must NOT be in the rejection set"
    - "All previously-passing tests in test_decoder.py still pass — no regression"
  artifacts:
    - path: "firestarter_app/firestarter/serial_comm.py"
      provides: "EXPECTED_PREFIXES surgical removal + STATE_MACHINE_PREFIXES empty + _log_rurp_feedback edit + _format_message sentinel handling"
      contains: "STATE_MACHINE_PREFIXES = []"
    - path: "firestarter_app/tests/test_decoder.py"
      provides: "ID-frame decode tests for INIT/MAIN/END acks + P-02/P-03/P-04 sentinel tests"
      contains: "test_init_done_arrives_as_id_frame"
  key_links:
    - from: "firestarter_app/firestarter/serial_comm.py"
      to: "firestarter_app/firestarter/messages.py"
      via: "CATALOG severity-band lookup (already wired in Phase 6)"
      pattern: "CATALOG\\["
    - from: "firestarter_app/tests/test_decoder.py"
      to: "firestarter_app/firestarter/messages.py"
      via: "import MSG_INIT_DONE, MSG_MAIN_DONE, MSG_END_DONE, MSG_OK_REV, MSG_OK_CFG, MSG_OK_FW_HANDSHAKE"
      pattern: "from firestarter.messages import"
---

<objective>
Wave 3 — surgical removal of the host parser's line-prefix matching for the state-machine acks (`MAIN`, `INIT`, `END`) and host-side sentinel-byte rendering for the P-02/P-03/P-04 reshaped IDs. Adds the Wave-0 gap tests from VALIDATION.md: ID-frame decode tests for MSG_INIT_DONE/MSG_MAIN_DONE/MSG_END_DONE; P-02/P-03/P-04 sentinel-rendering tests; updated `test_wire_format_text_catalog_id_rejected_as_id_frame` to drop 0x06 from the rejection set. This plan does NOT touch any firmware — every firmware emit still flows as text for OK/INIT/MAIN/END/DATA acks until Plan 04 + 05 land the populate-site conversions. The catalog (Plan 01) already declares MSG_INIT_DONE/MSG_MAIN_DONE/MSG_END_DONE/MSG_OK_REV/MSG_OK_CFG/MSG_OK_FW_HANDSHAKE, so the host parser can decode them properly the moment Plan 04 emits them.

Purpose: separating host-parser changes from firmware-emit changes keeps the diff per file small and reviewable. After this plan, hand-crafted Python test fixtures can roundtrip ID-frame INIT/MAIN/END/REV/CFG/FW_HANDSHAKE frames through the decoder and assert the expected `Response` object — the proof that Plan 04 + 05's firmware-emit conversions will be correctly decoded host-side.
Output: surgical edits to `serial_comm.py` (four targeted sites per RESEARCH §"Host Parser Surgical Removal Map"); new + updated tests in `test_decoder.py`; all tests green.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-CONTEXT.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md
@.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-02-SUMMARY.md

<interfaces>
serial_comm.py current state (after Plan 02):
- EXPECTED_PREFIXES at lines 123-133 (post-Plan-02 — may have shifted) currently contains the strings `"OK"`, `"INIT"`, `"MAIN"`, `"END"`, `"DATA"`, `"INFO"`, `"DEBUG"`, `"ERROR"`, `"WARN"` (verify exact order from the file).
- STATE_MACHINE_PREFIXES at line ~146 is a non-empty list, used by `_log_rurp_feedback` to short-circuit the message text to `"Done"` for those prefixes.
- PREFIX_REGEX is compiled from EXPECTED_PREFIXES at module load.
- _decode_id_frame at lines 299-389 — works correctly; no change needed for the surgical removal (Phase 6 already wires severity from CATALOG[msg_id].severity).
- _format_message renders params into format strings for ID frames. It must be extended to handle the P-02/P-03/P-04 sentinel-byte cases:
  - MSG_OK_REV: if params[1] == 0xFF, render `"Rev{0}"` (drop the override clause); else render `"Rev{1}, Override HW: Rev{0}"`.
  - MSG_OK_CFG: if params[2] == 0xFF, render `"R1: {0}, R2: {1}"`; else render `"R1: {0}, R2: {1}, Override HW: Rev{2}"`.
  - MSG_OK_FW_HANDSHAKE: if params[0] == 0xFF, render `"FW: {2}, Cmd: 0x{1:02x}"`; else render `"FW: {2}, HW: Rev{0}, Cmd: 0x{1:02x}"`.
- _decode_id_frame WR-03 guard at lines ~338-351 still rejects binary frames for `wire_format="text"` catalog entries. After Plan 01's catalog flip of 0x06 to "id_frame", the guard naturally allows 0x06 — but `test_wire_format_text_catalog_id_rejected_as_id_frame` must be updated.

Phase 8 target state of serial_comm.py:
- EXPECTED_PREFIXES: remove `"INIT"`, `"MAIN"`, `"END"`. Keep `"OK"` (still text until Plan 04 lands), `"DATA"` (still text until Plan 05 lands), and the INFO/WARN/ERROR/DEBUG entries (those are already ID frames per Phase 7).
- STATE_MACHINE_PREFIXES: set to empty list (`STATE_MACHINE_PREFIXES = []`) with a comment explaining W-01 ID-frame migration.
- _log_rurp_feedback: remove the `if response.type in STATE_MACHINE_PREFIXES: message = "Done"` branch (the catalog format string is the authoritative renderer for ID frames).
- _format_message: add the three P-02/P-03/P-04 sentinel-aware branches. Keep the existing format-string rendering for all other IDs unchanged.

Existing test_decoder.py test landscape:
- TestIdFrameDecoder class with ~12 tests covering MSG_OK_READY, MSG_OK_REQ_DATA, various INFO/WARN/ERROR shapes, the WR-03 guard, and the conftest.build_frame helper.
- test_wire_format_text_catalog_id_rejected_as_id_frame currently asserts BOTH 0x03 (MSG_OK_FW_VERSION) AND 0x06 (MSG_OK_FW_HANDSHAKE) are rejected as id_frames. After Plan 01 + this plan, only 0x03 stays in the rejection set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave-0 gap decoder tests for INIT/MAIN/END/REV/CFG/FW_HANDSHAKE + update WR-03 rejection test (RED step)</name>
  <files>firestarter_app/tests/test_decoder.py</files>
  <read_first>
    - firestarter_app/tests/test_decoder.py (full file — read every existing test in TestIdFrameDecoder to learn the assertion conventions; locate test_wire_format_text_catalog_id_rejected_as_id_frame for the 0x06 update)
    - firestarter_app/tests/conftest.py (post-Plan-02 — build_frame already u16 len)
    - firestarter_app/firestarter/serial_comm.py (current state — note the existing _format_message rendering for OK_REV/OK_CFG before this plan modifies it)
    - firestarter_app/firestarter/messages.py (post-Plan-01 — confirm MSG_INIT_DONE/MSG_MAIN_DONE/MSG_END_DONE/MSG_OK_REV/MSG_OK_CFG/MSG_OK_FW_HANDSHAKE are exported as Python ints and CATALOG carries the new shapes)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Common Pitfalls" §P4 (WR-03 guard change for 0x06)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-VALIDATION.md (Wave 0 gap rows for test_decoder.py)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter_app/tests/test_decoder.py" (new tests to add)
  </read_first>
  <behavior>
    - test_init_done_arrives_as_id_frame: builds a frame for MSG_INIT_DONE with no params; feeds via fake_serial; drives one response; asserts response is non-None, response.type == "INIT" (catalog severity), response.message matches the MSG_INIT_DONE format string (likely just "Done" or similar — read the catalog entry).
    - test_main_done_arrives_as_id_frame: same pattern with MSG_MAIN_DONE → response.type == "MAIN".
    - test_end_done_arrives_as_id_frame: same with MSG_END_DONE → response.type == "END".
    - test_fw_handshake_p04_with_hw_revision_decodes: builds a frame for MSG_OK_FW_HANDSHAKE with params = bytes([0x01, 0x0F]) + bytes([len("2.0.11-dev")]) + b"2.0.11-dev" (hw=0x01, cmd=0x0F, ascii_str fw_version). Asserts response.type == "OK", response.message == "FW: 2.0.11-dev, HW: Rev1, Cmd: 0x0f" (verify exact format-string render against the catalog entry).
    - test_fw_handshake_p04_no_hw_revision_decodes: same with hw=0xFF sentinel. Asserts response.message == "FW: 2.0.11-dev, Cmd: 0x0f" (no HW: clause).
    - test_ok_rev_p02_with_override_decodes: builds frame for MSG_OK_REV with params = bytes([0x01, 0x02]) (physical=1, effective=2). Asserts response.message == "Rev2, Override HW: Rev1".
    - test_ok_rev_p02_no_override_decodes: builds frame for MSG_OK_REV with params = bytes([0x01, 0xFF]). Asserts response.message == "Rev1".
    - test_ok_cfg_p03_with_override_decodes: builds frame for MSG_OK_CFG with params = struct.pack(">II", 10000, 4700) + bytes([0x02]) (r1=10000, r2=4700, override=2). Asserts response.message == "R1: 10000, R2: 4700, Override HW: Rev2".
    - test_ok_cfg_p03_no_override_decodes: same with override=0xFF. Asserts response.message == "R1: 10000, R2: 4700".
    - test_wire_format_text_catalog_id_rejected_as_id_frame (UPDATE): change the assertion to test ONLY id 0x03 (MSG_OK_FW_VERSION) is rejected. Add a positive assertion: 0x06 (MSG_OK_FW_HANDSHAKE) is NOT in the rejection set (build a frame with valid P-04 params and assert the decoder accepts it).
  </behavior>
  <action>
    Step 1 — add imports at the top of test_decoder.py: `from firestarter.messages import MSG_INIT_DONE, MSG_MAIN_DONE, MSG_END_DONE, MSG_OK_REV, MSG_OK_CFG, MSG_OK_FW_HANDSHAKE` and `import struct` if absent.

    Step 2 — append the following tests inside the existing `TestIdFrameDecoder` class. Each follows the existing test pattern (use fake_serial.feed, _drive_one_response, assert on response.type and response.message):
    - `test_init_done_arrives_as_id_frame`
    - `test_main_done_arrives_as_id_frame`
    - `test_end_done_arrives_as_id_frame`
    - `test_fw_handshake_p04_with_hw_revision_decodes`
    - `test_fw_handshake_p04_no_hw_revision_decodes`
    - `test_ok_rev_p02_with_override_decodes`
    - `test_ok_rev_p02_no_override_decodes`
    - `test_ok_cfg_p03_with_override_decodes`
    - `test_ok_cfg_p03_no_override_decodes`

    Each test body builds the frame via `build_frame(MSG_*, params_bytes)` (conftest.build_frame already u16-correct post-Plan-02), feeds to fake_serial, drives one response, asserts response.type matches the expected severity label and response.message matches the expected human-readable rendering. For the P-02/P-03/P-04 sentinel tests, the expected response.message values are exactly what `_format_message` must render after Task 2 of this plan — these tests are the RED step.

    Step 3 — modify the existing `test_wire_format_text_catalog_id_rejected_as_id_frame` test. Change it from "asserts rejection for both 0x03 and 0x06" to "asserts rejection for 0x03 ONLY". Add an inline positive sub-assertion: build a valid P-04 frame for 0x06 (hw=0xFF, cmd=0x00, ascii_str = "test"), feed it, and assert the decoder accepts it (returns a non-None response with response.type == "OK").

    Step 4 — RED-state verification. Run `cd firestarter_app && python -m pytest tests/test_decoder.py -v 2>&1 | tee /tmp/ph8-03-red.txt`. Expected: the 9 new tests and the updated WR-03 test will FAIL (the host parser hasn't been updated yet — _format_message doesn't render the sentinel branches; the WR-03 guard still rejects 0x06; INIT/MAIN/END routing may or may not need updates). Document which exact tests fail in `/tmp/ph8-03-red.txt`.

    Step 5 — defer commit. Stage but do not commit. Task 2 lands the serial_comm.py edits together with these tests.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom/firestarter_app &amp;&amp; grep -q "test_init_done_arrives_as_id_frame" tests/test_decoder.py &amp;&amp; grep -q "test_fw_handshake_p04_with_hw_revision_decodes" tests/test_decoder.py &amp;&amp; grep -q "test_ok_rev_p02_no_override_decodes" tests/test_decoder.py &amp;&amp; grep -q "test_ok_cfg_p03_with_override_decodes" tests/test_decoder.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_init_done_arrives_as_id_frame\|def test_main_done_arrives_as_id_frame\|def test_end_done_arrives_as_id_frame" firestarter_app/tests/test_decoder.py` returns 3.
    - `grep -c "def test_fw_handshake_p04" firestarter_app/tests/test_decoder.py` returns 2.
    - `grep -c "def test_ok_rev_p02\|def test_ok_cfg_p03" firestarter_app/tests/test_decoder.py` returns 4.
    - `grep -A20 "def test_wire_format_text_catalog_id_rejected_as_id_frame" firestarter_app/tests/test_decoder.py | grep -c "0x06\|MSG_OK_FW_HANDSHAKE"` — the test must reference 0x06 in a POSITIVE-acceptance sub-assertion, not a rejection set entry. Manually verify the test body asserts acceptance for 0x06.
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits NON-ZERO (RED state — at least the 9 new tests + WR-03 update are failing). Output saved to `/tmp/ph8-03-red.txt`.
    - No commit landed yet.
  </acceptance_criteria>
  <done>
    Wave-0 gap tests present in test_decoder.py; test_wire_format_text_catalog_id_rejected_as_id_frame updated for P-04. RED state — tests are failing as expected because serial_comm.py hasn't been touched yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surgical removal of state-machine prefix matching + sentinel-byte rendering in serial_comm.py (GREEN step)</name>
  <files>firestarter_app/firestarter/serial_comm.py</files>
  <read_first>
    - firestarter_app/firestarter/serial_comm.py (full file — read EXPECTED_PREFIXES around lines 123-133, STATE_MACHINE_PREFIXES around line 146, PREFIX_REGEX compilation site, _log_rurp_feedback around lines 282-283, _format_message — locate by grep for the function name)
    - firestarter_app/tests/test_decoder.py (the RED tests from Task 1 — these are the GREEN target)
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-RESEARCH.md §"Architectural Responsibility Map" + §"Host Parser Surgical Removal Map"
    - .planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-PATTERNS.md §"firestarter_app/firestarter/serial_comm.py" §"Site 1" through §"Site 4"
    - firestarter_app/CLAUDE.md
  </read_first>
  <behavior>
    - EXPECTED_PREFIXES list no longer contains "INIT", "MAIN", or "END" entries.
    - PREFIX_REGEX (recompiled from EXPECTED_PREFIXES at module load) no longer matches "INIT:", "MAIN:", "END:" line prefixes — verified by a grep against the regex pattern OR by an assertion in test_decoder.py.
    - STATE_MACHINE_PREFIXES is `[]` (empty list) with an inline comment explaining the W-01 ID-frame migration.
    - _log_rurp_feedback no longer rewrites response.message to "Done" for response.type in STATE_MACHINE_PREFIXES — the conditional branch is deleted (the empty list would never match anyway, but delete the dead conditional for clarity).
    - _format_message has three new sentinel-byte branches for MSG_OK_REV, MSG_OK_CFG, MSG_OK_FW_HANDSHAKE. Each branch checks the catalog id against the imported `MSG_OK_REV` / `MSG_OK_CFG` / `MSG_OK_FW_HANDSHAKE` ints and chooses the with-override vs no-override render based on the sentinel byte at the documented param index.
    - All 14+ existing test_decoder.py tests still pass; the 9 new Wave-0 tests now pass; the updated WR-03 test now passes.
  </behavior>
  <action>
    Step 1 — EXPECTED_PREFIXES surgical removal. Locate the list (lines ~123-133). Remove the entries for `"INIT"`, `"MAIN"`, and `"END"`. Keep `"OK"`, `"DATA"`, `"INFO"`, `"DEBUG"`, `"ERROR"`, `"WARN"` (the OK + DATA stay until their Plan 04 / 05 conversions land; the others were already migrated in Phase 7). Add a comment above the list explaining: `# Phase 8 W-01: INIT/MAIN/END removed (now arrive as ID frames). OK + DATA remain until Plan 04 + 05 firmware conversions land.`

    Step 2 — STATE_MACHINE_PREFIXES empty. Replace the existing list with `STATE_MACHINE_PREFIXES = []  # W-01: state-machine acks now arrive as ID frames; catalog format strings own the rendering.`. Do NOT delete the symbol entirely — other modules may import it.

    Step 3 — _log_rurp_feedback delete the dead branch. Locate the `if response.type in STATE_MACHINE_PREFIXES: message = "Done"` line (lines ~282-283). Delete it. The catalog format-string is now the authoritative render for ID frames (e.g. MSG_INIT_DONE.format = "Init done" — verify against messages.py).

    Step 4 — _format_message sentinel handling. Locate the function. Add an early branch (before the generic format-string rendering) that checks the catalog id:
    - If `msg_id == MSG_OK_REV` and `len(params) == 2` and `params[1] == 0xFF`: return `f"Rev{params[0]}"`.
    - If `msg_id == MSG_OK_REV` and `params[1] != 0xFF`: return `f"Rev{params[1]}, Override HW: Rev{params[0]}"`.
    - If `msg_id == MSG_OK_CFG` and `params[2] == 0xFF`: return `f"R1: {params[0]}, R2: {params[1]}"`.
    - If `msg_id == MSG_OK_CFG` and `params[2] != 0xFF`: return `f"R1: {params[0]}, R2: {params[1]}, Override HW: Rev{params[2]}"`.
    - If `msg_id == MSG_OK_FW_HANDSHAKE` and `params[0] == 0xFF`: return `f"FW: {params[2]}, Cmd: 0x{params[1]:02x}"`.
    - If `msg_id == MSG_OK_FW_HANDSHAKE` and `params[0] != 0xFF`: return `f"FW: {params[2]}, HW: Rev{params[0]}, Cmd: 0x{params[1]:02x}"`.
    For all other catalog ids fall through to the existing format-string rendering. Import the three MSG_OK_* names at the top of serial_comm.py from firestarter.messages.

    Step 5 — verify pyserial paths untouched. Confirm:
    - _decode_id_frame body — no edits required (it already routes severity via CATALOG[msg_id].severity per Phase 6).
    - The WR-03 guard at lines ~338-351 — no edits required. The guard reads `CATALOG[msg_id].wire_format` which is sourced from messages.py; Plan 01 already flipped 0x06 to "id_frame" so the guard now correctly accepts 0x06.
    - expect_ack() — no edits required (it already routes by response.type == "OK").

    Step 6 — GREEN verification. Run `cd firestarter_app && python -m pytest tests/test_decoder.py -v 2>&1 | tee /tmp/ph8-03-green.txt`. ALL tests must pass (the 12+ baseline + 9 new + 1 updated WR-03 = 22+ tests). If any baseline test fails because of a regression, surface immediately — do NOT commit a regression.

    Step 7 — commit. Single host-side commit with subject `feat(host): drop INIT/MAIN/END line-prefix matching, render P-02/P-03/P-04 sentinels (W-01, W-02)`. Files: `firestarter/serial_comm.py` and `tests/test_decoder.py` together.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_prom/firestarter_app &amp;&amp; python -m pytest tests/test_decoder.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0 with at least 22 tests passing (12 baseline + 9 new + 1 updated WR-03).
    - `grep -E '"INIT"|"MAIN"|"END"' firestarter_app/firestarter/serial_comm.py | grep -v '#' | grep -v 'test'` returns NO matches inside the EXPECTED_PREFIXES list area (any remaining hits are in comments — the `grep -v '#'` filter strips them).
    - `grep -q "STATE_MACHINE_PREFIXES = \[\]" firestarter_app/firestarter/serial_comm.py` exits 0.
    - `grep -q "MSG_OK_REV\|MSG_OK_CFG\|MSG_OK_FW_HANDSHAKE" firestarter_app/firestarter/serial_comm.py` exits 0 (imports + sentinel branches present).
    - `grep -A30 "def _format_message" firestarter_app/firestarter/serial_comm.py | grep -c "0xFF"` returns at least 3 (the three sentinel checks).
    - Single host-side commit landed with subject starting `feat(host): drop INIT/MAIN/END line-prefix matching`.
  </acceptance_criteria>
  <done>
    Host parser surgically updated: EXPECTED_PREFIXES no longer matches state-machine acks; STATE_MACHINE_PREFIXES empty; _log_rurp_feedback no longer rewrites to "Done"; _format_message handles P-02/P-03/P-04 sentinels. All tests green. Ready for Plans 04 + 05 to land the firmware-side emit conversions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firmware emit → host decode | After this plan, the host can decode every ID frame the catalog declares; firmware is still emitting most state-machine acks as text. Mixed flow is intentional during Wave 3 → Wave 4 transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Adversarial frame with mismatched sentinel byte | mitigate | _format_message validates exact-length params before reading the sentinel byte; out-of-range params already raise ValueError in WR-04. |
| T-08-03-02 | Information disclosure | _log_rurp_feedback emits the full ID-frame text payload including handshake fw_version | accept | fw_version is non-sensitive (operator-installed); the log channel is operator-side anyway. |
| T-08-03-03 | DoS | Removed prefix-matching branches in PREFIX_REGEX leave a parser gap if firmware reverts to text emit | mitigate | The 0x0A text-line fallback path in `_read_and_parse_lines` still handles unknown text — the line just gets logged with a "unmatched prefix" warning rather than parsed. No infinite loop. |
</threat_model>

<verification>
- All 22+ tests in test_decoder.py pass.
- EXPECTED_PREFIXES no longer contains MAIN/INIT/END.
- STATE_MACHINE_PREFIXES empty.
- _format_message handles P-02/P-03/P-04 sentinel rendering.
- Single host-side commit landed.
</verification>

<success_criteria>
- Host parser is ready to receive ID-frame INIT/MAIN/END acks from Plan 04 and P-02/P-03/P-04 frames from Plan 05.
- Wave-0 gap tests from VALIDATION.md (INIT/MAIN/END + P-02/P-03/P-04 + WR-03 update) all pass.
- No firmware files touched in this plan (clean separation; Plan 04 + 05 handle firmware).
</success_criteria>

<output>
After completion, create `.planning/phases/08-convert-state-machine-prefix-call-sites-ok-init-main-end/08-03-SUMMARY.md` with: the four surgical-edit sites in serial_comm.py with their pre/post line ranges, the count of new tests added (9 + 1 update), the pytest summary line, and a one-line "host parser ID-frame ready" assertion.
</output>
</content>
