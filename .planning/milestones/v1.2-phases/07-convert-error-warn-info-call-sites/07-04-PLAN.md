---
phase: 07-convert-error-warn-info-call-sites
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - firestarter/src/proms/flash_intel.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "Every active ERROR + WARN populate-site in flash_intel.cpp emits via LOG_ERROR_ID_* / LOG_WARN_ID_*"
    - "Dynamic-severity sites keep FLAG_FORCE branching"
    - "response_code state machine still drives operation-flow abort (LOG_*_ID call followed by handle->response_code assignment)"
    - "Both AVR builds still link cleanly; native dispatch tests still pass"
  artifacts:
    - path: "firestarter/src/proms/flash_intel.cpp"
      provides: "converted populate-sites: REV0_VPP_UNSUPPORTED, VPP_HIGH (dynamic), VPP_LOW, INTEL_VPP, INTEL_PROGRAM, INTEL_SR_TIMEOUT, CHIP_ID_MISMATCH (dynamic)"
      contains: "LOG_ERROR_ID(MSG_ERR_INTEL_VPP"
  key_links:
    - from: "firestarter/src/proms/flash_intel.cpp"
      to: "MSG_ERR_VPP_HIGH (0xB8) + MSG_ERR_CHIP_ID_MISMATCH (0xB9)"
      via: "LOG_ERROR_ID_BYTES populate (dynamic-severity branches)"
      pattern: "MSG_ERR_VPP_HIGH\\|MSG_ERR_CHIP_ID_MISMATCH"
---

<objective>
Convert every active ERROR + WARN populate-site in `firestarter/src/proms/flash_intel.cpp` to use the new `LOG_ERROR_ID_*` / `LOG_WARN_ID_*` macros. Per `07-RESEARCH.md` section 1, flash_intel.cpp has 7 populate-sites: lines 29, 41-43 (dynamic-severity VPP high), 45-47 (WARN VPP low), 135, 140, 147, 159 (dynamic-severity chip ID mismatch).

Purpose: Wave-2 populate-site wave (per LMIG-02 "one PROM module at a time").
Output: All ERROR/WARN populate-sites in flash_intel.cpp route through `rurp_log_id`; one `feat(flash_intel.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
New macros from Plan 01: LOG_ERROR_ID(...), LOG_WARN_ID(...), and the _U8/_U16/_U24/_U32/_BYTES variants.

Catalog IDs used in this file:
- MSG_WARN_REV0_VPP_UNSUPPORTED 0x80   params: []
- MSG_WARN_VPP_HIGH             0x82   params: [u16, u16, u16, u16]
- MSG_ERR_VPP_HIGH              0xB8   params: [u16, u16, u16, u16]   (Plan 02)
- MSG_WARN_VPP_LOW              0x81   params: [u16, u16, u16, u16]
- MSG_ERR_INTEL_VPP             0xB4   params: []
- MSG_ERR_INTEL_PROGRAM         0xB5   params: []
- MSG_ERR_INTEL_SR_TIMEOUT      0xB6   params: []
- MSG_WARN_CHIP_ID_MISMATCH     0x83   params: [u16, u16]
- MSG_ERR_CHIP_ID_MISMATCH      0xB9   params: [u16, u16]             (Plan 02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert flash_intel.cpp populate-sites (lines 29, 41-43, 45-47, 135, 140, 147, 159)</name>
  <files>firestarter/src/proms/flash_intel.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_intel.cpp lines 1-180 (every populate-site listed in 07-RESEARCH.md section 1 with surrounding control flow so FLAG_FORCE branches are visible)
    - firestarter/include/logging_id.h (new macro families from Plan 01)
    - firestarter/include/messages.h (confirm MSG_ERR_VPP_HIGH 0xB8 + MSG_ERR_CHIP_ID_MISMATCH 0xB9 present)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/flash_intel.cpp" and section 10 Pitfall 1 (dynamic-severity branches)
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    Make the following surgical replacements in `firestarter/src/proms/flash_intel.cpp`. Add `#include "logging_id.h"` if not already present (read top of file to confirm).

    REPLACEMENT 1 — flash_intel.cpp:29 (Rev0 VPP unsupported, WARN, 0 params):
    Current: `firestarter_warning_response("Rev0 dont support reading VPP/VPE");`
    Replace with two lines:
    - `LOG_WARN_ID(MSG_WARN_REV0_VPP_UNSUPPORTED);`
    - `handle->response_code = RESPONSE_CODE_WARNING;`

    REPLACEMENT 2 — flash_intel.cpp:41-43 (VPP high, DYNAMIC severity, 4xu16 params):
    Current (approx):
    ```
    int response_code = is_flag_set(FLAG_FORCE) ? RESPONSE_CODE_WARNING : RESPONSE_CODE_ERROR;
    firestarter_response_format(response_code, "VPP is high: %u.%uV > %u.%uV",
        (vpp_mv + 50) / 1000, (((vpp_mv + 50) / 100) % 10),
        (handle->vpp_mv + 50) / 1000, (((handle->vpp_mv + 50) / 100) % 10));
    ```
    Build an 8-byte stack array `_b[8]` packing the four u16 voltage values MSB-first (bytes 0-1 = v0, 2-3 = v1, 4-5 = v2, 6-7 = v3) and branch on `FLAG_FORCE`:
    ```
    if (is_flag_set(FLAG_FORCE)) {
        LOG_WARN_ID_BYTES(MSG_WARN_VPP_HIGH, _b, 8);
        handle->response_code = RESPONSE_CODE_WARNING;
    } else {
        LOG_ERROR_ID_BYTES(MSG_ERR_VPP_HIGH, _b, 8);
        handle->response_code = RESPONSE_CODE_ERROR;
    }
    ```
    Delete the now-unused `int response_code = ...;` local. CRITICAL (RESEARCH Pitfall 1): keep BOTH branches.

    REPLACEMENT 3 — flash_intel.cpp:45-47 (VPP low, WARN-only, 4xu16 params):
    Current:
    ```
    firestarter_warning_response_format("VPP is low: %u.%uV < %u.%uV",
        (vpp_mv + 50) / 1000, (((vpp_mv + 50) / 100) % 10),
        (handle->vpp_mv + 50) / 1000, (((handle->vpp_mv + 50) / 100) % 10));
    ```
    Replace with an 8-byte stack array (same packing as Replacement 2) and a single WARN emit:
    ```
    LOG_WARN_ID_BYTES(MSG_WARN_VPP_LOW, _b, 8);
    handle->response_code = RESPONSE_CODE_WARNING;
    ```

    REPLACEMENT 4 — flash_intel.cpp:135 (Intel flash VPP error, ERROR, 0 params):
    Current: `firestarter_error_response("Intel flash: VPP error");`
    Replace with:
    - `LOG_ERROR_ID(MSG_ERR_INTEL_VPP);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    REPLACEMENT 5 — flash_intel.cpp:140 (Intel flash program error, ERROR, 0 params):
    Current: `firestarter_error_response("Intel flash: program error");`
    Replace with:
    - `LOG_ERROR_ID(MSG_ERR_INTEL_PROGRAM);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    REPLACEMENT 6 — flash_intel.cpp:147 (Intel flash SR timeout, ERROR, 0 params):
    Current: `firestarter_error_response("Intel flash: SR timeout");`
    Replace with:
    - `LOG_ERROR_ID(MSG_ERR_INTEL_SR_TIMEOUT);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    REPLACEMENT 7 — flash_intel.cpp:159 (chip ID mismatch, DYNAMIC severity, 2xu16 params):
    Current (approx): `firestarter_response_format(response_code, "Chip ID %#04x dont match expected ID %#04x", chip_id, handle->chip_id);`
    Note: format matches catalog (no D-03 drift here). Build a 4-byte stack array `_b[4]` packing chip_id MSB-first into bytes 0-1 and handle->chip_id MSB-first into bytes 2-3. Branch on FLAG_FORCE:
    ```
    if (is_flag_set(FLAG_FORCE)) {
        LOG_WARN_ID_BYTES(MSG_WARN_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_WARNING;
    } else {
        LOG_ERROR_ID_BYTES(MSG_ERR_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_ERROR;
    }
    ```
    Delete the now-unused `int response_code = ...;` local if it was set immediately before the call. Keep BOTH branches per Pitfall 1.

    After all 7 replacements: run native dispatch tests, dual-board build, then commit from `firestarter/`: `feat(flash_intel.cpp): convert populate-sites to LOG_ERROR_ID_*/LOG_WARN_ID_*`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response" firestarter/src/proms/flash_intel.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -nE "firestarter_response_format" firestarter/src/proms/flash_intel.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_INTEL_VPP\|MSG_ERR_INTEL_PROGRAM\|MSG_ERR_INTEL_SR_TIMEOUT' firestarter/src/proms/flash_intel.cpp` >= 3.
    - `grep -c 'MSG_ERR_VPP_HIGH' firestarter/src/proms/flash_intel.cpp` >= 1 (dynamic-severity branch present).
    - `grep -c 'MSG_ERR_CHIP_ID_MISMATCH' firestarter/src/proms/flash_intel.cpp` >= 1.
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(flash_intel.cpp): convert populate-sites to LOG_ERROR_ID_*/LOG_WARN_ID_*` landed.
  </acceptance_criteria>
  <done>
    All 7 populate-sites in flash_intel.cpp converted; dynamic-severity branches preserved; dispatch tests + dual builds green.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment `firestarter_(error|warning)_response` references in flash_intel.cpp.
- Dynamic-severity sites have both WARN + ERROR branches.
- Native dispatch tests green; both boards build.
</verification>

<success_criteria>
- 7 populate-sites converted; FLAG_FORCE branching preserved.
- response_code routing unchanged.
- One commit `feat(flash_intel.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-04-SUMMARY.md`.
</output>
