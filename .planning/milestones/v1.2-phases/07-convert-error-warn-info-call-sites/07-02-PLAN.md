---
phase: 07-convert-error-warn-info-call-sites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/catalog/messages.toml
  - firestarter/include/messages.h
  - firestarter/src/messages.c
  - firestarter/tools/catalog/messages.toml
  - firestarter_app/firestarter/messages.py
  - firestarter_app/tools/catalog/messages.toml
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "MSG_ERR_VPP_HIGH (0xB8) exists in canonical catalog + both generated artifacts"
    - "MSG_ERR_CHIP_ID_MISMATCH (0xB9) exists in canonical catalog + both generated artifacts"
    - "MSG_ERR_MEM_SIZE_TOO_SMALL (0xBA) exists in canonical catalog + both generated artifacts"
    - "All three new IDs share their WARN counterparts' format string + param shape (only severity + ID differs)"
    - "Codegen drift gate passes: re-running codegen + sync produces zero diff"
  artifacts:
    - path: ".planning/catalog/messages.toml"
      provides: "3 new ERROR-severity entries in 0xB8..0xBA range"
      contains: "MSG_ERR_VPP_HIGH"
    - path: "firestarter/include/messages.h"
      provides: "MSG_ERR_VPP_HIGH / MSG_ERR_CHIP_ID_MISMATCH / MSG_ERR_MEM_SIZE_TOO_SMALL constants"
      contains: "MSG_ERR_VPP_HIGH"
    - path: "firestarter_app/firestarter/messages.py"
      provides: "CATALOG dict entries for 0xB8, 0xB9, 0xBA"
      contains: "MSG_ERR_VPP_HIGH"
  key_links:
    - from: ".planning/catalog/messages.toml"
      to: "firestarter/include/messages.h"
      via: "codegen.py (cpp emitter)"
      pattern: "MSG_ERR_VPP_HIGH"
    - from: ".planning/catalog/messages.toml"
      to: "firestarter_app/firestarter/messages.py"
      via: "codegen.py (python emitter)"
      pattern: "MSG_ERR_VPP_HIGH"
    - from: ".planning/catalog/messages.toml"
      to: "firestarter/tools/catalog/messages.toml + firestarter_app/tools/catalog/messages.toml"
      via: "sync_to_subrepos.sh (byte-identical vendor copies)"
      pattern: "MSG_ERR_VPP_HIGH"
---

<objective>
Resolve the three Phase 6 catalog gaps identified in `07-RESEARCH.md` section 2. Three dynamic-severity populate-sites in `proms/*.cpp` use `firestarter_response_format(response_code, ...)` where the runtime `response_code` is either `RESPONSE_CODE_WARNING` (when `FLAG_FORCE` is set) or `RESPONSE_CODE_ERROR` (otherwise). Phase 6 only allocated the WARN variants; the ERROR variants must exist before any Wave-2 PROM-module plan can convert those sites.

Per D-03: each gap is fixed with a SEPARATE `chore(catalog):` commit before any conversion proceeds. The drift gate (CI `regen && git diff --exit-code`) must remain green throughout.

Purpose: Wave-1 infrastructure — unblocks Plans 03 (`eprom.cpp`), 04 (`flash_intel.cpp`), 06 (`eeprom_28c.cpp`), and 12 (`flash_type_3.cpp`).
Output: 3 new catalog entries (0xB8, 0xB9, 0xBA), regenerated artifacts in both sub-repos, 3 sub-repo chore commits per ID.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
WARN counterparts already in catalog (lines 350-401 of messages.toml). Copy format + params VERBATIM into the new ERROR entries; change ONLY id + name + severity.

- WARN 0x82 MSG_WARN_VPP_HIGH:
  format = "VPP is high: %u.%uV > %u.%uV"
  params = [{u16}, {u16}, {u16}, {u16}]

- WARN 0x83 MSG_WARN_CHIP_ID_MISMATCH:
  format = "Chip ID %#04x dont match expected ID %#04x"
  params = [{u16}, {u16}]

- WARN 0x84 MSG_WARN_MEM_SIZE_TOO_SMALL:
  format = "mem_size %lu too small for chip-id check"
  params = [{u32, render = "dec"}]

ERROR ID block in catalog runs 0xA0..0xB7 (last existing ERROR is `MSG_ERR_OP_TIMEOUT` at 0xB7).
Next 3 IDs: 0xB8, 0xB9, 0xBA (RESEARCH section 2 "Suggested new IDs").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MSG_ERR_VPP_HIGH (0xB8) and commit chore</name>
  <files>
    .planning/catalog/messages.toml,
    firestarter/include/messages.h,
    firestarter/src/messages.c,
    firestarter/tools/catalog/messages.toml,
    firestarter_app/firestarter/messages.py,
    firestarter_app/tools/catalog/messages.toml
  </files>
  <read_first>
    - .planning/catalog/messages.toml (entry 0x82 MSG_WARN_VPP_HIGH lines 371-382 — copy format + params verbatim; entries 0xB6 / 0xB7 lines 602-616 — confirm last ERROR ID is 0xB7)
    - .planning/catalog/codegen.py (deterministic; idempotent emitters for cpp + python)
    - .planning/catalog/sync_to_subrepos.sh (vendor copy distribution)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 2 "D-03 Pre-Flight Audit"
    - firestarter/CLAUDE.md (build conventions)
    - firestarter_app/CLAUDE.md (pytest invocation for regression check)
  </read_first>
  <action>
    Append a single new TOML table to `.planning/catalog/messages.toml`, AFTER the last existing ERROR entry `MSG_ERR_OP_TIMEOUT` (currently at line 616) and BEFORE the `# DATA (0xE0..0xEF)` section header (lines 619-620). Copy the format string and params shape verbatim from `MSG_WARN_VPP_HIGH` (0x82, lines 371-382). The only differences are `id`, `name`, and `severity`.

    Entry to add (TOML keys in this exact shape):
    - id = 0xB8
    - name = "MSG_ERR_VPP_HIGH"
    - severity = "ERROR"
    - format = "VPP is high: %u.%uV > %u.%uV"
    - params = [{ type = "u16" }, { type = "u16" }, { type = "u16" }, { type = "u16" }]
    - wire_format = "id_frame"

    Then from the meta-repo root:

    1. Run `python .planning/catalog/codegen.py` (regenerates `firestarter/include/messages.h` + `firestarter/src/messages.c` + `firestarter_app/firestarter/messages.py`).
    2. Run `bash .planning/catalog/sync_to_subrepos.sh` (copies the canonical `messages.toml` byte-identically to both sub-repo `tools/catalog/` directories).
    3. Idempotence check: re-run `python .planning/catalog/codegen.py` and confirm the second run produces zero `git diff` on the generated files. If a diff appears, codegen is non-deterministic — STOP and surface.
    4. In `firestarter/` sub-repo: stage `include/messages.h`, `src/messages.c`, `tools/catalog/messages.toml` and commit with subject `chore(catalog): add MSG_ERR_VPP_HIGH 0xB8 (Phase 6 gap fix, see Phase 7)`.
    5. In `firestarter_app/` sub-repo: stage `firestarter/messages.py`, `tools/catalog/messages.toml` and commit with the same subject.
    6. The meta-repo `.planning/catalog/messages.toml` edit is committed by the orchestrator separately with the rest of the `.planning/` artifacts.

    Run host pytest to confirm decoder still parses cleanly after catalog change: `cd firestarter_app && python -m pytest tests/test_decoder.py -v`.
  </action>
  <verify>
    <automated>python /workspaces/firestarter_prom/.planning/catalog/codegen.py &amp;&amp; bash /workspaces/firestarter_prom/.planning/catalog/sync_to_subrepos.sh &amp;&amp; cd /workspaces/firestarter_prom/firestarter_app &amp;&amp; python -m pytest tests/test_decoder.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MSG_ERR_VPP_HIGH' .planning/catalog/messages.toml` returns 1.
    - `grep -c 'MSG_ERR_VPP_HIGH' firestarter/include/messages.h` >= 1.
    - `grep -c 'MSG_ERR_VPP_HIGH' firestarter_app/firestarter/messages.py` >= 1.
    - `grep -c '0xB8' firestarter/include/messages.h` >= 1.
    - Re-running `python .planning/catalog/codegen.py` produces zero `git diff` (idempotence).
    - `diff .planning/catalog/messages.toml firestarter/tools/catalog/messages.toml` returns empty.
    - `diff .planning/catalog/messages.toml firestarter_app/tools/catalog/messages.toml` returns empty.
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0.
    - Both sub-repos have a commit `chore(catalog): add MSG_ERR_VPP_HIGH 0xB8 (Phase 6 gap fix, see Phase 7)`.
  </acceptance_criteria>
  <done>
    `MSG_ERR_VPP_HIGH` is allocated at 0xB8 in canonical catalog + both generated artifacts + both vendored copies. Codegen is byte-identical on re-run. Host decoder regression suite passes. Two sub-repo chore commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add MSG_ERR_CHIP_ID_MISMATCH (0xB9) and commit chore</name>
  <files>
    .planning/catalog/messages.toml,
    firestarter/include/messages.h,
    firestarter/src/messages.c,
    firestarter/tools/catalog/messages.toml,
    firestarter_app/firestarter/messages.py,
    firestarter_app/tools/catalog/messages.toml
  </files>
  <read_first>
    - .planning/catalog/messages.toml (entry 0x83 MSG_WARN_CHIP_ID_MISMATCH lines 384-393 — copy format + params verbatim; confirm Task 1's 0xB8 entry is already present)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 2 "D-03 Pre-Flight Audit"
  </read_first>
  <action>
    Append a single new TOML table to `.planning/catalog/messages.toml`, AFTER the newly added 0xB8 `MSG_ERR_VPP_HIGH` entry from Task 1 (still inside the ERROR section, before the `# DATA` section header). Copy format + params verbatim from `MSG_WARN_CHIP_ID_MISMATCH` (0x83, lines 384-393).

    Entry to add:
    - id = 0xB9
    - name = "MSG_ERR_CHIP_ID_MISMATCH"
    - severity = "ERROR"
    - format = "Chip ID %#04x dont match expected ID %#04x"
    - params = [{ type = "u16" }, { type = "u16" }]
    - wire_format = "id_frame"

    Then repeat the codegen + sync + idempotence sequence from Task 1:

    1. `python .planning/catalog/codegen.py`
    2. `bash .planning/catalog/sync_to_subrepos.sh`
    3. Re-run codegen and confirm zero diff (idempotence).
    4. Sub-repo commits in `firestarter/` and `firestarter_app/` with subject `chore(catalog): add MSG_ERR_CHIP_ID_MISMATCH 0xB9 (Phase 6 gap fix, see Phase 7)`.
    5. `cd firestarter_app && python -m pytest tests/test_decoder.py -v` — regression check.
  </action>
  <verify>
    <automated>python /workspaces/firestarter_prom/.planning/catalog/codegen.py &amp;&amp; bash /workspaces/firestarter_prom/.planning/catalog/sync_to_subrepos.sh &amp;&amp; cd /workspaces/firestarter_prom/firestarter_app &amp;&amp; python -m pytest tests/test_decoder.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MSG_ERR_CHIP_ID_MISMATCH' .planning/catalog/messages.toml` returns 1.
    - `grep -c 'MSG_ERR_CHIP_ID_MISMATCH' firestarter/include/messages.h` >= 1.
    - `grep -c 'MSG_ERR_CHIP_ID_MISMATCH' firestarter_app/firestarter/messages.py` >= 1.
    - `grep -c '0xB9' firestarter/include/messages.h` >= 1.
    - Idempotence: re-running codegen yields zero diff.
    - `diff .planning/catalog/messages.toml firestarter/tools/catalog/messages.toml` returns empty.
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0.
    - Both sub-repos have `chore(catalog): add MSG_ERR_CHIP_ID_MISMATCH 0xB9 (Phase 6 gap fix, see Phase 7)` commit.
  </acceptance_criteria>
  <done>
    `MSG_ERR_CHIP_ID_MISMATCH` allocated at 0xB9. Codegen idempotent. Drift gate passes. Two sub-repo chore commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add MSG_ERR_MEM_SIZE_TOO_SMALL (0xBA) and commit chore</name>
  <files>
    .planning/catalog/messages.toml,
    firestarter/include/messages.h,
    firestarter/src/messages.c,
    firestarter/tools/catalog/messages.toml,
    firestarter_app/firestarter/messages.py,
    firestarter_app/tools/catalog/messages.toml
  </files>
  <read_first>
    - .planning/catalog/messages.toml (entry 0x84 MSG_WARN_MEM_SIZE_TOO_SMALL lines 395-401 — copy format + params verbatim, including `render = "dec"` on the u32 param; confirm 0xB8 + 0xB9 entries from Tasks 1-2 are present)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 2 "D-03 Pre-Flight Audit"
  </read_first>
  <action>
    Append a single new TOML table to `.planning/catalog/messages.toml`, AFTER the 0xB9 `MSG_ERR_CHIP_ID_MISMATCH` entry from Task 2 (still inside the ERROR section, before the `# DATA` section header). Copy format + params verbatim from `MSG_WARN_MEM_SIZE_TOO_SMALL` (0x84, lines 395-401), including the `render = "dec"` modifier on the u32 param.

    Entry to add:
    - id = 0xBA
    - name = "MSG_ERR_MEM_SIZE_TOO_SMALL"
    - severity = "ERROR"
    - format = "mem_size %lu too small for chip-id check"
    - params = [{ type = "u32", render = "dec" }]
    - wire_format = "id_frame"

    Then repeat the codegen + sync + idempotence sequence:

    1. `python .planning/catalog/codegen.py`
    2. `bash .planning/catalog/sync_to_subrepos.sh`
    3. Re-run codegen and confirm zero diff.
    4. Sub-repo commits in `firestarter/` and `firestarter_app/` with subject `chore(catalog): add MSG_ERR_MEM_SIZE_TOO_SMALL 0xBA (Phase 6 gap fix, see Phase 7)`.
    5. `cd firestarter_app && python -m pytest tests/test_decoder.py -v` — regression check.

    Final cross-check after all three gap fixes: confirm exactly three new entries exist in the canonical catalog:
    `grep -c '^name        = "MSG_ERR_VPP_HIGH"\|^name        = "MSG_ERR_CHIP_ID_MISMATCH"\|^name        = "MSG_ERR_MEM_SIZE_TOO_SMALL"' .planning/catalog/messages.toml` should return 3.
  </action>
  <verify>
    <automated>python /workspaces/firestarter_prom/.planning/catalog/codegen.py &amp;&amp; bash /workspaces/firestarter_prom/.planning/catalog/sync_to_subrepos.sh &amp;&amp; cd /workspaces/firestarter_prom/firestarter_app &amp;&amp; python -m pytest tests/test_decoder.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MSG_ERR_MEM_SIZE_TOO_SMALL' .planning/catalog/messages.toml` returns 1.
    - `grep -c 'MSG_ERR_MEM_SIZE_TOO_SMALL' firestarter/include/messages.h` >= 1.
    - `grep -c 'MSG_ERR_MEM_SIZE_TOO_SMALL' firestarter_app/firestarter/messages.py` >= 1.
    - `grep -c '0xBA' firestarter/include/messages.h` >= 1.
    - Idempotence: re-running codegen yields zero diff.
    - `diff .planning/catalog/messages.toml firestarter/tools/catalog/messages.toml` returns empty.
    - `diff .planning/catalog/messages.toml firestarter_app/tools/catalog/messages.toml` returns empty.
    - `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0.
    - Both sub-repos have `chore(catalog): add MSG_ERR_MEM_SIZE_TOO_SMALL 0xBA (Phase 6 gap fix, see Phase 7)` commit.
    - All three new MSG_ERR_* IDs (0xB8, 0xB9, 0xBA) appear in `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`.
  </acceptance_criteria>
  <done>
    All three Phase 6 catalog gaps closed. Each gap landed as a separate sub-repo chore commit (six commits total: 3 in firestarter, 3 in firestarter_app). Drift gate green. Host decoder still parses unchanged.
  </done>
</task>

</tasks>

<verification>
- `grep -c '0xB8\|0xB9\|0xBA' firestarter/include/messages.h` >= 3.
- Idempotence: `python .planning/catalog/codegen.py` produces zero diff when re-run.
- `diff .planning/catalog/messages.toml firestarter/tools/catalog/messages.toml` returns empty.
- `diff .planning/catalog/messages.toml firestarter_app/tools/catalog/messages.toml` returns empty.
- `cd firestarter_app && python -m pytest tests/test_decoder.py -v` exits 0.
</verification>

<success_criteria>
- Three new ERROR entries land in canonical catalog at 0xB8, 0xB9, 0xBA.
- Generated artifacts in both sub-repos contain the new IDs and constants.
- Codegen drift gate green: re-running codegen produces byte-identical output.
- Sub-repo chore commits landed for each gap (6 commits total across the two sub-repos).
- Host decoder regression suite (`tests/test_decoder.py`) still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-02-SUMMARY.md`.
</output>
