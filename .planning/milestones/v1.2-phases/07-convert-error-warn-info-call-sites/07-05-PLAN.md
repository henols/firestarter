---
phase: 07-convert-error-warn-info-call-sites
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/src/proms/flash_utils.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "flash_type_4.cpp:88 populate-site emits MSG_ERR_FL4_VERIFY_TIMEOUT via LOG_ERROR_ID_BYTES with 5-byte stack array (u8 + u24 + u8)"
    - "flash_utils.cpp:47 populate-site emits MSG_ERR_OP_TIMEOUT via LOG_ERROR_ID (0 params)"
    - "OK-path copy_to_buffer (flash_type_4.cpp:51 Skipping erase) is untouched"
    - "Both AVR builds still link cleanly; native dispatch tests still pass"
  artifacts:
    - path: "firestarter/src/proms/flash_type_4.cpp"
      provides: "converted ERROR populate-site at line 88"
      contains: "LOG_ERROR_ID_BYTES(MSG_ERR_FL4_VERIFY_TIMEOUT"
    - path: "firestarter/src/proms/flash_utils.cpp"
      provides: "converted ERROR populate-site at line 46"
      contains: "LOG_ERROR_ID(MSG_ERR_OP_TIMEOUT"
  key_links:
    - from: "firestarter/src/proms/flash_type_4.cpp"
      to: "MSG_ERR_FL4_VERIFY_TIMEOUT (0xB3)"
      via: "LOG_ERROR_ID_BYTES populate"
      pattern: "MSG_ERR_FL4_VERIFY_TIMEOUT"
    - from: "firestarter/src/proms/flash_utils.cpp"
      to: "MSG_ERR_OP_TIMEOUT (0xB7)"
      via: "LOG_ERROR_ID populate"
      pattern: "MSG_ERR_OP_TIMEOUT"
---

<objective>
Convert the single ERROR populate-site in `firestarter/src/proms/flash_type_4.cpp` (line 88) and the single ERROR populate-site in `firestarter/src/proms/flash_utils.cpp` (line 46). Combined into one plan because each file is a single-site conversion — RESEARCH section 8 explicitly justifies the collapse.

Purpose: Wave-2 populate-site wave.
Output: Both files converted; one commit per file (2 commits total) for bisectability per LMIG-02 "one PROM module at a time".
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
Macros from Plan 01: LOG_ERROR_ID(id), LOG_ERROR_ID_BYTES(id, buf, n).

Catalog IDs (Phase 6, already present):
- MSG_ERR_FL4_VERIFY_TIMEOUT 0xB3   params: [u8 hex_byte, u24 hex_addr, u8 hex_byte]
- MSG_ERR_OP_TIMEOUT         0xB7   params: []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert flash_type_4.cpp:88 (verify timeout, ERROR, 3 params u8+u24+u8)</name>
  <files>firestarter/src/proms/flash_type_4.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_type_4.cpp lines 40-100 (the populate-site at line 88 plus the OK-path `copy_to_buffer` at line 51 — out of scope)
    - firestarter/include/logging_id.h (new ERROR macros from Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/flash_type_4.cpp" and section 10 Pitfall 5 (stack array as named local, not compound literal)
  </read_first>
  <action>
    Make the following surgical replacement in `firestarter/src/proms/flash_type_4.cpp`. Add `#include "logging_id.h"` if not already present.

    REPLACEMENT — flash_type_4.cpp:88 (verify timeout, ERROR, 3 params):
    Current: `firestarter_error_response_format("Timeout verifying 0x%02x at 0x%06lx (got 0x%02x)", expected, address, observed);`

    Catalog `MSG_ERR_FL4_VERIFY_TIMEOUT` (0xB3) param shape is [u8, u24, u8] = 5 wire bytes. Pack into a named local `uint8_t _b[5]` inside a `{ ... }` block:
    - byte 0 = (uint8_t)expected
    - bytes 1-3 = (uint8_t)((address >> 16) & 0xFF), (uint8_t)((address >> 8) & 0xFF), (uint8_t)(address & 0xFF)  (MSB-first u24)
    - byte 4 = (uint8_t)observed

    Then:
    - `LOG_ERROR_ID_BYTES(MSG_ERR_FL4_VERIFY_TIMEOUT, _b, 5);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    DO NOT TOUCH (out of scope, Phase 8): flash_type_4.cpp:51 `copy_to_buffer(handle->response_msg, "Skipping erase.");` — OK-path populate.

    Commit from `firestarter/`: `feat(flash_type_4.cpp): convert populate-site to LOG_ERROR_ID_BYTES`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response" firestarter/src/proms/flash_type_4.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_FL4_VERIFY_TIMEOUT' firestarter/src/proms/flash_type_4.cpp` >= 1.
    - `grep -c 'copy_to_buffer(handle->response_msg' firestarter/src/proms/flash_type_4.cpp` unchanged from baseline (OK-path preserved).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(flash_type_4.cpp): convert populate-site to LOG_ERROR_ID_BYTES` landed.
  </acceptance_criteria>
  <done>
    flash_type_4.cpp:88 converted, OK-path preserved, builds + dispatch tests green, one commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert flash_utils.cpp:47 (operation timed out, ERROR, 0 params)</name>
  <files>firestarter/src/proms/flash_utils.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_utils.cpp lines 30-60 (the populate-site at line 46 plus surrounding control flow)
    - firestarter/include/logging_id.h (new ERROR macros from Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/flash_utils.cpp"
  </read_first>
  <action>
    Make the following surgical replacement in `firestarter/src/proms/flash_utils.cpp`. Add `#include "logging_id.h"` if not already present.

    REPLACEMENT — flash_utils.cpp:47 (operation timed out, ERROR, 0 params):
    Current: `firestarter_error_response("Operation timed out");`

    Replace with two lines:
    - `LOG_ERROR_ID(MSG_ERR_OP_TIMEOUT);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    Commit from `firestarter/`: `feat(flash_utils.cpp): convert populate-site to LOG_ERROR_ID`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response" firestarter/src/proms/flash_utils.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_OP_TIMEOUT' firestarter/src/proms/flash_utils.cpp` >= 1.
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(flash_utils.cpp): convert populate-site to LOG_ERROR_ID` landed.
  </acceptance_criteria>
  <done>
    flash_utils.cpp:47 converted, builds + dispatch tests green, one commit landed.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment `firestarter_(error|warning)_response` references in either file.
- Native dispatch tests green; both boards build.
- Two separate sub-repo commits (one per file).
</verification>

<success_criteria>
- Both single-site files converted via separate commits for LMIG-02 bisectability.
- response_code routing unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-05-SUMMARY.md`.
</output>
