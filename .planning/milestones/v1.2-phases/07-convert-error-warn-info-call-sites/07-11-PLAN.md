---
phase: 07-convert-error-warn-info-call-sites
plan: 11
type: execute
wave: 3
depends_on: [01]
files_modified:
  - firestarter/src/dev_tools.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "All 7 INFO call-sites in dev_tools.cpp emit via LOG_INFO_ID_* macros"
    - "ascii_str + u8 packing for MSG_INFO_REG_HEADER (reg_name + reg) uses {len, bytes, val} stack array layout"
    - "ascii_str packing for MSG_INFO_BIT_HEADER + MSG_INFO_BIT_STR uses {len, bytes} stack array layout with fixed-size buffer (RESEARCH section 3 — prefer uint8_t _b[32] over VLA)"
    - "Multi-param CE/OE site (used twice) emits MSG_INFO_CE_OE via 2-byte stack array {ce, oe}"
    - "Both AVR builds still link cleanly"
  artifacts:
    - path: "firestarter/src/dev_tools.cpp"
      provides: "7 converted INFO call-sites (REG_HEADER, BIT_HEADER, BIT_STR, CE_OE x2, ADDR, ADDR_REMAP)"
      contains: "LOG_INFO_ID_BYTES(MSG_INFO_REG_HEADER"
---

<objective>
Convert all 7 INFO call-sites in `firestarter/src/dev_tools.cpp` (lines 23, 26, 46, 66, 102, 103, 105) to the new `LOG_INFO_ID_*` macros. Per D-04a, `dev_tools.cpp` is in-scope: `-D DEV_TOOLS` is active in `platformio.ini` (not commented), so these sites compile into both Uno + Leonardo binaries and count toward SC#1 grep + flash budget.

The catalog has dedicated entries for each shape:
- `MSG_INFO_REG_HEADER` (0x52): ascii_str + u8 hex_byte
- `MSG_INFO_BIT_HEADER` (0x53): ascii_str
- `MSG_INFO_BIT_STR` (0x54): ascii_str
- `MSG_INFO_CE_OE` (0x55): u8 + u8
- `MSG_INFO_ADDR` (0x56): u24 hex_addr
- `MSG_INFO_ADDR_REMAP` (0x57): u24 hex_addr (catalog preserves the `remappend` typo intentionally per RESEARCH Pitfall + section 1)

Purpose: Wave-3 direct-log wave.
Output: 7 INFO sites converted, one `feat(dev_tools.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
Macros from Plan 01: LOG_INFO_ID, LOG_INFO_ID_U24, LOG_INFO_ID_BYTES (all FLAG_VERBOSE-gated per existing INFO family).

ascii_str wire format (Phase 6 LHOST-02): `{length_byte, byte0, byte1, ..., byte(len-1)}` — host's decoder reads length then `length` bytes of ASCII payload.

For MSG_INFO_REG_HEADER ([ascii_str, u8]): pack as `{len, name[0..len-1], reg_byte}` — total wire bytes = 1 + len + 1.
For MSG_INFO_BIT_HEADER + MSG_INFO_BIT_STR ([ascii_str]): pack as `{len, str[0..len-1]}` — total wire bytes = 1 + len.
For MSG_INFO_CE_OE ([u8, u8]): pack as `{ce, oe}` — 2 wire bytes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert dev_tools.cpp INFO call-sites (lines 23, 26, 46, 66, 102, 103, 105)</name>
  <files>firestarter/src/dev_tools.cpp</files>
  <read_first>
    - firestarter/src/dev_tools.cpp lines 15-115 (every call-site listed in 07-RESEARCH.md section 1 plus the surrounding control flow)
    - firestarter/include/logging_id.h (LOG_INFO_ID, LOG_INFO_ID_U24, LOG_INFO_ID_BYTES from Phase 6 + Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/dev_tools.cpp" (7-row table) + section 3 "Multi-Param Composer Recommendation" "ascii_str populate" (the `bit_str` packing example) + section 10 Pitfall 5 (stack array as named local) + Pitfall 7 (stack pressure assessment LOW for dev-tool paths)
    - firestarter/platformio.ini (confirm `-D DEV_TOOLS` is active — RESEARCH section 1 D-04a)
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    Make the following surgical replacements in `firestarter/src/dev_tools.cpp`. Add `#include "logging_id.h"` if not already present. The DEV_TOOLS conditional compilation (`#ifdef DEV_TOOLS`) wraps the whole file — leave that intact.

    Replacement 1 — dev_tools.cpp:23 (register header, INFO, ascii_str + u8):
    Current: `log_info_format("%s: 0x%02X", reg_name, reg);`

    Build a stack array `uint8_t _b[N]` where `N = 1 + strlen(reg_name) + 1`. Pack:
    - `_b[0] = (uint8_t)strlen(reg_name);`
    - `memcpy(&_b[1], reg_name, strlen(reg_name));`
    - `_b[1 + strlen(reg_name)] = (uint8_t)reg;`

    For predictability, use a fixed-size buffer like `uint8_t _b[16]` and clamp `strlen` to `15 - 1 = 14` (RESEARCH section 3 prefers fixed-size over VLA). Compute `uint8_t _len = (uint8_t)strlen(reg_name); if (_len > 14) _len = 14;` then:
    ```
    {
        uint8_t _len = (uint8_t)strlen(reg_name);
        if (_len > 14) _len = 14;
        uint8_t _b[16];
        _b[0] = _len;
        memcpy(&_b[1], reg_name, _len);
        _b[1 + _len] = (uint8_t)reg;
        LOG_INFO_ID_BYTES(MSG_INFO_REG_HEADER, _b, 1 + _len + 1);
    }
    ```

    Replacement 2 — dev_tools.cpp:26 (bit header, INFO, ascii_str):
    Current: `log_info_format("%s|D7|D6|D5|D4|D3|D2|D1|D0|", size == 9 ? "\|D8" : "");`

    The argument is either `"\|D8"` (3 chars) or `""` (0 chars). The catalog format string `"%s|D7|D6|D5|D4|D3|D2|D1|D0|"` includes the literal `|D7|...|D0|` so the wire only sends the variable prefix. Build:
    ```
    {
        const char* _prefix = (size == 9 ? "|D8" : "");
        uint8_t _len = (uint8_t)strlen(_prefix);
        uint8_t _b[8];
        _b[0] = _len;
        memcpy(&_b[1], _prefix, _len);
        LOG_INFO_ID_BYTES(MSG_INFO_BIT_HEADER, _b, 1 + _len);
    }
    ```
    (Note: the catalog has `|D8` not `\|D8` — the `\|` in the C source is an escaped pipe; on the wire it's a plain `|`. Confirm during read of the actual source line.)

    Replacement 3 — dev_tools.cpp:46 (bit string, INFO, ascii_str):
    Current: `log_info(bit_str);`

    The `bit_str` is a stack-allocated string of up to ~28 bytes per RESEARCH section 3. Use a fixed-size 32-byte buffer:
    ```
    {
        uint8_t _len = (uint8_t)strlen(bit_str);
        if (_len > 31) _len = 31;
        uint8_t _b[32];
        _b[0] = _len;
        memcpy(&_b[1], bit_str, _len);
        LOG_INFO_ID_BYTES(MSG_INFO_BIT_STR, _b, 1 + _len);
    }
    ```

    Replacement 4 — dev_tools.cpp:66 (CE/OE, INFO, 2 x u8):
    Current: `log_info_format("CE: %d, OE: %d", is_flag_set(FLAG_CHIP_ENABLE), is_flag_set(FLAG_OUTPUT_ENABLE));`

    Build a 2-byte stack array:
    ```
    {
        uint8_t _b[2] = {
            (uint8_t)is_flag_set(FLAG_CHIP_ENABLE),
            (uint8_t)is_flag_set(FLAG_OUTPUT_ENABLE),
        };
        LOG_INFO_ID_BYTES(MSG_INFO_CE_OE, _b, 2);
    }
    ```

    Replacement 5 — dev_tools.cpp:102 (CE/OE, INFO, 2 x u8 — same as Replacement 4 in different function):
    Current: `log_info_format("CE: %d, OE: %d", ...);` (verify arg order matches Replacement 4 — should be CE then OE)
    Replace with the same pattern as Replacement 4.

    Replacement 6 — dev_tools.cpp:103 (Address, INFO, u24):
    Current: `log_info_format("Address: 0x%06x", handle->address);`
    Replace with: `LOG_INFO_ID_U24(MSG_INFO_ADDR, handle->address);`

    Replacement 7 — dev_tools.cpp:105 (Address remapped, INFO, u24, catalog has "remappend" typo preserved):
    Current: `log_info_format("Address: 0x%06x remappend", address);`
    Replace with: `LOG_INFO_ID_U24(MSG_INFO_ADDR_REMAP, address);`
    (The catalog format `"Address: 0x%06x remappend"` preserves the original typo — call-site does not need to do anything special; the host renders using the catalog string.)

    Commit from `firestarter/`: `feat(dev_tools.cpp): convert INFO call-sites to LOG_INFO_ID_BYTES/_U24`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "log_(info_const|info_format|info\()" firestarter/src/dev_tools.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_INFO_REG_HEADER\|MSG_INFO_BIT_HEADER\|MSG_INFO_BIT_STR\|MSG_INFO_CE_OE\|MSG_INFO_ADDR\|MSG_INFO_ADDR_REMAP' firestarter/src/dev_tools.cpp` >= 6 (MSG_INFO_CE_OE appears twice, MSG_INFO_ADDR + MSG_INFO_ADDR_REMAP are distinct).
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - `cd firestarter && pio test -e native` exits 0.
    - Commit `feat(dev_tools.cpp): convert INFO call-sites to LOG_INFO_ID_BYTES/_U24` landed.
  </acceptance_criteria>
  <done>
    All 7 INFO sites in dev_tools.cpp converted; fixed-size stack buffers used per RESEARCH; both boards build; native test suite green.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment legacy INFO macro references in dev_tools.cpp.
- All 6 MSG_INFO_* IDs referenced (CE_OE twice).
- Both boards build.
</verification>

<success_criteria>
- 7 INFO sites converted using fixed-size buffer pattern.
- One commit `feat(dev_tools.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-11-SUMMARY.md`.
</output>
