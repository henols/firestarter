---
phase: 07-convert-error-warn-info-call-sites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/include/logging_id.h
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "logging_id.h exports an unconditional LOG_ERROR_ID family (no FLAG_VERBOSE gate)"
    - "logging_id.h exports an unconditional LOG_WARN_ID family (no FLAG_VERBOSE gate)"
    - "Existing LOG_INFO_ID_* macros and LOG_ID_* primitives stay byte-identical"
    - "Both boards still compile cleanly; no new linker symbols are required"
  artifacts:
    - path: "firestarter/include/logging_id.h"
      provides: "LOG_ERROR_ID, LOG_ERROR_ID_U8/U16/U24/U32/BYTES + same surface for LOG_WARN_ID"
      contains: "#define LOG_ERROR_ID("
    - path: "firestarter/include/logging_id.h"
      provides: "LOG_WARN_ID family"
      contains: "#define LOG_WARN_ID("
  key_links:
    - from: "firestarter/include/logging_id.h"
      to: "rurp_log_id (boards/rurp_serial_utils.cpp)"
      via: "LOG_ID_* primitive expansion (unchanged)"
      pattern: "rurp_log_id\\("
---

<objective>
Add the unconditional `LOG_ERROR_ID_*` and `LOG_WARN_ID_*` macro families to `firestarter/include/logging_id.h`, mirroring the existing `LOG_INFO_ID_*` surface but WITHOUT the `FLAG_VERBOSE` gate (ERROR + WARN must always emit). These macros are thin aliases over the existing `LOG_ID_*` primitives, so no new runtime code, no new symbols, no flash cost beyond what subsequent waves pull in at call-sites.

Purpose: Wave-1 infrastructure — every Wave-2 / Wave-3 plan converts call-sites to these new macros, so they must exist and compile cleanly first.
Output: Updated `logging_id.h` with 12 new macros, native + dual-board builds green.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-VALIDATION.md

<interfaces>
<!-- Current logging_id.h surface (Phase 6 output) — Phase 7 ADDS to this file, does not modify existing macros. -->

Existing primitives (untouched):
- LOG_ID(id)                      → rurp_log_id(id, NULL, 0)
- LOG_ID_U8(id, p1)                → 1-byte param
- LOG_ID_U16(id, p1)               → MSB-first 2 bytes
- LOG_ID_U24(id, p1)               → MSB-first 3 bytes (top byte of u32 dropped)
- LOG_ID_U32(id, p1)               → MSB-first 4 bytes
- LOG_ID_BYTES(id, buf, count)     → caller-supplied byte array

Existing INFO family (untouched; FLAG_VERBOSE-gated):
- LOG_INFO_ID, _U8, _U16, _U24, _U32, _BYTES → each wraps LOG_ID_* in `if (is_flag_set(FLAG_VERBOSE))`

ADDED in this plan (UNCONDITIONAL — NO FLAG_VERBOSE gate):
- LOG_ERROR_ID, _U8, _U16, _U24, _U32, _BYTES
- LOG_WARN_ID,  _U8, _U16, _U24, _U32, _BYTES

Each new macro is a one-line alias over the matching LOG_ID_* primitive — zero runtime cost vs raw.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LOG_ERROR_ID_* and LOG_WARN_ID_* macro families to logging_id.h</name>
  <files>firestarter/include/logging_id.h</files>
  <read_first>
    - firestarter/include/logging_id.h (the whole file — read top to bottom; both new families append at the bottom right before `#endif`)
    - firestarter/CLAUDE.md (firmware sub-repo conventions: build commands, native test invocation)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md (D-02 — symmetric macro surface, unconditional emit)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md §"Code Examples" — `LOG_ERROR_ID_*` / `LOG_WARN_ID_*` macro additions (logging_id.h)
  </read_first>
  <action>
    Open `firestarter/include/logging_id.h`. Locate the `#endif  // __LOGGING_ID_H__` line at the very bottom of the file (current line 121). IMMEDIATELY BEFORE that `#endif`, INSERT the following two macro families (12 macros total).

    Family 1 — `LOG_ERROR_ID_*` (unconditional, no FLAG_VERBOSE gate). Each macro is a one-line alias over the matching `LOG_ID_*` primitive:

    - `LOG_ERROR_ID(id)            LOG_ID(id)`
    - `LOG_ERROR_ID_U8(id, p1)     LOG_ID_U8((id), (p1))`
    - `LOG_ERROR_ID_U16(id, p1)    LOG_ID_U16((id), (p1))`
    - `LOG_ERROR_ID_U24(id, p1)    LOG_ID_U24((id), (p1))`
    - `LOG_ERROR_ID_U32(id, p1)    LOG_ID_U32((id), (p1))`
    - `LOG_ERROR_ID_BYTES(id, b, n) LOG_ID_BYTES((id), (b), (n))`

    Family 2 — `LOG_WARN_ID_*` (same shape, also unconditional):

    - `LOG_WARN_ID(id)             LOG_ID(id)`
    - `LOG_WARN_ID_U8(id, p1)      LOG_ID_U8((id), (p1))`
    - `LOG_WARN_ID_U16(id, p1)     LOG_ID_U16((id), (p1))`
    - `LOG_WARN_ID_U24(id, p1)     LOG_ID_U24((id), (p1))`
    - `LOG_WARN_ID_U32(id, p1)     LOG_ID_U32((id), (p1))`
    - `LOG_WARN_ID_BYTES(id, b, n) LOG_ID_BYTES((id), (b), (n))`

    Style: match the existing file (single-line `#define name expansion` form is fine; the `do { } while (0)` wrappers used by `LOG_INFO_ID_*` are NOT required for these aliases because they expand to a single statement). Group with a `// --- Unconditional ERROR severity ---` and `// --- Unconditional WARN severity ---` section comment, mirroring the existing `// --- Unconditional ID-frame emit ---` / `// --- FLAG_VERBOSE-gated variants (INFO severity equivalent) ---` style.

    CRITICAL: do NOT wrap any of the new macros in `if (is_flag_set(FLAG_VERBOSE))` — these are unconditional per D-02. Also do NOT alter any existing macro definition or include directive in the file.

    Build with a clean cache after editing (PlatformIO sometimes misses header-only changes):
    `cd firestarter && pio run -e uno --target clean && pio run -e uno && pio run -e leonardo --target clean && pio run -e leonardo`

    Commit after build + tests pass: `feat(logging_id.h): add LOG_ERROR_ID_* and LOG_WARN_ID_* families` from within `firestarter/`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio run -e uno --target clean &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo --target clean &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native -f "*test_messages*"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#define LOG_ERROR_ID' firestarter/include/logging_id.h` returns at least 6.
    - `grep -c '^#define LOG_WARN_ID' firestarter/include/logging_id.h` returns at least 6.
    - `grep -c 'FLAG_VERBOSE' firestarter/include/logging_id.h` is unchanged from baseline (i.e. the existing INFO-family count; ERROR + WARN families MUST NOT introduce additional FLAG_VERBOSE references).
    - `cd firestarter && pio run -e uno` exits 0.
    - `cd firestarter && pio run -e leonardo` exits 0.
    - `cd firestarter && pio test -e native -f "*test_messages*"` exits 0 (existing wire-frame suite still passes — proves the Phase 6 emit path is untouched).
    - Commit landed in `firestarter/` sub-repo with subject `feat(logging_id.h): add LOG_ERROR_ID_* and LOG_WARN_ID_* families`.
  </acceptance_criteria>
  <done>
    Both new families are present in `logging_id.h`. Both boards still compile. Existing `test_messages` suite still passes. No existing macro was modified.
  </done>
</task>

</tasks>

<verification>
- `grep -c '#define LOG_ERROR_ID' firestarter/include/logging_id.h` ≥ 6
- `grep -c '#define LOG_WARN_ID'  firestarter/include/logging_id.h` ≥ 6
- `cd firestarter && pio run -e uno && pio run -e leonardo && pio test -e native -f "*test_messages*"` all exit 0
</verification>

<success_criteria>
- `logging_id.h` exposes 12 new macros (`LOG_ERROR_ID_*` × 6 + `LOG_WARN_ID_*` × 6), each a one-line alias over the corresponding `LOG_ID_*` primitive, all unconditional.
- Native `test_messages` suite still passes — proves the Phase 6 emit path is untouched.
- Both AVR builds (Uno + Leonardo) succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-01-SUMMARY.md`.
</output>
