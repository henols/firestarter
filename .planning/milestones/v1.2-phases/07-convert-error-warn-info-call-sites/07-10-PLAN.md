---
phase: 07-convert-error-warn-info-call-sites
plan: 10
type: execute
wave: 3
depends_on: [01]
files_modified:
  - firestarter/src/firestarter.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "All 20 active direct call-sites in firestarter.cpp use LOG_*_ID_* macros"
    - "firestarter.cpp:86 dead-code if-block (log_error(handle->response_msg) inside RESPONSE_CODE_ERROR check after json_parse) is deleted including the surrounding `if` guard"
    - "firestarter.cpp:171 hybrid log_error_format_buf converts to LOG_ERROR_ID_U8(MSG_ERR_CMD_TIMEOUT, handle.cmd) with no buffer touch (RESEARCH section 5)"
    - "firestarter.cpp:238 log_error_P_int_buf converts to LOG_ERROR_ID_U8(MSG_ERR_UNKNOWN_CMD, handle.cmd) per D-03 catalog format normalization"
    - "EXTRA_INFO_LOGGING-guarded INFO sites are converted (grep SC#1 requires it even though they don't compile into production)"
    - "DEV_TOOLS + EXTRA_INFO_LOGGING double-guarded OE/CE sites are converted (same reason — SC#1 grep does not preprocess)"
    - "Both AVR builds still link cleanly; native dispatch tests still pass"
  artifacts:
    - path: "firestarter/src/firestarter.cpp"
      provides: "20 converted direct call-sites + 1 dead-code deletion"
      contains: "LOG_ERROR_ID_U8(MSG_ERR_CMD_TIMEOUT"
---

<objective>
Convert all 20 active direct log call-sites in `firestarter/src/firestarter.cpp` (the loop dispatcher + setup/config/command path) to the new `LOG_*_ID_*` macros. Also delete the dead-code `if (handle->response_code == RESPONSE_CODE_ERROR)` block at line 86 (RESEARCH section 1 + Pitfall 3 + Assumption A1 — `json_parse` never sets `response_code = ERROR`). Also convert the line 176 hybrid `log_error_format_buf(handle.response_msg, ...)` per RESEARCH section 5 to a direct emit with no buffer touch.

This is the largest single-file batch in Phase 7 (21 call-sites total). All edits land in one commit because they share the same file and the dead-code deletion is interleaved with the conversions. **Planner decision (Plan 07 revision, W3):** The 21 ops are intentionally kept in a single task — splitting unguarded vs guarded sites into two tasks would add a second commit boundary + a second full build/test verify run without changing the executor's actual work (still 21 ops on the same file). The SC#1 grep gate in Plan 13 is the safety net for any missed call-site. The explicit "DO NOT SKIP GUARDED BLOCKS" callout below + per-replacement guard annotations are sufficient mitigation.

Purpose: Wave-3 direct-log wave; covers the firmware entry-loop + command dispatcher + config parsing call-sites.
Output: 20 sites converted, 1 dead-code if-block deleted, one `feat(firestarter.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
Macros from Plan 01: LOG_INFO_ID (FLAG_VERBOSE gated), LOG_INFO_ID_U8/U16/U32 (FLAG_VERBOSE gated), LOG_ERROR_ID, LOG_ERROR_ID_U8 (unconditional).

Catalog IDs (Phase 6):
- MSG_INFO_BUF_VAL         0x44   params: [u8 hex_byte]
- MSG_ERR_BAD_JSON         0xA0   params: []
- MSG_INFO_TOKEN_COUNT     0x45   params: [i16]
- MSG_ERR_NO_CMD           0xA1   params: []
- MSG_INFO_FLAG_FORCE      0x46   params: [u8]
- MSG_INFO_FLAG_CAN_ERASE  0x47   params: [u8]
- MSG_INFO_FLAG_SKIP_ERASE 0x48   params: [u8]
- MSG_INFO_FLAG_SKIP_BLANK 0x49   params: [u8]
- MSG_INFO_FLAG_VPE_AS_VPP 0x4A   params: [u8]
- MSG_ERR_SETUP            0xA2   params: []
- MSG_INFO_FLAG_OUTPUT_EN  0x4B   params: [u8]
- MSG_INFO_FLAG_CHIP_EN    0x4C   params: [u8]
- MSG_ERR_PARSE_CFG        0xA3   params: []
- MSG_INFO_BUFFER_SIZE     0x4D   params: [u16]
- MSG_ERR_EMPTY_INPUT      0xA4   params: []
- MSG_INFO_MEM_SIZE        0x4E   params: [u32]
- MSG_INFO_ADDR_MASK       0x4F   params: [u32]
- MSG_INFO_MATCH_LINES     0x50   params: [u16]
- MSG_ERR_CMD_TIMEOUT      0xAA   params: [u8]
- MSG_ERR_UNKNOWN_CMD      0xAB   params: [u8]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert firestarter.cpp call-sites + delete line 86 dead-code + convert line 176 hybrid</name>
  <files>firestarter/src/firestarter.cpp</files>
  <read_first>
    - firestarter/src/firestarter.cpp lines 60-250 (every site listed in 07-RESEARCH.md section 1 table — lines 69, 70, 76, 78, 84-88 (dead-code block), 93-97 (EXTRA_INFO_LOGGING guarded), 100, 106-107 (DEV_TOOLS + EXTRA_INFO_LOGGING), 115, 131, 134, 146-148, 176, 243)
    - firestarter/include/logging_id.h (new macros from Plan 01)
    - firestarter/src/json_parser.c (CONFIRM the dead-code assumption: `grep response_code firestarter/src/json_parser.c` returns zero assignments — if NON-zero, the line 86 deletion is wrong)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/firestarter.cpp" (the 21-row table) + section 5 "The firestarter.cpp:171 Hybrid" + section 10 Pitfall 3 (dead-code at line 86)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md "Claude's Discretion" → firestarter.cpp:171 conversion target
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    !!! CRITICAL — DO NOT SKIP GUARDED BLOCKS !!!

    This task converts 21 operations: 15 active (unguarded) + 5 EXTRA_INFO_LOGGING-guarded + 2 DEV_TOOLS+EXTRA_INFO_LOGGING double-guarded + 1 dead-code deletion. The guarded blocks (Replacements E and G below) DO NOT compile into production firmware because `-D EXTRA_INFO_LOGGING` is commented out in `platformio.ini` — BUT they still need to be converted because:

    1. SC#1 grep gate (Plan 07-13) runs against source text, NOT preprocessed output. A `log_info_format(...)` left inside `#ifdef EXTRA_INFO_LOGGING` will fail the grep gate and block phase close.
    2. RESEARCH section 1 "Important nuance" explicitly enumerates these guarded sites in the 21-row table.

    Do NOT skip Replacement E (lines 93-97 — 5 EXTRA_INFO_LOGGING-guarded sites) or Replacement G (lines 106-107 — 2 DEV_TOOLS+EXTRA_INFO_LOGGING double-guarded sites). If you do, the SC#1 grep gate will fail and you'll be back to convert them anyway. Keep the `#ifdef` guards intact around the converted macros — only the macro call inside the guard changes.

    PRE-FLIGHT — verify the line 86 dead-code assumption holds:

    Run: `grep -n 'response_code' firestarter/src/json_parser.c`

    Expected: zero hits (json_parse never sets `handle->response_code` to anything — the json_parse routines return error via the return value, not via the handle field).

    If the grep returns ANY hit that ASSIGNS `response_code` (i.e. `handle->response_code = ...` somewhere in `json_parser.c`), STOP. Do NOT delete the line 86 block — instead, convert the `log_error(handle->response_msg)` at line 86 to the appropriate `LOG_ERROR_ID_*` macro and surface the finding so RESEARCH Assumption A1 can be updated.

    If the grep is zero (the expected case): proceed with the dead-code deletion in Replacement D below.

    Add `#include "logging_id.h"` near the top of `firestarter.cpp` if not already present (it likely is; confirm during read).

    REPLACEMENTS — apply each, in increasing line order:

    Replacement A — line 69 (Buf val, INFO, u8 param, no guard):
    Current: `log_info_format("Buf val: 0x%02x", handle->data_buffer[0]);`
    Replace with: `LOG_INFO_ID_U8(MSG_INFO_BUF_VAL, handle->data_buffer[0]);`

    Replacement B — line 70 (Bad JSON, ERROR, 0 params):
    Current: `log_error_const("Bad JSON");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_BAD_JSON);`

    Replacement C — line 76 (Token count, INFO, i16 param):
    Current: `log_info_format("Token count: %d", token_count);`
    Replace with: `LOG_INFO_ID_U16(MSG_INFO_TOKEN_COUNT, (uint16_t)token_count);` (signed token_count is passed as u16 — catalog declares i16 shape and host handles sign on render).

    Replacement D — line 78 (No cmd, ERROR, 0 params):
    Current: `log_error_const("No cmd");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_NO_CMD);`

    DEAD-CODE DELETION — lines 84-88 (after json_parse, the `if (handle->response_code == RESPONSE_CODE_ERROR)` block):
    Per RESEARCH Pitfall 3, the exact code to delete is the entire `if` block:
    ```
    if (handle->response_code == RESPONSE_CODE_ERROR) {
        log_error(handle->response_msg);
        return false;
    }
    ```
    Delete ALL of: the `if` line, the `log_error(...)` line, the `return false;` line, AND the closing `}`. This is dead code because `json_parse()` never sets `response_code`. The `return false` in this block is NOT the state-machine abort — it's specifically the dead branch's return. The actual `return false` paths at lines 72, 79, 102, 116 (per RESEARCH Pitfall 3 clarification) are different and STAY.

    Replacement E — line 93-97 (EXTRA_INFO_LOGGING-guarded flag dumps, INFO, u8 params):
    These are inside `#ifdef EXTRA_INFO_LOGGING` so they don't compile into production, BUT SC#1 grep requires conversion (see CRITICAL callout above). Convert each:
    - Line 93 `log_info_format("Force: %d", is_flag_set(FLAG_FORCE))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_FORCE, is_flag_set(FLAG_FORCE));`
    - Line 94 `log_info_format("Can erase: %d", is_flag_set(FLAG_CAN_ERASE))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_CAN_ERASE, is_flag_set(FLAG_CAN_ERASE));`
    - Line 95 `log_info_format("Skip erase: %d", is_flag_set(FLAG_SKIP_ERASE))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_SKIP_ERASE, is_flag_set(FLAG_SKIP_ERASE));`
    - Line 96 `log_info_format("Skip blank check: %d", is_flag_set(FLAG_SKIP_BLANK_CHECK))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_SKIP_BLANK, is_flag_set(FLAG_SKIP_BLANK_CHECK));`
    - Line 97 `log_info_format("VPE as VPP: %d", is_flag_set(FLAG_VPE_AS_VPP))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_VPE_AS_VPP, is_flag_set(FLAG_VPE_AS_VPP));`

    Keep the `#ifdef EXTRA_INFO_LOGGING` guard around the block.

    Replacement F — line 100 (Setup error, ERROR, 0 params):
    Current: `log_error_const("Setup error");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_SETUP);`

    Replacement G — line 106-107 (DEV_TOOLS + EXTRA_INFO_LOGGING-guarded OE/CE dumps, INFO, u8 params):
    These are inside `#ifdef DEV_TOOLS` AND `#ifdef EXTRA_INFO_LOGGING` (double-guarded). Convert each — DO NOT SKIP (see CRITICAL callout above):
    - Line 106 `log_info_format("Output enable: %d", is_flag_set(FLAG_OUTPUT_ENABLE))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_OUTPUT_EN, is_flag_set(FLAG_OUTPUT_ENABLE));`
    - Line 107 `log_info_format("Chip enable: %d", is_flag_set(FLAG_CHIP_ENABLE))` → `LOG_INFO_ID_U8(MSG_INFO_FLAG_CHIP_EN, is_flag_set(FLAG_CHIP_ENABLE));`

    Keep both guards.

    Replacement H — line 115 (Failed parsing config, ERROR, 0 params):
    Current: `log_error_const("Failed parsing config");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_PARSE_CFG);`

    Replacement I — line 131 (Buffer size, INFO, u16, EXTRA_INFO_LOGGING-guarded):
    Current: `log_info_format("Buffer size: %d", handle->data_size);`
    Replace with: `LOG_INFO_ID_U16(MSG_INFO_BUFFER_SIZE, (uint16_t)handle->data_size);`

    Replacement J — line 134 (Empty input, ERROR, 0 params):
    Current: `log_error_const("Empty input");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_EMPTY_INPUT);`

    Replacement K — line 146 (Memory size, INFO, u32, EXTRA_INFO_LOGGING-guarded):
    Current: `log_info_format("Memory size 0x%lx", handle->mem_size);`
    Replace with: `LOG_INFO_ID_U32(MSG_INFO_MEM_SIZE, (uint32_t)handle->mem_size);`

    Replacement L — line 147 (Address mask, INFO, u32, EXTRA_INFO_LOGGING-guarded):
    Current: `log_info_format("Address mask 0x%lx", handle->bus_config.address_mask);`
    Replace with: `LOG_INFO_ID_U32(MSG_INFO_ADDR_MASK, (uint32_t)handle->bus_config.address_mask);`

    Replacement M — line 148 (Matching lines, INFO, u16, EXTRA_INFO_LOGGING-guarded):
    Current: `log_info_format("Matching lines %u", handle->bus_config.matching_lines);`
    Replace with: `LOG_INFO_ID_U16(MSG_INFO_MATCH_LINES, (uint16_t)handle->bus_config.matching_lines);`

    Replacement N — line 176 (HYBRID, RESEARCH section 5, ERROR, u8 param, NO buffer touch):
    Current: `log_error_format_buf(handle.response_msg, "Cmd: %d, timeout", handle.cmd);`
    Replace with: `LOG_ERROR_ID_U8(MSG_ERR_CMD_TIMEOUT, handle.cmd);`
    NOTE: Do NOT add a `handle.response_code = RESPONSE_CODE_ERROR;` line — `command_done()` immediately resets the handle and nothing downstream reads `response_code` on this path. RESEARCH section 5 confirms this.

    Replacement O — line 243 (Unknown cmd hybrid, ERROR, u8 param, D-03 catalog format adapt):
    Current: `log_error_P_int_buf(handle.response_msg, "Unknown cmd: ", handle.cmd);`
    Catalog format is `"Unknown cmd: %d"` (D-03 sub-case: call-site adapts to catalog). The new emit is one line, no buffer touch:
    Replace with: `LOG_ERROR_ID_U8(MSG_ERR_UNKNOWN_CMD, handle.cmd);`

    POST-EDIT VERIFICATION (before committing):
    1. `grep -nE "log_(info_const|info_format|warn|error_const|error_format|error_P|info_P|error\()" firestarter/src/firestarter.cpp` returns zero non-comment hits.
    2. The dead-code if-block (lines 84-88) is gone.
    3. The guarded INFO blocks (inside `#ifdef EXTRA_INFO_LOGGING` and `#ifdef DEV_TOOLS`) are CONVERTED — guards still present, but the macro calls inside are now `LOG_INFO_ID_U8(...)`, NOT `log_info_format(...)`.
    4. `cd firestarter && pio test -e native -f "*test_dispatch*"` passes.
    5. `cd firestarter && pio run -e uno && pio run -e leonardo` both succeed.

    Commit from `firestarter/`: `feat(firestarter.cpp): convert direct log_* calls to LOG_*_ID_* (incl. dead-code deletion at :86 and hybrid at :176)`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "log_(info_const|info_format|warn|error_const|error_format|error_P|info_P)" firestarter/src/firestarter.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits (covers active AND guarded blocks — grep does not preprocess).
    - `grep -c '^[[:space:]]*log_error(handle->response_msg)' firestarter/src/firestarter.cpp` returns 0 (dead-code deleted).
    - `grep -c 'MSG_ERR_CMD_TIMEOUT' firestarter/src/firestarter.cpp` >= 1 (line 176 hybrid converted).
    - `grep -c 'MSG_ERR_UNKNOWN_CMD' firestarter/src/firestarter.cpp` >= 1 (line 243 converted).
    - `grep -c 'MSG_ERR_BAD_JSON\|MSG_ERR_NO_CMD\|MSG_ERR_SETUP\|MSG_ERR_PARSE_CFG\|MSG_ERR_EMPTY_INPUT' firestarter/src/firestarter.cpp` >= 5.
    - `grep -c 'MSG_INFO_BUF_VAL\|MSG_INFO_TOKEN_COUNT\|MSG_INFO_FLAG_FORCE\|MSG_INFO_FLAG_CAN_ERASE\|MSG_INFO_FLAG_SKIP_ERASE\|MSG_INFO_FLAG_SKIP_BLANK\|MSG_INFO_FLAG_VPE_AS_VPP\|MSG_INFO_FLAG_OUTPUT_EN\|MSG_INFO_FLAG_CHIP_EN\|MSG_INFO_BUFFER_SIZE\|MSG_INFO_MEM_SIZE\|MSG_INFO_ADDR_MASK\|MSG_INFO_MATCH_LINES' firestarter/src/firestarter.cpp` >= 13 (all guarded + unguarded INFO sites converted).
    - `grep -c '#ifdef EXTRA_INFO_LOGGING' firestarter/src/firestarter.cpp` is unchanged from baseline (guards preserved — only macro calls inside were touched).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(firestarter.cpp): ...` landed.
  </acceptance_criteria>
  <done>
    All 20 active direct call-sites converted (including the 5+2 guarded sites — guards preserved, macros swapped); line 86 dead-code if-block deleted (assumption A1 reverified); line 176 hybrid converted with no buffer touch; line 243 hybrid converted per D-03 catalog format; both boards build; dispatch tests still pass.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment legacy macro references in firestarter.cpp.
- Dead-code if-block at line 86 is gone.
- Both line 176 + line 243 hybrids converted; no `handle.response_msg` write at line 176.
- All 7 guarded INFO sites converted (5 EXTRA_INFO_LOGGING + 2 DEV_TOOLS-double-guarded); guards still in place.
- Native dispatch tests green; both boards build.
</verification>

<success_criteria>
- 20 active sites converted, 1 dead-code block deleted, 2 hybrids handled (line 176 + line 243).
- json_parse dead-code assumption reverified before deletion.
- One commit `feat(firestarter.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-10-SUMMARY.md`.
</output>
</content>
</invoke>