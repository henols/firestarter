---
phase: 07-convert-error-warn-info-call-sites
plan: 07
type: execute
wave: 2
depends_on: [01]
files_modified:
  - firestarter/src/proms/memory.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "memory.cpp:116 dispatch fallthrough emits MSG_ERR_MEM_TYPE_UNSUPPORTED via LOG_ERROR_ID_U8(handle->mem_type)"
    - "memory.cpp:223 verify mismatch site emits MSG_ERR_VERIFY via LOG_ERROR_ID_BYTES with 5-byte stack array (u8+u8+u24)"
    - "memory.cpp:359 blank-check site emits MSG_ERR_NOT_BLANK via LOG_ERROR_ID_BYTES with 4-byte stack array (u24+u8)"
    - "Both AVR builds still link cleanly; native dispatch tests still pass (memory.cpp:116 IS THE DISPATCH FALLTHROUGH — the dispatch test suite directly verifies it returns RESPONSE_CODE_ERROR)"
  artifacts:
    - path: "firestarter/src/proms/memory.cpp"
      provides: "converted populate-sites for MEM_TYPE_UNSUPPORTED, VERIFY, NOT_BLANK"
      contains: "LOG_ERROR_ID_U8(MSG_ERR_MEM_TYPE_UNSUPPORTED"
  key_links:
    - from: "firestarter/src/proms/memory.cpp"
      to: "configure_memory dispatch fallthrough"
      via: "LOG_ERROR_ID_U8 populate + RESPONSE_CODE_ERROR (test_configure_memory.cpp verifies)"
      pattern: "MSG_ERR_MEM_TYPE_UNSUPPORTED"
---

<objective>
Convert all three populate-sites in `firestarter/src/proms/memory.cpp`: line 116 (dispatch fallthrough — unsupported mem_type), line 219 (verify mismatch), line 287 (blank-check failure). The line 116 site is EXERCISED DIRECTLY by `test/native/avr/test_dispatch/test_configure_memory.cpp` which asserts `handle->response_code == RESPONSE_CODE_ERROR` for unknown mem_types — the conversion must preserve that test pass.

Purpose: Wave-2 populate-site wave.
Output: All 3 populate-sites converted; one `feat(memory.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
New macros from Plan 01: LOG_ERROR_ID_U8, LOG_ERROR_ID_BYTES.

Catalog IDs (Phase 6):
- MSG_ERR_MEM_TYPE_UNSUPPORTED 0xAE   params: [u8 hex_byte]
- MSG_ERR_VERIFY               0xAF   params: [u8 hex_byte, u8 hex_byte, u24 hex_addr]
- MSG_ERR_NOT_BLANK            0xB0   params: [u24 hex_addr, u8 hex_byte]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert memory.cpp populate-sites (lines 116, 219, 287)</name>
  <files>firestarter/src/proms/memory.cpp</files>
  <read_first>
    - firestarter/src/proms/memory.cpp lines 100-300 (every populate-site listed in 07-RESEARCH.md section 1 plus surrounding dispatch context)
    - firestarter/include/logging_id.h (new ERROR macros from Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/memory.cpp" and section 10 Pitfall 5 (stack array as named local)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (verify the dispatch test references — the test asserts response_code == ERROR after configure_memory returns on unknown mem_type; the new code path MUST still satisfy this)
    - firestarter/CLAUDE.md "Protocol Dispatch" — note the dispatch chain ends at memory.cpp:116 which IS the configure_memory fallthrough
  </read_first>
  <action>
    Make the following surgical replacements in `firestarter/src/proms/memory.cpp`. Add `#include "logging_id.h"` if not already present.

    REPLACEMENT 1 — memory.cpp:116 (dispatch fallthrough, ERROR, u8 param):
    Current: `firestarter_error_response_format("Memory type 0x%02x not supported", handle->mem_type);`

    Catalog `MSG_ERR_MEM_TYPE_UNSUPPORTED` (0xAE) param shape is [u8 hex_byte]. Replace with:
    - `LOG_ERROR_ID_U8(MSG_ERR_MEM_TYPE_UNSUPPORTED, handle->mem_type);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    The dispatch test `test_configure_memory.cpp` calls `configure_memory(&handle)` and asserts `handle.response_code == RESPONSE_CODE_ERROR` after passing an unknown mem_type — the new two-line form still satisfies that contract because the second line still sets `response_code = ERROR`.

    REPLACEMENT 2 — memory.cpp:223 (verify byte mismatch, ERROR, 3 params u8+u8+u24):
    Current: `firestarter_error_response_format("0x%02x != 0x%02x at 0x%06x", expected, byte, handle->address + i);`

    Catalog `MSG_ERR_VERIFY` (0xAF) param shape is [u8 hex_byte, u8 hex_byte, u24 hex_addr] = 5 wire bytes. Build a 5-byte named local `uint8_t _b[5]` inside a `{ ... }` block:
    - byte 0 = (uint8_t)expected
    - byte 1 = (uint8_t)byte
    - bytes 2-4 = MSB-first pack of `(uint32_t)(handle->address + i)` — high byte of u24 to low byte: `((addr >> 16) & 0xFF)`, `((addr >> 8) & 0xFF)`, `(addr & 0xFF)`. Compute `addr = (uint32_t)(handle->address + i);` into a local first to avoid recomputing.

    Then:
    - `LOG_ERROR_ID_BYTES(MSG_ERR_VERIFY, _b, 5);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    REPLACEMENT 3 — memory.cpp:359 (blank-check failure, ERROR, 2 params u24+u8):
    Current: `firestarter_error_response_format("Not blank, at 0x%06x, v: 0x%02x", i, val);`

    Catalog `MSG_ERR_NOT_BLANK` (0xB0) param shape is [u24 hex_addr, u8 hex_byte] = 4 wire bytes. Build a 4-byte named local `uint8_t _b[4]` inside a `{ ... }` block:
    - bytes 0-2 = MSB-first pack of `(uint32_t)i` — `((i >> 16) & 0xFF)`, `((i >> 8) & 0xFF)`, `(i & 0xFF)`
    - byte 3 = (uint8_t)val

    Then:
    - `LOG_ERROR_ID_BYTES(MSG_ERR_NOT_BLANK, _b, 4);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    Commit from `firestarter/`: `feat(memory.cpp): convert populate-sites to LOG_ERROR_ID_*`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response" firestarter/src/proms/memory.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_MEM_TYPE_UNSUPPORTED' firestarter/src/proms/memory.cpp` >= 1.
    - `grep -c 'MSG_ERR_VERIFY' firestarter/src/proms/memory.cpp` >= 1.
    - `grep -c 'MSG_ERR_NOT_BLANK' firestarter/src/proms/memory.cpp` >= 1.
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0 (dispatch fallthrough still routes to response_code = ERROR).
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(memory.cpp): convert populate-sites to LOG_ERROR_ID_*` landed.
  </acceptance_criteria>
  <done>
    All 3 populate-sites converted; dispatch test still passes (response_code routing intact for unknown mem_type); both boards build.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment legacy macro references in memory.cpp.
- test_configure_memory dispatch suite still asserts ERROR fallthrough.
- Both boards build.
</verification>

<success_criteria>
- 3 populate-sites converted; the dispatch fallthrough at line 116 still drives response_code = ERROR.
- One commit `feat(memory.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-07-SUMMARY.md`.
</output>
