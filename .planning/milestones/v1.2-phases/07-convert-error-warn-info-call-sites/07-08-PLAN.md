---
phase: 07-convert-error-warn-info-call-sites
plan: 08
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - firestarter/src/proms/flash_type_3.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "flash_type_3.cpp:135 dynamic-severity chip-id site routes to MSG_WARN_CHIP_ID_MISMATCH / MSG_ERR_CHIP_ID_MISMATCH based on FLAG_FORCE"
    - "flash_type_3.cpp:87 'Skipping erase of memory' classification is confirmed and documented as OK-path Phase 8 territory (NOT converted in this phase)"
    - "Both AVR builds still link cleanly; native dispatch tests still pass"
  artifacts:
    - path: "firestarter/src/proms/flash_type_3.cpp"
      provides: "converted dynamic-severity chip-id populate-site at line 135"
      contains: "MSG_WARN_CHIP_ID_MISMATCH"
  key_links:
    - from: "firestarter/src/proms/flash_type_3.cpp"
      to: "MSG_ERR_CHIP_ID_MISMATCH (0xB9)"
      via: "LOG_ERROR_ID_BYTES populate (dynamic-severity branch)"
      pattern: "MSG_ERR_CHIP_ID_MISMATCH"
---

<objective>
Convert the dynamic-severity chip-ID populate-site at `firestarter/src/proms/flash_type_3.cpp:135` to use `LOG_WARN_ID_BYTES` / `LOG_ERROR_ID_BYTES` macros + `MSG_WARN_CHIP_ID_MISMATCH` / `MSG_ERR_CHIP_ID_MISMATCH` IDs.

ALSO: resolve the RESEARCH open question about `flash_type_3.cpp:87` (`copy_to_buffer(handle->response_msg, "Skipping erase of memory")`). RESEARCH section 1 + Assumptions A3 flag this as needing in-implementation confirmation. Per the read evidence in the planner's source inspection: line 87 is `copy_to_buffer(handle->response_msg, "Skipping erase of memory");` — an OK-path populate that does NOT set `response_code = ERROR/WARNING`. Therefore it is Phase 8 territory and STAYS UNCHANGED in this plan. The plan task explicitly verifies this classification.

Purpose: Wave-2 populate-site wave; addresses the only remaining RESEARCH open question.
Output: line 135 converted (one populate-site); line 87 documented as OK-path / Phase 8 in the commit message; one `feat(flash_type_3.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
New macros from Plan 01: LOG_WARN_ID_BYTES, LOG_ERROR_ID_BYTES.

Catalog IDs:
- MSG_WARN_CHIP_ID_MISMATCH 0x83   params: [u16, u16]
- MSG_ERR_CHIP_ID_MISMATCH  0xB9   params: [u16, u16]                 (Plan 02 Task 2)
- MSG_INFO_SKIPPING_ERASE_MEM 0x59 params: []                          (already in catalog; Phase 8 will convert the line 87 site)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert flash_type_3.cpp:135 dynamic-severity chip-id site + classify line 87 site</name>
  <files>firestarter/src/proms/flash_type_3.cpp</files>
  <read_first>
    - firestarter/src/proms/flash_type_3.cpp lines 60-150 (lines 86-87 for the `copy_to_buffer` classification + line 135 populate-site + surrounding FLAG_FORCE control flow)
    - firestarter/include/logging_id.h (new ERROR/WARN macros from Plan 01)
    - firestarter/include/messages.h (confirm MSG_ERR_CHIP_ID_MISMATCH 0xB9 present from Plan 02)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/flash_type_3.cpp" + "Note on flash_type_3.cpp" + Assumptions A3 (the open question about the line 87 site)
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    STEP A — Classify flash_type_3.cpp:87 ("Skipping erase of memory"):

    Read flash_type_3.cpp lines 80-95. Confirm the following structure:
    - Line 86: `debug("Skipping erase of memory");`
    - Line 87: `copy_to_buffer(handle->response_msg, "Skipping erase of memory");`
    - The surrounding code does NOT set `handle->response_code = RESPONSE_CODE_ERROR` or `RESPONSE_CODE_WARNING` — this is an OK-path populate that flows through `_check_response`'s `RESPONSE_CODE_OK` branch and gets emitted as text via `log_info(handle->response_msg)`. OK-path is Phase 8 territory.

    If the read confirms this: leave line 87 UNCHANGED, and add a comment on the line above it (or annotate in the commit message) noting "Phase 8 will convert this OK-path site to MSG_INFO_SKIPPING_ERASE_MEM (0x59)". DO NOT convert it in this plan.

    If the read DOES NOT confirm this (i.e. the site DOES set response_code = ERROR or WARNING): STOP and surface to the orchestrator. The site would then need conversion in this plan instead, and the RESEARCH Assumptions A3 was wrong.

    STEP B — Convert flash_type_3.cpp:135 (dynamic-severity chip-id mismatch):

    Current code (approx): `firestarter_response_format(response_code, "Chip ID %#04x dont match expected ID %#04x", chip_id, handle->chip_id);`

    Format matches catalog (no D-03 drift). Build a 4-byte stack array `uint8_t _b[4]` inside a `{ ... }` block, packing `(uint16_t)chip_id` into bytes 0-1 (MSB-first) and `(uint16_t)handle->chip_id` into bytes 2-3 (MSB-first). Branch on FLAG_FORCE per RESEARCH Pitfall 1:
    ```
    if (is_flag_set(FLAG_FORCE)) {
        LOG_WARN_ID_BYTES(MSG_WARN_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_WARNING;
    } else {
        LOG_ERROR_ID_BYTES(MSG_ERR_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_ERROR;
    }
    ```
    Delete the now-unused `int response_code = ...;` local if any. Keep BOTH branches.

    Add `#include "logging_id.h"` if not already present.

    Commit message: `feat(flash_type_3.cpp): convert chip-id populate-site to LOG_*_ID_BYTES`. Include the classification of the line 87 site in the commit body: "`flash_type_3.cpp:87` Skipping-erase site confirmed OK-path; deferred to Phase 8 (MSG_INFO_SKIPPING_ERASE_MEM 0x59)." Commit from `firestarter/`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response|firestarter_response_format" firestarter/src/proms/flash_type_3.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_WARN_CHIP_ID_MISMATCH' firestarter/src/proms/flash_type_3.cpp` >= 1.
    - `grep -c 'MSG_ERR_CHIP_ID_MISMATCH' firestarter/src/proms/flash_type_3.cpp` >= 1.
    - `grep -c 'copy_to_buffer(handle->response_msg, "Skipping erase of memory")' firestarter/src/proms/flash_type_3.cpp` == 1 (line 87 preserved unchanged).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(flash_type_3.cpp): convert chip-id populate-site to LOG_*_ID_BYTES` landed, body documents line 87 classification.
  </acceptance_criteria>
  <done>
    flash_type_3.cpp:135 converted with FLAG_FORCE branching; line 87 confirmed OK-path and deferred to Phase 8; builds + dispatch tests green.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment legacy macro references in flash_type_3.cpp.
- Line 87 OK-path site preserved unchanged.
- Both boards build; dispatch tests pass.
</verification>

<success_criteria>
- Line 135 dynamic-severity site converted; both FLAG_FORCE branches present.
- Line 87 classification confirmed and documented in commit message.
- One commit `feat(flash_type_3.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-08-SUMMARY.md`.
</output>
