---
phase: 07-convert-error-warn-info-call-sites
plan: 12
type: execute
wave: 3
depends_on: [01]
files_modified:
  - firestarter/src/eprom_operations.cpp
  - firestarter/src/hardware_operations.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "eprom_operations.cpp's 3 log_error_const sites emit via LOG_ERROR_ID(MSG_ERR_*)"
    - "hardware_operations.cpp's 2 log_error_const sites emit via LOG_ERROR_ID(MSG_ERR_*)"
    - "HARDWARE_REVISION-guarded site stays guarded; conversion does not affect the guard"
    - "Both AVR builds still link cleanly"
  artifacts:
    - path: "firestarter/src/eprom_operations.cpp"
      provides: "3 converted ERROR call-sites"
      contains: "LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED"
    - path: "firestarter/src/hardware_operations.cpp"
      provides: "2 converted ERROR call-sites"
      contains: "LOG_ERROR_ID(MSG_ERR_CMD"
---

<objective>
Convert the 5 trivial `log_error_const` call-sites across two files: 3 in `firestarter/src/eprom_operations.cpp` (lines 40, 49, 95) and 2 in `firestarter/src/hardware_operations.cpp` (lines 20, 33). Each site is a single-line replacement to `LOG_ERROR_ID(MSG_ERR_*)`. RESEARCH section 8 (commit cadence) explicitly justifies collapsing these into one plan because each is a one-liner with no complexity, but we still commit per file for bisectability.

Purpose: Wave-3 direct-log wave — final cleanup of the easy single-line sites.
Output: 5 sites converted; 2 sub-repo commits (one per file).
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
Macros from Plan 01: LOG_ERROR_ID (zero-param, unconditional).

Catalog IDs (Phase 6):
- MSG_ERR_NOT_SUPPORTED  0xA5   params: []
- MSG_ERR_NO_CHIP_ID     0xA6   params: []
- MSG_ERR_OUT_OF_RANGE   0xA7   params: []
- MSG_ERR_REV0_VPP_RD    0xAC   params: []
- MSG_ERR_CMD            0xAD   params: []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert eprom_operations.cpp 3 ERROR sites (lines 40, 49, 95)</name>
  <files>firestarter/src/eprom_operations.cpp</files>
  <read_first>
    - firestarter/src/eprom_operations.cpp lines 30-100 (the 3 call-sites and surrounding control flow)
    - firestarter/include/logging_id.h (LOG_ERROR_ID from Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/eprom_operations.cpp"
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    Make three single-line replacements in `firestarter/src/eprom_operations.cpp`. Add `#include "logging_id.h"` if not already present.

    Replacement 1 — eprom_operations.cpp:41:
    Current: `log_error_const("Not supported");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED);`

    Replacement 2 — eprom_operations.cpp:50:
    Current: `log_error_const("No chip ID");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_NO_CHIP_ID);`

    Replacement 3 — eprom_operations.cpp:91:
    Current: `log_error_const("Out of range");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_OUT_OF_RANGE);`

    Commit from `firestarter/`: `feat(eprom_operations.cpp): convert log_error_const sites to LOG_ERROR_ID`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "log_error_const" firestarter/src/eprom_operations.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_NOT_SUPPORTED\|MSG_ERR_NO_CHIP_ID\|MSG_ERR_OUT_OF_RANGE' firestarter/src/eprom_operations.cpp` >= 3.
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - `cd firestarter && pio test -e native` exits 0.
    - Commit `feat(eprom_operations.cpp): convert log_error_const sites to LOG_ERROR_ID` landed.
  </acceptance_criteria>
  <done>
    3 ERROR sites in eprom_operations.cpp converted; both boards build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert hardware_operations.cpp 2 ERROR sites (lines 20, 33)</name>
  <files>firestarter/src/hardware_operations.cpp</files>
  <read_first>
    - firestarter/src/hardware_operations.cpp lines 10-40 (the 2 call-sites + the `#ifdef HARDWARE_REVISION` guard around line 20 — confirm guard is preserved)
    - firestarter/include/logging_id.h (LOG_ERROR_ID from Plan 01)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/hardware_operations.cpp"
    - firestarter/CLAUDE.md
  </read_first>
  <action>
    Make two single-line replacements in `firestarter/src/hardware_operations.cpp`. Add `#include "logging_id.h"` if not already present.

    Replacement 1 — hardware_operations.cpp:20 (inside `#ifdef HARDWARE_REVISION` guard — preserve guard):
    Current: `log_error_const("Rev0 dont support reading VPP/VPE");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_REV0_VPP_RD);`

    Replacement 2 — hardware_operations.cpp:33:
    Current: `log_error_const("Error cmd");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_CMD);`

    Commit from `firestarter/`: `feat(hardware_operations.cpp): convert log_error_const sites to LOG_ERROR_ID`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo &amp;&amp; pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "log_error_const" firestarter/src/hardware_operations.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_REV0_VPP_RD\|MSG_ERR_CMD' firestarter/src/hardware_operations.cpp` >= 2.
    - `grep -c '#ifdef HARDWARE_REVISION' firestarter/src/hardware_operations.cpp` unchanged from baseline (guard preserved).
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - `cd firestarter && pio test -e native` exits 0.
    - Commit `feat(hardware_operations.cpp): convert log_error_const sites to LOG_ERROR_ID` landed.
  </acceptance_criteria>
  <done>
    2 ERROR sites in hardware_operations.cpp converted; HARDWARE_REVISION guard preserved; both boards build.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment `log_error_const` references in either file.
- HARDWARE_REVISION guard intact.
- Both boards build; native suite green.
</verification>

<success_criteria>
- 5 ERROR sites converted via 2 separate sub-repo commits (one per file).
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-12-SUMMARY.md`.
</output>
