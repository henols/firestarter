---
phase: 07-convert-error-warn-info-call-sites
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - firestarter/src/proms/eprom.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "Every active firestarter_*_response_format / firestarter_*_response site in eprom.cpp emits via LOG_ERROR_ID_* / LOG_WARN_ID_*"
    - "Each converted site sets handle->response_code = RESPONSE_CODE_ERROR/WARNING immediately after the emit"
    - "Dynamic-severity sites (FLAG_FORCE toggles WARN vs ERROR) keep both branches"
    - "OK-path copy_to_buffer sites (eprom.cpp:103 Skipping erase, eprom.cpp:168-170 Number of retries) remain untouched (Phase 8 territory)"
    - "MSG_ERR_WRITE_FAILED (0xB1) emits 6 wire bytes total — params [u24 hex_addr, u8 retries, u16 mismatch] packed MSB-first into a named local uint8_t _b[6] (NOT 5 bytes — see RESEARCH §3 self-correction)"
    - "Both AVR builds still link cleanly; native dispatch tests still pass"
  artifacts:
    - path: "firestarter/src/proms/eprom.cpp"
      provides: "converted populate-sites for WRITE_FAILED (6 wire bytes), REV0_VPP_UNSUPPORTED, VPP_HIGH, VPP_LOW, CHIP_ID_MISMATCH"
      contains: "LOG_ERROR_ID_BYTES(MSG_ERR_WRITE_FAILED, _b, 6)"
  key_links:
    - from: "firestarter/src/proms/eprom.cpp"
      to: "MSG_ERR_VPP_HIGH (0xB8)"
      via: "LOG_ERROR_ID_BYTES populate"
      pattern: "MSG_ERR_VPP_HIGH"
    - from: "firestarter/src/proms/eprom.cpp"
      to: "MSG_ERR_CHIP_ID_MISMATCH (0xB9)"
      via: "LOG_ERROR_ID_U16 populate (dynamic-severity branch)"
      pattern: "MSG_ERR_CHIP_ID_MISMATCH"
---

<objective>
Convert every active ERROR + WARN populate-site in `firestarter/src/proms/eprom.cpp` to use the new `LOG_ERROR_ID_*` / `LOG_WARN_ID_*` macros from Plan 01 + the catalog IDs from Plan 02. Each converted site keeps its `handle->response_code = RESPONSE_CODE_ERROR/WARNING` assignment so the `_check_response` state machine still drives operation-flow abort.

Per `07-RESEARCH.md` section 1, eprom.cpp has 5 populate-sites to convert (lines 182, 203, 223-225, 227-229, 264). The OK-path `copy_to_buffer` sites at lines 103 ("Skipping erase.") and 168-170 ("Number of retries: %d") are EXPLICITLY OUT OF SCOPE — Phase 8 territory.

Purpose: Wave-2 populate-site wave (per LMIG-02 "one PROM module at a time").
Output: All ERROR/WARN populate-sites in `eprom.cpp` route through `rurp_log_id`; native `test_dispatch` regression suite still green; both boards still build; one `feat(eprom.cpp):` commit in `firestarter/` sub-repo.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-VALIDATION.md

<interfaces>
New macros available from Plan 01 (firestarter/include/logging_id.h):
- LOG_ERROR_ID(id), _U8, _U16, _U24, _U32, _BYTES (unconditional)
- LOG_WARN_ID(id),  _U8, _U16, _U24, _U32, _BYTES (unconditional)

Catalog IDs already allocated (Phase 6 or this phase's Plan 02):
- MSG_ERR_WRITE_FAILED          0xB1   params: [u24 hex_addr, u8, u16]   (6 wire bytes total — RESEARCH §3 self-correction)
- MSG_WARN_REV0_VPP_UNSUPPORTED 0x80   params: []
- MSG_WARN_VPP_HIGH             0x82   params: [u16, u16, u16, u16]
- MSG_ERR_VPP_HIGH              0xB8   params: [u16, u16, u16, u16]   (Plan 02 Task 1)
- MSG_WARN_VPP_LOW              0x81   params: [u16, u16, u16, u16]
- MSG_WARN_CHIP_ID_MISMATCH     0x83   params: [u16, u16]
- MSG_ERR_CHIP_ID_MISMATCH      0xB9   params: [u16, u16]             (Plan 02 Task 2)

Existing pattern at each populate-site:
- `firestarter_error_response_format(...)` / `firestarter_warning_response_format(...)` / `firestarter_response_format(response_code, ...)`
  do BOTH: format into handle->response_msg AND set handle->response_code.
New pattern (two adjacent lines):
- `LOG_{ERROR|WARN}_ID_*(MSG_*, params);`
- `handle->response_code = RESPONSE_CODE_{ERROR|WARNING};`

For dynamic-severity sites (eprom.cpp:223-225 + 264), the pattern is:
```
if (is_flag_set(FLAG_FORCE)) {
    LOG_WARN_ID_*(MSG_WARN_*, params);
    handle->response_code = RESPONSE_CODE_WARNING;
} else {
    LOG_ERROR_ID_*(MSG_ERR_*, params);
    handle->response_code = RESPONSE_CODE_ERROR;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert eprom.cpp populate-sites (lines 182, 203, 223-225, 227-229, 264)</name>
  <files>firestarter/src/proms/eprom.cpp</files>
  <read_first>
    - firestarter/src/proms/eprom.cpp lines 100-280 (every populate-site listed in 07-RESEARCH.md section 1 plus the surrounding control flow so the FLAG_FORCE branches are visible)
    - firestarter/include/logging_id.h (new LOG_ERROR_ID_* / LOG_WARN_ID_* macros from Plan 01)
    - firestarter/include/messages.h (confirm MSG_ERR_VPP_HIGH 0xB8 + MSG_ERR_CHIP_ID_MISMATCH 0xB9 are present from Plan 02)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/proms/eprom.cpp" (5 populate-sites + 1 format-string drift note) and section 3 "Multi-Param Composer Recommendation" (stack-array patterns for 8-byte and 6-byte arrays — explicitly NOT 5 bytes for WRITE_FAILED)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 10 "Pitfall 1: Dynamic-Severity Site Conversion Error" and section 10 "Pitfall 6: eprom.cpp Retries Format-String in Wrong Direction" (OK-path sites at lines 103 + 168-170 STAY)
    - firestarter/CLAUDE.md (build commands, native test invocation)
  </read_first>
  <action>
    Make the following surgical replacements in `firestarter/src/proms/eprom.cpp`. After all edits, the file must contain ZERO `firestarter_error_response`, `firestarter_warning_response`, or `firestarter_response_format` references (commented or live). Use `#include "logging_id.h"` if not already included — read the file's top to confirm; if absent, add it next to the existing logging includes.

    REPLACEMENT 1 — eprom.cpp:182 (write retry exhausted, ERROR, 3 params: u24+u8+u16 = 6 wire bytes):

    Current code:
    `firestarter_error_response_format("Failed to write memory, 0x%06x, retries: %d, bad bytes: %d", handle->address, retries, mismatch);`

    Replace with a **6-byte** stack-array pattern targeting `MSG_ERR_WRITE_FAILED` (0xB1) plus an explicit `response_code` assignment. The catalog param shape is `[u24, u8, u16]` which is **6 wire bytes total (3 + 1 + 2)** — RESEARCH §3 explicitly corrects an earlier 5-byte miscount with a self-correction ("wait — that's 6 bytes") and the canonical pattern is 6. Pack MSB-first into a named local `uint8_t _b[6]` declared inside a `{ ... }` block (RESEARCH section 10 Pitfall 5 — no compound literal in macro argument):

    - `_b[0] = (uint8_t)((handle->address >> 16) & 0xFF);` (u24 MSB)
    - `_b[1] = (uint8_t)((handle->address >> 8)  & 0xFF);`
    - `_b[2] = (uint8_t)( handle->address        & 0xFF);` (u24 LSB)
    - `_b[3] = (uint8_t)retries;` (u8)
    - `_b[4] = (uint8_t)(((uint16_t)mismatch >> 8) & 0xFF);` (u16 MSB)
    - `_b[5] = (uint8_t)( (uint16_t)mismatch       & 0xFF);` (u16 LSB)

    Then emit:
    - `LOG_ERROR_ID_BYTES(MSG_ERR_WRITE_FAILED, _b, 6);`
    - `handle->response_code = RESPONSE_CODE_ERROR;`

    The array MUST be `uint8_t _b[6]` (six elements), NOT `_b[5]`. The `LOG_ERROR_ID_BYTES` length argument MUST be `6`, NOT `5`. Any executor producing a 5-element array or a length-5 call is bug-for-bug wrong — see the `acceptance_criteria` 6-byte grep gate.

    REPLACEMENT 2 — eprom.cpp:203 (Rev0 VPP unsupported, WARN, 0 params):

    Current code:
    `firestarter_warning_response("Rev0 dont support reading VPP/VPE");`

    Replace with two lines:
    - `LOG_WARN_ID(MSG_WARN_REV0_VPP_UNSUPPORTED);`
    - `handle->response_code = RESPONSE_CODE_WARNING;`

    REPLACEMENT 3 — eprom.cpp:223-225 (VPP high, DYNAMIC severity, 4 params u16x4):

    Current code (approx, verify against source):
    ```
    int response_code = is_flag_set(FLAG_FORCE) ? RESPONSE_CODE_WARNING : RESPONSE_CODE_ERROR;
    firestarter_response_format(response_code, "VPP is high: %u.%uV > %u.%uV",
        (vpp_mv + 50) / 1000, (((vpp_mv + 50) / 100) % 10),
        (handle->vpp_mv + 50) / 1000, (((handle->vpp_mv + 50) / 100) % 10));
    ```

    Replace with the FLAG_FORCE branching pattern from `07-RESEARCH.md` "Dynamic-severity site conversion (eprom.cpp VPP high)". Build an 8-byte stack array `_b[8]` packing the 4 u16 values MSB-first into bytes 0-7. Then:
    ```
    if (is_flag_set(FLAG_FORCE)) {
        LOG_WARN_ID_BYTES(MSG_WARN_VPP_HIGH, _b, 8);
        handle->response_code = RESPONSE_CODE_WARNING;
    } else {
        LOG_ERROR_ID_BYTES(MSG_ERR_VPP_HIGH, _b, 8);
        handle->response_code = RESPONSE_CODE_ERROR;
    }
    ```
    Delete the now-unused `int response_code = ...;` local. CRITICAL (RESEARCH Pitfall 1): keep BOTH branches — do not collapse to always-WARN.

    REPLACEMENT 4 — eprom.cpp:227-229 (VPP low, WARN-only, 4 params u16x4):

    Current code:
    ```
    firestarter_warning_response_format("VPP is low: %u.%uV < %u.%uV",
        (vpp_mv + 50) / 1000, (((vpp_mv + 50) / 100) % 10),
        (handle->vpp_mv + 50) / 1000, (((handle->vpp_mv + 50) / 100) % 10));
    ```

    Replace with the same 8-byte stack-array pack as Replacement 3 but ONLY the WARN branch (no FLAG_FORCE check — low VPP is always WARN per current code). Target `MSG_WARN_VPP_LOW` (0x81):
    ```
    LOG_WARN_ID_BYTES(MSG_WARN_VPP_LOW, _b, 8);
    handle->response_code = RESPONSE_CODE_WARNING;
    ```

    REPLACEMENT 5 — eprom.cpp:264 (chip ID mismatch, DYNAMIC severity, 2 params u16x2 + FORMAT DRIFT per D-03):

    Current code (per RESEARCH section 2 "FORMAT-STRING DRIFT"):
    `firestarter_response_format(error_code, "Chip ID: %#x dont match: %#x", chip_id, handle->chip_id);`

    Catalog format (canonical) is `"Chip ID %#04x dont match expected ID %#04x"` — call-site adapts per D-03. The arguments `chip_id` and `handle->chip_id` (both u16) remain in the same order; only the wire format changes — the host renders using the catalog string. Replace with:
    ```
    if (is_flag_set(FLAG_FORCE)) {
        LOG_WARN_ID_BYTES(MSG_WARN_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_WARNING;
    } else {
        LOG_ERROR_ID_BYTES(MSG_ERR_CHIP_ID_MISMATCH, _b, 4);
        handle->response_code = RESPONSE_CODE_ERROR;
    }
    ```
    Where `_b` is a 4-byte stack array packing `chip_id` then `handle->chip_id` MSB-first (2 bytes each = 4 bytes total). Alternative: use `LOG_*_ID_U16` twice in sequence is NOT acceptable because the catalog format expects both u16 values in one frame. The `_BYTES` form with a manually packed array is correct. Delete the now-unused `int error_code = ...;` local if it was set immediately before the call. CRITICAL: keep BOTH branches per RESEARCH Pitfall 1.

    DO NOT TOUCH (out of scope — Phase 8):
    - eprom.cpp:103 `copy_to_buffer(handle->response_msg, "Skipping erase.");` — OK-path
    - eprom.cpp:168-170 `format(handle->response_msg, "Number of retries: %d", retries);` — OK-path

    OUT OF THIS PLAN'S SCOPE (handled by Plan 07-05):
    - `firestarter/src/proms/flash_type_4.cpp:88` (MSG_ERR_FL4_VERIFY_TIMEOUT, 5-byte stack array u8+u24+u8). That is Plan 05's territory and MUST NOT appear in this plan's truths or acceptance criteria.

    After all 5 replacements: run native dispatch tests, then dual-board build, then commit from inside `firestarter/`: `feat(eprom.cpp): convert populate-sites to LOG_ERROR_ID_*/LOG_WARN_ID_*`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "firestarter_(error|warning)_response" firestarter/src/proms/eprom.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -nE "firestarter_response_format" firestarter/src/proms/eprom.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits.
    - `grep -c 'MSG_ERR_WRITE_FAILED\|MSG_WARN_REV0_VPP_UNSUPPORTED\|MSG_WARN_VPP_HIGH\|MSG_ERR_VPP_HIGH\|MSG_WARN_VPP_LOW\|MSG_WARN_CHIP_ID_MISMATCH\|MSG_ERR_CHIP_ID_MISMATCH' firestarter/src/proms/eprom.cpp` >= 7 (each MSG_* appears at least once).
    - `grep -E 'LOG_ERROR_ID_BYTES\(MSG_ERR_WRITE_FAILED, _b, 6\)' firestarter/src/proms/eprom.cpp` returns at least 1 hit (6-byte length verified — NOT 5).
    - `grep -E 'uint8_t[[:space:]]+_b\[5\]' firestarter/src/proms/eprom.cpp` returns zero hits (no 5-element array for WRITE_FAILED — RESEARCH §3 self-correction).
    - `grep -c 'copy_to_buffer(handle->response_msg' firestarter/src/proms/eprom.cpp` is unchanged from baseline (OK-path sites untouched).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0 (dispatch + response_code routing still works).
    - `cd firestarter && pio run -e uno` exits 0.
    - `cd firestarter && pio run -e leonardo` exits 0.
    - Commit `feat(eprom.cpp): convert populate-sites to LOG_ERROR_ID_*/LOG_WARN_ID_*` landed in `firestarter/` sub-repo.
  </acceptance_criteria>
  <done>
    All 5 ERROR/WARN populate-sites in eprom.cpp emit via rurp_log_id, response_code state machine intact, OK-path sites untouched, both boards still build, dispatch tests still pass. MSG_ERR_WRITE_FAILED frame is 6 wire bytes.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment `firestarter_(error|warning)_response` references in eprom.cpp.
- All 7 expected MSG_* symbols referenced in the converted sites.
- MSG_ERR_WRITE_FAILED uses `_b[6]` and `LOG_ERROR_ID_BYTES(..., _b, 6)` (6 wire bytes — explicitly NOT 5).
- Native dispatch tests green.
- Both boards build.
</verification>

<success_criteria>
- eprom.cpp's 5 populate-sites converted; OK-path sites preserved.
- response_code routing unchanged (verified by test_dispatch).
- One commit `feat(eprom.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-03-SUMMARY.md`.
</output>
</content>
</invoke>