---
phase: 07-convert-error-warn-info-call-sites
plan: 09
type: execute
wave: 3
depends_on: [01]
files_modified:
  - firestarter/src/operation_utils.cpp
autonomous: true
requirements:
  - LMIG-02
must_haves:
  truths:
    - "All 6 direct ERROR/INFO call-sites in operation_utils.cpp converted to LOG_ERROR_ID_* / LOG_INFO_ID_*"
    - "_check_response WARNING + ERROR case bodies drop their log_* calls; OK + DATA branches stay verbatim; ERROR case keeps `return false`"
    - "_check_response OK branch's `log_info(handle->response_msg)` call SURVIVES the edit (D-01: Phase 7 does NOT touch OK/DATA paths — that is Phase 8 territory)"
    - "_check_response DATA branch's `log_data(handle->response_msg)` call SURVIVES the edit (D-01: Phase 7 does NOT touch OK/DATA paths)"
    - "All ~14 commented-out `// log_*` breadcrumb lines deleted per D-04b"
    - "Native dispatch suite still passes: WARNING case falls through (no abort), ERROR case still aborts (return false)"
    - "Both AVR builds still link cleanly"
  artifacts:
    - path: "firestarter/src/operation_utils.cpp"
      provides: "6 converted direct-log sites; _check_response stripped of WARN+ERROR log calls; OK+DATA log calls preserved; breadcrumbs deleted"
      contains: "LOG_INFO_ID(MSG_INFO_MAIN_START"
  key_links:
    - from: "firestarter/src/operation_utils.cpp _check_response"
      to: "operation flow abort mechanism"
      via: "RESPONSE_CODE_ERROR case still returns false"
      pattern: "case RESPONSE_CODE_ERROR"
    - from: "firestarter/src/operation_utils.cpp _check_response OK branch"
      to: "Phase 8 OK-path emit (still text in Phase 7)"
      via: "log_info(handle->response_msg) call PRESERVED"
      pattern: "log_info\\(handle->response_msg\\)"
---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware→host (UART wire) | ID-frame encoded ERROR/WARN/INFO log messages cross this boundary; Phase 6 owns CRC + magic preamble + length-prefixed framing |
| host→firmware (UART command stream) | unchanged in Phase 7; this plan does not alter command parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | wire-frame integrity | accept (upstream) | Phase 6 owns CRC8 + magic preamble + length-prefixed framing; this plan emits via the same `rurp_log_id` path and inherits those guarantees verbatim |
| T-07-09-02 | Information Disclosure | response_msg buffer no longer carries ERROR/WARN text | accept | Buffer is internal to firmware; OK+DATA paths still populate it but Phase 7 stops the ERROR+WARN populate path. Phase 9 deletes the buffer; transitional state is benign. |
| T-07-09-03 | Denial of Service | operation flow abort (`return false`) on ERROR | mitigate | `_check_response` RESPONSE_CODE_ERROR branch MUST still `return false` post-edit. Verified by `test_dispatch` regression — failure surfaces immediately. |
| T-07-09-04 | Elevation of Privilege | no new untrusted input ingestion | n/a | Phase 7 is firmware-only emit-side; no new input parsing surface |
</threat_model>

<objective>
Convert the 6 direct-log call-sites in `firestarter/src/operation_utils.cpp` (lines 118, 171, 187, 218, 250, 252) AND perform the surgical edit on `_check_response` (lines 321-342) per D-01 AND delete the ~14 commented-out breadcrumb lines per D-04b. All three changes land in the same commit because they are interleaved within the same file and a half-commit would leave the file in an awkward state.

The dispatcher edit is the highest-risk operation in Phase 7: the WARNING + ERROR case bodies of `_check_response` drop their `log_warn` / `log_error` calls (those emit happened at the populate-site in Wave 2), but the `return false` in the ERROR case MUST stay (it is the operation-flow abort mechanism). **The OK + DATA branches stay completely verbatim** — both `log_info(handle->response_msg)` in the OK case AND `log_data(handle->response_msg)` in the DATA case MUST be preserved (Phase 8 territory per D-01). An over-eager executor deleting them would silently break the host's OK/DATA path in Phase 7 even though Phase 8 will delete them later.

Purpose: Wave-3 direct-log wave; the dispatcher edit makes Wave 2's populate-site conversions effective (it stops the OLD path from double-emitting).
Output: 6 direct-log sites converted, dispatcher stripped of WARN+ERROR log calls (OK+DATA preserved), 14 breadcrumb lines deleted, one `feat(operation_utils.cpp):` commit.
</objective>

<execution_context>
@/workspaces/firestarter_prom/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/firestarter_prom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md
@.planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md

<interfaces>
New macros from Plan 01: LOG_INFO_ID (FLAG_VERBOSE gated), LOG_ERROR_ID, LOG_ERROR_ID_U16 (unconditional).

Catalog IDs (Phase 6):
- MSG_ERR_TIMEOUT       0xA8   params: []
- MSG_ERR_DATA_ERR_N    0xA9   params: [i16]
- MSG_INFO_MAIN_START   0x40   params: []
- MSG_INFO_MAIN_DONE    0x41   params: []
- MSG_INFO_INIT_START   0x42   params: []
- MSG_INFO_END_START    0x43   params: []

_check_response unified-diff sketch (RESEARCH section 6 — apply verbatim):

```
case RESPONSE_CODE_OK:
    log_info(handle->response_msg);   // STAY — Phase 8 territory (D-01)
    break;
case RESPONSE_CODE_WARNING:
    break;                            // log_warn DELETED, fall through silent
case RESPONSE_CODE_DATA:
    log_data(handle->response_msg);   // STAY — Phase 8 territory (D-01)
    rurp_communication_write(handle->data_buffer, handle->data_size);
    break;
case RESPONSE_CODE_ERROR:
default:
    return false;                     // log_error DELETED; return false STAYS (abort mechanism)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert operation_utils.cpp direct-log sites + _check_response surgical edit + breadcrumb deletion</name>
  <files>firestarter/src/operation_utils.cpp</files>
  <read_first>
    - firestarter/src/operation_utils.cpp lines 1-50 (includes and file-level structure) + lines 70-260 (the 6 direct-log sites at 118, 171, 187, 218, 250, 252 plus the ~14 commented-out breadcrumb lines around 78, 80, 134, 139, 146, 178, 202, 204, 208, 211, 241, 243, 245) + lines 315-345 (the _check_response dispatcher at 321-342 plus the `// log_info_const("- OK -");` breadcrumb at 325)
    - firestarter/include/logging_id.h (LOG_INFO_ID, LOG_ERROR_ID, LOG_ERROR_ID_U16 + Plan 01 additions)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-CONTEXT.md (D-01 dispatcher edit; D-04b breadcrumb deletion)
    - .planning/phases/07-convert-error-warn-info-call-sites/07-RESEARCH.md section 1 "firestarter/src/operation_utils.cpp" + section 6 "_check_response Surgical Edit" (the exact unified-diff sketch — apply verbatim)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp (the test that exercises `_check_response` indirectly via configure_memory → response_code flow — must still pass)
    - firestarter/CLAUDE.md "Protocol Dispatch" (note that the dispatch tests verify response_code flow, which is exactly what this plan's surgical edit must preserve)
  </read_first>
  <action>
    Make the following interleaved edits in `firestarter/src/operation_utils.cpp`. Add `#include "logging_id.h"` near the top if not already included (it likely is; confirm during read).

    PART A — DIRECT-LOG CONVERSIONS (6 sites):

    Replacement A1 — operation_utils.cpp:124 (timeout, ERROR, 0 params):
    Current: `log_error_const("Timeout");`
    Replace with: `LOG_ERROR_ID(MSG_ERR_TIMEOUT);`

    Replacement A2 — operation_utils.cpp:179 (data err, ERROR, 1 param i16/int):
    Current: `log_error_P_int("Data err ", res);`
    Replace with: `LOG_ERROR_ID_U16(MSG_ERR_DATA_ERR_N, (uint16_t)res);` (signed `res` is passed as u16 — wire is raw bytes, host's i16 catalog render handles sign per `MSG_ERR_DATA_ERR_N` shape)

    Replacement A3 — operation_utils.cpp:195 (main done, INFO, 0 params):
    Current: `log_info_const("Main done");`
    Replace with: `LOG_INFO_ID(MSG_INFO_MAIN_DONE);` (FLAG_VERBOSE-gated per existing INFO family — that's intentional)

    Replacement A4 — operation_utils.cpp:226 (main start, INFO, 0 params):
    Current: `log_info_const("Main start");`
    Replace with: `LOG_INFO_ID(MSG_INFO_MAIN_START);`

    Replacement A5 — operation_utils.cpp:258 (init start, INFO, 0 params):
    Current: `log_info_const("Init start");`
    Replace with: `LOG_INFO_ID(MSG_INFO_INIT_START);`

    Replacement A6 — operation_utils.cpp:260 (end start, INFO, 0 params):
    Current: `log_info_const("End start");`
    Replace with: `LOG_INFO_ID(MSG_INFO_END_START);`

    PART B — _check_response SURGICAL EDIT (RESEARCH section 6, apply verbatim):

    The function `_check_response` (currently at lines 321-342) has four `switch` cases. Edit ONLY the WARNING and ERROR case bodies; leave OK and DATA verbatim.

    Before (current state):
    ```
    case RESPONSE_CODE_OK:
        log_info(handle->response_msg);
        // log_info_const("- OK -");
        break;
    case RESPONSE_CODE_WARNING:
        log_warn(handle->response_msg);
        break;
    case RESPONSE_CODE_DATA:
        log_data(handle->response_msg);
        rurp_communication_write(handle->data_buffer, handle->data_size);
        break;
    case RESPONSE_CODE_ERROR:
    default:
        log_error(handle->response_msg);
        return false;
    ```

    After (Phase 7 state):
    ```
    case RESPONSE_CODE_OK:
        log_info(handle->response_msg);
        break;
    case RESPONSE_CODE_WARNING:
        break;
    case RESPONSE_CODE_DATA:
        log_data(handle->response_msg);
        rurp_communication_write(handle->data_buffer, handle->data_size);
        break;
    case RESPONSE_CODE_ERROR:
    default:
        return false;
    ```

    Changes (exact):
    - DELETE the line `log_warn(handle->response_msg);` inside RESPONSE_CODE_WARNING (the `break` stays).
    - DELETE the line `log_error(handle->response_msg);` inside RESPONSE_CODE_ERROR (the `return false;` STAYS — abort mechanism).
    - DELETE the line `// log_info_const("- OK -");` inside RESPONSE_CODE_OK (this is a breadcrumb covered by Part C as well).
    - **DO NOT DELETE** `log_info(handle->response_msg);` from RESPONSE_CODE_OK — that call STAYS verbatim (D-01: Phase 7 does not touch OK path).
    - **DO NOT DELETE** `log_data(handle->response_msg);` from RESPONSE_CODE_DATA — that call STAYS verbatim (D-01: Phase 7 does not touch DATA path).
    - The OK + DATA case bodies (including `log_info(handle->response_msg)` and `log_data(handle->response_msg)`) remain VERBATIM. They are Phase 8 territory.

    PART C — BREADCRUMB DELETION (D-04b):

    Delete ALL commented-out `// log_*_const` / `// log_*_format` / `// log_info_const` etc. breadcrumb lines. Per RESEARCH section 1 these are approximately at lines 78, 80, 134, 139, 146, 178, 202, 204, 208, 211, 241, 243, 245, and 325 (the OK-branch breadcrumb already covered in Part B). The exact line numbers will shift as Parts A + B edits land; the safest approach is to perform Parts A + B first, then grep-and-delete the breadcrumbs in a single pass.

    Identify all breadcrumbs via:
    `grep -nE "^\s*//\s*log_(info|warn|error|data)" firestarter/src/operation_utils.cpp`

    Delete every matched line. Do NOT delete other comments — only the `// log_*` breadcrumbs that reference legacy log macros.

    FINAL VERIFICATION (run before committing):

    1. `grep -nE "^\s*//\s*log_" firestarter/src/operation_utils.cpp` should return zero hits.
    2. `grep -nE "^\s*log_(info_const|info_format|warn|error_const|error_format|error_P_int|info_P)" firestarter/src/operation_utils.cpp | grep -v '_check_response'` should return zero hits (the direct-log sites are converted; `_check_response` is excluded because its OK + DATA branches still call `log_info(...)` and `log_data(...)` for Phase 8 reasons — those are NOT `log_*_const` style, they take a `handle->response_msg` argument).
    3. `_check_response` ERROR case still ends in `return false;`.
    4. `_check_response` OK case still contains `log_info(handle->response_msg);` (D-01 preservation).
    5. `_check_response` DATA case still contains `log_data(handle->response_msg);` (D-01 preservation).
    6. Native dispatch suite passes — `cd firestarter && pio test -e native -f "*test_dispatch*"`.

    Commit from `firestarter/`: `feat(operation_utils.cpp): convert direct log_* calls, drop _check_response emit, delete breadcrumbs`.
  </action>
  <verify>
    <automated>cd firestarter &amp;&amp; pio test -e native -f "*test_dispatch*" &amp;&amp; pio run -e uno &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^\s*//\s*log_" firestarter/src/operation_utils.cpp` returns zero hits (no breadcrumbs left).
    - `grep -c 'log_info_const\|log_error_const\|log_error_P_int\|log_warn' firestarter/src/operation_utils.cpp | grep -v '^[^:]*:[[:space:]]*//'` returns zero non-comment hits for the converted macros (`log_info(...)` and `log_data(...)` calls inside `_check_response` are NOT in that list and stay).
    - `grep -c 'MSG_ERR_TIMEOUT\|MSG_ERR_DATA_ERR_N\|MSG_INFO_MAIN_START\|MSG_INFO_MAIN_DONE\|MSG_INFO_INIT_START\|MSG_INFO_END_START' firestarter/src/operation_utils.cpp` >= 6.
    - `grep -A2 'case RESPONSE_CODE_WARNING' firestarter/src/operation_utils.cpp | grep -v 'log_warn'` confirms WARN case body has only `break;` (no log_warn).
    - `grep -A3 'case RESPONSE_CODE_ERROR' firestarter/src/operation_utils.cpp | grep 'return false'` returns at least one hit (abort mechanism preserved).
    - `grep -A3 'case RESPONSE_CODE_ERROR' firestarter/src/operation_utils.cpp | grep -v '//' | grep 'log_error'` returns zero hits (log_error deleted).
    - **`grep -c 'log_info(handle->response_msg)' firestarter/src/operation_utils.cpp` returns at least 1** (OK-branch preservation verified — D-01 requirement that OK/DATA paths stay intact in Phase 7; protects against an over-eager executor deleting the OK-branch `log_info` along with the ERROR/WARN ones).
    - `grep -c 'log_data(handle->response_msg)' firestarter/src/operation_utils.cpp` returns at least 1 (DATA-branch preservation verified — D-01).
    - `cd firestarter && pio test -e native -f "*test_dispatch*"` exits 0 (response_code flow verified: WARN falls through, ERROR aborts, OK + DATA still log).
    - `cd firestarter && pio run -e uno && pio run -e leonardo` exits 0.
    - Commit `feat(operation_utils.cpp): convert direct log_* calls, drop _check_response emit, delete breadcrumbs` landed.
  </acceptance_criteria>
  <done>
    All 6 direct-log sites converted; `_check_response` WARN + ERROR case bodies stripped of log calls; ERROR case still returns false; OK + DATA branches untouched (OK still has `log_info(handle->response_msg)`, DATA still has `log_data(handle->response_msg)`); 14 breadcrumbs deleted; dispatch tests + dual builds green.
  </done>
</task>

</tasks>

<verification>
- Grep gate: zero non-comment legacy log macro references outside `_check_response`'s OK/DATA branches.
- Zero `// log_*` breadcrumb comments remain.
- `_check_response` ERROR case still returns false; WARN case is empty (just `break`); OK + DATA verbatim (OK still calls `log_info(handle->response_msg)`, DATA still calls `log_data(handle->response_msg)`).
- Native dispatch tests green (response_code flow preserved).
- Both boards build.
</verification>

<success_criteria>
- 6 direct-log sites converted; dispatcher edited per D-01; breadcrumbs deleted per D-04b.
- response_code state machine intact: ERROR aborts, WARN falls through silent, OK + DATA branches preserved verbatim.
- One commit `feat(operation_utils.cpp): ...` landed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-convert-error-warn-info-call-sites/07-09-SUMMARY.md`.
</output>
</content>
</invoke>