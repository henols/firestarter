---
status: complete
phase: 112-dev-test-handler-wiring
source: [112-VERIFICATION.md]
started: 2026-07-03T00:00:00Z
updated: 2026-07-03T14:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Phase-111 SC2 bench re-verify — measured voltage tracks the write pulse
expected: On Leonardo + Rev 2.0 with an electrically-erasable chip (W27C512 or W29C020), `firestarter dev test <chip> --destructive` renders vpp_before/after + vpe_before/after voltage rows tracking real rail behavior (plausible droop) across the write pulse, not flat/static/absent.
result: pass
evidence: "chip-test/dev-test-w27c512.md — live destructive sweep on Leonardo + Rev 2.0-class (W27C512, protocol 7, host 3.0.0b10). All 6 steps OK (id/read/blank-check/write/verify/erase). Voltage block renders both before+after rows: vpp 12000/12000 mV, vpe 13700/13700 mV — present and plausible for the rails (before==after = regulator held steady under the write pulse, no measurable droop; rows are real reads, not absent/placeholder). is_submittable=true. Operator ran the sweep and passed."

### 2. `dev test` interactive provenance prompts — usable and warranted
expected: Running `firestarter dev test <chip> --destructive` collects only provenance the tool genuinely cannot self-report, with prompts that accept natural input.
result: pass
disposition: "resolved-by-descope + verified. The reported friction (broken `new`/`used`/`2.0` prompts) is eliminated: 112-04 removed the interactive provenance-collection layer entirely (operator-approved) and moved self-reportable provenance to AutoCapture; 112-05 re-verify confirmed 9/9 with zero provenance-prompt symbols remaining in firestarter_app/ (grep-confirmed 2026-07-03). The test's principle — 'only collect what the tool cannot self-report' — is now maximally satisfied (nothing is prompted; fw/board/hw auto-captured, unverifiable/redundant fields dropped). Recorded as pass (the completion predicate recognizes pass/passed, not the interim `resolved` marker); full history preserved in the fields below."
resolved_by: "112-04 (interactive provenance descope, operator-approved)"
resolution: "prompt_provenance() + SHIELD_REV_CHOICES + _CHIP_ORIGIN_CHOICES deleted (killed the slash-in-choice-string bug that rejected new/used/2.0); is_submittable() redefined on AutoCapture completeness only (no human-provenance gate); hw_revision now auto-captured via HardwareManager. Phase-112 re-verification confirmed zero provenance-prompt symbols remain in firestarter/. The --destructive TTY safety confirm (SAFE-03) is retained — it is a safety gate, not provenance."
reported: "Actually too complicated. Do you need to ask all the questions? [bench: `new`/`used`/`2.0` all rejected with 'Please select one of the available options']. I dont see the point of any questions, the shield version, hw and fw can be queried. Why is chip origin a relevant question / why does it matter?"
severity: major

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0
resolved: 0
note: "test 2 = pass-by-verified-descope (was interim `resolved`); see its disposition field"

## Gaps

- truth: "`dev test` collects only provenance the firmware cannot self-report, via prompts that accept natural input"
  status: resolved
  resolved_by: "112-04 (provenance descope, operator-approved); confirmed by Phase-112 re-verification 2026-07-03"
  reason: "User reported: prompts too complicated and partly broken — `new`/`used`/`2.0` rejected; and shield/hw/fw are queryable so the questions add friction without value"
  severity: major
  test: 2
  root_cause: >
    TRIGGER BUG — firestarter/diagnostic_report.py:125 `_CHIP_ORIGIN_CHOICES =
    ["new/blank", "pulled/used"]`. The choice STRINGS contain `/`, which is the
    same character rich.prompt.Prompt.ask uses to SEPARATE choices in the
    `[...]` display. So the prompt renders `[new/blank/pulled/used]` (looks like
    4 options) but only two exact strings are accepted; natural inputs `new`,
    `blank`, `pulled`, `used` are all rejected. Same class of friction on shield
    revision — a partial `2.0` is rejected; only the exact `Rev 2.0` is accepted.
    ROOT DESIGN DECISION (operator-approved this session) — the entire
    interactive provenance-collection model (RPT-04 / D-04 / D-05 / D-06) was
    descoped in 112-04. Rationale established with operator:
      * fw version + board identity are auto-capturable (threaded via AutoCapture).
      * hardware/shield revision IS queryable via HardwareManager; a COARSE bucket
        (REVISION_2_0 = 0x02 covers 2.0/2.1/2.2) or REVISION_UNKNOWN (0xFE) is
        good enough to auto-capture, no human prompt needed.
      * chip_origin is self-reported + unverifiable + largely REDUNDANT with the
        objective chip_id_expected/actual + mismatch reason AutoCapture records. Dropped.
      * owns_eraser is the weakest signal (only says a UV chip COULD be re-erased). Dropped.
      * pot_touched (the one genuinely-invisible voltage confounder) — FULL removal
        this session; a future `--pot-adjusted` flag is a possible later addition,
        explicitly out of scope now (do NOT re-introduce a prompt for it).
  debug_session: ""
