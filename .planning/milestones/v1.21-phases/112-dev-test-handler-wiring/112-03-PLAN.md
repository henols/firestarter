---
phase: 112-dev-test-handler-wiring
plan: 03
type: execute
wave: 3
depends_on: ["112-01", "112-02"]
files_modified:
  - firestarter_app/tools/check_devtest_orchestrator.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
  - firestarter_app/tests/test_dev_test_cmd.py
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03]
must_haves:
  truths:
    - "The SAFE-03 AST checker now scans the real dev test handler in cli_handlers.py (not the nonexistent dev_test_cli.py stub) and exits 0 on the clean handler (mandatory planner note 1)"
    - "The checker exits non-zero on a handler-shaped planted violation (VPP-set / raw-wire-dict / --force) injected via an env-override seam — anti-hollow, Phase-109 D-02/D-03"
    - "The dev test handler is unit-testable with no hardware via EpromDatabase(skip_local_override=True) + a mock operator + injectable prompts + forced off-TTY (SC4)"
  artifacts:
    - "firestarter_app/tools/check_devtest_orchestrator.py — _DEVTEST_CLI_HANDLER repointed to firestarter/cli_handlers.py; docstring de-stubbed; handler env-override seam"
    - "firestarter_app/tests/test_check_devtest_orchestrator.py — new handler-shaped planted-violation test + preserved clean-pass baseline"
    - "firestarter_app/tests/test_dev_test_cmd.py — CliRunner unit tests for the dev test handler (exit codes, prompt gating, sampler, dual-artifact)"
  key_links:
    - "the checker's _DEVTEST_CLI_HANDLER points at cli_handlers.py so main() actually scans the handler; the scanned-empty fail-closed guard stays"
    - "the handler tests inject prompt_provenance(ask=Mock, confirm=Mock) and monkeypatch sys.stdin.isatty so tests never block on a TTY; sampler=None / mock hardware_manager keeps them bench-free"
  prohibitions:
    - statement: "A checker that silently skips the now-existing handler is the hollow-gate failure mode Phase 109 D-02/D-03 forbid — the handler path MUST resolve to a real file and be scanned"
      status: enforced
      verification: "cd firestarter_app && python tools/check_devtest_orchestrator.py prints the cli_handlers.py path in its PASS: scanned line"
    - statement: "The negative-fixture test proves the checker flips non-zero on a real subprocess-level planted violation, never an in-process synthetic"
      status: enforced
      verification: "the new test writes a fixture to disk and runs the checker via subprocess with the handler env-override, asserting returncode != 0 and FAIL: in stdout"
    - statement: "No new VPP-set / raw-wire-dict / --force is present in the shipped handler (SAFE-01/02/03) — the extended checker green against the real cli_handlers.py proves it"
      status: enforced
      verification: "cd firestarter_app && python tools/check_devtest_orchestrator.py exits 0 with cli_handlers.py in scope"
---

<objective>
Close the two mandatory integration items: (1) extend the Phase-109 SAFE-03 AST
checker to actually scan the now-existing dev test handler (repoint the
nonexistent-stub path to cli_handlers.py, add a handler-shaped negative-fixture
test) so the gate is not hollow; and (2) prove SC4 — the handler is fully
unit-testable with no hardware via the dev validate-family test seam.

Purpose: A checker that tolerates the handler's absence was correct for Phase
109; now that the handler exists, silently skipping it is exactly the v1.12
hollow-GATE-03 failure this project forbids. And SC4 requires demonstrating the
wiring needs no bench access.
Output: the repointed+de-stubbed checker, a paired handler-shaped planted-violation
test, and a full CliRunner test module for the dev test handler.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-dev-test-handler-wiring/112-CONTEXT.md
@.planning/phases/112-dev-test-handler-wiring/112-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint + de-stub the SAFE-03 checker to scan the real handler, add a handler-shaped negative fixture test</name>
  <files>firestarter_app/tools/check_devtest_orchestrator.py, firestarter_app/tests/test_check_devtest_orchestrator.py</files>
  <read_first>
    - firestarter_app/tools/check_devtest_orchestrator.py:40-71 (scope-tolerance docstring + _DEFAULT_CHIP_TEST + FIRESTARTER_DEVTEST_SRC env seam + the _DEVTEST_CLI_HANDLER stub at :71 pointing at nonexistent dev_test_cli.py)
    - firestarter_app/tools/check_devtest_orchestrator.py:183-297 (_scan_file missing-file tolerance :190, _assert_host_only, main() target loop :239-293, scanned-empty fail-closed guard :263-269)
    - firestarter_app/tests/test_check_devtest_orchestrator.py (all 6 existing tests — the subprocess _run_checker helper + env-override injection pattern + clean-pass baseline test_checker_exits_zero_on_clean_source)
    - firestarter_app/firestarter/cli_handlers.py (the real handler landed by plan 02 — the new scan target)
    - .planning/phases/112-dev-test-handler-wiring/112-PATTERNS.md (§MODIFY SAFE-03 AST checker — repoint + anti-hollow requirement)
  </read_first>
  <action>
    In check_devtest_orchestrator.py: repoint `_DEVTEST_CLI_HANDLER` (currently at
    :71, `os.path.join(_HERE, "..", "firestarter", "dev_test_cli.py")`) to the REAL
    handler location `os.path.join(_HERE, "..", "firestarter", "cli_handlers.py")`.
    Scanning the whole cli_handlers.py is acceptable and intended — the deny buckets
    (_VPP_SET_NAMES, _WIRE_DICT_KEYS, force/--force) must find ZERO hits across the
    entire host CLI for the gate to stay green, which is the invariant.

    Add a dedicated env-override seam for the handler path so the paired pytest can
    inject a handler-shaped violating fixture without editing the real
    cli_handlers.py: read `FIRESTARTER_DEVTEST_HANDLER` (mirroring the existing
    FIRESTARTER_DEVTEST_SRC seam at :65) and use it as the handler target in main()'s
    `targets` list. Default it to the repointed cli_handlers.py path.

    Update the docstring: remove the "not-yet-existing / DEFERRED to Phase 112"
    scope-tolerance language for the handler (Phase 112 has now landed it). KEEP the
    _scan_file missing-file tolerance (:190) — it still legitimately covers a test
    fixture path — and KEEP the scanned-empty fail-closed guard (:263-269). The
    handler path now resolves to a real file, so main() actually scans it (anti-hollow,
    D-02/D-03): a handler that is never scanned is the forbidden hollow-gate.

    In test_check_devtest_orchestrator.py: add a test that plants a
    handler-shaped violation (e.g. a fixture whose body calls `op.set_vpp(...)` or
    passes `force=True` or builds a >=2-wire-key dict) written to disk and injected
    via `FIRESTARTER_DEVTEST_HANDLER`, asserting the checker returns non-zero with
    FAIL: in stdout — the anti-hollow proof for the handler leg specifically (mirror
    the existing subprocess+env-override tests 2-5). Also assert the clean-pass
    baseline (test_checker_exits_zero_on_clean_source) still passes against the REAL
    cli_handlers.py now that it is in scope — this is the load-bearing proof the
    shipped handler is orchestrator-only.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_check_devtest_orchestrator.py -q && python tools/check_devtest_orchestrator.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dev_test_cli.py" firestarter_app/tools/check_devtest_orchestrator.py` returns ZERO lines (stub path removed).
    - `grep -n "cli_handlers.py" firestarter_app/tools/check_devtest_orchestrator.py` shows _DEVTEST_CLI_HANDLER now points there.
    - `cd firestarter_app && python tools/check_devtest_orchestrator.py` exits 0 and its "PASS: scanned ..." line names cli_handlers.py (the handler is actually scanned, not skipped).
    - The new handler-shaped planted-violation test writes a fixture to disk, runs the checker via subprocess with `FIRESTARTER_DEVTEST_HANDLER` set, and asserts returncode != 0 and "FAIL:" in stdout.
    - All existing tests in test_check_devtest_orchestrator.py still pass.
  </acceptance_criteria>
  <done>The checker scans the real cli_handlers.py handler, exits 0 on the clean handler, and its paired test proves it flips non-zero on a handler-shaped planted violation; no dev_test_cli.py stub reference remains.</done>
</task>

<task type="auto">
  <name>Task 2: Add hardware-free CliRunner unit tests for the dev test handler (SC4)</name>
  <files>firestarter_app/tests/test_dev_test_cmd.py</files>
  <read_first>
    - firestarter_app/tests/test_validate_family_cmd.py:1-70 (make_app_context + CliRunner fixture + EpromDatabase(skip_local_override=True) + Mock(spec=...) manager seam to mirror exactly)
    - firestarter_app/firestarter/cli_handlers.py (the dev test handler from plan 02 — its flags, composition, and exit-code path)
    - firestarter_app/firestarter/chip_test.py:434-438 (verdict constants) + :925-949 (count_applicable / BannerCounts) for constructing StepResult fixtures
    - firestarter_app/firestarter/diagnostic_report.py:148-201 (prompt_provenance injectable ask=/confirm=) + :207-214 (is_submittable)
  </read_first>
  <behavior>
    - Exit-code mapping (D-01): a mocked sweep returning an OK-only result set exits 0; a set containing a BAD exits 1; a marginal-only set exits 2; a chip-ID-mismatch id step (BAD) exits 1; a non-destructive clean run with N<M (locked_destructive non-empty) exits 0.
    - Prompt gating (D-02): monkeypatch sys.stdin.isatty→True and assert provenance is prompted before the sweep and the destructive confirm shows on a --destructive run; monkeypatch isatty→False and assert both are skipped and the report is not-submittable (blank Provenance).
    - --yes bypass (D-03): TTY + --yes bypasses the destructive confirm but still prompts provenance.
    - Sampler (D-04): a --destructive run fires the sampler around OP_WRITE (via plan 01's run_plan hook) filling the report split voltage slots from the mock hardware_manager; sampler=None on a non-destructive run.
    - Dual-artifact (D-05): --output-dir writes exactly dev-test-<chip>.json + dev-test-<chip>.md; no --output-dir writes nothing but still renders to stdout.
  </behavior>
  <action>
    Create tests/test_dev_test_cmd.py mirroring test_validate_family_cmd.py's
    make_app_context helper: construct a fresh AppContext with
    `EpromDatabase(skip_local_override=True)`, `Mock(spec=EpromOperator)`,
    `Mock(spec=HardwareManager)` (its sample_vpp_mv/sample_vpe_mv return canned mV
    ints), and the other Mock managers, per test. Drive the handler with
    `CliRunner().invoke(cli, ["dev", "test", <chip>, ...])`.

    Force determinism/no-blocking: monkeypatch `sys.stdin.isatty` per test to
    control the TTY branch (D-02); where the handler prompts, inject via the
    handler's use of prompt_provenance / Confirm (patch at the seam the handler
    exposes — mock the operator's step methods so the sweep returns controlled
    verdicts). Assert `result.exit_code` for each D-01 case (0/1/2 + chip-ID
    mismatch → 1 + N<M → 0). Assert the off-TTY run stores a blank Provenance /
    is_submittable False. Assert the --output-dir case writes exactly the two
    hyphenated files (list tmp_path contents) and the no-dir case writes none.
    Assert the sampler fires around OP_WRITE on a --destructive run (the mock
    hardware_manager sampler is called and the report split slots are filled). Pass
    sampler=None implicitly on non-destructive runs (assert the standalone vpp_mv/
    vpe_mv slots are filled instead). Keep every test bench-free — no real serial,
    no real hardware (SC4).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_dev_test_cmd.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dev_test_cmd.py -q` collects and passes tests covering: exit 0 / 1 / 2, chip-ID-mismatch→1, N<M→0, TTY vs off-TTY prompt gating, --yes bypass, sampler-around-OP_WRITE, and dual-artifact-only-under-output-dir.
    - No test opens a real serial port or requires hardware (all managers are Mock(spec=...); EpromDatabase uses skip_local_override=True).
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` pass (py3.11 target).
    - `pytest tests/ --cov=firestarter --cov-fail-under=70` passes (coverage floor held).
  </acceptance_criteria>
  <done>A hardware-free CliRunner test module proves the dev test wiring: all D-01 exit codes, D-02/D-03 prompt gating, D-04 sampler bracketing, and D-05 dual-artifact behavior, with zero bench access (SC4).</done>
</task>

</tasks>

<verification>
- `cd firestarter_app && python tools/check_devtest_orchestrator.py` — exits 0 AND its PASS line names cli_handlers.py (handler actually scanned, anti-hollow).
- `cd firestarter_app && python -m pytest tests/test_check_devtest_orchestrator.py tests/test_dev_test_cmd.py -q` — all green (checker anti-hollow proof + SC4 handler seam).
- `cd firestarter_app && python -m pytest tests/ -q` — full suite green (no regression from the signature/handler changes across plans 01-03).
- `ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/` (py3.11 CI target).
- `cd firestarter_app && python tools/check_mypy_watermark.py` — exits 0; the new `test_dev_test_cmd.py` module + `check_devtest_orchestrator.py` changes do not regress the strict-mypy watermark on `cli_handlers.py` (a CI-enforced strict-8 module). This is a real CI gate step (ci.yml "mypy type check (watermark gate)"), separate from ruff.
- HARDWARE-GATED / DEFERRABLE (mandatory planner note 2 — Phase-111 SC2 re-verify): on Leonardo + Rev 2.0 with an electrically-erasable chip (W27C512 / W29C020), a `firestarter dev test <chip> --destructive` run should show vpp_before/after + vpe_before/after tracking real rail behavior across the write. This is a bench/UAT check; the software wiring above is fully unit-tested NOW and MUST NOT block on it. Capture in UAT; safe to defer again if no bench session.
</verification>

<success_criteria>
The SAFE-03 checker scans the real handler and is green on it while flipping
non-zero on a handler-shaped planted violation; the dev test handler is proven
fully unit-testable with no hardware (SC4). Phase-111 SC2 bench re-verify is
recorded as a hardware-gated, deferrable UAT item.
</success_criteria>

<output>
Create `.planning/phases/112-dev-test-handler-wiring/112-03-SUMMARY.md` when done.
</output>
