---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 08
type: execute
wave: 5
depends_on: [115-04, 115-05, 115-06, 115-07]
files_modified:
  - firestarter_app/doc/beta-testing-install.md
  - firestarter
  - firestarter_app
autonomous: true
requirements: [ONBOARD-04]
must_haves:
  truths:
    - "firestarter_app/doc/beta-testing-install.md is finalized: any gotcha discovered during the bench runs is folded in and the per-board commands/tables match observed reality (D-04)"
    - "The meta-repo submodule gitlinks for firestarter + firestarter_app are bumped from PINNED b10 to the 3.0.0b11 commits produced by this phase's publish (D-01)"
  artifacts:
    - path: firestarter_app/doc/beta-testing-install.md
      provides: "Finalized community onboarding doc reflecting live bench findings"
      contains: "community-validation"
  key_links:
    - from: "meta-repo submodule gitlinks (firestarter, firestarter_app)"
      to: "the sub-repo 3.0.0b11 commits"
      via: "git submodule pointer bump off PINNED b10"
      pattern: "3.0.0b11"
  prohibitions:
    - "MUST NOT perform the v1.21 milestone git tag, the final --no-ff merge to beta, or /gsd-ship / /gsd-complete-milestone (D-02/D-06 — separate operator-gated close step AFTER phase verification)"
    - "MUST NOT re-open the ONBOARD-04 doc's scope — fold in bench findings only, do not rewrite the graduation-ladder content (that lives in community-validation.md)"
---

<objective>
Close the loop: fold the live bench findings (Plans 05-07) back into the ONBOARD-04
onboarding doc so it is accurate for the next stranger (D-04 finalize), and bump
the meta-repo submodule gitlinks off PINNED b10 to the `3.0.0b11` commits this
phase's publish produced (D-01 runbook step 5). The b11 beta is already complete
at publish; this is the post-run doc truing-up + dev-workspace gitlink
consistency.

Purpose: The doc ships in b11 as a draft (Plan 01) and the community reads it from
GitHub/v1.21 regardless of pip build (D-04), so the finalize is a legitimate
post-publish repo update; the gitlink bump is what finally moves the meta repo off
its long-standing PINNED-at-b10 hold now that b11 is public.

OUT of scope (D-02/D-06): the v1.21 milestone tag, the final --no-ff beta merge,
and the ship / complete-milestone ceremony — those are a SEPARATE operator-gated
step AFTER this phase's verification passes.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@firestarter_app/doc/beta-testing-install.md
@chip-test/dev-test-w27c512.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the onboarding doc from bench findings</name>
  <files>firestarter_app/doc/beta-testing-install.md</files>
  <read_first>
    firestarter_app/doc/beta-testing-install.md (the Plan 01 draft). The three
    bench evidence records chip-test/onboard-uno.md, onboard-leonardo.md,
    onboard-uno328pb.md (Plans 05-07). RESEARCH.md D-04 (finalize-from-findings).
  </read_first>
  <action>
    Read the three per-board evidence records and fold any newly-discovered gotcha
    into firestarter_app/doc/beta-testing-install.md: correct any command / port /
    .hex detail that differed from the draft, and add a short "known issues per
    board" note if the bench surfaced one (e.g. the uno328pb instability + FUT
    item from Plan 07, the Leonardo re-enumeration around the 1200-baud reset if
    observed). Remove the draft-first caveat banner now that findings are folded
    in. Keep the doc's scope unchanged — this is a truing-up against observed
    reality, NOT a rewrite; the dev test graduation-ladder content stays in
    community-validation.md, which this doc still hands off into. Commit on the
    firestarter_app v1.21 branch (the community reads docs from GitHub/v1.21
    regardless of pip build — D-04).
  </action>
  <verify>
    <automated>test -f firestarter_app/doc/beta-testing-install.md && grep -q 'community-validation' firestarter_app/doc/beta-testing-install.md && grep -q 'firestarter_uno.hex' firestarter_app/doc/beta-testing-install.md</automated>
  </verify>
  <acceptance_criteria>
    firestarter_app/doc/beta-testing-install.md reflects the live bench findings,
    the draft-first caveat is removed, the per-board commands/tables match observed
    reality, and the community-validation.md hand-off remains. Committed on v1.21.
  </acceptance_criteria>
  <done>beta-testing-install.md finalized from the three evidence records (draft caveat removed, hand-off intact), committed on the firestarter_app v1.21 branch.</done>
</task>

<task type="auto">
  <name>Task 2: Bump meta-repo submodule gitlinks off PINNED b10 to the b11 commits</name>
  <files>firestarter, firestarter_app</files>
  <read_first>
    RESEARCH.md Runbook step 5 (lines 270-271). STATE.md "gitlinks PINNED at b10"
    + the v1.21 Substrate note. CONTEXT.md D-01 (gitlink bump in scope) and D-02
    (what is OUT of scope — no beta merge / tag).
  </read_first>
  <action>
    In each submodule (/workspaces/firestarter and /workspaces/firestarter_app),
    fetch and check out the 3.0.0b11 commit that this phase's publish produced on
    the v1.21 branch (the CI update_version.py auto-commit; confirm each sub-repo's
    __init__.py / version.h now reads 3.0.0b11 at that commit). Then in the meta
    repo, stage the two submodule gitlink updates (`git add firestarter
    firestarter_app`) moving them off the PINNED b10 to the b11 commits, and
    commit in the meta repo. This is the D-01 gitlink bump only — a dev-workspace
    consistency step (the community installs via PyPI/GitHub, not meta gitlinks),
    and it is reversible (a gitlink can be re-pointed). Do NOT tag v1.21, do NOT
    merge to beta, do NOT run any ship / complete-milestone ceremony (D-02/D-06).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -q '3.0.0b11' include/version.h && cd /workspaces/firestarter_app && grep -q '3.0.0b11' firestarter/__init__.py && cd /workspaces && git diff --cached --submodule 2>/dev/null | grep -Eq 'firestarter|firestarter_app' || git status --short firestarter firestarter_app | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    Both submodules are checked out at their 3.0.0b11 commit (version.h /
    __init__.py read 3.0.0b11) and the meta-repo gitlinks are updated to those
    commits and committed. No v1.21 tag, no beta merge, no ship ceremony performed.
  </acceptance_criteria>
  <done>Both submodule gitlinks bumped to the 3.0.0b11 commits (version strings confirm b11) and committed in the meta repo; no tag/beta-merge/ship performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench findings -> published doc | Observed reality is folded back into stranger-facing guidance |
| meta gitlinks -> sub-repo commits | The dev-workspace submodule pointers move off the long-standing PINNED b10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-12 | Tampering (scope creep) | performing the OUT-of-scope close ceremony (tag / beta merge / ship) | high | mitigate | Task 2 action + prohibitions explicitly forbid the v1.21 tag, the --no-ff beta merge, and ship/complete-milestone; only the gitlink pointer + doc finalize are done here (D-02/D-06). |
| T-115-13 | Tampering | gitlink pointed at the wrong (non-b11) commit | low | mitigate | Task 2 verifies each sub-repo's version string reads 3.0.0b11 at the checked-out commit before bumping the gitlink; the bump is reversible. |
| T-115-DOC | Tampering (hardware, via stale instructions) | finalized doc drifting from observed reality | low | mitigate | Finalize is driven directly by the three per-board evidence records, not from memory. |
</threat_model>

<verification>
- firestarter_app/doc/beta-testing-install.md finalized from the three evidence
  records; draft caveat removed; hand-off intact; committed on v1.21.
- Both submodule gitlinks bumped to the 3.0.0b11 commits (version strings confirm
  b11) and committed in the meta repo.
- No v1.21 tag, no --no-ff beta merge, no ship / complete-milestone ceremony.
</verification>

<success_criteria>
The onboarding doc is accurate against live hardware and the meta workspace is
consistent at b11 — the phase's deliverables are complete and ready for
verification, leaving the milestone-close ceremony as the separate operator-gated
next step.
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-08-SUMMARY.md` when done.
</output>
