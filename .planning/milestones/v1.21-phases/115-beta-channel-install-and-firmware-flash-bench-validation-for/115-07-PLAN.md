---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 07
type: execute
wave: 4
depends_on: [115-03, 115-04]
files_modified:
  - chip-test/onboard-uno328pb.md
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]
must_haves:
  truths:
    - "chip-test/onboard-uno328pb.md exists, recording the full fresh-venv install -> fw -i flash -> fw/hw smoke chain outcome for uno328pb (best-effort, advisory — D-05)"
    - "If any step fails or is flaky, the specific failure mode is recorded verbatim (no retry into a false green) and a FUT item is noted"
    - "If the physical third board proves to be a plain Uno, that is recorded explicitly and -b uno / firestarter_uno.hex is used — never a silent substitution"
  artifacts:
    - path: chip-test/onboard-uno328pb.md
      provides: "uno328pb per-board evidence record (best-effort; outcome + failure mode honestly captured)"
      contains: "3.0.0b11"
  key_links:
    - from: "firestarter fw -i -b uno328pb"
      to: "firestarter_uno328pb.hex asset"
      via: "board override -> fetch_release_info asset match -> avr_tool urclock flash+verify (atmega328pb)"
      pattern: "firestarter_uno328pb.hex"
  prohibitions:
    - "MUST NOT block phase verification on the uno328pb outcome (best-effort / advisory — D-05)"
    - "MUST NOT silently substitute firestarter_uno.hex for firestarter_uno328pb.hex; if the board is a plain Uno, record it explicitly and switch to -b uno (D-05)"
    - "MUST NOT retry a flaky run into a false green (D-05/D-08)"
    - "MUST NOT run a chip write/verify in the smoke test (ONBOARD-03 — fw + hw only)"
---

<objective>
Run the community onboarding chain on the third bench board (uno328pb) as a
BEST-EFFORT / advisory gate (D-05). The deliverable is an honest evidence record,
not a PASS: memory documents uno328pb as bench-unstable (read timeouts, 0xff
drift, VPP misread, PROGRAM brownout) and once mis-identified as a plain Uno
running the wrong firmware. A flaky or failed run is recorded with its specific
failure mode + a FUT item and does NOT block milestone close.

Purpose: Complete the "each bench board" sweep the ROADMAP asks for while keeping
uno328pb advisory. Output: `chip-test/onboard-uno328pb.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-PATTERNS.md
@chip-test/dev-test-w27c512.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator connects + identifies the uno328pb (or confirms it is a plain Uno)</name>
  <read_first>
    memory feedback_verify_port_identity_each_task; project_uno328pb_correction
    (may be a plain Uno w/ wrong FW); project_uno328pb_bench_instability_27_04.
    RESEARCH.md Q3 board-detection + D-05 .hex-selection rule (lines 170-176).
  </read_first>
  <action>
    Halt for the operator to physically connect + power the third board, confirm
    its serial port, AND confirm its true identity (genuine uno328pb vs plain Uno
    — the mis-ID case). Record the confirmed port + identity for Task 2's .hex
    selection; the reported controller string alone is not decisive.
  </action>
  <what-built>
    Both channels are public at 3.0.0b11. The uno328pb run needs the physical third
    board connected + powered and its serial port confirmed. This board has a
    history of being mis-identified as a plain Uno (ATmega328PB signature 0x1E9516
    vs 328P 0x1E950F) — the operator must confirm the true board identity so the
    correct .hex is chosen (never a silent substitution).
  </what-built>
  <how-to-verify>
    Operator connects + powers the third board and states its serial port. The
    operator confirms whether it is genuinely a uno328pb (ATmega328PB) or a plain
    Uno. The agent may probe `firestarter fw` to read the reported `controller:`
    identity, but the reported identity alone is not decisive (the mis-ID case) —
    the operator's physical confirmation governs which .hex is used.
  </how-to-verify>
  <resume-signal>
    Operator replies "uno328pb connected on /dev/ttyXXX" OR "third board is a
    plain Uno, on /dev/ttyXXX — use -b uno" (naming the confirmed port + identity).
  </resume-signal>
  <acceptance_criteria>
    The third board is powered, its serial port is confirmed, and its true
    identity (uno328pb vs plain Uno) is recorded for Task 2's .hex selection.
  </acceptance_criteria>
  <done>The third board is powered, its serial port confirmed, and its true identity (uno328pb vs plain Uno) recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-venv install -> fw -i flash -> fw/hw smoke -> evidence record (best-effort)</name>
  <files>chip-test/onboard-uno328pb.md</files>
  <read_first>
    RESEARCH.md Q4 fresh-venv recipe + isolation gotchas (lines 190-209), Q5 smoke
    commands + the uno328pb caveat (lines 218-226: expect instability on the live
    op; record the outcome, do not retry into a false green). chip-test/dev-test-w27c512.md
    (evidence-record shape + honest-fallback null / "not measured").
  </read_first>
  <action>
    Using the confirmed port + confirmed identity, run the isolated chain as a
    stranger would (D-07): throwaway venv, `pip install --pre firestarter` (pulls
    3.0.0b11), export FIRESTARTER_CONFIG_DIR to a clean temp dir BEFORE any
    firestarter call. Then:
    (1) `firestarter --version` — expect 3.0.0b11 (ONBOARD-01);
    (2) bare `firestarter fw -i -b uno328pb` (installed-beta auto-route to --pre;
    -b uno328pb resolves firestarter_uno328pb.hex; avr_tool urclock/atmega328pb
    flash+verify). If Task 1 confirmed the board is actually a plain Uno, use
    `-b uno` / firestarter_uno.hex instead and RECORD that explicitly in the
    evidence — never a silent substitution (D-05). Capture the resolved channel,
    asset name, and avrdude output (ONBOARD-02);
    (3) `firestarter fw` — capture the reported version + controller identity
    (ONBOARD-03 part 1);
    (4) `firestarter hw` — the minimal live op (ONBOARD-03 part 2; NOT a chip
    write/verify).
    Because uno328pb is bench-unstable, record every step's ACTUAL outcome
    verbatim — timeouts, 0xff drift, VPP misread, brownout — do NOT retry a flaky
    result into a green (D-05/D-08). Write chip-test/onboard-uno328pb.md mirroring
    dev-test-w27c512.md (verdict table + fenced JSON with schema_version,
    generated timestamp, auto_capture.host_version, resolved channel + asset,
    avrdude return, hw result; honest null / "not measured" for anything blank).
    If any step fails or is flaky, add a short "known-instability note + FUT item"
    section to the record capturing the specific failure mode. Teardown venv +
    config dir. This is ADVISORY — a fail/flaky outcome is recorded, not a
    phase-verification blocker.
  </action>
  <verify>
    <automated>test -f chip-test/onboard-uno328pb.md && grep -Eq 'firestarter_uno328pb.hex|firestarter_uno.hex' chip-test/onboard-uno328pb.md</automated>
  </verify>
  <acceptance_criteria>
    chip-test/onboard-uno328pb.md exists and honestly records the full chain
    outcome (host_version, resolved channel + asset, avrdude result, fw+hw
    outcomes), the correct .hex for the confirmed identity (uno328pb or, if a
    plain Uno, explicitly noted + uno), and — if anything failed/was flaky — the
    specific failure mode + a FUT item. No false green; no silent substitution.
  </acceptance_criteria>
  <done>chip-test/onboard-uno328pb.md honestly records the full chain outcome + correct .hex for the confirmed identity + any failure mode/FUT item; advisory, non-blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI + GitHub prerelease -> fresh venv | Published artifacts install here |
| host avrdude -> uno328pb over USB | Firmware image written across this boundary; signature 0x1E9516 governs which .hex is valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-02 | Tampering (hardware) | wrong .hex flashed to a mis-identified board | medium | mitigate | Operator confirms true identity (Task 1); explicit `-b uno328pb` (or `-b uno` if confirmed plain Uno, recorded); ATmega328PB signature check rejects a 328P image and vice-versa. |
| T-115-11 | Denial of service | uno328pb bench instability (timeouts / 0xff drift / brownout) | low | accept | Advisory board (D-05) — instability recorded honestly + FUT item; never retried into a false green; does not block phase verification. |
| T-115-04 | Tampering | fw -i silent stable fallback | medium | mitigate | Evidence record captures the resolved channel + tag (--pre / 3.0.0b11), not just "flash OK". |
| T-115-SC | Malicious code / supply chain | pip install --pre firestarter | low | accept | Installs only the project's own published package; no new third-party dependency. |
</threat_model>

<verification>
- chip-test/onboard-uno328pb.md exists with the verdict table + fenced JSON.
- The correct .hex for the confirmed identity is recorded (uno328pb, or plain-Uno
  noted + uno) with the resolved channel/tag and avrdude/fw/hw outcomes.
- Any failure/flakiness is captured verbatim with a FUT item; no false green.
- uno328pb outcome is advisory and does NOT gate phase verification.
</verification>

<success_criteria>
The uno328pb sweep is completed and honestly recorded (best-effort). A PASS
graduates it toward a future hard gate; a fail/flaky run is a documented
known-instability note + FUT item — either way the milestone close is not blocked
by it (D-05).
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-07-SUMMARY.md` when done.
</output>
