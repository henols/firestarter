---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02]
must_haves:
  truths:
    - "The firestarter_app v1.21 tree passes the release CI gates locally (codegen drift, ruff check + format, pytest) before any publish"
    - "The firestarter v1.21 tree passes codegen drift + pio native tests, and pio run builds all three firestarter_<board>.hex artifacts"
    - "The operator has confirmed gh auth to the henols repos, repo secrets, and the workflow_dispatch-from-v1.21 trigger mechanism before any irreversible publish"
  artifacts: []
  key_links:
    - from: "pre-flight local validation"
      to: "beta-release.yml + beta-build.yml CI gates"
      via: "mirrors the exact gate steps so the dispatched run cannot fail after a partial publish"
      pattern: "codegen|ruff|pytest|pio"
  prohibitions:
    - "MUST NOT trigger any publish, push, or beta merge in this plan (pre-flight is local + reversible only)"
    - "MUST NOT hand-edit __init__.py / version.h version strings — CI update_version.py owns the b10->b11 bump"
---

<objective>
Run the full release pre-flight locally so the irreversible `3.0.0b11` publish
(Plans 03/04) cannot fail a CI gate after a partial publish (RESEARCH Pitfall 5).
Mirror the beta-release.yml (app) and beta-build.yml (firmware) gate steps on the
exact v1.21 trees, then resolve the two publish-blockers with the operator:
BLOCKER-1 (trigger mechanism: workflow_dispatch from the v1.21 branch ref with an
explicit `beta_version`, NOT push-to-beta / NOT the deferred beta merge) and
BLOCKER-2 (gh auth to henols/* + repo secrets configured).

Purpose: A green pre-flight + a confirmed trigger mechanism + confirmed operator
credentials are the preconditions for a safe, single-shot beta cut.
Output: Recorded pre-flight results; no repo files changed.

Note (env, verified at plan time): `pio` 6.1.19 is on PATH so the firmware native
tests + build are a real executed pass. Python is 3.12 only (no python3.11
binary), so the app ruff/format/pytest legs run under 3.12 with the CI-PENDING /
structurally-green caveat for the py3.11 CI target (established Phase 98/103
precedent — never fabricate a py3.11 PASS).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@firestarter_app/.github/workflows/beta-release.yml
@firestarter/.github/workflows/beta-build.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: App repo pre-flight — mirror beta-release.yml gates</name>
  <files></files>
  <read_first>
    firestarter_app/.github/workflows/beta-release.yml (the exact gate step
    order: `pip install -e .[test]` -> catalog validity check -> codegen drift
    gate `codegen.py --target firestarter/messages.py` then ruff-normalize then
    `git diff --exit-code firestarter/messages.py` -> `ruff check .` ->
    `ruff format --check .` -> `pytest tests/ -v`). RESEARCH.md runbook step 0b
    (lines 251-252) + the CI traps (lines 119-125). memory
    reference_betarelease_ci_gotchas_v18 (.[dev] vs .[test]; use .[test]).
  </read_first>
  <action>
    In /workspaces/firestarter_app (on the v1.21 branch), reproduce the
    beta-release.yml gate steps verbatim: install the test extras
    (`pip install -e .[test]`), run the catalog validity check, run the codegen
    drift gate (regenerate firestarter/messages.py via the workflow's codegen
    invocation, ruff-normalize as the workflow does, then `git diff --exit-code
    firestarter/messages.py` — no drift allowed), then `ruff check .`,
    `ruff format --check .`, and `pytest tests/ -v`. Use the exact flags the
    workflow uses (read them from the workflow file — do not guess). Record any
    failure; a red gate here blocks the cut. Per memory, the three known
    pre-existing REDs on a live-board bench session (test_audit_coverage_matrix
    golden + the two no_programmer_found characterization tests) are NOT this
    phase's regressions — cross-check any failure against `git diff beta..HEAD`
    before treating it as a blocker. This is local + reversible; do NOT commit or
    push anything.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -m pip install -e '.[test]' >/dev/null 2>&1; ruff check . && ruff format --check . && git diff --exit-code firestarter/messages.py && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    ruff check + ruff format --check are clean, messages.py has no codegen drift,
    and the pytest suite is green (modulo the documented pre-existing REDs, each
    confirmed outside `git diff beta..HEAD`). Recorded as CI-PENDING for the
    py3.11 target (devcontainer is 3.12).
  </acceptance_criteria>
  <done>App release CI gates reproduced green locally on the v1.21 tree (ruff, format, no messages.py drift, pytest); nothing committed or pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Firmware repo pre-flight — mirror beta-build.yml gates</name>
  <files></files>
  <read_first>
    firestarter/.github/workflows/beta-build.yml (gate order: catalog check ->
    messages.h codegen drift gate `codegen.py --target include/messages.h
    --language cpp` then `git diff --exit-code` -> `pio test -e native` ->
    `pytest tests/` -> `pio run`). firestarter/platformio.ini (default_envs =
    uno, uno328pb, leonardo) + name_firmware.py (PROGNAME =
    firestarter_<RURP_BOARD_NAME>). RESEARCH.md runbook step 0b (line 253).
  </read_first>
  <action>
    In /workspaces/firestarter (on the v1.21 branch), reproduce the beta-build.yml
    gates: run the messages.h codegen drift gate (regenerate include/messages.h
    via the workflow's codegen invocation, then `git diff --exit-code
    include/messages.h`), run `pio test -e native`, and run `pio run` confirming
    the build emits all three artifacts: firestarter_uno.hex,
    firestarter_uno328pb.hex, firestarter_leonardo.hex (search .pio/build/**).
    pio 6.1.19 is on PATH so these are real executed passes. Record any failure as
    a cut blocker. Local + reversible; do NOT commit or push.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git diff --exit-code include/messages.h && pio test -e native && pio run && ls .pio/build/uno/firestarter_uno.hex .pio/build/uno328pb/firestarter_uno328pb.hex .pio/build/leonardo/firestarter_leonardo.hex</automated>
  </verify>
  <acceptance_criteria>
    include/messages.h has no codegen drift, pio native tests pass, and pio run
    produced firestarter_uno.hex, firestarter_uno328pb.hex, and
    firestarter_leonardo.hex.
  </acceptance_criteria>
  <done>Firmware codegen clean, pio native tests pass, and pio run built all three firestarter_<board>.hex; nothing committed or pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator precondition — gh auth, secrets, trigger mechanism</name>
  <read_first>
    RESEARCH.md BLOCKER-1 (lines 407) + BLOCKER-2 (line 408) + the Release-Cut
    Runbook (lines 245-273) + Assumptions A1/A3.
  </read_first>
  <action>
    Pause for the operator to clear the two publish blockers before any
    irreversible publish. Present the gh-auth / repo-secrets / trigger-mechanism
    checklist below, capture the operator's confirmation (or the blocking missing
    precondition), and do not proceed to Plans 03/04 until confirmed.
  </action>
  <what-built>
    Both v1.21 trees pre-validated against their release CI gates (Tasks 1-2). The
    next step is the irreversible outward-facing publish (Plans 03/04), which
    requires operator credentials + an agreed trigger mechanism. At plan time gh
    is authenticated as `henols` with `repo` + `workflow` scope, so the dispatch
    is technically runnable, but D-03 keeps the human authorization gate.
  </what-built>
  <how-to-verify>
    Operator confirms, before any publish:
    1. `gh auth status` shows an account with rights to run workflows on
       henols/firestarter AND henols/firestarter_app.
    2. The workflow secrets exist on those repos: PERSONAL_ACCESS_TOKEN +
       (for the app) PYPI_API_TOKEN, and GITHUB_TOKEN for the firmware build.
    3. The trigger mechanism (BLOCKER-1) is the recommended one:
       `workflow_dispatch` against the v1.21 branch ref with an explicit
       `-f beta_version=3.0.0b11` — which publishes WITHOUT merging to beta and
       WITHOUT auto-firing on a push, keeping the D-03 gate and deferring the
       beta merge to the close ceremony (D-02/D-06). Confirm this is acceptable,
       or state the alternative (push-to-beta) explicitly if the operator wants
       the beta merge now (which would change Plans 03/04).
  </how-to-verify>
  <resume-signal>
    Operator replies "preconditions confirmed — dispatch from v1.21 with
    beta_version=3.0.0b11" (or states the chosen alternative / blocks with the
    missing precondition).
  </resume-signal>
  <acceptance_criteria>
    Operator has explicitly confirmed gh auth + repo secrets + the
    workflow_dispatch-from-v1.21 trigger mechanism (or an explicit alternative).
    No publish has been fired.
  </acceptance_criteria>
  <done>Operator has confirmed gh auth + repo secrets + the trigger mechanism (or blocked with the missing precondition); no publish fired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator gh CLI -> henols GitHub Actions | Credentials that can dispatch outward-facing publishes cross here |
| local tree -> CI gate parity | A drift between local pre-flight and CI is what causes a half-published state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-05 | Information disclosure | PYPI_API_TOKEN / PERSONAL_ACCESS_TOKEN | high | transfer | Secrets live only as GitHub repo secrets on henols/*; never printed, committed, or embedded in the plan. gh auth is operator-side. |
| T-115-06 | Tampering | half-published state from a CI gate failing after a partial publish | high | mitigate | Full local pre-flight (Tasks 1-2) on the exact tree before any dispatch (Pitfall 5); publish firmware + app only once both pre-validate green. |
| T-115-SC | Tampering | pip install -e .[test] | low | accept | Installs only the project's own package + its pre-existing declared test extras; no new third-party dependency (v1.21 no-new-deps lock; RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- App gates green (ruff check, ruff format --check, no messages.py drift, pytest);
  py3.11 recorded CI-PENDING.
- Firmware gates green (no messages.h drift, pio native tests, pio run builds all
  three .hex).
- Operator has confirmed gh auth + secrets + the trigger mechanism.
- No repo file committed or pushed; no version string hand-edited.
</verification>

<success_criteria>
Both trees are release-ready and the operator has cleared the two publish
blockers, so Plans 03/04 can fire the irreversible cut safely and in one shot.
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-02-SUMMARY.md` when done.
</output>
