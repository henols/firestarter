---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 05
type: execute
wave: 4
depends_on: [115-03, 115-04]
files_modified:
  - chip-test/onboard-uno.md
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]
must_haves:
  truths:
    - "In a fresh throwaway venv, pip install --pre firestarter installs 3.0.0b11 and firestarter --version reports it (Uno run)"
    - "A bare firestarter fw -i -b uno auto-routes to the --pre channel (D-23/D-24), downloads firestarter_uno.hex, and avrdude flashes + verifies"
    - "Post-flash, firestarter fw reports 3.0.0b11 + controller: uno, and firestarter hw (live op, no chip) succeeds"
    - "chip-test/onboard-uno.md records host_version 3.0.0b11, the resolved fw -i channel + downloaded asset, the avrdude flash+verify output, and the smoke-op result"
  artifacts:
    - path: chip-test/onboard-uno.md
      provides: "Uno per-board install->flash->smoke evidence record (verdict table + fenced JSON)"
      contains: "3.0.0b11"
  key_links:
    - from: "fresh-venv installed 3.0.0b11 app"
      to: "--pre channel"
      via: "bare fw -i auto-route (_maybe_auto_route_to_pre, D-23/D-24)"
      pattern: "pre"
    - from: "firestarter fw -i -b uno"
      to: "firestarter_uno.hex asset"
      via: "board override -> fetch_release_info asset match -> avr_tool flash+verify"
      pattern: "firestarter_uno.hex"
  prohibitions:
    - "MUST NOT run a chip write/verify in the smoke test (ONBOARD-03 — fw + hw only)"
    - "MUST NOT use the operator's editable -e install or ~/.firestarter config — fresh venv + FIRESTARTER_CONFIG_DIR exported BEFORE any firestarter call (D-07)"
    - "MUST NOT record a false green — any blank/failed field is recorded honestly (D-08)"
---

<objective>
Run the full community onboarding chain on the Arduino Uno (a HARD pass/fail gate,
D-05) in a fresh isolated environment and capture a per-board evidence record.
This proves ONBOARD-01/02/03 on real hardware for the Uno: fresh-venv beta
install -> bare `fw -i` auto-route + board `.hex` flash+verify -> `fw` + `hw`
smoke.

Purpose: The unit tests already cover the code paths; this is the "stranger on a
fresh machine reaches a live beta stack" proof for the Uno. Output:
`chip-test/onboard-uno.md` (mirroring chip-test/dev-test-w27c512.md).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-PATTERNS.md
@chip-test/dev-test-w27c512.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator connects + identifies the Arduino Uno</name>
  <read_first>
    memory feedback_verify_port_identity_each_task (ttyACM* numbers shuffle across
    replug). RESEARCH.md Q3 board-detection (lines 170-176) + Environment
    Availability (ttyACM0/ttyACM1/ttyUSB0 attached).
  </read_first>
  <action>
    Halt for the operator to physically connect + power the Arduino Uno and
    confirm which serial port it enumerated on (the ttyACM shuffle gotcha). Record
    the confirmed port for Task 2. The agent may probe candidate ports to help
    identify the board, but the operator confirms the physical identity.
  </action>
  <what-built>
    Both channels are public at 3.0.0b11 (Plans 03/04). The Uno run needs the
    physical board connected + powered, and its serial port confirmed — port
    numbers shuffle across replug so the mapping must be re-established per board.
    Claude drives the flash + smoke over USB passthrough; the operator only seats
    and identifies the board (no chip is needed — the smoke test seats none).
  </what-built>
  <how-to-verify>
    Operator connects + powers the Arduino Uno and states which serial port it
    enumerated on (e.g. /dev/ttyACM0). The agent may probe `firestarter fw` on the
    candidate ports to read the `controller:` identity and confirm which port is
    the Uno, but the operator confirms the physical board is the Uno (not the
    uno328pb / Leonardo).
  </how-to-verify>
  <resume-signal>
    Operator replies "Uno connected on /dev/ttyXXX" (naming the confirmed port).
  </resume-signal>
  <acceptance_criteria>
    The Uno is powered and its serial port is confirmed and recorded for Task 2.
  </acceptance_criteria>
  <done>The Uno is powered and its serial port confirmed + recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-venv install -> fw -i flash -> fw/hw smoke -> evidence record</name>
  <files>chip-test/onboard-uno.md</files>
  <read_first>
    RESEARCH.md Q4 fresh-venv recipe (lines 195-209) + the two isolation gotchas
    (lines 190-192: FIRESTARTER_CONFIG_DIR is read at import time — export BEFORE
    invoking the CLI; firmware.py has its own ~/.firestarter .hex download-path
    leak). Q5 smoke commands (lines 218-226). chip-test/dev-test-w27c512.md
    (evidence-record shape: verdict table + fenced JSON with schema_version,
    generated, auto_capture.host_version, per-step records).
  </read_first>
  <action>
    Using the confirmed Uno port, run the isolated onboarding chain exactly as a
    stranger would (D-07): create a throwaway venv under a mktemp dir, upgrade pip,
    `pip install --pre firestarter` into it (pulls 3.0.0b11 from PyPI — allow a
    short retry for propagation), and export FIRESTARTER_CONFIG_DIR to a clean
    temp dir BEFORE invoking the venv's firestarter shim (import-time gotcha — set
    it in the environment of every firestarter call; use the venv's own
    firestarter binary, never the operator's -e install on PATH). Then:
    (1) `firestarter --version` — MUST print 3.0.0b11 (ONBOARD-01);
    (2) bare `firestarter fw -i -b uno` (no --pre/--stable/--firmware-version so
    the installed-beta auto-route to --pre fires, D-23/D-24; -b uno forces the
    board so it resolves firestarter_uno.hex, Pitfall 3) — capture the resolved
    channel, the downloaded asset name, and the avrdude flash+verify output
    (ONBOARD-02);
    (3) `firestarter fw` — capture the "Current firmware version: 3.0.0b11, for
    controller: uno ..." identity line (ONBOARD-03 part 1);
    (4) `firestarter hw` — the one minimal live protocol op on a bare board
    (ONBOARD-03 part 2; NOT a chip write/verify).
    Pass the confirmed port explicitly if auto-detect is ambiguous. Write
    chip-test/onboard-uno.md mirroring dev-test-w27c512.md: a top `| Step | Verdict
    | Reason |` table (rows: install, --version, fw -i (channel+asset), avrdude
    flash+verify, fw (version+board), hw) and a fenced JSON block carrying
    schema_version, a generated timestamp, auto_capture.host_version (the
    3.0.0b11 string — the locus proving the b10->b11 bump), the resolved channel +
    asset, the avrdude return, and the hw result. Record any blank/failed field
    honestly (null / "not measured") — never a false green (D-08). Teardown the
    venv + config dir when done. As a HARD gate, all steps must be OK for the Uno
    to pass.
  </action>
  <verify>
    <automated>test -f chip-test/onboard-uno.md && grep -q '3.0.0b11' chip-test/onboard-uno.md && grep -q 'firestarter_uno.hex' chip-test/onboard-uno.md</automated>
  </verify>
  <acceptance_criteria>
    chip-test/onboard-uno.md exists with host_version 3.0.0b11, the resolved
    --pre channel + firestarter_uno.hex asset, the avrdude flash+verify result,
    and the fw+hw smoke outcomes. For the HARD Uno gate, the install / --version /
    fw -i flash+verify / fw / hw steps are all OK (or the record shows the exact
    failure verbatim and the gate is failed honestly).
  </acceptance_criteria>
  <done>chip-test/onboard-uno.md records host_version 3.0.0b11, --pre channel + firestarter_uno.hex, avrdude flash+verify, and fw+hw smoke — all OK for the HARD Uno gate (or an honest failure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI + GitHub prerelease -> fresh venv | Untrusted-at-first-contact published artifacts install here |
| host avrdude -> Uno over USB | Firmware image is written to the board across this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-02 | Tampering (hardware) | wrong .hex flashed to the Uno | medium | mitigate | Explicit `-b uno` forces firestarter_uno.hex; the evidence record captures the resolved asset name so a mismatch is visible; atmega328p signature check in avrdude rejects a wrong-signature image. |
| T-115-04 | Tampering | fw -i silent stable fallback | medium | mitigate | The evidence record captures the resolved channel + tag (must be --pre / 3.0.0b11), not just "flash OK"; Step 0 (Plan 03) already confirmed the prerelease exists. |
| T-115-08 | Information disclosure | operator ~/.firestarter config leaking into the "fresh machine" claim | low | mitigate | Fresh venv + FIRESTARTER_CONFIG_DIR exported before any CLI call (D-07); the transient ~/.firestarter .hex download-path leak (RESEARCH) is noted and does not affect config/DB isolation. |
| T-115-SC | Malicious code / supply chain | pip install --pre firestarter | low | accept | Installs only the project's own published package (verified reachable in Plan 04); no new third-party dependency. |
</threat_model>

<verification>
- chip-test/onboard-uno.md exists with the verdict table + fenced JSON.
- host_version == 3.0.0b11; resolved channel --pre; asset firestarter_uno.hex.
- avrdude flash+verify recorded; fw reports controller: uno; hw succeeded.
- Fresh venv + clean FIRESTARTER_CONFIG_DIR used (no -e install, no chip write).
</verification>

<success_criteria>
The Uno HARD gate passes: a fresh-machine beta install flashes the Uno beta
firmware and the flashed stack is alive (fw + hw), captured in
chip-test/onboard-uno.md.
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-05-SUMMARY.md` when done.
</output>
