---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/doc/beta-testing-install.md
  - firestarter_app/README.md
autonomous: true
requirements: [ONBOARD-04]
must_haves:
  truths:
    - "A stranger-oriented onboarding doc exists at firestarter_app/doc/beta-testing-install.md covering the full per-board install -> flash -> smoke chain"
    - "The doc names the correct firestarter_<board>.hex per board and states the avrdude prerequisite"
    - "The doc documents the /dev/ttyACM* controller-identity shuffle gotcha and hands off into dev test via community-validation.md"
    - "README.md links to the new doc without duplicating the per-board matrix"
  artifacts:
    - path: firestarter_app/doc/beta-testing-install.md
      provides: "Community beta-install + firmware-flash onboarding narrative (draft)"
      contains: "firestarter_uno.hex"
    - path: firestarter_app/README.md
      provides: "Pointer link to the new onboarding doc"
      contains: "beta-testing-install"
  key_links:
    - from: firestarter_app/README.md
      to: firestarter_app/doc/beta-testing-install.md
      via: "markdown pointer link in the Beta / Pre-release Channel area"
      pattern: "beta-testing-install"
    - from: firestarter_app/doc/beta-testing-install.md
      to: firestarter_app/doc/community-validation.md
      via: "hand-off link into the dev test graduation-ladder doc"
      pattern: "community-validation"
  prohibitions:
    - "MUST NOT duplicate the per-board .hex/channel matrix into README.md (D-09 — pointer link only)"
    - "MUST NOT change any source module, chip_database.json, or firmware behavior (docs only)"
---

<objective>
Draft the ONBOARD-04 community onboarding doc BEFORE the `3.0.0b11` cut (D-04
draft-first) so the beta ships with it. The doc walks a stranger on a fresh
machine through the whole chain: `pip install --pre firestarter` -> a bare
`firestarter fw -i -b <board>` auto-routes to the `--pre` channel and flashes the
board-matching `.hex` -> a `firestarter fw` + `firestarter hw` smoke check -> the
hand-off into `firestarter dev test <chip>`.

Purpose: The install/flash/channel-select feature already exists; this doc makes
the community path discoverable and repeatable by someone who has never touched
the project. It is finalized from live bench findings in Plan 08 (D-04).
Output: `firestarter_app/doc/beta-testing-install.md` (new) + a one-line pointer
in `firestarter_app/README.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-PATTERNS.md
@firestarter_app/doc/community-validation.md
@firestarter_app/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft firestarter_app/doc/beta-testing-install.md</name>
  <files>firestarter_app/doc/beta-testing-install.md</files>
  <read_first>
    firestarter_app/doc/community-validation.md (structural analog — lead paragraph
    stating audience + one-sentence purpose; state/step tables; explicit "what this
    is NOT" framing; fenced command excerpts). RESEARCH.md Q3 (board->.hex->avrdude
    table, lines 162-166), Q4 (fresh-venv recipe, lines 195-209), Q6 (avrdude
    prerequisite, lines 230-241). PATTERNS.md "beta-testing-install.md" section.
  </read_first>
  <action>
    Create firestarter_app/doc/beta-testing-install.md as an operator-canonical,
    stranger-oriented onboarding doc (D-09), matching the voice + structure of
    community-validation.md. Include, in this order:
    (1) A lead paragraph naming the audience ("a community member on a fresh
    machine who wants to help test PROMs") and the one-sentence purpose (install
    the beta, flash beta firmware, then run dev test).
    (2) An avrdude prerequisite section: install avrdude (apt/brew or via
    PlatformIO's toolchain); avrdude >= 7.0 needs no config path, 6.3 needs
    `-c/--avrdude-config-path`; the app auto-detects avrdude on PATH else pass
    `--avrdude-path` (per D-23/RESEARCH Q6).
    (3) A per-board table mapping bench board -> PIO board name -> the exact
    firestarter_<board>.hex artifact -> avrdude partno/programmer/baud: Arduino Uno
    -> uno -> firestarter_uno.hex -> atmega328p/arduino/115200; Arduino Leonardo ->
    leonardo -> firestarter_leonardo.hex -> atmega32u4/avr109/57600; uno328pb ->
    uno328pb -> firestarter_uno328pb.hex -> atmega328pb/urclock/115200
    (per RESEARCH Q3).
    (4) The per-board command sequence a stranger runs (fresh venv ->
    `pip install --pre firestarter` -> `firestarter --version` should print the
    3.0.0bN beta -> bare `firestarter fw -i -b <board>` which auto-routes to the
    `--pre` channel per D-23/D-24, downloads the board-matching .hex, and avrdude
    flashes+verifies -> `firestarter fw` to confirm the version+board -> a single
    `firestarter hw` live op). State that `firestarter --version` must report the
    beta string, not a stale stable.
    (5) The /dev/ttyACM* controller-identity gotcha: port numbers shuffle across
    replug / board cycle, so re-check which port is which board before each flash
    (feedback_verify_port_identity_each_task).
    (6) An explicit "This is NOT a chip write/verify" note: the smoke test proves
    the flashed beta stack is alive and speaks the protocol; proving a chip works
    is dev test's job (ONBOARD-03 boundary).
    (7) A closing hand-off section linking to doc/community-validation.md as the
    next step (run `firestarter dev test <chip>` and file a report).
    Add a short note at the top that this doc is authored draft-first for the
    3.0.0bN beta and is finalized from live bench findings (D-04). Do NOT inline
    the actual b11 dispatch/release-engineering runbook — that is operator/CI
    ceremony, not stranger-facing content.
  </action>
  <verify>
    <automated>test -f firestarter_app/doc/beta-testing-install.md && grep -q 'firestarter_uno.hex' firestarter_app/doc/beta-testing-install.md && grep -q 'firestarter_leonardo.hex' firestarter_app/doc/beta-testing-install.md && grep -q 'firestarter_uno328pb.hex' firestarter_app/doc/beta-testing-install.md && grep -q 'community-validation' firestarter_app/doc/beta-testing-install.md && grep -qi 'avrdude' firestarter_app/doc/beta-testing-install.md && grep -qi 'ttyACM' firestarter_app/doc/beta-testing-install.md</automated>
  </verify>
  <acceptance_criteria>
    The doc exists at firestarter_app/doc/beta-testing-install.md; it names all
    three firestarter_<board>.hex artifacts, the avrdude prerequisite, the ttyACM
    controller-identity gotcha, the per-board command sequence, the "not a chip
    write" framing, and a hand-off link to community-validation.md.
  </acceptance_criteria>
  <done>firestarter_app/doc/beta-testing-install.md exists covering all D-09 content (per-board commands, avrdude prereq, ttyACM gotcha, correct .hex per board, dev test hand-off).</done>
</task>

<task type="auto">
  <name>Task 2: Add README pointer link (no duplication)</name>
  <files>firestarter_app/README.md</files>
  <read_first>
    firestarter_app/README.md — the existing "Installing the Firestarter Python
    Program" / "Beta / Pre-release Channel" / "Channel selection matrix" area
    (~lines 108-214) and its existing markdown-link style.
  </read_first>
  <action>
    Add a single one-line pointer link near the existing Beta / Pre-release Channel
    section pointing readers to doc/beta-testing-install.md for the full
    stranger-oriented per-board install+flash walkthrough. Match the README's
    existing markdown-link voice. Per D-09, do NOT copy the per-board table or the
    channel-selection matrix into the README — the README is already ~35 KB; it
    gets a pointer, not a duplicate.
  </action>
  <verify>
    <automated>grep -q 'beta-testing-install' firestarter_app/README.md</automated>
  </verify>
  <acceptance_criteria>
    firestarter_app/README.md contains exactly one pointer link to
    doc/beta-testing-install.md, in the beta/install area, with no duplicated
    per-board matrix.
  </acceptance_criteria>
  <done>firestarter_app/README.md contains one pointer link to doc/beta-testing-install.md in the beta/install area, no duplicated matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc -> community reader | A stranger follows the doc's commands verbatim against their own hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-DOC | Tampering (hardware, via bad instructions) | beta-testing-install.md per-board table | medium | mitigate | Board->.hex table sourced verbatim from RESEARCH Q3 (verified against platformio.ini + name_firmware.py); the correct firestarter_<board>.hex per board is spelled out so a reader cannot flash a signature-mismatched image; finalized against live bench findings in Plan 08. |
| T-115-SC | Tampering | pip / avrdude install guidance | low | accept | Doc directs installing the project's own published `firestarter` package + upstream avrdude only; no new third-party dependency is introduced (v1.21 no-new-deps lock). |
</threat_model>

<verification>
- firestarter_app/doc/beta-testing-install.md exists and covers all D-09 required
  content (per-board commands, avrdude prereq, ttyACM gotcha, correct .hex per
  board, dev test hand-off).
- firestarter_app/README.md links to it without duplicating the matrix.
- No source module, chip_database.json, or firmware file changed.
</verification>

<success_criteria>
The draft onboarding doc is committed to the firestarter_app v1.21 branch so the
subsequent `3.0.0b11` cut (Plan 04) ships with it (D-04 draft-first). README
points to it.
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-01-SUMMARY.md` when done.
</output>
