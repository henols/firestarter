---
phase: 115-beta-channel-install-and-firmware-flash-bench-validation-for
plan: 06
type: execute
wave: 4
depends_on: [115-03, 115-04]
files_modified:
  - chip-test/onboard-leonardo.md
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]
must_haves:
  truths:
    - "In a fresh throwaway venv, pip install --pre firestarter installs 3.0.0b11 and firestarter --version reports it (Leonardo run)"
    - "A bare firestarter fw -i -b leonardo auto-routes to --pre (D-23/D-24), downloads firestarter_leonardo.hex, and avrdude flashes + verifies (including the 1200-baud touch reset)"
    - "Post-flash, firestarter fw reports 3.0.0b11 + controller: leonardo, and firestarter hw (live op, no chip) succeeds"
    - "chip-test/onboard-leonardo.md records host_version 3.0.0b11, the resolved fw -i channel + downloaded asset, the avrdude flash+verify output, and the smoke-op result"
  artifacts:
    - path: chip-test/onboard-leonardo.md
      provides: "Leonardo per-board install->flash->smoke evidence record (verdict table + fenced JSON)"
      contains: "3.0.0b11"
  key_links:
    - from: "fresh-venv installed 3.0.0b11 app"
      to: "--pre channel"
      via: "bare fw -i auto-route (_maybe_auto_route_to_pre, D-23/D-24)"
      pattern: "pre"
    - from: "firestarter fw -i -b leonardo"
      to: "firestarter_leonardo.hex asset"
      via: "board override -> fetch_release_info asset match -> avr_tool avr109 flash+verify (atmega32u4, 1200-baud touch reset)"
      pattern: "firestarter_leonardo.hex"
  prohibitions:
    - "MUST NOT run a chip write/verify in the smoke test (ONBOARD-03 — fw + hw only)"
    - "MUST NOT use the operator's editable -e install or ~/.firestarter config — fresh venv + FIRESTARTER_CONFIG_DIR exported BEFORE any firestarter call (D-07)"
    - "MUST NOT record a false green — any blank/failed field is recorded honestly (D-08)"
---

<objective>
Run the full community onboarding chain on the Arduino Leonardo (a HARD pass/fail
gate, D-05) in a fresh isolated environment and capture a per-board evidence
record. Proves ONBOARD-01/02/03 on real hardware for the Leonardo: fresh-venv
beta install -> bare `fw -i` auto-route + `firestarter_leonardo.hex` flash+verify
(the avr109/atmega32u4 path with a 1200-baud touch reset) -> `fw` + `hw` smoke.

Purpose: The Leonardo is the second HARD-gate board (and the milestone's pinned
bench oracle). Output: `chip-test/onboard-leonardo.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-CONTEXT.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-RESEARCH.md
@.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-PATTERNS.md
@chip-test/dev-test-w27c512.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator connects + identifies the Arduino Leonardo</name>
  <read_first>
    memory feedback_verify_port_identity_each_task (ttyACM* numbers shuffle across
    replug). RESEARCH.md Q3 board-detection (lines 170-176). Leonardo needs a
    1200-baud touch reset before flashing (avr_tool _trigger_reset, RESEARCH Q6
    line 236) and may re-enumerate on a different port during flashing.
  </read_first>
  <action>
    Halt for the operator to physically connect + power the Arduino Leonardo and
    confirm which serial port it enumerated on. Record the confirmed port for
    Task 2. Note the Leonardo re-enumerates across the 1200-baud reset; the agent
    handles that, the operator confirms the physical identity.
  </action>
  <what-built>
    Both channels are public at 3.0.0b11 (Plans 03/04). The Leonardo run needs the
    physical board connected + powered and its serial port confirmed. Note the
    Leonardo re-enumerates across the 1200-baud reset, so its bootloader port may
    differ from its run port — the operator confirms the physical board is the
    Leonardo; the agent handles the reset/enumeration.
  </what-built>
  <how-to-verify>
    Operator connects + powers the Arduino Leonardo and states which serial port
    it enumerated on. The agent may probe `firestarter fw` on the candidate ports
    to read the `controller:` identity; the operator confirms the physical board
    is the Leonardo (not the Uno / uno328pb).
  </how-to-verify>
  <resume-signal>
    Operator replies "Leonardo connected on /dev/ttyXXX" (naming the confirmed port).
  </resume-signal>
  <acceptance_criteria>
    The Leonardo is powered and its serial port is confirmed and recorded for Task 2.
  </acceptance_criteria>
  <done>The Leonardo is powered and its serial port confirmed + recorded for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-venv install -> fw -i flash -> fw/hw smoke -> evidence record</name>
  <files>chip-test/onboard-leonardo.md</files>
  <read_first>
    RESEARCH.md Q4 fresh-venv recipe + isolation gotchas (lines 190-209), Q5 smoke
    commands (lines 218-226), Q6 avrdude / Leonardo 1200-baud reset (lines
    230-241). chip-test/dev-test-w27c512.md (evidence-record shape).
  </read_first>
  <action>
    Using the confirmed Leonardo port, run the isolated onboarding chain as a
    stranger would (D-07): throwaway venv, `pip install --pre firestarter` (pulls
    3.0.0b11), export FIRESTARTER_CONFIG_DIR to a clean temp dir BEFORE invoking
    the venv's firestarter shim (import-time gotcha). Then:
    (1) `firestarter --version` — MUST print 3.0.0b11 (ONBOARD-01);
    (2) bare `firestarter fw -i -b leonardo` (installed-beta auto-route to --pre,
    D-23/D-24; -b leonardo resolves firestarter_leonardo.hex; avr_tool applies the
    1200-baud touch reset then flashes via avr109/atmega32u4 and verifies) —
    capture the resolved channel, downloaded asset name, and avrdude flash+verify
    output (ONBOARD-02);
    (3) `firestarter fw` — capture the "3.0.0b11, for controller: leonardo"
    identity line (ONBOARD-03 part 1);
    (4) `firestarter hw` — the one minimal live protocol op (ONBOARD-03 part 2;
    NOT a chip write/verify).
    Handle the Leonardo re-enumeration around the reset (the port may change);
    pass the confirmed/observed port explicitly if auto-detect is ambiguous. Write
    chip-test/onboard-leonardo.md mirroring dev-test-w27c512.md: verdict table
    (install, --version, fw -i (channel+asset), avrdude flash+verify, fw
    (version+board), hw) + fenced JSON (schema_version, generated timestamp,
    auto_capture.host_version 3.0.0b11, resolved channel + asset, avrdude return,
    hw result). Record blank/failed fields honestly (D-08); never a false green.
    Teardown venv + config dir. HARD gate — all steps must be OK to pass.
  </action>
  <verify>
    <automated>test -f chip-test/onboard-leonardo.md && grep -q '3.0.0b11' chip-test/onboard-leonardo.md && grep -q 'firestarter_leonardo.hex' chip-test/onboard-leonardo.md</automated>
  </verify>
  <acceptance_criteria>
    chip-test/onboard-leonardo.md exists with host_version 3.0.0b11, the resolved
    --pre channel + firestarter_leonardo.hex asset, the avrdude flash+verify
    result, and the fw+hw smoke outcomes. For the HARD Leonardo gate, all steps
    are OK (or the record shows the exact failure verbatim and the gate is failed
    honestly).
  </acceptance_criteria>
  <done>chip-test/onboard-leonardo.md records host_version 3.0.0b11, --pre channel + firestarter_leonardo.hex, avrdude flash+verify, and fw+hw smoke — all OK for the HARD Leonardo gate (or an honest failure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI + GitHub prerelease -> fresh venv | Published artifacts install here |
| host avrdude -> Leonardo over USB | Firmware image is written across the 1200-baud-reset boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-02 | Tampering (hardware) | wrong .hex flashed to the Leonardo | medium | mitigate | Explicit `-b leonardo` forces firestarter_leonardo.hex; evidence record captures the resolved asset; atmega32u4 signature check rejects a wrong-signature image. |
| T-115-09 | Denial of service | Leonardo lost after the 1200-baud reset (port re-enumeration) | medium | mitigate | The agent tracks re-enumeration and retries on the bootloader port; a stuck board is recorded honestly, not retried into a false green. |
| T-115-04 | Tampering | fw -i silent stable fallback | medium | mitigate | Evidence record captures the resolved channel + tag (--pre / 3.0.0b11), not just "flash OK". |
| T-115-SC | Malicious code / supply chain | pip install --pre firestarter | low | accept | Installs only the project's own published package; no new third-party dependency. |
</threat_model>

<verification>
- chip-test/onboard-leonardo.md exists with the verdict table + fenced JSON.
- host_version == 3.0.0b11; resolved channel --pre; asset firestarter_leonardo.hex.
- avrdude flash+verify recorded; fw reports controller: leonardo; hw succeeded.
- Fresh venv + clean FIRESTARTER_CONFIG_DIR used (no -e install, no chip write).
</verification>

<success_criteria>
The Leonardo HARD gate passes: a fresh-machine beta install flashes the Leonardo
beta firmware and the flashed stack is alive (fw + hw), captured in
chip-test/onboard-leonardo.md.
</success_criteria>

<output>
Create `.planning/phases/115-beta-channel-install-and-firmware-flash-bench-validation-for/115-06-SUMMARY.md` when done.
</output>
