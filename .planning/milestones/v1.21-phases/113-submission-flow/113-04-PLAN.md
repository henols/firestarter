---
phase: 113-submission-flow
plan: 04
type: execute
wave: 4
depends_on: [113-03]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/test_dev_test_cmd.py
  - firestarter_app/tools/check_devtest_orchestrator.py
  - firestarter_app/tests/test_check_devtest_orchestrator.py
autonomous: true
requirements: [SUB-01, SUB-02]
must_haves:
  truths:
    - "dev_test gains a `--submit` Click flag (is_flag, default False) and a `submit: bool` parameter; a bare `dev test <chip>` run (submit omitted) never calls submit_report (no submission side effect)"
    - "When --submit is passed, after the report is rendered and always-persisted, the handler lazily imports submit and calls submit_report(report, chip, json_file, console=console) — consuming the in-memory report + saved json path, never re-running the sweep"
    - "The SAFE-03 orchestrator checker gains a THIRD full-scan leg for submit.py (FIRESTARTER_DEVTEST_SUBMIT env-override); the real submit.py passes the checker (0 VPP-set, 0 wire-dict, 0 force pass-through) and the checker's PASS line names submit.py"
    - "A planted-violation fixture injected via FIRESTARTER_DEVTEST_SUBMIT flips the checker non-zero (anti-hollow proof for the new leg), and a clean fixture through the same seam still passes"
  artifacts:
    - "firestarter_app/firestarter/cli_handlers.py :: --submit flag + submit param + lazy submit_report call site in dev_test"
    - "firestarter_app/tools/check_devtest_orchestrator.py :: FIRESTARTER_DEVTEST_SUBMIT constant + submit.py full-scan leg in main() + _assert_host_only coverage"
    - "firestarter_app/tests/test_dev_test_cmd.py :: --submit end-to-end tests (mock seams)"
    - "firestarter_app/tests/test_check_devtest_orchestrator.py :: planted + clean submit.py-leg fixtures"
  key_links:
    - "the call site passes the in-memory `report`, `chip`, and the already-resolved `json_file` path (cli_handlers.py:1881) into submit_report — no re-run (RESEARCH §Anti-Patterns #1)"
    - "the --submit test injects submit_report's seams (which_fn/run_fn/browser_open/isatty_fn/confirm_fn) via patching so CliRunner tests never touch PATH/network/browser/TTY"
    - "the new checker leg mirrors the existing FIRESTARTER_DEVTEST_SRC full-scan aggregation (check_devtest_orchestrator.py:337,350-355) and _assert_host_only firmware guard"
  prohibitions:
    - "The --submit call site and submit.py add no VPP-set, no wire-dict literal, and no force-override token — the SAFE-03 checker (now scanning submit.py) must stay green"
    - "Never submit as a side effect of a bare `dev test` run — submission requires the explicit --submit flag (SUB-02)"
    - "Do NOT reuse -y/--yes for submission — it is scoped to the --destructive chip-sacrifice confirm only"
    - "Never widen the scoped cli_handlers.py handler scan into a whole-file scan (10 pre-existing legitimate force flags on unrelated commands would false-positive)"
    - "Zero new firmware dispatch entries, zero new third-party dependency"
---

<objective>
Wire the completed `submit.py` into the CLI and extend the SAFE-03 orchestrator gate to cover
the new module. Add the `--submit` flag to the `dev test` handler with a lazy `submit_report`
call site (consuming the in-memory report, never re-running the sweep), prove it end-to-end via
`CliRunner` with injected seams, and add `submit.py` as a third full-scan leg of
`check_devtest_orchestrator.py` with an anti-hollow planted-violation test.

Purpose: Close the phase — `--submit` becomes a real, explicit, interactive-only CLI action
(SUB-01/02), and the milestone's SAFE-02/SAFE-03 non-regression invariant is extended to the new
orchestrator module so it can never silently grow a VPP-set, a wire-dict, or a force pass-through.

Output: `--submit` flag + call site in `dev_test`; `FIRESTARTER_DEVTEST_SUBMIT` scan leg;
end-to-end `--submit` tests; planted + clean submit.py-leg checker fixtures.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-submission-flow/113-CONTEXT.md
@.planning/phases/113-submission-flow/113-RESEARCH.md
@.planning/phases/113-submission-flow/113-PATTERNS.md
@.planning/phases/113-submission-flow/113-VALIDATION.md

# Run all commands from the firestarter_app/ submodule root.
@firestarter_app/firestarter/submit.py
@firestarter_app/firestarter/cli_handlers.py
@firestarter_app/tools/check_devtest_orchestrator.py
@firestarter_app/tests/test_dev_test_cmd.py
@firestarter_app/tests/test_check_devtest_orchestrator.py
</context>

## Artifacts this phase produces (whole phase — for source-grounding / drift-exclusion)

Same phase-wide new-symbol list as 113-01/02/03. This plan adds: the `--submit` Click flag +
`submit: bool` parameter + lazy `submit_report` call site on `dev_test` (cli_handlers.py); the
`FIRESTARTER_DEVTEST_SUBMIT` env-override constant + submit.py full-scan leg
(check_devtest_orchestrator.py); and the paired end-to-end + planted-violation tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --submit flag + lazy submit_report call site in dev_test (+ end-to-end tests)</name>
  <files>firestarter_app/firestarter/cli_handlers.py, firestarter_app/tests/test_dev_test_cmd.py</files>
  <read_first>
    - firestarter_app/firestarter/cli_handlers.py:1751-1789 (the dev_test decorator stack + signature; --destructive / --output-dir / -y/--yes flags to sibling)
    - firestarter_app/firestarter/cli_handlers.py:1871-1902 (render → always-persist json_file → console.print → sys.exit — the call site lands after persist and before sys.exit)
    - firestarter_app/firestarter/submit.py (submit_report signature + seams)
    - firestarter_app/tests/test_dev_test_cmd.py:1-90 (make_app_context + _isolate_config_dir + the _is_interactive patch idiom; _CHIP_NO_ID / _CHIP_WITH_ID fixtures)
    - 113-RESEARCH.md §Wiring Point + Test Seams
  </read_first>
  <behavior>
    - `firestarter dev test <chip>` WITHOUT --submit renders + persists as today and never invokes submit_report (patch submit_report; assert not called).
    - `firestarter dev test <chip> --submit` invokes submit_report exactly once with the in-memory report, the chip name, and the resolved json_file path (assert the positional args' types/values via a patched submit_report).
    - The flag is a Click is_flag defaulting False; --submit does not change the exit code contract (exit code still derives from step verdicts).
    - End-to-end through submit_report with injected off-TTY seam: a `--submit` run off-TTY prints the body + URL and does not open a browser / run gh (reuses the Plan-03 D-04 behavior via the real submit_report with mock browser_open/run_fn).
  </behavior>
  <action>
    Add an `@click.option("--submit", is_flag=True, default=False, help=...)` to the dev_test
    decorator stack (sibling of --destructive/--output-dir/-y, cli_handlers.py:1774-1780) and a
    `submit: bool` parameter to `def dev_test(...)` (cli_handlers.py:1783-1789). Help text: files
    the run's report to the maintainer's GitHub tracker (explicit + interactive-only; never on a
    bare run). At the call site AFTER the always-persist + `console.print("Report written to
    ...")` (cli_handlers.py:1897) and BEFORE the `sys.exit(code)` (cli_handlers.py:1899-1902), add
    `if submit:` a LAZY import (`from firestarter import submit as submit_mod`) and
    `submit_mod.submit_report(report, chip, json_file, console=console)` — pass the in-memory
    report, chip, and the already-resolved json_file path (no re-run, RESEARCH §Anti-Patterns #1).
    Do NOT reuse -y/--yes. Keep the call clean of any force token / wire-dict so the scoped
    SAFE-03 handler scan stays green. Extend tests/test_dev_test_cmd.py with `--submit` cases named
    with the substring `submit`: (a) no --submit → patched submit_report NOT called; (b) --submit →
    called once with (report, chip, json_file); (c) an off-TTY --submit end-to-end via the real
    submit_report with a Mock browser_open + run_fn asserting neither is called (D-04). Use the
    existing make_app_context + _is_interactive patch seam.
  </action>
  <verify>
    <automated>pytest tests/test_dev_test_cmd.py -k submit -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_dev_test_cmd.py -k submit -x` exits 0 (VALIDATION row: SUB-01/02 integration).
    - A test asserts a bare `dev test <chip>` never calls submit_report (no submission side effect, SUB-02).
    - A test asserts `--submit` calls submit_report once with the in-memory report + chip + json_file path.
    - The lazy import keeps submit.py off every other command's import path (import is inside the `if submit:` block).
    - `python tools/check_devtest_orchestrator.py` still exits 0 (the scoped cli_handlers handler scan stays green after adding the flag + call site).
    - `ruff check firestarter/ tests/` + `ruff format --check firestarter/ tests/` pass.
  </acceptance_criteria>
  <done>`--submit` is a Click flag wired to a lazy submit_report call consuming the in-memory report; a bare run never submits; the scoped SAFE-03 handler scan stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SAFE-03 — add submit.py as a third full-scan leg + anti-hollow test</name>
  <files>firestarter_app/tools/check_devtest_orchestrator.py, firestarter_app/tests/test_check_devtest_orchestrator.py</files>
  <read_first>
    - firestarter_app/tools/check_devtest_orchestrator.py:76-98 (the env-overridable path-constant idiom to copy for FIRESTARTER_DEVTEST_SUBMIT)
    - firestarter_app/tools/check_devtest_orchestrator.py:321-396 (main(): targets list, full-scan aggregation, _assert_host_only loop, scanned-empty fail-closed guard, PASS line)
    - firestarter_app/tools/check_devtest_orchestrator.py:231-247 (_scan_file — submit.py is a fresh module scanned in FULL, like chip_test.py)
    - firestarter_app/tests/test_check_devtest_orchestrator.py:62-101,168-186 (clean-pass baseline + planted-violation + clean-env-override patterns to mirror for the new leg)
    - 113-RESEARCH.md §SAFE-03 Orchestrator Checker (recommendation to add submit.py as a third full-scan target)
  </read_first>
  <behavior>
    - The checker scans submit.py IN FULL (not scoped) — submit.py is a fresh orchestrator module
      with zero pre-existing force flags, like chip_test.py.
    - The real submit.py passes: `python tools/check_devtest_orchestrator.py` exits 0 and the PASS
      line names submit.py (proving the leg actually ran, not skipped).
    - A planted-violation fixture (e.g. a VPP-set call or a force=True pass-through) injected via
      FIRESTARTER_DEVTEST_SUBMIT flips the checker non-zero with a FAIL summary.
    - A clean fixture injected via the same env-override still passes (proves the seam is a
      faithful re-target).
  </behavior>
  <action>
    In check_devtest_orchestrator.py add `_DEFAULT_DEVTEST_SUBMIT = os.path.join(_HERE, "..",
    "firestarter", "submit.py")` and `FIRESTARTER_DEVTEST_SUBMIT =
    os.environ.get("FIRESTARTER_DEVTEST_SUBMIT", _DEFAULT_DEVTEST_SUBMIT)`, mirroring the
    FIRESTARTER_DEVTEST_SRC constant (lines 76-98). In main(): append FIRESTARTER_DEVTEST_SUBMIT to
    the `targets` list (so _assert_host_only covers it), add a `submit_visitor =
    _scan_file(FIRESTARTER_DEVTEST_SUBMIT)` leg that — when not None — appends to `scanned` and
    extends the three violation buckets (mirror the FIRESTARTER_DEVTEST_SRC full-scan aggregation
    at 350-355). submit.py is scanned in FULL via _scan_file (fresh module, zero pre-existing force
    usage) — do NOT use the scoped _scan_target_functions path (that is only for the large
    pre-existing cli_handlers.py). The scanned-empty fail-closed guard already protects the new
    leg. Extend tests/test_check_devtest_orchestrator.py with: (a) a planted submit-shaped fixture
    (a function with op.set_vpp(...) or force=True) injected via FIRESTARTER_DEVTEST_SUBMIT →
    non-zero + FAIL; (b) a clean submit-shaped fixture via the same env-override → 0 + PASS; (c)
    re-assert the real-source baseline exits 0 and the PASS line now names submit.py. Update the
    module docstring's scanned-source description to mention the submit.py leg.
  </action>
  <verify>
    <automated>python tools/check_devtest_orchestrator.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/check_devtest_orchestrator.py` exits 0 and its PASS line names submit.py (VALIDATION row: SAFE-03).
    - `pytest tests/test_check_devtest_orchestrator.py -x` exits 0, including the new planted-submit-leg (non-zero) and clean-submit-leg (zero) fixtures — proving the leg is not hollow.
    - submit.py is scanned in FULL (via _scan_file), and FIRESTARTER_DEVTEST_SUBMIT is included in the _assert_host_only firmware guard.
    - `ruff check firestarter/ tests/ tools/` + `ruff format --check firestarter/ tests/ tools/` pass.
  </acceptance_criteria>
  <done>The SAFE-03 gate scans submit.py in full, passes on the real module (PASS line names it), and flips red on a planted violation through the new env-override — proven by the paired tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bare `dev test` run → submission | a submission must require the explicit --submit flag, never a side effect |
| new orchestrator module (submit.py) → firmware/hardware surface | must remain host-only: no VPP-set, no wire-dict, no force, no dispatch entry — now CI-enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-01 | Tampering / EoP | submit.py orchestrator scope | high | mitigate | SAFE-03 checker extended to scan submit.py in full; anti-hollow planted-violation test proves the leg fails on a real violation |
| T-113-04 | Repudiation | --submit explicit flag | high | mitigate | Submission requires the explicit --submit flag + (in submit_report) a TTY confirm; a bare run never submits — asserted end-to-end |
| T-113-05 | Tampering | submit target repo | high | mitigate | The wired call reaches submit_report whose SUBMIT_REPO is hardcoded (D-01); no CLI option exposes a configurable target |
| T-113-SC | Tampering (supply chain) | pip installs | low | accept | Zero package installs; zero new firmware dispatch entries (host-only, asserted by _assert_host_only) |
</threat_model>

<verification>
Run from `firestarter_app/`:
- `pytest tests/test_dev_test_cmd.py -k submit -x` — `--submit` wiring end-to-end (SUB-01/02).
- `python tools/check_devtest_orchestrator.py` — exits 0, PASS line names submit.py (SAFE-03).
- `pytest tests/test_check_devtest_orchestrator.py -x` — anti-hollow planted + clean submit-leg fixtures.
- Full suite: `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`.
- `ruff check firestarter/ tests/ tools/` + `ruff format --check firestarter/ tests/ tools/` — CI-3.11 style gate.
- `python tools/check_mypy_watermark.py` — mypy count at/below watermark.
</verification>

<success_criteria>
- `--submit` is a Click flag on dev_test wired to a lazy submit_report call consuming the in-memory report + saved json path; a bare run never submits (SUB-01/02).
- The SAFE-03 gate scans submit.py in full, passes on the real module, and flips red on a planted violation via FIRESTARTER_DEVTEST_SUBMIT (anti-hollow).
- Full suite green (cov >= 70), orchestrator gate green, ruff + watermark gates green.
</success_criteria>

<output>
Create `.planning/phases/113-submission-flow/113-04-SUMMARY.md` when done.
</output>
