---
phase: 111-measured-voltage-sampler-hardware-gated
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - firestarter_app/tests/test_hardware.py
  - firestarter_app/tests/test_diagnostic_report.py
autonomous: true
requirements: [VOLT-01]
must_haves:
  truths:
    - "Wave-0 RED tests exist for every VOLT-01 behavior in the VALIDATION.md test map (parse_voltage, sample_vpp, sample_vpe, sample_none, median, format_pin, report voltage split)."
    - "Every new sampler test builds synthetic 0xE4/0xE5 frames with build_frame + struct.pack('>HHHH', ...) — bench-free (D-05)."
    - "Existing read_vpp_voltage/read_vpe_voltage regression tests remain untouched (SC3)."
  artifacts:
    - "firestarter_app/tests/test_hardware.py (new sampler test functions added; existing tests unchanged)"
    - "firestarter_app/tests/test_diagnostic_report.py (new voltage-split serialization test added)"
  key_links:
    - "conftest.build_frame(0xE4|0xE5, struct.pack('>HHHH', v_int, v_dec, vcc_int, vcc_dec)) → the sampler under test"
    - "patched SerialCommunicator.find_and_connect → make_comm() → fake_serial.feed(...) harness (test_hardware.py:44-120)"
---

<objective>
Create the Wave-0 RED test scaffolds for the measured-voltage sampler (VOLT-01) BEFORE any implementation exists, satisfying the Nyquist validation contract (111-VALIDATION.md marks all sampler tests ❌ W0). These tests reference `sample_vpp_mv`/`sample_vpe_mv`/`_parse_voltage_frame` on `HardwareManager` and the split voltage fields on `DiagnosticReport` — symbols Plans 02/03 create. On this plan they are expected to FAIL (import/attribute errors) or be marked xfail-until-green; Plans 02/03 turn them GREEN.

Purpose: The test map in 111-VALIDATION.md requires seven automated verifications (six unit + one report-serialization), all currently non-existent. Writing them first pins the exact contract (KAT values, honest-fallback, format-string guard) the implementation must satisfy, and guarantees the implementation plans have a real RED→GREEN signal.
Output: New test functions in `test_hardware.py` and `test_diagnostic_report.py`. No production code touched. No existing test modified (SC3 regression tests stay byte-identical).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-CONTEXT.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase (111) creates the following NEW symbols. The source-grounding / drift pass MUST treat these as newly-created (not pre-existing, not drift):

- `HardwareManager._parse_voltage_frame(message: str) -> Optional[int]` (Plan 02, `hardware.py`)
- `HardwareManager._sample_one_voltage(state: int, n: int = 3, flags: int = 0) -> Optional[int]` (Plan 02, `hardware.py`)
- `HardwareManager.sample_vpp_mv(n: int = 3) -> Optional[int]` (Plan 02, `hardware.py`)
- `HardwareManager.sample_vpe_mv(n: int = 3) -> Optional[int]` (Plan 02, `hardware.py`)
- `DiagnosticReport.vpp_before_mv`, `.vpp_after_mv`, `.vpe_before_mv`, `.vpe_after_mv`, `.vpp_mv`, `.vpe_mv` fields (Plan 03, `diagnostic_report.py`; replace the removed `vpp_vpe_mv` slot)
- `DiagnosticReport._voltage_dict()` helper (Plan 03, `diagnostic_report.py`)

Tests referencing these symbols in THIS plan are expected RED until Plans 02/03 land.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 sampler unit-test scaffold in test_hardware.py</name>
  <files>firestarter_app/tests/test_hardware.py</files>
  <read_first>
    - firestarter_app/tests/test_hardware.py:1-120 (existing voltage-read harness: `hw_config` fixture, `make_comm`/`fake_serial`, `patch("firestarter.serial_comm.SerialCommunicator.find_and_connect", return_value=comm)`, `_ok_frame_bytes` = `b"OK: ready\n"`; the read_vpp/vpe tests to keep unchanged, SC3)
    - firestarter_app/tests/conftest.py:52-152 (`build_frame(msg_id, params)` = magic|len|id|params|crc|0x0A with auto CRC8; `fake_serial.feed(...)`; `make_comm` factory bypassing real serial I/O)
    - firestarter_app/firestarter/messages.py:681-698 (0xE4 `MSG_DATA_VPP_VOLTAGE` / 0xE5 `MSG_DATA_VPE_VOLTAGE`: `format="VPP: %u.%uV, Internal VCC: %u.%uV"` / `"VPE: ..."`, `params=4×("u16","dec")`, `param_bytes=8`)
    - firestarter_app/firestarter/hardware.py:166-263 (the handshake the sampler mirrors: `find_and_connect` → `expect_ack` → `send_ack` → `get_response` loop acking each DATA frame; `COMMAND_READ_VPP`=11 / `COMMAND_READ_VPE`=12 are the `state` values)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md:130-211 (§Make-or-Break: `Response.payload` is `None` for 0xE4/0xE5 — parse `Response.message`; wire carries whole volts + one tenths digit, reconstructed mV = `v_int*1000 + v_dec*100`, 100 mV grid) and lines 483-496 (synthetic-frame example)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-VALIDATION.md:44-49 (the six unit-test rows: parse_voltage, sample_vpp, sample_vpe, sample_none, median, format_pin)
  </read_first>
  <action>
    Add new test functions to `firestarter_app/tests/test_hardware.py` covering the six VOLT-01 unit behaviors from 111-VALIDATION.md. Do NOT modify or delete any existing test (SC3 regression set at :44-120 stays byte-identical). Name each test so a `-k` selector hits exactly one behavior:

    (a) `test_parse_voltage_frame_reconstructs_mv` (selector `parse_voltage`): construct a `HardwareManager(hw_config)`, call `hw._parse_voltage_frame("VPP: 20.9V, Internal VCC: 5.0V")`, assert `== 20900` (v_int*1000 + v_dec*100 per RESEARCH §Make-or-Break). Add a second assertion: `hw._parse_voltage_frame("VPE: 21.0V, Internal VCC: 5.0V") == 21000`. This is the Pitfall-2 KAT that pins the units contract (never read v_int alone as mV, never treat the wire as mV).

    (b) `test_sample_vpp_mv_returns_median` (selector `sample_vpp`): feed the ready handshake then three synthetic 0xE4 frames via `build_frame(0xE4, struct.pack(">HHHH", 20, 9, 5, 0))` (repeat feed ×3), patch `find_and_connect` → `make_comm()`, assert `hw.sample_vpp_mv() == 20900`. Include the import `import struct` at module top if absent.

    (c) `test_sample_vpe_mv_uses_state_12` (selector `sample_vpe`): same shape but feed 0xE5 frames via `build_frame(0xE5, struct.pack(">HHHH", 21, 0, 5, 0))` ×3 and assert `hw.sample_vpe_mv() == 21000`. Assert state 12 is used by capturing the `command_for_connect` dict passed to `find_and_connect` (via the patch's `call_args`) and checking `state == COMMAND_READ_VPE` (import `COMMAND_READ_VPE` from `firestarter.constants`).

    (d) `test_sample_returns_none_on_error` (selector `sample_none`): two sub-cases — (i) patch `find_and_connect` with `side_effect=ProgrammerNotFoundError("no port")` → `hw.sample_vpp_mv() is None`; (ii) feed the ready handshake then an `ERROR: simulated\n` frame (reuse `_error_frame_bytes()`) → `hw.sample_vpp_mv() is None`. Assert the return is EXACTLY `None`, never `0` (honest-fallback, D-04 / T-111-INPUT).

    (e) `test_sample_median_of_even_n_off_grid` (selector `median`): feed the ready handshake then two frames `build_frame(0xE4, struct.pack(">HHHH", 20, 9, 5, 0))` and `build_frame(0xE4, struct.pack(">HHHH", 21, 0, 5, 0))`, call `hw.sample_vpp_mv(n=2)`, assert the result is `int` type and `== 20950` (median of [20900, 21000] cast to int, Pitfall 5). This pins the even-N off-grid behavior.

    (f) `test_voltage_format_pin` (selector `format_pin`): import `from firestarter.messages import CATALOG`; assert the 0xE4 and 0xE5 entries' `format` strings each still match the sampler's tolerant regex `r"(\d+)\.(\d+)\s*V"` (Pitfall 3 guard — a codegen regen that changes the format string breaks the parser silently). Build the expected rendered string by `CATALOG[0xE4].format % (20, 9, 5, 0)` and assert `re.search(r"(\d+)\.(\d+)\s*V", rendered)` finds `("20","9")` as the FIRST match. Do the same for 0xE5.

    Model every serial test on the existing `test_read_vpp_voltage_finish_on_ok` pattern (:74-88): `fake_serial.feed(...)`; `comm = make_comm()`; `with patch("firestarter.serial_comm.SerialCommunicator.find_and_connect", return_value=comm): ...`. Because `sample_vpp_mv`/`_parse_voltage_frame` do not exist yet (Plan 02 creates them), these tests are RED on this plan — that is the intended Wave-0 state. Do NOT stub the production symbols to make them pass here.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_hardware.py -k "parse_voltage or sample_vpp or sample_vpe or sample_none or median or format_pin" --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_hardware.py --co -q` COLLECTS all six new tests (selectors parse_voltage, sample_vpp, sample_vpe, sample_none, median, format_pin) with no collection error.
    - Running `pytest tests/test_hardware.py -k "parse_voltage or sample_vpp" -x` FAILS with AttributeError/collection on the missing `_parse_voltage_frame`/`sample_vpp_mv` symbols (RED confirms the tests bind to real production symbols, not stubs).
    - `git diff tests/test_hardware.py` shows ONLY additions below the existing tests; the existing `test_read_vpp_voltage_*` / `test_read_vpe_voltage_*` bodies are unchanged (SC3).
    - Every new serial test feeds frames built via `build_frame(0xE4|0xE5, struct.pack(">HHHH", ...))` — no hand-assembled bytes, no hardware (D-05).
  </acceptance_criteria>
  <done>Six new named unit tests exist in test_hardware.py, all collect cleanly, all reference the real (not-yet-existing) sampler symbols, and no existing test was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 report voltage-split test scaffold in test_diagnostic_report.py</name>
  <files>firestarter_app/tests/test_diagnostic_report.py</files>
  <read_first>
    - firestarter_app/tests/test_diagnostic_report.py:1-60 (test taxonomy + bench-free seam: real `EpromDatabase(skip_local_override=True)` + `Mock(spec=[...])` operator; analog tests `test_transport_not_measured` at :219 and `test_dual_render_single_source` at :144)
    - firestarter_app/firestarter/diagnostic_report.py:42-43 (`NOT_MEASURED = "not measured"` sentinel) + :283-401 (the `DiagnosticReport` dataclass, current `vpp_vpe_mv: int | None = None` slot at :303, `_transport_dict()` NOT_MEASURED substitution pattern at :324-336, single-source `to_dict()` at :379-401)
    - firestarter_app/firestarter/diagnostic_report.py:397-463 (`render()` sources from `d = self.to_dict()` — the single-source contract, D-01)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md:349-398 (Pattern 2: split field list `vpp_before_mv`/`vpp_after_mv`/`vpe_before_mv`/`vpe_after_mv`/`vpp_mv`/`vpe_mv`, nested `"voltage": {...}` sub-dict, `NOT_MEASURED` substitution lives in `to_dict()` only)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-VALIDATION.md:51 (the split-field serialization row)
  </read_first>
  <action>
    Add a new test function to `firestarter_app/tests/test_diagnostic_report.py` for the D-01 voltage split (selector `voltage`). Do NOT modify existing tests. Reuse the file's existing report-construction helper/fixtures (the same `AutoCapture`/`TransportHealth`/`Plan` composition used by `test_transport_not_measured` at :219 — copy its construction shape).

    `test_voltage_split_fields_serialize` (selector `voltage`): build a `DiagnosticReport` two ways and assert on `to_dict()["voltage"]`:
    (a) destructive-run shape: set `vpp_before_mv=20900`, `vpp_after_mv=17400`, `vpe_before_mv=23900`, `vpe_after_mv=23800`, leave `vpp_mv`/`vpe_mv` as `None`; assert `to_dict()["voltage"]["vpp_before_mv"] == 20900`, `...["vpp_after_mv"] == 17400`, `...["vpe_before_mv"] == 23900`, `...["vpe_after_mv"] == 23800`, and BOTH `...["vpp_mv"] == NOT_MEASURED` and `...["vpe_mv"] == NOT_MEASURED` (import `NOT_MEASURED` from `firestarter.diagnostic_report`).
    (b) non-destructive standalone shape: fresh report with `vpp_mv=20900`, `vpe_mv=23900`, all four before/after `None`; assert `...["vpp_mv"] == 20900`, `...["vpe_mv"] == 23900`, and all four before/after keys `== NOT_MEASURED` (never a false `0`, D-04 honest-fallback).
    (c) single-source assertion: mirror `test_dual_render_single_source` (:144) — call `report.render()` and assert the rendered table exposes a voltage row consistent with `to_dict()["voltage"]` (e.g. the rendered string contains `"20900"` or the row is present), proving `render()` sources from `to_dict()` and does not maintain a second field list (D-01).

    Because the split fields (`vpp_before_mv` etc.) and the `"voltage"` dict key do not exist yet (Plan 03 creates them; the current field is `vpp_vpe_mv`), this test is RED on this plan — the intended Wave-0 state. Do NOT add the fields to the dataclass here.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_diagnostic_report.py -k voltage --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_diagnostic_report.py -k voltage --co -q` collects `test_voltage_split_fields_serialize` with no collection error.
    - Running `pytest tests/test_diagnostic_report.py -k voltage -x` FAILS (the `vpp_before_mv` field / `"voltage"` dict key do not exist yet) — RED confirms the test binds to the real split contract.
    - `git diff tests/test_diagnostic_report.py` shows only additions; existing tests (`test_transport_not_measured`, `test_dual_render_single_source`, etc.) unchanged.
    - The test asserts absent readings serialize to `NOT_MEASURED`, never `0`.
  </acceptance_criteria>
  <done>test_voltage_split_fields_serialize exists, collects cleanly, asserts both the destructive before/after and non-destructive standalone shapes with NOT_MEASURED fallback and single-source render, and no existing test was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device (firmware) → host parser | 0xE4/0xE5 DATA frames are device-supplied; the sampler-under-test treats them as untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-INPUT | Tampering (V5 input validation) | `_parse_voltage_frame` / `_sample_one_voltage` (tested here) | medium | mitigate | Wave-0 `sample_none` + `parse_voltage` tests assert malformed/error frames return `None`, never a fabricated `0` — pins the honest-fallback contract before implementation |
| T-111-DRIFT | Denial-of-info | 0xE4/0xE5 catalog format string | low | mitigate | Wave-0 `format_pin` test pins the `CATALOG[0xE4/0xE5].format` against the tolerant regex so a codegen regen can't silently disable the sampler |
| T-111-SC | Tampering | package installs | low | accept | No new packages this phase (stdlib `re`/`statistics`/`struct` only) — no legitimacy gate needed |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_hardware.py tests/test_diagnostic_report.py --co -q` collects all new tests with zero collection errors.
- New sampler/report tests are RED (bind to not-yet-existing symbols) — the intended Wave-0 state.
- Existing regression tests remain byte-identical (SC3): `git diff --stat` shows only additions in the two test files.
</verification>

<success_criteria>
- All seven VOLT-01 verifications from 111-VALIDATION.md have a corresponding named test in the suite.
- Every sampler test is bench-free (synthetic `build_frame` frames, patched `find_and_connect`).
- No production code and no existing test modified.
</success_criteria>

<output>
Create `.planning/phases/111-measured-voltage-sampler-hardware-gated/111-01-SUMMARY.md` when done
</output>
