---
phase: 111-measured-voltage-sampler-hardware-gated
plan: 03
type: execute
wave: 1
depends_on: ["111-01"]
files_modified:
  - firestarter_app/firestarter/diagnostic_report.py
autonomous: true
requirements: [VOLT-01]
must_haves:
  truths:
    - "DiagnosticReport's single combined vpp_vpe_mv slot is split into VPP/VPE before-after (destructive) + standalone (non-destructive) fields (D-01/D-03/D-04)."
    - "Every absent voltage reading serializes to NOT_MEASURED, never a false 0 (honest fallback, D-04)."
    - "The NOT_MEASURED substitution lives in one place (a _voltage_dict helper feeding to_dict); render() sources from to_dict() — single-source contract preserved (Phase 110 D-01)."
    - "The report module imports no transport/hardware class (SAFE-02 orchestrator-only structural scan stays green)."
  artifacts:
    - "firestarter_app/firestarter/diagnostic_report.py (vpp_vpe_mv removed; vpp_before_mv/vpp_after_mv/vpe_before_mv/vpe_after_mv/vpp_mv/vpe_mv fields + _voltage_dict + voltage block in to_dict + render row)"
  key_links:
    - "DiagnosticReport voltage fields → _voltage_dict() (NOT_MEASURED substitution) → to_dict()['voltage'] → render() table row"
    - "sampler median mV (Phase 112 orchestrator) → these fields on a destructive/non-destructive run"
---

<objective>
Split the Phase-110 `DiagnosticReport.vpp_vpe_mv: int | None` slot into the D-01/D-03/D-04 voltage field set in `firestarter_app/firestarter/diagnostic_report.py`: `vpp_before_mv`, `vpp_after_mv`, `vpe_before_mv`, `vpe_after_mv` (destructive-run before/after per rail) plus `vpp_mv`, `vpe_mv` (non-destructive standalone). Surface them through a new `_voltage_dict()` helper into the single-source `to_dict()` (substituting `NOT_MEASURED` for any `None`), and add a `render()` row so the split values are human-visible (the current `vpp_vpe_mv` slot renders nothing). Keep the single-source render contract intact (Phase 110 D-01) — `render()` sources only from `to_dict()`.

Purpose: Give the sampler's median mV values (Plan 02) somewhere to land, split by rail and by before/after so a maintainer reads "the rail sagged 20.9V → 17.4V across the write" vs "the regulator never reached 21V" — two very different diagnoses. Phase 112's orchestrator populates these fields around `run_plan`.
Output: `diagnostic_report.py` gains six voltage fields + `_voltage_dict()`; the Plan-01 report test turns GREEN; the single-source and SAFE-02 structural tests stay green.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-CONTEXT.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-VALIDATION.md
@firestarter_app/firestarter/diagnostic_report.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split the combined slot into the D-01/D-03/D-04 field set + _voltage_dict helper</name>
  <files>firestarter_app/firestarter/diagnostic_report.py</files>
  <read_first>
    - firestarter_app/firestarter/diagnostic_report.py:38-43 (`SCHEMA_VERSION`, `NOT_MEASURED = "not measured"` sentinel)
    - firestarter_app/firestarter/diagnostic_report.py:277-299 (the `DiagnosticReport` dataclass fields incl. the `vpp_vpe_mv: int | None = None` slot at :303 whose docstring says it is "left for Phase 111's measured-voltage sampler")
    - firestarter_app/firestarter/diagnostic_report.py:318-330 (`_transport_dict()` — the canonical "ONE place the sentinel is substituted" NOT_MEASURED pattern to copy for `_voltage_dict()`)
    - firestarter_app/firestarter/diagnostic_report.py:373-395 (single-source `to_dict()`, incl. the `"vpp_vpe_mv": self.vpp_vpe_mv` line at :393 to replace)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md:349-398 (Pattern 2: exact field list, nested `"voltage": {...}` sub-dict, NOT_MEASURED substitution in `to_dict()` only, do NOT switch to `dataclasses.asdict()`)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md:95-114 (slot-to-split, NOT_MEASURED copy pattern, single-source contract to PRESERVE)
  </read_first>
  <behavior>
    - A report with `vpp_before_mv=20900, vpp_after_mv=17400, vpe_before_mv=23900, vpe_after_mv=23800` and `vpp_mv=None, vpe_mv=None` → `to_dict()["voltage"]` has those four numbers and `vpp_mv`/`vpe_mv` == `NOT_MEASURED`.
    - A report with `vpp_mv=20900, vpe_mv=23900` and all before/after `None` → `to_dict()["voltage"]` has `vpp_mv`/`vpe_mv` numbers and all four before/after == `NOT_MEASURED`.
    - No field ever serializes to `0` for an absent reading.
  </behavior>
  <action>
    <!-- planner-discipline-allow: vpp_vpe_mv -->
    The old combined-slot identifier (spelled `vpp_vpe_mv`) is named below ONLY so the executor knows the exact field to DELETE; it must NOT survive in the written source or any comment (the acceptance criterion negative-greps it to 0). Do NOT reintroduce it in a docstring, comment, or "renamed from" note.

    In `firestarter_app/firestarter/diagnostic_report.py`, edit the `DiagnosticReport` dataclass:

    (a) REMOVE the combined-slot field `vpp_vpe_mv: int | None = None` (:303) and REPLACE it with the six split fields, each `int | None = None`, in this order: `vpp_before_mv`, `vpp_after_mv`, `vpe_before_mv`, `vpe_after_mv`, `vpp_mv`, `vpe_mv`. Add a short comment tying them to D-01 (split), D-03 (before/after on destructive), D-04 (standalone on non-destructive). Rewrite the dataclass docstring line that references the old combined slot (:289-290) to describe the split fields instead — do not leave the old name in the prose.

    (b) ADD a `_voltage_dict(self) -> dict[str, Any]` method modeled EXACTLY on `_transport_dict()` (:324-336): return a dict with keys `vpp_before_mv`, `vpp_after_mv`, `vpe_before_mv`, `vpe_after_mv`, `vpp_mv`, `vpe_mv`, each value `NOT_MEASURED if self.<field> is None else self.<field>`. Its docstring states this is the ONE place the sentinel is substituted for a voltage field (mirrors the `_transport_dict` docstring) and cites the 100 mV grid + honest-fallback.

    (c) In `to_dict()` (:379-401), REPLACE the old-slot serialization line (:393, currently emitting the combined key) with `"voltage": self._voltage_dict(),`. Keep `to_dict()` hand-written (do NOT switch to `dataclasses.asdict()`, Pitfall 3) and keep it the single source. The NOT_MEASURED substitution must NOT be duplicated anywhere else.

    Do NOT import `SerialCommunicator`, `HardwareManager`, or any transport/hardware class into this module (SAFE-02 — the report model is orchestrator-only). Do NOT add a VPP-set or `--force` token.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_diagnostic_report.py -k voltage -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_diagnostic_report.py -k voltage -x` exits 0: both destructive (before/after) and non-destructive (standalone) shapes serialize correctly with absent readings == `NOT_MEASURED`.
    - `grep -c "vpp_vpe_mv" firestarter/diagnostic_report.py` returns 0 (old slot fully removed).
    - `grep -n "NOT_MEASURED if" firestarter/diagnostic_report.py` shows the six voltage substitutions live only inside `_voltage_dict` (single-source substitution preserved alongside the pre-existing `_transport_dict`).
    - `grep -nE "SerialCommunicator|HardwareManager|import .*hardware|vpp_mv *=|--force" firestarter/diagnostic_report.py` shows no transport/hardware import and no VPP-set/force token introduced (SAFE-02).
  </acceptance_criteria>
  <done>The six split voltage fields + _voltage_dict exist, vpp_vpe_mv is gone, to_dict emits a "voltage" block with NOT_MEASURED fallback, and the -k voltage test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the voltage block + confirm single-source and SAFE-02 stay green</name>
  <files>firestarter_app/firestarter/diagnostic_report.py</files>
  <read_first>
    - firestarter_app/firestarter/diagnostic_report.py:397-463 (`render()` — sources from `d = self.to_dict()` at :409; the existing `table.add_row(...)` rows for transport_health at :430-438 and banner at :440-441 to model the new voltage row on)
    - firestarter_app/tests/test_diagnostic_report.py:144 (`test_dual_render_single_source`) + :219 (`test_transport_not_measured`) + :238 (`test_report_module_is_orchestrator_only` — the SAFE-02 structural scan that must stay green)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md:113-114 (add ONE `table.add_row("voltage", ...)` line sourced from `d["voltage"]`, never a second field list)
  </read_first>
  <behavior>
    - `report.render()` produces a table containing a voltage row whose values come from `to_dict()["voltage"]` (e.g. a destructive report shows `20900`/`17400`; a non-destructive report shows the standalone `vpp_mv` and `NOT_MEASURED` for before/after).
    - `render()` maintains no second voltage field list — it reads only `d = self.to_dict()`.
    - The SAFE-02 orchestrator-only structural scan still passes.
  </behavior>
  <action>
    In `render()` (`firestarter_app/firestarter/diagnostic_report.py`), add exactly ONE voltage row sourced from the already-computed `d = self.to_dict()` — read `v = d["voltage"]` and `table.add_row("voltage", ...)` with a compact one-line summary string, e.g. formatting `vpp before/after`, `vpe before/after`, and the standalone `vpp_mv`/`vpe_mv` (model the format on the transport_health row at :430-438). Place it after the banner row (:441) and before the provenance block (:443), matching the existing row ordering. Do NOT read the dataclass fields directly in `render()` — read them via `d["voltage"]` (single-source contract, D-01).

    Make no other change. Confirm the module still imports no transport/hardware class and contains no VPP-set / `--force` token, so `test_report_module_is_orchestrator_only` (SAFE-02, :238) stays green.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_diagnostic_report.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_diagnostic_report.py -q` (whole file) exits 0 — the new `-k voltage` test, the pre-existing `test_dual_render_single_source`, `test_transport_not_measured`, and `test_report_module_is_orchestrator_only` (SAFE-02) all pass.
    - `render()` contains exactly one new voltage `add_row` and reads `d["voltage"]` (single-source); `grep -n "self.vpp_before_mv" firestarter/diagnostic_report.py` shows the field is referenced only inside `_voltage_dict`, not inside `render()`.
    - `cd firestarter_app && ruff check firestarter/diagnostic_report.py && ruff format --check firestarter/diagnostic_report.py` pass.
  </acceptance_criteria>
  <done>render() shows a voltage row sourced from to_dict(), the full test_diagnostic_report.py suite is green including single-source and SAFE-02 tests, and ruff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sampler → report model | measured mV values (or `None`) flow into `DiagnosticReport` fields; the model must never fabricate a `0` for an absent reading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-FALSE0 | Information disclosure (misleading data) | `_voltage_dict` / `to_dict` | medium | mitigate | Every absent reading → `NOT_MEASURED`, never `0`; single-source substitution in `_voltage_dict` only; Plan-01 test asserts the fallback |
| T-111-SAFE2 | Elevation-of-behavior | report module imports | high | mitigate | Report model imports no `SerialCommunicator`/`HardwareManager`; no VPP-set, no `--force`; `test_report_module_is_orchestrator_only` (SAFE-02) re-verified green |
| T-111-DRIFT | Denial-of-info | single-source render | low | mitigate | `render()` sources only from `to_dict()`; no second field list can silently diverge |
| T-111-SC | Tampering | package installs | low | accept | No new packages — stdlib only |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_diagnostic_report.py -q` — voltage split test + single-source + SAFE-02 all green.
- `grep -c "vpp_vpe_mv" firestarter/diagnostic_report.py` == 0 (old slot removed).
- `cd firestarter_app && ruff check firestarter/diagnostic_report.py && ruff format --check firestarter/diagnostic_report.py` — clean.
- Report model imports no transport/hardware class (SAFE-02 structural scan green).
</verification>

<success_criteria>
- D-01/D-03/D-04: the report exposes VPP/VPE before-after (destructive) + standalone (non-destructive) voltage fields.
- Honest fallback: absent readings serialize to `NOT_MEASURED`, never `0`.
- Single-source contract (Phase 110 D-01) preserved: `render()` reads only `to_dict()`.
</success_criteria>

<output>
Create `.planning/phases/111-measured-voltage-sampler-hardware-gated/111-03-SUMMARY.md` when done
</output>
