---
phase: 111-measured-voltage-sampler-hardware-gated
plan: 02
type: execute
wave: 1
depends_on: ["111-01"]
files_modified:
  - firestarter_app/firestarter/hardware.py
autonomous: true
requirements: [VOLT-01]
must_haves:
  truths:
    - "HardwareManager exposes value-returning sample_vpp_mv()/sample_vpe_mv() that parse 0xE4/0xE5 frames and return the MEDIAN mV reading over N=3 samples per rail (SC1, D-02)."
    - "The sampler reconstructs mV as v_int*1000 + v_dec*100 from the whole-volts + tenths wire values (100 mV grid), NOT by treating the wire value as mV."
    - "Any transport error / no-DATA / no-parse returns None, never a false 0 (honest fallback, D-04)."
    - "_read_voltage_loop, read_vpp_voltage, read_vpe_voltage are NOT modified — existing monitor tests stay green (SC3)."
    - "The sampler sends only {\"state\": 11|12} — no VPP-set, no raw wire-dict, no --force (SAFE-01/02/03)."
  artifacts:
    - "firestarter_app/firestarter/hardware.py (new _parse_voltage_frame, _sample_one_voltage, sample_vpp_mv, sample_vpe_mv on HardwareManager)"
  key_links:
    - "sample_vpp_mv/sample_vpe_mv → _sample_one_voltage(COMMAND_READ_VPP=11 | COMMAND_READ_VPE=12) → find_and_connect/expect_ack/send_ack/get_response handshake"
    - "_parse_voltage_frame(Response.message) → regex (\\d+)\\.(\\d+)\\s*V → v_int*1000 + v_dec*100"
    - "statistics.median(samples) cast to int → DiagnosticReport voltage fields (populated by Phase 112 orchestrator)"
---

<objective>
Implement the value-returning VPP/VPE millivolt sampler on `HardwareManager` in `firestarter_app/firestarter/hardware.py` (VOLT-01, SC1). Add four additive symbols beside the existing bool-returning monitor methods: `_parse_voltage_frame`, `_sample_one_voltage`, `sample_vpp_mv`, `sample_vpe_mv`. Use RESEARCH Pattern A (re-parse `Response.message`) — the raw 4×u16 payload is discarded before it reaches `Response` (`Response.payload` is `None` for 0xE4/0xE5), so CONTEXT §D-05's "parse raw param_bytes via `_decode_param`" premise is SUPERSEDED and MUST NOT be planned.

Purpose: Turn the existing print-only VPP/VPE monitor into a value the diagnostic report can record — "the write failed at 18.2V on a chip that needs 21V." This is the milestone's one hardware-gated capability, shipped software-complete + synthetic-frame-tested now; the live SC2 check (Leonardo + Rev 2.0) is deferred (D-05).
Output: `firestarter_app/firestarter/hardware.py` gains the four sampler symbols; the Wave-0 hardware tests from Plan 01 turn GREEN; the existing monitor methods are untouched (SC3).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-CONTEXT.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md
@.planning/phases/111-measured-voltage-sampler-hardware-gated/111-VALIDATION.md
@firestarter_app/firestarter/hardware.py
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _parse_voltage_frame + _sample_one_voltage to HardwareManager</name>
  <files>firestarter_app/firestarter/hardware.py</files>
  <read_first>
    - firestarter_app/firestarter/hardware.py:1-27 (module imports: `import logging`, `import time`, `from typing import Optional, Tuple`, `COMMAND_READ_VPE`/`COMMAND_READ_VPP` already imported from constants, exceptions tuple `HardwareOperationError`/`ProgrammerNotFoundError`/`SerialError`/`SerialTimeoutError`, `SerialCommunicator`)
    - firestarter_app/firestarter/hardware.py:166-263 (`_read_voltage_loop` — the exact handshake to MIRROR: `command_for_connect = {"state": state_to_set}`, optional `flags`, `find_and_connect` → `expect_ack()` → on `is_ok` `send_ack()` → `get_response()` loop where `response.type == "DATA"` and `response.message` carries the string; the except-tuple at :236-244; the `finally: comm.disconnect()` at :249-251) — DO NOT MODIFY this method or `read_vpp_voltage`/`read_vpe_voltage` (:253-263), SC3
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md:284-341 (Pattern 1 full code shape for `_parse_voltage_frame`/`_sample_one_voltage`) and :130-211 (§Make-or-Break: `Response.message` carries `"VPP: 20.9V, Internal VCC: 5.0V"`; `Response.payload` is None — parse the message; reconstructed mV = `v_int*1000 + v_dec*100`, 100 mV grid)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-PATTERNS.md:24-91 (imports-extend list, handshake mirror, error-handling copy-verbatim, parse+median pattern)
    - firestarter_app/tests/conftest.py:52-152 (the `build_frame` + `make_comm` + `fake_serial` seam the Plan-01 tests use)
  </read_first>
  <behavior>
    - `_parse_voltage_frame("VPP: 20.9V, Internal VCC: 5.0V")` returns `20900`; `_parse_voltage_frame("VPE: 21.0V, ...")` returns `21000` (v_int*1000 + v_dec*100).
    - `_parse_voltage_frame("garbage no match")` and `_parse_voltage_frame(None)` return `None` (honest fallback, never 0).
    - `_sample_one_voltage(COMMAND_READ_VPP, n=3)` reads 3 synthetic 0xE4 DATA frames and returns the median parsed mV.
    - Transport error / expect_ack not-ok / no DATA / no parseable sample → `None`.
  </behavior>
  <action>
    In `firestarter_app/firestarter/hardware.py`, add two new private methods to `HardwareManager`, placed AFTER `_read_voltage_loop`/`read_vpp_voltage`/`read_vpe_voltage` (do not edit those). Extend the module-top imports: add `import re` and `import statistics` alongside the existing `import time` (keep alphabetical/grouping consistent with the file's style).

    Add a module-level (or class-level) compiled regex `_VOLTAGE_RE = re.compile(r"(\d+)\.(\d+)\s*V")` — tolerant, matches the FIRST `%u.%uV` pair anywhere in a 0xE4/0xE5 message (Pitfall 3 guard against catalog format drift).

    `_parse_voltage_frame(self, message: str) -> Optional[int]`: search `_VOLTAGE_RE` against `message or ""`; if no match return `None`; else `v_int, v_dec = int(group1), int(group2)` and return `v_int * 1000 + v_dec * 100`. Docstring must state the 100 mV grid and the honest-fallback (`None`, never `0`). Parse `Response.message` — NEVER read `Response.payload` (it is `None` for voltage frames, RESEARCH §Make-or-Break / Pitfall 1).

    `_sample_one_voltage(self, state: int, n: int = 3, flags: int = 0) -> Optional[int]`: mirror the `_read_voltage_loop` handshake exactly but (a) stop after `n` frames via `for _ in range(n)` instead of `while True`, (b) parse+collect instead of `print(...)`, (c) return a value. Sequence: build `command_for_connect = {"state": state}`, add `command_for_connect["flags"] = flags` only if `flags`; `comm = None`; in a `try`: `comm = SerialCommunicator.find_and_connect(command_for_connect, self.config)`; `is_ok, _ = comm.expect_ack()`; if not `is_ok` return `None`; `comm.send_ack()` to start the loop; then `for _ in range(n)`: `resp = comm.get_response()`; if `resp.type != "DATA"` break; `mv = self._parse_voltage_frame(resp.message)`; if `mv is not None` append to a `samples: list[int]`; then `comm.send_ack()` to request the next reading (mirrors :219). Copy the except-tuple VERBATIM from :236-244 (`ProgrammerNotFoundError, SerialError, SerialTimeoutError, HardwareOperationError`) but `return None` (silent — no `print`, no `logger.error` required beyond an optional `logger.debug`). `finally: if comm: comm.disconnect()`. After the try, return `int(statistics.median(samples))` if `samples` else `None` (D-02: N samples per rail → report the MEDIAN, N default 3 per D-02's 3–5 range; median is robust against a single transient misread, this project's VPP-misread history). Type-annotate for consistency; note `hardware.py` is NOT in the strict-8 mypy set so no strict-mypy budget is consumed (Pattern A keeps the change inside `hardware.py`).

    Do NOT construct any wire-command dict beyond `{"state": ...}` (+ optional `flags`); do NOT set `vpp_mv`; do NOT pass `--force`/`force=True` (SAFE-01/02/03). `flags=0` is correct (RESEARCH A3 — the firmware read path consults no flag).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_hardware.py -k "parse_voltage or sample_none or median" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_hardware.py -k parse_voltage -x` exits 0: `_parse_voltage_frame("VPP: 20.9V, Internal VCC: 5.0V") == 20900`.
    - `pytest tests/test_hardware.py -k sample_none -x` exits 0: transport error AND error-frame both yield `None` (never `0`).
    - `pytest tests/test_hardware.py -k median -x` exits 0: even-N median is `int` and `== 20950`.
    - `grep -nE "Response\.payload|_decode_param" firestarter/hardware.py` returns nothing (Pattern A — never touch the payload path).
    - `grep -nE "vpp_mv|--force|force=True|set_vpp|enable_vpp" firestarter/hardware.py` shows no NEW sampler-introduced match (SAFE-01/02/03).
  </acceptance_criteria>
  <done>_parse_voltage_frame and _sample_one_voltage exist on HardwareManager, the parse/median/none tests pass, and the sampler reads Response.message (not payload) and sends only a state command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sample_vpp_mv / sample_vpe_mv wrappers + confirm SC3 regression</name>
  <files>firestarter_app/firestarter/hardware.py</files>
  <read_first>
    - firestarter_app/firestarter/hardware.py:253-263 (`read_vpp_voltage`/`read_vpe_voltage` — the bool-returning siblings the new value-returning wrappers sit beside; `COMMAND_READ_VPP`/`COMMAND_READ_VPE` usage pattern)
    - firestarter_app/firestarter/constants.py:66-67 (`COMMAND_READ_VPP = 11` / `COMMAND_READ_VPE = 12` — the `state` values; already imported in hardware.py)
    - .planning/phases/111-measured-voltage-sampler-hardware-gated/111-RESEARCH.md:336-347 (thin-wrapper shape + the SC3 structural argument: `_read_voltage_loop` and `read_vpp/vpe_voltage` are not edited)
    - firestarter_app/tests/test_hardware.py:74-120 (the SC3 regression tests that MUST stay green)
  </read_first>
  <behavior>
    - `sample_vpp_mv(n=3)` returns the median mV from 3 synthetic 0xE4 frames (`build_frame(0xE4, struct.pack(">HHHH", 20, 9, 5, 0))` ×3) → `20900`, using state 11.
    - `sample_vpe_mv(n=3)` returns `21000` from 0xE5 frames, using state 12.
    - Existing `read_vpp_voltage`/`read_vpe_voltage` tests still pass (SC3).
  </behavior>
  <action>
    In `firestarter_app/firestarter/hardware.py`, add two thin public wrappers to `HardwareManager` directly after the new `_sample_one_voltage`:

    `sample_vpp_mv(self, n: int = 3) -> Optional[int]`: `return self._sample_one_voltage(COMMAND_READ_VPP, n=n)`.
    `sample_vpe_mv(self, n: int = 3) -> Optional[int]`: `return self._sample_one_voltage(COMMAND_READ_VPE, n=n)`.

    Each gets a one-line docstring naming it as the value-returning sibling of `read_vpp_voltage`/`read_vpe_voltage` and stating the 100 mV resolution + `None` (never `0`) fallback. Both reuse the already-imported `COMMAND_READ_VPP` (11) / `COMMAND_READ_VPE` (12) — do NOT add a new command constant, do NOT add a firmware dispatch entry (SAFE-01).

    Do not touch `_read_voltage_loop`, `read_vpp_voltage`, or `read_vpe_voltage`. Confirm the whole `test_hardware.py` file (including the existing :74-120 monitor tests) passes — the additive sibling must not perturb the print-loop behavior (SC3).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_hardware.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_hardware.py -k sample_vpp -x` exits 0: `sample_vpp_mv() == 20900`, state == `COMMAND_READ_VPP` (11).
    - `pytest tests/test_hardware.py -k sample_vpe -x` exits 0: `sample_vpe_mv() == 21000`, state == `COMMAND_READ_VPE` (12).
    - `pytest tests/test_hardware.py -q` (whole file) exits 0 — all six new sampler tests GREEN AND the pre-existing `test_read_vpp_voltage_*`/`test_read_vpe_voltage_*` regression tests still pass (SC3).
    - `git diff firestarter/hardware.py` shows the bodies of `_read_voltage_loop`/`read_vpp_voltage`/`read_vpe_voltage` unchanged.
    - `cd firestarter_app && ruff check firestarter/hardware.py && ruff format --check firestarter/hardware.py` pass.
  </acceptance_criteria>
  <done>sample_vpp_mv/sample_vpe_mv exist and pass their tests, the full test_hardware.py suite is green including the SC3 regression tests, and ruff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware → host (`hardware.py` sampler) | device-supplied 0xE4/0xE5 DATA frames cross into the parser as untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-111-INPUT | Tampering (V5 input validation) | `_parse_voltage_frame` / `_sample_one_voltage` | medium | mitigate | Tolerant regex + `None` (never `0`) on any parse/transport failure; bounded `for _ in range(n)` loop; `get_response` timeout → `SerialTimeoutError` → `None`. No `assert` on device values. |
| T-111-DOS | Denial of service | sampler read loop | low | mitigate | Bounded `for _ in range(n)` loop (never `while True`) — cannot hang the sweep |
| T-111-SAFE | Elevation-of-behavior | sampler opening serial / touching VPP | high | mitigate | Sampler sends only `{"state": 11|12}` (monitor command); no `vpp_mv`, no raw wire-dict, no `--force`; reuses existing `COMMAND_READ_VPP/VPE`; zero new firmware dispatch entry (SAFE-01/02/03). The Phase-109 AST gate scans `chip_test.py` — this plan does not modify it (sampler stays in `hardware.py`). |
| T-111-SC | Tampering | package installs | low | accept | No new packages — stdlib `re` + `statistics` only |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_hardware.py -q` — all sampler tests GREEN + SC3 regression tests still pass.
- `cd firestarter_app && ruff check firestarter/hardware.py && ruff format --check firestarter/hardware.py` — clean.
- `grep -nE "Response\.payload" firestarter/hardware.py` — no match (Pattern A confirmed).
- The sampler introduces no VPP-set / raw-wire-dict / `--force`; `chip_test.py` is not modified, so the Phase-109 orchestrator AST gate stays clean by construction.
</verification>

<success_criteria>
- SC1: `HardwareManager` exposes value-returning `sample_vpp_mv()`/`sample_vpe_mv()` parsing 0xE4/0xE5 and returning median mV.
- SC3: `_read_voltage_loop`/`read_vpp_voltage`/`read_vpe_voltage` unchanged; existing monitor tests green.
- Honest fallback: any error/no-frame → `None`, never `0`.
</success_criteria>

<output>
Create `.planning/phases/111-measured-voltage-sampler-hardware-gated/111-02-SUMMARY.md` when done
</output>
