---
phase: 108-test-plan-engine-address-derived-pattern-fingerprint
plan: 04
type: execute
wave: 3
depends_on: ["108-01", "108-02", "108-03"]
files_modified:
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/tests/test_chip_test.py
autonomous: true
requirements: [SWEEP-02, SWEEP-03, SWEEP-04, RPT-03]
must_haves:
  truths:
    - "Each op runs as an independent non-fatal step with an OK/BAD/NA/SKIPPED verdict; a BAD or exception on one step never prevents remaining steps from running"
    - "The sweep runs id-check first; a chip-ID mismatch hard-gates all destructive steps SKIPPED for that run (chip pristine) while id/read findings are still recorded"
    - "Destructive/verify steps run N>=2; disagreement across runs reports marginal (never forced PASS/FAIL); runs<2 is rejected before any operator call"
    - "Each executed op routes through resolve_chip (guard-honoring); a resolver refusal is captured as SKIPPED/NA with reason, never bypassed"
    - "Each per-step result carries the exact firmware error_code captured from EpromOperationError.error_code"
  artifacts:
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/tests/test_chip_test.py
  key_links:
    - "check_eprom_id result -> destructive_gate flag -> every destructive step checks it first"
    - "resolve_chip(name, db) per executed op -> operator method (guard-honoring execution)"
    - "EpromOperationError.error_code (108-01) -> per-step result.error_code"
    - "generate_pattern + classify_fingerprint (108-02) -> write/verify step fingerprint"
---

<objective>
Add `run_plan()` to `chip_test.py` — the non-fatal step executor (SWEEP-02), the id-first
destructive gate (SWEEP-03), and the N≥2 marginal policy (SWEEP-04) — wiring in the Plan 108-01
`error_code` capture (RPT-03) and the Plan 108-02 pattern/classifier for the write/verify step.
Every executed op routes through the guard-honoring `resolve_chip` path (never the derivation
dict, Pitfall 2) and calls the existing `EpromOperator` methods only — zero new firmware
dispatch, zero VPP-set, zero raw wire dict. Fully bench-free via the mock-operator seam.

Purpose: Run the derived plan so one step's failure (a locked boot block, a wrong chip) is a
recorded finding, not an abort, and a single flaky run is never reported as a confident PASS.
Output: `run_plan(plan, operator, db, *, runs=2)`, the per-step result record (verdict +
error_code + fingerprint + run_count), and the executor unit tests (bench-free).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-CONTEXT.md
@.planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-RESEARCH.md
@.planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-PATTERNS.md
@firestarter_app/CLAUDE.md
@firestarter_app/firestarter/eprom_operations.py
@firestarter_app/firestarter/cli_handlers.py
</context>

<artifacts_this_phase_produces>
This plan (108-04) produces (in `firestarter_app/firestarter/chip_test.py`):
- `run_plan(plan, operator, db, *, runs: int = 2) -> ...` — the non-fatal step executor
- the per-step result record (Claude's discretion on name/shape) carrying: verdict (`OK`/`BAD`/`NA`/`SKIPPED`, plus `marginal` for destructive/verify), captured `error_code`, optional `Fingerprint`, run_count, and a reason string
- the `runs < 2` reject guard mirroring `consistency_check_eprom`
- the id-first `destructive_gate` mechanism (SWEEP-03)
(This completes the phase's engine: derive_plan [108-03] + run_plan [this plan] + the pattern/classifier [108-02] + the error_code seam [108-01].)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-fatal per-step executor with verdict vocabulary + error_code capture</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_chip_test.py</files>
  <read_first>
    - firestarter_app/firestarter/cli_handlers.py (dev_validate_family :1476, and the resolve_chip + operator.write_cycle_eprom compose pattern at :1568 — the guard-honoring execution precedent)
    - firestarter_app/firestarter/eprom_operations.py (check_eprom_id :1695 returns Tuple[bool, Optional[int]]; write_eprom :1555; verify_eprom :1592; erase_eprom :1628; check_eprom_blank :1658; read_eprom :622)
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-PATTERNS.md (§ Pattern 2 execution routes through resolve_chip; § Pattern 6 non-fatal try/except; § error_code preservation)
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-RESEARCH.md (§ Deep-Dive 2 execution path; Pitfall 1, Pitfall 2; § 5 unit-test seam (a))
  </read_first>
  <behavior>
    - Test (non_fatal): a plan where an early step's operator method raises EpromOperationError still runs the later steps; the raising step's verdict is BAD and its result carries the exception's error_code
    - Test (verdict_vocab): verdicts are drawn only from OK/BAD/NA/SKIPPED (plus marginal in Task 3); an NA step from derive_plan is not executed (no operator call) and stays NA
    - Test: a resolve_chip refusal (ChipNotImplementedError / ChipNotFoundError side_effect) maps the step to SKIPPED/NA with the reason recorded — the op was still listed
    - Test: executed steps call resolve_chip(name, db) and the mock operator method — NOT the derivation dict
  </behavior>
  <action>
    In `chip_test.py` implement `run_plan(plan, operator, db, *, runs=2)` iterating the plan's
    step descriptors. For each step: NA steps are recorded NA without any operator call; supported
    steps re-resolve via `eprom_data = resolve_chip(name, db=db)` (guard-honoring — Pattern 2,
    Pitfall 2) inside a per-step `try/except` (Pattern 6). Catch `EpromOperationError` → verdict
    `BAD` capturing `err.error_code` (from the 108-01 seam); catch `ChipNotImplementedError` /
    `ChipNotFoundError` → `SKIPPED`/`NA` with the reason string. On success the operator method's
    bool → `OK`/`BAD`. One step's failure MUST NOT abort the rest (no exception escapes the loop).
    Dispatch each op to the matching existing method: id→check_eprom_id, read→read_eprom,
    blank-check→check_eprom_blank, write→write_eprom, verify→verify_eprom, erase→erase_eprom. The
    engine sets NO VPP, builds NO wire dict, passes NO --force. Import resolve_chip and the
    typed exceptions. Per-step result record carries verdict, error_code, reason, run_count (1
    here; N-run in Task 3). Reference cli_handlers.py:1566 in a comment.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_chip_test.py -k "non_fatal or verdict_vocab or resolver_refusal or run_plan_routes" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `chip_test.py` contains `def run_plan(` and calls `resolve_chip(` inside the per-step loop
    - test_chip_test.py::(non_fatal / verdict_vocab) exit 0 — a raising step does not abort later steps and its result carries error_code
    - a test proving a resolve_chip refusal maps to SKIPPED/NA (not an unhandled raise) exits 0
    - `grep -c 'vpp\|force' firestarter_app/firestarter/chip_test.py` shows no VPP-set / --force call site introduced (manual review: no `operation_flags` FLAG_FORCE, no vpp write)
  </acceptance_criteria>
  <done>run_plan executes each op as an independent non-fatal step through the guard-honoring resolve_chip path, mapping outcomes to OK/BAD/NA/SKIPPED and capturing the firmware error_code; no step aborts the rest; engine sets no VPP and builds no wire dict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: id-first chip-ID mismatch destructive gate</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_chip_test.py</files>
  <read_first>
    - firestarter_app/firestarter/eprom_operations.py (check_eprom_id :1695 — returns Tuple[bool, Optional[int]]: (is_ok, detected_id))
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-RESEARCH.md (§ Deep-Dive 2 id-first gating; Pitfall 4 chip-ID mismatch must actually gate destructive steps)
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-PATTERNS.md (§ Pattern 7 id-first destructive gate)
  </read_first>
  <behavior>
    - Test (id_mismatch_gate): with check_eprom_id returning (False, 0x9999) or a detected id != expected, every destructive step (write/erase) is SKIPPED and the operator's write_eprom/erase_eprom is NEVER called (chip pristine); id and read findings are still recorded
    - Test: with a matching chip ID, destructive steps are NOT gated (they proceed subject to --destructive annotation)
    - Test: id mismatch does not gate non-destructive steps (read/blank-check still run)
  </behavior>
  <action>
    Make id-check the first executed step in `run_plan`. From its
    `(is_ok, detected_id)` result set `destructive_gate = CLOSED` when `is_ok is False` OR
    `detected_id != expected_id` (Pitfall 4). Every destructive step (write, erase) checks
    `destructive_gate` FIRST and, when CLOSED, marks itself `SKIPPED` with reason
    "chip-ID mismatch — destructive steps gated (chip left pristine)" WITHOUT calling the operator
    method. Non-destructive id/read/blank-check findings are still recorded. Assert in tests that
    `operator.write_eprom` / `operator.erase_eprom` are never called when the gate is closed (use
    Mock assert_not_called). The expected id comes from the plan/DB entry (chip-id). Do NOT let a
    gated run touch the operator's destructive methods.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_chip_test.py -k "id_mismatch_gate or id_match_ungated" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_chip_test.py::test_id_mismatch_gate exits 0 and asserts `operator.write_eprom.assert_not_called()` (and erase) under a closed gate
    - a test proving a matching chip-ID leaves destructive steps ungated exits 0
    - `chip_test.py` run_plan sets a destructive-gate flag from the id-step result and every destructive step consults it before any operator call
  </acceptance_criteria>
  <done>id-check runs first; a chip-ID mismatch hard-gates all destructive steps SKIPPED (operator destructive methods never called, chip pristine) while id/read findings are still recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: N>=2 marginal policy on destructive/verify + write-step fingerprint wiring</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/tests/test_chip_test.py</files>
  <read_first>
    - firestarter_app/firestarter/eprom_operations.py (consistency_check_eprom runs guard :703-709; SHA-256 verdict :808-819 — the N-run divergence pattern to mirror; DEFAULT here is N=2 not 3)
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-CONTEXT.md (D-05 default N=2 + runs<2 reject; D-06 marginal is destructive/verify only, read disagreement is a divergence metric not a verdict flip)
    - .planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-RESEARCH.md (§ Deep-Dive 3 N>=2/marginal; § Deep-Dive 1 addr_base for the write/verify fingerprint)
  </read_first>
  <behavior>
    - Test (runs_boundary): run_plan(..., runs=1) is rejected BEFORE any operator call (no operator method invoked) with a clear error verdict/return
    - Test (marginal): a destructive/verify step whose two runs disagree (e.g. write_eprom returns True then False, or read-backs diverge) reports verdict `marginal` — never PASS/FAIL
    - Test: two agreeing destructive/verify runs report a confident OK (or BAD) — not marginal
    - Test: a read step's run disagreement is reported as a byte-level divergence metric (D-06), NOT marginal and NOT a verdict flip
    - Test: the write/verify step attaches a Fingerprint (from classify_fingerprint) computed against the expected generate_pattern with addr_base == the write region start
  </behavior>
  <action>
    Add `runs: int = 2` to `run_plan` and mirror the `if runs < 2:` reject guard from
    consistency_check_eprom BEFORE any operator/resolve call (D-05). For destructive/verify steps,
    execute the step `runs` times, collect per-run outcomes (per-run SHA-256 of read-back or the
    per-run verify bool), and when the runs DISAGREE set the step verdict `marginal` (D-06) —
    never coerce to PASS/FAIL (the AM27C020 write#1 60/64 vs write#2 0/64 case made structural).
    For read steps, compute byte-level divergence across runs via the Plan 108-02 diff helper and
    record it as a divergence metric only — NOT a verdict flip, NOT marginal (D-06). Wire the
    write/verify step to build the expected image with `generate_pattern(start, length)` and, after
    read-back, call `classify_fingerprint(expected, actual, repeat_divergent=<runs diverged>, addr_base=start)`
    (Deep-Dive 1 / Pitfall 3 — addr_base is the region start so clustering is on absolute-address
    bits). Attach the resulting `Fingerprint` to the step result. Keep marginal confined to
    destructive/verify per D-06.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_chip_test.py -k "runs_boundary or marginal or read_divergence or write_fingerprint" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_chip_test.py::test_runs_boundary exits 0 and asserts no operator method was called when runs<2
    - test_chip_test.py::test_marginal exits 0 (disagreeing destructive/verify runs -> "marginal")
    - a test proving read-step disagreement is a divergence metric (not marginal, not a verdict flip) exits 0
    - a test proving the write/verify step attaches a Fingerprint with addr_base == region start exits 0
    - `chip_test.py` run_plan contains an `if runs < 2` reject guard before any resolve/operator call
  </acceptance_criteria>
  <done>Destructive/verify steps run N>=2 and report marginal on disagreement; runs<2 is rejected before any operator call; read disagreement is a divergence metric only; the write/verify step attaches an absolute-address fingerprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host orchestration -> firmware serial ops | Each executed op crosses here via existing EpromOperator methods through resolve_chip |
| repeated-run outcomes -> verdict | N>=2 outcomes cross into a confident-vs-marginal decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-108-09 | Spoofing | wrong chip written (false PASS / bricked part) | high | mitigate | id-first; chip-ID mismatch hard-gates destructive steps SKIPPED, operator destructive methods never called (SWEEP-03, Pitfall 4) |
| T-108-10 | Information Disclosure | N=1 fluke reported as confident PASS/FAIL (AM27C020) | high | mitigate | Destructive/verify N>=2; disagreement -> marginal; runs<2 rejected early (SWEEP-04, Pitfall 5) |
| T-108-11 | Elevation of Privilege | engine sets VPP / builds raw command -> 12V hazard | high | mitigate | Compose existing operator methods only; set no VPP; pass no --force; Phase 109 CI-gates zero new dispatch + zero VPP-set sites |
| T-108-12 | Denial | fail-fast sweep hides the surprise (W29C040 locked boot block) | high | mitigate | Each step independent non-fatal try/except; one BAD/exception never aborts the rest (SWEEP-02, Pitfall 1) |
| T-108-13 | Elevation of Privilege | guard bypass leaks into execution | high | mitigate | Execution re-resolves via resolve_chip per op; derivation dict never reused for execution (Pitfall 2) |
| T-108-SC | Tampering | npm/pip installs | n/a | accept | No package installs (composes existing in-repo methods); no legitimacy gate required |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_chip_test.py -q` — SWEEP-02/03/04 + RPT-03-capture tests pass
- `cd firestarter_app && python -m pytest -q` — full app suite stays green; `--cov-fail-under=70` holds
- `cd firestarter_app && ruff check firestarter/chip_test.py tests/test_chip_test.py && ruff format --check firestarter/chip_test.py tests/test_chip_test.py`
- Manual: confirm no `write_eprom`/`erase_eprom` call when the id-gate is closed; confirm no VPP-set / --force / raw wire-dict introduced in chip_test.py (Phase 109 will CI-enforce this)
</verification>

<success_criteria>
- Each op executes independently with an OK/BAD/NA/SKIPPED verdict; a BAD/exception never aborts the rest (SWEEP-02, success criterion 2).
- id-first; chip-ID mismatch gates destructive steps shut, chip pristine, id/read findings recorded (SWEEP-03, success criterion 3).
- Destructive/verify run N>=2; disagreement reported marginal, never a silent single-run conclusion; runs<2 rejected (SWEEP-04, success criterion 4).
- Each per-step result carries the exact firmware error_code (RPT-03 consumed, success criterion 6).
</success_criteria>

<output>
Create `.planning/phases/108-test-plan-engine-address-derived-pattern-fingerprint/108-04-SUMMARY.md` when done.
</output>
