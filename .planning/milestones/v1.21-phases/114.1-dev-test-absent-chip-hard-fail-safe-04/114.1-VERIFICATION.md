---
phase: 114.1-dev-test-absent-chip-hard-fail-safe-04
verified: 2026-07-10T20:05:00Z
status: passed
score: 3/3 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 3/3
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 114.1: `dev test` Absent-Chip Hard-Fail (SAFE-04) Verification Report

**Phase Goal:** A tester who typos a chip name (or names a chip the DB has never heard of) gets an immediate, unambiguous `exit 1` failure *before* any serial connection is opened or hardware energized — never a full-but-hollow diagnostic report for a chip that doesn't exist. `dev test <chip>` hard-fails (exit 1, bare `Error: <chip>: not found in database`, no "did you mean") when the chip name is absent from the DB (`db.get_eprom` empty), short-circuiting in `dev_test` (`cli_handlers.py`) BEFORE `read_hardware_revision_value()` / any `AutoCapture` / report render — while a present-but-unsupported chip (support-status refusal, case B) STILL runs the full sweep. Guards case A only (keys off `get_eprom` emptiness, never a `resolve_chip` refusal, so it cannot swallow case B).

**Verified:** 2026-07-10T20:05:00Z
**Status:** passed
**Re-verification:** Yes — independent second pass corroborating a prior 19:00Z passing report; all evidence re-derived from the codebase, not the prior report. Note: the prior report cited pre-guard commit `dffcca9`; the actual parent of the guard commit is `dcd2986` (`d6359de~1`) — regression checks in this pass were run against the correct hash, conclusions unchanged.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (SC1 / D1) | `dev test <absent-chip>` exits 1 with bare `Error: <chip>: not found in database` (no fuzzy suggestion), short-circuiting before `read_hardware_revision_value()`/`AutoCapture`/`run_plan`/report render | ✓ VERIFIED | Guard at `firestarter_app/firestarter/cli_handlers.py:1846-1847` — `if not app.db.get_eprom(chip): raise ChipNotFoundError(f"{chip}: not found in database")` — sits after the `--destructive` confirm block (ends :1842) and strictly before `plan = derive_plan(...)` (:1852) / `AutoCapture(...)` (:1861) / `read_hardware_revision_value()` (:1864). Behavioral test `TestAbsentChipHardFail::test_dev_test_absent_chip_hard_fails_before_hardware` PASSED (run live by verifier). Contains the load-bearing assertions: `app.hardware_manager.read_hardware_revision_value.assert_not_called()` (test :709), `app.eprom_operator.read_eprom.assert_not_called()` (:710), and no-report-banner check (:711). **RED-proven independently:** the same test was run against pre-guard commit `dcd2986` (`d6359de~1`) in a throwaway git worktree — it FAILED, and the failure output demonstrates the exact false-green trap: the pre-guard handler still exits 1 (via `TypeError('Object of type Mock is not JSON serializable')`) but only AFTER rendering the `dev test -- NO_SUCH_CHIP_XYZ` report banner, i.e. after touching hardware and building the report. An exit-code-only test would have false-greened; this test discriminates correctly. |
| 2 (SC2 / D2) | A present-but-unsupported chip (case B, e.g. AT28C16 adapter-required) STILL runs the full sweep; guard keys strictly off `get_eprom` emptiness, never a `resolve_chip` refusal | ✓ VERIFIED | Guard code (:1844-1850) references only `app.db.get_eprom(chip)` — no `resolve_chip`/support_status anywhere in the guard. Case-B preconditions independently re-proven live: `EpromDatabase(skip_local_override=True).get_eprom("AT28C16")` truthy; `resolve_chip("AT28C16", db)` raises `ChipNotImplementedError: AT28C16: adapter required: ...`. Behavioral test `test_dev_test_present_but_unsupported_still_sweeps` PASSED (run live): asserts `exit_code == 0`, `hw.read_hardware_revision_value.assert_called()` (guard did NOT short-circuit), reads the written `dev-test-AT28C16.json` artifact, and asserts `id=NA`, `read=SKIPPED`, `blank-check=SKIPPED` with "adapter" in the reasons — the refusal is recorded as findings, not swallowed. |
| 3 (SC3 / D3) | Both behaviors covered by hardware-free tests in `tests/test_dev_test_cmd.py` using the established `make_app_context()` + `EpromDatabase(skip_local_override=True)` + `Mock(spec=...)` + `_off_tty()` seam | ✓ VERIFIED | `class TestAbsentChipHardFail` (test file :686-739) uses exactly this seam — `make_app_context(...)`, `Mock(spec=EpromOperator/HardwareManager)`, `_off_tty()`, `CliRunner`; no bench, no serial port. `cd firestarter_app && /usr/local/bin/python -m pytest tests/test_dev_test_cmd.py` → **23 passed** (21 pre-existing + 2 new), 0 failed. |

**Score:** 3/3 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `firestarter_app/firestarter/cli_handlers.py` | 2-line absent-chip guard inside `dev_test`, after `--destructive` confirm block, before `derive_plan`/`AutoCapture`/`read_hardware_revision_value()` | ✓ VERIFIED | Guard present at :1844-1850 with SAFE-04 rationale comment, correctly positioned (direct file read). Commit `d6359de` is +8 lines in this file only. Reuses existing `ChipNotFoundError` (imported :59, no new import) and `@map_typed_errors` (:1796 on `dev_test`). Substantive + wired. |
| `firestarter_app/tests/test_dev_test_cmd.py` | `test_dev_test_absent_chip_hard_fails_before_hardware` (case A) and `test_dev_test_present_but_unsupported_still_sweeps` (case B) | ✓ VERIFIED | Both present in `TestAbsentChipHardFail` (:686-739), +63 lines in commit `6ab06a7`; both pass; case A RED-proven against the pre-guard commit. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| Guard (`app.db.get_eprom(chip)` emptiness) | exit 1 + `Error: <chip>: not found in database` | `raise ChipNotFoundError` → `@map_typed_errors` (`ChipNotFoundError` arm at cli_handlers.py:139) → `click.ClickException(str(e))` | ✓ WIRED | `ChipNotFoundError` is a bare `Exception` subclass — `str(e)` is exactly `"{chip}: not found in database"`, no doubling. Behaviorally proven: case-A test asserts `exit_code == 1` and the message in `result.output`; PASSED live. |
| Case B path (`derive_plan` via `db.get_eprom`, NOT `resolve_chip`) | full sweep + SKIPPED findings | Unchanged pre-existing path: `run_plan` re-resolves each step via `resolve_chip`, catches refusal, records SKIPPED | ✓ WIRED | Guard does not touch `derive_plan`/`run_plan`. Case-B test PASSED live: hw read reached, JSON report written, SKIPPED verdicts with "adapter" reasons present in the artifact. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Both SAFE-04 tests pass (GREEN) | `/usr/local/bin/python -m pytest tests/test_dev_test_cmd.py::TestAbsentChipHardFail -q` | 2 passed | ✓ PASS |
| Case-A test fails on pre-guard code (RED-provable, not vacuous) | same test run in a throwaway worktree at `dcd2986` (`d6359de~1`) | FAILED — report banner rendered pre-guard before the crash-driven exit 1; exit-code-only assertion would have false-greened | ✓ PASS |
| Case-B chip precondition | live Python: `get_eprom("AT28C16")` + `resolve_chip("AT28C16", db)` | truthy in DB; raises `ChipNotImplementedError: AT28C16: adapter required...` | ✓ PASS |
| Absent-chip precondition | live Python: `not db.get_eprom("NO_SUCH_CHIP_XYZ")` | True (falsy) | ✓ PASS |
| Full test file, no new regressions | `/usr/local/bin/python -m pytest tests/test_dev_test_cmd.py` | 23 passed, 0 failed | ✓ PASS |
| Lint/format gates | `ruff check` + `ruff format --check` on both modified files | All checks passed; 2 files already formatted | ✓ PASS |
| mypy claim (pre-existing failure) | `mypy firestarter/cli_handlers.py` at HEAD vs `dcd2986` | identical single error `firestarter/submit.py:386` at BOTH commits — pre-existing, NOT introduced by this phase | ✓ PASS |

### Prohibitions

| Prohibition | Status | Evidence |
|-------------|--------|----------|
| Guard MUST NOT key off a `resolve_chip`/support-status refusal (would swallow case B) | ✓ HELD | Guard references only `app.db.get_eprom(chip)` (code read); case-B test proves the in-DB-unsupported chip still sweeps. |
| No "did you mean"/fuzzy-match suggestion; bare fail only | ✓ HELD | grep for `did you mean`/`difflib`/`get_close_matches` in cli_handlers.py: zero hits. |
| No new dependency / firmware change / chip_database.json change / new exception type / `logger.error + sys.exit(1)` style | ✓ HELD | Commit `d6359de` is +8 lines in cli_handlers.py only, reusing existing `ChipNotFoundError`; commit `6ab06a7` touches tests only. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SAFE-04 | 114.1-01-PLAN.md | `dev test <chip>` hard-fails on absent chip before hardware energize; case B unaffected | ✓ SATISFIED | Truths 1-3 above; plan `requirements:` frontmatter declares exactly `[SAFE-04]` — no orphaned requirements for this phase. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none | — | grep for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER over both modified files: zero hits. The 2-line guard is exactly what the plan specified, with an explanatory SAFE-04 / case-A-vs-case-B comment. |

### Human Verification Required

None. Both success criteria are fully exercised by hardware-free behavioral tests that this verifier executed directly, including an independent RED-proof against the pre-guard commit in an isolated worktree — nothing was trusted from SUMMARY.md claims.

### Gaps Summary

No gaps. The guard exists, is substantive, is correctly positioned above the first hardware touch (`read_hardware_revision_value()` at :1864), and is wired through the existing `ChipNotFoundError` → `@map_typed_errors` → `click.ClickException` path with no new exception type and no fuzzy suggestion. Case B is provably unaffected both by code inspection (the guard never references `resolve_chip`) and by a live-run test asserting the sweep still executes and records SKIPPED findings. Commits `d6359de` (guard, +8 lines) and `6ab06a7` (tests, +63 lines) exist on the submodule branch `v1.21-community-chip-validation-command` and match SUMMARY claims exactly. The single `mypy` failure (`submit.py:386`) was independently reproduced at the pre-guard commit `dcd2986` and is not a regression from this phase.

---

*Verified: 2026-07-10T20:05:00Z*
*Verifier: Claude (gsd-verifier)*
