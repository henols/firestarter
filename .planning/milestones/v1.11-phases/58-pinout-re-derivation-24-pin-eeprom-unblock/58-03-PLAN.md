---
phase: 58-pinout-re-derivation-24-pin-eeprom-unblock
plan: 03
type: execute
wave: 2
depends_on: ["58-02"]
files_modified:
  - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md
  - firestarter_app/doc/pinout-safety-review.md
autonomous: true
requirements: [PIN-02, PIN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "GATE-03 (check_dispatch.py) returns 0 VPP-routing violations across the full regenerated chip set — no chip with a 5V-EEPROM electrical type routes to a VPP-asserting handler (PIN-02)"
    - "firestarter info AT28C16 (and family members) returns a valid entry rather than 'chip not found' (PIN-03)"
    - "SR-1 safety review exists in BOTH layers (planning artifact + shipped sub-repo doc) covering DIP24_2816 AND every pinout the re-derivation changed (D-10/D-11)"
    - "No uncertain or 5V chip has a VPP-asserting dispatch path (enforced mechanically by GATE-03)"
  artifacts:
    - path: ".planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md"
      provides: "SR-1 planning-layer safety audit trail for every changed pinout"
      contains: "DIP24_2816"
    - path: "firestarter_app/doc/pinout-safety-review.md"
      provides: "Operator/GitHub-visible SR-1 safety-guarantee doc (two-layer mirror)"
      contains: "DIP24_2816"
  key_links:
    - from: "firestarter_app/tools/check_dispatch.py"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "GATE-03 full-class VPP-safety scan returns 0 violations"
      pattern: "configure_eprom"
    - from: ".planning/phases/58-.../58-SR-1-CHECKLIST.md"
      to: "firestarter_app/doc/pinout-safety-review.md"
      via: "two-layer lockstep (D-10) — operator doc is the subset of the planning artifact"
      pattern: "DIP24_2816"
---

<objective>
Close the safety gates for Phase 58: run GATE-03 (`check_dispatch.py`) against the regenerated DB to prove 0 VPP-routing violations (PIN-02's mechanical proof), confirm the unblocked chips are CLI-reachable (`firestarter info AT28C16`), and author the SR-1 safety review in both layers (planning artifact + shipped sub-repo doc) per D-10/D-11 — covering DIP24_2816 AND every existing pinout whose selection changed under the re-derivation.

Purpose: PIN-02 (no VPP-on-wrong-pin damage path, proven by GATE-03), PIN-03 (SR-1 safety review + CLI reachability of the unblocked family).
Output: 58-SR-1-CHECKLIST.md (planning), firestarter_app/doc/pinout-safety-review.md (sub-repo), GATE-03 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-CONTEXT.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run GATE-03 and confirm CLI reachability of the unblocked family (PIN-02, PIN-03)</name>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (GATE-03 predicate ~lines 140-143: `if etype == "Flash/EEPROM" and handler == "configure_eprom"`; dispatch() ~64-84 with 0x0D->configure_eeprom28c; DB load ~89-96 — confirm it reads the regenerated DB)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"GATE-03 Auto-Covers DIP24_2816" and §"configure_eeprom28c is Pin-Count-Agnostic"
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-VALIDATION.md §"Per-Task Verification Map" (PIN-02 GATE-03 + PIN-03 firestarter info rows)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-02-SUMMARY.md (the regenerated DB state + diagnostics)
    - reference: firestarter_app test env per project memory — use /usr/local python; restore with `pip install -e '.[test]'`
  </read_first>
  <action>
    From `firestarter_app/`, run `python3 tools/check_dispatch.py` against the Plan 02 regenerated chip_database.json and confirm it reports 0 VPP-routing violations (this is PIN-02's reframed mechanical proof per D-05 — GATE-03 needs NO code change, it is pinout-agnostic and auto-covers DIP24_2816). If any violation appears, STOP and report it as a blocker against Plan 02 (do not edit check_dispatch.py and do not hand-edit the DB). Then install the package (`pip install -e '.[test]'` per the project test-env memory) and confirm `firestarter info AT28C16` exits 0 with a valid entry (algorithm 0x0D / pinout DIP24_2816), plus spot-check one more family member (`firestarter info AT28C04`) and one previously-dangerous chip (`firestarter info AM28C16A`). Run the full pytest suite (`python3 -m pytest -q`) to confirm no regression across the ~480 tests.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 tools/check_dispatch.py && firestarter info AT28C16 >/dev/null && firestarter info AM28C16A >/dev/null && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/check_dispatch.py` returns 0 violations (exit 0; CLI output asserts no VPP-asserting dispatch for any Flash/EEPROM chip) — PIN-02 proof.
    - `firestarter info AT28C16` exits 0 and returns a valid entry (not "chip not found") — PIN-03; `firestarter info AM28C16A` also exits 0 (the previously-dangerous chip is now reachable and safe).
    - `python3 -m pytest -q` is fully green (~480+ tests, including all Plan 01 Wave 0 classes).
    - No edits to check_dispatch.py or chip_database.json in this task.
  </acceptance_criteria>
  <done>GATE-03 returns 0 violations on the regenerated DB; the unblocked + fixed chips are CLI-reachable; the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the SR-1 safety review in both layers (PIN-03, D-10/D-11)</name>
  <read_first>
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"DIP24_2816 Pinout Definition (SR-1 Pre-Verified)" (the 9-item SR-1 checklist), §"Open Questions" #2 (doc filename recommendation), Assumptions Log A1/A2 (datasheet items the SR-1 must flag)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-CONTEXT.md (D-10 two-layer lockstep; D-11 scope = every changed pinout; D-09 citation convention)
    - .planning/v1.7-SHIELD-REVS.md (the two-layer meta+sub-repo doc precedent to mirror — investigation-canonical planning artifact + operator-canonical shipped subset)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-02-SUMMARY.md (the captured Rule-1/D-06 diagnostics — the authoritative list of which pinouts/chips the re-derivation actually changed, so SR-1 scope per D-11 is complete)
    - firestarter_app/firestarter/data/pinouts.json (the DIP24_2816 entry to verify the checklist against; the DIP24_2716 contrast)
  </read_first>
  <action>
    Author TWO lockstep documents (D-10). Planning layer: `.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md` — the full audit trail. For DIP24_2816, complete the 9-item SR-1 checklist from RESEARCH: vpp-pin ABSENT; rw-pin=[21]=WE; oe-pin=[20]; ce-pin=[18]; vcc-pin=[24]; gnd-pin=[12]; address bus [8,7,6,5,4,3,2,1,23,22,19]=A0-A10 no overlap; data bus [9,10,11,13,14,15,16,17]=D0-D7 no overlap; pin 21 NOT shared with any VPP path (contrast DIP24_2716 vpp-pin=[21] — the whole point of the separate entry); all 24 pins accounted for. Per D-11, ALSO cover every OTHER pinout whose selection or layout changed under the re-derivation — derive that list from the Plan 02 SUMMARY diagnostics (e.g. SRAM chips moving DIP24_2716->DIP24_6116) and run the same vpp-absent / rw=WE / oe / ce checklist per changed pinout. Flag RESEARCH Assumptions A1 (DIP24_2816 pin assignment is JEDEC-standard/DIP24_6116-confirmed, not a fresh datasheet read) and A2 (variant_lo=0x10 chips are genuine 5V EEPROMs) as items requiring operator datasheet confirmation. Cite the minipro source per the Phase 56 D-05/D-06 convention (pinned GitLab commit-permalink URLs to one recorded SHA — the mask decode IS the citation per D-09; do NOT vendor minipro source, do NOT maintain a separate "one-rom verified" list). Operator layer: `firestarter_app/doc/pinout-safety-review.md` — the GitHub-visible subset (mirror the shield-revisions subset pattern): the resulting safety guarantees (DIP24_2816 is 5V-only, no VPP path; GATE-03 enforces 0 violations across the full set; configure_eeprom28c is 5V-safe). Keep the two layers in lockstep (the operator doc is a strict subset of the planning artifact). State that BENCH-01 (real-hardware write/program validation) is deferred to v2 per REQUIREMENTS.md — source-correctness only.
  </action>
  <verify>
    <automated>test -f .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md && test -f firestarter_app/doc/pinout-safety-review.md && grep -q "DIP24_2816" .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md && grep -q "DIP24_2816" firestarter_app/doc/pinout-safety-review.md && grep -q "vpp-pin" .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: `58-SR-1-CHECKLIST.md` (planning) and `firestarter_app/doc/pinout-safety-review.md` (sub-repo).
    - The planning checklist completes all 9 SR-1 items for DIP24_2816 (vpp-pin absent asserted explicitly) AND covers every other pinout the re-derivation changed (D-11 scope), with the change list sourced from the Plan 02 diagnostics.
    - Citations use the Phase 56 minipro commit-permalink+SHA convention (D-09); no vendored minipro source; no "one-rom verified" list.
    - Assumptions A1/A2 are flagged as operator datasheet-confirmation items; BENCH-01 noted as v2-deferred.
    - The operator doc is a strict subset of the planning artifact (two-layer lockstep, D-10).
  </acceptance_criteria>
  <done>SR-1 review exists in both layers covering DIP24_2816 and every changed pinout; citations follow the minipro permalink convention; the two layers are in lockstep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip_database.json → firmware dispatch | A 5V EEPROM routed to a VPP-asserting handler (configure_eprom) asserts 12V on a 5V part's WE/control pin → dead chip |
| SR-1 documentation → operator | An incomplete/incorrect SR-1 review can leave a damage path undocumented and unreviewed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering (physical-hardware damage) | Full regenerated chip set | mitigate | GATE-03 (`check_dispatch.py`) asserts 0 VPP-routing violations across the entire set — the mechanical proof that no Flash/EEPROM chip reaches configure_eprom. Blocking: any violation halts Phase 58 and bounces to Plan 02. |
| T-58-05 | Repudiation (undocumented safety change) | SR-1 review scope | mitigate | D-11 scope = every pinout the re-derivation changed (not just DIP24_2816), sourced from the Plan 02 diagnostics; two-layer lockstep doc (D-10) gives an auditable + operator-visible trail. |
| T-58-06 | Information disclosure (datasheet assumption) | DIP24_2816 pin assignment (Assumption A1) | accept | Pin assignment is JEDEC-standard + DIP24_6116-confirmed but not a fresh datasheet read this session; flagged in SR-1 as an operator confirmation item. Low risk (electrically identical to a long-shipped SRAM pinout); BENCH-01 deferred to v2. |
</threat_model>

<verification>
- `python3 tools/check_dispatch.py` returns 0 violations on the regenerated DB (PIN-02).
- `firestarter info AT28C16` / `AM28C16A` exit 0 (PIN-03 reachability).
- `python3 -m pytest -q` fully green.
- Both SR-1 documents exist, cover DIP24_2816 + every changed pinout, and are in lockstep.
</verification>

<success_criteria>
- PIN-02: GATE-03 returns 0 VPP-routing violations across the full regenerated set.
- PIN-03: the unblocked 24-pin EEPROM family is CLI-reachable and SR-1-reviewed in both doc layers.
- The SR-1 review covers every pinout the re-derivation changed (D-11), cites minipro per the Phase 56 convention (D-09), and flags A1/A2 + BENCH-01 deferral.
</success_criteria>

<output>
Create `.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-03-SUMMARY.md` when done. Record the GATE-03 violation count (must be 0) and the final list of changed pinouts covered by SR-1.
</output>
