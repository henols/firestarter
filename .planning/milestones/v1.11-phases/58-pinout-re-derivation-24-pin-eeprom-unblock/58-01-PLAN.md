---
phase: 58-pinout-re-derivation-24-pin-eeprom-unblock
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - firestarter_app/firestarter/data/pinouts.json
  - firestarter_app/tests/test_decoder.py
autonomous: true
requirements: [PIN-01, PIN-02, PIN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "DIP24_2816 exists in pinouts.json with rw-pin=21, oe-pin=20, ce-pin=18, vcc=24, gnd=12 and NO vpp-pin key"
    - "Failing-first test classes exist asserting the principled resolve_pinout_key contract, guess-table deletion, WARNING-5 survival, DIP24_2816 safety, and the dangerous/blocked 24-pin EEPROM fix"
    - "No 24-pin EEPROM pinout entry routes VPP to a WE/control pin (DIP24_2816 has no vpp-pin field)"
  artifacts:
    - path: "firestarter_app/firestarter/data/pinouts.json"
      provides: "DIP24_2816 5V EEPROM pinout entry (no vpp-pin)"
      contains: "DIP24_2816"
    - path: "firestarter_app/tests/test_decoder.py"
      provides: "Wave 0 test classes: TestResolvedPinoutKey, TestGuessTablesDeleted, TestWarning5Rule, TestDIP24_2816Pinout, TestDangerous24pinEEPROMFixed"
      contains: "class TestResolvedPinoutKey"
  key_links:
    - from: "firestarter_app/tests/test_decoder.py"
      to: "tools.build_db.resolve_pinout_key"
      via: "import inside test methods"
      pattern: "from tools.build_db import resolve_pinout_key"
    - from: "firestarter_app/tests/test_decoder.py"
      to: "firestarter_app/firestarter/data/pinouts.json"
      via: "DIP24_2816 presence + no-vpp-pin assertion"
      pattern: "DIP24_2816"
---

<objective>
Lay the Wave 0 test scaffolding and add the `DIP24_2816` pinout entry that the Phase 58 `resolve_pinout_key` rewrite (Plan 02) will be implemented against. This is the test-first foundation: the five Wave 0 test classes defined in 58-VALIDATION.md must exist (RED — most fail against the current guess-table code) before any build_db.py logic changes land, and `DIP24_2816` must exist in `pinouts.json` so `VALID_PINOUT_KEYS` contains it when the rewritten function returns it.

Purpose: Nyquist test-first coverage for PIN-01/02/03; establish the SR-1-safe `DIP24_2816` electrical contract (no vpp-pin) before the rewrite consumes it.
Output: `DIP24_2816` entry in `pinouts.json`; five new test classes in `tests/test_decoder.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-CONTEXT.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-PATTERNS.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DIP24_2816 pinout entry to pinouts.json (D-07)</name>
  <read_first>
    - firestarter_app/firestarter/data/pinouts.json (the existing DIP24_6116 entry ~lines 94-103 is the exact electrical template; DIP24_2716 ~line 6 is the hazardous contrast with vpp-pin=[21])
    - firestarter_app/CLAUDE.md (data layout; pinouts.json is consumed by database.py)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"DIP24_2816 Pinout Definition (SR-1 Pre-Verified)"
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-PATTERNS.md §"pinouts.json — add DIP24_2816"
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-CONTEXT.md (D-07: dedicated entry, NOT reuse of DIP24_6116)
  </read_first>
  <action>
    Insert a new key `DIP24_2816` into `pinouts.json` immediately after the existing `DIP24_6116` entry (keeps the DIP24 family grouped). Copy DIP24_6116's electrical layout exactly: vcc-pin=[24], gnd-pin=[12], address-bus-pins=[8,7,6,5,4,3,2,1,23,22,19] (A0-A10 over-allocated per D-08 — one entry covers both AT28C16 2KB and AT28C04 512B; firmware restricts driving by mem_size), data-bus-pins=[9,10,11,13,14,15,16,17], ce-pin=[18], oe-pin=[20], rw-pin=[21]. CRITICAL SR-1 invariant per D-07/D-11 and RESEARCH Pitfall 4: the entry MUST NOT contain a `vpp-pin` key — pin 21 is WE (rw-pin), never VPP. Set `name` to "JEDEC 24-pin 5V parallel EEPROM (AT28C16/AT28C04 family)" and a `comment` documenting: same physical layout as DIP24_6116; NO vpp-pin (configure_eeprom28c is 5V-only, SDP-disable + DQ7 page-write polling, VPP regulator never asserted); over-allocated to A0-A10; pin 21=WE not VPP (contrast DIP24_2716 which has vpp-pin=21); cite Atmel AT28C16 datasheet Table 1 pin description. Verify the JSON parses. Note: ROADMAP SC#3/#4 say "DIP24_6116" but CONTEXT D-07 supersedes that with this dedicated DIP24_2816 entry for SR-1 traceability.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -c "import json; p=json.load(open('firestarter/data/pinouts.json')); e=p['DIP24_2816']; assert 'vpp-pin' not in e['pins'], 'vpp-pin present — SR-1 violation'; assert e['pins']['rw-pin']==[21] and e['pins']['ce-pin']==[18] and e['pins']['oe-pin']==[20] and e['pins']['vcc-pin']==[24] and e['pins']['gnd-pin']==[12]; print('DIP24_2816 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pinouts.json` parses as valid JSON and contains key `DIP24_2816`.
    - `DIP24_2816.pins` has NO `vpp-pin` key (source assertion; the defining SR-1 safety property).
    - `rw-pin=[21]`, `oe-pin=[20]`, `ce-pin=[18]`, `vcc-pin=[24]`, `gnd-pin=[12]`, address-bus = A0-A10 (11 pins), data-bus = 8 pins.
    - The verify command above prints `DIP24_2816 OK` and exits 0.
  </acceptance_criteria>
  <done>DIP24_2816 exists in pinouts.json, electrically identical to DIP24_6116, with no vpp-pin field, and the JSON parses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the five Wave 0 test classes to test_decoder.py (RED-first)</name>
  <read_first>
    - firestarter_app/tests/test_decoder.py (the existing TestBuildDbDecodeCorrectness class ~lines 691-817 is the exact shape/import analog: class docstring + def test_<behavior>(self) + import-inside-method)
    - firestarter_app/tests/test_eprom_database.py (~lines 35-68: the mandatory EpromDatabase(skip_local_override=True) pattern for any test asserting specific chip data)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map"
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-PATTERNS.md §"test_decoder.py — add resolve_pinout_key regression suite"
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"Full Principled Rule Structure" (the exact (pin_count, pm_idx, variant_lo)→key mapping each unit test asserts) and §"The 9 Blocked Chips" + §"Dangerous 24-pin EEPROMs Already In The DB" (the chip names the integration test asserts)
  </read_first>
  <behavior>
    - TestResolvedPinoutKey (unit, imports `resolve_pinout_key` inside each method): (24,pm_idx=23,variant_lo=0x01)->DIP24_2732; (24,23,0x00)->DIP24_2716; (24,23,0x10)->DIP24_2816 [KEY]; (24,pm_idx=0)->DIP24_6116; (24,pm_idx=99)->None [D-06 fail-safe]; (28,22,0x10)->DIP28_27512; (28,22,0x11)->DIP28_27256; (28,22,0x00)->DIP28_2764; (28,21)->DIP28_2764; (28,20)->DIP28_28C256; (28,19)->DIP28_28C64; (28,18)->DIP28_28C64; (32,pm_idx in {5,7,9,10,11,12,13}, proto 0x06)->DIP32_SST39SF040; (32, same, proto 0x0D)->DIP32_28C512_EEPROM; (32, same, proto 0x07)->DIP32_STD; (32,pm_idx=0)->DIP32_SST39SF040.
    - TestGuessTablesDeleted: assert `not hasattr(tools.build_db, "PIN_MAP_TO_PINOUT")`, same for `PIN_MAP_PROTO_TO_PINOUT` and `DIP28_VARIANT_MAP` (D-02).
    - TestWarning5Rule: a chip resolving to DIP28_28C256 (pm_idx=20) with flags erasable (_etype=Flash/EEPROM) and proto_id=0x07 ends up with algorithm 0x0D (PIN-02 regression guard). Assert against the rewritten rule outcome — see Plan 02; test the resolve+override path if a helper is exposed, else assert via the regenerated DB after Plan 02.
    - TestDIP24_2816Pinout: DIP24_2816 present in pinouts.json; NO vpp-pin key [CRITICAL SR-1 gate]; rw-pin=[21]; ce-pin=[18]; oe-pin=[20]; vcc-pin=[24]; gnd-pin=[12].
    - TestDangerous24pinEEPROMFixed (integration, loads regenerated chip_database.json via EpromDatabase(skip_local_override=True) or json.load): the 9 blocked chips (AT28C16, AT28C04, MICROCHIP 28C16A, NEC UPD28C04 …) AND the 10 dangerous chips (AMD AM28C16A, CAT28C16A, EXEL XL2804A/XL2816A, MICROCHIP 2804/2816, XICOR X2804A/X2816A/X2816B) all have algorithm==0x0D and pinout=="DIP24_2816".
  </behavior>
  <action>
    Append five new test classes to `tests/test_decoder.py` following the exact established shape: `class TestXxx:` with a docstring citing the requirement (PIN-01/02/03), `def test_<behavior>(self):` methods, and `from tools.build_db import resolve_pinout_key` (and for integration, `from firestarter.database import EpromDatabase`) imported INSIDE each method body. For TestResolvedPinoutKey, call `resolve_pinout_key` with explicit keyword args matching the new signature `resolve_pinout_key(pin_count, variant, flags_int, pm_idx=..., proto_id=..., type_int=..., mem_size=...)` — encode variant_lo via the `variant` low byte (e.g. variant=0x00004310 → variant_lo=0x10). For TestDangerous24pinEEPROMFixed use `EpromDatabase(skip_local_override=True)` per the mandatory test pattern (bare EpromDatabase() is forbidden). These tests are RED-first: TestResolvedPinoutKey/TestGuessTablesDeleted/TestWarning5Rule/TestDangerous24pinEEPROMFixed are EXPECTED to fail now (current code still has the guess tables, wrong signature, and dangerous chips) — that is correct. TestDIP24_2816Pinout should pass once Task 1 lands. Do NOT modify build_db.py in this plan. Mark the RED-expected behavior in the SUMMARY so Plan 02 knows the bar to turn GREEN.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_decoder.py::TestDIP24_2816Pinout -x -q && python3 -m pytest tests/test_decoder.py -q --co | grep -c -E "TestResolvedPinoutKey|TestGuessTablesDeleted|TestWarning5Rule|TestDIP24_2816Pinout|TestDangerous24pinEEPROMFixed"</automated>
  </verify>
  <acceptance_criteria>
    - All five classes (TestResolvedPinoutKey, TestGuessTablesDeleted, TestWarning5Rule, TestDIP24_2816Pinout, TestDangerous24pinEEPROMFixed) are collected by pytest (`--co` lists them; the grep -c count is > 0 and matches the expected method count).
    - `TestDIP24_2816Pinout` passes GREEN (depends only on Task 1's pinouts.json entry).
    - `TestResolvedPinoutKey`, `TestGuessTablesDeleted`, `TestDangerous24pinEEPROMFixed` are present and currently RED (documented as expected-RED in the SUMMARY; they turn GREEN in Plan 02).
    - Every test method imports from `tools.build_db` / `firestarter.database` inside the method body (matches the established convention).
    - Integration tests use `EpromDatabase(skip_local_override=True)` (no bare EpromDatabase()).
  </acceptance_criteria>
  <done>Five Wave 0 test classes exist in test_decoder.py; TestDIP24_2816Pinout is green; the rewrite-dependent classes are present and RED, documented as the GREEN bar for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| infoic.xml (upstream GitLab) → build_db.py | Untrusted decode input crosses into the DB pipeline; wrong decode can route VPP to a non-VPP pin |
| chip_database.json + pinouts.json → firmware dispatch | A pinout with vpp-pin on a 5V EEPROM's WE pin asserts 12V on a 5V part → dead chip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering (physical-hardware damage) | DIP24_2816 pinout entry | mitigate | Entry MUST have NO `vpp-pin` key (pin 21 = rw-pin/WE). Verified by Task 1 assertion + TestDIP24_2816Pinout::test_dip24_2816_has_no_vpp_pin_field. This is the SR-1-safe contract the rewrite consumes. |
| T-58-02 | Information disclosure (silent test gap) | Wave 0 test scaffolding | mitigate | All five 58-VALIDATION.md test classes must be collected by pytest; RED-first classes documented in SUMMARY so a missing/skipped test cannot mask a later regression. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_decoder.py -q --co` collects all five new classes.
- `python3 -m pytest tests/test_decoder.py::TestDIP24_2816Pinout -x -q` is green.
- `python3 -c "import json; e=json.load(open('firestarter/data/pinouts.json'))['DIP24_2816']; assert 'vpp-pin' not in e['pins']"` exits 0.
</verification>

<success_criteria>
- DIP24_2816 exists in pinouts.json with no vpp-pin field and the correct rw/oe/ce/vcc/gnd pins.
- The five Wave 0 test classes from 58-VALIDATION.md exist; TestDIP24_2816Pinout is green; the rest are RED-first (correct pre-rewrite state).
- No build_db.py changes in this plan (logic rewrite is Plan 02).
</success_criteria>

<output>
Create `.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-01-SUMMARY.md` when done. Record which Wave 0 classes are RED (the GREEN bar Plan 02 must clear) vs GREEN.
</output>
