---
phase: 58-pinout-re-derivation-24-pin-eeprom-unblock
plan: 02
type: execute
wave: 1
depends_on: ["58-01"]
files_modified:
  - firestarter_app/tools/build_db.py
  - firestarter_app/firestarter/data/chip_database.json
autonomous: true
requirements: [PIN-01, PIN-02, PIN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "resolve_pinout_key is a pure function of decoded fields (pin_count, pm_idx, variant_lo, type_int, mem_size, proto_id) — no per-IC names, no chip-name lists, no family lookup tables (D-03)"
    - "PIN_MAP_TO_PINOUT, PIN_MAP_PROTO_TO_PINOUT and DIP28_VARIANT_MAP no longer exist in tools.build_db (D-02)"
    - "All (pm_idx=23, variant_lo=0x10) 24-pin chips route to DIP24_2816 + algorithm=0x0D — both the 9 blocked AND the 10 dangerous chips fixed (PIN-03)"
    - "WARNING-5 and fm1608 survive as named rule outcomes, not hardcoded patches (D-05); two-pass _etype invariant preserved (PATTERNS key pattern 5)"
    - "Any chip resolve_pinout_key cannot classify is skipped with a loud warning and never emitted — no uncertain chip emits a VPP-asserting dispatch (D-06)"
  artifacts:
    - path: "firestarter_app/tools/build_db.py"
      provides: "Principled resolve_pinout_key + Rule 1/2/3 overrides + D-06 fail-safe; guess tables deleted"
      contains: "def resolve_pinout_key"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "Regenerated DB with the 9 unblocked + 10 fixed 24-pin EEPROMs on algo=0x0D / DIP24_2816"
      contains: "DIP24_2816"
  key_links:
    - from: "firestarter_app/tools/build_db.py"
      to: "firestarter_app/firestarter/data/pinouts.json"
      via: "VALID_PINOUT_KEYS includes DIP24_2816; resolve_pinout_key returns it"
      pattern: "DIP24_2816"
    - from: "firestarter_app/tools/build_db.py"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "python3 tools/build_db.py regenerates the DB"
      pattern: "build_db"
---

<objective>
Rewrite `resolve_pinout_key` in `build_db.py` as the principled, fully data-driven function specified in 58-RESEARCH.md §"Full Principled Rule Structure" and 58-PATTERNS.md — keyed only on decoded fields (`pin_count`, `pm_idx`, `variant_lo`, `type_int`, `mem_size`, `proto_id`) with ZERO per-IC / per-family special code (D-03). Delete the three guess tables entirely (D-02). Convert the three load-bearing safety behaviors (24-pin EEPROM skip, WARNING-5, fm1608) into named rule outcomes (D-05), add the D-06 fail-safe for unclassifiable chips, preserve the two-pass `_etype` invariant, then regenerate `chip_database.json`. This turns the Plan 01 RED tests GREEN.

Purpose: PIN-01 (principled re-derivation), PIN-02 (safety overrides as rule outcomes), PIN-03 (route the 9 blocked + fix the 10 dangerous 24-pin EEPROMs).
Output: Rewritten build_db.py; regenerated chip_database.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-CONTEXT.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-PATTERNS.md
@.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite resolve_pinout_key, delete guess tables, convert overrides to rules (PIN-01, PIN-02, D-02/D-03/D-05/D-06)</name>
  <read_first>
    - firestarter_app/tools/build_db.py (FULL read of the change region: DIP28_VARIANT_MAP line 125, PIN_MAP_TO_PINOUT line 149, PIN_MAP_PROTO_TO_PINOUT line 198, VALID_PINOUT_KEYS line 259, resolve_pinout_key def line 266 body through 327, the call site line ~435, the 24-pin skip block lines ~419-432, _etype Pass 1 lines ~452-459, WARNING-5 block lines ~461-489, fm1608 block lines ~512-534, _etype Pass 2 lines ~547-555)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-PATTERNS.md §"build_db.py — rewrite resolve_pinout_key" (the exact replacement rule structure, the Rule 1/2/3 shapes, the D-06 fail-safe shape, and the MANDATORY execution order list)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"Full Principled Rule Structure", §"Safety Overrides Become Rule Outcomes", §"D-06 Fail-Safe Options", and Pitfalls 1-5
    - firestarter_app/doc/infoic-field-dictionary.md (pm_idx, variant, flags, type, protocol_id decode authority — the rules cite this, do not re-invent decode)
    - firestarter_app/tests/test_decoder.py (the Plan 01 RED tests define the exact contract this rewrite must satisfy)
  </read_first>
  <action>
    Delete the three module-level dicts entirely (D-02): `DIP28_VARIANT_MAP` (~125-130), `PIN_MAP_TO_PINOUT` (~149-186), `PIN_MAP_PROTO_TO_PINOUT` (~198-256), and update the resolve_pinout_key docstring/comments that reference them. Change the signature to `resolve_pinout_key(pin_count, variant, flags_int, pm_idx=None, proto_id=None, type_int=1, mem_size=0)` and rewrite the body as the principled branch structure: compute `variant_lo = variant & 0xFF`; pin_count==24 → pm_idx==23 → {variant_lo==0x01:DIP24_2732, variant_lo==0x10:DIP24_2816, else:DIP24_2716}, pm_idx==0 → DIP24_6116, else None; pin_count==28 → pm_idx==22 → {0x10:DIP28_27512, 0x11:DIP28_27256, else:DIP28_2764}, pm_idx==21:DIP28_2764, pm_idx==20:DIP28_28C256, pm_idx==19:DIP28_28C64, pm_idx==18:DIP28_28C64, pm_idx==0 → (type_int==4 or proto_id in {0x27,0x28,0x29}) → (mem_size<=8192 ? DIP28_JEDEC_SRAM_8K : DIP28_28C256), elif proto_id==0x05 → DIP28_28C256, else None; pin_count==32 → pm_idx==0:DIP32_SST39SF040, pm_idx in {5,7,9,10,11,12,13} → (proto in {0x05,0x06}:DIP32_SST39SF040, proto==0x0D:DIP32_28C512_EEPROM, proto in {0x07,0x08,0x10}:DIP32_STD, else None), else None. Preserve the `VALID_PINOUT_KEYS` tail guard unchanged. Update the call site (~435) to pass `type_int=type_int, mem_size=mem_size` (both already decoded by line ~389). CRITICAL per D-03: NO `if name ==`, no chip-name lists, no family tables — discrimination is `variant_lo == 0x10` for the 28C family (NOT `flags & 0x10`, which misses the 10 dangerous chips — RESEARCH Pitfall 1). Preserve the 28-pin pm_idx==22 variant_lo discriminator EXACTLY (RESEARCH Pitfall 3 — flipping 27512/27256 routes 12V to a wrong VPP pin).

    Delete the hardcoded 24-pin safety-skip block (~419-432). Replace it with the D-06 fail-safe AFTER the resolve_pinout_key call: `if pinout_key is None:` print a loud `WARN: skipping {mfg_name}/{name} — unclassifiable pinout (pin_count=..., pm_idx=..., variant_lo=0x...); add override via ~/.firestarter/database.json` to sys.stderr and `continue` (no VPP-asserting dispatch — D-06 hard constraint). Then add the three named override rules in this MANDATORY order: (Rule 1, replaces the skip) `if pinout_key == "DIP24_2816": proto_id = 0x0D` with an INFO log noting 28C-EEPROM family → configure_eeprom28c no VPP; (Pass 1 flags-based _etype runs next, preserved exactly ~452-459); (Rule 2 WARNING-5, kept shape ~479-489) `if pinout_key in ("DIP28_2764","DIP28_28C256") and proto_id == 0x07 and _etype == "Flash/EEPROM": proto_id = 0x0D` with INFO log; (Rule 3 fm1608, kept shape ~512-534) `if type_int == 4 and proto_id in (0x07,0x08,0x0B): proto_id = 0x28` with its existing SRAM pinout/size-label assignment; (Pass 2 protocol-aware _etype, preserved exactly ~547-555). Enforce the execution order from PATTERNS: resolve_pinout_key → D-06 skip → Pass 1 _etype → Rule 1 → Rule 2 → Rule 3 → Pass 2 _etype → chip_entry. Use `print(..., file=sys.stderr)` for all diagnostics (established build_db.py pattern). Run `ruff check` and `ruff format --check` to keep the CI gate green.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 -m pytest tests/test_decoder.py::TestResolvedPinoutKey tests/test_decoder.py::TestGuessTablesDeleted tests/test_decoder.py::TestWarning5Rule -x -q && ruff check tools/build_db.py && ruff format --check tools/build_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `TestResolvedPinoutKey`, `TestGuessTablesDeleted`, and `TestWarning5Rule` all pass GREEN.
    - `not hasattr(tools.build_db, "PIN_MAP_TO_PINOUT")` and same for `PIN_MAP_PROTO_TO_PINOUT`, `DIP28_VARIANT_MAP` (source assertion via TestGuessTablesDeleted).
    - `resolve_pinout_key` signature includes `type_int` and `mem_size`; body contains no chip-name string literals and no module-level lookup-table references (D-03 — grep `tools/build_db.py` for `if name ==` and chip-name lists returns nothing in resolve_pinout_key).
    - The 28-pin pm_idx==22 discriminator returns DIP28_27512 for variant_lo=0x10 and DIP28_27256 for 0x11 (RESEARCH Pitfall 3 preserved).
    - `ruff check` and `ruff format --check` pass on build_db.py.
  </acceptance_criteria>
  <done>resolve_pinout_key is principled and data-driven; guess tables deleted; overrides are named rules; D-06 fail-safe in place; two-pass _etype preserved; Plan 01's logic tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate chip_database.json and prove the 9-unblock + 10-fix (PIN-03)</name>
  <read_first>
    - firestarter_app/tools/build_db.py (the just-rewritten generator; confirm MINIPRO_XML_URL / pinned infoic.xml snapshot usage from Phase 56)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-RESEARCH.md §"The 9 Blocked Chips" and §"Dangerous 24-pin EEPROMs Already In The DB" (the exact chip names that must land on algo=0x0D + DIP24_2816), §"Runtime State Inventory" (use the Phase 56 pinned snapshot consistently)
    - firestarter_app/tests/test_decoder.py (TestDangerous24pinEEPROMFixed defines the integration bar)
    - firestarter_app/firestarter/data/chip_database.json (the prior 734-chip baseline being regenerated)
  </read_first>
  <action>
    Regenerate the DB with `python3 tools/build_db.py` from `firestarter_app/`, using the same infoic.xml source the Phase 56/57 baseline used (the pinned snapshot if present, else MINIPRO_XML_URL — be consistent so the Phase 59 GATE-02 diff is meaningful). Capture build_db.py's stderr diagnostics (the INFO/WARN lines from Rule 1 and the D-06 skips) into the SUMMARY — these are the audit trail for the Phase 59 per-chip diff (RESEARCH Pitfall 5: blast radius is intentional, every reassignment must be explainable). Confirm the 9 blocked chips (ATMEL AT28C04/AT28C16, MICROCHIP 28C04A/28C16A, NEC UPD28C04) now appear in the DB with algorithm=0x0D and pinout=DIP24_2816, and the 10 previously-dangerous chips (AMD AM28C16A, CAT28C16A, EXEL XL2804A/XL2816A/XLE28C16A/XLE28C16B, MICROCHIP 2804/2816, XICOR X2804A/X2816A/X2816B/X2816C) are fixed off DIP24_2716+0x0B onto DIP24_2816+0x0D. Do NOT hand-edit chip_database.json — it is generated output only. Record the regenerated chip count in the SUMMARY for Phase 59.
  </action>
  <verify>
    <automated>cd firestarter_app && python3 tools/build_db.py >/tmp/build58.log 2>&1; python3 -m pytest tests/test_decoder.py::TestDangerous24pinEEPROMFixed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/build_db.py` completes without error and regenerates `chip_database.json`.
    - `TestDangerous24pinEEPROMFixed` passes GREEN: all 9 blocked + 10 dangerous 24-pin EEPROMs have `algorithm==0x0D` and `pinout=="DIP24_2816"` in the regenerated DB.
    - No chip in the regenerated DB has `pinout=="DIP24_2716"` AND `algorithm==0x0B` for a variant_lo=0x10 part (behavior assertion — the dangerous combination is eliminated).
    - build_db.py stderr Rule-1/D-06 diagnostics captured into the SUMMARY for Phase 59 GATE-02.
  </acceptance_criteria>
  <done>chip_database.json regenerated; the 9 blocked chips are exposed and the 10 dangerous chips are fixed onto DIP24_2816 + algorithm=0x0D; integration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| infoic.xml → resolve_pinout_key → chip_database.json | Wrong pinout/algorithm selection can route 12V VPP to a 5V EEPROM's WE pin → dead chip |
| Unclassifiable chip → DB emission | Emitting an uncertain chip with a VPP-asserting handler is a hardware-damage path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering (physical-hardware damage) | resolve_pinout_key 24-pin EEPROM branch | mitigate | Discriminate the 28C family by `variant_lo == 0x10` (NOT `flags & 0x10`), routing all 19 affected chips to DIP24_2816 (no vpp-pin) + algorithm=0x0D (configure_eeprom28c, 5V-only). Proven by TestDangerous24pinEEPROMFixed; backstopped by GATE-03 in Plan 03. |
| T-58-03 | Tampering (regression) | 28-pin pm_idx==22 variant_lo discriminator | mitigate | Preserve the 0x10→DIP28_27512 / 0x11→DIP28_27256 mapping exactly (RESEARCH Pitfall 3); a flip would put 12V on a wrong VPP pin of a real UV-EPROM. Covered by TestResolvedPinoutKey 28-pin cases. |
| T-58-04 | Denial (unclassifiable chip) | D-06 fail-safe | mitigate | resolve_pinout_key returns None for unknown clusters → loud WARN + continue, never emitted. Hard constraint: no uncertain chip emits a VPP-asserting dispatch (D-06). Operator adds it via ~/.firestarter/database.json. |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase (host-only edits to existing tooling); pip test deps already vetted in prior phases. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_decoder.py -x -q` green (all Plan 01 RED classes now GREEN).
- `python3 tools/build_db.py` regenerates chip_database.json without error.
- `resolve_pinout_key` contains no per-IC special code (D-03).
- GATE-03 (`python3 tools/check_dispatch.py`) is verified in Plan 03 against this regenerated DB.
</verification>

<success_criteria>
- PIN-01: resolve_pinout_key is principled and data-driven; the three guess tables are gone.
- PIN-02: WARNING-5 and fm1608 survive as rule outcomes; two-pass _etype preserved; D-06 fail-safe never emits a VPP-asserting uncertain chip.
- PIN-03: the 9 blocked chips are exposed and the 10 dangerous chips are fixed onto DIP24_2816 + algorithm=0x0D.
</success_criteria>

<output>
Create `.planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-02-SUMMARY.md` when done. Include the regenerated chip count and the captured Rule-1/D-06 stderr diagnostics for Phase 59 GATE-02.
</output>
