---
phase: 57-decode-bug-fixes-protocol-map-check-dispatch-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/check_dispatch.py
autonomous: true
requirements: [GATE-03, DEC-05]
must_haves:
  truths:
    - "check_dispatch.py no longer maps 0x35/0x39 in _ALGO_MEM_TYPE or routes them in dispatch()"
    - "check_dispatch.py loads pinouts.json and builds the set of vpp-pin pinouts dynamically"
    - "check_dispatch.py flags any chip with a vpp-pin pinout AND algorithm in {0x05,0x06,0x0D} routing to configure_eprom (full-class VPP guard, not just DIP28_2764)"
    - "check_dispatch.py exits 0 against the current chip_database.json"
    - "check_dispatch.py passes ruff check and ruff format --check"
  artifacts:
    - path: "firestarter_app/tools/check_dispatch.py"
      provides: "Full-class VPP-safety dispatch guard + 0x35/0x39 sync removal"
      contains: "_vpp_pinouts"
  key_links:
    - from: "firestarter_app/tools/check_dispatch.py"
      to: "firestarter/data/pinouts.json"
      via: "json.load of _PINOUT_FILE to find vpp-pin pinouts"
      pattern: "vpp-pin"
    - from: "check_dispatch.py vpp_eeprom_in_eprom"
      to: "sys.exit(1) gate"
      via: "inclusion in the errors-or-... exit condition"
      pattern: "vpp_eeprom_in_eprom"
---

<objective>
Extend `check_dispatch.py` from the single `DIP28_2764` WARNING-5 case to a full-class VPP-safety guard (GATE-03), and sync the 0x35/0x39 removal (DEC-05) so the dispatch script stays consistent with the build_db.py allowlist change in plan 57-01. This plan touches ONLY `check_dispatch.py` (zero file overlap with 57-01, so the two wave-1 plans run in parallel).

GATE-03 is the dominant safety requirement of this phase (threat T-57-01): a chip with a `vpp-pin` pinout AND a 5V-EEPROM-family handler (algorithm in {0x05, 0x06, 0x0D}) must never route to a VPP-asserting path (`configure_eprom`), because that applies 12V programming voltage to a 5V part — a hardware-damage path. The current guard only covers the `DIP28_2764` pinout; this generalizes it to every pinout that has a `vpp-pin` field, loaded dynamically from `pinouts.json` so it auto-covers any pinout Phase 58 may add.

Purpose: lock in the VPP-safety invariant as a full-class assertion BEFORE the Phase 58 pinout re-derivation lands (per the STATE.md ordering constraint "check_dispatch.py full-class VPP-safety guard BEFORE re-derivation changes land").

Output: a corrected `firestarter_app/tools/check_dispatch.py` that exits 0 on the current DB with the new guard active and the 0x35/0x39 entries removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md
@.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-PATTERNS.md
@firestarter_app/doc/infoic-field-dictionary.md
@firestarter_app/tools/check_dispatch.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 0x35/0x39 from check_dispatch.py to sync with build_db.py (DEC-05)</name>
  <files>firestarter_app/tools/check_dispatch.py</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (lines 35-49 _ALGO_MEM_TYPE; line 72 dispatch() configure_flash4 branch)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md (SC-4 secondary fix for check_dispatch.py; Pitfall 1 — must remove in the same change set as the build_db.py KNOWN_PROTOCOLS removal)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-PATTERNS.md (the _ALGO_MEM_TYPE removal block + the dispatch() trailing-space alignment idiom for line 72)
  </read_first>
  <action>
    Remove the `0x35: 5` and `0x39: 5` entries from `_ALGO_MEM_TYPE` (lines 47-48) entirely — no replacement comment needed; they are phantom IDs that build_db.py no longer emits after 57-01. In `dispatch()` (line 72) change `if protocol in (0x05, 0x35, 0x39): return "configure_flash4"` to `if protocol == 0x05: return "configure_flash4"`, preserving the trailing-space column alignment so the `return` keyword stays aligned with the surrounding `dispatch()` return statements (the function right-aligns returns at the same column; adjust padding after narrowing the tuple to a single equality).
  </action>
  <verify>
    <automated>cd firestarter_app && python -c "import sys; sys.path.insert(0,'.'); import tools.check_dispatch as cd; assert 0x35 not in cd._ALGO_MEM_TYPE and 0x39 not in cd._ALGO_MEM_TYPE; assert cd.dispatch(0x05,5)=='configure_flash4'; assert cd.dispatch(0x06,3)=='configure_flash3'" && ruff check tools/check_dispatch.py && ruff format --check tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `_ALGO_MEM_TYPE` has no 0x35 or 0x39 keys.
    - `dispatch(0x05, 5) == "configure_flash4"` and the function no longer references 0x35/0x39.
    - `grep -c '0x35\|0x39' firestarter_app/tools/check_dispatch.py` returns 0.
    - ruff check + ruff format --check on check_dispatch.py pass.
  </acceptance_criteria>
  <done>0x35/0x39 removed from both _ALGO_MEM_TYPE and dispatch(); column alignment preserved; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the full-class vpp-pin + 5V-EEPROM-family VPP-safety guard (GATE-03)</name>
  <files>firestarter_app/tools/check_dispatch.py</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (full file — especially the existing eeprom28c_in_eprom guard: list init at line 97, extraction + check at lines 119-130, FAIL reporting at lines 166-174, exit gate at line 148, PASS message at lines 185-190)
    - firestarter_app/firestarter/data/pinouts.json (to confirm the "pins" -> "vpp-pin" structure the dynamic load keys off; do NOT hardcode the pinout names)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md (SC-5 / GATE-03 section — dynamic pinouts.json load, _5v_eeprom_algos={0x05,0x06,0x0D}, current violation count = 0, AT29C256/AT29LV256 route to configure_flash4 not configure_eprom)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-PATTERNS.md (GATE-03 block — copy the eeprom28c_in_eprom guard idiom verbatim; the 8 enumerated insertion points)
  </read_first>
  <action>
    Implement the GATE-03 full-class guard by copying the existing `eeprom28c_in_eprom` guard idiom verbatim and changing only the list name, predicate, and FAIL/PASS messages, per the 8 insertion points in 57-PATTERNS.md:

    1. After the `DB_FILE` constant (around line 30), add `_PINOUT_FILE = os.path.join(_DATA_DIR, "pinouts.json")`. Use the already-imported `json` module (do not add a `_json` alias).
    2. Inside `main()`, after the DB is loaded (after line 87), load pinouts.json and build `_vpp_pinouts = frozenset(k for k, v in _pinouts_raw.items() if "vpp-pin" in v.get("pins", {}))` and `_5v_eeprom_algos = frozenset({0x05, 0x06, 0x0D})`. Add a comment that the dynamic load avoids hardcoding so Phase 58 pinouts auto-covered.
    3. After `eeprom28c_in_eprom = []` (line 97), add `vpp_eeprom_in_eprom = []`.
    4. Inside the chip loop, immediately after the existing `eeprom28c_in_eprom.append(...)` block (after line 130), add the new check. REUSE the already-extracted `pinout` variable (extracted at line 121); do NOT re-extract it and do NOT use `etype` (the predicate is `proto in _5v_eeprom_algos`, not etype-based): if `pinout in _vpp_pinouts and proto in _5v_eeprom_algos and handler == "configure_eprom"`, append `f"{mfg}/{part} proto=0x{proto:02X} pinout={pinout}"` to `vpp_eeprom_in_eprom`. Comment it `# GATE-03: full-class VPP-safety guard`.
    5. Add `vpp_eeprom_in_eprom` to the exit-gate condition at line 148 (`if errors or sram_in_eprom or eeprom28c_in_eprom or vpp_eeprom_in_eprom or wire_regressions:`).
    6. Add a `if vpp_eeprom_in_eprom:` FAIL-reporting block mirroring the eeprom28c_in_eprom block, with message `FAIL: {n} vpp-pin Flash/EEPROM chips route to configure_eprom (GATE-03: VPP-class hazard):` and the same `[:20]` / `... and N more` truncation.
    7. Add a new line to the PASS f-string: `0 vpp-pin Flash/EEPROM chips route to configure_eprom;`.

    Do not modify the existing sram_in_eprom, eeprom28c_in_eprom, or wire_regressions logic — only add alongside them.
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/check_dispatch.py; echo "exit=$?"; ruff check tools/check_dispatch.py && ruff format --check tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd firestarter_app && python tools/check_dispatch.py` exits 0 (0 violations) against the current DB.
    - Its PASS line includes the phrase `vpp-pin Flash/EEPROM chips route to configure_eprom`.
    - `grep -c '_vpp_pinouts' firestarter_app/tools/check_dispatch.py` >= 2 (definition + use); the set is built from `pinouts.json`, not a hardcoded pinout-name literal (no literal `{"DIP28_2764", ...}` style set for vpp pinouts).
    - `_5v_eeprom_algos` equals `{0x05, 0x06, 0x0D}`.
    - ruff check + ruff format --check on check_dispatch.py pass.
  </acceptance_criteria>
  <done>The full-class vpp-pin + {0x05,0x06,0x0D} -> configure_eprom guard is active, exits 0 on the current DB, and the vpp-pinout set is loaded dynamically from pinouts.json; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip_database.json -> firmware dispatch | A chip's algorithm + pinout determine which firmware handler runs; a wrong pairing can assert 12V VPP on a 5V part's pin (hardware damage) |
| pinouts.json -> check_dispatch guard | The set of VPP-bearing pinouts is read from pinouts.json; the guard's coverage is only as correct as that file's vpp-pin fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering/Elevation (PRIMARY) | check_dispatch.py dispatch guard | mitigate | Full-class assertion: no chip with a vpp-pin pinout AND algorithm in {0x05,0x06,0x0D} (5V EEPROM/flash family) may route to configure_eprom (the VPP-asserting path). Generalizes the single-pinout WARNING-5 guard to every vpp-pin pinout, loaded dynamically from pinouts.json so Phase 58 additions are auto-covered. Must exit 0 on the current DB (verified: AT29C256/AT29LV256 route to configure_flash4). This is the safety gate that prevents 12V on a 5V part. |
| T-57-04 | Tampering | _ALGO_MEM_TYPE / dispatch() 0x35/0x39 dead paths | mitigate | Removing the 0x35/0x39 dispatch entries eliminates dead routing that could silently mis-handle a phantom-ID chip if upstream drift ever emitted one. |
| T-57-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; edits to an existing file only. |
</threat_model>

<verification>
- `cd firestarter_app && python tools/check_dispatch.py` exits 0 with the extended PASS message (run against the CURRENT, not-yet-regenerated DB — the guard must already pass; per research the current DB has 0 violations).
- `cd firestarter_app && ruff check tools/check_dispatch.py && ruff format --check tools/check_dispatch.py` — clean against the firestarter_app target.
- Note: after plan 57-03 regenerates the DB, check_dispatch.py is re-run as the GATE-03 acceptance check on the regenerated set; this plan establishes the guard and proves it passes on the pre-regeneration DB.
</verification>

<success_criteria>
- GATE-03: check_dispatch.py asserts the full-class vpp-pin + {0x05,0x06,0x0D} invariant (not just DIP28_2764) and exits 0 on the current chip set; the vpp-pinout set is dynamic from pinouts.json.
- DEC-05 (sync): 0x35/0x39 removed from _ALGO_MEM_TYPE and dispatch(), consistent with the build_db.py KNOWN_PROTOCOLS change in 57-01.
- check_dispatch.py passes ruff check + ruff format --check; existing sram/eeprom28c/wire guards unchanged.
</success_criteria>

<output>
Create `.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-02-SUMMARY.md` when done.
</output>
