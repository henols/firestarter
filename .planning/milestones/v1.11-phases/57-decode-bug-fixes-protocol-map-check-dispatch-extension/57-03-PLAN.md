---
phase: 57-decode-bug-fixes-protocol-map-check-dispatch-extension
plan: 03
type: execute
wave: 2
depends_on: [57-01, 57-02]
files_modified:
  - firestarter_app/firestarter/data/chip_database.json
  - firestarter_app/tests/__snapshots__/test_characterization.ambr
autonomous: false
requirements: [DEC-02, DEC-03, DEC-04, GATE-03]
must_haves:
  truths:
    - "chip_database.json regenerated from the corrected build_db.py"
    - "W27C512 reports pulse_duration 100 us (not 10000 us) in the regenerated DB and via firestarter info"
    - "A baseline diff vs chip_database.baseline.json shows ONLY pulse_duration and vcc/vdd field changes (no algorithm/pinout/vpp_mv/electrical.type chipset changes; no chip added or removed)"
    - "check_dispatch.py exits 0 against the regenerated DB"
    - "full pytest suite is green (470+ tests) with coverage >= 70% after snapshot refresh"
  artifacts:
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "Regenerated database with corrected decode values"
    - path: "firestarter_app/tests/__snapshots__/test_characterization.ambr"
      provides: "Refreshed snapshots reflecting corrected pulse_duration values"
  key_links:
    - from: "firestarter_app/tools/build_db.py"
      to: "firestarter/data/chip_database.json"
      via: "python tools/build_db.py regeneration"
      pattern: "chip_database.json"
    - from: "firestarter_app/firestarter/data/chip_database.json"
      to: "firestarter_app/tools/baseline/chip_database.baseline.json"
      via: "before/after diff harness (Phase 56)"
      pattern: "baseline"
---

<objective>
Regenerate `chip_database.json` from the corrected `build_db.py` (plan 57-01), prove via a baseline diff that ONLY the intended fields changed, re-run the GATE-03 guard (plan 57-02) against the regenerated set, refresh the affected syrupy snapshots, and confirm the full suite plus the end-user `firestarter info W27C512` behavior. This is the wave-2 integration/verification plan; it depends on both wave-1 source edits.

This plan owns the artifacts that both wave-1 plans deliberately did NOT touch: the generated `chip_database.json` and the test snapshots. The full per-chip diff REVIEW belongs to Phase 59 (GATE-02); this plan's diff is a focused before/after to confirm Phase-57 intent (pulse_duration + vcc/vdd corrections, no chipset/routing change), not the full GATE-02 review.

Purpose: deliver the user-visible DEC-03 outcome (`firestarter info W27C512` shows 100 us) and confirm GATE-03 passes on the regenerated DB, while proving the decode fixes did not silently alter algorithm/pinout/vpp routing for any chip.

Output: regenerated `chip_database.json`, refreshed snapshots, a recorded baseline-diff summary in the plan SUMMARY, and a green full suite. Contains a blocking human-verify checkpoint for the DB regeneration (live upstream fetch) and the `firestarter info` CLI surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md
@.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-VALIDATION.md
@firestarter_app/doc/infoic-field-dictionary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate chip_database.json and diff against the Phase 56 baseline (DEC-02/DEC-03/DEC-04)</name>
  <files>firestarter_app/firestarter/data/chip_database.json</files>
  <read_first>
    - firestarter_app/tools/build_db.py (the corrected file after 57-01 — confirm the four fixes are present before regenerating)
    - firestarter_app/tools/baseline/chip_database.baseline.json (the Phase 56 pre-milestone snapshot used for before/after diffing)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md (Pitfall 6 on live upstream fetch drift; the 252-chips-affected and W27C512 10000->100 expectations; the AT28C256 open question on nibble 0x02/0x03)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-VALIDATION.md (Wave 0 baseline-diff harness note)
  </read_first>
  <action>
    Run `cd firestarter_app && python tools/build_db.py` to regenerate `firestarter/data/chip_database.json` from the corrected decoder. The tool fetches live from upstream master (D-01); if the fetch fails, retry, and if upstream is unreachable, STOP and surface the failure rather than committing a partial DB.

    Produce a focused before/after diff between the regenerated `firestarter/data/chip_database.json` and `tools/baseline/chip_database.baseline.json` using jq or a short inline Python script (do NOT add a new committed tool). Classify every changed chip by which field(s) changed. EXPECTED change classes: (a) `programming.pulse_duration` for ~252 chips on protocols 0x07/0x0B (e.g. W27C512 "10000 us" -> "100 us"); (b) `electrical.vcc` / `electrical.vdd` values where a chip's voltages nibbles differ (label swap and/or newly-decoded 0x02/0x03 nibbles). UNEXPECTED change classes that must NOT appear: any change to `programming.algorithm`, `pinout`, `electrical.vpp_mv`, or `electrical.type`, and any chip added or removed (the 0x35/0x39 removal must not change the chip set — research confirms no DB chip uses those algorithms). Record the per-field change counts in the SUMMARY. If any unexpected change class appears, STOP and report it as a finding before proceeding (may indicate upstream drift per Pitfall 6, to be reconciled, or a fix regression).

    Spot-check the AT28C256 (and AT28C64) entries: record whether their `electrical.vcc`/`vdd` changed from "5V" to "4V"/"4.5V" (confirms the BUG-1 benefit) or stayed "5V" (means their nibble is 0x00 and the fix corrects other chips) — note the observed result in the SUMMARY either way.
  </action>
  <verify>
    <automated>cd firestarter_app && python -c "import json; db=json.load(open('firestarter/data/chip_database.json')); hits=[c['programming']['pulse_duration'] for cs in db.values() for c in cs if 'W27C512' in c.get('part_number','')]; print('W27C512 pulse_duration:', hits); assert any(h=='100 us' for h in hits), hits"</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/build_db.py` completes without error and writes `firestarter/data/chip_database.json`.
    - W27C512 `programming.pulse_duration` == "100 us" in the regenerated DB.
    - The baseline diff shows changes confined to `programming.pulse_duration` and `electrical.vcc`/`electrical.vdd`; zero changes to `programming.algorithm`, `pinout`, `electrical.vpp_mv`, `electrical.type`; zero chips added/removed. Counts recorded in the SUMMARY.
    - AT28C256/AT28C64 vcc/vdd observed result recorded.
  </acceptance_criteria>
  <done>DB regenerated; W27C512 reads 100 us; baseline diff confined to the two intended field classes with no chipset/routing change; per-field counts and the AT28C256 result recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run GATE-03 and refresh snapshots against the regenerated DB (GATE-03)</name>
  <files>firestarter_app/tests/__snapshots__/test_characterization.ambr</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (the extended guard after 57-02)
    - firestarter_app/tests/__snapshots__/test_characterization.ambr and firestarter_app/tests/test_characterization.py (test_list line ~239, test_search_w27 line ~261, test_info_known_chip line ~246)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md (Test Suite Architecture section + Pitfall 2 — snapshot update after pulse_duration fix; test_info_known_chip pins a crash and should NOT need updating)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-PATTERNS.md (Test / Regression Analogs section — which snapshots to refresh and what to inspect in the diff)
  </read_first>
  <action>
    Run `cd firestarter_app && python tools/check_dispatch.py` against the regenerated DB; it must exit 0 with the extended PASS message (including the `vpp-pin Flash/EEPROM chips route to configure_eprom` counter). This is the GATE-03 acceptance check on the real regenerated chip set.

    Run the snapshot-affected characterization tests: `cd firestarter_app && python -m pytest tests/test_characterization.py -q`. Where tests fail only because `pulse_duration` values changed for 0x07/0x0B chips, refresh with `python -m pytest tests/test_characterization.py --snapshot-update`, then INSPECT the snapshot diff (`git diff tests/__snapshots__/test_characterization.ambr`) and confirm every changed line is a pulse_duration (or vcc/vdd) value change — no unrelated output changed. Do NOT blanket-update if a non-pulse_duration / non-voltage line changed; investigate first. Note: `test_info_known_chip` pins an existing `firestarter info W27C512` crash (a vpp-pin TypeError in ic_layout.py NOT fixed in this phase) — it should remain unchanged; if it changed, that is a finding to record, not a rubber-stamp.
  </action>
  <verify>
    <automated>cd firestarter_app && python tools/check_dispatch.py && python -m pytest tests/test_characterization.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/check_dispatch.py` exits 0 against the regenerated DB with the extended PASS message.
    - `python -m pytest tests/test_characterization.py -q` passes after the snapshot refresh.
    - `git diff tests/__snapshots__/test_characterization.ambr` shows only pulse_duration / vcc / vdd value changes (recorded in SUMMARY); test_info_known_chip snapshot unchanged (or any change explicitly noted as a finding).
  </acceptance_criteria>
  <done>GATE-03 passes on the regenerated DB; only pulse_duration/voltage-driven snapshot lines refreshed; no unexplained snapshot changes.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + ruff gate green (DEC-02/DEC-03/DEC-04/GATE-03 regression close)</name>
  <files>firestarter_app/tests/test_decoder.py</files>
  <read_first>
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-VALIDATION.md (full suite command + coverage floor 70%)
    - .planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-RESEARCH.md (coverage floor note — tools/ scripts may affect the floor; CI targets py39/3.11)
  </read_first>
  <action>
    Run the full regression suite and quality gates against the firestarter_app target: `cd firestarter_app && python -m pytest tests/ --cov-fail-under=70` (470+ tests green, coverage >= 70%), and `ruff check .` + `ruff format --check .` over the touched files (`tools/build_db.py`, `tools/check_dispatch.py`, `tests/test_decoder.py`). Per project memory, validate ruff against the firestarter_app CI target (py39/3.11 semantics) — do not rely solely on the local 3.12 result; if any f-string or codegen drift would differ under the CI target, fix it here. If coverage dropped below 70% because new decoder tests or guard lines shifted the measured set, add the minimal targeted test needed to restore the floor (the 57-01 decoder tests should already cover the new branches).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/ --cov-fail-under=70 -q && ruff check tools/build_db.py tools/check_dispatch.py tests/test_decoder.py && ruff format --check tools/build_db.py tools/check_dispatch.py tests/test_decoder.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ --cov-fail-under=70` passes (470+ tests, coverage >= 70%).
    - `ruff check` and `ruff format --check` pass on all touched files.
  </acceptance_criteria>
  <done>Full suite green at >=70% coverage; ruff clean on all touched files against the firestarter_app target.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify DEC-03 CLI surface and clean regeneration</name>
  <action>Pause for operator verification of the regenerated DB per the how-to-verify steps below; no automated work in this task.</action>
  <what-built>The corrected decode pipeline was regenerated into chip_database.json (W27C512 pulse_duration 100 us), GATE-03 passes on the regenerated DB, and the full suite is green. This checkpoint verifies the user-visible DEC-03 CLI surface and confirms the regeneration came from a clean live upstream fetch (not a partial/drifted snapshot).</what-built>
  <how-to-verify>
    1. From `firestarter_app/`, run `firestarter info W27C512` (or the nearest equivalent DB query) and confirm the reported pulse/timing value reads 100 us, NOT 10000 us. (If `firestarter info W27C512` hits the known ic_layout.py vpp-pin crash that is out of scope for Phase 57, instead confirm via `python -c "import json; db=json.load(open('firestarter/data/chip_database.json')); print([c['programming']['pulse_duration'] for cs in db.values() for c in cs if 'W27C512' in c.get('part_number','')])"` showing "100 us".)
    2. Review the baseline-diff summary recorded in 57-03-SUMMARY.md and confirm the per-field change counts match the Phase-57 intent (pulse_duration + vcc/vdd only; no algorithm/pinout/vpp_mv/type changes; no chips added/removed).
    3. Confirm the regenerated chip_database.json was produced from a successful live upstream fetch (no fetch error in the build_db.py output).
  </how-to-verify>
  <resume-signal>Type "approved" to accept, or describe the discrepancy (e.g. wrong pulse value, unexpected diff field, or fetch problem).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream infoic.xml (live fetch) -> regenerated chip_database.json | A live fetch may drift from the Phase 56 baseline; drift could introduce changes unrelated to the Phase-57 fixes |
| chip_database.json -> firmware dispatch | A regenerated chip's algorithm+pinout pairing determines the firmware handler; GATE-03 re-validated here against the real regenerated set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering/Elevation (PRIMARY) | check_dispatch.py vs regenerated DB | mitigate | GATE-03 full-class VPP guard is re-run against the regenerated chip set and must exit 0 — proving no chip with a vpp-pin pinout + {0x05,0x06,0x0D} routes to the VPP-asserting configure_eprom path after regeneration. |
| T-57-05 | Tampering | live upstream fetch drift | mitigate | Baseline diff vs chip_database.baseline.json classifies every changed field; any change outside {pulse_duration, vcc, vdd} halts the task as a finding rather than being committed silently (Pitfall 6). Blocking human-verify confirms the fetch succeeded and the diff matches intent. |
| T-57-SC | Tampering | npm/pip/cargo installs | n/a | No package installs; regeneration uses the already-pinned requests/pytest toolchain. |
</threat_model>

<verification>
- `cd firestarter_app && python tools/build_db.py` regenerates the DB from a clean live fetch.
- Baseline diff vs `tools/baseline/chip_database.baseline.json` confined to pulse_duration + vcc/vdd (counts recorded in SUMMARY).
- `cd firestarter_app && python tools/check_dispatch.py` exits 0 (GATE-03 on the regenerated set).
- `cd firestarter_app && python -m pytest tests/ --cov-fail-under=70` green; snapshot refresh limited to pulse_duration/voltage lines.
- `ruff check` + `ruff format --check` clean on touched files against the firestarter_app target.
- Blocking human-verify confirms `firestarter info W27C512` shows 100 us and the diff matches intent.
</verification>

<success_criteria>
- DEC-03 (user-visible): `firestarter info W27C512` / DB query reports 100 us, not 10000 us.
- DEC-04 (regenerated): vcc/vdd corrections present in the regenerated DB; AT28C256/AT28C64 result recorded.
- DEC-02 (umbrella close): regeneration + baseline diff prove only the intended decode fields changed across the whole DB.
- GATE-03: check_dispatch.py exits 0 on the regenerated chip set with the full-class guard.
- Full suite green (470+ tests) at >=70% coverage; snapshots refreshed to corrected values only; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/57-decode-bug-fixes-protocol-map-check-dispatch-extension/57-03-SUMMARY.md` when done.
</output>
