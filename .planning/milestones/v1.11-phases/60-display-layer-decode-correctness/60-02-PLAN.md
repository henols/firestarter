---
phase: 60-display-layer-decode-correctness
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - firestarter_app/tests/__snapshots__/test_characterization.ambr
autonomous: true
requirements: [DEC-01, DEC-02, DEC-03, DEC-04, DEC-05]
must_haves:
  truths:
    - "test_info_known_chip snapshot reflects the corrected W27C512 EEPROM output (Type=EEPROM, electrically erasable, VPP shown, no '-- NOT VERIFIED --', reconciled flags) (regression canary)"
    - "test_list snapshot is confirmed unaffected or, if affected, updated and inspected"
    - "firestarter info W27C512 subprocess matches the updated snapshot"
  artifacts:
    - path: "firestarter_app/tests/__snapshots__/test_characterization.ambr"
      provides: "Updated W27C512 info snapshot = post-fix EEPROM ground truth"
      contains: "EEPROM"
  key_links:
    - from: "firestarter_app/tests/test_characterization.py test_info_known_chip"
      to: "tests/__snapshots__/test_characterization.ambr"
      via: "syrupy subprocess snapshot of `firestarter info W27C512`"
      pattern: "test_info_known_chip"
---

<objective>
Regenerate and inspect the `test_info_known_chip` syrupy snapshot so it captures the
corrected post-fix W27C512 EEPROM output produced by Plan 01. This snapshot is the
end-to-end regression canary: it currently pins the WRONG output (Type: UV-EPROM,
Can be erased: false UV-only, no VPP, `-- NOT VERIFIED --`). After Plan 01's code edits it
must reflect Type=EEPROM, electrically-erasable, VPP shown, marker removed, and the
reconciled flags description.

Purpose: A stale snapshot will fail CI after the display fix; regenerating it locks in the
corrected operator-facing output. Also confirm whether the separate `test_list` view (a
different `print_eprom_list_table` code path keyed on type-int) is affected.

Output: updated `tests/__snapshots__/test_characterization.ambr`.

HOST-ONLY. Edits land in the `firestarter_app/` sub-repo on branch
`v1.11-infoic-decode-correctness`. Snapshot is regenerated via syrupy, never hand-edited.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-display-layer-decode-correctness/60-CONTEXT.md
@.planning/phases/60-display-layer-decode-correctness/60-RESEARCH.md
@.planning/phases/60-display-layer-decode-correctness/60-VALIDATION.md
@.planning/phases/60-display-layer-decode-correctness/60-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and inspect the W27C512 info snapshot (regression canary)</name>
  <files>firestarter_app/tests/__snapshots__/test_characterization.ambr</files>
  <read_first>
    - .planning/phases/60-display-layer-decode-correctness/60-RESEARCH.md (§Snapshot Impact — exact lines that must change; §Don't Hand-Roll — use `--snapshot-update`, never hand-edit; Open Question 3 — test_list path)
    - .planning/phases/60-display-layer-decode-correctness/60-CONTEXT.md (D-01, D-02, D-05, D-07 — what the corrected output must show)
    - .planning/phases/60-display-layer-decode-correctness/60-VALIDATION.md (row "Snapshot non-regression"; sign-off item "snapshot updated to corrected EEPROM output")
    - .planning/phases/60-display-layer-decode-correctness/60-01-SUMMARY.md (what Plan 01 actually changed)
    - firestarter_app/tests/test_characterization.py (L239 test_list, L246 test_info_known_chip; the scrubbing helper)
    - firestarter_app/tests/__snapshots__/test_characterization.ambr (current W27C512 block ~L313-360 + the test_list block ~L362+)
  </read_first>
  <action>
    Confirm Plan 01 is complete (60-01-SUMMARY.md exists and its gate passed). From
    `firestarter_app/` run `pytest --snapshot-update tests/test_characterization.py::test_info_known_chip`.
    Then INSPECT the regenerated `.ambr` block and confirm it reflects the corrected EEPROM
    output (per RESEARCH §Snapshot Impact): the `Eprom Info` header no longer shows
    `-- NOT VERIFIED --` (D-05); `Type:` shows the EEPROM label (D-01); `Can be erased:`
    shows electrically-erasable wording (D-02); a `VPP:` line now appears (D-07); the `Flags:`
    value and properties reflect the reconciled 0x10 = electrically-erasable description
    (D-03 + D-07). Do NOT hand-edit the `.ambr` file — regenerate via syrupy only.

    Then check `test_list` (Open Question 3): run `pytest tests/test_characterization.py::test_list -x`.
    Research expects the list view (`print_eprom_list_table`, type-int keyed) to be unaffected
    by the info-path Type change — CONFIRM, don't assume. If `test_list` fails because the
    list Type column genuinely changed, regenerate that snapshot too with
    `--snapshot-update` and inspect the diff; if it passes, leave it untouched. Do NOT
    regenerate `chip_database.json`; do NOT touch firmware.
  </action>
  <verify>
    <automated>cd firestarter_app && pytest tests/test_characterization.py::test_info_known_chip tests/test_characterization.py::test_list -q</automated>
  </verify>
  <acceptance_criteria>
    - The regenerated W27C512 block in `test_characterization.ambr` shows the EEPROM Type label, electrically-erasable "Can be erased" line, a VPP line, and no `-- NOT VERIFIED --`.
    - `pytest tests/test_characterization.py::test_info_known_chip -q` exits 0 against the updated snapshot.
    - `pytest tests/test_characterization.py::test_list -q` exits 0 (snapshot unchanged if list path unaffected, or regenerated + inspected if affected — decision recorded in SUMMARY).
    - `.ambr` was regenerated via `--snapshot-update`, not hand-edited.
    (Ties to 60-VALIDATION.md row "Snapshot non-regression" + sign-off item.)
  </acceptance_criteria>
  <done>Snapshot canary reflects corrected EEPROM output; test_list status confirmed; both snapshot tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite green before /gsd-verify-work</name>
  <files>firestarter_app/tests/__snapshots__/test_characterization.ambr</files>
  <read_first>
    - .planning/phases/60-display-layer-decode-correctness/60-VALIDATION.md (§Sampling Rate "Before /gsd-verify-work: Full suite must be green"; rows Ruff clean + Coverage floor)
    - firestarter_app/CLAUDE.md (Tooling gate)
  </read_first>
  <action>
    Run the full phase gate from `firestarter_app/` to confirm the snapshot update did not
    regress anything: ruff check + ruff format check + full suite with coverage floor. No
    code edits expected here — this is the pre-verify gate. If the toolchain is wiped restore
    with `pip install -e '.[test]'` (use `/usr/local` python).
  </action>
  <verify>
    <automated>cd firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && pytest --cov=firestarter --cov-fail-under=70 -q</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check firestarter/ tests/` exits 0.
    - `ruff format --check firestarter/ tests/` exits 0.
    - `pytest --cov=firestarter --cov-fail-under=70 -q` exits 0 (all tests incl. updated snapshots pass; coverage ≥ 70%).
    (Ties to 60-VALIDATION.md sign-off: full suite green before /gsd-verify-work.)
  </acceptance_criteria>
  <done>Full suite + snapshots green, ruff clean, coverage ≥ 70%. Phase ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only regenerates a test snapshot of display text from the trusted local DB. |

## STRIDE Threat Register

No applicable threats — display-only host code reading a trusted local DB; no auth,
session, access-control, crypto, network, or untrusted input. No new ASVS categories apply.
</threat_model>

<verification>
- `pytest tests/test_characterization.py::test_info_known_chip tests/test_characterization.py::test_list -q`
- `ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/`
- `pytest --cov=firestarter --cov-fail-under=70 -q`
</verification>

<success_criteria>
- `test_info_known_chip` snapshot reflects corrected W27C512 EEPROM output (regression canary).
- `test_list` snapshot status confirmed (unaffected, or updated + inspected).
- Full suite green, ruff clean, coverage ≥ 70%.
</success_criteria>

<output>
Create `.planning/phases/60-display-layer-decode-correctness/60-02-SUMMARY.md` when done
</output>
