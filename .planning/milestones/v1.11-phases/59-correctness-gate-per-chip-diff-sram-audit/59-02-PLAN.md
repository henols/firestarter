---
phase: 59-correctness-gate-per-chip-diff-sram-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md
  - firestarter_app/doc/sram-nvram-behavior.md
autonomous: true
requirements: [GATE-04]
must_haves:
  truths:
    - "configure_sram is audited and confirmed a near-no-op (no VPP assertion, no regulator enable)"
    - "The host-side SRAM dispatch (check_dispatch.py BLOCKER-2 guard) is audited: 0 SRAM-protocol chips route to configure_eprom"
    - "Three NVRAM behavioral truths are documented in both layers: blank-check limitation, WP# behavior (DS1225/M48T08 class), RTC-oscillator side effect"
    - "A safety verdict is recorded: no genuine safety issue found, no firmware escalation needed (D-04) — and the escalation criterion is stated, not silently dismissed"
  artifacts:
    - path: ".planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md"
      provides: "GATE-04 planning-layer audit trail (investigation-canonical)"
      contains: "Safety Verdict"
      min_lines: 40
    - path: "firestarter_app/doc/sram-nvram-behavior.md"
      provides: "GATE-04 shipped, GitHub-visible operator-facing doc (D-04 layer 2)"
      contains: "RTC"
      min_lines: 30
  key_links:
    - from: "firestarter_app/doc/sram-nvram-behavior.md"
      to: ".planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md"
      via: "front-matter 'Full audit trail' pointer"
      pattern: "59-SRAM-AUDIT"
    - from: "59-SRAM-AUDIT.md"
      to: "firestarter/src/proms/sram.cpp"
      via: "firmware-layer audit (near-no-op finding)"
      pattern: "configure_sram"
---

<objective>
Deliver GATE-04: audit the `configure_sram` NVRAM/SRAM path and document its behavioral
truths in two layers (D-04) — a planning-layer audit trail
(`.planning/phases/59-.../59-SRAM-AUDIT.md`, investigation-canonical) and a shipped,
GitHub-visible operator doc (`firestarter_app/doc/sram-nvram-behavior.md`), kept in lockstep.
Document all three required truths: (a) blank-check limitation (NVRAM/FRAM is never
factory-blank), (b) WP# pin behavior for representative families (DS1225 / M48T08 class),
(c) RTC-oscillator side effect for timekeepers. Record an explicit safety verdict with the
escalation criterion.

Purpose: `configure_sram` is a near-no-op in firmware today (logs only). The audit ensures
operators do not mistake firmware silence for a guarantee — and confirms whether any genuine
hardware-damage path exists (research found none). This is a HOST-only, documentation-only
deliverable; the firmware sub-repo stays untouched UNLESS a real safety issue surfaces, in
which case it becomes a firmware backlog item (NOT a silent dismissal — D-04).
Output: two committed, lockstep documentation artifacts + a recorded safety verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-CONTEXT.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-RESEARCH.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the SRAM/NVRAM path (firmware + host-side dispatch) and author the planning-layer 59-SRAM-AUDIT.md</name>
  <files>.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md</files>
  <read_first>
    - firestarter/src/proms/sram.cpp (the audit subject — confirm configure_sram is a near-no-op: LOG_DEBUG_ID_SUB(DBG_CONFIGURING_SRAM) only; no pin config, no VPP assertion, no regulator enable)
    - firestarter/include/sram.h (read-only context — declares only `void configure_sram(firestarter_handle_t* handle)`; no WP#-control state)
    - firestarter_app/tools/check_dispatch.py (host-side BLOCKER-2 guard: _SRAM_PROTOCOLS = {0x0E,0x27,0x28,0x29} never route to configure_eprom — the primary host-side safety proof)
    - .planning/research/PITFALLS.md (§E-3 — NVRAM behavioral truths: never factory-blank, WP#, RTC oscillator; SR-1 checklist source)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-RESEARCH.md (§SRAM/NVRAM Behavioral Audit (GATE-04) — the full firmware + host-side findings, the three truths, and the safety verdict; Pitfall 5)
    - .planning/phases/58-pinout-re-derivation-24-pin-eeprom-unblock/58-SR-1-CHECKLIST.md (the planning-layer two-layer analog — structure model)
  </read_first>
  <action>
    Create the planning-layer audit trail at
    `.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md` (the
    investigation-canonical half of the D-04 two-layer doc; the shipped doc in Task 2 is the
    operator-facing subset). Follow the 59-PATTERNS.md 58-SR-1-CHECKLIST structure model:
    Scope -> Firmware Layer Audit -> Host-Side Dispatch Audit -> NVRAM Behavioral Truths
    (three sub-sections) -> Safety Verdict -> Shipped Doc pointer.

    Per 59-RESEARCH.md Pitfall 5, the audit MUST cover BOTH layers, not just the 2-line firmware
    file:
      - Firmware Layer Audit: state that `configure_sram` in `firestarter/src/proms/sram.cpp` is a
        near-no-op (emits `LOG_DEBUG_ID_SUB(DBG_CONFIGURING_SRAM)` and nothing else — no pin
        configuration, no VPP assertion, no regulator enable); the generic firestarter_handle_t
        read/write callbacks handle bus I/O. `sram.h` adds no WP#-control state. Quote the finding,
        do not inline a fenced code block.
      - Host-Side Dispatch Audit: state that protocols {0x0E, 0x27, 0x28, 0x29} all route to
        configure_sram, and the check_dispatch.py BLOCKER-2 guard confirms 0 SRAM-protocol chips
        route to configure_eprom across all 743 chips (configure_eprom would enable the VPP boost
        regulator on a 5V SRAM part). This is the primary host-side safety proof.

    Document the three required NVRAM behavioral truths (D-04 (a)/(b)/(c), sourced from PITFALLS.md
    §E-3 and 59-RESEARCH.md §SRAM/NVRAM Behavioral Audit):
      (a) Blank-check limitation: NVRAM/FRAM (DS1225/DS1230/DS1245/M48T02/M48T08/M48T35/FM1608/
          FM16W08/FM1808/BQ4010/11) retain data indefinitely (battery/ferroelectric) — NEVER
          factory-blank (all 0xFF). A write that runs blank-check first (FLAG_SKIP_BLANK_CHECK
          absent) will fail on a non-blank chip; operators must set FLAG_SKIP_BLANK_CHECK or accept
          that blank-check always fails for these parts (not a defect).
      (b) WP# pin behavior: DS1225 class — hardware WP# pin (pin 26) typically internally pulled low
          (writes enabled by default); M48T08 class — write-protect is bit 7 of the control register
          byte (e.g. address 0x1FF8 for M48T08), software-accessible, NOT a hardware pin, and the
          generic firmware SRAM write path does NOT clear it automatically. Mark the DS1225/M48T08
          datasheet behavioral claims as [ASSUMED] pending operator datasheet confirmation (risk LOW
          per 59-RESEARCH.md Assumptions A2 — worst case is a non-destructive write failure).
      (c) RTC-oscillator side effect: timekeeper NVRAMs (DS1225/M48T08/M48T35) contain a 32.768 kHz
          oscillator + RTC counter; whenever VCC is applied (including during RURP read/write) the
          oscillator runs and the clock advances — a state-change side effect, NOT a hardware-damage
          path. Operators who care about RTC accuracy must re-set the time after programming.

    Record the Safety Verdict explicitly: no genuine safety issue found — `configure_sram` as a
    near-no-op is functionally correct for the JEDEC SRAM byte-write use case; the WP# and RTC
    behaviors are operator considerations, not hardware-damage paths; therefore NO firmware backlog
    item is needed and the firmware sub-repo stays untouched (D-04 / D-05 host-only boundary). STATE
    the escalation criterion plainly: a firmware backlog item would be raised ONLY if a real safety
    issue had surfaced — it did not, and this is an audited verdict, not a silent dismissal. Cite
    PITFALLS.md §E-3 inline using the `[CITED: .planning/research/PITFALLS.md §E-3]` convention.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md && grep -qi "Safety Verdict" /workspaces/.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md && grep -qi "blank" /workspaces/.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md && grep -qi "RTC" /workspaces/.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `59-SRAM-AUDIT.md` exists and contains all six sections: Scope, Firmware Layer Audit, Host-Side Dispatch Audit, NVRAM Behavioral Truths (with three sub-sections), Safety Verdict, Shipped Doc pointer.
    - Firmware Layer Audit states `configure_sram` is a near-no-op (no VPP assertion / no regulator enable) and `sram.h` adds no WP#-control state.
    - Host-Side Dispatch Audit names the SRAM protocol set {0x0E,0x27,0x28,0x29} and states the BLOCKER-2 guard confirms 0 SRAM chips route to configure_eprom (`cd firestarter_app && python tools/check_dispatch.py` cited as the proof).
    - All three behavioral truths are present: `grep -qi "blank" && grep -qi "WP" && grep -qi "RTC"` all match; DS1225 and M48T08 are both named.
    - Safety Verdict states "no firmware escalation needed" AND states the escalation criterion ("only if a real safety issue had surfaced"); DS1225/M48T08 claims carry an [ASSUMED] marker.
  </acceptance_criteria>
  <done>The planning-layer GATE-04 audit trail exists, covers both firmware and host-side layers, documents all three NVRAM behavioral truths, and records an explicit no-escalation safety verdict with the escalation criterion stated.</done>
</task>

<task type="auto">
  <name>Task 2: Author the shipped operator-facing doc/sram-nvram-behavior.md as the lockstep subset of the audit</name>
  <files>firestarter_app/doc/sram-nvram-behavior.md</files>
  <read_first>
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md (the planning-layer source authored in Task 1 — the shipped doc is its operator-facing subset; the two must be in lockstep)
    - firestarter_app/doc/pinout-safety-review.md (THE analog — copy its header/front-matter pattern: one-sentence scope + explicit "Full audit trail:" pointer to the planning artifact; its section-per-guarantee structure: header -> prose -> bullet-facts)
    - firestarter_app/doc/infoic-field-dictionary.md (citation-format analog — lines 9-11 citation commit / permalink-base style)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-PATTERNS.md (sram-nvram-behavior.md section — header pattern, three-section structure, citation format; two-layer lockstep commit discipline)
    - firestarter_app/CLAUDE.md (§Constants last paragraph — two-layer doc lockstep convention)
  </read_first>
  <action>
    Create the shipped, GitHub-visible doc at `firestarter_app/doc/sram-nvram-behavior.md` as the
    operator-facing subset of `59-SRAM-AUDIT.md` (D-04 layer 2). Mirror the
    `firestarter_app/doc/pinout-safety-review.md` pattern exactly:
      - Title: `# SRAM / NVRAM Behavior — Phase 59`.
      - Front matter: a one-sentence scope line plus an explicit
        `**Full audit trail:** .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md (meta-repo, investigation-canonical).`
        pointer (this is the key_link the two-layer pattern requires).
      - Three operator-facing sections, one per behavioral truth, each using the analog's
        header -> prose -> bullet-facts structure:
          (a) "Blank-check limitation" — NVRAM/FRAM is never factory-blank; operators must set
              FLAG_SKIP_BLANK_CHECK or accept blank-check failure (not a defect). Name the affected
              families.
          (b) "Write-protect (WP#) behavior" — DS1225 class hardware WP# pin (writes enabled by
              default) vs M48T08 class software control-register bit 7 (firmware does not clear it
              automatically).
          (c) "RTC-oscillator side effect" — timekeepers run the clock whenever VCC is applied;
              re-set the time after programming; not a hardware-damage path.
      - A brief "Safety" closing note: the firmware `configure_sram` is a near-no-op and these are
        operator considerations, not hardware-damage paths — no firmware change in v1.11.

    Use the project citation convention: `[CITED: .planning/research/PITFALLS.md §E-3]` inline, and
    name the DS1225 / M48T08 datasheet families as the behavioral sources (carry the [ASSUMED] marker
    forward for the DS1225/M48T08 specifics, consistent with the audit). The shipped doc is the
    SUBSET — it omits the firmware-source quoting and the host-side dispatch-proof internals (those
    live in the planning layer) and focuses on what an operator must know before programming
    NVRAM/SRAM. Keep both files in lockstep: commit this doc together with the Task 1 audit (same
    plan wave); the shipped doc's "Full audit trail" pointer must resolve to the Task 1 file path.
    Do NOT touch the firmware sub-repo (host-only milestone; the shipped doc lives in
    `firestarter_app/doc/`, matching the Phase 58 precedent).
  </action>
  <verify>
    <automated>test -f /workspaces/firestarter_app/doc/sram-nvram-behavior.md && grep -q "59-SRAM-AUDIT" /workspaces/firestarter_app/doc/sram-nvram-behavior.md && grep -qi "blank" /workspaces/firestarter_app/doc/sram-nvram-behavior.md && grep -qi "WP" /workspaces/firestarter_app/doc/sram-nvram-behavior.md && grep -qi "RTC" /workspaces/firestarter_app/doc/sram-nvram-behavior.md</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/doc/sram-nvram-behavior.md` exists with title `# SRAM / NVRAM Behavior — Phase 59`.
    - It contains a `Full audit trail:` pointer that resolves to `.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-SRAM-AUDIT.md` (`grep -q "59-SRAM-AUDIT"` matches).
    - All three behavioral-truth sections are present: `grep -qi "blank" && grep -qi "WP" && grep -qi "RTC"` all match; DS1225 and M48T08 are both named.
    - It contains a Safety closing note stating no firmware change in v1.11 (configure_sram near-no-op; operator considerations only).
    - The two layers are consistent: every truth in the shipped doc is present in (a subset of) `59-SRAM-AUDIT.md`; no truth in the shipped doc is absent from the audit.
  </acceptance_criteria>
  <done>The shipped operator-facing doc exists, is the lockstep subset of the planning audit, covers all three NVRAM truths, points back to the audit trail, and confirms no firmware change — GATE-04 is green across both layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware configure_sram → socket bus | The SRAM handler is a near-no-op; bus I/O is via generic firestarter_handle_t callbacks. The audit's job is to confirm no VPP/regulator path exists here. |
| host dispatch (algorithm) → firmware handler | The SRAM protocol set {0x0E,0x27,0x28,0x29} must route to configure_sram, never configure_eprom (which would enable the 12V VPP boost regulator on a 5V SRAM part). |

This is a documentation-only plan (GATE-04). No code is changed; no network, auth, or
user-input surfaces exist. The genuine safety surface is whether the SRAM/NVRAM path has a
hidden VPP-on-5V-part hazard — the audit verifies it does not. If a real hazard had surfaced
it would become a firmware backlog item (D-04), but research and this audit found none.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | Tampering | SRAM dispatch path (VPP-on-5V-part) | accept | check_dispatch.py BLOCKER-2 (proven in Plan 01 SC#2) already guarantees 0 SRAM chips route to configure_eprom; configure_sram asserts no VPP. The audit documents this; no new mitigation is introduced by a doc-only plan. Rationale recorded in 59-SRAM-AUDIT.md Safety Verdict. |
| T-59-06 | Repudiation | undocumented NVRAM behavior (operator misunderstanding) | mitigate | The two-layer doc makes the blank-check / WP# / RTC truths explicit so operators do not mistake firmware silence for a guarantee — the documented-behavioral-truth mitigation, not a code change. |
| T-59-07 | Tampering (escalation discipline) | a real SRAM safety issue being silently dismissed | mitigate | The audit states the escalation criterion and records the no-escalation verdict explicitly (D-04) — a surfaced hazard becomes a firmware backlog item, never a silent dismissal. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No packages introduced (documentation-only plan). No supply-chain surface. |
</threat_model>

<verification>
- `59-SRAM-AUDIT.md` exists with all six sections and all three behavioral truths; Safety Verdict records no-escalation + the escalation criterion.
- `firestarter_app/doc/sram-nvram-behavior.md` exists, points back to the audit trail, and covers all three truths.
- The two layers are in lockstep (every shipped-doc truth is a subset of the audit).
- The firmware sub-repo is untouched (host-only milestone; no `firestarter/` changes).
</verification>

<success_criteria>
- GATE-04 green: configure_sram NVRAM/SRAM behavior is audited and documented in two lockstep layers (planning audit + shipped doc) covering the blank-check limitation, WP# behavior (DS1225/M48T08 class), and RTC-oscillator side effect.
- An explicit safety verdict is recorded: no genuine safety issue found, no firmware escalation needed — with the escalation criterion stated (not a silent dismissal).
- Firmware sub-repo untouched (host-only).
</success_criteria>

<output>
Create `.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-02-SUMMARY.md` when done.
Record: the firmware near-no-op finding, the host-side BLOCKER-2 proof reference, the three documented truths, the safety verdict, and confirmation both layers are committed in lockstep.
</output>
