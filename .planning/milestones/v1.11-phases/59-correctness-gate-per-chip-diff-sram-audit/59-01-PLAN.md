---
phase: 59-correctness-gate-per-chip-diff-sram-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/build_db.py
  - firestarter_app/tools/diff_db.py
  - firestarter_app/firestarter/data/chip_database.json
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "A re-runnable diff script accounts for every changed and new chip between the pinned baseline and the regenerated DB"
    - "diff_db.py exits 0 when every changed chip maps to a cited root-cause rule"
    - "diff_db.py exits 1 and names the offending chip(s) on any unexplained diff (D-03 BLOCK)"
    - "check_dispatch.py exits 0 across the full 743-chip set including DIP24_2816 (SC#2)"
    - "Two consecutive build_db.py runs produce byte-identical chip_database.json (SC#4)"
  artifacts:
    - path: "firestarter_app/tools/diff_db.py"
      provides: "GATE-02 re-runnable grouped-by-cause full-record diff"
      exports: ["main", "_classify_diff", "_make_index"]
      min_lines: 90
    - path: "firestarter_app/tools/build_db.py"
      provides: "sort_keys=True determinism hardening on the json.dump call (line 517)"
      contains: "sort_keys=True"
    - path: "firestarter_app/firestarter/data/chip_database.json"
      provides: "Regenerated DB (743 chips) under review"
  key_links:
    - from: "firestarter_app/tools/diff_db.py"
      to: "firestarter_app/tools/baseline/chip_database.baseline.json"
      via: "BASELINE_FILE env-overridable constant"
      pattern: "BASELINE_FILE"
    - from: "firestarter_app/tools/diff_db.py"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "DB_FILE env-overridable constant"
      pattern: "DB_FILE"
    - from: "firestarter_app/tools/diff_db.py::_classify_diff"
      to: "the five root-cause categories (BUG2_TIMING, BUG3_VCC_VDD, RULE_ALGO, BUG2_AND_BUG3, SRAM_PINOUT)"
      via: "field-delta classification"
      pattern: "BUG2_AND_BUG3"
---

<objective>
Deliver the GATE-02 correctness gate for the v1.11 milestone: a committed, re-runnable
`diff_db.py` script that compares the regenerated `chip_database.json` (743 chips) against
the immutable pre-milestone baseline (`tools/baseline/chip_database.baseline.json`, 734
chips, commit `f92873d`), explains every changed chip by a cited root-cause rule (grouped
by cause per D-01, full-record per D-02), and BLOCKS (exits 1) on any unexplained diff (D-03).
Also harden pipeline determinism (`sort_keys=True` per SC#4) and re-confirm the GATE-03
dispatch-safety guard across the full regenerated set (SC#2).

Purpose: This is the closing proof that the Phase 57 decode-bug fixes and the Phase 58
pinout re-derivation moved exactly the chips they should have moved — no silent collateral
damage. The dominant hazard is a wrong field decode routing VPP to the wrong pin (dead 5V
chip); GATE-02's BLOCK-on-unexplained-diff disposition is the direct mitigation.
Output: `firestarter_app/tools/diff_db.py` (new), one-line `build_db.py` hardening,
regenerated `chip_database.json`, and recorded SC#2/SC#4 proof in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-CONTEXT.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-RESEARCH.md
@.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden build_db.py determinism (sort_keys=True), regenerate DB, prove SC#4 byte-identity + SC#2 dispatch safety</name>
  <files>firestarter_app/tools/build_db.py, firestarter_app/firestarter/data/chip_database.json</files>
  <read_first>
    - firestarter_app/tools/build_db.py (target: json.dump call at line 517; MINIPRO_XML_URL live fetch at line 11; citation SHA a8efaedc at line 26)
    - firestarter_app/tools/check_dispatch.py (the SC#2 gate — already keyed on electrical.type, dynamic pinouts.json load; re-run only)
    - firestarter_app/firestarter/data/chip_database.json (the regenerated DB, 743 chips — the determinism + dispatch subject)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-RESEARCH.md (§SC#4 Determinism — Technical Analysis; §GATE-03 Re-confirmation; Pitfall 3)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-PATTERNS.md (build_db.py one-line edit section)
  </read_first>
  <action>
    Edit the single `json.dump(complete_db, f, indent=2)` call at `firestarter_app/tools/build_db.py`
    line 517 to add the keyword argument `sort_keys=True` — making the output key-order stable
    regardless of Python dict insertion order (SC#4 determinism hardening per 59-RESEARCH.md §SC#4
    and 59-PATTERNS.md). This is the ONLY change to build_db.py; do NOT alter the live
    `MINIPRO_XML_URL` fetch (the pinned anchor is the committed baseline JSON output, not a local XML
    — Phase 56 D-01 keeps the live fetch). No import or formatting changes are required for a
    keyword-argument addition; the edit must remain ruff-clean.

    CRITICAL ORDERING (59-RESEARCH.md Pitfall 3): make the `sort_keys=True` edit FIRST, then
    regenerate the DB ONCE so the committed `chip_database.json` reflects the new sorted key order,
    THEN run the two-consecutive-run byte-compare. Do NOT run Run 1 before the edit and Run 2 after —
    any build_db.py change between runs makes them non-comparable.

    Run the SC#4 two-run byte-compare harness (manual shell procedure, NOT a committed script per the
    CONTEXT.md deferred note on CI wiring): `cd firestarter_app`; `python3 tools/build_db.py`;
    `cp firestarter/data/chip_database.json /tmp/chip_database_run1.json`; `python3 tools/build_db.py`;
    `cp firestarter/data/chip_database.json /tmp/chip_database_run2.json`;
    `diff /tmp/chip_database_run1.json /tmp/chip_database_run2.json` (expected: empty output =
    byte-identical). Record the empty-diff result in the plan summary as SC#4 proof. Two back-to-back
    runs fetch the same upstream `infoic.xml` state, satisfying the Phase 56 D-04 "deterministic given
    a stable upstream master" form of SC#4.

    Re-confirm SC#2 (GATE-03 re-run, no code change to check_dispatch.py): `cd firestarter_app &&
    python tools/check_dispatch.py` — expected exit 0 with the PASS line reporting all 743 chips
    dispatch validly, 0 SRAM chips and 0 Flash/EEPROM chips route to configure_eprom, 0 wire-key
    regressions. The guard already auto-covers DIP24_2816 because it keys on `electrical.type`
    (pinout-agnostic, Phase 57 CR-01). After regeneration, leave the regenerated
    `chip_database.json` committed as the gate subject for Task 2's diff.

    NOTE: regenerating with `sort_keys=True` re-orders keys vs the pre-edit DB; this is expected and
    is itself a (uniform, explainable) change that Task 2's full-record diff will surface — it does NOT
    constitute an unexplained chip-field diff because it is a serialization key-order change, not a
    field-value change. If the diff script's per-chip field comparison (Task 2) is field-keyed rather
    than line-keyed, key re-ordering does not register as a chip diff.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -q "sort_keys=True" tools/build_db.py && python tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sort_keys=True" firestarter_app/tools/build_db.py` reports exactly 1 (filtered: the edit landed on the json.dump call, not a comment).
    - `cd firestarter_app && python tools/check_dispatch.py` exits 0 and prints the PASS line covering all 743 chips (SC#2 re-confirmed; DIP24_2816 covered).
    - Two consecutive `cd firestarter_app && python3 tools/build_db.py` runs produce byte-identical `chip_database.json`: `diff /tmp/chip_database_run1.json /tmp/chip_database_run2.json` reports no difference (SC#4); the empty-diff result is recorded in the plan summary.
    - `cd firestarter_app && ruff check tools/build_db.py && ruff format --check tools/build_db.py` exit 0 (no style regression from the edit).
    - build_db.py MINIPRO_XML_URL live fetch is unchanged (no local-XML vendoring introduced).
  </acceptance_criteria>
  <done>build_db.py emits sort-stable JSON; the regenerated 743-chip DB is committed; SC#4 byte-identity is demonstrated and recorded; SC#2 (check_dispatch.py 0 errors) is re-confirmed across the full set.</done>
</task>

<task type="auto">
  <name>Task 2: Author diff_db.py — re-runnable grouped-by-cause full-record per-chip diff with D-03 BLOCK exit contract</name>
  <files>firestarter_app/tools/diff_db.py</files>
  <read_first>
    - firestarter_app/tools/check_dispatch.py (THE analog — copy its imports block, module-top env-overridable path constants, encoding="utf-8" JSON opens, per-manufacturer iteration, exit-code contract with truncated [:20] error lists, and `if __name__ == "__main__": main()` guard)
    - firestarter_app/tools/baseline/chip_database.baseline.json (IMMUTABLE diff anchor, 734 chips, commit f92873d — read its structure: outer dict mfg_name -> list of chip records; each chip has part_number, programming.{algorithm,pulse_duration}, electrical.{vcc,vdd,type}, pinout)
    - firestarter_app/firestarter/data/chip_database.json (regenerated DB, 743 chips, from Task 1)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-RESEARCH.md (§Diff Blast Radius — Characterized Root Causes: 5 causes + new chips; Pattern 1 classifier; Pitfalls 1/2/4; §Citation Convention a8efaedc)
    - .planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-PATTERNS.md (diff_db.py section — exact imports, path constants, _classify_diff body, exit-code mirror)
  </read_first>
  <action>
    Create `firestarter_app/tools/diff_db.py` as a standalone stdlib-only Python script (imports:
    `json`, `os`, `sys` — NO `EpromDatabase` import; the diff operates on raw JSON dicts). Mirror
    `check_dispatch.py` structure exactly: module docstring stating the exit-code contract (0 = all
    changed chips explained by a cited root-cause rule; 1 = at least one unexplained diff, the D-03
    BLOCK signal naming the offending chip[s]); module-top env-overridable path constants
    `DB_FILE` (env `FIRESTARTER_DB_FILE`, default the regenerated DB) and `BASELINE_FILE`
    (env `FIRESTARTER_BASELINE_FILE`, default `tools/baseline/chip_database.baseline.json`);
    `encoding="utf-8"` on both JSON opens; a `main()` entry; the `if __name__ == "__main__": main()`
    guard verbatim.

    Build a flat `part_number -> (mfg, chip_record)` index for BOTH files via `_make_index` (key on
    the EXACT part_number string — do NOT parse/split the comma-separated alias list; 59-RESEARCH.md
    Pitfall 1/4 — both files came from the same build_db.py normalization, so exact-string keying is
    correct; manufacturer name is metadata, never the diff key).

    Implement `_classify_diff(bl_chip, cu_chip)` returning a root-cause label or `None`, per the
    59-PATTERNS.md / 59-RESEARCH.md Pattern 1 algorithm. The five categories and their EXACT ordering
    (combined case MUST be tested BEFORE the single-bug buckets — 59-RESEARCH.md Pitfall 2):
      1. `RULE_ALGO` — `programming.algorithm` changed (Phase 58 Rule 1/2/3 algorithm corrections:
         Rule 1 variant_lo=0x10 -> DIP24_2816 + 0x0D; Rule 2 WARNING-5 0x07->0x0D; Rule 3 fm1608 flip).
         Citation: minipro database.c @ a8efaedc + WARNING-5 in .planning/v1.0-MILESTONE-AUDIT.md.
      2. `BUG2_AND_BUG3` — `programming.pulse_duration` changed AND (`electrical.vcc` or
         `electrical.vdd` changed): the 188-chip combined case (BUG-2 timing ×100 removal + BUG-3
         vcc/vdd label swap applied to the same record). MUST be matched before BUG2_TIMING/BUG3_VCC_VDD.
      3. `BUG2_TIMING` — pulse_duration changed, no voltage change, no pinout change: BUG-2 ×100
         multiplier removal for proto 0x07/0x0B. Citation: minipro database.c#L866 @ a8efaedc
         (pulse_delay is µs for all protocols, no multiplier).
      4. `BUG3_VCC_VDD` — voltage (vcc/vdd) changed, no timing change, no algo change: BUG-3 label
         swap (vcc=bits 11-8, vdd=bits 15-12). Citation: minipro database.c#L921-L923 @ a8efaedc.
      5. `SRAM_PINOUT` — pinout changed, no algo change, no timing change: Phase 58 principled
         resolve_pinout_key re-routed pm_idx=0 28-pin SRAM chips to the correct JEDEC SRAM layout.
      Fallthrough -> `None` = UNEXPLAINED = D-03 BLOCK.

    Coverage is FULL-RECORD per D-02: the classifier keys its decision on the five priority SC fields
    (algorithm, pinout, vpp_mv, pulse_duration, electrical.type) PLUS vcc/vdd, but the diff must also
    detect ANY other field delta (flags, chip_id, mem_size, etc.). When a chip's records differ but
    the classifier returns a known label, the chip is "explained"; if a chip differs ONLY in a field
    no rule accounts for (i.e. `bl_chip != cu_chip` is True but `_classify_diff` returns None), it is
    unexplained and triggers BLOCK. Compute the per-chip difference using Python dict `==` deep-compare
    on the full record (don't hand-roll recursion — 59-RESEARCH.md §Don't Hand-Roll); only run
    `_classify_diff` on records where `bl_chip != cu_chip`.

    Partition into three buckets and report grouped-by-cause (D-01): (a) CHANGED chips present in both
    files whose records differ — grouped under their root-cause label with the embedded minipro
    citation string, with a per-group count; (b) NEW chips present in current but not baseline —
    reported separately as "NEW — Rule 1 unblock via DIP24_2816 + algo=0x0D" (expected 9: AT28C04/16
    family); (c) MISSING chips present in baseline but not current — these would indicate a regression
    and MUST be reported (expected 0). Embed the `[VERIFIED: minipro <file>#L<lineno> @ a8efaedc]`
    citation convention (Phase 56 D-05/D-06; permalink base
    https://gitlab.com/DavidGriffith/minipro/-/blob/a8efaedc/src/database.c) in each group's rationale
    string so the output is self-documenting when re-run.

    Exit-code contract (mirror check_dispatch.py): collect an `unexplained` list (changed chips whose
    classifier returned None) AND treat any MISSING chip as a failure; if either is non-empty, print
    `FAIL: N chips with unexplained diffs:` followed by up to 20 part numbers (`... and N more` if
    truncated) and `sys.exit(1)` — this is the D-03 BLOCK (an unexplained diff is a build_db.py logic
    bug to fix-and-re-regen, NOT document-and-accept). Otherwise print
    `PASS: all <changed> changed chips explained (<new> new chips confirmed; <missing> removed)` and
    fall through (implicit exit 0). Per the discretion in 59-CONTEXT.md, exact prose/grouping format
    is the author's call as long as it is re-runnable, grouped-by-cause, and full-record.

    The script must be ruff-clean (`ruff check` + `ruff format --check`). It is in `tools/`, outside
    the 8-module mypy-strict set, so mypy-strict does not apply — but keep it type-clean and stdlib-only.
    Do NOT inline any fenced code into this plan's action; the implementation lives in the file.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check tools/diff_db.py && ruff format --check tools/diff_db.py && python tools/diff_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tools/diff_db.py` exists, imports only `json`/`os`/`sys`, has the module docstring stating the exit-code contract, and ends with `if __name__ == "__main__": main()`.
    - `cd firestarter_app && python tools/diff_db.py` exits 0 and prints a grouped-by-cause report; every changed chip is accounted for under one of the five root-cause labels with an embedded `a8efaedc` minipro citation; the 9 new AT28C04/16-family chips are reported separately as NEW; 0 chips reported MISSING.
    - The script BLOCKS correctly on an unexplained diff: `FIRESTARTER_BASELINE_FILE=firestarter/data/chip_database.json python tools/diff_db.py` (baseline == current) exits 0 with 0 changed chips; and a deliberately-mutated-field probe (e.g. via an injected test fixture or a one-off env-pointed file with a chip whose `flags` changed but no rule covers it) causes exit 1 naming that chip. Demonstrate the exit-1 path once and record it in the summary.
    - `cd firestarter_app && ruff check tools/diff_db.py && ruff format --check tools/diff_db.py` exit 0.
    - The classifier matches `BUG2_AND_BUG3` BEFORE `BUG2_TIMING`/`BUG3_VCC_VDD` (verified by 0 chips landing in `unexplained` despite ~188 combined-cause chips existing).
  </acceptance_criteria>
  <done>diff_db.py is a committed, re-runnable, grouped-by-cause, full-record diff that exits 0 with every changed chip explained by a cited rule and exits 1 (naming the chip) on any unexplained diff — GATE-02 is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream infoic.xml → build_db.py | Live-fetched XML (trusted upstream GitLab project, pinned by recorded SHA a8efaedc citation, in use since Phase 56). The only externally-sourced input. |
| baseline.json + current.json → diff_db.py | Two trusted local JSON files committed in-repo; no untrusted input. |

This is a host-only data-pipeline + regression-tooling plan. There are no network endpoints,
no auth paths, no user-input processing surfaces. ASVS V2/V3/V4 do not apply. The genuine
safety surface is decode-correctness, not a web threat: a wrong field decode can route VPP to
the wrong pin and kill a 5V chip (see .planning/research/PITFALLS.md). The mitigations are the
GATE itself, listed below.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | diff_db.py classifier | mitigate | D-03 BLOCK: classifier returns None for any field delta no cited rule covers → exit 1 naming the chip → forces a build_db.py fix + re-regen + re-diff. No document-and-accept escape hatch lets a wrong decode slip through. |
| T-59-02 | Tampering | regenerated chip_database.json (dispatch safety) | mitigate | SC#2 re-run of check_dispatch.py asserts 0 Flash/EEPROM (5V) chips route to configure_eprom (12V VPP-on-5V-part hazard) across all 743 chips incl. DIP24_2816 — the wrong-decode→dead-chip hazard is caught here. |
| T-59-03 | Repudiation/Integrity | build_db.py pipeline determinism | mitigate | SC#4 two-run byte-compare + sort_keys=True hardening ensures a non-deterministic regen cannot silently mask a regression between runs; the diff anchor (baseline.json) is immutable. |
| T-59-04 | Information Disclosure | diff_db.py / build_db.py | accept | Pure local data-pipeline tooling; no secrets, no PII, no network egress beyond the long-standing pinned upstream XML fetch. No mitigation required. |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No new external packages introduced (stdlib only — 59-RESEARCH.md §Package Legitimacy Audit: N/A). No supply-chain surface added by this plan. |
</threat_model>

<verification>
- `cd firestarter_app && python tools/diff_db.py` exits 0 — all changed chips explained, 9 new confirmed, 0 missing (GATE-02).
- `cd firestarter_app && python tools/check_dispatch.py` exits 0 across the full 743-chip set incl. DIP24_2816 (SC#2).
- Two consecutive `cd firestarter_app && python3 tools/build_db.py` runs are byte-identical (SC#4).
- `cd firestarter_app && ruff check tools/ && ruff format --check tools/` exit 0.
- `cd firestarter_app && pytest --cov-fail-under=70` passes (no regression in the existing suite).
</verification>

<success_criteria>
- GATE-02 green: diff_db.py is committed, re-runnable, grouped-by-cause, full-record; exits 0 with every changed chip explained by a cited root-cause rule; exits 1 (naming the chip) on any unexplained diff.
- SC#2 green: check_dispatch.py exits 0 across the full regenerated 743-chip set including the new 24-pin EEPROMs.
- SC#4 green: regenerating from the live-fetched (stable-upstream) infoic.xml yields byte-identical chip_database.json across two consecutive runs; sort_keys=True hardening landed.
- No unexplained diffs remain; the D-03 BLOCK path is demonstrated.
</success_criteria>

<output>
Create `.planning/phases/59-correctness-gate-per-chip-diff-sram-audit/59-01-SUMMARY.md` when done.
Record: the grouped-by-cause diff output (per-cause counts), the SC#4 empty-diff proof, the SC#2 PASS line, and the demonstrated exit-1 (D-03 BLOCK) probe result.
</output>
