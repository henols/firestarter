---
phase: 56-snapshot-field-dictionary-corrected-docs
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - firestarter_app/doc/infoic-field-dictionary.md
autonomous: true
requirements: [DEC-01, DEC-03, DEC-04, DEC-05]
must_haves:
  truths:
    - "An authoritative field dictionary exists documenting all 13 in-scope infoic.xml attributes, each marked CONFIRMED / INFERRED / UNKNOWN"
    - "The dictionary records ONE minipro citation SHA (a8efaedc) at the top; all permalinks share it"
    - "The dictionary states CORRECT decode semantics for BUG-1..4, each annotated 'correct is X; current build_db.py does Y; fix deferred to Phase 57'"
    - "flags bits 3/6/7 are explicitly marked UNKNOWN (no MP_* constant), not INFERRED"
  artifacts:
    - path: "firestarter_app/doc/infoic-field-dictionary.md"
      provides: "Canonical source-cited authority Phase 57 codes against (DEC-01)"
      min_lines: 120
      contains: "a8efaedc236c1d9718bd28299dfbb99536b010ff"
  key_links:
    - from: "firestarter_app/doc/infoic-field-dictionary.md"
      to: "minipro database.c/database.h @ a8efaedc"
      via: "GitLab commit-permalink citations"
      pattern: "gitlab.com/DavidGriffith/minipro/-/blob/a8efaedc"
---

<objective>
Author the authoritative field dictionary `firestarter_app/doc/infoic-field-dictionary.md` (NEW file, D-07): the single source of truth documenting every Firestarter-relevant `infoic.xml` attribute, source-cited against minipro source pinned to ONE recorded SHA. This is the authority Phase 57's bug-fix code cites and the three doc rewrites (Plan 03) derive from (DEC-01, D-08).

The dictionary documents CORRECT decode semantics. It does NOT change `build_db.py` — every bug entry must say "correct decode is X; current build_db.py does Y; fix deferred to Phase 57" so no executor mistakes this for a code-change task (RESEARCH Pitfall 1).

Purpose: Anchor all downstream v1.11 decode work in one source-grounded reference.
Output: `firestarter_app/doc/infoic-field-dictionary.md` covering 13 attributes with CONFIRMED/INFERRED/UNKNOWN markers, single citation SHA, and the four confirmed bug semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-CONTEXT.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write infoic-field-dictionary.md with all 13 attributes, citation SHA, and bug semantics</name>
  <files>firestarter_app/doc/infoic-field-dictionary.md</files>
  <read_first>
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md (§"Field Dictionary — Authoritative Attribute Reference" gives the per-attribute content verbatim; §"D-11 Open Items — FULLY RESOLVED" gives BUG-1..4 with source line numbers; §Metadata gives the citation SHA and permalink pattern)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md (§"firestarter_app/doc/infoic-field-dictionary.md" — top-of-file citation block, per-attribute H3 section structure, 13-attribute list, CONFIRMED/INFERRED/UNKNOWN rule, citation permalink format; §"Logo-Header Block" — verbatim line-1 HTML)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-CONTEXT.md (D-05/D-06/D-07/D-11 — citation rules, dictionary home, correct-semantics requirement)
    - firestarter_app/doc/protocol-flags.md (analog doc-family: logo-header at line 1, `---` separator at line 3, H2 title, markdown table layout to mirror)
    - firestarter_app/CLAUDE.md (WARNING-5 override: bit 4 = MP_ERASE_MASK / flags & 0x10 is the discriminator; the dictionary's flags entry must stay consistent with it)
  </read_first>
  <action>
    Create `firestarter_app/doc/infoic-field-dictionary.md` (D-07). Structure:

    1. Line 1: the verbatim logo-header `<p align="left"><img ... firestarter_logo.png ... width="200"></p>` block (copy exactly from 56-PATTERNS §Logo-Header Block); blank line 2; `---` on line 3 (D-09).
    2. H2 title (e.g. `## infoic.xml Field Dictionary`) and a one-line intro stating this is the canonical source-cited authority Phase 57 codes against.
    3. Top-of-file citation block (D-06): record the SINGLE minipro citation SHA `a8efaedc236c1d9718bd28299dfbb99536b010ff` (2026-03-23) and the permalink base `https://gitlab.com/DavidGriffith/minipro/-/blob/a8efaedc236c1d9718bd28299dfbb99536b010ff/src/`. All per-attribute citations reuse this one SHA (single place to bump). No minipro source is vendored (D-05).
    4. One H3 section per attribute for all 13 in-scope attributes — `package_details`, `type`, `variant`, `protocol_id`, `flags`, `voltages`, `pin_map`, `pulse_delay`, `chip_id`, `code_memory_size`, `page_size`, `chip_info`, `blank_value` — each headed `### \`name\` (type) — CONFIRMED | INFERRED | UNKNOWN`, with: source permalink (file#Lline @ a8efaedc), bit-layout/value table, build_db.py usage note, and a BUG note where applicable. Use the verbatim content from 56-RESEARCH §"Field Dictionary".

    Land these exact corrected semantics (D-11), each phrased "correct decode is X; current build_db.py does Y; fix deferred to Phase 57":
    - `pulse_delay` (BUG-2 / DEC-03): raw value is microseconds for ALL protocols (loaded raw via get_attr_value, database.c L866); the interpret_timing ×100 multiplier for 0x07/0x0B in build_db.py is wrong. Mark CONFIRMED.
    - `voltages` VCC/VDD nibble table (BUG-1 / DEC-04): complete table 0x00=5V, 0x01=3.3V, 0x02=4V, 0x03=4.5V, 0x04=5.5V, 0x05=6.5V from tl866ii_vcc_voltages[] (database.c L130-135); build_db.py VCC_VOLTAGES is MISSING 0x02 (4V) and 0x03 (4.5V). Mark CONFIRMED.
    - `voltages` field positions (BUG-3 / DEC-04): vdd = bits 15-12 (>>12), vcc = bits 11-8 (>>8), vpp = bits 7-0 (database.c L921-923); build_db.py L510-511 has vdd/vcc swapped. Mark CONFIRMED.
    - `protocol_id` (BUG-4 / DEC-05): canonical IC2_ALG_* names from database.h L24-77; record the full table and exclusion rationale — 0x2A=IC2_ALG_GAL16 (GAL PLD), 0x2C=IC2_ALG_GAL22 (GAL PLD), 0x2E=IC2_ALG_PIC32X_2 (PIC MCU), 0x35=IC2_ALG_ITE (ITE EC MCU, TQFP128), 0x39=phantom (no IC2_ALG constant, INFOIC2PLUS-unreachable), 0x3C=invented (not in minipro source). build_db.py PROTOCOL_MAP labels for these are wrong/phantom/invented. Mark CONFIRMED.

    For `flags`: mark source-confirmed bits (1, 4, 5, 12, 13, 14, 15, 18, 19, 20-21) CONFIRMED with their MP_* constant; mark bits 3, 6, 7 explicitly UNKNOWN with the note "not a defined MP_* constant in database.c lines 39-50" (RESEARCH Pitfall 2 — UNKNOWN, never INFERRED). State that bit 4 = MP_ERASE_MASK (0x10) "can be electrically erased" and is the WARNING-5 discriminator (consistent with CLAUDE.md, RESEARCH Pitfall 3 — do NOT copy the swapped build_db semantics into the dictionary).

    Do NOT edit `build_db.py` or `chip_database.json` (RESEARCH Pitfall 1). Do NOT paste full source-file bodies — cite via permalink.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && test -f doc/infoic-field-dictionary.md && grep -q "a8efaedc236c1d9718bd28299dfbb99536b010ff" doc/infoic-field-dictionary.md && N=$(grep -c -E '^### `(package_details|type|variant|protocol_id|flags|voltages|pin_map|pulse_delay|chip_id|code_memory_size|page_size|chip_info|blank_value)`' doc/infoic-field-dictionary.md) && [ "$N" -eq 13 ] && grep -qE "MP_ERASE_MASK" doc/infoic-field-dictionary.md && grep -qE "UNKNOWN" doc/infoic-field-dictionary.md && grep -qE "IC2_ALG_GAL16" doc/infoic-field-dictionary.md && grep -qE "4V|0x02" doc/infoic-field-dictionary.md && echo DICT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f firestarter_app/doc/infoic-field-dictionary.md` succeeds
    - The citation SHA `a8efaedc236c1d9718bd28299dfbb99536b010ff` appears at the top of the file
    - Exactly 13 H3 attribute headings present (one per in-scope attribute, matched by the grep -c -E above returning 13)
    - `grep -qE "MP_ERASE_MASK" doc/infoic-field-dictionary.md` succeeds (bit 4 = can_erase documented)
    - `grep -qE "UNKNOWN" doc/infoic-field-dictionary.md` succeeds (bits 3/6/7 marked UNKNOWN)
    - `grep -qE "IC2_ALG_GAL16" doc/infoic-field-dictionary.md` succeeds (canonical protocol names + exclusion present — BUG-4)
    - Dictionary contains the VCC nibbles 0x02=4V and 0x03=4.5V (BUG-1) and states vdd=>>12 / vcc=>>8 (BUG-3) and pulse_delay = raw µs no multiplier (BUG-2)
    - Each bug entry phrased "correct is X; current build_db.py does Y; fix deferred to Phase 57"
    - `git -C firestarter_app diff --name-only build_db.py firestarter/data/chip_database.json 2>/dev/null` shows neither changed (no decode-behavior change — RESEARCH Pitfall 1)
    - Line 1 is the verbatim logo-header block; line 3 is `---`
  </acceptance_criteria>
  <done>`firestarter_app/doc/infoic-field-dictionary.md` exists with all 13 attributes marked CONFIRMED/INFERRED/UNKNOWN, single citation SHA at top, BUG-1..4 correct semantics with Phase-57-deferral wording, bits 3/6/7 UNKNOWN, and build_db.py/chip_database.json unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| minipro GitLab source → dictionary citations | External source referenced (not vendored); provenance pinned by immutable commit SHA |
| dictionary → Phase 57 code | Dictionary is the trusted decode authority; correctness/citation integrity is the risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05 | Tampering (supply chain) | citation provenance | mitigate | All permalinks pinned to ONE immutable SHA a8efaedc (D-06); permalink resolves to a frozen commit, not mutable master — recorded once at top of file |
| T-56-06 | Repudiation | incorrect decode semantics in dictionary | mitigate | Every attribute carries a source line citation; BUG entries cross-checked against 56-RESEARCH §D-11 (manual-review per 56-VALIDATION) |
| T-56-07 | Information Disclosure | dictionary content | accept | Markdown reference doc; public minipro semantics; no secrets/PII |
| T-56-08 | Elevation of Privilege | n/a (no executable code) | accept | Plan adds only a markdown doc; no code path, no input handling |
| T-56-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan; N/A |
</threat_model>

<verification>
- Dictionary file exists with single citation SHA at top
- All 13 in-scope attributes present, each marked CONFIRMED/INFERRED/UNKNOWN
- BUG-1..4 correct semantics documented with Phase-57-deferral wording
- bits 3/6/7 marked UNKNOWN; bit 4 = MP_ERASE_MASK
- build_db.py and chip_database.json unchanged
- Manual review: spot-check 2-3 permalinks resolve to cited symbols (56-VALIDATION manual-only)
</verification>

<success_criteria>
- DEC-01: authoritative source-cited dictionary covering all 13 attributes with confidence markers
- DEC-03: pulse_delay = µs (no multiplier) documented, code fix deferred to Phase 57
- DEC-04: VCC nibbles complete (0x02/0x03 added) + vdd/vcc positions correct, code fix deferred to Phase 57
- DEC-05: canonical IC2_ALG_* names + exclusion rationale, code fix deferred to Phase 57
</success_criteria>

<output>
Create `.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-02-SUMMARY.md` when done.
</output>
