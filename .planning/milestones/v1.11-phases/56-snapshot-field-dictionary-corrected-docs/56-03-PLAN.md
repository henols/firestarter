---
phase: 56-snapshot-field-dictionary-corrected-docs
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - firestarter_app/doc/protocol-id.md
  - firestarter_app/doc/protocol-flags.md
  - firestarter_app/doc/package-details.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "protocol-id.md shows canonical IC2_ALG_* names; the 0x39 phantom error is fixed; infeasible/non-memory IDs carry explicit exclusion rationales"
    - "protocol-flags.md carries corrected protocol names and the bit-4 fix (bit 4 = MP_ERASE_MASK / can_erase, NOT 'requires write-enable sequence')"
    - "package-details.md is re-titled to describe flags; bit meanings are source-grounded; inferred bits 3/6/7 are explicitly marked NOT source-confirmed (UNKNOWN)"
    - "All three docs preserve the verbatim logo-header at line 1 and are derived from the field dictionary"
    - "The existing pytest suite stays green at --cov-fail-under=70 (docs/json-only changes do not break CI)"
  artifacts:
    - path: "firestarter_app/doc/protocol-id.md"
      provides: "Corrected protocol-id reference (DOC-03)"
      contains: "IC2_ALG"
    - path: "firestarter_app/doc/protocol-flags.md"
      provides: "Corrected protocol flags reference (DOC-02)"
      contains: "MP_ERASE_MASK"
    - path: "firestarter_app/doc/package-details.md"
      provides: "Corrected package_details/flags reference (DOC-01)"
      contains: "UNKNOWN"
  key_links:
    - from: "firestarter_app/doc/{protocol-id,protocol-flags,package-details}.md"
      to: "firestarter_app/doc/infoic-field-dictionary.md"
      via: "derived from the dictionary authority (D-08)"
      pattern: "consistent IC2_ALG names, bit-4 semantics, UNKNOWN markers"
---

<objective>
Rewrite the three decode docs FRESH, derived from the field dictionary (D-08): `protocol-id.md` (DOC-03), `protocol-flags.md` (DOC-02), `package-details.md` (DOC-01). Each correction is enumerated in D-10 and grounded in 56-RESEARCH. Close the wave with the regression gate confirming the existing pytest suite stays green (docs/json-only changes must not affect CI).

This phase changes NO `build_db.py` decode behavior. The docs are derived topic-level views of the dictionary written in Plan 02.

Purpose: Deliver corrected operator/maintainer-facing decode references consistent with the canonical dictionary.
Output: three rewritten `doc/*.md` files (each preserving the logo-header), plus a confirmed-green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-CONTEXT.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-VALIDATION.md
@firestarter_app/doc/infoic-field-dictionary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite protocol-id.md (DOC-03) and protocol-flags.md (DOC-02) from the dictionary</name>
  <files>firestarter_app/doc/protocol-id.md, firestarter_app/doc/protocol-flags.md</files>
  <read_first>
    - firestarter_app/doc/infoic-field-dictionary.md (the authority these docs derive from — D-08; use its protocol_id and flags entries verbatim for names/semantics)
    - firestarter_app/doc/protocol-id.md (current incorrect target — confirm errors: 0x39 listed as FLASH_INTEL_ALT, wrong NVRAM names; preserve line-1 logo-header)
    - firestarter_app/doc/protocol-flags.md (current incorrect target — confirm bit 4 mislabeled "requires write-enable sequence"; preserve line-1 logo-header)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md (§"protocol-id.md" and §"protocol-flags.md" rewrite structures; §"Logo-Header Block"; §"WARNING-5 Safety Consistency")
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md (§"D-11 BUG-4" full IC2_ALG table + exclusion rationale; §"flags" CONFIRMED bits + UNKNOWN bits 3/6/7; §"State of the Art" old-vs-correct table)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-CONTEXT.md (D-09 logo-header, D-10 specific corrections)
  </read_first>
  <action>
    Rewrite both docs fresh, derived from the dictionary (D-08). Each: line 1 = verbatim logo-header block (D-09); line 2 blank; line 3 = `---`.

    protocol-id.md (DOC-03) — follow 56-PATTERNS §"protocol-id.md" rewrite structure: H2 title, intro citing minipro IC2_ALG_* @ a8efaedc as source of truth, a summary table (Protocol-ID | IC2_ALG Constant | Firestarter Label | Firmware Handler | Notes) for the in-scope IDs (0x05/0x06/0x07/0x08/0x0B/0x0D/0x0E/0x10/0x27/0x28/0x29), and an "Excluded / Infeasible IDs" subsection with rationale for: 0x11 (IC2_ALG_FWH — LPC serial + 3.3V, infeasible on RURP), 0x2A (IC2_ALG_GAL16 — GAL PLD), 0x2C (IC2_ALG_GAL22 — GAL PLD), 0x2E (IC2_ALG_PIC32X_2 — PIC MCU), 0x35 (IC2_ALG_ITE — ITE EC MCU, TQFP128), 0x39 (PHANTOM — no IC2_ALG constant, INFOIC2PLUS-unreachable), 0x3C (INVENTED — not in minipro source). The 0x39 phantom error MUST be fixed (D-10, RESEARCH Pitfall 7 — do not list it as FLASH_INTEL_ALT/AT49F040).

    protocol-flags.md (DOC-02) — follow 56-PATTERNS §"protocol-flags.md" rewrite structure: H2 title, source line (MP_* constants from database.c L39-50 @ a8efaedc), a source-confirmed bits table (bits 1, 4, 5, 12, 13, 14, 15, 18, 19, 20-21 with MP_* constant + meaning), an UNKNOWN-bits table (bits 3, 6, 7 with "current docs claim" vs "correct: UNKNOWN — no MP_* constant"), and a WARNING-5 note. Bit 4 MUST be corrected to MP_ERASE_MASK (0x10) = "can be electrically erased" (the WARNING-5 discriminator `flags & 0x10`), NOT "requires write-enable sequence" (D-10). Keep names/semantics consistent with the dictionary and CLAUDE.md WARNING-5 (56-PATTERNS §WARNING-5 Safety Consistency).

    Do NOT touch build_db.py or chip_database.json.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -qE "IC2_ALG" doc/protocol-id.md && grep -qiE "phantom" doc/protocol-id.md && ! grep -qE "FLASH_INTEL_ALT" doc/protocol-id.md && grep -qE "MP_ERASE_MASK|can_erase" doc/protocol-flags.md && grep -qE "UNKNOWN" doc/protocol-flags.md && head -1 doc/protocol-id.md | grep -q "firestarter_logo.png" && head -1 doc/protocol-flags.md | grep -q "firestarter_logo.png" && echo DOCS_AB_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "IC2_ALG" doc/protocol-id.md` succeeds (canonical names present)
    - `grep -qiE "phantom" doc/protocol-id.md` succeeds (0x39 fixed and labeled phantom) AND `grep -qE "FLASH_INTEL_ALT" doc/protocol-id.md` returns NOTHING (the phantom 0x39 label is gone)
    - protocol-id.md contains exclusion rationale for 0x11, 0x2A, 0x2C, 0x2E, 0x35, 0x39, 0x3C
    - `grep -qE "MP_ERASE_MASK|can_erase" doc/protocol-flags.md` succeeds (bit 4 corrected)
    - `grep -qE "UNKNOWN" doc/protocol-flags.md` succeeds (bits 3/6/7 marked UNKNOWN)
    - Line 1 of both files is the verbatim logo-header (`head -1 | grep firestarter_logo.png`)
    - `git -C firestarter_app diff --name-only build_db.py firestarter/data/chip_database.json 2>/dev/null` shows neither changed
  </acceptance_criteria>
  <done>protocol-id.md uses canonical IC2_ALG_* names with the 0x39 phantom fixed and all exclusions documented; protocol-flags.md has bit 4 = MP_ERASE_MASK and bits 3/6/7 UNKNOWN; both preserve the logo-header; build_db.py/chip_database.json unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite package-details.md (DOC-01) and run the regression gate</name>
  <files>firestarter_app/doc/package-details.md</files>
  <read_first>
    - firestarter_app/doc/infoic-field-dictionary.md (authority — use its package_details and flags entries; D-08)
    - firestarter_app/doc/package-details.md (current target — confirm it is mis-titled and presents inferred bits as facts; preserve line-1 logo-header)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md (§"package-details.md" rewrite structure; §"Logo-Header Block"; §"CONFIRMED / INFERRED / UNKNOWN Labelling")
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md (§"package_details" bit layout; §"flags" CONFIRMED bits + UNKNOWN bits 3/6/7; §"Validation Architecture" for the regression command)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-VALIDATION.md (full-suite gate `cd firestarter_app && python -m pytest tests/ --cov-fail-under=70`)
  </read_first>
  <action>
    Rewrite package-details.md (DOC-01) fresh from the dictionary, following 56-PATTERNS §"package-details.md": line 1 = verbatim logo-header (D-09); line 3 = `---`. Re-title to describe `flags` (D-10 — current title is misleading; package_details governs the DIP filter, flags governs the bit table). Include: the `package_details` uint32 layout (bit 31 SMD, bits 29-24 pin count, bits 15-8 ICSP serial index, bits 7-0 adapter type) and the build_db filter note (`24 <= pin_count <= 32`, `is_smd == 0`, `is_serial == 0`, `type_int in [1,4]`); and a source-cited `flags` bits table with a Status column. Mark bits 3, 6, 7 explicitly UNKNOWN with the note "not a defined MP_* constant in database.c lines 39-50" (D-10, RESEARCH Pitfall 2). Bit 4 = MP_ERASE_MASK = "can be electrically erased" (consistent with WARNING-5). Do NOT touch build_db.py or chip_database.json.

    Then run the wave-close regression gate: `cd firestarter_app && python -m pytest tests/ --cov-fail-under=70`. The suite must stay green — Phase 56 adds only `.md`/`.json` artifacts and must not affect CI (56-VALIDATION). If coverage trips (the v1.8 floor has ~0 headroom per project memory), investigate whether any non-doc change crept in; do NOT lower the floor.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && grep -qE "UNKNOWN" doc/package-details.md && grep -qiE "flags" doc/package-details.md && head -1 doc/package-details.md | grep -q "firestarter_logo.png" && python -m pytest tests/ --cov-fail-under=70 -q && echo DOC_C_AND_REGRESSION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "UNKNOWN" doc/package-details.md` succeeds (bits 3/6/7 marked UNKNOWN)
    - package-details.md is re-titled to describe `flags` (title references flags, not the old misleading heading)
    - package-details.md documents the package_details uint32 layout and the build_db DIP filter
    - Bit 4 documented as MP_ERASE_MASK / can-erase (consistent with WARNING-5)
    - Line 1 is the verbatim logo-header
    - `cd firestarter_app && python -m pytest tests/ --cov-fail-under=70` exits 0 (existing suite green, coverage floor held)
    - `git -C firestarter_app diff --name-only build_db.py firestarter/data/chip_database.json 2>/dev/null` shows neither changed
  </acceptance_criteria>
  <done>package-details.md is re-titled to flags, source-grounded with bits 3/6/7 marked UNKNOWN and the logo-header preserved; the full pytest suite passes at --cov-fail-under=70; build_db.py/chip_database.json unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| field dictionary → derived docs | Dictionary is the trusted source; docs must not diverge from it |
| docs → operator/maintainer | Docs inform safe decode decisions (WARNING-5 consistency); incorrect bit-4 semantics is the meaningful risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-09 | Tampering | doc/dictionary divergence | mitigate | Docs derived from dictionary (D-08); grep gates assert canonical names (IC2_ALG), bit-4 fix (MP_ERASE_MASK), UNKNOWN markers; WARNING-5 consistency enforced |
| T-56-10 | Repudiation | bit-4 mislabel persists | mitigate | Negative grep asserts `FLASH_INTEL_ALT` is gone and `MP_ERASE_MASK|can_erase` present; bit 4 stays consistent with build_db `flags & 0x10` discriminator |
| T-56-11 | Denial of Service | CI regression | mitigate | Wave-close `pytest --cov-fail-under=70` gate confirms docs/json-only changes do not break the suite or trip the coverage floor |
| T-56-12 | Elevation of Privilege | n/a (no executable code) | accept | Plan adds only markdown docs; no code path, no input handling |
| T-56-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan; N/A |
</threat_model>

<verification>
- protocol-id.md: canonical IC2_ALG_* names, 0x39 phantom fixed, exclusions present, logo-header preserved
- protocol-flags.md: bit 4 = MP_ERASE_MASK, bits 3/6/7 UNKNOWN, logo-header preserved
- package-details.md: re-titled to flags, bits 3/6/7 UNKNOWN, source-grounded, logo-header preserved
- Existing pytest suite green at --cov-fail-under=70
- build_db.py and chip_database.json unchanged across all three rewrites
</verification>

<success_criteria>
- DOC-01: package-details.md re-titled to flags, source-grounded, inferred bits 3/6/7 marked UNKNOWN
- DOC-02: protocol-flags.md canonical names + bit-4 fix (can_erase)
- DOC-03: protocol-id.md canonical IC2_ALG_* names, 0x39 error fixed, exclusion rationales
- Regression gate green; no decode-behavior change
</success_criteria>

<output>
Create `.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-03-SUMMARY.md` when done.
</output>
