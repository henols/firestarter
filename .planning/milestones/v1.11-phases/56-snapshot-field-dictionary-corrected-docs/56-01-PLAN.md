---
phase: 56-snapshot-field-dictionary-corrected-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/baseline/chip_database.baseline.json
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "A committed pre-milestone baseline snapshot of the current generated chip_database.json exists in the firestarter_app sub-repo"
    - "The baseline is byte-identical to the current chip_database.json (no hand edits, no decode-behavior change)"
    - "The baseline is the FIRST committed artifact of Phase 56, uncontaminated by any doc/dictionary work"
  artifacts:
    - path: "firestarter_app/tools/baseline/chip_database.baseline.json"
      provides: "Regression anchor for Phase 59 GATE-02 per-chip diff"
      contains: "734 chip / 58 manufacturer DB; identical structure to chip_database.json"
  key_links:
    - from: "firestarter_app/tools/baseline/chip_database.baseline.json"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "point-in-time copy (no transform)"
      pattern: "identical JSON content at phase start"
---

<objective>
Commit a pre-milestone baseline snapshot of the CURRENT generated `chip_database.json` as the immutable regression anchor for v1.11 (GATE-01, re-derived per D-02/D-03). The anchor is the OUTPUT DB, not the input XML — `build_db.py` keeps fetching XML live from upstream master (D-01); the XML is deliberately NOT pinned.

This MUST be the first committed artifact of Phase 56, before any dictionary or doc work, so the snapshot is uncontaminated (RESEARCH Pitfall 5).

Purpose: Phase 59's GATE-02 diffs the regenerated DB against this frozen baseline, isolating v1.11 decode-correctness changes from any upstream `master` drift.
Output: `firestarter_app/tools/baseline/chip_database.baseline.json` committed on the `beta` branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-CONTEXT.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md
@.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot current chip_database.json as the GATE-01 baseline</name>
  <files>firestarter_app/tools/baseline/chip_database.baseline.json</files>
  <read_first>
    - firestarter_app/firestarter/data/chip_database.json (the file being snapshotted — confirm it is the current generated state)
    - firestarter_app/CLAUDE.md (note: "do NOT edit chip_database.json by hand" — the baseline is a verbatim copy, never hand-edited)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-RESEARCH.md (§"Validation Architecture", §"Runtime State Inventory" — baseline mechanics; §"Open Questions" #1 confirms `firestarter_app/tools/baseline/` is the chosen path; A2/A3 note current count is 734 chips / 58 manufacturers / 14063 lines)
    - .planning/phases/56-snapshot-field-dictionary-corrected-docs/56-PATTERNS.md (§"firestarter_app/tools/baseline/chip_database.baseline.json" — analog is chip_database.json; copy-at-point-in-time; tools/baseline/ dir does not exist yet and is created by the file itself; no .gitkeep/README needed)
  </read_first>
  <action>
    Confirm the working sub-repo is `firestarter_app` on the `beta` branch (per RESEARCH §Project Constraints). Create directory `firestarter_app/tools/baseline/` by copying the CURRENT `firestarter_app/firestarter/data/chip_database.json` verbatim to `firestarter_app/tools/baseline/chip_database.baseline.json`.

    Do NOT regenerate via `python tools/build_db.py` for this snapshot — copy the already-committed generated file as-is so the baseline reflects the exact pre-milestone state (RESEARCH A2/A3: a live re-run risks capturing upstream master drift). Do NOT hand-edit the JSON. Do NOT change any `build_db.py` decode behavior (that is Phase 57).

    This commit MUST land before the dictionary (Plan 02) and docs (Plan 03) — it is Wave 1 and the first Phase 56 artifact (RESEARCH Pitfall 5). The baseline content invariant per 56-PATTERNS is 734 chips / 58 manufacturers / 14063 lines; record the actual chip+manufacturer count observed at commit time in the SUMMARY (RESEARCH A3 — note actual count if upstream has drifted).
  </action>
  <verify>
    <automated>test -f /workspaces/firestarter_app/tools/baseline/chip_database.baseline.json && diff -q /workspaces/firestarter_app/firestarter/data/chip_database.json /workspaces/firestarter_app/tools/baseline/chip_database.baseline.json && echo BASELINE_IDENTICAL</automated>
  </verify>
  <acceptance_criteria>
    - `test -f firestarter_app/tools/baseline/chip_database.baseline.json` succeeds (file exists)
    - `diff -q firestarter_app/firestarter/data/chip_database.json firestarter_app/tools/baseline/chip_database.baseline.json` reports no differences (byte-identical copy)
    - `git -C firestarter_app diff --stat firestarter/data/chip_database.json` shows NO change to the source DB (decode behavior untouched)
    - `git -C firestarter_app diff --name-only build_db.py 2>/dev/null` shows build_db.py unchanged
    - The new file is staged/committed on the `beta` branch as the first Phase 56 artifact
  </acceptance_criteria>
  <done>Baseline JSON exists at `firestarter_app/tools/baseline/chip_database.baseline.json`, is byte-identical to the source `chip_database.json`, source DB and build_db.py are unchanged, and it is committed first on the beta branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream minipro `master` XML → build_db.py | Untrusted external input; NOT consumed in this plan (no regeneration — verbatim copy of already-generated DB) |
| committed baseline → Phase 59 diff | The baseline is the trusted reference; tamper/staleness is the only relevant risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | baseline JSON content | mitigate | Verbatim `diff -q` against source `chip_database.json` enforced in `<verify>`; no hand edits permitted |
| T-56-02 | Information Disclosure | baseline JSON | accept | DB is public generated data (734 chips); no secrets/PII |
| T-56-03 | Tampering/staleness | baseline freshness | mitigate | Snapshot is a verbatim copy of the current committed DB (not a live re-run), eliminating upstream-drift contamination at snapshot time; actual count recorded in SUMMARY |
| T-56-04 | Elevation of Privilege | n/a (no executable code) | accept | Plan adds only a committed JSON artifact; no code path, no input handling |
| T-56-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan; N/A |
</threat_model>

<verification>
- Baseline file exists and is byte-identical to the source DB (`diff -q` clean)
- Source `chip_database.json` and `build_db.py` are unchanged (no decode-behavior change)
- Baseline committed first, before any dictionary/doc work
</verification>

<success_criteria>
- `firestarter_app/tools/baseline/chip_database.baseline.json` committed on beta, byte-identical to current `chip_database.json`
- GATE-01 satisfied per re-derived D-02/D-03 (OUTPUT DB anchored, not input XML)
- No changes to build_db.py decode behavior
</success_criteria>

<output>
Create `.planning/phases/56-snapshot-field-dictionary-corrected-docs/56-01-SUMMARY.md` when done. Record actual chip + manufacturer count of the snapshot.
</output>
