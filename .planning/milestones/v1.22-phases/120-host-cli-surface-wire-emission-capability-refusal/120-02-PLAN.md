---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/constants.py
autonomous: true
requirements: [HOST-03]
must_haves:
  truths:
    - "`COMMAND_NAMES[COMMAND_SDP_UNLOCK]` and `COMMAND_NAMES[COMMAND_SDP_LOCK]` both resolve, so `_setup_operation` and `_operation_context` no longer `KeyError` on cmd 9 / cmd 10."
    - "`FLAG_SKIP_SDP_UNLOCK == 0x100` matches firmware's `firestarter.h:148` value exactly."
  artifacts:
    - "firestarter_app/firestarter/constants.py"
  key_links:
    - "`constants.py` COMMAND_* block ↔ `firestarter/include/firestarter.h:61-62` (`CMD_SDP_UNLOCK 9`, `CMD_SDP_LOCK 10`)."
    - "`constants.py` FLAG_* block ↔ `firestarter/include/firestarter.h:148` (`FLAG_SKIP_SDP_UNLOCK 0x100`)."
  prohibitions:
    - "MUST NOT modify any file under /workspaces/firestarter. `git -C /workspaces/firestarter status --porcelain` empty and tip still `0048b3d` at plan end. The firmware half already landed in Phase 119 — this plan closes the pair from the host side only."
    - "MUST NOT add a `FLAG_*` constant with value `0x200`. There is no `0x200` flag: firmware's flag block ends at `FLAG_SKIP_SDP_UNLOCK 0x100` (RESEARCH F-08 / F-120-05). ROADMAP.md:363 and Phase 120's *Depends on* line both say `0x100`/`0x200` and are wrong — record the correction, do not edit REQUIREMENTS.md."
    - "MUST NOT rename `COMMAND_DEV_REGISTERS` to match firmware's singular `CMD_DEV_REGISTER`. The plural host name has callers; the name mismatch is an enumerated exemption in plan 120-07's parity gate, not a defect."
    - "MUST NOT touch `CTRL_VPP_VPE_DROP_ENABLE = 0x100` or any other `CTRL_*` constant. Different namespace (control register, mirror of `rurp_pinout.h`), unrelated to the wire flag that shares its numeric value."
    - "MUST NOT mark HOST-03 Complete. HOST-03 spans plans 120-02 and 120-07; only 120-07 may tick it."
---

<objective>
Land the three host constants that close the wire-surface pair Phase 119 deliberately left open: `COMMAND_SDP_UNLOCK`, `COMMAND_SDP_LOCK`, `FLAG_SKIP_SDP_UNLOCK`, plus the two **mandatory** `COMMAND_NAMES` entries (HOST-03, D-12 prerequisite).

Purpose: `_setup_operation` does `COMMAND_NAMES[cmd]` at `eprom_operations.py:301` **and again at `:377`** in `_operation_context` — two `KeyError` sites. Without the name entries, every later plan that emits cmd 9 or cmd 10 crashes at operation setup. This is the phase's hard ordering constraint #1.
Output: `firestarter/constants.py` with three new constants and two new `COMMAND_NAMES` entries.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COMMAND_SDP_UNLOCK / COMMAND_SDP_LOCK and their mandatory COMMAND_NAMES entries</name>
  <files>firestarter_app/firestarter/constants.py</files>
  <read_first>
    - `firestarter_app/firestarter/constants.py` lines 50-90 — the `COMMAND_*` block (`COMMAND_READ = 1` … `COMMAND_HW_VERSION = 15`, with a numeric gap at 9/10) and the `COMMAND_NAMES` dict. Note the `# Wire-protocol command codes — Firmware sync: firestarter.h` banner above the block; the banner convention is what the meta `CLAUDE.md` duplication rule is enforced by.
    - `/workspaces/firestarter/include/firestarter.h` lines 50-70 — **read-only.** `CMD_SDP_UNLOCK 9` and `CMD_SDP_LOCK 10` at `:61-62`, both at file scope and deliberately **not** inside `#ifdef DEV_TOOLS`, with an in-source comment already naming Phase 120 HOST-01/HOST-03 as the host half.
    - `firestarter_app/firestarter/eprom_operations.py` lines 296-305 and 372-380 — the two `COMMAND_NAMES[cmd]` lookup sites.
  </read_first>
  <action>
Edit `firestarter_app/firestarter/constants.py`.

Insert `COMMAND_SDP_UNLOCK = 9` and `COMMAND_SDP_LOCK = 10` into the existing `COMMAND_*` block, filling the numeric 9/10 gap between `COMMAND_DEV_REGISTERS = 8` and `COMMAND_READ_VPP = 11`, so the block stays in ascending numeric order.

Add a short comment beside them recording two facts: that both are **unconditional** in firmware (`firestarter.h:61-62`, never `DEV_TOOLS`-gated, because they are real user-facing operations in every build), and that their `COMMAND_NAMES` entries are **load-bearing, not cosmetic** — `COMMAND_NAMES[cmd]` is dereferenced at `eprom_operations.py:301` and `:377`, so a missing entry is a `KeyError` at operation setup rather than a cosmetic gap.

Add exactly two entries to `COMMAND_NAMES`: `COMMAND_SDP_UNLOCK: "SDP_UNLOCK"` and `COMMAND_SDP_LOCK: "SDP_LOCK"`, placed after `COMMAND_DEV_REGISTERS: "DEV_REGISTERS"` and before `COMMAND_READ_VPP: "READ_VPP"` so the dict order mirrors the constant order. Use the existing all-caps underscored naming style of the surrounding values.

Do not reorder, reword or renumber any existing constant or entry.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
from firestarter.constants import COMMAND_SDP_UNLOCK, COMMAND_SDP_LOCK, COMMAND_NAMES
assert COMMAND_SDP_UNLOCK==9 and COMMAND_SDP_LOCK==10
assert COMMAND_NAMES[COMMAND_SDP_UNLOCK]=='SDP_UNLOCK'
assert COMMAND_NAMES[COMMAND_SDP_LOCK]=='SDP_LOCK'
assert len(COMMAND_NAMES)==15
print('OK')" && python3 -m pytest tests/test_revision_constants_parity.py -q && ruff check firestarter/constants.py && ruff format --check firestarter/constants.py</automated>
  </verify>
  <acceptance_criteria>
    - `COMMAND_SDP_UNLOCK == 9` and `COMMAND_SDP_LOCK == 10`.
    - `COMMAND_NAMES[9] == "SDP_UNLOCK"` and `COMMAND_NAMES[10] == "SDP_LOCK"`.
    - `len(COMMAND_NAMES) == 15` (was 13).
    - The `COMMAND_*` block is still in ascending numeric order with no gap at 9/10.
    - `python3 -m pytest tests/test_revision_constants_parity.py -q` still passes (6 tests, pre-rebuild shape — plan 120-07 rebuilds it).
    - `ruff check` and `ruff format --check` pass on `firestarter/constants.py`.
    - `git -C /workspaces/firestarter status --porcelain` is empty.
  </acceptance_criteria>
  <done>Both SDP command codes and both name entries exist, so cmd 9 / cmd 10 can reach `_setup_operation` without a `KeyError`.</done>
</task>

<task type="auto">
  <name>Task 2: Add FLAG_SKIP_SDP_UNLOCK = 0x100 with the CTRL-namespace disambiguation comment</name>
  <files>firestarter_app/firestarter/constants.py</files>
  <read_first>
    - `firestarter_app/firestarter/constants.py` lines 88-125 — the `FLAG_*` block ending at `FLAG_VERBOSE = 0x80`, and the `RURP_CONTROL_REGISTER_BITS` block below it whose first member is `CTRL_VPP_VPE_DROP_ENABLE = 0x100`.
    - `/workspaces/firestarter/include/firestarter.h` lines 131-150 — **read-only.** The nine firmware `FLAG_*` defines, ending at `FLAG_SKIP_SDP_UNLOCK 0x100`. Confirm for yourself that the block ends there and that no `0x200` flag exists.
  </read_first>
  <action>
Edit `firestarter_app/firestarter/constants.py`.

Add `FLAG_SKIP_SDP_UNLOCK = 0x100` immediately after `FLAG_VERBOSE = 0x80`, inside the existing `# Control Flags — Firmware sync: firestarter.h` block, so all nine wire flags sit together in ascending bit order.

Add a two-part comment beside it. Part one: this is the **ninth and highest** wire flag; firmware's `ctrl_flags` is `uint32_t`, so `0x100` is in range, and firmware's flag block **ends here** — there is no `0x200` flag, contrary to `ROADMAP.md:363` and Phase 120's *Depends on* line (F-120-05). Part two: `CTRL_VPP_VPE_DROP_ENABLE` further down this file also has the value `0x100` but lives in the **control-register** namespace mirroring `rurp_pinout.h`, is documentary only (Python never writes the control register), and has its own separate parity leg — the two `0x100`s are unrelated and must not be conflated.

Do not touch the `CTRL_*` block.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import firestarter.constants as c
assert c.FLAG_SKIP_SDP_UNLOCK==0x100
assert c.CTRL_VPP_VPE_DROP_ENABLE==0x100
flags={n:v for n,v in vars(c).items() if n.startswith('FLAG_')}
assert len(flags)==9, flags
assert max(flags.values())==0x100
assert not any(v==0x200 for v in flags.values())
print('OK', sorted(flags.items(), key=lambda kv: kv[1]))" && python3 -m pytest tests/test_revision_constants_parity.py -q && ruff check firestarter/constants.py && ruff format --check firestarter/constants.py</automated>
  </verify>
  <acceptance_criteria>
    - `FLAG_SKIP_SDP_UNLOCK == 0x100`.
    - Exactly **nine** `FLAG_*` constants exist in `firestarter/constants.py`; the maximum value is `0x100`; **no** `FLAG_*` constant has value `0x200`.
    - `CTRL_VPP_VPE_DROP_ENABLE` is still `0x100` and the `CTRL_*` block is unmodified (`git diff` shows no change in that region).
    - `python3 -m pytest tests/test_revision_constants_parity.py -q` still passes.
    - `ruff check` and `ruff format --check` pass.
    - `git -C /workspaces/firestarter status --porcelain` is empty.
  </acceptance_criteria>
  <done>The one new wire flag exists at the firmware-matching bit value, with the two-`0x100`-namespaces trap documented in-source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `constants.py` → wire frame | These integers become the `cmd` and `flags` fields of a JSON command frame that drives physical hardware. A wrong value drives the wrong operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-05 | Tampering | A host `cmd` or `flags` value that disagrees with `firestarter.h` | high | mitigate | Values transcribed from the firmware header read in this plan; plan 120-07 replaces the hollow literal-comparison test with a real two-way header-parsing gate so future drift fails closed. |
| T-120-06 | Spoofing | A reader conflating `FLAG_SKIP_SDP_UNLOCK 0x100` with `CTRL_VPP_VPE_DROP_ENABLE 0x100` | low | mitigate | Explicit in-source disambiguation comment; the `FLAG_*`-scoped extractor in 120-07 never sees the `CTRL_*` block. |
| T-120-07 | Denial of Service | Missing `COMMAND_NAMES` entry → `KeyError` at operation setup | medium | mitigate | Both entries added here; 120-07's gate asserts every parsed `CMD_*` has both a constant **and** a `COMMAND_NAMES` entry. |
| — | Tampering (supply chain) | Package installs | n/a | accept | Zero packages installed in this plan. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_revision_constants_parity.py tests/test_eprom_operations.py -q`.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` — CI-scoped form only.
- `python3 tools/check_mypy_watermark.py` — reported error count stays at **1**.
- `git -C /workspaces/firestarter status --porcelain` empty; tip `0048b3d`.
- `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/ firestarter/messages.py` empty.
- Known-RED baselines named, not tolerated: `test_audit_coverage_matrix.py::test_golden_file_matches`; `test_no_programmer_found_*` with a board attached.
</verification>

<success_criteria>
- Three new constants and two new `COMMAND_NAMES` entries exist at firmware-matching values.
- No `0x200` flag was invented.
- The existing parity test still passes unchanged (its rebuild is plan 120-07's work).
- HOST-03 is **not** ticked by this plan.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New symbols:** `COMMAND_SDP_UNLOCK = 9`, `COMMAND_SDP_LOCK = 10`, `FLAG_SKIP_SDP_UNLOCK = 0x100`, `COMMAND_NAMES[9] = "SDP_UNLOCK"`, `COMMAND_NAMES[10] = "SDP_LOCK"`.

**New files:** none.

**Requirement IDs this plan may mark Complete:** **none.** HOST-03 spans plans 120-02 (constants + `COMMAND_NAMES`) and 120-07 (the rebuilt parity gate); only 120-07 may tick it. Re-check `.planning/REQUIREMENTS.md` after this plan.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-02-SUMMARY.md` when done.

The SUMMARY must record the F-120-05 correction as a fact: firmware's flag block ends at `FLAG_SKIP_SDP_UNLOCK 0x100`; there is **no** `0x200` flag; `ROADMAP.md:363` and Phase 120's *Depends on* line are wrong; `REQUIREMENTS.md` was deliberately **not** edited. Also record that both `CMD_SDP_*` defines are unconditional in firmware by design, and that `COMMAND_NAMES` has two dereference sites (`eprom_operations.py:301` and `:377`), not one.
</output>
