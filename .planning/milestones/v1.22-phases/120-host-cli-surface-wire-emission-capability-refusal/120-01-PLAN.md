---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/sdp_capability.py
  - firestarter_app/tests/test_sdp_capability.py
autonomous: true
requirements: [HOST-04]
must_haves:
  truths:
    - "`sdp_capability('at28c256', db)` returns `(True, reason)` and `sdp_capability('2816', db)` returns `(False, reason)` with a reason naming why."
    - "A `0x0D` part whose alias tokens are not all on the allow-list is refused, including a part that exists only in a user-supplied local override."
    - "The predicate reads `db.get_eprom(name)`'s `name` + `protocol-id` keys and never a `resolve_chip` programmer dict (D-03 mechanism correction, RESEARCH F-06)."
  artifacts:
    - "firestarter_app/firestarter/sdp_capability.py"
    - "firestarter_app/tests/test_sdp_capability.py"
  key_links:
    - "`SDP_CAPABLE_TOKENS` ↔ the 43 ALLOW entries in `120-sdp-partition.json` (transcribed, never re-derived)."
    - "The exhaustiveness gate ↔ the shipped `firestarter/data/chip_database.json` 84 `algorithm == 13` entries."
  prohibitions:
    - "MUST NOT modify any file under /workspaces/firestarter. `git -C /workspaces/firestarter status --porcelain` empty and `git -C /workspaces/firestarter rev-parse --short HEAD` still `0048b3d` at plan end."
    - "MUST NOT modify firestarter/data/chip_database.json, firestarter/messages.py, tools/build_db.py, or any `support_status` value."
    - "MUST NOT add `tools/infoic.xml` or any copy of it to either sub-repo. Nothing in this plan reads `infoic.xml` at runtime or in CI."
    - "MUST NOT re-derive, re-infer, widen, narrow or 'tidy' the partition membership. Transcribe from `120-sdp-partition.json`."
    - "MUST NOT replace the token table with a structural or lexical rule (RESEARCH F-03: `DIP28_28C64` splits 15 ALLOW / 20 REFUSE; `2817` sits on a different pinout from `2804`/`2816`)."
    - "MUST NOT mark HOST-04 Complete. HOST-04 spans plans 120-01, 120-05 and 120-09; only 120-09 may tick it."
    - "MUST NOT import `click`, `serial`, `firestarter.serial_comm` or `firestarter.eprom_operations` in `sdp_capability.py` (D-03 purity, and Phase 121's GATE-01 will assert against this module)."
---

<objective>
Land the SDP capability predicate and its derived allow-set as a new pure module, plus the core exhaustiveness gate that makes the partition a machine-checked fact (HOST-04, D-01/D-02/D-03).

Purpose: on a `0x0D` part with no SDP command decoder the `AA-55-A0` / `AA-55-80-AA-55-20` sequences are **not inert** — post-Phase-117 the command writes reach silicon and are stored as data at the bus-truncated magic addresses. A fail-closed allow-list is therefore a safety requirement, not tidiness.
Output: `firestarter/sdp_capability.py` (allow-set + predicate) and `tests/test_sdp_capability.py` (partition totality, per-entry predicate agreement, non-vacuity).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-PATTERNS.md
</context>

## ⚠ Authoritative partition — read before writing a single token

`120-SDP-PARTITION.md` **supersedes `120-RESEARCH.md` § F-01**. The partition is **ALLOW 43 / REFUSE 41 = 84**, derived from minipro `infoic.xml` `flags` **bit 15** (`0x8000`, `MP_PROTECT_AFTER`) in the `<database type="INFOIC2PLUS">` section at commit `a8efaedc236c1d9718bd28299dfbb99536b010ff`.

- RESEARCH § F-01's curated **37 / 47** partition and all five of its judgement calls are **superseded**. Do not plan or code against 37/47.
- The interim operator placeholder "allow both disputed groups / 74-10" is likewise **superseded**.
- `120-sdp-partition.json` is the machine-readable source. **Transcribe the exact `part_number` strings from it.** Do not regenerate the list by rule.

## Verified arithmetic — pin these exact numbers

| Fact | Value |
|---|---|
| Shipped-DB entries with `programming.algorithm == 13` | **84** `(manufacturer, part_number)` pairs |
| ALLOW / REFUSE pairs | **43 / 41** |
| Distinct `part_number` strings across the 84 | **81** (40 ALLOW / 41 REFUSE) |
| Why 81 not 84 | `M28010`, `M28C64,M28C64A` and `M28C64-xxW` are each listed under **both** `SGS-THOMSON` and `ST` (second-source duplicates). Both copies land on the **same** side. |
| Total alias-token instances across the 84 | **134** |
| Distinct uppercased tokens | **130** — **65 ALLOW / 65 REFUSE, intersection empty** |

Every one of these was measured against the shipped `firestarter/data/chip_database.json` at plan time.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create firestarter/sdp_capability.py with the derived allow-set and provenance docstring</name>
  <files>firestarter_app/firestarter/sdp_capability.py</files>
  <read_first>
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json` — the exact `part_number` strings to transcribe (`allow` array, 43 objects).
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` §1 (provenance), §3 (the tables), §7 (how it ships).
    - `firestarter_app/firestarter/eprom_operations.py` lines 1650-1680 — `_SRAM_PROTO_IDS`' frozen-module-constant-with-a-why-comment shape. Copy that shape. Do **not** copy its two `.get()` data-access lines.
    - `firestarter_app/firestarter/constants.py` lines 50-60 — the `# ... — Firmware sync: <header>` banner convention for mirrored constant blocks.
  </read_first>
  <behavior>
    - `len(SDP_CAPABLE_TOKENS) == 65` and every member is uppercase with no surrounding whitespace.
    - `split_part_number_tokens("AT28C64,AT28C64B(Non-Standard),AT28HC64,AT28HC64L")` returns a 4-tuple of uppercased tokens with the parenthetical **retained**.
    - `"AT28C64B(NON-STANDARD)" not in SDP_CAPABLE_TOKENS` and `"AT28C64B" in SDP_CAPABLE_TOKENS` — the two are different entries in `infoic.xml` and the DB mirrors that split.
    - `FRAM_TOKENS == frozenset({"FM28V020", "MB85R256H"})`.
  </behavior>
  <action>
Create `firestarter_app/firestarter/sdp_capability.py`.

Start the file with `from __future__ import annotations` — CI targets **py3.9/3.11**, so builtin-generic annotations such as `tuple[bool, str]` and `frozenset[str]` require it.

Module docstring must record the provenance so a future reader can re-derive rather than re-guess. Name, in this order: (a) that this table is a static fail-closed allow-list held in production code so a `0x0D` part a user adds to `~/.firestarter/database.json` (merged live at `database.py:187-199`, invisible to CI) is **refused**, not silently permitted; (b) the source — minipro `infoic.xml` at commit `a8efaedc236c1d9718bd28299dfbb99536b010ff`, section `<database type="INFOIC2PLUS">`, axis `flags` bit 15 (`0x8000`, `MP_PROTECT_AFTER`); (c) the counts 43 ALLOW / 41 REFUSE / 84 total and 65 distinct allow tokens; (d) that **nothing reads `infoic.xml` at runtime or in CI** and it is not committed to either sub-repo; (e) a pointer to `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md`; (f) the three ground-truth probes that validated the axis (HOST-04's 8 named pre-SDP entries all bit-15=0; both FRAM parts bit-15=0; the four datasheet-of-record Atmel parts all bit-15=1); (g) that bit 15 is **not** a page-write proxy — it disagrees with `page_size > 1` on 12 of the 84 — and a pointer to §4's 9-entry residual-risk watch-list; (h) that decoding bits 14/15 into the DB proper would later make this table generated rather than transcribed, narrowing but not removing the curation.

Define `SDP_PROTOCOL_ID = 13` with a comment that this is the DB's `protocol-id` value (the same integer `programming.algorithm` carries), and that the key to read is `protocol-id` from `db.get_eprom()` and **never** `algorithm` from `resolve_chip()`/`convert_to_programmer()` (RESEARCH F-02, F-06).

Define `SDP_CAPABLE_TOKENS: frozenset[str]` — the **65 distinct uppercased alias tokens** obtained by comma-splitting the 43 `part_number` strings in `120-sdp-partition.json`'s `allow` array, `.strip()`ped and `.upper()`ed, with parentheticals **retained verbatim**. Transcribe; do not compute from a rule. Write the tokens grouped by manufacturer in the same order the JSON lists them, with a short `# <MANUFACTURER>` comment per group so a reviewer can diff the file against `120-SDP-PARTITION.md` §3 by eye. Add an adjacent comment recording that four tokens (`M28010`, `M28C64`, `M28C64A`, `M28C64-XXW`) appear on two entries each — `SGS-THOMSON` and `ST` second-source listings of the same parts — which is why 43 entries yield 65 distinct tokens, and that both copies are on the same side so token keying is well-defined.

Define `FRAM_TOKENS: frozenset[str]` containing exactly `FM28V020` and `MB85R256H`, with a comment stating both carry `electrical.type == "EEPROM"` in the DB (nothing in the DB says FRAM), so the existing `etype in ("SRAM", "FRAM")` idiom is blind to them and no structural rule can find them — this is why HOST-04 says "resolved in code".

Define `PRE_SDP_NAMED_TOKENS: frozenset[str]` containing the tokens of HOST-04's named pre-SDP class plus its identical-generation second sources, exactly: `2804`, `2816`, `2817`, `X2804A`, `X2804AI`, `X2816A`, `X2816B`, `X2816C`, `XL2804A`, `XL2816A`, `XLE28C16A`, `XLS28C16A`. Comment that `2817` sits on `DIP28_28C64` while `2804`/`2816` sit on `DIP24_2816`, so the trio spans two pinouts and no pinout rule can express it (RESEARCH F-03).

Define `split_part_number_tokens(part_number: str) -> tuple[str, ...]` — comma-split, `.strip()`, `.upper()`, drop empties. Docstring must state rule 1 verbatim in intent: key on the exact token as it appears in `part_number`; **do not strip parentheticals** — stripping collapses `AT28C64B(Non-Standard)` onto the separate `AT28C64B` entry and produces a spurious MIXED verdict (`120-SDP-PARTITION.md` §5), and it makes the key not a function of one entry.

Do not add any other public symbol in this task; the predicate is Task 2.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "from firestarter import sdp_capability as s; assert len(s.SDP_CAPABLE_TOKENS)==65, len(s.SDP_CAPABLE_TOKENS); assert s.SDP_PROTOCOL_ID==13; assert s.FRAM_TOKENS==frozenset({'FM28V020','MB85R256H'}); assert len(s.PRE_SDP_NAMED_TOKENS)==12; assert all(t==t.upper().strip() and t for t in s.SDP_CAPABLE_TOKENS); assert s.split_part_number_tokens('AT28C64,AT28C64B(Non-Standard),AT28HC64,AT28HC64L')==('AT28C64','AT28C64B(NON-STANDARD)','AT28HC64','AT28HC64L'); assert 'AT28C64B' in s.SDP_CAPABLE_TOKENS and 'AT28C64B(NON-STANDARD)' not in s.SDP_CAPABLE_TOKENS; print('OK')" && ruff check firestarter/sdp_capability.py && ruff format --check firestarter/sdp_capability.py</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/firestarter/sdp_capability.py` exists and its first statement is `from __future__ import annotations`.
    - `len(SDP_CAPABLE_TOKENS) == 65`; every member equals its own `.strip().upper()`; no member is empty.
    - The 65 tokens are exactly the comma-split tokens of the 43 `part_number` strings in `120-sdp-partition.json`'s `allow` array (verified by the Task 3 gate; here verified by count and by the two `AT28C64B` spot checks).
    - `SDP_PROTOCOL_ID == 13`.
    - `FRAM_TOKENS == frozenset({"FM28V020", "MB85R256H"})`.
    - `len(PRE_SDP_NAMED_TOKENS) == 12` and `"2817" in PRE_SDP_NAMED_TOKENS`.
    - `split_part_number_tokens` retains the parenthetical: the 4-tuple in the verify command matches exactly.
    - The module docstring contains the literal commit id `a8efaedc236c1d9718bd28299dfbb99536b010ff`, the literal `INFOIC2PLUS`, the literal `bit 15`, and the literal `120-SDP-PARTITION.md`.
    - `ruff check` and `ruff format --check` pass on the new file.
  </acceptance_criteria>
  <done>The derived 43-entry allow-set ships as 65 exact tokens in a new pure module whose docstring records the full derivation provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the name-keyed sdp_capability predicate with a four-way refusal taxonomy</name>
  <files>firestarter_app/firestarter/sdp_capability.py</files>
  <read_first>
    - `firestarter_app/firestarter/eprom_operations.py` lines 1652-1677 — `check_eprom_blank`'s pre-wire short-circuit. Copy its **four-part wording register** (name the chip upper-cased, name the observed field values, state the mechanism, state the consequence). Do **not** copy its `eprom_data_dict.get("electrical-type")` / `.get("protocol-id")` access — that is the anti-pattern this task exists to avoid.
    - `firestarter_app/firestarter/database.py` lines 364-441 — `_map_data`, which is where `name` and `protocol-id` come from on `get_eprom()`'s output.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-RESEARCH.md` § "Pitfall 1" and § F-06 — the measured proof that `resolve_chip`'s dict carries neither `protocol-id` nor `name`, and that `check_eprom_blank`'s short-circuit is therefore vacuous in production.
  </read_first>
  <behavior>
    - `sdp_capability("at28c256", db)` → `(True, reason)` where `REASON_ALLOWED` is a substring of `reason`.
    - `sdp_capability("2816", db)` → `(False, reason)` where `REASON_NOT_CAPABLE` is a substring.
    - `sdp_capability("fm28v020", db)` → `(False, reason)` where `REASON_FRAM` is a substring.
    - `sdp_capability("w27c512", db)` → `(False, reason)` where `REASON_WRONG_PROTOCOL` is a substring and the reason names the observed protocol number.
    - `sdp_capability("no-such-chip-xyz", db)` → `(False, reason)` where `REASON_NOT_FOUND` is a substring.
    - An entry whose tokens are split across allow and refuse is refused (unanimity), never allowed.
  </behavior>
  <action>
Extend `firestarter_app/firestarter/sdp_capability.py`.

Define five module-level reason-fragment constants so tests assert on stable substrings rather than whole sentences: `REASON_NOT_FOUND = "not found in the chip database"`; `REASON_WRONG_PROTOCOL = "SDP lock/unlock applies only to protocol 0x0D parallel EEPROMs"`; `REASON_FRAM = "ferroelectric RAM (FRAM)"`; `REASON_NOT_CAPABLE = "not on the SDP-capable list"`; `REASON_ALLOWED = "SDP-capable per infoic.xml INFOIC2PLUS flags bit 15"`.

Define `sdp_capability_for_entry(entry: Mapping[str, Any] | None, display_name: str) -> tuple[bool, str]`. `entry` is a `db.get_eprom()`-shaped **full** dict. Evaluate in this order and return on the first match:

1. `entry` falsy → `(False, "<DISPLAY_NAME>: " + REASON_NOT_FOUND)`.
2. `entry.get("protocol-id")` is not `SDP_PROTOCOL_ID` → `(False, ...)` naming `REASON_WRONG_PROTOCOL` and the observed protocol rendered as two-digit uppercase hex. Treat a missing `protocol-id` key as a **hard failure of the caller's contract**, not as protocol 0: raise `KeyError` (or `ValueError`) with a message naming `resolve_chip`/`convert_to_programmer` as the likely wrong dict, because a silent default is exactly how `check_eprom_blank`'s `_SRAM_PROTO_IDS` short-circuit became vacuous in production (RESEARCH F-06). This anti-vacuity behaviour is asserted by plan 120-05's shape leg.
3. Tokens of `entry["name"]` via `split_part_number_tokens`; if **any** token is in `FRAM_TOKENS` → `(False, ...)` naming `REASON_FRAM`, stating that FRAM has no EEPROM software-data-protection command decoder at all, and stating the consequence: the sequence would store three bytes as data.
4. **Unanimity rule** — collect tokens not in `SDP_CAPABLE_TOKENS`. If any → `(False, ...)` naming `REASON_NOT_CAPABLE`, listing the unrecognised tokens, saying whether they are in `PRE_SDP_NAMED_TOKENS` ("pre-SDP generation") or simply unrecognised, and stating the consequence in the four-part register: refused fail-closed because the SDP command sequence is not inert on a part without an SDP command decoder — its bytes are stored as data at the bus-truncated magic addresses.
5. Otherwise `(True, "<NAME>: " + REASON_ALLOWED)`.

Docstring must state, in one sentence each: the unanimity rule and why it is fail-closed (`EXEL/XL2816A,XLE28C16A,XLS28C16A` refuses as a whole entry even though two of its three tokens look like `28C`-generation parts); and that the function is pure — no serial, no Click, no DB construction, no file I/O.

Define `sdp_capability(chip_name: str, db: Any) -> tuple[bool, str]` = `sdp_capability_for_entry(db.get_eprom(chip_name), chip_name)`. Its docstring must state the D-03 mechanism correction explicitly: CONTEXT.md D-03 said "no DB-loader coupling", which is **not achievable** because the predicate needs the part number, and `resolve_chip`'s programmer dict carries neither `protocol-id` nor `name` nor any part number (RESEARCH F-06, measured). The predicate is therefore **name-keyed with an injected `db`**; D-03's semantics (pure function, no serial, no Click, `-> (allowed, reason)`) are preserved. Also state that the entry the predicate evaluates is the entry `db.get_eprom` **actually chose** — never the user's typed string — because `get_eprom_config` returns the first alias match in DB iteration order and two entries can match the same paren-stripped alias.

Keep the module import list to `__future__` plus `typing` only. Do not import `click`, `serial`, `firestarter.serial_comm`, `firestarter.eprom_operations` or `firestarter.database`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import ast
from firestarter.database import EpromDatabase
from firestarter import sdp_capability as s
db=EpromDatabase(skip_local_override=True)
ok,r=s.sdp_capability('at28c256',db); assert ok and s.REASON_ALLOWED in r, r
ok,r=s.sdp_capability('2816',db); assert not ok and s.REASON_NOT_CAPABLE in r, r
ok,r=s.sdp_capability('fm28v020',db); assert not ok and s.REASON_FRAM in r, r
ok,r=s.sdp_capability('w27c512',db); assert not ok and s.REASON_WRONG_PROTOCOL in r, r
ok,r=s.sdp_capability('no-such-chip-xyz',db); assert not ok and s.REASON_NOT_FOUND in r, r
tree=ast.parse(open('firestarter/sdp_capability.py').read())
mods=set()
for n in ast.walk(tree):
    if isinstance(n,ast.Import): mods.update(a.name.split('.')[0] for a in n.names)
    if isinstance(n,ast.ImportFrom) and n.module: mods.add(n.module.split('.')[0])
assert mods <= {'__future__','typing'}, mods
print('OK')" && ruff check firestarter/sdp_capability.py && ruff format --check firestarter/sdp_capability.py</automated>
  </verify>
  <acceptance_criteria>
    - `sdp_capability` and `sdp_capability_for_entry` exist with the signatures named above and return a 2-tuple `(bool, str)`.
    - All five behaviour rows in `<behavior>` hold, asserted by the verify command.
    - `sdp_capability_for_entry` raises (does not return `(False, ...)` and does not return `(True, ...)`) when handed a dict with no `protocol-id` key, and the exception message names `resolve_chip`.
    - Unanimity holds: `sdp_capability('xl2816a', db)` is `(False, ...)`.
    - The module's top-level import set is a subset of `{"__future__", "typing"}`, asserted by AST in the verify command.
    - `sdp_capability`'s docstring contains the literal `RESEARCH F-06` (or `F-06`) and the literal `name-keyed`.
    - `ruff check` and `ruff format --check` pass.
  </acceptance_criteria>
  <done>A pure, name-keyed predicate answers both "is this even `0x0D`" and "does this `0x0D` part have SDP" in one call, with a four-way refusal taxonomy and a hard failure — never a silent default — on the wrong dict shape.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the core exhaustiveness gate — 43/41 totality, per-entry predicate agreement, non-vacuity</name>
  <files>firestarter_app/tests/test_sdp_capability.py</files>
  <read_first>
    - `firestarter_app/tests/test_sdp_db_invariant.py` — **the template. Copy its structure verbatim:** the numbered-Coverage module docstring ending in an explicit non-vacuity item; the `_FA_DIR` / `_DB_FILE` / `_ALGORITHM_0X0D` constants; the shared `_select_0x0d_chips` helper with its comment about the nested `programming` object; the try/except/else inversion of the non-vacuity leg; and its documented paragraph explaining why it deliberately carries **no** skip marker.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json` — the 43 + 41 `part_number` strings to transcribe as the test-side expected partition.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` §8 — the validation consequences, including that the counts change from 74/10 and 37/47 to **43/41**.
  </read_first>
  <action>
Create `firestarter_app/tests/test_sdp_capability.py`.

Module docstring: follow `test_sdp_db_invariant.py`'s shape — a numbered Coverage list whose last item is the non-vacuity proof, plus the no-skip-marker paragraph (this module reads only the packaged `chip_database.json`, always present in host-only CI, so a `FW_ABSENT`-style skip marker must never leak in here and silently make HOST-04 vacuous). Add one paragraph recording that the expected partition below is **transcribed** from `120-sdp-partition.json` (derived from `infoic.xml` bit 15) and is the independent expectation the production allow-list is checked against — production holds only the ALLOW half, so the refuse half living here is what makes the gate non-vacuous rather than a tautology.

Transcribe two module-level constants: `EXPECTED_ALLOW_PART_NUMBERS: frozenset[str]` from the JSON `allow` array's `part_number` values, and `EXPECTED_REFUSE_PART_NUMBERS: frozenset[str]` from `refuse`. Note in a comment that these are **40** and **41** distinct strings respectively (not 43/41) because three `part_number` strings are listed under both `SGS-THOMSON` and `ST`, and that the 43/41 figure counts `(manufacturer, part_number)` pairs.

Reuse `_select_0x0d_chips`'s shape from the template (copy it into this module rather than importing across test modules, matching the template's own self-containment).

Add these legs:

`test_partition_covers_exactly_the_84_0x0d_entries` — load the shipped DB with `json.loads(_DB_FILE.read_text(encoding="utf-8"))`; select `algorithm == 13`; assert `len(selected) == 84`; assert the count of pairs whose `part_number` is in `EXPECTED_ALLOW_PART_NUMBERS` is `43` and in `EXPECTED_REFUSE_PART_NUMBERS` is `41`; assert the set of distinct `part_number` values equals `EXPECTED_ALLOW_PART_NUMBERS | EXPECTED_REFUSE_PART_NUMBERS` (81 strings); assert the two expected sets are disjoint. On failure the message must name the offending `part_number` values and say that a `build_db.py` regeneration that adds or renames a `0x0D` part must update `120-sdp-partition.json` and this table together, never widen the allow-list by default.

`test_allow_and_refuse_token_sets_are_disjoint_and_total` — comma-split all 84 entries' `part_number` values; assert `134` total token instances and `130` distinct uppercased tokens; assert the distinct allow-side tokens number `65` and equal `sdp_capability.SDP_CAPABLE_TOKENS` exactly; assert the distinct refuse-side tokens number `65`; assert the two token sets are disjoint. This is the leg that catches a typo in the production transcription.

`test_predicate_agrees_with_the_derived_partition_on_all_84_entries` — for each selected `(manufacturer, chip)` build a minimal full-dict shape `{"name": chip["part_number"], "protocol-id": chip["programming"]["algorithm"]}` and call `sdp_capability_for_entry`, asserting `allowed is (chip["part_number"] in EXPECTED_ALLOW_PART_NUMBERS)` for all 84, and that the returned reason is a non-empty string. Add a comment stating why a literal dict is correct **here** and wrong elsewhere: this leg's subject is the shipped `part_number` ground truth, and plan 120-05's shape leg is the one that proves `db.get_eprom()` really produces `name` + `protocol-id` for real parts. Collect all mismatches and assert once, so a failure lists every disagreeing part rather than the first.

`test_synthetic_unknown_0x0d_entry_is_refused_non_vacuous` — copy the template's try/except/else inversion. Build a synthetic in-memory DB dict with one entry `part_number: "SYNTHETIC_0X0D_VIOLATION"`, `programming.algorithm: 13`; run it through the **same** `_select_0x0d_chips` helper the real leg uses; assert exactly one selected; then assert `sdp_capability_for_entry` refuses it with `REASON_NOT_CAPABLE` in the reason, and assert the totality helper the real leg uses raises `AssertionError` on it. Use the else-branch `raise AssertionError("Non-vacuity failure: ...")` shape so the leg cannot silently pass.

Do not add a skip marker of any kind to this module.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_sdp_capability.py -q && grep -c "skipif" tests/test_sdp_capability.py | grep -qx 0 && ruff check tests/test_sdp_capability.py && ruff format --check tests/test_sdp_capability.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sdp_capability.py` exists and all four legs pass.
    - `grep -c "skipif" tests/test_sdp_capability.py` returns `0` — no skip marker of any kind.
    - The gate pins the exact numbers `84`, `43`, `41`, `81`, `134`, `130`, `65`, `65`.
    - `EXPECTED_ALLOW_PART_NUMBERS` has 40 members; `EXPECTED_REFUSE_PART_NUMBERS` has 41; they are disjoint.
    - `test_allow_and_refuse_token_sets_are_disjoint_and_total` asserts set equality against `sdp_capability.SDP_CAPABLE_TOKENS` (not merely a length check).
    - `test_predicate_agrees_with_the_derived_partition_on_all_84_entries` covers all 84 pairs and reports all mismatches in one assertion message.
    - The non-vacuity leg uses the try/except/else inversion and raises an `AssertionError` whose message contains the literal `Non-vacuity failure` when the helper fails to fire.
    - `ruff check` and `ruff format --check` pass.
  </acceptance_criteria>
  <done>The partition is a machine-checked fact: total over the shipped 84, agreeing with the predicate entry-by-entry, and provably capable of failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.firestarter/database.json` → live DB | Untrusted user-authored chip entries merge into the live DB at `database.py:187-199`, invisible to CI, and reach this predicate at runtime. |
| DB entry → SDP command emission | The predicate is the authorization boundary deciding which physical part may receive an SDP command sequence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-01 | Tampering | `~/.firestarter/database.json` entry claiming `algorithm: 13` for an arbitrary part | high | mitigate | The allow-list is a **runtime** production-code check (D-02), so an unrecognised token is refused by the unanimity rule regardless of what CI saw. A CI-only gate would not see this file at all. |
| T-120-02 | Elevation of Privilege (hardware) | An unrecognised chip token defaulting to permitted | high | mitigate | Fail-closed default: unknown ⇒ refuse. Enforced by the unanimity rule and pinned by Task 3's totality and token-set legs. |
| T-120-03 | Tampering (physical) | SDP sequence stored as data at truncated magic addresses on a part with no SDP decoder | high | mitigate | This predicate is the pre-wire refusal; plans 120-08 and 120-09 are its two call sites. |
| T-120-04 | Repudiation | A predicate that silently returns the fail-open branch of a default (`_SRAM_PROTO_IDS` vacuity mode) | high | mitigate | `sdp_capability_for_entry` **raises** on a dict with no `protocol-id`, and plan 120-05 ships the shape leg that machine-excludes the vacuity mode. |
| — | Tampering (supply chain) | Package installs | n/a | accept | This plan installs zero packages; no `pip install`, no `pyproject.toml` dependency change, no new third-party import. No Package Legitimacy Gate applies. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_sdp_capability.py -q` — all legs green.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` — the **CI-scoped** form. A bare `ruff check .` reports 4 pre-existing failures in `tools/` that are outside CI scope; do not chase them.
- `python3 tools/check_mypy_watermark.py` — reported error count must stay at **1** (the pre-existing `firestarter/submit.py:446`). The watermark is 35 with 34 slack, so a bare pass is not evidence.
- `git -C /workspaces/firestarter status --porcelain` empty and `git -C /workspaces/firestarter rev-parse --short HEAD` is `0048b3d`.
- `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/ firestarter/messages.py tools/build_db.py` empty.
- Known-RED baselines, named not tolerated: `tests/test_audit_coverage_matrix.py::test_golden_file_matches` (stale golden, pre-existing) and `tests/test_no_programmer_found_*` (go RED with a board attached; three are attached). Neither is this plan's regression.
</verification>

<success_criteria>
- `firestarter/sdp_capability.py` holds the 65-token derived allow-set with full provenance in its docstring and a pure name-keyed predicate.
- `tests/test_sdp_capability.py` pins 43/41 over the shipped 84 with per-entry predicate agreement, a token-set equality leg, and a non-vacuity leg, and carries no skip marker.
- Firmware sub-repo byte-untouched; DB and codegen untouched.
- HOST-04 is **not** ticked by this plan.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New files:** `firestarter_app/firestarter/sdp_capability.py`, `firestarter_app/tests/test_sdp_capability.py`.

**New symbols:** `SDP_PROTOCOL_ID`, `SDP_CAPABLE_TOKENS`, `FRAM_TOKENS`, `PRE_SDP_NAMED_TOKENS`, `REASON_NOT_FOUND`, `REASON_WRONG_PROTOCOL`, `REASON_FRAM`, `REASON_NOT_CAPABLE`, `REASON_ALLOWED`, `split_part_number_tokens()`, `sdp_capability_for_entry()`, `sdp_capability()`.

**New test symbols:** `EXPECTED_ALLOW_PART_NUMBERS`, `EXPECTED_REFUSE_PART_NUMBERS`, `_select_0x0d_chips`, `test_partition_covers_exactly_the_84_0x0d_entries`, `test_allow_and_refuse_token_sets_are_disjoint_and_total`, `test_predicate_agrees_with_the_derived_partition_on_all_84_entries`, `test_synthetic_unknown_0x0d_entry_is_refused_non_vacuous`.

**Requirement IDs this plan may mark Complete:** **none.** HOST-04 spans plans 120-01, 120-05 and 120-09; only 120-09 may tick it. Re-check `.planning/REQUIREMENTS.md` after this plan and confirm HOST-01..HOST-06 all still read Pending.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-01-SUMMARY.md` when done.

The SUMMARY must record: the partition's provenance (minipro commit, `INFOIC2PLUS`, bit 15) and that it **supersedes** RESEARCH § F-01's curated 37/47 and the interim 74/10 placeholder; that the refusal-cost argument changed in our favour — over-refusal costs a working `write` only on a part that *has* SDP, so for the refused set suppressing auto-unlock is a no-op **and** avoids F-120-01's three stored bytes, meaning refusal has no cost for the parts it refuses and the trade-off the operator was asked to weigh is dissolved rather than decided; that residual risk is confined to `120-SDP-PARTITION.md` §4's 9-entry watch-list; and the D-03 mechanism correction (name-keyed with an injected `db`, because `resolve_chip`'s dict has no part number — semantics preserved).
</output>
