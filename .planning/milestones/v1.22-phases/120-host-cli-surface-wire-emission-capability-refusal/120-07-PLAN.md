---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 07
type: execute
wave: 2
depends_on: ["120-02"]
files_modified:
  - firestarter_app/tests/test_revision_constants_parity.py
  - firestarter_app/tests/fixtures/planted_constants_value_drift.h
  - firestarter_app/tests/fixtures/planted_constants_host_missing.h
  - firestarter_app/tests/fixtures/planted_constants_fw_missing.h
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HOST-03]
must_haves:
  truths:
    - "The parity test parses `firestarter/include/firestarter.h` and asserts two-way correspondence for every `#define CMD_*` and `#define FLAG_*` — same value both ways, nothing missing on either side."
    - "Every parsed `CMD_*` is asserted to have both a `constants.py` constant and a `COMMAND_NAMES` entry."
    - "Three planted-violation fixtures each trip exactly one leg, proving value drift, host-missing and firmware-missing are separately detected."
    - "An unreadable or absent header path is an ERROR, never a silent pass."
    - "The set of defines found at preprocessor depth greater than zero equals exactly `{CMD_DEV_ADDRESS, CMD_DEV_REGISTER}`."
  artifacts:
    - "firestarter_app/tests/test_revision_constants_parity.py"
    - "firestarter_app/tests/fixtures/planted_constants_value_drift.h"
    - "firestarter_app/tests/fixtures/planted_constants_host_missing.h"
    - "firestarter_app/tests/fixtures/planted_constants_fw_missing.h"
  key_links:
    - "The parser maps to `/workspaces/firestarter/include/firestarter.h` via the existing module-level `FIRMWARE_HEADER` constant, which doubles as the fixture-injection point."
    - "The exemption pair map maps firmware `CMD_DEV_REGISTER` (singular) to host `COMMAND_DEV_REGISTERS` (plural)."
  prohibitions:
    - "MUST NOT modify any file under /workspaces/firestarter. Firmware tree byte-untouched, tip `0048b3d`. The fixtures are *copies* placed under `tests/fixtures/`, never edits to the real header."
    - "MUST NOT rename `COMMAND_DEV_REGISTERS` to match firmware's singular `CMD_DEV_REGISTER`. It has callers. The mismatch is an enumerated exemption, not a defect — a naive `CMD_X` to `COMMAND_X` map reports a false gap and invites exactly that wrong fix."
    - "MUST NOT skip a define silently. Every `#define CMD_*` and `#define FLAG_*` found must be either mapped or present in the enumerated exemption table; an unrecognised one fails the gate closed. A regex that silently drops `CMD_FRAME_MAX` because its value is a macro rather than a literal is the exact hollowness this rebuild removes."
    - "MUST NOT remove the `FW_ABSENT` skipif from the header-reading legs (D-13). Host-only CI must stay green."
    - "MUST NOT delete or alter `test_revision_byte_values_match_firmware_enum` (host-literals-only, deliberately unskipped) or `test_cmd_frame_max_parity` (`CMD_FRAME_MAX`'s own dedicated gate)."
    - "MUST NOT reuse `check_is_memory_cmd_no_ifdef.py`'s `_find_function_body` / `_predicate_def_pattern`. They brace-match a function body; these are file-scope defines. Reuse `_strip_comments` only."
    - "MUST NOT create a new `tools/` script for this gate. `tools/` is outside CI's ruff scope (`ruff check firestarter/ tests/`), so a new tool would ship un-linted, and D-12/D-13 want one gate with the `FW_ABSENT` skipif retained — a `tools/` script that skips is a contradiction."
---

<objective>
Rebuild `tests/test_revision_constants_parity.py` from a set of hardcoded literals into a real header-parsing two-way gate with `COMMAND_NAMES` coverage, an enumerated exemption pair map, a machine-checked conditional-compilation assertion, a fail-closed path, and three planted-violation fixtures (HOST-03, D-12/D-13). Closes HOST-03.

Purpose: today the file asserts hardcoded literals with the firmware define named in a trailing comment and **never reads the header**. It is 100% hollow with respect to firmware drift — which is precisely why `CMD_SDP_UNLOCK 9` and `CMD_SDP_LOCK 10` landed in Phase 119 unnoticed by it.
Output: a rebuilt gate, three isolated fixtures, and HOST-03 ticked.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-PATTERNS.md
</context>

## The complete two-side inventory (read at plan time; re-derive nothing)

Firmware `CMD_*` in `/workspaces/firestarter/include/firestarter.h`:

| Line | Define | Value | Preprocessor context |
|---|---|---|---|
| 24 | `CMD_FRAME_MAX` | `DATA_BUFFER_SIZE` | file scope; **value is a macro, not a literal** |
| 34 | `CMD_IDLE` | `0` | file scope |
| 35-40 | `CMD_READ` … `CMD_VERIFY` | `1`…`6` | file scope |
| 43 | `CMD_DEV_ADDRESS` | `7` | **inside `#ifdef DEV_TOOLS` (42) … `#endif` (45)** |
| 44 | `CMD_DEV_REGISTER` | `8` | same block |
| 61 | `CMD_SDP_UNLOCK` | `9` | file scope — unconditional by design |
| 62 | `CMD_SDP_LOCK` | `10` | file scope — unconditional by design |
| 64-68 | `CMD_READ_VPP` … `CMD_HW_VERSION` | `11`…`15` | file scope |

Firmware `FLAG_*`: **nine** defines, all file scope, `firestarter.h:131-148` — `FLAG_FORCE 0x01`, `FLAG_CAN_ERASE 0x02`, `FLAG_SKIP_ERASE 0x04`, `FLAG_SKIP_BLANK_CHECK 0x08`, `FLAG_VPE_AS_VPP 0x10`, `FLAG_OUTPUT_ENABLE 0x20`, `FLAG_CHIP_ENABLE 0x40`, `FLAG_VERBOSE 0x80`, `FLAG_SKIP_SDP_UNLOCK 0x100`. A grep over the whole of `include/` and `src/` returns exactly these nine. **There is no `0x200` flag.**

The four enumerated exemptions, as a name **pair** map (never a skip-set):

| Firmware define | Host counterpart | Exemption reason |
|---|---|---|
| `CMD_IDLE` | none | Firmware-internal state; no shipped host path emits cmd 0 (Phase 119 D-01 / RESEARCH F-B2) |
| `CMD_FRAME_MAX` | `CMD_FRAME_MAX` | `CMD_`-prefixed but not a command code, and its value is a macro not a literal; has its own dedicated gate already in this file |
| `CMD_DEV_ADDRESS` | `COMMAND_DEV_ADDRESS` | Conditionally compiled (`#ifdef DEV_TOOLS`) |
| `CMD_DEV_REGISTER` | `COMMAND_DEV_REGISTERS` | Conditionally compiled **and name-mismatched** — singular in firmware, plural on the host |

`_strip_comments` is **load-bearing, not hygiene**, for this gate specifically: `firestarter.h`'s comment block above `CMD_SDP_UNLOCK` (lines 50-60) literally contains the strings `constants.py CMD_SDP_*`, `COMMAND_NAMES` and `#ifdef DEV_TOOLS`, and the block above `FLAG_SKIP_SDP_UNLOCK` (`:141-147`) contains `--skip-sdp-unlock / constants.py`. A scan over uncleaned text matches those comment strings as if they were defines.

<tasks>

<task type="auto">
  <name>Task 1: Create three isolated planted-violation fixture headers</name>
  <files>firestarter_app/tests/fixtures/planted_constants_value_drift.h, firestarter_app/tests/fixtures/planted_constants_host_missing.h, firestarter_app/tests/fixtures/planted_constants_fw_missing.h</files>
  <read_first>
    - `firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h` lines 1-30 — the mandatory ~30-line header-comment convention. It states six things: which pytest owns the fixture; which phase / decision / requirement id it proves; that the file is never compiled and is not in `platformio.ini`; exactly which seam points at it; **which single assertion it trips and which it deliberately does NOT trip** (verbatim rationale: a fixture that failed for two reasons at once could not prove which check fired); and a warning that fixing the file would hollow the gate, naming the anti-hollow contract.
    - `firestarter_app/tests/fixtures/planted_log_in_window.cpp` — the second in-tree example, for register.
    - `/workspaces/firestarter/include/firestarter.h` lines 20-70 and 125-155 — **read-only.** The regions each fixture copies.
  </read_first>
  <action>
Create three files under `firestarter_app/tests/fixtures/`. Each is a **copy** of the relevant regions of `firestarter.h` carrying exactly **one** planted drift, so the failure names one leg. Three separate fixtures rather than one three-drift fixture is required by the isolation convention: a fixture that failed for two reasons at once could not prove which check fired.

`planted_constants_value_drift.h` — copy the `CMD_*` and `FLAG_*` define regions faithfully, then change **one** `CMD_*` value so it disagrees with `constants.py`. Pick a command with no special handling (for example `CMD_VERIFY`, changed from `6` to a different small integer). Everything else must match the real header exactly, including the nine `FLAG_*` defines and both `CMD_SDP_*` defines.

`planted_constants_host_missing.h` — copy the regions faithfully and **add one new** `CMD_*` define with a plausible name that has no counterpart in `constants.py` and is not in the exemption table (for example a `CMD_`-prefixed name at an unused numeric slot). Change no existing value.

`planted_constants_fw_missing.h` — copy the regions faithfully and **delete one** `FLAG_*` define that `constants.py` does have (for example `FLAG_VPE_AS_VPP`). Change no existing value and add nothing.

Each file opens with the six-element block comment in `planted_ifdef_in_predicate.h`'s register, stating: the owning pytest is `tests/test_revision_constants_parity.py`; the ids proved are Phase 120 / HOST-03 / D-12 / D-13; the file is never compiled and is not referenced from `platformio.ini`; the injection seam is a `monkeypatch.setattr` on the module-level `FIRMWARE_HEADER` path constant in the owning pytest; **which single leg it trips** and, explicitly, which legs it deliberately does **not** trip; and the anti-hollow warning that repairing this file to match the real header would hollow the gate it exists to prove.

Do not add these fixtures to any build configuration. Do not edit the real `firestarter.h`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import re,pathlib
d=pathlib.Path('tests/fixtures')
names=['planted_constants_value_drift.h','planted_constants_host_missing.h','planted_constants_fw_missing.h']
for n in names:
    t=(d/n).read_text()
    assert len(t.splitlines())>25, (n,'header comment too short')
    for lit in ['test_revision_constants_parity.py','HOST-03','D-12','FIRMWARE_HEADER']:
        assert lit in t, (n,lit)
print('OK')" && git -C /workspaces/firestarter status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - All three fixture files exist under `firestarter_app/tests/fixtures/`.
    - Each opens with a block comment of at least 25 lines carrying all six required elements, including an explicit statement of which leg it does **not** trip.
    - Each contains the literals `test_revision_constants_parity.py`, `HOST-03`, `D-12` and `FIRMWARE_HEADER`.
    - `planted_constants_value_drift.h` differs from the real header in exactly one `CMD_*` **value** and nothing else structural.
    - `planted_constants_host_missing.h` adds exactly one `CMD_*` define and changes no value.
    - `planted_constants_fw_missing.h` removes exactly one `FLAG_*` define and changes nothing else.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing — the real header is untouched.
    - Neither fixture appears in any `platformio.ini`.
  </acceptance_criteria>
  <done>Three isolated fixtures exist, each provably able to trip exactly one leg of the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the hardcoded literals with a real two-way header-parsing gate</name>
  <files>firestarter_app/tests/test_revision_constants_parity.py</files>
  <read_first>
    - `firestarter_app/tests/test_revision_constants_parity.py` in full — the `FW_ABSENT` guard block and its comment (`:47-58`), the module-level `FIRMWARE_HEADER` path constant, `test_command_values_match_firmware` (`:77-119`) and `test_flag_values_match_firmware` (`:122-145`) which are the hollow literals being replaced, `test_revision_byte_values_match_firmware_enum` (`:61-75`, keep untouched), the `CTRL_*` parity leg (`:183-185`) and `test_cmd_frame_max_parity` (`:188-214`, keep untouched).
    - `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py` lines 85-120 and 155-200 — the fail-closed `FIRESTARTER_*_SRC` seam and its comment (translate the *pattern* to a `monkeypatch.setattr` on `FIRMWARE_HEADER`), the frozen `_EXPECTED_CMD_NAMES` set with its deliberately-not-auto-derived comment, and `_strip_comments` (the length- and line-preserving comment blanker — **reuse this**).
    - `firestarter_app/firestarter/constants.py` lines 50-100 — the `COMMAND_*`, `COMMAND_NAMES` and `FLAG_*` blocks as landed by plan 120-02.
    - `/workspaces/firestarter/include/firestarter.h` lines 20-70 and 125-155 — **read-only**, to confirm the inventory table above for yourself.
  </read_first>
  <action>
Rewrite the two hollow legs in `firestarter_app/tests/test_revision_constants_parity.py`. Keep the file's existing module docstring, the `FW_ABSENT` guard block and its comment, `test_revision_byte_values_match_firmware_enum`, the `CTRL_*` parity leg and `test_cmd_frame_max_parity` — all untouched.

Add module-level helpers:

`_strip_comments(text)` — copy from `check_is_memory_cmd_no_ifdef.py` verbatim. Add a comment stating it is load-bearing here, not hygiene: `firestarter.h`'s comment blocks above `CMD_SDP_UNLOCK` and `FLAG_SKIP_SDP_UNLOCK` literally contain define-shaped strings, so a scan over uncleaned text would match comment prose.

A define-extraction function that, over the comment-stripped text, walks lines while tracking preprocessor nesting depth (increment on `#if`, `#ifdef`, `#ifndef`; decrement on `#endif`; `#else` / `#elif` do not change depth) and yields, for every line matching a file-scope-or-nested `#define` of a name beginning `CMD_` or `FLAG_`, a triple of name, raw value token and depth. **Every match must be yielded** — never filtered by whether its value parses as an integer, because that would silently exempt `CMD_FRAME_MAX`.

A frozen exemption **pair map** `_EXEMPT_FW_TO_HOST` mapping the four firmware names in the table above to their host counterpart or to `None`, with a comment in `_EXPECTED_CMD_NAMES`' register: this table is deliberately **not** auto-derived, because the whole point of the gate is to catch an unreviewed drift rather than mirror it, and adding a fifth exemption must be a deliberate edit to this line. State the `CMD_DEV_REGISTER` / `COMMAND_DEV_REGISTERS` singular-versus-plural mismatch inline, together with the warning that the host name has callers and must not be renamed to satisfy the gate.

A `_host_name(fw_name)` helper: return the exemption map's value when the name is exempt; otherwise map a `CMD_` prefix to `COMMAND_` and return `FLAG_*` names unchanged (the two sides use identical `FLAG_*` names).

Then add these legs, each carrying the existing `@pytest.mark.skipif(FW_ABSENT, ...)`:

`test_every_firmware_cmd_define_maps_two_way_to_constants_py` — for every extracted `CMD_*`: if exempt with a `None` counterpart, assert no host constant of the mapped name exists and continue; if exempt with a counterpart, assert the host constant exists (skip the value comparison only for `CMD_FRAME_MAX`, whose value is a macro and which has its own dedicated gate); otherwise assert a host constant of the mapped name exists **and** its value equals the parsed integer. Then assert the reverse direction: every `COMMAND_*` constant in `constants.py` corresponds to some extracted firmware `CMD_*` under the same mapping — so a host constant with no firmware define also fails. Collect all discrepancies and assert once with a message naming each offending pair and its two values.

`test_every_firmware_flag_define_maps_two_way_to_constants_py` — the same bidirectional check over `FLAG_*`. Assert exactly **nine** firmware `FLAG_*` defines were extracted and exactly nine host `FLAG_*` constants exist, and that the maximum value on both sides is `0x100`. Add a comment that `CTRL_VPP_VPE_DROP_ENABLE = 0x100` is a control-register bit in a different namespace with its own parity leg, and that a `FLAG_*`-scoped extractor never sees it.

`test_every_firmware_cmd_has_a_command_names_entry` — D-13's leg. For every non-exempt extracted `CMD_*`, assert the mapped host constant's value is a key in `COMMAND_NAMES`. Docstring must state that this closes the crash path as well as the value-drift path: `COMMAND_NAMES[cmd]` is dereferenced at `eprom_operations.py:301` **and** `:377`, so a missing entry is a `KeyError` at operation setup, not a cosmetic gap.

`test_conditionally_compiled_defines_are_exactly_the_dev_tools_pair` — assert the set of extracted define names at depth greater than zero equals exactly `{"CMD_DEV_ADDRESS", "CMD_DEV_REGISTER"}`. Docstring: this turns "these two are conditional" from an assumption in a comment into a machine-checked fact, and it is what would have flagged Phase 119's placement of `CMD_SDP_UNLOCK` / `CMD_SDP_LOCK` **outside** the `#ifdef` as a deliberate choice rather than luck.

Update the module docstring's coverage list to describe the new legs, and record D-13's **residual gap** as known-and-explained: in host-only CI `FW_ABSENT` is true, so the whole header-reading gate skips and a host-only PR would not catch a missing `COMMAND_NAMES` entry. D-13 considered splitting `COMMAND_NAMES` into its own always-on test and declined in favour of one gate; note that decision and its cost rather than silently living with it. Also note in-source that `FIRMWARE_HEADER` doubles as the fixture-injection point for Task 3.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_revision_constants_parity.py -q && python3 -m pytest tests/test_revision_constants_parity.py -q -k "maps_two_way or command_names_entry or conditionally_compiled" && ruff check tests/test_revision_constants_parity.py && ruff format --check tests/test_revision_constants_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - The two hollow legs are gone: the file no longer asserts a `CMD_*` value as a bare host literal with the firmware define named only in a trailing comment.
    - The file reads `FIRMWARE_HEADER` and parses it — `_strip_comments` and the depth-tracking extractor both exist.
    - Four new legs exist with the names given above and all pass.
    - The flag leg asserts exactly nine defines on each side and a maximum of `0x100` on each side.
    - The conditional-depth leg asserts set equality with `{"CMD_DEV_ADDRESS", "CMD_DEV_REGISTER"}`.
    - `_EXEMPT_FW_TO_HOST` has exactly four entries and is a name-pair map, not a skip-set.
    - `test_revision_byte_values_match_firmware_enum`, the `CTRL_*` leg and `test_cmd_frame_max_parity` are byte-unchanged (`git diff` shows no edit in those regions).
    - Every header-reading leg carries `@pytest.mark.skipif(FW_ABSENT, ...)`.
    - The module docstring records D-13's residual host-only-CI skip gap as known-and-explained.
    - `ruff check` and `ruff format --check` pass.
  </acceptance_criteria>
  <done>The parity gate reads the firmware header and fails closed on any unmapped define, in either direction, including a missing name entry.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the gate fails — three planted-violation legs, the COMMAND_NAMES delitem leg, and the fail-closed path; then tick HOST-03</name>
  <files>firestarter_app/tests/test_revision_constants_parity.py, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py` lines 45-60 and 115-130 and 295-325 — the fixture-path constant idiom, the paired-test invocation shape, and its two fail-closed legs (a nonexistent path exits 1 with `ERROR:` on stderr; a path with no target exits 1). Translate those to `monkeypatch.setattr` on `FIRMWARE_HEADER` plus `pytest.raises`.
    - `firestarter_app/tests/test_revision_constants_parity.py` as rewritten in Task 2 — the extractor and the four legs the fixtures must trip.
    - `.planning/REQUIREMENTS.md` lines 73-81 and 176-195 — HOST-03's requirement line, its traceability row, and the Coverage bullets.
  </read_first>
  <action>
Extend `firestarter_app/tests/test_revision_constants_parity.py` with the planted-violation and fail-closed legs. None of these carry `FW_ABSENT` skips where they do not need the real header — a fixture-driven leg reads a file inside `tests/fixtures/`, which is always present, so those legs must **not** skip in host-only CI. Say so in a comment; this partially offsets D-13's residual gap.

`test_planted_value_drift_is_detected` — `monkeypatch.setattr` the module's `FIRMWARE_HEADER` to `tests/fixtures/planted_constants_value_drift.h`, then assert the two-way `CMD_*` leg's underlying check raises `AssertionError` and that the message names the drifted define. Factor the check body into a helper the real leg also calls, so the planted leg exercises the same code rather than a parallel reimplementation.

`test_planted_host_missing_define_is_detected` — point `FIRMWARE_HEADER` at `planted_constants_host_missing.h`; assert the check raises and the message names the added firmware define as having no host counterpart. Assert it does **not** report a value drift, proving leg isolation.

`test_planted_firmware_missing_flag_is_detected` — point `FIRMWARE_HEADER` at `planted_constants_fw_missing.h`; assert the reverse-direction check raises and the message names the host `FLAG_*` constant with no firmware define. Assert it does **not** report a value drift.

`test_missing_command_names_entry_is_detected` — take a shallow copy of `COMMAND_NAMES`, `monkeypatch.delitem` one entry (choose one of the two new SDP entries so the leg is directly about this phase's addition), and assert the `COMMAND_NAMES`-coverage check raises naming that command. Docstring: this is the leg that distinguishes a missing **name entry** from a missing **constant** — the crash path from the drift path.

`test_gate_fails_closed_on_an_unreadable_header_path` — point `FIRMWARE_HEADER` at a path that does not exist under `tmp_path` and assert the extractor or the leg raises rather than returning an empty define set and passing vacuously. Docstring must state the failure mode excluded: an unreadable header must be an ERROR, never a silent pass, because an empty define set makes every downstream assertion vacuously true.

Then update `.planning/REQUIREMENTS.md` with scoped `Edit` calls only:
- Tick HOST-03's checkbox and append a short parenthetical recording what closed it: `COMMAND_SDP_UNLOCK 9` / `COMMAND_SDP_LOCK 10` / `FLAG_SKIP_SDP_UNLOCK 0x100` plus two mandatory `COMMAND_NAMES` entries in plan 120-02, and the parity test **rebuilt** in plan 120-07 from hardcoded literals into a real two-way header-parsing gate with three planted-violation fixtures, a `COMMAND_NAMES`-coverage leg, a fail-closed path and a machine-checked conditional-compilation assertion. Also record the honest reading of HOST-03's "same commit pair" wording: firmware landed the defines in **Phase 119** and the host lands them here, deliberately, because HOST-06 forbids the reverse order — what the requirement demands and what is now proven is that the pair *agrees*, machine-checked in both directions.
- Update HOST-03's traceability-table row from `Pending` to `Complete`.
- Do **not** change HOST-03's requirement wording, and do **not** touch any other requirement's checkbox, row, or the Coverage bullets' arithmetic (the `36` total is unchanged by this plan).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_revision_constants_parity.py -q && python3 -m pytest tests/test_revision_constants_parity.py -q -k "planted or fails_closed or missing_command_names" && cd /workspaces && python3 -c "import re; t=open('.planning/REQUIREMENTS.md').read(); m=re.search(r'- \[(.)\] \*\*HOST-03\*\*', t); assert m and m.group(1)=='x', m; assert re.search(r'\| HOST-03 \| Phase 120 \| Complete', t); assert re.search(r'- \[ \] \*\*HOST-01\*\*', t) and re.search(r'- \[ \] \*\*HOST-02\*\*', t) and re.search(r'- \[ \] \*\*HOST-04\*\*', t) and re.search(r'- \[ \] \*\*HOST-05\*\*', t) and re.search(r'- \[ \] \*\*HOST-06\*\*', t); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Five new legs exist with the names given above and all pass.
    - The three planted legs each assert the expected failure **and** assert the absence of the other failure kind, proving leg isolation.
    - The planted and fail-closed legs do **not** carry a `FW_ABSENT` skip — they read files under `tests/fixtures/` or `tmp_path`, which are always present.
    - The planted legs call the **same** helper the real legs call, not a parallel reimplementation.
    - `test_gate_fails_closed_on_an_unreadable_header_path` proves an absent path raises rather than yielding an empty define set.
    - `.planning/REQUIREMENTS.md`: HOST-03's checkbox is `[x]`; its traceability row reads `Complete`; HOST-01, HOST-02, HOST-04, HOST-05 and HOST-06 checkboxes all still read `[ ]`.
    - HOST-03's requirement **wording** is unchanged; the appended text is a parenthetical only; the Coverage bullets' `36` total is unchanged.
    - `git diff --stat -- .planning/REQUIREMENTS.md` shows a scoped edit, not a full-file replacement.
    - `ruff check tests/` and `ruff format --check tests/` pass.
  </acceptance_criteria>
  <done>Every leg of the rebuilt gate is proven able to fail, the crash path is distinguished from the drift path, and HOST-03 is closed with an honest record of what "same commit pair" actually means here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware header source text → host constants | A cross-repo textual boundary. The header is the authority; the host mirror is what reaches the wire. Drift here silently drives the wrong physical operation. |
| fixture path seam → gate input | The injection seam is how the gate's own failure mode is proven. A seam that fails open makes the gate vacuous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-22 | Tampering | Host and firmware disagreeing on a `cmd` or `flags` value with no gate that would notice | high | mitigate | Bidirectional value comparison against the parsed header, with three planted fixtures proving each detection direction fires. |
| T-120-23 | Repudiation | A gate that passes vacuously because the header path was unreadable and the define set was empty | high | mitigate | `test_gate_fails_closed_on_an_unreadable_header_path`; the extractor raises rather than returning empty. |
| T-120-24 | Denial of Service | A missing `COMMAND_NAMES` entry crashing operation setup with a `KeyError` | medium | mitigate | The `COMMAND_NAMES`-coverage leg plus the `monkeypatch.delitem` planted leg. |
| T-120-25 | Tampering | A silent exemption — a regex dropping `CMD_FRAME_MAX` because its value is a macro | medium | mitigate | Every match is yielded; unmapped names fail closed; exemptions are an enumerated four-entry pair map. |
| T-120-26 | Spoofing | "Fixing" the false `COMMAND_DEV_REGISTER` gap by renaming the host constant that has callers | medium | mitigate | The mismatch is an enumerated exemption with an inline warning; the prohibition is stated in this plan's front matter. |
| — | Tampering (supply chain) | Package installs | n/a | accept | Zero packages installed in this plan. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_revision_constants_parity.py -q` — all legs green, including the three planted ones.
- `python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_sdp_table_parity.py -q` — the sibling firmware-source-scanning gates, unaffected but confirmed rather than assumed.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` — CI-scoped form only. Note the three new `.h` fixtures are not Python and are outside ruff's reach entirely.
- `python3 tools/check_mypy_watermark.py` — reported error count stays at **1**.
- `git -C /workspaces/firestarter status --porcelain` empty; tip `0048b3d`. This is also the honest, non-vacuous proof for PROJECT.md FOURTH CORRECTION item 5's concern: a path-scoped `git diff` against a shorthand path can pass vacuously, but an empty `status --porcelain` subsumes every path and cannot.
- `git -C /workspaces/firestarter_app diff --stat -- firestarter/` empty — this plan touches only `tests/` in the app repo.
- Known-RED baselines named, not tolerated: `test_audit_coverage_matrix.py::test_golden_file_matches`; `test_no_programmer_found_*` with a board attached.
</verification>

<success_criteria>
- The parity test parses the real header and asserts two-way `CMD_*` and `FLAG_*` correspondence plus `COMMAND_NAMES` coverage.
- Exemptions are a frozen four-entry name-pair map; nothing is silently skipped.
- Conditional compilation is machine-checked, not assumed in a comment.
- Three isolated planted fixtures plus a `delitem` leg plus a fail-closed leg all prove the gate can fail.
- HOST-03 is ticked; HOST-01, HOST-02, HOST-04, HOST-05 and HOST-06 all still read Pending.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New files:** `firestarter_app/tests/fixtures/planted_constants_value_drift.h`, `firestarter_app/tests/fixtures/planted_constants_host_missing.h`, `firestarter_app/tests/fixtures/planted_constants_fw_missing.h`.

**New test symbols:** `_strip_comments`, the depth-tracking define extractor, `_EXEMPT_FW_TO_HOST`, `_host_name`, `test_every_firmware_cmd_define_maps_two_way_to_constants_py`, `test_every_firmware_flag_define_maps_two_way_to_constants_py`, `test_every_firmware_cmd_has_a_command_names_entry`, `test_conditionally_compiled_defines_are_exactly_the_dev_tools_pair`, `test_planted_value_drift_is_detected`, `test_planted_host_missing_define_is_detected`, `test_planted_firmware_missing_flag_is_detected`, `test_missing_command_names_entry_is_detected`, `test_gate_fails_closed_on_an_unreadable_header_path`.

**Requirement IDs this plan may mark Complete:** **HOST-03 only.** Do not touch HOST-01, HOST-02, HOST-04, HOST-05 or HOST-06. Re-check `.planning/REQUIREMENTS.md` after this plan.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-07-SUMMARY.md` when done.

The SUMMARY must record: that the old gate was **100% hollow** with respect to firmware drift, which is why `CMD_SDP_UNLOCK 9` / `CMD_SDP_LOCK 10` landed in Phase 119 unnoticed by it; the four enumerated exemptions and the `CMD_DEV_REGISTER` / `COMMAND_DEV_REGISTERS` singular-plural mismatch that a naive name map would misreport; that three isolated fixtures were chosen over one three-drift fixture because a fixture failing for two reasons could not prove which check fired; D-13's residual host-only-CI skip gap, recorded as known-and-explained, and the partial offset that the fixture-driven legs do **not** skip; the honest reading of HOST-03's "same commit pair" wording — firmware landed in Phase 119 and the host lands here deliberately, because HOST-06 forbids the reverse order, and what is proven is that the pair *agrees* bidirectionally; and that **row 7** of the nine-row CORRECTION-4 gate table is the one row this phase deliberately **changes**, so "unchanged" is the wrong verdict for it in the final sweep.
</output>
