---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md
  - .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-WATCHLIST.md
  - .planning/notes/infoic-xml-protection-flags-research.md
autonomous: true
requirements: [HOST-04]
must_haves:
  truths:
    - "`120-VALIDATION.md`'s HOST-04 rows state the derived 43 / 41 split; no row still states the superseded 74/10 or 37/47 figures."
    - "A named 9-entry residual-risk watch-list exists on disk, so a future bench report against any of those parts is recognised rather than re-investigated from scratch."
    - "`.planning/notes/infoic-xml-protection-flags-research.md` carries a scoped exception that leaves its 2026-07-10 negative verdict intact."
  artifacts:
    - ".planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-WATCHLIST.md"
  key_links:
    - "`120-WATCHLIST.md` maps to `120-SDP-PARTITION.md` §4's bit-15-clear-but-paged table."
    - "The scoped exception maps to the note's own protection-kind / status-readable taxonomy verdict, which is NOT overturned."
  prohibitions:
    - "MUST NOT touch either sub-repo. This plan is meta-repo only; no submodule gitlink may be staged, and both sub-repo working trees must be no worse than at plan start."
    - "MUST NOT delete, weaken, or overturn the note's verdict that bits 14/15 are too coarse to derive lock-status metadata and should not be re-investigated. That verdict is about the protection-kind / status-readable / unlockability taxonomy and remains true. Append a scoped exception; change no existing sentence."
    - "MUST NOT edit doc/lockable-proms.md in either sub-repo. Its section 17 is wrong about AT28C16 — record the finding here; correcting the doc is GATE-02, Phase 121."
    - "MUST NOT edit .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md or .planning/STATE.md. Those belong to plan 120-11 (D-20) and to the closing plans."
    - "MUST NOT edit 120-RESEARCH.md, 120-CONTEXT.md, 120-PATTERNS.md, 120-SDP-PARTITION.md, 120-sdp-partition.json or 120-derive-sdp-allowset.py. Superseded content is corrected by a superseding artifact, never by rewriting the record."
    - "MUST NOT mark HOST-04 Complete. Only plan 120-09 may tick it."
---

<objective>
Correct the validation contract to the derived 43/41 split and put the derivation's residual risk and scoped-exception findings on disk where a future reader will find them (HOST-04 record-keeping).

Purpose: `120-VALIDATION.md` was written against the interim 74/10 figure and RESEARCH F-01's 37/47 — both superseded by `120-SDP-PARTITION.md`. The derivation also produced three findings that must be recorded rather than acted on: a 9-entry residual-risk watch-list, a scoped exception to a standing do-not-re-investigate note, and an in-tree doc error whose correction belongs to Phase 121.
Output: a corrected `120-VALIDATION.md`, a new `120-WATCHLIST.md`, and an appended scoped exception on the standing note.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md
@.planning/notes/infoic-xml-protection-flags-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct 120-VALIDATION.md's HOST-04 rows to the derived 43/41 split and widen four oracle legs</name>
  <files>.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md` in full — the Requirement to Oracle Map (its HOST-04 partition row carries the superseded interim figure), the Wave 0 Requirements list, and the Planted-Violation Fixtures table.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` section 8, Validation consequences — the exact list of what changes and the legs that gain reach or are newly worth adding.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-01-PLAN.md` and `120-05-PLAN.md` — the actual test names and pytest `-k` selectors the corrected rows must point at, so every row stays reachable by a real command.
  </read_first>
  <action>
Use scoped `Edit` calls on `120-VALIDATION.md`. Do not rewrite the file wholesale.

Rewrite the HOST-04 partition row so it states the derived split as forty-three ALLOW and forty-one REFUSE over the 84 `algorithm == 13` entries. In the same row or an immediately adjacent note, state the full arithmetic the gate pins: 84 manufacturer-plus-part-number pairs; 43 ALLOW pairs and 41 REFUSE pairs; 81 distinct `part_number` strings split 40 and 41; 134 total alias-token instances; 130 distinct uppercased tokens split 65 and 65 with an empty intersection. Add a parenthetical recording that this supersedes both the interim operator placeholder figure and RESEARCH F-01's curated figure, and pointing the reader at `120-SDP-PARTITION.md` as authoritative.

Widen the named-refusals row: it can now assert that all nineteen `DIP24_2816` parts refuse and that both FRAM parts refuse with the FRAM-specific reason, not merely HOST-04's eight named entries.

Make the adapter-required row exhaustive: all nine `adapter-required` `0x0D` parts must hear the capability reason, so D-08's capability-before-support-status ordering is load-bearing on every adapter-required part in the bucket rather than on a hypothetical subset.

Add one new row for the two derived structural invariants — the allow-set contains no `adapter-required` part and no part on pinout `DIP24_2816`. Note that these hold on the derived partition and exist to catch a careless hand-widening of the table.

Update each corrected row's Automated Command column to the real pytest `-k` selector taken from plans 120-01 and 120-05, and update its File Exists column to record that `tests/test_sdp_capability.py` is created by plan 120-01.

Leave every other row, the Test Infrastructure table, the Sampling Rate section, the Manual-Only table and the Validation Ceiling section untouched. Do not change the front-matter `status` or `nyquist_compliant` values — those belong to the final-sweep plan.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/120-host-cli-surface-wire-emission-capability-refusal && python3 -c "import re; t=open('120-VALIDATION.md').read(); body=''.join(l for l in t.splitlines(True) if not l.lstrip().startswith(chr(62))); bad=re.findall(r'74\s*/\s*10|37\s*/\s*47', body); assert not bad, bad; assert re.search(r'43\s*/\s*41', body); assert 'DIP24_2816' in body; assert 'adapter-required' in body; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c` check above passes: no `74 / 10` and no `37 / 47` remains outside blockquote lines, and a `43 / 41` figure is present.
    - The HOST-04 partition row states all six pinned numbers: 84, 43, 41, 81, 134, 130.
    - The named-refusals row names both nineteen `DIP24_2816` parts and both FRAM parts.
    - The adapter-required row says all nine.
    - A structural-invariants row exists naming both invariants (no `adapter-required` in the allow-set, no `DIP24_2816` in the allow-set).
    - Every corrected row's Automated Command is a real pytest invocation whose `-k` selector matches a test name defined in plan 120-01 or 120-05.
    - `git diff --stat -- .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md` shows a scoped edit, not a full-file replacement, and no other file in that directory is modified.
  </acceptance_criteria>
  <done>The validation contract's HOST-04 half states the derived partition and its oracles reach every derived invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Write 120-WATCHLIST.md naming the nine residual-risk entries and the two recorded-not-acted-on findings</name>
  <files>.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-WATCHLIST.md</files>
  <read_first>
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` sections 4, 5 and 6 in full — the disagreement table, the residual-risk paragraph, the MIXED-verdict resolution, and the list of what is superseded.
    - `.planning/REQUIREMENTS.md` section "Validation Ceiling" — the exact permitted and forbidden claims, and the line listing the capability partition among the things not provable this milestone.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-RESEARCH.md` section F-17 — the alias-group split finding this derivation answers.
  </read_first>
  <action>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-WATCHLIST.md`.

Section 1, "Why this file exists": the derived partition's axis is `infoic.xml` flags bit 15, and bit 15 is not a page-write proxy — it disagrees with a page size greater than one on twelve of the eighty-four entries. Eleven of those twelve have bit 15 clear yet a page size above one; two of the eleven are the FRAM parts, where bit 15 clear is the correct answer and the page-size figure is a buffer size rather than an EEPROM page. The remaining **nine** are the entries where "no SDP" is least intuitive: an early part with page-write but no software protect. State that these nine are the only place a wrong bit-15 value would cost a real `write` regression under D-04's auto-set, and that naming them here is so a future bench report against any of them is recognised immediately rather than re-investigated from scratch.

Section 2, the watch-list table. One row per entry, with manufacturer, `part_number`, the `page_size` value from `120-SDP-PARTITION.md` section 4, and its pinout. The nine entries, transcribed exactly: AMD `AM28C64A,AM28C64AE,AM28C64B,AM28C64BE`; ATMEL `AT28PC64,AT28PC64E`; CATALYST(CSI) `CAT28C64A,CAT28C65`; EXEL `XLE2865A,XLS2865A`; EXEL `XLE28C16B,XLS28C16B`; EXEL `XLE28C64A,XLS28C64A`; NEC `UPD28C64`; XICOR `X2816B,X2816C`; XICOR `X2864AP`. Add a column stating what a contradicting bench report would look like: a user reporting that `dev sdp <part> enable` or `disable` is refused on a part they believe has SDP, or a `write` that regressed against `3.0.0b11` on one of these nine.

Section 3, "What a bench report against a watch-list entry means, and what it does not": a credible report is evidence that this one entry's bit-15 value is wrong for that part, not evidence that the axis is wrong. The remedy is to move that entry between the two sets in `120-sdp-partition.json`, the production allow-list and the test-side expected partition together — never to widen the allow-list by default and never to relax the unanimity rule.

Section 4, "Two findings recorded here and deliberately not acted on":
- `doc/lockable-proms.md` section 17 is **wrong about AT28C16**. It lists "Atmel AT28C16 / 64 / 256" as SDP-capable, but `AT28C16`, `AT28C16E,AT28C16F` and plain `AT28C64` all carry bit 15 clear, a page size of one, and byte-write. State that correcting that doc is **GATE-02, Phase 121**, and that this phase records the finding and changes no doc. Name the two Atmel entries the split turns on so the Phase 121 author does not have to re-derive them.
- RESEARCH F-17's "the DB splits alias groups and we cannot see why" is **answered**. `chip_database.json`'s split mirrors `infoic.xml`'s own split: `AT28C64` with flags `0x10`, byte-write and no software protect, versus `AT28C64B` with flags `0xc010`, a 64-byte page and software protect. Therefore F-02 rule 1's "do not strip parentheticals" is now load-bearing on **correctness**, not merely on stability — stripping `(Non-Standard)` collapses that token onto the separate `AT28C64B` entry and produces a spurious MIXED verdict.

Section 5, "Ceiling, restated": `REQUIREMENTS.md`'s Validation Ceiling still lists "that the curated capability partition is correct per family" among the things not provable this milestone, and that remains true. This derivation makes the partition **derived and reproducible**, not **bench-verified**. No AT28C part is on the bench. The gate proves the partition is total and stable; `infoic.xml` raises confidence in its contents; neither proves it right on silicon. `0x0D` stays `UNVERIFIED`; zero `support_status` changes; the 84-chip count is unchanged.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/120-host-cli-surface-wire-emission-capability-refusal && python3 -c "
t=open('120-WATCHLIST.md').read()
need=['AM28C64A,AM28C64AE,AM28C64B,AM28C64BE','AT28PC64,AT28PC64E','CAT28C64A,CAT28C65','XLE2865A,XLS2865A','XLE28C16B,XLS28C16B','XLE28C64A,XLS28C64A','UPD28C64','X2816B,X2816C','X2864AP','GATE-02','AT28C16','F-17','Validation Ceiling']
missing=[n for n in need if n not in t]
assert not missing, missing
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `120-WATCHLIST.md` exists and the `python3 -c` check above passes — all nine `part_number` strings are present verbatim, plus the literals `GATE-02`, `AT28C16`, `F-17` and `Validation Ceiling`.
    - The watch-list table has exactly nine data rows.
    - Section 3 states that the remedy is to move an entry between the two sets in all three places together, and never to widen the allow-list by default.
    - Section 4 states that `doc/lockable-proms.md` section 17 is wrong about `AT28C16` and that the correction is GATE-02 / Phase 121, and that no doc is changed by this phase.
    - Section 5 quotes or paraphrases the ceiling's "correct per family" exclusion and states that this derivation is reproducible, not bench-verified.
    - No file under `/workspaces/firestarter` or `/workspaces/firestarter_app` was modified.
  </acceptance_criteria>
  <done>The derivation's residual risk has a named, reviewable home, and two findings are recorded without acting on either.</done>
</task>

<task type="auto">
  <name>Task 3: Append a scoped exception to the 2026-07-10 infoic-flags note without overturning its verdict</name>
  <files>.planning/notes/infoic-xml-protection-flags-research.md</files>
  <read_first>
    - `.planning/notes/infoic-xml-protection-flags-research.md` in full — especially its Conclusions section (`status_readable` is not derivable; absence of bits does not mean no protection; bit 15 hedged as roughly an SDP page-write family marker, "heuristics only"), its Consequence section, and its do-not-re-investigate framing.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` section 6, "What this supersedes, and what it does not" — the exact scoping argument to reproduce.
  </read_first>
  <action>
Append a single new section to the **end** of `.planning/notes/infoic-xml-protection-flags-research.md` using `Edit` (append-only). Change no existing sentence, heading, table row or conclusion in the file.

Title it as a scoped exception dated 2026-07-29 and attribute it to Phase 120 / HOST-04.

Content, in this order:

1. **What is unchanged.** This note's verdict stands: bits 14 and 15 are too coarse to derive lock-status metadata, `status_readable` is not derivable, and the taxonomy question should not be re-investigated. `W29C020C` with its permanent boot-block lockout and `W29EE011` with SDP only are still flag-identical, so readability is still not derivable. **Neither of those parts is in the `0x0D` bucket.**

2. **The narrower question Phase 120 asked.** Not "what kind of protection does this part have and can its state be read" but only "does this 28C-family protocol-`0x0D` part have an SDP command decoder at all". Bit 15 was tested against that single question.

3. **Three independent probes, all passing.** HOST-04's eight named pre-SDP entries plus identical-generation second sources: bit 15 clear on all eight, six of them with flags exactly zero. Both FRAM parts, a different memory technology with no EEPROM command decoder: bit 15 clear on both, both with flags exactly zero. The four datasheet-of-record Atmel parts (`AT28C256`, `AT28C64B`, `AT28C010`, `AT28C040`): bit 15 set on all four. No probe failed and nothing needed a special case.

4. **Result and where it lives.** All 84 protocol-`0x0D` entries matched; zero unmatched, zero MIXED under exact-token keying; ALLOW 43 / REFUSE 41. Point at `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-SDP-PARTITION.md` and `120-sdp-partition.json`.

5. **The hedge this sharpens.** This note called bit 15 "roughly an SDP page-write family marker". Tested directly, bit 15 and a page size greater than one **disagree on twelve of the eighty-four**, so bit 15 is not a page-write proxy — it carries information page size does not, which is what makes it usable as its own signal. Point at `120-WATCHLIST.md` for the nine residual-risk entries.

6. **Both findings are correct about different questions.** State this explicitly, so a future reader treats neither as overturned. State that nothing reads `infoic.xml` at runtime or in CI, that `infoic.xml` is not committed to either sub-repo, and that the shipped artifact is a static transcribed token table.

7. **The loose ends stay loose.** The note's bit 22 and bit 9 observations, and its routing of decodable bits 14/15 work to the pending `decode-infoic-flags-bits-14-15-protect-metadata` todo, are unaffected. Decoding those bits into the DB proper would later make the Phase 120 table generated rather than transcribed — narrowing the curation, not removing the need for a partition.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/check_note_append_only.py</automated>
  </verify>
  <acceptance_criteria>
    - `git show HEAD:.planning/notes/infoic-xml-protection-flags-research.md` is a **prefix** of the working-tree file — the edit is provably append-only and no existing sentence changed. Assert this with a small throwaway python check (write it under the phase directory as `check_note_append_only.py`, or run it inline with `python3 -c`), not by eyeballing a diff.
    - The appended section contains the literals `2026-07-29`, `Phase 120`, `120-SDP-PARTITION.md`, `120-WATCHLIST.md` and `W29EE011`.
    - The appended section states that the note's existing verdict **stands** and is scoped to the protection-kind / status-readable taxonomy.
    - The appended section states that neither `W29C020C` nor `W29EE011` is a `0x0D` part.
    - The appended section names all three ground-truth probes with their pass counts (8/8, 2/2, 4/4).
    - The appended section states the twelve-of-eighty-four disagreement between bit 15 and a page size greater than one.
    - No existing heading, table row or conclusion line in the note was modified or deleted.
    - No file under `/workspaces/firestarter` or `/workspaces/firestarter_app` was modified.
  </acceptance_criteria>
  <done>The standing negative verdict survives intact, with a dated scoped exception recording that a strictly narrower question was answered against the same axis and passed three independent probes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future planner | These artifacts are the only durable carrier of why the allow-set contains what it contains. A wrong or missing record causes a future phase to re-guess or to overturn a still-true finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-11 | Repudiation | A validation contract still pinning a superseded split, so the gate is judged against the wrong number | medium | mitigate | Task 1 corrects every HOST-04 row and names what it supersedes. |
| T-120-12 | Tampering (record) | Overturning a standing, still-true negative verdict while recording a narrower positive one | medium | mitigate | Append-only edit, machine-checked by the prefix assertion; the scoped-exception text states explicitly that both findings are correct about different questions. |
| T-120-13 | Repudiation | A future bench report against a watch-list entry being re-investigated from scratch, or read as invalidating the whole axis | low | mitigate | `120-WATCHLIST.md` names the nine entries, the shape of a contradicting report, and the bounded remedy. |
| — | Tampering (supply chain) | Package installs | n/a | accept | Zero packages installed in this plan. |
</threat_model>

<verification>
- Both sub-repo working trees unchanged by this plan: `git -C /workspaces/firestarter status --porcelain` empty and tip `0048b3d`; `git -C /workspaces/firestarter_app status --porcelain` no worse than at plan start.
- The meta-repo commit stages only the three `.planning/` paths this plan owns plus the throwaway checker if it is kept; no submodule gitlink is staged (`git diff --cached --stat` shows no `firestarter` or `firestarter_app` entry).
- The append-only prefix assertion on the note passes.
- `120-VALIDATION.md` carries no superseded split figure outside blockquote lines.
</verification>

<success_criteria>
- `120-VALIDATION.md`'s HOST-04 rows state 43/41 with the full pinned arithmetic and four widened/added oracle legs.
- `120-WATCHLIST.md` exists with the nine entries verbatim, the bounded remedy, and the two recorded-not-acted-on findings.
- The standing infoic note has a dated scoped exception and is otherwise byte-identical to its committed form.
- HOST-04 is **not** ticked by this plan.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New files:** `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-WATCHLIST.md`.

**Modified files:** `120-VALIDATION.md` (scoped edits), `.planning/notes/infoic-xml-protection-flags-research.md` (append-only).

**New symbols:** none — this plan produces no code.

**Requirement IDs this plan may mark Complete:** **none.** HOST-04 spans plans 120-01, 120-05 and 120-09; only 120-09 may tick it. Re-check `.planning/REQUIREMENTS.md` after this plan.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-04-SUMMARY.md` when done.

The SUMMARY must record: that `120-VALIDATION.md`'s HOST-04 rows were corrected from the superseded interim and curated figures to the derived 43/41; that the nine-entry residual-risk watch-list is now named on disk; that the standing 2026-07-10 infoic note was **not** overturned but given a dated scoped exception, and why both findings are correct about different questions; that `doc/lockable-proms.md` section 17's `AT28C16` error is recorded and its correction is GATE-02 / Phase 121 with no doc changed here; and that RESEARCH F-17's alias-group split question is answered by `infoic.xml`'s own split, making the do-not-strip-parentheticals rule load-bearing on correctness rather than only on stability.
</output>
