---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 12
type: execute
wave: 7
depends_on: ["120-11"]
files_modified:
  - .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md
  - .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md
  - firestarter_app/tests/test_submit.py
autonomous: true
requirements: [HOST-01, HOST-02, HOST-03, HOST-04, HOST-05, HOST-06]
must_haves:
  truths:
    - "All nine CORRECTION-4 cross-repo gate rows are re-run at the phase's final commit and their verdicts recorded — row 7 expected CHANGED, row 9 expected unchanged-but-at-risk."
    - "The firmware sub-repo is byte-untouched: `git -C /workspaces/firestarter status --porcelain` empty and tip still `0048b3d`."
    - "The shipped DB, the generated catalog mirror and `build_db.py` are untouched, and no `support_status` value changed."
    - "`submit.SUBMIT_REPO` is `henols/firestarter_prom` and is pinned by an existing test; the wrong-repo behaviour in `3.0.0b11` is a released-artifact fact, not a source defect."
    - "The full host suite is green except the single named pre-existing failure."
  artifacts:
    - ".planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md"
  key_links:
    - "The nine-row table is inherited from `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` §5 and is mandatory in every phase from 118 on (PROJECT.md FOURTH CORRECTION item 4)."
    - "`120-NONREGRESSION.md` follows `118-NONREGRESSION.md`'s and `119-NONREGRESSION.md`'s eight-section shape."
  prohibitions:
    - "MUST NOT modify any file under /workspaces/firestarter. Firmware tree byte-untouched, tip `0048b3d`, `version.h` still `3.0.0b11`."
    - "MUST NOT change `firestarter/submit.py`. `SUBMIT_REPO` is already correct on this branch (commit `e615b4c`) and on `beta`. This plan verifies and records; it does not re-fix."
    - "MUST NOT tick, un-tick or re-word any requirement. All six HOST ids were closed by plans 120-07 through 120-10; this plan only verifies they read `[x]`."
    - "MUST NOT mark a gate row PASS on the strength of a path-scoped `git diff` against a path that may not exist. PROJECT.md FOURTH CORRECTION item 5's `flash_utils.{h,cpp}` warning: a vacuously-passing row must not be mistaken for a real one. Use `git -C /workspaces/firestarter status --porcelain` being empty, which subsumes every path."
    - "MUST NOT record row 7 as unchanged. Plan 120-07 deliberately rebuilt `tests/test_revision_constants_parity.py`, so 'unchanged' is the wrong verdict for that row — record its new expected shape and its re-run result."
    - "MUST NOT silently tolerate either known-RED baseline. Name `tests/test_audit_coverage_matrix.py::test_golden_file_matches` and `tests/test_no_programmer_found_*` explicitly, with their causes, and state whether each reproduced."
    - "MUST NOT make any claim that SDP works on real AT28C silicon, or any claim about actual protection state. Nothing in this phase is evidence about AT28C silicon."
    - "MUST NOT change any chip's `support_status`, the 84-chip count, or `0x0D`'s `UNVERIFIED` status in `PROTOCOL-LEDGER`."
    - "MUST NOT run a bare `ruff check .` or a bare `mypy` as the gate. Both are RED at baseline for reasons unrelated to this phase — four `tools/` files are lint-dirty outside CI's scope, and `mypy` over the strict-8 list reports one pre-existing error."
---

<objective>
Re-run the mandatory nine-row cross-repo gate checklist at the phase's final commit, verify the two frozen-artifact fences, discharge the operator's `dev test --submit` repo-target ask as a verification rather than a code change, and write `120-NONREGRESSION.md` (all six HOST ids — verified, none newly marked).

Purpose: PROJECT.md FOURTH CORRECTION item 4 makes this checklist mandatory in every phase from 118 on. Phase 117 broke four host-side gates because **no plan owned that step**; Phase 118 fixed the process by giving a named plan every one of them and it worked. This phase edits no firmware, so the risk runs the other way — confirm rather than assume.
Output: `120-NONREGRESSION.md`, a settled `120-VALIDATION.md`, and a recorded `submit.py` verification.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md
</context>

## The nine rows, with their exact commands (run from `/workspaces/firestarter_app`)

| # | Gate | Command | Expected |
|---|------|---------|----------|
| 1 | `check_no_log_in_sdp_window.py` (HIGH-risk row) | `python3 tools/check_no_log_in_sdp_window.py` | PASS, exit 0 |
| 2 | its paired pytest | `python3 -m pytest tests/test_check_no_log_in_sdp_window.py -q` | PASS |
| 3 | `test_sdp_table_parity.py` (broken 3× by Phase 117) | `python3 -m pytest tests/test_sdp_table_parity.py -q` | PASS |
| 4 | `check_is_memory_cmd_no_ifdef.py` + pytest + fixture | `python3 tools/check_is_memory_cmd_no_ifdef.py` then `python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py -q` | PASS, exit 0 |
| 5 | `gen_sdp_bus_config.py` generator idempotence | `python3 tools/gen_sdp_bus_config.py` then `git -C /workspaces/firestarter status --short` | PASS, firmware status **empty** afterward |
| 6 | `test_sdp_bus_config_drift.py` | `python3 -m pytest tests/test_sdp_bus_config_drift.py -q` | PASS |
| 7 | `test_revision_constants_parity.py` | `python3 -m pytest tests/test_revision_constants_parity.py -q` | **CHANGED by this phase** — rebuilt in plan 120-07. Record the new shape and the result; "unchanged" is the wrong verdict |
| 8 | `test_dispatch_mirror.py` | `python3 -m pytest tests/test_dispatch_mirror.py -q` | PASS |
| 9 | `check_dispatch.py` + `check_devtest_orchestrator.py` (+ its pytest) | `python3 tools/check_dispatch.py` then `python3 tools/check_devtest_orchestrator.py` then `python3 -m pytest tests/test_check_devtest_orchestrator.py -q` | PASS — **the one host-side row at real risk**: `check_devtest_orchestrator.py` asserts "firmware untouched (host-only, asserted)" over `chip_test.py`, `cli_handlers.py` and `submit.py`, and this phase edits `cli_handlers.py` in plans 120-08 and 120-09 |

Two caveats carried from Phase 119: row 5's `_shared/sdp_expected.h` whole-file blob-SHA shorthand is **retired** and must not be reached for. And `.github/workflows/catalog-sync-check.yml` cannot go green for v1.22 catalog work because it checks out both sub-repos at `ref: main` — that red job is **known-and-expected**, not this phase's damage.

<tasks>

<task type="auto">
  <name>Task 1: Re-run all nine gate rows plus the full suite and the CI-scoped tooling gate at the phase's final commit</name>
  <files>.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` in full — its eight-section shape, its §5 nine-row table, and its §4 per-board flash/RAM figures. This is the template.
    - `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md` — the earlier instance of the same shape, for the register of the known-and-explained-conditions section.
    - `.planning/PROJECT.md` FOURTH CORRECTION items 4 and 5 — the mandate and the vacuous-path warning.
    - `firestarter_app/.github/workflows/ci.yml` — the exact CI ruff/format scope and the mypy gate invocation, so the recorded commands match what CI actually runs.
  </read_first>
  <action>
Run every command in the nine-row table above from `/workspaces/firestarter_app`, at the phase's final commit, and capture each verdict verbatim. Do **not** trust any prior plan's SUMMARY for a row's status; re-run it.

Then run, and capture:
- Full host suite: `python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`.
- CI-scoped lint: `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/`. Record explicitly that the CI scope is `firestarter/ tests/` and **not** `.`, and that a bare `ruff check .` reports four pre-existing failures in `tools/` outside CI's scope — so a bare run would send a reader chasing unrelated failures.
- Type gate: `python3 tools/check_mypy_watermark.py`. Record the reported error count and assert it is still **1** (the pre-existing `firestarter/submit.py:446` assignment error). Record that the watermark is 35 with 34 slack, so a bare pass is **not** evidence — a new type error in `cli_handlers.py` or `serial_comm.py` would pass CI silently, and both were edited by this phase.
- The two frozen-artifact fences: `git -C /workspaces/firestarter status --porcelain` (must print nothing) and `git -C /workspaces/firestarter rev-parse --short HEAD` (must be `0048b3d`); and `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/ firestarter/messages.py tools/build_db.py tools/catalog/` (must be empty). Also confirm `firestarter/include/version.h` still reads `3.0.0b11`.
- The derived structural invariants as a final cross-check: `python3 -m pytest tests/test_sdp_capability.py tests/test_dev_sdp_cmd.py tests/test_write_skip_sdp_unlock.py -q`.

For each of the two known-RED baselines, record whether it **reproduced** in this run and its cause, never a pre-excuse: `tests/test_audit_coverage_matrix.py::test_golden_file_matches` is a stale golden (research measured 186034 bytes produced against a 184631-byte golden, first diff at index 1178) and needs a dedicated golden regen, which is not this phase's work; `tests/test_no_programmer_found_*` go RED with a board attached (`/dev/ttyACM0`, `/dev/ttyACM1`, `/dev/ttyUSB0` are present) and did **not** reproduce during research — state which behaviour was observed this time.

Capture all of it as raw notes for Task 3; do not write `120-NONREGRESSION.md` yet.

If any row fails, do **not** proceed to Task 3 with a smoothed-over verdict. Record the failure, identify whether it is caused by this phase, and either fix it in this plan (if it is a gate this phase broke) or record it as an honest finding with its cause named.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_dispatch.py && python3 tools/check_devtest_orchestrator.py && python3 -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py tests/test_check_devtest_orchestrator.py tests/test_sdp_db_invariant.py -q && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python3 tools/check_mypy_watermark.py && test -z "$(git -C /workspaces/firestarter status --porcelain)" && test "$(git -C /workspaces/firestarter rev-parse --short HEAD)" = "0048b3d" && echo ALL-NINE-PLUS-FENCES-OK</automated>
  </verify>
  <acceptance_criteria>
    - All nine rows re-run and their verdicts captured verbatim, each attributed to the command that produced it.
    - Row 5's generator was actually run and the firmware status confirmed empty **afterward** — proving idempotence, not merely that the script exited 0.
    - Row 7's verdict is recorded as **CHANGED**, with its new shape described, not as unchanged.
    - Row 9's three commands all run, with an explicit note that it is the one host-side row at real risk because `check_devtest_orchestrator.py` scans `cli_handlers.py`.
    - `git -C /workspaces/firestarter status --porcelain` prints nothing and the tip is `0048b3d`; `version.h` still reads `3.0.0b11`.
    - `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/ firestarter/messages.py tools/build_db.py tools/catalog/` is empty.
    - `python3 tools/check_mypy_watermark.py` reports error count **1**, and that number is recorded alongside the note that the watermark's 34 slack makes a bare pass non-evidence.
    - Full-suite result captured with the coverage figure and the exact set of failures.
    - Both known-RED baselines named with their causes and with whether each reproduced **this** run.
    - No row is marked PASS on the strength of a path-scoped `git diff` against a possibly-nonexistent path.
  </acceptance_criteria>
  <done>Every gate row, both frozen-artifact fences and the full tooling gate have a re-run verdict at the final commit, with the two known-RED baselines named rather than pre-excused.</done>
</task>

<task type="auto">
  <name>Task 2: Verify — not change — dev test --submit's repo target and its argv assertion idiom</name>
  <files>firestarter_app/tests/test_submit.py</files>
  <read_first>
    - `firestarter_app/firestarter/submit.py` lines 60-80 — `SUBMIT_REPO` and the rationale comment above it. **Read only; change nothing.**
    - `firestarter_app/tests/test_submit.py` line 237 — the existing pin `assert submit.SUBMIT_REPO == "henols/firestarter_prom"`, plus the other repo-target pins at `:217`, `:234`, `:281`, `:392`, `:532`, `:649`, `:772`.
    - `firestarter_app/tests/test_submit.py` lines 275-291 — Idiom A, whole-argv equality via `run_fn.assert_called_once_with([...], input=..., text=..., capture_output=..., check=...)`.
    - `firestarter_app/tests/test_submit.py` lines 301-320 — Idiom B, the **negative** argv assertion, including its comment stating exactly what a mocked `run_fn` can and cannot honestly prove.
  </read_first>
  <action>
The operator asked to pull the `--submit` wrong-repo fix into this phase from Phase 121. **It is already fixed on this branch** — `firestarter/submit.py:73` reads `SUBMIT_REPO = "henols/firestarter_prom"` with a rationale comment citing `firestarter_prom#6`, landed at commit `e615b4c`, present on `beta` as well. The `v1.21` tag still carries `henols/firestarter_app`, which is why shipped `3.0.0b11` misfiles — a **released-artifact fact, not a source defect**. So this task closes the operator's ask honestly, without widening the code surface.

Do three things:

(a) **Assert the pin exists.** Confirm `tests/test_submit.py:237` still asserts `submit.SUBMIT_REPO == "henols/firestarter_prom"` and that it passes. Do not add a duplicate pin.

(b) **Confirm the argv idiom is present on the create path.** Confirm the positive whole-argv equality leg at `:275-291` and the **negative** argv leg at `:301-320` both exist and pass. Exit-code-only assertions are a known false-green here: a mocked `run_fn` cannot prove GitHub accepts the create call, but it **can** prove the argv never carries the permission-gated label flag. If — and only if — a repo-target-specific negative leg is missing, add exactly one following **Idiom B verbatim**: named `test_submit_via_gh_argv_targets_the_project_wide_tracker`, asserting the argv contains `"--repo"` immediately followed by `"henols/firestarter_prom"`, asserting the string `henols/firestarter_app` appears nowhere in the joined argv, and asserting `"shell" not in run_fn.call_args.kwargs`. Include the Idiom-B-style comment stating what the mocked `run_fn` can and cannot honestly prove.

(c) **Change no production code.** `firestarter/submit.py` must be byte-unchanged by this plan.

Record for the SUMMARY and for `120-NONREGRESSION.md`: the fix **pre-landed** at `e615b4c`, is on this branch and on `beta`, is pinned by an existing test, and **reaches users only at the next beta cut, not before** — so `3.0.0b11` in the wild continues to misfile until then. Also record that `gh issue create --label` aborts before creating unless the label pre-exists **and** the user has write access, which community testers have neither of — the reason the negative argv assertion is the load-bearing one.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_submit.py -q && python3 -c "from firestarter import submit; assert submit.SUBMIT_REPO=='henols/firestarter_prom', submit.SUBMIT_REPO; print('OK')" && test -z "$(git -C /workspaces/firestarter_app diff --stat -- firestarter/submit.py)" && echo SUBMIT-SOURCE-UNCHANGED && ruff check tests/test_submit.py && ruff format --check tests/test_submit.py</automated>
  </verify>
  <acceptance_criteria>
    - `submit.SUBMIT_REPO == "henols/firestarter_prom"` and the existing pin at `tests/test_submit.py:237` passes.
    - `firestarter/submit.py` is byte-unchanged by this plan (`git diff --stat -- firestarter/submit.py` empty).
    - The positive whole-argv leg and the negative argv leg both exist and pass.
    - If a negative repo-target leg was added, it follows Idiom B — asserting `--repo` is immediately followed by `henols/firestarter_prom`, that `henols/firestarter_app` appears nowhere in the joined argv, and that `shell` is not in the call kwargs — and carries the what-a-mock-can-honestly-prove comment. At most **one** new test was added.
    - `python3 -m pytest tests/test_submit.py -q` fully green.
    - `ruff check tests/test_submit.py` and `ruff format --check tests/test_submit.py` pass.
  </acceptance_criteria>
  <done>The operator's `--submit` ask is discharged as a verification with a recorded released-artifact explanation, with zero production-code change.</done>
</task>

<task type="auto">
  <name>Task 3: Write 120-NONREGRESSION.md and settle 120-VALIDATION.md's status</name>
  <files>.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md, .planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` in full — the eight-section shape to mirror: the claim as precise statements; the changed-surface matrix with every changed cell flagged and an honest count of how many existing test expectations moved; the frozen-artifact identity story; the nine-row gate table; the known-and-explained conditions; the validation-ceiling quote plus the safety argument; and the deliberately-not-taken options.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" — the exact permitted and forbidden claims, to quote rather than paraphrase.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md` — its front-matter `status` and `nyquist_compliant` fields and its Validation Sign-Off checklist.
    - Task 1's and Task 2's captured raw results.
  </read_first>
  <action>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md`, mirroring `119-NONREGRESSION.md`'s eight-section shape.

§1 **The claim, as precise statements.** Three of them: (a) the host now emits `cmd 9` / `cmd 10` and `FLAG_SKIP_SDP_UNLOCK 0x100`, and firmware and host are proven to agree on every `CMD_*` and `FLAG_*` value bidirectionally by a header-parsing gate with planted-violation fixtures; (b) a fail-closed pre-wire capability refusal keeps SDP commands away from 41 of the 84 protocol-`0x0D` parts on **both** the `dev sdp` path and `write`'s automatic unlock, with zero `chip_database.json` change; (c) one class of previously-invisible firmware report lines is now visible at default verbosity, and one class of previously-silent host failures (a declined unlock that firmware ignored) is now loud and failing.

§2 **The changed-surface matrix**, per command and per protocol, with every changed cell flagged. State honestly and explicitly **how many existing test expectations moved** — the expected answer for the D-09 promotion is **zero**, verified by searching the suite for level assertions and record-count assertions on the `SerialComm` logger; state whether that held across the whole phase, and name any expectation that did move together with why. Also state the deliberate behaviour divergence: `write` on the capability-refused `0x0D` subset differs from `3.0.0b11` because the host now auto-sets `FLAG_SKIP_SDP_UNLOCK` — a **deliberate host-side change**, never a no-op.

§3 **The frozen-artifact identity story.** `git -C /workspaces/firestarter status --porcelain` empty and tip `0048b3d`, which is the honest, **non-vacuous** proof — restate PROJECT.md FOURTH CORRECTION item 5's warning that a path-scoped `git diff` against a shorthand path can pass vacuously, and that an empty `status --porcelain` subsumes every path and cannot. Then the app-repo fences: DB, `messages.py`, `build_db.py` and `tools/catalog/` all unchanged; `version.h` still `3.0.0b11`; row 5's generator re-run leaving the firmware tree clean; and the note that `_shared/sdp_expected.h`'s whole-file blob-SHA shorthand is **retired** and was not reached for.

§4 **The nine-row gate table** with this phase's verdicts. Row 7 recorded as **CHANGED by design** with its new shape described. Row 9 recorded with its at-risk status and its actual result. Hand the table forward to Phases 121 and 122, as Phase 119 handed it to 120.

§5 **Known-and-explained conditions.** The two known-RED baselines with causes and whether each reproduced; `catalog-sync-check.yml`'s expected-red-until-milestone-merge status (it pins both sub-repos at `ref: main`); the mypy watermark's 34 slack and the recorded error count of 1; D-13's residual host-only-CI skip gap on the rebuilt parity gate; and the `--submit` released-artifact finding from Task 2 with `e615b4c` named and the statement that it reaches users only at the next beta cut.

§6 **The validation ceiling, quoted not paraphrased.** Quote the permitted claim and the forbidden claim from `REQUIREMENTS.md` verbatim. Then a line-by-line review statement: this phase makes **no** affirmative silicon-validation claim. `0x0D` stays `UNVERIFIED`; **zero** chips changed `support_status`; the 84-chip count is unchanged; no `PROTOCOL-LEDGER` entry was added. State that the capability partition is now **derived and reproducible** (from `infoic.xml` bit 15 at minipro commit `a8efaedc236c1d9718bd28299dfbb99536b010ff`) but **not bench-verified** — the ceiling still lists "that the curated capability partition is correct per family" among the things not provable this milestone, and that remains true. Point at `120-WATCHLIST.md` for the nine residual-risk entries.

§7 **The safety argument.** No bench byte could lock or unlock a real part in this phase: no AT28C part is on the bench, and this phase adds **no bench work at all**. State that the pre-wire refusal means no serial byte is emitted for a refused part, proven by `find_and_connect.assert_not_called()` rather than by exit code.

§8 **Deliberately not taken.** Enumerate each with its reason: the `dev test` redesign implementation (Phase 121; only the amendment landed); GATE-01's AST capability gate over `sdp_capability.py` (Phase 121 — this phase left the module in a shape it can assert against, with an import-purity leg); GATE-02's doc corrections including `doc/lockable-proms.md` §17's `AT28C16` error (Phase 121); the `MSG_INFO_SDP_UNLOCK_DONE_US` caveat text (a catalog change needing both sub-repos to regenerate — Phase 121/122, answered host-side by D-10); `_probe_port`'s version-capture regex widening (deferred, ring-fenced path, D-16 introduces no version floor); the wider CLI flag re-design (`-f` splitting, `-b` polarity, a project-wide `-y`); the `0x0D` flag-surface honesty problem (all 84 carry a firmware-inert `FLAG_CAN_ERASE`); the `${sysenv.*}` DEV_TOOLS gating and `dev sdp`'s release-channel disposition (999.15 / gh#8); a separate always-on `COMMAND_NAMES` test; and decoding `infoic.xml` bits 14/15 into the DB (the `page_size` phase, which would make the allow-set generated rather than transcribed).

Then update `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-VALIDATION.md` with scoped `Edit` calls: set `status` to `complete` (or the value the sign-off checklist implies), set `nyquist_compliant: true` and `wave_0_complete: true` **only if** every ❌ row's file now exists on disk and every row is reachable by a real command — verify each before flipping, and if any is not, leave the flag false and record why. Tick the Validation Sign-Off checklist items that are genuinely satisfied and leave the rest, with a note. Replace the `**Approval:** pending` line's value only if the sign-off items are all satisfied.

Finally, verify `.planning/REQUIREMENTS.md`: all six of HOST-01 through HOST-06 read `[x]` with `Complete` traceability rows, DEVTEST-01 still reads `[ ]`, and every Phase 121 id created by plan 120-11 still reads `[ ]`. Change nothing there.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import re
n=open('.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md').read()
for lit in ['0048b3d','3.0.0b11','UNVERIFIED','84','a8efaedc236c1d9718bd28299dfbb99536b010ff','e615b4c','120-WATCHLIST.md','test_audit_coverage_matrix','catalog-sync-check','CHANGED']:
    assert lit in n, lit
assert n.count('|')>40, 'nine-row table missing'
t=open('.planning/REQUIREMENTS.md').read()
for rid in ['HOST-01','HOST-02','HOST-03','HOST-04','HOST-05','HOST-06']:
    m=re.search(r'- \[(.)\] \*\*'+rid+r'\*\*', t); assert m and m.group(1)=='x', rid
m=re.search(r'- \[(.)\] \*\*DEVTEST-01\*\*', t); assert m and m.group(1)==' '
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `120-NONREGRESSION.md` exists with all eight sections in `119-NONREGRESSION.md`'s shape.
    - §2 states an explicit count of how many existing test expectations moved, and names the deliberate `write`-path divergence from `3.0.0b11` as deliberate, not a no-op.
    - §3 restates FOURTH CORRECTION item 5's vacuous-path warning and uses the empty `status --porcelain` as the proof.
    - §4's nine-row table records row 7 as **CHANGED** and row 9 with its at-risk status and actual result, and hands the table forward to Phases 121 and 122.
    - §5 names both known-RED baselines with causes and reproduction status, `catalog-sync-check.yml`'s expected-red status, the mypy error count of 1 with the 34-slack caveat, D-13's residual skip gap, and the `e615b4c` released-artifact finding.
    - §6 **quotes** the permitted and forbidden claims verbatim and states `0x0D` stays `UNVERIFIED`, zero `support_status` changes, and the 84-chip count unchanged.
    - §8 enumerates at least ten deliberately-not-taken items, each with a reason and a destination.
    - `120-VALIDATION.md`'s flags are flipped only where genuinely satisfied, with any remaining false flag carrying a recorded reason.
    - `.planning/REQUIREMENTS.md` verified unchanged by this plan: all six HOST ids `[x]`, DEVTEST-01 `[ ]`, every new Phase 121 id `[ ]`.
    - No file under `/workspaces/firestarter` modified; tip still `0048b3d`.
  </acceptance_criteria>
  <done>The phase has an honest non-regression record in the established shape, and the validation contract is settled or its remaining gaps are named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate verdict → future phase's confidence | A vacuously-passing row is worse than a failing one: it is a false statement about a fence that later phases will lean on. |
| closing record → milestone close | Phase 122's closeout comments to gh#11 and gh#12 are built on this record. An overclaim here becomes an overclaim to a community reporter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-47 | Repudiation | A gate row marked PASS on a `git diff` against a path that does not exist | high | mitigate | The proof is an empty `git -C /workspaces/firestarter status --porcelain`, which subsumes every path; FOURTH CORRECTION item 5's warning is restated in §3. |
| T-120-48 | Repudiation | A silently tolerated known-RED baseline masking a real regression introduced by this phase | high | mitigate | Both baselines named with causes and with whether each reproduced this run; the audit-matrix golden is flagged as a hazard for any future matrix-touching change. |
| T-120-49 | Repudiation | Any claim that SDP works on real AT28C silicon, or any protection-state claim | critical | mitigate | §6 quotes the permitted and forbidden claims verbatim and states no affirmative silicon claim is made; no bench work exists in this phase to make one from. |
| T-120-50 | Tampering | Re-fixing `submit.py` and thereby claiming a fix that was already landed, or masking that `3.0.0b11` still misfiles | medium | mitigate | Verification-only task; `submit.py` byte-unchanged is an acceptance criterion; the released-artifact framing with `e615b4c` is recorded. |
| T-120-51 | Repudiation | A bare `mypy` or bare `ruff check .` pass or fail being read as this phase's signal | medium | mitigate | The CI-scoped commands are the recorded gate; the four out-of-scope `tools/` failures and the watermark's 34 slack are both stated. |
| — | Tampering (supply chain) | Package installs | n/a | accept | Zero packages installed in this plan. |
</threat_model>

<verification>
- All nine rows re-run at the final commit with verdicts recorded; row 7 CHANGED, row 9 confirmed.
- `python3 -m pytest tests/ --cov=firestarter --cov-fail-under=70` — green except the single named pre-existing failure.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` clean; the bare-`.` form's four out-of-scope failures noted but not chased.
- `python3 tools/check_mypy_watermark.py` — reported error count **1**.
- `git -C /workspaces/firestarter status --porcelain` empty; tip `0048b3d`; `version.h` still `3.0.0b11`.
- `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/ firestarter/messages.py tools/build_db.py tools/catalog/ firestarter/submit.py` empty.
- `.planning/REQUIREMENTS.md` unchanged by this plan.
</verification>

<success_criteria>
- Nine-row gate table re-run and recorded, with row 7 honestly marked CHANGED and row 9's at-risk status resolved.
- Both frozen-artifact fences verified by a proof that cannot pass vacuously.
- The `--submit` ask discharged as a verification, with `submit.py` byte-unchanged and the released-artifact fact recorded.
- `120-NONREGRESSION.md` written in the established eight-section shape, quoting the ceiling verbatim and making no silicon claim.
- `120-VALIDATION.md` settled, or its remaining gaps named.
- All six HOST ids verified `[x]`; nothing newly ticked by this plan.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New files:** `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md`.

**Modified files:** `120-VALIDATION.md` (front-matter flags and sign-off, scoped edits); `firestarter_app/tests/test_submit.py` (at most one added negative-argv leg).

**New test symbols:** at most one — `test_submit_via_gh_argv_targets_the_project_wide_tracker`, and only if a repo-target-specific negative leg is genuinely missing.

**Requirement IDs this plan may mark Complete:** **none.** All six HOST ids were closed by plans 120-07 through 120-10. This plan **verifies** they read `[x]` and changes nothing in `.planning/REQUIREMENTS.md`.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-12-SUMMARY.md` when done.

The SUMMARY must record: the nine-row verdicts individually, with row 7 stated as **CHANGED by design** and row 9's actual result named; that row 5's generator was re-run and left the firmware tree clean, and that the retired `_shared/sdp_expected.h` blob-SHA shorthand was not reached for; the full-suite result with its coverage figure and the exact failure set; the mypy reported error count with the 34-slack caveat; both frozen-artifact fences with the honest non-vacuous proof and PROJECT.md FOURTH CORRECTION item 5's warning restated; whether each known-RED baseline reproduced; the `--submit` verification outcome — pre-landed at `e615b4c`, on this branch and on `beta`, pinned by an existing test, **reaching users only at the next beta cut** so `3.0.0b11` in the wild continues to misfile; that `gh issue create --label` aborts before creating without a pre-existing label and write access, which is why the negative argv assertion is load-bearing; and an explicit statement that this phase makes **no** affirmative silicon-validation claim — `0x0D` stays `UNVERIFIED`, zero `support_status` changes, the 84-chip count unchanged, and the capability partition is derived and reproducible but **not** bench-verified.
</output>
