---
phase: 120-host-cli-surface-wire-emission-capability-refusal
plan: 11
type: execute
wave: 6
depends_on: ["120-10"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [DEVTEST-01]
must_haves:
  truths:
    - "ROADMAP.md's Phase 121 entry and Phase Details carry the operator's `dev test` redesign in scope, with success criteria covering it."
    - "REQUIREMENTS.md carries new requirement ids for the redesign, traceability rows mapped to Phase 121 with status Pending, and corrected Coverage arithmetic."
    - "PROJECT.md carries a SEVENTH CORRECTION block recording the redesign as a reversal, its three collisions, and this phase's two recorded-not-fixed findings."
    - "Phase 121 is still present and un-renumbered; no requirement whose other half is unlanded is ticked."
  artifacts:
    - ".planning/PROJECT.md"
  key_links:
    - "The amendment follows `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-PLAN.md`'s precedent exactly — meta-repo-only, scoped `Edit` calls, no submodule gitlink staged."
    - "The reversal-recording pattern is Phase 119 D-18's: record a reversal *as* a reversal, with its constraints named, so the next phase does not read it as the new default."
  prohibitions:
    - "MUST NOT touch either sub-repo. This plan is meta-repo only; no submodule gitlink may be staged, and both sub-repo working trees must be clean at the end. `git -C /workspaces/firestarter status --porcelain` empty and tip still `0048b3d`."
    - "MUST NOT implement any part of the `dev test` redesign. Only the amendment lands here. No `chip_test.py`, `cli_handlers.py`, `diagnostic_report.py`, `submit.py`, `tools/parse_devtest_issue.py` or test file is edited by this plan."
    - "MUST NOT tick DEVTEST-01's checkbox. Its host half is Phase 121's."
    - "MUST NOT tick any newly created requirement id. All new ids land as Pending, mapped to Phase 121."
    - "MUST NOT edit the WORDING of DEVTEST-01, SUB-01, SUB-02, SAFE-01, SAFE-03, LOCK-04, LOCK-06, or any HOST requirement. A correction block is the record; requirement text is not rewritten."
    - "MUST NOT remove Phase 121 or Phase 122 from ROADMAP.md, and MUST NOT renumber any phase. Amend Phase 121's scope text and criteria; the phase stays."
    - "MUST NOT run `gsd roadmap phases.clear` or any destructive ROADMAP mutation. This project has 50-plus preserved phase directories that such a call would hard-delete."
    - "MUST NOT rewrite ROADMAP.md, REQUIREMENTS.md, PROJECT.md or STATE.md wholesale. Use scoped `Edit` calls only — ROADMAP.md is 2206 lines and carries every phase of this milestone plus preserved earlier ones."
    - "MUST NOT re-tick or re-word HOST-01 through HOST-06. All six were closed by plans 120-07 through 120-10; verify they still read `[x]` and leave them alone."
---

<objective>
Land the ROADMAP + REQUIREMENTS + PROJECT amendment for the operator's `dev test` redesign as an owned task of this phase (D-20), following 119-09's precedent exactly. Implement none of the redesign.

Purpose: operator decision, 2026-07-29 — fold the redesign into Phase 121 rather than inserting a new phase or deferring it to a v1.23 seed. The amendment must record it **as a reversal**, because it reverses three locked decisions, and must carry the three collisions a Phase 121 researcher and planner will otherwise hit blind.
Output: an amended Phase 121 scope, new Phase 121 requirement ids with corrected coverage arithmetic, and a PROJECT.md SEVENTH CORRECTION block.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-PLAN.md
</context>

## The redesign, verbatim from the operator (2026-07-29)

`dev test` takes **no flags**. "Destructive" applies **only to UV-erasable EPROMs**. The sweep **stops and asks** whether to do a destructive write, where **yes** means the full device may be written and **no** means only a small part of it is written. **Every** run asks whether to file an issue, checking first whether the user has already reported an identical one and creating a new issue only when it differs. The `gh` path is used wherever it can replace the URL/browser path.

## The three collisions the amendment MUST record — each verified in live source

**(a) It reverses three locked decisions.** Confirmed verbatim in the tree:
- `cli_handlers.py:1808-1812` — `dev_test`'s docstring states it *"Issues ZERO interactive prompts about tester-supplied identity (Phase 112 Plan 04 reversal, operator-approved per `112-UAT.md`): shield revision, chip origin, and pot-adjustment are no longer asked."*
- `cli_handlers.py:1828-1832` — *"SAFE-03: the ONLY interactive input left in this handler is the `--destructive` safety confirm."*
- `cli_handlers.py:1758-1760` — SAFE-01's lock in the option help: *"CLI-only flag -- never read from config or environment (SAFE-01)."*

Record it **as a reversal**, the way Phase 119 D-18 recorded reversing Phase 118's D-12 — with its constraints named, so the next phase does not read it as the new default.

**(b) "Non-destructive means a partial write" is a contract change, not a flag change.** `derive_plan` at `chip_test.py:319-425`, and `Plan.locked_destructive`'s docstring at `:298-316` which states *"`run_plan` MUST NOT iterate this field."* Today `destructive=False` **structurally omits** `OP_WRITE`, `OP_VERIFY` and `OP_ERASE` from `Plan.steps` and records them as `(op, reason)` tuples on the advisory-only `locked_destructive` list — there is literally no code path from `run_plan` to a destructive op on a non-destructive plan. A third "partial write" mode therefore needs a **new representation**, not a flag flip. Its ripple set, all confirmed present: the closed six-string op vocabulary `OP_ID` / `OP_READ` / `OP_BLANK_CHECK` / `OP_WRITE` / `OP_VERIFY` / `OP_ERASE` (`chip_test.py:273-278`); `tools/parse_devtest_issue.py`; `diagnostic_report.py`'s renderer and its `ladder_state` tag (`:247`, GRAD-01); `dedup_fingerprint` (`diagnostic_report.py:174`, read back by `submit.py:169-174`); and `tests/test_audit_coverage_matrix.py`'s golden — **which is already RED**, so a matrix-touching change lands on top of an existing failure and must not be allowed to mask it.

**(c) "Destructive only for UV-erasable" needs an explicit axis pick, and the obvious axis is unavailable where it is needed.** `electrical.type == "UV-EPROM"` exists in the DB (**301 parts**, measured) and *is* reachable in `derive_plan`, which reads `full = db.get_eprom(name)`. It is **not** reachable at the execution layer: `run_plan` → `_resolve_or_none` → `resolve_chip` (`chip_test.py:505`), whose dict drops `electrical-type`. The project already worked around this once, at `_write_region_for` (`:637-663`), falling back to `algorithm == 0x0B`. **Measured coverage of that fallback: 32 of 301.** UV-EPROM by algorithm: `0x07` → 163, `0x08` → 106, `0x0B` → 32. So the existing execution-time UV signal **misses 89% of UV parts**. Any Phase 121 design gating destructiveness on "UV-erasable" must either widen the algorithm set to `{0x07, 0x08, 0x0B}` (structural, no type-string dependency — the project's stated preference, since `protocol_id` is the algorithm axis and **not** the UV-versus-EEPROM axis) or thread the `full` dict to the execution layer.

## Two further constraints the amendment must carry

- **`--submit`'s contract is contradicted.** SUB-01/SUB-02 lock it as *"explicit + interactive-only; never on a bare run"*; always-ask contradicts that. Record it as **reversed**, not silently dropped.
- **`gh issue create --label` aborts before creating** unless the label pre-exists **and** the user has write access — which community testers have neither of. Any `gh`-first design must assert the **negative** argv (the label flag is never sent), the idiom already in `tests/test_submit.py:301-320`.

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md's Phase 121 entry and Phase Details to carry the redesign</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` line 156 — Phase 121's one-line checkbox entry in the v1.22 phase list, ending in its requirement-id list.
    - `.planning/ROADMAP.md` lines 377-392 — `### Phase 121: dev test FIX + GATES + DOCS`: its Goal, Depends on, Requirements, five Success Criteria, Plans and Research flag lines. Note criterion 2 already carries the Phase 119 early-landing record; do not disturb it.
    - `.planning/ROADMAP.md` lines 360-376 — Phase 120's own block, to see the established shape and to leave it alone.
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-PLAN.md` — the precedent for exactly how much ROADMAP text an amendment task touches.
  </read_first>
  <action>
Use scoped `Edit` calls on `.planning/ROADMAP.md`. Never a wholesale rewrite — the file is 2206 lines and carries every phase of this milestone plus preserved earlier ones.

Amend Phase 121's **one-line checkbox entry** (line 156): extend its scope sentence to include the `dev test` redesign in the operator's own terms (no flags; destructive scoped to UV-erasable EPROMs; the sweep stops and asks whether to do a destructive write, where yes means the full device may be written and no means only a small part of it is written; every run asks whether to file an issue, checking first for an identical prior report by the same user and creating a new one only when it differs; `gh` replaces the URL/browser path wherever it can). Append the new requirement ids created in Task 2 to the trailing id list. Leave the existing DEVTEST-01 / GATE-01 / GATE-02 / GATE-03 text and the `(likely needs /gsd-plan-phase --research-phase 121)` note intact.

Amend the **Phase 121 Phase Details block**:
- Extend the **Goal** line to name the redesign alongside the existing three goals.
- Extend the **Requirements** line with the new ids.
- Add new **Success Criteria** covering the redesign — one per operator-stated behaviour, each phrased as something that must be TRUE. Number them after the existing five; do not renumber or reword criteria 1 through 5.
- Add an explicit note under the Success Criteria recording that the redesign **reverses three locked decisions** (Phase 112 Plan 04's deliberate removal of every interactive prompt, operator-approved per `112-UAT.md`; SAFE-01's CLI-only `--destructive`; SAFE-03's "only interactive input left" statement) and that this is recorded *as a reversal*, following Phase 119 D-18's pattern. Also record collision (b) in one sentence — that non-destructive-means-partial-write is a **contract change** in `derive_plan`, not a flag change, and name its ripple set — and collision (c) in one sentence with the measured **32 of 301** anchor.
- Leave the **Research flag** line's `yes` intact and strengthen its parenthetical to note that the redesign reinforces the need for `--research-phase 121`.
- Leave **Plans: TBD** alone.

Do not touch Phase 122's block. Do not remove or renumber any phase. Do not run any destructive ROADMAP mutation.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
t=open('.planning/ROADMAP.md').read()
assert 'Phase 121' in t and 'Phase 122' in t and 'Phase 120' in t
assert t.count('### Phase 121') == 1 and t.count('### Phase 122') == 1
for lit in ['112-UAT.md','SAFE-01','SAFE-03','32 of 301','derive_plan']:
    assert lit in t, lit
print('OK lines:', len(t.splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` still contains exactly one `### Phase 121` and one `### Phase 122` heading, and Phase 120's block is unmodified.
    - The file's line count is **greater than or equal to** its pre-edit count — nothing was deleted wholesale (capture the pre-edit count before editing and state both numbers in the SUMMARY).
    - Phase 121's checkbox entry names all five redesign behaviours and lists the new requirement ids.
    - Phase 121's Phase Details Goal and Requirements lines are extended; criteria 1 through 5 are byte-unchanged; new criteria are appended.
    - A reversal note names `112-UAT.md`, SAFE-01 and SAFE-03, and cites Phase 119 D-18 as the recording pattern.
    - Collision (b) is recorded naming `derive_plan` and its ripple set; collision (c) is recorded with the literal `32 of 301`.
    - `git diff --stat -- .planning/ROADMAP.md` shows a scoped edit, not a full-file replacement.
    - No submodule gitlink is staged.
  </acceptance_criteria>
  <done>Phase 121's ROADMAP scope carries the redesign, its reversal status, and both structural collisions a researcher would otherwise hit blind.</done>
</task>

<task type="auto">
  <name>Task 2: Add the new Phase 121 requirement ids, traceability rows and corrected coverage arithmetic</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 82-97 — the `dev test` Correctness section (holding only DEVTEST-01 today) and the Gates & Documentation and Close sections that follow.
    - `.planning/REQUIREMENTS.md` lines 100-131 — Future Requirements and the Out of Scope table, where SUB-01/SUB-02's contradicted contract must be recorded.
    - `.planning/REQUIREMENTS.md` lines 147-195 — the Traceability table and the Coverage bullets (`36` total, the per-phase tally, `36/36` mapped, `0` unmapped).
    - `.planning/REQUIREMENTS.md` lines 73-81 — the HOST block, to confirm all six now read `[x]` and to leave them untouched.
  </read_first>
  <action>
Use scoped `Edit` calls on `.planning/REQUIREMENTS.md`. No wholesale rewrite.

Add new requirement ids to the existing `### "dev test" Correctness` section, one per operator-stated behaviour, in the file's established `- [ ] **ID**: text` style. Recommended granularity — five ids continuing the existing prefix, `DEVTEST-02` through `DEVTEST-06`, covering: (1) `dev test` takes no flags; (2) destructiveness is scoped to UV-erasable EPROMs on an explicitly-chosen axis; (3) the sweep stops and asks whether to do a destructive write, with yes meaning the full device may be written and no meaning only a small part of it is written — recorded as a **third mode** requiring a new representation, not a flag flip; (4) every run asks whether to file an issue, with a prior-identical-report check first and a new issue only when it differs; (5) the `gh` path replaces the URL/browser path wherever it can, asserting the **negative** argv because `gh issue create --label` aborts before creating unless the label pre-exists and the user has write access. If a different granularity is chosen, keep it to five or fewer ids and keep every arithmetic consequence consistent.

Each new id's text must name its own binding constraint inline, so a Phase 121 planner reading only `REQUIREMENTS.md` still learns it: for (2) the measured **32 of 301** coverage of the existing execution-time UV signal and the `{0x07, 0x08, 0x0B}` structural alternative; for (3) that `derive_plan(destructive=False)` structurally omits the destructive ops today and `Plan.locked_destructive` is advisory-only with `run_plan` forbidden from iterating it; for (4) that this **reverses** SUB-01/SUB-02's *"explicit + interactive-only; never on a bare run"* contract.

Record the SUB-01/SUB-02 reversal in the appropriate section (Out of Scope or Future Requirements, wherever those ids currently live) as **reversed, not silently dropped**, naming Phase 121 as where the new contract lands. Do **not** edit SUB-01's or SUB-02's own wording.

Add one Traceability row per new id, mapped to **Phase 121**, status **Pending**.

Update the Coverage bullets so the arithmetic is consistent: the total requirement count, the per-category tally (the `DEVTEST` count changes), the mapped count, and the Phase 121 line in the per-phase breakdown. `Unmapped: 0` must stay true. Update the file's trailing `*Last updated: ...*` line to record this amendment.

Verify and leave alone: all six of HOST-01 through HOST-06 read `[x]` with `Complete` traceability rows; DEVTEST-01's checkbox stays `[ ]`; every new id's checkbox is `[ ]`. Do not edit the wording of DEVTEST-01, LOCK-04, LOCK-06 or any HOST requirement.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import re
t=open('.planning/REQUIREMENTS.md').read()
for rid in ['HOST-01','HOST-02','HOST-03','HOST-04','HOST-05','HOST-06']:
    m=re.search(r'- \[(.)\] \*\*'+rid+r'\*\*', t); assert m and m.group(1)=='x', rid
m=re.search(r'- \[(.)\] \*\*DEVTEST-01\*\*', t); assert m and m.group(1)==' ', 'DEVTEST-01 must stay unticked'
new=re.findall(r'- \[ \] \*\*(DEVTEST-0[2-9])\*\*', t)
assert len(new)>=1, 'no new DEVTEST ids found'
for rid in new:
    assert re.search(r'\| '+rid+r' \| Phase 121 \| Pending', t), rid
assert '32 of 301' in t
print('OK new ids:', new)"</automated>
  </verify>
  <acceptance_criteria>
    - New requirement ids exist in the `dev test` Correctness section, all with `[ ]` checkboxes, each carrying its binding constraint inline.
    - Every new id has a Traceability row reading `| <ID> | Phase 121 | Pending |`.
    - The Coverage bullets' total, per-category tally, per-phase breakdown and mapped count are all internally consistent, and `Unmapped: **0**` still holds.
    - The literal `32 of 301` appears in the file.
    - The SUB-01/SUB-02 contract reversal is recorded as a reversal naming Phase 121, without editing SUB-01's or SUB-02's wording.
    - All six HOST ids read `[x]`; DEVTEST-01 reads `[ ]`.
    - DEVTEST-01's, LOCK-04's, LOCK-06's and every HOST requirement's wording is unchanged.
    - The trailing `*Last updated: ...*` line records this amendment.
    - `git diff --stat -- .planning/REQUIREMENTS.md` shows a scoped edit.
  </acceptance_criteria>
  <done>Phase 121's redesign has requirement ids, traceability and consistent arithmetic, with each id carrying the constraint that will otherwise be rediscovered the hard way.</done>
</task>

<task type="auto">
  <name>Task 3: Write PROJECT.md's SEVENTH CORRECTION block and update STATE.md</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 91-108 — the SIXTH CORRECTION block in full, for the established shape (a bold `⚠ Nth CORRECTION — Phase NNN close (date): headline` opener followed by a numbered list), and specifically its **item 6**, whose stated reason for the `_SRAM_PROTO_IDS` keep-disposition this phase found to be false.
    - `.planning/PROJECT.md` lines 73-80 — the FOURTH CORRECTION, whose **item 4** mandates the cross-repo gate checklist in every phase from 118 on, and whose **item 5** carries the `flash_utils.{h,cpp}` vacuous-path warning.
    - `.planning/STATE.md` lines 1-50 — the front matter (`current_phase`, `current_phase_name`, `status`, `stopped_at`, `progress`) and the Current Position block, plus the long HTML comment at line 49 documenting the STATE.md tooling defects.
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-PLAN.md` — its STATE.md ordering discipline, to copy.
  </read_first>
  <action>
Append a **SEVENTH CORRECTION** block to `.planning/PROJECT.md` immediately after the SIXTH CORRECTION block, in the established shape. Use `Edit` for a scoped insertion; do not rewrite the file.

Headline it as Phase 120's close correction, dated, framing it as the phase where the host surface landed and where the operator's `dev test` redesign was folded into Phase 121.

Numbered items, all of them:

1. **The `dev test` redesign is folded into Phase 121, and it is a REVERSAL.** State the redesign verbatim in the operator's terms. Name the three reversed locked decisions with their in-source citations (`cli_handlers.py:1808-1812`, `:1831-1835`, `:1760-1762`; `112-UAT.md`; SAFE-01; SAFE-03). State that it is recorded *as* a reversal following Phase 119 D-18's pattern, so the next phase does not read it as the new default. State that Phase 120 landed only the amendment and **none** of the implementation.

2. **"Non-destructive means a partial write" is a contract change, not a flag change.** Collision (b) in full, with the `chip_test.py:319-425` / `:298-316` / `:273-278` citations, the ripple set (`tools/parse_devtest_issue.py`, `diagnostic_report.py`'s renderer and `ladder_state` at `:247`, `dedup_fingerprint` at `:177` read back by `submit.py:169-174`, and `tests/test_audit_coverage_matrix.py`'s golden), and the warning that the matrix golden is **already RED** so a matrix-touching change must not mask an existing failure.

3. **"Destructive only for UV-erasable" needs an explicit axis pick.** Collision (c) with the measured numbers: 301 UV-EPROM parts by `electrical.type`; the execution-layer signal at `_write_region_for` (`chip_test.py:637-663`) falls back to `algorithm == 0x0B` and therefore covers **32 of 301**; the algorithm breakdown `0x07` → 163, `0x08` → 106, `0x0B` → 32. State both viable remedies (widen to `{0x07, 0x08, 0x0B}`, or thread the `full` dict to the execution layer) and the project's stated preference for structural guards over type-string keys, noting that `protocol_id` is the **algorithm** axis and not the UV-versus-EEPROM axis.

4. **`--submit`'s contract is reversed, and its wrong-repo defect is a released-artifact fact, not a source defect.** SUB-01/SUB-02's *"explicit + interactive-only; never on a bare run"* is contradicted by always-ask. Separately: `firestarter/submit.py:73` already reads `SUBMIT_REPO = "henols/firestarter_prom"` on this branch (commit `e615b4c`) and on `beta`, pinned by `tests/test_submit.py:237`. The `v1.21` tag still carries `henols/firestarter_app`, which is why shipped `3.0.0b11` misfiles — so **no source change is needed**, and the fix reaches users only at the next beta cut. Also record that `gh issue create --label` aborts before creating unless the label pre-exists **and** the user has write access, which community testers have neither of, so any `gh`-first design must assert the **negative** argv (`tests/test_submit.py:301-320`'s idiom).

5. **SIXTH CORRECTION item 6's stated reason is false; its disposition stands.** `check_eprom_blank`'s `_SRAM_PROTO_IDS` short-circuit reads `electrical-type` and `protocol-id` from the dict its callers pass, but both production callers (`cli_handlers.py:576`, `chip_test.py:737`) pass `resolve_chip`'s **programmer** dict, which carries neither — measured returning `False` for a real SRAM part. **The short-circuit is vacuous in production.** Item 6's KEEP disposition is still correct; its stated reason — that it fires and produces a materially better message — is **not**. Phase 120 recorded this, did not fix it and did not delete the code. State the consequence that made this load-bearing: a `sdp_capability` predicate keyed on the same dict would have reproduced the identical silent vacuity, which is why the predicate is name-keyed and why `tests/test_sdp_capability.py` ships a dict-shape anti-vacuity leg — a validation surface this milestone had to invent.

6. **The HOST-04 partition is derived, not curated.** ALLOW 43 / REFUSE 41 = 84, from minipro `infoic.xml` `<database type="INFOIC2PLUS">` `flags` bit 15 (`0x8000`, `MP_PROTECT_AFTER`) at commit `a8efaedc236c1d9718bd28299dfbb99536b010ff`; all 84 matched, zero unmatched, zero MIXED; three independent ground-truth probes passed 8/8, 2/2 and 4/4. This **supersedes** `120-RESEARCH.md` F-01's curated 37/47 and the interim 74/10 placeholder. The mechanism is unchanged — a static fail-closed allow-list in `firestarter/sdp_capability.py` plus a runtime exhaustiveness gate; nothing reads `infoic.xml` at runtime or in CI and it is **not** added to either sub-repo. Note that the 2026-07-10 `infoic-xml-protection-flags-research.md` verdict **stands**, scoped to the protection-kind / status-readable taxonomy, and received an append-only scoped exception. Note the two recorded-not-acted-on findings: `doc/lockable-proms.md` §17 is wrong about `AT28C16` (GATE-02, Phase 121), and bit 15 is **not** a page-write proxy — it disagrees with `page_size > 1` on 12 of 84, with the nine residual-risk entries named in `120-WATCHLIST.md`.

7. **Two ROADMAP/catalog corrections restated.** There is **no** `0x200` flag — firmware's flag block ends at `FLAG_SKIP_SDP_UNLOCK 0x100`, so `ROADMAP.md:363` and Phase 120's *Depends on* line are wrong and the host wires **one** flag; `REQUIREMENTS.md` was deliberately not edited. And `MSG_INFO_SDP_UNLOCK_DONE_US` (`0x5F`) still lacks the honesty caveat `0x61` carries — answered host-side by D-10's symmetric summary line, with the catalog fix deferred to Phase 121/122.

8. **The class lesson worth keeping.** Phase 118's OBS-01 was verified in firmware and discarded by the host for a whole phase, because `_log_rurp_feedback` mapped the entire INFO band to `logging.DEBUG` while root is `INFO` unless `-v`. **A two-repo requirement can pass its own phase's verification and still be false end to end.** Phase 120's D-09 fixed it and, in doing so, also made Phase 35's CR-02 hard-fail-loud revision warning (`0x5B` `MSG_INFO_HW`, emitted through the unconditional `LOG_WARN_ID_U8` alias) visible for the first time — **six** newly-visible ids, not five.

9. **FOURTH CORRECTION item 4's checklist held.** State the nine-row cross-repo gate outcome (plan 120-12 owns the sweep and the `120-NONREGRESSION.md` artifact), that **row 7** was deliberately rebuilt by this phase so "unchanged" is the wrong verdict for it, and that **row 9** (`check_devtest_orchestrator.py`, which scans `cli_handlers.py`) was the one host-side row at real risk. Restate item 5's warning in its honest form: a path-scoped `git diff` can pass vacuously, so the proof used is `git -C /workspaces/firestarter status --porcelain` being empty, which subsumes every path.

Then update `.planning/STATE.md`, following the ordering discipline documented in its own HTML comment: call `state.record-session` **FIRST**, then any progress / metric / decision calls, then **hand-verify** `current_phase_name` (it is repeatedly mangled on the em-dash and the trailing parenthetical) and `progress.percent`. **Never trust the returned `updated` array** — diff STATE.md before and after every call. Record Phase 120's position, the twelve-plan graph, and a short cross-phase-consequences block for Phases 121 and 122 mirroring the existing blocks' shape. Do not mark the phase complete or verified — plan 120-12 is still to run.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
t=open('.planning/PROJECT.md').read()
assert 'SEVENTH CORRECTION' in t
i6=t.index('SIXTH CORRECTION'); i7=t.index('SEVENTH CORRECTION'); assert i7>i6, 'SEVENTH must follow SIXTH'
for lit in ['112-UAT.md','32 of 301','_SRAM_PROTO_IDS','a8efaedc236c1d9718bd28299dfbb99536b010ff','e615b4c','0x5B','43','41','AT28C16']:
    assert lit in t, lit
s=open('.planning/STATE.md').read()
assert 'current_phase: 120' in s
assert 'HOST — CLI surface' in s, 'current_phase_name was mangled'
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains a `SEVENTH CORRECTION` block positioned **after** the SIXTH CORRECTION, in the established shape, with all nine numbered items.
    - The block contains the literals `112-UAT.md`, `32 of 301`, `_SRAM_PROTO_IDS`, `a8efaedc236c1d9718bd28299dfbb99536b010ff`, `e615b4c`, `0x5B` and `AT28C16`.
    - Item 1 records the redesign **as a reversal** naming all three reversed decisions with their in-source citations, and states that no implementation landed in Phase 120.
    - Item 5 states that SIXTH CORRECTION item 6's disposition stands and its stated reason is false, and that Phase 120 recorded rather than fixed it.
    - Item 6 records the derived 43/41 partition with its full provenance, that it supersedes 37/47 and 74/10, and that the 2026-07-10 note's verdict stands with an append-only scoped exception.
    - No existing PROJECT.md correction block was modified — the FOURTH, FIFTH and SIXTH blocks are byte-unchanged.
    - `.planning/STATE.md` front matter reads `current_phase: 120`, and `current_phase_name` is the **full** roadmap title (not a bare parenthetical and not truncated mid-parenthetical) — hand-verified after every state call.
    - STATE.md does **not** claim the phase is complete or verified.
    - `git -C /workspaces/firestarter status --porcelain` empty and tip `0048b3d`; `git -C /workspaces/firestarter_app status --porcelain` no worse than at plan start; no submodule gitlink staged.
    - `git diff --stat` for all four `.planning/` files shows scoped edits, not full-file replacements.
  </acceptance_criteria>
  <done>PROJECT.md carries the phase's correction record and every collision a Phase 121 researcher needs, and STATE.md reflects the position without over-claiming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → next phase's researcher and planner | These four files are the only durable carrier of the redesign's collisions. A missing collision is rediscovered mid-execution, at the cost of a wrong design. |
| scoped edit → 2206-line ROADMAP | A wholesale write destroys every phase entry outside the diff window, including 50-plus preserved earlier phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-120-42 | Tampering (record) | A wholesale rewrite of ROADMAP.md, REQUIREMENTS.md, PROJECT.md or STATE.md destroying content outside the diff window | high | mitigate | Scoped `Edit` calls only; the line-count and heading-count assertions in Task 1's verify; the explicit prohibition on `gsd roadmap phases.clear`. |
| T-120-43 | Repudiation | Recording a reversal as if it were the new default, so a later phase re-litigates a settled decision | high | mitigate | Phase 119 D-18's pattern — the reversal is recorded *as* a reversal with its constraints named, in both ROADMAP and PROJECT.md. |
| T-120-44 | Repudiation | Ticking a requirement whose other half is unlanded | high | mitigate | Explicit prohibitions plus the verify assertion that DEVTEST-01 and every new id stay `[ ]`. |
| T-120-45 | Repudiation | A false "fix" claim for `--submit`'s wrong-repo behaviour | medium | mitigate | Item 4 records it as a **released-artifact** fact with the pre-landing commit `e615b4c` and states the fix reaches users only at the next beta cut. |
| T-120-46 | Tampering | Staging a submodule gitlink from a meta-only plan, silently moving a sub-repo pointer | medium | mitigate | Explicit prohibition plus the `git status --porcelain` and no-gitlink-staged acceptance criteria. |
| — | Tampering (supply chain) | Package installs | n/a | accept | Zero packages installed in this plan; no code is written at all. |
</threat_model>

<verification>
- All four `.planning/` files edited with scoped `Edit` calls; `git diff --stat` confirms no full-file replacement.
- `.planning/ROADMAP.md` still has exactly one `### Phase 121` and one `### Phase 122`; its line count did not shrink.
- `.planning/REQUIREMENTS.md` arithmetic internally consistent; `Unmapped: **0**` holds; DEVTEST-01 and every new id unticked; all six HOST ids `[x]`.
- `.planning/PROJECT.md` SEVENTH CORRECTION present and positioned after SIXTH; earlier correction blocks byte-unchanged.
- `.planning/STATE.md` `current_phase_name` hand-verified as the full title; `progress.percent` hand-verified; STATE.md diffed before and after every state call, and the returned `updated` array not trusted.
- Both sub-repos: `git -C /workspaces/firestarter status --porcelain` empty and tip `0048b3d`; `git -C /workspaces/firestarter_app status --porcelain` no worse than at plan start.
- The meta-repo commit stages only `.planning/` paths — `git diff --cached --stat` shows no `firestarter` or `firestarter_app` entry.
</verification>

<success_criteria>
- ROADMAP Phase 121 carries the redesign in scope with new criteria and both structural collisions.
- REQUIREMENTS.md carries new Pending ids mapped to Phase 121, the SUB-01/02 reversal recorded, and consistent coverage arithmetic.
- PROJECT.md carries a nine-item SEVENTH CORRECTION block including the `_SRAM_PROTO_IDS` reason correction and the derived-partition record.
- STATE.md reflects Phase 120's position without claiming completion.
- No sub-repo file touched; no requirement wrongly ticked; no phase removed or renumbered.
</success_criteria>

## Artifacts this phase produces (this plan's share)

**New requirement ids:** the Phase 121 `dev test` redesign ids (recommended `DEVTEST-02` through `DEVTEST-06`), all Pending.

**New document sections:** `.planning/PROJECT.md`'s SEVENTH CORRECTION block; new Phase 121 Success Criteria in `.planning/ROADMAP.md`; new Traceability rows and corrected Coverage bullets in `.planning/REQUIREMENTS.md`.

**New symbols / new files:** none — this plan produces no code and no new file.

**Requirement IDs this plan may mark Complete:** **none.** DEVTEST-01 stays `[ ]` (its host half is Phase 121's) and every newly created id lands `[ ]`. All six HOST ids were already closed by plans 120-07 through 120-10 — verify they still read `[x]` and leave them alone.

<output>
Create `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-11-SUMMARY.md` when done.

The SUMMARY must record: that the redesign is an **owned amendment only** and none of it was implemented; the three reversed locked decisions with their in-source citations, recorded *as* a reversal per Phase 119 D-18; both structural collisions with their measured anchors (`derive_plan`'s contract, and 32 of 301); that `--submit`'s wrong-repo behaviour is a **released-artifact fact** already fixed at `e615b4c` on this branch and on `beta`, so no source change was made and the fix reaches users only at the next beta cut; that `gh issue create --label` aborts before creating without a pre-existing label and write access, so a `gh`-first design must assert the negative argv; that SIXTH CORRECTION item 6's KEEP disposition stands while its stated reason is false, and why that mattered to this phase's own predicate design; the new requirement ids created and the corrected coverage arithmetic; and the ROADMAP.md pre-edit and post-edit line counts, as evidence the 2206-line file was edited scope-wise rather than rewritten.
</output>
