---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/catalog/messages.toml
  - firestarter/tools/catalog/messages.toml
  - firestarter/tools/catalog/codegen.py
  - firestarter/include/messages.h
  - firestarter_app/tools/catalog/messages.toml
  - firestarter_app/tools/catalog/codegen.py
  - firestarter_app/firestarter/messages.py
autonomous: true
requirements: [LOCK-02]

must_haves:
  truths:
    - "D-13: the standalone unlock reuses Phase 118's existing ids MSG_INFO_SDP_UNLOCK (0x5E) and MSG_INFO_SDP_UNLOCK_DONE_US (0x5F) so an SDP unlock reads identically however it was triggered. NO new unlock id is added. The lock gets its own new emitted + duration pair, following 118 D-04's separate-literal-ids shape, with no parameterised discriminator."
    - "D-12: OK means 'the sequence was emitted', and the honesty lives in the MESSAGE TEXT, not in a status code. The lock's duration line must state BOTH that the sequence was emitted AND that the protection state cannot be read back. response_code is never written on the SDP path (117 D-05 / 118 D-02, permanently enforced by test_case8_completion_poll_preserves_prior_severity), so a host reading the id alone must not be able to infer a protection state."
    - "D-16: the worst-case per-byte page-load interval is reported in ONE line after the write completes, which requires a third new INFO id. D-13's 'two new ids' counts the LOCK's pair only; D-16's single report line needs its own. Both decisions are satisfied by keeping this to ONE three-repo codegen ritual, which is D-13's real constraint. This resolution is planner-made and recorded here so a verifier does not read the third id as scope creep."
    - "D-13: free ranges after Phase 118 are INFO from 0x60 and WARN from 0x88. MSG_ERR_NOT_SUPPORTED (0xA5) ALREADY EXISTS, so D-06's generic refusal needs NO new ERROR id, and MSG_WARN_SDP_TBLC_EXCEEDED (0x87) is reused by the lock's budget check, so no new WARN id is needed either."
    - "118 D-03's three-repo ritual is the only permitted distribution path: edit tools/catalog/messages.toml in the META repo, then run tools/catalog/sync_to_subrepos.sh. firestarter/include/messages.h and firestarter_app/firestarter/messages.py are CODEGEN OUTPUT."
    - "catalog-sync-check.yml cannot go green for v1.22 catalog work because it checks out both sub-repos at ref main. Known and expected, not this plan's damage. The in-phase proof is a local three-way cmp plus both codegen.py --check gates."
    - "Names must be 32 characters or fewer or messages.h's generated column layout reflows (118-02's finding)."
  prohibitions:
    - "MUST NOT hand-edit firestarter/include/messages.h or firestarter_app/firestarter/messages.py. Both are generated with CI drift gates."
    - "MUST NOT hand-normalise the generated messages.py. Raw codegen output is already ruff-clean and format-stable."
    - "MUST NOT add a new unlock id. D-13 reuses 0x5E and 0x5F."
    - "MUST NOT add a new ERROR id for D-06's refusal. 0xA5 exists."
    - "MUST NOT add CMD_SDP_ or any new constant to firestarter_app/firestarter/constants.py. That is Phase 120 HOST-03; firmware-before-host is non-negotiable."
  artifacts:
    - tools/catalog/messages.toml
    - firestarter/include/messages.h
    - firestarter_app/firestarter/messages.py
  key_links:
    - "firestarter_app/firestarter/codec.py:206-209 logs 'Unknown message ID 0x.. -- catalog out of date?' and DROPS the frame, so a released 3.0.0b11 host degrades gracefully against the three new ids. That graceful-degradation path is what makes firmware-before-host safe."
    - "The new lock pair is consumed by eeprom28c_sdp_lock_execute via the shared bracket helper in Plan 119-04; the page-load id is consumed by eeprom28c_write_execute in Plan 119-08. This plan MUST land first, because the catalog ritual precedes the call sites that emit the ids."
---

<objective>
Add three new INFO-band catalog ids — the lock's emitted/duration pair and the page-load worst-interval
line — through the full three-repo codegen ritual, so the firmware call sites in Plans 119-04 and 119-08
have ids to emit.

Purpose: CONTEXT.md's hard ordering constraint is that the catalog ritual precedes the call sites that
emit the new ids. This plan is behaviourally inert — it adds catalog entries and regenerates two
artifacts, and no firmware code emits them yet.

Output: `messages.toml` carrying `MSG_INFO_SDP_LOCK` (0x60), `MSG_INFO_SDP_LOCK_DONE_US` (0x61) and
`MSG_INFO_PAGE_LOAD_WORST_US` (0x62); both generated artifacts regenerated and drift-free; a local
three-way byte-identity proof.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-02-SUMMARY.md
</context>

## Repo and commit mechanics

This is a meta-repo with two submodules. Both `firestarter/` and `firestarter_app/` are already checked
out on `v1.22-at28c-software-data-protection-lifecycle` (verified at plan time: firmware `1880054`, host
`d3f9128`). **Do not create or switch branches.** Confirm both with `git branch --show-current` before
any write — the milestone-branch check has been a real trap twice. Commit three times:

1. Inside `/workspaces/firestarter` — `tools/catalog/messages.toml`, `tools/catalog/codegen.py`,
   `include/messages.h`.
2. Inside `/workspaces/firestarter_app` — `tools/catalog/messages.toml`, `tools/catalog/codegen.py`,
   `firestarter/messages.py`.
3. In `/workspaces` — `tools/catalog/messages.toml`, the two submodule gitlinks, and the `.planning/`
   SUMMARY.

## Artifacts this phase produces

**Created by THIS plan:** catalog ids `MSG_INFO_SDP_LOCK` (0x60), `MSG_INFO_SDP_LOCK_DONE_US` (0x61),
`MSG_INFO_PAGE_LOAD_WORST_US` (0x62), plus their regenerated appearances in
`firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`.

**Created later in Phase 119 (for orientation, do not create here):** `CMD_SDP_UNLOCK` (9) /
`CMD_SDP_LOCK` (10) and `is_memory_cmd()` in `include/firestarter.h`; `[env:native_nodevtools]` in
`platformio.ini` plus its CI step; `test/native/avr/test_cmd_admission/`;
`firestarter_app/tools/check_is_memory_cmd_no_ifdef.py` with its paired pytest and
`tests/fixtures/planted_ifdef_in_predicate.h`; `EEPROM_SDP_ENABLE[3]` and its `extern` declaration;
`eeprom28c_emit_sdp_sequence_timed()`; `eeprom28c_sdp_lock_execute()` /
`eeprom28c_sdp_unlock_execute()`; `eprom_sdp_lock()` / `eprom_sdp_unlock()`; the
`SDP_FIXED_LOCK_{DIP28_28C256,DIP28_28C64,DIP24_2816,DIP32_28C512_EEPROM}` goldens; the scripted
`micros()` tick queue; the generic NULL-`main` refusal in `operation_utils.cpp`; the page-load
worst-interval tracker; `119-MEASUREMENT.md`; `119-NONREGRESSION.md`.

## Requirement ownership

This plan lands **only the catalog portion** of LOCK-02 (the standalone lock's report surface, D-12 and
D-13). **LOCK-02 stays OPEN** until Plan 119-07 lands — the requirement spans Plans 119-01 (catalog),
119-04 (commands, ops, wiring), 119-06 (report-shape proof) and 119-07 (dispatch proofs, which closes
it). **Do NOT mark LOCK-02 Complete in `REQUIREMENTS.md` from this plan.** This project has had
executors prematurely tick multi-plan requirements four times in Phase 116. The number of requirement
rows this plan may change in `REQUIREMENTS.md` is zero.

<tasks>

<task type="auto">
  <name>Task 1: Add the three INFO ids to the canonical catalog</name>
  <files>tools/catalog/messages.toml</files>
  <read_first>
    - tools/catalog/messages.toml lines 250-300 (the INFO band's tail: MSG_INFO_CMD 0x5D, MSG_INFO_SDP_UNLOCK 0x5E at :276-282, MSG_INFO_SDP_UNLOCK_DONE_US 0x5F at :284-290, then the WARN band comment at :292) — this is the exact insertion point and the exact entry shape to mirror
    - tools/catalog/messages.toml lines 410-430 (the existing MSG_ERR_NOT_SUPPORTED = 0xA5 entry — read it to confirm no new ERROR id is needed and that its params list is empty)
    - tools/catalog/codegen.py (the emitter — read the name-column layout logic so you can see why names over 32 chars reflow the generated header)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-02-SUMMARY.md (the four-id precedent: the 32-char finding, the ritual's exact command sequence, and the expected-red catalog-sync-check framing)
    - firestarter/src/proms/eeprom_28c.cpp lines 425-450 (the existing gh#11 citation comment — its wording is what entry 3's comment must reuse rather than inventing new framing)
  </read_first>
  <action>
Append three `[[messages]]` blocks to `tools/catalog/messages.toml` immediately after the
`MSG_INFO_SDP_UNLOCK_DONE_US` (0x61 is new; 0x5F is the existing one) block and before the WARN band
comment. Mirror 118's pair exactly in field order (`id`, `name`, `severity`, `format`, `params`,
`wire_format`) and in column alignment.

Entry 1 — the lock's "starting" line, the symmetric counterpart of 0x5E:
`id = 0x60`; `name = "MSG_INFO_SDP_LOCK"` (17 chars); `severity = "INFO"`;
`format = "SDP lock: enabling write protection"`; `params = []`; `wire_format = "id_frame"`.

Entry 2 — the lock's duration line, and the ONE line that carries D-12's honesty requirement:
`id = 0x61`; `name = "MSG_INFO_SDP_LOCK_DONE_US"` (25 chars); `severity = "INFO"`;
`format = "SDP lock sequence emitted in %lu us; protection state is not readable"`;
`params = [{ type = "u32", render = "dec" }]`; `wire_format = "id_frame"`.

The two clauses in entry 2's format string are **mandatory, not stylistic**: that the sequence was
emitted, and that the protection state cannot be read back. Exact wording is discretion per CONTEXT.md
`### Claude's Discretion`, but both clauses must be present in that single line. Rationale to record in
the SUMMARY: `response_code` is deliberately never written on the SDP path, so an untouched path reports
OK — per D-12 the honesty must live in the text, never in a status code the host could misread. This is
also what lets Phase 120's HOST-05 avoid manufacturing a state boolean, and it is why reporting the DQ6
toggle poll's outcome as lock evidence was rejected (a settled toggle bit proves a write cycle finished,
not that protection latched — FIX-02's deleted mistake in a new costume).

Entry 3 — D-16's page-load worst-interval line:
`id = 0x62`; `name = "MSG_INFO_PAGE_LOAD_WORST_US"` (27 chars); `severity = "INFO"`;
`format = "Page load worst byte interval %lu us"`; `params = [{ type = "u32", render = "dec" }]`;
`wire_format = "id_frame"`.

Add a short TOML comment above entry 3 naming it as D-16's single-line report (Plan 119-08's call site),
stating that it is a MEASUREMENT with no budget comparison attached — a per-byte runtime compare in the
hot path is what 118 D-10 declined and D-16 preserves that declination — and framing gh#11 as a
**conflation** bug per Phase 117's finding, never as a sampling-rate bug. Reuse the wording already in
`eeprom_28c.cpp`'s existing gh#11 citation comment.

Do not touch the WARN band: `MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) is reused by the lock's budget check via
the shared helper, so no new WARN id is needed. Do not add an ERROR id: 0xA5 exists and is already
`eprom_erase`'s refusal id.
  </action>
  <verify>
    <automated>grep -c 'MSG_INFO_SDP_LOCK"\|MSG_INFO_SDP_LOCK_DONE_US"\|MSG_INFO_PAGE_LOAD_WORST_US"' /workspaces/tools/catalog/messages.toml && grep -c '0x60\|0x61\|0x62' /workspaces/tools/catalog/messages.toml</automated>
  </verify>
  <acceptance_criteria>
    - `tools/catalog/messages.toml` contains exactly three new `[[messages]]` blocks, at ids `0x60`, `0x61`, `0x62`, all `severity = "INFO"`, all `wire_format = "id_frame"`
    - The names are `MSG_INFO_SDP_LOCK`, `MSG_INFO_SDP_LOCK_DONE_US`, `MSG_INFO_PAGE_LOAD_WORST_US`, each 32 characters or fewer
    - `0x61`'s `format` string contains BOTH a clause stating the sequence was emitted AND a clause stating the protection state is not readable
    - `0x61` and `0x62` each declare exactly one `u32` param with `render = "dec"`; `0x60` declares an empty `params` list
    - `git diff tools/catalog/messages.toml` shows additions only, all inside the INFO band, and shows no change to the 0x5E or 0x5F blocks, no new ERROR id and no new WARN id
    - Entry 3 carries a comment naming it as a measurement with no budget comparison, and framing gh#11 as a conflation bug
    - The verify command prints `OK 3 new INFO ids present, unique, names<=32`
  </acceptance_criteria>
  <done>Three new INFO entries exist in the canonical catalog with the lock's two honesty clauses in 0x61's format string; no other band was touched and no id or name collides.</done>
</task>

<task type="auto">
  <name>Task 2: Run the three-repo ritual and prove byte-identity plus drift-free codegen</name>
  <files>firestarter/tools/catalog/messages.toml, firestarter/tools/catalog/codegen.py, firestarter/include/messages.h, firestarter_app/tools/catalog/messages.toml, firestarter_app/tools/catalog/codegen.py, firestarter_app/firestarter/messages.py</files>
  <read_first>
    - tools/catalog/sync_to_subrepos.sh (read whole — it copies messages.toml and codegen.py into both sub-repos, regenerates firestarter/include/messages.h via --language cpp and firestarter_app/firestarter/messages.py via --language python, and asserts the two vendored TOML copies are byte-identical)
    - .github/workflows/catalog-sync-check.yml lines 40-60 (the cmp job that pins meta vs firmware vs host — read it so you can state precisely why it is expected-red on a milestone branch: it checks out both sub-repos at ref main)
    - firestarter/.github/workflows/build.yml lines 55-70 (the firmware codegen --check drift gate — the exact command shape to re-run locally)
    - firestarter/include/messages.h first 40 lines (the generated-file banner, so you can confirm you are never hand-editing it)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md section 4 and section 6 (the flash provenance shape and the known-and-explained-conditions discipline this task's records must follow)
  </read_first>
  <action>
Run `bash tools/catalog/sync_to_subrepos.sh` from `/workspaces`. It is idempotent. Expect `copied:` lines
for both files into both sub-repos, `OK: sub-repo catalogs are byte-identical.`, both regeneration lines,
and `OK: catalog synced to both sub-repos.`

Then prove the ritual landed, with four independent checks.

1. **Three-way TOML byte-identity.** `cmp` the meta `tools/catalog/messages.toml` against
   `firestarter/tools/catalog/messages.toml` and against `firestarter_app/tools/catalog/messages.toml`.
   All three must be byte-identical. This local `cmp` is the in-phase substitute for
   `catalog-sync-check.yml`, which cannot go green for v1.22 catalog work because it pins both sub-repos
   at `ref: main`. Record that framing in the SUMMARY as known-and-expected, exactly as
   `118-NONREGRESSION.md` did, never as this phase's damage.

2. **Firmware drift gate.** From `/workspaces/firestarter`, run `python3 tools/catalog/codegen.py` with
   `--catalog tools/catalog/messages.toml --language cpp --target include/messages.h --check` (the same
   invocation `build.yml` uses), then `git diff --exit-code -- include/messages.h` after staging, proving
   the committed artifact equals what the generator emits.

3. **Host drift gate.** The same two steps in `/workspaces/firestarter_app` for
   `firestarter/messages.py` with `--language python`.

4. **Generated content spot-check.** Confirm all three new symbol names appear in
   `firestarter/include/messages.h` and in `firestarter_app/firestarter/messages.py`, and that
   `MSG_INFO_SDP_UNLOCK`, `MSG_INFO_SDP_UNLOCK_DONE_US` and `MSG_ERR_NOT_SUPPORTED` are still present in
   both.

Do **not** hand-normalise `messages.py`. Its raw codegen output is already ruff-clean and format-stable;
re-formatting it desynchronises it from the generator and turns the drift gate red. If `ruff format
--check` disagrees with the emitted file, that is a codegen defect to report, not a file to fix here.

Then confirm the host lint still passes against the **CI** Python targets, not the devcontainer's 3.12:
run `ruff check` and `ruff format --check` in `/workspaces/firestarter_app` and record which target
version you validated against (CI pins py3.9 and py3.11 for host lint; the codegen job pins 3.11).

Re-run the host gates that scan firmware source, all of which must stay at their measured baseline
because this plan touches no firmware source:
`python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py -q`
(baseline 21 passed), `python3 tools/check_no_log_in_sdp_window.py` (baseline: the `PASS:` line naming
emitter lines 222-238 and completion-poll lines 272-285, exit 0), and `python3 tools/check_dispatch.py`
(exit 0). Two modules are known-RED and are NOT this plan's damage: `tests/test_audit_coverage_matrix.py`
(stale golden) and `tests/test_no_programmer_found_*` (goes red with a live board attached, and three
boards are attached right now). Name them if they surface; do not chase them.

Finally build the firmware to confirm the regenerated header still compiles: `pio run` from
`/workspaces/firestarter`, which links `uno`, `uno328pb` and `leonardo`. Record the three flash and RAM
figures. Baseline at phase base `1880054` is Leonardo 25680/28672, Uno 23542/32256, uno328pb
23592/32384. Three currently-unreferenced catalog entries should move flash by only their PROGMEM format
strings plus three table rows; record whatever the measurement says. This is the base Plan 119-10's
LOCK-06 arithmetic starts from.
  </action>
  <verify>
    <automated>cmp /workspaces/tools/catalog/messages.toml /workspaces/firestarter/tools/catalog/messages.toml && cmp /workspaces/tools/catalog/messages.toml /workspaces/firestarter_app/tools/catalog/messages.toml && grep -c 'MSG_INFO_SDP_LOCK_DONE_US' /workspaces/firestarter/include/messages.h /workspaces/firestarter_app/firestarter/messages.py</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/catalog/sync_to_subrepos.sh` completed and printed `OK: catalog synced to both sub-repos.`
    - Both `cmp` invocations in the verify command succeed (meta vs firmware vs host `messages.toml` byte-identical)
    - `grep -c 'MSG_INFO_SDP_LOCK_DONE_US'` reports a non-zero count for BOTH `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`; the same holds for `MSG_INFO_SDP_LOCK` and `MSG_INFO_PAGE_LOAD_WORST_US`
    - `codegen.py --check` exits 0 in both sub-repos, and `git diff --exit-code` against the staged generated artifact exits 0 in both
    - `messages.py` was not hand-edited or reformatted (its diff contains only generator-emitted lines for the three new ids)
    - `ruff check` and `ruff format --check` pass in `firestarter_app`, validated against the py3.9/3.11 CI targets and the version recorded
    - `python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py -q` reports 21 passed
    - `python3 tools/check_no_log_in_sdp_window.py` prints its `PASS:` line and exits 0; `python3 tools/check_dispatch.py` exits 0
    - `pio run` reports SUCCESS for all three of `uno`, `uno328pb`, `leonardo`, and the three flash/RAM figures are recorded in the SUMMARY
    - `git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short` are clean after the three commits
    - `REQUIREMENTS.md` is unchanged by this plan (no requirement row was ticked)
  </acceptance_criteria>
  <done>The three new ids exist byte-identically in all three repos, both generated artifacts are drift-free under `--check`, the firmware builds on all three envs, and every host gate is at its measured baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical catalog to generated artifacts | A hand-edit of either generated file diverges the wire contract from its single source of truth |
| firmware message id space to a released host | A new id reaching a `3.0.0b11` host that has never seen it |
| message text to a host's inference | Text that lets a host infer a protection state the firmware cannot read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-01-HANDEDIT | Tampering | `include/messages.h`, `firestarter/messages.py` | high | mitigate | Only `sync_to_subrepos.sh` writes them; `codegen.py --check` plus `git diff --exit-code` are acceptance criteria in both sub-repos |
| T-119-01-DRIFT | Tampering | three vendored `messages.toml` copies | high | mitigate | Three-way `cmp` is an acceptance criterion; the sync script's own cross-sub-repo invariant runs first |
| T-119-01-FABSTATE | Repudiation | `0x61`'s format string | critical | mitigate | D-12: the format string must carry both the emitted clause and the not-readable clause; asserted as an acceptance criterion so the honesty cannot be dropped in wording review |
| T-119-01-UNKNOWNID | Denial of service | released `3.0.0b11` host | low | accept | `codec.py:206-209` logs and drops an unknown id rather than crashing; verified in tree, no change needed |
| T-119-01-HOSTLEAK | Elevation of privilege | `firestarter_app/firestarter/constants.py` | medium | mitigate | Explicit prohibition: no `CMD_SDP_*` in `constants.py` this phase. Only generated catalog code may change in the host repo |
| T-119-01-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. No npm/pip/cargo install step exists anywhere in Phase 119, so the Package Legitimacy Gate does not apply |
</threat_model>

<verification>
1. `git branch --show-current` in both sub-repos reads `v1.22-at28c-software-data-protection-lifecycle`.
2. Three-way `cmp` of `messages.toml` succeeds across meta, firmware and host.
3. `codegen.py --check` exits 0 in both sub-repos; `git diff --exit-code` on both generated artifacts exits 0.
4. All three new names appear in both generated artifacts; 0x5E, 0x5F and 0xA5 are unchanged.
5. `pio run` SUCCESS on all three AVR envs with the flash figures recorded.
6. Host gate baseline holds: 21/21 on the four scanning modules, `check_no_log_in_sdp_window.py` exit 0, `check_dispatch.py` exit 0; the two known-RED modules named and not chased.
7. Three commits. Suggested subjects: `feat(119-01): add SDP lock report pair and page-load worst-interval catalog ids` inside each sub-repo, and `docs(119-01): sync catalog to both sub-repos (LOCK-02 catalog portion)` in the meta repo.
8. `REQUIREMENTS.md` untouched — LOCK-02 stays open until Plan 119-07.
</verification>

<success_criteria>
- `MSG_INFO_SDP_LOCK` (0x60), `MSG_INFO_SDP_LOCK_DONE_US` (0x61) and `MSG_INFO_PAGE_LOAD_WORST_US` (0x62) exist in the canonical catalog and in both generated artifacts, byte-identically distributed.
- 0x61's text satisfies D-12 in words: sequence emitted, protection state not readable.
- No new unlock id, no new ERROR id, no new WARN id, no host `constants.py` change.
- Both drift gates green; the expected-red `catalog-sync-check.yml` framing recorded rather than chased.
- The firmware still builds on all three AVR envs, with figures recorded as LOCK-06's arithmetic base.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-01-SUMMARY.md` when done.
Record: the three ids with their exact final format strings; the three-way `cmp` result; both
`--check` outcomes; the three flash/RAM figures; the ruff target version validated; the two known-RED
host modules named as pre-existing; and an explicit note that LOCK-02 remains open pending Plan 119-07.
</output>
