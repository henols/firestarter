---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 02
type: execute
wave: 2
depends_on: [119-01]
files_modified:
  - firestarter/platformio.ini
  - firestarter/.github/workflows/build.yml
  - firestarter/CLAUDE.md
  - firestarter/include/firestarter.h
  - firestarter/src/firestarter.cpp
  - firestarter/test/native/avr/test_cmd_admission/test_cmd_admission.cpp
  - firestarter/test/native/avr/test_cmd_admission/host_stubs.cpp
  - firestarter/test/native/avr/test_cmd_admission/avr/pgmspace.h
autonomous: true
requirements: [LOCK-03, LOCK-02]

must_haves:
  truths:
    - "D-01: is_memory_cmd() enumerates exactly {CMD_READ, CMD_WRITE, CMD_ERASE, CMD_BLANK_CHECK, CMD_CHECK_CHIP_ID, CMD_VERIFY, CMD_SDP_UNLOCK, CMD_SDP_LOCK} and the resulting behaviour change for cmd 7 and 8 is TAKEN, not preserved. Today a RELEASE build (no -D DEV_TOOLS) runs json_parse AND configure_memory for CMD_DEV_ADDRESS (7) and CMD_DEV_REGISTER (8) before rejecting them at loop()'s default with MSG_ERR_UNKNOWN_CMD, i.e. it configures a memory handler for a command it will refuse. An honest enumeration excludes 7 and 8, so a release build stops doing that. This is a deliberate SAFETY TIGHTENING and must be recorded as one in the source and in the SUMMARY, never as an unremarked refactor side effect. Cmd 7 and 8 keep MSG_ERR_UNKNOWN_CMD in a release build."
    - "D-02: is_memory_cmd() carries no preprocessor conditional of any kind inside its body. The predicate's whole value is that it reads the same in both build configurations. Plan 119-03's source-scan gate makes this machine-checked; this plan makes it true."
    - "D-03: the two free command slots are 9 and 10, and both sit ABOVE CMD_DEV_ADDRESS 7. That is exactly why LOCK-03 is a PREREQUISITE for LOCK-02 and not a parallel cleanup: with the ordinal guard in place, a DEV_TOOLS build would route cmd 9 and 10 into the dev-tools branch and never call configure_memory, so the new commands would have no bus configuration at all."
    - "D-04: LOCK-03 is proven by a SECOND native env AND a source-scan gate. [env:native_nodevtools] overrides build_flags to omit -D DEV_TOOLS, which is necessary because -D DEV_TOOLS lives in the shared [env] block at platformio.ini:26 and is inherited by all three AVR envs AND native. A no-DEV_TOOLS build had never been compiled before this milestone. The same truth-table suite runs in BOTH envs, giving a semantic proof rather than a textual one."
    - "D-04 resolves the folded todo prove-pio-dev-flag-fails-closed.md ITEM 4 ONLY (pio test passes with DEV_TOOLS absent) as a by-product. Items 1 through 3 stay with 999.15 / gh#8 and MUST NOT be attempted here."
    - "RESEARCH F-C, load-bearing and not recorded in CONTEXT.md: CMD_DEV_ADDRESS and CMD_DEV_REGISTER are themselves inside a DEV_TOOLS conditional in firestarter.h:42-45. The truth-table suite therefore MUST NOT name those macros; it uses numeric literals 7 and 8 with a citation comment, exactly as firestarter_app/tests/test_revision_constants_parity.py:110-112 already does for the same reason."
    - "RESEARCH F-F, structurally decisive: [env:native] compiles only src/proms/, src/boards/rurp_serial_utils.cpp and src/json_parser.c. firestarter.cpp is NOT compiled natively and no stub supplies its symbols. The predicate must therefore be static inline IN A HEADER to be natively linkable at all."
    - "RESEARCH F-B2, a THIRD behaviour delta beyond cmd 7 and 8: CMD_IDLE (0) today satisfies cmd < CMD_READ_VPP in both configurations, so {\"cmd\":0} runs json_parse and configure_memory and produces two error frames (0xBB then MSG_ERR_SETUP). After is_memory_cmd() it falls to loop()'s existing case CMD_IDLE: break; and produces silence. Accepted deliberately and recorded; not discovered by a verifier."
    - "PATTERNS: a second live ordinal-range guard exists at firestarter.cpp:124 gating three DEBUG lines. Its scope decision is made explicitly in this plan, not left for a verifier to find."
    - "Baselines to hold: pio test -e native 112/112 across 16 suites, pio run 3/3 SUCCESS. Any reduction is a regression, not a trade-off."
  prohibitions:
    - "MUST NOT put any preprocessor conditional inside is_memory_cmd()'s body. That recreates the divergence LOCK-03 exists to remove and makes the invariance test pass vacuously."
    - "MUST NOT name CMD_DEV_ADDRESS or CMD_DEV_REGISTER in the truth-table suite. Those macros do not exist in native_nodevtools and the suite would fail to compile there."
    - "MUST NOT add native_nodevtools to default_envs at platformio.ini:16. pio run would try to link a main()-less target."
    - "MUST NOT use TEST_IGNORE_MESSAGE anywhere. An IGNORED result does not demonstrate anything."
    - "MUST NOT attempt items 1 through 3 of prove-pio-dev-flag-fails-closed.md (the sysenv fail-open/fail-closed matrix and avr-nm symbol capture). Those belong to 999.15 / gh#8."
    - "MUST NOT add CMD_SDP_UNLOCK or CMD_SDP_LOCK to firestarter_app/firestarter/constants.py. Phase 120 HOST-03."
    - "MUST NOT wire the two new command values to any handler in this plan. The defines and the predicate land here; configure_eeprom28c's arms and loop()'s case arms land in Plan 119-04."
  artifacts:
    - firestarter/include/firestarter.h
    - firestarter/src/firestarter.cpp
    - firestarter/platformio.ini
    - firestarter/.github/workflows/build.yml
    - firestarter/test/native/avr/test_cmd_admission/test_cmd_admission.cpp
    - firestarter/test/native/avr/test_cmd_admission/host_stubs.cpp
    - firestarter/test/native/avr/test_cmd_admission/avr/pgmspace.h
  key_links:
    - "[env:native] uses a POSITIVE test_filter allowlist plus a parallel -I list, both 16 entries long. A suite is invisible until it appears in BOTH. The new suite needs a line in each list in BOTH envs, i.e. four new lines total."
    - "-D HARDWARE_REVISION is load-bearing in the new env, not incidental: test/native/avr/_shared/host_stubs_common.inc:208-224 gates four hardware-revision stubs on it, and HOST_STUBS_REAL_REGISTER_UTILS suppresses them precisely because [env:native] inherits that flag. Dropping it changes which stubs compile and breaks the SDP suites."
    - "firestarter/.github/workflows/build.yml triggers on push/pull_request to main only, so the new CI step will not fire on the milestone branch. The in-phase proof is the local run, exactly as with catalog-sync-check.yml."
---

<objective>
Replace the `#ifdef DEV_TOOLS`-conditional ordinal admission guard with an explicit `is_memory_cmd()`
predicate, define `CMD_SDP_UNLOCK` (9) and `CMD_SDP_LOCK` (10), stand up a second native environment
that compiles without `-D DEV_TOOLS`, and prove the predicate's accepted set is identical in both build
configurations by running one exhaustive truth-table suite in both envs.

Purpose: LOCK-03 is a hard prerequisite for LOCK-02 (D-03). Until the ordinal guard is gone, a
`-D DEV_TOOLS` build routes cmd 9 and 10 into the dev-tools branch and never calls `configure_memory`,
so the new commands would have no bus configuration at all. The predicate is also the phase's access
control gate: it decides which commands may configure the hardware bus, and `configure_eprom` enables
the 12 V VPP boost regulator, which is a hazard on a 5 V part.

Output: `is_memory_cmd()` in `firestarter.h`; the two new command values; `[env:native_nodevtools]` with
its own full `test_filter` and `-I` lists; a CI step for the second env; a new `test_cmd_admission` suite
asserting the predicate over every value in [0, 255] and passing in both envs; a corrected
`firestarter/CLAUDE.md` claim about new native suites.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-VALIDATION.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-01-SUMMARY.md
@.planning/todos/prove-pio-dev-flag-fails-closed.md
</context>

## Repo and commit mechanics

All work lands inside the `firestarter/` submodule, already on
`v1.22-at28c-software-data-protection-lifecycle`. Confirm with `git branch --show-current` before writing.
Commit inside the submodule; then one meta commit staging the submodule gitlink plus the `.planning/`
SUMMARY. `firestarter_app/` must be untouched by this plan.

Firmware plans in this phase are **strictly sequential, not merely wave-ordered**: every plan invokes
`pio` against the single shared `firestarter/.pio/build/` tree, so concurrent runs corrupt each other's
outputs. Do not start this plan before 119-01's commits exist.

## Artifacts this phase produces

**Created by THIS plan:** `CMD_SDP_UNLOCK` (9) and `CMD_SDP_LOCK` (10);
`static inline bool is_memory_cmd(uint8_t cmd)` in `include/firestarter.h`; `[env:native_nodevtools]` in
`platformio.ini`; a `pio test -e native_nodevtools` step in `.github/workflows/build.yml`; the suite
`test/native/avr/test_cmd_admission/` with `test_cmd_admission.cpp`, `host_stubs.cpp` and
`avr/pgmspace.h`; four new `test_filter` / `-I` lines (two per env); a corrected staleness claim in
`firestarter/CLAUDE.md`.

**Created earlier:** the three catalog ids `MSG_INFO_SDP_LOCK` (0x60), `MSG_INFO_SDP_LOCK_DONE_US` (0x61),
`MSG_INFO_PAGE_LOAD_WORST_US` (0x62) — Plan 119-01.

**Created later:** `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py` with its pytest and
`tests/fixtures/planted_ifdef_in_predicate.h` — Plan 119-03; `EEPROM_SDP_ENABLE[3]`,
`eeprom28c_emit_sdp_sequence_timed()`, `eeprom28c_sdp_lock_execute()`,
`eeprom28c_sdp_unlock_execute()`, `eprom_sdp_lock()`, `eprom_sdp_unlock()` and the two `loop()` case arms
— Plan 119-04; the `SDP_FIXED_LOCK_*` goldens and the scripted `micros()` queue — Plan 119-05; the
generic NULL-`main` refusal and the widened `build_src_filter` — Plan 119-07; the page-load tracker —
Plan 119-08; `119-NONREGRESSION.md` — Plan 119-10; `119-MEASUREMENT.md` — Plan 119-11.

## Requirement ownership

This plan lands **LOCK-03's firmware and semantic-proof half**: the predicate, the guard replacement, the
second env, and the two-env truth table. **LOCK-03 stays OPEN** until Plan 119-03 lands D-04's textual
source-scan gate with its planted-violation fixture, which is the other half of D-04's proof shape.

It also lands **only the two command DEFINES** of LOCK-02 (D-03's prerequisite ordering). **LOCK-02 stays
OPEN** until Plan 119-07. **Do not mark LOCK-02 or LOCK-03 Complete in `REQUIREMENTS.md` from this plan.**
The only requirement rows this plan may change is none.

<tasks>

<task type="auto">
  <name>Task 1: Stand up [env:native_nodevtools], its CI step, and correct CLAUDE.md's stale suite claim</name>
  <files>firestarter/platformio.ini, firestarter/.github/workflows/build.yml, firestarter/CLAUDE.md</files>
  <read_first>
    - firestarter/platformio.ini read whole (lines 11-16 the default_envs constraint and its comment; lines 18-27 the shared [env] block where -D DEV_TOOLS lives at :26; lines 69-156 [env:native] with its 16-entry test_filter at :102-118, its parallel 16-entry -I list at :123-138, lib_deps, build_src_filter and test_build_src)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc lines 200-230 (the four HARDWARE_REVISION-gated stubs — this is why -D HARDWARE_REVISION must be spelled out explicitly in the new env)
    - firestarter/.github/workflows/build.yml lines 80-115 (the "Run native unit tests" step at :85-91 and the "Build PlatformIO Project" step — the sibling step shape to copy, including its Phase-6 WR-01 rationale comment)
    - firestarter/CLAUDE.md, the section containing "Reuse pattern for future native tests" (it claims platformio.ini needs no change for a new suite — that claim is STALE; the later positive test_filter allowlist made it false)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-N and F-G (the empirically validated shape of the new env, and the measured 112/112 result it produced)
  </read_first>
  <action>
Append a new `[env:native_nodevtools]` block to `firestarter/platformio.ini`, copying `[env:native]`'s
shape with exactly three deliberate differences. RESEARCH F-G already validated this shape empirically in
a throwaway experiment that passed 112/112.

The three differences:
1. `build_flags` must NOT begin with `${env.build_flags}`. Spell out `-D MONITOR_SPEED=250000` and
   `-D HARDWARE_REVISION` instead, and omit `-D DEV_TOOLS`. `-D HARDWARE_REVISION` is load-bearing: four
   hardware-revision stubs in `_shared/host_stubs_common.inc` are gated on it, and dropping it changes
   which stubs compile and breaks the SDP suites.
2. Duplicate the **whole** `test_filter` list and the **whole** `-I` list. RESEARCH resolved the "full
   filter or a subset" discretion item in favour of the full filter: measured zero test-porting cost
   (no test file references `DEV_TOOLS` or `CMD_DEV_*` anywhere) and roughly 52 s of cold-build CI time,
   in exchange for DEV_TOOLS-invariance coverage across all 16 suites rather than just the truth table.
3. Do **not** add the new env to `default_envs` at `platformio.ini:16`. `pio run` would try to link a
   `main()`-less target with "undefined reference to main", the exact failure that constraint exists to
   prevent.

Everything else matches `[env:native]`: `platform = native`, `test_framework = unity`, `-std=gnu++17`,
`-I include`, `-D RURP_BOARD_NAME=\"native\"`, `lib_deps = fabiobatsilva/ArduinoFake@^0.4.0`,
`build_src_filter = +<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c>`, `test_build_src = yes`.

Add a provenance comment block above the new env, following the comment discipline the rest of this file
uses: name it as v1.22 Phase 119 D-04's second env; state that `-D DEV_TOOLS` lives in the shared `[env]`
block and is inherited by all three AVR envs and by `native`, so a build without it had never been
compiled or tested before this milestone; state that this env exists to make LOCK-03's
DEV_TOOLS-invariance a semantic proof rather than a textual one; and state that it discharges item 4 only
of `.planning/todos/prove-pio-dev-flag-fails-closed.md`, with items 1 through 3 belonging to 999.15 /
gh#8.

**Do not add the `test_cmd_admission` lines yet.** At this point the suite directory does not exist, and
a `test_filter` entry for a missing directory is a fail-for-no-reason. Task 3 adds the four lines once the
suite exists. That ordering keeps this task independently green.

Then prove the env works before it carries any new suite: run `pio test -e native` (expect 112/112 across
16 suites, the measured baseline) and `pio test -e native_nodevtools` (expect the same 112/112 with zero
test-code changes). Also run `pio run` (expect 3/3 SUCCESS). Record all three outcomes; the
`native_nodevtools` result is the empirical answer to the folded todo's item 4 and must be recorded
against that todo file so 999.15 inherits it.

Add one CI step to `firestarter/.github/workflows/build.yml`, immediately after the existing "Run native
unit tests" step, running `pio test -e native_nodevtools`. Give it a comment naming v1.22 Phase 119 D-04
and stating what it proves (the admission predicate behaves identically without `-D DEV_TOOLS`). Note in
the SUMMARY that the workflow triggers on `main` only, so this step will not fire on the milestone
branch; the in-phase proof is the local run, exactly as with `catalog-sync-check.yml`.

Finally correct `firestarter/CLAUDE.md`. Its "Reuse pattern for future native tests" section claims the
`[env:native]` configuration needs no changes for new suites. That is false: the positive `test_filter`
allowlist means a suite is invisible until it appears in `test_filter` **and** has a matching `-I` entry.
Rewrite that claim to state the real requirement (both lists, and now in **both** native envs) and cite
`platformio.ini`'s `test_filter` block. This is a cheap doc correction that prevents the next phase
repeating the error.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'native_nodevtools' platformio.ini .github/workflows/build.yml && pio test -e native_nodevtools 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `platformio.ini` contains an `[env:native_nodevtools]` block whose `build_flags` spells out `-D MONITOR_SPEED=250000` and `-D HARDWARE_REVISION` and does not reference `${env.build_flags}`
    - That block's `test_filter` and `-I` lists each contain the same 16 entries as `[env:native]`'s (verified by comparing the two lists entry by entry)
    - `platformio.ini:16`'s `default_envs` line is unchanged and still reads `uno, uno328pb, leonardo`
    - The new env carries a provenance comment naming D-04, the shared-`[env]` inheritance of `-D DEV_TOOLS`, and the folded todo's item 4 as the only item discharged
    - `pio test -e native` reports 112/112 test cases across 16/16 suites PASSED
    - `pio test -e native_nodevtools` reports 112/112 test cases across 16/16 suites PASSED
    - `pio run` reports SUCCESS for `uno`, `uno328pb` and `leonardo`
    - `.github/workflows/build.yml` contains a step running `pio test -e native_nodevtools`, placed after the existing `pio test -e native` step, with a comment naming D-04
    - `firestarter/CLAUDE.md` no longer claims `platformio.ini` needs no change for a new native suite, and instead names both the `test_filter` and `-I` requirements in both native envs
    - `.planning/todos/prove-pio-dev-flag-fails-closed.md` records item 4 as answered with the measured result, and items 1 through 3 still open and untouched
  </acceptance_criteria>
  <done>A second native env exists that compiles without `-D DEV_TOOLS`, both envs pass 112/112 with zero test-code changes, the CI step is wired, the folded todo's item 4 is answered against the todo file, and CLAUDE.md's stale suite claim is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Define CMD_SDP_UNLOCK / CMD_SDP_LOCK, add is_memory_cmd(), and replace the ordinal guard</name>
  <files>firestarter/include/firestarter.h, firestarter/src/firestarter.cpp</files>
  <read_first>
    - firestarter/include/firestarter.h lines 30-80 (the CMD block at :34-51 with the DEV_TOOLS-conditional CMD_DEV_ADDRESS 7 / CMD_DEV_REGISTER 8 at :42-45, CMD_READ_VPP 11 at :47, then RESPONSE_CODE_* and the FLAG_* block; and FLAG_SKIP_SDP_UNLOCK 0x100 at :70-76 whose comment block is the exact shape to copy for a firmware-only-this-phase addition)
    - firestarter/include/operation_utils.h lines 41-51 (set_operation_in_progress / is_operation_in_progress — the in-tree `static inline` header-helper precedent to copy)
    - firestarter/src/firestarter.cpp lines 52-140 (parse_json, the guard at :76-95 being replaced, and the SECOND ordinal-range guard at :128 gating three DEBUG lines)
    - firestarter/src/firestarter.cpp lines 196-255 (loop()'s command switch, its `case CMD_IDLE: break;` at the CMD_IDLE arm, the DEV_TOOLS-conditional cmd 7/8 arms, and the `default: LOG_ERROR_ID_U8(MSG_ERR_UNKNOWN_CMD, handle.cmd)` arm that cmd 7 and 8 will fall to in a release build)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md sections F-B, F-B2, F-C and F-F (the full 16-row command table for both build configurations, the CMD_IDLE delta, the conditional CMD_DEV_* definitions, and why the predicate must be header-inline)
  </read_first>
  <action>
Three edits in `include/firestarter.h`, then one in `src/firestarter.cpp`.

**Edit 1 — the two new command values.** Add `#define CMD_SDP_UNLOCK 9` and `#define CMD_SDP_LOCK 10`
into the CMD block, placed after the DEV_TOOLS-conditional `CMD_DEV_*` pair and before
`#define CMD_READ_VPP 11`, so the block reads in numeric order. Both defines are **unconditional** —
outside any preprocessor conditional. Give them a comment block copying `FLAG_SKIP_SDP_UNLOCK`'s shape at
`:70-76`: state that these are the standalone SDP-enable / SDP-disable commands on protocol `0x0D`, that
slots 9 and 10 were the only free values and both sit above `CMD_DEV_ADDRESS 7` which is why the ordinal
guard had to go first (D-03), and that the host CLI surface (`dev sdp` and `constants.py` / `COMMAND_NAMES`)
arrives in Phase 120 HOST-01 / HOST-03 — firmware-before-host.

**Edit 2 — the predicate.** Add, immediately after the CMD block and before the `RESPONSE_CODE_*`
defines:

`static inline bool is_memory_cmd(uint8_t cmd)` returning true for exactly the eight values
`CMD_READ`, `CMD_WRITE`, `CMD_ERASE`, `CMD_BLANK_CHECK`, `CMD_CHECK_CHIP_ID`, `CMD_VERIFY`,
`CMD_SDP_UNLOCK`, `CMD_SDP_LOCK`, and false for every other value. Spell it as a `switch` over those
eight `case` labels with `return true;` and a `default: return false;`, or as a disjunction of the eight
named macros — either is acceptable, but the body must name the eight `CMD_*` macros (not bare numbers)
so the enumeration and the defines above cannot silently drift apart.

Three hard constraints on this function:
- **No preprocessor conditional of any kind inside the body.** All eight named macros are
  unconditionally defined, so none is needed. Plan 119-03's gate will enforce this textually; D-02 is why.
- It must be `static inline` **in this header**, not in a `.cpp`. `[env:native]`'s `build_src_filter`
  compiles only `src/proms/`, `src/boards/rurp_serial_utils.cpp` and `src/json_parser.c`, so a definition
  in `firestarter.cpp` or a new translation unit would not link into any native test binary and D-04's
  truth-table suite could not exist. `operation_utils.h:41-51` is the in-tree precedent for this shape.
- The body must **not** name `CMD_DEV_ADDRESS` or `CMD_DEV_REGISTER`. Those macros only exist under
  `-D DEV_TOOLS`.

Give the predicate a comment block that records, in the source, three things a verifier must not have to
reconstruct: (a) that this replaces the `#ifdef DEV_TOOLS`-conditional ordinal guard at the old
`firestarter.cpp:79`, and that the conditional there was **structurally forced**, not lazy, because
`CMD_DEV_ADDRESS` does not exist in a release build — the predicate removes the conditional by not naming
those symbols at all; (b) that excluding cmd 7 and 8 is a **deliberate safety tightening** taken under
D-01: a release build currently configures a memory handler for dev commands it will then refuse at
`loop()`'s `default:` with `MSG_ERR_UNKNOWN_CMD`, and after this change it does not, while cmd 7 and 8
keep their `MSG_ERR_UNKNOWN_CMD` refusal in a release build; (c) that this predicate is an access-control
gate, not hygiene — it decides which commands may configure the hardware bus, and `configure_eprom`
enables the 12 V VPP boost regulator, a hazard on a 5 V part.

**Edit 3 — the guard replacement in `src/firestarter.cpp`.** Replace the guard at `:76-95` so the outer
`if (handle->cmd < CMD_READ_VPP)` and the `json_parse` call are unchanged, the inner test becomes
`if (is_memory_cmd(handle->cmd))` with **no** surrounding preprocessor conditional, the five
`LOG_DEBUG_ID_SUB_U8` flag lines and the `op_execute_function(configure_memory, handle)` call plus its
`MSG_ERR_SETUP` failure path stay exactly as they are, and the `else` branch becomes unconditional with
the `DEV_TOOLS` conditional moved **inside** its body so it wraps only the two
`LOG_DEBUG_ID_SUB_U8(DBG_FLAG_OUTPUT_EN, ...)` / `LOG_DEBUG_ID_SUB_U8(DBG_FLAG_CHIP_EN, ...)` lines. In a
release build the `else` body then compiles empty, which is correct and intended. The result is that
neither the predicate nor the branch structure carries a build-configuration conditional — only two debug
log lines do.

**The second ordinal guard, decided explicitly.** `firestarter.cpp:124` carries an independent
`handle->cmd > CMD_IDLE && handle->cmd < CMD_READ_VPP` range test gating three `LOG_DEBUG_ID_SUB_*` lines
(`DBG_MEM_SIZE`, `DBG_ADDR_MASK`, `DBG_MATCH_LINES`). **Leave it as it is.** Add a one-line comment there
naming it as deliberately not converted, with the reason: it gates diagnostic output only, not hardware
configuration, so it is not an admission gate; converting it would silently drop those three debug lines
for cmd 7 and 8 in a `DEV_TOOLS` build, a diagnostic regression, for zero safety gain and non-zero flash;
and the two new commands (9 and 10) already satisfy its existing range test, so there is no gap for them.
Record this decision in the SUMMARY so a verifier does not discover it.

**The CMD_IDLE delta, decided explicitly.** After this change, an explicit `{"cmd":0}` frame no longer
reaches `configure_memory`; it falls to `loop()`'s existing `case CMD_IDLE: break;`, produces no frame at
all, and the firmware returns to waiting. Today it produces two error frames (`0xBB`
`MSG_ERR_PROTOCOL_NOT_IMPLEMENTED` from `configure_not_implemented`, then `MSG_ERR_SETUP`). **Accept this
and record it as a third named behaviour delta beside cmd 7 and 8.** Rationale to state: `CMD_IDLE` is a
firmware-internal state, not a command the host emits, so no shipped host path is affected; adding an
explicit refusal arm for cmd 0 was considered and declined because it spends flash against 2992 B of
Leonardo headroom for a frame no host sends. Name all three deltas (7, 8, 0) in the SUMMARY — D-01 names
only 7 and 8, and this plan owns the third.

Do **not** add `case CMD_SDP_UNLOCK:` or `case CMD_SDP_LOCK:` arms to `loop()`'s switch here, and do not
touch `configure_eeprom28c`. Those land in Plan 119-04. Until then the two values are defined and
admitted by the predicate but reach `loop()`'s `default:` — which is correct, inert, and exactly what the
truth table in Task 3 asserts about the predicate rather than about dispatch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'CMD_SDP_UNLOCK 9\|CMD_SDP_LOCK 10\|static inline bool is_memory_cmd' include/firestarter.h && grep -c 'is_memory_cmd(handle->cmd)' src/firestarter.cpp && pio run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `include/firestarter.h` defines `CMD_SDP_UNLOCK` as 9 and `CMD_SDP_LOCK` as 10, both outside any preprocessor conditional, placed between the `CMD_DEV_*` pair and `CMD_READ_VPP 11`
    - Both new defines carry a comment naming D-03's ordering reason and Phase 120 HOST-01/HOST-03 as the host surface
    - `include/firestarter.h` declares `static inline bool is_memory_cmd(uint8_t cmd)` and its body names exactly the eight macros `CMD_READ`, `CMD_WRITE`, `CMD_ERASE`, `CMD_BLANK_CHECK`, `CMD_CHECK_CHIP_ID`, `CMD_VERIFY`, `CMD_SDP_UNLOCK`, `CMD_SDP_LOCK` and no others
    - The predicate body contains no preprocessor directive and does not name `CMD_DEV_ADDRESS` or `CMD_DEV_REGISTER` (confirmed by reading the extracted body; Plan 119-03's gate makes this machine-checked)
    - The predicate carries a comment recording all three of: the structurally-forced nature of the old conditional, the deliberate cmd 7/8 tightening under D-01, and its role as an access-control gate over the 12 V VPP path
    - `src/firestarter.cpp` calls `is_memory_cmd(handle->cmd)` as the inner admission test, with no preprocessor conditional around that `if` or its `else`
    - The `else` branch is unconditional and the `DEV_TOOLS` conditional wraps only the two flag debug lines inside it
    - The five `LOG_DEBUG_ID_SUB_U8` flag lines, the `op_execute_function(configure_memory, handle)` call and its `MSG_ERR_SETUP` failure path are unchanged (verified by `git diff`)
    - `src/firestarter.cpp:124`'s range guard is unchanged and now carries a comment naming it as deliberately not converted, with the diagnostic-only reason
    - `loop()`'s switch has no new `case` arm and `configure_eeprom28c` is untouched by this plan
    - `pio run` reports SUCCESS for all three AVR envs, and both native envs still report 112/112
    - The SUMMARY names all THREE behaviour deltas explicitly: cmd 7, cmd 8, and cmd 0 (`CMD_IDLE`)
  </acceptance_criteria>
  <done>The ordinal guard is gone, replaced by a conditional-free header-inline predicate that names exactly eight commands including the two new ones; all three behaviour deltas are recorded; the second ordinal guard's scope decision is written down; every build and both native envs stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the test_cmd_admission truth-table suite and run it in BOTH native envs</name>
  <files>firestarter/test/native/avr/test_cmd_admission/test_cmd_admission.cpp, firestarter/test/native/avr/test_cmd_admission/host_stubs.cpp, firestarter/test/native/avr/test_cmd_admission/avr/pgmspace.h, firestarter/platformio.ini</files>
  <read_first>
    - firestarter/test/native/avr/test_dispatch/host_stubs.cpp (35 lines, a pure pass-through to `../_shared/host_stubs_common.inc` — copy near-verbatim, changing only the header comment's suite name)
    - firestarter/test/native/avr/test_dispatch/avr/pgmspace.h (67 lines — copy VERBATIM; PROGMEM, PSTR, PGM_P, pgm_read_*, strncmp_P, all `#ifndef`-guarded)
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp lines 36-62 and 151-204 (the setUp Serial mocks, the assertion-pair shape, and the `main()` / UNITY_BEGIN / RUN_TEST / UNITY_END skeleton)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 75-90 (the four load-bearing ArduinoFake mocks and their "do not remove as unused" comment — ArduinoFake ABORTS with SIGABRT on any unmocked virtual, and a SIGABRT in a brand-new suite is this, not the deferred Unity-teardown flake)
    - firestarter_app/tests/test_revision_constants_parity.py lines 103-117 (the standalone-numeric-literals idiom and the exact reason it exists — quote it in the new suite's comment)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-B (the full 16-row expected-value table for both build configurations — the source of truth for the suite's expectations)
  </read_first>
  <action>
Create `firestarter/test/native/avr/test_cmd_admission/` with three files.

`host_stubs.cpp` — copy `test_dispatch/host_stubs.cpp` near-verbatim, changing only the header comment's
suite name. `avr/pgmspace.h` — copy `test_dispatch/avr/pgmspace.h` verbatim.

`test_cmd_admission.cpp` — a Unity suite including `<Arduino.h>`, `<ArduinoFake.h>`, `<unity.h>` and
`"firestarter.h"`. In `setUp()`, call `ArduinoFakeReset()` and install the Serial write/flush mocks from
`test_configure_memory.cpp:36-62` plus the four `delay` / `delayMicroseconds` / `millis` / `micros` mocks
from `test_sdp_harness.cpp:75-90`, with the "load-bearing, do not remove as unused" comment. The predicate
itself calls nothing, but ArduinoFake aborts on any unmocked virtual reached by anything the suite links,
and this suite must be robust to Plan 119-07 widening `build_src_filter`.

Four cases, all asserting on `is_memory_cmd()` only — never on dispatch:

1. `test_admission_truth_table_over_every_cmd_value` — the load-bearing case. Loop `c` from 0 through 255
   inclusive. Compute the expected value as membership in the literal set `{1, 2, 3, 4, 5, 6, 9, 10}`
   using numeric literals, and assert `is_memory_cmd((uint8_t)c)` equals it with a
   `TEST_ASSERT_EQUAL_MESSAGE` whose message includes the offending value so a failure names it. This is
   **every** value, not a sample: that exhaustiveness is what makes the two-env run a set-equality proof
   rather than a spot check.
2. `test_admission_rejects_dev_tool_ordinals_7_and_8` — asserts `is_memory_cmd(7)` and `is_memory_cmd(8)`
   are both false, using the bare numeric literals `7` and `8`. Comment: `CMD_DEV_ADDRESS` and
   `CMD_DEV_REGISTER` are defined inside a `DEV_TOOLS` conditional at `firestarter.h:42-45`, so naming
   those macros here would fail to compile in `native_nodevtools`; the host's
   `test_revision_constants_parity.py:103-117` carries the same idiom for the same reason — quote its
   sentence. State that this exclusion is D-01's deliberate safety tightening and that a release build
   previously configured a memory handler for these two before refusing them.
3. `test_admission_rejects_cmd_idle_zero` — asserts `is_memory_cmd(0)` is false, with a comment recording
   RESEARCH F-B2's third behaviour delta: an explicit `{"cmd":0}` frame used to produce two error frames
   and now produces silence, accepted deliberately in Task 2.
4. `test_admission_rejects_non_memory_commands` — asserts false for `CMD_READ_VPP`, `CMD_READ_VPE`,
   `CMD_FW_VERSION`, `CMD_CONFIG`, `CMD_HW_VERSION` by name (all five are unconditionally defined, so
   naming them is safe in both envs).

Then add the suite to **both** native envs in `platformio.ini`: append `native/avr/test_cmd_admission` to
each env's `test_filter` list and `-I test/native/avr/test_cmd_admission` to each env's `build_flags` `-I`
list. That is four new lines. A suite is invisible until it appears in both lists, which is exactly the
stale claim Task 1 corrected in `CLAUDE.md`.

Run the suite in both envs, then both full suites:
`pio test -e native -f "*test_cmd_admission*"`, `pio test -e native_nodevtools -f "*test_cmd_admission*"`,
then `pio test -e native` and `pio test -e native_nodevtools`. Both full runs must now report 116/116
across 17 suites (112 pre-existing cases plus this suite's 4, and 16 pre-existing suites plus this one) —
if the case count differs from 116, report the actual number rather than adjusting an assertion to match.
Also re-run `pio run` (3/3 SUCCESS).

If the new suite aborts with SIGABRT, that is Pitfall 5: an unmocked ArduinoFake virtual in a brand-new
suite. It is **not** the deferred `test_flash_intel_vpp` Unity-teardown flake. Add the missing mock rather
than ignoring the case, and never use `TEST_IGNORE_MESSAGE`.

Finally re-run the host gates that scan firmware source, because this task edited `firestarter.h` and
`firestarter.cpp`: `python3 -m pytest tests/test_revision_constants_parity.py tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_sdp_bus_config_drift.py -q`
(baseline 21 passed), `python3 tools/check_no_log_in_sdp_window.py` (exit 0), `python3 tools/check_dispatch.py`
(exit 0). `test_revision_constants_parity.py` is the one that reads `firestarter.h`; RESEARCH F-S verified
it asserts thirteen hardcoded `CMD_*` literals rather than enumerating the header, so firmware-only
additions are invisible to it — confirm that empirically rather than assuming it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'test_cmd_admission' platformio.ini && pio test -e native 2>&1 | tail -5 && pio test -e native_nodevtools 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test/native/avr/test_cmd_admission/` contains `test_cmd_admission.cpp`, `host_stubs.cpp` and `avr/pgmspace.h`
    - `host_stubs.cpp` includes `../_shared/host_stubs_common.inc` and differs from `test_dispatch/host_stubs.cpp` only in its header comment
    - `avr/pgmspace.h` is byte-identical to `test_dispatch/avr/pgmspace.h`
    - `test_cmd_admission.cpp` contains a case that loops over all 256 values of a `uint8_t` and asserts `is_memory_cmd()` against membership in `{1,2,3,4,5,6,9,10}`
    - The suite nowhere names `CMD_DEV_ADDRESS` or `CMD_DEV_REGISTER`; cmd 7 and 8 are referenced as numeric literals with a citation comment quoting `test_revision_constants_parity.py`
    - The suite contains a case asserting `is_memory_cmd(0)` is false, with F-B2's delta recorded in its comment
    - `grep -c 'test_cmd_admission' platformio.ini` reports 4 (one `test_filter` line and one `-I` line in each of the two native envs)
    - `pio test -e native -f "*test_cmd_admission*"` passes 4/4; `pio test -e native_nodevtools -f "*test_cmd_admission*"` passes 4/4
    - `pio test -e native` and `pio test -e native_nodevtools` both report 116/116 across 17/17 suites (or the actual count is reported as a finding rather than assertions being adjusted to match)
    - `pio run` reports SUCCESS for all three AVR envs
    - `TEST_IGNORE_MESSAGE` appears nowhere in the new suite
    - Host gates hold: 21 passed on the four scanning modules, `check_no_log_in_sdp_window.py` exit 0, `check_dispatch.py` exit 0
    - `git -C /workspaces/firestarter_app status --short` is clean (this plan edits no host file)
  </acceptance_criteria>
  <done>One exhaustive truth-table suite asserts the predicate over every possible cmd value and passes identically in a `-D DEV_TOOLS` build and in a build without it, giving LOCK-03 its semantic proof; all 17 suites and all three AVR builds are green in both envs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire `cmd` integer to hardware bus configuration | `is_memory_cmd()` is the gate; `configure_eprom` beyond it enables the 12 V VPP boost regulator |
| build configuration to runtime behaviour | A release build behaving differently from the build that was tested |
| firmware header text to host source-scanning gates | A firmware header edit that leaves the firmware suite green while breaking host CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-02-ADMIT | Elevation of privilege | `is_memory_cmd()` at the `configure_memory` gate | critical | mitigate | Explicit enumeration of eight named macros, never an ordinal range; an exhaustive 256-value truth table; a second build configuration running the same source; Plan 119-03's textual gate as the third oracle |
| T-119-02-DIVERGE | Tampering | `-D DEV_TOOLS` build-configuration divergence | high | mitigate | `[env:native_nodevtools]` compiles and runs the full 17-suite filter without the flag; both full runs are acceptance criteria; the predicate names no conditional macro |
| T-119-02-VACUOUS | Tampering | a preprocessor conditional inside the predicate | high | mitigate | Prohibited explicitly; the two-env run would still pass vacuously with one, which is why Plan 119-03's textual gate exists and why LOCK-03 stays open until it lands |
| T-119-02-VPP | Tampering (destructive) | 12 V VPP boost via `configure_eprom` | high | mitigate | The predicate narrows, never widens, the admitted set: cmd 7 and 8 lose admission in a release build; no value gains admission except 9 and 10, which route only to the `0x0D` handler once Plan 119-04 wires them |
| T-119-02-SILENCE | Repudiation | `CMD_IDLE` losing its two error frames | low | accept | `CMD_IDLE` is a firmware-internal state, not a host-emitted command. Accepted under RESEARCH F-B2 and named as the third behaviour delta in the SUMMARY; an explicit refusal arm was declined on flash grounds against 2992 B |
| T-119-02-HOSTGATE | Tampering (of controls) | `test_revision_constants_parity.py` | medium | mitigate | Re-run empirically after the header edit rather than trusting F-S's analysis; all four scanning modules re-run as an acceptance criterion |
| T-119-02-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. `pio test -e native` and `pio test -e native_nodevtools` both report 116/116 across 17/17 suites.
2. `pio test -e native -f "*test_cmd_admission*"` and the same under `native_nodevtools` both pass 4/4.
3. `pio run` SUCCESS on `uno`, `uno328pb`, `leonardo`; Leonardo flash recorded for LOCK-06's arithmetic.
4. `grep -c 'test_cmd_admission' platformio.ini` reports 4; `default_envs` unchanged.
5. The predicate's body, read directly, contains no preprocessor directive and names exactly eight `CMD_*` macros.
6. `firestarter.cpp` contains no preprocessor conditional around the admission `if` or its `else`.
7. `.planning/todos/prove-pio-dev-flag-fails-closed.md` records item 4 answered; items 1-3 untouched.
8. `firestarter/CLAUDE.md`'s new-suite claim is corrected.
9. Host gates at baseline: 21 passed, both checker scripts exit 0. `firestarter_app` working tree clean.
10. Commits: one or more inside `firestarter/`, then one meta commit staging the gitlink plus the SUMMARY. Suggested firmware subject: `feat(119-02): replace the ordinal admission guard with is_memory_cmd() and add a no-DEV_TOOLS native env (LOCK-03)`.
11. `REQUIREMENTS.md` untouched — LOCK-03 stays open until Plan 119-03, LOCK-02 until Plan 119-07.
</verification>

<success_criteria>
- `is_memory_cmd()` exists as a header-inline predicate naming exactly eight commands with no build-configuration conditional in its body, and it is the only admission test at the `configure_memory` gate.
- `CMD_SDP_UNLOCK` (9) and `CMD_SDP_LOCK` (10) are defined unconditionally, with D-03's ordering reason recorded in source.
- A second native env compiles without `-D DEV_TOOLS` and runs the full filter; both envs pass 116/116 across 17 suites; the folded todo's item 4 is answered against the todo file.
- All three behaviour deltas (cmd 7, cmd 8, cmd 0) are named in the SUMMARY, and the second ordinal guard at `:128` has a written scope decision.
- `firestarter/CLAUDE.md`'s stale new-suite claim is corrected; every host gate is at baseline.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-02-SUMMARY.md` when done.
Record: the predicate's final spelling and where it lives; the exact `else`-branch restructuring; all
three behaviour deltas (7, 8, 0) with their dispositions; the `:128` guard decision and its reason; both
envs' case/suite counts; the three flash/RAM figures; the folded-todo item-4 result as recorded against
the todo; the `CLAUDE.md` correction; and an explicit note that LOCK-03 remains open pending Plan 119-03
and LOCK-02 pending Plan 119-07.
</output>