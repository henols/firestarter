---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 05
type: execute
wave: 5
depends_on: [119-04]
files_modified:
  - firestarter/test/native/avr/_shared/sdp_expected.h
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
autonomous: true
requirements: [LOCK-01, LOCK-05]

must_haves:
  truths:
    - "LOCK-01 is proven by GOLDEN TRACE, not by prose: the production lock op's emitted bus stream must equal a dump-authored golden on each of the four 0x0D pinouts (DIP28_28C256, DIP28_28C64, DIP24_2816, DIP32_28C512_EEPROM), and the DIP32 case needs a deliberately stale upper-address seed because its remap is the identity under a zero CONTROL seed, so only the OE-edge reordering distinguishes shipped from fixed there."
    - "D-10's no-payload invariant is a STREAM-LENGTH assertion, not a table comparison. AA-55-A0 is byte-identical to the protected-write prefix; the discriminator is that no data write follows. An absence cannot be observed by comparing tables, so a case must assert the lock stream terminates after exactly three command writes with no trailing data write."
    - "Divergence is asserted as an EXACT INDEX, never as `!= -1`. The in-tree discipline at test_sdp_harness.cpp:280-289 asserts index 26 on the shipped shape; a golden pinned to the wrong expectation stays green, which is Pitfall 4."
    - "Goldens are authored EMPIRICALLY, never hand-derived. sdp_expected.h's own header states that every literal array in it was authored from a recorded dump of real production code. The dump is behind #ifdef SDP_TRACE_DUMP and pio test SWALLOWS printf from test bodies, so the built suite binary must be run directly."
    - "RESEARCH A1 is an ASSUMPTION, not a fact: the predicted FIXED-shape lock golden is 30 entries with write #3's payload at index 27, diverging from both the unlock stream and the FLASH_ERASE stream at index 27. It was derived arithmetically and cross-validated against the in-tree index-26 assertion on the shipped shape, but NOT dumped. If the dump disagrees, the DUMP WINS: use the dumped values and record the correction as a finding."
    - "D-10 forces sdp_expected.h's whole-file blob SHA to change, so Phases 117 and 118's blob-SHA identity shorthand NO LONGER APPLIES to this file. The non-regression proof shifts to PER-ARRAY byte-identity of the pre-existing arrays, asserted here and recorded in 119-NONREGRESSION.md."
    - "RESEARCH F-O: the micros() mock is a 2-slot PARITY ALTERNATOR indexed by call count modulo 2. D-16's per-byte page-load tracker (Plan 119-08) adds micros() calls inside write_execute, so the parity model breaks for any case driving both write_init and write_execute. Upgrading it to a scripted tick QUEUE is prerequisite work here, and cases 11 and 12 MUST be explicitly re-verified afterwards - they are the oracle 118's OBS-03 and OBS-04 proofs rest on."
    - "Assert on the ordered stream's CONTENT, never on a call count. Production register-write elision is invisible to a counting test (116 research finding 10), and sdp_expected.h's comparator 'never counts anything; every comparison is positional'."
    - "Register-cache seeding order is load-bearing: configure_memory itself calls mem_util_set_address(handle, 0) which writes registers, so it must always be configure_memory, THEN reset_register_cache, THEN clear_strobes. Both existing suites' helpers encode this and label it load-bearing order."
    - "Baselines to hold: pio test -e native and -e native_nodevtools at 116/116 across 17 suites before this plan's additions, pio run 3/3 SUCCESS, all host gates at Plan 119-04's recorded baseline."
  prohibitions:
    - "MUST NOT hand-derive a golden array. Drive the production code, dump, and hand-check the dump against an independently-derived latch table."
    - "MUST NOT assert `sdp_first_divergence(...) != -1`. Assert the exact index with a TEST_ASSERT_EQUAL_MESSAGE naming what diverges and why."
    - "MUST NOT assert on strobe_count() alone as the no-payload proof, and MUST NOT assert on a call count anywhere."
    - "MUST NOT use the whole-file blob SHA of sdp_expected.h as an identity proof in this phase."
    - "MUST NOT modify any pre-existing SDP_SHIPPED_* or SDP_FIXED_* array. Adding arrays is the only permitted change to those literals; a per-array byte-identity check must pass for every one of them."
    - "MUST NOT use TEST_IGNORE_MESSAGE. An IGNORED result demonstrates nothing."
    - "MUST NOT change any production file in this plan. Plan 119-04 landed the code; this plan only observes it. If a case cannot pass without a production change, that is a finding to report, not a licence to edit."
    - "MUST NOT delete, renumber or weaken cases 1 through 12."
  artifacts:
    - firestarter/test/native/avr/_shared/sdp_expected.h
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  key_links:
    - "Driving the new lock op natively works because src/proms/eeprom_28c.cpp IS in [env:native]'s build_src_filter. The sequence is: build the handle from an SDP_BUS_CONFIGS row, set h.cmd = CMD_SDP_LOCK, call configure_memory(&h), THEN reset_register_cache(...), THEN clear_strobes(), then call h.firestarter_operation_main(&h) - init and end are NULL by design."
    - "AT28C_TBLC_MAX_US (100) and AT28C_TWC_MAX_MS (10) are #defines local to eeprom_28c.cpp and are not exported by eeprom_28c.h. Mirror the value as a named local with an explicit eeprom_28c.cpp citation, as existing Case 11 does. The lock's budget is 3 times 100 = 300 us."
    - "sdp_snapshot(snap, N) must be called before a second drive(), because drive() calls clear_strobes() and without a snapshot the first stream is gone."
---

<objective>
Author the four `SDP_FIXED_LOCK_*` goldens empirically from the production lock op, upgrade the `micros()`
mock from a two-slot parity alternator to a scripted tick queue, re-verify cases 11 and 12 against the new
mock, and pin the lock's emitted stream on all four `0x0D` pinouts including the load-bearing absence of any
trailing data write.

Purpose: LOCK-01's whole claim is that the lock emits exactly the datasheet-specified three loads and then
stops. The three writes are byte-identical to the protected-write prefix, so what makes the sequence a lock
rather than a write is an **absence** — and an absence can only be asserted on the stream. This plan is that
assertion.

Output: four new goldens, a scripted `micros()` queue with cases 11 and 12 re-verified, seven new cases
pinning the lock stream per pinout plus its termination and its exact divergence indices, and LOCK-01 closed.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-VALIDATION.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-04-SUMMARY.md
@.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md
</context>

## Repo and commit mechanics

Both files land inside `firestarter/`, already on `v1.22-at28c-software-data-protection-lifecycle`. Confirm
with `git branch --show-current` before writing. Commit inside the submodule, then one meta commit staging
the gitlink plus the `.planning/` SUMMARY. `firestarter_app/` must be untouched.

Firmware plans are **strictly sequential** against the shared `firestarter/.pio/build/` tree. Do not start
before 119-04's commits exist and its host gates are green.

## Artifacts this phase produces

**Created by THIS plan:** the goldens `SDP_FIXED_LOCK_DIP28_28C256`, `SDP_FIXED_LOCK_DIP28_28C64`,
`SDP_FIXED_LOCK_DIP24_2816`, `SDP_FIXED_LOCK_DIP32_28C512_EEPROM` and their `_LEN` macros in
`_shared/sdp_expected.h`; the scripted `micros()` tick queue and its reset/scripting helper in
`test_eeprom28c_sdp.cpp`; new cases 13 through 19.

**Created earlier:** the three catalog ids (119-01); `CMD_SDP_UNLOCK`, `CMD_SDP_LOCK`, `is_memory_cmd()`,
`[env:native_nodevtools]`, `test_cmd_admission` (119-02); `check_is_memory_cmd_no_ifdef.py` with its pytest
and fixture (119-03); `EEPROM_SDP_ENABLE[3]`, `eeprom28c_emit_sdp_sequence_timed()`,
`eeprom28c_sdp_lock_execute()`, `eeprom28c_sdp_unlock_execute()`, `eprom_sdp_lock()`, `eprom_sdp_unlock()`,
the `configure_eeprom28c` arms, the `loop()` arms and the appended emit anchor (119-04).

**Created later:** the three-way table-identity and distinctness guard, the D-12 report-shape proof, the
D-14 lock-budget pair and the host `test_sdp_table_parity.py` enable-table leg (119-06); the generic
NULL-`main` refusal and the widened `build_src_filter` (119-07); the page-load worst-interval tracker
(119-08); the Phase 121 scope amendment (119-09); `119-NONREGRESSION.md` (119-10); `119-MEASUREMENT.md`
(119-11).

## Requirement ownership

This plan **closes LOCK-01**: the datasheet-specified body, the `t_WC` call and the no-payload termination
are all pinned against dump-authored goldens on all four pinouts.

It advances **LOCK-05** by landing the no-payload stream-length assertion — the half of LOCK-05 that lives
on the stream. **LOCK-05 stays OPEN** until Plan 119-06 lands the three-way table-identity and
distinct-objects guard. Mark **only LOCK-01** Complete in `REQUIREMENTS.md`; LOCK-02, LOCK-04, LOCK-05 and
LOCK-06 stay Pending.

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the micros() mock to a scripted tick queue and re-verify cases 11 and 12</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 120-260 (the s_micros_ticks[2] declaration and s_micros_call_count at :124-138 with the comment explaining the modulo-2 indexing; the AlwaysDo micros lambda at :212-216; the file-static reset block at :218-230; the captured_frames vector at :150 and sdp_captured_frame_ids / sdp_ids_contains at :157-181; and make_sdp_handle with its extra_flags default-arg extension seam at :248-258)
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 700-840 (test_case11_tblc_budget_exceeded_warns and test_case12_flag_absent_emits_exactly_two_report_frames in full, plus the main() RUN_TEST list — these two cases are what the mock upgrade must not break)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-O and Open Question 4 (why the parity model breaks under D-16's tracker, and the instruction to enumerate every frame-count assertion in the native suite before adding a report line)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-05-SUMMARY.md (how cases 11 and 12 were originally proven, including the independent proof that inverting the budget comparison turns them RED)
  </read_first>
  <action>
Replace the two-slot parity alternator with a scripted tick queue in
`test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp`. The file already includes `<vector>` for
`captured_frames`, so no new include is needed.

Introduce three file-statics: a `std::vector<uint32_t>` holding the scripted tick sequence, a `size_t`
monotonic cursor, and a `uint32_t` default-tail value returned once the script is exhausted. The `micros`
mock returns the tick at the cursor and advances it; past the end it returns the tail value without
advancing further. Add a small helper that installs a script and resets the cursor, so a case reads as
"script these ticks, then drive". Reset all three in `setUp()` alongside the existing file-static reset
block.

Document the tail value's semantics in a comment, because it is the part that can silently corrupt a
measurement: state what the tail is, that it is returned indefinitely once the script runs out, and that a
case which needs a *bounded* number of reads must therefore assert on the reported value rather than
assuming the script was consumed exactly. Also state, in the same comment, **why** the parity alternator is
being retired: D-16's per-byte page-load tracker (Plan 119-08) adds `micros()` calls inside
`eeprom28c_write_execute`, so a modulo-2 model makes every interval alternate between zero and a constant
for any case that drives both `write_init` and `write_execute` — meaningless, and silently so.

Then **explicitly re-verify cases 11 and 12 by name**, converting each to the scripted queue:
- `test_case11_tblc_budget_exceeded_warns` depends on `drive_write_init` producing exactly two `micros()`
  reads with a difference above the 600 us unlock budget. Script two ticks whose difference exceeds it, and
  keep mirroring `AT28C_TBLC_MAX_US` as a named local with its explicit `eeprom_28c.cpp` citation — the
  constant is a `#define` local to that `.cpp` and is not exported by `eeprom_28c.h`.
- `test_case12_flag_absent_emits_exactly_two_report_frames` drives `drive_write_init` only, so
  `write_execute`'s future report line is out of its scope. That was incidental before; it is now
  **load-bearing**. Add a comment saying so, and stating that if the case is ever widened to drive
  `write_execute` its frame expectation must be re-scoped.

Do not renumber, weaken or delete any of cases 1 through 12. Run the suite in both envs and confirm 12/12
in this suite and 116/116 overall across 17 suites, unchanged.

Then do RESEARCH Open Question 4's audit, because Plan 119-08 depends on its answer: enumerate **every**
frame-count or frame-set assertion across all 17 native suites (grep for the frame-id helpers, for
`captured_frames`, and for any assertion whose expectation is a count of emitted frames), list each with its
file, case name and what it counts, and state for each whether it drives `eeprom28c_write_execute`. Put that
enumeration in the SUMMARY. It is the input Plan 119-08 uses to decide what its new report line perturbs,
and doing it here — in the plan that already owns this file — is cheaper than rediscovering it there.

Independently re-prove the budget check is still load-bearing rather than trusting the green: temporarily
invert the comparison in the shared helper, confirm case 11 goes RED, then revert. This is the same
anti-hollow move 118-05 used. **Revert before committing** and state in the SUMMARY that the inversion was
temporary and reverted, with the RED outcome observed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*" 2>&1 | tail -8 && pio test -e native_nodevtools -f "*test_eeprom28c_sdp*" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `micros()` mock is a scripted queue: a `std::vector<uint32_t>` script, a monotonic cursor and a documented default-tail value, all reset in `setUp()`
    - The two-slot parity alternator and its modulo-2 indexing are gone
    - A comment documents the tail value's semantics and states why the parity model was retired, naming D-16's per-byte tracker as the reason
    - `test_case11_tblc_budget_exceeded_warns` passes under the new mock, still mirrors `AT28C_TBLC_MAX_US` as a named local with its `eeprom_28c.cpp` citation, and still asserts the WARN fires
    - `test_case12_flag_absent_emits_exactly_two_report_frames` passes under the new mock and now carries a comment stating that its `write_init`-only scope is load-bearing
    - Cases 1 through 12 are all present, unrenumbered and unweakened; `TEST_IGNORE_MESSAGE` appears nowhere
    - `pio test -e native -f "*test_eeprom28c_sdp*"` and the same under `native_nodevtools` both pass 12/12
    - `pio test -e native` and `pio test -e native_nodevtools` both report 116/116 across 17/17 suites
    - The SUMMARY contains the complete frame-count-assertion enumeration across all 17 suites, each row naming file, case and whether it drives `eeprom28c_write_execute`
    - The temporary budget-comparison inversion was performed, case 11 was observed RED, and the inversion was reverted before committing — all three stated in the SUMMARY
    - No production file is modified in the committed diff (`git diff --stat` touches only `test/`)
  </acceptance_criteria>
  <done>The tick source is a scripted queue with documented exhaustion semantics, cases 11 and 12 are re-verified by name under it, the budget check is re-proven load-bearing by a reverted inversion, and the frame-count-assertion inventory Plan 119-08 needs is written down.</done>
</task>

<task type="auto">
  <name>Task 2: Author the four SDP_FIXED_LOCK_* goldens empirically from the production lock op</name>
  <files>firestarter/test/native/avr/_shared/sdp_expected.h, firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/_shared/sdp_expected.h read WHOLE (the header's authoring-discipline statement at :12-16; the sdp_strobe_t triple shape and STROBE_KIND_DATA / STROBE_KIND_PIN values; sdp_first_divergence, sdp_assert_stream_equals and sdp_snapshot at :60-122 with the comment that the comparator never counts anything; and the existing arrays at :144-310 including SDP_FIXED_DIP28_28C256 at :195-225, which the lock golden is a 30-entry prefix of with one byte changed)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 160-215 (the SDP_TRACE_DUMP dump helper behind its #ifdef, and how case 1 invokes it — the mechanism to replicate for the lock op)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc lines 40-135 (the HOST_STUBS_RECORD_BUS / HOST_STUBS_REAL_REGISTER_UTILS opt-in contract and the two hooked symbols, so you understand what the recorder does and does not see — production register-write elision is captured, not replicated)
    - firestarter/test/native/avr/_shared/sdp_bus_config.h (the four SDP_BUS_CONFIGS rows, their mem_size and bus_config values, and their index order)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-I (the golden mechanism, the 10-entry per-write FIXED shape, the predicted index-27 payload position and length 30, the method's cross-validation against the in-tree index-26 assertion, and the DIP32 stale-seed requirement)
    - .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md section 6 (DIP32_28C512_EEPROM's stale-upper-address hazard and the per-pinout inhibit table — the lock's four-pinout coverage is judged against this)
  </read_first>
  <action>
Author four new goldens in `_shared/sdp_expected.h`, one per `SDP_BUS_CONFIGS` row:
`SDP_FIXED_LOCK_DIP28_28C256`, `SDP_FIXED_LOCK_DIP28_28C64`, `SDP_FIXED_LOCK_DIP24_2816`,
`SDP_FIXED_LOCK_DIP32_28C512_EEPROM`, each a `static const sdp_strobe_t NAME[]` of `{kind, pin, value}`
triples with a matching `#define NAME_LEN (int)(sizeof(NAME) / sizeof(NAME[0]))`.

**Author them by dump, never by hand.** The procedure:
1. Add a temporary dump invocation in `test_eeprom28c_sdp.cpp` behind `#ifdef SDP_TRACE_DUMP`, mirroring
   `test_sdp_harness.cpp`'s helper, that drives the production lock op for one `SDP_BUS_CONFIGS` row and
   prints the recorded stream as ready-to-paste triples.
2. Drive the op through the production path, in this exact order because `configure_memory` itself writes
   registers via `mem_util_set_address(handle, 0)`: build the handle from the row, set `h.cmd = CMD_SDP_LOCK`,
   call `configure_memory(&h)`, **then** `reset_register_cache(...)`, **then** `clear_strobes()`, then call
   `h.firestarter_operation_main(&h)`. `init` and `end` are NULL by design, so calling `main` directly is the
   whole operation. Getting this order wrong elides or corrupts the first write's latches and shifts every
   index — Pitfall 6.
3. Build with `SDP_TRACE_DUMP` defined and **run the built suite binary directly** — `pio test` swallows
   `printf` from test bodies. Locate the binary under `.pio/build/native/` for this suite and execute it.
4. Hand-check the dump against an independently-derived latch table before pasting: for each of the three
   writes, the expected `OE` edge, the LSB and MSB latch pulses with the pinout's remapped address bytes,
   the payload data byte, and the `CE` low/high pulse. The MSB byte differs per pinout because
   `remap(0x5555)` differs per row.
5. Paste the dumped triples in, then **remove the temporary dump invocation** (or leave it behind the
   `#ifdef` in the harness style, matching how `test_sdp_harness.cpp` keeps its dump permanently but never
   compiled — either is acceptable, but state which you chose).

For `DIP32_28C512_EEPROM` use a **deliberately stale upper-address CONTROL seed** in
`reset_register_cache`, not a zero seed. Its remap is the identity under a zero seed, so only the OE-edge
reordering would distinguish shipped from fixed, and a plain trace there would prove almost nothing. The
stale-seed shape is the one 116-PREMISE section 6 describes and that existing cases 4 and 5 already use for
the 64 KB-and-above pinouts.

Give each golden a comment naming the pinout, the CONTROL seed used, the commit the dump was taken at, and
the sentence that it was authored empirically from the production `eeprom28c_sdp_lock_execute` path.

**RESEARCH A1 is a prediction, not a fact.** The predicted FIXED-shape lock golden is 30 entries with write
number 3's payload byte (`0xA0`) at index 27, since each un-elided write contributes 10 entries. That was
derived arithmetically and the method was cross-validated against the in-tree index-26 assertion on the
shipped shape — but it was never dumped. **If the dump disagrees with the prediction in length or in index,
the dump wins.** Use the dumped values, and record the discrepancy in the SUMMARY as a finding with both
numbers, because Task 3's divergence assertions and Plan 119-06's reasoning both cite the index.

Finally add a **per-array byte-identity** check for every pre-existing array in this file, because D-10
forces the whole-file blob SHA to change and Phases 117 and 118's blob-SHA shorthand no longer applies here.
Extract each pre-existing `SDP_SHIPPED_*` and `SDP_FIXED_*` array's literal triples from
`git show <phase-base>:test/native/avr/_shared/sdp_expected.h` and compare them element-wise against the
current file, confirming that only additions were made. Record the method and the result; Plan 119-10's
`119-NONREGRESSION.md` restates it. Also confirm the other two `_shared/` files
(`host_stubs_common.inc`, `sdp_bus_config.h`) are still blob-SHA identical to the phase base — those two
keep the old shorthand.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'SDP_FIXED_LOCK_DIP28_28C256\|SDP_FIXED_LOCK_DIP28_28C64\|SDP_FIXED_LOCK_DIP24_2816\|SDP_FIXED_LOCK_DIP32_28C512_EEPROM' test/native/avr/_shared/sdp_expected.h && pio test -e native 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_shared/sdp_expected.h` defines all four `SDP_FIXED_LOCK_*` arrays with matching `_LEN` macros
    - Each golden carries a comment naming its pinout, the CONTROL seed used, the commit the dump was taken at, and that it was authored empirically from the production `eeprom28c_sdp_lock_execute` path
    - The `DIP32_28C512_EEPROM` golden was recorded under a deliberately stale upper-address CONTROL seed, and its comment says so with the reason
    - The dump was produced by running the built suite binary directly, not through `pio test`, and the SUMMARY records the binary path used
    - The dumped values were hand-checked against an independently-derived latch table before pasting, and the SUMMARY states that check was done
    - The SUMMARY states, with both numbers, whether the dumped length and payload index matched RESEARCH A1's prediction of length 30 and index 27; where they differ, the dumped values were used
    - Every pre-existing `SDP_SHIPPED_*` and `SDP_FIXED_*` array is byte-identical to the phase base, proven per-array against `git show <phase-base>:...`, with the method and result recorded
    - `host_stubs_common.inc` and `sdp_bus_config.h` are still blob-SHA identical to the phase base
    - No pre-existing array was modified; the diff to `sdp_expected.h` is additions only
    - `pio test -e native` and `pio test -e native_nodevtools` still report 116/116 across 17/17 suites (Task 3 adds the cases that consume these goldens)
    - No production file is modified in the committed diff
  </acceptance_criteria>
  <done>Four empirically-dumped goldens pin the production lock op's stream on all four pinouts, the DIP32 one under a stale upper-address seed; the prediction-versus-dump outcome is recorded either way; and the pre-existing arrays are proven byte-identical per array rather than by a whole-file SHA that necessarily changed.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the lock stream per pinout, its three-write termination, and its exact divergence indices</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 375-500 (cases 1 through 5: the per-pinout stream-equality shape, the drive helper, and how cases 4 and 5 seed a stale upper address — the exact template for the four lock cases)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 245-315 (test_negativeA_unlock_mutated_diverges_and_matches_erase and test_negativeB_lock_table_swapped_for_write_prefix — the exact-divergence-index discipline, including the index-26 assertion and its message text, plus the sdp_snapshot-before-second-drive pattern)
    - firestarter/include/flash_utils.h lines 24-59 (FLASH_ERASE's terminal 0x10 and FLASH_DISABLE_WRITE_PROTECTION's terminal 0x20 — read-only; the erase-divergence case needs the erase table's shape but MUST NOT edit this file)
    - firestarter/test/native/avr/_shared/sdp_expected.h as Task 2 left it (the four new goldens and the comparator signatures)
  </read_first>
  <action>
Add seven cases to `test_eeprom28c_sdp.cpp`, numbered 13 through 19, and register each with `RUN_TEST` in
`main()`. Every case drives the **production** lock op — never a transcribed table loop — using the
load-bearing order from Task 2 (`configure_memory`, then `reset_register_cache`, then `clear_strobes`, then
`h.firestarter_operation_main(&h)` with `h.cmd = CMD_SDP_LOCK`).

Cases 13 through 16 — one per pinout, asserting the production lock stream equals its golden via
`sdp_assert_stream_equals(SDP_FIXED_LOCK_<row>, SDP_FIXED_LOCK_<row>_LEN, "<context>")`. Case 16 uses the
stale upper-address CONTROL seed the DIP32 golden was recorded under, and its message must say so.
`sdp_assert_stream_equals` already checks overflow, then length, then positional triple equality naming the
first diverging index and both triples — that is the whole assertion; do not add a count check beside it.

Case 17 — **the no-payload termination, D-10's load-bearing absence.** Assert that the lock stream ends
after exactly the three command writes with no trailing data write. Do this positionally, not by counting:
assert the stream's length equals the golden's length, and assert that the final triple is the last write's
`CE`-high pin edge rather than a `STROBE_KIND_DATA` entry. State in the case's message and comment why this
case exists: `EEPROM_SDP_ENABLE` is byte-identical to `FLASH_ENABLE_WRITE`, the protected-write prefix, so
the only discriminator between a lock and a byte write is that no data write follows — an absence that a
table comparison structurally cannot observe (Pitfall 4). Also assert no `STROBE_KIND_DATA` entry appears
after the index of the third write's payload, so a future edit appending a dummy byte fails here loudly.

Case 18 — the lock stream diverges from the **unlock** stream at an exact index. Drive the lock op, snapshot
with `sdp_snapshot` (mandatory: `drive()` calls `clear_strobes()` and without the snapshot the first stream
is gone), then compare against the six-write unlock golden and assert the exact divergence index with a
`TEST_ASSERT_EQUAL_MESSAGE` naming what differs — write number 3's payload byte, `0xA0` against `0x80`. Use
the index Task 2's dump established, not the predicted one, and say in the message which byte position it is.

Case 19 — the lock stream diverges from the **chip-erase** stream at an exact index. Same shape, comparing
against the stream `FLASH_ERASE` produces. Message must record that erase's third payload is also `0x80`, so
the divergence index is the same as case 18's, and that this is the one-nibble hazard class FIX-05 guarded
for the unlock table now checked for the lock table. Read `FLASH_ERASE` from `flash_utils.h` but **do not
edit that file**.

Never assert `sdp_first_divergence(...) != -1` in cases 18 or 19. The exact index is the assertion; a
golden pinned to the wrong expectation stays green, which is precisely Pitfall 4.

Run the suite in both envs (19/19 in this suite), then both full suites, which must now report 123/123
across 17 suites (116 plus this plan's 7 new cases). If the count differs, report the actual number rather
than adjusting an assertion to match. Then `pio run` (3/3 SUCCESS) and re-run the host gate set from Plan
119-04's baseline, since `_shared/sdp_expected.h` and the suite are test-only files but
`test_sdp_table_parity.py` scans firmware source and must be confirmed rather than assumed.

Finally mark **LOCK-01 Complete** in `.planning/REQUIREMENTS.md`, with a parenthetical naming the four
dump-authored goldens, the `t_WC` call and the no-payload termination case. Change no other requirement row.
Re-read LOCK-02, LOCK-04, LOCK-05 and LOCK-06 and confirm they still read Pending; if any does not, report
it as a defect rather than fixing it here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -6 && pio test -e native_nodevtools 2>&1 | tail -4 && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Seven new cases numbered 13 through 19 exist and are all registered with `RUN_TEST` in `main()`
    - Cases 13 through 16 each drive the production lock op via `h.firestarter_operation_main(&h)` with `h.cmd = CMD_SDP_LOCK` and assert stream equality against the matching `SDP_FIXED_LOCK_*` golden
    - Every case uses the order `configure_memory`, then `reset_register_cache`, then `clear_strobes`, then the op call, and a comment names that order as load-bearing
    - Case 16 uses the stale upper-address CONTROL seed its golden was recorded under, and says so in its message
    - Case 17 asserts the stream's length equals the golden's, asserts the final triple is a pin edge rather than a data entry, and asserts no data entry follows the third write's payload index; its comment names the byte-identity-with-the-write-prefix reason
    - Cases 18 and 19 each assert an EXACT divergence index with a message naming the differing byte and position; neither asserts merely `!= -1`
    - Case 19's message records that the erase table's third payload is also `0x80` and ties the case to FIX-05's one-nibble hazard class
    - `git diff --quiet -- include/flash_utils.h` exits 0
    - `pio test -e native -f "*test_eeprom28c_sdp*"` and the same under `native_nodevtools` both pass 19/19
    - `pio test -e native` and `pio test -e native_nodevtools` both report 123/123 across 17/17 suites, or the actual count is reported as a finding
    - `pio run` reports SUCCESS on all three AVR envs with the flash figures recorded
    - The host gate set is at Plan 119-04's recorded baseline, `test_sdp_table_parity.py` confirmed empirically
    - `.planning/REQUIREMENTS.md` shows LOCK-01 Complete, and LOCK-02, LOCK-04, LOCK-05, LOCK-06 all still Pending
    - No production file is modified in the committed diff
  </acceptance_criteria>
  <done>The production lock op's emitted stream is pinned byte-exact on all four `0x0D` pinouts, its termination after exactly three command writes with no trailing data write is asserted positionally, and its divergence from both the unlock and the chip-erase streams is asserted at exact indices — LOCK-01 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a dumped golden to a claim about the emitted stream | A golden authored from a wrong drive order pins the wrong expectation and stays green |
| a table comparison to a claim about an absence | The lock's discriminator is that no data write follows; tables cannot show that |
| a mocked tick source to a measured interval | A mock whose model no longer matches the code under test produces meaningless numbers, silently |
| test-only files to closed non-regression claims | `sdp_expected.h` must change, so the identity proof used in two prior phases becomes unavailable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-05-WRONGGOLDEN | Repudiation | the four `SDP_FIXED_LOCK_*` arrays | critical | mitigate | Dump-authored from the production op, hand-checked against an independently-derived latch table, with the drive order pinned as load-bearing; the prediction-versus-dump discrepancy is a recorded finding rather than a silent adjustment |
| T-119-05-ABSENCE | Tampering (destructive) | the no-payload invariant | critical | mitigate | Case 17 asserts positionally on the stream: length equality, a pin edge as the final triple, and no data entry after the third payload — so appending a dummy byte fails loudly |
| T-119-05-NOTMINUS1 | Repudiation | divergence assertions | high | mitigate | Exact-index assertions with messages naming the differing byte; `!= -1` is prohibited |
| T-119-05-MOCKDRIFT | Tampering | the `micros()` tick source | high | mitigate | Scripted queue with documented exhaustion semantics; cases 11 and 12 re-verified by name; the budget check independently re-proven by a reverted inversion |
| T-119-05-BLOBSHA | Repudiation | the non-regression identity proof | high | mitigate | Per-array byte-identity against `git show <phase-base>:...` replaces the whole-file SHA for this file only; the other two `_shared/` files keep the SHA proof; both recorded for Plan 119-10 |
| T-119-05-FROZEN | Tampering | `include/flash_utils.h` | high | mitigate | Read-only use for `FLASH_ERASE`; `git diff --quiet` on that path is an acceptance criterion |
| T-119-05-COUNTING | Repudiation | any call-count assertion | medium | mitigate | Prohibited; production register-write elision is invisible to a counting test, and the comparator is positional by contract |
| T-119-05-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. Four `SDP_FIXED_LOCK_*` arrays plus `_LEN` macros exist, each with pinout, seed, commit and empirical-authoring provenance in its comment.
2. The dump was taken by running the built suite binary directly; the binary path and the hand-check are recorded.
3. RESEARCH A1's prediction (length 30, index 27) is confirmed or corrected in the SUMMARY with both numbers.
4. Cases 13 through 19 pass; the suite reports 19/19 and both full runs report 123/123 across 17 suites.
5. Case 17 asserts the absence positionally; cases 18 and 19 assert exact indices.
6. Cases 11 and 12 pass under the scripted queue; the reverted inversion RED observation is recorded.
7. Per-array byte-identity holds for every pre-existing golden; `host_stubs_common.inc` and `sdp_bus_config.h` are blob-SHA identical.
8. `git diff --quiet -- include/flash_utils.h` exits 0; no production file appears in the committed diff.
9. `pio run` 3/3 SUCCESS; host gates at Plan 119-04's baseline.
10. `REQUIREMENTS.md` shows LOCK-01 Complete only.
11. Commits: one or more inside `firestarter/`, then one meta commit staging the gitlink plus the SUMMARY. Suggested firmware subject: `test(119-05): pin the SDP-enable stream on four pinouts with dump-authored goldens (LOCK-01)`.
</verification>

<success_criteria>
- The lock's emitted stream is byte-exact against dump-authored goldens on all four `0x0D` pinouts, including DIP32 under a stale upper-address seed.
- The load-bearing absence — no data write after the three loads — is asserted on the stream, positionally.
- Divergence from the unlock and the chip-erase streams is asserted at exact indices, with the differing byte named.
- The tick source is a scripted queue and cases 11 and 12 are re-verified under it, with the budget check re-proven load-bearing.
- Per-array byte-identity replaces the whole-file SHA proof for `sdp_expected.h`, recorded for Plan 119-10.
- LOCK-01 is Complete; no other requirement row moved.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md` when done.
Record: the four goldens' lengths and their third-payload indices; whether A1's prediction held, with both
numbers; the CONTROL seed used per pinout; the suite binary path the dump came from and confirmation of the
hand-check; the complete frame-count-assertion inventory across all 17 suites; the reverted-inversion RED
observation for case 11; the per-array byte-identity method and result plus the two blob-SHA-identical
`_shared/` files; case counts in both envs; the three flash figures; and confirmation that LOCK-01 is the
only requirement row changed and that LOCK-05 remains open pending Plan 119-06.
</output>
