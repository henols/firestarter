---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 11
type: execute
wave: 11
depends_on: [119-10]
files_modified:
  - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md
autonomous: true
requirements: [LOCK-06]

must_haves:
  truths:
    - "D-18: THREE boards, not one - reversing Phase 118's D-12 Leonardo-only scope. Operator decision 2026-07-28: 'Same as 118 and test uno and uno328pb that is also connected.' Per 118-MEASUREMENT.md section 2 the ports were /dev/ttyACM0 leonardo, /dev/ttyACM1 uno, /dev/ttyUSB0 uno328pb - but port numbers SHUFFLE across replug, so re-verify the `controller:` identity per candidate port before driving anything. Record the reversal AS a reversal with its constraints named, so the next phase does not read three boards as the new default."
    - "D-18 item 1: the operator stated on 2026-07-28 that ALL THREE SOCKETS ARE EMPTY. That is what makes this plan `autonomous: true` with NO operator checkpoint anywhere, including before the Uno-class uploads. The chip-OUT-before-sideload rule (Uno-class only; the Leonardo is exempt because only Uno-class uploads drive the shield bus) is satisfied by that statement, and the plan MUST SAY SO rather than silently skipping the rule."
    - "D-18 item 2: uno328pb is bench-unstable. Retry on timeout and NEVER trust N=1 on that board."
    - "D-18 item 3: uno328pb has a VPP-recal and program-brownout history, but 0x0D is a 5 V protocol with NO VPP rail, so that mechanism should not apply. STATE THE REASONING. If it browns out anyway, that is a FINDING, not a failed plan."
    - "D-18 item 4: uno328pb is really a plain Uno carrying mismatched firmware. Do NOT read a per-board difference as ATmega328PB silicon behaviour."
    - "D-18 item 5: report the flash delta for ALL THREE envs. Phase 118 reported Leonardo and Uno; uno328pb is the third."
    - "D-19 (nested inside D-18 as item 6, so the decision-coverage gate does not track it separately, but its content is binding): on ANY board's failure, PROCEED and record not-measured with the reason. Never a fabricated PASS. Never a hard block on five requirements because of one USB enumeration failure. This is Phase 118's D-14 discipline and this project's CI-PENDING / structurally-green precedent from Phases 98 and 103."
    - "D-17: the page-load loop IS reachable with the SHIPPED CLI via `write -b --force`. -b gets past the blank check an empty socket fails, and 0x0D has NO erase arm, so -b skips nothing else on this family. `CMD_SDP_LOCK` is NOT reachable - `dev sdp` is Phase 120 - so every bench byte in this phase is driven by released host code and the firmware-before-host invariant holds in practice, not just on paper. A throwaway raw-frame script COBS-framing cmd 9 / cmd 10 was explicitly REJECTED: it would exercise a brand-new state-mutating command on hardware through an unreviewed instrument."
    - "D-17: the lock's OWN hardware duration is UNREACHABLE this phase. Do NOT plan or attempt a standalone lock on hardware. It is deferred to Phase 120 by design."
    - "D-16: the number is the WORST per-byte page-load interval, reported ONCE per write, and it answers a TIMING question that PROJECT.md's FIFTH CORRECTION item 3 conflated with LOCK-06's FLASH budget. Name the conflation explicitly, then answer the timing question anyway because the page-load loop is where gh#11's symptom actually lives. gh#11 is a CONFLATION bug, never a sampling-rate bug."
    - "D-20: the numbers live in a dedicated 119-MEASUREMENT.md carrying, PER BOARD: the exact command, the `controller:` identity line, board and firmware build identity, `pio run` flash/RAM figures cross-checked against 119-NONREGRESSION.md, and the RAW captured log. It mirrors 118-MEASUREMENT.md exactly, including its section 1 'what this is not' and section 6 validation-ceiling sections. A rounded figure in prose is NOT a substitute."
    - "The measurement's subject is the MCU driving its own latches. It is NEVER evidence about AT28C silicon. Any sentence readable as bench-validating 0x0D crosses the validation ceiling; 118-MEASUREMENT.md sections 1 and 6 are the wording that already survived that review."
  prohibitions:
    - "MUST NOT insert a checkpoint of any kind - no checkpoint:human-verify, no checkpoint:human-action, no checkpoint:decision - and MUST NOT ask the operator about socket state, chip presence or shield revision. The operator already stated on 2026-07-28 that all three sockets are empty."
    - "MUST NOT attempt a standalone SDP lock or unlock on hardware. cmd 9 and cmd 10 are unreachable from the shipped CLI and a throwaway raw-frame instrument was explicitly rejected."
    - "MUST NOT fabricate a value for a board that did not run. Record not-measured with the exact failure and the exact command."
    - "MUST NOT assume a port from its device number. Verify the `controller:` identity line per candidate port, before and after each upload."
    - "MUST NOT trust N=1 on uno328pb, and MUST NOT read a per-board difference as ATmega328PB silicon behaviour."
    - "MUST NOT write any sentence readable as bench-validating 0x0D on AT28C silicon, as the die accepting a sequence, or as t_BLC being met as accepted by the die."
    - "MUST NOT record this number in the PROTOCOL-LEDGER. That file records bench-verification status against silicon and 0x0D must stay UNVERIFIED."
    - "MUST NOT run any hardware command beyond the identity checks, the uploads and one write per board. No monitor loop, no voltage-rail command, no repeated sweeps."
    - "MUST NOT edit either submodule. Firmware is built and uploaded here, never edited."
  artifacts:
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md
  key_links:
    - "The `controller:` identity line comes from `firestarter fw` — firestarter_app/firestarter/firmware.py:123 renders `Current firmware version: <v>, for controller: <board> on port <port>`. That is the identity command to run per port before driving anything, and again after each upload to prove the board is running the build you just made."
    - "Plan 119-08's single-exit restructure is what makes this measurement reachable: with an empty socket the FIRST page's write poll fails, so a report line placed only at the loop's normal exit would emit nothing. The reported value therefore covers only the bytes loaded before the abort, which is a real number and must be described as such rather than presented as a full-write figure."
    - "The report line uses the unconditional bare LOG_ID_U32 spelling, so a default `firestarter write` emits it without FLAG_VERBOSE. If it does not appear, that is a firmware defect to report, not a flag to add."
---

<objective>
Take D-16's page-load worst-interval measurement to all three attached boards with the shipped CLI, capture the
verbatim output per board, and record the numbers with full provenance in a dedicated artifact — naming the
flash-versus-timing conflation in the directive and staying inside the validation ceiling.

Purpose: F-118-01 turned "the t_BLC check should never fire" into a measured 4.7 % margin on one board, and the
page-load loop runs under the identical per-byte constraint. Whether that margin is board-invariant is an open
question, and the Leonardo is an ATmega32U4 while the Uno-class boards are ATmega328P with a different
register-write path and a 512-byte data buffer. Three boards is the operator's reversal of Phase 118's
Leonardo-only scope, and it is only possible because all three sockets are empty.

Output: `119-MEASUREMENT.md` with a per-board provenance block, raw log and number — or a not-measured record with
the exact failure for any board that does not run.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md
</context>

## Repo and commit mechanics

The only file this plan writes is `119-MEASUREMENT.md` in the **meta repo** under `.planning/`. Commit in
`/workspaces`, staging that single path explicitly. Both sub-repos must be clean and unchanged by this plan — the
firmware is only **built and uploaded** here, never edited. Do not stage either submodule gitlink.

## No operator checkpoint — and why, stated rather than skipped

This plan is `autonomous: true` **by decision**, not by omission. D-18 item 1 records the operator's statement of
2026-07-28 that all three sockets are empty. Do **not** insert a `checkpoint:human-verify`, a
`checkpoint:human-action`, a `checkpoint:decision`, or any question about socket state, chip presence or shield
revision.

The Uno-class chip-OUT-before-sideload rule is satisfied by that statement, and this plan says so rather than
silently skipping it: that rule exists because Uno-class uploads drive the shield bus, and the Leonardo is exempt.
With all three sockets empty there is nothing to remove.

The one verification that **does** belong here is Claude's own `controller:` port-identity check — a command run
inside Task 1, not a question asked of the operator. Port numbers shuffle across replug, so the port must be
identified, never assumed.

Note for a verifier: the plan-phase dispatch guidance asked that hardware tasks be marked `autonomous: false`.
That is overridden here by locked decision **D-18 item 1**, which is explicit that this plan is `autonomous: true`
with no checkpoint anywhere. Phase 118's Plan 118-07 set the same precedent under the same reasoning. The
override is deliberate and recorded, not an oversight.

## Artifacts this phase produces

**Created by THIS plan:** `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md`.

**Created earlier in Phase 119, and observable in this run's captured output:** catalog id
`MSG_INFO_PAGE_LOAD_WORST_US` (0x62) — Plan 119-01; the worst-per-byte-interval tracker, its single-exit
restructure and its report call in `eeprom28c_write_execute` — Plan 119-08. Also present in the uploaded firmware
but not exercised by this run: `CMD_SDP_UNLOCK` (9), `CMD_SDP_LOCK` (10), `is_memory_cmd()`,
`EEPROM_SDP_ENABLE[3]`, `eeprom28c_emit_sdp_sequence_timed()`, `eeprom28c_sdp_lock_execute()`,
`eeprom28c_sdp_unlock_execute()`, `eprom_sdp_lock()`, `eprom_sdp_unlock()`, the generic NULL-`main` refusal, and
catalog ids `MSG_INFO_SDP_LOCK` (0x60) and `MSG_INFO_SDP_LOCK_DONE_US` (0x61) — the last two are unreachable from
the shipped CLI until Phase 120's `dev sdp`, which is exactly why no lock is attempted here.

**Also observable:** the auto-unlock's existing report pair `MSG_INFO_SDP_UNLOCK` (0x5E) and
`MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F) from Phase 118, since a `write` on `0x0D` runs the auto-unlock — so this run
incidentally re-measures F-118-01's number on three boards instead of one. Capture it; it is a second datapoint
the milestone did not have.

## Requirement ownership

**LOCK-06 was closed by Plan 119-10** on its flash-delta clause. This plan does **not** re-open or re-mark it. It
discharges the **timing** directive PROJECT.md's FIFTH CORRECTION item 3 attached to LOCK-06, which is a different
budget — that conflation is named in the document rather than inherited. `REQUIREMENTS.md` must be **unchanged**
by this plan.

Confirm before finishing that LOCK-01 through LOCK-06 all read Complete and DEVTEST-01 still reads Pending. If any
disagrees, report it as a defect rather than fixing it here.

<tasks>

<task type="auto">
  <name>Task 1: Identify all three ports, flash each board, and capture one 0x0D write per board</name>
  <files>.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md sections 2 and 3 (the provenance block's four required items and the raw-log discipline, plus the recorded port map: /dev/ttyACM0 leonardo, /dev/ttyACM1 uno, /dev/ttyUSB0 uno328pb — and the fact that this map must be re-verified, not reused)
    - firestarter_app/firestarter/firmware.py lines 100-135 (the `Current firmware version: ..., for controller: ... on port ...` render at :123 — the identity line this task captures) and the `fw` command definition in the CLI handler module
    - firestarter/CLAUDE.md section "Build Commands" (`pio run -e <env>`, `pio run -t upload -e <env>`, and the 250000-baud monitor — note which envs map to which board)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md (the tracker's final code, the report line's spelling, and the fact that the report fires on the aborting exit — which is the exit an empty socket takes)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md section 4 (the three boards' flash and RAM figures, which each build in this task must match)
  </read_first>
  <action>
Seven steps. Capture the verbatim stdout of every command; Task 2 writes it up. Work one board at a time, fully,
before moving to the next — so a failure on board two does not contaminate board one's record.

**Step 1 — enumerate and identify every port.** List `/dev/ttyACM*` and `/dev/ttyUSB*`. At plan time three devices
were present. **Do not assume** which is which: the numbering shuffles across replug and Phase 118's recorded map
is a starting hypothesis, not an identity. For **each** candidate port, run `firestarter fw` against it and capture
the `Current firmware version: ..., for controller: ... on port ...` line verbatim. Build the port-to-board map
from those lines. Record all three identity lines, including any port that turns out not to be one of the three
expected boards. This is a Claude-side check — do not ask the operator which port to use.

**Step 2 — build and confirm build identity, per env.** From `/workspaces/firestarter`, run `pio run` and capture
the flash and RAM figures for `leonardo`, `uno` and `uno328pb`. Each must match the figure
`119-NONREGRESSION.md` section 4 recorded. If any differs, the working tree has drifted since the sweep — stop and
report rather than uploading a build the phase never swept. Record the firmware git SHA
(`git -C /workspaces/firestarter rev-parse --short HEAD`) and its branch as the build identity for all three.

**Step 3 — per board: upload, then re-verify identity.** For each board in turn:
`pio run -t upload -e <env>`, targeting the port identified in Step 1 if the upload needs it explicitly. All three
sockets are empty per the operator's 2026-07-28 statement, so the Uno-class chip-OUT-before-sideload rule is
satisfied and **no socket manipulation is required and none may be requested** — the Leonardo is exempt from that
rule anyway, because only Uno-class uploads drive the shield bus. Immediately after each upload, re-run
`firestarter fw` against that port and capture the identity line again: that is what proves the board is running
the build you just made.

**Step 4 — prepare one payload, used for all three boards.** `firestarter write` needs an input file. Create a
deterministic file in the scratch directory. It must be large enough to span **at least two 64-byte pages** so the
per-byte tracker sees more than one page's worth of loads — but note that with an empty socket the first page's
write poll fails and the loop takes the aborting exit, so in practice the reported interval covers the first page's
loads. Record the exact byte count and content pattern. If the host rejects a file shorter than the chip's
`memory-size`, pad to the full 32768 bytes for `at28c256` and record that instead. Do not use a repo file as the
payload; the record must state exactly what was written.

**Step 5 — one write per board, and nothing else.** For each board, issue
`firestarter write at28c256 -b --force <payload>` against its identified port and capture the **complete verbatim
output**. Both flags are load-bearing and their reasons must be recorded:
- `-b` skips the blank check, which an empty socket fails, so the run reaches `eeprom28c_write_execute` at all.
  On `0x0D` this skips **nothing else**, because that protocol has no erase arm — the standing caveat that
  `write -b` also skips erase and can silently corrupt a non-blank chip on other families does not apply here.
  State that reasoning rather than relying on the memory note.
- `--force` demotes a chip-id mismatch from an error to a warning so `eeprom28c_write_init` proceeds rather than
  returning early.

Expect in the output, in roughly this order: a chip-id mismatch warning; `MSG_INFO_SDP_UNLOCK`'s rendered line;
`MSG_INFO_SDP_UNLOCK_DONE_US`'s rendered line with its microsecond value (a **second datapoint for F-118-01 on
this board** — capture it, it is a number the milestone did not have); then the write failing at the first page
because the socket is empty; and `MSG_INFO_PAGE_LOAD_WORST_US`'s rendered line with the worst per-byte interval.
**The downstream write failure is expected and is not a defect** — it is exactly the aborting exit Plan 119-08's
single-exit restructure made reportable. Record the worst-interval value exactly as printed; do not round it. Also
record whether `MSG_WARN_SDP_TBLC_EXCEEDED` appeared on the unlock path; if it did on any board, that is a genuine
finding worth recording, not something to fix here.

If the page-load line does **not** appear at all, that is a firmware defect to report — the report uses the
unconditional bare `LOG_ID_U32` spelling, so a default `write` must emit it. Do **not** add `--verbose` or any
flag to coax it out; that would prove the wrong thing.

**Step 6 — uno328pb's extra handling.** Never trust N=1 on that board: it is bench-unstable, so on a timeout,
retry (at most twice) and record every attempt including the failures. It also has a VPP-recal and
program-brownout history, but `0x0D` is a 5 V protocol with **no VPP rail**, so that mechanism should not apply —
state that reasoning explicitly, and if it browns out anyway, record it as a **finding** rather than treating the
plan as failed. And remember it is really a plain Uno carrying mismatched firmware: if its number differs from the
Uno's, do **not** attribute the difference to ATmega328PB silicon.

**Step 7 — D-19 on any failure: PROCEED.** If a board does not enumerate, an upload fails, an identity line never
names the expected controller, or a run never reaches the page-load line: **proceed to the next board.** Do not
retry more than twice per board, do not substitute one board for another, and do not fabricate a value. Record
not-measured for that board with the exact failure and the exact command that produced it. Never hard-block on one
USB enumeration — that is Phase 118's D-14 discipline and this project's CI-PENDING / structurally-green precedent
from Phases 98 and 103. A partial three-board result with one honest gap is the expected acceptable outcome, not a
failure.

Do **not** run any other command against any board. Identity checks, one upload each, one write each. No monitor
loop, no `vpp` or `vpe` command, no `dev reg`, no repeated sweeps, and **no attempt at cmd 9 or cmd 10** — the
standalone lock and unlock are unreachable from the shipped CLI and a raw-frame instrument was explicitly rejected.
  </action>
  <verify>
    <automated>ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null; cd /workspaces/firestarter && git rev-parse --short HEAD && pio run 2>&1 | grep -E 'Flash|RAM|SUCCESS' | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - An identity line is captured verbatim for EVERY candidate port, and the port-to-board map is built from those lines rather than from Phase 118's recorded map
    - `pio run` flash and RAM figures for all three envs match `119-NONREGRESSION.md` section 4; any mismatch stopped the task and was reported
    - The firmware git SHA and branch are captured as the build identity
    - For each board attempted: the upload ran, and `firestarter fw` was re-run afterwards with its identity line captured
    - No socket manipulation was requested and no operator question was asked at any point
    - The payload's exact byte count and content pattern are recorded, and it spans at least two 64-byte pages
    - For each board attempted: the complete verbatim output of exactly one `firestarter write at28c256 -b --force <payload>` run is captured
    - The reasons for both `-b` and `--force` are recorded, including that `-b` skips nothing else on `0x0D` because that protocol has no erase arm
    - The `MSG_INFO_PAGE_LOAD_WORST_US` value is recorded exactly as printed and unrounded, per board
    - The `MSG_INFO_SDP_UNLOCK_DONE_US` value is also captured per board as a second F-118-01 datapoint, and whether `MSG_WARN_SDP_TBLC_EXCEEDED` appeared is recorded either way
    - Exactly one write command was issued per board; no monitor loop, no voltage-rail command, no repeated sweeps, and no attempt at cmd 9 or cmd 10
    - For uno328pb: every attempt including failures is recorded, at most two retries were made, and the no-VPP-rail reasoning is stated
    - For any board that failed: the exact failure and command are recorded, no value was fabricated, no board was substituted, and the run proceeded to the next board
  </acceptance_criteria>
  <done>Every attached port is identified by command, each reachable board is running the phase's swept build, and each has produced one captured `0x0D` write with its worst per-byte page-load interval and its SDP unlock duration — or an honest not-measured record with the exact failure.</done>
</task>

<task type="auto">
  <name>Task 2: Write 119-MEASUREMENT.md with per-board provenance, inside the validation ceiling</name>
  <files>.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md read WHOLE (sections 1 through 7 plus the Disposition — sections 1 and 6 are the exact wording that survived validation-ceiling review and must be followed; sections 2 and 3 repeat PER BOARD here)
    - .planning/REQUIREMENTS.md section "Validation Ceiling" (the provable list, the NOT-provable list including "that tBLC is met *as accepted by the die*", the permitted claim and the forbidden claim — all quoted verbatim in section 6)
    - .planning/PROJECT.md's FIFTH correction block (F-118-01's 572-against-600 finding and its explicit page-load directive at LOCK-06) and the SIXTH block Plan 119-09 added (the flash-versus-timing conflation already recorded there)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md sections 4 and 7 (the flash figures to cross-reference rather than restate, and the ceiling wording already used in that document)
    - firestarter/src/proms/eeprom_28c.cpp as Plan 119-08 left it (the tracker's gh#11 comment — its conflation framing is the wording this document reuses)
  </read_first>
  <action>
Write `119-MEASUREMENT.md`. It is a standalone artifact, not a section of a SUMMARY — SUMMARYs are read
frontmatter-first downstream, and Phase 122's closeout and any future gh#11 work both need these numbers findable
with their provenance intact. Mirror `118-MEASUREMENT.md` section for section, with sections 2 and 3 repeated per
board.

**Section 1 — what was measured, and what it is not.** State it in the first paragraph, before any number appears:
this is the worst wall-clock interval between consecutive `handle->firestarter_set_data` calls in
`eeprom28c_write_execute`'s per-byte page-load loop, measured with `micros()` reads inside that loop, on real
boards. **The subject is the MCU driving its own latches, never the chip.** It does not show that any byte was
accepted, that the die entered or left the protected state, or that `t_BLC` is met *as accepted by the die* —
REQUIREMENTS.md's validation ceiling lists that last item explicitly as not provable without an AT28C part.

Then name the **conflation**, because D-16 requires it said out loud: PROJECT.md's FIFTH CORRECTION item 3 directs
this measurement at LOCK-06, but LOCK-06 is a **flash** budget in bytes of program memory and F-118-01 is a
**timing** budget in microseconds per byte load. Those are different budgets. The timing question is answered
anyway, because the page-load loop runs under the identical `AT28C_TBLC_MAX_US` constraint and is where gh#11's
symptom actually lives. State that LOCK-06 itself was closed by Plan 119-10 on the flash axis and that this
document does not re-open it.

Then state gh#11's real shape, reusing the firmware comment's wording: a completion-and-data-landed **conflation**
bug — a whole-byte equality compare that passed spuriously whenever the old byte already equalled the new one —
**not** a sampling-rate or polling-frequency bug. FIX-06 split it into two functions with one job each.

Finally state what the reported value covers: with an empty socket the first page's write poll fails and the loop
takes its aborting exit, so the number covers only the bytes loaded before that abort. That is a real and useful
number — and it is the condition under which gh#11's symptom appears — but it is **not** a full-write figure and
must not be described as one.

**Section 2 — provenance block, per board.** For each of the three boards, all four items D-20 requires:
- the exact command issued, verbatim, including `-b`, `--force` and the payload path
- the `controller:` identity line for the port, verbatim, plus the identity re-check after upload
- board and firmware build identity: board name, env name, firmware git SHA and branch, and that env's flash and
  RAM figures from the build that was uploaded — cross-referencing `119-NONREGRESSION.md` section 4 rather than
  restating the deltas
- the payload's byte count and content pattern

**Section 3 — raw captured log, per board.** The complete verbatim output of each run, in a fenced block, unedited.
Include the chip-id mismatch warning and the downstream write failure — both are expected with an empty socket and
both are part of the evidence that the run happened as described. Do not trim, summarise or reorder. For uno328pb,
include every attempt, failures included.

**Section 4 — the numbers.** Per board, on its own greppable line: the worst per-byte page-load interval exactly as
printed, unrounded. Alongside them, the per-byte datasheet maximum `AT28C_TBLC_MAX_US` = 100 us that the interval
is being read against, stated as **context, not a pass/fail gate** — D-16 declined a runtime budget check on this
path, so there is no threshold being tested here. Then a short comparison across the three boards, with the
explicit caution that a difference between the Uno and the uno328pb must **not** be attributed to ATmega328PB
silicon, because that board is really a plain Uno carrying mismatched firmware. Also record, per board, the SDP
unlock duration from `MSG_INFO_SDP_UNLOCK_DONE_US` against its 600 us budget — a second and third datapoint for
F-118-01, which had only the Leonardo's 572 us. Note whether `MSG_WARN_SDP_TBLC_EXCEEDED` fired on any board; if it
did not, say that a non-firing check is the expected and correct outcome on a 16 MHz AVR, not an absence of
evidence.

**Section 5 — socket state and bench scope, stated plainly.** All three sockets were empty, per the operator's
statement of 2026-07-28. Explain why that does not invalidate the number: the measurement is the MCU driving its own
latches, so socket contents are irrelevant to load timing — and it is precisely why the abort path is the one that
reports. This is also why no operator confirmation was needed and none was requested, and why the Uno-class
chip-OUT-before-sideload rule was satisfied rather than skipped. Record the **three-board scope as a reversal** of
Phase 118's deliberate Leonardo-only D-12, with the operator's words and every constraint that came with it, so the
next phase does not read three boards as the new default. And state that the **lock's own hardware duration was not
measured and was not attempted**: `CMD_SDP_LOCK` is unreachable from the shipped CLI until Phase 120's `dev sdp`,
and a throwaway raw-frame instrument was explicitly rejected because it would exercise a brand-new state-mutating
command on hardware through an unreviewed tool. That is a deliberate deferral, not a gap.

**Section 6 — validation ceiling.** Quote REQUIREMENTS.md's permitted claim and forbidden claim **verbatim**, plus
the NOT-provable list. State which side of the line these measurements sit on: they support the permitted claim's
clause about "a documented and measured host-side timing assumption" and support **nothing** in the forbidden
claim. `0x0D` stays `UNVERIFIED` in the PROTOCOL-LEDGER, **zero** chips change `support_status`, and the
**84**-chip count is unchanged. State explicitly that these numbers must **not** be recorded in the
PROTOCOL-LEDGER: that file records bench-verification status against silicon, and a measurement there invites
exactly the ceiling-crossing misread this milestone guards against.

**Section 7 — downstream consumers.** Name them so the numbers are findable: Phase 122's closeout, which frames
gh#11 and gh#12 as "here is what changed and why we believe it addresses your report; please re-test" and never as
a verified fix; and any future phase that revisits gh#11 on real silicon, which needs the raw numbers with
provenance rather than a rounded figure quoted in prose. Note that the three-board comparison is the datapoint
F-118-01's board-invariance question was actually asking for.

**Disposition.** A short closing block: what was measured, what was not measured and why, and any board recorded
not-measured with its exact failure. State that `REQUIREMENTS.md` is unchanged by this plan and that LOCK-06 was
closed by Plan 119-10 on the flash axis.

**If any board took D-19's failure path**, that board keeps sections 2 (as far as it got) and gets a
**not-measured** record in place of its sections 3 and 4: the exact failure, the exact command, how many attempts
were made, and the statement that no value was fabricated and no board was substituted. The other boards' records
stand unaffected.

Then review the finished document **line by line** for validation-ceiling compliance: no sentence may be readable
as bench-validating `0x0D` on AT28C silicon, as the die accepting a sequence, or as `t_BLC` being met as accepted
by the die. Record in the SUMMARY that the review was done and what it found.

Leave `.planning/REQUIREMENTS.md` **unchanged**. Confirm by reading that LOCK-01 through LOCK-06 all read Complete
and DEVTEST-01 still reads Pending.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md && grep -c 'controller:\|UNVERIFIED\|support_status\|AT28C_TBLC_MAX_US\|--force\|conflation' /workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-MEASUREMENT.md && cd /workspaces && git diff --quiet .planning/REQUIREMENTS.md && echo REQS_UNCHANGED</automated>
  </verify>
  <acceptance_criteria>
    - `119-MEASUREMENT.md` exists with sections 1 through 7 plus a Disposition block, mirroring `118-MEASUREMENT.md`
    - Section 1's first paragraph states that the subject is the MCU driving its own latches and not the chip, before any number appears, and states that `t_BLC` as accepted by the die is not provable
    - Section 1 names the flash-versus-timing conflation explicitly, states that LOCK-06 was closed on the flash axis by Plan 119-10, and frames gh#11 as a conflation bug rather than a sampling-rate bug
    - Section 1 states that the reported value covers only the bytes loaded before the abort and is not a full-write figure
    - Section 2 contains a per-board provenance block with all four D-20 items, including the verbatim `controller:` line before and after upload, the firmware SHA and branch, and the env's flash/RAM figures cross-referenced to `119-NONREGRESSION.md`
    - Section 3 contains the complete unedited run output per board in fenced blocks, including the chip-id mismatch warning and the downstream failure, and every uno328pb attempt including failures
    - Section 4 records each board's worst interval unrounded on its own greppable line, presents `AT28C_TBLC_MAX_US` = 100 us as context rather than a gate, cautions against attributing an Uno-versus-uno328pb difference to ATmega328PB silicon, and records each board's SDP unlock duration against 600 us as additional F-118-01 datapoints
    - Section 4 states whether `MSG_WARN_SDP_TBLC_EXCEEDED` fired on any board, and if not, that a non-firing check is the expected correct outcome
    - Section 5 states the empty sockets and why they do not invalidate the number, records the three-board scope AS a reversal of Phase 118's D-12 with the operator's words and its constraints, and states that the lock's own hardware duration was deliberately not attempted with D-17's reason
    - Section 6 quotes the permitted and forbidden claims verbatim, contains `UNVERIFIED`, `support_status` and `84`, and explicitly forbids recording these numbers in the PROTOCOL-LEDGER with the reason
    - Section 7 names Phase 122's closeout and any future gh#11 work as consumers, with the never-as-a-verified-fix framing
    - Any board that failed has a not-measured record with the exact failure, the attempt count, and the statement that nothing was fabricated and no board substituted
    - No sentence is readable as bench-validating `0x0D` on AT28C silicon or as the die accepting a sequence; the line-by-line review outcome is recorded in the SUMMARY
    - `git diff --quiet .planning/REQUIREMENTS.md` succeeds and the verify command prints `REQS_UNCHANGED`; LOCK-01 through LOCK-06 read Complete and DEVTEST-01 reads Pending
    - `git -C /workspaces show --stat HEAD` lists only `.planning/` paths; both sub-repo working trees are clean
  </acceptance_criteria>
  <done>Three boards' worst per-byte page-load intervals — or honest not-measured records — are captured with full per-board provenance and raw logs, the flash-versus-timing conflation is named, the three-board scope is recorded as a reversal with its constraints, the lock's unmeasured hardware duration is recorded as a deliberate deferral, and nothing in the document crosses the validation ceiling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a USB port number to a board identity | `/dev/ttyACM*` numbering shuffles across replug, so a port number is not an identity |
| a measured microsecond number to a claim about silicon | The single most likely ceiling crossing in this milestone |
| a failed board run to the recorded result | The pressure to produce a value where none was measured |
| host driving real hardware | Three physical boards with empty sockets; a stray command could route voltage or drive a bus |
| an unreachable new command to a bench attempt | `cmd 10` is irreversibly protective on some parts and has no CLI safety gate until Phase 120 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-11-CEILING | Repudiation | `119-MEASUREMENT.md` wording | critical | mitigate | The MCU-not-the-chip statement is required in the first paragraph before any number; the permitted and forbidden claims are quoted verbatim; the document forbids recording the numbers in the PROTOCOL-LEDGER; a line-by-line review for silicon claims is an acceptance criterion |
| T-119-11-LOCKATTEMPT | Denial of service (physical) | an irreversible SDP lock on a real part | critical | mitigate | Explicitly prohibited: `cmd 9` and `cmd 10` are unreachable from the shipped CLI and a raw-frame instrument was rejected. Only identity checks, one upload and one `write` per board are permitted. The destructiveness confirm, `-y` and the SAFE-04 absent-chip hard-fail land with Phase 120's HOST-01 |
| T-119-11-WRONGPORT | Spoofing | `/dev/ttyACM*` selection | high | mitigate | Per-candidate `firestarter fw` identity check with every line captured, the map built from those lines rather than from Phase 118's, and a re-check after each upload |
| T-119-11-FABRICATE | Repudiation | a value for a board that did not run | critical | mitigate | D-19 is written into Task 1 step 7 and Task 2's alternate path: proceed, record not-measured with the exact failure, never fabricate, never substitute a board, at most two retries |
| T-119-11-STRAYCMD | Denial of service | the three shields | high | mitigate | Exactly one write per board; no monitor loop, no `vpp` or `vpe`, no `dev reg`, no repeated sweeps — all stated as task-level criteria |
| T-119-11-DRIFT | Tampering | the uploaded build versus the swept build | medium | mitigate | Step 2 requires each env's flash figure to match `119-NONREGRESSION.md` section 4 and stops if it differs; the firmware SHA is captured as build identity and the identity line is re-checked after each upload |
| T-119-11-CHECKPOINT | Repudiation | an inserted operator question | medium | mitigate | The plan is `autonomous: true` by locked decision D-18 item 1 and explicitly forbids any socket-state or shield-revision question; the override of the dispatch guidance is recorded in the plan rather than left implicit |
| T-119-11-328PB | Repudiation | attributing a per-board difference to 328PB silicon | medium | mitigate | Recorded in the must-haves, in Task 1 step 6 and as an acceptance criterion on section 4: that board is really a plain Uno carrying mismatched firmware |
| T-119-11-BROWNOUT | Denial of service | uno328pb's program-brownout history | medium | accept | `0x0D` is a 5 V protocol with no VPP rail, so the mechanism should not apply; the reasoning is stated and a brownout is recorded as a finding rather than as a failed plan |
| T-119-11-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. An identity line is captured for every candidate port and the port-to-board map is derived from them.
2. Each env's `pio run` figures match `119-NONREGRESSION.md` section 4; the firmware SHA and branch are recorded.
3. Exactly one `firestarter write at28c256 -b --force <payload>` per board, with complete verbatim output captured.
4. Each board's worst per-byte interval and SDP unlock duration are recorded unrounded; the WARN's presence or absence is stated.
5. `119-MEASUREMENT.md` has sections 1 to 7 plus Disposition and passes the line-by-line ceiling review.
6. Any failing board has a not-measured record with the exact failure and attempt count; nothing fabricated, no board substituted.
7. No operator question was asked; no lock or unlock command was attempted; no extra hardware command was run.
8. `REQUIREMENTS.md` is unchanged; LOCK-01 through LOCK-06 read Complete and DEVTEST-01 reads Pending.
9. Both sub-repo working trees clean; one meta commit staging only `.planning/` paths. Suggested subject: `docs(119-11): record the measured page-load worst-interval on three boards with provenance (D-16)` — or, if a board took D-19's path, `docs(119-11): record the page-load measurement on three boards, one not-measured with reason (D-16, D-19)`.
</verification>

<success_criteria>
- Every attached port was identified by command, never assumed, and each reachable board ran the phase's swept build.
- Each reachable board produced one captured `0x0D` write with its worst per-byte page-load interval recorded unrounded, plus its SDP unlock duration as an additional F-118-01 datapoint.
- Any unreachable board is recorded not-measured with its exact failure, with nothing fabricated and no board substituted.
- The flash-versus-timing conflation is named, gh#11 stays framed as a conflation bug, and the abort-path scope of the number is stated.
- The three-board scope is recorded as a reversal of Phase 118's D-12, with the operator's words and every constraint.
- The lock's own hardware duration is recorded as a deliberate deferral to Phase 120, not as a gap.
- No sentence crosses the validation ceiling; `0x0D` stays `UNVERIFIED` and `REQUIREMENTS.md` is untouched.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-11-SUMMARY.md` when done.
Record: the port-to-board map with every verbatim `controller:` line; the firmware SHA and each env's flash figure
as uploaded; the payload's byte count and pattern; per board the worst per-byte interval, the SDP unlock duration,
and whether the t_BLC WARN fired; every uno328pb attempt including failures; any board recorded not-measured with
its exact failure and attempt count; confirmation that no lock or unlock was attempted and no operator question was
asked; the line-by-line ceiling-review outcome for `119-MEASUREMENT.md`; and confirmation that `REQUIREMENTS.md` is
unchanged with LOCK-01 through LOCK-06 Complete and DEVTEST-01 Pending.
</output>
