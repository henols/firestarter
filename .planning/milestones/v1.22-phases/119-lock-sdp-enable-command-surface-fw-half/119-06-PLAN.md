---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 06
type: execute
wave: 6
depends_on: [119-05]
files_modified:
  - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  - firestarter_app/tests/test_sdp_table_parity.py
autonomous: true
requirements: [LOCK-05, LOCK-02]

must_haves:
  truths:
    - "D-10, and this is LOCK-05's discharge: a THREE-WAY byte-identity guard asserting EEPROM_SDP_ENABLE equals FLASH_ENABLE_WRITE_PROTECTION equals FLASH_ENABLE_WRITE byte-for-byte, AND that they are three DISTINCT objects. That makes 'the name is the only discriminator' a machine-checked fact rather than a comment. The distinctness half guards the alias-refactor hazard: a future dedupe that pointed one name at another's storage would still satisfy the equality legs alone."
    - "RESEARCH F-K: the TWO-WAY leg already exists in-tree at test_sdp_harness.cpp:297-311 (test_lock05_enable_write_and_write_protection_identical), together with its comment recording the datasheet rationale and stating that a trace-based negative between THOSE TWO SPECIFIC tables is impossible by construction and a later editor must not try to add one. Only the third leg, the distinctness assertions and the no-payload assertion are new. Do not duplicate or replace the existing case."
    - "D-12: the lock reports OK and the honesty lives in the message text. The proof is a frame-id plus response_code assertion: MSG_INFO_SDP_LOCK (0x60) and MSG_INFO_SDP_LOCK_DONE_US (0x61) both appear in the captured frame ids, in order, and handle->response_code is still RESPONSE_CODE_OK after the op - the SDP path never writes it (117 D-05 / 118 D-02, permanently enforced by test_case8_completion_poll_preserves_prior_severity)."
    - "D-14: the lock's t_BLC budget check must be proven to FIRE when over budget AND to NOT FIRE at a normal elapsed time. The paired positive-plus-control shape is mandatory: a check that always fires and a check that never fires are both hollow. The lock's budget is 3 times AT28C_TBLC_MAX_US = 300 us, and at F-118-01's measured ~95 us/byte a real board lands near ~286 us, the same ~4.7% margin as the unlock's 572-against-600."
    - "D-13's 'an SDP unlock reads identically however it was triggered' is assertable: the standalone unlock op's emitted stream must be byte-identical to the auto-unlock's stream from eeprom28c_write_init, because both go through the same shared helper with the same table and the same completion wait."
    - "RESEARCH F-L: test_sdp_table_parity.py's extractor is name-exact and word-anchored, so EEPROM_SDP_ENABLE cannot collide with EEPROM_SDP_DISABLE - but that module was broken three times by Phase 117's identifier and declaration-syntax changes, so it is re-verified empirically, never trusted. Adding an enable-table parity leg gives D-10 a second, INDEPENDENT oracle: the firmware one is a link-time object comparison, this one is textual."
    - "Assert on the ordered stream's CONTENT and on frame-id membership in order, never on counts. sdp_expected.h's comparator never counts anything, and sdp_ids_contains is a content-order membership check by design."
    - "Baselines to hold: pio test -e native and -e native_nodevtools at 123/123 across 17 suites before this plan's additions, pio run 3/3 SUCCESS, and every host gate at Plan 119-04's recorded baseline."
  prohibitions:
    - "MUST NOT edit firestarter/include/flash_utils.h. It is read for FLASH_ENABLE_WRITE and FLASH_ENABLE_WRITE_PROTECTION only, and FIX-04 asserts it byte-untouched."
    - "MUST NOT add a trace-based negative between FLASH_ENABLE_WRITE_PROTECTION and FLASH_ENABLE_WRITE. The existing case's comment states it is impossible by construction and that a later editor must not try. Honour that."
    - "MUST NOT delete, replace or weaken test_lock05_enable_write_and_write_protection_identical. Extend beside it."
    - "MUST NOT dedupe, alias or delete FLASH_ENABLE_WRITE_PROTECTION. LOCK-05 requires the duplication PRESERVED; the distinctness assertions exist to make a future dedupe fail loudly."
    - "MUST NOT modify any production file in this plan. Plan 119-04 landed the code; this plan only observes it."
    - "MUST NOT assert a budget WARN by count, and MUST NOT let the control case pass merely because no frames were captured - it must capture the other frames and assert the WARN id is absent from them."
    - "MUST NOT use TEST_IGNORE_MESSAGE."
    - "MUST NOT add CMD_SDP_* to firestarter_app/firestarter/constants.py. Phase 120 HOST-03."
  artifacts:
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
    - firestarter_app/tests/test_sdp_table_parity.py
  key_links:
    - "The three-way guard can pin the PRODUCTION EEPROM_SDP_ENABLE array only because Plan 119-04 gave it external linkage. Add an `extern const byte_flip_t EEPROM_SDP_ENABLE[3];` declaration in test_sdp_harness.cpp beside the existing one for EEPROM_SDP_DISABLE at :43-48, copying its comment about why the extern is load-bearing."
    - "sdp_tables_identical(a, b, len) at test_sdp_harness.cpp:152-159 is the positional table comparator to reuse. sdp_snapshot(snap, N) must precede any second drive() because drive() calls clear_strobes()."
    - "captured_frames plus sdp_captured_frame_ids() plus sdp_ids_contains() in test_eeprom28c_sdp.cpp:150-181 are the frame-id capture chain for D-12's and D-14's assertions. test_sdp_harness.cpp mocks micros() to AlwaysReturn(0), so budget-timing cases belong in test_eeprom28c_sdp with its scripted tick queue, not in the harness."
---

<objective>
Close LOCK-05 with a three-way byte-identity and distinct-objects guard over the production tables, prove
the lock's report shape satisfies D-12 in the emitted frames, prove D-14's budget check both fires and does
not fire, assert the standalone unlock's stream is byte-identical to the auto-unlock's, and add a second
independent source-text oracle for the three-way identity in the host suite.

Purpose: `AA-55-A0` is the sharpest hazard in the phase — the whole table is identical to the write-enable
prefix, so the name is the only discriminator. LOCK-05 asks for that duplication to be preserved *and for
the reason to be recorded*. A comment alone is the decorative-invariant shape this project has been paying
down since v1.12's hollow GATE-03. This plan makes it a machine-checked fact in two independent ways.

Output: the three-way guard and its distinctness assertions in `test_sdp_harness`; cases 20 through 23 in
`test_eeprom28c_sdp`; an `EEPROM_SDP_ENABLE` parity leg in `test_sdp_table_parity.py`; LOCK-05 closed.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-04-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md
</context>

## Repo and commit mechanics

Two firmware test files land inside `firestarter/`, one host test module inside `firestarter_app/` — both
sub-repos already on `v1.22-at28c-software-data-protection-lifecycle`. Confirm with
`git branch --show-current` before writing. Commit inside each submodule, then one meta commit staging both
gitlinks plus the `.planning/` SUMMARY.

Firmware plans are **strictly sequential** against the shared `firestarter/.pio/build/` tree. Do not start
before 119-05's commits exist.

## Artifacts this phase produces

**Created by THIS plan:** `test_lock05_three_way_enable_table_identity` and
`test_lock05_enable_table_objects_distinct` (or equivalently named cases) plus an
`extern const byte_flip_t EEPROM_SDP_ENABLE[3];` declaration in `test_sdp_harness.cpp`; cases 20 through 23
in `test_eeprom28c_sdp.cpp`; an `EEPROM_SDP_ENABLE` parity leg in
`firestarter_app/tests/test_sdp_table_parity.py`.

**Created earlier:** the three catalog ids (119-01); the command values, predicate, second native env and
`test_cmd_admission` (119-02); `check_is_memory_cmd_no_ifdef.py` with its pytest and fixture (119-03);
`EEPROM_SDP_ENABLE[3]`, the shared timed-emit helper, both SDP ops, the `configure_eeprom28c` arms, the
`loop()` arms and the appended emit anchor (119-04); the four `SDP_FIXED_LOCK_*` goldens, the scripted
`micros()` queue and cases 13 through 19 (119-05).

**Created later:** the generic NULL-`main` refusal and the widened `build_src_filter` (119-07); the page-load
worst-interval tracker (119-08); the Phase 121 scope amendment (119-09); `119-NONREGRESSION.md` (119-10);
`119-MEASUREMENT.md` (119-11).

## Requirement ownership

This plan **closes LOCK-05**: the duplication is preserved, the three-way identity and the three-way
distinctness are machine-checked against the production objects, the no-payload stream assertion landed in
Plan 119-05, and the rationale is recorded in two places (the `eeprom_28c.cpp` comment from Plan 119-04 and
the pre-existing `test_sdp_harness.cpp:291-296` comment). Record in the SUMMARY that ROADMAP criterion 5's
"header comment" was satisfied in `eeprom_28c.cpp` instead, deliberately, because `flash_utils.h` is FIX-04
byte-frozen — the same deviation class as D-05 and D-15.

It advances **LOCK-02** by proving the report shape and the standalone unlock's stream identity.
**LOCK-02 stays OPEN** until Plan 119-07's dispatch proofs. Mark **only LOCK-05** Complete; LOCK-02, LOCK-04
and LOCK-06 stay Pending.

<tasks>

<task type="auto">
  <name>Task 1: Add the three-way table-identity and distinct-objects guard beside the existing two-way leg</name>
  <files>firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 40-60 (the `extern const byte_flip_t EEPROM_SDP_DISABLE[6];` declaration with its comment explaining that plan 117-02 granted external linkage so the guard can pin the PRODUCTION table rather than a transcription — copy this comment shape for EEPROM_SDP_ENABLE)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 145-160 (sdp_tables_identical — positional, no counting) and lines 120-150 (drive_reference_emitter with its load-bearing configure_memory-then-reset_register_cache-then-clear_strobes order)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 291-311 (test_lock05_enable_write_and_write_protection_identical and its full comment — read the comment carefully: it states the datasheet rationale, that LOCK-05 requires the duplication PRESERVED, and that a trace-based negative between those two specific tables is impossible by construction and a later editor must not add one)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 325-415 (test_fix05_terminal_byte_and_table_identity_guards and test_fix05_guard_rejects_planted_terminal_mutation — the terminal-byte pinning and the pointer-distinctness idiom for the unlock table, plus the planted-mutation counterpart that proves the guard can fail)
    - firestarter/include/flash_utils.h lines 24-59 (FLASH_ENABLE_WRITE, FLASH_ENABLE_WRITE_PROTECTION, FLASH_ERASE, FLASH_DISABLE_WRITE_PROTECTION — read only; do not edit)
    - firestarter/src/proms/eeprom_28c.cpp as Plan 119-04 left it (the EEPROM_SDP_ENABLE definition and its extern line)
  </read_first>
  <action>
Add an `extern const byte_flip_t EEPROM_SDP_ENABLE[3];` declaration to
`test/native/avr/test_sdp_harness/test_sdp_harness.cpp`, immediately beside the existing one for
`EEPROM_SDP_DISABLE` at `:43-48`, and copy that comment's shape: state that the array is DEFINED in
`src/proms/eeprom_28c.cpp` which `[env:native]` links into every test binary via
`build_src_filter = +<proms/>` with `test_build_src = yes`, that Plan 119-04 granted it external linkage, and
that this declaration is what lets the guard below read the PRODUCTION table directly rather than a
transcribed test-local copy.

Add two cases immediately after `test_lock05_enable_write_and_write_protection_identical`, and register both
with `RUN_TEST` in `main()`. **Do not modify, replace or weaken the existing case** — it already carries the
datasheet rationale and the instruction that a trace-based negative between those two specific tables must
never be added. Extend beside it.

**Case A — the third identity leg.** Assert, using `sdp_tables_identical` over length 3, that
`EEPROM_SDP_ENABLE` is byte-identical to `FLASH_ENABLE_WRITE_PROTECTION` and byte-identical to
`FLASH_ENABLE_WRITE`. Assert both legs separately with distinct messages rather than relying on transitivity,
so a failure names which pair diverged. Also pin the terminal byte: assert `EEPROM_SDP_ENABLE[2].byte` is
`0xA0` and its address is `0x5555`, and assert the first two entries' address/byte pairs as well — that is
three writes fully pinned, mirroring what `test_fix05_terminal_byte_and_table_identity_guards` does for the
six-write unlock table.

The case's message text and comment must record why the identity is a **safety** property: `AA-55-A0` is
simultaneously the SDP-enable body and the protected-**write** prefix, so the name is the only discriminator
and the duplication is datasheet-correct rather than dead code. Cite Atmel doc0270 section 19 note 2. State
that LOCK-05 requires the duplication PRESERVED, that abandoned commit `0052c42`'s dedupe stays abandoned,
and that the load-bearing absence — no data write following the three loads — is asserted on the stream in
Plan 119-05's case 17 because a table comparison structurally cannot observe an absence.

**Case B — three-way distinctness.** Assert all three pairwise pointer inequalities over
`(const void*)EEPROM_SDP_ENABLE`, `(const void*)FLASH_ENABLE_WRITE_PROTECTION` and
`(const void*)FLASH_ENABLE_WRITE`, each with its own message naming the pair. Comment: this is the
alias-refactor hazard. A future "cleanup" that pointed one name at another's storage would still satisfy
every equality leg in Case A while destroying the semantic distinction LOCK-05 exists to preserve — the same
hazard `test_fix05_...` already guards for the unlock table against `FLASH_ERASE`. State that these three
assertions are what make a dedupe fail loudly.

Also, for completeness against the one-nibble hazard class, assert `EEPROM_SDP_ENABLE` is a distinct object
from `EEPROM_SDP_DISABLE` and that their lengths differ (3 versus 6) — cheap, and it pins the structural
difference between lock and unlock at the table level to complement Plan 119-05's stream-level divergence
assertions.

Do **not** add a trace-based negative between `FLASH_ENABLE_WRITE_PROTECTION` and `FLASH_ENABLE_WRITE`. The
existing case's comment states it is impossible by construction; adding one would be a test that cannot fail.

Run the harness suite in both envs, then both full suites. The harness suite's case count rises by 2.
`test_sdp_harness` mocks `micros()` to `AlwaysReturn(0)`, so no timing case belongs here — that is Task 2's
file.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'EEPROM_SDP_ENABLE' test/native/avr/test_sdp_harness/test_sdp_harness.cpp && pio test -e native -f "*test_sdp_harness*" 2>&1 | tail -6 && git diff --quiet -- include/flash_utils.h && echo FROZEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test_sdp_harness.cpp` declares `extern const byte_flip_t EEPROM_SDP_ENABLE[3];` with a comment naming the external-linkage rationale and the production-table-not-a-transcription reason
    - A case asserts `EEPROM_SDP_ENABLE` byte-identical to `FLASH_ENABLE_WRITE_PROTECTION` AND byte-identical to `FLASH_ENABLE_WRITE`, in two separately-messaged assertions, using `sdp_tables_identical`
    - The same case pins all three entries' address and byte values, including the terminal `{0x5555, 0xA0}`
    - Its comment cites Atmel doc0270 section 19 note 2, states the name-is-the-only-discriminator safety framing, states that the duplication is PRESERVED per LOCK-05 with commit `0052c42` staying abandoned, and points at Plan 119-05's case 17 for the absence assertion
    - A second case asserts all three pairwise `(const void*)` inequalities among the three tables, each separately messaged, plus distinctness from `EEPROM_SDP_DISABLE` and the 3-versus-6 length difference
    - That case's comment names the alias-refactor hazard explicitly and states that these assertions are what make a dedupe fail loudly
    - `test_lock05_enable_write_and_write_protection_identical` is unmodified (verified by `git diff`)
    - No trace-based negative between `FLASH_ENABLE_WRITE_PROTECTION` and `FLASH_ENABLE_WRITE` was added
    - `git diff --quiet -- include/flash_utils.h` exits 0 and the verify command prints `FROZEN_OK`
    - `pio test -e native -f "*test_sdp_harness*"` and the same under `native_nodevtools` both pass with the case count risen by exactly 2
    - No production file is modified in the committed diff
  </acceptance_criteria>
  <done>The three-way byte-identity and the three-way object distinctness are machine-checked against the production arrays, beside the pre-existing two-way leg rather than replacing it, with the safety framing and the preserved-duplication decision recorded in the case messages.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the lock's report shape, its budget check both ways, and the standalone unlock's stream identity</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 145-200 (captured_frames, sdp_captured_frame_ids with its documented rurp_log_id wire layout, and sdp_ids_contains as a content-order membership check) and the scripted micros() queue as Plan 119-05 left it
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 610-830 (case 8's response_code-preservation invariant, case 9's suppression assertion, case 10's full-stream assertion, case 11's budget-WARN case with its AT28C_TBLC_MAX_US mirroring and citation, case 12's exactly-two-report-frames enumeration, and the main() RUN_TEST list)
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp cases 13 through 19 as Plan 119-05 left them (the lock-op drive helper and its load-bearing ordering)
    - firestarter/src/proms/eeprom_28c.cpp as Plan 119-04 left it (the shared helper's parameter order, the two ops' bodies, and AT28C_TBLC_MAX_US at :58 — the constant is a local #define, so mirror it as a named local with an explicit citation)
    - firestarter/include/messages.h (the generated values of MSG_INFO_SDP_LOCK, MSG_INFO_SDP_LOCK_DONE_US, MSG_INFO_SDP_UNLOCK, MSG_INFO_SDP_UNLOCK_DONE_US and MSG_WARN_SDP_TBLC_EXCEEDED — reference them by NAME in the test, never by hex literal)
  </read_first>
  <action>
Add four cases numbered 20 through 23 to `test_eeprom28c_sdp.cpp`, each registered with `RUN_TEST` in
`main()`. All four drive the **production** ops through `h.firestarter_operation_main(&h)` after
`configure_memory`, `reset_register_cache` and `clear_strobes`, in that load-bearing order.

**Case 20 — D-12's report shape.** Drive the lock op with `h.cmd = CMD_SDP_LOCK`, then assert three things:
`MSG_INFO_SDP_LOCK` appears in the captured frame ids; `MSG_INFO_SDP_LOCK_DONE_US` appears after it (assert
the ordered positions, not just membership, so the "starting" line cannot follow the "emitted" line); and
`handle.response_code` still equals `RESPONSE_CODE_OK`. Reference the ids by their generated **names**, never
by hex literals, so a catalog renumber cannot silently desync the test.

Its comment must record D-12's reasoning in full, because this case is the firmware-side proof of HOST-05:
`response_code` is deliberately never written on the SDP path, so an untouched path reports OK; therefore the
honesty cannot live in the status code and lives in the message text instead — `MSG_INFO_SDP_LOCK_DONE_US`'s
format string states both that the sequence was emitted and that the protection state is not readable. Also
state what is forbidden and why: an unconditional unverifiable-state WARN on every lock was rejected because
it warns on a correctly completed operation and trains users to ignore the WARN band; and reporting the DQ6
toggle poll's outcome as lock evidence was rejected because a settled toggle bit proves a write cycle
finished, not that protection latched — FIX-02's deleted mistake in a new costume. Add an assertion that
`MSG_WARN_SDP_UNLOCK_SKIPPED` and the unlock's two ids do **not** appear on the lock path, so the two
sequences' report surfaces cannot be conflated.

Also assert the report lines are the **unconditional** spelling by construction: drive the case with
`FLAG_VERBOSE` **not** set and assert both lock ids still appear. That is what pins the bare
`LOG_ID` / `LOG_ID_U32` choice (118 D-01) rather than the `FLAG_VERBOSE`-gated `LOG_INFO_ID*` family — with
the gated family a default `dev sdp enable` would go silent, which is the defect this spelling exists to
avoid.

**Case 21 — D-14's budget WARN fires.** Script the tick queue so the lock's two `micros()` reads differ by
more than the lock's budget, mirroring `AT28C_TBLC_MAX_US` as a named local with an explicit
`eeprom_28c.cpp:54` citation and computing the budget as three times that value (300 us) from the sequence
length, never as a literal. Drive the lock op and assert `MSG_WARN_SDP_TBLC_EXCEEDED` appears in the captured
frame ids **and** that `handle.response_code` is still `RESPONSE_CODE_OK` — the WARN carries severity in its
band alone and must never write `response_code` (117 D-05 / 118 D-02).

**Case 22 — the anti-hollow control: the budget WARN does NOT fire at a normal elapsed time.** Script ticks
whose difference is comfortably inside 300 us, drive the lock op, and assert `MSG_WARN_SDP_TBLC_EXCEEDED` is
**absent** from the captured frame ids **while** `MSG_INFO_SDP_LOCK` and `MSG_INFO_SDP_LOCK_DONE_US` are
present. The presence half is load-bearing: without it the case would pass vacuously if nothing were captured
at all. Comment: a check that always fires and a check that never fires are both hollow, so D-14 needs the
pair. Record F-118-01's numbers in the comment — 572 us measured against a 600 us budget on a real Leonardo,
about 95 us per byte against a 100 us per-byte datasheet maximum, so the lock's 300 us budget lands near
286 us and this check is genuinely load-bearing at n=3 rather than a latent invariant.

**Case 23 — the standalone unlock's stream is byte-identical to the auto-unlock's.** Drive the standalone
unlock op (`h.cmd = CMD_SDP_UNLOCK`) and snapshot the stream with `sdp_snapshot` — mandatory, because the
next drive calls `clear_strobes()`. Then drive `eeprom28c_write_init` on a fresh handle with
`FLAG_SKIP_SDP_UNLOCK` clear, and assert `sdp_first_divergence(snapshot, snapshot_len)` equals `-1` against
the resulting stream. Comment: this is D-13's "an SDP unlock reads identically however it was triggered" made
assertable, and it holds because both paths call the same shared helper with the same table and the same
completion wait. Also assert the standalone unlock's captured frame ids contain `MSG_INFO_SDP_UNLOCK` and
`MSG_INFO_SDP_UNLOCK_DONE_US` and **not** the lock's two ids — the reused-ids decision made explicit.

For case 23, be careful with the two drives' frame capture: clear `captured_frames` between them if the case
asserts on each separately, and say in the comment that the stream comparison is the primary assertion while
the id assertion is the secondary one.

Run the suite in both envs (23/23), then both full suites, which must now report 127/127 across 17 suites
(123 plus 4). If the count differs, report the actual number rather than adjusting an assertion. Then
`pio run` (3/3 SUCCESS).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*" 2>&1 | tail -8 && pio test -e native_nodevtools 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Four cases numbered 20 through 23 exist and are registered with `RUN_TEST` in `main()`
    - Case 20 asserts `MSG_INFO_SDP_LOCK` and `MSG_INFO_SDP_LOCK_DONE_US` appear in the captured ids with the ordered positions asserted, asserts `response_code` is still `RESPONSE_CODE_OK`, and asserts the unlock's ids and `MSG_WARN_SDP_UNLOCK_SKIPPED` are absent
    - Case 20 runs with `FLAG_VERBOSE` unset and still observes both lock ids, pinning the unconditional `LOG_ID` spelling
    - All message ids in all four cases are referenced by their generated names, never by hex literals
    - Case 20's comment records D-12's reasoning including both rejected alternatives (the blanket unverifiable-state WARN, and reporting the DQ6 poll as lock evidence)
    - Case 21 asserts `MSG_WARN_SDP_TBLC_EXCEEDED` appears and `response_code` is unchanged, with the budget derived from the sequence length times a locally-mirrored `AT28C_TBLC_MAX_US` carrying an explicit `eeprom_28c.cpp:54` citation
    - Case 22 asserts the WARN id is absent AND both lock INFO ids are present, so it cannot pass vacuously, and its comment records F-118-01's 572-against-600 measurement and the resulting ~286-against-300 expectation
    - Case 23 snapshots the standalone unlock's stream before the second drive and asserts an exact divergence of `-1` against the auto-unlock stream, plus the reused-ids membership and the absence of the lock's ids
    - `pio test -e native -f "*test_eeprom28c_sdp*"` and the same under `native_nodevtools` both pass 23/23
    - `pio test -e native` and `pio test -e native_nodevtools` both report 127/127 across 17/17 suites, or the actual count is reported as a finding
    - `pio run` reports SUCCESS on all three AVR envs
    - No production file is modified in the committed diff; `TEST_IGNORE_MESSAGE` appears nowhere
  </acceptance_criteria>
  <done>The lock reports OK with honest text proven by frame id and an untouched `response_code`; the t_BLC budget check is proven to fire and proven not to fire; and the standalone unlock is proven byte-identical to the auto-unlock, making D-13's reused-ids claim assertable rather than asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Add the host source-text parity leg, sweep every gate, and close LOCK-05</name>
  <files>firestarter_app/tests/test_sdp_table_parity.py</files>
  <read_first>
    - firestarter_app/tests/test_sdp_table_parity.py read whole, with attention to :117-125 (`_extract_byte_flip_pairs` and its name-exact word-anchored regex, which already accepts both `NAME[] = {` and `NAME[6] = {` because Phase 117 widened it, and whose required trailing `=` excludes the bare extern declaration line) and to how the module resolves the firmware source paths
    - firestarter/src/proms/eeprom_28c.cpp as Plan 119-04 left it (the EEPROM_SDP_ENABLE definition's exact declaration syntax, since the extractor matches on it)
    - firestarter/include/flash_utils.h lines 42-53 (FLASH_ENABLE_WRITE and FLASH_ENABLE_WRITE_PROTECTION as the extractor sees them)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-L and section F-R (why the extractor is safe against the new name, and the complete nine-gate checklist with this phase's disposition per gate)
  </read_first>
  <action>
Add one parity leg to `firestarter_app/tests/test_sdp_table_parity.py`, reusing `_extract_byte_flip_pairs`
unchanged: extract `EEPROM_SDP_ENABLE` from `firestarter/src/proms/eeprom_28c.cpp` and both
`FLASH_ENABLE_WRITE_PROTECTION` and `FLASH_ENABLE_WRITE` from `firestarter/include/flash_utils.h`, then
assert all three extracted address/byte pair lists are equal, with a message naming which pair diverged on
failure. Assert the extracted enable list has exactly three pairs and that its last pair is address `0x5555`
with byte `0xA0`.

Give the test a docstring explaining that it is a **second, independent** oracle for D-10's three-way
identity: the firmware guard in `test_sdp_harness` is a link-time comparison of the actual objects, and this
one is a source-text comparison. Two oracles with different failure modes is the point — a source-text
refactor that broke the firmware guard's linkage would still be caught here, and vice versa. Also note that
this keeps the host gate's coverage in step with the firmware, which is CORRECTION-4 item 4's spirit.

Target Python 3.9 syntax and conform to the module's existing style. Do not modify `_extract_byte_flip_pairs`
itself — if the new table does not extract cleanly, that is a finding about the firmware declaration syntax,
and RESEARCH F-L predicted it would extract cleanly, so report the discrepancy rather than widening the
regex silently.

Then sweep **every** gate on the CORRECTION-4 item-4 checklist, because this phase has now touched
`eeprom_28c.cpp`, `firestarter.cpp`, `firestarter.h`, `eprom_operations.{cpp,h}`, `platformio.ini` and three
test files:

- `python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q`
- `python3 tools/check_no_log_in_sdp_window.py` (exit 0, and its `PASS:` line's ranges should match the new
  baseline Plan 119-04 recorded — if they shifted again, record the new ones)
- `python3 tools/check_is_memory_cmd_no_ifdef.py` (exit 0)
- `python3 tools/check_dispatch.py` (exit 0 — this phase changes no dispatch **order**, so it must be
  unaffected; confirm rather than assume)
- `python3 tools/gen_sdp_bus_config.py` regenerate-and-diff, confirming `_shared/sdp_bus_config.h` stays
  blob-identical (no pinout change is in scope)
- `python3 tools/check_devtest_orchestrator.py` (it explicitly rejects any firmware path and asserts
  `dev test` is host-only Python; confirm it is untouched and that DEVTEST-01's firmware half does not
  interact with it)
- `ruff check` and `ruff format --check` against the py3.9/3.11 CI targets, with the version recorded

The two known-RED modules (`tests/test_audit_coverage_matrix.py`, `tests/test_no_programmer_found_*`) are not
this plan's damage — name them if they surface, do not chase them. Three boards are attached, which is why the
second one goes red.

Finally mark **LOCK-05 Complete** in `.planning/REQUIREMENTS.md`, with a parenthetical naming the three-way
identity guard, the three-way distinctness assertions, the no-payload stream assertion from Plan 119-05, and
the host source-text parity leg. In the SUMMARY, record explicitly that ROADMAP criterion 5's "header
comment" instruction was satisfied by the rationale comment beside `EEPROM_SDP_ENABLE` in `eeprom_28c.cpp`
plus the pre-existing `test_sdp_harness.cpp:291-296` comment, **deliberately** rather than by editing the
FIX-04 byte-frozen `flash_utils.h`, and that this is a recorded deviation of the same class as D-05's
LOCK-04 mechanism correction and D-15's headroom correction — so a verifier reads criterion 5 as
intent-satisfied, not failed. Do **not** edit `REQUIREMENTS.md`'s LOCK-05 wording, only its checkbox and the
parenthetical. Change no other requirement row.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_sdp_table_parity.py -q && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sdp_table_parity.py` contains a leg asserting the extracted `EEPROM_SDP_ENABLE`, `FLASH_ENABLE_WRITE_PROTECTION` and `FLASH_ENABLE_WRITE` pair lists are all equal, with a per-pair failure message
    - That leg also asserts the enable list has exactly three pairs and that its last pair is `0x5555` / `0xA0`
    - Its docstring explains that it is a second, independent, source-text oracle for D-10 and names the different failure modes
    - `_extract_byte_flip_pairs` is unmodified; if extraction needed a change, that was reported as a finding instead
    - `python3 -m pytest` over the six named host modules passes with zero failures
    - `python3 tools/check_no_log_in_sdp_window.py`, `python3 tools/check_is_memory_cmd_no_ifdef.py`, `python3 tools/check_dispatch.py` and `python3 tools/check_devtest_orchestrator.py` all exit 0
    - `tools/gen_sdp_bus_config.py` regenerates `_shared/sdp_bus_config.h` blob-identically
    - `ruff check` and `ruff format --check` pass against the py3.9/3.11 CI targets, with the version recorded
    - The two known-RED host modules are named as pre-existing if they surface, and were not chased
    - `pio test -e native`, `pio test -e native_nodevtools` and `pio run` are re-confirmed green after all edits
    - `.planning/REQUIREMENTS.md` shows LOCK-05 Complete with the parenthetical naming all four oracles, LOCK-05's wording unchanged, and LOCK-02, LOCK-04, LOCK-06 all still Pending
    - The SUMMARY records the criterion-5 deviation as deliberate and of the same class as D-05 and D-15
  </acceptance_criteria>
  <done>D-10's three-way identity has two independent oracles — a link-time object comparison in firmware and a source-text comparison in the host suite — every gate on the CORRECTION-4 checklist is green, and LOCK-05 is Complete with criterion 5's deviation recorded as intent-satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a table name to its semantics | `AA-55-A0` is simultaneously a lock body and a write prefix; only the name distinguishes them |
| an equality assertion to an identity claim | Three names aliased to one object satisfy every equality leg while destroying the distinction |
| a status code to a physical state | `response_code` reports OK on a path that cannot read the state it just tried to set |
| a runtime check to a proven invariant | A check that always fires and a check that never fires are equally hollow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-06-ALIAS | Tampering (destructive) | a future dedupe of the three `AA-55-A0` tables | critical | mitigate | Three pairwise `(const void*)` inequality assertions over the production objects, plus distinctness from `EEPROM_SDP_DISABLE` and the 3-versus-6 length check; a dedupe fails loudly rather than silently satisfying the equality legs |
| T-119-06-FABSTATE | Repudiation | the lock's reported outcome | critical | mitigate | Case 20 asserts both lock ids present, ordered, with `response_code` untouched, and asserts the unlock's ids absent; the honesty clause lives in 0x61's catalog text and both rejected alternatives are recorded in the case comment |
| T-119-06-HOLLOWCHECK | Repudiation | the t_BLC budget check | high | mitigate | Paired cases 21 and 22: fires when over budget, does not fire at normal elapsed, with the control also asserting the INFO ids are present so it cannot pass vacuously |
| T-119-06-SILENTREPORT | Repudiation | report-line spelling | medium | mitigate | Case 20 runs with `FLAG_VERBOSE` unset and still observes both ids, pinning the unconditional `LOG_ID` choice against a future switch to the gated family |
| T-119-06-CONFLATE | Repudiation | lock and unlock report surfaces | medium | mitigate | Case 20 asserts the unlock's ids are absent on the lock path; case 23 asserts the lock's ids are absent on the unlock path |
| T-119-06-HEXDRIFT | Tampering | message ids referenced as literals | medium | mitigate | All ids referenced by generated name; a catalog renumber then breaks the build rather than silently desyncing a test |
| T-119-06-FROZEN | Tampering | `include/flash_utils.h` | high | mitigate | Read-only; `git diff --quiet` on that path is an acceptance criterion and criterion 5's rationale lives in `eeprom_28c.cpp` instead |
| T-119-06-GATEDRIFT | Tampering (of controls) | `test_sdp_table_parity.py`'s extractor | medium | mitigate | The extractor is not modified; a non-extracting table is reported as a firmware-declaration finding rather than accommodated by widening the regex |
| T-119-06-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. The three-way identity and three-way distinctness cases pass in both native envs; the pre-existing two-way case is unmodified.
2. Cases 20 through 23 pass; the suite reports 23/23 and both full runs 127/127 across 17 suites.
3. `git diff --quiet -- include/flash_utils.h` exits 0; no production file appears in the committed firmware diff.
4. The host parity leg passes and `_extract_byte_flip_pairs` is unmodified.
5. All six host modules pass; four checker scripts exit 0; `sdp_bus_config.h` regenerates blob-identically; ruff clean against the CI targets.
6. `pio run` 3/3 SUCCESS.
7. `REQUIREMENTS.md` shows LOCK-05 Complete only; its wording is unchanged.
8. Commits: one or more inside `firestarter/`, one inside `firestarter_app/`, then one meta commit staging both gitlinks plus the SUMMARY. Suggested subjects: `test(119-06): machine-check the three-way AA-55-A0 identity and distinctness (LOCK-05)` and `test(119-06): add the EEPROM_SDP_ENABLE source-text parity leg (LOCK-05)`.
</verification>

<success_criteria>
- The duplication LOCK-05 protects is preserved and its correctness is machine-checked twice, by two oracles with different failure modes.
- A future alias-refactor dedupe fails loudly rather than silently satisfying equality.
- The lock reports OK with honest text, proven by ordered frame ids and an untouched `response_code`, with `FLAG_VERBOSE` unset.
- The lock's t_BLC budget check is proven both to fire and not to fire.
- The standalone unlock's stream is proven byte-identical to the auto-unlock's, making D-13's claim assertable.
- Every gate on the CORRECTION-4 checklist is green; LOCK-05 is Complete with criterion 5's deviation recorded.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-06-SUMMARY.md` when done.
Record: the new case names and counts in both envs; the exact assertions in cases 21 and 22 with the scripted
tick values used; whether `check_no_log_in_sdp_window.py`'s `PASS:` ranges shifted again and the new values;
whether `EEPROM_SDP_ENABLE` extracted cleanly under the unmodified host extractor; the criterion-5 deviation
recorded as deliberate and of the same class as D-05 and D-15; the two known-RED host modules named as
pre-existing; and confirmation that LOCK-05 is the only requirement row changed and that LOCK-02 remains open
pending Plan 119-07.
</output>
