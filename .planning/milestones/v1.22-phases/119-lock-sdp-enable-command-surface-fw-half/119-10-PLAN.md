---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 10
type: execute
wave: 10
depends_on: [119-09]
files_modified:
  - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LOCK-06]

must_haves:
  truths:
    - "D-15: LOCK-06's 3348 B is a SUPERSEDED pre-117 figure. Judge this phase's delta against the LIVE Leonardo number, 25680/28672 leaving 2992 B at phase base 1880054, and SHOW THE ARITHMETIC. Make NO threshold claim beyond 'fits' - that is how Phases 117 and 118 recorded their deltas, as measured facts with provenance. Do NOT edit REQUIREMENTS.md's LOCK-06 wording; record the correction in the phase artifacts."
    - "The -D DEV_TOOLS configuration is the TIGHTER one and therefore the binding constraint for LOCK-06: a release-config Leonardo build measured 24388/28672, so the flag costs 1292 B. Report the delta against the DEV_TOOLS build."
    - "Uno and uno328pb capacities DIFFER (32256 versus 32384) because the two board JSONs reserve different bootloader sizes, so a cross-board comparison must compare DELTAS, never percentages or free-space figures."
    - "D-08 item 2: the cross-family byte-identity claim is mandatory and must be stated precisely - the 0x05, 0x06, 0x07, 0x08, 0x0B, 0x10 and SRAM bus streams are byte-identical, and every command-by-protocol cell that previously returned a silent OK is enumerated with its new outcome. Plan 119-07 produced that matrix; this document restates it rather than re-deriving it."
    - "D-10 forces _shared/sdp_expected.h's whole-file blob SHA to change, so Phases 117 and 118's blob-SHA identity shorthand DOES NOT APPLY to that file this phase. Its proof is PER-ARRAY byte-identity of the pre-existing arrays, performed in Plan 119-05. The other two _shared/ files (host_stubs_common.inc, sdp_bus_config.h) keep the blob-SHA proof. State all of that explicitly - a later phase must not reach for the retired shorthand."
    - "Pitfall 9, the vacuous path check: the ROADMAP's flash_utils.{h,cpp} shorthand is NOT a real path (the real one is include/flash_utils.h), so a `git diff -- src/flash_utils.h` check passes VACUOUSLY. Enumerate this phase's diff explicitly with `git diff --name-only <base>..HEAD` and assert against the resulting real-path list, exactly as 118-NONREGRESSION.md:147-149 does."
    - "CORRECTION-4 item 4 is mandatory from Phase 118 onward: every phase must include an explicit task checking firmware renames and deletions against firestarter_app's source-scanning gates. This phase ADDED one (check_is_memory_cmd_no_ifdef.py), so the gate table grows from eight rows to nine and must be handed forward to Phases 120, 121 and 122."
    - "catalog-sync-check.yml is expected-RED-until-merge by design because it checks out both sub-repos at ref main. Same for firestarter/.github/workflows/build.yml's new native_nodevtools step, which triggers on main only and so will not fire on the milestone branch. Both are known-and-explained conditions, never silent, and neither is this phase's damage."
    - "Two host modules are known-RED and pre-existing: tests/test_audit_coverage_matrix.py (stale golden) and tests/test_no_programmer_found_* (goes red with a live board attached, and three boards are attached). Name them with their reasons; do not chase them."
    - "Validation ceiling, unchanged: 0x0D stays UNVERIFIED, ZERO chips change support_status, the 84-chip count is unchanged, and no claim may be made that SDP lock or unlock works on real AT28C silicon."
  prohibitions:
    - "MUST NOT edit REQUIREMENTS.md's LOCK-06 wording, or LOCK-04's. Only LOCK-06's checkbox and its parenthetical may change in this plan."
    - "MUST NOT use the whole-file blob SHA of _shared/sdp_expected.h as an identity proof."
    - "MUST NOT check a frozen file with a path that does not exist. Enumerate the diff and assert against the real-path list."
    - "MUST NOT make any threshold claim about the flash delta beyond 'fits'. No percentage-of-headroom framing, no 'plenty of room', no cumulative-milestone-budget framing (D-15 rejected the last one because LOCK-06 then cannot be judged from this phase's artifacts alone)."
    - "MUST NOT write any sentence readable as bench-validating 0x0D on AT28C silicon."
    - "MUST NOT touch either submodule. This plan is meta-repo only; both sub-repo working trees must stay clean."
    - "MUST NOT mark DEVTEST-01 Complete. Its host half is Phase 121."
  artifacts:
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md
  key_links:
    - "118-NONREGRESSION.md is the section-for-section template: 1 the claim stated precisely, 2 the enumerated serial-channel exception as one table, 3 why the bus stream is genuinely unchanged, 4 flash and RAM, 5 the CORRECTION-4 item-4 gate table, 6 known-and-explained conditions never silent, 7 validation ceiling, 8 deliberately not taken, then a Sweep summary."
    - "Section 4 is where D-15's arithmetic goes; section 5 is the gate table this phase extends with the new D-04 gate; section 8 is where the phase's declined options are listed so a later reader finds them rather than inheriting silence."
    - "Plan 119-08's Task 3 already produced the flash decomposition, the enumerated diff and the golden identity story in this document's section shapes. Lift those records rather than reconstructing them, and verify each one still holds at the phase's final commit."
---

<objective>
Sweep all three repos at the phase's final code state, decompose the measured flash delta against the live
2992 B Leonardo headroom with the arithmetic shown, extend the cross-repo gate table to nine rows, and write
`119-NONREGRESSION.md` — then close LOCK-06.

Purpose: LOCK-06's clause is a measured `pio run` flash delta reported against live headroom. Everything else in
this document exists because Phase 118 proved that re-running every gate at every wave and writing the result
down is what produced zero host-CI surprises, and because this phase's generic op-layer guard changed observable
behaviour across every protocol family — a claim that has to be stated precisely or it is not a claim at all.

Output: `119-NONREGRESSION.md` in the eight-section shape `118-NONREGRESSION.md` established, and LOCK-06 closed
with its superseded-headroom correction recorded.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-VALIDATION.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-SUMMARY.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md
</context>

## Repo and commit mechanics

This plan writes **only** meta-repo files under `.planning/`. It **builds and tests** both sub-repos but **edits
neither** — `git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short`
must both be empty at the end. Commit in `/workspaces`, staging the two `.planning/` paths explicitly. Do not
stage either submodule gitlink.

Use scoped `Edit` for `REQUIREMENTS.md`. Never rewrite it wholesale.

## Artifacts this phase produces

**Created by THIS plan:** `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md`,
and the nine-row CORRECTION-4 item-4 gate table inside it that Phases 120, 121 and 122 inherit.

**Created earlier in Phase 119:** catalog ids `MSG_INFO_SDP_LOCK` (0x60), `MSG_INFO_SDP_LOCK_DONE_US` (0x61),
`MSG_INFO_PAGE_LOAD_WORST_US` (0x62) — 119-01; `CMD_SDP_UNLOCK` (9), `CMD_SDP_LOCK` (10), `is_memory_cmd()`,
`[env:native_nodevtools]`, its CI step, `test_cmd_admission/` — 119-02;
`firestarter_app/tools/check_is_memory_cmd_no_ifdef.py`, its pytest and
`tests/fixtures/planted_ifdef_in_predicate.h` — 119-03; `EEPROM_SDP_ENABLE[3]`,
`eeprom28c_emit_sdp_sequence_timed()`, `eeprom28c_sdp_lock_execute()`, `eeprom28c_sdp_unlock_execute()`,
`eprom_sdp_lock()`, `eprom_sdp_unlock()`, the `configure_eeprom28c` and `loop()` arms, the appended
`_EMIT_ANCHOR_PATTERNS` entry — 119-04; the four `SDP_FIXED_LOCK_*` goldens and the scripted `micros()` queue —
119-05; the three-way identity and distinctness guard and the host `test_sdp_table_parity.py` enable-table leg —
119-06; the generic NULL-`main` refusal, the Open-Question-1 resolution and the command-by-protocol matrix —
119-07; the page-load worst-interval tracker — 119-08; the Phase 121 amendment and PROJECT.md's sixth correction
block — 119-09.

**Created later:** `119-MEASUREMENT.md` — 119-11.

## Requirement ownership

This plan **closes LOCK-06** — the measured `pio run` flash delta on all three envs, reported against the live
2992 B headroom with the arithmetic shown. Its parenthetical must record that the `3348 B` figure in the
requirement text is superseded and that the judgement was made against 2992 B; **LOCK-06's wording itself must
not be edited**.

After this plan, LOCK-01 through LOCK-06 all read Complete. **DEVTEST-01 stays Pending** — its host half is Phase
121. Change no other requirement row.

<tasks>

<task type="auto">
  <name>Task 1: Sweep all three repos, enumerate the diff by real path, and build the nine-row gate table</name>
  <files>.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md read WHOLE (all eight sections plus the Sweep summary; note especially section 2's single-table enumeration of the serial-channel exception, section 5's gate table with its eight rows and per-gate disposition, section 6's known-and-explained-conditions discipline, and the explicit diff enumeration at :147-149 that avoids the vacuous path check)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md (Plan 119-08 Task 3's already-produced flash decomposition, enumerated diff and golden identity story — lift and re-verify rather than reconstruct)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md (the complete command-by-protocol matrix table with every changed cell flagged — section 2's content, and the list of pre-existing test cases whose expectations moved)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md (the per-array byte-identity method and result for sdp_expected.h, and the two blob-SHA-identical _shared/ files)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-R (the nine-gate host checklist with each gate's scan target and this phase's disposition)
  </read_first>
  <action>
Gather the sweep data. No document is written yet — Task 3 writes it. Capture verbatim outputs.

**Firmware, both envs and all three boards.** From `/workspaces/firestarter`: `pio test -e native`,
`pio test -e native_nodevtools`, `pio run`. Record full case and suite counts for both native envs and confirm
they are **identical** — a divergence at this point would mean something in this phase's code is
build-configuration dependent, which is exactly what LOCK-03 exists to prevent, and it must be reported as a
defect rather than accepted. Record all six flash/RAM figures.

**Host, the full gate set.** From `/workspaces/firestarter_app`:
`python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q`;
then `python3 tools/check_no_log_in_sdp_window.py`, `python3 tools/check_is_memory_cmd_no_ifdef.py`,
`python3 tools/check_dispatch.py`, `python3 tools/check_devtest_orchestrator.py`; then
`tools/gen_sdp_bus_config.py` regenerate-and-diff; then `ruff check` and `ruff format --check` against the
py3.9/3.11 CI targets with the version recorded. Also run `tools/diff_db.py` (or the DB identity check it
provides) to confirm `chip_database.json` is untouched — zero DB change is in scope for this phase and CLOSE-01
depends on it.

**Catalog, the three-way identity.** `cmp` the meta `tools/catalog/messages.toml` against both sub-repo copies,
and re-run `codegen.py --check` in both sub-repos with `git diff --exit-code` on each generated artifact.

**Enumerate the diff by REAL path, in all three repos.** Run
`git -C /workspaces/firestarter diff --name-only <phase-base>..HEAD`,
`git -C /workspaces/firestarter_app diff --name-only <phase-base>..HEAD` and the equivalent in `/workspaces` for
`.planning/` and `tools/catalog/`. Capture the three lists verbatim. Then assert against those lists — never
against a path shorthand — that `include/flash_utils.h`, `src/proms/flash_5v_page.cpp` and
`src/proms/flash_nor_unlock.cpp` do **not** appear in the firmware list. The ROADMAP's `flash_utils.{h,cpp}`
shorthand is not a real path, so a `git diff -- src/flash_utils.h` check passes vacuously; state in the document
that the check was done by enumeration, and note the same trap applies to any new path-based gate.

Also confirm from the host list that the **only** `firestarter_app` changes this phase are generated catalog code
plus source-scanning-gate additions and repairs. Anything else in that list is a scope violation — specifically
confirm `firestarter/constants.py`, `firestarter/eprom_operations.py`, any CLI module and `data/chip_database.json`
are all absent.

**The golden identity story.** Confirm `_shared/host_stubs_common.inc` and `_shared/sdp_bus_config.h` are
blob-SHA identical to the phase base. Confirm `_shared/sdp_expected.h`'s blob SHA **changed** and re-verify Plan
119-05's per-array byte-identity result for every pre-existing `SDP_SHIPPED_*` and `SDP_FIXED_*` array against
`git show <phase-base>:test/native/avr/_shared/sdp_expected.h`. Record both the method and the result; the
whole-file shorthand is retired for this file and that retirement must be stated so a later phase does not reach
for it.

**Build the gate table, now nine rows.** For each of `check_no_log_in_sdp_window.py` (plus its pytest and
fixture), `test_sdp_table_parity.py`, `gen_sdp_bus_config.py` with `test_sdp_bus_config_drift.py`,
`test_revision_constants_parity.py`, `check_dispatch.py`, `test_dispatch_mirror.py`,
`check_devtest_orchestrator.py`, `build_db.py` / `diff_db.py` / `audit_coverage_matrix.py`, and the **new**
`check_is_memory_cmd_no_ifdef.py` with its pytest and fixture: record what firmware artefact it scans, what this
phase did to it, and its final result. `check_no_log_in_sdp_window.py`'s row must record the appended emit anchor,
that the anchor tuples are append-only by contract, that the helper's own body must never become a third scanned
window because it carries `LOG_*` calls by design, and the two remaining tripwires (the literal `void` return-type
requirement in `_func_def_pattern`, and step 2's dependence on `eeprom28c_wait_for_sdp_completion` continuing to
exist). Mark the table explicitly as the checklist Phases 120, 121 and 122 inherit.

**Known-and-explained conditions.** Capture, each with its reason: `catalog-sync-check.yml` red-until-merge
because it pins both sub-repos at `ref: main`; `firestarter/.github/workflows/build.yml`'s new
`native_nodevtools` step not firing on the milestone branch because the workflow triggers on `main` only;
`tests/test_audit_coverage_matrix.py` pre-existing RED on a stale golden; and `tests/test_no_programmer_found_*`
RED because three boards are attached. None of these are this phase's damage and none may be silent.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -4 && pio test -e native_nodevtools 2>&1 | tail -4 && pio run 2>&1 | grep -cE 'SUCCESS' && cd /workspaces/firestarter_app && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` and `pio test -e native_nodevtools` report identical case and suite counts, both green, both captured verbatim; any divergence is reported as a defect rather than accepted
    - `pio run` reports SUCCESS for `uno`, `uno328pb` and `leonardo` with all six flash/RAM figures captured
    - All six host pytest modules pass; all four checker scripts exit 0; `_shared/sdp_bus_config.h` regenerates blob-identically; ruff passes against the py3.9/3.11 CI targets with the version recorded; the DB identity check confirms `chip_database.json` untouched
    - The three-way `messages.toml` `cmp` succeeds and `codegen.py --check` plus `git diff --exit-code` pass in both sub-repos
    - Three verbatim `git diff --name-only <phase-base>..HEAD` listings are captured, one per repo
    - `include/flash_utils.h`, `src/proms/flash_5v_page.cpp` and `src/proms/flash_nor_unlock.cpp` are absent from the firmware listing, asserted against the real-path list, with the vacuous-shorthand trap named
    - The host listing contains only generated catalog code plus gate additions and repairs; `constants.py`, `eprom_operations.py`, every CLI module and `data/chip_database.json` are confirmed absent
    - `host_stubs_common.inc` and `sdp_bus_config.h` are blob-SHA identical to the phase base; `sdp_expected.h`'s SHA changed and Plan 119-05's per-array byte-identity result is re-verified with its method recorded
    - A nine-row gate table is assembled with scan target, this phase's action and final result per gate, including the new `check_is_memory_cmd_no_ifdef.py` row and the three tripwires on the no-log gate's row
    - All four known-and-explained conditions are captured with their reasons
    - Both sub-repo working trees are clean (this task builds and tests but edits nothing)
  </acceptance_criteria>
  <done>Every gate in all three repos is at a recorded state, the phase's diff is enumerated by real path in each repo with the frozen files proven absent, the golden identity story is re-verified with the retired shorthand flagged, and the nine-row checklist Phases 120 to 122 inherit is assembled.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose the flash delta against the live 2992 B headroom, with the arithmetic shown</name>
  <files>.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md section 4 (the exact flash/RAM provenance shape: both datapoints with their bases, `f8d10a5` to `1880054`, Leonardo 25680/28672 and Uno 23542/32256 — the form to mirror, extended to a third board)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md (the per-plan flash decomposition Plan 119-08 already produced — re-verify each attribution at this phase's final commit rather than re-deriving)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-01-SUMMARY.md, 119-02-SUMMARY.md, 119-04-SUMMARY.md, 119-07-SUMMARY.md (each plan's own recorded flash figures, which are the decomposition's inputs)
    - .planning/PROJECT.md's FOURTH and FIFTH correction blocks and the new SIXTH block (the `+204 B` and `+152 B` datapoints, and the 2992 B / 1292 B figures Plan 119-09 recorded)
    - .planning/REQUIREMENTS.md, the LOCK-06 line (read it so the parenthetical you add is accurate without editing the requirement text)
  </read_first>
  <action>
Produce the flash and RAM section's content. Three parts.

**Part 1 — the measured figures with provenance.** For each of `leonardo`, `uno` and `uno328pb`: the phase-base
figure, the final figure, and the delta. The phase base at firmware `1880054` is Leonardo 25680/28672, Uno
23542/32256, uno328pb 23592/32384. Name the base commit and the final commit. Report RAM alongside flash for each
board.

Note explicitly that Uno and uno328pb **capacities differ** (32256 versus 32384) because the two board JSONs
reserve different bootloader sizes, so any cross-board comparison must compare **deltas**, never percentages and
never free-space figures. Phase 118 reported two boards; this phase reports three, per D-18 item 5.

**Part 2 — D-15's arithmetic, shown.** State that LOCK-06's requirement text cites **3348 B** of headroom and that
this figure is a **pre-117 measurement, superseded**: `+204 B` from Phase 117 and `+152 B` from Phase 118 are
already spent, which is exactly the difference. The live Leonardo headroom at phase base is 28672 minus 25680 =
**2992 B**, and that is the number this phase's delta is judged against. Show the subtraction, the delta, and the
remaining headroom after this phase, as arithmetic a reader can check.

Then state that the `-D DEV_TOOLS` configuration is the **tighter** one and therefore the binding constraint: a
release-config Leonardo build measured 24388/28672, so the flag costs **1292 B**, and the delta is reported
against the `DEV_TOOLS` build. Reporting against the looser configuration would flatter the result.

**Make no threshold claim beyond "fits".** No percentage-of-headroom framing, no "plenty of room", and explicitly
not a cumulative-milestone-budget framing — D-15 rejected that last one because LOCK-06 then cannot be judged from
this phase's artifacts alone. Also record that reporting both framings was rejected, because it invites a later
reader to quote whichever is more flattering.

**Part 3 — the decomposition, attributed per plan.** Attribute the total Leonardo delta to its parts using each
plan's own recorded figures: the three catalog ids (119-01, unreferenced at that point so PROGMEM format strings
plus three table rows), `is_memory_cmd()` plus the two command defines plus the guard restructure (119-02, and note
the predicate is `static inline` so its cost lands at its one call site), the enable table plus the shared helper
plus the two ops plus the two entry points plus the two switch arms (119-04, and note the helper **de-duplicates**
118's inline bracket so its own contribution may be neutral or negative), the generic NULL-`main` refusal (119-07),
and the page-load tracker plus its report line (119-08). Where an attribution does not reconcile with the total,
say so and say by how much rather than forcing the numbers — compiler layout effects are real and an honest residual
is better than a fabricated tidy sum.

State that the second native env, the four goldens, the `test_cmd_admission` suite and every host-side artefact cost
**zero** production flash, and that under Plan 119-07's option (a) the widened `build_src_filter` is native-only and
also costs zero — so a reader does not look for their contribution in the delta.

Then mark **LOCK-06 Complete** in `.planning/REQUIREMENTS.md` with a parenthetical giving the measured Leonardo
delta, the 2992 B live headroom it was judged against, and the note that the requirement's `3348 B` is superseded.
**Do not edit LOCK-06's requirement wording.** Confirm LOCK-01 through LOCK-05 all already read Complete and that
DEVTEST-01 still reads Pending; if any disagrees, report it as a defect rather than fixing it here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run 2>&1 | grep -E 'Flash|RAM' && cd /workspaces && grep -n 'LOCK-0' .planning/REQUIREMENTS.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Phase-base, final and delta flash and RAM figures are recorded for all three boards, with the base and final commits named
    - The Uno-versus-uno328pb capacity difference is noted with the deltas-not-percentages instruction
    - The arithmetic is shown: 28672 minus 25680 equals 2992 B live headroom; the `3348 B` requirement figure is stated as superseded with `+204 B` and `+152 B` named as the difference; the delta and the remaining headroom are both shown
    - The `-D DEV_TOOLS` configuration is named as the tighter and binding one, citing the 1292 B flag cost and the 24388/28672 release-config measurement, and the delta is reported against the `DEV_TOOLS` build
    - No threshold claim beyond "fits" appears; no percentage-of-headroom framing, no cumulative-milestone-budget framing, and the both-framings option is recorded as rejected
    - The delta is decomposed per plan (119-01, 119-02, 119-04, 119-07, 119-08) using each plan's recorded figures, with any residual stated honestly rather than forced
    - The zero-flash items are named explicitly: the second native env, the four goldens, `test_cmd_admission`, every host artefact, and (under option (a)) the widened `build_src_filter`
    - `.planning/REQUIREMENTS.md` shows LOCK-06 Complete with a parenthetical naming the measured delta, the 2992 B headroom and the superseded `3348 B`; LOCK-06's wording is unchanged
    - LOCK-01 through LOCK-05 all read Complete; DEVTEST-01 still reads Pending
    - The `REQUIREMENTS.md` edit was made with a scoped `Edit`, and its diff touches only LOCK-06's checkbox and parenthetical
  </acceptance_criteria>
  <done>LOCK-06 is settled by measurement on all three boards, judged against the live 2992 B headroom with the arithmetic shown and the superseded figure named, decomposed per plan with an honest residual, and closed with no requirement wording edited.</done>
</task>

<task type="auto">
  <name>Task 3: Write 119-NONREGRESSION.md in the eight-section shape, inside the validation ceiling</name>
  <files>.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md read WHOLE again, this time for section headings, tone and structure — this document mirrors it section for section
    - .planning/REQUIREMENTS.md, the "Validation Ceiling" section verbatim (the provable list, the NOT-provable list, the permitted claim and the forbidden claim — all four are quoted in section 7)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md (the command-by-protocol matrix and the moved test expectations — section 2's and section 3's content)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md, the `<deferred>` block (every declined option, for section 8 — so the next owner finds them rather than inheriting silence)
    - .planning/PROJECT.md's new SIXTH correction block from Plan 119-09 (this document references it rather than restating its arguments)
  </read_first>
  <action>
Write `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md`, mirroring
`118-NONREGRESSION.md` section for section.

**Section 1 — the claim, stated precisely.** This phase added the milestone's only new state-mutating operation
and one generic refusal that changes observable behaviour across every protocol family. So the claim is not
"nothing changed". State it exactly: the bus streams for `0x05`, `0x06`, `0x07`, `0x08`, `0x0B`, `0x10` and SRAM
are byte-identical; the serial channel gained a bounded, enumerated set of new frames; and one class of
previously-silent outcomes became explicit refusals, enumerated cell by cell. Anything not in those three
statements is unchanged.

**Section 2 — the enumerated exceptions, in tables.** Two tables. First, every new serial frame this phase can
emit, with its id, name, when it fires, and which plan added it: `MSG_INFO_SDP_LOCK` (0x60),
`MSG_INFO_SDP_LOCK_DONE_US` (0x61), `MSG_INFO_PAGE_LOAD_WORST_US` (0x62), and `MSG_ERR_NOT_SUPPORTED` (0xA5) on
paths that previously emitted nothing. Second, Plan 119-07's complete command-by-protocol matrix, restated with
every cell that changed from silent-OK to `0xA5` flagged — that is D-08 item 2's deliverable. Include the honest
qualification for `0x0D` plus `CMD_CHECK_CHIP_ID`: it is already refused upstream in practice because
`eprom_check_chip_id` rejects a zero chip id and TRACE-05 pinned `chip_id_check: false` across all 84
`algorithm == 13` entries, so note it without over-claiming it. Also list every pre-existing test case whose
expectation moved, by name, with the one-line justification that its old expectation encoded the phantom-success
defect.

**Section 3 — why the bus stream is genuinely unchanged.** The structural argument plus its evidence: no cell in
any column the other six families exercise changed from a generic or handler main to anything else; the change is
purely in the previously-NULL cells, none of which emitted bus traffic; the five `test_val_*` stream suites are
green with **no golden regenerated**; and the per-array byte-identity result for the pre-existing
`sdp_expected.h` arrays holds. State plainly that `sdp_expected.h`'s whole-file blob SHA **necessarily changed**
because D-10 added four arrays, that Phases 117 and 118's blob-SHA shorthand therefore does not apply to that
file, and that `host_stubs_common.inc` and `sdp_bus_config.h` do keep the blob-SHA proof.

**Section 4 — flash and RAM.** Task 2's content in full: the three boards' figures with provenance, D-15's
arithmetic against 2992 B with the superseded `3348 B` named, the `-D DEV_TOOLS` binding-configuration note, the
per-plan decomposition with its honest residual, and the zero-flash items. No threshold claim beyond "fits".

**Section 5 — the CORRECTION-4 item-4 gate table.** Task 1's nine-row table, with the explicit statement that it
is the checklist Phases 120, 121 and 122 inherit and that `check_is_memory_cmd_no_ifdef.py` is this phase's
addition to it. Keep the no-log gate's three tripwires in its row.

**Section 6 — known-and-explained conditions, never silent.** All four from Task 1, each with its reason and an
explicit statement that it is not this phase's damage: `catalog-sync-check.yml` red-until-merge; the new
`native_nodevtools` CI step not firing on the milestone branch; `test_audit_coverage_matrix.py` pre-existing RED;
`test_no_programmer_found_*` RED with three boards attached.

**Section 7 — validation ceiling.** Quote `REQUIREMENTS.md`'s permitted claim and forbidden claim **verbatim**,
plus the NOT-provable list. Then state which side of the line this phase sits on: the lock and unlock sequences
are emitted exactly as specified, verified byte-exact by golden register trace across all four `0x0D` pinouts,
with a documented and measured host-side timing assumption — and **nothing** in this phase supports the forbidden
claim. `0x0D` stays `UNVERIFIED` in the PROTOCOL-LEDGER, **zero** chips change `support_status`, and the
**84**-chip count is unchanged, confirmed by the DB identity check. State that no bench byte in this phase could
lock a real part, because `CMD_SDP_LOCK` is unreachable from the shipped CLI until Phase 120's `dev sdp` — and
that this is the strongest safety argument for the firmware-before-host ordering, not merely a scheduling
convenience.

**Section 8 — deliberately not taken.** Every declined option, so the next owner finds it: `default:` arms in all
six `configure_*` handlers (priced at roughly 90 to 130 B, rejected); a pre-dispatch protocol check in
`configure_memory` (rejected on v1.20's protocol-only-rebuild grounds); any `default:` arm in
`configure_eeprom28c` at all (declined in favour of a naming comment, at zero flash); a runtime t_BLC WARN on the
page-load loop (D-16's declination, preserving 118 D-10); a distinct "compiled out" refusal id for `CMD_DEV_*`
in a release build (pre-empts 999.15's channel-split design); an explicit refusal arm for cmd 0 / `CMD_IDLE`
(declined on flash grounds, the delta recorded instead); four distinct catalog ids so the log distinguishes
auto-unlock from standalone unlock (D-13 reused the pair); a `cmd`-carrying variant of `MSG_ERR_NOT_SUPPORTED`
(would need a new id, which D-06 avoids); deleting the host's `_SRAM_PROTO_IDS` workaround (identified here,
correct Phase 120 disposition is KEEP); widening the trace recorder to a data-bus-direction strobe kind (carried
forward, still not taken); and the lock's own hardware duration measurement (unreachable until Phase 120's CLI,
D-17). Also record the four mechanism-versus-intent corrections by reference to PROJECT.md's sixth block rather
than restating their arguments.

**Sweep summary.** A short table: every gate, its command, and its result, so a reader can see the whole state at
a glance.

Then review the finished document **line by line** for validation-ceiling compliance: no sentence may be readable
as bench-validating `0x0D` on AT28C silicon, and none may claim the die accepted a sequence or that `t_BLC` is met
as accepted by the die. Record in the SUMMARY that the review was done and what it found.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md && grep -c 'UNVERIFIED\|support_status\|2992\|3348\|check_is_memory_cmd_no_ifdef\|MSG_ERR_NOT_SUPPORTED' /workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - `119-NONREGRESSION.md` exists with all eight sections plus a Sweep summary, mirroring `118-NONREGRESSION.md`'s headings
    - Section 1 states the claim as three precise statements rather than as "nothing changed"
    - Section 2 contains both tables: the four new/newly-reachable frame ids with their plans, and the complete command-by-protocol matrix with every changed cell flagged, plus the honest `CMD_CHECK_CHIP_ID` qualification and the list of moved test expectations by name
    - Section 3 states that the five `test_val_*` stream suites are green with no golden regenerated, that `sdp_expected.h`'s blob SHA necessarily changed with per-array byte-identity as the replacement proof, and that the other two `_shared/` files keep the blob-SHA proof
    - Section 4 contains Task 2's figures, D-15's shown arithmetic against 2992 B, the superseded `3348 B`, the `-D DEV_TOOLS` binding note, the per-plan decomposition with its residual, and the zero-flash items — with no threshold claim beyond "fits"
    - Section 5 contains the nine-row gate table, names it as the Phases 120-122 checklist, names the new gate as this phase's addition, and keeps the three no-log-gate tripwires
    - Section 6 contains all four known-and-explained conditions with reasons and explicit not-this-phase's-damage statements
    - Section 7 quotes the permitted and forbidden claims verbatim, contains `UNVERIFIED`, `support_status` and `84`, and states that `CMD_SDP_LOCK` is unreachable from the shipped CLI so no bench byte could lock a part — framed as the safety argument for firmware-before-host
    - Section 8 lists every declined option named above, and references PROJECT.md's sixth correction block for the four mechanism corrections rather than restating them
    - No sentence in the document is readable as bench-validating `0x0D` on AT28C silicon or as claiming the die accepted a sequence; the line-by-line review outcome is recorded in the SUMMARY
    - `git -C /workspaces show --stat HEAD` lists only `.planning/` paths — no gitlink, no sub-repo file
    - `git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short` are both empty
  </acceptance_criteria>
  <done>The phase's non-regression claim is written down precisely rather than asserted broadly: the byte-identity that holds, the exceptions enumerated in tables, the flash arithmetic shown against the live headroom, the nine-row gate checklist handed forward, the four known-red conditions explained, the ceiling quoted, and every declined option recorded for the next owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured flash figure to a headroom judgement | The requirement's own headroom figure is stale by two phases |
| a sweep result to a non-regression claim | A generic guard changed behaviour across every family, so "nothing changed" would be false |
| a path shorthand to a frozen-file proof | `src/flash_utils.h` does not exist, so a check on it passes vacuously |
| a phase artifact to a claim about silicon | The single most likely ceiling crossing in the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-10-CEILING | Repudiation | `119-NONREGRESSION.md` wording | critical | mitigate | Section 7 quotes the permitted and forbidden claims verbatim; a line-by-line review for silicon claims is an acceptance criterion; the unreachability of `CMD_SDP_LOCK` from the shipped CLI is stated |
| T-119-10-STALEBUDGET | Repudiation | LOCK-06's `3348 B` | high | mitigate | Judged against the live 2992 B with the arithmetic shown and the superseded figure named; the cumulative-budget and both-framings options recorded as rejected; no threshold claim beyond "fits" |
| T-119-10-VACUOUSPATH | Repudiation | the frozen-file proof | high | mitigate | The diff is enumerated with `git diff --name-only` in all three repos and asserted against the real-path lists, with the shorthand trap named in the document |
| T-119-10-BROADCLAIM | Repudiation | section 1's claim | high | mitigate | The claim is decomposed into three precise statements and every exception is enumerated in a table, including the full command-by-protocol matrix |
| T-119-10-RETIREDSHA | Repudiation | `sdp_expected.h`'s blob-SHA shorthand | medium | mitigate | Section 3 states the retirement explicitly and names per-array byte-identity as the replacement, so a later phase does not reach for a proof that cannot hold |
| T-119-10-GATEFORWARD | Tampering (of controls) | the CORRECTION-4 checklist | medium | mitigate | The table grows to nine rows, is explicitly named as the Phases 120-122 checklist, and carries the no-log gate's three tripwires so the next phase inherits them |
| T-119-10-SCOPELEAK | Elevation of privilege | `firestarter_app` beyond generated code and gates | medium | mitigate | The host diff listing is asserted to contain nothing else, with `constants.py`, `eprom_operations.py`, the CLI modules and `chip_database.json` confirmed absent by name |
| T-119-10-WHOLEFILE | Denial of service | `REQUIREMENTS.md` | high | mitigate | Scoped `Edit` only; the diff must touch only LOCK-06's checkbox and parenthetical |
| T-119-10-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. Both native envs report identical counts, both green; `pio run` SUCCESS on all three AVR envs.
2. Every host gate passes or exits 0; ruff clean against the CI targets; the DB identity check confirms `chip_database.json` untouched.
3. The three-way catalog `cmp` and both `--check` gates pass.
4. Three real-path diff listings captured; the three frozen firmware paths absent from the firmware list; the host list contains only generated catalog code and gate work.
5. `host_stubs_common.inc` and `sdp_bus_config.h` blob-SHA identical; `sdp_expected.h`'s per-array identity re-verified.
6. `119-NONREGRESSION.md` has all eight sections plus the Sweep summary and passes the line-by-line ceiling review.
7. `REQUIREMENTS.md` shows LOCK-01 through LOCK-06 all Complete and DEVTEST-01 Pending, with LOCK-06's wording unchanged.
8. Both sub-repo working trees clean; one meta commit staging only `.planning/` paths. Suggested subject: `docs(119-10): record the phase non-regression sweep and the measured flash delta (LOCK-06)`.
</verification>

<success_criteria>
- The non-regression claim is three precise statements with every exception enumerated in a table, not a broad assertion.
- LOCK-06 is closed by measurement on all three boards against the live 2992 B headroom, with the arithmetic shown, the superseded figure named, and no threshold claim beyond "fits".
- The frozen files are proven untouched by real-path enumeration, never by a shorthand that passes vacuously.
- The retired `sdp_expected.h` blob-SHA shorthand is flagged with its replacement named.
- The gate checklist grows to nine rows and is explicitly handed to Phases 120, 121 and 122.
- Every declined option is recorded, and the validation ceiling is quoted and respected line by line.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-10-SUMMARY.md` when done.
Record: both native envs' counts; all six flash/RAM figures with the delta and the per-plan decomposition
including any residual; the three real-path diff listings' key absences; the per-array identity re-verification
result; the nine-row gate table's outcomes; the four known-and-explained conditions; the line-by-line
ceiling-review outcome; and confirmation that LOCK-06 is the only requirement row changed, that LOCK-01 through
LOCK-06 now all read Complete, and that DEVTEST-01 remains Pending for Phase 121.
</output>
