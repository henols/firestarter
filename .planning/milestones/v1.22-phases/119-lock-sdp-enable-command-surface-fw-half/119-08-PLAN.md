---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 08
type: execute
wave: 8
depends_on: [119-07]
files_modified:
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
autonomous: true
requirements: [LOCK-06]

must_haves:
  truths:
    - "D-16: the page-load t_BLC measurement is TAKEN - worst-case, reported ONCE. Apply the D-14 bracket idiom to eeprom28c_write_execute's per-byte set_data loop, track the WORST per-byte interval across all pages, and report it in a SINGLE line after the write completes. A naive per-page report would emit about 512 lines on a 32 KB write (32768 / 64), which violates 118's OBS-05 named-exceptions discipline."
    - "D-16 names a CONFLATION in PROJECT.md's FIFTH CORRECTION item 3 and requires it be said out loud: LOCK-06 is a FLASH budget in bytes of program memory; F-118-01 is a TIMING budget in microseconds per byte load. The directive conflates them. Say so explicitly, then answer the timing question anyway, because the page-load loop is where gh#11's symptom actually lives."
    - "gh#11 framing discipline (Phase 117's correction): gh#11 is a CONFLATION bug - a whole-byte equality compare that passed spuriously whenever the old byte already equalled the new one - NOT a sampling-rate bug. Aim every sentence at the conflation. eeprom_28c.cpp's existing citation comment at :425-450 already says this; reuse its wording rather than inventing new framing."
    - "D-16 explicitly declines a runtime WARN on this path, preserving 118's D-10: no AT28C_TBLC_MAX_US comparison and no WARN is added to the page-load loop. The max-tracking compare is inherent to the phrase 'worst per-byte interval' and is NOT a budget check - state that distinction so a verifier does not read the tracker as the declined WARN."
    - "F-118-01 is why this measurement exists: the unlock emitter measured 572 us against a 600 us budget on a real Leonardo - about 95 us per byte against a 100 us per-byte datasheet maximum, only 4.7% headroom. CONTEXT D-09 framed the runtime check as an invariant that should never fire; the measurement said it barely does not. The page-load loop runs under the IDENTICAL constraint and had received a citation comment only. This plan gives it a number."
    - "The report must fire on the ABORT path too, not only on a clean completion. eeprom28c_write_execute's per-byte loop has two early returns (a failed page-write poll and a failed page read-back), and with an empty socket on the bench the FIRST page fails - so a report line placed only at the loop's normal exit would produce nothing at all in Plan 119-11's bench run. The single-exit restructure is what makes the bench measurement reachable."
    - "Nothing follows the per-byte loop in eeprom28c_write_execute today - the loop's closing brace is the function's closing brace - so converting the two early `return;` statements into a flagged `break;` followed by the report and a `return` is behaviour-preserving by inspection, not by hope. Verify that against live source before editing."
    - "Plan 119-05 produced an inventory of every frame-count and frame-set assertion across all 17 native suites, and stated that test_case12's write_init-only scope is now load-bearing. That inventory is this plan's input: every entry that drives eeprom28c_write_execute must be re-checked against the new report line."
    - "Baselines to hold: pio test -e native and -e native_nodevtools green at Plan 119-07's recorded counts, pio run 3/3 SUCCESS, every host gate at Plan 119-07's baseline. check_no_log_in_sdp_window.py does NOT scan eeprom28c_write_execute, so this edit is outside its windows - confirm that rather than assume it."
  prohibitions:
    - "MUST NOT add a runtime budget comparison or a WARN to the page-load loop. D-16 declines it explicitly, preserving 118 D-10."
    - "MUST NOT emit a report line per page or per byte. One line per write, after the loop."
    - "MUST NOT introduce a new index or a new bound into the per-byte loop. eeprom28c_write_execute's index invariant (window_start <= i < data_size, never derived from a wire field) is a V12 control and must be preserved exactly."
    - "MUST NOT add a new catalog id. MSG_INFO_PAGE_LOAD_WORST_US (0x62) already exists from Plan 119-01."
    - "MUST NOT reframe gh#11 as a sampling-rate or polling-frequency problem anywhere in code, comments or the SUMMARY. It is a conflation bug."
    - "MUST NOT change eeprom28c_emit_command_sequence, eeprom28c_wait_for_sdp_completion or eeprom28c_write_init in this plan. Their bodies are scanned windows or were settled in Plan 119-04."
    - "MUST NOT change the two early-exit conditions' semantics. A failed page-write poll and a failed read-back must still abort the write and must still emit their own errors."
    - "MUST NOT use TEST_IGNORE_MESSAGE."
  artifacts:
    - firestarter/src/proms/eeprom_28c.cpp
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  key_links:
    - "MSG_INFO_PAGE_LOAD_WORST_US is 0x62, INFO band, one u32 param rendered decimal, created by Plan 119-01. It must be emitted with the unconditional bare LOG_ID_U32 spelling, not the FLAG_VERBOSE-gated LOG_INFO_ID_U32 - otherwise Plan 119-11's bench run against a default `firestarter write` produces no line and the measurement is unreachable."
    - "Plan 119-11 reaches this line with the SHIPPED CLI via `firestarter write at28c256 -b --force`: -b gets past the blank check an empty socket fails, and 0x0D has no erase arm so -b skips nothing else on this family. That is why the firmware-before-host invariant holds in practice here, not just on paper."
    - "The scripted micros() tick queue from Plan 119-05 is what makes the tracker natively testable. Its documented default-tail value matters: once the script is exhausted every subsequent read returns the tail, so a case asserting a specific worst interval must script enough ticks to cover every read the driven path makes."
---

<objective>
Give the page-load loop a number. Track the worst per-byte interval across all pages of a write and report it
once, on both the completing and the aborting path, then prove the line fires and carries the right value
natively — while adding no budget comparison to the hot path and naming the flash-versus-timing conflation the
directive contains.

Purpose: F-118-01 turned "the t_BLC check should never fire" into a measured 4.7 % margin. The page-load loop
runs under the identical per-byte constraint, is where gh#11's symptom actually lives, and had received a
citation comment only. PROJECT.md's FIFTH CORRECTION item 3 directs the measurement at this phase. This plan
is the firmware half; Plan 119-11 takes it to three real boards.

Output: a worst-interval tracker with one report line reachable on both exits, native cases proving it fires
with the right value on a completing write and on an aborting one, and the conflation stated in source.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md
</context>

## Repo and commit mechanics

Both files land inside `firestarter/`, already on `v1.22-at28c-software-data-protection-lifecycle`. Confirm
with `git branch --show-current` before writing. Commit inside the submodule, then one meta commit staging the
gitlink plus the `.planning/` SUMMARY. `firestarter_app/` must be untouched.

Firmware plans are **strictly sequential** against the shared `firestarter/.pio/build/` tree. This is the last
firmware code plan in the phase; do not start before 119-07's commits exist.

## Artifacts this phase produces

**Created by THIS plan:** the worst-per-byte-interval tracker locals in `eeprom28c_write_execute`, its
single-exit restructure, and the `MSG_INFO_PAGE_LOAD_WORST_US` report call; new native cases proving the line
fires on both exits with the correct value.

**Created earlier:** the three catalog ids including `MSG_INFO_PAGE_LOAD_WORST_US` (119-01); the command
values, predicate, second native env and `test_cmd_admission` (119-02); the D-04 gate with its fixture
(119-03); `EEPROM_SDP_ENABLE[3]`, the shared timed-emit helper, both SDP ops and the wiring (119-04); the four
`SDP_FIXED_LOCK_*` goldens, the scripted `micros()` queue, cases 13 through 19 and the frame-count inventory
(119-05); the three-way identity guard, cases 20 through 23 and the host parity leg (119-06); the generic
NULL-`main` refusal, the Open-Question-1 resolution and the command-by-protocol matrix (119-07).

**Created later:** the Phase 121 scope amendment (119-09); `119-NONREGRESSION.md` (119-10);
`119-MEASUREMENT.md` (119-11).

## Requirement ownership

This plan lands the firmware that Plan 119-11 measures on three boards, and its flash cost is part of what
Plan 119-10 reports. It **does not close LOCK-06** — LOCK-06's clause is a measured `pio run` flash delta
reported against live headroom, and Plan 119-10 owns that. Record this plan's own flash increment with its
attribution so Plan 119-10 can decompose the total.

Mark **no requirement Complete** in `REQUIREMENTS.md` from this plan. LOCK-06 stays Pending; DEVTEST-01 stays
Pending until Phase 121's host half.

<tasks>

<task type="auto">
  <name>Task 1: Track the worst per-byte page-load interval and report it once, on both exits</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp lines 415-475 (eeprom28c_write_execute in full: the window_start local, the existing gh#11 citation comment at :425-450 whose wording must be reused, the per-byte loop at :446-469 with its firestarter_set_data call, the page_end / last_byte flush condition, the two early `return;` statements after eeprom28c_wait_for_page_write and eeprom28c_verify_page_readback, and the fact that the loop's closing brace is immediately followed by the function's closing brace — VERIFY that last point against live source before editing)
    - firestarter/src/proms/eeprom_28c.cpp lines 287-400 as Plan 119-04 left it (the shared eeprom28c_emit_sdp_sequence_timed helper — the bracket IDIOM to mirror, though the tracker is per-byte and cannot reuse the helper itself)
    - firestarter/src/proms/eeprom_28c.cpp lines 30-60 (PAGE_SIZE 64 at :33, AT28C_TWC_MAX_MS at :42, AT28C_TBLC_MAX_US at :58 — read for context; the tracker must NOT compare against the last of these)
    - firestarter/include/messages.h (the generated MSG_INFO_PAGE_LOAD_WORST_US value and its param shape) and firestarter/include/logging_id.h lines 26-40 (LOG_ID_U32's unconditional spelling versus the FLAG_VERBOSE-gated LOG_INFO_ID_U32 family)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md sections 1 and 4 (the 572-against-600 number with its provenance, and the wording that survived validation-ceiling review)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-U (the reporting shape, the two-budget conflation, the gh#11 framing discipline, and the bench reachability argument)
  </read_first>
  <action>
Edit `eeprom28c_write_execute` in `src/proms/eeprom_28c.cpp`. Nothing else in the file changes.

**First verify the structural precondition** the restructure depends on: read live source and confirm that
nothing follows the per-byte loop — that the loop's closing brace is immediately followed by the function's
closing brace. If anything does follow it, stop and report; the single-exit restructure would then change
behaviour and needs a different shape.

**The tracker.** Before the loop, add two function-local `uint32_t`s: one holding the worst interval seen (init
0) and one holding the previous `micros()` reading (init from a `micros()` call immediately before the loop).
Inside the loop, immediately after `handle->firestarter_set_data(handle, address, data);`, read `micros()`
into a local, compute the unsigned difference against the previous reading, update the worst value if the
difference is larger, and store the new reading as the previous. Use unsigned 32-bit subtraction so a clock
wraparound mid-write still yields a correct interval, the same reasoning the shared helper's bracket uses.

The `if (difference > worst)` compare is **inherent to the phrase "worst per-byte interval"** and is not a
budget check. Say that in the comment, explicitly, because D-16 declined a runtime budget WARN on this path and
a reader must not mistake one for the other: **no** `AT28C_TBLC_MAX_US` comparison and **no** WARN appears
anywhere in this loop.

Do not introduce any new index, bound or wire-derived value. `window_start <= i < data_size` by construction is
a V12 input-validation control and must be preserved exactly as it is.

**The single-exit restructure, so the report is reachable on an abort.** Add a `bool` abort flag initialised
false. Convert each of the two early `return;` statements into "set the flag, then `break;`". After the loop,
emit the report line, then `return;`. Because nothing followed the loop, the aborting behaviour is otherwise
identical: the write still stops at the same byte, and
`eeprom28c_wait_for_page_write` / `eeprom28c_verify_page_readback` still emit their own errors and set their own
`response_code` before returning false — this restructure adds no error and suppresses none.

State the reason for the restructure in the comment, because it is the difference between a measurement and no
measurement: with an **empty socket** the very first page's write poll fails, so a report placed only at the
loop's normal exit would emit nothing at all — and an empty socket is exactly the bench condition Plan 119-11
runs under, and exactly the condition in which gh#11's symptom appears. Also note that the reported value on an
aborting write covers only the bytes loaded before the abort, which is a real and useful number and must be
described as such in `119-MEASUREMENT.md` rather than presented as a full-write figure.

**The report line.** One `LOG_ID_U32(MSG_INFO_PAGE_LOAD_WORST_US, worst)` after the loop. Use the **bare
unconditional** `LOG_ID_U32` spelling, matching the two SDP report lines (118 D-01), not the
`FLAG_VERBOSE`-gated `LOG_INFO_ID_U32`. Under the gated family a default `firestarter write` emits nothing and
Plan 119-11's measurement becomes unreachable with the shipped CLI. Write no `response_code` on this path.

**The comment block, and the conflation.** Extend the existing gh#11 citation comment at `:425-450` rather than
writing a new one beside it, reusing its wording. It must now record:
1. **The conflation, named.** PROJECT.md's FIFTH CORRECTION item 3 directs this measurement at LOCK-06, but
   LOCK-06 is a **flash** budget measured in bytes of program memory while F-118-01 is a **timing** budget
   measured in microseconds per byte load. Those are different things. The directive conflates them. The timing
   question is answered anyway because the page-load loop runs under the identical `AT28C_TBLC_MAX_US`
   constraint and is where gh#11's symptom actually lives.
2. **gh#11's real shape**, in the existing comment's own words: a completion-and-data-landed **conflation**
   bug — a whole-byte equality compare that passed spuriously whenever the old byte already equalled the new
   one — **not** a sampling-rate or polling-frequency bug. FIX-06 split it into two functions with one job
   each. Any future phase revisiting gh#11 on real silicon should look at the conflation.
3. **What the number is and is not.** It is the host-side interval between consecutive `set_data` calls as
   measured by the MCU's own `micros()`. It is a measurement of the MCU driving its own latches. It says
   nothing about whether an AT28C die accepted any byte, and nothing about whether `t_BLC` is met *as accepted
   by the die* — REQUIREMENTS.md's validation ceiling lists that last item explicitly as not provable without
   an AT28C part.
4. **Why there is no check here.** D-16 declines a runtime compare in the hot per-byte path, preserving 118's
   D-10. The `if (difference > worst)` max-tracking compare is not that check.
5. **F-118-01's numbers** as the reason this loop is being measured at all: 572 us against a 600 us budget on a
   real Leonardo, about 95 us per byte against a 100 us per-byte datasheet maximum, only 4.7 % headroom, when
   the decision's premise had been that the check would never fire.

Then rebuild: `pio run` (3/3 SUCCESS). Record the flash delta against Plan 119-07's figures and attribute it —
two `micros()` calls plus a compare and two `uint32_t`s in the loop, a `bool`, and one report call. Also
confirm `python3 tools/check_no_log_in_sdp_window.py` still exits 0: `eeprom28c_write_execute` is **not** one of
its three scanned windows, so a `LOG_*` call here is legitimate — but confirm that empirically rather than
assuming it, because that gate has failed closed on this milestone's firmware edits before.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'MSG_INFO_PAGE_LOAD_WORST_US' src/proms/eeprom_28c.cpp && pio run 2>&1 | tail -6 && cd /workspaces/firestarter_app && python3 tools/check_no_log_in_sdp_window.py</automated>
  </verify>
  <acceptance_criteria>
    - The structural precondition was verified against live source and recorded: nothing followed the per-byte loop before the edit
    - `eeprom28c_write_execute` declares a worst-interval `uint32_t` and a previous-reading `uint32_t`, seeds the latter from a `micros()` call immediately before the loop, and updates both immediately after each `firestarter_set_data` call using unsigned subtraction
    - No `AT28C_TBLC_MAX_US` comparison and no `LOG_WARN_*` call appears anywhere in `eeprom28c_write_execute`
    - The two early `return;` statements are replaced by a flagged `break;`, the report line follows the loop, and the abort semantics are otherwise unchanged (both helper functions still emit their own errors and set their own `response_code`)
    - No new index or bound was introduced; the `window_start <= i < data_size` invariant is unchanged
    - Exactly one `LOG_ID_U32(MSG_INFO_PAGE_LOAD_WORST_US, ...)` call exists in the function, using the bare unconditional spelling, and no `response_code` is written on this path
    - The extended gh#11 comment records all five required items: the flash-versus-timing conflation named as a conflation; gh#11 as a conflation bug and not a sampling-rate bug; what the number is and is not, including that `t_BLC` as accepted by the die is not provable; that D-16 declines a runtime check and the max compare is not one; and F-118-01's 572-against-600 numbers
    - No new catalog id was added
    - `eeprom28c_emit_command_sequence`, `eeprom28c_wait_for_sdp_completion` and `eeprom28c_write_init` are unchanged (verified by `git diff`)
    - `pio run` reports SUCCESS on all three AVR envs, with the flash delta against Plan 119-07's figures recorded and attributed
    - `python3 tools/check_no_log_in_sdp_window.py` exits 0, confirmed empirically
  </acceptance_criteria>
  <done>The page-load loop tracks its worst per-byte interval and reports it once, reachable on both the completing and the aborting exit, with no budget comparison in the hot path and the flash-versus-timing conflation named in source.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the report line fires with the right value, on both the completing and the aborting exit</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp: the scripted micros() tick queue and its script-installing helper as Plan 119-05 left it, including the documented default-tail semantics; captured_frames / sdp_captured_frame_ids / sdp_ids_contains at :150-181; make_sdp_handle at :248-258 with its extra_flags default-arg seam; and the main() RUN_TEST list as Plan 119-06 left it
    - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp (the 0x0D Tier-1 suite, including the write-path cases 117-03 added — read to see how a write_execute drive is set up, what mock_get_data behaviour makes a page poll succeed, and which of those cases now also see the new report line)
    - firestarter/src/proms/eeprom_28c.cpp as Task 1 left it (the tracker, the abort flag and the report call — you need to know exactly how many micros() reads the driven path makes in order to script enough ticks)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-05-SUMMARY.md (the frame-count assertion inventory across all 17 suites, and the note that case 12's write_init-only scope is now load-bearing)
  </read_first>
  <action>
Add cases to `test_eeprom28c_sdp.cpp`, numbered from 24, each registered with `RUN_TEST` in `main()`.

**Case A — the report line fires on a completing write, with the correct worst value.** Drive
`eeprom28c_write_execute` on a handle whose `data_size` spans at least two pages so the flush path runs more
than once, with the address-keyed mock read behaviour that makes both `eeprom28c_wait_for_page_write` and
`eeprom28c_verify_page_readback` succeed. Script the `micros()` queue so the per-byte intervals are known and
**not** monotonic — put the largest gap somewhere in the middle, not at the first or last byte, so the case
proves the tracker keeps a running maximum rather than reporting the first or the last interval. Assert
`MSG_INFO_PAGE_LOAD_WORST_US` appears in the captured frame ids, and assert the reported u32 parameter equals
the scripted maximum by decoding it out of `captured_frames` using the documented `rurp_log_id` wire layout
(4-byte magic, 2-byte big-endian length, 1 id byte, params, 1 crc byte, 1 anchor byte) — the same layout
`sdp_captured_frame_ids` already walks. Membership alone is not enough: a tracker that reported zero would pass
a membership-only assertion.

Script enough ticks to cover **every** `micros()` read the driven path makes, and say in the comment why:
once the script is exhausted every subsequent read returns the documented tail value, so an under-scripted case
silently measures the tail instead of the intended intervals.

**Case B — the report line fires on the aborting exit.** Drive the same function with mock read behaviour that
makes the first page's write poll fail, so the loop takes the early exit. Assert three things:
`MSG_INFO_PAGE_LOAD_WORST_US` still appears in the captured ids; the page-write failure's own error id also
appears; and the reported worst value corresponds to the bytes actually loaded before the abort. Comment: this
is the case that makes Plan 119-11's bench run meaningful, because with an empty socket the first page always
fails, and it is also the condition under which gh#11's symptom appears — so a report unreachable on the abort
path would have measured nothing where it matters most.

**Case C — no budget WARN on this path.** Script an interval far above `AT28C_TBLC_MAX_US`, drive
`eeprom28c_write_execute`, and assert `MSG_WARN_SDP_TBLC_EXCEEDED` is **absent** from the captured ids while
`MSG_INFO_PAGE_LOAD_WORST_US` is present. The presence half is load-bearing so the case cannot pass vacuously.
Comment: D-16 deliberately declines a runtime compare in the hot per-byte path, preserving 118's D-10, and this
case pins that declination so a future editor who adds one sees it fail here and has to make the decision
deliberately.

**Case D — `response_code` is untouched by the report line.** Set a known `response_code` before driving a
completing write and assert it is unchanged after, mirroring
`test_case8_completion_poll_preserves_prior_severity`'s invariant onto this new emission. Reference every
message id by generated name, never by hex literal, in all four cases.

Then work through Plan 119-05's frame-count inventory: for **every** entry in it that drives
`eeprom28c_write_execute`, re-run that case and re-check its expectation against the new report line. Cases in
`test_val_eeprom28c` are the likely ones. For each affected case, either widen its expectation to include the
new id — naming it explicitly rather than loosening the assertion to "at least N frames" — or, if the case is
scoped to a phase that does not include the report, record that its scope is now load-bearing, exactly as Plan
119-05 did for case 12. List every case you touched, by name, in the SUMMARY. Do not blanket-loosen any frame
assertion.

Run the suite in both envs, then both full suites, and record the case and suite counts. Then `pio run`
(3/3 SUCCESS).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -20 && pio test -e native_nodevtools 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Four new cases exist from number 24 onward, all registered with `RUN_TEST` in `main()`
    - Case A drives a multi-page completing write, scripts a non-monotonic tick sequence with the largest gap in the middle, and asserts the DECODED u32 parameter equals the scripted maximum — not merely that the id is present
    - Case A's comment states why the script must cover every `micros()` read the path makes, naming the default-tail hazard
    - Case B drives the aborting exit, asserts both the report id and the page-write failure's own error id are present, and asserts the value corresponds to the bytes loaded before the abort; its comment names Plan 119-11's empty-socket bench condition
    - Case C asserts `MSG_WARN_SDP_TBLC_EXCEEDED` absent AND `MSG_INFO_PAGE_LOAD_WORST_US` present at an interval far above `AT28C_TBLC_MAX_US`, with the D-16 declination recorded in its comment
    - Case D asserts `response_code` is unchanged across a completing write
    - All message ids in all four cases are referenced by generated name, never by hex literal
    - Every entry in Plan 119-05's frame-count inventory that drives `eeprom28c_write_execute` was re-run and re-checked, and every affected case is listed by name in the SUMMARY with what changed
    - No frame assertion was loosened to an at-least form; widened expectations name the new id explicitly
    - `pio test -e native` and `pio test -e native_nodevtools` are both green with case and suite counts recorded
    - `pio run` reports SUCCESS on all three AVR envs
    - `TEST_IGNORE_MESSAGE` appears nowhere; no production file is modified in this task's diff
  </acceptance_criteria>
  <done>The worst-interval report is proven to fire with the correct decoded value on a completing write, to fire on the aborting exit that the bench will actually take, to add no budget WARN, and to leave `response_code` alone — with every pre-existing frame-count assertion re-checked by name.</done>
</task>

<task type="auto">
  <name>Task 3: Full firmware and host regression sweep, with the flash increment attributed</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md (Plan 119-07's recorded flash figures, case counts and host gate baseline — this task diffs against those)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md sections 4 and 5 (the flash-provenance shape and the CORRECTION-4 item-4 gate table Plan 119-10 must extend — read so this task's records land in the form that plan consumes)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-R (the complete nine-gate host checklist with this phase's disposition per gate, including the two known-RED modules)
  </read_first>
  <action>
This task adds no code. It produces the records Plan 119-10 consumes, at the point where the phase's firmware is
complete.

Run the full firmware set and record every number:
- `pio test -e native` — full case and suite counts
- `pio test -e native_nodevtools` — the same, and confirm the two match each other exactly. A divergence between
  the two envs at this point would mean something in this phase's code is build-configuration dependent, which is
  precisely what LOCK-03 exists to prevent; report it as a defect rather than accepting it.
- `pio run` — flash and RAM for `uno`, `uno328pb` and `leonardo`

Then produce the **flash decomposition** Plan 119-10 needs. Diff the current three figures against the phase
base (Leonardo 25680/28672, Uno 23542/32256, uno328pb 23592/32384, all at `1880054`) and, using each earlier
plan's recorded figures, attribute the total to its parts: the three catalog ids (119-01), the predicate and the
two command defines (119-02), the enable table plus the shared helper plus the two ops plus the entry points and
switch arms (119-04), the generic NULL-`main` refusal (119-07), and this plan's tracker and report line (119-08).
Show the arithmetic against the **2992 B** live Leonardo headroom, not against LOCK-06's stale 3348 B. Make **no
threshold claim beyond "fits"** — that is D-15's instruction, matching how Phases 117 and 118 recorded their
deltas as measured facts with provenance. Note explicitly that the `-D DEV_TOOLS` configuration is the tighter
one (a release build measured 24388/28672, so the flag costs 1292 B) and is therefore the binding constraint.

Then run the complete host gate set and record each result:
`python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q`,
then `python3 tools/check_no_log_in_sdp_window.py`, `python3 tools/check_is_memory_cmd_no_ifdef.py`,
`python3 tools/check_dispatch.py` and `python3 tools/check_devtest_orchestrator.py` (all exit 0), then
`tools/gen_sdp_bus_config.py` regenerate-and-diff (`_shared/sdp_bus_config.h` blob-identical), then `ruff check`
and `ruff format --check` against the py3.9/3.11 CI targets with the version recorded.

Enumerate the phase's firmware diff **explicitly** rather than by a path shorthand:
`git -C /workspaces/firestarter diff --name-only <phase-base>..HEAD`. Assert against the resulting list that
`include/flash_utils.h`, `src/flash_5v_page.cpp` and `src/proms/flash_nor_unlock.cpp` do not appear — using the
**real** paths from that listing, never the ROADMAP's `flash_utils.{h,cpp}` shorthand, which is not a real path
and makes a `git diff -- src/flash_utils.h` check pass vacuously. State in the SUMMARY that the check was done
by enumeration.

Confirm the golden identity story in the form Plan 119-10 restates: `_shared/host_stubs_common.inc` and
`_shared/sdp_bus_config.h` are blob-SHA identical to the phase base; `_shared/sdp_expected.h`'s blob SHA
**necessarily changed** because D-10 added four arrays, and its identity proof is the per-array byte-identity
check Plan 119-05 performed — restate that method and its result rather than re-deriving it.

Name the two known-RED host modules (`tests/test_audit_coverage_matrix.py` stale golden;
`tests/test_no_programmer_found_*` red with a live board attached, and three boards are attached) as
pre-existing, with their memory references, and confirm they were not chased.

Record everything in the SUMMARY in the section shape `118-NONREGRESSION.md` uses, so Plan 119-10 can lift it
rather than reconstruct it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run 2>&1 | grep -E 'Flash|RAM|SUCCESS' | tail -12 && cd /workspaces/firestarter_app && python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` and `pio test -e native_nodevtools` report identical case and suite counts, both green, both recorded; any divergence is reported as a defect
    - `pio run` reports SUCCESS on all three AVR envs with all six flash/RAM figures recorded
    - The SUMMARY contains a flash decomposition attributing the total delta to Plans 119-01, 119-02, 119-04, 119-07 and 119-08, with the arithmetic shown against 2992 B of live Leonardo headroom and no threshold claim beyond "fits"
    - The SUMMARY records that the `-D DEV_TOOLS` configuration is the tighter and therefore binding one, citing the 1292 B flag cost and the 24388/28672 release-config measurement
    - All six host pytest modules pass; all four checker scripts exit 0; `_shared/sdp_bus_config.h` regenerates blob-identically; ruff passes against the py3.9/3.11 CI targets with the version recorded
    - The phase firmware diff was enumerated with `git diff --name-only <phase-base>..HEAD` and the SUMMARY states that `include/flash_utils.h`, the `5v_page` and the `nor_unlock` sources are absent from that real-path listing — not checked via the ROADMAP's non-existent path shorthand
    - `_shared/host_stubs_common.inc` and `_shared/sdp_bus_config.h` are confirmed blob-SHA identical to the phase base
    - The SUMMARY restates that `_shared/sdp_expected.h`'s blob SHA necessarily changed and that its identity proof is Plan 119-05's per-array byte-identity check, with that method and result carried forward
    - The two known-RED host modules are named as pre-existing with their reasons and were not chased
    - `REQUIREMENTS.md` is unchanged by this plan; LOCK-06 still Pending
  </acceptance_criteria>
  <done>The phase's firmware is complete and swept: both native envs agree exactly, all three AVR builds link, the flash delta is decomposed per plan against the live 2992 B headroom, every host gate is green, the diff is enumerated by real path, and the golden identity story is recorded in the form Plan 119-10 consumes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured microsecond interval to a claim about silicon | The most likely validation-ceiling crossing in the phase |
| a report placed at one exit to a measurement that must survive the other | With an empty socket the first page always fails |
| a max-tracking compare to a declined budget check | D-16 declined the latter; a reader must not mistake one for the other |
| a mocked tick script to a decoded parameter value | An under-scripted queue silently returns its tail and the case still passes on membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-08-CEILING | Repudiation | the gh#11 comment and the reported number | critical | mitigate | The comment must state that the number is the MCU driving its own latches, that it says nothing about the die accepting a byte, and that `t_BLC` as accepted by the die is explicitly not provable per the validation ceiling; the same wording carries into `119-MEASUREMENT.md` |
| T-119-08-UNREACHABLE | Repudiation | a report line only at the normal exit | high | mitigate | The single-exit restructure plus Case B, which drives the aborting path explicitly, because the bench condition is an empty socket where the first page always fails |
| T-119-08-VACUOUSVALUE | Repudiation | a membership-only assertion on the report id | high | mitigate | Case A decodes the u32 parameter out of the captured frame and asserts it equals a scripted non-monotonic maximum, so a tracker reporting zero or the last interval fails |
| T-119-08-DECLINEDWARN | Tampering | a future runtime budget compare in the hot loop | medium | mitigate | Case C pins the absence of `MSG_WARN_SDP_TBLC_EXCEEDED` on this path while asserting the report is present, so adding a WARN requires a deliberate decision that breaks a named case |
| T-119-08-FRAMECOUNT | Tampering | pre-existing frame-count assertions | medium | mitigate | Plan 119-05's inventory is worked through entry by entry; widened expectations name the new id rather than loosening to an at-least form; every touched case is listed |
| T-119-08-INDEX | Tampering | `eeprom28c_write_execute`'s index invariant | medium | mitigate | Explicit prohibition on introducing any new index or bound; `window_start <= i < data_size` preserved, asserted as a criterion |
| T-119-08-GHCONFLATE | Repudiation | gh#11's framing | medium | mitigate | The existing citation comment's wording is reused, and reframing gh#11 as a sampling-rate problem is prohibited in code, comments and the SUMMARY |
| T-119-08-VACUOUSPATH | Repudiation | the frozen-file check | medium | mitigate | Task 3 enumerates the diff with `git diff --name-only` and asserts against the real-path listing, never against the ROADMAP's non-existent `src/flash_utils.h` shorthand |
| T-119-08-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. The structural precondition (nothing after the loop) was verified against live source before editing.
2. `eeprom28c_write_execute` contains the tracker, the abort flag, exactly one `LOG_ID_U32(MSG_INFO_PAGE_LOAD_WORST_US, ...)` after the loop, no `AT28C_TBLC_MAX_US` comparison and no WARN.
3. The extended gh#11 comment carries all five required records including the named flash-versus-timing conflation.
4. Cases A through D pass; Case A decodes and asserts the value; Case B covers the aborting exit; Case C pins the declined WARN; Case D pins `response_code`.
5. Every frame-count-inventory entry that drives `write_execute` was re-checked and every touched case is listed by name.
6. Both native envs report identical counts, both green; `pio run` 3/3 SUCCESS.
7. The flash decomposition is recorded per plan against 2992 B with no threshold claim beyond "fits", and names `-D DEV_TOOLS` as the binding configuration.
8. Every host gate exits 0 or passes; `sdp_bus_config.h` blob-identical; ruff clean against the CI targets; the diff enumerated by real path.
9. `REQUIREMENTS.md` unchanged.
10. Commits: one or more inside `firestarter/`, then one meta commit staging the gitlink plus the SUMMARY. Suggested firmware subject: `feat(119-08): report the worst per-byte page-load interval once per write (D-16)`.
</verification>

<success_criteria>
- The page-load loop's worst per-byte interval is tracked and reported once, reachable on both the completing and the aborting exit, with no budget comparison added to the hot path.
- The report's value is proven correct by decoding the emitted frame against a scripted non-monotonic maximum, not merely by id membership.
- The flash-versus-timing conflation in the directive is named in source, and gh#11 stays framed as a conflation bug.
- Every pre-existing frame-count assertion is re-checked by name; nothing was blanket-loosened.
- The phase's firmware is complete and fully swept, with the flash delta decomposed per plan against the live 2992 B headroom and the diff enumerated by real path.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md` when done.
Record: the verified structural precondition; the tracker's final code and the single-exit restructure; the four
new cases with the scripted tick values and the decoded value asserted; every frame-count case touched, by name;
both envs' identical counts; all six flash/RAM figures with the per-plan decomposition and the arithmetic against
2992 B; the `-D DEV_TOOLS` binding-configuration note; the enumerated firmware diff with the frozen files absent
by real path; the golden identity story for Plan 119-10 to lift; the two known-RED host modules; and confirmation
that no requirement row was changed and LOCK-06 remains open pending Plan 119-10.
</output>
