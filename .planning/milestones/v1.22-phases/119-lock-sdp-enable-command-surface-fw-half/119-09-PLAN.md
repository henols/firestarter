---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 09
type: execute
wave: 9
depends_on: [119-08]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [DEVTEST-01]

must_haves:
  truths:
    - "D-08 item 1: Phase 121's DEVTEST-01 firmware half landed EARLY, in Phase 119. Its ROADMAP scope, its plan set and REQUIREMENTS.md's LOCK/DEVTEST mapping must be amended AS AN EXPLICIT TASK IN THIS PHASE, with the amendment recorded in STATE.md and PROJECT.md. This is an owned, operator-approved scope addition, not a leak, and it must not be discovered by Phase 121's planner."
    - "DEVTEST-01's HOST half stays in Phase 121: OP_ERASE marked NA for protocol 0x0D with a named reason in the dev test sweep. So DEVTEST-01's REQUIREMENTS.md checkbox stays UNCHECKED. What changes is the mapping and the phase text, not the status."
    - "D-05: LOCK-04's mechanism correction must be recorded in the phase artifacts so a verifier does not read LOCK-04 as failed - and REQUIREMENTS.md's LOCK-04 WORDING must NOT be edited. The distinction is sharp: D-08 sanctions amending the DEVTEST-01 mapping; D-05 and D-15 forbid editing LOCK-04's and LOCK-06's requirement text."
    - "D-15: LOCK-06's 3348 B is a superseded pre-117 figure. The live Leonardo number is 25680/28672 leaving 2992 B, because +204 B (Phase 117) and +152 B (Phase 118) are already spent. Record the correction in PROJECT.md and the phase artifacts; do NOT edit REQUIREMENTS.md's LOCK-06 text."
    - "RESEARCH F-K / Open Question 2: ROADMAP criterion 5's 'header comment' instruction conflicts with D-09's byte-frozen flash_utils.h and with FIX-04's closed byte-untouched claim. The intent was satisfied in eeprom_28c.cpp instead. This is the FOURTH divergence in this phase between a criterion's stated mechanism and its intent, and it belongs in the same correction record as D-05 and D-15."
    - "RESEARCH F-B2: a THIRD command behaviour delta beyond cmd 7 and 8 - CMD_IDLE (0) now produces silence where it previously produced two error frames. D-01 names only 7 and 8, so this one must be recorded where a future reader finds it."
    - "RESEARCH F-F2: the host's _SRAM_PROTO_IDS workaround does NOT become dead code under D-06's guard. It short-circuits SRAM and FRAM blank-check before any firmware command is issued, so it fires earlier and produces a materially better message than a bare 0xA5 would. The correct Phase 120 disposition is KEEP. Identified here, acted on there - never deleted in this phase."
    - "The folded todo prove-pio-dev-flag-fails-closed.md has ITEM 4 answered by Plan 119-02's second native env. Items 1 through 3 stay open and belong to 999.15 / gh#8. The todo file must show that split so 999.15 inherits the answer without inheriting a false sense that the whole todo is done."
    - "STATE.md tooling under-writes and re-clobbers fields. Call state.record-session FIRST, then progress/metric/decision calls, then hand-verify current_phase_name (it has been mangled on the em-dash and trailing parenthetical repeatedly) and progress.percent regardless of call order. NEVER trust the returned `updated` array - it has reported [] while mutating the file."
  prohibitions:
    - "MUST NOT tick DEVTEST-01's checkbox in REQUIREMENTS.md. Its host half is Phase 121."
    - "MUST NOT edit LOCK-04's or LOCK-06's requirement WORDING in REQUIREMENTS.md. Only the DEVTEST-01 mapping row and the traceability table may change, plus checkboxes Plans 119-03, 119-05, 119-06 and 119-07 already set."
    - "MUST NOT remove Phase 121 from ROADMAP.md or renumber any phase. Amend its scope text and criteria; the phase stays."
    - "MUST NOT delete or edit firestarter_app/firestarter/eprom_operations.py's _SRAM_PROTO_IDS. Record the keep-disposition only."
    - "MUST NOT touch either submodule. This plan is meta-repo only and both sub-repo working trees must stay clean."
    - "MUST NOT run `gsd roadmap phases.clear` or any destructive ROADMAP mutation. This project has 50-plus preserved phase directories that such a call would hard-delete."
    - "MUST NOT mark items 1 through 3 of prove-pio-dev-flag-fails-closed.md as answered."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/STATE.md
  key_links:
    - "Phase 121's ROADMAP criteria and its Depends-on line are what a future planner reads. If they still claim the firmware fail-close as Phase 121 work, that planner will either re-implement it or mark it failed. Both outcomes are the discovery D-08 exists to prevent."
    - "PROJECT.md's five existing correction blocks are the established shape for recording a mid-milestone correction. This plan adds a sixth in that shape, gathering the phase's four mechanism-versus-intent divergences and the three behaviour deltas in one place."
    - "Plan 119-10's 119-NONREGRESSION.md and Plan 119-11's 119-MEASUREMENT.md both cite these corrections. Landing them before those two plans means those documents reference a recorded correction rather than restating an argument."
---

<objective>
Own D-08's consequences in writing: amend Phase 121's ROADMAP scope and `REQUIREMENTS.md`'s DEVTEST-01 mapping to
record that the firmware half landed in Phase 119, add a PROJECT.md correction block gathering this phase's four
mechanism-versus-intent divergences and its three command behaviour deltas, record the `_SRAM_PROTO_IDS`
keep-disposition for Phase 120, close out the folded todo's item 4, and bring STATE.md into agreement with all of it.

Purpose: D-08 makes this an explicit task rather than a footnote, because the alternative is a Phase 121 planner
who either re-implements a fix that already shipped or marks a requirement failed. This phase produced four
separate cases where a criterion's stated mechanism was wrong while its intent was right; recording them in one
place is what turns them from surprises into decisions.

Output: an amended Phase 121, an amended DEVTEST-01 mapping with its checkbox still unticked, a sixth PROJECT.md
correction block, a closed-out todo item 4, and a hand-verified STATE.md.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-02-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-08-SUMMARY.md
@.planning/todos/prove-pio-dev-flag-fails-closed.md
</context>

## Repo and commit mechanics

This plan writes **only** meta-repo files under `.planning/`. Commit in `/workspaces`, staging those paths
explicitly. Do **not** stage either submodule gitlink — neither sub-repo is touched, and
`git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short` must both
be clean at the end.

Use `Edit` for scoped replacements in `ROADMAP.md`, `REQUIREMENTS.md`, `PROJECT.md` and `STATE.md`. **Never**
rewrite any of those files wholesale — `ROADMAP.md` alone carries every phase of this milestone and several
preserved earlier ones, and a whole-file write destroys everything outside the edit window.

## Artifacts this phase produces

**Created by THIS plan:** the amended Phase 121 scope text and criteria in `ROADMAP.md`; the amended DEVTEST-01
row and traceability entry in `REQUIREMENTS.md`; a sixth correction block in `PROJECT.md`; the item-4 closeout in
`.planning/todos/prove-pio-dev-flag-fails-closed.md`; and updated `STATE.md` position and decision records.

**Created earlier in Phase 119:** the three catalog ids (119-01); `CMD_SDP_UNLOCK` (9), `CMD_SDP_LOCK` (10),
`is_memory_cmd()`, `[env:native_nodevtools]` and `test_cmd_admission` (119-02);
`check_is_memory_cmd_no_ifdef.py` with its pytest and `planted_ifdef_in_predicate.h` (119-03);
`EEPROM_SDP_ENABLE[3]`, `eeprom28c_emit_sdp_sequence_timed()`, `eeprom28c_sdp_lock_execute()`,
`eeprom28c_sdp_unlock_execute()`, `eprom_sdp_lock()`, `eprom_sdp_unlock()` and the appended emit anchor (119-04);
the four `SDP_FIXED_LOCK_*` goldens and the scripted `micros()` queue (119-05); the three-way identity guard and
the host parity leg (119-06); the generic NULL-`main` refusal (119-07); the page-load worst-interval tracker
(119-08).

**Created later:** `119-NONREGRESSION.md` (119-10); `119-MEASUREMENT.md` (119-11).

## Requirement ownership

This plan changes **DEVTEST-01's mapping, not its status**. Its firmware half landed in Plan 119-07; its host
half — `OP_ERASE` marked `NA` for `0x0D` with a named reason in the `dev test` sweep — stays in Phase 121.
DEVTEST-01's checkbox stays **unticked**.

No other requirement status changes here. Confirm by reading that LOCK-01, LOCK-02, LOCK-03, LOCK-04 and LOCK-05
already read Complete from Plans 119-03, 119-05, 119-06 and 119-07, and that LOCK-06 still reads Pending pending
Plan 119-10. If any of those disagrees, report it as a defect rather than fixing it here.

<tasks>

<task type="auto">
  <name>Task 1: Amend Phase 121's ROADMAP scope and REQUIREMENTS.md's DEVTEST-01 mapping</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md around line 156 (Phase 121's one-line milestone entry, which names "firmware fail-closed on CMD_ERASE for 0x0D" as Phase 121 scope) and the "Phase Details" block for Phase 121 starting near line 325 (its Goal, Depends-on line, Requirements list and numbered Success Criteria — criterion 1 or 2 is the one that claims the firmware fail-close)
    - .planning/ROADMAP.md the "Phase Details" block for Phase 119 near line 290 (its six success criteria — criteria 4, 5 and 6 are the ones this phase corrected; read them so the correction record quotes them accurately)
    - .planning/REQUIREMENTS.md lines 82-90 (the DEVTEST-01 requirement text and the GATE-01/02/03 block) and lines 147-195 (the traceability table with DEVTEST-01 mapped to Phase 121, and the Coverage counts at the bottom which must stay arithmetically correct)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md (what the firmware half actually delivered, including the command-by-protocol matrix and the honest chip-id qualification — the amendment must describe what shipped, not what was hoped)
  </read_first>
  <action>
Use `Edit` for every change below. Scoped replacements only.

**ROADMAP.md, Phase 121's one-line milestone entry.** Amend the clause that currently claims "firmware
fail-closed on `CMD_ERASE` for `0x0D`" as Phase 121 scope, so it records that the firmware half landed in Phase
119 under D-06/D-07/D-08 and that Phase 121 retains the host half. Keep the entry's `- [ ]` checkbox, its phase
number, its title and every other clause intact.

**ROADMAP.md, Phase 121's Phase Details block.** Two edits:
1. Amend the Success Criterion that asserts the firmware fail-close so it reads as **satisfied by Phase 119**,
   naming the mechanism that actually shipped: one generic NULL-`firestarter_operation_main` refusal at the
   operation layer emitting `MSG_ERR_NOT_SUPPORTED` with `RESPONSE_CODE_ERROR`, which closes the phantom-success
   class for every command and every protocol rather than for `CMD_ERASE` on `0x0D` alone. State that Phase 121's
   remaining DEVTEST-01 work is the host half: `OP_ERASE` marked `NA` for protocol `0x0D` with a named reason in
   the `dev test` sweep.
2. Add a short note to that block recording **why** it moved: `op_execute_stateful_operation`'s NULL-`main`
   fall-through made every unconfigured command on every protocol report OK having done nothing, so it was a
   whole-dispatch-layer defect that DEVTEST-01 had noticed one instance of, and fixing the class generically in
   Phase 119 was cheaper than fixing it twice. Name it as an operator-approved scope addition with its
   cross-family sweep accepted, not as a leak.

**ROADMAP.md, Phase 119's Phase Details block.** Add a correction note under its Success Criteria recording that
three of the six criteria were superseded by `119-CONTEXT.md`, with what replaced each:
- **Criterion 4** named a `default:` arm in `configure_eeprom28c`. That mechanism was disproven against live
  source: `configure_memory` pre-sets the generic `main` for `CMD_READ`, `CMD_WRITE` and `CMD_VERIFY` before the
  protocol handler runs, and read and verify fall through that switch, so a blanket arm would refuse them on all
  84 `0x0D` chips. The intent — lock and unlock fail-closed for any protocol other than `0x0D`, never silently
  accepted — is satisfied by D-06's op-layer guard. **Mechanism-corrected, intent-satisfied. Not failed.**
- **Criterion 5** asked for a **header** comment in `flash_utils.h`. That file is FIX-04 byte-frozen and FIX-04 is
  a closed requirement asserting it byte-untouched, so editing it — even comment-only — would re-open that claim
  and break `118-NONREGRESSION.md`'s framing. The rationale lives beside `EEPROM_SDP_ENABLE` in
  `eeprom_28c.cpp` instead, plus the pre-existing comment at `test_sdp_harness.cpp:291-296`, and the duplication
  is machine-checked by a three-way identity and distinctness guard. **Intent-satisfied in a different file,
  deliberately.**
- **Criterion 6** cited **3348 B** of headroom. That is a pre-117 figure; `+204 B` and `+152 B` are already spent,
  so the live Leonardo number is 25680/28672 leaving **2992 B**. Judge against 2992 B, with the arithmetic shown
  and no threshold claim beyond "fits".

Do **not** renumber, delete or reorder any phase, and do not run any destructive ROADMAP mutation — this project
carries more than fifty preserved phase directories that a `phases.clear` would hard-delete.

**REQUIREMENTS.md.** Two edits, both narrow:
1. In the DEVTEST-01 block, append a parenthetical or a following line recording the split: the firmware half
   (fail-closed on `CMD_ERASE` for `0x0D`, via the generic op-layer NULL-`main` refusal) landed in **Phase 119**
   under D-06/D-07/D-08; the host half (`OP_ERASE` marked `NA` for `0x0D` with a named reason in the `dev test`
   sweep) remains **Phase 121**. **Leave the checkbox `- [ ]` unticked.** Do not reword the requirement itself.
2. In the traceability table, change DEVTEST-01's Phase cell to record both phases (firmware Phase 119, host Phase
   121) and leave its Status as Pending. Then re-check the Coverage block at the bottom: the total requirement
   count is unchanged at 36 and the unmapped count stays 0, but the per-phase tally sentence now needs
   DEVTEST-01 shown as split. Update that sentence so the arithmetic still reads correctly, and state in the
   SUMMARY what you changed there.

**Do not touch LOCK-04's or LOCK-06's requirement wording.** D-05 and D-15 both say so explicitly. The only
`REQUIREMENTS.md` changes permitted in this plan are the two DEVTEST-01 edits and the Coverage sentence. Confirm
by diff that nothing else moved.
  </action>
  <verify>
    <automated>cd /workspaces && git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md && grep -c 'Phase 119' .planning/REQUIREMENTS.md && grep -n 'DEVTEST-01' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `ROADMAP.md`'s Phase 121 one-line entry records the firmware half as landed in Phase 119 and retains its `- [ ]` checkbox, phase number, title and all other clauses
    - `ROADMAP.md`'s Phase 121 Phase Details block has the firmware-fail-close criterion amended to read as satisfied by Phase 119, naming the generic NULL-`main` refusal with `MSG_ERR_NOT_SUPPORTED` and `RESPONSE_CODE_ERROR`, and names the host half as Phase 121's remaining DEVTEST-01 work
    - That block carries a note explaining why it moved, framing it as an operator-approved scope addition with the cross-family sweep accepted
    - `ROADMAP.md`'s Phase 119 Phase Details block carries a correction note covering criteria 4, 5 and 6, each with the disproven mechanism, what replaced it, and the phrase mechanism-corrected / intent-satisfied for criterion 4
    - No phase was renumbered, deleted or reordered; no destructive ROADMAP mutation was run
    - `REQUIREMENTS.md`'s DEVTEST-01 block records the firmware/host split by phase and its checkbox is still `- [ ]`
    - `REQUIREMENTS.md`'s traceability row for DEVTEST-01 names both phases and reads Status Pending
    - The Coverage block's arithmetic still reads correctly with DEVTEST-01 shown as split, and the change is described in the SUMMARY
    - `git diff .planning/REQUIREMENTS.md` shows changes ONLY to the DEVTEST-01 block, its traceability row and the Coverage sentence — LOCK-04's and LOCK-06's wording untouched
    - Every edit was made with a scoped `Edit`; neither file was rewritten wholesale
  </acceptance_criteria>
  <done>A Phase 121 planner reading the ROADMAP and REQUIREMENTS will find the firmware half recorded as already shipped in Phase 119 with the mechanism named, the host half still theirs, and Phase 119's three superseded criteria explained — with no requirement wording edited and no phase disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Add PROJECT.md's sixth correction block, close out todo item 4, and hand-verify STATE.md</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md, the "Current Milestone: v1.22" section with all five existing correction blocks (read every one — the THIRD's 66-of-84 and per-pinout inhibit table, the FOURTH's +204 B and its item 4 gate lesson and item 5 vacuous-path warning, and the FIFTH's F-118-01 finding with its page-load directive at LOCK-06 and its two flash datapoints). The sixth block must match their shape, tone and level of specificity
    - .planning/STATE.md read whole, with particular attention to the frontmatter block and the long HTML comment recording every observed way the state tooling under-writes and re-clobbers fields
    - .planning/todos/prove-pio-dev-flag-fails-closed.md read whole (all four items, and the "Why symbol-checking, not exit codes" section explaining why a passing `pio run` and a flash-size delta are both weak oracles)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-02-SUMMARY.md (the measured no-DEV_TOOLS result and the three behaviour deltas) and 119-08-SUMMARY.md (the flash decomposition and the enumerated diff)
  </read_first>
  <action>
**PROJECT.md — a sixth correction block**, in the same shape as the existing five, appended to the
"Current Milestone: v1.22" section. It gathers everything this phase corrected or discovered that a later reader
must not have to reconstruct. Seven items:

1. **LOCK-04's mechanism is superseded (D-05).** The `default:`-arm mechanism in `configure_eeprom28c` was
   disproven against live source; a blanket arm would refuse `read` and `verify` on all 84 protocol-`0x0D` chips
   because `configure_memory` pre-sets their generic mains before the handler runs. The intent is satisfied by
   one generic NULL-`main` refusal at the operation layer. **Mechanism-corrected, intent-satisfied — never read
   LOCK-04 as failed.** `REQUIREMENTS.md`'s LOCK-04 wording was deliberately not edited.
2. **Criterion 5's header comment moved (F-K).** `flash_utils.h` is FIX-04 byte-frozen, so the datasheet
   rationale for the `AA-55-A0` duplication lives beside `EEPROM_SDP_ENABLE` in `eeprom_28c.cpp` plus the
   pre-existing `test_sdp_harness.cpp:291-296` comment, and is machine-checked by a three-way identity and
   distinctness guard plus a host source-text parity leg. Intent satisfied in a different file, deliberately.
3. **LOCK-06's 3348 B is superseded (D-15).** The live Leonardo figure is 25680/28672 leaving **2992 B**;
   `+204 B` (Phase 117) and `+152 B` (Phase 118) are spent. Also record that the `-D DEV_TOOLS` configuration is
   the **tighter** one and therefore the binding constraint: a release-config Leonardo build measured
   24388/28672, so the flag costs **1292 B**. `REQUIREMENTS.md`'s LOCK-06 wording was deliberately not edited.
4. **DEVTEST-01's firmware half landed in Phase 119 (D-07/D-08).** Name the mechanism, name that it closes the
   phantom-success class for every command and protocol rather than one cell, and name that Phase 121 keeps the
   host half. Reference the amendment Task 1 made.
5. **Three command behaviour deltas, all deliberate.** Cmd 7 and cmd 8 lose `configure_memory` admission in a
   release build (D-01's safety tightening — a release build previously configured a memory handler for dev
   commands it would then refuse); and **cmd 0 / `CMD_IDLE`** now produces silence where an explicit `{"cmd":0}`
   frame previously produced two error frames (`0xBB` then `MSG_ERR_SETUP`). The third one is F-B2 and D-01 names
   only the first two, which is why it is recorded here. An explicit refusal arm for cmd 0 was considered and
   declined on flash grounds; `CMD_IDLE` is a firmware-internal state no host emits.
6. **`_SRAM_PROTO_IDS` does NOT become dead code (F-F2).** The host's SRAM/FRAM blank-check short-circuit fires
   **before** any firmware command is issued, so D-06's guard is unreachable from `check_eprom_blank`, and the
   workaround produces a materially better message than a bare `0xA5` would. Its comment's claim that the
   firmware emits `0xA4 MSG_ERR_EMPTY_INPUT` is a follow-on artifact of the silent completion, not a firmware
   refusal. **Correct Phase 120 disposition: KEEP.** It was identified in Phase 119 and deliberately not touched.
7. **A new firmware-source-scanning host gate exists.** `check_is_memory_cmd_no_ifdef.py` joins the
   CORRECTION-4 item-4 checklist for Phases 120, 121 and 122, alongside `check_no_log_in_sdp_window.py`,
   `test_sdp_table_parity.py`, `gen_sdp_bus_config.py` / `test_sdp_bus_config_drift.py`,
   `test_revision_constants_parity.py`, `check_dispatch.py`, `test_dispatch_mirror.py` and
   `check_devtest_orchestrator.py`. Also record that `sdp_expected.h`'s whole-file blob SHA **necessarily
   changed** this phase, so 117's and 118's blob-SHA identity shorthand no longer applies to that file and the
   proof is per-array byte-identity — a later phase must not reach for the old shorthand.

**The folded todo.** Edit `.planning/todos/prove-pio-dev-flag-fails-closed.md` to mark **item 4 only** as
answered, with the measured evidence: `[env:native_nodevtools]` compiles without `-D DEV_TOOLS` and the full
suite passes with zero test-code changes, so `pio test` does pass with `DEV_TOOLS` absent; plus the 1292 B
Leonardo flash cost of the flag. State that the env is now permanent in `platformio.ini` with a CI step, and that
**items 1 through 3 remain open** and belong to 999.15 / gh#8 (the `${sysenv.*}` fail-open versus fail-closed
matrix with `avr-nm` symbol capture, and the choice of gating mechanism). Do not mark items 1 to 3 answered, and
do not close the todo.

**STATE.md.** Follow the sequence the file's own HTML comment prescribes, because this tooling has under-written
and re-clobbered fields on every path in this project — planning, plan execution and phase completion:
1. Call `state.record-session` **first**.
2. Then any progress, metric and decision calls.
3. Then **hand-verify and hand-correct** the frontmatter: `current_phase` and `current_phase_name` (this field has
   been mangled by splitting on the em-dash and the trailing parenthetical repeatedly — it must read the full
   Phase 119 title), `status`, `stopped_at`, `last_activity_desc`, `progress.total_plans` (11 for this phase) and
   `progress.percent`. **Diff STATE.md before and after every call and never trust the returned `updated`
   array** — it has reported `[]` while mutating the file.

Then update STATE.md's body: the Current Position block for Phase 119's progress, and a short record of this
phase's three cross-phase consequences pointing at PROJECT.md's new correction block rather than duplicating it —
the DEVTEST-01 move, LOCK-04's mechanism correction, and the `_SRAM_PROTO_IDS` keep-disposition for Phase 120.
Keep the existing Phase 118 and earlier records intact; append, do not rewrite.

Finally confirm both sub-repo working trees are clean and that this plan's commit stages only `.planning/` paths —
no gitlink, no sub-repo file.
  </action>
  <verify>
    <automated>cd /workspaces && git status --short && grep -c 'SIXTH\|_SRAM_PROTO_IDS\|2992\|1292' .planning/PROJECT.md && git -C firestarter status --short && git -C firestarter_app status --short</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT.md` carries a sixth correction block in the same shape as the existing five, containing all seven items described above
    - Item 1 states LOCK-04 as mechanism-corrected and intent-satisfied and records that its requirement wording was deliberately not edited
    - Item 3 records 2992 B as the live Leonardo headroom, names `+204 B` and `+152 B` as spent, and records the 1292 B `-D DEV_TOOLS` cost with the 24388/28672 release-config measurement and the binding-configuration conclusion
    - Item 5 names all three behaviour deltas including cmd 0 / `CMD_IDLE`, with the declined-refusal-arm decision recorded
    - Item 6 records the `_SRAM_PROTO_IDS` KEEP disposition for Phase 120 with F-F2's reasoning and the note that its own comment's `0xA4` claim is a follow-on artifact
    - Item 7 lists the full nine-gate CORRECTION-4 checklist including the new gate, and records that `sdp_expected.h`'s blob-SHA shorthand no longer applies with per-array byte-identity as the replacement
    - `.planning/todos/prove-pio-dev-flag-fails-closed.md` marks item 4 answered with the measured evidence and the 1292 B figure, states items 1 through 3 remain open under 999.15 / gh#8, and is not closed
    - `STATE.md`'s frontmatter reads `current_phase: 119`, the FULL Phase 119 title in `current_phase_name` (not a bare parenthetical, not truncated), `progress.total_plans: 11`, and a `progress.percent` consistent with the plans completed — all hand-verified after the last state call
    - `STATE.md`'s body records the three cross-phase consequences by pointing at PROJECT.md's new block, and every earlier phase record is intact
    - `git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short` are both empty
    - `git -C /workspaces show --stat HEAD` lists only `.planning/` paths — no gitlink, no sub-repo file
    - Every edit was made with a scoped `Edit`; no file was rewritten wholesale
  </acceptance_criteria>
  <done>Every correction, deviation and behaviour delta this phase produced is recorded where a later reader finds it: PROJECT.md carries a sixth correction block with all seven items, the folded todo shows item 4 answered and items 1 to 3 open, and STATE.md agrees with reality after hand-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a shipped fix to a later phase's plan | An unamended Phase 121 leads to a re-implementation or a false failure |
| a corrected mechanism to a requirement's text | Editing the text hides the correction; not recording it makes the requirement read as failed |
| a state-tooling call to the file it claims to have updated | The tooling has reported no writes while mutating fields |
| a whole-file write to a document holding every phase | `ROADMAP.md` carries more than fifty preserved phases outside any edit window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-09-DISCOVERY | Repudiation | Phase 121's unamended scope | high | mitigate | Task 1 amends both the one-line entry and the Phase Details criteria, names the shipped mechanism, and records why it moved as an operator-approved addition; the DEVTEST-01 mapping row records both phases |
| T-119-09-FALSEFAIL | Repudiation | LOCK-04, criterion 5 and LOCK-06 read literally | high | mitigate | All three corrections recorded in ROADMAP Phase 119's block and in PROJECT.md's sixth block, with mechanism-corrected / intent-satisfied stated for LOCK-04; requirement wording deliberately untouched |
| T-119-09-EARLYTICK | Repudiation | DEVTEST-01's checkbox | high | mitigate | Explicit prohibition on ticking it; the host half is Phase 121; the acceptance criteria assert the checkbox is still `- [ ]` |
| T-119-09-WHOLEFILE | Denial of service | `ROADMAP.md`, `REQUIREMENTS.md`, `PROJECT.md`, `STATE.md` | critical | mitigate | Scoped `Edit` mandated for every change; whole-file rewrites and destructive ROADMAP mutations prohibited; a `git diff --stat` review is an acceptance criterion |
| T-119-09-STATEDRIFT | Tampering | `STATE.md` frontmatter | medium | mitigate | The prescribed call order, hand-verification of six named fields afterwards, a before-and-after diff, and an explicit instruction never to trust the returned `updated` array |
| T-119-09-HOSTDELETE | Tampering | `_SRAM_PROTO_IDS` | medium | mitigate | Recorded as KEEP for Phase 120 with its reasoning; explicit prohibition on editing it; both sub-repo trees asserted clean |
| T-119-09-SHORTHAND | Repudiation | `sdp_expected.h`'s blob-SHA shorthand | medium | mitigate | Item 7 records that the shorthand no longer applies to that file and names per-array byte-identity as the replacement, so a later phase does not reach for a proof that cannot hold |
| T-119-09-SC | Tampering | package-manager installs | low | accept | This plan installs nothing and touches no code; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. Phase 121's one-line entry and Phase Details block both record the firmware half as landed in Phase 119, with the mechanism named and the host half retained.
2. Phase 119's Phase Details block carries the criteria 4, 5 and 6 correction note.
3. `REQUIREMENTS.md`'s DEVTEST-01 block and traceability row record the split; the checkbox is still unticked; the Coverage arithmetic reads correctly; LOCK-04's and LOCK-06's wording is untouched.
4. `PROJECT.md`'s sixth correction block contains all seven items with the specified figures.
5. The folded todo shows item 4 answered with evidence and items 1 to 3 open.
6. `STATE.md`'s six named frontmatter fields are hand-verified correct, and every earlier phase record is intact.
7. Both sub-repo working trees are clean; the commit stages only `.planning/` paths.
8. No phase was renumbered or deleted; no destructive ROADMAP mutation was run.
9. One commit in `/workspaces`. Suggested subject: `docs(119-09): record the DEVTEST-01 firmware-half move and the phase's four criterion corrections (D-08)`.
</verification>

<success_criteria>
- A Phase 121 planner will read that DEVTEST-01's firmware half already shipped, with the mechanism named, and that only the host half remains theirs.
- All four mechanism-versus-intent divergences this phase produced (LOCK-04, criterion 5, LOCK-06's headroom, and the ROADMAP criterion 1 imprecision) are recorded in one place, with no requirement wording edited.
- All three command behaviour deltas, including the one D-01 does not name, are written down.
- `_SRAM_PROTO_IDS` is recorded as KEEP for Phase 120 rather than deleted here.
- The new host gate is on the CORRECTION-4 checklist and the retired blob-SHA shorthand is flagged.
- The folded todo shows exactly one item answered, and STATE.md agrees with reality after hand-verification.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-09-SUMMARY.md` when done.
Record: the exact ROADMAP and REQUIREMENTS edits made, quoted; what changed in the Coverage sentence; the seven
items of PROJECT.md's sixth block; the todo's item-4 closeout wording; every STATE.md field you had to
hand-correct and what the tooling had left it as; and confirmation that both sub-repo trees are clean, that no
requirement status changed, and that DEVTEST-01's checkbox is still unticked.
</output>
