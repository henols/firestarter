---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 07
type: execute
wave: 7
depends_on: [119-06]
files_modified:
  - firestarter/platformio.ini
  - firestarter/src/operation_utils.cpp
  - firestarter/include/operation_utils.h
  - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
autonomous: true
requirements: [LOCK-04, LOCK-02, DEVTEST-01]

must_haves:
  truths:
    - "D-05: LOCK-04's stated mechanism is SUPERSEDED and must never be implemented. configure_memory pre-sets the generic main for CMD_READ, CMD_WRITE and CMD_VERIFY at memory.cpp:48-58 BEFORE calling configure_eeprom28c, and read and verify fall through that switch keeping those generic mains. A literal `default: -> MSG_ERR_NOT_SUPPORTED` arm there would refuse read and verify on ALL 84 protocol-0x0D chips. Separately, configure_eeprom28c only ever runs FOR 0x0D, so a default: arm there cannot refuse another protocol at all. LOCK-04 must be recorded as MECHANISM-CORRECTED, INTENT-SATISFIED - never as failed."
    - "D-06: ONE guard, at the operation layer. A NULL firestarter_operation_main becomes MSG_ERR_NOT_SUPPORTED plus RESPONSE_CODE_ERROR at operation_utils.cpp's fall-through. Single site, smallest flash cost, and provably TOTAL: any protocol whose handler has no arm for a command is refused, present and future, with no per-handler maintenance. Rejected: a pre-dispatch protocol check in configure_memory (puts 0x0D-specific knowledge into the generic dispatcher v1.20's protocol-only rebuild deliberately kept clean); rejected: a default: arm in every configure_* handler (six sites, roughly 90-130 B against 2992 B, and each arm has to be written not to swallow the pre-set generic mains)."
    - "D-07: the guard is GENERIC, so the whole phantom-success class is fixed here. op_execute_stateful_operation returns false immediately when main is NULL, every caller inverts it, so the command reports FINISHED with response_code left at the RESPONSE_CODE_OK loop() set - and emits NO error and no MSG_MAIN_DONE either. It emits nothing at all. That is precisely DEVTEST-01's 'reports OK having done nothing' for CMD_ERASE on 0x0D. One refusal in the shared path fixes SDP, erase and chip-ID phantom-success together."
    - "D-08 item 2: the blast radius crosses every protocol family, so a full cross-family sweep is MANDATORY. The 0x05, 0x06, 0x07, 0x08, 0x0B, 0x10 and SRAM streams must stay byte-identical, and every command-by-protocol combination that previously returned a silent OK must be ENUMERATED with its new outcome. RESEARCH F-E supplies the complete precomputed matrix, so this is an enumeration to verify, not an exploration."
    - "RESEARCH Open Question 1 is BLOCKING and is this plan's FIRST task, not a later wave. [env:native] does not currently compile the op layer at all (build_src_filter = +<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c>), so whether LOCK-04's and DEVTEST-01's proofs are TESTS or PROSE depends on resolving it. Option (a): widen build_src_filter with +<operation_utils.cpp>, verifying all 17 suites for ArduinoFake aborts - RECOMMENDED. Option (b): a static inline helper in operation_utils.h - the recorded fallback, under which the one-line call site stays natively unproven and D-08's enumeration is prose."
    - "RESEARCH A2 is an ASSUMPTION with MEDIUM confidence: operation_utils.cpp's externals are all satisfiable (rurp_communication_* from the already-linked rurp_serial_utils.cpp, millis and delay from ArduinoFake, strncmp_P and PSTR from the per-suite pgmspace shims), but ArduinoFake ABORTS with SIGABRT on any unmocked virtual and only 4 of the 17 suites currently mock millis and delay. A SIGABRT here reads exactly like the deferred test_flash_intel_vpp Unity-teardown flake but is NOT it (Pitfall 5)."
    - "RESEARCH F-E's scoping proof, to cite rather than re-derive: hw_read_voltage, fw_get_version, hw_get_version, hw_get_config, dt_set_registers and dt_set_address never touch the op layer, and op_execute_stateful_operation is reached ONLY from the eprom_* entry points. So vpp, vpe, fw, config, hw, dev reg and dev address are structurally outside the blast radius."
    - "RESEARCH F-E on configure_not_implemented protocols: they are UNCHANGED. That handler sets response_code = RESPONSE_CODE_ERROR, so op_execute_function(configure_memory, ...) already returns false and parse_json already emits MSG_ERR_SETUP - the op layer is never reached. No double error, no new frame. Worth a case so a reviewer can see it."
    - "MSG_ERR_NOT_SUPPORTED (0xA5) already exists and is already eprom_erase's refusal id, so D-06 needs NO new catalog id and costs no catalog decision."
    - "Baselines to hold: pio test -e native and -e native_nodevtools at 127/127 across 17 suites before this plan's additions, pio run 3/3 SUCCESS, every host gate at Plan 119-06's recorded baseline."
  prohibitions:
    - "MUST NOT add a blanket default: arm to configure_eeprom28c or to any other configure_* handler. D-05's disproof is why."
    - "MUST NOT add a protocol check to configure_memory. v1.20's protocol-only rebuild deliberately removed protocol-specific knowledge from the generic dispatcher."
    - "MUST NOT add a new ERROR catalog id. 0xA5 exists."
    - "MUST NOT scope the guard to the SDP path only, and MUST NOT put it behind an enumerated allowlist. D-07 rejected both: the value is that it is generic and total."
    - "MUST NOT delete or modify firestarter_app/firestarter/eprom_operations.py's _SRAM_PROTO_IDS workaround. RESEARCH F-F2 found it does NOT become dead code - it fires earlier than the firmware and produces a materially better message. Identify it here, record that the correct Phase 120 disposition is KEEP, and act there. Host surface is Phase 120."
    - "MUST NOT amend ROADMAP.md, REQUIREMENTS.md's DEVTEST-01 mapping, PROJECT.md or STATE.md in this plan. That is Plan 119-09's owned task."
    - "MUST NOT diagnose a new-suite SIGABRT as the deferred Unity-teardown flake. It is an unmocked ArduinoFake virtual."
    - "MUST NOT use TEST_IGNORE_MESSAGE, and MUST NOT weaken a case to accommodate a link failure. Take option (b) and record it instead."
  artifacts:
    - firestarter/src/operation_utils.cpp
    - firestarter/platformio.ini
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  key_links:
    - "The refusal's exact shape is copied from eprom_operations.cpp:36-39 (LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED)) plus the response_code store idiom used by configure_not_implemented and by eeprom_28c.cpp:480-483 (handle->response_code = RESPONSE_CODE_ERROR)."
    - "Why the guard cannot break read or verify: configure_memory's pre-set means CMD_READ, CMD_WRITE and CMD_VERIFY are NEVER NULL-main for any protocol that reaches a configure_* handler. That is a source-level invariant, not a hope, and it is worth pinning as its own positive native case."
    - "op_execute_simple_operation's _single_step_operation_callback has exactly one command-specific branch, `if (handle->cmd == CMD_BLANK_CHECK)` for communication-mode frame flushing. It does not fire for cmd 9 or 10, so the wrapper is otherwise generic - but the planner should know it is not fully generic."
---

<objective>
Close the phantom-success class with one generic refusal at the operation layer: a NULL
`firestarter_operation_main` becomes `MSG_ERR_NOT_SUPPORTED` with `RESPONSE_CODE_ERROR` instead of a silent
OK. Resolve RESEARCH Open Question 1 first, as a bounded spike with a recorded fallback, because it decides
whether LOCK-04's and DEVTEST-01's proofs are tests or prose. Then enumerate the complete
command-by-protocol matrix and verify every previously-silent cell's new outcome.

Purpose: `op_execute_stateful_operation`'s bare `return false` on a NULL `main` means every unconfigured
command on every protocol reports OK having done nothing, emitting no error and no `MAIN_DONE` — nothing at
all. That is not a `0x0D` bug; it is a whole-dispatch-layer bug that DEVTEST-01 happened to notice one
instance of. Fixing the class generically is cheaper than fixing it twice, and the operator chose that
knowingly, accepting the cross-family sweep and the Phase 121 amendment.

Output: the single guard; the matrix enumerated as native cases under option (a) or as a recorded prose
matrix under option (b); the read/write/verify never-NULL positive invariant; the `cmd 9` / `cmd 10` dispatch
proofs; LOCK-04 and LOCK-02 closed and DEVTEST-01's firmware half done.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-VALIDATION.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-04-SUMMARY.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-06-SUMMARY.md
</context>

## Repo and commit mechanics

All work lands inside `firestarter/`, already on `v1.22-at28c-software-data-protection-lifecycle`. Confirm
with `git branch --show-current` before writing. Commit inside the submodule, then one meta commit staging the
gitlink plus the `.planning/` SUMMARY. `firestarter_app/` is **read** in this plan (to identify
`_SRAM_PROTO_IDS`) but must not be **modified** — its working tree stays clean.

Firmware plans are **strictly sequential** against the shared `firestarter/.pio/build/` tree. Do not start
before 119-06's commits exist.

## Task 1 is a blocking decision, not a warm-up

RESEARCH Open Question 1 changes this plan's shape. Resolve it in Task 1 as a bounded spike-then-commit, and
do not begin Task 3 until it is resolved and recorded. Do not spread the dependent proofs across later plans.

## Artifacts this phase produces

**Created by THIS plan:** the generic NULL-`main` refusal in `src/operation_utils.cpp`; under option (a), a
widened `build_src_filter` in both native envs; under option (b), a `static inline` refusal helper in
`include/operation_utils.h`; new cases in `test_dispatch/test_configure_memory.cpp` covering the
command-by-protocol matrix, the read/write/verify never-NULL invariant and the `cmd 9` / `cmd 10` dispatch
wiring; and, under option (a), refusal-frame cases in `test_eeprom28c_sdp.cpp`.

**Created earlier:** the three catalog ids (119-01); the command values, predicate, second native env and
`test_cmd_admission` (119-02); `check_is_memory_cmd_no_ifdef.py` with its pytest and fixture (119-03);
`EEPROM_SDP_ENABLE[3]`, the shared timed-emit helper, both SDP ops, the `configure_eeprom28c` arms, the
`loop()` arms and the appended emit anchor (119-04); the four `SDP_FIXED_LOCK_*` goldens, the scripted
`micros()` queue and cases 13 through 19 (119-05); the three-way identity and distinctness guard, cases 20
through 23 and the host parity leg (119-06).

**Created later:** the page-load worst-interval tracker (119-08); the Phase 121 scope amendment (119-09);
`119-NONREGRESSION.md` (119-10); `119-MEASUREMENT.md` (119-11).

## Requirement ownership

This plan closes three things:

- **LOCK-04**, as **mechanism-corrected, intent-satisfied**. The parenthetical in `REQUIREMENTS.md` must say
  so: the `default:`-arm mechanism the requirement text names was disproven against live source (it would
  refuse read and verify on all 84 `0x0D` chips), and the intent — lock and unlock fail-closed for any
  protocol other than `0x0D`, never silently accepted — is met by D-06's op-layer guard. Do **not** edit
  LOCK-04's requirement wording.
- **LOCK-02**, whose remaining half is the dispatch proof: `configure_eeprom28c` sets a main for cmd 9 and
  cmd 10 and leaves `init` and `end` NULL, and the standalone op is a single-step operation.
- **DEVTEST-01's firmware half** — `CMD_ERASE` on `0x0D` refused rather than silently OK. DEVTEST-01's host
  half (`OP_ERASE` marked `NA` for `0x0D` with a named reason in the `dev test` sweep) stays in Phase 121, so
  **DEVTEST-01's `REQUIREMENTS.md` row stays Pending** and its phase mapping is amended by Plan 119-09, not
  here. Record the firmware half as done in this plan's SUMMARY.

Mark LOCK-04 and LOCK-02 Complete. LOCK-06 stays Pending until Plan 119-10.

<tasks>

<task type="auto">
  <name>Task 1: Resolve Open Question 1 as a bounded spike, with option (b) as the recorded fallback</name>
  <files>firestarter/platformio.ini, firestarter/include/operation_utils.h</files>
  <read_first>
    - firestarter/platformio.ini lines 140-160 (the `build_src_filter` line and the two provenance comments recording its prior widenings: `rurp_serial_utils.cpp` for Phase 6 and `json_parser.c` for Phase 44, each naming the consuming suite — copy that comment discipline) plus the `[env:native_nodevtools]` block Plan 119-02 added, which needs the identical change
    - firestarter/src/operation_utils.cpp read whole (its full external surface: `rurp_communication_*` calls, `millis` and `delay` use, `strncmp_P` and `PSTR` use, the `LOG_*` macros, and the state machine at :62-84, :195-260, :270-297, :304-312)
    - firestarter/include/operation_utils.h lines 20-60 (the state macros at :24-27 and the `static inline` precedent at :41-51 — the two symbols `src/proms/` links today, and the shape option (b) would follow)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 75-90 (the four load-bearing ArduinoFake mocks and the comment stating that a SIGABRT in a NEW suite is an unmocked virtual, not the deferred Unity-teardown flake)
    - firestarter/test/native/avr/*/host_stubs.cpp across all suites (survey which suites already mock `millis` and `delay` — RESEARCH says only 4 do, and those are the 13 that may need mocks added under option (a))
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-F and Assumption A2 and Open Question 1 (the two options with their costs, the externals analysis, and the recommendation)
  </read_first>
  <action>
This is a **bounded spike, then commit the outcome**. Time-box it: if option (a) is not green across all 17
suites in both envs after adding the four standard ArduinoFake mocks to whichever suites abort, take option
(b) and record it. Do not iterate indefinitely, and do not weaken any suite to make (a) work.

**Attempt option (a) first**, per RESEARCH's recommendation. Append `+<operation_utils.cpp>` to
`build_src_filter` in **both** `[env:native]` and `[env:native_nodevtools]` — they must stay in lockstep or
the two envs stop testing the same code. Add a provenance comment in the same style as the two existing
widenings, naming v1.22 Phase 119 D-06/D-07 and the consuming suites, and stating that this is what makes the
NULL-`main` refusal and D-08's command-by-protocol enumeration machine-checked rather than reasoned.

Then run `pio test -e native` and `pio test -e native_nodevtools` and read the result suite by suite, not just
the total. Three outcomes are possible:

1. **All 17 suites link and pass.** Option (a) is taken. Record the case and suite counts, which must still be
   127/127 across 17 — this task adds no test.
2. **One or more suites abort with SIGABRT.** That is Pitfall 5: an unmocked ArduinoFake virtual on a path
   `operation_utils.cpp` now reaches, most likely `millis` or `delay`. It is **not** the deferred
   `test_flash_intel_vpp` Unity-teardown flake, and it must not be diagnosed as one. Add the four standard
   mocks (`delay`, `delayMicroseconds`, `millis`, `micros`) to each aborting suite's `setUp()`, with the
   "load-bearing, do not remove as unused" comment, and re-run. List every suite you had to touch and what
   you added.
3. **A link error.** Report the exact undefined symbol. If it is genuinely unsatisfiable in a host build,
   option (a) is not viable — go to (b).

**Option (b), the recorded fallback.** Revert the `build_src_filter` change in both envs, then extract the
refusal into a `static inline` helper in `include/operation_utils.h` following the `:41-51` precedent — a
function taking the handle, emitting `LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED)`, setting
`handle->response_code = RESPONSE_CODE_ERROR` and returning `false` — which Task 2 then calls from
`operation_utils.cpp`'s fall-through. Under (b) the predicate and refusal are natively testable in isolation
in both envs, with zero risk to existing suites, but the **one-line call site stays natively unproven** and
D-08's command-by-protocol enumeration becomes **prose, not a test**. If you take (b), say all of that
explicitly in the SUMMARY and in `119-NONREGRESSION.md`'s input — "weaker, but honest if recorded as such" is
the standard, and quietly presenting a prose matrix as a proof is not.

Whichever option lands, record in the SUMMARY: which option, why, the exact suite-by-suite outcome of the
attempt, any suites whose mocks you had to extend, and what LOCK-04's and DEVTEST-01's proofs consequently
rest on. Then re-run `pio run` (3/3 SUCCESS) and record the three flash figures — under option (a) the
production flash is unchanged (a native-only filter change), which is itself worth stating.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -25 && pio test -e native_nodevtools 2>&1 | tail -6 && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Option (a) was attempted first: `+<operation_utils.cpp>` was added to `build_src_filter` in BOTH native envs with a provenance comment naming D-06/D-07 and the consuming suites
    - The suite-by-suite result of that attempt is recorded, not just the totals
    - If any suite aborted, it was diagnosed as an unmocked ArduinoFake virtual (not as the deferred Unity-teardown flake), the four standard mocks were added to that suite's `setUp()` with the load-bearing comment, and every touched suite is listed
    - If option (b) was taken, the `build_src_filter` change was reverted in BOTH envs, a `static inline` refusal helper exists in `include/operation_utils.h` following the `:41-51` precedent, and the SUMMARY states explicitly that the call site is natively unproven and that D-08's enumeration is prose rather than a test
    - Both envs stay in lockstep: whichever option landed, `[env:native]` and `[env:native_nodevtools]` have identical `build_src_filter` values
    - `pio test -e native` and `pio test -e native_nodevtools` both report 127/127 across 17/17 suites (this task adds no test case)
    - `pio run` reports SUCCESS on all three AVR envs with the three flash figures recorded; under option (a) the SUMMARY states that production flash is unchanged because the filter is native-only
    - No suite was weakened, and `TEST_IGNORE_MESSAGE` appears nowhere
    - The SUMMARY names the chosen option, the reason, and what LOCK-04's and DEVTEST-01's proofs rest on
  </acceptance_criteria>
  <done>Open Question 1 is resolved and committed, with the chosen option and its suite-by-suite evidence recorded, both native envs in lockstep, and an explicit statement of whether the dependent proofs are tests or prose.</done>
</task>

<task type="auto">
  <name>Task 2: Land the single generic NULL-main refusal at the operation layer</name>
  <files>firestarter/src/operation_utils.cpp</files>
  <read_first>
    - firestarter/src/operation_utils.cpp lines 52-105 (op_execute_simple_operation at :58-60, op_execute_stateful_operation at :62-84 with the `if (handle->firestarter_operation_main)` at :63 and the bare `return false` at :83 — that fall-through is the phantom-success mechanism — and op_execute_function at :97-102)
    - firestarter/src/operation_utils.cpp lines 190-320 (_execute_operation_house_keeping at :195-217, _execute_operation_house_keeping_func at :230-260 which still calls op_wait_for_ack and emits INIT/END frame pairs even for a NULL callback, _single_step_operation_callback at :270-297 with its one CMD_BLANK_CHECK branch, and _execute_operation at :304-312)
    - firestarter/src/eprom_operations.cpp lines 19-56 (all six entry points; note every one inverts the op-layer return with `!`, so `false` from the op layer means the command reports FINISHED — and eprom_erase:36-39 is the refusal idiom to copy)
    - firestarter/src/firestarter.cpp lines 196-205 (`handle.response_code = RESPONSE_CODE_OK;` set BEFORE the switch — this is why a path that writes nothing reports OK)
    - firestarter/src/proms/eeprom_28c.cpp lines 500-515 (the `handle->response_code = RESPONSE_CODE_ERROR;` store idiom to copy) and firestarter/src/proms/not_implemented.cpp (configure_not_implemented, which sets RESPONSE_CODE_ERROR so op_execute_function already returns false and the op layer is never reached for those protocols)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md sections F-D and F-E (the corrected admission point with its message id, the line-exact mechanism, the complete matrix, and the scoping proof that the guard cannot reach the non-memory commands)
  </read_first>
  <action>
Replace the bare fall-through `return false` in `op_execute_stateful_operation` — the branch taken when
`handle->firestarter_operation_main` is NULL — with the refusal. Three statements, in this order:
`LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED);` then `handle->response_code = RESPONSE_CODE_ERROR;` then
`return false;`. Under option (b) from Task 1, call the `static inline` helper instead and let it do all
three.

`MSG_ERR_NOT_SUPPORTED` is `0xA5`, already in the catalog, already `eprom_erase`'s refusal id. Do **not** add
a new id. A `cmd`-carrying variant would be more diagnosable but would require a new catalog entry, which
D-06 explicitly avoids; if bench diagnosis later proves painful, that is a future decision.

Keep the `return false` semantics: every `eprom_*` caller inverts it, so the command still reports **finished**
and `command_done()` still runs — the chip is disabled and the registers zeroed as before. What changes is
that an error frame is now emitted and `response_code` is `RESPONSE_CODE_ERROR` instead of the
`RESPONSE_CODE_OK` that `loop()` set at `:201`. Say that explicitly in the comment: the command still
terminates cleanly, it just stops lying about what it did.

The comment on this refusal is load-bearing and must record five things:
1. **The mechanism it fixes.** With `main` NULL this function returned `false` at the fall-through, every
   caller inverted it, `loop()` had already set `response_code = RESPONSE_CODE_OK`, and nothing on the path
   wrote it — so the operation reported OK, emitted no error, and emitted no `MSG_MAIN_DONE` either. It
   emitted nothing at all. That is DEVTEST-01's phantom erase.
2. **Why the guard lives here and nowhere else (D-06).** Single site, smallest flash cost, and provably total:
   any protocol whose handler has no arm for a command is refused, present and future, with no per-handler
   maintenance. Name the two rejected alternatives and why — a pre-dispatch protocol check in
   `configure_memory` would put `0x0D`-specific knowledge into the generic dispatcher that v1.20's
   protocol-only rebuild deliberately cleaned, and `default:` arms in all six `configure_*` handlers would
   cost roughly 90 to 130 B against 2992 B of Leonardo headroom while requiring each arm to be hand-written
   not to swallow the pre-set generic mains.
3. **Why this cannot break read, write or verify (D-05's other half).** `configure_memory` pre-sets the
   generic main for `CMD_READ`, `CMD_WRITE` and `CMD_VERIFY` at `memory.cpp:48-58` before the protocol chain
   runs, so those three are never NULL for any protocol that reaches a `configure_*` handler. That is a
   source-level invariant, not a hope, and Task 3 pins it as a positive case. Also record that LOCK-04's
   stated `default:`-arm mechanism was disproven against live source because a blanket arm in
   `configure_eeprom28c` fires for read and verify too, on all 84 `0x0D` chips — mechanism corrected, intent
   satisfied.
4. **The blast radius, and what it does not reach.** The refusal is generic, so it changes observable
   behaviour for every previously-silent command-by-protocol cell — intentionally, per D-07 and D-08. But
   `op_execute_stateful_operation` is reached only from the six `eprom_*` entry points, and
   `hw_read_voltage`, `fw_get_version`, `hw_get_version`, `hw_get_config`, `dt_set_registers` and
   `dt_set_address` never touch the op layer, so `vpp`, `vpe`, `fw`, `config`, `hw`, `dev reg` and
   `dev address` are structurally outside it. Also record that `configure_not_implemented` protocols are
   unchanged: that handler sets `RESPONSE_CODE_ERROR`, so `op_execute_function` already returns false and
   `parse_json` already emits `MSG_ERR_SETUP` — the op layer is never reached, so there is no double error and
   no new frame.
5. **The host-side note.** `firestarter_app/firestarter/eprom_operations.py`'s `_SRAM_PROTO_IDS` workaround
   short-circuits SRAM and FRAM blank-check **before** issuing any firmware command, so this guard is not
   reachable from `check_eprom_blank`. It does **not** become dead code: it fires earlier and produces a
   materially better user-facing message than a bare `0xA5` would. Its comment's claim that the firmware emits
   `0xA4 MSG_ERR_EMPTY_INPUT` is a follow-on artifact of the silent completion, not a firmware refusal. The
   correct Phase 120 disposition is **keep**. Do not touch that file in this phase.

Then rebuild and re-run: `pio run` (3/3 SUCCESS, with the flash delta recorded — a `LOG_ERROR_ID` call plus a
store, so a small attributable increment) and `pio test -e native` plus `pio test -e native_nodevtools`.

**Expect existing cases to move.** Any suite that drives a command whose protocol handler sets no main will now
see an error frame and `RESPONSE_CODE_ERROR` where it previously saw a silent OK. `test_dispatch`,
`test_not_implemented`, `test_val_sram` and `test_val_eeprom28c` are the likely candidates. For each case that
moves: read it, decide whether its old expectation encoded the phantom-success defect, and if so update the
expectation and say so by name in the SUMMARY. **Do not blanket-update expectations.** A case whose old
expectation was correct and now fails is a genuine regression to investigate, not an assertion to adjust —
that distinction is the whole content of D-08's sweep.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'MSG_ERR_NOT_SUPPORTED' src/operation_utils.cpp include/operation_utils.h && pio test -e native 2>&1 | tail -20 && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The NULL-`main` fall-through in `op_execute_stateful_operation` emits `LOG_ERROR_ID(MSG_ERR_NOT_SUPPORTED)`, sets `handle->response_code = RESPONSE_CODE_ERROR` and returns `false`, either inline or through Task 1's option-(b) helper
    - No new catalog id was added; `MSG_ERR_NOT_SUPPORTED` is reused
    - The `return false` semantics are unchanged, so the command still reports finished and `command_done()` still runs; the comment says so explicitly
    - The refusal's comment records all five required items: the phantom-success mechanism; D-06's single-site rationale with both rejected alternatives named and priced; why read, write and verify can never be NULL-main plus LOCK-04's mechanism correction; the blast radius including the six structurally-excluded commands and the unchanged `configure_not_implemented` protocols; and the `_SRAM_PROTO_IDS` keep-disposition for Phase 120
    - No `default:` arm was added to any `configure_*` handler, and `configure_memory` gained no protocol check
    - `pio run` reports SUCCESS on all three AVR envs with the flash delta from Task 1's figures recorded and attributed
    - `pio test -e native` and `pio test -e native_nodevtools` are green, and every case whose expectation changed is listed BY NAME in the SUMMARY with a one-line justification that its old expectation encoded the phantom-success defect
    - No expectation was blanket-updated; any case failing for a reason other than the intended behaviour change is reported as a regression rather than adjusted
    - `firestarter_app/firestarter/eprom_operations.py` is unmodified; `git -C /workspaces/firestarter_app status --short` is clean
  </acceptance_criteria>
  <done>One refusal at one site turns every silent-OK command-by-protocol cell into an explicit `MSG_ERR_NOT_SUPPORTED` with an error response code, with the mechanism, the rationale, the blast radius and the host-side keep-disposition all recorded in source, and every moved test expectation justified by name.</done>
</task>

<task type="auto">
  <name>Task 3: Enumerate the command-by-protocol matrix, pin the never-NULL invariant, and prove the cmd 9/10 dispatch</name>
  <files>firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp, firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp read whole (the make_handle factory, the `_sets_operation` assertion-pair shape at :151-176 asserting both that response_code is not ERROR and that operation_main is not NULL, and the main() RUN_TEST list)
    - firestarter/test/native/avr/test_not_implemented/ (the configure_not_implemented dispatch suite — the case shape for the unchanged-protocols assertion)
    - firestarter/src/proms/memory.cpp lines 42-114 (configure_memory's pre-set mains and the full protocol chain, so the matrix's protocol column is read from the real dispatch order rather than from a document)
    - firestarter/src/proms/{eprom,sram,flash_intel,flash_nor_unlock,flash_5v_page,not_implemented}.cpp (all six configure_* bodies — note configure_sram's body is literally just a debug log, which is why three of its cells are silent-OK today)
    - firestarter/CLAUDE.md, the dispatch-order source-of-truth table (it must match memory.cpp line for line — confirm it still does after this phase)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md section F-E (the complete precomputed matrix with G / H / null cells per protocol group, and the seven-item reading of the blast radius — this is the enumeration to verify, not to rediscover)
  </read_first>
  <action>
Add cases to `test_dispatch/test_configure_memory.cpp`, registering each with `RUN_TEST` in `main()`. Under
Task 1's option (a), also add refusal-frame cases to `test_eeprom28c_sdp.cpp`, which already has the
`captured_frames` chain.

**Case group 1 — LOCK-04's positive safety invariant.** For **every** protocol id that reaches a `configure_*`
handler (walk `configure_memory`'s chain in `memory.cpp:70-113` and cover each group: `0x07`/`0x08`/`0x0B`,
`0x0D`, `0x10`, `0x06`, `0x05`/`0x35`/`0x39`, the SRAM group `0x0E`/`0x27`/`0x28`/`0x29`), assert that
`CMD_READ`, `CMD_WRITE` and `CMD_VERIFY` all leave `firestarter_operation_main` **non-NULL**. This is the case
that would have caught LOCK-04's literal mechanism: a blanket `default:` arm in `configure_eeprom28c` makes
the `0x0D` read and verify rows fail here. Say that in the case's message. Table-drive it so adding a protocol
is one row, not one function.

**Case group 2 — LOCK-04's fail-closed claim.** For every protocol **other than** `0x0D` that reaches a
handler, assert `CMD_SDP_UNLOCK` and `CMD_SDP_LOCK` leave `firestarter_operation_main` NULL — so the op-layer
guard refuses them. Message must state that this is LOCK-04's intent satisfied by D-06's generic guard rather
than by a per-handler `default:` arm, and that the guard is provably total because it needs no per-handler
maintenance.

**Case group 3 — LOCK-02's dispatch half.** For `0x0D`, assert `CMD_SDP_UNLOCK` and `CMD_SDP_LOCK` each leave
`firestarter_operation_main` **non-NULL** and leave both `firestarter_operation_init` and
`firestarter_operation_end` **NULL**. The NULL `init`/`end` is what makes these single-step payload-free
commands. Add a comment recording RESEARCH F-T's correction: NULL `init`/`end` does **not** mean those phases
are skipped — `_execute_operation_house_keeping_func` still calls `op_wait_for_ack()` and still emits the INIT
and END frame pairs — so LOCK-02's true claim is "no data payload and no `DONE` round-trip", which is exactly
what is absent.

**Case group 4 — DEVTEST-01's firmware half and the `0x0D` gaps.** Assert that on `0x0D`, `CMD_ERASE` and
`CMD_CHECK_CHIP_ID` both leave `firestarter_operation_main` NULL, so both are refused by the op-layer guard.
Comment must name DEVTEST-01 explicitly: `dev test` today reports `OP_ERASE` OK having done nothing and
auto-tags a community-fail ladder state even when write and verify pass, and this is the firmware half of that
fix. Note the honest qualification RESEARCH F-E gives for `CMD_CHECK_CHIP_ID`: `eprom_check_chip_id` already
refuses earlier with `MSG_ERR_NO_CHIP_ID` when `handle->chip_id == 0`, and TRACE-05 pinned
`chip_id_check: false` across all 84 `algorithm == 13` entries, so in practice the host never sends a non-zero
chip id for `0x0D` and that cell is already refused upstream. Note it; do not over-claim it.

**Case group 5 — the newly-refused non-`0x0D` cells, enumerated.** Assert NULL `main` for the SRAM group's
`CMD_ERASE`, `CMD_BLANK_CHECK` and `CMD_CHECK_CHIP_ID` — `configure_sram`'s body is literally just a debug
log, which is why these were silent OKs. Comment on the SRAM `CMD_BLANK_CHECK` cell specifically: the host's
`_SRAM_PROTO_IDS` workaround short-circuits it before any firmware command is issued, so this guard is not
reachable from `check_eprom_blank`; the workaround does **not** become dead code, it fires earlier and gives a
better message, and the correct Phase 120 disposition is **keep**. Do not delete it — host surface is Phase 120.

**Case group 6 — the unchanged path.** Assert that a `configure_not_implemented` protocol (use `0x11` or `0`)
still produces `RESPONSE_CODE_ERROR` from `configure_memory` itself, so `op_execute_function` returns false and
the op layer is never reached — no double error, no new frame. This is the case that lets a reviewer see the
absence of a regression rather than infer it.

**Under option (a) only — the refusal frame itself.** In `test_eeprom28c_sdp.cpp`, add a case that builds a
handle whose `main` is NULL, calls the real `op_execute_stateful_operation`, and asserts: the captured frame
ids contain `MSG_ERR_NOT_SUPPORTED` (by generated name, not hex); `handle.response_code` equals
`RESPONSE_CODE_ERROR`; and the function returned `false`. Add a companion case driving `CMD_ERASE` on `0x0D`
end to end through `eprom_erase` and asserting the same three things — that is DEVTEST-01's firmware half
proven at the wiring level rather than at the dispatch level. Under option (b) these two cases are not
possible; record the matrix as a prose table in the SUMMARY instead, and state plainly that the wiring is
proven only by the AVR build compiling.

**Whichever option:** produce the complete command-by-protocol matrix in the SUMMARY as a table, with one row
per protocol group and one column per command, marking each cell as generic-main, handler-main or refused, and
flagging every cell whose behaviour changed from silent-OK to `0xA5`. That table is D-08 item 2's deliverable
and Plan 119-10's `119-NONREGRESSION.md` restates it.

Also assert the cross-family byte-identity D-08 requires: run the full native suite in both envs and confirm
the `0x05`, `0x06`, `0x07`, `0x08`, `0x0B`, `0x10` and SRAM stream suites (`test_val_eprom`,
`test_val_nor_unlock`, `test_val_5v_page`, `test_val_flash_intel`, `test_val_sram`) are all green with no
golden regenerated. Confirm `firestarter/CLAUDE.md`'s dispatch-order table still matches `memory.cpp` line for
line, and `python3 tools/check_dispatch.py` still exits 0 — this phase changes no dispatch **order**, so both
must be unaffected; confirm rather than assume.

Then re-run every host gate at Plan 119-06's baseline. Finally mark **LOCK-04 and LOCK-02 Complete** in
`.planning/REQUIREMENTS.md`. LOCK-04's parenthetical must say **mechanism-corrected, intent-satisfied**, naming
D-05's disproof and D-06's guard, without editing LOCK-04's requirement wording. Leave **DEVTEST-01 Pending** —
its host half is Phase 121 and its mapping amendment is Plan 119-09. Leave LOCK-06 Pending.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -22 && pio test -e native_nodevtools 2>&1 | tail -6 && cd /workspaces/firestarter_app && python3 tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - Case group 1 asserts non-NULL `firestarter_operation_main` for `CMD_READ`, `CMD_WRITE` and `CMD_VERIFY` across every protocol group reaching a handler, table-driven, with a message naming it as the case that would catch LOCK-04's literal mechanism
    - Case group 2 asserts NULL `main` for `CMD_SDP_UNLOCK` and `CMD_SDP_LOCK` on every non-`0x0D` protocol group
    - Case group 3 asserts, for `0x0D`, non-NULL `main` and NULL `init` and NULL `end` for both new commands, with F-T's correction recorded in its comment
    - Case group 4 asserts NULL `main` for `CMD_ERASE` and `CMD_CHECK_CHIP_ID` on `0x0D`, names DEVTEST-01, and records the honest `chip_id == 0` upstream-refusal qualification without over-claiming
    - Case group 5 asserts NULL `main` for the SRAM group's `CMD_ERASE`, `CMD_BLANK_CHECK` and `CMD_CHECK_CHIP_ID`, and records the `_SRAM_PROTO_IDS` keep-disposition for Phase 120
    - Case group 6 asserts a `configure_not_implemented` protocol still yields `RESPONSE_CODE_ERROR` from `configure_memory`, with no double error and no new frame
    - Under option (a): a case asserts `MSG_ERR_NOT_SUPPORTED` in the captured ids plus `RESPONSE_CODE_ERROR` plus a `false` return from the real `op_execute_stateful_operation`, and a companion case proves the same end to end through `eprom_erase` on `0x0D`; ids are referenced by generated name, not hex
    - Under option (b): the SUMMARY states plainly that those two cases are not possible, that the wiring is proven only by the AVR build compiling, and the matrix is recorded as prose
    - The SUMMARY contains the complete command-by-protocol matrix as a table, one row per protocol group, with every changed cell flagged
    - `test_val_eprom`, `test_val_nor_unlock`, `test_val_5v_page`, `test_val_flash_intel` and `test_val_sram` are all green with no golden regenerated
    - `firestarter/CLAUDE.md`'s dispatch-order table still matches `memory.cpp` line for line, and `python3 tools/check_dispatch.py` exits 0
    - `pio test -e native` and `pio test -e native_nodevtools` are both green with the case and suite counts recorded; `pio run` SUCCESS on all three AVR envs
    - Every host gate is at Plan 119-06's recorded baseline
    - `.planning/REQUIREMENTS.md` shows LOCK-04 and LOCK-02 Complete, LOCK-04's parenthetical reading mechanism-corrected and intent-satisfied with LOCK-04's wording unchanged, DEVTEST-01 still Pending, and LOCK-06 still Pending
  </acceptance_criteria>
  <done>The complete command-by-protocol matrix is enumerated and verified with every changed cell flagged, read/write/verify are pinned never-NULL for every protocol, the two new commands are proven configured on `0x0D` and refused everywhere else, DEVTEST-01's firmware half is proven, and the cross-family streams are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an operation's return value to its reported outcome | `false` from the op layer is inverted by every caller into "finished", and `loop()` pre-set OK |
| a generic guard to every protocol family | One change alters observable behaviour for every previously-silent command-by-protocol cell |
| a requirement's stated mechanism to its intent | Implementing LOCK-04 literally would break read and verify on all 84 `0x0D` chips |
| a prose matrix to a proof | Under option (b) the enumeration is reasoning, not a test, and must be labelled as such |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-07-PHANTOM | Repudiation | `op_execute_stateful_operation`'s NULL-`main` fall-through | critical | mitigate | The refusal emits `MSG_ERR_NOT_SUPPORTED` and sets `RESPONSE_CODE_ERROR` at the single site; under option (a) the frame and the response code are asserted by test, and the whole matrix is enumerated |
| T-119-07-READVERIFY | Denial of service | read and verify on all 84 `0x0D` chips | critical | mitigate | LOCK-04's literal `default:`-arm mechanism is prohibited with its disproof recorded; case group 1 pins read, write and verify never-NULL across every protocol group, so the harmful mechanism cannot be reintroduced silently |
| T-119-07-CROSSFAMILY | Tampering | the six other protocol families' bus streams | high | mitigate | Case groups 5 and 6 enumerate the changed cells; the five `test_val_*` stream suites must stay green with no golden regenerated; every moved expectation is justified by name rather than blanket-updated |
| T-119-07-PROSEPROOF | Repudiation | option (b)'s enumeration | high | mitigate | Option (a) attempted first per RESEARCH; if (b) is taken, the SUMMARY and `119-NONREGRESSION.md` must state that the call site is natively unproven and the matrix is prose. Quietly presenting prose as proof is the failure mode |
| T-119-07-SIGABRT | Denial of service | 13 suites that do not mock `millis` and `delay` | medium | mitigate | Diagnosed as an unmocked ArduinoFake virtual, never as the deferred Unity-teardown flake; the four standard mocks added per aborting suite with the load-bearing comment; every touched suite listed |
| T-119-07-HOSTDEAD | Tampering | `_SRAM_PROTO_IDS` | medium | mitigate | RESEARCH F-F2 found it is not dead code. Explicit prohibition on deleting it, with the keep-disposition recorded in source and in the SUMMARY for Phase 120 to act on |
| T-119-07-ENVSKEW | Tampering | the two native envs diverging | medium | mitigate | `build_src_filter` must be identical in both envs; asserted as an acceptance criterion, because a skewed filter silently stops the DEV_TOOLS-invariance proof from covering the same code |
| T-119-07-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; no npm/pip/cargo install step exists anywhere in Phase 119 |
</threat_model>

<verification>
1. Task 1's chosen option is recorded with suite-by-suite evidence; both native envs have identical `build_src_filter`.
2. The refusal exists at the single NULL-`main` site, reuses `0xA5`, and its comment carries all five required records.
3. No `default:` arm was added to any `configure_*` handler; `configure_memory` gained no protocol check.
4. All six case groups pass; under option (a) the refusal-frame and end-to-end `eprom_erase` cases pass too.
5. The complete command-by-protocol matrix appears in the SUMMARY with every changed cell flagged.
6. The five `test_val_*` stream suites are green with no golden regenerated; `check_dispatch.py` exits 0; `CLAUDE.md`'s dispatch table still matches `memory.cpp`.
7. `pio test -e native` and `pio test -e native_nodevtools` both green with counts recorded; `pio run` 3/3 SUCCESS with the flash delta attributed.
8. `firestarter_app` working tree clean; every host gate at Plan 119-06's baseline.
9. `REQUIREMENTS.md` shows LOCK-04 and LOCK-02 Complete with LOCK-04's mechanism-corrected parenthetical; DEVTEST-01 and LOCK-06 still Pending.
10. Commits: one or more inside `firestarter/`, then one meta commit staging the gitlink plus the SUMMARY. Suggested firmware subject: `fix(119-07): refuse a NULL-main operation with MSG_ERR_NOT_SUPPORTED instead of a silent OK (LOCK-04, DEVTEST-01 fw half)`.
</verification>

<success_criteria>
- Open Question 1 is resolved as the first task, with the chosen option and its evidence recorded, and an explicit statement of whether the dependent proofs are tests or prose.
- One refusal at one site closes the phantom-success class for every command and every protocol, reusing an existing error id.
- LOCK-04's intent is satisfied and its mechanism correction is recorded in source, in `REQUIREMENTS.md`'s parenthetical and in the SUMMARY — never as a failed requirement.
- Read, write and verify are pinned never-NULL for every protocol group, so the harmful literal mechanism cannot be reintroduced silently.
- The complete command-by-protocol matrix is enumerated with every changed cell flagged, and the six other families' streams are proven unchanged.
- DEVTEST-01's firmware half is done and recorded, with its row left Pending for Plan 119-09's mapping amendment and Phase 121's host half.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-07-SUMMARY.md` when done.
Record: the chosen option for Open Question 1 with suite-by-suite evidence and any suites whose mocks were
extended; the refusal's exact final code; the complete command-by-protocol matrix table with changed cells
flagged; every pre-existing test case whose expectation moved, by name, with its justification; the
`_SRAM_PROTO_IDS` keep-disposition for Phase 120; the flash delta attributed to this plan; confirmation that
the five `test_val_*` suites are green with no golden regenerated; and that LOCK-04 and LOCK-02 are the only
requirement rows changed, with DEVTEST-01's firmware half done but its row left Pending.
</output>
