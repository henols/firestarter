---
phase: 119-lock-sdp-enable-command-surface-fw-half
plan: 03
type: execute
wave: 3
depends_on: [119-02]
files_modified:
  - firestarter_app/tools/check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py
  - firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h
autonomous: true
requirements: [LOCK-03]

must_haves:
  truths:
    - "D-04: LOCK-03's proof needs BOTH oracles. Plan 119-02 landed the semantic one (the same source text compiling and passing in two build configurations). This plan lands the TEXTUAL one: a firestarter_app source-scan gate asserting the predicate's body contains no DEV_TOOLS conditional and enumerates exactly the expected command set. Different failure modes need different oracles: the two-env run would still pass vacuously if a conditional were inside the predicate, because both branches could be written to agree."
    - "D-02: the predicate carries no preprocessor conditional, AND A GATE ENFORCES THAT. Without the gate, D-02 is a comment. This plan is what makes it a machine-checked fact."
    - "Every gate in this project ships a committed planted-violation fixture proving it actually fails. Exit-code-only assertions are insufficient: pair each with an output-content assertion, and derive any line number from the fixture at test time rather than hardcoding a second literal that a future re-plant could silently desync from. This is the anti-hollow discipline that paid down v1.12's hollow GATE-03 debt, followed again by v1.21 SAFE-03 and Phase 118 D-06."
    - "Structural scan, never a bare grep: brace-match the predicate DEFINITION and scan only its body, and strip comments length- and line-preservingly first. Comment stripping is directly load-bearing here because Plan 119-02's predicate carries a rationale comment that names the DEV_TOOLS conditional it removed."
    - "Fail closed, always naming the fix. An unresolvable input raises rather than passing, and the message tells the maintainer to add the new anchor or name rather than to delete the gate. The FIRESTARTER_*_SRC override seam pointing at a missing file must be an ERROR, never a silent pass."
    - "CORRECTION-4 item 4: this is a NEW firmware-source-scanning gate in firestarter_app, so it joins the mandatory cross-repo gate checklist for Phases 120, 121 and 122. Plan 119-10 adds it to that table."
    - "RESEARCH Open Question 5 resolved: scan include/firestarter.h with a brace-matched extraction of the predicate body, not a dedicated TU. test_revision_constants_parity.py reads the same header only via hardcoded literals, so a structural scan of it is not a conflict, and keeping the predicate in firestarter.h is worth more than the gate's marginal simplicity (F-F consequence 1 forces the header placement anyway)."
    - "Baselines to hold: host pytest 21/21 on the four firmware-scanning modules, check_no_log_in_sdp_window.py exit 0, check_dispatch.py exit 0, ruff clean against the py3.9/3.11 CI targets."
  prohibitions:
    - "MUST NOT reuse check_no_log_in_sdp_window.py's `_func_def_pattern`. It requires the return type to be literally `void`; the predicate is `static inline bool`, so this gate needs its own definition pattern."
    - "MUST NOT modify check_no_log_in_sdp_window.py in this plan. Its emit-anchor append belongs to Plan 119-04, in the same plan as the firmware edit that breaks it."
    - "MUST NOT weaken the gate to a substring grep over the whole header. A DEV_TOOLS conditional elsewhere in firestarter.h is legitimate and must PASS; only one inside the predicate body is a violation. Position, not presence, is the discriminator."
    - "MUST NOT `fix` the planted fixture. Its whole purpose is to stay violating."
    - "MUST NOT touch firestarter_app/firestarter/constants.py, any firmware file, or any generated artifact in this plan."
  artifacts:
    - firestarter_app/tools/check_is_memory_cmd_no_ifdef.py
    - firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py
    - firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h
  key_links:
    - "The gate reads firestarter/include/firestarter.h across the sub-repo boundary via a relative default path plus an env override, exactly as check_no_log_in_sdp_window.py:92-112 reads firestarter/src/proms/eeprom_28c.cpp. That cross-repo read is the whole point: it is the oracle that catches a firmware edit the firmware suite cannot see."
    - "The expected command set the gate asserts is the same eight names Plan 119-02 put in the predicate: CMD_READ, CMD_WRITE, CMD_ERASE, CMD_BLANK_CHECK, CMD_CHECK_CHIP_ID, CMD_VERIFY, CMD_SDP_UNLOCK, CMD_SDP_LOCK. If Phase 120 or later adds a ninth memory command, this gate is the file that must be updated deliberately."
---

<objective>
Ship the textual half of D-04's proof: a fail-closed source-scan gate in `firestarter_app` that reads the
firmware's `is_memory_cmd()` predicate, asserts its body contains no build-configuration conditional and
enumerates exactly the eight expected commands, together with a paired pytest and a committed
planted-violation fixture proving the gate can actually fail.

Purpose: the two-env run Plan 119-02 landed proves the predicate *behaves* identically in both build
configurations. It cannot prove the predicate is *free of* a conditional — a conditional whose two
branches happen to agree would pass it vacuously. D-04 asks for both oracles, and LOCK-03 is not closed
until this one exists.

Output: `tools/check_is_memory_cmd_no_ifdef.py`, `tests/test_check_is_memory_cmd_no_ifdef.py`,
`tests/fixtures/planted_ifdef_in_predicate.h`, and LOCK-03 closed.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-CONTEXT.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-RESEARCH.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md
@.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-02-SUMMARY.md
</context>

## Repo and commit mechanics

All three files land inside the `firestarter_app/` submodule, already on
`v1.22-at28c-software-data-protection-lifecycle`. Confirm with `git branch --show-current` before writing.
Commit inside the submodule, then one meta commit staging the gitlink plus the `.planning/` SUMMARY.
`firestarter/` must be **untouched** by this plan — the firmware predicate already exists from Plan 119-02
and this plan only reads it.

## Artifacts this phase produces

**Created by THIS plan:** `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py`,
`firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py`,
`firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h`, and the
`FIRESTARTER_CMD_ADMISSION_SRC` environment override seam.

**Created earlier:** the three catalog ids (Plan 119-01); `CMD_SDP_UNLOCK` (9), `CMD_SDP_LOCK` (10),
`is_memory_cmd()`, `[env:native_nodevtools]`, its CI step and the `test_cmd_admission` suite (Plan 119-02).

**Created later:** `EEPROM_SDP_ENABLE[3]`, `eeprom28c_emit_sdp_sequence_timed()`,
`eeprom28c_sdp_lock_execute()`, `eeprom28c_sdp_unlock_execute()`, `eprom_sdp_lock()`, `eprom_sdp_unlock()`,
the two `loop()` case arms and the appended `_EMIT_ANCHOR_PATTERNS` entry (Plan 119-04); the
`SDP_FIXED_LOCK_*` goldens and the scripted `micros()` queue (Plan 119-05); the three-way table-identity
guard and the host `test_sdp_table_parity.py` enable-table leg (Plan 119-06); the generic NULL-`main`
refusal and the widened `build_src_filter` (Plan 119-07); the page-load worst-interval tracker (Plan
119-08); the Phase 121 scope amendment (Plan 119-09); `119-NONREGRESSION.md` (Plan 119-10);
`119-MEASUREMENT.md` (Plan 119-11).

## Requirement ownership

This plan **closes LOCK-03**. Plan 119-02 landed the predicate, the guard replacement, the second native
env and the exhaustive two-env truth table; this plan lands D-04's second oracle. After this plan,
`REQUIREMENTS.md`'s LOCK-03 row may be marked Complete — and **only** LOCK-03. LOCK-01, LOCK-02, LOCK-04,
LOCK-05 and LOCK-06 stay Pending; this project has had executors tick multi-plan requirements early four
times in Phase 116.

<tasks>

<task type="auto">
  <name>Task 1: Write the fail-closed is_memory_cmd() source-scan gate</name>
  <files>firestarter_app/tools/check_is_memory_cmd_no_ifdef.py</files>
  <read_first>
    - firestarter_app/tools/check_no_log_in_sdp_window.py read whole (402 lines — the exact structural analog. Note specifically: the env-overridable path constant at :92-112; `_func_def_pattern` at :115-126 and its docstring habit of naming which properties are load-bearing; `_strip_comments`, which blanks `//` and `/* */` spans length- and line-preservingly; `_find_function_body`'s brace matching; the fail-closed `ValueError` at :271-316 whose message names the fix; and the `main()` exit-code plus PASS/FAIL output contract at :355-398)
    - firestarter/include/firestarter.h (the actual predicate this gate reads, as Plan 119-02 left it: the `static inline bool is_memory_cmd(uint8_t cmd)` definition, its rationale comment, and the eight `CMD_*` macros it names)
    - firestarter_app/tools/check_dispatch.py (a second, smaller in-tree example of a cross-repo firmware source scan — read it for its path-resolution and error-reporting conventions)
    - .planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-PATTERNS.md, the section on `check_is_memory_cmd_no_ifdef.py` (the five structural elements to copy, and the note that `_func_def_pattern` cannot be reused because it requires a literal `void` return type)
  </read_first>
  <action>
Create `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py`, copying
`check_no_log_in_sdp_window.py`'s five structural elements and nothing else. Target Python 3.9 syntax —
host CI runs py3.9 and py3.11, and the devcontainer's 3.12 will silently accept syntax CI rejects.

**1. Env-overridable path constant.** Compute `_DEFAULT_CMD_ADMISSION_SRC` from `os.path.dirname(__file__)`
joined with `..`, `..`, `firestarter`, `include`, `firestarter.h` — mirroring `:92-112`'s shape exactly —
then read `FIRESTARTER_CMD_ADMISSION_SRC` from the environment with that as the default. Comment it as the
seam that lets the paired pytest point the checker at a deliberately-violating fixture without editing the
real, clean `firestarter.h`, and state that the seam is **fail-closed**: a path that does not exist is an
error, never a silent pass.

**2. Its own definition pattern.** Do NOT reuse `_func_def_pattern`; it hardcodes a literal `void` return
type. Write a pattern matching the predicate DEFINITION only — `static`, then `inline`, then `bool`, then
the name `is_memory_cmd`, then a parenthesised parameter list, then the body-opening brace — with tolerant
whitespace between tokens. Give it the same docstring habit: state that the trailing brace is what excludes
a `;`-terminated forward declaration, and that the pattern deliberately pins `static inline` because
RESEARCH F-F makes header-inline placement load-bearing (a `.cpp` definition would not link into any
native test binary, so D-04's truth-table suite could not exist).

**3. Comment stripping before the scan.** Reuse the length- and line-preserving `_strip_comments` approach.
This is directly load-bearing here, not defensive hygiene: Plan 119-02's predicate carries a rationale
comment that names the build-configuration conditional it removed, and a naive scan would flag that
comment as the violation. Add a comment saying exactly that, so a future editor does not "simplify" the
stripping away.

**4. Two assertions over the extracted body.** Brace-match the predicate's body from the definition match,
then assert both:
   - **(a) No build-configuration conditional inside the body.** Detect any preprocessor conditional line
     within the body span — `#if`, `#ifdef`, `#ifndef`, `#elif`, `#else`, `#endif` — and report every hit
     with its 1-indexed line number and the directive text. Do not special-case the token `DEV_TOOLS`: any
     conditional inside this body defeats D-02's purpose, and a narrower check would be evadable by
     conditioning on a different macro. State that reasoning in a comment.
   - **(b) The body enumerates exactly the eight expected commands.** Extract every `CMD_[A-Z0-9_]+`
     identifier occurring in the stripped body, deduplicate, and compare as a SET against the frozen
     expected set `{CMD_READ, CMD_WRITE, CMD_ERASE, CMD_BLANK_CHECK, CMD_CHECK_CHIP_ID, CMD_VERIFY,
     CMD_SDP_UNLOCK, CMD_SDP_LOCK}`. Report missing and unexpected names separately and by name. Define
     that expected set as a module-level frozen constant with a comment stating that adding a ninth memory
     command is a deliberate act that must edit this line, and that `CMD_DEV_ADDRESS` and
     `CMD_DEV_REGISTER` must never appear here because they are conditionally defined in the firmware
     header.

**5. Fail-closed `ValueError` and the exit-code / output contract.** If the definition is not found or its
braces do not balance, raise with a message that names the fix — that the predicate was renamed or moved,
so update this checker's definition pattern rather than deleting the gate. In `main()`: a missing source
path prints an `ERROR:` line to stderr and returns 1; a `ValueError` prints `ERROR: {e}` to stderr and
returns 1; violations print a `FAIL:` line to stdout followed by one indented `line N: ...` line per
violation and return 1; success prints a single `PASS:` line naming the resolved path and the predicate's
body line range and returns 0. End with the `if __name__ == "__main__": sys.exit(main())` guard.

The `PASS:` line's exact text is discretion, but it must contain the literal token `PASS:`, the resolved
path, and the body's line range, because the paired pytest's clean-control case asserts on those.

Run the gate against the real header. It must exit 0 and print its `PASS:` line. If it fails, the fault is
either in this gate or in Plan 119-02's predicate — diagnose which before changing either, and never
loosen the gate to make a real violation pass.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_is_memory_cmd_no_ifdef.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py` exists and, run with no arguments from the `firestarter_app` root, exits 0 and prints a line containing `PASS:`, the resolved path to `firestarter/include/firestarter.h`, and the predicate body's line range
    - It reads its source path from `FIRESTARTER_CMD_ADMISSION_SRC` with a relative default, and returns 1 with an `ERROR:` line on stderr when that path does not exist
    - It matches the predicate by a `static inline bool is_memory_cmd(...)` DEFINITION pattern of its own, not by reusing `_func_def_pattern`
    - It strips comments length- and line-preservingly before scanning, and carries a comment stating that the predicate's own rationale comment is why
    - It reports any preprocessor conditional inside the body — not only `DEV_TOOLS`-conditioned ones — with line numbers, and carries a comment explaining why the check is not narrowed to that token
    - It compares the body's `CMD_*` identifiers as a set against a module-level frozen expected set of exactly the eight names, and reports missing and unexpected names separately
    - It raises a `ValueError` naming the fix when the definition is absent or brace-unbalanced, and `main()` converts that to exit code 1 with an `ERROR:` line
    - The file is valid under Python 3.9 syntax, and `ruff check` plus `ruff format --check` pass on it against the py3.9/3.11 CI targets
    - `firestarter/` is unmodified (`git -C /workspaces/firestarter status --short` is clean)
  </acceptance_criteria>
  <done>A fail-closed structural gate reads the production predicate across the sub-repo boundary, asserts both the absence of any build-configuration conditional in its body and the exact eight-command enumeration, and exits 0 with a `PASS:` line against the real header.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the planted-violation fixture and the paired pytest, then re-run the full host gate set</name>
  <files>firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h, firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py</files>
  <read_first>
    - firestarter_app/tests/test_check_no_log_in_sdp_window.py read whole (321 lines — the analog. Note specifically: the docstring case-coverage plan at :22-49; `_run_checker` and `_line_number_of_marker` at :57-85; and the load-bearing anti-hollow case at :112-140 that asserts exit code AND output content AND a line number derived from the fixture at test time)
    - firestarter_app/tests/fixtures/planted_log_in_window.cpp (68 lines — copy its header-comment contract in spirit: the standalone never-compiled shape, the statement that it is not built by platformio.ini, and the explicit "do NOT fix this file" notice with the reason. Note the `// PLANTED VIOLATION` marker string is the contract with `_line_number_of_marker`)
    - firestarter/include/firestarter.h (the real predicate — the fixture must be a minimal standalone copy of its SHAPE, close enough that the gate's definition pattern resolves it, with the violation planted inside the body)
    - firestarter_app/pyproject.toml or setup.cfg (the ruff and pytest configuration, so the new files land conforming; confirm the Python target version the CI lints against)
  </read_first>
  <action>
Create `firestarter_app/tests/fixtures/planted_ifdef_in_predicate.h`. It is a minimal, standalone,
**never-compiled** C++ header containing a copy of the predicate's shape with a build-configuration
conditional planted **inside** the body, on a line carrying the exact marker comment `// PLANTED VIOLATION`.
Keep the eight-command enumeration otherwise correct, so the fixture isolates assertion (a) and does not
also trip assertion (b) — a fixture that fails for two reasons cannot prove which check fired.

Copy `planted_log_in_window.cpp`'s header-comment contract in spirit: state that the file is a
deliberately-violating fixture for `tests/test_check_is_memory_cmd_no_ifdef.py`, that it is never compiled
and is not part of any firmware target, that it exists only so the paired pytest can point the
`FIRESTARTER_CMD_ADMISSION_SRC` override at it and prove the checker exits non-zero, and that "fixing" it
would silently hollow D-04's textual oracle — the anti-hollow gate this project has required since the
v1.12 hollow-GATE-03 debt. **Do NOT fix this file.**

Create `firestarter_app/tests/test_check_is_memory_cmd_no_ifdef.py`, copying the analog's `_run_checker`
subprocess helper (invoking `sys.executable tools/check_is_memory_cmd_no_ifdef.py` with `cwd` set to the
`firestarter_app` root and an env-overrides dict) and its `_line_number_of_marker` helper. Open with a
docstring listing the case-coverage plan, exactly as the analog does at `:22-49`.

Six cases:

1. **Clean control.** Run the checker with no override. Assert exit 0 and `PASS:` in stdout. This is the
   case that goes red if a future firmware edit puts a conditional in the predicate — the whole point of
   the gate.
2. **Committed planted violation — the load-bearing anti-hollow case.** Assert the fixture file exists,
   read it, derive the planted line number with `_line_number_of_marker(text, "PLANTED VIOLATION")`, run
   the checker with `FIRESTARTER_CMD_ADMISSION_SRC` pointed at the fixture, then assert **all** of: exit
   code 1; `FAIL:` in stdout; and `f"line {planted_line}"` in stdout. Exit-code-only would be hollow —
   this project's own dev-test false-green trap is the standing lesson.
3. **Out-of-body control — discriminates by POSITION, not presence.** Write a temp header (use pytest's
   `tmp_path`) containing a legitimate build-configuration conditional OUTSIDE the predicate body — for
   instance around a neighbouring macro definition, mirroring the real `firestarter.h` where the
   `CMD_DEV_*` pair is conditionally defined a few lines above. Assert exit 0 and `PASS:`. Without this
   case the gate could be a whole-file grep and nobody would know.
4. **Comment-not-a-violation control.** Write a temp header whose predicate body is clean but whose
   rationale comment inside the body mentions the conditional by name. Assert exit 0 and `PASS:`. This
   pins `_strip_comments` as load-bearing, and it reflects the real predicate, whose comment does exactly
   this.
5. **Wrong command set.** Write a temp header with a conditional-free body that omits one expected command
   or names an extra one. Assert exit code 1 and that the offending name appears in stdout. This proves
   assertion (b) is real and independent of assertion (a).
6. **Fail-closed inputs.** Two sub-assertions: `FIRESTARTER_CMD_ADMISSION_SRC` pointed at a nonexistent
   path returns 1 with `ERROR:` on stderr; and a temp header with no `is_memory_cmd` definition at all
   returns 1 with `ERROR:` on stderr and a message naming the fix. Neither may pass.

Then re-run the whole host gate set, because this plan adds a module to it:
`python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py -q` (expect 6 passed), then
`python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py -q`
(baseline 21 passed — this plan touches no firmware source, so the baseline must hold exactly), then
`python3 tools/check_no_log_in_sdp_window.py` (exit 0), `python3 tools/check_dispatch.py` (exit 0), and
`python3 tools/check_is_memory_cmd_no_ifdef.py` (exit 0). Then `ruff check` and `ruff format --check`
against the py3.9/3.11 CI targets, recording the version validated — never the devcontainer's 3.12.

Two host modules are known-RED and are NOT this plan's damage: `tests/test_audit_coverage_matrix.py` (stale
golden) and `tests/test_no_programmer_found_*` (goes red with a live board attached, and three boards are
attached right now). Name them if they surface; do not chase them.

Finally, mark **LOCK-03 Complete** in `.planning/REQUIREMENTS.md`, with a parenthetical naming both oracles:
the two-env truth table from Plan 119-02 and this gate with its planted-violation fixture. Change no other
requirement row. Re-read the LOCK-01, LOCK-02, LOCK-04, LOCK-05 and LOCK-06 rows and confirm they still
read Pending rather than re-deriving them; if any does not, report it as a defect rather than fixing it here.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py -q && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_dispatch.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/planted_ifdef_in_predicate.h` exists, contains a build-configuration conditional inside the predicate body on a line carrying the literal marker `// PLANTED VIOLATION`, keeps the eight-command enumeration otherwise correct, and carries a header comment with an explicit do-not-fix notice and its reason
    - `tests/test_check_is_memory_cmd_no_ifdef.py` contains all six cases described above and `python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py -q` reports 6 passed
    - The planted-violation case asserts exit code 1 AND `FAIL:` in stdout AND a line number derived from the fixture at test time via `_line_number_of_marker` (no hardcoded second line-number literal)
    - The out-of-body control and the comment-only control both assert exit 0 and `PASS:`, proving the gate discriminates by position and that comment stripping is load-bearing
    - The wrong-command-set case exits 1 and names the offending command in stdout, proving assertion (b) is independent of assertion (a)
    - Both fail-closed sub-assertions (missing path, absent definition) exit 1 with `ERROR:` on stderr
    - `python3 tools/check_is_memory_cmd_no_ifdef.py`, `python3 tools/check_no_log_in_sdp_window.py` and `python3 tools/check_dispatch.py` all exit 0
    - `python3 -m pytest tests/test_sdp_table_parity.py tests/test_check_no_log_in_sdp_window.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py -q` reports 21 passed
    - `ruff check` and `ruff format --check` pass against the py3.9/3.11 CI targets, with the version recorded
    - `.planning/REQUIREMENTS.md` shows LOCK-03 Complete with a parenthetical naming both oracles, and LOCK-01, LOCK-02, LOCK-04, LOCK-05, LOCK-06 all still Pending
    - `git -C /workspaces/firestarter status --short` is clean — no firmware file was edited
  </acceptance_criteria>
  <done>The gate is provably non-hollow: a committed fixture makes it exit 1 with the planted line named, three controls prove it discriminates by position and ignores comments, two fail-closed inputs prove it never passes on an unresolvable source, and LOCK-03 is Complete with both of D-04's oracles in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware source text to host gate | The gate resolves firmware source by name and position; a rename must become an error, not a silent pass |
| a gate to its own credibility | A gate that cannot fail is worse than no gate, because it is read as evidence |
| the `FIRESTARTER_CMD_ADMISSION_SRC` seam | A test-only override that must never let production scanning be skipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-119-03-HOLLOW | Repudiation | the new gate's credibility | critical | mitigate | A committed planted-violation fixture plus a case asserting exit code, `FAIL:` output, and the fixture-derived line number; three positive controls; two fail-closed input cases. All are acceptance criteria |
| T-119-03-VACUOUS | Tampering | a conditional inside the predicate escaping both oracles | high | mitigate | This gate is the oracle the two-env run cannot be: it detects the conditional's presence, not its behaviour. The check covers every preprocessor conditional, not just `DEV_TOOLS`-conditioned ones, so it cannot be evaded by conditioning on another macro |
| T-119-03-RENAME | Tampering (of controls) | the predicate being renamed or moved | high | mitigate | Fail-closed `ValueError` naming the fix, exercised by case 6's definition-absent sub-assertion; the gate joins the CORRECTION-4 checklist in Plan 119-10 so Phases 120-122 re-check it |
| T-119-03-FALSEPOS | Denial of service | a legitimate conditional elsewhere in the header | medium | mitigate | Brace-matched body extraction plus the out-of-body control case; the real header conditionally defines `CMD_DEV_*` a few lines above the predicate, so a whole-file grep would be permanently red |
| T-119-03-SEAM | Elevation of privilege | the env override | medium | mitigate | The seam is fail-closed: a nonexistent path is an `ERROR:` and exit 1, asserted by case 6. It can redirect the scan but never disable it |
| T-119-03-PYVER | Tampering | devcontainer Python 3.12 masking CI py3.9 | medium | mitigate | Target py3.9 syntax explicitly and validate ruff against the py3.9/3.11 CI targets, recording the version — never the devcontainer's 3.12 |
| T-119-03-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. If the host test env needs rebuilding, `pip install -e '.[test]'` reinstalls only already-pinned in-tree dev extras; no new package enters the dependency set |
</threat_model>

<verification>
1. `python3 tools/check_is_memory_cmd_no_ifdef.py` exits 0 against the real `firestarter/include/firestarter.h` and prints a `PASS:` line naming the path and body line range.
2. `python3 -m pytest tests/test_check_is_memory_cmd_no_ifdef.py -q` reports 6 passed, including the committed-fixture case asserting exit code, `FAIL:` and the derived line number.
3. The four pre-existing firmware-scanning modules still report 21 passed; `check_no_log_in_sdp_window.py` and `check_dispatch.py` still exit 0.
4. `ruff check` and `ruff format --check` pass against the py3.9/3.11 CI targets; the validated version is recorded.
5. `git -C /workspaces/firestarter status --short` is clean.
6. `REQUIREMENTS.md` shows LOCK-03 Complete and the other five LOCK rows Pending.
7. Commits: one inside `firestarter_app/`, then one meta commit staging the gitlink plus the SUMMARY. Suggested host subject: `test(119-03): add the is_memory_cmd() no-conditional source gate with a planted-violation fixture (LOCK-03)`.
</verification>

<success_criteria>
- A fail-closed structural gate reads the firmware predicate across the sub-repo boundary and asserts both the absence of any build-configuration conditional in its body and the exact eight-command enumeration.
- The gate is provably non-hollow: the committed fixture makes it exit 1 with the planted line named, and three controls prove it discriminates by position rather than presence.
- The two fail-closed inputs never pass.
- Every pre-existing host gate is at its measured baseline; ruff is validated against the CI Python targets.
- LOCK-03 is Complete with both of D-04's oracles in place, and no other requirement row moved.
</success_criteria>

<output>
Create `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-03-SUMMARY.md` when done.
Record: the gate's `PASS:` line verbatim; the six pytest case names and their outcomes; the planted line
number the fixture-derived assertion resolved to; the ruff target version validated; the two known-RED host
modules named as pre-existing; the fact that the new gate must be added to the CORRECTION-4 checklist in
Plan 119-10; and confirmation that LOCK-03 is the only requirement row this plan changed.
</output>
