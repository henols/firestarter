---
phase: 121-dev-test-fix-gates-docs-redesign
plan: 07
subsystem: testing
tags: [diagnostic-report, schema-versioning, dedup-fingerprint, community-validation, backward-compat, pytest]

# Dependency graph
requires:
  - phase: 121-dev-test-fix-gates-docs-redesign (plan 06)
    provides: "OP_WRITE_PARTIAL = \"write-partial\" op string registered in _DESTRUCTIVE_OPS and _MULTI_RUN_OPS"
provides:
  - "SCHEMA_VERSION bumped 1.1 -> 1.2 with in-source rationale (no consumer breakage)"
  - "Proof (not argument) that partial vs full write runs differentiate in dedup_fingerprint"
  - "Proof that a partial run still auto-tags community-reported via unchanged build_db_diff"
  - "Proof that a partial run can never cross-agree with a full run toward N>=2 promotion"
  - "Frozen literal 3.0.0b11 report body fixture proving back-compat parsing/grouping"
affects: ["121-08", "121-09", "121-14"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Deliberate-break TDD proof: temporarily strip a hash input, confirm RED, restore, confirm GREEN, assert clean git status before committing"
    - "Frozen literal fixture (never regenerated from live code) as the back-compat pinning mechanism for a wire/report schema"

key-files:
  created: []
  modified:
    - firestarter_app/firestarter/diagnostic_report.py
    - firestarter_app/tests/test_diagnostic_report.py
    - firestarter_app/tests/test_parse_devtest_issue.py

key-decisions:
  - "diagnostic_report.py needed zero renderer/to_dict edits (RESEARCH C-1 confirmed): only SCHEMA_VERSION and two comment blocks changed"
  - "Cross-agreement proof built saved bodies through the REAL pipeline (sanitize_dict -> build_body) rather than hand-rolled JSON, per plan instruction"
  - "b11 back-compat fixture is a hand-written literal JSON string, never generated by calling to_dict(), so it stays pinned even if the module changes later"

patterns-established:
  - "GRAD-01 no-auto-graduate lock holds through the fingerprint hash, not the ladder_state tag -- documented in-source and proven by a deliberate-break test"

requirements-completed: []  # DEVTEST-04 contributes only; closed by plan 121-09 per requirement_ownership lock

coverage:
  - id: D1
    description: "SCHEMA_VERSION bumped 1.1 -> 1.2, diff confined to the constant and comment blocks (no function body touched)"
    verification:
      - kind: unit
        ref: "tests/test_diagnostic_report.py, tests/test_parse_devtest_issue.py (full suite green after bump)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Partial and full write runs produce different dedup_fingerprint values; identical partial runs produce the same fingerprint"
    verification:
      - kind: unit
        ref: "tests/test_diagnostic_report.py#test_fingerprint_differs_for_partial_versus_full_write"
        status: pass
      - kind: unit
        ref: "tests/test_diagnostic_report.py#test_fingerprint_is_stable_for_identical_partial_runs"
        status: pass
    human_judgment: false
  - id: D3
    description: "A partial run still auto-tags ladder_state=community-reported via unmodified build_db_diff; community-confirmed is never produced"
    verification:
      - kind: unit
        ref: "tests/test_diagnostic_report.py#test_partial_run_still_tags_community_reported"
        status: pass
    human_judgment: false
  - id: D4
    description: "A partial run and a full run of the same chip never cross-agree in count_agreeing (GRAD-01 lock holds through the fingerprint), proven via the real sanitize_dict/build_body pipeline plus a deliberate-break RED/GREEN cycle"
    verification:
      - kind: unit
        ref: "tests/test_diagnostic_report.py#test_partial_and_full_runs_never_cross_agree"
        status: pass
    human_judgment: false
  - id: D5
    description: "A frozen literal 3.0.0b11 report body (schema_version 1.1, six original op strings, no write-partial) still parses and still groups with a current-schema body sharing its fingerprint; mixed fingerprints group independently of schema_version"
    verification:
      - kind: unit
        ref: "tests/test_parse_devtest_issue.py#test_legacy_vocabulary_b11_body_still_parses"
        status: pass
      - kind: unit
        ref: "tests/test_parse_devtest_issue.py#test_legacy_vocabulary_b11_body_still_groups"
        status: pass
      - kind: unit
        ref: "tests/test_parse_devtest_issue.py#test_mixed_schema_versions_group_independently"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-07-29
status: complete
---

# Phase 121 Plan 07: Report-side D-06/D-08 fingerprint proof + b11 back-compat Summary

**Bumped `diagnostic_report.SCHEMA_VERSION` to 1.2 for the seventh `write-partial` op string and turned the three D-06/D-08 safety claims — differentiation, the free ladder tag, and cross-agreement impossibility — from arguments into passing tests, including a deliberate-break RED/GREEN proof and a frozen literal `3.0.0b11` back-compat fixture.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-07-29T18:40:00Z (approx)
- **Completed:** 2026-07-29T18:51:00Z
- **Tasks:** 3
- **Files modified:** 3 (`diagnostic_report.py`, `test_diagnostic_report.py`, `test_parse_devtest_issue.py`)

## Accomplishments

- `SCHEMA_VERSION` bumped `"1.1"` -> `"1.2"`; the diff is confined to the constant line and two comment blocks (verified via `git diff` — no function body changed), matching RESEARCH C-1's prediction that the renderer/`to_dict` need zero edits.
- Added an in-source comment above `dedup_fingerprint` recording the two properties Phase 121 relies on: op-name-driven differentiation, and that the GRAD-01 no-auto-graduate lock (Phase 114) holds **through the fingerprint**, not through the `ladder_state` tag.
- Four new tests in `test_diagnostic_report.py` prove: (1) a partial vs full write of the same chip differ in fingerprint, (2) two identical partial runs share a fingerprint (proving the differentiation is signal, not noise), (3) `build_db_diff` (unmodified) tags a partial run `community-reported` exactly like a full run, and never emits `community-confirmed`, and (4) `count_agreeing`, fed real saved-body artifacts built through `sanitize_dict`/`build_body`, groups a partial and a full run into two separate singleton groups — never one group of two.
- Executed the plan-mandated deliberate-break proof: temporarily dropped `result.op` from `dedup_fingerprint`'s per-step string, confirmed `test_fingerprint_differs_for_partial_versus_full_write` and `test_partial_and_full_runs_never_cross_agree` both went RED, then restored the line and confirmed GREEN with a clean `git status --porcelain` on `diagnostic_report.py`.
- Three new tests in `test_parse_devtest_issue.py` pin a frozen, hand-written `3.0.0b11`-shaped report body (`schema_version` `"1.1"`, six original op strings, no `write-partial`) and prove it still parses, still groups with a current-schema body sharing its fingerprint, and that mixed fingerprints group independently of `schema_version` — `tools/parse_devtest_issue.py` needed zero code change (verified via `git status --porcelain` empty on that file).
- Extended `test_parse_devtest_issue.py`'s module docstring taxonomy with a new `LEGACY` category following the file's existing convention.

## Task Commits

1. **Task 1: Bump SCHEMA_VERSION and record why the renderer needed nothing** - `1377056` (feat)
2. **Task 2: Prove differentiation, the free ladder tag, and cross-agreement impossibility** - `e2721ce` (test)
3. **Task 3: Prove b11 six-string report bodies still parse and still group** - `053aa86` (test)

_All three commits are in the `firestarter_app` sub-repo on branch `v1.22-at28c-software-data-protection-lifecycle`._

**Meta-repo commit (this file):** see final commit below.

## Files Created/Modified

- `firestarter_app/firestarter/diagnostic_report.py` - `SCHEMA_VERSION` 1.1 -> 1.2 plus two comment blocks; no function body changed
- `firestarter_app/tests/test_diagnostic_report.py` - 4 new tests proving D-06/D-08's differentiation/ladder/cross-agreement claims
- `firestarter_app/tests/test_parse_devtest_issue.py` - 3 new tests + frozen b11 fixture proving back-compat; docstring taxonomy extended; 1 pre-existing test's hardcoded `schema_version` assertion updated (see Deviations)

## Decisions Made

- Confirmed RESEARCH C-1's correction: `diagnostic_report.py` needed **zero** renderer/`to_dict` edits for the schema bump — only the constant and two comment blocks changed.
- Built the cross-agreement proof through the real production pipeline (`sanitize_dict(report.to_dict())` -> `build_body` -> `count_agreeing`) rather than a hand-rolled JSON blob, per the plan's explicit instruction, so the test exercises the exact artifact shape a community tester's issue actually carries.
- Chose the explicit-literal-fixture approach for the b11 back-compat proof (plan left this to discretion): a hand-written, never-regenerated JSON string pins the pre-`write-partial` shape independent of any future code change to `to_dict()`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale hardcoded `schema_version == "1.1"` assertion**
- **Found during:** Task 1 verification (`pytest tests/test_diagnostic_report.py tests/test_parse_devtest_issue.py -q`)
- **Issue:** `test_detect_realistic_dev_test_body_parses` in `tests/test_parse_devtest_issue.py` asserted `obj["schema_version"] == "1.1"` — a value baked in when that test's helper (`_build_realistic_title_body`) built a report through the CURRENT `to_dict()`, which now reads the bumped `SCHEMA_VERSION`. This is exactly the "snapshot carrying the old version string" the Task 1 acceptance criteria anticipated needing regeneration.
- **Fix:** Updated the assertion to `"1.2"` with a comment explaining the split: this test reflects the CURRENT schema version because it builds via current builders, while the b11 back-compat proof lives in the separate frozen-fixture LEGACY section added in Task 3.
- **Files modified:** `firestarter_app/tests/test_parse_devtest_issue.py`
- **Verification:** Full suite green (`pytest tests/ -p no:cacheprovider -q`, 0 failed) both immediately after this fix and again after Tasks 2/3.
- **Committed in:** `1377056` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** Necessary to keep the suite green after the version bump; no scope creep — the fix is scoped to a single hardcoded assertion in a test the plan already flagged as a likely regeneration point.

## Issues Encountered

- `ruff format` reformatted two multi-import/long-line statements added by this plan (`test_diagnostic_report.py`'s new multi-symbol import, `test_parse_devtest_issue.py`'s `current_body = _B11_BODY.replace(...)` line) — cosmetic, auto-applied via `ruff format`, no logic change.
- `/tmp/venv311/bin/ruff` referenced by the plan's acceptance criteria does not exist in this session's container; used the container's globally-installed `ruff` (0.16.0) instead. `ruff check` and `ruff format --check` both pass on `firestarter/` and `tests/`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The D-06/D-08 safety properties this plan depends on (differentiation, free ladder tag, cross-agreement impossibility, b11 back-compat) are now proven by test, not just argued — plans 121-09 (which closes DEVTEST-04) and 121-14 (nonregression close) can cite these tests directly.
- `chip_test.py` was intentionally NOT touched (owned this wave by plan 121-08); no conflict.
- Suite left at 1089 passed, 0 failed (1082 baseline + 7 new tests), coverage 81.91% (>= 70% gate).

## Self-Check: PASSED

- FOUND: `/workspaces/firestarter_app/firestarter/diagnostic_report.py`
- FOUND: `/workspaces/firestarter_app/tests/test_diagnostic_report.py`
- FOUND: `/workspaces/firestarter_app/tests/test_parse_devtest_issue.py`
- FOUND commit: `1377056`
- FOUND commit: `e2721ce`
- FOUND commit: `053aa86`

---
*Phase: 121-dev-test-fix-gates-docs-redesign*
*Completed: 2026-07-29*
