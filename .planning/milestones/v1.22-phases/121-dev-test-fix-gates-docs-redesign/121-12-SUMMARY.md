---
phase: 121-dev-test-fix-gates-docs-redesign
plan: 12
subsystem: firmware-host-message-catalog
tags: [codegen, catalog, sdp, honesty-caveat, cross-repo-gates, native-tests]

# Dependency graph
requires:
  - phase: 121 (plans 10, 11)
    provides: warn-and-proceed on --skip-erase for 0x0D (D-13), dedup-before-ask submission flow (DEVTEST-05/06)
  - phase: 120 (plan 10)
    provides: D-15's host-side symmetric summary line (F-120-03), the asymmetry this plan closes at the catalog
  - phase: 118 (plan 02)
    provides: MSG_INFO_SDP_LOCK_DONE_US (0x61) with the original protection-state-is-not-readable caveat
provides:
  - MSG_INFO_SDP_UNLOCK_DONE_US (0x5F) format string carries the same protection-state-is-not-readable caveat as its 0x61 sibling, making the two directions symmetric
  - Three-way messages.toml byte-identity re-proven after the edit, with sync idempotence proven by a second run
  - Cross-repo firmware-source-scanning gates re-run in both directions post-edit, all green with confirmed-existing target paths
affects: [121-13, 121-14, 122]

# Tech tracking
tech-stack:
  added: []
  patterns: ["canonical-catalog-edit-then-sync-script regeneration (never hand-edit generated artifacts)"]

key-files:
  created: []
  modified:
    - tools/catalog/messages.toml (meta, canonical)
    - firestarter/tools/catalog/messages.toml (mirror, regenerated by sync script)
    - firestarter_app/tools/catalog/messages.toml (mirror, regenerated by sync script)
    - firestarter_app/firestarter/messages.py (generated artifact, regenerated by sync script)
    - firestarter/include/messages.h (generated artifact; byte-identical after regen, id defines only)

key-decisions:
  - "D-15's mechanism corrected per RESEARCH C-7: edited the meta-repo canonical copy only, then ran bash tools/catalog/sync_to_subrepos.sh to regenerate both mirrors and both codegen artifacts — never hand-copied or hand-normalised any generated file."
  - "firestarter/include/messages.h carries only numeric id #defines, not format strings (format text is decoded host-side in messages.py), so the C++ header ends up byte-identical to its pre-plan committed state — zero reflow risk, zero lines changed, confirmed by git diff --exit-code."
  - "No gate repair was needed in Task 2 — the change renames/deletes nothing, so all four host source-scanning gates and both native test environments stayed green at their pre-plan baselines (141/141), consistent with the plan's own prediction."

requirements-completed: []

coverage:
  - id: D1
    description: "MSG_INFO_SDP_UNLOCK_DONE_US (0x5F) format string gains the protection-state-is-not-readable caveat, symmetric with MSG_INFO_SDP_LOCK_DONE_US (0x61)"
    verification:
      - kind: unit
        ref: "pio test -e native (test/native/avr/test_messages, test/native/avr/test_eeprom28c_sdp) — 141/141"
        status: pass
      - kind: unit
        ref: "pio test -e native_nodevtools — 141/141"
        status: pass
      - kind: other
        ref: "cmp tools/catalog/messages.toml firestarter/tools/catalog/messages.toml && cmp tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml (three-way byte identity, md5 8c9f79af841537310e2db197decc62b2)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Sync is idempotent and both sub-repo drift gates (codegen --check + regenerate against each sub-repo's own catalog) stay clean"
    verification:
      - kind: other
        ref: "bash tools/catalog/sync_to_subrepos.sh run twice, git status --porcelain identical in all three repos between runs"
        status: pass
      - kind: other
        ref: "python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check (both sub-repos, exit 0)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Cross-repo host gates that scan firmware source stay green after the firmware-side regeneration, in both directions"
    verification:
      - kind: unit
        ref: "python3 tools/check_no_log_in_sdp_window.py, check_is_memory_cmd_no_ifdef.py, check_dispatch.py, check_no_community_support_status_write.py — all PASS, exit 0"
        status: pass
      - kind: unit
        ref: "python3 -m pytest tests/ -p no:cacheprovider -q (firestarter_app) — 0 failed"
        status: pass
      - kind: other
        ref: "ruff check firestarter/ (firestarter_app) — All checks passed"
        status: pass
    human_judgment: false

# Metrics
duration: 35min
completed: 2026-07-29
status: complete
---

# Phase 121 Plan 12: SDP Unlock-Done Message Symmetry Summary

**Closed the honesty asymmetry between the SDP lock and unlock completion messages by editing only the canonical catalog and regenerating both sub-repo mirrors — firmware footprint is one message string, zero code-behaviour change.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-07-29T21:13:00Z
- **Completed:** 2026-07-29T21:48:48Z
- **Tasks:** 2 (2 complete)
- **Files modified:** 5 (1 canonical catalog + 2 mirror copies + 2 regenerated artifacts, 1 of which ended up byte-identical)

## Accomplishments

- `MSG_INFO_SDP_UNLOCK_DONE_US` (`0x5F`) format string changed from `"SDP unlock emitted in %lu us"` to `"SDP unlock sequence emitted in %lu us; protection state is not readable"`, symmetric with `MSG_INFO_SDP_LOCK_DONE_US`'s (`0x61`) existing `"SDP lock sequence emitted in %lu us; protection state is not readable"`.
- Added a comment above the `0x5F` entry mirroring the `0x61` entry's convention: response_code is never written on the SDP path (117 D-05 / 118 D-02); Phase 120 F-120-03 found the asymmetry and answered it host-side only (120 D-10); Phase 121 D-15 closes it at the catalog.
- Ran `bash tools/catalog/sync_to_subrepos.sh` to copy the catalog + codegen.py into both sub-repos and regenerate `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py` — never hand-edited either generated artifact.
- Re-ran the sync script a second time and confirmed `git status --porcelain` was byte-identical before and after in all three repos (idempotence proven).
- Re-ran `codegen.py --check` and a fresh regeneration against each sub-repo's own local catalog copy independently of the sync script, confirming no additional drift.
- Ran all four host cross-repo firmware-source-scanning gates (`check_no_log_in_sdp_window.py`, `check_is_memory_cmd_no_ifdef.py`, `check_dispatch.py`, `check_no_community_support_status_write.py`) — all PASS, both scanned firmware paths confirmed to exist.
- Ran both firmware native test environments (`native`, `native_nodevtools`) — both 141/141, matching the researched baseline exactly.
- Ran `pio run -e uno` — SUCCESS, Flash 23932/32256 bytes (unchanged from the pre-plan figure, since the format-string change never touches the C++ header).
- Ran the full `firestarter_app` pytest suite (0 failed) and `ruff check firestarter/` (all checks passed) against the regenerated `messages.py`.

## Task Commits

Each task was committed atomically, per repo:

1. **Task 1: Correct the canonical catalog entry and regenerate both mirrors**
   - `8cbdab9` (meta) — `feat(121-12): mirror the protection-state caveat onto the SDP unlock-done message (D-15)` — `tools/catalog/messages.toml`
   - `8c2e177` (firestarter) — same message — `tools/catalog/messages.toml`
   - `38e597f` (firestarter_app) — same message — `tools/catalog/messages.toml`, `firestarter/messages.py`
2. **Task 2: Run the cross-repo gate check in both directions and the firmware native suites** — verification-only task, no gate repair was needed, so no commit was made (as permitted by the plan).

**Plan metadata:** committed separately per `<final_commit>` protocol below.

_Note: `firestarter/include/messages.h` was regenerated but produced zero diff — the C++ header only carries numeric id `#define`s, not format text, so the format-string change is invisible there. This is expected, not a defect._

## Files Created/Modified

- `tools/catalog/messages.toml` (meta) — canonical `0x5F` entry: format string + new comment
- `firestarter/tools/catalog/messages.toml` — mirror, script-regenerated, byte-identical to the meta copy
- `firestarter_app/tools/catalog/messages.toml` — mirror, script-regenerated, byte-identical to the meta copy
- `firestarter_app/firestarter/messages.py` — regenerated, one field changed (`format=...`), raw codegen output, no hand-normalisation
- `firestarter/include/messages.h` — regenerated, byte-identical to its pre-plan committed state (id defines only)

## Decisions Made

- Followed RESEARCH C-7's corrected mechanism exactly: edit the meta copy, run the sync script, never touch a sub-repo copy or a generated artifact by hand.
- Recorded in a source comment (matching the `0x61` entry's convention) why the caveat lives in the message text rather than a status code, and the Phase 120→121 provenance of the fix, so a future reader does not need to re-derive it from CONTEXT.md.
- No architectural changes were needed or made; this is a pure data (catalog string) correction.

## Deviations from Plan

None - plan executed exactly as written. Both tasks completed with zero Rule 1-4 deviations; the firmware header's zero-diff outcome was anticipated by the plan's own "reflow risk is low" framing and confirmed, not a surprise requiring a fix.

## Issues Encountered

None. The devcontainer's referenced `/tmp/venv311/bin/ruff` path from the plan's acceptance criteria did not exist in this environment; the globally-installed `ruff` (0.16.0, on `PATH`) was used instead and produced the same "All checks passed" result against the regenerated module. Recorded honestly rather than silently substituted.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- GATE-02 is still open (contributes only from this plan) — Plan 121-13 lands the documentation set that closes it; Plan 121-14 re-verifies every REQUIREMENTS.md row. Neither `.planning/REQUIREMENTS.md` nor any requirement checkbox was touched by this plan, per its ownership scope.
- The catalog-sync CI workflow (`.github/workflows/catalog-sync-check.yml`) remains expected-red-until-milestone-merge, unchanged by this plan — it checks out both sub-repos at their default branch (`ref: main`), a Phase 118-established pattern. The real in-phase proof is the local three-way `cmp` (md5 `8c9f79af841537310e2db197decc62b2` across all three `messages.toml` copies) plus both sub-repos' independent `codegen.py --check` gates, both recorded above.
- Both sub-repo working trees are clean modulo the pre-existing, explicitly out-of-scope dirt named in the dispatch prompt (`firestarter_app/.gitignore`, `.coverage`, `SECURITY.md`, `doc/lockable-proms.md`, `write_test_port.sh`, `.planning/config.json`, and `firestarter/firestarter/`) — none of it was touched, added, or committed by this plan.
- No submodule gitlink was staged in the meta repo at any commit in this plan (verified via `git status --porcelain` before each meta-repo `git add`).

---
*Phase: 121-dev-test-fix-gates-docs-redesign*
*Completed: 2026-07-29*

## Self-Check: PASSED

- FOUND: `tools/catalog/messages.toml`
- FOUND: `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-12-SUMMARY.md`
- FOUND: `firestarter/include/messages.h`
- FOUND: `firestarter_app/firestarter/messages.py`
- FOUND: meta commit `8cbdab9`
- FOUND: meta commit `dbdf3c6`
- FOUND: firestarter commit `8c2e177`
- FOUND: firestarter_app commit `38e597f`
