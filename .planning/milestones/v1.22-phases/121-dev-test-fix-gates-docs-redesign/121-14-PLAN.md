---
phase: 121-dev-test-fix-gates-docs-redesign
plan: 14
type: execute
wave: 10
depends_on: ["121-13"]
files_modified:
  - .planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "GATE-03: the full non-regression set is green at the phase's FINAL commit, re-run verbatim rather than trusted from any prior plan's SUMMARY."
    - "GATE-03 / RESEARCH Pitfall 3: the py3.9 clause is satisfied by ruff and mypy at their config-pinned py39 target plus the packaging classifier, and the impossibility of a py3.9 pytest run is RECORDED with its cause rather than quietly skipped."
    - "The ruff verdict is reported against the version CI actually resolves, not the devcontainer's — and the resolved version is named alongside the verdict."
    - "RESEARCH C-2: the second audit-matrix regeneration is expected to be a NO-OP. A non-empty diff here is a finding to investigate, not a routine refresh."
    - "`diff_db.py` identity means 'still exactly 2 explained changes, 0 new, 0 removed' — not zero — and the record says so, so no future reader misreads the gate."
    - "Requirement closure is re-verified row by row against the live tree; nothing is ticked by this plan except GATE-03."
  artifacts:
    - .planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md
  key_links:
    - "The nine-row cross-repo gate table from `119-NONREGRESSION.md` §CORRECTION-4 was explicitly handed to Phases 120-122 and is mandatory here; this phase touched firmware, so the rename-risk row runs in both directions."
    - "Both sub-repo working trees must be proven clean with porcelain status, never with a path-scoped diff, which passes vacuously on a path nothing touched."
---

<objective>
Close GATE-03: re-run the full non-regression set at the phase's final commit, record it, and re-verify every requirement row.

Purpose: this is the plan that makes every earlier plan's claim checkable by someone who was not there. Phase 120's capstone established the discipline — re-run every row verbatim at the final commit rather than trusting any prior SUMMARY, name the rows that changed by design, and record the known-and-explained conditions rather than smoothing them over. This phase adds two obligations of its own: the ruff verdict must be reported against the version CI resolves (the devcontainer's is a different version and disagrees), and the second audit-matrix regeneration must be proven a no-op, because if it is not, something touched the database that nothing in this phase should have.

Output: `121-NONREGRESSION.md` in the established eight-section shape, and a re-verified `REQUIREMENTS.md`.

Repos: the artifact lands in the **meta** repo. Commands run in both sub-repos; neither is modified by this plan except by the requirement-closure edit in meta.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
</context>

<requirement_ownership>
**This plan MAY mark `GATE-03` Complete** — and only `GATE-03`.

- `GATE-03` — **this plan closes it**, and it can only be closed here: it is the only plan that runs the full sweep at the final commit.
- `DEVTEST-01` (plan 121-08), `DEVTEST-02`/`03`/`04` (plan 121-09), `DEVTEST-05`/`06` (plan 121-11), `GATE-01` (plan 121-03), `GATE-02` (plan 121-13) — **re-verify** each still reads Complete with a traceability sentence, and report any that does not. Do **not** re-tick, re-word or re-close any of them.
- `CLOSE-01`..`CLOSE-03` belong to Phase 122. Leave them alone.
</requirement_ownership>

<tasks>

<task type="auto">
  <name>Task 1: Re-run every gate row at the final commit under CI parity</name>
  <files>.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md</files>
  <read_first>
    - `/workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` — §CORRECTION-4's nine-row cross-repo gate table (the one explicitly handed to Phases 120-122) and the document's eight-section structure.
    - `/workspaces/.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md` — the immediately preceding capstone, for how it recorded a row as changed-by-design and how it named carried-forward findings it did not fix.
    - `/workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-RESEARCH.md` — Grep for `### F-8` and read the full numbered command table with its measured baselines, then Grep for `Pitfall 2` and `Pitfall 3` and read both, then Grep for `Verified CI-parity recipe` for the `uv` provisioning steps.
    - `/workspaces/.planning/REQUIREMENTS.md` — the Validation Ceiling section, so nothing recorded here crosses it.
  </read_first>
  <action>
Run every row yourself, at this phase's final commit, and record each result verbatim.

**Provision or re-verify CI parity first.** Reuse `/tmp/venv311` from plan `121-01` if it still exists; otherwise re-provision it with the `uv` recipe (cache and python-install directories redirected under `/tmp`, since the default cache location is not writable here). Record `/tmp/venv311/bin/python --version` and `/tmp/venv311/bin/ruff --version`. **Report the ruff verdict against that version**, and state the version next to the verdict everywhere it appears — a ruff result without its version is not interpretable in this project.

**Rows to run.** From `/workspaces/firestarter`: both native environments. From `/workspaces/firestarter_app`: the full host pytest suite on the devcontainer interpreter **and** on the parity interpreter; the coverage-gated run; `ruff check` and `ruff format --check` under the parity ruff; the mypy watermark tool; `check_dispatch.py`; `check_devtest_orchestrator.py`; the new `check_sdp_capability_invariants.py`; `check_no_community_support_status_write.py`; `check_no_log_in_sdp_window.py`; `check_is_memory_cmd_no_ifdef.py`; `diff_db.py`; the SDP bus-config generator plus its drift test; the SDP table-parity test; the revision-constants-parity test. From `/workspaces`: the catalog validity check and both codegen drift gates, plus the three-way catalog `cmp`.

**Record each row as: exact command, exact output line(s), verdict, and comparison to the researched baseline.** Where a row's result differs from the baseline, mark it **CHANGED BY DESIGN** with the plan that changed it and why — do not silently absorb it. Expected changes this phase: the host pytest count rises; `test_audit_coverage_matrix` goes from red to green; `check_devtest_orchestrator.py`'s scanned-function set grows; `test_revision_constants_parity` should be **unchanged** (no constant moved) and if it is not, that is a finding.

**`diff_db.py` needs its own sentence.** Its passing output names two explained changes, zero new and zero removed. Identity here means "still exactly those two", not "zero". Write that down explicitly — a verifier expecting a zero-diff will misread this gate.

**Cross-repo rename check, both directions.** This phase touched firmware (the regenerated header from plan `121-12`). For each host gate that scans firmware source text, confirm the path it resolves **exists** and record the resolved path. A gate pointed at a missing path passes vacuously, and this failure mode has bitten this project four times.

**Prove both sub-repo trees clean with porcelain status**, never a path-scoped diff. Record each repo's tip commit SHA alongside.

**Record the honest impossibility.** A py3.9 pytest run cannot be performed: the test extra requires a snapshot library whose floor is Python 3.10, so dependency resolution fails on 3.9. State that plainly, state that ruff and mypy are config-pinned to the py39 target and therefore behave identically on any interpreter, and state that the milestone's py3.9 support claim therefore rests on those pinned configurations plus the packaging classifier — not on a test run. Also record that ruff at the py39 target does **not** diagnose 3.10-only syntax, so it is not a py3.9 compatibility gate.

Write the results into `121-NONREGRESSION.md` as you go; Task 3 completes the document's remaining sections.

Commit (meta repo): `docs(121-14): record the full non-regression sweep at the phase's final commit`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -p no:cacheprovider -q && /tmp/venv311/bin/ruff check firestarter/ tests/ tools/ && /tmp/venv311/bin/ruff format --check firestarter/ tests/ tools/ && python3 tools/check_dispatch.py && python3 tools/check_devtest_orchestrator.py && python3 tools/check_sdp_capability_invariants.py && python3 tools/check_no_community_support_status_write.py && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/diff_db.py && python3 tools/check_mypy_watermark.py</automated>
  </verify>
  <acceptance_criteria>
    - Every row in the table is recorded with its exact command, exact output and verdict; no row is marked from memory or from a prior SUMMARY.
    - Host pytest: 0 failed on the devcontainer interpreter **and** 0 failed on `/tmp/venv311`; both counts recorded.
    - Coverage gate passes; the percentage is recorded.
    - `/tmp/venv311/bin/ruff check` and `format --check` both exit 0, with the resolved ruff version stated next to each verdict.
    - `python3 tools/check_mypy_watermark.py` reports an error count at or below 35; the count is recorded.
    - Both firmware native environments pass; both counts recorded and compared to the 141/141 baseline.
    - `python3 tools/diff_db.py` exits 0 with its two-explained-changes output, and the record explicitly states that identity means two, not zero.
    - Both firmware-source-scanning host gates report a resolved target path that exists; both paths recorded.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` are both empty; both tip SHAs recorded.
    - The three-way catalog `cmp` is clean and both codegen drift gates pass.
    - The py3.9 impossibility is recorded with its dependency cause and the two consequences (config-pinned tools, and ruff not being a version-compatibility gate).
  </acceptance_criteria>
  <done>Every gate row is re-run at the final commit with its output recorded, the ruff verdict carries its version, and both trees are proven clean by porcelain status.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the second audit-matrix regeneration is a no-op and re-verify every requirement row</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `/workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-01-SUMMARY.md` — the first regeneration's recorded byte counts and its explicit statement that this second regen must be a no-op.
    - `/workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-RESEARCH.md` — Grep for `C-2` and re-read that correction row: the matrix generator reads only the chip database and a planning ledger and has no `dev test` op vocabulary, so this phase should not move the matrix at all.
    - `/workspaces/.planning/REQUIREMENTS.md` — the nine phase requirement rows and their traceability parentheticals.
    - Each of `121-03`, `121-08`, `121-09`, `121-11`, `121-13`'s SUMMARY files — the plans that ticked a requirement, so the re-verification checks the claim against the live tree rather than against the SUMMARY's own assertion.
  </read_first>
  <action>
Two independent verifications, both recorded.

**The no-op regeneration.** Re-run the coverage-matrix generator with the committed meta ledger, exactly as plan `121-01` did, and diff the output against the committed golden. It must be byte-identical. If it is identical, record that as confirmation of RESEARCH C-2: this phase genuinely did not change the coverage matrix, and plan `121-01`'s refresh was purely pre-existing drift. If it is **not** identical, do not regenerate the golden — stop, investigate what moved the database or the ledger, and record it as a finding in `121-NONREGRESSION.md`'s findings section. A silent second refresh here would defeat D-18's entire purpose.

**Requirement re-verification, row by row, against the live tree.** For each of the nine ids, confirm the claimed behaviour by an independent check rather than by reading the SUMMARY that claimed it:
- the erase step's NA verdict and family-fact reason on a `0x0D` chip, with no erase dispatched;
- the zero-option command surface and each removed flag erroring;
- the UV axis's exactness and the handler's use of it;
- the stop-and-ask's three branches and the partial op's presence in the destructive-gate set;
- the dedup-before-ask ordering and the always-ask behaviour;
- both `gh` paths' deny-set argv assertions;
- the capability gate failing on each planted class and passing on real source;
- the required statements present in all eight docs;
- and GATE-03's own sweep from Task 1.

Record each as verified, with the command or assertion used. If any row's claim does not hold, report it prominently — a requirement ticked on a claim that does not hold is worse than an unticked one.

**Tick GATE-03 only.** Add its traceability sentence naming plan `121-14` and summarising the sweep's shape (which suites, which interpreter, which ruff version). Then confirm by reading the diff that no other requirement row moved.

**Record the two carried-forward conditions honestly**, neither of them this phase's damage: the meta repo's catalog-sync workflow is expected-red-until-milestone-merge because it checks out both sub-repos at their default branch; and any finding carried forward from Phase 120's capstone that this phase did not fix stays named rather than dropped.

Commit (meta repo): `docs(121-14): close GATE-03 after re-verifying all nine requirement rows against the live tree`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_audit_coverage_matrix.py -q && python3 -m pytest tests/ -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - The second regeneration is byte-identical to the committed golden — proven with `cmp` and recorded. If it is not, no golden was regenerated and a finding is written instead.
    - `git -C /workspaces/firestarter_app status --porcelain tests/golden/` is empty after the second regeneration.
    - All nine requirement rows are individually re-verified against the live tree, each with the command or assertion used recorded in the SUMMARY.
    - `.planning/REQUIREMENTS.md` shows `GATE-03` newly ticked with a traceability sentence naming plan `121-14`, the suites run, the interpreter and the ruff version.
    - `git -C /workspaces diff .planning/REQUIREMENTS.md` shows changes confined to the GATE-03 row — read the diff and confirm, do not infer from the stat.
    - `DEVTEST-01` through `DEVTEST-06`, `GATE-01` and `GATE-02` all still read Complete with their original traceability text intact.
    - `CLOSE-01`, `CLOSE-02` and `CLOSE-03` remain unticked.
    - The two carried-forward conditions are recorded with their causes.
  </acceptance_criteria>
  <done>The second regen is proven a no-op, all nine rows are independently re-verified, and GATE-03 is the only row that moved.</done>
</task>

<task type="auto">
  <name>Task 3: Complete 121-NONREGRESSION.md in the established eight-section shape</name>
  <files>.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md</files>
  <read_first>
    - `/workspaces/.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` — the eight-section structure to mirror.
    - `/workspaces/.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-NONREGRESSION.md` — the most recent instance, for tone and for how it handled a changed-by-design row.
    - Every `121-NN-SUMMARY.md` produced by this phase — for the corrections, reversals and findings each recorded.
  </read_first>
  <action>
Finish the document so a reader who was not present can audit this phase.

Sections, in the established order:

1. **The claim, as precise statements.** What this phase asserts is now true, worded so each statement is individually checkable. Include: an unmapped op string cannot reach a chip-mutating operator method; UV-ness is decided once from the exact axis and only read downstream; the partial write is inside the chip-ID gate; the erase step is NA on `0x0D` with an actionable reason; the command takes no options and always writes; every run runs the dedup check then asks; and the docs say so.

2. **The command-by-gate matrix**, from Task 1, with every changed cell flagged and attributed to the plan that changed it.

3. **Golden and generated-artifact identity.** The coverage-matrix golden's one intentional refresh (plan `121-01`) and the proven no-op second regeneration; the three-way catalog identity and both codegen drift gates; and — stated explicitly — that no firmware `.cpp` or `.h` other than the generated header was touched.

4. **The nine-row cross-repo gate table** from `119-NONREGRESSION.md` §CORRECTION-4, re-run and re-recorded, with the both-directions rename check and each gate's resolved target path.

5. **Corrections recorded, not acted on in `REQUIREMENTS.md`.** List every C-1 through C-9 correction the phase encountered, what the stated mechanism was, what reality was, and what the phase did instead. Add D-06's own correction about the issue parser having no op vocabulary, and D-17's doc-list widening. State the established response explicitly: satisfy the intent, record the correction, do not edit the requirement wording.

6. **The three recorded reversals**, each with its constraints named: the `dev test` redesign itself (reversing SAFE-01, SAFE-03 and Phase 112 Plan 04); the erase-capability note in the database transform; and v1.21's submission contract.

7. **Known and explained conditions.** The catalog-sync workflow's expected-red state and its cause; the py3.9 pytest impossibility and its dependency cause; the search-index eventual-consistency limitation on dedup; assumption A1 about comment permissions and the fallback that makes it non-load-bearing; and D-03's owned trade-off — an off-TTY run writes to silicon without anyone consenting, bounded to the small region, mitigated only by the unconditional notice and the docs.

8. **Deliberately not taken.** Every option the phase considered and declined, with its reason: removing the advisory locked-destructive field and its banner rather than keeping them; deleting the now-unreferenced-turned-live multi-run frozenset; pinning the ruff version instead of excluding the golden directory; adding a partner op string for the verify step; adding a read-only mode; and a provenance header on the protection reference.

**Validation-ceiling review.** Read every sentence you wrote against `REQUIREMENTS.md`'s Validation Ceiling section and record the review's outcome. This document must contain zero affirmative claims that SDP or the `0x0D` erase model has been demonstrated on real AT28C silicon. No AT28C part was on the bench; `0x0D` stays unverified; zero chips changed support status; the 84-chip count is unchanged.

Commit (meta repo): `docs(121-14): write 121-NONREGRESSION.md — the phase's audit record`.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md && grep -c "^## " /workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and contains all eight top-level sections in the established order — `grep -c "^## "` returns at least 8.
    - Section 5 names every correction C-1 through C-9 the phase encountered, plus D-06's parser correction and D-17's widening.
    - Section 6 names all three reversals with their constraints.
    - Section 7 names the off-TTY unconsented-write trade-off as owned and operator-chosen, not as an oversight.
    - The validation-ceiling review is recorded and finds zero affirmative silicon-validation claims; the four ceiling facts (no AT28C on the bench, `0x0D` unverified, zero support-status changes, 84-chip count unchanged) are stated.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` are both empty.
    - `python3 -m pytest tests/ -p no:cacheprovider -q` in `firestarter_app` still reports 0 failures at the final commit.
  </acceptance_criteria>
  <done>The audit record is complete in the established shape, every correction and reversal is named, and the validation-ceiling review finds no forbidden claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior plan SUMMARY → phase-level claim | A capstone that trusts a SUMMARY inherits its errors |
| generated artifact → committed oracle | A second silent regeneration would hide exactly what the first was isolated to expose |
| phase record → milestone close | Phase 122 builds its honesty ledger on this document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-58 | Repudiation | a gate row marked green from a prior SUMMARY rather than re-run | high | mitigate | Every row is re-executed at the final commit with its exact output recorded; the acceptance criteria require the output, not the verdict |
| T-121-59 | Tampering | a silent second golden regeneration masking a real change | high | mitigate | The second regeneration must be proven byte-identical; a non-identical result forbids regeneration and requires a written finding |
| T-121-60 | Spoofing | a ruff verdict reported without its version | medium | mitigate | The resolved parity ruff version is recorded next to every ruff verdict |
| T-121-61 | Spoofing | claiming firmware or host cleanliness with a vacuously-passing path-scoped diff | medium | mitigate | Cleanliness proven with `git status --porcelain` plus recorded tip SHAs |
| T-121-62 | Repudiation | a requirement ticked on a claim that does not hold in the live tree | high | mitigate | Row-by-row independent re-verification against the tree, each with the command used recorded |
| T-121-63 | Repudiation | a closing claim crossing the validation ceiling | high | mitigate | A per-sentence ceiling review is required and its outcome recorded, restating the four ceiling facts |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

**New planning artifacts (meta)**
- `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md` (**this plan**)

**Newly tracked files (`firestarter_app`)**: `doc/lockable-proms.md` (plan 121-13)

**Changed generated artifacts (both sub-repos)**: `firestarter/include/messages.h`, `firestarter_app/firestarter/messages.py` (plan 121-12)

**New source symbols (`firestarter_app`)**
- `find_prior_report(...)`, `comment_via_gh(...)` — `firestarter/submit.py` (plan 121-11)
- The `--skip-erase` warn-and-proceed arm in `write` (plan 121-10)
- `_is_uv_eprom(app, chip)`, `_resolve_write_scope(...)`, extended `_HANDLER_FUNCTION_NAMES` (plan 121-09)
- `SCHEMA_VERSION` bumped `"1.1"` → `"1.2"` (plan 121-07)
- `OP_WRITE_PARTIAL = "write-partial"` (plan 121-06); `_PROTOCOL_UV_EPROM` removed (plan 121-06)
- `is_uv_eprom(full)`, `Plan.is_uv`, `Step.write_region`, `derive_plan(..., write_scope=...)` (plan 121-05)
- `_MULTI_RUN_OPS` repurposed into the live dispatch allow-list (plan 121-02)
- A protocol-`0x0D` constant and the family-fact NA erase reason in `chip_test.py` (plan 121-08)

**New files (`firestarter_app`)**: `tests/test_write_skip_erase_0x0d.py` (plan 121-10); `tools/check_sdp_capability_invariants.py` + `FIRESTARTER_SDP_CAPABILITY_SRC`, `tests/test_check_sdp_capability.py`, `tests/fixtures/planted_permit_by_default.py`, `tests/fixtures/planted_widenable_allowset.py` (plan 121-03)

**New config keys**: `[tool.ruff] extend-exclude` in `firestarter_app/pyproject.toml` (plan 121-01)

**Changed CLI-facing behaviour**: `dev test <chip>` takes zero options and always writes; every run runs the dedup check then asks about filing; `write --skip-erase` warns on `0x0D`; `firestarter erase` on `0x0D` is refused one firmware layer earlier with the same wire message id.
</artifacts_produced>

<verification>
- Every gate row re-executed at the final commit with recorded output.
- Second coverage-matrix regeneration proven byte-identical.
- All nine requirement rows independently re-verified.
- Both sub-repo trees clean by porcelain status, tip SHAs recorded.
- `121-NONREGRESSION.md` complete in the eight-section shape with a recorded ceiling review.
</verification>

<success_criteria>
- The full non-regression set is green at the phase's final commit, reported against the CI-resolved ruff version.
- The second matrix regeneration is a proven no-op.
- The py3.9 impossibility is recorded with its cause rather than skipped.
- `GATE-03` is the only requirement this plan ticks; the other eight are re-verified, not re-touched.
- No closing claim crosses the validation ceiling.
</success_criteria>

<output>
Create `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-14-SUMMARY.md` when done.
</output>
