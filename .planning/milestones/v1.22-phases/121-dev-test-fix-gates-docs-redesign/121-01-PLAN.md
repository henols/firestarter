---
phase: 121-dev-test-fix-gates-docs-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/pyproject.toml
  - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md
autonomous: true
requirements: [GATE-03]
must_haves:
  truths:
    - "D-18: the pre-existing audit-matrix golden drift is regenerated as its own commit, with ZERO DEVTEST code anywhere in the tree, so this phase's later matrix delta (if any) is attributable in isolation."
    - "D-18: `pytest tests/test_audit_coverage_matrix.py` is GREEN at this plan's final commit, and the full host suite has zero failures at that same commit."
    - "The `ruff format --check` collision between the CI-resolved ruff version and the byte-asserted golden is closed BEFORE any `ruff format` is ever run in this phase (RESEARCH Pitfall 2)."
    - "A CI-parity Python 3.11 venv exists and its ruff version is recorded, so every later plan can report a ruff verdict against the version CI actually resolves rather than the devcontainer's."
  artifacts:
    - firestarter_app/pyproject.toml
    - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md
    - /tmp/venv311 (CI-parity interpreter, not committed)
  key_links:
    - "`[tool.ruff] extend-exclude` must cover `tests/golden` before any formatter run, or `ruff format` rewrites the very file `test_golden_file_matches` asserts byte-identical."
    - "`tools/audit_coverage_matrix.py::generate_matrix` must be fed the committed meta-repo ledger `.planning/v1.3-defect-coverage-ids.json`, exactly as `test_golden_file_matches` does, or the regenerated golden will not match the test."
---

<objective>
Land D-18's golden regeneration as the phase's first commits, with zero `dev test` code in the tree, and close the ruff-version/golden collision that would otherwise turn two gates against each other.

Purpose: `tests/test_audit_coverage_matrix.py::test_golden_file_matches` is RED today (186034 produced vs 184631 golden bytes, first divergence at index 1178) from a pre-existing pinout split introduced at Phase 98's `362bfa0`. If that stays red while this phase lands code, a genuinely new failure is masked — exactly what PROJECT.md SEVENTH CORRECTION item 2 forbids. Separately, `ruff format --check` is green on the devcontainer's ruff 0.15.20 and RED under the 0.16.0 that CI resolves from the unpinned `ruff>=0.15.14`, and the single file it wants to reformat is that same golden.

Output: a ruff config exclusion, a regenerated golden, a recorded CI-parity ruff version, and a green host suite at a commit containing no DEVTEST work.

Repo: all edits land in `/workspaces/firestarter_app` on branch `v1.22-at28c-software-data-protection-lifecycle`. The meta repo is read-only here (the ledger is an input, not an output).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
</context>

<requirement_ownership>
**This plan may mark NOTHING Complete.**

- `GATE-03` — **contributes only.** GATE-03 is closed by plan `121-14` after the full nine-row sweep. Do not tick it here.
- Every other `DEVTEST-NN` / `GATE-NN` id is out of this plan's scope entirely. Leave `REQUIREMENTS.md` byte-unchanged.
</requirement_ownership>

<tasks>

<task type="auto">
  <name>Task 1: Add the ruff exclusion that protects the byte-asserted golden and the planted-violation fixtures</name>
  <files>firestarter_app/pyproject.toml</files>
  <read_first>
    - `/workspaces/firestarter_app/pyproject.toml` — the `[tool.ruff]` block (currently `target-version = "py39"`, `line-length = 88`, and **no** `exclude`/`extend-exclude` key at all), plus `[project.optional-dependencies]`'s `ruff>=0.15.14` (no upper bound) and `[tool.pytest.ini_options]`.
    - `/workspaces/firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md` — read only enough to confirm it contains a fenced Python block (around golden line ~447, the `build_db.py` predicate quoted verbatim). This is the block ruff 0.16 wants to reformat.
    - `/workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-RESEARCH.md` — read the `### Pitfall 2` section and `## Environment Availability` (the verified `uv` recipe). Use Grep for `Pitfall 2` and `Verified CI-parity recipe` to find the line ranges rather than loading the whole 114 KB file.
  </read_first>
  <action>
Provision the CI-parity interpreter first, then land the exclusion.

**Provision (not committed):**
Export `UV_CACHE_DIR=/tmp/uvcache` and `UV_PYTHON_INSTALL_DIR=/tmp/uvpy` (the default `~/.cache/uv` is not writable in this devcontainer). Run `uv python install 3.11`, `uv venv --python 3.11 /tmp/venv311`, then from `/workspaces/firestarter_app` run `uv pip install --python /tmp/venv311/bin/python -e '.[test]'`. Record the resolved ruff version by running `/tmp/venv311/bin/ruff --version` and write that exact version string into the SUMMARY. RESEARCH measured 0.16.0, but assumption A4 states the resolved version drifts under an unpinned floor — report what you actually get, never the researched number.

**Reproduce the collision before fixing it:** run `/tmp/venv311/bin/ruff format --check firestarter/ tests/` and capture the output. It is expected to name `tests/golden/v1.3-COVERAGE-MATRIX.md` as "1 file would be reformatted". Record the verbatim output in the SUMMARY. If it does NOT reproduce, record that instead and still land the exclusion — the exclusion is correct regardless, because the byte-identity contract and a formatter both claiming ownership of one file is a latent trap.

**Land the exclusion:** add an `extend-exclude` key to the existing `[tool.ruff]` table in `firestarter_app/pyproject.toml` listing exactly two paths: `tests/golden` and `tests/fixtures`. Do not add a top-level `exclude` key (that would drop ruff's built-in defaults). Do not pin or bound the `ruff` dependency — RESEARCH rejected pinning `ruff==0.15.*` because it freezes lint hygiene project-wide for one markdown file.

Add a comment above the key stating both reasons in this order: (1) `tests/golden/v1.3-COVERAGE-MATRIX.md` is asserted byte-identical to `tools/audit_coverage_matrix.py`'s output by `tests/test_audit_coverage_matrix.py::test_golden_file_matches`, and ruff 0.16+ formats Python code blocks embedded in markdown, so a formatter run and the generator would fight over the same bytes; (2) `tests/fixtures/` holds deliberately-violating planted source for the source-scan gates — it is fixture input, never project source, and Phase 121 adds the first Python files to that directory.

**Never run bare `ruff format`** (without `--check`) at any point in this plan or this phase.

**Verify the exclusion works:** re-run `/tmp/venv311/bin/ruff format --check firestarter/ tests/` and `/tmp/venv311/bin/ruff check firestarter/ tests/`. Both must pass. Also run the devcontainer ruff (`ruff format --check firestarter/ tests/`) so both versions are recorded.

Commit this task alone: `fix(121-01): exclude tests/golden and tests/fixtures from ruff (CI-resolved ruff reformats the byte-asserted golden)`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && /tmp/venv311/bin/ruff format --check firestarter/ tests/ && /tmp/venv311/bin/ruff check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/venv311/bin/python --version` reports a 3.11.x interpreter.
    - `/tmp/venv311/bin/ruff --version` output is recorded verbatim in the SUMMARY (whatever version it resolves to).
    - `python3 -c "import tomllib,pathlib;d=tomllib.loads(pathlib.Path('pyproject.toml').read_text());print(d['tool']['ruff']['extend-exclude'])"` run from `/workspaces/firestarter_app` prints a list containing both `tests/golden` and `tests/fixtures`.
    - The same command shows no `exclude` key was added to `[tool.ruff]` (only `extend-exclude`).
    - `/tmp/venv311/bin/ruff format --check firestarter/ tests/` exits 0.
    - `/tmp/venv311/bin/ruff check firestarter/ tests/` exits 0.
    - `git -C /workspaces/firestarter_app show --stat HEAD` for this commit lists exactly one file: `pyproject.toml`.
    - The pre-fix `ruff format --check` output (whether it reproduced the collision or not) is quoted verbatim in the SUMMARY alongside the ruff version that produced it.
  </acceptance_criteria>
  <done>`[tool.ruff] extend-exclude` covers `tests/golden` and `tests/fixtures`; both ruff versions agree that `format --check` and `check` pass; the commit touches only `pyproject.toml`.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the stale audit-matrix golden, alone, and prove the host suite green at that commit</name>
  <files>firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_audit_coverage_matrix.py` — the `test_golden_file_matches` method (around lines 576-645). It resolves `meta_root = Path(__file__).resolve().parents[2]`, seeds a tmp ledger byte-identically from `<meta_root>/.planning/v1.3-defect-coverage-ids.json`, calls `generate_matrix(output=<tmp>, ledger_path=<tmp ledger>)`, and asserts byte-identity against `tests/golden/v1.3-COVERAGE-MATRIX.md`. Your regeneration MUST use that exact input pair.
    - `/workspaces/firestarter_app/tools/audit_coverage_matrix.py` — the `generate_matrix(output, ledger_path, check=False)` signature (around line 1436) and the `main()`/argparse block (around line 1884). Confirm it writes with `newline="\n"` and takes no timestamp input (its own docstring guarantees two consecutive runs are byte-identical).
    - `/workspaces/.planning/phases/121-dev-test-fix-gates-docs-redesign/121-RESEARCH.md` — Grep for `C-2` and read that row of the corrections table, plus the `### F-8` "Known-red / known-flaky" bullet on this golden. The live drift is a **pinout split** (golden `DIP32_STD 127` vs produced `DIP32_27C020 88` + `DIP32_STD 39`), traced by `git log -S` to `362bfa0 feat(98-01)` — it is NOT a renderer change and NOT caused by this phase.
  </read_first>
  <action>
Regenerate the golden and nothing else.

**Confirm the tree is DEVTEST-free first.** Run `git -C /workspaces/firestarter_app status --porcelain` and confirm no modification to `firestarter/chip_test.py`, `firestarter/cli_handlers.py`, `firestarter/database.py`, `firestarter/diagnostic_report.py`, or `firestarter/submit.py` is staged or unstaged. Use `git status --porcelain`, never a path-scoped `git diff` (PROJECT.md SEVENTH CORRECTION item 9: a path-scoped diff on a path nothing touched passes vacuously). If anything DEVTEST-shaped is dirty, stop and report — D-18's whole point is that this commit contains no DEVTEST code.

**Capture the RED baseline:** run `python3 -m pytest tests/test_audit_coverage_matrix.py::TestAuditCoverageMatrix::test_golden_file_matches -x` and record the byte counts from the assertion message verbatim in the SUMMARY (expected: produced 186034 vs golden 184631). If the numbers differ from those, record the actual ones — the drift may have moved.

**Regenerate:** from `/workspaces/firestarter_app`, call `tools.audit_coverage_matrix.generate_matrix` with `ledger_path` set to the committed meta ledger `/workspaces/.planning/v1.3-defect-coverage-ids.json` and `output` set to a scratch path under the session scratchpad, then copy the produced bytes over `tests/golden/v1.3-COVERAGE-MATRIX.md`. Do not hand-edit the golden. Do not run the generator with a different ledger. Do not use `--check`.

**Prove determinism:** regenerate a second time to a different scratch path and assert the two produced files are byte-identical to each other and to the new golden. The generator's own contract promises this; prove it rather than trusting it.

**Prove the ledger was not mutated:** `git -C /workspaces status --porcelain .planning/v1.3-defect-coverage-ids.json` must be empty after both runs. If the ledger moved, stop — the regen is not deterministic and D-18's premise is broken.

**Characterise the delta honestly in the SUMMARY:** report the byte delta, the line delta, and name the pinout-split cause (`DIP32_STD` splitting into `DIP32_27C020` + `DIP32_STD`) with its `362bfa0` provenance, plus RESEARCH C-2's finding that `tools/audit_coverage_matrix.py` has no `dev test` op vocabulary at all — so this refresh is pre-existing DB drift, not a Phase 121 change. State explicitly that the second regeneration at phase end (plan `121-14`) is therefore expected to be a **no-op**, and that a non-empty diff there is a finding.

**Prove the whole host suite green:** run `python3 -m pytest tests/ -p no:cacheprovider -q`. The expected result is 1052 passed, 0 failed (the 1051 previously-passing plus this now-green golden test). If any other test is red, stop and report it — it is not this plan's to fix, but it must not be silently absorbed.

Commit this task alone: `fix(121-01): regenerate stale v1.3 coverage-matrix golden (pre-existing DIP32 pinout-split drift from 362bfa0)`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_audit_coverage_matrix.py -q && python3 -m pytest tests/ -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_audit_coverage_matrix.py::TestAuditCoverageMatrix::test_golden_file_matches -x` exits 0.
    - `python3 -m pytest tests/ -p no:cacheprovider -q` reports **0 failed**.
    - `git -C /workspaces/firestarter_app show --stat HEAD` for this commit lists exactly one file: `tests/golden/v1.3-COVERAGE-MATRIX.md`.
    - `git -C /workspaces status --porcelain .planning/v1.3-defect-coverage-ids.json` is empty (the generator did not mutate its own input).
    - Two independent regenerations produced byte-identical output (asserted with `cmp`, result recorded in the SUMMARY).
    - The SUMMARY records: the RED baseline byte counts, the post-regen byte count, the pinout-split cause with `362bfa0` provenance, RESEARCH C-2's "no op vocabulary in the matrix generator" finding, and the explicit statement that plan `121-14`'s second regen must be a no-op.
    - No file under `firestarter/` is modified by either commit in this plan.
  </acceptance_criteria>
  <done>`test_golden_file_matches` is green, the full host suite has zero failures, the regen commit touches only the golden, and the delta is characterised as pre-existing drift with provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → committed golden | `tools/audit_coverage_matrix.py` output becomes a committed regression oracle; a wrong regeneration silently rebaselines the oracle |
| formatter → committed golden | `ruff format` can rewrite a file whose bytes another gate asserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-121-01 | Tampering | `tests/golden/v1.3-COVERAGE-MATRIX.md` | high | mitigate | Regenerate only via `generate_matrix` with the committed meta ledger — the exact input pair `test_golden_file_matches` uses; never hand-edit; prove determinism with two independent runs compared by `cmp` |
| T-121-02 | Repudiation | D-18 commit isolation | high | mitigate | Assert `git status --porcelain` shows no DEVTEST source dirty before committing, and assert the commit's `--stat` lists exactly one file — so a later matrix delta cannot be blamed on, or hidden by, this refresh |
| T-121-03 | Tampering | `ruff format` vs the byte-asserted golden | medium | mitigate | `extend-exclude = ["tests/golden", "tests/fixtures"]` lands before any formatter run; bare `ruff format` is prohibited for the whole phase |
| T-121-04 | Information Disclosure | CI-parity venv under `/tmp` | low | accept | `/tmp/venv311` is scratch, never committed, and contains no secret; `.gitignore` is untouched |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

Symbols, files and behaviours **newly created by Phase 121** (listed phase-wide so drift verification does not flag them as unacknowledged):

**New source symbols (`firestarter_app`)**
- `OP_WRITE_PARTIAL = "write-partial"` — `firestarter/chip_test.py` (seventh op string, D-06)
- `is_uv_eprom(full)` — `firestarter/chip_test.py` (public pure predicate, D-02)
- `Plan.is_uv: bool` — `firestarter/chip_test.py` dataclass field (D-02)
- `Step.write_region: tuple[int, int] | None` — `firestarter/chip_test.py` dataclass field (D-02)
- `derive_plan(name, db, *, write_scope="full"|"partial")` — signature replaces the `destructive: bool` kwarg
- `_is_uv_eprom(...)`, `_resolve_write_scope(...)` — `firestarter/cli_handlers.py` handler helpers (D-01/D-03)
- `find_prior_report(...)`, `comment_via_gh(...)` — `firestarter/submit.py` (D-09/D-11)
- `_LEGACY_OP_VOCABULARY` — `firestarter/diagnostic_report.py` (b11 back-compat constant, D-06)
- `SCHEMA_VERSION` value bumped from `"1.1"` to `"1.2"`

**New files (`firestarter_app`)**
- `tools/check_sdp_capability_invariants.py` (GATE-01 checker) + its `FIRESTARTER_SDP_CAPABILITY_SRC` env seam
- `tests/test_check_sdp_capability.py`
- `tests/fixtures/planted_permit_by_default.py`
- `tests/fixtures/planted_widenable_allowset.py`
- `tests/test_write_skip_erase_0x0d.py`
- `doc/lockable-proms.md` becomes **newly tracked** (was untracked, D-16)

**New config keys**
- `[tool.ruff] extend-exclude` in `firestarter_app/pyproject.toml`

**New planning artifacts (meta)**
- `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md`

**Changed CLI-facing behaviour**
- `firestarter dev test <chip>` accepts **zero** options and always writes to the chip
- `firestarter write --skip-erase` on a protocol-`0x0D` chip warns and proceeds
- Every `dev test` run reaches the filing ask
</artifacts_produced>

<verification>
- `python3 -m pytest tests/ -p no:cacheprovider -q` → 0 failed.
- `/tmp/venv311/bin/ruff check firestarter/ tests/` → exit 0.
- `/tmp/venv311/bin/ruff format --check firestarter/ tests/` → exit 0.
- Exactly two commits, each touching exactly one file.
- `git -C /workspaces/firestarter status --porcelain` empty (firmware untouched by this plan).
</verification>

<success_criteria>
- The pre-existing golden drift is closed in a commit containing no `dev test` code.
- The ruff/golden collision is closed before any formatter runs in this phase.
- The CI-resolved ruff version is recorded for every later plan to reuse.
- Full host suite: 0 failures.
</success_criteria>

<output>
Create `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-01-SUMMARY.md` when done.
</output>
