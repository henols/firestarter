---
phase: 116-ground-truth-trace-harness
plan: 05
type: execute
wave: 3
depends_on: ["116-01", "116-02"]
sub_repo: firestarter
files_modified:
  - firestarter/test/native/avr/_shared/sdp_expected.h
  - firestarter/test/native/avr/test_sdp_harness/host_stubs.cpp
  - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp
  - firestarter/platformio.ini
  - firestarter/test/native/avr/test_eeprom28c_chip_id/
autonomous: true
requirements: [TRACE-01, TRACE-03, TRACE-04]

must_haves:
  truths:
    - "An allowlisted, permanently-green native suite proves the ordered strobe stream interleaves data bytes, latch strobes and CE/OE edges, including production's register-write elision."
    - "Feeding the emitter a unlock table whose terminal byte is changed produces a demonstrably different ordered stream, and that stream is identical to the real chip-erase table's stream."
    - "Feeding the emitter the three-write lock/write-prefix table where the six-write unlock stream is expected diverges at a named index."
    - "The identity-gate assertions that do not hinge on the SDP outcome execute in CI against an address-keyed mock."
    - "The retired suite directory no longer exists and no call-ordered byte-array fixture survives anywhere under the native test tree."
  artifacts:
    - "firestarter/test/native/avr/_shared/sdp_expected.h — literal tuple arrays plus the shared ordered comparator"
    - "firestarter/test/native/avr/test_sdp_harness/{host_stubs.cpp,test_sdp_harness.cpp}"
    - "firestarter/platformio.ini — test_filter and -I entries for the harness suite"
  key_links:
    - "The harness suite drives flash_util_byte_flipping directly, never eeprom28c_write_init — Phase 117 must leave flash_utils.cpp byte-untouched, so this suite is green by construction across the fix."
    - "reset_register_cache(0x00, 0x00, 0x00) after configure_memory is load-bearing: the real header's cache powers on at 0xff, which ORs a VPP-regulator bit into the first CONTROL write."
    - "The expected arrays must be authored from a recorded dump of real production code, never hand-derived."
---

<objective>
Stand up the always-green harness suite (D-03): the executable proof that the ordered recorder
captures what it claims to, that the harness can tell unlock from lock from erase, and that the
identity-gate assertions worth keeping now actually run in CI. Retire the scripted-mock suite they
came from (D-12).

Purpose: TRACE-01's capability half and TRACE-03's two in-suite negatives have no dependency on the
Phase-117 fix, so they can and must be green from day one — that keeps GATE-03 covering the harness
while the `0x0D` suite is parked. This suite is also where `_shared/sdp_expected.h` — the single
source of truth both suites assert against — is authored and pinned.

Output: a new allowlisted native suite, the shared expected-stream header, two `platformio.ini`
lines, and one deleted directory.

**Zero production-code risk fence:** nothing under `firestarter/src/` or `firestarter/include/` is
modified. Every table this suite drives is either a real production table from `flash_utils.h` or a
deliberately-mutated test-local copy of one.
</objective>

## Artifacts this phase produces

These symbols/paths are **created by Phase 116** and do not exist in the tree yet. Drift
verification must treat them as new, not as unresolved references.

**New firmware suite directories** — `firestarter/test/native/avr/test_sdp_harness/`,
`firestarter/test/native/avr/test_eeprom28c_sdp/`

**New firmware files** — `test_sdp_harness/host_stubs.cpp`, `test_sdp_harness/test_sdp_harness.cpp`,
`test_eeprom28c_sdp/host_stubs.cpp`, `test_eeprom28c_sdp/test_eeprom28c_sdp.cpp`,
`test_eeprom28c_sdp/RED-BASELINE.md`, `_shared/sdp_expected.h`, `_shared/sdp_bus_config.h` (GENERATED)

**New firmware preprocessor / C symbols** — `HOST_STUBS_REAL_REGISTER_UTILS`,
`HOST_STUBS_MAX_STROBES`, `HOST_STUBS_CUSTOM_CONTROL_PIN`, `HOST_STUBS_CUSTOM_DATA_BUFFER`,
`HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`, `STROBE_KIND_DATA`, `STROBE_KIND_PIN`, `strobe_entry_t`,
`strobe_push`, `clear_strobes()`, `strobe_count()`, `strobe_overflowed()`, `strobe_kind(int)`,
`strobe_pin(int)`, `strobe_value(int)`, `reset_register_cache(...)`, `sdp_strobe_t`,
`sdp_assert_stream_equals(...)`, `sdp_first_divergence(...)`, `SDP_SHIPPED_*`, `SDP_FIXED_*`,
`*_LEN`, `sdp_bus_config_row_t`, `SDP_BUS_CONFIGS[]`, `SDP_BUS_CONFIG_COUNT`

**New `platformio.ini` keys** — `test_filter` entry for the harness suite; `build_flags`
`-I test/native/avr/test_sdp_harness` and `-I test/native/avr/test_eeprom28c_sdp`

**New host files** — `tools/gen_sdp_bus_config.py`, `tools/check_no_log_in_sdp_window.py`,
`tests/test_sdp_bus_config_drift.py`, `tests/test_sdp_db_invariant.py`,
`tests/test_sdp_table_parity.py`, `tests/test_check_no_log_in_sdp_window.py`,
`tests/fixtures/planted_log_in_window.cpp`

**New host symbols** — env-override seam `FIRESTARTER_SDP_SRC`; `--target` and `--check` argparse
options on `gen_sdp_bus_config.py`

**Retired** — the whole directory `firestarter/test/native/avr/test_eeprom28c_chip_id/`

**New meta files** — `.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md`; a third
⚠ correction block inside `.planning/PROJECT.md`

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter/CLAUDE.md
@.planning/phases/116-ground-truth-trace-harness/116-01-SUMMARY.md
@.planning/phases/116-ground-truth-trace-harness/116-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_sdp_harness suite skeleton + _shared/sdp_expected.h shipped arrays + platformio.ini wiring (TRACE-01, D-03, D-06)</name>
  <files>firestarter/test/native/avr/_shared/sdp_expected.h, firestarter/test/native/avr/test_sdp_harness/host_stubs.cpp, firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp, firestarter/platformio.ini</files>
  <read_first>
    - firestarter/test/native/avr/test_val_eeprom28c/host_stubs.cpp (whole file, 29 lines — copy the banner, the `extern "C"` include block, the flag-BEFORE-include ordering comment, and the relative `.inc` include verbatim)
    - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp (whole file — the imports + recording-API extern block at :31-47, the `setUp` shape at :49-55, the `make_handle` factory at :59-67, the named-message assert helper at :73-84, and the explicit `RUN_TEST` list in `main()` at :116-126)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (as extended by plan 116-01 — the exact new flag name and the six accessor signatures)
    - firestarter/test/native/avr/_shared/sdp_bus_config.h (as generated by plan 116-02 — the row struct and the five rows)
    - firestarter/include/flash_utils.h lines 15-59 — the `flash_execute_command` macro (which requires a variable literally named `handle`, so call `flash_util_byte_flipping(&h, ...)` directly) and the four command tables `FLASH_ERASE`, `FLASH_ENABLE_WRITE`, `FLASH_ENABLE_WRITE_PROTECTION`, `FLASH_DISABLE_WRITE_PROTECTION`
    - firestarter/src/proms/flash_utils.cpp lines 20-27 and 51-66 — `flash_util_byte_flipping`, `fu_flash_flip_data`, `fu_flash_fast_address`
    - firestarter/include/rurp_register_utils.h lines 12-14 (the three non-static cache globals `lsb_address` / `msb_address` / `control_register`, initialised to 0xff)
    - firestarter/platformio.ini §`[env:native]` — the two parallel allowlists (`test_filter` and the `-I` entries in `build_flags`) that must be edited together
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 423-465 (§F4 — the verbatim 54-entry shipped stream for DIP28_28C256 and the per-pinout latch table), lines 1071-1115 (§Code Examples — the new suite's `host_stubs.cpp`, the cache-reset seam, and the mandatory ArduinoFake mocks), lines 916-964 (Pitfalls 1, 2, 3, 4), lines 1156-1172 (the dump-then-hand-check technique for authoring the arrays)
  </read_first>
  <action>
Create the always-green harness suite and the shared expected-stream header, and wire the suite into
`[env:native]`.

**`test_sdp_harness/host_stubs.cpp`** — copy `test_val_eeprom28c/host_stubs.cpp` wholesale, then:
substitute the new opt-in flag name for `HOST_STUBS_RECORD_BUS`; keep the "MUST be defined BEFORE
the include" pitfall comment (it is a recorded in-tree pitfall, not decoration); after the `.inc`
include, add `#include "rurp_register_utils.h"` (D-05); and add an `extern "C"`
`reset_register_cache(uint8_t lsb, uint8_t msb, rurp_register_t ctrl)` seam that assigns the real
header's three cache globals. Comment the seam with *why* it is mandatory: those globals are
non-static, power on at `0xff`, persist across Unity cases in the same binary, and a `0xff` CONTROL
value ORs a VPP-regulator bit into the first address write — so without a deliberate reset the suite
would appear to show the 5V-only `0x0D` path enabling the VPP regulator.

**`_shared/sdp_expected.h`** — the single source of truth both Phase-116 suites assert against
(D-06). It contains, in this order:
- the `extern "C"` declarations of the six recorder accessors, so both suites get them from one place
- `typedef struct { uint8_t kind; uint8_t pin; uint8_t value; } sdp_strobe_t;`
- `static int sdp_first_divergence(const sdp_strobe_t* expected, int expected_len)` — returns the
  index of the first element where the recorded stream differs from `expected` (treating a length
  mismatch as a divergence at the shorter length), or `-1` when they are equal
- `static void sdp_assert_stream_equals(const sdp_strobe_t* expected, int expected_len, const char* ctx)`
  — asserts `strobe_overflowed()` is 0, asserts the recorded count equals `expected_len`, then
  asserts element-by-element and, on mismatch, fails with a message that **names the diverging
  index** and prints both the expected and the recorded `{kind, pin, value}` triple at that index.
  D-06 raises the bar here deliberately: the closest in-tree analog
  (`test_val_5v_page.cpp:199-217`) uses a *sub-sequence scan*, which RESEARCH §F5 proves cannot
  distinguish the shipped stream from the fixed one — identical length, identical MSB set on DIP32.
  Ordered full-stream equality is the only assertion shape that works, and counting anything is
  explicitly forbidden as a discriminator.
- the literal `static const sdp_strobe_t SDP_SHIPPED_<PINOUT>[]` arrays plus matching `_LEN` defines,
  one per representative chip

**Authoring the literal arrays — empirically, never by hand.** `pio test` swallows `printf` from
test bodies, so: add a `dump_strobes(const char* tag)` helper to the suite guarded behind
`#ifdef SDP_TRACE_DUMP` (keep it — Phase 117 will want it), build the suite, locate the built binary
under `firestarter/.pio/build/native/` and run it directly to capture the stream, then transcribe
the captured entries into the header. **Cross-check gate:** the resulting
`SDP_SHIPPED_DIP28_28C256` array must be exactly the 54-entry stream published verbatim in
RESEARCH §F4 (lines 429-447), including the elision at index 30 where write #4 emits only
`DATA 0xAA` plus the OE and CE edges because the cached LSB/MSB already hold `0x55`/`0x55`. If the
captured stream differs from §F4 in any element, STOP and report — something in the recorder or the
handle setup is wrong and no later assertion can be trusted.

**`test_sdp_harness.cpp`** — the suite body:
- `setUp()` extends the analog's: `ArduinoFakeReset()`, the three `Serial` mocks, plus
  `delayMicroseconds`, `delay` and `millis` mocks. Comment *why* the timing mocks are there — the
  real `rurp_register_utils.h` calls `delayMicroseconds` and ArduinoFake aborts with SIGABRT on an
  unmocked virtual; a bare SIGABRT in a new suite is this, not the pre-existing Unity-teardown flake
  D-13 defers. Do not let a later editor delete them as unused.
- a `make_sdp_handle(const sdp_bus_config_row_t& row)` factory: zero-init handle, `protocol` `0x0D`,
  `cmd` `CMD_WRITE`, `response_code` OK, `chip_id` 0, `mem_size` and `bus_config` from the row,
  `ctrl_flags` carrying `FLAG_SKIP_BLANK_CHECK`.
- a `drive(table, len, row, ctrl_seed)` helper performing the load-bearing sequence in this exact
  order: `configure_memory(&h)` to install the real memory function pointers, then
  `reset_register_cache(0x00, 0x00, ctrl_seed)`, then `clear_strobes()`, then
  `flash_util_byte_flipping(&h, table, len)`. The cache reset and the strobe clear must both come
  *after* `configure_memory`, which itself writes address 0 — the analog records this hazard at
  `test_val_5v_page.cpp:150-175`. A `ctrl_seed` of `0x00` is what makes the 54-entry expected array
  correct: `flash_util_byte_flipping`'s two `CTRL_READ_WRITE` clears then produce no CONTROL entry
  because the cached value is unchanged.
- **Case 1 — ordered capture:** drive the real `FLASH_DISABLE_WRITE_PROTECTION` table (from
  `flash_utils.h`, so there is no transcription) for `DIP28_28C256` and assert the stream equals
  `SDP_SHIPPED_DIP28_28C256` via the ordered comparator. Add a header comment stating why this suite
  drives `flash_util_byte_flipping` **directly** and never `eeprom28c_write_init`: Phase 117 leaves
  `flash_utils.cpp` byte-untouched (its own success criterion 2), so this suite is green by
  construction across the fix, while the parked `0x0D` suite is the one that flips RED to GREEN.
- **Case 2 — elision is real:** assert the entry at index 30 is a `DATA` entry with value `0xAA`
  (write #4 emitting no address latch at all), and that `strobe_overflowed()` is 0. Comment that a
  raw call-log golden would assert six entries the shield never sees.
- **Case 3 — CE/OE edges are distinguishable:** assert that the stream contains both `OUTPUT_ENABLE`
  and `CHIP_ENABLE` pin entries and that within each command write the ordering is OE-then-CE-low-
  then-CE-high, using positional indices from the expected array rather than a scan.

**`platformio.ini`** — add `native/avr/test_sdp_harness` to the `test_filter` allowlist **and**
`-I test/native/avr/test_sdp_harness` to `build_flags`. Both are required: `[env:native]` uses a
positive allowlist (a documented PIO version quirk) and a suite is invisible without the
`test_filter` line and unbuildable without the `-I` line. Do not add anything for the parked suite
in this task — plan 116-06 owns that edit.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*" 2>&1 | tail -20 && pio test -e native 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*"` exits 0 with 3 passing cases
    - `cd /workspaces/firestarter && pio test -e native` exits 0 and reports 85 test cases, 0 failures (82 carried in from plan 116-01 plus this suite's 3)
    - `SDP_SHIPPED_DIP28_28C256_LEN` is exactly 54 and the array's element at index 30 is `{STROBE_KIND_DATA, 0, 0xAA}` — matching RESEARCH §F4 verbatim
    - `grep -c 'sdp_assert_stream_equals' firestarter/test/native/avr/_shared/sdp_expected.h` returns at least 2 (declaration/definition plus use in the divergence helper or comment)
    - The comparator's failure path names the diverging index: temporarily corrupting one element of the expected array makes `pio test -e native -f "*test_sdp_harness*"` fail with a message containing that index; the executor must perform this check and record the observed message, then revert
    - `grep -c 'delayMicroseconds' firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp` returns at least 1
    - `awk '/^test_filter =/,/^build_flags =/' firestarter/platformio.ini | grep -c 'test_sdp_harness'` returns 1
    - `grep -c -- '-I test/native/avr/test_sdp_harness' firestarter/platformio.ini` returns 1
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing
  </acceptance_criteria>
  <done>An allowlisted, green suite records the ordered stream and pins it element-by-element against a literal array that reproduces RESEARCH §F4 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: TRACE-03a/b in-suite negatives + the fixed-stream reference-emitter guard (D-04, D-06)</name>
  <files>firestarter/test/native/avr/_shared/sdp_expected.h, firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp and firestarter/test/native/avr/_shared/sdp_expected.h (as written in Task 1)
    - firestarter/include/flash_utils.h lines 33-59 — note that `FLASH_ENABLE_WRITE_PROTECTION` and `FLASH_ENABLE_WRITE` are byte-identical three-write tables, and that `FLASH_ERASE` differs from `FLASH_DISABLE_WRITE_PROTECTION` only in the terminal byte
    - firestarter/src/proms/memory.cpp lines 218-234 — `memory_set_data`, the function Phase 117's remap-aware emitter is specified to be built on (`handle->firestarter_set_data`)
    - firestarter/src/proms/memory.cpp lines 258-282 — `mem_util_remap_address_bus`, the remap `fu_flash_fast_address` skips
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 466-518 (§F5 — the remap-aware target latches per pinout, the per-write shape difference, and the proof that stream length is identical shipped-vs-fixed), lines 519-545 (§F6 — internal linkage of the shipped table), lines 966-981 (Pitfalls 5 and 6)
    - .planning/ROADMAP.md §"Phase 116" success criterion 3 and §"Phase 119" success criterion 5 (the `FLASH_ENABLE_WRITE_PROTECTION` preservation requirement this task must not undermine)
  </read_first>
  <action>
Add TRACE-03's two in-suite negatives, the lock-vs-write-prefix finding, and the fixed-stream
literal arrays with the guard that pins them to real production behaviour.

**Negative A — unlock table mutated (TRACE-03a).** Build a test-local `byte_flip_t` array that is a
copy of the six unlock pairs with the terminal byte changed to the chip-erase value, feed it through
the same emitter, and assert two things: (i) the recorded stream is **not** element-wise equal to
`SDP_SHIPPED_DIP28_28C256`, and `sdp_first_divergence(...)` returns exactly the index of the sixth
write's payload byte — naming the index is what makes this non-hollow; and (ii) the recorded stream
**is** element-wise equal to the stream produced by driving the real `FLASH_ERASE` table from
`flash_utils.h`. Clause (ii) is the stronger claim: a one-nibble slip in the terminal byte turns
SDP-disable into chip erase, and this test makes that hazard machine-visible one phase before
FIX-04 formalises it.

**Negative B — lock table swapped for the write prefix (TRACE-03b).** Drive the three-write
`FLASH_ENABLE_WRITE_PROTECTION` table where the six-write unlock stream is expected, and assert the
ordered comparison against `SDP_SHIPPED_DIP28_28C256` diverges at the index where the third write's
payload byte differs. Together with Negative A this discharges the phase goal's "the harness can
distinguish lock from unlock from erase".

**Record the LOCK-05 finding as its own case, not as prose.** `FLASH_ENABLE_WRITE_PROTECTION` and
`FLASH_ENABLE_WRITE` are byte-identical in `flash_utils.h`, so no register trace can ever
distinguish them. Add a case asserting the two tables produce element-wise identical streams, with a
comment stating that this duplication is datasheet-correct (Atmel doc0270 §19 note 2), that Phase 119
LOCK-05 requires it be preserved rather than deduplicated, and that a trace-based negative between
*those two specific tables* is therefore impossible by construction — so a later editor must not
"strengthen" the harness by trying to add one.

**Fixed-stream literal arrays (`SDP_FIXED_<PINOUT>[]` in `_shared/sdp_expected.h`).** These are the
post-Phase-117 target streams the parked RED suite (plan 116-06) asserts against. Author them the
same empirical way, from a **reference emitter built out of existing production code**: for each of
the five representative rows, drive `h.firestarter_set_data(&h, address, byte)` once per unlock pair
— that call is exactly what FIX-01 specifies the fixed emitter will be built on, and it routes
through `mem_util_remap_address_bus`, so the resulting stream is the remap-aware target without any
hand derivation. Capture via the `SDP_TRACE_DUMP` helper and transcribe.

**Guard the fixed literals against staleness — the case that makes them trustworthy.** Add a case
per row asserting that driving the reference emitter reproduces `SDP_FIXED_<PINOUT>` element-wise.
This case is green today and stays green after Phase 117 (it exercises `memory_set_data`, which
Phase 117 must not change), so it pins the literal arrays to production behaviour permanently
while leaving the parked suite as the sole RED-to-GREEN signal.

**Cross-check gates against RESEARCH §F5** — stop and report on any disagreement:
- the fixed `(LSB, MSB)` latch values must be `(0x55, 0x95)` / `(0xAA, 0x2A)` for `DIP28_28C256`,
  `(0x55, 0x15)` / `(0xAA, 0x0A)` for `DIP28_28C64`, `(0x55, 0x05)` / `(0xAA, 0x02)` for
  `DIP24_2816`, and `(0x55, 0x55)` / `(0xAA, 0x2A)` for both `DIP32_28C512_EEPROM` rows
- the shipped and fixed streams must have **identical length** on every pinout, confirming that no
  length- or count-based assertion could discriminate them
- on `DIP32_28C512_EEPROM` with a zero CONTROL seed the fixed and shipped address bytes must be
  identical, confirming Pitfall 5 — record this in the summary; it is why plan 116-06's DIP32 cases
  must use a deliberately stale upper-address seed rather than a plain trace.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*" 2>&1 | tail -25 && pio test -e native 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*"` exits 0 with at least 11 passing cases (3 from Task 1, 2 negatives, 1 lock/write-prefix identity, 5 reference-emitter guards)
    - `cd /workspaces/firestarter && pio test -e native` exits 0 with 0 failures
    - Negative A asserts a specific first-divergence index, not merely "streams differ"
    - Negative A also asserts element-wise equality between the mutated-table stream and the real `FLASH_ERASE` stream
    - `SDP_FIXED_DIP28_28C256_LEN` equals `SDP_SHIPPED_DIP28_28C256_LEN` (RESEARCH §F5's identical-length finding, verified in code)
    - Every `SDP_FIXED_<PINOUT>` array has a companion reference-emitter case asserting it element-wise — `grep -c 'SDP_FIXED_' firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp` returns at least 5
    - `grep -c 'LOCK-05' firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp` returns at least 1
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing
  </acceptance_criteria>
  <done>Both in-suite negatives are executable and index-precise, the fixed-stream literals are pinned to real production behaviour by a permanently-green guard, and the datasheet-correct lock/write-prefix duplication is recorded as a test rather than a comment.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the surviving identity-gate assertions onto an address-keyed mock and retire the old suite (TRACE-04, D-12, D-13)</name>
  <files>firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp, firestarter/test/native/avr/test_eeprom28c_chip_id/, firestarter/platformio.ini</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_chip_id/test_eeprom28c_chip_id.cpp (whole file — the handle builder at :69-82, the load-bearing re-assign-after-configure step at :106-109, and the four cases; this whole directory is being deleted)
    - firestarter/test/native/avr/test_eeprom28c_chip_id/ (list the directory; it also contains a `host_stubs.cpp` and an `avr/` shim subdirectory)
    - firestarter/src/proms/eeprom_28c.cpp lines 50-117 — `eeprom28c_check_chip_id` and `eeprom28c_write_init`, to confirm which of the four assertions survive independently of the SDP outcome
    - firestarter/platformio.ini — the KNOWN-FLAKY comment block above `test_filter`, which names the suite being retired, and the two allowlists, to confirm the retired suite has neither a `test_filter` nor an `-I` entry today
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 246-277 (CORRECTION 2 — the executed migration matrix; two tests go RED, not one), lines 555-574 (§F8 — the measured per-address read counts and all three fixture sites), lines 853-860 (Pattern 3 — the address-keyed mock and the both-pointers re-assign hazard)
    - .planning/phases/116-ground-truth-trace-harness/116-CONTEXT.md — D-12 (the migration and split) and D-13 (the SIGABRT flake stays deferred)
  </read_first>
  <action>
Migrate the identity-gate assertions that do **not** hinge on the SDP outcome into the always-green
harness suite, on an address-keyed mock, and delete the directory they came from.

**Address-keyed mock (TRACE-04, Pattern 3).** Add to the harness suite a `firestarter_get_data`
mock that dispatches on the **address** rather than on call order: a `switch` returning virgin
`0xFF` by default, with only the two manufacturer/device identity bytes planted at
`mem_size - 64` and `mem_size - 63`. Maintain two per-address read counters — reads at the
manufacturer address, and reads at the SDP completion-poll address — so any assertion that used to
depend on call ordinality can be re-expressed positionally. Remember that `configure_memory`
overwrites **both** `firestarter_get_data` and `firestarter_set_data`, so both must be re-assigned
after it; the retired suite's own comment covers only the read pointer.

**Cases to migrate here (the always-green half of D-12), per RESEARCH CORRECTION 2's executed
matrix:**
- the mismatching-identity case, asserting `RESPONSE_CODE_ERROR` — it early-returns before the SDP
  sequence, so it is outcome-independent and green
- the zero-identity gate case, **re-expressed** as a per-address read counter: assert the
  manufacturer-address read count is 0 (the identity helper was never called). The old vehicle for
  that assertion was a call-ordinal counter that no longer exists under an address-keyed mock; the
  intent is preserved and is now independent of the SDP outcome. RESEARCH §F8 measured
  `mfr=0, poll=2000, total=2000` for this case.

**Cases that must NOT be migrated here:** the matching-identity case and the
mismatching-identity-with-force case. RESEARCH CORRECTION 2 corrects D-12 on the second of these:
under an address-keyed mock the force case ends in `RESPONSE_CODE_ERROR`, not `WARNING`, because the
SDP wait is unconditional and overwrites the response code. Both belong in the RED-parked suite
(plan 116-06). Do not weaken either assertion to make it fit here — the force/severity fork is
precisely the case the v1.16 Phase-89 CR-01 regression slipped through, and filing it as
always-green would re-open that hole.

**Retire the old suite (D-12, Open Question 4).** Delete the entire
`firestarter/test/native/avr/test_eeprom28c_chip_id/` directory including its `host_stubs.cpp` and
its `avr/` shim subdirectory. Before deleting, confirm no other suite includes anything from it
(it currently has no `-I` entry, so nothing can). Then update the KNOWN-FLAKY comment block in
`platformio.ini`: it names both parked suites, and after this deletion it would reference a
directory that no longer exists. Keep the `test_flash_intel_vpp` half intact — that is live debt
documentation for a suite that still exists — and add one line recording that the `eeprom28c` half
migrated in v1.22 Phase 116. Do not attempt the SIGABRT root cause: D-13 defers it explicitly, and a
new suite aborting with SIGABRT is the ArduinoFake unmocked-virtual problem (Pitfall 3), not that
flake.

**TRACE-04's fourth success criterion** is that the call-ordered scripted fixture "must not survive
in that form anywhere". Deleting the directory removes all three sites; the acceptance criteria
below re-check the whole native test tree so a future reintroduction is caught.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -10 && test ! -d test/native/avr/test_eeprom28c_chip_id && echo DIR-RETIRED</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d firestarter/test/native/avr/test_eeprom28c_chip_id` succeeds — the directory and its `avr/` subdirectory are gone
    - `grep -rl 's_mock_byte_idx' firestarter/test/ | wc -l` returns 0
    - `grep -rn 'wait_for_write(0x5555' firestarter/test/ | wc -l` returns 0 — no comment anywhere in the native test tree still records the inverted check as expected behaviour
    - `cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*"` exits 0 with at least 13 passing cases (11 from Task 2 plus the 2 migrated identity-gate cases)
    - `cd /workspaces/firestarter && pio test -e native` exits 0 with 0 failures
    - The zero-identity case asserts a per-address read count of 0 at the manufacturer address, not a call ordinal
    - `grep -c 'test_flash_intel_vpp' firestarter/platformio.ini` returns at least 1 — the surviving half of the KNOWN-FLAKY debt note was not deleted
    - `grep -c 'Phase 116' firestarter/platformio.ini` returns at least 1 — the migration is recorded where the retired suite used to be named
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing
  </acceptance_criteria>
  <done>TRACE-04 is met: the call-ordered mock is gone from the tree, the outcome-independent identity assertions execute in CI on an address-keyed mock, and the retired directory's debt documentation is corrected rather than erased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expected-stream literals → every Phase-116/117 claim | If the literals are wrong, both suites can agree with each other while diverging from what the shield sees |
| test-local command tables → production tables | A hand-copied table is a transcription that can stale silently |
| `platformio.ini` allowlists → what CI actually runs | A suite absent from the allowlist is silently uncollected with no error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-05-HANDDERIVE | Tampering | expected arrays | critical | mitigate | Arrays authored from a recorded dump of real production code and cross-checked element-by-element against RESEARCH §F4's independently produced stream; a mismatch stops the task |
| T-116-05-SUBSEQ | Tampering | assertion shape | critical | mitigate | Ordered full-stream equality with a named diverging index (D-06); the sub-sequence scan in `test_val_5v_page.cpp:199-217` is an explicit anti-pattern here, and counting anything is forbidden as a discriminator |
| T-116-05-TRANSCRIBE | Tampering | command tables | high | mitigate | Drive the real `flash_utils.h` tables; the only test-local table is the deliberately-mutated negative, and plan 116-03's parity gate pins the header table to the shipped `0x0D` table |
| T-116-05-CACHE | Spoofing | register cache | high | mitigate | `reset_register_cache(0x00, 0x00, 0x00)` after `configure_memory`, commented with why; without it the suite would appear to show the 5V-only path enabling the VPP regulator (Pitfall 1) |
| T-116-05-UNCOLLECTED | Repudiation | `test_filter` allowlist | high | mitigate | Both the `test_filter` line and the `-I` line are acceptance-criteria greps; a suite directory must also start with `test` or PIO silently collects nothing |
| T-116-05-WEAKEN | Tampering | migrated force/severity case | high | mitigate | The force case is explicitly excluded from this suite and routed to the parked one; weakening it to fit here would re-open the v1.16 Phase-89 CR-01 hole |
| T-116-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed. RESEARCH §Package Legitimacy Audit records zero new dependencies |
</threat_model>

<verification>
- `cd /workspaces/firestarter && pio test -e native` → 0 failures, count = 82 + the harness suite's cases
- `cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*"` → exits 0
- `test ! -d firestarter/test/native/avr/test_eeprom28c_chip_id`
- `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` → empty
- Run `pio` from `/workspaces/firestarter`, never from `/workspaces`
</verification>

<success_criteria>
- The always-green harness suite is allowlisted and green (D-03), and proves ordered capture including elision
- TRACE-03's two in-suite negatives are index-precise, and one of them makes the one-nibble chip-erase hazard machine-visible
- The fixed-stream literals are pinned to real production behaviour by a permanently-green guard
- TRACE-04 is met tree-wide: no call-ordered byte-array fixture survives anywhere under `test/`
- D-13's deferred flake is untouched and its surviving documentation is intact
</success_criteria>

<output>
Create `.planning/phases/116-ground-truth-trace-harness/116-05-SUMMARY.md` when done. Record: the
final `pio test -e native` count, whether the captured shipped stream matched RESEARCH §F4 exactly,
the observed diverging-index message from the corrupted-array check, the first-divergence indices
both negatives assert, and confirmation that the DIP32 shipped and fixed streams are identical under
a zero CONTROL seed (which plan 116-06 depends on).
</output>
