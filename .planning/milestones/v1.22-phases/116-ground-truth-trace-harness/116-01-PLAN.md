---
phase: 116-ground-truth-trace-harness
plan: 01
type: execute
wave: 1
depends_on: []
sub_repo: firestarter
files_modified:
  - firestarter/test/native/avr/_shared/host_stubs_common.inc
  - firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp
autonomous: true
requirements: [TRACE-01, TRACE-03]

must_haves:
  truths:
    - "A native suite can opt in to an ordered strobe stream that interleaves data-buffer bytes, latch strobes and /CE + /OE edges, produced by production's own elision logic."
    - "With the extension compiled in but no suite opting in, the 14 pre-existing native suites still report exactly 80 passing assertions and 0 failures."
    - "Issuing a protocol adjacent to 0x0D that is not 0x0D reaches configure_not_implemented() and never reaches configure_eeprom28c."
    - "Both sub-repos are on a v1.22 branch forked off beta before any file in either sub-repo is written."
  artifacts:
    - "firestarter/test/native/avr/_shared/host_stubs_common.inc — extended with the HOST_STUBS_REAL_REGISTER_UTILS opt-in block and four opt-out guards"
    - "firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp — two new fail-closed cases"
    - "branch v1.22-at28c-software-data-protection-lifecycle in firestarter and firestarter_app"
  key_links:
    - "HOST_STUBS_REAL_REGISTER_UTILS must suppress exactly the six symbols the real rurp_register_utils.h / rurp_hw_rev_utils.h pair supplies, or the suite TU will not compile."
    - "The opt-out guards must be #ifndef-shaped so the flag-off preprocessor output for all 14 existing suites is textually unchanged."
---

<objective>
Create the v1.22 branch in both sub-repos, then extend the shared native-test stub layer with a
second opt-in recording flag that captures the **ordered** bus stream (data bytes + latch strobes +
/CE and /OE edges) using production's own register-write elision, and prove the extension is inert
when not opted into. Also land TRACE-03's fourth negative — the plain fail-closed
`protocol != 0x0D` case (D-04 item 4).

Purpose: this is the single edit point every later Phase-116 suite depends on (D-05, D-07, TRACE-01).
Every downstream plan in this phase is blocked until the branch exists and this recorder compiles.

Output: an extended `host_stubs_common.inc`, two new `test_not_implemented` cases, and the recorded
80/80 byte-exactness proof (TRACE-01b).

**Zero production-code risk fence:** this plan writes only under `firestarter/test/`. No file under
`firestarter/src/` or `firestarter/include/` may be modified. The fix is Phase 117.
</objective>

## Artifacts this phase produces

These symbols/paths are **created by Phase 116** and do not exist in the tree yet. Drift
verification must treat them as new, not as unresolved references.

**New firmware suite directories**
- `firestarter/test/native/avr/test_sdp_harness/` (always-green harness suite, D-03)
- `firestarter/test/native/avr/test_eeprom28c_sdp/` (RED, parked out of `test_filter`, D-01)

**New firmware files**
- `test/native/avr/test_sdp_harness/host_stubs.cpp`
- `test/native/avr/test_sdp_harness/test_sdp_harness.cpp`
- `test/native/avr/test_eeprom28c_sdp/host_stubs.cpp`
- `test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp`
- `test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md`
- `test/native/avr/_shared/sdp_expected.h` (literal tuple arrays + shared comparator, D-06)
- `test/native/avr/_shared/sdp_bus_config.h` (GENERATED, `DO NOT EDIT`, D-08/D-10)

**New firmware preprocessor / C symbols**
- `HOST_STUBS_REAL_REGISTER_UTILS` (new opt-in flag), `HOST_STUBS_MAX_STROBES`
- `HOST_STUBS_CUSTOM_CONTROL_PIN`, `HOST_STUBS_CUSTOM_DATA_BUFFER`, `HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`
- `STROBE_KIND_DATA`, `STROBE_KIND_PIN`, `strobe_entry_t`, `strobe_push`
- `clear_strobes()`, `strobe_count()`, `strobe_overflowed()`, `strobe_kind(int)`, `strobe_pin(int)`, `strobe_value(int)`
- `reset_register_cache(uint8_t, uint8_t, rurp_register_t)` (per-suite `host_stubs.cpp`)
- `sdp_strobe_t`, `assert_strobe_stream_equals(...)`, `SDP_SHIPPED_*`, `SDP_FIXED_*`, `*_LEN`
- `sdp_bus_config_row_t`, `SDP_BUS_CONFIGS[]`, `SDP_BUS_CONFIG_COUNT`

**New `platformio.ini` keys** — `test_filter` entry for the harness suite;
`build_flags` `-I test/native/avr/test_sdp_harness` and `-I test/native/avr/test_eeprom28c_sdp`

**New host files**
- `firestarter_app/tools/gen_sdp_bus_config.py`, `firestarter_app/tools/check_no_log_in_sdp_window.py`
- `firestarter_app/tests/test_sdp_bus_config_drift.py`, `firestarter_app/tests/test_sdp_db_invariant.py`
- `firestarter_app/tests/test_sdp_table_parity.py`, `firestarter_app/tests/test_check_no_log_in_sdp_window.py`
- `firestarter_app/tests/fixtures/planted_log_in_window.cpp`

**New host symbols** — env-override seam `FIRESTARTER_SDP_SRC`; `--target` / `--check` argparse
options on `gen_sdp_bus_config.py`

**Retired** — the whole directory `firestarter/test/native/avr/test_eeprom28c_chip_id/`

**New meta files** — `.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md`; a third
⚠ correction block inside `.planning/PROJECT.md`

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@/workspaces/CLAUDE.md
@/workspaces/firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork the v1.22 branch off beta in both sub-repos (F10)</name>
  <files>firestarter (git ref only), firestarter_app (git ref only)</files>
  <read_first>
    - /workspaces/CLAUDE.md — the meta-repo/submodule layout rules
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 611-628 (§F10, the verified ancestry result)
  </read_first>
  <action>
Create the branch `v1.22-at28c-software-data-protection-lifecycle` off `beta` in **both**
sub-repos, before any sub-repo file is written by any Phase-116 plan.

For each of `/workspaces/firestarter` and `/workspaces/firestarter_app`, in this order:

1. Re-verify the base is correct rather than trusting the research: run
   `git merge-base --is-ancestor v1.21-community-chip-validation-command beta` and require exit 0.
   If it exits non-zero in either repo, STOP and report — the standing fork-off-`beta` policy does
   not hold and the operator must decide the base (this project has had two prior base exceptions).
2. Record the current `beta` tip SHA for the summary.
3. Create and check out the branch off `beta` with a single
   `git checkout -b v1.22-at28c-software-data-protection-lifecycle beta`.

Do **not** create or switch any branch in the meta repo — it is already on
`gsd/v1.22-at28c-software-data-protection-lifecycle`.

Do **not** `git stash`, `git checkout -- .`, `git clean`, or otherwise discard the operator's
pre-existing uncommitted work. `firestarter_app` currently carries a modified `.gitignore` plus
several untracked files (`SECURITY.md`, `doc/lockable-proms.md`, `write_test_port.sh`,
`.coverage`, `.planning/config.json`, `write_test_port.sh`). If `git checkout -b` is refused
because a tracked modification would be overwritten, STOP and report the exact refusal — do not
force it.

Do not commit anything in this task; it creates refs only.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git branch --show-current | grep -qx 'v1.22-at28c-software-data-protection-lifecycle' && cd /workspaces/firestarter_app && git branch --show-current | grep -qx 'v1.22-at28c-software-data-protection-lifecycle' && echo BRANCHES-OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && git branch --show-current` prints exactly `v1.22-at28c-software-data-protection-lifecycle`
    - `cd /workspaces/firestarter_app && git branch --show-current` prints exactly `v1.22-at28c-software-data-protection-lifecycle`
    - In each sub-repo, `git merge-base --is-ancestor beta HEAD` exits 0 (the new branch descends from `beta`)
    - In each sub-repo, `git rev-list --count beta..HEAD` prints `0` at the moment of creation
    - `cd /workspaces && git branch --show-current` still prints `gsd/v1.22-at28c-software-data-protection-lifecycle` (meta untouched)
    - `cd /workspaces/firestarter_app && git status --short` still lists the pre-existing ` M .gitignore` entry and the pre-existing untracked files — none were discarded
  </acceptance_criteria>
  <done>Both sub-repos are on the v1.22 branch, each descending from `beta` with zero commits ahead, and no pre-existing local work was destroyed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend host_stubs_common.inc with the ordered strobe recorder (TRACE-01, D-05, D-07)</name>
  <files>firestarter/test/native/avr/_shared/host_stubs_common.inc</files>
  <read_first>
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (whole file — the existing `HOST_STUBS_RECORD_BUS` opt-in block at :42-80 is the structure to copy, and lines :82, :90, :98, :125 are the four stubs to guard)
    - firestarter/include/rurp_register_utils.h lines 1-100 (the cache globals at :12-14, the elision at :24-60, the latch sequencing at :63-89, the cached read at :91-101)
    - firestarter/include/rurp_hw_rev_utils.h (the four HARDWARE_REVISION definitions that collide: :15, :43, :61, :100)
    - firestarter/include/rurp_shield.h lines 50-60 (the single `uint8_t` pin namespace: LSB 0x01, MSB 0x02, OE 0x04, CONTROL 0x08, CE 0x20)
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 200-245 (CORRECTION 1 — the six redefinition errors and why the include is intra-TU, not inter-TU), lines 389-422 (§F3 — the two-hook seam and the recommended entry layout), lines 994-1069 (§Code Examples — the complete verified guard set)
    - firestarter/platformio.ini §`[env:native]` (note `-D HARDWARE_REVISION` is inherited from `[env]`, which is the whole reason CORRECTION 1 exists)
  </read_first>
  <action>
Add a **second** opt-in recording layer to the shared stub file, alongside (not replacing) the
existing `HOST_STUBS_RECORD_BUS` layer. Name the new flag `HOST_STUBS_REAL_REGISTER_UTILS` — it
names the *mechanism* (D-05's real-header include), which is the thing a future editor must not
break.

Restructure the opener currently at `:54` from `#ifdef HOST_STUBS_RECORD_BUS … #else … #endif`
into a three-way chain: `#ifdef HOST_STUBS_REAL_REGISTER_UTILS … #elif defined(HOST_STUBS_RECORD_BUS)
… #else … #endif`. The `#elif` arm must carry the existing recording body **byte-unchanged** and the
`#else` arm the existing no-op `rurp_write_to_register`.

In the new `HOST_STUBS_REAL_REGISTER_UTILS` arm:

- `#define HOST_STUBS_MAX_STROBES 512` (the SDP window measured 54 entries; 512 is ~10× headroom —
  RESEARCH Pitfall 2 measured ~6057 entries when the real timeout poll is left unmocked).
- Internally define three opt-out guards so the real header can supply what the stubs used to:
  `HOST_STUBS_CUSTOM_CONTROL_PIN`, `HOST_STUBS_CUSTOM_DATA_BUFFER`,
  `HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`.
- Define `enum { STROBE_KIND_DATA = 1, STROBE_KIND_PIN = 2 };` and
  `struct strobe_entry_t { uint8_t kind; uint8_t pin; uint8_t value; };` — for `STROBE_KIND_DATA`,
  `value` is the data-buffer byte and `pin` is 0; for `STROBE_KIND_PIN`, `pin` is one of the five
  `rurp_shield.h` constants and `value` is the level (0/1).
- Buffer + count + a **saturation flag**: a static array, `s_strobe_count`, and `s_strobe_overflow`.
  The existing recorder drops silently on overflow; this one must set the flag on the `else` branch
  of the bounds check so a test can state whether it is asserting over a complete or truncated
  stream.
- Mirror the existing accessor-trio shape exactly so test files read identically:
  `extern "C"` `clear_strobes()` (resets count *and* the overflow flag), `strobe_count()`,
  `strobe_overflowed()`, `strobe_kind(int)`, `strobe_pin(int)`, `strobe_value(int)`, plus a
  file-static `strobe_push(kind, pin, value)`.
- Hook exactly **two** functions (D-07's scope — nothing wider):
  `rurp_write_data_buffer(uint8_t)` pushes a `STROBE_KIND_DATA` entry, and
  `rurp_set_control_pin(uint8_t pin, uint8_t state)` pushes a `STROBE_KIND_PIN` entry. Per
  RESEARCH §F3, these two carry latch strobes *and* chip /CE and /OE in one distinguishable
  namespace, so no third hook and no wider taxonomy is needed.
- **Do not** define `rurp_write_to_register` or `rurp_read_from_register` in this arm — the real
  `include/rurp_register_utils.h` supplies both, and the suite's `host_stubs.cpp` includes it
  *after* this file.

Then add the four opt-out wrappers elsewhere in the same file, all `#ifndef`-shaped so the flag-off
preprocessor output is textually unchanged for every existing suite:

- wrap the `rurp_read_from_register` stub (currently `:82-85`) in `#ifndef HOST_STUBS_REAL_REGISTER_UTILS`
- wrap the `rurp_set_control_pin` stub (currently `:90-93`) in `#ifndef HOST_STUBS_CUSTOM_CONTROL_PIN`
- wrap the `rurp_write_data_buffer` stub (currently `:98-100`) in `#ifndef HOST_STUBS_CUSTOM_DATA_BUFFER`
- change the `HARDWARE_REVISION` block opener (currently `:125`) to
  `#if defined(HARDWARE_REVISION) && !defined(HOST_STUBS_CUSTOM_HW_REVISION_BLOCK)`

Add a documentation comment block for the new flag in the same voice and shape as the existing
`:46-53` block. It must state: (a) the flag is opt-IN, define it **before** including this file;
(b) it exists so a suite can `#include "rurp_register_utils.h"` and get production's real
cache-compare elision and latch sequencing instead of a replica (D-05); (c) it suppresses six
symbols the real header pair supplies — name them: `rurp_write_to_register`,
`rurp_read_from_register`, `rurp_map_ctrl_reg_for_hardware_revision`,
`rurp_get_physical_hardware_revision`, `rurp_detect_hardware_revision`, `rurp_get_hardware_revision`;
(d) the `HARDWARE_REVISION` collision only exists because `[env:native]` inherits
`-D HARDWARE_REVISION` from `[env]`'s `build_flags`; (e) no existing suite may define this flag —
flag off must stay byte-exact.

Do not touch `firestarter/src/` or `firestarter/include/`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native` reports **80 test cases, 0 failures** across the 14 allowlisted suites — the identical count and result recorded as the pre-phase baseline (TRACE-01b, the byte-exactness half of TRACE-01)
    - `grep -c 'HOST_STUBS_REAL_REGISTER_UTILS' firestarter/test/native/avr/_shared/host_stubs_common.inc` returns at least 5 (opener, three internal guards' comment block, and the `#ifndef` on the read stub)
    - `grep -c 'strobe_overflowed' firestarter/test/native/avr/_shared/host_stubs_common.inc` returns 1
    - `grep -vE '^\s*[*/]' firestarter/test/native/avr/_shared/host_stubs_common.inc | grep -c 'HOST_STUBS_MAX_STROBES 512'` returns 1
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing (zero production-code risk fence)
    - `cd /workspaces/firestarter && git diff beta..HEAD -- test/native/avr/_shared/host_stubs_common.inc | grep -c '^-'` shows only the four guard-line rewrites plus the opener rewrite — no deletion of any existing stub body
  </acceptance_criteria>
  <done>The new opt-in layer compiles into every native suite, is inert when not opted into (80/80 unchanged), and suppresses exactly the six symbols the real header pair will supply.</done>
</task>

<task type="auto">
  <name>Task 3: TRACE-03d — fail-closed cases for protocols adjacent to 0x0D (D-04 item 4)</name>
  <files>firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp (whole file — copy its `make_handle(protocol, mem_type, cmd)` builder, its four-assertion case body, and its explicit `RUN_TEST` list in `main()`)
    - firestarter/src/proms/memory.cpp — `configure_memory`, to confirm which protocol values reach `configure_eeprom28c` and which fall to the terminal fail-closed arm
    - firestarter/CLAUDE.md §"Protocol Dispatch" (the numbered dispatch order and the fail-closed invariant)
    - .planning/phases/116-ground-truth-trace-harness/116-CONTEXT.md — D-04's fourth bullet ("a plain positive test — existing `test_not_implemented` pattern"); this negative deliberately needs no fault injection
  </read_first>
  <action>
Add two cases to the existing `test_not_implemented` suite proving that a protocol value adjacent to
`0x0D` but not equal to it never reaches `configure_eeprom28c` and instead fail-closes to
`configure_not_implemented()` / `MSG_ERR_PROTOCOL_NOT_IMPLEMENTED (0xBB)`.

Use protocol `0x0C` and protocol `0x0F` — the two nearest unassigned neighbours of `0x0D` in the
dispatch chain. For each: build the handle with the file's existing `make_handle(protocol, 0, CMD_WRITE)`
builder, call `configure_memory(&h)`, then assert `RESPONSE_CODE_ERROR` and all three of
`firestarter_operation_init` / `firestarter_operation_main` / `firestarter_operation_end` NULL —
exactly the four-assertion body the file's existing cases use. `CMD_WRITE` (not `CMD_READ`) is the
right command here because `CMD_WRITE` is precisely the command that *would* install
`eeprom28c_write_init` if dispatch leaked.

Register both with `RUN_TEST` in `main()` under a new section comment in the file's existing style,
naming this as TRACE-03d / D-04 item 4 and stating the contract in one sentence: only
`protocol == 0x0D` may reach `configure_eeprom28c`; every other value fail-closes with zero hardware
side effects.

This raises the native assertion count from 80 to 82. Record that in the plan summary — downstream
Phase-116 plans must compare against 82, not 80.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_not_implemented*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native -f "*test_not_implemented*"` exits 0 and the suite's passing-case count is exactly 2 higher than before this task
    - `cd /workspaces/firestarter && pio test -e native` reports **82 test cases, 0 failures**
    - `grep -c 'RUN_TEST' firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp` is exactly 2 higher than the pre-task value
    - `grep -c 'TRACE-03d' firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp` returns at least 1
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing
  </acceptance_criteria>
  <done>`pio test -e native` is 82/82 and two executable cases prove the `protocol != 0x0D` fail-closed path, satisfying TRACE-03's fourth negative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → firmware production headers | The new flag pulls a real production header into a test TU; a mismatch silently changes what the oracle observes |
| build config (`-D HARDWARE_REVISION`) → stub layer | An inherited build flag decides which symbols collide; a change there breaks the guard set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-01-HOLLOW | Tampering | new opt-in recorder | high | mitigate | The recorder sets an explicit `s_strobe_overflow` flag instead of dropping silently, so a saturated stream can never be mistaken for a short one (RESEARCH Pitfall 2) |
| T-116-01-DRIFT | Tampering | elision replica | high | mitigate | D-05: `#include` the real `rurp_register_utils.h` rather than replicating its cache-compare; the flag name encodes the mechanism so a later editor cannot swap in a replica without renaming the flag |
| T-116-01-REGRESS | Denial of Service | 14 pre-existing suites | high | mitigate | Every guard is `#ifndef`/`#elif`-shaped; the 80/80 run is a hard acceptance criterion before the phase proceeds |
| T-116-01-LEAK | Elevation of Privilege | test code in production build | low | accept | Structurally prevented: `env:uno`/`env:leonardo` `src_filter` excludes `test/`; this plan writes only under `test/` |
| T-116-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan or this phase — RESEARCH §Package Legitimacy Audit records zero new dependencies on either side of the wire. Any plan proposing one is a scope breach |
</threat_model>

<verification>
- `cd /workspaces/firestarter && pio test -e native` → 82/82 (80 pre-existing + 2 new fail-closed cases)
- Both sub-repos on `v1.22-at28c-software-data-protection-lifecycle`, each descending from `beta`
- `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` empty
- Run `pio` from `/workspaces/firestarter`, never from `/workspaces` — the untracked
  `/workspaces/platformio.ini` is malformed (duplicate `[platformio]` section) and any `pio`
  invocation from the meta root fails with `InvalidProjectConfError`
</verification>

<success_criteria>
- Branch setup done in both sub-repos before any sub-repo file write (F10, quality gate)
- The `HOST_STUBS_REAL_REGISTER_UTILS` opt-in layer exists, hooks exactly `rurp_write_data_buffer`
  and `rurp_set_control_pin`, and exposes the six-accessor API plus a saturation flag
- Flag-off byte-exactness demonstrated at exactly 80/80 (TRACE-01b)
- TRACE-03's fourth negative lands as two executable, allowlisted cases
</success_criteria>

<output>
Create `.planning/phases/116-ground-truth-trace-harness/116-01-SUMMARY.md` when done. Record: the
`beta` tip SHA in each sub-repo, the exact `pio test -e native` counts before Task 3 (must be 80)
and after (must be 82), and the new flag/accessor names for downstream plans.
</output>
