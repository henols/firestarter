---
phase: 116-ground-truth-trace-harness
plan: 07
type: execute
wave: 5
depends_on: ["116-02", "116-03", "116-04", "116-05", "116-06"]
sub_repo: meta (.planning only)
files_modified:
  - .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md
  - .planning/PROJECT.md
autonomous: false
requirements: [TRACE-06]

must_haves:
  truths:
    - "The full non-regression set is green: the native suite, host pytest, ruff and ruff format against the py3.9 CI target, and chip-database identity."
    - "A written artifact states, with evidence produced by this phase's harness, whether a 0x0D write aborts at INIT on the current tree — and states the validation ceiling that bounds the claim."
    - "PROJECT.md carries a third correction block replacing the all-84 framing with the measured per-pinout 66-of-84 figure, so the six downstream phases read a corrected premise."
    - "Every claim in both documents is software-layer; none asserts what AT28C silicon did."
  artifacts:
    - ".planning/phases/116-ground-truth-trace-harness/116-PREMISE.md"
    - ".planning/PROJECT.md — a third ⚠ correction block under §Current Milestone: v1.22"
  key_links:
    - "The premise artifact must cite evidence re-runnable from the parked suite and RED-BASELINE.md, not a transcript pasted from research."
    - "Overclaiming here is the phase's highest-severity real harm: an honest-looking artifact asserting more than it proved, read by six downstream researchers."
---

<objective>
Close the phase: run the full non-regression gate, then settle TRACE-06 in writing — a premise
artifact stating whether `firestarter write at28c256` aborts at INIT on `3.0.0b11`, and the third
⚠ correction block in `PROJECT.md` that finding implies (D-14).

Purpose: the milestone's highest-value PREDICTED claim becomes a recorded, ceiling-bounded finding
one phase early, and the framing error every downstream phase would otherwise inherit — "all 84
chips" where the measured figure is 66 of 84 — is corrected in the document those phases read.

Output: `116-PREMISE.md`, a corrected `PROJECT.md`, and an operator sign-off on the wording.

**Zero production-code risk fence:** this plan writes only into `.planning/` in the meta repo. No
sub-repo file is modified.
</objective>

## Artifacts this phase produces

These symbols/paths are **created by Phase 116** and do not exist in the tree yet. Drift
verification must treat them as new, not as unresolved references.

**New firmware suite directories** — `firestarter/test/native/avr/test_sdp_harness/`,
`firestarter/test/native/avr/test_eeprom28c_sdp/`

**New firmware files** — `test_sdp_harness/host_stubs.cpp`, `test_sdp_harness/test_sdp_harness.cpp`,
`test_eeprom28c_sdp/host_stubs.cpp`, `test_eeprom28c_sdp/test_eeprom28c_sdp.cpp`,
`test_eeprom28c_sdp/RED-BASELINE.md`, `_shared/sdp_expected.h`, `_shared/sdp_bus_config.h` (GENERATED)

**New firmware preprocessor / C symbols** — `HOST_STUBS_REAL_REGISTER_UTILS`,
`HOST_STUBS_MAX_STROBES`, `HOST_STUBS_CUSTOM_CONTROL_PIN`, `HOST_STUBS_CUSTOM_DATA_BUFFER`,
`HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`, `STROBE_KIND_DATA`, `STROBE_KIND_PIN`, `strobe_entry_t`,
`strobe_push`, `clear_strobes()`, `strobe_count()`, `strobe_overflowed()`, `strobe_kind(int)`,
`strobe_pin(int)`, `strobe_value(int)`, `reset_register_cache(...)`, `sdp_strobe_t`,
`sdp_assert_stream_equals(...)`, `sdp_first_divergence(...)`, `SDP_SHIPPED_*`, `SDP_FIXED_*`,
`*_LEN`, `sdp_bus_config_row_t`, `SDP_BUS_CONFIGS[]`, `SDP_BUS_CONFIG_COUNT`

**New `platformio.ini` keys** — `test_filter` entry for the harness suite; `build_flags`
`-I test/native/avr/test_sdp_harness` and `-I test/native/avr/test_eeprom28c_sdp`

**New host files** — `tools/gen_sdp_bus_config.py`, `tools/check_no_log_in_sdp_window.py`,
`tests/test_sdp_bus_config_drift.py`, `tests/test_sdp_db_invariant.py`,
`tests/test_sdp_table_parity.py`, `tests/test_check_no_log_in_sdp_window.py`,
`tests/fixtures/planted_log_in_window.cpp`

**New host symbols** — env-override seam `FIRESTARTER_SDP_SRC`; `--target` and `--check` argparse
options on `gen_sdp_bus_config.py`

**Retired** — the whole directory `firestarter/test/native/avr/test_eeprom28c_chip_id/`

**New meta files** — `.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md`; a third
⚠ correction block inside `.planning/PROJECT.md`

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@/workspaces/CLAUDE.md
@.planning/phases/116-ground-truth-trace-harness/116-05-SUMMARY.md
@.planning/phases/116-ground-truth-trace-harness/116-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full non-regression gate + datasheet-presence audit</name>
  <files>(no file written; results recorded in the plan summary and consumed by Task 2)</files>
  <read_first>
    - .planning/phases/116-ground-truth-trace-harness/116-VALIDATION.md — the sampling-rate and phase-gate contract this task discharges
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 1269-1294 (§Environment Availability — the Python 3.12-versus-CI-py3.9/3.11 trap and the malformed meta-root `platformio.ini`), lines 1257-1266 (Open Question 5 — the unresolved datasheet prerequisite this task settles)
    - .planning/STATE.md — the four known pre-existing host test failures that must not be mistaken for regressions
  </read_first>
  <action>
Run the phase gate and record every result verbatim for the summary. Do not fix anything discovered
here inside this task — report it. A failure in this gate is a phase-level finding, not a cleanup
opportunity.

**Firmware leg** — from `/workspaces/firestarter` (never from `/workspaces`; the untracked meta-root
`platformio.ini` is malformed with a duplicate `[platformio]` section and any `pio` invocation there
fails with `InvalidProjectConfError`):
- `pio test -e native` — must exit 0 with 0 failures. Record the case count and compare it against
  the 80-case pre-phase baseline plus everything Phase 116 added.
- `pio run -e leonardo` and `pio run -e uno` — must build. This is the structural proof that no test
  code leaked into a production build; the production `src_filter` excludes `test/`, and this
  confirms it stayed true.

**Host leg** — from `/workspaces/firestarter_app`:
- `python -m pytest -x -q` on the full suite. The only acceptable failures are the four pre-existing
  ones recorded in STATE.md: the `test_audit_coverage_matrix` golden drift, and the three
  characterization tests that go red only with a live board attached. Any other failure is a
  regression — name it and stop.
- `ruff check .` and `ruff format --check .` — the CI targets are py3.9/3.11 while the devcontainer
  runs 3.12, and `pyproject.toml` pins `target-version = "py39"`. Confirm the six new Phase-116
  Python files are clean. Four pre-existing `tools/*.py` lint failures are known debt from before
  this phase; distinguish them from anything new by checking whether the offending file is inside
  this phase's diff.
- `python tools/check_dispatch.py` — must exit 0.
- `python tools/diff_db.py` — must report no differences. `chip_database.json` is byte-untouched by
  this phase and that is a GATE-03 / CLOSE-01 concern.

**Scope-fence leg** — in both sub-repos:
- `git diff --name-only beta..HEAD -- src/ include/` in `firestarter` must be empty
- `git diff --name-only beta..HEAD -- firestarter/` in `firestarter_app` must be empty
- neither sub-repo may show a change to `chip_database.json`, `messages.toml`, `messages.h`, or
  `messages.py` — Phase 116 adds no message ID, and a proposal to add one is a signal the work
  drifted into Phase 118

**Datasheet-presence audit (RESEARCH Open Question 5).** This is the one CONFLICT-1 prerequisite
Phase 116 was asked to carry, and it blocks no TRACE requirement. List
`firestarter_app/datasheets/` and record exactly which PDFs are present and which of the citations
of record are absent — specifically DS20006432B (AT28C64B), DS20006386B (AT28C256) and Atmel
doc0270. Record the finding as an explicit absence rather than silence; Task 2 carries it into
`116-PREMISE.md` so the citation status is honest at the point of use.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -8 && pio run -e leonardo 2>&1 | tail -3 && cd /workspaces/firestarter_app && python -m pytest -q 2>&1 | tail -8 && python tools/diff_db.py 2>&1 | tail -3 && python tools/check_dispatch.py 2>&1 | tail -3 && ls datasheets/</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native` exits 0 with 0 failures, and the recorded case count equals 80 plus every case Phase 116 added
    - `cd /workspaces/firestarter && pio run -e leonardo` and `pio run -e uno` both succeed
    - `cd /workspaces/firestarter_app && python -m pytest -q` shows no failures other than the four pre-existing ones named in STATE.md
    - `ruff check` and `ruff format --check` are clean on all six new Phase-116 Python files
    - `cd /workspaces/firestarter_app && python tools/diff_db.py` reports no differences and `python tools/check_dispatch.py` exits 0
    - `cd /workspaces/firestarter && git diff --name-only beta..HEAD -- src/ include/` prints nothing
    - `cd /workspaces/firestarter_app && git diff --name-only beta..HEAD -- firestarter/` prints nothing
    - The datasheet audit is recorded as a concrete present/absent list, not as a general statement
  </acceptance_criteria>
  <done>Every leg of the phase gate has a recorded verbatim result, the scope fences are proven, and the datasheet prerequisite is settled as a documented presence or absence.</done>
</task>

<task type="auto">
  <name>Task 2: 116-PREMISE.md + the third ⚠ correction block in PROJECT.md (TRACE-06, D-14, CORRECTION 4)</name>
  <files>.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/notes/dev-test-unknown-chip-fail-fast.md (whole file — the target prose shape named by CONTEXT §specifics: front-matter block, the finding, the two cases, the decision, and a "Why the distinction is load-bearing" section so a later editor does not undo it)
    - .planning/PROJECT.md §"Current Milestone: v1.22" — the two existing ⚠ correction blocks at lines 53 and 59; match their heading level, ⚠ prefix, dated-attribution form and voice
    - firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md (written by plan 116-06 — the evidence this artifact cites)
    - .planning/REQUIREMENTS.md §"Validation Ceiling" — the exact permitted claim and the exact forbidden claim
    - .planning/phases/116-ground-truth-trace-harness/116-RESEARCH.md lines 338-377 (§F1 — the measured INIT-abort result and the verbatim permitted wording), lines 313-333 (CORRECTION 4 — the per-pinout 66-of-84 table), lines 982-989 (Pitfall 7 — the overclaiming failure mode and its warning sign)
    - .planning/phases/116-ground-truth-trace-harness/116-06-SUMMARY.md — the observed response codes and per-case RED reasons
  </read_first>
  <action>
Write the premise artifact and apply the PROJECT.md correction it implies.

**`116-PREMISE.md`** — modelled on `.planning/notes/dev-test-unknown-chip-fail-fast.md`. Sections:

1. **The question.** Does `firestarter write at28c256` abort at INIT on `3.0.0b11`? Milestone
   research listed this as the highest-value PREDICTED claim to settle first.
2. **The finding, at the software layer.** `eeprom28c_write_init` returns `RESPONSE_CODE_ERROR`
   before any data byte is transferred, for all four `0x0D` pinouts. Give the mechanism in one
   traced paragraph: the completion check polls the command address expecting the sequence's own
   terminal byte, never observes it, exhausts its 2000-iteration loop, emits the EEPROM-timeout
   message and sets the error code; `eeprom28c_write_init` then returns early, so the blank check
   never runs and no data byte is ever sent.
3. **The evidence, and how to re-run it.** Cite the parked suite and `RED-BASELINE.md` by path, with
   the exact command that reproduces the result (adding the suite's one allowlist line and running
   the native environment). Cite the observed response code per pinout from plan 116-06's summary.
   This must be evidence produced by **this phase's harness**, not a transcript copied from research.
4. **The validation ceiling — stated, not implied.** Use RESEARCH §F1's permitted wording. Say
   software-layer. Name the datasheet bridge as a **citation, not an observation**, and record what
   Task 1's audit found about which of those PDFs are actually in the tree. State plainly that no
   AT28C part was on the bench and that this is not a silicon-state claim. Then state the forbidden
   claims explicitly so a later reader cannot mistake the artifact's scope. Pitfall 7's warning sign
   is the review test: no sentence in this document may have the chip as its subject rather than the
   code.
5. **CORRECTION 4 — the framing error this phase corrects.** `REQUIREMENTS.md` §Framing and the
   research summary both state the write-inhibit affects "all 84" `0x0D` chips. The measured figure
   is **66 of 84**: `DIP28_28C64` ×35 inhibited on 4 of 6 writes, `DIP24_2816` ×19 on 2 of 6,
   `DIP28_28C256` ×12 on 4 of 6, and `DIP32_28C512_EEPROM` ×18 on 0 of 6 at INIT-time register
   state. Add the sub-finding that `DIP24_2816` is inhibited on the *opposite* writes from the DIP28
   pinouts, so any prose saying "the `0x5555` writes are inhibited" is wrong for 19 chips.
6. **Why the distinction is load-bearing.** Two consequences a later editor must not undo: the
   DIP32 band is inhibit-free at INIT-time state, which is exactly why plan 116-06's DIP32 cases are
   stale-upper-address cases rather than plain traces — deleting that framing makes those cases
   decorative; and because the INIT abort is unconditional, there is no working `write` behaviour on
   this family to preserve, which voids the backward-compatibility objection to changing the
   auto-unlock default.

**`PROJECT.md`** — add a third ⚠ correction block under `## Current Milestone: v1.22`, immediately
after the existing second reframing block, using a scoped edit that touches nothing else in the
file. Match the two existing blocks' heading level, ⚠ prefix, dated attribution and voice. It must:
carry the **66 of 84** figure with the per-pinout breakdown; state the confirmed INIT-abort finding
in ceiling-compliant wording; point at `116-PREMISE.md` and at the firmware sub-repo's
`RED-BASELINE.md` for the evidence; and state that the "all 84" wording in `REQUIREMENTS.md`
§Framing and in the research summary is superseded. Leave both existing correction blocks byte-
unchanged — they are provenance.

Do not modify `REQUIREMENTS.md` itself; superseding it in PROJECT.md is the established idiom here
and the requirements file is the milestone's signed scope record.
  </action>
  <verify>
    <automated>test -s .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md && grep -c '66 of 84' .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md && grep -c '66 of 84' .planning/PROJECT.md && grep -c '⚠' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md` exists and contains all six sections named above
    - `grep -c '66 of 84' .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md` returns at least 1, and the per-pinout breakdown 35 / 19 / 12 / 18 appears
    - The artifact cites `RED-BASELINE.md` by path and gives the exact re-run command
    - The artifact states the validation ceiling explicitly, names the datasheet bridge as a citation, records Task 1's PDF presence/absence finding, and contains no sentence whose subject is the chip rather than the code
    - `.planning/PROJECT.md` §"Current Milestone: v1.22" now contains **three** ⚠ correction blocks, and `git diff .planning/PROJECT.md` shows the two pre-existing blocks byte-unchanged
    - `git diff --name-only -- .planning/REQUIREMENTS.md` prints nothing — the requirements record is superseded in PROJECT.md, not rewritten
    - `git diff --stat` in both sub-repos is empty for this task — it writes only into the meta repo
  </acceptance_criteria>
  <done>TRACE-06 is settled in writing with re-runnable evidence and an explicit ceiling, and the six downstream phases now read a corrected premise carrying the measured 66-of-84 figure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING operator review of the premise wording (TRACE-06 / D-14, the phase's one manual-only verification)</name>
  <read_first>
    - .planning/phases/116-ground-truth-trace-harness/116-PREMISE.md (written in Task 2)
    - .planning/PROJECT.md §"Current Milestone: v1.22" (the three ⚠ blocks)
    - .planning/REQUIREMENTS.md §"Validation Ceiling"
  </read_first>
  <action>
Present both documents to the operator and BLOCK until they explicitly approve the wording or name
the sentences that must change. Do not mark TRACE-06 complete, do not write the phase summary's
"settled" language, and do not advance the phase before the resume signal. Capture the operator's
verdict verbatim in the plan summary. If the operator names corrections, apply exactly those
corrections to `116-PREMISE.md` and/or the PROJECT.md block and re-present — no silent
auto-approval, and no rewording beyond what was asked for.
  </action>
  <verify>
    <human-check>Operator has typed "approved", or has named the sentences to change and confirmed the reworded versions.</human-check>
  </verify>
  <done>The operator has explicitly approved the wording of `116-PREMISE.md` and the third PROJECT.md ⚠ correction block, and the verdict is recorded in the plan summary.</done>
  <what-built>
`116-PREMISE.md` and a third ⚠ correction block in `PROJECT.md`, both derived from this phase's
harness evidence. Everything else in Phase 116 is machine-verified; the accuracy of a premise
correction's *wording* is a human judgement, and this is the one manual-only verification
`116-VALIDATION.md` records. It matters disproportionately: `PROJECT.md` is read by every researcher
for Phases 117 through 122, and RESEARCH's security review names an honest-looking artifact that
asserts more than it proved as this phase's highest-likelihood real harm.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/116-ground-truth-trace-harness/116-PREMISE.md`.
2. Read the new (third) ⚠ block in `.planning/PROJECT.md` §"Current Milestone: v1.22", and confirm
   the two older blocks are unchanged.
3. Check the claim boundary. `.planning/REQUIREMENTS.md` §"Validation Ceiling" gives the permitted
   claim and the forbidden claim verbatim. Confirm both documents stay on the permitted side, and
   apply the one-line test from RESEARCH Pitfall 7: **no sentence may have the chip as its subject
   rather than the code.**
4. Check the figure. Both documents must say **66 of 84**, not "all 84", with the per-pinout
   breakdown 35 / 19 / 12 / 18.
5. Check the datasheet honesty. `firestarter_app/datasheets/` contains `AT28C256.pdf`,
   `SST39SF0x0A.pdf` and `W27C020.pdf` — the DS20006432B, DS20006386B and doc0270 citations of
   record are not in the tree. Confirm the premise artifact says so rather than implying the PDFs
   were read.
6. Optionally re-run the evidence yourself: add the parked suite's one line to `test_filter` in
   `firestarter/platformio.ini`, run `cd firestarter && pio test -e native`, observe the seven RED
   cases, then remove the line again.
  </how-to-verify>
  <resume-signal>Type "approved", or describe which sentence crosses the ceiling and what it should say instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase evidence → written claim | The step where a measured software result can quietly become an unearned silicon claim |
| `PROJECT.md` → six downstream phases | Every Phase 117-122 researcher reads this file; an error here propagates for the rest of the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-07-OVERCLAIM | Repudiation | `116-PREMISE.md` and the PROJECT.md block | critical | mitigate | RESEARCH §F1's permitted wording used verbatim; the ceiling stated inside each artifact; the datasheet bridge presented as a citation with its in-tree absence recorded; a blocking human checkpoint applying Pitfall 7's subject test |
| T-116-07-STALEFRAME | Tampering | the "all 84" framing | high | mitigate | The measured 66-of-84 per-pinout table lands in the document downstream phases read, and explicitly marks the older wording superseded |
| T-116-07-PROVENANCE | Tampering | the two existing ⚠ blocks | medium | mitigate | Scoped edit only; a `git diff` acceptance criterion proves both older blocks are byte-unchanged |
| T-116-07-LEAK | Elevation of Privilege | test code in a production build | low | mitigate | `pio run -e leonardo` and `pio run -e uno` are gate legs, confirming the production `src_filter` still excludes `test/` |
| T-116-07-DBWRITE | Tampering | `chip_database.json` | high | mitigate | `diff_db.py` identity is a gate leg (GATE-03 / CLOSE-01) |
| T-116-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed anywhere in this phase. RESEARCH §Package Legitimacy Audit records zero new dependencies; a proposal to add one is a scope breach |
</threat_model>

<verification>
- Full gate green: native suite, both production builds, host pytest (four known pre-existing
  failures only), ruff and ruff format against the py3.9 target, `check_dispatch.py`, `diff_db.py`
- `116-PREMISE.md` exists, cites re-runnable evidence, and states the ceiling
- `PROJECT.md` carries three ⚠ blocks with the two older ones byte-unchanged
- Operator has approved the wording at the blocking checkpoint
- Run `pio` from `/workspaces/firestarter`, never from `/workspaces`
</verification>

<success_criteria>
- The phase gate has a recorded verbatim result for every leg
- TRACE-06 is settled in writing, bounded by the validation ceiling, with re-runnable evidence
- D-14's third ⚠ correction block lands in this phase, carrying the measured 66-of-84 figure
- The wording has passed a blocking human review
</success_criteria>

<output>
Create `.planning/phases/116-ground-truth-trace-harness/116-07-SUMMARY.md` when done. Record: every
gate leg's verbatim result, the final native case count, the datasheet present/absent list, and the
operator's checkpoint verdict.
</output>
