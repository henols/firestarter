---
phase: 118-observe-auto-unlock-visible-opt-out-able-fw-half
plan: 03
type: execute
wave: 3
depends_on: [118-02]
files_modified:
  - firestarter/include/firestarter.h
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp
autonomous: true
requirements: [OBS-02, OBS-03]

must_haves:
  truths:
    - "D-09: `AT28C_TBLC_MAX_US = 100` exists as a real `#define` in `eeprom_28c.cpp`, adjacent to its sibling `AT28C_TWC_MAX_MS`, and is destined to be a RUNTIME BUDGET CHECK rather than a documentation-only constant. This plan lands the constant and its citations; Plan 118-04 lands the check that makes it load-bearing. Rejected: a documentation-only constant — prose-only satisfaction of OBS-03 is the hollow-gate debt shape this project keeps paying down."
    - "D-10: the constant is cited at BOTH sites the t_BLC window touches — the SDP unlock emit path and `eeprom28c_write_execute`'s per-byte `set_data` loop — while the runtime check stays scoped to the unlock only. The page-load citation is comment-only, with no code change and no per-byte compare in the hot path. Rejected: silence at the page-load site; rejected: checking both."
    - "D-10 framing: the page-load citation must be written as documenting a SHARED physical constraint, and must NOT be written as if it fixes or explains gh#11. Phase 117 established gh#11 is a CONFLATION bug, not a sampling-rate bug."
    - "`FLAG_SKIP_SDP_UNLOCK` is defined as `0x100` in `firestarter/include/firestarter.h`. `ctrl_flags` is already `uint32_t` and `get_flags` already uses `extract_long`, so no struct widening and no `json_parser.c` change is needed or permitted."
    - "The host FLAG-parity test is confirmed non-exhaustive by assertion, not by assumption: `firestarter_app/tests/test_revision_constants_parity.py` imports and asserts exactly eight hardcoded literals under a `FW_ABSENT` skipif and never enumerates the header, so a firmware-only ninth flag does not trip it."
    - "`FLAG_SKIP_SDP_UNLOCK` is NOT added to `firestarter_app/firestarter/constants.py` — that is Phase 120 HOST-03 and firmware-before-host is non-negotiable."
    - "A `micros` mock is present in every native suite that drives `eeprom28c_write_init`, because `micros()` appears nowhere in the firmware today and ArduinoFake aborts with SIGABRT on an unmocked method call."
    - "This plan changes no behaviour: the native suite result and both board flash figures are unchanged from the pre-plan baseline."
  artifacts:
    - firestarter/include/firestarter.h
    - firestarter/src/proms/eeprom_28c.cpp
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp
  key_links:
    - "`AT28C_TWC_MAX_MS`'s existing comment at `eeprom_28c.cpp:35-41` already forward-declares `AT28C_TBLC_MAX_US = 100` by name and draws the t_BLC-vs-t_WC distinction — extend that block, do not restate the distinction."
    - "`firestarter_app/tests/test_sdp_table_parity.py` scans `eeprom_28c.cpp` source text and was broken three times by Phase 117's identifier changes. It must be re-run explicitly after every edit to that file."
    - "`test_val_eeprom28c` reaches the write path via the page-write execute path, not via `eeprom28c_write_init` — confirm that before deciding it needs no `micros` mock."
---

<objective>
Land the two new firmware constants and the two `AT28C_TBLC_MAX_US` citations, and give every native
suite that drives `eeprom28c_write_init` a `micros` mock — with zero behaviour change.

Purpose: this is the deliberately inert half of the firmware work. Separating it from Plan 118-04's
behaviour change means the flag bit, the constant, and the ArduinoFake mocking prerequisite can each
be proven in isolation against an unchanged native suite result and an unchanged flash figure, so
Plan 118-04's flash delta and suite delta are attributable to the report lines alone.

Output: `FLAG_SKIP_SDP_UNLOCK`, `AT28C_TBLC_MAX_US`, two citations, two `micros` mocks.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-CONTEXT.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md
</context>

## Repo and commit mechanics

Every file in this plan lives in the **`firestarter` submodule** at `/workspaces/firestarter`
(branch `v1.22-at28c-software-data-protection-lifecycle`; confirm with
`git -C /workspaces/firestarter branch --show-current` before the first write). Commit **inside that
submodule**. Do not touch the meta repo and do not bump the gitlink.

Every `pio` invocation in this phase runs against the single shared `/workspaces/firestarter/.pio/`
build tree, which is why all Phase 118 plans are strictly sequential rather than merely
wave-ordered. Do not run `pio` concurrently with any other plan.

## Artifacts this phase produces

**Created by THIS plan:** `FLAG_SKIP_SDP_UNLOCK` = `0x100` (`firestarter/include/firestarter.h`);
`AT28C_TBLC_MAX_US` = `100` (`firestarter/src/proms/eeprom_28c.cpp`).

**Created elsewhere in Phase 118** (listed so a drift check does not flag them as unresolved):
catalog ids `MSG_INFO_SDP_UNLOCK` (0x5E), `MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F),
`MSG_WARN_SDP_UNLOCK_SKIPPED` (0x86), `MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) — Plan 118-02, already
landed and available in `firestarter/include/messages.h` by the time this plan runs; firmware locals
`sdp_seq_len`, `sdp_emit_start_us`, `sdp_emit_us` — Plan 118-04; host gate identifiers
`_EMITTER_FUNC_NAME`, `_POLL_FUNC_NAME`, `_resolve_windows`, widened
`_find_function_body(cleaned_text, func_name)` — Plan 118-01; native test cases
`test_case9_skip_flag_suppresses_unlock_stream`,
`test_case10_flag_absent_emits_full_unlock_stream`, `test_case11_tblc_budget_exceeded_warns`,
`test_case12_flag_absent_emits_exactly_two_report_frames` and helpers
`make_sdp_handle_with_flags`, `s_micros_ticks`, `captured_frames` — Plan 118-05; host test cases
`test_checker_exits_nonzero_on_log_planted_in_completion_poll_body`,
`test_checker_fails_closed_when_emitter_body_is_absent` — Plan 118-01;
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md` —
Plan 118-07.

## Requirement ownership (do NOT mark these Complete here)

This plan lands the flag *definition* half of **OBS-02** and the *constant plus citations* half of
**OBS-03**. Both stay **open** in `.planning/REQUIREMENTS.md`: OBS-02 needs Plan 118-04 (firmware
actually honouring the flag) and Plan 118-05 (the trace proving the sequence's total absence);
OBS-03 needs Plan 118-01 (the source-scan gate, already landed), Plan 118-04 (the runtime budget
check) and Plan 118-05 (the budget-WARN anti-hollow proof). A `#define` nobody reads satisfies
nothing.

<tasks>

<task type="auto">
  <name>Task 1: Define FLAG_SKIP_SDP_UNLOCK 0x100 and AT28C_TBLC_MAX_US 100</name>
  <files>firestarter/include/firestarter.h, firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/include/firestarter.h lines 50-110 (the `FLAG_*` block at :59-68 where `FLAG_VERBOSE 0x80` is the highest used value; `is_flag_set` at :70-71; `ctrl_flags` declared `uint32_t` at :96)
    - firestarter/src/proms/eeprom_28c.cpp lines 19-60 (the constant block: `PAGE_SIZE` at :33, the `AT28C_TWC_MAX_MS` comment at :35-41 that already forward-declares this plan's constant by name, the `#define` at :42, then `AT28C_DQ6_TOGGLE_MASK`, `AT28C_TOGGLE_POLL_MAX_READS` and the poll-address constant)
    - firestarter/src/json_parser.c lines 55-62 and 280-290 (`key_flags` and `get_flags` using `extract_long` — confirm `0x100` already parses off the wire so no parser change is made)
    - firestarter_app/tests/test_revision_constants_parity.py lines 118-148 (the FLAG parity block — read it to ASSERT the negative rather than assume it)
  </read_first>
  <action>
Two `#define`s, in two files.

**`firestarter/include/firestarter.h`** — add `FLAG_SKIP_SDP_UNLOCK` with value `0x100` to the
existing control-flag block. `FLAG_VERBOSE 0x80` at line 68 is the highest value in use and there
are no gaps below it, so `0x100` is the next free bit. Follow the block's existing visual grouping
(blank-line-separated clusters) and place the new define after `FLAG_VERBOSE`. Add a short comment
naming what it declines — the SDP-disable command sequence on protocol `0x0D` — and noting that it is
firmware-only in this milestone phase, with the host CLI surface arriving in Phase 120 (HOST-03).

No struct change and no parser change. `handle->ctrl_flags` is already `uint32_t`
(`firestarter.h:96`) and `get_flags` uses `extract_long` (`json_parser.c:471-472`), so `0x100`
already parses off the wire unchanged. `is_flag_set` is a bare mask macro that captures `handle` from
caller scope and works unchanged at `0x100`. Do not widen anything and do not add a wire field —
no wire change is needed or permitted in this phase.

**`firestarter/src/proms/eeprom_28c.cpp`** — add `#define AT28C_TBLC_MAX_US 100` immediately after
`#define AT28C_TWC_MAX_MS 10` (line 42). The comment block at lines 35-41 that introduces
`AT28C_TWC_MAX_MS` **already names `AT28C_TBLC_MAX_US = 100` explicitly and already draws the
t_BLC-versus-t_WC distinction** — t_BLC bounds the inter-byte window inside the SDP-disable command
sequence, t_WC bounds the internal write cycle that follows the sequence's last byte. **Extend that
existing block; do not restate the distinction.** The new constant's own comment should add only what
the sibling block does not already say: that t_BLC is a datasheet *maximum* and not a delay to
insert, that post-Phase-117 the emitter is a bare `set_data` loop with `pulse_delay = 0` and no
inter-byte wait so the six writes already run far under budget, and that Plan 118-04 turns this
number into a runtime budget check rather than leaving it decorative. Carry the same
`[CITED: ...]`-style datasheet attribution shape the sibling comment uses.

Do **not** add `FLAG_SKIP_SDP_UNLOCK` to `firestarter_app/firestarter/constants.py`. The meta
`CLAUDE.md` rule says the two flag blocks change together; this milestone's firmware-before-host
ordering invariant overrides that for one phase, and Phase 120 HOST-03 closes the pair. Read
`firestarter_app/tests/test_revision_constants_parity.py` lines 118-148 and confirm by inspection
that it imports exactly eight named literals under a `FW_ABSENT` skipif and never enumerates
`firestarter.h`, so a firmware-only ninth flag cannot trip it — then run that test to prove it. Assert
the negative; do not assume it.

Build both boards after the edit. This task changes no behaviour, so the Leonardo and Uno flash
figures must be identical to the pre-task baseline. Capture that baseline first.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run -e leonardo -e uno 2>&1 | tail -30 && pio test -e native 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FLAG_SKIP_SDP_UNLOCK' /workspaces/firestarter/include/firestarter.h` shows one `#define` with the value `0x100`
    - `grep -n 'AT28C_TBLC_MAX_US' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows one `#define` line with the value `100`, in addition to the pre-existing mention inside the `AT28C_TWC_MAX_MS` comment block
    - `git -C /workspaces/firestarter diff --name-only` lists exactly `include/firestarter.h` and `src/proms/eeprom_28c.cpp`
    - `git -C /workspaces/firestarter diff -- src/json_parser.c include/messages.h` is empty (no parser change, no generated-header change)
    - `cd /workspaces/firestarter && pio run -e leonardo` succeeds and reports the SAME flash and RAM byte figures as the pre-task baseline; the same for `-e uno`
    - `cd /workspaces/firestarter && pio test -e native` reports the same pass count as the pre-task baseline with zero failures
    - `cd /workspaces/firestarter_app && python -m pytest tests/test_revision_constants_parity.py -q` exits 0 (the firmware-only ninth flag did not trip the host FLAG-parity block)
    - `cd /workspaces/firestarter_app && python -m pytest tests/test_sdp_table_parity.py -q` exits 0 (the source-text scanner over `eeprom_28c.cpp` is still green)
    - `git -C /workspaces/firestarter_app diff --name-only` is empty (this task writes no host file)
  </acceptance_criteria>
  <done>Both `#define`s exist, both boards build with byte-identical flash figures to baseline, the native suite is unchanged, and the two host gates that scan `eeprom_28c.cpp` and `firestarter.h` are proven still green.</done>
</task>

<task type="auto">
  <name>Task 2: Cite AT28C_TBLC_MAX_US at the page-load loop — comment only, no code</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp lines 303-341 (`eeprom28c_write_execute`; `window_start` declared at :316, the per-byte loop at :317-320 with the `handle->firestarter_set_data` call at :320, the page-boundary block at :322-339)
    - firestarter/src/proms/eeprom_28c.cpp lines 190-222 (the emitter and its hard-constraint comment — the other t_BLC site, for wording consistency)
    - .planning/phases/117-fix-remap-aware-0x0d-emitter-honest-completion-signal/117-CONTEXT.md (D-05, and the established finding that gh#11 is a conflation bug rather than a sampling-rate bug — the framing constraint on this comment)
  </read_first>
  <action>
Add a comment, and only a comment, naming the page-load loop's shared t_BLC exposure. **No code
change in `eeprom28c_write_execute`.** Place it immediately above the `for (uint32_t i = 0; i <
handle->data_size; i++)` loop at line 317 — after the `uint32_t window_start = 0;` declaration at
line 316 — or immediately above the `handle->firestarter_set_data` call at line 320. Either position
satisfies OBS-03's "cited at every call site the timing window touches" literally.

The comment must state three things and no more:

1. This per-byte `set_data` loop runs with `handle->pulse_delay = 0` and no inter-byte wait, under the
   **identical** t_BLC constraint as the SDP-disable command sequence — reference `AT28C_TBLC_MAX_US`
   by name so a reader can find the constant and its sibling comment.
2. The runtime budget check deliberately stays scoped to the unlock (D-09/D-10). Give the reason: a
   per-byte compare in this hot path costs flash and cycles for a surface no OBS requirement covers,
   and the flash delta matters because Phase 119's LOCK-06 headroom judgement must be made against
   Phase 117's **measured +204 B**, not against the research's predicted saving.
3. Where the constraint would next be acted on: whichever future phase revisits gh#11 on real
   silicon. Name that as the breadcrumb.

**Framing constraint, load-bearing.** Do **not** write this comment as if it explains or fixes gh#11.
Phase 117 established that gh#11 is a **conflation** bug — a completion poll conflated with a
data-landed check — and **not** a sampling-rate or timing-budget bug. A comment implying otherwise
re-introduces the framing error this milestone has already corrected twice. Aim the breadcrumb at the
conflation, not at the sampling rate.

Also do not weaken or restate the emitter's existing hard-constraint comment at lines 196-205. It
already states that nothing bus-visible and no `LOG_` call belongs inside the emitter body; Plan
118-04 depends on that constraint holding and Plan 118-01's rewritten gate now enforces it
mechanically.

Rebuild both boards. A comment-only change must leave both flash figures byte-identical.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run -e leonardo 2>&1 | tail -12 && cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py && python -m pytest tests/test_sdp_table_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter diff -U0 -- src/proms/eeprom_28c.cpp` for this task shows comment lines only — every added line begins with `//`, `/*`, ` *` or `*/`; zero added statement lines
    - `grep -c 'AT28C_TBLC_MAX_US' /workspaces/firestarter/src/proms/eeprom_28c.cpp` is at least 3 (the sibling comment's forward reference, the `#define` from Task 1, and this new citation)
    - The new comment contains no phrasing that attributes gh#11 to sampling rate or to a timing budget (reviewed against the Phase-117 conflation finding and noted in the SUMMARY)
    - `cd /workspaces/firestarter && pio run -e leonardo` reports flash and RAM figures byte-identical to the Task-1 figures
    - `cd /workspaces/firestarter && pio test -e native` unchanged pass count, zero failures
    - `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0 (the rewritten gate still resolves both bodies after this file changed)
    - `cd /workspaces/firestarter_app && python -m pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py -q` exits 0
  </acceptance_criteria>
  <done>The page-load loop carries a t_BLC citation naming `AT28C_TBLC_MAX_US`, no executable line changed in `eeprom28c_write_execute`, flash figures are unchanged, and every host gate that scans this file is green.</done>
</task>

<task type="auto">
  <name>Task 3: Add micros mocks to every native suite that drives eeprom28c_write_init</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp, firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp lines 90-260 (the `setUp` mock block at :94-118 with its load-bearing-do-not-remove comment, the handle factories `make_sdp_handle` at :126-137 and `make_identity_handle` at :139-155, and the `drive_write_init` / `drive_write_init_after_real_read` helpers at :194-244)
    - firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp lines 60-110 (the byte-identical mock block at :71-84, and the `firestarter_operation_init` drive at :509)
    - firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp lines 55-80 (its `setUp`; confirm whether it reaches `eeprom28c_write_init` or only the page-write execute path before deciding it needs no mock)
    - firestarter/src/proms/eeprom_28c.cpp lines 44-54 (the `AT28C_TOGGLE_POLL_MAX_READS` comment — it documents exactly why a fixed-value clock mock forces an iteration bound rather than a wall-clock deadline; the same reasoning drives the mock choice here)
  </read_first>
  <action>
`micros()` appears **nowhere** in the firmware today. ArduinoFake supplies it for native builds but
**aborts with SIGABRT on any unmocked method call**, so the moment Plan 118-04 adds two `micros()`
reads to `eeprom28c_write_init`, every native suite that drives that function crashes — and the crash
reads exactly like the deferred Unity-teardown SIGABRT flake, which would cost a debugging session.
Land the mocks here, before the production call exists.

**Sweep, do not guess the suite list.** Grep the native test tree for every suite that reaches
`eeprom28c_write_init` — via `h.firestarter_operation_init(&h)` after a `configure_memory` dispatch,
or via the suites' own `drive_write_init` helpers. At plan time the sweep found
`test_eeprom28c_sdp` (helpers `drive_write_init` at :211 and `drive_write_init_after_real_read` at
:227, plus direct `firestarter_operation_init` calls at :435 and :464) and `test_sdp_harness`
(:509). `test_val_eeprom28c` and `test_not_implemented` reference the `0x0D` handler but appear not to
invoke `write_init` — `test_val_eeprom28c` exercises the page-write execute path and
`test_not_implemented` asserts `firestarter_operation_init` is NULL. **Confirm both by reading, and
if either does reach `write_init`, add the mock there too.** Re-run the sweep after Plan 118-04 lands
as well; a missing mock manifests as SIGABRT, not as a compile error.

**`test_sdp_harness` — fixed value.** Insert one line into the `setUp` mock block immediately after
the existing `millis` mock, returning a fixed `micros()` value so the measured duration is 0 and the
budget can never be exceeded. That keeps this always-green harness suite's behaviour unchanged.

**`test_eeprom28c_sdp` — controllable counter.** This suite is where Plan 118-05's budget-exceeded
case lives, so a fixed value is not enough. Add a file-static tick source — name it `s_micros_ticks`
— reset in `setUp` alongside the other file-static reset lines at :133-139, and mock `micros()` to
return successive values from it. Note the exact call shape: the two production `micros()` reads
bracket the emit call, so **exactly two** reads occur per `write_init` drive. Default the tick source
so both reads return the same value (elapsed 0, budget not exceeded) — that keeps all eight existing
cases behaviourally identical. Plan 118-05 will set the tick source per-case to synthesise a
budget-exceeding elapsed value. Expose whatever minimal seam Plan 118-05 needs (a settable
file-static array or a settable delta) and document it in a comment.

Extend the existing do-not-remove comment in each suite's mock block rather than writing a new one.
That comment already explains that the mocks are load-bearing and must not be pruned as unused; add
that `micros` is mocked because Plan 118-04's OBS-04 duration bracket calls it twice per drive, and
that removing the mock produces a SIGABRT indistinguishable from the deferred Unity-teardown flake.

`firestarter/platformio.ini` needs **no** change: `test_filter` already contains both
`native/avr/test_sdp_harness` (:118) and `native/avr/test_eeprom28c_sdp` (:119), and both already have
`-I` entries in `build_flags`. Confirm that and leave the file alone.

The suite is GREEN at 8/8 before this task and must be GREEN at 8/8 after it. A mock for a function
nothing yet calls is inert.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'micros' /workspaces/firestarter/test/native/avr/test_sdp_harness/test_sdp_harness.cpp` is at least 1, inside the `setUp` mock block
    - `grep -c 'micros' /workspaces/firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp` is at least 1, and the file contains `s_micros_ticks`
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 8 passed, 0 failed — the same eight case names as the pre-task baseline
    - `cd /workspaces/firestarter && pio test -e native -f "*test_sdp_harness*"` reports the same pass count as the pre-task baseline, 0 failed
    - `cd /workspaces/firestarter && pio test -e native` reports the same total pass count as the pre-task baseline, 0 failed, and no SIGABRT in the output
    - `git -C /workspaces/firestarter diff -- platformio.ini` is empty
    - The SUMMARY records the full result of the write-init sweep: every native suite path checked, and for each, whether it reaches `eeprom28c_write_init`
  </acceptance_criteria>
  <done>Both SDP suites mock `micros()`, `test_eeprom28c_sdp` exposes a controllable tick seam for Plan 118-05, the native suite is unchanged and SIGABRT-free, `platformio.ini` is untouched, and the sweep result is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire `flags` field → `handle->ctrl_flags` | A new high flag bit must not silently truncate or alias an existing bit |
| firmware `FLAG_*` header → host `constants.py` | The two blocks are conventionally changed together; this phase deliberately breaks that for one phase |
| firmware source text → host source-scanning gates | Any edit to `eeprom_28c.cpp` can break a host gate that parses it |
| ArduinoFake → native suites | An unmocked method aborts the process rather than failing a test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-03-FLAGALIAS | Tampering | `FLAG_SKIP_SDP_UNLOCK 0x100` vs existing flags | high | mitigate | `0x100` is above the highest used bit `0x80` with no gaps below; `ctrl_flags` is already `uint32_t` and `get_flags` uses `extract_long`, both confirmed by reading before the edit; native suite re-run proves no existing flag behaviour changed |
| T-118-03-PARITYBREAK | Tampering | host `test_revision_constants_parity.py` | medium | mitigate | The negative is asserted, not assumed — the test is read AND run in Task 1's acceptance criteria |
| T-118-03-SRCSCAN | Tampering | host gates that parse `eeprom_28c.cpp` | high | mitigate | Tasks 1 and 2 both re-run `test_sdp_table_parity.py`, `test_dispatch_mirror.py`, `test_sdp_db_invariant.py` and the rewritten no-log gate. This is CORRECTION 4 item 4's mandatory check and it is a task-level criterion, not a footnote |
| T-118-03-SIGABRT | Denial of service | native suites driving `eeprom28c_write_init` | high | mitigate | `micros` mocks land before the production call site; the suite sweep is recorded in the SUMMARY and re-run after Plan 118-04 |
| T-118-03-DECORATIVE | Repudiation | `AT28C_TBLC_MAX_US` as a comment-only constant | medium | mitigate | This plan explicitly does NOT close OBS-03; Plan 118-04's runtime check and Plan 118-05's budget-WARN case are named as the closing conditions |
| T-118-03-GH11FRAME | Repudiation | the page-load citation's wording | medium | mitigate | Task 2 forbids gh#11-as-sampling-rate framing and requires the reviewed wording be recorded in the SUMMARY |
| T-118-03-SC | Tampering | package-manager installs | low | accept | This plan installs nothing — no npm/pip/cargo install task exists in Phase 118, so no package legitimacy audit is required |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && pio test -e native` — same total pass count as the pre-plan baseline, zero failures, no SIGABRT.
2. `cd /workspaces/firestarter && pio run -e leonardo -e uno` — both build; flash and RAM figures byte-identical to the pre-plan baseline, because this plan changes no behaviour.
3. `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0, and `python -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py -q` exits 0 — the full CORRECTION-4 cross-repo gate checklist.
4. `git -C /workspaces/firestarter_app status --short` is clean — this plan writes no host file.
5. Commits inside `/workspaces/firestarter` only. Suggested subjects: `feat(118-03): define FLAG_SKIP_SDP_UNLOCK 0x100 and AT28C_TBLC_MAX_US 100 (OBS-02, OBS-03)`; `docs(118-03): cite shared t_BLC exposure at the page-load loop (OBS-03, D-10)`; `test(118-03): mock micros() in both native SDP suites ahead of the OBS-04 bracket`.
</verification>

<success_criteria>
- `FLAG_SKIP_SDP_UNLOCK` = `0x100` and `AT28C_TBLC_MAX_US` = `100` exist in firmware.
- `AT28C_TBLC_MAX_US` is cited at both t_BLC sites; only the unlock will carry the runtime check.
- No wire, parser, struct or `constants.py` change.
- Every native suite driving `eeprom28c_write_init` mocks `micros()`; the sweep result is recorded.
- Both board flash figures are unchanged from baseline — this plan is behaviourally inert.
- OBS-02 and OBS-03 remain **Pending** in `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
Create `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-03-SUMMARY.md` when done.
Record: the pre-plan and post-plan Leonardo and Uno flash/RAM figures side by side, the full write-init suite sweep result, the exact `micros` mock shape and tick seam name in `test_eeprom28c_sdp` (Plan 118-05 consumes it), and the reviewed wording of the page-load t_BLC citation.
</output>
