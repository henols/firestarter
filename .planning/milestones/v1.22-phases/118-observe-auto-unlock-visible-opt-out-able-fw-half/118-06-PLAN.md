---
phase: 118-observe-auto-unlock-visible-opt-out-able-fw-half
plan: 06
type: execute
wave: 6
depends_on: [118-05]
files_modified:
  - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md
autonomous: true
requirements: [OBS-01, OBS-05]

must_haves:
  truths:
    - "D-07: OBS-05's byte-identity claim is asserted on the recorded BUS stream, not the serial stream. The two new serial frames on the flag-absent path are a NAMED, ENUMERATED exception — written down here in one place, not scattered or hidden. Rejected: golden identity plus flash delta plus prose only, which leaves the frame count unverified; that gap is closed by Plan 118-05's frame-enumeration case, which this plan cites rather than re-runs."
    - "D-03: the in-phase proof of three-repo catalog identity is a LOCAL three-way `cmp`. Meta `.github/workflows/catalog-sync-check.yml` checks out both sub-repos at `ref: main` (lines 27 and 38), so it CANNOT go green for v1.22 catalog work until the milestone merges. That red is expected-until-merge and is recorded here explicitly so no later reader mistakes it for a Phase-118 regression."
    - "The full firmware-rename-versus-host-source-scanning-gate checklist is executed as a named task, not assumed. PROJECT.md CORRECTION 4 item 4 requires every phase from 118 on to include an explicit task doing this, because Phase 117 shipped a commit claiming zero `firestarter_app` files changed while four host gates were broken."
    - "The measured Leonardo flash delta is reported against Phase 117's measured **+204 B**, never against the research's predicted saving. Phase 119's LOCK-06 headroom judgement consumes this number."
    - "Every claim written down here stays inside the validation ceiling: `0x0D` remains `UNVERIFIED`, zero chips change `support_status`, and the 84-chip count is unchanged. Nothing here is evidence about AT28C silicon."
  artifacts:
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md
  key_links:
    - "The vacuous-path trap: a `git diff -- <path>` check proves nothing if `<path>` does not exist. The ROADMAP's `flash_utils.{h,cpp}` shorthand does not match a real `src/flash_utils.h`, so every path-based check written here must assert the file exists first."
    - "Two host pytest failures are pre-existing environment/debt artifacts and are NOT this phase's regressions: `test_audit_coverage_matrix`'s stale golden, and the `test_no_programmer_found_*` characterization tests which fail whenever a live board is attached at `/dev/ttyACM*`. Both must be named in the record with that disposition, never silently counted as passes."
---

<objective>
Close OBS-05 and OBS-01 by executing the full non-regression sweep across all three repos and writing
down the one artifact that says, in a single place, exactly what changed and exactly what did not.

Purpose: OBS-05's claim is "byte-identical to `3.0.0b11` apart from the corrected emitter and the two
new report lines". A claim of that shape is only worth what its enumeration of exceptions is worth.
This plan produces that enumeration and proves the negatives.

Output: `118-NONREGRESSION.md` in the phase directory, plus updated requirement statuses.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-CONTEXT.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-03-SUMMARY.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-04-SUMMARY.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-05-SUMMARY.md
</context>

## Repo and commit mechanics

The only file this plan writes is in the **meta repo** under `.planning/`. Commit in
`/workspaces` and stage that single path explicitly. Both sub-repos should be **clean** when this plan
runs — if `git -C /workspaces/firestarter status --short` or the host equivalent is dirty, an earlier
plan left work uncommitted; stop and report rather than sweeping it up here. Do **not** stage the
submodule gitlinks.

`pio` runs against the shared `/workspaces/firestarter/.pio/` tree — no concurrent runs.

## Artifacts this phase produces

**Created by THIS plan:**
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md`.

**Created earlier in Phase 118, all referenced by this plan's record:** catalog ids
`MSG_INFO_SDP_UNLOCK` (0x5E), `MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F),
`MSG_WARN_SDP_UNLOCK_SKIPPED` (0x86), `MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) — Plan 118-02; firmware
macros `FLAG_SKIP_SDP_UNLOCK` (0x100) and `AT28C_TBLC_MAX_US` (100) — Plan 118-03; firmware locals
`sdp_seq_len`, `sdp_emit_start_us`, `sdp_emit_us` — Plan 118-04; host gate identifiers
`_EMITTER_FUNC_NAME`, `_POLL_FUNC_NAME`, `_resolve_windows`, widened
`_find_function_body(cleaned_text, func_name)` — Plan 118-01; host test cases
`test_checker_exits_nonzero_on_log_planted_in_completion_poll_body` and
`test_checker_fails_closed_when_emitter_body_is_absent` — Plan 118-01; native test cases
`test_case9_skip_flag_suppresses_unlock_stream`,
`test_case10_flag_absent_emits_full_unlock_stream`, `test_case11_tblc_budget_exceeded_warns`,
`test_case12_flag_absent_emits_exactly_two_report_frames` with helpers
`make_sdp_handle_with_flags`, `s_micros_ticks`, `captured_frames` — Plan 118-05.

**Created later in Phase 118:**
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md` — Plan
118-07.

## Requirement ownership

This plan **closes OBS-05** and **closes OBS-01**. Mark both Complete in `.planning/REQUIREMENTS.md`
only after the sweep passes and the record is written. **OBS-04 stays open** — its board measurement
is Plan 118-07 and this plan does not list it in `requirements`. OBS-02 and OBS-03 were closed by Plan
118-05; re-confirm their status rows read Complete rather than re-deriving them.

<tasks>

<task type="auto">
  <name>Task 1: Run the full three-repo non-regression sweep and the cross-repo gate checklist</name>
  <files>.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md</files>
  <read_first>
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md section "`eeprom_28c.cpp` cross-repo source-scan checklist (CORRECTION 4 item 4 — mandatory)" — the seven-row gate table with each gate's scan target and risk level
    - .planning/PROJECT.md section "Current Milestone: v1.22" — all four ⚠ correction blocks, in particular CORRECTION 3 (66 of 84, not "all 84") and CORRECTION 4 items 3 and 4 (the measured +204 B and the mandatory gate-checklist task)
    - .planning/phases/117-fix-remap-aware-0x0d-emitter-honest-completion-signal/117-05-SUMMARY.md — the frozen-artifact and host-untouched proof shape this sweep extends
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-03-SUMMARY.md and 118-04-SUMMARY.md — the flash figures to difference
  </read_first>
  <action>
Execute the sweep. Every leg is a command whose output you capture verbatim; nothing here is
inferred from an earlier plan's SUMMARY.

**Leg 1 — firmware native suite.** `cd /workspaces/firestarter && pio test -e native`. Record the
total pass count and confirm zero failures and no SIGABRT. `test_eeprom28c_sdp` must show 12/12.

**Leg 2 — both board builds and the flash delta.** `pio run -e leonardo` and `pio run -e uno`.
Record flash and RAM bytes for each. Compute the Leonardo delta against the Phase-118 base commit's
figure. **Report it against Phase 117's measured +204 B** — the reference point for LOCK-06's headroom
judgement in Phase 119 — and explicitly **not** against the research's predicted net saving, which
CORRECTION 4 item 3 records as contradicted by measurement.

**Leg 3 — the CORRECTION-4 item-4 gate checklist, executed row by row.** This is the named task
PROJECT.md requires of every phase from 118 on. For each host gate that reads firmware source, run it
and record PASS/FAIL with the command:

- `firestarter_app/tools/check_no_log_in_sdp_window.py` (rewritten by Plan 118-01 — the HIGH-risk row)
- `firestarter_app/tests/test_check_no_log_in_sdp_window.py`
- `firestarter_app/tests/test_sdp_table_parity.py` (scans `eeprom_28c.cpp` source text; broken three
  times by Phase 117 — the MEDIUM-risk row; this phase did not touch the `EEPROM_SDP_DISABLE`
  declaration syntax, so it must be green, and that must be demonstrated rather than assumed)
- `firestarter_app/tests/test_dispatch_mirror.py`
- `firestarter_app/tests/test_sdp_db_invariant.py`
- `firestarter_app/tools/gen_sdp_bus_config.py` plus `firestarter_app/tests/test_sdp_bus_config_drift.py`
- `firestarter_app/tests/test_revision_constants_parity.py` (the eight-literal FLAG block — verified
  non-exhaustive, so the firmware-only ninth flag must not trip it)
- `firestarter_app/tools/check_dispatch.py` and `firestarter_app/tools/build_db.py` — expected
  untouched by this phase; confirm rather than assume

**Avoid the vacuous-path trap.** If you write any path-based check, assert the path exists first. The
ROADMAP's `flash_utils.{h,cpp}` shorthand does not correspond to a real `src/flash_utils.h`, so a
`git diff -- src/flash_utils.h` check passes vacuously. Any check in this sweep whose subject is a
path must fail loudly when the path is absent.

**Leg 4 — full host pytest.** `cd /workspaces/firestarter_app && python -m pytest -q`. Two failures
are pre-existing and are NOT this phase's regressions; name both with that disposition and never fold
them into a pass count: `test_audit_coverage_matrix` (stale golden, needs a dedicated regen) and the
`test_no_programmer_found_*` characterization tests (an environment artifact — they fail whenever a
live board is enumerated at `/dev/ttyACM*`, which defeats their `comports=[]` monkeypatch). If any
**other** failure appears, it is this phase's and must be fixed before the record is written.

**Leg 5 — host lint.** `ruff check .` and `ruff format --check .` in `firestarter_app`. Pre-existing
violations in `tools/*.py` are prior debt; confirm each is outside this phase's diff before accepting
it, using the same discipline Phase 107 Plan 03 used.

**Leg 6 — catalog three-way identity.** `cmp` all three `messages.toml` copies (meta ↔ firmware,
meta ↔ host) and run both sub-repos' `codegen.py --check` plus regenerate-and-`git diff --exit-code`
for `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`. Record that meta
`.github/workflows/catalog-sync-check.yml` is **expected-red-until-milestone-merge** because both its
sub-repo checkouts pin `ref: main` (lines 27 and 38), and that the local `cmp` is the in-phase proof.
State this as a known-and-explained condition, not as a defect and not as a thing to fix here.

**Leg 7 — frozen `_shared/` artifacts.** Re-confirm the three blob SHAs Plan 118-05 recorded, running
the `git rev-parse <base>:<path>` comparison yourself rather than copying the numbers.

**Leg 8 — Python version disclosure.** Record the interpreter version actually used for every
Python leg. Both sub-repos' CI pins 3.11 and this devcontainer has only 3.12, so every Python leg is
recorded CI-PENDING/structurally-green under 3.12 per this project's established Phase-98 and
Phase-103 precedent. Never fabricate a 3.11 run.

Capture every command and its verbatim output as you go; Task 2 writes them up.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -20 && pio run -e leonardo -e uno 2>&1 | tail -20 && cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py && python -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native` exits 0; `test_eeprom28c_sdp` shows 12 passed; total failures 0; the output contains no `SIGABRT`
    - `cd /workspaces/firestarter && pio run -e leonardo` and `pio run -e uno` both exit 0; flash and RAM byte figures captured verbatim for both
    - The Leonardo flash delta versus the Phase-118 base commit is computed and stated alongside Phase 117's measured +204 B reference
    - All nine rows of the CORRECTION-4 gate checklist were executed, each with its command and PASS/FAIL captured; every row is PASS
    - `cd /workspaces/firestarter_app && python -m pytest -q` shows exactly the two named pre-existing failures and no others
    - `cd /workspaces/firestarter_app && ruff check . ; ruff format --check .` — every reported violation is proven to be outside `git diff` for this phase before being accepted as prior debt
    - `cmp` of meta ↔ firmware and meta ↔ host `tools/catalog/messages.toml` both exit 0
    - Regenerate-and-`git diff --exit-code` is clean for both `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`
    - The three `_shared/` blob SHAs are re-derived here and match Plan 118-05's recorded values
    - Any path-based check written during this sweep asserts its path exists before asserting anything about it
    - The interpreter version used for every Python leg is captured
  </acceptance_criteria>
  <done>Every sweep leg has been executed with verbatim output captured, all nine gate-checklist rows are PASS, the host suite shows only the two named pre-existing failures, and the flash delta plus the three frozen blob SHAs are re-derived rather than copied.</done>
</task>

<task type="auto">
  <name>Task 2: Write 118-NONREGRESSION.md — the enumerated OBS-05 exception record</name>
  <files>.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md section "Validation Ceiling" (the permitted claim and the forbidden claim, verbatim) and the OBS-01..OBS-05 statements
    - .planning/phases/117-fix-remap-aware-0x0d-emitter-honest-completion-signal/117-05-SUMMARY.md (the frozen-artifact record shape and the narrowed host-untouched claim this document extends)
    - firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md section "Phase 118 observability baseline" (written by Plan 118-05 — this document cites it rather than duplicating it)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-05-SUMMARY.md (the frame-enumeration case and the blob SHAs)
  </read_first>
  <action>
Write `118-NONREGRESSION.md` in the phase directory. It is the single place a later reader looks to
answer "what did Phase 118 change, and what did it prove unchanged".

Required sections:

**1. The OBS-05 claim, stated precisely.** With no new flag set, a `0x0D` write's outward behaviour is
unchanged from `3.0.0b11` apart from (a) Phase 117's corrected emitter and (b) the two new report
lines. State that the byte-identity half of that claim is asserted on the recorded **bus** stream, and
that the serial channel carries a named, enumerated exception. Do not let the word "byte-identical"
stand unqualified anywhere in the document.

**2. The enumerated serial-channel exception — the whole list, in one table.** Four ids, with the path
each is reachable on:

- `MSG_INFO_SDP_UNLOCK` (0x5E) — flag-absent path, before the sequence, unconditional
- `MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F) — flag-absent path, after the sequence, unconditional, carries
  the measured emit duration
- `MSG_WARN_SDP_UNLOCK_SKIPPED` (0x86) — `FLAG_SKIP_SDP_UNLOCK` path only, emitted in place of the
  pair above
- `MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) — flag-absent path only, and only when the measured emit
  duration exceeds the sequence length times `AT28C_TBLC_MAX_US`; expected never to fire on a 16 MHz
  AVR

State that on a default write exactly **two** frames are added, cite Plan 118-05's
`test_case12_flag_absent_emits_exactly_two_report_frames` as the machine check, and note that the
general-purpose serial-frame baseline recorder was deliberately not built (D-07) — the per-case Serial
capture is not that recorder.

**3. Why the bus stream is genuinely unchanged.** Report lines go out via `rurp_log_id` to Serial; the
Phase-116 recorder observes only `rurp_write_to_register`, `rurp_write_data_buffer` and
`rurp_set_control_pin`, so a `LOG_*` call cannot append a strobe. Cite the three `_shared/` blob SHAs
proving no golden was regenerated, with the phase-base commit they were compared against.

**4. Flash and RAM.** Both boards, base versus HEAD, with the Leonardo delta stated against Phase
117's measured **+204 B** and explicitly not against the research's predicted saving. Note that Phase
119's LOCK-06 headroom judgement consumes this number.

**5. The CORRECTION-4 item-4 gate table.** All nine rows with command and verdict. This is the
artifact that makes the mandatory check auditable. Add a line stating why the check exists: Phase 117
shipped a commit claiming zero `firestarter_app` files changed while four host gates were broken, the
firmware suite stayed green throughout, and only the regression gate caught it.

**6. Known-and-explained conditions — never silent.** Three entries:
- meta `catalog-sync-check.yml` is expected-red-until-milestone-merge because both its sub-repo
  checkouts pin `ref: main`; the local three-way `cmp` is the in-phase proof
- the two pre-existing host pytest failures, each with its cause and its disposition as prior debt
- every Python leg is CI-PENDING/structurally-green under the interpreter actually available, with the
  version named

**7. Validation ceiling.** Quote REQUIREMENTS.md's permitted claim and forbidden claim verbatim, then
state which of this phase's results sit inside the permitted claim. `0x0D` stays `UNVERIFIED`, zero
chips change `support_status`, the 84-chip count is unchanged. Nothing in this document is evidence
about AT28C silicon: the assertions are native register-trace, native serial-frame and source-scan
proofs against mocked hardware.

**8. Deliberately not taken.** Two entries, so the next owner finds them rather than inheriting
silence: the recorder widening to a third strobe kind (data-bus direction), which Phase 117's D-12
nominally named Phase 118's owner and which this phase did **not** take; and the runtime t_BLC budget
check on `eeprom28c_write_execute`'s page-load loop, which is cited but deliberately unchecked
(D-10), with its natural home being whichever phase next revisits gh#11 on silicon — aimed at the
**conflation**, per Phase 117's correction, never at the sampling rate.

Then update `.planning/REQUIREMENTS.md`: mark **OBS-01** and **OBS-05** Complete in the traceability
table. Leave **OBS-04** Pending — Plan 118-07 owns it. Re-read the OBS-02 and OBS-03 rows and confirm
Plan 118-05 already set them Complete; if it did not, that is Plan 118-05's gap and must be reported,
not silently fixed here.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md && grep -c 'MSG_WARN_SDP_TBLC_EXCEEDED\|catalog-sync-check\|204 B\|UNVERIFIED' /workspaces/.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-NONREGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - `118-NONREGRESSION.md` exists and contains all eight required sections
    - Its serial-exception table lists exactly the four ids `MSG_INFO_SDP_UNLOCK`, `MSG_INFO_SDP_UNLOCK_DONE_US`, `MSG_WARN_SDP_UNLOCK_SKIPPED`, `MSG_WARN_SDP_TBLC_EXCEEDED` with their hex values and reachability paths
    - It names `test_case12_flag_absent_emits_exactly_two_report_frames` as the machine check for the two-frame claim
    - It contains the three `_shared/` blob SHAs and the phase-base commit they were compared against
    - It states the Leonardo flash delta and references Phase 117's measured `+204 B`
    - Its gate table has nine rows, each with a command and a verdict
    - It states that meta `catalog-sync-check.yml` is expected-red-until-milestone-merge, with the `ref: main` reason
    - It names both pre-existing host pytest failures with their disposition as prior debt
    - It quotes REQUIREMENTS.md's permitted and forbidden claims verbatim, and contains the words `UNVERIFIED`, `support_status` and `84`
    - It contains a "deliberately not taken" section naming both the recorder widening and the page-load runtime check
    - The document contains no sentence claiming SDP lock or unlock works on AT28C silicon (reviewed line by line and recorded in the SUMMARY)
    - `.planning/REQUIREMENTS.md` shows OBS-01, OBS-02, OBS-03 and OBS-05 as Complete and OBS-04 as Pending
    - `git -C /workspaces show --stat HEAD` lists only `.planning/` paths — no gitlink, no sub-repo file
  </acceptance_criteria>
  <done>`118-NONREGRESSION.md` exists with all eight sections, the four-id serial exception is enumerated in one table, every negative is proven rather than asserted, the validation ceiling is quoted and honoured, and OBS-01 and OBS-05 are marked Complete with OBS-04 still Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware source changes → host source-scanning gates | The Phase-117 failure boundary: firmware edits silently break host gates and the firmware suite stays green |
| a written claim → the evidence behind it | An unqualified "byte-identical" claim over-reaches unless its exceptions are enumerated |
| this phase's results → the milestone's validation ceiling | Any wording readable as bench-validating `0x0D` crosses a line the milestone set before work began |
| a red CI workflow → a later reader | An expected-red workflow can be misread as a regression years later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-06-UNOWNEDGATE | Tampering | host gates reading firmware source | high | mitigate | The nine-row CORRECTION-4 checklist is a task with per-row commands and verdicts, written into `118-NONREGRESSION.md` as an auditable table — the mandatory named task PROJECT.md requires of every phase from 118 on |
| T-118-06-OVERCLAIM | Repudiation | the OBS-05 "byte-identical" wording | high | mitigate | The word never stands unqualified; the serial exception is a four-row table in the same document; the frame count cites a named machine check |
| T-118-06-CEILING | Repudiation | `118-NONREGRESSION.md` wording | high | mitigate | REQUIREMENTS.md's permitted and forbidden claims are quoted verbatim; a line-by-line review for silicon claims is an acceptance criterion |
| T-118-06-SILENTRED | Repudiation | `catalog-sync-check.yml` | medium | mitigate | Recorded as a known-and-explained condition with the `ref: main` cause, alongside the local `cmp` that is the actual in-phase proof |
| T-118-06-VACUOUS | Repudiation | path-based checks in the sweep | medium | mitigate | Every path-based check must assert its path exists first; the `src/flash_utils.h` trap is named explicitly |
| T-118-06-COUNTEDDEBT | Repudiation | pre-existing host pytest failures | medium | mitigate | Both are named with their cause and disposition and are never folded into a pass count; any third failure blocks the record |
| T-118-06-STALEFLASH | Repudiation | the flash-delta reference point | medium | mitigate | The delta is stated against Phase 117's measured +204 B, with the research's predicted saving explicitly excluded as a reference |
| T-118-06-SC | Tampering | package-manager installs | low | accept | This plan installs nothing — no npm/pip/cargo install task exists in Phase 118, so no package legitimacy audit is required |
</threat_model>

<verification>
1. Every sweep leg in Task 1 executed with verbatim output captured; all nine gate rows PASS.
2. `cd /workspaces/firestarter && pio test -e native` — zero failures, no SIGABRT, `test_eeprom28c_sdp` at 12/12.
3. `cd /workspaces/firestarter && pio run -e leonardo -e uno` — both build; flash figures recorded and differenced.
4. `cd /workspaces/firestarter_app && python -m pytest -q` — exactly the two named pre-existing failures.
5. Catalog three-way `cmp` clean; both regenerate-and-`git diff --exit-code` legs clean.
6. Three `_shared/` blob SHAs re-derived and matching.
7. `118-NONREGRESSION.md` exists with all eight sections and passes the line-by-line validation-ceiling review.
8. `.planning/REQUIREMENTS.md` updated: OBS-01 and OBS-05 Complete; OBS-04 Pending.
9. One commit in `/workspaces`, staging only `.planning/` paths. Suggested subject: `docs(118-06): record the Phase 118 non-regression sweep and the enumerated OBS-05 exception (OBS-01, OBS-05)`.
10. `git -C /workspaces/firestarter status --short` and `git -C /workspaces/firestarter_app status --short` are both clean.
</verification>

<success_criteria>
- The full three-repo sweep passes, with the two known pre-existing host failures named rather than absorbed.
- All nine host gates that scan firmware source are proven green after this phase's firmware edits.
- `118-NONREGRESSION.md` enumerates the exact four-id serial-channel exception and cites its machine check.
- The Leonardo flash delta is reported against Phase 117's measured +204 B.
- The three `_shared/` golden files are proven blob-identical to the phase base.
- Both deliberately-not-taken items are recorded so the next owner finds them.
- No claim anywhere crosses the validation ceiling.
- OBS-01 and OBS-05 are Complete; OBS-04 remains Pending.
</success_criteria>

<output>
Create `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-06-SUMMARY.md` when done.
Record: the nine-row gate verdict table, both boards' base and HEAD flash/RAM figures with the Leonardo delta, the exact host pytest pass/fail counts with the two named exclusions, the interpreter version used, and confirmation that the line-by-line validation-ceiling review of `118-NONREGRESSION.md` found no silicon claim.
</output>