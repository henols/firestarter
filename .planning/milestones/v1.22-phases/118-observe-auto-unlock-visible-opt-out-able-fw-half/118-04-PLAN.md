---
phase: 118-observe-auto-unlock-visible-opt-out-able-fw-half
plan: 04
type: execute
wave: 4
depends_on: [118-03]
files_modified:
  - firestarter/src/proms/eeprom_28c.cpp
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04]

must_haves:
  truths:
    - "D-01: both report lines are UNCONDITIONAL — emitted via the bare `LOG_ID` / `LOG_ID_U32` macros with INFO-band ids, NOT via the `FLAG_VERBOSE`-gated `LOG_INFO_ID*` family. These become the tree's first non-verbose-gated INFO-band call sites; there are currently zero. The break with the 19-call-site house convention is argued in the source comment, not merely done, because a verbose-gated report line would leave a default `firestarter write at28c256` silent — which is the exact defect this phase exists to remove."
    - "D-01 corollary: unconditional emission is also what makes OBS-05's 'byte-identical apart from the two report lines' a real claim instead of a vacuous one. Under verbose-gating the default path would emit zero new frames and OBS-05 would be trivially true."
    - "D-02: when `FLAG_SKIP_SDP_UNLOCK` is set, a WARN-band id is emitted unconditionally IN PLACE OF the before/after pair, and the completion wait is skipped too — there is no internal write cycle to wait for. Rejected: the `MSG_INFO_SKIPPING_ERASE` INFO shape at `flash_5v_page.cpp:70`, because a silently-skipped destructive step is the footgun v1.16 Phase 92 had to fix."
    - "D-02 hard prohibition: nothing on the SDP path writes `handle->response_code`. That preserves Phase 117's D-05 and is permanently enforced by `test_case8_completion_poll_preserves_prior_severity`. Copy the `LOG_WARN_ID*` macro spelling from `eeprom_28c.cpp:138`, NOT its accompanying severity write. Rejected: also setting a WARNING response code — it breaks D-05 and warns on every skip regardless of whether the skip mattered."
    - "D-05: `micros()` brackets the six command writes ONLY. Both reads sit OUTSIDE `eeprom28c_emit_command_sequence`'s body, so they perturb inter-byte timing not at all. The `t_WC` wait and the DQ6 poll are deliberately excluded — a fixed `delay` plus an iteration-bounded loop would add a constant plus mock-dependent noise. Rejected: emit + t_WC + poll; rejected: reporting both numbers."
    - "D-09: the measured emit duration is compared at runtime against the sequence length times `AT28C_TBLC_MAX_US`, and a WARN is emitted when exceeded. On a 16 MHz AVR it should never fire — that is what a latent invariant looks like. It speaks up only if a future edit puts real work inside the loop. Rejected: a documentation-only constant."
    - "The emitter body and the completion-poll body remain free of any `LOG_` call and of any new bus-visible call. The rewritten gate from Plan 118-01 mechanically enforces this, and the emitter's own hard-constraint comment states it."
  artifacts:
    - firestarter/src/proms/eeprom_28c.cpp
  key_links:
    - "Placement: the before-line must sit AFTER `eeprom28c_check_chip_id`'s early return — the `if (handle->chip_id > 0) { ... }` block ends at :279 — because reporting an unlock that never ran would be dishonest."
    - "`LOG_WARN_ID*` and every other severity-named macro family is a literal `#define` alias of `LOG_ID*`, i.e. already unconditional (`logging_id.h:105-164`). Only the INFO band's macros are gated, which is why only the two INFO-band lines break convention."
    - "The emit call must retain the literal substring `handle, EEPROM_SDP_DISABLE` so the gate's append-only `_EMIT_ANCHOR_PATTERNS` keeps matching."
---

<objective>
Make the auto-unlock visible and declinable: emit an unconditional report line before the SDP-disable
sequence and an unconditional duration-carrying line after it, bracket the emit call with `micros()`,
compare the measured duration against the t_BLC budget and WARN when exceeded, and honour
`FLAG_SKIP_SDP_UNLOCK` by replacing the whole sequence with a single honest WARN.

Purpose: silence was the defect. A decision that leaves the default path silent is not a smaller
version of this phase, it is a no-op.

Output: one modified function body in one firmware file, with four new call sites.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-CONTEXT.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-03-SUMMARY.md
</context>

## Repo and commit mechanics

The single file this plan edits lives in the **`firestarter` submodule** at
`/workspaces/firestarter` (branch `v1.22-at28c-software-data-protection-lifecycle`; confirm with
`git -C /workspaces/firestarter branch --show-current`). Commit inside that submodule. No meta-repo
file, no host file, no gitlink bump.

`pio` runs against the shared `/workspaces/firestarter/.pio/` tree — do not run it concurrently with
another plan.

## Hard ordering precondition

Plan 118-01 **must** already be committed. The after-line lands between the emit call site and the
wait call site, which is exactly the span the *pre-rewrite* gate scanned. Against the old gate this
plan's own verification would fail. Confirm before starting: `cd /workspaces/firestarter_app && python
tools/check_no_log_in_sdp_window.py` must print a PASS line naming two line ranges inside
`eeprom_28c.cpp` 206-222 and 256-269. If it names a single range in the 291-297 region, Plan 118-01
has not landed — stop.

## Artifacts this phase produces

**Created by THIS plan** — three function-local identifiers inside `eeprom28c_write_init`:
`sdp_seq_len` (the single sequence-length expression), `sdp_emit_start_us`, `sdp_emit_us`.

**Consumed here, created earlier in Phase 118:** `MSG_INFO_SDP_UNLOCK` (0x5E),
`MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F), `MSG_WARN_SDP_UNLOCK_SKIPPED` (0x86),
`MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) — Plan 118-02, present in `firestarter/include/messages.h`;
`FLAG_SKIP_SDP_UNLOCK` (0x100) and `AT28C_TBLC_MAX_US` (100) — Plan 118-03.

**Created elsewhere in Phase 118:** host gate identifiers `_EMITTER_FUNC_NAME`, `_POLL_FUNC_NAME`,
`_resolve_windows`, widened `_find_function_body(cleaned_text, func_name)`, and host test cases
`test_checker_exits_nonzero_on_log_planted_in_completion_poll_body` /
`test_checker_fails_closed_when_emitter_body_is_absent` — Plan 118-01; native test cases
`test_case9_skip_flag_suppresses_unlock_stream`,
`test_case10_flag_absent_emits_full_unlock_stream`, `test_case11_tblc_budget_exceeded_warns`,
`test_case12_flag_absent_emits_exactly_two_report_frames` with helpers
`make_sdp_handle_with_flags`, `s_micros_ticks`, `captured_frames` — Plan 118-05;
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md` —
Plan 118-07.

## Requirement ownership (do NOT mark these Complete here)

This plan closes the firmware-code half of **OBS-01**, **OBS-02**, **OBS-03** and **OBS-04**. None of
the four may be marked Complete here:

- **OBS-01** additionally needs Plan 118-06's non-regression sweep to confirm the two lines are the
  only serial-channel delta.
- **OBS-02** additionally needs Plan 118-05's trace proving the sequence's *total absence* from the
  recorded stream when the flag is set. Code that reads the flag is not proof that the sequence is
  gone.
- **OBS-03** additionally needs Plan 118-05's case proving the budget WARN actually fires. A check
  that has never been observed to fire is indistinguishable from a dead branch.
- **OBS-04** additionally needs Plan 118-07's real-board measurement.

<tasks>

<task type="auto">
  <name>Task 1: Emit the two unconditional report lines and bracket the emit call with micros()</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp lines 180-305 (read the whole span: the emitter's hard-constraint comment at :196-205, the emitter body :206-222, the completion-poll body :256-269, and `eeprom28c_write_init` :271-301 with the identity block ending at :279, the emit call at :291 and the wait call at :297)
    - firestarter/src/proms/eeprom_28c.cpp lines 106-114 (`EEPROM_SDP_DISABLE`'s extern declaration and definition — the array whose length drives both the emit call and the budget)
    - firestarter/include/logging_id.h lines 25-50 and 105-164 (the unconditional `LOG_ID` / `LOG_ID_U32` forms at :28-37; the `FLAG_VERBOSE`-gated `LOG_INFO_ID*` family at :42-84; and the severity-named alias block at :105-164 showing `LOG_WARN_ID*` is a literal alias of `LOG_ID*`)
    - firestarter/include/messages.h (confirm the four new ids from Plan 118-02 are present with values 0x5E, 0x5F, 0x86, 0x87)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md sections "(a) Where the before-line goes", "(f) The hard constraint on the emitter body", and "No Analog Found" rows 1 and 2
  </read_first>
  <action>
Edit `eeprom28c_write_init` only. Nothing in `eeprom28c_emit_command_sequence`'s body and nothing in
`eeprom28c_wait_for_sdp_completion`'s body may change.

**Hoist the sequence length to one expression.** Declare a function-local
`sdp_seq_len` initialised from the existing `sizeof(EEPROM_SDP_DISABLE) /
sizeof(EEPROM_SDP_DISABLE[0])` expression currently written inline at the emit call on line 291, and
pass it to the emit call. There must be exactly **one** length expression in the function — Task 2's
budget derives from the same local, so a second copy would be a silent second source of truth.
Constraint: the rewritten emit call must still contain the literal substring
`handle, EEPROM_SDP_DISABLE`, because the gate's append-only `_EMIT_ANCHOR_PATTERNS` matches on it.

**Before-line.** Emit `MSG_INFO_SDP_UNLOCK` through the bare, unconditional `LOG_ID` macro. Place it
**after** the identity block — the `if (handle->chip_id > 0) { ... }` that ends at line 279 — and
before the emit call. Reporting an unlock that never ran would be dishonest, and on an identity
mismatch without `--force` the function returns from inside that block.

**`micros()` bracket.** One read into `sdp_emit_start_us` immediately before the emit call; one read
immediately after it, with the elapsed value computed into `sdp_emit_us` as an unsigned 32-bit
subtraction so a `micros()` rollover during the window still yields a correct interval. Both reads sit
**outside** the emitter body — that is what makes D-05's "perturb inter-byte timing not at all"
literally true, and it is why the bracket measures the six command writes only. Do **not** extend the
bracket over `eeprom28c_wait_for_sdp_completion`: that call is a fixed `delay(AT28C_TWC_MAX_MS)` plus
an iteration-bounded poll, so including it would add a constant plus mock-dependent noise to the one
number that has engineering meaning. Do not report two numbers.

**After-line.** Emit `MSG_INFO_SDP_UNLOCK_DONE_US` through the bare, unconditional `LOG_ID_U32`
macro, carrying `sdp_emit_us`. Place it immediately after the second `micros()` read and before the
completion wait. Under Plan 118-01's rewritten gate that position is legal and is exactly what D-06
made room for; under the pre-rewrite gate it was a violation, which is why Plan 118-01 lands first.

**Argue the convention break in the source.** Both new INFO-band call sites use the *unconditional*
macros. Every one of the tree's 19 existing `MSG_INFO_*` emissions goes through the
`FLAG_VERBOSE`-gated `LOG_INFO_ID*` family, and there are currently **zero** bare `LOG_ID*` call
sites anywhere in `firestarter/src/`. There is no analog to cite; do not pretend otherwise. Write a
comment at the report lines stating: that the INFO band's only macro family is the gated one; that
gating these two lines would leave a default `firestarter write at28c256` silent, which is the defect
this phase exists to remove; and that unconditional emission is also what makes OBS-05's
"byte-identical apart from the two report lines" a substantive claim rather than a vacuous one. Note
also which frames a released `3.0.0b11` host will do with these ids — it logs an out-of-date-catalog
line and drops the frame — so an older host degrades gracefully.

**Prohibitions, all load-bearing.** No `handle->response_code` write anywhere on this path — that
preserves Phase 117's D-05 and `test_case8_completion_poll_preserves_prior_severity` will catch a
violation automatically. No new bus-visible call: the emitter's comment at lines 196-205 states that
anything beyond the explicit data-direction call and the `set_data` loop appends recorded strobes and
breaks the `SDP_FIXED_*` full-stream equality. No change to `eeprom28c_check_chip_id`'s behaviour. No
`LOG_DEBUG_ID_SUB` breadcrumb unless you also confirm it costs zero flash in the production build —
the paired-debug idiom at `flash_nor_unlock.cpp:85-86` is available but discretionary.

Rebuild both boards and record the flash delta against Plan 118-03's baseline. Then run the rewritten
host gate: it must still exit 0, proving the report lines are outside both scanned bodies.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -25 && pio run -e leonardo -e uno 2>&1 | tail -20 && cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'MSG_INFO_SDP_UNLOCK\b' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows exactly one call site, invoked through the bare unconditional macro (the identifier `LOG_INFO_ID` must NOT appear on that line)
    - `grep -n 'MSG_INFO_SDP_UNLOCK_DONE_US' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows exactly one call site, invoked through the bare unconditional u32 macro
    - `grep -c 'sizeof(EEPROM_SDP_DISABLE)' /workspaces/firestarter/src/proms/eeprom_28c.cpp` is exactly 1 (one length expression, hoisted into `sdp_seq_len`)
    - `grep -c 'micros()' /workspaces/firestarter/src/proms/eeprom_28c.cpp` is exactly 2, and `git diff` confirms both reads are between the identity block's closing brace and the completion-wait call — never inside `eeprom28c_emit_command_sequence`
    - `git -C /workspaces/firestarter diff -- src/proms/eeprom_28c.cpp` shows zero changed lines between line 206 and line 269 of the pre-plan file (the emitter body and the completion-poll body are untouched)
    - The emit call line still contains the substring `handle, EEPROM_SDP_DISABLE`
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 8 passed, 0 failed — in particular `test_case8_completion_poll_preserves_prior_severity` passes, proving no new `response_code` write on the SDP path
    - `cd /workspaces/firestarter && pio test -e native` reports the same total pass count as Plan 118-03's baseline, 0 failed, no SIGABRT
    - `cd /workspaces/firestarter && pio run -e leonardo` and `-e uno` both succeed; the flash delta versus Plan 118-03's baseline is recorded in the SUMMARY
    - `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0 with `PASS:` in stdout — the report lines are outside both scanned bodies
  </acceptance_criteria>
  <done>Two unconditional report lines exist with a `micros()` bracket around the emit call only; the emitter and poll bodies are byte-unchanged; the native suite is green including case 8; the rewritten gate still passes; the flash delta is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Turn AT28C_TBLC_MAX_US into a runtime budget check</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp lines 35-55 (the `AT28C_TBLC_MAX_US` `#define` from Plan 118-03 and the sibling `AT28C_TWC_MAX_MS` comment block it extends) and lines 271-301 as modified by Task 1
    - firestarter/src/proms/eeprom_28c.cpp lines 140-172 (the two live WARN call sites in this same file: `LOG_WARN_ID_U32(MSG_WARN_MEM_SIZE_TOO_SMALL, ...)` at :144 and `LOG_WARN_ID_BYTES(MSG_WARN_CHIP_ID_MISMATCH, ...)` at :168 — copy the macro spelling, NOT the `handle->response_code` write that accompanies each)
    - firestarter/include/messages.h (`MSG_WARN_SDP_TBLC_EXCEEDED` = 0x87, u32 param)
  </read_first>
  <action>
Compare Task 1's measured `sdp_emit_us` against the t_BLC budget and emit
`MSG_WARN_SDP_TBLC_EXCEEDED` through the unconditional `LOG_WARN_ID_U32` macro, carrying the measured
duration, when the budget is exceeded.

**Budget expression.** Total, not per-byte average: `sdp_seq_len` multiplied by `AT28C_TBLC_MAX_US`,
computed in unsigned 32-bit arithmetic. Deriving it from `sdp_seq_len` rather than a literal `6`
means the budget tracks the sequence length automatically if `EEPROM_SDP_DISABLE` ever changes
length — which matters because Phase 119 will drive the same emitter with a different table.

**Placement.** Immediately after the after-line from Task 1, before the completion wait. That keeps
the check outside the emit loop and reuses the number OBS-04 already computes — no extra measurement,
per D-09.

**Prohibition, restated because it is the phase's most-repeated invariant:** do not write
`handle->response_code`. The two live WARN call sites in this file both set it and both are wrong to
copy wholesale for this path. `test_case8_completion_poll_preserves_prior_severity` enforces the
separation permanently and will fail if you copy the severity write. Note also that
`MSG_WARN_FL4_BOOT_BLOCK_LOCKED` (0x85), which the discussion cited as this severity's precedent, has
**zero** firmware call sites — it is a catalog-entry precedent only, so the call sites at :144 and
:168 are your real analogs for macro spelling.

**Comment.** State that on a 16 MHz AVR this branch should never fire; that a latent invariant which
speaks up only when a future edit puts real work inside the emit loop is the correct outcome, not a
sign the check is useless; and that this is what makes `AT28C_TBLC_MAX_US` load-bearing rather than
decorative — a documentation-only constant was explicitly rejected because prose-only satisfaction of
OBS-03 is the hollow-gate debt shape this project keeps paying down. Cross-reference the page-load
citation added in Plan 118-03 so a reader sees the constraint is shared while the check is scoped.

**Discretionary, and you may take it:** additionally reporting the budget alongside the measured
duration on the after-line was rejected *as a substitute* for the branch but is compatible with it. If
you take it, the after-line's format string must not gain a hardcoded budget number — that would
duplicate `AT28C_TBLC_MAX_US`. Simplest compliant choice is to leave the after-line as Task 1 built
it; record your decision in the SUMMARY either way.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -25 && pio run -e leonardo 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'MSG_WARN_SDP_TBLC_EXCEEDED' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows exactly one call site, through the unconditional `LOG_WARN_ID_U32` macro
    - `grep -c 'AT28C_TBLC_MAX_US' /workspaces/firestarter/src/proms/eeprom_28c.cpp` is at least 4 (sibling-comment forward reference, the `#define`, the page-load citation, and this budget expression)
    - The budget expression references `sdp_seq_len`, and `grep -c 'sizeof(EEPROM_SDP_DISABLE)' /workspaces/firestarter/src/proms/eeprom_28c.cpp` is still exactly 1
    - No line added by this task contains an assignment to `handle->response_code` (verify by reading the diff, and prove it by the test below)
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 8 passed, 0 failed, including `test_case8_completion_poll_preserves_prior_severity`
    - `cd /workspaces/firestarter && pio test -e native` unchanged total pass count versus Plan 118-03's baseline, 0 failed
    - `cd /workspaces/firestarter && pio run -e leonardo` succeeds; flash delta recorded
    - `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0
  </acceptance_criteria>
  <done>The measured emit duration is compared at runtime against a `sdp_seq_len`-derived t_BLC budget, a WARN fires past it, nothing on the path writes `response_code`, and case 8 still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Honour FLAG_SKIP_SDP_UNLOCK — replace the whole sequence with one honest WARN</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - firestarter/src/proms/eeprom_28c.cpp lines 271-301 as modified by Tasks 1 and 2 (the region you are wrapping), plus line 298's `if (!is_flag_set(FLAG_SKIP_BLANK_CHECK)) { ... }` — the in-file skip-gate idiom to copy
    - firestarter/src/proms/flash_5v_page.cpp lines 55-80 (the REJECTED shape: `LOG_INFO_ID(MSG_INFO_SKIPPING_ERASE)` at :71 — read it so you can see what not to reproduce)
    - firestarter/include/firestarter.h lines 59-71 (`FLAG_SKIP_SDP_UNLOCK` from Plan 118-03 and the `is_flag_set` macro)
    - firestarter/include/messages.h (`MSG_WARN_SDP_UNLOCK_SKIPPED` = 0x86, zero params)
  </read_first>
  <action>
Gate the entire unlock block on the new flag, using the file's own skip-gate idiom inverted:
`if (!is_flag_set(FLAG_SKIP_SDP_UNLOCK)) { ... } else { ... }`, mirroring the shape already used for
`FLAG_SKIP_BLANK_CHECK` at line 298.

**The non-skip arm** contains everything Tasks 1 and 2 built: the before-line, the first `micros()`
read, the emit call, the second `micros()` read, the after-line, the budget check, **and** the
`eeprom28c_wait_for_sdp_completion(handle)` call.

**The skip arm** emits `MSG_WARN_SDP_UNLOCK_SKIPPED` through the unconditional `LOG_WARN_ID` macro and
does nothing else. Three properties, all required:

1. It is emitted **in place of** the before/after pair — never in addition to them. A skip path that
   also reports an unlock would be a lie.
2. The completion wait is skipped too. There is no internal write cycle to wait for when no command
   sequence was emitted, so waiting `AT28C_TWC_MAX_MS` and then polling would be pure latency plus
   two recorded read strobes that Plan 118-05's absence assertion would then have to explain away.
3. It does **not** write `handle->response_code`. WARN severity is carried by the message id's band
   alone. Setting a warning response code would break Phase 117's D-05 and would warn on every skip
   regardless of whether the skip mattered — on an unprotected part a skipped unlock is harmless.

The blank check at line 298 stays **outside** the new conditional. `FLAG_SKIP_BLANK_CHECK` is an
independent axis and its behaviour must not change.

**Comment.** State why WARN and not INFO: on an SDP-protected part a skipped unlock means the write
will not land, so the user must be told loudly — but the message must not fabricate an
operation-level failure for the unprotected case where the skip is harmless, which is why the
severity lives in the log line and not in `response_code`. Record that the `MSG_INFO_SKIPPING_ERASE`
INFO shape at `flash_5v_page.cpp:70` was considered and rejected: `write -b` silently skipping erase
is the footgun v1.16 Phase 92 had to fix, and this project's own memory records that `write -b` skips
erase entirely and still reports success. Also note that the host CLI flag that will set this bit
arrives in Phase 120 (HOST-01/HOST-03) — until then `0x100` is reachable only by a hand-built wire
`flags` value, and `get_flags`'s `extract_long` already parses it.

Rebuild both boards, record the final cumulative flash delta for the whole plan, and re-run the full
native suite plus the rewritten host gate.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -25 && pio run -e leonardo -e uno 2>&1 | tail -20 && cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py && python -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FLAG_SKIP_SDP_UNLOCK' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows exactly one `is_flag_set` use
    - `grep -n 'MSG_WARN_SDP_UNLOCK_SKIPPED' /workspaces/firestarter/src/proms/eeprom_28c.cpp` shows exactly one call site, through the unconditional `LOG_WARN_ID` macro
    - `git -C /workspaces/firestarter diff -- src/proms/eeprom_28c.cpp` confirms `eeprom28c_wait_for_sdp_completion(handle)` is inside the non-skip arm, and the `FLAG_SKIP_BLANK_CHECK` conditional is outside the new arm entirely
    - The whole plan's diff to `src/proms/eeprom_28c.cpp` contains zero changed lines in the pre-plan spans 206-222 and 256-269
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 8 passed, 0 failed
    - `cd /workspaces/firestarter && pio test -e native` unchanged total pass count versus Plan 118-03's baseline, 0 failed, no SIGABRT
    - `cd /workspaces/firestarter && pio run -e leonardo -e uno` both succeed; the SUMMARY records the Leonardo flash delta for the whole plan against Plan 118-03's figure
    - `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0
    - `cd /workspaces/firestarter_app && python -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py -q` exits 0 — the full CORRECTION-4 host-gate checklist against a changed `eeprom_28c.cpp`
    - `git -C /workspaces/firestarter_app status --short` is clean — this plan writes no host file
  </acceptance_criteria>
  <done>The flag gates the whole unlock block including the completion wait; the skip path emits exactly one WARN and no report lines; the blank-check axis is unchanged; the native suite and every host source-scanning gate are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire `flags` bit `0x100` → SDP behaviour | An untrusted host value now controls whether a write-protection-removal step runs |
| SDP path → `handle->response_code` | The operation-level severity channel, which this path must never touch |
| SDP command sequence inter-byte window | A timing-critical region where any inserted call is a correctness hazard |
| new serial frames → released b11 host | Firmware emits ids an older host has never seen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-04-SILENTSKIP | Repudiation | skip path | high | mitigate | Task 3 emits `MSG_WARN_SDP_UNLOCK_SKIPPED` unconditionally at WARN severity in place of the report pair; the INFO shape at `flash_5v_page.cpp:70` is explicitly rejected in the source comment so it is not reintroduced |
| T-118-04-SEVCLOBBER | Tampering | `handle->response_code` on the SDP path | high | mitigate | Three separate task-level prohibitions plus the existing permanent enforcement by `test_case8_completion_poll_preserves_prior_severity`, asserted in each task's criteria |
| T-118-04-LOGINWINDOW | Tampering | emitter and completion-poll bodies | high | mitigate | Plan 118-01's rewritten gate is a hard ordering precondition and is re-run in all three tasks; diff-scoped criteria assert zero changed lines in the two body spans |
| T-118-04-SILENTDEFAULT | Repudiation | default (no-flag) path | high | mitigate | D-01's unconditional macros: the bare `LOG_ID` / `LOG_ID_U32` forms, with the convention break argued in the source comment. A verbose-gated line would be a no-op for this phase's purpose |
| T-118-04-DEADBRANCH | Repudiation | the t_BLC budget check | medium | mitigate | Plan 118-05 adds a case proving the WARN actually fires; this plan explicitly does not close OBS-03 |
| T-118-04-BUSDELTA | Tampering | recorded bus stream vs `SDP_FIXED_*` goldens | high | mitigate | No new bus-visible call; `micros()` and `LOG_*` are not recorded strobe kinds; the eight existing stream cases are re-run in every task |
| T-118-04-ROLLOVER | Tampering | `micros()` interval arithmetic | low | mitigate | Elapsed computed as an unsigned 32-bit subtraction so a rollover inside the window still yields a correct interval |
| T-118-04-STALEHOST | Information disclosure | b11 host receiving 0x5E/0x5F/0x86/0x87 | low | accept | `codec.py:206-209` logs an out-of-date-catalog line and drops the frame; confirmed in Plan 118-02 |
| T-118-04-SC | Tampering | package-manager installs | low | accept | This plan installs nothing — no npm/pip/cargo install task exists in Phase 118, so no package legitimacy audit is required |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && pio test -e native` — same total pass count as Plan 118-03's baseline, zero failures, no SIGABRT. `test_eeprom28c_sdp` at 8/8 including case 8.
2. `cd /workspaces/firestarter && pio run -e leonardo -e uno` — both build; record the cumulative flash delta against Plan 118-03. Judge it against Phase 117's measured **+204 B**, not against any predicted saving.
3. `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0.
4. `cd /workspaces/firestarter_app && python -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py tests/test_sdp_db_invariant.py -q` exits 0 — the mandatory CORRECTION-4 item-4 check of firmware edits against every host gate that scans firmware source.
5. `git -C /workspaces/firestarter_app status --short` and `git -C /workspaces status --short` show no file staged by this plan outside the `firestarter` submodule.
6. Three commits inside `/workspaces/firestarter`. Suggested subjects: `feat(118-04): report the SDP auto-unlock with a micros()-measured duration (OBS-01, OBS-04)`; `feat(118-04): enforce the t_BLC budget at runtime on the SDP unlock (OBS-03, D-09)`; `feat(118-04): honour FLAG_SKIP_SDP_UNLOCK with an honest WARN (OBS-02, D-02)`.
</verification>

<success_criteria>
- A default `0x0D` write emits exactly two new INFO-band frames — one before the sequence, one after, carrying the measured emit duration.
- Neither frame is `FLAG_VERBOSE`-gated, and the convention break is argued in the source.
- Setting `FLAG_SKIP_SDP_UNLOCK` replaces the sequence, the report pair and the completion wait with one WARN frame.
- The t_BLC budget is enforced at runtime from a single `sdp_seq_len`-derived expression.
- Nothing on the SDP path writes `handle->response_code`; case 8 proves it.
- The emitter body and completion-poll body are byte-unchanged; the rewritten gate passes.
- OBS-01, OBS-02, OBS-03 and OBS-04 remain **Pending** in `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
Create `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-04-SUMMARY.md` when done.
Record: the final shape of `eeprom28c_write_init` described in prose (arm structure and call order), the exact identifier names used for the three locals, the Leonardo and Uno flash deltas against Plan 118-03, the decision taken on the discretionary budget-on-the-after-line composition, and the two window line ranges the rewritten gate reported after the edit (Plan 118-05 and Plan 118-06 both cite them).
</output>
