---
phase: 118-observe-auto-unlock-visible-opt-out-able-fw-half
plan: 05
type: execute
wave: 5
depends_on: [118-04]
files_modified:
  - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
  - firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md
autonomous: true
requirements: [OBS-02, OBS-03, OBS-05]

must_haves:
  truths:
    - "D-08 constraint 1: the skip-proof case drives PRODUCTION `eeprom28c_write_init` through `configure_memory` dispatch — never the harness's `drive_reference_emitter`, which drives a different table."
    - "D-08 constraint 2: the skip-proof case asserts on the ordered stream's CONTENT, never on a call count. Register-write elision is invisible to a counting test (Phase 116 research finding 10), so a bare `strobe_count() == 0` is itself a count assertion and is not sufficient on its own."
    - "D-08 constraint 3: the flag-absent counterpart, asserting the full `SDP_FIXED_*` stream from the same handle factory, ships in the SAME commit as the skip case. The pair is what makes the contrast executable in one place."
    - "D-07: OBS-05 is asserted on the recorded BUS stream. The `SDP_FIXED_*` arrays in `firestarter/test/native/avr/_shared/sdp_expected.h` stay byte-identical with no regeneration, proven by literal git blob SHA against the phase base, and the flag-absent path drives the same sequence."
    - "D-07: the two new serial frames are a NAMED, ENUMERATED exception on the serial channel — written down, not hidden. Report lines go out via `rurp_log_id` to Serial, which the Phase-116 recorder does not observe: it records only `rurp_write_to_register`, `rurp_write_data_buffer` and `rurp_set_control_pin`, so the bus stream genuinely is unchanged."
    - "D-07 boundary respected: no general-purpose serial-frame baseline recorder is built. That was rejected as Phase-116-class harness work. The frame enumeration added here is a per-case Serial capture using the existing in-tree idiom from `test_rurp_log_id.cpp`, not new harness infrastructure."
    - "D-09's runtime budget check is proven to actually fire — a case that drives the emit with a synthesised over-budget elapsed value and observes the WARN frame. A check never observed to fire is indistinguishable from a dead branch, which is the hollow-gate shape this project keeps paying down."
  artifacts:
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp
    - firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md
  key_links:
    - "`sdp_strobe_t` records only `{kind, pin, value}` with `STROBE_KIND_DATA = 1` and `STROBE_KIND_PIN = 2` (`sdp_expected.h:57-58`) — `rurp_log_id` is not a recorded kind, and `Serial.write` is mocked in both suites' `setUp`, so a `LOG_*` before or after the emit cannot append a strobe."
    - "`make_identity_handle(expected_chip_id, ctrl_flags)` at `test_eeprom28c_sdp.cpp:139-155` already takes `ctrl_flags` as a parameter and ORs `FLAG_SKIP_BLANK_CHECK` in — it is the cleaner analog for adding a flags argument to `make_sdp_handle`."
    - "The suite deliberately does NOT stub `handle->firestarter_set_data`; Phase 117's emitter is built on that pointer. Only `get_data` is mocked. Do not reintroduce a `set_data` no-op."
    - "Plan 118-03's `s_micros_ticks` seam in this suite is what makes the budget-exceeded case possible; a fixed-value `micros` mock would make the branch unreachable."
---

<objective>
Prove in the native suite what the firmware now does: that `FLAG_SKIP_SDP_UNLOCK` removes the unlock
sequence from the recorded bus stream entirely, that its absence contrasts against a full
`SDP_FIXED_*` stream from the same handle factory, that the t_BLC budget WARN actually fires, and
that the flag-absent path emits exactly the two new report frames and nothing else on the serial
channel.

Purpose: OBS-02's success criterion is the sequence's *total absence from the emitted stream*, not
"the code reads the flag". OBS-03's runtime check is worthless if nobody has seen it fire. OBS-05's
byte-identity claim rests on the goldens being provably unregenerated.

Output: four new cases in `test_eeprom28c_sdp`, a golden blob-SHA identity record, and an appended
baseline heading.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-CONTEXT.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-03-SUMMARY.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-04-SUMMARY.md
</context>

## Repo and commit mechanics

Both files live in the **`firestarter` submodule** at `/workspaces/firestarter` (branch
`v1.22-at28c-software-data-protection-lifecycle`; confirm with `git -C /workspaces/firestarter branch
--show-current`). Commit inside that submodule. No meta-repo file, no host file, no gitlink bump.
`pio` runs against the shared `.pio/` tree — no concurrent runs.

`firestarter/platformio.ini` needs **no** change: `test_filter` already lists
`native/avr/test_eeprom28c_sdp` at line 119 and the suite already has an `-I` entry. Confirm and leave
it alone.

## Artifacts this phase produces

**Created by THIS plan** — new native test cases in `test_eeprom28c_sdp.cpp`:
`test_case9_skip_flag_suppresses_unlock_stream`,
`test_case10_flag_absent_emits_full_unlock_stream`,
`test_case11_tblc_budget_exceeded_warns`,
`test_case12_flag_absent_emits_exactly_two_report_frames`; plus helpers
`make_sdp_handle_with_flags` (or a `ctrl_flags` parameter added to the existing `make_sdp_handle`),
`captured_frames`, and use of Plan 118-03's `s_micros_ticks`.

**Consumed here, created earlier in Phase 118:** `MSG_INFO_SDP_UNLOCK` (0x5E),
`MSG_INFO_SDP_UNLOCK_DONE_US` (0x5F), `MSG_WARN_SDP_UNLOCK_SKIPPED` (0x86),
`MSG_WARN_SDP_TBLC_EXCEEDED` (0x87) — Plan 118-02; `FLAG_SKIP_SDP_UNLOCK` (0x100) and
`AT28C_TBLC_MAX_US` (100) — Plan 118-03; `s_micros_ticks` — Plan 118-03; `sdp_seq_len`,
`sdp_emit_start_us`, `sdp_emit_us` — Plan 118-04.

**Created elsewhere in Phase 118:** host gate identifiers `_EMITTER_FUNC_NAME`, `_POLL_FUNC_NAME`,
`_resolve_windows`, widened `_find_function_body(cleaned_text, func_name)`, host test cases
`test_checker_exits_nonzero_on_log_planted_in_completion_poll_body` and
`test_checker_fails_closed_when_emitter_body_is_absent` — Plan 118-01;
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md` —
Plan 118-07.

## Requirement ownership

This plan **closes OBS-02** (flag honoured, proven by the sequence's total absence from the recorded
stream) and **closes OBS-03** (constant, both citations, runtime check, and now the proof it fires).
Mark those two Complete in `.planning/REQUIREMENTS.md` only after this plan's cases pass.

**OBS-05 stays open.** This plan lands its bus-stream and golden-identity half; the full
non-regression sweep across both boards, the full native suite, the host gate set and the catalog
three-way `cmp` is Plan 118-06. Do not mark OBS-05 Complete here.

**OBS-01 and OBS-04 stay open** here too — OBS-01 closes with Plan 118-06's sweep, OBS-04 with Plan
118-07's board measurement. This plan lists neither in its `requirements`.

<tasks>

<task type="auto">
  <name>Task 1: The skip/no-skip stream pair — content-positional, from one handle factory</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp (whole file — the `setUp` mock block including Plan 118-03's `micros` mock and `s_micros_ticks`, `make_sdp_handle` at :126-137, `make_identity_handle(expected_chip_id, ctrl_flags)` at :139-155, `drive_reference_emitter` at :194-201, `drive_write_init` at :211-220, the eight existing cases, and the `RUN_TEST` block at :505-512)
    - firestarter/test/native/avr/_shared/sdp_expected.h lines 40-120 and 190-315 (`sdp_strobe_t`, the `STROBE_KIND_*` values at :57-58, `sdp_first_divergence`, `sdp_assert_stream_equals` at :86-107, and the four `SDP_FIXED_*` arrays with their `_LEN` macros)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (the recorder, `clear_strobes` / `strobe_count` / `strobe_kind` / `strobe_pin` / `strobe_value` / `strobe_overflowed`, and the `HOST_STUBS_RECORD_BUS` / `HOST_STUBS_REAL_REGISTER_UTILS` opt-in contract)
    - firestarter/src/proms/eeprom_28c.cpp lines 271-310 as modified by Plan 118-04 (the arm structure the two cases discriminate)
  </read_first>
  <action>
Add two cases that ship together, adjacent in the file and adjacent in the `RUN_TEST` block, so the
skip/no-skip contrast is a single executable comparison rather than an inference across two distant
cases.

**Handle factory with flags.** `make_sdp_handle(row)` at lines 126-137 hardcodes
`h.ctrl_flags = FLAG_SKIP_BLANK_CHECK`. Give it a `ctrl_flags` parameter that is OR'd with
`FLAG_SKIP_BLANK_CHECK`, following the shape `make_identity_handle(expected_chip_id, ctrl_flags)` at
lines 139-155 already uses — or add a sibling `make_sdp_handle_with_flags(row, ctrl_flags)` if
changing the existing signature would churn the eight current call sites more than you want. Either
way both new cases must obtain their handle from the **same** factory with the **same** row, so the
only difference between them is the flag bit. `FLAG_SKIP_BLANK_CHECK` must stay set in both, exactly as
today, so the blank-check axis contributes no strobes to either stream.

**`test_case9_skip_flag_suppresses_unlock_stream`.** Drive production `eeprom28c_write_init` via the
existing `drive_write_init` helper — which goes through `configure_memory` dispatch — with
`FLAG_SKIP_SDP_UNLOCK` set, on the `DIP28_28C256` row. Assert on the ordered stream's **content**,
positionally:

- `strobe_overflowed()` is 0.
- The recorded stream does **not** begin with `SDP_FIXED_DIP28_28C256`'s first entries. Express this
  positionally: `sdp_first_divergence(SDP_FIXED_DIP28_28C256, SDP_FIXED_DIP28_28C256_LEN)` must return
  index **0**, i.e. the streams diverge at the very first element, and assert that exact index rather
  than merely `!= -1`. That is a content assertion about position 0, not a count.
- Neither of the two data bytes the SDP command sequence is defined by appears anywhere in the
  recorded stream as a `STROBE_KIND_DATA` entry — walk the recording with `strobe_kind(i)` and
  `strobe_value(i)` and assert the command-sequence payload bytes from `EEPROM_SDP_DISABLE` are absent.
  This is the assertion that survives register-write elision: a count-based check would pass even if
  the emitter ran and every write was elided.
- Only then, as a secondary corroboration and never as the primary assertion, you may also record what
  `strobe_count()` is. Do not make a bare count the load-bearing assertion — a bare
  `strobe_count() == 0` **is** a count assertion and D-08 forbids it as the proof.

Give the case a `TEST_ASSERT_*_MESSAGE` context string naming the requirement (OBS-02) and the flag,
matching the existing cases' message style.

**`test_case10_flag_absent_emits_full_unlock_stream`.** Same factory, same row, `ctrl_flags` without
`FLAG_SKIP_SDP_UNLOCK`. Drive `drive_write_init` and assert the full stream with
`sdp_assert_stream_equals(SDP_FIXED_DIP28_28C256, SDP_FIXED_DIP28_28C256_LEN, ...)`. Case 1 already
makes a similar assertion but from the *old* factory shape; this counterpart exists so the pair is
self-contained and so a future edit to the factory cannot silently weaken one side of the contrast.
Add a comment saying exactly that, and cross-reference case 1.

Register both in `RUN_TEST` order after `test_case8_completion_poll_preserves_prior_severity`. Do not
reorder or rename any existing case. Do not stub `handle->firestarter_set_data` — the suite
deliberately leaves it real because Phase 117's emitter is built on that pointer.

Update the suite's file-header docstring case list to cover the new cases.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 10 passed, 0 failed
    - `grep -c 'test_case9_skip_flag_suppresses_unlock_stream' /workspaces/firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp` is 2 (definition plus `RUN_TEST`); same for `test_case10_flag_absent_emits_full_unlock_stream`
    - Case 9's assertions include an exact-index-0 divergence assertion and an explicit walk over `strobe_kind`/`strobe_value` for the `EEPROM_SDP_DISABLE` payload bytes — verified by reading the case, and recorded in the SUMMARY
    - Anti-hollow spot check: temporarily invert the production skip condition in `eeprom_28c.cpp` (so the flag is ignored), re-run the suite, and confirm case 9 FAILS; then revert. Do not commit the inversion. Record the observed failure message in the SUMMARY
    - Anti-hollow spot check 2: temporarily force the skip arm unconditionally, re-run, and confirm case 10 FAILS; then revert. Record it
    - Both cases obtain their handle from the same factory with the same `sdp_bus_config_row_t` row (verified by reading)
    - `cd /workspaces/firestarter && pio test -e native` unchanged total pass count plus 2, 0 failed, no SIGABRT
    - `git -C /workspaces/firestarter diff -- src/proms/eeprom_28c.cpp platformio.ini` is empty after the spot checks are reverted
  </acceptance_criteria>
  <done>Ten cases pass; the skip case asserts stream content positionally and survives register-write elision; the flag-absent counterpart asserts the full golden stream from the same factory; both anti-hollow inversions were observed to fail and were reverted.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the t_BLC budget WARN fires, and enumerate the two new serial frames</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp `setUp` (the Serial write mocks currently `AlwaysReturn(1)`, and Plan 118-03's `micros` mock plus `s_micros_ticks`)
    - firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp lines 30-120 (the in-tree Serial-capture idiom: an `AlwaysDo` handler on the single-byte `Serial.write` overload accumulating into a host `std::vector`, and the frame layout — 4 magic bytes, 2 length bytes, 1 id byte, params, 1 CRC, 1 re-sync anchor)
    - firestarter/src/proms/eeprom_28c.cpp lines 271-315 as modified by Plan 118-04 (the budget expression and the four call sites)
    - firestarter/include/messages.h (the four new id values, needed as expected id bytes)
  </read_first>
  <action>
Two cases. Both need the emitted serial frames to be observable, so this task retrofits the suite's
`setUp` with the tree's existing Serial-capture idiom.

**Serial capture, additively.** The suite's `setUp` currently mocks the single-byte `Serial.write`
overload with `AlwaysReturn(1)`. Replace that one mock with an `AlwaysDo` handler that pushes the byte
into a file-static `captured_frames` container **and returns 1**, exactly the shape
`test_rurp_log_id.cpp` uses at lines 59-63. Clear the container in `setUp` alongside the existing
resets. Leave the buffer-form `write` overload and the `flush` mock as they are. This must be
behaviourally transparent to the ten existing cases — accumulating bytes changes nothing they assert —
so re-run all ten and confirm 10/10 before adding the new cases.

**Scope discipline (D-07).** This is a per-suite capture using an existing idiom, not a general-purpose
serial-frame baseline recorder. Building such a recorder was explicitly rejected as Phase-116-class
harness work and is **not** taken here. Do not add anything to `_shared/`, do not add a new opt-in
compile flag, and do not touch `host_stubs_common.inc`. Say so in a comment so a later reader does not
mistake this for the rejected recorder.

Write a small file-local helper that scans `captured_frames` for the id byte of a frame given the
known layout — the id byte sits at a fixed offset after the 4-byte magic and the 2-byte length. Reuse
`test_rurp_log_id.cpp`'s documented layout; do not re-derive it. Assert on **which ids appear and in
what order**, never on a raw byte count of the whole buffer.

**`test_case11_tblc_budget_exceeded_warns`.** Drive `drive_write_init` with the flag absent, having
first set Plan 118-03's `s_micros_ticks` seam so the two production `micros()` reads return values
whose difference **exceeds** the budget — the budget is the SDP sequence length times
`AT28C_TBLC_MAX_US`, so any elapsed value above that works; pick one comfortably above, and derive it
from the same constants rather than hardcoding a magic number if the seam allows. Assert that
`MSG_WARN_SDP_TBLC_EXCEEDED`'s id appears in the captured frames, and that it appears **after**
`MSG_INFO_SDP_UNLOCK_DONE_US` (the check runs after the after-line). Then add the paired control in the
same case or an adjacent one: with the default tick behaviour (elapsed 0) the WARN id does **not**
appear. The pair is the anti-hollow proof — an appearing-only assertion could pass against a branch
that always fires.

Also assert, in this case, that `handle->response_code` is still `RESPONSE_CODE_OK` after the drive.
The budget WARN must not clobber severity; Phase 117's D-05 forbids it on this path.

**`test_case12_flag_absent_emits_exactly_two_report_frames`.** Drive `drive_write_init` with the flag
absent and default ticks, and assert the captured frame ids contain **exactly** `MSG_INFO_SDP_UNLOCK`
followed by `MSG_INFO_SDP_UNLOCK_DONE_US`, and neither `MSG_WARN_SDP_UNLOCK_SKIPPED` nor
`MSG_WARN_SDP_TBLC_EXCEEDED`. Add the skip-path mirror: with `FLAG_SKIP_SDP_UNLOCK` set, the captured
frame ids contain exactly `MSG_WARN_SDP_UNLOCK_SKIPPED` and neither INFO id. That mirror is the
executable form of D-02's "in place of, never in addition to".

This case makes OBS-05's serial-channel exception machine-checked rather than prose-only. That is a
strengthening of D-07, not a replacement: D-07's primary assertion remains the bus-stream byte
identity proven in Task 1 and Task 3, and this case does not build the rejected recorder. State that
relationship in the case's comment.

Register both in `RUN_TEST` after the Task-1 cases. Update the file-header docstring case list.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` reports 12 passed, 0 failed
    - The ten pre-existing cases still pass after the `Serial.write` mock was changed from `AlwaysReturn` to `AlwaysDo` (run them before adding the new cases and record that intermediate 10/10 in the SUMMARY)
    - `grep -c 'test_case11_tblc_budget_exceeded_warns' /workspaces/firestarter/test/native/avr/test_eeprom28c_sdp/test_eeprom28c_sdp.cpp` is 2; same for `test_case12_flag_absent_emits_exactly_two_report_frames`
    - Case 11 asserts BOTH that the WARN id appears with an over-budget elapsed value AND that it does not appear with the default elapsed value
    - Case 11 asserts `handle->response_code` is still `RESPONSE_CODE_OK` after the drive
    - Case 12 asserts the exact id sequence on the flag-absent path and the exact single id on the skip path
    - Anti-hollow spot check: temporarily invert the budget comparison in `eeprom_28c.cpp` (fire when under budget), re-run, confirm case 11 FAILS, then revert. Record the failure message in the SUMMARY. Do not commit the inversion
    - `git -C /workspaces/firestarter diff --name-only` after the spot check lists only the test file, and specifically does NOT list `test/native/avr/_shared/host_stubs_common.inc` or any other `_shared/` path (D-07's rejected-recorder boundary held)
    - `cd /workspaces/firestarter && pio test -e native` reports the Task-1 total plus 2, 0 failed, no SIGABRT
  </acceptance_criteria>
  <done>Twelve cases pass; the budget WARN is observed to fire on an over-budget elapsed value and observed not to fire otherwise; the flag-absent path is proven to emit exactly the two report frames and the skip path exactly one WARN frame; no `_shared/` file was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the SDP_FIXED_* goldens are byte-identical and append the baseline record</name>
  <files>firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md</files>
  <read_first>
    - firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md (whole file — the seven-case record, the verbatim RED capture, the `## Post-suite-edit RED baseline (Phase 117 commit 1 — D-03)` appended heading convention, and the section "Declined widening, recorded as an open hook")
    - firestarter/test/native/avr/_shared/sdp_expected.h (the four `SDP_FIXED_*` arrays — the byte-identity subject; confirm no line changed)
    - .planning/phases/117-fix-remap-aware-0x0d-emitter-honest-completion-signal/117-05-SUMMARY.md (the git-blob-SHA identity technique this task reuses, and the phase-base commit it compared against)
  </read_first>
  <action>
Prove the golden arrays were not regenerated, and record this phase's baseline in the existing
artifact rather than a new one.

**Blob-SHA identity.** Determine Phase 118's base commit in `/workspaces/firestarter` — the commit
this phase forked from, i.e. `f8d10a5` per the plan-time record; confirm it against the actual first
Phase-118 commit's parent chain rather than trusting the literal. Then compare the literal git blob
SHA of `test/native/avr/_shared/sdp_expected.h` at that base against its SHA at `HEAD` using
`git rev-parse <base>:<path>` and `git rev-parse HEAD:<path>`. They must be equal. This is the same
technique Plan 117-05 used to freeze six artifacts and it is stronger than a `git diff` check because
it cannot be satisfied by a whitespace-normalising diff option. Do the same for
`test/native/avr/_shared/host_stubs_common.inc` and `test/native/avr/_shared/sdp_bus_config.h` — all
three `_shared/` files must be blob-identical, which also proves the declined recorder widening was
not accidentally taken.

If any SHA differs, stop and report it. A changed golden means the bus stream changed, which
contradicts D-07 and would invalidate OBS-05's central claim.

**Append to `RED-BASELINE.md`.** Follow the file's existing appended-heading convention. Add a
heading in the shape `## Phase 118 observability baseline (OBS-02, OBS-03, OBS-05)` recording:

- the four new case names and their one-line purpose;
- the verbatim `pio test -e native -f "*test_eeprom28c_sdp*"` output showing 12/12;
- the three `_shared/` blob SHAs with the base commit they were compared against, stated as
  unchanged;
- the enumerated serial-channel exception in plain words: on the flag-absent path the firmware now
  emits two additional INFO frames (`MSG_INFO_SDP_UNLOCK`, `MSG_INFO_SDP_UNLOCK_DONE_US`) and on the
  skip path one WARN frame (`MSG_WARN_SDP_UNLOCK_SKIPPED`), with a fourth WARN
  (`MSG_WARN_SDP_TBLC_EXCEEDED`) reachable only when the measured emit duration exceeds the t_BLC
  budget. State that the recorded **bus** stream is unchanged because `rurp_log_id` writes to Serial
  and the Phase-116 recorder observes only `rurp_write_to_register`, `rurp_write_data_buffer` and
  `rurp_set_control_pin`;
- an explicit note that the recorder widening to a third strobe kind (data-bus direction) named in
  this file's "Declined widening" section as Phase 118's owner was **NOT** taken — no OBS requirement
  needs it, taking it would force regeneration of `sdp_expected.h` and `test_sdp_harness`'s
  reference-emitter guards, and it stays deferred for the next owner to find rather than inheriting
  silence.

Do not rewrite or reflow any existing section of the file.

**Validation ceiling.** Nothing in what you write may read as evidence about AT28C silicon. These are
native register-trace and serial-frame assertions against mocked hardware. `0x0D` stays `UNVERIFIED`,
zero chips change `support_status`, and the 84-chip count is unchanged.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && for p in test/native/avr/_shared/sdp_expected.h test/native/avr/_shared/host_stubs_common.inc test/native/avr/_shared/sdp_bus_config.h; do echo "$p base=$(git rev-parse f8d10a5:$p) head=$(git rev-parse HEAD:$p)"; done</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter rev-parse <base>:test/native/avr/_shared/sdp_expected.h` equals `git -C /workspaces/firestarter rev-parse HEAD:test/native/avr/_shared/sdp_expected.h`
    - The same equality holds for `test/native/avr/_shared/host_stubs_common.inc` and `test/native/avr/_shared/sdp_bus_config.h`
    - The base commit used was confirmed from the parent chain of the first Phase-118 firmware commit, not taken on faith from the plan text; the confirmed SHA is recorded in the SUMMARY
    - `RED-BASELINE.md` contains a new heading matching `## Phase 118 observability baseline`
    - That section contains the verbatim 12/12 suite output, all three blob SHAs, the four-frame serial-channel enumeration, and the explicit not-taken note for the declined recorder widening
    - `git -C /workspaces/firestarter diff` shows zero changed lines in any existing `RED-BASELINE.md` section — additions only
    - `RED-BASELINE.md`'s new section contains no claim about AT28C silicon behaviour; it names mocked native assertions only (reviewed and recorded in the SUMMARY)
    - `cd /workspaces/firestarter && pio test -e native` reports 12 passing in `test_eeprom28c_sdp` and 0 failures overall
  </acceptance_criteria>
  <done>All three `_shared/` files are proven blob-identical to the phase base, the baseline record is appended without disturbing existing sections, the serial-channel exception is enumerated in writing, and the declined recorder widening is recorded as still not taken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production handler → recorded bus stream | The stream is the phase's primary evidence; anything that perturbs it invalidates OBS-05 |
| test assertions → the property they claim to prove | A count-based or exit-code-only assertion can pass while the property is false |
| golden arrays → regeneration | Silently re-blessing a golden converts a regression into a "pass" |
| serial capture mock → the ten existing cases | Changing a shared `setUp` mock can perturb unrelated cases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-05-COUNTONLY | Repudiation | case 9's skip proof | high | mitigate | Assertions are content-positional: exact divergence index 0 plus an explicit walk over recorded `STROBE_KIND_DATA` values for the `EEPROM_SDP_DISABLE` payload bytes. A bare `strobe_count()` check is explicitly forbidden as the load-bearing assertion |
| T-118-05-DEADBRANCH | Repudiation | the t_BLC budget check | high | mitigate | Case 11 observes the WARN firing on an over-budget elapsed value AND not firing on the default value; an inverted-comparison spot check must be observed to fail the case |
| T-118-05-GOLDENREBLESS | Tampering | `SDP_FIXED_*` goldens | high | mitigate | Literal git blob SHA comparison against the confirmed phase-base commit for all three `_shared/` files; the base commit is confirmed from the parent chain rather than trusted from plan text |
| T-118-05-MOCKSPILL | Tampering | the shared `setUp` Serial mock | medium | mitigate | Task 2 requires the ten pre-existing cases to be re-run and recorded at 10/10 after the mock change and before the new cases are added |
| T-118-05-SCOPECREEP | Tampering | `_shared/` recorder infrastructure | medium | mitigate | Task 2's criteria assert the negative — no `_shared/` path appears in `git diff --name-only`. D-07's rejected serial-frame baseline recorder stays unbuilt and the declined strobe-kind widening stays untaken, recorded explicitly in Task 3 |
| T-118-05-CEILING | Repudiation | `RED-BASELINE.md` wording | high | mitigate | Task 3 requires the new section to name mocked native assertions only, with the reviewed wording recorded in the SUMMARY; `0x0D` stays `UNVERIFIED` and no chip changes `support_status` |
| T-118-05-SC | Tampering | package-manager installs | low | accept | This plan installs nothing — no npm/pip/cargo install task exists in Phase 118, so no package legitimacy audit is required |
</threat_model>

<verification>
1. `cd /workspaces/firestarter && pio test -e native -f "*test_eeprom28c_sdp*"` — 12 passed, 0 failed.
2. `cd /workspaces/firestarter && pio test -e native` — total pass count equals Plan 118-04's baseline plus 4, zero failures, no SIGABRT.
3. Three `_shared/` blob SHAs equal at base and HEAD.
4. Three anti-hollow spot checks each observed to fail the intended case, each reverted, each recorded: skip-condition inverted (case 9 fails), skip arm forced (case 10 fails), budget comparison inverted (case 11 fails).
5. `cd /workspaces/firestarter_app && python tools/check_no_log_in_sdp_window.py` exits 0 and `python -m pytest tests/test_sdp_table_parity.py tests/test_sdp_bus_config_drift.py -q` exits 0 — this plan touches only test files, but re-confirm the host gates because `sdp_bus_config.h` is a generated artifact with a drift gate.
6. `git -C /workspaces/firestarter diff -- platformio.ini src/ include/` is empty — no production or config file changed.
7. Two commits inside `/workspaces/firestarter`. Suggested subjects: `test(118-05): prove FLAG_SKIP_SDP_UNLOCK removes the unlock stream and the t_BLC WARN fires (OBS-02, OBS-03)`; `docs(118-05): append the Phase 118 observability baseline and golden blob-SHA identity (OBS-05)`.
8. Update `.planning/REQUIREMENTS.md`: mark **OBS-02** and **OBS-03** Complete. Leave OBS-01, OBS-04 and OBS-05 Pending.
</verification>

<success_criteria>
- `FLAG_SKIP_SDP_UNLOCK` set: the unlock sequence is provably absent from the recorded bus stream, asserted on content and position rather than on a count.
- Flag absent: the full `SDP_FIXED_DIP28_28C256` stream is asserted from the same handle factory, in the same commit.
- The t_BLC budget WARN is observed to fire, and observed not to fire under budget.
- The flag-absent path emits exactly two report frames; the skip path exactly one WARN frame.
- All three `_shared/` files are blob-identical to the phase base — no golden regeneration.
- The declined recorder widening is recorded as still not taken.
- OBS-02 and OBS-03 are marked Complete; OBS-01, OBS-04 and OBS-05 stay Pending.
</success_criteria>

<output>
Create `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-05-SUMMARY.md` when done.
Record: the four new case names with their exact assertion shapes, the three anti-hollow spot-check failure messages verbatim, the confirmed phase-base commit SHA and the three `_shared/` blob SHAs, the intermediate 10/10 result after the Serial-mock change, and the reviewed validation-ceiling wording of the new `RED-BASELINE.md` section.
</output>
