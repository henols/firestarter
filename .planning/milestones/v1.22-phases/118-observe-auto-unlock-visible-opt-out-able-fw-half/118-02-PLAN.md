---
phase: 118-observe-auto-unlock-visible-opt-out-able-fw-half
plan: 02
type: execute
wave: 2
depends_on: [118-01]
files_modified:
  - tools/catalog/messages.toml
  - firestarter/tools/catalog/messages.toml
  - firestarter/tools/catalog/codegen.py
  - firestarter/include/messages.h
  - firestarter_app/tools/catalog/messages.toml
  - firestarter_app/tools/catalog/codegen.py
  - firestarter_app/firestarter/messages.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04]

must_haves:
  truths:
    - "D-03: the three-repo codegen ritual is owned here as a named plan — edit the canonical meta `tools/catalog/messages.toml`, run `tools/catalog/sync_to_subrepos.sh`, regenerate BOTH `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`. A 'FW half' phase necessarily writes into `firestarter_app`; that is owned, not incidental."
    - "D-03: firmware-before-host stays intact because the host delta is generated code only — no CLI flag, no wire emission, and specifically NO `FLAG_SKIP_SDP_UNLOCK` added to `firestarter_app/firestarter/constants.py` (that is Phase 120 HOST-03)."
    - "D-03 in-phase proof is a LOCAL three-way `cmp` of all three `messages.toml` copies plus both sub-repos' `codegen.py --check` drift gates. The meta workflow `.github/workflows/catalog-sync-check.yml` checks out both sub-repos at `ref: main` and therefore CANNOT go green for v1.22 catalog work until the milestone merges — that red is expected-until-merge, not a phase defect."
    - "D-04: four separate ids with literal format strings, not one parameterised id carrying an unlock/lock discriminator. Phase 119 adds its own lock pair reusing this pattern. Rejected: a generic pair with a `0=unlock / 1=lock` u8 param."
    - "Every new id name is at most 32 characters, so `messages.h`'s `#define` name column does not reflow — the 0x5D-and-below padding is preserved and the generated diff stays reviewable."
    - "`messages.h` and `messages.py` are GENERATED. Neither is hand-edited or hand-normalised at any point in this plan."
  artifacts:
    - tools/catalog/messages.toml
    - firestarter/include/messages.h
    - firestarter_app/firestarter/messages.py
  key_links:
    - "`messages.toml`'s own header forbids reordering entries (codegen sorts by id ascending) — new INFO ids go after the 0x5D entry, new WARN ids after the 0x85 entry and before the `# ERROR (0xA0..0xDF)` divider."
    - "A released `3.0.0b11` host degrades gracefully on the four new ids: `firestarter_app/firestarter/codec.py:206-209` logs an unknown-message-id line and drops the frame — no crash, no garbled render."
---

<objective>
Add the four new message-catalog ids Phase 118's report lines need, and distribute them through the
full three-repo codegen ritual so all three `messages.toml` copies stay byte-identical and both
generated artifacts are regenerated rather than hand-written.

Purpose: Phase 117's structural failure was an unowned cross-repo step. This plan makes the
cross-repo step a named plan with its own verification, so nothing is discovered later.

Output: four ids in canonical `tools/catalog/messages.toml`, regenerated
`firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`, and three commits —
one per repo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-CONTEXT.md
@.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md
</context>

## Repo and commit mechanics — THREE repos, three commits

This plan is the phase's only multi-repo plan. Stage explicitly; never `git commit -a`.

1. **Meta** (`/workspaces`): `tools/catalog/messages.toml`. Commit **only** that path. Do **NOT**
   stage the `firestarter` or `firestarter_app` gitlinks — submodule pointers stay pinned, and both
   working trees will be dirty when you commit here. `git -C /workspaces status --short` will show
   ` M firestarter` and ` M firestarter_app`; leave those lines alone.
2. **Firmware** (`/workspaces/firestarter`, branch `v1.22-at28c-software-data-protection-lifecycle`,
   HEAD `f8d10a5` at plan time): `tools/catalog/messages.toml`, `tools/catalog/codegen.py` (only if
   the sync script actually changed it), `include/messages.h`.
3. **Host** (`/workspaces/firestarter_app`, same branch, HEAD after Plan 118-01):
   `tools/catalog/messages.toml`, `tools/catalog/codegen.py` (only if changed),
   `firestarter/messages.py`.

Verify both sub-repo branches with `git -C <path> branch --show-current` before the first write.

## Artifacts this phase produces

**New message-catalog ids created by THIS plan** (canonical `tools/catalog/messages.toml`, generated
into `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`):

| id | name | severity | params | format string |
|----|------|----------|--------|---------------|
| `0x5E` | `MSG_INFO_SDP_UNLOCK` | INFO | none | `SDP unlock: disabling write protection` |
| `0x5F` | `MSG_INFO_SDP_UNLOCK_DONE_US` | INFO | u32 dec | `SDP unlock emitted in %lu us` |
| `0x86` | `MSG_WARN_SDP_UNLOCK_SKIPPED` | WARN | none | `SDP unlock skipped -- write will not land on an SDP-protected part` |
| `0x87` | `MSG_WARN_SDP_TBLC_EXCEEDED` | WARN | u32 dec | `SDP unlock exceeded t_BLC budget: %lu us` |

Name lengths are 19, 27, 27 and 26 characters — all at or under the current longest name
`MSG_ERR_PROTOCOL_NOT_IMPLEMENTED` (32), so the generated header's name column does not reflow.

**Also produced elsewhere in Phase 118** (listed so a drift check does not flag them):
`FLAG_SKIP_SDP_UNLOCK` (0x100, `firestarter/include/firestarter.h`, Plan 118-03);
`AT28C_TBLC_MAX_US` (100, `firestarter/src/proms/eeprom_28c.cpp`, Plan 118-03); firmware locals
`sdp_seq_len` / `sdp_emit_start_us` / `sdp_emit_us` (Plan 118-04); host gate identifiers
`_EMITTER_FUNC_NAME`, `_POLL_FUNC_NAME`, `_resolve_windows` and the widened
`_find_function_body(cleaned_text, func_name)` (Plan 118-01); native test cases
`test_case9_skip_flag_suppresses_unlock_stream`,
`test_case10_flag_absent_emits_full_unlock_stream`, `test_case11_tblc_budget_exceeded_warns`,
`test_case12_flag_absent_emits_exactly_two_report_frames` with helpers
`make_sdp_handle_with_flags` / `s_micros_ticks` / `captured_frames` (Plan 118-05); host test cases
`test_checker_exits_nonzero_on_log_planted_in_completion_poll_body` and
`test_checker_fails_closed_when_emitter_body_is_absent` (Plan 118-01);
`.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-MEASUREMENT.md`
(Plan 118-07).

## Requirement ownership (do NOT mark these Complete here)

This plan lands the catalog-id prerequisite for **OBS-01**, **OBS-02**, **OBS-03** and **OBS-04**.
All four stay **open** until their sibling plans land: OBS-01 and OBS-04 need Plan 118-04 (the report
lines and the `micros()` bracket); OBS-02 needs Plans 118-03/04/05 (flag define, honouring, trace
proof); OBS-03 needs Plans 118-03/04/05 (the constant, its two citations, the runtime check and its
anti-hollow proof); OBS-04 additionally needs Plan 118-07 (the board measurement). A catalog id with
no call site satisfies nothing — `MSG_WARN_FL4_BOOT_BLOCK_LOCKED` (0x85) has been in this catalog
since Phase 95 with **zero** firmware call sites, which is exactly the shape to avoid claiming.

<tasks>

<task type="auto">
  <name>Task 1: Add the four ids to the canonical meta catalog</name>
  <files>tools/catalog/messages.toml</files>
  <read_first>
    - tools/catalog/messages.toml lines 1-20 (the header's severity-band map and the DO-NOT-REORDER rule) and lines 220-345 (the INFO tail through the WARN tail and the ERROR divider — the four entry shapes you copy)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md section "`tools/catalog/messages.toml` (meta, canonical) + the three-repo codegen ritual (D-03)"
  </read_first>
  <action>
Edit **only** `/workspaces/tools/catalog/messages.toml`. It is the canonical copy; the two sub-repo
copies are vendored by the sync script in Task 2 and must never be hand-edited.

Append the two INFO entries immediately after the `MSG_INFO_CMD` (0x5E is the next free INFO id;
`MSG_INFO_CMD` at 0x5D is the current highest, ending at line 274) and before the
`# WARN (0x80..0x9F)` band divider. Append the two WARN entries immediately after
`MSG_WARN_FL4_BOOT_BLOCK_LOCKED` (0x85, ending at line 339) and before the
`# ERROR (0xA0..0xDF)` divider at lines 341-343. Do not reorder any existing entry — the file header
states codegen sorts by id ascending and reordering is forbidden.

Use the exact ids, names, severities, params and format strings from the table in
"Artifacts this phase produces" above. Every entry gets `wire_format = "id_frame"`. The two
zero-param entries copy the `MSG_INFO_SKIPPING_ERASE` shape (lines 223-230); the two u32-carrying
entries copy the `MSG_WARN_MEM_SIZE_TOO_SMALL` shape (lines 325-331) exactly —
`params = [{ type = "u32", render = "dec" }]`.

Two constraints on the chosen names and strings, both load-bearing:

- **Name length ≤ 32 characters.** `messages.h` pads the `#define` name column to the longest name,
  currently `MSG_ERR_PROTOCOL_NOT_IMPLEMENTED` at 32. A 33+ character name reflows the whole
  generated header; the `0xBB` precedent commit turned 8 catalog lines into a 131-line diff that way.
  OBS-05 and this phase's flash-delta reporting both make claims about diff size, so keep the padding
  intact.
- **`MSG_WARN_SDP_TBLC_EXCEEDED`'s format string carries the measured duration only, never a literal
  budget number.** The budget is derived at runtime from `AT28C_TBLC_MAX_US` times the sequence
  length (Plan 118-04); baking `600` into the format string would create a second source of truth
  that silently drifts.

Per D-04 these are four separate ids with literal format strings, not one parameterised id.
Explicitly rejected during discussion and still rejected: a generic report-id pair carrying a u8
`0=unlock / 1=lock` discriminator. Phase 119 will add its own lock pair reusing this pattern, the
ritual, and the high-flag-bit plumbing.

Add no other id. In particular do not pre-add anything for Phase 119.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 tools/catalog/codegen.py --catalog /workspaces/tools/catalog/messages.toml --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MSG_INFO_SDP_UNLOCK$\|MSG_INFO_SDP_UNLOCK"' /workspaces/tools/catalog/messages.toml` finds the entry; the same for `MSG_INFO_SDP_UNLOCK_DONE_US`, `MSG_WARN_SDP_UNLOCK_SKIPPED`, `MSG_WARN_SDP_TBLC_EXCEEDED`
    - `/workspaces/tools/catalog/messages.toml` contains `id          = 0x5E`, `id          = 0x5F`, `id          = 0x86` and `id          = 0x87`
    - `python3 /workspaces/firestarter/tools/catalog/codegen.py --catalog /workspaces/tools/catalog/messages.toml --check` exits 0 (catalog validity: no duplicate id, every id inside its declared severity band)
    - Every one of the four new names is at most 32 characters (verify with an explicit length check, not by eye)
    - `git -C /workspaces diff --stat -- tools/catalog/messages.toml` shows insertions only, zero deletions
  </acceptance_criteria>
  <done>Four new entries exist in the canonical catalog, `--check` validates, no existing entry moved or changed, and no name exceeds 32 characters.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sync-and-regenerate ritual and prove three-way identity locally</name>
  <files>firestarter/tools/catalog/messages.toml, firestarter/tools/catalog/codegen.py, firestarter/include/messages.h, firestarter_app/tools/catalog/messages.toml, firestarter_app/tools/catalog/codegen.py, firestarter_app/firestarter/messages.py</files>
  <read_first>
    - tools/catalog/sync_to_subrepos.sh (whole file — its header states the edit-then-sync contract and the script performs the copy, the byte-identity diff, and both regenerations)
    - .github/workflows/catalog-sync-check.yml lines 20-60 (the three-way `cmp` steps AND the two `ref: main` checkout lines at :27 and :38 that make this workflow unable to go green for v1.22 work)
    - firestarter/.github/workflows/build.yml lines 55-70 (the firmware `--check` + regenerate + `git diff --exit-code include/messages.h` drift gate)
    - firestarter_app/.github/workflows/ci.yml lines 30-45 (the host equivalent for `firestarter/messages.py`)
  </read_first>
  <action>
Run `/workspaces/tools/catalog/sync_to_subrepos.sh` from the meta repo root. It is idempotent by
design. It copies `messages.toml` and `codegen.py` from meta into both sub-repos'
`tools/catalog/`, diffs the two vendored copies for byte-identity, and regenerates
`firestarter/include/messages.h` (`--language cpp`) and `firestarter_app/firestarter/messages.py`
(`--language python`).

**Python version, recorded honestly.** Both sub-repos' CI pins `python-version: '3.11'`
(`firestarter/.github/workflows/build.yml:58`, `firestarter_app/.github/workflows/ci.yml:32`) and the
`0xBB` precedent commit bodies state the regeneration was done under 3.11. This devcontainer has
only `python3` 3.12 — there is no `python3.11` binary. Do **not** fabricate a 3.11 run. Instead:
regenerate under 3.12, then run the sync script a second time and confirm `git diff --exit-code` is
clean on both generated files (idempotence under the version actually used), and record the exact
interpreter version in the SUMMARY as CI-PENDING/structurally-green per this project's established
Phase-98 and Phase-103 precedent. If the CI drift gates later disagree because codegen output is
version-sensitive, that surfaces as a CI failure, not as a silent divergence.

**Never hand-edit or hand-normalise either generated file.** `firestarter/include/messages.h` is
codegen-generated and both sub-repos have `--check` + `git diff --exit-code` drift gates that fail a
hand-edit. `firestarter_app/firestarter/messages.py`'s raw codegen output is already format-stable
and ruff-clean — do not run a formatter over it and do not tidy its layout.

**In-phase proof is a LOCAL three-way `cmp`, not the CI workflow.** Byte-compare all three copies of
`messages.toml`: meta ↔ firmware, meta ↔ host. Then run each sub-repo's own drift gate the way its
CI does: in `/workspaces/firestarter`, `python3 tools/catalog/codegen.py --catalog
tools/catalog/messages.toml --check` followed by a regenerate into `include/messages.h` and
`git diff --exit-code include/messages.h`; the analogous pair in `/workspaces/firestarter_app` for
`firestarter/messages.py`.

State plainly in the SUMMARY that meta `.github/workflows/catalog-sync-check.yml` checks out both
sub-repos at `ref: main` (lines 27 and 38), so it compares the meta *branch* copy against the
sub-repos' `main` and **cannot** go green for v1.22 catalog work until the milestone merges. That red
is expected-until-merge and must not be read later as a Phase-118 regression. Do not "fix" the
workflow's refs here; retargeting a CI workflow is out of this phase's scope.

Then confirm the generated diff shape: `git -C /workspaces/firestarter diff --stat --
include/messages.h` should show a small insertion-dominated diff, not a whole-file reflow. If the
header reflowed, a new name exceeded 32 characters — go back to Task 1 and shorten it rather than
accepting the reflow.

Finally, confirm the **negative**: `firestarter_app/firestarter/constants.py` is NOT modified by this
plan. `FLAG_SKIP_SDP_UNLOCK` does not belong there — that is Phase 120 HOST-03, and firmware-before-
host is a non-negotiable ordering invariant of this milestone.
  </action>
  <verify>
    <automated>cmp /workspaces/tools/catalog/messages.toml /workspaces/firestarter/tools/catalog/messages.toml && cmp /workspaces/tools/catalog/messages.toml /workspaces/firestarter_app/tools/catalog/messages.toml && cd /workspaces/firestarter && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --target include/messages.h --language cpp && git diff --exit-code include/messages.h && cd /workspaces/firestarter_app && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --target firestarter/messages.py --language python && git diff --exit-code firestarter/messages.py</automated>
  </verify>
  <acceptance_criteria>
    - `cmp` of meta ↔ firmware `tools/catalog/messages.toml` exits 0; `cmp` of meta ↔ host exits 0 (three-way byte identity)
    - `cd /workspaces/firestarter && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0
    - Regenerating `include/messages.h` then `git -C /workspaces/firestarter diff --exit-code include/messages.h` exits 0 (drift gate clean)
    - Regenerating `firestarter/messages.py` then `git -C /workspaces/firestarter_app diff --exit-code firestarter/messages.py` exits 0
    - `git -C /workspaces/firestarter diff --stat -- include/messages.h` shows an insertion-dominated diff, NOT a whole-file reflow (a reflow means a name exceeded 32 characters)
    - `firestarter/include/messages.h` contains all four of `MSG_INFO_SDP_UNLOCK`, `MSG_INFO_SDP_UNLOCK_DONE_US`, `MSG_WARN_SDP_UNLOCK_SKIPPED`, `MSG_WARN_SDP_TBLC_EXCEEDED` with values `0x5E`, `0x5F`, `0x86`, `0x87`
    - `firestarter_app/firestarter/messages.py` `CATALOG` contains all four ids with matching severities and format strings
    - `git -C /workspaces/firestarter_app diff --name-only` does NOT list `firestarter/constants.py` (the Phase-120 boundary is intact)
    - `cd /workspaces/firestarter_app && ruff check firestarter/messages.py && ruff format --check firestarter/messages.py` exit 0 with the file untouched by hand
  </acceptance_criteria>
  <done>All three `messages.toml` copies are byte-identical, both generated artifacts are regenerated and drift-gate-clean, the header did not reflow, and `constants.py` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm nothing else consumes the catalog, then commit in all three repos</name>
  <files>tools/catalog/messages.toml, firestarter/include/messages.h, firestarter_app/firestarter/messages.py</files>
  <read_first>
    - firestarter_app/firestarter/codec.py lines 200-215 (the unknown-message-id path that makes a b11 host degrade gracefully — confirm it before claiming it)
    - firestarter_app/tests/test_val_wire_5v_page.py lines 280-305 (the existing catalog-presence assertion style, to check whether any host test enumerates the catalog by count)
    - .planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-PATTERNS.md section "`eeprom_28c.cpp` cross-repo source-scan checklist (CORRECTION 4 item 4 — mandatory)"
  </read_first>
  <action>
Two confirmations, then three commits.

**Confirmation A — no catalog-cardinality test breaks.** Search the host test suite for any assertion
on the total number of catalog entries or on the highest id in a band. The known catalog assertions
are presence-only (`test_val_wire_5v_page.py` asserts specific names exist), which four new entries
cannot break — but confirm it rather than assuming. Run the full host pytest suite and compare
against the pre-plan baseline; there are two known pre-existing REDs that are NOT this plan's
regressions (`test_audit_coverage_matrix`'s stale golden, and the `test_no_programmer_found_*`
characterization tests which fail whenever a live board is attached at `/dev/ttyACM*`).

**Confirmation B — graceful degradation is real.** Read `firestarter_app/firestarter/codec.py` around
lines 206-209 and confirm an unrecognised message id is logged as an out-of-date-catalog line and the
frame is dropped, returning `None`. This is what makes it safe for Phase-118 firmware to emit ids a
released `3.0.0b11` host has never seen. Record the confirmed behaviour in the SUMMARY; do not assert
it via a new test (no requirement covers it and Phase 120 owns the host surface).

**Commits.** Three commits, one per repo, staged by explicit path. Suggested subjects, following the
`0xBB` precedent commit pair's shape:

- meta: `feat(118-02): add four SDP report-line catalog ids 0x5E/0x5F/0x86/0x87 (OBS-01..OBS-04)` —
  stage `tools/catalog/messages.toml` only. Do NOT stage the two gitlinks; both submodules are dirty
  at this point and staging them would bump pinned pointers.
- firmware: same subject with `(118-02)` — stage `tools/catalog/messages.toml`,
  `include/messages.h`, and `tools/catalog/codegen.py` only if the sync actually changed it.
- host: same subject — stage `tools/catalog/messages.toml`, `firestarter/messages.py`, and
  `tools/catalog/codegen.py` only if changed.

Each commit body must state the interpreter version used for regeneration and that the meta
`catalog-sync-check.yml` workflow is expected-red-until-milestone-merge because it checks out both
sub-repos at `ref: main`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -m pytest -q -k "wire_5v_page or messages or codec" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter_app && python -m pytest -q` shows no NEW failures versus the pre-plan baseline captured at the start of this task
    - No host test asserts a catalog entry count or a band maximum (confirmed by search, recorded in the SUMMARY)
    - `firestarter_app/firestarter/codec.py` unknown-id path confirmed to log-and-drop rather than raise (quote the line numbers in the SUMMARY)
    - `git -C /workspaces log --oneline -1` shows the meta commit; `git -C /workspaces show --stat HEAD` lists exactly one path, `tools/catalog/messages.toml`
    - `git -C /workspaces show --stat HEAD` does NOT list `firestarter` or `firestarter_app` (no gitlink bump)
    - `git -C /workspaces/firestarter show --stat HEAD` lists only catalog and generated-header paths
    - `git -C /workspaces/firestarter_app show --stat HEAD` lists only catalog and generated-module paths
  </acceptance_criteria>
  <done>Three commits exist, one per repo, each staged by explicit path with no gitlink bump; the host suite has no new failures; the graceful-degradation and no-cardinality-test confirmations are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical catalog → two generated artifacts | A hand-edit on either side of the generator silently desynchronises firmware and host renderings of the same id |
| firmware-emitted id → released b11 host | New ids reach hosts that predate them |
| meta repo → submodule gitlinks | An over-broad `git add` bumps pinned submodule pointers as a side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-118-02-HANDEDIT | Tampering | `messages.h` / `messages.py` | high | mitigate | Both regenerated by `sync_to_subrepos.sh`; both sub-repos' `--check` + `git diff --exit-code` drift gates run locally in Task 2; explicit prohibition on hand-normalising `messages.py` |
| T-118-02-DESYNC | Tampering | three `messages.toml` copies | high | mitigate | Local three-way `cmp` in Task 2; the CI workflow that normally enforces this is expected-red-until-merge and is documented as such rather than relied on |
| T-118-02-GITLINK | Tampering | meta submodule pointers | medium | mitigate | Task 3 stages by explicit path and asserts the negative — `git show --stat HEAD` must not list either submodule |
| T-118-02-STALEHOST | Information disclosure | b11 host rendering unknown ids | low | accept | `codec.py:206-209` logs an out-of-date-catalog line and drops the frame; no crash and no garbled output. Confirmed, not assumed |
| T-118-02-BANDVIOL | Tampering | severity-band allocation | medium | mitigate | `codegen.py --check` validates band membership and duplicate ids; ids chosen from the documented free ranges (INFO from 0x5E, WARN from 0x86) |
| T-118-02-SC | Tampering | package-manager installs | low | accept | This plan installs nothing — no npm/pip/cargo install task exists in Phase 118, so no package legitimacy audit is required |
</threat_model>

<verification>
1. Three-way `cmp` of `messages.toml` across meta, firmware and host — all identical.
2. `cd /workspaces/firestarter && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0.
3. Regenerate-and-`git diff --exit-code` clean for both `firestarter/include/messages.h` and `firestarter_app/firestarter/messages.py`.
4. `git -C /workspaces/firestarter diff --stat -- include/messages.h` shows no whole-file reflow.
5. `cd /workspaces/firestarter_app && python -m pytest -q` — no new failures versus baseline.
6. `cd /workspaces/firestarter_app && python -m pytest tests/test_check_no_log_in_sdp_window.py -q` — Plan 118-01's seven cases still pass (this plan does not touch the gate, but the repo is shared).
7. Three commits, one per repo, each with an explicitly staged path list and no gitlink bump.
8. Recorded in the SUMMARY: the interpreter version used, and the statement that meta `catalog-sync-check.yml` is expected-red-until-milestone-merge because of its `ref: main` checkouts.
</verification>

<success_criteria>
- Four new ids exist in all three catalog copies and in both generated artifacts.
- No generated file was hand-edited or hand-normalised.
- `messages.h`'s `#define` name column did not reflow.
- `firestarter_app/firestarter/constants.py` is unmodified — the Phase-120 host boundary holds.
- OBS-01, OBS-02, OBS-03 and OBS-04 remain **Pending** in `.planning/REQUIREMENTS.md`.
</success_criteria>

<output>
Create `.planning/phases/118-observe-auto-unlock-visible-opt-out-able-fw-half/118-02-SUMMARY.md` when done.
Record: the four final id/name/format triples verbatim, the interpreter version used for regeneration, the `include/messages.h` diff stat, the three commit SHAs (one per repo), and the expected-red-until-merge note for `catalog-sync-check.yml`.
</output>