---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 03
type: execute
wave: 2
depends_on: ["122-02"]
files_modified:
  - firestarter_app/firestarter/submit.py
  - firestarter_app/tests/test_submit.py
  - firestarter_app/firestarter/__init__.py
  - firestarter/include/version.h
autonomous: true
requirements: [CLOSE-03]
must_haves:
  truths:
    - "`origin/beta` is merged INTO the v1.22 branch in both sub-repos, so the outbound merge that fires CI later is a clean merge and `beta` never sees an unproven intermediate tree (D-06)"
    - "The two conflicted app files are resolved whole-file `--ours`, and the resolution is proven to be exactly branch HEAD by an EMPTY diff — never by a hunk-level hand-merge (C-11/C-12)"
    - "The firmware inbound merge is confirmed conflict-free and its only content change is `include/version.h` b11 to b13 (C-1)"
    - "Nothing is pushed by this plan; `origin/beta` in both repos is unchanged at the end of it"
  artifacts:
    - path: "firestarter_app/firestarter/submit.py"
      provides: "Post-merge content byte-identical to branch HEAD (zero-diff resolution)"
      contains: "henols/firestarter_prom"
    - path: "firestarter_app/tests/test_submit.py"
      provides: "Post-merge content byte-identical to branch HEAD; all 77 HEAD test functions retained"
    - path: "firestarter/include/version.h"
      provides: "Auto-merged version string advanced from 3.0.0b11 to 3.0.0b13"
      contains: "3.0.0b13"
  key_links:
    - from: "firestarter_app/firestarter/submit.py (merged)"
      to: "firestarter_app/firestarter/submit.py at branch HEAD"
      via: "git diff HEAD^1 HEAD -- <path> must be EMPTY; the resolution is proven identical, not merely tested"
      pattern: "submit_via_browser"
---

<objective>
Merge `origin/beta` **into** the v1.22 branch in both sub-repos, resolving the app's two conflicted
files whole-file `--ours` with an empty-diff proof. This is D-06's inbound half: the resolution and its
proof land on the branch, so `beta` never sees an unproven intermediate tree and the later outbound
push that fires CI is a clean merge.

Purpose: CONTEXT constraints 2 and 6. The gate must run on the tree that actually gets merged, and
`submit.py` — one of the two conflicted files — is scanned by nine-row gate rows 9a/9b, so the `--ours`
resolution has to be re-proved against those gates (plan 122-04) before anything is pushed.

Output: two inbound merge commits, one per sub-repo. **Nothing is pushed. No release is cut.**

Critical constraint, C-12: **hunk-level resolution of `submit.py` is FORBIDDEN.** Its conflict hunks 3
and 4 sandwich a shared region that branch HEAD needs **twice** — once on the comment-degrade path and
once on the new-issue gh-degrade path. A textual "ours" on both hunks collapses two
`submit_via_browser(` call sites into one and leaves a dangling `elif url:` bound to the wrong `if`.
That result **compiles and the suite may still pass.** Whole-file `--ours` plus an empty diff is the
only accepted resolution.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repos: **`firestarter_app`** (sub-repo, `/workspaces/firestarter_app`) and **`firestarter`** (sub-repo,
`/workspaces/firestarter`). Both are on branch
`v1.22-at28c-software-data-protection-lifecycle`. Commits land **inside** each submodule. The meta repo
gets no gitlink bump (D-07).

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` +
`122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` + `122-GH12-COMMENT.md` (122-10).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** `firestarter_app` inbound merge with `firestarter/submit.py` and
`tests/test_submit.py` at zero diff vs branch HEAD and `firestarter/__init__.py` auto-merged to
`3.0.0b13` (**this plan**), then outbound merge + push (122-07); `firestarter` inbound merge with
`include/version.h` auto-merged to `3.0.0b13` (**this plan**), then outbound merge + push (122-07).
**Remote state:** two `beta` refs · two prerelease bodies via `gh release edit --notes-file` · one PyPI
release via `publish.yml` dispatch · two `gh issue comment` calls on `henols/firestarter_prom` #11/#12.
**Deliberately NOT produced:** no `v1.22` tag, no gitlink bump (D-07); no `PROTOCOL-LEDGER` edit (D-09);
no `check_ledger.py` run or fix (C-4); no workflow-trigger edit; no b12 deletion; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-probe, then inbound-merge origin/beta into the v1.22 branch in firestarter_app with whole-file --ours on exactly two files</name>
  <files>firestarter_app/firestarter/submit.py, firestarter_app/tests/test_submit.py, firestarter_app/firestarter/__init__.py</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` §"Pre-flight state" (written by 122-02) — the measured conflict set and superset proof this plan acts on, plus any recorded divergence.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Merge Ground Truth (D-06)" (lines 334-430) — the prescribed sequence, the superset proof table, and the C-12 hunk trap diagram.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md` §"`firestarter_app/firestarter/submit.py` + `tests/test_submit.py` (merge resolution)" (lines 437-479) — the warning signs and the negative-argv idiom to preserve.
    - `/workspaces/firestarter_app/firestarter/submit.py` — read the tail of `submit_report` at branch HEAD BEFORE merging, so the two `submit_via_browser(` call sites and the `elif url:` binding are in context as the ground truth the resolution must reproduce.
    - `/workspaces/firestarter_app/CLAUDE.md` — the sub-repo's tooling gate and conventions.
  </read_first>
  <action>
Work in `/workspaces/firestarter_app`. Confirm `git rev-parse --abbrev-ref HEAD` is
`v1.22-at28c-software-data-protection-lifecycle` before anything else.

Step 1 — re-probe immediately before mutating. `git fetch origin`, then
`git merge-tree --write-tree --messages HEAD origin/beta` filtered for lines starting `CONFLICT`. The
expected result is exactly two conflicts: `firestarter/submit.py` and `tests/test_submit.py`.
`122-RESEARCH.md` is stamped valid for only ~3 days precisely because `origin/beta` moves on any push,
so this re-probe is mandatory and is not a duplicate of 122-02's. **If the conflict set differs from
those two** — a third file, a different pair, or zero — STOP, do not merge, and record the observed set
in the SUMMARY as a blocking finding. A third conflicted file is outside this plan's proven resolution
strategy.

Step 2 — merge. `git merge --no-ff --no-edit origin/beta`. Pass `--no-edit` explicitly: without it git
opens an editor for the merge message in an interactive session and the command appears to hang.

Step 3 — resolve, whole-file only.
`git checkout --ours -- firestarter/submit.py tests/test_submit.py` followed by
`git add firestarter/submit.py tests/test_submit.py`. Do **not** open either file in an editor, do not
resolve any individual hunk, and do not delete conflict markers by hand. The whole-file form is the
mitigation for C-12.

Step 4 — prove the resolution before committing.
`git diff --cached HEAD -- firestarter/submit.py tests/test_submit.py` must produce **empty output**.
During a merge, `HEAD` is still the pre-merge branch tip, so an empty diff proves the staged content is
byte-identical to branch HEAD — exactly what the superset proof says is correct. If the output is
non-empty, the resolution is wrong: re-run the `--ours` checkout for those paths rather than editing the
diff away.

Step 5 — confirm `firestarter/__init__.py` auto-merged. It is **not** a conflict (C-2); after the merge
its `__version__` must read `3.0.0b13`. If it appears in the conflict list, that is a divergence from
C-2 and a blocking finding.

Step 6 — `git commit --no-edit` to complete the merge with git's default merge message.

Step 7 — post-commit proof.
`git diff HEAD^1 HEAD -- firestarter/submit.py tests/test_submit.py` must be **empty** (first parent is
the branch side). Then check both C-12 warning signs directly: `submit.py`'s line count must equal
branch HEAD's, and its `submit_via_browser(` call-site count must equal branch HEAD's. A merged
`submit_report` containing exactly one `submit_via_browser(` call, or a `submit.py` shorter than branch
HEAD's, is the C-12 failure — treat either as blocking.

Do **not** push. Do **not** check out or touch `beta`. Do not run `ruff format`, `ruff check --fix`, or
any other formatter — the resolution must be byte-identical to branch HEAD, and a formatter run would
destroy that proof. The four pre-existing `ruff check` findings and four `ruff format` drift files live
in `tools/` and `.github/scripts/`, are outside `ci.yml`'s `firestarter/ tests/` scope, and are
explicitly not this phase's work (C-8).
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` is still `v1.22-at28c-software-data-protection-lifecycle`.
    - `git -C /workspaces/firestarter_app log -1 --format=%P | wc -w` returns **2** — HEAD is a merge commit.
    - `git -C /workspaces/firestarter_app diff HEAD^1 HEAD -- firestarter/submit.py tests/test_submit.py` produces **empty** output.
    - `grep -c 'submit_via_browser(' /workspaces/firestarter_app/firestarter/submit.py` equals `git -C /workspaces/firestarter_app show HEAD^1:firestarter/submit.py | grep -c 'submit_via_browser('`.
    - `wc -l < /workspaces/firestarter_app/firestarter/submit.py` equals the output of `git -C /workspaces/firestarter_app show HEAD^1:firestarter/submit.py | wc -l`.
    - Neither `firestarter/submit.py` nor `tests/test_submit.py` contains a conflict marker line (`<<<<<<<`, `>>>>>>>`, or a bare `=======` line).
    - `grep -c '3.0.0b13' /workspaces/firestarter_app/firestarter/__init__.py` returns **1**.
    - `grep -c 'def test_' /workspaces/firestarter_app/tests/test_submit.py` equals the branch-HEAD count recorded in 122-02 (expected 77).
    - `git -C /workspaces/firestarter_app rev-parse origin/beta` is unchanged from 122-02's recorded value — nothing was pushed.
    - `git -C /workspaces/firestarter_app status --porcelain` shows only the pre-existing dirt (modified `.gitignore`; untracked `.coverage`, `.planning/config.json`, `SECURITY.md`, `write_test_port.sh`) and no unmerged paths.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(git log -1 --format=%P | wc -w)" = "2" && test -z "$(git diff HEAD^1 HEAD -- firestarter/submit.py tests/test_submit.py)" && test "$(grep -c 'submit_via_browser(' firestarter/submit.py)" = "$(git show HEAD^1:firestarter/submit.py | grep -c 'submit_via_browser(')" && grep -q '3.0.0b13' firestarter/__init__.py && ! grep -lE '^(<<<<<<<|>>>>>>>|=======)$' firestarter/submit.py tests/test_submit.py && echo APP-INBOUND-ZERO-DIFF-OK</automated>
  </verify>
  <done>The app inbound merge commit exists on the v1.22 branch; both conflicted files are byte-identical to branch HEAD (empty first-parent diff); `__init__.py` auto-merged to `3.0.0b13`; both C-12 warning signs are absent; nothing was pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-probe, then inbound-merge origin/beta into the v1.22 branch in firestarter — expected conflict-free</name>
  <files>firestarter/include/version.h</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Merge Ground Truth (D-06)" → "Firmware" (lines 336-347) — the zero-conflict `merge-tree` result and the single-file `version.h` delta.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` §"Pre-flight state" — the firmware numbers measured in 122-02.
    - `/workspaces/firestarter/include/version.h` — current content at branch HEAD (`3.0.0b11`).
    - `/workspaces/firestarter/CLAUDE.md` — the sub-repo's conventions.
  </read_first>
  <action>
Work in `/workspaces/firestarter`. Confirm the branch is
`v1.22-at28c-software-data-protection-lifecycle`.

`git fetch origin`, then re-run the dry-run probe
`git merge-tree --write-tree --messages HEAD origin/beta` filtered for lines starting `CONFLICT`.
Expect **no conflict output** (C-1). If any conflict appears, STOP and record it as a blocking finding
— the firmware inbound merge has no resolution strategy planned because it needs none.

Then `git merge --no-ff --no-edit origin/beta`. Afterwards confirm the only content change introduced
relative to branch HEAD is `include/version.h`: `git diff --name-only HEAD^1 HEAD` must output that one
path and nothing else, and the file must now read `3.0.0b13`. Also confirm the absorbed `beta`-side
commits are the two `Apply automatic changes` version bumps via `git log HEAD^1..HEAD --oneline`.

Do **not** edit `include/version.h` by hand and do not hand-set any version — the value must arrive by
auto-merge. Do **not** hand-edit `include/messages.h` under any circumstance: it is codegen-generated
and ID-only, and its canonical source is the meta repo's `tools/catalog/messages.toml`. If the merge had
touched `messages.h`, that is a divergence to record, not a file to repair. Do not push and do not check
out `beta`.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` is still `v1.22-at28c-software-data-protection-lifecycle`.
    - `git -C /workspaces/firestarter log -1 --format=%P | wc -w` returns **2**.
    - `git -C /workspaces/firestarter diff --name-only HEAD^1 HEAD` outputs exactly `include/version.h` — one line, nothing else.
    - `grep -c '3.0.0b13' /workspaces/firestarter/include/version.h` returns **1**.
    - `git -C /workspaces/firestarter diff --name-only HEAD^1 HEAD -- include/messages.h` is empty.
    - `git -C /workspaces/firestarter rev-parse origin/beta` is unchanged from 122-02's recorded value.
    - `git -C /workspaces/firestarter status --porcelain` outputs exactly `?? firestarter/` and no unmerged paths.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(git log -1 --format=%P | wc -w)" = "2" && test "$(git diff --name-only HEAD^1 HEAD)" = "include/version.h" && grep -q '3.0.0b13' include/version.h && test "$(git status --porcelain)" = "?? firestarter/" && echo FW-INBOUND-CLEAN-OK</automated>
  </verify>
  <done>The firmware inbound merge commit exists on the v1.22 branch, was conflict-free, changed only `include/version.h` to `3.0.0b13`, left `messages.h` untouched, and pushed nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Record both merge commits and re-assert the catalog three-way identity across the merged trees</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-03-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Full-suite baselines at branch HEAD (pre-merge)" (lines 620-637) — the catalog validity command, the three-way `cmp` identity, and the pre-merge baselines this merge must not have disturbed.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Common Pitfalls" → Pitfall 7 (lines 940-946) — why `catalog-sync-check.yml` is red-until-`main`-merge **by design** and must not be chased.
    - `/workspaces/tools/catalog/messages.toml` — the meta-repo canonical catalog source, and the two generated mirrors it must still match.
  </read_first>
  <action>
No file in either sub-repo is modified by this task. Produce the evidence that the two merges left the
cross-repo generated artifacts consistent, and record the commit identities the rest of the phase
depends on.

1. Record both merge commit SHAs (`git -C <repo> rev-parse HEAD` in each sub-repo) and both first-parent
   SHAs. These are the "merged tree" identities plans 122-04 and 122-07 must cite; the `122-LEDGER.md`
   header pins them (per the `PROTOCOL-LEDGER.md` header-provenance pattern, which pins **commits**, not
   version strings).
2. Re-run the catalog validity gate from the meta repo:
   `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` — expect
   `OK: catalog valid`.
3. Re-run the three-way catalog identity with `cmp`: the meta canonical catalog against the firmware
   mirror and against the app mirror. Both must exit 0. This is the in-phase proof that
   `catalog-sync-check.yml`'s red state is the known, by-design `ref: main` artifact and not real drift
   — do not chase that workflow, and do not "fix" it.
4. Assert both sub-repo working trees are clean modulo the named pre-existing dirt, using
   `git status --porcelain` (the whole-tree form). Do **not** use a path-scoped `git diff` as the
   cleanliness proof: PROJECT.md's SEVENTH CORRECTION item 9 records that a path-scoped diff can pass
   vacuously, and `status --porcelain` subsumes every path.
5. Assert the meta repo's gitlinks are still `0048b3d…` / `96e0622…` via
   `git -C /workspaces ls-tree HEAD firestarter firestarter_app`. The inbound merges advanced both
   working tips; the committed gitlinks must **not** follow (D-07, A5). Do not stage a submodule pointer.
6. Assert `origin/beta` is unchanged in both repos.
  </action>
  <acceptance_criteria>
    - `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` (from `/workspaces`) exits 0 and prints `OK: catalog valid`.
    - Both `cmp` invocations comparing the meta catalog against the firmware mirror and the app mirror exit 0.
    - `git -C /workspaces ls-tree HEAD firestarter firestarter_app` still reports `0048b3d9a3b9aaec5e7e3030f9313acce8e6411a` and `96e062261b8a5e8c29fe3eb6d888468cf876a6cf`.
    - `git -C /workspaces status --porcelain` shows no staged submodule gitlink change.
    - `git -C /workspaces/firestarter status --porcelain` is exactly `?? firestarter/`; `git -C /workspaces/firestarter_app status --porcelain` matches the named pre-existing dirt exactly.
    - The SUMMARY records both merge SHAs, both first-parent SHAs, the four version-string values after merge, and the statement that `catalog-sync-check.yml` staying red is expected-by-design until the `main` merge owned by `/gsd-complete-milestone`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check && git ls-tree HEAD firestarter firestarter_app | grep -q 0048b3d && git ls-tree HEAD firestarter firestarter_app | grep -q 96e0622 && test -z "$(git status --porcelain -- firestarter firestarter_app)" && echo POST-MERGE-IDENTITY-OK</automated>
  </verify>
  <done>Both merge identities are recorded; the catalog is valid and three-way identical; the meta gitlinks are provably still pinned one phase behind (D-07); both sub-repo trees are clean modulo the named pre-existing dirt; `origin/beta` is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `origin/beta` history → the v1.22 branch tree | Foreign commits (the five double-applied `quick-260728-ahy` hotfixes with different SHAs) enter a tree that later ships to every `pip install --pre` user |
| merge resolution → published artifact | A resolution that compiles and passes is not a resolution that is correct (C-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-05 | Tampering | `firestarter_app/firestarter/submit.py` resolution | critical | mitigate | Whole-file `git checkout --ours`; `git diff --cached HEAD` empty pre-commit and `git diff HEAD^1 HEAD` empty post-commit; both C-12 warning signs (line count, `submit_via_browser(` call-site count) checked against branch HEAD; hunk-level resolution explicitly forbidden |
| T-122-03 | Tampering | publishing from an unproven tree | high | mitigate | This plan performs the **inbound** merge only. Nothing is pushed; the nine-row gate + both full suites + CLOSE-01's four mechanisms run on this merged tree in 122-04, before the outbound push in 122-07 (constraints 2 and 6) |
| T-122-12 | Tampering | `firestarter/include/messages.h` | medium | mitigate | Codegen-generated and ID-only; asserted untouched by the merge; hand-editing forbidden. Canonical source is the meta `tools/catalog/messages.toml`, re-validated plus three-way `cmp` in Task 3 |
| T-122-13 | Tampering | meta-repo submodule gitlinks | medium | mitigate | D-07/A5: gitlinks asserted still `0048b3d` / `96e0622`; no submodule pointer staged |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit); `pyproject.toml`'s dependency set introduces nothing new in this phase. An install proposal is a scope escape |
</threat_model>

<verification>
1. Both sub-repos have a merge commit at HEAD on the v1.22 branch (two parents each).
2. App: `git diff HEAD^1 HEAD -- firestarter/submit.py tests/test_submit.py` is empty; no conflict
   markers; `__init__.py` reads `3.0.0b13`; `test_submit.py` still has 77 `def test_` functions.
3. Firmware: `git diff --name-only HEAD^1 HEAD` is exactly `include/version.h`, now `3.0.0b13`.
4. Catalog valid + three-way `cmp` identity both green from the meta repo.
5. `git -C /workspaces ls-tree HEAD firestarter firestarter_app` unchanged (`0048b3d` / `96e0622`).
6. `origin/beta` unchanged in both repos — **nothing pushed by this plan.**

**Known pre-existing dirt — not this plan's damage:** `firestarter`: untracked `firestarter/`.
`firestarter_app`: modified `.gitignore`, untracked `.coverage`, `.planning/config.json`, `SECURITY.md`,
`write_test_port.sh`. Meta: modified `.planning/config.json`.

**Do NOT run** `.planning/v1.16/ledger/tools/check_ledger.py` at any point — it is pre-existing RED
(C-4: two `LEDGER-01` violations from v1.19 Phase 104's `flash3`/`flash4` rename) and is not a gate for
anything in this phase.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-03 closes only in 122-13.
</verification>

<success_criteria>
- Two inbound merge commits exist, one per sub-repo, both on the v1.22 branch.
- The app's two-file conflict is resolved to a provable zero diff against branch HEAD, with both C-12
  warning signs checked and absent.
- The firmware merge is confirmed conflict-free with a single-file `version.h` delta.
- Catalog validity and three-way identity are green; the meta gitlinks are still pinned.
- `origin/beta` is unchanged in both repos.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-03-SUMMARY.md`
when done. Record: both merge SHAs and first-parent SHAs, the observed conflict sets from both re-probes,
the empty-diff proof output, both C-12 warning-sign counts, the four post-merge version strings, and the
gitlink assertion.
</output>
