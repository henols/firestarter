---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 06
type: execute
wave: 5
depends_on: ["122-05"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [CLOSE-01]
must_haves:
  truths:
    - "`PROJECT.md` carries an EIGHTH CORRECTION block recording that the milestone's headline premise is now silicon-confirmed by a community report while the fix remains unproven (D-10)"
    - "The correction states the community datapoint's provenance honestly — an issue-comment paste, no captured logs beyond the text, board revision and firmware build unconfirmed"
    - "The correction records that `0x0D` stays `UNVERIFIED`, zero chips changed `support_status`, and the 84-chip count is unchanged — the asymmetry is premise-confirmed / fix-unproven"
    - "The ordinal is EIGHTH because the live file's sequence ends at SEVENTH (C-10 corrects CONTEXT's off-by-one count)"
    - "The edit is purely additive: `git diff --numstat` on `PROJECT.md` shows zero deleted lines, so no pre-existing correction block was disturbed"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "EIGHTH CORRECTION block — the project-level evidence record for the v1.22 close"
      contains: "EIGHTH CORRECTION"
  key_links:
    - from: ".planning/PROJECT.md EIGHTH CORRECTION"
      to: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md"
      via: "the correction cross-references the ledger's claim classes rather than restating their evidence; the ledger is the single source of the permitted wording"
      pattern: "122-LEDGER"
---

<objective>
Append an EIGHTH CORRECTION block to `.planning/PROJECT.md` §"Current Milestone: v1.22", recording the
asymmetric new fact this close discovered — the milestone's headline **defect** is now confirmed on real
AT28C256 silicon by a community report, while the **fix** remains unproven — together with the
mechanism corrections this phase established.

Purpose: D-10. The community datapoint is a claim class the validation ceiling called unprovable, and it
confirms the defect rather than the fix. That is a genuinely new and asymmetric fact, in the same
register as the seven correction blocks before it, and it belongs in the project-level evidence record —
not only in a reply to a stranger.

Output: one scoped, purely additive edit to `.planning/PROJECT.md`. The ledger written in 122-05 already
holds the evidence; this block cross-references it rather than duplicating it (constraint 5 — the ledger
is the single source, and it exists first).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. No sub-repo file is touched.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` +
`122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` + `122-GH12-COMMENT.md` (122-10).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (**this plan**) ·
`.planning/REQUIREMENTS.md` ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03), outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs · two prerelease bodies · one PyPI release · two issue comments.
**Deliberately NOT produced:** no `v1.22` tag, no gitlink bump (D-07); no `PROTOCOL-LEDGER` edit and no
`check_ledger.py` run or fix (D-09, C-4); no `REQUIREMENTS.md` wording edit; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the EIGHTH CORRECTION block and insert it immediately after the SEVENTH</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` lines 55-120 — the full ordinal sequence. Read the **SEVENTH CORRECTION** block (lines 101-111) as the exact format analog: a bold `⚠` opener carrying ordinal + phase + date + a one-sentence thesis, then a numbered item list where every item opens with its own bold claim sentence. Note precisely where the SEVENTH block ends and the plain bullet list beginning `- **No AT28C part in operator inventory**` starts — the new block is inserted between them.
    - `.planning/PROJECT.md` line 85 (FIFTH CORRECTION item 3, `⚠ FINDING F-118-01`) — the precedent for a *finding inside a correction*, including the "this corrects the decision's premise, not the code" move. D-10's asymmetric silicon datapoint should take that shape.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md` (122-05) — the claim classes and figures this block cross-references rather than restates.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Community Thread Ground Truth" → gh#11 (lines 647-675) — the verbatim 2024 symptom, the input file being 32 Kb of random data, and the three live comments 10-12.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Corrections to CONTEXT.md / ROADMAP Framings" (lines 266-294) — C-1 through C-13, the source of items 3 through 8 below.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §D-10 — the decision's own framing and its two rejected alternatives.
  </read_first>
  <action>
Use a **scoped `Edit`**, never `Write`, on `.planning/PROJECT.md`. A whole-file write would destroy the
six preceding correction blocks and every archive section. Anchor the edit on the last line of the
SEVENTH CORRECTION's item 9 and insert the new block immediately after it, leaving a blank line before
the `- **No AT28C part in operator inventory**` bullet that follows.

The ordinal is **EIGHTH**. The live file's ordinal sequence is `SECOND REFRAMING`, `THIRD`, `FOURTH`,
`FIFTH`, `SIXTH`, `SEVENTH` — six ⚠ blocks, so the next ordinal is EIGHTH. CONTEXT.md's
`<canonical_refs>` says "all seven"; that count is off by one (C-10) and the ordinal is unaffected.

Opener, matching the SEVENTH's shape: a bold `⚠ EIGHTH CORRECTION — Phase 122 close (<date>):` followed
by a one-sentence thesis naming the asymmetry — the milestone's headline **defect** is now confirmed on
real AT28C256 silicon by a community report while the **fix** remains unproven — then the standard
lead-in sentence naming the phase, its plan count, and the requirements it closed.

Numbered items, each opening with a bold claim sentence:

1. **The headline premise is silicon-confirmed by a stranger; the fix is not (D-10).** On 2026-07-27 the
   author of gh#11 re-ran `firestarter write at28c256` on `3.0.0b11` against a real AT28C256 and pasted
   the exact inverted-check INIT abort — `ERROR: EEPROM timeout at 0x005555: wrote 0x20 got 0xff` — that
   Phase 116 could only predict in software. This raises TRACE-06 from software-predicted to
   **community-corroborated**. State the provenance honestly and in these terms: an issue-comment paste,
   no captured logs beyond the text, board revision and firmware build unconfirmed. State the asymmetry
   explicitly: the **defect** now has silicon evidence; the **fix** does not. `0x0D` stays `UNVERIFIED`
   in `PROTOCOL-LEDGER`, **zero** chips changed `support_status`, and the 84-chip `algorithm == 13`
   count is unchanged — all three re-verified on the merged tree (`122-NONREGRESSION.md`).
2. **Their re-test looked like a regression and was not — say so, because the reporter will not assume
   it.** In 2024 the write *completed*, reporting success after 339 s, and silently burned only part of
   the image. On `3.0.0b11` it hard-fails at INIT. That is the fix landing halfway: the inverted success
   check turned a silent partial write into an honest refusal. Anyone reading b11 as *worse* than 2024
   is reading it reasonably and wrongly.
3. **A locked decision was corrected before it reached a stranger (C-5).** D-14 prescribed telling
   another gh#12 participant that their AT28C parts should now work. Measured over all 84 `0x0D` entries
   with the production predicate, every 2K×8 part sits on pinout `DIP24_2816` and **all 19 of 19** are
   REFUSED by the SDP allow-set — 7 as pre-SDP generation, 12 as unrecognised — and the shipped
   `dev sdp --help` text says so itself. The reply is therefore phrased **by size class, not by an
   assumed part number**, and states the refusal plainly. Record this as a divergence from a locked
   decision, accepted by the operator at the D-16 wording review, not as a silent rewrite.
   *Emission traced byte-exact for a pinout* and *the operation permitted on parts with that pinout* are
   different claims; `122-LEDGER.md` carries the qualifier.
4. **CLOSE-01's implied `check_ledger.py` is pre-existing RED, was not gated on, and was not fixed
   (C-4).** It exits 1 with two `LEDGER-01` violations because rows `0x05`/`0x06` carry `matrix_family`
   `flash4`/`flash3`, which v1.19 Phase 104 renamed to `5v_page`/`nor_unlock`.
   `tools/validation_matrix_spec.json` is absent from the `beta...HEAD` diff, so this is not v1.22's
   damage. CLOSE-01's text does not mention the tool. Fixing it would edit a closed milestone's artifact
   — exactly what D-09 refuses. Recommend a backlog seed.
5. **D-06's conflict set was wrong, and the correct resolution is a structural proof rather than a green
   suite (C-1/C-2/C-11/C-12).** The firmware inbound merge had **zero** conflicts; the app conflicted in
   exactly two files, `firestarter/submit.py` and `tests/test_submit.py`. `include/version.h` and
   `firestarter/__init__.py` both auto-merged and were never conflicts. Resolution was whole-file
   `--ours`, justified by a mechanical superset proof (all 60 of `beta`'s `test_submit.py` functions
   exist among branch HEAD's 77) and proven by an **empty diff**. Hunk-level resolution was forbidden:
   hunks 3 and 4 sandwich a shared region branch HEAD needs twice, and a textual "ours" there produces
   code that **compiles and passes while being wrong**.
6. **The manual `publish.yml` dispatch is the norm, not a contingency (C-3).** Six of thirteen published
   app GitHub betas never reached PyPI — b4, b5, b6, b9, b10, b12 — a 46% historical miss rate, caused
   by `beta-release.yml` creating the release with a PAT that lacks `workflow` scope, which suppresses
   the `release.published` event. `publish.yml`'s own in-file comment documents the mechanism. *"CI is
   green"* is not evidence a channel is live, which is why D-03 gated the community comments on a
   resolution check.
7. **The published cut tag is read, never assumed (A3), and `ci.yml` never runs on a `beta` push
   (C-8).** CI producing the next beta number is derived from `update_version.py`'s git-tag scan, not
   executed, so every downstream step consumes the observed tag. And the merge push fires only
   `beta-build.yml` / `beta-release.yml`; `ci.yml` and firmware `build.yml` trigger on `main` and
   pull requests only. A green beta workflow is therefore a **narrower** statement than "CI is green":
   no ruff, no mypy, no coverage floor, no vector-catalog gate, no CLI smoke test ran. The four
   pre-existing `ruff check` findings and four `ruff format` drift files all sit in `tools/` +
   `.github/scripts/`, outside `ci.yml`'s `firestarter/ tests/` scope — structurally invisible, and
   deliberately not fixed here.
8. **One owned trade-off, chosen with its cost named (D-01).** No bench smoke-test of the published
   beta's install/flash path was run, so the `pip install --pre` → `firestarter fw -i` → one-live-op
   chain that Phase 115 existed to prove is **trusted, not re-verified**, before two community members
   are pointed at it. Recorded so no downstream agent quietly re-opens it, and so that if an install
   problem surfaces the record shows a known, accepted gap rather than an oversight.
9. **The class lesson: this milestone's closing criterion is only half mechanizable.** ROADMAP criterion
   4 — every claim in the closing documentation matches the validation ceiling — splits three ways. The
   *mechanizable* half is a forbidden-phrase / required-caveat gate over the closing artifacts, shipped
   with two committed planted-violation fixtures under the GATE-01 anti-hollow discipline. The
   *judgement* half is the D-16 blocking operator wording review, because a string scan cannot detect an
   implied overclaim, a misleading omission, or wrong tone. And one whole class — that SDP works on real
   AT28C silicon — has a sampling rate of **zero, permanently, by design**. A green claim-scan must
   never be reported as satisfying criterion 4.

Cross-reference `122-LEDGER.md`, `122-NONREGRESSION.md`, `122-DECISION.md` and `122-CHANNELS.md` by name
rather than restating their figures. Keep every figure measured: cite **66 of 84** and never "all 84";
keep the flash budget (2600 B free) and the timing budget (572 µs against 600 µs) as separate mentions,
because a prior PROJECT.md directive conflated them and `119-MEASUREMENT.md` records the correction.

Do not touch any other part of `PROJECT.md`. Do not renumber, reword, or reflow any existing correction
block. Do not edit the v1.21 or earlier archive sections.
  </action>
  <acceptance_criteria>
    - `grep -c '⚠ EIGHTH CORRECTION' .planning/PROJECT.md` returns exactly **1**.
    - `grep -c '⚠ SEVENTH CORRECTION' .planning/PROJECT.md` still returns exactly **1**, and the same holds for SECOND REFRAMING, THIRD, FOURTH, FIFTH and SIXTH — six pre-existing blocks, unchanged.
    - `git -C /workspaces diff --numstat -- .planning/PROJECT.md` reports **0 deleted lines** — the edit is purely additive.
    - The new block appears **after** the SEVENTH CORRECTION's item 9 and **before** the `- **No AT28C part in operator inventory**` bullet: `grep -n 'EIGHTH CORRECTION\|No AT28C part in operator inventory\|SEVENTH CORRECTION' .planning/PROJECT.md` shows the three line numbers in that order.
    - The block contains **9** numbered items, each opening with a bold claim sentence.
    - Item 1 contains the verbatim error string `ERROR: EEPROM timeout at 0x005555: wrote 0x20 got 0xff`, the words `UNVERIFIED`, `support_status`, and an explicit defect-confirmed / fix-unproven statement.
    - Item 3 names the measured 19-of-19 `DIP24_2816` refusal and states the reply is phrased by size class, and labels the item a divergence from a locked decision.
    - `grep -c 'all 84' .planning/PROJECT.md` — the count is unchanged from before this edit (the phrase is not newly introduced; the existing THIRD CORRECTION already discusses it).
    - The block cross-references `122-LEDGER.md` and `122-NONREGRESSION.md` by name.
    - `git -C /workspaces status --porcelain -- .planning/REQUIREMENTS.md .planning/v1.16/ledger/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -c '⚠ EIGHTH CORRECTION' .planning/PROJECT.md)" = "1" && test "$(grep -c '⚠ SEVENTH CORRECTION' .planning/PROJECT.md)" = "1" && test "$(grep -c '⚠ SIXTH CORRECTION' .planning/PROJECT.md)" = "1" && test "$(git diff --numstat -- .planning/PROJECT.md | awk '{print $2}')" = "0" && grep -q 'EEPROM timeout at 0x005555' .planning/PROJECT.md && grep -q '122-LEDGER' .planning/PROJECT.md && test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md .planning/v1.16/ledger/)" && echo EIGHTH-CORRECTION-OK</automated>
  </verify>
  <done>The EIGHTH CORRECTION block exists with nine items in the SEVENTH's format, inserted between the SEVENTH block and the following bullet list, purely additive (zero deleted lines), cross-referencing the ledger and the non-regression artifact, with the community datapoint's provenance and the defect/fix asymmetry stated explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the edit's structural integrity and gate the new block against the claim scanner</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` — the file as edited in Task 1, read to confirm the ordinal sequence and the insertion boundaries by eye as well as by grep.
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/check_note_append_only.py` — the meta-repo precedent for proving an edit was append-only by comparing the working tree against `git show HEAD:<path>`. Adapt its *idea* (a mechanical structural assertion, not a narrative claim); do not create a new committed script for it.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (122-01) — the forbidden pattern labels and the canonical required-caveat fragment.
  </read_first>
  <action>
Two mechanical proofs, then one gate. No new script is committed by this task — these are one-off
assertions whose outputs go into the SUMMARY.

Proof 1 — **purely additive.** Compare the committed `HEAD` version of `.planning/PROJECT.md` against
the working tree and assert that every line present at `HEAD` is still present, in order, with the new
block inserted as a contiguous run. The simple mechanical form: `git diff --numstat -- .planning/PROJECT.md`
must report zero deletions, and `git diff -- .planning/PROJECT.md` must contain no line beginning with a
single `-` other than the `--- a/` header. This is the analog of `check_note_append_only.py`'s
prefix-assertion, adapted to a mid-file insertion.

Proof 2 — **ordinal integrity.** Assert exactly one occurrence of each of the seven ordinal ⚠ markers
(SECOND REFRAMING, THIRD, FOURTH, FIFTH, SIXTH, SEVENTH, EIGHTH), and assert the seven line numbers are
strictly increasing. A duplicated or out-of-order ordinal means the insertion landed in the wrong place
— for example inside the v1.21 archive rather than in §"Current Milestone: v1.22".

Then the gate: run the Wave 0 claim scanner over `.planning/PROJECT.md` via the env seam and require
exit 0. `PROJECT.md` is **not** one of the scanner's five default targets — it is an internal evidence
record, not outward-facing prose — so this is a courtesy check on the new prose, not a contract. Note in
the SUMMARY that the scanner's required-caveat leg will only pass if `PROJECT.md` contains the canonical
caveat fragment; if the new block does not naturally carry it, run the scanner against a temporary
extract of the new block alone rather than weakening the gate or adding a caveat sentence to
`PROJECT.md` purely to satisfy a courtesy check. Record which of the two forms was used.

Finally, record in the SUMMARY that this plan closes **no** requirement. CLOSE-01's checkbox is ticked
only in 122-13, and only after the whole phase's evidence exists. Four executors in Phase 116 ticked a
multi-plan requirement early and one Phase 121 plan needed a revert for it; re-read CLOSE-01's **prose**
in `REQUIREMENTS.md` (not a plan's summary of it) before any tick, and do not tick it here.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces diff -- .planning/PROJECT.md | grep -c '^-[^-]'` returns **0** — no existing line was removed or altered.
    - Each of the seven ordinal markers appears exactly once, and `grep -n` reports their line numbers in strictly increasing order SECOND < THIRD < FOURTH < FIFTH < SIXTH < SEVENTH < EIGHTH.
    - The EIGHTH block's line number is greater than the SEVENTH's and less than the line number of the `## v1.21 Archive` heading — proving it landed inside §"Current Milestone: v1.22".
    - The claim scanner exits 0 against either `.planning/PROJECT.md` or a temporary extract of the new block, and the SUMMARY records which form was used and why.
    - The SUMMARY states explicitly that no requirement checkbox was ticked by this plan.
    - `git -C /workspaces status --porcelain -- .planning/REQUIREMENTS.md` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(git diff -- .planning/PROJECT.md | grep -c '^-[^-]')" = "0" && for o in 'SECOND REFRAMING' 'THIRD CORRECTION' 'FOURTH CORRECTION' 'FIFTH CORRECTION' 'SIXTH CORRECTION' 'SEVENTH CORRECTION' 'EIGHTH CORRECTION'; do test "$(grep -c "⚠ $o" .planning/PROJECT.md)" = "1" || { echo "ORDINAL FAIL: $o"; exit 1; }; done && test "$(grep -n '⚠ EIGHTH CORRECTION' .planning/PROJECT.md | cut -d: -f1)" -lt "$(grep -n '^## v1.21 Archive' .planning/PROJECT.md | cut -d: -f1)" && test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)" && echo PROJECT-STRUCTURE-OK</automated>
  </verify>
  <done>The edit is proven purely additive with zero altered pre-existing lines; all seven ordinals appear exactly once in strictly increasing order; the new block is inside the current-milestone section; the claim scanner is green on the new prose; no requirement was ticked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a third party's issue-comment paste → the project's evidence record | Unverifiable-by-us silicon evidence enters a document later phases treat as ground truth; provenance must travel with it |
| a scoped edit → a 140-line multi-milestone document | A whole-file write here would silently destroy six correction blocks and every archive section |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-18 | Tampering | `.planning/PROJECT.md` (six pre-existing correction blocks + archives) | high | mitigate | Scoped `Edit` only, never `Write`; `git diff --numstat` asserted zero deletions; `grep -c '^-[^-]'` on the diff asserted 0; all seven ordinals asserted present exactly once in increasing order |
| T-122-19 | Spoofing | community-reported silicon evidence treated as first-party measurement | high | mitigate | Provenance stated in-block: issue-comment paste, no captured logs beyond the text, board revision and firmware build unconfirmed; the datapoint is labelled `COMMUNITY-CORROBORATED` in the ledger, distinct from `PERMITTED` |
| T-122-17 | Repudiation | a locked-decision divergence applied silently | high | mitigate | Item 3 records the C-5/D-14 divergence in the project's own evidence record, naming both what D-14 said and what was measured, so the D-16 operator review can accept or overturn it |
| T-122-16 | Tampering | citing a predicted or conflated figure | medium | mitigate | 66 of 84 cited, "all 84" not newly introduced; flash (2600 B) and timing (572 µs / 600 µs) kept as separate mentions per `119-MEASUREMENT.md`'s recorded correction |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. Exactly one `⚠ EIGHTH CORRECTION` block, with nine numbered items in the SEVENTH's format.
2. `git diff --numstat -- .planning/PROJECT.md` reports zero deletions; the diff contains no removed
   line.
3. All seven ordinals appear exactly once, in strictly increasing line order, and EIGHTH precedes the
   `## v1.21 Archive` heading.
4. Item 1 carries the verbatim community error string, the honest provenance, and the defect-confirmed /
   fix-unproven asymmetry, plus the three CLOSE-01 facts.
5. Item 3 carries the flagged C-5/D-14 locked-decision divergence.
6. The claim scanner is green on the new prose (form recorded).
7. `REQUIREMENTS.md` and `.planning/v1.16/ledger/` untouched; both sub-repo trees show only the named
   pre-existing dirt; nothing pushed.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-01 closes only in 122-13, after re-reading
its prose in `REQUIREMENTS.md`.
</verification>

<success_criteria>
- The project-level evidence record carries the asymmetric community datapoint with honest provenance,
  and the five mechanism corrections that outlive this phase.
- The edit is provably additive and structurally sound.
- The C-5/D-14 divergence is visible at project level, not only inside a draft.
- No requirement checkbox moved.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-06-SUMMARY.md`
when done. Record the diff numstat, the seven ordinal line numbers, the scanner form used, and the
explicit no-requirement-ticked statement.
</output>
