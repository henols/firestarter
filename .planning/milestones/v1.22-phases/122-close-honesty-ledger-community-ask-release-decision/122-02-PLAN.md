---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md
autonomous: true
requirements: [CLOSE-03]
must_haves:
  truths:
    - "A recorded accept/avoid/cleanup decision for the `beta` push exists in the phase record and is committed BEFORE any push to `beta` happens (CLOSE-03's literal text; D-05 is the recorded choice)"
    - "The decision names what was accepted, what was declined, and why — including that the stray b12 prereleases stay public and that no workflow trigger is edited"
    - "The pre-flight state the push decision rests on is captured as evidence in the same artifact: both branch tips, both `beta` version strings, the live dry-run merge conflict set, and the `--ours` superset proof"
    - "The `v1.22` tag and the meta gitlink bump are explicitly recorded as OUT of this phase's scope, owned by `/gsd-complete-milestone` (D-07)"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md"
      provides: "CLOSE-03's recorded accept/avoid/cleanup decision plus the pre-flight evidence it rests on"
      contains: "ACCEPT"
      min_lines: 60
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md"
      to: "the pushed outbound merge commits (plan 122-07)"
      via: "this artifact's meta-repo commit must be strictly earlier than either sub-repo push; `git log --date=iso` timestamps order the two events"
      pattern: "ACCEPT|auto-fire"
---

<objective>
Record CLOSE-03's accept/avoid/cleanup decision for the `beta` push, and capture — in the same
artifact — the live pre-flight evidence that decision rests on. Nothing is pushed, merged, or
published by this plan; it produces one committed markdown artifact in the meta repo.

Purpose: CLOSE-03's text is literal — the decision must be *made and recorded before any push*.
v1.21's close skipped this step and auto-cut a stray `3.0.0b12`. Committing the decision in Wave 1,
before the inbound merge and long before the outbound push, makes the ordering provable by commit
timestamp rather than by narrative.

Output: `.planning/phases/122-…/122-DECISION.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only, branch `gsd/v1.22-at28c-software-data-protection-lifecycle`. This
plan runs read-only `git`/`gh` commands against both sub-repos but writes no file in either.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `.planning/phases/122-…/check_permitted_claims.py`,
`test_check_permitted_claims.py`, `fixtures/{clean_control,clean_control_second,planted_forbidden_claim,planted_missing_caveat}.md`
(122-01) · `122-DECISION.md` (**this plan**) · `122-NONREGRESSION.md` (122-04) · `122-LEDGER.md`
(122-05) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md`
(122-09) · `122-GH11-COMMENT.md` + `122-GH12-COMMENT.md` (122-10).

**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) ·
`.planning/REQUIREMENTS.md` CLOSE-01/02/03 checkboxes + 3 traceability rows (122-13 only).

**Sub-repo git state (no source authored):** `firestarter_app` inbound merge with
`firestarter/submit.py` + `tests/test_submit.py` resolved whole-file `--ours` to a ZERO diff vs branch
HEAD, then outbound merge to `beta` + push (122-03, 122-07); `firestarter` inbound merge
(`include/version.h` auto-merged b11→b13), then outbound merge to `beta` + push (122-03, 122-07).

**Remote state:** two `beta` refs moved · two GitHub prereleases created by CI then body-edited via
`gh release edit --notes-file` · one PyPI release via `publish.yml` manual dispatch · two
`gh issue comment` calls on `henols/firestarter_prom` #11 and #12.

**Deliberately NOT produced:** no `v1.22` tag, no meta gitlink bump (D-07); no edit to
`.planning/v1.16/ledger/PROTOCOL-LEDGER.{md,json}` or `check_ledger.py` (D-09, C-4); no stable
release; no workflow-trigger edit; no deletion of the stray b12 (D-05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the live pre-flight state — branch tips, beta version strings, dry-run merge conflict set, superset proof</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Merge Ground Truth (D-06)" (lines 334-430) — the recorded conflict set, the superset proof, the C-12 trap, and the prescribed sequence.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Corrections to CONTEXT.md / ROADMAP Framings" (lines 266-294) — rows C-1, C-2, C-9, C-11, C-12 in particular.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Code Examples" (lines 1031-1050) — the dry-run merge probe and the superset-proof commands, verbatim.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §"Setup precondition" (the last block of `<code_context>`) — the branch-base trap that has bitten this project twice.
  </read_first>
  <action>
Run only non-mutating commands, then write the evidence into a new `122-DECISION.md` under a
`## Pre-flight state (measured <date>)` heading. **`122-RESEARCH.md` is stamped valid until
~2026-08-02 precisely because `origin/beta` moves on any push — re-measure, do not copy its numbers.**

In `/workspaces/firestarter` and `/workspaces/firestarter_app`, both explicitly by name (never by
iterating `planning.sub_repos`, which lists four repos, two of them v1.29 PY32 scratch repos outside
this milestone):
1. `git fetch origin` (fetch only — no merge, no checkout, no push).
2. `git rev-parse --abbrev-ref HEAD` and `git rev-parse HEAD` — expect
   `v1.22-at28c-software-data-protection-lifecycle` at `48c36e5…` (firmware) / `c3c9424…` (app).
3. `git rev-list --left-right --count HEAD...origin/beta` — expect 42/2 (firmware) and 75/7 (app).
4. `git merge-base HEAD origin/beta` and `git rev-parse v1.21^{commit}` — record that they coincide
   (C-9: v1.22 forked off `beta`; the fork-off-the-previous-version exception applied to v1.15 and
   v1.21, **not** v1.22).
5. The version string on **both sides**: firmware `git show origin/beta:include/version.h` vs the
   working tree's, and app `git show origin/beta:firestarter/__init__.py` vs the working tree's.
   Expect `3.0.0b13` on `beta` and `3.0.0b11` on the branch in both repos.
6. `git tag --list '3.0.0b*'` in both repos — record the highest tag and record that `3.0.0b14` is
   absent from both (this is what makes the auto-increment's target derivable; A3 still applies —
   the *observed* cut tag is read later, in 122-07, never assumed here).
7. The **dry-run merge probe**, `git merge-tree --write-tree --messages HEAD origin/beta` in each
   repo, filtered for `^CONFLICT`. Record the conflict set verbatim. Expect **zero** conflicts in
   `firestarter` (C-1) and exactly two in `firestarter_app` — `firestarter/submit.py` and
   `tests/test_submit.py` (C-2). `include/version.h` and `firestarter/__init__.py` both auto-merge and
   are **not** conflicts; a plan or SUMMARY naming `version.h` as a conflict is wrong.
8. The **superset proof** for the `--ours` resolution: the `comm -23` pipeline from RESEARCH §"Code
   Examples" comparing `origin/beta:tests/test_submit.py`'s `def test_*` names against
   `HEAD:tests/test_submit.py`'s. Record the exact output (expected **empty**) and both counts
   (expected 60 on `beta`, 77 on HEAD). Also spot-confirm the five beta hotfix behaviours are present
   on HEAD by grepping `firestarter/submit.py` for `henols/firestarter_prom`, `getattr(proc, "stderr"`,
   `degrading to`, and `Report filed`.
9. The A5 baseline: `git -C /workspaces ls-tree HEAD firestarter firestarter_app` — record
   `0048b3d…` / `96e0622…` as the values that must be **unchanged** at phase end (D-07).
10. The working-tree dirt in all three repos via `git status --porcelain`, so a later cleanliness
    assertion cannot read pre-existing dirt as its own damage.

If any measurement disagrees with RESEARCH's recorded value, **do not proceed to write the decision as
if it agreed**: record the divergence under a `### ⚠ Divergence from 122-RESEARCH.md` subheading with
both values, and state plainly whether it changes the resolution strategy. A newly-appeared third
conflicted file, or a `beta` that has moved past `3.0.0b13`, is exactly the case RESEARCH's 3-day
validity window exists to catch.
  </action>
  <acceptance_criteria>
    - `122-DECISION.md` §"Pre-flight state" records, as measured values with the command that produced each: both branch names + tip SHAs; both ahead/behind pairs; the merge-base and its coincidence with `v1.21^{commit}`; four version strings; the highest `3.0.0b*` tag in each repo and the absence of `3.0.0b14`; the per-repo `merge-tree` conflict set; the `comm -23` output and the 60/77 counts; the two gitlink SHAs; the `git status --porcelain` output for all three repos.
    - The artifact states explicitly that `include/version.h` and `firestarter/__init__.py` are **auto-merged, not conflicted** (C-1/C-2).
    - `grep -c 'merge-tree' 122-DECISION.md` ≥ 1 and `grep -c 'comm -23' 122-DECISION.md` ≥ 1 — the probe and the superset proof are both named.
    - No mutating git command was run: `git -C /workspaces/firestarter rev-parse HEAD` and `git -C /workspaces/firestarter_app rev-parse HEAD` are unchanged from the values recorded in step 2, and `git -C <each> rev-parse --abbrev-ref HEAD` still reports the v1.22 branch.
    - `git -C /workspaces/firestarter_app log origin/beta -1 --format=%H` equals the value fetched in step 1 — nothing was pushed.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md && grep -q 'merge-tree' "$D" && grep -q 'comm -23' "$D" && test "$(git -C firestarter rev-parse --abbrev-ref HEAD)" = "v1.22-at28c-software-data-protection-lifecycle" && test "$(git -C firestarter_app rev-parse --abbrev-ref HEAD)" = "v1.22-at28c-software-data-protection-lifecycle" && echo PREFLIGHT-RECORDED</automated>
  </verify>
  <done>The pre-flight evidence is measured live and committed in `122-DECISION.md`, with any divergence from RESEARCH flagged rather than smoothed over, and both sub-repos are provably unmutated.</done>
</task>

<task type="auto">
  <name>Task 2: Write the recorded accept/avoid/cleanup decision itself (CLOSE-03, D-05) with its declined options named</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §"The push mechanics — CLOSE-03" — D-05, D-06, D-07 verbatim, including every **Rejected:** clause.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Release Mechanics" (lines 433-514) — what the merge push actually triggers, the gate set of each beta workflow, the PAT suppression root cause in `publish.yml`'s own words, C-3's 6-of-13 PyPI miss history, and the post-cut local/remote divergence.
    - `.planning/REQUIREMENTS.md` line 104 — CLOSE-03's literal text.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` — the pre-flight section written in Task 1.
  </read_first>
  <action>
Append the decision itself to `122-DECISION.md`. Structure it as: a one-line verdict, a three-column
table of the three options with their disposition, the accepted sequence, and the declined items with
their reasons.

The verdict is **ACCEPT the auto-fire — the merge IS the b14 cut** (D-05). State it in those terms so
the artifact is greppable for `ACCEPT`.

Record all three options explicitly, because CLOSE-03 asks for an *accept/avoid/cleanup* decision, not
merely a plan:
- **ACCEPT — chosen.** Both repos carry `on: push: branches: [beta]` with auto-increment from a
  git-tag scan and both sit at `3.0.0b13`, so the outbound merge push cuts the next beta by itself in
  both. `paths-ignore` cannot suppress either workflow (the app merge carries 42 non-ignored paths;
  the firmware merge carries `src/`, `include/`, `test/`, `platformio.ini`, `tools/`). This is the
  *"do the cut FROM beta so the merge IS the cut"* option the v1.21 post-mortem named.
- **AVOID — declined.** A `workflow_dispatch` cut from the branch with an explicit `-f beta_version`
  plus temporarily disabling `push: beta` means two repos of trigger edits, and a forgotten re-enable
  silently kills every future beta. Record that **no workflow trigger is edited by this phase** and
  that `paths-ignore` is not weakened.
- **CLEANUP — declined.** The stray `3.0.0b12` prereleases **stay public**. They have been public for
  days and may already be installed; firmware b12 is byte-identical to b11 and app b12 was the v1.21
  close artifact. Deleting a published artifact is an operator-driven outward-facing act, not close
  work. Record that no `--force` push, no history rewrite, and no deletion of any published artifact
  occurs in this phase.

Then record the accepted sequence in order, each step naming the plan that owns it and the constraint
it satisfies: (1) this decision recorded and committed — constraint 1; (2) `beta` → branch inbound
merge with the two-file `--ours` resolution, plan 122-03 — D-06; (3) the eleven nine-row commands plus
both full suites plus CLOSE-01's four mechanisms **on the merged tree**, plan 122-04 — constraints 2
and 6; (4) branch → `beta` outbound `--no-ff` merge and push, plan 122-07, letting CI cut the next
beta and auto-commit the version bump onto `beta`; (5) a **manual** `gh workflow run publish.yml
--repo henols/firestarter_app -f tag=<observed tag>` for PyPI, plan 122-08 — constraint 7; (6) both
channels verified public, plan 122-08 — constraint 3; (7) the D-16 blocking wording review, plan
122-11 — constraint 4; (8) the two comments posted, plan 122-12.

Record these four facts the sequence depends on, each as its own line:
- The observed cut tag is **read, never assumed** (A3). CI producing `3.0.0b14` is derived from
  `update_version.py`'s tag scan, not executed; every downstream step — `publish.yml -f tag=`, both
  `gh release edit`, and the PyPI check — must consume the *observed* tag.
- The PyPI publish is **not** a side effect of the merge. `beta-release.yml` creates the app release
  using `secrets.PERSONAL_ACCESS_TOKEN`, and a PAT without `workflow` scope suppresses the
  `release.published` event — documented in `publish.yml`'s own in-file comment. C-3 measures the
  consequence: **6 of 13** app GH betas never reached PyPI (b4, b5, b6, b9, b10, b12). The manual
  dispatch is the norm at a 46% historical miss rate, not a contingency.
- `ci.yml` **never runs on a `beta` push** (`main` + `pull_request` only), and neither does firmware
  `build.yml`. The merge push fires only `beta-release.yml` / `beta-build.yml`. Do not wait for, or
  chase, `ci.yml` output. The four pre-existing `ruff check` findings and four `ruff format` drift
  files all sit in `tools/` + `.github/scripts/`, outside `ci.yml`'s `firestarter/ tests/` scope — they
  are structurally invisible to CI, out of this phase's scope, and are **not** to be fixed here (C-8).
- After CI, local `beta` is **1 commit behind** the remote in each repo, because each workflow's
  `git-auto-commit-action` pushes the version bump onto `beta`. Any post-cut local `beta` operation
  must `git fetch` first. This is the mechanism that produced `a981642` and `6611fba`.

Finally, a short `## Out of scope for this phase (D-07)` section: the `v1.22` annotated tag and the
meta-repo gitlink bump both stay with `/gsd-complete-milestone`. Record the current gitlink values
(`0048b3d` / `96e0622`, one phase behind the working tips) as deliberately left alone, and that
`git ls-tree HEAD firestarter firestarter_app` is asserted unchanged at phase end (A5). Also record
the one owned trade-off D-01 accepted: **no bench smoke-test of the b14 install/flash path** — the
`pip install --pre` → `fw -i` → one live op chain that Phase 115 existed to prove is trusted, not
re-verified. Name it as a known, accepted gap so no downstream agent silently re-opens it and so a
later install problem reads as accepted rather than overlooked.
  </action>
  <acceptance_criteria>
    - `grep -c 'ACCEPT' 122-DECISION.md` ≥ 1 and the artifact contains a disposition line for each of accept, avoid, and cleanup.
    - The artifact states that the stray b12 prereleases stay public, and that no `--force` push, history rewrite, or published-artifact deletion occurs.
    - The artifact states that no workflow trigger is edited and `paths-ignore` is not weakened.
    - The artifact names the manual `gh workflow run publish.yml --repo henols/firestarter_app -f tag=<observed tag>` step and states the tag is the observed one, not a hardcoded `3.0.0b14`.
    - `grep -c '3.0.0b14' 122-DECISION.md` — every occurrence (if any) is inside a sentence explaining what the auto-increment is *expected* to derive, never inside a command to be executed verbatim. Verify by reading each hit.
    - The artifact records the eight-step sequence with each step's owning plan and the CONTEXT constraint number it satisfies.
    - The artifact records the D-01 no-bench-smoke-test trade-off and the D-07 out-of-scope tag/gitlink items.
    - `git -C /workspaces log -1 --format='%H %ad' --date=iso -- .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` returns a commit — the decision is **committed**, not merely written (CLOSE-03 says recorded).
    - At this point `git -C /workspaces/firestarter_app rev-parse origin/beta` still resolves to the pre-flight value: nothing has been pushed.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md && grep -q 'ACCEPT' "$D" && grep -qi 'cleanup' "$D" && grep -qi 'avoid' "$D" && grep -q 'publish.yml' "$D" && git log -1 --format=%H -- "$D" | grep -qE '^[0-9a-f]{7,}$' && echo DECISION-COMMITTED</automated>
  </verify>
  <done>`122-DECISION.md` is committed in the meta repo with the ACCEPT verdict, both declined options and their reasons, the eight-step sequence mapped to plans and constraints, the observed-tag rule, and the D-07 / D-01 out-of-scope records. Nothing has been pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workstation → GitHub `origin` | Only `git fetch` crosses it in this plan; every mutating direction is deferred to 122-03 / 122-07 |
| recorded decision → irreversible push | The commit timestamp of this artifact is the evidence that consent preceded the act |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-04 | Repudiation | irreversible outward action without recorded consent | high | mitigate | The decision artifact is committed in Wave 1, before any merge or push exists; 122-13 re-asserts its commit predates both sub-repo pushes by `git log --date=iso` |
| T-122-07 | Tampering | GitHub Actions workflow configuration | high | mitigate | The AVOID option is declined **in writing**: no trigger edit, no `paths-ignore` weakening (D-05). Recorded so a later plan cannot rationalise trigger surgery |
| T-122-10 | Tampering | stale research driving a live merge | medium | mitigate | Every pre-flight number is re-measured live; a divergence from RESEARCH is flagged under an explicit `⚠ Divergence` heading rather than smoothed over |
| T-122-11 | Information Disclosure | secrets named in a committed artifact | medium | mitigate | The artifact may name `PERSONAL_ACCESS_TOKEN` / `GITHUB_TOKEN` / `PYPI_API_TOKEN` **as identifiers only**; never run `gh auth token`, never echo a value, never paste a workflow env dump |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). No legitimacy checkpoint required; an install proposal is a scope escape |
</threat_model>

<verification>
1. `122-DECISION.md` exists, is ≥60 lines, and is committed in the meta repo.
2. Both sub-repo HEADs and branch names are unchanged; `origin/beta` in both repos is unchanged from
   the fetched pre-flight value. Nothing merged, nothing pushed.
3. `git -C /workspaces ls-tree HEAD firestarter firestarter_app` still `0048b3d…` / `96e0622…`.
4. The recorded conflict set matches C-1/C-2 (firmware zero, app exactly two) **or** a
   `⚠ Divergence from 122-RESEARCH.md` section exists explaining what changed.

**Known pre-existing dirt — not this plan's damage:** `firestarter`: untracked `firestarter/`.
`firestarter_app`: modified `.gitignore`, untracked `.coverage`, `.planning/config.json`,
`SECURITY.md`, `write_test_port.sh`. Meta: modified `.planning/config.json` (leave uncommitted; do not
iterate `planning.sub_repos`).

**Requirement ticking:** this plan ticks **nothing**. CLOSE-03 is closed only in 122-13, and only
after the decision, the push, and both channel verifications all exist.
</verification>

<success_criteria>
- One committed meta-repo artifact carrying (a) live pre-flight evidence and (b) the accept/avoid/
  cleanup decision with both declined options named.
- The decision's commit provably precedes any push (nothing has been pushed yet at all).
- The observed-tag rule (A3), the manual-dispatch requirement (constraint 7 / C-3), the
  `ci.yml`-does-not-run fact (C-8), and the post-cut divergence (Pitfall 3) are each recorded.
- Both sub-repos are provably unmutated.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-02-SUMMARY.md`
when done. Record the measured pre-flight numbers, any divergence from RESEARCH, and the
`122-DECISION.md` commit SHA + ISO timestamp — 122-13 needs that timestamp to prove the ordering.
</output>
