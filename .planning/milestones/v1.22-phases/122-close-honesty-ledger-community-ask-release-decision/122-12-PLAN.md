---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 12
type: execute
wave: 10
depends_on: ["122-11"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md
autonomous: false
requirements: [CLOSE-02, CLOSE-03]
must_haves:
  truths:
    - "Both prerelease bodies are published from the operator-approved committed files via `gh release edit --notes-file`, never an inline string (D-08)"
    - "Both community comments are posted from the operator-approved committed files via `gh issue comment --body-file`, and the posted text is verified byte-equal to the committed file — which is what proves posted == reviewed (D-16)"
    - "Both issues remain OPEN — closing either would read as a verified fix, which CLOSE-02 forbids regardless of the comment text (D-13)"
    - "No label flag is sent on either call: `gh issue create --label` and `gh issue edit --add-label` both abort before acting on a missing label or without write access, and neither issue carries any label"
    - "Every `gh` call targets the OBSERVED tag / the correct repo read from `122-CUT.md` — no hardcoded version, and `henols/firestarter_prom` for both issues"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md"
      provides: "The delivery record — every gh argv, every byte-equality result, the comment URLs, and the before/after issue state"
      contains: "byte-equal"
      min_lines: 45
  key_links:
    - from: "the four `gh` delivery calls"
      to: "the five files frozen by 122-11"
      via: "`--notes-file` / `--body-file` read the committed files directly; blob SHAs asserted unchanged immediately before and after each call"
      pattern: "notes-file|body-file"
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md"
      to: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CHANNELS.md"
      via: "constraint 3 — both channels were verified public before either comment posted; the delivery record cites the channel artifact as its precondition"
      pattern: "122-CHANNELS"
---

<objective>
Deliver the four outward-facing artifacts: add both hand-written prerelease bodies, then post both
community comments — each from the exact committed file the operator approved in 122-11, and each verified
byte-equal to that file after posting.

Purpose: CLOSE-02's delivery half and CLOSE-03's channel-completion half. Every CONTEXT constraint is now
satisfied and provable: the decision was recorded before the push (constraint 1), the gate ran on the merged
tree before the outbound merge (constraints 2 and 6), the beta exists and both channels are verified public
(constraint 3), the blocking wording review has happened (constraint 4), the ledger and the EIGHTH
CORRECTION preceded all four artifacts (constraint 5), and the PyPI publish was a manual dispatch
(constraint 7).

Output: two published release bodies, two posted comments, and `122-DELIVERY.md` as the evidence record.

**This is irreversible outward text reaching two named people.** `autonomous: false`, with a final go/no-go
between the release bodies and the comments — because by then the rendered public bodies are visible, which
is new information the operator did not have at the wording review.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo for the artifact: **meta** (`/workspaces`). Remote targets: releases in `henols/firestarter` and
`henols/firestarter_app`; issues **11** and **12** in `henols/firestarter_prom` — the project-wide tracker,
not `firestarter_app`.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md
@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CUT.md` (122-07) · `122-CHANNELS.md` (122-08) ·
`122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` +
`122-GH12-COMMENT.md` (122-10) · `122-DELIVERY.md` (**this plan**).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03); outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs (122-07) · one PyPI release (122-08) · **both prerelease bodies added and
both issue comments posted (this plan)**.
**Deliberately NOT produced:** no issue closed (D-13); **no `--label` / `--add-label` / `-l` flag**; no new
issue; no `v1.22` tag, no gitlink bump (D-07); no deletion of the stray b12; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Publish both prerelease bodies with `gh release edit --notes-file` and verify byte-equality</name>
  <files>none in-tree (remote release bodies only)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md` (122-07) — the OBSERVED CUT TAG for each repo. Read both values from here; do not type a version.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-11-SUMMARY.md` — the frozen blob SHAs and byte lengths for `122-RELEASE-NOTES-fw.md` and `122-RELEASE-NOTES-app.md`.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Release bodies (C-6, C-7)" (lines 503-507) — the confirmation that both bodies are empty strings, so this is an **add** via `gh release edit --notes-file`, and that only the firmware release carries assets.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md` §"Delivery pattern" (lines 188-194) — the exact `gh release edit` invocation shape.
  </read_first>
  <action>
Before either call, assert the freeze held: `git rev-parse HEAD:<path>` for both release-note files must
equal the blob SHAs recorded in `122-11-SUMMARY.md`, and neither file may be dirty in
`git status --porcelain`. If a SHA differs, STOP — the file changed after the operator approved it, and the
posted-equals-reviewed guarantee is broken.

Then, for each repo, one call:
- `gh release edit <OBSERVED CUT TAG (firmware)> --repo henols/firestarter --notes-file .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md`
- `gh release edit <OBSERVED CUT TAG (app)> --repo henols/firestarter_app --notes-file .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md`

**Always `--notes-file` with the committed path — never `--notes "…"` and never a heredoc or shell-piped
string.** An interpolated body cannot be proven equal to the reviewed text and is the path by which local
output or a secret could leak into public text.

Do not pass any other flag. In particular do not touch `--prerelease`/`--latest`/`--draft`/`--tag`: the
releases were created by CI as prereleases and must stay exactly as they are. Do not edit or delete any
other release, including the stray b12.

Then verify byte-equality for each, and record the method:
1. Retrieve the live body: `gh release view <tag> --repo <repo> --json body -q '.body'`, written to a file
   in a scratch directory (`$(mktemp -d)`), not into either repo.
2. Normalize **only** two things before comparing: CRLF → LF, and a single trailing newline. GitHub may
   store or return either. Record in the artifact exactly which normalization was applied.
3. `diff` the normalized retrieved body against the committed file. It must be **identical**. Any difference
   beyond the two normalizations named above is a failure: record the diff verbatim and STOP rather than
   re-editing to paper over it.
4. Also record the retrieved body's byte length against the committed file's, and confirm `isPrerelease` is
   still true and the firmware release still carries exactly its three `.hex` assets.

Never run `gh auth token`; never echo any token; never paste a workflow log into the artifact.
  </action>
  <acceptance_criteria>
    - Both release-note files' blob SHAs matched `122-11-SUMMARY.md`'s frozen values immediately before the calls.
    - Exactly two `gh release edit` calls were made, each with `--notes-file` and a committed path, and no other flag; no `--notes` string form was used anywhere.
    - `gh release view <firmware tag> --repo henols/firestarter --json body -q '.body|length'` is now **greater than 0**, and the same for the app release.
    - For each release, the normalized retrieved body `diff`s identical against the committed file; the normalization applied is recorded.
    - `gh release view <firmware tag> --repo henols/firestarter --json isPrerelease,assets -q '{p:.isPrerelease,n:(.assets|length)}'` still reports prerelease true and 3 assets; the app release still reports prerelease true and 0 assets.
    - `gh release list --repo henols/firestarter --limit 20` and the app equivalent both still contain `3.0.0b12` — nothing deleted.
    - Both issue comment counts are still unchanged at this point — no comment has been posted yet.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/122-close-honesty-ledger-community-ask-release-decision && FT=$(grep -m1 'OBSERVED CUT TAG (firmware)' $D/122-CUT.md | grep -oE '3\.0\.0b[0-9]+') && AT=$(grep -m1 'OBSERVED CUT TAG (app)' $D/122-CUT.md | grep -oE '3\.0\.0b[0-9]+') && T=$(mktemp -d) && gh release view "$FT" --repo henols/firestarter --json body -q '.body' | tr -d '\r' > $T/fw.md && gh release view "$AT" --repo henols/firestarter_app --json body -q '.body' | tr -d '\r' > $T/app.md && diff <(sed -e '$a\' $D/122-RELEASE-NOTES-fw.md) <(sed -e '$a\' $T/fw.md) && diff <(sed -e '$a\' $D/122-RELEASE-NOTES-app.md) <(sed -e '$a\' $T/app.md) && echo BODIES-PUBLISHED-BYTE-EQUAL</automated>
  </verify>
  <done>Both prerelease bodies are published from the frozen committed files via `--notes-file`, each verified byte-equal to its source under only CRLF/trailing-newline normalization, with prerelease status and asset inventories unchanged and nothing deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING final go/no-go before the two comments reach two named strangers</name>
  <files>none (gate only)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-11-SUMMARY.md` — the operator's approval verdict and the frozen blob SHAs for both comment drafts.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md` and `122-GH12-COMMENT.md` — the exact text about to be posted.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CHANNELS.md` — the channel verification that makes the re-test ask meaningful.
  </read_first>
  <what-built>
Everything upstream of the two comments is done and verifiable:
- The beta is published on both channels and verified by live resolution checks, not by a CI status
  (`122-CHANNELS.md`).
- Both prerelease bodies are now **live and rendered**, published from the operator-approved files and
  verified byte-equal to them. This is the first time the closing prose is visible as a reader will see it.
- Both comment drafts are frozen at the blob SHAs the operator approved in 122-11, and the approval verdict
  is on the record.
  </what-built>
  <action>
Present and BLOCK until the operator gives an explicit go or no-go:

1. **The two live release pages.** Give their URLs and ask the operator to read the rendered bodies. Prose
   that reads correctly in a plain file can read differently rendered — a heading level, a bolded caveat
   that disappeared into a paragraph, a bullet list that flattened. If anything needs changing, it is still
   changeable: a release body can be re-edited, and nothing has been said to anyone yet.
2. **The two comment texts, final.** State that these will be posted to `henols/firestarter_prom` issues
   **11** and **12**, from the committed files, and that both issues **stay OPEN**.
3. **What is irreversible from here.** A posted issue comment notifies the recipients immediately and cannot
   be unsaid. Everything before this point could still be revised.
4. **The ask being made of two volunteers.** One is being asked to re-run a write and a verify on their own
   AT28C256 and report back either way. Confirm the operator is content with that ask, including the
   optional `dev test` offer and its always-writes warning.

Record the verdict **verbatim**. If the operator asks for a change to either release body, apply exactly
that change, re-verify byte-equality, and re-present. If they decline the comments, stop the plan here and
record it — the phase's CLOSE-02 delivery then remains open rather than being force-closed.

This gate is **not auto-approvable**. Do not proceed on silence or on `auto_advance`.
  </action>
  <acceptance_criteria>
    - The operator's verdict is recorded verbatim, including any change requested to either release body.
    - Both comment drafts' blob SHAs still match `122-11-SUMMARY.md`'s frozen values at the moment the gate closes.
    - Both issue comment counts are still unchanged; both issues still `OPEN`.
    - If a release-body change was requested, it was applied, re-verified byte-equal, and re-presented before the gate closed.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has typed an explicit go for posting both comments — or a no-go / a named change to a release body — and the verdict is recorded verbatim in the plan SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "post" to publish both comments, or name the change / decline.</resume-signal>
  <done>The operator has given an explicit go or no-go after seeing the live rendered release bodies; the verdict is recorded verbatim; no comment has been posted before the verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Post both comments with `gh issue comment --body-file`, verify byte-equality and OPEN state, and write the delivery record</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-10-SUMMARY.md` §the delivery pre-brief — the exact argv and the explicit list of forbidden flags.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md` §"`gh` argv pattern" (lines 226-254) — the in-tree permission-independent idiom, and the standing rule to never add a label flag.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-11-SUMMARY.md` — the frozen blob SHAs for both drafts.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-08-SUMMARY.md` — the pre-delivery comment counts for issues 11 and 12 (expected 12 and 8), needed for the exact-increment assertion.
  </read_first>
  <action>
Record the pre-state first: for each issue, `gh issue view <n> --repo henols/firestarter_prom --json state,comments -q '{state:.state,n:(.comments|length)}'` and the current label list. Assert both are `OPEN`
and both counts match `122-08-SUMMARY.md`. Re-assert both drafts' blob SHAs against the frozen values.

Then exactly two calls:
- `gh issue comment 11 --repo henols/firestarter_prom --body-file .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md`
- `gh issue comment 12 --repo henols/firestarter_prom --body-file .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md`

**Argv discipline, from the in-tree idiom.** List form, explicit `--repo`, `--body-file` with the committed
path. Never `--body "…"`, never a heredoc, never a shell-piped string — an interpolated body cannot be
proven equal to the reviewed text. Never pass `--label`, `--add-label`, `-l`, `--assignee`, `-a`,
`--milestone`, `-m`, `--project`, `-p`, `--web`, `-w`, `--editor`, `-e`, `--edit-last` or `--delete-last`.
The label flags **abort before acting** unless the label pre-exists and the caller has write access, and
neither issue carries any label; the others open an interactive surface or mutate the wrong thing. Record
the exact argv used for both calls in the artifact so the negative can be audited.

Do **not** close either issue and do not run `gh issue close`. D-13 is explicit: closing would read as
*verified fixed*, which CLOSE-02 forbids regardless of the comment text.

Then verify, per comment:
1. Retrieve the posted body: `gh issue view <n> --repo henols/firestarter_prom --json comments -q '.comments[-1].body'`, written to a scratch file.
2. Normalize **only** CRLF → LF and a single trailing newline; record the normalization applied.
3. `diff` against the committed draft — must be identical. Any other difference is a failure: record the
   diff verbatim and surface it rather than editing the comment to match.
4. Record the new comment's URL and author.
5. Assert the comment count incremented by **exactly 1** on each issue, that `state` is still `OPEN`, and
   that the issue's label list is still empty.

Finally write `122-DELIVERY.md`: a table of the four delivery calls (two `gh release edit`, two
`gh issue comment`) with columns `#` · `Target` · `Exact argv` · `Source file + blob SHA` · `Byte-equality
verdict` · `Result URL`; then a short section recording the before/after issue state, the exact-increment
assertion, the still-`OPEN` assertion, the empty-label assertion, the normalization applied, and the
explicit negative list of flags that were **not** sent. Cross-reference `122-CHANNELS.md` as the
precondition that made the re-test ask meaningful (constraint 3) and `122-11-SUMMARY.md` as the approval
that made the text postable (constraint 4).

Close the artifact with the constraint-satisfaction ledger: all seven CONTEXT sequencing constraints plus
the observed-tag rule, each with the plan and evidence artifact that satisfied it. This is the record
122-13 reads when it ticks the requirement checkboxes.
  </action>
  <acceptance_criteria>
    - Exactly two `gh issue comment` calls were made, both with `--repo henols/firestarter_prom` and `--body-file <committed path>`; the recorded argv contains **none** of the forbidden flags.
    - For each comment, the normalized posted body `diff`s identical against the committed draft; the normalization applied is recorded.
    - `gh issue view 11 --repo henols/firestarter_prom --json comments -q '.comments|length'` equals the pre-delivery count **+1**, and the same for issue 12.
    - `gh issue view 11 --repo henols/firestarter_prom --json state -q '.state'` returns `OPEN`, and the same for 12.
    - `gh issue view 11 --repo henols/firestarter_prom --json labels -q '.labels|length'` returns `0`, and the same for 12.
    - Both comment URLs are recorded.
    - `122-DELIVERY.md` exists, is ≥45 lines, contains the literal phrase `byte-equal`, the four-call table, the negative flag list, and the seven-constraint satisfaction ledger plus the observed-tag rule.
    - No secret value appears in the artifact; `gh auth token` was not run.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && D=.planning/phases/122-close-honesty-ledger-community-ask-release-decision && T=$(mktemp -d) && for n in 11 12; do gh issue view $n --repo henols/firestarter_prom --json comments -q '.comments[-1].body' | tr -d '\r' > $T/c$n.md; test "$(gh issue view $n --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" || { echo "ISSUE $n NOT OPEN"; exit 1; }; test "$(gh issue view $n --repo henols/firestarter_prom --json labels -q '.labels|length')" = "0" || { echo "ISSUE $n GAINED A LABEL"; exit 1; }; done && diff <(sed -e '$a\' $D/122-GH11-COMMENT.md) <(sed -e '$a\' $T/c11.md) && diff <(sed -e '$a\' $D/122-GH12-COMMENT.md) <(sed -e '$a\' $T/c12.md) && test "$(wc -l < $D/122-DELIVERY.md)" -ge 45 && grep -q 'byte-equal' $D/122-DELIVERY.md && echo COMMENTS-POSTED-BYTE-EQUAL-AND-OPEN</automated>
  </verify>
  <done>Both comments are posted from the frozen committed drafts via `--body-file`, each verified byte-equal under only CRLF/trailing-newline normalization; both issues are still OPEN with exactly one new comment each and no label; `122-DELIVERY.md` records every argv, every verdict, the negative flag list, and the seven-constraint satisfaction ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed reviewed file → permanent public text | The only crossing allowed is `--body-file` / `--notes-file` reading the committed path; an interpolated string breaks the proof and is the leak path for local output or a secret |
| local session credentials → public artifact | `gh` runs as an authenticated account with admin on the tracker; no token value may ever enter a body, an artifact, or a log quote |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-06 | Repudiation | posted text drifting from reviewed text | critical | mitigate | Blob SHAs re-asserted against 122-11's freeze immediately before every call; delivery exclusively via `--body-file` / `--notes-file`; posted text retrieved and `diff`ed byte-equal under only CRLF/trailing-newline normalization, with the normalization recorded |
| T-122-01 | Information Disclosure | secret or local-path leakage into public text | high | mitigate | No interpolated body of any kind; no heredoc, no shell pipe; `gh auth token` never run; no workflow log quoted; the four outward-facing files already asserted free of `/workspaces/` paths, `/dev/tty` names and token names |
| T-122-26 | Denial of Service | a `gh` flag that aborts before the comment is created | medium | mitigate | Explicit negative list — no `--label`/`--add-label`/`-l`/`-a`/`-m`/`-p`/`--web`/`--editor`/`--edit-last`/`--delete-last`; the exact argv is recorded so the negative is auditable; issue label counts asserted still 0 |
| T-122-29 | Repudiation | closing an issue implying a verified fix | high | mitigate | D-13: neither issue is closed; `state` asserted `OPEN` after each post; `gh issue close` never run |
| T-122-21 | Tampering | editing the wrong release | medium | mitigate | Both tags read from `122-CUT.md`'s OBSERVED CUT TAG values; no other flag passed to `gh release edit`; `isPrerelease` and asset inventories asserted unchanged; `3.0.0b12` asserted still present in both repos |
| T-122-04 | Repudiation | irreversible outward action without consent | critical | mitigate | Task 2's blocking final go/no-go, positioned after the release bodies are live so the operator judges rendered prose — new information the wording review did not have |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. Both release bodies are non-empty and byte-equal to their committed source files.
2. Both prereleases keep `isPrerelease` true; firmware still has 3 `.hex` assets, app still has 0.
3. `3.0.0b12` still present in both repos' release lists.
4. The operator's final go is recorded verbatim, taken after the rendered bodies were visible.
5. Both comments posted from the committed drafts, each byte-equal to its source.
6. Both issues `OPEN`, each with exactly one new comment, each with zero labels.
7. `122-DELIVERY.md` records the four calls, the negative flag list, and the seven-constraint satisfaction
   ledger plus the observed-tag rule.
8. Meta gitlinks still `0048b3d` / `96e0622`; no `v1.22` tag (D-07).

**Requirement ticking:** this plan ticks **nothing**, even though it completes the outward-facing work.
CLOSE-01, CLOSE-02 and CLOSE-03 are ticked only in 122-13, after re-reading each requirement's **prose** in
`REQUIREMENTS.md`. Four executors in Phase 116 ticked a multi-plan requirement early and one Phase 121 plan
needed a revert for it.
</verification>

<success_criteria>
- Four outward-facing deliveries made, each from an operator-approved committed file, each byte-verified.
- Both issues remain OPEN with no label and exactly one new comment each.
- No published artifact was deleted, no release attribute other than the body was touched, no issue closed.
- The delivery record makes every constraint's satisfaction auditable in one place.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-12-SUMMARY.md`
when done. Record the operator's final verdict verbatim, all four exact argv strings, all four
byte-equality verdicts with the normalization applied, both comment URLs, the before/after issue state, and
the seven-constraint satisfaction ledger.
</output>
