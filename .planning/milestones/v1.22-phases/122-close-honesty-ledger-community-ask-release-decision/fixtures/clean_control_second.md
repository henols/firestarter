<!-- test fixture for check_permitted_claims.py -- NOT a closing artifact; never add to _DEFAULT_TARGETS -->

# Second clean control fixture

This is a second, textually distinct clean control. Its only job is to let
the anti-skip test prove the scanner's PASS line names both scanned files at
once. Like its sibling, it paraphrases the permitted claim: emission of the
SDP command sequences is byte-exact by golden register trace, and nothing
here claims the sequence changed state on a real part.

No AT28C silicon was tested.
