---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 13
type: execute
wave: 11
depends_on: ["122-12"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLOSE-01, CLOSE-02, CLOSE-03]
must_haves:
  truths:
    - "Each of CLOSE-01, CLOSE-02 and CLOSE-03 is re-verified against its own PROSE in `REQUIREMENTS.md` — not against a plan's summary of it — and each tick cites the evidence artifact that closes it"
    - "This is the ONLY plan in the phase that ticks a requirement checkbox; every earlier plan ticked nothing"
    - "The `REQUIREMENTS.md` edit is scoped: only three checkboxes, their parentheticals, and three traceability rows change"
    - "`git ls-tree HEAD firestarter firestarter_app` is asserted UNCHANGED at phase end — the `v1.22` tag and the gitlink bump stay with `/gsd-complete-milestone` (D-07, RESEARCH A5)"
    - "The claim scanner is green on all five default targets at the phase's final commit, and the phase record states plainly that this does NOT by itself satisfy ROADMAP criterion 4"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLOSE-01/02/03 marked Complete with evidence parentheticals, and three traceability rows moved from Pending to Complete"
      contains: "CLOSE-03"
  key_links:
    - from: ".planning/REQUIREMENTS.md CLOSE-01/02/03"
      to: "122-NONREGRESSION.md, 122-DELIVERY.md, 122-DECISION.md, 122-CHANNELS.md"
      via: "each closing parenthetical names the artifact and the command/observation that closes that requirement, never a plan number alone"
      pattern: "122-NONREGRESSION|122-DELIVERY|122-DECISION|122-CHANNELS"
---

<objective>
Close the phase: re-verify each of CLOSE-01, CLOSE-02 and CLOSE-03 against its own requirement prose, tick
the three checkboxes and three traceability rows with evidence parentheticals, and make the phase-end
assertions — including that the meta-repo gitlinks are still pinned and the `v1.22` tag was never created.

Purpose: requirement completion in this project is per-plan, not per-requirement, and all three CLOSE ids
span several plans. Four executors in Phase 116 ticked a multi-plan requirement early, and one Phase 121
plan needed a revert for it. This is the single plan permitted to tick, and only after re-reading each
requirement's **prose**.

Output: a scoped edit to `.planning/REQUIREMENTS.md`, plus the phase-end verification record.

**No outward-facing action happens here.** Nothing is posted, pushed, published, tagged, or deleted.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. Read-only `git`/`gh` checks touch both sub-repos and both remotes.

@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CUT.md` (122-07) · `122-CHANNELS.md` (122-08) ·
`122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` +
`122-GH12-COMMENT.md` (122-10) · `122-DELIVERY.md` (122-12).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · **`.planning/REQUIREMENTS.md`
CLOSE-01/02/03 checkboxes + 3 traceability rows (this plan — the only plan permitted to tick).**
**Sub-repo git state:** inbound merges (122-03); outbound merges + pushes (122-07). No source authored by
the phase.
**Remote state:** two `beta` refs (122-07) · two prereleases with hand-written bodies (122-07 + 122-12) ·
one PyPI release (122-08) · two issue comments (122-12).
**Deliberately NOT produced, and asserted absent by this plan:** no `v1.22` tag; no meta gitlink bump
(D-07, A5); no `PROTOCOL-LEDGER.{md,json}` edit and no `check_ledger.py` fix (D-09, C-4); no
`chip_database.json` change; no `support_status` change; no ladder-state promotion; no stable release; no
workflow-trigger edit; no deletion of the stray b12; neither issue closed.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify CLOSE-01, CLOSE-02 and CLOSE-03 against their own prose, and build the evidence table</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-13-SUMMARY.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 100-104 — the **prose** of CLOSE-01, CLOSE-02 and CLOSE-03, read directly. Do not substitute a plan's paraphrase; read the requirement text itself, clause by clause.
    - `.planning/ROADMAP.md` §"Phase 122" Success Criteria (lines 486-491) — the four criteria the phase is verified against.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` — CLOSE-01's evidence.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md` — CLOSE-02's delivery evidence and the seven-constraint satisfaction ledger.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md`, `122-CUT.md` and `122-CHANNELS.md` — CLOSE-03's evidence: the recorded decision, the observed cut, and both verified channels.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-11-SUMMARY.md` — the operator's wording verdict and the D-14 ruling.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Common Pitfalls" → Pitfall 6 (lines 932-938) — the premature-ticking failure mode and the specific note that CLOSE-02 cannot be ticked until both comments are posted and CLOSE-03 not until the decision, the push and both channel verifications all exist.
  </read_first>
  <action>
Split each requirement into its literal clauses and re-verify each clause independently, executing the check
rather than citing a prior SUMMARY.

**CLOSE-01** has three clauses. (a) `0x0D` stays `UNVERIFIED` in `PROTOCOL-LEDGER` — re-run the grep and
also assert `git status --porcelain -- .planning/v1.16/ledger/` is empty, so the requirement was satisfied
by verification and not by a write (D-09). (b) **Zero** chips change `support_status` — re-run
`tools/diff_db.py` (exit 0; identity means still exactly 2 explained `PGSZ_PAGE_SIZE` changes, 0 new, 0
removed — do not read the "2" as a failure) and `tools/check_no_community_support_status_write.py`. (c) The
84-chip count is unchanged — re-run `pytest tests/test_sdp_db_invariant.py -q`. Note in the table that
`check_ledger.py` is **not** a CLOSE-01 gate: it is pre-existing RED from v1.19 Phase 104's `flash3`/`flash4`
rename, CLOSE-01's text does not mention it, and fixing it would edit a closed milestone's artifact.

**CLOSE-02** has three clauses. (a) gh#12 answered with the decided auto-unlock policy — confirm the posted
comment exists and contains all four policy elements. (b) gh#11 followed up — confirm the posted comment
exists. (c) Both framed as *"here is what changed and why we believe it addresses your report; please
re-test"*, **never as a verified fix** — cite the operator's wording approval, the byte-equality proofs, the
green default-target claim scan, and the fact that both issues are still `OPEN`. State explicitly that
clause (c) is closed by the **operator review**, not by the scanner: the scanner is the mechanizable half of
ROADMAP criterion 4 and does not satisfy it alone.

**CLOSE-03** has one clause with a hard ordering: the accept/avoid/cleanup decision is made and recorded
**before** any push. Re-prove the ordering mechanically — `122-DECISION.md`'s commit ISO timestamp against
the two outbound merge/push timestamps on `origin/beta` — and cite `122-DECISION.md` for the decision
itself (all three options dispositioned), `122-CUT.md` for the observed cut, and `122-CHANNELS.md` for both
verified channels. Note that D-05's chosen ACCEPT means no trigger was edited and the stray b12 stays
public.

Then check the four ROADMAP success criteria the same way, and record which artifact closes each. For
criterion 4 record the three-way split honestly: the mechanizable half (the green default-target claim scan
over all five artifacts), the judgement half (the D-16 operator review, with the verdict and the D-14
ruling cited), and the class with sampling rate **zero, permanently, by design** — that SDP works on real
AT28C silicon, which is the forbidden claim and is not asserted anywhere.

Produce a table in the SUMMARY: `Requirement / criterion` · `Clause` · `Check executed` · `Observed` ·
`Closing artifact` · `Verdict`. Every row's `Check executed` must be a command or an observation made in
this plan, not a citation of an earlier plan's PASS.

If any clause does not verify, do **not** tick that requirement. Record the gap and stop — an untickable
requirement at this point is a real finding, not a formality to push through.
  </action>
  <acceptance_criteria>
    - The evidence table covers all three CLOSE requirements clause by clause and all four ROADMAP success criteria, with every `Check executed` cell naming a command or observation performed in this plan.
    - `grep -c '^| `0x0D` .*\*\*UNVERIFIED\*\*' .planning/v1.16/ledger/PROTOCOL-LEDGER.md` returns **1** and `git status --porcelain -- .planning/v1.16/ledger/` is empty.
    - `cd /workspaces/firestarter_app && python3 tools/diff_db.py` exits 0; `python3 tools/check_no_community_support_status_write.py` exits 0; `python3 -m pytest tests/test_sdp_db_invariant.py -q` reports 4 passed.
    - Both issues have the new comments and both are `OPEN`; both comment bodies are still byte-equal to their committed drafts.
    - The CLOSE-03 ordering proof shows `122-DECISION.md`'s commit timestamp strictly earlier than both push/merge timestamps.
    - The criterion-4 row records all three parts of the split and states that the scanner alone does not satisfy it.
    - No `check_ledger.py` invocation appears among the executed checks.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -c '^| `0x0D` .*\*\*UNVERIFIED\*\*' .planning/v1.16/ledger/PROTOCOL-LEDGER.md)" = "1" && test -z "$(git status --porcelain -- .planning/v1.16/ledger/)" && cd firestarter_app && python3 tools/diff_db.py && python3 tools/check_no_community_support_status_write.py && python3 -m pytest tests/test_sdp_db_invariant.py -q && cd /workspaces && test "$(gh issue view 11 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && test "$(gh issue view 12 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && echo CLOSE-REQS-REVERIFIED</automated>
  </verify>
  <done>Every clause of all three CLOSE requirements and all four ROADMAP criteria are re-verified by commands executed in this plan, the criterion-4 three-way split is recorded honestly, and the CLOSE-03 ordering is proven by timestamp.</done>
</task>

<task type="auto">
  <name>Task 2: Tick CLOSE-01/02/03 and their three traceability rows with a scoped edit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 100-104 (the three CLOSE requirement lines) and lines 200-202 (the three traceability rows currently reading `Pending`).
    - `.planning/REQUIREMENTS.md` lines 96-98 — the GATE-01/02/03 lines, as the in-file precedent for how a closing parenthetical is written in this project: the closing plan named, the mechanism, and the measured evidence, ending with `— **ticked**`.
    - The evidence table from Task 1.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DELIVERY.md` — the artifact names to cite in the parentheticals.
  </read_first>
  <action>
Use scoped `Edit` calls on `.planning/REQUIREMENTS.md`. **Never `Write`** — a whole-file write would destroy
the other 38 requirements, the Future Requirements section, the Out of Scope table, the Validation Ceiling
and the traceability table.

Change exactly six things:
1. `- [ ] **CLOSE-01**` → `- [x] **CLOSE-01**`, appending a closing parenthetical in the GATE-03 style: the
   three clauses each with the mechanism and the observed result (`PROTOCOL-LEDGER.md` `0x0D` row read
   `**UNVERIFIED**` with the ledger directory provably unmodified; `diff_db.py` exit 0 with identity meaning
   still exactly 2 explained changes; `check_no_community_support_status_write.py` exit 0;
   `test_sdp_db_invariant.py` 4 passed pinning the 84 count), the note that all four ran on the **merged**
   tree per the ordering this phase followed, the record that `check_ledger.py` was **not** used and why,
   and the closing artifact `122-NONREGRESSION.md`, ending `— **ticked**`.
2. `- [ ] **CLOSE-02**` → `- [x] **CLOSE-02**`, with a parenthetical naming: both comments posted to
   `henols/firestarter_prom` #11 and #12 from committed files via `--body-file` and verified byte-equal;
   both issues left **OPEN**; the blocking operator wording review as the mechanism that closed the
   never-as-a-verified-fix clause; the claim gate recorded as the mechanizable half only; and the corrected
   size-class answer replacing D-14's prescribed wording, with the operator's ruling cited. Closing
   artifacts `122-DELIVERY.md`, `122-GH11-COMMENT.md`, `122-GH12-COMMENT.md`, ending `— **ticked**`.
3. `- [ ] **CLOSE-03**` → `- [x] **CLOSE-03**`, with a parenthetical naming: the accept/avoid/cleanup
   decision recorded and **committed before any push**, with the timestamp ordering proof; ACCEPT chosen
   (the merge IS the cut) with AVOID and CLEANUP both declined and their reasons; the observed cut tag read
   rather than assumed; the manual `publish.yml` dispatch; and both channels verified public. Closing
   artifacts `122-DECISION.md`, `122-CUT.md`, `122-CHANNELS.md`, ending `— **ticked**`.
4-6. The three traceability rows: `| CLOSE-01 | Phase 122 | Pending |` → `| CLOSE-01 | Phase 122 |
   Complete |`, and the same for CLOSE-02 and CLOSE-03.

Do **not** edit any other requirement's wording. In particular, do not "fix" LOCK-04's or HOST-04's
mechanism text, do not touch the Validation Ceiling, and do not adjust the Coverage arithmetic beyond what
the three status changes imply — the 41/41 mapped and 0 unmapped counts are unaffected. Every mechanism
correction this phase found is recorded in `122-LEDGER.md` and `PROJECT.md`'s EIGHTH CORRECTION, which is
this project's established response (Pattern 2).

Update the file's trailing `*Last updated: …*` line to name this phase's close, following the existing
line's format.

Then assert the edit's integrity: the diff must touch only the six lines plus the trailing metadata line;
`grep -c '^- \[ \] \*\*CLOSE-0'` must return 0 and `grep -c '^- \[x\] \*\*CLOSE-0'` must return 3; the
`| Pending |` count in the traceability table must drop to 0; and the total requirement-line count must be
unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*CLOSE-0' .planning/REQUIREMENTS.md` returns **3**; `grep -c '^- \[ \] \*\*CLOSE-0' .planning/REQUIREMENTS.md` returns **0**.
    - `grep -c '| Pending |' .planning/REQUIREMENTS.md` returns **0**.
    - Each of the three CLOSE lines ends with `— **ticked**` and names at least one `122-*` closing artifact.
    - `git -C /workspaces diff --numstat -- .planning/REQUIREMENTS.md` shows changes confined to the six requirement/traceability lines plus the trailing metadata line — verify by reading the diff; no other requirement line appears in it.
    - `grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md` equals the pre-edit count **+3**.
    - The Validation Ceiling section is byte-unchanged: `git diff -- .planning/REQUIREMENTS.md` contains no line from §"Validation Ceiling".
    - The Coverage block still reads 41 total and 0 unmapped.
    - `Write` was not used on `.planning/REQUIREMENTS.md` — only `Edit`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -c '^- \[x\] \*\*CLOSE-0' .planning/REQUIREMENTS.md)" = "3" && test "$(grep -c '^- \[ \] \*\*CLOSE-0' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -c '| Pending |' .planning/REQUIREMENTS.md)" = "0" && grep -q 'Unmapped: \*\*0\*\*' .planning/REQUIREMENTS.md && grep -q 'Forbidden claim' .planning/REQUIREMENTS.md && echo REQUIREMENTS-TICKED-SCOPED</automated>
  </verify>
  <done>All three CLOSE checkboxes and all three traceability rows are Complete with evidence parentheticals in the project's established style; the edit is scoped to six lines plus the metadata line; no other requirement's wording, the Validation Ceiling, or the Coverage arithmetic was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Make the phase-end assertions and record what stays for `/gsd-complete-milestone`</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-13-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Assumptions Log" A5 (line 1326) — the gitlink assertion this task discharges.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §D-07 and §"Explicitly NOT in scope" — the exact list of what stays with the close ritual.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-03-SUMMARY.md` — both inbound merge SHAs, for the tree-identity assertion.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-VALIDATION.md` §"Validation Sign-Off" — the checklist this phase's validation artifact is settled against.
    - `.planning/STATE.md` lines 1-20 and the long inline tooling-defect note (around line 99) — the documented pattern that STATE.md tooling under-writes and re-clobbers fields, so the frontmatter must be hand-verified.
  </read_first>
  <action>
Make the phase-end assertions and record them. No outward-facing action, no source change.

Assertions:
1. **A5 — gitlinks unchanged.** `git -C /workspaces ls-tree HEAD firestarter firestarter_app` must still
   report `0048b3d9a3b9aaec5e7e3030f9313acce8e6411a` and `96e062261b8a5e8c29fe3eb6d888468cf876a6cf`. The
   working tips advanced (inbound merges, then the outbound merges landed on `beta`), and the committed
   gitlinks deliberately did **not** follow. Also assert `git -C /workspaces status --porcelain -- firestarter firestarter_app` is empty — no submodule pointer staged.
2. **No `v1.22` tag exists anywhere.** `git -C /workspaces/firestarter tag --list 'v1.22*'` and the app
   equivalent are empty, and `gh release view v1.22 --repo <each>` fails. The tag stays with
   `/gsd-complete-milestone` (D-07).
3. **Sub-repo branch tips unchanged since 122-03.** Both repos are still on
   `v1.22-at28c-software-data-protection-lifecycle`, and both HEADs equal the inbound merge SHAs recorded in
   `122-03-SUMMARY.md`. The phase authored no source; the outbound merges created commits on `beta`, not on
   the milestone branch.
4. **The claim scanner is green on all five default targets** at the phase's final commit — run it with no
   arguments and no env override, and record the PASS line. Then state plainly, once more, that this does
   **not** by itself satisfy ROADMAP criterion 4.
5. **`check_permitted_claims.py`'s own paired test still passes** —
   `python3 -m pytest .planning/phases/122-…/test_check_permitted_claims.py -q` reports 7 passed. A gate
   whose test rotted is a gate that has stopped failing.
6. **Both channels still resolve.** Re-check the PyPI JSON API for the observed version and re-check the
   firmware prerelease's three `.hex` assets. A point-in-time verification at 122-08 plus a re-check here
   bounds any propagation weirdness.
7. **Both issues still OPEN with the posted comments intact**, both bodies still byte-equal to the committed
   drafts, and both label lists still empty.
8. **Both prerelease bodies still non-empty and byte-equal** to their committed sources.
9. **`3.0.0b12` still present in both repos' release lists** — nothing published was deleted (D-05).
10. **`PROTOCOL-LEDGER.{md,json}` and `check_ledger.py` untouched** — `git status --porcelain -- .planning/v1.16/ledger/` empty, and no invocation of `check_ledger.py` anywhere in the phase's executed commands.
11. **No workflow file changed in either sub-repo** across the whole phase: `git -C <each> diff <phase base> --name-only -- .github/` is empty, using the phase-base SHAs `0048b3d` / `96e0622`.

Then settle `122-VALIDATION.md`'s sign-off block: set `status: complete`, `nyquist_compliant: true` and
`wave_0_complete: true` **only after** individually re-verifying every row that was originally marked
`❌ W0` against the real landed file and command names — Plan 120-12's precedent is that three such rows'
references were wrong and had to be corrected in place. Fill the Task ID / Plan / Wave columns of the
per-task verification map from the plans as executed, and correct any command reference that does not match
what actually shipped rather than leaving a stale one.

Then record, in a clearly-headed section, exactly what stays for `/gsd-complete-milestone`:
- The `v1.22` annotated tag in both sub-repos.
- The meta-repo gitlink bump from `0048b3d` / `96e0622` to the current tips.
- The `main`-branch merges, which are also what finally turns `catalog-sync-check.yml` green (it checks out
  both sub-repos at `ref: main`, so its red state until then is by design) and what first executes firmware
  `build.yml`'s `pio test -e native_nodevtools` step for real.
- The stable release, which remains operator-gated — the PyPI stable channel is still `2.0.7` and nothing in
  this phase approached it.
- The `check_ledger.py` RED, recommended as a backlog seed rather than a fix (fixing it edits a closed
  milestone's artifact).
- The stray `3.0.0b12` prereleases, which stay public unless the operator decides otherwise.

Finally, hand-verify `.planning/STATE.md`'s frontmatter after any state tooling call: this project has
recorded the `state.*` handlers under-writing and re-clobbering `current_phase_name`, `status`,
`stopped_at`, `progress.percent` and `progress.total_plans`, including a case where a later call in the same
session silently reverted a field an earlier call had just fixed. Call `state.record-session` first, then the
progress/metric/decision calls, then read the frontmatter and correct any stale field by hand. Note that
`current_phase_name` is at particular risk here because the Phase 122 title contains an em-dash, which is
the documented trigger for the title-splitting defect. Record the before/after frontmatter values.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces ls-tree HEAD firestarter firestarter_app` reports `0048b3d9a3b9aaec5e7e3030f9313acce8e6411a` and `96e062261b8a5e8c29fe3eb6d888468cf876a6cf`; `git -C /workspaces status --porcelain -- firestarter firestarter_app` is empty.
    - `git -C /workspaces/firestarter tag --list 'v1.22*'` and the app equivalent are both empty.
    - Both sub-repos are on `v1.22-at28c-software-data-protection-lifecycle` with HEAD equal to 122-03's recorded inbound merge SHA.
    - `python3 .planning/phases/122-…/check_permitted_claims.py` (no arguments) exits 0 and names all five targets.
    - `python3 -m pytest .planning/phases/122-…/test_check_permitted_claims.py -q` reports **7 passed**.
    - The PyPI JSON API still lists the observed version; the firmware prerelease still has three `.hex` assets; both release bodies still non-empty and byte-equal; both issues `OPEN` with zero labels and byte-equal comments; `3.0.0b12` still present in both repos.
    - `git status --porcelain -- .planning/v1.16/ledger/` is empty.
    - `git -C /workspaces/firestarter diff 0048b3d --name-only -- .github/` and `git -C /workspaces/firestarter_app diff 96e0622 --name-only -- .github/` are both empty.
    - `122-VALIDATION.md` frontmatter reads `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, and every originally-`❌ W0` row was individually re-verified against real landed names (corrections applied in place where needed).
    - The what-stays-for-the-close-ritual section names all six items.
    - `.planning/STATE.md`'s frontmatter is hand-verified with before/after values recorded, and `current_phase_name` is the full Phase 122 title, not a truncated parenthetical.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git ls-tree HEAD firestarter firestarter_app | grep -q 0048b3d9a3b9aaec5e7e3030f9313acce8e6411a && git ls-tree HEAD firestarter firestarter_app | grep -q 96e062261b8a5e8c29fe3eb6d888468cf876a6cf && test -z "$(git status --porcelain -- firestarter firestarter_app)" && test -z "$(git -C firestarter tag --list 'v1.22*')" && test -z "$(git -C firestarter_app tag --list 'v1.22*')" && python3 .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py && python3 -m pytest .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py -q 2>&1 | grep -qE '7 passed' && test -z "$(git status --porcelain -- .planning/v1.16/ledger/)" && echo PHASE-END-ASSERTIONS-OK</automated>
  </verify>
  <done>All eleven phase-end assertions hold; `122-VALIDATION.md` is settled with every Wave 0 row individually re-verified; the six close-ritual items are recorded; STATE.md's frontmatter is hand-verified with before/after values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a multi-plan requirement → a ticked checkbox | Ticking is a claim about the whole phase; this project has had four premature ticks and one revert |
| this phase → `/gsd-complete-milestone` | The tag, the gitlink bump, the `main` merges and the stable release must be left untouched and explicitly handed over |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-30 | Repudiation | premature or unearned requirement tick | high | mitigate | Each requirement is split into its literal clauses and each clause re-verified by a command executed in this plan, never by citing an earlier SUMMARY; this is the only plan permitted to tick; a non-verifying clause blocks the tick and is recorded as a finding |
| T-122-18 | Tampering | `.planning/REQUIREMENTS.md` (38 other requirements, the Ceiling, the Coverage arithmetic) | high | mitigate | Scoped `Edit` only, never `Write`; the diff asserted confined to six lines plus the metadata line; the Validation Ceiling asserted byte-unchanged; the Coverage counts asserted unchanged |
| T-122-13 | Tampering | meta gitlinks / the `v1.22` tag | medium | mitigate | A5/D-07: gitlinks asserted still `0048b3d` / `96e0622`, no submodule pointer staged, no `v1.22*` tag in either repo, and the hand-over list names both explicitly |
| T-122-27 | Repudiation | a green gate presented as a satisfied criterion | high | mitigate | Criterion 4's row records all three parts of the split, and the phase record states twice that the scanner alone does not satisfy it |
| T-122-08 | Tampering | the gate itself rotting | medium | mitigate | The paired anti-hollow test is re-run at the phase's final commit and must still report 7 passed |
| T-122-14 | Tampering | closed-milestone artifacts | medium | mitigate | `git status --porcelain -- .planning/v1.16/ledger/` asserted empty; `check_ledger.py` never invoked; its RED handed to the backlog rather than fixed |
| T-122-07 | Tampering | workflow configuration across the whole phase | high | mitigate | `git diff <phase base> --name-only -- .github/` asserted empty in both sub-repos, so no trigger drifted at any point |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs across the entire phase (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. Every clause of CLOSE-01, CLOSE-02, CLOSE-03 and all four ROADMAP criteria re-verified by commands run
   in this plan; the evidence table records each.
2. All three checkboxes and all three traceability rows Complete with evidence parentheticals; the edit is
   scoped and the Validation Ceiling is byte-unchanged.
3. Gitlinks still `0048b3d` / `96e0622`; no submodule pointer staged; no `v1.22*` tag in either repo.
4. Both sub-repos still on the milestone branch at 122-03's merge SHAs; no `.github/` file changed across
   the phase.
5. Claim scanner green on all five default targets; its paired test still 7 passed.
6. Both channels still resolve; both bodies non-empty and byte-equal; both issues `OPEN`, zero labels,
   comments byte-equal; `3.0.0b12` still present.
7. `PROTOCOL-LEDGER` untouched; `check_ledger.py` never run.
8. `122-VALIDATION.md` settled with every Wave 0 row individually re-verified.
9. STATE.md frontmatter hand-verified, `current_phase_name` intact.
10. The six close-ritual hand-over items recorded.

**Known pre-existing dirt — not this plan's damage:** `firestarter`: untracked `firestarter/`.
`firestarter_app`: modified `.gitignore`, untracked `.coverage`, `.planning/config.json`, `SECURITY.md`,
`write_test_port.sh`. Meta: modified `.planning/config.json` — leave it uncommitted, and never iterate
`planning.sub_repos` (it lists four repos, two of them v1.29 PY32 scratch repos outside this milestone).
</verification>

<success_criteria>
- All three CLOSE requirements are Complete, each earned clause by clause against its own prose and each
  citing the artifact that closes it.
- The phase leaves the tag, the gitlink bump, the `main` merges and the stable release cleanly to
  `/gsd-complete-milestone`, with the hand-over written down.
- The claim gate and its anti-hollow test are still green at the final commit.
- Criterion 4 is recorded honestly as half-mechanized, half-judged, and one class permanently unsampleable.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-13-SUMMARY.md`
when done. It MUST contain: the clause-by-clause evidence table, the `REQUIREMENTS.md` diff summary, all
eleven phase-end assertion results, the `122-VALIDATION.md` sign-off with any corrected Wave 0 row, the
STATE.md before/after frontmatter values, and the six-item hand-over list for `/gsd-complete-milestone`.
</output>
