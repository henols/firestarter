---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 07
type: execute
wave: 6
depends_on: ["122-04", "122-06"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md
autonomous: false
requirements: [CLOSE-03]
must_haves:
  truths:
    - "The operator explicitly authorized the push before it happened — an outward-facing, irreversible act with a recorded decision (D-05) and a green merged-tree gate behind it"
    - "The v1.22 branch is merged into `beta` with `--no-ff` in both sub-repos and pushed, and CI cuts the next beta by itself in both — no workflow trigger was edited and no `workflow_dispatch` version was hand-set (D-05)"
    - "The firmware push is verified green and cut BEFORE the app push, so a firmware CI failure cannot leave an app-only beta"
    - "The published cut tag is READ from the live release, never assumed to be `3.0.0b14` (RESEARCH A3), and is recorded as the single value every downstream step consumes"
    - "The `v1.22` tag and the meta gitlink bump are NOT created — they stay with `/gsd-complete-milestone` (D-07)"
    - "No `--force` push, no history rewrite, and no deletion of any published artifact — the stray b12 prereleases stay public (D-05)"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md"
      provides: "The observed cut record — both outbound merge SHAs, both workflow run ids, the OBSERVED tag, and the post-CI beta state"
      contains: "OBSERVED CUT TAG"
      min_lines: 40
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md"
      to: "plans 122-08, 122-09, 122-12"
      via: "the OBSERVED CUT TAG recorded here is the value consumed by `gh workflow run publish.yml -f tag=`, both `gh release edit`, and the PyPI check — never a hardcoded 3.0.0b14 (A3)"
      pattern: "OBSERVED CUT TAG"
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md"
      to: "the pushed outbound merge commits"
      via: "the decision's meta-repo commit timestamp must be strictly earlier than either push — CLOSE-03's literal ordering requirement"
      pattern: "ACCEPT"
---

<objective>
Merge the v1.22 branch out to `beta` in both sub-repos and push, letting CI cut the next beta by itself
(D-05's accepted auto-fire), then read and record the **observed** cut tag.

Purpose: CONTEXT constraints 1, 2 and 6 are all now satisfiable and provable — the accept/avoid/cleanup
decision is committed (122-02), the inbound merge is done (122-03), and the eleven nine-row commands,
both full suites, CLOSE-01's four mechanisms and both beta workflows' local gate sets are green on the
merged tree (122-04). `beta` therefore never sees an unproven tree, and the push that fires CI is a clean
merge.

Output: two outbound merge commits pushed to `beta`, two CI-cut prereleases, and
`.planning/phases/122-…/122-CUT.md` recording the observed tag.

**This plan performs the phase's one irreversible outward act.** It is `autonomous: false` and opens with
a blocking operator authorization. No comment is posted here; no release body is written here; PyPI is
not published here (that is 122-08's manual dispatch, constraint 7).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repos: **`firestarter`** and **`firestarter_app`** (both sub-repos) for the merges and pushes; **meta**
(`/workspaces`) for the `122-CUT.md` artifact. Both sub-repos start on
`v1.22-at28c-software-data-protection-lifecycle` carrying 122-03's inbound merge, and must be left on
that branch at the end.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CUT.md` (**this plan**) · `122-CHANNELS.md` (122-08) ·
`122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` +
`122-GH12-COMMENT.md` (122-10).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03); **outbound `--no-ff` merges to `beta` + pushes (this
plan)**; each repo's `beta` then receives a CI version-bump auto-commit.
**Remote state:** two `beta` refs moved · **two GitHub prereleases created by CI with empty bodies (this
plan)** · bodies added in 122-12 · one PyPI release via manual dispatch (122-08) · two issue comments
(122-12).
**Deliberately NOT produced:** no `v1.22` tag, no gitlink bump (D-07); no workflow-trigger edit and no
`paths-ignore` change (D-05); no `-f beta_version` hand-set version; no `--force` push; no history
rewrite; no deletion of the stray b12; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: BLOCKING operator authorization for the push to `beta` — the phase's one irreversible outward act</name>
  <files>none (gate only)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` — the recorded accept/avoid/cleanup decision and its pre-flight evidence, to present to the operator.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` — the merged-tree gate results, to present as the proof that `beta` will not receive an unproven tree.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Release Mechanics" (lines 433-514) — exactly what the push triggers, so the operator is told what will happen rather than asked to trust it.
    - `.planning/REQUIREMENTS.md` line 104 — CLOSE-03's literal text.
  </read_first>
  <what-built>
Everything the push depends on is already done and provable:
- The accept/avoid/cleanup decision is **committed** in `122-DECISION.md` (ACCEPT the auto-fire — the
  merge IS the cut; AVOID declined, no trigger surgery; CLEANUP declined, the stray b12 stays public).
- `origin/beta` is already merged **into** the v1.22 branch in both sub-repos, with the app's two-file
  conflict resolved whole-file `--ours` and proven byte-identical to branch HEAD by an empty diff.
- On that merged tree: the eleven nine-row cross-repo commands PASS; app pytest and both firmware suites
  are at their baselines; CLOSE-01's four mechanisms are green; and **both beta workflows' gate sets were
  pre-validated locally**, including `pio run` across all three `default_envs`, so the push should not
  fail CI before its version-bump step.
- `122-LEDGER.md` and the `PROJECT.md` EIGHTH CORRECTION are written and committed.
  </what-built>
  <action>
Present these to the operator and BLOCK until they explicitly authorize or decline:

1. **What will happen, precisely.** A `--no-ff` merge of `v1.22-at28c-software-data-protection-lifecycle`
   into `beta`, pushed, in `firestarter` first and then `firestarter_app`. Each push fires that repo's
   beta workflow (`beta-build.yml` / `beta-release.yml`), which auto-increments the version from a
   git-tag scan, auto-commits the bump onto `beta`, and creates a GitHub **prerelease** — the firmware
   one carrying three `.hex` assets, the app one carrying none. Both repos sit at `3.0.0b13` with tags up
   to `3.0.0b13`, so the derived next value is `3.0.0b14` — but the actual tag is **read after the fact**,
   never assumed (A3).
2. **What will NOT happen.** No `v1.22` tag. No meta gitlink bump. No PyPI publish (that is a separate
   manual `workflow_dispatch` in the next plan). No release body written yet — CI writes an empty body and
   the hand-written bodies are added later, after the wording review. No comment posted to either
   community issue. No workflow trigger edited. No `--force`, no history rewrite, no deletion of the
   stray b12.
3. **What is irreversible.** The push itself, the created prerelease, and the tag CI creates. Deleting any
   of them afterwards would be an outward-facing act this phase has explicitly declined.
4. **The one accepted gap.** D-01 declined a bench smoke-test of the published beta's install/flash path.
   The `pip install --pre` → `firestarter fw -i` → one-live-op chain Phase 115 existed to prove is
   trusted, not re-verified, before two community members are pointed at it. Confirm the operator still
   accepts that.
5. Show the exact argv that will be run, per repo, and the current `origin/beta` SHA in each.

If the operator declines or names a condition, do exactly what they say and record it verbatim in the
plan SUMMARY. Do not proceed on silence, and do not proceed on `auto_advance` — this gate is not
auto-approvable.
  </action>
  <acceptance_criteria>
    - The operator's verdict is captured **verbatim** in the plan SUMMARY, including any condition attached.
    - Nothing was pushed before the verdict: `git -C /workspaces/firestarter rev-parse origin/beta` and `git -C /workspaces/firestarter_app rev-parse origin/beta` still match the values recorded in `122-DECISION.md`.
    - `122-DECISION.md`'s commit exists and its ISO timestamp is strictly earlier than the current time — the ordering CLOSE-03 requires is already satisfied before the act.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has typed an explicit authorization to push to `beta` in both sub-repos, or has declined / named conditions — and the verdict is recorded verbatim in the plan SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "authorized" to proceed with both pushes, or name the condition / decline.</resume-signal>
  <done>The operator has explicitly authorized (or declined) the push, the verdict is recorded verbatim, and nothing has been pushed up to this point.</done>
</task>

<task type="auto">
  <name>Task 2: Outbound `--no-ff` merge to `beta` and push — firmware first, verified cut, then the app</name>
  <files>none in-tree (git refs only: firestarter `beta`, firestarter_app `beta`)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` §the accepted sequence — steps 4 and its constraint mapping.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Release Mechanics" → "What the merge push actually triggers" and "Gates each beta workflow runs" (lines 435-460) — the trigger table and the per-workflow gate sequence, so a red run is diagnosed rather than retried blindly.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Common Pitfalls" → Pitfall 3 (lines 908-913) — local `beta` goes stale immediately after CI because `git-auto-commit-action` pushes the version bump.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-03-SUMMARY.md` — the inbound merge SHAs that are about to be merged out.
  </read_first>
  <action>
Do the firmware repo completely, verify its cut, and only then do the app repo. Sequencing them means a
firmware CI failure cannot leave an app-only beta published.

**Firmware — `/workspaces/firestarter`:**
1. `git fetch origin`. Confirm `origin/beta` is still at the value recorded in `122-DECISION.md`; if it
   moved, STOP and re-run the dry-run probe before doing anything else.
2. `git checkout beta` then `git reset --hard origin/beta` **only if** local `beta` is behind and has no
   local commits — verify with `git log origin/beta..beta --oneline` being empty first. If local `beta`
   carries any commit not on `origin/beta`, STOP and record it; do not force anything.
3. `git merge --no-ff --no-edit v1.22-at28c-software-data-protection-lifecycle`. Expect a clean merge —
   the inbound merge in 122-03 already absorbed everything `beta` had.
4. `git push origin beta`. **Plain push only.** No `--force`, no `--force-with-lease`, no
   `--follow-tags`, no tag push.
5. Watch the workflow: `gh run list --repo henols/firestarter --workflow beta-build.yml --limit 3` then
   `gh run watch <id>` (or poll `gh run view <id>`) until it concludes. Record the run id and conclusion.
   If it fails, record which step failed and STOP — do not push the app repo, and do not retry blindly.
   The gate sequence is catalog validity → codegen drift on `include/messages.h` → `pio test -e native` →
   firmware `pytest tests/` → version bump + auto-commit → `pio run` → Release; 122-04 pre-validated all
   of those locally, so a failure here is new information.
6. Confirm the cut: `gh release list --repo henols/firestarter --limit 3` and record the newest tag as
   observed. Do **not** type an expected value into the record — read it.
7. `git fetch origin` again and record that local `beta` is now 1 commit behind `origin/beta` (the CI
   version-bump auto-commit). Do not merge or reset it; just record the fact.
8. `git checkout v1.22-at28c-software-data-protection-lifecycle` — leave the repo on the milestone
   branch so no later plan operates on `beta` by accident.

**App — `/workspaces/firestarter_app`:** repeat steps 1-8 with `beta-release.yml` and
`--repo henols/firestarter_app`. Its gate sequence is `pip install -e .[test]` → catalog validity →
codegen drift on `firestarter/messages.py` (ruff-normalized, then `git diff --exit-code`) → `pytest
tests/` → version bump + auto-commit → Release. Note that the app release carries **zero assets** (C-7) —
PyPI is the app's sole distribution channel, and PyPI has not been published at this point.

Absolute prohibitions for this task: do **not** create any tag by hand; do **not** create or bump the
meta-repo gitlinks; do **not** edit any workflow file or trigger; do **not** pass `-f beta_version` to
anything; do **not** delete or edit any existing release, including the stray b12; do **not** run
`gh workflow run publish.yml` here (that is 122-08, constraint 7); do **not** post any issue comment; do
**not** write any release body.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter log origin/beta --oneline -3` shows the outbound merge commit, and `git -C /workspaces/firestarter branch --contains <inbound merge SHA> -r` lists `origin/beta`.
    - Same for `firestarter_app`.
    - Both beta workflow runs concluded **success**, and both run ids are recorded.
    - `gh release list --repo henols/firestarter --limit 3` shows a new prerelease newer than `3.0.0b13`, and the same for `henols/firestarter_app`; both observed tags are recorded verbatim.
    - `gh release view <observed tag> --repo henols/firestarter --json assets` reports exactly **3** assets named `firestarter_leonardo.hex`, `firestarter_uno.hex`, `firestarter_uno328pb.hex`.
    - `gh release view <observed tag> --repo henols/firestarter_app --json assets` reports **0** assets (expected — C-7).
    - `gh release list --repo henols/firestarter --limit 20` still contains `3.0.0b12`, and the same for the app repo — nothing was deleted.
    - No tag was created locally by hand: `git -C <each repo> log --oneline -1` on the milestone branch is unchanged from 122-03's merge SHA, and no `git tag` command was run.
    - Both repos are left on `v1.22-at28c-software-data-protection-lifecycle`: `git -C <each> rev-parse --abbrev-ref HEAD` confirms it.
    - `git -C /workspaces ls-tree HEAD firestarter firestarter_app` still reports `0048b3d…` / `96e0622…` — no gitlink bump (D-07, A5).
    - `git -C /workspaces status --porcelain -- firestarter firestarter_app` is empty — no submodule pointer staged.
    - No workflow file was modified in either repo: `git -C <each> diff origin/beta --name-only -- .github/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && for r in firestarter firestarter_app; do test "$(git -C $r rev-parse --abbrev-ref HEAD)" = "v1.22-at28c-software-data-protection-lifecycle" || { echo "WRONG BRANCH in $r"; exit 1; }; done && gh release list --repo henols/firestarter --limit 20 | grep -q '3.0.0b12' && gh release list --repo henols/firestarter_app --limit 20 | grep -q '3.0.0b12' && test "$(gh release view "$(gh release list --repo henols/firestarter --limit 1 --json tagName -q '.[0].tagName')" --repo henols/firestarter --json assets -q '.assets|length')" = "3" && git ls-tree HEAD firestarter firestarter_app | grep -q 0048b3d && echo OUTBOUND-PUSH-AND-CUT-OK</automated>
  </verify>
  <done>Both outbound merges are pushed to `beta`, both beta workflows concluded success, both prereleases exist (firmware with three `.hex` assets, app with none), the stray b12 is untouched, no tag or gitlink was created by hand, no workflow file was edited, and both repos are back on the milestone branch.</done>
</task>

<task type="auto">
  <name>Task 3: Record the observed cut in 122-CUT.md as the single tag value every downstream step consumes</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Assumptions Log" A3 (line 1324) — why the tag must be read rather than assumed, and what could have changed it.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Why the cut is `b14` (D-04)" (lines 495-501) — the derivation, so the observed value can be compared against the expectation and any divergence explained.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` — the ISO commit timestamp needed for the CLOSE-03 ordering proof.
  </read_first>
  <action>
Write `122-CUT.md` in the meta phase directory. Its job is to be the one place every later step reads the
tag from, so no plan hardcodes a version.

Record, each as a labelled line or table row:
- **OBSERVED CUT TAG (firmware)** — read from `gh release list --repo henols/firestarter`.
- **OBSERVED CUT TAG (app)** — read from `gh release list --repo henols/firestarter_app`.
  State explicitly whether the two match. If they diverge, that is a finding to record and surface, not
  a value to reconcile by editing anything.
- The **expected** derived value and whether the observation matched it. D-04's derivation is: the merged
  version file reads `3.0.0b13` → base `3.0.0` → `git tag --list '3.0.0b*'` → max 13 → next 14. If the
  observation differs, record the observation as authoritative and explain the difference (a concurrent
  push, a gate failing before the version step, or a manual `beta_version` input are the named
  possibilities).
- Both outbound merge commit SHAs on `beta`, and both CI version-bump auto-commit SHAs.
- Both workflow run ids, their conclusions, and their URLs.
- The firmware prerelease's three asset names, and the app prerelease's asset count (0).
- Both prerelease bodies' current length — expected **0** in both, because CI passes no `body` and no
  `generate_release_notes` (C-6). This makes the later body edit an *add*, not an overwrite; record that
  reading.
- `isPrerelease` true for both.
- The post-CI divergence fact: local `beta` is 1 commit behind `origin/beta` in each repo, and any later
  local `beta` operation must `git fetch` first (Pitfall 3).
- The **CLOSE-03 ordering proof**: `122-DECISION.md`'s commit SHA and ISO timestamp alongside the two
  push timestamps (or the two outbound merge commit dates), showing the decision strictly precedes both.
- An explicit statement that PyPI has **not** been published at this point, that no release body has been
  written, and that no community comment has been posted.
- An explicit statement that the `v1.22` tag and the meta gitlink bump were **not** created (D-07), with
  the gitlink values still at `0048b3d` / `96e0622`.

Add a short `## How downstream steps consume this` block naming the three consumers by plan: 122-08's
`gh workflow run publish.yml --repo henols/firestarter_app -f tag=<OBSERVED CUT TAG (app)>`, 122-09's
release-note prose (which must not hardcode a different version), and 122-12's two
`gh release edit <OBSERVED CUT TAG> --repo <r> --notes-file <path>` calls plus the PyPI check. State that
**no downstream step may hardcode `3.0.0b14`** — each reads this file.
  </action>
  <acceptance_criteria>
    - `122-CUT.md` exists, is ≥40 lines, and contains the literal label `OBSERVED CUT TAG` at least twice (firmware and app).
    - Both observed tags are recorded verbatim as read from `gh`, and the artifact states whether they match.
    - Both workflow run ids and conclusions are recorded; both conclusions are `success`.
    - Both prerelease body lengths are recorded as observed (expected 0) with the "so the body edit is an add" reading.
    - The firmware asset list has three `.hex` names; the app asset count is 0.
    - The CLOSE-03 ordering proof is present: `122-DECISION.md`'s commit timestamp is shown to be strictly earlier than both push/merge timestamps.
    - The artifact states that PyPI is not yet published, no body written, no comment posted, no `v1.22` tag, no gitlink bump.
    - The `## How downstream steps consume this` block names all three consuming plans and forbids hardcoding a version.
    - `grep -c 'OBSERVED CUT TAG' 122-CUT.md` ≥ 2.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && C=122-CUT.md && test -f "$C" && test "$(wc -l < $C)" -ge 40 && test "$(grep -c 'OBSERVED CUT TAG' $C)" -ge 2 && grep -q 'publish.yml' $C && grep -q '0048b3d' $C && echo CUT-RECORD-OK</automated>
  </verify>
  <done>`122-CUT.md` records both observed tags, both run ids and conclusions, the asset inventories, the empty-body observation, the CLOSE-03 ordering proof, and the explicit not-yet-done list; every downstream consumer is named and forbidden from hardcoding a version.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local merged tree → `origin/beta` → public prerelease | The one irreversible outward crossing in the phase; everything downstream of it is public |
| derived expectation → executed reality | The cut tag is computed by CI from a tag scan; assuming its value would make three later steps target a release that may not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-04 | Repudiation | irreversible outward action without consent | critical | mitigate | Blocking operator authorization gate (Task 1), not auto-approvable; `122-DECISION.md` committed in Wave 1 with the ordering proven by timestamp in Task 3 |
| T-122-03 | Tampering | publishing an artifact built from an unproven tree | high | mitigate | Constraints 2 and 6: the eleven nine-row commands, both full suites, CLOSE-01's four mechanisms and both beta workflows' gate sets all green on the merged tree (122-04) before this push |
| T-122-07 | Tampering | GitHub Actions workflow configuration | high | mitigate | No workflow file edited; asserted by `git diff origin/beta --name-only -- .github/` being empty in both repos. No `-f beta_version` hand-set version (D-05's declined AVOID option) |
| T-122-20 | Tampering | published artifacts (the stray b12) | high | mitigate | Plain `git push` only — no `--force`, no `--force-with-lease`, no history rewrite; `gh release list` asserted to still contain `3.0.0b12` in both repos |
| T-122-21 | Tampering | targeting the wrong release downstream | medium | mitigate | A3: both tags read from `gh` and recorded in `122-CUT.md`; every downstream consumer named and explicitly forbidden from hardcoding a version |
| T-122-13 | Tampering | meta-repo submodule gitlinks / `v1.22` tag | medium | mitigate | D-07: neither created; gitlinks asserted still `0048b3d` / `96e0622`; no submodule pointer staged |
| T-122-01 | Information Disclosure | secrets consumed by CI | high | mitigate | `PERSONAL_ACCESS_TOKEN`, `GITHUB_TOKEN` and `PYPI_API_TOKEN` are consumed by workflows only; never run `gh auth token`, never echo a value, never add a step printing a workflow env dump, never paste a run log containing one into an artifact |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero local installs in phase scope; CI's own `pip install -e .[test]` runs inside the workflow and is not a phase action (`122-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
1. The operator's authorization is recorded verbatim; nothing was pushed before it.
2. Both outbound merges are on `origin/beta`; both beta workflows concluded success; both run ids
   recorded.
3. Firmware prerelease carries exactly three `.hex` assets; app prerelease carries zero (C-7).
4. `3.0.0b12` still present in both repos' release lists — nothing deleted (D-05).
5. No workflow file modified in either repo; no `-f beta_version` used; no tag created by hand.
6. Both repos left on `v1.22-at28c-software-data-protection-lifecycle`.
7. Meta gitlinks still `0048b3d` / `96e0622`; no submodule pointer staged (D-07, A5).
8. `122-CUT.md` records both observed tags, and the CLOSE-03 ordering proof shows the decision commit
   strictly precedes both pushes.
9. PyPI is **not** published; no release body written; no comment posted.

**Do not chase:** `catalog-sync-check.yml` stays red until v1.22 reaches `main`
(`/gsd-complete-milestone`'s job) because it checks out both sub-repos at `ref: main`; firmware
`build.yml` and app `ci.yml` do not run on a `beta` push at all.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-03 closes only in 122-13, after both channels
are verified.
</verification>

<success_criteria>
- The push happened with explicit, recorded operator authorization, after a green merged-tree gate, and
  after a committed decision.
- Both prereleases exist and were cut by CI's own auto-increment — no trigger surgery, no hand-set
  version.
- The observed tag is recorded once and is the only value any downstream step consumes.
- Nothing published was deleted; no tag or gitlink was created.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-07-SUMMARY.md`
when done. Record the operator's verdict verbatim, both observed tags, both run ids and conclusions, the
asset inventories, the empty-body observation, and the CLOSE-03 ordering timestamps.
</output>
