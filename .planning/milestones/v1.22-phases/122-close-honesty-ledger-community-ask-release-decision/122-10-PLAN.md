---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 10
type: execute
wave: 8
depends_on: ["122-05", "122-06", "122-08"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md
autonomous: true
requirements: [CLOSE-02]
must_haves:
  truths:
    - "gh#11's reply gives the full account, names BOTH defects, explains why the reporter's re-test looked worse than 2024 without being a regression, and credits their reproduction (D-15)"
    - "gh#11's ask is a plain write plus verify FIRST, with `dev test` offered SECOND and optional, carrying the always-writes warning in substance (D-02, inherited from Phase 121 D-04)"
    - "gh#12's reply answers the maintainer's own unanswered 2024 design question with the decided auto-unlock policy, and the issue is left OPEN — closing it would read as a verified fix (D-13)"
    - "gh#12's three drifted participants are answered inline; nothing is split into a new issue (D-14)"
    - "D-14's prescribed `No-Hazmats` wording is CORRECTED, not inherited: the answer is phrased by size class and states the measured refusal (RESEARCH C-5), and the divergence is recorded as a flagged item for the operator review — not applied silently and not dropped silently"
    - "No unverified fact reaches a public reply: the `pdr0663` firmware version and exception name are either re-confirmed from the full comment body or dropped entirely (RESEARCH A1)"
    - "Neither draft is posted by this plan; delivery is `gh issue comment --body-file <committed path>` in 122-12, after the D-16 blocking wording review"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md"
      provides: "The gh#11 follow-up draft — full account, credit, and the re-test ask"
      contains: "no AT28C silicon was tested"
      min_lines: 30
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md"
      provides: "The gh#12 reply draft — the decided auto-unlock policy plus three inline answers"
      contains: "no AT28C silicon was tested"
      min_lines: 30
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md"
      to: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md"
      via: "the `No-Hazmats` answer restates the ledger's measured 19-of-19 `DIP24_2816` refusal by size class; the ledger's mechanism-corrections section carries the flagged D-14 divergence"
      pattern: "2K"
    - from: "both drafts"
      to: "gh issue comment --body-file (plan 122-12)"
      via: "delivery re-reads the committed file, which is what proves posted == reviewed (D-16)"
      pattern: "body-file"
---

<objective>
Write the two community comment drafts as committed files: a follow-up on gh#11 and a reply on gh#12.
Both are outward-facing text addressed to strangers, in the one phase whose entire job is not
overclaiming.

Purpose: CLOSE-02. gh#12 must be answered with the decided auto-unlock policy — which answers its
reporter's own unanswered 2024 design question — and gh#11 must be followed up, both framed as *"here is
what changed and why we believe it addresses your report; please re-test"*, **never as a verified fix**.

Output: `122-GH11-COMMENT.md` and `122-GH12-COMMENT.md`, both in the meta phase directory.

**Nothing is posted by this plan.** The drafts land in the phase record first so what was said is
auditable against the ledger; the D-16 blocking operator wording review is plan 122-11; delivery via
`gh issue comment --body-file <committed path>` is plan 122-12, which is what makes the posted text
provably equal to the reviewed text.

**One locked decision is being deliberately diverged from.** D-14 prescribes telling `No-Hazmats` their
AT28C parts should now work. That is measured false — see Task 2 and Task 3. The divergence is applied
*and flagged*, so the operator review can accept or overturn it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. Both issues live in `henols/firestarter_prom` — the project-wide
tracker — not in `firestarter_app`.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md
@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CUT.md` (122-07) · `122-CHANNELS.md` (122-08) ·
`122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` +
`122-GH12-COMMENT.md` (**this plan**).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03); outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs (122-07) · two prereleases, bodies added in 122-12 · one PyPI release
(122-08) · **two `gh issue comment` calls on `henols/firestarter_prom` #11 and #12 — delivered in 122-12
from the files this plan writes.**
**Deliberately NOT produced:** no issue closed (D-13 — both stay OPEN); **no `--label` / `--add-label` /
`-l` flag ever sent** (both commands abort on a missing label and neither issue carries any); no new issue
opened (D-14 declined splitting `pdr0663`'s crash out); no `v1.22` tag, no gitlink bump (D-07); no stable
release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the gh#11 follow-up draft — both defects named, the apparent regression explained, the reproduction credited, and the re-test ask</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Community Thread Ground Truth" → gh#11 (lines 647-675) — the 2024 symptom verbatim (including the 339.49 s success line), the confirmation that the input file was 32 Kb of random data, the rev-2 mod, and the live comments 10-12 ending in *"That's fine, happy to test for you."* Also the practical note that recent comments arrive by email reply and carry quoted signature blocks — post clean markdown, do not mirror that formatting.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §D-15 and §D-02 — the required content and the inherited always-writes obligation.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md` (122-05) — the claim classes; author every claim from its permitted-wording column.
    - `.planning/PROJECT.md` FOURTH CORRECTION item 2 — gh#11's cause is a **conflation** bug, not a sampling-rate bug. State it that way.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CUT.md` and `122-CHANNELS.md` — the observed tag and the verified install path the ask points at.
    - `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-CONTEXT.md` §D-04 — `dev test` always writes and expects a blank or scratch part; the warning this reply must carry.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (122-01) — the forbidden set and the canonical caveat fragment.
  </read_first>
  <action>
Write `122-GH11-COMMENT.md` as the literal body of one GitHub issue comment. Clean markdown, addressed to
the reporter, no GSD-internal vocabulary (no `D-NN`, no `C-NN`, no requirement ids, no plan ids), no
quoted email signature blocks.

Cover, in this order:

1. **Thanks, specifically for the reproduction.** Their 2026-07-27 re-test on real AT28C256 hardware turned
   a prediction made from reading the source into a confirmed defect. Say that plainly and say why it
   mattered: nobody on the project has an AT28C part, so that paste is the only real-silicon evidence this
   work has. Acknowledge their *"happy to test for you"* offer directly.

2. **What was actually wrong — two separate defects, not one.**
   - The command emitter. The SDP-disable sequence the firmware has always sent on a 28C write went through
     a shared helper that bypassed the address-bus remap, so on 28C-family pinouts at least one command
     write was emitted with the write-enable line held HIGH — a documented Write Inhibit. Its success check
     was also inverted: both datasheets state the command bytes are not written to the device, so the check
     could only pass when the sequence had **not** been recognised.
   - The page verification. This is the one more likely to explain what they actually saw in 2024.
     Per-page write verification sampled a single byte per 64-byte page, so a partial write could report
     success. Describe it as a **conflation** defect — completion polling and readback verification were
     the same check — not as a sampling-rate tweak. It is now per-byte, with the failing address named, and
     the two concerns are separate functions.

3. **Why their re-test looked worse than 2024, and why it wasn't a regression.** Quote back the shape of
   their two observations: in 2024 the write *completed*, reported success after 339 seconds, and only part
   of the image was actually burned; on the previous beta it hard-fails at INIT with the timeout at
   `0x005555`. That is the first fix landing halfway — the inverted check turned a silent partial write
   into an honest refusal. Say explicitly that reading the beta as worse than what they had is a reasonable
   reading and is not what happened.

4. **What is in this beta, and how to get it.** `pip install --pre --upgrade firestarter` for the host, and
   `firestarter fw --install` for the matching firmware; note the three board `.hex` files are also
   attached to the firmware release. Note their board is a rev-2-modded shield per their own earlier
   report, and that the firmware they need is the one matching their board.

5. **What is proven and what is not.** In substance: the lock and unlock sequences are emitted exactly as
   specified, verified byte-exact by golden register trace across all four 28C-family pinouts, with a
   measured host-side timing figure. Then, plainly: on this chip family the protection state cannot be read
   back afterward, so a successful run means only that the command sequence was **emitted**, nothing more —
   and the caveat in the project's canonical wording: **no AT28C silicon was tested.** Add that the 28C
   protocol bucket is still recorded as UNVERIFIED in the project's own validation ledger, and that this is
   exactly why their re-test would be valuable rather than a formality.

6. **The ask, in two parts, in this order.**
   - **First, the plain path:** `firestarter write at28c256 <file>` followed by `firestarter verify
     at28c256 <file>`. This answers the one question that matters — does the INIT abort at `0x005555` still
     happen — with no new command to learn. Note that on this family a non-blank part needs `-b`, because
     the 28C family has no erase operation and each page write auto-erases internally.
   - **Second, optional:** `firestarter dev test at28c256`, which runs a full capability sweep and offers to
     file a structured report. Carry the warning in substance and prominently, before the invitation:
     **`dev test` always writes to the chip and expects a blank or scratch part.** Then note, because their
     own 2024 report says the input was 32 Kb of random data, that a full-device write is safe on *that*
     part specifically — but they should not run it on anything they care about. Note the command takes no
     options.
   - Say that either one on its own is useful and that they should not feel obliged to do both.

7. **What happens next.** The issue stays open pending a silicon re-test — it is not being closed as fixed,
   because nothing here has been confirmed against a real part. Invite the result either way, including a
   result that still fails.

Hard constraints: never use a phrase from the claim gate's forbidden set (run the gate — do not guess);
never state or imply that SDP works on an AT28C256; **do not** write a negated phrasing built on the
`silicon-verified` string — the gate flags it and the canonical caveat wording says the same thing better;
include no absolute path beginning `/workspaces/`, no `/dev/tty` device name, no username other than the
GitHub handles already public in the thread, and no token name; do not paste any local traceback or
workstation output; and do not hardcode a version string other than the observed app tag from
`122-CUT.md` (prefer `pip install --pre --upgrade`, which needs no version).
  </action>
  <acceptance_criteria>
    - `122-GH11-COMMENT.md` exists, is ≥30 lines, and contains the canonical caveat fragment `no AT28C silicon was tested`.
    - It names both defects and describes the page-verification one as a conflation, not a sampling-rate, defect.
    - It contains the 2024-versus-beta explanation including the `0x005555` address and the words that this is not a regression.
    - It contains, in order, the plain `write` + `verify` ask **before** the `dev test` offer: the line number of the first `verify` mention is less than the line number of the first `dev test` mention.
    - It states, in substance and before the `dev test` invitation, that `dev test` always writes to the chip and expects a blank or scratch part.
    - It states the issue stays open pending a silicon re-test.
    - `grep -c 'all 84' 122-GH11-COMMENT.md` returns **0**.
    - `grep -cE '/workspaces/|/dev/tty|PERSONAL_ACCESS_TOKEN|GITHUB_TOKEN|PYPI_API_TOKEN' 122-GH11-COMMENT.md` returns **0**.
    - `grep -cE 'D-[0-9]{2}|C-[0-9]{1,2}\b|CLOSE-0|FIX-0|LOCK-0|OBS-0|HOST-0|DEVTEST-0|TRACE-0' 122-GH11-COMMENT.md` returns **0**.
    - No local traceback, no `Traceback (most recent call last)`, and no line beginning `PS D:\` copied from the reporter's own paste as if it were new output.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && G=122-GH11-COMMENT.md && test "$(wc -l < $G)" -ge 30 && grep -qi 'no AT28C silicon was tested' $G && grep -q '0x005555' $G && grep -qi 'always writes' $G && test "$(grep -n 'verify' $G | head -1 | cut -d: -f1)" -lt "$(grep -n 'dev test' $G | head -1 | cut -d: -f1)" && test "$(grep -c 'all 84' $G)" = "0" && test "$(grep -cE '/workspaces/|/dev/tty|PERSONAL_ACCESS_TOKEN|GITHUB_TOKEN|PYPI_API_TOKEN' $G)" = "0" && test "$(grep -cE 'D-[0-9]{2}|CLOSE-0|FIX-0|LOCK-0|HOST-0|DEVTEST-0|TRACE-0' $G)" = "0" && echo GH11-DRAFT-OK</automated>
  </verify>
  <done>The gh#11 draft exists as clean reader-facing markdown covering all seven points, credits the reproduction, names both defects with the conflation framing, explains the apparent regression, puts the plain ask before the `dev test` offer with the always-writes warning ahead of it, carries the canonical caveat, and leaks no internal vocabulary, path, device name or token.</done>
</task>

<task type="auto">
  <name>Task 2: Write the gh#12 reply draft — the decided policy, three inline answers, and the CORRECTED No-Hazmats wording</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Community Thread Ground Truth" → gh#12 (lines 677-727) — the maintainer's own unanswered 2024 question verbatim, the three drifted participants' actual words in table form with their honest dispositions, and the shipped `dev sdp --help` text to reuse.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Corrections" row **C-5** (line 276) — the measured 0-of-19 refusal that makes D-14's prescribed wording an overclaim, and the reasons the refusal is correct.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Assumptions Log" **A1** (line 1322) — the two `pdr0663` facts that are NOT confirmed and the two acceptable responses.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §D-13 and §D-14 — the required content and the declined alternatives (no close, no label, no split-out issue).
    - `.planning/REQUIREMENTS.md` §"Future Requirements" SDP-F1, SDP-F6, SDP-F7, SDP-F8 (lines 112-123) — the deferral reasons this reply states in one line each.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md` (122-05) — the measured 43/41 split, the pinout composition, and the flagged D-14 divergence already recorded there.
  </read_first>
  <action>
Write `122-GH12-COMMENT.md` as the literal body of one GitHub issue comment. Clean markdown, no
GSD-internal vocabulary.

Cover, in this order:

1. **Answer the 2024 question that was never answered.** The question was the maintainer's own — *"Always
   unlock, write and lock the chip again? A special command for unlocking and locking? What are the use
   cases?"* — and the reporter never came back, so answer it with the decided policy:
   - The automatic unlock before a write stays **on by default**, and is now **reported** rather than
     silent — one line before the sequence and one after.
   - `firestarter write --skip-sdp-unlock` declines it.
   - `firestarter dev sdp <chip> enable|disable` gives standalone control in both directions, chip first,
     mode second.
   - An opt-in post-write re-lock is **deferred**, with the reason stated: an unconditional re-lock step
     inside a conditional pipeline is a hazard, and a re-lock after a failed verify would strand the user
     with a locked chip they cannot retry on.

2. **What was wrong underneath, briefly.** The sequence the firmware had always emitted almost certainly
   never reached silicon — a shared helper bypassed the address-bus remap, so at least one command write
   went out with the write-enable line held HIGH, and the success check was inverted. Both are fixed in
   this beta. Keep this short; gh#11 carries the long version and can be linked.

3. **The capability boundary, stated before anyone assumes it applies to their part.** The SDP commands are
   refused on 28C-bucket parts that have no SDP command decoder at all — the two FRAM parts and the pre-SDP
   `2804`/`2816`/`2817` generation. Of the 84 parts in this protocol bucket, **43 are allowed and 41 are
   refused**. Explain why refusing is correct rather than a gap: on a part with no decoder the command
   bytes are not inert, they would be stored as data at truncated addresses.

4. **What is proven and what is not.** In substance: the lock and unlock sequences are emitted exactly as
   specified, verified byte-exact by golden register trace across all four 28C-family pinouts, with a
   measured host-side timing figure. Then the shipped help text's own wording in substance — on this chip
   family the resulting protection state cannot be read back afterward, so neither direction can be
   confirmed; a successful run means only that the command sequence was **emitted**, nothing more —
   followed by the canonical caveat: **no AT28C silicon was tested.**

5. **Three inline answers to the people who arrived later. Nothing is split into a new issue.**
   - **The SST39SF request (2024).** Chip-erase and byte-program sequences for that family are **deferred**,
     with the reason: it multiplies the no-silicon problem across families that currently have settled bench
     evidence. Then answer the concern that participant flagged as most important, because it is already
     satisfied and worth saying: this protocol family is 5 V-only and the firmware never asserts the
     programming-voltage regulator on it, so there is no risk of programming voltage reaching a pin on
     these parts.
   - **The 2025 AT28C256 write failure.** Frame it by what is confirmed: the log header shows the host app
     at version 1.3.44 on Windows 11, and the debug dump uses a command field that was removed from the
     wire protocol in a later release — so the report predates the transport rewrite that replaced that
     whole layer. Invite a fresh report if it still reproduces on this beta. **Do not name the firmware
     version and do not name the exception class** unless Task 3's re-retrieval confirms them from the full
     comment body; the retrievable body is truncated exactly where both would appear. The pre-3.0 framing
     stands on the app version alone.
   - **The 2K×8 part request (2026) — this is the CORRECTED answer.** D-14 prescribed saying their AT28C
     parts should now work. That is measured false and must not be posted. Every 2K×8 part in this protocol
     bucket sits on the 24-pin `2816`-class pinout, and **all 19 of those 19 parts are on the refused
     side** of the allow-set — 7 as pre-SDP generation, 12 as unrecognised. So: phrase the answer **by size
     class, not by an assumed part number** (they never named one); state plainly that a 2K×8 part in this
     family is not in the SDP allow-set, that this beta's SDP work therefore does not apply to it, and that
     whatever they hit is not the write-protection symptom this issue is about; note that the datasheet
     verification for that specific generation and the socket-level pin question for that pinout are both
     recorded as open and deferred; and note that another participant already suggested an alternative part
     to them in 2026. Offer to look at their actual failure if they can say what part they have and what
     the command output was — that is a real offer, not a deflection.

6. **What happens next.** The issue **stays open** pending a silicon re-test — closing it would read as
   *fixed and confirmed*, and nothing here has been confirmed against a real part. Point at the beta:
   `pip install --pre --upgrade firestarter` plus `firestarter fw --install`.

Hard constraints: never use a phrase from the claim gate's forbidden set (run the gate — do not guess);
never state or imply that SDP works on an AT28C256; **never** pair "AT28C" with a works-now phrasing under
any qualifier — that specific sentence shape is the overclaim this task exists to prevent; do not write a
negated phrasing built on the `silicon-verified` string; include no absolute path beginning `/workspaces/`,
no `/dev/tty` device name, no token name; assert **no** label flag anywhere in the eventual delivery; and
do not close the issue.
  </action>
  <acceptance_criteria>
    - `122-GH12-COMMENT.md` exists, is ≥30 lines, and contains the canonical caveat fragment `no AT28C silicon was tested`.
    - It contains all four policy elements: default-on-and-reported auto-unlock, `--skip-sdp-unlock`, `dev sdp <chip> enable|disable`, and the deferred re-lock with its stated reason.
    - It contains the measured 43 / 41 split and names the pre-SDP `2804`/`2816`/`2817` generation and the two FRAM parts as refused.
    - It contains three clearly separated inline answers, and the 2K×8 answer states the refusal explicitly, is phrased by size class, and contains no assumed part number.
    - `grep -ciE 'AT28C[^.]{0,80}(should )?now work' 122-GH12-COMMENT.md` returns **0** — the D-14 overclaim shape is absent.
    - It states the issue stays open and does not ask for it to be closed.
    - The firmware version `1.4.2` and the string `ClearCommError` appear **only** if Task 3 recorded them as re-confirmed from the full comment body; otherwise both are absent.
    - `grep -c 'all 84' 122-GH12-COMMENT.md` returns **0**.
    - `grep -cE '/workspaces/|/dev/tty|PERSONAL_ACCESS_TOKEN|GITHUB_TOKEN|PYPI_API_TOKEN' 122-GH12-COMMENT.md` returns **0**.
    - `grep -cE 'D-[0-9]{2}|C-[0-9]{1,2}\b|CLOSE-0|SDP-F[0-9]|HOST-0|DEVTEST-0' 122-GH12-COMMENT.md` returns **0** — the deferral reasons are stated in plain language, never by internal id.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && G=122-GH12-COMMENT.md && test "$(wc -l < $G)" -ge 30 && grep -qi 'no AT28C silicon was tested' $G && grep -q 'skip-sdp-unlock' $G && grep -q 'dev sdp' $G && grep -q '43' $G && grep -q '2816' $G && test "$(grep -ciE 'AT28C[^.]{0,80}(should )?now work' $G)" = "0" && test "$(grep -c 'all 84' $G)" = "0" && test "$(grep -cE '/workspaces/|/dev/tty|PERSONAL_ACCESS_TOKEN|GITHUB_TOKEN|PYPI_API_TOKEN' $G)" = "0" && test "$(grep -cE 'D-[0-9]{2}|CLOSE-0|SDP-F[0-9]|HOST-0|DEVTEST-0' $G)" = "0" && echo GH12-DRAFT-OK</automated>
  </verify>
  <done>The gh#12 draft answers the 2024 design question with all four policy elements, states the capability boundary with measured numbers, answers all three drifted participants inline without splitting anything out, carries the corrected size-class answer with the refusal stated and the D-14 overclaim shape provably absent, and leaks no internal vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the one unverified factual claim, gate both drafts, and record the D-14 divergence as a flagged item for the operator review</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Assumptions Log" A1 (line 1322) — exactly which two facts are unconfirmed, what IS confirmed, and the two acceptable mitigations.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Corrections" row C-5 (line 276) — the measurement behind the divergence.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (122-01) — the forbidden labels, the canonical caveat, and the env seam's list form.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md` §"Mechanism corrections recorded here, not in REQUIREMENTS.md" — where the divergence is already recorded, so this task cross-references rather than duplicates.
    - Both drafts written in Tasks 1 and 2.
  </read_first>
  <action>
Three passes, none of which posts anything.

Pass 1 — **resolve A1, the only unverified factual claim heading into a public reply.** The 2025 comment on
gh#12 is truncated in the retrievable body exactly where the firmware version and the exception class name
would appear. Make one focused attempt to retrieve the full body: list the issue's comments with
`gh api repos/henols/firestarter_prom/issues/12/comments --paginate` and read that specific comment's
`body` from the per-comment payload (the per-comment endpoint returns the full body where a summarised view
may not). Record the outcome in the SUMMARY as one of exactly two dispositions:
- **CONFIRMED** — both the firmware version and the exception class are visible verbatim in the retrieved
  body. Quote the substring that shows them. Only then may the draft name either fact.
- **NOT CONFIRMED** — either fact is still absent or ambiguous. Then **delete both from the draft** and
  keep the pre-3.0 framing on the confirmed app version alone, which is sufficient. This is the default,
  and it is not a loss: the honest framing does not need either fact.
Never assert either fact on the strength of a prior document's paraphrase. Do not open a browser-dependent
step as a blocker — if the API attempt does not resolve it, take the NOT CONFIRMED path and note that the
operator may add either fact at the wording review if they can see it in their browser.

Pass 2 — **the gate.** Run the Wave 0 scanner over both drafts via the env seam, both paths joined by
`os.pathsep`, and require exit 0 with a PASS line naming **both** files. If it flags a phrase, reword the
draft; never weaken the pattern set, never add an exclusion, never move a file out of the default target
list.

Then a second, draft-specific negative check the scanner does not cover: assert the D-14 overclaim *shape*
is absent from both drafts by searching case-insensitively for "AT28C" within a short window of a
works-now phrasing. This is a shape check, not a phrase check, and it is the specific harm C-5 identified.

Pass 3 — **record the flagged divergence, prominently, for the D-16 operator review.** Write into the
SUMMARY a clearly-headed block containing:
- **What CONTEXT D-14 said**, quoted: the 2K×8 reporter's AT28C parts should now work.
- **What was measured**: all 19 of 19 parts on the 24-pin `2816`-class pinout are refused by the SDP
  allow-set (7 as pre-SDP generation, 12 as unrecognised), the shipped `dev sdp --help` text says so
  itself, and the conclusion holds for any 2K×8 part they own because every 2K×8 entry in the bucket sits
  on that pinout.
- **What this plan did**: phrased the answer by size class, stated the refusal plainly, and noted the two
  open deferrals for that generation.
- **What the operator is being asked to do**: accept the divergence or overturn it at the 122-11 wording
  review. Frame it as an explicit decision, not as a fait accompli, and note it is already recorded in
  `122-LEDGER.md`'s mechanism-corrections section and in `PROJECT.md`'s EIGHTH CORRECTION item 3.

Also record in the SUMMARY, as a delivery pre-brief for 122-12: both issues **stay OPEN**; delivery is
`gh issue comment <n> --repo henols/firestarter_prom --body-file <committed path>` with no `--label`, no
`--add-label`, no `-l`, no `--web`, no `--editor` — those flags either abort on a missing label or open an
interactive surface; and neither issue carries any label today, so a label flag would abort before the
comment was created.

Do **not** run `gh issue comment` in this plan. Confirm at the end that both issues' comment counts are
unchanged.
  </action>
  <acceptance_criteria>
    - The A1 disposition is recorded as exactly one of CONFIRMED (with the quoted substring) or NOT CONFIRMED (with both facts removed from the draft), and the draft's content matches the recorded disposition.
    - The scanner exits **0** with both draft file names in a single PASS line.
    - `grep -ciE 'AT28C[^.]{0,80}(should )?now work' <each draft>` returns **0**.
    - The SUMMARY contains a clearly-headed flagged-divergence block with all four elements: what D-14 said, what was measured, what the plan did, and what the operator is asked to decide.
    - The SUMMARY contains the delivery pre-brief naming the exact `gh issue comment` argv and the forbidden flags.
    - `gh issue view 11 --repo henols/firestarter_prom --json comments -q '.comments|length'` and the same for issue 12 both equal the counts recorded in `122-08-SUMMARY.md` — nothing was posted.
    - `gh issue view 11 --repo henols/firestarter_prom --json state -q '.state'` and the same for 12 both return `OPEN`.
    - Each draft's count of ``` fence markers is even.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && FIRESTARTER_CLAIMSCAN_TARGETS="$PWD/122-GH11-COMMENT.md:$PWD/122-GH12-COMMENT.md" python3 check_permitted_claims.py | grep -q '122-GH12-COMMENT.md' && for f in 122-GH11-COMMENT.md 122-GH12-COMMENT.md; do test "$(grep -ciE 'AT28C[^.]{0,80}(should )?now work' $f)" = "0" || { echo "OVERCLAIM SHAPE in $f"; exit 1; }; test $(( $(grep -c '```' $f) % 2 )) -eq 0 || { echo "UNBALANCED FENCE in $f"; exit 1; }; done && test "$(gh issue view 11 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && test "$(gh issue view 12 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && echo DRAFTS-GATED-AND-FLAGGED</automated>
  </verify>
  <done>A1 is resolved to one of two recorded dispositions with the draft matching it; both drafts pass the claim gate and the overclaim-shape check; the D-14 divergence is recorded as a prominent flagged item with an explicit operator decision attached; the delivery pre-brief names the exact argv and the forbidden flags; both issues are still OPEN with unchanged comment counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `.planning/` draft → a permanent public reply to a named stranger | Once posted it cannot be unsaid; two people are being asked to spend their own time and their own hardware on the strength of it |
| an unverified fact in a source document → a factual assertion about a third party's setup | Misattributing someone's firmware version or crash class in public is a specific, avoidable harm |
| a locked decision → a public sentence | D-14 is locked and wrong; applying it silently and dropping it silently are both failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-02 | Information Disclosure | overclaim in a public issue comment | critical | mitigate | Authored from the ledger's permitted-wording column; the Wave 0 gate plus a draft-specific overclaim-**shape** check; the corrected size-class answer states the measured refusal; the D-16 blocking review reads both drafts in 122-11 |
| T-122-25 | Spoofing | asserting an unverified fact about a third party | high | mitigate | A1 resolved to CONFIRMED-with-quote or NOT-CONFIRMED-and-removed; NOT CONFIRMED is the default; the pre-3.0 framing stands on the confirmed app version alone |
| T-122-24 | Information Disclosure | inviting a destructive write | high | mitigate | The always-writes warning precedes the `dev test` invitation; the plain `write` + `verify` path is offered first; the full-device-write safety note is grounded in the reporter's own statement that the input was throwaway random data |
| T-122-17 | Repudiation | a locked-decision divergence applied silently | high | mitigate | The divergence is flagged in the SUMMARY as an explicit operator decision, and is already recorded in `122-LEDGER.md` and `PROJECT.md`'s EIGHTH CORRECTION |
| T-122-01 | Information Disclosure | local-environment / secret leakage into public text | high | mitigate | Zero `/workspaces/` paths, zero `/dev/tty` device names, zero token names asserted in both drafts; no local traceback pasted; bodies authored, never interpolated from shell output |
| T-122-26 | Denial of Service | a `gh` flag that aborts before the comment is created | medium | mitigate | Delivery pre-brief forbids `--label`, `--add-label`, `-l`, `--web` and `--editor`; both commands abort on a missing label and neither issue carries any label |
| T-122-06 | Repudiation | posted text drifting from reviewed text | high | mitigate | Drafts are committed files delivered later with `--body-file`, never an inline string; nothing posted by this plan |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. Both drafts exist as committed files, ≥30 lines each, each carrying the canonical silicon caveat.
2. gh#11: both defects named with the conflation framing; the apparent-regression explanation present; the
   plain `write` + `verify` ask precedes the `dev test` offer; the always-writes warning precedes the
   invitation; the issue-stays-open statement present.
3. gh#12: all four policy elements present; the 43/41 boundary present; three inline answers present with
   nothing split out; the size-class answer states the refusal; the D-14 overclaim shape provably absent.
4. A1 resolved to a recorded disposition, and the draft matches it.
5. Both drafts pass the Wave 0 scanner in one invocation naming both files.
6. Neither contains "all 84", a `/workspaces/` path, a `/dev/tty` device name, a token name, or any
   GSD-internal identifier.
7. Both issues are still OPEN with unchanged comment counts — nothing posted.
8. The flagged D-14 divergence is recorded prominently with an explicit operator decision attached.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-02 closes only in 122-13, after both comments
are actually posted and verified byte-equal to these committed drafts.
</verification>

<success_criteria>
- Two ceiling-compliant, reader-facing drafts exist as committed files, authored from the ledger.
- The one unverified factual claim is resolved rather than asserted.
- The C-5 / D-14 locked-decision divergence is applied *and* flagged, with the operator holding the
  decision.
- Nothing is posted; delivery via `--body-file` is deferred to 122-12, after the D-16 review.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-10-SUMMARY.md`
when done. It MUST contain: the A1 disposition with evidence, the scanner PASS line verbatim, the
flagged D-14 divergence block with its four elements, the delivery pre-brief argv, and confirmation that
both issues are still OPEN with unchanged comment counts.
</output>
