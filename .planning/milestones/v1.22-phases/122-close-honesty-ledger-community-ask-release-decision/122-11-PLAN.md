---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 11
type: execute
wave: 9
depends_on: ["122-09", "122-10"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md
autonomous: false
requirements: [CLOSE-02]
must_haves:
  truths:
    - "The blocking operator wording review happens BEFORE any comment or release body reaches GitHub (D-16, constraint 4)"
    - "All five closing artifacts are reviewed together — the ledger, both release bodies, and both comment drafts — because they must all match one permitted wording"
    - "The Wave 0 claim scanner runs against its FIVE DEFAULT targets and exits 0, which is the mechanizable half of ROADMAP criterion 4 and is explicitly NOT presented as satisfying it"
    - "The C-5 / D-14 locked-decision divergence is surfaced to the operator as an explicit accept-or-overturn decision, not buried in a draft"
    - "If the operator names corrections, exactly those corrections are applied — no silent auto-approval and no rewording beyond what was asked for"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md"
      provides: "Operator-approved final text, byte-frozen for delivery in 122-12"
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md"
      provides: "Operator-approved final text, byte-frozen for delivery in 122-12"
  key_links:
    - from: "the operator's approval recorded in this plan's SUMMARY"
      to: "the `gh issue comment --body-file` / `gh release edit --notes-file` calls in 122-12"
      via: "delivery re-reads the same committed files this review approved, which is what proves posted == reviewed (D-16)"
      pattern: "body-file|notes-file"
---

<objective>
Put all five closing artifacts in front of the operator and BLOCK until they explicitly approve the
wording or name the sentences that must change — then apply exactly those changes and nothing more.

Purpose: D-16 and CONTEXT constraint 4. The ceiling wording gets a human read before it reaches two
strangers and two public release pages. `122-VALIDATION.md` is explicit that this is not advisory: a
forbidden-phrase scan cannot detect *"we've addressed this"* used to mean *"this is fixed"*, cannot judge
whether omitting the refusal misleads a reader, and cannot weigh tone. **A green claim-scan must never be
presented as satisfying ROADMAP criterion 4** — this review is the other half.

Output: operator-approved final text in all five artifacts, with the verdict recorded verbatim.

Precedent: plan 116-07 gated its `PROJECT.md` correction behind exactly this kind of blocking operator
wording review. Same shape here, widened from one artifact to five.

**Nothing is posted by this plan.** Delivery is 122-12.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. Both issues live in `henols/firestarter_prom`.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-VALIDATION.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (122-05) · `122-CUT.md` (122-07) · `122-CHANNELS.md` (122-08) ·
`122-RELEASE-NOTES-fw.md` + `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` +
`122-GH12-COMMENT.md` (122-10). **This plan may EDIT the five outward-facing/ledger artifacts, and only as
the operator directs.**
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03); outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs (122-07) · two prereleases, bodies added in 122-12 · one PyPI release
(122-08) · two issue comments (122-12).
**Deliberately NOT produced:** nothing posted here; no issue closed; no label flag; no `v1.22` tag; no
gitlink bump (D-07); no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the claim scanner against its five default targets and assemble the review packet</name>
  <files>none (read-only assembly)</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (122-01) — the five default targets, the forbidden labels, the required caveat, and the two-code exit contract.
    - All five artifacts to be reviewed: `122-LEDGER.md`, `122-RELEASE-NOTES-fw.md`, `122-RELEASE-NOTES-app.md`, `122-GH11-COMMENT.md`, `122-GH12-COMMENT.md`.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-09-SUMMARY.md` — the claim-sentence → ledger-claim-class traceability table for both release bodies.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-10-SUMMARY.md` — the A1 disposition and the flagged D-14 divergence block.
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 144-152) — the normative text the operator is judging against.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-VALIDATION.md` §"Manual-Only Verifications" — the four rows this review discharges.
  </read_first>
  <action>
Run the Wave 0 scanner **with no arguments and no env override**, so it resolves its five default targets
for the first time in the phase. Until now every run used the env seam because the targets did not exist;
all five exist now, so this is the contract run. Require exit 0 and a PASS line naming all five files. If
any file is missing, the fail-closed guard fires — that is the guard working, and it means a preceding plan
did not land its artifact.

Then assemble the review packet as a single presentation for the operator. Do not paraphrase the artifacts
— point at them and summarise only the decisions the operator has to make. The packet contains:

1. **The scanner result**, with the PASS line verbatim, immediately followed by the statement that a green
   scan is the mechanizable half of criterion 4 only and does not satisfy it.
2. **The five artifacts by path**, with a one-line description of who reads each: the ledger (internal
   evidence record and the single source of the permitted wording), the two release bodies (public release
   pages), and the two comment drafts (public replies to two named people).
3. **The three-way split**, quoted from `122-VALIDATION.md`: what is mechanically checkable, what needs
   this review, and the one class whose sampling rate is zero permanently by design.
4. **The flagged C-5 / D-14 divergence**, reproduced in full from `122-10-SUMMARY.md`: what D-14 said, what
   was measured (all 19 of 19 parts on the 24-pin `2816`-class pinout refused), what the draft does
   instead, and the explicit question — accept the divergence, or overturn it.
5. **The A1 disposition**, reproduced from `122-10-SUMMARY.md`: whether the 2025 reporter's firmware version
   and exception class were re-confirmed or removed, and the note that the operator may add either fact if
   they can see it in their own browser.
6. **The ledger-traceability tables** from `122-09-SUMMARY.md` and any equivalent from `122-10-SUMMARY.md`,
   so the operator can check claims against classes rather than reading five documents cold.
7. **Four specific questions to answer**, drawn from `122-VALIDATION.md`'s manual-only rows:
   - Is each artifact *honest*, not merely free of banned strings — is there any sentence that will be read
     as "this is fixed" even though it does not say so?
   - Is the 2K×8 answer size-class-correct and does it state the refusal plainly?
   - Are the two unconfirmed 2025 facts handled acceptably?
   - Is the tone right for two volunteers who are being asked to spend their own time and hardware?
8. **What happens on approval**: two `gh release edit --notes-file` calls and two `gh issue comment
   --body-file` calls in 122-12, reading these exact committed files. Both issues stay OPEN. No label flag
   is ever sent.

Also state plainly in the packet what is already irreversible and what is not: the beta is published on
both channels, but **no** release body has been written and **no** comment has been posted, so every word
in these five files is still fully revisable.
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` — invoked with **no arguments and no env override** — exits **0**, and its PASS line names all five default targets.
    - The review packet exists in the plan's working output and contains all eight numbered elements.
    - The packet contains the literal statement that a green claim-scan does not satisfy ROADMAP criterion 4.
    - The packet reproduces the flagged D-14 divergence with all four of its elements and poses the accept-or-overturn question explicitly.
    - `gh release view <observed firmware tag> --repo henols/firestarter --json body -q '.body|length'` is still **0**, and the same for the app release — nothing published yet.
    - `gh issue view 11 --repo henols/firestarter_prom --json comments -q '.comments|length'` and the same for 12 are unchanged from `122-08-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && OUT=$(python3 .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py) && echo "$OUT" | grep -q 'PASS:' && for f in 122-LEDGER.md 122-RELEASE-NOTES-fw.md 122-RELEASE-NOTES-app.md 122-GH11-COMMENT.md 122-GH12-COMMENT.md; do echo "$OUT" | grep -q "$f" || { echo "PASS line omits $f"; exit 1; }; done && echo DEFAULT-TARGET-SCAN-GREEN</automated>
  </verify>
  <done>The scanner's first default-target run is green with all five files named in the PASS line, and the eight-element review packet is assembled with the flagged divergence and the A1 disposition surfaced, not buried.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review of all five closing artifacts (D-16 — the phase's one judgement-only verification)</name>
  <files>none (gate only)</files>
  <read_first>
    - The review packet from Task 1.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md`
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md`
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md`
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md`
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md`
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling"
  </read_first>
  <what-built>
Five closing artifacts are written, committed, and mechanically gated:
- `122-LEDGER.md` — claim classes, each pairing a permitted wording with an explicit statement of what it
  does not prove, plus the measured-figure register and the negative space.
- `122-RELEASE-NOTES-fw.md` and `122-RELEASE-NOTES-app.md` — hand-written prerelease bodies, because CI
  writes an empty body and an empty body still lets a reader infer capability.
- `122-GH11-COMMENT.md` — the follow-up: both defects named, the apparent regression explained, the
  reporter's real-silicon reproduction credited, the plain `write` + `verify` ask first and `dev test`
  offered second with the always-writes warning ahead of it.
- `122-GH12-COMMENT.md` — the reply: the decided auto-unlock policy answering the 2024 design question,
  the measured capability boundary, and three inline answers with nothing split into a new issue.

The Wave 0 claim scanner passes on all five with no exclusions. The beta is already published and
channel-verified — but **no release body has been written and no comment has been posted**, so every word
is still fully revisable.
  </what-built>
  <action>
Present the packet and BLOCK until the operator explicitly approves the wording or names the sentences
that must change.

Ask them to read all five documents and answer four questions:
1. **Is each one honest, not merely unbanned?** A string scan cannot detect a sentence that will be read as
   *"this is fixed"* without saying so. That judgement is theirs and only theirs.
2. **The flagged divergence.** CONTEXT decision D-14 — a locked decision — prescribes telling the 2K×8
   reporter that their AT28C parts should now work. Measured over all 84 parts in this protocol bucket with
   the production predicate: every 2K×8 part sits on the 24-pin `2816`-class pinout and **all 19 of 19 are
   refused** by the SDP allow-set (7 as pre-SDP generation, 12 as unrecognised), and the shipped
   `dev sdp --help` text says so itself. The draft therefore answers **by size class** and states the
   refusal plainly. **Accept this divergence, or overturn it?** Do not proceed on ambiguity here.
3. **The two unconfirmed 2025 facts.** The retrievable comment body is truncated exactly where that
   reporter's firmware version and exception class would appear. The draft's disposition is recorded in the
   packet. If the operator can see either fact in their own browser and wants it named, they can say so now.
4. **Tone.** Two volunteers are being asked to spend their own time and their own hardware. Is the ask
   proportionate, is the credit genuine, and does the "please re-test, this is not confirmed" framing read
   as honest rather than hedged?

Capture the operator's verdict **verbatim** in the plan SUMMARY. If they name corrections, apply **exactly
those corrections** in Task 3 — no silent auto-approval, and no rewording beyond what was asked for. If
they overturn the D-14 divergence, record that as their decision and implement what they specify; do not
argue it further in the artifact.

This gate is **not auto-approvable**. Do not proceed on silence and do not treat `auto_advance` as
approval.
  </action>
  <acceptance_criteria>
    - The operator's verdict is recorded **verbatim** in the plan SUMMARY, including every named correction and their decision on the D-14 divergence.
    - The D-14 accept-or-overturn decision is recorded unambiguously as one or the other.
    - Nothing was posted during or before this gate: both release bodies are still length 0 and both issue comment counts are unchanged.
    - Both issues are still `OPEN`.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has typed an explicit approval of the wording of all five artifacts, or has named the specific sentences to change and confirmed the reworded versions — and has explicitly accepted or overturned the D-14 divergence. The verdict is recorded verbatim in the plan SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "approved" to freeze all five artifacts for delivery, or name the sentences that must change and state accept/overturn on the D-14 divergence.</resume-signal>
  <done>The operator has explicitly approved the wording or named exact corrections, and has ruled on the D-14 divergence; the verdict is recorded verbatim; nothing has been posted.</done>
</task>

<task type="auto">
  <name>Task 3: Apply exactly the corrections the operator named, re-gate, and freeze the five artifacts for delivery</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md</files>
  <read_first>
    - The operator's verbatim verdict as recorded in this plan's SUMMARY by Task 2.
    - Each artifact the operator named a correction against — read the current text before editing it.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` — to re-gate after editing.
  </read_first>
  <action>
If the operator approved with no changes, make no edit and skip straight to the freeze step below. Record
"approved unchanged" and the artifacts' current git SHAs.

If the operator named corrections:
- Apply **exactly** those corrections, using scoped `Edit` calls on the named files. Do not reword adjacent
  sentences, do not "improve" anything the operator did not raise, and do not restructure a section because
  a single sentence moved.
- If a correction is ambiguous, ask one clarifying question rather than guessing. A guess here becomes
  permanent public text.
- If the operator **overturned** the D-14 divergence, implement precisely the wording they specified and
  record the reversal in this plan's SUMMARY as their decision, with a cross-reference note that
  `122-LEDGER.md`'s mechanism-corrections section and `PROJECT.md`'s EIGHTH CORRECTION item 3 both describe
  the divergence and should be updated to match the operator's ruling. Update those two records if and only
  if the ruling changes what they assert.
- If a correction touches `122-LEDGER.md`, re-check that the changed wording still agrees with the two
  release bodies and both comment drafts. The ledger is the single source of the permitted wording; a
  change there that leaves the four downstream artifacts saying something else defeats constraint 5.

Then re-gate and freeze:
1. Re-run the scanner with **no arguments** (default targets). Require exit 0 and all five names in the
   PASS line.
2. Re-run the hygiene assertions on the four outward-facing files: no `/workspaces/` path, no `/dev/tty`
   device name, no token name, no GSD-internal identifier, no "all 84", balanced fence counts, canonical
   caveat present in each.
3. Re-run the overclaim-**shape** check on both comment drafts.
4. Commit all five files, then record each file's **git blob SHA and byte length** in the SUMMARY. These
   are the freeze values 122-12 asserts against immediately before delivery and immediately after posting
   — the mechanism that proves posted text equals reviewed text.
5. Confirm nothing has been posted: both release bodies still length 0, both comment counts unchanged, both
   issues `OPEN`.
  </action>
  <acceptance_criteria>
    - Either "approved unchanged" is recorded with no edit to any of the five files, or every named correction is applied and each applied edit is traceable to a line of the operator's verbatim verdict.
    - No file was edited that the operator did not name.
    - `python3 .planning/phases/122-…/check_permitted_claims.py` with no arguments exits **0** and names all five files.
    - All four outward-facing files pass the hygiene assertions and the overclaim-shape check.
    - The SUMMARY records, for each of the five files, its git blob SHA (`git rev-parse HEAD:<path>`) and its byte length (`wc -c`).
    - All five files are committed — `git status --porcelain` shows none of them as modified or untracked.
    - Both release bodies are still length 0; both issue comment counts unchanged; both issues `OPEN`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py && test -z "$(git status --porcelain -- .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH11-COMMENT.md .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-GH12-COMMENT.md)" && test "$(gh issue view 11 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && test "$(gh issue view 12 --repo henols/firestarter_prom --json state -q '.state')" = "OPEN" && echo ARTIFACTS-FROZEN</automated>
  </verify>
  <done>Exactly the operator's corrections are applied (or none, if approved unchanged), the scanner and every hygiene check are green on the default target set, all five files are committed, and each file's blob SHA and byte length are recorded as the freeze values 122-12 will assert against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| machine-checkable prose properties → human judgement of honesty | The scanner and the operator answer different questions; conflating them is the specific failure this plan guards against |
| reviewed text → delivered text | The freeze (blob SHA + byte length) is what makes the later delivery provably the reviewed text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-06 | Repudiation | posted text drifting from reviewed text | high | mitigate | The five files are committed and their blob SHAs + byte lengths recorded as freeze values; 122-12 delivers with `--body-file` / `--notes-file` reading those exact files and re-asserts the SHAs before and after |
| T-122-27 | Repudiation | a green gate presented as a satisfied criterion | high | mitigate | The packet states, and the SUMMARY repeats, that a green claim-scan is the mechanizable half of criterion 4 only; the operator review is the judgement half; one claim class is permanently unsampleable |
| T-122-17 | Repudiation | a locked-decision divergence applied without consent | high | mitigate | The D-14 divergence is put to the operator as an explicit accept-or-overturn question; the ruling is recorded unambiguously; an overturn propagates to the ledger and the EIGHTH CORRECTION |
| T-122-28 | Tampering | scope creep during correction application | medium | mitigate | Only the operator's named corrections are applied, via scoped `Edit`; no file the operator did not name is touched; ambiguity triggers one clarifying question rather than a guess |
| T-122-02 | Information Disclosure | overclaim reaching the public | critical | mitigate | This gate is the last human read before publication, and it precedes every posting call (constraint 4) |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. The scanner's default-target run (no arguments, no env override) exits 0 with all five files named.
2. The eight-element review packet was presented, including the flagged D-14 divergence and the A1
   disposition.
3. The operator's verdict is recorded verbatim, with an unambiguous accept-or-overturn ruling on D-14.
4. Exactly the named corrections are applied; no unnamed file edited.
5. All five files are committed, with blob SHAs and byte lengths recorded as freeze values.
6. Both release bodies still length 0; both issue comment counts unchanged; both issues `OPEN`.
7. The SUMMARY states that a green claim-scan does not satisfy ROADMAP criterion 4.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-02 closes only in 122-13, after both comments
are posted and verified byte-equal to these frozen files.
</verification>

<success_criteria>
- The blocking operator wording review happened before anything reached GitHub.
- All five artifacts were reviewed together against one permitted wording.
- The D-14 divergence was decided by the operator, on the record.
- The five artifacts are frozen with recorded blob SHAs, ready for a delivery that provably matches.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-11-SUMMARY.md`
when done. It MUST contain: the operator's verdict verbatim, the D-14 ruling, every applied correction
traced to the verdict, the five files' blob SHAs and byte lengths, the default-target PASS line, and the
explicit statement that a green claim-scan does not satisfy criterion 4.
</output>
