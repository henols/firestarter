---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 04
type: execute
wave: 3
depends_on: ["122-03"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md
autonomous: true
requirements: [CLOSE-01]
must_haves:
  truths:
    - "`0x0D` still reads `**UNVERIFIED**` in `PROTOCOL-LEDGER.md` — verified by reading, never by editing (D-09)"
    - "Zero chips changed `support_status` and the 84-chip `algorithm == 13` count is unchanged, proven on the MERGED tree (constraint 6), by the three mechanisms that already exist plus one independent second measurement path"
    - "The eleven cross-repo nine-row non-regression commands are green on the merged tree, including rows 9a/9b which scan `submit.py` — the very file the merge conflicted (Pitfall 5)"
    - "Both beta workflows' local gate sets are pre-validated before the push, so the outbound push cannot fail CI before the version step and leave a half-cut state"
    - "`check_ledger.py` is NEVER run as a CLOSE-01 gate — it is pre-existing RED from v1.19 Phase 104's `flash3`/`flash4` rename (C-4), and fixing it would edit a closed milestone's artifact (D-09)"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md"
      provides: "The gate-result artifact for the merged tree — CLOSE-01's four mechanisms, the eleven nine-row commands, both full suites, both CI gate sets"
      contains: "UNVERIFIED"
      min_lines: 90
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md"
      to: ".planning/v1.16/ledger/PROTOCOL-LEDGER.md"
      via: "read-only grep asserting the 0x0D row still reads UNVERIFIED; the file is never written (D-09)"
      pattern: "0x0D.*UNVERIFIED"
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md"
      to: "firestarter_app/tools/diff_db.py"
      via: "exit code 0 is the assertion; identity means 'still exactly 2 explained PGSZ_PAGE_SIZE changes, 0 new, 0 removed', never zero diff (C-13, GATE-03)"
      pattern: "diff_db"
---

<objective>
Run every CLOSE-01 mechanism, the eleven-command cross-repo non-regression set, both full suites, and
both beta workflows' local gate sets — all **against the merged tree** produced by 122-03 — and record
the results in `122-NONREGRESSION.md`.

Purpose: CONTEXT constraint 6 makes this the load-bearing sample of the phase. CLOSE-01's result must be
a statement about the tree that actually gets published, not about an earlier commit. Constraint 2 means
`beta` must never see an unproven tree, so this runs *before* the outbound push. And rows 9a/9b of the
nine-row gate scan `firestarter/submit.py` — one of the two files the merge conflicted — so the `--ours`
resolution has to be re-proved against the gates rather than trusted from a prior SUMMARY.

Output: `.planning/phases/122-…/122-NONREGRESSION.md`. No source file in either sub-repo is modified.

**Do not rebuild any verification mechanism.** All four CLOSE-01 proofs already exist and were green on
2026-07-30. The failure mode here is not "the check is missing" — it is a plan inventing a fifth check,
or resurrecting a sixth (`check_ledger.py`) that is already RED for reasons this milestone did not cause.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo for the artifact: **meta** (`/workspaces`). Commands run in **`firestarter_app`** and
**`firestarter`** (both sub-repos, both on the v1.22 branch, both carrying 122-03's inbound merge). No
sub-repo file is written.

@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (**this plan**) ·
`122-LEDGER.md` (122-05) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` +
`122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` + `122-GH12-COMMENT.md` (122-10).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03), outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs · two prerelease bodies · one PyPI release · two issue comments.
**Deliberately NOT produced:** no `v1.22` tag, no gitlink bump (D-07); no `PROTOCOL-LEDGER.{md,json}`
edit and no `check_ledger.py` run or fix (D-09, C-4); no `chip_database.json` change; no
`support_status` change; no ladder-state promotion; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run CLOSE-01's four existing mechanisms on the merged tree, plus one independent second measurement path and the per-pinout SDP split</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"CLOSE-01's Verification Mechanisms (they exist — do not rebuild)" (lines 517-589) — the four commands with their live results, the independent Python measurement, the four-pinout ALLOW/REFUSE table, the `check_ledger.py` RED record, and the ledger header's stale firmware pin.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Code Examples" → "CLOSE-01 — the complete verification, runnable as one block" and "The SDP ALLOW/REFUSE split" (lines 962-1012) — the exact command block and the `sdp_capability_for_entry` invocation shape, including the hard-fail-on-a-programmer-dict caveat.
    - `.planning/v1.16/ledger/PROTOCOL-LEDGER.md` lines 1-40 — the header provenance block, the status key, and the `0x0D` row at line 27. **Read only. Never write this file.**
    - `.planning/REQUIREMENTS.md` line 102 (CLOSE-01's literal text) and lines 96-98 (GATE-03's "identity means still exactly 2" reading of `diff_db.py`).
    - `/workspaces/firestarter_app/tools/diff_db.py` module docstring — its three-code exit contract, so the exit code is used rather than the printed "2 changed chips" number being misread as a failure.
  </read_first>
  <action>
All commands run against the **merged** tree from 122-03. Record every command verbatim with its exact
output into a scratch note you will fold into the artifact in Task 3.

CLOSE-01's four mechanisms, in this order:
1. `0x0D` still `UNVERIFIED`. From `/workspaces`, grep `.planning/v1.16/ledger/PROTOCOL-LEDGER.md` for a
   line beginning with the `0x0D` table cell and containing `**UNVERIFIED**`; the count must be exactly
   1. This is a **read**, not a write — D-09 makes this file read-only for the whole phase, because
   editing it would make its stale `a296195` firmware pin (an ancestor of neither `beta` nor the live
   line) newly load-bearing, and would drag the v1.19-broken `check_ledger.py` into the gate set.
2. The 84-count and `chip_id_check` invariant. From `/workspaces/firestarter_app`,
   `python3 -m pytest tests/test_sdp_db_invariant.py -q` — expect 4 passed. This is TRACE-05's test; it
   pins the count itself and carries no `skipif`. Do not write a new assertion script.
3. DB identity. From `/workspaces/firestarter_app`, `python3 tools/diff_db.py`. **Use the exit code**
   (expect 0). Its PASS line reads "all 2 changed chips explained (0 new chips confirmed; 0 chips removed
   from baseline)" — GATE-03's recorded reading is that identity here means *still exactly 2 explained
   `PGSZ_PAGE_SIZE` changes, 0 new, 0 removed*, **not** zero diff. Do not report a two-change result as a
   failure; the tool interprets its own result (C-13).
4. No code path writes `support_status`. From `/workspaces/firestarter_app`,
   `python3 tools/check_no_community_support_status_write.py` — expect exit 0 and a PASS line naming both
   scanned files.

Then one **independent second measurement path** — a non-test route to the same facts, so CLOSE-01 does
not rest solely on a test's own assertions. Load `firestarter/data/chip_database.json` directly and
report: total chip count (expect 746), the count at `programming.algorithm == 13` (expect 84), the set of
`chip_id_check` values among those 84 (expect exactly `{False}`), the `support_status` histogram among
those 84 (expect supported 75 / adapter-required 9), and the `pinout` histogram (expect `DIP28_28C64` 35,
`DIP24_2816` 19, `DIP32_28C512_EEPROM` 18, `DIP28_28C256` 12).

Then the **per-pinout SDP ALLOW/REFUSE split**, because `122-LEDGER.md`'s most important row depends on
it and 122-10's corrected `No-Hazmats` answer depends on it being measured rather than quoted. Run
`sdp_capability.sdp_capability_for_entry` over all 84 entries, per pinout. Note the shape trap from
RESEARCH: the predicate **hard-fails on a programmer dict** by Phase 120's anti-vacuity design, so each
entry must be passed in the `db.get_eprom()` shape — build it by copying the DB entry and adding
`protocol-id: 13` and `name` from `part_number`, exactly as RESEARCH's example does. Expect
`DIP28_28C64` 15/20, `DIP24_2816` **0 ALLOW / 19 REFUSE**, `DIP32_28C512_EEPROM` 18/0, `DIP28_28C256`
10/2, totalling **43 ALLOW / 41 REFUSE**, reproducing STATE.md's derived partition exactly.

Record explicitly, as a named finding: **emission-traced byte-exact for a pinout and operation-permitted
on parts with that pinout are different claims.** For `DIP24_2816` the first is true and the second is
false for all 19 chips. Every downstream artifact that says "all four `0x0D` pinouts" must carry this
distinction or it reads as broader capability than shipped.

**Do NOT run** `.planning/v1.16/ledger/tools/check_ledger.py`. It exits 1 today with two `LEDGER-01`
violations — rows `0x05`/`0x06` carry `matrix_family` `flash4`/`flash3`, which v1.19 Phase 104 renamed to
`5v_page`/`nor_unlock` — and `tools/validation_matrix_spec.json` is not in the `beta...HEAD` diff, so this
is pre-existing and unrelated to v1.22. CLOSE-01's text does not mention it. Record it as a
known-and-explained condition with its v1.19 cause and the note that fixing it would edit a closed
milestone's artifact (RESEARCH Open Question 4 recommends a backlog seed instead). The `0x0D` row's own
join key (`eeprom28c`, protocols `[13]`, rep chip `AT28C256`) is present and valid — only `0x05`/`0x06`
are stale.
  </action>
  <acceptance_criteria>
    - The `0x0D` `UNVERIFIED` grep count is exactly **1**, and `git -C /workspaces status --porcelain -- .planning/v1.16/ledger/` is **empty** — the ledger pair was read and not modified.
    - `cd /workspaces/firestarter_app && python3 -m pytest tests/test_sdp_db_invariant.py -q` reports 4 passed.
    - `cd /workspaces/firestarter_app && python3 tools/diff_db.py` exits **0**.
    - `cd /workspaces/firestarter_app && python3 tools/check_no_community_support_status_write.py` exits **0** and its PASS line names both scanned files.
    - The independent measurement reports 746 total, 84 at `algorithm == 13`, `chip_id_check` set `{False}`, `support_status` 75 supported / 9 adapter-required, and the four pinout counts 35/19/18/12.
    - The measured SDP split totals **43 ALLOW / 41 REFUSE** with `DIP24_2816` at **0/19**.
    - No invocation of `check_ledger.py` appears anywhere in the executed commands or the artifact's command list, and the artifact records its RED with the v1.19 Phase 104 cause.
    - `git -C /workspaces/firestarter_app status --porcelain` and `git -C /workspaces/firestarter status --porcelain` still show only the named pre-existing dirt — running the gates changed no file.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_sdp_db_invariant.py -q && python3 tools/diff_db.py && python3 tools/check_no_community_support_status_write.py && test "$(grep -c '^| `0x0D` .*\*\*UNVERIFIED\*\*' /workspaces/.planning/v1.16/ledger/PROTOCOL-LEDGER.md)" = "1" && test -z "$(git -C /workspaces status --porcelain -- .planning/v1.16/ledger/)" && echo CLOSE01-MERGED-TREE-OK</automated>
  </verify>
  <done>All four CLOSE-01 mechanisms are green on the merged tree; the independent measurement reproduces every count; the 43/41 and 0/19 splits are measured, not quoted; `PROTOCOL-LEDGER` is provably unmodified; `check_ledger.py` was not run and its RED is recorded with its cause.</done>
</task>

<task type="auto">
  <name>Task 2: Run the eleven nine-row cross-repo commands, both full suites, and both beta workflows' gate sets on the merged tree</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"GATE-03's Nine-Row Non-Regression Set (D-06)" (lines 592-637) — the eleven commands in table form, the reason re-running after the inbound merge is still correct, and the pre-merge full-suite baselines.
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` §5 (from line 275) — the CORRECTION-4 nine-row gate table as originally authored and explicitly handed to Phases 120-122.
    - `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md` lines 1-50 — the artifact shape to mirror, including the "every row was re-executed in this session" pledge.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Release Mechanics" → "Gates each beta workflow runs" (lines 449-460) — the exact gate sequence of `beta-build.yml` and `beta-release.yml`, and the explicit list of what `beta-release.yml` does NOT run.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Common Pitfalls" → Pitfall 5 and Pitfall 7 (lines 923-946).
  </read_first>
  <action>
Run all eleven nine-row commands from `/workspaces/firestarter_app`, in order, recording each command
and its verbatim result. Do not accept a prior phase's PASS for any row.

Rows 1-9b: `tools/check_no_log_in_sdp_window.py`; `pytest tests/test_check_no_log_in_sdp_window.py -q`;
`pytest tests/test_sdp_table_parity.py -q`; `tools/check_is_memory_cmd_no_ifdef.py`;
`pytest tests/test_check_is_memory_cmd_no_ifdef.py -q`; `tools/gen_sdp_bus_config.py`;
`pytest tests/test_sdp_bus_config_drift.py -q`; `pytest tests/test_revision_constants_parity.py -q`;
`pytest tests/test_dispatch_mirror.py -q`; `tools/check_dispatch.py`;
`tools/check_devtest_orchestrator.py`.

Two row-specific requirements:
- **Row 5 is an idempotence check, not a bare exit code.** After running `gen_sdp_bus_config.py`, assert
  the firmware tree is unchanged. The correct assertion is that
  `git -C /workspaces/firestarter status --porcelain` output equals its pre-existing baseline `?? firestarter/`
  — **not** that it is empty. The untracked nested `firestarter/` directory is pre-existing dirt; reading
  it as a generator-induced change would be a false failure. Follow Plan 120-12's precedent of actually
  re-executing the generator and confirming the tree afterwards.
- **Rows 9a/9b are the ones at real risk.** They scan `firestarter/submit.py` and `cli_handlers.py`, and
  `submit.py` is one of the two files the inbound merge conflicted. Row 9b's own PASS line names
  `submit.py` explicitly — record that PASS line verbatim and confirm the named file list is unchanged
  from Phase 121's recorded value. This is Pitfall 5: the host suite can stay green while a cross-repo
  source-scanning gate silently breaks, which happened four times in Phase 117.

Then both full suites, on the merged tree:
- `cd /workspaces/firestarter_app && python3 -m pytest -q` — pre-merge baseline was **1150 passed**.
  Report the exact count. If it differs, state the delta and its cause; do not round or paraphrase.
- `cd /workspaces/firestarter && pio test -e native` — baseline **141/141 succeeded** across 17 suites.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — baseline **8 passed**.

Then pre-validate **both beta workflows' local gate sets**, so the outbound push in 122-07 cannot fail CI
before its version-bump step and leave a half-cut state:
- `beta-build.yml`'s set: catalog validity (`python3 tools/catalog/codegen.py --catalog
  tools/catalog/messages.toml --check` from the meta repo); the firmware codegen drift gate — regenerate
  `include/messages.h` and confirm `git diff --exit-code` is clean; `pio test -e native`; firmware
  `pytest tests/ -v`; and `pio run` across `default_envs` (`uno, uno328pb, leonardo`) — three
  environments, all SUCCESS. Record the three flash/RAM figures.
- `beta-release.yml`'s set: `pip install -e .[test]` is already satisfied in this devcontainer (do **not**
  re-install; RESEARCH's Package Legitimacy Audit records zero installs in phase scope); catalog
  validity; the app codegen drift gate — regenerate `firestarter/messages.py`, ruff-normalize as the
  workflow does, and confirm `git diff --exit-code` is clean; `pytest tests/ -v`.

Record explicitly what these two workflows do **not** run, so their green tick is not over-read:
`beta-release.yml` runs no ruff, no mypy, no coverage floor, no vector-catalog gate and no CLI smoke test
— those live only in `ci.yml`, which triggers on `main` and `pull_request` and therefore **never runs on
a `beta` push** (C-8). Same for firmware `build.yml`. Consequently the four pre-existing `ruff check`
findings and four `ruff format` drift files (all in `tools/` + `.github/scripts/`) are structurally
invisible to CI, are out of this phase's scope, and must **not** be fixed here.

Also record that `catalog-sync-check.yml` is **red-until-`main`-merge by design** — it checks out both
sub-repos at `ref: main`, so it cannot go green until v1.22 reaches `main`, which is
`/gsd-complete-milestone`'s job. The in-phase proof is the three-way `cmp` plus `codegen --check`
(Task 3 of 122-03 and this task). Do not chase that red. Same for firmware `build.yml`'s
`pio test -e native_nodevtools` step, which also first executes for real at that `main` merge.

Do not run any formatter with a write flag. Do not modify any generated file to make a drift gate pass —
if a drift gate goes red, that is a finding, not a file to patch.
  </action>
  <acceptance_criteria>
    - All eleven nine-row commands are executed on the merged tree and all eleven PASS; each command and its verbatim result is recorded.
    - Row 5's post-generator assertion is stated against the baseline `?? firestarter/`, and `git -C /workspaces/firestarter status --porcelain` equals exactly that string after the generator ran.
    - Row 9b's PASS line is recorded verbatim and its named file list is confirmed unchanged from Phase 121's record.
    - `cd /workspaces/firestarter_app && python3 -m pytest -q` reports **1150 passed** (or the exact observed count with a stated delta and cause).
    - `cd /workspaces/firestarter && pio test -e native` reports **141/141 succeeded**.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` reports **8 passed**.
    - `pio run` succeeds for all three `default_envs`, and the three flash/RAM figures are recorded.
    - Both codegen drift gates (`include/messages.h`, `firestarter/messages.py`) are clean by `git diff --exit-code` after regeneration, and no generated file was hand-edited.
    - Both sub-repo trees still show only the named pre-existing dirt afterwards.
    - The artifact records the "what the beta workflows do not run" list and the two by-design red workflows.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_no_log_in_sdp_window.py && python3 tools/check_is_memory_cmd_no_ifdef.py && python3 tools/check_dispatch.py && python3 tools/check_devtest_orchestrator.py && python3 -m pytest tests/test_check_no_log_in_sdp_window.py tests/test_sdp_table_parity.py tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_bus_config_drift.py tests/test_revision_constants_parity.py tests/test_dispatch_mirror.py -q && python3 -m pytest -q 2>&1 | tail -2 && cd /workspaces/firestarter && pio test -e native 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -2 && test "$(git status --porcelain)" = "?? firestarter/" && echo NINEROW-AND-SUITES-OK</automated>
  </verify>
  <done>Eleven nine-row commands green on the merged tree with rows 5 and 9a/9b handled per their specific requirements; both full suites at their baselines; both beta workflows' gate sets pre-validated locally including `pio run` across three envs; the not-run list and the two by-design reds recorded; no generated file hand-edited.</done>
</task>

<task type="auto">
  <name>Task 3: Write 122-NONREGRESSION.md in 121-NONREGRESSION.md's shape</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md</files>
  <read_first>
    - `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md` — the exact-match analog. Copy its header block (base + HEAD commits for all three repos) and its re-execution pledge, plus its §1 "the claim, as precise statements" style where each statement is individually checkable against a named artifact.
    - `.planning/phases/119-lock-sdp-enable-command-surface-fw-half/119-NONREGRESSION.md` §6 — the "known-and-explained conditions" section shape, where `check_ledger.py`'s RED and the two by-design red workflows belong.
    - The recorded outputs from Tasks 1 and 2 of this plan.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-03-SUMMARY.md` — both merge SHAs and first-parent SHAs to pin in the header.
  </read_first>
  <action>
Write `122-NONREGRESSION.md` in the meta phase directory.

Header block: pin the **firmware phase base** and **host phase base** (`0048b3d` / `96e0622`, the values
the meta gitlinks still carry), the **firmware and host HEAD at this sweep** (122-03's two merge commit
SHAs), and the four post-merge version strings. Carry the re-execution pledge verbatim in substance:
every row below was re-executed in this session, nothing is copied from a prior plan's SUMMARY, and where
a prior SUMMARY made a claim this document re-checks it against the live tree and says so.

Sections, in this order:
1. **The claim, as precise statements.** Each statement individually checkable against a named artifact:
   `0x0D` is still `UNVERIFIED` and `PROTOCOL-LEDGER` was read, not written; zero chips changed
   `support_status` and none can (both the DB-identity exit code and the source-scan gate); the
   `algorithm == 13` count is 84, measured twice by independent paths; the cross-repo source-scanning
   gates survive a merge that touched one of the files they scan.
2. **CLOSE-01's four mechanisms**, as a table of command → expected → observed, plus the independent
   second measurement path with all its counts, plus the four-pinout ALLOW/REFUSE table with the
   emission-traced-vs-operation-permitted distinction stated as a named finding.
3. **The eleven-row cross-repo gate table**, with rows 5 and 9a/9b annotated per Task 2.
4. **Full-suite results** — app pytest, firmware native, firmware script tests — as observed counts
   against the recorded pre-merge baselines.
5. **Both beta workflows' local gate sets** as pre-validated, with the three `pio run` flash/RAM figures
   and an explicit list of what those workflows do **not** run.
6. **Known-and-explained conditions**, each with its cause and why it is not this phase's work:
   `check_ledger.py` RED (v1.19 Phase 104's `flash3`/`flash4` rename; fixing it would edit a closed
   milestone's artifact); `catalog-sync-check.yml` red until the `main` merge (`ref: main` by design);
   firmware `build.yml`'s `native_nodevtools` step first executing at that same merge; the four
   pre-existing `ruff check` findings and four `ruff format` drift files in `tools/` + `.github/scripts/`,
   structurally outside `ci.yml`'s scope; the mypy watermark at 1 error against a 35 watermark; and the
   named pre-existing working-tree dirt in all three repos.
7. **The validation-ceiling statement**, quoting REQUIREMENTS.md's permitted claim and forbidden claim
   verbatim, followed by a line-by-line confirmation that nothing in this document asserts the forbidden
   claim. Cite measured figures only — 66 of 84 (**never** "all 84"), 43 ALLOW / 41 REFUSE, 301/301 UV via
   `electrical-type`, and keep the flash budget (LOCK-06, 2600 B free) and the timing budget (F-118-01,
   572 µs vs 600 µs) in **separate** statements, because PROJECT.md conflated them once and
   `119-MEASUREMENT.md` records the correction.
8. **Deliberately not taken**, naming: no `PROTOCOL-LEDGER` edit; no `check_ledger.py` fix; no ruff
   finding fixed; no generated file hand-edited; no gitlink bump; no formatter run on the merge-resolved
   files.

Then run the Wave 0 claim scanner over this artifact via the env seam
(`FIRESTARTER_CLAIMSCAN_TARGETS=<this file> python3 .../check_permitted_claims.py`) as an early smoke
test of both the gate and the prose. `122-NONREGRESSION.md` is **not** one of the scanner's five default
targets — it is an internal gate-result artifact, not outward-facing prose — so this is a courtesy check,
not a contract. If it flags a phrase, reword the artifact; never weaken the pattern set.

**State plainly in the artifact** that a green claim-scan does not satisfy ROADMAP criterion 4. That
criterion is closed by this gate plus the D-16 blocking operator wording review (plan 122-11), and one
whole claim class — that SDP works on real AT28C silicon — has a sampling rate of zero, permanently, by
design.
  </action>
  <acceptance_criteria>
    - `122-NONREGRESSION.md` exists, is ≥90 lines, and contains all eight numbered sections.
    - Its header pins `0048b3d`, `96e0622`, both 122-03 merge SHAs, and the four post-merge version strings.
    - `grep -c 'UNVERIFIED' 122-NONREGRESSION.md` ≥ 1 and `grep -c 'check_ledger' 122-NONREGRESSION.md` ≥ 1 (recorded as known-RED, never as an executed gate).
    - `grep -c 'all 84' 122-NONREGRESSION.md` returns **0** — the honest trace figure is 66 of 84.
    - The artifact quotes REQUIREMENTS.md's permitted claim and forbidden claim verbatim.
    - The flash budget and the timing budget appear as two separate statements, never combined into one figure.
    - The artifact states that a green claim-scan does not satisfy criterion 4.
    - `FIRESTARTER_CLAIMSCAN_TARGETS=<abs path to 122-NONREGRESSION.md> python3 .planning/phases/122-…/check_permitted_claims.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && test "$(wc -l < 122-NONREGRESSION.md)" -ge 90 && grep -q 'UNVERIFIED' 122-NONREGRESSION.md && grep -q 'check_ledger' 122-NONREGRESSION.md && test "$(grep -c 'all 84' 122-NONREGRESSION.md)" = "0" && FIRESTARTER_CLAIMSCAN_TARGETS="$PWD/122-NONREGRESSION.md" python3 check_permitted_claims.py && echo NONREGRESSION-ARTIFACT-OK</automated>
  </verify>
  <done>`122-NONREGRESSION.md` records the merged-tree sweep in the established eight-section shape, cites measured figures only, keeps the two budgets separate, records every known-and-explained condition with its cause, passes the claim scanner, and states the criterion-4 non-claim explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged tree → published artifact | This is the last gate before the outbound push cuts a public release |
| firmware source text → host-side scanning gates | Rows 9a/9b read `firestarter/submit.py` and `cli_handlers.py`; a cross-repo break is invisible to the firmware suite |
| closed-milestone artifact (`PROTOCOL-LEDGER`) → this phase | Read-only boundary; crossing it makes a stale firmware pin newly load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-03 | Tampering | publishing from an unproven tree | high | mitigate | Eleven nine-row commands + both full suites + CLOSE-01's four mechanisms + both beta workflows' gate sets, all executed on the merged tree before the outbound push (constraints 2 and 6) |
| T-122-05 | Tampering | the `--ours` resolution reaching every `pip install --pre` user | critical | mitigate | Rows 9a/9b re-run against the merged `submit.py`, row 9b's named file list confirmed unchanged, plus the full 1150-test app suite |
| T-122-14 | Tampering | closed-milestone artifact mutation | medium | mitigate | D-09: `PROTOCOL-LEDGER.{md,json}` read-only; `git status --porcelain -- .planning/v1.16/ledger/` asserted empty; `check_ledger.py` never invoked (C-4) |
| T-122-15 | Tampering | vacuous cleanliness proof | medium | mitigate | Whole-tree `git status --porcelain` against a **recorded baseline** including the known untracked dirt, per SEVENTH CORRECTION item 9; never a path-scoped `git diff` |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope; `pip install -e .[test]` is already satisfied in the devcontainer and must NOT be re-run as part of the workflow gate-set dry run (`122-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
1. All four CLOSE-01 mechanisms green on the merged tree; `PROTOCOL-LEDGER` provably unmodified.
2. Independent measurement reproduces 746 / 84 / `{False}` / 75+9 / 35-19-18-12.
3. Measured SDP split 43 ALLOW / 41 REFUSE with `DIP24_2816` 0/19.
4. Eleven nine-row commands PASS; row 5 idempotent against the `?? firestarter/` baseline; row 9b's file
   list unchanged.
5. App pytest 1150 passed; firmware native 141/141; firmware script tests 8 passed; `pio run` 3/3.
6. Both codegen drift gates clean; catalog valid; three-way `cmp` identity clean.
7. `122-NONREGRESSION.md` written, ≥90 lines, scanner-clean, with the criterion-4 non-claim stated.
8. `check_ledger.py` was NOT run; its RED is recorded with the v1.19 Phase 104 cause.

**Known pre-existing dirt — not this plan's damage:** `firestarter`: untracked `firestarter/`.
`firestarter_app`: modified `.gitignore`, untracked `.coverage`, `.planning/config.json`, `SECURITY.md`,
`write_test_port.sh`. Meta: modified `.planning/config.json`.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-01 is closed only in 122-13, after the ledger
and the EIGHTH CORRECTION exist and after the phase's other requirements have landed.
</verification>

<success_criteria>
- CLOSE-01's entire verification surface is green on the tree that will actually be published, using the
  mechanisms that already exist — no fifth check invented, no sixth resurrected.
- The eleven-command cross-repo gate is green post-merge, with the two at-risk rows specifically
  re-proved.
- Both beta workflows' gate sets pass locally, so the outbound push is not a gamble.
- `122-NONREGRESSION.md` is the committed record, in the established shape, citing measured figures only.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-04-SUMMARY.md`
when done. Record every observed count against its baseline, the 43/41 and 0/19 splits, the three
`pio run` figures, and the explicit statement that a green claim-scan does not satisfy criterion 4.
</output>
