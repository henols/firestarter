---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/clean_control.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/clean_control_second.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_forbidden_claim.md
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_missing_caveat.md
autonomous: true
requirements: [CLOSE-02]
must_haves:
  truths:
    - "A meta-repo gate script mechanically rejects any closing artifact that carries a forbidden overclaim, or that omits the required silicon caveat (the mechanizable half of ROADMAP criterion 4, per D-08 and D-11)"
    - "The gate is proven able to FAIL: a committed planted-violation fixture drives it to exit 1, and a test asserts that"
    - "The gate is fail-closed: a missing scan target and an empty target list both exit 1, never a vacuous pass"
    - "The gate's PASS line names every file it scanned, so a silently-skipped artifact is visible"
    - "A green run of this gate is never presented as satisfying ROADMAP criterion 4 — the D-16 operator review is the other, non-mechanizable half"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py"
      provides: "Forbidden-phrase / required-caveat scanner over the five closing artifacts"
      contains: "FIRESTARTER_CLAIMSCAN_TARGETS"
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py"
      provides: "Anti-hollow (GATE-01) pairing — 7 subprocess legs including two planted-violation legs"
      contains: "subprocess"
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_forbidden_claim.md"
      provides: "Committed deliberately-violating fixture (forbidden phrase present, caveat present)"
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_missing_caveat.md"
      provides: "Committed deliberately-violating fixture (caveat absent, no forbidden phrase)"
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py"
      to: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py"
      via: "subprocess.run([sys.executable, <scanner path>], env={**os.environ, 'FIRESTARTER_CLAIMSCAN_TARGETS': <fixture>}) — the exit code IS the assertion"
      pattern: "subprocess\\.run"
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py"
      to: ".planning/REQUIREMENTS.md"
      via: "the forbidden set and the required caveat are distilled from §Validation Ceiling's permitted/forbidden claim pair"
      pattern: "Validation Ceiling|forbidden"
---

<objective>
Build the phase's one Wave 0 dependency: a small meta-repo gate that scans this phase's five closing
artifacts for forbidden overclaims and for the presence of the required silicon caveat — plus the
mandatory anti-hollow pairing (committed planted-violation fixtures and a test proving the gate exits
1 on them).

Purpose: ROADMAP criterion 4 ("every claim in the closing documentation matches the validation
ceiling's permitted claim and none matches its forbidden claim") is otherwise 100% human judgement.
Research C-5 proved an overclaim already reached a *locked decision* (D-14), so the risk is
demonstrated, not hypothetical. This gate mechanizes the half a machine can check, in service of the
permitted-wording contract D-08 and D-11 impose on every closing artifact.

Output: `check_permitted_claims.py`, `test_check_permitted_claims.py`, and four committed fixtures —
all in the meta repo's phase directory. No sub-repo file is touched.

**Explicit non-claim (load-bearing, `122-VALIDATION.md` §"What can and cannot be sampled"):** a green
run of this gate does NOT satisfy ROADMAP criterion 4. It cannot detect an implied overclaim, a
misleading omission, or wrong tone. Criterion 4 is closed by this gate PLUS the D-16 blocking
operator wording review (plan 122-11). Any SUMMARY that reports this gate as closing criterion 4 is
wrong.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. Branch `gsd/v1.22-at28c-software-data-protection-lifecycle`.
Neither sub-repo is touched by this plan.

@.planning/REQUIREMENTS.md
@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<source_coverage_audit>
The phase-wide multi-source coverage audit (GOAL / REQ / RESEARCH / CONTEXT) lives here so it is
auditable in one place. All rows COVERED; no MISSING row.

| SOURCE | ID | Item | Plan | Status |
|--------|-----|------|------|--------|
| GOAL | — | Honest verifiable record of what was/wasn't proven | 122-05, 122-06 | COVERED |
| GOAL | — | Two community reporters get an accurate, non-overclaiming update | 122-10, 122-11, 122-12 | COVERED |
| GOAL | — | Beta-push decision made deliberately, not by accident | 122-02, 122-07 | COVERED |
| REQ | CLOSE-01 | `0x0D` stays `UNVERIFIED`, zero `support_status` change, 84-count unchanged | 122-04, 122-05, 122-06, 122-13 | COVERED |
| REQ | CLOSE-02 | gh#12 answered, gh#11 followed up, never as a verified fix | 122-01, 122-05, 122-09, 122-10, 122-11, 122-12, 122-13 | COVERED |
| REQ | CLOSE-03 | Accept/avoid/cleanup decision recorded before any push | 122-02, 122-03, 122-07, 122-08, 122-12, 122-13 | COVERED |
| RESEARCH | C-1/C-2 | Firmware inbound merge has ZERO conflicts; app conflicts in exactly 2 files | 122-03 | COVERED |
| RESEARCH | C-3 | Manual `publish.yml` dispatch is the norm (46% PyPI miss rate) | 122-08 | COVERED |
| RESEARCH | C-4 | `check_ledger.py` is pre-existing RED — never a CLOSE-01 gate | 122-04, 122-05 | COVERED |
| RESEARCH | C-5 | D-14's `No-Hazmats` answer is measured false (19/19 `DIP24_2816` REFUSED) | 122-10 (flagged divergence) | COVERED |
| RESEARCH | C-6 | Release bodies are EMPTY, so the body is an *add* via `--notes-file` | 122-09, 122-12 | COVERED |
| RESEARCH | C-7 | Only the FIRMWARE release carries `.hex` assets; app release has zero | 122-08, 122-09 | COVERED |
| RESEARCH | C-8 | `ci.yml` never runs on a `beta` push; 4 ruff findings structurally invisible | 122-07 | COVERED |
| RESEARCH | C-9 | v1.22 forked off `beta` — no archaeology surprise | 122-02 | COVERED |
| RESEARCH | C-10 | PROJECT.md has six ordinal ⚠ blocks → EIGHTH is correct | 122-06 | COVERED |
| RESEARCH | C-11/C-12 | `--ours` whole-file, superset proof, hunk-level resolution FORBIDDEN | 122-02, 122-03 | COVERED |
| RESEARCH | C-13 | `diff_db.py` interprets its own result and exits 0 | 122-04 | COVERED |
| RESEARCH | A1 | `pdr0663` firmware version / exception name UNVERIFIED | 122-10 | COVERED |
| RESEARCH | A3 | CI's cut tag is derived, not executed — read the OBSERVED tag | 122-07, 122-08, 122-09, 122-12 | COVERED |
| RESEARCH | A5 | Assert `git ls-tree HEAD firestarter firestarter_app` unchanged at phase end | 122-13 | COVERED |
| RESEARCH | Wave 0 | Forbidden-phrase scanner + planted fixture + test | 122-01 | COVERED |
| CONTEXT | D-01 | Phase publishes the beta itself, then comments | 122-07 | COVERED |
| CONTEXT | D-02 | Ask = plain write+verify first, `dev test` second and optional | 122-10 | COVERED |
| CONTEXT | D-03 | Both channels verified public before any comment | 122-08 | COVERED |
| CONTEXT | D-04 | The cut continues the beta series (auto-increment, observed tag) | 122-07, 122-08 | COVERED |
| CONTEXT | D-05 | Accept the auto-fire — the merge IS the cut | 122-02, 122-07 | COVERED |
| CONTEXT | D-06 | Conflicts resolved on the branch, gate proven there first | 122-03, 122-04 | COVERED |
| CONTEXT | D-07 | `v1.22` tag + gitlink bump stay with `/gsd-complete-milestone` | 122-02, 122-07, 122-13 | COVERED |
| CONTEXT | D-08 | Prerelease bodies hand-written, carrying the ceiling | 122-01, 122-09 | COVERED |
| CONTEXT | D-09 | New `122-LEDGER.md`; `PROTOCOL-LEDGER` verified, never edited | 122-04, 122-05 | COVERED |
| CONTEXT | D-10 | EIGHTH CORRECTION + premise-confirmed row for the community datapoint | 122-06 | COVERED |
| CONTEXT | D-11 | Ledger organised as claim-class rows carrying explicit non-claims | 122-01, 122-05 | COVERED |
| CONTEXT | D-12 | Ledger carries a what-we-chose-not-to-prove section | 122-05 | COVERED |
| CONTEXT | D-13 | gh#12 answered and left OPEN; no label flag ever sent | 122-10, 122-12 | COVERED |
| CONTEXT | D-14 | gh#12's drifted asks addressed inline, nothing split out | 122-10 | COVERED |
| CONTEXT | D-15 | gh#11 gets the full account + credit for the b11 reproduction | 122-10 | COVERED |
| CONTEXT | D-16 | Both drafts behind a blocking operator wording review, then post via `gh` | 122-11, 122-12 | COVERED |
</source_coverage_audit>

<artifacts_this_phase_produces>
Full phase manifest (repeated in every plan so no executor has to infer it). **Every path is in the
meta repo `/workspaces` unless annotated otherwise.**

**New gate code + fixtures (this plan):**
- `.planning/phases/122-…/check_permitted_claims.py` — CLI `python3 <path> [target…]`; functions
  `scan_text(text)`, `resolve_targets(argv)`, `main(argv)`; module constants `_DEFAULT_TARGETS`,
  `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERN`, `REQUIRED_CAVEAT_PROSE`; env seam
  `FIRESTARTER_CLAIMSCAN_TARGETS`
- `.planning/phases/122-…/test_check_permitted_claims.py` — helper `_run_scanner(...)` + 7 test
  functions
- `.planning/phases/122-…/fixtures/clean_control.md`
- `.planning/phases/122-…/fixtures/clean_control_second.md`
- `.planning/phases/122-…/fixtures/planted_forbidden_claim.md`
- `.planning/phases/122-…/fixtures/planted_missing_caveat.md`

**New planning artifacts (later plans):**
- `.planning/phases/122-…/122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
  `122-LEDGER.md` (122-05) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` and
  `122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` and `122-GH12-COMMENT.md` (122-10)

**Edited planning artifacts (later plans):**
- `.planning/PROJECT.md` — EIGHTH CORRECTION block (122-06)
- `.planning/REQUIREMENTS.md` — CLOSE-01/02/03 checkboxes + 3 traceability rows (122-13 only)

**Sub-repo git state (later plans, no source file authored):**
- `firestarter_app` (sub-repo): inbound merge commit; `firestarter/submit.py` + `tests/test_submit.py`
  resolved whole-file `--ours` to a ZERO diff against branch HEAD; outbound merge to `beta`; push
- `firestarter` (sub-repo): inbound merge commit (`include/version.h` auto-merged); outbound merge to
  `beta`; push

**Remote state mutated (later plans):** two GitHub `beta` refs · two GitHub prereleases (bodies added
via `gh release edit --notes-file`) · one PyPI release (`publish.yml` manual dispatch) · two GitHub
issue comments on `henols/firestarter_prom` #11 and #12.

**Deliberately NOT produced:** no `v1.22` tag, no meta gitlink bump (D-07); no edit to
`.planning/v1.16/ledger/PROTOCOL-LEDGER.{md,json}` or its `check_ledger.py` (D-09, C-4); no stable
release; no workflow-trigger edit; no deletion of the stray b12.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the forbidden-phrase / required-caveat scanner in the meta phase directory</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tools/check_no_community_support_status_write.py` — the PRIMARY analog. Read it in full and copy four things concretely: (a) the exit-code contract stated in the module docstring, (b) the env-override path seam and why it exists, (c) the fail-closed missing-target guard AND the never-vacuous `if not scanned` guard, (d) the `_print_bucket` FAIL summary capped at 20 and the PASS line that names every scanned file.
    - `/workspaces/.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/check_note_append_only.py` — the tiny meta-repo phase-local script shape (module-top path constant, `print("OK: …")`, `sys.exit(0)`). Take the *shape* from here and the *anti-hollow discipline* from the analog above.
    - `/workspaces/.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 144-152) — the normative permitted claim and forbidden claim this gate distils.
    - `/workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-VALIDATION.md` §"Wave 0 Requirements" and §"What can and cannot be sampled" — the exact contract and the explicit non-claim.
    - `/workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md` §`check_permitted_claims.py` (lines 301-433) — the four concrete excerpts to mirror.
  </read_first>
  <action>
Create the scanner as a standalone Python 3 script in the meta repo's phase directory (NOT in either
sub-repo — it validates `.planning/` artifacts, and the sub-repos must carry no v1.22-close artifact
that would need merging).

Module docstring: state the purpose, then an explicit `Exit codes:` block in the analog's style —
`0` = every resolved target exists, carries the required caveat, and contains zero forbidden matches
(a `PASS:` line naming every scanned file is printed); `1` = a target is missing from disk
(fail-closed), OR zero targets resolved (never-vacuous), OR at least one forbidden match, OR a target
lacks the required caveat (a bucketed `FAIL:` summary is printed). Two exit codes only — follow
`check_no_community_support_status_write.py`, not `diff_db.py`'s three-code variant; there is no
infra-error class here worth distinguishing.

Also state in the docstring: **this gate is the mechanizable half of ROADMAP criterion 4 only.** It
cannot detect an implied overclaim, a misleading omission, or wrong tone; criterion 4 is closed by
this gate PLUS the D-16 blocking operator wording review. A green run must never be reported as
satisfying criterion 4.

Module constants:
- `_HERE` = `os.path.dirname(os.path.abspath(__file__))`.
- `_DEFAULT_TARGETS` — an **explicit five-element list** of `os.path.join(_HERE, name)` for exactly:
  `122-LEDGER.md`, `122-RELEASE-NOTES-fw.md`, `122-RELEASE-NOTES-app.md`, `122-GH11-COMMENT.md`,
  `122-GH12-COMMENT.md`. **Never a glob and never a directory walk** — the `fixtures/` directory
  deliberately contains violating text and must never be reachable from the default target set (same
  discipline as `firestarter_app/tests/fixtures/planted_log_in_window.cpp`). Add an in-source comment
  saying exactly that.
- `FIRESTARTER_CLAIMSCAN_TARGETS` — the env-override seam, mirroring
  `check_no_community_support_status_write.py`'s `FIRESTARTER_DISP01_REPORT` and
  `check_devtest_orchestrator.py`'s `FIRESTARTER_DEVTEST_SRC`. When the variable is **present** in
  the environment it replaces `_DEFAULT_TARGETS` entirely; split on `os.pathsep`; drop empty
  segments. An explicitly-set-but-empty value must resolve to zero targets and therefore exit 1 —
  it must NOT silently fall back to the defaults. Add an in-source comment stating the seam exists so
  the paired pytest can point the checker at deliberately-violating fixtures without editing a real
  closing artifact.
- `FORBIDDEN_PATTERNS` — a list of `(label, compiled_regex)` pairs, all `re.IGNORECASE`. Use these
  eight labels and patterns exactly:
  `verified-fixed` → `verified\s+fixed` ·
  `confirmed-working` → `confirmed\s+working` ·
  `silicon-verified` → `silicon[-\s]verified` ·
  `verified-on-silicon` → `verified\s+(?:on|against)\s+(?:real\s+)?(?:at28c\w*|silicon)` ·
  `works-on-silicon` → `works?\s+on\s+(?:\w+\s+){0,2}(?:at28c\w*|silicon)` ·
  `now-works` → `now\s+works?\b` ·
  `should-now-work` → `should\s+now\s+work` ·
  `proven-on-silicon` → `proven\s+on\s+(?:\w+\s+){0,2}(?:at28c\w*|silicon)`.
  Record in an in-source comment that `122-VALIDATION.md`'s example list is prefixed "e.g.", and that
  two of its examples are deliberately **narrowed to a silicon/AT28C object** (`works on`,
  `proven on`) so the gate stays a real signal instead of firing on "works on the merged tree"; the
  unqualified `now works?` form is kept broad on purpose.
- `REQUIRED_CAVEAT_PROSE` — the canonical sentence fragment `no AT28C silicon was tested`.
- `REQUIRED_CAVEAT_PATTERN` — the same fragment as a whitespace-tolerant `re.IGNORECASE` regex
  (`no\s+AT28C\s+silicon\s+was\s+tested`). Add an in-source comment recording the deliberate
  interaction: an honest negated phrasing such as "nothing is silicon-verified here" **trips**
  `silicon-verified`, and the correct response is to reword the artifact to the canonical caveat, NOT
  to weaken the pattern set.

Functions:
- `scan_text(text)` → returns `(forbidden_hits, caveat_present)` where `forbidden_hits` is a list of
  `(label, matched_substring)` and `caveat_present` is a bool from `REQUIRED_CAVEAT_PATTERN.search`.
- `resolve_targets(argv)` → precedence: explicit positional `argv` paths win; else the env seam if
  the variable is present in `os.environ`; else `_DEFAULT_TARGETS`.
- `main(argv)` → resolve targets; if any resolved path is not `os.path.isfile`, print a `FAIL:` line
  naming every missing path and the words "the gate cannot vacuously pass with a target silently
  skipped", then exit 1; if the resolved list is empty, print a `FAIL:` never-vacuous line and exit
  1; scan every target; accumulate two violation buckets (`forbidden phrase match(es)` and `missing
  required silicon caveat`), each printed through a `_print_bucket(label, violations)` helper capped
  at 20 with an `… and N more` tail; on any violation exit 1; otherwise print a single `PASS:` line
  naming **every** scanned file as a path relative to `_HERE` plus the caveat-present count, and exit
  0. Guard `main` behind `if __name__ == "__main__": sys.exit(main(sys.argv[1:]))`.

Do not add a `--fix`, `--quiet`, or watch mode. Do not import anything outside the stdlib.

The forbidden literals below are named here only so the executor writes the pattern table correctly;
they are the gate's own vocabulary, not text destined for any closing artifact.
<!-- planner-discipline-allow: verified fixed -->
<!-- planner-discipline-allow: confirmed working -->
<!-- planner-discipline-allow: silicon-verified -->
<!-- planner-discipline-allow: now works -->
<!-- planner-discipline-allow: should now work -->
<!-- planner-discipline-allow: works on -->
<!-- planner-discipline-allow: proven on -->
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` exits **1** at this point in the phase (the five default targets do not exist yet) and its stdout names all five missing paths — this is the fail-closed guard working, not a defect.
    - `grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` ≥ 2 (the constant plus its documenting comment).
    - `grep -c 'glob\|os.walk\|rglob' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` returns **0** — the default target set is an explicit list, never discovered.
    - `python3 -c "import ast,sys; ast.parse(open('.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py').read())"` exits 0.
    - The module docstring contains both the literal `Exit codes:` and a sentence stating the gate does not by itself satisfy criterion 4 (verify by reading; `grep -c 'criterion 4'` ≥ 1).
    - `python3 .../check_permitted_claims.py <a path that does not exist>` exits 1 and prints `FAIL:`.
    - The `FORBIDDEN_PATTERNS` table has exactly 8 entries with the 8 labels named in the action.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py; python3 -c "import ast;ast.parse(open('$P').read())" && python3 "$P" /nonexistent-target.md; test $? -eq 1 && grep -qc 'Exit codes:' "$P" && test "$(grep -c 'glob\|os.walk\|rglob' "$P")" = "0" && echo SCANNER-SHAPE-OK</automated>
  </verify>
  <done>The scanner exists, parses, is fail-closed on a missing target, has no glob-based target discovery, and documents its exit codes and its explicit non-claim about criterion 4.</done>
</task>

<task type="auto">
  <name>Task 2: Commit four fixtures — two clean controls and two deliberately-violating planted files</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/clean_control.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/clean_control_second.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_forbidden_claim.md, .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/planted_missing_caveat.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (written in Task 1) — the exact `FORBIDDEN_PATTERNS` labels and `REQUIRED_CAVEAT_PROSE` value the fixtures must satisfy or violate.
    - `/workspaces/firestarter_app/tools/check_no_log_in_sdp_window.py` — its docstring's reference to `tests/fixtures/planted_log_in_window.cpp`; the precedent that the violating fixture is a **committed file**, never generated at test time.
    - `/workspaces/.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 144-152) — so the clean fixtures' prose is genuinely ceiling-compliant rather than filler.
  </read_first>
  <action>
Create a `fixtures/` subdirectory inside the phase directory and write four short markdown files.
Every one opens with an HTML comment stating: this is a deliberate test fixture for
`check_permitted_claims.py`, it is NOT a closing artifact, and it must never be added to
`_DEFAULT_TARGETS`.

- `clean_control.md` — three or four sentences of genuinely ceiling-compliant prose (paraphrase
  REQUIREMENTS.md's permitted claim: the SDP lock and unlock sequences are emitted byte-exact by
  golden register trace across all four `0x0D` pinouts, with a measured host-side timing assumption),
  containing the canonical caveat sentence fragment `no AT28C silicon was tested` verbatim and **zero**
  forbidden matches.
- `clean_control_second.md` — a second, textually different clean control, same properties. Its only
  job is to let the anti-skip test prove the PASS line names *both* files.
- `planted_forbidden_claim.md` — `clean_control.md`'s prose **plus the canonical caveat** (so the
  caveat leg is satisfied) plus exactly one added sentence that matches exactly one
  `FORBIDDEN_PATTERNS` entry. Use the `should-now-work` label's phrasing, because that is precisely
  the overclaim RESEARCH C-5 caught inside locked decision D-14 — the fixture then documents a real
  historical near-miss rather than a synthetic one. Add an HTML comment naming which label it is
  planted to trip, so the failure is attributable to the forbidden phrase alone.
- `planted_missing_caveat.md` — `clean_control.md`'s prose with the canonical caveat sentence
  **removed** and zero forbidden matches, so its failure is attributable to the absent caveat alone.

Keep each fixture under ~15 lines. Do not put any absolute `/workspaces/` path or any `/dev/tty*`
device name in any fixture (the same outward-facing hygiene the real artifacts obey).
  </action>
  <acceptance_criteria>
    - Pointing the scanner at `fixtures/clean_control.md` via `FIRESTARTER_CLAIMSCAN_TARGETS` exits **0** and prints `PASS:`.
    - Pointing it at `fixtures/planted_forbidden_claim.md` exits **1** and stdout contains both `FAIL:` and the label `should-now-work`.
    - Pointing it at `fixtures/planted_missing_caveat.md` exits **1** and stdout names the missing-required-caveat bucket.
    - Pointing it at both clean controls (`os.pathsep`-joined) exits **0** and the single `PASS:` line names **both** `clean_control.md` and `clean_control_second.md`.
    - `grep -rc '/workspaces/\|/dev/tty' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/fixtures/` reports 0 for every file.
    - Every fixture's first line is an HTML comment containing the words `test fixture`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && P=check_permitted_claims.py && FIRESTARTER_CLAIMSCAN_TARGETS=fixtures/clean_control.md python3 $P && FIRESTARTER_CLAIMSCAN_TARGETS="fixtures/clean_control.md:fixtures/clean_control_second.md" python3 $P | grep -q clean_control_second.md && ! FIRESTARTER_CLAIMSCAN_TARGETS=fixtures/planted_forbidden_claim.md python3 $P && ! FIRESTARTER_CLAIMSCAN_TARGETS=fixtures/planted_missing_caveat.md python3 $P && echo FIXTURES-OK</automated>
  </verify>
  <done>Four committed fixtures exist; the clean pair drives the scanner to 0 with both names in the PASS line, and each planted fixture drives it to 1 for its own single, attributable reason.</done>
</task>

<task type="auto">
  <name>Task 3: Pair the gate with the mandatory anti-hollow pytest (GATE-01 discipline)</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_check_no_community_support_status_write.py` — the PRIMARY analog. Read it in full: copy its docstring's stated contract (every planted-violation test injects a REAL subprocess-level violation via the env seam, never an in-process synthetic), its `_run_checker` subprocess helper shape, and its numbered coverage list.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (Task 1) — the exact env-var name, exit codes, and stdout markers to assert on.
    - The four fixtures from Task 2 — their exact filenames and which label each is planted to trip.
  </read_first>
  <action>
Write a standalone pytest module in the phase directory (no `conftest.py`, no package `__init__.py`,
no import of the scanner — the scanner is invoked as a **subprocess** so the exit code IS the
assertion). Runnable as `python3 -m pytest .planning/phases/122-…/test_check_permitted_claims.py -q`
from `/workspaces` with the system Python 3.12 / pytest 9.x (verified available; no install step).

Module docstring: state that this is the **mandatory anti-hollow pairing** for the claim gate — a
checker with no negative-fixture test is exactly this project's v1.12 hollow-GATE-03 failure mode
(GATE-01) — then a numbered coverage list matching the legs below.

Helper `_run_scanner(targets=None, argv=None)`: builds `env = {**os.environ, ...}`, sets
`FIRESTARTER_CLAIMSCAN_TARGETS` only when `targets` is not None (so the "variable absent" path is
also reachable), and returns `subprocess.run([sys.executable, str(_HERE / "check_permitted_claims.py"), *(argv or [])], cwd=str(_HERE), capture_output=True, text=True, env=env)`.
Derive `_HERE` from `pathlib.Path(__file__).parent` — never a hardcoded absolute path.

Seven test functions, each asserting on BOTH the returncode and a stdout marker, and each carrying an
assertion message that prints the captured stdout/stderr on failure:
1. `test_scanner_exits_zero_on_clean_fixture` — clean control via the seam → returncode 0, `PASS:` in
   stdout. This is also the control leg proving the injection seam itself is innocent.
2. `test_planted_forbidden_phrase_flips_checker_to_failure` — `planted_forbidden_claim.md` →
   returncode 1, stdout contains `FAIL:` and the label `should-now-work`.
3. `test_planted_missing_caveat_flips_checker_to_failure` — `planted_missing_caveat.md` →
   returncode 1, stdout names the missing-caveat bucket.
4. `test_fail_closed_on_nonexistent_target` — a path that does not exist → returncode 1, stdout says
   the target was not found.
5. `test_never_vacuous_on_explicitly_empty_target_list` — the env var set to the empty string →
   returncode 1. Assert explicitly that it did NOT fall back to the defaults (returncode is 1 and
   stdout does not contain `PASS:`).
6. `test_pass_line_names_every_scanned_file` — both clean controls joined by `os.pathsep` →
   returncode 0 and BOTH basenames present in the single `PASS:` line (anti-skip).
7. `test_positional_argv_overrides_the_env_seam` — env seam pointed at
   `planted_forbidden_claim.md` while `argv` passes `clean_control.md` → returncode 0. Pins the
   documented precedence so a future change cannot silently invert it.

Do not use `pytest.mark.skipif` anywhere. Do not assert on stderr contents. Do not add a leg that
runs the scanner against the five real default targets — they do not exist until wave 8, and leg 4
already covers the fail-closed path.
  </action>
  <acceptance_criteria>
    - `cd /workspaces && python3 -m pytest .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py -q` reports **7 passed**, 0 failed, 0 skipped.
    - `grep -c 'skipif' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py` returns **0**.
    - `grep -c 'subprocess.run' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py` ≥ 1 — the checker is exercised as a real process, not imported.
    - `grep -c '/workspaces' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py` returns **0** — paths are derived from `__file__`.
    - Deliberate-break control, run manually and recorded in the SUMMARY (revert immediately after): temporarily delete one `FORBIDDEN_PATTERNS` entry (`should-now-work`) and confirm leg 2 goes RED; restore the entry and confirm 7 passed again. This proves the test is bound to the gate's behaviour, not to the fixture's mere existence.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 -m pytest .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py -q 2>&1 | tail -3 | grep -qE '7 passed' && test "$(grep -c 'skipif' .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py)" = "0" && echo ANTIHOLLOW-OK</automated>
  </verify>
  <done>Seven subprocess-level legs pass; two of them are real planted violations that drive the gate to exit 1; the deliberate-break control was executed, observed RED, and reverted, with the observation recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `.planning/` prose → public GitHub release body / issue comment | Untrusted-by-omission text crosses into permanent public record; this gate is the machine half of that boundary check |
| test fixture → gate default target set | A deliberately-violating file lives beside the artifacts the gate protects; a glob-based target set would make the fixture self-defeating |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-01 | Information Disclosure | closing artifacts → public GitHub | high | mitigate | Fixtures carry no `/workspaces/` path and no `/dev/tty*` device name (Task 2 acceptance criterion); the scanner never echoes environment values |
| T-122-02 | Information Disclosure | overclaim reaching a stranger | high | mitigate | The 8-pattern forbidden set plus the mandatory required-caveat check; `should-now-work` is planted from the real C-5 near-miss inside D-14 |
| T-122-08 | Tampering | the gate itself (hollow-gate risk) | high | mitigate | GATE-01 pairing: two committed planted fixtures, two subprocess legs asserting exit 1, plus a recorded deliberate-break control that observes leg 2 RED and reverts |
| T-122-09 | Tampering | vacuous pass | medium | mitigate | Fail-closed on any missing target; never-vacuous on an empty resolved list; PASS line names every scanned file (anti-skip leg 6) |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | **Zero package-manager installs in this phase's scope** (`122-RESEARCH.md` §Package Legitimacy Audit: no `pip install`, no `npm install`, no `cargo add`; stdlib only). No legitimacy checkpoint is required. A plan or task proposing an install is a scope escape — reject it and re-read that section. |
</threat_model>

<verification>
1. `cd /workspaces && python3 -m pytest .planning/phases/122-close-honesty-ledger-community-ask-release-decision/test_check_permitted_claims.py -q` → 7 passed.
2. `python3 .planning/phases/122-…/check_permitted_claims.py` (no arguments, defaults) exits **1** and names five missing targets — expected and correct at this wave.
3. `FIRESTARTER_CLAIMSCAN_TARGETS=fixtures/clean_control.md python3 check_permitted_claims.py` (from the phase directory) exits 0.
4. `git -C /workspaces/firestarter status --porcelain` output is exactly `?? firestarter/` and `git -C /workspaces/firestarter_app status --porcelain` matches the pre-existing dirt named below — **this plan touches no sub-repo**.
5. `git -C /workspaces ls-tree HEAD firestarter firestarter_app` still reports `0048b3d…` and `96e0622…` (A5 — gitlinks stay pinned, D-07).

**Known pre-existing working-tree dirt — do NOT read as this plan's damage:** `firestarter` carries
untracked `firestarter/`; `firestarter_app` carries modified `.gitignore` plus untracked `.coverage`,
`.planning/config.json`, `SECURITY.md`, `write_test_port.sh`; the meta repo carries modified
`.planning/config.json` (the uncommitted four-`sub_repos` change — do NOT commit it and do NOT
iterate `planning.sub_repos`, which lists two v1.29 PY32 scratch repos outside this milestone).

**Requirement ticking:** this plan ticks **nothing** in `REQUIREMENTS.md`. CLOSE-02 is closed only in
plan 122-13, and only after both comments are posted.
</verification>

<success_criteria>
- The scanner exists in the meta phase directory with a two-code exit contract, an explicit
  five-element default target list (no glob), an env-override seam, a fail-closed missing-target
  guard, a never-vacuous guard, a capped bucketed FAIL summary, and a PASS line naming every scanned
  file.
- Four fixtures are committed; two are deliberately violating, each for one attributable reason.
- Seven subprocess pytest legs pass, including two planted-violation legs, one fail-closed leg, one
  never-vacuous leg, one anti-skip leg, and one precedence leg.
- The deliberate-break control was run, observed RED, and reverted — recorded in the SUMMARY.
- The SUMMARY states plainly that a green run of this gate does NOT satisfy ROADMAP criterion 4.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-01-SUMMARY.md`
when done. Record: the eight forbidden labels as shipped, the canonical caveat fragment, the
deliberate-break observation, and the explicit criterion-4 non-claim.
</output>
