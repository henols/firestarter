---
phase: 122-close-honesty-ledger-community-ask-release-decision
plan: 05
type: execute
wave: 4
depends_on: ["122-01", "122-04"]
files_modified:
  - .planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md
autonomous: true
requirements: [CLOSE-01, CLOSE-02]
must_haves:
  truths:
    - "A new `122-LEDGER.md` exists; `PROTOCOL-LEDGER.{md,json}` was verified and never edited (D-09)"
    - "The ledger is organised as claim-class rows, each pairing a permitted wording with an explicit statement of what it does NOT prove (D-11)"
    - "The ledger cites measured figures only — never a predicted or superseded one — and keeps the flash budget and the timing budget in separate rows"
    - "The ledger carries a section on what the milestone chose NOT to prove: SDP-F1 through SDP-F8's deferral reasons plus the two trade-offs Phase 121 owned (D-12)"
    - "The ledger records the emission-traced-versus-operation-permitted distinction with the measured 43 ALLOW / 41 REFUSE split and `DIP24_2816` at 0 of 19, so 'all four pinouts' cannot read as broader capability than shipped"
    - "The ledger is the single source of the permitted wording the release bodies and both comment drafts must match (constraint 5) — it exists before any of them is written"
    - "The ledger passes the Wave 0 claim scanner, and the ledger itself states that a green scan does not satisfy ROADMAP criterion 4"
  artifacts:
    - path: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md"
      provides: "The v1.22 honesty ledger — claim classes with permitted wording and explicit non-claims, the measured-figure register, and the negative space"
      contains: "does NOT prove"
      min_lines: 120
  key_links:
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md"
      to: ".planning/REQUIREMENTS.md"
      via: "§Validation Ceiling's permitted claim and forbidden claim are the normative source the claim classes distil; quoted verbatim, never rewritten"
      pattern: "Permitted claim|Forbidden claim"
    - from: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md"
      to: ".planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md"
      via: "every CLOSE-01 figure in the ledger is cross-referenced to the merged-tree measurement that produced it"
      pattern: "122-NONREGRESSION"
---

<objective>
Write `122-LEDGER.md`: the v1.22 honesty ledger. Claim-class rows, each pairing the wording this
milestone is permitted to use with an explicit statement of what that wording does **not** prove
(D-11), plus a measured-figure register, plus a section on what the milestone deliberately chose not
to prove (D-12).

Purpose: this is the artifact that makes the permitted-versus-forbidden line **auditable** instead of
re-derived. CONTEXT constraint 5 makes it the single source of the permitted wording that the two
prerelease bodies and both community comment drafts must all match — so it is written before any of
them.

Output: `.planning/phases/122-…/122-LEDGER.md`. `PROTOCOL-LEDGER.{md,json}` is **verified, never
edited** (D-09) — CLOSE-01 asks that `0x0D` *stays* `UNVERIFIED`, which is a check, and 122-04 already
performed it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Repo: **meta** (`/workspaces`) only. No sub-repo file is read for content beyond what the measurements
in 122-04 already produced.

@.planning/REQUIREMENTS.md
@.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Meta repo (`/workspaces`) — new:** `check_permitted_claims.py` + `test_check_permitted_claims.py` +
4 `fixtures/*.md` (122-01) · `122-DECISION.md` (122-02) · `122-NONREGRESSION.md` (122-04) ·
`122-LEDGER.md` (**this plan**) · `122-CHANNELS.md` (122-08) · `122-RELEASE-NOTES-fw.md` +
`122-RELEASE-NOTES-app.md` (122-09) · `122-GH11-COMMENT.md` + `122-GH12-COMMENT.md` (122-10).
**Meta repo — edited:** `.planning/PROJECT.md` EIGHTH CORRECTION (122-06) · `.planning/REQUIREMENTS.md`
ticks (122-13 only).
**Sub-repo git state:** inbound merges (122-03), outbound merges + pushes (122-07). No source authored.
**Remote state:** two `beta` refs · two prerelease bodies · one PyPI release · two issue comments.
**Deliberately NOT produced:** no `v1.22` tag, no gitlink bump (D-07); **no edit to
`.planning/v1.16/ledger/PROTOCOL-LEDGER.{md,json}` and no `check_ledger.py` run or fix** (D-09, C-4);
no `REQUIREMENTS.md` wording edit — mechanism corrections are recorded in this ledger instead
(Pattern 2); no `support_status` change; no stable release.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write the ledger header, the permitted-versus-non-claim key, and the eight claim-class rows</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Validation Ceiling" (lines 144-152) — the normative permitted claim and forbidden claim, to be quoted **verbatim** and then distilled into claim classes. Also read the per-requirement parentheticals for LOCK-01..06 and HOST-01..06 (lines 66-80), which already carry the evidence each class summarises.
    - `.planning/v1.16/ledger/PROTOCOL-LEDGER.md` lines 1-40 — **structure only, read-only.** Copy the shape: a pinned-provenance header block (including its "version string caveat" and "composes with (cross-reference only — no data copied)" clauses), one wide table, then a status-key legend.
    - `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-NONREGRESSION.md` §1 "The claim, as precise statements" — the project's established way of writing a claim so it is individually checkable. This is the closest in-tree analog to D-11's permitted-wording/non-claim pairing.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Measured Figures the Ledger Must Cite" (lines 730-768) — every figure with its source, including the budget-conflation correction.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` (written by 122-04) — the merged-tree results each claim class cross-references.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-CONTEXT.md` §D-09, §D-11 — the shape constraints and what is left to discretion.
  </read_first>
  <action>
Create `122-LEDGER.md` in the meta phase directory.

**Header block** (mirror `PROTOCOL-LEDGER.md:1-14`'s provenance pattern, adapted): milestone name;
**two pinned commits** — the firmware and host HEADs at 122-03's inbound merge, taken from
`122-03-SUMMARY.md` — plus the published cut tag left as a field to be filled by plan 122-08 once
observed (A3: read the observed tag, never assume `3.0.0b14`); a **version-string caveat** line in
`PROTOCOL-LEDGER`'s own idiom, recording that the version strings in the merged tree read `3.0.0b13`
and are *not* the identity of what was published — the commits and the observed tag are; the oracle
(software-only: native register trace, host pytest, source-scan gates, plus three-board host-side
timing measurements); the generation date; and a **"composes with (cross-reference only — no data
copied)"** list naming `.planning/REQUIREMENTS.md` §Validation Ceiling, `122-NONREGRESSION.md`,
`119-MEASUREMENT.md`, `120-SDP-PARTITION.md` and `.planning/v1.16/ledger/PROTOCOL-LEDGER.md`. State
explicitly that `PROTOCOL-LEDGER` is **referenced and verified, never edited** (D-09), and that its own
header pins firmware `a296195`, an ancestor of neither `beta` nor the live line — which is precisely
why editing it was refused.

**Claim-class table.** One row per claim class. Columns: `Class` · `Permitted wording` · `Evidence
(measured, with source)` · `Explicitly does NOT prove`. Write exactly these eight classes (D-11), in
this order:

1. **Per-pinout emission byte-exactness.** Permitted: the SDP lock and unlock sequences are emitted
   exactly as specified, verified byte-exact by golden register trace across all four `0x0D` pinouts.
   Does NOT prove: that any silicon accepted the sequence, entered or left the protected state, or that
   the magic addresses are correct for every family member — SDP-F7 leaves them UNVERIFIED for
   AT28C040 / AT28C16 / AT28C04. Cross-reference the trace-coverage figure honestly as **66 of 84**,
   never "all 84".
2. **Measured host-side timing (the SDP unlock emitter).** Permitted: the emitted sequence's host-side
   duration is measured, per board, against a documented budget of 600 µs (`6 × AT28C_TBLC_MAX_US`) —
   Leonardo **568 µs**, Uno **412 µs**, uno328pb **424 µs**. Note F-118-01's separately measured
   **572 µs** on the Leonardo as a second observation of the same emitter and board class: **cite one,
   note the other, never average them.** Does NOT prove: that `t_BLC` is met *as accepted by the die*.
3. **Page-load per-byte interval (context, not a gate).** Permitted: the worst per-byte page-load
   interval is measured — Uno **84 µs**, Leonardo **88 µs** — against the 100 µs datasheet maximum.
   Record that Phase 119 D-16 explicitly declined a runtime budget check on this path, so these are
   context. Does NOT prove: any silicon-side timing conformance, and the Leonardo figure is not
   directly comparable to the Uno-class figures (its measurement folds in a page-boundary crossing —
   `119-MEASUREMENT.md` §1/§4 names the two kinds of number so they are never compared as if they
   measured the same thing).
4. **`0x0D`-scoped fail-closed refusal.** Permitted: lock and unlock are fail-closed for any
   `protocol != 0x0D`, machine-checked. Record LOCK-04's mechanism correction: shipped as **one generic
   op-layer NULL-`main` refusal** in `operation_utils.cpp`, **not** the requirement's literal `default:`
   arm in `configure_eeprom28c` — which would have refused read and verify on all 84 `0x0D` chips.
   Does NOT prove: anything about behaviour on a real `0x0D` part.
5. **`DEV_TOOLS` invariance of the admission guard.** Permitted: the explicit `is_memory_cmd()`
   predicate is proven identical with and without `-D DEV_TOOLS`, by a two-env truth table over all 256
   `cmd` values plus a brace-matched source-scan gate with a planted-violation fixture. Does NOT prove:
   anything about SDP itself; this is a prerequisite invariant.
6. **Other protocol families byte-identical.** Permitted: the `0x05`/`0x06`/`0x07`/`0x10`/SRAM golden
   traces are byte-identical, and `flash_utils.{h,cpp}`, `flash_5v_page.cpp` and
   `flash_nor_unlock.cpp` are byte-untouched. Does NOT prove: that the `0x0D` change is correct — only
   that it is contained.
7. **The host refuses before the wire.** Permitted: a pre-wire capability refusal keeps SDP commands
   away from `0x0D` parts with no SDP command decoder, as a **fail-closed allow-list** derived from
   minipro `infoic.xml` `INFOIC2PLUS` `flags` bit 15 — **43 ALLOW / 41 REFUSE** across the 84 chips,
   with zero `chip_database.json` change. Does NOT prove: that the partition is correct per family
   (the ceiling names this explicitly as not provable without parts), nor that any refused part is
   actually SDP-incapable in silicon. Name `120-WATCHLIST.md`'s nine residual-risk entries.
8. **The defect is silicon-confirmed by community report; the fix is not.** Permitted: on 2026-07-27 a
   community reporter running `3.0.0b11` on a real AT28C256 reproduced the exact inverted-check INIT
   abort this milestone predicted in software — raising Phase 116's TRACE-06 from software-predicted to
   community-corroborated. State the provenance honestly: an issue-comment paste, no captured logs
   beyond the text, board revision and firmware build unconfirmed. Does NOT prove: that the **fix**
   works; `0x0D` stays `UNVERIFIED`; zero chips changed `support_status`. This asymmetry — premise
   confirmed, fix unproven — is the sharpest honesty statement in the close and must be stated in
   exactly those terms.
9. **Flash budget (a separate row from timing, deliberately).** Permitted: the Leonardo flash delta is
   reported and fits — final Leonardo `26072/28672` = **2600 B free**, judged against the live
   phase-base headroom of **2992 B**, with `-D DEV_TOOLS` (the tighter, binding configuration) costing
   **1292 B**; Uno `23932/32256`, uno328pb `23976/32384`, compared by **delta only** because their
   capacities differ by bootloader reservation. Record that LOCK-06's `3348 B` is a **superseded
   pre-117 figure**. Does NOT prove: any timing property — `119-MEASUREMENT.md:33-34` records that
   LOCK-06 is a **flash** budget and F-118-01 is a **timing** budget, that they are different budgets,
   and that a prior PROJECT.md directive conflated them. **Never merge rows 2/3 and row 9 into one
   claim.**

(Nine rows, because D-11's "roughly eight classes" splits the timing claim into the emitter measurement
and the page-load measurement — those have different sources and different dispositions. Record that
deviation and its reason in the SUMMARY.)

**Status / claim key legend**, in `PROTOCOL-LEDGER.md:31-36`'s style but adapted to this ledger's
purpose: define `PERMITTED` (a wording backed by a measured, re-runnable software artifact),
`CONTEXT-ONLY` (measured but explicitly not a gate), `COMMUNITY-CORROBORATED` (a real-silicon datapoint
from a third party, provenance stated, not reproducible on this bench), and `FORBIDDEN` (the ceiling's
forbidden claim, which appears in this ledger only as a quotation of what is not claimed).

Quote REQUIREMENTS.md's permitted claim and forbidden claim **verbatim** in a short preamble above the
table, attributed to `.planning/REQUIREMENTS.md` §"Validation Ceiling", so no downstream artifact has
to re-derive them.
  </action>
  <acceptance_criteria>
    - `122-LEDGER.md` exists with a pinned-provenance header naming both inbound-merge commit SHAs, a version-string caveat, a `composes with` cross-reference list, and an explicit statement that `PROTOCOL-LEDGER` is verified and never edited.
    - The claim-class table has **9** rows with the four columns named in the action, and every row's "does NOT prove" cell is non-empty.
    - REQUIREMENTS.md's permitted claim and forbidden claim both appear verbatim, attributed.
    - `grep -c 'all 84' 122-LEDGER.md` returns **0**; `grep -c '66 of 84' 122-LEDGER.md` ≥ 1.
    - `grep -c '3348' 122-LEDGER.md` ≥ 1 **and** every occurrence is labelled superseded (verify by reading each hit).
    - The flash row and the timing rows are separate rows; no single row states both a byte figure and a microsecond figure as one budget.
    - `grep -c '2600' 122-LEDGER.md` ≥ 1, `grep -c '568' 122-LEDGER.md` ≥ 1, `grep -c '572' 122-LEDGER.md` ≥ 1, and the artifact states that the two timing figures are not averaged.
    - `grep -c '43' 122-LEDGER.md` ≥ 1 and `grep -c '41' 122-LEDGER.md` ≥ 1 for the ALLOW/REFUSE split.
    - The status key defines all four of `PERMITTED`, `CONTEXT-ONLY`, `COMMUNITY-CORROBORATED`, `FORBIDDEN`.
    - `git -C /workspaces status --porcelain -- .planning/v1.16/ledger/` is **empty**.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && L=122-LEDGER.md && test -f "$L" && test "$(grep -c 'all 84' $L)" = "0" && grep -q '66 of 84' $L && grep -q 'does NOT prove' $L && grep -q '2600' $L && grep -q '568' $L && grep -q '572' $L && grep -q 'COMMUNITY-CORROBORATED' $L && test -z "$(git -C /workspaces status --porcelain -- .planning/v1.16/ledger/)" && echo LEDGER-CLASSES-OK</automated>
  </verify>
  <done>The ledger header, the verbatim ceiling quotation, the nine claim-class rows with non-empty non-claim cells, and the four-value status key all exist; measured figures only; the two budgets are separate; `PROTOCOL-LEDGER` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the pinout-composition section (emission-traced vs operation-permitted) and the mechanism-corrections record</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-NONREGRESSION.md` — the measured four-pinout ALLOW/REFUSE table and the named emission-traced-vs-operation-permitted finding from 122-04 Task 1.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"The four `0x0D` pinouts named by the ceiling — with counts" (lines 538-556) — the table and the block-quoted warning that this is the ledger's most important row.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Corrections to CONTEXT.md / ROADMAP Framings" (lines 266-294) — C-1, C-2, C-4, C-5, C-10, C-11, C-12, C-13, the corrections this ledger records.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RESEARCH.md` §"Architecture Patterns" → Pattern 2 (lines 828-833) — record a mechanism correction in phase artifacts; never edit `REQUIREMENTS.md`.
    - `.planning/STATE.md` lines 32-80 — the derived-partition banner, so the ledger's provenance for the 43/41 split matches the project's own record.
  </read_first>
  <action>
Append two sections to `122-LEDGER.md`.

**Section: `## The four `0x0D` pinouts — composition, and why "all four pinouts" needs a qualifier`.**
Reproduce the measured table from 122-04: pinout · chip count · SDP ALLOW · SDP REFUSE, with rows
`DIP28_28C64` 35/15/20, `DIP24_2816` 19/**0**/**19**, `DIP32_28C512_EEPROM` 18/18/0, `DIP28_28C256`
12/10/2, total 84/43/41. Then state the distinction as a first-class finding, in its own paragraph:
*emission traced byte-exact for a pinout* and *the operation permitted on parts with that pinout* are
**different claims**; for `DIP24_2816` the first is true and the second is false for all 19 chips. A
ledger row (or a release body, or a public comment) that says "all four `0x0D` pinouts" without this
qualifier reads as broader capability than shipped.

Record the two reasons `DIP24_2816`'s refusal is *correct* rather than a gap: on a part with no SDP
command decoder the sequence is not inert — post-Phase-117 the command writes reach silicon, so
`0xAA`/`0x55`/`0xA0` would be stored **as data** at bus-truncated magic addresses; and SDP-F7 (magic
addresses unverified for AT28C16 / AT28C04) plus SDP-F8 (`DIP24_2816` carries no `static-high-pins`)
both name that exact family. Also record the honest nuance SDP-F8 needs: the 28- and 32-pin `0x0D`
pinouts carry no `static-high-pins` either, so the asymmetry is specifically among **24-pin** pinouts
sharing that socket position — which is why SDP-F8 says to confirm against the shield schematic before
acting.

Cite the derived provenance precisely: minipro `infoic.xml`, section `<database type="INFOIC2PLUS">`,
`flags` bit 15 (`0x8000`, `MP_PROTECT_AFTER`) at commit
`a8efaedc236c1d9718bd28299dfbb99536b010ff`, all 84 matched, zero unmatched, zero MIXED — superseding
Phase 120 RESEARCH's curated 37/47 and the interim ~74/10 placeholder. Note that derivation makes the
partition **reproducible**, not **bench-verified**.

**Section: `## Mechanism corrections recorded here, not in REQUIREMENTS.md`.** This is Pattern 2: when
a requirement's or a decision's stated mechanism turns out narrower or wrong, satisfy the intent,
record the correction in the phase record, and leave the requirement text alone. One short entry each:
- **CLOSE-01's implied `check_ledger.py` (C-4).** That tool exits 1 today with two `LEDGER-01`
  violations, because rows `0x05`/`0x06` carry `matrix_family` `flash4`/`flash3` and v1.19 Phase 104
  renamed those to `5v_page`/`nor_unlock`. `tools/validation_matrix_spec.json` is not in the
  `beta...HEAD` diff, so this is pre-existing and not v1.22's damage. CLOSE-01's text does not mention
  the tool; it was **not** run as a gate and was **not** fixed, because fixing it would edit a closed
  milestone's artifact (D-09). The `0x0D` row's own join key is present and valid. Recommend a backlog
  seed.
- **D-06's stated conflict set (C-1/C-2).** The firmware inbound merge had **zero** conflicts; the app
  conflicted in exactly two files, `firestarter/submit.py` and `tests/test_submit.py`.
  `include/version.h` and `firestarter/__init__.py` both **auto-merged** and were never conflicts. The
  resolution was whole-file `--ours`, justified by a mechanical superset proof and proven by an empty
  diff — hunk-level resolution was forbidden because hunks 3 and 4 sandwich a shared region branch HEAD
  needs twice, and a textual "ours" there produces code that compiles and passes while being wrong
  (C-11/C-12).
- **D-14's `No-Hazmats` answer (C-5).** Flag this as the phase's one **locked-decision divergence**:
  D-14 prescribed telling that reporter their AT28C parts should now work; measured over all 84 `0x0D`
  entries, every 2K×8 part sits on `DIP24_2816` and all 19 of 19 are REFUSED. The corrected answer is
  owned by plan 122-10 and is phrased **by size class, not by an assumed part number**. Record here
  that CONTEXT D-14 said one thing, research proved another, and the plan does the latter — so the
  divergence is visible in the evidence record rather than buried in a draft.
- **C-6/C-7's release mechanics.** Both prior release bodies are **empty strings**, not generated
  commit lists, so writing a body is an *add* via `gh release edit --notes-file`. Only the **firmware**
  release carries assets (three `.hex` files); the app GH release carries zero and PyPI is the app's
  sole distribution channel.
- **C-13's `diff_db.py` reading.** The tool interprets its own result and exits 0 on the two explained
  changes; identity means *still exactly 2 explained `PGSZ_PAGE_SIZE` changes, 0 new, 0 removed*, not
  zero diff.

State at the top of this section that **`REQUIREMENTS.md`'s wording is deliberately not edited** — the
same discipline applied to LOCK-04, LOCK-06, HOST-04 and Phase 121's D-06/D-17.
  </action>
  <acceptance_criteria>
    - The pinout-composition table is present with all four rows and the 84/43/41 totals, and `DIP24_2816` reads 19 chips / 0 ALLOW / 19 REFUSE.
    - The emission-traced-vs-operation-permitted distinction is stated as its own paragraph, not a parenthetical.
    - `grep -c 'a8efaedc236c1d9718bd28299dfbb99536b010ff' 122-LEDGER.md` ≥ 1 — the derived provenance is pinned to a commit.
    - The mechanism-corrections section names C-4, C-1/C-2, C-5, C-6/C-7 and C-13 with one entry each, and opens with the statement that `REQUIREMENTS.md` is deliberately not edited.
    - The C-5 / D-14 divergence is present and explicitly labelled as a locked-decision divergence, naming both what D-14 said and what was measured.
    - `git -C /workspaces status --porcelain -- .planning/REQUIREMENTS.md` is **empty** — this plan does not touch that file.
    - `grep -c 'SDP-F7' 122-LEDGER.md` ≥ 1 and `grep -c 'SDP-F8' 122-LEDGER.md` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && L=122-LEDGER.md && grep -q 'DIP24_2816' $L && grep -q 'a8efaedc236c1d9718bd28299dfbb99536b010ff' $L && grep -q 'SDP-F7' $L && grep -q 'SDP-F8' $L && grep -qi 'divergence' $L && test -z "$(git -C /workspaces status --porcelain -- .planning/REQUIREMENTS.md)" && echo LEDGER-PINOUTS-AND-CORRECTIONS-OK</automated>
  </verify>
  <done>The pinout composition and the emission-vs-permission distinction are recorded with derived provenance; five mechanism corrections including the flagged C-5/D-14 locked-decision divergence are recorded in the ledger; `REQUIREMENTS.md` is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add D-12's negative space, run the claim scanner, and review the whole ledger against the ceiling line by line</name>
  <files>.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Future Requirements" (lines 108-123) — SDP-F1 through SDP-F8 with their stated deferral reasons, and the Out of Scope table (lines 127-140).
    - `.planning/phases/121-dev-test-fix-gates-docs-redesign/121-CONTEXT.md` — D-03 and D-16, the two trade-offs Phase 121 recorded and owned, in the reporter's own terms.
    - `.planning/REQUIREMENTS.md` line 97 (GATE-02's closing note about `doc/lockable-proms.md` being first-committed with deliberately no provenance header — an owned trade-off, not to be re-opened).
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-VALIDATION.md` §"What can and cannot be sampled — stated explicitly" — the three-way split this ledger must reflect.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/check_permitted_claims.py` (122-01) — the forbidden pattern labels and the canonical required-caveat fragment.
  </read_first>
  <action>
Append the final two sections and then gate the whole artifact.

**Section: `## What this milestone chose not to prove` (D-12).** A close that lists only wins reads as
overclaiming even when every individual claim is true. Two subsections:
- **Deferred by decision or on research grounds — SDP-F1 to SDP-F8, one line each**, each carrying its
  own stated reason from `REQUIREMENTS.md` §Future Requirements: `--sdp-relock` (with the constraint if
  it returns — an unconditional step in a conditional pipeline is the Phase-112 `if destructive:`
  lesson repeating, and a re-lock after a failed verify strands the user with a locked chip);
  the three-field SDP report shape (HOST-05 retains the honesty floor); a `dev test` SDP step
  (perturbs `dedup_fingerprint` for all 84 `0x0D` chips and triples the blast radius); write-probe SDP
  inference (destructive, and an honest-labelling design problem); the datasheet 6-byte software
  chip-erase (an *erase* feature riding an *SDP* milestone, needing `OE = V_H = 12 V` — the
  T-93-CANERASE hazard class); the AT29C / SST39SF / W29EE families (multiplies the no-silicon problem
  across settled bench evidence); datasheet verification of the SDP magic addresses for AT28C040 /
  AT28C16 / AT28C04 (recorded UNVERIFIED rather than assumed); and `DIP24_2816`'s missing
  `static-high-pins` (19 chips with `static_high_mask == 0`; the remap fix does not address it; confirm
  against the shield schematic before acting).
- **Two trade-offs Phase 121 recorded and owned.** First: an off-TTY `dev test` writes silicon with
  nobody consenting — state it plainly as an accepted consequence of the zero-option redesign, not as a
  defect discovered here. Second: `doc/lockable-proms.md` ships roughly 300 datasheet-compiled rows
  with **no provenance header**, deliberately, as an owned trade-off that is explicitly not to be
  re-opened.
Also record the one trade-off **this** phase owns: D-01 declined a bench smoke-test of the b14
install/flash path, so the `pip install --pre` → `fw -i` → one-live-op chain Phase 115 existed to prove
is trusted rather than re-verified. Name it as a known, accepted gap.

**Section: `## What no test, gate or review in this phase can close`.** Reproduce
`122-VALIDATION.md`'s three-way split in the ledger's own voice, because this is the ledger's whole
point:
- **Mechanically checkable** — every CLOSE-01 sub-claim, every CLOSE-03 sub-claim, and CLOSE-02's
  *delivery* facts. Cheap, deterministic, re-runnable.
- **Requires the blocking operator review (D-16)** — whether the prose is *honest*, not merely free of
  banned strings. A string scan cannot detect an implied overclaim, cannot judge whether omitting the
  `DIP24_2816` refusal misleads a reader, and cannot weigh tone. **State explicitly that a green
  claim-scan does NOT satisfy ROADMAP criterion 4.**
- **Inherently unverifiable in-phase** — that silicon enters or leaves the protected state; that `tBLC`
  is met as accepted by the die; that the gh#11 symptom is gone; that the capability partition is
  correct per family. `0x0D` stays `UNVERIFIED` precisely because these are open. **Sampling rate for
  this class is zero, permanently, by design.** The single asymmetry: the defect is
  community-corroborated on real AT28C256 silicon; the fix is not.

**Then gate the artifact.** Run the Wave 0 scanner against `122-LEDGER.md` via the env seam and require
exit 0. The ledger must contain the canonical required-caveat fragment the scanner checks for. If the
scanner flags a phrase, **reword the ledger** — never weaken the pattern set, and never add the ledger
to an exclusion list. Note the one deliberate interaction the scanner documents: an honest negated
phrasing built on the `silicon-verified` string still trips the gate, and the correct fix is to use the
canonical caveat wording instead.

**Finally, review the whole ledger line by line against the ceiling** and record the review in the
SUMMARY: confirm zero affirmative silicon-validation claims, confirm every figure is measured rather
than predicted, confirm no superseded figure is cited except where explicitly labelled superseded, and
confirm the phrase "all 84" appears nowhere.
  </action>
  <acceptance_criteria>
    - All eight SDP-F ids (SDP-F1 … SDP-F8) appear, each with a one-line reason.
    - Both Phase 121 owned trade-offs appear, plus this phase's own D-01 no-bench-smoke-test trade-off.
    - The three-way split section exists and contains the literal statement that a green claim-scan does not satisfy criterion 4, and the statement that the sampling rate for the unverifiable class is zero, permanently, by design.
    - `FIRESTARTER_CLAIMSCAN_TARGETS=<abs path to 122-LEDGER.md> python3 .planning/phases/122-…/check_permitted_claims.py` exits **0** and its PASS line names `122-LEDGER.md`.
    - The ledger contains the canonical required-caveat fragment the scanner enforces.
    - `grep -c 'all 84' 122-LEDGER.md` returns **0**.
    - `wc -l < 122-LEDGER.md` ≥ 120.
    - The line-by-line ceiling review is recorded in the SUMMARY with its verdict (zero affirmative silicon-validation claims).
    - `git -C /workspaces status --porcelain -- .planning/v1.16/ledger/ .planning/REQUIREMENTS.md` is **empty**.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/122-close-honesty-ledger-community-ask-release-decision && L=122-LEDGER.md && for f in SDP-F1 SDP-F2 SDP-F3 SDP-F4 SDP-F5 SDP-F6 SDP-F7 SDP-F8; do grep -q "$f" $L || { echo "MISSING $f"; exit 1; }; done && grep -q 'criterion 4' $L && test "$(wc -l < $L)" -ge 120 && FIRESTARTER_CLAIMSCAN_TARGETS="$PWD/$L" python3 check_permitted_claims.py | grep -q 122-LEDGER.md && test -z "$(git -C /workspaces status --porcelain -- .planning/v1.16/ledger/ .planning/REQUIREMENTS.md)" && echo LEDGER-COMPLETE-OK</automated>
  </verify>
  <done>The ledger's negative space records all eight SDP-F deferrals and all three owned trade-offs; the three-way sampling split is stated including the explicit criterion-4 non-claim; the artifact passes the Wave 0 scanner with its name in the PASS line; the line-by-line ceiling review is recorded; `PROTOCOL-LEDGER` and `REQUIREMENTS.md` are both untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence → permitted wording | The ledger is where a measurement becomes a sentence someone may quote publicly; a wrong distillation here propagates to two release bodies and two public comments |
| closed-milestone artifact → this phase | `PROTOCOL-LEDGER.{md,json}` is read-only (D-09); editing it makes a stale firmware pin newly load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-122-02 | Information Disclosure | permitted-wording distillation | high | mitigate | Every claim class carries an explicit "does NOT prove" cell; the pinout-composition section forces the emission-vs-permission qualifier; the Wave 0 scanner gates the artifact; the D-16 review reads it in 122-11 |
| T-122-14 | Tampering | `PROTOCOL-LEDGER.{md,json}` | medium | mitigate | Verified-not-edited (D-09); `git status --porcelain -- .planning/v1.16/ledger/` asserted empty in every task; `check_ledger.py` never invoked (C-4) |
| T-122-16 | Tampering | citing a predicted or superseded figure as measured | medium | mitigate | Measured-not-predicted discipline: 3348 B labelled superseded, 568/572 µs cited separately and never averaged, "all 84" grep-asserted absent, flash and timing budgets kept in separate rows |
| T-122-17 | Repudiation | a locked-decision divergence applied silently | high | mitigate | The C-5/D-14 divergence is recorded in the ledger as an explicit locked-decision divergence naming both the decision and the measurement, so the D-16 operator review can accept or overturn it |
| T-122-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in phase scope (`122-RESEARCH.md` §Package Legitimacy Audit). An install proposal is a scope escape |
</threat_model>

<verification>
1. `122-LEDGER.md` exists, ≥120 lines, with header provenance, the verbatim ceiling quotation, nine
   claim-class rows each with a non-empty non-claim cell, and the four-value status key.
2. The pinout-composition section carries the measured 84/43/41 table and the emission-vs-permission
   paragraph.
3. Five mechanism corrections recorded, including the flagged C-5/D-14 locked-decision divergence.
4. All eight SDP-F deferrals plus three owned trade-offs recorded.
5. The three-way sampling split is stated, including that a green claim-scan does not satisfy
   criterion 4 and that one claim class has sampling rate zero by design.
6. The Wave 0 scanner exits 0 on the ledger and names it in the PASS line.
7. `git -C /workspaces status --porcelain -- .planning/v1.16/ledger/ .planning/REQUIREMENTS.md` empty.
8. Both sub-repo trees still show only the named pre-existing dirt; nothing pushed.

**Requirement ticking:** this plan ticks **nothing**. CLOSE-01 and CLOSE-02 close only in 122-13.
</verification>

<success_criteria>
- One committed meta-repo ledger that makes the permitted-versus-forbidden line auditable, citing
  measured figures only.
- Every claim class pairs a permitted wording with an explicit non-claim (D-11).
- The negative space is recorded (D-12), including the one trade-off this phase itself owns.
- The C-5/D-14 locked-decision divergence is visible in the evidence record, not buried.
- The ledger is scanner-clean and states its own criterion-4 non-claim.
</success_criteria>

<output>
Create `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-05-SUMMARY.md`
when done. Record: the nine-row deviation from D-11's "roughly eight" and its reason, the line-by-line
ceiling review verdict, the scanner result, and the C-5/D-14 divergence as a flagged item for the
operator review in 122-11.
</output>
