---
phase: 27-root-cause-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - .planning/v1.6-EVIDENCE.md
  - firestarter/platformio.ini
  - firestarter/src/boards/leonardo_rurp_shield.cpp
autonomous: false
executes_only_if: needs_bench
requirements:
  - RCA-01
  - RCA-02
  - RCA-03
tags:
  - rca
  - leonardo
  - read-bug
  - bench
  - conditional
  - wave-b
user_setup:
  - service: operator-bench
    why: "Wave B requires the operator to be on bench with the Leonardo board (/dev/ttyACM1) + Rev 2.0 (or modified Rev 0 + voltage-divider) RURP shield + W27C512 (0xda01) chip in socket, AND a PlatformIO-capable workstation to flash the instrumented build."
    dashboard_config:
      - task: "Have the Leonardo board powered + USB-tethered to /dev/ttyACM1 (or operator's equivalent path)"
        location: "Operator workstation USB"
      - task: "Have the W27C512 chip from the Phase 26 baseline in the RURP socket (same physical chip — do NOT swap; the H2 evidence depends on the same partially-erased cell distribution)"
        location: "RURP shield DIP-28 socket"
must_haves:
  truths:
    - "Wave B fires ONLY IF Plan 27-01's Wave A verifier emitted `needs_bench: true`. Per RESEARCH HIGH-confidence finding, this trigger does NOT fire and Wave B stays parked."
    - "If activated: an instrumented firmware build runs on Leonardo and produces log output that confirms (or refutes) the H2 (data-bus address-LSB-bleed) hypothesis with higher specificity than Wave A's binary-mining evidence."
    - "If activated: the RCA narrative in .planning/v1.6-EVIDENCE.md gains a Wave B addendum citing the instrumented-build log evidence + (if a new evidence row is appended) the D-08 9-column row schema is honored verbatim."
    - "Per D-03 + D-12: the firmware sub-repo branch `firestarter/v1.6-read-bug` is cut from `beta@3.0.0b4` (= commit d955846 per RESEARCH verification) ONLY at the start of this plan; no commits to `beta` directly."
  artifacts:
    - path: ".planning/v1.6-EVIDENCE.md"
      provides: "Wave B addendum subsection under the existing ## Phase 27 — RCA Findings section"
      contains: "### Wave B addendum"
    - path: "firestarter/platformio.ini"
      provides: "`-D RCA_INSTRUMENT_*` build flags inside the [env:leonardo] block (Wave B only)"
      contains: "RCA_INSTRUMENT"
    - path: "firestarter/src/boards/leonardo_rurp_shield.cpp"
      provides: "instrumentation hooks wrapping rurp_read_data_buffer + rurp_set_data_input under #ifdef RCA_INSTRUMENT_*"
      contains: "#ifdef RCA_INSTRUMENT"
  key_links:
    - from: ".planning/v1.6-EVIDENCE.md (Wave B addendum)"
      to: "Bench log of the instrumented run (operator captures into .planning/v1.6/consistency-check-runs/{chip}-leonardo-{timestamp}-rca/)"
      via: "inline backticked path citation"
      pattern: "consistency-check-runs.*rca"
---

<objective>
**THIS PLAN IS DRAFTED BUT DOES NOT EXECUTE BY DEFAULT.**

Wave B is a conditional safety valve per CONTEXT D-01 / D-07. The plan exists in the workflow so the executor can activate it at runtime IF AND ONLY IF Plan 27-01's Task 2 §"Wave A verifier decision" emitted `needs_bench: true`. Per RESEARCH §"Wave B Trigger Criteria" (HIGH confidence), none of the three D-01 escalation triggers (T1: ≥2 hypotheses with similar weight; T2: candidate fix with non-trivial GATE-1.6 risk; T3: binaries internally inconsistent with desk-side hypothesis) fires — so Wave A is expected to close Phase 27 and this plan stays parked.

If activated: cut `firestarter/v1.6-read-bug` from `beta@3.0.0b4`, add `-D RCA_INSTRUMENT_*` build flags to the `[env:leonardo]` block of `firestarter/platformio.ini`, instrument `rurp_read_data_buffer` + `rurp_set_data_input` to log pre/post-NOP byte values + address-bus state, flash the Leonardo via `firestarter fw -i --pre` (after cutting a one-off RCA pre-release tag) or direct `pio run -t upload -e leonardo`, rerun the consistency-check tool, append findings to the existing `## Phase 27 — RCA Findings` section in `.planning/v1.6-EVIDENCE.md` as a `### Wave B addendum` subsection (with a new evidence row using the D-08 9-column schema).

Purpose (if activated): produce instrumented-build log evidence that disambiguates which compounding-issue dominates (settling-delay vs PORTx-clear-gap), giving Phase 28 sharper guidance on whether `PORTD = 0x00` alone closes the bug or whether `_NOP()` settling is also required.

Output (if activated): three sub-repo commits (firmware platformio.ini + firmware leonardo_rurp_shield.cpp + new bench-log directory) + one meta-repo commit (the Wave B addendum to v1.6-EVIDENCE.md). All on `v1.6-read-bug` branches per memory `feedback_branching`.
</objective>

<execution_context>
@/workspaces/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspaces/.planning/ROADMAP.md
@/workspaces/.planning/REQUIREMENTS.md
@/workspaces/.planning/STATE.md
@/workspaces/.planning/phases/27-root-cause-analysis/27-CONTEXT.md
@/workspaces/.planning/phases/27-root-cause-analysis/27-RESEARCH.md
@/workspaces/.planning/phases/27-root-cause-analysis/27-PATTERNS.md
@/workspaces/.planning/phases/27-root-cause-analysis/27-VALIDATION.md
@/workspaces/.planning/v1.6-EVIDENCE.md
@/workspaces/.planning/phases/27-root-cause-analysis/27-01-PLAN.md

<interfaces>
<!-- Existing function signatures Wave B instruments. Read-only references. -->

From firestarter/src/boards/leonardo_rurp_shield.cpp:112-141 (the targets):

  uint8_t rurp_read_data_buffer();   // lines 112-129 — PORTD/PORTC/PORTE shift-and-mask reassembly
  void    rurp_set_data_input();     // lines 137-141 — clears DDRD/DDRC/DDRE; missing PORTx=0x00

From firestarter/src/boards/uno_rurp_shield.cpp:120-129 (the contrast, source of the fix to mirror):

  void    rurp_set_data_input();     // clears PORTD=0x00 FIRST, then DDRD=0x00 (the df5fb44 fix)

From firestarter/platformio.ini [env:leonardo] block (lines 57-65 — the build-flag injection point):

  [env:leonardo]
  extends = env:base
  board = leonardo
  build_flags =
      ${env.build_flags}
      -D RURP_BOARD_NAME=\"${this.board}\"
      ; TEMP: 512 to match Uno for buffer-size A/B test (was 1024)
      -D DATA_BUFFER_SIZE=512

From firestarter/include/logging_id.h (the catalog-routed structured-debug family
to use for instrumentation per RESEARCH §"Don't Hand-Roll"):

  LOG_DEBUG_ID_SUB_U8       // 1-byte sub-id + 1-byte param
  LOG_DEBUG_ID_SUB_U8_U8    // 1-byte sub-id + 2-byte params
  LOG_DEBUG_ID_SUB_U16_U16  // 1-byte sub-id + two u16 params

Branch base (verified at RESEARCH time 2026-05-21): firestarter beta HEAD = d955846 = tag 3.0.0b4.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Wave A → Wave B trigger evaluation (pre-execution gate)</name>
  <decision>Did Plan 27-01's Task 2 §"Wave A verifier decision" emit `needs_bench: true`?</decision>
  <context>
    Wave B is conditional per CONTEXT D-01 / D-07. The executor reads the produced `## Phase 27 — RCA Findings` section in `.planning/v1.6-EVIDENCE.md` and confirms the `needs_bench:` value. Per RESEARCH §"Wave B Trigger Criteria" (HIGH confidence), the expected value is `false` — all three D-01 escalation triggers were NOT triggered:
    - T1: H2 wins decisively over H1/H3/H4/H5; single-bit-flip distribution (78%) + address-bit-3 correlation (63%) is incompatible with USB-CDC / framing-layer hypotheses.
    - T2: GATE-1.6 all three axes are GREEN (read-only code path; no write/VPP/pulse interaction).
    - T3: Binary divergence pattern is internally consistent with H2.

    If `needs_bench: false`, ABORT this plan. Phase 27 closes with Plan 27-01's deliverable alone; this plan stays parked.

    Operator decision required ONLY if `needs_bench: true` (off-nominal path): confirm bench availability (Leonardo at /dev/ttyACM1 + W27C512 0xda01 chip in socket + PlatformIO workstation) before proceeding to Task 1.
  </context>
  <options>
    <option id="parked">
      <name>Plan stays parked (RESEARCH-predicted path)</name>
      <pros>Phase 27 closes with Wave A alone. No firmware sub-repo commits. No bench session needed. Plan 27-02 remains drafted for posterity but unexecuted.</pros>
      <cons>None expected — RESEARCH HIGH-confidence finding supports this path.</cons>
    </option>
    <option id="activate">
      <name>Activate Wave B (off-nominal path)</name>
      <pros>Produces instrumented-build evidence with higher specificity than binary mining. Disambiguates settling-delay vs PORTx-clear-gap dominance for Phase 28 fix-shape guidance.</pros>
      <cons>Requires 1-2 hours of operator bench time, a firmware sub-repo branch cut, a one-off pre-release tag for `firestarter fw -i --pre` install, and a fresh consistency-check bench session. Real cost is non-trivial; only worth paying if Wave A could not close Phase 27.</cons>
    </option>
  </options>
  <resume-signal>If Wave A emitted `needs_bench: false` (expected), reply `parked` — this plan does NOT execute and Phase 27 closes via Plan 27-01 alone. If Wave A emitted `needs_bench: true`, reply `activate` AND confirm bench availability (Leonardo board + W27C512 chip + PlatformIO workstation) before proceeding.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Cut firmware sub-repo branch + add instrumentation build flags</name>
  <files>firestarter/platformio.ini</files>
  <read_first>
    - firestarter/platformio.ini lines 57-65 (the [env:leonardo] block — instrumentation injection point)
    - .planning/phases/27-root-cause-analysis/27-CONTEXT.md §"D-03" + §"D-12" + §"Claude's Discretion" (instrumentation-flag choice deferred to planner — this plan picks the coarse-grained `-D RCA_INSTRUMENT_READ_TRACE`)
    - .planning/phases/27-root-cause-analysis/27-RESEARCH.md §"Wave B Trigger Criteria" + §"Don't Hand-Roll" (use LOG_DEBUG_ID_SUB_* catalog-routed debug, NOT raw Serial.print)
    - .planning/v1.6-EVIDENCE.md (the Wave A section that confirmed needs_bench: true triggered this plan)
  </read_first>
  <action>
    Activate ONLY if Task 0 resolved to `activate`.

    Step 1: cut the firmware sub-repo branch off `beta` per CONTEXT D-03 + D-12 + memory `feedback_branching`. From `/workspaces/firestarter`:
    `git fetch && git checkout beta && git pull && git checkout -b v1.6-read-bug`
    Confirm `git rev-parse HEAD` == `d955846` (= tag `3.0.0b4`) per RESEARCH verification. If `beta` has advanced, abort and surface the divergence — the planner's `beta@3.0.0b4` base assumption no longer holds.

    Step 2: edit `firestarter/platformio.ini` [env:leonardo] block (lines 57-65). Append a single build-flag line inside the existing `build_flags =` block, AFTER the existing `-D DATA_BUFFER_SIZE=512` line, with the flag `-D RCA_INSTRUMENT_READ_TRACE`. Do NOT modify the existing flags. Do NOT touch the [env:uno] or [env:uno328pb] blocks — Wave B is Leonardo-only.

    Step 3: commit on `v1.6-read-bug` with message `feat(rca): add RCA_INSTRUMENT_READ_TRACE build flag to [env:leonardo] (Phase 27 Wave B)`.

    Per CONTEXT D-03: this is the SINGLE commit on the new firmware sub-repo branch in Task 1. Task 2 lands the wraparound `#ifdef RCA_INSTRUMENT_READ_TRACE` code as a separate atomic commit.
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && git rev-parse --abbrev-ref HEAD` returns `v1.6-read-bug`.
    - `cd /workspaces/firestarter && git rev-parse v1.6-read-bug~0^` (= parent of the new commit) returns `d955846`.
    - `grep -n 'RCA_INSTRUMENT_READ_TRACE' /workspaces/firestarter/platformio.ini` returns exactly one match inside the `[env:leonardo]` block.
    - `grep -A 10 '\[env:leonardo\]' /workspaces/firestarter/platformio.ini | grep -E 'DATA_BUFFER_SIZE=512'` still matches (the existing flag is preserved).
    - `grep -A 10 '\[env:uno\]' /workspaces/firestarter/platformio.ini | grep RCA_INSTRUMENT` returns nothing (other envs are not modified).
    - `cd /workspaces/firestarter && pio run -e leonardo` compiles clean (sanity check that the new flag doesn't break the build before Task 2 lands the code that consumes it).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter &amp;&amp; git rev-parse --abbrev-ref HEAD | grep -E '^v1\.6-read-bug$' &amp;&amp; grep -A 10 '\[env:leonardo\]' platformio.ini | grep -c 'RCA_INSTRUMENT_READ_TRACE' | grep -E '^1$' &amp;&amp; pio run -e leonardo</automated>
  </verify>
  <done>
    `firestarter/v1.6-read-bug` branch exists cut from `beta@3.0.0b4`; the new `-D RCA_INSTRUMENT_READ_TRACE` flag is the only change on the branch's first commit; `[env:uno]` + `[env:uno328pb]` are untouched; `pio run -e leonardo` compiles green (the flag is inert until Task 2 adds the code that gates on it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Instrument leonardo_rurp_shield.cpp under #ifdef RCA_INSTRUMENT_READ_TRACE, flash, rerun consistency-check</name>
  <files>firestarter/src/boards/leonardo_rurp_shield.cpp</files>
  <read_first>
    - .planning/v1.6-EVIDENCE.md (the file Task 3 will append the Wave B addendum into — Task 2's captured log feeds into Task 3's narrative citations; the executor must see the existing Phase 27 section + sentinels)
    - .planning/phases/27-root-cause-analysis/27-RESEARCH.md §"Wave B Trigger Criteria" + §"Don't Hand-Roll" (instrumentation strategy + LOG_DEBUG catalog-routed emitter pattern) + §"Hypothesis Cross-Check H2" (the baseline this instrumentation is verifying against)
    - firestarter/src/boards/leonardo_rurp_shield.cpp lines 112-141 (the two functions being instrumented: `rurp_read_data_buffer` 112-129 + `rurp_set_data_input` 137-141)
    - firestarter/include/logging_id.h (LOG_DEBUG_ID_SUB_U8_U8 family — catalog-routed structured-debug emitter to use per RESEARCH §"Don't Hand-Roll")
    - firestarter_app/firestarter/eprom_operations.py lines 431+ (consistency_check_eprom — the host-side caller of the read path)
    - .planning/phases/27-root-cause-analysis/27-CONTEXT.md §"Claude's Discretion" (instrumentation-strategy guidance)
  </read_first>
  <action>
    Step 1: Wrap `rurp_read_data_buffer` (lines 112-129) with `#ifdef RCA_INSTRUMENT_READ_TRACE` block that, after computing `data`, emits a LOG_DEBUG_ID_SUB_U8_U8 frame containing (a) the address LSB at the moment of read (read `addr_low = PORTB` or via `rurp_get_last_set_address_lsb()` if available) and (b) the reassembled `data` byte. Goal: a future analysis can correlate divergent positions with the address bus state observed at the read instant.

    Step 2: Wrap `rurp_set_data_input` (lines 137-141) with `#ifdef RCA_INSTRUMENT_READ_TRACE` block that emits a LOG_DEBUG_ID_SUB_U8_U8 frame containing the residual `PORTD` and `PORTC` register values BEFORE the DDR clear. Goal: confirm or refute that PORTx residue is non-zero between reads.

    Step 3: Commit on `v1.6-read-bug` with message `feat(rca): instrument leonardo_rurp_shield.cpp rurp_read_data_buffer + rurp_set_data_input (Phase 27 Wave B)`.

    Step 4: Cut a one-off pre-release tag for bench install per CONTEXT D-03 (e.g. `3.0.0-rca1`) OR `pio run -t upload -e leonardo -p /dev/ttyACM1` direct flash if the workstation is the same as the bench. Operator's call which flow.

    Step 5: Rerun the Phase 26 consistency-check tool against the same physical W27C512 (chip id 0xda01) in the same Leonardo socket. From `/workspaces/firestarter_app`:
    `firestarter dev consistency-check W27C512 --runs 3 --output-dir /workspaces/.planning/v1.6/consistency-check-runs/W27C512-leonardo-{YYYYMMDD-HHMMSS}-rca --port /dev/ttyACM1 2>&1 | tee /workspaces/.planning/v1.6/bench-logs/W27C512-leonardo-{YYYYMMDD-HHMMSS}-rca.log`

    Step 6: Confirm the instrumented log contains the LOG_DEBUG frames (decoded by the host's catalog decoder per Phase 8 / v1.2 plumbing). The frames must surface (a) address-LSB-at-read-time per byte and (b) residual PORTx values per `rurp_set_data_input` entry.

    Step 7: Stage the captured log + binaries for Task 3 to cite. Do NOT yet edit `.planning/v1.6-EVIDENCE.md`.
  </action>
  <acceptance_criteria>
    - `grep -n '#ifdef RCA_INSTRUMENT_READ_TRACE' /workspaces/firestarter/src/boards/leonardo_rurp_shield.cpp` returns ≥2 matches (one per instrumented function).
    - `grep -nE 'LOG_DEBUG_ID_SUB_U8_U8' /workspaces/firestarter/src/boards/leonardo_rurp_shield.cpp` returns ≥2 matches.
    - The instrumented build compiles for Leonardo: `cd /workspaces/firestarter && pio run -e leonardo` exits 0.
    - The non-instrumented build still compiles (sanity that `#ifdef` gating is correct): `cd /workspaces/firestarter && pio run -e uno && pio run -e uno328pb` both exit 0 with no RCA-related warnings.
    - The new bench log directory `/workspaces/.planning/v1.6/consistency-check-runs/W27C512-leonardo-*-rca/` exists with 3 run binaries (each 65,536 bytes).
    - The tee'd log `/workspaces/.planning/v1.6/bench-logs/W27C512-leonardo-*-rca.log` contains decoded `MSG_DEBUG` frames (grep for `DEBUG` or the catalog-decoded labels for the new sub-IDs).
    - Pytest non-regression smoke: `cd /workspaces/firestarter_app && pytest tests/ -x` exits 0 (Wave B added no host-side code changes).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter &amp;&amp; grep -c '#ifdef RCA_INSTRUMENT_READ_TRACE' src/boards/leonardo_rurp_shield.cpp | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; grep -cE 'LOG_DEBUG_ID_SUB_U8_U8' src/boards/leonardo_rurp_shield.cpp | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; ls /workspaces/.planning/v1.6/consistency-check-runs/W27C512-leonardo-*-rca/run_03.bin &amp;&amp; cd /workspaces/firestarter_app &amp;&amp; pytest tests/ -x</automated>
    <automated_full>cd /workspaces/firestarter &amp;&amp; pio run -e leonardo &amp;&amp; pio run -e uno</automated_full>
    <note>The `&lt;automated&gt;` fast block runs in &lt;5s (grep + ls + pytest smoke). The `&lt;automated_full&gt;` PlatformIO compile gate (both Leonardo + Uno envs) runs in ~60-180s — invoked only at end-of-task before committing the instrumentation, not on every-task fast-feedback sampling. Split per Dimension 8b feedback-latency target.</note>
  </verify>
  <done>
    Two `#ifdef RCA_INSTRUMENT_READ_TRACE` wrappers landed in `leonardo_rurp_shield.cpp` (one per instrumented function), each emitting a LOG_DEBUG_ID_SUB_U8_U8 frame. Instrumented Leonardo build flashed to the bench board. Three consecutive 64KB instrumented reads captured under `.planning/v1.6/consistency-check-runs/W27C512-leonardo-*-rca/`. Tee'd bench log captured under `.planning/v1.6/bench-logs/W27C512-leonardo-*-rca.log` with MSG_DEBUG frames decoded by the host catalog. All builds compile clean across all 3 envs. Pytest non-regression smoke green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Wave B addendum to .planning/v1.6-EVIDENCE.md ## Phase 27 — RCA Findings</name>
  <files>.planning/v1.6-EVIDENCE.md</files>
  <read_first>
    - .planning/v1.6-EVIDENCE.md (existing Phase 27 H2 section from Plan 27-01 + the line-21/22 sentinels reserving Phase 28 / Phase 29 slots)
    - .planning/phases/27-root-cause-analysis/27-RESEARCH.md §"Wave B Trigger Criteria" + §"Hypothesis Cross-Check H2" (the evidence base the addendum reasons against; the H2 baseline numbers Wave B's instrumented data either confirms or refines)
    - .planning/phases/27-root-cause-analysis/27-CONTEXT.md §"D-04" (Phase 27 section append rule + 9-column row schema applies if new evidence rows added)
    - .planning/phases/27-root-cause-analysis/27-PATTERNS.md §"Pattern 4" (9-column row schema MUST be applied verbatim if a new evidence row is added)
    - The Task 2 bench log + binaries (the substrate Wave B's addendum cites)
  </read_first>
  <action>
    Insert a NEW H3 subsection at the END of the existing `## Phase 27 — RCA Findings` section (i.e., AFTER the existing "Wave A verifier decision" subsection but BEFORE the line-21 `<!-- Phase 28 ... -->` sentinel). The H3 header is:

      ### Wave B addendum (instrumented build evidence)

    Subsection contents:

    1. A 1-2 paragraph narrative summarizing what the LOG_DEBUG frames showed. Required tokens: `RCA_INSTRUMENT_READ_TRACE`, `LOG_DEBUG_ID_SUB_U8_U8`, the literal new bench-log path, and an explicit verdict on whether the instrumented evidence (a) confirms the H2 dominance OR (b) shifts the H2 narrative toward a more specific sub-mechanism (settling-delay vs PORTx-clear-gap).

    2. A NEW evidence row appended to the existing 9-column Phase 26 baseline table (per D-04 + PATTERNS Pattern 4). The row uses the D-08 9-column schema VERBATIM:
       | Board | Port | Chip | N | SHAs distinct | Divergent bytes (run1 vs run2) | First-diverge offset | Verdict | Log |
       Values: `leonardo (RCA-instrumented)` | `/dev/ttyACM1` | `W27C512 (id 0xda01)` | `3` | `{1 or 3}` | `{count}` | `{offset}` | `FAIL (instrumented — see Wave B narrative)` or `PASS (instrumented build — H2 not reproducible under tracing!)` | `.planning/v1.6/consistency-check-runs/W27C512-leonardo-*-rca/`
       (The exact values are filled in from Task 2's captured numbers.)

    3. An explicit one-line update to the existing "Wave A verifier decision" subsection: a follow-up line stating `Wave B activated: <activation timestamp>; Wave B closure: <closure timestamp>; revised needs_bench: false (Wave B sufficient to close)`. Do NOT delete or rewrite the original Wave A decision block; only append the follow-up line at the end of that block.

    4. Update the §"Fix sketch (Phase 28 handoff)" subsection: if Wave B's evidence shifted the candidate fix shape (e.g., from "PORTD = 0x00 mirror + optional _NOP()" to "PORTD = 0x00 alone sufficient" or "_NOP() settling required"), append a follow-up paragraph stating the revised candidate. Do NOT delete the Wave A fix sketch; append after it.

    5. Commit the meta-repo change with message `docs(27): append Wave B instrumented-build addendum to ## Phase 27 — RCA Findings`.

    Per D-04 anti-pattern guard: do NOT modify Phase 26's existing rows, Verdict, Scope, Hardware-metadata, or Phase 27 entry conditions subsections. Append only. The line-21 + line-22 HTML-comment sentinels MUST be preserved for Phase 28 + Phase 29.
  </action>
  <acceptance_criteria>
    - `grep -E '### Wave B addendum' /workspaces/.planning/v1.6-EVIDENCE.md` returns exactly one match.
    - `grep -E 'RCA_INSTRUMENT_READ_TRACE|LOG_DEBUG_ID_SUB_U8_U8' /workspaces/.planning/v1.6-EVIDENCE.md` returns ≥2 matches.
    - `grep -E 'leonardo \(RCA-instrumented\)' /workspaces/.planning/v1.6-EVIDENCE.md` returns exactly one match (the new evidence-row Board cell).
    - `grep -E 'consistency-check-runs/W27C512-leonardo-.*-rca' /workspaces/.planning/v1.6-EVIDENCE.md` returns ≥1 match (the new log path citation).
    - All RCA-01 / RCA-02 / RCA-03 / SC#4 / hypothesis disposition tokens from Plan 27-01's acceptance criteria still pass (Wave B does not regress any Wave A claim).
    - HTML-comment sentinels preserved: `grep -cE '<!-- Phase 2[89]' /workspaces/.planning/v1.6-EVIDENCE.md` returns ≥2.
    - Pytest non-regression smoke: `cd /workspaces/firestarter_app && pytest tests/ -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -E '### Wave B addendum' /workspaces/.planning/v1.6-EVIDENCE.md &amp;&amp; grep -E 'RCA_INSTRUMENT_READ_TRACE' /workspaces/.planning/v1.6-EVIDENCE.md &amp;&amp; grep -E 'leonardo \(RCA-instrumented\)' /workspaces/.planning/v1.6-EVIDENCE.md &amp;&amp; grep -cE '<!-- Phase 2[89]' /workspaces/.planning/v1.6-EVIDENCE.md | awk '$1 &gt;= 2 {exit 0} {exit 1}' &amp;&amp; cd /workspaces/firestarter_app &amp;&amp; pytest tests/ -x</automated>
  </verify>
  <done>
    `.planning/v1.6-EVIDENCE.md` has a new `### Wave B addendum (instrumented build evidence)` H3 subsection at the end of the existing `## Phase 27 — RCA Findings` H2 (before the line-21 Phase 28 sentinel). The Phase 26 baseline table has one new row using the D-08 9-column schema verbatim. The Wave A verifier decision block has a follow-up line documenting Wave B activation + closure. The fix sketch subsection has a follow-up paragraph (if Wave B evidence shifted the candidate). All Plan 27-01 acceptance grep tokens still pass. Pytest non-regression smoke green. Wave B closes Phase 27 with a richer RCA narrative than Wave A alone would have produced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host ↔ Serial CDC ↔ Leonardo firmware | The instrumented LOG_DEBUG_ID_SUB_U8_U8 frames flow over the same CRC8-protected ID-frame wire protocol as production messages — no new boundary. |
| Bench operator → instrumented firmware | The instrumented build is a development-only artifact (carries `RCA_INSTRUMENT_READ_TRACE` build flag); never promoted to `beta`/`main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27B-01 | Information Disclosure | Instrumented bench log (`.planning/v1.6/bench-logs/W27C512-leonardo-*-rca.log`) | accept | Log contains MCU register values + chip-byte stream; no PII, no secrets. Internal planning artifact, low-value target. |
| T-27B-02 | Tampering | The new `-D RCA_INSTRUMENT_READ_TRACE` build flag could accidentally ship in a `beta` cut | accept (gated by branch model) | Per CONTEXT D-03 + memory `feedback_branching`, the flag lives on `firestarter/v1.6-read-bug` branch only. Promotion to `beta` happens in Phase 28 boundary AFTER the fix lands and AFTER the instrumentation is removed. The Wave B summary records this gate. |
| T-27B-03 | Denial of Service | LOG_DEBUG frame emission inside `rurp_read_data_buffer` could slow the read enough to mask the bug | mitigate | The LOG_DEBUG_ID_SUB_U8_U8 catalog-routed emitter is the same family already used by Phase 8 / v1.2 production debug paths — its per-call overhead is well-characterized (≤200µs per frame on 32U4). If the bug magnitude shifts dramatically between instrumented and non-instrumented runs (e.g., 2.1% → 0.0%), that itself is evidence the bug is timing-dependent and the Wave B narrative captures the finding. |
| T-27B-04 | Information Disclosure | Untrusted serial input → host parser (`serial_comm.py:_decode_id_frame` 398-447) | accept | Existing CRC8 check + length-authoritative parsing — unchanged by this plan. New LOG_DEBUG sub-IDs use the existing catalog plumbing; no new security-relevant code. |

**Disposition rationale:** Wave B introduces firmware code under `#ifdef RCA_INSTRUMENT_READ_TRACE` only — no production-path changes. The branch-gating (cut from `beta`, never merged back into `beta` until Phase 28 removes the instrumentation) is the primary mitigation against accidental promotion. No high-severity threats apply.
</threat_model>

<verification>
After all 3 tasks complete, run:

```bash
# Wave B section + new evidence row landed
grep -E '### Wave B addendum' /workspaces/.planning/v1.6-EVIDENCE.md
grep -E 'leonardo \(RCA-instrumented\)' /workspaces/.planning/v1.6-EVIDENCE.md

# Firmware instrumentation gated correctly
grep -c '#ifdef RCA_INSTRUMENT_READ_TRACE' /workspaces/firestarter/src/boards/leonardo_rurp_shield.cpp  # >= 2
grep -A 10 '\[env:leonardo\]' /workspaces/firestarter/platformio.ini | grep -c 'RCA_INSTRUMENT_READ_TRACE'  # == 1

# All 3 envs still compile
cd /workspaces/firestarter && pio run -e uno && pio run -e leonardo && pio run -e uno328pb

# Wave A claims still hold
grep -E 'leonardo_rurp_shield\.cpp|rurp_read_data_buffer|rurp_set_data_input' /workspaces/.planning/v1.6-EVIDENCE.md
grep -cE 'H[1-7].*(REFUTED|CONFIRMED|out of scope)' /workspaces/.planning/v1.6-EVIDENCE.md  # >= 7
grep -cE 'write-path|VPP|pulse interval' /workspaces/.planning/v1.6-EVIDENCE.md  # >= 3

# Host-side non-regression
cd /workspaces/firestarter_app && pytest tests/ -x

# Branch model invariant
cd /workspaces/firestarter && git branch --show-current  # v1.6-read-bug
```

All must pass before handing off to `/gsd-verify-work 27`.
</verification>

<success_criteria>
- Task 0 trigger evaluation runs FIRST; default path is `parked` (RESEARCH HIGH-confidence prediction) and the plan does NOT execute.
- IF activated:
  - `firestarter/v1.6-read-bug` branch cut from `beta@3.0.0b4` with exactly two commits (one for platformio.ini build-flag, one for leonardo_rurp_shield.cpp instrumentation).
  - All 3 firmware envs (`uno`, `leonardo`, `uno328pb`) compile clean — the `#ifdef RCA_INSTRUMENT_READ_TRACE` gating is correct.
  - 3 instrumented consistency-check runs captured under `.planning/v1.6/consistency-check-runs/W27C512-leonardo-*-rca/` (each 65,536 bytes).
  - Tee'd bench log captured under `.planning/v1.6/bench-logs/W27C512-leonardo-*-rca.log` with decoded MSG_DEBUG frames.
  - `.planning/v1.6-EVIDENCE.md` has a new `### Wave B addendum` subsection at the end of the existing `## Phase 27 — RCA Findings` H2; one new evidence row appended to the Phase 26 baseline table using the D-08 9-column schema verbatim.
  - Wave A's verifier decision block + fix sketch subsection have follow-up lines (not rewritten — appended).
  - All Plan 27-01 acceptance grep checks still pass (Wave B does not regress any Wave A claim).
  - Pytest non-regression smoke green.
  - Phase 27 closes with a sharper RCA narrative than Wave A alone produced; Phase 28's planner has Wave B's instrumented evidence as additional input.
</success_criteria>

<output>
After completion, create `.planning/phases/27-root-cause-analysis/27-02-SUMMARY.md` per `@/workspaces/.claude/get-shit-done/templates/summary.md`. Summary MUST record:
- Whether Task 0 resolved to `parked` (default) or `activate` (off-nominal)
- If activated: the 3 commits (sub-repo platformio.ini + sub-repo leonardo_rurp_shield.cpp + meta-repo v1.6-EVIDENCE.md addendum) with SHAs
- The instrumented-run results (3 SHA-256s, divergent-byte count, first-divergence offset)
- Whether Wave B confirmed H2's dominance or shifted the fix-shape candidate for Phase 28
- Branch state at plan close: `firestarter/v1.6-read-bug` HEAD; `firestarter_app/v1.6-read-bug` HEAD (unchanged if no host-side instrumentation needed); meta-repo `main` HEAD with the addendum commit
- Cross-references to RCA-01 / RCA-02 / RCA-03 final closure (gap-fill complete if applicable)
</output>
</content>
</invoke>