---
phase: 207-the-version-and-the-record
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: "firestarter_fw and firestarter_app on v1.41-verification-to-host, inside each submodule, never pushed; plus one meta gitlink commit on v1.41-verification-to-host"
files_modified:
  - firestarter_fw/.github/CONTRIBUTING.md
  - firestarter_fw/include/version.h
  - firestarter_app/.planning/codebase/STACK.md
  - firestarter_app/firestarter/__init__.py
  - firestarter_app/README.md
  - firestarter_fw
  - firestarter_app
  - .planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt
autonomous: true
requirements: [REL-01]
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-01: in both sub-repos, origin/beta (app 3.0.0b50, firmware 3.0.0b35) is merged into v1.41-verification-to-host by a merge commit that is the direct parent of the version-bump commit, so the bump never meets beta in a conflict"
    - "`firestarter_fw/include/version.h` line 11 reads `#define VERSION \"3.1.0b1\"` and `firestarter_app/firestarter/__init__.py` line 1 reads `__version__ = \"3.1.0b1\"`"
    - "The two strings move in one commit pair: a firmware commit and an app commit, each touching only its version file, each with a subject beginning `chore(207-01): bump`, and the app commit's body names the firmware commit's sha"
    - "Each sub-repo's own publisher, `GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run`, prints exactly `DRY_RUN: 3.1.0b1` on the bumped milestone branch"
    - "D-01 outcome: `git merge-tree --write-tree --name-only HEAD origin/beta` prints exactly one line in both sub-repos, so the ship PR merges with no conflict on the version line"
    - "The meta gitlinks `firestarter_fw` and `firestarter_app` equal each sub-repo's HEAD, advanced by one meta commit on v1.41-verification-to-host whose change is exactly those two paths"
    - "On the bumped tree the app gates pass on Python 3.11 (ruff check, ruff format --check, full pytest with no failure and at least 2363 passed) and the firmware tests/ tree passes with no failure"
    - "`firestarter_app/README.md` tells the reader to upgrade the CLI first, then the firmware, and links the wiki Breaking-Changes page instead of claiming every mismatched pair fails loudly"
  artifacts:
    - path: "firestarter_fw/include/version.h"
      provides: "the firmware version string the image reports in its FW: line and ready ack"
      contains: "#define VERSION \"3.1.0b1\""
    - path: "firestarter_app/firestarter/__init__.py"
      provides: "the CLI version string that pyproject.toml reads dynamically and --version prints"
      contains: "__version__ = \"3.1.0b1\""
    - path: "firestarter_app/README.md"
      provides: "the corrected upgrade-order sentence that reaches the PyPI long description at the beta cut"
      contains: "Upgrade the CLI first"
    - path: ".planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt"
      provides: "the remote-ref baseline and the recorded output of every REL-01 oracle and gate"
      contains: "DRY_RUN: 3.1.0b1"
  key_links:
    - from: "firestarter_fw/include/version.h"
      to: "firestarter_fw/.github/scripts/update_version.py"
      via: "the publisher keeps only the major.minor.patch base and scans tags for 3.1.0bN, emitting b1 when none exists"
      pattern: "DRY_RUN: 3.1.0b1"
    - from: "firestarter_app/firestarter/__init__.py"
      to: "firestarter_app/.github/scripts/update_version.py"
      via: "the same base-plus-tag-scan derivation on the app side"
      pattern: "DRY_RUN: 3.1.0b1"
    - from: "meta gitlinks firestarter_fw, firestarter_app"
      to: "each sub-repo HEAD on v1.41-verification-to-host"
      via: "git ls-tree HEAD in /workspaces"
      pattern: "160000 commit"
  prohibitions:
    - statement: "Nothing is pushed from this plan. beta, main and v1.41-verification-to-host are left as the remote has them in both sub-repos and in the meta repo, because a beta push in either sub-repo publishes to PyPI and GitHub pre-releases and a PyPI version can never be reused."
      requirement_id: REL-01
      status: enforced
      verification: "Task 3 prohibition leg (PASS-207-PROHIBITION): after a fresh fetch neither bump commit is an ancestor of origin/beta or origin/main, and git ls-remote shows no v1.41-verification-to-host branch and no 3.1.0 tag in either sub-repo."
    - statement: "No tag is created in any of the three repositories and no GitHub Release is cut from the meta repository. A v1.41 tag or Release parses as PEP 440 1.41 and would report every stranded CLI as already up to date (ROADMAP criterion 4)."
      requirement_id: REL-01
      status: enforced
      verification: "Task 3 prohibition leg: gh api repos/henols/firestarter/releases --jq length prints 0, /workspaces/.github/workflows does not exist, git tag --list 'v1.41*' is empty, and git tag --contains over every new commit is empty in every repo."
---

<objective>
Move both repositories to `3.1.0b1` in one inspectable commit pair, after bringing `beta` in, so the
version the publisher will cut is decided here in a reviewed commit rather than in a ship-time conflict
editor.

Purpose: REL-01. The v1.41 milestone breaks the protocol (ordinals 4 and 6 retired), and PROJECT.md's
activation decision D-5 calls for a minor bump in both repos. Research found the trap: `beta` has taken
two bot bumps since the milestone branches forked (app `3.0.0b48` to `3.0.0b50`, firmware `3.0.0b33` to
`3.0.0b35`). A bump on the unsynced branch conflicts with `beta` on exactly the version line, and taking
`beta`'s side at ship would publish `3.0.0b51` / `3.0.0b36` and burn a PyPI version permanently. D-01
(operator, 2026-09-23) settles it: merge `origin/beta` into each milestone branch first, then bump.

Output: in each sub-repo, a merge of `origin/beta` followed by a single-file version commit; one app
README commit correcting a false mixed-version claim (research F6, option A); one meta gitlink commit;
and an evidence file recording the publisher's own verdict (`DRY_RUN: 3.1.0b1`) in both repos.

Tracer note: Task 1 is the thinnest end-to-end slice — one repo, from the beta merge through the source
string to the publisher's derivation and the ship-mergeability oracle. Task 2 repeats the proven slice
for the app with the gates it needs; Task 3 closes the pair in the meta repo and proves the whole.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/207-the-version-and-the-record/207-CONTEXT.md
@.planning/phases/207-the-version-and-the-record/207-RESEARCH.md
@.planning/phases/207-the-version-and-the-record/207-PATTERNS.md
@firestarter_fw/include/version.h
@firestarter_app/firestarter/__init__.py
@firestarter_app/README.md

Do NOT reason from STATE.md's v1.40 lines. They are stale: v1.40 IS merged to beta in all three repos
(2026-09-20), `3.0.0b49` and `3.0.0b50` are on PyPI, and the `v1.40` tag is on the remote. Every
published-state claim in this plan comes from `git ls-remote`, `gh api` or the publisher's dry-run.

Standing hazards the executor must respect in every task:
- The shell's `grep` is a ugrep wrapper that honours `.gitignore`. Every check uses `/usr/bin/grep`, and
  every pattern that could start with a dash goes through `-e`.
- `firestarter_fw`'s `tests/` tree contains a test that asserts the whole repository's porcelain is
  empty. Run that tree only after the task's commit, never mid-edit.
- `firestarter_app/.venv311` currently has a dangling interpreter (the container rebuild removed it).
  Task 2 rebuilds it before any `.venv311` command.
- The meta working tree carries unrelated modified and untracked files (`.devcontainer/*`, `.vscode/*`,
  `.planning/estimation-calibration.json`, `.planning/graphs/*`, `anything.txt`, `tmp/`,
  `firestarter.wiki/`, `setup-claude-pr-policy.sh`, and the Phase 205 `205-LEARNINGS.md`). None of them
  is staged or committed by this plan. Stage by explicit path only.
- `firestarter_app` carries two untracked paths that predate this phase
  (`connect-cost-2026-09-23-110029/`, `datasheets/LST62832I.pdf`). Leave them untracked.
- After every `gsd-tools query commit`, run `git -C /workspaces rev-parse --abbrev-ref HEAD` and confirm
  it prints `v1.41-verification-to-host`; that verb has switched branches in this project before.
</context>

## Decisions carried into this plan

- **D-01 (locked, operator):** merge `origin/beta` into `v1.41-verification-to-host` in each sub-repo
  BEFORE the bump, then bump from `3.0.0b50` / `3.0.0b35` to `3.1.0b1`.
- **F2 (Claude's discretion, research recommendation taken):** follow the Phase 204 commit-pair shape
  exactly — firmware commit (`include/version.h` only), app commit (`firestarter/__init__.py` only, body
  names the firmware sha), then one meta commit naming both.
- **F6 (Claude's discretion, research option A taken):** correct `firestarter_app/README.md`'s
  mixed-version sentence in a separate app commit after the version commit, so the version commit
  stays single-file and inspectable by `git show --stat`.
- **Reversibility:** the merges and bumps are local and unpushed in this phase, so undo is a branch
  reset. They become permanent history at ship, and the `3.1.0` base then decides every later published
  version. That is `costly`, not `one-way` inside this phase, and D-01 already records the operator's
  choice, so no decision checkpoint is inserted.
- **No external API integration:** the api-coverage detector returned `detected: false` over the phase
  scope; no COVERAGE.md is required.

## Artifacts this phase produces

Phase-wide list. Items marked **(this plan)** are produced here.

- **(this plan)** `firestarter_fw`: one merge commit bringing `origin/beta` (`3.0.0b35`) into
  `v1.41-verification-to-host`, and one commit `chore(207-01): bump firmware version 3.0.0b35 -> 3.1.0b1`
  touching only `include/version.h`.
- **(this plan)** `firestarter_app`: one merge commit bringing `origin/beta` (`3.0.0b50`) in, one commit
  `chore(207-01): bump app version 3.0.0b50 -> 3.1.0b1` touching only `firestarter/__init__.py`, and one
  docs commit touching only `README.md`.
- **(this plan)** meta: one commit `chore(207-01): advance firestarter_fw and firestarter_app gitlinks`
  touching exactly the two gitlinks.
- **(this plan)** `.planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt`.
- **(this plan)** `.planning/phases/207-the-version-and-the-record/207-01-SUMMARY.md`.
- (plan 207-02) Wiki clone `/workspaces/firestarter.wiki`: three local commits on `master` on top of
  `f967398` — the 3.1.0b1 Breaking-Changes entry, the new `Writing-and-Verifying.md` page with its two
  navigation lines, and the Install-Beta / Testing-Chips version-pairing notes. `207-02-SUMMARY.md`.
- (plan 207-03) The live wiki `henols/firestarter.wiki.git` `master`, fast-forwarded from `81229d8` after
  operator approval; `evidence/207-03-wiki-prepush.txt`; `evidence/207-03-wiki-postpush-freshclone.txt`;
  `207-03-SUMMARY.md`.
- Nothing else: no tag, no GitHub Release, no push to any `beta`, `main` or milestone branch, no catalog
  or codegen change.

<tasks>

<task type="tracer">
  <name>Task 1: Firmware end to end: beta merged in, version bumped, publisher derives 3.1.0b1, ship merge clean</name>
  <reversibility rating="costly">Local and unpushed here, so undo is a reset of the milestone branch; at ship the merge becomes permanent history and the 3.1.0 base decides every later firmware version.</reversibility>
  <precondition>`git -C /workspaces/firestarter_fw rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`, `git -C /workspaces/firestarter_fw status --porcelain` prints nothing, and `git -C /workspaces/firestarter_fw rev-parse HEAD` prints `6e11d057b59977dd870c1ddcc588dd2f6f3ea1db` (the pre-phase tip).</precondition>
  <files>firestarter_fw/.github/CONTRIBUTING.md, firestarter_fw/include/version.h, .planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt</files>
  <read_first>
    - `firestarter_fw/include/version.h` in full (13 lines): the MIT header on lines 1-6, the guard on lines 8-9, the define on line 11, `#endif` on line 13, and no trailing newline. This is the file being modified.
    - `firestarter_fw/.github/scripts/update_version.py` in full: `get_version()` parses the define, the beta path keeps only `major.minor.patch` and scans tags for `<base>bN`. This is why the oracle is the publisher's own dry-run.
    - `207-RESEARCH.md` § Q1 (every place the version lives), § Q2 (the conflict the bump creates, simulated with merge-tree), § Open Design Forks F1, and § Common Pitfalls 1 and 6.
    - `207-PATTERNS.md` § "Merge origin/beta into each milestone branch" and § "firestarter_fw/include/version.h" (the `5a7c18c` and `31fa175` precedents).
    - `/workspaces/firestarter_fw/CLAUDE.md` § "What CI runs": `beta-build.yml` publishes on any push to `beta`, and the `tests/` tree needs full git history.
  </read_first>
  <action>
All commits in this task land inside `/workspaces/firestarter_fw` on `v1.41-verification-to-host`. Nothing is pushed.

1. Create the evidence directory `/workspaces/.planning/phases/207-the-version-and-the-record/evidence/` if absent. Record the pre-phase remote baseline into `evidence/207-01-oracles.txt`: for each of `/workspaces/firestarter_fw`, `/workspaces/firestarter_app` and `/workspaces`, write a heading line naming the repo, then the output of `git -C <repo> ls-remote origin refs/heads/beta refs/heads/main refs/heads/v1.41-verification-to-host`. At planning time (2026-09-23) these read: app beta `c6e48ff8…`, main `02d020a9…`; firmware beta `a6ba2b72…`, main `b21f94a0…`; meta beta `efd36140…`, main `0d784cab…`; and no `v1.41-verification-to-host` branch on any remote. A differing value is recorded, not acted on: the Task 3 prohibition leg proves "not published" by ancestry, not by these values.

2. Run `git -C /workspaces/firestarter_fw fetch origin beta`. Then `git -C /workspaces/firestarter_fw merge-tree --write-tree --name-only HEAD origin/beta` must print exactly one line (a tree sha). If it prints any path or a `CONFLICT` line, STOP and report the conflicted paths. Do not resolve anything on this branch unasked.

3. Confirm `git -C /workspaces/firestarter_fw show origin/beta:include/version.h` carries `#define VERSION "3.0.0b35"`. If it carries a different `3.0.0bNN`, `beta` has published again since planning: use that value everywhere this task says `3.0.0b35`, and say so in the SUMMARY.

4. Per D-01, merge: `git -C /workspaces/firestarter_fw merge --no-ff origin/beta` with the subject `Merge origin/beta (3.0.0b35) into v1.41-verification-to-host before the 3.1.0b1 bump` and a two-sentence body in the `5a7c18c` register: it brings beta's documentation fix (`.github/CONTRIBUTING.md`) and beta's own version bump onto the milestone branch, so the version line is decided here in a reviewed commit and the milestone merges out to beta with no conflict. Relative to its first parent the merge changes exactly `.github/CONTRIBUTING.md` and `include/version.h`.

5. Assert line 11 of `include/version.h` now reads exactly `#define VERSION "3.0.0b35"` using `/usr/bin/grep -Fx -e`. Then change only that line to `#define VERSION "3.1.0b1"` with a line-addressed `sed -i '11s/.../.../'`. Do not rewrite the file with the Write tool: the file has no trailing newline and a rewrite adds a spurious end-of-file hunk. `git -C /workspaces/firestarter_fw diff --numstat` must print exactly one line, `1`, tab, `1`, tab, `include/version.h`.

6. Commit only that file. Subject: `chore(207-01): bump firmware version 3.0.0b35 -> 3.1.0b1`. Body: "Minor bump for the release that moves verify and blank check from the firmware to the host. CI keeps only the major.minor.patch base from this line and derives the pre-release number from existing tags, so the first beta push publishes 3.1.0b1." Record the commit's full sha as FW_SHA for Task 2 and the SUMMARY.

7. Run the oracles and append each command line and its output to the evidence file: `git -C /workspaces/firestarter_fw fetch -q --tags origin`; from `/workspaces/firestarter_fw`, `GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run` (expect exactly `DRY_RUN: 3.1.0b1`, and it writes no file); `git merge-tree --write-tree --name-only HEAD origin/beta | wc -l` (expect `1`); `git show --name-only --format= HEAD` (expect `include/version.h`); `git status --porcelain` (expect nothing); and `/usr/local/py-utils/bin/pytest tests/ -q -p no:cacheprovider` last line (baseline before this phase: `316 passed`). The tests/ tree runs only now, after the commit, because one of its tests asserts an empty porcelain.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_fw && B=$(git log -1 --format=%H --fixed-strings --grep='chore(207-01): bump firmware version' HEAD) && [ -n "$B" ] && git merge-base --is-ancestor "$B" HEAD && M=$(git rev-parse "$B^1") && [ "$(sed -n 11p include/version.h)" = '#define VERSION "3.1.0b1"' ] && [ "$(git show --name-only --format= "$B")" = "include/version.h" ] && git log -1 --format=%s "$B" | /usr/bin/grep -qxE -e 'chore\(207-01\): bump firmware version 3\.0\.0b[0-9]+ -> 3\.1\.0b1' && [ "$(git rev-parse "$M^1")" = "6e11d057b59977dd870c1ddcc588dd2f6f3ea1db" ] && [ "$(git rev-parse "$M^2")" = "$(git rev-parse origin/beta)" ] && [ "$(git diff --name-only 6e11d057b59977dd870c1ddcc588dd2f6f3ea1db "$M" | LC_ALL=C sort | tr '\n' ' ')" = ".github/CONTRIBUTING.md include/version.h " ] && [ "$(GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run)" = "DRY_RUN: 3.1.0b1" ] && [ "$(git merge-tree --write-tree --name-only HEAD origin/beta | wc -l)" = "1" ] && [ -z "$(git status --porcelain)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207-01-T1</automated>
    <fails_when>stdout lacks the line `PASS-207-01-T1`: a `[ ]` comparison is false (the dry-run prints a `3.0.0b` or `3.1.0b2` value, merge-tree prints more than one line, the bump commit touches more than `include/version.h`, or the merge is not the bump's direct parent)</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/local/py-utils/bin/pytest tests/ -q -p no:cacheprovider 2>&1 | tail -1</automated>
    <fails_when>the printed summary line contains `failed` or `error`, or does not begin with a passed count of at least 316</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 11p /workspaces/firestarter_fw/include/version.h` prints exactly `#define VERSION "3.1.0b1"`.
    - `git -C /workspaces/firestarter_fw show --name-only --format= HEAD` prints exactly `include/version.h`.
    - `git -C /workspaces/firestarter_fw log -1 --format=%s HEAD` prints `chore(207-01): bump firmware version 3.0.0b35 -> 3.1.0b1` (or the recorded differing beta value in place of `3.0.0b35`).
    - D-01 ordering: with B the bump sha and M its parent (`git rev-parse "$B^1"`), `git rev-parse "$M^1"` prints `6e11d057b59977dd870c1ddcc588dd2f6f3ea1db` and `git rev-parse "$M^2"` equals `origin/beta`, so the merge of beta is the bump's direct parent.
    - `git -C /workspaces/firestarter_fw diff --name-only 6e11d057b59977dd870c1ddcc588dd2f6f3ea1db "$M"` lists exactly `.github/CONTRIBUTING.md` and `include/version.h`.
    - `GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run` in `/workspaces/firestarter_fw` prints exactly `DRY_RUN: 3.1.0b1`.
    - `git -C /workspaces/firestarter_fw merge-tree --write-tree --name-only HEAD origin/beta | wc -l` prints `1`.
    - `/usr/local/py-utils/bin/pytest tests/ -q -p no:cacheprovider` in `/workspaces/firestarter_fw` ends with a line reporting at least `316 passed` and no `failed`.
    - `evidence/207-01-oracles.txt` exists, carries a remote-ref block for each of the three repositories, and carries the firmware `DRY_RUN: 3.1.0b1` line.
    - `git -C /workspaces/firestarter_fw rev-parse --abbrev-ref HEAD` still prints `v1.41-verification-to-host`, and nothing was pushed (no `git push` was run).
  </acceptance_criteria>
  <done>The firmware half of the pair is committed on the milestone branch over a merge of beta, and the firmware's own publisher code says the first beta push will cut 3.1.0b1 with no conflict.</done>
</task>

<task type="auto">
  <name>Task 2: App half: rebuild the 3.11 venv, merge beta in, single-file bump naming the firmware sha, gates green</name>
  <reversibility rating="costly">Local and unpushed here, so undo is a reset; at ship the 3.1.0 base reaches PyPI, where a version can never be reused.</reversibility>
  <precondition>`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`, `git -C /workspaces/firestarter_app status --porcelain --untracked-files=no` prints nothing, `git -C /workspaces/firestarter_app rev-parse HEAD` prints `d723cf77b7dac0e6d862e480b08d46de30b20ea9`, and Task 1's FW_SHA is recorded.</precondition>
  <files>firestarter_app/.planning/codebase/STACK.md, firestarter_app/firestarter/__init__.py, .planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt</files>
  <read_first>
    - `firestarter_app/firestarter/__init__.py` (one line). This is the file being modified.
    - `firestarter_app/.github/scripts/update_version.py`: the same base-plus-tag-scan derivation as the firmware copy.
    - `firestarter_app/pyproject.toml` lines 10-12 and 92-97: `dynamic = ["version"]`, `version = { attr = "firestarter.__version__" }`, `requires-python`, and the pytest `addopts = "-ra -q"` that makes `-o addopts=""` necessary.
    - `/workspaces/firestarter_app/CLAUDE.md` § "What CI runs": CI runs Python 3.11; ruff check, ruff format --check, pytest, smoke install.
    - `207-RESEARCH.md` § Verification Commands V0 (the tested venv rebuild recipe) and V5, and § Environment Availability.
    - `207-PATTERNS.md` § "firestarter_app/firestarter/__init__.py" (the `cfc0e1c` precedent and the `0ef0563` merge-message precedent).
  </read_first>
  <action>
All commits in this task land inside `/workspaces/firestarter_app` on `v1.41-verification-to-host`. Nothing is pushed.

1. Rebuild the test venv (research V0, tested in scratch). From `/workspaces/firestarter_app`, with `UV_CACHE_DIR=/tmp/claude-uv-cache` exported (the default `~/.cache` is root-owned), run `uv venv --clear --python 3.11 .venv311`, then `uv pip install --python .venv311/bin/python -e '.[test]'`. `.venv311/bin/python --version` must print `Python 3.11.` followed by a patch number. `Permission denied (os error 13)` means the cache variable was not exported for that command. Do not fall back to `/usr/local` or the system Python 3.13: CI runs 3.11, and a later interpreter masks 3.11-only failures.

2. Run `git -C /workspaces/firestarter_app fetch origin beta`. `git -C /workspaces/firestarter_app merge-tree --write-tree --name-only HEAD origin/beta` must print exactly one line; if not, STOP and report.

3. Confirm `git -C /workspaces/firestarter_app show origin/beta:firestarter/__init__.py` reads `__version__ = "3.0.0b50"`. If it reads another `3.0.0bNN`, use that value where this task says `3.0.0b50` and say so in the SUMMARY.

4. Per D-01, merge: `git -C /workspaces/firestarter_app merge --no-ff origin/beta` with the subject `merge: bring origin/beta (3.0.0b50) into the v1.41 milestone branch before the 3.1.0b1 bump` and a short body in the `0ef0563` register: a pre-ship inward merge that settles the version line on the milestone branch first; it brings beta's repository-name fix to `.planning/codebase/STACK.md` and beta's b50 version bump. Relative to its first parent the merge changes exactly `.planning/codebase/STACK.md` and `firestarter/__init__.py`.

5. Assert line 1 of `firestarter/__init__.py` reads exactly `__version__ = "3.0.0b50"` with `/usr/bin/grep -Fx -e`. Change only that line to `__version__ = "3.1.0b1"` with a line-addressed `sed -i '1s/.../.../'`. `git -C /workspaces/firestarter_app diff --numstat` must print exactly `1`, tab, `1`, tab, `firestarter/__init__.py`.

6. Commit only that file. Subject: `chore(207-01): bump app version 3.0.0b50 -> 3.1.0b1`. Body: "Pairs with firestarter_fw FW_SHA (chore(207-01): bump firmware version 3.0.0b35 -> 3.1.0b1). Both repositories move to the 3.1.0 base together; CI derives the pre-release number from tags, so the first beta push publishes 3.1.0b1 in both." Substitute the real FW_SHA (at least its first 7 characters). Record this commit's full sha as APP_SHA.

7. Append to the evidence file each command and its output: `git -C /workspaces/firestarter_app fetch -q --tags origin`; from `/workspaces/firestarter_app`, `GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run` (expect `DRY_RUN: 3.1.0b1`); `git merge-tree --write-tree --name-only HEAD origin/beta | wc -l` (expect `1`); `git show --name-only --format= HEAD` (expect `firestarter/__init__.py`); `.venv311/bin/firestarter --version` (expect `Firestarter, version 3.1.0b1`); and `.venv311/bin/python -c 'from firestarter.channel import is_prerelease_build; print(is_prerelease_build())'` (expect `True`: the build stays on the beta channel, so `fw` keeps routing to `--pre`).

8. Run the app gates on the bumped tree, reusing Phase 206's commands verbatim: `.venv311/bin/ruff check firestarter/ tests/`, `.venv311/bin/ruff format --check firestarter/ tests/`, and `.venv311/bin/python -m pytest tests/ -o addopts="" -p no:cacheprovider -q` (about 190 s; allow a 600 s timeout). Phase 206 closed at 2363 passed; the merge and the bump add and remove no test, so expect 2363 passed and no failure. Append the three result lines to the evidence file.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && B=$(git log -1 --format=%H --fixed-strings --grep='chore(207-01): bump app version' HEAD) && FW=$(git -C /workspaces/firestarter_fw log -1 --format=%H --fixed-strings --grep='chore(207-01): bump firmware version' HEAD) && [ -n "$B" ] && [ -n "$FW" ] && git merge-base --is-ancestor "$B" HEAD && M=$(git rev-parse "$B^1") && [ "$(sed -n 1p firestarter/__init__.py)" = '__version__ = "3.1.0b1"' ] && [ "$(git show --name-only --format= "$B")" = "firestarter/__init__.py" ] && git show -s --format=%B "$B" | /usr/bin/grep -qF -e "$(printf '%s' "$FW" | cut -c1-7)" && [ "$(git rev-parse "$M^1")" = "d723cf77b7dac0e6d862e480b08d46de30b20ea9" ] && [ "$(git rev-parse "$M^2")" = "$(git rev-parse origin/beta)" ] && [ "$(git diff --name-only d723cf77b7dac0e6d862e480b08d46de30b20ea9 "$M" | LC_ALL=C sort | tr '\n' ' ')" = ".planning/codebase/STACK.md firestarter/__init__.py " ] && [ "$(GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run)" = "DRY_RUN: 3.1.0b1" ] && [ "$(git merge-tree --write-tree --name-only HEAD origin/beta | wc -l)" = "1" ] && [ "$(.venv311/bin/firestarter --version)" = "Firestarter, version 3.1.0b1" ] && .venv311/bin/python --version | /usr/bin/grep -qE -e '^Python 3\.11\.' && echo PASS-207-01-T2</automated>
    <fails_when>stdout lacks the line `PASS-207-01-T2`: the venv interpreter is missing or not 3.11, the dry-run prints anything but `DRY_RUN: 3.1.0b1`, the commit touches more than `firestarter/__init__.py`, its body does not carry the firmware sha, or the merge is not the bump's direct parent</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/ && .venv311/bin/ruff format --check firestarter/ tests/ && .venv311/bin/python -m pytest tests/ -o addopts="" -p no:cacheprovider -q 2>&1 | tail -1</automated>
    <fails_when>non-zero exit from either ruff command (ruff prints the offending paths), or the printed pytest summary line contains `failed` or `error` or reports fewer than 2363 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `.venv311/bin/python --version` in `/workspaces/firestarter_app` prints a `Python 3.11.` version.
    - `sed -n 1p /workspaces/firestarter_app/firestarter/__init__.py` prints exactly `__version__ = "3.1.0b1"`.
    - `git -C /workspaces/firestarter_app show --name-only --format= HEAD` prints exactly `firestarter/__init__.py`, and `git log -1 --format=%s HEAD` prints `chore(207-01): bump app version 3.0.0b50 -> 3.1.0b1`.
    - The app bump commit's body contains the first 7 characters of FW_SHA (the commit-pair link).
    - D-01 ordering: with B the app bump sha and M its parent (`git rev-parse "$B^1"`), `git rev-parse "$M^1"` prints `d723cf77b7dac0e6d862e480b08d46de30b20ea9` and `git rev-parse "$M^2"` equals `origin/beta`.
    - `git -C /workspaces/firestarter_app diff --name-only d723cf77b7dac0e6d862e480b08d46de30b20ea9 "$M"` lists exactly `.planning/codebase/STACK.md` and `firestarter/__init__.py`.
    - In `/workspaces/firestarter_app`, the dry-run prints exactly `DRY_RUN: 3.1.0b1`, and `merge-tree ... | wc -l` prints `1`.
    - `.venv311/bin/firestarter --version` prints exactly `Firestarter, version 3.1.0b1`, and `is_prerelease_build()` prints `True`.
    - `ruff check` prints `All checks passed!`, `ruff format --check` reports every file already formatted, and the pytest summary line reports at least `2363 passed` with no `failed`.
    - The two pre-existing untracked paths are still untracked: `git -C /workspaces/firestarter_app status --porcelain` lists them with `??` and lists nothing else.
  </acceptance_criteria>
  <done>The app half of the pair is committed on the milestone branch over a merge of beta, names its firmware partner, passes the CI-parity gates on Python 3.11, and its publisher says the first beta push cuts 3.1.0b1.</done>
</task>

<task type="auto">
  <name>Task 3: README upgrade-order fix, meta gitlink advance, and the whole-pair proof</name>
  <reversibility rating="reversible">A one-paragraph README edit and a gitlink commit, both local and unpushed.</reversibility>
  <precondition>Tasks 1 and 2 are committed; FW_SHA and APP_SHA are recorded; `git -C /workspaces diff --cached --name-only` prints nothing.</precondition>
  <files>firestarter_app/README.md, firestarter_fw, firestarter_app, .planning/phases/207-the-version-and-the-record/evidence/207-01-oracles.txt</files>
  <read_first>
    - `firestarter_app/README.md` lines 38-55: the pre-release install block (lines 41-47) and the bold mixed-version paragraph (lines 49-51). This is the file being modified.
    - `207-PATTERNS.md` § "firestarter_app/README.md lines 49-51" and § "Meta gitlink advance" (the `5e675d35` body shape).
    - `207-RESEARCH.md` § Q4 item 2, § Open Design Forks F6, and § Verification Commands V1, V2, V3, V4, V9, V10.
  </read_first>
  <action>
1. README (research F6, option A). In `/workspaces/firestarter_app/README.md` replace only the three-line bold paragraph on lines 49-51. Its current lead sentence says the CLI and the firmware are upgraded together, and its second sentence makes a claim about how every mismatched pair fails; from 3.1.0b1 that claim is false, because some mixed pairs work and one fails quietly. New text: the bold lead `**Upgrade the CLI first, then the firmware.**`, then one sentence saying that from 3.1.0b1 not every mixed CLI and firmware pair is refused, and one sentence pointing to the wiki for which pairings work, keeping the absolute link `[Breaking Changes](https://github.com/henols/firestarter/wiki/Breaking-Changes)` to the page root (no anchor: GitHub's anchor for a heading with an em dash is fragile). Leave the install block on lines 41-47 unchanged; it is already in the right order. The text carries no phase number, no decision id, no `.planning/` path and no milestone label: the README is the PyPI long description. Commit only `README.md` with the subject `docs(207-01): tell readers to upgrade the CLI before the firmware`. Record its sha as README_SHA.

2. Meta gitlink advance (F2, `5e675d35` precedent). Confirm `git -C /workspaces diff --cached --name-only` prints nothing. Commit with plain git and an explicit pathspec, so only the two gitlinks are recorded and none of the unrelated dirty meta files ride along: `git -C /workspaces commit -m "chore(207-01): advance firestarter_fw and firestarter_app gitlinks" -m "BODY" -- firestarter_fw firestarter_app`. BODY carries one bullet per sub-repo in the `<name> @ <short-sha> (<subject>)` form: `firestarter_fw @ FW_SHA (chore(207-01): bump firmware version 3.0.0b35 -> 3.1.0b1)`; `firestarter_app @ README_SHA (docs(207-01): tell readers to upgrade the CLI before the firmware), on top of APP_SHA (chore(207-01): bump app version 3.0.0b50 -> 3.1.0b1)`; and one plain sentence that each sub-repo tip sits on a merge of origin/beta made before its bump (D-01). Then `git -C /workspaces rev-parse --abbrev-ref HEAD` must print `v1.41-verification-to-host`.

3. Whole-pair proof. Run the three verify legs below and append each command and its output to `evidence/207-01-oracles.txt`. The third leg is the criterion-4 prohibition proof; it fetches `beta` and `main` fresh and reads GitHub's release count. Commit the evidence file together with the plan SUMMARY through the executor's normal SUMMARY commit, passing explicit `--files`, and re-check the meta branch afterwards.
  </action>
  <verify>
    <automated>FW=$(git -C /workspaces/firestarter_fw log -1 --format=%H --fixed-strings --grep='chore(207-01): bump firmware version' HEAD) && APP=$(git -C /workspaces/firestarter_app log -1 --format=%H --fixed-strings --grep='chore(207-01): bump app version' HEAD) && G=$(git -C /workspaces log -1 --format=%H --fixed-strings --grep='chore(207-01): advance firestarter_fw and firestarter_app gitlinks' HEAD) && [ -n "$FW" ] && [ -n "$APP" ] && [ -n "$G" ] && [ "$(git -C /workspaces/firestarter_fw show --name-only --format= "$FW")" = "include/version.h" ] && [ "$(git -C /workspaces/firestarter_app show --name-only --format= "$APP")" = "firestarter/__init__.py" ] && [ "$(sed -n 1p /workspaces/firestarter_app/firestarter/__init__.py)" = '__version__ = "3.1.0b1"' ] && [ "$(/usr/bin/grep -E -e '^#define VERSION ' /workspaces/firestarter_fw/include/version.h)" = '#define VERSION "3.1.0b1"' ] && [ "$(git -C /workspaces/firestarter_app show --name-only --format= HEAD)" = "README.md" ] && [ "$(/usr/bin/grep -cF -e '**Upgrade the CLI first, then the firmware.**' /workspaces/firestarter_app/README.md)" = "1" ] && /usr/bin/grep -qF -e '(https://github.com/henols/firestarter/wiki/Breaking-Changes)' /workspaces/firestarter_app/README.md && [ "$(/usr/bin/grep -cE -e 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]' /workspaces/firestarter_app/README.md)" = "0" ] && [ "$(git -C /workspaces ls-tree HEAD firestarter_app | awk '{print $3}')" = "$(git -C /workspaces/firestarter_app rev-parse HEAD)" ] && [ "$(git -C /workspaces ls-tree HEAD firestarter_fw | awk '{print $3}')" = "$(git -C /workspaces/firestarter_fw rev-parse HEAD)" ] && [ "$(git -C /workspaces show --name-only --format= "$G" | LC_ALL=C sort | tr '\n' ' ')" = "firestarter_app firestarter_fw " ] && for r in /workspaces /workspaces/firestarter_app /workspaces/firestarter_fw; do [ "$(git -C $r rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] || { echo "BRANCH MOVED $r"; exit 1; }; done && echo PASS-207-01-PAIR</automated>
    <fails_when>stdout lacks `PASS-207-01-PAIR`, or prints `BRANCH MOVED` with a repository path: a version commit touches more than its one file, a gitlink differs from its sub-repo HEAD, the meta commit touches more than the two gitlinks, or the README still lacks the upgrade-order lead or carries a planning identifier</fails_when>
    <automated>for r in firestarter_app firestarter_fw; do git -C /workspaces/$r fetch -q --tags origin && git -C /workspaces/$r fetch -q origin beta main || { echo "FETCH FAIL $r"; exit 1; }; out=$(cd /workspaces/$r && GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run); [ "$out" = "DRY_RUN: 3.1.0b1" ] || { echo "FAIL $r: $out"; exit 1; }; n=$(git -C /workspaces/$r merge-tree --write-tree --name-only HEAD origin/beta | wc -l); [ "$n" = "1" ] || { echo "FAIL $r: merge-tree printed $n lines"; exit 1; }; done; echo PASS-207-01-ORACLE</automated>
    <fails_when>stdout prints `FETCH FAIL`, or `FAIL <repo>: DRY_RUN: 3.0.0b…` (bump missing) or `DRY_RUN: 3.1.0b2` (a 3.1.0b1 tag already exists, a collision), or `FAIL <repo>: merge-tree printed N lines` (the ship PR would conflict), and `PASS-207-01-ORACLE` is absent</fails_when>
    <automated>export XDG_CACHE_HOME=/tmp/claude-gh-cache; for r in firestarter_app firestarter_fw; do git -C /workspaces/$r fetch -q origin beta main || { echo "FETCH FAIL $r"; exit 1; }; S=$(git -C /workspaces/$r log -1 --format=%H --fixed-strings --grep='chore(207-01): bump' HEAD); [ -n "$S" ] || { echo "NO BUMP SHA $r"; exit 1; }; for b in beta main; do git -C /workspaces/$r rev-parse -q --verify "origin/$b" >/dev/null || { echo "NO REF $r $b"; exit 1; }; if git -C /workspaces/$r merge-base --is-ancestor "$S" "origin/$b"; then echo "PUBLISHED $r $b"; exit 1; fi; done; [ -z "$(git -C /workspaces/$r ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/3.1.0*')" ] || { echo "REMOTE REF $r"; exit 1; }; [ -z "$(git -C /workspaces/$r tag --contains "$S")" ] || { echo "TAG $r"; exit 1; }; done; G=$(git -C /workspaces log -1 --format=%H --fixed-strings --grep='chore(207-01): advance firestarter_fw and firestarter_app gitlinks' HEAD); [ -n "$G" ] || { echo "NO GITLINK COMMIT"; exit 1; }; [ -z "$(git -C /workspaces tag --contains "$G")" ] || { echo "META TAG"; exit 1; }; [ -z "$(git -C /workspaces ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/v1.41*')" ] || { echo "META REMOTE REF"; exit 1; }; [ -z "$(git -C /workspaces tag --list 'v1.41*')" ] || { echo "META LOCAL TAG"; exit 1; }; [ "$(gh api repos/henols/firestarter/releases --jq length)" = "0" ] || { echo "META RELEASE"; exit 1; }; [ ! -d /workspaces/.github/workflows ] || { echo "META WORKFLOWS"; exit 1; }; echo PASS-207-PROHIBITION</automated>
    <fails_when>stdout prints any of `PUBLISHED`, `REMOTE REF`, `TAG`, `META TAG`, `META REMOTE REF`, `META LOCAL TAG`, `META RELEASE`, `META WORKFLOWS`, `FETCH FAIL`, `NO REF`, `NO BUMP SHA` or `NO GITLINK COMMIT`, and `PASS-207-PROHIBITION` is absent (a gh error prints text rather than 0, so it fails closed)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app show --name-only --format= HEAD` prints exactly `README.md`, and the README carries exactly one `**Upgrade the CLI first, then the firmware.**` line and the absolute link `https://github.com/henols/firestarter/wiki/Breaking-Changes`.
    - `/usr/bin/grep -cE -e 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]' /workspaces/firestarter_app/README.md` prints `0`.
    - `git -C /workspaces show --name-only --format=` on the gitlink commit lists exactly `firestarter_app` and `firestarter_fw`, and its body names FW_SHA, README_SHA and APP_SHA.
    - `git -C /workspaces ls-tree HEAD firestarter_app firestarter_fw` shows each gitlink equal to that sub-repo's `rev-parse HEAD`.
    - Both sub-repos print `DRY_RUN: 3.1.0b1` after a fresh tag fetch, and both `merge-tree` checks print one line (`PASS-207-01-ORACLE`).
    - The prohibition leg prints `PASS-207-PROHIBITION`: no bump commit is on any remote `beta` or `main`, no `v1.41-verification-to-host` branch and no `3.1.0*` tag exist on either sub-repo remote, no tag contains any new commit, the meta repo has no `v1.41` tag locally or remotely, and `gh api repos/henols/firestarter/releases --jq length` prints `0`.
    - `git rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host` in `/workspaces`, `/workspaces/firestarter_app` and `/workspaces/firestarter_fw`.
    - `evidence/207-01-oracles.txt` contains two `DRY_RUN: 3.1.0b1` lines, `PASS-207-01-ORACLE` and `PASS-207-PROHIBITION`.
  </acceptance_criteria>
  <done>REL-01 holds and is proven by the publisher's own code in both repos; the meta repo records the pair; the README no longer contradicts the release; and nothing was pushed, tagged or released.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone branch to `beta` | A later push or merge to `beta` in either sub-repo publishes immediately (PyPI for the app, a GitHub pre-release for the firmware), with no path filter. |
| local git to remote | Any push from this plan would cross into public, irreversible published state. |
| meta working tree to meta commit | Unrelated dirty files share the working tree with the two gitlinks this plan commits. |
| package index to `.venv311` | The venv rebuild installs the project's own `[test]` extra from PyPI via uv. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-01 | Tampering | version line at ship merge | high | mitigate | D-01: beta is merged into each milestone branch before the bump (Tasks 1-2), so the ship PR has no version conflict to mis-resolve; a one-line `merge-tree --write-tree --name-only` result and `DRY_RUN: 3.1.0b1` are asserted in both repos (Task 3 oracle leg). A wrong resolution would have published `3.0.0b51` / `3.0.0b36` permanently. |
| T-207-02 | Elevation of Privilege | `git push` to `beta` / `main` | high | mitigate | No task issues any push. Task 3's prohibition leg proves by ancestry after a fresh fetch that neither bump commit reached `origin/beta` or `origin/main`, and that no milestone branch or `3.1.0*` tag exists on either remote. |
| T-207-03 | Spoofing | meta GitHub Release / `v1.41` tag | high | mitigate | No tag or release command in the plan; the prohibition leg asserts 0 Releases via `gh api`, no `.github/workflows` in meta, and no `v1.41` tag locally or remotely. |
| T-207-04 | Tampering | meta gitlink commit | medium | mitigate | Explicit-pathspec `git commit -- firestarter_fw firestarter_app` with an empty index first; the pair leg asserts the commit's change list is exactly the two gitlinks. |
| T-207-05 | Tampering | `gsd-tools query commit` branch switch | medium | mitigate | `rev-parse --abbrev-ref HEAD` is asserted in all three repos in Task 3 and after every gsd commit. |
| T-207-06 | Repudiation | commit-pair provenance | low | mitigate | The app bump body names FW_SHA; the meta body names all three sub-repo shas; the evidence file records every oracle output. |
| T-207-SC | Tampering | `uv pip install -e '.[test]'` | low | accept | No new package: the rebuild reinstalls the project's own pinned `[test]` extra that CI already installs (research § Package Legitimacy Audit: not applicable, none flagged). |
</threat_model>

<verification>
- Task 1 legs: `PASS-207-01-T1`, and the firmware tests/ tree reports at least 316 passed.
- Task 2 legs: `PASS-207-01-T2`, both ruff commands clean, and pytest reports at least 2363 passed on Python 3.11.
- Task 3 legs: `PASS-207-01-PAIR`, `PASS-207-01-ORACLE`, `PASS-207-PROHIBITION`.
- ROADMAP success criterion 1 is met: both files read `3.1.0b1`, bumped in one commit pair.
- ROADMAP success criterion 4 holds at this plan's end: no Release, no tag, no push.
</verification>

<success_criteria>
- `firestarter_fw/include/version.h` and `firestarter_app/firestarter/__init__.py` both read `3.1.0b1`, each changed by one single-file commit, the app commit naming the firmware sha.
- Each bump sits directly on a merge of `origin/beta` (D-01), and both ship merges are proven conflict-free.
- Both publishers print `DRY_RUN: 3.1.0b1`.
- The meta gitlinks point at both sub-repo HEADs through one two-path commit.
- App gates green on Python 3.11 and the firmware tests/ tree green on the bumped tree.
- Nothing pushed, tagged or released.
</success_criteria>

<output>
Create `.planning/phases/207-the-version-and-the-record/207-01-SUMMARY.md` when done. It must record:
the commit trio in the form "firmware `<FW_SHA>`, host `<APP_SHA>` (+ README `<README_SHA>`), meta gitlink
advance `<G>`"; both merge-commit shas; the beta versions actually merged (expected `3.0.0b35` / `3.0.0b50`)
and any deviation; the pytest and ruff result lines; and the three PASS markers. Commit the SUMMARY with
`evidence/207-01-oracles.txt` using explicit `--files`, then confirm the meta branch is still
`v1.41-verification-to-host`.
</output>
