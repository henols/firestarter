---
phase: 207-the-version-and-the-record
plan: 03
type: execute
wave: 2
depends_on: ["207-01", "207-02"]
commits_land_in: "the live public wiki henols/firestarter.wiki.git master, by one operator-approved fast-forward push; plus the meta commit carrying this plan's SUMMARY and evidence"
files_modified:
  - .planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt
  - .planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-postpush-freshclone.txt
autonomous: false
requirements: [REL-04]
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-02: before approving, the operator saw the full `git log origin/master..HEAD` of /workspaces/firestarter.wiki with f967398 named as the unpublished tracker and PR-routing fix that publishes in the same push, and saw its full diff"
    - "D-03: the wiki is pushed in this phase only after the operator replies push approved (or reports pushed) at a checkpoint:human-action, and on hold nothing is published"
    - "The push is a fast-forward of master from 81229d8: afterwards `git ls-remote origin refs/heads/master` equals the clone's HEAD, 81229d8 is an ancestor of it, and HEAD is the exact sha the operator approved"
    - "A fresh, independent clone of henols/firestarter.wiki.git (not the working clone) carries Writing-and-Verifying.md and the 3.1.0b1 Breaking-Changes entry with both `Unknown command` lines, links the new page from Home and the sidebar, resolves every internal link, and carries no planning identifier"
    - "ROADMAP criterion 4 holds at the phase's end: henols/firestarter has 0 GitHub Releases, no v1.41 tag exists locally or remotely, no tag contains any phase commit, and neither sub-repo's beta or main contains a 207 bump commit"
  artifacts:
    - path: ".planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt"
      provides: "everything the operator reviewed before approving: the outgoing log naming f967398, its diff, the navigation and Breaking-Changes diffs, the hedged lines, the export-based checks and the prohibition leg"
      contains: "PREFLIGHT PASS"
    - path: ".planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-postpush-freshclone.txt"
      provides: "proof the push landed, taken against a second independent clone rather than the edited copy"
      contains: "POSTPUSH PASS"
  key_links:
    - from: "/workspaces/firestarter.wiki master"
      to: "https://github.com/henols/firestarter.wiki.git refs/heads/master"
      via: "one fast-forward git push after the operator's reply"
      pattern: "81229d8"
    - from: "Home.md and _Sidebar.md on the live wiki"
      to: "Writing-and-Verifying.md on the live wiki"
      via: "relative links checked in a fresh clone"
      pattern: "(Writing-and-Verifying)"
  prohibitions:
    - statement: "The only push in this phase is the wiki master fast-forward. No --force, no --force-with-lease, no --tags, no other ref, and no push from the meta repository or from either sub-repo, because a beta push in a sub-repo publishes irreversibly."
      requirement_id: REL-04
      status: enforced
      verification: "Task 3: 81229d8 is an ancestor of the pushed HEAD, the remote master equals the approved sha, and PASS-207-PROHIBITION shows no sub-repo or meta branch or tag reached a remote."
    - statement: "No GitHub Release is cut from the meta repository and no tag is created in any repository in this phase (ROADMAP criterion 4): a v1.41 tag or Release parses as PEP 440 1.41 and would report every stranded CLI as already up to date."
      requirement_id: REL-04
      status: enforced
      verification: "PASS-207-PROHIBITION in Tasks 1 and 3: gh api repos/henols/firestarter/releases --jq length prints 0, no v1.41 tag locally or remotely, git tag --contains is empty for every phase commit, and the wiki clone has no tags."
    - statement: "No credential is written to disk and the wiki remote URL is never rewritten; the one fallback uses a single-command credential helper."
      requirement_id: REL-04
      status: enforced
      verification: "Task 3: git -C /workspaces/firestarter.wiki config --get remote.origin.url prints exactly https://github.com/henols/firestarter.wiki.git after the push."
---

<objective>
Publish the 3.1.0b1 wiki record — and the older `f967398` fix it sits on — to the live public wiki,
only after the operator has seen every outgoing commit, and prove it landed from a clone the executor
never edited.

Purpose: REL-04 is not met while the pages exist only in a local clone, and D-03 (operator, 2026-09-23)
chose to push in-phase so REL-04 is verifiable against the live wiki now. A wiki push publishes the
instant it lands: no build, no preview, no pull request. D-02 chose to publish `f967398` (2026-09-15,
never pushed) in the same push, because the live Contributing page still routes firmware pull requests
to the planning repository; research found no `.planning/` record of that commit, so the operator must
see it named, with its diff, before approving.

**This plan writes to a live, public repository.** `autonomous: false` is load-bearing. The push sits
behind a `checkpoint:human-action`, the one checkpoint type that `--auto` and `--chain` never
auto-approve. Do not batch any other approval into it, and do not proceed on an inferred approval.

Output: the live wiki fast-forwarded from `81229d8`; a pre-push evidence file (what the operator
approved); a post-push evidence file from a fresh clone; a final criterion-4 proof.

Tracer note: Task 1 is the end-to-end slice up to the gate — the exact outgoing tree is exported with
`git archive` and every check runs against the export, so the evidence describes the bytes that will be
published, not a working copy that might differ.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/207-the-version-and-the-record/207-CONTEXT.md
@.planning/phases/207-the-version-and-the-record/207-RESEARCH.md
@.planning/phases/207-the-version-and-the-record/207-01-SUMMARY.md
@.planning/phases/207-the-version-and-the-record/207-02-SUMMARY.md

Precedent: v1.35's wiki push — author in a clone, run the checks, an operator `human-action` gate
showing the commit stat and the navigation hunks, push, then re-run the checks against a second,
independent fresh clone (`.planning/milestones/v1.35-phases/171-stray-the-root-level-documentation-files/171-01-PLAN.md`,
Task 2). The link checker that precedent used (`tools/wiki/`) was deleted on 2026-09-08; the inline
shell checks in this plan replace it. There is no automated wiki guard.

Rules: use `/usr/bin/grep` with `-e` for dash-leading patterns; after any `gsd-tools query commit`,
confirm `git -C /workspaces rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`; stage the
meta commit by explicit path only (the meta working tree carries unrelated dirty files).
</context>

## Decisions carried into this plan

- **D-02 (locked, operator):** push from the existing clone, publishing `f967398` with the 3.1.0b1 docs;
  the checkpoint shows the full `git log origin/master..HEAD` with `f967398` named.
- **D-03 (locked, operator):** push in-phase behind a `checkpoint:human-action` the operator approves;
  every new statement is labelled from 3.1.0b1 (enforced in plan 207-02, re-checked here on the
  published bytes).
- **Reversibility:** the push is rated `costly`, not `one-way`: public history keeps whatever lands and a
  wrong sentence can only be corrected by a later public commit, but no data migration or machine-read
  contract is involved. The operator already chose to publish in-phase (D-03), so no separate decision
  checkpoint re-asks it; the human-action gate below is where the operator confirms the exact bytes.

## Artifacts this phase produces

Phase-wide list. Items marked **(this plan)** are produced here.

- (plan 207-01) `firestarter_fw` and `firestarter_app`: a merge of `origin/beta` and a single-file
  `chore(207-01): bump … -> 3.1.0b1` commit in each; an app README commit; one meta gitlink commit;
  `evidence/207-01-oracles.txt`; `207-01-SUMMARY.md`.
- (plan 207-02) Three local wiki commits on top of `f967398` (Breaking-Changes entry;
  Writing-and-Verifying page with Home and sidebar lines; Install-Beta and Testing-Chips notes);
  `207-02-SUMMARY.md`.
- **(this plan)** The live wiki `henols/firestarter.wiki.git` `master`, fast-forwarded from `81229d8` to
  the approved sha (four commits: `f967398` and plan 207-02's three).
- **(this plan)** `evidence/207-03-wiki-prepush.txt` and `evidence/207-03-wiki-postpush-freshclone.txt`.
- **(this plan)** `207-03-SUMMARY.md` (or `207-03-BLOCKED.md` on `hold`, so a later resume re-runs this plan).
- Nothing else: no tag, no GitHub Release, no push of any sub-repo or meta branch.

<tasks>

<task type="tracer">
  <name>Task 1: Pre-flight end to end: export the exact outgoing tree, check it, and write the evidence the operator will review</name>
  <reversibility rating="reversible">Read-only against the remote; writes one evidence file in the meta repo.</reversibility>
  <precondition>`207-01-SUMMARY.md` and `207-02-SUMMARY.md` exist; `git -C /workspaces/firestarter.wiki status --porcelain` prints nothing; network access to github.com works (`git -C /workspaces/firestarter.wiki ls-remote origin refs/heads/master` returns a sha).</precondition>
  <files>.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt</files>
  <read_first>
    - `.planning/phases/207-the-version-and-the-record/207-02-SUMMARY.md`: the three wiki commit shas and the sweep output.
    - `.planning/phases/207-the-version-and-the-record/207-01-SUMMARY.md`: the bump shas the prohibition leg looks for.
    - `207-RESEARCH.md` § Q4 (the unpushed `f967398`, the push precedent), § Open Design Forks F7 and F8, § Common Pitfall 3, and § Verification Commands V7, V8, V9 and the post-push command.
    - `.planning/milestones/v1.35-phases/171-stray-the-root-level-documentation-files/171-01-PLAN.md` Task 2: the precedent gate's evidence list and its three-way resume signal.
    - `.planning/phases/207-the-version-and-the-record/evidence/` (created by plan 207-01): the evidence file written here goes beside `207-01-oracles.txt`.
  </read_first>
  <action>
Nothing is pushed in this task.

1. `git -C /workspaces/firestarter.wiki fetch origin`. `origin/master` must still be `81229d8ed8a280b935cf84c4c74f4c97afcdf475`, and `git -C /workspaces/firestarter.wiki merge-base --is-ancestor origin/master HEAD` must succeed, so the push will be a fast-forward. If the remote moved, STOP: do not rebase, do not merge; report the new remote sha to the operator.

2. Run the export leg (the first verify command). It exports HEAD with `git archive` into a fresh `mktemp -d` directory and runs the content, planning-identifier and link checks against the export, so an uncommitted edit in the clone cannot make them pass.

3. Write `evidence/207-03-wiki-prepush.txt` with these sections, in this order, each under a plain heading line:
   a. The outgoing commit list: `git -C /workspaces/firestarter.wiki log --format='%H %an %ad %s' --date=short origin/master..HEAD`, in full (expect four lines). Directly under it, one line naming `f967398` per D-02: "f967398fdf653f4ee77f7ad07b37a416927763be — Repoint the tracker at henols/firestarter and fix the firmware repo name — written 2026-09-15 and never published; it publishes in this push. It fixes the live Contributing page, which still sends firmware pull requests to the planning repository, and repoints Install-Beta and Testing-Chips from the old tracker alias."
   b. The full diff of `f967398` (`git show f967398`), because no planning record describes it.
   c. `git show --stat --format='%h %s'` for each of plan 207-02's three commits.
   d. `git diff origin/master..HEAD -- Home.md _Sidebar.md` in full (the two navigation hunks).
   e. `git diff origin/master..HEAD -- Breaking-Changes.md` in full (the preamble change and the new entry).
   f. The code-derived statements, for the operator's eye: `/usr/bin/grep -n -F -e 'not yet observed on hardware' Breaking-Changes.md Writing-and-Verifying.md` output, with one line saying these rows come from reading the code and were never observed on a bench, so the operator can strike them.
   g. The page list of the new page: `/usr/bin/grep -n -e '^## ' Writing-and-Verifying.md`.
   h. The export leg's output, ending `PASS-207-03-EXPORT`.
   i. The prohibition leg's output (the second verify command), ending `PASS-207-PROHIBITION`.
   j. The push target and the exact command: remote `https://github.com/henols/firestarter.wiki.git`, branch `master`, fast-forward from `81229d8` to the full sha printed by `git -C /workspaces/firestarter.wiki rev-parse HEAD`, command `git -C /workspaces/firestarter.wiki push origin master`. Label that sha `APPROVAL_SHA`.
   The file's last line is exactly `PREFLIGHT PASS`, written only when sections h and i both ended with their PASS markers.
  </action>
  <verify>
    <automated>W=/workspaces/firestarter.wiki; X=$(mktemp -d) && git -C $W fetch -q origin && [ "$(git -C $W rev-parse origin/master)" = "81229d8ed8a280b935cf84c4c74f4c97afcdf475" ] && git -C $W merge-base --is-ancestor origin/master HEAD && [ -z "$(git -C $W status --porcelain)" ] && git -C $W archive HEAD | tar -x -C "$X" && cd "$X" && [ -f Writing-and-Verifying.md ] && for p in 'Unknown command: 6' 'Unknown command: 4' 'Not blank, at 0x000000, v: 0x11' '## 3.1.0b1 — verify and blank check run on the host' 'pip install --pre --upgrade firestarter' '--verify' '--full' '--no-blank-check' 'Refusing write to'; do /usr/bin/grep -qF -e "$p" *.md || { echo "MISSING: $p"; exit 1; }; done && ! /usr/bin/grep -nE 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]' *.md && bad=0 && for l in $(/usr/bin/grep -ohE '\]\([A-Za-z-]+\)' *.md | sed -E 's/^\]\(|\)$//g' | sort -u); do [ -f "$l.md" ] || { echo "BROKEN: $l"; bad=1; }; done && [ "$bad" = "0" ] && [ "$(git -C $W rev-list --count origin/master..HEAD)" = "4" ] && git -C $W log --format=%H origin/master..HEAD | /usr/bin/grep -qx -e 'f967398fdf653f4ee77f7ad07b37a416927763be' && echo PASS-207-03-EXPORT</automated>
    <fails_when>stdout prints `MISSING: <text>`, `BROKEN: <Page>`, or lines matching a planning identifier, or lacks `PASS-207-03-EXPORT` because the remote moved off 81229d8, the push would not be a fast-forward, the clone is dirty, or the outgoing list is not exactly four commits including f967398</fails_when>
    <automated>export XDG_CACHE_HOME=/tmp/claude-gh-cache; for r in firestarter_app firestarter_fw; do git -C /workspaces/$r fetch -q origin beta main || { echo "FETCH FAIL $r"; exit 1; }; S=$(git -C /workspaces/$r log -1 --format=%H --fixed-strings --grep='chore(207-01): bump' HEAD); [ -n "$S" ] || { echo "NO BUMP SHA $r"; exit 1; }; for b in beta main; do git -C /workspaces/$r rev-parse -q --verify "origin/$b" >/dev/null || { echo "NO REF $r $b"; exit 1; }; if git -C /workspaces/$r merge-base --is-ancestor "$S" "origin/$b"; then echo "PUBLISHED $r $b"; exit 1; fi; done; [ -z "$(git -C /workspaces/$r ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/3.1.0*')" ] || { echo "REMOTE REF $r"; exit 1; }; [ -z "$(git -C /workspaces/$r tag --contains "$S")" ] || { echo "TAG $r"; exit 1; }; done; G=$(git -C /workspaces log -1 --format=%H --fixed-strings --grep='chore(207-01): advance firestarter_fw and firestarter_app gitlinks' HEAD); [ -n "$G" ] || { echo "NO GITLINK COMMIT"; exit 1; }; [ -z "$(git -C /workspaces tag --contains "$G")" ] || { echo "META TAG"; exit 1; }; [ -z "$(git -C /workspaces ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/v1.41*')" ] || { echo "META REMOTE REF"; exit 1; }; [ -z "$(git -C /workspaces tag --list 'v1.41*')" ] || { echo "META LOCAL TAG"; exit 1; }; [ "$(gh api repos/henols/firestarter/releases --jq length)" = "0" ] || { echo "META RELEASE"; exit 1; }; [ ! -d /workspaces/.github/workflows ] || { echo "META WORKFLOWS"; exit 1; }; [ -z "$(git -C /workspaces/firestarter.wiki tag --list)" ] || { echo "WIKI TAG"; exit 1; }; echo PASS-207-PROHIBITION</automated>
    <fails_when>stdout prints any of `PUBLISHED`, `REMOTE REF`, `TAG`, `META TAG`, `META REMOTE REF`, `META LOCAL TAG`, `META RELEASE`, `META WORKFLOWS`, `WIKI TAG`, `FETCH FAIL`, `NO REF`, `NO BUMP SHA` or `NO GITLINK COMMIT`, and `PASS-207-PROHIBITION` is absent</fails_when>
    <automated>E=/workspaces/.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt; [ "$(tail -n 1 "$E")" = "PREFLIGHT PASS" ] && /usr/bin/grep -qF -e 'f967398fdf653f4ee77f7ad07b37a416927763be' "$E" && /usr/bin/grep -qF -e 'Repoint the tracker at henols/firestarter and fix the firmware repo name' "$E" && /usr/bin/grep -qF -e '_Sidebar.md' "$E" && /usr/bin/grep -qF -e 'not yet observed on hardware' "$E" && /usr/bin/grep -qF -e 'PASS-207-03-EXPORT' "$E" && /usr/bin/grep -qF -e 'PASS-207-PROHIBITION' "$E" && /usr/bin/grep -qF -e "$(git -C /workspaces/firestarter.wiki rev-parse HEAD)" "$E" && echo PASS-207-03-EVIDENCE</automated>
    <fails_when>stdout lacks `PASS-207-03-EVIDENCE`: the evidence file is missing, its last line is not `PREFLIGHT PASS`, or it omits the f967398 sha and subject, the navigation diff, the hedged lines, either PASS marker, or the approval sha</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter.wiki rev-parse origin/master` prints `81229d8ed8a280b935cf84c4c74f4c97afcdf475` after a fresh fetch, and `origin/master` is an ancestor of HEAD.
    - The export leg prints `PASS-207-03-EXPORT`, run against a `git archive HEAD` export, not the working tree.
    - `git -C /workspaces/firestarter.wiki rev-list --count origin/master..HEAD` prints `4`, and the list contains `f967398fdf653f4ee77f7ad07b37a416927763be`.
    - The prohibition leg prints `PASS-207-PROHIBITION`.
    - `evidence/207-03-wiki-prepush.txt` contains sections a to j in order: the full outgoing log with the D-02 line naming `f967398`, `f967398`'s full diff, the three commit stats, the Home and sidebar diff, the Breaking-Changes diff, the `not yet observed on hardware` lines, the new page's `##` list, both PASS markers, and the push target with `APPROVAL_SHA`; its last line is exactly `PREFLIGHT PASS`.
    - No `git push` was run.
  </acceptance_criteria>
  <done>The exact bytes that would be published are checked from an export, criterion 4 is re-proven, and one evidence file holds everything the operator needs to decide, ending PREFLIGHT PASS.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator gate: publish the four wiki commits, f967398 among them, to the live public wiki</name>
  <read_first>
    - `.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt` in full: it is the entire basis of the decision.
  </read_first>
  <what-built>
    Four local commits on `master` in `/workspaces/firestarter.wiki`, none of them published: `f967398`
    (2026-09-15, the tracker and PR-routing fix that D-02 chose to publish now) and plan 207-02's three —
    the Breaking-Changes 3.1.0b1 entry, the new `Writing-and-Verifying` page with its Home and sidebar
    lines, and the Install-Beta and Testing-Chips version-pairing notes. Every check passed against a
    `git archive` export of that exact HEAD, criterion 4 is re-proven, and the live wiki is still at
    `81229d8`.
  </what-built>
  <action>
**This is the phase's irreversible, outward-facing act.** A wiki push is public the moment it lands, and
this plan cannot unpublish it. It is a `human-action` checkpoint because `--auto` and `--chain` auto-approve
`human-verify` and `decision` gates, and this type is the one that reaches the operator. Present it even
under `--auto` or `--chain`. Do not batch any other approval into it, and do not proceed on an inferred
or implied approval.

Present, from the evidence file, before asking anything:
1. Section a — the full outgoing log, four lines, with `f967398` named on its own line as the commit
   being published alongside the 3.1.0b1 pages (D-02).
2. Section b — `f967398`'s full diff (8 lines changed across Contributing, Install-Beta, Testing-Chips).
3. Section d — the Home and sidebar hunks in full.
4. Section e — the Breaking-Changes diff: the rewritten preamble (now `--upgrade`) and the new entry.
5. Section f — the rows marked `not yet observed on hardware`, stated as code-derived and never
   bench-observed, so the operator can keep or strike each.
6. Section j — the target `https://github.com/henols/firestarter.wiki.git`, branch `master`, a
   fast-forward from `81229d8` to `APPROVAL_SHA`, and the command that will run.

Then ask for exactly one of: `push approved`, `pushed`, `revise: <what to change>`, or `hold`.
Record the operator's reply verbatim in the SUMMARY.
  </action>
  <instructions>
    Already automated by Task 1: the fetch proving the live wiki is still at `81229d8`, the `git archive`
    export of the exact outgoing HEAD, the content, label, link and planning-identifier checks against
    that export, the criterion-4 prohibition leg, and the evidence file ending `PREFLIGHT PASS`.

    The one thing only the operator can do is decide to publish. Operator steps:
    1. Read the four-line outgoing log and confirm `f967398` is meant to publish now (D-02).
    2. Read `f967398`'s diff, the navigation hunks and the Breaking-Changes diff.
    3. Decide on each `not yet observed on hardware` row: keep, reword or strike.
    4. Confirm the target is `henols/firestarter.wiki.git`, branch `master`, and the sha is `APPROVAL_SHA`.
    5. Reply `push approved` to have the executor push; `pushed` if you ran `git -C /workspaces/firestarter.wiki push origin master` yourself; `revise: <change>` to have the executor make the change as a new local wiki commit, re-run Task 1, and present this gate again; or `hold` to stop with nothing published.
  </instructions>
  <verification>
    Before asking: the gate-ready leg below prints `PASS-207-03-GATE-READY` (evidence ends `PREFLIGHT PASS`,
    live wiki still at `81229d8`). After the reply: Task 3's post-push leg proves the remote `master`
    equals `APPROVAL_SHA` from a fresh clone; on `hold`, `git ls-remote` still shows `81229d8`.
  </verification>
  <resume-signal>Type "push approved", "pushed", "revise: ..." or "hold"</resume-signal>
  <verify>
    <automated>E=/workspaces/.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt; [ "$(tail -n 1 "$E")" = "PREFLIGHT PASS" ] && [ "$(git -C /workspaces/firestarter.wiki ls-remote origin refs/heads/master | cut -f1)" = "81229d8ed8a280b935cf84c4c74f4c97afcdf475" ] && echo PASS-207-03-GATE-READY</automated>
    <fails_when>stdout lacks `PASS-207-03-GATE-READY`: the evidence file does not end `PREFLIGHT PASS`, or the live wiki already moved off 81229d8 before the operator replied</fails_when>
  </verify>
  <acceptance_criteria>
    - The operator was shown sections a, b, d, e, f and j before being asked, and `f967398` was named explicitly (D-02).
    - The operator's reply is one of `push approved`, `pushed`, `revise: …` or `hold`, and it is recorded verbatim in the SUMMARY (or in `207-03-BLOCKED.md` on `hold`).
    - No `git push` ran before the reply; the gate-ready leg shows the live wiki still at `81229d8` at the moment of asking.
    - On `revise: …`, the change lands as a new local wiki commit with no planning identifiers, Task 1 is re-run, and this gate is presented again; the approval then refers to the new `APPROVAL_SHA`.
  </acceptance_criteria>
  <done>The operator has decided, with the full outgoing log and f967398's diff in front of them, and the decision is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Fast-forward push on approval, then prove it from a fresh clone and re-prove criterion 4</name>
  <reversibility rating="costly">Public wiki history keeps whatever is pushed, and a wrong statement can only be corrected by a later public commit while readers may already act on it; no data migration or machine-read contract is involved. The operator approves the exact sha at Task 2 (D-03).</reversibility>
  <precondition>The operator replied `push approved` or `pushed` at Task 2. On `hold`, do not run this task: write `207-03-BLOCKED.md` (never `207-03-SUMMARY.md`, which would make a later resume skip this plan) recording the reply and that nothing was published, and stop.</precondition>
  <files>.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-postpush-freshclone.txt</files>
  <read_first>
    - `.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt`: `APPROVAL_SHA` in section j.
    - `207-RESEARCH.md` § Verification Commands, "Post-push (wiki)".
  </read_first>
  <action>
1. On `push approved`: `git -C /workspaces/firestarter.wiki fetch origin`; confirm `origin/master` is still `81229d8ed8a280b935cf84c4c74f4c97afcdf475` and `git -C /workspaces/firestarter.wiki rev-parse HEAD` equals `APPROVAL_SHA` exactly. If either differs, do not push: re-run Task 1 and present Task 2 again. Then run `git -C /workspaces/firestarter.wiki push origin master` — plain: never `--force`, never `--force-with-lease`, never `--tags`, never any ref other than `master`. It authenticates through the configured credential helper. Only if it fails with an authentication error, retry once with `git -C /workspaces/firestarter.wiki -c credential.helper= -c 'credential.helper=!gh auth git-credential' push origin master`; never rewrite the remote URL and never write a token into any config file. Record the push output line (`81229d8..<sha>  master -> master`) in the SUMMARY.
   On `pushed`: skip the push; go to step 2.

2. Run the post-push leg (the first verify command). It confirms the remote `master` equals the clone's HEAD and `APPROVAL_SHA`, that `81229d8` is an ancestor (a fast-forward), and that the remote URL is unchanged; then clones `https://github.com/henols/firestarter.wiki.git` with `--depth 1` into a fresh `mktemp -d` directory — never the working clone, which shows its own commits whether or not the push landed — and re-runs the content, D-03 label, navigation, planning-identifier, link and claim-stamp checks against that fresh clone. Write its full output to `evidence/207-03-wiki-postpush-freshclone.txt`.

3. Run the prohibition leg (the second verify command) and append its output to the same file. Then append the line `POSTPUSH PASS`, only if both legs printed their PASS markers.

4. Commit `207-03-SUMMARY.md` and both `207-03` evidence files through the executor's SUMMARY commit with explicit `--files`, then confirm `git -C /workspaces rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`. The SUMMARY records: the operator's reply verbatim, the push output line, the published wiki sha, and the three PASS markers.
  </action>
  <verify>
    <automated>W=/workspaces/firestarter.wiki; E=/workspaces/.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-prepush.txt; A=$(git -C $W rev-parse HEAD) && /usr/bin/grep -qF -e "$A" "$E" && [ "$(git -C $W ls-remote origin refs/heads/master | cut -f1)" = "$A" ] && git -C $W merge-base --is-ancestor 81229d8ed8a280b935cf84c4c74f4c97afcdf475 "$A" && [ "$(git -C $W config --get remote.origin.url)" = "https://github.com/henols/firestarter.wiki.git" ] && X=$(mktemp -d) && git clone -q --depth 1 https://github.com/henols/firestarter.wiki.git "$X/wiki" && cd "$X/wiki" && [ "$(git rev-parse HEAD)" = "$A" ] && [ -f Writing-and-Verifying.md ] && for p in 'Unknown command: 6' 'Unknown command: 4' '## 3.1.0b1 — verify and blank check run on the host' 'pip install --pre --upgrade firestarter' '--verify' '--full' '--no-blank-check' 'Refusing write to'; do /usr/bin/grep -qF -e "$p" *.md || { echo "MISSING: $p"; exit 1; }; done && ! /usr/bin/grep -nE 'Phase [0-9]{3}|\bD-[0-9]+\b|\.planning/|v1\.4[0-9]' *.md && awk '/^## /{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} s=$0; f=0} /3\.1\.0b1/{f=1} END{if(s!=""&&!f){print "UNLABELLED: " s; bad=1} exit bad}' Writing-and-Verifying.md && bad=0 && for l in $(/usr/bin/grep -ohE '\]\([A-Za-z-]+\)' *.md | sed -E 's/^\]\(|\)$//g' | sort -u); do [ -f "$l.md" ] || { echo "BROKEN: $l"; bad=1; }; done && [ "$bad" = "0" ] && /usr/bin/grep -qF -e '(Writing-and-Verifying)' Home.md && /usr/bin/grep -qF -e '(Writing-and-Verifying)' _Sidebar.md && [ "$(tail -n 1 Breaking-Changes.md)" = "$(git -C $W show 81229d8ed8a280b935cf84c4c74f4c97afcdf475:Breaking-Changes.md | tail -n 1)" ] && echo PASS-207-03-POSTPUSH</automated>
    <fails_when>stdout prints `MISSING:`, `UNLABELLED:`, `BROKEN:` or planning-identifier lines, or lacks `PASS-207-03-POSTPUSH`: the remote master is not the approved sha, the push was not a fast-forward from 81229d8, the remote URL was rewritten, the fresh clone's HEAD differs, the new page or a navigation link is absent from the published bytes, or the claim-stamp line changed</fails_when>
    <automated>export XDG_CACHE_HOME=/tmp/claude-gh-cache; for r in firestarter_app firestarter_fw; do git -C /workspaces/$r fetch -q origin beta main || { echo "FETCH FAIL $r"; exit 1; }; S=$(git -C /workspaces/$r log -1 --format=%H --fixed-strings --grep='chore(207-01): bump' HEAD); [ -n "$S" ] || { echo "NO BUMP SHA $r"; exit 1; }; for b in beta main; do git -C /workspaces/$r rev-parse -q --verify "origin/$b" >/dev/null || { echo "NO REF $r $b"; exit 1; }; if git -C /workspaces/$r merge-base --is-ancestor "$S" "origin/$b"; then echo "PUBLISHED $r $b"; exit 1; fi; done; [ -z "$(git -C /workspaces/$r ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/3.1.0*')" ] || { echo "REMOTE REF $r"; exit 1; }; [ -z "$(git -C /workspaces/$r tag --contains "$S")" ] || { echo "TAG $r"; exit 1; }; done; G=$(git -C /workspaces log -1 --format=%H --fixed-strings --grep='chore(207-01): advance firestarter_fw and firestarter_app gitlinks' HEAD); [ -n "$G" ] || { echo "NO GITLINK COMMIT"; exit 1; }; [ -z "$(git -C /workspaces tag --contains "$G")" ] || { echo "META TAG"; exit 1; }; [ -z "$(git -C /workspaces ls-remote origin refs/heads/v1.41-verification-to-host 'refs/tags/v1.41*')" ] || { echo "META REMOTE REF"; exit 1; }; [ -z "$(git -C /workspaces tag --list 'v1.41*')" ] || { echo "META LOCAL TAG"; exit 1; }; [ "$(gh api repos/henols/firestarter/releases --jq length)" = "0" ] || { echo "META RELEASE"; exit 1; }; [ ! -d /workspaces/.github/workflows ] || { echo "META WORKFLOWS"; exit 1; }; [ -z "$(git -C /workspaces/firestarter.wiki tag --list)" ] || { echo "WIKI TAG"; exit 1; }; echo PASS-207-PROHIBITION</automated>
    <fails_when>stdout prints any of `PUBLISHED`, `REMOTE REF`, `TAG`, `META TAG`, `META REMOTE REF`, `META LOCAL TAG`, `META RELEASE`, `META WORKFLOWS`, `WIKI TAG`, `FETCH FAIL`, `NO REF`, `NO BUMP SHA` or `NO GITLINK COMMIT`, and `PASS-207-PROHIBITION` is absent</fails_when>
    <automated>E=/workspaces/.planning/phases/207-the-version-and-the-record/evidence/207-03-wiki-postpush-freshclone.txt; [ "$(tail -n 1 "$E")" = "POSTPUSH PASS" ] && /usr/bin/grep -qF -e 'PASS-207-03-POSTPUSH' "$E" && /usr/bin/grep -qF -e 'PASS-207-PROHIBITION' "$E" && [ "$(git -C /workspaces rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207-03-RECORD</automated>
    <fails_when>stdout lacks `PASS-207-03-RECORD`: the post-push evidence file is missing, does not end `POSTPUSH PASS`, lacks either PASS marker, or the meta branch moved off v1.41-verification-to-host</fails_when>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter.wiki ls-remote origin refs/heads/master` prints the same sha as `git -C /workspaces/firestarter.wiki rev-parse HEAD`, and that sha is `APPROVAL_SHA` from the pre-push evidence.
    - `git -C /workspaces/firestarter.wiki merge-base --is-ancestor 81229d8ed8a280b935cf84c4c74f4c97afcdf475 HEAD` succeeds (a fast-forward; no force).
    - `git -C /workspaces/firestarter.wiki config --get remote.origin.url` prints exactly `https://github.com/henols/firestarter.wiki.git`.
    - A fresh `git clone --depth 1` of `https://github.com/henols/firestarter.wiki.git` into a new directory has HEAD equal to that sha, contains `Writing-and-Verifying.md`, `Unknown command: 6`, `Unknown command: 4`, `## 3.1.0b1 — verify and blank check run on the host`, `pip install --pre --upgrade firestarter`, `--verify`, `--full`, `--no-blank-check` and `Refusing write to`, and links `(Writing-and-Verifying)` from both `Home.md` and `_Sidebar.md`.
    - In the fresh clone, every `## ` section of `Writing-and-Verifying.md` contains `3.1.0b1` (D-03), no page matches a planning identifier, the link check prints no `BROKEN:` line, and `Breaking-Changes.md`'s last line equals `81229d8`'s.
    - The prohibition leg prints `PASS-207-PROHIBITION` (criterion 4 at the phase's end).
    - `evidence/207-03-wiki-postpush-freshclone.txt` ends with `POSTPUSH PASS`, and `207-03-SUMMARY.md` records the operator's reply verbatim, the push output line and the published sha.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`.
  </acceptance_criteria>
  <done>The live wiki carries the 3.1.0b1 record, proven from a clone the executor never touched; the push was the approved fast-forward and nothing else; criterion 4 holds at the end of the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local wiki clone to live public wiki | A push publishes page text and commit history instantly and permanently; there is no preview and no pull request. |
| operator approval to executor action | The executor may act outward only on an explicit operator reply, never on an inferred one or an auto-mode approval. |
| git credential helper | The push authenticates with the operator's GitHub credential. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207-12 | Tampering | unreviewed commit riding the push (`f967398`) | high | mitigate | D-02: the pre-push evidence carries the full outgoing log with `f967398` named on its own line and its full diff; the export leg asserts the outgoing list is exactly four commits including it. |
| T-207-13 | Elevation of Privilege | push without operator approval | high | mitigate | D-03: `checkpoint:human-action` (never auto-approved under `--auto` / `--chain`), `autonomous: false`; Task 3's precondition requires an explicit `push approved` or `pushed`, and `hold` writes `207-03-BLOCKED.md` and publishes nothing. |
| T-207-14 | Tampering | force-push rewriting public history | high | mitigate | Plain `git push origin master` only; Task 3 refuses to push unless `origin/master` is still `81229d8` and HEAD equals `APPROVAL_SHA`, and asserts afterwards that `81229d8` is an ancestor of the published sha. |
| T-207-15 | Tampering | bytes changed between approval and push | medium | mitigate | `APPROVAL_SHA` is written into the evidence the operator reviews; Task 3 pushes only if HEAD still equals it, and the post-push leg proves the remote equals it. |
| T-207-16 | Information Disclosure | credential leak into config | medium | mitigate | The fallback uses a one-command `-c credential.helper` and never rewrites the remote URL; the post-push leg asserts the URL is unchanged. |
| T-207-17 | Spoofing | meta Release / `v1.41` tag arming stranded CLIs | high | mitigate | Criterion 4 prohibition leg in Tasks 1 and 3: 0 Releases via `gh api`, no `v1.41` tag locally or remotely, no tag containing any phase commit, no meta workflows. |
| T-207-18 | Information Disclosure | planning identifiers in published text | medium | mitigate | The export and post-push legs reject phase numbers, decision ids, `.planning/` paths and v1.4x labels in every published page. |
</threat_model>

<verification>
- Task 1: `PASS-207-03-EXPORT`, `PASS-207-PROHIBITION`, `PASS-207-03-EVIDENCE`.
- Task 2: `PASS-207-03-GATE-READY`, and the operator's verbatim reply.
- Task 3: `PASS-207-03-POSTPUSH`, `PASS-207-PROHIBITION`, `PASS-207-03-RECORD`.
- ROADMAP criteria 2 and 3 are met on the live wiki; criterion 4 holds at the phase's end.
</verification>

<success_criteria>
- The live wiki `master` is the operator-approved sha, reached by a fast-forward from `81229d8`.
- A fresh clone proves the 3.1.0b1 entry, the new page and its navigation are published, labelled from 3.1.0b1, link-clean and free of planning identifiers.
- `f967398` was published knowingly, named in the evidence the operator approved (D-02).
- No tag, no Release, no sub-repo or meta push.
</success_criteria>

<output>
Create `.planning/phases/207-the-version-and-the-record/207-03-SUMMARY.md` when done (or
`207-03-BLOCKED.md` on `hold`). Record the operator's reply verbatim, the push output line, the
published wiki sha, which `not yet observed on hardware` rows the operator kept or struck, and the PASS
markers. Commit it with both `207-03` evidence files using explicit `--files`, then confirm
`git -C /workspaces rev-parse --abbrev-ref HEAD` prints `v1.41-verification-to-host`.
</output>
