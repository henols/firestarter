---
phase: 207.1-address-v1-41-tech-debt
plan: 04
subsystem: docs
tags: [bench-evidence, planning-hygiene, fw-docs, git-exclude, firestarter_fw, firestarter_app]

# Dependency graph
requires:
  - phase: 206-dev-test-keeps-its-fidelity-on-one-session
    provides: "the connect-cost bench measurement (median 2.613s, N=10) this plan moves into tracked evidence, and the CI-measured `tests/` collected count this plan corrects"
provides:
  - "D-16: the 206 connect-cost log is tracked bench evidence in meta, byte-identical to the app-tree original, with both citations in 206-SESSION-COST.md repointed"
  - "D-15: firestarter_fw/CLAUDE.md states the live `tests/` collected count (316), re-collected at execution time and dated the day it was measured"
  - "D-17: the deliberately-untracked LST62832I.pdf datasheet is hidden from `git status` via a per-clone `.git/info/exclude` line, never committed or deleted"
affects: [207.1-05, 207.1-06, 207.1-07]

# Actuals (#2632)
actuals:
  tokens: 1029
  tasks: 2
  commits: 2
  commits_by_repo:
    meta: 1
    firestarter_fw: 1
  plan_head_before:
    meta: 2767ab71f0a222bb48c8ad4c4b6411e84eacc4e1
    firestarter_fw: a55f2d8

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bench evidence artifacts move from an untracked app-tree scratch directory into a tracked `.planning/phases/<phase>/evidence/` directory beside the record that cites them, copy-verify-commit-prove-then-remove, never `git clean`"

key-files:
  created:
    - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt
  modified:
    - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md
    - firestarter_fw/CLAUDE.md
    - firestarter_app/.git/info/exclude (per-clone, untracked, never committed)

key-decisions:
  - "D-16 order followed exactly as specified: copy, cmp, repoint citations, stage-by-name, commit, prove committed blob sha256, only then rm -r the app-tree copy -- no git clean used anywhere"
  - "D-17 used a single anchored exact-path pattern (`/datasheets/LST62832I.pdf`), not a directory or wildcard, per the plan and PATTERNS.md correction that the exclude file holds only the 6 stock git comment lines (not the `claude-code-runtime` block RESEARCH.md mentioned)"

patterns-established: []

requirements-completed: []

coverage:
  - id: D1
    description: "D-16: the 206 connect-cost log is committed in meta as bench evidence, byte-identical to the app-tree original, with both citing sentences repointed"
    verification:
      - kind: other
        ref: "plan automated verify leg -- printed PASS-207.1-04-T1"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-15: firestarter_fw/CLAUDE.md states the live re-collected tests/ count and today's date"
    verification:
      - kind: other
        ref: "plan automated verify leg -- printed PASS-207.1-04-D15 316"
        status: pass
    human_judgment: false
  - id: D3
    description: "D-17: the kept datasheet is ignored per clone via .git/info/exclude, never committed or deleted"
    verification:
      - kind: other
        ref: "plan automated verify leg -- printed PASS-207.1-04-D17"
        status: pass
    human_judgment: false

duration: 25 min
completed: 2026-09-23
status: complete
---

# Phase 207.1 Plan 04: Bench Evidence, Test-Count Correction and Per-Clone Ignore Summary

**The 206 connect-cost bench log (median 2.613s, N=10) is now tracked meta evidence beside the record that cites it, `firestarter_fw/CLAUDE.md` states the live 316-collected-test count instead of the stale 320, and the deliberately-untracked LST62832I.pdf datasheet is hidden from `git status` via one anchored per-clone exclude line.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-09-23 (this session)
- **Completed:** 2026-09-23
- **Tasks:** 2 (1 tracer, 1 auto)
- **Files modified:** 4 (1 new tracked file, 2 modified tracked files, 1 modified per-clone untracked file)

## Accomplishments

- **D-16:** Copied `firestarter_app/connect-cost-2026-09-23-110029/connect-cost-log.txt` (23 lines: pinned port `/dev/ttyACM0`, controller identity, per-connect timing samples, no user path or secret) into `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt`, confirmed byte-identical with `cmp`, repointed both citations in `206-SESSION-COST.md` (the § 2 "Artifact:" sentence and the provenance-table Source cell) to the new meta path, committed exactly those two files in meta, proved the committed blob's sha256 equals `7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f`, and only then removed the app-tree copy with an explicit `rm -r` (never `git clean`).
- **D-15:** Re-collected `firestarter_fw`'s `tests/` Python suite (`316 tests collected`, `316 passed` in 10.90s) and corrected the stale "320 collected tests on 2026-09-22" sentence in `firestarter_fw/CLAUDE.md` to "316 collected tests on 2026-09-23", committed alone on `v1.41-verification-to-host` on top of `a55f2d8`.
- **D-17:** Appended the single anchored line `/datasheets/LST62832I.pdf` to `firestarter_app/.git/info/exclude` (confirmed to hold only the 6 stock git comment lines beforehand, per `207.1-PATTERNS.md`'s correction of `207.1-RESEARCH.md`'s `claude-code-runtime`-block claim). The datasheet stays on disk, untracked, unchanged (sha256 `76a831007b622ad66efeb389aecab7fb4fcf4d5a614bd3bd564ed2a9c75737df`), and now no longer appears in `git status --porcelain`.

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer): D-16 end to end** — `35feaea0` (meta repo) `docs(207.1-04): D-16 keep the 206 connect-cost log as bench evidence` (docs)
2. **Task 2: D-15 + D-17** — `4b14111` (firestarter_fw repo) `docs(207.1-04): D-15 correct the tests/ collected count` (docs)
   - D-17 produced no commit: the `.git/info/exclude` line is per-clone and deliberately never committed.

**Plan metadata:** this SUMMARY commit (meta repo, separate from the commits above).

## Files Created/Modified

- `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` — new tracked bench-evidence file, byte-identical to the removed app-tree original
- `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md` — two citations repointed from the app-tree path to the new meta evidence path
- `firestarter_fw/CLAUDE.md` — the `tests/` collected-count sentence corrected from 320/2026-09-22 to 316/2026-09-23
- `firestarter_app/.git/info/exclude` — per-clone, untracked, never committed; gained one anchored line, `/datasheets/LST62832I.pdf`

## Decisions Made

- Followed the plan's exact D-16 ordering (copy -> cmp -> repoint -> stage-by-name -> commit -> prove blob sha256 -> remove) with no shortcuts; `git clean` was never invoked in either repository.
- Used the single anchored exact-path exclude pattern for D-17, not a directory or wildcard, so any future datasheet added to the app tree stays visible in `git status`.

## Deviations from Plan

None — plan executed exactly as written. Both automated `<research_first>` corrections cited by the plan (D-15's live count of 316 vs. the stale 320; the exclude file holding only 6 stock lines, per `207.1-PATTERNS.md`'s own correction of `207.1-RESEARCH.md`) were already reflected in the plan's action text, so no in-flight adjustment was needed.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Both sub-repos (`firestarter_app`, `firestarter_fw`) and the meta repo remain on `v1.41-verification-to-host`; nothing was pushed from any repository, and no tag was created.
- The meta repo's `firestarter_app` and `firestarter_fw` gitlinks were deliberately left untouched (both now show ` M` in `git status`, as expected) — `207.1-07` owns advancing both gitlinks once, after every sub-repo commit in this phase.
- No `VERIFICATION.md` file was touched (confirmed via `git diff --quiet 10c9b615 HEAD -- ':(glob).planning/phases/20[2-7]-*/*-VERIFICATION.md'`, D-19 accepted for `206-VERIFICATION.md`'s `covered_files` drift on `206-SESSION-COST.md`).
- No blockers for subsequent plans (`207.1-05` through `207.1-07`).

## PASS Tokens (plan's `<automated>` verify legs)

- `PASS-207.1-04-T1` — printed after the committed blob's sha256 matched, the D-16 commit touched exactly the two named paths, both old-path citations were gone, at least two new-path citations were present, the app-tree directory no longer existed, the 206 phase directory was clean, and the meta branch was still `v1.41-verification-to-host`.
- `PASS-207.1-04-D15 316` — printed after the re-collected count (316) matched the corrected sentence in `CLAUDE.md`, the stale "320" sentence was gone, the D-15 commit existed on top of `a55f2d8`, touched only `CLAUDE.md`, the fw tree was clean, and the fw branch was still `v1.41-verification-to-host`.
- `PASS-207.1-04-D17` — printed after `check-ignore -v` named `.git/info/exclude` with the anchored pattern, the line existed exactly once, the datasheets directory no longer appeared in `git status --porcelain`, the PDF stayed untracked with its hash unchanged, and the whole app tree's status was empty.

## Command Output Evidence

**D-16 blob proof:**
```
$ sha256sum "$T"
7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f  <tmpfile>
```
(matches the plan-required hash exactly)

**D-15 collect and pass lines** (`firestarter_fw`, `/usr/local/py-utils/bin/pytest tests/ ... -o addopts=""`):
```
316 tests collected in 0.10s
...
316 passed in 10.90s
```

**D-17 check-ignore output** (`firestarter_app`):
```
$ git check-ignore -v datasheets/LST62832I.pdf
.git/info/exclude:7:/datasheets/LST62832I.pdf
```

## Self-Check: PASSED

- Meta commit `35feaea0` found in `git log --oneline --all` (meta repo).
- `firestarter_fw` commit `4b14111` found in `git log --oneline --all` (firestarter_fw repo), confirmed ancestor of neither `beta` nor `main` (sits only on `v1.41-verification-to-host`, on top of `a55f2d8`).
- All 3 claimed-created/modified files found on disk: the new evidence file (meta), `206-SESSION-COST.md` (meta), `firestarter_fw/CLAUDE.md`.
- The `firestarter_app/.git/info/exclude` line was verified present and functional via `check-ignore -v` (per-clone file, not part of any commit history to search).
- Re-ran all three plan-level `<automated>` legs: `PASS-207.1-04-T1`, `PASS-207.1-04-D15 316`, `PASS-207.1-04-D17` all printed.
- Re-ran the plan-level `<verification>` block: `.planning/phases/206-.../` status clean, `firestarter_app` status clean, `firestarter_fw` status clean with HEAD subject starting `docs(207.1-04): D-15`, and no `*-VERIFICATION.md` file touched since `10c9b615`.
- Confirmed all three repos (meta, `firestarter_app`, `firestarter_fw`) remain on branch `v1.41-verification-to-host`; nothing pushed; no tag created.
- Confirmed `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/config.json` show zero diff (`git status --porcelain` on those four paths is empty).

---
*Phase: 207.1-address-v1-41-tech-debt*
*Plan: 04*
*Completed: 2026-09-23*
