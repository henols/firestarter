---
phase: 207.1-address-v1-41-tech-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt
  - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md
  - firestarter_fw/CLAUDE.md
autonomous: true
requirements: []
coupling_justified:
  - "207.1-01: this plan's only app-side actions are removing one UNTRACKED directory and appending one line to the per-clone .git/info/exclude; neither touches the app index, a tracked file or a commit, so 207.1-01's app commits are correct in either order"
  - "207.1-05: both plans commit in meta, but on disjoint paths and with named-path staging only, and each checks the index is empty before staging; either order yields the same two commits"

estimate:
  tokens: 14000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-16: the measured per-connect cost log (median 2.613 s, N=10) is committed in meta at `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` with sha256 `7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f`, byte-identical to the app-tree original."
    - "D-16: both citations in `206-SESSION-COST.md` point at the new meta path, and no app-tree path to the log remains in that file."
    - "D-16: the app-tree `connect-cost-2026-09-23-110029/` directory is removed only after the meta commit exists and its blob hash matches; `git clean` is never used."
    - "D-15: `firestarter_fw/CLAUDE.md` states the live `tests/` collected count, re-collected in the task (316 at planning time), dated the day it was measured, committed on fw branch `v1.41-verification-to-host`."
    - "D-17: `firestarter_app/datasheets/LST62832I.pdf` is still on disk with sha256 `76a831007b622ad66efeb389aecab7fb4fcf4d5a614bd3bd564ed2a9c75737df`, still untracked, and ignored by the one anchored line `/datasheets/LST62832I.pdf` in the per-clone `/workspaces/firestarter_app/.git/info/exclude`, which is never committed."
    - "Nothing is pushed from meta, fw or app."
  artifacts:
    - path: .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt
      provides: "the 206 bench evidence, now tracked in meta (D-16)"
      min_lines: 23
    - path: .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md
      provides: "two citations repointed to the meta evidence path (D-16)"
      contains: "evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt"
    - path: firestarter_fw/CLAUDE.md
      provides: "the corrected tests/ collected count (D-15)"
  key_links:
    - from: .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md
      to: .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt
      via: "the § 2 artifact citation and the provenance-table Source cell"
      pattern: "evidence/connect-cost-2026-09-23-110029-connect-cost-log\\.txt"
---

<objective>
Clear the three housekeeping items that sit outside the app's source: D-16 (keep the 206 bench
measurement as evidence in meta instead of as untracked clutter in the app), D-15 (correct the fw
`CLAUDE.md` test count to the live number) and D-17 (hide the deliberately untracked datasheet from
`git status` without committing or deleting it).

Purpose: the audit found untracked clutter that would surface in a clean sdist, and a count in the
fw guide that the tree no longer matches. A bench measurement is never swept away: the log moves to
where the record that cites it lives.

Output: one meta evidence file and two repointed citations; one fw doc commit; one per-clone ignore
line. Covers audit items 205 "fw CLAUDE.md says 320" and cross-phase "untracked sub-repo clutter".
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_fw/CLAUDE.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate.

## Artifacts this phase produces (this plan)

- New tracked file path: `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` (and its new `evidence/` directory).
- New untracked per-clone line: `/datasheets/LST62832I.pdf` in `/workspaces/firestarter_app/.git/info/exclude`.
- No source symbol, CLI flag or dataclass field.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): D-16 end to end -- the connect-cost log becomes tracked 206 evidence, its citations follow it, and only then does the app copy go</name>
  <files>.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt, .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md</files>
  <read_first>
    - /workspaces/firestarter_app/connect-cost-2026-09-23-110029/connect-cost-log.txt (the 23-line log to preserve)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md (the § 2 re-anchored connect-cost paragraph about lines 80-90, and the provenance table about lines 318-330)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-VERIFICATION.md (frontmatter `covered_files`, which includes 206-SESSION-COST.md)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (Pitfall 6)
  </read_first>
  <action>
    Per D-16. Order matters: copy, repoint, commit, prove, and only then remove.

    1. Read the log and confirm it holds only the pinned port (`/dev/ttyACM0`), the controller identity and timing lines: no user path, no secret. `mkdir -p` the new directory `/workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence`, then `cp -p` the one file from the app's untracked `connect-cost-2026-09-23-110029` directory to `.../evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` (the timestamped name is kept; the 206 log has no plan prefix of its own). Run `cmp` on the two files and require exit 0. Confirm `git -C /workspaces check-ignore` does not ignore the target.

    2. In 206-SESSION-COST.md, replace the backticked app-tree path in the § 2 "Artifact:" sentence (about line 86) and in the Source cell of the "Leonardo-class connect median (re-anchored, this session)" provenance row (about line 323) with the new meta path `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt`, backticked the same way. Change nothing else in the file; do not keep the old app-tree path anywhere in it. 206-VERIFICATION.md lists this file in `covered_files`, so its digest drifts further; that is accepted under D-19. Do not run the `verification fingerprint` verb or edit any VERIFICATION.md.

    3. Confirm `git -C /workspaces diff --cached --name-only` is empty (the meta tree carries unrelated unstaged modifications; if anything is already staged, stop and report). Stage exactly the two paths by name with `git -C /workspaces add -- <evidence path> <206-SESSION-COST.md path>`, confirm the staged list is those two, and commit with `git -C /workspaces commit -m "docs(207.1-04): D-16 keep the 206 connect-cost log as bench evidence"`. Never `git add -A`, `git add .` or `commit -a`. Confirm the meta branch is still `v1.41-verification-to-host`.

    4. Prove the committed blob: write `git -C /workspaces show HEAD:<evidence path>` to a `mktemp` file and require its `sha256sum` to equal `7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f`. Only then remove the app copy with the explicit `rm -r /workspaces/firestarter_app/connect-cost-2026-09-23-110029`. Never use `git clean` in either repository. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt && Q=.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md && T=$(mktemp) && git show "HEAD:$P" > "$T" && [ "$(sha256sum "$T" | cut -d' ' -f1)" = "7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f" ] && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-04): D-16' HEAD) && [ -n "$C" ] && F=$(git show --name-only --format= "$C") && [ "$(printf '%s\n' "$F" | LC_ALL=C sort | tr '\n' ' ')" = ".planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md $P " ] && [ "$(/usr/bin/grep -cE -e 'firestarter_app/connect-cost-[0-9-]+/' "$Q")" = "0" ] && [ "$(/usr/bin/grep -cF -e "$P" "$Q")" -ge 2 ] && [ ! -e /workspaces/firestarter_app/connect-cost-2026-09-23-110029 ] && [ -z "$(git status --porcelain -- .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207.1-04-T1</automated>
    <fails_when>PASS-207.1-04-T1 is not printed: the committed blob hash differs (the git show of HEAD:path fails when the file is not committed), no D-16 commit, the commit touches paths other than the two, an app-tree citation remains, fewer than two new-path citations, the app directory still exists, the 206 directory is dirty, or the meta branch moved</fails_when>
  </verify>
  <acceptance_criteria>
    - The blob committed at `.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` hashes (sha256) to `7d683964d4a9abf78930c10ffc3f11661fc15b508bb4ad18b6a7c96cc50c6c9f`.
    - The commit `docs(207.1-04): D-16 keep the 206 connect-cost log as bench evidence` touches exactly the evidence file and `206-SESSION-COST.md`.
    - `206-SESSION-COST.md` has zero matches for the regex `firestarter_app/connect-cost-[0-9-]+/` and at least two occurrences of the new meta path.
    - `/workspaces/firestarter_app/connect-cost-2026-09-23-110029` no longer exists, and no `git clean` was run.
    - No VERIFICATION.md changed; nothing is pushed.
  </acceptance_criteria>
  <done>The 206 connect-cost measurement is tracked in meta beside the record that cites it, both citations resolve, and the app tree no longer carries the untracked copy.</done>
</task>

<task type="auto">
  <name>Task 2: D-15 the fw tests/ count matches the live tree; D-17 the kept datasheet is ignored per clone</name>
  <files>firestarter_fw/CLAUDE.md</files>
  <read_first>
    - /workspaces/firestarter_fw/CLAUDE.md (§ What CI runs, the two-test-trees paragraph about lines 32-37)
    - /workspaces/firestarter_app/.git/info/exclude (six stock comment lines, ending in a newline)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (Pitfalls 7 and 8)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md (correction 1: the exclude file holds only the stock lines)
  </read_first>
  <action>
    D-15, in /workspaces/firestarter_fw on branch `v1.41-verification-to-host`. Re-collect before editing: run `/usr/local/py-utils/bin/pytest tests/ --collect-only -q -p no:cacheprovider -o addopts=""` and read the `N tests collected` line (316 at planning time), then run `/usr/local/py-utils/bin/pytest tests/ -q -p no:cacheprovider -o addopts=""` and confirm `N passed`. Record both lines in the SUMMARY. In CLAUDE.md, in the sentence about the separate Python suite in `tests/` (about line 34), replace the stale count 320 with the re-collected N and the date 2026-09-22 with today's date in `YYYY-MM-DD` form, keeping the one-sentence form and the words around it (`measured at N collected tests on DATE.`). This count is the `tests/` Python tree that `build.yml` runs as `pytest tests/ -v`, not the PlatformIO `test/` tree. Confirm `git -C /workspaces/firestarter_fw diff --cached --name-only` is empty, stage `CLAUDE.md` by name and commit with subject `docs(207.1-04): D-15 correct the tests/ collected count`. Never commit on `beta`; do not push (a push to `beta` would cut a firmware pre-release, and `build.yml` ignores markdown-only commits anyway).

    D-17, per clone and never committed. Append the single anchored line `/datasheets/LST62832I.pdf` to `/workspaces/firestarter_app/.git/info/exclude` (the file ends in a newline, so a plain append keeps one pattern per line). Do not use `datasheets/` or any wildcard, which would silently hide future datasheets. Do not touch the PDF itself: Phase 197 deliberately left it untracked, and it must never be committed or deleted. Confirm `git -C /workspaces/firestarter_app check-ignore -v datasheets/LST62832I.pdf` names `.git/info/exclude` and that `git -C /workspaces/firestarter_app status --porcelain` no longer lists the datasheets directory.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_fw && O=$(/usr/local/py-utils/bin/pytest tests/ --collect-only -q -p no:cacheprovider -o addopts="" 2>&1) && N=$(printf '%s\n' "$O" | tail -1 | sed -nE 's/^([0-9]+) tests collected in .*/\1/p') && [ -n "$N" ] && /usr/bin/grep -qE -e "measured at $N collected tests on 20[0-9]{2}-[0-9]{2}-[0-9]{2}\." CLAUDE.md && [ "$(/usr/bin/grep -cE -e 'measured at [0-9]+ collected tests' CLAUDE.md)" -ge 1 ] && [ "$(/usr/bin/grep -cF -e 'measured at 320 collected tests' CLAUDE.md)" = "0" ] && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-04): D-15' HEAD) && [ -n "$C" ] && git merge-base --is-ancestor a55f2d8 "$C" && F=$(git show --name-only --format= "$C") && [ "$F" = "CLAUDE.md" ] && [ -z "$(git status --porcelain)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo "PASS-207.1-04-D15 $N"</automated>
    <fails_when>PASS-207.1-04-D15 is not printed: the collect line is missing, CLAUDE.md does not state the live count with a date, the old 320 sentence remains, no D-15 commit (or it touches more than CLAUDE.md, or is not on top of a55f2d8), the fw tree is dirty, or the fw branch moved</fails_when>
    <automated>cd /workspaces/firestarter_app && I=$(git check-ignore -v datasheets/LST62832I.pdf) && printf '%s\n' "$I" | /usr/bin/grep -qE -e '^\.git/info/exclude:[0-9]+:/datasheets/LST62832I\.pdf' && [ "$(/usr/bin/grep -cxF -e '/datasheets/LST62832I.pdf' .git/info/exclude)" -ge 1 ] && [ -z "$(git status --porcelain -- datasheets/)" ] && [ -z "$(git ls-files -- datasheets/LST62832I.pdf)" ] && [ "$(sha256sum datasheets/LST62832I.pdf | cut -d' ' -f1)" = "76a831007b622ad66efeb389aecab7fb4fcf4d5a614bd3bd564ed2a9c75737df" ] && [ -z "$(git status --porcelain)" ] && echo PASS-207.1-04-D17</automated>
    <fails_when>PASS-207.1-04-D17 is not printed: check-ignore exits 1 or names another source, the anchored line is absent, `git status` still lists the datasheets directory, the PDF became tracked, its hash changed (or the file is gone), or the app status shows any other entry</fails_when>
  </verify>
  <acceptance_criteria>
    - `/workspaces/firestarter_fw/CLAUDE.md` states `measured at N collected tests on YYYY-MM-DD.` where N equals the count `pytest tests/ --collect-only` reports in the task; the commit `docs(207.1-04): D-15 correct the tests/ collected count` touches only `CLAUDE.md` and sits on `v1.41-verification-to-host` on top of `a55f2d8`.
    - `git -C /workspaces/firestarter_app check-ignore -v datasheets/LST62832I.pdf` output starts `.git/info/exclude:` and ends with the anchored pattern.
    - `datasheets/LST62832I.pdf` is untracked, present, and hashes to `76a831007b622ad66efeb389aecab7fb4fcf4d5a614bd3bd564ed2a9c75737df`.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty (the connect-cost directory went in Task 1 and the PDF is now ignored).
    - Nothing is pushed from fw or app.
  </acceptance_criteria>
  <done>The fw guide's count matches what the tree collects today, and the app's status is clean without the kept datasheet being committed or deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app working tree -> meta repository | a bench measurement moves from an untracked file into a tracked, later-public record |
| fw branch -> fw publisher | a fw commit reaching `beta` would cut a firmware pre-release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-12 | Information disclosure | the connect-cost log published into meta | low | accept | The log holds the pinned port `/dev/ttyACM0`, the controller identity string and timings only; Task 1 reads it before copying to confirm no user path or secret. |
| T-207.1-13 | Repudiation (loss of bench evidence) | removal of the app-tree copy | medium | mitigate | Copy, `cmp`, commit, then prove the committed blob's sha256 before an explicit `rm -r` of the one directory; `git clean` is never used. |
| T-207.1-14 | Information hiding | the per-clone exclude line | low | mitigate | One anchored exact-path pattern, not a directory or wildcard; the file lives in `.git/` and is never committed; the PDF's hash is checked unchanged. |
| T-207.1-15 | Tampering (release) | the D-15 fw commit | high | mitigate | Committed on `v1.41-verification-to-host` with a branch assert; nothing is pushed; a `beta` push is the only way a fw commit publishes. |
</threat_model>

<verification>
- `git -C /workspaces status --porcelain -- .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/` is empty.
- `git -C /workspaces/firestarter_app status --porcelain` is empty.
- `git -C /workspaces/firestarter_fw status --porcelain` is empty and `git -C /workspaces/firestarter_fw log -1 --format=%s` starts `docs(207.1-04): D-15`.
- `git -C /workspaces diff --quiet 10c9b615 HEAD -- ':(glob).planning/phases/20[2-7]-*/*-VERIFICATION.md'` succeeds (no verification digest touched, D-19).
</verification>

<success_criteria>
- D-15, D-16 and D-17 are done; the 205 count item and the cross-phase clutter item are fixed.
- The bench evidence survived the move byte-for-byte.
- Nothing is pushed from any repository.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-04-SUMMARY.md` when done. Record the meta and fw commit shas, the collect and pass lines, and the check-ignore output.
</output>
