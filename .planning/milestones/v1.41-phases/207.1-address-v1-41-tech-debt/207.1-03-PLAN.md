---
phase: 207.1-address-v1-41-tech-debt
plan: 03
type: execute
wave: 3
depends_on: ["207.1-01", "207.1-02"]
files_modified:
  - firestarter_app/.github/scripts/update_version.py
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/firestarter/compare.py
  - .planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md
  - .planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
  - .planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md
  - .planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
autonomous: true
requirements: []

estimate:
  tokens: 27000
  raw_tokens: 54000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-12: `ruff format --check .` over the whole `firestarter_app` tree exits 0; `.github/scripts/update_version.py` changed in formatting only, proved by `ast.dump` equality against its `64da8f6` blob and by identical `--dry-run` output on both the beta and the stable path; it keeps mode 100755 and its `#!/usr/bin/env python3` shebang."
    - "D-03: `_run_step_untimed`'s `(SerialError, HardwareOperationError)` and `EpromOperationError` branches still hard-set `run_count=1`; a comment at both sites names the trade-off (the `--fast` dedup re-key, the re-key of filed reports a change would cause, the harm limited to already-ERROR or BAD reports) and states the mechanism precisely (this `1` is the cause only on the per-step path; the nine filed reports D-04 counted are the cycle-block shape)."
    - "D-09: the `if not offs:` branch in `CompareAccumulator.feed` is kept, with a comment naming it deliberate defensive redundancy against a caller that passes an `expected` longer than `actual`."
    - "D-03 and D-09 changed comments only: `ast.dump` of `chip_test.py` and `compare.py` equals that of their `64da8f6` blobs."
    - "The CI-equivalent app gate on Python 3.11 passes after every app change in this phase: ruff check and ruff format --check over `firestarter/ tests/`, the full pytest run with coverage at or above 70 %, at least 2373 passed (2365 baseline plus the 8 tests this phase adds), 36 snapshots passed, and `firestarter --help` exits 0."
    - "The two folded todos (D-05's `2026-09-20-read-abort-window-untested-at-its-boundary.md`, D-09's `2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md`) are in `.planning/todos/completed/` and no longer in `pending/`, committed in meta with named paths only."
    - "Nothing is pushed; app commits are on `firestarter_app` `v1.41-verification-to-host`, the todo commit on meta `v1.41-verification-to-host`."
  artifacts:
    - path: firestarter_app/.github/scripts/update_version.py
      provides: "the ruff-formatted publish-path version script (D-12)"
    - path: firestarter_app/firestarter/chip_test.py
      provides: "the D-03 trade-off comment at both except branches of `_run_step_untimed`"
      contains: "207.1 D-03"
    - path: firestarter_app/firestarter/compare.py
      provides: "the D-09 defensive-branch comment in `CompareAccumulator.feed`"
      contains: "207.1 D-09"
    - path: .planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md
      provides: "the folded todo closed by D-05"
    - path: .planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
      provides: "the folded todo closed by D-09"
  key_links:
    - from: firestarter_app/firestarter/chip_test.py
      to: firestarter_app/firestarter/diagnostic_report.py
      via: "`repeat_policy_tag` keys on `run_count == 1` for `_REPEAT_POLICY_OPS` steps and `dedup_fingerprint` appends the tag when non-empty -- the reason the D-03 comment exists"
      pattern: "repeat_policy_tag"
    - from: firestarter_app/.github/workflows/beta-release.yml
      to: firestarter_app/.github/scripts/update_version.py
      via: "the publish path runs the script; its output must be identical before and after the format"
      pattern: "update_version.py"
---

<objective>
Finish the host side of the phase and prove it whole: D-12 (format the publish-path version script,
format-only), D-03 (say at the two kept `run_count=1` branches why they stay, accurately) and D-09
(say at the kept defensive branch why it stays). Then run the CI-equivalent gate once over every app
change this phase made, and close the two folded todos that D-05 and D-09 resolve.

Purpose: D-12 makes `ruff format --check .` clean over the whole tree at zero behaviour risk. D-03 and
D-09 are decisions to keep code as it is; the comment is what stops the next reader from "fixing" it.
D-03's comment must be precise, because research found the operator's reversal note re-keys none of
the nine filed reports (207.1-RESEARCH.md § D-03, finding 1).

Output: one formatted script, two comment blocks and one one-line pointer, one green full gate, two
todo moves.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md
@/workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-REVIEW-FIX.md

<interfaces>
Extracted from the live tree at firestarter_app 64da8f6 (line numbers drift; locate by symbol).

firestarter/chip_test.py
  _MULTI_RUN_OPS = frozenset({OP_WRITE, OP_WRITE_PARTIAL, OP_ERASE, OP_VERIFY})   (about line 865)
  _REPEAT_POLICY_OPS = _MULTI_RUN_OPS | {OP_READ}; REPEAT_POLICY_DEGRADED_TAG = "runs=1"   (about lines 1043-1055, with the BY-DESIGN comment block above)
  def repeat_policy_tag(results) -> str: returns the tag when a _REPEAT_POLICY_OPS step has run_count == 1   (about line 1058)
  _aggregate_cycle_results: `if len(results) == 1: return results[0]`   (about line 1361)
  _run_cycle_block: calls _run_step(..., runs=1, ...) per cycle; `if result.error_code is not None: hardware_refused = True`; `if hardware_refused: break`   (about lines 1618-1653)
  run_plan: _run_step(..., runs=runs) on the per-step path   (about lines 1837-1845)
  def _run_step_untimed(   (about line 2543); except (SerialError, HardwareOperationError) at about 2612 and except EpromOperationError at about 2620, both building StepResult(..., run_count=1)
  style analog: the comment directly above `run_count=runs,` at about lines 3548-3553

firestarter/compare.py  CompareAccumulator.feed(self, address, expected, actual)   (about line 216)
  docstring: `expected` and `actual` are assumed the same length
  Tier 2: `if expected == actual:` ... return; Tier 3 comment; offs = [o for o in range(chunk_len) if expected[o] != actual[o]]; `if not offs:` (about line 252); then `self._first_offset = address + offs[0] - self._addr_base`
</interfaces>
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate.

## Artifacts this phase produces (this plan)

- No new source symbol, CLI flag or dataclass field.
- New file paths (moved, not authored): `.planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md`, `.planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md`.

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): D-12 end to end -- the publish-path version script is ruff-formatted and provably behaves identically</name>
  <precondition>`git -C /workspaces/firestarter_app diff --quiet 64da8f6 HEAD -- firestarter/chip_test.py firestarter/compare.py .github/scripts/update_version.py` succeeds (no earlier 207.1 commit touched these three files, so `64da8f6` holds their pre-phase content).</precondition>
  <files>firestarter_app/.github/scripts/update_version.py</files>
  <read_first>
    - /workspaces/firestarter_app/.github/scripts/update_version.py (whole file, 212 lines)
    - /workspaces/firestarter_app/.github/workflows/beta-release.yml (the step that runs the script, about line 64)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ D-12: the exact diff ruff produces)
  </read_first>
  <action>
    Per D-12. Run `.venv311/bin/ruff format .github/scripts/update_version.py` from /workspaces/firestarter_app. The expected change is formatting only (207.1-RESEARCH.md § D-12): five single-to-double quote swaps, one f-string quote swap that is AST-equivalent, two call or tuple explosions onto one argument per line, and one blank line after `import sys` in `__main__`. Make no logic change and no hand edit; if ruff's diff contains anything beyond those classes, stop and report it. Save the pre-format blob with `git show 64da8f6:.github/scripts/update_version.py` into a `mktemp -d` scratch directory, then prove the change is format-only three ways: `ast.dump(ast.parse(...))` of the old and new text are equal; `GITHUB_REF=refs/heads/beta python3 <script> --dry-run` prints the same line for the old copy and the new file (both run with cwd /workspaces/firestarter_app, because the script reads `firestarter/__init__.py` and `git tag` relative to cwd); and the same equality holds for `GITHUB_REF=refs/heads/main`. `--dry-run` writes nothing on either path. Confirm the file keeps mode 100755 in the index and its first line `#!/usr/bin/env python3`, and that `.venv311/bin/ruff check` on it passes. Commit inside /workspaces/firestarter_app with subject `style(207.1-03): D-12 ruff format the publish-path version script`. Do not push: a push to `beta` would run this very script and publish.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && X=$(mktemp -d) && git show 64da8f6:.github/scripts/update_version.py > "$X/old.py" && .venv311/bin/python -c "import ast,sys; a=ast.dump(ast.parse(open(sys.argv[1]).read())); b=ast.dump(ast.parse(open(sys.argv[2]).read())); sys.exit(0 if a==b else 1)" "$X/old.py" .github/scripts/update_version.py && OB=$(GITHUB_REF=refs/heads/beta python3 "$X/old.py" --dry-run) && NB=$(GITHUB_REF=refs/heads/beta python3 .github/scripts/update_version.py --dry-run) && OM=$(GITHUB_REF=refs/heads/main python3 "$X/old.py" --dry-run) && NM=$(GITHUB_REF=refs/heads/main python3 .github/scripts/update_version.py --dry-run) && [ -n "$NB" ] && [ "$OB" = "$NB" ] && [ -n "$NM" ] && [ "$OM" = "$NM" ] && .venv311/bin/ruff format --check . && .venv311/bin/ruff check .github/scripts/update_version.py && M=$(git ls-files -s .github/scripts/update_version.py) && [ "$(printf '%s' "$M" | cut -c1-6)" = "100755" ] && [ "$(sed -n 1p .github/scripts/update_version.py)" = '#!/usr/bin/env python3' ] && ! git diff --quiet 64da8f6 HEAD -- .github/scripts/update_version.py && S=$(git log --format=%s 64da8f6..HEAD) && printf '%s\n' "$S" | /usr/bin/grep -qF -e '(207.1-03): D-12' && [ -z "$(git status --porcelain --untracked-files=no)" ] && echo "PASS-207.1-03-T1 $NB / $NM"</automated>
    <fails_when>PASS-207.1-03-T1 is not printed: the AST comparison exits 1 (a logic change), a dry-run line differs between the old and new script or is empty, `ruff format --check .` still reports a file that would be reformatted, a ruff check finding, the mode is not 100755, the shebang changed, the script is unchanged since 64da8f6, no D-12 commit, or tracked changes are left</fails_when>
  </verify>
  <acceptance_criteria>
    - `ast.dump` of `.github/scripts/update_version.py` equals that of its `64da8f6` blob.
    - `--dry-run` output is identical between the `64da8f6` copy and the formatted file for `GITHUB_REF=refs/heads/beta` and for `GITHUB_REF=refs/heads/main`, and neither is empty.
    - `.venv311/bin/ruff format --check .` run from /workspaces/firestarter_app exits 0 (the whole tree, not only the CI scope).
    - `git ls-files -s` shows mode `100755`; line 1 is `#!/usr/bin/env python3`.
    - A commit whose subject starts `style(207.1-03): D-12` exists and touches only `.github/scripts/update_version.py`; nothing is pushed.
  </acceptance_criteria>
  <done>The version script on the publish path is formatted, behaves identically on both channels, and the whole app tree passes `ruff format --check .`.</done>
</task>

<task type="auto">
  <name>Task 2: D-03 and D-09 -- the two kept-by-decision branches say why they stay (comment only)</name>
  <files>firestarter_app/firestarter/chip_test.py, firestarter_app/firestarter/compare.py</files>
  <reversibility rating="costly">D-03 keeps a fingerprint pre-image input that filed public reports already depend on; reversing it later is a declared re-key that needs a Phase 177-style re-key record (already decided by the operator in 207.1-CONTEXT.md, so no checkpoint).</reversibility>
  <read_first>
    - /workspaces/firestarter_app/firestarter/chip_test.py (lines 1040-1075: `_REPEAT_POLICY_OPS` block and `repeat_policy_tag`; lines 1355-1395: `_aggregate_cycle_results`; lines 1610-1660: `_run_cycle_block`; lines 1830-1850: `run_plan`'s per-step call; lines 2543-2640: `_run_step_untimed` and its except branches; lines 3540-3556: the style analog)
    - /workspaces/firestarter_app/firestarter/diagnostic_report.py (lines 300-320: where the tag enters `dedup_fingerprint`)
    - /workspaces/firestarter_app/firestarter/compare.py (lines 214-260: `CompareAccumulator.feed`)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ D-03 and § D-04: the two paths and the 7-issue / 9-report count; § D-09)
    - /workspaces/.planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
  </read_first>
  <action>
    Per D-03 and D-09. Both are keep decisions: change comments only, no code, no test.

    D-03 (chip_test.py). In `_run_step_untimed`, inside the `except (SerialError, HardwareOperationError) as exc:` branch, place a comment block directly above its `run_count=1,` argument, at that argument's indentation, in the style of the analog at about lines 3548-3553 (backticked identifiers, `--` dashes, about 75-column wrap, decision ids inline). It says: this stays at 1 by decision (206-REVIEW-FIX WR-02 observation; 207.1 D-03). `repeat_policy_tag` keys on `run_count == 1` for every `_REPEAT_POLICY_OPS` step, so a default-policy run whose per-step-path step raises here stamps the degraded `runs=1` tag into `dedup_fingerprint` and is re-keyed into the `--fast` dedup group. Changing it to the nominal `runs` would re-key reports already filed, and the harm is limited to dedup grouping of reports that are already ERROR or BAD, so the trade is accepted. Then the mechanism, stated precisely: this `1` is the cause only on the per-step path (`run_plan` calling `_run_step(..., runs=runs)`, which today means the read step); on the cycle-block path `_run_cycle_block` already calls `_run_step(..., runs=1)`, so this `1` equals the `runs` passed, and the tag comes instead from the `hardware_refused` break after one cycle and `_aggregate_cycle_results` returning that single result. Every filed report 207.1 D-04 counted (7 issues, 9 reports) is the cycle-block shape, so reverting this branch alone would re-key none of them. In the `except EpromOperationError as exc:` branch, place a one-line comment directly above its `run_count=1,` saying it is the same trade-off as the branch above and is kept at 1 (207.1 D-03). Keep the vocabulary consistent with the block above `_REPEAT_POLICY_OPS`. Do not touch the long comment above the preceding `except` clause, any code, or any test.

    D-09 (compare.py). In `CompareAccumulator.feed`, place a comment between the `offs = [...]` line and `if not offs:`, in the method's `# Tier N:` prose style at 8-space indent. It says: this branch is defensive, not dead (202-REVIEW IN-01, 207.1 D-09); with the equal-length contract in the docstring held, Tier 2 has already returned for every chunk with no differing offset, so it never runs; it is reachable only when a caller passes an `expected` longer than `actual` and equal over `actual`'s length (Tier 2's `expected == actual` is then False, yet no offset differs), while a shorter `expected` raises IndexError in the comprehension first; it is kept so that contract violation closes the open range and returns instead of indexing `offs[0]` on an empty list below. Do not change the branch.

    Run `.venv311/bin/ruff format` and `.venv311/bin/ruff check` on both files. Commit inside /workspaces/firestarter_app with subject `docs(207.1-03): D-03 and D-09 say why two kept branches stay`. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && X=$(mktemp -d) && git show 64da8f6:firestarter/chip_test.py > "$X/chip_test.py" && git show 64da8f6:firestarter/compare.py > "$X/compare.py" && .venv311/bin/python -c "import ast,sys; d=lambda p: ast.dump(ast.parse(open(p).read())); ok=d(sys.argv[1])==d('firestarter/chip_test.py') and d(sys.argv[2])==d('firestarter/compare.py'); sys.exit(0 if ok else 1)" "$X/chip_test.py" "$X/compare.py" && [ "$(/usr/bin/grep -cF -e '207.1 D-03' firestarter/chip_test.py)" -ge 2 ] && /usr/bin/grep -qF -e '7 issues, 9 reports' firestarter/chip_test.py && /usr/bin/grep -qF -e '207.1 D-09' firestarter/compare.py && .venv311/bin/ruff check firestarter/chip_test.py firestarter/compare.py && .venv311/bin/ruff format --check firestarter/chip_test.py firestarter/compare.py && .venv311/bin/python -m pytest tests/test_chip_test.py tests/test_chip_test_blank_check_order.py tests/test_chip_test_cycle.py tests/test_chip_test_sdp_leg.py tests/test_chip_test_timing.py tests/test_chip_test_uv_slot_write.py tests/test_compare.py tests/test_diagnostic_report.py -o addopts="" -p no:cacheprovider -q 2>&1 | tail -1 | /usr/bin/grep -qE -e '^[0-9]+ passed in ' && S=$(git log --format=%s 64da8f6..HEAD) && printf '%s\n' "$S" | /usr/bin/grep -qF -e '(207.1-03): D-03 and D-09' && echo PASS-207.1-03-T2</automated>
    <fails_when>PASS-207.1-03-T2 is not printed: the AST comparison exits 1 (something other than a comment changed), fewer than two D-03 comment sites, the D-04 count missing from the comment, no D-09 comment, a ruff finding, the chip_test/compare/diagnostic_report tests do not end "N passed in", or no commit</fails_when>
  </verify>
  <acceptance_criteria>
    - `ast.dump` of `firestarter/chip_test.py` and `firestarter/compare.py` equals that of their `64da8f6` blobs (comments only).
    - `firestarter/chip_test.py` contains `207.1 D-03` at least twice (both except branches) and the text `7 issues, 9 reports`; both branches still carry `run_count=1,`.
    - `firestarter/compare.py` contains `207.1 D-09` between the `offs = ` line and `if not offs:`.
    - The eight chip_test, compare and diagnostic_report test files pass; ruff check and format --check are clean on both files.
    - A commit whose subject starts `docs(207.1-03): D-03 and D-09` exists; nothing is pushed.
  </acceptance_criteria>
  <done>Both keep decisions are explained at the code they protect, with the D-03 mechanism stated as research measured it, and nothing but comments changed.</done>
</task>

<task type="auto">
  <name>Task 3: the CI-equivalent app gate over the whole phase, then close the two folded todos (D-05, D-09)</name>
  <files>.planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md, .planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md, .planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md, .planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md</files>
  <read_first>
    - /workspaces/firestarter_app/CLAUDE.md (§ What CI runs)
    - /workspaces/.planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md
    - /workspaces/.planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ Commit Mechanics: the todo verb and its dry-run)
  </read_first>
  <action>
    First the gate, which must pass before any record claims the fixes landed. From /workspaces/firestarter_app run exactly what `firestarter_app/CLAUDE.md` § What CI runs lists, on the Python 3.11 venv: `.venv311/bin/ruff check firestarter/ tests/`, `.venv311/bin/ruff format --check firestarter/ tests/`, the full `pytest tests/ --cov=firestarter --cov-fail-under=70` (with `-o addopts=""` so the count line is visible), and `.venv311/bin/firestarter --help`. Also confirm the eight tests this phase adds are all collected (the five from 207.1-01, the three from 207.1-02; ids in those plans' "Artifacts" sections). Record the passed count, the coverage percentage and the snapshot line in the SUMMARY. If anything fails, stop: do not move the todos.

    Then the two folded todos, per D-05 and D-09 (207.1-CONTEXT.md § Folded Todos). From /workspaces, confirm `git -C /workspaces diff --cached --name-only` is empty (the meta tree carries unrelated unstaged modifications that must never be committed here; if anything is already staged, stop and report it). Run `node .claude/gsd-core/bin/gsd-tools.cjs todo complete 2026-09-20-read-abort-window-untested-at-its-boundary.md` and `node .claude/gsd-core/bin/gsd-tools.cjs todo complete 2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md`; the verb moves each file from `.planning/todos/pending/` to `.planning/todos/completed/` and sets `completed:` and `status: completed` in its frontmatter (it does not commit). Stage exactly the four paths by name (`git -C /workspaces add -- ` followed by the two pending paths and the two completed paths), confirm `git diff --cached --name-only` lists those four and nothing else, and commit with `git -C /workspaces commit -m "docs(207.1-03): close 2 resolved todos (D-05, D-09)"`. Never use `git add -A`, `git add .` or `commit -a`. Confirm `git -C /workspaces rev-parse --abbrev-ref HEAD` is still `v1.41-verification-to-host`. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/ && .venv311/bin/ruff format --check firestarter/ tests/ && .venv311/bin/firestarter --help > /dev/null && L=$(mktemp) && if timeout 580 .venv311/bin/python -m pytest tests/ -o addopts="" -p no:cacheprovider -q --cov=firestarter --cov-fail-under=70 > "$L" 2>&1; then tail -2 "$L"; else tail -8 "$L"; exit 1; fi && /usr/bin/grep -qxF -e '36 snapshots passed.' "$L" && /usr/bin/grep -qF -e 'Required test coverage of 70% reached' "$L" && N=$(tail -1 "$L" | sed -nE 's/^([0-9]+) passed in .*/\1/p') && [ -n "$N" ] && [ "$N" -ge 2373 ] && C=$(.venv311/bin/python -m pytest --collect-only -q -o addopts="" -p no:cacheprovider tests/test_write_blank_guard_pinning.py tests/test_write_blank_guard.py tests/test_write_verify.py tests/test_serial_comm.py tests/test_eprom_operations.py 2>&1) && for t in test_write_eprom_negative_misaligned_start_address_refuses_as_negative_first test_cli_write_negative_misaligned_address_on_a_page_write_part_gets_the_negative_message test_incomplete_refusal_text_is_exactly_one_line_with_the_coverage test_incomplete_refusal_text_carries_no_remedy_clause test_write_verify_arm4b_landed_but_sdp_skip_unacknowledged_exits_1 test_setup_command_asserts_the_link_is_connected_before_sending test_timeout_just_inside_the_acceptance_window_is_the_intended_abort test_timeout_exactly_at_the_acceptance_window_is_the_intended_abort; do printf '%s\n' "$C" | /usr/bin/grep -qF -e "::$t" || { echo "NOT COLLECTED $t"; exit 1; }; done && echo "PASS-207.1-APP-GATE $N"</automated>
    <fails_when>PASS-207.1-APP-GATE is not printed: a ruff finding, `firestarter --help` exits non-zero, pytest exits non-zero (the tail shows failures or the coverage floor missed), the "36 snapshots passed." or coverage line is absent, fewer than 2373 passed, or a "NOT COLLECTED" line names a missing new test</fails_when>
    <automated>cd /workspaces && for f in 2026-09-20-read-abort-window-untested-at-its-boundary.md 2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md; do [ ! -e ".planning/todos/pending/$f" ] || { echo "STILL PENDING $f"; exit 1; }; git ls-files --error-unmatch ".planning/todos/completed/$f" > /dev/null || { echo "NOT TRACKED $f"; exit 1; }; /usr/bin/grep -qxF -e 'status: completed' ".planning/todos/completed/$f" || { echo "NO STATUS $f"; exit 1; }; done && C=$(git log -1 --format=%H --fixed-strings --grep='docs(207.1-03): close 2 resolved todos (D-05, D-09)' HEAD) && [ -n "$C" ] && F=$(git show --no-renames --name-only --format= "$C") && [ "$(printf '%s\n' "$F" | LC_ALL=C sort | tr '\n' ' ')" = ".planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md .planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md .planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md .planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md " ] && [ -z "$(git status --porcelain -- .planning/todos/)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207.1-03-TODOS</automated>
    <fails_when>PASS-207.1-03-TODOS is not printed: a "STILL PENDING", "NOT TRACKED" or "NO STATUS" line, no commit with the todo subject, the commit touches any path other than the four todo paths, the todos directory is dirty, or the meta branch moved</fails_when>
  </verify>
  <acceptance_criteria>
    - On `.venv311` (Python 3.11): ruff check and ruff format --check over `firestarter/ tests/` are clean, `firestarter --help` exits 0, and the full pytest run exits 0 with `Required test coverage of 70% reached`, `36 snapshots passed.` and at least 2373 passed.
    - All eight new test ids from 207.1-01 and 207.1-02 are collected.
    - Both folded todos exist under `.planning/todos/completed/` with `status: completed`, and neither exists under `pending/`.
    - The meta commit `docs(207.1-03): close 2 resolved todos (D-05, D-09)` touches exactly the four todo paths (with `--no-renames`); nothing else in meta was staged or committed.
    - Both meta and app remain on `v1.41-verification-to-host`; nothing is pushed.
  </acceptance_criteria>
  <done>The whole phase's app work passes the CI-equivalent gate on Python 3.11, and the two todos it resolved are closed in the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> release pipeline | `beta-release.yml` and `release.yml` execute `.github/scripts/update_version.py` on push |
| host fingerprint -> public issue tracker | `dedup_fingerprint` groups filed `dev test` reports on `henols/firestarter`; its pre-image is external state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-09 | Tampering (release pipeline) | `.github/scripts/update_version.py` | high | mitigate | Format-only, proved by AST equality against `64da8f6` and identical `--dry-run` output on the beta and the stable path; mode 100755 and the shebang are checked; nothing is pushed, so no workflow runs the changed file in this phase. |
| T-207.1-10 | Tampering (a later wrong fix re-keys filed reports) | the D-03 comment in `_run_step_untimed` | medium | mitigate | Comment-only, proved by AST equality; the comment states both tag paths and the D-04 count, so a reader who wants to reverse D-03 sees that the two branches alone re-key none of the filed reports and that the operator's reversal is a declared re-key. |
| T-207.1-11 | Repudiation (a record claims a fix that did not land) | the todo closures | low | mitigate | The todos move only after the full CI-equivalent gate is green and all eight new tests are collected; the commit touches exactly the four todo paths. |
</threat_model>

<verification>
- Task 3's gate leg prints `PASS-207.1-APP-GATE N` with N at least 2373.
- `git -C /workspaces/firestarter_app status --porcelain --untracked-files=no` is empty.
- `git -C /workspaces/firestarter_app log --format=%s 64da8f6..HEAD` lists the D-08, D-07, D-06, D-11, D-05, D-12 and D-03/D-09 commits (plus any RED test commits).
</verification>

<success_criteria>
- The cross-phase `ruff format --check .` item (D-12), the 206 run_count item (D-03) and 202 IN-01 (D-09) are resolved; every app change in the phase is proven by the CI-equivalent gate on Python 3.11.
- The two folded todos are closed in meta with named-path staging.
- Nothing is pushed from any repository.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-03-SUMMARY.md` when done. Record each commit sha, the gate's passed count, coverage and snapshot line, and the two todo moves.
</output>
