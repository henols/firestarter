---
phase: 207.1-address-v1-41-tech-debt
plan: 06
type: execute
wave: 4
depends_on: ["207.1-01", "207.1-02", "207.1-03"]
files_modified:
  - .planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md
  - .planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md
  - .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md
autonomous: false
requirements: []

estimate:
  tokens: 10000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "D-02: `202-SECURITY.md`, `204-SECURITY.md` and `205-SECURITY.md` exist, each produced by `/gsd-secure-phase` run in the main session (not by an executor), each with frontmatter `status: verified`, `threats_open: 0` and `asvs_level: 1`, each committed by the workflow's own `docs(phase-NNN): add/update security threat verification` commit."
    - "D-02: `205-SECURITY.md`'s Accepted Risks Log lists the two high-severity accepted threats T-205-10 and T-205-16 (post-205 host against pre-205 firmware)."
    - "D-02: an `OPEN_THREATS`, `ESCALATE` or `SECURITY BLOCKED` result on any of the three phases stops Phase 207.1 for an operator call; it is never absorbed, never accepted on the operator's behalf, and never worked around by writing a SECURITY.md by hand."
    - "The secure-phase runs happen after every app change in this phase has landed (207.1-01, -02, -03), so each audit reads the tree that will ship."
    - "Meta stays on branch `v1.41-verification-to-host` after all three workflow commits; nothing is pushed."
  artifacts:
    - path: .planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md
      provides: "the phase 202 threat verification record (D-02)"
      contains: "threats_open: 0"
    - path: .planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md
      provides: "the phase 204 threat verification record (D-02)"
      contains: "threats_open: 0"
    - path: .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md
      provides: "the phase 205 threat verification record, with T-205-10 and T-205-16 accepted (D-02)"
      contains: "T-205-16"
  key_links:
    - from: .claude/gsd-core/workflows/secure-phase.md
      to: .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md
      via: "the workflow builds the register from each PLAN's `<threat_model>` block and writes and commits `{NNN}-SECURITY.md` itself"
      pattern: "add/update security threat verification"
---

<objective>
Produce the three missing SECURITY.md files the audit named, for phases 202, 204 and 205 (D-02), by
running `/gsd-secure-phase` for each, and prove the results.

Purpose: `workflow.security_enforcement` is on and the verify:post security step hook is active, so
these records are owed, not optional. D-02 decides they are produced, not accepted.

Why this plan has checkpoints and no tracer: `/gsd-secure-phase` is an orchestrator-level workflow.
It can call `AskUserQuestion` (when threats are open) and spawn `gsd-security-auditor` through
`Agent`; `gsd-executor` has neither tool (207.1-RESEARCH.md § D-02). So the operator, or the main
session, runs the three workflows at `checkpoint:human-action` steps, and an executor then verifies
what they wrote. The tracer-first shape does not apply to steps an executor cannot perform; the
thinnest end-to-end slice is Task 1, the single 202 run, which the operator confirms before Task 2
expands to 204 and 205.

Output: three SECURITY.md files, committed by the workflow; one verification record in the SUMMARY.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SECURITY.md
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate.

## Artifacts this phase produces (this plan)

- New file paths, written by the workflow: `.planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md`, `.planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md`, `.planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md`.
- No source symbol, CLI flag or dataclass field.

## Stop condition (D-02)

If any `/gsd-secure-phase` run ends in `## OPEN_THREATS`, `## ESCALATE` or `PHASE N SECURITY BLOCKED`,
Phase 207.1 STOPS. Report the phase number, the verdict and the open threat ids to the operator and
wait. Do not continue to the next run, do not write or edit a SECURITY.md by hand, and do not choose
an "accept" option on the operator's behalf. The operator's own choice inside the workflow's prompt
is the only way an open threat becomes accepted.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: D-02 slice one -- the operator runs /gsd-secure-phase 202 in the main session</name>
  <precondition>`node /workspaces/.claude/gsd-core/bin/gsd-tools.cjs loop render-hooks verify:post --raw` (run from /workspaces) prints a step hook whose ref names `secure-phase`, and none of `202-SECURITY.md`, `204-SECURITY.md`, `205-SECURITY.md` exists yet.</precondition>
  <read_first>
    - /workspaces/.claude/gsd-core/workflows/secure-phase.md (the short-circuit rule, the OPEN_THREATS / ESCALATE branches and the commit step)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SECURITY.md (the precedent output: frontmatter keys and sections)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ D-02: register inputs 202 = 22 rows across 5 PLANs)
  </read_first>
  <action>Run `/gsd-secure-phase 202` in the main Claude Code session, then report its verdict. Per D-02. Claude has already confirmed the hook is active and that phase 202's five PLAN.md files carry `<threat_model>` blocks; the one thing Claude cannot do from an executor is run an orchestrator workflow that may ask questions or spawn an auditor.</action>
  <instructions>
    Before returning this checkpoint, the executor runs these read-only checks from /workspaces and reports them: the precondition above; `ls .planning/phases/202-one-comparison-engine-on-the-host/*-PLAN.md | wc -l` (expect 5) and that each contains `<threat_model>`; `git rev-parse --abbrev-ref HEAD` is `v1.41-verification-to-host`; `git diff --cached --name-only` is empty.

    The operator then, in the main session (not under `--auto` or `--chain`, which auto-approve gates):
    1. Types `/gsd-secure-phase 202` and lets the workflow run. At ASVS L1 with every threat closed it short-circuits and writes the file without an auditor, as 206 did; otherwise it asks or spawns `gsd-security-auditor`.
    2. If the workflow ends in OPEN_THREATS, ESCALATE or SECURITY BLOCKED, STOPS here and follows the plan's Stop condition. Any accept decision is the operator's own, made in the workflow's prompt.
    3. After a secured result, checks `git -C /workspaces rev-parse --abbrev-ref HEAD` still prints `v1.41-verification-to-host` (the workflow commits through `gsd-tools query commit`, which has switched branches in this repo before).
  </instructions>
  <verification>`202-SECURITY.md` exists with `status: verified` and `threats_open: 0`; `git -C /workspaces log -1 --format=%s -- .planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md` prints `docs(phase-202): add/update security threat verification`; the meta branch is unchanged.</verification>
  <resume-signal>Type `secured 202` (and paste the workflow's final verdict line), or `blocked 202: <verdict and threat ids>` to stop the phase.</resume-signal>
  <acceptance_criteria>
    - The operator's reply is `secured 202` with the verdict line, or a `blocked 202:` reply that stops the phase.
    - On `secured 202`: `.planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md` exists and is committed by the workflow; meta is on `v1.41-verification-to-host`.
  </acceptance_criteria>
  <done>Phase 202 has a verified SECURITY.md written and committed by the workflow, or the phase is stopped for the operator with the verdict recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: D-02 expansion -- the operator runs /gsd-secure-phase 204, then /gsd-secure-phase 205</name>
  <read_first>
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ D-02: 204 = 30 rows across 5 PLANs; 205 = 45 rows across 8 PLANs, including T-205-10 in 205-03 and T-205-16 in 205-04 as high/accept)
    - /workspaces/.planning/phases/205-the-pre-flights-leave-the-firmware/205-03-PLAN.md (the `<threat_model>` row T-205-10)
    - /workspaces/.planning/phases/205-the-pre-flights-leave-the-firmware/205-04-PLAN.md (the `<threat_model>` row T-205-16)
  </read_first>
  <action>Run `/gsd-secure-phase 204`, and after it secures, `/gsd-secure-phase 205`, in the main session, then report both verdicts. Per D-02. Claude has already confirmed 202 is secured (Task 1) and that the 204 and 205 PLAN files carry `<threat_model>` blocks.</action>
  <instructions>
    Before returning this checkpoint, the executor runs read-only checks from /workspaces and reports them: `202-SECURITY.md` exists with `status: verified`; `ls .planning/phases/204-the-command-surfaces-leave-the-firmware/*-PLAN.md | wc -l` (expect 5) and `ls .planning/phases/205-the-pre-flights-leave-the-firmware/*-PLAN.md | wc -l` (expect 8), each containing `<threat_model>`; the meta branch is `v1.41-verification-to-host`; the index is empty.

    The operator then, in the main session, not under `--auto` or `--chain`:
    1. Runs `/gsd-secure-phase 204`. On OPEN_THREATS, ESCALATE or SECURITY BLOCKED, STOPS and follows the Stop condition; 205 is not run.
    2. Checks the meta branch is still `v1.41-verification-to-host`.
    3. Runs `/gsd-secure-phase 205`. Its register carries two high-severity ACCEPTED threats, T-205-10 and T-205-16, both "post-205 host against pre-205 firmware", accepted under 205's own D-04 and documented by REL-04. They count as closed only if the written 205-SECURITY.md lists them in its Accepted Risks Log. If the workflow presents them as open, that is the operator's call inside the workflow's prompt, per D-02; on any other OPEN_THREATS, ESCALATE or SECURITY BLOCKED result, STOPS.
    4. Checks the meta branch again.
  </instructions>
  <verification>`204-SECURITY.md` and `205-SECURITY.md` exist with `status: verified` and `threats_open: 0`; each is committed by a `docs(phase-NNN): add/update security threat verification` commit; 205-SECURITY.md's Accepted Risks Log names T-205-10 and T-205-16; the meta branch is unchanged.</verification>
  <resume-signal>Type `secured 204 205` (and paste both final verdict lines), or `blocked NNN: <verdict and threat ids>` to stop the phase.</resume-signal>
  <acceptance_criteria>
    - The operator's reply is `secured 204 205` with both verdict lines, or a `blocked` reply that stops the phase.
    - On `secured 204 205`: both files exist and are committed by the workflow; meta is on `v1.41-verification-to-host`.
  </acceptance_criteria>
  <done>Phases 204 and 205 have verified SECURITY.md files written and committed by the workflow, or the phase is stopped for the operator with the verdict recorded.</done>
</task>

<task type="auto">
  <name>Task 3: verify all three D-02 records and write the verdicts into the SUMMARY</name>
  <files>.planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md, .planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md, .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md</files>
  <read_first>
    - /workspaces/.planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md
    - /workspaces/.planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md
    - /workspaces/.planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md
  </read_first>
  <action>Per D-02. Read-only verification of what the workflows wrote; modify nothing. Run the verify block from /workspaces. Then write into 207.1-06-SUMMARY.md, for each of 202, 204 and 205: the file path, the commit sha that added it (from `git log -1 --format=%h -- <path>`), the frontmatter `status`, `threats_open` and `asvs_level`, whether the run short-circuited at L1 or spawned an auditor (from the file's own register-origin and audit-trail sections), and the operator's verdict line from the resume signal. For 205, quote the Accepted Risks Log rows for T-205-10 and T-205-16. If any check fails, do not repair the file: stop and report under the plan's Stop condition.</action>
  <verify>
    <automated>cd /workspaces && for p in 202-one-comparison-engine-on-the-host:202 204-the-command-surfaces-leave-the-firmware:204 205-the-pre-flights-leave-the-firmware:205; do d=${p%%:*}; n=${p##*:}; f=.planning/phases/$d/$n-SECURITY.md; [ -f "$f" ] || { echo "MISSING $f"; exit 1; }; /usr/bin/grep -qxF -e 'status: verified' "$f" || { echo "NOT VERIFIED $n"; exit 1; }; /usr/bin/grep -qxF -e 'threats_open: 0' "$f" || { echo "OPEN THREATS $n"; exit 1; }; /usr/bin/grep -qxF -e 'asvs_level: 1' "$f" || { echo "ASVS $n"; exit 1; }; c=$(git log -1 --format=%H --fixed-strings --grep="docs(phase-$n): add/update security threat verification" -- "$f"); [ -n "$c" ] || { echo "NOT COMMITTED BY WORKFLOW $n"; exit 1; }; [ -z "$(git status --porcelain -- "$f")" ] || { echo "DIRTY $n"; exit 1; }; done; A=$(sed -n '/^## Accepted Risks Log/,/^## /p' .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md); for t in T-205-10 T-205-16; do printf '%s\n' "$A" | /usr/bin/grep -qF -e "$t" || { echo "NOT IN ACCEPTED RISKS LOG $t"; exit 1; }; done; [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] || { echo "BRANCH MOVED"; exit 1; }; echo PASS-207.1-06-D02</automated>
    <fails_when>PASS-207.1-06-D02 is not printed: a MISSING, NOT VERIFIED, OPEN THREATS, ASVS, NOT COMMITTED BY WORKFLOW, DIRTY, NOT IN ACCEPTED RISKS LOG or BRANCH MOVED line appears</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the three files has the exact frontmatter lines `status: verified`, `threats_open: 0` and `asvs_level: 1`.
    - Each file's latest commit is the workflow's `docs(phase-NNN): add/update security threat verification`, and each is clean in the working tree.
    - `205-SECURITY.md`'s `## Accepted Risks Log` section names T-205-10 and T-205-16.
    - Meta HEAD is on `v1.41-verification-to-host`; the SUMMARY records sha, frontmatter values, audit route and operator verdict for all three.
  </acceptance_criteria>
  <done>All three owed SECURITY.md files are proven present, verified, workflow-committed and on the milestone branch, with 205's two accepted high threats in its accepted-risks log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| security workflow -> phase record | `/gsd-secure-phase` decides whether a phase's threats are closed and writes the record the audit relies on |
| operator decision -> accepted risk | only the operator may turn an open threat into an accepted one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-19 | Repudiation (an open threat absorbed) | the D-02 runs | high | mitigate | The plan's Stop condition halts Phase 207.1 on OPEN_THREATS, ESCALATE or BLOCKED; Task 3 requires `threats_open: 0` and `status: verified` and never repairs a file by hand. |
| T-207.1-20 | Tampering (commit on the wrong branch) | the workflow's `gsd-tools query commit` | medium | mitigate | The operator checks the branch after each run and Task 3's verify fails with BRANCH MOVED; the known branch-switch defect is named in the instructions. |
| T-207.1-21 | Elevation (a gate approved without a human) | checkpoint handling under auto mode | medium | mitigate | `checkpoint:human-action` always stops, and the instructions forbid running the workflows under `--auto` or `--chain`. |
</threat_model>

<verification>
- Task 3 prints `PASS-207.1-06-D02`.
- `git -C /workspaces log --format=%s -3 -- ':(glob).planning/phases/20[245]-*/*-SECURITY.md'` lists the three workflow commits.
</verification>

<success_criteria>
- Audit items 202, 204 and 205 "NO SECURITY.md" are fixed by D-02, or the phase is stopped for the operator with the verdict recorded.
- No SECURITY.md is written or edited by hand; nothing is pushed.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-06-SUMMARY.md` when done, with the per-phase record Task 3 describes.
</output>
