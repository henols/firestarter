---
phase: 207.1-address-v1-41-tech-debt
verified: 2026-09-24T18:42:08Z
status: passed
score: 22/22 must-haves verified
covered_files:
  - .planning/WINDOWS.md
  - .planning/milestones/v1.41-MILESTONE-AUDIT.md
  - .planning/phases/202-one-comparison-engine-on-the-host/202-SECURITY.md
  - .planning/phases/204-the-command-surfaces-leave-the-firmware/204-SECURITY.md
  - .planning/phases/205-the-pre-flights-leave-the-firmware/205-SECURITY.md
  - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md
  - .planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt
  - .planning/phases/207-the-version-and-the-record/207-REVIEW-FIX.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-01-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-01-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-02-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-02-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-03-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-03-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-04-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-04-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-05-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-05-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-06-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-06-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-07-PLAN.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-07-SUMMARY.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
  - .planning/phases/207.1-address-v1-41-tech-debt/207.1-LEDGER.md
  - firestarter_app/.github/scripts/update_version.py
  - firestarter_app/firestarter/chip_test.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/compare.py
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/firestarter/write_blank_guard.py
  - firestarter_app/tests/test_eprom_operations.py
  - firestarter_app/tests/test_fw_update_path_gate.py
  - firestarter_app/tests/test_fwguard.py
  - firestarter_app/tests/test_hw_revision_gate.py
  - firestarter_app/tests/test_serial_comm.py
  - firestarter_app/tests/test_write_blank_guard.py
  - firestarter_app/tests/test_write_blank_guard_pinning.py
  - firestarter_app/tests/test_write_verify.py
  - firestarter_fw/CLAUDE.md
covered_digest: "v1:sha256:5b337b526740f1fc7cfdeff3bb6d198ed65963dc9d3cdf85cc795f16447efc70"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 207.1: Address v1.41 tech debt Verification Report

**Phase Goal:** Every open item in the v1.41 close-time audit is either resolved or explicitly
accepted by the operator before the milestone ships. The list is the `tech_debt` frontmatter of
`.planning/milestones/v1.41-MILESTONE-AUDIT.md`: 26 open items, of which 4 need an operator decision.
**Verified:** 2026-09-24T18:42:08Z
**Status:** passed
**Re-verification:** No. This is the initial verification.

**Must-have sources.** ROADMAP.md gives this phase no success criteria and no requirement IDs
("TBD — no v1.41 requirement is open"). The must-haves are the phase goal, the must_haves blocks
of the 7 PLANs, and decisions D-01..D-20 in `207.1-CONTEXT.md`. The audit frontmatter was not
edited during the phase. Its last commit is `4bab20c9`, which is before the phase. So the 26-item
list checked here is the list the phase was planned against.

**Covered state.** app `5302f63`, fw `4b14111`, meta `231dca1c`. The app and fw HEADs are now
`b8b920c` and `dbdf9c8`. Each of those adds only a CLAUDE.md rewrite made after the phase. See the
D-13 note.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The closing ledger maps each of the 26 open audit items plus 202 WR-02 to a D-NN and one outcome (fixed / accepted / closed on evidence). D-01..D-20 are all accounted for. | ✓ VERIFIED | `207.1-LEDGER.md`: rows 1-26 plus the WR-02 row, in the audit's own order. Each row was checked by hand against the audit frontmatter (202: 4+1, 203: 4, 204: 4, 205: 4, 206: 4, 207: 1, cross: 5). D-NN coverage: D-01 r9; D-02 r4/11/15; D-03/D-04 r17 + § D-04; D-05 r1; D-06 r5; D-07 r6; D-08 r7; D-09 r2; D-10/D-11 r18; D-12 r26; D-13 r22; D-14 r21; D-15 r14; D-16/D-17 r25; D-18 r3/8/10/13/16/19/23; D-19 r12/20/24; D-20 the WR-02 row. Every meta, app and fw sha the ledger cites resolves and is an ancestor of its repo's HEAD, including fw `1cf1b22`. |
| 2 | D-01: WINDOWS entry 3 reads `fixed` in both the table and the JSON block, with the same reason in both. The counts are open 0 / fixed 3, and the drift check passes. The reason cites the operator's same-chip confirmation, 0xDA08 on six runs on the same rig, and VPP as the probable cause (not a measured one). | ✓ VERIFIED | `.planning/WINDOWS.md` frontmatter: `open_count: 0`, `fixed_count: 3`. `broken-windows.cjs` was run read-only: `renderTable(entries) === extractTableRegion(text)` is true and `openCount` is 0. The operator's answer is recorded in `207.1-DISCUSSION-LOG.md:36,46` ("Same chip; close on evidence"). `206-SESSION-COST.md:97-100,289,333` shows `chip_id_actual: 55816` on all six runs. `206-04-SUMMARY.md:130` records the 13.0 V to 12.0 V trim. The "VPP is high 13.0V" warning appears in both `204-BENCH-TRACER.md` and `205-BENCH-MATRIX.md`. |
| 3 | D-02: 202, 204 and 205 SECURITY.md exist, each `status: verified`, `threats_open: 0`, `asvs_level: 1`, and each committed by the workflow's own `docs(phase-NNN): add/update security threat verification` commit. 205 lists T-205-10 and T-205-16 as accepted. The OPEN_THREATS state stopped the work for an operator call and was not absorbed. | ✓ VERIFIED | The frontmatter of all three files was read. Commits: `105c2523`, `c6116726`, `db82425e`, each 1 file, with the workflow subject. `205-SECURITY.md:68,74` has T-205-10 and T-205-16 with disposition accept. `:111-112` has AR-205-03 and AR-205-04. `:134-136` records `threats_open` 2 at classification, the stop at the user gate, and the operator choosing "Accept all open". The operator's choice is evidenced by the record only; no independent transcript exists. |
| 4 | D-03: both `_run_step_untimed` branches still hard-set `run_count=1`. A comment at both sites names the trade-off: the `--fast` re-key, the re-key of filed reports that a change would cause, and harm limited to reports already ERROR/BAD. The change is comment-only. | ✓ VERIFIED | `chip_test.py:2618-2641` holds the full comment on the `(SerialError, HardwareOperationError)` branch. `:2649` holds the cross-reference on the `EpromOperationError` branch. Both still pass `run_count=1`. `ast.dump` equals the `64da8f6` blob (run here). Advisory: code review WR-01 found the cycle-block mechanism sentence incomplete. See Advisory. |
| 5 | D-04: the read-only gh query and its count are in the record, and D-03 stands at that count. | ✓ VERIFIED | `207.1-LEDGER.md` § The D-04 count gives the two commands, the discriminator, the result (7 issues, 9 reports) and the statement that D-03 stands. Independent read-only re-fetch here of issues #65, #66, #70, #71, #83, #86, #90 and comments 5698646617 and 5713093962: in all 9, `read` has `run_count 2`, the cycle-block steps have `run_count 1`, and at least one cycle-block step carries an `error_code` (175/176/183/189). That is the `hardware_refused`-break shape the ledger claims. |
| 6 | D-05: `TestVerifyEpromReadAbort` pins both sides of the window. Just inside the window gives verdict 1, exactly at it gives 1, and the existing beyond-window leg still gives 2. The constant is still the literal 3.0 and is not configurable. The accepted-outcome note names the callers and fail-closed. | ✓ VERIFIED | `tests/test_eprom_operations.py`: `test_timeout_just_inside_the_acceptance_window_is_the_intended_abort` (WINDOW − 0.001 gives 1) and `test_timeout_exactly_at_the_acceptance_window_is_the_intended_abort` (WINDOW gives 1). The existing leg gives 2. `eprom_operations.py:112-123` holds the note, which names verify, blank, the write blank guard and write --verify, and says the guard path is fail-closed. `READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0`. The legs pass in the full-suite run. |
| 7 | D-06: a `write --verify` whose write landed but whose `--skip-sdp-unlock` ack is missing exits 1 and prints the new landed-but-unacknowledged line. It does not print the did-not-complete line and does not call `verify_eprom`. The help text and snapshots are unchanged. | ✓ VERIFIED | `cli_handlers.py:605-608` defines `_WRITE_VERIFY_VERDICT_LANDED_UNACKNOWLEDGED`. `:977-987` is arm 4b, `attempt_verdict == 0`. Reachability was checked: `write_eprom` sets verdict 0 at `eprom_operations.py:2654`, before the ack block at `:2700-2712` flips `is_ok`, and resets it at entry (`:2432`). The test `test_write_verify_arm4b_landed_but_sdp_skip_unacknowledged_exits_1` passes. No snapshot file is in the diff. CONTEXT asked to "update the test that pins the old wording", but that test does not exist. This was confirmed with `git grep` at `64da8f6`: the only test of that state pins the verdict value, not the wording. So adding a test is the correct reading. |
| 8 | D-07: an incomplete-but-not-mismatched guard read still refuses: no COMMAND_WRITE, verdict 1. It logs one ERROR line that states the coverage, with no address or value. The mismatch-path `refusal_text` / `_REFUSAL_FORMAT` are unchanged. | ✓ VERIFIED | `write_blank_guard.py:122-126,291-305` holds `_INCOMPLETE_REFUSAL_FORMAT` and `incomplete_refusal_text`. `eprom_operations.py:365-383,2372-2383` makes the selection on `first_offset is None`, and the `0` substitutions are removed. `_REFUSAL_FORMAT` and `refusal_text` do not change in the diff. The rewritten pin and the two helper tests pass. |
| 9 | D-08: `require_non_negative_address` runs after `require_page_size` and before `require_page_alignment`, at both tiers. W29C020 with `-a -1` exits 1 with exactly one line, the negative-address line, and the operator tier raises `NegativeStartAddressError` before `_operation_context`. | ✓ VERIFIED | `cli_handlers.py:861,876-877` and `eprom_operations.py:2469,2481-2482` show the new order. Tests `test_write_eprom_negative_misaligned_start_address_refuses_as_negative_first` and `test_cli_write_negative_misaligned_address_on_a_page_write_part_gets_the_negative_message` pass. The CLI helper asserts `len(lines) == 1` and the exact line. |
| 10 | D-09: the `if not offs:` branch is kept, with a comment naming it defensive redundancy. The change is comment-only. | ✓ VERIFIED | `compare.py:252-262` has the comment ("defensive, not dead ... 207.1 D-09"), and the branch is intact. `ast.dump` equals the `64da8f6` blob. |
| 11 | D-10: the lease docstring no longer claims thirty calls and thirty resets. It gives an "on the order of" count labelled as derived and cites the measured 15.1 %. | ✓ VERIFIED | `eprom_operations.py:826-839` says "on the order of twenty ... a derived count and not a measured one" and cites 15.1 % (40.709 s of 268.992 s, N=3). All figures were confirmed in `206-SESSION-COST.md:133,274-277`. |
| 12 | D-11: `setup_command` asserts `is_connected()` as its first statement. A not-connected call raises AssertionError and sends nothing. The three probe harnesses patch `is_connected`. The docstring is rewritten. | ✓ VERIFIED | `serial_comm.py:858` is `assert self.is_connected(), ...`, the first statement after the docstring. `:836-848` is the new docstring (AssertionError, not SerialError; the `_probe_port` `except Exception` behaviour; stripped by `python -O`). `test_setup_command_asserts_the_link_is_connected_before_sending` asserts `calls == []`. `test_fw_update_path_gate.py`, `test_fwguard.py` and `test_hw_revision_gate.py` each patch `is_connected` to True. |
| 13 | D-12: `ruff format --check .` over the whole app tree passes. `update_version.py` changed in formatting only, and it keeps mode 100755 and its shebang. | ✓ VERIFIED | Run here: `ruff format --check .` reports "175 files already formatted". `ast.dump` of `update_version.py` equals the `64da8f6` blob. `git ls-files -s` shows mode `100755`. The first line is `#!/usr/bin/env python3`. |
| 14 | D-13: meta's two gitlinks were advanced once, in one commit touching exactly those two paths, to the sub-repo HEADs after all of the phase's sub-repo commits. | ✓ VERIFIED | `3f46a284` changes 2 files (`firestarter_app` 2a08c7d → 5302f63, `firestarter_fw` a55f2d8 → 4b14111). It is the only gitlink commit in `10c9b615..HEAD`. 5302f63 (00:08Z) and 4b14111 were the last phase commits in each sub-repo, and both predate `3f46a284` (11:49Z). Post-phase lag: see Notes. |
| 15 | D-14: `207-REVIEW-FIX.md` records `13571d2` as pushed at 2026-09-23T20:25:36Z and as `origin/master`, and `status: all_fixed` is unchanged. STATE.md's Phase 207 line records WR-01 as fixed and pushed. STATE.md was edited by hand. | ✓ VERIFIED | `207-REVIEW-FIX.md:9,39-40`. The local wiki clone shows `13571d2` as `origin/master`. `STATE.md:3258` carries the corrected line. `e5a4e645` touches only STATE.md and 207-REVIEW-FIX.md. No STATE line still calls WR-01 unfixed or unpushed. |
| 16 | D-15: `firestarter_fw/CLAUDE.md` gives the live `tests/` count, re-collected and dated, committed on fw `v1.41-verification-to-host`. | ✓ VERIFIED | fw `4b14111` changes "320 collected tests on 2026-09-22" to "316 collected tests on 2026-09-23". A live `pytest --collect-only` here gives 316. The later rewrite `dbdf9c8` (not phase work) removed the count sentence, so no wrong count exists in the current tree either. |
| 17 | D-16: the connect-cost log is committed in meta, byte-identical, and both citations in `206-SESSION-COST.md` point at the meta path. The app-tree directory is gone. | ✓ VERIFIED | `evidence/connect-cost-2026-09-23-110029-connect-cost-log.txt` is tracked. Its sha256 is `7d683964…c6c9f`, as planned. It has 23 lines, including `median: 2.613s`. `206-SESSION-COST.md:86,323` cite the meta path, and no app-tree path remains. `firestarter_app/connect-cost-2026-09-23-110029/` no longer exists. |
| 18 | D-17: `datasheets/LST62832I.pdf` is still on disk, untracked, and ignored by the per-clone exclude, which is not committed. | ✓ VERIFIED | sha256 `76a83100…737df` matches. `git ls-files` returns nothing for the file. `git check-ignore -v` names `.git/info/exclude:7:/datasheets/LST62832I.pdf`. |
| 19 | D-18 / D-19 / D-20: each accepted-without-work item has its own line citing its source. The stale digests are accepted and no VERIFICATION.md in 202-207 changed. WR-02 is listed as closed by 206. | ✓ VERIFIED | The ledger §§ "Accepted without work", "Verification digests" and "202 WR-02" are present. `git diff --quiet 10c9b615 HEAD -- '.planning/phases/20[2-7]-*/*-VERIFICATION.md'` exits 0. The operator confirmed the disposition table (`207.1-DISCUSSION-LOG.md` § Disposition of the remaining items). |
| 20 | Both folded todos are in `todos/completed/` and no longer in `pending/`. | ✓ VERIFIED | Both `2026-09-20-*` files are in `completed/` and absent from `pending/`. Commit `5b51463a`. |
| 21 | The CI-equivalent app gate on Python 3.11 is green after all of the phase's app changes: ruff check and format are clean, coverage is at least 70 %, at least 2373 tests pass, and 36 snapshots pass. | ✓ VERIFIED | Run once here on `.venv311` (Python 3.11.16) with `-o addopts=""`: **2373 passed, 36 snapshots passed, coverage 86.40 %**. `ruff check firestarter/ tests/` reports "All checks passed!". |
| 22 | Nothing from the phase is on any remote. | ✓ VERIFIED | For meta, app and fw, HEAD is not an ancestor of `origin/beta`. `git ls-remote --heads origin 'v1.41*'` is empty for all three, and `git ls-remote --tags origin 'v1.41*'` is empty. There is no local `v1.41*` tag. |

**Score:** 22/22 truths verified (0 present, behavior-unverified).

The behavior-dependent truths are D-05, D-06, D-07, D-08 and D-11. Each one is backed by a named
test that passed in this verification's single full-suite run.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `firestarter_app/firestarter/write_blank_guard.py` | `incomplete_refusal_text` (D-07) | ✓ VERIFIED | Defined, imported by `eprom_operations.py:80`, used at `:377` |
| `firestarter_app/firestarter/cli_handlers.py` | `_WRITE_VERIFY_VERDICT_LANDED_UNACKNOWLEDGED`, the D-08 order | ✓ VERIFIED | Constant, plus arm 4b, plus the reordered gates |
| `firestarter_app/firestarter/eprom_operations.py` | D-07 selection, D-08 order, D-05 note, D-10 docstring | ✓ VERIFIED | All four present |
| `firestarter_app/firestarter/serial_comm.py` | `assert self.is_connected()` | ✓ VERIFIED | `:858` |
| `firestarter_app/firestarter/chip_test.py` | "207.1 D-03" comment | ✓ VERIFIED | `:2619`, `:2649` |
| `firestarter_app/firestarter/compare.py` | "207.1 D-09" comment | ✓ VERIFIED | `:253` |
| `firestarter_app/.github/scripts/update_version.py` | formatted, AST-equal | ✓ VERIFIED | |
| 8 new tests plus 1 rewritten pin plus 3 harness patches | D-05/06/07/08/11 | ✓ VERIFIED | All pass |
| `.planning/WINDOWS.md` | entry 3 fixed | ✓ VERIFIED | Contains "same physical W27C512" |
| `.planning/phases/206-.../evidence/connect-cost-…-log.txt` | at least 23 lines, sha matches | ✓ VERIFIED | |
| `202/204/205-SECURITY.md` | verified / 0 / L1 | ✓ VERIFIED | |
| `.planning/todos/completed/2026-09-20-*.md` (×2) | moved | ✓ VERIFIED | |
| `207.1-LEDGER.md` | contains the `\| 26 \|` row | ✓ VERIFIED | |
| `firestarter_fw/CLAUDE.md` | 316 | ✓ VERIFIED | At `4b14111`. Sentence later removed by a non-phase rewrite |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `cli_handlers.write` | `write_blank_guard.require_non_negative_address` | called before `require_page_alignment` | WIRED (`:876-877`) |
| `_run_write_blank_guard` | `incomplete_refusal_text` | `_write_blank_guard_refusal_message` when `first_offset is None` | WIRED |
| `cli_handlers.write` (`ok is False`) | `EpromOperator.last_write_attempt_verdict` | `attempt_verdict == 0` gives arm 4b | WIRED; verdict 0 set before the ack block |
| `_probe_port` / leased `_setup_operation` | `setup_command` assert | both call only on an open link | WIRED; the assert cannot fire on either production path (the reviewer agrees) |
| boundary legs | `READ_ABORT_ACCEPTANCE_WINDOW_S` | `check_time` computed from the module constant | WIRED |
| `206-SESSION-COST.md` | meta evidence path | § 2 citation and provenance table | WIRED (`:86`, `:323`) |
| meta gitlinks | app `5302f63` / fw `4b14111` | `3f46a284` | WIRED; every cited app sha is reachable from meta HEAD |
| `WINDOWS.md` table | JSON block | `renderTable === extractTableRegion` | WIRED (drift check passes) |

### Data-Flow Trace (Level 4)

Not applicable. This phase renders no dynamic data. The CLI output lines are constants, and the
tests assert them.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full app gate on Python 3.11 | `.venv311/bin/python -m pytest -o addopts="" tests/ --cov=firestarter --cov-fail-under=70` | 2373 passed, 36 snapshots, 86.40 % | ✓ PASS |
| Whole-tree format | `ruff format --check .` | 175 files already formatted | ✓ PASS |
| Lint | `ruff check firestarter/ tests/` | All checks passed | ✓ PASS |
| Comment-only / format-only edits | `ast.dump` vs the `64da8f6` blob for `update_version.py`, `chip_test.py`, `compare.py` | AST-EQUAL ×3 | ✓ PASS |
| fw `tests/` count | `pytest tests/ --collect-only -q` | 316 collected | ✓ PASS |
| WINDOWS drift | `broken-windows.cjs` `renderTable` vs `extractTableRegion` (read-only) | equal; open 0 | ✓ PASS |
| D-04 shape | read-only `gh issue view` / `gh api …/comments/<id>` on the 9 reports | all 9: read rc 2, cycle-block rc 1 with error_code | ✓ PASS |

### Probe Execution

Not applicable. No PLAN or SUMMARY declares a `scripts/*/tests/probe-*.sh` probe.

### Requirements Coverage

The phase carries no requirement IDs. All 7 PLANs declare `requirements: []`. REQUIREMENTS.md does
not mention 207.1, has 34 checked items and 0 unchecked, and did not change during the phase. There
are no orphaned requirements. The coverage unit is D-01..D-20 plus the 27 ledger rows, verified
above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (all 15 app files the phase changed, fw CLAUDE.md, and the meta records) | — | TBD / FIXME / XXX / TODO / HACK / PLACEHOLDER | — | None found in any file the phase touched, or in any added line |

### Advisory (code review, not phase scope)

These come from `207.1-REVIEW.md` (0 critical, 3 warning, 6 info). Each warning was re-checked
against the code. None of them makes a must-have fail, and none of them is one of the 26 audit
items. They are listed here so that they are not lost before `/gsd-ship`.

| # | Finding | Category | Why advisory |
|---|---|---|---|
| 1 | **WR-01**: the new D-03 comment (`chip_test.py:2630-2640`) says it states the mechanism "precisely", but it names only one cycle-block route (the `hardware_refused` break). It omits the second route. In that route, a cycle that raised here becomes SKIPPED with no `error_code`, so there is no break, and it drops out of `ran`. `_aggregate_cycle_results` then gives `run_count=len(ran)=1` (confirmed at `:1364,1389`). | other (comment accuracy on a costly-to-reverse decision) | The trade-off statement D-03 requires is correct. The claim "reverting this branch alone would re-key none of [the filed reports]" still holds. All 9 filed reports are the `hardware_refused` shape (confirmed here). Recommended: a one-sentence comment fix before ship. |
| 2 | **WR-02** (Phase 206, pre-existing): the leased branch calls `consume_remaining_input()` at `eprom_operations.py:695`, outside the `try/except SerialError` that drops the held link. A drain failure therefore leaves a dead link leased, and every later leased step fails the same way. | other (correctness; contradicts 206-SECURITY.md T-206-12 and `lease()`'s D-06 text) | Not one of the 26 audit items. Confirmed in code. The operator should decide whether to fix it before ship or file it as backlog. |
| 3 | **WR-03** (pre-existing): `update_version.py --set-version` does not enter beta mode. On a non-beta ref it silently bumps a stable patch. | other (publish path; stable releases are operator-gated) | D-12 made no logic change (AST-equal). Not an audit item. It needs an operator disposition. |
| 4 | IN-01..IN-06 | info | Style, duplication and hygiene: arm 4b does not check `skip_sdp_unlock`; the boundary legs are duplicated; there is no direct unit test of `_write_blank_guard_refusal_message`; a vacuous "0 of N" line; a point-in-time count in source; `update_version.py` hygiene. |

### Notes for milestone close

- **Gitlink lag after the phase (not phase work).** Meta HEAD records app `5302f63` and fw
  `4b14111`. The sub-repo HEADs are `b8b920c` and `dbdf9c8`, from the parallel operator session.
  Each adds only a CLAUDE.md rewrite (`git diff --stat` shows only `CLAUDE.md` changed in each).
  Meta's working tree shows ` M firestarter_app` / ` M firestarter_fw`. The milestone close needs
  one more gitlink advance if those rewrites are to ship. This does not affect D-13, which was
  correct when it was committed.
- The fw CLAUDE.md rewrite removed the `tests/` count sentence that D-15 corrected. The audit item
  (a wrong count) stays resolved, because the current file makes no count claim.
- The app's second CI job (`tests/test_pyusb_api_surface.py`, `py32` extra) was not run. The phase
  touched no pyusb code.

### Human Verification Required

None. Every must-have has code, record or test evidence. The three review warnings above are
dispositions the operator must decide. They are not verification items, and they fall outside the
phase's 26-item scope.

### Gaps Summary

None. All 26 open audit items and 202 WR-02 have a disposition, and each disposition is backed by
evidence in the codebase or the record:

- **Fixed (14 rows):** these are real code or record changes. The tests that pass cover the
  behavior-dependent ones.
- **Accepted (11 rows):** each one traces to an operator-confirmed decision (D-03, D-18, D-19).
- **Closed on evidence (2 rows):** WINDOWS #3 and WR-02. Each cites the measured record.

The four operator decisions the audit named were each made by the operator (DISCUSSION-LOG) and
carried out as decided:

- WINDOWS #3: closed on evidence.
- run_count: kept, and the count was recorded.
- SECURITY.md ×3: produced, with the 205 open-threat gate answered by the operator.
- 202 WR-01: a boundary test plus a note.

---

_Verified: 2026-09-24T18:42:08Z_
_Verifier: Claude (gsd-verifier)_
