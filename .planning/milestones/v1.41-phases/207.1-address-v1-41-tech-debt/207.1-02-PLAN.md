---
phase: 207.1-address-v1-41-tech-debt
plan: 02
type: execute
wave: 2
depends_on: ["207.1-01"]
files_modified:
  - firestarter_app/firestarter/serial_comm.py
  - firestarter_app/tests/test_serial_comm.py
  - firestarter_app/tests/test_fw_update_path_gate.py
  - firestarter_app/tests/test_fwguard.py
  - firestarter_app/tests/test_hw_revision_gate.py
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/tests/test_eprom_operations.py
autonomous: true
requirements: []

estimate:
  tokens: 32000
  raw_tokens: 64000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "D-11: `SerialCommunicator.setup_command` asserts `self.is_connected()` as its first statement; a call on a communicator whose `connection` is `None` raises `AssertionError` and sends nothing on the wire."
    - "D-11: the full app suite on Python 3.11 still passes after the assert lands, because the three probe harnesses that replace `SerialCommunicator.__init__` now also patch `is_connected` to True; the intermediate `12 failed` observation that proves why is recorded in the SUMMARY."
    - "D-11: the `setup_command` docstring no longer argues against a second guard; it states that the precondition is asserted, that the assert raises `AssertionError` (not `SerialError`), that `_probe_port`'s `except Exception` logs it and returns `None`, and that `python -O` strips it."
    - "D-05: `TestVerifyEpromReadAbort` pins both sides of the 3.0 s window: a deliberate stop whose `MSG_ERR_TIMEOUT` frame arrives just inside the window, and one exactly at it, both resolve to verdict 1 (the intended abort), while the existing leg one second beyond still resolves to 2."
    - "D-05: `READ_ABORT_ACCEPTANCE_WINDOW_S` is still the literal 3.0 and still non-configurable; the note above it says a false exit 2 under extreme host scheduling or USB latency is a known, accepted outcome, names every caller of `_drive_region_compare`, and says the write-guard path stays fail-closed."
    - "D-10: the `EpromOperator.lease()` docstring no longer claims an unmeasured count of thirty calls and thirty resets; it gives an on-the-order-of count labelled as derived and cites the measured 15.1 % saving from `206-SESSION-COST.md`."
    - "No commit in this plan is pushed; every commit lands on `firestarter_app` branch `v1.41-verification-to-host`."
  artifacts:
    - path: firestarter_app/firestarter/serial_comm.py
      provides: "the D-11 precondition assert and the rewritten `setup_command` docstring"
      contains: "assert self.is_connected()"
    - path: firestarter_app/tests/test_serial_comm.py
      provides: "the D-11 positive test"
      contains: "def test_setup_command_asserts_the_link_is_connected_before_sending"
    - path: firestarter_app/tests/test_eprom_operations.py
      provides: "two D-05 boundary legs in TestVerifyEpromReadAbort"
      contains: "def test_timeout_just_inside_the_acceptance_window_is_the_intended_abort"
    - path: firestarter_app/firestarter/eprom_operations.py
      provides: "the D-05 accepted-outcome note at the constant and the D-10 docstring"
  key_links:
    - from: firestarter_app/firestarter/serial_comm.py
      to: firestarter_app/firestarter/serial_comm.py
      via: "`_probe_port` calls `communicator.setup_command(...)` on a freshly constructed communicator; its `except Exception` arm turns an `AssertionError` into a logged port-walk miss"
      pattern: "communicator\\.setup_command\\("
    - from: firestarter_app/firestarter/eprom_operations.py
      to: firestarter_app/firestarter/serial_comm.py
      via: "the leased `_setup_operation` branch calls `self.comm.setup_command(...)` only after `self.comm.is_connected()`"
      pattern: "self\\.comm\\.setup_command\\("
    - from: firestarter_app/tests/test_eprom_operations.py
      to: firestarter_app/firestarter/eprom_operations.py
      via: "the boundary legs fake `time.monotonic` for exactly two calls (the stop, then the check) and compute `check_time` from `eprom_ops_mod.READ_ABORT_ACCEPTANCE_WINDOW_S`"
      pattern: "READ_ABORT_ACCEPTANCE_WINDOW_S"
---

<objective>
Close the three transport-and-timing items on the host side: D-11 (206 IN-02, assert the open-link
precondition of `setup_command` so a future caller that skips it fails at the right frame), D-05
(202 WR-01, pin the 3.0 s read-abort window from the inside and write down that a false exit 2 under
extreme latency is an accepted outcome) and D-10 (206 IN-01, stop claiming an unmeasured thirty
board resets).

Purpose: D-11 turns a caller bug into a failure that names the caller's frame. D-05 gives the next
reader of a "verify says hardware error but the chip only mismatches" report the answer before they
chase a hardware fault. D-10 replaces an overstated number with the measured one.

Output: one assert, one rewritten docstring passage, one harness line in each of three test files,
three new tests, one comment paragraph, one docstring sentence.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/CLAUDE.md
@/workspaces/firestarter_app/CLAUDE.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-CONTEXT.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md
@/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-PATTERNS.md
@/workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-REVIEW.md
@/workspaces/.planning/phases/202-one-comparison-engine-on-the-host/202-REVIEW.md

<interfaces>
Extracted from the live tree at firestarter_app 64da8f6 (line numbers drift; locate by symbol).

firestarter/serial_comm.py  class SerialCommunicator
  def is_connected(self) -> bool: return self.connection is not None and self.connection.is_open   (about line 215)
  def setup_command(self, command_to_send: dict, config_manager: ConfigManager, *, allow_outdated_firmware: bool = False) -> bool   (about line 805)
    docstring about lines 812-852; the passage to rewrite about lines 833-844; first statement self.send_json_command(command_to_send) about line 869, then is_ok, msg = self.expect_ack(); `if not is_ok: return False`
  _probe_port: `if not communicator.setup_command(` about line 995; `except Exception as e: logger.error(f"Unexpected error while probing {port_name}: {e}")` then disconnect, `return None` (about lines 1028-1032)

tests/conftest.py
  make_comm fixture: a factory; make_comm() builds a SerialCommunicator via __new__ with connection = fake_serial (_FakeSerial.is_open is True)

firestarter/eprom_operations.py
  READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0 under the `# 202-04 D-08:` block (about lines 102-111)
  single consumer in _drive_region_compare: `and (time.monotonic() - stopped_at) <= READ_ABORT_ACCEPTANCE_WINDOW_S` (about line 2777)
  EpromOperator.lease() docstring: the "What a lease removes that is not only time" paragraph (about lines 793-800)

tests/test_eprom_operations.py
  class TestVerifyEpromReadAbort (about line 641); test_timeout_outside_the_acceptance_window_returns_two (about lines 802-856) with a LOCAL _FakeTime class
</interfaces>
</context>

## Spec-less probe fallback

Skipped, visibly: this phase has no requirement IDs, so there are no spec probes to generate.

## Artifacts this phase produces (this plan)

- `firestarter_app/tests/test_serial_comm.py`: `test_setup_command_asserts_the_link_is_connected_before_sending`.
- `firestarter_app/tests/test_eprom_operations.py`: `TestVerifyEpromReadAbort.test_timeout_just_inside_the_acceptance_window_is_the_intended_abort`, `TestVerifyEpromReadAbort.test_timeout_exactly_at_the_acceptance_window_is_the_intended_abort`.
- No new source symbol: D-11 adds an `assert` statement; D-05 and D-10 change a comment and a docstring.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): D-11 end to end -- setup_command asserts an open link, and the whole probe path stays green</name>
  <files>firestarter_app/firestarter/serial_comm.py, firestarter_app/tests/test_serial_comm.py, firestarter_app/tests/test_fw_update_path_gate.py, firestarter_app/tests/test_fwguard.py, firestarter_app/tests/test_hw_revision_gate.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/serial_comm.py (`is_connected` about line 215; `setup_command` about lines 805-890; `_probe_port` about lines 960-1035)
    - /workspaces/firestarter_app/firestarter/eprom_operations.py (the leased branch of `_setup_operation`, about lines 650-670, read-only)
    - /workspaces/firestarter_app/tests/test_serial_comm.py (lines 421-530: the three existing `setup_command` tests)
    - /workspaces/firestarter_app/tests/conftest.py (the `make_comm` and `fake_serial` fixtures, about lines 140-220)
    - /workspaces/firestarter_app/tests/test_fw_update_path_gate.py (harness `_probe_with_identity`, about lines 48-66)
    - /workspaces/firestarter_app/tests/test_fwguard.py (harness `_probe_with_identity`, about lines 40-58)
    - /workspaces/firestarter_app/tests/test_hw_revision_gate.py (harness `_probe`, about lines 358-380)
    - /workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-RESEARCH.md (§ D-11: the measured 12-test fallout and the proved harness fix)
  </read_first>
  <behavior>
    - New test: `comm = make_comm()`, then `comm.connection = None`; `SerialCommunicator.send_json_command` monkeypatched to record its call; `SerialCommunicator.expect_ack` monkeypatched to return `(False, "stub: not ok")`; `comm.setup_command({"state": 13}, MagicMock())` raises `AssertionError`, and the recorded call list is empty. On today's code it returns False with one recorded call, so `pytest.raises` reports DID NOT RAISE (RED).
    - After the assert lands and before the harness fix: tests/test_fw_update_path_gate.py, tests/test_fwguard.py and tests/test_hw_revision_gate.py report 12 failed (research measured 12 of their 50).
    - After the harness fix: all tests in those three files, tests/test_serial_comm.py and tests/test_session_lease.py pass, and the full suite passes.
  </behavior>
  <action>
    Per D-11 (206-REVIEW IN-02).

    RED first. In tests/test_serial_comm.py, directly after `test_setup_command_refuses_when_the_firmware_gate_fails`, add `test_setup_command_asserts_the_link_is_connected_before_sending(make_comm, monkeypatch)` implementing the behavior block. Record calls with a list and a function that accepts `(self, *args, **kwargs)` and appends the first positional argument; stub `expect_ack` with a function that accepts `(self, *args, **kwargs)` and returns the tuple. Import `MagicMock` locally as the neighbouring tests do. The docstring says: 206-REVIEW IN-02 / 207.1 D-11 -- a caller that skips the open-link precondition must fail here, at `setup_command`'s own frame, before any byte is sent. Run it and observe DID NOT RAISE. Record the RED evidence naming this test.

    GREEN, step 1. In `SerialCommunicator.setup_command`, add `assert self.is_connected(), "setup_command requires an open link"` as the first statement after the docstring, before the existing comment block that precedes `self.send_json_command(command_to_send)`. Run the new test and observe it pass.

    GREEN, step 2 (observe the fallout before fixing it). Run `.venv311/bin/python -m pytest tests/test_fw_update_path_gate.py tests/test_fwguard.py tests/test_hw_revision_gate.py -o addopts="" -p no:cacheprovider -q 2>&1 | tail -1` and record the exact summary line in the SUMMARY; research measured `12 failed`. If the count is not 12, stop and list the failing test ids in the SUMMARY before continuing, so the difference is explained rather than patched over. The cause is that each harness patches `SerialCommunicator.__init__` to a no-op, so `self.connection` never exists and the assert fails inside `_probe_port`'s `except Exception`.

    GREEN, step 3. In each of the three harnesses (`_probe_with_identity` in tests/test_fw_update_path_gate.py, `_probe_with_identity` in tests/test_fwguard.py, `_probe` in tests/test_hw_revision_gate.py) add the one line `patch.object(SerialCommunicator, "is_connected", return_value=True),` to the `with (...)` tuple, next to the other method patches and before the `__init__` patch, with a short trailing comment saying `setup_command` asserts the link is open (207.1 D-11). Keep the two `_probe_with_identity` helpers duplicated, as the fw-update docstring says they deliberately are. Make no new fixture.

    GREEN, step 4. Rewrite the docstring passage that currently argues this method must not duplicate `send_bytes`'s not-connected check. The new passage keeps the first half (a genuine transport failure during the send/read, a raised `SerialError`, propagates unchanged, because teardown is caller policy) and then says: a call on a link that is not connected is a caller bug, and this method asserts that precondition as its first statement (206-REVIEW IN-02, 207.1 D-11), so the failure names this frame instead of surfacing later as `send_bytes`'s not-connected `SerialError`; the assert raises `AssertionError`, not `SerialError`, so inside `_probe_port` it lands in the `except Exception` arm, which logs an "Unexpected error while probing" line and returns `None` while the port walk continues; `python -O` strips it; both production callers only ever invoke this on a link they have just confirmed is open (`_probe_port` on a communicator it just constructed, and a lease's setup site on `self.comm` after its own `is_connected()` check), so neither is affected. Leave the rest of the docstring as it is.

    Run `.venv311/bin/ruff format` and `.venv311/bin/ruff check` on the five files. Commit inside /workspaces/firestarter_app: the RED test commit, then the GREEN commit with subject `fix(207.1-02): D-11 assert setup_command's open-link precondition`. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest "tests/test_serial_comm.py::test_setup_command_asserts_the_link_is_connected_before_sending" tests/test_serial_comm.py tests/test_session_lease.py tests/test_fw_update_path_gate.py tests/test_fwguard.py tests/test_hw_revision_gate.py -o addopts="" -p no:cacheprovider -q 2>&1 | tail -1 | /usr/bin/grep -E -e '^[0-9]+ passed in '</automated>
    <fails_when>no output line (grep exit 1): the summary reads "N failed", "error" or "not found" instead of "N passed in"</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "import ast,sys; t=ast.parse(open('firestarter/serial_comm.py').read()); f=[n for c in t.body if isinstance(c,ast.ClassDef) and c.name=='SerialCommunicator' for n in c.body if isinstance(n,ast.FunctionDef) and n.name=='setup_command'][0]; d=ast.get_docstring(f) or ''; s=f.body[1]; ok=isinstance(s,ast.Assert) and ast.unparse(s.test)=='self.is_connected()' and 'AssertionError' in d and '-O' in d and 'D-11' in d; print(ast.unparse(s)[:80]); sys.exit(0 if ok else 1)" && [ "$(/usr/bin/grep -cF -e 'redundant guard' firestarter/serial_comm.py)" = "0" ] && for f in tests/test_fw_update_path_gate.py tests/test_fwguard.py tests/test_hw_revision_gate.py; do [ "$(/usr/bin/grep -cF -e 'patch.object(SerialCommunicator, "is_connected", return_value=True)' $f)" = "1" ] || { echo "HARNESS $f"; exit 1; }; done && .venv311/bin/ruff check firestarter/serial_comm.py tests/ && .venv311/bin/ruff format --check firestarter/serial_comm.py tests/ && S=$(git log --format=%s 64da8f6..HEAD) && printf '%s\n' "$S" | /usr/bin/grep -qF -e '(207.1-02): D-11' && echo PASS-207.1-02-T1-SOURCE</automated>
    <fails_when>PASS-207.1-02-T1-SOURCE is not printed: the AST check exits 1 (first statement after the docstring is not the assert, or the docstring lacks AssertionError / -O / D-11), the old passage wording still present, a harness without exactly one is_connected patch ("HARNESS" line), a ruff finding, or no D-11 commit</fails_when>
    <automated>cd /workspaces/firestarter_app && L=$(mktemp) && if timeout 580 .venv311/bin/python -m pytest tests/ -o addopts="" -p no:cacheprovider -q > "$L" 2>&1; then tail -2 "$L"; else tail -8 "$L"; exit 1; fi && N=$(tail -1 "$L" | sed -nE 's/^([0-9]+) passed in .*/\1/p') && [ -n "$N" ] && [ "$N" -ge 2371 ] && /usr/bin/grep -qxF -e '36 snapshots passed.' "$L" && echo "PASS-207.1-02-T1-SUITE $N"</automated>
    <fails_when>PASS-207.1-02-T1-SUITE is not printed: pytest exits non-zero (the tail shows "N failed" or errors), the last line is not "N passed in", fewer than 2371 passed (2365 baseline + 5 from 207.1-01 + 1 here), or the "36 snapshots passed." line is missing</fails_when>
  </verify>
  <acceptance_criteria>
    - `setup_command`'s first statement after its docstring is `assert self.is_connected(), ...` (AST check).
    - `test_setup_command_asserts_the_link_is_connected_before_sending` exists, passes, and its RED evidence (DID NOT RAISE on today's code) is recorded.
    - The SUMMARY records the intermediate three-file summary line observed between the assert and the harness fix (expected `12 failed`), or lists the failing ids if the count differed.
    - Each of the three harnesses carries exactly one `patch.object(SerialCommunicator, "is_connected", return_value=True)` line.
    - The docstring states AssertionError, `_probe_port`'s `except Exception` behaviour, `python -O`, and D-11; the old passage wording is gone.
    - The full app suite on `.venv311` (Python 3.11) exits 0 with at least 2371 passed and `36 snapshots passed.`.
    - A GREEN commit whose subject carries `(207.1-02): D-11` exists (the TDD flow may add a separate RED `test(...)` commit); nothing is pushed.
  </acceptance_criteria>
  <done>A not-connected `setup_command` call fails at its own frame with an AssertionError and sends nothing, and every existing test still passes on Python 3.11.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 boundary legs plus the accepted-outcome note, and D-10's measured lease figure</name>
  <files>firestarter_app/tests/test_eprom_operations.py, firestarter_app/firestarter/eprom_operations.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/eprom_operations.py (the `# 202-04 D-08:` block and `READ_ABORT_ACCEPTANCE_WINDOW_S` about lines 102-111; `lease()` docstring about lines 769-805; the consumer in `_drive_region_compare` about lines 2765-2785)
    - /workspaces/firestarter_app/tests/test_eprom_operations.py (the imports at lines 19-37; `class TestVerifyEpromReadAbort` docstring about lines 641-653; `test_timeout_outside_the_acceptance_window_returns_two` about lines 802-856)
    - /workspaces/.planning/phases/206-dev-test-keeps-its-fidelity-on-one-session/206-SESSION-COST.md (§ 3 about lines 103-140, derived census; § 5 about lines 188-280, measured 15.1 %)
    - /workspaces/.planning/phases/202-one-comparison-engine-on-the-host/202-READ-ABORT-ANSWER.md (the measured one-second-per-abort cost)
    - /workspaces/.planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md (the two routes D-05 takes)
  </read_first>
  <action>
    Per D-05 (202-REVIEW WR-01; folded todo `2026-09-20-read-abort-window-untested-at-its-boundary.md`) and D-10 (206-REVIEW IN-01). Research proved both new legs green on arrival on the current code (207.1-RESEARCH.md § D-05), so they are pins of the `<=` boundary, not a RED/GREEN pair; this task is not tdd. The pair of new legs plus the existing beyond-window leg on the same fixture is what shows the fixture discriminates.

    D-05 tests. In `TestVerifyEpromReadAbort`, directly after `test_timeout_outside_the_acceptance_window_returns_two`, add two methods by copying that method whole, including its local `_FakeTime` class (copy it; do not hoist it, so the existing test stays untouched), and changing only the name, the docstring, the `check_time` line and the final assertion. `test_timeout_just_inside_the_acceptance_window_is_the_intended_abort` sets `check_time = stop_time + eprom_ops_mod.READ_ABORT_ACCEPTANCE_WINDOW_S - 0.001` and asserts `verdict == 1`. `test_timeout_exactly_at_the_acceptance_window_is_the_intended_abort` sets `check_time = stop_time + eprom_ops_mod.READ_ABORT_ACCEPTANCE_WINDOW_S` and asserts `verdict == 1`, which pins the inclusive `<=`. Each docstring says: 207.1 D-05 (202-REVIEW WR-01) -- the deliberate stop's `MSG_ERR_TIMEOUT` arrives inside the acceptance window, so the run resolves to the abort's own verdict, exit 1 for a mismatch, not exit 2; this leg pins the window boundary and makes no abort claim of its own, so the class docstring's ack/feed-count rule does not apply to it; `time.monotonic` is faked for the same two calls as the beyond-window leg.

    D-05 note. Directly above `READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0`, after the existing `# 202-04 D-08:` derivation paragraph, add a second `#` paragraph in the same style: 207.1 D-05 (202-REVIEW WR-01) -- the window is derived, not measured under host scheduling or USB latency, and it stays 3.0 s and non-configurable. Its one consumer is `_drive_region_compare`, so it governs every caller: verify, blank, the write blank guard and write --verify. A deliberate stop whose frame arrives later than this -- extreme host load, a stalled USB stack -- is reported as exit 2, a hardware or transport failure, instead of the abort's own verdict. That false exit 2 is a known, accepted outcome: for a report of "verify says hardware error but the chip only mismatches", check host load before suspecting the hardware. On the write-guard path a false 2 refuses the write as a guard-read failure, which is fail-closed. `TestVerifyEpromReadAbort` pins both sides of the boundary. Do not name the constant inside the new paragraph, and do not change the value, the consumer or anything that reads configuration.

    D-10 docstring. In `EpromOperator.lease()`'s "What a lease removes that is not only time" paragraph, replace only the sentence that states an unmeasured count of about thirty calls and thirty board resets. The replacement says, at the docstring's existing wrap width: a lease removes the board reset for every `EpromOperator` call inside the block -- on the order of twenty in a default `dev test` plan, a derived count and not a measured one (206-SESSION-COST.md § 3: at most about 17-20 of a plan's roughly 30 connects are `EpromOperator`'s own; `HardwareManager`'s connects stay outside the lease, 206 D-05); the measured wall-clock saving is 15.1 % (40.709 s of a 268.992 s cold-arm median, N=3 per arm, W27C512 on a Leonardo; 206-SESSION-COST.md § 5). Keep the sentences before and after it (the DTR reset, and the fidelity risk SESS-02 measured) unchanged.

    Run `.venv311/bin/ruff format` and `.venv311/bin/ruff check` on both files. Commit inside /workspaces/firestarter_app with subject `test(207.1-02): D-05 pin the read-abort window boundary; D-10 cite the measured lease saving`. Do not push. Do not move the folded todo here; Plan 03 moves both folded todos after the full gate.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -m pytest "tests/test_eprom_operations.py::TestVerifyEpromReadAbort::test_timeout_just_inside_the_acceptance_window_is_the_intended_abort" "tests/test_eprom_operations.py::TestVerifyEpromReadAbort::test_timeout_exactly_at_the_acceptance_window_is_the_intended_abort" "tests/test_eprom_operations.py::TestVerifyEpromReadAbort::test_timeout_outside_the_acceptance_window_returns_two" tests/test_eprom_operations.py -o addopts="" -p no:cacheprovider -q 2>&1 | tail -1 | /usr/bin/grep -E -e '^[0-9]+ passed in '</automated>
    <fails_when>no output line (grep exit 1): the summary reads "N failed", "error" or "not found" instead of "N passed in"</fails_when>
    <automated>cd /workspaces/firestarter_app && .venv311/bin/python -c "import firestarter.eprom_operations as m; d=m.EpromOperator.lease.__doc__ or ''; assert m.READ_ABORT_ACCEPTANCE_WINDOW_S == 3.0; assert '15.1' in d and '206-SESSION-COST.md' in d and 'on the order of' in d, d[-900:]; print('OK')" && /usr/bin/grep -qxF -e 'READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0' firestarter/eprom_operations.py && [ "$(/usr/bin/grep -h -F -e 'READ_ABORT_ACCEPTANCE_WINDOW_S' firestarter/*.py | /usr/bin/grep -vcE -e '^\s*#')" = "2" ] && [ "$(/usr/bin/grep -cF -e 'roughly thirty' firestarter/eprom_operations.py)" = "0" ] && /usr/bin/grep -qF -e '207.1 D-05' firestarter/eprom_operations.py && /usr/bin/grep -qF -e 'fail-closed' firestarter/eprom_operations.py && .venv311/bin/ruff check firestarter/eprom_operations.py tests/test_eprom_operations.py && .venv311/bin/ruff format --check firestarter/eprom_operations.py tests/test_eprom_operations.py && S=$(git log --format=%s 64da8f6..HEAD) && printf '%s\n' "$S" | /usr/bin/grep -qF -e '(207.1-02): D-05' && [ -z "$(git status --porcelain --untracked-files=no)" ] && [ "$(git rev-parse --abbrev-ref HEAD)" = "v1.41-verification-to-host" ] && echo PASS-207.1-02-T2</automated>
    <fails_when>PASS-207.1-02-T2 is not printed: an AssertionError traceback (constant changed, or the lease docstring lacks 15.1 / the source file / "on the order of"), the constant line not present exactly once, a non-comment reference count other than 2 (a new consumer or knob), the thirty claim still present, the D-05 note missing, a ruff finding, no D-05 commit, tracked changes left, or the branch moved</fails_when>
  </verify>
  <acceptance_criteria>
    - Both new legs exist with the exact names above and pass; `test_timeout_outside_the_acceptance_window_returns_two` is byte-unchanged and still passes.
    - `READ_ABORT_ACCEPTANCE_WINDOW_S = 3.0` occurs once; the non-comment references to the constant across `firestarter/*.py` are exactly 2 (the definition and the `_drive_region_compare` consumer).
    - The note above the constant contains `207.1 D-05`, names verify, blank, the write blank guard and write --verify, and says the guard path is fail-closed.
    - `EpromOperator.lease.__doc__` contains `on the order of`, `15.1` and `206-SESSION-COST.md`, and the file no longer contains the thirty claim.
    - A commit whose subject carries `(207.1-02): D-05` exists; the tracked app tree is clean; nothing is pushed.
  </acceptance_criteria>
  <done>The read-abort window is pinned from inside and at its edge, the accepted false exit 2 is written at the constant, and the lease docstring cites the measured saving instead of an unmeasured count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host transport -> firmware | `setup_command` sends the operation's command frame; a send on a dead link is the precondition D-11 asserts |
| firmware timing -> host verdict | the arrival time of `MSG_ERR_TIMEOUT` relative to the host's stop decides verdict 1 vs 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-207.1-05 | Denial of Service / Repudiation | `SerialCommunicator.setup_command` assert changes the exception class on the transport path | medium | mitigate | Both production callers pre-check the link (`_probe_port` constructs the communicator; the leased branch guards with `self.comm.is_connected()`), so no runtime change. `_probe_port`'s `except Exception` logs and returns `None`, so a bad port cannot abort the walk. The docstring states AssertionError and `python -O`. The full suite gate in Task 1 proves no other caller breaks. |
| T-207.1-06 | Tampering (a harness masks a real regression) | the `is_connected` patch in three probe harnesses | low | mitigate | The patch sits only in the three harnesses that already replace `__init__` with a no-op (so no real link exists there); the new positive test proves the assert fires on a real not-connected communicator. |
| T-207.1-07 | Repudiation (a misleading exit 2) | the D-05 accepted-outcome note | low | accept | Record-only change: the window value, the `<=` comparison and the consumer are unchanged. A false 2 on the guard path refuses the write, which is fail-closed, and the note says so. |
| T-207.1-08 | Repudiation (overstated claim) | the D-10 lease docstring | low | mitigate | The new sentence labels the count as derived and cites the measured 15.1 % with its source section, so no unmeasured number is presented as a measurement. |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && .venv311/bin/ruff check firestarter/ tests/ && .venv311/bin/ruff format --check firestarter/ tests/` is clean.
- Task 1's full-suite leg exits 0 with at least 2371 passed and `36 snapshots passed.`.
- `.venv311/bin/python -m pytest --collect-only -q -o addopts="" -p no:cacheprovider tests/test_serial_comm.py tests/test_eprom_operations.py` lists the three new tests.
</verification>

<success_criteria>
- 206 IN-02 (D-11), 202 WR-01 (D-05) and 206 IN-01 (D-10) are fixed.
- The window stays 3.0 s and non-configurable; no production call path changes behaviour.
- All commits are on `firestarter_app` `v1.41-verification-to-host`; nothing is pushed.
</success_criteria>

<output>
Create `/workspaces/.planning/phases/207.1-address-v1-41-tech-debt/207.1-02-SUMMARY.md` when done. Record every commit sha, the D-11 RED evidence, the intermediate three-file summary line (expected `12 failed`), and the full-suite count.
</output>
