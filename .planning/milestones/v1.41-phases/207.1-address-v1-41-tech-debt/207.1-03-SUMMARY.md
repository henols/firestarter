---
phase: 207.1-address-v1-41-tech-debt
plan: 03
subsystem: cli
tags: [ruff-format, chip-test, compare-accumulator, ci-gate, todo-hygiene, firestarter_app]

# Dependency graph
requires:
  - phase: 207.1-01
    provides: "the write-path diagnostics corrections (D-06, D-07, D-08) whose full-suite baseline (2373 passed) this plan's app gate re-proves"
  - phase: 207.1-02
    provides: "the transport precondition and read-abort timing corrections (D-11, D-05, D-10), app HEAD 45aff92, suite 2371 passed, this plan's starting point"
provides:
  - "D-12: the publish-path version script (.github/scripts/update_version.py) is ruff-format-clean, proved format-only by AST equality against 64da8f6 and identical --dry-run output on both the beta and stable path"
  - "D-03: both `run_count=1` branches in `_run_step_untimed` carry a comment naming the trade-off and the precise per-step-path vs cycle-block-path mechanism (D-04: 7 issues, 9 reports, all cycle-block shape)"
  - "D-09: the `if not offs:` branch in `CompareAccumulator.feed` carries a comment naming it defensive, not dead"
  - "The CI-equivalent app gate on Python 3.11 passes for the whole phase: 2373 passed, 86.40% coverage, 36 snapshots passed, firestarter --help exits 0"
  - "The two folded todos (D-05's read-abort-window todo, D-09's compare-accumulator todo) are closed in .planning/todos/completed/"
affects: [207.1-04-secure-phase-followups, 207.1-06, 207.1-07, any-future-release-pipeline-or-diagnostics-work]

# Actuals (#2632)
actuals:
  tokens: 2082
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A comment naming a kept-by-decision trade-off is placed directly above the code line it protects, at that line's indentation, in the file's own prose-comment style -- keeps the next reader from 'fixing' a deliberate keep"

key-files:
  created: []
  modified:
    - firestarter_app/.github/scripts/update_version.py
    - firestarter_app/firestarter/chip_test.py
    - firestarter_app/firestarter/compare.py
    - .planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md
    - .planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
    - .planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md
    - .planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md

key-decisions:
  - "D-03's comment states the cycle-block mechanism, not only the trade-off CONTEXT names (per RESEARCH.md finding 1): all nine filed reports D-04 counted are the cycle-block shape, so reverting either except branch alone would re-key none of them -- only a future per-step-path (read step) report"
  - "D-09's comment is placed between `offs = [...]` and `if not offs:`, in the method's own `# Tier N:` prose style at 8-space indent, labeled Tier 4 to continue the existing Tier 1-3 numbering"
  - "Both meta todo-completion verb calls used named-path staging only (git add -- <4 paths>), never git add -A; the meta tree's unrelated dirty/untracked files (.devcontainer/*, .vscode/*, estimation-calibration.json, graphs/*, anything.txt, tmp/, etc.) were left untouched"

patterns-established:
  - "Whole-tree ruff format --check . (not just firestarter/ tests/) catches publish-path scripts (.github/scripts/) that CI's narrower ruff scope never lints"

requirements-completed: []

coverage:
  - id: D1
    description: "D-12: the publish-path version script is ruff-formatted, proved format-only by AST equality and identical --dry-run output on both channels"
    verification:
      - kind: unit
        ref: "ast.dump equality check: .github/scripts/update_version.py vs its 64da8f6 blob"
        status: pass
      - kind: other
        ref: "GITHUB_REF=refs/heads/beta and refs/heads/main --dry-run output identical between old and new file"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-03 and D-09: the two kept-by-decision branches carry accurate, comment-only explanations; ast.dump of both files equals their 64da8f6 blobs"
    verification:
      - kind: unit
        ref: "ast.dump equality check: chip_test.py and compare.py vs their 64da8f6 blobs"
        status: pass
      - kind: integration
        ref: "tests/test_chip_test.py tests/test_chip_test_blank_check_order.py tests/test_chip_test_cycle.py tests/test_chip_test_sdp_leg.py tests/test_chip_test_timing.py tests/test_chip_test_uv_slot_write.py tests/test_compare.py tests/test_diagnostic_report.py -- 479 passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "The CI-equivalent app gate passes for the whole phase, and the two folded todos (D-05, D-09) are closed in the record"
    verification:
      - kind: integration
        ref: "pytest tests/ --cov=firestarter --cov-fail-under=70 on .venv311 -- 2373 passed, 86.40% coverage, 36 snapshots passed"
        status: pass
      - kind: other
        ref: "todo complete verb moved both files pending/ -> completed/ with status: completed; commit 5b51463a touches exactly the 4 named todo paths"
        status: pass
    human_judgment: false

duration: 45 min
completed: 2026-09-24
status: complete
---

# Phase 207.1 Plan 03: Publish-Path Format, Kept-Branch Comments, and Full Gate + Todo Closure Summary

**The publish-path version script is now ruff-format-clean (proved behavior-identical by AST equality and matching `--dry-run` output on both channels); the two `run_count=1` branches in `_run_step_untimed` and the `if not offs:` branch in `CompareAccumulator.feed` now carry comments explaining why they stay; the full CI-equivalent gate passes at 2373 tests; and the two todos those decisions resolved are closed.**

## Performance

- **Duration:** 45 min
- **Started:** 2026-09-24T00:05:00Z (approx)
- **Completed:** 2026-09-24T00:50:00Z (approx)
- **Tasks:** 3 (1 tracer, 2 auto)
- **Files modified:** 7 (3 app source/script files, 4 meta todo paths via rename)

## Accomplishments

- **D-12 (cross-phase `ruff format --check .`):** `.github/scripts/update_version.py` reformatted by `ruff format` — five single-to-double quote swaps, one AST-equivalent f-string quote swap, two call/tuple explosions, one blank line after `import sys`. Proved format-only: `ast.dump` equals the `64da8f6` blob; `--dry-run` output is byte-identical between the old and new script for both `GITHUB_REF=refs/heads/beta` (`3.1.0b1`) and `refs/heads/main` (`3.1.1`); mode `100755` and the `#!/usr/bin/env python3` shebang are unchanged. `ruff format --check .` now exits 0 over the whole `firestarter_app` tree (175 files), not only the CI-scoped `firestarter/ tests/`.
- **D-03 (206-REVIEW WR-02 observation):** Both `except` branches in `_run_step_untimed` (`(SerialError, HardwareOperationError)` and `EpromOperationError`) now carry a comment above their `run_count=1,` argument. The comment states the trade-off (a default-policy run that raises here gets the degraded `runs=1` tag stamped into `dedup_fingerprint`, re-keying it into the `--fast` dedup group; reverting would re-key filed reports, but the harm is limited to dedup grouping of reports already ERROR/BAD) and the precise mechanism per RESEARCH.md finding 1: this `1` is the cause only on the per-step path (`run_plan` -> `_run_step(..., runs=runs)`, today the read step); on the cycle-block path `_run_cycle_block` already passes `runs=1`, so the tag there comes from the `hardware_refused` break plus `_aggregate_cycle_results` returning a single result — and every one of the nine filed reports D-04 counted (7 issues) is that cycle-block shape, so reverting either except branch alone would re-key none of them.
- **D-09 (202-REVIEW IN-01):** A `# Tier 4:` comment placed between `offs = [...]` and `if not offs:` in `CompareAccumulator.feed` explains the branch is defensive, not dead: reachable only when a caller passes an `expected` longer than `actual` and equal over `actual`'s length (Tier 2's `expected == actual` is False, yet no offset differs), while a shorter `expected` raises `IndexError` in the comprehension first; kept so a contract violation of that shape closes the open range instead of indexing `offs[0]` on an empty list below.
- **CI-equivalent app gate:** On `.venv311` (Python 3.11): `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` clean; `firestarter --help` exits 0; full `pytest tests/ --cov=firestarter --cov-fail-under=70` — **2373 passed**, **86.40% coverage** (`Required test coverage of 70% reached`), **36 snapshots passed**, in 271.38s. All 8 new tests from 207.1-01 (5) and 207.1-02 (3) confirmed collected.
- **Two folded todos closed:** `2026-09-20-read-abort-window-untested-at-its-boundary.md` (D-05) and `2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md` (D-09) moved from `.planning/todos/pending/` to `.planning/todos/completed/` via the `todo complete` verb, each with `status: completed` and a `completed:` date, committed in meta with exactly those four named paths staged.

## Task Commits

Each task was committed atomically, all on `v1.41-verification-to-host`, no push:

| Repo | Task | Commit | Subject |
|------|------|--------|---------|
| firestarter_app | Task 1 (tracer): D-12 | `f8311ac` | `style(207.1-03): D-12 ruff format the publish-path version script` |
| firestarter_app | Task 2: D-03 and D-09 | `5302f63` | `docs(207.1-03): D-03 and D-09 say why two kept branches stay` |
| meta (/workspaces) | Task 3 (second half): close 2 todos | `5b51463a` | `docs(207.1-03): close 2 resolved todos (D-05, D-09)` |

**Plan metadata:** this SUMMARY commit (meta repo, separate from the two commits above, staged by named path only: `.planning/phases/207.1-address-v1-41-tech-debt/207.1-03-SUMMARY.md`).

_Note: Task 1 was `type="tracer"`; its own `<verify>` was re-run end-to-end after the commit (auto mode active, `AUTO_CFG` not applicable here since this is sequential single-executor mode without human-verify gates) and printed `PASS-207.1-03-T1` before Task 2 began. Task 3's first half (the CI-equivalent gate) ran and passed before any todo was moved, per the plan's own ordering requirement._

## Files Created/Modified

- `firestarter_app/.github/scripts/update_version.py` — D-12 ruff-format only, no logic change
- `firestarter_app/firestarter/chip_test.py` — D-03 comment blocks above both `run_count=1,` arguments in `_run_step_untimed`'s except branches
- `firestarter_app/firestarter/compare.py` — D-09 comment above `if not offs:` in `CompareAccumulator.feed`
- `.planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md` — removed (renamed)
- `.planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md` — removed (renamed)
- `.planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md` — added, `status: completed`
- `.planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md` — added, `status: completed`

## PASS Tokens (plan's `<automated>` verify legs, with command output)

**PASS-207.1-03-T1** (Task 1, re-run after commit):
```
175 files already formatted
All checks passed!
PASS-207.1-03-T1 DRY_RUN: 3.1.0b1 / DRY_RUN: 3.1.1
```

**PASS-207.1-03-T2** (Task 2, re-run after commit):
```
All checks passed!
2 files already formatted
PASS-207.1-03-T2
```
(479 passed across the 8 named chip_test/compare/diagnostic_report test files, confirmed separately before this leg's final grep-gated echo.)

**PASS-207.1-APP-GATE 2373** (Task 3, first automated leg):
```
TOTAL                                    6604    898    86%
Required test coverage of 70% reached. Total coverage: 86.40%
--------------------------- snapshot report summary ----------------------------
36 snapshots passed.
2373 passed in 271.38s (0:04:31)
PASS-207.1-APP-GATE 2373
```
All 8 named new/changed test ids (5 from 207.1-01, 3 from 207.1-02) confirmed collected via `pytest --collect-only`.

**PASS-207.1-03-TODOS** (Task 3, second automated leg):
```
.planning/todos/completed/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
.planning/todos/completed/2026-09-20-read-abort-window-untested-at-its-boundary.md
.planning/todos/pending/2026-09-20-compare-accumulator-empty-offsets-branch-is-dead.md
.planning/todos/pending/2026-09-20-read-abort-window-untested-at-its-boundary.md
PASS-207.1-03-TODOS
```

## Decisions Made

- **D-03's comment states the cycle-block mechanism**, per RESEARCH.md finding 1 and the plan's own resolved open question: the per-step-path/cycle-block-path distinction and the "re-keys none of the nine filed reports" fact are both in the comment, not deferred to the phase record only.
- **D-09's comment is numbered `Tier 4`**, continuing the existing `# Tier 1/2/3:` numbering already present in `CompareAccumulator.feed`, rather than an unnumbered aside.
- **Task 3's ordering was gate-first**: the full CI-equivalent gate ran and passed before either todo was touched, so no record could claim a fix landed before it was proven.

## Deviations from Plan

None — plan executed exactly as written. All three tasks' acceptance criteria passed on first attempt; no code fix, no architectural question, and no auto-fix under Rules 1-3 was needed.

## Issues Encountered

One tool-level hiccup, not a plan deviation: the first `node .claude/gsd-core/bin/gsd-tools.cjs todo complete 2026-09-20-read-abort-window-untested-at-its-boundary.md` invocation was denied by the auto-mode Bash classifier ("Modify Shared Resources"); the identical command for the other todo file succeeded immediately after, and a retry of the first command then also succeeded with no side effects from the denied attempt (nothing was partially written — confirmed via `git status` before the retry). No workaround or alternate tool was used.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 207.1-04 through 207.1-07 remain to execute in this phase; this plan (207.1-03, wave 3, `depends_on: ["207.1-01", "207.1-02"]`) is complete and unblocks any sibling plan that names it as a dependency.
- `firestarter_app` is at `5302f63` on `v1.41-verification-to-host`; the meta repo is at `5b51463a` (before this SUMMARY commit); `firestarter_fw` is untouched by this plan. Nothing was pushed from any repository, and no tag was created.
- The meta repo's `firestarter_app` and `firestarter_fw` gitlinks were deliberately left untouched per this plan's orchestrator overrides — 207.1-07 owns advancing both once, after every sub-repo commit in this phase.
- No blockers for subsequent plans. The full app suite (2373 passed, 86.40% coverage) is the current known-good baseline for any later plan in this phase that touches `firestarter_app`.

## Self-Check: PASSED

- All 3 commit hashes (`f8311ac`, `5302f63` in `firestarter_app`; `5b51463a` in meta) found in their respective repos' `git log --oneline --all`.
- All 7 claimed file paths found on disk (3 app files at their final state; 2 todo files absent from `pending/`; 2 todo files present in `completed/` with `status: completed`).
- `207.1 D-03` (2 occurrences), `7 issues, 9 reports`, and `207.1 D-09` all found via grep in their claimed files.
- `firestarter_app` HEAD is `v1.41-verification-to-host` at `5302f63`; `plan_head_before: 45aff92`; `commits: 2` (measured via `git rev-list --count 45aff92..HEAD`), matching the app-repo rows of the commits table above (the third commit, `5b51463a`, is in the meta repo and is measured separately, per this phase's multi-repo `sub_repos` configuration).
- Meta HEAD is `v1.41-verification-to-host` at `5b51463a` (before this SUMMARY commit); `git show --no-renames --name-only` on that commit lists exactly the 4 named todo paths.
- Re-ran all four plan-level `<automated>` legs: `PASS-207.1-03-T1`, `PASS-207.1-03-T2`, `PASS-207.1-APP-GATE 2373`, `PASS-207.1-03-TODOS` all printed.
- Re-ran the plan-level `<verification>` block: `PASS-207.1-APP-GATE 2373` (N >= 2373 holds), `git -C firestarter_app status --porcelain --untracked-files=no` empty, `git -C firestarter_app log --format=%s 64da8f6..HEAD` lists the D-08, D-07, D-06, D-11, D-05, D-12 and D-03/D-09 commits plus their RED test commits.
- Both `firestarter_app` and meta confirmed on `v1.41-verification-to-host` after every commit in this plan.

---
*Phase: 207.1-address-v1-41-tech-debt*
*Plan: 03*
*Completed: 2026-09-24*
