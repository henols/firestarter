---
phase: 204-the-command-surfaces-leave-the-firmware
plan: 04
type: execute
wave: 4
depends_on: [204-03]
files_modified:
  - tools/catalog/messages.toml
  - firestarter_fw/CLAUDE.md
  - firestarter_fw/PROTOCOLS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FWCMD-02, FWCMD-03]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`DBG_VERIFY_PROM` and `DBG_BLANK_CHECK_PROM` keep their `tools/catalog/messages.toml` entries and each gains a comment recording that its only emit site was retired in `3.1.0` and that the id is not free for reuse (D-06, FWCMD-02)."
    - "Adding those comments changes no generated output: regenerating the firmware header and the host catalog module from the amended catalog produces files byte-identical to the ones both sub-repos already carry, so Phase 204 stays a two-repo phase and no sub-repo sync is triggered (D-06)."
    - "The catalog validator passes against the amended catalog."
    - "Every firmware documentation citation the sweep staled is repaired: the dispatch-summary sentence in `firestarter_fw/CLAUDE.md` that names a retired ordinal among the three the configure switch assigns; the same file's claim that registering a native suite costs four lines when it costs two; and the three `PROTOCOLS.md` sentences that name a retired ordinal as a live command surface (FWCMD-03)."
    - "`PROTOCOLS.md`'s INV-05 row no longer cites a test module that does not exist anywhere in the tree — a pre-existing stale citation repaired while the row was being touched anyway."
    - "CMP-F1 in `.planning/REQUIREMENTS.md` records the measured finding that pre-`3.1.0` firmware already parses the clean-stop message into its own message enumeration and its acknowledgement wait already ignores it, so a future host that sends it degrades to exactly today's one-second timeout path and the change is forward-compatible by construction (D-12)."
    - "The ROADMAP paragraph that nominates this phase by name as the home for the clean-stop change is corrected to record that it was offered here and declined, and why, so a later reader does not re-derive the nomination (D-12)."
    - "Nothing in this plan regenerates or hand-edits a generated catalog artifact inside either sub-repo."
  artifacts:
    - tools/catalog/messages.toml
    - firestarter_fw/PROTOCOLS.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "`tools/catalog/messages.toml` is the single source of truth for message ids; codegen runs in the meta repository only and both sub-repos consume synced artifacts. A catalog change that altered generated output would turn Phase 204 into a three-repo lockstep commit set."
    - "The codegen emitter has a stated determinism contract — sorted by id, no timestamps, fixed line endings and hex casing — which is what makes a byte-identical regeneration a meaningful proof rather than a coincidence."
  prohibitions:
    - statement: "Never regenerate or hand-edit a generated catalog artifact inside a sub-repo. Message ids are generated in the meta repository only, and both sub-repos consume synced copies."
      requirement_id: FWCMD-02
      category: integrity
    - statement: "Ordinals 4 and 6, and the two orphaned debug ids, are never reused for any new command, flag or message on any ladder — the ids cost nothing to keep and reusing one makes a stale host or a stale log reader silently wrong."
      requirement_id: FWCMD-03
      category: safety
    - statement: "A repaired citation names something that exists. Never replace one stale reference with another unverified one — check that the file, function or test being cited is present in the tree before citing it."
      requirement_id: FWCMD-03
      category: transparency
    - statement: "No push to `beta` in any of the three repositories during this phase. A push to `beta` in either sub-repo cuts a release, the app's reaches PyPI, and a PyPI version can never be reused."
      requirement_id: FWCMD-03
      category: safety
---

<objective>
Close the record the sweep leaves behind: two orphaned message ids that are deliberately kept, five
documentation sentences that became false, and one deferred finding that would otherwise be
re-derived from scratch by the next author who reads the roadmap.

Purpose: plans 01 and 03 removed the command surfaces. Three consequences of that removal sit outside
both sub-repos' source trees and none of them is self-evident. Two catalog debug ids now have no emit
site at all, and the operator's decision (D-06) is to KEEP them rather than retire them — which only
works if the reason is written down where someone reaching for a free id will see it, and only stays
cheap if the comments provably change no generated output. Five documentation sentences in the
firmware repository describe command surfaces that no longer exist, and this project's standing rule
is to repair a stale citation rather than accept it. And the roadmap currently nominates this phase by
name as the home for a clean-stop firmware change that D-12 declined; leaving that nomination
standing guarantees the next author re-derives the same analysis.

None of this is optional record-keeping. The catalog comments are what make the kept ids safe, the
documentation repairs are what stop a future reader implementing against a retired surface, and the
CMP-F1 finding is the only place the forward-compatibility argument is written down.

Output: two commented catalog entries with a proof that codegen output is unchanged, five repaired
firmware documentation citations, and a CMP-F1 entry plus a corrected roadmap paragraph recording the
deferral and the measurement behind it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/204-the-command-surfaces-leave-the-firmware/204-CONTEXT.md
@.planning/phases/204-the-command-surfaces-leave-the-firmware/204-RESEARCH.md
@.planning/phases/204-the-command-surfaces-leave-the-firmware/204-PATTERNS.md
@.planning/phases/204-the-command-surfaces-leave-the-firmware/204-03-SUMMARY.md
@.planning/phases/202-one-comparison-engine-on-the-host/202-READ-ABORT-ANSWER.md
</context>

<environment_facts_measured_at_plan_time>
Measured on 2026-09-21.

- **The catalog is at `/workspaces/tools/catalog/messages.toml`, in the meta repository**, and it is
  git-tracked. Its generator is `/workspaces/tools/catalog/codegen.py`, and a sync script sits beside
  them. The generator's own module docstring states a determinism contract: messages sorted by id
  ascending, no timestamps or hostnames in the banner, fixed line endings, and upper-case two-digit
  hexadecimal literals. That contract is what makes a byte-identical regeneration a proof.
- **The generator's interface:** it takes a catalog path, an output path and a language of either the
  C++ header form or the Python module form, and it has a validate-only mode that runs the full
  catalog validator and writes nothing.
- **The two orphaned ids** are the debug entries at `0x08` and `0x0B` in the catalog's debug message
  table. Their generated forms live at `firestarter_fw/include/messages.h` and
  `firestarter_app/firestarter/messages.py`, both of which exist and are both synced artifacts.
- **Five stale firmware documentation sites**, each read verbatim this session:
  - `firestarter_fw/CLAUDE.md` line 88 — a sentence describing the protocol dispatch summary that
    names three ordinals the configure switch assigns a main operation for, one of which is retired.
  - `firestarter_fw/CLAUDE.md` line 455 — a claim that a new native suite must appear in BOTH
    environments' filter and include lists, "which is four new lines". Measured: the PlatformIO
    config defines each list once in the shared base section and both environments extend it, so it
    is two lines.
  - `firestarter_fw/PROTOCOLS.md` line 180 — a VPP-behaviour sentence stating that VPP is not enabled
    for the read or blank-check operations.
  - `firestarter_fw/PROTOCOLS.md` line 321 — an erase-model paragraph ending with a clause that the
    blank command remains available as its own independent command through an unaffected handler arm.
    That arm is one of the five plan 03 deleted.
  - `firestarter_fw/PROTOCOLS.md` line 549 — the INV-05 invariant row, which both names a retired
    ordinal AND cites a test named `test_inv05_eprom_vpp_skip_on_read`. **A tree-wide search for that
    name and for the string `inv05` returns nothing at all** — the cited test does not exist under
    that name anywhere in `test/`. That is a pre-existing stale citation, independent of this phase.
- **`.planning/ROADMAP.md` lines 234-235** carry a sentence naming "phase 204" as the home for a
  one-line firmware clean-stop change, described as a cheaper route to CMP-F1 than the abort it
  originally assumed. D-12 declined it.
- **`.planning/REQUIREMENTS.md` line 113** is the CMP-F1 row in the future-requirements table.
- **The measured forward-compatibility facts behind D-12:** the firmware's message parser already maps
  the clean-stop token to its own enumeration value, and the acknowledgement wait already returns only
  on the acknowledge and error values — the clean-stop value falls through to the retry delay and
  loops to the one-second timeout. So a future host that sends it to pre-`3.1.0` firmware degrades to
  exactly today's timeout path.
</environment_facts_measured_at_plan_time>

<design_forks_resolved>
**Fork A — the codegen no-diff proof regenerates to a temporary path and compares, rather than
regenerating in place and checking git porcelain.** Regenerating in place would leave the sub-repo
working trees dirty if the comments DID change output, which is the failure case, and a dirty sub-repo
tree is exactly what the whole-repo-porcelain gate in the firmware test suite trips on. Compare
against a temporary file and never write into either sub-repo.

**Fork B — the INV-05 citation is repaired by naming what actually covers the invariant, or by
marking the row as uncovered if nothing does.** The cited test does not exist. Replacing one
unverified name with another would be the same defect again. Search the eprom validation suite for a
test that actually asserts the VPP-skip behaviour on the read path; if one exists, cite it by its real
name; if none does, say so in the row rather than inventing a name — an honestly uncovered invariant
is information, a fabricated citation is not.

**Fork C — the roadmap paragraph is CORRECTED IN PLACE, not deleted.** The nomination is part of the
record of how the milestone was reasoned about, and deleting it would make the next reader wonder
whether it was ever considered. It gains a clause recording that it was offered at Phase 204 and
declined, and the one-line reason: the firmware half is one line but the host half is not, and the
host surface it would rework is the one Phase 202 had just built.
</design_forks_resolved>

<probe_disposition>
This plan owns no unresolved rows of the spec-less probe ledger; the full ledger is in
`204-01-PLAN.md`. The two requirements it advances, FWCMD-02 and FWCMD-03, have their flagged
assumptions recorded in `204-03-PLAN.md`, which is where the substance of both lands.

**Flagged assumption (A6 from RESEARCH).** The claim that a comment added to the catalog cannot
change generated output is reasoned from the generator's determinism contract, not observed — no
codegen run was made during research. Task 1 settles it by regenerating and comparing. If the
comments DO change output, that is a finding, not a workaround target: Phase 204 would become a
three-repo lockstep commit set, which is exactly what D-06 exists to avoid, and the right response is
to surface it rather than to sync the artifacts quietly.

**Canon-referral breadcrumb (prohibition probe).** Supply-chain integrity of the codegen toolchain is
canon territory and is not minted as a bespoke prohibition here; the relevant control is that codegen
runs in one repository and its outputs are synced rather than re-derived.
</probe_disposition>

<artifacts_this_phase_produces>
This plan creates no code symbols. Its artifacts are records:

- Two new comment blocks in `tools/catalog/messages.toml`, attached to the debug entries at `0x08`
  and `0x0B`.
- A corrected CMP-F1 row in `.planning/REQUIREMENTS.md` carrying the forward-compatibility finding.
- A corrected paragraph in `.planning/ROADMAP.md` recording that the clean-stop change was offered at
  Phase 204 and declined.
- Five repaired citations across `firestarter_fw/CLAUDE.md` and `firestarter_fw/PROTOCOLS.md`.

No new file is created by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Comment the two orphaned debug ids, and prove codegen output is unchanged</name>
  <reversibility rating="reversible">A catalog comment is documentation, not a contract; removing it later costs one edit and breaks nothing.</reversibility>
  <precondition>Plan 03's commits are on the branch, so both wrappers that carried the two debug emit sites are already deleted.</precondition>
  <files>tools/catalog/messages.toml</files>
  <read_first>
    - `/workspaces/tools/catalog/messages.toml` — the debug message table around the entries at `0x08` and `0x0B`, and any existing comment in the file that explains why an id is kept or reserved. Match the file's own comment convention rather than inventing one.
    - `/workspaces/tools/catalog/codegen.py` — its module docstring in full, especially the determinism contract and the note about a third emitter that was dropped, and its argument parser, so the regeneration command is right.
    - `/workspaces/tools/catalog/sync_to_subrepos.sh` — read it to understand what a sync would do, so the SUMMARY can state plainly that none was run.
    - `/workspaces/CLAUDE.md` § "Cross-repo obligations" — the paragraph stating that messages are generated in the meta repo only and that neither sub-repo may regenerate or hand-edit the generated artifacts.
    - `204-CONTEXT.md` D-06, for the operator's reasoning about keeping the ids and keeping 204 a two-repo phase.
    - `firestarter_fw/include/messages.h` and `firestarter_app/firestarter/messages.py` — enough of each to see their shape, since both are the comparison targets.
  </read_first>
  <behavior>
    - The catalog still defines both debug ids, with the same numeric values and the same names and format strings as before.
    - Regenerating the C++ header form from the amended catalog produces a file byte-identical to `firestarter_fw/include/messages.h`.
    - Regenerating the Python module form from the amended catalog produces a file byte-identical to `firestarter_app/firestarter/messages.py`.
    - The catalog validator exits zero against the amended catalog.
  </behavior>
  <action>
Add a comment to each of the two orphaned debug entries and prove the addition is inert to the
generator.

**The comments.** Both ids exist solely for debug lines inside the two operation wrappers plan 01 and
plan 03 deleted, and nothing else in either repository uses them. Per D-06 they are KEPT. Each comment
states: that its only emit site was the corresponding operation wrapper, that the wrapper and its
command surface were retired in `3.1.0`, that the id is deliberately retained rather than recycled,
and why retaining it costs nothing — the debug logging entry point is id-only, so no format string
reaches firmware flash, and the whole premise of this milestone is reclaiming flash. Say plainly that
the number is not free for reuse: a log reader or a replayed capture from an older firmware would be
silently misinterpreted if the id were reassigned. Match the file's own comment convention.

Do NOT change either entry's id, name, format string or parameter list. Do NOT delete either entry.

**The proof, which is the real work here.** Run the generator against the amended catalog twice, once
for each language, writing to temporary paths OUTSIDE both sub-repositories, and compare each output
byte-for-byte against the artifact the corresponding sub-repo already carries. Both comparisons must
show no difference. Write to a temporary path, never in place: if the comments DID change output, an
in-place regeneration would leave a sub-repo working tree dirty, and the firmware test suite carries a
gate that asserts whole-repo porcelain.

Also run the generator's validate-only mode against the amended catalog.

If either comparison shows a difference, STOP and record it. That result would mean Phase 204 becomes
a three-repo lockstep commit set — meta codegen plus a sync into both sub-repos — which is precisely
what D-06 exists to avoid, and the decision about whether to accept that belongs to the operator, not
to a workaround. Do not run the sync script under any circumstance in this task.

Commit the catalog alone. State in the SUMMARY that both regenerated outputs were byte-identical,
that the validator passed, and that no sync was run.
  </action>
  <acceptance_criteria>
    - `tools/catalog/messages.toml` still defines both debug ids with unchanged id, name, format and parameter list.
    - Each of the two entries carries a comment naming its retired emit site, the release that retired it, and the reason the id must not be reused.
    - Regenerating the C++ header form to a temporary path yields a file byte-identical to `firestarter_fw/include/messages.h`.
    - Regenerating the Python module form to a temporary path yields a file byte-identical to `firestarter_app/firestarter/messages.py`.
    - The catalog validator exits zero.
    - Neither sub-repository has any modified file after this task.
    - The sync script was not run; the SUMMARY says so explicitly.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check</automated>
    <fails_when>non-zero exit, meaning the catalog validator rejected the amended file — the validator runs unconditionally on every generation too, so a rejected catalog would break every future codegen run</fails_when>
    <automated>cd /workspaces && OUT=$(mktemp -d) && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --language cpp --target "$OUT/messages.h" && diff -u firestarter_fw/include/messages.h "$OUT/messages.h" && echo "cpp output byte-identical"</automated>
    <fails_when>non-zero exit, or `diff` prints any hunk — any difference means the comment changed generated output and Phase 204 would become a three-repo commit set, which must be surfaced rather than absorbed</fails_when>
    <automated>cd /workspaces && OUT=$(mktemp -d) && python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --language python --target "$OUT/messages.py" && diff -u firestarter_app/firestarter/messages.py "$OUT/messages.py" && echo "python output byte-identical"</automated>
    <fails_when>non-zero exit, or `diff` prints any hunk</fails_when>
    <automated>cd /workspaces && python3 -c "import pathlib,re; t=pathlib.Path('tools/catalog/messages.toml').read_text(); n1='DBG_VERIFY'+'_PROM'; n2='DBG_BLANK_CHECK'+'_PROM'; assert n1 in t and n2 in t, 'an orphaned debug id was deleted; D-06 keeps both'; import sys; blocks=[b for b in t.split('[[debug.messages]]') if n1 in b or n2 in b]; assert len(blocks)==2, 'expected two matching entries, found %d' % len(blocks); assert all('3.1.0' in b for b in blocks), 'an entry comment does not name the release that retired its emit site'; print('both orphaned ids kept and annotated')"</automated>
    <fails_when>non-zero exit (an AssertionError names whether an id was deleted, whether the entry count was wrong, or whether a comment omits the release), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces/firestarter_fw && git status --porcelain</automated>
    <fails_when>any output at all — a modified file in the firmware sub-repo means something regenerated or synced into it, which this task must never do</fails_when>
    <automated>cd /workspaces/firestarter_app && git status --porcelain</automated>
    <fails_when>any output at all — a modified file in the host sub-repo means something regenerated or synced into it, which this task must never do</fails_when>
  </verify>
  <done>Both orphaned debug ids are kept and carry a comment recording the retired emit site, the release and the never-reuse reason; both generated artifacts regenerate byte-identically from the amended catalog; the validator passes; neither sub-repo was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Repair every firmware documentation citation the sweep staled</name>
  <precondition>Plan 03's commits are on the branch, so every documentation claim being repaired is now genuinely false rather than merely about to become false.</precondition>
  <files>firestarter_fw/CLAUDE.md, firestarter_fw/PROTOCOLS.md</files>
  <read_first>
    - `firestarter_fw/CLAUDE.md` § the protocol-dispatch summary around line 88 in full, and § the native-test-registration paragraph around line 455 in full.
    - `firestarter_fw/platformio.ini` `[native_base]` and both native environment sections — the measured basis for the two-lines correction.
    - `firestarter_fw/PROTOCOLS.md` around line 180 — the whole VPP-behaviour paragraph, so the repair does not break the surrounding argument about the drop path and the jumper routing.
    - `firestarter_fw/PROTOCOLS.md` around line 321 — the whole erase-model paragraph. It is long and carries four decision ids; only its final clause about the blank command is stale.
    - `firestarter_fw/PROTOCOLS.md` around line 549 — the INV-05 row and the surrounding invariant table, so the repair matches the table's column conventions.
    - `firestarter_fw/test/native/avr/test_val_eprom/test_val_eprom.cpp` in full — search it for any test that actually asserts VPP is not engaged on the read path, so the INV-05 row can cite a real name or be honestly marked uncovered.
    - `firestarter_fw/src/proms/eprom.cpp` — the VPP-skip behaviour on the read path, so the repaired invariant statement describes what the firmware now actually does.
    - `204-RESEARCH.md` § "Firmware documentation (three more sites, not in CONTEXT)".
    - `/workspaces/CLAUDE.md` — the repair-citations rule and the statement that each sub-repo's own guidance file owns its build and test commands.
  </read_first>
  <behavior>
    - The dispatch-summary sentence names only ordinals the configure switch still assigns a main operation for.
    - The native-test-registration paragraph states the measured cost of two lines and names the shared base section as the reason.
    - The VPP-behaviour sentence describes VPP-skip in terms of the read path only.
    - The erase-model paragraph's final clause no longer claims the blank command is reachable through a firmware handler arm.
    - The INV-05 row names a test that exists, or states plainly that the invariant has no automated coverage.
  </behavior>
  <action>
Five repairs across two files in the firmware repository. Each one names something that no longer
exists; none of them is cosmetic, because each would lead a future reader to implement against a
retired surface.

**The dispatch summary.** The sentence lists three ordinals the configure switch assigns a main
operation for. One of them is retired. Correct the list to the ordinals that remain, and add the
clause that the retired command surfaces left the firmware in `3.1.0` with their ordinals reserved —
so a reader who remembers the old text knows what happened rather than assuming the file is wrong.

**The native-test-registration paragraph.** It claims a new suite must appear in both environments'
filter and include lists, "which is four new lines". Measured: the PlatformIO config defines the
filter once and the include list once, in the shared base section, and both environments extend it.
Correct the count to two and name the shared base section as the reason. Keep the paragraph's real
point — that the suite must run in both environments — and add the measured reason it does: they
share one filter, which is why a new suite reaches the environment that always runs in CI as well as
the one that runs only on pull requests.

**The VPP-behaviour sentence.** It says VPP is not enabled for the read or blank-check operations.
The blank-check ordinal no longer exists, so restate the property in terms of the read path alone, and
keep the cross-reference to the invariant row intact.

**The erase-model paragraph's final clause.** It ends by saying the blank command remains available as
its own independent command through an unaffected handler arm. That arm is one of the five plan 03
deleted. Replace the clause with the truth: the blank command is now a host-side read-and-compare and
sends no blank-check ordinal at all, and the firmware handler arm it used to reach was retired in
`3.1.0`. Leave the rest of that long paragraph, including its four decision ids and its honesty
statement, exactly as it is.

**The INV-05 row.** Two defects in one row. First, it names a retired ordinal as part of the invariant
statement — restate the invariant in terms of the read path. Second, it cites a test whose name
appears nowhere in the tree; a search for the whole name and for its short form both return nothing.
Per Fork B, search the eprom validation suite for a test that genuinely asserts the VPP-skip behaviour
and cite it by its real name. If none exists, say so in the row rather than inventing a name — an
honestly uncovered invariant is information and a fabricated citation is not. Whichever way it goes,
record which in the SUMMARY.

Commit both files together in the firmware repository, then advance the meta gitlink. This commit
touches documentation only. Note that the firmware build workflow ignores markdown-only commits, so
this commit triggers no CI run — say so in the SUMMARY so nobody reads the absence of a run as a
failure, and run the four legs locally before committing anyway.
  </action>
  <acceptance_criteria>
    - Neither `firestarter_fw/CLAUDE.md` nor `firestarter_fw/PROTOCOLS.md` contains either retired ordinal identifier.
    - The native-test-registration paragraph says two lines and names the shared base section.
    - The erase-model paragraph retains all four of its decision ids and its honesty statement; only its final clause changed.
    - The INV-05 row cites a test that exists in the tree, or states that the invariant has no automated coverage.
    - The SUMMARY records which of those two outcomes applied and, if a test was cited, its exact name and path.
    - All four firmware CI legs are green locally before the commit.
    - The commit touches exactly the two documentation files.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_fw && /usr/local/py-utils/bin/python -c "import pathlib; n1='CMD_'+'BLANK_CHECK'; n2='CMD_'+'VERIFY'; bad=[(f,n) for f in ('CLAUDE.md','PROTOCOLS.md') for n in (n1,n2) if n in pathlib.Path(f).read_text()]; assert not bad, 'retired ordinal still documented as live: '+str(bad); print('firmware documentation names no retired ordinal')"</automated>
    <fails_when>non-zero exit (an AssertionError names the file and identifier still present), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/local/py-utils/bin/python -c "import re,pathlib; t=pathlib.Path('CLAUDE.md').read_text(); assert 'four new lines' not in t, 'the registration paragraph still claims four lines'; assert 'native_base' in t, 'the corrected paragraph does not name the shared base section as the reason'; print('registration cost corrected to the measured value')"</automated>
    <fails_when>non-zero exit (an AssertionError names whether the stale count survived or the reason is missing), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/local/py-utils/bin/python -c "import pathlib,re; t=pathlib.Path('PROTOCOLS.md').read_text(); stale='test_inv05_eprom_vpp'+'_skip_on_read'; assert stale not in t, 'the INV-05 row still cites a test that does not exist in the tree'; rows=[l for l in t.splitlines() if l.startswith('| INV-05')]; assert len(rows)==1, 'expected exactly one INV-05 row, found %d' % len(rows); print('INV-05 row repaired')"</automated>
    <fails_when>non-zero exit (an AssertionError names the surviving fabricated citation or a wrong row count), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces/firestarter_fw && /usr/local/py-utils/bin/pytest tests/ -o addopts="" -p no:cacheprovider -q</automated>
    <fails_when>non-zero exit accompanied by any FAILED line naming a module other than `tests/test_flash_path_record_sync.py` — several source-scan gates read documentation files, so a documentation-only edit can still move a gate</fails_when>
    <automated>cd /workspaces/firestarter_fw && pio test -e native_nodevtools</automated>
    <fails_when>non-zero exit, or any SUMMARY row reads FAILED</fails_when>
    <automated>cd /workspaces/firestarter_fw && COMMIT_FILES=$(git show --name-only --format="" HEAD) && printf '%s\n' "$COMMIT_FILES"</automated>
    <fails_when>non-zero exit, or the listed paths are anything other than exactly `CLAUDE.md` and `PROTOCOLS.md`</fails_when>
  </verify>
  <done>All five stale firmware documentation citations are repaired, including the pre-existing fabricated test name in the INV-05 row; the firmware test trees are green; the commit is documentation-only.</done>
</task>

<task type="auto">
  <name>Task 3: Record the deferred clean stop on CMP-F1 and correct the roadmap's nomination</name>
  <precondition>`.planning/REQUIREMENTS.md` carries the CMP-F1 row in its future-requirements table, and `.planning/ROADMAP.md` carries the v1.41 paragraph naming this phase as the home for the clean-stop change.</precondition>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/202-one-comparison-engine-on-the-host/202-READ-ABORT-ANSWER.md` in full. It is the document that nominated this phase by name, and its implication section is the reasoning that D-12 re-examined. Read the abort mechanism, the four-condition discrimination, and the measured per-abort cost.
    - `.planning/REQUIREMENTS.md` — the future-requirements table and the CMP-F1 row, plus the neighbouring rows so the repaired row matches the table's column conventions.
    - `.planning/ROADMAP.md` — the v1.41 paragraph that names this phase for the clean-stop change, in full, plus the surrounding known-open-mechanic section it belongs to.
    - `firestarter_fw/src/operation_utils.cpp` — the acknowledgement wait and the message parser beneath it. The parser already maps the clean-stop token to an enumeration value and the wait already returns only on the acknowledge and error values, so the clean-stop value falls through to the retry delay. That is the measured forward-compatibility fact the record needs.
    - `firestarter_app/firestarter/eprom_operations.py` — the region-compare drive's four-condition discrimination, which keys on the firmware timeout error code. That is the host half D-12 found is not one line.
    - `204-CONTEXT.md` D-12 in full.
  </read_first>
  <behavior>
    - CMP-F1's row states the forward-compatibility finding: old firmware already parses the clean-stop token and already ignores it, so a future host sending it degrades to today's timeout path.
    - CMP-F1's row states that the change is therefore not time-critical and can land in any later phase with the same properties.
    - The roadmap paragraph records that the change was offered at Phase 204 and declined, with the reason.
    - Neither edit changes the status of any tracked requirement in this milestone.
  </behavior>
  <action>
Two planning-file edits, one commit, so the next author does not re-derive an analysis that has
already been done twice.

**CMP-F1 in the requirements table.** Extend its row with the measured finding. The content: the
firmware half genuinely is one line — the acknowledgement wait returns on the acknowledge and error
values and lets the clean-stop value fall through to the retry delay and then the one-second timeout.
The host half is not one line — today the host aborts by withholding acknowledgements, sending
nothing at all, and the region-compare drive's four-condition discrimination keys on the firmware
timeout error code, which a clean stop would no longer produce. Both sides therefore move together,
and the surface they would rework is the one Phase 202 had just built. And the fact that makes
deferring free: pre-`3.1.0` firmware already parses the clean-stop token into its own enumeration
value and already ignores it, so a future host that sends it degrades to exactly today's timeout path.
The change is forward-compatible by construction and can land in any later phase with the same
properties.

Keep the row's existing why-deferred content; this extends the record rather than replacing it.

**The roadmap paragraph.** It currently names this phase by name as the home for the change. Per Fork
C, correct it in place rather than deleting it: keep the observation that a clean stop is a cheaper
route to CMP-F1 than the abort originally assumed, and add that it was offered at Phase 204 and
declined there, with the one-line reason and a pointer to the phase CONTEXT decision. Deleting the
nomination would make a later reader wonder whether it was ever considered; correcting it records
that it was.

Do not change the status of any tracked requirement, and do not touch any other row or section in
either file. Commit both together.
  </action>
  <acceptance_criteria>
    - The CMP-F1 row names the forward-compatibility finding — that old firmware already parses the clean-stop token and its acknowledgement wait already ignores it.
    - The CMP-F1 row states that both sides must move together and names the host-side discrimination that keys on the timeout error code as the reason.
    - The CMP-F1 row retains its original why-deferred content.
    - The roadmap paragraph records the offer and the declination, names the reason, and points at the phase CONTEXT decision.
    - No requirement checkbox anywhere in `.planning/REQUIREMENTS.md` changes state in this commit.
    - The commit touches exactly the two planning files.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 -c "import pathlib; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); rows=[l for l in t.splitlines() if '**CMP-F1**' in l]; assert len(rows)==1, 'expected exactly one CMP-F1 row, found %d' % len(rows); r=rows[0]; assert 'forward-compatible' in r, 'the CMP-F1 row does not carry the forward-compatibility finding'; assert '204' in r, 'the CMP-F1 row does not record where the finding came from'; print('CMP-F1 carries the deferral finding')"</automated>
    <fails_when>non-zero exit (an AssertionError names whether the row count was wrong or which clause is missing), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces && python3 -c "import pathlib; t=pathlib.Path('.planning/ROADMAP.md').read_text(); assert 'D-12' in t, 'the roadmap paragraph does not point at the phase CONTEXT decision'; assert 'declined' in t or 'Declined' in t, 'the roadmap paragraph does not record the declination'; print('roadmap nomination corrected in place')"</automated>
    <fails_when>non-zero exit (an AssertionError names the missing clause), or the confirmation line is absent from stdout</fails_when>
    <automated>cd /workspaces && python3 -c "import subprocess, pathlib; r=subprocess.run(['git','show','HEAD~1:.planning/REQUIREMENTS.md'], capture_output=True, text=True); assert r.returncode==0, 'git show failed: '+r.stderr; before=sum(1 for l in r.stdout.splitlines() if l.startswith('- [x]')); after=sum(1 for l in pathlib.Path('.planning/REQUIREMENTS.md').read_text().splitlines() if l.startswith('- [x]')); assert before==after, 'completed-requirement count moved from %d to %d' % (before, after); print('completed-requirement count unchanged at', after)"</automated>
    <fails_when>non-zero exit (an AssertionError names the git failure or the moved count), or the confirmation line is absent from stdout — a changed count means a requirement checkbox was flipped in a record-keeping commit, which is the recorded failure mode of requirements being marked complete early. The git status is asserted explicitly rather than swallowed by a pipeline</fails_when>
    <automated>cd /workspaces && COMMIT_FILES=$(git show --name-only --format="" HEAD) && printf '%s\n' "$COMMIT_FILES"</automated>
    <fails_when>non-zero exit, or the listed paths are anything other than exactly `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`</fails_when>
  </verify>
  <done>CMP-F1 records the measured forward-compatibility finding and why both sides must move together; the roadmap's nomination of this phase is corrected in place to record the offer and the declination; no requirement status changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta catalog → both sub-repo artifacts | `tools/catalog/messages.toml` is the single source of truth; both sub-repos carry synced generated copies they must never regenerate. A catalog edit that changes output silently desynchronises three repositories. |
| documentation → future implementer | A stale citation is untrusted input to a future author, who may implement against a surface that no longer exists. |
| planning record → future planner | A stale nomination in the roadmap causes an analysis to be re-derived, or worse, acted on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-18 | Tampering | `tools/catalog/messages.toml` and the two synced generated artifacts | high | mitigate | A catalog edit that changed generated output would leave two sub-repos carrying stale artifacts with no gate to catch it, since neither sub-repo may regenerate. Both outputs are regenerated to a temporary path and compared byte-for-byte before the catalog is committed, and both sub-repo working trees are asserted clean afterwards. |
| T-204-19 | Spoofing | the two orphaned debug ids | medium | mitigate | A reassigned debug id makes a replayed capture or a log from older firmware silently misinterpreted. Both ids are kept, and each carries a comment recording the retired emit site and the never-reuse reason at the place someone would reach for a free id. |
| T-204-20 | Repudiation | `firestarter_fw/PROTOCOLS.md`'s invariant table | medium | mitigate | A row citing a test that does not exist claims coverage the tree does not have, and the row reads as evidence. The fabricated citation is replaced with a real test name or with an honest statement that the invariant is uncovered; which of the two applied is recorded. |
| T-204-21 | Information Disclosure | none applicable | low | accept | This plan touches documentation and a message catalog. No secret, credential or user data crosses any boundary it changes. |
| T-204-22 | Elevation of Privilege | `beta` branches in all three repositories | high | mitigate | A push to `beta` publishes in both sub-repos and uploads to PyPI from the app, with no path filter — and this plan's commits are documentation-only, which is exactly the shape that has published unintentionally before. All work stays on the milestone branch; no task pushes. |
</threat_model>

<verification>
- Both generated catalog artifacts regenerate byte-identically from the amended catalog, proven by a
  diff against the committed files.
- The catalog validator exits zero.
- Neither sub-repository has a modified file after task 1.
- No retired ordinal identifier remains in either firmware documentation file.
- The INV-05 row cites something that exists, or states that nothing does.
- The completed-requirement count in `.planning/REQUIREMENTS.md` is unchanged by task 3.
- No branch in any of the three repositories is `beta` at any point.
</verification>

<success_criteria>
1. Both orphaned debug ids are kept and annotated, and the annotation provably changes no generated
   output — so Phase 204 remains a two-repo phase.
2. Every firmware documentation sentence the sweep staled is repaired, plus the one pre-existing
   fabricated test citation found while repairing them.
3. CMP-F1 carries the measured forward-compatibility finding and the reason both sides must move
   together.
4. The roadmap's nomination of this phase for the clean-stop change is corrected in place to record
   that it was offered and declined.
</success_criteria>

<output>
Create `.planning/phases/204-the-command-surfaces-leave-the-firmware/204-04-SUMMARY.md` when done.
</output>
