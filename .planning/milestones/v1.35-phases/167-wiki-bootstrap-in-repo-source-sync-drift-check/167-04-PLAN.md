---
phase: 167-wiki-bootstrap-in-repo-source-sync-drift-check
plan: 04
type: execute
wave: 3
depends_on: [167-02]
files_modified:
  - wiki/Home.md
  - wiki/How-This-Wiki-Is-Published.md
  - wiki/_Sidebar.md
  - .planning/v1.35/MIGRATION-TABLE.md
autonomous: true
requirements: [WIKI-02, WIKI-05]

must_haves:
  truths:
    - "D-01: the in-repo wiki source tree is `wiki/` at the repository root, holding content only — no tooling file is created inside `wiki/`."
    - "D-02: `wiki/` is flat — `find wiki -mindepth 1 -type d` returns nothing, so source path equals published page and the drift check stays a plain directory diff."
    - "D-03: every filename in `wiki/` is `Title-Case-With-Hyphens.md`, so the rendered page title and the page URL are both readable and both greppable by Phases 169 and 170."
    - "D-04: page names are derived mechanically from filenames with no manifest, and migration provenance lives in a separate checked-in table `.planning/v1.35/MIGRATION-TABLE.md` carrying a rendered-title column."
    - "D-09: `Home.md` is hand-authored and `_Sidebar.md` is generated — `wiki.py sidebar --check` exits 0 against the committed pair, and `Home.md` is the only reachability evidence."
    - "D-10: the generated `_Sidebar.md` is committed to `wiki/`, so the drift check stays exclusion-free and every exclusion-shaped hole is avoided."
    - "D-12: the wiki ends this phase holding `Home.md` plus `How-This-Wiki-Is-Published.md`, and that second page states the WIKI-02 authority rule and warns that web-UI edits are overwritten on the next publish."
    - "D-06: the beta-versus-released gap is stated on a page a reader will see, not left implicit in `.planning/` — `How-This-Wiki-Is-Published.md` says the wiki tracks `beta`."
    - "`python3 tools/wiki/wiki.py check` exits 0 against the real `wiki/` tree with no `--source-dir` override."
  artifacts:
    - wiki/Home.md
    - wiki/How-This-Wiki-Is-Published.md
    - wiki/_Sidebar.md
    - .planning/v1.35/MIGRATION-TABLE.md
  key_links:
    - "`Home.md` links `How-This-Wiki-Is-Published` in the single legal link form, and names the Phase 168 pages as plain text — a link to a page that does not exist yet would fail the broken-link leg."
    - "`wiki/_Sidebar.md` is byte-identical to `wiki.py sidebar` output, which is what the CI freshness leg asserts."
---

<objective>
Create the wiki's content tree: `wiki/` at the repository root (D-01), flat (D-02), with
`Title-Case-With-Hyphens.md` filenames (D-03). The phase ends with exactly three files there —
hand-authored `Home.md`, the D-12 scaffolding page `How-This-Wiki-Is-Published.md`, and the
generated, committed `_Sidebar.md` (D-09, D-10). Alongside it, `.planning/v1.35/MIGRATION-TABLE.md` is
created as the D-04 provenance shell that Phase 168 fills in, with the rendered-title column that
makes the hyphen-to-space title loss visible at review time instead of on the live wiki.

**Scope fence: no documentation content moves in this phase.** The 13 `doc/` files are Phase 168's
work, the front-door README is 169's, the sub-repo READMEs are 170's. Nothing under
`firestarter/doc/` or `firestarter_app/doc/` is read, copied, moved or deleted here.

Purpose: this is the first content the pipeline built in plans 167-01 through 167-03 will publish,
and it is what makes `wiki.py check` runnable against the real tree rather than only against
fixtures.
Output: three files under `wiki/`, one provenance table under `tools/wiki/`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-CONTEXT.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-PATTERNS.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-02-SUMMARY.md
</context>

<hard_rules>
**Scope fence.** Do not read, copy, move, rename or delete anything under `firestarter/doc/` or
`firestarter_app/doc/`. Do not create a README anywhere. Do not add a fourth file to `wiki/`.

**No new product documentation.** `How-This-Wiki-Is-Published.md` is pipeline scaffolding — it
describes the wiki's own mechanics, not Firestarter. It must not describe chips, protocols, shield
revisions, install procedures or anything else the product does. The milestone's activation decision
is relocate-and-correct only; this page is the single named exception and it stays inside its
justification.

**Honesty constraint.** No page may state that the wiki is live, that publishing has been
demonstrated against GitHub, or that any page is reachable at a `github.com` wiki URL. The wiki
repository does not exist yet. Describe the mechanism in the present tense and the live state as
pending.

<!-- planner-discipline-allow: doc/ -->
</hard_rules>

<tasks>

<task type="auto">
  <name>Task 1: Author Home.md and How-This-Wiki-Is-Published.md</name>
  <files>wiki/Home.md, wiki/How-This-Wiki-Is-Published.md</files>
  <read_first>
    - tools/wiki/wiki.py (`check_page_names`, `check_link_forms`, `check_orphans`, `render_title`, `NAV_EXCLUDED_PAGES`, `HOME_PAGE` — the exact rules this content must satisfy, and the exact link form that is legal)
    - CLAUDE.md (register and tone reference only — the closest match in this repo for a short orienting document)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-CONTEXT.md (§"Implementation Decisions" D-06, D-09, D-12 in full — D-12 fixes what each of the two pages carries; D-06 fixes the beta caveat that must appear on a reader-visible page)
    - .planning/ROADMAP.md (§"Phase 168: MIGRATE" criterion 1 — the 13 filenames Home names as coming, and §"v1.35" activation decisions)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Pitfall 4: A page title that needs a literal hyphen"; §"Pattern 4" link-form allowlist)
  </read_first>
  <action>
Create the directory `wiki/` at the repository root (D-01) and author two markdown files in it. The
directory is flat (D-02) and both filenames use `Title-Case-With-Hyphens.md` (D-03):
`wiki/Home.md` and `wiki/How-This-Wiki-Is-Published.md`.

**`wiki/Home.md`** — hand-authored (D-09), and the only source of reachability evidence the checker
trusts. It must contain:
- A top-level heading naming the project wiki.
- One or two sentences orienting a reader who arrived from the repository.
- A markdown link to the other page, in the single legal internal link form:
  `[How this wiki is published](How-This-Wiki-Is-Published)`. No `.md` suffix, no double-bracket
  form, no reference-style link, exact case. Any other form fails `wiki.py links`.
- A clearly-labelled section naming the pages Phase 168 will bring, **as plain text, not as links**.
  This is the trap in this task: `Home.md` must *name* the future pages per D-12, and a markdown
  link to a page that does not exist yet makes the broken-link leg exit 1. Write them as a plain
  list of names with no `[](...)` syntax around them. Derive the names from the 13 files in
  ROADMAP.md §"Phase 168" criterion 1.
- The D-06 caveat, in the reader's own words: this wiki tracks the `beta` integration branch, so it
  can describe behaviour that a released version has not shipped. D-06 accepts this cost on the
  condition that it is stated on the wiki rather than left implicit, so it belongs on a page a
  reader will see.

**`wiki/How-This-Wiki-Is-Published.md`** — the D-12 scaffolding page. It must state:
- The WIKI-02 authority rule: pages are authored as markdown files inside the `firestarter_prom`
  repository under `wiki/`, and that in-repo copy is the single source of truth.
- The overwrite warning: an edit made in the wiki's web UI is destroyed by the next publish, because
  the publish mirrors the source directory rather than merging into the wiki. This is the only place
  a drive-by wiki editor would ever learn the rule before losing their edit, which is D-12's whole
  justification.
- How to publish: `python3 tools/wiki/wiki.py publish` previews and checks for drift without writing
  anything, and `python3 tools/wiki/wiki.py publish --push` publishes (D-05, D-08).
- The page-naming convention: a file named `Shield-Revisions.md` becomes the page "Shield Revisions"
  at the URL path `/wiki/Shield-Revisions` (D-03, D-04), that hyphens render as spaces so a title
  cannot contain a literal hyphen, and that the derivation is mechanical with no manifest to keep in
  step.
- The one legal internal link form, `[Text](Page-Name)`, and that `.md`-suffixed links,
  double-bracket links and reference-style links are rejected by the check.
- That `_Sidebar.md` is generated by `python3 tools/wiki/wiki.py sidebar` and must not be hand-edited
  (D-09, D-10).
- The D-06 branch fact restated in mechanical terms: the wiki is published from `beta`.

Write in plain prose aimed at a contributor, not at an agent. No GSD phase numbers, no requirement
ids, no `.planning/` paths, no decision ids — this page is public and must read as reference
documentation. The rationale that the operator's zero-comment rule keeps out of `wiki.py`,
`selftest.sh` and the workflow YAML lives here; that is the relocation target named in
`167-PATTERNS.md`.

Both files end with a single trailing LF and contain no CR bytes. Do not create a third page, a
`_Footer.md`, or any subdirectory under `wiki/`.
  </action>
  <verify>
    <automated>python3 tools/wiki/wiki.py links</automated>
  </verify>
  <acceptance_criteria>
    - `ls wiki/` lists exactly `Home.md` and `How-This-Wiki-Is-Published.md` at the end of this task.
    - `find wiki -mindepth 1 -type d` returns nothing.
    - `python3 tools/wiki/wiki.py links` exits 0 against the real `wiki/` tree with no `--source-dir` override, and its output includes the line `How-This-Wiki-Is-Published -> "How This Wiki Is Published"`.
    - `grep -c '](How-This-Wiki-Is-Published)' wiki/Home.md` returns exactly `1`.
    - `Home.md` contains no link to a page that does not exist: `grep -oE '\]\([A-Za-z0-9-]+\)' wiki/Home.md | tr -d '](\)' | sort -u` yields only page stems for which `wiki/<stem>.md` exists.
    - `grep -c '.md)' wiki/Home.md` returns `0` and `grep -c '.md)' wiki/How-This-Wiki-Is-Published.md` returns `0`.
    - `grep -c '\[\[' wiki/Home.md` returns `0`.
    - The D-06 caveat is present on a reader-visible page: `grep -c 'beta' wiki/Home.md` returns at least `1` and `grep -c 'beta' wiki/How-This-Wiki-Is-Published.md` returns at least `1`.
    - The overwrite warning is present: `grep -ci 'overwritten' wiki/How-This-Wiki-Is-Published.md` returns at least `1`.
    - Neither page leaks planning vocabulary: `grep -cE '(WIKI-0[1-6]|D-0[1-9]|D-1[0-2]|\.planning/|Phase 16[0-9])' wiki/How-This-Wiki-Is-Published.md` returns `0`.
    - Neither page claims a live wiki: `grep -c 'github.com/henols/firestarter_prom/wiki' wiki/Home.md` returns `0`.
    - Scope fence intact: `git status --porcelain firestarter firestarter_app` reports no modification, and no file under either `doc/` directory was read, copied or removed.
    - Both files end with a single LF and contain no CR: `grep -c $'\r' wiki/Home.md` returns `0`; likewise for the other page.
  </acceptance_criteria>
  <done>Both hand-authored pages exist, `wiki.py links` passes against the real tree, and no future-page name is written as a link.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and commit wiki/_Sidebar.md, then prove the freshness leg against the real tree</name>
  <files>wiki/_Sidebar.md</files>
  <read_first>
    - tools/wiki/wiki.py (`generate_sidebar`, `cmd_sidebar`, `render_title`, `NAV_EXCLUDED_PAGES` — the exact output shape and the determinism contract in the module docstring)
    - wiki/Home.md, wiki/How-This-Wiki-Is-Published.md (the two pages the sidebar is generated from)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-CONTEXT.md (§D-09, §D-10 — generated, committed, freshness-asserted)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Pattern 5: Deterministic sidebar generation")
  </read_first>
  <action>
Run `python3 tools/wiki/wiki.py sidebar` with no `--source-dir` override so it writes
`wiki/_Sidebar.md` from the real tree, and commit the result (D-10 — generated artifacts in this
project are committed, following the `messages.h` / `messages.py` precedent).

The generated file must contain exactly two lines, `- [Home](Home)` first and
`- [How This Wiki Is Published](How-This-Wiki-Is-Published)` second, LF endings, single trailing
newline.

**Do not add a do-not-edit banner or any header line to `_Sidebar.md`.** `.planning/codebase/CONVENTIONS.md`
records a convention of banners on generated artifacts, but this artifact is published to a public
wiki where a banner would render as visible sidebar text, and the operator's zero-comment rule
forbids authored comment blocks in any case. The do-not-edit fact lives in
`wiki/How-This-Wiki-Is-Published.md` and in `wiki.py sidebar --help` instead. If a banner appears in
the output, the generator in plan 167-01 wrote one and the generator is what must change, not this
file.

Then run three assertions against the real tree and record their output in the plan SUMMARY:
- `python3 tools/wiki/wiki.py sidebar --check` exits 0 and reports the file fresh.
- `python3 tools/wiki/wiki.py check` exits 0 and reports both offline legs passing. This is the
  first time the aggregator runs against real content rather than a fixture, and it is the exact
  command `.github/workflows/wiki-check.yml` will run in plan 167-05.
- Regenerate once more into a temporary copy and `cmp` it against the committed file to confirm
  byte-stability over the real tree, not only over the fixture.
  </action>
  <verify>
    <automated>python3 tools/wiki/wiki.py check</automated>
  </verify>
  <acceptance_criteria>
    - `ls wiki/` lists exactly `Home.md`, `How-This-Wiki-Is-Published.md` and `_Sidebar.md` — three files, no more.
    - `python3 tools/wiki/wiki.py sidebar --check` exits 0.
    - `python3 tools/wiki/wiki.py check` exits 0 and its final stdout line is the aggregator's `OK:` line.
    - `wc -l wiki/_Sidebar.md` returns `2`.
    - `head -1 wiki/_Sidebar.md` is exactly `- [Home](Home)`.
    - `grep -c '](How-This-Wiki-Is-Published)' wiki/_Sidebar.md` returns exactly `1`.
    - No banner and no comment: `grep -cE '^(<!--|#|Generated|DO NOT|AUTO)' wiki/_Sidebar.md` returns `0`.
    - `grep -c $'\r' wiki/_Sidebar.md` returns `0`.
    - Regenerating into a temporary path and `cmp`-ing against `wiki/_Sidebar.md` succeeds.
    - `git status --porcelain wiki/` shows `_Sidebar.md` as a tracked addition, not as ignored: `git check-ignore wiki/_Sidebar.md` returns non-zero.
    - The plan SUMMARY records the verbatim output of all three assertions.
  </acceptance_criteria>
  <done>`wiki/_Sidebar.md` is generated, committed, byte-stable over the real tree, banner-free, and the aggregator passes against real content.</done>
</task>

<task type="auto">
  <name>Task 3: Create the D-04 migration provenance table with its rendered-title column</name>
  <files>.planning/v1.35/MIGRATION-TABLE.md</files>
  <read_first>
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-CONTEXT.md (§D-03, §D-04 in full — what the table is for, and why it is a separate checked-in artifact rather than something the publish path consults)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Pitfall 4: A page title that needs a literal hyphen" — why the rendered-title column exists and why U+2010 is forbidden)
    - .planning/ROADMAP.md (§"Phase 168: MIGRATE" criterion 1 — the 13 source files and the per-file mapping the table must be able to hold; §"v1.35" §"Sequencing hazard" — Backlog 999.9 will grep this table)
    - tools/wiki/wiki.py (`render_title` — the derivation the `Rendered title` column records)
  </read_first>
  <action>
Create `.planning/v1.35/MIGRATION-TABLE.md`: the D-04 provenance table, created here as a shell and filled
by Phase 168. It lives under `tools/wiki/` and NOT under `wiki/`, because it is project provenance
rather than a published page — `wiki/` must end this phase holding exactly three files.

Structure:
- A short title and one paragraph stating what the table records and why it exists: the publish path
  derives page names mechanically from filenames and consults no manifest (D-04), so this table is
  the auditable record of where each page came from, and it is what the Backlog 999.9 repository
  rename sweep will grep. State explicitly that the publish command does not read this file — a
  reader must not mistake it for a registry that publishing depends on.
- A markdown table with exactly these columns, in this order:
  `Source repo` | `Source path` | `Wiki page` | `Rendered title` | `Moved in`
- Two filled rows for the pages this phase authored, so the conventions are demonstrated by real
  data rather than by an empty template: `Home.md` and `How-This-Wiki-Is-Published.md`, each with a
  `Source repo` of `firestarter_prom`, a `Source path` of `—` (authored in place, no prior path), the
  `Wiki page` stem, the `Rendered title` produced by hyphen-to-space substitution (`Home` and
  `How This Wiki Is Published`), and `Moved in` = `167`.
- Thirteen placeholder rows, one per file in ROADMAP.md §"Phase 168" criterion 1, with the
  `Source repo` and `Source path` columns filled from that list and the `Wiki page`,
  `Rendered title` and `Moved in` columns holding a visible `TBD` marker for Phase 168 to replace.
  Filling the source columns now is what makes the table auditable at Phase 168 review time instead
  of trusting 168 to remember all thirteen.
- A closing note recording the hyphen hazard for whoever fills the table: GitHub renders a hyphen in
  a filename as a space, so a title that needs a literal hyphen cannot be expressed and must be
  reworded; do not substitute a non-ASCII hyphen character to smuggle one through, because Phases 169
  and 170 hand-write these URLs into READMEs and an invisible non-ASCII character in a URL is a worse
  problem than a reworded title. Name the specific at-risk cases from the thirteen — the part-number
  and slash-bearing titles — so 168 checks them deliberately.

This is a markdown document, not source, so it carries prose freely. Do not add any executable
content, and do not make any tool read it.
  </action>
  <verify>
    <automated>test -f .planning/v1.35/MIGRATION-TABLE.md && python3 tools/wiki/wiki.py check</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.35/MIGRATION-TABLE.md` exists and `ls wiki/` still lists exactly three files.
    - The table header line contains all five column names: `grep -c 'Source repo' .planning/v1.35/MIGRATION-TABLE.md` returns at least `1`, and the same header line also contains `Source path`, `Wiki page`, `Rendered title` and `Moved in`.
    - Fifteen data rows are present: `grep -cE '^\|' .planning/v1.35/MIGRATION-TABLE.md` returns `17` (header, separator, and fifteen data rows).
    - The two authored pages appear as filled rows: `grep -c 'How This Wiki Is Published' .planning/v1.35/MIGRATION-TABLE.md` returns at least `1`.
    - All thirteen Phase 168 source files are listed: each of `PROTOCOLS.md`, `SHIELD-REVISIONS.md`, `AT28C04-ADAPTER.md`, `PY32F071-FIRMWARE-INSTALL.md`, `beta-testing-install.md`, `community-validation.md`, `infoic-field-dictionary.md`, `lockable-proms.md`, `package-details.md`, `pinout-safety-review.md`, `protocol-flags.md`, `protocol-id.md`, `sram-nvram-behavior.md` appears at least once.
    - `grep -c 'TBD' .planning/v1.35/MIGRATION-TABLE.md` returns at least `13`.
    - The file states that the publish path does not read it: `grep -ciE 'publish.{0,40}(does not|never) read' .planning/v1.35/MIGRATION-TABLE.md` returns at least `1`.
    - No non-ASCII hyphen was introduced: `grep -cP '\x{2010}' .planning/v1.35/MIGRATION-TABLE.md` returns `0`.
    - No tool depends on the table: `grep -c 'MIGRATION-TABLE' tools/wiki/wiki.py` returns `0`.
    - Scope fence intact: `git status --porcelain firestarter firestarter_app` reports no modification.
    - `python3 tools/wiki/wiki.py check` still exits 0 — the new file is outside `wiki/` and does not affect any leg.
  </acceptance_criteria>
  <done>The D-04 provenance table exists with a rendered-title column, thirteen pre-filled source rows for Phase 168, and an explicit statement that the publish path does not consult it.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NEW in Phase 167. None of it exists in the repository before this phase, so
none of it can be verified against prior source.

**New files**
- `tools/wiki/wiki.py`
- `tools/wiki/selftest.sh`
- `.planning/v1.35/MIGRATION-TABLE.md`
- `wiki/Home.md`
- `wiki/How-This-Wiki-Is-Published.md`
- `wiki/_Sidebar.md`
- `.github/workflows/wiki-check.yml`
- `.github/workflows/wiki-publish.yml`

**New directories:** `wiki/`, `tools/wiki/`

**`wiki.py` subcommands:** `sidebar`, `links`, `check`, `publish`
**`wiki.py` flags:** `--source-dir`, `--wiki-remote`, `--check` (on `sidebar`), `--push` (on `publish`), `--require-wiki` (on `publish`)
**`wiki.py` exit-code contract:** `0` = asserted property holds; `1` = asserted property is false; `2` = precondition not met (source directory missing, or wiki remote absent)
**`wiki.py` constants:** `DEFAULT_WIKI_REMOTE`, `WIKI_BRANCH`, `DEFAULT_SOURCE_DIR`, `HOME_PAGE`, `SIDEBAR_PAGE`, `NAV_EXCLUDED_PAGES`, `ILLEGAL_NAME_CHARS`, `LEGAL_LINK_RE`, `EXTERNAL_LINK_PREFIXES`, `COMMANDS`
**`wiki.py` functions:** `render_title`, `page_files`, `page_stems`, `generate_sidebar`, `cmd_sidebar`, `strip_code_spans`, `extract_internal_links`, `check_page_names`, `check_link_forms`, `check_orphans`, `cmd_links`, `cmd_check`, `cmd_publish`, `_git`, `_build_argparser`, `main`

**`selftest.sh` helpers:** `banner`, `new_source_dir`, `new_bare_wiki`, `rc_of`, `record`, `assert_rc`, `print_evidence_table`, `CASES`, `EVIDENCE`
**`selftest.sh` case ids:** `drift_detected_exit_1`, `hand_edit_overwritten`, `idempotent_head_unchanged`, `deleted_page_removed`, `orphan_exit_1`, `sidebar_link_is_not_evidence`, `broken_link_exit_1`, `md_suffix_link_exit_1`, `illegal_filename_exit_1`, `stale_sidebar_exit_1`, `wiki_absent_exit_2`, `sidebar_deterministic`

**Wiki page names and rendered titles:** `Home` → "Home"; `How-This-Wiki-Is-Published` → "How This Wiki Is Published"
**Migration table columns:** `Source repo`, `Source path`, `Wiki page`, `Rendered title`, `Moved in`

**Workflow ids:** file `wiki-check.yml`, `name: Wiki check`, job `wiki-check`; file `wiki-publish.yml`, `name: Wiki publish`, job `wiki-publish`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `wiki/` content → public GitHub wiki | Everything in this directory becomes publicly readable on the first publish |
| hand-authored `Home.md` → reachability evidence set | The only navigation file the checker trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-13 | Information disclosure | `wiki/` page content | medium | mitigate | `wiki/` is published verbatim to a public wiki. No credential, token, absolute host path or private URL may appear in any page; the acceptance criteria bar `.planning/` paths, requirement ids and decision ids from the published pages, which also keeps internal process vocabulary out of public documentation. |
| T-167-14 | Repudiation | claims made on `Home.md` | high | mitigate | The honesty constraint: no page states that the wiki is live or that publishing was demonstrated against GitHub, because it has not been. The D-06 beta caveat is stated affirmatively so a reader is not misled about what the documented behaviour corresponds to. |
| T-167-15 | Tampering | future-page names written as links | medium | mitigate | Phase 168's pages do not exist, so `Home.md` names them as plain text; a link would fail `check_link_forms` and would tempt a later executor to relax the leg. An acceptance criterion enumerates every link target in `Home.md` and requires a matching file. |
| T-167-16 | Repudiation | `MIGRATION-TABLE.md` mistaken for a registry | medium | mitigate | The table explicitly states that the publish path does not read it, and an acceptance criterion asserts no tool references it. D-04 rejected a consulted manifest because a stale manifest is a drift class the drift check does not cover. |
| T-167-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages. `167-RESEARCH.md` §"Package Legitimacy Audit" records `pytest` as alternative-only, priced and rejected; no install task exists in any plan of this phase. |
</threat_model>

<verification>
- `ls wiki/` lists exactly `Home.md`, `How-This-Wiki-Is-Published.md`, `_Sidebar.md`.
- `find wiki -mindepth 1 -type d` returns nothing.
- `python3 tools/wiki/wiki.py check` exits 0 against the real tree with no `--source-dir` override.
- `python3 tools/wiki/wiki.py links` prints the rendered title of every page.
- `bash tools/wiki/selftest.sh` still exits 0 — this plan changed no tooling.
- `git status --porcelain firestarter firestarter_app` reports no modification; the Phase 168 scope fence is intact.
</verification>

<success_criteria>
- `wiki/` exists at the repository root, is flat, and holds exactly the three files D-12 specifies.
- `Home.md` is hand-authored, links `How-This-Wiki-Is-Published` in the single legal form, names the Phase 168 pages as plain text, and carries the D-06 beta caveat.
- `How-This-Wiki-Is-Published.md` states the WIKI-02 authority rule, the overwrite warning, the publish commands, the naming convention and the sidebar-is-generated rule, in contributor-facing prose with no planning vocabulary.
- `wiki/_Sidebar.md` is generated, committed, banner-free and byte-stable over the real tree.
- `.planning/v1.35/MIGRATION-TABLE.md` exists with a rendered-title column, thirteen pre-filled Phase 168 source rows, and an explicit non-registry statement.
</success_criteria>

<output>
Create `.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-04-SUMMARY.md` when done.
</output>
