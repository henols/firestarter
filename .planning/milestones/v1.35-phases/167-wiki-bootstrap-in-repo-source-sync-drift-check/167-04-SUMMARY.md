---
phase: 167-wiki-bootstrap-in-repo-source-sync-drift-check
plan: 04
subsystem: docs
tags: [markdown, wiki, github-wiki, content-authoring]

requires:
  - phase: 167-wiki-bootstrap-in-repo-source-sync-drift-check
    provides: "tools/wiki/wiki.py links/check/sidebar/publish subcommands and their exit-code contract; tools/wiki/selftest.sh fixture harness"
provides:
  - "wiki/ at the repository root — flat, three files, first real content the pipeline built in plans 167-01..03 has published against"
  - "wiki/Home.md — hand-authored reachability root, names the 13 Phase 168 pages as plain text, states the D-06 beta-tracking caveat"
  - "wiki/How-This-Wiki-Is-Published.md — D-12 scaffolding page: WIKI-02 authority rule, overwrite warning, publish commands, naming convention, legal link form, sidebar-is-generated rule"
  - "wiki/_Sidebar.md — generated via wiki.py sidebar, committed, byte-stable over the real tree"
  - ".planning/v1.35/MIGRATION-TABLE.md — D-04 provenance table shell with two filled rows and thirteen source-pre-filled TBD rows for Phase 168"
affects: [168]

tech-stack:
  added: []
  patterns:
    - "inline-code and fenced-code escaping of illegal link-form examples: every example of a rejected link shape ([[Page]], [text][ref], a .md-suffixed link) is wrapped in backtick code spans so wiki.py's own strip_code_spans excludes it from link-form checking, rather than the page failing its own tool's check"
    - "single-physical-line placement of any sentence a grep acceptance criterion must match — a soft-wrapped sentence whose keywords straddle a line break is invisible to a non-multiline grep -c"

key-files:
  created:
    - wiki/Home.md
    - wiki/How-This-Wiki-Is-Published.md
    - wiki/_Sidebar.md
    - .planning/v1.35/MIGRATION-TABLE.md
  modified: []

key-decisions:
  - "Home.md names the 13 Phase 168 pages using their current source filenames (PROTOCOLS, SHIELD-REVISIONS, etc.) as plain text rather than inventing Title-Case-With-Hyphens wiki page names in advance — the actual wiki page name for each is Phase 168's decision (see MIGRATION-TABLE.md's TBD rows), and naming them here would create a second, possibly-stale name to keep in sync"
  - "Every illegal-link-form example inside How-This-Wiki-Is-Published.md's prose ([[Page Name]], [text][ref]) is wrapped in single-backtick inline code, and the one legal-form example is placed inside a fenced code block, so wiki.py's own strip_code_spans excludes them from link-form parsing — without this the page authored to explain the rule would itself fail the rule it explains"
  - "MIGRATION-TABLE.md's opening paragraph was rewritten mid-task from a soft-wrapped multi-line sentence to two single-line sentences after discovering the acceptance grep 'publish.{0,40}(does not|never) read' does not match across a literal newline — the words 'does not' and 'read' were on adjacent wrapped lines in the first draft"

requirements-completed: []

coverage:
  - id: D1
    description: "wiki/ holds exactly three files (Home.md, How-This-Wiki-Is-Published.md, _Sidebar.md), flat, with wiki.py links/check exiting 0 against the real tree with no --source-dir override"
    requirement: WIKI-05
    verification:
      - kind: unit
        ref: "python3 tools/wiki/wiki.py check — exit 0, both offline legs report OK against the real wiki/ tree (see Observed Output below)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Home.md links How-This-Wiki-Is-Published in the single legal form, names the 13 Phase 168 pages as plain text (no links to non-existent pages), and states the D-06 beta-tracking caveat"
    requirement: WIKI-05
    verification:
      - kind: unit
        ref: "manual grep suite run against wiki/Home.md — link count 1, zero .md)-suffixed or [[ links, zero links to nonexistent stems, beta caveat present, no live-wiki claim (see Observed Output below)"
        status: pass
    human_judgment: false
  - id: D3
    description: "How-This-Wiki-Is-Published.md states the WIKI-02 authority rule, the overwrite warning, both publish commands, the naming/link-form conventions and the sidebar-is-generated rule, with zero planning vocabulary leaked"
    requirement: WIKI-05
    verification:
      - kind: unit
        ref: "manual grep suite run against wiki/How-This-Wiki-Is-Published.md — overwritten (3), beta (1), planning-vocabulary leak (0), .md)-suffix (0) (see Observed Output below)"
        status: pass
    human_judgment: false
  - id: D4
    description: "wiki/_Sidebar.md is generated by wiki.py sidebar against the real tree, byte-stable on a fresh regeneration, banner-free, and committed; wiki.py sidebar --check and wiki.py check both exit 0"
    requirement: WIKI-02
    verification:
      - kind: unit
        ref: "python3 tools/wiki/wiki.py sidebar --check (exit 0) + python3 tools/wiki/wiki.py check (exit 0) + cmp against a fresh regeneration into a temp dir (exit 0) — all three captured verbatim below"
        status: pass
    human_judgment: false
  - id: D5
    description: ".planning/v1.35/MIGRATION-TABLE.md exists with the five required columns, two filled demonstration rows, thirteen source-pre-filled TBD rows for the 13 Phase 168 files, an explicit non-registry statement, and no non-ASCII hyphen"
    requirement: WIKI-05
    verification:
      - kind: unit
        ref: "manual grep suite — 17 table rows, all 13 filenames present, 'publish...does not read' phrase matches, zero U+2010, zero references from wiki.py itself (see Observed Output below)"
        status: pass
    human_judgment: false
  - id: D6
    description: "tools/wiki/selftest.sh stays green (12 cases) — this plan changed no tooling"
    requirement: WIKI-05
    verification:
      - kind: unit
        ref: "bash tools/wiki/selftest.sh — 12 of 12 cases PASS, unchanged from plan 167-03's baseline"
        status: pass
    human_judgment: false

duration: 8min
completed: 2026-08-30
status: complete
---

# Phase 167 Plan 04: Wiki Bootstrap Content (Home, Publishing Page, Sidebar, Migration Table) Summary

**Authored the wiki's first real content — `Home.md` and `How-This-Wiki-Is-Published.md` — generated and committed `_Sidebar.md` from the real tree, and created the D-04 migration provenance table shell; `wiki.py check` now passes against real content instead of only a fixture, and `wiki/` ends the plan holding exactly the three files D-12 specifies.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-08-30T12:25:00Z
- **Completed:** 2026-08-30T12:33:00Z
- **Tasks:** 3
- **Files modified:** 4 (all created)

## Accomplishments
- Authored `wiki/Home.md` — orients a reader, links `How-This-Wiki-Is-Published` in the single legal form, names the 13 Phase 168 source files as a plain-text list (no premature wiki-page-name invention), and states the D-06 beta-tracking caveat
- Authored `wiki/How-This-Wiki-Is-Published.md` — the WIKI-02 authority rule, the web-UI overwrite warning, both publish commands, the hyphen-to-space naming convention, the single legal internal link form (with illegal-form examples escaped so the page doesn't fail its own rule), the sidebar-is-generated rule, and the D-06 branch fact — all in contributor-facing prose with zero planning vocabulary
- Generated and committed `wiki/_Sidebar.md` via `python3 tools/wiki/wiki.py sidebar` against the real tree; confirmed fresh, byte-stable on regeneration into a temp copy, and banner-free
- Created `.planning/v1.35/MIGRATION-TABLE.md` — the D-04 provenance table, with two demonstration rows for this plan's own pages and thirteen source-pre-filled `TBD` rows for the 13 files Phase 168 will migrate, plus an explicit statement that the publish path never reads this file and a named hyphen-hazard warning for the two at-risk titles (`AT28C04-ADAPTER`, `sram-nvram-behavior`)
- `python3 tools/wiki/wiki.py check` now exits 0 against real content for the first time — the exact command `.github/workflows/wiki-check.yml` will run in plan 167-05

## Task Commits

Each task was committed atomically:

1. **Task 1: Author Home.md and How-This-Wiki-Is-Published.md** - `6faddf7b` (feat)
2. **Task 2: Generate and commit wiki/_Sidebar.md, prove the freshness leg against the real tree** - `4d3d6679` (feat)
3. **Task 3: Create the D-04 migration provenance table** - `1638bfbf` (docs)

## Files Created/Modified
- `wiki/Home.md` - hand-authored reachability root; orients a reader, links the publishing page, names the 13 coming pages as plain text, states the beta caveat
- `wiki/How-This-Wiki-Is-Published.md` - D-12 scaffolding page; authority rule, overwrite warning, publish commands, naming convention, link-form rule, sidebar-generated rule, beta caveat
- `wiki/_Sidebar.md` - generated, two entries (`Home`, `How This Wiki Is Published`), committed
- `.planning/v1.35/MIGRATION-TABLE.md` - D-04 provenance table shell, two filled rows + thirteen TBD rows for Phase 168

## Decisions Made
- Named the 13 Phase 168 pages in `Home.md` by their current source filenames rather than pre-deciding their eventual `Title-Case-With-Hyphens` wiki page names — that decision (visible in `MIGRATION-TABLE.md`'s `TBD` columns) belongs to Phase 168, and inventing it here would risk `Home.md` naming a page under a name Phase 168 doesn't end up using
- Escaped every illegal-link-form example in `How-This-Wiki-Is-Published.md` (`[[Page Name]]`, `[text][ref]`) in inline code spans, and the one legal-form example in a fenced code block, so `wiki.py`'s own `strip_code_spans` removes them before link-form parsing — otherwise the page written to document the link rule would itself violate it
- Rewrote `MIGRATION-TABLE.md`'s opening paragraph from one soft-wrapped sentence into two single-line sentences after the acceptance grep `publish.{0,40}(does not|never) read` failed to match a case where "does not" and "read" were separated by a literal line break the first draft introduced

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] MIGRATION-TABLE.md's "publish does not read this table" sentence was invisible to its own acceptance grep**
- **Found during:** Task 3, running the plan's own acceptance-criteria grep suite before committing
- **Issue:** The first draft's paragraph explaining that `wiki.py publish` never reads `MIGRATION-TABLE.md` wrapped across two lines with "does not" ending one line and "read" starting the next. `grep -ciE 'publish.{0,40}(does not|never) read'` operates per line (no `-z`/multiline mode) and returned 0 — the acceptance criterion would have failed even though the file's prose correctly conveyed the fact.
- **Fix:** Split the paragraph into two shorter sentences, keeping "`wiki.py publish` does not read this table" on a single physical line.
- **Files modified:** .planning/v1.35/MIGRATION-TABLE.md
- **Verification:** `grep -ciE 'publish.{0,40}(does not|never) read' .planning/v1.35/MIGRATION-TABLE.md` returns `1`
- **Committed in:** 1638bfbf (Task 3 commit — fixed before the task was committed, no separate fix commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — a self-inflicted authoring bug caught by running the plan's own acceptance grep before committing, not a defect in prior plans' work)
**Impact on plan:** No scope creep. No files beyond the four the plan specified.

## Issues Encountered
None beyond the one auto-fixed authoring bug documented above.

## Pre-existing condition noted, not caused by this plan

`git status --porcelain firestarter firestarter_app` reports `M firestarter_app` (a submodule commit-pointer drift: the superproject records `cb189a9b0` while the checked-out submodule is at `d56424e19`, 183 commits ahead). This was confirmed present **before any task in this plan touched anything** (checked immediately after loading plan context, prior to creating `wiki/`). Per this plan's hard rules, `firestarter_app/` is out of scope and was not read, copied, or modified in the course of this plan's three tasks — the drift is left exactly as found. `.planning/config.json` also shows a one-line pre-existing modification, likewise untouched. Both are recorded here rather than silently worked around, since the plan's own acceptance criteria assert this command reports no modification and it does not, for reasons unrelated to this plan's three tasks.

## Observed Output

`python3 tools/wiki/wiki.py sidebar --check`:
```
OK: _Sidebar.md is fresh (2 entries).
```

`python3 tools/wiki/wiki.py check` (against the real `wiki/` tree, no `--source-dir` override):
```
OK: _Sidebar.md is fresh (2 entries).
Home -> "Home"
How-This-Wiki-Is-Published -> "How This Wiki Is Published"
OK: 2 pages, all reachable from Home.md, all internal links resolve, all filenames legal.
OK: all offline legs passed (2 legs).
```

Regeneration into a temp directory and `cmp` against the committed `wiki/_Sidebar.md`: exit `0` (byte-identical).

`bash tools/wiki/selftest.sh` final line: `OK: selftest complete (12 cases)` — all 12 cases PASS, unchanged from plan 167-03.

`ls wiki/`: `Home.md`, `How-This-Wiki-Is-Published.md`, `_Sidebar.md` — exactly three files.

`find wiki -mindepth 1 -type d`: no output (flat).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `wiki/` now holds real, checked content that `wiki.py check` validates against directly — plan 167-05's CI workflow (`.github/workflows/wiki-check.yml`) can run the exact same command against this tree with no fixture substitution.
- `.planning/v1.35/MIGRATION-TABLE.md` is ready for Phase 168 to fill in: all 13 source paths are pre-recorded, and the hyphen-hazard note names the two titles (`AT28C04-ADAPTER`, `sram-nvram-behavior`) that need a deliberate reworded title rather than a mechanical hyphen-to-space substitution.
- **WIKI-05 is still only `Pending` in REQUIREMENTS.md, deliberately not marked complete** — this plan carries `wiki.py`'s remaining validation against real content, but WIKI-05 also spans plan 167-05 per this plan's own frontmatter; do not mark it complete until that plan lands.
- **WIKI-02 is still only `Pending`** — spans through plan 167-06's operator-gated live demonstration.
- No blockers for the next wave. The pre-existing `firestarter_app` submodule pointer drift noted above is unrelated to this plan's scope and was left untouched; a future plan or the orchestrator should decide whether it needs remediation.

## Self-Check: PASSED

- FOUND: wiki/Home.md
- FOUND: wiki/How-This-Wiki-Is-Published.md
- FOUND: wiki/_Sidebar.md
- FOUND: .planning/v1.35/MIGRATION-TABLE.md
- FOUND: 6faddf7b
- FOUND: 4d3d6679
- FOUND: 1638bfbf

---
*Phase: 167-wiki-bootstrap-in-repo-source-sync-drift-check*
*Completed: 2026-08-30*
