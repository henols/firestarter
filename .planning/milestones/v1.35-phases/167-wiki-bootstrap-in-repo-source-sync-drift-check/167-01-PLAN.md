---
phase: 167-wiki-bootstrap-in-repo-source-sync-drift-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/wiki/wiki.py
  - tools/wiki/selftest.sh
autonomous: true
requirements: [WIKI-04]

must_haves:
  truths:
    - "D-01: wiki tooling lives under `tools/wiki/`, separate from content — `tools/wiki/wiki.py` and `tools/wiki/selftest.sh` exist and no tooling file is created inside `wiki/`."
    - "D-09: `_Sidebar.md` is machine-generated — `python3 tools/wiki/wiki.py sidebar` writes it from the `wiki/` filename set and nothing else, with no hand-authored entries."
    - "D-10: a stale committed `_Sidebar.md` is detectable — `wiki.py sidebar --check` exits 1 on staleness and writes no file, so the checker cannot repair what it checks."
    - "The selftest harness can itself go red: a deliberately wrong expectation makes `bash tools/wiki/selftest.sh` exit non-zero and name the offending case."
    - "Sidebar generation is byte-stable: two consecutive `wiki.py sidebar` runs over an unchanged source directory produce identical bytes."
  artifacts:
    - tools/wiki/wiki.py
    - tools/wiki/selftest.sh
  key_links:
    - "`wiki.py --source-dir` — parameterises the source tree so every negative case is buildable against a fixture instead of mutating the real `wiki/`."
    - "`selftest.sh` case ids match the ids in `167-VALIDATION.md` verbatim, so plan criteria, selftest output and CI logs are greppable by one string."
---

<objective>
Stand up the two files everything else in Phase 167 depends on: the single stdlib-only CLI
`tools/wiki/wiki.py` (D-01 — tooling under `tools/wiki/`, content under `wiki/`) and the fixture
test driver `tools/wiki/selftest.sh`. Deliver the first working capability — deterministic
`_Sidebar.md` generation (D-09) with a freshness check that cannot rewrite what it checks (D-10).

The harness comes first and is proved capable of failing before any capability is written. Two of
the eleven observed-red negative cases (`stale_sidebar_exit_1`, `sidebar_deterministic`) are
authored, run RED, and only then taken green.

Purpose: `wiki.py` is the one code path D-08 depends on; without `--source-dir` and `--wiki-remote`
parameterisation nothing in this phase is testable before the operator creates the wiki.
Output: `tools/wiki/wiki.py` (skeleton + `sidebar`), `tools/wiki/selftest.sh` (harness + 2 cases).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-CONTEXT.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-PATTERNS.md
@.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-VALIDATION.md
</context>

<hard_rules>
**Zero comments in authored source.** `tools/wiki/wiki.py` and `tools/wiki/selftest.sh` carry no
comment lines at all beyond the shebang. This is an operator hard rule and this plan cannot override
it. Explanation goes in the `wiki.py` module docstring (which doubles as usage text), in `argparse`
`description=`/`help=` strings, and in descriptive function and variable names. The existing
comments in `tools/catalog/sync_to_subrepos.sh`, `tools/catalog/codegen.py` and
`.github/workflows/catalog-sync-check.yml` are read as evidence and left untouched; they are not a
pattern to copy.

**Never replicate the self-comparison defect.** `tools/catalog/sync_to_subrepos.sh:84-86` and
`:97-99` compare a path to itself and print a success line for a property never tested. Every
assertion authored here must have two traceably distinct operands and a failure branch that has
been run and seen red.

**No `--allow-empty`, no `|| true`, no swallowed exit codes** anywhere in either file.

<!-- planner-discipline-allow: || true -->
<!-- planner-discipline-allow: allow-empty -->
<!-- planner-discipline-allow: expected exit 7 -->
</hard_rules>

<tasks>

<task type="auto">
  <name>Task 1: Create the fixture selftest harness and prove it can fail</name>
  <files>tools/wiki/selftest.sh</files>
  <read_first>
    - tools/catalog/sync_to_subrepos.sh (lines 1-35 — `#!/usr/bin/env bash` + `set -euo pipefail`, script-relative path resolution from `BASH_SOURCE`, `=== banner ===` progress; lines 60-70 — the one CORRECT two-distinct-operand assertion; lines 84-99 — the self-diff ANTI-PATTERN, read so it is recognised and not reproduced)
    - .planning/codebase/CONVENTIONS.md (lines 74-78 — shell conventions and banner style)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-VALIDATION.md (§"Negative Cases That Must Be OBSERVED RED" — the 11 case ids, verbatim, are the case function names)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Code Examples" → "Verified idempotent mirror publish" for the `git init --bare --initial-branch=master` fixture form; §"Anti-Patterns to Avoid")
  </read_first>
  <action>
Create `tools/wiki/selftest.sh` as the phase's only test driver. There is no test harness in this
repository, so this file defines the shape.

Header: `#!/usr/bin/env bash` then `set -euo pipefail`. No comment lines.

Resolve paths from the script's own location, never from `cwd`:
`SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`, `WIKI_PY="$SCRIPT_DIR/wiki.py"`,
`REPO_ROOT="$SCRIPT_DIR/../.."`.

Create a `mktemp -d` work root held in `WORK`, and a `trap 'rm -rf "$WORK"' EXIT` cleanup.

Provide these helper functions, all comment-free:

- `banner <text>` — echoes `=== <text> ===` to stdout.
- `new_source_dir <path>` — builds a minimal VALID fixture wiki source at `<path>`: `Home.md`
  containing a markdown link to `Page-One` in the legal form `[Page One](Page-One)`, `Page-One.md`
  containing any body with no internal links, and `_Sidebar.md` containing exactly the two lines
  `- [Home](Home)` and `- [Page One](Page-One)` in that order. This fixture must pass every offline
  leg cleanly — it is the control operand for every mutated case.
- `new_bare_wiki <path>` — `git init --bare --initial-branch=master -q "<path>"`. The
  `--initial-branch=master` is mandatory: `init.defaultBranch` is unset in this container and a
  GitHub wiki serves only `master`.
- `rc_of <command...>` — runs the command with output captured to a file under `$WORK`, and echoes
  the exit code. Because `set -e` aborts on non-zero, capture with the
  `rc=0; <cmd> >"$out" 2>&1 || rc=$?` form; never let the shell abort on an expected failure and
  never use `|| true`.
- `record <case_id> <expected_rc> <observed_rc> <control_rc> <note>` — appends a pipe-delimited row
  to a global `EVIDENCE` array.
- `assert_rc <case_id> <expected_rc> <observed_rc>` — on mismatch, prints
  `ERROR: <case_id>: expected exit <expected_rc>, observed <observed_rc>` to stderr and returns 1;
  on match prints `OK: <case_id> exit <observed_rc>` to stdout.
- `print_evidence_table` — prints a `case | expected | observed | control | note | verdict` table
  from `EVIDENCE`, one row per case, to stdout after all cases have run.

Driver loop: a `CASES=( ... )` array of case ids (empty at the end of this task), and a loop that
for each id calls `banner "$id"` then `"case_$id"`, accumulating a non-zero `exit_code` on failure
rather than dying on the first failure — accumulate then gate, following
`sync_to_subrepos.sh`'s `exit_code=1` / final-check structure. After the loop, call
`print_evidence_table`, then `echo "OK: selftest complete (<n> cases)"` on success or
`ERROR: selftest failed (<n> of <m> cases red)` to stderr, and `exit "$exit_code"`.

Every case function must assert BOTH legs: the control leg (unmutated fixture) exits 0, and the
mutated leg exits the expected non-zero code. A case that only asserts the mutated leg is the
`sync_to_subrepos.sh` defect wearing different clothes.

With zero cases registered, `bash tools/wiki/selftest.sh` must exit 0 and print the table header.

Then prove the harness's failure branch is reachable: temporarily register a throwaway case
function that runs `true` and asserts an expected exit of 7, run `bash tools/wiki/selftest.sh`,
capture the full output verbatim (it must exit non-zero and print an `ERROR:` line naming the
throwaway case id and `expected exit 7, observed 0`), then remove the throwaway case. Paste the
captured failing output into the plan SUMMARY under a heading `Harness negative control`. Do not
commit the throwaway case.
  </action>
  <verify>
    <automated>bash tools/wiki/selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/wiki/selftest.sh` exits 0 with zero cases registered.
    - `bash tools/wiki/selftest.sh` stdout contains the literal `case | expected | observed | control | note | verdict`.
    - `head -1 tools/wiki/selftest.sh` is exactly `#!/usr/bin/env bash`.
    - `grep -n 'set -euo pipefail' tools/wiki/selftest.sh` returns exactly one match.
    - Zero comment lines beyond the shebang: `tail -n +2 tools/wiki/selftest.sh | grep -cE '^[[:space:]]*#'` returns `0`.
    - `grep -c '|| true' tools/wiki/selftest.sh` returns `0`.
    - `grep -c 'init --bare' tools/wiki/selftest.sh` returns at least `1`, and every `git init` occurrence in the file is accompanied by `--initial-branch=master`: `grep -c 'git init' tools/wiki/selftest.sh` equals `grep -c 'initial-branch=master' tools/wiki/selftest.sh`.
    - The plan SUMMARY contains a `Harness negative control` section holding the verbatim captured output of the throwaway-case run, showing a non-zero exit and an `ERROR:` line reporting `expected exit 7, observed 0`.
    - `git grep -n 'expected exit 7' -- tools/wiki/selftest.sh` returns nothing (the throwaway case was removed before commit).
  </acceptance_criteria>
  <done>The harness exists, runs green with no cases, and its red state has been observed and captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the two sidebar negative cases and observe them RED</name>
  <files>tools/wiki/selftest.sh</files>
  <read_first>
    - tools/wiki/selftest.sh (the harness from Task 1 — helper signatures and the CASES array)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-VALIDATION.md (§"Negative Cases" rows 10 and the `sidebar_deterministic` row in §"Requirement Map")
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Pattern 5: Deterministic sidebar generation" — the determinism contract and the must-not-rewrite rule)
  </read_first>
  <behavior>
    - `case_stale_sidebar_exit_1`: control leg — a fresh valid fixture, `wiki.py sidebar --check --source-dir <fixture>` exits 0. Mutated leg — add `Page-Two.md` to the fixture source and do NOT regenerate `_Sidebar.md`; the same command exits 1 and prints the delta naming `Page-Two`. Third assertion: `_Sidebar.md` is byte-unchanged after the failing `--check` run, proving the checker wrote nothing.
    - `case_sidebar_deterministic`: run `wiki.py sidebar --source-dir <fixture>` twice over an unchanged fixture; both runs exit 0 and the generated `_Sidebar.md` bytes are identical between the two runs (`cmp` on a copy taken after run 1). Also assert the file ends with a single trailing LF and contains no CR bytes.
  </behavior>
  <action>
Add case functions `case_stale_sidebar_exit_1` and `case_sidebar_deterministic` to
`tools/wiki/selftest.sh` and register both ids in `CASES`, using the case ids verbatim as they
appear in `167-VALIDATION.md` so the selftest output, the plan criteria and the CI log are all
greppable by the same string.

Both cases invoke `python3 "$WIKI_PY" sidebar` with the `--source-dir` flag pointed at a fixture
directory built by `new_source_dir`. Neither case may touch the repository's own `wiki/` directory.

For `case_stale_sidebar_exit_1`, capture the pre-check bytes of the fixture `_Sidebar.md` with
`cp`, run the failing `--check`, then `cmp` the file against the copy — a checker that regenerates
the artifact it is checking reports green forever, so this assertion is load-bearing.

For `case_sidebar_deterministic`, assert byte-equality with `cmp` between the run-1 copy and the
run-2 output, and assert absence of CR with `grep -c $'\r' <file>` returning 0.

Record both cases in `EVIDENCE` via `record`, with `control` populated from the unmutated leg.

Now run `bash tools/wiki/selftest.sh`. `tools/wiki/wiki.py` does not exist yet, so both cases must
go RED. Capture the full verbatim output and paste it into the plan SUMMARY under a heading
`Observed RED: stale_sidebar_exit_1, sidebar_deterministic`, alongside the exit code of the
selftest run. Commit the RED state — this commit is the evidence that these assertions were
reachable before the capability existed.
  </action>
  <verify>
    <automated>rc=0; bash tools/wiki/selftest.sh >/tmp/167-01-t2.log 2>&1 || rc=$?; test "$rc" -ne 0 && grep -q 'stale_sidebar_exit_1' /tmp/167-01-t2.log && grep -q 'sidebar_deterministic' /tmp/167-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/wiki/selftest.sh` exits non-zero at the end of this task (RED is the expected state).
    - The captured selftest output names both `stale_sidebar_exit_1` and `sidebar_deterministic`.
    - `grep -c 'case_stale_sidebar_exit_1' tools/wiki/selftest.sh` returns at least `1`; likewise `case_sidebar_deterministic`.
    - `grep -c 'source-dir' tools/wiki/selftest.sh` returns at least `2`.
    - No fixture path in either case resolves inside the repository: `grep -c 'REPO_ROOT/wiki' tools/wiki/selftest.sh` returns `0`.
    - Zero comment lines beyond the shebang: `tail -n +2 tools/wiki/selftest.sh | grep -cE '^[[:space:]]*#'` returns `0`.
    - The plan SUMMARY carries an `Observed RED: stale_sidebar_exit_1, sidebar_deterministic` section with the verbatim failing output and the observed exit code.
  </acceptance_criteria>
  <done>Both cases exist, are registered, and their RED output has been captured and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create wiki.py with the CLI skeleton, exit-code contract and sidebar generation</name>
  <files>tools/wiki/wiki.py</files>
  <read_first>
    - tools/catalog/codegen.py (lines 1-45 — shebang, stdlib-only import block, module docstring used as usage text and as the home for the determinism contract; lines 460-470 — the deterministic sort helper; lines 665-735 — `LANGUAGE_EMITTERS` dispatch map, `_build_argparser`, `main()` returning 0/1/2, the `--check` branch that returns BEFORE any write, `args.target.write_text(..., newline="\n")`, and `sys.exit(main())`)
    - .planning/codebase/CONVENTIONS.md (lines 66-70 — `#!/usr/bin/env python3`, module docstring as usage text, `from __future__ import annotations`, stdlib only, snake_case; lines 86-92 — Black-by-editor, no formatter config to be added)
    - tools/wiki/selftest.sh (the two cases from Task 2 — the exact flags and exit codes they assert)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-RESEARCH.md (§"Pattern 3" exit-code table; §"Pattern 5" determinism contract; §"Security Domain" V5 and V7)
    - .planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-PATTERNS.md (§`tools/wiki/wiki.py` — CLI skeleton and exit-code contract; §`sidebar` generator)
  </read_first>
  <behavior>
    - `python3 tools/wiki/wiki.py --help` lists the subcommand `sidebar` and the global flags `--source-dir` and `--wiki-remote`, and its text states the exit-code contract 0 / 1 / 2.
    - `python3 tools/wiki/wiki.py sidebar --source-dir <fixture>` writes `<fixture>/_Sidebar.md` and exits 0.
    - `python3 tools/wiki/wiki.py sidebar --check --source-dir <fixture>` exits 0 when the committed file matches the generated content, exits 1 and prints the delta when it does not, and never writes.
    - Generated output is `- [Home](Home)` first, then remaining page stems in `sorted()` order, one `- [<Rendered Title>](<Page-Stem>)` line each, `_Sidebar.md` and `_Footer.md` excluded from the list, LF endings, single trailing newline, no timestamp, no hostname, no hash, no banner line.
    - Two consecutive generation runs over an unchanged source directory produce byte-identical output.
  </behavior>
  <action>
Create `tools/wiki/wiki.py`. Header: `#!/usr/bin/env python3`, then a module docstring, then
`from __future__ import annotations`, then stdlib imports only (`argparse`, `re`, `subprocess`,
`sys`, `pathlib.Path`). No comment lines anywhere in the file.

The module docstring is the sanctioned home for everything a comment would otherwise carry, and it
doubles as usage text. It must state, as prose or as an indented table:
the exit-code contract (`0` = asserted property holds, `1` = asserted property is false, `2` =
precondition not met / wiki remote absent); the determinism contract for generated output (sorted
deterministically, no timestamps or hostnames or hashes, LF endings via
`write_text(..., newline="\n")`); and the reason `--wiki-remote` and `--source-dir` are parameters
rather than constants (they are what makes every offline negative case testable before the operator
creates the wiki). No docstring line may begin with `#`.

Module constants:
- `DEFAULT_WIKI_REMOTE = "https://github.com/henols/firestarter_prom.wiki.git"`
- `WIKI_BRANCH = "master"`
- `DEFAULT_SOURCE_DIR = Path(__file__).resolve().parent.parent.parent / "wiki"` — resolved from the
  script's own location so the tool runs correctly from any `cwd`, exactly as
  `sync_to_subrepos.sh` resolves `SCRIPT_DIR` from `BASH_SOURCE`.
- `HOME_PAGE = "Home.md"`
- `SIDEBAR_PAGE = "_Sidebar.md"`
- `NAV_EXCLUDED_PAGES = ("_Sidebar.md", "_Footer.md")`

Functions:
- `render_title(stem: str) -> str` — the page title GitHub will display, derived mechanically from
  the filename per D-04: hyphens become spaces. This function exists to *report* the rendered
  title, never to preview or validate GitHub's rendering.
- `page_files(source_dir: Path) -> list[Path]` — every `*.md` directly inside `source_dir`, sorted
  by name. Non-recursive: D-02 makes `wiki/` flat, so a subdirectory is not traversed.
- `generate_sidebar(source_dir: Path) -> str` — builds a `parts: list[str]` and returns
  `"".join(parts)`. `Home` first if `Home.md` exists, then the remaining stems in `sorted()` order,
  each as `- [<render_title(stem)>](<stem>)\n`. Entries in `NAV_EXCLUDED_PAGES` are omitted from the
  list. No banner, no header, no timestamp, no trailing blank line beyond the single final LF.
- `cmd_sidebar(args) -> int` — when `args.check` is set, generate into memory, compare against the
  committed file, and RETURN before any write, mirroring `codegen.py`'s `--check` control flow
  exactly; on mismatch print `ERROR: _Sidebar.md is stale` to stderr followed by a unified diff
  produced with `difflib.unified_diff`, and return 1; on match print
  `OK: _Sidebar.md is fresh (<n> entries).` and return 0. When `--check` is not set, write with
  `write_text(content, encoding="utf-8", newline="\n")`, print `OK: wrote <path> (<n> entries).`
  and return 0.
- `_build_argparser()` — `argparse.ArgumentParser(prog="wiki.py", description=...)` with
  `add_subparsers(dest="command", required=True)`. Global options on the top-level parser:
  `--source-dir` (`type=Path`, `default=DEFAULT_SOURCE_DIR`, help naming it as the in-repo wiki
  source tree and stating that fixtures override it) and `--wiki-remote` (`default=DEFAULT_WIKI_REMOTE`,
  help stating that a local bare repository path is accepted so the publish path is testable before
  the GitHub wiki exists). Register only the `sidebar` subcommand in this plan, with its own
  `--check` flag whose help text ends with the phrase `No files written.` — that phrase is
  load-bearing and is copied from `codegen.py`.
- `COMMANDS = {"sidebar": cmd_sidebar}` — the dispatch map, following `codegen.py`'s
  `LANGUAGE_EMITTERS` idiom. Later plans add entries; nothing else changes.
- `main()` — parse args; if `args.source_dir` is not an existing directory, print
  `ERROR: source directory not found: <path>` to stderr and return 2; otherwise
  `return COMMANDS[args.command](args)`.
- `if __name__ == "__main__": sys.exit(main())`.

Message discipline, copied from both analogs: `ERROR: <what> <offending value>` to stderr,
`OK: <property asserted>` to stdout and last. Never a bare `print` of a failure without the
`ERROR:` prefix.

Add `difflib` to the stdlib import list. Do not add any third-party import; do not create a
`pyproject.toml`, `pytest.ini`, `tests/` directory or any formatter configuration — the meta repo
deliberately has none and this phase adds none.

Then re-run `bash tools/wiki/selftest.sh`. Both cases from Task 2 must now be GREEN and the driver
must exit 0. Capture the verbatim green output including the evidence table and paste it into the
plan SUMMARY under a heading `Observed GREEN: stale_sidebar_exit_1, sidebar_deterministic`, directly
beneath the RED capture from Task 2 so the red-then-green pair reads as one record.
  </action>
  <verify>
    <automated>bash tools/wiki/selftest.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/wiki/selftest.sh` exits 0, and its evidence table shows `stale_sidebar_exit_1` with expected `1` / observed `1` / control `0`, and `sidebar_deterministic` with expected `0` / observed `0`.
    - `python3 tools/wiki/wiki.py --help` exits 0 and its output contains `sidebar`, `--source-dir` and `--wiki-remote`.
    - `python3 tools/wiki/wiki.py sidebar --help` output contains the literal `No files written.`
    - `head -1 tools/wiki/wiki.py` is exactly `#!/usr/bin/env python3`.
    - `grep -c 'from __future__ import annotations' tools/wiki/wiki.py` returns `1`.
    - Zero comment lines beyond the shebang: `tail -n +2 tools/wiki/wiki.py | grep -cE '^[[:space:]]*#'` returns `0`.
    - Stdlib only: `grep -cE '^(import|from) ' tools/wiki/wiki.py` equals the count of lines matching `^(import|from) (argparse|difflib|re|subprocess|sys|pathlib|__future__)`.
    - `grep -c 'newline="\\n"' tools/wiki/wiki.py` returns at least `1`.
    - `grep -c 'WIKI_BRANCH = "master"' tools/wiki/wiki.py` returns `1`.
    - No test-harness scaffolding was introduced: `ls pyproject.toml pytest.ini tests 2>&1` reports all three absent.
    - A freshly generated sidebar over the Task-1 fixture has `- [Home](Home)` as its first line and contains no CR bytes.
    - The plan SUMMARY carries the `Observed GREEN: stale_sidebar_exit_1, sidebar_deterministic` section with the verbatim evidence table.
  </acceptance_criteria>
  <done>`wiki.py` exists with the skeleton, exit-code contract and a deterministic, non-self-repairing sidebar generator; both Task-2 cases are green after being seen red.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol below is NEW in Phase 167. None of it exists in the repository before this phase, so
none of it can be verified against prior source.

**New files**
- `tools/wiki/wiki.py`
- `tools/wiki/selftest.sh`
- `.planning/v1.35/MIGRATION-TABLE.md`
- `wiki/Home.md`
- `wiki/How-This-Wiki-Is-Published.md`
- `wiki/_Sidebar.md`
- `.github/workflows/wiki-check.yml`
- `.github/workflows/wiki-publish.yml`

**New directories:** `wiki/`, `tools/wiki/`

**`wiki.py` subcommands:** `sidebar`, `links`, `check`, `publish`
**`wiki.py` flags:** `--source-dir`, `--wiki-remote`, `--check` (on `sidebar`), `--push` (on `publish`), `--require-wiki` (on `publish`)
**`wiki.py` exit-code contract:** `0` = asserted property holds; `1` = asserted property is false; `2` = precondition not met (source directory missing, or wiki remote absent)
**`wiki.py` constants:** `DEFAULT_WIKI_REMOTE`, `WIKI_BRANCH`, `DEFAULT_SOURCE_DIR`, `HOME_PAGE`, `SIDEBAR_PAGE`, `NAV_EXCLUDED_PAGES`, `ILLEGAL_NAME_CHARS`, `LEGAL_LINK_RE`, `COMMANDS`
**`wiki.py` functions:** `render_title`, `page_files`, `page_stems`, `generate_sidebar`, `cmd_sidebar`, `extract_internal_links`, `strip_code_spans`, `check_page_names`, `check_link_forms`, `check_orphans`, `cmd_links`, `cmd_check`, `cmd_publish`, `_git`, `_build_argparser`, `main`

**`selftest.sh` helpers:** `banner`, `new_source_dir`, `new_bare_wiki`, `rc_of`, `record`, `assert_rc`, `print_evidence_table`, `CASES`, `EVIDENCE`
**`selftest.sh` case ids:** `drift_detected_exit_1`, `hand_edit_overwritten`, `idempotent_head_unchanged`, `deleted_page_removed`, `orphan_exit_1`, `sidebar_link_is_not_evidence`, `broken_link_exit_1`, `md_suffix_link_exit_1`, `illegal_filename_exit_1`, `stale_sidebar_exit_1`, `wiki_absent_exit_2`, `sidebar_deterministic`

**Workflow ids:** file `wiki-check.yml`, `name: Wiki check`, job `wiki-check`; file `wiki-publish.yml`, `name: Wiki publish`, job `wiki-publish`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo `wiki/` filenames → `subprocess` argv | Repo-controlled but still untrusted input to a process invocation |
| generated artifact → committed file | A checker that repairs what it checks silently destroys the assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-01 | Tampering | `wiki.py` process invocation | high | mitigate | `subprocess` list form only; never `shell=True`; no shell string with an interpolated filename. No `subprocess` call is introduced in this plan; the constraint is established in the module now so plan 03 inherits it. |
| T-167-06 | Repudiation | `sidebar --check` freshness leg | high | mitigate | `cmd_sidebar` returns before any write when `--check` is set, copying `codegen.py`'s control flow; `case_stale_sidebar_exit_1` asserts the committed file is byte-unchanged after a failing check, and its RED state is captured before the capability exists. |
| T-167-09 | Repudiation | `selftest.sh` driver itself | high | mitigate | Task 1's harness negative control: a deliberately wrong expectation is run and the harness is observed exiting non-zero and naming the case. A driver whose own failure branch has never been seen is the `sync_to_subrepos.sh` defect one level up. |
| T-167-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero packages. `167-RESEARCH.md` §"Package Legitimacy Audit" records `pytest` as alternative-only, priced and rejected; no install task exists in any plan of this phase. |
</threat_model>

<verification>
- `bash tools/wiki/selftest.sh` exits 0 with 2 cases green and an evidence table printed.
- `python3 tools/wiki/wiki.py --help` and `python3 tools/wiki/wiki.py sidebar --help` both exit 0.
- Zero comment lines beyond the shebang in both authored files.
- `git status --porcelain` shows only `tools/wiki/wiki.py` and `tools/wiki/selftest.sh` as additions from this plan; `wiki/` is not created here.
- SUMMARY carries three captured runs: `Harness negative control`, `Observed RED: …`, `Observed GREEN: …`.
</verification>

<success_criteria>
- `tools/wiki/wiki.py` exists with the `sidebar` subcommand, the 0/1/2 exit contract, `--source-dir` and `--wiki-remote` parameterisation, and a determinism contract in its docstring.
- `tools/wiki/selftest.sh` exists, exits 0, and has been observed exiting non-zero on a deliberately wrong expectation.
- Negative cases `stale_sidebar_exit_1` and `sidebar_deterministic` each have a captured RED run and a captured GREEN run, in that order.
</success_criteria>

<output>
Create `.planning/phases/167-wiki-bootstrap-in-repo-source-sync-drift-check/167-01-SUMMARY.md` when done.
</output>
