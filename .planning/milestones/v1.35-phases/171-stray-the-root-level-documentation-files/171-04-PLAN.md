---
phase: 171-stray-the-root-level-documentation-files
plan: 04
type: execute
wave: 4
depends_on: ["171-03"]
commits_land_in: meta (/workspaces), branch gsd/v1.35-documentation-consolidation-wiki-migration
files_modified:
  - firestarter
  - firestarter_app
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-gitlink-equality.txt
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-validation-sweep.txt
autonomous: true
requirements: [LEGACY-04, LEGACY-05, LEGACY-07]
user_setup: []

must_haves:
  truths:
    - "Meta's recorded gitlink for `firestarter_app` equals the submodule's checked-out HEAD, which is the Phase 171 deletion commit"
    - "Meta's recorded gitlink for `firestarter` equals the firmware submodule's checked-out HEAD — the bump sweeps up Phase 170's un-pinned commit, and says so in its message"
    - "Every automated row V-01 through V-16 and V-19 is green against final phase state, and its output is recorded"
    - "The two slow rows V-17 and V-18 are discharged by the evidence files plan 171-02 recorded, not re-run"
    - "LEGACY-04, LEGACY-05 and LEGACY-07 are all satisfied and this is the plan that says so"
  artifacts:
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-gitlink-equality.txt"
      provides: "The recorded-vs-actual equality assertion for both submodules, with the pre-bump values alongside so the Phase 170 sweep-up is visible"
      min_lines: 4
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-validation-sweep.txt"
      provides: "The closing battery — every fast V-row re-run against final state, since no CI covers any of it"
      min_lines: 17
  key_links:
    - from: "meta `HEAD`'s gitlink for `firestarter_app`"
      to: "the Phase 171 deletion commit in the submodule"
      via: "an equality assertion between `git ls-tree HEAD firestarter_app` and `git -C firestarter_app rev-parse HEAD`, not the presence of an `M` marker"
      pattern: "git ls-tree HEAD firestarter_app | awk '{print $3}'"
  prohibitions:
    - statement: "The bump does not sweep in `.planning/notes/dev-test-sequence-cost-model.md`, which is modified in meta's working tree and predates this phase."
      status: enforced
      verification: "Task 1 acceptance criterion — the commit stat names exactly `firestarter` and `firestarter_app`, and `git status --short` still reports the note as modified afterwards."
    - statement: "Phase 170's record is not amended. Its two un-pinned README commits are carried by this bump and named in the commit message, but `.planning/notes/v135-phases-169-170-executed-ad-hoc.md` already reconciles that phase and is left alone."
      status: enforced
      verification: "`git diff HEAD~1 --name-only` in meta does not name any file under `.planning/notes/`."
    - statement: "No security-reporting statement is added anywhere by this plan, including in the commit message or the sweep evidence (D-02)."
      status: enforced
      verification: "V-05 is re-run as part of the closing sweep and must still return zero hits."
---

<objective>
Re-pin both submodule gitlinks in meta so the repository records the state the phase actually
produced, then re-run every fast validation row against final state and record the result.

Purpose: without the re-pin, meta still points at a `firestarter_app` tree that contains all three
deleted files, and the phase would be invisible from the meta repository. Without the closing sweep,
nothing has checked the phase end-to-end — there is no CI covering any part of it, so the recorded
local output is the only evidence that exists.

Output: one gitlink commit, one evidence commit, and the phase's requirement marks.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-VALIDATION.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-01-SUMMARY.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-02-SUMMARY.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-03-SUMMARY.md
</context>

<!-- planner-discipline-allow: things.md -->
<!-- planner-discipline-allow: autocomplete.md -->
<!-- planner-discipline-allow: SECURITY.md -->
<!-- planner-discipline-allow: report a vulnerability -->
<!-- planner-discipline-allow: security policy -->
<!-- planner-discipline-allow: responsible disclosure -->

## Precedence note for the executor

`171-RESEARCH.md`'s corrections table (line 9) outranks `171-CONTEXT.md`. Two corrections bind this
plan:

- **C-6.** Meta's gitlinks were **already one commit stale each before this phase began**. Meta
  `HEAD` recorded `firestarter bbcdc39f292dfad378f84124bc5a4c7fbc3244ad` and `firestarter_app
  50f85b20b2948e31d2db77f1cf685e306ab01705`, while the checked-out submodules stood at `c26562ae`
  and `767079a0`. Both gaps are Phase 170's two ad-hoc README commits, never pinned. `git status` in
  meta has therefore shown `M firestarter` / `M firestarter_app` since before Phase 171 started.
  Those markers are **not** this phase's uncommitted work, and they must not be panic-reverted. This
  bump legitimately carries them, and the commit message must say so.
- **C-1.** There is no CI covering this phase. `wiki-check.yml` is not registered with GitHub Actions
  (absent from the default branch) and the app's `ci.yml` skips markdown-only commits. The closing
  sweep in Task 2 is the phase's only end-to-end check, and its recorded output is the only evidence.

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Shell-Completion.md` and both wiki navigation entries | live wiki | plan 171-01 |
| `evidence/171-01-*.txt`, `evidence/171-02-*.txt`, `evidence/171-03-*` | meta | plans 171-01 … 171-03 |
| The `Shell-Completion` main-table row and the `## Removed without ever being published` section in `.planning/v1.35/MIGRATION-TABLE.md` | meta | plan 171-03 |
| `evidence/171-04-gitlink-equality.txt` | meta | this plan, Task 1 |
| `evidence/171-04-validation-sweep.txt` | meta | this plan, Task 2 |

<tasks>

<task type="auto">
  <name>Task 1: Re-pin both submodule gitlinks in one meta commit</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md` § D.11 and § "Pitfall 5" — the measured pre-phase gitlink values and how this exact bump goes wrong
    - `.planning/notes/v135-phases-169-170-executed-ad-hoc.md` — the existing reconciliation of Phases 169 and 170; read it so you can cite it rather than duplicate it, and **do not amend it**
    - the output of `git -C /workspaces ls-tree HEAD firestarter firestarter_app` and `git -C /workspaces status --short`, taken before staging anything
  </read_first>
  <files>firestarter, firestarter_app, .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-gitlink-equality.txt</files>
  <action>
    Snapshot first. Record `git -C /workspaces ls-tree HEAD firestarter firestarter_app` and both
    `git -C <submodule> rev-parse HEAD` values **before** staging. You need the pre-bump values to
    show honestly what this commit moves.

    Stage the two gitlinks by path — `git -C /workspaces add firestarter firestarter_app` — and
    nothing else. `.planning/notes/dev-test-sequence-cost-model.md` is modified in meta's working
    tree, predates this phase and is unrelated to it; it must not be swept in. Never `git add -A` and
    never `git commit -a` here.

    Commit with subject `chore(171): advance submodule pointers`. Precedent: `f62021b4`. The body
    must state plainly that the bump moves more than Phase 171's own work: meta's gitlinks were
    already one commit behind each submodule before this phase began, because Phase 170's two ad-hoc
    README commits were never pinned. Name both transitions with their short SHAs — `firestarter`
    from `bbcdc39f` to `c26562ae` (Phase 170's firmware README cut, no Phase 171 change: this phase
    never touched the firmware repo), and `firestarter_app` from `50f85b20` through Phase 170's app
    README commit `767079a0` to Phase 171's deletion commit. Cite
    `.planning/notes/v135-phases-169-170-executed-ad-hoc.md` as where Phase 170 is already
    reconciled, and do not amend that note or Phase 170's ROADMAP entry.

    Then discharge V-19 with an **equality** assertion, not with the absence of an `M` marker: for
    each submodule, compare `git ls-tree HEAD <name>` field 3 against
    `git -C <name> rev-parse HEAD` and require them equal. Write the pre-bump values, the post-bump
    values and the equality result for both submodules to
    `evidence/171-04-gitlink-equality.txt`, then commit that file to meta with a path-scoped commit.

    Asserting "there is no `M`" would be the wrong check: the marker was present before this phase
    started, so its presence proves nothing and its absence could be produced by a bump to the wrong
    commit.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-gitlink-equality.txt; ok=1; for m in firestarter firestarter_app; do rec=$(git ls-tree HEAD "$m" | awk '{print $3}'); act=$(git -C "$m" rev-parse HEAD); printf "%-16s recorded=%s actual=%s %s\n" "$m" "$rec" "$act" "$([ "$rec" = "$act" ] && echo OK || echo STALE)"; [ "$rec" = "$act" ] || ok=0; done | tee -a "$EV"; test "$ok" = 1 && test "$(git ls-tree HEAD firestarter_app | awk '{print $3}')" = "$(git -C firestarter_app rev-parse HEAD)" && test "$(git ls-tree HEAD firestarter | awk '{print $3}')" = "$(git -C firestarter rev-parse HEAD)" && git status --short | grep -qx ' M .planning/notes/dev-test-sequence-cost-model.md'</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-tree HEAD firestarter_app | awk '{print $3}'` equals `git -C firestarter_app rev-parse HEAD` — this is V-19, and it is an equality assertion, not an `M`-marker check
    - `git ls-tree HEAD firestarter | awk '{print $3}'` equals `git -C firestarter rev-parse HEAD`
    - `git -C /workspaces status --short` no longer reports `M firestarter` or `M firestarter_app`
    - `git -C /workspaces status --short` still reports ` M .planning/notes/dev-test-sequence-cost-model.md` — the unrelated pending edit was not swept in
    - The gitlink commit's `git show --stat --format="" HEAD~1` (or `HEAD`, depending on whether the evidence commit followed) names exactly `firestarter` and `firestarter_app` and no other path
    - The commit message names both pre-bump short SHAs `bbcdc39f` and `50f85b20`, states that Phase 170's un-pinned commits are carried by this bump, and cites `.planning/notes/v135-phases-169-170-executed-ad-hoc.md`
    - `git diff HEAD~1 --name-only` names no file under `.planning/notes/` — Phase 170's record was not amended
    - `evidence/171-04-gitlink-equality.txt` shows both pre-bump and post-bump values, and `OK` for both submodules
  </acceptance_criteria>
  <done>Meta records exactly the submodule commits the working tree holds, the bump's extra scope is stated in its own message, and the equality is on the record.</done>
</task>

<task type="auto">
  <name>Task 2: Closing validation sweep across every fast V-row, recorded as evidence</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-VALIDATION.md` § "Per-Task Verification Map" — the 19 rows, their exact commands and their expected outputs
    - `.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-02-sdist-clean-tree-diff.txt` and `.../evidence/171-02-py311-suite.txt` — the two slow rows' recorded results, which this task cites rather than re-runs
    - `.planning/REQUIREMENTS.md:58,59,61` — LEGACY-04, LEGACY-05 and LEGACY-07, whose checkboxes flip at this plan's close and not before
  </read_first>
  <files>.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-validation-sweep.txt</files>
  <action>
    Re-run every fast validation row against final phase state and write each row's identifier,
    command and result to `evidence/171-04-validation-sweep.txt`, one labelled block per row. There
    is no CI covering any part of this phase, so this recorded output is the phase's only end-to-end
    evidence.

    Rows to re-run, grouped by where they run:

    - **`firestarter_app` branch tip** — V-01 and V-02 (`things.md` gone and its deletion committed),
      V-04 (`SECURITY.md` gone from the branch tip), V-08 (`autocomplete.md` gone).
    - **Frozen-blob recoverability** — V-03, V-06 and V-09: the three `sha256sum | cut -c1-16` checks
      against `d56424e1979edf7245cffb9ec3111c0469f5b23f`, expecting `637974e9dcab7870`,
      `35077cac80e15a8a` and `6e3a0116f2a3759f`.
    - **Meta** — V-07 (the canonical Phase 69 audit record still exists), V-15 (the SHA appears at
      least 8 times in `MIGRATION-TABLE.md` and the `Shell-Completion` main-table row is present),
      V-16 (`parse_migration_table` returns 8 rows, none a deletion row), V-19 (both gitlink equality
      assertions).
    - **The D-02 guard** — V-05: the negated case-insensitive `git grep` over `firestarter_app`
      excluding its own `.planning/`, which must still find nothing.
    - **Three repositories** — V-14: the link sweep for the three filenames, whose only permitted hit
      is `firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md:637`.
    - **The live wiki, from a fresh shallow clone** — V-10 (`Shell-Completion.md` present), V-11
      (`wiki.py links` rc=0 with `OK: 10 pages,` and `Shell-Completion -> "Shell Completion"`), V-12
      (the three-line head assertion), V-13 (all six `###` sections present).

    Do **not** re-run V-17 or V-18. The clean-tree sdist build and the py3.11 venv suite each cost
    real time and neither can have changed since plan 171-02 recorded them: no plan after 171-02
    touches `firestarter_app`. Cite the two evidence files by path in the sweep output, with their
    headline results, and mark both rows as discharged-by-evidence rather than re-run. State that
    distinction explicitly in the file so a reader is not misled into thinking they were re-executed.

    Do **not** add a `gh api …/community/profile` leg, and do **not** attempt any `gh workflow run` —
    the first is vacuous and the second has nothing registered to run.

    Close out the requirements. LEGACY-04, LEGACY-05 and LEGACY-07 each span plans 171-01 through
    171-04 and their `.planning/REQUIREMENTS.md` checkboxes are flipped **here**, at this plan's
    close, and nowhere earlier. Each mark rests on: the deletion (V-01/V-04/V-08), the recoverable
    oracle (V-03/V-06/V-09), the provenance row (V-15/V-16), and for LEGACY-07 additionally the live
    page (V-10 through V-13).

    Commit the evidence file to meta with a path-scoped commit.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-validation-sweep.txt; test -s "$EV" && ( cd firestarter_app && ! test -e things.md && ! git cat-file -e HEAD:things.md 2>/dev/null && ! test -e SECURITY.md && ! git cat-file -e HEAD:SECURITY.md 2>/dev/null && ! test -e autocomplete.md && ! git cat-file -e HEAD:autocomplete.md 2>/dev/null && git log -1 --diff-filter=D --name-only --format="" HEAD -- things.md | grep -qx things.md ) && test "$(git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:things.md | sha256sum | cut -c1-16)" = "637974e9dcab7870" && test "$(git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:SECURITY.md | sha256sum | cut -c1-16)" = "35077cac80e15a8a" && test "$(git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:autocomplete.md | sha256sum | cut -c1-16)" = "6e3a0116f2a3759f" && test -f .planning/milestones/v1.12-phases/69-cli-command-surface-robustness-audit/69-SECURITY.md && ! git -C firestarter_app grep -riq "report a vulnerability\|security policy\|responsible disclosure" -- ':(exclude).planning' && test "$(for f in things.md autocomplete.md SECURITY.md; do git -C /workspaces grep -n -- "$f" -- . ':(exclude).planning' 2>/dev/null; git -C /workspaces/firestarter_app grep -n -- "$f" 2>/dev/null; git -C /workspaces/firestarter grep -n -- "$f" 2>/dev/null; done | grep -vc 'test_eeprom28c_sdp/RED-BASELINE.md')" -eq 0 && test "$(grep -c 'd56424e1979edf7245cffb9ec3111c0469f5b23f' .planning/v1.35/MIGRATION-TABLE.md)" -ge 8 && grep -qF '| firestarter_app | firestarter_app/autocomplete.md | Shell-Completion | Shell Completion | d56424e1979edf7245cffb9ec3111c0469f5b23f | 171 |' .planning/v1.35/MIGRATION-TABLE.md && python3 -c "import sys; sys.path.insert(0,'tools/wiki'); from pathlib import Path; from honest01_claims import parse_migration_table; rows=parse_migration_table(Path('.planning/v1.35/MIGRATION-TABLE.md')); assert len(rows)==8, len(rows); assert not [r for r in rows if r['Wiki page'] in ('things.md','SECURITY.md')]" && test "$(git ls-tree HEAD firestarter_app | awk '{print $3}')" = "$(git -C firestarter_app rev-parse HEAD)" && test "$(git ls-tree HEAD firestarter | awk '{print $3}')" = "$(git -C firestarter rev-parse HEAD)"</automated>
    <automated>V=$(mktemp -d); cd /workspaces && git clone --depth 1 https://github.com/henols/firestarter_prom.wiki.git "$V/wiki" >/dev/null 2>&1 && test -f "$V/wiki/Shell-Completion.md" && python3 tools/wiki/wiki.py links --source-dir "$V/wiki" > "$V/links.txt" 2>&1 && grep -q '^OK: 10 pages,' "$V/links.txt" && grep -qF 'Shell-Completion -> "Shell Completion"' "$V/links.txt" && sed -n '3,5p' "$V/wiki/Shell-Completion.md" | tr '\n' '|' | grep -qx -- '---||# Shell Completion|' && for s in Bash Zsh Fish PowerShell "pipx Installations" "Migrating from a previous Firestarter"; do grep -qF "### $s" "$V/wiki/Shell-Completion.md" || { echo "MISSING: $s"; exit 1; }; done</automated>
    <automated>cd /workspaces && EV=.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-04-validation-sweep.txt; for r in V-01 V-02 V-03 V-04 V-05 V-06 V-07 V-08 V-09 V-10 V-11 V-12 V-13 V-14 V-15 V-16 V-17 V-18 V-19; do grep -q "$r" "$EV" || { echo "sweep file does not mention $r"; exit 1; }; done && grep -q '171-02-sdist-clean-tree-diff.txt' "$EV" && grep -q '171-02-py311-suite.txt' "$EV" && ! grep -q 'community/profile' "$EV"</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/171-04-validation-sweep.txt` mentions every row identifier V-01 through V-19, so coverage is auditable by grep
    - Rows V-01, V-02, V-04 and V-08 are green against the final `firestarter_app` branch tip
    - Rows V-03, V-06 and V-09 reproduce `637974e9dcab7870`, `35077cac80e15a8a` and `6e3a0116f2a3759f` exactly
    - Row V-05 finds zero hits, and the sweep file labels it as a must-NOT guard rather than as proof the work was done
    - Row V-07 confirms `.planning/milestones/v1.12-phases/69-cli-command-surface-robustness-audit/69-SECURITY.md` still exists
    - Row V-14's sweep returns only the one documented `RED-BASELINE.md:637` hit
    - Rows V-10 through V-13 are re-run against a **fresh shallow clone** of the live wiki, not against any clone left behind by plan 171-01, and `wiki.py links` exits 0 with `OK: 10 pages,`
    - Rows V-15 and V-16 are green: at least 8 SHA occurrences, the `Shell-Completion` main-table row present, and `parse_migration_table` returning exactly 8 rows with no deletion row among them
    - Row V-19's equality holds for both submodules
    - Rows V-17 and V-18 are recorded as **discharged by the evidence files plan 171-02 wrote**, naming both paths and their headline results, and are explicitly labelled as not re-run
    - The sweep file contains no `community/profile` call and no `gh workflow run` invocation
    - `.planning/REQUIREMENTS.md`'s LEGACY-04, LEGACY-05 and LEGACY-07 checkboxes are marked complete, and no other requirement's mark is changed
  </acceptance_criteria>
  <done>Every fast validation row is green against final state and recorded; the two slow rows are cited to their evidence; the three requirements are marked and the phase's claims are all checkable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta gitlink → submodule commit | The gitlink is meta's only record of what the sub-repos contain. A wrong pin makes the phase invisible from meta, or silently ships another phase's work under this phase's commit. |
| local validation output → the phase's evidence | With no CI anywhere in scope, a recorded local run is the *only* thing standing behind every claim the phase makes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-15 | Repudiation | the gitlink bump commit | medium | mitigate | Meta's `M firestarter` / `M firestarter_app` markers predate the phase, so a bump that "clears the M" could be pinning anything. V-19 asserts recorded-equals-actual for both submodules, and the commit message names both pre-bump short SHAs and states that Phase 170's un-pinned commits are carried. |
| T-171-16 | Tampering | unrelated pending edits in meta | low | mitigate | `.planning/notes/dev-test-sequence-cost-model.md` is modified and unrelated. Staging is path-scoped to the two gitlinks; the note's continued modified status after the commit is an acceptance criterion. |
| T-171-17 | Repudiation | the closing validation sweep | high | mitigate | No CI covers this phase, so an unrecorded or partial sweep would leave the phase's claims unbacked. Every V-row identifier must appear in the evidence file — checked by grep — and the two rows that are cited rather than re-run must say so explicitly, naming the evidence files they rest on. |
| T-171-18 | Repudiation | a vacuous verification leg | high | mitigate | `gh api …/community/profile` returns `null` before and after and would pass a plan that did nothing; a working-tree sdist build reports two deleted files as packaged. Neither appears in any leg here, and the sweep file is asserted not to contain `community/profile`. |
| T-171-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. `171-RESEARCH.md` § "Package Legitimacy Audit" records the gate as vacuous for the phase: zero external packages in any ecosystem, zero `[SUS]`, zero `[SLOP]`, every manifest and lockfile untouched. |
</threat_model>

<verification>
This plan's V-rows from `171-VALIDATION.md`: **V-19** (Task 1), and a closing re-run of **V-01
through V-16 plus V-19** (Task 2). **V-17 and V-18** are discharged by citation to plan 171-02's
evidence files rather than re-executed, and the sweep file must label them that way.

Across the four plans, every row V-01 … V-19 is claimed:

| Plan | Rows |
|---|---|
| 171-01 | V-10, V-11, V-12, V-13 |
| 171-02 | V-01, V-02, V-03, V-04, V-05, V-06, V-07, V-08, V-09, V-14, V-17, V-18 |
| 171-03 | V-15, V-16 |
| 171-04 | V-19, plus a closing re-run of V-01 … V-16 |

The four manual-only checks are handled as `171-VALIDATION.md` requires: the wiki push is a
`checkpoint:human-action` in plan 171-01 on a plan marked `autonomous: false`; the rendered-page
visual check is a `checkpoint:human-verify` in plan 171-01; the Security-tab observation is
**deferred to the milestone merge** and has no verify leg anywhere, because it is vacuous today; and
"did this make `wiki-check.yml` red" gets **no action** — the workflow is not registered with GitHub
Actions at all — and is recorded in `evidence/171-03-out-of-scope-ledger.md` instead.
</verification>

<success_criteria>
- Meta's recorded gitlinks equal both submodules' checked-out HEADs
- The bump commit names exactly the two gitlinks and states that Phase 170's un-pinned commits are carried
- `.planning/notes/dev-test-sequence-cost-model.md` is still modified and uncommitted
- `evidence/171-04-validation-sweep.txt` mentions every row V-01 … V-19 and is green for every one it re-runs
- V-17 and V-18 are cited to plan 171-02's evidence and labelled as not re-run
- LEGACY-04, LEGACY-05 and LEGACY-07 are marked complete in `.planning/REQUIREMENTS.md`
- No verify leg anywhere in the phase calls `community/profile`, builds an sdist in the live working tree, or invokes `gh workflow run`
</success_criteria>

<output>
Create `.planning/phases/171-stray-the-root-level-documentation-files/171-04-SUMMARY.md` when done.

Record in it: the three ROADMAP success criteria and the evidence each rests on; that criterion 2's
GitHub-surface property becomes observable only at the milestone merge, since `SECURITY.md` never
reached `origin/main` — the phase *prevented* the misrepresentation rather than remediating a live
one; that no CI covered any part of this phase and every check was local and recorded; and the
pointer to `evidence/171-03-out-of-scope-ledger.md` for the eight items deliberately not fixed.
</output>
