---
phase: 171-stray-the-root-level-documentation-files
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter_prom.wiki.git (live public wiki) — plus one meta commit for this plan's evidence files under .planning/phases/171-stray-the-root-level-documentation-files/evidence/
files_modified:
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-postpush-freshclone.txt
autonomous: false
requirements: [LEGACY-07]
user_setup: []

must_haves:
  truths:
    - "`Shell-Completion.md` exists on the live `firestarter_prom` wiki and is present in a fresh, independent clone of it — not merely in the clone the executor edited"
    - "The page is listed in `_Sidebar.md` and linked from `Home.md`'s Reference list, so `wiki.py links` against a fresh clone reports `OK: 10 pages,` with rc=0"
    - "The published page's lines 3-5 are `---`, an empty line, and `# Shell Completion` — the same opening shape every other non-`Home` wiki page uses"
    - "All six `###` sections of the source guide (Bash, Zsh, Fish, PowerShell, pipx Installations, Migrating from a previous Firestarter) survive verbatim on the published page"
    - "The published page renders `Shell-Completion -> \"Shell Completion\"` in the checker's page listing, confirming the filename produces the intended title"
  artifacts:
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt"
      provides: "The pre-push oracle output — the only gate that exists, because wiki-check.yml is not registered with GitHub Actions"
      min_lines: 11
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-postpush-freshclone.txt"
      provides: "Proof the push actually landed, taken against a second independent clone rather than the working copy"
      min_lines: 11
  key_links:
    - from: "wiki `Home.md` Reference list"
      to: "wiki `Shell-Completion.md`"
      via: "a markdown link whose text and target are both the bare stem"
      pattern: "[Shell-Completion](Shell-Completion)"
    - from: "wiki `_Sidebar.md`"
      to: "wiki `Shell-Completion.md`"
      via: "a bullet appended after the Breaking-Changes entry"
      pattern: "- [Shell-Completion](Shell-Completion)"
  prohibitions:
    - statement: "This plan adds no security-reporting statement to the wiki — no page, section, sentence or link telling a reader how to report a vulnerability. The project has no private disclosure channel and a page implying one would be exactly the class of false claim this milestone exists to remove (D-02)."
      status: enforced
      verification: "V-05 — the D-02 prohibition grep, discharged in plan 171-02 Task 2. This plan additionally publishes exactly one new page, named Shell-Completion, and no other."
    - statement: "This plan authors no new topic. It relocates one existing document and corrects three shape defects in it. No Installing-avrdude page, no salvaged hackaday.io link, no Breaking-Changes version heading for the argcomplete-to-Click swap (D-04 refuses that anchor)."
      status: enforced
      verification: "The fresh clone holds exactly 10 pages plus _Sidebar.md; Breaking-Changes.md and Home.md are byte-unchanged apart from Home.md's single appended Reference bullet."
    - statement: "No HTML comment, provenance marker or claim stamp is written into Shell-Completion.md. The project's hard no-comments rule applies, and honest02 was measured to report zero missing stamps with this page present."
      status: enforced
      verification: "Task 3 acceptance criterion — the published page contains no HTML comment opener sequence."
---

<objective>
Publish `firestarter_app/autocomplete.md` as the tenth page of the live `firestarter_prom` wiki,
under the name `Shell-Completion`, with the two navigation edits that a new page owes, in a single
push — and prove it landed from a second, independent clone.

Purpose: this is the "publish" half of publish-then-delete (D-03, LEGACY-07). Plan 171-02 deletes
the source file, and it is gated on this plan having succeeded. If the order were reversed and this
push failed, the guide would exist only in git history.

Output: one new live wiki page, two navigation edits, and two evidence files recording the only two
oracle runs that exist for this phase.

**This plan writes to a live, public wiki.** `autonomous: false` is deliberate and load-bearing.
This project runs GSD with `--chain`, which auto-approves `human-verify` and `decision` checkpoints
and auto-selects their first option; only `human-action` checkpoints are actually presented. The
push in Task 2 is therefore a `checkpoint:human-action` and this flag is what stops an unattended
outward-facing publish.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-CONTEXT.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-PATTERNS.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-VALIDATION.md
@firestarter_app/autocomplete.md
@tools/wiki/wiki.py
</context>

<!-- planner-discipline-allow: autocomplete.md -->
<!-- planner-discipline-allow: report a vulnerability -->
<!-- planner-discipline-allow: security policy -->

## Precedence note for the executor

`171-RESEARCH.md`'s corrections table (line 9) overturns seven `171-CONTEXT.md` claims and outranks
CONTEXT.md wherever they disagree. Two of its corrections bind this plan directly:

- **C-1.** `.github/workflows/wiki-check.yml` does **not** run. It is absent from `origin/main`, the
  default branch of `henols/firestarter_prom`, and GitHub registers `schedule` and
  `workflow_dispatch` only from the default branch. `gh workflow list --all` returns one workflow,
  `Catalog sync check`. There is no weekly cron watching the wiki and no CI safety net for this
  phase. A forgotten `_Sidebar.md` entry would not go red a week later — it would go unnoticed
  indefinitely. Do not write or run any `gh workflow run` command; there is nothing to run.
- **C-3.** CONTEXT.md's "the move is close to byte-for-byte" is imprecise. The source diverges from
  the established page shape in three measurable ways, and `wiki.py links` returns rc=0 for a
  naive copy — nothing automated catches a mis-shaped page. Task 1 asserts the shape directly.

## Artifacts this phase produces

New symbols and files created by Phase 171. Drift checks must treat these as new, not as unresolved
references.

| Artifact | Repo / location | Created by |
|---|---|---|
| `Shell-Completion.md` | `firestarter_prom.wiki.git` (live wiki, root) | this plan, Task 1 |
| The `_Sidebar.md` bullet `- [Shell-Completion](Shell-Completion)` | live wiki | this plan, Task 1 |
| The `Home.md` Reference-list bullet linking to `Shell-Completion` | live wiki | this plan, Task 1 |
| `evidence/171-01-wiki-links-prepush.txt` | meta | this plan, Task 1 |
| `evidence/171-01-wiki-links-postpush-freshclone.txt` | meta | this plan, Task 2 |
| The `Shell-Completion` row in `.planning/v1.35/MIGRATION-TABLE.md`'s main table | meta | plan 171-03 |
| The `## Removed without ever being published` section in `.planning/v1.35/MIGRATION-TABLE.md` | meta | plan 171-03 |
| `evidence/171-02-*.txt`, `evidence/171-03-*`, `evidence/171-04-*.txt` | meta | plans 171-02 … 171-04 |

<tasks>

<task type="auto">
  <name>Task 1: Clone the wiki, author Shell-Completion.md with the three shape edits, make both navigation edits, and gate on wiki.py links</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-PATTERNS.md` §1, §2 and §3 — these carry the verbatim target bytes: the canonical six-line page opening quoted from `Breaking-Changes.md:1-8`, the full current contents of `_Sidebar.md`, and the current `Home.md:42-51` Reference list with its row shape
    - `firestarter_app/autocomplete.md` in full (69 lines) — the document being moved; note its headings are at `:4`, `:10`, `:20`, `:30`, `:46`, `:57`, `:65`
    - `tools/wiki/wiki.py:50-52` — `render_title` (stem hyphens become spaces) and `_LEGAL_TARGET_RE` (internal link targets admit only `[A-Za-z0-9-]`, never a `.md` suffix and never a path)
    - `tools/wiki/wiki.py:210-266` — `check_orphans`, `check_sidebar_lists_every_page` and `cmd_links`; these define the exact success string and both failure messages
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md` §A.4 and §B.5 — the measured shape divergence, and the proof that a naive copy passes rc=0
  </read_first>
  <files>working clone of firestarter_prom.wiki.git at /tmp/gsd-171-wiki (deterministic scratch path, outside all three repositories), .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt</files>
  <action>
    Export `WIKI=/tmp/gsd-171-wiki` and use that exact path in every task of this plan, so the clone
    survives between tasks. Clone `https://github.com/henols/firestarter_prom.wiki.git` into it.
    The clone is anonymous and needs no token. It must live outside `/workspaces`,
    `/workspaces/firestarter` and `/workspaces/firestarter_app`: `/workspaces/CLAUDE.md` states that
    documentation lives only in the wiki and there is no in-repo copy of it, so nothing produced in
    this task may ever be `git add`ed in the meta repo or either submodule.

    Copy `/workspaces/firestarter_app/autocomplete.md` to `$WIKI/Shell-Completion.md`. Copy it —
    never retype, summarise, reflow or re-author it. Then apply exactly three edits to its head and
    nothing else anywhere in the file:

    (a) insert a blank line immediately after the `---` horizontal rule that sits on line 3, so that
    line 4 becomes empty;

    (b) demote the heading marker on what was line 4 from two hash characters to one, making it an
    H1 to match the eight non-`Home` pages;

    (c) replace that heading's text `Enabling Shell Autocompletion` with `Shell Completion`, which
    is exactly what `render_title("Shell-Completion")` produces from the filename.

    After the three edits, lines 1-6 must read: the logo `<p align="left">` block; an empty line;
    `---`; an empty line; `# Shell Completion`; an empty line. That is byte-identical to the opening
    of `Breaking-Changes.md`, `Install-Beta.md`, `Testing-Chips.md` and `Shield-Revisions.md`. The
    logo `<img src>` URL is already the same one every live page carries, so no URL edit is owed.
    Source lines 7-69 are carried through unchanged, including all six `###` section headings and
    the whole `Migrating from a previous Firestarter` section, which stays on this page per D-04.

    Add nothing else. Relocate-and-correct only (milestone activation decision 4): no new topic, no
    claim stamp — `honest02` was measured to report zero missing stamps with this page present — and
    no HTML comment of any kind. The project's hard no-comments rule covers this file.

    Navigation, both edits in this same change or the checker fails:

    (d) `_Sidebar.md` — append a new line 10, directly after the existing
    `- [Breaking-Changes](Breaking-Changes)` on line 9. The bullet's link text and its target are
    both the bare stem `Shell-Completion`. That matches the hyphenated-stem form used by lines 4-9;
    do not use the spaced form that lines 2-3 use. The list is deliberately not alphabetical — it is
    in the order pages were added — so append, do not sort.

    (e) `Home.md` — insert a new line 50, directly after the `- [Breaking-Changes](Breaking-Changes)
    — what changed between versions, and what to do about it` bullet on line 49, and before the
    blank line that precedes the `---` on line 51. The row shape is the one every neighbouring row
    uses: hyphen, space, a markdown link whose text and target are both `Shell-Completion`, space, a
    U+2014 em dash (—), space, then a lower-case clause with no terminating period. Use the clause
    `turning on tab completion for` followed by `firestarter` wrapped in backticks followed by
    `in your shell`. This placement gives the orphan leg the simplest reachable path from `Home.md`.

    Do not touch `Home.md:5` (`# Firestarter`) — it is the one deliberate exception to
    `render_title` and must not be "fixed".

    Then run the gate. From `/workspaces`, run `python3 tools/wiki/wiki.py links --source-dir "$WIKI"`
    and require rc=0 with a stdout line beginning `OK: 10 pages,` and a stdout line reading
    `Shell-Completion -> "Shell Completion"`. Write the full stdout, stderr and the observed exit
    status to `.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt`.
    A rc=1 here names its own cause: `page missing from _Sidebar.md: Shell-Completion` means edit (d)
    is missing; `orphan page not reachable from Home.md by any link path: Shell-Completion` means
    edit (e) is missing. This local run is the only gate that exists for the wiki — see the C-1 note
    above — so treat a non-zero exit as a hard stop, not as advisory output.

    Finally, stage exactly the three files (`Shell-Completion.md`, `_Sidebar.md`, `Home.md`) in the
    clone and create the commit — but **do not push**. Task 2 owns the push. Subject line form is
    the wiki repository's own, `docs:` followed by what the page is for, with no GSD scope, because
    the wiki repo carries no GSD scoping (precedent: wiki commit `7ec9988`, which added a page plus
    one line to each of `Home.md` and `_Sidebar.md` as a single commit — the exact decomposition to
    mirror here).

    Commit the evidence file to the meta repo with a path-scoped `git commit -- <path>`. Meta's
    working tree already shows `M firestarter`, `M firestarter_app` and
    `M .planning/notes/dev-test-sequence-cost-model.md` before this phase begins; none of those are
    yours and none may be swept into this commit.
  </action>
  <verify>
    <automated>WIKI=/tmp/gsd-171-wiki; EV=/workspaces/.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt; cd /workspaces && python3 tools/wiki/wiki.py links --source-dir "$WIKI" > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -q '^OK: 10 pages,' "$EV" && grep -qF 'Shell-Completion -> "Shell Completion"' "$EV" && grep -qx 'exit 0' "$EV" && sed -n '3,5p' "$WIKI/Shell-Completion.md" | tr '\n' '|' | grep -qx -- '---||# Shell Completion|' && ! grep -q '<!--' "$WIKI/Shell-Completion.md" && test "$(git -C "$WIKI" show --stat --format="" HEAD | grep -cE 'Home\.md|_Sidebar\.md|Shell-Completion\.md')" -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - `$WIKI/Shell-Completion.md` exists and `wc -l` reports 70 lines (the 69-line source plus the one blank line inserted by edit (a))
    - `sed -n '3,5p' "$WIKI/Shell-Completion.md" | tr '\n' '|'` outputs exactly `---||# Shell Completion|` — this is V-12, and it is the only check that catches the three shape edits
    - `grep -c '^## ' "$WIKI/Shell-Completion.md"` returns 0 — the H2 title was demoted, not duplicated
    - `grep -c 'Enabling Shell Autocompletion' "$WIKI/Shell-Completion.md"` returns 0
    - For each of `### Bash`, `### Zsh`, `### Fish`, `### PowerShell`, `### pipx Installations`, `### Migrating from a previous Firestarter`, `grep -qF` finds it in `$WIKI/Shell-Completion.md` — this is V-13
    - `diff <(tail -n +7 "$WIKI/Shell-Completion.md") <(tail -n +6 /workspaces/firestarter_app/autocomplete.md)` reports no differences — the offsets differ by one because edit (a) inserted a line, and the body below the head was copied rather than rewritten
    - `grep -c '<!--' "$WIKI/Shell-Completion.md"` returns 0 — no comment, no provenance marker, no claim stamp
    - `tail -n 1 "$WIKI/_Sidebar.md"` is exactly `- [Shell-Completion](Shell-Completion)` and `wc -l` on `_Sidebar.md` reports 10
    - `grep -n '(Shell-Completion)' "$WIKI/Home.md"` reports exactly one hit, on line 50, and the line contains a U+2014 em dash
    - `git -C "$WIKI" diff HEAD~1 --stat` shows exactly three files changed: `Shell-Completion.md` created, `Home.md` +1, `_Sidebar.md` +1 — matching wiki commit `7ec9988`'s shape
    - `python3 tools/wiki/wiki.py links --source-dir "$WIKI"` exits 0 and its stdout contains both `OK: 10 pages,` and `Shell-Completion -> "Shell Completion"`
    - The evidence file exists, is non-empty, and its last line is `exit 0`
    - `git -C "$WIKI" log origin/master..HEAD --oneline | wc -l` returns 1 — exactly one unpushed commit; nothing was pushed by this task
    - No file under any tracked path in `/workspaces`, `/workspaces/firestarter` or `/workspaces/firestarter_app` was created or modified by this task except the one evidence file
  </acceptance_criteria>
  <done>The wiki clone holds a shape-correct `Shell-Completion.md`, both navigation edits, and one local commit; the only oracle that exists for this phase has been run and recorded green; nothing has been pushed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator gate — push the commit to the live public wiki</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-prepush.txt` — the pre-flight oracle output produced by Task 1
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md` § "Open Questions" item 1 — the push mechanism was deliberately not exercised during research; the token's `admin:true, push:true` permission was verified, the push itself was not
  </read_first>
  <files>working clone of firestarter_prom.wiki.git at /tmp/gsd-171-wiki, .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-postpush-freshclone.txt</files>
  <what-built>
    A single local commit in a clone of the live public wiki, adding `Shell-Completion.md` (the
    relocated shell-completion guide, shape-corrected to match the other nine pages) and appending
    one navigation line to each of `_Sidebar.md` and `Home.md`. The `wiki.py links` gate has been run
    against it and exits 0 with `OK: 10 pages,`. Nothing has been pushed and nothing has been deleted
    from `firestarter_app`.
  </what-built>
  <action>
    **This is the phase's irreversible outward-facing act.** It writes to a public repository and it
    is not undoable by a plan. It is a `human-action` checkpoint specifically because this project
    runs GSD with `--chain`, which auto-approves `human-verify` and `decision` gates — `human-action`
    is the only type that actually reaches the operator. Do not batch any other approval into this
    one, and do not proceed past it on an inferred approval.

    Present the operator with the pre-flight evidence before asking for anything:

    1. the contents of the pre-push evidence file (the `wiki.py links` run, ending `exit 0`);
    2. `git -C /tmp/gsd-171-wiki show --stat --format=""` — the three-file commit stat;
    3. `git -C /tmp/gsd-171-wiki diff HEAD~1 -- Home.md _Sidebar.md` — the two one-line navigation
       hunks in full, so the operator can read exactly what is being appended;
    4. `sed -n '1,6p' /tmp/gsd-171-wiki/Shell-Completion.md` — the corrected page head;
    5. the target: `https://github.com/henols/firestarter_prom.wiki.git`, branch `master`.

    On `push approved`, run `git -C /tmp/gsd-171-wiki push origin master`. Authenticate with the
    locally configured credential helper. If it fails, the documented fallback is to set the scratch
    clone's remote to the token-bearing HTTPS form using `gh auth token` — apply that with
    `git remote set-url` **in the scratch clone only**, never in a tracked repository, and never
    commit a `.git/config` carrying it. Report the failure and the fallback in the summary either way.

    On `pushed`, the operator ran it themselves; skip the push and go straight to the verification.

    On `hold`, stop the phase here. Nothing has been published and nothing has been deleted, so the
    tree is in a consistent state: plan 171-02 is gated on the page being live and will not run.

    After the push (by whichever hand), immediately discharge the post-push oracle against a
    **second, independent, fresh** clone — not `/tmp/gsd-171-wiki`. The working clone shows your own
    commit whether or not the push succeeded, so it proves nothing. Clone
    `https://github.com/henols/firestarter_prom.wiki.git` shallow into a new scratch directory, run
    `wiki.py links` against it, and write the full output plus the exit status to
    `evidence/171-01-wiki-links-postpush-freshclone.txt`. Commit that evidence file to meta with a
    path-scoped commit.

    Record the resulting wiki commit SHA in the plan summary.
  </action>
  <how-to-verify>
    1. Read the five pre-flight items above, in particular the two navigation hunks.
    2. Confirm the target repository and branch are `henols/firestarter_prom.wiki.git` / `master`.
    3. Decide: reply `push approved` to have the executor push, `pushed` if you ran
       `git -C /tmp/gsd-171-wiki push origin master` yourself, or `hold` to stop the phase with
       nothing published and nothing deleted.
  </how-to-verify>
  <resume-signal>Type "push approved", "pushed", or "hold"</resume-signal>
  <verify>
    <automated>V=$(mktemp -d); EV=/workspaces/.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-01-wiki-links-postpush-freshclone.txt; git clone --depth 1 https://github.com/henols/firestarter_prom.wiki.git "$V/wiki" >/dev/null 2>&1 && test -f "$V/wiki/Shell-Completion.md" && cd /workspaces && python3 tools/wiki/wiki.py links --source-dir "$V/wiki" > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -q '^OK: 10 pages,' "$EV" && grep -qF 'Shell-Completion -> "Shell Completion"' "$EV" && grep -qx 'exit 0' "$EV" && sed -n '3,5p' "$V/wiki/Shell-Completion.md" | tr '\n' '|' | grep -qx -- '---||# Shell Completion|' && for s in Bash Zsh Fish PowerShell "pipx Installations" "Migrating from a previous Firestarter"; do grep -qF "### $s" "$V/wiki/Shell-Completion.md" || { echo "MISSING: $s"; exit 1; }; done && grep -q 'Shell-Completion' "$V/wiki/_Sidebar.md" && grep -q '(Shell-Completion)' "$V/wiki/Home.md"</automated>
  </verify>
  <acceptance_criteria>
    - A fresh `git clone --depth 1` of `https://github.com/henols/firestarter_prom.wiki.git` into a directory that is **not** `/tmp/gsd-171-wiki` contains `Shell-Completion.md` — this is V-10
    - `python3 tools/wiki/wiki.py links --source-dir <fresh clone>` exits 0 and prints both `OK: 10 pages,` and `Shell-Completion -> "Shell Completion"` — this is V-11
    - `sed -n '3,5p' <fresh clone>/Shell-Completion.md | tr '\n' '|'` outputs exactly `---||# Shell Completion|` — this is V-12, taken against the published bytes rather than the working copy
    - All six `### ` section headings are present in the fresh clone's copy — this is V-13
    - `grep -c 'Shell-Completion' <fresh clone>/_Sidebar.md` returns 1 and `grep -c '(Shell-Completion)' <fresh clone>/Home.md` returns 1
    - `ls <fresh clone>/*.md | wc -l` returns 11 — ten pages plus `_Sidebar.md`, meaning exactly one page was added and none removed
    - `evidence/171-01-wiki-links-postpush-freshclone.txt` exists and its last line is `exit 0`
    - No `gh workflow run` command was issued at any point — `wiki-check.yml` is not registered with GitHub Actions (C-1) and there is nothing to run
  </acceptance_criteria>
  <done>`Shell-Completion` is live on the public wiki, reachable from `Home`, listed in the sidebar, shape-correct, and proven so from an independent clone rather than from the copy that was edited.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator eyeballs the rendered page on github.com</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-VALIDATION.md` § "Manual-Only Verifications" — the second row is this check and states why no automated leg can stand in for it
  </read_first>
  <files>none — observation only</files>
  <what-built>
    `Shell-Completion` published on the live wiki with both navigation entries, verified from a fresh
    clone by `wiki.py links`.
  </what-built>
  <action>
    Present the operator with the two live URLs below and the seven-point checklist, then wait. This
    is the only check in the phase that looks at rendered output: `wiki.py links` parses markdown and
    never renders anything, so a page with a broken logo URL, a mangled code fence or a wrong title
    passes it with rc=0. Nothing automated stands behind this.

    If the operator reports a defect, fix it in the same working clone at `/tmp/gsd-171-wiki`, re-run
    the `wiki.py links` gate, and push a follow-up commit — the outward-facing approval in Task 2
    covers corrective pushes to this same page and its two navigation entries, and nothing else.

    Record the operator's verdict in the plan summary. Do not attempt to substitute a `curl` of the
    rendered HTML for this check.
  </action>
  <how-to-verify>
    1. Open `https://github.com/henols/firestarter_prom/wiki/Shell-Completion`.
    2. Confirm the page title at the top reads **Shell Completion** — two words, no hyphen, and not
       "Enabling Shell Autocompletion".
    3. Confirm the Firestarter logo image at the top of the page actually renders — it is a remote
       `raw.githubusercontent.com` URL, and `wiki.py links` never fetches it.
    4. Confirm the four shell sections (Bash, Zsh, Fish, PowerShell), the pipx section and the
       "Migrating from a previous Firestarter" section are all present and that their code blocks
       render as code rather than as raw backticks.
    5. Confirm the right-hand sidebar lists **Shell-Completion** as its last entry.
    6. Open `https://github.com/henols/firestarter_prom/wiki` and confirm the Reference list carries
       a `Shell-Completion` bullet directly after `Breaking-Changes`, with an em dash separator that
       matches its neighbours.
    7. Confirm no stray HTML comment, phase number, or GSD framing is visible anywhere on the page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what renders wrongly</resume-signal>
  <verify>
    <human-check>Operator confirms the rendered page, its title, its logo, its six sections and its sidebar placement on github.com. `wiki.py links` renders nothing and cannot substitute for this.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator has viewed the live rendered page and the wiki home page and reported the outcome
    - The page title renders as "Shell Completion"
    - The logo image resolves
    - The sidebar lists the page
  </acceptance_criteria>
  <done>The published page has been seen by a human and looks like the other nine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → `firestarter_prom.wiki.git` | An `admin`-scoped GitHub token writes to a public repository. This is the only outward-facing write in the phase and it is irreversible by plan. |
| wiki page → public reader | Anything published here is read as project documentation by strangers with no way to check it against the code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-01 | Information disclosure | the `git push` remote URL in the scratch clone | high | mitigate | Push with the configured credential helper. The token-in-URL fallback is applied with `git remote set-url` in `/tmp/gsd-171-wiki` only, never in a tracked repository, and no `.git/config` carrying it is ever committed. |
| T-171-02 | Tampering | the live public wiki | high | mitigate | The push is a `checkpoint:human-action` with `autonomous: false` — the only checkpoint type `--chain` does not auto-approve. The operator sees the three-file stat and both navigation hunks before approving. Exactly three files are staged; the commit stat is asserted at 3. |
| T-171-03 | Repudiation | `Shell-Completion` page content | medium | mitigate | The page is produced by `cp` plus three enumerated head edits, never retyped. `diff` of lines 7-onward against the source is an acceptance criterion, so no claim can be silently upgraded or dropped in the move. |
| T-171-04 | Repudiation | a security-reporting statement smuggled onto the wiki | high | mitigate | D-02 forbids one anywhere in this phase. This plan publishes exactly one page, `Shell-Completion`, whose content is a byte-diff of the completion guide; the fresh clone is asserted at 11 markdown files so no extra page can appear unnoticed. Verified by V-05 in plan 171-02. |
| T-171-05 | Denial of service (to readers) | the relocated guide | medium | mitigate | Publish-then-delete ordering. Plan 171-02 is `depends_on: ["171-01"]` and re-asserts the page is live from a fresh clone before deleting the source, so the content is never in a state where it exists nowhere. |
| T-171-SC | Tampering | package-manager installs | low | accept | This plan installs nothing. `171-RESEARCH.md` § "Package Legitimacy Audit" records the gate as vacuous for this phase: zero external packages in any ecosystem, `MANIFEST.in`, `pyproject.toml`, `requirements.txt` and every lockfile untouched. No `[ASSUMED]` or `[SUS]` package exists, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
This plan's V-rows from `171-VALIDATION.md`: **V-10, V-11, V-12, V-13** (all four discharged against
a fresh clone in Task 2's verify block; V-12 is additionally pre-checked against the working clone in
Task 1).

The two named oracle traps do not appear anywhere in this plan and must not be added:
`gh api …/community/profile` (vacuous — reads the default branch) and a working-tree sdist build
(false positive). Neither is relevant here; both are named so a reviewer can confirm their absence.

`wiki-check.yml` is out of scope and unrunnable. Separately, `.github/workflows/wiki-check.yml:104-107`
carries a pre-existing defect — it passes `--wiki-dir` to `dispatch_mirror.py`, which accepts only
`--app-dir` / `--fw-dir` (`tools/wiki/dispatch_mirror.py:157-158`), so that step exits 2 the first
time the workflow ever runs. Introduced by `4b14a5a2`. **Do not fix it in this phase.** It is recorded
here and in `evidence/171-03-out-of-scope-ledger.md` so a future red run is not misattributed to
Phase 171.
</verification>

<success_criteria>
- A fresh clone of the live wiki contains `Shell-Completion.md` with lines 3-5 equal to `---`, empty, `# Shell Completion`
- `wiki.py links` against that fresh clone exits 0 and reports `OK: 10 pages,`
- The page is listed in `_Sidebar.md` and linked from `Home.md`'s Reference list
- All six `###` sections of the source guide survive on the published page
- Both evidence files exist, end with `exit 0`, and are committed to meta
- Nothing in `firestarter_app` has been deleted by this plan
</success_criteria>

<output>
Create `.planning/phases/171-stray-the-root-level-documentation-files/171-01-SUMMARY.md` when done.

Record in it: the wiki commit SHA produced by the push; whether the push used the credential helper
or the token-URL fallback; and the fact that `LEGACY-07` is **not** complete at this plan's close —
it spans plans 171-01 through 171-04 and its checkbox is flipped only by 171-04.
</output>
