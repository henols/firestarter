---
phase: 171-stray-the-root-level-documentation-files
plan: 03
type: execute
wave: 3
depends_on: ["171-02"]
commits_land_in: meta (/workspaces), branch gsd/v1.35-documentation-consolidation-wiki-migration
files_modified:
  - .planning/v1.35/MIGRATION-TABLE.md
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-migration-table-parse.txt
  - .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-out-of-scope-ledger.md
autonomous: true
requirements: [LEGACY-04, LEGACY-05, LEGACY-07]
user_setup: []

must_haves:
  truths:
    - "D-06: `.planning/v1.35/MIGRATION-TABLE.md` answers \"what happened to this document\" for all three of Phase 171's files, from the table alone, without reading a phase record"
    - "`autocomplete.md` has a Phase 171 row in the main migration table naming its wiki page, its rendered title and its full pre-deletion SHA"
    - "`things.md` and `SECURITY.md` each have a row in a new section for files removed without ever being published, naming what the file was, why it went, and the command that recovers it"
    - "`honest01_claims.parse_migration_table` returns 8 SHA-bearing rows after the change — the 7 it returns today plus `Shell-Completion` — and none of them is a deletion row"
    - "The five known out-of-scope items and pre-existing defects are recorded, so a future failure is not misattributed to Phase 171"
  artifacts:
    - path: ".planning/v1.35/MIGRATION-TABLE.md"
      provides: "The provenance record for all three dispositions (D-06)"
      min_lines: 160
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-migration-table-parse.txt"
      provides: "Proof the new section does not corrupt the only machine reader of this table"
      min_lines: 9
    - path: ".planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-out-of-scope-ledger.md"
      provides: "The eight recorded-not-fixed items, so a future red run or drift report is attributed correctly"
      min_lines: 20
  key_links:
    - from: "the `Shell-Completion` main-table row"
      to: "the live wiki page `Shell-Completion`"
      via: "the Wiki page column, which the Backlog 999.9 rename sweep greps"
      pattern: "| firestarter_app | firestarter_app/autocomplete.md | Shell-Completion | Shell Completion | d56424e1979edf7245cffb9ec3111c0469f5b23f | 171 |"
    - from: "the two deletion rows"
      to: "the frozen pre-deletion blobs"
      via: "an inline-code recovery command inside the last cell, never a fifth column"
      pattern: "git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:"
  prohibitions:
    - statement: "The new deletion section's table has at most 4 columns. `parse_migration_table` (`tools/wiki/honest01_claims.py:74-93`) sets its `header` once from the first table-shaped line in the whole file and never resets it, so a fifth cell would be keyed `Pre-deletion SHA`, survive the `NO_SHA_MARKER` filter at `:93`, and make `honest01` try to resolve a non-migration row against a wiki page named by its third cell."
      status: enforced
      verification: "V-16 — the `parse_migration_table` snippet returns exactly 8 rows and none has `things.md` or `SECURITY.md` in its `Wiki page` field. Plus a structural assertion that every row in the new section splits into exactly 3 cells."
    - statement: "This plan adds no security-reporting statement to `MIGRATION-TABLE.md`. The `SECURITY.md` row records what the file was and where it went; it does not state, imply or link to a way to report a vulnerability (D-02)."
      status: enforced
      verification: "V-05, discharged in plan 171-02 Task 2, plus a direct grep of the new section for the three disclosure-policy phrases."
    - statement: "The pre-existing `Protocol-Flags` and `Protocol-ID` drift at `MIGRATION-TABLE.md:18-19` is not fixed here. CONTEXT.md's deferred list assigns it to whoever owns the table; this phase only appends."
      status: enforced
      verification: "`git diff` of `MIGRATION-TABLE.md` shows only additions — zero deleted or modified lines."
    - statement: "No comment of any kind is written into `MIGRATION-TABLE.md` or into any script. The project's hard no-comments rule is absolute and a plan cannot override it; the two new rows carry their explanation as table prose, which is the file's own established form."
      status: enforced
      verification: "The `git diff` adds no line containing an HTML comment opener and no `#`-prefixed annotation."
---

<objective>
Record all three of Phase 171's dispositions in `.planning/v1.35/MIGRATION-TABLE.md`, and prove the new
rows do not break the only tool that machine-reads that file.

Purpose: D-06. Deletion in this project is recorded, never silent — the operator's post-168 wiki
reorganisation removed six pages and each is recorded in this table with what happened to it. After
this plan, "what happened to `things.md`?" is answerable from the table alone, without reading a
phase record, and the answer includes the command that gets the bytes back.

Output: one appended main-table row, one new short section with two rows, and two evidence files.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-CONTEXT.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-PATTERNS.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-VALIDATION.md
@.planning/phases/171-stray-the-root-level-documentation-files/171-02-SUMMARY.md
@.planning/v1.35/MIGRATION-TABLE.md
@tools/wiki/honest01_claims.py
</context>

<!-- planner-discipline-allow: things.md -->
<!-- planner-discipline-allow: autocomplete.md -->
<!-- planner-discipline-allow: SECURITY.md -->
<!-- planner-discipline-allow: report a vulnerability -->
<!-- planner-discipline-allow: security policy -->
<!-- planner-discipline-allow: responsible disclosure -->

## Precedence note for the executor

`171-RESEARCH.md`'s corrections table (line 9) outranks `171-CONTEXT.md`. One correction binds the
prose written here:

- **C-7.** `things.md` is **7 lines / 265 bytes with no trailing newline**. `wc -l` reports 6
  because it counts newline characters. CONTEXT.md says five and the ROADMAP says six; both are
  wrong. Write "seven lines" or omit the count. Do not copy either wrong number into a permanent
  provenance record.

Also note the operator's standing rule that `.planning/` `file:LINE` citations must be repaired
rather than left stale. Appending rows shifts every line number below the insertion point in
`MIGRATION-TABLE.md`, so any citation of that file written by this phase must use post-change line
numbers, verified after the edit.

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Shell-Completion.md` and both wiki navigation entries | live wiki | plan 171-01 |
| `evidence/171-02-*.txt` (four files) | meta | plan 171-02 |
| The `Shell-Completion` row appended to `MIGRATION-TABLE.md`'s main table | meta | this plan, Task 1 |
| The `## Removed without ever being published` section heading, its prose paragraph, its 3-column table and its two rows in `MIGRATION-TABLE.md` | meta | this plan, Task 1 |
| `evidence/171-03-migration-table-parse.txt` | meta | this plan, Task 2 |
| `evidence/171-03-out-of-scope-ledger.md` | meta | this plan, Task 2 |
| `evidence/171-04-*.txt` | meta | plan 171-04 |

<tasks>

<task type="auto">
  <name>Task 1: Append the Shell-Completion main-table row and add the removed-never-published section</name>
  <read_first>
    - `.planning/v1.35/MIGRATION-TABLE.md` in full (145 lines) — read the whole file before editing it; the main table is at `:10-19`, the "Retired from the wiki after the migration closed" section at `:45-60`, its 3-column header and separator at `:52-53`, its exemplar rows at `:54-58`, and the `## Honesty note` heading at `:67`
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-PATTERNS.md` §4 — the two row shapes quoted byte-exact, and the machine-reader constraint spelled out
    - `tools/wiki/honest01_claims.py:47-49` and `:74-93` — `TABLE_ROW_RE`, `NO_SHA_MARKER` (U+2014), and `parse_migration_table`'s single-header behaviour; this is why the column count is a hard constraint rather than a style preference
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-02-SUMMARY.md` — what `things.md` actually contained and its true line count
  </read_first>
  <files>.planning/v1.35/MIGRATION-TABLE.md</files>
  <action>
    **(a) The main-table row.** Append one row immediately after the existing
    `firestarter_app/doc/protocol-id.md` row at line 19, as the last row of the main table. Six
    cells, in the header's order (`Source repo`, `Source path`, `Wiki page`, `Rendered title`,
    `Pre-deletion SHA`, `Moved in`): `firestarter_app`; `firestarter_app/autocomplete.md`;
    `Shell-Completion`; `Shell Completion`; the full 40-character SHA
    `d56424e1979edf7245cffb9ec3111c0469f5b23f`; `171`. Match the exemplar at line 15 exactly — the
    source path carries the repository-name prefix, the SHA is written full length rather than
    abbreviated, and no cell is wrapped in backticks in this table.

    **(b) The new section.** Insert a new `##` section immediately before the
    `## Honesty note: HONEST-01 no longer applies to the surviving rewritten pages` heading — line 67
    today, line 68 once (a) has added its row. Heading text: `Removed without ever being published`.

    Follow the shape of the existing "Retired from the wiki after the migration closed" section at
    `:45-58`: an `##` heading, then a short prose paragraph, then the table. The paragraph explains
    that these two files sat at the `firestarter_app` repository root and were never wiki pages at
    all; that they were removed in Phase 171 under LEGACY-04 and LEGACY-05; that their content is
    still recoverable at the recorded commit so nothing is lost; and that they are recorded here
    rather than dropped, because "what happened to this document" is the question this table exists
    to answer. Keep it to the four-or-five-line register the neighbouring paragraph uses.

    The table has **exactly three columns**, mirroring `:52-53`: a header row whose cells are
    `Source path`, `What it was`, `What happened`, and a separator row of three plain `|---|` groups
    with no alignment colons. **This is a hard constraint, not a style choice.** A fifth column would
    be keyed `Pre-deletion SHA` by `parse_migration_table`'s never-reset header, survive its
    `NO_SHA_MARKER` filter, and make `honest01` resolve a non-migration row against a wiki page named
    by its third cell. Carry the recoverable SHA as inline code inside the last cell instead.

    Row 1, `things.md`. `Source path` cell: the path `firestarter_app/things.md` wrapped in
    backticks, matching `:54-58`. `What it was` cell: a seven-line note — say seven, not five and not
    six — holding the logo block, one sentence about finding avrtools on Windows, and one external
    link. `What happened` cell: deleted outright in Phase 171 under LEGACY-04 (D-05), because its
    single fact is already answered on the wiki `Home` page, which gives the `apt` and `brew` lines
    and adds that avrdude also ships inside the Arduino IDE and PlatformIO; then the recovery
    command as inline code, `git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:things.md`.
    End the cell with a period, as every neighbouring row does.

    Row 2, `SECURITY.md`. `Source path` cell: `firestarter_app/SECURITY.md` in backticks. `What it
    was` cell: the GSD Phase 69 security-audit record dated 2026-06-15 — its first lines were
    `# SECURITY.md`, `## Phase Security Audit` and a `**Phase:** 69 — cli-command-surface-robustness-audit`
    line — sitting at the path GitHub reads as a repository's security policy. `What happened` cell:
    deleted outright in Phase 171 under LEGACY-05 (D-01) with **no replacement**, because the same
    audit already has a canonical home in the meta repo at
    `.planning/milestones/v1.12-phases/69-cli-command-surface-robustness-audit/69-SECURITY.md`, from
    which it differs only in framing; then the recovery command as inline code,
    `git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:SECURITY.md`. End with a
    period.

    Do **not** write anything in either cell about how to report a problem, who to contact, or what
    the project would do with such a report. D-02 forbids a disclosure statement anywhere in this
    phase, and a provenance table is not an exemption.

    **What not to touch.** This is an append-only edit. Do not fix the `Protocol-Flags` and
    `Protocol-ID` rows at `:18-19`, which point at pages the live wiki no longer has — confirmed-real
    drift, explicitly deferred by CONTEXT.md to whoever owns the table. Do not reflow, re-align or
    re-pad any existing row. Do not add a comment of any kind: the project's hard no-comments rule is
    absolute here.

    Commit to meta with a path-scoped `git commit -- .planning/v1.35/MIGRATION-TABLE.md`. Meta's working
    tree carries pre-existing `M firestarter`, `M firestarter_app` and
    `M .planning/notes/dev-test-sequence-cost-model.md` entries that are not yours; the gitlinks
    belong to plan 171-04. Subject: `docs(171-03): record the three stray-file dispositions in the migration table`.
    Precedent for touching this table alone in its own commit: `d10bd4b7`.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(grep -c 'd56424e1979edf7245cffb9ec3111c0469f5b23f' .planning/v1.35/MIGRATION-TABLE.md)" -ge 8 && grep -qF '| firestarter_app | firestarter_app/autocomplete.md | Shell-Completion | Shell Completion | d56424e1979edf7245cffb9ec3111c0469f5b23f | 171 |' .planning/v1.35/MIGRATION-TABLE.md && grep -qx '## Removed without ever being published' .planning/v1.35/MIGRATION-TABLE.md && grep -qF 'git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:things.md' .planning/v1.35/MIGRATION-TABLE.md && grep -qF 'git -C firestarter_app show d56424e1979edf7245cffb9ec3111c0469f5b23f:SECURITY.md' .planning/v1.35/MIGRATION-TABLE.md && test "$(sed -n '/^## Removed without ever being published$/,/^## /p' .planning/v1.35/MIGRATION-TABLE.md | grep '^|' | awk -F'|' '{print NF}' | sort -u | tr -d '\n')" = "5" && test "$(git diff HEAD~1 --numstat -- .planning/v1.35/MIGRATION-TABLE.md | awk '{print $2}')" -eq 0 && ! git diff HEAD~1 -- .planning/v1.35/MIGRATION-TABLE.md | grep -q '^+.*<!--'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'd56424e1979edf7245cffb9ec3111c0469f5b23f' .planning/v1.35/MIGRATION-TABLE.md` returns 10 — the 7 present before this plan, plus the new main-table row, plus one inline recovery command in each of the two deletion rows; V-15 requires at least 8
    - `grep -qF '| firestarter_app | firestarter_app/autocomplete.md | Shell-Completion | Shell Completion | d56424e1979edf7245cffb9ec3111c0469f5b23f | 171 |'` succeeds — the main-table row matches the exemplar's shape byte-for-byte
    - The new row is the last row of the main table and the row above it is the `firestarter_app/doc/protocol-id.md` row
    - A heading line reading exactly `## Removed without ever being published` exists, and it sits immediately before the `## Honesty note:` heading
    - Every table line inside the new section splits into exactly 5 fields on `|` — that is a 3-column row — so the section's table can never key a fifth cell as `Pre-deletion SHA`
    - The new section's separator line is exactly `|---|---|---|` with no alignment colons
    - Both new rows' `Source path` cells wrap the path in backticks, and both `What happened` cells end with a period
    - The `things.md` row says seven lines, and the strings `five lines`, `5 lines`, `six lines` and `6 lines` appear nowhere in the new section
    - `git diff HEAD~1 --numstat -- .planning/v1.35/MIGRATION-TABLE.md` reports 0 deletions — the edit is append-only, and the deferred `Protocol-Flags` / `Protocol-ID` drift at `:18-19` is untouched
    - The diff adds no line containing `<!--` and no `#`-prefixed annotation comment
    - `git grep -in "report a vulnerability\|security policy\|responsible disclosure" -- .planning/v1.35/MIGRATION-TABLE.md` returns nothing
    - `git show --stat --format="" HEAD` in meta names exactly one file, `.planning/v1.35/MIGRATION-TABLE.md`; neither gitlink is in the commit
  </acceptance_criteria>
  <done>All three dispositions are recorded in the migration table with a recoverable oracle each, the new section is 3 columns wide, and nothing existing was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Prove honest01's parse is unharmed, and write the out-of-scope ledger</name>
  <read_first>
    - `tools/wiki/honest01_claims.py:74-93` — `parse_migration_table`, and `:234` where it is called; it is the only reader of this table
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md` § C.9 — the exact snippet to run and the expected 8-row result
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-RESEARCH.md` § "CONTEXT.md CORRECTIONS" C-1 and C-4, and the out-of-scope note beneath the table — the source facts for ledger items 2, 3 and 4
  </read_first>
  <files>.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-migration-table-parse.txt, .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-out-of-scope-ledger.md</files>
  <action>
    **(a) The parse check (V-16).** From `/workspaces`, run the § C.9 snippet: put `tools/wiki` on
    `sys.path`, import `parse_migration_table` from `honest01_claims`, call it on
    `.planning/v1.35/MIGRATION-TABLE.md`, print the row count and then each row's `Source path`,
    `Wiki page` and `Moved in`. Expect exactly **8** rows — the 7 it returns today plus
    `firestarter_app/autocomplete.md -> Shell-Completion 171` — and no row whose `Wiki page` is
    `things.md`, `SECURITY.md`, or a fragment of prose. Write the full output to
    `evidence/171-03-migration-table-parse.txt`.

    `honest01_claims.py` is a retired one-shot rather than a standing CI gate
    (`MIGRATION-TABLE.md:104-105`), so a malformed row would not turn anything red — it would break
    silently, and surface only when someone next runs the checker. That is precisely why this leg
    exists.

    **(b) The out-of-scope ledger.** Write
    `evidence/171-03-out-of-scope-ledger.md` — a plain markdown list, no frontmatter needed —
    recording every item this phase deliberately did **not** act on, each with the measured fact, the
    reason, and who owns it. Eight items, one short paragraph or bullet each:

    1. `MIGRATION-TABLE.md`'s `Protocol-Flags` and `Protocol-ID` rows list pages a fresh clone of the
       live wiki does not have. Confirmed-real drift from the operator's post-168 reorganisation.
       Deferred by CONTEXT.md to whoever owns the table; this phase only appends. Worth fixing before
       the Backlog 999.9 rename sweep greps the table for source paths.
    2. `.github/workflows/wiki-check.yml:104-107` passes `--wiki-dir` to `dispatch_mirror.py`, which
       accepts only `--app-dir` and `--fw-dir` (`tools/wiki/dispatch_mirror.py:157-158`). That step
       exits 2 the first time the workflow ever runs. Introduced by `4b14a5a2`. Present on
       `origin/beta` too. Not fixed here — recorded so a future red run is not blamed on Phase 171.
    3. `wiki-check.yml` is absent from `origin/main`, the default branch of `henols/firestarter_prom`,
       so GitHub never registered its `schedule` or `workflow_dispatch`; `gh workflow list --all`
       returns only `Catalog sync check`. There was **no CI safety net** for this phase — every check
       ran locally and its output is in this evidence directory. Putting the workflow on the default
       branch is Phase 172/173 territory.
    4. `firestarter/test/native/avr/test_eeprom28c_sdp/RED-BASELINE.md:637` names `SECURITY.md` in a
       Phase 117 list of files that were untracked at that time. It is not a link. Historical-by-intent;
       no action; do not edit it. It also corrects CONTEXT.md's claim that the only hit was the file's
       own title line.
    5. `firestarter_app/tools/build_db.py` carries an uncommitted modification that predates Phase 171
       and is unrelated to it. Excluded from every Phase 171 commit by path-scoped staging, and left
       unresolved rather than stashed or reverted.
    6. `firestarter_app/firestarter.egg-info/SOURCES.txt` is gitignored, still lists two of the deleted
       files, and is re-absorbed by setuptools into every sdist built in that tree — the cause of the
       220-entry false positive. No repository change is owed; the rule is simply never to build the
       packaging oracle in the live working tree.
    7. `github.com/henols/firestarter_app/blob/main/autocomplete.md` still resolves, and still serves
       the older argcomplete-era text, until the milestone branch merges to `main`. The three success
       criteria are branch-scoped properties, so this is a sequencing note rather than a criterion
       failure.
    8. Observing that `henols/firestarter_app`'s Security tab is empty is deferred to the milestone
       merge. The tab derives from the default branch, where `SECURITY.md` has never existed, so the
       API reports no policy today and recording that as a pass would be vacuous.

    Commit both evidence files to meta with a path-scoped commit.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-migration-table-parse.txt && python3 -c "import sys; sys.path.insert(0,'tools/wiki'); from pathlib import Path; from honest01_claims import parse_migration_table; rows=parse_migration_table(Path('.planning/v1.35/MIGRATION-TABLE.md')); print('rows with a SHA:', len(rows)); [print(' ', r['Source path'], '->', r['Wiki page'], r['Moved in']) for r in rows]" > "$EV" 2>&1 && grep -qx 'rows with a SHA: 8' "$EV" && grep -qF 'firestarter_app/autocomplete.md -> Shell-Completion 171' "$EV" && ! grep -qE '\-> (things\.md|SECURITY\.md)' "$EV" && test -s .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-out-of-scope-ledger.md && test "$(grep -cE 'wiki-check\.yml|dispatch_mirror\.py|Protocol-Flags|RED-BASELINE\.md|build_db\.py|SOURCES\.txt|Security tab|blob/main' .planning/phases/171-stray-the-root-level-documentation-files/evidence/171-03-out-of-scope-ledger.md)" -ge 8</automated>
  </verify>
  <acceptance_criteria>
    - The parse snippet prints `rows with a SHA: 8` — 7 before this phase plus `Shell-Completion`; this is V-16
    - The printed rows include `firestarter_app/autocomplete.md -> Shell-Completion 171`
    - No printed row's `Wiki page` field is `things.md`, `SECURITY.md`, or a prose fragment — the 3-column deletion section was absorbed and filtered out exactly as the existing retired section is
    - `evidence/171-03-migration-table-parse.txt` holds that full output
    - `evidence/171-03-out-of-scope-ledger.md` exists and names all eight items, each identifiable by its own anchor string: `wiki-check.yml`, `dispatch_mirror.py`, `Protocol-Flags`, `RED-BASELINE.md`, `build_db.py`, `SOURCES.txt`, `Security tab`, `blob/main`
    - The ledger states for item 3 that no CI covered this phase, so a reader does not later assume a green cron existed
    - Neither evidence file contains a security-reporting statement
    - `git show --stat --format="" HEAD` in meta names exactly the two evidence files; neither gitlink is in the commit
  </acceptance_criteria>
  <done>The only machine reader of the migration table still parses it to 8 clean rows, and every deliberately-unfixed item is on the record with its cause and its owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `MIGRATION-TABLE.md` → `honest01_claims.parse_migration_table` | The table is machine-read. A malformed row breaks a shipped tool without turning any CI red, because the checker is a retired one-shot rather than a standing gate. |
| `MIGRATION-TABLE.md` → the Backlog 999.9 rename sweep | A future sweep greps this table for source paths before renaming repositories. A wrong or missing path sends it somewhere that does not exist. |
| provenance prose → future reader | A row is the permanent answer to "what happened to this document". A wrong fact here outlives every phase record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-171-11 | Tampering | `honest01_claims.parse_migration_table` | medium | mitigate | The new section is capped at 3 columns so its rows are truncated by `zip` against the never-reset 6-column header, get no `Pre-deletion SHA` key, and are filtered at `:93` — exactly as the existing retired section already is. Asserted structurally (every row splits into 5 `|`-fields) and behaviourally (V-16 returns 8 rows). |
| T-171-12 | Repudiation | the two deletion rows' prose | medium | mitigate | Each row carries the file's true nature and a runnable recovery command against a frozen, verified SHA, so the claim "nothing was lost" is checkable rather than asserted. C-7's corrected line count is used, so the permanent record does not inherit CONTEXT.md's or the ROADMAP's wrong figure. |
| T-171-13 | Repudiation | a disclosure statement smuggled into the provenance record | high | mitigate | D-02 covers every artifact in the phase, a provenance table included. A direct grep of the file for the three disclosure-policy phrases is an acceptance criterion, on top of V-05's repo-wide guard in plan 171-02. |
| T-171-14 | Tampering | existing table rows | low | mitigate | The edit is append-only. `git diff --numstat` reporting 0 deletions is an acceptance criterion, which also protects the deliberately-deferred `Protocol-Flags` / `Protocol-ID` rows from a well-meaning drive-by fix. |
| T-171-SC | Tampering | package-manager installs | low | accept | This plan installs nothing and runs only stdlib Python already present in the repository. `171-RESEARCH.md` § "Package Legitimacy Audit" records the gate as vacuous for this phase: zero external packages in any ecosystem, zero `[SUS]`, zero `[SLOP]`. |
</threat_model>

<verification>
This plan's V-rows from `171-VALIDATION.md`: **V-15** (Task 1) and **V-16** (Task 2).

Neither oracle trap is present: no working-tree sdist build and no `community/profile` call appears
in any `<automated>` leg here. `wiki-check.yml` is not invoked — it is not registered with GitHub
Actions and there is nothing to run.
</verification>

<success_criteria>
- `MIGRATION-TABLE.md` carries a Phase 171 main-table row for `autocomplete.md` matching the exemplar's shape
- It carries a new 3-column section recording `things.md` and `SECURITY.md` with a recovery command each
- `grep -c` for the branch-point SHA returns at least 8
- `parse_migration_table` returns 8 rows, none of them a deletion row
- The diff is append-only: zero deleted lines, zero modified existing rows, no comment added
- Both evidence files exist and are committed to meta
- The eight out-of-scope items are on the record
</success_criteria>

<output>
Create `.planning/phases/171-stray-the-root-level-documentation-files/171-03-SUMMARY.md` when done.

Record in it: the post-edit line numbers of the new main-table row and the new section heading, so
any later `.planning/` citation of this file uses current line numbers rather than the pre-edit ones
quoted in `171-PATTERNS.md`; and that LEGACY-04, LEGACY-05 and LEGACY-07 are **not** complete at this
plan's close — the gitlink leg lands in plan 171-04, which is where the requirement checkboxes flip.
</output>
