# Phase 173: CLOSE — Beta Cut Under Protection, Close Procedure & Honesty Ledger - Pattern Map

**Mapped:** 2026-09-02
**Files analyzed:** 8 (1 new code file, 1 new generator-or-inline step, 2 new records, 4 modified)
**Analogs found:** 8 / 8 (every deliverable has a tracked in-repo analog; nothing is greenfield)

RESEARCH.md §"Architecture Patterns" already names three patterns — the `tools/wiki/` checker
contract, the `wiki-check.yml` leg shape, and planted-failure-first. This file **grounds** them in
excerpts and adds the analogs research did not name: `honest01_claims.py` (the true closest analog,
not `dispatch_mirror.py`), the ROADMAP 999.x row shape, the `.planning/notes/` header shape, and the
`CLAUDE.md` pointer-bullet shape.

**Tracked-source gate:** every analog path below was verified with `git ls-files` in
`/workspaces` and is tracked meta-repo source. No path here is a gitignored mirror.

---

## Hard constraints the planner must carry into every plan

| Constraint | Mechanical form |
|---|---|
| **NO COMMENTS.** Zero `#`/`//`/`<!-- -->` comments in anything written. A plan cannot override it. | All four `tools/wiki/*.py` measure **exactly 1** line matching `^\s*#` — the shebang `[VERIFIED: /usr/bin/grep -cE '^\s*#' tools/wiki/*.py → 1,1,1,1]`. Prose lives in the **module docstring** only. `wiki-check.yml` is dense with `#` blocks written by earlier phases — **do not match local style, match the rule.** A usable verify leg: `test "$(/usr/bin/grep -cE '^\s*#' tools/wiki/<new>.py)" = 1`, and the new `wiki-check.yml` leg block contains zero `#`. |
| **`/usr/bin/grep` for all gate evidence.** | PATH `grep` is ugrep 7.8.4 and honours `.gitignore`. Repo-wide scans scope to `git ls-files`. Note the *existing* LEGACY-01 CI leg uses bare `grep` — that is correct **inside the runner** (GNU) and must not be copied into a local gate. |
| **Do not run `tools/wiki/selftest.sh`** — it mutates Phase 168 evidence. If run anyway, `git checkout --` the evidence files and assert restoration. |
| **`ROADMAP.md` writes are the orchestrator's**, hand-authored, never regenerated by a GSD verb. |
| **`.planning/config.json` is hand-edited**, never through a GSD verb. |
| **Never assert an empty `git status --porcelain` on `firestarter_app`** — it carries ` M tools/build_db.py` (999.45). |

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| NEW `tools/wiki/provenance_footers.py` (name at discretion) | utility / checker CLI | batch file-I/O + transform (two inputs → failure list → exit code) | `tools/wiki/honest01_claims.py` | **exact** — same two inputs (`--table` MIGRATION-TABLE.md, `--wiki-dir` clone), same row parser, same per-row resolve-then-compare loop |
| NEW footer *generator* (a mode of the same script, or a one-shot) | utility | transform (table row → footer line) | `honest01_claims.py::parse_migration_table` + `read_destination_page` | **exact** (read side); the write side has no analog — see §No Analog Found |
| MODIFY `.github/workflows/wiki-check.yml` (a 4th `run:` leg) | config / CI | request-response (invoke, exit code) | the three existing legs at `:107-125`; LEGACY-01 leg at `:99-105` is the most recently added | **exact** |
| NEW `.planning/v1.35/CLOSE-RECORD.md` | record | document | `.planning/v1.34/CLOSE-RECORD.md` | **exact** |
| NEW `.planning/notes/v135-close-procedure-under-protection.md` | record | document | `.planning/notes/v135-wiki-only-reversal.md` | **exact** |
| MODIFY `.planning/config.json` (`git.base_branch`, `git.protected_branches`) | config | key-value | the existing `"git": { "branching_strategy": "milestone" }` block | **exact** |
| MODIFY `/workspaces/CLAUDE.md` (pointer lines) | config / docs | document | the `## Key Architecture Points` bullet list | role-match |
| MODIFY `.planning/ROADMAP.md` (`### Phase 999.46`) | record | document | `### Phase 999.45` at `:5093-5105` | **exact** |
| MODIFY wiki clone pages (6 footers) | content | file-I/O, out-of-repo | `honest02_truth.py`'s `STAMP_RE` stamp comment | partial — see §No Analog Found |

**Corrected scope, per RESEARCH C-1:** the footer set is **6 pages**, not 12. `MIGRATION-TABLE.md`
has 17 data rows across three tables; the main table has 10 rows; 2 have `—` for source path and SHA
(`Home`, `Contributing`), 2 name dead pages (`Protocol-Flags`, `Protocol-ID`). The 6 are
`Programming-Protocols`, `Shield-Revisions`, `Install-Beta`, `Testing-Chips`, `Lockable-PROMs`,
`Shell-Completion`.

---

## Pattern Assignments

### NEW `tools/wiki/<checker>.py` (utility, batch transform)

**Analog: `tools/wiki/honest01_claims.py`** — not `dispatch_mirror.py`. RESEARCH.md recommends
`dispatch_mirror.py` for the *contract*, and that is right, but `honest01_claims.py` already takes
**exactly the two inputs D-10 needs** (`--table` = MIGRATION-TABLE.md, `--wiki-dir` = wiki clone)
and already resolves rows to pages. Copy `honest01_claims.py`'s I/O skeleton; copy
`dispatch_mirror.py`'s single-`OK:`-line ending if the checker has one leg, or
`honest02_truth.py`'s per-leg reporting if it has two (bidirectional — see below).

**Module docstring pattern — the only place prose is allowed** (`honest01_claims.py:2-31`,
`dispatch_mirror.py:2-21`). It states the exit-code contract explicitly and names the failure
vocabulary:

```python
#!/usr/bin/env python3
"""
tools/wiki/honest01_claims.py -- one-shot proof that migrating 12 doc/ files
onto the published GitHub wiki upgraded no claim.

Exit-code contract:
  0 = every claim token the source carried is present at least as often on
      the published page, for every resolvable row
  1 = a claim token was dropped, or a row's source or destination text could
      not be resolved and read
  2 = a precondition was not met (a required path missing, or the
      vocabulary file missing or malformed)
"""
```

`honest02_truth.py:24-38` shows the **distinct-vocabulary** convention for a multi-leg checker
(`MISSING STAMP` / `UNRESOLVED` / `STALE STAMP` / `UNCHECKED`), *"so a database disagreement and a
navigation-shaped problem never arrive as one indistinguishable red"*. A bidirectional footer
checker should name its own two vocabularies the same way, e.g. `FOOTER MISSING` / `FOOTER DRIFTED`
(table→page) and `UNRECORDED PAGE` (page→table).

**Imports pattern** (`honest01_claims.py:33-45`) — stdlib only, `from __future__` first, and the
sibling-import trick if `wiki.py` helpers are wanted:

```python
from __future__ import annotations

import argparse
import json
import re
import subprocess
import sys
from collections import Counter
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from wiki import strip_code_spans
```

**Module-level `UPPER_CASE` constants for every path, delimiter and regex** (`honest01_claims.py:47-50`):

```python
TABLE_ROW_RE = re.compile(r"^\|(.+)\|\s*$")
WHITESPACE_RE = re.compile(r"\s+")
NO_SHA_MARKER = "—"
REQUIRED_VOCAB_KEYS = ("schema_version", "families", "expected_zero")
```

**The anchored-table parser — copy this verbatim** (`honest01_claims.py:72-92`). It is the single
highest-value reuse in the phase, and it *already* solves RESEARCH Pitfall 7 (the `—` rows) in its
final line:

```python
def parse_migration_table(table_path: Path) -> list[dict[str, str]]:
    header: list[str] = []
    in_table = False
    rows: list[dict[str, str]] = []
    for line in table_path.read_text(encoding="utf-8").splitlines():
        match = TABLE_ROW_RE.match(line)
        if match is None:
            in_table = False
            continue
        cells = [cell.strip() for cell in match.group(1).split("|")]
        if not header:
            header = cells
            continue
        if all(set(cell) <= {"-", ":"} for cell in cells):
            in_table = True
            continue
        if not in_table:
            continue
        rows.append(dict(zip(header, cells)))
    return [row for row in rows if row.get("Pre-deletion SHA", NO_SHA_MARKER) != NO_SHA_MARKER]
```

> **Behaviour the planner must know before reusing it.** `header` is captured from the **first**
> table only and never reset, so rows from the second table (`| Source path | Was published as |
> What happened |`, `MIGRATION-TABLE.md:59`) and the third (`| Source path | What it was | What
> happened |`, `:82`) are also appended — keyed by the *first* table's header names. They survive
> only because they have no `Pre-deletion SHA` cell and the final filter drops them. That is
> load-bearing accident, not design. A **bidirectional** checker (RESEARCH C-2) must enumerate the
> retired tables deliberately rather than lean on this, or it will report the 5 retired pages and
> the 2 deleted ones as "unrecorded". Recommend: parse the main table by header identity
> (`"Wiki page" in header`) and build the retired/deleted page set from the other two explicitly.

**Row → page resolution and the `UnresolvedRow` failure carrier** (`honest01_claims.py:51-56, :122-129`):

```python
class UnresolvedRow(Exception):
    def __init__(self, page: str, reason: str) -> None:
        super().__init__(reason)
        self.page = page
        self.reason = reason


def read_destination_page(wiki_dir: Path, wiki_page: str) -> str:
    page_path = wiki_dir / f"{wiki_page}.md"
    if not page_path.is_file():
        raise UnresolvedRow(wiki_page, f"{page_path} does not exist")
    text = page_path.read_text(encoding="utf-8")
    if not text.strip():
        raise UnresolvedRow(wiki_page, f"{page_path} is empty")
    return text
```

This is **already RED on the real inputs** for `Protocol-Flags` and `Protocol-ID` — D-10's
predicted first failure, reached with zero new code.

**Page enumeration for the page→table direction** — copy `honest02_truth.py:314-320`, which is
itself a copy of `wiki.py`'s `NAV_EXCLUDED_PAGES` precedent (`wiki.py:37`):

```python
NAV_PAGES = frozenset(
    {"Home.md", "How-To-Edit-This-Wiki.md", "_Sidebar.md", "_Footer.md"}
)

def load_pages(wiki_dir: Path) -> dict[str, str]:
    pages: dict[str, str] = {}
    for path in sorted(wiki_dir.glob("*.md")):
        if path.name in NAV_PAGES:
            continue
        pages[path.name] = path.read_text(encoding="utf-8")
    return pages
```

**Argparse pattern** (`honest01_claims.py:199-209`) — `prog=`, `description=__doc__`,
`RawDescriptionHelpFormatter`, `type=Path`, `required=True`, no positional args, no defaults that
could silently point at the wrong tree:

```python
def _build_argparser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(
        prog="honest01_claims.py",
        description=__doc__,
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument("--table", type=Path, required=True, default=None)
    parser.add_argument("--wiki-dir", type=Path, required=True, default=None)
    parser.add_argument("--vocab", type=Path, required=True, default=None)
    parser.add_argument("--repo-root", type=Path, required=True, default=None)
    return parser
```

`wiki.py:274-285` shows the `help=` variant with the reasoning inline, and its stated rule —
*"a required argument, not a module constant with a default"* — is why the new checker must take
`--migration-table` (or `--table`) rather than hardcoding it.

**`main()` pattern: preconditions → compare → failures → single OK line**
(`honest01_claims.py:212-226` for the precondition block, `dispatch_mirror.py:245-251` for the tail):

```python
def main() -> int:
    args = _build_argparser().parse_args()

    if not args.table.is_file():
        print(f"ERROR: --table not found: {args.table}", file=sys.stderr)
        return 2
    if not args.wiki_dir.is_dir():
        print(f"ERROR: --wiki-dir not found: {args.wiki_dir}", file=sys.stderr)
        return 2
```

```python
    if failures:
        for message in failures:
            print(f"ERROR: {message}", file=sys.stderr)
        return 1

    print(f"OK: {len(doc_table)} protocols compared across firmware doc, host tool and firmware.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

**The vacuity guard** — `honest01_claims.py:274-278` and `dispatch_mirror.py:36, :202-208` both
refuse to pass on an empty/degenerate input. `dispatch_mirror.py` returns **2**, not 0, when the
parsed row count drops below `MIN_BUCKET_ROWS = 6`:

```python
    bucket_rows = parse_bucket_rows(region)
    non_phantom_rows = [row for row in bucket_rows if not row[2]]
    if len(non_phantom_rows) < MIN_BUCKET_ROWS:
        print(
            f"ERROR: claims region parsed to {len(non_phantom_rows)} bucket "
            f"row(s), fewer than the required minimum of {MIN_BUCKET_ROWS}",
            file=sys.stderr,
        )
        return 2
```

`honest01_claims.py:226` is the same idea in one line: `if not rows: … "contains no rows with a
Pre-deletion SHA" … return 2`. **The new checker owes this**: a checker that greens on an empty
table would be exactly the "check never seen to fail" D-10 forbids. The natural floor is the 6
footer-eligible rows.

**Count-bearing OK line** (`honest01_claims.py:301-304`) — the count is what makes the green line
falsifiable:

```python
    print(
        f"OK: {pages_compared} pages compared, {tokens_compared} tokens compared, "
        f"0 dropped, {len(all_added)} added, {len(vacuous)} vacuous."
    )
```

---

### MODIFY `.github/workflows/wiki-check.yml` (config, CI)

**Analog: the three python legs at `:107-125`, plus the LEGACY-01 leg at `:99-105`** (Phase 172's,
the most recently added). All four are `- name:` + `run: |` + the invocation + a plain-words
`echo "OK: …"`. Verbatim:

```yaml
      - name: WIKI-05 reachability check
        run: |
          python3 meta/tools/wiki/wiki.py links --source-dir wiki-clone
          echo "OK: every wiki page is reachable from Home.md and listed in _Sidebar.md"

      - name: HONEST-02 truth check
        run: |
          python3 meta/tools/wiki/honest02_truth.py \
            --wiki-dir wiki-clone \
            --db firestarter_app/firestarter/data/chip_database.json \
            --allowlist meta/tools/wiki/claim-allowlist.json
          echo "OK: every stamped claims region still resolves against the current chip database"

      - name: Dispatch-mirror check
        run: |
          python3 meta/tools/wiki/dispatch_mirror.py \
            --app-dir firestarter_app \
            --fw-dir firestarter
          echo "OK: the wiki's dispatch table still mirrors the host tool and the firmware test"
```

The new leg follows exactly, appended after the dispatch-mirror leg:

```yaml
      - name: <REQ-ID> provenance footer check
        run: |
          python3 meta/tools/wiki/<script>.py \
            --wiki-dir wiki-clone \
            --migration-table meta/.planning/v1.35/MIGRATION-TABLE.md
          echo "OK: <the property, in plain words>"
```

**Path conventions are load-bearing:** meta is checked out at `path: meta`, so meta files are
`meta/tools/wiki/…`; the wiki clone is `wiki-clone`; the sub-repos are bare `firestarter` /
`firestarter_app`. `[VERIFIED: wiki-check.yml:51-97]` **No new checkout or clone step is needed.**

**NO-COMMENTS applies to the new leg** even though `:14-50` is a 37-line comment block. The leg
above carries none.

**LEGACY-01 leg for contrast** (`:99-105`) — an inline-shell leg rather than a python one; note it
uses `--exclude-dir=.git --exclude-dir=.planning` and bare `grep` (GNU on the runner):

```yaml
      - name: LEGACY-01 dead tracker link check
        run: |
          if grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git --exclude-dir=.planning; then
            echo "FAIL: a page links to an issue tracker that is disabled"
            exit 1
          fi
          echo "OK: no page links to a disabled issue tracker"
```

---

### NEW `.planning/v1.35/CLOSE-RECORD.md` (record, document)

**Analog: `.planning/v1.34/CLOSE-RECORD.md`** (245 lines). `.planning/v1.35/` does not exist and
must be created.

**Header block** (`:1-7`) — title names the three products, then Milestone / Closed / Discharges /
Status, and a status line that tells the reader where to start:

```markdown
# v1.34 — Close Record: Evidence Table, Merge Recommendation & Honesty Ledger

**Milestone:** v1.34 Pre-Merge Hardware Regression Validation
**Closed:** 2026-08-29, by operator direction ("stop testing, close ASAP")
**Discharges:** CLOSE-01…05, RCA-01…05
**Status of this close:** **EARLY / SCOPE-REDUCED.** This is not a completed sweep. Read
§1 before reading anything else.
```

**Ledger table — copy the header verbatim** (`:196`), re-keyed to v1.35 and, per D-11,
comprehensive rather than curated at ten:

```markdown
| # | What v1.34 claims | What v1.34 explicitly does NOT claim |
|---|---|---|
| H-6 | Read chains and write-read-verify were SHA-judged on silicon | **That program-window VPP/VCC under load was measured. IT WAS NOT.** … **v1.34 makes no electrical claim whatsoever.** |
```

The row voice worth copying: the claim half is a flat statement of what was measured; the non-claim
half **names the specific unmeasured thing and bolds the negation**. Rows H-4 and H-7 model the
"consistent with, not proof of" and "UNDETERMINED" registers.

**Criterion-4 "filed, not carried as prose" table** (`:211-227`), header `| Finding | Filing |`:

```markdown
| Finding | Filing |
|---|---|
| Ratiometric ~+7.5 % VPP ADC error (§3.4) | **new backlog item** |
| uno328pb program-path brownout | existing **999.2** (RCA-05) |
```

Note both dispositions appear — a **new** row and a pointer to an **existing** one. That is the
shape CONTEXT.md §Claude's Discretion asks for ("its own backlog row versus a line in an existing
row").

**§1 "Scope — what ran, and what did not"** (`:11-30`), table `| Phase | Planned | Actually
executed | Status |`, followed by bolded plain-English enumerations of the gaps ("**The six chips
never swept:** …"). v1.35's analogue is the phase checklist discrepancy CONTEXT.md names (Phase 172
unchecked, Phases 169/170 ad-hoc).

**§7 deviation section** (`:229-245`) — D-03 **explicitly declined to reuse this**. Present only as
the shape *not* to take: POLICY-04 is marked complete on the probe with a ledger non-claim, not
recorded as a deviation.

---

### NEW `.planning/notes/v135-close-procedure-under-protection.md` (record, document)

**Analog: `.planning/notes/v135-wiki-only-reversal.md`** — the most recent note and the one this
phase's readers already know.

**Header block** (`:1-5`):

```markdown
# v1.35 — Operator reverses the in-repo wiki source model

**Date:** 2026-08-30
**Raised during:** `/gsd-discuss-phase 168` (session halted before `168-CONTEXT.md` was written)
**Status:** Decided by operator; ROADMAP.md and REQUIREMENTS.md repair APPLIED (WIKI-03/04 withdrawn, …)
```

Then `## The decision` (a quoted operator sentence and what it reverses), then `## What this voids`
as a `| Artifact | Disposition |` table. For POLICY-05 the analogous sections are: the decision
(`base_branch: beta`), what it changes (the 7 consumer sites RESEARCH lists with line numbers), and
**what is blocked end to end** (D-08: PR is the only route to `main`,
`current_user_can_bypass: never`, and the version bump then fails per 999.46).

---

### MODIFY `.planning/config.json` (config, key-value)

**Analog: the file's own `git` block.** Current tracked state, verbatim:

```json
  "git": {
    "branching_strategy": "milestone"
  },
```

Target (RESEARCH proved this read-back against a scratch copy; `protected_branches` is
**nested-only** — a top-level spelling is deliberately not honoured, `git-base-branch.cjs:84-87`):

```json
  "git": {
    "branching_strategy": "milestone",
    "base_branch": "beta",
    "protected_branches": ["main"]
  },
```

JSON has no comment syntax, so the NO-COMMENTS rule is satisfied structurally. Two-space indent,
trailing-comma-free, matching every sibling block. **Hand-edited, not via a GSD verb.**

**Verify-leg trap to carry forward:** `--is-protected main` is **already** `true` before the edit
(tier 2 resolves `main` as base branch, and the base branch is always folded in at
`git-base-branch.cjs:305`). The distinguishing read-backs are `git.base-branch` → `beta` and
`--is-protected beta` flipping `false` → `true`, plus `--is-protected <feature-branch>` → `false`
to prove the resolver verified rather than fail-closed.

---

### MODIFY `/workspaces/CLAUDE.md` (config/docs)

**Analog: the `## Key Architecture Points` bullet list** — bolded lead-in, a backticked path, one
sentence of consequence:

```markdown
- **Serial protocol changes** must be kept in sync between `firestarter_app/firestarter/serial_comm.py` and `firestarter/src/firestarter.cpp`.
- **Constants/flag bits** are duplicated between `firestarter_app/firestarter/constants.py` (Python) and `firestarter/include/firestarter.h` (C++). Change both together.
```

The pointer lines follow that voice: a bolded lead-in naming the fact (`main` is PR-only; the close
targets `beta`), the backticked note path, no `<!-- -->` and no prose paragraph. Note the sibling
`## Repository Structure` section already states the tracked-directory rule the checker relies on
(*"`tools/wiki/` holds the checkers that run against a clone of that wiki"*) — the new lines belong
near it or in a short new `##` section, not buried in the Arduino build commands.

---

### MODIFY `.planning/ROADMAP.md` — `### Phase 999.46` (record, orchestrator write)

**Analog: `### Phase 999.45`** at `:5093`, the highest existing row, inserted immediately after its
`---` and before the `### v1.14 — Feasible-Gap Implementation (✅ PROMOTED …)` divider.

**Heading shape** (`:5093`):

```markdown
### Phase 999.45: Unattributed `_AT28C_DIP24_NAMES` rename in `build_db.py`, and the twice-stale docstring citation it exposed (BACKLOG — filed 2026-09-02 during v1.35 Phase 172, product code out of scope)
```

**Body shape**, in order: `**Goal:**` one sentence; a bolded framing paragraph naming *why it is
filed and not fixed*; measured evidence with `file#Lnnn` links; `**Chosen fix**`; `**Test
surface.**`; then `---`. Verbatim excerpts of the two that matter most:

```markdown
**Filed, not fixed, on purpose.** v1.35 is documentation and repository configuration only.
`REQUIREMENTS.md`'s scope note says any product-code change found necessary in this milestone is
**filed rather than fixed** … so this phase may not touch it.
```

```markdown
**The rename.** `_AT28C_DIP24_NAMES` → `AT28C_DIP24_NAMES`, two substitutions, at
[`build_db.py:545`](../firestarter_app/tools/build_db.py#L545) (the definition) and
[`build_db.py:564`](../firestarter_app/tools/build_db.py#L564) (the only use).
```

The link form is relative-from-`.planning/` with `#Lnnn` — reuse it for 999.46's six re-verified
workflow citations (`release.yml:2-5`, `:32-35`, `:37-43`, `build.yml:34`, `:182-183`, `:199-200`).
999.45 also models **naming the rejected alternatives with reasons** (`**Why (a) alone was
rejected:**`), which is exactly D-05's "recommended remedy named, not a menu".

---

### MODIFY the wiki clone's pages — 6 provenance footers (content, out-of-repo file-I/O)

**Analog: `honest02_truth.py`'s stamp**, the only existing machine-checkable per-page trailer
(`honest02_truth.py:89-92`):

```python
STAMP_RE = re.compile(
    r"<!--\s*firestarter-claim-stamp:\s*db-sha256-16=([0-9a-f]{16})\s+"
    r"verified=(\d{4}-\d{2}-\d{2})\s*-->"
)
```

**Two ways the analogy must be broken, both measured in RESEARCH:**

1. The stamp is an **HTML comment**. D-09's footer is *reader-facing prose*, so it must be visible
   markdown, not a comment — and the NO-COMMENTS rule independently forbids emitting the comment
   form. Take from the stamp only the **structure**: a fixed prefix, machine-parseable fields, one
   line, end of file.
2. **Pitfall 2 (demonstrated RED):** a footer whose source path carries a part-number-shaped token
   flips `honest02_truth.py` leg 1 to `MISSING STAMP` on an unstamped page. None of the six source
   paths (`PROTOCOLS.md`, `SHIELD-REVISIONS.md`, `beta-testing-install.md`,
   `community-validation.md`, `lockable-proms.md`, `autocomplete.md`) contains one, and all six
   footers were proven green against both existing checkers in a throwaway clone. **The regression
   leg:** after pushing, re-run `wiki.py links` and `honest02_truth.py` against a **fresh** clone and
   assert leg 1's "matched the claim signature" count stays at 5.

**Delivery has no in-repo analog** — clone-commit-push to
`https://github.com/henols/firestarter_prom.wiki.git`, no PR, no review, no CI on the edit. The
footers add no page, so the two navigation edits (`_Sidebar.md`, `Home.md`) are **not** owed unless
the `Protocol-Flags`/`Protocol-ID` resolution adds or deletes a page.

---

## Shared Patterns

### Zero-comment source
**Source:** all four `tools/wiki/*.py`
**Apply to:** the new checker, the `wiki-check.yml` leg, the config keys
Exactly one `#` line per file — the shebang. All explanation lives in the module docstring.
Assertable: `test "$(/usr/bin/grep -cE '^\s*#' <file>)" = 1`.

### The `0 / 1 / 2` exit-code contract
**Source:** `dispatch_mirror.py:8-12`, `wiki.py:6-11`, `honest02_truth.py:13-22`
**Apply to:** the new checker and its CI leg
```
0 = the asserted property holds
1 = the asserted property is false
2 = a precondition was not met (source directory missing, or missing
    on the command line at all)
```
Three checkers and three CI legs already depend on it. `ERROR:` lines go to **stderr**; the single
count-bearing `OK:` line goes to **stdout**; `2` is reserved for preconditions and is never used for
a real disagreement.

### Precondition-first `main()`
**Source:** `honest01_claims.py:212-226`, `dispatch_mirror.py:176-198`, `honest02_truth.py:337-346`
**Apply to:** the new checker
Every path argument is `.is_file()`/`.is_dir()`-checked with its own `ERROR: --<arg> not found:`
message and `return 2`, before any parsing.

### Planted-failure-first
**Source:** Phase 172's LEGACY-01 leg, evidence at
`.planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-legacy01-redgreen.txt`
**Apply to:** the new checker (D-10), and to any config read-back leg
1. GREEN on real inputs → 2. plant a named violation in a **copy** (never the live wiki), record RED
with the exact `ERROR:` line → 3. restore byte-identically, GREEN again → 4. commit all three to
`evidence/`.
Natural plants here: a footer SHA altered by one character; a footer deleted from one page; a table
row naming a nonexistent page (**already RED on real inputs** — `Protocol-Flags`, `Protocol-ID`).
`honest01_claims.py`'s own `VACUOUS` reporting (`:274-278`) is the same instinct in-band: a check
that compared nothing says so rather than printing OK.

### Non-vacuity floor
**Source:** `dispatch_mirror.py:36, :200-208`; `honest01_claims.py:226`
**Apply to:** the new checker
A degenerate input returns **2**, never 0. Set the floor at the 6 footer-eligible rows.

### `/usr/bin/grep` scoped to `git ls-files`
**Source:** plan 172-09's scanner (0 matches under ugrep vs 38 under GNU grep, identical paths)
**Apply to:** every local gate leg in this phase
The CI legs are exempt (GNU grep on `ubuntu-latest`); local evidence is not.

---

## No Analog Found

| File / step | Role | Data flow | Reason |
|---|---|---|---|
| The footer **write** side (generator emitting into the wiki clone) | utility | transform → file-I/O | Every `tools/wiki/` script is read-only by construction; `wiki.py`'s `publish`/`sidebar` subcommands, the only writers this repo ever had, were **retired** at the wiki-only reversal (`wiki.py:13-15, :297` — one `add_parser`). Nothing in `tools/` writes to a wiki clone today. Follow RESEARCH's shape: generate from `MIGRATION-TABLE.md`, never author six strings by hand, and keep the generator's parser identical to the checker's so the two cannot disagree. |
| The ruleset push probe (POLICY-04) | test / integration | network, one-shot | No probe script or harness exists anywhere in the repo. Use RESEARCH's recipe verbatim (`git fetch origin main` → `checkout -B` → `commit --allow-empty` → `push origin HEAD:main`) and its acceptance rule: **the rejection text is the evidence, not the exit code** — assert a server-side `remote:` rule-violation line, capture full stderr verbatim, and record `non-fast-forward` / `fetch first` / `Permission denied` as **inconclusive**, never green. |
| `.planning/v1.35/` directory | — | — | Does not exist; must be created. |
| The four upstream reply drafts | record | document | v1.22 D-02's blocking-wording-review precedent is prose in `.planning/MILESTONES.md`, not a reusable artifact shape. Drafts land in the phase record; D-13 gates the posting. |

---

## Metadata

**Analog search scope:** `tools/wiki/`, `.github/workflows/`, `.planning/notes/`,
`.planning/v1.34/`, `.planning/ROADMAP.md`, `.planning/config.json`, `/workspaces/CLAUDE.md`
**Files scanned:** 4 checkers (1308 lines total), 1 workflow, 1 close record, 1 note, 1 config,
2 records
**Tracked-source verification:** `git ls-files` confirmed all 11 named analog paths
**Pattern extraction date:** 2026-09-02
