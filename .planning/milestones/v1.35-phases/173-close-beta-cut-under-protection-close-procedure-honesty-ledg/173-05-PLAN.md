---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 05
type: execute
wave: 2
depends_on: ["173-01"]
files_modified:
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-operator-approval.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-prepush-suite.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-push-transcript.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-postpush-freshclone.txt
autonomous: false
requirements: [POLICY-05]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Six migrated wiki pages carry a generated provenance footer naming the source repository, the source path and the pre-deletion SHA, stating whether the content was moved intact or edited afterwards, and stating that it was not re-verified — D-09, put where the reader actually is."
    - "The footers were generated from `.planning/v1.35/MIGRATION-TABLE.md` rather than authored six times, so the wiki text and the provenance record cannot disagree."
    - "An independent fresh clone taken after the push shows all four `tools/wiki` checkers green, including the new provenance checker at `OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.`"
    - "`honest02_truth.py` leg 1 still reports 5 pages matching the claim signature and 0 missing stamp on the published wiki, so the footers did not trip RESEARCH.md Pitfall 2 in production."
    - "`wiki.py links` still reports 11 pages, all reachable, all internal links resolving, all listed in `_Sidebar.md` — the footers added no page, so no navigation edit was owed and none was made."
    - "The push happened only after an on-disk operator approval file existed under the phase evidence directory, and the gate asserts that file rather than trusting the checkpoint alone."
    - "Exactly one commit was added to the wiki repository, touching exactly the six pages."
  artifacts:
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-operator-approval.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-prepush-suite.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-push-transcript.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-postpush-freshclone.txt
  key_links:
    - "`.planning/v1.35/MIGRATION-TABLE.md` -> `provenance_footers.py --emit` -> wiki clone -> commit -> push -> `firestarter_prom.wiki.git`. There is no pull request, no review and no CI gate anywhere on that path; the checker registered in plan 173-06 is the only guard that will ever exist on it."
    - "published wiki -> a fresh independent clone -> all four checkers. Verification is from a clone taken after the push, never from the working clone that produced it."
  prohibitions:
    - statement: "No page is added to or removed from the wiki; the footers add no page, so the two navigation edits to `_Sidebar.md` and `Home.md` are not owed and are not made."
      status: required
      verification: explicit
    - statement: "No footer text is hand-authored on a page; every footer is emitted by the generator from the table."
      status: required
      verification: explicit
    - statement: "No pre-deletion SHA is changed on the wiki or in the table to make a check green."
      status: required
      verification: explicit
    - statement: "The push proceeds only if the on-disk approval file carries its exact literal; an approved-looking checkpoint is not authorization."
      status: required
      verification: explicit
    - statement: "Verification is never taken from the working clone that produced the commit, and never from a green CI tick — always from an independently taken fresh clone."
      status: required
      verification: explicit
    - statement: "`tools/wiki/selftest.sh` is not run; if it is run anyway, Phase 168's evidence files are restored and the restoration is asserted."
      status: required
      verification: explicit
    - statement: "No file this plan writes carries a comment of any kind, and no footer uses the HTML comment form `honest02_truth.py`'s stamp regex matches."
      status: required
      verification: explicit
---

<objective>
Publish the six provenance footers to the live wiki, and prove from an independent clone that the
milestone's central non-claim now sits at the foot of every page it qualifies.

Purpose: D-09 chose per-page footers over an internal-only record because relocation-is-not-
verification is a claim about the migrated pages, and the readers who need it are on those pages.
The internal ledger in plan 173-08 carries the full account; the footer carries the one line the
wiki reader needs.

The wiki is reached by clone, commit and push. There is no pull request, no review, no diff and no
CI gate on the edit — which is exactly why plan 173-01 built a checker and plan 173-06 registers it.

Output: one wiki commit touching six pages, plus a post-push fresh-clone verification of all four
checkers.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-01-SUMMARY.md
@.planning/v1.35/MIGRATION-TABLE.md
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · `.github/workflows/wiki-check.yml` new step
  `Provenance footer check` (plan 173-06) · `.planning/config.json` keys `git.base_branch` and
  `git.protected_branches` (plan 173-02) · `/workspaces/CLAUDE.md` section
  `## Milestone close and branch protection` (plan 173-02) · `.planning/REQUIREMENTS.md` checkboxes
  (plan 173-09).

**New records**
- `.planning/v1.35/CLOSE-RECORD.md` (plan 173-08) · `.planning/notes/v135-close-procedure-under-protection.md`
  (plan 173-02) · `.planning/phases/173-.../173-UPSTREAM-REPLIES.md` (plan 173-04) ·
  **this plan's four `evidence/173-05-*` files.**

**Orchestrator-owned ROADMAP writes, handed off and never written by an executor**
- `### Phase 999.46`, `### Phase 999.47`, and the Phase 172 / Phase 173 checklist boxes

**Out-of-repo**
- **Six provenance footers on `firestarter_prom.wiki.git` (this plan).**
- Four issue comments, gh#7 and gh#6 closed, gh#9 pinned (plan 173-07)

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Operator authorizes the wiki footer push and writes the approval file</name>
  <decision>Publish six generated provenance footers to the live `firestarter_prom` wiki, or decline and keep the milestone's central non-claim internal only.</decision>
  <options>
    <option id="publish">
      <name>Publish the six footers (D-09 as decided)</name>
      <pros>Puts relocation-is-not-verification where the reader actually is, on every page it qualifies. The text is generated from `MIGRATION-TABLE.md`, so it cannot disagree with the provenance record. Plan 173-01 already proved, against a throwaway clone, that the footers leave `wiki.py links` and `honest02_truth.py` green and leave leg 1 at 5 matched.</pros>
      <cons>Costly to undo: reverting means six further wiki edits by clone, commit and push, and the footers will be cited by the checker leg once plan 173-06 registers it. The wiki has no pull request and no review, so the edit is live the moment it is pushed.</cons>
    </option>
    <option id="internal-only">
      <name>Keep the ledger internal only</name>
      <pros>Nothing outward-facing happens; v1.34's precedent and the cheapest option.</pros>
      <cons>The milestone's central non-claim would be legible only to whoever reads `.planning/` — not the readers the wiki was built for. It also strands plan 173-01's checker with nothing to check and plan 173-06's leg with nothing to guard.</cons>
    </option>
  </options>
  <resume-signal>Write the approval file as described below, then type "approved". To decline, write the decline line into the same file and type "declined".</resume-signal>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-operator-approval.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — D-09, its reversibility note (costly), and its rejected alternatives.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-01-suite-regression.txt` — plan 173-01's proof that the six footers leave all four checkers green, taken against a throwaway clone before anything was published.
  </read_first>
  <reversibility rating="costly">Undoing means six further wiki edits by clone, commit and push, and the footers become load-bearing for the checker leg plan 173-06 registers. The wiki has no pull request and no review, so there is no pre-publication diff anyone sees.</reversibility>
  <action>
Show the operator the exact six footer lines that will be published — read them out of plan 173-01's
throwaway-clone evidence, verbatim, rather than describing them — together with the six pages they
land on: `Programming-Protocols`, `Shield-Revisions`, `Install-Beta`, `Testing-Chips`,
`Lockable-PROMs` and `Shell-Completion`.

State the five things that make the scope decidable:

1. **Six, not twelve.** CONTEXT.md says twelve; the measured footer-eligible set is six, per
   RESEARCH.md C-1. `Home` and `Contributing` were authored rather than migrated and have no source
   path or SHA to state; `Breaking-Changes`, `Chip-Database-Fields` and `Pin-Maps` likewise.
2. **Two footer variants.** Four of the six pages were edited after the move — `Install-Beta` and
   `Testing-Chips` rewritten, `Shield-Revisions` and `Lockable-PROMs` trimmed, all on 2026-08-31 —
   so their footers say so instead of claiming the content is unchanged.
3. **No page is added or removed**, so no navigation edit is owed.
4. **It is costly to undo**, and the footers become load-bearing once plan 173-06's leg is live.
5. **The regression is already measured**, not predicted: plan 173-01 ran all four checkers against
   a throwaway clone carrying these exact footers and they were green, with `honest02_truth.py`
   leg 1 unmoved at 5 matched and 0 missing.

On authorization, write `evidence/173-05-operator-approval.txt` containing a line reading exactly
`APPROVED-FOR-WIKI-PUSH: 6 pages`, the operator's name, the date, and one sentence in the operator's
own words. **Tasks 2 and 3 assert this file and that literal before they push.** An auto-approved
gate writes no file, so the push cannot proceed without a human act; `autonomous: false` alone is
known not to be self-protecting here.

On a decline, write `DECLINED-FOR-WIKI-PUSH:` and the reason into the same file, halt this plan, and
record in plan 173-08's ledger that D-09's per-page half was not delivered and that the ledger is
internal only. Plan 173-06 must then not register a leg that would go red on an unfootered wiki.
  </action>
  <acceptance_criteria>
    - `evidence/173-05-operator-approval.txt` exists and carries either the exact line `APPROVED-FOR-WIKI-PUSH: 6 pages` or a line beginning `DECLINED-FOR-WIKI-PUSH:`.
    - The file names the operator and carries a date.
    - The live wiki is unchanged at the moment this task completes.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && mkdir -p "$E" && F=$E/173-05-operator-approval.txt && test -s "$F" && { /usr/bin/grep -qx 'APPROVED-FOR-WIKI-PUSH: 6 pages' "$F" || /usr/bin/grep -q '^DECLINED-FOR-WIKI-PUSH:' "$F"; } && /usr/bin/grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$F" && D=$(mktemp -d) && git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$D/w" && test "$(python3 tools/wiki/provenance_footers.py --table .planning/v1.35/MIGRATION-TABLE.md --wiki-dir "$D/w" 2>&1 | /usr/bin/grep -c '^ERROR: FOOTER MISSING')" -eq 6
</automated>
    <fails_when>non-zero exit; or the approval file is absent or empty, which is the state an auto-approved checkpoint leaves and which must block Tasks 2 and 3; or it carries neither the exact approval line nor a decline line; or it carries no date; or the live wiki does not report exactly six `FOOTER MISSING` failures, meaning either footers are already published (this task ran twice, or someone else edited the wiki) or the footer-eligible set is no longer six and the scope shown to the operator was wrong.</fails_when>
  </verify>
  <done>An on-disk approval or decline exists, written by a human, naming the six-page scope; the live wiki is still unfootered.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the footers into a working clone, run the full suite before pushing, then push one commit</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-prepush-suite.txt, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-push-transcript.txt</files>
  <read_first>
    - `tools/wiki/provenance_footers.py` — the emitter's behaviour, specifically that it strips any existing footer block before appending, so it is idempotent and a re-run is safe.
    - `.planning/v1.35/MIGRATION-TABLE.md` — the corrected table, which is the generator's only input.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-operator-approval.txt` — Task 1's output; this task must not run without its exact literal.
    - `.planning/phases/172-policy-one-tracker-protected-main/172-01-SUMMARY.md` — the wiki clone-commit-push sequence this repository has actually performed, including the pre-push `wiki.py links` gate.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md` — Pitfall 2, the demonstrated RED where a footer's source path flipped `honest02_truth.py` leg 1 to `MISSING STAMP`, and its measured baseline of 5 matched.
  </read_first>
  <precondition>`evidence/173-05-operator-approval.txt` exists and contains the exact line `APPROVED-FOR-WIKI-PUSH: 6 pages`. Halt without pushing if it does not.</precondition>
  <action>
Assert the approval file's exact literal first. If it is absent or carries a decline, stop and push
nothing — the file is the authorization, not the checkpoint.

Clone `https://github.com/henols/firestarter_prom.wiki.git` into a scratch working directory. Run
`provenance_footers.py --emit` against it with the corrected `.planning/v1.35/MIGRATION-TABLE.md`. Six
pages should change and no others; confirm that with a porcelain listing before doing anything else.
The footer text is generated, never typed: hand-authoring even one of them would let the wiki and the
provenance record disagree, which is the whole reason D-09 chose generation.

**Run the full suite before pushing, not after.** Against the working clone, run `wiki.py links`,
`honest02_truth.py` (with the app's `chip_database.json` and `tools/wiki/claim-allowlist.json`),
`dispatch_mirror.py`, and `provenance_footers.py`. Record every exit code and tail line into the
pre-push evidence file. The measurement that decides whether the push happens is
`honest02_truth.py` leg 1's "matched the claim signature" count: RESEARCH.md's live baseline is 5,
and a footer whose text pushes an unstamped page over that signature turns leg 1 to `MISSING STAMP`.
If the count is not 5, **do not push** — that is a footer-wording defect, and it is far cheaper to
fix before publication than after.

Then commit and push. One commit, touching exactly the six pages, with a message naming the phase
and D-09. Capture the full push output into the push transcript, plus the resulting wiki commit SHA,
the list of files the commit touched, and the count of commits ahead of the remote before the push
(which must be exactly one).

No page is added and none is removed, so the two navigation edits to `_Sidebar.md` and `Home.md`
that a page addition would owe are not made. Do not touch either file.

Do not run `tools/wiki/selftest.sh` — it mutates Phase 168's evidence files and nothing here needs
it.
  </action>
  <acceptance_criteria>
    - The pre-push evidence file records all four checkers at exit 0 against the working clone.
    - It records `honest02_truth.py` leg 1 at exactly 5 matched and 0 missing stamp.
    - It records `wiki.py links` at 11 pages, all reachable, all listed in `_Sidebar.md`.
    - It records `provenance_footers.py` at `OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.`
    - The porcelain listing before the commit shows exactly six modified files, all of them the six named pages, and neither `_Sidebar.md` nor `Home.md`.
    - The push transcript records exactly one commit ahead of the remote before the push, the resulting commit SHA, and the six file names.
    - The push exited 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && /usr/bin/grep -qx 'APPROVED-FOR-WIKI-PUSH: 6 pages' "$E/173-05-operator-approval.txt" && PRE=$E/173-05-prepush-suite.txt && PUSH=$E/173-05-push-transcript.txt && test -s "$PRE" && test -s "$PUSH" && test "$(/usr/bin/grep -c '^rc=0$' "$PRE")" -ge 4 && /usr/bin/grep -q 'leg1 stamp-present 5 matched/0 missing' "$PRE" && /usr/bin/grep -q '^OK: 11 pages,' "$PRE" && /usr/bin/grep -qx 'OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.' "$PRE" && /usr/bin/grep -qx 'pages modified before commit: 6' "$PRE" && ! /usr/bin/grep -qE '^ M (_Sidebar|Home)\.md' "$PRE" && /usr/bin/grep -qx 'commits ahead of remote before push: 1' "$PUSH" && /usr/bin/grep -qE '^wiki commit: [0-9a-f]{7,40}$' "$PUSH" && /usr/bin/grep -qx 'push rc=0' "$PUSH" && for p in Programming-Protocols Shield-Revisions Install-Beta Testing-Chips Lockable-PROMs Shell-Completion; do /usr/bin/grep -qF "$p.md" "$PUSH" || { echo "PAGE NOT IN COMMIT: $p"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or a `PAGE NOT IN COMMIT: <page>` line. The absent `leg1 stamp-present 5 matched/0 missing` line is the one that should stop a push: it means a footer's wording pushed an unstamped page over `honest02_truth.py`'s claim signature and the wiki would go red the next time the scheduled check runs. `commits ahead of remote before push: 1` missing means more than one commit was about to land, so the change is not the single reviewable edit the operator approved. A modification to `_Sidebar.md` or `Home.md` means a navigation edit was made that no page addition owed.</fails_when>
    <automated>cd /workspaces && test -z "$(git status --porcelain .planning/phases/168-* 2>/dev/null)" && test -z "$(git status --porcelain tools/wiki/)"
</automated>
    <fails_when>non-zero exit; or any Phase 168 evidence file reports a modification, meaning `tools/wiki/selftest.sh` was run and not cleaned up; or `tools/wiki/` reports a modification, meaning the checker or the table was edited during a publication task instead of in plan 173-01.</fails_when>
  </verify>
  <done>One commit carrying six generated footers is on `firestarter_prom.wiki.git`, pushed only after the approval file's exact literal was asserted, and the full four-checker suite was green against the working clone before the push rather than after it.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify from an independent fresh clone, because the clone that produced the commit is not a witness</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-postpush-freshclone.txt</files>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-01-SUMMARY.md` — the fresh-clone re-verify this repository already performs after a wiki push, and why the working clone does not count.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-push-transcript.txt` — the commit SHA the fresh clone must be at or ahead of.
    - `tools/wiki/provenance_footers.py` — the checker whose green line is the primary assertion here.
  </read_first>
  <action>
Take a **new** shallow clone of the wiki into a different temporary directory, and clone both
sub-repositories at the milestone branch, so all four checkers can run against a tree nothing in
this plan produced. Verification from the working clone would only prove that the emitter agrees
with itself.

Run all four checkers against that fresh clone and record every exit code and tail line:
`wiki.py links`, `honest02_truth.py`, `dispatch_mirror.py`, and `provenance_footers.py`.

Then read the published footers back page by page. For each of the six pages, print its last line
verbatim into the evidence file, so the record carries what a reader actually sees rather than a
checker's summary of it. Confirm the two variants landed correctly: `Programming-Protocols` and
`Shell-Completion` say the content was moved intact and not edited since; `Install-Beta`,
`Testing-Chips`, `Shield-Revisions` and `Lockable-PROMs` name their post-move edit instead.

Record the fresh clone's HEAD SHA next to the SHA the push transcript reported, so the record shows
which published state was verified. Record the wiki's page count and confirm it is still 12 files,
11 content pages plus `_Sidebar.md` — the footers added no page.

Finally, state in the evidence file, in words the ledger can inherit: the footers are now the
milestone's central non-claim in the place the reader is, they are guarded by a checker that has
been seen to fail, and that checker is **not yet running in CI** — plan 173-06 registers the leg,
and until then the guard is manual. That distinction matters and is the same shape as Phase 172's
NON-CLAIM 3, where `Wiki check` was registered and had never run.
  </action>
  <acceptance_criteria>
    - The evidence file records a fresh clone taken after the push, with its HEAD SHA, and that SHA matches or descends from the SHA the push transcript reported.
    - All four checkers exit 0 against that fresh clone.
    - `provenance_footers.py` reports `OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.`
    - `honest02_truth.py` leg 1 reports 5 matched and 0 missing stamp.
    - `wiki.py links` reports 11 pages.
    - The six published footer lines appear verbatim, and the two variants are correctly distributed across the six pages.
    - The wiki carries 12 Markdown files.
    - The evidence file states that the checker is not yet running in CI.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && EV=$E/173-05-postpush-freshclone.txt && V=$(mktemp -d) && git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$V/wiki" && git clone --depth 1 -q -b gsd/v1.35-documentation-consolidation-wiki-migration https://github.com/henols/firestarter_app.git "$V/app" && git clone --depth 1 -q -b gsd/v1.35-documentation-consolidation-wiki-migration https://github.com/henols/firestarter.git "$V/fw" && { echo "fresh clone HEAD: $(git -C "$V/wiki" rev-parse HEAD)"; echo "md files: $(ls -1 "$V/wiki"/*.md | wc -l)"; python3 tools/wiki/wiki.py links --source-dir "$V/wiki" | tail -1; echo "rc=$?"; python3 tools/wiki/honest02_truth.py --wiki-dir "$V/wiki" --db "$V/app/firestarter/data/chip_database.json" --allowlist tools/wiki/claim-allowlist.json | tail -1; echo "rc=$?"; python3 tools/wiki/dispatch_mirror.py --app-dir "$V/app" --fw-dir "$V/fw" | tail -1; echo "rc=$?"; python3 tools/wiki/provenance_footers.py --table .planning/v1.35/MIGRATION-TABLE.md --wiki-dir "$V/wiki"; echo "rc=$?"; for p in Programming-Protocols Shield-Revisions Install-Beta Testing-Chips Lockable-PROMs Shell-Completion; do printf '%s :: ' "$p"; tail -1 "$V/wiki/$p.md"; done; } >> "$EV" 2>&1; test "$(/usr/bin/grep -c '^rc=0$' "$EV")" -ge 4 && /usr/bin/grep -qx 'md files: 12' "$EV" && /usr/bin/grep -q '^OK: 11 pages,' "$EV" && /usr/bin/grep -q 'leg1 stamp-present 5 matched/0 missing' "$EV" && /usr/bin/grep -qx 'OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.' "$EV" && test "$(/usr/bin/grep -c ' :: \*Relocated from ' "$EV")" -eq 6 && test "$(/usr/bin/grep -c 'Moved intact and not edited since; not re-verified against the code' "$EV")" -eq 2 && test "$(/usr/bin/grep -cE ', then (rewritten|trimmed) 2026-08-31 on the wiki; not re-verified against the code' "$EV")" -eq 4 && /usr/bin/grep -qiE 'not yet running in CI|registered.*never run|leg is not yet live' "$EV"</automated>
    <fails_when>non-zero exit; or fewer than four `rc=0` lines, meaning a checker failed against the published wiki; or `md files: 12` is absent, meaning a page was added or removed by the push; or the `provenance_footers.py` OK line is absent; or `leg1 stamp-present 5 matched/0 missing` is absent, meaning the published footers tripped RESEARCH.md Pitfall 2 in production; or the verbatim footer tallies are not 6 total with 2 moved-intact and 4 post-move-edited, meaning a page carries a footer claiming its content is unchanged when the table records an edit; or no line states that the checker is not yet running in CI, leaving a "registered equals guarded" implication the ledger would inherit as false.</fails_when>
  </verify>
  <done>An independently taken fresh clone shows all four checkers green on the published wiki, the six footer lines recorded verbatim with both variants correctly distributed, the page count unchanged at 12, and the limit — the guard is not yet in CI — written down for the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working clone → `firestarter_prom.wiki.git` | The publication boundary. No pull request, no diff review, no CI gate exists anywhere on it; the push is live the moment it lands. |
| operator → the phase evidence directory | The approval file is the authorization; an approved-looking checkpoint is not, because auto-advance modes approve checkpoints. |
| published wiki → an independent fresh clone | Verification must cross this boundary, or it only proves the emitter agrees with itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-04 | Tampering | live wiki pages — an unreviewed edit removing or drifting a footer | high | mitigate | `provenance_footers.py` is registered as a `wiki-check.yml` leg in plan 173-06 and becomes the only standing guard on a surface with no review of any kind; Task 3 records explicitly that until that leg is live the guard is manual. |
| T-173-22 | Elevation of Privilege | an outward-facing push proceeding on an auto-approved checkpoint | high | mitigate | Tasks 2 and 3 assert an on-disk approval file carrying an exact literal before pushing. An auto-approved gate writes no file. |
| T-173-23 | Spoofing | a footer claiming content is unchanged on a page that was rewritten | high | mitigate | Two generated variants keyed on the table's `Post-move edits` column; Task 3 counts the published lines and requires exactly 2 moved-intact and 4 post-move-edited. |
| T-173-24 | Repudiation | verifying from the clone that produced the commit | medium | mitigate | Task 3 takes a new clone in a different directory and records its HEAD SHA next to the pushed SHA. Never a green CI tick, never the working clone. |
| T-173-25 | Denial of Service | a footer flipping `honest02_truth.py` leg 1 to `MISSING STAMP` on the published wiki | medium | mitigate | The full suite runs against the working clone **before** the push, with leg 1's matched count of 5 as a stop condition, and again from the fresh clone after it. RESEARCH.md demonstrated this failure RED. |
| T-173-26 | Tampering | an unowed navigation edit to `_Sidebar.md` or `Home.md` | low | mitigate | The footers add no page, so no navigation edit is owed; the porcelain gate fails if either file is modified. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package. It uses `git`, `python3` and the existing stdlib-only checkers. There is no `[ASSUMED]` or `[SUS]` package to gate. |
</threat_model>

<verification>
- An on-disk operator approval with its exact literal precedes the push, and the gate asserts it.
- The four-checker suite is green against the working clone before the push, with `honest02_truth.py` leg 1 at 5 matched.
- Exactly one commit lands on the wiki, touching exactly the six named pages, and neither navigation file.
- An independently taken fresh clone shows all four checkers green, 12 Markdown files, and the six footer lines verbatim with both variants correctly distributed.
- The evidence records that the checker is not yet running in CI.
- Nothing under `tools/wiki/` or `.planning/phases/168-*` is modified by this plan.
</verification>

<success_criteria>
- D-09's per-page half is delivered: the milestone's central non-claim sits at the foot of every migrated page it qualifies, generated from the provenance table rather than authored.
- No footer states something the table does not license.
- Verification is from a published, independently cloned state, never from a prediction and never from the producing clone.
- Four evidence files are committed under the phase evidence directory.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-05-SUMMARY.md` when done.
</output>
