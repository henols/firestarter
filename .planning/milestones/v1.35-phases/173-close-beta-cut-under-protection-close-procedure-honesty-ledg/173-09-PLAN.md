---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 09
type: execute
wave: 5
depends_on: ["173-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/v1.35/CLOSE-RECORD.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-closing-sweep.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-gitlink-equality.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-operator-approval.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-beta-cut.txt
autonomous: false
requirements: [POLICY-04, POLICY-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A closing sweep file exists that walks every one of the phase's five ROADMAP success criteria and both requirements against named evidence files, and it was written before either requirement checkbox was flipped."
    - "POLICY-04 and POLICY-05 are marked complete in `.planning/REQUIREMENTS.md` only where the sweep names the evidence discharging them; POLICY-04 is marked complete on the probe per D-03 if the cut was not authorized, with the missing half already carried as a ledger non-claim."
    - "Both submodule gitlinks are re-pinned and per-submodule equality is proven: each submodule's HEAD equals the gitlink meta records for it, and both are on the milestone branch."
    - "The `beta` lockstep cut is either performed under an on-disk operator authorization and verified from the published artefacts, or not performed and recorded as not performed with the ledger row already stating it."
    - "If the cut is performed it follows D-04's recipe with v1.30's pull-request posture: pull requests to `beta` in all three repositories, CI cutting the matched pair, both channels verified from a clean virtual environment and the release APIs rather than predicted, and meta tagged."
    - "The manual `publish.yml` dispatch D-04 called mandatory is dropped as redundant, and the change of posture is recorded rather than silently deviated from — `beta-release.yml` now calls `publish.yml` by workflow call."
    - "No stable release is cut; stable remains operator-gated and, per Backlog 999.46, its path is blocked end to end anyway."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-closing-sweep.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-gitlink-equality.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-operator-approval.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-beta-cut.txt
  key_links:
    - "the sweep -> the requirement checkboxes. The sweep is written first; this project's recorded failure mode is executors marking multi-plan requirements complete ahead of their evidence."
    - "submodule HEAD -> meta's recorded gitlink, per submodule. Phase 171 plan 04 and Phase 172 plan 09's pattern; a worktree checkout leaves submodules empty and under-detects, so equality is asserted per submodule rather than globally."
    - "a merged pull request to `beta` -> `beta-release.yml` / `beta-build.yml` -> the prerelease pair -> `publish.yml` by workflow call -> PyPI. The cut is fired by CI on the push to `beta`; no local step produces it."
  prohibitions:
    - statement: "No requirement checkbox is flipped before the closing sweep file exists and names the evidence for it."
      status: required
      verification: explicit
    - statement: "No stable release is cut, and no stable release workflow is triggered."
      status: required
      verification: explicit
    - statement: "Nothing is pushed to `beta` in any repository unless the on-disk operator approval file carries its exact literal; a push to `beta` fires the pre-release cut and cannot be rehearsed."
      status: required
      verification: explicit
    - statement: "No release tag or version is predicted, assumed or hardcoded; every observed tag is read from the release API and every channel is verified from a clean virtual environment, never from the editable install and never from a green CI tick."
      status: required
      verification: explicit
    - statement: "`git cherry` is used for every ancestry question, never `--is-ancestor`, because the v1.30 squash-merge made ancestry lie; and local `beta` is recreated from `origin/beta` first because it is measurably stale."
      status: required
      verification: explicit
    - statement: "No verify leg asserts an empty `git status --porcelain` on `firestarter_app`; it carries one unstaged product-source modification that Backlog 999.45 owns and that this milestone may not touch, revert or commit."
      status: required
      verification: explicit
    - statement: "No executor writes `.planning/ROADMAP.md`; the Phase 173 checklist box is the orchestrator's write."
      status: required
      verification: explicit
    - statement: "No file this plan writes carries a comment of any kind."
      status: required
      verification: explicit
---

<objective>
Close the phase the way this project closes phases: write the evidence sweep first, mark the
requirements only against it, re-pin the gitlinks and prove equality per submodule, and put the one
remaining outward-facing step — the `beta` lockstep cut — behind the operator.

Purpose: this project's recorded failure mode is executors marking multi-plan requirements complete
ahead of their evidence, which is why plan 172-09 discharged its sweep before any checkbox moved.
And D-01 splits the probe from the cut precisely so the milestone can close honestly whether or not
the cut is authorized: D-03 permits POLICY-04 complete on the probe with the missing half stated.

Output: a criterion-by-criterion sweep, two requirement marks, proven gitlink equality, and either a
performed and verified cut or a recorded non-performance.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/v1.35/CLOSE-RECORD.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/172-09-SUMMARY.md
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · `.github/workflows/wiki-check.yml` new step
  (plan 173-06) · `.planning/config.json` keys `git.base_branch` and `git.protected_branches`
  (plan 173-02) · `/workspaces/CLAUDE.md` section `## Milestone close and branch protection`
  (plan 173-02) · **`.planning/REQUIREMENTS.md` — POLICY-04 and POLICY-05 checkboxes (this plan).**

**New records**
- `.planning/v1.35/CLOSE-RECORD.md` (plan 173-08, §2 completed by this plan) ·
  `.planning/notes/v135-close-procedure-under-protection.md` (plan 173-02) ·
  `.planning/phases/173-.../173-UPSTREAM-REPLIES.md` (plan 173-04) ·
  **this plan's four `evidence/173-09-*` files.**

**Orchestrator-owned ROADMAP writes, handed off and never written by an executor**
- `### Phase 999.46`, `### Phase 999.47` (plan 173-08) · **the Phase 173 checklist box (this plan's handoff).**

**Out-of-repo**
- Six provenance footers on `firestarter_prom.wiki.git` (plan 173-05) · a fourth pull request into a
  protected `main` (plan 173-06) · four issue comments, gh#7 and gh#6 closed, gh#9 pinned (plan 173-07)
- **Conditionally: three pull requests to `beta`, a prerelease pair, a PyPI upload and a meta `v1.35` tag (this plan).**

<tasks>

<task type="auto">
  <name>Task 1: Write the closing sweep against every criterion first, then mark the two requirements against it and nothing else</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-closing-sweep.txt, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt` — the shape: a walk over every criterion with the evidence file named beside it, four explicit non-claims and three carried findings, written before any checkbox moved.
    - `.planning/ROADMAP.md` — the Phase 173 entry with all five success criteria verbatim; the sweep addresses them one at a time, in order, quoting each.
    - `.planning/REQUIREMENTS.md:68-69` — POLICY-04 and POLICY-05's texts, and `:118-120`, the constraint block whose middle sentence RESEARCH.md C-3 disproves.
    - `.planning/v1.35/CLOSE-RECORD.md` — plan 173-08's ledger; the sweep cites it rather than restating it.
    - Every `evidence/173-0*` file from plans 173-01 through 173-08 — the sweep names the file discharging each criterion, and a criterion with no file is recorded as not discharged rather than argued.
  </read_first>
  <action>
Write `evidence/173-09-closing-sweep.txt` **before** touching `.planning/REQUIREMENTS.md`. That order
is the point: this project has a recorded failure mode of executors marking multi-plan requirements
complete ahead of their evidence, and plan 172-09's discharge is the pattern that catches it.

Walk the five ROADMAP success criteria in order. Quote each verbatim, then state DISCHARGED,
PARTIALLY DISCHARGED or NOT DISCHARGED, then name the evidence files. No criterion is discharged by
argument; each is discharged by a named file or it is not discharged.

- **Criterion 1** — the probe transcripts and verdict record from plan 173-03, and the cut's
  disposition from Task 3 of this plan. If the cut was not authorized, say so here and say that
  criterion 1's own wording permits an equivalent dry run.
- **Criterion 2** — the base-branch read-back, the consumer-site evidence, the note and the
  `CLAUDE.md` pointer from plan 173-02, plus the four pull requests into a protected `main`. State
  the half that cannot be satisfied: no documented admin bypass exists, because
  `current_user_can_bypass` is `never`.
- **Criterion 3** — `.planning/v1.35/CLOSE-RECORD.md`, naming its ledger row count and confirming its
  three named minimums are present.
- **Criterion 4** — the two backlog headings now in ROADMAP.md, the retired todo, the findings table,
  and the rename-sweep target list.
- **Criterion 5** — the post transcript, the after-state capture, the pin read-back, and the flipped
  review record; or, if the posting was deferred, the deferral and its stated reason, which criterion
  5's own wording permits.

Then a short block of **explicit non-claims** handed forward to the milestone close, in Phase 172's
register — the same courtesy this phase received. At minimum: the provenance checker's CI leg has not
been observed green in a real run; the six footers cover six of eleven pages and the exclusions are
named; GSD now treats `beta` as protected while GitHub does not; and, if the cut was not performed,
that no beta lockstep cut was performed under the rulesets.

Then mark `.planning/REQUIREMENTS.md`. Flip **only** POLICY-04 and POLICY-05, by hand edit with a
scoped replacement — never through a GSD requirements verb, which reformats the whole file. Mark
POLICY-05 complete against criterion 2's evidence. Mark POLICY-04 complete against the probe per
D-03, whose ledger row already states that no cut is claimed; if the cut *was* performed, the row and
the mark rest on both halves. Touch no other checkbox.

Finally, re-run the four `tools/wiki` checkers against one fresh wiki clone as the phase-gate sample
and record the result in the sweep, so the phase does not close on a checker that was last seen green
several plans ago. Budget generously for any record gate that reads `STATE.md`: it carries a roughly
52 000-character single line, the gate goes superlinear on it, and a timeout returns 124, which reads
like a genuine failure and is not one.
  </action>
  <acceptance_criteria>
    - `evidence/173-09-closing-sweep.txt` exists, quotes all five criteria, and gives each a DISCHARGED, PARTIALLY DISCHARGED or NOT DISCHARGED verdict with at least one named evidence file.
    - It carries an explicit non-claims block naming at least the four listed above.
    - It records all four `tools/wiki` checkers green against one fresh clone.
    - `.planning/REQUIREMENTS.md` has POLICY-04 and POLICY-05 checked.
    - No other requirement checkbox changed state relative to HEAD.
    - The sweep file's git history shows it committed no later than the requirements edit.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && S=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-closing-sweep.txt && test -s "$S" && test "$(/usr/bin/grep -cE '^(CRITERION|Criterion) [1-5]' "$S")" -eq 5 && test "$(/usr/bin/grep -cE 'DISCHARGED|NOT DISCHARGED' "$S")" -ge 5 && test "$(/usr/bin/grep -c 'NON-CLAIM' "$S")" -ge 4 && test "$(/usr/bin/grep -c '^OK: ' "$S")" -ge 4 && /usr/bin/grep -qF 'current_user_can_bypass' "$S" && /usr/bin/grep -qE '^- \[x\] \*\*POLICY-04\*\*' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^- \[x\] \*\*POLICY-05\*\*' .planning/REQUIREMENTS.md && python3 -c "
import re,subprocess
def marks(text):
    return {m.group(2): m.group(1) for m in re.finditer(r'^- \[( |x)\] \*\*([A-Z]+-[0-9]+)\*\*', text, re.M)}
old=marks(subprocess.run(['git','show','HEAD:.planning/REQUIREMENTS.md'],capture_output=True,text=True).stdout)
new=marks(open('.planning/REQUIREMENTS.md').read())
changed=sorted(k for k in new if old.get(k)!=new[k])
print('checkboxes changed:', changed)
assert changed==['POLICY-04','POLICY-05'], changed
print('only the two owed requirements moved')"
</automated>
    <fails_when>non-zero exit; or the sweep does not address all five criteria with verdicts, or carries fewer than four explicit non-claims, or does not record four green checkers; or the printed `checkboxes changed:` list is anything other than exactly POLICY-04 and POLICY-05 — an executor marking a requirement it did not discharge is this project's recorded failure mode, and the list names precisely which one moved.</fails_when>
    <automated>cd /workspaces && S=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-closing-sweep.txt && SC=$(git log --format=%ct -1 -- "$S") && RC=$(git log --format=%ct -1 -- .planning/REQUIREMENTS.md) && test -n "$SC" && test -n "$RC" && test "$SC" -le "$RC"
</automated>
    <fails_when>non-zero exit, or either commit timestamp is empty (a file was never committed), or the sweep's last-commit timestamp is later than the requirements file's — the checkbox was flipped before the evidence sweep existed, which is the ordering this task exists to enforce.</fails_when>
  </verify>
  <done>A criterion-by-criterion sweep exists naming the evidence for each of the five criteria and carrying at least four explicit non-claims, all four wiki checkers were re-run green at the phase gate, and exactly two requirement checkboxes moved — after the sweep was committed, not before.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin both gitlinks and prove equality per submodule, without asserting a porcelain the tree cannot give</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-gitlink-equality.txt</files>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-09-SUMMARY.md` — the per-submodule gitlink equality pattern, why a global assertion under-detects (a worktree checkout leaves submodules empty), and the four pre-existing worktrees that must survive any cleanup.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — the mechanical constraints: sub-repo changes land inside the submodule on the milestone branch; `firestarter_app` is not porcelain-clean and carries one unstaged product-source modification Backlog 999.45 owns.
  </read_first>
  <action>
Re-pin both submodule gitlinks in meta and prove equality one submodule at a time.

For each of `firestarter` and `firestarter_app`, record: the submodule's current branch, its HEAD
SHA, and the SHA meta's tree records for that path. Assert the branch is
`gsd/v1.35-documentation-consolidation-wiki-migration` and that the two SHAs are equal. Do this per
submodule rather than with one global check: a worktree checkout leaves submodules empty and a global
assertion then under-detects, which is why plan 172-09 asserted per submodule.

**Do not assert an empty `git status --porcelain` on `firestarter_app`.** It carries one unstaged
modification to a product-source file that Backlog 999.45 owns and this milestone may not touch,
revert or commit. Two prior plans have already recorded this as a non-fix. Instead, assert the
narrower and true thing: that the only entry in that submodule's porcelain output is that one
product-source path, and that nothing this phase produced is sitting uncommitted in either
submodule.

Also record, for the milestone tail: recreate local `beta` in meta from `origin/beta` — it is
measurably stale, roughly 205 commits behind — and then use `git cherry` to count how far the
milestone branch is ahead. Never `--is-ancestor`: the v1.30 squash-merge made ancestry lie, which is
a standing constraint in this repository. Record that meta's `origin/beta` tip is a merge commit with
two parents, so the close tail must be merged rather than fast-forwarded, and confirm that property
by reading the parent count rather than assuming it.

None of this pushes anything. This task establishes the state the cut in Task 3 would act on, whether
or not the cut is authorized.
  </action>
  <acceptance_criteria>
    - The evidence file records, per submodule, the branch, the HEAD SHA and the recorded gitlink SHA, and the two SHAs are equal for both.
    - Both submodules are on `gsd/v1.35-documentation-consolidation-wiki-migration`.
    - `firestarter`'s porcelain output is empty; `firestarter_app`'s contains only the one product-source path Backlog 999.45 owns.
    - The evidence records the `git cherry` count of the milestone branch ahead of a freshly-fetched `origin/beta`, and states that `git cherry` was used rather than an ancestry test.
    - The evidence records that `origin/beta`'s tip has two parents, read rather than assumed.
    - No push occurred.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-gitlink-equality.txt && git fetch -q origin beta && { for s in firestarter firestarter_app; do echo "== $s"; echo "branch=$(git -C $s branch --show-current)"; echo "head=$(git -C $s rev-parse HEAD)"; echo "gitlink=$(git ls-tree HEAD $s | awk '{print $3}')"; echo "porcelain:"; git -C $s status --porcelain; done; echo "== meta tail"; echo "cherry ahead=$(git cherry origin/beta HEAD | /usr/bin/grep -c '^+')"; echo "origin/beta parents=$(git rev-list --parents -n1 origin/beta | wc -w)"; echo "method: git cherry, never --is-ancestor, because the v1.30 squash-merge made ancestry lie"; } > "$EV" 2>&1; for s in firestarter firestarter_app; do test "$(git -C $s branch --show-current)" = 'gsd/v1.35-documentation-consolidation-wiki-migration' || { echo "WRONG BRANCH: $s"; exit 1; }; test "$(git -C $s rev-parse HEAD)" = "$(git ls-tree HEAD $s | awk '{print $3}')" || { echo "GITLINK MISMATCH: $s"; exit 1; }; done && test -z "$(git -C firestarter status --porcelain)" && test "$(git -C firestarter_app status --porcelain | wc -l)" -eq 1 && git -C firestarter_app status --porcelain | /usr/bin/grep -q 'tools/build_db.py' && /usr/bin/grep -qE '^cherry ahead=[0-9]+$' "$EV" && test "$(/usr/bin/grep -oE '^origin/beta parents=[0-9]+$' "$EV" | /usr/bin/grep -oE '[0-9]+')" -eq 3 && /usr/bin/grep -qF 'git cherry' "$EV"</automated>
    <fails_when>non-zero exit, or a `WRONG BRANCH: <sub>` or `GITLINK MISMATCH: <sub>` line naming which submodule is unpinned or on the wrong branch; or `firestarter`'s porcelain is non-empty; or `firestarter_app`'s porcelain has anything other than exactly the one Backlog 999.45 product-source entry, which would mean this phase left work uncommitted inside a submodule or that the known non-fix was reverted or committed; or `origin/beta` no longer reports three tokens from a parents listing (one commit plus two parents), which would mean the tip is no longer a merge commit and the close tail's merge-not-fast-forward assumption needs re-deriving.</fails_when>
  </verify>
  <done>Both gitlinks are re-pinned and proven equal per submodule with both on the milestone branch, `firestarter_app`'s single known product-source modification is recorded as carried rather than asserted away, and the milestone tail's position relative to `origin/beta` is measured with `git cherry` against a freshly fetched ref.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Operator decides the `beta` lockstep cut; it is performed and verified, or recorded as not performed</name>
  <decision>Perform the full `beta` lockstep cut for the Phase 171 to 173 remainder — pull requests to `beta` in all three repositories, the CI-cut prerelease pair, both channels verified, meta tagged `v1.35` — or close the milestone with POLICY-04 complete on the probe and no cut claimed.</decision>
  <options>
    <option id="cut">
      <name>Perform the full lockstep cut (D-04, with the C-4 correction)</name>
      <pros>Gives criterion 1 the actual cut rather than the equivalent dry run, under the rulesets, so the milestone's central protection claim is demonstrated end to end. Follows v1.22's recipe with v1.30's pull-request posture, and the PyPI upload now arrives automatically by workflow call rather than needing the manual dispatch D-04 assumed.</pros>
      <cons>One-way. A published GitHub prerelease and a PyPI upload cannot be unpublished without an outward-facing deletion, and this project has declined that before — the stray prereleases from an earlier milestone are still public by decision. It is a remainder cut: Phases 168 and 169 already reached `beta` about 22 hours before the rulesets existed, so it carries 171 to 173.</cons>
    </option>
    <option id="no-cut">
      <name>Close with no cut, POLICY-04 complete on the probe</name>
      <pros>D-03 permits exactly this, and criterion 1's own wording permits it: "an actual cut **or** an equivalent dry run that exercises the same paths". The ledger row is already written and states plainly that no cut is claimed.</pros>
      <cons>The milestone closes without a lockstep cut having been performed under the rulesets, so the protection claim rests on the probe alone. That limit is stated rather than hidden, but it is a real limit.</cons>
    </option>
  </options>
  <resume-signal>Write the approval file as described below, then type "approved". To close without a cut, write the decline line into the same file and type "no cut".</resume-signal>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-operator-approval.txt, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-beta-cut.txt, .planning/v1.35/CLOSE-RECORD.md</files>
  <read_first>
    - `.planning/MILESTONES.md` — v1.22's recorded recipe verbatim, which is what D-04 means by "the full lockstep": both repositories merged to `beta`, the observed cut tag derived from the release API and never hardcoded, both community channels independently verified from a clean environment, and no stable release. Also the v1.34 close's pull-request posture.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md` — §"The beta lockstep cut recipe", the C-4 correction showing the PyPI upload is now automatic, and the Runtime State Inventory recording that Phases 168 and 169 reached `beta` about 22 hours before the rulesets existed, so this is a remainder cut rather than the whole milestone.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-gitlink-equality.txt` — Task 2's measurement of where the milestone branch stands relative to a freshly fetched `origin/beta`.
    - `.planning/v1.35/CLOSE-RECORD.md` — the ledger whose POLICY-04 row this task completes either way.
  </read_first>
  <reversibility rating="one-way">A published GitHub prerelease and a PyPI upload cannot be unpublished without an outward-facing deletion, which this project has declined before. Three merges to `beta` each fire their own prerelease cut, and a merge to `beta` cannot be rehearsed.</reversibility>
  <action>
Put the decision to the operator with the facts that make it decidable, then act on the file they
write.

State: that this is a **remainder** cut carrying Phases 171 to 173, because Phases 168 and 169
already reached `beta` roughly 22 hours before the rulesets existed and are therefore not criterion-1
evidence; that `beta` carries no ruleset in any of the three repositories and both beta workflows
auto-commit their version bump onto `beta` rather than `main`, so the cut's own CI path is clear of
the protection; that the cut is one-way; and that if it is declined, POLICY-04 still closes complete
on the probe per D-03 with the ledger row already stating that no cut is claimed.

**Record one deliberate change of posture rather than deviating silently.** D-04 calls the
`publish.yml` dispatch mandatory on the grounds that it is manual and that six of thirteen historical
app betas never reached PyPI. That premise is stale: `beta-release.yml` now calls `publish.yml`
directly by workflow call, and PyPI already carries the three most recent betas with no manual
dispatch. So the plan **drops the manual dispatch and keeps the verification discipline**, and says
so in `CLOSE-RECORD.md` §4 as a correction to D-04 rather than a silent deviation. The dispatch
remains harmless if the operator wants it kept — the publish workflow skips existing versions — but
it is not required and is not performed by default.

On authorization, write `evidence/173-09-operator-approval.txt` with a line reading exactly
`APPROVED-FOR-BETA-CUT: firestarter_prom firestarter firestarter_app`, the operator's name, the date,
and one sentence in their own words. **The cut proceeds only if that file carries that literal.** An
auto-approved gate writes no file; `autonomous: false` alone is known not to be self-protecting here.

Then perform the cut and record it in `evidence/173-09-beta-cut.txt`:

1. Recreate local `beta` from `origin/beta` in all three repositories before anything else, and use
   `git cherry` for every ancestry question — the v1.30 squash-merge made ancestry lie.
2. Open a pull request to `beta` in each of the three repositories carrying the remainder. Merge
   them. `beta-build.yml` and `beta-release.yml` fire on the push to `beta`; each auto-commits its
   version bump onto `beta`, which carries no ruleset.
3. **Read the observed tags from the release API. Never predict, never hardcode, never derive a
   version from planning prose.** Record both observed tags.
4. Verify both channels independently: the firmware prerelease and its assets from the release API,
   and the app from the PyPI JSON API plus a `pip install --pre` in a clean virtual environment
   created under a temporary directory. Never the editable install, and never a green CI tick.
5. Meta's `origin/beta` tip is a merge commit, so meta's close tail is merged, not fast-forwarded.
6. Tag meta `v1.35` and push the tag. A tag push is ungoverned by the rulesets, which are all scoped
   to branches.
7. **No stable release.** Stable remains operator-gated and, per Backlog 999.46, its path is blocked
   end to end anyway.

On a decline, write `DECLINED-FOR-BETA-CUT:` and the reason into the same file, perform no cut, and
confirm that `CLOSE-RECORD.md` §2 and the POLICY-04 ledger row already say plainly that no beta
lockstep cut was performed under the rulesets. Either way, update §2 of the close record with the
outcome and hand the Phase 173 checklist box to the orchestrator; an executor does not write
`ROADMAP.md`.
  </action>
  <acceptance_criteria>
    - `evidence/173-09-operator-approval.txt` exists carrying either the exact line `APPROVED-FOR-BETA-CUT: firestarter_prom firestarter firestarter_app` or a line beginning `DECLINED-FOR-BETA-CUT:`, with a name and a date.
    - On the declined path: no new prerelease exists in either sub-repository beyond those recorded before this phase, no meta `v1.35` tag exists, and `CLOSE-RECORD.md` states that no cut was performed.
    - On the approved path: `evidence/173-09-beta-cut.txt` records three merged pull requests targeting `beta`, both observed tags read from the release API, a clean-virtual-environment resolution of the app beta, a PyPI JSON confirmation, and the meta `v1.35` tag.
    - On the approved path, the recorded tags appear nowhere in the plan or in planning prose as predictions — they are read, and the evidence shows the command that read them.
    - `CLOSE-RECORD.md` §4 records the dropped manual dispatch as a stated correction to D-04.
    - No stable release exists in either sub-repository beyond what existed before this phase.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && F=$E/173-09-operator-approval.txt && test -s "$F" && /usr/bin/grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$F" && C=.planning/v1.35/CLOSE-RECORD.md && if /usr/bin/grep -q '^DECLINED-FOR-BETA-CUT:' "$F"; then /usr/bin/grep -qiE 'no beta lockstep cut was performed|no cut was performed' "$C" && test "$(git tag -l v1.35 | wc -l)" -eq 0 && test "$(git ls-remote --tags origin 'refs/tags/v1.35' | wc -l)" -eq 0; else /usr/bin/grep -qx 'APPROVED-FOR-BETA-CUT: firestarter_prom firestarter firestarter_app' "$F" && B=$E/173-09-beta-cut.txt && test -s "$B" && test "$(/usr/bin/grep -cE '^pull request: (firestarter_prom|firestarter|firestarter_app)#[0-9]+ -> beta \(MERGED\)$' "$B")" -eq 3 && /usr/bin/grep -qE '^observed firmware tag: [0-9]' "$B" && /usr/bin/grep -qE '^observed app tag: [0-9]' "$B" && /usr/bin/grep -qF 'gh release list' "$B" && /usr/bin/grep -qF 'pypi.org/pypi/firestarter/json' "$B" && /usr/bin/grep -qF 'pip install --pre' "$B" && /usr/bin/grep -qE '^clean venv resolved: [0-9]' "$B" && test "$(git ls-remote --tags origin 'refs/tags/v1.35' | wc -l)" -eq 1 && APP=$(/usr/bin/grep -oE '^observed app tag: .*' "$B" | awk '{print $4}') && VENV=$(/usr/bin/grep -oE '^clean venv resolved: .*' "$B" | awk '{print $4}') && test "$APP" = "$VENV" && curl -s https://pypi.org/pypi/firestarter/json | python3 -c "import json,sys; d=json.load(sys.stdin); v='$APP'; assert v in d['releases'], ('APP TAG NOT ON PYPI', v); print('both channels agree on', v)"; fi</automated>
    <fails_when>non-zero exit. On the declined path: a `v1.35` tag exists locally or on the remote, meaning a cut happened without authorization, or the close record does not state that no cut was performed. On the approved path: fewer than three merged pull requests targeting `beta`; a missing observed-tag line, which means a version was predicted rather than read; no evidence that the tags came from the release API and the PyPI JSON API; the clean-virtual-environment resolution disagreeing with the observed app tag, which is the channel drift the v1.21 close recorded; an `APP TAG NOT ON PYPI` tuple, meaning the GitHub prerelease exists and PyPI does not carry it; or no `v1.35` tag on the meta remote.</fails_when>
    <automated>cd /workspaces && B=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-09-beta-cut.txt && { echo "== stable release sets read now"; for r in firestarter firestarter_app; do echo "$r stable: $(gh release list --repo henols/$r --limit 100 --json isPrerelease,tagName --jq '[.[]|select(.isPrerelease==false)|.tagName]|sort|join(",")')"; done; } > /tmp/173-stable-now.txt 2>&1 && test -s "$B" && /usr/bin/grep -qE '^firestarter stable at phase start: ' "$B" && /usr/bin/grep -qE '^firestarter_app stable at phase start: ' "$B" && test "$(/usr/bin/grep -oE '^firestarter stable at phase start: .*' "$B" | cut -d' ' -f7-)" = "$(/usr/bin/grep -oE '^firestarter stable: .*' /tmp/173-stable-now.txt | cut -d' ' -f3-)" && test "$(/usr/bin/grep -oE '^firestarter_app stable at phase start: .*' "$B" | cut -d' ' -f7-)" = "$(/usr/bin/grep -oE '^firestarter_app stable: .*' /tmp/173-stable-now.txt | cut -d' ' -f3-)"</automated>
    <fails_when>non-zero exit; or the beta-cut evidence does not record a `stable at phase start:` line for each sub-repository, so there is no baseline to compare against and the no-stable-release claim would be unfalsifiable; or either recorded set differs from the set read now, meaning a stable release was published during this phase — which is operator-gated, was not authorized, and per Backlog 999.46 should not have been mechanically possible.</fails_when>
  </verify>
  <done>Either an authorized full lockstep cut has been performed with both channels verified from the release and package APIs and a clean virtual environment, the meta `v1.35` tag pushed and both stable release sets unchanged; or no cut was performed, no `v1.35` tag exists, and the close record states plainly that no beta lockstep cut was performed under the rulesets. Either way `CLOSE-RECORD.md` §2 records the outcome and §4 records the dropped manual PyPI dispatch as a stated correction to D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the evidence sweep → the requirement checkboxes | A checkbox flipped ahead of its evidence is this project's recorded failure mode; the sweep must exist and be committed first. |
| operator → the phase evidence directory | The approval file is the authorization for a cut that cannot be rehearsed or withdrawn. |
| a merge to `beta` → the prerelease pair and PyPI | CI fires the cut; no local step produces it and no local step can undo it. |
| submodule HEAD → meta's recorded gitlink | A stale gitlink makes meta cite a tree that is not the one the phase produced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-43 | Repudiation | a requirement marked complete ahead of its evidence | high | mitigate | The sweep is written and committed first; the gate compares the two files' commit timestamps and asserts that exactly POLICY-04 and POLICY-05 changed state relative to HEAD, naming any other checkbox that moved. |
| T-173-44 | Elevation of Privilege | a cut proceeding on an auto-approved checkpoint | high | mitigate | The cut runs only if the on-disk approval file carries its exact literal; the declined branch of the gate asserts that no `v1.35` tag exists locally or on the remote, so a decline cannot be quietly overridden. |
| T-173-45 | Spoofing | a predicted or hardcoded release version standing in for an observed one | high | mitigate | Both tags must be read from the release API with the reading command shown in the evidence; the clean-virtual-environment resolution must equal the observed app tag, and PyPI's JSON must carry it — the channel-drift check the v1.21 close recorded. |
| T-173-46 | Tampering | an unauthorized stable release | high | mitigate | The evidence records both sub-repositories' stable release sets at phase start and the gate compares them to the sets read at the end; stable remains operator-gated and, per 999.46, its path is blocked. |
| T-173-47 | Tampering | a stale or empty gitlink making meta cite the wrong tree | medium | mitigate | Per-submodule equality of HEAD against the recorded gitlink plus a branch assertion, rather than one global check that a worktree checkout would let pass vacuously. |
| T-173-48 | Repudiation | asserting a porcelain the tree cannot give, or reverting a known non-fix to get one | medium | mitigate | `firestarter_app`'s single Backlog 999.45 product-source modification is asserted as the *only* entry rather than asserted away, and the milestone may not touch, revert or commit it. |
| T-173-49 | Spoofing | an ancestry test that lies | medium | mitigate | `git cherry` against a freshly fetched `origin/beta`, never an ancestry test, because the v1.30 squash-merge made ancestry unreliable in these repositories; and local `beta` is recreated before the comparison because it is measurably stale. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package into the project. The only package operation is a `pip install --pre` of this project's own published beta inside a throwaway virtual environment, which is the verification itself. There is no `[ASSUMED]` or `[SUS]` third-party package to gate. |
</threat_model>

<verification>
- The closing sweep exists, addresses all five criteria with verdicts and named evidence, carries at least four explicit non-claims, and records all four wiki checkers green at the phase gate.
- Exactly two requirement checkboxes moved, and the sweep was committed no later than that edit.
- Both gitlinks are equal per submodule and both submodules are on the milestone branch; `firestarter_app`'s single known modification is recorded rather than asserted away.
- The milestone tail's position is measured with `git cherry` against a freshly fetched `origin/beta`, whose tip is confirmed a merge commit by reading its parents.
- The cut is either performed with both channels verified from the release and package APIs and a clean virtual environment and the meta tag pushed, or provably not performed with no `v1.35` tag anywhere.
- Both sub-repositories' stable release sets are unchanged from the recorded phase-start baseline.
</verification>

<success_criteria>
- The phase closes with every criterion's disposition written down before any mark was made.
- POLICY-04 and POLICY-05 are complete against named evidence, POLICY-04 on the probe per D-03 if the cut was not authorized, with the missing half already a ledger row.
- The one deliberate deviation from D-04 — dropping the now-redundant manual PyPI dispatch — is recorded as a correction rather than taken silently.
- Both gitlinks are re-pinned and proven, and no stable release was cut.
- Four evidence files are committed under the phase evidence directory.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-09-SUMMARY.md` when done.
</output>
