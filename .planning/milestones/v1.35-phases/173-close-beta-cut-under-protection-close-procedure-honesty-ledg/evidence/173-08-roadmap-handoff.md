# Plan 173-08 — ROADMAP.md handoff to the orchestrator

**These are the orchestrator's writes, not this plan's.** `roadmap.update-plan-progress`
overwrites `.planning/ROADMAP.md`'s progress row positionally and has previously clobbered an
unrelated phase's dependency table. v1.35's ROADMAP was hand-authored and must never be
regenerated by a generating verb. This plan authors the content below as complete, insertion-
ready Markdown; it does not call any `roadmap.*` gsd-tools verb and does not write
`.planning/ROADMAP.md` itself.

## What to insert, and where

1. **`evidence/173-08-backlog-999.46.md`**, verbatim, immediately after `### Phase 999.45`'s
   closing horizontal rule (`.planning/ROADMAP.md`, currently ending at line 5146) and before the
   `### v1.14 — Feasible-Gap Implementation (✅ PROMOTED …)` divider that follows it.
2. **`evidence/173-08-backlog-999.47.md`**, verbatim, immediately after 999.46's own closing
   horizontal rule (the file above already ends in `---`).
3. **Two checklist corrections** in the v1.35 phase list (`.planning/ROADMAP.md`, the checklist
   block starting around line 238):
   - **Phase 172's box**, currently `- [ ] **Phase 172: POLICY — One Tracker, Protected `main`** …`,
     flips to `- [x] **Phase 172: …`. All four of its requirements (POLICY-01, POLICY-02,
     POLICY-03, LEGACY-01) are marked complete in `REQUIREMENTS.md`, cited in
     `evidence/172-09-closing-sweep.txt` — this is the discrepancy `.planning/v1.35/CLOSE-RECORD.md`
     §1 names.
   - **Phase 173's own box**, `- [ ] **Phase 173 (close): CLOSE — …`, flips to `- [x]` once this
     phase's plans (including this one and 173-09) are complete — this plan does not itself
     determine that the phase is finished; the orchestrator applies this once the phase as a whole
     closes, not as part of this task.

## Verification after insertion

- `/usr/bin/grep -c '^### Phase 999\.46' .planning/ROADMAP.md` → `1`
- `/usr/bin/grep -c '^### Phase 999\.47' .planning/ROADMAP.md` → `1`
- Every pre-existing `### Phase 999.40` through `### Phase 999.45` heading still appears exactly
  once — a positional insertion must not clobber a neighbouring row, the exact failure mode
  `roadmap.update-plan-progress` has already produced in this repository.
- `Phase 172`'s checklist line matches `^- \[x\] \*\*Phase 172:`.
- `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` is deleted
  only after `### Phase 999.46` exists in the file — the todo is retired by the row existing, not
  by intent to create one.

## Source content

- `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` — 999.46's
  source; folded as D-05 in `173-CONTEXT.md`. Delete this file only after step 1 above lands.
- Phase 172's `evidence/172-09-closing-sweep.txt` FINDING B — 999.47's source.
- `.planning/v1.35/CLOSE-RECORD.md` §1 — the Phase 172 checklist discrepancy this handoff
  corrects.
