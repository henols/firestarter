---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 06
type: execute
wave: 3
depends_on: ["173-01", "173-05"]
files_modified:
  - .github/workflows/wiki-check.yml
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-leg-local-run.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-operator-approval.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-main-pr.txt
autonomous: false
requirements: [POLICY-05]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.github/workflows/wiki-check.yml` carries a fourth `run:` leg invoking `tools/wiki/provenance_footers.py` against the wiki clone and the migration table, in the same three-line shape as the three existing python legs — D-10's carrier."
    - "The new leg block carries no comment of any kind, even though the surrounding file is dense with comment blocks written by earlier phases."
    - "The leg's exact command, with the runner's path layout substituted, is run locally against a fresh wiki clone and exits 0, so its correctness is observed rather than assumed from a workflow that has never run."
    - "The workflow's `permissions: contents: read` is unchanged and the leg adds no `uses:` action, so the job's privilege is not widened."
    - "The leg invokes the checker in check mode only; it never passes the emit flag, so CI cannot rewrite the wiki."
    - "The leg reaches prom's default branch by pull request, which is a fourth live exercise of the flow POLICY-05 documents — recorded as POLICY-05 evidence rather than as overhead, alongside the three merged on 2026-09-02."
    - statement: "The leg runs green in a real scheduled CI run."
      verification: backstop
  artifacts:
    - .github/workflows/wiki-check.yml
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-leg-local-run.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-operator-approval.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-main-pr.txt
  key_links:
    - "`wiki-check.yml` job checkouts (`meta`, `firestarter`, `firestarter_app`, `wiki-clone`) -> the new leg's argument paths. Meta files are `meta/tools/wiki/...`; the wiki clone is bare `wiki-clone`. A path error here is invisible until the weekly cron fires."
    - "the leg -> a pull request into a protected `main` -> merge. The only route; `current_user_can_bypass` is `never` on prom's ruleset."
  prohibitions:
    - statement: "The new leg block carries no comment of any kind. The surrounding file's dense comment blocks are earlier phases' work and must not be matched — match the operator's rule, not the local style."
      status: required
      verification: explicit
    - statement: "`permissions:` is not widened, and the leg adds no `uses:` action, so no new action version enters the supply chain."
      status: required
      verification: explicit
    - statement: "The leg never passes the checker's emit flag; a CI job must not be able to rewrite the wiki."
      status: required
      verification: explicit
    - statement: "No existing leg, checkout step or trigger in `wiki-check.yml` is modified, reordered or removed."
      status: required
      verification: explicit
    - statement: "The pull request into `main` is opened and merged only after the on-disk operator approval file carries its exact literal."
      status: required
      verification: explicit
    - statement: "The leg is not registered on `main` before the footers are published, because it would then go red on the next scheduled run against a wiki that has no footers."
      status: required
      verification: explicit
---

<objective>
Wire the checker plan 173-01 built into the workflow that already clones everything it needs, and get
it onto the branch where the scheduled run will actually see it.

Purpose: D-10 asks for a mechanical guard, not a static string. The wiki has no pull request, no
review and no CI gate on an edit, so a footer removed or drifted by a careless edit would be noticed
by nothing. `wiki-check.yml` is registered with Actions as of Phase 172 and already checks out meta,
both sub-repositories and a fresh wiki clone — this is a leg, not new infrastructure.

Output: a fourth, comment-free `run:` leg; a local run of that leg's exact command proving it green;
and a pull request into a protected `main`, which is itself the fourth live exercise of the flow
POLICY-05 documents.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-PATTERNS.md
@.github/workflows/wiki-check.yml
@tools/wiki/provenance_footers.py
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · **`.github/workflows/wiki-check.yml` — new step named
  `Provenance footer check` (this plan)** · `.planning/config.json` keys `git.base_branch` and
  `git.protected_branches` (plan 173-02) · `/workspaces/CLAUDE.md` section
  `## Milestone close and branch protection` (plan 173-02) · `.planning/REQUIREMENTS.md` checkboxes
  (plan 173-09).

**New records**
- `.planning/v1.35/CLOSE-RECORD.md` (plan 173-08) · `.planning/notes/v135-close-procedure-under-protection.md`
  (plan 173-02) · `.planning/phases/173-.../173-UPSTREAM-REPLIES.md` (plan 173-04) ·
  **this plan's three `evidence/173-06-*` files.**

**Orchestrator-owned ROADMAP writes, handed off and never written by an executor**
- `### Phase 999.46`, `### Phase 999.47`, and the Phase 172 / Phase 173 checklist boxes

**Out-of-repo**
- Six provenance footers on `firestarter_prom.wiki.git` (plan 173-05)
- **A fourth pull request into a protected `main`, carrying this leg (this plan).**
- Four issue comments, gh#7 and gh#6 closed, gh#9 pinned (plan 173-07)

<tasks>

<task type="auto">
  <name>Task 1: Add the fourth leg, comment-free, and run its exact command locally before trusting a workflow that has never run</name>
  <files>.github/workflows/wiki-check.yml, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-leg-local-run.txt</files>
  <read_first>
    - `.github/workflows/wiki-check.yml` — the whole file. `:2-5` the schedule and dispatch triggers; `:7-8` `permissions: contents: read`; `:14-50` the 37-line comment block written by an earlier phase; `:51-97` the four checkout and clone steps that establish the path layout (`meta`, `firestarter`, `firestarter_app`, `wiki-clone`); `:99-105` the LEGACY-01 inline-shell leg; `:107-125` the three python legs, which are the exact shape to copy.
    - `tools/wiki/provenance_footers.py` — its argument names and its exit-code contract, so the leg invokes it correctly.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-PATTERNS.md` — the verbatim leg excerpts and the note that meta is checked out at `path: meta`, so meta files are addressed as `meta/tools/wiki/...` while the wiki clone is bare `wiki-clone`.
    - `.planning/phases/172-policy-one-tracker-protected-main/172-03-SUMMARY.md` — how the previous leg was added and gated, including its assertion that the diff introduced zero comment lines.
  </read_first>
  <action>
Append one step to `.github/workflows/wiki-check.yml`, after the dispatch-mirror leg, named
`Provenance footer check`. Three lines in the same shape as the three python legs already there: the
`run: |` block invokes `python3 meta/tools/wiki/provenance_footers.py` with `--wiki-dir wiki-clone`
and `--table meta/.planning/v1.35/MIGRATION-TABLE.md`, followed by an `echo` stating the property in plain
words — that every migrated wiki page still carries the provenance footer its migration-table row
states, and that every live page is accounted for by that table.

**The path layout is load-bearing and is easy to get wrong in a workflow that has never run.** Meta
is checked out at `path: meta`, so meta files are `meta/tools/wiki/...`. The wiki clone is bare
`wiki-clone`. The sub-repositories are bare `firestarter` and `firestarter_app`. No new checkout or
clone step is needed; everything the leg reads is already on disk in that job.

**The new leg carries no comment of any kind.** The surrounding file is dense with comment blocks
written by earlier phases, including a 37-line block above the first checkout. Do not match the
local style; match the operator's standing rule. The leg's own diff must introduce zero comment
lines.

**Do not widen the job.** `permissions: contents: read` stays exactly as it is, and the leg adds no
`uses:` action, so no new third-party action version enters the supply chain. Change no trigger, no
existing leg, and no checkout step; the diff is an addition and nothing else.

**Never pass the emit flag.** The leg runs the checker in check mode only. A CI job that could
rewrite the wiki would turn a guard into a writer.

**Then run the leg's exact command locally, with the runner's layout reproduced.** A workflow that
has never run is not evidence that a leg works: `Wiki check` is registered with Actions and has zero
runs, which is Phase 172's NON-CLAIM 3. Build the same directory shape in a scratch tree — a `meta`
symlink or copy pointing at this repository, a `wiki-clone` from a fresh shallow clone of the wiki,
and both sub-repositories — extract the leg's `run:` body out of the YAML rather than retyping it,
and execute it. Record the extracted body, the command's output and its exit code. Extracting rather
than retyping is the point: a leg that works when retyped by hand and fails as written in the file is
the exact defect this task exists to catch.

Record explicitly in the evidence that this is a local reproduction of the leg, and that the leg has
still never run in CI. Phase 171's criterion 2 established the register for this: a property
observable only after merge is recorded as prevention, not as remediation.
  </action>
  <acceptance_criteria>
    - `wiki-check.yml` contains a step named `Provenance footer check`, placed after the dispatch-mirror leg.
    - Its `run:` body invokes `meta/tools/wiki/provenance_footers.py` with `--wiki-dir wiki-clone` and `--table meta/.planning/v1.35/MIGRATION-TABLE.md`, and contains no emit flag.
    - The diff against HEAD introduces zero added lines that are comments.
    - `permissions:` still reads `contents: read` and the file's `uses:` count is unchanged from HEAD.
    - The file still has exactly the two triggers it had — a schedule and a manual dispatch.
    - The three pre-existing legs and the four checkout steps are unchanged.
    - The extracted `run:` body, executed against a locally reproduced runner layout, exits 0 and prints the checker's OK line.
    - The evidence file states that the leg has not yet run in CI.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && W=.github/workflows/wiki-check.yml && /usr/bin/grep -q 'name: Provenance footer check' "$W" && /usr/bin/grep -qF 'meta/tools/wiki/provenance_footers.py' "$W" && /usr/bin/grep -qF -- '--wiki-dir wiki-clone' "$W" && /usr/bin/grep -qF -- '--table meta/.planning/v1.35/MIGRATION-TABLE.md' "$W" && ! /usr/bin/grep -qF -- '--emit' "$W" && test "$(git diff HEAD -- "$W" | /usr/bin/grep -c '^+[[:space:]]*#')" -eq 0 && test "$(git diff HEAD --numstat -- "$W" | awk '{print $2}')" = '0' && /usr/bin/grep -A1 '^permissions:' "$W" | /usr/bin/grep -qx '  contents: read' && test "$(/usr/bin/grep -c 'uses:' "$W")" -eq "$(git show HEAD:"$W" | /usr/bin/grep -c 'uses:')" && test "$(/usr/bin/grep -c '^\s*- cron:' "$W")" -eq 1 && test "$(/usr/bin/grep -c '^  workflow_dispatch:' "$W")" -eq 1 && test "$(awk '/name: Dispatch-mirror check/{d=NR} /name: Provenance footer check/{p=NR} END{print (d>0 && p>d) ? 1 : 0}' "$W")" -eq 1 && for l in 'LEGACY-01 dead tracker link check' 'WIKI-05 reachability check' 'HONEST-02 truth check' 'Dispatch-mirror check' 'Check out meta repository' 'Clone the live wiki'; do /usr/bin/grep -qF "$l" "$W" || { echo "REMOVED STEP: $l"; exit 1; }; done
</automated>
    <fails_when>non-zero exit, or a `REMOVED STEP: <name>` line naming a pre-existing step the edit deleted; or the added-comment count is not 0, meaning the new leg matched the file's local comment style instead of the operator's rule; or the numstat deleted-lines count is not `0`, meaning the edit removed lines rather than only appending; or the emit flag appears anywhere in the workflow, meaning CI could rewrite the wiki; or the `uses:` count differs from HEAD, meaning a new third-party action entered the supply chain; or `permissions:` no longer reads `contents: read`.</fails_when>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && EV=$E/173-06-leg-local-run.txt && W=.github/workflows/wiki-check.yml && R=$(mktemp) && H=$(mktemp -d) && awk '/- name: Provenance footer check/{f=1} f&&/^ *run: \|$/{r=1;next} r&&/^      - name:/{exit} r{print}' "$W" > "$R" && test -s "$R" && ln -s /workspaces "$H/meta" && git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$H/wiki-clone" && { echo "== extracted leg body"; cat "$R"; echo "== executed from the reproduced runner layout"; ( cd "$H" && bash "$R" ); echo "rc=$?"; echo "== CI run history for this workflow"; gh run list --repo henols/firestarter_prom --workflow 'Wiki check' --limit 5 | cat; } > "$EV" 2>&1; /usr/bin/grep -qx 'rc=0' "$EV" && ! /usr/bin/grep -q '{{' "$R" && /usr/bin/grep -qx 'OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.' "$EV" && /usr/bin/grep -qiE 'has not (yet )?run in CI|never run in CI|not yet observed in CI' "$EV"</automated>
    <fails_when>non-zero exit; or the extracted `run:` body is empty, meaning the awk range did not find the new leg and nothing was actually tested; or `rc=0` is absent, meaning the leg as written in the file fails when executed against the runner's own path layout — the defect this task exists to catch, and one the weekly cron would otherwise surface a week later; or the body contains a workflow expression placeholder, which cannot be executed outside Actions; or the checker's OK line is absent; or no line states that the leg has not yet run in CI, leaving a registered-equals-guarded implication the ledger would inherit as false.</fails_when>
  </verify>
  <done>`wiki-check.yml` carries a fourth, comment-free leg in the established three-line shape; the leg's body was extracted from the file rather than retyped and executed green against a reproduced runner layout; permissions, triggers, action count and every pre-existing step are untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Operator authorizes the pull request into a protected `main` and writes the approval file</name>
  <decision>Open and merge a pull request carrying the new leg into prom's protected `main`, or leave the leg on the milestone branch and record that the guard is not yet live.</decision>
  <options>
    <option id="open-pr">
      <name>Open and merge the fourth pull request into `main`</name>
      <pros>The scheduled `Wiki check` run reads prom's default branch, so the leg only guards anything once it is on `main`. It is also a fourth live exercise of the exact flow POLICY-05 documents, banked as evidence alongside the three merged on 2026-09-02. `permissions` is unchanged and the leg adds no action, so the pull request carries no new privilege.</pros>
      <cons>Outward-facing and effectively one-way: removing the leg later needs another pull request into a protected branch. It also puts the leg live against a wiki whose footers were published only in plan 173-05 — if that plan declined, the leg would go red on the next cron.</cons>
    </option>
    <option id="defer">
      <name>Leave the leg on the milestone branch</name>
      <pros>Nothing outward-facing happens; the leg still ships with the milestone and reaches `main` whenever the milestone does.</pros>
      <cons>The guard is not live, so D-10 is delivered as code rather than as a check. It must then be recorded as a ledger non-claim in exactly the shape of Phase 172's NON-CLAIM 3, where `Wiki check` was registered and had never run.</cons>
    </option>
  </options>
  <resume-signal>Write the approval file as described below, then type "approved". To defer, write the defer line into the same file and type "deferred".</resume-signal>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-operator-approval.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-leg-local-run.txt` — Task 1's local execution of the leg, which is what makes this decision informed rather than speculative.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-05-postpush-freshclone.txt` — proof the footers are published; without them the leg would go red on the next cron.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — D-10's reversibility note (costly: removing the leg later needs another pull request into a protected branch).
  </read_first>
  <reversibility rating="costly">The leg reaches prom's default branch by pull request; removing it later needs another pull request into a protected branch. The merge itself is a permanent public record.</reversibility>
  <action>
Show the operator the exact diff to `wiki-check.yml` — it is a handful of lines — together with
Task 1's local run showing the leg green against a reproduced runner layout, and plan 173-05's
fresh-clone evidence showing the footers are published so the leg will not go red on the next cron.

State the three facts that make the decision decidable: the leg adds no action and does not widen
`permissions`; the pull request is a fourth live exercise of the flow POLICY-05 documents, so it is
evidence rather than pure cost; and removing the leg later needs another pull request into a
protected branch.

**Check the ordering precondition before asking.** If plan 173-05 declined and the wiki carries no
footers, do not open the pull request at all — a leg registered against an unfootered wiki goes red
on the next scheduled run. In that case write the defer line and record it.

On authorization, write `evidence/173-06-operator-approval.txt` containing a line reading exactly
`APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg`, the operator's name, the date, and one
sentence in the operator's own words. **Task 3 asserts this file and that literal before it opens
anything.** An auto-approved gate writes no file.

On a defer, write `DEFERRED-FOR-MAIN-PR:` and the reason into the same file, skip Task 3, and record
in plan 173-08's ledger that the guard exists as code but is not live — the same shape as Phase 172's
NON-CLAIM 3.
  </action>
  <acceptance_criteria>
    - `evidence/173-06-operator-approval.txt` exists and carries either the exact line `APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg` or a line beginning `DEFERRED-FOR-MAIN-PR:`.
    - The file names the operator and carries a date.
    - No pull request has been opened at the time this task completes.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && F=$E/173-06-operator-approval.txt && test -s "$F" && { /usr/bin/grep -qx 'APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg' "$F" || /usr/bin/grep -q '^DEFERRED-FOR-MAIN-PR:' "$F"; } && /usr/bin/grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$F" && if /usr/bin/grep -qx 'APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg' "$F"; then test -s "$E/173-05-postpush-freshclone.txt" && /usr/bin/grep -qx 'OK: 6 footers verified, 11 pages accounted for, 0 unrecorded.' "$E/173-05-postpush-freshclone.txt"; else true; fi</automated>
    <fails_when>non-zero exit; or the approval file is absent or empty, which is the state an auto-approved checkpoint leaves and which must block Task 3; or it carries neither literal; or it carries no date; or it approves the pull request while plan 173-05's post-push evidence does not show the published wiki green at 6 footers — registering the leg then would put a guard live against a wiki it will fail on at the next cron.</fails_when>
  </verify>
  <done>An on-disk approval or deferral exists, written by a human, and no pull request has been opened.</done>
</task>

<task type="auto">
  <name>Task 3: Take the leg into `main` by the only route there is, and record the merge as POLICY-05 evidence</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-main-pr.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-06-operator-approval.txt` — Task 2's output; this task must not run without its exact literal.
    - `.planning/notes/v135-close-procedure-under-protection.md` — plan 173-02's note, which this merge becomes the fourth banked instance of.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md` — the three pull requests already merged into a protected `main` on 2026-09-02 (`prom#54`, `firestarter#58`, `firestarter_app#57`), which this one joins rather than precedes.
    - `.claude/gsd-core/workflows/pr-branch.md` — the mechanism this repository uses to produce a PR branch that excludes `.planning/` commits, so the pull request carries the workflow change and nothing else.
  </read_first>
  <precondition>`evidence/173-06-operator-approval.txt` exists and contains the exact line `APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg`. Skip this task entirely, without opening anything, if it does not.</precondition>
  <action>
Assert the approval file's exact literal first. If it is absent or carries the defer line, skip this
task without opening a pull request; the file is the authorization, not the checkpoint.

Produce a branch off `origin/main` carrying **only** the `wiki-check.yml` change — no `.planning/`
commits, no `tools/wiki/` changes, nothing else. The provenance checker itself must already be on
`main` for the leg to resolve, so check that first: if `tools/wiki/provenance_footers.py` is not on
`origin/main`, the branch carries both files and the pull request says so. Verify what the branch
contains before opening anything, because a pull request into a protected default branch is not a
place to discover an unintended file.

Open the pull request against `main` in `henols/firestarter_prom` with a title naming the phase and
the leg, and a body that states what the leg asserts, that it adds no action and does not widen
`permissions`, that its command was executed locally against a reproduced runner layout with the
output quoted, and that the wiki footers it checks are already published. Then merge it.

**Record the merge as POLICY-05 evidence, not as overhead.** Capture the pull request number, its
merge commit SHA, its merge timestamp, and the fact that it went through the pull-request route
because `current_user_can_bypass` is `never`. Write it into the evidence file next to the three
already-merged instances — `prom#54`, `firestarter#58` and `firestarter_app#57`, all merged
2026-09-02 after the rulesets existed. This is the fourth instance, and the plan should say so
rather than presenting it as the first.

Then read back, from the API rather than from the merge output: that `main` in
`henols/firestarter_prom` now carries the `Provenance footer check` step in
`.github/workflows/wiki-check.yml`. Read GitHub state back from the API, never from a settings page —
the standing rule, and Phase 172 supplies the reason.

Finally, record what is still not true: the leg is now on the default branch but the workflow's next
run is the weekly cron or a manual dispatch, so the leg has still never been observed green in a real
CI run. Whether or not the executor triggers a dispatch, the record must distinguish "on `main`" from
"observed green in CI" — Phase 172's NON-CLAIM 3 exists because those two were once conflated.
  </action>
  <acceptance_criteria>
    - A pull request exists in `henols/firestarter_prom` targeting `main`, and it is merged.
    - Its changed-file set contains `.github/workflows/wiki-check.yml` and no `.planning/` path.
    - `main` in `henols/firestarter_prom`, read from the API, contains the string `Provenance footer check` in `.github/workflows/wiki-check.yml`.
    - The evidence file records the pull request number, the merge commit SHA and the merge timestamp.
    - The evidence file names `prom#54`, `firestarter#58` and `firestarter_app#57` as the three prior instances and this one as the fourth.
    - The evidence file distinguishes the leg being on `main` from the leg having been observed green in CI.
    - `permissions: contents: read` is unchanged on `main`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && F=$E/173-06-operator-approval.txt && EV=$E/173-06-main-pr.txt && if /usr/bin/grep -q '^DEFERRED-FOR-MAIN-PR:' "$F"; then ! gh api repos/henols/firestarter_prom/contents/.github/workflows/wiki-check.yml --ref main --jq .content | base64 -d | /usr/bin/grep -q 'name: Provenance footer chec[k]'; else /usr/bin/grep -qx 'APPROVED-FOR-MAIN-PR: wiki-check.yml provenance footer leg' "$F" && test -s "$EV" && PR=$(/usr/bin/grep -oE '^pull request: #[0-9]+' "$EV" | /usr/bin/grep -oE '[0-9]+') && test -n "$PR" && test "$(gh pr view "$PR" --repo henols/firestarter_prom --json state --jq .state)" = 'MERGED' && test "$(gh pr view "$PR" --repo henols/firestarter_prom --json baseRefName --jq .baseRefName)" = 'main' && gh pr view "$PR" --repo henols/firestarter_prom --json files --jq '.files[].path' | /usr/bin/grep -qx '.github/workflows/wiki-check.yml' && ! gh pr view "$PR" --repo henols/firestarter_prom --json files --jq '.files[].path' | /usr/bin/grep -q '^\.planning/' && gh api repos/henols/firestarter_prom/contents/.github/workflows/wiki-check.yml --ref main --jq .content | base64 -d > /tmp/wc-main.yml && /usr/bin/grep -q 'name: Provenance footer check' /tmp/wc-main.yml && /usr/bin/grep -A1 '^permissions:' /tmp/wc-main.yml | /usr/bin/grep -qx '  contents: read' && /usr/bin/grep -qE '^merge commit: [0-9a-f]{7,40}$' "$EV" && /usr/bin/grep -qF 'prom#54' "$EV" && /usr/bin/grep -qF 'firestarter#58' "$EV" && /usr/bin/grep -qF 'firestarter_app#57' "$EV" && /usr/bin/grep -qiE 'fourth (instance|pull request|exercise)' "$EV" && /usr/bin/grep -qiE 'not (yet )?been observed green in CI|never been observed green|on .main. is not the same as observed' "$EV"; fi</automated>
    <fails_when>non-zero exit. On the approved path: no `pull request: #N` line in the evidence, or the pull request is not `MERGED`, or its base is not `main`, or its file set omits the workflow or includes a `.planning/` path (the PR carried planning commits into the default branch); or `main`'s copy of the workflow does not contain the step name, meaning the merge did not land what it claimed; or `permissions:` on `main` is no longer `contents: read`, meaning the merge widened the job; or the three prior pull requests are not cited, or the record does not distinguish being on `main` from having been observed green in CI. On the deferred path: the step name is already present on `main`, which means the pull request was opened despite the deferral.</fails_when>
  </verify>
  <done>Either the leg is on prom's `main` by a merged pull request whose file set is exactly the workflow, recorded as the fourth banked POLICY-05 instance with the limit — never observed green in CI — written down; or the deferral is recorded and `main` provably does not carry the leg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the leg's `run:` body → the runner's filesystem layout | Meta is at `meta/`, the wiki clone at `wiki-clone`. A path written for the local tree silently fails only when the weekly cron fires. |
| feature branch → protected `main` | A pull request is the only route; `current_user_can_bypass` is `never`. Whatever the branch carries reaches the default branch. |
| workflow `permissions` and `uses:` set → the job's privilege and supply chain | A leg that widens either changes what a scheduled job can do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-27 | Repudiation | a leg that is registered but has never been observed to work | high | mitigate | Task 1 extracts the leg's `run:` body out of the YAML rather than retyping it and executes it against a reproduced runner layout; both Task 1 and Task 3 require the record to distinguish registered from observed green in CI, the exact conflation behind Phase 172's NON-CLAIM 3. |
| T-173-28 | Elevation of Privilege | widening `permissions:` or adding a `uses:` action | high | mitigate | The gate asserts `permissions: contents: read` locally and again on `main` after the merge, and asserts the `uses:` count is identical to HEAD, so no new third-party action version enters the supply chain. |
| T-173-29 | Tampering | a CI job able to rewrite the wiki | high | mitigate | The leg invokes the checker in check mode only; the gate fails if the emit flag appears anywhere in the workflow file. |
| T-173-30 | Elevation of Privilege | a pull request into a protected default branch carrying unintended files | medium | mitigate | The branch is produced off `origin/main` carrying only the workflow change; the gate asserts the merged pull request's file set contains the workflow and no `.planning/` path. |
| T-173-31 | Elevation of Privilege | the merge proceeding on an auto-approved checkpoint | high | mitigate | Task 3 asserts an on-disk approval file carrying an exact literal, and the deferred branch of the gate asserts that `main` does **not** carry the step — so a deferral cannot be quietly overridden. |
| T-173-32 | Denial of Service | a guard registered against a wiki with no footers, going red on the next cron | medium | mitigate | Task 2's gate refuses an approval unless plan 173-05's post-push fresh-clone evidence shows the published wiki green at 6 footers; the plan depends on 173-05 and sits in a later wave. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package and adds no `uses:` action, so the workflow's pinned-action set is unchanged from HEAD and asserted so. There is no `[ASSUMED]` or `[SUS]` package to gate. |
</threat_model>

<verification>
- `wiki-check.yml` carries a fourth leg named `Provenance footer check`, comment-free, after the dispatch-mirror leg, with no line deleted from the file.
- The leg's body, extracted from the YAML and executed against a reproduced runner layout, exits 0 and prints the checker's OK line.
- `permissions: contents: read` and the `uses:` count are unchanged, locally and on `main` after the merge.
- The emit flag appears nowhere in the workflow.
- Either a merged pull request carries the workflow into `main` with no `.planning/` path in its file set, or the deferral is recorded and `main` provably does not carry the step.
- The evidence names the three prior `main` pull requests and calls this the fourth.
- The evidence distinguishes the leg being on `main` from the leg having been observed green in CI.
</verification>

<success_criteria>
- D-10's guard is wired into the workflow that already clones everything it needs, in the established leg shape, with no new privilege and no new action.
- The leg's correctness is observed locally rather than assumed from a workflow with zero runs.
- POLICY-05's pull-request flow gains a fourth banked instance, recorded as evidence rather than as overhead.
- Three evidence files are committed under the phase evidence directory.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-06-SUMMARY.md` when done.
</output>
