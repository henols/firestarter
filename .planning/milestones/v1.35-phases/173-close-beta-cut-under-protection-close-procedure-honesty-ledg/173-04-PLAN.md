---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-issue-state-before.json
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-draft-link-check.txt
autonomous: true
requirements: [POLICY-04]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Four reply bodies exist in the phase record — one each for gh#5, gh#6, gh#7 and gh#9 — each stating what was delivered, what was declined and why, and where the surviving tracker is, per D-12 and D-13."
    - "The gh#6 draft names both of Phase 172 D-11's declines by name — required status checks and required review-thread resolution — so they read as decided rather than quietly skipped."
    - "The gh#7 draft states that its generated-site premise was rejected at the 2026-07-27 backlog review, that the Wiki was chosen instead, and that its content requirements live on in gh#5."
    - "The gh#9 draft describes the configured end state and says the issue stays open as the pinned orientation issue, which it was never actually pinned as."
    - "The gh#5 draft states that FUT-W-01 through FUT-W-05 are deferred, not delivered, and that gh#5 is the surviving upstream tracker for them."
    - "Every link in every draft resolves, and each draft is stored as a separate body file so the posting plan can pass it to `--body-file` unmodified."
    - "Nothing is posted, closed or pinned by this plan; the pre-post state of all four issues is captured so the posting plan has a before-half."
  artifacts:
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-issue-state-before.json
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-draft-link-check.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh5.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh6.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh7.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/173-gh9.md
  key_links:
    - "`173-UPSTREAM-REPLIES.md` (the reviewable record) -> `evidence/bodies/173-gh<n>.md` (the exact bytes posted) -> plan 173-07's `gh issue comment --body-file`. The record and the body files must not diverge, so the record embeds each body verbatim."
    - "Backlog 999.12 and REQUIREMENTS.md's FUT-W-01..05 list -> the gh#5 draft. Backlog 999.13 and Phase 172 D-11 -> the gh#6 draft."
  prohibitions:
    - statement: "Nothing is posted, commented, closed, pinned or otherwise written to GitHub by this plan; it makes read-only API calls only."
      status: required
      verification: explicit
    - statement: "No draft claims a wiki page, a feature or a check exists that this milestone did not actually produce; every claim in a draft must be traceable to a `.planning/` artifact or a live GitHub or wiki read."
      status: required
      verification: explicit
    - statement: "No draft presents the two declines from Phase 172 D-11 as delivered, deferred-without-decision, or absent."
      status: required
      verification: explicit
    - statement: "No draft promises FUT-W-01 through FUT-W-05, or any date for them."
      status: required
      verification: explicit
    - statement: "No file this plan writes carries a comment of any kind."
      status: required
      verification: explicit
---

<objective>
Write the four upstream replies criterion 5 owes, into the phase record, where the operator can read
them before a word of them is public.

Purpose: D-13 requires a blocking operator wording review before any of these is posted, on exactly
v1.22 D-02's precedent — the only pattern this project has for text going out under the operator's
name. A comment on a public tracker is indexed, and `updatedAt` bumps on creation rather than on a
body edit, so a correction reads as a second statement rather than a replacement. Drafting and
posting are therefore separate plans in separate waves.

Output: `173-UPSTREAM-REPLIES.md` carrying all four bodies verbatim for review, four separate body
files that plan 173-07 passes to `--body-file` unmodified, and a captured pre-post state of all four
issues.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · `.github/workflows/wiki-check.yml` new step
  `Provenance footer check` (plan 173-06) · `.planning/config.json` keys `git.base_branch` and
  `git.protected_branches` (plan 173-02) · `/workspaces/CLAUDE.md` section
  `## Milestone close and branch protection` (plan 173-02) · `.planning/REQUIREMENTS.md` checkboxes
  (plan 173-09).

**New records**
- **`.planning/phases/173-.../173-UPSTREAM-REPLIES.md` and the four `evidence/bodies/173-gh<n>.md` files (this plan).**
- `.planning/v1.35/CLOSE-RECORD.md` (plan 173-08) · `.planning/notes/v135-close-procedure-under-protection.md` (plan 173-02)

**Orchestrator-owned ROADMAP writes, handed off and never written by an executor**
- `### Phase 999.46`, `### Phase 999.47`, and the Phase 172 / Phase 173 checklist boxes

**Out-of-repo**
- Six provenance footers on `firestarter_prom.wiki.git` (plan 173-05)
- Four issue comments, gh#7 and gh#6 closed, gh#9 pinned (plan 173-07)

<tasks>

<task type="auto">
  <name>Task 1: Capture what the four issues say and what this milestone actually delivered against each</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-issue-state-before.json</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the full v1.35 requirement list with its current marks, the `## Future Requirements` block naming FUT-W-01 through FUT-W-05 against Backlog 999.12, and the `## Out of Scope` table with the generated-site row and its stated reason.
    - `.planning/ROADMAP.md` — the v1.35 milestone section with its seven activation decisions and the model reversal, the v1.35 phase checklist (Phases 169 and 170 recorded as executed ad hoc; Phase 172's box unchecked despite all four of its requirements being marked complete), and the Backlog 999.12 and 999.13 rows.
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt` — NON-CLAIM 2 in particular, which says in its own words that Phase 173's ledger should state gh#6's two declines as non-claims rather than let them read as quietly delivered.
    - `.planning/notes/v135-phases-169-170-executed-ad-hoc.md` — the criterion-by-criterion re-check Phases 169 and 170's marks rest on, including FRONT-02 being NOT met by operator decision. A reply must not claim what FRONT-02 would have delivered.
    - `.planning/notes/v135-wiki-only-reversal.md` — what the 2026-08-30 reversal voided and what survived, so no draft cites tooling that no longer exists.
  </read_first>
  <action>
Establish, from artifacts and live reads rather than from memory, exactly what each of the four
issues asked for and exactly what v1.35 delivered against it. This is the research the drafts rest
on; writing first and checking later is how a wrong claim reaches a public tracker.

Read all four issue bodies and their current state from the API — number, title, state, label set,
comment count and the URL — and write them into
`evidence/173-04-issue-state-before.json` keyed by issue number. This is the before-half plan 173-07
compares against; it is also the proof that all four were open with zero comments before this
milestone said anything upstream.

Then, for each issue, assemble a short internal ledger inside your working notes for Task 2 —
delivered, declined, deferred, and where the surviving tracker is:

- **gh#5 "Move documentation"** — Backlog 999.12's source. Delivered: the wiki exists and carries
  the migrated pages; both sub-repository `doc/` directories are gone; the READMEs were cut to repo
  scope. Deferred, not delivered: FUT-W-01 through FUT-W-05 — the compatibility matrix, per-family
  pages, algorithm and command-set pages, task-oriented tutorials, and repository metadata keywords.
  Surviving tracker: gh#5 itself, which stays open.
- **gh#6 "Protect main branches and centralize issue tracking"** — Backlog 999.13's source, and the
  authoritative list of what "one tracker, protected main" means. Delivered: one tracker stated in
  the documentation, Issues disabled on both sub-repositories, issue templates covering a bug
  report, a feature request and a chip-validation report, and enforcing rulesets on all three `main`
  branches. **Declined, and must be named:** required status checks, and required review-thread
  resolution — both decided by Phase 172 D-11.
- **gh#7 "Improve Firestarter discoverability with generated documentation"** — its generated-site
  premise was rejected at the 2026-07-27 backlog review when the operator chose the Wiki; the SEO
  and discoverability goal it was filed for is given up, and `REQUIREMENTS.md`'s Out of Scope table
  says so. Its content requirements were carried into Backlog 999.12 and live on upstream in gh#5.
- **gh#9 "Repository Structure and Contribution Guide"** — its text is the source Phase 172 relocated
  to the wiki `Contributing` page under POLICY-01. It stays open as the orientation issue and gets
  pinned; measure and record that prom's pinned-issue set is currently **empty**, so pinning was
  always intended and never done.

Record every fact you will assert in a draft with the artifact or the API read that establishes it.
Anything you cannot establish does not go in a draft.

Make read-only calls only. Nothing in this task posts, comments, closes or pins.
  </action>
  <acceptance_criteria>
    - `evidence/173-04-issue-state-before.json` exists with entries for issues 5, 6, 7 and 9.
    - Each entry carries `state`, `comments`, `title` and `url`.
    - All four entries read `state: open` and `comments: 0`, matching the state RESEARCH.md measured.
    - The file records prom's pinned-issue count, and it is `0`.
    - No write-shaped API call was made: the four issues still have zero comments after this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && mkdir -p "$E" && F=$E/173-04-issue-state-before.json && test -s "$F" && python3 -c "
import json
d=json.load(open('$F'))
issues=d['issues'] if 'issues' in d else d
assert sorted(str(k) for k in issues)==['5','6','7','9'], sorted(str(k) for k in issues)
for k,v in issues.items():
    assert v['state'].lower()=='open', ('NOT OPEN', k, v['state'])
    assert int(v['comments'])==0, ('HAS COMMENTS', k, v['comments'])
    assert v['title'] and v['url']
assert int(d.get('pinned_issue_count', -1))==0, ('PINNED SET NOT EMPTY', d.get('pinned_issue_count'))
print('before-capture OK')" && for n in 5 6 7 9; do test "$(gh api repos/henols/firestarter_prom/issues/$n --jq .comments)" = '0' || { echo "COMMENT ALREADY POSTED ON gh#$n"; exit 1; }; done</automated>
    <fails_when>non-zero exit; or a `NOT OPEN`, `HAS COMMENTS` or `PINNED SET NOT EMPTY` tuple naming which issue broke the expectation, which means the upstream state moved since RESEARCH.md measured it and the drafts must be rewritten against the new state; or a `COMMENT ALREADY POSTED ON gh#<n>` line, which means this plan wrote to GitHub when it is read-only.</fails_when>
  </verify>
  <done>The pre-post state of all four issues is captured from the API, all four are confirmed open with zero comments, prom's pinned set is confirmed empty, and every fact the drafts will assert has a named source.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the four replies into the phase record, as the exact bytes that will be posted</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-draft-link-check.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — D-12's per-issue disposition and D-13's blocking-review requirement, plus the Claude's Discretion clause that fixes the three things every reply must contain: what was delivered, what was declined and why, and where the surviving tracker is.
    - `evidence/173-04-issue-state-before.json` — Task 1's capture; the drafts must agree with it.
    - `.planning/MILESTONES.md` — the v1.23 release-state paragraph recording the blocking wording review D-13 follows, for the register these replies should be written in.
    - `.planning/v1.35/MIGRATION-TABLE.md` — the current page list, so a draft links only to pages that exist. `Protocol-Flags` and `Protocol-ID` do **not** exist on the live wiki; no draft may link to them.
  </read_first>
  <action>
Write the four replies. Prose is at your discretion subject to D-13's review, provided each states
what was delivered, what was declined and why, and where the surviving tracker is.

Store each body **twice and identically**: once verbatim inside
`173-UPSTREAM-REPLIES.md`, which is the document the operator reads, and once as its own file under
`evidence/bodies/173-gh<n>.md`, which is what plan 173-07 passes to `gh issue comment --body-file`
unmodified. The record embeds the bodies rather than describing them, so what the operator approves
and what gets posted cannot diverge.

`173-UPSTREAM-REPLIES.md` opens with a header block — date, the requirement and criterion it serves,
the D-13 review status set to `PENDING OPERATOR REVIEW`, and a table of the four issues with their
intended dispositions: gh#5 reply and stay open; gh#6 reply and close; gh#7 reply and close; gh#9
reply, stay open, and pin. Then one section per issue holding the body verbatim in a fenced block.

Content requirements per issue, on top of the three universal ones:

- **gh#7 — reply and close.** State that the generated-site premise was rejected at the 2026-07-27
  backlog review and the Wiki was chosen; that the SEO and discoverability goal the issue was filed
  for is consequently given up rather than met; and that its content requirements were carried into
  Backlog 999.12 and are tracked upstream on gh#5. Say why it is being closed: leaving it open
  presents a rejected premise as a live feature request.
- **gh#5 — reply, stays open.** State what the migration delivered, and state plainly that
  FUT-W-01 through FUT-W-05 are **deferred, not delivered** — name them. State that gh#5 is the
  surviving upstream tracker for them and that closing it would leave them visible only inside this
  repository's planning directory. Promise no dates.
- **gh#6 — reply and close.** This is D-12's deliberate widening; criterion 5 does not list it. It
  is the issue this milestone most directly delivered. Enumerate what shipped, then name **both**
  declines explicitly — required status checks and required review-thread resolution, both decided
  by Phase 172 D-11 — with the reason each was declined and the condition under which it would be
  revisited. NON-CLAIM 2 of Phase 172's closing sweep says in its own words that these must be
  stated rather than left to read as quietly delivered.
- **gh#9 — reply, stays open, gets pinned.** Describe the configured end state: one tracker, both
  sub-repositories' Issues disabled, pull requests going to the repository holding the changed code,
  and the orientation text now living on the wiki `Contributing` page. State that the issue stays
  open as the pinned orientation issue.

Two honesty constraints that bind the drafting:

1. **Do not claim what was not done.** Phase 169's FRONT-02 was declined outright by the operator
   rather than met, Phases 169 and 170 were executed ad hoc with no plans and no summaries, and the
   `Wiki check` workflow is registered but has never run. No reply may imply otherwise. Where a
   reply would naturally claim one of these, either drop the claim or state the limit.
2. **Every link must resolve.** Link only to wiki pages that exist on the live wiki today, to
   backlog rows that exist, and to issues that exist. `Protocol-Flags` and `Protocol-ID` are not
   live pages.

Backlog 999.9 will rename all three repositories and invalidate every URL these replies contain.
That is accepted at activation, and these four replies join the set of this phase's outputs needing
re-sweeping — recorded in plan 173-08's criterion-4 section. Keep the links in a plain, greppable
form so that sweep can find them.

Verify the links mechanically rather than by eye: extract every URL from the four body files and
resolve each one, recording the status per URL in `evidence/173-04-draft-link-check.txt`.

Nothing is posted by this task. `173-UPSTREAM-REPLIES.md` leaves this plan with its review status
still reading `PENDING OPERATOR REVIEW`; plan 173-07 is the only thing that may change it.
  </action>
  <acceptance_criteria>
    - `173-UPSTREAM-REPLIES.md` exists, carries the four dispositions in a table, and holds all four bodies verbatim.
    - Four body files exist under `evidence/bodies/`, one per issue, each non-empty.
    - Each body file's content appears verbatim inside `173-UPSTREAM-REPLIES.md` — the record and the payload agree byte for byte.
    - The gh#6 body names both `required status checks` and review-thread resolution as declined.
    - The gh#5 body names all five of FUT-W-01 through FUT-W-05 and the words `deferred, not delivered`.
    - The gh#7 body names the 2026-07-27 backlog review and points at gh#5.
    - The gh#9 body describes the configured end state and says the issue stays open.
    - No body links to `Protocol-Flags` or `Protocol-ID`.
    - Every URL in every body resolves; the link-check evidence records one status line per URL and no failures.
    - `173-UPSTREAM-REPLIES.md` still reads `PENDING OPERATOR REVIEW`.
    - All four issues still have zero comments.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && R=$P/173-UPSTREAM-REPLIES.md && B=$P/evidence/bodies && test -s "$R" && for n in 5 6 7 9; do test -s "$B/173-gh$n.md" || { echo "MISSING BODY: gh#$n"; exit 1; }; python3 -c "
import sys,pathlib
body=pathlib.Path('$B/173-gh$n.md').read_text().strip()
rec=pathlib.Path('$R').read_text()
sys.exit(0 if body and body in rec else 1)" || { echo "BODY NOT EMBEDDED VERBATIM: gh#$n"; exit 1; }; done && /usr/bin/grep -qiF 'required status checks' "$B/173-gh6.md" && /usr/bin/grep -qiE 'review[- ]thread' "$B/173-gh6.md" && for f in FUT-W-01 FUT-W-02 FUT-W-03 FUT-W-04 FUT-W-05; do /usr/bin/grep -qF "$f" "$B/173-gh5.md" || { echo "MISSING IN gh#5 BODY: $f"; exit 1; }; done && /usr/bin/grep -qiF 'deferred, not delivered' "$B/173-gh5.md" && /usr/bin/grep -qF '2026-07-27' "$B/173-gh7.md" && /usr/bin/grep -qE 'issues/5|gh#5' "$B/173-gh7.md" && /usr/bin/grep -qiF 'stays open' "$B/173-gh9.md" && ! /usr/bin/grep -rqE 'Protocol-Flags|Protocol-ID' "$B" && /usr/bin/grep -qF 'PENDING OPERATOR REVIEW' "$R" && for n in 5 6 7 9; do test "$(gh api repos/henols/firestarter_prom/issues/$n --jq .comments)" = '0' || { echo "POSTED EARLY: gh#$n"; exit 1; }; done</automated>
    <fails_when>non-zero exit, or any of the named diagnostic lines. `BODY NOT EMBEDDED VERBATIM: gh#<n>` is the one that matters most: the operator would be reviewing text different from what gets posted, which defeats D-13 entirely. `POSTED EARLY: gh#<n>` means this plan wrote to GitHub before the wording review. A missing decline string in the gh#6 body means Phase 172's NON-CLAIM 2 is being left to read as quietly delivered.</fails_when>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && EV=$P/evidence/173-04-draft-link-check.txt && : > "$EV" && URLS=$(/usr/bin/grep -ohE 'https?://[^ )>"]+' "$P/evidence/bodies/"*.md | sed 's/[.,]$//' | sort -u) && test -n "$URLS" && for u in $URLS; do code=$(curl -s -o /dev/null -w '%{http_code}' -L --max-time 20 "$u"); echo "$code $u" >> "$EV"; done; test "$(wc -l < "$EV")" -ge 1 && ! /usr/bin/grep -qE '^(4[0-9][0-9]|5[0-9][0-9]|000) ' "$EV"
</automated>
    <fails_when>non-zero exit; or no URL was extracted from any body, meaning the drafts cite nothing and the link check is vacuous; or any line in the evidence file begins with a 4xx, 5xx or 000 status, naming the URL that does not resolve — a dead link in a public reply is the kind of thing D-13's review exists to catch, and Backlog 999.9's rename will produce more of them later.</fails_when>
  </verify>
  <done>Four reply bodies exist as files and verbatim inside `173-UPSTREAM-REPLIES.md`, each carrying what was delivered, what was declined and why, and where the surviving tracker is; every URL in them resolves; the record still reads `PENDING OPERATOR REVIEW`; and all four issues still have zero comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` artifacts and live API reads → draft text | Every claim in a public reply crosses here. A claim that cannot be traced back does not cross. |
| draft record → the exact bytes posted | If the reviewed text and the posted text can differ, the review guarantees nothing. |
| this plan (read-only) → GitHub | Nothing may cross. Posting is plan 173-07's, behind its own approval file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-05 | Repudiation | a public comment posted without review | high | mitigate | Drafting and posting are separate plans in separate waves; this plan writes nothing to GitHub and its gate re-reads all four comment counts to prove it. `updatedAt` bumps on creation rather than on a body edit, so a correction reads as a second statement — there is no cheap undo. |
| T-173-19 | Tampering | the reviewed text and the posted text diverging | high | mitigate | Each body is stored once as a file and embedded verbatim in the review record; the gate asserts the file's content appears inside the record byte for byte, and plan 173-07 posts the file with `--body-file` rather than re-typing it. |
| T-173-20 | Spoofing | a reply claiming something this milestone did not deliver | high | mitigate | Task 1 assembles a per-issue delivered/declined/deferred ledger from artifacts and API reads before a word is drafted; the gate asserts the specific strings for the two declines, the five deferred requirements and the rejected premise, and forbids links to the two pages that do not exist. |
| T-173-21 | Repudiation | a dead link in a public reply | medium | mitigate | Every URL in every body is extracted and resolved, one status line per URL, and any 4xx, 5xx or connection failure fails the gate. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package. It uses `gh`, `curl` and `python3`, all already present. There is no `[ASSUMED]` or `[SUS]` package to gate. |
</threat_model>

<verification>
- All four issues are captured open with zero comments before anything is drafted, and prom's pinned set is captured empty.
- Four bodies exist as files and appear verbatim inside `173-UPSTREAM-REPLIES.md`.
- The gh#6 body names both Phase 172 D-11 declines; the gh#5 body names all five FUT-W requirements and says deferred, not delivered; the gh#7 body names the 2026-07-27 review and points at gh#5; the gh#9 body describes the end state and says the issue stays open.
- No body links to `Protocol-Flags` or `Protocol-ID`.
- Every URL in every body resolves.
- The record still reads `PENDING OPERATOR REVIEW` and all four issues still have zero comments when the plan ends.
</verification>

<success_criteria>
- The four replies criterion 5 owes exist in reviewable form, with nothing public.
- What the operator will read is byte-identical to what will be posted.
- The two declines Phase 172 handed forward are named in text, not left implicit.
- Three evidence artifacts plus four body files are committed under the phase directory.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-04-SUMMARY.md` when done.
</output>
