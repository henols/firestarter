---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 08
type: execute
wave: 4
depends_on: ["173-02", "173-03", "173-05", "173-06", "173-07"]
files_modified:
  - .planning/v1.35/CLOSE-RECORD.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.46.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.47.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-roadmap-handoff.md
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-rename-sweep-targets.txt
autonomous: false
requirements: [POLICY-04, POLICY-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/v1.35/CLOSE-RECORD.md` exists and pairs every claim this milestone makes with its explicit non-claim, comprehensively rather than curated at ten rows — D-11."
    - "The ledger carries criterion 3's three named minimums in its own words: relocation is not verification; FUT-W-01 through FUT-W-05 are deferred, not delivered; and HONEST-02's stamp-or-check establishes agreement with the database at a point in time, not continuously."
    - "The ledger carries all four non-claims and all three findings Phase 172's closing sweep handed forward, stated rather than rediscovered."
    - "The ledger carries POLICY-04's own non-claim per D-03: v1.35 claims the rulesets block a direct push to `main` and do not block a push to an unprotected ref; it does not claim a beta lockstep cut was performed under them unless plan 173-09 records one."
    - "The ledger carries what Phases 167 through 171 left open, including FRONT-02 which the operator declined outright rather than met."
    - "The ledger carries the four corrections this phase's own research found in its inputs — the footer count, the unrecorded pages, `REQUIREMENTS.md:119`'s false premise, and the stale manual-PyPI-dispatch premise — because a close phase's criteria are not themselves evidence."
    - "A findings table files every unfixed finding as a backlog item rather than carrying it as prose, with both dispositions represented: a new row, and a pointer to an existing one — criterion 4's first half."
    - "Two complete backlog row bodies are authored as handoff artifacts for the orchestrator to insert — 999.46 for the rulesets blocking the stable-release version bump, with the off-`main` bump named as the recommended remedy and the other two candidates recorded with the reasons they are not recommended (D-05), and 999.47 for prom's default branch being red before Phase 172's merges."
    - "Backlog 999.46 exists as a heading in ROADMAP.md and the pending todo file it came from is gone."
    - "The 999.9 rename sweep is recorded with phases 169, 170 and 172 named, plus this phase's own link-writing outputs, in a mechanically greppable form — criterion 4's second half."
  artifacts:
    - .planning/v1.35/CLOSE-RECORD.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.46.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.47.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-roadmap-handoff.md
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-rename-sweep-targets.txt
  key_links:
    - "`.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` -> `evidence/173-08-backlog-999.46.md` -> the orchestrator's ROADMAP insertion -> the todo file removed. The executor authors the body and never writes ROADMAP.md."
    - "`.planning/phases/172-.../evidence/172-09-closing-sweep.txt` (4 non-claims, 3 findings) -> the ledger rows and the findings table. Inherited stated, not rediscovered."
    - "plan 173-03's probe verdict + plan 173-09's cut disposition -> POLICY-04's ledger row. The row's non-claim half depends on whether the cut was authorized."
  prohibitions:
    - statement: "No executor writes `.planning/ROADMAP.md`. Both backlog bodies and the checklist corrections are authored as handoff artifacts and inserted by the orchestrator; `roadmap.update-plan-progress` overwrites positionally and has clobbered an unrelated phase's dependency table, and v1.35's ROADMAP was hand-authored and must never be regenerated."
      status: required
      verification: explicit
    - statement: "No workflow file in either sub-repository is edited; 999.46 files the release-path breakage and does not fix it."
      status: required
      verification: explicit
    - statement: "No ledger row states a claim the phase's own evidence files do not support; every row cites the artifact that establishes it."
      status: required
      verification: explicit
    - statement: "The close record does not use v1.34's deviation section for POLICY-04; D-03 explicitly declined that shape in favour of complete-on-the-probe with a stated non-claim."
      status: required
      verification: explicit
    - statement: "No unfixed finding is carried as prose only; each is either filed as a backlog row or pointed at an existing one, and the findings table shows which."
      status: required
      verification: explicit
    - statement: "No file this plan writes carries a comment of any kind."
      status: required
      verification: explicit
---

<objective>
Write the milestone's non-claims down as plainly as its claims, and file every unfixed finding
instead of leaving it in prose.

Purpose: ROADMAP criterion 3 asks for a ledger pairing **every** claim with its explicit non-claim,
which is why D-11 chose comprehensive over v1.34's curated ten rows. Criterion 4 asks that findings
be filed rather than carried. Both exist because this project's recorded failure mode is a claim
that reads stronger than its evidence — and because three things this phase's own roadmap asserts
turned out not to be true when measured.

Output: `.planning/v1.35/CLOSE-RECORD.md`, two complete backlog row bodies handed to the
orchestrator, and a greppable record of what Backlog 999.9's rename will invalidate.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.34/CLOSE-RECORD.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt
@.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · `.github/workflows/wiki-check.yml` new step
  (plan 173-06) · `.planning/config.json` keys `git.base_branch` and `git.protected_branches`
  (plan 173-02) · `/workspaces/CLAUDE.md` section `## Milestone close and branch protection`
  (plan 173-02) · `.planning/REQUIREMENTS.md` checkboxes (plan 173-09).

**New records**
- **`.planning/v1.35/CLOSE-RECORD.md` (this plan)** · `.planning/notes/v135-close-procedure-under-protection.md`
  (plan 173-02) · `.planning/phases/173-.../173-UPSTREAM-REPLIES.md` (plan 173-04) ·
  **this plan's four `evidence/173-08-*` files.**

**Orchestrator-owned ROADMAP writes, handed off by this plan and never written by an executor**
- **`### Phase 999.46: The v1.35 `Protect main` rulesets block the stable-release version bump in both sub-repositories`**
- **`### Phase 999.47: `firestarter_prom`'s default branch was already red on `Catalog sync check` before Phase 172's merges`**
- **The Phase 172 and Phase 173 checklist boxes in the v1.35 phase list**

**Out-of-repo**
- Six provenance footers on `firestarter_prom.wiki.git` (plan 173-05) · a fourth pull request into a
  protected `main` (plan 173-06) · four issue comments, gh#7 and gh#6 closed, gh#9 pinned (plan 173-07)

<tasks>

<task type="auto">
  <name>Task 1: Author the honesty ledger — every claim beside its non-claim, comprehensively</name>
  <files>.planning/v1.35/CLOSE-RECORD.md</files>
  <read_first>
    - `.planning/v1.34/CLOSE-RECORD.md` — the shape to copy: the header block at `:1-7`, §1 "Scope — what ran, and what did not" at `:11-30`, §5's ledger table with the verbatim header `| # | What v1.34 claims | What v1.34 explicitly does NOT claim |` at `:196`, §6's `| Finding | Filing |` at `:215`, and §7, the deviation section D-03 explicitly declined to reuse. Note the row voice worth copying: the claim half is a flat statement of what was measured, and the non-claim half names the specific unmeasured thing and bolds the negation.
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt` — the single most important input. Four non-claims and three findings, written so this ledger inherits them stated rather than rediscovered: NON-CLAIM 1 the Actions-bypass HTTP 422 and the `actor_id: null` residual; NON-CLAIM 2 gh#6's two declines; NON-CLAIM 3 `Wiki check` registered with zero runs; NON-CLAIM 4 the prefill check; FINDING A the release-path breakage; FINDING B prom's default branch already red; FINDING C the `build_db.py` rename.
    - `.planning/notes/v135-phases-169-170-executed-ad-hoc.md` — the criterion-by-criterion re-check Phases 169 and 170's marks rest on, including FRONT-02 being NOT met by operator decision.
    - `.planning/notes/v135-wiki-only-reversal.md` — what the reversal voided, so the ledger does not claim tooling that no longer exists.
    - `.planning/REQUIREMENTS.md` — the full v1.35 requirement list with its marks, the FUT-W-01…05 list, HONEST-02's text and WIKI-04's withdrawal note.
    - `.planning/ROADMAP.md` — the v1.35 milestone section, the seven activation decisions, and the phase checklist where Phase 172's box is unchecked though all four of its requirements are marked complete.
    - Every `evidence/173-0*` file produced by plans 173-02 through 173-07 — the ledger cites artifacts, and a row with no artifact behind it does not go in.
  </read_first>
  <action>
Create `.planning/v1.35/` — it does not exist — and write `CLOSE-RECORD.md` in the shape of v1.34's,
re-keyed to v1.35 and comprehensive rather than curated, per D-11.

Sections, in order:

**Header block.** Title naming the three subjects. Milestone, closed date, what it discharges
(POLICY-04, POLICY-05), and a status line telling the reader where to start.

**§1 Scope — what ran, what did not, and the record discrepancies.** A `| Phase | Planned | Actually
executed | Status |` table across Phases 167 to 173, followed by bolded plain-English enumerations of
the gaps. Name the three discrepancies the roadmap itself carries: Phase 172's checklist box is
unchecked though all four of its requirements are marked complete; Phases 169 and 170 were executed
ad hoc with no plans, no summaries, no verifier pass and no phase directory; and Phase 169's FRONT-02
was declined outright by the operator rather than met.

**§2 POLICY-04 — the probe, and the cut's disposition.** Summarize plan 173-03's verdict record and
state the cut's outcome as plan 173-09 recorded it. Per D-03, if the cut was not authorized,
POLICY-04 is marked **complete on the probe** with the missing half stated as a ledger non-claim —
criterion 1's own wording permits it. Do **not** use v1.34's §7 deviation shape; D-03 explicitly
declined it.

**§3 POLICY-05 — the close procedure under protection.** The config repoint with its read-back, the
note, the `CLAUDE.md` pointer, and the four pull requests into a protected `main` — the three merged
2026-09-02 plus plan 173-06's, cited as banked evidence that the flow works.

**§4 Corrections to this phase's own inputs.** Four rows with the measurement that established each:
the footer set is 6 pages and the table had 17 data rows, not the twelve and twenty-one CONTEXT.md
asserted; three live wiki pages had no migration-table row at all, a direction nothing had checked;
`REQUIREMENTS.md:119`'s stated rationale for POLICY-05 is factually false, and the three real failure
surfaces are named in its place; and D-04's "manual PyPI dispatch" premise is stale because
`beta-release.yml` now calls `publish.yml` by workflow call. Add the three the roadmap itself got
wrong that CONTEXT.md already found — no admin bypass exists to document, gh#9 was never pinned, and
the migration table listed two pages that do not exist. Say plainly what this section is for: a close
phase's criteria are not themselves evidence, and every one of these was found by measuring rather
than by reading.

**§5 The honesty ledger.** The table, header re-keyed to v1.35, with row ids. Comprehensive. It must
carry, at minimum:

- Criterion 3's three named minimums, each as its own row: relocation is not verification — a
  document moved to the wiki is confirmed intact, not confirmed accurate; FUT-W-01 through FUT-W-05
  are deferred, not delivered; HONEST-02's stamp-or-check establishes agreement with the database at
  a point in time, not continuously.
- Phase 172's four non-claims, inherited stated: the Actions bypass was rejected at creation with
  HTTP 422 because all three repositories are owned by a personal user account, so the bypass actor
  is a deploy key whose `actor_id` is null — meaning *any* deploy key, present or future, though all
  three repositories measure zero today; gh#6's required status checks and required review-thread
  resolution are knowingly not implemented; `Wiki check` is registered and has never run, so
  "registered" and "observed green in CI" are different facts and only the first is true; and the
  prefill check came back prefilled, so there was no regression to file.
- POLICY-04's own non-claim per D-03, worded as D-03 words it.
- The footer scope: six pages carry a provenance footer, not all eleven. Name the five exclusions and
  why each has no provenance to state, so no reader infers that every page is provenanced.
- `Breaking-Changes` has no recorded pre-deletion SHA because Phase 170 was executed ad hoc, so its
  provenance is recorded as a page rather than as a move.
- The `beta` asymmetry: `git.base_branch: "beta"` makes GSD treat `beta` as locally protected while
  GitHub leaves it carrying no ruleset at all.
- Phase 171's criterion 2, which prevented a latent misrepresentation rather than remediating a live
  one, and is recorded that way.
- The new provenance checker has been seen to fail on five planted defects and on the real inputs,
  but its CI leg's first live run is a weekly cron — so it is guarded by a check that works, not yet
  by a check that has been observed working in CI.
- Anything else Phases 167 through 171 left open that the requirement marks do not carry on their
  face.

Each row's non-claim half names the specific thing that is not claimed and bolds the negation, in
v1.34's register. Each row cites the artifact that establishes its claim half; a row with no
artifact does not go in.

**§6 Filed, not carried as prose.** The `| Finding | Filing |` table, criterion 4's first half. Both
dispositions must appear — a **new** backlog row and a pointer to an **existing** one — which is the
shape CONTEXT.md's discretion clause asks for. At minimum: the rulesets blocking the stable-release
version bump goes to **new 999.46**; prom's default branch already red on `Catalog sync check` goes
to **new 999.47**; the unattributed `build_db.py` rename points at existing **999.45**; the migration
table's two stale rows and three unrecorded pages are recorded as **fixed here**, not filed, with the
commit that fixed them; and the `actor_id: null` deploy-key residual is recorded as a **carried
non-claim rather than an unfixed finding**, with the reason — Phase 172 recorded it that way and this
close does not silently re-classify it.

**§7 Backlog 999.9 — what this milestone's own links will invalidate.** Criterion 4's second half.
Name Phases 169, 170 and 172 as the phases whose links need re-sweeping, and add this phase's own
outputs to that set: the four upstream reply comments with their URLs, the six wiki footers, the
close-procedure note, the `CLAUDE.md` pointer, the migration table's new rows, and the workflow
citations inside 999.46. Give the grep recipe that finds them, and note that it must use
`/usr/bin/grep` scoped to `git ls-files`, because the devcontainer's `grep` is ugrep and honours
`.gitignore` — plan 172-09 measured 0 matches under it against 38 under GNU grep over identical
paths.

Write no comments of any kind and no HTML comment openers anywhere in the file.
  </action>
  <acceptance_criteria>
    - `.planning/v1.35/CLOSE-RECORD.md` exists with all seven sections.
    - §5's table header names v1.35 in both the claim and non-claim columns, and the table has at least 15 data rows.
    - §5 contains the phrases `relocation is not verification`, `deferred, not delivered`, and `point in time`.
    - §5 names `FRONT-02`, `actor_id`, `Wiki check`, `FUT-W-01`, `HONEST-02` and `beta`.
    - §6's table header is `| Finding | Filing |` and its body contains `999.46`, `999.47` and `999.45`.
    - §7 names phases `169`, `170` and `172`, and names this phase's own outputs.
    - §4 names all four corrections and states that a close phase's criteria are not themselves evidence.
    - The file contains no section headed as a deviation record.
    - No HTML comment opener appears in the file.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && C=.planning/v1.35/CLOSE-RECORD.md && test -s "$C" && for s in 'relocation is not verification' 'deferred, not delivered' 'point in time' 'FRONT-02' 'actor_id' 'Wiki check' 'FUT-W-01' 'FUT-W-05' 'HONEST-02' '999.46' '999.47' '999.45' 'REQUIREMENTS.md:119' 'Breaking-Changes' 'Catalog sync check'; do /usr/bin/grep -qF "$s" "$C" || { echo "MISSING FROM LEDGER: $s"; exit 1; }; done && /usr/bin/grep -qE '^\| # \| What v1\.35 claims \| What v1\.35 explicitly does NOT claim \|' "$C" && test "$(awk '/^\| # \| What v1\.35 claims/{f=1;next} f&&/^\|---/{next} f&&/^\|/{c++} f&&!/^\|/{exit} END{print c+0}' "$C")" -ge 15 && /usr/bin/grep -qF '| Finding | Filing |' "$C" && for p in 169 170 172; do /usr/bin/grep -qE "(^|[^0-9])$p([^0-9]|$)" "$C" || { echo "RENAME SWEEP DOES NOT NAME PHASE: $p"; exit 1; }; done && ! /usr/bin/grep -qiE '^#+ .*deviation' "$C" && ! /usr/bin/grep -qE '<[!]--' "$C"
</automated>
    <fails_when>non-zero exit, or a `MISSING FROM LEDGER: <string>` line naming the first required fact the ledger omits, or a `RENAME SWEEP DOES NOT NAME PHASE: <n>` line; or the ledger table has fewer than 15 data rows, which means D-11's comprehensive table was curated back down toward v1.34's ten; or a heading matching a deviation record exists, which is the shape D-03 explicitly declined for POLICY-04; or an HTML comment opener appears.</fails_when>
    <automated>cd /workspaces && C=.planning/v1.35/CLOSE-RECORD.md && python3 -c "
import pathlib,re,sys
t=pathlib.Path('$C').read_text()
m=re.search(r'^\| # \| What v1\.35 claims.*?$(.*?)(?=^#|\Z)', t, re.S|re.M)
assert m, 'LEDGER TABLE NOT FOUND'
rows=[l for l in m.group(1).splitlines() if l.startswith('|') and not set(l.replace('|','').strip()) <= set('-: ')]
bad=[r for r in rows if '**' not in r.split('|')[-2]]
print('ledger rows:', len(rows), 'rows whose non-claim half bolds nothing:', len(bad))
for r in bad[:5]: print('  UNBOLDED NON-CLAIM:', r[:120])
assert not bad, 'a non-claim half must name the specific thing and bold the negation'
print('ledger voice OK')"</automated>
    <fails_when>non-zero exit, `LEDGER TABLE NOT FOUND`, or one or more `UNBOLDED NON-CLAIM:` lines quoting rows whose non-claim column bolds nothing — the row voice v1.34 established, where the negation is bolded so a skimming reader cannot mistake a non-claim for a claim, has not been carried over.</fails_when>
  </verify>
  <done>`.planning/v1.35/CLOSE-RECORD.md` exists with seven sections, a comprehensive ledger of at least fifteen rows each pairing a cited claim with a bolded non-claim, a findings table carrying both dispositions, a corrections section naming all four measured input errors, and a rename-sweep section naming phases 169, 170 and 172 plus this phase's own outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Author both backlog row bodies in full, ready for the orchestrator to insert, and name the recommended remedy rather than a menu</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.46.md, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-backlog-999.47.md, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-roadmap-handoff.md</files>
  <read_first>
    - `.planning/ROADMAP.md` around `### Phase 999.45` — the shape to copy exactly: the heading line with its parenthetical filing note, then `**Goal:**` in one sentence, a bolded framing paragraph naming why it is filed and not fixed, measured evidence with relative links carrying line anchors, a `**Chosen fix**` section, a `**Test surface.**` section, then a horizontal rule. 999.45 also models naming the rejected alternatives with their reasons, which is exactly D-05's "recommended remedy named, not a menu".
    - `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` — 999.46's source content, carrying the three candidate remedies and the reason the obvious one is not obviously right.
    - `firestarter_app/.github/workflows/release.yml` — `:2-5` the push-to-`main` trigger, `:32-35` the auto-commit action with its token override commented out, `:37-43` the release step which does pass the token. Read to cite, never to edit.
    - `firestarter/.github/workflows/build.yml` — `:34` the branch filter, `:182-183` the auto-commit gated to push-to-`main`, `:199-200` the release step depending on it. Read to cite, never to edit.
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt` — FINDING A, which becomes 999.46, and FINDING B, which becomes 999.47.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md` — §"Backlog 999.46 — insertion shape", including the re-verified citation table and the statement that the orchestrator writes this row, not an executor.
  </read_first>
  <action>
Write both backlog row bodies as complete, insertion-ready Markdown, each in its own evidence file,
following 999.45's structure exactly so the orchestrator can paste without reshaping.

**999.46 — the rulesets block the stable-release version bump in both sub-repositories.** This is
D-05, and it is the folded todo. Body:

- Heading in 999.45's form, with the filing parenthetical naming the date and that it was filed
  during v1.35 Phase 173 with product and workflow code out of scope.
- `**Goal:**` one sentence.
- The bolded framing paragraph: filed, not fixed, on purpose. `REQUIREMENTS.md`'s scope note says any
  product-code change found necessary is filed rather than fixed, and the milestone's own scope note
  binds both release workflows by name.
- The measured mechanism, with relative links carrying line anchors, using the six citations
  re-verified in RESEARCH.md: the app's release workflow triggers on push to `main`, its auto-commit
  step's token override is commented out, and its release step does pass the token; the firmware's
  build workflow filters branches, gates its auto-commit on push to `main`, and gates its release on
  the same condition. State the consequence plainly: both sub-repositories auto-commit a version bump
  back onto `main` from CI using the default token, the `pull_request` rule refuses it, and the lone
  deploy-key bypass actor does not cover a default-token push.
- `**Chosen fix**`: the todo's option 2 — move the version bump off `main`, either by bumping on
  `beta` or by a tag-triggered release with no push back — because it removes the conflict rather
  than carving an exception through it.
- The two rejected candidates **with the reasons they are not recommended**, in 999.45's voice:
  re-enabling the commented-out personal access token does not clearly work, because
  `current_user_can_bypass` is `never` and a token pushing as the account owner meets the same
  pull-request rule; and registering a deploy key relies on the bypass actor's null `actor_id`
  conferring bypass on *any* deploy key, which is a residual this milestone has already recorded as a
  non-claim rather than something to lean on.
- `**Test surface.**` What would have to be proven: a stable release reaching both channels end to
  end without a push to `main`, and the rulesets left unamended.

**No workflow file is edited by this task.** Both are read to cite.

**999.47 — prom's default branch was already red before Phase 172's merges.** This is FINDING B,
which is an unfixed finding and therefore owed a filing by criterion 4. Same structure: a heading, a
one-sentence goal, a framing paragraph stating that it predates this milestone's merges and is
therefore carried rather than caused, the measured evidence (a failing `Catalog sync check` on
`main`, with the run id and timestamp re-confirmed during this phase's research), a chosen fix, and a
test surface. State the reason it is filed rather than fixed: it is a check over catalog content
outside this milestone's documentation and configuration scope.

**Then write the handoff.** `evidence/173-08-roadmap-handoff.md` tells the orchestrator exactly what
to insert and where: both row bodies by file path, the insertion point immediately after 999.45's
horizontal rule and before the next divider, and the two checklist corrections the roadmap owes —
Phase 172's box, unchecked though all four of its requirements are complete, and Phase 173's box at
close. State explicitly that these are the orchestrator's writes: `roadmap.update-plan-progress`
overwrites positionally and has clobbered an unrelated phase's dependency table, and v1.35's ROADMAP
was hand-authored and must never be regenerated.

The todo file is removed only once the 999.46 row exists in ROADMAP.md. Do not remove it before.
  </action>
  <acceptance_criteria>
    - Both backlog body files exist, each opening with a `### Phase 999.4x:` heading and containing `**Goal:**`, a `**Chosen fix**` section and a `**Test surface.**` section.
    - The 999.46 body cites all six workflow line references and names both rejected candidates with reasons.
    - The 999.46 body names the off-`main` bump as the recommended remedy.
    - The 999.47 body names `Catalog sync check` and gives a run id and a timestamp.
    - `evidence/173-08-roadmap-handoff.md` names both body files, the insertion point, and the two checklist corrections, and states that ROADMAP writes are the orchestrator's.
    - Neither `firestarter_app/.github/workflows/release.yml` nor `firestarter/.github/workflows/build.yml` is modified.
    - `.planning/ROADMAP.md` is unmodified by this task.
    - No HTML comment opener appears in either body file.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence && for n in 46 47; do B=$E/173-08-backlog-999.$n.md; test -s "$B" || { echo "MISSING BODY: 999.$n"; exit 1; }; /usr/bin/grep -qE "^### Phase 999\.$n:" "$B" || { echo "NO HEADING: 999.$n"; exit 1; }; for s in '**Goal:**' '**Chosen fix' '**Test surface.**'; do /usr/bin/grep -qF "$s" "$B" || { echo "MISSING SECTION IN 999.$n: $s"; exit 1; }; done; ! /usr/bin/grep -qE '<[!]--' "$B" || { echo "COMMENT OPENER IN 999.$n"; exit 1; }; done && B46=$E/173-08-backlog-999.46.md && for s in 'release.yml' 'build.yml' 'current_user_can_bypass' 'deploy key' 'PERSONAL_ACCESS_TOKEN'; do /usr/bin/grep -qF "$s" "$B46" || { echo "MISSING FROM 999.46: $s"; exit 1; }; done && test "$(/usr/bin/grep -cE '#L[0-9]+' "$B46")" -ge 6 && /usr/bin/grep -qF 'Catalog sync check' "$E/173-08-backlog-999.47.md" && /usr/bin/grep -qE '[0-9]{8,}' "$E/173-08-backlog-999.47.md" && H=$E/173-08-roadmap-handoff.md && test -s "$H" && /usr/bin/grep -qF '173-08-backlog-999.46.md' "$H" && /usr/bin/grep -qF '173-08-backlog-999.47.md' "$H" && /usr/bin/grep -qF '999.45' "$H" && /usr/bin/grep -qiF 'orchestrator' "$H" && /usr/bin/grep -qF 'Phase 172' "$H" && test -z "$(git status --porcelain .planning/ROADMAP.md)" && test -z "$(git -C firestarter status --porcelain .github/workflows/build.yml)" && test -z "$(git -C firestarter_app status --porcelain .github/workflows/release.yml)"
</automated>
    <fails_when>non-zero exit, or any diagnostic line naming the missing body, heading, section or string. Fewer than six line-anchored links in the 999.46 body means the measured mechanism is asserted rather than cited. A non-empty `git status --porcelain` on `.planning/ROADMAP.md` means an executor wrote the roadmap, which is the orchestrator's write and has previously clobbered an unrelated phase's dependency table. A non-empty status on either release workflow means a file the milestone's scope note forbids editing was edited.</fails_when>
  </verify>
  <done>Two complete, insertion-ready backlog row bodies exist in 999.45's structure, 999.46 naming the off-`main` bump as the recommended remedy and both rejected candidates with their reasons, plus a handoff naming the insertion point and the two checklist corrections. No roadmap and no workflow file was touched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Orchestrator inserts 999.46 and 999.47 into ROADMAP.md; then the todo is retired and the rename-sweep target list is captured</name>
  <instructions>
    1. Open `.planning/ROADMAP.md` and find `### Phase 999.45` and the horizontal rule that closes it.
    2. Paste `evidence/173-08-backlog-999.46.md` verbatim immediately after that rule, by scoped edit — never through `roadmap.update-plan-progress` or any generating verb.
    3. Paste `evidence/173-08-backlog-999.47.md` verbatim immediately after 999.46's closing rule.
    4. In the v1.35 phase checklist, check Phase 172's box; all four of its requirements are marked complete and the box is the last discrepancy the close record names.
    5. Confirm every pre-existing `### Phase 999.40` through `### Phase 999.45` heading still appears exactly once.
    6. Only once `### Phase 999.46` exists in the file, delete `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md`.
  </instructions>
  <verification>
    `/usr/bin/grep -c '^### Phase 999\.46' .planning/ROADMAP.md` returns 1, the same for 999.47, every 999.40 through 999.45 heading still returns 1, Phase 172's checklist line matches `^- \[x\] \*\*Phase 172:`, and the todo file no longer exists.
  </verification>
  <resume-signal>Type "inserted" once both rows are in ROADMAP.md, Phase 172's box is checked and the todo file is deleted.</resume-signal>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-rename-sweep-targets.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-roadmap-handoff.md` — the instruction set: which bodies, which insertion point, which checklist boxes.
    - `.planning/ROADMAP.md` around `### Phase 999.45` — the insertion point and the divider that follows it.
    - `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` — the todo retired once the row exists, and not before.
  </read_first>
  <precondition>`evidence/173-08-backlog-999.46.md`, `evidence/173-08-backlog-999.47.md` and `evidence/173-08-roadmap-handoff.md` all exist and are non-empty. Halt if any is missing — there is nothing for the orchestrator to insert.</precondition>
  <action>
Hand the two backlog bodies to the orchestrator for insertion into `.planning/ROADMAP.md`, and stop.

**An executor does not write ROADMAP.md.** `roadmap.update-plan-progress` overwrites positionally and
has clobbered an unrelated phase's dependency table; v1.35's ROADMAP was hand-authored and must never
be regenerated. This is precisely why plan 172-09 filed the release-path breakage as a todo rather
than as a `999.x` row, and why its own text says the promotion is the orchestrator's write.

What the orchestrator inserts, by scoped edit and never by a generating verb:

1. `evidence/173-08-backlog-999.46.md` verbatim, immediately after 999.45's closing horizontal rule
   and before the next divider.
2. `evidence/173-08-backlog-999.47.md` verbatim, immediately after 999.46's closing rule.
3. Phase 172's checklist box in the v1.35 phase list, which is unchecked though all four of its
   requirements are marked complete — the discrepancy §1 of the close record names.

Once the 999.46 heading exists in `ROADMAP.md`, and only then, delete
`.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md`. The todo is
retired by the row existing, not by intent to create one.

Then capture the rename-sweep target list into
`evidence/173-08-rename-sweep-targets.txt`, which is criterion 4's second half made mechanical rather
than narrative. Scan the tracked tree for every reference to the three repository names this phase
wrote or inherited, scoped to `git ls-files` so gitignored scratch cannot produce a false hit, and
using `/usr/bin/grep` rather than the `grep` on PATH, which is ugrep and honours `.gitignore` — plan
172-09 measured 0 matches under it against 38 under GNU grep over identical paths. Record the total,
the per-file counts, and a section listing this phase's own outputs by name: the four upstream reply
comment URLs, the six wiki footers, the close-procedure note, the `CLAUDE.md` pointer, the migration
table's new rows, and 999.46's workflow citations. Name phases 169, 170 and 172 alongside them as the
phases the roadmap already accepted would need re-sweeping.
  </action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains exactly one line matching `^### Phase 999.46` and exactly one matching `^### Phase 999.47`.
    - `.planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md` no longer exists.
    - Phase 172's checklist box in the v1.35 phase list is checked.
    - `evidence/173-08-rename-sweep-targets.txt` exists, records a non-zero total, lists per-file counts, and names phases 169, 170 and 172.
    - The rename-sweep scan was run with `/usr/bin/grep` scoped to `git ls-files`, and the evidence says so.
    - No `### Phase 999.4x` heading that existed before this task was removed or renumbered.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(/usr/bin/grep -c '^### Phase 999\.46' .planning/ROADMAP.md)" -eq 1 && test "$(/usr/bin/grep -c '^### Phase 999\.47' .planning/ROADMAP.md)" -eq 1 && test ! -e .planning/todos/pending/2026-09-02-rulesets-block-stable-release-version-bump.md && for n in 40 41 42 43 44 45; do test "$(/usr/bin/grep -c "^### Phase 999\.$n" .planning/ROADMAP.md)" -eq 1 || { echo "PRE-EXISTING BACKLOG ROW LOST OR DUPLICATED: 999.$n"; exit 1; }; done && /usr/bin/grep -qE '^- \[x\] \*\*Phase 172:' .planning/ROADMAP.md && test "$(git show HEAD:.planning/ROADMAP.md | /usr/bin/grep -c '^### Phase ')" -le "$(/usr/bin/grep -c '^### Phase ' .planning/ROADMAP.md)"</automated>
    <fails_when>non-zero exit, or a `PRE-EXISTING BACKLOG ROW LOST OR DUPLICATED: 999.<n>` line — the insertion clobbered or duplicated a neighbouring row, which is the exact failure mode positional roadmap writes have already caused in this repository; or either new heading is absent or appears more than once; or the todo file still exists, meaning it was retired on intent rather than on the row existing; or Phase 172's box is still unchecked; or the total count of phase headings dropped below its value at HEAD, meaning the edit deleted content.</fails_when>
    <automated>cd /workspaces && EV=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-08-rename-sweep-targets.txt && test -s "$EV" && /usr/bin/grep -qF '/usr/bin/grep' "$EV" && /usr/bin/grep -qF 'git ls-files' "$EV" && /usr/bin/grep -qE '^total references: [0-9]+$' "$EV" && test "$(/usr/bin/grep -oE '^total references: [0-9]+$' "$EV" | /usr/bin/grep -oE '[0-9]+')" -gt 0 && for p in 169 170 172; do /usr/bin/grep -qE "(^|[^0-9])$p([^0-9]|$)" "$EV" || { echo "SWEEP LIST DOES NOT NAME PHASE: $p"; exit 1; }; done && for s in 'v135-close-procedure-under-protection.md' 'CLAUDE.md' 'MIGRATION-TABLE.md' '999.46'; do /usr/bin/grep -qF "$s" "$EV" || { echo "SWEEP LIST OMITS THIS PHASE OUTPUT: $s"; exit 1; }; done
</automated>
    <fails_when>non-zero exit, or a `SWEEP LIST DOES NOT NAME PHASE: <n>` or `SWEEP LIST OMITS THIS PHASE OUTPUT: <name>` line; or the total is zero, which would mean the scan matched nothing and is vacuous — almost certainly because bare `grep` was used and silently honoured `.gitignore`; or the evidence does not record that the scan used `/usr/bin/grep` scoped to `git ls-files`.</fails_when>
  </verify>
  <done>Both backlog rows exist in ROADMAP.md exactly once each with every neighbouring row intact, the pending todo is gone because the row now exists, Phase 172's checklist box is checked, and the rename-sweep target list is captured with a non-zero, GNU-grep-measured total naming phases 169, 170, 172 and this phase's own outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase evidence files → ledger claims | Every claim half must cross this boundary with a citation. A ledger row with no artifact behind it is the failure mode the ledger exists to prevent. |
| executor → `.planning/ROADMAP.md` | Nothing may cross. Roadmap writes are the orchestrator's; a positional write has previously clobbered an unrelated phase's dependency table. |
| the pending todo → a backlog row | The todo is retired by the row existing, not by an intention to create one. |
| the devcontainer's `grep` → sweep evidence | PATH `grep` is ugrep and honours `.gitignore`, so a repository-wide absence scan silently under-reports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-38 | Repudiation | a ledger row claiming more than its evidence supports | high | mitigate | Every row cites the artifact establishing its claim half; the gate asserts fifteen named facts are present and that every row's non-claim half bolds its negation, so a non-claim cannot be skim-read as a claim. |
| T-173-39 | Tampering | a positional roadmap write clobbering a neighbouring backlog row | high | mitigate | The insertion is the orchestrator's, by scoped edit and never by a generating verb; the gate asserts every pre-existing 999.40 through 999.45 heading still appears exactly once and that the total phase-heading count did not drop below HEAD's. |
| T-173-40 | Repudiation | an unfixed finding left as prose | high | mitigate | The findings table carries both dispositions with named filings, and the gate requires 999.46, 999.47 and 999.45 to appear in it; the two new rows must exist as headings in ROADMAP.md before the plan closes. |
| T-173-41 | Repudiation | a vacuous rename-sweep scan reporting zero | medium | mitigate | The scan must use `/usr/bin/grep` scoped to `git ls-files`, the evidence must say so, and the gate requires a non-zero total — plan 172-09 measured 0 matches under ugrep against 38 under GNU grep over identical paths. |
| T-173-42 | Tampering | editing a workflow the milestone's scope note forbids touching | medium | mitigate | 999.46 reads both release workflows to cite them and edits neither; the gate asserts an empty `git status --porcelain` on both files inside their own repositories. |
| T-173-06 | Elevation of Privilege | the `actor_id: null` deploy-key bypass | medium | accept | Carried as a ledger row inherited from Phase 172's NON-CLAIM 1 rather than silently re-classified, and 999.46's recommended remedy explicitly avoids registering a deploy key, with the reason stated. |
| T-173-08 | Elevation of Privilege | widening the personal access token's scope to fix the release path | medium | accept | Recorded in 999.46 as a rejected candidate with its reason — a token pushing as the account owner meets the same pull-request rule, because `current_user_can_bypass` is `never`. Filed, not fixed. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package; it writes Markdown records only. There is no `[ASSUMED]` or `[SUS]` package to gate. |
</threat_model>

<verification>
- `.planning/v1.35/CLOSE-RECORD.md` exists with seven sections, a ledger of at least fifteen cited rows each bolding its non-claim, and a findings table carrying both dispositions.
- The ledger carries criterion 3's three minimums verbatim in substance, Phase 172's four non-claims, POLICY-04's D-03 non-claim, FRONT-02's outright decline, and this phase's four input corrections.
- Two insertion-ready backlog bodies exist in 999.45's structure, with 999.46 citing six line-anchored workflow references and naming both rejected remedies with reasons.
- `.planning/ROADMAP.md` gains exactly one 999.46 heading and one 999.47 heading, with every pre-existing backlog row intact and the total heading count not below HEAD's.
- The pending todo is gone, and only after the row existed.
- The rename-sweep target list has a non-zero GNU-grep-measured total and names phases 169, 170, 172 and this phase's own outputs.
- Neither release workflow and no `.planning/ROADMAP.md` line was written by an executor.
</verification>

<success_criteria>
- ROADMAP criterion 3 is discharged: every claim this milestone makes sits beside its explicit non-claim, comprehensively.
- ROADMAP criterion 4 is discharged in both halves: every unfixed finding is filed rather than carried as prose, and the 999.9 rename sweep is recorded with phases 169, 170 and 172 named plus this phase's own outputs.
- D-03's shape is honoured: POLICY-04 is complete on the probe with a stated non-claim, not recorded as a deviation.
- Five evidence artifacts and one close record are committed under the phase and milestone directories.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-08-SUMMARY.md` when done.
</output>
