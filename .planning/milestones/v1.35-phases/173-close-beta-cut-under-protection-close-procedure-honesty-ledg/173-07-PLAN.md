---
phase: 173-close-beta-cut-under-protection-close-procedure-honesty-ledg
plan: 07
type: execute
wave: 2
depends_on: ["173-04"]
files_modified:
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-operator-approval.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-post-transcript.txt
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-issue-state-after.json
  - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md
autonomous: false
requirements: [POLICY-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of gh#5, gh#6, gh#7 and gh#9 carries exactly one new comment, and each comment's body is byte-identical to the draft the operator approved — posted with `--body-file`, never retyped."
    - "gh#7 and gh#6 are closed; gh#5 and gh#9 remain open — D-12's per-issue disposition."
    - "gh#9 is pinned on `henols/firestarter_prom`, taking the repository's pinned-issue count from 0 to 1, closing a gap where both criterion 5 and Backlog 999.13 called it pinned and nothing ever was."
    - "Nothing was posted before an on-disk operator approval file existed carrying its exact literal, and the gate asserts that file rather than trusting the checkpoint — D-13's blocking wording review, made mechanical."
    - "`173-UPSTREAM-REPLIES.md`'s review status is changed from PENDING to APPROVED-AND-POSTED only after the posting, and records the comment URL for each of the four issues."
    - "The pin is performed with the GraphQL `pinIssue` mutation, because the REST API has no pinned-issue endpoint."
  artifacts:
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-operator-approval.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-post-transcript.txt
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-issue-state-after.json
    - .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md
  key_links:
    - "`evidence/bodies/173-gh<n>.md` -> `gh issue comment --body-file` -> the posted comment. The approved bytes and the posted bytes are the same file, so the review cannot be bypassed by a rewrite at posting time."
    - "repository node id `R_kgDOSX4ERw` + issue node id `I_kwDOSX4ER88AAAABId5Qdw` -> `gh api graphql` `pinIssue`. REST has no equivalent, so this is the only route."
  prohibitions:
    - statement: "No comment body is composed, edited or retyped at posting time; every post reads the approved body file with `--body-file`."
      status: required
      verification: explicit
    - statement: "gh#5 and gh#9 are not closed; closing gh#5 would leave the deferred wiki content tracked only inside this repository's planning directory."
      status: required
      verification: explicit
    - statement: "Nothing is posted, closed or pinned before the on-disk approval file carries its exact literal; an approved-looking checkpoint is not authorization."
      status: required
      verification: explicit
    - statement: "No more than one comment is posted per issue; a duplicate post cannot be withdrawn cleanly because a public comment is indexed on creation."
      status: required
      verification: explicit
    - statement: "No issue other than 5, 6, 7 and 9 is commented on, closed, reopened or pinned."
      status: required
      verification: explicit
    - statement: "No file this plan writes carries a comment of any kind."
      status: required
      verification: explicit
---

<objective>
Say upstream what this milestone did, what it declined, and where the rest is tracked — after the
operator has read every word.

Purpose: ROADMAP criterion 5 owes replies on gh#7, gh#5 and gh#9, and D-12 widens deliberately to
gh#6 because it is the issue this milestone most directly delivered and because Phase 172's two
declines must be named rather than left to read as quietly skipped. D-13 puts a blocking wording
review in front of all four, on v1.22 D-02's precedent: a comment on a public tracker is indexed,
and `updatedAt` bumps on creation rather than on a body edit, so a correction reads as a second
statement rather than a replacement.

Output: four posted comments, two closures, one pin, and a review record that says what was approved.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md
@.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-04-SUMMARY.md
</context>

## Artifacts this phase produces

**New source**
- `tools/wiki/provenance_footers.py` — checker and generator; flags `--table`, `--wiki-dir`, `--emit`;
  failure words `PAGE MISSING`, `FOOTER MISSING`, `FOOTER DRIFTED`, `UNRECORDED PAGE`, `UNSAFE ROW` (plan 173-01).

**Modified source**
- `.planning/v1.35/MIGRATION-TABLE.md` (plan 173-01) · `.github/workflows/wiki-check.yml` new step
  (plan 173-06) · `.planning/config.json` keys `git.base_branch` and `git.protected_branches`
  (plan 173-02) · `/workspaces/CLAUDE.md` section `## Milestone close and branch protection`
  (plan 173-02) · `.planning/REQUIREMENTS.md` checkboxes (plan 173-09).

**New records**
- `.planning/v1.35/CLOSE-RECORD.md` (plan 173-08) · `.planning/notes/v135-close-procedure-under-protection.md`
  (plan 173-02) · `.planning/phases/173-.../173-UPSTREAM-REPLIES.md` (plan 173-04, **status flipped by this plan**) ·
  **this plan's three `evidence/173-07-*` files.**

**Orchestrator-owned ROADMAP writes, handed off and never written by an executor**
- `### Phase 999.46`, `### Phase 999.47`, and the Phase 172 / Phase 173 checklist boxes

**Out-of-repo**
- Six provenance footers on `firestarter_prom.wiki.git` (plan 173-05)
- A fourth pull request into a protected `main` (plan 173-06)
- **Four issue comments, gh#7 and gh#6 closed, gh#9 pinned (this plan).**

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: The blocking wording review — operator reads all four bodies and writes the approval file</name>
  <decision>Approve the four drafted replies for posting as written, with the dispositions gh#5 open, gh#6 close, gh#7 close, gh#9 open and pinned — or send them back, or defer the posting entirely.</decision>
  <options>
    <option id="approve">
      <name>Approve and post all four as drafted</name>
      <pros>Discharges ROADMAP criterion 5 with the thing it asks for rather than with a deferral. Says upstream what was delivered and, on gh#6, names the two declines Phase 172 handed forward so they do not read as quietly skipped. Pins gh#9, closing a gap where both criterion 5 and Backlog 999.13 called it the pinned orientation issue and prom's pinned set has always been empty.</pros>
      <cons>One-way. Four comments on a public tracker are indexed on creation; `updatedAt` bumps on creation rather than on a body edit, so a later correction reads as a second statement rather than a replacement. Two issues are closed, which is a public statement about a contributor's request.</cons>
    </option>
    <option id="revise">
      <name>Send back for rewording</name>
      <pros>Nothing public until the wording is right; this is exactly what the review is for.</pros>
      <cons>Costs a round trip; plan 173-04 must re-draft and this plan re-runs.</cons>
    </option>
    <option id="defer">
      <name>Defer the posting with a stated reason</name>
      <pros>Criterion 5's own escape hatch permits it: "sent **or explicitly deferred with a reason**".</pros>
      <cons>Meets the criterion with a deferral rather than the thing it asks for, and leaves four open issues with zero comments after a whole milestone of work on exactly their subject.</cons>
    </option>
  </options>
  <resume-signal>Write the approval file as described below, then type "approved". To send back, type "revise" and name the issues. To defer, write the defer line into the same file and type "deferred".</resume-signal>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-operator-approval.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md` — the four bodies verbatim, and the disposition table.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-draft-link-check.txt` — the per-URL resolution check, so the operator is not asked to eyeball links.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — D-12 and D-13, including the reversibility note.
  </read_first>
  <reversibility rating="one-way">A comment on a public tracker is indexed on creation, and `updatedAt` bumps on creation rather than on a body edit, so a correction reads as a second statement rather than a replacement. Two issue closures are public statements about contributors' requests.</reversibility>
  <action>
Present all four bodies to the operator **in full and verbatim** — not summarized, not paraphrased.
The whole value of D-13 is that the operator reads the words that will be public. Alongside each,
state its disposition and the one thing that makes it contentious:

- **gh#7 — reply and close.** Contentious because closing it is a public statement that a
  contributor's premise was rejected. The reply says why, and says the content requirements survive
  on gh#5.
- **gh#5 — reply, stays open.** Contentious because it names five things as deferred and promises no
  date.
- **gh#6 — reply and close.** Contentious because it names two declines. This widening is D-12's,
  not criterion 5's, and it exists so the declines do not read as quietly delivered.
- **gh#9 — reply, stays open, gets pinned.** Contentious because the pin is a repository-level
  change and because the reply describes an end state whose CI guard has not yet been observed
  running.

Also put the link-check evidence in front of the operator rather than asking them to check links.

On approval, write `evidence/173-07-operator-approval.txt` containing a line reading exactly
`APPROVED-FOR-POST: gh5 gh6 gh7 gh9`, the operator's name, the date, one sentence in the operator's
own words, and the sha256 of each of the four body files as approved. **Tasks 2 and 3 assert this
file, its exact literal, and those four hashes against the files they are about to post.** An
auto-approved gate writes no file and cannot produce hashes, so nothing can be posted without a
human act — `autonomous: false` alone is known not to be self-protecting in this project.

On a revise, record which issues need rework and halt; plan 173-04 re-drafts.

On a defer, write `DEFERRED-FOR-POST:` and the reason into the same file, halt this plan, and record
in plan 173-08's ledger that criterion 5 was met by explicit deferral rather than by sending —
which criterion 5's own wording permits, and which must be stated rather than blurred.
  </action>
  <acceptance_criteria>
    - `evidence/173-07-operator-approval.txt` exists and carries either the exact line `APPROVED-FOR-POST: gh5 gh6 gh7 gh9` or a line beginning `DEFERRED-FOR-POST:`.
    - On approval it carries four sha256 lines, one per body file, each matching that file's current content.
    - The file names the operator and carries a date.
    - All four issues still have zero comments at the time this task completes.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && F=$P/evidence/173-07-operator-approval.txt && test -s "$F" && { /usr/bin/grep -qx 'APPROVED-FOR-POST: gh5 gh6 gh7 gh9' "$F" || /usr/bin/grep -q '^DEFERRED-FOR-POST:' "$F"; } && /usr/bin/grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$F" && if /usr/bin/grep -qx 'APPROVED-FOR-POST: gh5 gh6 gh7 gh9' "$F"; then for n in 5 6 7 9; do H=$(sha256sum < "$P/evidence/bodies/173-gh$n.md" | cut -c1-64); /usr/bin/grep -qF "$H" "$F" || { echo "HASH NOT APPROVED: gh#$n"; exit 1; }; done; fi && for n in 5 6 7 9; do test "$(gh api repos/henols/firestarter_prom/issues/$n --jq .comments)" = '0' || { echo "POSTED BEFORE REVIEW: gh#$n"; exit 1; }; done
</automated>
    <fails_when>non-zero exit; or the approval file is absent or empty, which is the state an auto-approved checkpoint leaves and which must block Tasks 2 and 3; or it carries neither literal; or a `HASH NOT APPROVED: gh#<n>` line, meaning a body file changed after the operator read it and the approval no longer covers what would be posted; or a `POSTED BEFORE REVIEW: gh#<n>` line, meaning a comment reached a public tracker before the blocking review.</fails_when>
  </verify>
  <done>An on-disk approval or deferral exists, written by a human, hash-bound to the exact four bodies, and all four issues still have zero comments.</done>
</task>

<task type="auto">
  <name>Task 2: Post the four approved bodies, byte for byte, and close the two issues D-12 closes</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-post-transcript.txt</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-operator-approval.txt` — Task 1's output, including the four approved hashes. This task must not run without them.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/bodies/` — the four body files; these are the payload, not a reference for re-composition.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-issue-state-before.json` — the before-half this task's after-capture is compared against.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-CONTEXT.md` — D-12's per-issue disposition, including that gh#5 and gh#9 stay open.
  </read_first>
  <precondition>`evidence/173-07-operator-approval.txt` exists, contains the exact line `APPROVED-FOR-POST: gh5 gh6 gh7 gh9`, and carries a sha256 matching each of the four body files as they stand now. Halt without posting if any of that is untrue.</precondition>
  <action>
Assert the approval file, its exact literal, and all four hashes against the body files as they
stand right now — before anything is posted. If a body file has changed since the operator read it,
the approval does not cover it; stop and return to Task 1. The file and its hashes are the
authorization; an approved-looking checkpoint is not.

Post each body with `gh issue comment` against `henols/firestarter_prom`, passing the body file
with `--body-file`. **Do not compose, edit, reformat or retype a body at posting time.** The point
of the review is that the approved bytes and the posted bytes are the same bytes; recomposing here
would silently void D-13.

Post exactly one comment per issue, and post to exactly the four issues 5, 6, 7 and 9. A duplicate
comment cannot be withdrawn cleanly — it is indexed on creation — so re-check the comment count for
an issue before posting to it, and skip any issue that already carries a comment from this phase
rather than posting a second.

Then close gh#7 and gh#6, in that order, each with a closing comment already covered by the body
just posted rather than a second body. Leave gh#5 and gh#9 open. Closing gh#5 would leave the
compatibility matrix, family pages, algorithm pages and tutorials tracked only inside this
repository's planning directory, which is the opposite of what deferred-not-delivered means.

Capture into the transcript, per issue: the command issued (with the body file path, showing that
`--body-file` was used), the returned comment URL, the comment id, and the resulting state. Then
record the closure of gh#7 and gh#6 with their `closedAt` timestamps.

Touch no other issue in the repository. Nothing here reopens, relabels or edits an existing comment.
  </action>
  <acceptance_criteria>
    - Each of issues 5, 6, 7 and 9 has exactly one comment.
    - Each comment's body is byte-identical to the corresponding approved body file.
    - gh#7 and gh#6 are `closed`; gh#5 and gh#9 are `open`.
    - The transcript records a comment URL and a comment id for each of the four issues, and shows `--body-file` in every posting command.
    - No issue other than 5, 6, 7 and 9 gained a comment during this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && F=$P/evidence/173-07-operator-approval.txt && EV=$P/evidence/173-07-post-transcript.txt && /usr/bin/grep -qx 'APPROVED-FOR-POST: gh5 gh6 gh7 gh9' "$F" && test -s "$EV" && for n in 5 6 7 9; do test "$(gh api repos/henols/firestarter_prom/issues/$n --jq .comments)" = '1' || { echo "WRONG COMMENT COUNT: gh#$n"; exit 1; }; gh api "repos/henols/firestarter_prom/issues/$n/comments" --jq '.[0].body' > /tmp/posted-$n.md; python3 -c "
import pathlib,sys
a=pathlib.Path('/tmp/posted-$n.md').read_text().replace('\r\n','\n').strip()
b=pathlib.Path('$P/evidence/bodies/173-gh$n.md').read_text().replace('\r\n','\n').strip()
sys.exit(0 if a==b else 1)" || { echo "POSTED BODY DIFFERS FROM APPROVED: gh#$n"; exit 1; }; /usr/bin/grep -qF -- "--body-file" "$EV" || { echo "BODY NOT POSTED FROM FILE"; exit 1; }; done && test "$(gh api repos/henols/firestarter_prom/issues/7 --jq .state)" = 'closed' && test "$(gh api repos/henols/firestarter_prom/issues/6 --jq .state)" = 'closed' && test "$(gh api repos/henols/firestarter_prom/issues/5 --jq .state)" = 'open' && test "$(gh api repos/henols/firestarter_prom/issues/9 --jq .state)" = 'open' && test "$(/usr/bin/grep -cE 'https://github.com/henols/firestarter_prom/issues/[5679]#issuecomment-[0-9]+' "$EV")" -ge 4
</automated>
    <fails_when>non-zero exit, or any diagnostic line. `POSTED BODY DIFFERS FROM APPROVED: gh#<n>` is the one that voids D-13: the operator reviewed one text and a different one is public. `WRONG COMMENT COUNT: gh#<n>` catches both a missing post and a duplicate, and a duplicate cannot be withdrawn cleanly. A state mismatch means a disposition was applied to the wrong issue — closing gh#5 in particular would strand the deferred wiki content upstream.</fails_when>
    <automated>cd /workspaces && python3 -c "
import json,subprocess
out=subprocess.run(['gh','api','repos/henols/firestarter_prom/issues?state=all&per_page=100','--jq','[.[]|{n:.number,c:.comments}]'],capture_output=True,text=True)
rows=json.loads(out.stdout)
stray=[r for r in rows if r['n'] not in (5,6,7,9) and r['c']>0]
print('issues with comments outside the four:', stray)
assert not stray, stray
print('no collateral comments')"</automated>
    <fails_when>non-zero exit, or the printed list of issues with comments outside 5, 6, 7 and 9 is non-empty — this plan commented on an issue it has no business touching, and a public comment cannot be withdrawn cleanly.</fails_when>
  </verify>
  <done>Four comments are public, each byte-identical to the body the operator approved and each posted from that file; gh#7 and gh#6 are closed; gh#5 and gh#9 are open; no other issue in the repository was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Pin gh#9 by the only API that can, then capture the after-state and flip the review record</name>
  <files>.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-07-issue-state-after.json, .planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md</files>
  <read_first>
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-RESEARCH.md` — §"Pinning gh#9 (D-12) — GraphQL only", with the node ids measured this session: repository `R_kgDOSX4ERw`, issue 9 `I_kwDOSX4ER88AAAABId5Qdw`, and the current `pinnedIssues` state of `totalCount: 0, nodes: []`.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/evidence/173-04-issue-state-before.json` — the before-half, including the pinned count of 0.
    - `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-UPSTREAM-REPLIES.md` — the review record whose status this task flips.
  </read_first>
  <precondition>`evidence/173-07-operator-approval.txt` contains the exact line `APPROVED-FOR-POST: gh5 gh6 gh7 gh9`, and gh#9 already carries its posted comment from Task 2. Halt without pinning if either is untrue.</precondition>
  <action>
Pin gh#9 on `henols/firestarter_prom` with the GraphQL `pinIssue` mutation. REST has no
pinned-issue endpoint, so this is the only route; do not look for one.

Re-resolve both node ids from the API at run time rather than pasting the measured values — the ids
in RESEARCH.md are a cross-check, not the input. Confirm the resolved issue node id matches
`I_kwDOSX4ER88AAAABId5Qdw` and the repository node id matches `R_kgDOSX4ERw`; a mismatch means the
mutation is about to be aimed at the wrong object and must stop.

This closes a gap that measurement found rather than reading: both ROADMAP criterion 5 and Backlog
999.13's note call gh#9 "the pinned orientation issue", and prom's pinned-issue set has always been
empty. Pinning was intended and never done.

Then capture the complete after-state into `evidence/173-07-issue-state-after.json` in the same
shape as plan 173-04's before-capture: state, comment count, title and URL for each of 5, 6, 7 and 9,
the comment URL and id for each, plus the repository's pinned-issue count and the number of the
pinned issue. Read it back from the API, never from a web page.

Finally, edit `173-UPSTREAM-REPLIES.md`: change the review status from `PENDING OPERATOR REVIEW` to
`APPROVED AND POSTED`, add the approval date and the operator's name, and add the comment URL beside
each of the four bodies. The record then says what was approved and where it landed, which is how
criterion 5's "sent" is evidenced. Change no body text — the bodies are now public and the record
must continue to match them.

Hand the four comment URLs forward to plan 173-08: they join the set of this phase's outputs that
Backlog 999.9's repository rename will invalidate, and criterion 4 owes that set a record.
  </action>
  <acceptance_criteria>
    - `henols/firestarter_prom` has a pinned-issue count of 1, and the pinned issue is number 9.
    - `evidence/173-07-issue-state-after.json` carries all four issues with their state, comment count and comment URL, plus the pinned count and pinned issue number.
    - Compared with plan 173-04's before-capture: every comment count went 0 to 1; gh#7 and gh#6 went open to closed; gh#5 and gh#9 stayed open; the pinned count went 0 to 1.
    - `173-UPSTREAM-REPLIES.md` no longer contains `PENDING OPERATOR REVIEW`, contains `APPROVED AND POSTED`, and carries a comment URL for each of the four issues.
    - The four body texts inside `173-UPSTREAM-REPLIES.md` are unchanged from what was posted.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && A=$P/evidence/173-07-issue-state-after.json && B=$P/evidence/173-04-issue-state-before.json && test -s "$A" && PIN=$(gh api graphql -f query='{repository(owner:"henols",name:"firestarter_prom"){pinnedIssues(first:10){totalCount nodes{issue{number}}}}}' --jq '.data.repository.pinnedIssues') && echo "$PIN" | python3 -c "
import json,sys
d=json.load(sys.stdin)
assert d['totalCount']==1, ('PINNED COUNT', d['totalCount'])
nums=[n['issue']['number'] for n in d['nodes']]
assert nums==[9], ('PINNED ISSUE', nums)
print('gh#9 pinned')" && python3 -c "
import json
a=json.load(open('$B')); b=json.load(open('$A'))
ai=a['issues'] if 'issues' in a else a
bi=b['issues'] if 'issues' in b else b
for k in ('5','6','7','9'):
    assert int(ai[k]['comments'])==0 and int(bi[k]['comments'])==1, ('COMMENT DELTA', k, ai[k]['comments'], bi[k]['comments'])
    assert bi[k].get('comment_url','').startswith('https://'), ('NO COMMENT URL', k)
assert bi['7']['state'].lower()=='closed' and bi['6']['state'].lower()=='closed', 'CLOSURE MISSING'
assert bi['5']['state'].lower()=='open' and bi['9']['state'].lower()=='open', 'WRONGLY CLOSED'
assert int(a.get('pinned_issue_count',-1))==0 and int(b.get('pinned_issue_count',-1))==1, 'PIN DELTA'
print('before/after delta OK')"</automated>
    <fails_when>non-zero exit, or any printed tuple. `PINNED COUNT` or `PINNED ISSUE` other than 1 and `[9]` means the mutation aimed at the wrong object or did not run. `COMMENT DELTA` names an issue whose count did not go from 0 to exactly 1. `WRONGLY CLOSED` means gh#5 or gh#9 was closed, which would strand the deferred content and the orientation issue.</fails_when>
    <automated>cd /workspaces && P=.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg && R=$P/173-UPSTREAM-REPLIES.md && ! /usr/bin/grep -qF 'PENDING OPERATOR REVIEW' "$R" && /usr/bin/grep -qF 'APPROVED AND POSTED' "$R" && test "$(/usr/bin/grep -cE 'https://github.com/henols/firestarter_prom/issues/[5679]#issuecomment-[0-9]+' "$R")" -ge 4 && for n in 5 6 7 9; do python3 -c "
import pathlib,sys
body=pathlib.Path('$P/evidence/bodies/173-gh$n.md').read_text().strip()
sys.exit(0 if body in pathlib.Path('$R').read_text() else 1)" || { echo "RECORD NO LONGER MATCHES POSTED BODY: gh#$n"; exit 1; }; done
</automated>
    <fails_when>non-zero exit; or `PENDING OPERATOR REVIEW` is still present, meaning the record was never flipped and the phase would close claiming an unfinished review; or `APPROVED AND POSTED` is absent; or fewer than four comment URLs appear in the record, so "sent" is claimed without a landing place; or a `RECORD NO LONGER MATCHES POSTED BODY: gh#<n>` line, meaning the record was edited away from the public text and no longer evidences what was approved.</fails_when>
  </verify>
  <done>gh#9 is pinned, taking prom's pinned-issue count from 0 to 1; the after-state is captured from the API and reconciles against plan 173-04's before-capture on every field; and the review record reads APPROVED AND POSTED with a comment URL beside each unchanged body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved draft bytes → posted comment bytes | If these can differ, the blocking review guarantees nothing. |
| operator → the phase evidence directory | The approval file, hash-bound to the four bodies, is the authorization. An approved-looking checkpoint is not, because auto-advance modes approve checkpoints. |
| this plan → every other issue in the repository | Only 5, 6, 7 and 9 may be touched; a comment anywhere else is indexed and cannot be withdrawn cleanly. |
| GraphQL `pinIssue` → the repository's pinned set | A repository-level change aimed by node id, where a wrong id targets the wrong object. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-173-05 | Repudiation | four public comments posted without review | high | mitigate | D-13's blocking wording review made mechanical: an on-disk approval file carrying an exact literal **and** a sha256 per body, asserted against the files immediately before posting. An auto-approved gate writes no file and produces no hashes. |
| T-173-33 | Tampering | the reviewed text and the posted text diverging | high | mitigate | Every post uses `--body-file` against the approved file; the gate then re-reads each posted comment from the API and compares it to the body file byte for byte after newline normalization. |
| T-173-34 | Repudiation | a duplicate comment that cannot be withdrawn | high | mitigate | The comment count is re-checked before posting to each issue, and the gate requires exactly 1 per issue afterwards. |
| T-173-35 | Tampering | a comment or closure landing on an unintended issue | medium | mitigate | A repository-wide sweep asserts that no issue outside 5, 6, 7 and 9 carries a comment, and per-issue state assertions require gh#5 and gh#9 to remain open. |
| T-173-36 | Spoofing | the `pinIssue` mutation aimed at the wrong node | medium | mitigate | Both node ids are re-resolved at run time and cross-checked against the values measured in RESEARCH.md before the mutation runs; the read-back requires a pinned count of exactly 1 naming issue 9. |
| T-173-37 | Repudiation | a record that no longer evidences what was approved | medium | mitigate | The gate asserts each body file's text still appears verbatim inside `173-UPSTREAM-REPLIES.md` after the status flip, and that the record carries a comment URL per issue. |
| T-173-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no external package. It uses `gh` (REST and GraphQL) and `python3`, both already present. There is no `[ASSUMED]` or `[SUS]` package to gate. |
</threat_model>

<verification>
- An on-disk approval, hash-bound to the four bodies, precedes any post, and the gate asserts it.
- Each of gh#5, gh#6, gh#7 and gh#9 carries exactly one comment, byte-identical to its approved body.
- gh#7 and gh#6 are closed; gh#5 and gh#9 are open.
- gh#9 is pinned; prom's pinned-issue count is 1 and names issue 9.
- No issue outside the four carries a comment.
- The before and after captures reconcile on every field: comment counts 0 to 1, two closures, two left open, pinned count 0 to 1.
- `173-UPSTREAM-REPLIES.md` reads APPROVED AND POSTED, carries four comment URLs, and its four bodies still match the public text.
</verification>

<success_criteria>
- ROADMAP criterion 5 is discharged by sending rather than by deferral, on all four issues including D-12's deliberate widening to gh#6.
- Phase 172's two declines are named in public text rather than left to read as quietly delivered.
- The pin both criterion 5 and Backlog 999.13 assumed existed now actually exists.
- What the operator approved and what the public reads are the same bytes, provably.
- Three evidence files plus the flipped review record are committed under the phase directory.
</success_criteria>

<output>
Create `.planning/phases/173-close-beta-cut-under-protection-close-procedure-honesty-ledg/173-07-SUMMARY.md` when done.
</output>
