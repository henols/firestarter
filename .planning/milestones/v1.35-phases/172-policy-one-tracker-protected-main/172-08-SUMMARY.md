---
phase: 172-policy-one-tracker-protected-main
plan: 08
subsystem: infra
tags: [github, pull-request, rulesets, branch-protection, issue-templates, ci, wiki-check]

# Dependency graph
requires:
  - phase: 172-policy-one-tracker-protected-main
    provides: "plan 172-07's three open, `.github`-scoped pull requests and its measured Pitfall 5 verdict, both re-read from evidence before Task 1 acted"
provides:
  - "POLICY-02's four issue templates live on `henols/firestarter_prom`'s default branch — the only place GitHub reads the New Issue chooser from"
  - "`.github/CONTRIBUTING.md` live on all three default branches, firing GitHub's native contribution affordance wherever a pull request is opened"
  - "LEGACY-01's `wiki-check.yml` both present at `?ref=main` and REGISTERED with Actions, which is what finally gives the wiki any CI at all"
  - "The first live demonstration that PR-only `main` works — three ordinary merges under active rulesets, never bypassed — which is the observation Phase 173's POLICY-05 documents"
  - "A recorded PREFILLED verdict on the `dev test --submit` browser tier, closing D-07's open question"
affects: [172-09, 173-policy-04, 173-policy-05]

# Actuals (#2633)
actuals:
  tokens: 2400
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "recording a PRE-MERGE absence baseline so a post-merge presence read is evidence rather than a coincidence — the plan asserted only presence"
    - "measuring a merge's METHOD from its commit parent count rather than trusting the flag that was typed"
    - "running a plan's own assertions AGAINST hand-written evidence instead of running verify legs that open with `: > $EV` and would truncate it"
    - "discharging a product-code prohibition by blob-sha equality across the phase rather than by the absence of an observed reason to edit"

key-files:
  created:
    - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-pre-merge-baseline.txt
    - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-merge-record.txt
    - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-post-merge-surfaces.txt
    - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-prefill-check.txt
  modified: []

key-decisions:
  - "A pre-merge baseline was added that the plan did not ask for. Its two automated legs assert only that the six surfaces are PRESENT at ?ref=main, which cannot distinguish 'the merge put it there' from 'it was already there and the merge proved nothing'. All six were read first and all six returned 404, with prom's workflow list holding 'Catalog sync check' alone. Every post-merge read in this plan is therefore a paired observation."
  - "The merge method was measured, not assumed: each merge commit was read back and has TWO parents, so all three were true `--merge` commits rather than squashes or rebases. Firmware's second parent 579f04a is the same head sha 172-07 recorded, so the commit whose empty check-run list produced the Pitfall 5 verdict is provably the commit that was merged."
  - "The no-release-cut assertion was strengthened beyond the plan's specification. The plan wanted the latest release's published_at to be strictly earlier than the firmware mergedAt; what is recorded is that the release is byte-IDENTICAL to the pre-merge baseline (2.0.6, 2025-11-16T13:50:02Z) AND that the most recent workflow run of any kind on firestarter's main is from November 2025 — today's merge triggered nothing at all. The verdict is confirmed by outcome, not only by prediction."
  - "The plan's own verify legs were NOT run verbatim. Both open with `: > \"$EV\"`, which would have truncated the richer hand-written evidence to their own minimal line set. The evidence was written first and the plan's assertions then run against it — all seven leg-1 assertions and both leg-2 counts pass AS AUTHORED. This is why the ruleset block carries three bare `ruleset_X=1:active` lines with the id/bypass/can_bypass detail on separate lines beneath: the plan's grep is end-anchored (`^ruleset_.*=1:active$`) and richer lines on the same line would have failed a gate that should pass."
  - "A stale acceptance criterion in the plan itself was recorded as stale rather than reconciled. 172-08-PLAN.md and T-172-32 both expect 'the single Integration/15368 bypass'; 172-05 revised D-09 to a DeployKey after the Integration form returned 422, and both 172-06 and this plan read back DeployKey:null:always on all three. The plan's automated legs never inspect the actor so nothing needed re-mechanising — the prose is what is wrong, and the control plane was NOT reconfigured to match a superseded decision."
  - "`dev test --submit` was deliberately not run to produce the prefill probe. The plan permits either route; the URL was instead generated by calling submit.py's own build_issue_url, which makes it provably the same shape rather than a lookalike, at none of the cost of a bench run and with no risk of filing a real issue in the public tracker as a side effect of a test."
  - "No requirement IDs were marked complete. Plan 172-09 declares all four of POLICY-01, POLICY-02, POLICY-03 and LEGACY-01 and has produced no SUMMARY.md, so every one of this plan's three declared IDs is still owed by a sibling plan. Marking them here is the premature-completion failure mode; they are left for 172-09, which owns pairing each mark with its evidence."

requirements-completed: []

coverage:
  - id: D1
    description: "All four issue templates present on prom's default branch, read from the only path GitHub consults"
    requirement: "POLICY-02"
    verification:
      - kind: other
        ref: "evidence/172-08-post-merge-surfaces.txt (`templates=` line, GET contents/.github/ISSUE_TEMPLATE?ref=main) paired against evidence/172-08-pre-merge-baseline.txt (404)"
        status: pass
    human_judgment: false
  - id: D2
    description: "`.github/CONTRIBUTING.md` present on all three default branches"
    requirement: "POLICY-02"
    verification:
      - kind: other
        ref: "evidence/172-08-post-merge-surfaces.txt (three `contributing_` lines) paired against the baseline's three 404s"
        status: pass
    human_judgment: false
  - id: D3
    description: "`wiki-check.yml` on prom's default branch AND registered with Actions — two separate facts, both recorded"
    requirement: "LEGACY-01"
    verification:
      - kind: other
        ref: "evidence/172-08-post-merge-surfaces.txt (`workflow_file=wiki-check.yml`, `workflow_registered=Catalog sync check,Wiki check`) paired against the baseline's 404 and single-workflow list"
        status: pass
    human_judgment: false
  - id: D4
    description: "Three pull requests reached MERGED through the ordinary pull-request route with the protection active and undisturbed"
    requirement: "POLICY-03"
    verification:
      - kind: other
        ref: "evidence/172-08-merge-record.txt (three state=MERGED lines, two-parent proof per merge commit) + evidence/172-08-post-merge-surfaces.txt (three rulesets still 1:active, ids and bypass lists unchanged, can_bypass=never before and after)"
        status: pass
    human_judgment: false
  - id: D5
    description: "No stable firmware release was cut by the firmware merge"
    requirement: "POLICY-03"
    verification:
      - kind: other
        ref: "evidence/172-08-merge-record.txt (release unchanged from baseline at 2.0.6/2025-11-16T13:50:02Z; most recent main workflow run of any kind dated 2025-11-16)"
        status: pass
    human_judgment: false
  - id: D6
    description: "The browser prefill question answered from a logged-in browser and its outcome recorded either way"
    requirement: "POLICY-02"
    verification:
      - kind: other
        ref: "evidence/172-08-prefill-check.txt (operator observation: PREFILLED; probe URL generated by submit.py's own build_issue_url; submit.py blob 179af6ed identical across the phase)"
        status: pass
    human_judgment: true
    rationale: "Only a logged-in browser can see whether /issues/new with query parameters bypasses the chooser once templates exist; unauthenticated probes return a login redirect that is consistent with either answer. The operator's observation is the evidence, and the prohibition fencing it is discharged separately by blob sha."

duration: ~20min active (Task 1 gated on operator authorization, Task 3 on an operator browser observation)
completed: 2026-09-02
status: complete
---

# Phase 172 Plan 08: Merge the three pull requests and prove the surfaces live Summary

**Three `.github`-only pull requests merged through the ordinary route into three protected default branches (`firestarter_prom#54`, `firestarter#58`, `firestarter_app#57`), every surface they carry read back live at `?ref=main`, protection re-read and undisturbed, no firmware release cut — and the `dev test --submit` browser tier confirmed PREFILLED, closing D-07.**

## Performance

- **Duration:** ~20 min of active execution across two operator gates
- **Completed:** 2026-09-02
- **Tasks:** 3 (Task 1 `checkpoint:human-action`, Task 2 auto, Task 3 `checkpoint:human-verify`)
- **Files modified:** 4 created (all evidence files, meta repo only)

## Accomplishments
- Merged all three pull requests through the pull-request route under active rulesets, in the plan's prescribed order (prom first, so the `wiki-check.yml` registration was visible before the two sub-repositories moved), inside an 8-second window
- Proved POLICY-02's four templates, POLICY-01's three `CONTRIBUTING.md` files and LEGACY-01's `wiki-check.yml` all live on their default branches — the only place GitHub reads any of them from
- Got `Wiki check` REGISTERED with Actions, closing the standing finding that `wiki-check.yml` was not registered and that the wiki had no CI at all
- Confirmed the Pitfall 5 prediction by outcome: the latest firmware release is unchanged from the pre-merge baseline and today's merge triggered no workflow run of any kind
- Confirmed T-172-32 negatively: all three rulesets still active with unchanged ids and bypass lists, and `current_user_can_bypass=never` both before and after, so the forbidden override was never even available
- Closed D-07's open question with an operator browser observation: **PREFILLED**

## Task Commits

1. **Pre-merge baseline** (not a plan task — added on judgment) - `63480f25` (docs)
2. **Task 1: `checkpoint:human-action`** — no commit (gate only); operator performed the three merges after authorization
3. **Task 2: Read every merged surface back from the default branches** - `60c9a829` (docs)
4. **Task 3: `checkpoint:human-verify`** - `5bfa3d33` (docs) — operator reported PREFILLED

**Plan metadata:** captured in this SUMMARY's own commit.

## Files Created/Modified
- `evidence/172-08-pre-merge-baseline.txt` - the six 404s and the single-workflow list, taken with all three PRs still OPEN, plus the pre-merge release value and `can_bypass=never` ×3; not called for by the plan
- `evidence/172-08-merge-record.txt` - three PR numbers, states, `mergedAt` timestamps, merge commit shas and head refs; the two-parent method proof; the release assertion; and the POLICY-05 hand-forward
- `evidence/172-08-post-merge-surfaces.txt` - every default-branch read-back, the Phase 168 attribution line, the three-ruleset re-read, and two honest observations (prom's pre-existing red; `Wiki check` registered-but-never-run)
- `evidence/172-08-prefill-check.txt` - the PREFILLED outcome, the exact probe URL and how it was generated, why `--submit` was not run, and the blob-sha discharge of the product-code prohibition

Outside meta (GitHub-side state only): three merge commits on three public default branches — `71148eda` (prom), `135bf9a3` (firestarter), `1625aef6` (app).

## Decisions Made
See `key-decisions` in frontmatter. In summary: a pre-merge baseline was added to make the plan's presence-only assertions non-vacuous; the merge method and the no-release-cut claim were both measured beyond what the plan specified; the plan's truncating verify legs were run as assertions against hand-written evidence rather than verbatim; a stale acceptance criterion in the plan was recorded as stale rather than reconciled by reconfiguring the control plane; and all four requirement IDs were left unmarked for 172-09.

## Deviations from Plan

### Auto-fixed Issues

None — nothing required fixing during execution.

### Documented Non-Fixes (out of scope, not auto-fixed)

**1. [Stale plan text] The plan's expected ruleset bypass actor is a superseded decision**
- **Found during:** Task 2
- **Issue:** `172-08-PLAN.md`'s acceptance criteria and threat register (T-172-32) both name "the single `Integration`/15368 bypass" as the expected state.
- **Root cause:** Plan 172-05 revised D-09 to a `DeployKey` actor after the Integration form returned HTTP 422; 172-06's read-back records `DeployKey:null:always` on all three repositories. Plan 08 was authored before that revision.
- **Fix:** None applied to the control plane, deliberately. Reconfiguring three live rulesets to match a superseded decision so a stale criterion would read true is the wrong direction. The plan's two automated legs assert only `1:active` and never inspect the actor, so nothing needed re-mechanising.
- **Verification:** recorded in both `172-08-pre-merge-baseline.txt` and `172-08-post-merge-surfaces.txt`; the actual actor is read back in both.
- **Committed in:** `63480f25`, `60c9a829`

**2. [Pre-existing, outside a documentation milestone] Prom's default branch is already red**
- **Found during:** Task 2
- **Issue:** The most recent `Catalog sync check` run on prom's `main` is a FAILURE.
- **Root cause:** It sits on `ad08a06` — the FIRST PARENT of today's prom merge commit, i.e. the previous tip of `main` — dated 2026-08-31T22:47:56Z, belonging to pull request #34, a v1.31 merge. It predates these merges by two days.
- **Fix:** None. CI repair is product work and outside a documentation-and-configuration milestone. Recorded so that a reader finding prom's `main` red does not attribute it to this phase, and flagged as a candidate for the milestone's success-criterion-4 filing.
- **Committed in:** `60c9a829`

**3. [Pre-existing, filed] `firestarter_app/tools/build_db.py` is still dirty**
- **Found during:** carried in from 172-07, which documented the same non-fix
- **Issue:** An uncommitted, unattributed rename (`_AT28C_DIP24_NAMES` → `AT28C_DIP24_NAMES`).
- **Root cause:** Unclaimed by any artifact in Phases 170–172; product source, which this milestone's scope note forbids editing.
- **Fix:** None. Filed instead as **Backlog 999.45** (commit `2e0a3629`), which also records a second, pre-existing defect the investigation exposed: the docstring citation `build_db.py:594` has been stale by ~49 lines since the v1.33 Phase 154 comment sweep.
- **Committed in:** `2e0a3629` (the backlog item; the working-tree change itself remains uncommitted by design)

**Total deviations:** 0 auto-fixed, 3 documented non-fixes. **Impact:** none on this plan's deliverables; two were pre-existing and one is stale plan prose that no automated gate depends on.

## Authentication Gates

None newly required. `gh` was already authenticated as `henols` throughout. The three merges themselves were performed by the operator after the harness classifier declined `gh pr merge` from the agent — recorded not as a defect but as the guard behaving correctly for a merge into three public default branches.

## Known Stubs

`Wiki check` is registered but has **zero runs**; its first fire is the weekly cron or a manual `workflow_dispatch`. Its confidence rests on plan 172-03 having run all three legs green against fresh clones under the fixed invocation before the workflow was carried into the pull request. Registered-and-never-run is its honest state, and 172-09 is the plan that exercises the checkers against fresh clones.

## Threat Flags

None new. This plan's five listed threats are exactly what its evidence discharges: T-172-32 (admin override) by three unchanged active rulesets plus `can_bypass=never` before and after; T-172-28 (unintended release) by an unchanged release value and a total absence of workflow runs; T-172-33 (claiming POLICY-02 from a feature branch) by reading every surface at `?ref=main` and pairing it with a 404 baseline; T-172-13 (prefill loss) by the operator's PREFILLED observation; T-172-34 (a red first cron) carried forward as the Known Stub above.

## Self-Check: PASSED

- All four evidence files present → FOUND
- `git log --oneline --grep="172-08"` → FOUND (`63480f25`, `60c9a829`, `5bfa3d33`)
- Task 2's leg-1 assertions re-run against the evidence file: `templates` ✓, three `contributing_` ✓, `workflow_file` ✓, `workflow_registered` contains `Wiki check` ✓, `grep -c '^ruleset_.*=1:active$'` = 3 ✓ — seven of seven
- Task 2's leg-2 assertions re-run: `state=MERGED` = 3 ✓, `head=policy/contributor-policy` = 3 ✓, `2025-11-16T13:50:02Z < 2026-09-02T08:57:02Z` ✓
- Plan-level `<verification>` re-checked line by line: four template filenames ✓; `CONTRIBUTING.md` ×3 ✓; `wiki-check.yml` present and registered ✓; three MERGED with the right head ✓; three rulesets `1:active` ✓; release predates the merge ✓; prefill outcome recorded ✓
- Both prohibitions discharged: no product-code change made in response to any finding (`submit.py` blob `179af6ed` identical across the phase; the one product-source item found was FILED as Backlog 999.45, not fixed), and no merge used an override, force-push or disabled ruleset (two-parent merge commits, three unchanged active rulesets, `can_bypass=never`)

Next: Phase 172 has one plan left, **172-09** — the closing sweep: three checkers green against fresh clones, the three-way ruleset equality, both gitlinks re-pinned, and the four requirement marks paired with their evidence. All four of POLICY-01, POLICY-02, POLICY-03 and LEGACY-01 are still owed and are 172-09's to mark.
