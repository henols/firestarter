---
phase: 172-policy-one-tracker-protected-main
plan: 05
type: execute
wave: 3
depends_on: [172-01, 172-02, 172-03, 172-04]
commits_land_in: firestarter_prom (meta, /workspaces) on gsd/v1.35-documentation-consolidation-wiki-migration — plus one live ruleset created on henols/firestarter_prom through the GitHub REST API
files_modified:
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-body.json
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-4998759-pre-delete.json
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-prom-canary-readback.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-actions-bypass-probe.txt
autonomous: false
requirements: [POLICY-03]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'The full JSON of ruleset `4998759` is captured into phase evidence and parses with `.id == 4998759` before any DELETE is issued — the GitHub API has no undelete, so this file is the only record of what was there'
    - 'One canonical ruleset body exists as a committed file, and every one of the three POSTs in this phase reads that same file — three rulesets born from one body is what makes a three-way equality meaningful'
    - '`henols/firestarter_prom` carries exactly one ruleset after this plan, with `enforcement: active`, and `henols/firestarter` and `henols/firestarter_app` are untouched — the canary proves the API accepts this body on a user-owned repository before anything irreversible happens'
    - 'The read-back confirms GitHub actually stored the `Integration`/15368 bypass rather than silently dropping it; a three-way equality alone would not catch a silent drop, because all three would drop it identically'
    - 'The premise that a push authenticated by the default `GITHUB_TOKEN` is covered by that bypass has been probed against a throwaway repository, or the probe has been explicitly declined and the risk recorded — it is not carried past this point unexamined'
  artifacts:
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-4998759-pre-delete.json"
      provides: "The only surviving record of the incumbent ruleset once plan 172-06 deletes it — its id, its 2025-04-22 creation history, its rules and its dead DeployKey bypass"
      min_lines: 1
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-body.json"
      provides: "The one canonical POST body all three rulesets are created from, committed so the three-way equality in plan 172-06 has a single named source"
      min_lines: 20
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-prom-canary-readback.txt"
      provides: "The canary read-back proving the API accepts an Integration bypass actor on a user-owned repository and stores it rather than dropping it"
      min_lines: 6
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-actions-bypass-probe.txt"
      provides: "The disposition of D-09's premise — the probe result, or the explicit decision not to probe and what that costs"
      min_lines: 4
  key_links:
    - from: "`evidence/172-05-ruleset-body.json`"
      to: "the three `POST /repos/{owner}/{repo}/rulesets` calls"
      via: "`gh api --method POST --input <that file>`, the same file every time"
      pattern: "--input"
    - from: "the ruleset's single bypass actor"
      to: "the GitHub Actions application"
      via: "an Integration actor id resolved from the API, not guessed"
      pattern: "\"actor_id\": 15368"
  prohibitions:
    - statement: "No ruleset created by this phase names a human — including the operator — as a bypass actor. `RepositoryRole`, `OrganizationAdmin`, `Team` and `User` actor types are all forbidden. With one maintainer, an always-on admin bypass would make POLICY-03's \"no direct push\" true of nobody, which is a ruleset claiming protection it does not provide (D-09)."
      status: enforced
      verification: "judgment tier — no mechanical check is wired beyond this phase. Task 1's verify asserts the stored bypass list is exactly `Integration:15368:always` and that `current_user_can_bypass` reads `never` on the canary; plan 172-06 repeats both assertions for all three repositories."
    - statement: "No irreversible GitHub API call is issued from this plan. The capture happens here, the canary happens here, and the DELETE happens in plan 172-06 behind two checkpoints. A plan that both captures the evidence and destroys the subject could lose both in one interrupted run."
      status: enforced
      verification: "judgment tier — Task 1's verify asserts `henols/firestarter` still returns exactly one ruleset whose id is 4998759 after this plan completes."
---

<objective>
Capture the incumbent ruleset before anything can destroy it, write the one canonical body all three
rulesets will be born from, create `firestarter_prom`'s ruleset as a canary, and settle — or
explicitly decline to settle — the one assumption in this phase that can break the next stable
release in both sub-repositories.

Purpose: D-12 puts the rulesets last, after everything else has landed on the milestone branch, so
that no step in this phase ever needs a direct push to a protected branch. Within that, research
adds one prerequisite: create prom's ruleset **first**, because whether the API accepts an
`Integration` bypass actor on a user-owned repository is assumed rather than documented, and
discovering it does not is far cheaper before the one-way delete than after it.

Output: two JSON evidence files, one live ruleset on `firestarter_prom`, and a recorded disposition
for D-09's premise.

**Nothing irreversible happens in this plan.** Ruleset `4998759` is still there when this plan
finishes. Plan 172-06 owns the delete, behind a decision checkpoint and a human-action checkpoint.

`autonomous: false`: Task 2 is a `blocking-human` gate that must not be auto-approved.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md
@.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md
@.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md
</context>

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Contributing.md`, the `_Sidebar.md` bullet, the `Home.md` Reference bullet | live wiki | 172-01 |
| The `Contributing` row and prose note in `.planning/v1.35/MIGRATION-TABLE.md` | meta | 172-01 |
| `.github/ISSUE_TEMPLATE/{bug-report.yml,feature-request.yml,dev-test-report.md,config.yml}` | prom | 172-02 |
| The `env:` block, default-branch gate and `LEGACY-01 dead tracker link check` step in `wiki-check.yml` | prom | 172-03 |
| `.github/CONTRIBUTING.md` | prom, firestarter, firestarter_app | 172-04 |
| `evidence/172-05-ruleset-body.json` — the one canonical POST body | meta | 172-05 |
| `evidence/172-05-ruleset-4998759-pre-delete.json` — the incumbent, captured | meta | 172-05 |
| Ruleset `Protect main` on `henols/firestarter_prom` | GitHub control plane | 172-05 |
| Ruleset `Protect main` on `henols/firestarter` (new id) and on `henols/firestarter_app` | GitHub control plane | 172-06 |
| Branch `policy/contributor-policy` in all three repos, and three pull requests | GitHub | 172-07 |
| `evidence/172-01-*` … `evidence/172-09-*` | meta | each plan |

<tasks>

<task type="auto">
  <name>Task 1: Capture ruleset 4998759, write the one canonical body, and create prom's ruleset as a canary</name>
  <precondition>`gh auth status` succeeds and `gh api /repos/henols/firestarter_prom --jq .permissions.admin` prints `true`; the same holds for the other two repositories. Without admin the POST returns 403 and nothing below is meaningful.</precondition>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` § "The one canonical ruleset body, and the three calls" — the body verbatim, the pasted pre-delete JSON of `4998759`, and the field-by-field justification including why two fields the incumbent returns are deliberately omitted
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pattern 2 — the read-back normaliser and the exact list of volatile fields that must be stripped
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pitfall 3 point 3 — the silent-drop case: if GitHub drops the `Integration` bypass on a user-owned repository, a three-way equality still passes while the intent is lost, so the bypass list must be asserted explicitly
    - `.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md` D-09, D-10 and D-11 — the single bypass actor and why, the delete-and-recreate and why, and the two rules from gh#6 that are deliberately not included
    - `.planning/ROADMAP.md` Phase 172 success criterion 4 — the named trap: `henols/firestarter` has had a ruleset called `Protect main` since 2025-04-22 whose enforcement is `disabled`, so existence is not compliance
  </read_first>
  <files>.planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-4998759-pre-delete.json, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-body.json, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-prom-canary-readback.txt</files>
  <action>
    **Capture first, before anything else.** Run
    `gh api /repos/henols/firestarter/rulesets/4998759` and write the full response to
    `evidence/172-05-ruleset-4998759-pre-delete.json`. Confirm it parses and that `.id` is `4998759`.
    D-10 is the only one-way decision in this phase: deleting that ruleset destroys its identity and
    its 2025-04-22 creation history permanently, and the GitHub API has no undelete. This file is the
    only recovery record that will exist. Commit it before issuing any other API call.

    **Write the one canonical body** to `evidence/172-05-ruleset-body.json`. It is committed rather
    than left in `/tmp` so that "all three born from one body" is auditable afterwards and so plan
    172-06 reads the identical bytes. Contents:

    `name` is `Protect main` — the incumbent's name, which reads correctly and keeps the
    settings-page label stable. `target` is `branch`. `enforcement` is `active`; this is the whole
    point, and `disabled` is the trap the roadmap names. `conditions.ref_name.include` is the single
    element `~DEFAULT_BRANCH` with an empty `exclude`, following the measured incumbent so the rule
    keeps applying if a default branch is ever renamed.

    `rules` is exactly three entries and no more, per D-11: `deletion`; `non_fast_forward`; and
    `pull_request` with `required_approving_review_count` 0, `dismiss_stale_reviews_on_push` false,
    `require_code_owner_review` false, `require_last_push_approval` false,
    `required_review_thread_resolution` false, and `allowed_merge_methods` listing merge, squash and
    rebase. Do **not** add a `required_status_checks` rule: gh#6 asks for one, D-11 declines it, and
    pinning a check name that never reports would deadlock that repository's `main` behind a check
    that can never go green — prom has exactly one registered workflow today. Do **not** set
    `required_review_thread_resolution` true either; gh#6 asks, D-11 declines, and both declines are
    recorded as non-claims for Phase 173's ledger rather than quietly delivered.

    Omit `required_reviewers` and `require_extra_approval_for_unattributed_changes` from the body
    even though the incumbent returns both. Neither is in the documented `pull_request` parameter
    list; omitting them lets GitHub apply its own defaults, and because all three rulesets are born
    from this one body those defaults land identically — which is exactly what the three-way equality
    then asserts.

    `bypass_actors` is exactly one entry: `actor_id` 15368, `actor_type` `Integration`, `bypass_mode`
    `always`. Re-resolve the id rather than trusting this paragraph —
    `gh api /apps/github-actions --jq '{id,slug}'` must return `15368` and `github-actions`. Add no
    second actor. No `RepositoryRole`, no `User`, no `OrganizationAdmin`, no `Team`, and not the dead
    `DeployKey` entry the incumbent carries — all three repositories have zero deploy keys, so that
    entry grants nothing to anyone. With Actions bypassing and nobody else, "no direct push" is
    literally true of every person including the operator.

    **Then the canary.** `POST /repos/henols/firestarter_prom/rulesets` with
    `--input <the committed body file>`. prom goes first deliberately: whether `actor_type:
    "Integration"` is even accepted on a user-owned repository is assumed, not documented, and prom
    is the repository whose protection this phase can most cheaply undo if the answer is no.

    **Read it back from the API, never from the settings page.** Fetch prom's ruleset list, take the
    single id, fetch that ruleset, and write the raw JSON plus the assertion results to
    `evidence/172-05-prom-canary-readback.txt`. Assert: exactly one ruleset on prom; `enforcement` is
    `active`; the sorted rule types are exactly deletion, non_fast_forward, pull_request; the bypass
    list rendered as `actor_type:actor_id:bypass_mode` is exactly `Integration:15368:always`; the
    `ref_name.include` list is exactly `~DEFAULT_BRANCH`; and `current_user_can_bypass` reads
    `never`.

    That bypass-list assertion is the one that matters most here. If GitHub silently drops an
    `Integration` actor on a user-owned repository, the three-way equality plan 172-06 runs would
    still pass — all three would drop it identically — while the whole intent of D-09 was lost. The
    equality cannot catch that; only an explicit assertion can.

    Finally confirm nothing else moved: `henols/firestarter` still returns exactly one ruleset and
    its id is still `4998759`, and `henols/firestarter_app` still returns an empty list.

    Commit all three evidence files to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/172-policy-one-tracker-protected-main/evidence && CAP=$E/172-05-ruleset-4998759-pre-delete.json && BODY=$E/172-05-ruleset-body.json && test -s "$CAP" && jq -e '.id == 4998759' "$CAP" >/dev/null && jq -e '.enforcement == "disabled"' "$CAP" >/dev/null && jq -e '.name == "Protect main"' "$CAP" >/dev/null && test -s "$BODY" && jq -e '.enforcement == "active"' "$BODY" >/dev/null && test "$(jq -r '[.rules[].type]|sort|join(",")' "$BODY")" = 'deletion,non_fast_forward,pull_request' && test "$(jq -r '[.bypass_actors[]|"\(.actor_type):\(.actor_id):\(.bypass_mode)"]|join(",")' "$BODY")" = 'Integration:15368:always' && test "$(jq -r '.conditions.ref_name.include|join(",")' "$BODY")" = '~DEFAULT_BRANCH' && test "$(jq -r '[.rules[].type]|map(select(. == "required_status_checks"))|length' "$BODY")" -eq 0 && test "$(gh api /apps/github-actions --jq .id)" = '15368'</automated>
    <fails_when>the capture file is missing or empty, or `jq -e '.id == 4998759'` prints `false` or exits non-zero; or the body's rule-type join is anything other than `deletion,non_fast_forward,pull_request` (a fourth rule was added, or one of the three is missing); or the bypass join is not exactly `Integration:15368:always` (a human-shaped actor was added, or the dead DeployKey entry was carried forward); or `gh api /apps/github-actions --jq .id` does not print `15368`</fails_when>
    <automated>cd /workspaces && E=.planning/phases/172-policy-one-tracker-protected-main/evidence && EV=$E/172-05-prom-canary-readback.txt && D=$(mktemp -d) && gh api /repos/henols/firestarter_prom/rulesets > "$D/list.json" && test "$(jq length "$D/list.json")" -eq 1 && ID=$(jq -r '.[0].id' "$D/list.json") && gh api "/repos/henols/firestarter_prom/rulesets/$ID" > "$D/raw.json" && { echo "== firestarter_prom ruleset id=$ID"; cat "$D/raw.json"; echo; echo "enforcement=$(jq -r .enforcement "$D/raw.json")"; echo "rules=$(jq -r '[.rules[].type]|sort|join(",")' "$D/raw.json")"; echo "bypass=$(jq -r '[.bypass_actors[]|"\(.actor_type):\(.actor_id):\(.bypass_mode)"]|join(",")' "$D/raw.json")"; echo "include=$(jq -r '.conditions.ref_name.include|join(",")' "$D/raw.json")"; echo "can_bypass=$(jq -r .current_user_can_bypass "$D/raw.json")"; echo "fw_rulesets=$(gh api /repos/henols/firestarter/rulesets --jq length)"; echo "fw_id=$(gh api /repos/henols/firestarter/rulesets --jq '.[0].id')"; echo "app_rulesets=$(gh api /repos/henols/firestarter_app/rulesets --jq length)"; } > "$EV" 2>&1 && grep -qx 'enforcement=active' "$EV" && grep -qx 'rules=deletion,non_fast_forward,pull_request' "$EV" && grep -qx 'bypass=Integration:15368:always' "$EV" && grep -qx 'include=~DEFAULT_BRANCH' "$EV" && grep -qx 'can_bypass=never' "$EV" && grep -qx 'fw_rulesets=1' "$EV" && grep -qx 'fw_id=4998759' "$EV" && grep -qx 'app_rulesets=0' "$EV"</automated>
    <fails_when>`bypass=` is empty or reads `DeployKey:...` — GitHub silently dropped the Integration actor on this user-owned repository, which the later three-way equality would not catch because all three would drop it identically; or `enforcement=disabled`, reproducing the exact trap the roadmap names; or `can_bypass=` is anything other than `never`, meaning the authenticated human can push directly; or `fw_id` is not `4998759` or `app_rulesets` is not `0`, meaning the canary touched a repository it should not have</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/172-05-ruleset-4998759-pre-delete.json` exists, is non-empty, and `jq -e '.id == 4998759'` succeeds against it; it also records `enforcement: "disabled"`, the `DeployKey` bypass entry, and the `2025-04-22` `created_at`
    - The capture file was committed before any POST was issued — verifiable from the commit order in `git log --oneline`
    - `evidence/172-05-ruleset-body.json` exists and is valid JSON with `enforcement: "active"`, `target: "branch"`, `conditions.ref_name.include == ["~DEFAULT_BRANCH"]`
    - Its `rules` array has exactly three entries whose sorted types are `deletion,non_fast_forward,pull_request`, and contains no `required_status_checks` entry
    - Its `pull_request` parameters set `required_review_thread_resolution` to false, and the body omits `required_reviewers` and `require_extra_approval_for_unattributed_changes` entirely
    - Its `bypass_actors` array has exactly one entry, `{actor_id: 15368, actor_type: "Integration", bypass_mode: "always"}`, and `gh api /apps/github-actions --jq .id` independently returns `15368`
    - `gh api /repos/henols/firestarter_prom/rulesets --jq length` returns `1`
    - The read-back evidence records `enforcement=active`, `rules=deletion,non_fast_forward,pull_request`, `bypass=Integration:15368:always`, `include=~DEFAULT_BRANCH` and `can_bypass=never`
    - `henols/firestarter` still returns exactly one ruleset with id `4998759`, and `henols/firestarter_app` still returns an empty list — nothing irreversible happened in this plan
    - All evidence came from `gh api` output; no settings-page screenshot, description or recollection appears in any evidence file
  </acceptance_criteria>
  <done>The incumbent is captured and committed; one canonical body exists as a tracked file; prom carries a real, enforcing ruleset whose stored bypass list is exactly the GitHub Actions app; and the two sub-repositories are provably untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    `henols/firestarter_prom` now carries a live, enforcing `Protect main` ruleset, read back from the
    API and recorded in `evidence/172-05-prom-canary-readback.txt`. Its single bypass actor is the
    GitHub Actions app, id 15368, mode `always`. `henols/firestarter` and `henols/firestarter_app`
    are untouched; `4998759` is captured but still live.

    **The one assumption this phase cannot settle from documentation is now the next thing in the
    way, and it has the highest blast radius in the phase.**

    D-09 assumes that a push authenticated by a workflow's default `GITHUB_TOKEN` is covered by an
    `Integration`/15368 bypass. That matters because **both sub-repositories auto-commit a version
    bump back onto `main` from CI**: `firestarter_app/.github/workflows/release.yml:31` runs
    `stefanzweifel/git-auto-commit-action` on push-to-`main`, and `firestarter`'s `build.yml` does the
    same below its publish boundary. If the assumption is wrong, the **next stable release fails in
    both sub-repositories** at the version-bump step, and in `firestarter` the release-publishing step
    that follows never runs.

    What is actually established: the `GITHUB_TOKEN` is documented as a GitHub App installation access
    token; GitHub Apps are documented as eligible bypass actors. **No page connects the two.** The
    community material that exists runs the other way — the canonical discussion's most authoritative
    reply is a GitHub staff comment arguing such a bypass would be insecure, and every 2024-era answer
    in that thread uses a custom app instead. Research raised this from *unproven* to *contradicted by
    the available secondary evidence*.

    A `.github`-only merge triggers neither release workflow, so nothing later in this phase will
    exercise the bypass. It would ship configured and unproven unless it is probed here.
  </what-built>
  <how-to-verify>
    Choose one of the two, then tell me which.

    **A — run the probe.** It is decisive and takes minutes, and it touches nothing real:

    1. Create a throwaway repository under your account, for example `henols/ruleset-probe-172`.
    2. Apply the identical ruleset body — I have it committed at
       `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-ruleset-body.json`, so
       it is one `gh api --method POST /repos/henols/ruleset-probe-172/rulesets --input <that file>`.
    3. Add a `workflow_dispatch` workflow with `permissions: contents: write` that makes an empty
       commit and pushes to `main` using the default `GITHUB_TOKEN` — no PAT, no custom app token.
    4. Dispatch it and read the run.
    5. Delete the repository.

    A successful push means D-09 holds and the bypass covers the default token. A rejection naming
    the ruleset means it does not.

    Repository creation and deletion are account mutations, which is why this is a gate and not
    something I do unattended. Tell me the outcome and I will record it.

    **B — decline the probe.** Then the phase proceeds exactly as designed and the risk is recorded
    honestly in the evidence file, in these terms rather than softer ones: the bypass is configured,
    unproven, and the available secondary evidence is against it; the next stable release in both
    sub-repositories is the thing that will discover the answer. Phase 173's honesty ledger carries
    it forward. Note the two remedies that stay available either way: `bypass_mode: "exempt"`, added
    by GitHub in September 2025 for trusted high-volume automation, and a dedicated GitHub App token
    — which is not a person, so it would not make "no direct push" false of anybody.

    Either way I write the outcome, the date and the reasoning to
    `evidence/172-05-actions-bypass-probe.txt` and neither the phase record nor Phase 173's ledger
    will describe this as settled.
  </how-to-verify>
  <resume-signal>Type "A" plus the probe outcome (pushed / rejected), or "B" to decline and carry the risk on the record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the operator's `gh` token → three repositories' access-control configuration | An `admin: true` token can create, alter and destroy protection on all three repositories. Everything in this phase's control-plane work crosses this boundary. |
| a ruleset's `bypass_actors` list → the claim "no person can push directly" | Every entry in that list is an exception to the claim the documentation makes. One wrong entry makes the claim false without making it look false. |
| CI's `GITHUB_TOKEN` → a protected default branch | Both sub-repositories' release workflows push to `main` as an automation identity. Whether that identity is inside or outside the bypass decides whether releases keep working. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-01 | Repudiation | a ruleset that exists but does not enforce | high | mitigate | Every assertion reads `enforcement` and the rule set from the API, never the name and never the settings page; the incumbent `4998759` is the live proof that a `Protect main` can mean nothing |
| T-172-02 | Elevation of privilege | `bypass_actors` containing a human | high | mitigate | The body carries exactly one non-human actor; the read-back asserts the stored list verbatim and asserts `current_user_can_bypass` is `never` |
| T-172-21 | Tampering | GitHub silently dropping the `Integration` actor on a user-owned repository | high | mitigate | prom goes first as a canary and the bypass list is asserted explicitly rather than by three-way equality, which would pass on a uniform silent drop |
| T-172-22 | Denial of service | the release workflows losing their push to `main` | high | mitigate | The premise is probed against a throwaway repository behind a `blocking-human` gate before the one-way delete; if declined, the risk is recorded in the terms research established rather than softened |
| T-172-03 | Denial of service | loss of the incumbent ruleset's record | high | mitigate | The capture is written and committed before any other API call in the phase, and nothing irreversible happens in this plan at all |
| T-172-23 | Denial of service | a required status check pinned to a name that never reports | medium | mitigate | D-11 declines status checks; the body is asserted to contain no `required_status_checks` rule |
</threat_model>

<verification>
- `evidence/172-05-ruleset-4998759-pre-delete.json` parses with `.id == 4998759` and was committed before any POST
- `evidence/172-05-ruleset-body.json` has exactly three rules, exactly one bypass actor, and `enforcement: active`
- prom returns exactly one ruleset whose read-back shows `enforcement=active`, `bypass=Integration:15368:always` and `can_bypass=never`
- `firestarter` still has ruleset `4998759`; `firestarter_app` still has none
- The D-09 disposition is written to evidence, whichever way the gate went
</verification>

<success_criteria>
- The irreversible step in plan 172-06 is now recoverable-by-record and de-risked by a canary
- The one assumption that can break both sub-repositories' next release has been examined rather than ridden past
- One canonical body exists as a tracked file, so "three rulesets born from one body" is auditable
</success_criteria>

<output>
Create `.planning/phases/172-policy-one-tracker-protected-main/172-05-SUMMARY.md` when done
</output>
