---
phase: 172-policy-one-tracker-protected-main
plan: 03
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter_prom (meta, /workspaces) on gsd/v1.35-documentation-consolidation-wiki-migration
files_modified:
  - .github/workflows/wiki-check.yml
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-workflow-fixes.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-full-suite.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-legacy01-redgreen.txt
autonomous: true
requirements: [LEGACY-01]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - 'The dispatch-mirror step no longer passes an argument `dispatch_mirror.py` does not declare; the workflow''s own invocation of it exits 0 rather than 2'
    - 'On a scheduled run the resolver lands both sub-repos on `beta`, not on `main` — making the comment at `wiki-check.yml:42-50`, which already claims exactly this, true'
    - 'A manual dispatch of a branch that exists in both sub-repos still resolves to that branch; a dispatch of a branch that exists in neither still falls through to `beta`'
    - 'All three pre-existing legs — `wiki.py links`, `honest02_truth.py`, `dispatch_mirror.py` — pass under the fixed invocation against a fresh wiki clone and fresh `beta` clones of both sub-repos, which is D-14''s stated precondition for registering the workflow'
    - 'A LEGACY-01 grep leg covering all three checkouts and the wiki clone, excluding only `.git` and `.planning`, exits 0 against the current trees'
    - 'That leg has been observed RED on a planted `henols/firestarter/issues` link and RED on a planted `henols/firestarter_app/issues` link, and GREEN again after both are removed — it is not trusted until it has been seen to fail'
    - 'The leg does not match the live tracker `https://github.com/henols/firestarter_prom/issues`, which appears in all three READMEs today and must keep appearing'
    - 'Nothing this plan writes into `wiki-check.yml` is a comment, even though the surrounding file is comment-dense'
    - 'The workflow still declares `permissions: contents: read` — nothing added here needs write'
  artifacts:
    - path: ".github/workflows/wiki-check.yml"
      provides: "The meta CI job that carries every LEGACY-01 surface on one disk, with its two blocking defects fixed and the new guard added"
      min_lines: 115
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-workflow-fixes.txt"
      provides: "The resolver simulated under three scenarios, proving scheduled runs now land on beta"
      min_lines: 15
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-full-suite.txt"
      provides: "D-14's precondition discharged — all three pre-existing legs green against fresh clones under the fixed invocation"
      min_lines: 6
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-legacy01-redgreen.txt"
      provides: "The guard demonstrated failing before it is trusted: green, red on two planted links, green again, plus the no-false-positive control"
      min_lines: 12
  key_links:
    - from: "`.github/workflows/wiki-check.yml` resolver step"
      to: "the two sub-repo checkout steps"
      via: "the `subref` step outputs, now gated so the meta default branch is never a candidate"
      pattern: "steps.subref.outputs.firestarter_ref"
    - from: "`.github/workflows/wiki-check.yml` LEGACY-01 step"
      to: "the meta, firestarter, firestarter_app checkouts and the wiki clone"
      via: "a recursive grep over the four directories that job already has on disk"
      pattern: "grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone"
  prohibitions:
    - statement: "No guard added by this phase is trusted until it has been observed RED on a planted violation. A check that is green because its pattern matches nothing is false assurance, not a guard — and it is worse than no guard, because it displaces the attention a missing guard would attract (D-14; REQUIREMENTS.md set the same bar for HONEST-02)."
      status: enforced
      verification: "judgment tier — no mechanical meta-check is wired. Task 2's verify plants both dead-tracker forms, requires exit 1 for each, removes them, requires exit 0, and records all four runs in the evidence file; the reviewer confirms the red runs named the planted file."
    - statement: "This phase never sweeps `.planning/` for dead tracker links. Those citations are historical-by-intent and repairing them destroys the evidence they exist to preserve (REQUIREMENTS.md Out of Scope). The guard's exclusion of `.planning/` is a deliberate scope boundary, not an allowlist covering up a defect."
      status: enforced
      verification: "judgment tier — Task 2 records the count of matches that exist inside `.planning/` today alongside the zero found outside it, so the exclusion's cost is on the record rather than invisible."
---

<objective>
Fix the two reproduced defects in `.github/workflows/wiki-check.yml` that make it fail
unconditionally, then add the LEGACY-01 dead-tracker-link guard and demonstrate it failing before
it is trusted.

Purpose: LEGACY-01 is **already satisfied** — zero dead-tracker links exist outside `.planning/`
today. What it lacks is a guard, and D-13 puts that guard here because this job is the only place
where meta, both sub-repos and the live wiki are on one disk at the same time. D-14 additionally
requires the workflow's three existing legs to pass before it is registered, so that registration
does not immediately produce a red weekly run — and today two of them cannot pass at all.

Output: one workflow file with three changes, and three evidence files.

**Order matters inside this plan.** The two fixes come first and the guard second. Adding a guard to
a job that already exits 2 on two of its three steps would prove nothing about the guard and would
leave D-14's precondition unmet.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md
@.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md
@.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md
@.github/workflows/wiki-check.yml
@tools/wiki/dispatch_mirror.py
</context>

<!-- planner-discipline-allow: henols/firestarter/issues -->
<!-- planner-discipline-allow: henols/firestarter_app/issues -->
<!-- planner-discipline-allow: --wiki-dir -->

## Flagged Assumptions

Surfaced here rather than resolved. Do not invent an acceptance criterion for it.

- **LEGACY-01 edge, `unclassified`.** The edge probe run for this phase returned
  `unclassified — review manually` for LEGACY-01 and could not classify an edge case for it. Seven
  of the eight applicable edges resolved to explicit acceptance criteria and are carried in the
  `must_haves.truths` of the plans that own them; this eighth one did not, and is recorded as an
  open assumption rather than backfilled with a plausible-sounding criterion. The practical shape of
  the gap: LEGACY-01 is stated as an absence ("no documentation page sends a reader to a disabled
  tracker"), and an absence has no natural boundary, adjacency or ordering edge for a shape
  taxonomy to find. The guard this plan adds is the best available evidence for the absence; it is
  not a proof that the absence is edge-free. If a reviewer can name the missing edge, it belongs in
  the phase record rather than in this plan.

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Contributing.md`, the `_Sidebar.md` bullet, the `Home.md` Reference bullet | live wiki | 172-01 |
| The `Contributing` row and prose note in `.planning/v1.35/MIGRATION-TABLE.md` | meta | 172-01 |
| `.github/ISSUE_TEMPLATE/{bug-report.yml,feature-request.yml,dev-test-report.md,config.yml}` | prom | 172-02 |
| The `env:` block and default-branch gate in `wiki-check.yml`'s resolver step | prom | 172-03 |
| The `LEGACY-01 dead tracker link check` step in `wiki-check.yml` | prom | 172-03 |
| `.github/CONTRIBUTING.md` | prom, firestarter, firestarter_app | 172-04 |
| Ruleset `Protect main` on all three repositories | GitHub control plane | 172-05, 172-06 |
| Branch `policy/contributor-policy` in all three repos, and three pull requests | GitHub | 172-07 |
| `evidence/172-01-*` … `evidence/172-09-*` | meta | each plan |

<tasks>

<task type="auto">
  <name>Task 1: Fix the dispatch-mirror argument and the ref resolver, then prove all three pre-existing legs pass</name>
  <read_first>
    - `.github/workflows/wiki-check.yml` in full — the file being modified. Note lines 42-50: a comment written by an earlier phase already claims scheduled runs land on `beta`. That claim is false today; this task makes it true rather than editing or deleting the comment.
    - `tools/wiki/dispatch_mirror.py:157-158` — the script's entire argument surface is `--app-dir` and `--fw-dir`, both `required=True`. There is no `--wiki-dir`.
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pitfall 1 and Pitfall 2 — both defects reproduced with pasted output, including the measured consequence that `firestarter_app@main` has no `firestarter/data/chip_database.json` and `firestarter@main` has no `PROTOCOLS.md`
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` § "Open Questions" item 2 — the recommendation to fix the behaviour so the existing comment becomes true, rather than authoring or deleting a comment
    - `.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md` § "Test Infrastructure" — the full-suite recipe, verbatim, that mirrors a fixed CI
  </read_first>
  <files>.github/workflows/wiki-check.yml, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-workflow-fixes.txt, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-full-suite.txt</files>
  <action>
    Two changes to `.github/workflows/wiki-check.yml`. Write no comment in either; the operator's
    standing no-comments rule binds what this phase writes, and the fact that this file is dense with
    comments from earlier phases does not license matching the local style. Do not delete or reword
    any existing comment either — the second change is designed so the existing one becomes true.

    **Change A — the dispatch-mirror step. A pure deletion.** In the `Dispatch-mirror check` step,
    delete the single continuation line that passes the wiki directory to `dispatch_mirror.py`. The
    script declares only `--app-dir` and `--fw-dir`, so the extra argument makes argparse exit 2 on
    every run, on every branch. Delete only that line; leave the `python3` invocation, the remaining
    two arguments and the trailing `echo "OK: ..."` untouched. Because the fix is a deletion, the
    no-comments rule is satisfied trivially.

    **Change B — the resolver step.** Replace the body of the
    `Resolve sub-repo ref (same branch name, else beta)` step with an equivalent that does two
    things the current one does not.

    First, move both Actions expressions out of the `run:` block and into an `env:` block on the
    step: `META_REF: ${{ github.head_ref || github.ref_name }}` and
    `META_DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}`. Both `schedule` and
    `workflow_dispatch` payloads carry `repository`, so the second expression resolves for both of
    this workflow's triggers. This is not cosmetic: interpolating a branch name straight into a shell
    line is the documented script-injection shape, and it also makes the step untestable outside
    Actions, because `bash` rejects the expression syntax as a bad substitution. With the values in
    `env:` the `run:` body becomes plain shell that can be extracted and simulated, which is what the
    verify below does.

    Second, gate the candidate. Set `DEF` from `META_DEFAULT_BRANCH` with `main` as the shell default
    if it is empty; set `CAND` from `META_REF`; and if `CAND` equals `DEF`, set `CAND` to the empty
    string before the probe. Then in the per-repo loop initialise `RESOLVED` to `beta` and overwrite
    it with `CAND` only when `CAND` is non-empty **and** `git ls-remote --exit-code --heads` finds
    that branch in the sub-repo. Keep both existing `echo` lines — the one that reports the ref under
    test and the one that appends `${repo}_ref=$RESOLVED` to `$GITHUB_OUTPUT` — and keep the
    per-repo `echo "$repo -> $RESOLVED"`. Where the ref-under-test echo would print an empty string,
    print a short standing phrase instead so the log is readable.

    The effect: on a `schedule` run `github.head_ref` is empty and `github.ref_name` is the
    workflow's own default branch, so `CAND` becomes empty and both sub-repos fall through to `beta`.
    That is what the comment at lines 42-50 already asserts and what the two other legs need — the
    wiki's claim stamps record `db-sha256-16=ccbc8d2c4866a5af`, which is the database on `beta`, and
    `firestarter@main` has no `PROTOCOLS.md` for the dispatch-mirror leg to read. A manual dispatch of
    a real feature branch is unaffected: that branch is not the meta default branch, so it is still
    probed and still wins where it exists.

    Do not touch the two checkout steps, the wiki clone step, or the `permissions: contents: read`
    block. Nothing added here needs write access.

    Then produce the two evidence files. The first records the resolver simulated under three
    scenarios by extracting the step's `run:` body and running it as plain shell with the two
    environment variables set — scheduled (`META_REF` equal to the default branch), a dispatch of the
    milestone branch (which exists in both sub-repos), and a dispatch of a branch that exists in
    neither. The second records the full-suite recipe from `172-VALIDATION.md`, run against a fresh
    wiki clone and fresh `beta` clones of both sub-repos, with `dispatch_mirror.py` invoked the way
    the fixed workflow now invokes it. Never substitute `/workspaces/firestarter` or
    `/workspaces/firestarter_app` for those clones and call it "as CI would" — the working trees are
    on the milestone branch, not on `beta`.

    Commit the workflow and both evidence files to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-workflow-fixes.txt && W=.github/workflows/wiki-check.yml && R=$(mktemp) && T=$(mktemp -d) && awk '/- name: Resolve sub-repo ref/{f=1} f&&/^ *run: \|$/{r=1;next} r&&/^      - name:/{exit} r{print}' "$W" > "$R" && { echo "== extracted resolver body"; cat "$R"; echo "== scenario A schedule"; META_REF=main META_DEFAULT_BRANCH=main GITHUB_OUTPUT="$T/a" bash "$R"; cat "$T/a"; echo "== scenario B dispatch milestone branch"; META_REF=gsd/v1.35-documentation-consolidation-wiki-migration META_DEFAULT_BRANCH=main GITHUB_OUTPUT="$T/b" bash "$R"; cat "$T/b"; echo "== scenario C dispatch unknown branch"; META_REF=no-such-branch-172 META_DEFAULT_BRANCH=main GITHUB_OUTPUT="$T/c" bash "$R"; cat "$T/c"; } > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -qx 'exit 0' "$EV" && ! grep -q '{{' "$R" && grep -qx 'firestarter_ref=beta' "$T/a" && grep -qx 'firestarter_app_ref=beta' "$T/a" && grep -qx 'firestarter_ref=gsd/v1.35-documentation-consolidation-wiki-migration' "$T/b" && grep -qx 'firestarter_app_ref=gsd/v1.35-documentation-consolidation-wiki-migration' "$T/b" && grep -qx 'firestarter_ref=beta' "$T/c" && ! grep -q -- '--wiki-dir' "$W" && grep -A1 '^permissions:' "$W" | grep -qx '  contents: read' && test "$(grep -c 'META_REF' "$W")" -eq 2</automated>
    <fails_when>scenario A's output file contains `firestarter_ref=main` (the resolver still lands scheduled runs on the default branch); or the extracted body still contains `{{` (the Actions expressions were not moved into `env:`, and `bash` will print `bad substitution`); or `--wiki-dir` is still present in the workflow; or scenario B resolves to `beta` (the gate is too wide and now breaks manual dispatch); or the `permissions:` block no longer reads `contents: read`</fails_when>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-full-suite.txt && D=$(mktemp -d) && { git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$D/wiki-clone" && git clone --depth 1 -q -b beta https://github.com/henols/firestarter.git "$D/firestarter" && git clone --depth 1 -q -b beta https://github.com/henols/firestarter_app.git "$D/firestarter_app" && python3 tools/wiki/wiki.py links --source-dir "$D/wiki-clone" | tail -1 && python3 tools/wiki/honest02_truth.py --wiki-dir "$D/wiki-clone" --db "$D/firestarter_app/firestarter/data/chip_database.json" --allowlist tools/wiki/claim-allowlist.json | tail -1 && python3 tools/wiki/dispatch_mirror.py --app-dir "$D/firestarter_app" --fw-dir "$D/firestarter" | tail -1; } > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -qx 'exit 0' "$EV" && grep -qc '^OK: ' "$EV" && test "$(grep -c '^OK: ' "$EV")" -eq 3</automated>
    <fails_when>the evidence file's last line is not `exit 0`; or fewer than three lines begin with `OK: `; or the file contains `unrecognized arguments`, `ERROR: PROTOCOLS.md not found`, or `ERROR: cannot load database`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--wiki-dir' .github/workflows/wiki-check.yml` returns 0
    - The `Dispatch-mirror check` step still invokes `dispatch_mirror.py` with `--app-dir firestarter_app` and `--fw-dir firestarter`, and still ends with its `echo "OK: ...` line
    - The resolver step carries an `env:` block declaring exactly `META_REF` and `META_DEFAULT_BRANCH`, and its `run:` body contains no `${{` sequence
    - Extracting the resolver's `run:` body and running it with `META_REF=main META_DEFAULT_BRANCH=main` writes `firestarter_ref=beta` and `firestarter_app_ref=beta` to `$GITHUB_OUTPUT`
    - The same extraction with `META_REF=gsd/v1.35-documentation-consolidation-wiki-migration` writes that branch name for both repos — the fix does not break manual dispatch
    - The same extraction with a branch that exists in neither sub-repo writes `beta` for both
    - `grep -A1 '^permissions:' .github/workflows/wiki-check.yml` still shows `  contents: read`
    - The comment block at `wiki-check.yml:42-50` is byte-unchanged — the behaviour was fixed to match it, not the other way round
    - `git diff HEAD~1 -- .github/workflows/wiki-check.yml` adds no line matching `^\s*#`
    - The full-suite recipe against a fresh wiki clone and fresh `beta` clones prints three `OK: ` lines and exits 0
    - The full-suite evidence contains no `unrecognized arguments`, no `PROTOCOLS.md not found` and no `cannot load database`
  </acceptance_criteria>
  <done>Both reproduced defects are fixed; the resolver is testable outside Actions and lands scheduled runs on `beta`, making the workflow's existing comment true; all three pre-existing legs pass under the fixed invocation against fresh clones, discharging D-14's precondition.</done>
</task>

<task type="auto">
  <name>Task 2: Add the LEGACY-01 grep leg and demonstrate it RED before trusting it GREEN</name>
  <precondition>Task 1's `evidence/172-03-full-suite.txt` exists and ends `exit 0` — a guard added to a job whose other steps cannot pass proves nothing about the guard.</precondition>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pattern 3 — the leg verbatim, the three properties proven against the real four-directory layout, and the specific reason the `! grep …` form must not be used
    - `.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md` D-13 and D-15 — why this workflow carries the guard, and why the pattern covers every surface rather than markdown only
    - `.planning/REQUIREMENTS.md` "Out of Scope" — the `.planning/` exclusion is a scope decision recorded by the milestone, not an allowlist invented here
    - `.github/workflows/wiki-check.yml` as left by Task 1 — the `Clone the live wiki` step is where the new step goes, immediately after it and before the `WIKI-05 reachability check`
  </read_first>
  <files>.github/workflows/wiki-check.yml, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-legacy01-redgreen.txt</files>
  <action>
    Insert one new step into `.github/workflows/wiki-check.yml`, directly after the `Clone the live
    wiki` step and before `WIKI-05 reachability check`. Name it
    `LEGACY-01 dead tracker link check`. It needs no `uses:` and no new checkout — meta, both
    sub-repos and the wiki clone are all already on disk in that job by this point.

    The step body is a single `if` block. Recursively grep the four directories `meta`, `firestarter`,
    `firestarter_app` and `wiki-clone` for the extended regular expression matching the two disabled
    trackers, with `-I` to skip binaries and `-n` so a failure names a line number, and with
    `--exclude-dir=.git` and `--exclude-dir=.planning`. If the grep succeeds — meaning at least one
    hit — echo a one-line failure message naming what went wrong and `exit 1`. After the block, echo
    the standing success line. Match the house step shape used by the three legs already in the file:
    a `run: |` block, the check, then an `echo "OK: ..."` confirmation.

    **Write it as `if grep …; then … exit 1; fi`.** Do not write it as an inverted `! grep …` followed
    by an echo. Under the runner's `bash -e` a command whose status is inverted with `!` does not
    trip errexit, and the trailing echo then resets the step's exit status to 0 — the leg would
    report success on a real hit. The explicit form has no such hole.

    The pattern's anchor is load-bearing and must not be loosened: the `/` immediately after the
    optional `_app` group is what stops it matching the live tracker. In
    `henols/firestarter_prom/issues` the character following `henols/firestarter` is an underscore,
    so neither branch of the optional group can be followed by the issues path. All three READMEs
    carry the live tracker link today and must keep carrying it, so a pattern that matched it would
    turn the whole repository red on the first run.

    Write no comment. The two exclusions are self-describing and the failure message says what the
    check is for.

    **Then demonstrate the leg failing before trusting it.** Build a scratch harness that reproduces
    the job's four-directory layout — the two sub-repo working trees, `/workspaces` as `meta`, and a
    fresh wiki clone — and run the leg's grep block against it in five recorded steps: green as the
    trees stand; red with a `henols/firestarter/issues` link planted in the wiki clone; red with a
    `henols/firestarter_app/issues` link planted instead; green again after the plant is removed; and
    a reach control that runs the same grep **without** the `.planning` exclusion and records the
    number of matches it then finds. That last one is what proves the leg is not vacuous: every one
    of those matches is inside `.planning/`, which is out of scope by REQUIREMENTS.md, and recording
    the count keeps the cost of the exclusion visible instead of invisible. Plant only inside the
    scratch wiki clone; never plant into a tracked file in any of the three repositories.

    Record all five steps with their exit statuses in `evidence/172-03-legacy01-redgreen.txt`, then
    confirm the scratch clone is back to a clean `git status` before the harness is discarded.

    Commit the workflow and the evidence file to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-legacy01-redgreen.txt && H=$(mktemp -d) && git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$H/wiki-clone" && ln -s /workspaces "$H/meta" && ln -s /workspaces/firestarter "$H/firestarter" && ln -s /workspaces/firestarter_app "$H/firestarter_app" && leg() { ( cd "$H" && if grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git --exclude-dir=.planning; then echo "FAIL: a page links to an issue tracker that is disabled"; return 1; fi; echo "OK: no page links to henols/firestarter/issues or henols/firestarter_app/issues" ); } && { echo "== 1 green as the trees stand"; leg; echo "rc=$?"; echo "== 2 red on a planted firmware tracker link"; echo 'x https://github.com/henols/firestarter/issues' >> "$H/wiki-clone/Home.md"; leg; echo "rc=$?"; git -C "$H/wiki-clone" checkout -- Home.md; echo "== 3 red on a planted host tracker link"; echo 'x https://github.com/henols/firestarter_app/issues' >> "$H/wiki-clone/Home.md"; leg; echo "rc=$?"; git -C "$H/wiki-clone" checkout -- Home.md; echo "== 4 green again after removal"; leg; echo "rc=$?"; echo "== 5 reach control, matches inside .planning"; ( cd "$H" && grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git | wc -l ); echo "== 6 scratch clone clean"; git -C "$H/wiki-clone" status --porcelain | wc -l; } > "$EV" 2>&1; test "$(grep -c '^rc=0$' "$EV")" -eq 2 && test "$(grep -c '^rc=1$' "$EV")" -eq 2 && test "$(sed -n '/== 5 reach control/{n;p;}' "$EV")" -gt 0 && test "$(tail -1 "$EV")" -eq 0</automated>
    <fails_when>fewer than two `rc=0` lines or fewer than two `rc=1` lines in the evidence file — a leg that never goes red on a planted link is vacuous, and a leg that stays red after removal has a false positive; or the reach-control count is 0, meaning the grep never reaches any file at all; or the last line is not `0`, meaning the scratch wiki clone was left dirty</fails_when>
    <automated>cd /workspaces && W=.github/workflows/wiki-check.yml && grep -q 'name: LEGACY-01 dead tracker link check' "$W" && grep -qF "grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git --exclude-dir=.planning" "$W" && grep -q 'exit 1' "$W" && ! grep -q '! grep -rInE' "$W" && test "$(awk '/name: Clone the live wiki/{c=NR} /name: LEGACY-01 dead tracker link check/{l=NR} /name: WIKI-05 reachability check/{w=NR} END{print (c<l && l<w) ? 1 : 0}' "$W")" -eq 1 && test "$(git diff HEAD~1 -- "$W" | grep -c '^+[[:space:]]*#')" -eq 0</automated>
    <fails_when>the step name is absent; or the grep line does not carry both `--exclude-dir=.git` and `--exclude-dir=.planning`; or an inverted `! grep -rInE` form is present (it fails open under the runner's `bash -e`); or the awk ordering check prints 0, meaning the new step is not between the wiki clone and the WIKI-05 leg; or the diff added a line beginning with a YAML comment marker</fails_when>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/wiki-check.yml` contains a step named `LEGACY-01 dead tracker link check`, positioned after `Clone the live wiki` and before `WIKI-05 reachability check`
    - That step's grep names all four directories `meta firestarter firestarter_app wiki-clone` and carries `--exclude-dir=.git` and `--exclude-dir=.planning`
    - The step uses the `if grep …; then … exit 1; fi` form; `grep -c '! grep' .github/workflows/wiki-check.yml` returns 0
    - The step ends with an `echo "OK: ...` line, matching the house shape of the three legs already in the file
    - `git diff HEAD~1 -- .github/workflows/wiki-check.yml | grep -c '^+[[:space:]]*#'` returns 0 — no comment was added
    - The evidence file records exactly two `rc=0` runs and exactly two `rc=1` runs, in the order green, red, red, green
    - Each `rc=1` run's output names `wiki-clone/Home.md` and a line number — the failure output is actionable
    - The reach control records a match count greater than zero for the same grep run without the `.planning` exclusion, proving the leg reaches files rather than matching nothing
    - Neither planted link matched `https://github.com/henols/firestarter_prom/issues`, which is present in all three READMEs throughout every run and never appears in a `rc=1` output
    - `git -C <scratch wiki clone> status --porcelain` is empty at the end — no plant survives
    - No tracked file in any of the three repositories was modified by the demonstration
  </acceptance_criteria>
  <done>The guard is in the workflow in the fail-closed form, positioned where all four surfaces are on disk, and it has been observed red on both dead-tracker forms and green either side — it is trusted because it was seen to fail, not because it printed OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an Actions expression → a `run:` shell block | `github.head_ref` is a branch name and therefore attacker-influenced on any pull-request-triggered run. Interpolating it into a shell line is the documented injection shape. |
| the CI guard → the claim that no page points at a dead tracker | A green step is the only evidence anyone will look at. If the pattern is wrong, the green is a lie that displaces attention. |
| the runner's workspace → three repositories plus a live wiki clone | One job holds every LEGACY-01 surface at once, which is why the guard belongs here; it also means a write-capable token here would reach everything. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-04 | Tampering | the LEGACY-01 grep leg | high | mitigate | The leg is demonstrated RED on both planted dead-tracker forms and GREEN either side before it is trusted; a reach control records the match count without the `.planning` exclusion so a pattern that matches nothing cannot pass as clean |
| T-172-15 | Tampering | the leg's exit-status handling | high | mitigate | The `if grep …; then … exit 1; fi` form is mandated and the inverted `! grep` form is negative-grepped out; under the runner's `bash -e` the inverted form silently fails open |
| T-172-05 | Tampering | `${{ github.head_ref }}` interpolated into the resolver's `run:` block | medium | mitigate | Both expressions move into an `env:` block; the `run:` body is asserted to contain no `${{` sequence |
| T-172-16 | Elevation of privilege | `wiki-check.yml` job permissions | medium | mitigate | `permissions: contents: read` is asserted unchanged; nothing this plan adds needs write |
| T-172-17 | Repudiation | the `.planning/` exclusion | low | accept | The exclusion is a milestone-level scope decision, not a defect cover-up; the reach control records how many matches it hides so the cost stays on the record |
</threat_model>

<verification>
- `--wiki-dir` is gone from the workflow and the dispatch-mirror leg exits 0
- The resolver's `run:` body is plain shell, and simulating it under a scheduled event yields `beta` for both sub-repos
- All three pre-existing legs print `OK: ` against fresh clones
- The LEGACY-01 step exists in the fail-closed form, between the wiki clone and the WIKI-05 leg
- The red/green demonstration recorded two reds and two greens, with a non-zero reach control
- No comment was added to the workflow by either task
</verification>

<success_criteria>
- D-14's precondition is discharged with pasted rc=0 for all three legs, so registering the workflow in plan 172-08 will not produce a red first run
- LEGACY-01 has a mechanical guard covering every surface it names, including the wiki, where edits have no pull request and no review
- The guard has been seen to fail
</success_criteria>

<output>
Create `.planning/phases/172-policy-one-tracker-protected-main/172-03-SUMMARY.md` when done
</output>
