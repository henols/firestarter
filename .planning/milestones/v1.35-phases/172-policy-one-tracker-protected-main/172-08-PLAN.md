---
phase: 172-policy-one-tracker-protected-main
plan: 08
type: execute
wave: 6
depends_on: [172-07]
commits_land_in: three merge commits on the three public `main` branches, made through the GitHub pull request route — plus meta commits on gsd/v1.35-documentation-consolidation-wiki-migration for this plan's evidence files
files_modified:
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-merge-record.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-post-merge-surfaces.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-prefill-check.txt
autonomous: false
requirements: [POLICY-02, LEGACY-01, POLICY-03]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'All four issue-template files are present on `henols/firestarter_prom`''s default branch, read back with `gh api .../contents/.github/ISSUE_TEMPLATE?ref=main` — which is the only place GitHub reads them from'
    - '`.github/CONTRIBUTING.md` is present on the default branch of all three repositories, so GitHub fires its native contribution affordance wherever a pull request is opened'
    - '`wiki-check.yml` is on prom''s default branch and appears in `gh workflow list`, so the LEGACY-01 guard is registered rather than inert'
    - 'All three pull requests reached `MERGED` through the pull-request route, under active rulesets — which is the first live demonstration that PR-only `main` works, and the observation Phase 173''s POLICY-05 has to document'
    - 'The three rulesets still read `enforcement: active` with the same single bypass actor after the merges — merging through the route did not disturb the protection'
    - 'No stable firmware release was cut by the firmware merge: the latest release on `henols/firestarter` predates the merge timestamp'
    - 'Whether `/issues/new?title=&body=` still prefills with templates present has been checked from a logged-in browser and its answer recorded — and if it regressed, the finding is filed rather than fixed, because product-code changes are out of scope for this milestone'
  artifacts:
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-merge-record.txt"
      provides: "The three pull request numbers, merge states, merge commit shas and merge timestamps — the record Phase 173's POLICY-05 cites when it documents the PR-only close procedure"
      min_lines: 8
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-post-merge-surfaces.txt"
      provides: "The default-branch read-backs proving POLICY-02's templates and LEGACY-01's guard are actually live rather than merely constructed"
      min_lines: 12
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-prefill-check.txt"
      provides: "The recorded outcome of the browser prefill check — the one thing in this phase only a logged-in human can observe"
      min_lines: 4
  key_links:
    - from: "`henols/firestarter_prom` default branch"
      to: "the New Issue chooser"
      via: "`.github/ISSUE_TEMPLATE/` read from the default branch, the only place GitHub looks"
      pattern: "contents/.github/ISSUE_TEMPLATE?ref=main"
    - from: "`.github/workflows/wiki-check.yml` on the default branch"
      to: "a registered weekly Actions schedule"
      via: "GitHub registering `schedule` and `workflow_dispatch` triggers from the default branch only"
      pattern: "gh workflow list --repo henols/firestarter_prom"
  prohibitions:
    - statement: "No product-code change is made in response to anything this plan discovers. If the browser prefill tier of `dev test --submit` has regressed, the finding is filed as a backlog item with its evidence, not fixed — REQUIREMENTS.md's scope note is explicit that any product-code change found necessary is filed, not fixed here."
      status: enforced
      verification: "judgment tier — Task 3's verify asserts `firestarter_app/firestarter/submit.py` is byte-unchanged across this phase, and the prefill evidence file records the outcome and, on a negative result, the backlog item rather than a patch."
    - statement: "No merge in this plan is performed with an admin override, a force-push or a temporarily disabled ruleset. The point of these three merges is to exercise the route the rulesets leave open; a merge that bypassed them would demonstrate nothing and would make POLICY-03's claim false in the same act that was supposed to confirm it."
      status: enforced
      verification: "judgment tier — Task 2 re-reads all three rulesets after the merges and asserts `enforcement=active` and the unchanged bypass list; the merge record names the merge method used for each."
---

<objective>
Merge the three `.github`-only pull requests, then prove from the API that the surfaces they carry
are actually live on the default branches — and check the one thing in this phase that only a
logged-in browser can see.

Purpose: POLICY-02 asks that `firestarter_prom` *offers* issue templates. A template on a feature
branch offers nothing; GitHub reads them from the default branch only. The same is true of
`CONTRIBUTING.md` and of a workflow's `schedule` trigger. D-08 chose to make these observable rather
than to record them as "prevention, not remediation", which means the phase owes an observation.

Output: three merges through the pull-request route, three evidence files, and a recorded answer to
the prefill question.

**These merges are also the first live exercise of PR-only `main`.** Nothing else in this milestone
has pushed to a protected default branch since the rulesets went active. Phase 173's POLICY-05 has
to document the close procedure under protection, and this is the observation it will cite.

`autonomous: false`: Task 1 merges into three public default branches and Task 3 is a
`blocking-human` observation.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md
@.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md
@.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md
@firestarter_app/firestarter/submit.py
</context>

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Contributing.md`, the `_Sidebar.md` bullet, the `Home.md` Reference bullet | live wiki | 172-01 |
| The `Contributing` row and prose note in `.planning/v1.35/MIGRATION-TABLE.md` | meta | 172-01 |
| `.github/ISSUE_TEMPLATE/{bug-report.yml,feature-request.yml,dev-test-report.md,config.yml}` | prom | 172-02 |
| The `env:` block, default-branch gate and `LEGACY-01 dead tracker link check` step in `wiki-check.yml` | prom | 172-03 |
| `.github/CONTRIBUTING.md` | prom, firestarter, firestarter_app | 172-04 |
| `evidence/172-05-ruleset-body.json`, `evidence/172-05-ruleset-4998759-pre-delete.json` | meta | 172-05 |
| Ruleset `Protect main` on all three repositories | GitHub control plane | 172-05, 172-06 |
| Branch `policy/contributor-policy` in all three repos, and three pull requests | GitHub | 172-07 |
| Three merge commits on three public `main` branches | GitHub | 172-08 |
| A registered `Wiki check` workflow on `henols/firestarter_prom` | GitHub Actions | 172-08 |
| `evidence/172-01-*` … `evidence/172-09-*` | meta | each plan |

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Merge the three pull requests into three protected default branches</action>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-07-fw-checkruns.txt` — **read this first.** It carries the Pitfall 5 verdict: whether the firmware pull request's own check list shows the firmware CI build, and therefore whether merging it would cut a stable release.
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-07-pr-scope.txt` — the three pull request numbers and their server-side file lists
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-06-ruleset-readback.txt` — the protection these merges will be performed under
  </read_first>
  <instructions>
    Three pull requests are open, each carrying only `.github/` paths, each based on `main`, each
    under an active ruleset. Nothing has been merged.

    **Read the firmware verdict before you decide the order.**
    `evidence/172-07-fw-checkruns.txt` states one of two things:

    - *The firmware CI build check is absent.* The `pull_request` trigger's path filter excluded the
      change, so the `push` trigger's identical filter will exclude the merge too. Merge normally.
    - *The firmware CI build check is present.* Merging would run `build.yml` against `main` and its
      publish steps would cut a stable release with `make_latest` set. **Do not merge the firmware
      pull request.** Take one of the two named fallbacks first — move `CONTRIBUTING.md` to the
      firmware repository root, where community-health precedence still finds it, or add `.github/**`
      to `build.yml`'s `paths-ignore` in that same pull request — then re-read the check list before
      merging.

    Suggested order, prom first: prom's merge registers `wiki-check.yml`, and seeing that land
    cleanly before touching the two sub-repositories keeps the blast radius of a surprise small.

        gh pr merge <prom-pr> --repo henols/firestarter_prom --merge
        gh pr merge <fw-pr>   --repo henols/firestarter      --merge
        gh pr merge <app-pr>  --repo henols/firestarter_app  --merge

    **Merge through the route, not around it.** Do not use an admin override, do not temporarily
    disable a ruleset, and do not force-push. If a merge is refused, that refusal is information
    about the protection and belongs in the record — stop and tell me rather than working around it.
    A merge that bypassed the ruleset would falsify POLICY-03's claim in the same act that was
    supposed to confirm it.

    Expect the PY32F071 workflow to run on the firmware merge. It triggers on every branch with no
    path filter and publishes nothing — one green build, no release.

    Tell me the three pull request numbers and that they are merged, and I will read every surface
    back from the API.
  </instructions>
  <verification>All three pull requests report state `MERGED`; the four template files, three `CONTRIBUTING.md` files and `wiki-check.yml` are all readable at `?ref=main`; `gh workflow list` on prom includes the wiki check; the three rulesets are still active; and the firmware repository's latest release predates the merge. Task 2 runs all of this.</verification>
  <resume-signal>Type "merged" with the three pull request numbers, or "held" naming which was not merged and why.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Read every merged surface back from the default branches and confirm nothing else moved</name>
  <precondition>The three pull requests report state `MERGED`. If any is still `OPEN`, the surfaces below are not live and the assertions must not be run as if they were.</precondition>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-07-pr-scope.txt` — the three pull request numbers this task reads back
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pitfall 9 — registering the workflow turns on two checks that have never run; the record should credit Phase 168 for writing them and this phase for making them run
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pitfall 5 — what a fired `build.yml` would produce, so the release assertion knows what it is looking for
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-06-ruleset-readback.txt` — the ruleset state this task re-asserts to prove the merges went through the route rather than around it
  </read_first>
  <files>.planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-merge-record.txt, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-post-merge-surfaces.txt</files>
  <action>
    Write two evidence files, both from `gh` output only.

    **The merge record.** For each of the three pull requests, record its number, its `state`, its
    `mergedAt` timestamp, its `mergeCommit` sha and its `headRefName`. All three states must read
    `MERGED` and all three head refs must still read `policy/contributor-policy`. This file is what
    Phase 173's POLICY-05 cites when it documents that the pull-request route works under the new
    rulesets — so record the merge method used for each, and record it plainly if any merge needed
    something other than the ordinary route.

    **The post-merge surfaces.** Read each of these from the default branch, because the default
    branch is the only place GitHub looks:

    - the sorted names in `.github/ISSUE_TEMPLATE` on prom at `?ref=main` — all four files
    - `.github/CONTRIBUTING.md` at `?ref=main` on each of the three repositories
    - `.github/workflows/wiki-check.yml` at `?ref=main` on prom
    - the workflow list for prom, which must now include the wiki check alongside the catalog sync
      check that was previously the only registered workflow
    - the three rulesets again: one per repository, `enforcement` active, the bypass list unchanged.
      A merge performed with an override rather than through the route would show up here as a
      changed or disabled ruleset, and this is the cheapest place to catch it.
    - the firmware repository's latest release tag and its `published_at`, and the firmware pull
      request's `mergedAt`. Assert the release predates the merge. If a release was published after
      the merge, `build.yml`'s publish path fired despite the check-run verdict and that is a
      finding, not a footnote — record the tag and stop.

    Add one line to the surfaces file recording, plainly, that registering this workflow also brings
    Phase 168's WIKI-05 reachability check and HONEST-02 truth check to life for the first time.
    They were written in Phase 168 and shipped inert because the workflow has never been on the
    default branch. Phase 168 wrote them; this phase is what makes them run. That belongs on the
    record rather than being absorbed silently into a LEGACY-01 line item.

    Commit both evidence files to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && E=.planning/phases/172-policy-one-tracker-protected-main/evidence && EV=$E/172-08-post-merge-surfaces.txt && : > "$EV" && { echo "templates=$(gh api /repos/henols/firestarter_prom/contents/.github/ISSUE_TEMPLATE?ref=main --jq '[.[].name]|sort|join(",")')"; for r in firestarter_prom firestarter firestarter_app; do echo "contributing_$r=$(gh api "/repos/henols/$r/contents/.github/CONTRIBUTING.md?ref=main" --jq .name)"; done; echo "workflow_file=$(gh api /repos/henols/firestarter_prom/contents/.github/workflows/wiki-check.yml?ref=main --jq .name)"; echo "workflow_registered=$(gh workflow list --repo henols/firestarter_prom --json name --jq '[.[].name]|join(",")')"; for r in firestarter_prom firestarter firestarter_app; do echo "ruleset_$r=$(gh api "/repos/henols/$r/rulesets" --jq length):$(gh api "/repos/henols/$r/rulesets/$(gh api "/repos/henols/$r/rulesets" --jq '.[0].id')" --jq .enforcement)"; done; } >> "$EV" 2>&1; grep -qx 'templates=bug-report.yml,config.yml,dev-test-report.md,feature-request.yml' "$EV" && grep -qx 'contributing_firestarter_prom=CONTRIBUTING.md' "$EV" && grep -qx 'contributing_firestarter=CONTRIBUTING.md' "$EV" && grep -qx 'contributing_firestarter_app=CONTRIBUTING.md' "$EV" && grep -qx 'workflow_file=wiki-check.yml' "$EV" && grep -q 'workflow_registered=.*Wiki check' "$EV" && test "$(grep -c '^ruleset_.*=1:active$' "$EV")" -eq 3</automated>
    <fails_when>the `templates=` line is not exactly the four sorted filenames, or the API returns `Not Found` for that path at `?ref=main` — the templates did not reach the default branch and POLICY-02 is not observable; or any `contributing_` line is not `CONTRIBUTING.md`; or `workflow_file=` is absent, meaning the guard is still inert; or `workflow_registered=` does not contain the wiki check, meaning the file landed but Actions has not registered it; or fewer than three `ruleset_*=1:active` lines, meaning a merge disturbed the protection or was performed around it</fails_when>
    <automated>cd /workspaces && E=.planning/phases/172-policy-one-tracker-protected-main/evidence && EV=$E/172-08-merge-record.txt && : > "$EV" && ok=1 && for r in firestarter_prom firestarter firestarter_app; do N=$(gh pr list --repo "henols/$r" --state merged --head policy/contributor-policy --json number --jq '.[0].number'); test -n "$N" || { ok=0; continue; }; echo "$r pr=$N state=$(gh pr view "$N" --repo "henols/$r" --json state --jq .state) mergedAt=$(gh pr view "$N" --repo "henols/$r" --json mergedAt --jq .mergedAt) head=$(gh pr view "$N" --repo "henols/$r" --json headRefName --jq .headRefName) mergeCommit=$(gh pr view "$N" --repo "henols/$r" --json mergeCommit --jq .mergeCommit.oid)" >> "$EV"; done; FWN=$(gh pr list --repo henols/firestarter --state merged --head policy/contributor-policy --json number --jq '.[0].number') && FWM=$(gh pr view "$FWN" --repo henols/firestarter --json mergedAt --jq .mergedAt) && REL=$(gh api /repos/henols/firestarter/releases/latest --jq '.tag_name + " " + .published_at') && echo "latest_firmware_release=$REL merged_at=$FWM" >> "$EV" && test "$ok" = 1 && test "$(grep -c 'state=MERGED' "$EV")" -eq 3 && test "$(grep -c 'head=policy/contributor-policy' "$EV")" -eq 3 && [[ "$(echo "$REL" | awk '{print $2}')" < "$FWM" ]]</automated>
    <fails_when>fewer than three `state=MERGED` lines — a pull request is still open or was closed unmerged; or any `head=` is not `policy/contributor-policy`; or the latest firmware release's `published_at` is not strictly earlier than the firmware pull request's `mergedAt`, which means `build.yml`'s publish path fired on the merge and cut a stable release the phase did not intend</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh api /repos/henols/firestarter_prom/contents/.github/ISSUE_TEMPLATE?ref=main` returns exactly the four filenames `bug-report.yml`, `config.yml`, `dev-test-report.md`, `feature-request.yml`
    - `.github/CONTRIBUTING.md` resolves at `?ref=main` in all three repositories
    - `.github/workflows/wiki-check.yml` resolves at `?ref=main` on prom, and `gh workflow list --repo henols/firestarter_prom` names the wiki check
    - All three pull requests report `state: MERGED` with head `policy/contributor-policy`, and each merge commit sha is recorded
    - All three repositories still return exactly one ruleset with `enforcement: active` and the single `Integration`/15368 bypass
    - `henols/firestarter`'s latest release `published_at` is strictly earlier than the firmware pull request's `mergedAt` — no stable release was cut by the merge
    - `evidence/172-08-post-merge-surfaces.txt` carries the line recording that this registration also wakes Phase 168's WIKI-05 and HONEST-02 checks for the first time, and credits Phase 168 for writing them
    - Every value in both evidence files came from `gh` output; neither file references the settings page or the rendered New Issue page
  </acceptance_criteria>
  <done>POLICY-02's templates and LEGACY-01's guard are provably live on the default branches; the three merges went through the pull-request route with the protection intact; and no firmware release was cut.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    All three pull requests are merged. The four issue templates, the three `CONTRIBUTING.md` files
    and `wiki-check.yml` are all readable at `?ref=main`, and the wiki check is registered with
    Actions. The three rulesets are still active with the single Actions bypass.

    **One question remains and only a logged-in browser can answer it.**

    D-07 keeps `blank_issues_enabled: true` specifically so that
    `https://github.com/henols/firestarter_prom/issues/new?title=…&body=…` still opens a prefilled
    blank issue. That URL is built by `firestarter_app/firestarter/submit.py:283` and it is the
    browser tier of `firestarter dev test <chip> --submit` — the tier that exists for testers who do
    not have `gh` installed. GitHub documents `config.yml` as customising *the chooser*; no page I
    could reach says whether `/issues/new` with query parameters still bypasses the chooser once
    templates exist. Unauthenticated probes only ever return a login redirect with the query intact,
    which is consistent with the prefill surviving but is not proof.

    It could not be checked before now: the templates only take effect from the default branch, which
    they reached minutes ago.
  </what-built>
  <how-to-verify>
    While logged in to GitHub, open a URL of the exact shape `submit.py` builds — the issues path with
    both a `title` and a `body` query parameter — against `henols/firestarter_prom`. The simplest way
    to get a real one is to run `firestarter dev test <chip> --submit` against a board and let it open
    the browser, but a hand-built URL of the same shape answers the question equally well.

    Then report which of these you see:

    **Prefilled.** A new-issue form opens with the title and body already filled in. D-07 holds, the
    browser tier is intact, and nothing further is owed.

    **The chooser.** You are redirected to the template chooser and the prefill is lost. That is a
    regression in the browser tier of `dev test --submit`. **It is filed, not fixed.** REQUIREMENTS.md
    is explicit that any product-code change found necessary in this milestone is filed rather than
    fixed, and `submit.py` is product code this phase may not edit. I will write a backlog item with
    the URL, the observed behaviour and the two candidate remedies, and Phase 173's honesty ledger
    will carry it.

    Also worth glancing at while you are there, though neither is a gate: the New Issue page should
    now offer the bug report, the feature request and the chip validation report, and should still
    offer a blank issue.
  </how-to-verify>
  <resume-signal>Type "prefilled" or "chooser", and I will record the outcome either way. On "chooser" I will also write the backlog item and will not touch `submit.py`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pull request → a protected default branch | The merge is the moment the protection is either honoured or bypassed. How it was done is as much a fact about POLICY-03 as the ruleset JSON is. |
| a merge to `main` in `firestarter` → the release machinery | A push to `main` is a release trigger unless a path filter excludes it. A documentation merge that publishes a firmware binary is a supply-chain event. |
| the default branch → every anonymous visitor | What lands here is what strangers are shown on the New Issue page. It is the first thing an outsider reads and the last thing anyone reviews. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-32 | Elevation of privilege | a merge performed with an admin override | high | mitigate | The human-action instructions forbid override, ruleset disabling and force-push and say to stop and report a refusal; Task 2 re-reads all three rulesets after the merges and asserts them unchanged and active |
| T-172-28 | Tampering | an unintended stable firmware release | high | mitigate | The Pitfall 5 verdict is read before the merge order is chosen, with two named fallbacks; Task 2 asserts the latest release predates the firmware merge timestamp |
| T-172-33 | Repudiation | claiming POLICY-02 is met from a feature branch | high | mitigate | Every surface is read at `?ref=main`; a template on the milestone branch offers nothing and the `?ref=main` read is the difference between constructed and observable |
| T-172-13 | Denial of service | the browser tier of `dev test --submit` losing its prefill | medium | accept | Checked from a logged-in browser after the merge, which is the earliest it can be checked. A negative result is filed as a backlog item; the product-code fix is out of scope by the milestone's own scope note |
| T-172-34 | Tampering | a newly registered weekly workflow going red on its first cron | medium | mitigate | Plan 172-03 ran all three pre-existing legs green against fresh clones under the fixed invocation before the workflow was carried into this pull request |
</threat_model>

<verification>
- Four template filenames at `?ref=main` on prom
- `CONTRIBUTING.md` at `?ref=main` in all three repositories
- `wiki-check.yml` at `?ref=main` and registered in `gh workflow list`
- Three `MERGED` pull requests with head `policy/contributor-policy`
- Three rulesets still `1:active`
- Latest firmware release predates the firmware merge
- The prefill outcome recorded either way
</verification>

<success_criteria>
- POLICY-02 is observable rather than constructed: the templates are on the surface a filer actually sees
- LEGACY-01's guard is registered and will run, and so, for the first time, will Phase 168's two checks
- PR-only `main` has been exercised once, with the record Phase 173's POLICY-05 needs
- Nothing was fixed that the milestone said should be filed
</success_criteria>

<output>
Create `.planning/phases/172-policy-one-tracker-protected-main/172-08-SUMMARY.md` when done
</output>
