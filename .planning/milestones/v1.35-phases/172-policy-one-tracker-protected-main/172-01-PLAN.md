---
phase: 172-policy-one-tracker-protected-main
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter_prom.wiki.git (live public wiki) — plus meta commits on gsd/v1.35-documentation-consolidation-wiki-migration for this plan's evidence files and the MIGRATION-TABLE.md row
files_modified:
  - .planning/v1.35/MIGRATION-TABLE.md
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-postpush-freshclone.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-migration-table-parse.txt
autonomous: false
requirements: [POLICY-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'The `Contributing` page, its `_Sidebar.md` entry and its `Home.md` link land in a single wiki commit; `git show --stat --format="" HEAD` on the wiki clone names exactly those three files. A partial push leaves `wiki.py links` red.'
    - '`Contributing.md` exists on the live `firestarter_prom` wiki and is present in a fresh, independent clone of it — not merely in the clone the executor edited'
    - 'The page carries all three POLICY-01 statements: `firestarter_prom` is the only tracker, both sub-repos have Issues disabled, and pull requests go to the repository containing the changed code'
    - 'The page carries gh#9''s four-step cross-repository protocol relocated intact, and D-04''s single security sentence stating the non-claim'
    - '`python3 tools/wiki/wiki.py links --source-dir <fresh clone>` reports `OK: 11 pages,` with rc=0 and lists `Contributing -> "Contributing"`'
    - 'The published page''s lines 3-5 are `---`, an empty line, and `# Contributing` — the same opening shape the other ten live pages use'
    - '`.planning/v1.35/MIGRATION-TABLE.md` carries the `Contributing` row with the U+2014 em dash in both the Source path and Pre-deletion SHA columns, so `parse_migration_table` still returns exactly 8 rows with a SHA'
  artifacts:
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt"
      provides: "The pre-push oracle output — currently the only gate on a wiki edit, because wiki-check.yml is not registered with Actions until plan 172-08"
      min_lines: 12
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-postpush-freshclone.txt"
      provides: "Proof the push landed, taken against a second independent clone rather than the working copy"
      min_lines: 12
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-migration-table-parse.txt"
      provides: "Proof the authored Contributing row leaves honest01_claims.parse_migration_table returning 8 rows with a SHA, preserving Phase 171's assertion"
      min_lines: 9
    - path: ".planning/v1.35/MIGRATION-TABLE.md"
      provides: "The provenance record for the eleventh wiki page, and the input the Backlog 999.9 rename sweep greps"
      min_lines: 160
  key_links:
    - from: "wiki `Home.md` Reference list"
      to: "wiki `Contributing.md`"
      via: "a markdown link whose text and target are both the bare stem"
      pattern: "[Contributing](Contributing)"
    - from: "wiki `_Sidebar.md`"
      to: "wiki `Contributing.md`"
      via: "a bullet appended after the Shell-Completion entry"
      pattern: "- [Contributing](Contributing)"
    - from: "`.planning/v1.35/MIGRATION-TABLE.md` main table"
      to: "the `Contributing` wiki page"
      via: "an authored-page row whose Pre-deletion SHA column holds the U+2014 no-SHA marker"
      pattern: "| firestarter_prom | — | Contributing | Contributing | — | 172 |"
  prohibitions:
    - statement: "No page, template or pointer file written by this phase claims a channel, commitment or response the project does not have. The security sentence on this page states the non-claim — there is no private disclosure channel — and does not promise a response time, a triage window, an embargo process or a support commitment (D-04; Phase 171 D-02 handed exactly this forward)."
      status: enforced
      verification: "judgment tier — no mechanical check is wired. Task 1's acceptance criteria assert the security section is one sentence and that it contains no response-time, embargo or private-channel wording; the reviewer reads it."
    - statement: "This plan authors no new topic. It relocates gh#9's existing text and adds one sentence. No code of conduct, no support policy, no roadmap page, no restatement of anything that already lives on another wiki page."
      status: enforced
      verification: "judgment tier — the fresh clone holds exactly 11 pages plus `_Sidebar.md`, and every file other than `Home.md`, `_Sidebar.md` and the new `Contributing.md` is byte-unchanged."
---

<objective>
Publish the canonical `Contributing` page as the eleventh page of the live `firestarter_prom` wiki
— the single place POLICY-01's three statements are made — with the two navigation edits a new page
owes, in one push, and record its provenance in `.planning/v1.35/MIGRATION-TABLE.md`.

Purpose: POLICY-01 asks for the policy to be stated **once**, canonically, and linked from the other
two repositories rather than restated in each. Nothing else in this phase can satisfy "linked" until
the link target exists. Plans 172-04 (three pointer files, three README trims) depend on this page
being live.

Output: one new live wiki page, two navigation edits, one provenance row plus a prose note, and
three evidence files.

**Tracer note.** Task 1 is `type="tracer"`: the thinnest complete path through this phase's
documentation layer — author, navigate, gate, publish, re-verify from an independent clone. It is a
tracer and not a prototype: the page it publishes is the final page. The tracer deliberately stops
at the documentation layer rather than reaching the GitHub control plane, because **D-12 is a locked
decision that puts the rulesets last**; a tracer slice that created a ruleset would violate it.

**This plan writes to a live, public wiki.** `autonomous: false` is deliberate. This project runs GSD
with `--chain`, which auto-approves `human-verify` and `decision` checkpoints; only `human-action`
actually stops. The push in Task 2 is therefore a `checkpoint:human-action`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md
@.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md
@.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md
@.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md
@tools/wiki/wiki.py
@.planning/v1.35/MIGRATION-TABLE.md
</context>

## Artifacts this phase produces

Every new file and named artifact Phase 172 creates. Drift checks must treat these as new, not as
unresolved references.

| Artifact | Repo / location | Created by |
|---|---|---|
| `Contributing.md` | `firestarter_prom.wiki.git` (live wiki, root) | 172-01 |
| `_Sidebar.md` bullet `- [Contributing](Contributing)` | live wiki | 172-01 |
| `Home.md` Reference-list bullet linking to `Contributing` | live wiki | 172-01 |
| The `Contributing` row in `.planning/v1.35/MIGRATION-TABLE.md`'s main table | meta | 172-01 |
| The authored-not-migrated prose note under that table | meta | 172-01 |
| `.github/ISSUE_TEMPLATE/bug-report.yml` | prom | 172-02 |
| `.github/ISSUE_TEMPLATE/feature-request.yml` | prom | 172-02 |
| `.github/ISSUE_TEMPLATE/dev-test-report.md` | prom | 172-02 |
| `.github/ISSUE_TEMPLATE/config.yml` | prom | 172-02 |
| The `env:` block and default-branch gate in `wiki-check.yml`'s resolver step | prom | 172-03 |
| The `LEGACY-01 dead tracker link check` step in `wiki-check.yml` | prom | 172-03 |
| `.github/CONTRIBUTING.md` | prom | 172-04 |
| `.github/CONTRIBUTING.md` | firestarter | 172-04 |
| `.github/CONTRIBUTING.md` | firestarter_app | 172-04 |
| Ruleset `Protect main` on `henols/firestarter_prom` | GitHub control plane | 172-05 |
| Ruleset `Protect main` on `henols/firestarter` (new id) | GitHub control plane | 172-06 |
| Ruleset `Protect main` on `henols/firestarter_app` | GitHub control plane | 172-06 |
| Branch `policy/contributor-policy` in all three repos, and three pull requests | GitHub | 172-07 |
| `evidence/172-01-*.txt` … `evidence/172-09-*.txt`, `evidence/172-05-ruleset-4998759-pre-delete.json` | meta | each plan |

<tasks>

<task type="tracer">
  <name>Task 1: Author Contributing.md, make both navigation edits, and gate on wiki.py links — one path, end to end</name>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` § "The `Contributing` wiki page — shape and navigation" — the verbatim page opening, the verbatim current `_Sidebar.md`, and the `LEGAL_LINK_RE` that decides what counts as a link
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` § "D-01's source text (gh#9), fetched this session" — the three statements and the four-step protocol this page relocates
    - `.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md` §1 — the analog page (`Shell-Completion.md`), the publish mechanics, and the verify-command idioms
    - `.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md` D-01 and D-04 — what the page is for, and the exact shape of the security sentence
    - `tools/wiki/wiki.py:39` (`LEGAL_LINK_RE`), `:50-52` (`render_title`, `_LEGAL_TARGET_RE`) and `:210-266` (`check_orphans`, `check_sidebar_lists_every_page`, `cmd_links`) — these define the success string and both failure messages
  </read_first>
  <files>working clone of firestarter_prom.wiki.git at /tmp/gsd-172-wiki (deterministic scratch path, outside all three repositories), .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt</files>
  <action>
    Export `WIKI=/tmp/gsd-172-wiki` and use that exact path in every task of this plan so the clone
    survives between tasks. Clone `https://github.com/henols/firestarter_prom.wiki.git` into it; the
    clone is anonymous and needs no token. It must live outside `/workspaces`,
    `/workspaces/firestarter` and `/workspaces/firestarter_app` — `/workspaces/CLAUDE.md` states
    documentation lives only in the wiki and there is no in-repo copy, so nothing produced here may
    ever be `git add`ed in meta or either submodule.

    Fetch the source text with `gh issue view 9 --repo henols/firestarter_prom --json body --jq .body`.
    This is a relocation of the operator's own words (D-01), not an authoring exercise. Do not
    paraphrase the three statements or the four-step protocol; carry their substance across and adapt
    only the framing to a wiki page.

    Create `$WIKI/Contributing.md`. Lines 1-6 must be byte-identical to the opening every other live
    page carries: the logo paragraph
    `<p align="left"><img src="https://raw.githubusercontent.com/henols/firestarter_app/refs/heads/main/images/firestarter_logo.png" alt="Firestarter EPROM Programmer" width="200"></p>`,
    an empty line, `---`, an empty line, `# Contributing`, an empty line. Copy the logo line from
    `$WIKI/Home.md:1` rather than retyping the URL.

    Body sections, in this order, with these H2 headings:

    (a) `## Where to report a problem` — states that `firestarter_prom` is the **only** repository
    where GitHub Issues are maintained, that Issues are disabled on both `firestarter` and
    `firestarter_app`, and that every bug report and feature request goes to
    `https://github.com/henols/firestarter_prom/issues` whichever part of the system it concerns.
    That URL is the live tracker and is the only issue-tracker URL permitted on this page.

    (b) `## Where to open a pull request` — states that pull requests go to the repository
    containing the code being changed, followed by the three-way routing gh#9 gives: application to
    `firestarter_app`, firmware to `firestarter`, project documentation or planning to
    `firestarter_prom`.

    (c) `## Changes that span repositories` — the four numbered steps from gh#9, relocated intact:
    create a single issue in `firestarter_prom`; reference that issue from each implementation PR;
    keep design discussion in the issue; keep implementation discussion in the relevant PRs.

    (d) `## Security reports` — exactly one sentence (D-04). It states the non-claim: Firestarter has
    no private disclosure channel, so a security report goes on the public tracker with everything
    else. Do not offer an email address, a response time, an embargo, a triage window or a support
    commitment. The project has none of those and a page implying one would be the exact class of
    false claim this milestone exists to remove.

    Write no HTML comment of any kind, no claim stamp and no provenance marker. The project's hard
    no-comments rule covers this file. Do not add a code of conduct, a support policy, a style guide
    or a restatement of anything that already lives on another wiki page.

    Navigation — both edits in this same change, or the checker fails:

    (e) `$WIKI/_Sidebar.md` — append a new line 11 directly after the existing
    `- [Contributing](Contributing)`-shaped neighbour `- [Shell-Completion](Shell-Completion)` on
    line 10. The bullet's link text and target are both the bare stem `Contributing`. The list is in
    the order pages were added, not alphabetical — append, do not sort.

    (f) `$WIKI/Home.md` — append a new bullet to the end of the `## Reference` list, directly after
    the `- [Shell-Completion](Shell-Completion) — turning on tab completion for ...` bullet on line
    50 and before the blank line that precedes the `---` on line 52. Row shape matches its
    neighbours: hyphen, space, a markdown link whose text and target are both `Contributing`, space,
    a U+2014 em dash, space, then a lower-case clause with no terminating period — use
    `where to report a problem, where to open a pull request, and how cross-repository changes work`.
    Only the bare-stem link form satisfies the orphan check; an absolute
    `https://github.com/.../wiki/Contributing` URL would not. Do not touch `Home.md:5`.

    Then run the gate. From `/workspaces`, run `python3 tools/wiki/wiki.py links --source-dir "$WIKI"`
    and require rc=0, a stdout line beginning `OK: 11 pages,` and a stdout line reading
    `Contributing -> "Contributing"`. Write full stdout, stderr and the observed exit status to
    `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt`.
    A rc=1 names its own cause: `page missing from _Sidebar.md: Contributing` means edit (e) is
    absent; `orphan page not reachable from Home.md by any link path: Contributing` means edit (f)
    is absent. This local run is the only gate that exists for a wiki edit until plan 172-08
    registers the workflow, so treat a non-zero exit as a hard stop.

    Finally stage exactly the three files in the clone and create ONE commit — do not push. Task 2
    owns the push. Subject-line form is the wiki repository's own, `docs:` followed by what the page
    is for, with no GSD scope; the wiki repo carries no GSD scoping.

    Commit the evidence file to meta with a path-scoped `git commit -- <path>`. Meta's working tree
    already carries unrelated modifications before this phase begins; none of them are yours and none
    may be swept into this commit.
  </action>
  <verify>
    <automated>WIKI=/tmp/gsd-172-wiki; EV=/workspaces/.planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt; cd /workspaces && python3 tools/wiki/wiki.py links --source-dir "$WIKI" > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -q '^OK: 11 pages,' "$EV" && grep -qF 'Contributing -> "Contributing"' "$EV" && grep -qx 'exit 0' "$EV" && sed -n '3,5p' "$WIKI/Contributing.md" | tr '\n' '|' | grep -qx -- '---||# Contributing|' && ! grep -q '<!--' "$WIKI/Contributing.md" && test "$(git -C "$WIKI" show --stat --format="" HEAD | grep -cE 'Home\.md|_Sidebar\.md|Contributing\.md')" -eq 3 && test "$(git -C "$WIKI" log origin/master..HEAD --oneline | wc -l)" -eq 1</automated>
    <fails_when>the evidence file's last line is not `exit 0`; or it says `OK: 10 pages,` (the new page is not counted); or stderr carries `page missing from _Sidebar.md: Contributing` or `orphan page not reachable from Home.md by any link path: Contributing`; or the `sed -n '3,5p'` pipeline prints anything other than `---||# Contributing|`; or the one-push count is not 3; or the unpushed-commit count is not 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `$WIKI/Contributing.md` exists and its first six lines are byte-identical to `$WIKI/Home.md`'s first four lines followed by `# Contributing` and an empty line, with only the H1 text differing
    - `sed -n '3,5p' "$WIKI/Contributing.md" | tr '\n' '|'` outputs exactly `---||# Contributing|`
    - `grep -c '^## ' "$WIKI/Contributing.md"` returns exactly 4, and `grep -o '^## .*' "$WIKI/Contributing.md"` lists, in order, `## Where to report a problem`, `## Where to open a pull request`, `## Changes that span repositories`, `## Security reports`
    - `grep -qF 'https://github.com/henols/firestarter_prom/issues' "$WIKI/Contributing.md"` succeeds, and `grep -cE 'henols/firestarter(_app)?/issues' "$WIKI/Contributing.md"` returns 0 — the page names the live tracker and neither disabled one
    - `grep -qF 'firestarter_app' "$WIKI/Contributing.md"` and `grep -qF 'firestarter_prom' "$WIKI/Contributing.md"` both succeed, and the pull-request section names all three destinations
    - The `## Changes that span repositories` section contains four numbered list items, matching gh#9's four steps
    - The `## Security reports` section body is exactly one sentence; `grep -icE 'response time|within [0-9]+ (hour|day)|embargo|security@|private (report|disclosure) (channel|process) (is|are) available|we will respond' "$WIKI/Contributing.md"` returns 0
    - `grep -c '<!--' "$WIKI/Contributing.md"` returns 0 — no comment, no provenance marker, no claim stamp
    - `tail -n 1 "$WIKI/_Sidebar.md"` is exactly `- [Contributing](Contributing)` and `wc -l < "$WIKI/_Sidebar.md"` reports 11
    - `grep -c '(Contributing)' "$WIKI/Home.md"` returns exactly 1, that line sits inside the `## Reference` list, and it contains a U+2014 em dash
    - `git -C "$WIKI" show --stat --format="" HEAD` names exactly three files: `Contributing.md` created, `Home.md` +1, `_Sidebar.md` +1
    - `python3 tools/wiki/wiki.py links --source-dir "$WIKI"` exits 0 with `OK: 11 pages,` and `Contributing -> "Contributing"` on stdout
    - `git -C "$WIKI" log origin/master..HEAD --oneline | wc -l` returns 1 — nothing was pushed by this task
    - No tracked file in `/workspaces`, `/workspaces/firestarter` or `/workspaces/firestarter_app` was created or modified except the one evidence file
  </acceptance_criteria>
  <done>The wiki clone holds a shape-correct `Contributing.md` carrying all three POLICY-01 statements, gh#9's four-step protocol and D-04's single security sentence, plus both navigation edits, in one local commit; the only oracle that exists has been run and recorded green; nothing has been pushed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Push the single commit to the live public `firestarter_prom` wiki</action>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-prepush.txt` — the pre-flight oracle output Task 1 produced
    - `.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md` §1 "Publish mechanics" — the Phase 171 plan 01 shape this mirrors
  </read_first>
  <instructions>
    I have prepared one local commit in a clone of the live public wiki at `/tmp/gsd-172-wiki`. It
    adds `Contributing.md` — the canonical statement of who takes issues, where pull requests go, how
    a cross-repository change is coordinated, and the fact that there is no private security channel
    — and appends one navigation line to each of `_Sidebar.md` and `Home.md`. `wiki.py links` has
    been run against it and exits 0 with `OK: 11 pages,`.

    Nothing has been pushed. The wiki is public, has no pull request, no review and no CI gate on the
    edit, so this push is the point of no return for the page's visibility.

    Review the commit, then push it:

        git -C /tmp/gsd-172-wiki show
        git -C /tmp/gsd-172-wiki push origin HEAD

    If the remote's default branch is `master` rather than `main`, `HEAD` already resolves correctly
    — do not rename anything.
  </instructions>
  <verification>A second, independent `--depth 1` clone of the wiki contains `Contributing.md`, and `wiki.py links` against that fresh clone exits 0 with `OK: 11 pages,`. Task 3 runs this and records it.</verification>
  <resume-signal>Type "pushed" when the push has completed, or describe what went wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Re-verify from an independent fresh clone, then record provenance in MIGRATION-TABLE.md</name>
  <read_first>
    - `tools/wiki/honest01_claims.py:49` (`NO_SHA_MARKER = "—"`, U+2014) and `:93` (the filter that keeps only rows whose `Pre-deletion SHA` differs from that marker) — this is why the em dash in the new row is load-bearing
    - `.planning/v1.35/MIGRATION-TABLE.md:10-20` — the main table, its six columns, and the two existing rows this new row is modelled on: the authored `Home` row at line 12 and Phase 171's `Shell-Completion` row at line 20
    - `.planning/v1.35/MIGRATION-TABLE.md:30-44` — the existing prose notes for `How-To-Edit-This-Wiki` and the two renames; the new note goes directly after the paragraph ending `see the honesty note below.` and before the `## Retired from the wiki after the migration closed` heading
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pitfall 6 — the measured before/after showing the count stays at 8
  </read_first>
  <files>.planning/v1.35/MIGRATION-TABLE.md, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-postpush-freshclone.txt, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-migration-table-parse.txt</files>
  <action>
    First, prove the push landed against a clone the executor did not create the commit in. Make a
    temp directory, `git clone --depth 1 https://github.com/henols/firestarter_prom.wiki.git` into
    it, and run `python3 tools/wiki/wiki.py links --source-dir <that clone>` from `/workspaces`.
    Require rc=0, `OK: 11 pages,` and `Contributing -> "Contributing"`. Write stdout, stderr and the
    exit status to `evidence/172-01-wiki-links-postpush-freshclone.txt`. Never substitute
    `/tmp/gsd-172-wiki` here — the working clone cannot prove a push.

    Then add one row to the main table in `.planning/v1.35/MIGRATION-TABLE.md`, immediately after the
    `Shell-Completion` row on line 20:

    Source repo `firestarter_prom`; Source path a U+2014 em dash; Wiki page `Contributing`; Rendered
    title `Contributing`; Pre-deletion SHA a U+2014 em dash; Moved in `172`.

    The em dash in the Pre-deletion SHA column is not cosmetic. `honest01_claims.py` treats that exact
    character as the no-SHA marker and filters the row out of `parse_migration_table`, which keeps the
    count at 8 and preserves the assertion Phase 171 plan 03 made. Any other value — an empty cell, a
    hyphen-minus, `n/a`, `none` — silently changes what the provenance checker counts. Copy the
    character from the existing `Home` row on line 12 rather than typing it.

    Then add one prose paragraph directly after the paragraph that ends
    `see the honesty note below.` and before the `## Retired from the wiki after the migration closed`
    heading. It records that `Contributing` was **authored from gh#9, not migrated**: it has no `doc/`
    source and therefore no pre-deletion SHA, its text is the operator's own from issue 9 relocated
    to a wiki page, and it was added in Phase 172 under POLICY-01. Keep it to the register and length
    of the neighbouring notes. Write no HTML comment.

    Finally run the parse oracle and record it. Use the Phase 171 invocation form
    (`171-03-PLAN.md:278`), writing the row count and the surviving page list to
    `evidence/172-01-migration-table-parse.txt`. Require `rows with a SHA: 8`.

    Commit the table edit and both evidence files to meta with a path-scoped
    `git commit -- <paths>`. Do not `git add -A`.
  </action>
  <verify>
    <automated>V=$(mktemp -d); EV=/workspaces/.planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-wiki-links-postpush-freshclone.txt; git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$V/wiki" && cd /workspaces && python3 tools/wiki/wiki.py links --source-dir "$V/wiki" > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -q '^OK: 11 pages,' "$EV" && grep -qF 'Contributing -> "Contributing"' "$EV" && grep -qx 'exit 0' "$EV" && test -f "$V/wiki/Contributing.md" && sed -n '3,5p' "$V/wiki/Contributing.md" | tr '\n' '|' | grep -qx -- '---||# Contributing|'</automated>
    <fails_when>`git clone` prints `Repository not found`; or the evidence file's last line is not `exit 0`; or it reports `OK: 10 pages,` (the push did not land); or `$V/wiki/Contributing.md` is absent</fails_when>
    <automated>EV=/workspaces/.planning/phases/172-policy-one-tracker-protected-main/evidence/172-01-migration-table-parse.txt; cd /workspaces && python3 -c "import sys; sys.path.insert(0,'tools/wiki'); from pathlib import Path; from honest01_claims import parse_migration_table; rows=parse_migration_table(Path('.planning/v1.35/MIGRATION-TABLE.md')); print('rows with a SHA:', len(rows)); [print(' ', r['Source path'], '->', r['Wiki page'], r['Moved in']) for r in rows]" > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -qx 'rows with a SHA: 8' "$EV" && grep -qx 'exit 0' "$EV" && grep -qF '| firestarter_prom | — | Contributing | Contributing | — | 172 |' .planning/v1.35/MIGRATION-TABLE.md && ! grep -qF 'Contributing' "$EV"</automated>
    <fails_when>the evidence file reports `rows with a SHA: 9` (the em dash was not used and the authored row leaked into the SHA-bearing set); or it reports `rows with a SHA: 8` but the exact pipe-delimited Contributing row is absent from the table; or `Contributing` appears in the parsed row listing</fails_when>
  </verify>
  <acceptance_criteria>
    - A fresh `--depth 1` clone of `firestarter_prom.wiki.git`, created in this task, contains `Contributing.md`, `_Sidebar.md` with 11 lines, and a `Home.md` carrying exactly one `(Contributing)` link
    - `python3 tools/wiki/wiki.py links --source-dir <fresh clone>` exits 0 and prints `OK: 11 pages,` and `Contributing -> "Contributing"`
    - `grep -c '^| ' .planning/v1.35/MIGRATION-TABLE.md` counts one more row than before this task in the main table, and `sed -n '21p' .planning/v1.35/MIGRATION-TABLE.md` is exactly `| firestarter_prom | — | Contributing | Contributing | — | 172 |`
    - The two em dashes in that row are U+2014, identical to the ones on line 12 — verified with `sed -n '21p' .planning/v1.35/MIGRATION-TABLE.md | grep -c $'—'` returning 2
    - `parse_migration_table` over the edited table returns exactly 8 rows and `Contributing` is not among them
    - A prose paragraph containing the words `authored` and `not migrated` and referencing issue 9 sits between the renames paragraph and the `## Retired from the wiki after the migration closed` heading
    - `grep -c '<!--' .planning/v1.35/MIGRATION-TABLE.md` returns 0
    - `git show --stat --format="" HEAD` on meta names only `.planning/v1.35/MIGRATION-TABLE.md` and the two evidence files added by this task
  </acceptance_criteria>
  <done>The page is proven live from an independent clone with `OK: 11 pages,`; `MIGRATION-TABLE.md` records its provenance as authored-not-migrated without disturbing the SHA-bearing row count; three evidence files are committed to meta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's `gh`/`git` credentials → public wiki | A push to `firestarter_prom.wiki.git` has no pull request, no review and no CI gate. Whatever is pushed is immediately the project's canonical policy statement. |
| the `Contributing` page → an untrusted reader | Every sentence is a claim a stranger will act on. A claim the project cannot back is the failure mode this milestone exists to remove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-09 | Repudiation | `Contributing.md` `## Security reports` section | medium | mitigate | D-04 restricts the section to one sentence stating the non-claim; Task 1's acceptance criteria negative-grep for response-time, embargo and private-channel wording |
| T-172-10 | Denial of service (social) | `Contributing.md` tracker links | high | mitigate | The page may name only `https://github.com/henols/firestarter_prom/issues`; acceptance criteria assert zero matches for either disabled tracker, and plan 172-03's guard enforces it thereafter |
| T-172-11 | Tampering | unreviewed push to a public wiki | medium | mitigate | The push is a `checkpoint:human-action`; the pre-push oracle runs first, and Task 3 re-verifies from an independent clone rather than trusting the working copy |
| T-172-12 | Repudiation | `MIGRATION-TABLE.md` provenance row | low | mitigate | The em-dash no-SHA marker is asserted with a codepoint count, and the parse oracle proves the SHA-bearing count is unchanged at 8 |
</threat_model>

<verification>
- `python3 tools/wiki/wiki.py links --source-dir <fresh clone>` exits 0 with `OK: 11 pages,` and `Contributing -> "Contributing"`
- Both wiki-links evidence files exist, are non-empty, and end with `exit 0`
- `parse_migration_table` returns 8 rows with a SHA, and `Contributing` is not one of them
- The wiki commit touched exactly three files
- No HTML comment appears in `Contributing.md` or `MIGRATION-TABLE.md`
</verification>

<success_criteria>
- POLICY-01's three statements exist on exactly one page, and that page is live and reachable
- gh#9's four-step cross-repository protocol is relocated intact
- D-04's security sentence states the non-claim and promises nothing
- The provenance of the eleventh page is answerable from `MIGRATION-TABLE.md` alone
- Plans 172-04's pointer files now have a real link target
</success_criteria>

<output>
Create `.planning/phases/172-policy-one-tracker-protected-main/172-01-SUMMARY.md` when done
</output>
