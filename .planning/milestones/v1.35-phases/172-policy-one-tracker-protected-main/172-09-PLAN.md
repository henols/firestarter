---
phase: 172-policy-one-tracker-protected-main
plan: 09
type: execute
wave: 7
depends_on: [172-08]
commits_land_in: firestarter_prom (meta, /workspaces) on gsd/v1.35-documentation-consolidation-wiki-migration — including one commit that re-pins both submodule gitlinks
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-full-suite-final.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-legacy01-final.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-gitlink-equality.txt
  - .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt
autonomous: true
requirements: [POLICY-01, POLICY-02, POLICY-03, LEGACY-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - 'The three `tools/wiki/` checkers pass against a **fresh** wiki clone and **fresh** `beta` clones of both sub-repositories — not against the local working trees — with the eleventh page present'
    - 'The LEGACY-01 grep leg is green over the final state of all four surfaces, including the new wiki page, the three pointer files and the three trimmed READMEs'
    - 'Both submodule gitlinks recorded in meta''s tree equal the actual `HEAD` of the corresponding submodule, asserted per submodule and recorded'
    - 'Both submodules are on `gsd/v1.35-documentation-consolidation-wiki-migration` and have no leftover PR worktree holding a stale branch'
    - 'The four phase requirements are marked complete in `REQUIREMENTS.md` only after their evidence exists, each traceable to a named evidence file'
    - 'ROADMAP.md is not written by this plan — the orchestrator owns roadmap writes, and the verbs that update it reformat the whole file and have clobbered a dependency table before'
  artifacts:
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-full-suite-final.txt"
      provides: "The three-checker suite green against fresh clones at the end of the phase — the phase gate 172-VALIDATION.md names before verification"
      min_lines: 6
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-legacy01-final.txt"
      provides: "The LEGACY-01 leg green over the final state of every surface the phase touched"
      min_lines: 4
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-gitlink-equality.txt"
      provides: "Per-submodule recorded-versus-actual sha equality, the Phase 171 plan 04 pattern"
      min_lines: 4
    - path: ".planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt"
      provides: "Each of the four requirements paired with the evidence file that discharges it, so a completion mark is traceable rather than asserted"
      min_lines: 10
  key_links:
    - from: "meta's recorded gitlink for each submodule"
      to: "that submodule's actual HEAD"
      via: "a per-submodule equality of `git ls-tree HEAD <sub>` against `git -C <sub> rev-parse HEAD`"
      pattern: "git ls-tree HEAD"
    - from: "each requirement mark in `.planning/REQUIREMENTS.md`"
      to: "the evidence file that discharges it"
      via: "a named row in the closing sweep"
      pattern: "evidence/172-"
  prohibitions:
    - statement: "No requirement is marked complete on the strength of a plan having run. Each of the four marks is made only against a named evidence file that exists and carries the asserted value — executors on this project have previously marked multi-plan requirements complete ahead of their evidence, and the closing sweep exists to make that impossible to do quietly."
      status: enforced
      verification: "judgment tier — Task 3's verify requires each of the four requirement rows in the closing sweep to name an evidence file that exists and is non-empty, and requires the four `REQUIREMENTS.md` checkboxes to be ticked only in the same commit."
    - statement: "This plan never sweeps `.planning/` for dead tracker links, and never repairs a `.planning/`-to-`.planning/` citation. Those records are historical by intent and repairing them destroys the evidence they exist to preserve."
      status: enforced
      verification: "judgment tier — the final LEGACY-01 leg carries the same `.planning` exclusion the CI leg does, and Task 1 records the count of in-scope-excluded matches rather than acting on them."
---

<objective>
Close the phase: prove the three checkers still pass against fresh clones with the eleventh wiki page
present, prove the LEGACY-01 guard is green over everything this phase changed, re-pin both submodule
gitlinks, and mark the four requirements complete against named evidence.

Purpose: this phase wrote to a live wiki, three repositories, three default branches and the GitHub
control plane. Each plan verified its own artifact; nothing so far has verified the whole together,
against clones rather than against the working trees. `172-VALIDATION.md` names exactly that as the
phase gate.

Output: four evidence files, two re-pinned gitlinks, four ticked requirement boxes.

**This plan does not write `ROADMAP.md`.** The orchestrator owns roadmap writes; the verbs that
update it reformat the whole file and have clobbered an unrelated phase's dependency table before
now. Requirement checkboxes in `REQUIREMENTS.md` are hand-edited here, and nothing else.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/172-policy-one-tracker-protected-main/172-CONTEXT.md
@.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md
@.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md
</context>

<!-- planner-discipline-allow: henols/firestarter/issues -->
<!-- planner-discipline-allow: henols/firestarter_app/issues -->

## Artifacts this phase produces

| Artifact | Repo / location | Created by |
|---|---|---|
| `Contributing.md`, the `_Sidebar.md` bullet, the `Home.md` Reference bullet | live wiki | 172-01 |
| The `Contributing` row and prose note in `.planning/v1.35/MIGRATION-TABLE.md` | meta | 172-01 |
| `.github/ISSUE_TEMPLATE/{bug-report.yml,feature-request.yml,dev-test-report.md,config.yml}` | prom | 172-02 |
| The `env:` block, default-branch gate and `LEGACY-01 dead tracker link check` step in `wiki-check.yml` | prom | 172-03 |
| `.github/CONTRIBUTING.md` | prom, firestarter, firestarter_app | 172-04 |
| `evidence/172-05-ruleset-body.json`, `evidence/172-05-ruleset-4998759-pre-delete.json` | meta | 172-05 |
| Ruleset `Protect main` on all three repositories | GitHub control plane | 172-05, 172-06 |
| Branch `policy/contributor-policy` in all three repos, and three pull requests | GitHub | 172-07 |
| Three merge commits on three public `main` branches, and a registered `Wiki check` workflow | GitHub | 172-08 |
| Two re-pinned submodule gitlinks, and four ticked requirement boxes | meta | 172-09 |
| `evidence/172-01-*` … `evidence/172-09-*` | meta | each plan |

<tasks>

<task type="auto">
  <name>Task 1: Run the full suite and the LEGACY-01 leg against the final state, from fresh clones</name>
  <precondition>Plan 172-08 recorded all three pull requests as `MERGED` and the four template files readable at `?ref=main`. The final state is only final once those merges have landed.</precondition>
  <read_first>
    - `.planning/phases/172-policy-one-tracker-protected-main/172-VALIDATION.md` § "Test Infrastructure" and § "Sampling Rate" — the full-suite recipe verbatim, and the phase-gate rule that it must run against fresh clones rather than the working trees
    - `.planning/phases/172-policy-one-tracker-protected-main/172-PATTERNS.md` § "The three `tools/wiki/` checkers" — each checker's exact invocation and its rc=0 output line, including the note that the page count becomes 11 once `Contributing` lands
    - `.planning/phases/172-policy-one-tracker-protected-main/172-RESEARCH.md` Pattern 3 — the LEGACY-01 leg and the three properties proven for it
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-03-full-suite.txt` — the same suite run mid-phase, for comparison; the only expected difference is the page count moving from 10 to 11
  </read_first>
  <files>.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-full-suite-final.txt, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-legacy01-final.txt</files>
  <action>
    Run the full suite exactly as `172-VALIDATION.md` specifies: a fresh `--depth 1` clone of the wiki,
    fresh `--depth 1 -b beta` clones of both sub-repositories, then `wiki.py links` against the wiki
    clone, `honest02_truth.py` against the wiki clone and the cloned app's chip database with the
    committed allowlist, and `dispatch_mirror.py` against the two cloned sub-repositories with only
    `--app-dir` and `--fw-dir`.

    Use clones. Not `/workspaces/firestarter`, not `/workspaces/firestarter_app`, not
    `/tmp/gsd-172-wiki`. The working trees are on the milestone branch and the wiki working clone is
    the one the phase edited; neither can prove anything about what a fresh consumer sees, and calling
    a working-tree run "as CI would" is the specific substitution research warns against.

    `wiki.py links` must now report `OK: 11 pages,` — ten before this phase plus `Contributing`. A
    report of `OK: 10 pages,` means the wiki push did not land or was reverted, and is a hard stop
    rather than a stale-cache excuse.

    Then run the LEGACY-01 leg over the final state of all four surfaces: `/workspaces` as meta, both
    sub-repository working trees, and the fresh wiki clone. Same pattern the CI step carries, same
    two exclusions. It must exit 0. This is the run that covers what the phase itself added — the new
    wiki page, the three pointer files, the four issue templates and the three trimmed READMEs — none
    of which existed when the leg was first proven in plan 172-03.

    Record alongside it the count of matches the same grep finds **without** the `.planning`
    exclusion. Those are all inside `.planning/`, which REQUIREMENTS.md places out of scope as
    historical-by-intent; recording the number keeps the cost of the exclusion visible and keeps the
    leg from reading as vacuous. Do not act on them, do not repair them, and do not add them to a
    backlog item — repairing a `.planning`-to-`.planning` citation destroys the evidence it exists to
    preserve.

    Commit both evidence files to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-full-suite-final.txt && D=$(mktemp -d) && { git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$D/wiki-clone" && git clone --depth 1 -q -b beta https://github.com/henols/firestarter.git "$D/firestarter" && git clone --depth 1 -q -b beta https://github.com/henols/firestarter_app.git "$D/firestarter_app" && python3 tools/wiki/wiki.py links --source-dir "$D/wiki-clone" | tail -1 && python3 tools/wiki/honest02_truth.py --wiki-dir "$D/wiki-clone" --db "$D/firestarter_app/firestarter/data/chip_database.json" --allowlist tools/wiki/claim-allowlist.json | tail -1 && python3 tools/wiki/dispatch_mirror.py --app-dir "$D/firestarter_app" --fw-dir "$D/firestarter" | tail -1; } > "$EV" 2>&1; echo "exit $?" >> "$EV"; grep -qx 'exit 0' "$EV" && test "$(grep -c '^OK: ' "$EV")" -eq 3 && grep -q '^OK: 11 pages,' "$EV" && test -f "$D/wiki-clone/Contributing.md"</automated>
    <fails_when>the evidence file's last line is not `exit 0`; or it reports `OK: 10 pages,` rather than 11, meaning the `Contributing` page is not on the live wiki; or fewer than three lines begin with `OK: `; or the file contains `unrecognized arguments`, `ERROR: PROTOCOLS.md not found` or `ERROR: cannot load database`</fails_when>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-legacy01-final.txt && H=$(mktemp -d) && git clone --depth 1 -q https://github.com/henols/firestarter_prom.wiki.git "$H/wiki-clone" && ln -s /workspaces "$H/meta" && ln -s /workspaces/firestarter "$H/firestarter" && ln -s /workspaces/firestarter_app "$H/firestarter_app" && { ( cd "$H" && if grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git --exclude-dir=.planning; then echo "FAIL: a page links to an issue tracker that is disabled"; exit 1; fi; echo "OK: no page links to henols/firestarter/issues or henols/firestarter_app/issues" ); echo "rc=$?"; echo -n "planning_excluded_matches="; ( cd "$H" && grep -rInE 'henols/firestarter(_app)?/issues' meta firestarter firestarter_app wiki-clone --exclude-dir=.git | wc -l ); } > "$EV" 2>&1; grep -qx 'rc=0' "$EV" && grep -q '^OK: no page links' "$EV" && test "$(grep -oE 'planning_excluded_matches=[0-9]+' "$EV" | cut -d= -f2)" -gt 0</automated>
    <fails_when>the evidence file contains `rc=1` and a `FAIL:` line naming a file and line number — a dead tracker link exists on a surface this phase touched; or `planning_excluded_matches=0`, which would mean the grep reached nothing at all and the green result is vacuous rather than clean</fails_when>
  </verify>
  <acceptance_criteria>
    - The full-suite evidence file ends `exit 0` and contains exactly three lines beginning `OK: `
    - One of those lines begins `OK: 11 pages,` — the eleventh page is live and reachable
    - The fresh wiki clone created by the verify contains `Contributing.md`
    - The suite ran against a fresh wiki clone and fresh `beta` clones; no path under `/workspaces/firestarter` or `/workspaces/firestarter_app` appears as a checker argument in the evidence
    - The LEGACY-01 evidence file records `rc=0` and the standing OK line
    - It also records `planning_excluded_matches=` with a value greater than zero, so the leg is demonstrably reaching files and the cost of the `.planning` exclusion is on the record
    - No file under `.planning/` was edited in response to those excluded matches
  </acceptance_criteria>
  <done>The three checkers pass against fresh clones with the eleventh page present, and the dead-tracker guard is green over every surface the phase changed — including the ones the phase itself created.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin both submodule gitlinks and clear the PR worktrees</name>
  <read_first>
    - `.planning/phases/171-stray-the-root-level-documentation-files/171-04-PLAN.md:150` — the gitlink equality idiom this task reuses verbatim: recorded sha from `git ls-tree HEAD <sub>`, actual sha from `git -C <sub> rev-parse HEAD`, compared per submodule and tee'd to evidence
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-07-pr-branches.txt` — the three worktree paths created in plan 172-07, which must be removed here
    - `git worktree list` output for `/workspaces` and for `firestarter` — note that `firestarter_py32_ci` is a pre-existing, unrelated worktree of the firmware repository and must be left alone
  </read_first>
  <files>.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-gitlink-equality.txt</files>
  <action>
    First remove the three throwaway PR worktrees plan 172-07 created — `/tmp/gsd-172-pr-prom`,
    `/tmp/gsd-172-pr-fw` and `/tmp/gsd-172-pr-app` — with `git -C <owning repo> worktree remove`, then
    `git -C <owning repo> worktree prune`. Leave `firestarter_py32_ci` alone: it is a pre-existing
    worktree of the firmware repository on an unrelated branch and has nothing to do with this phase.
    The local `policy/contributor-policy` branches may stay; they are merged and harmless, and the
    remote copies are GitHub's to garbage-collect.

    Confirm both submodules are still on `gsd/v1.35-documentation-consolidation-wiki-migration` and
    that neither has an unstaged or uncommitted change. Plan 172-04 committed inside each of them and
    deliberately left the gitlink un-pinned so that one commit here re-pins both.

    Then stage both gitlinks in meta and commit them in a single commit. If that commit legitimately
    sweeps up a gitlink movement from earlier work — the way Phase 171 plan 04's did — name that
    explicitly in the commit message rather than letting it look like this phase moved more than it
    did.

    Then assert the equality per submodule, not in aggregate: for each of `firestarter` and
    `firestarter_app`, the sha recorded in meta's tree by `git ls-tree HEAD <sub>` must equal
    `git -C <sub> rev-parse HEAD`. Print recorded, actual and a verdict per submodule to
    `evidence/172-09-gitlink-equality.txt`. An aggregate pass hides which one is stale; the per-line
    form is what Phase 171 plan 04 used and what a reader can act on.

    Commit the evidence file to meta with a path-scoped `git commit -- <path>`.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-gitlink-equality.txt && ok=1 && : > "$EV" && for m in firestarter firestarter_app; do rec=$(git ls-tree HEAD "$m" | awk '{print $3}'); act=$(git -C "$m" rev-parse HEAD); br=$(git -C "$m" branch --show-current); printf "%-16s recorded=%s actual=%s branch=%s %s\n" "$m" "$rec" "$act" "$br" "$([ "$rec" = "$act" ] && echo OK || echo STALE)" >> "$EV"; [ "$rec" = "$act" ] || ok=0; [ "$br" = gsd/v1.35-documentation-consolidation-wiki-migration ] || ok=0; done; test "$ok" = 1 && test "$(grep -c ' OK$' "$EV")" -eq 2 && ! grep -q 'STALE' "$EV" && test "$(grep -c 'branch=gsd/v1.35-documentation-consolidation-wiki-migration' "$EV")" -eq 2</automated>
    <fails_when>either line in the evidence file ends `STALE` — meta's recorded gitlink does not match the submodule's actual HEAD, so a reader checking out this commit would get a tree that does not contain the phase's sub-repo work; or a `branch=` value is not the milestone branch, meaning a submodule was left on a PR branch</fails_when>
    <automated>cd /workspaces && ! test -d /tmp/gsd-172-pr-prom && ! test -d /tmp/gsd-172-pr-fw && ! test -d /tmp/gsd-172-pr-app && test "$(git worktree list | grep -c 'gsd-172-pr')" -eq 0 && test "$(git -C firestarter worktree list | grep -c 'gsd-172-pr')" -eq 0 && test "$(git -C firestarter_app worktree list | grep -c 'gsd-172-pr')" -eq 0 && git -C firestarter worktree list | grep -q firestarter_py32_ci && test -z "$(git -C firestarter status --porcelain)" && test -z "$(git -C firestarter_app status --porcelain)"</automated>
    <fails_when>any of the three throwaway worktree directories still exists, or any repository's `git worktree list` still names one; or the pre-existing `firestarter_py32_ci` worktree is gone, meaning the cleanup removed an unrelated worktree; or either submodule has a non-empty `git status --porcelain`</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/172-09-gitlink-equality.txt` has exactly two lines, one per submodule, each ending `OK` and each naming the recorded sha, the actual sha and the branch
    - Neither line contains `STALE`
    - Both submodules are on `gsd/v1.35-documentation-consolidation-wiki-migration` with an empty `git status --porcelain`
    - The gitlink re-pin is a single meta commit covering both submodules, and its message names any earlier movement it legitimately sweeps up
    - None of `/tmp/gsd-172-pr-prom`, `/tmp/gsd-172-pr-fw`, `/tmp/gsd-172-pr-app` exists, and no repository's `git worktree list` names any of them
    - The pre-existing `firestarter_py32_ci` worktree of the firmware repository is untouched and still listed
  </acceptance_criteria>
  <done>Both gitlinks recorded in meta equal their submodules' actual heads, proven per submodule; the phase's throwaway worktrees are gone and the unrelated one is not.</done>
</task>

<task type="auto">
  <name>Task 3: Pair each requirement with its evidence and mark the four complete in REQUIREMENTS.md</name>
  <read_first>
    - `.planning/REQUIREMENTS.md:55` (LEGACY-01), `:65-67` (POLICY-01, POLICY-02, POLICY-03) and the status table around `:177-180` — the four checkboxes and the four rows to flip, and the `- [x]` form the completed neighbours use
    - Every evidence file this phase produced under `.planning/phases/172-policy-one-tracker-protected-main/evidence/` — a mark is made against a file, not against a memory of a plan having run
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-05-actions-bypass-probe.txt` — the D-09 disposition, which is carried forward as an open item rather than folded into POLICY-03's completion
    - `.planning/phases/172-policy-one-tracker-protected-main/evidence/172-08-prefill-check.txt` — the prefill outcome, which is carried forward the same way if it was negative
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt</files>
  <action>
    Write `evidence/172-09-closing-sweep.txt` first, then edit `REQUIREMENTS.md` — in that order, so
    the marks follow the evidence rather than the other way round. This project has previously seen
    executors mark a multi-plan requirement complete ahead of its evidence; the sweep file exists so
    that cannot happen quietly.

    The sweep file carries one row per requirement, each naming the evidence file that discharges it
    and the specific assertion inside it:

    - **POLICY-01** — the canonical page is live and the other two repositories link rather than
      restate. Evidence: `172-01-wiki-links-postpush-freshclone.txt` for the page, and
      `172-04-pointer-and-trim.txt` for the one sha256 shared by three pointer files and the three
      single-link README sections.
    - **POLICY-02** — three template kinds plus the chooser config, schema-valid and live on the
      default branch. Evidence: `172-02-issue-forms-validation.txt` for the validation, and
      `172-08-post-merge-surfaces.txt` for the four filenames read at `?ref=main`.
    - **POLICY-03** — three enforcing rulesets, identical, Actions-only bypass. Evidence:
      `172-06-ruleset-readback.txt`, and `172-05-ruleset-4998759-pre-delete.json` as the record of
      what was replaced.
    - **LEGACY-01** — zero dead-tracker links outside `.planning/`, with a registered guard that has
      been seen to fail. Evidence: `172-09-legacy01-final.txt` for the final state,
      `172-03-legacy01-redgreen.txt` for the red demonstration, and
      `172-08-post-merge-surfaces.txt` for the workflow being registered rather than inert.

    Then add two rows recording what is **not** claimed, so the completion marks do not absorb them:

    - The GitHub Actions bypass is configured and, unless the throwaway-repo probe was run, unproven
      — with the available secondary evidence against it. Cite
      `172-05-actions-bypass-probe.txt` and note that Phase 173's honesty ledger carries it.
    - gh#6's required status checks and required review-thread resolution are knowingly not
      implemented (D-11), so Phase 173 can state them as non-claims rather than have them read as
      quietly delivered.
    - If the prefill check came back `chooser`, a third row: the browser tier of `dev test --submit`
      regressed, it is filed rather than fixed, and the backlog item is named.

    Then flip the four checkboxes in `REQUIREMENTS.md` from `- [ ]` to `- [x]`, and change the four
    `Phase 172 | Pending` rows in the status table to `Complete`. Match the exact form the already
    completed neighbours use. Edit by hand: the GSD requirements verbs reformat the whole file, and
    this file carries hand-authored prose that a reformat would disturb.

    **Do not touch `ROADMAP.md`.** The orchestrator owns roadmap writes; the verb that updates plan
    progress overwrites positionally and has clobbered an unrelated phase's dependency table before.

    Commit `REQUIREMENTS.md` and the sweep file to meta with a path-scoped `git commit -- <paths>`.
  </action>
  <verify>
    <automated>cd /workspaces && EV=.planning/phases/172-policy-one-tracker-protected-main/evidence/172-09-closing-sweep.txt && E=.planning/phases/172-policy-one-tracker-protected-main/evidence && test -s "$EV" && ok=1 && for r in POLICY-01 POLICY-02 POLICY-03 LEGACY-01; do grep -q "$r" "$EV" || ok=0; done; for f in 172-01-wiki-links-postpush-freshclone.txt 172-02-issue-forms-validation.txt 172-03-legacy01-redgreen.txt 172-04-pointer-and-trim.txt 172-06-ruleset-readback.txt 172-08-post-merge-surfaces.txt 172-09-legacy01-final.txt; do test -s "$E/$f" || ok=0; grep -q "$f" "$EV" || ok=0; done; test -s "$E/172-05-ruleset-4998759-pre-delete.json" || ok=0; test -s "$E/172-05-actions-bypass-probe.txt" || ok=0; grep -q '172-05-actions-bypass-probe.txt' "$EV" || ok=0; test "$ok" = 1</automated>
    <fails_when>the sweep file is missing or does not name all four requirement ids; or any evidence file it cites is absent or zero-length — a requirement cited to a file that does not exist is exactly the unbacked completion mark this task exists to prevent; or the Actions-bypass disposition is not carried into the sweep</fails_when>
    <automated>cd /workspaces && R=.planning/REQUIREMENTS.md && test "$(grep -c '^- \[x\] \*\*POLICY-0[123]\*\*' "$R")" -eq 3 && grep -q '^- \[x\] \*\*LEGACY-01\*\*' "$R" && test "$(grep -c '^| POLICY-0[123] | Phase 172 | Complete |$' "$R")" -eq 3 && grep -qx '| LEGACY-01 | Phase 172 | Complete |' "$R" && test "$(grep -c 'Phase 172 | Pending' "$R")" -eq 0 && git diff --quiet HEAD~1 -- .planning/ROADMAP.md</automated>
    <fails_when>fewer than three ticked POLICY checkboxes or an unticked LEGACY-01 checkbox; or any row still reads `Phase 172 | Pending`; or `git diff --quiet HEAD~1 -- .planning/ROADMAP.md` exits non-zero, meaning this plan wrote the roadmap when the orchestrator owns it</fails_when>
  </verify>
  <acceptance_criteria>
    - `evidence/172-09-closing-sweep.txt` names all four requirement ids, and for each one names at least one evidence file that exists and is non-empty
    - It carries a row recording that the GitHub Actions bypass is configured and, absent the probe, unproven — with the secondary evidence against it — and cites `172-05-actions-bypass-probe.txt`
    - It carries a row recording that gh#6's required status checks and required review-thread resolution are knowingly not implemented per D-11
    - If the prefill check returned `chooser`, it carries a third row naming the filed backlog item and stating the fix was not applied
    - `.planning/REQUIREMENTS.md` shows `- [x]` for POLICY-01, POLICY-02, POLICY-03 and LEGACY-01
    - Its status table shows `Complete` for all four Phase 172 rows and no remaining `Phase 172 | Pending`
    - `git diff HEAD~1 -- .planning/ROADMAP.md` is empty — this plan wrote no roadmap
    - `REQUIREMENTS.md`'s prose sections and the rows for other phases are byte-unchanged; the diff touches only the four checkboxes and the four status rows
  </acceptance_criteria>
  <done>Each of the four requirements is marked complete against a named, existing evidence file, and the two things this phase does not claim are written down beside them rather than absorbed into the marks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a working tree → a fresh clone | The working trees carry uncommitted state and a branch nobody else has. Only a fresh clone answers what a consumer actually sees. |
| a requirement checkbox → everything downstream | The milestone close, Phase 173's ledger and every future reader treat a ticked box as settled. A box ticked ahead of its evidence is a false claim that survives the phase. |
| meta's gitlink → the submodule's real head | A stale gitlink means a reader who checks out this commit gets a tree that does not contain the phase's sub-repository work, with nothing to indicate it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-172-35 | Repudiation | a requirement marked complete ahead of its evidence | high | mitigate | The sweep file is written before the checkboxes are flipped, and the verify requires every cited evidence file to exist and be non-empty before any mark is accepted |
| T-172-36 | Repudiation | a green suite run against the working trees | medium | mitigate | The suite is run against fresh clones only, and the acceptance criteria require no `/workspaces/firestarter*` path to appear as a checker argument in the evidence |
| T-172-04 | Tampering | a LEGACY-01 result that is green because it reached nothing | medium | mitigate | The final leg records the without-exclusion match count and requires it to be greater than zero |
| T-172-37 | Tampering | a stale submodule gitlink | medium | mitigate | Per-submodule recorded-versus-actual equality, printed one line each so an aggregate pass cannot hide a stale one |
| T-172-38 | Tampering | a roadmap write that reformats the file | medium | mitigate | This plan writes no roadmap; the verify asserts `ROADMAP.md` is unchanged in the plan's own diff |
| T-172-39 | Repudiation | completion marks absorbing the phase's non-claims | high | mitigate | The sweep carries explicit rows for the unproven Actions bypass and for the two gh#6 items D-11 declines, so Phase 173's ledger inherits them stated rather than having to rediscover them |
</threat_model>

<verification>
- The three checkers green against fresh clones, with `OK: 11 pages,`
- The LEGACY-01 leg green over the final state with a non-zero without-exclusion count
- Two gitlink lines, both `OK`, both on the milestone branch
- Three throwaway worktrees gone, the unrelated one intact
- Four ticked checkboxes and four `Complete` rows, each traceable to a named evidence file
- `ROADMAP.md` untouched by this plan
</verification>

<success_criteria>
- Every requirement this phase owns is complete and traceable to evidence rather than to a plan having run
- The phase's two non-claims — the unproven Actions bypass, and the two gh#6 rules D-11 declines — are recorded beside the claims, ready for Phase 173's honesty ledger
- The repository is left with matching gitlinks, no stray worktrees and no roadmap damage
</success_criteria>

<output>
Create `.planning/phases/172-policy-one-tracker-protected-main/172-09-SUMMARY.md` when done
</output>
