---
phase: 168-migrate-the-13-doc-files-moved-without-upgrading-a-claim
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: meta (/workspaces) — branch creation only in firestarter/ and firestarter_app/
files_modified:
  - .planning/v1.35/MIGRATION-TABLE.md
autonomous: false
requirements: [MIGRATE-01, HONEST-01]
user_setup: []

must_haves:
  truths:
    - "A named v1.35 branch exists in firestarter/ and firestarter_app/, forked from a recorded base SHA, and the base choice is recorded with its reason"
    - "Every one of the 12 migrating doc files has a wiki page name, a rendered title and a pre-deletion git SHA recorded in MIGRATION-TABLE.md"
    - "git -C <subrepo> show <sha>:doc/<file> returns non-empty text for all 12 rows — the HONEST-01 oracle is proven readable before any delete"
    - "The deferred PY32F071 file has a recorded SHA too, so 'deferred' does not silently mean 'lost'"
    - "MIGRATION-TABLE.md no longer states that a publish path exists"
  artifacts:
    - path: ".planning/v1.35/MIGRATION-TABLE.md"
      provides: "the auditable per-file move record plus the HONEST-01 source oracle"
      contains: "Pre-deletion SHA"
  key_links:
    - from: ".planning/v1.35/MIGRATION-TABLE.md"
      to: "firestarter/ and firestarter_app/ git history"
      via: "the Pre-deletion SHA column, read by honest01_claims.py as `git show <sha>:doc/<file>`"
      pattern: "[0-9a-f]{40}"
---

<objective>
Create the v1.35 working branches in both sub-repos, then turn `.planning/v1.35/MIGRATION-TABLE.md`
from a 12-row shell into the phase's load-bearing record: the page-name mapping for all 12
migrating files, and the pre-deletion git SHA that is the *only* HONEST-01 source oracle once
`doc/` is gone (D-02).

Purpose: this plan exists first because **H-2 is irrecoverable**. After the `doc/` directories are
deleted, the pre-migration text of the 12 documents is reachable only via
`git -C <subrepo> show <sha>:doc/<file>`. No recorded SHA means no oracle means criterion 4 is
permanently unverifiable. Nothing else in this phase may commit into a sub-repo until the branch
question below is settled, because the branch base determines which tree all 79 measured repair
sites live in.

Output: two sub-repo branches, and a MIGRATION-TABLE with zero unfilled cells.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-CONTEXT.md
@.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-RESEARCH.md
@.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-PATTERNS.md
@.planning/v1.35/MIGRATION-TABLE.md
</context>

<!-- planner-region-allow: publish -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Settle the sub-repo branch base, then create both branches</name>
  <read_first>
    - `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-RESEARCH.md` § "H-4: no `gsd/v1.35-*` branch exists in either sub-repo"
    - `.planning/ROADMAP.md` § "v1.33 — Source Hygiene & Firmware Size Reduction"
  </read_first>
  <files>firestarter (branch ref only), firestarter_app (branch ref only)</files>
  <decision>
    Which commit do the two sub-repo `gsd/v1.35-documentation-consolidation-wiki-migration` branches
    fork from?
  </decision>

  <decision-answered date="2026-08-31" by="operator">
    **ANSWERED: `fork-from-current-head`.** Fork both sub-repo branches from the current working
    HEADs — `firestarter` at `a218b4f`, `firestarter_app` at `d56424e`. The operator was shown the
    stated cost (deviates from the fork-off-`beta` rule; v1.35 cannot merge to `beta` before fw#56
    and app#54 land) and selected it anyway, because forking `beta` would invalidate all 79 measured
    repair sites and resurrect the narrative comments v1.33 already stripped.

    **This gate is settled. Do not re-ask it.** Execute `<action>` below with
    `option id = fork-from-current-head`, and record both base SHAs and that option id in the plan
    summary as instructed.
  </decision-answered>
  <context>
    The standing project rule is: fork off `beta` in all three repositories. Measured this session,
    `beta` is **behind** both sub-repo working HEADs, because v1.33's source-hygiene pull requests
    (fw#56, app#54) are open and unmerged and v1.34 was the bench gate validating them:

    - `firestarter`: `beta` = `8695ee5`; working tree is detached at `a218b4f`, 10 commits ahead.
    - `firestarter_app`: `beta` = `6bfa645`; working tree is on `chore/strip-provenance-comments`
      at `d56424e`, ahead through the merge of PR #54 plus 3 further provenance-strip commits.

    Every measurement in `168-RESEARCH.md` — all 79 repair lines, `build_db.py:543`/`:569`,
    `proto_constants.h:11-16`, the 2,328-line test-module split, the 1976-test baseline — was taken
    against `a218b4f` / `d56424e`, not against `beta`.
  </context>
  <options>
    <option id="fork-from-current-head">
      <name>Fork from the current sub-repo HEADs (a218b4f / d56424e) — RECOMMENDED</name>
      <pros>Every line number, count and command in 168-RESEARCH.md holds as measured. The app tree keeps the v1.33 comment strip, so no repair site re-acquires a narrative comment the operator's no-comments rule already removed. Precedent exists: a milestone branch in this project has repeatedly forked the previous milestone rather than `beta`.</pros>
      <cons>Deviates from the stated fork-off-`beta` rule. v1.35's eventual merge inherits v1.33's unmerged PR topology, so v1.35 cannot merge to `beta` before fw#56 / app#54 do.</cons>
    </option>
    <option id="fork-from-beta">
      <name>Fork from `beta` (8695ee5 / 6bfa645), per the standing rule</name>
      <pros>Follows the project rule literally. v1.35 merges to `beta` independently of v1.33's open PRs.</pros>
      <cons>Invalidates the entire measured repair surface — the research must be re-run before any repair task can execute. Resurrects the narrative comments v1.33 stripped, so this phase would be editing comment blocks the operator has already ordered deleted. `firestarter_app/tools/build_db.py` line numbers move.</cons>
    </option>
  </options>
  <action>
    After the operator selects, create in **both** sub-repos a branch named exactly
    `gsd/v1.35-documentation-consolidation-wiki-migration` at the selected base, check it out, and
    record the base SHA of each. Do **not** rebase, merge, delete or force-push
    `chore/strip-provenance-comments`; record its HEAD SHA (`d56424e`) verbatim in the plan summary
    as untouched work so it is not silently orphaned. Do not push either new branch in this task —
    the first push happens with the first sub-repo commit in wave 3.
  </action>
  <resume-signal>Select: fork-from-current-head, or fork-from-beta</resume-signal>
  <acceptance_criteria>
    - `git -C firestarter rev-parse --abbrev-ref HEAD` prints `gsd/v1.35-documentation-consolidation-wiki-migration`
    - `git -C firestarter_app rev-parse --abbrev-ref HEAD` prints `gsd/v1.35-documentation-consolidation-wiki-migration`
    - `git -C firestarter_app rev-parse chore/strip-provenance-comments` still prints `d56424e1…` — the branch is unmoved
    - `git -C firestarter status --porcelain` and `git -C firestarter_app status --porcelain` are both empty
    - The plan summary records both base SHAs and the selected option id
  </acceptance_criteria>
  <verify>
    <automated>test "$(git -C firestarter rev-parse --abbrev-ref HEAD)" = gsd/v1.35-documentation-consolidation-wiki-migration && test "$(git -C firestarter_app rev-parse --abbrev-ref HEAD)" = gsd/v1.35-documentation-consolidation-wiki-migration && git -C firestarter_app rev-parse --verify chore/strip-provenance-comments</automated>
  </verify>
  <done>Both sub-repos are on the named v1.35 branch at an operator-selected base; the app's prior branch is intact and its SHA recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the 12 page names, rendered titles and pre-deletion SHAs</name>
  <read_first>
    - `.planning/v1.35/MIGRATION-TABLE.md` (the file being modified — all of it, including the hyphen-hazard section at `:41-55`)
    - `tools/wiki/wiki.py:52,60-61,141-166` — `_LEGAL_TARGET_RE`, `render_title`, `check_page_names`; these define the legal page-name alphabet
  </read_first>
  <files>.planning/v1.35/MIGRATION-TABLE.md</files>
  <action>
    Add a sixth column `Pre-deletion SHA` between `Rendered title` and `Moved in`, and fill all 12
    rows. Per D-02 the SHA is the sub-repo commit whose `doc/<file>` still holds the pre-migration
    text; use the Task 1 branch-creation HEAD of each sub-repo (record the full 40-character SHA,
    not the abbreviation), because `doc/` content is never edited in-repo by this phase — only
    deleted — so that commit is exact.

    Write these page names and rendered titles verbatim. They are settled here, not left to the
    executor, and they resolve the two hyphen hazards `MIGRATION-TABLE.md:48-55` names by rewording
    the title rather than smuggling a look-alike character:

    | Source file | Wiki page | Rendered title |
    |---|---|---|
    | `firestarter/doc/PROTOCOLS.md` | `Programming-Protocols` | Programming Protocols |
    | `firestarter/doc/SHIELD-REVISIONS.md` | `Shield-Revisions` | Shield Revisions |
    | `firestarter/doc/AT28C04-ADAPTER.md` | `AT28C04-Adapter` | AT28C04 Adapter |
    | `firestarter_app/doc/beta-testing-install.md` | `Beta-Testing-Install` | Beta Testing Install |
    | `firestarter_app/doc/community-validation.md` | `Community-Validation` | Community Validation |
    | `firestarter_app/doc/infoic-field-dictionary.md` | `Infoic-Field-Dictionary` | Infoic Field Dictionary |
    | `firestarter_app/doc/lockable-proms.md` | `Lockable-PROMs` | Lockable PROMs |
    | `firestarter_app/doc/package-details.md` | `Package-Details` | Package Details |
    | `firestarter_app/doc/pinout-safety-review.md` | `Pinout-Safety-Review` | Pinout Safety Review |
    | `firestarter_app/doc/protocol-flags.md` | `Protocol-Flags` | Protocol Flags |
    | `firestarter_app/doc/protocol-id.md` | `Protocol-ID` | Protocol ID |
    | `firestarter_app/doc/sram-nvram-behavior.md` | `SRAM-and-NVRAM-Behavior` | SRAM and NVRAM Behavior |

    Record the hyphen-hazard resolutions in the existing "The hyphen hazard" section: `AT28C04-Adapter`
    needs no literal hyphen in its intended title at all, and `sram-nvram-behavior`'s natural
    "SRAM/NVRAM" is illegal under `check_page_names` (which rejects `/`), so it is reworded to
    "SRAM and NVRAM Behavior". Both stems use only `[A-Za-z0-9-]`, the alphabet `_LEGAL_TARGET_RE`
    admits.

    Also update the two 167-era rows: `How-This-Wiki-Is-Published` is renamed to
    `How-To-Edit-This-Wiki` (rendered "How To Edit This Wiki") in this phase per D-21 — the old
    title asserts a publishing model that stopped existing at the reversal, so keeping the name
    would ship a false title on a public page for the sake of a URL with exactly one known inbound
    link (`Home.md:5`), which this phase rewrites in the same wiki commit. Record that trade-off in
    the table's prose.

    Add a "Deferred, not migrating" SHA line for `firestarter_app/doc/PY32F071-FIRMWARE-INSTALL.md`:
    it is deferred from migration but MIGRATE-02 still deletes it, so its pre-deletion SHA is what
    makes "deferred" recoverable rather than lost.
  </action>
  <verify>
    <automated>! grep -q 'TBD' .planning/v1.35/MIGRATION-TABLE.md && test "$(grep -cE '^\| firestarter(_app)? \|' .planning/v1.35/MIGRATION-TABLE.md)" = 12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TBD' .planning/v1.35/MIGRATION-TABLE.md` returns 0
    - The table header line contains the literal `Pre-deletion SHA`
    - Exactly 12 data rows begin `| firestarter |` or `| firestarter_app |`
    - Every one of those 12 rows carries a 40-character lowercase hex SHA
    - Each of the 12 wiki-page stems matches `^[A-Za-z0-9][A-Za-z0-9-]*$` (the `_LEGAL_TARGET_RE` alphabet)
    - The "Deferred, not migrating" section names a 40-character SHA for `PY32F071-FIRMWARE-INSTALL.md`
    - The prose records why `How-This-Wiki-Is-Published` is renamed and what the rename costs
  </acceptance_criteria>
  <done>All 12 rows carry a page name, a rendered title and a resolvable pre-deletion SHA; no unfilled cell remains.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the oracle reads, and repair the false publish prose</name>
  <read_first>
    - `.planning/v1.35/MIGRATION-TABLE.md:1-7` (the prose being repaired)
    - `.planning/notes/v135-wiki-only-reversal.md` § "What this voids"
  </read_first>
  <files>.planning/v1.35/MIGRATION-TABLE.md</files>
  <action>
    Prove the HONEST-01 read path works for every row before this plan is allowed to close: for each
    of the 12 rows plus the deferred PY32 row, run `git -C <source repo> show <sha>:<path relative
    to the sub-repo root>` and confirm non-empty output. Capture the 13 byte counts into
    `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/oracle-readable.txt`,
    one `<page> <bytes>` line per row. This is the non-vacuity gate on the oracle itself: a recorded
    SHA that does not resolve is worse than no SHA, because it reads as evidence.

    Then repair `MIGRATION-TABLE.md:1-7`. Both paragraphs describe a mechanical publish path and
    assert that a publish subcommand does not and never will read this table. After D-20 deletes
    that subcommand there is no publish path at all, so the sentences are false rather than merely
    stale. Replace them with prose that states what is now true: documentation lives only in the
    GitHub wiki, pages reach it by a cloned-and-pushed commit (D-19), and this table exists for two
    consumers — a reviewer checking that a moved file kept its content and its claims, and the
    Backlog 999.9 repository-rename sweep that greps it for a source path. Keep the table itself and
    keep the hyphen-hazard and deferred sections.
  </action>
  <verify>
    <automated>test "$(wc -l < .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/oracle-readable.txt)" -eq 13 && ! sed -n '1,8p' .planning/v1.35/MIGRATION-TABLE.md | grep -qi 'publish'</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/oracle-readable.txt` has exactly 13 lines and every byte count on it is greater than zero
    - The byte count recorded for `Programming-Protocols` is 49560 (measured this session for `firestarter/doc/PROTOCOLS.md` at the pre-migration tree); a different value means the wrong SHA was recorded
    - `sed -n '1,8p' .planning/v1.35/MIGRATION-TABLE.md | grep -ci 'publish'` returns 0 — the gate is scoped to the header prose region, because the deferred-file section legitimately discusses what is and is not published
    - `grep -c 'hyphen' .planning/v1.35/MIGRATION-TABLE.md` is greater than 0 — the hazard section survived the prose repair
    - `grep -c '999.9' .planning/v1.35/MIGRATION-TABLE.md` is greater than 0 — the rename-sweep consumer is still named
  </acceptance_criteria>
  <done>Every recorded SHA is proven to resolve to non-empty pre-migration text, and the table no longer describes a publish path that this phase deletes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → recorded git SHA | The SHA is the sole future oracle; a wrong or unresolvable SHA silently disarms criterion 4 |
| meta repo → sub-repo git history | The meta repo asserts facts about commits it does not contain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-01 | Repudiation | `MIGRATION-TABLE.md` SHA column | critical | mitigate | Task 3 proves every SHA resolves to non-empty text and pins one known byte count (49560) before the plan closes; a plausible-looking but wrong SHA cannot pass |
| T-168-02 | Tampering | sub-repo branch creation | high | mitigate | Branch base is an operator decision (Task 1); `chore/strip-provenance-comments` is explicitly not rebased, merged or deleted, and its SHA is recorded |
| T-168-03 | Information disclosure | none — no push, no external write in this plan | low | accept | Task 1 explicitly forbids pushing the new branches; the first sub-repo push is wave 3 |
| T-168-SC | Tampering | package installs | low | accept | This plan installs no package. No new npm/pip/cargo dependency is introduced anywhere in phase 168; `uv pip install -e '.[test]'` in plan 12 resolves only the app's already-declared, already-shipped dependency set |
</threat_model>

<verification>
- `git -C firestarter rev-parse --abbrev-ref HEAD` and the same for `firestarter_app` both name the v1.35 branch
- `grep -c 'TBD' .planning/v1.35/MIGRATION-TABLE.md` = 0
- `evidence/oracle-readable.txt` has 13 non-zero rows
- `bash tools/wiki/selftest.sh` still reports `OK: selftest complete (12 cases)` — this plan changes no tooling
</verification>

<success_criteria>
The HONEST-01 source oracle exists, is proven readable, and is recorded in a table with no unfilled
cells — before any `doc/` file is deleted anywhere.
</success_criteria>

<artifacts_this_phase_produces>
The complete symbol namespace for phase 168. **Bold** entries are owned by this plan; the rest are
listed so no plan invents a colliding name.

**New meta-repo files:** `tools/wiki/dispatch_mirror.py` · `tools/wiki/honest01_claims.py` ·
`tools/wiki/honest02_truth.py` · `tools/wiki/claim-vocabulary.json` · `tools/wiki/claim-allowlist.json`

**New/changed functions:** `wiki.py::check_sidebar_lists_every_page` ·
`dispatch_mirror.py::{parse_claims_region, parse_bucket_rows, parse_family_rows, check_dispatch_mirror, main}` ·
`honest01_claims.py::{load_vocabulary, parse_migration_table, read_source_at_sha, count_tokens, compare_multisets, main}` ·
`honest02_truth.py::{load_database, load_allowlist, extract_claim_region, find_stamp, check_stamp_present, check_claims_resolve, check_stamp_freshness, main}`

**CLI surface:** `wiki.py links --source-dir` (becomes REQUIRED) · `honest01_claims.py --table --wiki-dir --vocab --repo-root` ·
`honest02_truth.py --wiki-dir --db --allowlist` · `dispatch_mirror.py --wiki-dir --app-dir --fw-dir`.
**Removed:** `wiki.py sidebar`, `wiki.py check`, `wiki.py publish`, `--push`, `--require-wiki`, `--wiki-remote`

**Selftest case names (final set, 9):** `orphan_exit_1` · `sidebar_link_is_not_evidence` ·
`broken_link_exit_1` · `md_suffix_link_exit_1` · `illegal_filename_exit_1` ·
`wiki05_unreferenced_page_exit_1` · `dispatch_mirror_planted_drift_exit_1` ·
`honest01_weakened_claim_exit_1` · `honest02_absent_part_number_exit_1`.
**New fixture helper:** `new_source_repo <dir>` (non-bare `git init`, prints the commit SHA)

**Data-file keys:** `claim-vocabulary.json` → `schema_version`, `families.{support_status,negative_capability,voltage_ceiling}.{match,case_sensitive,tokens}`, `expected_zero` ·
`claim-allowlist.json` → `schema_version`, `part_tokens[].{token,reason}`, `algorithm_tokens[].{token,reason}`

**Wiki markup sentinels:** `<!-- firestarter-claim-stamp: db-sha256-16=<16hex> verified=<YYYY-MM-DD> -->` ·
`<!-- firestarter-claims-begin -->` / `<!-- firestarter-claims-end -->`

**Workflow paths:** `.github/workflows/wiki-check.yml` (rewritten, job `wiki-truth`) ·
`.github/workflows/wiki-publish.yml` (deleted)

**Wiki page names (14):** Home · How-To-Edit-This-Wiki · **Programming-Protocols · Shield-Revisions ·
AT28C04-Adapter · Beta-Testing-Install · Community-Validation · Infoic-Field-Dictionary ·
Lockable-PROMs · Package-Details · Pinout-Safety-Review · Protocol-Flags · Protocol-ID ·
SRAM-and-NVRAM-Behavior**

**New `MIGRATION-TABLE.md` column:** **`Pre-deletion SHA`**

**Evidence files under `.planning/phases/168-…/evidence/`:** **`oracle-readable.txt`** ·
`wiki05-live-orphan-RED.txt` · `wiki05-live-GREEN.txt` · `dispatch-mirror-planted-RED.txt` ·
`honest01-weakened-claim-RED.txt` · `honest01-live-GREEN.txt` · `honest02-fixture-RED.txt` ·
`honest02-live-run.txt` · `migrate03-py311-suite.txt` · `migrate03-sdist-doc-delta.txt` ·
`selftest-9-cases.txt`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-01-SUMMARY.md` when done
</output>
