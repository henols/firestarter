---
phase: 168-migrate-the-13-doc-files-moved-without-upgrading-a-claim
plan: 05
type: execute
wave: 3
depends_on: ["168-01", "168-02", "168-03"]
commits_land_in: firestarter_prom.wiki.git (live public wiki) — no meta or sub-repo source commits except evidence
files_modified:
  - .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt
autonomous: false
requirements: [MIGRATE-01, LEGACY-06, HONEST-02, WIKI-05]
user_setup: []

must_haves:
  truths:
    - "All 12 migrating documents are published on the live wiki under the names recorded in MIGRATION-TABLE.md"
    - "No published page contains a .planning/ path a public reader cannot open"
    - "The two GSD-framed pages read as reference documentation, with no phase-artifact title and no audit-trail pointer"
    - "Every page whose source made per-chip or per-protocol claims carries a database stamp"
    - "The live 12-orphan failure of wiki.py links is captured as evidence before Home.md is rewritten"
  artifacts:
    - path: ".planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt"
      provides: "WIKI-05's demonstrated failure, taken against the real wiki rather than a fixture"
      min_lines: 12
  key_links:
    - from: "the 12 published wiki pages"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "the firestarter-claim-stamp comment carrying a truncated sha256 of the database"
      pattern: "firestarter-claim-stamp: db-sha256-16="
---

<objective>
Get the content of all 12 migrating `doc/` files onto the live `firestarter_prom` wiki by cloning
`firestarter_prom.wiki.git`, copying each file to its recorded page name, applying an enumerated and
bounded set of edits, committing and pushing (D-19). Then — before anything else touches `Home.md` —
run `wiki.py links` against the clone and capture the 12-orphan failure that the currently-live Home
already produces.

Purpose: this is the migration itself. It is also the one place where the phase's honesty guarantee
is either earned or lost: **every page is produced by copying the source file and editing it, never
by retyping or summarising it.** A copied-then-edited page makes the HONEST-01 multiset comparison
green by construction; a retyped page makes it a lottery.

Output: 12 new pages on a live public wiki, and one captured RED.

**This plan writes to a live, public page.** `autonomous: false` is deliberate and load-bearing:
this project's `--auto` / `--chain` mode auto-approves human-verify gates, so the frontmatter flag is
what actually stops an unattended outward-facing publish.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-CONTEXT.md
@.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-RESEARCH.md
@.planning/v1.35/MIGRATION-TABLE.md
@tools/wiki/wiki.py
</context>

<!-- planner-discipline-allow: planning/ -->
<!-- planner-discipline-allow: PY32F071 -->
<!-- planner-discipline-allow: CITED: -->
<!-- planner-discipline-allow: audit trail -->

<tasks>

<task type="auto">
  <name>Task 1: Clone the wiki and lay down the three firmware-sourced pages</name>
  <read_first>
    - `.planning/v1.35/MIGRATION-TABLE.md` — the page names, rendered titles and pre-deletion SHAs filled by plan 168-01
    - `tools/wiki/wiki.py:52,141-166,169-207` — `_LEGAL_TARGET_RE`, `check_page_names`, `check_link_forms`; these define what a legal page name and a legal internal link are
    - `firestarter/doc/PROTOCOLS.md` § 0 only (the two tables) — read the table shapes, not the whole 556 lines
    - `firestarter/doc/SHIELD-REVISIONS.md`, `firestarter/doc/AT28C04-ADAPTER.md`
  </read_first>
  <files>working clone of firestarter_prom.wiki.git (scratch path, outside all three repositories)</files>
  <action>
    Clone `https://github.com/henols/firestarter_prom.wiki.git` into a scratch directory **outside**
    `/workspaces/firestarter`, `/workspaces/firestarter_app` and any tracked path in the meta repo.
    The clone is anonymous and needs no token (verified). WIKI-02 forbids an in-repo mirror of wiki
    content, so nothing produced in this task may ever be `git add`ed in the meta repo.

    For each of the three firmware-sourced files, `cp` the source into the clone under its recorded
    page name — `PROTOCOLS.md` to `Programming-Protocols.md`, `SHIELD-REVISIONS.md` to
    `Shield-Revisions.md`, `AT28C04-ADAPTER.md` to `AT28C04-Adapter.md` — and then apply only these
    edits. Do not rewrite, condense, reorder or re-author any content; activation decision 4 is
    relocate-and-correct only.

    (a) **Title.** Replace the H1 with the rendered title from `MIGRATION-TABLE.md`.

    (b) **Unopenable planning paths (D-11).** Remove every reference to a repository-internal
    planning path — measured spread for these three files: `PROTOCOLS.md` ×6 (two of them on one
    line at `:20`), `SHIELD-REVISIONS.md` ×4, `AT28C04-ADAPTER.md` ×1. The governing test is
    D-11's: *can a public reader act on this?* A path into a directory a wiki reader cannot open
    fails it and goes. A phase number inside a sentence passes it and **stays** — do not strip the
    52 in-prose phase mentions; stripping them would be rewriting, which decision 4 forbids, and it
    would flood the HONEST-01 comparison with edit noise. Where removing the path leaves a dangling
    sentence, rewrite the sentence minimally to stand on its own; where the path was the whole
    value of the line, delete the line.

    (c) **Cross-file links.** These three files contain no cross-file `.md` targets. Leave the
    same-page anchors alone.

    (d) **Claim stamp (D-08/D-09).** Append to `Programming-Protocols.md`, `Shield-Revisions.md` and
    `AT28C04-Adapter.md` a stamp as the file's last line, in exactly this form — an HTML comment, so
    it is invisible in the rendered page:
    `<!-- firestarter-claim-stamp: db-sha256-16=<16 hex chars> verified=<YYYY-MM-DD> -->`
    The hash is the first 16 characters of the sha256 of
    `firestarter_app/firestarter/data/chip_database.json`; compute it fresh rather than copying the
    `0cfd3a83e881bfcc` value measured on 2026-08-31, because plan 168-03 regenerates the database.
    Note in the summary that D-08's substitution is a decision, not an implementation detail: the
    requirement's literal "generated from DB vN" is unsatisfiable because `chip_database.json`
    carries no version field anywhere, and a content hash plus a date is the honest substitute
    because it changes exactly when the database changes.

    (e) **Claim region (D-09, revised on the evidence).** Wrap `Programming-Protocols.md` §0's two
    tables — and nothing else — between the sentinels `<!-- firestarter-claims-begin -->` and
    `<!-- firestarter-claims-end -->` on their own lines. Do the same around the part-number table
    in `AT28C04-Adapter.md`. **Do not add a region to `Shield-Revisions.md`**: it asserts zero
    database chips, and 11 of its 20 hex tokens collide numerically with algorithm values purely by
    coincidence, so a region there would produce an 11-of-20 pass on a page that makes no chip
    claim at all. It carries a stamp only, and plan 168-12’s checker reports that page as
    stamp-only rather than folding it into a pass. Free-text scraping is measurably falsified — 3%
    of part tokens in one migrating file resolve, and the rest are correct family wildcards — so
    the region is what bounds the check, and everything outside it is out of scope by construction.

    (f) **Table shape (Pitfall 6).** `Programming-Protocols.md` §0's two tables are parsed by a
    downstream gate that demands a 7-column bucket row starting `| 0xNN |` and a 4-column handler
    family row. **Migrate both tables byte-for-byte.** Do not reflow, re-align, reorder columns or
    re-pad cells. A reformat silently reduces the parse to zero rows and the gate then passes
    vacuously.
  </action>
  <verify>
    <automated>python3 tools/wiki/wiki.py links --source-dir "$WIKI_CLONE" 2>&1 | tee /tmp/w.log; grep -q 'Programming-Protocols' /tmp/w.log; test "$(grep -c '^| 0x' "$WIKI_CLONE/Programming-Protocols.md")" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - `Programming-Protocols.md`, `Shield-Revisions.md` and `AT28C04-Adapter.md` exist in the clone
    - `grep -c 'planning/' "$WIKI_CLONE"/Programming-Protocols.md "$WIKI_CLONE"/Shield-Revisions.md "$WIKI_CLONE"/AT28C04-Adapter.md` returns 0 for each
    - `grep -c '^| 0x' "$WIKI_CLONE/Programming-Protocols.md"` returns at least 12 — §0's bucket table survived the copy with its row shape intact
    - `diff <(sed -n '/firestarter-claims-begin/,/firestarter-claims-end/p' "$WIKI_CLONE/Programming-Protocols.md" | grep '^|') <(grep '^|' firestarter/doc/PROTOCOLS.md | head -n "$(sed -n '/firestarter-claims-begin/,/firestarter-claims-end/p' "$WIKI_CLONE/Programming-Protocols.md" | grep -c '^|')")` shows no differences in the §0 rows — byte-for-byte, per Pitfall 6
    - Each of the three files ends with a line matching `firestarter-claim-stamp: db-sha256-16=[0-9a-f]{16} verified=[0-9]{4}-[0-9]{2}-[0-9]{2}`
    - The computed hash equals `sha256sum firestarter_app/firestarter/data/chip_database.json | cut -c1-16` at execution time
    - Exactly two of the three files contain a `firestarter-claims-begin` sentinel; `Shield-Revisions.md` contains none
    - `python3 tools/wiki/wiki.py links --source-dir "$WIKI_CLONE"` reports the new pages by name in its orphan list (they are not yet linked from Home — expected at this point)
    - No file under any tracked path in `/workspaces`, `/workspaces/firestarter` or `/workspaces/firestarter_app` was created or modified by this task
  </acceptance_criteria>
  <done>Three firmware-sourced pages exist in the clone, copied not retyped, de-framed, stamped, and with §0's tables byte-identical to the source.</done>
</task>

<task type="auto">
  <name>Task 2: Lay down the nine app-sourced pages, including the two de-framed ones</name>
  <read_first>
    - `firestarter_app/doc/pinout-safety-review.md` and `firestarter_app/doc/sram-nvram-behavior.md` in full — these two are the LEGACY-06 targets and are short (88 and 114 lines)
    - `firestarter_app/doc/package-details.md:7`, `firestarter_app/doc/protocol-flags.md:7`, `firestarter_app/doc/protocol-id.md:7,22`, `firestarter_app/doc/infoic-field-dictionary.md:146` — the intra-corpus references that must be rewritten during the move, not after
    - `tools/wiki/wiki.py:52` — `_LEGAL_TARGET_RE` rejects a `.md` suffix on an internal link target
  </read_first>
  <files>working clone of firestarter_prom.wiki.git (scratch path, outside all three repositories)</files>
  <action>
    Copy the nine app-sourced files into the clone under their recorded page names —
    `beta-testing-install.md` → `Beta-Testing-Install.md`, `community-validation.md` →
    `Community-Validation.md`, `infoic-field-dictionary.md` → `Infoic-Field-Dictionary.md`,
    `lockable-proms.md` → `Lockable-PROMs.md`, `package-details.md` → `Package-Details.md`,
    `pinout-safety-review.md` → `Pinout-Safety-Review.md`, `protocol-flags.md` →
    `Protocol-Flags.md`, `protocol-id.md` → `Protocol-ID.md`, `sram-nvram-behavior.md` →
    `SRAM-and-NVRAM-Behavior.md` — then apply the same bounded edit set as Task 1 (title, unopenable
    planning paths, stamp) plus the three below. **`PY32F071-FIRMWARE-INSTALL.md` is not copied**:
    it is deferred by operator decision because the PY32F071 work is planning-stage with
    proof-of-concept code, so an install guide would promise a capability the project cannot back.

    (a) **Cross-file link rewrites.** Three relative `.md` targets exist across the corpus and all
    three are in these nine files: `beta-testing-install.md` → `community-validation.md`;
    `package-details.md` → `infoic-field-dictionary.md#protect-flags-bits-14-15`;
    `protocol-flags.md` → the same anchor. A `.md` suffix is rejected by `_LEGAL_TARGET_RE`, so the
    correct rewrite is the bare page stem plus the anchor —
    `[Text](Infoic-Field-Dictionary#protect-flags-bits-14-15)`. `protocol-id.md:22` cites the
    firmware `PROTOCOLS.md` across repositories; rewrite it as a link to `Programming-Protocols`.
    Leave the 18 same-page anchors untouched. Case must match the page filename exactly —
    `check_link_forms` rejects a case mismatch with its own message, and GitHub resolves
    case-insensitively so it will never report the problem itself.

    (b) **LEGACY-06 de-framing (D-11, D-12).** `Pinout-Safety-Review.md` and
    `SRAM-and-NVRAM-Behavior.md` both ship — they are not dropped, because each carries genuine
    operator-facing safety content (the DIP24 five-volt-only guarantee and the NVRAM blank-check
    limitation) and dropping content that tells an operator what not to do to a chip is the worse
    outcome LEGACY-06 permits. Each loses: the phase-number suffix in its title; its audit-trail
    pointer line into a repository-internal planning directory; and, in
    `SRAM-and-NVRAM-Behavior.md`, the three inline citation markers at `:35`, `:75` and `:97` that
    point at a planning research file — those are the same defect in a different syntax and would
    survive a search for the audit-trail phrase alone. Rewrite each opening paragraph so the page
    reads as reference documentation to someone who has never heard of a GSD phase.

    (c) **Claim regions.** Add a `firestarter-claims-begin` / `firestarter-claims-end` pair around
    the algorithm-value table in `Protocol-ID.md` and nowhere else in these nine. The remaining
    seven claim-bearing pages carry a stamp only. `Beta-Testing-Install.md` is the one page in the
    corpus with no per-chip or per-protocol claim at all and needs neither a region nor a stamp;
    confirm that before omitting it.
  </action>
  <verify>
    <automated>test "$(ls "$WIKI_CLONE"/*.md | wc -l)" -eq 15 && ! grep -rq 'planning/' "$WIKI_CLONE"/*.md && ! grep -rqE '\]\([A-Za-z0-9-]+\.md' "$WIKI_CLONE"/*.md && python3 tools/wiki/wiki.py links --source-dir "$WIKI_CLONE" 2>&1 | grep -c 'orphan'</automated>
  </verify>
  <acceptance_criteria>
    - The clone holds exactly 15 markdown files: the 12 new pages plus `Home.md`, `_Sidebar.md` and the 167-era published-model page (still unrewritten at this point — plan 168-08 owns it)
    - `grep -rl 'planning/' "$WIKI_CLONE"/*.md` returns nothing
    - `grep -rlE '\]\([A-Za-z0-9._-]+\.md[)#]' "$WIKI_CLONE"/*.md` returns nothing — no internal link carries a file suffix
    - `Pinout-Safety-Review.md` and `SRAM-and-NVRAM-Behavior.md` each have an H1 with no phase number in it, and `grep -c 'audit trail' <file>` returns 0 for both
    - `grep -c 'CITED:' "$WIKI_CLONE/SRAM-and-NVRAM-Behavior.md"` returns 0
    - `PY32F071` appears in no file in the clone
    - 11 of the 12 new pages carry a `firestarter-claim-stamp` line; `Beta-Testing-Install.md` is the one that does not
    - Exactly 3 pages across the whole corpus carry a `firestarter-claims-begin` sentinel: `Programming-Protocols`, `AT28C04-Adapter`, `Protocol-ID`
    - For each of the 12 pages, `wc -l` is within 10% of its source file's line count — a page far shorter than its source was summarised, not migrated
  </acceptance_criteria>
  <done>All 12 pages exist in the clone with their claims intact, their unopenable paths gone, their cross-file links legal, and the two GSD-framed pages reading as plain reference material.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Push the 12 pages, then capture the live orphan failure before touching Home</name>
  <read_first>
    - `tools/wiki/wiki.py:210-228` — `check_orphans`; only `Home.md` counts as reachability evidence
    - `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-RESEARCH.md` § "H-3: the WIKI-05 demonstrated-RED is free but time-boxed"
  </read_first>
  <files>working clone of firestarter_prom.wiki.git, .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt</files>
  <what-built>
    12 documentation pages staged in a clone of the live public wiki, copied from their `doc/`
    sources with a bounded edit set, stamped against the chip database, and with the two
    GSD-framed pages rewritten as plain reference documentation.
  </what-built>
  <action>
    Commit the 12 new pages in the clone as a single commit and push to `master` on
    `https://github.com/henols/firestarter_prom.wiki.git`. Per D-19 this is a local operator-run
    action; authenticate the push with the locally authenticated `gh` credential rather than any
    workflow token. Record the resulting wiki commit SHA in the plan summary.

    **Immediately after the push and before anything rewrites `Home.md`**, run
    `python3 tools/wiki/wiki.py links --source-dir <clone>` and write its full stdout and stderr to
    `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt`,
    together with the observed exit status on its own line. The currently-live `Home.md` lists the
    twelve page names as plain bullets rather than links, so this run reports all 12 as orphans and
    exits 1. That is WIKI-05's demonstrated failure, taken against the real wiki on first contact
    rather than against a fixture — strictly stronger evidence, and it costs nothing. It is also
    perishable: plan 168-08 rewrites `Home.md`, and once it does, this evidence cannot be recreated.
    Do not rewrite `Home.md` in this plan.
  </action>
  <how-to-verify>
    1. Open `https://github.com/henols/firestarter_prom/wiki` in a browser.
    2. Confirm 12 new pages are listed in the wiki's page index.
    3. Open `Programming-Protocols` and confirm its §0 tables render as tables, not as raw pipes.
    4. Open `AT28C04-Adapter` and `SRAM-and-NVRAM-Behavior` and confirm both page titles render
       correctly — these are the two hyphen-hazard names.
    5. Confirm no page shows a visible stamp or sentinel comment — they are HTML comments and must
       be invisible to a reader.
    6. Open `Pinout-Safety-Review` and confirm nothing on it requires knowing what a GSD phase is.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what renders wrongly</resume-signal>
  <verify>
    <automated>test -s .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt && test "$(grep -c 'orphan page not linked' .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt)" -eq 12 && grep -q 'exit 1' .planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/evidence/wiki05-live-orphan-RED.txt</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote https://github.com/henols/firestarter_prom.wiki.git master` returns a SHA newer than `0155a854752fa9088743daaedc2f2472223dd713`
    - A fresh clone of the wiki contains all 12 page files
    - `evidence/wiki05-live-orphan-RED.txt` contains exactly 12 lines matching `orphan page not linked`, and names each of the 12 page stems
    - The recorded exit status in that file is 1, not 0 and not 2 — an exit 2 would mean the check never ran
    - `Home.md` in the clone is byte-identical to its pre-push content — this plan did not touch it
    - The wiki commit SHA is recorded in the summary
  </acceptance_criteria>
  <done>The 12 pages are published, and the check that will guard their reachability has been seen to fail on the real wiki, for the right reason, with the failure recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → live public wiki | A push here is a publication; there is no review, no pull request and no CI gate on the result |
| source document → published page | Every edit is an opportunity to soften a claim the source made carefully |
| scratch clone → tracked repository | An accidental `git add` of the clone would recreate the in-repo mirror WIKI-02 forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-07 | Tampering | the 12 published pages | critical | mitigate | Pages are produced by `cp` plus an enumerated edit list, never retyped; a per-page line-count band catches a summarised page; plan 168-11’s multiset comparison is the independent oracle |
| T-168-08 | Information disclosure | pushing to a public wiki | high | mitigate | `autonomous: false` on the plan and a blocking human-verify gate on the push task — the frontmatter flag is what stops an unattended publish, because this project's auto mode approves human-verify gates |
| T-168-09 | Repudiation | the WIKI-05 demonstrated failure | high | mitigate | The RED is captured between the push and the Home rewrite and its exit status is recorded; an exit 2 recorded as evidence would mean the check never ran and is rejected by the acceptance criteria |
| T-168-10 | Elevation of privilege | scratch clone leaking into a tracked repo | medium | mitigate | The clone path is required to be outside all three repositories; an acceptance criterion asserts no tracked file changed |
| T-168-SC | Tampering | package installs | low | accept | No package is installed; `git`, `cp` and `python3` only |
</threat_model>

<verification>
- A fresh anonymous clone of `firestarter_prom.wiki.git` contains 15 markdown files
- `python3 tools/wiki/wiki.py links --source-dir <fresh clone>` exits 1 with 12 orphan lines
- `evidence/wiki05-live-orphan-RED.txt` holds that output verbatim with its exit status
- `git -C /workspaces status --porcelain` shows only the evidence file
</verification>

<success_criteria>
Twelve documents are readable on the live wiki with their claims and their negative statements
intact, no reader-unopenable path survives on any of them, and WIKI-05's guard has been seen to fail
against the real thing.
</success_criteria>

<artifacts_this_phase_produces>
The complete symbol namespace for phase 168. **Bold** entries are owned by this plan.

**New meta-repo files:** `tools/wiki/dispatch_mirror.py` · `tools/wiki/honest01_claims.py` ·
`tools/wiki/honest02_truth.py` · `tools/wiki/claim-vocabulary.json` · `tools/wiki/claim-allowlist.json`

**New/changed functions:** `wiki.py::check_sidebar_lists_every_page` ·
`dispatch_mirror.py::{parse_claims_region, parse_bucket_rows, parse_family_rows, check_dispatch_mirror, main}` ·
`honest01_claims.py::{load_vocabulary, parse_migration_table, read_source_at_sha, count_tokens, compare_multisets, main}` ·
`honest02_truth.py::{load_database, load_allowlist, extract_claim_region, find_stamp, check_stamp_present, check_claims_resolve, check_stamp_freshness, main}`

**CLI surface:** `wiki.py links --source-dir` (REQUIRED) · `honest01_claims.py --table --wiki-dir --vocab --repo-root` ·
`honest02_truth.py --wiki-dir --db --allowlist` · `dispatch_mirror.py --wiki-dir --app-dir --fw-dir`

**Selftest case names (final set, 9):** `orphan_exit_1` · `sidebar_link_is_not_evidence` ·
`broken_link_exit_1` · `md_suffix_link_exit_1` · `illegal_filename_exit_1` ·
`wiki05_unreferenced_page_exit_1` · `dispatch_mirror_planted_drift_exit_1` ·
`honest01_weakened_claim_exit_1` · `honest02_absent_part_number_exit_1`.
**New fixture helper:** `new_source_repo <dir>`

**Data-file keys:** `claim-vocabulary.json` → `schema_version`, `families.*`, `expected_zero` ·
`claim-allowlist.json` → `schema_version`, `part_tokens[].{token,reason}`, `algorithm_tokens[].{token,reason}`

**Wiki markup sentinels (authored by this plan):**
**`<!-- firestarter-claim-stamp: db-sha256-16=<16hex> verified=<YYYY-MM-DD> -->`** ·
**`<!-- firestarter-claims-begin -->` / `<!-- firestarter-claims-end -->`**

**Workflow paths:** `.github/workflows/wiki-check.yml` (rewritten, job `wiki-truth`) ·
`.github/workflows/wiki-publish.yml` (deleted) · `wiki/` (deleted)

**Wiki page names (14):** Home · How-To-Edit-This-Wiki · **Programming-Protocols · Shield-Revisions ·
AT28C04-Adapter · Beta-Testing-Install · Community-Validation · Infoic-Field-Dictionary ·
Lockable-PROMs · Package-Details · Pinout-Safety-Review · Protocol-Flags · Protocol-ID ·
SRAM-and-NVRAM-Behavior**

**New `MIGRATION-TABLE.md` column:** `Pre-deletion SHA`

**Evidence files under `.planning/phases/168-…/evidence/`:** `oracle-readable.txt` ·
**`wiki05-live-orphan-RED.txt`** · `wiki05-live-GREEN.txt` · `dispatch-mirror-planted-RED.txt` ·
`honest01-weakened-claim-RED.txt` · `honest01-live-GREEN.txt` · `honest02-fixture-RED.txt` ·
`honest02-live-run.txt` · `migrate03-py311-suite.txt` · `migrate03-sdist-doc-delta.txt` ·
`selftest-9-cases.txt`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/168-migrate-the-13-doc-files-moved-without-upgrading-a-claim/168-05-SUMMARY.md` when done
</output>
