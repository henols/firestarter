---
phase: 33-silkscreen-label-code-alias-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - firestarter/src/proms/eprom.cpp
  - firestarter/src/proms/flash_intel.cpp
  - firestarter/src/proms/memory.cpp
  - firestarter/src/proms/flash_type_4.cpp
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/src/proms/flash_utils.cpp
  - firestarter/src/hardware_operations.cpp
autonomous: true
requirements:
  - ALIAS-02
  - ALIAS-03
tags:
  - firmware
  - call-sites
  - proms
  - migration

must_haves:
  truths:
    - "All call-sites in firestarter/src/proms/eprom.cpp (22 lines) reference CTRL_* / PIN_* canonical names — zero old-name references outside comments"
    - "All call-sites in firestarter/src/proms/flash_intel.cpp (7 lines) reference CTRL_* names"
    - "firestarter/src/proms/memory.cpp (6 lines + load-bearing aliasing comment) references CTRL_* names; comment at :142-144 refreshed to new names"
    - "firestarter/src/proms/flash_type_4.cpp (3 lines), eeprom_28c.cpp (3 lines), flash_utils.cpp (3 lines) all migrated to CTRL_*"
    - "firestarter/src/hardware_operations.cpp (2 lines at :27, :30) migrated; CONTROL_REGISTER (latch selector) untouched"
    - "src/proms/ + src/hardware_operations.cpp is grep-zero for old names outside comments"
    - "All 3 AVR envs build clean; pio test -e native green; .hex byte-identical to baseline (old #defines in rurp_shield.h:25-89 still in place per D-06 restructure — Wave 3 final task deletes them atomically)"
  artifacts:
    - path: firestarter/src/proms/eprom.cpp
      provides: "UV-EPROM handler (algorithms 0x07/0x08/0x0B) with CTRL_*/PIN_* call-sites"
      contains: "CTRL_VPP_REGULATOR_ENABLE"
    - path: firestarter/src/proms/flash_intel.cpp
      provides: "Intel 28F flash handler with CTRL_VPP_P1_ENABLE call-sites"
      contains: "CTRL_VPP_P1_ENABLE"
    - path: firestarter/src/proms/memory.cpp
      provides: "Top-level dispatch + bus_config mask math with CTRL_* call-sites; load-bearing aliasing comment refreshed"
      contains: "CTRL_VPP_VPE_DROP_ENABLE and CTRL_ADDRESS_LINE_16 share"
  key_links:
    - from: firestarter/src/proms/memory.cpp
      to: firestarter/include/rurp_pinout.h
      via: "CTRL_* references resolve through rurp_shield.h transitive include (added in Wave 3 final task) — until then, rurp_pinout.h must be #included directly by any .cpp using the new names, OR resolution must happen through #include chain ending at rurp_pinout.h"
      pattern: "CTRL_VPP_REGULATOR_ENABLE|CTRL_ADDRESS_LINE_16|CTRL_VPP_VPE_DROP_ENABLE"
    - from: firestarter/src/proms/eprom.cpp
      to: firestarter/include/rurp_pinout.h
      via: "all 22 call-sites use canonical CTRL_* names"
      pattern: "CTRL_VPP_REGULATOR_ENABLE.*CTRL_VPP_VPE_DROP_ENABLE"
---

<objective>
Hard-rename all 46 call-sites across `firestarter/src/proms/{eprom,flash_intel,memory,flash_type_4,eeprom_28c,flash_utils}.cpp` + `firestarter/src/hardware_operations.cpp` from the 8 old names (VPE_ENABLE, VPE_TO_VPP, P1_VPP_ENABLE, A9_VPP_ENABLE, READ_WRITE, REGULATOR, ADDRESS_LINE_16/17/18) to the canonical `CTRL_*` namespace declared in `rurp_pinout.h` Wave 1. Per Pattern 2 (RESEARCH.md) + D-06: hard-rename, no shim wrapper at call-sites. After Wave 1, `rurp_pinout.h` declares the new canonical names AND `rurp_shield.h:25-89` retains its original `#define`s (unchanged) — both headers are active. Wave 2 migrates the src/proms/* + hardware_operations.cpp call-sites to the new names. The remaining `rurp_shield.h` old #defines simply become unused for these files; full deletion is deferred to Wave 3's final task (atomic D-06 enforcement).

Important: each `.cpp` migrated in this wave must be able to resolve the new `CTRL_*` names at compile time. Since `rurp_shield.h` does not yet `#include "rurp_pinout.h"` (that happens in Wave 3 Task 4 atomic edit), each renamed `.cpp` must either (a) already pull in `rurp_pinout.h` through an existing transitive include chain, or (b) add a direct `#include "rurp_pinout.h"` near its existing `#include "rurp_shield.h"` line. The executor verifies via build: if `error: 'CTRL_VPP_REGULATOR_ENABLE' was not declared in this scope` appears, add the explicit `#include "rurp_pinout.h"` to that .cpp.

Purpose: This wave migrates the largest cluster of call-sites (~46 lines including the 22-line cluster in eprom.cpp). After Wave 2 + Wave 3, the rurp_shield.h:25-89 old #defines are deleted (Wave 3 final task), proving D-06 hard-rename is enforced. The load-bearing comment refresh in `memory.cpp:142-144` documents the `CTRL_ADDRESS_LINE_16 == CTRL_VPP_VPE_DROP_ENABLE` aliasing semantics under the new names (Pitfall 1 documentation).

Output: 7 modified .cpp files; ~46 lines rewritten; build green on all 3 AVR envs; native test green; .hex byte-identical to Wave 0 baseline (#define expansion produces identical token stream regardless of source identifier).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-CONTEXT.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-RESEARCH.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-VALIDATION.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-01-SUMMARY.md
@firestarter/include/rurp_pinout.h
@firestarter/include/rurp_shield.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename call-sites in firestarter/src/proms/eprom.cpp (22 lines) + memory.cpp (6 lines + aliasing comment refresh)</name>
  <files>firestarter/src/proms/eprom.cpp, firestarter/src/proms/memory.cpp</files>
  <read_first>
    /workspaces/firestarter/src/proms/eprom.cpp,
    /workspaces/firestarter/src/proms/memory.cpp,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md,
    /workspaces/firestarter/include/rurp_pinout.h
  </read_first>
  <action>
    Per Pattern Assignment §5 + §7 (PATTERNS.md): edit `/workspaces/firestarter/src/proms/eprom.cpp` and `/workspaces/firestarter/src/proms/memory.cpp` to rename ALL old-name references with these exact mappings: `VPE_TO_VPP` → `CTRL_VPP_VPE_DROP_ENABLE`; `REGULATOR` → `CTRL_VPP_REGULATOR_ENABLE`; `VPE_ENABLE` → `CTRL_VPE_ENABLE`; `P1_VPP_ENABLE` → `CTRL_VPP_P1_ENABLE`; `A9_VPP_ENABLE` → `CTRL_VPP_A9_ENABLE`; `READ_WRITE` → `CTRL_READ_WRITE`; `ADDRESS_LINE_16` → `CTRL_ADDRESS_LINE_16`; `ADDRESS_LINE_17` → `CTRL_ADDRESS_LINE_17`; `ADDRESS_LINE_18` → `CTRL_ADDRESS_LINE_18`.

    If the build fails after rename with `error: 'CTRL_*' was not declared in this scope`, add `#include "rurp_pinout.h"` directly after the existing `#include "rurp_shield.h"` (or equivalent header chain) in each affected .cpp — rurp_shield.h does NOT yet `#include "rurp_pinout.h"` (that's Wave 3 Task 4's atomic edit).

    eprom.cpp expected target lines (per PATTERNS.md call-site map): `:114` (`programming_bits = VPE_ENABLE;` → CTRL_VPE_ENABLE), `:143-149` (canonical `REGULATOR | VPE_TO_VPP` pattern in `eprom_write_init` — 4 references on these lines plus accompanying comment refresh), `:180, :197` (`REGULATOR` set state changes), `:200` (`A9_VPP_ENABLE`), `:204` (`REGULATOR | A9_VPP_ENABLE`), `:219, :222` (`REGULATOR | VPE_TO_VPP`), `:270` (`REGULATOR | VPE_TO_VPP, 0`), `:276` (`REGULATOR` + comment "without dropping resistor"), `:279` (`A9_VPP_ENABLE | VPE_ENABLE` + comment about VPE_TO_VPP), `:286` (`REGULATOR | A9_VPP_ENABLE | VPE_ENABLE`), `:317-321` (using_p1_as_vpp helper — `VPE_ENABLE` and `P1_VPP_ENABLE` redirect), `:327-328` (`REGULATOR` get + set). Refresh comments at `:144, :147, :276, :278, :279` that reference old names ("VPE_TO_VPP dropping resistor", "without dropping resistor", "assumes VPE_TO_VPP isn't set", "VPP via VPE_ENABLE") to the new CTRL_* names.

    memory.cpp expected target lines: `:139` (`top_address` mask uses `ADDRESS_LINE_16 | ADDRESS_LINE_17 | ADDRESS_LINE_18 | READ_WRITE` — all four renamed), `:140` (`mask = A9_VPP_ENABLE | VPE_ENABLE | P1_VPP_ENABLE | REGULATOR` — all four renamed), `:142-144` (load-bearing aliasing comment block — refresh per RESEARCH §Pitfall 1: "// CTRL_VPP_VPE_DROP_ENABLE and CTRL_ADDRESS_LINE_16 share the same CONTROL bit — preserving CTRL_VPP_VPE_DROP_ENABLE would corrupt A16 for 32-pin (512KB) chips. DIP32 chips use CTRL_VPP_P1_ENABLE instead."), `:145` (`mask |= VPE_TO_VPP;` → `mask |= CTRL_VPP_VPE_DROP_ENABLE;`). Any other references in memory.cpp also renamed (file has 6 total per RESEARCH inventory).

    Use the Edit tool one occurrence at a time (NOT `sed -i`) so each diff is reviewable and comment refreshes are handled deliberately. After each file, run the per-file grep verification described in `<verify>`.
  </action>
  <verify>
    <automated>NONCOMMENT_HITS_EPROM=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|ADDRESS_LINE_1[678])\b' /workspaces/firestarter/src/proms/eprom.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); NONCOMMENT_HITS_MEM=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|ADDRESS_LINE_1[678])\b' /workspaces/firestarter/src/proms/memory.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); [ "$NONCOMMENT_HITS_EPROM" -eq 0 ] && [ "$NONCOMMENT_HITS_MEM" -eq 0 ] && grep -q 'CTRL_VPP_REGULATOR_ENABLE' /workspaces/firestarter/src/proms/eprom.cpp && grep -q 'CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/src/proms/memory.cpp && grep -q 'CTRL_VPP_VPE_DROP_ENABLE and CTRL_ADDRESS_LINE_16 share' /workspaces/firestarter/src/proms/memory.cpp && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|ADDRESS_LINE_1[678])\b' firestarter/src/proms/eprom.cpp | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//'` returns 0 matches
    - Same grep against `firestarter/src/proms/memory.cpp` returns 0 matches
    - eprom.cpp contains canonical patterns `CTRL_VPP_REGULATOR_ENABLE`, `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE`, `CTRL_VPP_A9_ENABLE | CTRL_VPE_ENABLE`
    - memory.cpp `:139-145` mask-math block uses `CTRL_ADDRESS_LINE_16 | CTRL_ADDRESS_LINE_17 | CTRL_ADDRESS_LINE_18 | CTRL_READ_WRITE` and `CTRL_VPP_A9_ENABLE | CTRL_VPE_ENABLE | CTRL_VPP_P1_ENABLE | CTRL_VPP_REGULATOR_ENABLE`
    - memory.cpp aliasing comment refreshed to `// CTRL_VPP_VPE_DROP_ENABLE and CTRL_ADDRESS_LINE_16 share the same CONTROL bit …`
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
    - `cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1` exits 0
    - `cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex` exits 0 (still byte-identical — both rurp_pinout.h new names and rurp_shield.h old #defines expand to the same hex values; no machine code change)
  </acceptance_criteria>
  <done>eprom.cpp + memory.cpp call-sites all use CTRL_* names; comments refreshed; all 3 AVR builds + native test pass; .hex byte-identical to baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename call-sites in flash_intel.cpp (7 lines) + flash_type_4.cpp (3 lines) + eeprom_28c.cpp (3 lines) + flash_utils.cpp (3 lines)</name>
  <files>
    firestarter/src/proms/flash_intel.cpp,
    firestarter/src/proms/flash_type_4.cpp,
    firestarter/src/proms/eeprom_28c.cpp,
    firestarter/src/proms/flash_utils.cpp
  </files>
  <read_first>
    /workspaces/firestarter/src/proms/flash_intel.cpp,
    /workspaces/firestarter/src/proms/flash_type_4.cpp,
    /workspaces/firestarter/src/proms/eeprom_28c.cpp,
    /workspaces/firestarter/src/proms/flash_utils.cpp,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
  </read_first>
  <action>
    Per Pattern Assignments §6, §8, §9, §10 (PATTERNS.md): apply the same name mappings as Task 1 across 4 files. If any file fails to build with `error: 'CTRL_*' was not declared in this scope`, add `#include "rurp_pinout.h"` directly after the existing `#include "rurp_shield.h"` (rurp_shield.h does not yet pull in rurp_pinout.h — that's Wave 3 Task 4's atomic edit). flash_intel.cpp lines `:105-114, :120, :145, :157` — all `REGULATOR | P1_VPP_ENABLE` → `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_P1_ENABLE`; comments at `:34, :80` ("Caller already asserted REGULATOR | P1_VPP_ENABLE") refreshed to new names. flash_type_4.cpp lines `:108, :116, :132` — `REGULATOR | VPE_TO_VPP | VPE_ENABLE` triplet → `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE | CTRL_VPE_ENABLE`. eeprom_28c.cpp lines `:70, :72, :77` — `REGULATOR` set → `CTRL_VPP_REGULATOR_ENABLE`; `A9_VPP_ENABLE` set → `CTRL_VPP_A9_ENABLE`; `REGULATOR | A9_VPP_ENABLE` combined rename. flash_utils.cpp lines `:21, :25, :30` — bare `READ_WRITE` → `CTRL_READ_WRITE`. Use the Edit tool per occurrence; do not sed-bulk. Refresh comment text in each file where the old name appears in a `// comment` context that documents semantics (e.g. flash_intel.cpp `:34` + `:80` "// Caller already asserted REGULATOR | P1_VPP_ENABLE" → "// Caller already asserted CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_P1_ENABLE").
  </action>
  <verify>
    <automated>for f in /workspaces/firestarter/src/proms/flash_intel.cpp /workspaces/firestarter/src/proms/flash_type_4.cpp /workspaces/firestarter/src/proms/eeprom_28c.cpp /workspaces/firestarter/src/proms/flash_utils.cpp; do c=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' "$f" 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); [ "$c" -eq 0 ] || { echo "FAIL $f: $c"; exit 1; }; done && grep -q 'CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_P1_ENABLE' /workspaces/firestarter/src/proms/flash_intel.cpp && grep -q 'CTRL_READ_WRITE' /workspaces/firestarter/src/proms/flash_utils.cpp && (cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - Across all 4 files, `grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' <file> | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l` returns 0 for each
    - flash_intel.cpp contains pattern `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_P1_ENABLE` at ≥3 distinct lines
    - flash_type_4.cpp contains pattern `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE | CTRL_VPE_ENABLE` at ≥3 distinct lines
    - eeprom_28c.cpp contains `CTRL_VPP_REGULATOR_ENABLE` and `CTRL_VPP_A9_ENABLE` references
    - flash_utils.cpp contains `CTRL_READ_WRITE` at ≥3 distinct lines
    - `cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1` exits 0
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
    - `cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/<env>.hex /workspaces/firestarter/.pio/build/<env>/firestarter_<env>.hex` exits 0 for all 3 envs
  </acceptance_criteria>
  <done>All 4 proms .cpp files migrated; build + native test pass; .hex byte-identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rename call-sites in firestarter/src/hardware_operations.cpp (2 lines at :27, :30)</name>
  <files>firestarter/src/hardware_operations.cpp</files>
  <read_first>
    /workspaces/firestarter/src/hardware_operations.cpp,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
  </read_first>
  <action>
    Per Pattern Assignment §11 (PATTERNS.md): edit `/workspaces/firestarter/src/hardware_operations.cpp` `:25-30` block — the canonical VPP-vs-VPE branch in `hw_read_voltage()`. Rename `:27` `rurp_write_to_register(CONTROL_REGISTER, REGULATOR | VPE_TO_VPP);` → `rurp_write_to_register(CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE);`. Rename `:30` `rurp_write_to_register(CONTROL_REGISTER, REGULATOR);` → `rurp_write_to_register(CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE);`. CRITICAL: `CONTROL_REGISTER` is NOT renamed (74HC573 latch selector — out of D-03 scope per Anti-Pattern bullet in RESEARCH). Verify the file has no other references to the 8 old names. If the build fails with `error: 'CTRL_*' was not declared in this scope`, add `#include "rurp_pinout.h"` directly after the existing `#include "rurp_shield.h"` (transitive include from rurp_shield.h to rurp_pinout.h is not added until Wave 3 Task 4).
  </action>
  <verify>
    <automated>c=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' /workspaces/firestarter/src/hardware_operations.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); [ "$c" -eq 0 ] && grep -q 'CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/src/hardware_operations.cpp && grep -q 'CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE)' /workspaces/firestarter/src/hardware_operations.cpp && grep -q 'CONTROL_REGISTER' /workspaces/firestarter/src/hardware_operations.cpp && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1) && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno328pb.hex /workspaces/firestarter/.pio/build/uno328pb/firestarter_uno328pb.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/leonardo.hex /workspaces/firestarter/.pio/build/leonardo/firestarter_leonardo.hex</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' firestarter/src/hardware_operations.cpp | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l` returns 0
    - hardware_operations.cpp contains `CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE`
    - hardware_operations.cpp contains `CONTROL_REGISTER, CTRL_VPP_REGULATOR_ENABLE)` (the VPE branch — REGULATOR alone)
    - `CONTROL_REGISTER` identifier is preserved (latch selector — not renamed per D-03)
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
    - `cmp` against all 3 baseline .hex files exits 0 (byte-identical preserved — call-sites now use new CTRL_* names that expand to the same hex values declared in rurp_pinout.h, AND rurp_shield.h:25-89 old #defines remain unchanged in case any straggler call-site still references them)
  </acceptance_criteria>
  <done>hardware_operations.cpp migrated; CONTROL_REGISTER preserved; build + cmp green for all 3 envs.</done>
</task>

</tasks>

<verification>
After all 3 tasks: src/proms/*.cpp + src/hardware_operations.cpp are grep-zero for old names outside comment-only lines. Per-wave check via `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh`: Assertion 3 (cmp on all 3 .hex) PASSES — byte-identical maintained. Assertion 1 (full-tree grep-zero) still FAILS because Wave 3 has not yet touched include/rurp_hw_rev_utils.h, include/rurp_register_utils.h, src/boards/*.cpp, and test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp — AND because rurp_shield.h:25-89 still carries the old #defines as the unchanged-backward-compat substrate (Wave 3 final task deletes them atomically per D-06 restructure).
</verification>

<success_criteria>
- src/proms/ + src/hardware_operations.cpp: zero old-name references outside comments
- All 3 AVR envs build clean
- pio test -e native green
- .hex byte-identical to baseline for all 3 envs (rurp_shield.h:25-89 old #defines still in place; Wave 3 final task deletes them)
- Load-bearing aliasing comment in memory.cpp:142-144 refreshed to CTRL_* names
</success_criteria>

<output>
Create `.planning/phases/33-silkscreen-label-code-alias-migration/33-02-SUMMARY.md` recording:
- Files modified + per-file rename count
- Confirmed: pio test -e native green
- Confirmed: cmp green for all 3 .hex
- Pending for Wave 3: include/rurp_hw_rev_utils.h dispatcher rename, include/rurp_register_utils.h, src/boards/*.cpp, test/native/avr/, then atomic deletion of rurp_shield.h:25-89 old-#define block (per D-06 restructure — no shim block was ever created)
</output>
