---
phase: 33-silkscreen-label-code-alias-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - firestarter/include/rurp_hw_rev_utils.h
  - firestarter/include/rurp_register_utils.h
  - firestarter/src/boards/uno_rurp_shield.cpp
  - firestarter/src/boards/rurp_common.cpp
  - firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp
  - firestarter/include/rurp_shield.h
autonomous: true
requirements:
  - ALIAS-02
  - ALIAS-03
tags:
  - firmware
  - call-sites
  - dispatcher
  - test
  - old-define-removal
  - migration

must_haves:
  truths:
    - "include/rurp_hw_rev_utils.h dispatcher body migrated — LHS canonical mask AND RHS REV_*-prefixed names both renamed (Pitfall 3); switch-case structure unchanged"
    - "include/rurp_register_utils.h settle-check on P1_VPP_ENABLE clear migrated; CONTROL_REGISTER (latch selector) preserved"
    - "src/boards/uno_rurp_shield.cpp comment refresh (READ_WRITE → CTRL_READ_WRITE)"
    - "src/boards/rurp_common.cpp analogRead(VOLTAGE_MEASURE_PIN) → analogRead(PIN_VPP_VOLTAGE_ADC)"
    - "test/native/avr/test_flash_intel_vpp.cpp mock + assertions migrated (P1_VPP_ENABLE → CTRL_VPP_P1_ENABLE, REGULATOR → CTRL_VPP_REGULATOR_ENABLE)"
    - "Old #defines DELETED from rurp_shield.h:25-89 — D-06 hard-rename now fully enforced via atomic removal of the unchanged-backward-compat substrate (replaces the previously-planned 'remove shim block' task)"
    - "After Wave 3: grep-zero for old names + REV_[12]_ prefix family across include/ + src/ + test/ (excluding REVISION_* enum which is preserved per D-03)"
    - "All 3 AVR envs build clean; pio test -e native green; .hex byte-identical to baseline (cmp exit 0) — GATE-1.7 met"
  artifacts:
    - path: firestarter/include/rurp_hw_rev_utils.h
      provides: "dispatcher rurp_map_ctrl_reg_for_hardware_revision() with CTRL_* canonical + CTRL_*_REV1/REV2 suffix family"
      contains: "CTRL_VPP_VPE_DROP_ENABLE_REV2"
    - path: firestarter/include/rurp_register_utils.h
      provides: "rurp_write_to_register() settle-check using CTRL_VPP_P1_ENABLE"
      contains: "CTRL_VPP_P1_ENABLE"
    - path: firestarter/include/rurp_shield.h
      provides: "POST-MIGRATION state: :25-94 old #define block DELETED; latch selectors + CONFIG_VERSION + function prototypes preserved; #include rurp_pinout.h added"
      contains: "#include \"rurp_pinout.h\""
  key_links:
    - from: firestarter/include/rurp_hw_rev_utils.h
      to: firestarter/include/rurp_pinout.h
      via: "dispatcher body references CTRL_*_REV2 suffix family declared in rurp_pinout.h"
      pattern: "CTRL_VPP_VPE_DROP_ENABLE_REV2"
    - from: firestarter/include/rurp_shield.h
      to: firestarter/include/rurp_pinout.h
      via: "#include directive — after Wave 3 final task, rurp_shield.h carries no #defines for RURP-signal aliases; all canonical names live in rurp_pinout.h"
      pattern: "#include \"rurp_pinout.h\""
---

<objective>
Close the rename sweep: migrate the remaining firmware headers (`rurp_hw_rev_utils.h` dispatcher, `rurp_register_utils.h` settle-check), board-adapter source (`uno_rurp_shield.cpp` comment, `rurp_common.cpp` ADC read), and native test (`test_flash_intel_vpp.cpp` mock + assertions). Then, in this wave's final task, atomically DELETE the old `#define`s from `firestarter/include/rurp_shield.h:25-89` (the unchanged-backward-compat substrate that Waves 1 + 2 left in place, per the D-06 restructure — no new shim block was ever created; the old declarations themselves served as the backward-compat path until atomic removal). After this wave, no old-name `#define` exists anywhere in the codebase — D-06 hard-rename is fully enforced. Per Pitfall 6 (RESEARCH.md), the native test path is the highest-risk surface for the `ADDRESS_LINE_16 == VPE_TO_VPP` legacy-path aliasing (Pitfall 1), because `[env:native]` does NOT set `-D HARDWARE_REVISION`. Wave 3 must therefore run the native test suite as the final gate, and the rurp_shield.h deletion task documents an explicit rollback path if Pitfall 1 preservation in rurp_pinout.h's legacy branch is incorrect.

Purpose: This wave produces the post-rename steady state. After it lands:
- `grep -rn '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|HARDWARE_REVISION_PIN|VOLTAGE_MEASURE_PIN)\b' firestarter/include/ firestarter/src/ firestarter/test/` returns 0 hits outside legitimate `// historical note` comments
- `grep -rn 'REV_[12]_' firestarter/include/ firestarter/src/ | grep -v REVISION_` returns 0 hits
- All 3 AVR `.hex` files are byte-identical to Wave 0 baseline (`cmp` exit 0). If any drift (≤ ~50 B per board) emerges, it must be documented in the fix-commit message with per-board `wc -c` deltas.
- `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` exits 0 — Phase 33 firmware migration is verified complete.

Output: 6 modified files (4 firmware-source + 1 native test + the atomic `rurp_shield.h:25-89` block deletion); the post-Wave-3 codebase is the steady state; ALIAS-02 + ALIAS-03 firmware-side criteria met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-CONTEXT.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-RESEARCH.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-VALIDATION.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-02-SUMMARY.md
@firestarter/include/rurp_pinout.h
@firestarter/include/rurp_shield.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename include/rurp_hw_rev_utils.h dispatcher body (LHS canonical mask + RHS REV_* suffix family) + rurp_detect_hardware_revision() pin references</name>
  <files>firestarter/include/rurp_hw_rev_utils.h</files>
  <read_first>
    /workspaces/firestarter/include/rurp_hw_rev_utils.h,
    /workspaces/firestarter/include/rurp_pinout.h,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
  </read_first>
  <action>
    Per Pattern Assignment §3 (PATTERNS.md) + Pitfall 3 (RESEARCH.md): edit `/workspaces/firestarter/include/rurp_hw_rev_utils.h` `:13-58` to apply two rename passes.

    Pass 1 — dispatcher `rurp_map_ctrl_reg_for_hardware_revision()` body at `:13-35`. LHS canonical input mask: `A9_VPP_ENABLE | VPE_ENABLE | P1_VPP_ENABLE | ADDRESS_LINE_17 | READ_WRITE | REGULATOR` → `CTRL_VPP_A9_ENABLE | CTRL_VPE_ENABLE | CTRL_VPP_P1_ENABLE | CTRL_ADDRESS_LINE_17 | CTRL_READ_WRITE | CTRL_VPP_REGULATOR_ENABLE`. RHS per-rev output bits: `REV_2_VPE_TO_VPP` → `CTRL_VPP_VPE_DROP_ENABLE_REV2`; `REV_2_ADDRESS_LINE_16` → `CTRL_ADDRESS_LINE_16_REV2`; `REV_2_ADDRESS_LINE_18` → `CTRL_ADDRESS_LINE_18_REV2`; `REV_1_VPE_TO_VPP` → `CTRL_VPP_VPE_DROP_ENABLE_REV1`. Bare-name LHS conditional checks (`data & VPE_TO_VPP`, `data & ADDRESS_LINE_16`, `data & ADDRESS_LINE_18`) rename to `data & CTRL_VPP_VPE_DROP_ENABLE`, `data & CTRL_ADDRESS_LINE_16`, `data & CTRL_ADDRESS_LINE_18`. Switch-case structure, REVISION_0/1/2_0/2_1/2_2 case labels, `default: break` line, function signature ALL preserved verbatim (per D-03 — REVISION_* are revision enum values, not RURP-signal aliases).

    Pass 2 — `rurp_detect_hardware_revision()` body at `:41-58`. References to `HARDWARE_REVISION_PIN` (lines `:42, :45` per PATTERNS.md) → `PIN_HW_REVISION_DETECT_ADC`. References to `VOLTAGE_MEASURE_PIN` (lines `:43, :48, :57`) → `PIN_VPP_VOLTAGE_ADC`. Function body otherwise unchanged.

    Refresh any inline comments referencing old names. Use Edit tool per occurrence (not sed) to keep diffs reviewable. Verify post-edit: `grep -E 'REV_[12]_|REGULATOR|VPE_TO_VPP|VPE_ENABLE|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|HARDWARE_REVISION_PIN|VOLTAGE_MEASURE_PIN|ADDRESS_LINE_1[678]' /workspaces/firestarter/include/rurp_hw_rev_utils.h | grep -v REVISION_` returns 0 hits.

    Note: until Task 4 of this wave deletes `rurp_shield.h:25-89`, the old #defines still exist there. That's fine — this task migrates the dispatcher's call-sites to the new names; the old #defines just become unused for this file. Task 4 cleans them up atomically.
  </action>
  <verify>
    <automated>c=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|HARDWARE_REVISION_PIN|VOLTAGE_MEASURE_PIN|ADDRESS_LINE_1[678])\b' /workspaces/firestarter/include/rurp_hw_rev_utils.h 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); rc=$(grep -nE '\bREV_[12]_' /workspaces/firestarter/include/rurp_hw_rev_utils.h 2>/dev/null | grep -v REVISION_ | wc -l); [ "$c" -eq 0 ] && [ "$rc" -eq 0 ] && grep -q 'CTRL_VPP_VPE_DROP_ENABLE_REV2' /workspaces/firestarter/include/rurp_hw_rev_utils.h && grep -q 'CTRL_VPP_VPE_DROP_ENABLE_REV1' /workspaces/firestarter/include/rurp_hw_rev_utils.h && grep -q 'PIN_HW_REVISION_DETECT_ADC' /workspaces/firestarter/include/rurp_hw_rev_utils.h && grep -q 'PIN_VPP_VOLTAGE_ADC' /workspaces/firestarter/include/rurp_hw_rev_utils.h && grep -q 'case REVISION_2_0:' /workspaces/firestarter/include/rurp_hw_rev_utils.h && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|HARDWARE_REVISION_PIN|VOLTAGE_MEASURE_PIN|ADDRESS_LINE_1[678])\b' firestarter/include/rurp_hw_rev_utils.h | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l` returns 0
    - `grep -nE '\bREV_[12]_' firestarter/include/rurp_hw_rev_utils.h | grep -v REVISION_ | wc -l` returns 0
    - Contains all 4 CTRL_*_REV2 names from the case REVISION_2_x branch: `CTRL_VPP_VPE_DROP_ENABLE_REV2`, `CTRL_ADDRESS_LINE_16_REV2`, `CTRL_ADDRESS_LINE_18_REV2`
    - Contains `CTRL_VPP_VPE_DROP_ENABLE_REV1` in case REVISION_0/1 branch
    - Contains `PIN_HW_REVISION_DETECT_ADC` and `PIN_VPP_VOLTAGE_ADC` references in `rurp_detect_hardware_revision()`
    - `REVISION_0` / `REVISION_1` / `REVISION_2_0` / `REVISION_2_1` / `REVISION_2_2` case labels preserved (out of D-03 alias scope)
    - Function signature `uint8_t rurp_map_ctrl_reg_for_hardware_revision(rurp_register_t data)` unchanged
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
  </acceptance_criteria>
  <done>rurp_hw_rev_utils.h dispatcher fully renamed (LHS + RHS); detect-fn pin references renamed; switch-case structure preserved; all 3 AVR envs build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename include/rurp_register_utils.h (P1_VPP_ENABLE settle-check) + src/boards/uno_rurp_shield.cpp (comment refresh) + src/boards/rurp_common.cpp (VOLTAGE_MEASURE_PIN → PIN_VPP_VOLTAGE_ADC)</name>
  <files>
    firestarter/include/rurp_register_utils.h,
    firestarter/src/boards/uno_rurp_shield.cpp,
    firestarter/src/boards/rurp_common.cpp
  </files>
  <read_first>
    /workspaces/firestarter/include/rurp_register_utils.h,
    /workspaces/firestarter/src/boards/uno_rurp_shield.cpp,
    /workspaces/firestarter/src/boards/rurp_common.cpp,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
  </read_first>
  <action>
    Per Pattern Assignments §4, §12, §13 (PATTERNS.md): apply 3 narrow renames.

    rurp_register_utils.h `:38-49` block — the `case CONTROL_REGISTER:` settle-check at `:42` references `P1_VPP_ENABLE` twice in the same conditional: `if ((control_register & P1_VPP_ENABLE) > (data & P1_VPP_ENABLE))`. Rename BOTH occurrences to `CTRL_VPP_P1_ENABLE`. Preserve `CONTROL_REGISTER` case label and `rurp_map_ctrl_reg_for_hardware_revision(data)` call verbatim (latch selector + dispatcher fn — out of D-03 scope / no signature change).

    uno_rurp_shield.cpp `:29` (per Open Question Q4 recommendation): comment-only reference `// NOTE: The original code included `READ_WRITE` (0x40), which would attempt to control PB6.` → refresh to `// NOTE: The original code included `CTRL_READ_WRITE` (0x40), which would attempt to control PB6.`. This is a documentary refresh — reduces future grep-confusion for `READ_WRITE` not finding only this historical comment.

    rurp_common.cpp `:58` (per Pattern Assignment §12): `uint32_t voltage_adc_reading = analogRead(VOLTAGE_MEASURE_PIN);` → `uint32_t voltage_adc_reading = analogRead(PIN_VPP_VOLTAGE_ADC);`. This is the only old-name reference in the boards directory's executable code (PATTERNS.md confirmed leonardo_rurp_shield.cpp has 0 hits, so it is intentionally NOT in this task's file list).

    Use Edit tool per occurrence.
  </action>
  <verify>
    <automated>c1=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' /workspaces/firestarter/include/rurp_register_utils.h 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); c2=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' /workspaces/firestarter/src/boards/uno_rurp_shield.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); c3=$(grep -nE '\bVOLTAGE_MEASURE_PIN\b' /workspaces/firestarter/src/boards/rurp_common.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); [ "$c1" -eq 0 ] && [ "$c2" -eq 0 ] && [ "$c3" -eq 0 ] && grep -q 'CTRL_VPP_P1_ENABLE' /workspaces/firestarter/include/rurp_register_utils.h && grep -q 'CONTROL_REGISTER' /workspaces/firestarter/include/rurp_register_utils.h && grep -q 'CTRL_READ_WRITE' /workspaces/firestarter/src/boards/uno_rurp_shield.cpp && grep -q 'PIN_VPP_VOLTAGE_ADC' /workspaces/firestarter/src/boards/rurp_common.cpp && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/include/rurp_register_utils.h` contains `CTRL_VPP_P1_ENABLE` twice in the `case CONTROL_REGISTER:` settle-check
    - `firestarter/include/rurp_register_utils.h` still contains `CONTROL_REGISTER` case label (latch selector preserved)
    - rurp_register_utils.h grep for the 6 old-name set returns 0 outside comments
    - `firestarter/src/boards/uno_rurp_shield.cpp:29` (or current equivalent) comment uses `CTRL_READ_WRITE` instead of bare `READ_WRITE`
    - uno_rurp_shield.cpp grep for old names returns 0 (the file only has comment-level references, all refreshed)
    - `firestarter/src/boards/rurp_common.cpp:58` (or current equivalent) contains `analogRead(PIN_VPP_VOLTAGE_ADC)` (NOT `VOLTAGE_MEASURE_PIN`)
    - rurp_common.cpp grep for `VOLTAGE_MEASURE_PIN` returns 0
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
    - `cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1` exits 0
  </acceptance_criteria>
  <done>3 files migrated; register_utils settle-check + uno_rurp_shield comment + rurp_common ADC read all use CTRL_*/PIN_*; CONTROL_REGISTER preserved; build + native test pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate native test test_flash_intel_vpp.cpp (7 lines — mock recorder + SAF-04 assertions)</name>
  <files>firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp</files>
  <read_first>
    /workspaces/firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-RESEARCH.md
  </read_first>
  <action>
    Per Pattern Assignment §14 (PATTERNS.md) + Pitfall 6 (RESEARCH.md — the native build path does NOT set `-D HARDWARE_REVISION`, so it exercises the legacy `#ifndef HARDWARE_REVISION` branch in `rurp_pinout.h`, which is the highest-risk surface for Pitfall 1's `CTRL_ADDRESS_LINE_16 == CTRL_VPP_VPE_DROP_ENABLE` aliasing). Edit `/workspaces/firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp` to rename: `P1_VPP_ENABLE` → `CTRL_VPP_P1_ENABLE` (at the mock recorder at `:43-48`, the SAF-04 assertion at `:186` via `TEST_ASSERT_BITS_HIGH`, and any other lines from the 7-line inventory). `REGULATOR` → `CTRL_VPP_REGULATOR_ENABLE` if it appears (per RESEARCH inventory, likely at `:170, :184, :186`). Comments mentioning the old names (e.g. "SAF-04: final control-register write must drive regulator low after VPP error" — the word "regulator" in lowercase prose stays as English; only the bare `REGULATOR` symbol-reference is renamed). Use Edit tool per occurrence. After edit, run `cd /workspaces/firestarter && pio test -e native` and confirm exit 0 — this is the gate that Pitfall 1 has been preserved correctly through the rename (if the legacy-path aliasing was lost, this test would fail).
  </action>
  <verify>
    <automated>c=$(grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' /workspaces/firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp 2>/dev/null | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l); [ "$c" -eq 0 ] && grep -q 'CTRL_VPP_P1_ENABLE' /workspaces/firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp && (cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\b(VPE_ENABLE|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR)\b' firestarter/test/native/avr/test_flash_intel_vpp/test_flash_intel_vpp.cpp | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//' | wc -l` returns 0
    - File contains `CTRL_VPP_P1_ENABLE` references (≥2 — mock recorder + at least one TEST_ASSERT)
    - File contains `CTRL_VPP_REGULATOR_ENABLE` references where the original had `REGULATOR`
    - `cd /workspaces/firestarter && pio test -e native` exits 0 — Pitfall 1 + Pitfall 6 cleared (legacy-path aliasing semantics preserved through rename)
  </acceptance_criteria>
  <done>test_flash_intel_vpp.cpp migrated; native test suite green — proves Pitfall 1 + 6 cleared.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: DELETE old #defines from firestarter/include/rurp_shield.h:25-89 + add #include "rurp_pinout.h" — atomic D-06 hard-rename enforcement gate</name>
  <files>firestarter/include/rurp_shield.h</files>
  <read_first>
    /workspaces/firestarter/include/rurp_shield.h,
    /workspaces/firestarter/include/rurp_pinout.h,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md,
    /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh,
    /workspaces/.planning/v1.7/phase-33-baseline-hex/BASELINE_COMMIT.txt
  </read_first>
  <action>
    Per D-06 (no shim, no backward-compat alias chain) + the D-06 restructure plan (Waves 1+2 leave `rurp_shield.h:25-89` untouched as unchanged-backward-compat; Wave 3 final task deletes it atomically): edit `/workspaces/firestarter/include/rurp_shield.h` to perform two changes.

    Change 1 — ADD `#include "rurp_pinout.h"` on a new line directly after the existing `#include "rurp_types.h"` line (currently around line 19). This makes the new canonical names visible to all downstream callers (`rurp_shield.h` is transitively included throughout the firmware).

    Change 2 — DELETE the original control-register bits block + REV_1_*/REV_2_* variants + `HARDWARE_REVISION_PIN` + `VOLTAGE_MEASURE_PIN` from lines `:25-94`. This is the load-bearing D-06 enforcement gate — the same block that Waves 1+2 explicitly LEFT IN PLACE as the unchanged backward-compat substrate (no NEW shim block was ever introduced; the original declarations themselves served as the backward-compat path until this atomic removal). Concretely: delete the legacy `#ifndef HARDWARE_REVISION` block (originally `:25-33`: VPE_TO_VPP, A9_VPP_ENABLE, VPE_ENABLE, P1_VPP_ENABLE, ADDRESS_LINE_17, ADDRESS_LINE_18, READ_WRITE, REGULATOR, plus the `#define ADDRESS_LINE_16 VPE_TO_VPP` alias); the HARDWARE_REVISION `#else` branch (originally `:43-51`: ADDRESS_LINE_16, A9_VPP_ENABLE, VPE_ENABLE, P1_VPP_ENABLE, ADDRESS_LINE_17, ADDRESS_LINE_18, READ_WRITE, REGULATOR, VPE_TO_VPP 0x100); the `#define ADDRESS_LINE_13 0x20` line (originally `:55`); the REV_1_* / REV_2_* per-rev variants (originally `:70-94`); the `VOLTAGE_MEASURE_PIN A2` line (originally `:21`); the `HARDWARE_REVISION_PIN A3` line (originally `:36`). KEEP everything outside that range verbatim: file header + header-guard + includes (`:1-19` PLUS the newly-added `#include "rurp_pinout.h"`); `CONFIG_VERSION "VER06"` (~`:98`) — DO NOT bump (Pitfall 5); `VALUE_R1` / `VALUE_R2` constants; `LEAST_SIGNIFICANT_BYTE` / `MOST_SIGNIFICANT_BYTE` / `OUTPUT_ENABLE` / `CONTROL_REGISTER` / `CHIP_ENABLE` latch selectors (74HC573 latch selectors, NOT RURP-signal aliases, OUT of D-03 scope per Anti-Pattern bullet in RESEARCH); all function prototypes; `rurp_chip_enable` / `rurp_chip_output` macro pair; the closing header-guard `#endif`. The `VPP_P1_32_DIP` / `VPP_P1_28_DIP` / `VPP_P21_24_DIP` block at `:55-58` (if present in the actual file) is RURP-signal-layer scope — verify whether those names are referenced in any `.cpp` (Waves 2+3 would have caught usage). If they ARE referenced and aren't part of the migration name set, keep them in `rurp_shield.h`; if they're orphan, delete with explanatory commit message. Final file: `rurp_shield.h` shrinks from ~194 lines to ~120 lines; macro substrate now lives in `rurp_pinout.h` (transitively included).

    Re-verify by running the full migration verifier: `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` — Assertion 1 (grep-zero on 8 old names) must now PASS, Assertion 2 (no `REV_[12]_*` outside REVISION_*) must now PASS, Assertion 3 (cmp on all 3 .hex) must STILL PASS.

    **Rollback path (W6):** If `cd /workspaces/firestarter && pio test -e native` fails after the rurp_shield.h delete with `TEST_ASSERT` failures in `test_flash_intel_vpp.cpp` (the highest-risk surface per Pitfall 6 — native env does NOT set `-D HARDWARE_REVISION`, so it exercises the legacy `#ifndef HARDWARE_REVISION` branch of `rurp_pinout.h`), restore the deleted block via `cd /workspaces/firestarter && git checkout HEAD~ -- include/rurp_shield.h` and investigate whether Pitfall 1 (`ADDRESS_LINE_16 == VPE_TO_VPP` macro-alias preservation in the legacy `#ifndef HARDWARE_REVISION` branch of `rurp_pinout.h`) is correctly mirrored — most likely cause of native-build Unity assertion failure after old-define delete. Fix the rurp_pinout.h legacy-branch alias if it has drifted, re-test, then re-attempt the rurp_shield.h delete. If the build itself fails with `error: <old_name> was not declared in this scope`, it indicates a missed call-site from Waves 2 or 3 — identify the offending file via the compiler error, rename the missed call-site, then re-attempt the delete (no rollback of rurp_shield.h needed in that case — the build failure is informative).

    After successful deletion: re-run `cd /workspaces/firestarter && pio run -e uno -e uno328pb -e leonardo && pio test -e native` and record per-board `.hex` file sizes in the SUMMARY (`wc -c .pio/build/uno/firestarter_uno.hex` etc.). Compare against the Wave 0 baseline `wc -c .planning/v1.7/phase-33-baseline-hex/*.hex` — expected delta is 0 B; if any non-zero drift surfaces, document it in the SUMMARY with byte counts + explanation (per RESEARCH GATE-1.7 ≤ ~50 B per board budget). Cross-reference the baseline commit SHA from `.planning/v1.7/phase-33-baseline-hex/BASELINE_COMMIT.txt` (recorded in Wave 0 Task 1) in the fix-commit message so the cmp can be traced back to the exact source state. Run `sha256sum` of all 3 post-rename .hex files for the SUMMARY commit record.
  </action>
  <verify>
    <automated>grep -q '#include "rurp_pinout.h"' /workspaces/firestarter/include/rurp_shield.h && grep -q 'CONFIG_VERSION "VER06"' /workspaces/firestarter/include/rurp_shield.h && [ "$(grep -cE '^[[:space:]]*#define[[:space:]]+(VPE_TO_VPP|VPE_ENABLE|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|VOLTAGE_MEASURE_PIN|HARDWARE_REVISION_PIN)[[:space:]]+' /workspaces/firestarter/include/rurp_shield.h)" -eq 0 ] && [ "$(grep -cE '^[[:space:]]*#define[[:space:]]+REV_[12]_' /workspaces/firestarter/include/rurp_shield.h)" -eq 0 ] && FULL=$(grep -rn 'VPE_ENABLE\|VPE_TO_VPP\|P1_VPP_ENABLE\|A9_VPP_ENABLE\|READ_WRITE\|REGULATOR\|ADDRESS_LINE_1[3678]\|VOLTAGE_MEASURE_PIN\|HARDWARE_REVISION_PIN\|REV_1_\|REV_2_' /workspaces/firestarter/include/ /workspaces/firestarter/src/ /workspaces/firestarter/test/ 2>/dev/null | grep -v '//' | grep -v REVISION_ | wc -l) && [ "$FULL" -eq 0 ] && (cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1) && (cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1) && (cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1) && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno328pb.hex /workspaces/firestarter/.pio/build/uno328pb/firestarter_uno328pb.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/leonardo.hex /workspaces/firestarter/.pio/build/leonardo/firestarter_leonardo.hex && bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/include/rurp_shield.h` contains exactly one line `#include "rurp_pinout.h"` (after `#include "rurp_types.h"`)
    - Zero `#define <name> 0x<value>` lines for any of: VPE_TO_VPP, VPE_ENABLE, P1_VPP_ENABLE, A9_VPP_ENABLE, READ_WRITE, REGULATOR, VOLTAGE_MEASURE_PIN, HARDWARE_REVISION_PIN in rurp_shield.h (these now live in rurp_pinout.h)
    - Zero `#define REV_1_*` or `#define REV_2_*` lines in rurp_shield.h
    - Zero `#define ADDRESS_LINE_13`, `ADDRESS_LINE_16`, `ADDRESS_LINE_17`, `ADDRESS_LINE_18` lines in rurp_shield.h
    - `CONFIG_VERSION "VER06"` line preserved verbatim (Pitfall 5 — NOT bumped)
    - Latch-selector defines preserved: `LEAST_SIGNIFICANT_BYTE`, `MOST_SIGNIFICANT_BYTE`, `OUTPUT_ENABLE`, `CONTROL_REGISTER`, `CHIP_ENABLE` (out of D-03 alias scope)
    - All function prototypes preserved (no signature changes)
    - `grep -rn "VPE_ENABLE\|VPE_TO_VPP\|P1_VPP_ENABLE\|A9_VPP_ENABLE\|READ_WRITE\|REGULATOR\|ADDRESS_LINE_1[3678]\|VOLTAGE_MEASURE_PIN\|HARDWARE_REVISION_PIN\|REV_1_\|REV_2_" /workspaces/firestarter/ | grep -v '//' | grep -v REVISION_ | wc -l` returns 0 (full-tree grep-zero — D-06 enforced; REVISION_* enum values + comment lines excluded)
    - `cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1` exits 0 (Unity native test green — Pitfall 1 + 6 cleared)
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0; same for uno328pb + leonardo
    - `cmp` against all 3 baseline `.hex` files exits 0 (byte-identical preserved — the atomic delete touches names but produces identical post-link machine code because the new names are textually-identical-expansion under both `#ifdef HARDWARE_REVISION` branches and the legacy branch alias is preserved)
    - `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` exits 0 — final phase gate PASS. Output line includes `PASS: alias migration verified clean`
    - SUMMARY records per-board `wc -c` + `sha256sum` for both baseline and post-rename .hex; expected delta 0 B per board (documented if non-zero, must be ≤ 50 B); baseline commit SHA from BASELINE_COMMIT.txt cross-referenced in fix-commit message
    - If verification fails: rollback path documented in action follows — `git checkout HEAD~ -- include/rurp_shield.h` restores deleted block; investigate Pitfall 1 (ADDRESS_LINE_16 == VPE_TO_VPP macro-alias in legacy `#ifndef HARDWARE_REVISION` branch of rurp_pinout.h) as most likely cause; fix rurp_pinout.h, re-test, re-attempt delete
  </acceptance_criteria>
  <done>rurp_shield.h:25-89 old-#define block atomically deleted (replaces previous "remove shim block" task per D-06 restructure); #include "rurp_pinout.h" added; full phase-gate verifier PASSES (grep-zero + REV_*-zero + cmp 3-way); GATE-1.7 met.</done>
</task>

</tasks>

<verification>
After all 4 tasks: `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` exits 0 — this is the load-bearing assertion that ALIAS-02 + ALIAS-03 firmware-side requirements are met. Per-board `wc -c` + `sha256sum` of post-rename `.hex` files recorded in SUMMARY. If cmp produces non-zero delta (≤ 50 B per RESEARCH allowance), per-board byte counts documented in commit message with rationale. Baseline commit SHA cross-referenced from `.planning/v1.7/phase-33-baseline-hex/BASELINE_COMMIT.txt` (Wave 0 Task 1).
</verification>

<success_criteria>
- 3 firmware files migrated: rurp_hw_rev_utils.h (dispatcher + detect-fn), rurp_register_utils.h (settle-check), test_flash_intel_vpp.cpp (mock + assertions)
- 2 board files migrated: uno_rurp_shield.cpp (comment), rurp_common.cpp (ADC read)
- rurp_shield.h:25-89 old-#define block DELETED — D-06 hard-rename enforced atomically (no shim block was ever introduced; the original declarations themselves served as the backward-compat path until this removal)
- rurp_shield.h gains `#include "rurp_pinout.h"` for transitive visibility of new canonical names
- check-migration.sh PASSES — Assertion 1 (grep-zero), Assertion 2 (REV_*-zero), Assertion 3 (cmp 3-way) all green
- All 3 AVR envs build clean; pio test -e native green
- .hex byte-identical to baseline (or documented ≤ 50 B drift)
- GATE-1.7 met (ALIAS-03 verified)
</success_criteria>

<output>
Create `.planning/phases/33-silkscreen-label-code-alias-migration/33-03-SUMMARY.md` recording:
- Per-board `wc -c` AND `sha256sum` for baseline vs post-rename .hex (3 boards × 2 metrics = 6 measurement rows)
- Baseline commit SHA from BASELINE_COMMIT.txt cross-referenced (for traceability per B2)
- Confirmed: check-migration.sh PASSES — all 3 assertions
- Confirmed: pio test -e native green (Pitfall 1 + 6 cleared)
- Confirmed: pio run -e uno -e uno328pb -e leonardo green
- Total firmware files modified in Phase 33: rurp_pinout.h (NEW in Wave 1), rurp_shield.h (Wave 3 final task only — #include add + :25-94 block delete; NOT modified in Waves 1 or 2), CLAUDE.md, 6 src/proms/, hardware_operations.cpp, 2 src/boards/, 2 include/rurp_*_utils.h, 1 test file — total 14 firmware files
- D-06 restructure note: no transient shim block was ever introduced; the rurp_shield.h:25-89 #defines remained unchanged through Waves 1+2 as the backward-compat substrate, then were deleted atomically in this wave's final task
- Pending for Wave 4: Python-side mirror + §7 fill
</output>
