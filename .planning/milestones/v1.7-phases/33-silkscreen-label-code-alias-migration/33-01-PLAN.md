---
phase: 33-silkscreen-label-code-alias-migration
plan: 01
type: execute
wave: 1
depends_on:
  - 33-00
files_modified:
  - firestarter/include/rurp_pinout.h
  - firestarter/CLAUDE.md
autonomous: true
requirements:
  - ALIAS-02
  - ALIAS-03
tags:
  - firmware
  - header
  - migration
  - alias

must_haves:
  truths:
    - "firestarter/include/rurp_pinout.h exists with CTRL_* / PIN_* / RES_* / JMP_* namespace declarations (per D-06 no shim block — Wave 1 introduces NEW names alongside the unchanged old #defines in rurp_shield.h:25-89)"
    - "Legacy #ifndef HARDWARE_REVISION branch in rurp_pinout.h preserves the load-bearing ADDRESS_LINE_16 == VPE_TO_VPP aliasing semantics under new names (CTRL_ADDRESS_LINE_16 == CTRL_VPP_VPE_DROP_ENABLE via macro-alias)"
    - "HARDWARE_REVISION branch preserves CTRL_VPP_VPE_DROP_ENABLE = 0x100 (NOT 0x01) — rev-dependent value mapping intact (Pitfall 2)"
    - "REV_1_* / REV_2_* prefix family declared as new CTRL_*_REV1 / CTRL_*_REV2 suffix family in rurp_pinout.h"
    - "rurp_shield.h is NOT modified in Wave 1 — its existing #defines at :25-94 remain in place and continue to serve all old-name call-sites until Wave 3 final task deletes them atomically (per D-06 — no NEW shim block is created; the existing declarations are the unchanged backward-compat path until removal)"
    - "firestarter/CLAUDE.md §Constants block lists the new CTRL_* names with bit values; algorithm table refreshed where it referenced VPE_TO_VPP / P1_VPP"
    - "All 3 AVR envs (uno / uno328pb / leonardo) still build cleanly — header-only addition must not perturb compilation"
    - "Post-build .hex for all 3 envs is byte-identical to .planning/v1.7/phase-33-baseline-hex/*.hex (cmp exits 0) — adding an unused header changes no machine code"
    - "CONFIG_VERSION VER06 unchanged"
  artifacts:
    - path: firestarter/include/rurp_pinout.h
      provides: "canonical CTRL_* / PIN_* / RES_* / JMP_* alias substrate"
      min_lines: 60
      contains: "#define CTRL_VPP_REGULATOR_ENABLE"
    - path: firestarter/CLAUDE.md
      provides: "§Constants doc refresh — new CTRL_* names listed alongside rurp_pinout.h as the new SoT pointer"
      contains: "CTRL_VPP_REGULATOR_ENABLE"
  key_links:
    - from: firestarter/include/rurp_pinout.h
      to: firestarter/include/rurp_pinout.h
      via: "macro alias preserves ADDRESS_LINE_16 == VPE_TO_VPP aliasing in legacy branch under new names"
      pattern: "#define CTRL_ADDRESS_LINE_16\\s+CTRL_VPP_VPE_DROP_ENABLE"
---

<objective>
Create the canonical `firestarter/include/rurp_pinout.h` header with the new 4-namespace alias substrate (`CTRL_*` for control-register bits, `PIN_*` for Arduino-pin assignments, `RES_*` for shield resistor designators, `JMP_*` for shield jumper designators — per Open Question Q1 recommendation locked at plan time). The header introduces ONLY the new canonical declarations. The `#ifndef HARDWARE_REVISION` / `#else` / `#endif` ifdef gating is mirrored VERBATIM from `rurp_shield.h:24-89` (Pitfalls 1+2). Refresh `firestarter/CLAUDE.md` §Constants block to list the new `CTRL_*` names alongside their hex values and point the SoT cross-reference at the new `rurp_pinout.h`.

Critically (per D-06 — no shim, no backward-compat alias chain): Wave 1 does NOT modify `rurp_shield.h`. Its existing `#define`s at `:25-94` remain in place, unchanged, throughout Waves 1 and 2 — they continue to serve every call-site that still references the old names. No NEW shim block is created in `rurp_pinout.h` (the planner does not introduce a new exception to D-06). The post-Wave-1 state has BOTH headers active: `rurp_shield.h` carries the old names that all current call-sites still use, AND `rurp_pinout.h` carries the new canonical names with zero callers yet. This is valid because the old `#define`s are not orphan symbols — they are the unchanged-backward-compat substrate that Waves 2 + 3 migrate away from, and Wave 3's final task atomically DELETES the `rurp_shield.h:25-89` block as the load-bearing D-06 enforcement gate.

Purpose: This wave introduces the new alias substrate without touching any call-site and without introducing a new shim. The proof that the substrate is correct: all 3 AVR envs still build clean (the new header has no callers, so it cannot break anything) and `.hex` is byte-identical to baseline (no machine code changes — only an unused header is added).

Output: New `rurp_pinout.h` header (~60-80 lines covering legacy + HARDWARE_REVISION branches + per-rev variants); `rurp_shield.h` unmodified in Wave 1; refreshed `firestarter/CLAUDE.md` §Constants + algorithm-table cells referencing old names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-CONTEXT.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-RESEARCH.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-VALIDATION.md
@.planning/phases/33-silkscreen-label-code-alias-migration/33-00-SUMMARY.md
@firestarter/include/rurp_shield.h
@firestarter/CLAUDE.md
@firestarter/platformio.ini
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create firestarter/include/rurp_pinout.h with 4-namespace alias substrate (no shim block — D-06 enforcement)</name>
  <files>firestarter/include/rurp_pinout.h</files>
  <read_first>
    /workspaces/firestarter/include/rurp_shield.h,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-PATTERNS.md,
    /workspaces/.planning/phases/33-silkscreen-label-code-alias-migration/33-RESEARCH.md
  </read_first>
  <action>
    Per ALIAS-02 (D-06 — hard-rename, NO shim block, NO backward-compat alias chain; D-07 — `#define` not `constexpr`) + Pattern Assignment §1 + Pitfall 1 + Pitfall 2: Create `/workspaces/firestarter/include/rurp_pinout.h` as a new header containing ONLY the new canonical declarations across `CTRL_*` / `PIN_*` / `RES_*` / `JMP_*` namespaces. Do NOT include any `#define <old_name> <new_name>` shim lines — those are explicitly prohibited per D-06. The post-Wave-1 build still passes because `rurp_shield.h:25-89` remains unchanged (untouched in this wave) and still resolves every old-name reference at every existing call-site; Waves 2 + 3 migrate call-sites individually until Wave 3's final task atomically deletes the old `#define`s from `rurp_shield.h`.

    Use the same header-guard + `extern "C"` shape as `rurp_shield.h:8-13`: `#ifndef __RURP_PINOUT_H__` / `#define __RURP_PINOUT_H__` / `#ifdef __cplusplus` / `extern "C" {` / `#endif` / (body) / closing `}` / `#endif // __RURP_PINOUT_H__`. Include `<stdint.h>` and `<Arduino.h>` (the latter for `A2`/`A3` symbols).

    Body has 4 sections — mirror the `#ifndef HARDWARE_REVISION` / `#else` / `#endif` ifdef structure from `rurp_shield.h:24-89` VERBATIM. Comments mark each section. NO TRANSIENT SHIM section.

    Section 1 — Arduino-pin assignments (PIN_*): `#define PIN_VPP_VOLTAGE_ADC A2` outside any ifdef. Inside `#ifdef HARDWARE_REVISION` add `#define PIN_HW_REVISION_DETECT_ADC A3`.

    Section 2 — Control-register bits LEGACY branch (`#ifndef HARDWARE_REVISION`): mirror `rurp_shield.h:25-33` exactly with the new names and matching hex values: `CTRL_VPP_VPE_DROP_ENABLE 0x01` (was VPE_TO_VPP), then the load-bearing aliasing `#define CTRL_ADDRESS_LINE_16 CTRL_VPP_VPE_DROP_ENABLE` (Pitfall 1 — macro-alias-as-macro, not duplicate hex), `CTRL_VPP_A9_ENABLE 0x02` (was A9_VPP_ENABLE), `CTRL_VPE_ENABLE 0x04` (was VPE_ENABLE), `CTRL_VPP_P1_ENABLE 0x08` (was P1_VPP_ENABLE), `CTRL_ADDRESS_LINE_17 0x10`, `CTRL_ADDRESS_LINE_18 0x20`, `CTRL_READ_WRITE 0x40` (was READ_WRITE), `CTRL_VPP_REGULATOR_ENABLE 0x80` (was REGULATOR).

    Section 3 — Control-register bits HARDWARE_REVISION branch (`#else` / `#endif`): mirror `rurp_shield.h:43-51` exactly: `CTRL_ADDRESS_LINE_16 0x01`, `CTRL_VPP_A9_ENABLE 0x02`, `CTRL_VPE_ENABLE 0x04`, `CTRL_VPP_P1_ENABLE 0x08`, `CTRL_ADDRESS_LINE_17 0x10`, `CTRL_ADDRESS_LINE_18 0x20`, `CTRL_READ_WRITE 0x40`, `CTRL_VPP_REGULATOR_ENABLE 0x80`, and CRITICALLY `CTRL_VPP_VPE_DROP_ENABLE 0x100` (Pitfall 2 — VALUE DIFFERS from legacy 0x01). Outside the ifndef/else block at end of file, mirror `rurp_shield.h:55` `#define CTRL_ADDRESS_LINE_13 0x20` with a comment `// reserved — no current call-site` per Open Question Q2 recommendation.

    Section 4 — Per-rev variant macros (REV_1_* and REV_2_* → renamed to CTRL_*_REV1 / CTRL_*_REV2 suffix family per RESEARCH "State of the Art" table) inside `#ifdef HARDWARE_REVISION` (mirror `rurp_shield.h:70-89`): For REV1 — `CTRL_VPP_VPE_DROP_ENABLE_REV1 0x01`, `CTRL_VPP_A9_ENABLE_REV1 0x02`, `CTRL_VPE_ENABLE_REV1 0x04`, `CTRL_VPP_P1_ENABLE_REV1 0x08`, `CTRL_ADDRESS_LINE_17_REV1 0x10`, `CTRL_ADDRESS_LINE_18_REV1 0x20`, `CTRL_READ_WRITE_REV1 0x40`, `CTRL_VPP_REGULATOR_ENABLE_REV1 0x80`, and the load-bearing alias `#define CTRL_ADDRESS_LINE_16_REV1 CTRL_VPP_VPE_DROP_ENABLE_REV1`. For REV2 — `CTRL_VPP_VPE_DROP_ENABLE_REV2 0x01`, `CTRL_VPP_A9_ENABLE_REV2 0x02`, `CTRL_VPE_ENABLE_REV2 0x04`, `CTRL_VPP_P1_ENABLE_REV2 0x08`, `CTRL_ADDRESS_LINE_17_REV2 0x10`, `CTRL_ADDRESS_LINE_16_REV2 0x20`, `CTRL_READ_WRITE_REV2 0x40`, and the load-bearing alias `#define CTRL_ADDRESS_LINE_18_REV2 CTRL_VPP_P1_ENABLE_REV2` (per existing `rurp_shield.h:88` REV_2_ADDRESS_LINE_18 alias).

    Critical preservation (per RESEARCH Pitfalls 1 + 2): legacy `CTRL_ADDRESS_LINE_16 == CTRL_VPP_VPE_DROP_ENABLE` aliasing via macro-alias-as-macro (NOT duplicate hex); HARDWARE_REVISION `CTRL_VPP_VPE_DROP_ENABLE 0x100` value distinct from legacy `0x01`; `CTRL_ADDRESS_LINE_18_REV2 == CTRL_VPP_P1_ENABLE_REV2` aliasing in REV2 block. Post-Wave-1 state: `rurp_pinout.h` is included by no source file — `rurp_shield.h` is unchanged. The new header sits idle until Wave 2 starts using its names; Wave 3 final task deletes `rurp_shield.h:25-89` once all call-sites migrate. NEVER inline fenced code blocks in the action prose — the directives above name every identifier; the executor writes the actual file content.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -f /workspaces/firestarter/include/rurp_pinout.h && grep -q '__RURP_PINOUT_H__' /workspaces/firestarter/include/rurp_pinout.h && grep -q '#define CTRL_VPP_REGULATOR_ENABLE' /workspaces/firestarter/include/rurp_pinout.h && grep -q '#define CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/include/rurp_pinout.h && grep -q '#define PIN_VPP_VOLTAGE_ADC' /workspaces/firestarter/include/rurp_pinout.h && grep -qE '#define[[:space:]]+CTRL_ADDRESS_LINE_16[[:space:]]+CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/include/rurp_pinout.h && grep -q 'CTRL_VPP_VPE_DROP_ENABLE_REV2' /workspaces/firestarter/include/rurp_pinout.h && ! grep -qi 'TRANSIENT' /workspaces/firestarter/include/rurp_pinout.h && ! grep -qi 'SHIM' /workspaces/firestarter/include/rurp_pinout.h && ! grep -qE '^#define[[:space:]]+(VPE_TO_VPP|VPE_ENABLE|P1_VPP_ENABLE|A9_VPP_ENABLE|READ_WRITE|REGULATOR|VOLTAGE_MEASURE_PIN|HARDWARE_REVISION_PIN|REV_[12]_)' /workspaces/firestarter/include/rurp_pinout.h</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/include/rurp_pinout.h` exists with header guard `__RURP_PINOUT_H__`
    - `grep -c '^#define CTRL_' /workspaces/firestarter/include/rurp_pinout.h` returns ≥ 20 (W3 — content-count assertion: 9 legacy CTRL_* + 9 wide-layout CTRL_* + 9 REV1 + 9 REV2 + reserved ADDRESS_LINE_13 ≈ 26 minimum after dedup; floor 20 covers conservative overlap)
    - `grep -c '^#define PIN_' /workspaces/firestarter/include/rurp_pinout.h` returns 2 (W3 — PIN_VPP_VOLTAGE_ADC + PIN_HW_REVISION_DETECT_ADC)
    - `grep -c '^#define RES_' /workspaces/firestarter/include/rurp_pinout.h` returns 0 (RES_* / JMP_* are §7-table-only namespaces in Phase 33 — no firmware declarations needed; future phases may add them)
    - `grep -c '^#define JMP_' /workspaces/firestarter/include/rurp_pinout.h` returns 0 (same as above)
    - Contains all 9 canonical `CTRL_*` control-register-bit names (CTRL_VPP_REGULATOR_ENABLE, CTRL_VPP_VPE_DROP_ENABLE, CTRL_READ_WRITE, CTRL_ADDRESS_LINE_18, CTRL_ADDRESS_LINE_17, CTRL_VPP_P1_ENABLE, CTRL_VPE_ENABLE, CTRL_VPP_A9_ENABLE, CTRL_ADDRESS_LINE_16)
    - Contains `PIN_VPP_VOLTAGE_ADC` (always) and `PIN_HW_REVISION_DETECT_ADC` (under HARDWARE_REVISION)
    - Legacy `#ifndef HARDWARE_REVISION` branch contains `#define CTRL_ADDRESS_LINE_16 CTRL_VPP_VPE_DROP_ENABLE` (macro-alias, NOT `0x01` duplicate hex) — Pitfall 1
    - HARDWARE_REVISION branch contains `CTRL_VPP_VPE_DROP_ENABLE 0x100` (NOT `0x01`) — Pitfall 2
    - Contains all REV1/REV2 suffix-family variants: CTRL_VPP_VPE_DROP_ENABLE_REV1, CTRL_VPP_VPE_DROP_ENABLE_REV2, CTRL_ADDRESS_LINE_16_REV1 (as alias), CTRL_ADDRESS_LINE_18_REV2 (as alias to CTRL_VPP_P1_ENABLE_REV2)
    - File contains NO `TRANSIENT` or `SHIM` text (D-06 enforcement — no shim block)
    - File contains NO `#define <old_name>` lines for VPE_TO_VPP / VPE_ENABLE / P1_VPP_ENABLE / A9_VPP_ENABLE / READ_WRITE / REGULATOR / VOLTAGE_MEASURE_PIN / HARDWARE_REVISION_PIN / REV_1_* / REV_2_* (D-06 enforcement)
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0 (the new header has no callers and rurp_shield.h is unchanged — compilation must succeed)
  </acceptance_criteria>
  <done>rurp_pinout.h exists with CTRL_*/PIN_*/REV1/REV2 names, ifdef structure mirrors rurp_shield.h:24-89, NO shim block (D-06 enforced), and is currently unused (rurp_shield.h unchanged in Wave 1).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify Wave 1 substrate addition produces byte-identical .hex (rurp_shield.h NOT modified — Wave 3 final task deletes its :25-94 block)</name>
  <files></files>
  <read_first>
    /workspaces/firestarter/include/rurp_shield.h,
    /workspaces/firestarter/include/rurp_pinout.h,
    /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh
  </read_first>
  <action>
    Per Pattern Assignment §2 (D-06 enforcement — NO modification of rurp_shield.h in Wave 1): this task makes ZERO source-file edits. It is purely a verification task that proves the Wave 1 substrate addition (the new rurp_pinout.h header from Task 1) does not perturb compilation OR machine code.

    Step 1: Confirm `firestarter/include/rurp_shield.h` is unchanged from Wave 0 baseline: `cd /workspaces/firestarter && git diff --stat include/rurp_shield.h` returns empty output (no lines changed).

    Step 2: Confirm the old `#define`s are still present at :25-94 (the unchanged-backward-compat substrate that all current call-sites still resolve through): `grep -c '^#define VPE_TO_VPP' /workspaces/firestarter/include/rurp_shield.h` returns 1; `grep -c '^#define REGULATOR' /workspaces/firestarter/include/rurp_shield.h` returns 1; `CONFIG_VERSION "VER06"` preserved.

    Step 3: Confirm all 3 AVR envs build clean: `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1`, `cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1`, `cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1` — each exits 0.

    Step 4: Confirm `pio test -e native` exits 0.

    Step 5: Confirm `.hex` byte-identical to Wave 0 baseline (adding an unused header creates no machine code): `cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex` exits 0; same for uno328pb + leonardo.

    The rurp_shield.h:25-89 deletion is deferred to Wave 3 Task 4 (atomic with full migration completion). Per D-06, no NEW shim block is introduced in Wave 1; the old #defines in rurp_shield.h serve as the unchanged backward-compat path until Wave 3's atomic deletion replaces the previous "remove shim block" task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && [ -z "$(git diff --stat include/rurp_shield.h)" ] && grep -q 'CONFIG_VERSION "VER06"' /workspaces/firestarter/include/rurp_shield.h && grep -q '^#define VPE_TO_VPP' /workspaces/firestarter/include/rurp_shield.h && grep -q '^#define REGULATOR' /workspaces/firestarter/include/rurp_shield.h && cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1 && cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1 && cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1 && cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1 && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno328pb.hex /workspaces/firestarter/.pio/build/uno328pb/firestarter_uno328pb.hex && cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/leonardo.hex /workspaces/firestarter/.pio/build/leonardo/firestarter_leonardo.hex</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && git diff --stat include/rurp_shield.h` produces empty output (rurp_shield.h NOT modified in Wave 1 — per D-06 restructure, the old #defines remain in place until Wave 3 final task deletes them atomically)
    - `firestarter/include/rurp_shield.h` retains `#define VPE_TO_VPP`, `#define REGULATOR`, `#define VPE_ENABLE`, `#define P1_VPP_ENABLE`, `#define A9_VPP_ENABLE`, `#define READ_WRITE`, `#define VOLTAGE_MEASURE_PIN`, `#define HARDWARE_REVISION_PIN`, and the REV_1_* / REV_2_* family (unchanged)
    - `CONFIG_VERSION "VER06"` line preserved verbatim (Pitfall 5 — NOT bumped)
    - `cd /workspaces/firestarter && pio run -e uno >/dev/null 2>&1` exits 0
    - `cd /workspaces/firestarter && pio run -e uno328pb >/dev/null 2>&1` exits 0
    - `cd /workspaces/firestarter && pio run -e leonardo >/dev/null 2>&1` exits 0
    - `cd /workspaces/firestarter && pio test -e native >/dev/null 2>&1` exits 0
    - `cmp /workspaces/.planning/v1.7/phase-33-baseline-hex/uno.hex /workspaces/firestarter/.pio/build/uno/firestarter_uno.hex` exits 0 (byte-identical — unused new header creates no machine code)
    - Same `cmp` for uno328pb + leonardo each exits 0
    - `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` reports `cmp` PASS for all 3 hex (Assertion 3 passes). Assertion 1 (grep) still fails because old names still live in both rurp_shield.h AND in src/proms/*.cpp call-sites (expected at this wave).
  </acceptance_criteria>
  <done>rurp_shield.h verified unchanged in Wave 1; all 3 AVR envs build clean; .hex byte-identical to baseline; D-06 restructure preserved (no new shim block, old #defines still in rurp_shield.h as unchanged backward-compat).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refresh firestarter/CLAUDE.md §Constants + algorithm-table cells to new CTRL_* names</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    /workspaces/firestarter/CLAUDE.md,
    /workspaces/firestarter/include/rurp_pinout.h
  </read_first>
  <action>
    Per Pattern E (RESEARCH.md) + Atomicity recommendation A6: edit `/workspaces/firestarter/CLAUDE.md` to update two regions to the new `CTRL_*` names. Region 1 — §Constants subsection currently reads (verbatim): `- `REGULATOR (0x80)` — enable VPP boost regulator` / `- `VPE_TO_VPP (0x01)` — drop VPE through resistor to VPP level` / `- `P1_VPP_ENABLE (0x08)` — route VPP to socket pin 1` / `- `A9_VPP_ENABLE (0x02)` — route VPP to A9 (for EPROM chip ID read)` / `- `VPE_ENABLE (0x04)` — apply VPE directly to PGM pin`. Replace each old name with the canonical new name, preserving the hex values and descriptions: `CTRL_VPP_REGULATOR_ENABLE (0x80)`, `CTRL_VPP_VPE_DROP_ENABLE (0x01 legacy / 0x100 rev2)` (per Pitfall 2 — both values documented), `CTRL_VPP_P1_ENABLE (0x08)`, `CTRL_VPP_A9_ENABLE (0x02)`, `CTRL_VPE_ENABLE (0x04)`. Update the section preamble line "Control register bits (from `rurp_shield.h`):" to "Control register bits (from `rurp_pinout.h`):". Region 2 — §Algorithm Handlers table currently has `VPP` column entries with values like `13V VPE_TO_VPP`, `12V P1_VPP`. Refresh to `13V via CTRL_VPP_VPE_DROP_ENABLE` (rows 0x07, 0x08) and `12V via CTRL_VPP_P1_ENABLE` (row 0x10 Intel flash). Row 0x0B keeps "12–18V direct" (no rename). Preserve every other cell verbatim (file size deltas should be small — only the named identifiers change). Also update the "Key Files" line that says `include/rurp_shield.h — control register bit definitions (REGULATOR, VPE_TO_VPP, P1_VPP_ENABLE, etc.)` to `include/rurp_pinout.h — control register bit definitions (CTRL_VPP_REGULATOR_ENABLE, CTRL_VPP_VPE_DROP_ENABLE, CTRL_VPP_P1_ENABLE, etc.)`. NEVER include fenced code blocks in this action — directly edit the prose lines.

    Note on traceability: this CLAUDE.md refresh in Wave 1 documents the new names BEFORE any call-site is migrated. Readers may briefly see CLAUDE.md citing new names while the firmware source still uses old names; this is intentional — CLAUDE.md is the architectural intent doc, and the call-site migration in Waves 2-3 catches up. Wave 3 final task deletes the rurp_shield.h:25-89 old #defines, closing the loop.
  </action>
  <verify>
    <automated>grep -q 'CTRL_VPP_REGULATOR_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -q 'CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -q 'CTRL_VPP_P1_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -q 'CTRL_VPP_A9_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -q 'CTRL_VPE_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -q 'rurp_pinout.h' /workspaces/firestarter/CLAUDE.md && ! grep -qE '^- `(REGULATOR|VPE_TO_VPP|P1_VPP_ENABLE|A9_VPP_ENABLE|VPE_ENABLE) \(0x' /workspaces/firestarter/CLAUDE.md && ! grep -E '\| 0x0[78] .*VPE_TO_VPP' /workspaces/firestarter/CLAUDE.md && ! grep -E '\| 0x10 .*P1_VPP' /workspaces/firestarter/CLAUDE.md && grep -E '\| 0x0[78] .*CTRL_VPP_VPE_DROP_ENABLE' /workspaces/firestarter/CLAUDE.md && grep -E '\| 0x10 .*CTRL_VPP_P1_ENABLE' /workspaces/firestarter/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/CLAUDE.md` §Constants section lists all 5 canonical CTRL_* names with bit values (CTRL_VPP_REGULATOR_ENABLE 0x80, CTRL_VPP_VPE_DROP_ENABLE 0x01 legacy / 0x100 rev2, CTRL_VPP_P1_ENABLE 0x08, CTRL_VPP_A9_ENABLE 0x02, CTRL_VPE_ENABLE 0x04)
    - Section preamble references `rurp_pinout.h` (not `rurp_shield.h`) as the source of bit definitions
    - §Algorithm Handlers table VPP column refreshed: rows 0x07 + 0x08 use `CTRL_VPP_VPE_DROP_ENABLE`, row 0x10 uses `CTRL_VPP_P1_ENABLE` (W5 verify clauses)
    - Algorithm table NO LONGER contains `| 0x07 .*VPE_TO_VPP` or `| 0x08 .*VPE_TO_VPP` rows (W5 negative assertions)
    - Algorithm table NO LONGER contains `| 0x10 .*P1_VPP` row (W5 negative assertion — must use `CTRL_VPP_P1_ENABLE` not bare `P1_VPP_ENABLE`)
    - Old bare-name pattern `- `REGULATOR (0x...)` / `- `VPE_TO_VPP (0x...)` etc. NO LONGER appears at start of any list line
    - "Key Files" line references `include/rurp_pinout.h` for control register bits
    - File structure preserved (all other sections — Build Commands, Architecture, Protocol Dispatch, JSON Wire Protocol, Native Test Environment — byte-identical except for the targeted updates)
  </acceptance_criteria>
  <done>firestarter/CLAUDE.md §Constants + algorithm table + Key Files reference the new CTRL_* names; rurp_pinout.h documented as the new SoT for control-register bits.</done>
</task>

</tasks>

<verification>
After all 3 tasks: (a) `cd /workspaces/firestarter && pio run -e uno -e uno328pb -e leonardo` builds clean; (b) `cd /workspaces/firestarter && pio test -e native` green; (c) `bash /workspaces/.planning/v1.7/phase-33-baseline-hex/check-migration.sh` Assertion 3 (cmp) passes — .hex byte-identical because the new rurp_pinout.h header has no callers yet AND the old #defines in rurp_shield.h are unchanged (D-06 restructure — no new shim block introduced). Assertion 1 (grep-zero on old names) STILL FAILS — by design — because both rurp_shield.h AND src/proms/*.cpp / src/boards/*.cpp / include/rurp_*.h still reference the old names. Wave 2 migrates call-sites in src/proms/* + hardware_operations.cpp; Wave 3 migrates remaining call-sites in src/boards/*, include/rurp_*_utils.h, test/ AND in its final task deletes the rurp_shield.h:25-89 old-#define block (the load-bearing D-06 enforcement gate).
</verification>

<success_criteria>
- rurp_pinout.h created with full 4-namespace declaration set (NO shim block — D-06 enforcement)
- rurp_shield.h NOT modified in Wave 1 (old #defines remain in place as unchanged backward-compat until Wave 3 final task atomically deletes them)
- firestarter/CLAUDE.md §Constants + algorithm table + Key Files refreshed to new names
- All 3 AVR envs (uno / uno328pb / leonardo) build clean post-changes
- pio test -e native green
- check-migration.sh Assertion 3 (cmp) passes — Wave 1 changes produce byte-identical .hex (unused header creates no machine code)
- Wave 1 introduces the substrate without touching any call-site and without introducing any shim
</success_criteria>

<output>
Create `.planning/phases/33-silkscreen-label-code-alias-migration/33-01-SUMMARY.md` recording:
- rurp_pinout.h line count + key namespace block boundaries
- Confirmed: rurp_shield.h NOT modified in Wave 1 (git diff --stat empty)
- Confirmed: pio test -e native green
- Confirmed: cmp Assertion passes for all 3 .hex (byte-identical to Wave 0 baseline)
- Pending for Wave 3 final task: atomic deletion of rurp_shield.h:25-89 old-#define block (replaces the previous "remove shim block" task per D-06 restructure)
</output>
