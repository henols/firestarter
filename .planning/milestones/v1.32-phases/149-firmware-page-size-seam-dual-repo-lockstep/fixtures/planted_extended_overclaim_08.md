<!-- test fixture for 149-check-claims.py -- NOT a phase artifact -- never add to _DEFAULT_TARGETS -->
<!-- planted violation for plan 08's extended-target-list RED proof: trips the confirmed-working
     forbidden label; the required caveat IS present, so this fixture fails for exactly one reason -->

This fixture stands in for a ninth artifact scanned alongside the real extended `_DEFAULT_TARGETS`
list, so that plan 08's RED proof is produced against the full eight-entry list plus one violation,
never a one-file subset.

This change is software-proven and unvalidated on silicon.

The planted sentence: the page-size seam was confirmed working on every mover part in the fleet.
