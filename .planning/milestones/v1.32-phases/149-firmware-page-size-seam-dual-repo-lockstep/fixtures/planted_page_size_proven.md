<!-- test fixture for 149-check-claims.py -- NOT a phase artifact -- never add to _DEFAULT_TARGETS -->
<!-- planted violation: trips the forbidden label for an explicit page-size validation claim; the
     required caveat IS present, so this fixture fails for exactly one reason -->

This change is software-proven and unvalidated on silicon.

The planted sentence: the page size is validated for the fifteen mover parts once bench data exists.
