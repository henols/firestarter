---
phase: 149-firmware-page-size-seam-dual-repo-lockstep
plan: 08
type: execute
wave: 8
depends_on: ["149-01", "149-02", "149-03", "149-04", "149-05", "149-06", "149-07"]
commits_land_in: [firestarter_app, firestarter, meta]
files_modified:
  - firestarter_app/README.md
  - .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md
  - .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py
  - .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/test_check_claims_v132.py
  - .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CLAIM-GATE-TRANSCRIPTS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PGSZ-01, PGSZ-02, PGSZ-03, PGSZ-04, PGSZ-05]
must_haves:
  truths:
    - "149-PAGE-SIZE.md is complete and reviewable whole, carrying every section D-16 enumerates (D-16)"
    - "One firestarter_app changelog line announces the behaviour change as software-proven and unvalidated on silicon (D-20)"
    - "The claim gate's target list now covers 149-PAGE-SIZE.md and every 149-NN-SUMMARY.md, enumerated and never globbed (D-19)"
    - "The extended gate was seen RED on a planted overclaim and GREEN after revert, with both transcripts committed"
    - "The README changelog line is scanned by the gate via argv, since it lives outside the phase directory"
    - "PGSZ-01 through PGSZ-05 are flipped to Complete here, in the one plan that finishes them"
    - "No CHANGELOG.md is created — the changelog surface is README.md, per Phase 148's recorded decision"
  artifacts:
    - path: ".planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md"
      provides: "The completed D-16 review artifact: provenance, mover lists, three non-claims, cold figures with leonardo headroom as a number, MERGE-05 breach named"
      contains: "software-proven and unvalidated on silicon"
    - path: "firestarter_app/README.md"
      provides: "The D-20 changelog subsection under Breaking Changes (v1.32)"
      contains: "software-proven and unvalidated on silicon"
    - path: ".planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py"
      provides: "The extended, enumerated target list plus a _CAVEAT_RULES entry per target"
      contains: "149-07-SUMMARY.md"
  key_links:
    - from: ".planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py"
      to: "every 149-NN-SUMMARY.md and 149-PAGE-SIZE.md"
      via: "_DEFAULT_TARGETS enumerated from _HERE, one _CAVEAT_RULES entry each, never a glob"
      pattern: "_DEFAULT_TARGETS"
    - from: ".planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py"
      to: "firestarter_app/README.md"
      via: "positional argv, which bypasses the directory-locality self-check by design"
      pattern: "README.md"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "both traceability tables flipped PGSZ-01..05 Pending -> Complete in the same commit"
      pattern: "PGSZ-05"
decisions_covered: [D-16, D-19, D-20]
---

<objective>
Close the record honestly, and prove the honesty gate covers what was written.

Three things land here and nowhere else. `149-PAGE-SIZE.md` becomes complete and reviewable whole —
D-16's condition for splitting the DB-side and firmware-side work across plans 03 and 04 was that the
artifact could still show both evidences separately, and this is where that is verified rather than
assumed. One `firestarter_app` changelog line announces the change: unlike Phase 148's VCC correction,
this one **does** alter write behaviour for 15 parts, so it is announced rather than merely recorded.
And the claim gate's target list is extended from one file to every artifact this phase produced, then
seen RED and GREEN again.

The ordering is forced. The gate has no exit-0-on-nothing-scanned branch and it fails closed on a target
missing from disk, so it could not have named a SUMMARY before that SUMMARY existed. That is why plan 02
armed it over `149-PAGE-SIZE.md` alone and this plan extends it — two transcripts, both real.

This plan also flips PGSZ-01 through PGSZ-05. No earlier plan flips any requirement checkbox, because
each of the five spans several plans and a checkbox flipped early is a claim nobody has finished
substantiating.

Purpose: leave a record a future investigator can read whole and trust, over a change that is
**software-proven and unvalidated on silicon**.
Output: the completed artifact, the changelog line, the extended gate with fresh transcripts, and five
flipped requirements.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CONTEXT.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-RESEARCH.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-VALIDATION.md
</context>

<planner_decisions>
Settled here so no executor re-opens them.

- **The changelog surface is `firestarter_app/README.md`, and no `CHANGELOG.md` is created.** There is no
  `CHANGELOG.md` in the repo, and Phase 148 recorded its absence as a deliberate decision and asserted it
  in a plan verify block. That assertion is **not** a live test, so creating one would not go red — but it
  would silently contradict a closed phase's decision. The new block is a `###` subsection under the
  existing `## Breaking Changes (v1.32)` heading.
- **Insertion point: immediately before the section's closing sentence**, which currently reads
  "This change is beta-only (v1.32). Nothing is promoted to stable without operator authorization." That
  sentence closes the whole v1.32 section; a `###` placed after it would orphan it. Its singular "This
  change" already covers two subsections and becomes a third — pluralise it deliberately in the same
  diff rather than leaving it wrong.
- **The heading carries its own disqualifier**, following Phase 148's model
  (`### AT28C-family VCC now reports 5.0v (correction, not a write-path fix)`). The 149 analogue names
  both the change and its limit in the heading itself.
- **The README is scanned via positional argv, not `_DEFAULT_TARGETS`.** It lives outside `_HERE`, so
  `_assert_default_targets_are_local()` would reject it — and that self-check must not be relaxed.
  `resolve_targets`'s argv precedence exists for exactly this, and the self-check only validates the
  defaults.
- **`149-08-SUMMARY.md` cannot be in `_DEFAULT_TARGETS`** — it does not exist when the gate is authored,
  and the gate fails closed on a missing target. It is covered by the argv run performed **after** the
  SUMMARY is written, and that must be stated in the SUMMARY itself so a reader knows which mechanism
  covered which file.
- **`149-CONTEXT.md`, `149-RESEARCH.md`, `149-DISCUSSION-LOG.md`, `149-CLAIM-GATE-TRANSCRIPTS.md`,
  `149-DB-TRANSCRIPTS.md`, `149-FW-TRANSCRIPTS.md`, `149-PARITY-TRANSCRIPTS.md`,
  `149-SIZE-TRANSCRIPTS.md`, the `149-*-cold-*.log` files and everything under `fixtures/` stay
  excluded.** All of them contain either the vocabulary the gate rejects or raw tool output. A test leg
  asserts each exclusion by name, which is what stops a future "just glob the phase directory" edit.
- **Hand-edit `REQUIREMENTS.md` and `ROADMAP.md` with scoped replacements; snapshot and diff both.** The
  `gsd requirements` / `gsd roadmap` verbs normalise the whole file, and `phase.complete` has clobbered an
  unrelated phase's `**Plans:**` line before. Take a `git stash`-free copy of both files before the edit
  and diff after, confirming the only changed lines are the ones intended.
- **Do NOT flip the ROADMAP phase checkbox** (`- [ ] **Phase 149: …**`). That belongs to
  `/gsd-verify-work` and `phase.complete`, not to a plan.
- **`autonomous: false`.** The wording review is a blocking human gate that the claim gate explicitly
  cannot discharge — its own donor states it "cannot detect an implied overclaim, a misleading omission, a
  wrong tone, or a true statement placed where it misleads". Do not run this plan under `--auto` or
  `--chain`; auto mode auto-approves outward-facing gates, and the changelog line is outward-facing.
- **Worktree isolation MUST be OFF for this plan: set `USE_WORKTREES_FOR_PLAN=false`.** This is an
  instruction, not a footnote. `git worktree add` on this superproject leaves **both** submodules with
  **0 entries**, and GSD's per-plan worktree gate intersects only `files_modified` against
  `.gitmodules` paths — a **lower bound** that cannot see a plan which merely *reads* a submodule. So
  read `commits_land_in` **plus the task bodies**, not `files_modified`. This plan's `commits_land_in` is
  `[firestarter_app, firestarter, meta]`, where **`firestarter` is read-only** — commits land in
  `firestarter_app` (the README changelog line) and in meta (the artifact, the claim gate,
  `REQUIREMENTS.md`, `ROADMAP.md`). It is a hard dependency because this plan runs the whole phase gate
  from `/workspaces/firestarter`: `pio test -e native`, `pio test -e native_nodevtools`,
  `python3 -m pytest tests/` and both `check_size_baseline.py` modes. None of that exists in an empty
  submodule, and the app-side `@requires_fw` legs would silently SKIP.
- **All eight plans in this phase are serialized, one plan per wave** (`149-01` wave 1 … `149-08`
  wave 8; every plan `depends_on` at least its immediate predecessor, and `149-08` depends on all
  seven). Two consequences for `execute-phase`: its
  worktree-cleanup step does **not** apply (there is no worktree to clean, see above), and its
  post-wave shared-artifact-write step does **not** apply either (a wave of one has no concurrent
  writer to reconcile). Each sequential executor therefore owns its own record writes, in its own
  commit, exactly as a single-plan wave normally would — and **for this plan that includes
  `REQUIREMENTS.md`**. Plans `149-01`…`149-07` each carry an explicit prohibition on touching
  `REQUIREMENTS.md` and on flipping any `PGSZ-0N` checkbox or traceability row; that prohibition exists
  precisely so that **this** plan is the single writer, and it does **not** apply here. This plan MUST
  flip all five `PGSZ-0N` checkboxes in `REQUIREMENTS.md`, all five `PGSZ-0N` traceability rows in
  `REQUIREMENTS.md`, and all five `PGSZ-0N` traceability rows in `ROADMAP.md` (Task 2, step (e)), and
  it is **not complete** until it has — Task 2's automated leg asserts all fifteen flips. The only
  record line still not yours is the ROADMAP phase checkbox, which `/gsd-verify-work` and
  `phase.complete` own.
</planner_decisions>


<!-- Comment-text discipline: the literals below are negative-grepped by this plan's
     acceptance criteria against a DIFFERENT file than any this plan authors, so naming them
     in an <action> body cannot echo into the written artifact. Allowlisted explicitly. -->
<!-- planner-discipline-allow: is proven -->
<!-- planner-discipline-allow: This change is beta-only -->

<tasks>

<task type="auto">
  <name>Task 1: Complete 149-PAGE-SIZE.md and add the one README changelog subsection</name>
  <files>
    .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md,
    firestarter_app/README.md
  </files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md` in full as it
      now stands — plans 01, 03, 04, 05 and 06 each appended a section. Read it end to end before adding
      anything; the job here is to make it reviewable **whole**, which means checking the sections agree
      with each other, not just that each exists.
    - `149-CONTEXT.md` **D-16** — the enumerated required contents, and **D-20** — the changelog line's
      required framing.
    - `firestarter_app/README.md:61-91` — the `## Breaking Changes (v1.32)` section: the two existing
      `###` subsections at `:63` and `:76`, the AT28C VCC subsection's honest-heading shape and its bold
      "does not fix, and does not claim to fix" paragraph, and the section's closing sentence at `:90`.
    - `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-08-SUMMARY.md` — Phase 148's
      recorded decision that the changelog surface is README.md and that **no** `CHANGELOG.md` was
      created.
    - `.planning/REQUIREMENTS.md` §"Out of Scope" and `.planning/PROJECT.md` §"Current Milestone: v1.32"
      — the Evidence Ceiling wording neither artifact may cross.
    - `149-RESEARCH.md` §R9(f) — the measured README structure and the three precision points.
  </read_first>
  <action>
**(a) Complete `149-PAGE-SIZE.md`.** Verify every section D-16 requires is present and correct, then add
what is still missing. Replace the `## Baseline update and closing record (plans 07-08)` placeholder with
real content.

Required, and each must be checked rather than assumed:
  - The upstream-provenance table, the 15-mover and 3-no-change lists, and the D-01 justification with its
    `doc/infoic-field-dictionary.md:241` and minipro citations (plan 01).
  - The three measured non-claims: no silicon claim; AT28C256 unchanged so gh#21 is untouched; the floor's
    safety **unproven** for the 11 promoted 16/32 rows (plan 01).
  - The DB-side evidence, separately legible: `diff_db` census, the X-1 correction, the wire golden RED
    and the golden-plus-18-deltas GREEN, the D-07 exhaustive proof and what it does not cover (plan 03).
  - The firmware-side evidence, separately legible: the six edit points, the flush-count oracle's RED and
    GREEN with 132 / 130 / 132, the mechanism-corrected resolve-site note, the corrected floor comment,
    D-08's deliberate non-change, and the proof's honest ceiling (plan 04).
  - Cross-repo parity evidence with the skip-leg transcript and the layout-agreement ceiling (plan 05).
  - Cold flash and RAM figures for all three targets with **leonardo's remaining headroom as a number**,
    and v1.31's MERGE-05 band breach **named** with the new exemption's justification (plan 06).
  - The baseline update, including the corrected `firmware_tree_sha` and both size gates green (plan 07).

Then add, new in this plan:
  - `## What ships, and what does not` — a short closing section. What ships: 15 AT28C010-class parts now
    write using their datasheet 128-byte page instead of the 64-byte floor; 3 more are corroborated at 64
    and demonstrably unchanged; 66 promoted rows and AT28C256 are untouched; the firmware falls back to
    its named floor when the field is absent or invalid. What does not: no silicon evidence, `0x0D` stays
    `UNVERIFIED`, no `support_status` changed, gh#21/#32/#11/#12 remain open, and no bench validation
    exists or is planned for this change. Close with the literal sentence stating the whole change is
    **software-proven and unvalidated on silicon**.
  - `## The changelog line, mirrored` — the exact text of the README subsection added in (b), quoted, so
    the artifact and the outward-facing text can be reviewed against each other in one place.
  - `## The deferred work, and where it is filed` — the four pending todos plan 07 filed, named by
    filename with a one-line summary each.

**(b) Add the README subsection.** Insert a new `###` block under `## Breaking Changes (v1.32)`,
**immediately before** the section's closing "This change is beta-only (v1.32)…" sentence, and pluralise
that sentence in the same diff so it covers all three subsections.

The heading must carry its own disqualifier, in the shape of the existing AT28C VCC heading — naming both
the change (15 AT28C010-class parts now write with a 128-byte page) and its limit
(software-proven, unvalidated on silicon).

The body, three short paragraphs:
  1. What changed, concretely: the database now delivers a per-chip page size over the existing JSON
     command path for the 18 chips whose upstream record is natively protocol `0x0D`; 15 of them move from
     the firmware's 64-byte floor to their datasheet 128-byte page, and 3 are corroborated at 64 and
     therefore unchanged. Firmware that receives no page-size field keeps using the conservative 64-byte
     floor, so an older host against this firmware still issues legal write cycles.
  2. A bold paragraph in the shape of the AT28C VCC subsection's: this **does** alter write behaviour for
     those 15 parts — unlike the VCC correction, which did not — and it is stated as
     **software-proven and unvalidated on silicon**. No physical AT28C part was tested. **AT28C256 is
     unaffected**: its upstream record is not natively `0x0D` and its page size was already 64, so nothing
     about its behaviour changes and nothing here explains the failure reported against it. Protocol
     `0x0D` remains `UNVERIFIED` and no chip's support status changed.
  3. One sentence on what a user might notice: writes to those 15 parts issue half as many page-write
     cycles, so they may complete faster; nothing else about the command surface changes.

Do **not** create `CHANGELOG.md`. Do not modify any other README section. Keep the wording tight — D-20
asks for one honest user-facing entry, not an essay.
  </action>
  <acceptance_criteria>
    - `149-PAGE-SIZE.md` contains **no** remaining `*(filled by plan` placeholder.
    - It contains all of: the four-way provenance arithmetic (`47`, `19`, `18`, `84`, `66`); all 18 named
      parts; `infoic-field-dictionary.md:241`; the words `unproven`, `byte-unchanged`, `mechanism-corrected`,
      `headroom`, `band breach`, `SKIPPED`, `layout agreement`; the numbers `132`, `130`, `686`, `56`,
      `744`, `512`; and the corrected-tree-SHA discussion (`3d8ec49`).
    - It contains the three new `##` sections named in the action.
    - The literal `software-proven and unvalidated on silicon` appears at least **three** times across the
      artifact — the opening statement, the closing section, and the mirrored changelog text.
    - `grep -c 'is proven' 149-PAGE-SIZE.md` is **0**; the file contains no sentence claiming any GitHub
      issue is closed, resolved or fixed; and it contains no `support_status` assignment or change claim.
    - `firestarter_app/README.md` has exactly **three** `###` headings between `## Breaking Changes (v1.32)`
      and `## Breaking Changes (v1.20)`.
    - The new `###` heading names both the change and its limit, and appears **before** the
      "beta-only (v1.32)" closing sentence.
    - The closing sentence is pluralised (it no longer begins "This change is beta-only").
    - The new README body contains the literal `software-proven and unvalidated on silicon`, the words
      `AT28C256 is unaffected`, `UNVERIFIED`, `15` and `128`.
    - `test ! -f firestarter_app/CHANGELOG.md` — no changelog file was created.
    - `git -C /workspaces/firestarter_app diff --stat README.md` shows changes only inside the v1.32
      section (line range roughly 61-92 before the edit).
    - **`-C` on every `git` *acceptance criterion* in this plan is required, not stylistic** (the
      `<verification>` gate block is exempt: it declares its own cwd in its header). A `git diff` /
      `git status` pathspec that points **inside** a submodule exits **0** from the superproject
      even while that submodule is dirty, so the meta-relative form (`git diff --quiet
      firestarter/...` run from `/workspaces`) is **vacuous**. The absolute `-C` path also makes
      each check cwd-independent, which the meta-relative `grep` paths in these same lists are not.
    - `python3 -m ruff check firestarter/ tests/` still exits 0 from `firestarter_app` (unchanged; the
      README is outside ruff's scope but the run confirms nothing else moved).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib,re
a=pathlib.Path('.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md').read_text()
assert '*(filled by plan' not in a, 'placeholders remain'
for s in ['47','19','18','84','66','infoic-field-dictionary.md:241','unproven','byte-unchanged','mechanism-corrected','headroom','band breach','SKIPPED','layout agreement','132','130','686','56','744','512','3d8ec49']:
    assert s in a, 'artifact missing '+repr(s)
for h in ['## What ships, and what does not','## The changelog line, mirrored','## The deferred work, and where it is filed']:
    assert h in a, 'missing section '+h
n=a.count('software-proven and unvalidated on silicon')
assert n>=3, 'the PGSZ-05 phrase appears %d times, need >= 3'%n
assert a.count('is proven')==0, 'forbidden phrase \"is proven\"'
parts=['AT28C010,AT28C010E','AT28C040,AT28C040E','AT28LV010','AT28MC020','AT28MC040','CAT28C512','CAT28C010','CAT28C020','CAT28C040','28C010,28C010T,28C011,28C011T','M28010','WE512K8','WME128K8','X28C010','AT28MC010','WE128K8','WE256K8']
for p in parts: assert p in a, 'missing part '+p
r=pathlib.Path('firestarter_app/README.md').read_text()
sec=r[r.index('## Breaking Changes (v1.32)'):r.index('## Breaking Changes (v1.20)')]
subs=re.findall(r'^### .*$', sec, re.M)
assert len(subs)==3, 'expected 3 subsections under v1.32, found %d: %r'%(len(subs),subs)
close=[l for l in sec.splitlines() if 'beta-only (v1.32)' in l]
assert close, 'closing beta-only sentence missing'
assert not close[0].strip().startswith('This change is beta-only'), 'closing sentence not pluralised'
assert sec.index(subs[2]) < sec.index(close[0]), 'new subsection placed after the closing sentence'
new=sec[sec.index(subs[2]):sec.index(close[0])]
for s in ['software-proven and unvalidated on silicon','AT28C256 is unaffected','UNVERIFIED','15','128']:
    assert s in new, 'README subsection missing '+repr(s)
assert not pathlib.Path('firestarter_app/CHANGELOG.md').exists(), 'CHANGELOG.md must not be created'
print('OK artifact complete (phrase x%d), README has 3 subsections with the new one before the pluralised closing sentence'%n)
" && echo "149-08-T1 OK"</automated>
  </verify>
  <done>`149-PAGE-SIZE.md` is complete with no placeholders, carries the DB-side and firmware-side
  evidence separately as D-16 requires, states the PGSZ-05 phrase at least three times, and mirrors the
  changelog text. `README.md` carries one new honest `###` subsection under `## Breaking Changes (v1.32)`,
  placed before a now-pluralised closing sentence, announcing the behaviour change while stating AT28C256
  is unaffected and `0x0D` remains `UNVERIFIED`. No `CHANGELOG.md` exists.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the claim gate to every 149 artifact, re-prove RED then GREEN, and flip PGSZ-01..05</name>
  <files>
    .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py,
    .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/test_check_claims_v132.py,
    .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CLAIM-GATE-TRANSCRIPTS.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py` as plan 02
      wrote it — `_DEFAULT_TARGETS`, `_CAVEAT_RULES`, `_required_caveats_for` (which fails **closed**: an
      unmapped basename gets the FULL caveat set), `_assert_default_targets_are_local` (both the locality
      and the `149-` prefix legs), `resolve_targets`'s argv-over-env-over-defaults precedence, and the
      exit-code contract with no exit-0-on-nothing-scanned branch.
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/test_check_claims_v132.py` — the
      legs that enumerate `_DEFAULT_TARGETS`, in particular the ones asserting every basename has a
      `_CAVEAT_RULES` entry and that the excluded files are excluded.
    - `.planning/REQUIREMENTS.md` §"Firmware Page-Size Seam (PGSZ)" (the five checkboxes) and its
      §"Traceability" table rows for PGSZ-01…PGSZ-05.
    - `.planning/ROADMAP.md` — its own traceability table rows for PGSZ-01…PGSZ-05, and the
      `- [ ] **Phase 149: …**` line, which this plan must **not** touch.
    - `149-RESEARCH.md` §R9(e) — the ordering hazard, the never-glob rule with the measured reason, and
      the recommendation to extend here rather than author late.
  </read_first>
  <action>
Working directory `/workspaces`.

**(a) Extend `_DEFAULT_TARGETS`.** Enumerate, one `os.path.join(_HERE, …)` per entry:
`149-PAGE-SIZE.md`, `149-01-SUMMARY.md`, `149-02-SUMMARY.md`, `149-03-SUMMARY.md`,
`149-04-SUMMARY.md`, `149-05-SUMMARY.md`, `149-06-SUMMARY.md`, `149-07-SUMMARY.md` — eight entries.
**Never a glob.** `149-CONTEXT.md` and `149-RESEARCH.md` both contain the vocabulary the gate rejects, as
do the five transcript files and everything under `fixtures/`; a `149-*.md` default set would sweep them
in and the gate would be permanently red for reasons that are not overclaims.

`149-08-SUMMARY.md` is deliberately **not** in the list: it does not exist when the gate runs during this
task, and the gate fails closed on a missing target. It is covered by the argv run in (c).

**(b) Extend `_CAVEAT_RULES`.** Add an entry for every new basename, each mapping to
`frozenset({"software-proven-unvalidated"})`, so PGSZ-05's phrase is required in every SUMMARY as well as
in the artifact. The fail-closed default stays: an unmapped basename gets the full caveat set. Update the
paired suite's `test_every_default_targets_basename_has_a_caveat_rule_entry` expectations if it hardcodes
a count.

**(c) Re-prove RED, then GREEN, on the extended list.**
  - **RED**: plant one overclaim in a **fixture** and run the gate through the env seam pointed at the
    full extended target list **plus** that fixture, so the RED is produced on the real list rather than a
    one-file subset. Paste the command, the literal output and `EXIT=1`.
  - **GREEN**: run the gate with no argv and no env seam — the extended defaults — and paste the command,
    the literal output and `EXIT=0`.
  - **ARGV**: run the gate with positional arguments naming the eight defaults **plus**
    `firestarter_app/README.md`, and paste it with `EXIT=0`. This is how the outward-facing changelog line
    is scanned; it lives outside `_HERE`, so `_assert_default_targets_are_local()` would reject it as a
    default, and that self-check must not be relaxed.
  - Run the paired suite: `python3 -m pytest test_check_claims_v132.py -q`.
  Append all four blocks to `149-CLAIM-GATE-TRANSCRIPTS.md` under a
  `## Extended target list (plan 08)` heading. As in plan 02, this file stays **out** of
  `_DEFAULT_TARGETS` and its planted text must not be quoted in any SUMMARY.

**(d) After writing `149-08-SUMMARY.md`**, run the argv form once more including
`149-08-SUMMARY.md` and `firestarter_app/README.md`, confirm `EXIT=0`, and record that run in the SUMMARY
itself, stating plainly which files were covered by the defaults and which by argv.

**(e) Flip PGSZ-01…PGSZ-05.** This is the only plan in the phase that flips a requirement checkbox,
because each of the five spans several plans. Use **scoped hand edits**, not the `gsd requirements` or
`gsd roadmap` verbs — those normalise the whole file, and `phase.complete` has clobbered an unrelated
phase's `**Plans:**` line before.
  - Copy `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to the scratch directory first.
  - In `REQUIREMENTS.md`: change `- [ ] **PGSZ-01**` … `- [ ] **PGSZ-05**` to `- [x]`, and change the five
    `| PGSZ-0N | Phase 149 | Pending |` traceability rows to `Complete`.
  - In `ROADMAP.md`: change the five `| PGSZ-0N | Phase 149 | Pending |` rows to `Complete`. Do **not**
    touch the `- [ ] **Phase 149: …**` checkbox — `/gsd-verify-work` and `phase.complete` own that.
  - Diff both files against the copies and confirm the **only** changed lines are the ten (five plus five)
    plus the five ROADMAP rows. Any other changed line means a normaliser ran; revert and redo by hand.

Stage only the files this plan names. The meta working tree carries both submodule gitlinks, untracked
`.claude/` and `package*.json`; never `git add -A`.
  </action>
  <acceptance_criteria>
    - `_DEFAULT_TARGETS` has exactly **8** entries; every one resolves inside `_HERE` and every basename
      starts with `149-`; `149-08-SUMMARY.md` is **not** among them.
    - No `glob`, `iglob`, `Path.glob` or `*.md` construction appears anywhere in `149-check-claims.py`.
    - Every `_DEFAULT_TARGETS` basename has a `_CAVEAT_RULES` entry, and each maps to a frozenset
      containing `software-proven-unvalidated`.
    - `149-CONTEXT.md`, `149-RESEARCH.md`, `149-DISCUSSION-LOG.md`, `149-CLAIM-GATE-TRANSCRIPTS.md`,
      `149-DB-TRANSCRIPTS.md`, `149-FW-TRANSCRIPTS.md`, `149-PARITY-TRANSCRIPTS.md` and
      `149-SIZE-TRANSCRIPTS.md` are all absent from `_DEFAULT_TARGETS`.
    - `python3 149-check-claims.py; echo EXIT=$?` from the phase directory prints `EXIT=0` over all eight
      defaults.
    - The argv form naming the eight defaults plus `firestarter_app/README.md` exits **0**.
    - A run through the env seam including a planted fixture exits **1**.
    - `python3 -m pytest test_check_claims_v132.py -q -o addopts=""` passes.
    - `149-CLAIM-GATE-TRANSCRIPTS.md` contains a `## Extended target list (plan 08)` section with all four
      blocks, each pasting its literal command line, and at least one `EXIT=1` and one `EXIT=0` inside it.
    - Every `149-NN-SUMMARY.md` for N in 01..07 contains the literal
      `software-proven and unvalidated on silicon` (this is what makes the GREEN run non-trivial).
    - `.planning/REQUIREMENTS.md` contains `- [x] **PGSZ-01**` through `- [x] **PGSZ-05**` and **zero**
      occurrences of `| PGSZ-0` followed by `| Pending |`.
    - `.planning/ROADMAP.md` contains **zero** occurrences of `| PGSZ-0` followed by `| Pending |`, and
      still contains the line `- [ ] **Phase 149:` unchanged.
    - `diff` of `REQUIREMENTS.md` and `ROADMAP.md` against the pre-edit copies shows only the intended
      lines changed — no reformatting anywhere else in either file.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep && python3 149-check-claims.py; echo "GATE_EXIT=$?"; python3 -m pytest test_check_claims_v132.py -q -o addopts="" && python3 -c "
import importlib.util,os,pathlib,subprocess,sys
spec=importlib.util.spec_from_file_location('cc149','149-check-claims.py')
m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
src=pathlib.Path('149-check-claims.py').read_text()
for bad in ['glob(','iglob','*.md']:
    assert bad not in src, 'globbing target list: '+bad
assert len(m._DEFAULT_TARGETS)==8, ('expected 8 targets, got', [os.path.basename(x) for x in m._DEFAULT_TARGETS])
bn={os.path.basename(x) for x in m._DEFAULT_TARGETS}
want={'149-PAGE-SIZE.md'}|{'149-%02d-SUMMARY.md'%i for i in range(1,8)}
assert bn==want, ('target drift', bn^want)
for x in m._DEFAULT_TARGETS:
    assert os.path.dirname(x)==m._HERE and os.path.basename(x).startswith('149-'), x
for excluded in ['149-08-SUMMARY.md','149-CONTEXT.md','149-RESEARCH.md','149-DISCUSSION-LOG.md','149-CLAIM-GATE-TRANSCRIPTS.md','149-DB-TRANSCRIPTS.md','149-FW-TRANSCRIPTS.md','149-PARITY-TRANSCRIPTS.md','149-SIZE-TRANSCRIPTS.md']:
    assert excluded not in bn, excluded+' must not be a default target'
for b in bn:
    assert b in m._CAVEAT_RULES, b+' has no _CAVEAT_RULES entry'
    assert 'software-proven-unvalidated' in m._CAVEAT_RULES[b], b+' does not require the PGSZ-05 caveat'
for i in range(1,8):
    p=pathlib.Path('149-%02d-SUMMARY.md'%i)
    assert p.is_file(), 'missing '+str(p)
    assert 'software-proven and unvalidated on silicon' in p.read_text(), str(p)+' lacks the PGSZ-05 phrase'
r=subprocess.run([sys.executable,'149-check-claims.py']+[str(x) for x in m._DEFAULT_TARGETS]+['/workspaces/firestarter_app/README.md'],capture_output=True,text=True)
assert r.returncode==0, 'argv run including README exited %d:\n%s'%(r.returncode,r.stdout+r.stderr)
tr=pathlib.Path('149-CLAIM-GATE-TRANSCRIPTS.md').read_text()
assert '## Extended target list (plan 08)' in tr, 'missing the plan-08 transcript section'
ext=tr[tr.index('## Extended target list (plan 08)'):]
assert 'EXIT=1' in ext and 'EXIT=0' in ext, 'extended section lacks a RED and a GREEN block'
print('OK gate extended to 8 enumerated targets, caveat rules complete, argv run incl. README exits 0')
" && cd /workspaces && python3 -c "
import pathlib,re
rq=pathlib.Path('.planning/REQUIREMENTS.md').read_text()
rm=pathlib.Path('.planning/ROADMAP.md').read_text()
for i in range(1,6):
    assert ('- [x] **PGSZ-0%d**'%i) in rq, 'PGSZ-0%d not flipped in REQUIREMENTS.md'%i
assert not re.search(r'\|\s*PGSZ-0\d\s*\|\s*Phase 149\s*\|\s*Pending\s*\|', rq), 'REQUIREMENTS traceability still Pending'
assert not re.search(r'\|\s*PGSZ-0\d\s*\|\s*Phase 149\s*\|\s*Pending\s*\|', rm), 'ROADMAP traceability still Pending'
assert '- [ ] **Phase 149:' in rm, 'the ROADMAP phase checkbox must NOT be flipped by a plan'
print('OK PGSZ-01..05 flipped in both traceability tables; phase checkbox untouched')
" && echo "149-08-T2 OK"</automated>
  </verify>
  <done>The claim gate now covers `149-PAGE-SIZE.md` and all seven earlier SUMMARYs as eight enumerated,
  never-globbed targets, each with a caveat rule requiring PGSZ-05's phrase; the extended list was seen
  RED on a planted fixture and GREEN on the real artifacts; the outward-facing README line is scanned via
  argv with the locality self-check intact; and PGSZ-01…PGSZ-05 are flipped to Complete in both
  traceability tables with the phase checkbox deliberately untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator wording review of the completed record and the outward-facing changelog line</name>
  <files>.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md, firestarter_app/README.md</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md` — as completed by
      task 1; the artifact under review, meant to be read whole.
    - `firestarter_app/README.md` §"Breaking Changes (v1.32)" — the three subsections, including the new one.
    - `.planning/PROJECT.md` §"Current Milestone: v1.32" and `.planning/REQUIREMENTS.md` §"Out of Scope" —
      the Evidence Ceiling this review enforces by hand.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py:87-95`
      — the gate's own statement of what a pattern scan cannot detect, which is the whole reason this
      checkpoint exists.
    - `149-VALIDATION.md` §"Manual-Only Verifications" — the wording-review row.
  </read_first>
  <action>
Present the completed record and the outward-facing line for operator review and STOP. The requirement flips
in task 2 have already landed; nothing further may be committed and the phase may not be handed to
`/gsd-verify-work` until the operator has answered.

This gate is blocking and is **not** auto-approvable even under auto-advance: the README subsection is the
only text this phase puts in front of users, and a gh#21 reporter reading it as a fix for their part is the
single most damaging outcome available here. Post the what-built and how-to-verify content below, then wait
for the resume signal. If the operator names a sentence to change, change it, re-run the gate in both the
default and argv forms, and re-present.
  </action>
  <what-built>
The complete `149-PAGE-SIZE.md` review artifact, one outward-facing `firestarter_app/README.md`
subsection announcing that 15 AT28C010-class parts now write with a 128-byte page, and a claim gate
extended to eight enumerated targets and re-proved RED then GREEN.

The gate is a **pattern** scan. Its own donor states plainly that it "cannot detect an implied overclaim,
a misleading omission, a wrong tone, or a true statement placed where it misleads". A green gate is
therefore necessary and not sufficient, and `149-VALIDATION.md` lists this wording review as a
manual-only verification for exactly that reason. The README line is the only text in this phase that
faces users rather than the project's own record.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-PAGE-SIZE.md` end to end, in
   one sitting. It is meant to be reviewable **whole**. Confirm:
   - no sentence implies silicon validation, a `0x0D` graduation, a `support_status` change, or that
     gh#21, gh#32, gh#11 or gh#12 is closed or explained;
   - the DB-side and firmware-side evidence are separately legible — that was D-16's stated condition for
     splitting them across two plans;
   - leonardo's remaining headroom is a **number**, and v1.31's MERGE-05 band breach is **named**;
   - the 16/32 promoted rows are described as **unproven**, never as overrunning today.
2. Read the new `###` subsection in `firestarter_app/README.md`. Ask the reader's question, not the
   author's: if you owned an AT28C256 and had filed gh#21, would this paragraph mislead you into thinking
   your problem was fixed? It must not. Confirm it says AT28C256 is unaffected, that `0x0D` remains
   `UNVERIFIED`, and that the change is **software-proven and unvalidated on silicon**.
3. Confirm the closing "beta-only (v1.32)" sentence reads correctly now that it covers three subsections.
4. Run the gate yourself, from the phase directory:
       python3 149-check-claims.py ; echo EXIT=$?
   and the argv form including the README. Both must print `EXIT=0`.
5. Confirm `firestarter_app/CHANGELOG.md` does not exist — Phase 148 recorded that decision and this
   phase follows it.
6. Spot-check two SUMMARYs for the opposite failure: a true statement placed where it misleads. The
   pattern table cannot see that, and it is the most likely remaining defect.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the wording, or name the sentence to change.</resume-signal>
  <verify>
    <automated>cd /workspaces/.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep && python3 149-check-claims.py; echo "GATE_EXIT=$?"; python3 -c "
import pathlib
a=pathlib.Path('149-PAGE-SIZE.md').read_text()
r=pathlib.Path('/workspaces/firestarter_app/README.md').read_text()
sec=r[r.index('## Breaking Changes (v1.32)'):r.index('## Breaking Changes (v1.20)')]
assert 'software-proven and unvalidated on silicon' in sec, 'README section lacks the PGSZ-05 phrase'
assert 'AT28C256 is unaffected' in sec, 'README section does not state AT28C256 is unaffected'
assert 'UNVERIFIED' in sec, 'README section does not state 0x0D remains UNVERIFIED'
assert not pathlib.Path('/workspaces/firestarter_app/CHANGELOG.md').exists(), 'CHANGELOG.md must not exist'
assert '*(filled by plan' not in a, 'artifact still carries placeholders'
print('OK record and changelog line ready for the wording review')
" && echo "149-08-T3 OK"</automated>
  </verify>
  <done>The operator has read `149-PAGE-SIZE.md` whole, confirmed no sentence implies silicon validation, a
  `0x0D` graduation, a `support_status` change or a closed issue, confirmed the DB-side and firmware-side
  evidence are separately legible, confirmed leonardo's headroom is a number and the band breach is named,
  and approved the outward-facing README subsection as not misleading a gh#21 reporter. Approval is recorded
  in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's record → a future investigator, including whoever answers gh#21 | the last point at which an overclaim can be prevented rather than retracted |
| `README.md` → users on the beta channel | the only outward-facing text this phase produces, and the one a reporter is most likely to read |
| a green pattern gate → the honesty claim it licenses | the gate's own donor disclaims exactly the failure modes a human review catches |
| a plan → `REQUIREMENTS.md` and `ROADMAP.md` | normalising verbs reformat whole files and `phase.complete` has clobbered an unrelated phase's line before |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-49 | Repudiation | an outward-facing changelog line a gh#21 reporter reads as a fix for their part | critical | mitigate | the README body must state AT28C256 is unaffected, that `0x0D` remains `UNVERIFIED`, and the PGSZ-05 phrase; the heading carries its own disqualifier; the human checkpoint asks the reader's question explicitly |
| T-149-50 | Repudiation | an artifact that is individually correct but not reviewable whole | high | mitigate | Task 1 requires reading the accumulated file end to end and checking the sections agree, and the acceptance criteria assert every cross-plan marker is present in one file; the checkpoint requires a single-sitting read |
| T-149-51 | Spoofing | a gate extended by globbing, sweeping in the upstream artifacts and going permanently red — or silently narrowed to hide a target | high | mitigate | no `glob` construction is permitted, the target list is asserted to be exactly the 8 expected basenames, and every excluded file is asserted absent by name |
| T-149-52 | Repudiation | a SUMMARY that never had the required caveat because its basename was unmapped | medium | mitigate | `_required_caveats_for` fails **closed** (an unmapped basename gets the full caveat set), a leg asserts every default target has a `_CAVEAT_RULES` entry, and the verify asserts all seven SUMMARYs carry the phrase |
| T-149-53 | Repudiation | `149-08-SUMMARY.md` never scanned because it did not exist when the gate ran | medium | mitigate | the argv run is performed after the SUMMARY is written and recorded in the SUMMARY itself, stating which files the defaults covered and which argv covered |
| T-149-54 | Tampering | a normalising verb reformatting `REQUIREMENTS.md` or `ROADMAP.md` and burying an unrelated change | high | mitigate | scoped hand edits only, both files copied before the edit and diffed after, with the acceptance criteria naming exactly which lines may change and asserting the phase checkbox is untouched |
| T-149-55 | Repudiation | flipping a requirement checkbox on work a later plan still touches | medium | mitigate | no earlier plan flips any checkbox; all five are flipped here, in the plan that finishes them, and every earlier plan's success criteria say so explicitly |
| T-149-56 | Repudiation | treating the green gate as sufficient and skipping the wording review | high | mitigate | the blocking checkpoint is the review, `autonomous: false` keeps auto mode from approving it, and the gate's own non-claim is quoted in the checkpoint text |
| T-149-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs; only pytest and the phase's own gate script run here |
</threat_model>

<verification>
Phase gate. Run all of it, and record each command with `; echo EXIT=$?` and its literal output.

From `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep`:

    python3 149-check-claims.py ; echo EXIT=$?                                  # EXIT=0, 8 targets
    python3 149-check-claims.py <8 defaults> /workspaces/firestarter_app/README.md ; echo EXIT=$?
    python3 -m pytest test_check_claims_v132.py -q -o addopts=""

From `/workspaces/firestarter_app`:

    python3 -m pytest tests/ -o addopts="" -q                       # budget >= 600 s
    python3 tools/build_db.py && git -C /workspaces/firestarter_app diff --quiet firestarter/data/chip_database.json
    python3 tools/diff_db.py ; echo EXIT=$?                          # EXIT=0, buckets 686/56/2
    python3 tools/check_dispatch.py ; echo EXIT=$?
    python3 -m ruff check firestarter/ tests/
    python3 -m ruff format --check firestarter/ tests/
    bash tools/ci_parity.sh                                          # 4 legs
    git diff --quiet tests/golden/wire_dict_baseline.json
    test ! -f CHANGELOG.md

From `/workspaces/firestarter`:

    pio test -e native && pio test -e native_nodevtools               # agreeing, suites 17
    python3 -m pytest tests/ -q
    python3 scripts/check_size_baseline.py --baseline scripts/baseline/size_baseline.json --avr-log … ; echo EXIT=$?
    python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log … ; echo EXIT=$?
    git diff --quiet src/proms/flash_5v_page.cpp platformio.ini include/messages.h

`tools/check_mypy_watermark.py` is python-version sensitive (devcontainer 3.12, app CI 3.11, mypy config
3.10). Run it, record the result, and say "green locally on 3.12" rather than "green in CI".

`autonomous: false` — this plan carries a blocking human-verify gate on outward-facing wording. Do
**not** run it under `--auto` or `--chain`.

Record-gate note: allow **300 s** for any `gsd-tools` record write. After every
`gsd-tools query commit`, check `HEAD` in all three repos. Snapshot `ROADMAP.md` before and after any
`phase.complete` and diff it — it has clobbered an unrelated phase's `**Plans:**` line before. If an
executor was interrupted at any point in this phase, re-check **commits**, not just the working tree: a
killed executor leaves commits behind.
</verification>

<success_criteria>
- `149-PAGE-SIZE.md` is complete, placeholder-free and reviewable whole, with the DB-side and
  firmware-side evidence separately legible, leonardo's headroom as a number, and v1.31's MERGE-05 band
  breach named.
- One README subsection announces the behaviour change for 15 parts, states AT28C256 is unaffected and
  `0x0D` remains `UNVERIFIED`, uses the literal PGSZ-05 phrase, and sits before a pluralised closing
  sentence. No `CHANGELOG.md` exists.
- The claim gate covers eight enumerated targets with a caveat rule each, never globs, and was seen RED
  then GREEN on the extended list; the README is scanned via argv with the locality self-check intact.
- Every `149-NN-SUMMARY.md` carries the required phrase, so the GREEN run is non-trivial.
- PGSZ-01…PGSZ-05 are flipped to Complete in both traceability tables, by scoped hand edits verified with
  a before/after diff, and the ROADMAP phase checkbox is untouched.
- The human wording review is approved.
- Every phase-gate command above is green and recorded with its literal output.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New content created by **this plan** (no new symbols):

| Artifact | Kind |
|---|---|
| `149-PAGE-SIZE.md` §"What ships, and what does not", §"The changelog line, mirrored", §"The deferred work, and where it is filed" | closing sections of the D-16 artifact |
| `firestarter_app/README.md` — one new `###` subsection under `## Breaking Changes (v1.32)` | the D-20 changelog line |
| `149-check-claims.py` `_DEFAULT_TARGETS` extended to 8 enumerated entries, with matching `_CAVEAT_RULES` | D-19's closing extension |
| `149-CLAIM-GATE-TRANSCRIPTS.md` §"Extended target list (plan 08)" | the second, real RED→GREEN pair |

The full phase-wide new-symbol surface is enumerated in `149-01-PLAN.md`
§"Artifacts this phase produces".
</artifacts_produced>

<output>
Create `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-08-SUMMARY.md` when done.

It must contain the literal `software-proven and unvalidated on silicon`, must **not** quote any planted
overclaim text, and must record the post-write argv gate run that covers it — stating which files the
enumerated defaults covered and which files argv covered.
</output>
