---
phase: 149-firmware-page-size-seam-dual-repo-lockstep
plan: 07
type: execute
wave: 7
depends_on: ["149-06"]
commits_land_in: [firestarter, meta]
files_modified:
  - firestarter/scripts/baseline/size_baseline.json
  - .planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md
  - .planning/todos/pending/fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md
  - .planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md
  - .planning/todos/pending/phase-44-read-timing-knobs-missing-json-parse-reset.md
  - .planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md
  - .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-SIZE-TRANSCRIPTS.md
autonomous: true
requirements: [PGSZ-04]
must_haves:
  truths:
    - "size_baseline.json is the live default baseline again: its AVR figures match the post-change cold measurement and the default byte-identity gate exits 0 (D-14)"
    - "meta.firmware_tree_sha is corrected to the tree actually measured — the stale Phase-144 value is gone (X-3)"
    - "native and native_nodevtools cases and succeeded are bumped by the SAME amount on BOTH envs; suites stays 17 and envs_agree stays true"
    - "No watermark is lowered and no band or exemption constant is touched by this plan"
    - "The folded todo is removed and four new pending todos carry their measured part lists and reasons (D-04, D-09, Open Question 5)"
  artifacts:
    - path: "firestarter/scripts/baseline/size_baseline.json"
      provides: "The updated live baseline with corrected tree SHA, post-change AVR figures, bumped native case counts and a superseding meta note"
      contains: "\"firmware_tree_sha\""
    - path: ".planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md"
      provides: "D-04 deliverable 1: the 66 promoted rows with the measured four-way provenance table and part-count breakdown"
      contains: "0x0D"
    - path: ".planning/todos/pending/fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md"
      provides: "D-04 deliverable 2: FM28V020 and MB85R256H named as a classification question"
      contains: "MB85R256H"
  key_links:
    - from: "firestarter/scripts/baseline/size_baseline.json"
      to: "firestarter/scripts/check_size_baseline.py default mode"
      via: "strict byte-identity comparison of every AVR figure against this file"
      pattern: "avr_targets"
    - from: "firestarter/scripts/baseline/size_baseline.json"
      to: "the plan 06 cold transcripts"
      via: "every AVR figure is transcribed from the committed cold logs, never re-derived warm"
      pattern: "flash_used"
decisions_covered: [D-04, D-14]
---

<objective>
Make `size_baseline.json` the live default baseline again, and fix the defect it has been carrying.

`meta.firmware_tree_sha` is `3d8ec4913913f5db4e636d88d5180172f83776f9`, the root tree of commit
`6cc4795` — a **Phase 144** commit that does **not** contain the +96 B the same file's own AVR figures
record. `meta.generated_by` correctly describes the Phase 145 re-measure; the tree SHA was simply never
updated alongside it. So the file's recorded provenance predates the numbers it reports. D-14's
phase-end update is the moment to correct it, or the same defect re-ships.

Everything numeric in this plan is **transcribed** from plan 06's committed cold transcripts. Nothing
is re-measured, because a re-measurement here would be warm by default and a warm figure written into
this file is exactly what makes the next cold CI run red.

Purpose: leave the next phase a baseline whose figures, provenance and case counts are all true, and
close out the deferred work this phase measured but deliberately did not do.
Output: the updated baseline, both size gates green, four new pending todos, and the folded one removed.
The change these figures measure is **software-proven and unvalidated on silicon**.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CONTEXT.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-RESEARCH.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-01-SUMMARY.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-04-SUMMARY.md
@.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-06-SUMMARY.md
</context>

<planner_decisions>
Settled here so no executor re-opens them.

- **Every figure is transcribed from plan 06's committed cold logs.** No `pio run`, no `pio test` and no
  `check_build_warnings.py --rebuild` in this plan. The one exception is the two gate runs at the end,
  which only read logs.
- **`native` and `native_nodevtools` are bumped by the SAME amount.** Plan 04 added 10 cases (5 per
  suite), so `cases` and `succeeded` go 141 → 151 on **both** pinned envs while `suites` stays **17**.
  Updating one env and not the other trips `envs_agree`, which is the single most common miss on this
  file. `native_pinmap_provisional` is untouched at 10 / 10 / 1. Use the number plan 06's SUMMARY
  actually recorded, not this predicted 10, if they differ.
- **No watermark is lowered.** `check_build_warnings.py`'s below-watermark arm returns INFO, not FAIL, so
  a cold figure below 1166 is not a problem to fix — and lowering the watermark to a figure measured in
  one session is how a future cold run goes red. The `warnings` block's `native` numbers stay 1166 /
  1166 / 138 and the AVR numbers stay 0 unless plan 06 measured a **higher** cold figure, in which case
  raise it and say why.
- **No band or exemption constant is touched.** `check_size_baseline.py` is plan 06's file and is
  byte-unchanged here; `size_baseline_base01.json` stays byte-unchanged for the whole phase.
- **`meta` gets a superseding note, not a rewrite.** Follow the file's own established Phase 144 / Phase
  145 pattern: `generated_by` gains a sentence naming what this update supersedes and scoping it, rather
  than replacing the prior provenance prose. `meta.supersedes`, `meta.consumed_by`,
  `meta.warm_vs_cold_correction` and `meta.roadmap_cross_check` all stay, with
  `roadmap_cross_check` re-derived for the new deltas.
- **Todo bookkeeping lives here, in one place.** The folded todo's code change landed in plan 04; its
  file is removed here so all five todo mutations are one reviewable diff.
- **Worktree isolation MUST be OFF for this plan: set `USE_WORKTREES_FOR_PLAN=false`.** This is an
  instruction, not a footnote. `git worktree add` on this superproject leaves **both** submodules with
  **0 entries**, and GSD's per-plan worktree gate intersects only `files_modified` against
  `.gitmodules` paths — a **lower bound** that cannot see a plan which merely *reads* a submodule. So
  read `commits_land_in` **plus the task bodies**, not `files_modified`. This plan's `commits_land_in` is
  `[firestarter, meta]` — commits land in **both**: `size_baseline.json` in `firestarter`, and the five
  `.planning/todos/` files plus `149-SIZE-TRANSCRIPTS.md` in meta.
- **All eight plans in this phase are serialized, one plan per wave** (`149-01` wave 1 … `149-08`
  wave 8; every plan `depends_on` at least its immediate predecessor, and `149-08` depends on all
  seven). Two consequences for `execute-phase`: its
  worktree-cleanup step does **not** apply (there is no worktree to clean, see above), and its
  post-wave shared-artifact-write step does **not** apply either (a wave of one has no concurrent
  writer to reconcile). Each sequential executor therefore owns its own record writes, in its own
  commit, exactly as a single-plan wave normally would — **but scoped**: update `STATE.md` and this
  phase's ROADMAP progress line yourself. Do **NOT** touch `REQUIREMENTS.md`, and do **NOT** flip any
  `PGSZ-0N` checkbox or `PGSZ-0N` traceability row in `REQUIREMENTS.md` or in `ROADMAP.md` — plan
  `149-08` alone flips all five, after the whole-phase gate is green, because every one of the five
  spans several plans and not one of them is finished when this plan ends. Your `requirements:`
  frontmatter records which requirement you **contribute to**, not which one you may mark Complete.
  Read that as a rule, not as an omission — and do not over-apply it either: `STATE.md` and the
  ROADMAP phase-progress line **are** yours to write, in your own commit; only the `PGSZ-0N` rows are
  not.
</planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Update size_baseline.json from the cold transcripts and run both size gates green</name>
  <files>
    firestarter/scripts/baseline/size_baseline.json,
    .planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-SIZE-TRANSCRIPTS.md
  </files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` in full. The blocks that change:
      `meta.generated`, `meta.phase`, `meta.generated_by`, `meta.firmware_tree_sha`,
      `meta.host_app_tree_sha`, `meta.roadmap_cross_check`, `meta.deltas_vs_base01.{leonardo,uno,uno328pb}`
      (each has `flash_used_delta`, `ram_used_delta` and a `merge05_clause` paragraph),
      `avr_targets.{uno,uno328pb,leonardo}.{flash_used,flash_free,ram_used,ram_free}`, and
      `native_envs.{native,native_nodevtools}.{cases,succeeded}`. The blocks that must **not** change:
      every `flash_total` / `ram_total`, every `suites`, `envs_agree`, `native_pinmap_provisional`, and
      the whole `warnings` block unless plan 06 measured a higher cold figure.
    - `.planning/phases/149-*/149-postchange-cold-uno.log`, `…-uno328pb.log`, `…-leonardo.log` — the
      only permitted source for the AVR figures.
    - `.planning/phases/149-*/149-06-SUMMARY.md` — the recorded `N`, `M`, and the exact number of native
      cases added.
    - `firestarter/scripts/check_size_baseline.py:1-60` — the two modes. Default mode is **strict
      byte-identity** against this file; `--policy merge05` is the band mode against BASE-01. Both must
      be green after this update. The argv parser takes `--baseline PATH`, repeated
      `--avr-log ENV=PATH`, repeated `--native-log ENV=PATH`, `--rebuild` and `--policy`.
    - `149-RESEARCH.md` §X-3 (the stale tree SHA, with the commit it actually belongs to), §R8(c) (the
      toolchain versions this file records, to be re-confirmed at update time), §R4(d) (the
      141 → 141+N bump on **both** pinned envs and the `envs_agree` trap), §"Common Pitfalls" §Pitfall 6
      and §Pitfall 7.
  </read_first>
  <action>
Working directory `/workspaces/firestarter`.

**(a) AVR figures.** Transcribe `flash_used` and `ram_used` for each of `uno`, `uno328pb`, `leonardo`
straight from plan 06's cold logs. Recompute `flash_free = flash_total - flash_used` and
`ram_free = ram_total - ram_used`. Leave every `flash_total` and `ram_total` unchanged — a changed total
means the board or framework moved, which is a finding, not an update.

**(b) `meta.firmware_tree_sha` — the X-3 correction.** Replace the stale
`3d8ec4913913f5db4e636d88d5180172f83776f9` with the root tree SHA of the firmware commit the plan 06
measurement was actually taken at (`git rev-parse HEAD^{tree}` on the firmware repo at that commit).
Add a short clause to `meta.generated_by` recording that the previous value was the root tree of commit
`6cc4795`, a Phase 144 commit that predated the +96 B the file's own figures recorded, and that this
update corrects that. Refresh `meta.host_app_tree_sha` the same way from `firestarter_app`.

**(c) `meta` provenance.** Set `meta.generated` to the update date and `meta.phase` to this phase.
Extend `meta.generated_by` with a superseding sentence in the file's own established voice, scoped
exactly: it supersedes the Phase 145 measurement for the three `avr_targets` `flash_used` / `flash_free`
/ `ram_used` / `ram_free` figures and for the two pinned `native_envs` case counts, and nothing else.
Re-confirm and, if they moved, update `platformio_core`, `platform_atmelavr`, `toolchain_atmelavr`,
`avr_gcc`, `framework_arduino_avr` and `framework_arduino_avr_minicore` from the live installation.

**(d) `meta.deltas_vs_base01`.** For each of the three envs set `flash_used_delta` and `ram_used_delta`
to the measured deltas against BASE-01, and extend each `merge05_clause` paragraph to name **both**
admitted exemptions — Phase 145's 96 B defect fix and this phase's page-size-seam exemption, by
constant name and value — keeping the "ADJUDICATED AND ADMITTED, not laundered" framing. If RAM moved,
the clause must say so and name the RAM exemption; the previous `ram_used_delta: 0` prose must not
survive a non-zero delta. Re-derive `meta.roadmap_cross_check` for the new numbers.

**(e) `native_envs`.** Bump `cases` and `succeeded` on **both** `native` and `native_nodevtools` by the
number plan 06 recorded (expected 10, giving 151). Leave `suites` at **17** on both, leave
`all_passed` true, leave `native_pinmap_provisional` untouched, and leave `envs_agree` true. Update
`envs_agree_note`'s quoted figures so its prose matches the new numbers — it currently names
`{cases: 141, suites: 17}` verbatim.

**(f) Warnings.** Leave the whole block as-is unless plan 06's cold run measured a **higher** figure. Do
not lower any watermark. If a figure came in lower, record it as INFO in the SUMMARY and leave the
watermark where it is.

**(g) Run both gates and commit the transcripts.** Append to `149-SIZE-TRANSCRIPTS.md`:

    python3 scripts/check_size_baseline.py \
      --baseline scripts/baseline/size_baseline.json \
      --avr-log uno=<postchange-cold-uno.log> \
      --avr-log uno328pb=<postchange-cold-uno328pb.log> \
      --avr-log leonardo=<postchange-cold-leonardo.log> ; echo EXIT=$?

under `## GREEN — default-mode byte identity against the updated baseline (D-14)`, expecting `EXIT=0`,
and re-run the `--policy merge05` form against BASE-01 under
`## GREEN — MERGE-05 re-confirmed after the baseline update`, also `EXIT=0`. Both must be green
simultaneously: the default gate proves the new figures are recorded exactly, and the band gate proves
the growth is admitted rather than absorbed.

Do not edit `check_size_baseline.py`, `size_baseline_base01.json`, any fixture, or any firmware source
in this task.
  </action>
  <acceptance_criteria>
    - `size_baseline.json` parses as JSON and its `avr_targets` `flash_used` / `ram_used` figures equal,
      byte for byte, the figures parsed from plan 06's three cold logs.
    - For each env, `flash_free == flash_total - flash_used` and `ram_free == ram_total - ram_used`.
    - Every `flash_total` and `ram_total` is unchanged from the pre-update file.
    - `meta.firmware_tree_sha` is **not** `3d8ec4913913f5db4e636d88d5180172f83776f9`, is a 40-character
      hex string, and `git -C /workspaces/firestarter cat-file -t <that sha>` reports `tree`.
    - `meta.generated_by` mentions `6cc4795` and the correction, and carries a superseding sentence
      scoped to the AVR figures and the two pinned native case counts.
    - `native_envs.native.cases == native_envs.native_nodevtools.cases` and the same for `succeeded`;
      both equal 141 plus the number plan 06 recorded (expected **151**).
    - `native_envs.native.suites == native_envs.native_nodevtools.suites == 17`, `envs_agree` is `true`,
      and `envs_agree_note` no longer contains the literal `141`.
    - `native_envs.native_pinmap_provisional` is byte-unchanged.
    - The `warnings` block is byte-unchanged, or any change is an **increase** with a stated reason. No
      value in it decreased.
    - Each of the three `meta.deltas_vs_base01.<env>.merge05_clause` paragraphs names
      `MERGE05_PAGE_SIZE_SEAM_EXEMPTION_BYTES` and its value, alongside the existing 96 B defect fix,
      and each `flash_used_delta` equals the measured delta against BASE-01.
    - `python3 scripts/check_size_baseline.py --baseline scripts/baseline/size_baseline.json --avr-log …`
      (default mode, no `--policy`) exits **0** against the three plan 06 cold logs.
    - `python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log …`
      exits **0**.
    - `git -C /workspaces/firestarter diff --quiet scripts/check_size_baseline.py`,
      `git -C /workspaces/firestarter diff --quiet scripts/baseline/size_baseline_base01.json`,
      `git -C /workspaces/firestarter diff --quiet tests`, and
      `git -C /workspaces/firestarter diff --quiet src include`.
    - **`-C` on every `git` *acceptance criterion* in this plan is required, not stylistic** (the
      `<verification>` gate block is exempt: it declares its own cwd in its header). A `git diff` /
      `git status` pathspec that points **inside** a submodule exits **0** from the superproject
      even while that submodule is dirty, so the meta-relative form (`git diff --quiet
      firestarter/...` run from `/workspaces`) is **vacuous**. The absolute `-C` path also makes
      each check cwd-independent, which the meta-relative `grep` paths in these same lists are not.
    - `149-SIZE-TRANSCRIPTS.md` contains both new GREEN sections with their literal command lines and
      `EXIT=0`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import json,re,pathlib,subprocess
P=pathlib.Path('/workspaces/.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep')
d=json.loads(pathlib.Path('scripts/baseline/size_baseline.json').read_text())
def cold(env):
    t=(P/('149-postchange-cold-%s.log'%env)).read_text(errors='replace')
    f=re.search(r'^Flash:.*used (\d+) bytes from (\d+) bytes',t,re.M)
    r=re.search(r'^RAM:.*used (\d+) bytes from (\d+) bytes',t,re.M)
    return int(f.group(1)),int(f.group(2)),int(r.group(1)),int(r.group(2))
tot={'uno':(32256,2048),'uno328pb':(32384,2048),'leonardo':(28672,2560)}
b01=json.loads(pathlib.Path('scripts/baseline/size_baseline_base01.json').read_text())
for env,(ft,rt) in tot.items():
    fu,ftl,ru,rtl=cold(env); a=d['avr_targets'][env]
    assert a['flash_used']==fu, '%s flash_used %d != cold %d'%(env,a['flash_used'],fu)
    assert a['ram_used']==ru, '%s ram_used %d != cold %d'%(env,a['ram_used'],ru)
    assert a['flash_total']==ft and a['ram_total']==rt, '%s totals moved'%env
    assert a['flash_free']==ft-fu and a['ram_free']==rt-ru, '%s free figures not recomputed'%env
    dl=d['meta']['deltas_vs_base01'][env]
    exp=fu-b01['avr_targets'][env]['flash_used']
    assert dl['flash_used_delta']==exp, '%s flash_used_delta %r != %d'%(env,dl['flash_used_delta'],exp)
    assert 'MERGE05_PAGE_SIZE_SEAM_EXEMPTION_BYTES' in dl['merge05_clause'], env+': merge05_clause does not name the new exemption'
sha=d['meta']['firmware_tree_sha']
assert sha!='3d8ec4913913f5db4e636d88d5180172f83776f9', 'X-3 not fixed: stale tree SHA remains'
assert re.fullmatch(r'[0-9a-f]{40}', sha), 'firmware_tree_sha is not a 40-char hex'
assert subprocess.run(['git','cat-file','-t',sha],capture_output=True,text=True).stdout.strip()=='tree', 'firmware_tree_sha is not a tree object'
assert '6cc4795' in d['meta']['generated_by'], 'generated_by does not record the correction'
n=d['native_envs']['native']; nd=d['native_envs']['native_nodevtools']
assert n['cases']==nd['cases'] and n['succeeded']==nd['succeeded'], 'pinned native envs disagree: %r vs %r'%(n,nd)
assert n['cases']>141, 'case count not bumped'
assert n['suites']==17==nd['suites'], 'suites moved'
assert d['envs_agree'] is True and '141' not in d['envs_agree_note'], 'envs_agree_note still quotes 141'
assert d['native_envs']['native_pinmap_provisional']=={'cases':10,'succeeded':10,'suites':1,'all_passed':True}, d['native_envs']['native_pinmap_provisional']
w=d['warnings']
assert w['native']['native']['total_watermark']>=1166 and w['native']['native_nodevtools']['total_watermark']>=1166, 'a native watermark was LOWERED'
assert w['native']['native_pinmap_provisional']['total_watermark']>=138, 'pinmap watermark lowered'
for e in ['uno','uno328pb','leonardo']:
    assert w['avr'][e]=={'macro_redefinition':0,'total':0}, 'AVR warning policy figures changed for '+e
print('OK baseline updated: cold figures transcribed, tree SHA corrected, both pinned envs bumped to %d, suites 17, no watermark lowered'%n['cases'])
" && P=/workspaces/.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep && python3 scripts/check_size_baseline.py --baseline scripts/baseline/size_baseline.json --avr-log uno=$P/149-postchange-cold-uno.log --avr-log uno328pb=$P/149-postchange-cold-uno328pb.log --avr-log leonardo=$P/149-postchange-cold-leonardo.log; echo "DEFAULT_GATE_EXIT=$?"; python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=$P/149-postchange-cold-uno.log --avr-log uno328pb=$P/149-postchange-cold-uno328pb.log --avr-log leonardo=$P/149-postchange-cold-leonardo.log; echo "MERGE05_GATE_EXIT=$?"; git diff --quiet scripts/check_size_baseline.py && git diff --quiet scripts/baseline/size_baseline_base01.json && git diff --quiet tests && git diff --quiet src include && python3 -c "
import pathlib
t=pathlib.Path('/workspaces/.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-SIZE-TRANSCRIPTS.md').read_text()
assert '## GREEN — default-mode byte identity against the updated baseline (D-14)' in t, 'missing default-gate transcript'
assert '## GREEN — MERGE-05 re-confirmed after the baseline update' in t, 'missing re-confirmation transcript'
print('OK both gate transcripts committed')
" && echo "149-07-T1 OK"</automated>
  </verify>
  <done>`size_baseline.json` is the live default baseline again: its AVR figures are transcribed from the
  committed cold logs with the free figures recomputed, `meta.firmware_tree_sha` is corrected to a real
  tree with the stale Phase-144 value recorded as corrected, both pinned native envs are bumped by the
  same amount with `suites` at 17 and `envs_agree` true, no watermark was lowered, both `merge05_clause`
  paragraphs name both admitted exemptions, and the default and band gates are green simultaneously.</done>
</task>

<task type="auto">
  <name>Task 2: File the four deferred todos with their measured lists, and remove the folded one</name>
  <files>
    .planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md,
    .planning/todos/pending/fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md,
    .planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md,
    .planning/todos/pending/phase-44-read-timing-knobs-missing-json-parse-reset.md,
    .planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md
  </files>
  <read_first>
    - `.planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md` — the folded todo, whose code
      change landed in plan 04. Read it to confirm what was folded before removing it.
    - Two existing pending todos, for the file format and front-matter conventions this project uses —
      e.g. `.planning/todos/pending/vcc-5500-high-margin-verify-rail-group.md` and
      `.planning/todos/pending/spike-databuffer-size-speed-delta.md`.
    - `149-CONTEXT.md` **D-04** (both deliverables, with the measured counts) and §`<deferred>` (the
      FRAM rows and the runtime INFO log).
    - `149-RESEARCH.md` §"D-01 Verification" — the four-way provenance table, the two FRAM rows with the
      3.3 V detail, and §"Open Questions" item 5 (the Phase 44 knobs' missing resets, with the
      recommendation not to fix them here).
    - `149-RESEARCH.md` §R2(d) — the measurement that `read_settling_us` and `read_strobe_us` are absent
      from `json_parse`'s reset block.
  </read_first>
  <action>
Working directory `/workspaces`.

**(a) Remove the folded todo.** `git rm .planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md`.
Its content shipped in plan 04: the definition and the declaration are deleted, and one stale comment in
`test_read_timing_params.cpp` was corrected. Note in the commit message that the measured flash saving
was **0 bytes** and was not counted toward this phase's exemption.

**(b) `promoted-0x0d-rows-keep-the-64-byte-floor.md`** — D-04 deliverable 1. Must carry the measured
numbers, not a summary: the 84 `algorithm: 13` rows split 18 upstream-native and 66 promoted by
`classify()` arm 2 (`build_db.py:371-386`); the four-way provenance table (`0x07` → 1×14, 16×1, 32×8,
64×22, 128×1, 256×1, total 47; `0x0B` → 1×17, 16×2, total 19; `0x0D` → 64×3, 128×15, total 18); the
promoted-row page-value breakdown — **31** at raw `1` (14 upstream `0x07`, 17 upstream `0x0B`), **8** at
32, **3** at 16, **1** at 256, **1** at 128; and the reason they keep the floor: a `page_size` read out
of a record filed under another algorithm is not evidence about a 28C page buffer. State that for the
**11** rows at 16/32 the floor's safety is **unproven**, not disproven, and that any future attempt needs
either datasheet curation per family or a new corroboration axis — neither of which `infoic.xml`
supplies, because `write_buffer_size`, `read_buffer_size` and `pages_per_block` were all measured not to
corroborate `page_size`. Record that extending `_PAGE_SIZE_BY_PART` or adding any per-chip guess table is
out of scope by `REQUIREMENTS.md` §Out of Scope / DATA-04.

**(c) `fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md`** — D-04 deliverable 2, deliberately a
**separate** todo because this is a classification question, not a page-size one. Name CYPRESS
`FM28V020` (upstream `0x07`, upstream page 128, `vcc_mv` 5000, `vpp_mv` 12000) and FUJITSU `MB85R256H`
(upstream `0x07`, upstream page 256, `vcc_mv` **3300**, `vpp_mv` 12000). Both are typed `EEPROM` and both
reach the `0x0D` handler by pinout promotion. State that FRAM has no page buffer and no internal write
cycle, which is why a delivered page size would be meaningless for them, and that this is the concrete
harm D-01's provenance rule prevents — the rule already excludes both, so this todo is about whether they
should be routed to that handler at all.

**(d) `runtime-info-log-naming-the-effective-page-size.md`** — the D-09 follow-up. A runtime INFO log
naming the page size the firmware actually used, so a future community `dev test` report can show its own
granularity. Declined in this phase on flash cost: a new message ID means an entry in
`firestarter/tools/catalog/messages.toml` plus a PROGMEM string, against a leonardo budget whose
MERGE-05 headroom was 0 B before this phase. Record the edit point as the catalog, **never**
`include/messages.h`, which is codegen-generated and ID-only with a CI drift gate. Tie it to the gh#21
re-run request: it is worth having when a reporter is asked for a fresh run.

**(e) `phase-44-read-timing-knobs-missing-json-parse-reset.md`** — research Open Question 5. Measured:
`read_settling_us` and `read_strobe_us` do **not** appear in `json_parse`'s optional-key reset block,
which is the same stale-value defect one field over from the one D-05 fixed for `page_size`. Both treat 0
as "use the default", so the symptom is a stale non-zero knob persisting into a later command rather than
a page overrun. Deliberately **not** fixed in Phase 149 because it would perturb
`test_read_timing_fields_default_zero_when_absent`, and because a defect in a neighbouring field is its
own change. Record the exact block and the two field names.

Each todo carries: what the finding is, how it was measured (with `file:line` citations), why it was
deferred, what would close it, and the phase and decision that filed it. None of them may claim anything
about silicon — every one of these is **software-proven and unvalidated on silicon** territory, and plan
08 does not scan `.planning/todos/`, so the discipline here is the author's.

Stage only these five paths. The meta working tree carries both submodule gitlinks, untracked `.claude/`
and `package*.json`; never `git add -A`.
  </action>
  <acceptance_criteria>
    - `.planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md` no longer exists, and the
      removal is a tracked deletion (`git log --diff-filter=D` shows it).
    - All four new todo files exist under `.planning/todos/pending/`.
    - `promoted-0x0d-rows-keep-the-64-byte-floor.md` contains the numbers `84`, `18`, `66`, `47`, `19`,
      `31`, `11`, `8`, `3`, `1`, `256`, `128`, and the word `unproven`, and cites
      `build_db.py:371-386`.
    - `fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md` contains `FM28V020`, `MB85R256H`,
      `3300` and `12000`, and the word `classification`.
    - `runtime-info-log-naming-the-effective-page-size.md` contains
      `firestarter/tools/catalog/messages.toml` and states that `include/messages.h` is
      codegen-generated and must not be edited directly.
    - `phase-44-read-timing-knobs-missing-json-parse-reset.md` contains `read_settling_us`,
      `read_strobe_us` and a `json_parser.c` line citation for the reset block.
    - Each of the four files contains a `file:line`-shaped citation and names Phase 149 and the decision
      that filed it.
    - No new todo contains the words `on silicon` in a claiming sense, and none claims any GitHub issue
      is closed, resolved or fixed.
    - `git -C /workspaces status --porcelain -- .planning/todos/` shows exactly five entries: four
      additions and one deletion, staged. (`.planning/` is meta content, so `-C /workspaces` here is
      about cwd-independence, not the submodule trap above.)
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib,re
T=pathlib.Path('.planning/todos/pending')
assert not (T/'remove-dead-json-init-sizeof-pointer-bug.md').exists(), 'folded todo not removed'
need={
 'promoted-0x0d-rows-keep-the-64-byte-floor.md':['84','18','66','47','19','31','11','unproven','build_db.py:371-386'],
 'fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md':['FM28V020','MB85R256H','3300','12000','classification'],
 'runtime-info-log-naming-the-effective-page-size.md':['firestarter/tools/catalog/messages.toml','codegen'],
 'phase-44-read-timing-knobs-missing-json-parse-reset.md':['read_settling_us','read_strobe_us','json_parser.c'],
}
for name,items in need.items():
    p=T/name
    assert p.is_file(), 'missing todo '+name
    t=p.read_text()
    for it in items:
        assert it in t, '%s missing %r'%(name,it)
    assert re.search(r'[\w./]+\.(py|c|cpp|h|json|md):\d+', t), name+': no file:line citation'
    assert '149' in t, name+': does not name the phase'
    low=t.lower()
    for bad in ['gh#21 closed','gh#21 is closed','issue closed','now works']:
        assert bad not in low, '%s contains a forbidden claim shape: %r'%(name,bad)
print('OK four todos filed with measured lists and citations, folded todo removed')
" && git status --porcelain .planning/todos/ && echo "149-07-T2 OK"</automated>
  </verify>
  <done>The folded todo is removed as a tracked deletion, and four new pending todos carry the measured
  provenance table and part-count breakdown, the two FRAM rows with the 3.3 V detail as a classification
  question, the deferred runtime INFO log with its catalog edit point, and the Phase 44 knobs' own
  missing resets — each with `file:line` citations and no claim about silicon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a committed measurement transcript → the baseline file the next phase measures against | the baseline is the reference every future flash claim is judged by; a warm or mistyped figure poisons the whole chain |
| the baseline's recorded provenance → the figures it reports | the file has already shipped once with a tree SHA that predated its own numbers |
| a deferred finding → whoever picks it up | a todo that summarises instead of recording the measurement forces a re-measurement, and a re-measurement is where a wrong number enters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-149-43 | Repudiation | a warm figure written into the live baseline | critical | mitigate | this plan runs no build at all; every figure is transcribed from plan 06's committed cold logs and the verify re-parses those logs and compares byte for byte |
| T-149-44 | Repudiation | re-shipping the stale `firmware_tree_sha` defect | high | mitigate | the acceptance criteria assert the field is not the stale value, is a 40-char hex, resolves to a real `tree` object, and that `generated_by` records the correction naming `6cc4795` |
| T-149-45 | Tampering | lowering a watermark to a figure measured in one session | high | mitigate | the verify asserts every native watermark is `>= ` its current value and the AVR figures are unchanged; a below-watermark cold reading is INFO, not a licence to lower |
| T-149-46 | Tampering | bumping one pinned native env and not the other, silently breaking `envs_agree` | medium | mitigate | the verify asserts the two envs' `cases` and `succeeded` are equal, `suites` is 17 on both, `envs_agree` is true, and `envs_agree_note` no longer quotes the old figure |
| T-149-47 | Tampering | quietly widening a band or re-anchoring BASE-01 while editing the neighbouring file | high | mitigate | `git diff --quiet` on `check_size_baseline.py`, `size_baseline_base01.json`, `tests/` and `src`/`include` are all acceptance criteria |
| T-149-48 | Repudiation | a deferred todo that overstates what is known about the promoted rows | medium | mitigate | the todo must say **unproven** rather than disproven for the 11 rows at 16/32, carry the four-way table and the measured breakdown, and record that no `infoic.xml` attribute corroborates `page_size` |
| T-149-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs; this plan runs no build and no test framework beyond the two gate scripts |
</threat_model>

<verification>
Plan gate, from `/workspaces/firestarter`, **after committing**:

    python3 scripts/check_size_baseline.py --baseline scripts/baseline/size_baseline.json --avr-log … ; echo EXIT=$?
    python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log … ; echo EXIT=$?
    python3 -m pytest tests/ -q
    git diff --quiet scripts/check_size_baseline.py scripts/baseline/size_baseline_base01.json
    git diff --quiet src include tests

And from `/workspaces`:

    git status --porcelain .planning/todos/

This plan runs **no** `pio` command. A re-measurement here would be warm and a warm figure written into
`size_baseline.json` is the exact failure mode `meta.warm_vs_cold_correction` documents.

Record-gate note: allow **300 s** for any `gsd-tools` record write. After every
`gsd-tools query commit`, check `HEAD` in both `firestarter` and the meta repo. Snapshot `ROADMAP.md`
before and after any `phase.complete` — it has clobbered an unrelated phase's `**Plans:**` line before.
</verification>

<success_criteria>
- `size_baseline.json`'s AVR figures match plan 06's cold transcripts exactly, with free figures
  recomputed and every total unchanged.
- `meta.firmware_tree_sha` is corrected to a real tree object and the stale Phase-144 value is recorded
  as corrected, so the X-3 defect does not re-ship.
- Both pinned native envs are bumped by the same amount, `suites` stays 17, `envs_agree` stays true, and
  `envs_agree_note`'s prose matches the new numbers.
- No watermark is lowered; no band, exemption constant, fixture or firmware source is touched.
- Both size gates — default byte-identity against the updated baseline and `--policy merge05` against
  BASE-01 — exit 0 simultaneously.
- Four pending todos carry the measured lists and citations; the folded todo is removed as a tracked
  deletion.
- No requirement checkbox is flipped by this plan. **Plan 08 flips all five of PGSZ-01…PGSZ-05.**
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

New files created by **this plan**:

| Artifact | Kind |
|---|---|
| `.planning/todos/pending/promoted-0x0d-rows-keep-the-64-byte-floor.md` | D-04 deliverable 1 |
| `.planning/todos/pending/fram-parts-ride-the-0x0d-handler-by-pinout-promotion.md` | D-04 deliverable 2 |
| `.planning/todos/pending/runtime-info-log-naming-the-effective-page-size.md` | D-09 follow-up |
| `.planning/todos/pending/phase-44-read-timing-knobs-missing-json-parse-reset.md` | research Open Question 5 |

`firestarter/scripts/baseline/size_baseline.json` is an **existing** file updated in place per D-14, not
a new artifact. Removed by this plan:
`.planning/todos/pending/remove-dead-json-init-sizeof-pointer-bug.md`. The full phase-wide new-symbol
surface is enumerated in `149-01-PLAN.md` §"Artifacts this phase produces".
</artifacts_produced>

<output>
Create `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-07-SUMMARY.md` when done.

Do not quote any planted overclaim text in the SUMMARY. Plan 08 extends the claim gate's target list to
every `149-*-SUMMARY.md`, including this one.
</output>
