---
phase: 151-protection-readability-lock-status
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: [meta]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/151-protection-readability-lock-status/151-DESIGN.md
autonomous: true
requirements: []
advances: [LOCK-02, LOCK-04]

must_haves:
  truths:
    - "Every planning artifact that names this phase's command surface names the beta-only `dev lock-status` form, and none names a top-level form."
    - "Every planning artifact that counts v1.32's firmware-touching workstreams says two, not one."
    - "A reader of any downstream plan can find the phase's discretionary design decisions — wire frame shape, exit-code map, corrected class census, the C-17 tiebreak rule — in exactly one committed file."
  artifacts:
    - .planning/phases/151-protection-readability-lock-status/151-DESIGN.md
  key_links:
    - "Phase 152's OUT-01/OUT-04/OUT-05 read their outward-facing claims from ROADMAP §Phase 151 and REQUIREMENTS §LOCK — this plan is the only place those texts are corrected."
    - "Plans 151-03, 151-06, 151-08, 151-11, 151-12, 151-13 all cite 151-DESIGN.md rather than re-deciding."
---

<objective>
Correct every pre-discuss planning text that still describes this phase's command as a
top-level `firestarter` command, correct the two sentences that still call v1.32 a
one-firmware-workstream milestone, and land the phase's discretionary design decisions as
one citable artifact so no downstream plan re-decides them.

Purpose: OD-1. Phase 152's OUT-01, OUT-04 and OUT-05 derive their outward-facing text from
ROADMAP §"Phase 151" and REQUIREMENTS §LOCK. If those still promise a top-level command,
Phase 152 will announce a command that does not exist on a stable install — the exact
overclaim shape v1.30 had to amend its own gh#12 reply to avoid. Per D-01 the surface is
beta-only `dev lock-status`, settled at discuss time (151-DISCUSSION-LOG.md:20 shows the
beta-only option selected, `:22` records that the host-only recommendation was overruled
deliberately, `:113` states the surface "was already settled as beta-only `dev lock-status`
in area 1"). RESEARCH C-2 confirms zero `lock-status` hits anywhere in code, so nothing
else settles it.

Output: five amended text sites, two amended workstream-count sentences, and a new
`151-DESIGN.md`.

**Requirement flips owned by this plan: none.** It advances LOCK-02 and LOCK-04 by fixing
the text they are graded against; the checkbox flips belong to `151-13`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-protection-readability-lock-status/151-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the five top-level-command sites and the two workstream-count sentences</name>
  <read_first>
    - `.planning/ROADMAP.md` lines 35-40 (the v1.32 milestone-index entry), 160-192 (the
      sequencing spine and the phase checkbox list), 365-380 (§"Phase 151" detail and its
      Success Criteria)
    - `.planning/REQUIREMENTS.md` lines 210-245 (§"Protection Readability — `lock-status` (LOCK)")
    - `.planning/STATE.md` line 110 (the One-writer-per-file paragraph)
    - `.planning/phases/151-protection-readability-lock-status/151-CONTEXT.md` §Implementation
      Decisions, D-01 (the beta-only surface and the `_DevGroup` / `channel.BETA_ONLY_DEV_COMMANDS`
      gate it rides)
    - `.planning/phases/151-protection-readability-lock-status/151-RESEARCH.md` §"C-2" and §"C-3"
  </read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Hand-edit each site below with the Edit tool, one scoped replacement per site. Do NOT
    use `gsd-tools query requirements.*` or `roadmap.*` write verbs on these files — those
    run `_normalizeMd` and reformat the whole document, producing an unreviewable diff.

    OD-1 named four sites. A fifth exists, measured: `ROADMAP.md:176`. Five sites total, all
    carrying the same pre-discuss text defect. Amend all five:

    1. `.planning/REQUIREMENTS.md:229` — LOCK-02. Replace the top-level command form with
       the beta-only `dev` subcommand form, and append a bracketed amendment note in the
       style REQUIREMENTS.md already uses for re-homed requirements: state that the surface
       was settled as beta-only at `/gsd-discuss-phase 151` on 2026-08-20 per CONTEXT D-01,
       cite `151-DISCUSSION-LOG.md:20` and `:22`, and state that it is registered only on a
       pre-release install via the `_DevGroup` / `channel.BETA_ONLY_DEV_COMMANDS` gate so a
       stable install never sees it. Do not alter LOCK-02's substance (what it must report).
    2. `.planning/ROADMAP.md:189` — the Phase 151 checkbox line. Same substitution, same
       dated rationale, compressed to one parenthetical.
    3. `.planning/ROADMAP.md:372` — §"Phase 151" Success Criterion 2. Same substitution. Add
       the dated correction note inline, in the `*(Corrected YYYY-MM-DD per …)*` style that
       criteria 1 and 4 of Phase 147 and criterion 1 of Phase 148 already use in this file.
    4. `.planning/ROADMAP.md:176` — the One-writer-per-file sequencing-spine bullet. The
       parenthetical describing the new registration must say the registration is a new
       `@dev.command` block inside `cli_handlers.py`'s existing `if _DEV_TOOLS_ENABLED:`
       module-level gate, not a top-level command registration. Keep the surrounding
       one-writer-per-file claim intact — it is still true.
    5. `.planning/STATE.md:110` — the same parenthetical, same correction. STATE.md's
       `current_phase` / `current_phase_name` frontmatter keys are hand-edit-only; do not
       touch frontmatter in this task.

    Then C-3, two sites, both claiming v1.32 has one firmware-touching workstream:
    6. `.planning/ROADMAP.md:37` — the v1.32 milestone-index entry parenthetical.
    7. `.planning/ROADMAP.md:163` — the "Mostly host-side; exactly one firmware-touching
       workstream" sequencing paragraph.
       At both sites: state two firmware-touching workstreams — Phase 149 (the page-size
       seam) and Phase 151 (the protection read) — both dual-repo lockstep, and add a dated
       amendment marker noting 151's firmware scope was established by CONTEXT D-01 on
       2026-08-20. Per CONTEXT.md §Phase Boundary this sentence is explicitly out of date and
       Phase 152's outward-facing text must not repeat it, so the correction is load-bearing
       downstream, not cosmetic.

    Before editing, snapshot `.planning/ROADMAP.md` to the scratch dir and diff after, then
    confirm the diff touches only the seven intended line regions. `phase.complete` has
    clobbered an unrelated phase's `**Plans:**` line in this repo before; a hand-edit pass
    over the same file deserves the same check.

    Do not change any RELOCK requirement, do not touch the §Out of Scope table, and do not
    describe `write --sdp-relock` as anything other than deferred to Backlog 999.28.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(grep -c 'firestarter lock-status' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md | awk -F: '{s+=$2} END {print s}')" = "0"</automated>
    <automated>cd /workspaces && test "$(grep -c 'dev lock-status' .planning/ROADMAP.md)" -ge 3 && grep -q 'dev lock-status' .planning/REQUIREMENTS.md && grep -q 'dev lock-status' .planning/STATE.md</automated>
    <automated>cd /workspaces && test "$(grep -c 'exactly one firmware-touching workstream' .planning/ROADMAP.md)" = "0"</automated>
    <automated>cd /workspaces && git diff --stat .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'firestarter lock-status'` summed over `ROADMAP.md`, `REQUIREMENTS.md` and
      `STATE.md` is exactly `0`, where it is `5` before this task.
    - `dev lock-status` appears at ROADMAP lines 176, 189 and 372, at REQUIREMENTS LOCK-02,
      and in the STATE.md One-writer-per-file paragraph.
    - `grep -c 'exactly one firmware-touching workstream' .planning/ROADMAP.md` is `0`, and
      both `:37` and the amended `:163` paragraph name Phase 149 **and** Phase 151 as the two
      firmware-touching workstreams.
    - Each of the seven amended regions carries a dated amendment or correction marker naming
      2026-08-20 and CONTEXT D-01 as the authority.
    - `git diff --stat` on `.planning/ROADMAP.md` shows a change count consistent with seven
      scoped line-region edits, not a whole-file reflow: the diff contains no hunk that
      rewrites a phase entry other than 151, and `**Plans:**` lines for phases 147, 148, 149,
      150 and 152 are byte-identical to their pre-task state.
    - REQUIREMENTS.md's LOCK-02 checkbox is still `[ ]` — this task amends text, it does not
      complete a requirement.
  </acceptance_criteria>
  <done>All five command-surface sites name the beta-only `dev lock-status` form with a dated
  rationale; both workstream-count sentences say two; the ROADMAP diff is scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Land 151-DESIGN.md — the discretionary decisions, decided once</name>
  <read_first>
    - `.planning/phases/151-protection-readability-lock-status/151-CONTEXT.md`
      §"Claude's Discretion" (the five items the planner owns) and D-08/D-09/D-10
    - `.planning/phases/151-protection-readability-lock-status/151-RESEARCH.md`
      §"Response framing options" (the three framing options and their measured costs),
      §"Does a new message ID require codegen?" (band occupancy), §"D-09's claimed partition
      — verified, with one hole" (the `0x34` orphan row, quoted in full), §"C-17" and §"C-18"
    - `.planning/phases/151-protection-readability-lock-status/151-VALIDATION.md`
      §"Unsatisfiable Criteria"
    - `firestarter/include/logging_id.h` lines 155-215 (the DATA and OK emitter macros that
      exist — `LOG_DATA_ID_BYTES`, `LOG_DATA_ID_U8`, `LOG_OK_ID_U8_U8`)
  </read_first>
  <files>.planning/phases/151-protection-readability-lock-status/151-DESIGN.md</files>
  <action>
    Create `151-DESIGN.md` recording the decisions below verbatim as the phase's single
    source of truth. Downstream plans cite this file; none of them may re-decide these.
    Each section states the decision, the measured facts it rests on, and the alternatives
    rejected — the same shape CONTEXT.md's decision blocks use.

    **§1 Wire shape (CONTEXT.md discretion item 1).** New command `CMD_LOCK_STATUS = 16`,
    host mirror `COMMAND_LOCK_STATUS = 16` plus a `COMMAND_NAMES[16]` entry. Response is a
    two-byte DATA id-frame emitted with the already-existing `LOG_DATA_ID_BYTES` macro under
    one new catalog id `MSG_DATA_PROTECTION_STATUS`, allocated in the DATA band (0xE0–0xFF,
    26 free ids measured). Byte 0 is the raw byte read from silicon, unmodified. Byte 1 is a
    firmware decode code: `0x00` reads-as-unprotected, `0x01` reads-as-protected, `0xFF`
    indeterminate/not-obtainable — reusing `hw_get_version`'s established `0xFF` sentinel
    convention. Record why the raw byte is on the wire: D-03's probe legs must record the
    raw result either way, so a wrong decode must not destroy the observation.
    Rejected, each with its measured reason: extending `MSG_OK_READY` (zero codegen, but it
    is the operation-setup ack emitted on every operation and every voltage read, so every
    command would pay the bytes and every ack would carry a protection claim);
    `LOG_DATA_ID_U16x4` per-region framing (there is no sector map anywhere in this project —
    `flash_nor_unlock_sector_erase` takes a caller-supplied address — so a per-region answer
    has no data source); a new ERROR-band id (the ERROR band has exactly one free id, `0xBF`,
    and no documented band-extension procedure, so it is not spent here).

    **§2 Scope of the answer (discretion item 2).** Device-global, not per-sector or
    per-region. Two measured reasons: no sector map exists in this project; and
    `_BOOT_BLOCK_SIZE = 0x4000` was derived for the W29C040 hint while
    `lockable-proms.md:21` documents W29C020/W29C020C as having 8 KB boot blocks, so reusing
    that constant on a `0x05` part would be a fabricated geometry claim. A device-global
    answer needs no geometry at all. Under D-08 a device-global answer renders as one leading
    class token with no multi-region reconciliation to specify.

    **§3 Exit-code map (D-10).** A literal `str -> int` dict, never a `max()` over
    severities — this codebase already carries an exit-code precedence defect where `max()`
    picked the wrong verdict. Four codes for eight classes:
    `protected` 0 · `unprotected` 0 · `not_readable` 2 · `not_implemented` 2 ·
    `undocumented_alias` 2 · `no_mechanism` 2 · `firmware_outdated` 3 · comms failure 3 ·
    `unadjudicated_probe` 4.
    State the reasoning for the fourth code: D-10 assigns 0 to a real state, one non-zero to
    "cannot answer" and a separate non-zero to operational failure, and does not place
    `unadjudicated_probe` in any of the three. It is not a state (D-07: never a state claim),
    not a refusal (the sequence ran), and not a failure. A fourth band is therefore the
    honest reading; D-10's rejected alternative was per-class codes throughout, which four
    codes over eight classes is not. Record that tests assert token and code together, never
    the code alone.

    **§4 The corrected class census (OD-2).** `0x34` — the single `XICOR/X88C64P,X88C64S`
    row, `algorithm: 52`, `support_status: "protocol-not-implemented"`,
    `protect_off_before: true` — resolves to `not_implemented`. Reasoning: D-09's seven
    named no-mechanism algorithms sum to 405, not 406, so this row lands in no class today;
    classing it `no_mechanism` would assert an absence of mechanism that upstream's
    `protect_off_before: true` contradicts, which is the fabricated claim LOCK-03/LOCK-04
    forbid; `not_implemented` is honest — no code in this project can read this chip's
    protection state — and preserves D-09's 8-class freeze without inventing a ninth.
    Consequence to pin as literals in the D-12 test, and this supersedes VALIDATION.md's
    figure: `not_implemented` = **40** (39 rows at `0x10` plus this one), not 39.
    Full census to pin: `no_mechanism` 405 · `not_implemented` 40 · `not_readable` at least
    84 (the `0x0D` rows) · the 217 `0x05`+`0x06` rows distributed across read-permitted /
    `not_readable` / `undocumented_alias` with the split fixed by the curation in `151-02`.
    405 + 40 + 84 + 217 = 746.

    **§5 The C-17 tiebreak — a mechanism, not a judgement.** `lockable-proms.md`'s row key at
    `:21` is `**W29C020 / W29C020C**` and covers both parts, but all four restatements
    outside the table (`:25`, `:30`, `:335`, `:350`) name `W29C020C` only, and bare
    `W29C020` appears exactly once in 399 lines. Neither reading is safe and D-06's three
    states have no slot for "documented, but the summary declines to repeat the verdict".
    The mechanism, stated as a rule rather than resolved per entry:
    (a) D-06's three readability states are NOT extended — the token set stays
    `documented-readable` / `documented-not-readable` / `undocumented`;
    (b) a named module-level tiebreak rule applies: where the source document's table row and
    its restatements disagree about a token, the token takes the **more restrictive** state;
    (c) the disagreement itself is recorded, not erased, in a reporting-only
    `AMBIGUOUS_DOC_CITATIONS` mapping whose value carries both readings' exact line
    references, and the refusal reason surfaces it;
    (d) a dedicated test leg asserts the mapping is non-empty and that bare `W29C020` is one
    of its keys.
    So bare `W29C020` curates to `documented-not-readable` **by rule**, not by a curator's
    adjudication. Record the fact that makes this consequence-free for the worked example:
    the `W29C020,W29C020C,W29C022` entry refuses under D-06 regardless of how C-17 resolves,
    because `W29C022` appears nowhere in the document — so the tiebreak changes the *number
    of named offending aliases* in the refusal, never the entry's verdict. Also record C-18:
    all three aliases are one upstream `<ic>` with one `chip_id 0x0000da45`, so a per-alias
    distinction is unobservable on the wire anyway.

    **§6 `--force` does not reach the wire (C-16).** D-07's `--force` is a host-side bypass
    of a **table** refusal. Firmware's `FLAG_FORCE` means one specific thing — downgrade a
    chip-ID mismatch from error to warning — and the lock-status read performs no chip-ID
    check, so the bit is not set on this command's frame. State the consequence plainly: on
    this command `--force` has no firmware-visible effect whatsoever, and a test asserts the
    flags word sent for `lock-status` is byte-identical with and without `--force`.

    **§7 The debug-output consequence of OD-3, stated as a choice.** The second ordinal range
    at `firestarter/src/firestarter.cpp:132-142` gates three `DBG_*` diagnostic lines and its
    own comment records that it was deliberately not converted to `is_memory_cmd()` because
    it "gates diagnostic output only". It is left unchanged. Consequence, chosen rather than
    discovered on the bench: `dev lock-status` emits **no** `DBG_*` diagnostic output. Record
    that the admission set at the safety gate grew from 8 commands to 9 while the diagnostic
    range did not.

    **§8 The evidence ceiling for this phase, in the words later artifacts must reuse.**
    Both sequences are datasheet-derived; the strongest available test is a pinned literal
    byte table plus a citation comment, which is **a change detector, not a correctness
    proof**. The `0x06` Autoselect read ships **software-proven and unrun on silicon** — no
    bench leg for it exists. The `0x05` read is exercised only through D-07's `--force` probe
    path and no artifact may claim the sequence is silicon-validated. Nothing here claims
    AT28C or `0x0D` silicon validation, and nothing closes the v1.17 W29C040 RCA, which asked
    for a second W29C040 sample.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/151-protection-readability-lock-status/151-DESIGN.md && for s in 'CMD_LOCK_STATUS = 16' 'MSG_DATA_PROTECTION_STATUS' 'LOG_DATA_ID_BYTES' 'not_implemented' 'AMBIGUOUS_DOC_CITATIONS' 'change detector, not a correctness proof' 'software-proven and unrun on silicon'; do grep -qF "$s" .planning/phases/151-protection-readability-lock-status/151-DESIGN.md || { echo "MISSING: $s"; exit 1; }; done</automated>
    <automated>cd /workspaces && grep -qE '^\| *`?unadjudicated_probe`? *\|? *4|unadjudicated_probe.*\b4\b' .planning/phases/151-protection-readability-lock-status/151-DESIGN.md</automated>
    <automated>cd /workspaces && grep -c '405' .planning/phases/151-protection-readability-lock-status/151-DESIGN.md && grep -q '746' .planning/phases/151-protection-readability-lock-status/151-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `151-DESIGN.md` exists and has eight numbered sections matching §1…§8 above.
    - §1 names `CMD_LOCK_STATUS = 16`, `COMMAND_LOCK_STATUS = 16`, a `COMMAND_NAMES` entry,
      `MSG_DATA_PROTECTION_STATUS` in the DATA band, and `LOG_DATA_ID_BYTES` — and states
      byte 0 is the unmodified raw silicon byte and byte 1 the decode code with `0xFF` as the
      indeterminate sentinel.
    - §1 names all three rejected framings with their measured reasons, including that the
      ERROR band has exactly one free id.
    - §3 lists all eight class tokens with an integer exit code each, uses exactly four
      distinct codes {0, 2, 3, 4}, assigns 0 to exactly `protected` and `unprotected`, and
      states the map is a literal dict rather than a `max()` over severities.
    - §4 states `not_implemented` = 40 and explicitly says this supersedes VALIDATION.md's
      figure of 39; the arithmetic 405 + 40 + 84 + 217 = 746 appears.
    - §5 states the four-part mechanism (a)–(d), names `AMBIGUOUS_DOC_CITATIONS`, and states
      that the `W29C020,W29C020C,W29C022` entry's verdict is unchanged by the tiebreak.
    - §6 states the flags word is identical with and without `--force` on this command.
    - §7 states that `dev lock-status` emits no `DBG_*` output and that the admission set grew
      from 8 to 9 while the diagnostic ordinal range did not.
    - §8 contains the literal phrases `change detector, not a correctness proof` and
      `software-proven and unrun on silicon`.
    - No section asserts that either sequence is correct or validated.
  </acceptance_criteria>
  <done>`151-DESIGN.md` is committed and every downstream plan can cite §1…§8 instead of
  re-deciding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning text → Phase 152 outward-facing posts | Phase 152 publishes GitHub comments and release notes derived from these texts; an uncorrected overclaim here becomes a public overclaim there. |
| planner decision → executor implementation | An undecided discretionary item becomes an ad-hoc decision inside an executor, unreviewed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-01 | Repudiation | `.planning/ROADMAP.md` §Phase 151 SC2 | high | mitigate | Task 1 amends the criterion and stamps it with a dated correction marker citing D-01, so the change is attributable rather than a silent rewrite. |
| T-151-02 | Information Disclosure | Phase 152 outward-facing text | high | mitigate | Task 1 removes the top-level command promise from all five sites so OUT-01/OUT-04 cannot inherit it; OUT-05's claim gate then has nothing to catch. |
| T-151-03 | Tampering | `.planning/ROADMAP.md` whole-file rewrite | high | mitigate | Hand-edits only, `gsd-tools` write verbs forbidden in Task 1's action, plus a snapshot/diff check and an acceptance criterion pinning other phases' `**Plans:**` lines byte-identical. |
| T-151-04 | Elevation of Privilege | none — no code path is touched | low | accept | This plan modifies only markdown under `.planning/`; no executable surface exists to elevate through. |
| T-151-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is owed. |
</threat_model>

<verification>
- `grep -c 'firestarter lock-status'` over the three meta files sums to `0`.
- `grep -c 'exactly one firmware-touching workstream' .planning/ROADMAP.md` is `0`.
- `151-DESIGN.md` exists and passes all eight section checks in Task 2's acceptance criteria.
- `git -C /workspaces diff --stat HEAD~1` shows only the four files in `files_modified`.
</verification>

<success_criteria>
- Five command-surface sites and two workstream-count sentences amended, each with a dated
  marker naming CONTEXT D-01 and 2026-08-20.
- `151-DESIGN.md` committed with §1…§8 complete.
- No requirement checkbox flipped.
- The ROADMAP diff touches no phase entry other than 151 and no other phase's `**Plans:**`
  line.
</success_criteria>

<output>
Create `.planning/phases/151-protection-readability-lock-status/151-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

This plan's contributions: `.planning/phases/151-protection-readability-lock-status/151-DESIGN.md`
(new), and amended regions in `.planning/ROADMAP.md` (:37, :163, :176, :189, :372),
`.planning/REQUIREMENTS.md` (LOCK-02, :229) and `.planning/STATE.md` (:110).

The **complete phase-wide artifact inventory** — every new symbol, constant, file, class
token and doc heading Phase 151 creates — is `## Artifacts Phase 151 produces` at the foot
of this file. Every other plan in this phase points here.

## Artifacts Phase 151 produces

**New host modules and symbols** (`firestarter_app/firestarter/`)
- `protection_readability.py` — `READABILITY_STATES` (3-tuple),
  `DOCUMENTED_READABLE_TOKENS` (frozenset), `DOCUMENTED_NOT_READABLE_TOKENS` (frozenset),
  `AMBIGUOUS_DOC_CITATIONS` (mapping), `MECHANISM_BY_TOKEN` (mapping),
  `PERMANENCE_BY_TOKEN` (mapping), `REASON_NOT_FOUND`, `REASON_NO_MECHANISM`,
  `REASON_NOT_IMPLEMENTED`, `REASON_NOT_READABLE`, `REASON_UNDOCUMENTED_ALIAS`,
  `REASON_READ_PERMITTED`, `GATE_TOKEN_READ_PERMITTED`, `readability_for_token()`,
  `protection_gate_for_entry()`
- `lock_status.py` — `PROTECTION_CLASSES` (frozen 8-tuple), `SILICON_ONLY_TOKENS`,
  `EXIT_BY_CLASS` (literal dict), `DECODE_UNPROTECTED`/`DECODE_PROTECTED`/`DECODE_INDETERMINATE`,
  `classify_protection_response()`, `exit_code_for_class()`, `render_lock_status()`
- `sdp_honesty.py` — additive only: `map_unknown_cmd_to_outdated_for_operation()`
- `constants.py` — `COMMAND_LOCK_STATUS = 16` and its `COMMAND_NAMES` entry
- `eprom_operations.py` — `read_protection_status()`
- `cli_handlers.py` — the `dev_lock_status` command registered as `dev lock-status`, with
  `--force`
- `channel.py` — `"lock-status"` appended to `BETA_ONLY_DEV_COMMANDS` (6-tuple → 7-tuple)

**The eight class tokens (D-09, frozen)**
`protected` · `unprotected` · `not_readable` · `not_implemented` · `undocumented_alias` ·
`no_mechanism` · `firmware_outdated` · `unadjudicated_probe`
plus the internal gate token `read_permitted`, which is never printed.

**New host tooling and tests** (`firestarter_app/`)
- `tools/check_protection_readability_invariants.py` — the AST gate, env seam
  `FIRESTARTER_PROTECTION_READABILITY_SRC`
- `tests/test_check_protection_readability.py`
- `tests/fixtures/planted_protection_permit_by_default.py`
- `tests/fixtures/planted_protection_widenable_tokenset.py`
- `tests/test_protection_table_citations.py`
- `tests/test_protection_resolution.py`
- `tests/test_lock_status_class_partition.py` — the D-12 invariant
- `tests/test_lock_status_wire.py`
- `tests/test_lock_status_cli.py`
- `tests/test_protect_flags_doc_measurements.py` — DATA-06's proof

**New firmware symbols** (`firestarter/`)
- `include/firestarter.h` — `CMD_LOCK_STATUS 16`, a ninth `case` in `is_memory_cmd()`
- `include/flash_utils.h` — `FLASH_READ_PROTECTION_5V_PAGE`, `FLASH_READ_PROTECTION_NOR`
  (pinned `byte_flip_t` tables), `flash_util_read_in_id_mode()`
- `src/proms/flash_utils.cpp` — `flash_util_read_in_id_mode()` definition;
  `flash_util_get_chip_id()` re-expressed on top of it
- `src/proms/flash_nor_unlock.cpp` — `flash_nor_unlock_read_protection_execute()` and its
  `CMD_LOCK_STATUS` dispatch arm
- `src/proms/flash_5v_page.cpp` — `flash_5v_page_read_protection_execute()` and its
  `CMD_LOCK_STATUS` dispatch arm
- `src/firestarter.cpp` — the widened parse gate at `:77` and a `CMD_LOCK_STATUS` arm in
  `loop()`
- `scripts/check_size_baseline.py` — `MERGE05_LOCK_STATUS_READ_EXEMPTION_BYTES` (flash) and
  `MERGE05_LOCK_STATUS_READ_RAM_EXEMPTION_BYTES` (RAM)

**New catalog id** (meta `tools/catalog/messages.toml`, propagated to both sub-repos)
- `MSG_DATA_PROTECTION_STATUS` — DATA band, two-byte payload

**New fixture family** (`firestarter/tests/fixtures/`)
- `captured_build_v151_{uno,uno328pb,leonardo}.log`
- `merge05_base01_anchor_v151_{uno,uno328pb,leonardo}.log`
- `merge05_lock_status_v151_{uno,uno328pb,leonardo}.log`
- `planted_size_baseline_policy_{uno_over_band,leonardo_growth,ram_moved}_v151.log`
- `planted_size_baseline_flash_regression_v151.log`

**New doc headings** (`firestarter_app/doc/`)
- `infoic-field-dictionary.md` — `### \`protect_off_before\` / \`protect_on_after\` (flags bits 14/15) — CONFIRMED decode, NO runtime consumer`
- `package-details.md` and `protocol-flags.md` — one italic pointer line under each bit table

**New meta artifacts** (`.planning/phases/151-protection-readability-lock-status/`)
- `151-DESIGN.md`
- `151-SEQUENCES.md`
- `151-SIZE-TRANSCRIPTS.md`
- `151-BENCH.md`
</content>
</invoke>
