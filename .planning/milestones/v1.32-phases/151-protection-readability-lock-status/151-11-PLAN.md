---
phase: 151-protection-readability-lock-status
plan: 11
type: execute
wave: 3
depends_on: [151-03, 151-05, 151-06]
commits_land_in: [firestarter_app]
files_modified:
  - firestarter_app/firestarter/lock_status.py
  - firestarter_app/firestarter/sdp_honesty.py
  - firestarter_app/firestarter/eprom_operations.py
  - firestarter_app/tests/test_lock_status_wire.py
  - firestarter_app/tests/test_sdp_honesty.py
autonomous: true
requirements: []
advances: [LOCK-02, LOCK-03, LOCK-04]

must_haves:
  truths:
    - "The two silicon-only class tokens are produced by exactly one function, and that function's signature requires a device response — so neither token can be produced by a path that did not read silicon."
    - "Every one of D-09's eight class tokens maps to an integer exit code through one literal dict, and exit 0 is reachable only from the two silicon-only tokens."
    - "Firmware predating the command produces `firmware_outdated`, keyed on the message id and never on message text."
    - "The `not_readable` caveat sentence is composed by calling the existing helper, never re-authored, so its seven pinning sites and two production surfaces are undisturbed."
  artifacts:
    - firestarter_app/firestarter/lock_status.py
    - firestarter_app/tests/test_lock_status_wire.py
  key_links:
    - "`sdp_honesty.unreadable_state_caveat()` has three landed production callers (`cli_handlers.py:2405`, `:2412`, `chip_test.py:1480`) plus four pinning tests in `test_chip_test_sdp_leg.py`. Editing its text breaks seven pinning sites and two production surfaces — the extension here is strictly additive."
    - "A frame-level harness already exists: `tests/conftest.py`'s `build_frame(msg_id, params)`, `_FakeSerial` with `.feed()`, and the `make_comm` factory fixture which bypasses `SerialCommunicator.__init__` via `__new__`. No new harness is needed."
---

<objective>
Author the response-consuming half of the pure/impure split: the only function in the codebase
that may produce a state claim, its exit-code map, the additive honesty extension, the operator
transport method, and a frame-level wire test.

Purpose: LOCK-02, LOCK-03, LOCK-04, D-04, D-08, D-09, D-10, D-11. `PATTERNS.md` records this
split as having **no analog** — `sdp_capability.py` is pure-only and `sdp_honesty.py` is
prose-only, so no two-function pure/impure precedent exists. The asymmetry is what makes plan
`151-09`'s AST leg non-decorative: the pure module's signature accepts no device response, so
the two state tokens are producible only here.

C-4 inverts D-11's premise and constrains the extension. D-11 assumed both of
`sdp_honesty.py`'s forward callers were deferred, making `lock-status` the first to land.
Measured: `unreadable_state_caveat()` already has **three landed production callers** —
`cli_handlers.py:2405`, `cli_handlers.py:2409` and `chip_test.py:1480` — plus four pinning
tests. Only `emission_summary()` and `map_unknown_cmd_to_outdated()` are callerless. So the
extension is **strictly additive**: no existing function's text changes.

Output: `lock_status.py`, an additive sibling in `sdp_honesty.py`,
`EpromOperator.read_protection_status`, and `tests/test_lock_status_wire.py`.

**Requirement flips owned by this plan: none.** It advances all three LOCK requirements; they
flip at `151-13`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-protection-readability-lock-status/151-CONTEXT.md
@.planning/phases/151-protection-readability-lock-status/151-DESIGN.md
@firestarter_app/firestarter/sdp_honesty.py
@firestarter_app/firestarter/protection_readability.py
</context>

<!-- planner-discipline-allow: protected -->
<!-- planner-discipline-allow: unprotected -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `lock_status.py` — the response-consuming classifier, the exit-code map, and the composed prose</name>
  <read_first>
    - `firestarter_app/firestarter/sdp_honesty.py` in full, 92 lines — `unreadable_state_caveat()`
      at `:33-42` returning the caveat clause **alone** with no preamble; `emission_summary()` at
      `:45-64` which **composes** it by calling it rather than duplicating (`:63`) and uppercases
      `chip_name` itself; `map_unknown_cmd_to_outdated()` at `:67-92` which keys on
      `exc.error_code != MSG_ERR_UNKNOWN_CMD`, **returns** a constructed
      `FirmwareOutdatedError` rather than raising so the caller owns `raise ... from exc`, and
      ends its message with `"upgrade with 'firestarter fw --install'."`; and the import-set
      invariant comment at `:22-28` recording that there is deliberately no `click`
    - `firestarter_app/firestarter/protection_readability.py` as landed by `151-02` and `151-06` —
      `GATE_TOKEN_READ_PERMITTED`, the `REASON_*` constants, `protection_gate_for_entry`
    - `firestarter_app/firestarter/cli_handlers.py` — the terminal idiom `sys.exit(0 if ok else 1)`
      used by the `dev` commands
    - `.planning/phases/151-protection-readability-lock-status/151-DESIGN.md` §1 (the two-byte
      payload and the `0xFF` sentinel), §3 (the four-code exit map with its reasoning) and §6
    - `.planning/phases/151-protection-readability-lock-status/151-PATTERNS.md` §3 — the split
      table, and the note that the exit-code mapping belongs beside
      `classify_protection_response` as an explicit literal `str -> int` dict and **not** a
      `max()` over severities, with the `dev test` `max(1,2)` precedence defect as the reason
  </read_first>
  <files>firestarter_app/firestarter/lock_status.py</files>
  <behavior>
    - `PROTECTION_CLASSES` is a frozen 8-tuple containing exactly D-09's eight tokens, and
      `GATE_TOKEN_READ_PERMITTED` is **not** among them.
    - `classify_protection_response(gate_token, payload, forced=False)` with a
      `read_permitted` gate token and a payload whose decode byte is the unprotected code returns
      the unprotected class token; with the protected code it returns the protected class token.
    - The same call with a payload whose decode byte is `0xFF` returns neither state token.
    - With `forced=True` and a gate token that is a refusal, it returns `unadjudicated_probe`
      regardless of the payload's decode byte — a forced read is never a state claim.
    - With `forced=False` and a refusal gate token, it returns that refusal token unchanged and
      consumes no payload.
    - `EXIT_BY_CLASS` maps all eight tokens to integers, uses exactly the four values from
      `151-DESIGN.md` §3, and `0` is reachable only from the two silicon-only tokens.
    - `exit_code_for_class` raises on a token outside `PROTECTION_CLASSES` rather than returning a
      default.
    - `render_lock_status(class_token, reason, raw_byte)` produces a string whose **first**
      whitespace-delimited field is the class token, and which contains the raw byte rendered in
      hex whenever a payload was consumed.
    - For the `not_readable` class, the rendered string contains
      `sdp_honesty.unreadable_state_caveat()`'s return value as a substring, composed by calling
      it.
  </behavior>
  <action>
    Create `firestarter_app/firestarter/lock_status.py`. Give it an import-set invariant comment
    in `sdp_honesty.py:22-28`'s shape naming the permitted modules — `__future__`, `typing`,
    `firestarter.exceptions`, `firestarter.messages`, `firestarter.protection_readability`,
    `firestarter.sdp_honesty` — and stating explicitly that there is deliberately no `click`,
    so a future report layer with no CLI context can import this module.

    Symbols, in order:
    - `PROTECTION_CLASSES: tuple[str, ...]` — D-09's eight tokens, frozen, with a comment stating
      that D-09 freezes the set at eight and that `read_permitted` is a **gate** token which is
      never printed and deliberately absent from this tuple.
    - `SILICON_ONLY_TOKENS: frozenset[str]` — the two tokens producible only from a device
      response. A comment must state that plan `151-09`'s AST gate asserts these two literals
      never appear as a return value in `protection_readability.py`, and that this module is the
      only place they may be produced.
    - `DECODE_UNPROTECTED`, `DECODE_PROTECTED`, `DECODE_INDETERMINATE` — the three firmware decode
      codes from `151-DESIGN.md` §1, as named integer constants with the `0xFF` sentinel's
      provenance noted (`hw_get_version`'s established convention).
    - `EXIT_BY_CLASS: Mapping[str, int]` — a literal dict over all eight tokens with the four
      codes from `151-DESIGN.md` §3. Above it, a comment carrying §3's reasoning verbatim in
      substance: why `unadjudicated_probe` gets its own fourth code, and why this is a literal
      dict and not a `max()` over severities — this codebase already has an exit-code precedence
      defect where `max(1, 2)` picked the wrong verdict.
    - `exit_code_for_class(class_token: str) -> int` — a dict lookup that raises `KeyError` on an
      unknown token. Never `.get(token, 1)`; a default here would let a new class silently exit
      non-zero-but-wrong.
    - `classify_protection_response(gate_token: str, payload: bytes | None, *, forced: bool) ->
      tuple[str, str]` — the only function permitted to return a silicon-only token. Guard
      cascade, one early return per outcome:
      1. `forced` and `gate_token` is not `read_permitted` → `unadjudicated_probe`, with a reason
         stating the read ran past a table refusal and is explicitly not a state claim, and naming
         the refusal the table gave. Do this **before** consulting the payload, so a forced probe
         can never become a state claim by accident.
      2. `gate_token` is not `read_permitted` → return `(gate_token, ...)` unchanged; the payload
         is not consumed at all.
      3. `payload` is `None` or shorter than 2 bytes → an operational failure token, not a state
         token. Use the same class the comms path uses so a truncated frame and a dead port are
         indistinguishable to a script, and say so in a comment.
      4. decode byte equals `DECODE_UNPROTECTED` → the unprotected token; equals
         `DECODE_PROTECTED` → the protected token.
      5. anything else, including `DECODE_INDETERMINATE` → **not** a state token. Return the
         not-readable class with a reason stating that the read completed but returned a value
         the decode does not recognise, and include the raw byte. Never coerce.
    - `render_lock_status(class_token: str, reason: str, raw_byte: int | None) -> str` — D-08's
      surface: the class token first, then the prose. The class token must be the first
      whitespace-delimited field so a test can assert the token without matching wording. Compose
      the `not_readable` caveat by **calling** `sdp_honesty.unreadable_state_caveat()`; do not
      re-author, re-word or reformat that sentence. Render `raw_byte` in hex whenever it is not
      `None`, including on the not-readable-because-unrecognised path, because D-03's probe
      depends on the raw observation surviving.

    Use no bare `except Exception:` anywhere. ruff's select list is `[E, F, I, UP]`, so every
    `# noqa: BLE001` in this repository is inert and a broad catch on this path is gated by
    nothing — and a swallowed error here is exactly how a fabricated state claim would reach a
    user.

    Do not import `firestarter.serial_comm` or `firestarter.eprom_operations` — this module
    classifies a payload it is handed and performs no I/O.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 -c "
import ast,sys
t=ast.parse(open('firestarter/lock_status.py').read())
mods={n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom)} | {a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}
allowed={'__future__','typing','firestarter.exceptions','firestarter.messages','firestarter.protection_readability','firestarter.sdp_honesty'}
bad=mods-allowed
print('IMPORTS',sorted(mods))
sys.exit(1 if bad else 0)
"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
from firestarter.lock_status import PROTECTION_CLASSES as C, SILICON_ONLY_TOKENS as S, EXIT_BY_CLASS as E, exit_code_for_class
from firestarter.protection_readability import GATE_TOKEN_READ_PERMITTED as G
assert len(C)==8 and len(set(C))==8, C
assert G not in C
assert set(E)==set(C), sorted(set(C)^set(E))
assert set(E.values())=={0,2,3,4}, sorted(set(E.values()))
assert {t for t in C if E[t]==0}==set(S), sorted({t for t in C if E[t]==0})
try:
    exit_code_for_class('nope'); raise SystemExit('no raise on unknown token')
except KeyError: pass
print('OK', dict(E))
"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
from firestarter.lock_status import classify_protection_response as cl, render_lock_status, DECODE_UNPROTECTED, DECODE_PROTECTED, DECODE_INDETERMINATE, SILICON_ONLY_TOKENS
from firestarter.protection_readability import GATE_TOKEN_READ_PERMITTED as G
from firestarter import sdp_honesty
t,_=cl(G, bytes([0x00, DECODE_UNPROTECTED]), forced=False); assert t in SILICON_ONLY_TOKENS, t
t,_=cl(G, bytes([0xFF, DECODE_PROTECTED]), forced=False); assert t in SILICON_ONLY_TOKENS, t
t,_=cl(G, bytes([0x5A, DECODE_INDETERMINATE]), forced=False); assert t not in SILICON_ONLY_TOKENS, t
t,_=cl('not_readable', bytes([0x00, DECODE_UNPROTECTED]), forced=True); assert t=='unadjudicated_probe', t
t,_=cl('undocumented_alias', None, forced=False); assert t=='undocumented_alias', t
s=render_lock_status('not_readable','because', 0x5A)
assert s.split()[0]=='not_readable', s
assert sdp_honesty.unreadable_state_caveat() in s
assert '5A' in s.upper()
print('OK')
"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
s=open('firestarter/lock_status.py').read()
assert 'except Exception' not in s
assert 'except:' not in s
assert 'max(' not in s.split('EXIT_BY_CLASS')[1][:2000], 'max() near the exit map'
print('OK')
"</automated>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/lock_status.py; ruff format --check firestarter/lock_status.py; python3 tools/check_mypy_watermark.py</automated>
  </verify>
  <acceptance_criteria>
    - The module's top-level import set is a subset of the six permitted modules, and the
      import-set invariant comment states there is deliberately no `click`.
    - `PROTECTION_CLASSES` is an 8-tuple of distinct tokens; `GATE_TOKEN_READ_PERMITTED` is not
      among them, and a comment states it is a gate token that is never printed.
    - `EXIT_BY_CLASS` covers exactly the eight tokens, uses exactly the four values `{0, 2, 3, 4}`,
      and the set of tokens mapping to `0` equals `SILICON_ONLY_TOKENS` exactly.
    - The comment above `EXIT_BY_CLASS` states why `unadjudicated_probe` has its own code and why
      the map is a literal dict rather than a `max()` over severities, naming the `max(1, 2)`
      precedence defect.
    - `exit_code_for_class` raises `KeyError` on an unknown token; no `.get(..., default)`.
    - Every behaviour in the `<behavior>` block holds, verified by the checks above.
    - A `forced=True` call with a refusal gate token returns `unadjudicated_probe` **without
      consulting the payload's decode byte** — verified by passing a payload whose decode byte is
      the unprotected code and observing the probe token.
    - An unrecognised decode byte returns a non-state token and the raw byte still appears in the
      rendered string.
    - `render_lock_status`'s output has the class token as its first whitespace-delimited field,
      and the `not_readable` rendering contains `unreadable_state_caveat()`'s exact return value.
    - No `except Exception:` and no bare `except:`; no `max(` in the exit-map region.
    - `ruff check`, `ruff format --check` and `tools/check_mypy_watermark.py` clean.
  </acceptance_criteria>
  <done>One function produces the two state tokens and its signature requires a device response;
  the exit map is a literal dict with four codes and 0 reachable only from those two tokens; the
  caveat is composed rather than re-authored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The strictly-additive `sdp_honesty.py` extension, and the operator transport method</name>
  <read_first>
    - `firestarter_app/firestarter/sdp_honesty.py` lines 67-92 —
      `map_unknown_cmd_to_outdated(exc, mode, chip_name)`, whose message reads
      `"{chip}: attached firmware does not implement SDP {mode} (unknown command) -- upgrade with
      'firestarter fw --install'."`. The `"SDP {mode}"` wording is what D-04 needs generalised.
    - `firestarter_app/tests/test_sdp_honesty.py` lines 20-60 and 120-160 — the module docstring's
      statement of what a false-green would look like here, the import list at `:51`, and the
      existing `map_unknown_cmd_to_outdated` legs at `:125-150` including the negative control on
      a different `error_code`. **Extend this file; never rewrite it.**
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` — the four legs pinning
      `unreadable_state_caveat()`'s text. Together with the three production callers
      (`cli_handlers.py:2405`, `:2412`, `chip_test.py:1480`) these are the seven pinning sites and
      two production surfaces an edit to that function's text would break.
    - `firestarter_app/firestarter/eprom_operations.py` lines 2185-2225 — `check_eprom_id`, the
      value-returning operator method: it opens `self._operation_context(...)`, runs
      `self._run_state_machine(op_name)`, and captures its value **inside** the context. Copy that
      structure. Note that `EpromOperator.comm` is torn down after every operator call, so any
      value not captured inside the context is unreadable afterwards.
    - `firestarter_app/firestarter/eprom_operations.py` lines 2098-2145 — `sdp_lock`, for the
      payload-free operation shape and the honesty wording in its docstring
    - `firestarter_app/firestarter/constants.py` — `COMMAND_LOCK_STATUS` from plan `151-03`
  </read_first>
  <files>firestarter_app/firestarter/sdp_honesty.py, firestarter_app/firestarter/eprom_operations.py, firestarter_app/tests/test_sdp_honesty.py</files>
  <behavior>
    - `unreadable_state_caveat()`'s return value is **byte-identical** to what it returns today.
    - `emission_summary("enable", "X")` and `emission_summary("disable", "X")` are byte-identical
      to what they return today.
    - A new sibling maps an `EpromOperationError` whose `error_code` is `MSG_ERR_UNKNOWN_CMD` to a
      constructed `FirmwareOutdatedError` whose message names the operation label it was given
      rather than the literal `SDP`.
    - The new sibling **returns** rather than raises, and returns `None` for any other
      `error_code` including `None`.
    - `EpromOperator.read_protection_status(...)` returns a 2-tuple of a bool and an optional
      2-byte payload, captured inside the operation context.
  </behavior>
  <action>
    In `sdp_honesty.py`, add a sibling — `map_unknown_cmd_to_outdated_for_operation(exc,
    operation_label, chip_name)` — with the same contract as the existing function: keyed on
    `exc.error_code != MSG_ERR_UNKNOWN_CMD`, **returns** a constructed `FirmwareOutdatedError`
    rather than raising so the caller owns `raise ... from exc`, and ends its message with
    `"upgrade with 'firestarter fw --install'."`. Its message names the operation label it is
    given instead of hard-coding `SDP`.

    The extension is **strictly additive**. Do not change the text returned by
    `unreadable_state_caveat()` or `emission_summary()`, and do not change
    `map_unknown_cmd_to_outdated`'s signature or message. C-4 measured why: the caveat has three
    landed production callers and four pinning tests, so editing its text breaks seven pinning
    sites and two production surfaces. Reimplementing the existing function in terms of the new
    one is permitted **only** if the existing function's returned message stays byte-identical;
    if that cannot be shown, leave it alone entirely. Preserve the import-set invariant comment at
    `:22-28` — in particular, still no `click`.

    Update the module docstring's "Forward contract" paragraph, which currently says Phase 134's
    leg-report rows and Phase 135's `write --sdp-relock` are the intended future callers. Record
    that Phase 134's rows and `write --sdp-relock` are both deferred (999.28), that Phase 151's
    `dev lock-status` is the first forward caller to actually land, and that the caveat function
    has since acquired three production callers so its text is now load-bearing at seven sites.

    In `eprom_operations.py`, add `read_protection_status(self, eprom_name: str, eprom_data_dict:
    dict, operation_flags: int = 0) -> Tuple[bool, Optional[bytes]]`, modelled on
    `check_eprom_id`:
    - open `self._operation_context(eprom_name, eprom_data_dict, COMMAND_LOCK_STATUS,
      operation_flags)`;
    - return `(False, None)` when `cmd_data` is falsy;
    - run `self._run_state_machine(op_name)` and capture the two-byte payload **inside** the
      context. `EpromOperator.comm` is torn down after every operator call, so a value read after
      the context has exited is unreadable — this is a measured property of this class, not a
      style preference. State it in the docstring.
    - do **not** set `FLAG_FORCE` in the flags word for this command. Per `151-DESIGN.md` §6
      `--force` is host-side only, firmware's `FLAG_FORCE` means "downgrade a chip-ID mismatch to
      a warning", and this command performs no chip-ID check. State that in the docstring so a
      later reader does not add the bit as an apparent omission.
    - the docstring must state, in the shape `sdp_lock`'s does, exactly what a `True` return means
      and does not mean: that the command was accepted and a payload was returned, never that the
      payload's decode is correct. Classification is `lock_status.py`'s job and this method makes
      no claim about the chip's state.

    In `tests/test_sdp_honesty.py`, **extend** — never rewrite. Add legs:
    - the new sibling returns a `FirmwareOutdatedError` for `MSG_ERR_UNKNOWN_CMD` and `None` for a
      different `error_code`, mirroring the existing negative control;
    - the returned message contains the operation label passed in and does **not** contain the
      literal `SDP` when a non-SDP label is given — the leg that proves the generalisation
      actually happened;
    - the new function **returns** rather than raises, asserted by calling it outside a
      `pytest.raises`;
    - `unreadable_state_caveat()`'s return value is unchanged, pinned as a literal string in the
      test so an edit anywhere goes red here as well as at the four existing pinning sites;
    - `emission_summary("enable", ...)` and `emission_summary("disable", ...)` are unchanged, same
      pinning style.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_sdp_honesty.py -x -o addopts="-ra"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_chip_test_sdp_leg.py -k caveat -x -o addopts="-ra"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
from firestarter.sdp_honesty import unreadable_state_caveat, emission_summary
assert unreadable_state_caveat() == (
  'The resulting protection state cannot be read back on this chip '
  \"family, so this is not a claim about the chip's actual state.\"
), repr(unreadable_state_caveat())
assert emission_summary('enable','x').startswith('SDP enable sequence for X was emitted.')
print('OK')
"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
import ast
t=ast.parse(open('firestarter/sdp_honesty.py').read())
mods={n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom)} | {a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}
assert mods <= {'__future__','firestarter.exceptions','firestarter.messages'}, sorted(mods)
fns=sorted(n.name for n in t.body if isinstance(n, ast.FunctionDef))
assert len(fns)>=4, fns
print('OK', fns)
"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "
import inspect
from firestarter.eprom_operations import EpromOperator
src=inspect.getsource(EpromOperator.read_protection_status)
assert 'FLAG_FORCE' not in src, 'FLAG_FORCE set on the lock-status path'
assert '_operation_context' in src and '_run_state_machine' in src
print('OK')
"</automated>
    <automated>cd /workspaces/firestarter_app; git diff -U0 firestarter/sdp_honesty.py | grep -c '^-' </automated>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/ tests/; ruff format --check firestarter/ tests/; python3 tools/check_mypy_watermark.py</automated>
  </verify>
  <acceptance_criteria>
    - `unreadable_state_caveat()` returns a string byte-identical to its pre-task value, asserted
      as a literal in both the new test leg and the check above.
    - `emission_summary("enable", ...)` and `emission_summary("disable", ...)` are byte-identical
      to their pre-task values.
    - `git diff -U0 firestarter/sdp_honesty.py` shows deletions only in the "Forward contract"
      docstring paragraph — no deletion inside any of the three existing function bodies or their
      returned strings. Record the deletion count and the lines it covers in the plan summary.
    - The new sibling exists, returns a `FirmwareOutdatedError` for `MSG_ERR_UNKNOWN_CMD`, returns
      `None` for any other `error_code` including `None`, and **returns rather than raises**.
    - A leg asserts the returned message contains the operation label given and does not contain
      the literal `SDP` for a non-SDP label.
    - `sdp_honesty.py`'s import set is still a subset of
      `{__future__, firestarter.exceptions, firestarter.messages}` — the no-`click` invariant
      survives.
    - `pytest tests/test_sdp_honesty.py -x -o addopts="-ra"` and
      `pytest tests/test_chip_test_sdp_leg.py -k caveat -x -o addopts="-ra"` both pass, with no
      leg skipped.
    - `EpromOperator.read_protection_status` exists, uses `_operation_context` and
      `_run_state_machine`, captures the payload inside the context, and its source contains no
      `FLAG_FORCE` reference.
    - Its docstring states what a `True` return does and does not mean, and states why
      `FLAG_FORCE` is deliberately absent.
    - `ruff check`, `ruff format --check` and `tools/check_mypy_watermark.py` clean.
  </acceptance_criteria>
  <done>The honesty module gained a sibling and lost nothing; the caveat's seven pinning sites are
  undisturbed; and the operator has a value-returning transport method that captures its payload
  inside the operation context and sets no force bit.</done>
</task>

<task type="auto">
  <name>Task 3: `test_lock_status_wire.py` — a real frame build-and-parse test on the existing harness</name>
  <read_first>
    - `firestarter_app/tests/conftest.py` lines 105-215 — the frame-level harness this test uses,
      which **already exists**: `_ref_crc8_ccitt` (table-free, so a regression in the production
      lookup table is caught), `build_frame(msg_id, params)` assembling
      `magic | len_u16 | id | params | crc | 0x0A` with the length counting id + params + crc,
      `_FakeSerial` with `read`, `in_waiting`, `write` and a test-side `feed(data)`, the
      `fake_serial` fixture, and the `make_comm` factory which builds a `SerialCommunicator` via
      `__new__` to bypass `__init__`
    - `firestarter_app/firestarter/serial_comm.py` lines 380-450 — how a DATA id-frame's params
      reach a caller, and `get_response()`'s INFO-band filter at `:424` (which is why the SDP
      duration frame is invisible to the operation layer — confirm the DATA band is **not**
      filtered before relying on it)
    - `firestarter_app/firestarter/messages.py` — `MSG_DATA_PROTECTION_STATUS` from plan `151-05`
    - `firestarter_app/tests/test_serial_comm.py` — the neighbouring decoder-test idiom, for how a
      fed frame is asserted
    - `.planning/phases/151-protection-readability-lock-status/151-PATTERNS.md` §"No Analog Found",
      the `test_lock_status_wire.py` row — which asked the planner to confirm what
      `tests/fixtures/fake_firestarter` exposes before assuming a frame-level harness exists.
      **Confirmed, and the answer is recorded here so the executor does not repeat the search:**
      `tests/fixtures/fake_firestarter/` is a deliberately-incomplete fake firmware **source**
      sibling for `tests/fw_presence.py`, holding only `README.md`, `include/firestarter.h` and
      `doc/PROTOCOLS.md`. It is **not** a device harness. The device harness is
      `conftest.py`'s `build_frame` + `_FakeSerial` + `make_comm`, and it is sufficient.
  </read_first>
  <files>firestarter_app/tests/test_lock_status_wire.py</files>
  <action>
    Create `firestarter_app/tests/test_lock_status_wire.py`. Use `conftest.py`'s existing harness —
    `build_frame`, the `fake_serial` fixture and the `make_comm` factory. Do not build a new
    harness and do not use `tests/fixtures/fake_firestarter/`, which is a firmware-source stand-in
    rather than a device.

    Legs:
    1. **Frame build.** Assert the request the host emits for this command carries
      `COMMAND_LOCK_STATUS` as its command value. Assert on the constant, not on a literal 16, so
      the leg survives a deliberate renumber but goes red on an accidental one.
    2. **Response parse, both definite decodes.** Feed a `MSG_DATA_PROTECTION_STATUS` frame built
      with `build_frame` carrying `bytes([raw, DECODE_UNPROTECTED])`, drive the decode path, and
      assert the two-byte payload reaches the caller with byte 0 equal to `raw` exactly. Repeat
      with `DECODE_PROTECTED`.
    3. **Raw-byte fidelity through the whole path.** Feed a frame whose raw byte is a value with
      bits set in both nibbles and whose decode byte is `DECODE_INDETERMINATE`; assert byte 0
      arrives unmasked, unshifted and unnormalised, and that `classify_protection_response`
      returns a non-state token for it. This is the leg that proves D-03's probe can record a raw
      observation the decode does not recognise.
    4. **CRC integrity.** Feed a frame whose CRC byte has been corrupted by one bit and assert the
      frame is rejected rather than partially accepted. `build_frame` uses a table-free reference
      CRC, so this leg also catches a regression in the production lookup table.
    5. **Truncated payload.** Feed a `MSG_DATA_PROTECTION_STATUS` frame with a one-byte payload and
      assert the path yields no state token — the operational-failure class from Task 1 step 3,
      not a coerced decode.
    6. **The DATA band is not filtered.** Assert directly that a `MSG_DATA_PROTECTION_STATUS`
      frame survives `get_response()`'s band filtering, and give the leg a docstring naming the
      measured reason it is worth asserting: the INFO band **is** filtered at
      `serial_comm.py:424`, which is why `sdp_honesty`'s own docstring records that the operation
      layer cannot see the firmware's `0x5F`/`0x61` duration frame. If the DATA band turned out to
      be filtered too, every other leg in this file would be vacuous.

    Do not assert any timing property and do not assert a full message string.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_lock_status_wire.py -x -o addopts="-ra"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_lock_status_wire.py -o addopts="" --collect-only -q | tail -3</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'fake_firestarter' tests/test_lock_status_wire.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c '\b16\b' tests/test_lock_status_wire.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/ -o addopts="-ra" --cov=firestarter --cov-report=term-missing --cov-fail-under=70 2>&1 | tail -12</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_lock_status_wire.py; ruff format --check tests/test_lock_status_wire.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_lock_status_wire.py -x -o addopts="-ra"` passes, collects at least 6 test
      functions, and reports no skip.
    - Leg 1 asserts against `constants.COMMAND_LOCK_STATUS`, and
      `grep -c '\b16\b' tests/test_lock_status_wire.py` is `0` — no bare literal command value.
    - Leg 3 asserts byte 0 arrives byte-equal to the fed value for a value with bits set in both
      nibbles, and that the classifier returns a non-state token for it.
    - Leg 4's corrupted-CRC frame is rejected; record the observed rejection behaviour in the plan
      summary.
    - Leg 6 asserts the DATA band survives `get_response()`'s filtering, and its docstring names
      the INFO-band filter at `serial_comm.py:424` as the measured reason the leg is not a
      tautology. If this leg had failed, the plan's wire design would have needed revisiting —
      record the observed result either way.
    - `grep -c 'fake_firestarter' tests/test_lock_status_wire.py` is `0`.
    - No leg asserts a timing property or a full message string.
    - The full host suite passes at `--cov-fail-under=70`.
    - `ruff check` and `ruff format --check` clean.
  </acceptance_criteria>
  <done>Six legs prove the frame is built with the right command constant, both definite decodes
  parse, the raw byte survives an unrecognised decode, a corrupted CRC is rejected, a truncated
  payload yields no state token, and the DATA band is genuinely unfiltered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device response → class token | This is the only boundary across which a state claim may be produced; everything downstream trusts the token. |
| firmware version → command availability | An older firmware answers with an unknown-command error; misreading it as a refusal would hide the upgrade path. |
| existing honesty wording → new caller | Editing the caveat's text would break seven pinning sites and two live production surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-151-50 | Spoofing | `classify_protection_response` | critical | mitigate | The forced-probe branch returns before the payload is consulted, an unrecognised decode returns a non-state token, and a truncated payload returns an operational-failure token — no path coerces. Legs in Tasks 1 and 3 assert each. |
| T-151-51 | Elevation of Privilege | `EXIT_BY_CLASS` | high | mitigate | A literal dict with four codes; the set of tokens mapping to 0 is asserted equal to `SILICON_ONLY_TOKENS`; `exit_code_for_class` raises rather than defaulting; no `max()` near the map. |
| T-151-52 | Tampering | `sdp_honesty.unreadable_state_caveat()` text | critical | mitigate | Strictly additive extension; the caveat and both `emission_summary` directions pinned byte-identical; `git diff -U0` deletions confined to the docstring's Forward-contract paragraph. |
| T-151-53 | Information Disclosure | a raw silicon byte lost to normalisation | high | mitigate | Task 3 leg 3 feeds a both-nibbles value and asserts byte 0 arrives unmasked, unshifted and unnormalised through the whole path. |
| T-151-54 | Tampering | a corrupted frame partially accepted | high | mitigate | Task 3 leg 4 corrupts one CRC bit and asserts rejection; `build_frame`'s reference CRC is table-free so a production-table regression is caught too. |
| T-151-55 | Denial of Service | a vacuous wire suite behind a band filter | high | mitigate | Task 3 leg 6 asserts the DATA band survives `get_response()` filtering, with the INFO-band filter named as the reason the leg is non-vacuous. |
| T-151-56 | Repudiation | `FLAG_FORCE` given a second meaning | medium | mitigate | `read_protection_status`'s source is asserted free of `FLAG_FORCE`, and its docstring records why. |
| T-151-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs. Only `pip install -e '.[test]'` against the existing pinned extra — never `.[dev]`, which is `pytest>=7.0` only. |
</threat_model>

<verification>
- `pytest tests/test_lock_status_wire.py tests/test_sdp_honesty.py -x -o addopts="-ra"` green, no
  skips.
- `pytest tests/test_chip_test_sdp_leg.py -k caveat -x -o addopts="-ra"` green.
- `lock_status.py`'s import set is a subset of the six permitted modules.
- `EXIT_BY_CLASS` covers all eight tokens with four codes; `0` maps exactly to the two
  silicon-only tokens.
- `unreadable_state_caveat()` and both `emission_summary` directions byte-identical to their
  pre-task values.
- `read_protection_status`'s source contains no `FLAG_FORCE`.
- Full host suite green at `--cov-fail-under=70`; `ruff check firestarter/ tests/`,
  `ruff format --check firestarter/ tests/` and `python tools/check_mypy_watermark.py` clean.
</verification>

<success_criteria>
- `lock_status.py` exists with the eight frozen classes, the two silicon-only tokens, the three
  decode constants, the literal four-code exit map, `classify_protection_response`,
  `exit_code_for_class` and `render_lock_status`.
- `sdp_honesty.py` gained one sibling and changed no existing returned string.
- `EpromOperator.read_protection_status` returns a bool and an optional two-byte payload captured
  inside the operation context, with no force bit.
- `tests/test_lock_status_wire.py` has at least 6 legs on the existing `conftest.py` harness,
  including CRC rejection, truncation and the DATA-band non-vacuity leg.
</success_criteria>

<output>
Create `.planning/phases/151-protection-readability-lock-status/151-11-SUMMARY.md` when done.
Record in it: the landed `EXIT_BY_CLASS` mapping in full; the `git diff -U0` deletion count on
`sdp_honesty.py` and which lines it covers; the observed corrupted-CRC rejection behaviour; and
the observed result of the DATA-band non-vacuity leg.
</output>

## Artifacts this phase produces

This plan's contributions: `firestarter_app/firestarter/lock_status.py` (new —
`PROTECTION_CLASSES`, `SILICON_ONLY_TOKENS`, `DECODE_UNPROTECTED`, `DECODE_PROTECTED`,
`DECODE_INDETERMINATE`, `EXIT_BY_CLASS`, `classify_protection_response`, `exit_code_for_class`,
`render_lock_status`); `map_unknown_cmd_to_outdated_for_operation` in
`firestarter_app/firestarter/sdp_honesty.py`; `EpromOperator.read_protection_status` in
`firestarter_app/firestarter/eprom_operations.py`; and
`firestarter_app/tests/test_lock_status_wire.py` (new), plus extended legs in
`firestarter_app/tests/test_sdp_honesty.py`.

The complete phase-wide artifact inventory is `## Artifacts Phase 151 produces` at the foot of
`151-01-PLAN.md`.
</content>
