---
phase: 152-outward-facing-close-operator-gated
plan: 06
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03", "152-04"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md
autonomous: true
requirements: [OUT-02]

must_haves:
  truths:
    - "`152-GH21-COMMENT.md` names what changed in code, what remains unproven, and requests a fresh `dev test` run stated as answerable BECAUSE the report now identifies its firmware."
    - "The request names BOTH install halves — the pre-release host install and `firestarter fw --install` — because Phase 153's write-path fix is in the firmware and the host test step is still flagless."
    - "It quotes the reporter's own pasted `fw_board_identity` value and the erase-step reason string, both citable from the issue body, and says the reason string is now false in the tree."
    - "It states gh#32's closure in a phrasing the narrowed `issue-closed` gate row permits, and does not claim gh#21 is closed."
    - "`FIRESTARTER_CLAIMSCAN_TARGETS_152=<the draft> python3 152-check-claims.py` exits 0."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md
  key_links:
    - "The criterion-2 phrase 'answerable because the report now identifies its firmware' is only true AFTER the merge Plan 152-11 performs — `origin/beta` still hardcodes the null value today. This draft is authored now and posted in Plan 152-15, after the merge."
    - "`_CAVEAT_RULES` requires `no-at28c-part-tested` on this basename."
  prohibitions:
    - "This plan does NOT post anything. Plan 152-15 posts, behind its own blocking operator checkpoint."
    - "Do NOT close gh#21, and do NOT claim it is closed, resolved or fixed. Do NOT edit the issue body."
    - "Do NOT reopen gh#32."
    - "Do NOT claim any AT28C silicon result, page-size validation on silicon, a `0x0D` graduation, or a `support_status` change — and do NOT cite `support_status: supported` as evidence about the write path."
    - "Do NOT recommend the `-b` / `--no-blank-check` workaround. Phase 153 removed the precondition that made it necessary; recommending it would state a claim 153 disproved."
    - "Do NOT name the deferred deliberate-protection command. Name Backlog 999.28 if the deferral needs mentioning at all."
    - "Do NOT name `tools/check_dispatch.py` as the control that prevents the 12 V-on-OE erase path from reaching this protocol — Phase 153 measured that it structurally cannot."
    - "Do NOT state that the page-size seam explains anything about gh#21. The AT28C256 row carries the 64-byte floor, exactly today's value, so that change cannot have altered this part's behaviour at all."
---

<objective>
Author the gh#21 comment (with gh#32 folded) that OUT-02 requires: what changed in code, what remains
unproven, and a request for a fresh `dev test` run that is worth making because the report will now
say which firmware produced it.

Purpose: gh#21's own report body is a directly citable oracle. It carries a null firmware-identity
field — the exact instrumentation defect Phase 147 fixed and which is **still live on `origin/beta`
today** — and an erase-step reason string that Phase 153 falsified. The comment can quote the
reporter's own paste rather than asserting anything about silicon.

Per **D-01** the comment is posted inside this phase and the draft is version-agnostic. Per **D-07**
and **D-08** the erase and blank-check policy this comment describes is already shipped and merged
before the comment goes out, so the reason string the reporter pasted can be called false in the tree
rather than promised as future work.

Output: `152-GH21-COMMENT.md`, a frozen draft, gate-clean, version-agnostic.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This plan produces a **draft only**. Nothing goes public here.

⚠ RESEARCH's issue-thread measurements are ~3 days old by design and §D is a moving target. Task 1
re-measures before a single sentence is written.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure gh#21 and gh#32 live and record the citable strings from the report body</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1020-1119 (§D table, §D-1 through §D-3) — the 2026-08-21 baseline this task re-measures against, and the three OUT-02 hooks §D-3 hands over.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1146-1171 (§D-5) — which oracles are sound. `updatedAt` is not one.
  </read_first>
  <action>
Re-measure before authoring. Run, and record every result verbatim into a scratch note that Task 2
writes from and the SUMMARY reproduces:

- `gh issue view 21 --repo henols/firestarter_prom --json number,title,state,author,createdAt,comments`
  — capture the **author login**, the state, the **comment count** (the 2026-08-21 baseline was 2),
  and each comment's author and creation date.
- `gh issue view 21 --repo henols/firestarter_prom --json body --jq '.body'` — capture the report
  body. From it, extract verbatim: the `host_version` value, the `fw_board_identity` value, the
  `hw_revision` value, the `protocol` value, the `schema_version`, the `generated` timestamp, the
  per-step verdict table, and **the erase step's full reason string**. That reason string is the
  claim Phase 153 falsified and it is the single most useful thing to quote.
- `gh issue view 32 --repo henols/firestarter_prom --json state,stateReason,closedAt` — confirm the
  closure and its timestamp.
- `git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -n fw_board_identity`
  — confirm the defect is still live on the published branch. This is what makes criterion 2's
  "answerable because" false until Plan 152-11 merges, and it is why this draft is not posted here.

Record any delta against RESEARCH §D in the SUMMARY as a measured delta, not a correction to
RESEARCH. If the comment count has moved, note it — Plan 152-15 re-captures it again immediately
before posting and asserts a delta of exactly 1.
  </action>
  <acceptance_criteria>
    - The SUMMARY records, verbatim: gh#21's author login, state, comment count, and the six extracted report-body fields including the erase step's full reason string.
    - `gh issue view 21 --repo henols/firestarter_prom --json state --jq '.state'` returns `OPEN`.
    - `gh issue view 32 --repo henols/firestarter_prom --json state --jq '.state'` returns `CLOSED`.
    - `git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -c fw_board_identity` returns ≥ 1 — the defect is confirmed still live on the published branch.
  </acceptance_criteria>
  <verify>
    <automated>gh issue view 21 --repo henols/firestarter_prom --json state,author,comments --jq '{state:.state, author:.author.login, comments:(.comments|length)}'; gh issue view 32 --repo henols/firestarter_prom --json state,stateReason,closedAt; git -C /workspaces/firestarter_app fetch origin --quiet; git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -c fw_board_identity</automated>
  </verify>
  <done>Every fact the draft will cite is measured live, the citable report-body strings are captured verbatim, and the still-live provenance defect on the published branch is confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 152-GH21-COMMENT.md from the measured oracles</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md</files>
  <read_first>
    - The Task 1 measurements — the draft cites nothing that was not measured there.
    - `.planning/ROADMAP.md` line 452 (`sed -n '452p'`) — criterion 2 as amended by Plan 152-03.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md` lines 16-18 — the tonal donor: name the shortfall before naming the gain. There is no exact donor for this comment.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 501-664 (§B) — what Phase 153 shipped and, at §B-10, the transcribe-don't-paraphrase list of what it did NOT do.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 591-616 (§B-8) — the matched-firmware finding: the host `dev test` write call is still flagless and `write_eprom` still defaults its flags to zero, so the fix only takes effect against v1.32 firmware.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 596-621 (§7) — the three analogs that supply this comment's shape, and the D-14 citation discipline (cite a false statement by location; here the false string is the reporter's own paste, which is safe to quote).
    - `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md` — the permitted phrasing for stating gh#32's closure and the two D-11-exempted statements of shipped command behaviour.
  </read_first>
  <action>
Author `152-GH21-COMMENT.md`. Address the reporter by the login measured in Task 1. Structure it on the
donor's bolded-section rhythm, shortfall before gain.

**Section 1 — what your report already told us that we could not act on.** Quote the reporter's own
`fw_board_identity` value verbatim from the report body. State plainly that a report which cannot say
which firmware produced it cannot be root-caused, that this was the first thing fixed in v1.32, and
that the fix is what makes the ask at the end of this comment worth making. Note that the report ran
the host version it records — not the latest pre-release — so anything said about what has changed is
measured against that version.

**Section 2 — the erase step's reason string is now false.** Quote the erase step's full reason string
from the report body verbatim, and say that it is no longer true of the tree: a standalone erase step
now exists on this protocol, implemented by the software six-byte chip-erase sequence from the Atmel
application note rather than the datasheet's 12 V hardware path. Say why the software path was chosen:
the hardware path would put 12 V on a pin of this pinout, and this project does not do that on a 5 V
part. Do not name a source-scanning checker as the control.

**Section 3 — the write and blank-check verdicts.** Two separate findings, do not conflate them:
- The **blank-check step** verdict is addressed on the host side: on this protocol a page write
  auto-erases internally, so no step in a test plan can ever leave the device blank, and the step is
  now marked not-applicable with that as its reason rather than run and failed.
- The **write step** verdict is addressed in the **firmware**: the pre-write blank check on this
  protocol is gone, because on this silicon a page write auto-erases and every page is read-back
  verified anyway, so the check was never a safety net — it was a precondition that made a non-blank
  part unwritable without an override flag. **State the consequence plainly: the host's test step
  sends no override flag, so this only takes effect on a board running v1.32 firmware.** Against older
  firmware the same failure reproduces exactly as before.

**Section 4 — gh#32.** State that gh#32 was closed and folded into this thread, using the phrasing
proven clean in `fixtures/clean_control.md`. Say that both reports carry the same fingerprint and that
the consolidated table in this thread preserves the folded one. Do not reopen it and do not imply this
thread is closed.

**Section 5 — what remains unproven.** Transcribe, do not paraphrase, RESEARCH §B-10's list: no AT28C
part was involved at any point — not in writing the erase sequence, not in choosing the unlock prefix,
not in any test; the protocol's ledger status is unchanged; no chip's support field moved and the chip
database is byte-unchanged; the erase timing constant is an Atmel-family maximum and a part with a
longer actual cycle time would read non-blank after a successful-looking erase with nothing in the
test suite able to catch that; and no test can prove the wall-clock wait is honoured, because the
native trace stubs record no time, so that assertion is structural rather than temporal. This section
must contain the mandated sentence the gate's `no-at28c-part-tested` row requires.

**Section 6 — the ask.** Request a fresh `dev test` run on the same part, stated as answerable
**because** the report will now name the firmware it ran on. Name **both** install halves: the
pre-release host install and `firestarter fw --install`. Say why both. Frame it the way the 146 donor
frames its ask — either outcome is useful: a pass tells us the write path works on real silicon for
the first time; a failure now comes with the firmware identity attached and is therefore
root-causeable. Do not promise a result.

**Do not** say anything about the page-size seam explaining this failure — the part's page size was
already at the value the seam now delivers, so that change cannot have altered its behaviour. **Do
not** cite a protection-class count; those counts are being re-derived in Plan 152-08 and no outward
sentence may cite one until that lands. **Do not** name a release tag; this draft is
version-agnostic and Plan 152-12 fills versions where they are needed.
  </action>
  <acceptance_criteria>
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-GH21-COMMENT.md python3 152-check-claims.py` exits 0.
    - `grep -c 'fw_board_identity' 152-GH21-COMMENT.md` ≥ 1 — the reporter's own paste is quoted.
    - `grep -c 'auto-erase' 152-GH21-COMMENT.md` ≥ 1.
    - `grep -c 'fw --install' 152-GH21-COMMENT.md` ≥ 1 and `grep -c 'dev test' 152-GH21-COMMENT.md` ≥ 1.
    - `grep -c 'gh#32' 152-GH21-COMMENT.md` ≥ 1.
    - `grep -c 'No AT28C part was tested at any point in v1.32' 152-GH21-COMMENT.md` == 1.
    - `grep -c 'support_status' 152-GH21-COMMENT.md` == 0 and `grep -c 'check_dispatch' 152-GH21-COMMENT.md` == 0.
    - `grep -cE '3\.0\.0b[0-9]+' 152-GH21-COMMENT.md` equals the number of times the report's own recorded host version is quoted, and no other tag appears — record the count in the SUMMARY with the quoted context.
    - `grep -ciE 'page.size' 152-GH21-COMMENT.md` == 0 — the seam is not offered as an explanation.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-GH21-COMMENT.md" python3 152-check-claims.py; echo "GATE rc=$?"; for n in 'fw_board_identity' 'auto-erase' 'fw --install' 'dev test' 'gh#32' 'No AT28C part was tested at any point in v1.32'; do printf '%s => ' "$n"; grep -c "$n" 152-GH21-COMMENT.md; done; printf 'support_status => '; grep -c 'support_status' 152-GH21-COMMENT.md; printf 'pagesize => '; grep -ciE 'page.size' 152-GH21-COMMENT.md</automated>
  </verify>
  <done>The draft cites only measured facts, quotes the reporter's own two strings, separates the host and firmware halves of the fix, names both install halves, transcribes the not-proven list, and passes the gate.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the draft and record the pre-post baseline Plan 152-15 will assert against</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md` — the authored draft.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1146-1171 (§D-5) — the sound oracle table: body-content re-read is primary, comment-count delta is the non-vacuity guard, `updatedAt` is never an oracle.
  </read_first>
  <action>
Commit the draft as frozen and record the handoff data Plan 152-15 needs:

- The draft's git blob SHA (`git hash-object 152-GH21-COMMENT.md`) and the commit SHA. Record both,
  and record explicitly in the SUMMARY that **a blob SHA proves what we intended to send and never
  what GitHub stored** — the landed proof is the body re-read and diff in Plan 152-15.
- The current comment count on gh#21, with its capture command and timestamp, labelled as the
  baseline Plan 152-15 re-captures immediately before posting.
- The exact `gh` invocation Plan 152-15 will use, written out for review:
  `gh issue comment 21 --repo henols/firestarter_prom --body-file <path to this draft>`.
- The exact verification invocation Plan 152-15 will use afterwards:
  `gh issue view 21 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body'` piped
  into `diff -u` against this draft.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces log --oneline -1 -- .planning/phases/152-*/152-GH21-COMMENT.md` is non-empty.
    - `git -C /workspaces status --porcelain -- .planning/phases/152-*/152-GH21-COMMENT.md` is empty — the draft is committed, not dirty.
    - The SUMMARY records the blob SHA, the commit SHA, the baseline comment count with its capture timestamp, and both `gh` invocations verbatim.
    - The SUMMARY states that the blob SHA is an intent oracle, not a landed oracle.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; git -C /workspaces status --porcelain -- .planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md; echo "PORCELAIN-END"; git hash-object 152-GH21-COMMENT.md; gh issue view 21 --repo henols/firestarter_prom --json comments --jq '.comments | length'; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The draft is committed and frozen, its intent oracle recorded as an intent oracle, and the pre-post baseline plus both `gh` invocations are handed to Plan 152-15.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md` — the frozen OUT-02
  draft; a `_DEFAULT_TARGETS` member from Plan 152-13 onward; `_CAVEAT_RULES` entry
  `{no-at28c-part-tested}` (pre-populated by Plan 152-01)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft → public comment | Posted verbatim in Plan 152-15. |
| RESEARCH §D → draft | §D is explicitly a moving target with a ~3-day validity; Task 1 re-measures rather than inheriting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-21 | Spoofing | claiming the report "now identifies its firmware" while the published branch still hardcodes the null value | critical | mitigate | Task 1 confirms the defect is still live on `origin/beta`; the draft is authored here and posted only in Plan 152-15, after Plan 152-11's merge. |
| T-152-22 | Repudiation | conflating the host-side blank-check step fix with the firmware-side write-path fix, so the ask omits the firmware install | high | mitigate | Task 2 separates them into two explicitly-labelled findings; `fw --install` is an acceptance criterion. |
| T-152-23 | Repudiation | offering the page-size seam as an explanation for this failure | medium | mitigate | Explicit prohibition plus a `grep -ciE 'page.size'` count of 0; the part's page size was already at the delivered value. |
| T-152-24 | Information disclosure | citing a protection-class count that has not been reconciled | medium | mitigate | Prohibited in this draft; Plan 152-08 owns the re-derivation. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** `gh` and `git` are used read-only. |
</threat_model>

<verification>
- Gate green on the draft via the env seam.
- gh#21 `OPEN`, gh#32 `CLOSED`, the null provenance field confirmed still live on `origin/beta`.
- Nine positive/negative greps as enumerated in Task 2.
- Draft committed clean; blob SHA, commit SHA and baseline comment count recorded.
</verification>

<success_criteria>
The OUT-02 draft exists, is gate-clean, cites only measured facts and the reporter's own paste,
separates the host and firmware halves of the fix, transcribes the not-proven list, and requests a
fresh run naming both install halves.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-06-SUMMARY.md` when done.

Record verbatim: every Task 1 measurement, any delta against RESEARCH §D, the blob and commit SHAs,
the baseline comment count with its timestamp, and both `gh` invocations.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary. In particular, when recording that gh#32 is closed, use the phrasing
proven clean in `fixtures/clean_control.md`, and never write that gh#21, gh#11 or gh#12 is closed,
resolved or fixed.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-06-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
