---
phase: 152-outward-facing-close-operator-gated
plan: 09
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03", "152-04"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md
autonomous: true
requirements: [OUT-04]

must_haves:
  truths:
    - "`152-RELEASE-NOTES-fw.md` lists every published firmware image asset, with the board each targets, measured live from the latest firmware release rather than inherited from any document."
    - "It states exactly two size figures for the Leonardo build and never conflates them: the image size against the USB-bootloader boundary, and — separately labelled as a different number — the size-band headroom against the recorded baseline."
    - "It states that the bootloader boundary is UNGUARDED, and why."
    - "It states the deferred deliberate-protection command's withdrawal explicitly, in the gate's mandated word order, naming Backlog 999.28."
    - "It carries all three mandated non-claim sentences, including the standing non-claim for the ARM image."
    - "`FIRESTARTER_CLAIMSCAN_TARGETS_152=<the draft> python3 152-check-claims.py` exits 0."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md
  key_links:
    - "This is the second of only two artifacts in the phase that may contain the deferred command's literal name, and only in the mandated withdrawal word order."
    - "`_CAVEAT_RULES` requires all three caveat labels on this basename."
    - "The version is a placeholder here and is filled by Plan 152-12 from `gh release list` after the cut."
  prohibitions:
    - "This plan does NOT post anything. Plan 152-18 posts, behind its own blocking operator checkpoint."
    - "Do NOT conflate the size-band headroom with the bootloader-boundary headroom. They are different numbers with different meanings and the record states this as a standing rule."
    - "Do NOT inherit any size figure from CONTEXT, RESEARCH or a prior record — all three Leonardo figures moved between 2026-08-20 and 2026-08-21. Measure them in this plan."
    - "Do NOT state a count of image assets from any document — measure it. A prior discretion note said three; the measured number is different."
    - "Do NOT promise that the firmware updater can be pointed at a chosen board. Measured project behaviour is that it flashes the attached board and ignores the board option."
    - "Do NOT present split or trimmed firmware builds as planned, queued or on a roadmap. That work is explicitly deferred and must appear, if at all, only as an unaddressed constraint."
    - "Do NOT claim the ARM image has run on silicon or that a board for it exists."
    - "Do NOT predict a version number; use the placeholder tokens."
    - "Do NOT claim any AT28C silicon result, and do NOT name `tools/check_dispatch.py` as the control guarding the 12 V-on-OE erase path — name the control that actually does."
---

<objective>
Author the firmware release body OUT-04 requires: what the firmware side of v1.32 ships, which image
belongs to which board, the two size figures that matter and their separation, and the deferred
command's withdrawal.

Purpose: the firmware half is where the write-path change actually lives, so a reader who upgrades
only the host gets none of it. This body is also the only public place the Leonardo bootloader
boundary is recorded, and the linker guard that used to enforce it was removed — the recorded number
is now the only control that exists.

Per **D-01** the body is authored version-agnostic and the tag is filled in at cut time. Per **D-02**
only this cut gets a body. Per **D-07** and **D-08** the firmware-side policy change is already in the
committed tree, so these notes describe what shipped rather than what is planned.

Output: `152-RELEASE-NOTES-fw.md`, a frozen draft, version-placeholdered, gate-clean.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This plan produces a **draft only**. Nothing goes public here.

⚠ Every size figure and every asset name in this file must be **measured in this plan**. RESEARCH §A-9
recorded that all three Leonardo figures moved in the 24 hours between CONTEXT and RESEARCH, and §G-2
recorded that the asset count in the discretion note was wrong.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the published assets and both Leonardo size figures live</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 384-414 (§A-9) — the three moved figures, the four-term exemption allowance, and the standing rule that the two numbers must never be conflated.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1378-1417 (§G-2) — the measured asset list, the paragraph shape recommended for the ceiling, and the two prohibitions attached to it.
    - `firestarter/scripts/baseline/size_baseline.json` and `firestarter/scripts/baseline/size_baseline_base01.json` — the live baselines. Read both `meta` blocks so the exemption terms can be named from the file rather than from a document.
    - `firestarter/platformio.ini` — the AVR environments and the upload-size setting, so the statement about the guard is made from the file.
  </read_first>
  <action>
Measure, and record every command with its output and a timestamp:

- `gh release list --repo henols/firestarter --limit 5` — the latest firmware pre-release tag.
- `gh release view <that tag> --repo henols/firestarter --json assets --jq '.assets[] | {name, size}'`
  — **the asset names and byte sizes.** Record the count. Do not assume it.
- `python3 -c` over `firestarter/scripts/baseline/size_baseline.json` — for each AVR environment:
  `flash_used`, `flash_total`, `flash_free`, `ram_used`, `ram_total`. Record all three environments,
  not only the Leonardo.
- The same over `size_baseline_base01.json`, and compute the Leonardo delta between them. Record the
  delta and the exemption terms named in the current baseline's `meta` block, summed, so the
  allowance is derived rather than quoted.
- The USB-bootloader boundary figure and its arithmetic: boundary minus the measured Leonardo
  `flash_used`. Record both operands and the result.
- `grep -n 'maximum_size' firestarter/platformio.ini` — confirm, from the file, whether the upload
  size limit is the real device flash size rather than a bootloader-protecting figure. This is what
  makes the boundary unguarded, and the statement in the notes must rest on this measurement.

⚠ Record the two Leonardo numbers under **separate labels** in the SUMMARY, each with its own
arithmetic, and write one sentence stating what each one means. Conflating them is the specific
mistake RESEARCH §Pitfall 6 names.
  </action>
  <acceptance_criteria>
    - The SUMMARY records the measured asset list with names and byte sizes, and the asset count.
    - The SUMMARY records `flash_used`, `flash_total`, `flash_free` for all three AVR environments, plus Leonardo `ram_used`/`ram_total`.
    - The SUMMARY records the Leonardo delta against the `base01` baseline, the exemption terms summed from the current baseline's `meta` block, and states whether the delta equals the allowance.
    - The SUMMARY records the bootloader-boundary arithmetic with both operands and the result, under a label distinct from the size-band figure.
    - The SUMMARY records the measured `maximum_size` value from `platformio.ini` and states whether it protects the bootloader region.
  </acceptance_criteria>
  <verify>
    <automated>gh release list --repo henols/firestarter --limit 5; TAG=$(gh release list --repo henols/firestarter --limit 5 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease)][0].tagName'); echo "TAG=$TAG"; gh release view "$TAG" --repo henols/firestarter --json assets --jq '.assets[] | {name, size}'; python3 -c "import json;d=json.load(open('/workspaces/firestarter/scripts/baseline/size_baseline.json'));print(json.dumps(d,indent=1)[:1600])"; grep -n 'maximum_size' /workspaces/firestarter/platformio.ini</automated>
  </verify>
  <done>Every asset name, every AVR size figure, both Leonardo headroom numbers with their arithmetic, and the upload-size setting are measured live and recorded under distinct labels.</done>
</task>

<task type="auto">
  <name>Task 2: Author 152-RELEASE-NOTES-fw.md from the measured figures</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md</files>
  <read_first>
    - Task 1's measurements — the draft cites nothing that was not measured there.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md` — **read all 112 lines.** Its line 3 asset line is the model and already lists four assets with byte sizes; `:56` is the hazards section, `:68` and `:85` are the two honesty sections, `:102` is the closing ask.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 622-697 (§8) — the donor paragraphs pinned, and the note that the firmware workflow is named differently from the app's.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 501-664 (§B) — what the firmware side shipped, with §B-2's deleted conditional and its replacement comment, §B-3's software erase path and its application-note citation, §B-5's mechanism correction naming the control that actually guards the hazard, and §B-10's not-proven list.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1188-1236 (§E-2, §E-3) — the firmware workflow's real filename, the absent body field, and the automatic publish path.
    - `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md` — the proven-clean withdrawal phrasing.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` — the sibling body from Plan 152-08, so the two agree on facts and use the same placeholder tokens. Read it; do not edit it.
  </read_first>
  <action>
Author `152-RELEASE-NOTES-fw.md`. Use `FW_TAG_TBD` and `APP_TAG_TBD` as placeholders, matching the app
body's tokens exactly so Plan 152-12 can substitute both files with one pass.

Sections, in this order:

1. **The version-read opening paragraph**, structured like the 146 donor's: the tag, where it was read
   from and when, the workflow that cut it — **named correctly; the firmware repository's workflow is
   not called the same thing as the app's, verify the filename** — and the matching host release, with
   the note that the two repositories version independently so the numbers do not agree and are not
   expected to. Leave the read values as placeholders with a bracketed note naming Plan 152-12.
2. **The asset line**, in the 146 donor's shape: every published image with its byte size, from Task
   1's measurement. Then one sentence per image naming the board it targets. Then how to install it:
   the firmware updater flashes **the attached board** — state that plainly and do **not** promise
   that a board can be chosen. Add the ARM image's standing non-claim in the same breath: no board
   for it exists and nothing in that port has run on the silicon.
3. **`## Before you plug anything in`** — the donor's hazards section. Cover: the write-path change is
   in the firmware, so a host upgrade alone does not deliver it; and the erase path implemented here
   is the **software** six-byte sequence, chosen because the datasheet's alternative drives a
   programming voltage onto a pin of that pinout on a 5 V part. Name the source scan that guards
   that hazard — **the one that actually does; RESEARCH §B-5 records that the checker the roadmap
   named structurally cannot, and that this was a mechanism correction, not a satisfied criterion.**
4. **`## What's new`** — the firmware side, in D-11-exempt terms:
   - the pre-write blank check is **gone** on the two protocols that auto-erase per page during the
     write, with the reason from the replacing in-tree comment: every page is read-back verified
     anyway, so the check was never a safety net; note that the override flag is consequently unread
     on those protocols and must not be restored on the grounds that it looks orphaned;
   - a **standalone erase** command arm exists on the 28C protocol, with the application-note
     reference and revision cited as the tree cites it, and the erase-cycle wait named;
   - the **page-size seam**: the page value is delivered from the chip database through the wire
     instead of a compiled-in constant. State the load-bearing non-claim with it: for the part in the
     community threads the delivered value equals the old constant, so its behaviour is unchanged and
     the seam explains nothing about those reports;
   - the **protection-read command** the host's `lock-status` uses, named beta-only and requiring
     matched firmware in both directions.
5. **`## The size picture`** — one short paragraph, maintainer-facing, stating **exactly two numbers**
   from Task 1 and never conflating them:
   - the Leonardo image size against its device flash size, and the USB-bootloader boundary, and the
     remaining bytes between them — stated as **UNGUARDED**, with the reason measured from
     `platformio.ini`: the upload size limit is the real device flash size, so the linker no longer
     refuses a build that would overwrite the bootloader. Say what happens if a future build crosses
     it.
   - and, explicitly labelled *a different number*, the size-band headroom against the recorded
     baseline: the measured delta and the summed exemption allowance, with the headroom that leaves.
   Name the constraint as unaddressed. Do **not** present any relief for it as planned or queued.
6. **`## The withdrawal`** — the sentence criterion 4 mandates and criterion 5 constrains: the deferred
   command's literal name followed **immediately** by a withdrawal predicate, naming Backlog
   **999.28**. Use a phrasing proven clean in `fixtures/clean_control.md`. Say what this means from the
   firmware side: the lock command exists in the firmware, but no host surface reaches it in this
   release, so there is no supported way to deliberately protect such a part — and on this family the
   protection bit cannot be read back afterwards either.
7. **`## What is established`** and **`## What this release does not establish`** — two separate
   sections, per the donor. The second transcribes RESEARCH §B-10 rather than paraphrasing it, and
   carries the three mandated non-claim sentences verbatim. Use the donor's bolded-lead-then-explain
   rhythm and name *why* each thing is unestablished — including that no test can prove the erase
   wall-clock wait is honoured, because the native harness records no time, so the assertion is
   structural rather than temporal.
8. **`## The capability boundary, and what would help`** — the donor's closing ask, naming both install
   halves and the "either outcome is useful" framing.
  </action>
  <acceptance_criteria>
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-RELEASE-NOTES-fw.md python3 152-check-claims.py` exits 0 — simultaneously proving the mandated word order and all three required caveats.
    - `grep -c 'sdp-relock' 152-RELEASE-NOTES-fw.md` ≥ 1 and `grep -c '999.28' 152-RELEASE-NOTES-fw.md` ≥ 1.
    - `grep -c '\.hex' 152-RELEASE-NOTES-fw.md` equals the asset count measured in Task 1.
    - `grep -ci 'unguarded' 152-RELEASE-NOTES-fw.md` ≥ 1.
    - `grep -c '28672' 152-RELEASE-NOTES-fw.md` ≥ 1 and the two Leonardo numbers appear under distinct labels — verified by `grep -n` showing them in different sentences, recorded in the SUMMARY.
    - `grep -c 'check_erase_no_vpp' 152-RELEASE-NOTES-fw.md` ≥ 1 and `grep -c 'check_dispatch' 152-RELEASE-NOTES-fw.md` == 0.
    - `grep -c 'FW_TAG_TBD' 152-RELEASE-NOTES-fw.md` ≥ 1 and `grep -c 'APP_TAG_TBD' 152-RELEASE-NOTES-fw.md` ≥ 1.
    - `grep -c 'No AT28C part was tested at any point in v1.32' 152-RELEASE-NOTES-fw.md` == 1 and `grep -c 'software-proven and unvalidated on silicon' 152-RELEASE-NOTES-fw.md` ≥ 1.
    - `grep -c 'support_status' 152-RELEASE-NOTES-fw.md` == 0.
    - Every size figure in the file matches a Task 1 measurement — checked figure by figure and recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-RELEASE-NOTES-fw.md" python3 152-check-claims.py; echo "GATE rc=$?"; for n in 'sdp-relock' '999.28' '.hex' '28672' 'check_erase_no_vpp' 'FW_TAG_TBD' 'APP_TAG_TBD' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon'; do printf '%s => ' "$n"; grep -c "$n" 152-RELEASE-NOTES-fw.md; done; printf 'unguarded => '; grep -ci 'unguarded' 152-RELEASE-NOTES-fw.md; printf 'dispatch => '; grep -c 'check_dispatch' 152-RELEASE-NOTES-fw.md</automated>
  </verify>
  <done>The firmware body exists, is gate-clean with the command name present in the mandated withdrawal order, lists the measured assets with their boards, states the two size figures separately with the boundary named unguarded, names the control that actually guards the over-voltage hazard, and carries all three non-claims.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the draft and hand the placeholder contract to Plan 152-12 and Plan 152-18</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md` — the authored draft.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` — the sibling, to confirm the placeholder tokens match exactly.
  </read_first>
  <action>
Commit the draft as frozen and record the handoff contract:

- Every placeholder token with its line number, its replacement source command, and confirmation that
  the tokens are byte-identical to the ones the app body uses so one substitution pass covers both.
- The exact posting invocation Plan 152-18 will use, written out for review:
  `gh release edit FW_TAG_TBD --repo henols/firestarter --notes-file <path to this draft>`.
- The exact verification invocation:
  `gh release view FW_TAG_TBD --repo henols/firestarter --json body --jq '.body'` piped into `diff -u`
  against the draft, plus the body-length read as the 0-to-N non-vacuity guard.
- The measurement command for the current body length of the candidate release, so the guard has a
  measured starting value rather than an assumed zero.
- The wording-review items for the operator checkpoint in Plan 152-18: the two size figures'
  separation, the unguarded statement, the asset-to-board mapping and the no-board-selection caveat,
  and the ARM image's standing non-claim.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces status --porcelain -- .planning/phases/152-*/152-RELEASE-NOTES-fw.md` is empty.
    - The placeholder tokens in the firmware body are byte-identical to those in the app body — proven by `grep -o 'APP_TAG_TBD\|FW_TAG_TBD'` on both files producing the same token set.
    - The SUMMARY lists every placeholder with its line and read command, both `gh` invocations, the measured current body length, and the five wording-review items.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; git -C /workspaces status --porcelain -- .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md; echo "PORCELAIN-END"; grep -o 'APP_TAG_TBD\|FW_TAG_TBD' 152-RELEASE-NOTES-fw.md | sort -u; grep -o 'APP_TAG_TBD\|FW_TAG_TBD' 152-RELEASE-NOTES-app.md | sort -u; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The firmware body is committed clean, its placeholders match the app body's exactly, and the posting, verification and review contracts are handed forward.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md` — the frozen
  OUT-04 firmware body; a `_DEFAULT_TARGETS` member from Plan 152-13 onward; `_CAVEAT_RULES` entry =
  all three caveat labels

**Placeholder tokens consumed** (shared with Plan 152-08, filled by Plan 152-12)
- `FW_TAG_TBD`, `APP_TAG_TBD`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft → published release body | Posted verbatim in Plan 152-18. |
| recorded size figure → the only remaining control | The linker guard over the bootloader region was removed; the published number is now the only thing standing between a future build and a bricked bootloader. |
| document-sourced figure → published figure | All three Leonardo figures moved in 24 hours; inheriting one publishes a wrong number. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-34 | Denial of service | publishing a firmware image whose future growth could overwrite a user's USB bootloader | high | mitigate | The boundary figure is measured in Task 1 and published as UNGUARDED with its arithmetic; the removed-guard statement rests on a measured `platformio.ini` value, not an assumption. |
| T-152-35 | Repudiation | conflating the size-band headroom with the bootloader-boundary headroom | high | mitigate | Task 2 requires the two figures in separately-labelled sentences; the SUMMARY records both with their own arithmetic. |
| T-152-36 | Spoofing | stating an asset count from a document rather than from the release | medium | mitigate | Task 1 measures the asset list; the `.hex` grep count must equal the measured count. |
| T-152-37 | Spoofing | promising board-targeted flashing the updater does not do | medium | mitigate | Explicit prohibition; the notes state that the attached board is what gets flashed. |
| T-152-38 | Repudiation | naming a checker as the hazard control that structurally cannot be one | high | mitigate | `check_dispatch` grep count of 0 and `check_erase_no_vpp` count ≥ 1 as acceptance criteria. |
| T-152-39 | Repudiation | presenting deferred split-firmware work as queued | low | mitigate | Explicit prohibition; the constraint is named as unaddressed. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** `gh`, `git`, `grep` and stdlib Python only. |
</threat_model>

<verification>
- Gate green on the firmware draft via the env seam.
- Asset count in the draft equals the measured count; every size figure traceable to a Task 1 reading.
- Ten positive/negative greps as enumerated in Task 2.
- Placeholder tokens byte-identical across both release bodies.
- Draft committed clean; HEAD on the milestone branch.
</verification>

<success_criteria>
The firmware release body exists, is gate-clean, names the deferred command only as withdrawn with
Backlog 999.28, publishes measured asset and size figures with the two Leonardo numbers kept apart and
the boundary named unguarded, and predicts no version.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-09-SUMMARY.md` when done.

Record verbatim: the measured asset list with sizes, all three AVR environments' size figures, both
Leonardo headroom numbers with separate labels and arithmetic, the measured upload-size setting, every
placeholder with its line and read command, both `gh` invocations, the measured current body length,
and the five wording-review items.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary. If it refers to the deferred command, use the mandated withdrawal word
order.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-09-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
