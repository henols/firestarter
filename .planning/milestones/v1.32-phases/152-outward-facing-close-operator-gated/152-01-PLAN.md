---
phase: 152-outward-facing-close-operator-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control_second.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_as_shipped.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_bare_flag.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh21.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh11.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh12.md
autonomous: true
requirements: [OUT-05]

must_haves:
  truths:
    - "`python3 152-check-claims.py` with no argv and no env seam exits 0 and its PASS line names `152-CLAIM-CLASSES.md`."
    - "The gate rejects the specified planted violation (the pre-amendment criterion-1 wording, per D-11 / ROADMAP criterion 5) with the label `sdp-relock-as-shipped` and no other forbidden label."
    - "The gate permits every withdrawal phrasing in `fixtures/clean_control.md` — the same literal command string in the mandated word order (D-11, RESEARCH §C-4)."
    - "The narrowed `issue-closed` row still fires on gh#21, gh#11 and gh#12 while permitting D-05's required statement about gh#32 (RESEARCH §C-5)."
    - "An explicitly empty env seam yields zero targets and rc≠0 — never a silent fall-back to defaults."
    - "A `_DEFAULT_TARGETS` entry absent from disk makes the gate rc≠0 and the message NAMES the absent target."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md
    - .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py
    - .planning/phases/152-outward-facing-close-operator-gated/fixtures/
  key_links:
    - "`_HERE` is `__file__`-derived and every `_DEFAULT_TARGETS` entry is built from it — the 137 `_HERE`-resolves-to-a-sibling fail-open (RESEARCH §C-3) cannot recur."
    - "`_required_caveats_for()` fails CLOSED — an unknown basename gets the FULL caveat set."
    - "The env seam is `FIRESTARTER_CLAIMSCAN_TARGETS_152`; the phase-number suffix is what stops it retargeting 146's or 149's live gate."
  prohibitions:
    - "Do NOT port 137's `UNARMED: ... return 0` branch (`check_permitted_claims.py:303-312`) — an exit-0-on-nothing-scanned path is a green that proves nothing."
    - "Do NOT use a wildcard or glob in `_DEFAULT_TARGETS`."
    - "`152-CONTEXT.md`, `152-RESEARCH.md`, `152-DISCUSSION-LOG.md`, `152-VALIDATION.md`, `152-PATTERNS.md`, `152-CLAIM-GATE-TRANSCRIPTS.md`, `fixtures/*` and every `152-NN-PLAN.md` must NEVER be `_DEFAULT_TARGETS` members — they carry forbidden vocabulary by design."
    - "Do NOT widen `proven-unqualified`'s lookbehind past exactly the one `software-` prefix — widening silently permits bench-proven, datasheet-proven and silicon-proven."
    - "Do NOT add a proximity window, an exclusion region, or an inline allow-marker to `scan_text` — every match anywhere in a scanned file's text is a violation."
    - "Do NOT weaken any assertion to turn a leg green; a fix that turns RED green must be locator-only."
---

<objective>
Build the fail-provable claim gate OUT-05 requires, as a sibling of `149-check-claims.py`, and prove
it rejects the planted violation ROADMAP criterion 5 names — before any outward text exists to scan.

Purpose: OUT-05's criterion is "**seen to fail on a planted violation** before any pass is believed".
This plan creates the gate, its human-readable contract, and the fixtures that make the two new
forbidden classes and the one modified class provable in both directions. Per D-09 the gate has two
invocation modes over one pattern table; this plan lands the file mode.

Output: `152-CLAIM-CLASSES.md` (the gate's contract, and its first and only `_DEFAULT_TARGETS`
member at this point in the phase), `152-check-claims.py`, and seven fixtures.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This is a records-and-publication phase. Read only the ranges named in each task's `<read_first>`.

⚠ **Do NOT read `.planning/STATE.md` whole** — it is 339 KB with a ~52k-char single line. Use
`grep`/`sed` slices only, and give any gate that scans it `timeout 300`.

⚠ **Do NOT add `152-CONTEXT.md`, `152-RESEARCH.md`, `152-DISCUSSION-LOG.md`, `152-VALIDATION.md` or
`152-PATTERNS.md` to `_DEFAULT_TARGETS`.** All five carry forbidden vocabulary as discussion prose.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 152-CLAIM-CLASSES.md — the gate's contract and its first scan target</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 444-460 (`sed -n '444,460p'`) — Phase 152's five success criteria; criterion 5 enumerates the five forbidden claim classes and names the planted violation.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 232-310 (D-09, D-10, D-11, D-12) — the gate's two modes, the narrowing of criterion 5's pairing clause, and the three exempted phrasings of shipped command behaviour.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 723-931 (§C-4, §C-5, §C-6, §C-7) — the derived fifth-class pattern, the second collision, the caveat rows, the target list.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 764-772 (Pattern D) — cite forbidden claims by location and finding id, never reproduce them.
  </read_first>
  <action>
Create `152-CLAIM-CLASSES.md`: the human-readable contract for `152-check-claims.py`, and
deliberately the gate's FIRST and (for now) ONLY `_DEFAULT_TARGETS` member. It exists so the gate is
armed against a real, existing artifact from wave 1 rather than being RED-by-construction or
vacuously green.

Required content, in this order:

1. A header paragraph stating: this file is a `152-check-claims.py` scan target; it describes the
   forbidden claim classes by **label and location**, per Pattern D, and it deliberately does not
   reproduce any offending phrasing — the rejected phrasings live in
   `152-CLAIM-GATE-TRANSCRIPTS.md`, which is permanently NOT a scan target.
2. `## The five forbidden claim classes (ROADMAP criterion 5)` — one row per class, naming the gate
   label(s) that enforce it: (a) AT28C silicon validation → `at28c256-fixed`, `verified-on-silicon`,
   `silicon-verified`, `works-on-silicon`, `proven-on-silicon`, `confirmed-working`,
   `proven-unqualified`; (b) page-size validation on silicon → `page-size-proven`; (c) a `0x0D`
   graduation → `graduation`; (d) a `support_status` change → `support-status-change`; (e) the
   deferred deliberate-protection command named as shipped or available →
   `sdp-relock-as-shipped` and `sdp-relock-flag-as-shipped`. For class (e), describe the mechanism as
   "the command name is rejected unless a withdrawal predicate immediately follows it" and cite
   `152-RESEARCH.md` §C-4 plus `152-CLAIM-GATE-TRANSCRIPTS.md` §RED 1-2 for the eighteen tested
   phrasings. Do NOT write out an offending phrasing here.
3. `## The mandated word order for class (e)` — state that the command name comes first and the
   withdrawal predicate immediately after; state that any other order is rejected; state that this
   is the intended cost, the same cost 149 accepted when it mandated one spelling of its own claim
   word.
4. `## The two narrowings, and why each is a narrowing` — (i) `proven-unqualified` keeps 149's
   `(?<!software-)` lookbehind verbatim, because "software-proven and unvalidated on silicon" is this
   milestone's established vocabulary in three measured places (149's own required row,
   `153-RECORD.md` §"What was NOT proven", and `firestarter/README.md`'s Protocol Notes); state that
   widening past that one prefix would silently permit three other hyphenated forms, named.
   (ii) `issue-closed` drops `32` from its alternation, because D-05 REQUIRES this phase to state a
   measured 2026-08-08 fact and the inherited row blocked the natural phrasings of it; state that
   gh#21, gh#11 and gh#12 all still fire, and cite the three fixtures that prove it.
5. `## The three required-caveat rows` — `software-proven-unvalidated`, `no-at28c-part-tested`,
   `zero-d-stays-unverified`, each with its mandated sentence and the basenames it is required on.
6. `## What D-11 exempts` — statements of shipped, user-visible command behaviour are exempt from the
   pairing clause; claims about `0x0D` write-path correctness or validation status are not. Give the
   exempt examples D-11 itself gives.
7. `## What this gate does NOT discharge` — a green gate is not a wording review. D-03's per-artifact
   blocking operator checkpoint is a separate control and this file must say so.
8. The three mandated caveat sentences must each appear verbatim in this file, because its
   `_CAVEAT_RULES` entry requires all three:
   - the software-proven / unvalidated-on-silicon sentence;
   - the sentence stating no AT28C part was tested at any point in v1.32;
   - the sentence stating protocol `0x0D` stays UNVERIFIED in PROTOCOL-LEDGER.
   Their exact spellings are fixed by Task 2's `REQUIRED_CAVEAT_PATTERNS` — write Task 2 first if the
   spelling is unclear, or author them from `152-RESEARCH.md` §C-6's two verified-clean sentences.

Every claim in this file is about the gate's own mechanism. Do not make any claim about AT28C
silicon, the write path, or a protocol's validation status.
  </action>
  <acceptance_criteria>
    - `152-CLAIM-CLASSES.md` exists and is non-empty.
    - `grep -c 'sdp-relock-as-shipped' 152-CLAIM-CLASSES.md` ≥ 1 and `grep -c 'sdp-relock-flag-as-shipped' 152-CLAIM-CLASSES.md` ≥ 1.
    - `grep -c 'issue-closed' 152-CLAIM-CLASSES.md` ≥ 1.
    - All three mandated caveat sentences are present (proven by Task 2's gate run exiting 0 on this file with the full caveat set required).
    - `grep -c 'CLAIM-GATE-TRANSCRIPTS' 152-CLAIM-CLASSES.md` ≥ 1 — the offending phrasings are cited by location, not reproduced.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md; echo "exists rc=$?"; grep -c 'sdp-relock-flag-as-shipped' .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md</automated>
  </verify>
  <done>The gate's contract exists, names every forbidden label and every required-caveat row, and carries the three mandated caveat sentences.</done>
</task>

<task type="auto">
  <name>Task 2: Copy 149-check-claims.py to 152-check-claims.py and apply the seven enumerated edit sites</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-check-claims.py` — **read in full (531 lines)**. Its module docstring `:23-56` mandates the first four renames; `:103-108` states the rule that there is deliberately no exit-0-on-nothing-scanned branch.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 57-267 (§1) — the seven edit sites with the verbatim blocks to transcribe, plus the anti-pattern section at `:234-267`.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 679-931 (§C-2 through §C-7) — the rename table, the `_HERE` fail-open trap, the derived fifth-class pattern with its backtick-placement bug recorded, the `issue-closed` collision, the caveat rows, and the target-list ownership table.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py` lines 295-320 — the `UNARMED: ... return 0` branch. Read it to know what NOT to port.
  </read_first>
  <action>
Copy `149-check-claims.py` to `152-check-claims.py` and apply exactly these seven edit sites. Every
other line — imports, `_HERE`, `resolve_targets`'s argv/env/defaults precedence, the
`os.environ.get()` with NO default, the startup self-check, the never-vacuous and
fail-closed-on-missing guards in `main()`, `_required_caveats_for()`'s fail-CLOSED default,
`scan_text`, `_print_bucket`'s 20-entry display cap — transcribes VERBATIM. Do not re-invent any of
it. Do not port 137's `UNARMED: ... return 0` branch.

Edit site 1 — `_DEFAULT_TARGETS`: exactly ONE entry,
`os.path.join(_HERE, "152-CLAIM-CLASSES.md")`. Enumerated, never a glob. Keep the donor's comment
block above it and retarget its exclusion list to name, one by one: `152-CONTEXT.md`,
`152-RESEARCH.md`, `152-DISCUSSION-LOG.md`, `152-VALIDATION.md`, `152-PATTERNS.md`,
`152-CLAIM-GATE-TRANSCRIPTS.md`, `fixtures/*` and every `152-NN-PLAN.md` as permanently out, by
design and not by oversight. Add a comment recording the ordering trap: any entry absent from disk
makes this gate rc=1, so later plans extend this list only once their artifact exists on disk, and
the phase's final SUMMARY is scanned via positional argv.

Edit site 2 — the self-check prefix literal, in BOTH the `startswith` call and its failure message:
`"149-"` becomes `"152-"`.

Edit site 3 — the env seam, at all four sites the donor uses it (the module-level constant name, the
`os.environ.get` key string, the docstring mention, and the `resolve_targets` reference):
`FIRESTARTER_CLAIMSCAN_TARGETS_149` becomes `FIRESTARTER_CLAIMSCAN_TARGETS_152`. Keep the donor's
comment explaining that the suffix is the phase number, not the milestone, precisely so one phase's
seam cannot retarget another phase's live gate.

Edit site 4 — the module docstring: retarget it at **OUT-05** (not PGSZ-05) and at **D-03's
per-artifact blocking operator gates** as the review this gate does not discharge. Keep the donor's
four-rename discipline paragraph, its wildcard-target-trap paragraph, and its
no-exit-0-on-nothing-scanned paragraph, all retargeted. Add one new paragraph recording the
backtick-placement bug from `152-RESEARCH.md` §C-4: in the fifth-class pattern the optional backtick
must live INSIDE the lookahead, never in the consumed part, or the engine backtracks off it, inspects
it, the alternation fails, and the class permits nothing — measured as 7/7 false HITs on a first
derivation. Also record the `152-VALIDATION.md` exclusion as a fourth non-target the donor's list did
not have.

Edit site 5 — `FORBIDDEN_PATTERNS`: transcribe all seventeen donor rows, then make three changes.
  (a) ADD `sdp-relock-as-shipped`: the derived negative-lookahead pattern in
      `152-RESEARCH.md` §C-4, transcribed exactly as written there. Above it, a comment recording
      that a bare pattern would reject ROADMAP criterion 4's own mandated sentence, that a verb
      allow-list fails OPEN by construction, that a Python lookbehind cannot be used because it must
      be fixed-width while the natural prose order puts the predicate after the name, and that the
      derivation was measured 11/11 reject and 7/7 allow on 2026-08-21.
  (b) ADD `sdp-relock-flag-as-shipped`: the same lookahead, but matching the bare flag
      `--sdp-relock` with a **fixed-width negative lookbehind** `(?<!write )` so it cannot
      double-fire on the same text as (a). The lookbehind is what preserves per-label leg isolation
      in the paired suite's meta test; say so in the comment. Note in the comment that
      `152-RESEARCH.md` records this companion as designed-but-untested (Assumptions Log A3) and that
      this plan tests it in both directions.
  (c) MODIFY `issue-closed`: drop `32` from the alternation, leaving `gh#(?:21|11|12)`. Copy the
      COMMENT SHAPE of the donor's `proven-unqualified` narrowing comment: state the measurement
      (the inherited row blocks the natural phrasings of gh#32's closure), state why this is a
      narrowing to the true claim class rather than a loosening (the forbidden class is claiming an
      issue this milestone did not close is closed; gh#32's closure is a measured 2026-08-08 fact,
      not a claim), and state that gh#21/#11/#12 must all still fire and are proven by three
      fixtures.
  Keep `proven-unqualified` VERBATIM, lookbehind unchanged.

Edit site 6 — `REQUIRED_CAVEAT_PATTERNS`: three rows, replacing the donor's one.
  (i) `software-proven-unvalidated` — the donor's row verbatim.
  (ii) `no-at28c-part-tested` — matches the mandated sentence "No AT28C part was tested at any point
       in v1.32."; write the pattern as whitespace-tolerant around the words `no`, `AT28C`, `part`,
       `was`, `tested`, case-insensitive.
  (iii) `zero-d-stays-unverified` — matches "Protocol `0x0D` stays UNVERIFIED in PROTOCOL-LEDGER.";
       write the pattern to tolerate an optional backtick after the protocol id and before
       `UNVERIFIED`, case-insensitive, `0x0[Dd]`.
  Splitting D-11's non-claim into two independently-enforced rows (ii) and (iii) is a deliberate
  refinement of `152-RESEARCH.md` §C-6's single-row proposal: each half then has its own fixture and
  its own named failure. Record that reason in a comment. `_ALL_CAVEAT_LABELS` derives from the table
  and must not be restated.
  ⚠ Record in a comment that D-11's word "once" is a cardinality constraint this mechanism cannot
  express — presence-only is what is enforced, "once" is a wording convention for the human review
  (RESEARCH Assumptions Log A7). Do not imply a count is enforced when it is not.

Edit site 7 — `_CAVEAT_RULES`: pre-populate EVERY basename this phase will ever scan, now, so no
later plan has to edit this map and no two wave-3 plans collide on this file. Extra entries for
files that do not exist yet are inert — the map is keyed on basename and touches no disk.
  - `152-CLAIM-CLASSES.md` → all three labels
  - `152-GH12-COMMENT.md`, `152-GH21-COMMENT.md`, `152-GH11-COMMENT.md` → `{no-at28c-part-tested}`
  - `152-RELEASE-NOTES-app.md`, `152-RELEASE-NOTES-fw.md`, `152-LEDGER.md` → all three labels
  - `152-MERGE-RECORD.md` → the empty frozenset, with a comment: it is a captured git/gh handoff
    record, not a claim register, mirroring the donor's own transcript exemption — and unlike the
    donor's, this one is a LIVE target, so the exemption mechanism is behaviourally exercised.
  - `152-CLAIM-GATE-TRANSCRIPTS.md` → the empty frozenset, inert, never a target, kept for the same
    reason the donor keeps it.
  - `152-01-SUMMARY.md` through `152-20-SUMMARY.md` → `{software-proven-unvalidated}` each.
Then run the gate and confirm rc=0 with `152-CLAIM-CLASSES.md` named in the PASS line. If it is RED
on a missing caveat, fix `152-CLAIM-CLASSES.md`'s sentence spelling — never the pattern.
  </action>
  <acceptance_criteria>
    - `python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` exits 0 and its stdout contains `152-CLAIM-CLASSES.md`.
    - `grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_149' 152-check-claims.py` returns 0; `grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_152' 152-check-claims.py` ≥ 4.
    - `grep -c '"149-"' 152-check-claims.py` returns 0; `grep -c '"152-"' 152-check-claims.py` ≥ 2 (the `startswith` call and its failure message).
    - `grep -c 'UNARMED' 152-check-claims.py` returns 0 — 137's exit-0-on-nothing-scanned branch is not ported.
    - `python3 -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('m','.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); labels=[l for l,_ in m.FORBIDDEN_PATTERNS]; assert len(labels)==19, labels; assert 'sdp-relock-as-shipped' in labels; assert 'sdp-relock-flag-as-shipped' in labels; assert len(m.REQUIRED_CAVEAT_PATTERNS)==3; print('OK', len(labels))"` prints `OK 19`.
    - The fifth class rejects the specified planted phrasing and permits the withdrawal phrasing — proven inline: an in-process check that the `sdp-relock-as-shipped` pattern searches TRUE on the pre-amendment criterion-1 wording and FALSE on a withdrawal sentence in the mandated order.
    - The bare-flag row rejects a bare-flag availability phrasing and does NOT fire on text where the flag is immediately preceded by the write verb — proving leg isolation is preserved.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152="" python3 152-check-claims.py` exits non-zero and prints the never-vacuous message.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=/nonexistent/does-not-exist.md python3 152-check-claims.py` exits non-zero and the message NAMES `/nonexistent/does-not-exist.md`.
  </acceptance_criteria>
  <verify>
    <automated>python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py; echo "GATE rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="" python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py; echo "EMPTY rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152=/nonexistent/does-not-exist.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py; echo "MISSING rc=$?"</automated>
  </verify>
  <done>`152-check-claims.py` exists with 19 forbidden rows and 3 required-caveat rows, exits 0 on its single default target, exits non-zero on an empty seam and on a missing target, and carries none of the donor's phase-149 literals.</done>
</task>

<task type="auto">
  <name>Task 3: Author the seven fixtures for the two added and one modified label, probing each before commit</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control_second.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_as_shipped.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_bare_flag.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh21.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh11.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh12.md</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/fixtures/planted_at28c256_fixed.md`, `fixtures/planted_missing_caveat.md`, `fixtures/clean_control.md` — the exact fixture shape: a two-line HTML header naming the gate and stating "NOT a phase artifact — never add to `_DEFAULT_TARGETS`", then the required caveats, then ONE plant.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 391-438 (§3) — the fixture shape verbatim, and the measured fixture-authoring hazard.
    - `.planning/ROADMAP.md` line 451 (`sed -n '451,451p'`) — criterion 1's parenthetical, which quotes the pre-amendment wording. That quoted wording IS the specified plant.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 723-851 (§C-4, §C-5) — the eleven reject phrasings and seven allow phrasings, and the four `issue-closed` verdicts.
  </read_first>
  <action>
Author seven fixtures in `fixtures/`. Every one carries the donor's two-line HTML header (retargeted
at `152-check-claims.py`) and, unless it is a missing-caveat plant, ALL THREE mandated caveat
sentences — because a fixture basename is absent from `_CAVEAT_RULES` and therefore held to the
fail-closed FULL set. Each plant must fail for EXACTLY ONE reason.

⚠ **Probe every candidate before committing it.** 149 measured that several candidate wordings
tripped an unintended second label — writing a forbidden label's own name into a fixture's HTML
comment can itself contain a forbidden substring. Run each file through the gate via the env seam and
read the bucketed output before `git add`.

1. `planted_sdp_relock_as_shipped.md` — **the specified planted violation.** Its plant is the
   pre-amendment criterion-1 wording this roadmap itself carried until 2026-08-20, taken verbatim
   from `ROADMAP.md:451`'s parenthetical. Must trip `sdp-relock-as-shipped` and NOTHING else — in
   particular not `sdp-relock-flag-as-shipped`, which the fixed-width lookbehind suppresses because
   the write verb immediately precedes the flag.
2. `planted_sdp_relock_bare_flag.md` — one bare-flag availability phrasing where the flag is NOT
   preceded by the write verb. Must trip `sdp-relock-flag-as-shipped` and nothing else.
3. `planted_issue_closed_gh21.md`, `planted_issue_closed_gh11.md`, `planted_issue_closed_gh12.md` —
   one per issue, each asserting that issue is closed / resolved / fixed respectively, proving the
   narrowed row still fires on all three. Each must trip `issue-closed` and nothing else.
4. `clean_control.md` — the two-sided control. It must contain, and be CLEAN with: all three
   mandated caveat sentences; the deferred command's literal name in the mandated withdrawal word
   order, naming Backlog 999.28; and D-05's required statement about gh#32 in the natural
   past-tense phrasing that the inherited row used to block. Its being clean is the proof that both
   narrowings permit exactly what this phase must say.
5. `clean_control_second.md` — a second clean control using DIFFERENT permitted phrasings: another
   withdrawal predicate from RESEARCH §C-4's allow set, the parenthetical gh#32 form, and the two
   D-11-exempted statements of shipped user-visible command behaviour (that standalone erase is now
   available on this protocol, and that write no longer performs a blank check on it) — proving the
   exemption D-11 grants is real and not accidentally blocked by `now-works` or `at28c256-fixed`.

Do NOT put the string `AT28C256` adjacent to the words fixed / works / now anywhere in a control —
that trips `at28c256-fixed`, which is a donor row, not this plan's.
  </action>
  <acceptance_criteria>
    - Both controls exit 0 through the gate: `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/fixtures/clean_control.md python3 152-check-claims.py` → rc 0; same for `clean_control_second.md`.
    - `planted_sdp_relock_as_shipped.md` exits non-zero, stdout contains `forbidden phrase match [sdp-relock-as-shipped]`, does NOT contain `[sdp-relock-flag-as-shipped]`, and does NOT contain `missing required caveat`.
    - `planted_sdp_relock_bare_flag.md` exits non-zero, stdout contains `forbidden phrase match [sdp-relock-flag-as-shipped]` and does NOT contain `[sdp-relock-as-shipped]`.
    - Each of the three `planted_issue_closed_*.md` exits non-zero with `forbidden phrase match [issue-closed]` and no other forbidden label in the bucket.
    - `python3 152-check-claims.py` (defaults, no seam) still exits 0 — the fixtures are not `_DEFAULT_TARGETS` members.
    - Every fixture's first line contains `never add to _DEFAULT_TARGETS`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; for f in fixtures/clean_control.md fixtures/clean_control_second.md; do FIRESTARTER_CLAIMSCAN_TARGETS_152="$f" python3 152-check-claims.py > /dev/null; echo "$f rc=$?"; done; for f in fixtures/planted_sdp_relock_as_shipped.md fixtures/planted_sdp_relock_bare_flag.md fixtures/planted_issue_closed_gh21.md fixtures/planted_issue_closed_gh11.md fixtures/planted_issue_closed_gh12.md; do FIRESTARTER_CLAIMSCAN_TARGETS_152="$f" python3 152-check-claims.py | grep -o 'forbidden phrase match \[[a-z0-9-]*\]' | sort -u; echo "--- $f"; done; python3 152-check-claims.py; echo "DEFAULTS rc=$?"</automated>
  </verify>
  <done>Seven fixtures exist; both controls are clean; each plant trips exactly its own label and fails for exactly one reason; the default-target run is still green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-CLASSES.md`
- `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control_second.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_as_shipped.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_sdp_relock_bare_flag.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh21.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh11.md`
- `.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_issue_closed_gh12.md`

**New gate symbols**
- Forbidden pattern labels ADDED: `sdp-relock-as-shipped`, `sdp-relock-flag-as-shipped`
- Forbidden pattern label MODIFIED: `issue-closed` (the `32` alternative dropped)
- Required-caveat labels: `software-proven-unvalidated` (inherited verbatim), `no-at28c-part-tested`
  (new), `zero-d-stays-unverified` (new)
- Env seam: `FIRESTARTER_CLAIMSCAN_TARGETS_152`
- Module constants: `_HERE`, `_DEFAULT_TARGETS`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
  `_ALL_CAVEAT_LABELS`, `_CAVEAT_RULES`
- Functions: `_required_caveats_for`, `_assert_default_targets_are_local`, `resolve_targets`,
  `scan_text`, `_print_bucket`, `main`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plan text → outward artifact | Every sentence authored here can reach an irreversible public surface later in the phase. |
| env → gate | `FIRESTARTER_CLAIMSCAN_TARGETS_152` is an untrusted-by-construction input to target resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-01 | Tampering | `152-check-claims.py` target resolution | high | mitigate | Never-vacuous guard + fail-closed-on-missing + no exit-0-on-nothing-scanned branch; all three proven by acceptance criteria in Task 2. |
| T-152-02 | Tampering | one phase's env seam retargeting another phase's live gate | medium | mitigate | Phase-number-suffixed seam `FIRESTARTER_CLAIMSCAN_TARGETS_152` plus the `startswith("152-")` self-check in both the comparison and the printed message. |
| T-152-03 | Elevation of privilege | `_required_caveats_for()` returning the empty set for an unrecognised basename | high | mitigate | Fail-CLOSED default transcribed verbatim from the donor; an unknown basename gets the FULL caveat set. |
| T-152-04 | Information disclosure | a fixture's own text exonerating the plant it is meant to prove | medium | mitigate | Probe every candidate fixture through the gate before `git add`; assert per-label leg isolation in Task 3's criteria. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** The gate is stdlib-only (`os`, `re`, `sys`); pytest is already present. No `152-RESEARCH.md` Package Legitimacy row is exercised. |
</threat_model>

<verification>
- `python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0, PASS line names `152-CLAIM-CLASSES.md`.
- Nineteen forbidden labels, three required-caveat labels, introspected in-process.
- Both controls clean; five plants each RED for exactly one reason with per-label isolation.
- Empty seam → rc≠0; missing target → rc≠0 with the target NAMED.
</verification>

<success_criteria>
The gate exists, is armed against a real existing artifact, has been seen to reject the planted
violation ROADMAP criterion 5 names, and permits the withdrawal sentence ROADMAP criterion 4
mandates. No exit-0-on-nothing-scanned path exists.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-01-SUMMARY.md` when done.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST therefore
contain the literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid
every forbidden label's vocabulary: do not use the claim word for "proved" outside that compound; do
not write that something now works or should now work; do not write "confirmed working",
"datasheet-conformant", "verified on silicon" or "silicon-verified"; do not assert that gh#21, gh#11
or gh#12 is closed, resolved or fixed; do not name the deferred deliberate-protection command except
in the mandated withdrawal word order. Cite any forbidden phrasing by `file:line` and label, never
reproduce it.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-01-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
