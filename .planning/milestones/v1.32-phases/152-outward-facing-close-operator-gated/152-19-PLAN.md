---
phase: 152-outward-facing-close-operator-gated
plan: 19
type: execute
wave: 13
depends_on: ["152-18"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-LEDGER.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py
  - .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py
autonomous: true
requirements: [OUT-05]

must_haves:
  truths:
    - "`152-LEDGER.md` exists, carries every v1.32 claim with its oracle and its explicit non-claim, and every row's non-claim cell is non-empty."
    - "Every HEAD, tag, comment id and gate count in the ledger is live-captured in this plan, with its command and timestamp, and never reused from a prior document's citation."
    - "The D-05, D-11 and D-15 amendments are registered in the ledger, per the decision that the ledger carries the register rather than a separate document."
    - "`152-LEDGER.md` is a hard-coded `_DEFAULT_TARGETS` member and the gate exits 0 over the full eight-target set."
    - "The arming leg asserts membership of all eight expected basenames."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-LEDGER.md
  key_links:
    - "The ledger is where the narrowed pairing discipline now lives, which is exactly why it must itself be gate-scanned — an unscanned internal document is the one most likely to state a claim generously."
    - "`_CAVEAT_RULES` already carries this basename with all three caveat labels, pre-populated in Plan 152-01, so this plan edits only `_DEFAULT_TARGETS`."
  prohibitions:
    - "Do NOT reuse any HEAD, SHA, tag, comment id or count from a prior document's citation. Capture each live and say so."
    - "Do NOT add any `152-NN-SUMMARY.md` to `_DEFAULT_TARGETS` here. Plan 152-20 owns that extension."
    - "Do NOT write a claim row whose non-claim cell is empty — a row without one does not satisfy the requirement."
    - "Do NOT reproduce a forbidden phrasing in the ledger. Cite it by location and label, per this phase's citation discipline."
    - "Do NOT claim any AT28C silicon result, page-size validation on silicon, a `0x0D` graduation, or a `support_status` change; do NOT cite the chip-level support field as evidence about the write path."
    - "Do NOT present the deferred command as anything other than withdrawn, and prefer naming the backlog item so the command string need not appear in the ledger at all."
    - "Do NOT post anything in this plan."
---

<objective>
Write the honesty ledger, arm the gate at it, and prove the gate green over every claim-bearing artifact
including the ledger itself.

Purpose: this phase narrowed criterion 5's pairing clause, and the narrowing moved the per-claim pairing
discipline into this document. That makes the ledger both the record of what may be claimed and the one
document most likely to state a claim generously — which is why it is a gate target.

Output: `152-LEDGER.md`, and a gate armed over eight artifacts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
All five public acts are complete. This plan records what was claimed, on what evidence, and what was
explicitly not claimed — and then proves the record itself clean.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-capture every HEAD, tag, handle and gate count the ledger will cite</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-LEDGER.md</files>
  <read_first>
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md` lines 1-45 — the live-capture attribution style and the Oracle block that names each gate and suite with its own count, including how it reports a red-by-construction leg honestly.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 494-546 (§5) — the heading skeleton, the four-column claim table's header and its non-empty-fourth-cell rule, the claim key, and the cross-reference block with its closing porcelain check.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 292-309 (D-12) — what the ledger must contain and why it is a gate target.
  </read_first>
  <action>
Capture, now, every value the ledger will cite. Each one carries its command and a timestamp and the
statement that it was measured here rather than reused from a prior document's citation.

- The three repository HEADs: `git -C /workspaces rev-parse HEAD`,
  `git -C /workspaces/firestarter rev-parse HEAD`,
  `git -C /workspaces/firestarter_app rev-parse HEAD`. Also the two published-branch SHAs:
  `git -C <each sub-repo> rev-parse origin/beta` after a fetch.
  ⚠ If a sibling artifact in this phase recorded a different SHA for the same repository, **state the
  divergence and say that both readings are correct for the moment each was taken** — that is the
  donor's own discipline and this phase's HEADs moved several times.
- Both cut tags, re-read from the release list now, with their body lengths.
- The three comment ids and URLs, re-read from the live issues, plus each issue's current state and
  comment count.
- Every gate and suite with its own count, each run live in this plan:
  the claim gate over its current default set (record the target count and the exit code);
  the paired suite (record the passed count);
  the configuration guard (record its exit code and the live key value);
  the record-corrections gate with a 300-second timeout ceiling (record its exemption tally and exit
  code);
  the firmware source-scan gates that guard this milestone's erase work (record each by name with its
  exit code).
- `git status --porcelain` in all three repos, so the ledger's cross-reference block can close with the
  porcelain statement the donor uses.

Record every value in a scratch capture the ledger is written from, and reproduce the capture in the
SUMMARY.
  </action>
  <acceptance_criteria>
    - Three HEAD SHAs and two published-branch SHAs recorded, each with its command and timestamp.
    - Both cut tags re-read with their current body lengths, both non-zero.
    - Three comment ids and URLs recorded, with each issue's state and comment count.
    - Each gate and suite recorded by name with its own count and exit code; the record-corrections gate run with a 300-second timeout ceiling.
    - Any SHA divergence against a sibling artifact in this phase is stated as a divergence, with both readings and their moments.
    - `git status --porcelain | grep -v '^??'` is empty in all three repos.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; date -u +%FT%TZ; for r in /workspaces /workspaces/firestarter /workspaces/firestarter_app; do printf '%s HEAD ' "$r"; git -C $r rev-parse HEAD; done; for r in firestarter firestarter_app; do git -C /workspaces/$r fetch origin --quiet; printf '%s origin/beta ' "$r"; git -C /workspaces/$r rev-parse origin/beta; done; for r in firestarter firestarter_app; do gh release list --repo henols/$r --limit 3; done; for i in 12 21 11; do gh issue view $i --repo henols/firestarter_prom --json state,comments --jq "{issue:$i, state:.state, n:(.comments|length), last:(.comments[-1]|{id,url})}"; done; python3 152-check-claims.py; echo "GATE rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; timeout 300 python3 ../130-*/check_record_corrections.py; echo "RECORDGATE rc=$?"; python3 /workspaces/firestarter/scripts/check_erase_no_vpp.py; echo "ERASE-VPP rc=$?"; for r in /workspaces /workspaces/firestarter /workspaces/firestarter_app; do git -C $r status --porcelain | grep -v '^??'; done; echo "PORCELAIN-END"</automated>
  </verify>
  <done>Every value the ledger cites is live-captured with its command and timestamp, every gate is re-run with its own count, and any SHA divergence against a sibling artifact is stated rather than resolved silently.</done>
</task>

<task type="auto">
  <name>Task 2: Write 152-LEDGER.md — the four-column claim table, the amendment register, and the negative space</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-LEDGER.md</files>
  <read_first>
    - Task 1's capture — the only source for every SHA, tag, id and count in this document.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md` — the full section skeleton, the claim key, the four-column table header, the negative-space section, the process-failures section and the closing "what no test, gate or review can close" section.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md` — the shorter claim-class shape, for rows that do not need the longer treatment.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1354-1377 (§G-1) — the decision that the amendments are recorded as labelled blocks in place PLUS a register entry in this ledger, and not in a separate correction document.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 623-664 (§B-10, §B-11) — the transcribe-don't-paraphrase not-proven list and the three now-false claims this phase must not repeat.
    - `.planning/PROJECT.md` §"Current Milestone: v1.32" — the Evidence ceiling block, which is binding and is what the ledger's ceiling section restates.
  </read_first>
  <action>
Write `152-LEDGER.md` on the donor's skeleton. Sections, in this order:

1. **Header with the live captures.** The three HEADs, both published-branch SHAs, both cut tags with
   their body lengths, the three comment ids and URLs — each with its command, its timestamp and the
   statement that it was measured here and never reused from a prior document's citation. Include any
   divergence statement from Task 1.
2. **The claim key** — reuse the donor's three levels unchanged, including the rule that the forbidden
   level appears in this ledger only as a citation of what is not claimed, never as prose asserting it.
3. **The Oracle block** — every gate and suite named with its own count and exit code, from Task 1's
   live runs. Report anything red-by-construction honestly, in the donor's manner, with its own
   assertion message quoted.
4. **The evidence ceiling** — restated from the project record, because it is binding and is the frame
   for every row below.
5. **The four-column claim table** — `Class | Permitted wording | Evidence (measured, source) | Explicitly does NOT prove`.
   One row per claim this phase made public, at minimum: the write-path blank-check removal; the
   standalone erase step and its timing constant; the protection-read command and its refusal-first
   behaviour; the report-provenance fix; the numeric database values; the page-size seam and its
   no-behaviour-change consequence for the part in the community threads; and the deferred command's
   withdrawal. **Every row's fourth cell must be non-empty** — that cell IS the explicit non-claim, and
   a row without one does not satisfy the requirement. Where the narrowed pairing clause exempts a
   statement of shipped, user-visible command behaviour, say so in the row rather than omitting the
   cell.
6. **The amendment register** — one entry per amendment this phase landed: the criterion-2 amendment,
   the criterion-5 narrowing, the requirements-document amendments, the stale record sites, and the
   disproven-premise correction. Each entry names the file, the amendment's dated marker, what the
   pre-amendment text said (by location, not reproduced where reproducing it would state the forbidden
   claim), and the decision id that authorised it. This is the register half of the recording
   mechanism; the labelled blocks in place are the other half.
7. **Negative space — every carry-forward** — including: the deferred command remains deferred for a
   second release, tracked as the backlog item, with the standing instruction that a future promotion
   must reverse the fifth gate class in the same change that lands the feature; the AT28C write-path
   backlog item stays open; the split-firmware constraint is unaddressed and is not on any roadmap; the
   protection-class counting method's ambiguity and which figure is therefore publishable; and the
   requirements-count reconciliation.
8. **Process failures recorded here, not only technical ones** — at minimum: two prior release-notes
   artifact sets were authored and never posted, which is why this phase's boundary put posting inside
   the phase; a published body still announced a deleted command for weeks; and the class-size figures
   published by a prior phase did not reproduce.
9. **What no test, gate or review can close** — the ceiling stated as a limit rather than a caveat: no
   part of the affected family was on a bench at any point, the protocol's ledger status is unchanged,
   and a green claim gate is not a wording review.
10. **The cross-reference block** — the sibling artifacts this ledger composes with, cited without
    copying data, closed with the porcelain statement from Task 1.

The document must carry all three mandated non-claim sentences verbatim, because its caveat rule
requires all three. Cite any forbidden phrasing by location and label; never reproduce it.
  </action>
  <acceptance_criteria>
    - `152-LEDGER.md` exists with all ten sections.
    - The four-column table's header matches the donor's exactly, and **every** data row has a non-empty fourth cell — checked row by row and the row count recorded.
    - Every SHA, tag, comment id and count in the file appears in Task 1's capture; no value is present that was not captured.
    - `grep -c 'measured here' 152-LEDGER.md` ≥ 1 — the live-capture discipline is stated.
    - `grep -c 'No AT28C part was tested at any point in v1.32' 152-LEDGER.md` == 1 and `grep -c 'software-proven and unvalidated on silicon' 152-LEDGER.md` ≥ 1 and the protocol-stays-unverified sentence is present.
    - The amendment register has one entry per amendment landed in Plans 152-03 and 152-04, each naming its dated marker.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-LEDGER.md python3 152-check-claims.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; test -s 152-LEDGER.md; echo "exists rc=$?"; grep -c 'Explicitly does NOT prove' 152-LEDGER.md; grep -c 'measured here' 152-LEDGER.md; grep -c 'No AT28C part was tested at any point in v1.32' 152-LEDGER.md; grep -c 'software-proven and unvalidated on silicon' 152-LEDGER.md; grep -c 'AMENDED 2026-08-21' 152-LEDGER.md; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-LEDGER.md" python3 152-check-claims.py; echo "SEAM rc=$?"</automated>
  </verify>
  <done>The ledger exists with every claim paired to a non-empty non-claim, every cited value live-captured, the amendment register complete, the negative space and process failures recorded, and the file gate-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add the ledger to _DEFAULT_TARGETS, extend the arming leg, and prove the gate green over all eight</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py, .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` — the current seven-entry `_DEFAULT_TARGETS` and the pre-populated caveat map.
    - `.planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py` — the arming leg's literal membership assertion, added in Plan 152-13.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 896-931 (§C-7) — the ordering trap and the rule that a caveat entry must accompany every added basename.
  </read_first>
  <action>
Add exactly one entry to `_DEFAULT_TARGETS`: the ledger, as an explicit `os.path.join(_HERE, ...)` on
its own line, bringing the list to eight. Do not touch `_CAVEAT_RULES` — the ledger's basename was
pre-populated with all three caveat labels in Plan 152-01, which is why this is a one-line edit.

Extend the arming leg's literal membership set from seven basenames to eight so a future omission fails
the suite rather than passing quietly.

Then run and record:
- the claim gate with no argv and no seam — rc 0, PASS line naming all eight artifacts, untruncated;
- the paired suite — fully green with its count line;
- the caveat-completeness leg specifically, proving every default basename still has a caveat entry.

⚠ Do not add any SUMMARY here. A `_DEFAULT_TARGETS` entry that does not exist on disk makes the gate
non-zero, and the phase's SUMMARY files are added in the final plan with the last one scanned via
positional argv.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - Introspection shows exactly 8 `_DEFAULT_TARGETS` entries, one of which is the ledger, every one existing on disk with dirname equal to the phase directory and a basename starting with `152-`.
    - `python3 152-check-claims.py` exits 0 and its PASS line names all eight artifacts.
    - `python3 -m pytest test_check_claims_152.py -q -o addopts=""` is fully green, including the strengthened arming leg and the caveat-completeness leg.
    - No `152-NN-SUMMARY.md` appears in `_DEFAULT_TARGETS`.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-claims.py; echo "GATE rc=$?"; python3 -c "import importlib.util,os; s=importlib.util.spec_from_file_location('m','152-check-claims.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); print('n=',len(m._DEFAULT_TARGETS)); [print(os.path.basename(t), os.path.isfile(t)) for t in m._DEFAULT_TARGETS]"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The ledger is a hard-coded gate target, the gate is green over all eight artifacts including the ledger itself, and the arming leg asserts all eight basenames literally.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-LEDGER.md` — the v1.32 honesty ledger;
  a `_DEFAULT_TARGETS` member from this plan onward; `_CAVEAT_RULES` entry = all three caveat labels
  (pre-populated by Plan 152-01)

**Modified files**
- `152-check-claims.py` — `_DEFAULT_TARGETS` extended from 7 to 8 entries
- `test_check_claims_152.py` — the arming leg's literal membership set extended to 8

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an internal record → the milestone's own truth | The ledger is where the narrowed pairing discipline lives; an unscanned internal document is the one most likely to state a claim generously. |
| a prior document's citation → this document's value | Every SHA in this phase has moved multiple times; inheriting one publishes a stale fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-86 | Repudiation | a claim row with an empty non-claim cell | high | mitigate | Every row's fourth cell asserted non-empty row by row, with the row count recorded; the requirement is restated in `must_haves.prohibitions`. |
| T-152-87 | Spoofing | citing a stale SHA, tag, id or count | high | mitigate | Task 1 live-captures every value with command and timestamp, and Task 2's criteria forbid any value not in that capture. |
| T-152-88 | Tampering | the ledger itself overclaiming because it is not scanned | high | mitigate | Task 3 makes it a hard-coded gate target and proves the gate green over it. |
| T-152-89 | Repudiation | recording the amendments only as in-place blocks, with no register | medium | mitigate | The register is a required section with one entry per amendment, each naming its dated marker. |
| T-152-90 | Tampering | adding a not-yet-existing SUMMARY and reddening the gate | high | mitigate | Explicit prohibition plus an acceptance criterion asserting no SUMMARY is in the list. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Every ledger-cited value live-captured with command and timestamp; divergences stated.
- All ten ledger sections present; every claim row's non-claim cell non-empty; all three mandated
  non-claim sentences present.
- Amendment register complete against Plans 152-03 and 152-04.
- Gate green over eight artifacts including the ledger; suite fully green; arming leg asserts eight.
</verification>

<success_criteria>
The milestone's claims and non-claims are recorded in one document, every cited value was measured in
this plan, and the document that carries the pairing discipline is itself under the gate that enforces
it.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-19-SUMMARY.md` when done.

Record verbatim: the full Task 1 capture, the ledger's claim-row count with confirmation that every
fourth cell is non-empty, the eight-entry target list, the untruncated PASS line and the suite count.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; cite any forbidden phrasing by location and label.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-19-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
