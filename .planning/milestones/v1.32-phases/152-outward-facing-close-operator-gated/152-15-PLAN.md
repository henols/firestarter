---
phase: 152-outward-facing-close-operator-gated
plan: 15
type: execute
wave: 9
depends_on: ["152-14"]
files_modified: []
autonomous: false
requirements: [OUT-02]

must_haves:
  truths:
    - "The gh#21 comment is POSTED and the stored body is byte-identical to the frozen draft."
    - "The comment count on gh#21 advanced by exactly 1."
    - "gh#21 is still OPEN after commenting; gh#32 was not reopened; neither issue body was edited."
    - "The posted-mode gate run over the stored body exits 0."
    - "The fresh-run request is answerable at the moment it is posted, because the published branch now contains the provenance fix — verified by reading the branch, not inferred."
  artifacts: []
  key_links:
    - "Criterion 2's 'answerable because the report now identifies its firmware' is only true after Plan 152-11's merge; Task 1 re-verifies that on the published branch immediately before posting."
    - "Posted-mode temp files are written under the SAME basename as the draft, because the caveat lookup is basename-keyed and fails closed."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Task 1's configuration guard is a partial control, not a substitute for the operator."
    - "Do NOT close gh#21. Do NOT edit its body. Do NOT edit any existing comment on it."
    - "Do NOT reopen gh#32 and do NOT comment on gh#32 — it is closed as a fold and stays closed."
    - "Do NOT post more than one comment. This plan posts exactly one."
    - "Do NOT re-run the posting command after a failure without first re-reading the comment count."
    - "Do NOT use `updatedAt` as a landed or edited oracle."
    - "Do NOT alter the draft in this plan. Wording changes go back through the gate before this plan proceeds."
---

<objective>
Post the gh#21 comment, and prove both that the stored body is the frozen draft and that the request it
makes is answerable at the moment it becomes public.

Purpose: OUT-02's criterion turns on a single conditional — the fresh run is worth asking for *because*
the report now identifies its firmware. That became true only when Plan 152-11 merged. This plan
re-verifies it on the published branch and then posts.

Output: one public comment on gh#21.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** The checkpoint in Task 2 is the only human
review before this public act, and approval given for the gh#12 post in Plan 152-14 explicitly does not
carry here — that is the whole reason D-03 requires a separate gate per artifact.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-post checks, plus the published-branch verification criterion 2 depends on</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-15-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH21-COMMENT.md` — the frozen draft. Read it in full; this is the last read before it is public.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-06-SUMMARY.md` — the draft's blob SHA, the recorded gh#21 baseline, and both `gh` invocations.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-12-SUMMARY.md` — the four published-branch content reads taken after the merge, which this task re-runs.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1071-1119 (§D-3) — the three citable hooks the draft uses, so the checkpoint can check them against the reporter's own paste.
  </read_first>
  <action>
Five checks, in this order. Record every command and its output.

1. `python3 .planning/phases/152-*/152-check-not-auto.py` — must exit 0, else **stop**.
2. `python3 .planning/phases/152-*/152-check-claims.py` on defaults — rc 0; then through the env seam on
   the gh#21 draft alone — rc 0. Record both PASS lines.
3. `python3 -m pytest .planning/phases/152-*/test_check_claims_152.py -q -o addopts=""` — fully green,
   count line recorded.
4. **The criterion-2 verification, re-run here rather than trusted from Plan 152-12.** Fetch and read
   the published host branch: the firmware-identity field must no longer be a hardcoded null, and the
   new protection-read command must be reachable. Also read the published firmware branch and confirm
   the standalone erase arm is present. Record each matched line. If any still shows the pre-merge
   shape, **stop** — the sentence the comment is about to make would be false.
5. Capture the live baseline: gh#21's state and comment count, plus the id and creation date of the
   current last comment. Separately confirm gh#32's state is still closed and record it — the draft
   states that fact and it must be true when posted. Record the draft's blob SHA, labelled as an intent
   oracle only.

Nothing proceeds on a red.
  </action>
  <acceptance_criteria>
    - `152-check-not-auto.py` exits 0 with the live configuration value recorded.
    - The gate exits 0 on defaults and on the gh#21 draft; both PASS lines recorded.
    - The paired suite is fully green with its count line recorded.
    - `git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -n fw_board_identity` shows the field is populated rather than a hardcoded null, and the matched lines are recorded.
    - `git -C /workspaces/firestarter_app show origin/beta:firestarter/main.py | grep -c 'lock.status'` ≥ 1.
    - `git -C /workspaces/firestarter show origin/beta:src/proms/eeprom_28c.cpp | grep -c 'eeprom28c_erase_execute'` ≥ 1.
    - `gh issue view 21 --json state --jq '.state'` returns `OPEN`; the baseline count is recorded; the last comment's id and date are recorded.
    - `gh issue view 32 --json state --jq '.state'` returns `CLOSED`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; python3 152-check-claims.py; echo "GATE-DEFAULTS rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-GH21-COMMENT.md" python3 152-check-claims.py; echo "GATE-DRAFT rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; git -C /workspaces/firestarter_app fetch origin --quiet; git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -n fw_board_identity; git -C /workspaces/firestarter_app show origin/beta:firestarter/main.py | grep -c 'lock.status'; git -C /workspaces/firestarter fetch origin --quiet; git -C /workspaces/firestarter show origin/beta:src/proms/eeprom_28c.cpp | grep -c 'eeprom28c_erase_execute'; gh issue view 21 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1]|{id,createdAt})}'; gh issue view 32 --repo henols/firestarter_prom --json state --jq '.state'</automated>
  </verify>
  <done>All gates are green, the published branch is confirmed to contain the work the comment's request depends on, and the pre-post baselines for gh#21 and gh#32 are captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review — authorise the gh#21 post</name>
  <what-built>
`152-GH21-COMMENT.md` — the gh#21 comment with gh#32 folded, built on the reporter's own pasted report
body. The claim gate is green on it, the paired suite is green, and the published branch has been
verified to contain the provenance fix the comment's request depends on. Nothing has been posted.
Approving this checkpoint posts one public comment.
  </what-built>
  <how-to-verify>
1. Read `152-GH21-COMMENT.md` in full.
2. Check the two quotations against the issue's own body: the firmware-identity value and the erase
   step's reason string must be quoted as the reporter pasted them, not paraphrased.
3. Check that the comment separates the **host-side** blank-check-step fix from the **firmware-side**
   write-path fix, and states plainly that the write-path fix only takes effect on matching firmware.
4. Check the ask names **both** install halves — the host pre-release and the firmware update — and
   states the reason both are needed.
5. Check the gh#32 sentence: it must state the fold as the measured fact it is, and must not read as
   reopening it or as closing this thread.
6. Check the not-proven section: it should transcribe the list of what the write-path work did not
   establish, including the erase timing constant being a family maximum and the wall-clock wait being
   structurally rather than temporally asserted.
7. Confirm nothing in it claims a silicon result, and nothing offers the page-size work as an
   explanation for the reported failure.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: post to gh#21" or describe the wording changes required first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post, prove the stored body is ours, and record the durable handle</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-15-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-06-SUMMARY.md` — the recorded posting and verification invocations.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1146-1171 (§D-5) — the sound-oracle table and the rejection of `updatedAt`.
  </read_first>
  <action>
Post exactly once, then prove it landed as authored.

**Post.** `gh issue comment 21 --repo henols/firestarter_prom --body-file <the frozen draft>`. Record
the command and its output. On error, do **not** re-run before re-reading the comment count.

**Prove identity.** Write the stored last-comment body to a temp file **named `152-GH21-COMMENT.md`** —
the same basename as the draft, because the gate's caveat lookup is basename-keyed and fails closed —
then `diff -u <the frozen draft> <that file>`. The diff must be empty. Paste the result.

**Prove non-vacuity.** Re-read the comment count and assert it equals the baseline plus exactly 1.
Record both numbers.

**Capture the durable handle.** Record the new comment's id, URL, creation timestamp and author.

**Prove the state assertions.** gh#21 must still be `OPEN`. gh#32 must still be `CLOSED` with the same
`closedAt` — a comment on gh#21 must not have disturbed it. The issue body must be unchanged, and no
pre-existing comment edited: compare ids and creation dates against Task 1's capture.

**Posted-mode gate.** Run the gate through the env seam over the same-basename temp file. It must exit
0. Paste the PASS line.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - The body diff between the frozen draft and the stored last-comment body produces no output.
    - The comment count equals the Task 1 baseline plus exactly 1.
    - `gh issue view 21 --json state --jq '.state'` returns `OPEN`.
    - `gh issue view 32 --json state,closedAt` matches the Task 1 capture exactly.
    - The new comment's id, URL, timestamp and author are recorded.
    - The posted-mode gate run over the same-basename temp file exits 0.
    - The pre-existing comments' ids and creation dates are unchanged from Task 1's capture.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; T=$(mktemp -d); gh issue view 21 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body' > "$T/152-GH21-COMMENT.md"; diff -u 152-GH21-COMMENT.md "$T/152-GH21-COMMENT.md"; echo "BODYDIFF rc=$?"; gh issue view 21 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1]|{id,url,createdAt,author:.author.login})}'; gh issue view 32 --repo henols/firestarter_prom --json state,closedAt; FIRESTARTER_CLAIMSCAN_TARGETS_152="$T/152-GH21-COMMENT.md" python3 152-check-claims.py; echo "POSTEDGATE rc=$?"; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>One comment is posted, its stored body diffs clean against the frozen draft, the count advanced by exactly one, gh#21 is still OPEN and gh#32 is untouched, and the posted-mode gate is green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** One public comment on gh#21.

**Recorded values handed forward to `152-LEDGER.md` (Plan 152-19)**
- The comment id, URL, creation timestamp and author
- The body-diff result and the comment-count delta
- The posted-mode gate PASS line
- The four published-branch content reads that make criterion 2's conditional true

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen draft → public comment | Irreversible; subscribers receive the original by email. |
| published branch state → the comment's own conditional | The comment's central request is only honest if the published branch contains the provenance fix. |
| gh#21 → gh#32 | gh#32 is closed as a fold; a comment on gh#21 must not disturb it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-65 | Elevation of privilege | an auto or chained run auto-approving this checkpoint | critical | mitigate | The configuration guard runs first and fails closed; the prohibition is restated. ⚠ Partial control; residual risk stated. |
| T-152-66 | Spoofing | asking for a run "answerable because" a fix the published branch does not have | critical | mitigate | Task 1 re-reads the published branch for all four pieces of work and stops on any pre-merge shape. |
| T-152-67 | Repudiation | assuming the post landed as authored | high | mitigate | Stored body re-read and diffed; count delta as non-vacuity guard; `updatedAt` rejected. |
| T-152-68 | Repudiation | reopening or otherwise disturbing the folded issue | medium | mitigate | gh#32's state and `closedAt` asserted unchanged against the Task 1 capture; commenting on it is prohibited. |
| T-152-69 | Tampering | a duplicated comment after a partially-succeeded post | medium | mitigate | Explicit prohibition on re-running before re-reading the count. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Auto guard, defaults gate, draft gate and paired suite green before the checkpoint.
- Four published-branch content reads confirm the comment's conditional.
- Body diff empty; count delta exactly 1; gh#21 `OPEN`; gh#32 unchanged.
- Posted-mode gate green over a same-basename temp file.
</verification>

<success_criteria>
OUT-02 is discharged: gh#21 carries a comment whose stored text is provably the reviewed text, whose
central request is true at the moment it is public, and whose posting left both issues' states intact.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-15-SUMMARY.md` when done.

Record verbatim: all five pre-post check results including the four branch reads, the baseline and
post-post comment counts, the body diff result, the comment id/URL/timestamp/author, the gh#32
state comparison, and the posted-mode PASS line.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary — do not write that gh#21, gh#11 or gh#12 is closed, resolved or fixed;
when recording gh#32's state, use the phrasing proven clean in `fixtures/clean_control.md`.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-15-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
