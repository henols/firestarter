---
phase: 152-outward-facing-close-operator-gated
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OUT-01, OUT-04, OUT-05]

must_haves:
  truths:
    - "OUT-01's bullet no longer states that the withdrawn deliberate-protection command is what `enable` returns as; it states a withdrawal, for a second release, naming Backlog 999.28."
    - "OUT-04's bullet no longer announces the deferred command as shipped; it requires the withdrawal to be stated explicitly, naming Backlog 999.28."
    - "OUT-05's bullet enumerates all five forbidden claim classes including the deferred command named as shipped or available, and carries D-11's narrowing of the pairing clause."
    - "The Coverage block's in-scope count and phase list are corrected and the 34-versus-35 off-by-one is reconciled with its arithmetic stated, not papered over."
    - "Every `database.py` line citation in this file matches the committed tree, with the drift recorded."
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "`REQUIREMENTS.md` is NOT a `check_record_corrections.py` default target — the bounded-diff assertion is the only structural guard on this edit."
    - "OUT-04's pre-amendment bullet is itself an instance of the fifth forbidden claim class; correcting it here is what stops the drafting plans in wave 3 quoting a violation out of their own requirements document."
  prohibitions:
    - "Do NOT use the `gsd-tools` requirements verb — `_normalizeMd` reformats the WHOLE file. Hand-edit only."
    - "Do NOT flip the OUT-01…OUT-05 checkboxes in this plan. They flip in Plan 152-20, after each requirement is actually satisfied. Flipping them here would assert five public acts that have not happened."
    - "Do NOT edit `.planning/ROADMAP.md` or `.planning/PROJECT.md` — Plan 152-03 owns both and runs in this same wave."
    - "Do NOT introduce a `recordscan:supersedes ... lines=N` marker."
    - "Do NOT restate an unreconciled requirement count. If the live checkbox count and the derived arithmetic disagree, stop and record both rather than choosing one silently."
    - "Do NOT add or remove a requirement ID. RELOCK-07's absence is deliberate and the gap between RELOCK-06 and RELOCK-08 must not be filled."
---

<objective>
Bring `REQUIREMENTS.md`'s OUT family and Coverage block up to what actually ships, so the five wave-3
drafts are written from a requirements document that does not itself contain the overclaim the gate
exists to reject.

Purpose: D-05 records that OUT-01 and OUT-04 still carry pre-amendment text — ROADMAP amended both on
2026-08-20 with the Phase 150 deferral and the requirements text never followed. **OUT-04's own bullet
is therefore currently an instance of OUT-05's fifth forbidden claim class.** RESEARCH §A-11 also
found the Coverage block stale by a whole phase, with a residual off-by-one against the file's own
footer.

Output: one hand-edited record file with every amendment dated, attributed, and diff-bounded.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **Hand-edit.** The `gsd-tools` requirements verb runs `_normalizeMd` over the whole file.

⚠ `REQUIREMENTS.md` is not a `152-check-claims.py` scan target and not a
`check_record_corrections.py` target. The bounded diff plus the dated markers are the only structural
proofs available here — both are mandatory.

⚠ Capture the baseline first: `git -C /workspaces diff --numstat -- .planning/REQUIREMENTS.md`
(expected empty) and the per-family checkbox counts (Task 2).
</context>

<!-- planner-discipline-allow: 25 in v1 scope -->
<!-- planner-discipline-allow: 34 in scope -->

<tasks>

<task type="auto">
  <name>Task 1: Amend OUT-01, OUT-02, OUT-04 and OUT-05 to the post-deferral, post-153 text</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 262-286 (`sed -n '262,286p'`) — the OUT family as it stands, including OUT-01's and OUT-04's pre-amendment text.
    - `.planning/ROADMAP.md` lines 449-460 (`sed -n '449,460p'`) — the five amended success criteria, which are the authority for this edit. Criteria 1 and 4 were amended 2026-08-20; criteria 2 and 5 are amended by Plan 152-03 in this same wave, so read `152-CONTEXT.md` D-05 and D-11 for their substance rather than depending on the sibling plan's write.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 123-144 (D-05), 262-291 (D-10, D-11), 332-346 (D-14) — the amendment class, the narrowing, and OUT-01's negative cases.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 96-108 (the Phase Requirements table, which states each OUT requirement **as amended**) and lines 591-616 (§B-8, the matched-firmware finding OUT-02 turns on).
    - `.planning/REQUIREMENTS.md` §"Out of Scope" — the `write --sdp-relock` deferral row, the "Reading protection state on `0x0D`/SDP parts" row (the gap OUT-01 must admit to), and the "Re-litigating RELOCK's verify-failure polarity" row (decided, not reopened).
  </read_first>
  <action>
Hand-edit four bullets in `REQUIREMENTS.md` §"Outward-Facing Close (OUT)". Use the same
**AMENDED [date] ([cause])** shape ROADMAP's criteria 1 and 4 already carry, retaining the
pre-amendment wording in a trailing parenthetical labelled as the negative case the gate must catch.
Do not flip any checkbox.

**OUT-01** — replace the clause stating what `enable` returns as. The amended text: the owed gh#12
reply is posted, and it states both halves plainly — `disable`'s behaviour survives as `write`'s
automatic, default-on auto-unlock, declinable via `--skip-sdp-unlock`; and `enable` returns as
**nothing in this release**, withdrawn since v1.30 and still tracked as Backlog **999.28**, so the ask
is half-answered **for a second release**. It must not describe the v1.30 gap as if the original ask
had been satisfied all along. Retain the pre-amendment clause in the parenthetical, labelled as
criterion 1's own negative case and as the phrasing OUT-05's fifth class is built to reject. Attribute
`AMENDED 2026-08-21 (152-CONTEXT.md D-05; ROADMAP criterion 1 amended 2026-08-20)`.

**OUT-02** — keep its substance and add the two findings that make it discharge-able: the fresh
`dev test` run is answerable **because the report now identifies its firmware**, and the request must
name **both** halves of the install — the pre-release host install **and** `firestarter fw --install`.
State the reason in the bullet: Phase 153's write-path fix is in the **firmware**, and the host's
`dev test` write step is still flagless, so against older firmware the failure reproduces exactly as
before. Attribute `AMENDED 2026-08-21 (152-CONTEXT.md D-05; 152-RESEARCH.md §B-8)`.

**OUT-04** — replace the clause announcing the deferred command as shipped. The amended text: the
release notes announce **`lock-status`** as shipped in the version that actually contains it, correct
the forward-looking wording v1.30 left behind, and state the deferred deliberate-protection command's
**withdrawal** explicitly — naming Backlog **999.28** — rather than announcing it or leaving it
unmentioned. Retain the pre-amendment clause in the parenthetical, labelled as the second instance of
criterion 5's fifth forbidden class living inside this project's own records. Attribute
`AMENDED 2026-08-21 (152-CONTEXT.md D-05; ROADMAP criterion 4 amended 2026-08-20)`.

**OUT-05** — expand it to match ROADMAP criterion 5 as amended: a **fail-provable** claim gate, seen
to fail on a planted violation before any pass is believed, rejects (a) AT28C silicon validation,
(b) page-size validation on silicon, (c) a `0x0D` graduation, (d) a `support_status` change, and
(e) the deferred deliberate-protection command named as a shipped or available command; and every
permitted `0x0D` claim is paired with its explicit non-claim — **narrowed per D-11** to claims about
`0x0D` write-path correctness or validation status, with statements of shipped, user-visible command
behaviour explicitly exempt. Add the standing instruction the deferral carries: a future promotion of
Backlog 999.28 **must reverse class (e) in the same change that lands the feature**, or the gate will
reject the very release notes announcing it. Attribute
`AMENDED 2026-08-21 (152-CONTEXT.md D-11)`.

⚠ When writing OUT-01's and OUT-04's retained pre-amendment parentheticals, the offending phrasing is
being deliberately preserved as the negative case. Label each one explicitly as such, in the same
sentence, so a future reader cannot mistake a retained negative case for a live requirement.
  </action>
  <acceptance_criteria>
    - `grep -c 'AMENDED 2026-08-21' .planning/REQUIREMENTS.md` == 4.
    - `sed -n '262,300p' .planning/REQUIREMENTS.md | grep -c '999.28'` ≥ 3 — OUT-01, OUT-04 and OUT-05 each name the backlog item.
    - `sed -n '262,300p' .planning/REQUIREMENTS.md | grep -c 'fw --install'` ≥ 1 — OUT-02 names the second install half.
    - `sed -n '262,300p' .planning/REQUIREMENTS.md | grep -c 'negative case'` ≥ 2 — both retained pre-amendment clauses are labelled as such.
    - `sed -n '262,300p' .planning/REQUIREMENTS.md | grep -c 'graduation'` ≥ 1, `grep -c 'support_status'` ≥ 1, `grep -c 'page-size'` ≥ 1 — OUT-05 enumerates the classes.
    - `grep -c '^- \[ \] \*\*OUT-0' .planning/REQUIREMENTS.md` == 5 — no checkbox flipped in this plan.
    - `grep -c 'RELOCK-07' .planning/REQUIREMENTS.md` is unchanged from its pre-edit value.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'AMENDED 2026-08-21' /workspaces/.planning/REQUIREMENTS.md; grep -c '^- \[ \] \*\*OUT-0' /workspaces/.planning/REQUIREMENTS.md; sed -n '262,300p' /workspaces/.planning/REQUIREMENTS.md | grep -c 'fw --install'</automated>
  </verify>
  <done>All four OUT bullets carry dated, attributed amendments with their pre-amendment clauses retained and explicitly labelled as negative cases; no checkbox moved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the Coverage block, reconcile the in-scope count, and fix the database.py citations</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 425-450 (`sed -n '425,450p'`) — the Coverage block and the Phase map.
    - `.planning/REQUIREMENTS.md` last 15 lines (`tail -15`) — the footer, whose "25 → 34 in scope, 42 defined" is the second half of the off-by-one.
    - `.planning/REQUIREMENTS.md` lines 314-345 (`sed -n '314,345p'`) — the ERASE bullets carrying `database.py:621` at `:318` and `:342`, and `database.py:629-630` at `:321`.
    - `firestarter_app/firestarter/database.py` lines 580-645 (`sed -n '580,645p'`) — the committed tree. Measure the exclusion-tuple line and the reversal comment's span live; do not inherit either number.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 424-450 (§A-11) and the §"Open Questions" entry 3 at line 1886 onward — the measured 35-by-checkbox versus 34-by-footer conflict and the instruction not to paper it over.
  </read_first>
  <action>
Three hand-edits.

**(a) The Coverage block.** It currently reads "33 total as authored; 25 in v1 scope … Mapped to
phases: 25 ✓ (Phases 147–149, 151, 152)" — both numbers and the phase list predate Phase 153.

First **re-count live**: count `- [x]` and `- [ ]` requirement checkboxes per family (DATA, ERASE,
LOCK, OUT, PGSZ, PROV) and count the `⏸` deferred RELOCK rows. Record the six per-family numbers and
the deferred number verbatim in the SUMMARY.

Then reconcile against this derivation, which resolves RESEARCH Open Question 3 and must be stated in
the file rather than asserted: 33 requirements as authored (PROV 6 + DATA 6 + PGSZ 5 + RELOCK 7 +
LOCK 4 + OUT 5); minus the 7 RELOCK requirements deferred 2026-08-20 leaves **26** in scope, not the
25 the block records — the single-unit undercount is DATA-06, which was **retained and re-homed** to
Phase 151 rather than deferred with its family, and was evidently subtracted once too often; plus the
9 ERASE requirements added 2026-08-20 gives **35 in scope**; 35 + 7 deferred = **42 defined**, which
the footer already states correctly. So the corrected block reads 42 defined, **35 in scope**, 7
deferred to Backlog 999.28, 0 unmapped, mapped to Phases **147, 148, 149, 151, 152, 153**.

⚠ If the live checkbox count does not equal 35, **stop and record both numbers with the discrepancy
named** rather than choosing one. Do not restate an unreconciled number, and do not adjust the
checkboxes to make the arithmetic work.

Also correct the **footer**'s "25 → 34 in scope" with the same reconciliation, as a dated labelled
correction that names the off-by-one and its cause rather than silently overwriting the digit.

**(b) The Phase map table.** Confirm the Phase 153 row exists (it does, as measured) and that its
requirements cell reads ERASE-01…09. No edit if already correct — record that as verified.

**(c) The `database.py` citations at `:318` and `:342`.** Both cite `:621`. Replace them with the line
measured live in this task, and add a labelled correction note recording the drift history: `:621`
was the pre-153 line, `:620` was Phase 153's own measurement, and the committed tree now reads the
measured line — the same citation has drifted three times, so the note records the history rather
than silently overwriting the number. Also confirm `:321`'s `database.py:629-630` against the live
tree and correct it if it has moved.
  </action>
  <acceptance_criteria>
    - `sed -n '425,445p' .planning/REQUIREMENTS.md | grep -c '25 in v1 scope'` == 0.
    - `sed -n '425,445p' .planning/REQUIREMENTS.md | grep -c '35'` ≥ 1 and `grep -c '42'` ≥ 1.
    - `sed -n '425,445p' .planning/REQUIREMENTS.md | grep -c '153'` ≥ 1 — the phase list gains 153.
    - `tail -15 .planning/REQUIREMENTS.md | grep -c '34 in scope'` == 0.
    - `grep -c 'database.py:621' .planning/REQUIREMENTS.md` == 0.
    - `grep -cE 'database\.py:6[0-9][0-9]' .planning/REQUIREMENTS.md` ≥ 2 and every cited number equals a line measured live in this task.
    - The live per-family checkbox counts are recorded in the SUMMARY and sum to the number written into the Coverage block.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '425,445p' /workspaces/.planning/REQUIREMENTS.md | grep -c '25 in v1 scope'; grep -c 'database.py:621' /workspaces/.planning/REQUIREMENTS.md; grep -cE 'database\.py:6[0-9][0-9]' /workspaces/.planning/REQUIREMENTS.md; for f in DATA ERASE LOCK OUT PGSZ PROV; do printf "%s " "$f"; grep -cE "^- \[[x ]\] \*\*$f-" /workspaces/.planning/REQUIREMENTS.md; done</automated>
  </verify>
  <done>The Coverage block and footer state a reconciled 35-in-scope / 42-defined with the off-by-one's cause named, the phase list includes 153, and every `database.py` citation matches the committed tree with its drift recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the amendments landed surgically and the traceability table is intact</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §traceability table (`grep -n '^| OUT-0' .planning/REQUIREMENTS.md`) — the OUT rows, which must remain `Pending` until Plan 152-20.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-VALIDATION.md` — the fourth validation question: a positive `grep -c` on each dated marker PAIRED with a bounded-diff assertion.
  </read_first>
  <action>
Run `git -C /workspaces diff --numstat -- .planning/REQUIREMENTS.md` and record the added/deleted
counts verbatim in the SUMMARY. The expected band is added+deleted **between 1 and 70** — four bullet
amendments with retained parentheticals, a Coverage block, a footer correction and three citation
fixes. A count above the band means a whole-file `_normalizeMd` reformat happened: revert and
hand-edit again. A count of zero means nothing landed. Do **not** phrase this as "the file is
byte-unchanged except…".

Assert the traceability table is structurally intact: the same number of requirement rows as before
the edit, all five OUT rows still `Pending`, and no requirement ID added or removed. The v1.32
milestone is not complete and this plan must not make the record say otherwise.

After committing, run `git -C /workspaces rev-parse --abbrev-ref HEAD` and confirm the branch is
still `gsd/v1.32-at28c-write-path-root-cause-report-provenance` — `gsd-tools query commit`'s
unanchored milestone regex has been measured to switch branches.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces diff --numstat HEAD~1 -- .planning/REQUIREMENTS.md` (post-commit) shows added+deleted ≥ 1 and ≤ 70.
    - `grep -c '^| OUT-0' .planning/REQUIREMENTS.md` == 5 and `grep -cE '^\| OUT-0[1-5] \|.*\| Pending' .planning/REQUIREMENTS.md` == 5.
    - `grep -cE '^- \[[x ]\] \*\*(DATA|ERASE|LOCK|OUT|PGSZ|PROV)-' .planning/REQUIREMENTS.md` equals the pre-edit value.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>git -C /workspaces diff --numstat -- .planning/REQUIREMENTS.md; grep -c '^| OUT-0' /workspaces/.planning/REQUIREMENTS.md; grep -cE '^\| OUT-0[1-5] \|.*\| Pending' /workspaces/.planning/REQUIREMENTS.md; grep -cE '^- \[[x ]\] \*\*(DATA|ERASE|LOCK|OUT|PGSZ|PROV)-' /workspaces/.planning/REQUIREMENTS.md; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The amendments are proven present and surgical, the traceability table is intact with all five OUT rows still Pending, and HEAD is on the milestone branch.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**Modified files** (hand-edited, no `gsd-tools` verb)
- `.planning/REQUIREMENTS.md` — OUT-01, OUT-02, OUT-04, OUT-05 bullet amendments; the Coverage block;
  the footer's in-scope count; the `database.py` citations at `:318`, `:321`, `:342`

**New record markers**
- `AMENDED 2026-08-21 (152-CONTEXT.md D-05; ROADMAP criterion 1 amended 2026-08-20)` — OUT-01
- `AMENDED 2026-08-21 (152-CONTEXT.md D-05; 152-RESEARCH.md §B-8)` — OUT-02
- `AMENDED 2026-08-21 (152-CONTEXT.md D-05; ROADMAP criterion 4 amended 2026-08-20)` — OUT-04
- `AMENDED 2026-08-21 (152-CONTEXT.md D-11)` — OUT-05

**Resolved research question**
- RESEARCH Open Question 3 — the in-scope count reconciles to **35** (42 defined, 7 deferred), with
  the 25/34 undercount attributed to DATA-06 having been subtracted with its deferred family despite
  being retained and re-homed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements document → outward draft | Wave 3's drafters read this file for what each OUT requirement demands; an uncorrected bullet is an overclaim handed to a public artifact. |
| tooling → record file | The `gsd-tools` requirements verb reformats the whole file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-13 | Repudiation | OUT-04's own bullet instructing a drafter to announce a deferred command as shipped | critical | mitigate | This plan runs in wave 1, before any draft exists; the amendment is asserted by a dated-marker grep and the retained clause is explicitly labelled a negative case. |
| T-152-14 | Tampering | a whole-file `_normalizeMd` reformat destroying unrelated requirement text | high | mitigate | Hand-edit only, plus a bounded-diff band of 1..70 changed lines and a requirement-row count assertion. |
| T-152-15 | Spoofing | flipping the OUT checkboxes before the five public acts happen | high | mitigate | Explicit prohibition plus an acceptance criterion asserting all five OUT rows are still `Pending` and all five bullets still unchecked. |
| T-152-16 | Repudiation | restating an unreconciled requirement count as fact | medium | mitigate | Task 2 requires a live re-count, states the reconciling arithmetic in the file, and stops rather than choosing if the count disagrees. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Four dated `AMENDED 2026-08-21` markers present.
- All five OUT bullets still unchecked and all five traceability rows still `Pending`.
- Coverage block and footer state a reconciled 35 in scope / 42 defined with 153 in the phase list.
- No `database.py:621` citation remains; substituted numbers were measured live.
- Bounded diff 1..70 changed lines; requirement-row count unchanged.
</verification>

<success_criteria>
The requirements document no longer contains the fifth forbidden claim class, states what actually
ships for a second release, and carries a reconciled scope count with 153 in the phase list.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-04-SUMMARY.md` when done.

Record verbatim: the bounded-diff `--numstat` output, the six live per-family checkbox counts and the
deferred count, the reconciled in-scope arithmetic, the live-measured `database.py` line numbers, and
which Phase-map items were verified already-correct rather than edited.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary. In particular, when describing what OUT-01's and OUT-04's retained
parentheticals contain, cite them by `REQUIREMENTS.md` line number and by the gate label they
exemplify — do NOT reproduce the offending phrasing in the SUMMARY.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-04-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
