---
phase: 152-outward-facing-close-operator-gated
plan: 02
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_proven_unqualified.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_graduation.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_support_status_change.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_at28c256_fixed.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_page_size_proven.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_software_proven.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_no_at28c.md
  - .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_unverified.md
  - .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py
  - .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md
autonomous: true
requirements: [OUT-05]

must_haves:
  truths:
    - "`python3 -m pytest test_check_claims_152.py -q -o addopts=\"\"` reports a non-zero passed count and zero failures, and the count line is visible."
    - "Every ADDED or MODIFIED forbidden label has a committed fixture that trips exactly that label and no other, and fails for the forbidden phrase rather than a missing caveat."
    - "Each of the three required-caveat rows has its own missing-caveat plant that fails for exactly that caveat label."
    - "All five ROADMAP-criterion-5 forbidden classes have a committed plant proven RED."
    - "`152-CLAIM-GATE-TRANSCRIPTS.md` carries the literal command, the literal stdout and the literal EXIT= line for every RED block — pasted, not described."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py
    - .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md
  key_links:
    - "Every behavioural leg is `subprocess`-driven, so a passing suite proves the GATE, not an in-process copy of its tables."
    - "The suite's basename is `test_check_claims_152.py`, distinct from every sibling phase's same-shaped suite — pytest's default `prepend` import mode collides on a repeated basename run from `/workspaces`."
    - "`152-CLAIM-GATE-TRANSCRIPTS.md` is NOT a `_DEFAULT_TARGETS` member and a leg proves it."
  prohibitions:
    - "Do NOT name the suite `test_check_claims_v132.py` — that basename is live in Phase 149 and would be an import collision, not a new suite."
    - "Do NOT use an in-process import for any behavioural leg; the by-path importer is for introspection legs only."
    - "Do NOT reword a RED block in the transcript to remove its forbidden vocabulary — that destroys the evidence the transcript exists to preserve."
    - "Do NOT add `152-CLAIM-GATE-TRANSCRIPTS.md` to `_DEFAULT_TARGETS`; every RED block would make the gate permanently fail against its own proof of itself working."
    - "If a leg is RED, do NOT weaken its assertion. A pre-authored leg can be structurally unreachable; the fix must be locator-only."
---

<objective>
Prove the gate built in Plan 152-01 with a paired fixture suite and commit the plant-and-revert
evidence, so that no later pass is believed on faith.

Purpose: ROADMAP criterion 5 requires the gate to be **seen to fail on a planted violation** before
any pass is believed, and `152-VALIDATION.md`'s third validation question requires three independent
legs: a committed transcript with the RED pasted, a fixture suite with per-label leg isolation, and
the arming legs. This plan lands all three.

Output: eight donor-class fixtures, `test_check_claims_152.py`, and `152-CLAIM-GATE-TRANSCRIPTS.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ `addopts` in `firestarter_app/pyproject.toml` is `-ra -q`. **Doubling `-q` hides the count line.**
Always `-o addopts=""` when a count matters.

⚠ Do NOT read `.planning/STATE.md` whole (339 KB, one ~52k-char line).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the eight donor-class fixtures — the four inherited forbidden classes and the three caveat rows</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_proven_unqualified.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_graduation.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_support_status_change.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_at28c256_fixed.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_page_size_proven.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_software_proven.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_no_at28c.md, .planning/phases/152-outward-facing-close-operator-gated/fixtures/planted_missing_caveat_unverified.md</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/fixtures/planted_proven_unqualified.md`, `planted_graduation.md`, `planted_support_status_change.md`, `planted_at28c256_fixed.md`, `planted_page_size_proven.md`, `planted_missing_caveat.md` — the six donors to adapt.
    - `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md` — Plan 152-01's control, for the exact spelling of the three mandated caveat sentences.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` — `REQUIRED_CAVEAT_PATTERNS` and `FORBIDDEN_PATTERNS`, so each plant is written against the live patterns.
  </read_first>
  <action>
Adapt the five donor plants and author three new missing-caveat plants. Retarget every HTML header at
`152-check-claims.py` and swap 149's single caveat sentence for this phase's three.

Forbidden-class plants (each carries all three caveat sentences, so it fails for exactly one reason —
the forbidden phrase):
1. `planted_proven_unqualified.md` — a bare unqualified use of the claim word for "proved", so the
   `proven-unqualified` row's narrowed lookbehind is exercised from the failing side.
2. `planted_graduation.md` — a protocol-`0x0D` graduation claim. ROADMAP criterion 5 class (c).
3. `planted_support_status_change.md` — a `support_status` change claim. Class (d).
4. `planted_at28c256_fixed.md` — an AT28C256-is-fixed claim. Class (a).
5. `planted_page_size_proven.md` — a page-size-validated-on-silicon claim. Class (b).

Missing-caveat plants (each carries ZERO forbidden phrases and omits EXACTLY ONE caveat sentence, so
each fails for exactly one caveat label):
6. `planted_missing_caveat_software_proven.md` — omits the software-proven / unvalidated-on-silicon
   sentence, carries the other two.
7. `planted_missing_caveat_no_at28c.md` — omits the "no AT28C part was tested" sentence, carries the
   other two.
8. `planted_missing_caveat_unverified.md` — omits the "protocol `0x0D` stays UNVERIFIED" sentence,
   carries the other two.

⚠ Probe every candidate through the gate via the env seam BEFORE `git add`. 149 measured that a
fixture's own HTML header can contain a forbidden substring and trip an unintended second label.
Read the bucketed output, not just the exit code.

⚠ Fixture 6 is the delicate one: it must omit the software-proven compound while still not using the
bare claim word anywhere, or it will fail for two reasons at once (a missing caveat AND
`proven-unqualified`). Write around the word entirely.
  </action>
  <acceptance_criteria>
    - Each of fixtures 1-5 exits non-zero with exactly one distinct `forbidden phrase match [<label>]` label in the bucket, and stdout does NOT contain `missing required caveat`.
    - Each of fixtures 6-8 exits non-zero with `missing required caveat`, names exactly one caveat label, and stdout contains NO `forbidden phrase match` line.
    - `python3 152-check-claims.py` (defaults, no seam) still exits 0.
    - Every fixture's first line contains `never add to _DEFAULT_TARGETS`.
    - `ls fixtures/ | wc -l` returns 15.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; for f in fixtures/planted_proven_unqualified.md fixtures/planted_graduation.md fixtures/planted_support_status_change.md fixtures/planted_at28c256_fixed.md fixtures/planted_page_size_proven.md; do echo "== $f"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$f" python3 152-check-claims.py | grep -oE 'forbidden phrase match \[[a-z0-9-]*\]|missing required caveat' | sort -u; done; for f in fixtures/planted_missing_caveat_software_proven.md fixtures/planted_missing_caveat_no_at28c.md fixtures/planted_missing_caveat_unverified.md; do echo "== $f"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$f" python3 152-check-claims.py | grep -oE 'forbidden phrase match \[[a-z0-9-]*\]|missing required caveat \[[a-z0-9-]*\]|missing required caveat' | sort -u; done; ls fixtures/ | wc -l; python3 152-check-claims.py; echo "DEFAULTS rc=$?"</automated>
  </verify>
  <done>Fifteen fixtures exist; the five inherited forbidden classes and the three caveat rows each have an isolated plant proven RED for exactly one reason.</done>
</task>

<task type="auto">
  <name>Task 2: Copy test_check_claims_v132.py to test_check_claims_152.py, apply the seven rename sites, and add the six new legs</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/test_check_claims_v132.py` — **read in full (721 lines, 20 tests)**. Its `:20-25` records the arming-leg deviation; `:52-58` records the fixture-authoring hazard; `:73-120` are the runner and by-path importer; `:158-184` is the single-reason assertion discipline; `:245-260` is the name-the-absent-target leg; `:674-721` is the meta leg.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 268-390 (§2) — the seven rename sites, the verbatim runner/importer/meta-leg blocks, and the twenty leg names to transcribe.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` — the live tables the suite introspects.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 932-1019 (§C-8, §C-9) — the leg classes, the basename-collision finding, and the posted-mode basename→caveat trap.
  </read_first>
  <action>
Copy `test_check_claims_v132.py` to `test_check_claims_152.py` and apply these seven rename sites,
transcribing everything else verbatim.

1. The file's own basename — `test_check_claims_152.py`. This is not cosmetic: pytest's default
   `prepend` import mode collides on a repeated basename when run from `/workspaces`.
2. `_SCANNER` → `"152-check-claims.py"`.
3. The env seam in `_run_scanner` → `FIRESTARTER_CLAIMSCAN_TARGETS_152`, at both the set and the
   `pop` sites.
4. `_import_scanner_module`'s arbitrary module name → `"check_claims_152_introspect"`.
5. `_CAVEAT_NEEDLE` → three needles, one per required-caveat row, taken from the live
   `REQUIRED_CAVEAT_PATTERNS`.
6. The arming leg → `test_armed_against_the_real_152_artifacts`, written **self-maintaining**: assert
   `_DEFAULT_TARGETS` is non-empty, every entry exists on disk, every entry's dirname is `_HERE`,
   every basename starts with `152-`, and the no-argv no-seam subprocess run exits 0. Written this
   way it stays true as later plans extend the list, and it is observable GREEN today because Plan
   152-01 wrote `152-CLAIM-CLASSES.md`. Do NOT point it at a file three plans away.
7. The meta leg's `label_to_fixture` map → the three ADDED/MODIFIED labels only:
   `sdp-relock-as-shipped` → `planted_sdp_relock_as_shipped.md`, `sdp-relock-flag-as-shipped` →
   `planted_sdp_relock_bare_flag.md`, `issue-closed` → `planted_issue_closed_gh21.md`. Keep the
   donor's non-vacuity guard (the fixture directory must be non-empty first), its stale-mapping
   assertion (every mapped label must exist in `FORBIDDEN_PATTERNS`), and its leg-isolation loop over
   `all_labels - {label}`.

Transcribe all twenty donor leg names, renaming `149`→`152` where a name carries a phase number.
Extend `test_context_research_and_discussion_log_are_not_gate_targets` to also cover
`152-VALIDATION.md` and `152-PATTERNS.md` — both exist on disk today and both carry forbidden
vocabulary as discussion prose.

Then ADD six legs:
- `test_planted_sdp_relock_is_rejected` — the specified planted violation is RED, names
  `sdp-relock-as-shipped`, and does NOT name `sdp-relock-flag-as-shipped` (leg isolation via the
  fixed-width lookbehind) nor a missing caveat.
- `test_planted_sdp_relock_bare_flag_is_rejected` — the bare-flag plant is RED and names only
  `sdp-relock-flag-as-shipped`.
- `test_withdrawal_sentence_is_permitted` — `clean_control.md`, which contains the same literal
  command name in the mandated withdrawal word order, exits 0. This is the leg that proves criterion
  4's mandated sentence survives criterion 5's class.
- `test_issue_closed_still_fires` — parametrised or looped over the three
  `planted_issue_closed_*.md` fixtures; each is RED and names `issue-closed`.
- `test_gh32_closure_statement_is_permitted` — `clean_control.md` carries D-05's required statement
  about gh#32 in the natural past-tense phrasing and exits 0.
- `test_each_required_caveat_row_fails_independently` — the three `planted_missing_caveat_*.md`
  fixtures each RED, each naming exactly one caveat label and no forbidden phrase.

Every behavioural leg goes through `_run_scanner` (subprocess). The by-path importer is for
introspection legs only — the gate's filename is not a valid Python identifier.

⚠ If any leg comes out RED, first check whether it is structurally unreachable (a locator problem —
wrong fixture name, wrong substring, wrong bucket wording). Fix the locator. Do NOT change what the
assertion asserts.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py -q -o addopts=""` reports 26 or more passed and 0 failed, with the count line visible.
    - `python3 -m pytest ... -q -o addopts="" -k "planted_sdp_relock"` selects at least 2 tests and all pass.
    - `python3 -m pytest ... -q -o addopts="" -k "withdrawal_sentence_is_permitted or issue_closed_still_fires or gh32_closure_statement_is_permitted"` selects at least 3 tests and all pass.
    - `python3 -m pytest ... -q -o addopts="" -k "every_forbidden_pattern_has_a_planted_fixture"` selects exactly 1 test and it passes.
    - `grep -c 'test_check_claims_v132' test_check_claims_152.py` returns 0.
    - `grep -c '152-VALIDATION.md' test_check_claims_152.py` ≥ 1 and `grep -c '152-PATTERNS.md' test_check_claims_152.py` ≥ 1.
    - `grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_149' test_check_claims_152.py` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; python3 -m pytest .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py -q -o addopts="" -k "planted_sdp_relock or withdrawal_sentence_is_permitted or issue_closed_still_fires or gh32_closure_statement_is_permitted or every_forbidden_pattern_has_a_planted_fixture"; echo "NEWLEGS rc=$?"</automated>
  </verify>
  <done>The paired suite exists under a distinct basename, is fully green, and its six new legs prove both directions of the two added classes and the modified one.</done>
</task>

<task type="auto">
  <name>Task 3: Write 152-CLAIM-GATE-TRANSCRIPTS.md with every RED pasted, then GREEN, then the paired-suite run</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md</files>
  <read_first>
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CLAIM-GATE-TRANSCRIPTS.md` — **read in full (274 lines)**. `:3-15` is the header disclaimer to adapt clause-by-clause; `:27-44` is the per-block shape; `:112-119` is the untruncated GREEN PASS line; `:141-155` is the does-prove / does-not-prove pair; `:232-274` is the final-target-list section shape.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 439-493 (§4) — the header disclaimer, the block shape, and the 152 section skeleton.
  </read_first>
  <action>
Write `152-CLAIM-GATE-TRANSCRIPTS.md`. Every RED must be a **literal paste** of the command, the
gate's stdout and an `EXIT=` line — never a description of what happened. Retarget the donor's header
disclaimer keeping every clause: this file deliberately contains forbidden vocabulary quoted verbatim
as evidence of what the gate rejected; it is not a claim about anything; it is deliberately NOT a gate
target and is absent from `_DEFAULT_TARGETS` by design; a future reader must not "fix" it by
rewording the RED blocks; and if it were ever added to `_DEFAULT_TARGETS` every RED block below would
make the gate permanently fail against its own proof of itself working.

Sections, in this order:

- `## RED — one block per forbidden-pattern label this phase added or modified`
  - `### 1. sdp-relock-as-shipped — ADDED (plant = the roadmap's pre-amendment criterion-1 wording)`
  - `### 2. sdp-relock-flag-as-shipped — ADDED (the bare-flag companion)`
  - `### 3. issue-closed — MODIFIED (32 dropped), with the three still-fires controls` — three
    blocks, gh#21 / gh#11 / gh#12.
- `## RED — donor-carried rows, for completeness` — one block each for
  `proven-unqualified`, `graduation`, `support-status-change`, `at28c256-fixed`,
  `page-size-proven`, and one per missing-caveat row (three blocks).
- `## ALLOW — the two clean controls` — the literal rc=0 runs for `clean_control.md` and
  `clean_control_second.md`, with a sentence naming what each proves: that the mandated withdrawal
  word order survives class (e), that D-05's gh#32 statement survives the narrowed `issue-closed`
  row, and that D-11's two exempted statements of shipped command behaviour survive `now-works` and
  `at28c256-fixed`.
- `## GREEN — the real default targets, no argv, no seam` — the untruncated `PASS:` line including
  its non-claim tail.
- `## Paired suite — python3 -m pytest test_check_claims_152.py -q -o addopts=""` — the literal
  count line.
- `## What this transcript does and does not prove` — the donor's **Does prove** / **Does not prove**
  pair. Under "does not prove", state explicitly: a green gate is not a wording review, and D-03's
  per-artifact blocking operator checkpoint is the control that reviews wording.
- Two placeholder headings, each with a one-line note naming the plan that fills it:
  `## Extended target list (Plan 152-13)` and
  `## Final target list (close-out, Plan 152-20 — the last SUMMARY added via argv)`.

Do not truncate any pasted output. Do not paraphrase a RED.
  </action>
  <acceptance_criteria>
    - `grep -c 'EXIT=1' 152-CLAIM-GATE-TRANSCRIPTS.md` ≥ 13 (five new-label blocks, five donor forbidden blocks, three missing-caveat blocks).
    - `grep -c 'EXIT=0' 152-CLAIM-GATE-TRANSCRIPTS.md` ≥ 3 (two controls plus the defaults GREEN).
    - `grep -c 'never add to _DEFAULT_TARGETS\|absent from' 152-CLAIM-GATE-TRANSCRIPTS.md` ≥ 1 and the header states the file is not a gate target.
    - `grep -c 'Extended target list (Plan 152-13)' 152-CLAIM-GATE-TRANSCRIPTS.md` == 1 and `grep -c 'Final target list' 152-CLAIM-GATE-TRANSCRIPTS.md` == 1.
    - `python3 -m pytest ... -k "the_transcript_file_is_not_a_gate_target"` selects exactly 1 test and it passes — the transcript is still not a `_DEFAULT_TARGETS` member.
    - `python3 152-check-claims.py` (defaults) still exits 0 after the transcript is written.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; grep -c 'EXIT=1' 152-CLAIM-GATE-TRANSCRIPTS.md; grep -c 'EXIT=0' 152-CLAIM-GATE-TRANSCRIPTS.md; python3 -m pytest test_check_claims_152.py -q -o addopts="" -k "the_transcript_file_is_not_a_gate_target"; echo "TRANSCRIPT-LEG rc=$?"; python3 152-check-claims.py; echo "DEFAULTS rc=$?"</automated>
  </verify>
  <done>The plant-and-revert evidence is committed with every RED pasted verbatim, the two ALLOW controls recorded, the GREEN untruncated, and the two later-plan sections stubbed with their owning plan named.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `fixtures/planted_proven_unqualified.md`, `planted_graduation.md`,
  `planted_support_status_change.md`, `planted_at28c256_fixed.md`, `planted_page_size_proven.md`,
  `planted_missing_caveat_software_proven.md`, `planted_missing_caveat_no_at28c.md`,
  `planted_missing_caveat_unverified.md`
- `.planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py`
- `.planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md`

**New test symbols**
- Suite module: `test_check_claims_152.py`; helpers `_SCANNER`, `_HERE`, `_run_scanner`,
  `_import_scanner_module` (module name `check_claims_152_introspect`), `_CAVEAT_NEEDLE` (three
  needles)
- New legs: `test_planted_sdp_relock_is_rejected`,
  `test_planted_sdp_relock_bare_flag_is_rejected`, `test_withdrawal_sentence_is_permitted`,
  `test_issue_closed_still_fires`, `test_gh32_closure_statement_is_permitted`,
  `test_each_required_caveat_row_fails_independently`
- Renamed legs: `test_armed_against_the_real_152_artifacts`, plus the nineteen other donor legs with
  `149`→`152` substitutions

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite → gate | The suite must exercise the gate as a subprocess, or it proves only its own copy of the tables. |
| transcript → future reader | A reader who "fixes" the transcript's forbidden vocabulary destroys the evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-05 | Tampering | a gate leg that passes vacuously | high | mitigate | Every plant asserted RED with a named label AND the absence of the other bucket; the meta leg carries a non-vacuity guard before any per-label assertion. |
| T-152-06 | Tampering | a pre-authored leg that is structurally unreachable | high | mitigate | Every new leg must be observed to pass in this plan's own run; any fix is locator-only and the plan says so. |
| T-152-07 | Repudiation | the transcript entering `_DEFAULT_TARGETS` and permanently reddening the gate | medium | mitigate | `test_the_transcript_file_is_not_a_gate_target` plus the header disclaimer. |
| T-152-08 | Tampering | a repeated suite basename silently shadowing Phase 149's live suite | medium | mitigate | Distinct basename `test_check_claims_152.py`, asserted by a grep criterion. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** pytest is already present and Phase 149's sibling suite was measured green on 2026-08-21. |
</threat_model>

<verification>
- `python3 -m pytest .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py -q -o addopts=""` → ≥26 passed, 0 failed.
- `python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
- Fifteen fixtures; each plant RED for exactly one reason; both controls clean.
- `152-CLAIM-GATE-TRANSCRIPTS.md` carries ≥13 `EXIT=1` blocks and ≥3 `EXIT=0` blocks, all pasted.
</verification>

<success_criteria>
The gate has been seen to fail on the specified planted violation and on every other forbidden class
and caveat row, each failure is committed as literal output, and a green paired suite proves the gate
itself rather than a copy of its tables.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-02-SUMMARY.md` when done.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary — in particular, when describing what a plant contains, cite the fixture
by filename and the label by name; do NOT reproduce the plant's own sentence in the SUMMARY.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-02-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
