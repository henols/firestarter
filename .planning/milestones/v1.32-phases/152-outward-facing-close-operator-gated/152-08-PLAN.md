---
phase: 152-outward-facing-close-operator-gated
plan: 08
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03", "152-04"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md
autonomous: true
requirements: [OUT-04]

must_haves:
  truths:
    - "RESEARCH Open Question 1 is settled by a live re-derivation through Phase 151's own code path, with both candidate counting methods run and both recorded — no outward sentence cites a count until this exists."
    - "`152-RELEASE-NOTES-app.md` announces `lock-status` with the refusal as the feature, named beta-only and matched-firmware-required, per D-13."
    - "It states the deferred deliberate-protection command's withdrawal explicitly, in the gate's mandated word order, naming Backlog 999.28 — announced neither as shipped nor left unmentioned."
    - "It corrects the forward-looking wording the published b14 body still carries, in the NEW notes, without editing the published body (D-02)."
    - "It carries the milestone-level non-claim and does not assert that the stable version on GitHub is installable from PyPI."
    - "`FIRESTARTER_CLAIMSCAN_TARGETS_152=<the draft> python3 152-check-claims.py` exits 0."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md
    - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md
  key_links:
    - "This is one of only two artifacts in the phase that may contain the deferred command's literal name, and only in the mandated withdrawal word order — the command name first, the withdrawal predicate immediately after."
    - "`_CAVEAT_RULES` requires all three caveat labels on this basename."
    - "The version is a placeholder here and is filled by Plan 152-12 from `gh release list` after the cut. Versions are read, never predicted."
  prohibitions:
    - "This plan does NOT post anything. Plan 152-17 posts, behind its own blocking operator checkpoint."
    - "Do NOT edit the published b14 or b15 release bodies, and do NOT backfill any bodiless release (D-02)."
    - "Do NOT predict a version number. Use the placeholder token and let Plan 152-12 fill it from a read."
    - "Do NOT assert that the current stable version is installable from PyPI — measured 2026-08-21, GitHub carries a stable release that PyPI does not have, and PyPI's own stable pointer is one version behind."
    - "Do NOT cite a protection-class count that Task 1 did not measure in this plan."
    - "Do NOT claim any AT28C silicon result, page-size validation on silicon, a `0x0D` graduation, or a `support_status` change; and do NOT cite `support_status: supported` as evidence about the write path."
    - "Do NOT recommend the `-b` / `--no-blank-check` workaround."
    - "Do NOT present the deferred split-firmware relief as planned or queued — it is explicitly deferred and must be named as an unaddressed constraint, if at all."
    - "Do NOT name `tools/check_dispatch.py` as the control guarding the 12 V-on-OE erase path."
---

<objective>
Settle the one measurement that gates an outward sentence, then author the app release body OUT-04
requires: `lock-status` announced as shipped, the forward-looking wording v1.30 left behind corrected,
and the deferred deliberate-protection command's withdrawal stated explicitly.

Purpose: this is the most-read artifact this project produces, and it is the one place where the
deferred command's literal name must appear — in exactly one word order, or the claim gate rejects it.
It is also where a class-size count would be published, and RESEARCH measured that the published
counts do not reproduce, so the count is re-derived here before anything cites it.

Per **D-01** the body is authored version-agnostic and the tag is filled in at cut time, never
predicted. Per **D-02** only this cut gets a body and the published historical bodies are not
rewritten. Per **D-07** and **D-08** the write-path policy is already shipped when these notes are
written, so the notes describe a landed change and never recommend the workaround it supersedes.

Output: `152-CLASS-SIZES.md` (the measurement record) and `152-RELEASE-NOTES-app.md` (the frozen
draft, version-placeholdered).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This plan produces **drafts only**. Nothing goes public here.

⚠ The version is a placeholder. Plan 152-12 reads both cut tags from `gh release list` after the merge
and fills them. Every prior release-notes artifact in this project opens by naming where the version
was read from and when; this one will too, once it can.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the protection-class partition through Phase 151's own code path and settle Open Question 1</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 338-383 (§A-8) — the 2026-08-21 derivation, its five class counts, and the measured ±1..3 disagreement with the published figures.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` §"Open Questions" entry 1 (from line 1886) and §"Assumptions Log" entry A6 (from line 1871) — the ambiguity to resolve and the robust substitute formulation.
    - `firestarter_app/firestarter/protection_readability.py` — `protection_gate_for_entry` and the alias tokenisation helper. Read the actual signatures; do not assume them.
    - `firestarter_app/firestarter/lock_status.py` — the eight class tokens and the exit-code map.
    - `.planning/phases/151-*/151-CONTEXT.md` §D-06 and §D-09 — the published class-size figures and the classifier's own description, so the two candidate methods can be named precisely.
  </read_first>
  <action>
Re-derive the partition live, through the production pipeline, and record both candidate methods.

Run the classification over all rows of `firestarter_app/firestarter/data/chip_database.json` twice:
  - **Method A, per-row canonical:** key each row on the FIRST alias of its `part_number` field.
  - **Method B, alias-aware:** use Phase 151's own tokenisation of `part_number` so a row whose first
    alias is uncurated lands in the undocumented-alias class rather than shifting its neighbours.

For each method record: the count per class token, the total, and the number of rows that raised. Then
write `152-CLASS-SIZES.md` containing:
  - the exact commands and the interpreter version, with a timestamp, in the live-capture style the
    ledger donors use ("measured here, never reused from a prior document's citation");
  - both methods' full partitions side by side;
  - the algorithm histogram over all rows, so the algorithm-13 and algorithm-5 bucket sizes are
    independently visible;
  - an explicit **resolution** section stating which method Phase 151 used, if that is now
    determinable, and — regardless — naming the **robust formulation** as the one any outward artifact
    may cite: the number of rows that resolve to a refusal class and the number that are read-permitted,
    because that split holds under either method. State the arithmetic.
  - a **do-not-cite** line naming every figure that is method-dependent and therefore unpublishable.

⚠ If the two methods disagree, that is the expected finding, not a failure — record both and publish
only the robust formulation. Do not reconcile by choosing.

⚠ `152-CLASS-SIZES.md` is a measurement record, not a claim register. Give it no `_CAVEAT_RULES`
entry and do **not** add it to `_DEFAULT_TARGETS` — it is not an outward artifact and it will contain
raw class-token vocabulary. Verify at the end of the task that `python3 152-check-claims.py` on
defaults is still green.
  </action>
  <acceptance_criteria>
    - `152-CLASS-SIZES.md` exists and contains both methods' per-class counts, each summing to the total row count of `chip_database.json`, with zero classification errors recorded.
    - It contains the algorithm histogram with the algorithm-13 and algorithm-5 bucket sizes.
    - It contains a `Resolution` section naming the robust formulation with its arithmetic, and a `Do not cite` list of the method-dependent figures.
    - It records the exact commands, the Python version and a timestamp.
    - `python3 .planning/phases/152-*/152-check-claims.py` (defaults) still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; test -s 152-CLASS-SIZES.md; echo "exists rc=$?"; grep -c 'Resolution' 152-CLASS-SIZES.md; grep -c 'Do not cite' 152-CLASS-SIZES.md; python3 152-check-claims.py; echo "DEFAULTS rc=$?"</automated>
  </verify>
  <done>Open Question 1 is settled by a live two-method derivation; the publishable formulation is named with its arithmetic and the unpublishable figures are listed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 152-RELEASE-NOTES-app.md — version-placeholdered, withdrawal in the mandated order</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md</files>
  <read_first>
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-app.md` — **read all 92 lines.** Its line 3 opening version-read paragraph is verbatim-reusable in structure; its `:7-11` channel paragraph, its `:56-86` established/not-established split and its `:88-92` ask section are the section models.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md` lines 10-21 — the `## Removed` section, which is the correct mapping for the retired protection commands. This is the section to quote-adapt and **extend**.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 622-697 (§8) — the donor paragraphs pinned verbatim, the two 152-specific adjustments, and the note that the Removed section is the one place the donor must be extended rather than copied.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 723-812 (§C-4) — the mandated word order for the withdrawal sentence and the seven proven-permitted phrasings.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 310-331 (D-13) — how `lock-status` is framed; lines 69-88 (D-02) — the b14 notes are historical and are corrected in the NEXT notes, never rewritten.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 273-306 (§A-5, §A-6) — the PyPI/GitHub stable-channel divergence, and the exact b14 sentence to correct with its `[ASSUMED]` flag: **re-grep the live b14 body before quoting it.**
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md` — Task 1's output; the only source for any count in this file.
    - `.planning/phases/152-outward-facing-close-operator-gated/fixtures/clean_control.md` — the proven-clean withdrawal phrasing.
  </read_first>
  <action>
Author `152-RELEASE-NOTES-app.md`. Use the placeholder token `APP_TAG_TBD` everywhere a version
belongs, and `FW_TAG_TBD` for the matching firmware version; Plan 152-12 substitutes both from a read.

**Before writing the correction paragraph:** re-grep the published b14 body for the exact
forward-looking sentence, because RESEARCH flags it `[ASSUMED]` at character level:
`gh release view 3.0.0b14 --repo henols/firestarter_app --json body --jq '.body'` and grep for the
sentence about an opt-in re-lock after a write. Quote what you measure, not what a document says the
sentence is. If it is absent, record that and correct the claim by description instead of by quotation.

Sections, in this order:

1. **The version-read opening paragraph**, structured exactly like the 146 donor's: the tag, where it
   was read from and when, the workflow that cut it, the PyPI confirmation verified **independently of
   GitHub**, the stable-channel statement, and the matching firmware release with the note that the two
   repositories version independently. Leave the read values as placeholders with a bracketed note
   naming Plan 152-12 as the filler. ⚠ Two 152-specific corrections to the donor: the firmware
   workflow is not named the same as the app's, and the stable channel is **divergent** — GitHub
   carries a stable release PyPI does not have, and PyPI's own stable pointer is a version behind. Say
   what is installable and do not imply the GitHub stable release is.
2. **The channel paragraph** (146 donor `:7-11`): the GitHub release page carries no files, PyPI is
   the distribution channel, the firmware is published separately and its images are what the firmware
   updater pulls, and a reader upgrading one half without the other should know which half does what.
   This paragraph is load-bearing here, because the write-path change is in the firmware.
3. **`## What's new`** — four items, each stated in D-11-exempt terms (shipped, user-visible command
   behaviour):
   - **`lock-status`**, framed per D-13: lead with what it actually does — either report a real state
     read from the silicon, or refuse with a named, actionable reason — because on this chip family
     the honest answer IS the refusal. Cite only the robust formulation from `152-CLASS-SIZES.md`.
     Name it **beta-channel only** and **matched-firmware-required**, and state why: it is a new
     firmware command, and against older firmware the host reports the firmware as out of date. State
     that the one exploratory run on a different family was a **probe**, never validation.
   - **The write-path policy**: on the two protocols that auto-erase per page during the write, the
     pre-write blank check is gone — it was never a safety net on that silicon, and it made a
     non-blank part unwritable without an override flag. The standalone blank-check step is unchanged.
   - **Standalone erase** on the 28C protocol, by the software six-byte sequence from the Atmel
     application note rather than the datasheet's hardware path, with the reason: the hardware path
     drives a programming voltage onto a pin of that pinout.
   - **Report provenance and numeric database values**: reports now name the firmware they ran on;
     voltages and timings are integers in millivolts and microseconds.
4. **`## Removed`** — quote-adapt the 137 donor's section, keeping both bullets' substance: the
   unlock half is gone because it is genuinely redundant, since a write already auto-unlocks by
   default and is declinable; and the lock half is **withdrawn with no replacement in this release**,
   with no supported way to deliberately protect such a part, and the protection bit unreadable
   afterwards on this family so even a replacement could not confirm its own result. **Extend it**
   with the sentence criterion 4 mandates and criterion 5 constrains: the deferred command's literal
   name followed **immediately** by a withdrawal predicate, naming Backlog **999.28**. Any other word
   order is rejected by the gate; use one of the phrasings proven clean in `fixtures/clean_control.md`.
   Then the correction of the published forward-looking sentence: quote what the published body says,
   say plainly that it is now wrong in the direction of promising less than a deferral, and state that
   the published body is deliberately left as published because it is historical — this is where the
   correction lives.
5. **`## What is established, and what is not`** — the 146 donor's Established / Not established
   bullet split. The not-established list transcribes RESEARCH §B-10 rather than paraphrasing it, and
   carries the milestone-level non-claim: no AT28C part was tested at any point in v1.32, and the
   protocol's ledger status is unchanged. Both mandated sentences appear here verbatim, plus the
   software-proven / unvalidated-on-silicon sentence. Use the donor's bolded-lead-then-explain rhythm
   and name *why* each thing is unestablished.
6. **`## The ask`** — the 146 donor's closing shape, with its "either outcome" framing, naming both
   install halves.

Do not name a phase number as process. Do not cite any count Task 1 did not measure.
  </action>
  <acceptance_criteria>
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-RELEASE-NOTES-app.md python3 152-check-claims.py` exits 0 — which proves simultaneously that the withdrawal sentence is in the mandated order and that all three required caveats are present.
    - `grep -c 'sdp-relock' 152-RELEASE-NOTES-app.md` ≥ 1 — the command name IS present, as criterion 4 requires.
    - `grep -c '999.28' 152-RELEASE-NOTES-app.md` ≥ 1.
    - `grep -c 'lock-status' 152-RELEASE-NOTES-app.md` ≥ 1 and `grep -c 'fw --install' 152-RELEASE-NOTES-app.md` ≥ 1.
    - `grep -c 'APP_TAG_TBD' 152-RELEASE-NOTES-app.md` ≥ 1 and `grep -c 'FW_TAG_TBD' 152-RELEASE-NOTES-app.md` ≥ 1 — the version is a placeholder, not a prediction.
    - `grep -cE '3\.0\.0b[0-9]+' 152-RELEASE-NOTES-app.md` counts only quotations of the **published** body being corrected; record the count and each quoted context in the SUMMARY.
    - `grep -c 'No AT28C part was tested at any point in v1.32' 152-RELEASE-NOTES-app.md` == 1 and `grep -c 'software-proven and unvalidated on silicon' 152-RELEASE-NOTES-app.md` ≥ 1.
    - `grep -c 'support_status' 152-RELEASE-NOTES-app.md` == 0 and `grep -c 'check_dispatch' 152-RELEASE-NOTES-app.md` == 0.
    - `grep -ci 'installable\|pip install' 152-RELEASE-NOTES-app.md` ≥ 1 and no sentence asserts the GitHub stable release is on PyPI — verified by the operator wording review in Plan 152-17 and recorded as a review item here.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-RELEASE-NOTES-app.md" python3 152-check-claims.py; echo "GATE rc=$?"; for n in 'sdp-relock' '999.28' 'lock-status' 'fw --install' 'APP_TAG_TBD' 'FW_TAG_TBD' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon'; do printf '%s => ' "$n"; grep -c "$n" 152-RELEASE-NOTES-app.md; done; printf 'support_status => '; grep -c 'support_status' 152-RELEASE-NOTES-app.md</automated>
  </verify>
  <done>The app body exists, is gate-clean with the command name present in the mandated withdrawal order, announces `lock-status` refusal-first, corrects the published forward-looking sentence without editing it, carries all three non-claims, and predicts no version.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze the draft and hand the placeholder contract to Plan 152-12 and Plan 152-17</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` — the authored draft.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1174-1236 (§E-1, §E-2, §E-3) — the two workflows, the absent `body:` in both, and the automatic PyPI path.
  </read_first>
  <action>
Commit the draft as frozen and record the handoff contract:

- Every placeholder token used, with its line number and what it must be replaced by, and the command
  Plan 152-12 must read the value from. Record explicitly that both `body:` fields are absent from
  both release workflows, so a body is only ever added manually and the placeholder cannot be filled
  by CI.
- The exact posting invocation Plan 152-17 will use, written out for review:
  `gh release edit APP_TAG_TBD --repo henols/firestarter_app --notes-file <path to this draft>`.
- The exact verification invocation Plan 152-17 will use afterwards:
  `gh release view APP_TAG_TBD --repo henols/firestarter_app --json body --jq '.body'` piped into
  `diff -u` against the draft, plus the body-length read as the 0-to-N non-vacuity guard.
- The current body length of the release the notes will land on, if it is already known to be zero for
  every candidate tag — record the measurement command rather than the assumption.
- The wording-review items for the operator checkpoint in Plan 152-17: the stable-channel sentence,
  the withdrawal sentence's word order, and the `lock-status` paragraph's refusal-first framing.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - `git -C /workspaces status --porcelain -- .planning/phases/152-*/152-RELEASE-NOTES-app.md .planning/phases/152-*/152-CLASS-SIZES.md` is empty.
    - The SUMMARY lists every placeholder token with its line number, its replacement source command, and the two `gh` invocations verbatim.
    - The SUMMARY records that neither release workflow passes a body, so bodies are always manual.
    - The SUMMARY lists the three wording-review items for Plan 152-17's operator checkpoint.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; git -C /workspaces status --porcelain -- .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md .planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md; echo "PORCELAIN-END"; grep -n 'APP_TAG_TBD\|FW_TAG_TBD' 152-RELEASE-NOTES-app.md; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>Both artifacts are committed clean, every placeholder is enumerated with its read command, and the posting and verification invocations plus the wording-review items are handed forward.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-CLASS-SIZES.md` — the two-method
  protection-class derivation record; deliberately **not** a gate target and **not** in
  `_CAVEAT_RULES`
- `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` — the frozen
  OUT-04 app body; a `_DEFAULT_TARGETS` member from Plan 152-13 onward; `_CAVEAT_RULES` entry = all
  three caveat labels

**New placeholder tokens** (filled by Plan 152-12 from a read, never predicted)
- `APP_TAG_TBD`, `FW_TAG_TBD`

**Resolved research question**
- RESEARCH Open Question 1 — settled in `152-CLASS-SIZES.md`; the publishable formulation is the
  refusal-versus-read-permitted split, which holds under both counting methods

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft → published release body | Posted verbatim in Plan 152-17 to the most-read surface this project has. |
| a claim gate's forbidden literal → a criterion's mandated sentence | The same command name is forbidden in one framing and mandatory in another; only the word order distinguishes them. |
| published historical body → new body | The b14 body is public and wrong; D-02 forbids editing it, so the correction must be findable in the new body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-29 | Repudiation | announcing the deferred command as shipped in the release notes | critical | mitigate | The gate's fifth class, run on this exact file as an acceptance criterion; the mandated word order taken from a fixture proven clean in both directions. |
| T-152-30 | Spoofing | announcing a version that does not exist, or one PyPI does not have | high | mitigate | Placeholder tokens only; Plan 152-12 fills them from `gh release list` and verifies PyPI independently by direct registry read. |
| T-152-31 | Spoofing | implying the GitHub stable release is installable from PyPI | high | mitigate | Measured divergence recorded; the stable-channel sentence is an explicit operator-review item in Plan 152-17. |
| T-152-32 | Information disclosure | publishing a class-size count that does not reproduce | medium | mitigate | Task 1 derives it two ways and publishes only the method-independent formulation; every method-dependent figure is listed as do-not-cite. |
| T-152-33 | Repudiation | quoting a published sentence at character level without re-reading it | medium | mitigate | Task 2 re-greps the live published body before quoting, and records what it measured. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** The derivation imports only modules already present in `firestarter_app`. |
</threat_model>

<verification>
- `152-CLASS-SIZES.md` present with both methods, the histogram, a Resolution and a Do-not-cite list.
- Gate green on the app draft via the env seam — which is simultaneously the proof of the mandated
  word order and of all three required caveats.
- Nine positive/negative greps as enumerated in Task 2.
- Both files committed clean; placeholders enumerated; HEAD on the milestone branch.
</verification>

<success_criteria>
The one gating measurement is settled and recorded; the app release body exists, is gate-clean, names
the deferred command only as withdrawn with Backlog 999.28, announces `lock-status` refusal-first,
corrects the published forward-looking sentence in the new notes, and predicts no version.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-08-SUMMARY.md` when done.

Record verbatim: both derivation methods' partitions, the publishable formulation, the do-not-cite
list, the measured b14 sentence (or its measured absence), every placeholder token with its line and
read command, both `gh` invocations, and the three wording-review items.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary. In particular: if it refers to the deferred command at all, use the
mandated withdrawal word order; do not write that something now works; cite any forbidden phrasing by
`file:line` and label.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-08-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
