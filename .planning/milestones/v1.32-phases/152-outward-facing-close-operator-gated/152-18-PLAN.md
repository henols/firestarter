---
phase: 152-outward-facing-close-operator-gated
plan: 18
type: execute
wave: 12
depends_on: ["152-17"]
files_modified: []
autonomous: false
requirements: [OUT-04]

must_haves:
  truths:
    - "The firmware release body is POSTED to the tag read in Plan 152-12, and the stored body is byte-identical to the frozen draft."
    - "The body length moved from its measured starting value to a non-zero value."
    - "The posted-mode gate run over the stored body exits 0 — the withdrawal sentence's word order is proven in the PUBLISHED text."
    - "The withdrawal sentence and all three non-claim sentences are proven present in the PUBLISHED body by positive greps."
    - "Every published image asset named in the body is confirmed present on the release, by name, after posting."
    - "No previously-published release body was edited."
  artifacts: []
  key_links:
    - "This body is the only public record of the Leonardo bootloader boundary, and the linker guard that used to enforce it was removed — the published number is now the only control."
    - "Posted-mode temp files are written under the SAME basename as the draft, because the caveat lookup is basename-keyed and fails closed."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Task 1's configuration guard is a partial control, not a substitute for the operator."
    - "Do NOT edit any release other than the tag read in Plan 152-12. Historical published bodies stay exactly as published."
    - "Do NOT backfill any bodiless release."
    - "Do NOT change the release's prerelease flag, tag, target commit, title or assets. Only the body is set."
    - "Do NOT run `gh workflow run`."
    - "Do NOT alter the draft in this plan. Wording changes go back through the gate before this plan proceeds."
    - "Do NOT accept a body diff that differs in content. A difference confined to trailing-whitespace or line-ending normalisation must be recorded explicitly, with the exact bytes named."
---

<objective>
Post the firmware release body to the tag CI cut, and prove the published text is the reviewed text —
including that every asset it names is actually on the release.

Purpose: the firmware half is where the write-path change actually lives, so a reader who upgrades only
the host gets none of it. This is also the last of the five irreversible public acts in the phase.

Output: one published release body on the firmware repository.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** Approval given for the app body in Plan
152-17 does not carry here — D-03 requires a separate blocking gate per artifact, and these are two
different repositories with two different bodies.

The tag was READ in Plan 152-12. Use that value; do not recompute or guess it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-post checks, the measured starting body length, and the asset cross-check</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-18-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md` — the frozen draft with its tags substituted. Read it in full; this is the last read before it is public.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-12-SUMMARY.md` — the read tag, its reading command and timestamp, and the measured starting body length.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-09-SUMMARY.md` — the measured asset list with byte sizes, both Leonardo size figures with their arithmetic, the placeholder contract, the recorded posting invocation and the five wording-review items for this checkpoint.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1188-1203 (§E-2) — the firmware workflow's real filename, its absent body field, and its asset globs.
  </read_first>
  <action>
Six checks, in this order. Record every command and its output.

1. `python3 .planning/phases/152-*/152-check-not-auto.py` — must exit 0, else **stop**.
2. `python3 .planning/phases/152-*/152-check-claims.py` on defaults — rc 0; then through the env seam on
   the firmware release-notes draft alone — rc 0. Record both PASS lines.
3. `python3 -m pytest .planning/phases/152-*/test_check_claims_152.py -q -o addopts=""` — fully green,
   count line recorded.
4. Confirm the draft still carries the withdrawal sentence and all three non-claim sentences, by
   positive grep, and that no placeholder token remains.
5. Read the release the body is going to: `gh release view <the read tag> --repo henols/firestarter
   --json tagName,isPrerelease,targetCommitish,createdAt,assets,body` — record the tag, the prerelease
   flag, the target commit, the **asset list with byte sizes**, and the **current body length** as a
   number. Confirm the tag matches the one Plan 152-12 read.
6. **Cross-check the assets against the draft.** Every asset name the draft lists must be present on the
   release, and every asset present on the release must be listed in the draft — a body that names an
   image the release does not carry, or omits one it does, is a factual error in a public artifact.
   Record both directions of the comparison and the byte sizes. If they disagree, **stop** and fix the
   draft through Plan 152-09's gate path.

Nothing proceeds on a red.
  </action>
  <acceptance_criteria>
    - `152-check-not-auto.py` exits 0 with the live configuration value recorded.
    - The gate exits 0 on defaults and on the firmware draft; both PASS lines recorded.
    - The paired suite is fully green with its count line recorded.
    - `grep -c 'sdp-relock' 152-RELEASE-NOTES-fw.md` ≥ 1; `grep -c 'No AT28C part was tested at any point in v1.32'` == 1; `grep -c 'software-proven and unvalidated on silicon'` ≥ 1; `grep -c 'FW_TAG_TBD\|APP_TAG_TBD'` == 0.
    - The target release's tag matches the tag Plan 152-12 read; its prerelease flag, target commit, asset list with sizes and current body length are recorded.
    - The asset cross-check passes in both directions, and the comparison is recorded.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; python3 152-check-claims.py; echo "GATE-DEFAULTS rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-RELEASE-NOTES-fw.md" python3 152-check-claims.py; echo "GATE-DRAFT rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; for n in 'sdp-relock' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon' 'FW_TAG_TBD'; do printf '%s => ' "$n"; grep -c "$n" 152-RELEASE-NOTES-fw.md; done; TAG=$(gh release list --repo henols/firestarter --limit 5 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease)][0].tagName'); echo "TAG=$TAG"; gh release view "$TAG" --repo henols/firestarter --json tagName,isPrerelease,targetCommitish,createdAt --jq '.'; gh release view "$TAG" --repo henols/firestarter --json assets --jq '.assets[] | {name,size}'; gh release view "$TAG" --repo henols/firestarter --json body -q '.body | length'; gh release view "$TAG" --repo henols/firestarter --json assets --jq '.assets[].name' | while read -r a; do printf '%s in draft => ' "$a"; grep -c "$a" 152-RELEASE-NOTES-fw.md; done</automated>
  </verify>
  <done>All gates are green, the draft's presence greps hold with no placeholder left, the target release is confirmed with its starting body length measured, and every asset is cross-checked in both directions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review — authorise the firmware release body</name>
  <what-built>
`152-RELEASE-NOTES-fw.md` — the firmware release body for the tag CI cut, with every version
substituted from a read and every size figure and asset name measured. The claim gate is green on it,
the paired suite is green, the presence greps hold, and every named asset has been cross-checked
against the release in both directions. Nothing has been posted. Approving this checkpoint sets the
body on a public release, and this is the last of the five public acts in the phase.
  </what-built>
  <how-to-verify>
1. Read `152-RELEASE-NOTES-fw.md` in full.
2. Check the **two size figures**. They must be in separately-labelled sentences and must not be
   conflated: one is the image size against the USB-bootloader boundary, with the remaining bytes; the
   other, explicitly a different number, is the size-band headroom against the recorded baseline. Check
   the arithmetic against the measured values in the Plan 152-09 record.
3. Check the **unguarded** statement and its reason — the upload size limit is the real device flash
   size, so the linker no longer refuses a build that would overwrite the bootloader. This body is the
   only public record of that.
4. Check the **asset section**: which image is for which board, and the statement that the updater
   flashes the attached board. It must not promise that a board can be chosen.
5. Check the **ARM image's standing non-claim**: no board for it exists and nothing in that port has run
   on the silicon.
6. Check the **hazard section**: the erase path implemented is the software sequence, and the reason the
   hardware alternative was not taken. Confirm the control named as guarding that hazard is the source
   scan that actually does — not the database-and-dispatch checker, which was measured structurally
   unable to.
7. Check the **withdrawal sentence**: name followed immediately by a withdrawal predicate, naming the
   backlog item.
8. Check the **does-not-establish** section reads as a transcription rather than a summary, including
   that no test can prove the erase wall-clock wait is honoured.
9. Confirm nothing presents split or trimmed firmware builds as planned or queued.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: set the firmware release body" or describe the wording changes required first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Set the body, prove the published text is ours, and re-prove both criterion halves plus the assets</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-18-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-09-SUMMARY.md` — the recorded posting and verification invocations and the measured asset list.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-VALIDATION.md` — the release-body proof: the diff for identity and the length change for non-vacuity.
  </read_first>
  <action>
Set the body exactly once, then prove it published as authored.

**Set.** `gh release edit <the read tag> --repo henols/firestarter --notes-file <the frozen draft>`.
Record the command and its output. Set **only** the notes.

**Prove identity.** `gh release view <tag> --repo henols/firestarter --json body --jq '.body'` into a temp
file **named `152-RELEASE-NOTES-fw.md`** — the same basename as the draft, because the caveat lookup is
basename-keyed and fails closed — then `diff -u <the frozen draft> <that file>`. The diff must be empty.
Paste the result.
⚠ On a non-empty diff, do not proceed and do not re-post. A difference confined to line-ending or
trailing-whitespace normalisation must be recorded explicitly, naming the exact bytes; any content
difference is a failure.

**Prove non-vacuity.** Re-read the body length and assert it changed from the Task 1 starting value to a
non-zero value. Record both numbers.

**Re-prove both criterion halves on the PUBLISHED text:** the posted-mode gate over the same-basename
temp file must exit 0 (criterion 5's half, proving the withdrawal word order survived publication), and
positive greps over that same file must find the withdrawal sentence and all three non-claim sentences
(criterion 4's half, which the gate cannot express).

**Re-prove the assets on the published release.** Re-read the asset list and confirm every name is still
present, unchanged in name and size from Task 1's capture, and still named in the published body.

**Prove nothing else moved.** Re-read the release's tag, target commit and prerelease flag and confirm
each matches Task 1's capture. Then confirm no other release's body length changed from the values Plan
152-12 recorded, in both repositories — this is the last public act, so this is the phase's final chance
to detect a body-setting command that landed on the wrong tag.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - The body diff between the frozen draft and the published body produces no output, or produces only a normalisation difference recorded byte-by-byte.
    - The body length changed from the Task 1 starting value to a non-zero value, and both numbers are recorded.
    - The posted-mode gate run over the same-basename temp file exits 0.
    - Positive greps over the published body: `sdp-relock` ≥ 1, `No AT28C part was tested at any point in v1.32` == 1, `software-proven and unvalidated on silicon` ≥ 1.
    - Every asset name and size matches Task 1's capture, and every one is named in the published body.
    - The release's tag, target commit and prerelease flag match Task 1's capture exactly.
    - No other release's body length changed in either repository from the values Plan 152-12 recorded.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; TAG=$(gh release list --repo henols/firestarter --limit 5 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease)][0].tagName'); echo "TAG=$TAG"; T=$(mktemp -d); gh release view "$TAG" --repo henols/firestarter --json body --jq '.body' > "$T/152-RELEASE-NOTES-fw.md"; diff -u 152-RELEASE-NOTES-fw.md "$T/152-RELEASE-NOTES-fw.md"; echo "BODYDIFF rc=$?"; gh release view "$TAG" --repo henols/firestarter --json body -q '.body | length'; FIRESTARTER_CLAIMSCAN_TARGETS_152="$T/152-RELEASE-NOTES-fw.md" python3 152-check-claims.py; echo "POSTEDGATE rc=$?"; for n in 'sdp-relock' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon'; do printf '%s => ' "$n"; grep -c "$n" "$T/152-RELEASE-NOTES-fw.md"; done; gh release view "$TAG" --repo henols/firestarter --json tagName,isPrerelease,targetCommitish,assets --jq '{tagName,isPrerelease,targetCommitish,assets:[.assets[]|{name,size}]}'; for t in 3.0.0b14 3.0.0b15; do printf 'fw %s => ' "$t"; gh release view "$t" --repo henols/firestarter --json body -q '.body | length'; printf 'app %s => ' "$t"; gh release view "$t" --repo henols/firestarter_app --json body -q '.body | length'; done; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The firmware release body is published, diffs clean against the frozen draft, its length moved from the measured start to non-zero, both criterion halves and the asset list are re-proven on the published release, and no other release changed.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** One published release body on the firmware repository's cut pre-release.

**Recorded values handed forward to `152-LEDGER.md` (Plan 152-19)**
- The tag, the release URL, and the body length before and after
- The body-diff result, including any recorded transport normalisation
- The posted-mode gate PASS line and the three positive-grep counts over the published text
- The published asset list with sizes, cross-checked in both directions against the body
- The unchanged-elsewhere evidence for both repositories

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen draft → published release body | Public and watched; the last irreversible act in the phase. |
| measured size figure → the only remaining control | The linker guard over the bootloader region was removed; the published number is what stands in its place. |
| body's asset list → the release's actual assets | A body naming an image the release does not carry is a factual error in the most public artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-80 | Elevation of privilege | an auto or chained run auto-approving this checkpoint | critical | mitigate | The configuration guard runs first and fails closed; the prohibition is restated. ⚠ Partial control; residual risk stated. |
| T-152-81 | Repudiation | the published body differing from the reviewed text | critical | mitigate | Body diff against the frozen draft; any non-empty diff halts the task and must be explained byte-by-byte. |
| T-152-82 | Denial of service | publishing a bootloader-boundary figure that is wrong, so a future build crosses it believing it has room | high | mitigate | Both figures were measured in Plan 152-09 with their arithmetic, the checkpoint re-checks them against that record, and the two are required to be in separately-labelled sentences. |
| T-152-83 | Spoofing | naming an asset the release does not carry, or omitting one it does | medium | mitigate | The asset cross-check runs in both directions before posting and is re-run on the published release afterwards. |
| T-152-84 | Tampering | editing a historical published body by aiming at the wrong tag | high | mitigate | The tag comes from Plan 152-12's read and is re-confirmed; both repositories' other release body lengths are compared against recorded values afterwards. |
| T-152-85 | Repudiation | naming a control that structurally cannot guard the hazard it is credited with | high | mitigate | An explicit checkpoint review item; the grep counts were asserted in Plan 152-09. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Auto guard, defaults gate, draft gate and paired suite green before the checkpoint.
- Draft presence greps hold; no placeholder remains; assets cross-checked both ways.
- Body diff empty (or normalisation-only, recorded byte-by-byte); length moved to non-zero.
- Posted-mode gate green, all three presence greps hold, and every asset re-confirmed on the published
  release.
- Release metadata unchanged; no other release's body length moved in either repository.
</verification>

<success_criteria>
OUT-04 is fully discharged across both repositories: both announced versions exist and were read rather
than predicted, both published bodies are provably the reviewed text, the deferred command is named as
withdrawn in the one word order the gate permits, and the firmware body carries the measured size and
asset facts a maintainer and a Leonardo user need.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-18-SUMMARY.md` when done.

Record verbatim: all six pre-post check results including both directions of the asset cross-check, the
starting and final body lengths, the body diff result, the posted-mode PASS line, the three
published-text grep counts, the published asset list with sizes, the release-metadata comparison, and
both repositories' other-release body-length comparison.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; if it refers to the deferred command, use the mandated withdrawal word
order.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-18-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
