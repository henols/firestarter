---
phase: 152-outward-facing-close-operator-gated
plan: 05
type: execute
wave: 3
depends_on: ["152-01", "152-02", "152-03", "152-04"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.diff
autonomous: true
requirements: [OUT-01]

must_haves:
  truths:
    - "`152-GH12-COMMENT.md` states that the ask is half-answered for a SECOND release, names Backlog 999.28, and promises no version."
    - "It keeps `137-GH12-COMMENT.md`'s two named paragraphs — the two-halves-don't-survive-equally framing and the 'this isn't the enable/disable you asked for' paragraph — in substance (D-14)."
    - "It names both halves of the install: the pre-release host install AND `firestarter fw --install`."
    - "`152-GH12-COMMENT.diff` is a committed unified diff against the 137 original, so the operator wording review sees exactly what changed."
    - "`FIRESTARTER_CLAIMSCAN_TARGETS_152=<the draft> python3 152-check-claims.py` exits 0."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md
    - .planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.diff
  key_links:
    - "The gate's `sdp-relock-as-shipped` and `sdp-relock-flag-as-shipped` labels must not fire on this file — the cleanest way to guarantee that is for the draft never to contain the deferred command's name at all, which is also how the 137 donor is written."
    - "`152-GH12-COMMENT.md`'s `_CAVEAT_RULES` entry requires `no-at28c-part-tested`, so the mandated sentence must be present verbatim."
  prohibitions:
    - "This plan does NOT post anything. Plan 152-14 posts, behind its own blocking operator checkpoint."
    - "Do NOT name the deferred deliberate-protection command anywhere in this draft. Name Backlog 999.28 instead — that is what D-14 asks for and what the 137 donor already does."
    - "Do NOT describe the v1.30 gap as if the original ask had been satisfied all along (ROADMAP criterion 1's negative case)."
    - "Do NOT narrate this project's milestone process, phase numbers as process, or any process failure. D-14: criterion 1 already mandates disclosing the slip in the terms that affect the reporter."
    - "Do NOT assert that gh#12, gh#11 or gh#21 is closed, resolved or fixed. Every one of them stays OPEN."
    - "Do NOT edit the published `b14` release notes or any prior comment. The correction lands in new text only (D-02)."
    - "Do NOT claim any AT28C silicon result, and do NOT cite `support_status: supported` as evidence about the write path — the chip-level field and the protocol-level ledger are different axes."
---

<objective>
Author the owed gh#12 reply as an adaptation of `137-GH12-COMMENT.md`, with the diff against the
original committed so the operator's wording review sees exactly what changed (D-14).

Purpose: this is the single most consequential sentence set in the phase. gh#12 was opened
2024-09-15 by someone who had to build a separate Arduino to disable software data protection before
Firestarter could write at all. v1.30 removed the surface; v1.32 was the milestone scoped to restore
it and did not. The reply must say the ask is half-answered **for a second release** — the honest
version of a shortfall that has now repeated.

Per **D-01** the comment is posted inside this phase and the draft is authored version-agnostic — the
version is never predicted. Per **D-07** and **D-08** the write-path policy this reply describes is
already shipped when the reply goes out, so it can describe a landed change rather than recommend a
superseded workaround.

Output: `152-GH12-COMMENT.md` (frozen draft) and `152-GH12-COMMENT.diff` (the review artifact).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
This plan produces a **draft only**. Nothing goes public here.

The draft is authored **version-agnostic**: it must not name a release tag. Plan 152-12 reads the cut
versions and Plan 152-14 posts. If a version must be referred to, refer to the pre-release channel by
its install command, never by a predicted number.
</context>

<!-- planner-discipline-allow: check_dispatch -->

<tasks>

<task type="auto">
  <name>Task 1: Copy the 137 donor verbatim as the diff baseline and commit that copy unchanged</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md` — **read all 46 lines.** This is D-14's base: the frozen v1.30 reply that was never posted.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 332-346 (D-14) — what to keep, what to add, what to omit, what it must not say.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 547-595 (§6) — the two paragraphs quoted verbatim so they can be pinned, and the note that the donor never names the deferred command.
  </read_first>
  <action>
Copy `137-GH12-COMMENT.md` byte-for-byte to
`.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` and commit that
unmodified copy as its own commit, with a message naming it the D-14 diff baseline.

The reason this is a separate commit: D-14 requires the review to see exactly what changed against the
137 original. A single commit that both creates and rewrites the file produces a diff against nothing.
Committing the verbatim copy first makes the adaptation a readable diff in git history as well as in
the standalone `.diff` artifact Task 3 produces.

Do not adapt anything in this task. Verify byte-identity before committing.
  </action>
  <acceptance_criteria>
    - `diff -q .planning/phases/137-*/137-GH12-COMMENT.md .planning/phases/152-*/152-GH12-COMMENT.md` reports no difference.
    - `git -C /workspaces log --oneline -1 -- .planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` is non-empty.
    - `wc -l .planning/phases/152-*/152-GH12-COMMENT.md` returns 46.
  </acceptance_criteria>
  <verify>
    <automated>diff -q /workspaces/.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md /workspaces/.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md; echo "IDENTICAL rc=$?"; wc -l < /workspaces/.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md</automated>
  </verify>
  <done>A byte-identical copy of the 137 donor is committed as the adaptation's diff baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt the draft — keep the two named paragraphs, add the second withdrawal, 999.28, lock-status and the erase policy</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` — the verbatim copy from Task 1, so the adaptation is made against the actual text.
    - `.planning/ROADMAP.md` line 451 (`sed -n '451p'`) — criterion 1 as amended, including its two negative cases.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1037-1070 (§D-1, §D-2) — the exact 2026-07-30 sentence being retracted, and the **later 2026-08-06 comment CONTEXT does not mention**: this reply is the third maintainer comment in a row on gh#12 and the second "install the pre-release and test" message. That materially affects tone.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 501-664 (§B-2 through §B-11) — what Phase 153 actually shipped, and §B-10's transcribe-don't-paraphrase list of what it did NOT do.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 310-331 (D-13) — how `lock-status` is described: the refusal is the feature; beta-only; matched-firmware-required; the W29C040 run was an exploratory probe, never validation.
    - `.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md` — the folded todo. Its four "what the reply must say" points and its four constraints are binding input, in particular that the published notes are historical and are corrected in the NEXT notes, and the required Removed mapping.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` — `FORBIDDEN_PATTERNS` and `_CAVEAT_RULES`, so the draft is written against the live gate.
  </read_first>
  <action>
Adapt the copy in place. Structure it on the donor's bolded-section rhythm and its discipline of
naming the shortfall before naming the gain.

**KEEP, in substance and as close to verbatim as the new facts allow:**
- The donor's `**What's changing**` paragraph and its two-bullet "the two halves don't survive
  equally" structure (donor `:6-14`), with the `disable`-survives-as-`write`'s-default-auto-unlock
  bullet and the `--skip-sdp-unlock` opt-out intact.
- The donor's *"This isn't the 'enable/disable' you asked for"* paragraph (donor `:16-18`) including
  its second limitation — that the protection bit on these parts cannot be read back, so even when a
  part is protected nothing can show you that it is. These are the hardest sentences in the file and
  they are already operator-reviewed once.

**CHANGE — the withdrawal is now the second one.** The donor's *"The design for one is settled and
the work is queued"* is no longer honest on its own: the work was scoped again, as this milestone's
Phase 150, and deferred again on 2026-08-20 before any research or plan existed. Replace it with a
sentence that says the ask is **half-answered for a second release**, that deliberate protection is
tracked as Backlog **999.28**, and that no version is promised. Name Backlog 999.28 explicitly. Do
**not** name the command.

**ADD — what did get better, accurately.** Rewrite the donor's `**What did get better**` section
around the three things v1.32 actually ships that touch this thread:
1. **`lock-status`** — framed per D-13: it either reports a real state read from the silicon or
   refuses with a named, actionable reason, and on this chip family the honest answer IS the refusal.
   Lead with that, not with "reports your chip's protection state". Say it is beta-channel only and
   needs matching firmware. Do not cite a class-size count in this comment — the counts are being
   re-derived in Plan 152-08 and no outward sentence may cite one until that lands.
2. **The write-path policy** — on this protocol a page write auto-erases internally, so a pre-write
   blank check was never a safety net; it is gone, and a standalone erase step now exists on this
   protocol, implemented by the **software** six-byte sequence from the Atmel application note, not
   the datasheet's 12 V-on-OE hardware path. State that the standalone blank check still exists as its
   own step. Use the phrasings D-11 exempts — statements of shipped, user-visible command behaviour.
3. **Report provenance** — a `dev test` report now names the firmware it ran on, which is why the ask
   in the next section is worth making at all.

**ADD — the tone correction RESEARCH §D-2 forces.** This is the third maintainer comment in a row on
this thread and the second time it asks the reporter to install the pre-release and test. Acknowledge
that in one clause: the last such message pointed at a build whose reports could not say which
firmware they ran on, and whose write step failed on a non-blank part for a reason since removed. One
clause, in the reporter's terms. Not a process narration.

**ADD — the install, both halves.** The donor's `**This is where I need help…**` section (donor
`:27-46`) is reusable nearly whole, including its fenced `dev test` invocation and its
non-destructive-by-default note. Extend it with the second half of the install: the firmware update,
`firestarter fw --install`. State plainly why both are needed — the write-path change is in the
firmware and the host's test step sends no override flag, so on older firmware the old failure
reproduces exactly as before. Keep the donor's "one run from a part in someone's hand" closing.

**ADD — the mandated non-claim.** The draft must contain, verbatim, the sentence the gate's
`no-at28c-part-tested` row requires: that no AT28C part was tested at any point in v1.32. The donor's
existing *"No AT28C silicon was tested during this milestone"* sentence is the right place — replace it
with the mandated spelling.

**OMIT** any process-failure narration, any phase number presented as process, any promise of a
version, and any reference to the `-b` / `--no-blank-check` workaround as a recommended path — Phase
153 removed the precondition that made it necessary and recommending it would be a claim 153
disproved.

**Do not** name `tools/check_dispatch.py` as what prevented the 12 V erase path from being used;
Phase 153 measured that it structurally cannot, and the roadmap's stated mechanism was corrected
rather than satisfied. If the hazard is mentioned at all, say the software path was chosen precisely
because it carries no over-voltage hazard on this pinout.
  </action>
  <acceptance_criteria>
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-GH12-COMMENT.md python3 152-check-claims.py` exits 0.
    - `grep -c '999.28' 152-GH12-COMMENT.md` ≥ 1.
    - `grep -c 'second release' 152-GH12-COMMENT.md` ≥ 1 — criterion 1's load-bearing clause.
    - `grep -c 'skip-sdp-unlock' 152-GH12-COMMENT.md` ≥ 1 — the surviving half is named precisely.
    - `grep -c 'lock-status' 152-GH12-COMMENT.md` ≥ 1.
    - `grep -c 'fw --install' 152-GH12-COMMENT.md` ≥ 1 — the second install half.
    - `grep -c 'dev test' 152-GH12-COMMENT.md` ≥ 1.
    - `grep -c 'No AT28C part was tested at any point in v1.32' 152-GH12-COMMENT.md` == 1.
    - `grep -c 'check_dispatch' 152-GH12-COMMENT.md` == 0.
    - `grep -c 'support_status' 152-GH12-COMMENT.md` == 0.
    - The two donor paragraphs survive: `grep -c "two halves don't survive equally" 152-GH12-COMMENT.md` == 1 and `grep -c "isn't the" 152-GH12-COMMENT.md` ≥ 1.
    - No release tag appears: `grep -cE '3\.0\.0b[0-9]+' 152-GH12-COMMENT.md` == 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-GH12-COMMENT.md" python3 152-check-claims.py; echo "GATE rc=$?"; for n in '999.28' 'second release' 'skip-sdp-unlock' 'lock-status' 'fw --install' 'No AT28C part was tested at any point in v1.32'; do printf '%s => ' "$n"; grep -c "$n" 152-GH12-COMMENT.md; done; printf 'tags => '; grep -cE '3\.0\.0b[0-9]+' 152-GH12-COMMENT.md; printf 'dispatch => '; grep -c 'check_dispatch' 152-GH12-COMMENT.md</automated>
  </verify>
  <done>The draft keeps the donor's two hardest paragraphs, states the second withdrawal with Backlog 999.28 named and no version promised, describes what actually shipped in D-11-exempt terms, asks for a fresh run naming both install halves, carries the mandated non-claim, and passes the gate.</done>
</task>

<task type="auto">
  <name>Task 3: Produce and commit the D-14 diff artifact against the 137 original</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.diff</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` — the adapted draft.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md` — the original.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 332-346 (D-14) — the requirement that the diff be committed so the review sees exactly what changed.
  </read_first>
  <action>
Generate a unified diff of the adapted draft against the 137 original and commit it as
`152-GH12-COMMENT.diff`:

`diff -u .planning/phases/137-*/137-GH12-COMMENT.md .planning/phases/152-*/152-GH12-COMMENT.md`

Note that `diff` exits 1 when files differ, which is the expected outcome here — capture the output,
do not treat rc=1 as a failure.

Prepend a short header to the artifact recording: the two file paths, the date, and one sentence per
change class (kept, changed, added, omitted) so the operator's wording review in Plan 152-14 can be
read against the diff rather than the whole file. Keep the header out of the diff hunks themselves so
the file remains machine-readable as a diff.

⚠ `152-GH12-COMMENT.diff` is deliberately **not** a `152-check-claims.py` target: it is a
`.diff`, not a claim-bearing markdown artifact, and it is absent from `_CAVEAT_RULES`. Do not add it
to `_DEFAULT_TARGETS`.

After committing, confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch.
  </action>
  <acceptance_criteria>
    - `.planning/phases/152-*/152-GH12-COMMENT.diff` exists and is non-empty.
    - `grep -c '^+++' 152-GH12-COMMENT.diff` ≥ 1 and `grep -c '^@@' 152-GH12-COMMENT.diff` ≥ 1 — it is a real unified diff with at least one hunk.
    - `grep -c '^+' 152-GH12-COMMENT.diff` ≥ 5 — the adaptation is substantive, not cosmetic.
    - `git -C /workspaces log --oneline -1 -- .planning/phases/152-*/152-GH12-COMMENT.diff` is non-empty.
    - `python3 152-check-claims.py` (defaults, no seam) still exits 0 — the `.diff` did not become a target.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; test -s 152-GH12-COMMENT.diff; echo "exists rc=$?"; grep -c '^@@' 152-GH12-COMMENT.diff; grep -c '^+' 152-GH12-COMMENT.diff; python3 152-check-claims.py; echo "DEFAULTS rc=$?"; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The adaptation diff is committed with a change-class header, the gate is still green on defaults, and HEAD is on the milestone branch.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` — the frozen OUT-01
  draft; a `_DEFAULT_TARGETS` member from Plan 152-13 onward; `_CAVEAT_RULES` entry
  `{no-at28c-part-tested}` (pre-populated by Plan 152-01)
- `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.diff` — the D-14 review
  artifact; deliberately never a gate target

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft → public comment | This file is posted verbatim in Plan 152-14; every sentence crosses into an irreversible public surface. |
| donor text → new facts | The 137 donor was true for v1.30. Two of its sentences are no longer honest and must be changed rather than inherited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-17 | Repudiation | inheriting the donor's "the work is queued" sentence unchanged after a second deferral | critical | mitigate | Task 2 replaces it explicitly; the `second release` and `999.28` greps are acceptance criteria. |
| T-152-18 | Information disclosure | naming the deferred command in a comment, tripping the fifth forbidden class | high | mitigate | The draft never names it — the donor does not either; the gate run on this file is an acceptance criterion. |
| T-152-19 | Spoofing | asking for a fresh run without naming the firmware half of the install, so the run reproduces the old failure | high | mitigate | `fw --install` grep criterion; the reason stated in the draft prose. |
| T-152-20 | Repudiation | repeating a mechanism claim Phase 153 disproved | medium | mitigate | Explicit prohibition on naming the dispatch checker as the guard; `check_dispatch` grep count of 0. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Gate green on the draft via the env seam.
- Eleven positive/negative greps as enumerated in Task 2's criteria.
- A real unified diff committed with at least one hunk and five added lines.
- Defaults run still green; HEAD on the milestone branch.
</verification>

<success_criteria>
The OUT-01 draft exists, is gate-clean, keeps the donor's two operator-reviewed paragraphs, states the
second withdrawal honestly with Backlog 999.28 named and no version promised, and is accompanied by a
committed diff the wording review can read.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-05-SUMMARY.md` when done.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; do not assert gh#12 is closed, resolved or fixed.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-05-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
