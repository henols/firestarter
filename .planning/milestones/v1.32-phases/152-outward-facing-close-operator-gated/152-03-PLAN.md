---
phase: 152-outward-facing-close-operator-gated
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements: [OUT-02, OUT-05]

must_haves:
  truths:
    - "ROADMAP criterion 2's OPEN list is amended to gh#21, gh#11, gh#12, with the pre-amendment wording retained and the amendment dated and attributed to 152-CONTEXT.md D-05."
    - "ROADMAP criterion 5's pairing clause is narrowed to claims about 0x0D write-path correctness or validation status, with statements of shipped user-visible command behaviour explicitly exempt, per D-11 — and the five forbidden classes are visibly untouched."
    - "The v1.32 milestone bullet no longer states that gh#32 is OPEN."
    - "Every ROADMAP and PROJECT citation of the FLAG_CAN_ERASE exclusion-tuple line number is corrected to the measured committed-tree line, with the drift history recorded."
    - "PROJECT.md carries one labelled correction block recording that Phase 121 D-12's stated premise is disproven (D-06 / D-15)."
    - "`check_record_corrections.py` still exits 0 after every edit."
  artifacts:
    - .planning/ROADMAP.md
    - .planning/PROJECT.md
  key_links:
    - "Both files are `check_record_corrections.py` default targets — every edit must be followed by a re-run of that gate."
    - "Only position-independent exemption mechanisms are used (`block`, `line-label`, `inline-history`, `inline-allow`), so no `lines=N` marker can be orphaned."
  prohibitions:
    - "Do NOT use the `gsd-tools` roadmap or requirements verbs — `_normalizeMd` reformats the WHOLE file. Hand-edit only."
    - "Do NOT introduce a `recordscan:supersedes ... lines=N` marker. There is currently no live `lines=N` marker in any file this phase edits and this phase must not create the first one."
    - "Do NOT restore criterion 1's or criterion 4's pre-amendment wording — both were amended 2026-08-20 and are correct as they stand."
    - "Do NOT weaken, delete or narrow any of criterion 5's five forbidden claim classes. D-11 narrows the PAIRING clause only and cannot reach them."
    - "Do NOT reopen gh#32."
    - "Do NOT restate a requirement count in ROADMAP.md — counts live in REQUIREMENTS.md's Coverage block only, so there is exactly one copy to keep true. Plan 152-04 owns that block."
    - "Do NOT edit `.planning/REQUIREMENTS.md` — Plan 152-04 owns it and runs in this same wave."
---

<objective>
Amend the in-repo record so no outward draft can inherit a stale claim: ROADMAP criterion 2's OPEN
list (D-05), criterion 5's pairing clause (D-11), the three stale record sites RESEARCH §A-11 found,
and the Phase 121 D-12 premise correction that D-15 leaves to this phase.

Purpose: five outward artifacts are drafted in wave 3 from these documents. A draft written from an
uncorrected record repeats the record's error into an irreversible public surface. This plan is the
sole writer of `ROADMAP.md` and `PROJECT.md` for the whole phase's amendment work.

Output: two hand-edited record files, every amendment dated, attributed and labelled, with the
record-corrections gate re-proven green.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **Hand-edit both files.** The `gsd-tools` requirements/roadmap verbs run `_normalizeMd` over the
whole file; the blast radius is measured and the bounded-diff criteria in this plan exist to catch it.

⚠ **Do NOT read `.planning/STATE.md` whole** (339 KB, one ~52k-char line). It is a
`check_record_corrections.py` target but this plan does not edit it. Give that gate `timeout 300`
even though it was measured at 23.3 s — a short timeout returns rc=124 and reads like a RED.

⚠ Capture the pre-edit diff baseline first: `git -C /workspaces rev-parse HEAD` and
`git -C /workspaces diff --numstat -- .planning/ROADMAP.md .planning/PROJECT.md` (expected empty).
</context>

<!-- planner-discipline-allow: 33/33 -->
<!-- planner-discipline-allow: recordscan:supersedes -->

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP criterion 2 (D-05) and criterion 5 (D-11) as dated labelled corrections</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 444-460 (`sed -n '444,460p'`) — Phase 152's block. Criteria 1 and 4 carry the exact **AMENDED 2026-08-20 (Phase 150 deferral)** shape this task must copy for criteria 2 and 5.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 123-144 (D-05) and 268-291 (D-11) — the two amendments, their rejected alternatives, and the requirement that the pre-amendment wording be retained.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 720-748 (Pattern A) — the five exemption verdicts `check_record_corrections.py` recognises, with a real in-repo example of each, and the rule that `superseded` is line-keyed and must be avoided.
    - `.planning/ROADMAP.md` line 604 and line 831 (`sed -n '604p;831p'`) — the two in-repo 146 CLOSE-04 labelled correction blocks, for the exact block shape.
  </read_first>
  <action>
Hand-edit two success criteria in `.planning/ROADMAP.md` §"Phase 152", using the same
**AMENDED [date] ([cause])** shape criteria 1 and 4 already carry, and retaining the
pre-amendment wording in a trailing parenthetical exactly as they do.

**Criterion 2 — D-05.** Prefix it `**AMENDED 2026-08-21 (152-CONTEXT.md D-05).**` and change the
OPEN list from four issues to three: gh#21, gh#11 and gh#12 are still OPEN at the close. Add, in the
retained-wording parenthetical: this criterion previously named gh#32 among the issues still OPEN;
gh#32 was CLOSED on 2026-08-08 with `stateReason: COMPLETED` and the operator's own fold comment
("Folded into #21 — same EPROM (at28c256). This report is preserved in the consolidated table
there."), which is 10 days before v1.32 opened — so the criterion was already false when written, and
the criterion's own phrase "with #32 folded in" describes exactly that state, which is why the
amendment makes it consistent with itself. Name the two rejected alternatives from D-05: reopening
gh#32, and reading "still OPEN" as "not closed by this milestone".

⚠ Write the closure statement in a phrasing the `152-check-claims.py` `issue-closed` row permits.
The row's alternation no longer contains 32, so the natural past-tense phrasing is fine here — but
`ROADMAP.md` is not a gate target and the sentence will be quoted into gate-scanned artifacts later,
so keep it in the shape `clean_control.md` proved clean.

**Criterion 5 — D-11.** Prefix it `**AMENDED 2026-08-21 (152-CONTEXT.md D-11).**` Narrow the
**pairing clause only**: the requirement that every permitted `0x0D` claim be paired with its explicit
non-claim is scoped to claims about `0x0D` **write-path correctness or validation status** — the
things silicon would have to confirm — and explicitly exempts statements of shipped, user-visible
command behaviour, giving D-11's own two examples (that standalone erase is now available on this
protocol, and that write no longer performs a blank check on it). State in the same breath, because
it is the load-bearing half: **the five forbidden claim classes are UNTOUCHED and this amendment
cannot reach them** — AT28C silicon validation, page-size validation on silicon, a `0x0D` graduation,
a `support_status` change, and the deferred deliberate-protection command named as shipped or
available all still get rejected, as do the unqualified claim words the gate's
`proven-unqualified`, `confirmed-working`, `datasheet-conformant` and `works-on-silicon` rows carry.
Add the inherited convention D-11 records: each release-note body carries the milestone-level
non-claim once, enforced by the gate's required-caveat table. Retain the pre-amendment wording in the
parenthetical. Note that Backlog 999.28's future promotion must reverse the fifth gate class in the
same change that lands the feature, or the gate will reject the very release notes announcing it.

Both amendments are position-independent labelled corrections. Do not use a `recordscan:supersedes`
marker.
  </action>
  <acceptance_criteria>
    - `grep -c 'AMENDED 2026-08-21 (152-CONTEXT.md D-05)' .planning/ROADMAP.md` == 1.
    - `grep -c 'AMENDED 2026-08-21 (152-CONTEXT.md D-11)' .planning/ROADMAP.md` == 1.
    - `sed -n '444,462p' .planning/ROADMAP.md | grep -c 'AMENDED 2026-08-20'` == 2 — criteria 1 and 4 are untouched.
    - The criterion-5 text still enumerates all five forbidden classes: `sed -n '444,462p' .planning/ROADMAP.md | grep -c 'graduation'` ≥ 1, `grep -c 'support_status'` ≥ 1, `grep -c 'page-size'` ≥ 1, `grep -c 'silicon'` ≥ 1.
    - `grep -c 'recordscan:supersedes' .planning/ROADMAP.md` is unchanged from its pre-edit value (measured 1 — a narrative mention at line 3581, not a live marker).
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'AMENDED 2026-08-21 (152-CONTEXT.md D-05)' /workspaces/.planning/ROADMAP.md; grep -c 'AMENDED 2026-08-21 (152-CONTEXT.md D-11)' /workspaces/.planning/ROADMAP.md; grep -c 'recordscan:supersedes' /workspaces/.planning/ROADMAP.md</automated>
  </verify>
  <done>Criteria 2 and 5 carry dated, attributed amendments with their pre-amendment wording retained; criteria 1 and 4 are untouched; the five forbidden classes are visibly intact.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three stale record sites and the Phase 121 D-12 premise</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/ROADMAP.md` line 37 (`sed -n '37p'`) — the v1.32 milestone bullet, which still states gh#32 is OPEN and still carries a requirement count.
    - `.planning/ROADMAP.md` lines 470-478 (`sed -n '470,478p'`) — Phase 153's criteria 2 and 6, both citing `firestarter_app/firestarter/database.py:621`.
    - `firestarter_app/firestarter/database.py` lines 580-645 (`sed -n '580,645p'`) — the committed tree. Measure the real exclusion-tuple line and the real span of the corrected reversal comment; do not inherit either number from any document.
    - `.planning/PROJECT.md` line 1333 (`sed -n '1333p'`) and lines 84-95 (`sed -n '84,95p'`) — the v1.22 decision-log row carrying the Phase 121 D-12 premise, and the workstream table (already carrying row 7 and the updated workstream-4 description — verify, do not re-do).
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 145-179 (D-06) and 347-360 (D-15) — the disproven premise, its two primary sources, and what D-15 leaves to this phase.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 424-450 (§A-11) and 537-590 (§B-4, §B-5, §B-7) — the three stale sites, the measured `:638`, and the fact that the code comment was already corrected by Phase 153.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 35-54 — the measured list of D-15 items **already done by Phase 153**. Grep before funding any of them.
  </read_first>
  <action>
Four hand-edits. Grep first for each; PATTERNS measured that most of D-15's PROJECT.md work is
already landed, and this plan must not redo it.

**(a) `ROADMAP.md:37`, the gh#32 state.** Amend the Evidence Ceiling sentence so it no longer states
gh#32 is OPEN. Replace the four-issue OPEN list with gh#21/#11/#12, and add a dated labelled
correction naming gh#32's 2026-08-08 closure with `stateReason: COMPLETED` and the fold into gh#21,
attributed to `152-CONTEXT.md D-05`. Same amendment class as Task 1, one site further out.

**(b) `ROADMAP.md:37`, the requirement count.** The bullet still says "33/33 requirements mapped",
which predates Phase 153. Do **not** substitute a new number here — REQUIREMENTS.md's Coverage block
is the single copy and Plan 152-04 owns it in this same wave. Replace the count with a pointer
sentence saying requirement counts are carried in `REQUIREMENTS.md`'s Coverage block only, so there is
exactly one copy to keep true, and label the change as a dated correction.

**(c) `ROADMAP.md:472` and `:476`, the `database.py` citations.** Both cite `:621`. Measure the real
sites in the committed tree first. RESEARCH §A-11(3) measured the exclusion tuple at
`database.py:629` and §B-7 measured the corrected reversal comment at `database.py:585-616`; both are
2026-08-21 readings and this task must re-measure rather than transcribe. Add a labelled correction
note recording the drift explicitly: `:621` was the pre-153 line, `:620` was Phase 153's own
measurement, and the committed tree now reads the measured line — the same citation has now drifted
three times, which is why the note records the history instead of silently overwriting the number.
Do not rewrite a completed phase's success-criterion prose; append the correction.

**(d) `PROJECT.md`, the Phase 121 D-12 premise — ONE labelled correction block.** Attach it to the
2026-07-30 v1.22 decision-log row at `:1333`, positioned so the block opener starts at the beginning
of a line (a `**⚠ CORRECTION (...)` line immediately after the table, not inside a table cell). It
must state: the premise recorded at `database.py:591` — that advertising the erase capability for
these chips was a false capability statement — is **disproven**; the capability is real in the silicon
(Microchip **DS20006386B** Table 6-1 Operating Modes, p11, with the Optional Chip Erase Mode paragraph
on the same page and the waveforms in §6.10, p15) and real in `infoic.xml` (the AT28C256 record's
`flags = 0x0000C010`, so the erasable bit is SET); what was false was only that *firestarter* could
perform it; Phase 153 closed that gap by the **software** AN 0544B six-byte path, not the datasheet's
12 V-on-OE hardware path, and restored the wire flag for algorithm 13; and the code comment half of
this correction was discharged by Phase 153 (ERASE-07), so this block is the `.planning`-side half
only. Attribute it `152-CONTEXT.md D-06 + D-15`, dated 2026-08-21.

⚠ Verify — do not re-do — the two D-15 items PATTERNS measured as already landed: `PROJECT.md`'s
three-firmware-touching-workstream count (`:45-47`), the workstream table's row 7 for Phase 153
(`:90`), and workstream 4's updated description (`:91`). If a grep confirms them present, record that
in the SUMMARY as verified-already-done and make no edit.

⚠ Do not name `tools/check_dispatch.py` as the mechanism that guards the 12 V-on-OE hazard. Phase 153
measured that it structurally cannot — it is database-and-dispatch-table scoped and never looks inside
a C++ handler body. The control that guards it is `firestarter/scripts/check_erase_no_vpp.py`. If this
block mentions the guard at all, name that one.
  </action>
  <acceptance_criteria>
    - `sed -n '37p' .planning/ROADMAP.md | grep -c 'gh#32'` ≥ 1 and the same line no longer asserts gh#32 is OPEN — proven by `sed -n '37p' .planning/ROADMAP.md | grep -c 'CLOSED'` ≥ 1.
    - `sed -n '37p' .planning/ROADMAP.md | grep -c '33/33'` == 0.
    - `sed -n '37p' .planning/ROADMAP.md | grep -c 'REQUIREMENTS.md'` ≥ 1 — the count is replaced by a pointer, not another number.
    - `grep -c 'database.py:621' .planning/ROADMAP.md` == 0.
    - `grep -cE 'database\.py:6[0-9][0-9]' .planning/ROADMAP.md` ≥ 1 and the cited number equals the line measured live in `firestarter_app/firestarter/database.py` in this task.
    - `grep -c 'check_erase_no_vpp' .planning/ROADMAP.md .planning/PROJECT.md` ≥ 0 and `grep -c 'check_dispatch.py' .planning/PROJECT.md` is unchanged from its pre-edit value — no new occurrence is introduced by this task.
    - `grep -c 'DS20006386B' .planning/PROJECT.md` == 1 — the correction block cites its primary source.
    - `grep -c '152-CONTEXT.md D-06' .planning/PROJECT.md` ≥ 1.
    - `grep -c 'recordscan:supersedes' .planning/PROJECT.md` == 0.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '37p' /workspaces/.planning/ROADMAP.md | grep -c '33/33'; grep -c 'database.py:621' /workspaces/.planning/ROADMAP.md; grep -c 'DS20006386B' /workspaces/.planning/PROJECT.md; grep -c 'recordscan:supersedes' /workspaces/.planning/PROJECT.md; grep -cE 'database\.py:6[0-9][0-9]' /workspaces/.planning/ROADMAP.md</automated>
  </verify>
  <done>All three stale record sites are corrected with their drift recorded, the Phase 121 D-12 premise carries one labelled correction block citing its primary sources, and no already-landed D-15 item was re-done.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the amendments landed without a whole-file normalise, and re-run the record-corrections gate</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py` — the gate. Its `_DEFAULT_TARGETS` are repo-root-relative and include `PROJECT.md`, `STATE.md` and `ROADMAP.md`; `REQUIREMENTS.md` is NOT a target.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 451-487 (§A-12) — the measured green baseline `{block: 23, line-label: 4, inline-history: 6, inline-allow: 10, superseded: 12}`, rc=0 in 23.3 s, and the finding that no live `lines=N` marker sits in any file this phase edits.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-VALIDATION.md` — the fourth validation question: a positive `grep -c` on the amendment's dated marker PAIRED with a bounded-diff assertion.
  </read_first>
  <action>
Two paired assertions per file, then the gate.

**Bounded diff.** Run `git -C /workspaces diff --numstat -- .planning/ROADMAP.md .planning/PROJECT.md`
and record the added/deleted counts verbatim in the SUMMARY. The expected bands are: `ROADMAP.md`
added+deleted **between 1 and 60**; `PROJECT.md` added+deleted **between 1 and 25**. A count above the
band means a whole-file `_normalizeMd` reformat happened — revert and hand-edit again. A count of zero
means nothing landed. Do **not** phrase this as "the file is byte-unchanged except…"; that criterion
class has broken in this project before.

**Dated markers.** Confirm each amendment's own dated marker is present by `grep -c`, as enumerated in
Tasks 1 and 2's criteria.

**Record-corrections gate.** Run
`timeout 300 python3 .planning/phases/130-*/check_record_corrections.py` and require rc=0. Record the
exemption-verdict tally it prints and compare it against the 2026-08-21 baseline above; the tally may
grow (new `block` / `line-label` entries) but `superseded` must not change, because this plan
introduces no line-keyed marker.

**HEAD check.** After committing, run `git -C /workspaces rev-parse --abbrev-ref HEAD` and confirm it
is still `gsd/v1.32-at28c-write-path-root-cause-report-provenance`. `gsd-tools query commit`'s
unanchored milestone regex has been measured to switch branches; check every time.
  </action>
  <acceptance_criteria>
    - `timeout 300 python3 .planning/phases/130-*/check_record_corrections.py` exits 0.
    - `git -C /workspaces diff --numstat HEAD~1 -- .planning/ROADMAP.md` (post-commit) shows added+deleted ≥ 1 and ≤ 60.
    - `git -C /workspaces diff --numstat HEAD~1 -- .planning/PROJECT.md` (post-commit) shows added+deleted ≥ 1 and ≤ 25.
    - The printed `superseded` exemption count equals 12 — unchanged from the 2026-08-21 baseline.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
  </acceptance_criteria>
  <verify>
    <automated>timeout 300 python3 /workspaces/.planning/phases/130-*/check_record_corrections.py; echo "RECORDGATE rc=$?"; git -C /workspaces diff --numstat -- .planning/ROADMAP.md .planning/PROJECT.md; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>Both amendments are proven present by their dated markers and proven surgical by a bounded diff; the record-corrections gate is green with an unchanged `superseded` count; HEAD is still on the milestone branch.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**Modified files** (hand-edited, no `gsd-tools` verb)
- `.planning/ROADMAP.md` — criterion 2 amendment (D-05), criterion 5 amendment (D-11), the `:37`
  gh#32 state correction, the `:37` requirement-count pointer, the two `database.py` citation
  corrections
- `.planning/PROJECT.md` — one labelled correction block for the Phase 121 D-12 premise (D-06/D-15)

**New record markers**
- `AMENDED 2026-08-21 (152-CONTEXT.md D-05)` — ROADMAP criterion 2 and the `:37` bullet
- `AMENDED 2026-08-21 (152-CONTEXT.md D-11)` — ROADMAP criterion 5
- `⚠ CORRECTION (Phase 152 / 152-CONTEXT.md D-06 + D-15 — 2026-08-21)` — PROJECT.md

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| record file → outward draft | Wave 3 authors five public artifacts from these documents; an uncorrected record propagates into an irreversible public surface. |
| tooling → record file | The `gsd-tools` roadmap/requirements verbs reformat the whole file; hand-editing is the boundary control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-09 | Tampering | a record edit silently reformatting a whole file | high | mitigate | Hand-edit only, plus a bounded-diff assertion per file with an explicit expected band; a count above the band means revert and redo. |
| T-152-10 | Tampering | orphaning a line-keyed `recordscan:supersedes lines=N` marker | medium | mitigate | Only position-independent mechanisms used; the `superseded` exemption count asserted unchanged at 12. |
| T-152-11 | Repudiation | narrowing criterion 5's five forbidden classes while narrowing its pairing clause | critical | mitigate | Task 1 asserts all five classes still enumerated in the criterion text after the edit; the prohibition is stated in `must_haves.prohibitions`. |
| T-152-12 | Tampering | `gsd-tools query commit` switching branches on a stale milestone regex | medium | mitigate | `git rev-parse --abbrev-ref HEAD` asserted after committing. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** The record gate is stdlib-only. |
</threat_model>

<verification>
- `timeout 300 python3 .planning/phases/130-*/check_record_corrections.py` → rc 0, `superseded` = 12.
- Both dated amendment markers present exactly once each.
- Bounded diffs: ROADMAP.md 1..60 changed lines; PROJECT.md 1..25.
- No `database.py:621` citation remains in ROADMAP.md; the substituted line number was measured live.
- HEAD still on `gsd/v1.32-at28c-write-path-root-cause-report-provenance`.
</verification>

<success_criteria>
Every stale claim the wave-3 drafts could inherit is corrected, dated and attributed; criteria 1 and 4
are untouched; criterion 5's five forbidden classes are intact; the record-corrections gate is green.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-03-SUMMARY.md` when done.

Record verbatim in it: the two bounded-diff `--numstat` outputs, the record gate's exemption-verdict
tally, the live-measured `database.py` line numbers, and which D-15 items were verified
already-done rather than edited.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary. In particular: do not assert that gh#21, gh#11 or gh#12 is closed,
resolved or fixed; do not name the deferred deliberate-protection command except in the mandated
withdrawal word order; cite any forbidden phrasing by `file:line` and label rather than reproducing
it.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-03-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
