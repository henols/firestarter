---
phase: 152-outward-facing-close-operator-gated
plan: 17
type: execute
wave: 11
depends_on: ["152-16"]
files_modified: []
autonomous: false
requirements: [OUT-04]

must_haves:
  truths:
    - "The app release body is POSTED to the tag read in Plan 152-12, and the stored body is byte-identical to the frozen draft."
    - "The body length moved from its measured starting value to a non-zero value."
    - "The posted-mode gate run over the stored body exits 0 — so the withdrawal sentence's word order is proven correct in the PUBLISHED text, not only in the draft."
    - "The withdrawal sentence and all three non-claim sentences are proven present in the PUBLISHED body by positive greps."
    - "No previously-published release body was edited."
  artifacts: []
  key_links:
    - "Criterion 5 forbids the deferred command as shipped; criterion 4 forbids leaving it unmentioned. The gate proves the first on the published text; the positive greps prove the second."
    - "Posted-mode temp files are written under the SAME basename as the draft, because the caveat lookup is basename-keyed and fails closed."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Task 1's configuration guard is a partial control, not a substitute for the operator."
    - "Do NOT edit any release other than the tag read in Plan 152-12. The historical published bodies stay exactly as published."
    - "Do NOT backfill any bodiless release."
    - "Do NOT change the release's prerelease flag, its tag, its target commit, its title or its assets. Only the body is set."
    - "Do NOT run `gh workflow run`."
    - "Do NOT alter the draft in this plan. Wording changes go back through the gate before this plan proceeds."
    - "Do NOT accept a body diff that differs in content. A difference confined to trailing-whitespace or line-ending normalisation must be recorded explicitly as transport normalisation, with the exact bytes named — never waved through."
---

<objective>
Post the app release body to the tag CI cut, and prove the published text is the reviewed text.

Purpose: OUT-04's verb is "announce", and the release body is the most-read artifact this project
produces. Neither release workflow attaches a body, so a body exists only if it is set by hand — which
is why every release since a specific point in this project's history has a body length of zero.

Output: one published release body.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** Approval given for the three comment posts
does not carry here. D-03 requires a separate blocking gate per artifact.

The tag was READ in Plan 152-12 from the release list after the merge, with its command and timestamp
recorded. Use that value. Do not recompute or guess it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-post checks and the measured starting body length</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-17-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` — the frozen draft with its tags substituted. Read it in full; this is the last read before it is public.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-12-SUMMARY.md` — the read tag, its reading command and timestamp, the target commit, the registry findings, and the measured starting body length.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-08-SUMMARY.md` — the placeholder contract, the recorded posting invocation and the three wording-review items for this checkpoint.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1174-1236 (§E-1, §E-3) — the workflow's absent body field and the automatic registry publish, so the SUMMARY can state why a manual body is required at all.
  </read_first>
  <action>
Six checks, in this order. Record every command and its output.

1. `python3 .planning/phases/152-*/152-check-not-auto.py` — must exit 0, else **stop**.
2. `python3 .planning/phases/152-*/152-check-claims.py` on defaults — rc 0; then through the env seam on
   the app release-notes draft alone — rc 0. Record both PASS lines.
3. `python3 -m pytest .planning/phases/152-*/test_check_claims_152.py -q -o addopts=""` — fully green,
   count line recorded.
4. Confirm the draft still carries the withdrawal sentence and all three non-claim sentences, by
   positive grep, and that no placeholder token remains. These are the criterion-4 half the gate cannot
   express.
5. Read the release the body is going to: `gh release view <the read tag> --repo henols/firestarter_app
   --json tagName,isPrerelease,targetCommitish,createdAt,body` — record the tag, the prerelease flag,
   the target commit and the **current body length** as a number. That number is the starting value the
   non-vacuity guard compares against. Confirm the tag matches the one Plan 152-12 read.
6. Re-verify the registry independently: read the registry directly and confirm the cut pre-release is
   present, and record the stable channel's own state. The body must not imply a stable version is
   installable if the registry does not have it.

Nothing proceeds on a red.
  </action>
  <acceptance_criteria>
    - `152-check-not-auto.py` exits 0 with the live configuration value recorded.
    - The gate exits 0 on defaults and on the app draft; both PASS lines recorded.
    - The paired suite is fully green with its count line recorded.
    - `grep -c 'sdp-relock' 152-RELEASE-NOTES-app.md` ≥ 1; `grep -c 'No AT28C part was tested at any point in v1.32'` == 1; `grep -c 'software-proven and unvalidated on silicon'` ≥ 1; `grep -c 'APP_TAG_TBD\|FW_TAG_TBD'` == 0.
    - The target release's tag matches the tag Plan 152-12 read, its prerelease flag is recorded, and its current body length is recorded as a number.
    - The registry read is recorded, including whether the cut pre-release is present and the stable channel's own state.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; python3 152-check-claims.py; echo "GATE-DEFAULTS rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-RELEASE-NOTES-app.md" python3 152-check-claims.py; echo "GATE-DRAFT rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; for n in 'sdp-relock' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon' 'APP_TAG_TBD'; do printf '%s => ' "$n"; grep -c "$n" 152-RELEASE-NOTES-app.md; done; TAG=$(gh release list --repo henols/firestarter_app --limit 5 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease)][0].tagName'); echo "TAG=$TAG"; gh release view "$TAG" --repo henols/firestarter_app --json tagName,isPrerelease,targetCommitish,createdAt --jq '.'; gh release view "$TAG" --repo henols/firestarter_app --json body -q '.body | length'; curl -s https://pypi.org/pypi/firestarter/json | python3 -c "import sys,json; d=json.load(sys.stdin); print('stable', d['info']['version'])"</automated>
  </verify>
  <done>All gates are green, the draft's criterion-4 presence greps hold with no placeholder left, the target release is confirmed with its starting body length measured, and the registry is re-verified independently.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review — authorise the app release body</name>
  <what-built>
`152-RELEASE-NOTES-app.md` — the host release body for the tag CI cut, with every version substituted
from a read rather than a prediction. The claim gate is green on it, the paired suite is green, the
criterion-4 presence greps hold, and the registry has been re-verified independently. Nothing has been
posted. Approving this checkpoint sets the body on a public release.
  </what-built>
  <how-to-verify>
1. Read `152-RELEASE-NOTES-app.md` in full.
2. Check the **opening version paragraph**: every version must be accompanied by where it was read from
   and when. Check the stable-channel sentence specifically — the release list and the package registry
   have been measured divergent on that channel, and this sentence must not imply a stable version is
   installable if the registry does not have it.
3. Check the **withdrawal sentence**: the deferred command's name followed immediately by a withdrawal
   predicate, naming the backlog item. Any other word order is rejected by the gate, so if you want
   different wording, say so now rather than after posting.
4. Check the **protection-read command paragraph**: it must lead with what the command actually does —
   report a real state read from the silicon, or refuse with a named actionable reason — because on the
   family this thread is about, the refusal is the honest answer. It must be named beta-channel only
   and matched-firmware-required.
5. Check the **Removed section**: does a reader learn from it that the previously-announced protection
   command is gone, and which half survives automatically? The published historical notes are not being
   edited, so this section is the only place that correction reaches anyone.
6. Check the **established / not established** split: the not-established list should read as a
   transcription of what the work did not prove, not a summary of it.
7. Check the **ask**, and that both install halves are named.
8. Confirm no count is cited that was not measured, and that nothing claims a silicon result.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: set the app release body" or describe the wording changes required first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Set the body, prove the published text is ours, and re-prove both criterion halves on it</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-17-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-08-SUMMARY.md` — the recorded posting and verification invocations.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-VALIDATION.md` — the release-body proof: the body diff for identity and the length change for non-vacuity.
  </read_first>
  <action>
Set the body exactly once, then prove it published as authored.

**Set.** `gh release edit <the read tag> --repo henols/firestarter_app --notes-file <the frozen draft>`.
Record the command and its output. Set **only** the notes — do not pass any flag that would change the
tag, the target commit, the title, the prerelease flag or the assets.

**Prove identity.** `gh release view <tag> --repo henols/firestarter_app --json body --jq '.body'` into a
temp file **named `152-RELEASE-NOTES-app.md`** — the same basename as the draft, because the gate's
caveat lookup is basename-keyed and fails closed — then `diff -u <the frozen draft> <that file>`. The
diff must be empty. Paste the result.
⚠ If the diff is non-empty, do not proceed and do not re-post. Inspect it. A difference confined to
line-ending or trailing-whitespace normalisation must be recorded explicitly as transport
normalisation, naming the exact bytes, and only then treated as acceptable. Any difference in content
is a failure.

**Prove non-vacuity.** Re-read the body length and assert it changed from the starting value measured in
Task 1 to a non-zero value. Record both numbers.

**Re-prove both criterion halves on the PUBLISHED text**, not the draft:
- the posted-mode gate over the same-basename temp file must exit 0 — this is criterion 5's half, and it
  is what proves the withdrawal sentence's word order survived publication;
- positive greps over the same temp file must find the withdrawal sentence and all three non-claim
  sentences — this is criterion 4's half, which the gate cannot express.

**Prove nothing else moved.** Re-read the release's tag, target commit, prerelease flag and asset list
and confirm each matches Task 1's capture. Then confirm that no other release's body length changed:
read the body lengths of the historical published releases and of the bodiless ones and compare them
against the values Plan 152-12 recorded.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - The body diff between the frozen draft and the published body produces no output, or produces only a normalisation difference recorded byte-by-byte in the SUMMARY.
    - The body length changed from the Task 1 starting value to a non-zero value, and both numbers are recorded.
    - The posted-mode gate run over the same-basename temp file exits 0.
    - Positive greps over the published body: `sdp-relock` ≥ 1, `No AT28C part was tested at any point in v1.32` == 1, `software-proven and unvalidated on silicon` ≥ 1.
    - The release's tag, target commit, prerelease flag and asset list match Task 1's capture exactly.
    - No other release's body length changed from the values Plan 152-12 recorded.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; TAG=$(gh release list --repo henols/firestarter_app --limit 5 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease)][0].tagName'); echo "TAG=$TAG"; T=$(mktemp -d); gh release view "$TAG" --repo henols/firestarter_app --json body --jq '.body' > "$T/152-RELEASE-NOTES-app.md"; diff -u 152-RELEASE-NOTES-app.md "$T/152-RELEASE-NOTES-app.md"; echo "BODYDIFF rc=$?"; gh release view "$TAG" --repo henols/firestarter_app --json body -q '.body | length'; FIRESTARTER_CLAIMSCAN_TARGETS_152="$T/152-RELEASE-NOTES-app.md" python3 152-check-claims.py; echo "POSTEDGATE rc=$?"; for n in 'sdp-relock' 'No AT28C part was tested at any point in v1.32' 'software-proven and unvalidated on silicon'; do printf '%s => ' "$n"; grep -c "$n" "$T/152-RELEASE-NOTES-app.md"; done; gh release view "$TAG" --repo henols/firestarter_app --json tagName,isPrerelease,targetCommitish,assets --jq '{tagName,isPrerelease,targetCommitish,assets:[.assets[].name]}'; for t in 3.0.0b14 3.0.0b15 2.0.8; do printf '%s => ' "$t"; gh release view "$t" --repo henols/firestarter_app --json body -q '.body | length'; done; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The app release body is published, diffs clean against the frozen draft, its length moved from the measured start to non-zero, both criterion halves are re-proven on the published text, and nothing else about the release or any other release changed.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** One published release body on the host repository's cut pre-release.

**Recorded values handed forward to `152-LEDGER.md` (Plan 152-19)**
- The tag, the release URL, and the body length before and after
- The body-diff result, including any recorded transport normalisation
- The posted-mode gate PASS line and the three positive-grep counts over the published text
- The unchanged-elsewhere evidence: tag, target commit, prerelease flag, assets, and the other
  releases' body lengths

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen draft → published release body | The most-read surface this project has. Watchers are notified. |
| release-list state → registry state | Measured divergent on the stable channel; an inferred install claim is a false one. |
| this release → every other release | A body-setting command aimed at the wrong tag would rewrite a historical published body, which is explicitly forbidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-75 | Elevation of privilege | an auto or chained run auto-approving this checkpoint | critical | mitigate | The configuration guard runs first and fails closed; the prohibition is restated. ⚠ Partial control; residual risk stated. |
| T-152-76 | Repudiation | the published body differing from the reviewed text | critical | mitigate | Body diff against the frozen draft; any non-empty diff halts the task and must be explained byte-by-byte. |
| T-152-77 | Repudiation | the withdrawal sentence surviving review but not publication | high | mitigate | Both criterion halves re-proven on the PUBLISHED text: the posted-mode gate for word order, positive greps for presence. |
| T-152-78 | Tampering | editing a historical published body by aiming at the wrong tag | high | mitigate | The tag is taken from Plan 152-12's read and re-confirmed in Task 1; the other releases' body lengths are compared against recorded values afterwards. |
| T-152-79 | Spoofing | implying a stable version is installable from the registry | high | mitigate | The registry is re-read in Task 1 and the stable-channel sentence is an explicit checkpoint review item. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Auto guard, defaults gate, draft gate and paired suite green before the checkpoint.
- Draft presence greps hold; no placeholder remains; registry re-read.
- Body diff empty (or normalisation-only, recorded byte-by-byte); length moved to non-zero.
- Posted-mode gate green and all three presence greps hold on the published text.
- Release metadata unchanged; no other release's body length moved.
</verification>

<success_criteria>
The host half of OUT-04 is discharged: the announced version exists, was read rather than predicted,
and its published body is provably the reviewed text — with the deferred command named as withdrawn in
the one word order the gate permits.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-17-SUMMARY.md` when done.

Record verbatim: all six pre-post check results, the starting and final body lengths, the body diff
result, the posted-mode PASS line, the three published-text grep counts, the release-metadata
comparison, and the other releases' body-length comparison.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; if it refers to the deferred command, use the mandated withdrawal word
order.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-17-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
