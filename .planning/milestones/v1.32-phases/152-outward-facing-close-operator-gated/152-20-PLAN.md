---
phase: 152-outward-facing-close-operator-gated
plan: 20
type: execute
wave: 14
depends_on: ["152-19"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py
  - .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py
  - .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OUT-01, OUT-02, OUT-03, OUT-04, OUT-05]

must_haves:
  truths:
    - "`_DEFAULT_TARGETS` names all eight claim-bearing artifacts plus every one of this phase's SUMMARY files except the last, and the gate exits 0 over the whole set."
    - "The final SUMMARY is scanned via positional argv and that run is pasted into the transcript's final-target-list section."
    - "All five OUT requirement checkboxes are flipped to complete and all five traceability rows read Complete — and each flip is justified by named evidence from the plan that discharged it."
    - "The meta repository reaches `beta` via a pull request, and the merge record states unambiguously that the merges are done, how to verify that, and which tail commits are not yet on `beta`."
    - "No requirement is marked complete whose evidence this phase did not produce."
  artifacts: []
  key_links:
    - "The ordering trap: a `_DEFAULT_TARGETS` entry absent from disk makes the gate non-zero, so the final SUMMARY is scanned via argv rather than added to the list."
    - "The meta pull request is last so this phase's own closing artifacts are included in it; the tail after it is named explicitly for the milestone close."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** The checkpoint in this plan reviews five requirement completions and a repository merge; `--auto`/`--chain` auto-approves human-verify checkpoints and `autonomous: false` alone is not self-protecting."
    - "Do NOT add `152-20-SUMMARY.md` to `_DEFAULT_TARGETS`. It does not exist while this plan runs and its presence in the list would make the gate non-zero."
    - "Do NOT mark any OUT requirement complete without naming the plan and the measured evidence that discharged it."
    - "Do NOT use the `gsd-tools` requirements verb — hand-edit. `_normalizeMd` reformats the whole file."
    - "Do NOT flip the ROADMAP phase checkbox or write the phase-complete state here — that belongs to verification and close. If a close tool is run later, snapshot ROADMAP.md first and diff afterwards: that tool has been measured to clobber an unrelated phase's plan-count line."
    - "Do NOT re-merge either sub-repo. They are already on `beta`; verify with `git cherry`, never with ancestry."
    - "Do NOT re-pin gitlinks to the published branches — record the intended pins and leave the re-pinning to the milestone close."
---

<objective>
Close the phase: arm the gate over the whole artifact set including the SUMMARY files, flip the five
requirement checkboxes on named evidence, merge the meta repository to `beta`, and hand the milestone
close an unambiguous record of what is done and what is not.

Purpose: the phase's five public acts are complete. What remains is making the gate cover everything it
should, saying truthfully in the requirements record which obligations were discharged, and leaving the
close unable to re-merge by mistake or to inherit a stale tail.

Output: an eight-plus-nineteen target set, five flipped checkboxes, one meta pull request, and a
completed merge record.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** The checkpoint in Task 2 is the review that
five requirement completions and a repository merge get.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend _DEFAULT_TARGETS over the SUMMARY files and record the final target list</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py, .planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py, .planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` — the current eight-entry `_DEFAULT_TARGETS` and the pre-populated caveat map, which already carries every SUMMARY basename.
    - `.planning/phases/152-outward-facing-close-operator-gated/test_check_claims_152.py` — the arming leg's literal membership assertion.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 896-931 (§C-7) — the ordering trap and its three consequences, including that the final SUMMARY is scanned via positional argv.
    - `.planning/phases/149-firmware-page-size-seam-dual-repo-lockstep/149-CLAIM-GATE-TRANSCRIPTS.md` lines 232-274 — the final-target-list section shape, which records exactly this argv manoeuvre.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CLAIM-GATE-TRANSCRIPTS.md` — the `## Final target list` stub left by Plan 152-02.
  </read_first>
  <action>
Extend `_DEFAULT_TARGETS` from eight entries to eight plus every `152-NN-SUMMARY.md` that exists on
disk — that is the SUMMARY files for Plans 152-01 through 152-19 — each an explicit
`os.path.join(_HERE, ...)` on its own line, enumerated, never a glob. Do **not** add
`152-20-SUMMARY.md`: it does not exist while this plan runs, and the fail-closed missing-target branch
would make the gate non-zero for the rest of the phase. Add a comment above the SUMMARY block naming
that reason.

Do not touch `_CAVEAT_RULES` — every SUMMARY basename was pre-populated in Plan 152-01 with the
software-proven caveat label.

Then extend the arming leg in the suite so it asserts, precisely: the eight named claim-bearing
basenames are members; the SUMMARY members are exactly the nineteen `152-01`-through-`152-19`
basenames; and `152-20-SUMMARY.md` is **not** a member. Add a comment on that last assertion naming the
ordering trap, so a future reader does not "fix" it.

Run and record:
- the claim gate with no argv and no seam — rc 0 over the full set, with the untruncated PASS line;
- the paired suite — fully green with its count line;
- an **argv run** over the nineteen SUMMARY files passed positionally, proving the positional path works
  on exactly the class of file that will carry the final one.

If any SUMMARY is red, the fix is in that SUMMARY's text — it is missing the mandated caveat sentence or
carries a forbidden phrase. **Fix the SUMMARY, never the pattern and never the caveat map.**

Then fill the transcript's `## Final target list` section with: the extended list; the untruncated
defaults GREEN; the argv run; and a note stating that `152-20-SUMMARY.md` is scanned via positional argv
after this plan's own SUMMARY is written, giving the exact command. The pasted result of that final argv
run is appended by this plan's output step.
  </action>
  <acceptance_criteria>
    - Introspection shows exactly 27 `_DEFAULT_TARGETS` entries (eight claim-bearing artifacts plus nineteen SUMMARY files), every one existing on disk, every dirname the phase directory, every basename starting with `152-`.
    - `152-20-SUMMARY.md` is not among them.
    - `python3 152-check-claims.py` exits 0 with an untruncated PASS line naming every target.
    - `python3 -m pytest test_check_claims_152.py -q -o addopts=""` is fully green including the extended arming leg and the caveat-completeness leg.
    - The argv run over the nineteen SUMMARY files exits 0.
    - The transcript's final-target-list section contains the extended list, the defaults GREEN, the argv run, and the command for the final SUMMARY's argv scan.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-claims.py; echo "GATE rc=$?"; python3 -c "import importlib.util,os; s=importlib.util.spec_from_file_location('m','152-check-claims.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); b=[os.path.basename(t) for t in m._DEFAULT_TARGETS]; print('n=',len(b)); print('has20=', '152-20-SUMMARY.md' in b); print(sorted(b))"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; python3 152-check-claims.py 152-0*-SUMMARY.md 152-1*-SUMMARY.md; echo "ARGV rc=$?"; grep -c 'Final target list' 152-CLAIM-GATE-TRANSCRIPTS.md</automated>
  </verify>
  <done>The gate covers every claim-bearing artifact and every SUMMARY but the last, the arming leg pins that shape including the deliberate exclusion, and the transcript records the extended list, the GREEN and the argv path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator review — authorise the five requirement completions and the meta merge</name>
  <what-built>
The claim gate now covers every claim-bearing artifact and every plan SUMMARY but the last, and is
green. All five public acts are complete and each was verified by re-reading what GitHub stored. What
remains is to mark the five requirements complete and to merge the meta repository to `beta`.
Nothing has been marked complete and no merge has happened yet.
  </what-built>
  <how-to-verify>
1. Review the five completion claims, one at a time, against the evidence each names:
   - the gh#12 reply: read the recorded comment URL, and check that the stored-body diff was empty and
     the comment count advanced by exactly one;
   - the gh#21 comment: the same, plus that the published branch was confirmed to carry the provenance
     fix before it was posted;
   - the gh#11 reply: the same, plus that the quoted commitment was verified character-identical;
   - both release bodies: the recorded release URLs, the body lengths before and after, and that the
     posted-mode gate was green on the published text of each;
   - the claim gate: that it was seen to reject the specified planted violation both in isolation and
     while armed at the real artifacts, and that the paired suite is green.
2. Confirm no requirement is being marked complete on the strength of a plan's own claim rather than a
   measured artifact.
3. Confirm the intended merge method for the meta repository's pull request, and that you are content
   for this phase's planning artifacts to land on `beta`.
4. Confirm you understand the tail: any commit made to the meta repository after that merge — including
   this plan's own SUMMARY and anything verification produces — will not be on `beta`, and the merge
   record names that explicitly for the close to pick up.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: flip the five requirements and merge meta, method squash, merge or rebase" or name what must change first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip the five requirement checkboxes on named evidence, merge meta to beta, and complete the merge record</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Outward-Facing Close (OUT)" and the traceability rows for OUT-01 through OUT-05 — the five checkboxes and the five rows this task edits.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md` — the record written in Plan 152-12, whose meta-repository row is deliberately still open.
    - The SUMMARY files for Plans 152-14 through 152-19 — the measured evidence each flip is justified by.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1250-1281 (§E-5) — the handoff record's six items and the three close-time warnings.
  </read_first>
  <action>
Three things, in this order.

**(a) Flip the five requirement checkboxes — explicitly, because this step is measured to get skipped.**
In `.planning/REQUIREMENTS.md`, hand-edit OUT-01 through OUT-05 from unchecked to checked, and change
each one's traceability row from pending to complete. For each, append a one-line evidence citation
naming the plan that discharged it and the measured artifact — a comment URL, a release URL, or a gate
run — so no flip rests on a plan's own claim. Then run the bounded-diff check: the changed-line count
must be small and consistent with five checkbox flips, five row edits and five citations; a count in
the hundreds means a whole-file reformat and must be reverted and redone by hand.

⚠ Do **not** flip the ROADMAP phase checkbox or write phase-complete state here. If a close tool is run
later, snapshot `ROADMAP.md` first and diff it afterwards — that tool has been measured to clobber an
unrelated phase's plan-count line, and this project has had to repair that damage before.

**(b) Merge the meta repository to `beta` via a pull request.** Commit everything outstanding in the
meta repository first, leaving the enumerated pre-existing untracked files alone. Then
`gh pr create --repo <meta repo> --base beta --head gsd/v1.32-at28c-write-path-root-cause-report-provenance`,
read `mergeable`/`mergeStateStatus`, resolve any conflict by hand and record it, and merge with the
method the operator named. Record the PR number, URL and the method actually used, read back from the
API. Afterwards `git fetch origin` and `git cherry origin/beta HEAD` — record the output. The meta
repository has no release workflow, so this merge cuts nothing; state that in the record.

**(c) Complete the merge record.** Fill the meta-repository row in `152-MERGE-RECORD.md` with the PR
number, URL and merge method, and add a clearly-labelled **tail** section naming, by path, every commit
that will be made to the meta repository after this merge — this plan's own SUMMARY, and whatever
verification and close produce — and stating that those are **not** on `beta` and must be pushed onto
`beta` by the close rather than by opening a second pull request or re-merging either sub-repository.
Restate the no-re-merge instruction literally. Re-run the gate through the env seam over the merge
record afterwards; it is a gate target and must still exit 0.

Confirm `git rev-parse --abbrev-ref HEAD` in all three repositories after every commit.
  </action>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*OUT-0' .planning/REQUIREMENTS.md` == 5 and `grep -c '^- \[ \] \*\*OUT-0' .planning/REQUIREMENTS.md` == 0.
    - `grep -cE '^\| OUT-0[1-5] \|.*\| Complete' .planning/REQUIREMENTS.md` == 5 and no OUT row still reads pending.
    - Each of the five bullets carries an evidence citation naming a plan and a measured artifact.
    - `git -C /workspaces diff --numstat HEAD~1 -- .planning/REQUIREMENTS.md` shows added+deleted ≥ 1 and ≤ 40.
    - The meta pull request is `MERGED` against base `beta`, with its number, URL and the method read back from the API recorded.
    - `git -C /workspaces cherry origin/beta HEAD` produces only `-` lines (or no output) at the point it is run.
    - `152-MERGE-RECORD.md` has a filled meta row and a labelled tail section naming the not-yet-on-`beta` commits by path, and it still passes the gate through the env seam.
    - `git rev-parse --abbrev-ref HEAD` is the milestone branch in all three repositories.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces; grep -c '^- \[x\] \*\*OUT-0' .planning/REQUIREMENTS.md; grep -c '^- \[ \] \*\*OUT-0' .planning/REQUIREMENTS.md; grep -cE '^\| OUT-0[1-5] \|.*\| Complete' .planning/REQUIREMENTS.md; git diff --numstat -- .planning/REQUIREMENTS.md; git fetch origin --quiet; git cherry origin/beta HEAD; echo "CHERRY-END"; cd .planning/phases/152-outward-facing-close-operator-gated; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-MERGE-RECORD.md" python3 152-check-claims.py; echo "MERGEREC rc=$?"; python3 152-check-claims.py; echo "DEFAULTS rc=$?"; for r in /workspaces /workspaces/firestarter /workspaces/firestarter_app; do git -C $r rev-parse --abbrev-ref HEAD; done</automated>
  </verify>
  <done>All five OUT requirements are complete with named measured evidence and a bounded diff, the meta repository is merged to `beta` via a pull request with its method recorded, and the merge record names the tail that is not yet on `beta`.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**Modified files**
- `152-check-claims.py` — `_DEFAULT_TARGETS` extended from 8 to 27 entries (the eight claim-bearing
  artifacts plus nineteen SUMMARY files); `152-20-SUMMARY.md` deliberately excluded, with the reason in
  a comment
- `test_check_claims_152.py` — the arming leg extended to pin the eight named basenames, the nineteen
  SUMMARY basenames, and the deliberate exclusion of the final one
- `152-CLAIM-GATE-TRANSCRIPTS.md` — the final-target-list section
- `152-MERGE-RECORD.md` — the meta-repository row and the labelled tail section
- `.planning/REQUIREMENTS.md` — five checkbox flips, five traceability rows, five evidence citations

**Handoff left for the milestone close**
- Archive the roadmap and requirements documents; the milestone entry; the meta tag; gitlink re-pinning
  to the published branches; the stable-promotion decision, which is operator-gated
- The tail commits named by path in `152-MERGE-RECORD.md`, to be pushed onto `beta` without a second
  pull request and without re-merging either sub-repository
- The three close-time warnings already recorded in `152-MERGE-RECORD.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a plan's own claim → a requirement marked complete | A checkbox flipped on a plan's assertion rather than on a measured artifact is the failure class this project has hit before. |
| this phase → the milestone close | The close can re-merge, clobber a roadmap line, or inherit a tail that is not on `beta`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-91 | Spoofing | marking a requirement complete without measured evidence | high | mitigate | Each flip carries a citation naming the plan and the measured artifact; the checkpoint reviews all five against their recorded evidence. |
| T-152-92 | Tampering | the requirements edit reformatting the whole file | high | mitigate | Hand-edit only, plus a bounded-diff band of 1..40 changed lines. |
| T-152-93 | Tampering | adding the final SUMMARY to the target list and reddening the gate | high | mitigate | Explicit prohibition, an acceptance criterion asserting its absence, and an arming-leg assertion with the reason in a comment. |
| T-152-94 | Tampering | the close re-merging a sub-repository on a false ancestry signal | high | mitigate | The merge record carries the post-merge `git cherry` captures and the literal no-re-merge instruction; the meta row and tail section complete it. |
| T-152-95 | Tampering | a close tool clobbering an unrelated phase's roadmap line | medium | mitigate | This plan does not write phase-complete state and instructs a snapshot-and-diff around any later close tool run. |
| T-152-96 | Repudiation | weakening a gate assertion to make a red SUMMARY pass | high | mitigate | Task 1 states the fix is in the SUMMARY's own text, never in the pattern or the caveat map. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- 27 `_DEFAULT_TARGETS` entries, all present on disk, the final SUMMARY excluded.
- Gate green over the full set with an untruncated PASS line; suite fully green; argv run over the
  nineteen SUMMARY files green.
- Five OUT checkboxes checked, five traceability rows complete, five evidence citations, bounded diff.
- Meta pull request `MERGED`; `git cherry` all `-`; merge record gate-clean with its tail section.
- All three repositories on the milestone branch.
</verification>

<success_criteria>
Every claim-bearing artifact and every plan SUMMARY is under the gate; all five OUT requirements are
complete on named measured evidence; all three repositories are on `beta`; and the milestone close is
handed a record it cannot misread into a re-merge or a lost tail.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-20-SUMMARY.md` when done.

Then, as the phase's final act, scan that SUMMARY via **positional argv** — it is deliberately not a
`_DEFAULT_TARGETS` member — and append the pasted result to the transcript's final-target-list section:
`python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py .planning/phases/152-outward-facing-close-operator-gated/152-20-SUMMARY.md`
must exit 0. Commit the transcript addition. Then re-run the defaults gate one last time and confirm it
still exits 0.

⚠ **This SUMMARY is a gate target via positional argv.** It MUST contain the literal sentence
`This ships software-proven and unvalidated on silicon.` and must avoid every forbidden label's
vocabulary — do not write that gh#21, gh#11 or gh#12 is closed, resolved or fixed; do not write that
something now works; if it refers to the deferred command, use the mandated withdrawal word order.

Record in it: the 27-entry target list, the untruncated PASS line, the suite count, the argv run, the
five evidence citations, the meta PR number/URL/method, the `git cherry` output, and the tail section's
contents.
</output>
