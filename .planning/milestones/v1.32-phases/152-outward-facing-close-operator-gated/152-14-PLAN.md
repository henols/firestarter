---
phase: 152-outward-facing-close-operator-gated
plan: 14
type: execute
wave: 8
depends_on: ["152-13"]
files_modified:
  - .planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md
  - .planning/todos/pending/write-sdp-relock-deferred.md
autonomous: false
requirements: [OUT-01]

must_haves:
  truths:
    - "The gh#12 reply is POSTED, and the stored comment body is byte-identical to the frozen draft."
    - "The comment count on gh#12 advanced by exactly 1."
    - "gh#12 is still OPEN after commenting, and no existing comment or the issue body was edited."
    - "The posted-mode gate run over the stored body exits 0."
    - "The folded todo is moved to completed with its resolution recorded; the second deferral todo stays pending with only its gh#12-follow-up half marked discharged."
  artifacts: []
  key_links:
    - "The blob SHA of the draft proves intent. The stored body, re-read and diffed, is the only proof that what we authored is what GitHub holds."
    - "Posted-mode temp files are written under the SAME basename as the draft, because the gate's caveat lookup is basename-keyed and fails closed to the full set for an unrecognised name."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Task 1 runs `152-check-not-auto.py` as a fail-closed guard, and that guard is a partial control, not a substitute for the operator."
    - "Do NOT close gh#12. Do NOT edit its body. Do NOT edit any existing comment on it."
    - "Do NOT edit the published b14 or b15 release bodies."
    - "Do NOT post more than one comment. This plan posts exactly one."
    - "Do NOT re-run the posting command after a failure without first re-reading the comment count — a partially-succeeded post would otherwise be duplicated."
    - "Do NOT use `updatedAt` as a landed or edited oracle. It bumps on creation and is null on every existing comment."
    - "Do NOT alter the draft in this plan. If the operator asks for wording changes, the change goes back through the gate and the diff artifact before this plan proceeds."
---

<objective>
Post the owed gh#12 reply, and prove the stored body is the frozen draft.

Purpose: OUT-01's verb is "is posted". This is the first of five irreversible public acts in the phase,
and D-03 requires its own blocking operator gate immediately before it, so that approval here cannot
carry to any other post.

Output: one public comment on gh#12; the folded todo moved to completed; the second deferral todo
annotated with a partial discharge.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** The checkpoint in Task 2 is the only human
review before a public, irreversible act. `autonomous: false` is measured not to be self-protecting;
Task 1's configuration guard fails closed on a set flag but its write path is unobserved in this
project, so it reduces rather than removes the dependence.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-post checks — the auto guard, the gate in file mode, and the live baseline</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-14-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.md` — the frozen draft that is about to be posted. Read it in full; this is the last read before it is public.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH12-COMMENT.diff` — the D-14 adaptation diff the operator reviews.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-05-SUMMARY.md` — the draft's blob SHA and the recorded gh#12 baseline.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1146-1171 (§D-5) — the sound-oracle table.
  </read_first>
  <action>
Four checks, in this order. Record every command and its output.

1. `python3 .planning/phases/152-*/152-check-not-auto.py` — must exit 0. If it exits non-zero, **stop**:
   the configuration says an auto or chained run is active and no public act may proceed.
2. `python3 .planning/phases/152-*/152-check-claims.py` with no argv and no seam — must exit 0 over all
   seven targets. Then the same gate through the env seam pointed at the gh#12 draft alone — must exit
   0. Record both PASS lines.
3. `python3 -m pytest .planning/phases/152-*/test_check_claims_152.py -q -o addopts=""` — must be fully
   green. Record the count line.
4. Capture the live baseline immediately before the checkpoint:
   `gh issue view 12 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length)}'`
   — record the state and the count. Also record the id and creation date of the current last comment,
   so the post's own comment can be identified unambiguously afterwards.
   Also record `git hash-object` of the draft, and state explicitly that this is an intent oracle only.

If any check fails, stop and record it. Nothing in this plan proceeds on a red.
  </action>
  <acceptance_criteria>
    - `152-check-not-auto.py` exits 0 and the live configuration value is recorded.
    - `152-check-claims.py` exits 0 on defaults, and exits 0 on the gh#12 draft via the env seam; both PASS lines recorded.
    - The paired suite is fully green with its count line recorded.
    - `gh issue view 12 --json state --jq '.state'` returns `OPEN`, and the baseline comment count is recorded as a number.
    - The current last comment's id and creation date are recorded.
    - The draft's blob SHA is recorded, labelled as an intent oracle.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; python3 152-check-claims.py; echo "GATE-DEFAULTS rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-GH12-COMMENT.md" python3 152-check-claims.py; echo "GATE-DRAFT rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; gh issue view 12 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1] | {id, createdAt})}'; git hash-object 152-GH12-COMMENT.md</automated>
  </verify>
  <done>The auto guard, the gate in both invocations, and the paired suite are all green, and the pre-post baseline is captured with the current last comment identified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review — authorise the gh#12 post</name>
  <what-built>
`152-GH12-COMMENT.md` — the owed gh#12 reply, adapted from the frozen v1.30 draft, with the adaptation
diff committed at `152-GH12-COMMENT.diff`. The claim gate is green on it and the paired suite is green.
Nothing has been posted. Approving this checkpoint posts one public comment on a thread opened in 2024
by a reporter who had to build separate hardware to work around the gap this reply describes.
  </what-built>
  <how-to-verify>
1. Read `152-GH12-COMMENT.diff` first — it shows exactly what changed against the once-reviewed v1.30
   draft, which is the point of the diff artifact.
2. Read `152-GH12-COMMENT.md` in full.
3. Check the load-bearing sentence: does it say plainly that the ask is **half-answered for a second
   release**? This is the single most consequential sentence in the phase.
4. Check the two negative cases: it must NOT name the deferred deliberate-protection command as a
   returning replacement, and it must NOT describe the earlier gap as if the original ask had been
   satisfied all along.
5. Check what was kept: the two-halves-don't-survive-equally framing and the "this isn't the
   enable/disable you asked for" paragraph, including the second limitation about the protection bit
   being unreadable.
6. Check tone: this is the third maintainer comment in a row on this thread and the second time it asks
   the reporter to install a pre-release and test. Does the reply acknowledge that in the reporter's
   own terms, without narrating our process?
7. Check the ask: does it name **both** install halves — the host pre-release and the firmware update?
8. Confirm the reply promises no version for the deferred work and names the backlog item instead.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: post to gh#12" or describe the wording changes required first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post, prove the stored body is ours, and resolve the folded todo</name>
  <files>.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md, .planning/todos/pending/write-sdp-relock-deferred.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-05-SUMMARY.md` — the recorded posting invocation.
    - `.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md` — the folded todo, including its four "what the reply must say" points and its four constraints, so the resolution note can state which were discharged.
    - `.planning/todos/pending/write-sdp-relock-deferred.md` — the second deferral todo. Only its gh#12-follow-up half is discharged here.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` lines 400-430 (§Folded Todos) — what each todo owes and what must not be presented as met.
  </read_first>
  <action>
Post exactly once, then prove it landed as authored.

**Post.** `gh issue comment 12 --repo henols/firestarter_prom --body-file <the frozen draft>`. Record
the command and its output. If it errors, do **not** re-run before re-reading the comment count — a
partially-succeeded post would otherwise be duplicated.

**Prove identity.** `gh issue view 12 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body'`
into a file, then `diff -u <the frozen draft> <that file>`. The diff must be **empty**. Paste the
command and its result. This is the only sound landed oracle; a blob SHA proves intent and
`updatedAt` proves nothing because it bumps on creation.

**Prove non-vacuity.** Re-read the comment count and assert it is exactly the baseline plus one. Record
both numbers.

**Capture the durable handle.** `gh issue view 12 --json comments --jq '.comments[-1] | {id, url, createdAt, author}'`
— record the comment id and URL. Plan 152-19 quotes them in the ledger.

**Prove the state assertions.** `gh issue view 12 --json state --jq '.state'` must return `OPEN`.
Confirm the issue body is unchanged and that no pre-existing comment was edited — compare the ids and
creation dates of the pre-existing comments against Task 1's capture.

**Posted-mode gate.** Write the stored body to a temp file **named `152-GH12-COMMENT.md`** — the same
basename as the draft, because the gate's caveat lookup is basename-keyed and fails closed to the full
caveat set for an unrecognised name — and run the gate through the env seam over that path. It must
exit 0. Paste the PASS line.

**Resolve the folded todo.** `git mv .planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md
.planning/todos/completed/` and append a dated resolution note to it recording: the comment id and URL,
which of its four "what the reply must say" points the posted text discharges, and that its constraint
about correcting the next release notes rather than rewriting the published ones is discharged by the
release bodies in this same phase.

**Annotate the second deferral todo, without resolving it.** Append a dated note to
`.planning/todos/pending/write-sdp-relock-deferred.md` recording that its gh#12-follow-up half is
discharged by this comment, that its "the shipping version's release notes announce it" half remains
**unmet** and must not be presented as met, that its `resolves_phase` stays `none`, and that its
standing instruction survives: a future promotion of the backlog item must reverse the fifth gate class
in the same change that lands the feature. **Leave the file in pending.**

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - `diff -u 152-GH12-COMMENT.md <(gh issue view 12 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body')` produces no output.
    - The comment count equals the Task 1 baseline plus exactly 1.
    - `gh issue view 12 --json state --jq '.state'` returns `OPEN`.
    - The comment id and URL are recorded in the SUMMARY.
    - The posted-mode gate run over a temp file named `152-GH12-COMMENT.md` exits 0.
    - `.planning/todos/completed/gh12-followup-after-dev-sdp-retirement.md` exists and `.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md` does not.
    - `.planning/todos/pending/write-sdp-relock-deferred.md` still exists, its `resolves_phase` is still `none`, and it carries a dated note recording the partial discharge and the un-met half.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; T=$(mktemp -d); gh issue view 12 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body' > "$T/152-GH12-COMMENT.md"; diff -u 152-GH12-COMMENT.md "$T/152-GH12-COMMENT.md"; echo "BODYDIFF rc=$?"; gh issue view 12 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1] | {id,url,createdAt})}'; FIRESTARTER_CLAIMSCAN_TARGETS_152="$T/152-GH12-COMMENT.md" python3 152-check-claims.py; echo "POSTEDGATE rc=$?"; ls /workspaces/.planning/todos/completed/gh12-followup-after-dev-sdp-retirement.md; ls /workspaces/.planning/todos/pending/write-sdp-relock-deferred.md; grep -c 'resolves_phase: none' /workspaces/.planning/todos/pending/write-sdp-relock-deferred.md; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>One comment is posted, its stored body diffs clean against the frozen draft, the count advanced by exactly one, gh#12 is still OPEN, the posted-mode gate is green, the folded todo is resolved and the second deferral todo carries an honest partial discharge.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** One public comment on gh#12.

**Modified files**
- `.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md` → moved to
  `.planning/todos/completed/` with a dated resolution note
- `.planning/todos/pending/write-sdp-relock-deferred.md` — a dated partial-discharge note appended; the
  file stays in pending and its `resolves_phase` stays `none`

**Recorded values handed forward to `152-LEDGER.md` (Plan 152-19)**
- The comment id, URL and creation timestamp
- The body-diff result and the comment-count delta
- The posted-mode gate PASS line

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen draft → public comment | Irreversible. Editing a posted comment is possible but the original is what a subscriber received by email. |
| blocking checkpoint → publication capability | The checkpoint is the access control on this act, and it is documented as insufficient under auto or chained modes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-60 | Elevation of privilege | an auto or chained run auto-approving this checkpoint | critical | mitigate | `152-check-not-auto.py` runs first and fails closed on a set flag or any unknown state. ⚠ Partial control — the prohibition is restated and the residual risk stays stated. |
| T-152-61 | Repudiation | assuming the post landed as authored | high | mitigate | The stored body is re-read and diffed against the frozen draft; the count delta is the non-vacuity guard; `updatedAt` is explicitly rejected as an oracle. |
| T-152-62 | Tampering | a duplicated comment after a partially-succeeded post | medium | mitigate | Explicit prohibition on re-running before re-reading the count. |
| T-152-63 | Repudiation | closing an issue this milestone did not resolve | high | mitigate | The state assertion requires `OPEN` after posting; closing is prohibited. |
| T-152-64 | Repudiation | presenting the deferral todo's un-met half as met | medium | mitigate | The annotation is explicitly a partial discharge; the file stays in pending and its `resolves_phase` is asserted unchanged. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Auto guard, defaults gate, draft gate, and paired suite all green before the checkpoint.
- Body diff empty; comment count delta exactly 1; issue still `OPEN`.
- Posted-mode gate green over a same-basename temp file.
- Folded todo in `completed/`; deferral todo still pending with `resolves_phase: none`.
</verification>

<success_criteria>
OUT-01 is discharged: the reply is posted, the stored text is provably the reviewed text, gh#12 stays
open, and the record says truthfully which obligations were met and which were not.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-14-SUMMARY.md` when done.

Record verbatim: all four pre-post check results, the baseline and post-post comment counts, the body
diff result, the comment id and URL, the posted-mode PASS line, and the exact resolution and
annotation text written into the two todos.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary — in particular, do not write that gh#12 is closed, resolved or fixed; it
received a comment and stays open.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-14-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
