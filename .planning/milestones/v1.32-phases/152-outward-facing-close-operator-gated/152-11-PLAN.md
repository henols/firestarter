---
phase: 152-outward-facing-close-operator-gated
plan: 11
type: execute
wave: 5
depends_on: ["152-10"]
files_modified: []
commits_land_in:
  - firestarter
  - firestarter_app
autonomous: false
requirements: [OUT-04]

must_haves:
  truths:
    - "Both sub-repos reach `beta` via a pull request, never a direct merge."
    - "The app PR's mergeability is measured before any resolution, and every commit is classified by `git cherry` before and after."
    - "A blocking operator checkpoint adjudicates the app conflict resolution before either PR is merged."
    - "After both merges, `git cherry origin/beta HEAD` is all `-` in both sub-repos."
    - "Both merge methods are recorded, because the method is what determines whether SHA ancestry works at all."
  artifacts: []
  key_links:
    - "Every merge to `beta` fires the pre-release workflow by design, so this plan causes two cuts. The versions are READ in Plan 152-12, never predicted here."
    - "The meta repo's PR is deliberately NOT created here — this phase keeps writing `.planning` artifacts after the cut, and Plan 152-20 owns the meta PR so the tail commits are included."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Merging to `beta` publishes a pre-release; the checkpoint in this plan is the only review before that happens."
    - "Do NOT use `git merge-base --is-ancestor` as a merge oracle. Use `git cherry`."
    - "Do NOT `git cherry-pick` or drop the commits `git cherry` marks as already upstream. Let the merge resolve them."
    - "Do NOT force-push to `beta`, and do NOT push directly to `beta` in either repo."
    - "Do NOT predict, guess or write down a cut version number in this plan. Plan 152-12 reads them."
    - "Do NOT run `gh workflow run` — it is blocked by the auto-mode classifier and settings edits cannot change that. Read-only `gh run` works."
    - "Do NOT create the meta repo's PR here."
    - "Do NOT re-pin gitlinks to `origin/beta` here — that is the milestone close's job."
---

<objective>
Merge both sub-repos to `beta` via pull requests, with the app merge's conflict resolution adjudicated
by a blocking operator checkpoint, and prove afterwards with `git cherry` that neither repo is
re-mergeable by mistake.

Purpose: criterion 2 asks the reporter to make a fresh run that is answerable *because* the report now
identifies its firmware — and the published branch still hardcodes the null value. Until these two
merges land, every outward sentence about what is available would be false in every published version.
This plan is what makes the announced version exist.

Output: no files. Two merged PRs, two CI cuts fired, and a recorded merge picture Plan 152-12 turns
into `152-MERGE-RECORD.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** `--auto`/`--chain` auto-approves
human-verify checkpoints, and `autonomous: false` is measured not to be self-protecting. The
checkpoint in Task 2 is the only human review between a conflict resolution and a published
pre-release.

Prior-milestone precedent for the PR numbers and shapes: the previous milestone used one PR per
sub-repo plus one for the meta repo; the milestone before that closed via a single squashed PR, and
that squash is what made SHA ancestry a false negative afterwards.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create both PRs, measure the app PR's mergeability, and resolve any conflict on the milestone branch</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-10-SUMMARY.md` — Plan 152-10's measured merge picture. This task re-runs the measurement rather than trusting it, but the recorded values are the comparison baseline.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 160-226 (§A-1, §A-2) — the named seven behind-commits on the app side, the five already-upstream-by-patch-id SHAs, and the finding that the firmware side is a clean fast-forwardable merge.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` §"Open Questions" entry 2 — the unresolved question of whether git resolves the patch-id duplicates cleanly, and the instruction to fund resolution rather than assume it.
    - `firestarter_app/.github/workflows/beta-release.yml` — the trigger, the release action, and the absence of a body field. Read it so the SUMMARY can state what the merge will cause.
    - `firestarter/.github/workflows/` — the firmware's pre-release workflow. Confirm its filename; it is not named the same as the app's.
  </read_first>
  <action>
Create one PR per sub-repo, from the milestone branch into `beta`. Firmware first, because it is the
clean one and it establishes the shape.

For each sub-repo:
- `gh pr create --repo henols/<repo> --base beta --head gsd/v1.32-at28c-write-path-root-cause-report-provenance`
  with a title naming the milestone and a body that states: what the branch contains, that merging
  fires the pre-release workflow by design, that no body is attached by that workflow so release notes
  are added manually afterwards, and the `git cherry` classification of the branch as measured now.
  Record the PR number and URL.
- `gh pr view <n> --repo henols/<repo> --json number,url,mergeable,mergeStateStatus,changedFiles,commits`
  — record `mergeable` and `mergeStateStatus` **before** touching anything.

For the app PR specifically, whose branch is behind `beta`:
- Re-run `git cherry origin/beta HEAD` and record the `+`/`-` classification with SHAs.
- If `mergeable` is not clean, resolve on the **milestone branch**: merge `origin/beta` into it, resolve
  each conflict by hand, and record for every conflicted file the conflicting hunks, the resolution
  taken and the reason. The commits already upstream by patch id are expected to be involved; **do not
  cherry-pick them and do not drop them** — let the merge resolve them.
- After resolving, push the milestone branch and re-read `mergeable`/`mergeStateStatus`.
- Re-run `git cherry origin/beta HEAD` and record the new classification, so the checkpoint can see
  what the resolution changed.
- Re-run the app test suite with the sibling firmware root severed, exactly as Plan 152-10 Task 3 did,
  because a conflict resolution can break a test that was green before it. Record the count line.

Record every command with its output. If `mergeStateStatus` reports a state that is neither clean nor
conflicted — a blocked or behind state, for instance — record it verbatim and state what it means
before proceeding.
  </action>
  <acceptance_criteria>
    - Two PR numbers and URLs recorded, both with base `beta` and head the milestone branch.
    - `gh pr view <n> --json mergeable,mergeStateStatus` recorded for both PRs, both before and (for the app) after any resolution.
    - For every conflicted file: the conflicting hunks, the resolution and its reason are recorded.
    - `git cherry origin/beta HEAD` recorded for the app both before and after resolution, with SHA lists.
    - The app test suite is green after resolution with the sibling root severed, and its count line is recorded.
    - `git -C /workspaces/firestarter_app status --porcelain | grep -v '^??'` is empty after the resolution is committed and pushed.
    - No `merge-base --is-ancestor` invocation appears anywhere in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>for r in firestarter firestarter_app; do echo "== $r"; gh pr list --repo henols/$r --base beta --state open --json number,url,headRefName,mergeable,mergeStateStatus; done; for r in firestarter firestarter_app; do echo "== cherry $r"; git -C /workspaces/$r fetch origin --quiet; git -C /workspaces/$r cherry origin/beta HEAD | awk '{print $1}' | sort | uniq -c; git -C /workspaces/$r status --porcelain | grep -v '^??'; echo "CLEAN-END"; done</automated>
  </verify>
  <done>Both PRs exist against `beta` with their mergeability measured, any app conflict is resolved by hand with every hunk and reason recorded, and the post-resolution test suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator review — adjudicate the app conflict resolution, then authorise both merges</name>
  <what-built>
Two pull requests into `beta`, one per sub-repo, with the app PR's conflict resolution (if any) already
made and pushed. Nothing is merged yet. Merging fires the pre-release workflow in each repository, so
approving this checkpoint publishes two pre-releases.
  </what-built>
  <how-to-verify>
1. Open the firmware PR at the URL recorded in Task 1. Confirm the base is `beta` and the head is the
   milestone branch. Review the file list.
2. Open the app PR. Confirm base and head. Review the file list, and **review the conflict resolution
   specifically** — Task 1's record names every conflicted file, the conflicting hunks, the resolution
   taken and the reason. This is the semantic call the plan cannot make on its own.
3. Confirm the `git cherry` classification recorded after the resolution: the commits marked already
   upstream should be the ones the record names, and nothing should have been cherry-picked or dropped.
4. Confirm the app test suite's post-resolution count line in the record, and that it was run with the
   sibling firmware root severed.
5. Decide the **merge method** for each PR and say which. A squash makes SHA ancestry a false negative
   afterwards — that is survivable because this phase verifies with `git cherry`, but the method must
   be recorded either way.
6. Confirm you are content that merging now publishes two pre-releases whose release notes are drafted
   but not yet posted.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: merge both, method squash, merge or rebase, per PR" or describe what must change first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge both PRs and prove neither sub-repo is re-mergeable by mistake</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md</files>
  <read_first>
    - The checkpoint's approval, including the merge method chosen per PR.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1250-1281 (§E-5) — the six items the merge record must carry and the one-line no-re-merge instruction, so this task captures what Plan 152-12 needs.
  </read_first>
  <action>
Merge both PRs using the method the operator named, firmware first.

- `gh pr merge <n> --repo henols/<repo> --<method>` for each. Record the merge commit SHA (or the
  squash commit SHA) and the method actually used, as reported by
  `gh pr view <n> --json state,mergedAt,mergeCommit,url`.
- After each merge: `git fetch origin` in that sub-repo, then `git cherry origin/beta HEAD` and record
  the output. **The expectation is that every line is `-`.** If any `+` remains, stop and record it —
  something did not land, and no outward artifact may be posted until it is understood.
- Record the post-merge `origin/beta` SHA for each sub-repo (`git rev-parse origin/beta`) and the
  gitlink each should eventually be pinned to. Do **not** re-pin here.
- Record that the merges fired the pre-release workflows, and capture the run ids with read-only
  `gh run list --repo henols/<repo> --limit 5`. Do **not** invoke `gh workflow run`.
- Do not read or record a cut version number in this plan. Plan 152-12 reads both tags from the release
  list after CI finishes, and versions in this project are read, never predicted.
- Confirm `git rev-parse --abbrev-ref HEAD` in all three repos is still the milestone branch.

⚠ Record the one-line handoff sentence verbatim, for Plan 152-12 to carry into the merge record: the
beta merges for this milestone are complete, do not re-merge, and verify with `git cherry`, never with
ancestry.
  </action>
  <acceptance_criteria>
    - `gh pr view <n> --repo henols/firestarter --json state --jq '.state'` returns `MERGED`, and the same for the app PR.
    - `git -C /workspaces/firestarter cherry origin/beta HEAD` produces only `-` lines (or no output), and the same for `firestarter_app`.
    - The merge method actually used is recorded per PR, from `gh pr view`, not from intent.
    - The post-merge `origin/beta` SHA is recorded for each sub-repo.
    - Workflow run ids are captured for both repos via read-only `gh run list`.
    - No version number appears anywhere in the SUMMARY.
    - `git rev-parse --abbrev-ref HEAD` is the milestone branch in all three repos.
  </acceptance_criteria>
  <verify>
    <automated>for r in firestarter firestarter_app; do echo "== $r"; gh pr list --repo henols/$r --base beta --state merged --limit 3 --json number,state,mergedAt,mergeCommit,url; git -C /workspaces/$r fetch origin --quiet; echo "cherry:"; git -C /workspaces/$r cherry origin/beta HEAD; echo "CHERRY-END"; git -C /workspaces/$r rev-parse origin/beta; gh run list --repo henols/$r --limit 3 --json databaseId,name,status,conclusion; git -C /workspaces/$r rev-parse --abbrev-ref HEAD; done; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>Both PRs are MERGED, `git cherry` is all `-` in both sub-repos, both merge methods and post-merge SHAs are recorded, both workflow runs are captured read-only, and no version was predicted.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** Two merged pull requests, two fired pre-release workflows.

**Recorded values handed forward to `152-MERGE-RECORD.md` (Plan 152-12)**
- Two PR numbers, URLs and the **merge method actually used** for each
- For each sub-repo, the post-merge `git cherry origin/beta HEAD` output (expected all `-`)
- The post-merge `origin/beta` SHA per sub-repo, and the gitlink each should be pinned to
- The two workflow run ids, captured read-only
- Every conflicted file with its hunks, resolution and reason
- The verbatim no-re-merge handoff sentence

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| milestone branch → `beta` | Crossing it publishes a pre-release in each sub-repo. Irreversible in the sense that the tag and the artifacts are public. |
| checkpoint mechanism → publication capability | The blocking checkpoint is the access control on two public cuts, and it is documented as insufficient under `--auto`/`--chain`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-45 | Elevation of privilege | `--auto`/`--chain` auto-approving the merge checkpoint | critical | mitigate | The prohibition is restated in this plan's frontmatter, its context block and its checkpoint. ⚠ **Stated residual risk, not a control** — Plan 152-13 adds the fail-closed configuration guard the posting plans run before each public act. |
| T-152-46 | Tampering | re-merging or dropping commits on a false ancestry signal | high | mitigate | `git cherry` before and after, all-`-` asserted post-merge; the ancestry invocation is prohibited and its absence is an acceptance criterion. |
| T-152-47 | Tampering | a conflict resolved silently, changing behaviour nobody reviewed | high | mitigate | Every conflicted file's hunks, resolution and reason recorded; the resolution is the checkpoint's named review subject; the app suite re-run after resolution. |
| T-152-48 | Spoofing | recording an intended merge method rather than the one used | medium | mitigate | The method is read back from `gh pr view` after the merge, not from the approval text. |
| T-152-49 | Spoofing | predicting a cut version | high | mitigate | No version may appear in this plan's SUMMARY; Plan 152-12 reads both tags after CI. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** If the post-resolution test run needs an environment it does not have, stop and record it rather than installing anything. |
</threat_model>

<verification>
- Both PRs `MERGED` against base `beta`, with the method read back from the API.
- `git cherry origin/beta HEAD` all `-` in both sub-repos.
- Post-merge `origin/beta` SHAs recorded; workflow run ids captured read-only.
- No version number in the SUMMARY.
- All three repos still on the milestone branch.
</verification>

<success_criteria>
The version the release notes will announce now exists, both sub-repos are provably merged by the only
oracle that survives a squash, and no re-merge can be triggered by a false ancestry signal.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md` when done.

Record verbatim: both PR numbers and URLs, both `mergeable`/`mergeStateStatus` readings, every
conflicted file with hunks/resolution/reason, the `git cherry` output before and after in both repos,
both merge methods as read back, both post-merge `origin/beta` SHAs, both workflow run ids, the
post-resolution test count line, and the no-re-merge handoff sentence.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; do not write that something now works, and do not assert that gh#21,
gh#11 or gh#12 is closed, resolved or fixed.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
