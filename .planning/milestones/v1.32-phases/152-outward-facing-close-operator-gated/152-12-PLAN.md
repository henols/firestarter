---
phase: 152-outward-facing-close-operator-gated
plan: 12
type: execute
wave: 6
depends_on: ["152-11"]
files_modified:
  - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md
  - .planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md
autonomous: true
requirements: [OUT-04]

must_haves:
  truths:
    - "Both cut tags are READ from the release list after CI, with the command and timestamp recorded, and never predicted."
    - "The package registry is verified independently of the release list, by a direct registry read."
    - "Both release-note drafts carry the read tags in place of every placeholder token, and both still pass the claim gate afterwards."
    - "`152-MERGE-RECORD.md` carries all six handoff items and the verbatim no-re-merge instruction."
    - "The published branch of the host repo now contains the provenance fix and the new protection-read command, verified by reading the branch rather than by inference."
  artifacts:
    - .planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md
  key_links:
    - "Criterion 2's phrase 'answerable because the report now identifies its firmware' becomes true at this point and not before — Task 1 verifies it against `origin/beta` directly."
    - "`152-MERGE-RECORD.md` is what stops the milestone close re-merging on a false ancestry signal."
  prohibitions:
    - "Do NOT predict, infer or compute a version number. Read both tags from the release list."
    - "Do NOT infer registry state from the release list. The two have been measured divergent on the stable channel; read the registry directly."
    - "Do NOT run `gh workflow run` — it is blocked by the auto-mode classifier. Read-only `gh run` works."
    - "Do NOT claim the current stable version is installable from the registry unless the direct registry read shows it."
    - "Do NOT edit any published release body in this plan. Plan 152-17 and Plan 152-18 post the bodies, each behind its own blocking operator checkpoint."
    - "Do NOT re-pin gitlinks — record the intended pins and leave the re-pinning to the milestone close."
    - "Do NOT add `152-MERGE-RECORD.md` to `_DEFAULT_TARGETS` here; Plan 152-13 owns that edit, and the file must exist first or the gate returns non-zero."
---

<objective>
Read what CI cut, verify the package registry independently, substitute the read versions into both
release bodies, and write the handoff record that stops the milestone close re-merging.

Purpose: every prior release-notes artifact in this project opens by naming where its version was read
from and when, because a predicted version is a claim about a thing that may not exist. This plan is
where the placeholders become measurements.

Per **D-01**'s surviving half the version-agnostic authoring is completed here by a read, not a
prediction; per **D-04** this phase owns the merges and therefore owns reading what they cut.

Output: `152-MERGE-RECORD.md`, and both release-note drafts with their placeholders substituted.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Both merges landed in Plan 152-11 and each fired a pre-release workflow. This plan reads the results.

⚠ Do not use a foreground sleep loop to wait for CI. Poll with repeated read-only `gh run list` /
`gh release list` calls and record each poll's timestamp. If a run is still in progress after a
reasonable number of polls, record that and stop — the release bodies cannot be posted against a tag
that does not exist yet.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read both cut tags, verify the registry independently, and confirm the published branch contains the announced work</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md` — the two workflow run ids, the merge methods and the post-merge branch SHAs.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1204-1249 (§E-3, §E-4) — the measured finding that the registry upload is now automatic via a workflow call, the reason it was made automatic, and the CI gotchas at a cut.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 248-306 (§A-4, §A-5) — the release-body baseline and the measured stable-channel divergence between the release list and the registry.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1808-1830 (§Code Examples, the version-read and registry-verification snippets) — the exact command shapes.
  </read_first>
  <action>
Poll, then read. Record every command with its output and a wall-clock timestamp.

**Poll the runs.** `gh run list --repo henols/<repo> --limit 5 --json databaseId,name,status,conclusion,headSha`
for both repos, repeatedly, until each pre-release workflow run reports a terminal status. Record each
poll. If a run concludes as anything other than success, record the conclusion and the failing job
names via read-only `gh run view <id> --repo henols/<repo> --json jobs` and **stop** — do not proceed to
substitute a tag from a failed cut.

**Read the tags.** `gh release list --repo henols/firestarter_app --limit 10` and the same for
`henols/firestarter`. Identify the newest pre-release in each. Record its tag, its creation timestamp,
its target commit and the command that read it. These two values are the only source for the
substitutions in Task 2.

**Read the current body length of each new release** — `gh release view <tag> --repo <r> --json body -q '.body | length'`.
This is the 0-to-N non-vacuity baseline that Plan 152-17 and Plan 152-18 assert against; a measured
starting value is required, not an assumed zero.

**Verify the registry independently.** `curl -s https://pypi.org/pypi/firestarter/json` and, from the
parsed JSON, record: the stable pointer version, whether the newly cut pre-release tag is present in
the release map, and its upload timestamp if so. Compare the upload timestamp against the release
creation timestamp and record the delta. Then separately record whether the newest **stable** tag in
the release list is present in the registry — RESEARCH measured that it was not, and OUT-04 must not
imply it is installable.

**Confirm the branch contains the announced work.** Read it, do not infer it:
- `git -C /workspaces/firestarter_app fetch origin` then
  `git show origin/beta:firestarter/cli_handlers.py | grep -n fw_board_identity` — the provenance fix
  must now be present in a form that is not a hardcoded null. Record the matched lines.
- `git show origin/beta:firestarter/main.py | grep -c 'lock.status'` — the new protection-read command
  must now be reachable on the published branch. Record the count.
- `git show origin/beta:firestarter/database.py | grep -c vcc_mv` — the numeric-values work must be
  present. Record the count.
- `git -C /workspaces/firestarter show origin/beta:src/proms/eeprom_28c.cpp | grep -c 'eeprom28c_erase_execute'`
  — the standalone erase arm must now be on the published firmware branch. Record the count.

These four reads are what make criterion 2's "answerable because" true. If any of them still shows the
pre-merge shape, stop and record it.
  </action>
  <acceptance_criteria>
    - Both pre-release workflow runs recorded with a terminal conclusion of success.
    - Both cut tags recorded with their creation timestamps, target commits, and the reading command.
    - The current body length of each new release recorded as a number.
    - The registry's stable pointer recorded, the new pre-release tag's presence in the registry recorded as a boolean with its upload timestamp, and the newest stable tag's registry presence recorded separately.
    - `git show origin/beta:firestarter/cli_handlers.py | grep -c fw_board_identity` ≥ 1 and the matched lines show the value is no longer a hardcoded null.
    - `git show origin/beta:firestarter/main.py | grep -c 'lock.status'` ≥ 1.
    - `git -C /workspaces/firestarter show origin/beta:src/proms/eeprom_28c.cpp | grep -c 'eeprom28c_erase_execute'` ≥ 1.
    - No version number in the SUMMARY is unaccompanied by the command and timestamp that read it.
  </acceptance_criteria>
  <verify>
    <automated>for r in firestarter firestarter_app; do echo "== $r"; gh run list --repo henols/$r --limit 5 --json databaseId,name,status,conclusion; gh release list --repo henols/$r --limit 5; done; git -C /workspaces/firestarter_app fetch origin --quiet; git -C /workspaces/firestarter_app show origin/beta:firestarter/cli_handlers.py | grep -n fw_board_identity; git -C /workspaces/firestarter_app show origin/beta:firestarter/main.py | grep -c 'lock.status'; git -C /workspaces/firestarter fetch origin --quiet; git -C /workspaces/firestarter show origin/beta:src/proms/eeprom_28c.cpp | grep -c 'eeprom28c_erase_execute'; curl -s https://pypi.org/pypi/firestarter/json | python3 -c "import sys,json; d=json.load(sys.stdin); print('stable', d['info']['version']); print('n_releases', len(d['releases']))"</automated>
  </verify>
  <done>Both cuts are confirmed successful, both tags are read with provenance, the registry is verified by direct read, and the published branch is confirmed to contain every piece of work the notes announce.</done>
</task>

<task type="auto">
  <name>Task 2: Substitute the read tags into both release bodies and re-prove the gate</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md, .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md</files>
  <read_first>
    - Task 1's recorded tags, timestamps, target commits and registry findings.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-08-SUMMARY.md` and `152-09-SUMMARY.md` — the placeholder contracts: every token, its line number and its read command.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md` and `152-RELEASE-NOTES-fw.md` — the current drafts.
  </read_first>
  <action>
Substitute every placeholder token in both bodies with the value read in Task 1, and complete the
opening version-read paragraph in each so it states, concretely: the tag, the command it was read
from, the timestamp of the read, the workflow that cut it, the target commit, the registry
confirmation **read from the registry rather than from the release list**, the stable-channel state as
measured, and the matching release in the sibling repository with the note that the two repositories
version independently.

Rules for the substitution:
- The two bodies use byte-identical placeholder tokens, so one pass covers both. Verify afterwards that
  zero placeholder tokens remain in either file.
- Where the app body says something about the registry, it must say what the **direct registry read**
  showed. If the newest stable tag is absent from the registry, the body must not imply it is
  installable; say what is installable, from the pre-release channel, and leave the stable channel
  factually described.
- Do not change any other sentence. In particular do not touch the withdrawal sentence's word order,
  the non-claim sentences, or the size figures. Prove that with a bounded diff.

Then re-prove both files against the gate through the env seam, and re-assert the two presence greps
that the gate alone cannot express: that the deferred command's name is still present in each body, and
that each body still carries its three required non-claim sentences. The gate proves the name is in the
mandated withdrawal order; the grep proves it was not silently dropped. Both halves are needed, because
criterion 4 forbids leaving it unmentioned independently of criterion 5 forbidding it as shipped.
  </action>
  <acceptance_criteria>
    - `grep -c 'APP_TAG_TBD\|FW_TAG_TBD' 152-RELEASE-NOTES-app.md` == 0 and the same for the firmware body.
    - Each body contains the tag read in Task 1, at least once, and the count is recorded.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-RELEASE-NOTES-app.md python3 152-check-claims.py` exits 0, and the same for the firmware body.
    - `grep -c 'sdp-relock' 152-RELEASE-NOTES-app.md` ≥ 1 and `grep -c 'sdp-relock' 152-RELEASE-NOTES-fw.md` ≥ 1 — the withdrawal is still stated, not silently dropped.
    - `grep -c 'No AT28C part was tested at any point in v1.32'` == 1 in each body, and `grep -c 'software-proven and unvalidated on silicon'` ≥ 1 in each.
    - `git diff --numstat` on each body shows added+deleted ≤ 30 — a substitution pass, not a rewrite.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; for f in 152-RELEASE-NOTES-app.md 152-RELEASE-NOTES-fw.md; do echo "== $f"; grep -c 'APP_TAG_TBD\|FW_TAG_TBD' "$f"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/$f" python3 152-check-claims.py > /dev/null; echo "GATE rc=$?"; grep -c 'sdp-relock' "$f"; grep -c 'No AT28C part was tested at any point in v1.32' "$f"; grep -c 'software-proven and unvalidated on silicon' "$f"; done; git -C /workspaces diff --numstat -- .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-app.md .planning/phases/152-outward-facing-close-operator-gated/152-RELEASE-NOTES-fw.md</automated>
  </verify>
  <done>Both bodies name their read tags with full provenance, no placeholder remains, both still pass the gate, both still state the withdrawal and all three non-claims, and each diff is a substitution rather than a rewrite.</done>
</task>

<task type="auto">
  <name>Task 3: Write 152-MERGE-RECORD.md — the six handoff items and the no-re-merge instruction</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1250-1281 (§E-5) — the six numbered items, the one-line instruction, and the three close-time warnings about the milestone-close tooling.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-11-SUMMARY.md` and `152-10-SUMMARY.md` — the measured values this record quotes.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md` lines 1-45 — the live-capture attribution style: command, timestamp, and the explicit statement that a value was measured here and never reused from a prior document's citation.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-PATTERNS.md` lines 698-717 (§9) — the no-analog note and the load-bearing line to reproduce literally.
  </read_first>
  <action>
Write `152-MERGE-RECORD.md`. There is no donor for this document class; its content is fully specified.
Use the live-capture attribution style for every value: the command, the timestamp, and the statement
that it was measured here rather than reused from a prior document's citation.

Sections:

1. **The three pull requests.** The two sub-repo PR numbers, URLs and the **merge method actually
   used** for each, read back from the API. Then a clearly-labelled row for the meta repository stating
   that its PR is deliberately **not** created yet, that Plan 152-20 owns it, and why: this phase keeps
   writing planning artifacts after the cut, so merging the meta repo earlier would leave the tail
   commits off `beta`.
2. **The `git cherry` capture, per sub-repo, taken AFTER the merge** — the full output, expected all
   `-`. State in the same section that this is the oracle that survives a squash and that ancestry is
   not, and name the prior milestone whose squashed merge produced the false negative.
3. **The two observed cut tags**, with the command that read them, the timestamp, and their target
   commits. Never a predicted value.
4. **The registry confirmation**, read directly from the registry rather than from the release list,
   including the stable-channel state as measured and the pre-release upload delta.
5. **The post-merge published-branch SHA per sub-repo**, and the gitlink each should be pinned to —
   stated as an instruction for the close, not performed here.
6. **The instruction**, reproduced literally: the beta merges for this milestone are COMPLETE; do not
   re-merge; verify with `git cherry`, never with ancestry.

Then a closing **`## Notes for the milestone close`** section carrying the three measured warnings:
that the close's own new-milestone step which clears phases is destructive and must be skipped; that
milestone close has previously broken its own record gates by archiving sections and removing a
requirements file that other gates' target lists still name; and that the research directory is not
archived at close and should be moved before the next milestone's researchers run.

⚠ This file becomes a `_DEFAULT_TARGETS` member in Plan 152-13 with an **empty** required-caveat set —
it is a captured handoff record, not a claim register. It must therefore still be free of every
forbidden phrase. Verify it through the env seam before committing, and confirm that
`python3 152-check-claims.py` on defaults is still green (this file is not yet a member).
  </action>
  <acceptance_criteria>
    - `152-MERGE-RECORD.md` exists with all six numbered sections plus the close-notes section.
    - `grep -c 'do not re-merge' 152-MERGE-RECORD.md` ≥ 1 and `grep -c 'git cherry' 152-MERGE-RECORD.md` ≥ 2.
    - `grep -ci 'is-ancestor' 152-MERGE-RECORD.md` ≥ 1 — named as the oracle NOT to use.
    - Both cut tags appear with a reading command and a timestamp beside each.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_152=<abs>/152-MERGE-RECORD.md python3 152-check-claims.py` exits 0.
    - `python3 152-check-claims.py` (defaults) still exits 0.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch after committing.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; test -s 152-MERGE-RECORD.md; echo "exists rc=$?"; grep -c 'do not re-merge' 152-MERGE-RECORD.md; grep -c 'git cherry' 152-MERGE-RECORD.md; grep -ci 'is-ancestor' 152-MERGE-RECORD.md; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-MERGE-RECORD.md" python3 152-check-claims.py; echo "SEAM rc=$?"; python3 152-check-claims.py; echo "DEFAULTS rc=$?"; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>The handoff record exists with all six items live-captured, the no-re-merge instruction reproduced literally, the close-time warnings recorded, and the file gate-clean.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**New files**
- `.planning/phases/152-outward-facing-close-operator-gated/152-MERGE-RECORD.md` — the no-re-merge
  handoff record; becomes a `_DEFAULT_TARGETS` member in Plan 152-13 with the **empty** caveat set
  (`_CAVEAT_RULES` entry pre-populated by Plan 152-01)

**Modified files**
- `152-RELEASE-NOTES-app.md`, `152-RELEASE-NOTES-fw.md` — placeholder substitution only

**Placeholder tokens retired**
- `APP_TAG_TBD`, `FW_TAG_TBD` — replaced by read values

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release list → registry | The two have been measured divergent on the stable channel; inferring one from the other publishes a false install claim. |
| document-recorded version → published body | A version that was predicted rather than read is a claim about a thing that may not exist. |
| this phase → the milestone close | The close has previously re-merged on a false ancestry signal; this record is the control against a repeat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-50 | Spoofing | announcing a version that CI did not cut, or one whose cut failed | high | mitigate | Task 1 polls to a terminal conclusion and stops on any non-success; every version carries its reading command and timestamp. |
| T-152-51 | Spoofing | implying a stable version is installable from the registry when it is absent | high | mitigate | The registry is read directly and the stable-channel state is recorded and written into the body as measured. |
| T-152-52 | Repudiation | announcing work the published branch does not contain | critical | mitigate | Four direct reads of `origin/beta` confirm the provenance fix, the protection-read command, the numeric values and the firmware erase arm before any substitution. |
| T-152-53 | Tampering | the substitution pass silently rewriting a constrained sentence | high | mitigate | Both bodies re-proven against the gate, both presence greps re-asserted, and each diff bounded to a substitution-sized change. |
| T-152-54 | Tampering | the milestone close re-merging on a false ancestry signal | high | mitigate | `152-MERGE-RECORD.md` carries the post-merge `git cherry` capture, both merge methods, and the literal no-re-merge instruction. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** `gh`, `git`, `curl` and stdlib Python only. |
</threat_model>

<verification>
- Both runs terminal-success; both tags read with provenance; both current body lengths measured.
- Registry read directly; stable-channel state and pre-release upload delta recorded.
- Four `origin/beta` content reads confirm the announced work is published.
- Zero placeholders remain; both bodies gate-green; both presence greps hold; diffs bounded.
- `152-MERGE-RECORD.md` present, gate-clean, with the literal instruction; defaults run still green.
</verification>

<success_criteria>
The announced versions exist and are named from a read; the registry is confirmed independently; both
bodies are complete and still gate-clean; and the milestone close is handed an unambiguous record that
the merges are done and how to verify that without the oracle that lies.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-12-SUMMARY.md` when done.

Record verbatim: every poll with its timestamp, both run conclusions, both tags with their reading
commands and timestamps and target commits, both current body lengths, the registry readings, the four
`origin/beta` content reads, the substitution diffs, and the merge record's own commit SHA.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary; if it refers to the deferred command, use the mandated withdrawal word
order.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-12-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
