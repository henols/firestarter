---
phase: 152-outward-facing-close-operator-gated
plan: 16
type: execute
wave: 10
depends_on: ["152-15"]
files_modified: []
autonomous: false
requirements: [OUT-03]

must_haves:
  truths:
    - "The gh#11 comment is POSTED and the stored body is byte-identical to the frozen draft."
    - "The comment count on gh#11 advanced by exactly 1."
    - "gh#11 is still OPEN after commenting; neither the issue body nor any existing comment was edited."
    - "The posted-mode gate run over the stored body exits 0."
    - "The commitment the reply discharges is quoted from the thread's own record, verified live immediately before posting."
  artifacts: []
  key_links:
    - "The obligation this reply discharges is a kept-late promise, not a broken silence — the 2026-08-03 question was answered the same day with a commitment, and that commitment is what the reply pays."
    - "Posted-mode temp files are written under the SAME basename as the draft, because the caveat lookup is basename-keyed and fails closed."
  prohibitions:
    - "⚠ **This phase must NOT be run under `--auto`/`--chain`.** Every OUT requirement is operator-reviewed before posting, and `--auto`/`--chain` auto-approves human-verify checkpoints — `autonomous: false` alone is not self-protecting. Task 1's configuration guard is a partial control, not a substitute for the operator."
    - "Do NOT close gh#11. Do NOT edit its body. Do NOT edit any existing comment on it."
    - "Do NOT post more than one comment. This plan posts exactly one."
    - "Do NOT re-run the posting command after a failure without first re-reading the comment count."
    - "Do NOT use `updatedAt` as a landed or edited oracle."
    - "Do NOT alter the draft in this plan. Wording changes go back through the gate before this plan proceeds."
---

<objective>
Post the gh#11 reply, and prove the stored body is the frozen draft.

Purpose: OUT-03 requires the 2024 report answered in terms of the conflation that is its actual shape,
rather than left silently superseded. The reply also discharges a commitment made on the thread on
2026-08-03. It is posted last of the three comments, so the two participants read it after the thread
it folds into has already been updated.

Output: one public comment on gh#11.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
⚠ **This phase must NOT be run under `--auto`/`--chain`.** Approval given for the two earlier posts
does not carry here — D-03 requires a separate blocking gate per artifact precisely so it cannot.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-post checks and a live re-read of the commitment the reply quotes</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-16-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-GH11-COMMENT.md` — the frozen draft. Read it in full; this is the last read before it is public.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-07-SUMMARY.md` — the draft's blob SHA, the recorded gh#11 baseline, the five load-bearing comments, and both `gh` invocations.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1120-1171 (§D-4, §D-5) — the measured comment timeline and the sound-oracle table.
  </read_first>
  <action>
Five checks, in this order. Record every command and its output.

1. `python3 .planning/phases/152-*/152-check-not-auto.py` — must exit 0, else **stop**.
2. `python3 .planning/phases/152-*/152-check-claims.py` on defaults — rc 0; then through the env seam on
   the gh#11 draft alone — rc 0. Record both PASS lines.
3. `python3 -m pytest .planning/phases/152-*/test_check_claims_152.py -q -o addopts=""` — fully green,
   count line recorded.
4. **Re-read the commitment.** Fetch gh#11's comments live and locate the 2026-08-03 maintainer reply
   whose text the draft quotes. Confirm the quoted words match the stored comment **character for
   character** — a misquotation of someone's own words in a reply to them is the worst small error this
   plan can make. Record the comment id, its timestamp and the matched text. If the quotation does not
   match, **stop** and fix the draft through Plan 152-07's gate path before proceeding.
   In the same read, confirm the community comment reporting the hand-hacked erase command is still
   present and record its id, since the reply refers to it.
5. Capture the live baseline: gh#11's state, its comment count, and the id and creation date of the
   current last comment. Record the draft's blob SHA, labelled as an intent oracle only.

Nothing proceeds on a red.
  </action>
  <acceptance_criteria>
    - `152-check-not-auto.py` exits 0 with the live configuration value recorded.
    - The gate exits 0 on defaults and on the gh#11 draft; both PASS lines recorded.
    - The paired suite is fully green with its count line recorded.
    - The quoted commitment text is confirmed character-identical against the live stored comment, and that comment's id and timestamp are recorded.
    - The community comment reporting the hand-hacked erase command is confirmed present, with its id recorded.
    - `gh issue view 11 --json state --jq '.state'` returns `OPEN`; the baseline count is recorded; the last comment's id and date are recorded.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; python3 152-check-not-auto.py; echo "NOTAUTO rc=$?"; python3 152-check-claims.py; echo "GATE-DEFAULTS rc=$?"; FIRESTARTER_CLAIMSCAN_TARGETS_152="$(pwd)/152-GH11-COMMENT.md" python3 152-check-claims.py; echo "GATE-DRAFT rc=$?"; python3 -m pytest test_check_claims_152.py -q -o addopts=""; echo "SUITE rc=$?"; gh issue view 11 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1]|{id,createdAt})}'; gh issue view 11 --repo henols/firestarter_prom --json comments --jq '.comments[] | select(.createdAt | startswith("2026-08-03")) | {id, createdAt, author:.author.login, body}'</automated>
  </verify>
  <done>All gates are green, the quoted commitment is verified character-identical against the live thread, and the pre-post baseline is captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING operator wording review — authorise the gh#11 post</name>
  <what-built>
`152-GH11-COMMENT.md` — the gh#11 reply: the 2024 report answered in conflation terms, the 2026-08-03
commitment discharged, and the page-write analysis acknowledged. The claim gate is green on it, the
paired suite is green, and the quoted commitment has been verified character-identical against the live
thread. Nothing has been posted. Approving this checkpoint posts one public comment on a thread with
two active community participants.
  </what-built>
  <how-to-verify>
1. Read `152-GH11-COMMENT.md` in full.
2. Check the framing: this must read as a **kept-late promise being paid**, not as an apology for
   silence. The question was answered the same day. An apology here would misstate the thread's own
   record to the people who are in it.
3. Check the conflation answer: does it build on the earlier answer rather than repeat it, and does it
   name the three places the host and the firmware disagreed about this family?
4. Check the datasheet citation: document number, table and page, so a reader can check it.
5. Check the honest limits on the shipped erase: the timing constant is a family maximum over a
   multi-vendor bucket, a longer-cycle part would read non-blank after an apparently successful erase,
   and no test can prove the wall-clock wait is honoured.
6. Check the page-write answer: it should say what the page-size work did and did not change, and it
   must not offer that work as an explanation for the failures in this thread.
7. Check the ask names **both** install halves, and that nothing directs the reporter anywhere other
   than the issue.
8. Confirm nothing claims a silicon result, and that the reply names the community member as the only
   person who has ever run any of this on real silicon of this family.
  </how-to-verify>
  <action>
Present the material named in the verification steps below and then **STOP**. Do not run any
command that writes to a remote, and do not proceed to the next task, until the operator answers with
the resume signal. This checkpoint is blocking by design and its approval covers this artifact only —
it does not carry to any other public act in this phase.

If the operator asks for changes, do not edit the artifact inside this checkpoint. Record what was
asked, return to the plan that owns the artifact, take the change through that plan's gate path, and
re-enter this checkpoint from the top.
  </action>
  <resume-signal>Type "approved: post to gh#11" or describe the wording changes required first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post, prove the stored body is ours, and record the durable handle</name>
  <files>.planning/phases/152-outward-facing-close-operator-gated/152-16-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/152-outward-facing-close-operator-gated/152-07-SUMMARY.md` — the recorded posting and verification invocations.
    - `.planning/phases/152-outward-facing-close-operator-gated/152-RESEARCH.md` lines 1146-1171 (§D-5) — the sound-oracle table.
  </read_first>
  <action>
Post exactly once, then prove it landed as authored.

**Post.** `gh issue comment 11 --repo henols/firestarter_prom --body-file <the frozen draft>`. Record
the command and its output. On error, do **not** re-run before re-reading the comment count.

**Prove identity.** Write the stored last-comment body to a temp file **named `152-GH11-COMMENT.md`** —
the same basename as the draft, because the caveat lookup is basename-keyed and fails closed — then
`diff -u <the frozen draft> <that file>`. The diff must be empty. Paste the result.

**Prove non-vacuity.** Re-read the comment count and assert it equals the baseline plus exactly 1.
Record both numbers.

**Capture the durable handle.** Record the new comment's id, URL, creation timestamp and author.

**Prove the state assertions.** gh#11 must still be `OPEN`. The issue body must be unchanged and no
pre-existing comment edited: compare the ids and creation dates of every pre-existing comment against
Task 1's capture, including the commitment comment the reply quotes.

**Posted-mode gate.** Run the gate through the env seam over the same-basename temp file. It must exit
0. Paste the PASS line.

Confirm `git rev-parse --abbrev-ref HEAD` is still the milestone branch after committing.
  </action>
  <acceptance_criteria>
    - The body diff between the frozen draft and the stored last-comment body produces no output.
    - The comment count equals the Task 1 baseline plus exactly 1.
    - `gh issue view 11 --json state --jq '.state'` returns `OPEN`.
    - The new comment's id, URL, timestamp and author are recorded.
    - The posted-mode gate run over the same-basename temp file exits 0.
    - Every pre-existing comment's id and creation date is unchanged from Task 1's capture, including the quoted commitment comment.
    - `git -C /workspaces rev-parse --abbrev-ref HEAD` returns the milestone branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/152-outward-facing-close-operator-gated; T=$(mktemp -d); gh issue view 11 --repo henols/firestarter_prom --json comments --jq '.comments[-1].body' > "$T/152-GH11-COMMENT.md"; diff -u 152-GH11-COMMENT.md "$T/152-GH11-COMMENT.md"; echo "BODYDIFF rc=$?"; gh issue view 11 --repo henols/firestarter_prom --json state,comments --jq '{state:.state, n:(.comments|length), last:(.comments[-1]|{id,url,createdAt,author:.author.login})}'; FIRESTARTER_CLAIMSCAN_TARGETS_152="$T/152-GH11-COMMENT.md" python3 152-check-claims.py; echo "POSTEDGATE rc=$?"; git -C /workspaces rev-parse --abbrev-ref HEAD</automated>
  </verify>
  <done>One comment is posted, its stored body diffs clean against the frozen draft, the count advanced by exactly one, gh#11 is still OPEN with every prior comment untouched, and the posted-mode gate is green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

**No new files.** One public comment on gh#11.

**Recorded values handed forward to `152-LEDGER.md` (Plan 152-19)**
- The comment id, URL, creation timestamp and author
- The body-diff result and the comment-count delta
- The posted-mode gate PASS line
- The verified character-identical quotation of the 2026-08-03 commitment, with that comment's id

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen draft → public comment | Irreversible; two named community participants are subscribed. |
| someone's own words → our quotation of them | A misquotation in a reply to the person who wrote it is a small error with a large cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-152-70 | Elevation of privilege | an auto or chained run auto-approving this checkpoint | critical | mitigate | The configuration guard runs first and fails closed; the prohibition is restated. ⚠ Partial control; residual risk stated. |
| T-152-71 | Repudiation | misquoting the commitment the reply exists to discharge | high | mitigate | Task 1 verifies the quotation character-for-character against the live stored comment and stops on any mismatch. |
| T-152-72 | Repudiation | framing a same-day answer as a silence | medium | mitigate | The checkpoint's second review item is exactly this; the draft's apology-word count was asserted zero in Plan 152-07. |
| T-152-73 | Repudiation | assuming the post landed as authored | high | mitigate | Stored body re-read and diffed; count delta as non-vacuity guard; `updatedAt` rejected. |
| T-152-74 | Tampering | a duplicated comment after a partially-succeeded post | medium | mitigate | Explicit prohibition on re-running before re-reading the count. |
| T-152-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package install occurs in this plan.** |
</threat_model>

<verification>
- Auto guard, defaults gate, draft gate and paired suite green before the checkpoint.
- The quoted commitment verified character-identical against the live thread.
- Body diff empty; count delta exactly 1; gh#11 `OPEN`; every prior comment untouched.
- Posted-mode gate green over a same-basename temp file.
</verification>

<success_criteria>
OUT-03 is discharged: gh#11 carries a comment whose stored text is provably the reviewed text, which
answers the report in the shape it actually has, and which pays a commitment rather than inventing a
silence.
</success_criteria>

<output>
Create `.planning/phases/152-outward-facing-close-operator-gated/152-16-SUMMARY.md` when done.

Record verbatim: all five pre-post check results, the verified quotation with its comment id, the
baseline and post-post comment counts, the body diff result, the new comment's id/URL/timestamp/author,
and the posted-mode PASS line.

⚠ **This SUMMARY becomes a `152-check-claims.py` gate target in Plan 152-20.** It MUST contain the
literal sentence `This ships software-proven and unvalidated on silicon.` and must avoid every
forbidden label's vocabulary — do not write that gh#11, gh#21 or gh#12 is closed, resolved or fixed,
and do not write that something now works.

Verify before committing:
`FIRESTARTER_CLAIMSCAN_TARGETS_152=$(pwd)/.planning/phases/152-outward-facing-close-operator-gated/152-16-SUMMARY.md python3 .planning/phases/152-outward-facing-close-operator-gated/152-check-claims.py` → rc 0.
</output>
