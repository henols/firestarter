---
phase: 153-write-path-erase-policy
plan: 16
type: execute
wave: 12
depends_on: ["153-14", "153-15"]
commits_land_in: [meta]
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/phases/153-write-path-erase-policy/153-RECORD.md
autonomous: true
requirements: [ERASE-09]

must_haves:
  truths:
    - "PROJECT.md and the ROADMAP milestone index both state that v1.32 has THREE firmware-touching workstreams, naming phases 149, 151 and 153, and PROJECT.md's workstream table carries a row for 153."
    - "The phase's own record states, verbatim, that the change ships software-proven and unvalidated on silicon."
    - "No artifact in this phase asserts the 0x0D write path is proven, graduates 0x0D out of UNVERIFIED, changes any support_status, or requires an AT28C part."
    - "The full phase gate was run from a committed tree across both sub-repositories, and every command and result is transcribed."
  artifacts:
    - .planning/phases/153-write-path-erase-policy/153-RECORD.md
  key_links:
    - "ROADMAP.md and PROJECT.md must be edited with SCOPED replacements only. A whole-file rewrite destroys every phase entry outside the diff window, and the repository's own requirements and roadmap helper verbs reformat the entire file as a side effect — so these are hand edits, and the diff must be inspected before committing."
    - "The milestone index line and the v1.32 header line are DIFFERENT lines with the same stale claim. The header was already amended to three; the index line was not. Correcting one and not the other leaves the contradiction in place."
    - "ERASE-09's grep target is this phase's own directory. The phrase must appear in a file under it, and plan 01 already put it in the decision record; this plan puts it in the phase record too, where a reader looking for the outcome will find it."
    - "Two host gates already forbid writing a support_status and forbid over-claiming diagnostic-report text. Both must be run as part of the phase gate, so ERASE-09's must-not clauses are machine-checked rather than merely intended."
---

<objective>
Close the phase: correct the two in-repo records this phase falsifies, write the honesty record in
this phase's own words, and run the whole phase gate from a committed tree.

Purpose: ERASE-09 and 152-CONTEXT.md `D-15`. Phase 152 drafts outward-facing text from these records,
and it runs immediately after this phase — so a stale count or an over-claim here becomes a public
claim there. `D-08` reordered the phases precisely so every claim would be true at the moment it
becomes public; this plan is where that gets checked.

Output: two scoped record corrections, one phase record, and a transcribed full-gate run.

**Requirement flips owned by this plan: ERASE-09.**

Cites `D-153-01` through `D-153-05` as the decisions the record summarises, and 152-CONTEXT.md
`D-15` as the authority for the record corrections.
Locked decisions honoured: **L-10** (the honesty wording is literal, and no artifact asserts the
`0x0D` write path is proven, graduates it out of `UNVERIFIED`, changes any `support_status`, or
requires an AT28C part) and **L-11** (the `PROJECT.md` and ROADMAP record corrections this phase
owns).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct PROJECT.md's firmware-workstream count and add a workstream row for 153 (D-15)</name>
  <read_first>
    - `.planning/PROJECT.md` — the current-milestone header block, whose subtitle line states that
      v1.32 is mostly host-side with **one** firmware-touching workstream and names the page-size seam
      as that one
    - the same file's target-workstreams table — all six rows, their column shape, and workstream 4's
      description with its deferral amendment
    - `.planning/ROADMAP.md` around the v1.32 header — the line already amended to three
      firmware-touching workstreams naming phases 149, 151 and 153, which is the wording PROJECT.md
      must now agree with
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` — `D-15`, the
      authority for these edits and the reason they are this phase's to make rather than 152's
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Use scoped replacements only. Do not rewrite this file: a whole-file write destroys content
    outside the diff window, and the repository's own roadmap and requirements helper verbs reformat
    the entire file as a side effect, which is why these are hand edits.

    Three changes:

    (a) The current-milestone subtitle line: change **one** firmware-touching workstream to
    **three**, and name all three phases — 149 for the page-size seam, 151 for the protection read,
    153 for the write-path erase policy — stating that all three are dual-repo lockstep. Match the
    wording already used in the ROADMAP's v1.32 header so the two records agree verbatim rather than
    approximately. Keep the amendment-provenance convention this project uses: note when and why the
    line changed, so a reader can see it was corrected rather than always having said this.

    (b) The target-workstreams table: add one row for workstream 7, the write-path erase policy. Fill
    its surface column as firmware and host, and its bench column as no. Its description states the
    two halves in one sentence — no pre-write blank check on the two auto-erasing protocols, and a
    standalone software chip erase on `0x0D` — and notes that it was added mid-milestone from Phase
    152's discuss session. Keep the table's column count and order identical to every other row.

    (c) Workstream 4's description: it currently describes closing the AT28C book as the deferred
    relock half plus the owed issue reply. Extend it to note that the write-path half of that book is
    now closed by workstream 7, while the relock half stays deferred — so a reader does not conclude
    from the deferral that nothing shipped for the AT28C family.

    After editing, inspect the diff before committing and confirm it contains only these three hunks
    and no reflow.
  </action>
  <verify>
    <automated>cd /workspaces; grep -n 'firmware-touching workstream' .planning/PROJECT.md</automated>
    <automated>cd /workspaces; grep -c 'one\nfirmware-touching workstream' .planning/PROJECT.md</automated>
    <automated>cd /workspaces; grep -cE '149.*151.*153|153' .planning/PROJECT.md</automated>
    <automated>cd /workspaces; grep -n '^| 7 |' .planning/PROJECT.md</automated>
    <automated>cd /workspaces; grep -n '^| [0-9] |' .planning/PROJECT.md | awk -F'|' '{print NF}' | sort -u</automated>
    <automated>cd /workspaces; git diff --numstat -- .planning/PROJECT.md</automated>
    <automated>cd /workspaces; git diff -- .planning/PROJECT.md | grep -c '^-' </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'firmware-touching workstream'` shows the subtitle now saying three, and no occurrence
      anywhere in the file still says one or two.
    - The subtitle names all three phases — 149, 151 and 153 — and states dual-repo lockstep, using
      the same wording as the ROADMAP's v1.32 header.
    - A workstream row 7 exists, and the `awk` field-count over all numbered workstream rows returns a
      single value, so the new row is not ragged.
    - Workstream 4's description mentions that the write-path half is closed by workstream 7 while the
      relock half stays deferred.
    - `git diff --numstat` shows a small, bounded change, and the count of removed lines is consistent
      with three targeted hunks — the summary states the numbers and confirms no reflow.
    - The amendment-provenance note records when and why the subtitle changed.
  </acceptance_criteria>
  <done>
    PROJECT.md agrees with the ROADMAP and with reality about how many firmware workstreams v1.32 has,
    and carries a row for the one this phase added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the ROADMAP milestone-index line (D-15)</name>
  <read_first>
    - `.planning/ROADMAP.md` — the milestone-index bullet for v1.32 near the top of the file. It
      states the phase range as ending at 152 and says the milestone has **two** firmware-touching
      workstreams, naming the page-size seam and the protection read.
    - the same file's v1.32 header further down — already amended to three, naming 149, 151 and 153.
      This is the wording the index line must agree with.
    - the same file's phase-153 entry and the deliberate non-numeric checklist-order note above the
      phase list — read both so neither is disturbed
    - `.planning/phases/152-outward-facing-close-operator-gated/152-CONTEXT.md` — `D-15`
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Use scoped replacements only. This file contains every phase entry for the project; a whole-file
    write would destroy the ones outside the diff window, and the repository's own roadmap helper
    verbs reformat the whole file as a side effect. Hand-edit the single bullet and inspect the diff.

    In the v1.32 milestone-index bullet: change the phase range so it includes 153, and change the
    **two** firmware-touching workstreams claim to three, naming the page-size seam (149), the
    protection read (151) and the write-path erase policy (153). Keep the bullet's existing amendment
    provenance and add to it in the same style, noting when and why this correction was made and that
    the v1.32 header carried the corrected count first. Leave every other clause of that bullet
    exactly as it is — the Evidence-Ceiling sentence, the backlog references, the requirement count,
    and the still-open issue list are all still true and must not be touched.

    Do **not** touch the deliberate non-numeric checklist-order note or the order of the phase
    checkboxes: phase 153 is listed before phase 152 on purpose, because next-phase resolution reads
    the first unchecked box and `D-08` requires 153 to complete first. Restoring numeric order there
    would send a chain straight into the outward-facing close.

    Do not update the phase-153 entry's plan list or plan count in this task; that is the planner's own
    bookkeeping and is done outside this plan.

    Confirm afterwards that the phase-checkbox order is unchanged and that the total number of phase
    entries in the file is unchanged.
  </action>
  <verify>
    <automated>cd /workspaces; grep -n 'firmware-touching workstream' .planning/ROADMAP.md</automated>
    <automated>cd /workspaces; grep -c 'Phases 147–152' .planning/ROADMAP.md</automated>
    <automated>cd /workspaces; grep -c '^### Phase ' .planning/ROADMAP.md</automated>
    <automated>cd /workspaces; grep -n 'CHECKLIST ORDER IS DELIBERATELY NON-NUMERIC' .planning/ROADMAP.md</automated>
    <automated>cd /workspaces; grep -n -A2 'CHECKLIST ORDER IS DELIBERATELY NON-NUMERIC' .planning/ROADMAP.md | head -8</automated>
    <automated>cd /workspaces; git diff --numstat -- .planning/ROADMAP.md</automated>
    <automated>cd /workspaces; git diff -- .planning/ROADMAP.md | grep -c '^-'</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of `firmware-touching workstream` anywhere in the file still says two; every
      occurrence says three and names 149, 151 and 153.
    - `grep -c 'Phases 147–152'` returns `0` — the index line's phase range now includes 153.
    - `grep -c '^### Phase '` returns the same value as before the edit, recorded in the summary: no
      phase entry was lost.
    - The deliberate non-numeric checklist-order note is still present, and the phase checkbox
      immediately after it is still phase 153, confirmed by the two-line context grep.
    - `git diff --numstat` shows a change confined to the one bullet, and the removed-line count is
      consistent with a single hunk. The summary states the numbers.
    - The bullet's Evidence-Ceiling sentence, backlog references, requirement count and open-issue list
      are all unchanged.
  </acceptance_criteria>
  <done>
    Both stale statements of the firmware-workstream count are corrected, and the deliberate phase
    ordering `D-08` depends on is intact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the phase record (ERASE-09) and run the full phase gate</name>
  <read_first>
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — all five decisions and both
      measured-position sections, which this record summarises rather than restates
    - `.planning/phases/153-write-path-erase-policy/153-VALIDATION.md` — the phase-gate list, which
      this task executes in full, and the trap list that invalidates a green run
    - `.planning/REQUIREMENTS.md` — the nine ERASE requirements and the milestone's out-of-scope table,
      so the record's claims are bounded by what was actually required
    - `firestarter_app/tools/check_no_community_support_status_write.py` and
      `firestarter_app/tools/check_diagnostic_report_claims.py` — the two gates that machine-check
      ERASE-09's must-not clauses
    - `.planning/phases/153-write-path-erase-policy/153-*-SUMMARY.md` — every prior summary in this
      phase, for the measured figures and observed-failure transcripts the record cites
  </read_first>
  <files>.planning/phases/153-write-path-erase-policy/153-RECORD.md</files>
  <action>
    Create `153-RECORD.md`, the phase's outcome record. It is short and load-bearing, not a narrative.
    Sections:

    **What shipped.** One paragraph per requirement, nine in all, each stating the observable outcome
    and citing the plan that delivered it. No requirement gets a paragraph claiming more than its
    criteria proved.

    **What was proven, and how.** Name the oracle for each central claim: the write-path removal proven
    by a native case asserting single-shot INIT and stream identity against the committed golden; the
    erase stream proven by head equality against the SDP golden, a terminal-byte assertion read from
    the in-tree tables, and an exact divergence index; the absence of any programming-rail write proven
    by a brace-matched negative source scan that was observed failing against a committed planted
    violation; the 84-row scope proven exhaustively over all 746 database rows; the size cost proven by
    three cold builds and both gate modes.

    **What was NOT proven.** State plainly, and in these words, that the change ships
    **software-proven and unvalidated on silicon**. No AT28C part was involved at any point. State the
    specific consequences: nothing here shows that a real AT28C part erases; `0x0D` stays `UNVERIFIED`;
    no `support_status` moved; the community issues stay open, because a code fix is not a validation
    and only a passing report from real silicon closes one. Also carry forward the two honest
    limitations the research logged: the application note's 20 ms cycle time is an Atmel family maximum
    while this bucket spans several other vendors, so a part with a longer cycle time would read
    non-blank after a successful-looking erase; and no native test can prove the wall-clock wait,
    because the stubs record no time, so that assertion is structural only.

    **Decisions.** A table of `D-153-01` through `D-153-05` with one line each, pointing at the
    decision record rather than restating it.

    **Two size figures, separately.** The MERGE-05 leonardo position after this phase, and the Caterina
    cliff headroom labelled UNGUARDED, with the sentence that they are never the same number.

    **Mechanism corrections made in this phase.** A short list, because these are the items a reader
    six months from now will need: the GATE-03 criterion's stated mechanism did not hold and the real
    control is a source scan; the sibling `0x05` conditional was located rather than assumed; ERASE-06
    needed no source edit and the adopted reading is recorded; ERASE-07's comment is the fourth
    recorded reversal in its chain; and the corrected line numbers for the four sites whose recorded
    positions had drifted.

    **What Phase 152 must not repeat.** One short list of claims that are now false and must not appear
    in outward-facing text: that the family has no erase operation; that the blank-check skip flag is
    required to write a non-blank AT28C part; and that v1.32 has fewer than three firmware-touching
    workstreams.

    Then run the full phase gate from a **committed** tree, in both sub-repositories, exactly as
    `153-VALIDATION.md` lists it: both native environments; the three cold AVR builds and both
    `check_size_baseline.py` modes; the build-warnings checker; the erase-body checker; the
    checker-convention meta-test; the firmware test modules; the full host suite with coverage against
    its floor; host lint, format and type gates; the dispatch checker with its un-diffed proof and its
    invariant suite; the SDP-window checker; the cross-repo parity tests; and the two ERASE-09 gates.
    Record every command and its result. Commit before running anything that asserts
    whole-repository porcelain — both repositories carry such a test.

    Also run the cross-repo gates once with the firmware-root environment variable pointed at a
    nonexistent path, and confirm they SKIP cleanly rather than error. The devcontainer's sibling
    layout hides exactly this class of failure from what CI sees.
  </action>
  <verify>
    <automated>cd /workspaces; grep -c 'software-proven and unvalidated on silicon' .planning/phases/153-write-path-erase-policy/153-RECORD.md</automated>
    <automated>cd /workspaces; grep -cE 'ERASE-0[1-9]' .planning/phases/153-write-path-erase-policy/153-RECORD.md</automated>
    <automated>cd /workspaces; grep -c 'UNGUARDED' .planning/phases/153-write-path-erase-policy/153-RECORD.md</automated>
    <automated>cd /workspaces; grep -cE 'D-153-0[1-5]' .planning/phases/153-write-path-erase-policy/153-RECORD.md</automated>
    <automated>cd /workspaces; grep -ciE 'proven on silicon|validated on hardware|graduat' .planning/phases/153-write-path-erase-policy/153-RECORD.md</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_no_community_support_status_write.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_diagnostic_report_claims.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; git diff --stat -- firestarter/data/chip_database.json</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70 -o addopts="" -q 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python3 tools/check_mypy_watermark.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_dispatch.py; echo "exit=$?"; git diff --quiet -- tools/check_dispatch.py; echo "undiffed=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_no_log_in_sdp_window.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py tests/test_check_dispatch_invariants.py -o addopts="" -q 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter_app; FIRESTARTER_FW_ROOT=/nonexistent-path-for-skip-check python3 -m pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q -rs 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter; pio test -e native 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter; pio test -e native_nodevtools 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter; python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -12</automated>
    <automated>cd /workspaces/firestarter; python3 scripts/check_erase_no_vpp.py; echo "exit=$?"; python3 scripts/check_build_warnings.py; echo "exit=$?"</automated>
    <automated>cd /workspaces; git status --porcelain | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `153-RECORD.md` exists and contains the phrase `software-proven and unvalidated on silicon`
      verbatim at least once.
    - All nine ERASE ids appear in the record, each with a paragraph stating its observable outcome and
      the plan that delivered it.
    - All five `D-153-0N` ids appear in the decisions table.
    - The record contains a section stating what was NOT proven, including that `0x0D` stays
      `UNVERIFIED`, that no `support_status` moved, that the community issues stay open, and both
      carried-forward limitations (the family-maximum cycle time and the unprovable wall clock).
    - `grep -ciE 'proven on silicon|validated on hardware|graduat'` returns `0`, or every occurrence is
      inside an explicit negation and the summary quotes each one — no over-claim slips through as
      prose.
    - Both ERASE-09 gates exit 0, and `git diff --stat` over the generated chip database is empty.
    - The full host suite passes with coverage at or above its floor, and lint, format and type gates
      all pass.
    - `check_dispatch.py` exits 0 and `git diff --quiet` over it returns 0.
    - Both native environments pass, the firmware pytest modules pass, and the erase-body and
      build-warnings checkers both exit 0.
    - The cross-repo gates SKIP cleanly with the firmware root pointed at a nonexistent path, and the
      summary records the skip reasons rather than treating an error as a skip.
    - `git status --porcelain` is clean at the end, from a committed tree, and the summary transcribes
      every gate command with its result.
  </acceptance_criteria>
  <done>
    The phase's outcome is recorded in its own words with its limits stated, both stale in-repo counts
    are corrected, and every gate has been run and transcribed from a committed tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo phase record → Phase 152's outward-facing text | Phase 152 drafts release notes and issue replies from these files, immediately after this phase. |
| phase record → future readers | An over-claim here becomes the project's memory of what was proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-91 | Repudiation / over-claim | the record implying the `0x0D` write path is validated | high | mitigate | The verbatim honesty phrase is required and grep-asserted; a negative grep over validation vocabulary must return zero or be explained occurrence by occurrence; both machine gates that forbid `support_status` writes and over-claiming report text must exit 0. |
| T-153-92 | Tampering | a whole-file write to ROADMAP.md or PROJECT.md destroying entries outside the diff window | critical | mitigate | Scoped replacements only, with criteria asserting the phase-entry count is unchanged, the workstream table's field count is uniform, and the removed-line counts are consistent with the intended hunks. |
| T-153-93 | Tampering | the deliberate non-numeric phase-checkbox order restored to numeric | high | mitigate | The action forbids touching it and a criterion asserts the note is present and that phase 153's checkbox still follows it — otherwise a chain runs the outward-facing close first. |
| T-153-94 | Repudiation | the two stale firmware-workstream counts corrected in one file but not the other | medium | mitigate | Both files are edited in this plan and both criteria negative-grep the stale wording across the whole file, not just the target line. |
| T-153-95 | Spoofing (false green) | a cross-repo gate erroring and being read as a skip | medium | mitigate | The gates are run with the firmware root pointed at a nonexistent path and the summary must record the skip reasons, which distinguishes a clean skip from an error. |
| T-153-96 | Spoofing | a gate run against an uncommitted tree, so a porcelain-asserting test fails for the wrong reason | medium | mitigate | The action requires committing first and a criterion asserts `git status --porcelain` is clean at the end. |
| T-153-97 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- `153-RECORD.md` carries the verbatim honesty phrase, all nine requirement ids, all five decision
  ids, both size figures with the Caterina one labelled UNGUARDED, and a what-was-not-proven section.
- No occurrence of the stale firmware-workstream count survives in either record file, and the phase
  entry count and checkbox order in the ROADMAP are unchanged.
- Both ERASE-09 gates exit 0 and the generated chip database is byte-unchanged.
- Both native environments, the firmware pytest modules, the full host suite with coverage, all lint
  and type gates, the dispatch checker with its un-diffed proof, the SDP-window checker, the
  erase-body checker and the build-warnings checker all pass.
- The cross-repo gates skip cleanly with the sibling absent.
- `git status --porcelain` is clean.
</verification>

<success_criteria>
- ERASE-09 is satisfied in the phase's own words, with the limits stated as plainly as the outcomes.
- Both in-repo records `D-15` names are corrected, with scoped edits and an inspected diff.
- Phase 152 inherits a record in which every claim is already true.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| `.planning/phases/153-write-path-erase-policy/153-RECORD.md` | new file (meta) | The phase outcome record; carries ERASE-09's verbatim phrase and the what-was-not-proven section |
| corrected PROJECT.md milestone subtitle | in-repo record correction | one to three firmware-touching workstreams, naming 149, 151, 153 |
| new PROJECT.md workstream row 7 | in-repo record | write-path erase policy; firmware and host; bench no |
| extended PROJECT.md workstream 4 description | in-repo record | write-path half closed, relock half still deferred |
| corrected ROADMAP milestone-index bullet | in-repo record correction | phase range includes 153; two to three workstreams |
| transcribed full phase-gate run | phase record | both sub-repositories, from a committed tree, with the sibling-absent skip check |

<output>
Create `.planning/phases/153-write-path-erase-policy/153-16-SUMMARY.md` when done.
</output>
