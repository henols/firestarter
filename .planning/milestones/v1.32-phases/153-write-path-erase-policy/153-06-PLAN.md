---
phase: 153-write-path-erase-policy
plan: 06
type: execute
wave: 6
depends_on: ["153-05"]
commits_land_in: [firestarter]
files_modified:
  - firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
  - firestarter/src/proms/flash_5v_page.cpp
autonomous: true
requirements: [ERASE-02]

must_haves:
  truths:
    - "The 0x05 sibling blank-check conditional was LOCATED in code and quoted before being changed — its existence was not assumed by symmetry with 0x0D."
    - "A `firestarter write` on protocol 0x05 performs no blank check, whether or not the caller passes the blank-check skip flag: write-INIT is single-shot and allocates no blank-check progress data."
    - "The 0x05 erase-on-write block is untouched — flash4's erase remains gated by FLAG_CAN_ERASE, which the host still clears for algorithm 5 because setting it would route a 12 V bulk erase onto a 5 V-only part."
    - "The proof was OBSERVED to fail before the deletion landed and to pass after."
  artifacts:
    - firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
  key_links:
    - "The conditional is at flash_5v_page.cpp lines 87-89 — NOT 88-90 as an earlier note recorded. Verify the line numbers in the file before editing; the RESEARCH figure was corrected by the pattern map."
    - "The erase-on-write block immediately above it (lines 79-86) is a DIFFERENT thing and stays. Deleting or re-gating it would strip flash4's only erase path; adding its counterpart to eeprom28c_write_init is forbidden by D-153-05."
    - "Every existing case in test_val_5v_page bypasses write_init and calls operation_main directly, so no existing case exercises the blank-check axis at all. A new case that drives INIT is required, or this deletion ships with zero coverage."
    - "`mem_util_blank_check` sets operation_in_progress and mallocs progress_data on its FIRST call regardless of mem_size, so those two observables are the oracle and are independent of what the hardware stubs return for a read."
---

<objective>
Remove the pre-write blank check from protocol `0x05` (flash4) as well, having first located its
conditional in code rather than assuming it exists.

Purpose: ERASE-02, whose wording is deliberate: the decomposition in 152-CONTEXT.md D-07 recorded
this sibling as "to locate", and the requirement demands it be located in code before being
changed. It is at `flash_5v_page.cpp` lines 87-89, byte-identical in shape to the `0x0D` one —
evidence of presence, not of absence.

Output: one new native case that drives write-INIT, then a three-line deletion. The erase-on-write
block above it is untouched.

**Requirement flips owned by this plan: ERASE-02.**

Cites `D-153-05` (the sibling's erase-on-write block is a pattern to recognise, not to copy).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-PATTERNS.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
@firestarter/src/proms/flash_5v_page.cpp
@firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Locate and quote the `0x05` conditional, then add a write-INIT case and OBSERVE it fail</name>
  <read_first>
    - `firestarter/src/proms/flash_5v_page.cpp` lines 72-92 — the whole of
      `flash_5v_page_write_init`: the `is_operation_in_progress` guard, the response-code early
      return, the `FLAG_CAN_ERASE` / `FLAG_SKIP_ERASE` erase block at 79-86, and the blank-check
      conditional at 87-89. Quote the conditional verbatim into the summary with its measured line
      numbers — that quotation is ERASE-02's located-in-code evidence.
    - `firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp` lines 205-232 —
      `make_write_handle_with_data`, whose own comment records that every existing case bypasses
      `write_init` and calls `operation_main` directly, and that `ctrl_flags = 0` means
      `write_init` *would* blank-check
    - the same file lines 428-455 — `main`'s `RUN_TEST` registration list and its section comments
    - the same file lines 30-45 — the include block; `operation_utils.h` is not yet included and is
      needed for `is_operation_in_progress`
    - `firestarter/src/proms/memory.cpp` lines 390-425 — `BLANK_CHECK_CHUNK_SIZE` is 2048, and
      `mem_util_blank_check` sets `operation_in_progress` and mallocs `progress_data` on its first
      call regardless of `mem_size`
    - `firestarter/test/native/avr/test_val_eeprom28c/test_val_eeprom28c.cpp` lines 200-212 — the
      `assert_no_vpp_in_recording` idiom, for the VPP-safety assertion this new case also carries
  </read_first>
  <files>firestarter/test/native/avr/test_val_5v_page/test_val_5v_page.cpp</files>
  <behavior>
    - With the blank-check skip flag CLEAR and `FLAG_CAN_ERASE` clear, one call to
      `flash_5v_page_write_init` leaves `is_operation_in_progress(&h)` FALSE.
    - With those flags clear, `h.progress_data` is NULL after that call.
    - With those flags clear, `h.response_code` is not `RESPONSE_CODE_ERROR`.
    - No VPP-enable control bit appears in the recording for that INIT call.
    - The first two assertions FAIL today, because `mem_util_blank_check` sets the flag and mallocs,
      and PASS after Task 2.
  </behavior>
  <action>
    Add `#include "operation_utils.h"` to the include block, after `"memory.h"`, so
    `is_operation_in_progress` resolves.

    Add a sibling handle factory `make_write_init_handle_blank_check_enabled(void)`, placed next to
    `make_write_handle_with_data`, building a `0x05` `CMD_WRITE` handle with `chip_id` 0,
    `ctrl_flags` 0 and a small `mem_size` (2048 is enough — the oracle is independent of `mem_size`,
    and a small value keeps the single INIT invocation cheap). Do not modify
    `make_write_handle_with_data`; its comment is relied on by the existing cases.

    Add `test_5v_page_write_init_no_blank_check_with_flag_clear_erase02(void)`: build that handle,
    call `configure_memory(&h)`, clear the bus recording, then invoke the production INIT through
    the dispatch pointer — `h.firestarter_operation_init(&h)` — rather than calling the function by
    name, so the test exercises what `configure_memory` actually wired. Then assert, in order:
    1. `TEST_ASSERT_FALSE_MESSAGE(is_operation_in_progress(&h), ...)` with a message naming ERASE-02
       and stating that `mem_util_blank_check` was the only setter of that flag on this path, so a
       FALSE here is the single-shot-INIT proof.
    2. `TEST_ASSERT_NULL_MESSAGE(h.progress_data, ...)` with a message stating that a non-NULL value
       means a blank-check progress block was allocated, i.e. the check still ran.
    3. `TEST_ASSERT_NOT_EQUAL_MESSAGE(RESPONSE_CODE_ERROR, h.response_code, ...)` — the removed
       check can no longer fail a write on a non-blank part.
    4. `assert_no_vpp_in_recording(...)` — or this suite's equivalent VPP assertion helper — with a
       message stating that with `FLAG_CAN_ERASE` clear the INIT path energises no rail, so the
       deletion does not accidentally open the erase branch. If this suite has no such helper, use
       its existing recording accessors directly and state in the summary which idiom was used.

    Register the case with a `RUN_TEST` line in `main`, in a new one-line section comment naming
    ERASE-02, placed after the write-execute section.

    Then run the suite and **observe the failure**. Transcribe into the summary which assertion
    fired first and the verbatim Unity failure line. Do not proceed to Task 2 until a real, named
    RED has been transcribed: a case that passes on arrival means the oracle is wrong, not that the
    code is right.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter; sed -n '72,92p' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -n 'FLAG_SKIP_BLANK_CHECK' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'make_write_init_handle_blank_check_enabled' test/native/avr/test_val_5v_page/test_val_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'RUN_TEST(test_5v_page_write_init_no_blank_check_with_flag_clear_erase02);' test/native/avr/test_val_5v_page/test_val_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'operation_utils.h' test/native/avr/test_val_5v_page/test_val_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; pio test -e native -f native/avr/test_val_5v_page 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The summary quotes the `0x05` blank-check conditional verbatim with its measured line numbers,
      and states whether they are 87-89 as the pattern map recorded or something else. This
      quotation is ERASE-02's located-in-code evidence and is required, not optional.
    - `make_write_init_handle_blank_check_enabled` exists and `make_write_handle_with_data` is
      unchanged.
    - `test_5v_page_write_init_no_blank_check_with_flag_clear_erase02` exists with the four
      assertions above, in order, and drives INIT through `h.firestarter_operation_init(&h)` rather
      than by function name.
    - The case is registered in `main` — `grep -c` for its `RUN_TEST` line returns 1.
    - `operation_utils.h` is included exactly once.
    - `pio test -e native -f native/avr/test_val_5v_page` reports the new case FAILING, and the
      summary transcribes which assertion fired with the verbatim Unity line.
    - Every other case in the suite still passes in the same run.
  </acceptance_criteria>
  <done>
    ERASE-02's sibling is located in code and quoted, and a reachable observed-RED oracle exists for
    its removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the `0x05` blank-check conditional and turn the case GREEN</name>
  <read_first>
    - `firestarter/src/proms/flash_5v_page.cpp` lines 72-92 — re-read immediately before editing;
      the target is the three-line `if` at 87-89 and nothing else
    - the same file lines 79-86 — the `FLAG_CAN_ERASE` / `FLAG_SKIP_ERASE` erase-on-write block that
      stays untouched
    - `firestarter_app/firestarter/database.py` around the algorithm-5 exclusion rationale — the
      live hardware-hazard reason `FLAG_CAN_ERASE` is still cleared for algorithm 5, which is why
      that erase branch is unreachable in practice and must nonetheless not be removed
    - `.planning/phases/153-write-path-erase-policy/153-RESEARCH.md` §"Architecture Patterns"
      Pattern 1 — the four blank-check sites this phase must NOT touch (`eprom.cpp` twice,
      `flash_intel.cpp`, `flash_nor_unlock.cpp`), because those protocols do not auto-erase per page
      and D-07's policy is scoped to `0x0D` and `0x05`
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — `D-153-05`
  </read_first>
  <files>firestarter/src/proms/flash_5v_page.cpp</files>
  <action>
    Delete the three-line conditional at the tail of `flash_5v_page_write_init` — the `if` that
    guards `mem_util_blank_check(handle);` on the blank-check skip flag — and replace it with a
    short comment, in the surrounding style, that:
    - names 152-CONTEXT.md `D-07` and ERASE-02 as the authority;
    - states that flash4 auto-erases per page during the page write, so a pre-write blank check is a
      false precondition rather than a safety net;
    - states that the blank-check skip flag is consequently unread on this protocol, so a future
      reader must not restore the conditional on the grounds that the bit looks orphaned;
    - states that the `FLAG_CAN_ERASE` erase block immediately above is a **different** thing and
      stays: `FLAG_CAN_ERASE` is still cleared by the host for algorithm 5 because setting it would
      route a 12 V bulk erase onto a 5 V-only part, and that exclusion is a live hardware hazard,
      not a retired one;
    - states, per `D-153-05`, that this block is the pattern an executor must NOT copy into
      `eeprom28c_write_init`.

    Change nothing else in the file. Do not touch the erase block, the response-code early return,
    the `is_operation_in_progress` guard, or the four blank-check sites in the other protocol
    handlers — those protocols do not auto-erase and are explicitly out of D-07's scope.

    After the edit, run the suite, then both native environments, then the firmware-scanning
    cross-repo gates and the new `check_erase_no_vpp.py` checker from plan 05.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter; grep -c 'FLAG_SKIP_BLANK_CHECK' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'mem_util_blank_check' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'FLAG_CAN_ERASE' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'flash_5v_page_erase_execute' src/proms/flash_5v_page.cpp</automated>
    <automated>cd /workspaces/firestarter; grep -c 'mem_util_blank_check' src/proms/eprom.cpp src/proms/flash_intel.cpp src/proms/flash_nor_unlock.cpp</automated>
    <automated>cd /workspaces/firestarter; pio test -e native -f native/avr/test_val_5v_page 2>&1 | tail -20</automated>
    <automated>cd /workspaces/firestarter; pio test -e native 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter; pio test -e native_nodevtools 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter; python3 scripts/check_erase_no_vpp.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_no_log_in_sdp_window.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_dispatch.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_dispatch_mirror.py tests/test_sdp_table_parity.py -o addopts="" -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'FLAG_SKIP_BLANK_CHECK' src/proms/flash_5v_page.cpp` returns `0` if the replacement
      comment does not name the flag, or `1` with the summary stating the single occurrence is
      comment-only. Either is acceptable; a hit inside a function body is not.
    - `grep -c 'mem_util_blank_check' src/proms/flash_5v_page.cpp` returns `0` — this protocol has
      no `CMD_BLANK_CHECK` arm of its own, so unlike `eeprom_28c.cpp` there is no legitimate
      remaining reference. If the count is non-zero, the summary must name the surviving site and
      justify it.
    - `grep -c 'FLAG_CAN_ERASE'` and `grep -c 'flash_5v_page_erase_execute'` are both unchanged from
      their pre-task values, recorded in the summary — the erase-on-write block survives intact.
    - The blank-check counts in `eprom.cpp`, `flash_intel.cpp` and `flash_nor_unlock.cpp` are
      unchanged: this phase's policy is scoped to the two auto-erasing protocols only.
    - `test_5v_page_write_init_no_blank_check_with_flag_clear_erase02` now PASSES, and the summary
      states the before/after pair for the specific assertion observed RED in Task 1.
    - `pio test -e native` and `pio test -e native_nodevtools` are both fully green and report the
      same case and suite counts as each other. Both counts are recorded — this plan adds one case
      and plan 14 needs the final figure.
    - `python3 scripts/check_erase_no_vpp.py` exits 0 with a `PASS:` line.
    - `check_no_log_in_sdp_window.py` prints `PASS:`; `check_dispatch.py` exits 0; and
      `pytest tests/test_dispatch_mirror.py tests/test_sdp_table_parity.py` passes.
    - The replacement comment names `D-07`, ERASE-02 and `D-153-05`, and states that the
      `FLAG_CAN_ERASE` block above it stays and why.
  </acceptance_criteria>
  <done>
    Both auto-erasing protocols now write without a blank check, the flash4 erase branch is intact,
    and the four out-of-scope handlers are provably untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host wire `flags` → firmware handler | The blank-check skip bit crosses here and becomes unread on this protocol too. |
| firmware handler → flash socket | The erase branch immediately above the deleted lines energises a 12 V rail when `FLAG_CAN_ERASE` is set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-29 | Tampering (physical destruction) | the `FLAG_CAN_ERASE` erase-on-write block damaged while editing the lines directly below it | critical | mitigate | The action forbids touching lines 79-86, the criteria pin the pre-task occurrence counts for `FLAG_CAN_ERASE` and `flash_5v_page_erase_execute`, and the replacement comment must restate the algorithm-5 hazard rationale at the site. |
| T-153-30 | Spoofing (false assurance) | shipping the deletion with zero coverage because every existing case bypasses INIT | high | mitigate | Task 1 adds a case that drives INIT through the dispatch pointer and requires it to be OBSERVED failing first. |
| T-153-31 | Tampering | scope creep into the four non-auto-erasing protocols' blank checks | high | mitigate | A criterion pins the `mem_util_blank_check` counts in `eprom.cpp`, `flash_intel.cpp` and `flash_nor_unlock.cpp` as unchanged. |
| T-153-32 | Tampering | the sibling erase-on-write block copied into `eeprom28c_write_init` | high | mitigate | `D-153-05` forbids it and the replacement comment names the prohibition at the exact site. |
| T-153-33 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- The `0x05` conditional was quoted verbatim with measured line numbers before being changed.
- `pio test -e native` and `pio test -e native_nodevtools` pass and agree; both counts recorded.
- `check_erase_no_vpp.py`, `check_no_log_in_sdp_window.py` and `check_dispatch.py` all pass.
- The four out-of-scope handlers' blank-check counts are unchanged.
- The new case was observed RED then GREEN, both transcribed.
</verification>

<success_criteria>
- ERASE-02 is satisfied, with its located-in-code requirement discharged by a verbatim quotation
  rather than an assumption of symmetry.
- Flash4's erase path and the four out-of-scope handlers are provably untouched.
- All firmware source changes for this phase are now complete, so plan 14 can measure.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| `make_write_init_handle_blank_check_enabled` | new C++ static function (test) | `test_val_5v_page.cpp`; `ctrl_flags = 0`, small `mem_size` |
| `test_5v_page_write_init_no_blank_check_with_flag_clear_erase02` | new Unity case | +1 native case count |
| `operation_utils.h` include in `test_val_5v_page.cpp` | include addition | needed for `is_operation_in_progress` |
| deleted: the blank-check conditional in `flash_5v_page_write_init` | source deletion | changes AVR flash bytes on all three targets |
| CLI-visible behaviour change | behaviour | `firestarter write <flash4 chip>` no longer blank-checks and no longer needs the blank-check skip flag on a non-blank part |

<output>
Create `.planning/phases/153-write-path-erase-policy/153-06-SUMMARY.md` when done.
</output>
