---
phase: 153-write-path-erase-policy
plan: 14
type: execute
wave: 10
depends_on: ["153-06", "153-12", "153-13"]
commits_land_in: [firestarter]
files_modified:
  - firestarter/scripts/check_size_baseline.py
  - firestarter/scripts/baseline/size_baseline.json
autonomous: true
requirements: [ERASE-08]

must_haves:
  truths:
    - "The flash and RAM delta of every firmware change in this phase is MEASURED cold on all three AVR targets against the verified pre-change base, never estimated."
    - "A fourth separately-named, SHA-attributed FLASH exemption funds the measured growth; no band literal is widened, BASE-01 is not re-anchored, no existing exemption constant absorbs the bytes, and no fix was shrunk to fit."
    - "The RAM clause still reads as exact equality plus its single fully-consumed 2 B exemption — the erase added 0 B of RAM by construction, and this is verified rather than asserted."
    - "The default-mode gate is green again: both the AVR figures AND the native case and suite counts were re-recorded in the same baseline revision."
    - "The two size figures are recorded separately: the MERGE-05 leonardo headroom and the UNGUARDED Caterina cliff headroom."
  artifacts:
    - firestarter/scripts/check_size_baseline.py
    - firestarter/scripts/baseline/size_baseline.json
  key_links:
    - "The default-mode gate demands byte identity on the native `cases` and `suites` figures as well as on the AVR figures. This phase added native cases in plans 02, 04 and 06, so the AVR block and the native block must move in the SAME baseline revision or the gate stays red after a perfect measurement."
    - "Neither repository's CI runs the size gate, and the devcontainer's sibling layout hides cross-repo failures. This is a local, explicit, COLD verification task and the plan record is the only evidence it ran."
    - "A warm build gives a different figure than a cold one — the baseline's own metadata records a 96-count warm/cold error that survived a whole milestone. Every measurement here requires deleting the build directory first."
    - "MERGE-05 leonardo flash headroom (0 B, at +594 against a 594 B allowance) and the Caterina cliff (28672 B, UNGUARDED since the flash ceiling was raised to the real 32768 B) are DIFFERENT numbers. Computing a growth budget from the wrong one either blocks legitimate work or bricks a bootloader."
    - "The native warning watermark has zero headroom and uses a less-than-or-equal rule, so more cases are fine unless a new warning class appears. It must be re-run, not assumed."
---

<objective>
Measure this phase's firmware footprint cold on all three AVR targets, fund the measured growth with
a fourth named exemption, and re-record the baseline so both gate modes are green.

Purpose: ERASE-08. Leonardo has **zero** MERGE-05 flash headroom after Phases 149 and 151, so any
growth needs its own adjudicated, named, SHA-attributed exemption — and its size must come from a
measurement, never a guess. The RAM clause is exact equality with its one 2 B exemption already fully
consumed, which is why `D-153-01` chose a RAM-neutral erase form; that choice now gets verified.

Output: one new exemption constant with a seven-element justification comment, one extended allowance
function, and one baseline revision covering the AVR figures, the native counts, the three MERGE-05
clauses and the metadata.

**Requirement flips owned by this plan: ERASE-08.**

Cites `D-153-01` (the funding posture and the RAM-neutral form) and the pre-change measured position
recorded by plan 01.
Locked decisions honoured: **L-01** (a fourth named `v153` flash exemption for leonardo, sized from
a measured figure and never a guess) and **L-10** (the exemption comment carries the literal honesty
wording).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-PATTERNS.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
@firestarter/scripts/check_size_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the constants lockstep, then the cold flash and RAM figures on all three AVR targets</name>
  <read_first>
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — the pre-change measured
      position section recorded by plan 01, which is the base this delta is measured against
    - `firestarter/scripts/baseline/size_baseline.json` — `avr_targets`, `native_envs`, `envs_agree`,
      `envs_agree_note`, `warnings`, and the whole `meta` block including
      `meta.warm_vs_cold_correction`, `meta.deltas_vs_base01` with its three per-target
      `merge05_clause` strings, `meta.roadmap_cross_check` and
      `meta.flash_ceiling_move_260820_a7w`
    - `firestarter/scripts/baseline/size_baseline_base01.json` — the BASE-01 anchor figures
    - `firestarter/scripts/check_size_baseline.py` lines 1-60 and 474-560 — the two modes and the two
      allowance functions
    - `firestarter/include/firestarter.h` and `firestarter_app/firestarter/constants.py` — the
      command ids and flag bits this phase relies on, for the lockstep half of ERASE-08
  </read_first>
  <files>.planning/phases/153-write-path-erase-policy/153-DECISIONS.md</files>
  <action>
    Two measurements, both transcribed into a new section of `153-DECISIONS.md` headed
    `## Post-change measured position (cold)`. Change no file in `firestarter/` in this task.

    **(a) Constants lockstep.** ERASE-08's first clause. Confirm that the erase and blank-check
    command ids and the erase-capability, erase-skip and blank-check-skip flag bits hold identical
    values in the firmware header and the host constants module. Record each name with its value from
    both files side by side. Also run the repository's existing constants-parity test. This phase
    minted no new constant on either side, so the expected result is that nothing moved — record that
    as a measured fact, not as an assumption.

    **(b) Cold AVR figures.** For each of `uno`, `uno328pb`, `leonardo`, in that order: delete the
    build directory for that environment, then run exactly one build, teeing the output to a log.
    A warm build gives a different figure — the baseline's own metadata records a 96-count warm/cold
    error that survived a whole milestone — so the deletion is mandatory and a warm figure must not be
    transcribed.

    Then run both native environments and record the case and suite counts each reports, and run the
    build-warnings checker.

    Record in the new section:
    - a three-row table of flash used, flash total, RAM used and RAM total per target, from the cold
      logs;
    - the delta of each figure against the plan-01 pre-change position, and separately against
      BASE-01;
    - for leonardo, the arithmetic that sizes the new exemption: the BASE-01 delta minus the existing
      594 B allowance. That difference, and nothing else, is the value Task 2 uses. If it is zero or
      negative, say so and record that no new exemption is needed — and do not add one;
    - the RAM delta for each target against both bases. `D-153-01` predicts 0 B; if any target moved,
      stop and report, because the RAM clause is exact equality with a fully-consumed 2 B exemption
      and a move there is a blocker rather than a note;
    - the Caterina headroom as a separate, distinctly labelled figure: 28672 minus the measured
      leonardo flash used, marked UNGUARDED. State plainly that this is a different number from the
      MERGE-05 clause and that neither may be computed from the other;
    - the native case and suite counts for both environments, whether they agree, and the delta
      against the committed baseline's figures;
    - the build-warnings checker's result and whether the watermark still holds.

    State in the summary that neither repository's CI runs the size gate, so this local cold run is
    the only evidence it was ever exercised.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter; grep -nE 'define (CMD_ERASE|CMD_BLANK_CHECK|FLAG_CAN_ERASE|FLAG_SKIP_ERASE|FLAG_SKIP_BLANK_CHECK) ' include/firestarter.h</automated>
    <automated>cd /workspaces/firestarter_app; grep -nE '^(FLAG_CAN_ERASE|FLAG_SKIP_ERASE|FLAG_SKIP_BLANK_CHECK|CMD_ERASE|CMD_BLANK_CHECK) *=' firestarter/constants.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_revision_constants_parity.py -o addopts="" -q 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter; pio test -e native 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter; pio test -e native_nodevtools 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter; python3 scripts/check_build_warnings.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter; git diff --quiet && echo "firmware tree clean: measurement touched no source"</automated>
    <automated>cd /workspaces; grep -c 'Post-change measured position' .planning/phases/153-write-path-erase-policy/153-DECISIONS.md</automated>
    <automated>cd /workspaces; grep -c 'UNGUARDED' .planning/phases/153-write-path-erase-policy/153-DECISIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - The constants lockstep is recorded name by name with values from both files, and the parity test
      passes. The summary states that no constant moved in this phase.
    - Each of the three AVR builds was run cold: the summary names the deleted build directory per
      environment and the figures come from those runs.
    - A section headed `## Post-change measured position (cold)` exists with all three targets,
      including `uno328pb`, and with flash used, flash total, RAM used and RAM total each.
    - The leonardo exemption arithmetic is written out explicitly as the BASE-01 delta minus 594, and
      its result is the only figure Task 2 may use.
    - The RAM delta is recorded per target against both bases. If any is non-zero the summary flags it
      as a blocker and Task 2 does not proceed until it is resolved.
    - The Caterina headroom appears as a separate figure labelled UNGUARDED, with a sentence stating
      it is not the same number as the MERGE-05 clause.
    - The native case and suite counts are recorded for both environments with their delta against the
      committed baseline, and whether they agree.
    - The build-warnings checker's exit code and verdict are recorded.
    - `git diff --quiet` in `firestarter/` succeeds — the measurement changed no source.
  </acceptance_criteria>
  <done>
    Every figure the exemption and the baseline revision depend on is measured, cold, on all three
    targets, and written down before any of it is spent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author `MERGE05_ERASE_STANDALONE_EXEMPTION_BYTES` and extend the flash allowance</name>
  <read_first>
    - `firestarter/scripts/check_size_baseline.py` lines 185-260 — the first two named exemptions and
      their module-level rejection lists
    - the same file lines 250-370 — the third flash exemption and the single RAM exemption. Read the
      third one in full: it is the template, and the pattern map extracted a seven-element contract
      from it.
    - the same file lines 474-530 — `_merge05_flash_allowance` and `_merge05_ram_allowance`, the
      single consumers, and the docstring at the top of the flash one that enumerates the stacked
      exemptions
    - the same file lines 530-560 — `compare_avr_policy_merge05` and its printed allowance
      expression, which gains a fourth term
    - `.planning/phases/153-write-path-erase-policy/153-PATTERNS.md` — the seven required elements of
      the exemption comment, listed explicitly
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — `D-153-01` and the
      post-change measured position from Task 1
    - `git log --oneline` for this phase's firmware commits — the SHAs the comment must attribute
  </read_first>
  <files>firestarter/scripts/check_size_baseline.py</files>
  <action>
    Add one constant, `MERGE05_ERASE_STANDALONE_EXEMPTION_BYTES`, set to the value Task 1 measured —
    the leonardo BASE-01 delta minus the existing 594 B allowance. If Task 1 measured that difference
    as zero or negative, add no constant, change no allowance function, and record in the summary that
    the phase fit inside the existing allowance. Do not round the value up for headroom: an exemption
    is an admission of a measured cost, not a budget.

    Give it a comment block carrying all seven elements the pattern map extracted from the third
    exemption:
    1. Ordinal and scope in the first sentence: the FOURTH, separately-named, SHA-attributed flash
       exemption, in bytes, added to each target's allowance alongside the three that precede it.
    2. Phase and decision attribution: Phase 153, ERASE-03/ERASE-04/ERASE-08, `D-153-01`.
    3. The single-consumer property: name `_merge05_flash_allowance` as the only place this literal is
       read.
    4. A **what the bytes ARE** breakdown, itemised per commit with its SHA and a pointer to the
       transcript in this phase's records. Itemise the deletions separately from the additions — two
       three-line blank-check conditionals were **removed**, which is a saving, and the erase
       operation plus its dispatch arm and constant were added — so a reader can see the net figure's
       composition rather than one lump. Record any zero-byte item explicitly rather than omitting it;
       the third exemption's own comment does this and it is what makes the breakdown auditable.
    5. A **why a named exemption rather than** rejection list, in the fullest form the RAM constant
       uses: not a general loosening of the band; not folded into any of the three existing
       exemption constants; not a re-anchoring of BASE-01; not a shrinking of the fix to fit; not a
       byte-neutral offset hunted in unrelated code; and not a leonardo-only compile-time gate that
       would make the board the operator actually writes with behave differently from the others.
    6. The Evidence-Ceiling sentence, in the same kind as the third exemption's: the change this
       constant funds is **software-proven and unvalidated on silicon** — no AT28C part was involved
       in measuring it, and the figure says nothing about runtime behaviour on real hardware.
    7. A tripwire-still-armed sentence naming the specific test leg that proves the widened allowance
       is still a ceiling, pointing at the fixture family plan 15 plants.

    Extend `_merge05_flash_allowance` to read the new constant and include it in the summed allowance,
    keeping its existing shape: the band stays a separate addend and leonardo's inline zero band stays
    a plain zero. Extend that function's docstring's enumeration of the stacked exemptions, and extend
    `compare_avr_policy_merge05`'s printed allowance expression so it carries a fourth term.

    Do not touch `_merge05_ram_allowance` or the RAM constant. `D-153-01` chose a RAM-neutral form
    precisely so this plan would not have to, and Task 1 verified the RAM delta is zero.

    Then run the band-mode gate against BASE-01, naming BASE-01 explicitly, for all three targets.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter; grep -n 'MERGE05_ERASE_STANDALONE_EXEMPTION_BYTES' scripts/check_size_baseline.py</automated>
    <automated>cd /workspaces/firestarter; grep -c 'software-proven and unvalidated on silicon' scripts/check_size_baseline.py</automated>
    <automated>cd /workspaces/firestarter; grep -c 'MERGE05_PAGE_SIZE_SEAM_RAM_EXEMPTION_BYTES = 2' scripts/check_size_baseline.py</automated>
    <automated>cd /workspaces/firestarter; git diff -- scripts/check_size_baseline.py | grep -c '^-.*MERGE05_UNO_CLASS_FLASH_BAND'</automated>
    <automated>cd /workspaces/firestarter; git diff --stat -- scripts/baseline/size_baseline_base01.json</automated>
    <automated>cd /workspaces/firestarter; python3 -m pytest tests/test_check_size_baseline.py -o addopts="" -q 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter; python3 -c "import sys; sys.path.insert(0,'scripts'); import check_size_baseline as c; print([n for n in dir(c) if n.startswith('MERGE05')]); print('leonardo', c._merge05_flash_allowance('leonardo')); print('uno', c._merge05_flash_allowance('uno')); print('ram', c._merge05_ram_allowance('leonardo'))"</automated>
  </verify>
  <acceptance_criteria>
    - `MERGE05_ERASE_STANDALONE_EXEMPTION_BYTES` exists with the value Task 1 measured, and the
      summary states the arithmetic that produced it — or, if the measured difference was not
      positive, no constant was added and the summary states that instead.
    - Its comment carries all seven elements, and the per-commit breakdown itemises the two deletions
      separately from the additions, with SHAs, recording any zero-byte item explicitly.
    - The rejection list names all six rejected alternatives, including byte-neutral offsetting and
      leonardo-only compile-time gating.
    - `grep -c 'software-proven and unvalidated on silicon' scripts/check_size_baseline.py` returns at
      least `1`.
    - The RAM constant is byte-unchanged at 2 and `_merge05_ram_allowance` is untouched.
    - `git diff` shows no removed band-literal line: no band was widened.
    - `git diff --stat -- scripts/baseline/size_baseline_base01.json` is empty: BASE-01 was not
      re-anchored.
    - `_merge05_flash_allowance` returns a sum that includes the new constant for both the leonardo
      and the uno-class shapes, verified by the importable check, and its docstring enumerates four
      exemptions.
    - `compare_avr_policy_merge05`'s printed allowance expression carries a fourth term.
    - `pytest tests/test_check_size_baseline.py` result is recorded. Legs that assert at the old
      allowance are expected to fail here; plan 15 re-plants them. The summary must name exactly which
      legs failed, so plan 15's scope is derived from an observed list rather than a guess.
  </acceptance_criteria>
  <done>
    The measured growth is funded by a fourth adjudicated exemption with an auditable per-commit
    breakdown, and every alternative is rejected on the record.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-record `size_baseline.json` — AVR figures, native counts, three MERGE-05 clauses and metadata</name>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` in full — `meta` (every key),
      `avr_targets`, `native_envs`, `envs_agree`, `envs_agree_note` and `warnings`
    - the three per-target `merge05_clause` strings under `meta.deltas_vs_base01` — read all three;
      each is a single extended string with a fixed set of beats
    - `.planning/phases/153-write-path-erase-policy/153-PATTERNS.md` — the
      `size_baseline.json` section listing the mandatory beats of a `merge05_clause`
    - `firestarter/scripts/check_size_baseline.py` lines 1-60 — the default mode's requirement of byte
      identity over the AVR figures **and** every native fact
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — Task 1's post-change measured
      position
  </read_first>
  <files>firestarter/scripts/baseline/size_baseline.json</files>
  <action>
    Update the baseline in one revision. All of the following move together, because the default mode
    demands byte identity over the AVR figures **and** every native fact — updating only the AVR block
    leaves the gate red after a perfect measurement.

    - `avr_targets`: the cold flash used, flash total, flash free, RAM used, RAM total and RAM free for
      all three targets, from Task 1's logs.
    - `native_envs`: the measured `cases`, `succeeded`, `suites` and `all_passed` for `native` and
      `native_nodevtools`. Leave `native_pinmap_provisional` alone unless it actually moved.
    - `envs_agree` and `envs_agree_note`: confirm the two environments still agree and that the note's
      existing exclusion of the provisional environment still reads correctly.
    - `warnings`: re-record whatever the build-warnings checker measured, keeping the watermark's
      less-than-or-equal semantics intact. Do not raise a watermark to make a run pass; if a new
      warning class appeared, stop and report it.
    - the three `merge05_clause` strings under `meta.deltas_vs_base01`: extend each in place, keeping
      every existing beat and adding the fourth. Each must carry: the adjudicated-and-admitted opening
      naming all four contributing phases; the printed allowance expression with its fourth term; the
      RAM expression unchanged as exact equality with its single 2 B exemption; a
      **what was not changed** beat reciting BASE-01's byte-unchanged figures for all three targets
      and both dimensions, and stating that no band literal was touched, no fix was shrunk and the
      archived milestone requirement was not edited; a **tripwire still armed** beat naming plan 15's
      new fixture family and stating that each planted fixture is re-derived from the new allowance
      plus one and **observed** to fail rather than merely asserted to fail; and the Caterina
      paragraph as a distinct figure, with the sentence that this clause and that budget are never the
      same number and must not be confused.
    - `meta`: add a superseding entry to `roadmap_cross_check` naming this phase, update `generated`,
      `phase`, `firmware_tree_sha` and `host_app_tree_sha`, extend `supersedes` and `consumed_by` as
      their existing conventions require, and leave `warm_vs_cold_correction` and
      `flash_ceiling_move_260820_a7w` intact — both are still true and still load-bearing.

    Then run both gate modes. Default mode must be green for all three targets and both native
    environments. Band mode, with BASE-01 named explicitly, must be green for all three targets and
    must print an allowance expression carrying four flash terms and the unchanged RAM expression.

    Do not touch `size_baseline_base01.json`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter; python3 -c "import json; d=json.load(open('scripts/baseline/size_baseline.json')); print(json.dumps(d['avr_targets'], indent=1)); print(json.dumps(d['native_envs'], indent=1)); print('envs_agree', d['envs_agree'])"</automated>
    <automated>cd /workspaces/firestarter; python3 -c "import json; d=json.load(open('scripts/baseline/size_baseline.json'))['meta']['deltas_vs_base01']; [print(k, '::', v['merge05_clause'][:400], '\n') for k,v in d.items()]"</automated>
    <automated>cd /workspaces/firestarter; python3 -c "import json; d=json.load(open('scripts/baseline/size_baseline.json'))['meta']['deltas_vs_base01']; [print(k, 'has_caterina', '28672' in v['merge05_clause'], 'has_tripwire', 'TRIPWIRE' in v['merge05_clause'].upper(), 'has_notchanged', 'NOT CHANGED' in v['merge05_clause'].upper()) for k,v in d.items()]"</automated>
    <automated>cd /workspaces/firestarter; git diff --stat -- scripts/baseline/size_baseline_base01.json</automated>
    <automated>cd /workspaces/firestarter; python3 scripts/check_build_warnings.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter; pio test -e native 2>&1 | tail -6</automated>
    <automated>cd /workspaces/firestarter; pio test -e native_nodevtools 2>&1 | tail -6</automated>
    <automated>cd /workspaces/firestarter; python3 scripts/check_erase_no_vpp.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `avr_targets` matches Task 1's cold figures exactly for all three targets, and `native_envs`
      matches the measured counts for both environments, with `envs_agree` still true.
    - Default mode exits 0 for all three AVR logs and both native logs. The exact commands and their
      output are transcribed in the summary — the gate runs in no CI leg, so this record is the only
      evidence.
    - Band mode, with BASE-01 named explicitly, exits 0 for all three targets, and its printed flash
      allowance expression carries four terms while the RAM expression is unchanged as exact equality
      with a single 2 B exemption.
    - All three `merge05_clause` strings contain the Caterina figure, a tripwire-still-armed beat and a
      what-was-not-changed beat, verified by the presence checks, and each recites BASE-01's
      byte-unchanged figures for all three targets in both dimensions.
    - Each clause's tripwire beat names plan 15's new fixture family and states that each planted
      fixture is re-derived from the new allowance plus one and observed to fail.
    - `git diff --stat -- scripts/baseline/size_baseline_base01.json` is empty.
    - The build-warnings checker exits 0 and no watermark was raised; if a new warning class appeared,
      the summary reports it as a blocker instead.
    - Both native environments still pass, and `check_erase_no_vpp.py` still exits 0.
  </acceptance_criteria>
  <done>
    Both gate modes are green against a single, coherent, cold-measured baseline revision, with the
    two size figures recorded separately and every rejected alternative on the record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured figures → adjudicated allowance | An exemption sized from an estimate is an unbounded allowance. |
| linker output → leonardo USB bootloader | Past 28672 B the bootloader is overwritten and the board needs an ISP to recover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-76 | Denial of service | leonardo bricked by linking over the Caterina bootloader | critical | mitigate | The Caterina headroom is measured and recorded as a figure distinct from the MERGE-05 clause, in the decision record and in all three baseline clauses, with an explicit sentence that the two are never the same number. |
| T-153-77 | Tampering (laundering) | BASE-01 re-anchored, a band widened, or a fix shrunk to make the gate green | high | mitigate | The exemption comment's rejection list names all six alternatives; criteria assert BASE-01 is byte-unchanged and no band literal was removed. |
| T-153-78 | Spoofing (false assurance) | an exemption sized from an estimate or rounded up for headroom | high | mitigate | The value comes from Task 1's cold arithmetic, the action forbids rounding, and a non-positive measured difference means no constant at all. |
| T-153-79 | Spoofing | a warm build's figure transcribed as cold | medium | mitigate | Build directory deletion per environment is mandatory and the deleted paths are named in the summary; the baseline's own warm/cold correction note is required reading. |
| T-153-80 | Spoofing (false green) | the default gate left red because only the AVR block was updated | medium | mitigate | The action requires the AVR block, the native block and the warnings block to move in the same revision, and criteria assert default mode exits 0 for both native logs. |
| T-153-81 | Tampering | a warning watermark raised to absorb a new warning class | medium | mitigate | The action forbids raising it and requires a stop-and-report if a new class appears; the checker's exit code is a criterion. |
| T-153-82 | Tampering | RAM growth absorbed silently against a fully-consumed exemption | high | mitigate | Task 1 records the RAM delta per target against both bases and treats any non-zero value as a blocker; the RAM constant and its allowance function must be byte-unchanged. |
| T-153-83 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- Three cold builds, both native environments and the build-warnings checker were run, and every
  figure is transcribed.
- Default mode exits 0 for all three AVR logs and both native logs; band mode against BASE-01 exits 0
  for all three targets with a four-term flash allowance.
- BASE-01 is byte-unchanged; no band literal was removed; the RAM constant and allowance are
  untouched.
- The Caterina figure appears separately in the decision record and in all three baseline clauses.
- The failing legs of the size-baseline test suite are enumerated for plan 15.
</verification>

<success_criteria>
- ERASE-08 is satisfied by measurement on all three targets against a verified pre-change base.
- The growth is funded by a fourth adjudicated, SHA-attributed, single-consumer exemption with an
  auditable per-commit breakdown.
- The RAM clause is untouched because the erase was designed RAM-neutral, and that is verified.
- The two size figures are never conflated anywhere in the record.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| `MERGE05_ERASE_STANDALONE_EXEMPTION_BYTES` | new module constant | `scripts/check_size_baseline.py`; FLASH only; value measured, not estimated |
| extended `_merge05_flash_allowance` | source change | fourth addend; leonardo's inline zero band unchanged |
| extended `compare_avr_policy_merge05` allowance expression | output change | four flash terms; RAM expression unchanged |
| `size_baseline.json` `avr_targets` revision | new baseline values | three targets, cold |
| `size_baseline.json` `native_envs` revision | new baseline values | both environments; suite count still 17 |
| three extended `merge05_clause` strings | baseline metadata | fourth term, what-was-not-changed, tripwire-armed, Caterina paragraph |
| `meta.roadmap_cross_check` superseding entry | baseline metadata | names this phase |
| `## Post-change measured position (cold)` | phase record section | `153-DECISIONS.md` |

<output>
Create `.planning/phases/153-write-path-erase-policy/153-14-SUMMARY.md` when done.
</output>
