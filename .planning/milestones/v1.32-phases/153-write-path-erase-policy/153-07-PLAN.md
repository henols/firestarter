---
phase: 153-write-path-erase-policy
plan: 07
type: execute
wave: 7
depends_on: ["153-03", "153-06"]
commits_land_in: [firestarter_app]
files_modified:
  - firestarter_app/firestarter/database.py
autonomous: true
requirements: [ERASE-03, ERASE-07]

must_haves:
  truths:
    - "All 84 algorithm-13 rows carry FLAG_CAN_ERASE on the wire; algorithm-5 rows and UV-EPROM rows still do not."
    - "The Phase 121 D-12 comment block no longer asserts either of its two now-false claims, and the still-valid algorithm-5 hardware-hazard rationale is preserved verbatim in substance."
    - "The correction is written in the project's mechanism-corrected / intent-satisfied reversal-record voice, as the fourth recorded reversal in this chain — not as a failure."
    - "Exactly the eight measured downstream test failures appear and no others, each named in advance, so a ninth failure is a genuine surprise rather than noise."
  artifacts:
    - firestarter_app/firestarter/database.py
  key_links:
    - "The edit is ONE character-level change on the exclusion tuple at database.py line 620 — `algo not in (5, 13)` becomes `algo not in (5,)`. Line 621 is the `simple_flags |=` body, not the edit site; the roadmap and REQUIREMENTS both cite 621 and both are off by one."
    - "Algorithm 5 STAYS excluded. Its exclusion is a live hardware-damage hazard (setting the flag routes flash4_write_init into a 12 V bulk erase on a 5 V-only part), which is a DIFFERENT and still-valid reason from algorithm 13's retired one. Dropping 5 as well would be a chip-destroying regression."
    - "This one-line change rewrites the `dev test` plan shape on all 84 algorithm-13 rows: erase flips from NA to supported-destructive and blank-check moves position. That is intended and is funded by plans 08-12; it must not be discovered."
    - "chip_database.json is GENERATED by tools/build_db.py and must never be hand-edited. No DB field changes are needed here — the flag is derived at conversion time and no DB entry carries a `flags` key."
---

<objective>
Restore `FLAG_CAN_ERASE` for algorithm 13 and correct the comment block that justified clearing it.

Purpose: ERASE-03's host half and ERASE-07. Phase 121 D-12 cleared the flag on the stated grounds
that the firmware had no erase operation at all, making the advertisement a false capability
statement. That premise is now disproven twice over: the capability was always real in the silicon
and in `infoic.xml` — what was false was only that *firestarter* could perform it — and as of plan
03 firestarter can. D-12 made the host claim less; this phase makes the firmware do more, which is
the honest direction.

Output: one tuple edit and one rewritten comment block. Nothing else in the file moves.

**Requirement flips owned by this plan: ERASE-07.** ERASE-03 flips once plans 08-12 land the
downstream inversions and the exhaustive assertion.

Cites `D-153-05` (restoring the flag must not make `write` start erasing implicitly, and must not
extend `write`'s auto-set path to `erase`).
Locked decisions honoured: **L-04** (the `database.py` comment is one of the four now-false claim
sites and is in scope) and **L-06** (`erase` stays out of `write`'s auto-set path).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-PATTERNS.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
@firestarter_app/firestarter/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop 13 from the exclusion tuple and enumerate the measured downstream failures</name>
  <read_first>
    - `firestarter_app/firestarter/database.py` lines 576-624 — the whole comment block and the
      `convert_to_programmer` flag derivation. Confirm the edit site line number in the file before
      editing: it is the `if algo not in (...)` line, not the `simple_flags |=` line below it.
    - `firestarter_app/firestarter/constants.py` around `FLAG_CAN_ERASE` — the bit value, so the
      summary can state it
    - `firestarter/src/eprom_operations.cpp` lines 30-45 — `eprom_erase`'s `FLAG_CAN_ERASE`
      precondition: the firmware layer that reads this bit and that plan 03 made meaningful
    - `.planning/phases/153-write-path-erase-policy/153-RESEARCH.md` §"Common Pitfalls" Pitfall 4 —
      the three coupled `chip_test.py` sites and the 84-row plan-shape ripple
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — `D-153-05`
  </read_first>
  <files>firestarter_app/firestarter/database.py</files>
  <action>
    Change the algorithm exclusion tuple in `convert_to_programmer` so that only algorithm 5 remains
    excluded: the membership test currently naming both 5 and 13 keeps 5 and drops 13. This is the
    entire functional change in this plan.

    Do not touch anything else on that code path: the `electrical-type` membership test above it
    stays, the `simple_flags |=` line stays, and the `programmer_data["flags"]` assignment stays.
    Do not edit `chip_database.json` — it is generated by `tools/build_db.py`, no entry carries a
    `flags` key, and no DB field change is needed.

    Then measure and record. Run the full host suite and confirm that **exactly** these eight tests
    fail, and no others. They were measured during planning against this exact one-line change:

    1. `tests/test_chip_test.py::test_devtest01_0x0d_sweep_erase_is_na_and_erase_eprom_never_called`
    2. `tests/test_chip_test.py::test_count_applicable_sdp_gated_allow_chip_ratio_drops`
    3. `tests/test_chip_test.py::test_count_applicable_sdp_banner_row_renders_the_dropped_ratio`
    4. `tests/test_chip_test_blank_check_order.py::test_at28c256_blank_check_is_na_with_family_fact_reason`
    5. `tests/test_chip_test_sdp_leg.py::test_baseline_gate_closes_dead_write_path_allow_chip_full_leg`
    6. `tests/test_database_conversion.py::test_convert_at28c256_flash_eeprom_flag_can_erase_cleared`
    7. `tests/test_eprom_operations.py::TestSdpOperationsWireShape::test_sdp_command_flags_do_not_carry_the_db_can_erase_bit`
    8. `tests/test_wire_dict_equivalence.py::test_live_capture_matches_golden_plus_the_149_deltas`

    Record in the summary, for each, the file that owns its fix and the plan that owns it: 6 and 7
    go to plan 09, 8 goes to plan 08, 1-3 and 5 go to plan 10, 4 goes to plan 10. If the observed
    failure set differs from this list in either direction, stop and report it: a missing failure
    means an oracle is weaker than planning measured, and an extra failure is a coupling nobody has
    budgeted.

    Also record these measured post-change figures, which plan 10 needs and must not re-derive by
    guesswork: for AT28C256 at `write_scope="full"` under the gated-allow operator double,
    `count_applicable` now reports `m_applicable = 10` and `n_ran = 6`, up from 9 and 5. And record
    the wire-dict delta shape plan 08 needs: exactly **84** records change, the changed field is
    `flags` and nothing else, and the value moves from `0` to `2` in every one of them.

    Use `-o addopts=""` for any command whose count line you intend to read; `addopts` is `-ra -q`
    and doubling `-q` suppresses the count line.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; grep -n 'algo not in' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -c "from firestarter.database import EpromDatabase; from firestarter.constants import FLAG_CAN_ERASE; db=EpromDatabase(skip_local_override=True); print('AT28C256', hex(db.convert_to_programmer(db.get_eprom('AT28C256'))['flags'])); print('W29C040', hex(db.convert_to_programmer(db.get_eprom('W29C040'))['flags'])); print('M27C512', hex(db.convert_to_programmer(db.get_eprom('M27C512'))['flags'])); print('FLAG_CAN_ERASE', hex(FLAG_CAN_ERASE))"</automated>
    <automated>cd /workspaces/firestarter_app; git diff --stat -- firestarter/data/chip_database.json</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -20</automated>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_mypy_watermark.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'algo not in' firestarter/database.py` shows a single-element tuple containing only
      `5`.
    - `AT28C256` converts to a `flags` value with the erase bit SET; `W29C040` (algorithm 5) and
      `M27C512` (UV-EPROM) both convert to a `flags` value with it CLEAR.
    - `git diff --stat -- firestarter/data/chip_database.json` is empty: the generated database was
      not hand-edited.
    - The full host suite reports exactly the eight named failures and no others. The summary lists
      them, states which plan owns each, and explicitly confirms the count is eight.
    - The summary records `m_applicable = 10` and `n_ran = 6` as the measured post-change figures
      for the AT28C256 gated-allow leg, alongside the pre-change 9 and 5.
    - The summary records the wire-dict delta shape: 84 records, `flags` only, `0` to `2`.
    - `ruff check` and `ruff format --check` both pass; the mypy watermark check exits 0.
  </acceptance_criteria>
  <done>
    The wire flag is restored, the generated database is untouched, and every downstream failure is
    named and assigned before anyone tries to fix one.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Phase 121 D-12 comment block (ERASE-07)</name>
  <read_first>
    - `firestarter_app/firestarter/database.py` lines 576-616 — the whole comment block, read in
      full. Identify three parts: the algorithm-5 rationale that STAYS, the algorithm-13 paragraph
      that is now false, and the REVERSAL RECORD paragraph whose parenthetical claim about the
      firmware never reading the flag is also now false.
    - `.planning/phases/153-write-path-erase-policy/153-PATTERNS.md` — the
      `database.py` section: the algorithm-5 rationale quoted verbatim as the voice to match, and
      the reversal-record voice pattern
    - `firestarter/src/proms/eeprom_28c.cpp` — the `case CMD_ERASE:` arm plan 03 added, and
      `firestarter/src/eprom_operations.cpp` lines 30-45 — `eprom_erase`'s `FLAG_CAN_ERASE`
      precondition. These are the two code facts the corrected text must cite.
    - `firestarter/src/proms/eeprom_28c.cpp` around the amended D-05 comment — the same
      mechanism-corrected / intent-satisfied framing, so the two repositories' records agree in
      voice as well as in content
  </read_first>
  <files>firestarter_app/firestarter/database.py</files>
  <action>
    Rewrite the algorithm-13 half of the comment block. Two specific claims must stop being made,
    and the criteria below negative-grep for their exact wording:
    - the claim that the firmware handler has no erase operation, which made the advertisement a
      false capability statement;
    - the parenthetical in the REVERSAL RECORD paragraph asserting that the `0x0D` firmware path
      never reads the capability flag.

<!-- planner-discipline-allow: has no erase operation at -->
<!-- planner-discipline-allow: never reads FLAG_CAN_ERASE -->

    The replacement must state three things, each with its code citation:
    (a) Phase 153 (ERASE-03/ERASE-04) implements the AN-0544B software six-byte chip erase in
        `configure_eeprom28c`, so the capability statement is now **true**;
    (b) the `0x0D` path **does** read the flag — at `eprom_operations.cpp`'s `eprom_erase`
        precondition, which is the standalone `erase` command's refusal gate — so the flag is no
        longer firmware-inert on this protocol;
    (c) per `D-153-05`, restoring the flag deliberately does **not** make `write` erase implicitly:
        no `FLAG_CAN_ERASE`-gated erase block was added to `eeprom28c_write_init`, and `erase` was
        not added to `write`'s `FLAG_SKIP_SDP_UNLOCK` auto-set path.

    Preserve the algorithm-5 rationale **in substance, verbatim where possible**: flash4 auto-erases
    per page, setting the flag for `0x05` routes the firmware write-init into an erase that asserts
    a VPP regulator bit on a 5 V-only part, that is a hardware-damage hazard, and the scope is
    algorithm 5 only. This is a live argument, not a retired one, and it is the reason the tuple
    keeps 5.

    Write the new algorithm-13 paragraph as a **REVERSAL RECORD**, in the same voice as the
    paragraph it supersedes: this is the fourth recorded reversal in the chain (119 D-18, 120 D-20,
    121 D-12, now 153). Frame it as mechanism-corrected and intent-satisfied, never as failed —
    D-12's *policy* was right given its premise, its premise has changed, and the honest resolution
    was to make the firmware do more rather than to make the host claim less. Also carry forward, in
    corrected form, D-12's own blast-radius note: no `chip_database.json` entry carries a `flags`
    key so `diff_db.py` identity cannot break; the other host reader of this bit is DEBUG-only
    logging; and the one benign behavioural delta this reversal produces is that `firestarter erase`
    on a `0x0D` part now performs a real erase rather than being refused one layer earlier.

    Also record, briefly, the plan-shape consequence so the next reader is not surprised by it:
    restoring the flag changes `derive_plan`'s output on all 84 algorithm-13 rows — erase becomes a
    supported destructive step and blank-check moves to sit after it, where it doubles as the
    erase's oracle.

    Keep the block's overall structure and indentation. Do not reflow unrelated lines: a large
    whitespace diff here would bury the substantive change.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; grep -c 'has no erase operation at' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'never reads FLAG_CAN_ERASE' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'hardware-damage hazard' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'REVERSAL RECORD' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -cE 'ERASE-03|Phase 153' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'eprom_operations.cpp' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'D-153-05' firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; git diff --numstat -- firestarter/database.py</automated>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/ && ruff format --check firestarter/</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_database_conversion.py -o addopts="" -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'has no erase operation at' firestarter/database.py` returns `0`.
    - `grep -c 'never reads FLAG_CAN_ERASE' firestarter/database.py` returns `0`.
    - `grep -c 'hardware-damage hazard'` returns at least `1` — the algorithm-5 rationale survives.
    - `grep -c 'REVERSAL RECORD'` returns at least `1`, and the paragraph identifies this as the
      fourth reversal in the chain naming 119 D-18, 120 D-20 and 121 D-12.
    - The block cites `eprom_operations.cpp` as the site that reads the flag, and names Phase 153
      and ERASE-03.
    - The block cites `D-153-05` and states that `write` does not erase implicitly and that
      `erase` was not added to the auto-set path.
    - `git diff --numstat -- firestarter/database.py` shows a change confined to the comment block
      plus the one tuple line from Task 1 — no unrelated reflow.
    - `ruff check` and `ruff format --check` pass.
    - `pytest tests/test_database_conversion.py` still shows exactly the one expected failure
      (`test_convert_at28c256_flash_eeprom_flag_can_erase_cleared`), which plan 09 inverts, and no
      new failures introduced by the comment edit.
  </acceptance_criteria>
  <done>
    The comment no longer makes either false claim, the still-valid hazard rationale is intact, and
    the reversal is recorded in the project's own voice with its code citations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated database → wire dict | `convert_to_programmer` composes what crosses the serial link. |
| host capability advertisement → firmware precondition | Both layers gate on this bit; they must agree or the command is refused one layer earlier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-34 | Tampering (physical destruction) | algorithm 5 accidentally dropped from the exclusion alongside 13 | critical | mitigate | The criterion asserts `W29C040` still converts with the bit CLEAR, and the comment must retain the hardware-hazard rationale with a grep asserting its presence. |
| T-153-35 | Repudiation / false capability | the comment continuing to justify a cleared flag that is no longer cleared | medium | mitigate | Two negative greps assert both false claims are gone; positive greps assert the corrected citations are present. |
| T-153-36 | Spoofing (silent scope creep) | `write` starting to erase implicitly because the flag is now set | high | mitigate | `D-153-05` forbids adding an erase-on-write block; plan 02's replacement comment restates it at the firmware site; this comment restates it at the host site. |
| T-153-37 | Tampering | the generated `chip_database.json` hand-edited to carry a flags key | medium | mitigate | A criterion asserts `git diff --stat` over that file is empty; the project skill rule forbids hand edits and the generator is the only edit site. |
| T-153-38 | Spoofing (unbudgeted coupling) | a ninth downstream failure mistaken for noise | medium | mitigate | The eight failures are enumerated in advance from a measured run; any deviation in either direction is a stop-and-report condition. |
| T-153-39 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- The exclusion tuple contains only `5`; AT28C256 gains the bit, W29C040 and M27C512 do not.
- `chip_database.json` is byte-unchanged.
- Both ERASE-07 negative greps return 0 and the algorithm-5 rationale is still present.
- Exactly eight host tests fail, each named and assigned to a later plan.
- `ruff check`, `ruff format --check` and the mypy watermark all pass.

**Repo state note, deliberate:** the host suite is RED between this plan and plan 12. Those eight
failures are the observed-RED evidence for the inversions in plans 08-10, and no plan in waves 7-9
may cite a full green host suite. Plan 16 is where the sweep goes green.
</verification>

<success_criteria>
- All 84 algorithm-13 rows advertise a capability the firmware can now actually perform.
- Algorithm 5 remains excluded for its own, still-valid, hardware reason.
- ERASE-07's comment correction is done in the same change that makes it necessary, in the
  project's reversal-record voice.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| `database.py` exclusion tuple change | source change | `algo not in (5, 13)` becomes `algo not in (5,)` |
| rewritten Phase 121 D-12 comment block | in-repo record correction | fourth recorded reversal in the chain |
| wire-value change | behaviour | 84 records' `flags` move from `0` to `2`; no other field on any of the 746 rows changes |
| CLI-visible behaviour change | behaviour | `firestarter erase <0x0D chip>` now runs; `dev test` plans an erase step on all 84 algorithm-13 rows and moves blank-check after it |
| measured figures for later plans | record | `m_applicable = 10`, `n_ran = 6`; 84 wire-dict rows, `flags` only, `0` to `2` |

<output>
Create `.planning/phases/153-write-path-erase-policy/153-07-SUMMARY.md` when done.
</output>
