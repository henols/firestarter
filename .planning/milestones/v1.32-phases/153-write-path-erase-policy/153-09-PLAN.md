---
phase: 153-write-path-erase-policy
plan: 09
type: execute
wave: 8
depends_on: ["153-07"]
commits_land_in: [firestarter_app]
files_modified:
  - firestarter_app/tests/test_database_conversion.py
  - firestarter_app/tests/test_eprom_operations.py
  - firestarter_app/tests/test_val_wire_eeprom28c.py
autonomous: true
requirements: [ERASE-03]

must_haves:
  truths:
    - "The conversion test asserts AT28C256 carries FLAG_CAN_ERASE, and its name states that outcome rather than the old one."
    - "The two negative controls — an algorithm-5 Flash/EEPROM part and a UV-EPROM part — keep their assertions, names and docstrings unedited and stay green, which is the scope proof that the flag did not bleed."
    - "The SDP-command wire-shape leg asserts the composed command flags now CARRY the erase bit, and its name and docstring state why that reversed."
    - "The tier-2 wire round-trip suite pins the erase bit on the family's representative chip, so the family's own wire contract records the capability rather than leaving it implicit."
  artifacts:
    - firestarter_app/tests/test_database_conversion.py
    - firestarter_app/tests/test_eprom_operations.py
    - firestarter_app/tests/test_val_wire_eeprom28c.py
  key_links:
    - "Each inversion must KEEP a positive assertion. Deleting an assertion because it became false leaves the opposite claim untested, and both of these legs exist specifically to catch a future reader flipping the bit back."
    - "The two negative controls in test_database_conversion.py are the only thing proving the flag did not bleed to algorithm 5 (a 12 V-on-a-5V-part hazard) or to UV-EPROM. Their assertions, names and docstrings must be left unedited, and their continued green is a required criterion, not incidental."
    - "This plan runs in the same wave as plans 08 and 13 and touches disjoint files. Per-task verification is scoped to the touched modules; no plan in this wave may run the full host suite, which is deliberately RED until plan 12, and both repositories carry a test asserting whole-repository porcelain."
---

<objective>
Invert the two host assertions that pin the erase bit CLEAR, and add the positive wire-contract
assertion the family's tier-2 suite is missing.

Purpose: ERASE-03's host proof. Two committed tests assert the old value — one at the conversion
layer and one at the composed-command wire layer — and both were measured failing in plan 07. Each
must be inverted while keeping a positive assertion, because each exists to catch the bit being
flipped back.

Output: two inversions with reversal-record docstrings, two untouched negative controls, and one new
positive assertion in the tier-2 wire suite.

**Requirement flips owned by this plan: none.** It advances ERASE-03.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-PATTERNS.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
@firestarter_app/firestarter/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert and rename the conversion test, leaving both negative controls untouched</name>
  <read_first>
    - `firestarter_app/tests/test_database_conversion.py` lines 78-135 — all three tests in the
      cluster: the UV-EPROM negative control, the AT28C256 test being inverted with its full
      REVERSAL RECORD docstring, and the algorithm-5 negative control with its own FIX-01a rationale
    - the same file's earlier positive-assert example (a bare truthiness assert on the masked flag)
      — the shape the inverted assertion takes
    - `firestarter_app/firestarter/database.py` — the comment block plan 07 rewrote, so the
      docstring's reversal narrative matches the source's
    - `.planning/phases/153-write-path-erase-policy/153-PATTERNS.md` — the
      `test_database_conversion.py` section, including the instruction to rename the function
      because its name states the outcome
  </read_first>
  <files>firestarter_app/tests/test_database_conversion.py</files>
  <action>
    Rename the AT28C256 test so its name states the new outcome — the old name asserts the bit is
    cleared, which is now false — and invert its single assertion from an equality against zero to a
    positive truthiness check on the masked flag. Keep it a positive assertion; do not delete it.

    Extend the docstring rather than replacing it. Keep the existing Phase 121 D-12 narrative intact
    as history and append the fourth reversal, in the same voice: D-12's policy was correct given its
    premise — an inert-but-false capability advertisement is still false — and this phase changed the
    premise rather than the policy, by implementing the AN-0544B software chip erase in
    `configure_eeprom28c` and wiring `CMD_ERASE` to it. State that the flag is therefore no longer
    firmware-inert on this protocol: it is read at `eprom_operations.cpp`'s `eprom_erase`
    precondition as the standalone erase command's refusal gate. Record it as
    mechanism-corrected and intent-satisfied, never as failed.

    Leave both negative controls unedited: the UV-EPROM test and the algorithm-5
    Flash/EEPROM test. Do not touch their bodies, names or docstrings. The criterion below is
    scoped to their assertions and docstrings, checked as a same-commit diff — not a claim that the
    file is byte-frozen against future formatter or guard passes. Cite them in this plan's
    summary as the scope proof — they are the only thing demonstrating that the flag did not bleed to
    a 12 V-erase-routing algorithm or to a family with no electrical erase at all.

    Run only this module, with `-o addopts=""`. Do not run the full host suite in this task: it is
    deliberately RED until plan 12.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_convert_at28c256_flash_eeprom_flag_can_erase_cleared' tests/test_database_conversion.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -n 'def test_convert_at28c256' tests/test_database_conversion.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'def test_convert_uv_eprom_no_flag_can_erase' tests/test_database_conversion.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'def test_convert_w29c040_no_flag_can_erase' tests/test_database_conversion.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'eprom_operations.cpp' tests/test_database_conversion.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_database_conversion.py -o addopts="" -q 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_database_conversion.py && ruff format --check tests/test_database_conversion.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c` for the old function name returns `0`; a renamed `test_convert_at28c256_*` function
      exists whose name states that the flag is SET.
    - The renamed test's assertion is positive (a truthiness check on the masked flag), not an
      equality against a new constant, and it is not deleted.
    - Both negative-control function names are still present, and `git diff` shows **zero** hunks
      inside either of their bodies or docstrings.
    - The docstring retains the Phase 121 D-12 narrative and appends the fourth reversal, cites
      `eprom_operations.cpp`, and uses the mechanism-corrected / intent-satisfied framing.
    - `pytest tests/test_database_conversion.py -o addopts="" -q` is fully green, and the summary
      records the module's test count and states that the two negative controls passed.
    - `ruff check` and `ruff format --check` pass on the file.
  </acceptance_criteria>
  <done>
    The conversion layer asserts the new contract by name and by assertion, with the anti-bleed
    controls provably untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Invert the SDP-command wire-shape leg in `test_eprom_operations.py`</name>
  <read_first>
    - `firestarter_app/tests/test_eprom_operations.py` — the whole of
      `TestSdpOperationsWireShape::test_sdp_command_flags_do_not_carry_the_db_can_erase_bit`,
      including its full REVERSAL RECORD docstring and the `_fake_find_and_connect` capture idiom
      that records the composed `command_dict`
    - the same class's neighbouring tests, for the fixture usage (`make_comm`, `fake_serial`) and the
      frame-feeding pattern
    - `firestarter_app/firestarter/constants.py` around the erase flag — so the assertion compares
      against the named constant rather than a bare integer
    - `firestarter_app/firestarter/database.py` — the rewritten comment block, so the docstring
      narrative agrees with the source
  </read_first>
  <files>firestarter_app/tests/test_eprom_operations.py</files>
  <action>
    Rename the test so its name states the new outcome — it currently says the composed flags do not
    carry the bit — and invert its assertion from an equality against zero to a positive check that
    the composed `command_dict` flags carry the erase bit, expressed against the named constant
    imported from `firestarter.constants` rather than a bare integer literal. Keep the assertion
    positive; do not delete it.

    Extend the docstring rather than replacing it, in the established voice: keep the Phase 121 D-12
    history, then record that Phase 153 restores the bit at the source because
    `configure_eeprom28c` now implements the AN-0544B software chip erase and `CMD_ERASE` is wired to
    it — so the wire flags for a `0x0D` part carry the bit again, and this leg exists to catch a
    future reader clearing it a second time. State explicitly, per `D-153-05`, that carrying the bit
    on an SDP command frame does **not** mean an SDP command erases anything: the bit is a capability
    advertisement read only by the firmware's standalone-erase precondition, and no erase-on-write
    block was added to the `0x0D` write path.

    Change nothing else in the class. Run only this module, with `-o addopts=""`, plus the class
    filter if the module is slow.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_sdp_command_flags_do_not_carry_the_db_can_erase_bit' tests/test_eprom_operations.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -n 'def test_sdp_command_flags' tests/test_eprom_operations.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'D-153-05' tests/test_eprom_operations.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_eprom_operations.py -k SdpOperationsWireShape -o addopts="" -q 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_eprom_operations.py -o addopts="" -q 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_eprom_operations.py && ruff format --check tests/test_eprom_operations.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c` for the old test name returns `0`; a renamed `test_sdp_command_flags_*` method exists
      whose name states that the flags DO carry the bit.
    - The assertion compares the captured `command_dict` flags against the named constant imported
      from `firestarter.constants`, with no bare integer literal for the bit value.
    - `grep -c 'D-153-05'` returns at least `1`, and the docstring states that carrying the bit on an
      SDP frame does not mean an SDP command erases.
    - `pytest tests/test_eprom_operations.py -o addopts="" -q` is fully green and the summary records
      the module's test count.
    - `ruff check` and `ruff format --check` pass on the file.
  </acceptance_criteria>
  <done>
    The composed-command wire shape asserts the restored bit, and the docstring forecloses the
    misreading that the bit makes an SDP command destructive.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pin the erase bit in the tier-2 EEPROM28c wire round-trip suite</name>
  <read_first>
    - `firestarter_app/tests/test_val_wire_eeprom28c.py` in full — the spec-sourced representative
      chip, the `_EXPECTED_PROTOCOLS` and `_EXPECTED_HANDLER` constants, the four existing tests, and
      the `make_comm` / `fake_serial` no-serial-port posture the docstring describes
    - `firestarter_app/tools/validation_matrix_spec.json` — the `eeprom28c` family entry, so the
      representative chip continues to be read from the spec rather than hardcoded
    - `firestarter_app/firestarter/constants.py` around the erase flag
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — `D-153-04`, for the sector
      address disposition the new docstring records
  </read_first>
  <files>firestarter_app/tests/test_val_wire_eeprom28c.py</files>
  <action>
    This suite pins the family's wire contract — `algorithm == 13`, dispatch to
    `configure_eeprom28c` — but pins nothing about the capability flags, which is why plan 07's
    change did not make it fail. That silence is a gap: the family's own wire contract should record
    the capability the firmware now implements, so a future clearing of the bit fails here too.

    Add `test_eeprom28c_wire_dict_carries_flag_can_erase(make_comm, fake_serial)`, following the
    existing tests' shape exactly: read the representative chip from the spec (never hardcode it),
    build the wire dict through `EpromDatabase.convert_to_programmer`, and assert positively that the
    `flags` value carries the erase bit, compared against the named constant imported from
    `firestarter.constants`.

    Add a second leg, `test_eeprom28c_wire_dict_carries_no_skip_or_sector_flags`, asserting that the
    same wire dict's `flags` value carries **only** the capability bit and none of the operation-time
    skip bits — the skip flags are composed per-invocation by the CLI, not by the database. State in
    the docstring, per `D-153-04`, that the `0x0D` erase is device-global by construction and that no
    sector address participates in it, so nothing sector-related belongs in this family's wire
    contract.

    Extend the module docstring's design list to mention the new capability-flag legs and to name
    Phase 153 / ERASE-03 as their origin, keeping the existing spec-sourced and no-hand-roll notes
    intact.

    Do not modify `validation_matrix_spec.json`. Run only this module.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_eeprom28c_wire_dict_carries_flag_can_erase' tests/test_val_wire_eeprom28c.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_eeprom28c_wire_dict_carries_no_skip_or_sector_flags' tests/test_val_wire_eeprom28c.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'FLAG_CAN_ERASE' tests/test_val_wire_eeprom28c.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'D-153-04' tests/test_val_wire_eeprom28c.py</automated>
    <automated>cd /workspaces/firestarter_app; git diff --stat -- tools/validation_matrix_spec.json</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_val_wire_eeprom28c.py -o addopts="" -q 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_val_wire_eeprom28c.py && ruff format --check tests/test_val_wire_eeprom28c.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_mypy_watermark.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests exist with the specified names, each appearing exactly once.
    - The capability assertion compares against the named constant imported from
      `firestarter.constants`, and the representative chip is read from the spec, not hardcoded.
    - The second leg asserts the wire `flags` value carries only the capability bit, and its
      docstring records the `D-153-04` sector-address disposition.
    - `git diff --stat -- tools/validation_matrix_spec.json` is empty.
    - `pytest tests/test_val_wire_eeprom28c.py -o addopts="" -q` is fully green, and the summary
      records the module's test count before and after.
    - Both new legs were OBSERVED failing against a temporary local revert of plan 07's tuple edit,
      with the failure messages transcribed and the revert confirmed reverted by `git diff` on
      `firestarter/database.py` being empty. A capability assertion that could not have failed before
      the change is not a gate.
    - `ruff check`, `ruff format --check` and the mypy watermark check all pass.
  </acceptance_criteria>
  <done>
    The family's tier-2 wire contract records the restored capability, and both new legs have been
    seen to fail without it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host capability advertisement → firmware precondition | These tests are the host-side record of what the wire carries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-46 | Tampering (physical destruction) | the erase bit bleeding to algorithm 5 or UV-EPROM unnoticed | critical | mitigate | Both negative controls in `test_database_conversion.py` stay byte-unchanged and their continued green is a stated criterion, with `git diff` asserting zero hunks in their bodies. |
| T-153-47 | Repudiation | an inverted assertion deleted rather than inverted, leaving the opposite claim untested | high | mitigate | Both inversions must keep a positive assertion; criteria assert the renamed functions exist and that the assertion is a positive check against a named constant. |
| T-153-48 | Spoofing (false assurance) | a new capability assertion that could not have failed before the change | high | mitigate | Task 3 requires both new legs to be OBSERVED failing against a temporary revert of plan 07's tuple edit, with the revert confirmed removed. |
| T-153-49 | Spoofing (misreading) | the erase bit on an SDP command frame read as meaning SDP commands erase | medium | mitigate | Task 2's docstring states the bit is a capability advertisement read only by the standalone-erase precondition, citing `D-153-05`. |
| T-153-50 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- `pytest tests/test_database_conversion.py tests/test_eprom_operations.py tests/test_val_wire_eeprom28c.py -o addopts=""` is green.
- Both old test names are absent; both renamed tests keep positive assertions.
- Both negative controls have unedited assertions and docstrings, verified by `git diff` showing no
  hunk inside either function, and both are green.
- The two new tier-2 legs were observed failing against a temporary revert.
- `ruff`, `ruff format` and the mypy watermark all pass.
</verification>

<success_criteria>
- Every host assertion that pinned the old value now pins the new one, by name and by assertion.
- The anti-bleed scope proof is intact and cited.
- The family's wire contract no longer stays silent about the capability.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| renamed + inverted AT28C256 conversion test | test change | `test_database_conversion.py`; positive assertion retained |
| renamed + inverted SDP command wire-shape test | test change | `test_eprom_operations.py`; asserts against the named constant |
| `test_eeprom28c_wire_dict_carries_flag_can_erase` | new test | `test_val_wire_eeprom28c.py` |
| `test_eeprom28c_wire_dict_carries_no_skip_or_sector_flags` | new test | `test_val_wire_eeprom28c.py`; records the `D-153-04` sector disposition |

<output>
Create `.planning/phases/153-write-path-erase-policy/153-09-SUMMARY.md` when done.
</output>
