---
phase: 153-write-path-erase-policy
plan: 12
type: execute
wave: 9
depends_on: ["153-07"]
commits_land_in: [firestarter_app]
files_modified:
  - firestarter_app/tests/test_erase_flag_invariants.py
  - firestarter_app/tests/test_ic_layout.py
  - firestarter_app/tests/test_erase_blank_step_nonregression.py
autonomous: true
requirements: [ERASE-03, ERASE-05, ERASE-06]

must_haves:
  truths:
    - "Exactly 84 of the 746 database rows gain the erase capability bit, exactly the algorithm-13 rows, and no non-algorithm-13 row changed — asserted exhaustively over the whole database, not on a representative chip."
    - "`info`'s can-be-erased row and the wire capability bit AGREE for an algorithm-13 chip, asserted in both directions in one leg, and still disagree in neither direction for a UV-EPROM control."
    - "`blank` remains available as its own step: the CLI command still reaches the firmware blank-check operation, and this is asserted against the existing tests rather than reimplemented."
    - "The `write_scope=\"none\"` plan-shape change on a 0x0D chip is pinned, so a step-count drop and a locked-op gain that no committed test would otherwise notice are on the record."
  artifacts:
    - firestarter_app/tests/test_erase_flag_invariants.py
    - firestarter_app/tests/test_erase_blank_step_nonregression.py
  key_links:
    - "A top-level scan over the database object finds nothing: the shape is a mapping of manufacturer to a list of chip records, so the selector must descend two levels. A top-level scan makes every downstream assertion pass vacuously — the exact anti-vacuity trap `test_page_size_invariants.py`'s own selector docstring documents."
    - "Rows key on `part_number`. There is no `name` key in the generated database."
    - "ERASE-05 is a NON-REGRESSION requirement, deliberately scoped so no plan mistakes it for new work. The path already works end to end; this plan asserts it, and must not reimplement or re-route it."
    - "ERASE-06 needs ZERO `ic_layout.py` edits: the can-be-erased row keys on electrical type only and already renders yes for every algorithm-13 chip. Restoring the wire bit makes the two axes agree as a by-product. The requirement is read as the two axes must not CONTRADICT, not as `info` must DERIVE from the wire bit — and the plan records which reading it adopts."
    - "`_interpret_flags` reads a THIRD value — an upstream database info-flags bit — which is neither the wire bit nor the electrical type. It is already consistent and is out of scope; the new leg must not conflate it with either axis."
---

<objective>
Add the positive coverage that closes ERASE-03, ERASE-05 and ERASE-06: an exhaustive whole-database
assertion, a cross-axis agreement assertion, a non-regression assertion, and a pin on the one plan
shape nothing else in the suite watches.

Purpose: the inversions in plans 08 through 11 prove the old assertions are gone. They do not prove
the change is scoped. Only an exhaustive pass over all 746 rows shows that exactly the intended 84
moved and nothing else did — and only a both-directions leg shows the contradiction `info` had with
the wire is actually closed rather than merely no longer measured.

Output: two new test modules, one extended module, and no source change.

**Requirement flips owned by this plan: ERASE-03, ERASE-05, ERASE-06.**

Cites `D-153-04` (the erase is device-global; no post-erase blank check is wired, which is exactly
why `blank` must remain its own step).
Locked decisions honoured: **L-07** (ERASE-05 is a non-regression assertion, not new work — this
plan adds no implementation task for it) and **L-08** (ERASE-06 needs zero `ic_layout.py` edits and
is planned as an assertion task with a both-directions leg).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-write-path-erase-policy/153-RESEARCH.md
@.planning/phases/153-write-path-erase-policy/153-PATTERNS.md
@.planning/phases/153-write-path-erase-policy/153-DECISIONS.md
@firestarter_app/firestarter/database.py
@firestarter_app/firestarter/ic_layout.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: New module `test_erase_flag_invariants.py` — the exhaustive 84-of-746 assertion and the plan-shape pins</name>
  <read_first>
    - `firestarter_app/tests/test_page_size_invariants.py` — two patterns to copy: the nested
      per-chip selector with its anti-vacuity docstring explaining that a top-level scan finds
      nothing, and the exhaustive-count leg that re-asserts the 746 total inside the same test and
      names offenders in the failure message
    - `firestarter_app/firestarter/database.py` — `convert_to_programmer`, since the capability bit
      is derived at conversion time and is not a stored field, so the leg must convert rather than
      read raw JSON
    - `firestarter_app/firestarter/constants.py` around the capability bit
    - `firestarter_app/tests/test_chip_test_blank_check_order.py` lines 40-60 — the module-level
      real-database fixture idiom and the named chip constants each carrying a one-comment
      justification
    - `.planning/phases/153-write-path-erase-policy/153-PATTERNS.md` — the exhaustive-leg section
  </read_first>
  <files>firestarter_app/tests/test_erase_flag_invariants.py</files>
  <action>
    Create a new module `firestarter_app/tests/test_erase_flag_invariants.py`. Its docstring names
    Phase 153 and ERASE-03, states that the inversions elsewhere prove the old assertions are gone
    while this module proves the change is *scoped*, and states the anti-vacuity rule up front: the
    generated database is a mapping of manufacturer to a list of chip records, so any selector must
    descend two levels, and rows are keyed by `part_number` with no `name` key.

    Factor the selection into one module-level helper that walks the database two levels deep and
    returns every `(manufacturer, chip_record)` pair whose programming algorithm equals 13, with a
    docstring stating explicitly that a top-level scan would find nothing and make every downstream
    assertion pass vacuously. Use one module-level real-database instance built with the local
    override skipped, so a developer's own override file cannot change the counts.

    Legs:
    1. `test_exactly_84_algorithm_13_rows_across_all_746_rows` — assert the total row count is 746
       in the same test, then assert the selector returns exactly 84 pairs, naming the found rows in
       the failure message. The total assertion is what makes the subset count meaningful.
    2. `test_every_algorithm_13_row_carries_the_erase_capability_bit` — convert each of the 84 rows
       through `convert_to_programmer` and assert the bit is set on all of them, collecting every
       offender by `part_number` into one message rather than failing on the first.
    3. `test_no_non_algorithm_13_row_gained_the_erase_capability_bit` — the scope proof. For every
       row in the database that is **not** algorithm 13, assert the bit's value matches what the
       pre-change rule would have produced: set when the electrical type is one of the two erasable
       types and the algorithm is not 5, clear otherwise. Express the expectation from the rule, not
       from a recaptured snapshot, so the leg states the invariant rather than freezing the current
       output. Name every offender.
    4. `test_algorithm_5_rows_still_do_not_carry_the_erase_capability_bit` — an explicit, separately
       named leg for the hazard case, with a docstring stating that setting the bit for algorithm 5
       routes the firmware write-init into an erase that energises a programming rail on a 5 V-only
       part, so this is a hardware-damage guard and not a duplicate of leg 3. Assert the
       algorithm-5 row count is non-zero first, so the leg cannot pass over an empty set.
    5. `test_at28c256_full_plan_shape_is_pinned` — pin the measured `write_scope="full"` plan for
       AT28C256: twelve steps in the exact order id, read, write, verify, erase, blank-check, then
       the six SDP-leg ops; the erase step supported and destructive; the blank-check step
       unsupported; and the blank-check index strictly greater than the erase index. Assert the op
       sequence as a list equality so an inserted or reordered step fails loudly.
    6. `test_at28c256_write_scope_none_shape_is_pinned` — pin the measured `write_scope="none"` plan:
       three steps, id, read and blank-check, with blank-check supported; and `locked_destructive`
       containing the erase op alongside write, verify and the six SDP-leg ops. State in the
       docstring that this shape CHANGED in this phase — the step list dropped from four to three
       because the unsupported erase step became a locked destructive one instead — and that no
       committed test asserted it before, so it would otherwise have been an unnoticed change. Assert
       the locked op set as a set equality.

    Derive every number in legs 5 and 6 from the live plan in this session before writing it. If a
    live value differs from the shape described here, report the difference rather than adjusting the
    assertion.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_erase_flag_invariants.py -o addopts="" -q 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'def test_' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c '746' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c '84' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'part_number' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'skip_local_override' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -cE 'pytest.skip|skipif' tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_erase_flag_invariants.py</automated>
    <automated>cd /workspaces/firestarter_app; ruff format --check tests/test_erase_flag_invariants.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_'` returns `6`, and every leg name above is present.
    - The module contains one nested selector helper whose docstring states the anti-vacuity rule,
      and every leg uses it rather than an inline scan.
    - The 746 total is asserted inside the same test as the 84 subset count.
    - Leg 3 expresses its expectation from the pre-change rule, not from a recaptured snapshot, and
      names offenders.
    - Leg 4 asserts a non-zero algorithm-5 row count before asserting the bit is clear, so it cannot
      pass over an empty set, and its docstring states the hardware-damage rationale.
    - Legs 5 and 6 assert the op sequence and the locked-op set with equality, not membership, and
      their figures were derived live in this session with the derivation recorded in the summary.
    - Leg 6's docstring states that the `write_scope="none"` shape changed in this phase and that no
      committed test previously asserted it.
    - `grep -c 'skip_local_override'` returns at least `1`; `grep -cE 'pytest.skip|skipif'` returns
      `0`.
    - Each of the six legs was OBSERVED failing against a temporary local revert of plan 07's tuple
      edit, or — for the legs that are invariant across that revert — the summary states which and
      why they are invariant and what mutation was used instead. No leg is trusted without an
      observed failure.
    - `pytest tests/test_erase_flag_invariants.py -o addopts="" -q` is fully green.
    - `ruff check` and `ruff format --check` pass.
  </acceptance_criteria>
  <done>
    The change is proven scoped to exactly 84 rows, both plan shapes are pinned, and the hazard case
    has its own named guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: ERASE-06 — assert `info`'s row and the wire bit agree, in both directions</name>
  <read_first>
    - `firestarter_app/firestarter/ic_layout.py` lines 574-590 — the can-be-erased block. Confirm
      before writing anything that it keys on electrical type only and already renders the
      affirmative string for an algorithm-13 chip, so no source edit is owed.
    - the same file lines 218-245 — `_interpret_flags`, which reads a THIRD value (an upstream
      database info-flags bit). It is already consistent and is out of scope; the new leg must not
      conflate it with either axis.
    - `firestarter_app/tests/test_ic_layout.py` — the module's `EpromSpecBuilder` fixture, the
      `build_specifications` happy-path test's shape, and the existing negative-row tests (the
      hidden-VPP-row test in particular) for how an absent row is asserted
    - `firestarter_app/firestarter/eprom_info.py` around where the can-be-erased row is rendered —
      so the summary can name the render site
    - `.planning/phases/153-write-path-erase-policy/153-RESEARCH.md` §"Assumptions Log" A6 — the two
      possible readings of ERASE-06 and the recommendation to state which one is adopted
  </read_first>
  <files>firestarter_app/tests/test_ic_layout.py</files>
  <action>
    Make no change to `ic_layout.py`. ERASE-06 is discharged by assertion, not by edit.

    Add `test_can_erase_row_and_wire_capability_bit_agree_for_algorithm_13`: for an algorithm-13 chip,
    build the display specifications through `build_specifications` and the wire dict through
    `convert_to_programmer`, and assert **both** in one leg — that the can-be-erased row is present
    and affirmative, and that the wire capability bit is set. One assert pair, both directions,
    because the requirement is about the two axes agreeing and a leg that checks only one axis cannot
    detect a contradiction.

    Add `test_can_erase_row_and_wire_capability_bit_agree_for_uv_eprom`: the negative control. For a
    UV-EPROM chip, assert the row is present and states the non-electrical outcome, and assert the
    wire bit is clear. Two axes, same chip, opposite polarity — this is what proves the first leg
    measures agreement rather than a constant.

    Both docstrings must state which reading of ERASE-06 is adopted and why: the requirement is that
    the two axes must not **contradict**, not that `info` must **derive** from the wire bit. The
    source of that reading is 152-CONTEXT.md `D-07`'s own decomposition, which describes the row as
    contradicting the wire flag — naming the contradiction, not the derivation. Under that reading no
    `ic_layout.py` change is owed, and the deliberate independence of the two axes is a feature: they
    are derived from different data, so their agreement is evidence rather than a tautology. State
    that explicitly, because a leg comparing a value to itself would prove nothing.

    Both docstrings must also state that `_interpret_flags` reads a third, unrelated value and is out
    of scope.

    Add nothing else to the module. Run only this module, with `-o addopts=""`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; git diff --stat -- firestarter/ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_can_erase_row_and_wire_capability_bit_agree_for_algorithm_13' tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'test_can_erase_row_and_wire_capability_bit_agree_for_uv_eprom' tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'can_erase_str' tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c '_interpret_flags' tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'D-07' tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_ic_layout.py -o addopts="" -q 2>&1 | tail -12</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_ic_layout.py</automated>
    <automated>cd /workspaces/firestarter_app; ruff format --check tests/test_ic_layout.py</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- firestarter/ic_layout.py` is empty: ERASE-06 required no source edit, and
      the summary states that this was confirmed by reading the block rather than assumed.
    - Both new legs exist with the specified names, each appearing once.
    - Each leg asserts **both** axes for the same chip in the same test — the display row and the
      wire bit — with opposite expected polarity between the two legs.
    - Both docstrings state the adopted reading of ERASE-06, cite `D-07`'s decomposition as its
      source, and state that the two axes' independence is what makes their agreement evidence
      rather than a tautology.
    - Both docstrings state that `_interpret_flags` reads a third value and is out of scope.
    - The affirmative leg was OBSERVED failing against a temporary local revert of plan 07's tuple
      edit — the contradiction it closes is the one gh#20's paste showed — with the failure message
      transcribed and the revert confirmed removed by an empty `git diff` on `firestarter/database.py`.
    - `pytest tests/test_ic_layout.py -o addopts="" -q` is fully green, and the summary records the
      module's test count before and after.
    - `ruff check` and `ruff format --check` pass.
  </acceptance_criteria>
  <done>
    The contradiction between what `info` says and what the wire carries is closed and asserted in
    both directions, with zero source edits and the adopted reading on the record.
  </done>
</task>

<task type="auto">
  <name>Task 3: ERASE-05 — assert `blank` remains its own step, as non-regression</name>
  <read_first>
    - `firestarter_app/firestarter/cli_handlers.py` around the `blank` command — the decorator and
      the function definition are on different lines; record both measured line numbers in the
      summary, since the requirement text cites the decorator line as though it were the definition
    - `firestarter_app/firestarter/eprom_operations.py` around `check_eprom_blank` — the host side of
      the path
    - `firestarter/src/proms/eeprom_28c.cpp` — the `CMD_BLANK_CHECK` switch arm, the single
      remaining reference to the firmware blank-check helper in that file after plan 02
    - `firestarter_app/tests/test_characterization.py` and
      `firestarter_app/tests/test_eprom_operations.py` — the existing blank-related tests this leg
      asserts against rather than duplicating
    - `.planning/phases/153-write-path-erase-policy/153-DECISIONS.md` — `D-153-04`, the reason
      `blank` staying its own step matters: no post-erase blank check is wired, so the standalone
      command is the only way to get a blank verdict
  </read_first>
  <files>firestarter_app/tests/test_erase_blank_step_nonregression.py</files>
  <action>
    Create `firestarter_app/tests/test_erase_blank_step_nonregression.py`. Its docstring states that
    ERASE-05 is a **non-regression** requirement, deliberately scoped so no plan mistakes it for new
    work: the path already worked end to end before this phase, and this module asserts it still
    does. It must also state, per `D-153-04`, why that matters now — no post-erase blank check is
    wired on `0x0D`, so the standalone command is the only way a user gets a blank verdict, which
    makes its continued existence load-bearing rather than incidental.

    Legs:
    1. `test_blank_command_is_registered_and_documented` — assert the `blank` command is present in
       the CLI group's command mapping and that invoking it with the help flag exits 0 and renders
       its help text. Record both the decorator line and the definition line in the summary as the
       measured positions.
    2. `test_blank_command_reaches_the_host_blank_check_call` — drive the command through the CLI
       runner against a doubled operator and assert the host blank-check entry point was called
       exactly once with the resolved chip, using a positive call assertion.
    3. `test_blank_command_reports_not_blank_correctly` — drive it with a double whose blank check
       reports not-blank and assert the command surfaces that outcome rather than reporting success.
       A blank command that cannot report not-blank is worse than none.
    4. `test_firmware_still_wires_the_blank_check_arm` — a cross-repo source assertion: the firmware
       `0x0D` handler still contains its `CMD_BLANK_CHECK` arm mapping to the shared blank-check
       helper, and that helper appears exactly once in that file — the count plan 02's criteria
       established. Resolve the firmware path through the repository's existing sibling-presence
       helper so the leg SKIPS cleanly when the sibling checkout is absent, exactly as the other
       cross-repo gates do, and state in the docstring that it skips rather than fails in that case
       and why that is correct.
    5. `test_no_post_erase_blank_check_was_wired_on_0x0d` — the `D-153-04` disposition as an
       assertion: the `0x0D` handler assigns no operation-end function for the erase command. Express
       it as a source assertion over the erase arm, resolved through the same sibling helper so it
       skips consistently with leg 4.

    Use `-o addopts=""` when reading counts. Do not modify any source file. Do not duplicate the
    existing blank tests in `test_characterization.py` or `test_eprom_operations.py`; cite them in the
    docstring as the pre-existing coverage this module complements, and run them alongside as part of
    the verification rather than reimplementing them.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_erase_blank_step_nonregression.py -o addopts="" -q 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'def test_' tests/test_erase_blank_step_nonregression.py</automated>
    <automated>cd /workspaces/firestarter_app; grep -c 'D-153-04' tests/test_erase_blank_step_nonregression.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 -m pytest tests/test_characterization.py tests/test_eprom_operations.py -k blank -o addopts="" -q 2>&1 | tail -10</automated>
    <automated>cd /workspaces/firestarter; grep -c 'mem_util_blank_check' src/proms/eeprom_28c.cpp</automated>
    <automated>cd /workspaces/firestarter; pio test -e native -f native/avr/test_val_eeprom28c 2>&1 | tail -12</automated>
    <automated>cd /workspaces/firestarter_app; git status --porcelain firestarter/</automated>
    <automated>cd /workspaces/firestarter_app; ruff check tests/test_erase_blank_step_nonregression.py</automated>
    <automated>cd /workspaces/firestarter_app; ruff format --check tests/test_erase_blank_step_nonregression.py</automated>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_mypy_watermark.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_'` returns `5`, and every leg name above is present.
    - The module docstring states that ERASE-05 is non-regression, cites the pre-existing blank tests
      by module name, and cites `D-153-04` for why the standalone command is load-bearing.
    - Legs 4 and 5 resolve the firmware path through the repository's existing sibling-presence
      helper and SKIP cleanly when the sibling is absent, with the docstring stating that this is
      correct because the host package's own CI has no firmware checkout.
    - `grep -c 'mem_util_blank_check' src/proms/eeprom_28c.cpp` returns `1`, matching plan 02's
      established count.
    - `pytest tests/test_characterization.py tests/test_eprom_operations.py -k blank -o addopts=""`
      passes, and the summary records how many tests that selected — a zero selection would mean the
      cited pre-existing coverage does not exist and is a stop-and-report condition.
    - `pio test -e native -f native/avr/test_val_eeprom28c` passes, including the pre-existing
      blank-check configure no-VPP case, which must be unchanged by this phase.
    - `git status --porcelain firestarter/` shows no modification to any host source file: this plan
      is assertion-only.
    - `ruff check`, `ruff format --check` and the mypy watermark check all pass.
  </acceptance_criteria>
  <done>
    `blank` is proven to still work as its own step, at the CLI, at the host call boundary and at the
    firmware dispatch arm, without a line of new production code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated database → wire capability advertisement | 84 rows now advertise a destructive capability; 662 must not. |
| `info` output → user's decision to run a destructive command | A contradiction between what `info` claims and what the tool can do is what produced gh#20's paste. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-63 | Tampering (physical destruction) | the capability bit reaching a row it should not, in bulk | critical | mitigate | Legs 3 and 4 of Task 1 assert the invariant over every non-algorithm-13 row and give the algorithm-5 hazard its own named guard with a non-zero-population precondition. |
| T-153-64 | Spoofing (vacuous pass) | a top-level database scan finding nothing | high | mitigate | One nested selector helper with an anti-vacuity docstring, used by every leg; the 746 total asserted inside the same test as the subset count. |
| T-153-65 | Spoofing (tautology) | an agreement leg comparing a value to itself | medium | mitigate | The two axes are derived from different data and the docstrings must state that; a UV-EPROM negative control with opposite polarity proves the leg measures agreement rather than a constant. |
| T-153-66 | Repudiation | an unnoticed plan-shape change at `write_scope="none"` | medium | mitigate | Task 1 leg 6 pins it with set and list equalities and its docstring records that it changed in this phase and was previously unasserted. |
| T-153-67 | Spoofing (false green) | ERASE-05 treated as satisfied by a leg that cannot fail | medium | mitigate | Task 3 requires a non-zero selection count from the cited pre-existing tests and asserts a not-blank outcome is actually surfaced. |
| T-153-68 | Denial of service | a cross-repo leg erroring instead of skipping in host-only CI | low | mitigate | Legs 4 and 5 resolve through the existing sibling-presence helper and skip cleanly, matching the other cross-repo gates. |
| T-153-69 | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is added. |
</threat_model>

<verification>
- All three modules pass with `-o addopts=""`.
- `git diff --stat -- firestarter/ic_layout.py` is empty and `git status --porcelain firestarter/`
  shows no host source modification.
- The exhaustive legs assert 746 and 84 in the same tests and name offenders.
- Every leg's reachability was established by an observed failure, or its invariance was stated with
  the alternative mutation used.
- `ruff check`, `ruff format --check` and the mypy watermark all pass.
</verification>

<success_criteria>
- ERASE-03 is closed with proof of scope, not just proof of change.
- ERASE-06 is closed with a both-directions agreement leg and zero source edits, with the adopted
  reading recorded.
- ERASE-05 is closed as non-regression at three layers, without new production code.
- The one plan shape nothing else watches is pinned.
</success_criteria>

## Artifacts this phase produces

This plan's share:

| Artifact | Kind | Notes |
|---|---|---|
| `firestarter_app/tests/test_erase_flag_invariants.py` | new test module | 6 legs: exhaustive 84-of-746, per-row bit, non-13 scope, algorithm-5 hazard guard, and two plan-shape pins |
| `firestarter_app/tests/test_erase_blank_step_nonregression.py` | new test module | 5 legs across CLI, host call boundary and firmware dispatch arm |
| `test_can_erase_row_and_wire_capability_bit_agree_for_algorithm_13` | new test | `test_ic_layout.py`; both axes, one leg |
| `test_can_erase_row_and_wire_capability_bit_agree_for_uv_eprom` | new test | `test_ic_layout.py`; opposite-polarity negative control |

No source symbol is created by this plan.

<output>
Create `.planning/phases/153-write-path-erase-policy/153-12-SUMMARY.md` when done.
</output>
