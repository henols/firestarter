---
phase: 148-numeric-database-values-the-at28c-vcc-decode
plan: 05
type: execute
wave: 5
depends_on: ["148-04"]
commits_land_in: firestarter_app
files_modified:
  - firestarter_app/tools/audit_coverage_matrix.py
  - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "No string-parsing coercion of a pulse duration survives anywhere in the repository"
    - "The coverage-matrix golden is regenerated by the tool, never hand-edited"
    - "No meta-repo tracked file is written or mutated by any regeneration run"
  artifacts:
    - path: "firestarter_app/tools/audit_coverage_matrix.py"
      provides: "Direct integer reads of programming.pulse_duration_us; parse_pulse_us deleted"
      contains: "pulse_duration_us"
    - path: "firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md"
      provides: "Regenerated matrix with integer pulse cells"
      contains: "|"
  key_links:
    - from: "firestarter_app/tools/audit_coverage_matrix.py"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "direct indexing chip['programming']['pulse_duration_us']"
      pattern: "pulse_duration_us"
---

<objective>
Delete `audit_coverage_matrix.py`'s `parse_pulse_us` — the **second** live string parser — and
regenerate the golden it renders into.

**D-09**: DATA-03's discipline is about the defect class, not one filename. `parse_pulse_us`
(`tools/audit_coverage_matrix.py:106-110`) **raises** on any non-`" us"` value, is called at seven
sites, and is exercised by an imported test suite against the live database. Leaving it — or
widening it to accept ints — is precisely the "bypassed, not gone" shape DATA-03 forbids, and a
tolerant reader outlives the last string it was written for.

RESEARCH.md found an **eighth** read CONTEXT.md does not name, and it is a *render*, not a parse:
`_enum_row` at `:537` passes `chip["programming"]["pulse_duration"]` through `_md_escape`. It
survives an int, but the rendered cell changes from `100 us` to `100` — which is what forces the
`tests/golden/v1.3-COVERAGE-MATRIX.md` regeneration. Measured: **297** value cells change; the
file carries 303 ` us` lines today, and the other **6** are `pulse_bucket` **labels**
(`< 100 us`, `100-999 us`) which this plan does **not** touch and must not delete.

Purpose: finish DATA-03 across the whole repository, not just `database.py`.
Output: a coercion-free tooling layer and a tool-regenerated golden, with the full suite green
again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-RESEARCH.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-PATTERNS.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-04-SUMMARY.md
</context>

<planner_decisions>
- **Open Question 3 (does the meta-repo `v1.3-defect-coverage-ids.json` get updated?) — settled: NO,
  and it is measured rather than assumed.** `pulse_bucket` already takes ints, so D-09 changes only
  the *rendering* of the pulse column and not the bucketing; defect signatures should be stable.
  Task 3 **measures** that rather than assuming it. The meta ledger is **already drifted today**
  (78 committed entries vs 68 from a fresh generate — consistent with the standing 121-01 note),
  and reconciling it is a separate, deliberate, cross-repo decision. Phase 148 must not be judged
  against it and must not write it.
</planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Delete parse_pulse_us and move all eight reads to direct integer indexing</name>
  <files>firestarter_app/tools/audit_coverage_matrix.py</files>
  <read_first>
    - `firestarter_app/tools/audit_coverage_matrix.py:106-110` — the `parse_pulse_us` definition and
      its "Pitfall 3 fail-fast" docstring.
    - `firestarter_app/tools/audit_coverage_matrix.py:516-542` — `_md_escape` and `_enum_row`; the
      **render** read at `:537`.
    - `firestarter_app/tools/audit_coverage_matrix.py:1717-1734` — `_members_with_parseable_pulse`
      and its `isinstance(pd, str) and pd.endswith(" us")` filter at `:1728`.
    - `firestarter_app/tools/audit_coverage_matrix.py:59-68` — `_REPO_ROOT` and the `DEFAULT_OUTPUT`
      / `DEFAULT_LEDGER` constants that resolve into the **meta** repo.
    - `148-PATTERNS.md` §"`tools/audit_coverage_matrix.py`".
    - `148-RESEARCH.md` §"Edit Sites §C" — the full nine-occurrence census.
  </read_first>
  <action>
Working directory `/workspaces/firestarter_app`.

Delete the `parse_pulse_us` definition at `:106-110` outright. Do **not** widen it to accept ints
and do **not** keep it as a passthrough — a tolerant reader is exactly what D-09 removes.

Rewrite the seven call sites to direct indexing — `chip["programming"]["pulse_duration_us"]` — at
`:307`, `:849` (the `detect_correctness` median/outlier computation), `:1165`, `:1178`, `:1196`,
`:1636` (the `--all-algorithms` wide-scan path) and `:1730`. Use direct indexing, not
`.get(..., 0)`: an absent key must raise, because `0` now means "algorithm-controlled" and a
silently-defaulted `0` would put a chip in the wrong pulse bucket (D-10).

Rewrite `_members_with_parseable_pulse` (`:1717-1734`): the string filter
`isinstance(pd, str) and pd.endswith(" us")` wrapping a `try: parse_pulse_us(pd) except ValueError:
continue` becomes a single `!= 0` test on `chip["programming"]["pulse_duration_us"]`. Update its
docstring — it no longer describes "a 'N us' string" but "a non-zero integer microsecond value",
and state that `0` means algorithm-controlled rather than unparseable, which is now true by
construction because `interpret_timing` makes a decode fault fatal (D-08).

Update the prose mention at `:1562` (the wide-scan reuse note listing the shared primitives) so it
no longer names a function that does not exist.

Leave `_enum_row` at `:537` alone apart from the key rename: `_md_escape` is
`str(s).replace("|", r"\|")` and survives an int unchanged. The rendered cell legitimately becomes
`100` instead of `100 us`; that is the change Task 2 regenerates the golden for.

**Never run this tool without explicit `--output` and `--ledger`.** `_REPO_ROOT` resolves to
`/workspaces`, so the defaults are `/workspaces/.planning/v1.3-COVERAGE-MATRIX.md` and
`/workspaces/.planning/v1.3-defect-coverage-ids.json` — **both tracked meta-repo files**, and the
tool **mutates** the ledger by minting new `DEFECT-COV-NN` IDs.
  </action>
  <acceptance_criteria>
    - `grep -c 'parse_pulse_us' tools/audit_coverage_matrix.py` returns **0** — definition, all
      seven call sites, and the prose mention.
    - `grep -c 'endswith(" us")' tools/audit_coverage_matrix.py` returns 0.
    - `grep -c 'pulse_duration_us' tools/audit_coverage_matrix.py` is at least 8.
    - No `["pulse_duration"]` subscript and no `.get("pulse_duration", ...)` call remains —
      asserted by parsing the file with `ast`, not by a substring count. Measured: **8** such reads
      today, at `:307 :537 :849 :1165 :1178 :1196 :1636 :1727`. A text count is not used because
      this task rewrites `_members_with_parseable_pulse`'s docstring, which names the old key in
      prose, and a whole-file substring count would flag that prose.
    - `grep -c 'except Exception' tools/audit_coverage_matrix.py` did not increase relative to
      `git show HEAD:tools/audit_coverage_matrix.py` — no broad except was introduced (nothing lints
      `tools/`). Both counts are **0** today, so each capture must be `|| true`-guarded: `grep -c`
      exits **1** on a zero count, which would otherwise abort the verify chain before it ran
      anything.
    - `python3 tools/audit_coverage_matrix.py --output "$SCRATCH/m.md" --ledger "$SCRATCH/l.json"`
      exits 0 with `$SCRATCH` a `mktemp -d` outside both repos.
    - `git -C /workspaces status --porcelain .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json`
      is empty — no meta-repo tracked file was written.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(grep -c 'parse_pulse_us' tools/audit_coverage_matrix.py)" = "0" && test "$(grep -c 'endswith(" us")' tools/audit_coverage_matrix.py)" = "0" && test "$(grep -c 'pulse_duration_us' tools/audit_coverage_matrix.py)" -ge 8 && python3 -c "
import ast
bad=[]
for n in ast.walk(ast.parse(open('tools/audit_coverage_matrix.py').read())):
    if isinstance(n,ast.Subscript) and isinstance(n.slice,ast.Constant) and n.slice.value=='pulse_duration': bad.append(n.lineno)
    if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='get' and n.args and isinstance(n.args[0],ast.Constant) and n.args[0].value=='pulse_duration': bad.append(n.lineno)
assert not bad, f'old pulse_duration key still read at lines {sorted(set(bad))}'
print('no pulse_duration reads OK')
" && BEFORE=$(git show HEAD:tools/audit_coverage_matrix.py | grep -c 'except Exception' || true) && AFTER=$(grep -c 'except Exception' tools/audit_coverage_matrix.py || true) && test "${AFTER:-0}" -le "${BEFORE:-0}" && S=$(mktemp -d) && python3 tools/audit_coverage_matrix.py --output "$S/m.md" --ledger "$S/l.json" > /dev/null && test -s "$S/m.md" && rm -rf "$S" && test -z "$(git -C /workspaces status --porcelain .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json)" && echo "PLAN-05-T1 OK"</automated>
  </verify>
  <done>`parse_pulse_us` no longer exists anywhere in the file, all eight reads go straight to the
  integer key, the tool runs clean against the migrated database, and no meta-repo tracked file was
  written.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the coverage-matrix golden with the tool, and prove the --check contract</name>
  <files>firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md</files>
  <read_first>
    - `firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md` — the header stating "DO NOT EDIT BY
      HAND. Re-run the tool."
    - `firestarter_app/tests/test_audit_coverage_matrix.py:440-510` — the subprocess invocations
      that already pass `--output` / `--ledger` into `tmp_path`, and the three-step `--check`
      contract.
    - `firestarter_app/tests/test_audit_coverage_matrix.py:576-648` — the golden byte-identity test.
    - `148-RESEARCH.md` §"Trap 3", §"Trap 4", §"Pitfall 5".
  </read_first>
  <action>
Working directory `/workspaces/firestarter_app`.

Set `S=$(mktemp -d)` outside both repos. Run
`python3 tools/audit_coverage_matrix.py --output "$S/m.md" --ledger "$S/l.json"` and copy `$S/m.md`
over `tests/golden/v1.3-COVERAGE-MATRIX.md`. **Never hand-edit the golden** — its own header
forbids it; regenerate and copy.

Then reproduce the `--check` three-step from `tests/test_audit_coverage_matrix.py:440-510` and
record each exit code:
  1. clean generate to `$S/l.json` -> exit 0
  2. `--check` against an **empty** `{}` ledger -> exit **1**
  3. `--check` against the step-1 ledger -> exit **0**
Step 2 exiting 0 would mean the check is vacuous. Do **not** run `--check` against the meta repo's
committed ledger — it holds 78 entries against a fresh 68 and is already drifted today, before
Phase 148 (Trap 4). A `--check` against it exits 1 for a reason that predates this phase.

`rm -rf "$S"` when done, and confirm with `git -C /workspaces status --porcelain` that no
`.planning/` tracked file moved.
  </action>
  <acceptance_criteria>
    - `grep -cE '\| [0-9]+ us' tests/golden/v1.3-COVERAGE-MATRIX.md` returns **0** — the **297**
      per-chip pulse cells now render as bare integers.
    - `grep -c '100-999 us' tests/golden/v1.3-COVERAGE-MATRIX.md` is at least 1 — `pulse_bucket`'s
      D-09 **labels** are unchanged, and must not be deleted to satisfy the line above. A whole-file
      `grep -c ' us'` **cannot** reach 0: of the 303 lines carrying ` us` today, 297 are value cells
      and **6** are bucket-label lines (`:54 :55 :1227 :1228 :1235 :1236`) emitted by
      `pulse_bucket`, which this task does not touch.
    - `python3 -m pytest tests/test_audit_coverage_matrix.py -o addopts="" -q` passes, including the
      byte-identity golden test.
    - A fresh generate into a scratch path is **byte-identical** to the committed golden.
    - `--check` against an empty `{}` ledger exits **1**; `--check` against a freshly generated
      ledger exits **0**.
    - `git -C /workspaces status --porcelain .planning/` shows no modification to
      `v1.3-COVERAGE-MATRIX.md` or `v1.3-defect-coverage-ids.json`.
    - The golden's "DO NOT EDIT BY HAND" header line is still present and unmodified.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && test "$(grep -cE '\| [0-9]+ us' tests/golden/v1.3-COVERAGE-MATRIX.md)" = "0" && test "$(grep -c '100-999 us' tests/golden/v1.3-COVERAGE-MATRIX.md)" -ge "1" && grep -qi 'do not edit by hand' tests/golden/v1.3-COVERAGE-MATRIX.md && S=$(mktemp -d) && python3 tools/audit_coverage_matrix.py --output "$S/m.md" --ledger "$S/l.json" > /dev/null && cmp "$S/m.md" tests/golden/v1.3-COVERAGE-MATRIX.md && echo '{}' > "$S/empty.json" && python3 tools/audit_coverage_matrix.py --check --output "$S/c1.md" --ledger "$S/empty.json" > /dev/null 2>&1; test $? -eq 1 && python3 tools/audit_coverage_matrix.py --check --output "$S/c2.md" --ledger "$S/l.json" > /dev/null 2>&1; test $? -eq 0 && rm -rf "$S" && test -z "$(git -C /workspaces status --porcelain .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json)" && python3 -m pytest tests/test_audit_coverage_matrix.py -o addopts="" -q && echo "PLAN-05-T2 OK"</automated>
  </verify>
  <done>The golden is tool-regenerated and byte-identical to a fresh run, zero ` us` cells remain,
  the `--check` contract fails on an empty ledger and passes on a fresh one, and no meta-repo file
  was touched.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the full suite to green and measure the defect-signature stability</name>
  <files>firestarter_app/tools/audit_coverage_matrix.py</files>
  <read_first>
    - `148-RESEARCH.md` §"Open Questions" 3 and §"Assumptions Log" A1.
    - `148-VALIDATION.md` §"Test Infrastructure" — the ~280 s / 1616-test runtime and the
      `-q`-doubling trap.
  </read_first>
  <action>
Working directory `/workspaces/firestarter_app`.

Commit everything first — then run the **full** suite: `python3 -m pytest -o addopts="" -q` with a
timeout of **at least 600 s**. It measured 1616 passed in 280 s before this phase; a 120 s default
returns rc=124 and reads exactly like a RED. Every pytest command must carry `-o addopts=""` —
`addopts` is `-ra -q` and a second `-q` suppresses the count line, so a run that collected nothing
looks identical to a run that passed.

This is the first point in the phase where the whole suite can be green again. If any test is still
RED, it belongs to a file this plan or Plan 04 owns — fix it here rather than deferring.

Then settle RESEARCH Open Question 3 by **measurement**, not assumption: generate a ledger into a
scratch path before and after the change (use `git stash` or a `git worktree` for the "before" side
if needed) and compare the `DEFECT-COV-NN` ID sets. Record the result in the plan SUMMARY. If the
sets are identical, state that the meta-repo ledger needs no update and why. If they differ, do
**not** update the meta ledger — record the delta and flag it as a separate cross-repo decision.

Do not make any part of this task depend on a local `tools/check_mypy_watermark.py` run: the
devcontainer is py3.12 while CI pins 3.11, so it exits 2 on numpy stubs and a local pass is not
evidence. Note also that `tools/` is outside mypy's scope entirely, so the only Phase 148 files
mypy sees are `firestarter/database.py`, `ic_layout.py`, `eprom_info.py` and `tests/`.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest -o addopts="" -q` reports **0 failed** and at least **1616** tests
      collected, and the count line is visible in the output.
    - `python3 tools/build_db.py` exits 0; `python3 tools/diff_db.py` exits 0;
      `python3 tools/check_dispatch.py` exits 0.
    - `python3 -m ruff check firestarter/ tests/` and
      `python3 -m ruff format --check firestarter/ tests/` pass.
    - `git diff --quiet tools/check_dispatch.py` and
      `git diff --quiet tests/__snapshots__/test_characterization.ambr` both succeed.
    - The SUMMARY records the measured `DEFECT-COV-NN` ID-set comparison and an explicit statement
      of whether the meta ledger needs an update.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && timeout 900 python3 -m pytest -o addopts="" -q 2>&1 | tail -5 | tee /tmp/full.txt && grep -qE '[0-9]+ passed' /tmp/full.txt && ! grep -qE '[0-9]+ failed' /tmp/full.txt && python3 tools/build_db.py > /dev/null && python3 tools/diff_db.py > /dev/null && python3 tools/check_dispatch.py > /dev/null && python3 -m ruff check firestarter/ tests/ && python3 -m ruff format --check firestarter/ tests/ && git diff --quiet tools/check_dispatch.py && git diff --quiet tests/__snapshots__/test_characterization.ambr && echo "PLAN-05-T3 OK"</automated>
  </verify>
  <done>The full suite is green again with the count line visible, all four gates pass, GATE-03 and
  the characterization snapshot are byte-unchanged, and the defect-signature stability question is
  answered by measurement rather than assumption.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| deleted symbol | `tools/audit_coverage_matrix.py::parse_pulse_us` |
| changed contract | `tools/audit_coverage_matrix.py::_members_with_parseable_pulse` now filters on `!= 0` |
| regenerated golden | `tests/golden/v1.3-COVERAGE-MATRIX.md` (**297** ` us` value cells become integers; the 6 `pulse_bucket` label lines are unchanged) |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-repo tool -> meta repo filesystem | `audit_coverage_matrix.py`'s default output and ledger paths resolve **outside** the sub-repo, into tracked meta-repo files |
| generated DB -> coverage ledger | the ledger mints persistent `DEFECT-COV-NN` identifiers from the database's contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-15 | Tampering | `_REPO_ROOT` defaults writing `/workspaces/.planning/v1.3-COVERAGE-MATRIX.md` and `v1.3-defect-coverage-ids.json` | mitigate | every invocation in this plan passes explicit `--output` / `--ledger` into a `mktemp -d` outside both repos, and each task asserts `git -C /workspaces status --porcelain` on both paths is empty |
| T-148-16 | Repudiation | minting new `DEFECT-COV-NN` IDs as a side effect of a regen | mitigate | Task 3 measures the ID-set delta explicitly and records it; a legitimate change is escalated as a separate cross-repo decision, never applied silently |
| T-148-17 | Denial of service | full-suite run timing out at a default 120 s and reading as RED | mitigate | the verify uses `timeout 900` against a measured 280 s runtime, and asserts the count line is present so a zero-collection run cannot masquerade as a pass |
| T-148-18 | Tampering | hand-editing the coverage-matrix golden to make a diff disappear | mitigate | Task 2 asserts a fresh tool run is byte-identical (`cmp`) to the committed golden and that the "DO NOT EDIT BY HAND" header survives |
| T-148-SC | Tampering | npm/pip/cargo installs | accept | no package installs; no dependency added |
</threat_model>

<verification>
From `/workspaces/firestarter_app`:

    python3 -m pytest -o addopts="" -q                       # budget >= 600 s
    python3 tools/build_db.py
    python3 tools/diff_db.py ; echo EXIT=$?
    python3 tools/check_dispatch.py ; echo EXIT=$?
    python3 -m ruff check firestarter/ tests/
    git diff --quiet tools/check_dispatch.py
    git diff --quiet tests/__snapshots__/test_characterization.ambr
    git -C /workspaces status --porcelain .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json
</verification>

<success_criteria>
- Zero occurrences of `parse_pulse_us` in the repository.
- `tests/golden/v1.3-COVERAGE-MATRIX.md` regenerated by the tool, byte-identical to a fresh run,
  zero ` us` cells.
- `--check` exits 1 on an empty ledger and 0 on a fresh one.
- Full suite green (>= 1616 passed, 0 failed) with the count line visible.
- No meta-repo tracked file written by any run.
</success_criteria>

<output>
Create `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-05-SUMMARY.md` when done.
</output>
