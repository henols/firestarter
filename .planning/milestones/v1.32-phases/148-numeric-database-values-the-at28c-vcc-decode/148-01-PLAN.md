---
phase: 148-numeric-database-values-the-at28c-vcc-decode
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter_app
files_modified:
  - firestarter_app/tests/golden/wire_dict_baseline.json
  - firestarter_app/tests/test_wire_dict_equivalence.py
  - .planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "A committed 746-record capture of every chip's host->wire dict exists, taken before any Phase 148 edit"
    - "A test asserts that capture byte-for-byte and fails loudly (never skips) when it drifts"
    - "The capture proves vcc and vpp_volts never cross the host->wire seam"
    - "The phase's review artifact records the measured pre-change state of all four gates"
  artifacts:
    - path: "firestarter_app/tests/golden/wire_dict_baseline.json"
      provides: "746-chip pre-change wire-dict baseline with a meta block"
      contains: "\"meta\""
    - path: "firestarter_app/tests/test_wire_dict_equivalence.py"
      provides: "Byte-identity assertion, 9-key union guard, non-vacuity leg"
      contains: "def test_"
    - path: ".planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md"
      provides: "D-12 review artifact, Before section"
      contains: "## Before"
  key_links:
    - from: "firestarter_app/tests/test_wire_dict_equivalence.py"
      to: "firestarter/database.py::convert_to_programmer"
      via: "EpromDatabase(skip_local_override=True) -> get_eprom -> convert_to_programmer"
      pattern: "convert_to_programmer"
    - from: "firestarter_app/tests/test_wire_dict_equivalence.py"
      to: "tests/golden/wire_dict_baseline.json"
      via: "byte comparison against the committed fixture"
      pattern: "wire_dict_baseline"
---

<objective>
Capture the 746-chip host->wire baseline **before any Phase 148 edit**, and commit the test that
will hold it byte-identical through the whole migration.

This is task 1 of the phase by hard ordering constraint (D-14, VALIDATION.md §"Wave 0
Requirements"). Every later plan changes the schema the wire dict is derived from. Without this
capture taken first, the D-14 equivalence claim — criterion 3's "every read / write / erase /
blank-check command still resolves the same effective electrical and timing values it resolves
today" — has no baseline to compare against and becomes an assertion nobody wrote.

Purpose: make "this phase cannot change what reaches the firmware" a measured fact rather than a
claim. Per **D-06**, `vcc` is inert on the wire; this plan is where that stops being prose.
Output: a committed golden, a committed test that is green on the pre-change tree, and the
`## Before` section of the phase's D-12 review artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-RESEARCH.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-PATTERNS.md
@.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-VALIDATION.md
</context>

<planner_decisions>
Settled here so no executor re-opens them. These are the CONTEXT.md `### Claude's Discretion`
items that bind this plan.

- **P-06 (D-14 fixture format and location).** Full canonical JSON at
  `tests/golden/wire_dict_baseline.json`, serialized `json.dumps(out, sort_keys=True, indent=2)`.
  Measured: 746 records, 332,716 bytes, SHA-256
  `027a43a0dcef1085afa6a35d2500bd35556140dde4b838dfcd65bfae8cac7dab` (reproduced by the planner on
  2026-08-19, independently of RESEARCH.md). Rejected: a per-chip SHA-256 map (~65 KB — names the
  chip but not the field) and a single blob SHA in the test source (64 B — reports only "something
  changed"). 325 KB is unremarkable next to the 1.1 MB `chip_database.json` already committed, and
  it is the only option that makes a regression self-explaining.
- **Record key is `f"{mfg}|{pn}|{i}"`, never `pn` alone.** 65-69 records share a `part_number`
  (`diff_db.py:246-266`'s CR-01 comment documents this); a `pn`-only key silently shadows ~9% of
  the database.
</planner_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Capture the 746-chip pre-change wire-dict golden</name>
  <files>firestarter_app/tests/golden/wire_dict_baseline.json</files>
  <read_first>
    - `firestarter_app/firestarter/database.py:370-441` (`_map_data`) and `:536-626`
      (`convert_to_programmer`) — the seam being captured.
    - `firestarter_app/tools/check_dispatch.py:360-376` — the production 746-chip loop
      (`db.get_eprom(part)` -> `db.convert_to_programmer(mapped)`) whose shape this reuses.
    - `firestarter_app/tests/golden/chip_database_field_inventory.json` — the **only** golden JSON
      in the tree carrying a `meta` block; its key set and prose-sentence style are the convention.
    - `148-PATTERNS.md` §"3. `tests/golden/wire_dict_baseline.json`".
    - `148-RESEARCH.md` §"D-14 — 746-chip wire-dict equivalence, measured" and §"F-8".
  </read_first>
  <action>
Working directory `/workspaces/firestarter_app`. **Run this before any other Phase 148 edit** —
the tree must be at HEAD `9701209` content for `tools/build_db.py`,
`firestarter/data/chip_database.json` and `firestarter/database.py`.

Write a throwaway capture script under the scratch dir (NOT committed) that constructs
`EpromDatabase(skip_local_override=True)` — `skip_local_override=True` is mandatory
(`tests/test_characterization.py:501` states the rule verbatim: a `~/.firestarter` override would
leak a spurious row) — iterates `db.proms.items()`, and for each `(mfg, chips)` and each
`(i, chip)` calls `db.get_eprom(chip["part_number"])` then `db.convert_to_programmer(mapped)`,
storing the result under key `f"{mfg}|{pn}|{i}"`.

Serialize the record map with `json.dumps(records, sort_keys=True, indent=2)`. Nest it under a
top-level `"records"` key alongside a `"meta"` object, then write the whole document with the same
`sort_keys=True, indent=2` convention so the file is stable.

The `meta` key set is fixed by the analog (`tests/golden/chip_database_field_inventory.json`) —
author the prose yourself but emit exactly these keys: `source`, `generator`, `recorded_by`,
`requirement`, `decision`, `recorded_at_head`, `how_to_update`, plus one phase-specific key
`why_nine_keys`.
  - `source`: `firestarter/database.py::EpromDatabase.convert_to_programmer`
  - `generator`: `firestarter/data/chip_database.json` via `tools/build_db.py`
  - `recorded_by`: `Phase 148 Plan 01`
  - `requirement`: `DATA-03`
  - `decision`: `D-14 (capture), D-06 (vcc is inert on the wire)`
  - `recorded_at_head`: the `firestarter_app` HEAD sha at capture time
  - `how_to_update`: state that this file is a **pre-Phase-148 capture** and must NOT be
    regenerated to make a failure disappear — a diff here means the host->wire contract moved,
    which Phase 148 is specifically forbidden to do; a legitimate future wire change must
    re-capture deliberately and say in the commit message which chips and which keys moved.
  - `why_nine_keys`: record that the union across all 746 chips is exactly nine keys
    (`algorithm, bus-config, chip-id, flags, memory-size, page-size, pin-count, pulse-delay,
    vpp_mv`) and that a five-key capture — the number D-06/D-14 assumed — would miss `bus-config`,
    `flags` and `page-size`, three real wire fields (RESEARCH F-8).

Do not commit the capture script. `tests/golden` is inside ruff's `extend-exclude`, so nothing
reformats this file; do not change the serialization or the recorded SHA becomes unverifiable.
  </action>
  <acceptance_criteria>
    - `firestarter_app/tests/golden/wire_dict_baseline.json` exists and parses as JSON.
    - It has exactly two top-level keys: `meta` and `records`.
    - `records` has exactly **746** entries; every key matches the shape `MFG|PART|INDEX`.
    - `json.dumps(doc["records"], sort_keys=True, indent=2)` is **332,716** bytes and its SHA-256 is
      `027a43a0dcef1085afa6a35d2500bd35556140dde4b838dfcd65bfae8cac7dab`.
    - The union of keys across all 746 records is exactly
      `{algorithm, bus-config, chip-id, flags, memory-size, page-size, pin-count, pulse-delay, vpp_mv}`
      — nine keys.
    - Neither `vcc` nor `vpp_volts` appears in any record (D-06).
    - `meta` carries all eight keys listed in the action, and `meta.recorded_at_head` is a 40-char sha.
    - `git status --porcelain firestarter_app/tools firestarter_app/firestarter` reports **no**
      modifications — the capture changed no source file.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import json,hashlib
d=json.load(open('tests/golden/wire_dict_baseline.json'))
assert sorted(d)==['meta','records'], sorted(d)
r=d['records']; assert len(r)==746, len(r)
blob=json.dumps(r, sort_keys=True, indent=2)
assert len(blob.encode())==332716, len(blob.encode())
h=hashlib.sha256(blob.encode()).hexdigest()
assert h=='027a43a0dcef1085afa6a35d2500bd35556140dde4b838dfcd65bfae8cac7dab', h
ks=set().union(*(set(v) for v in r.values()))
assert ks=={'algorithm','bus-config','chip-id','flags','memory-size','page-size','pin-count','pulse-delay','vpp_mv'}, sorted(ks)
assert 'vcc' not in ks and 'vpp_volts' not in ks
need={'source','generator','recorded_by','requirement','decision','recorded_at_head','how_to_update','why_nine_keys'}
assert need <= set(d['meta']), need - set(d['meta'])
assert len(d['meta']['recorded_at_head'])==40
print('OK 746 records, sha matches, 9 wire keys, vcc/vpp_volts absent')
" && test -z "$(git -C /workspaces/firestarter_app status --porcelain tools firestarter | grep -v '^??')" && echo "PLAN-01-T1 OK (golden captured, no source modification)"</automated>
  </verify>
  <done>The golden exists, carries 746 records under a `meta`+`records` document, reproduces the
  recorded 332,716-byte / `027a43a0…` canonical blob, exposes exactly the nine measured wire keys,
  and no source file was touched to produce it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit the byte-identity test and prove it green on the pre-change tree</name>
  <files>firestarter_app/tests/test_wire_dict_equivalence.py</files>
  <read_first>
    - `firestarter_app/tests/test_audit_coverage_matrix.py:576-648` — the byte-identity assertion
      and its "if this is a legitimate change, regenerate the golden alongside" message; and
      `:620-623`, the golden-missing guard that fails loudly rather than skipping.
    - `firestarter_app/tests/test_variant_decode_evidence_stability.py:100-131` — per-record diff
      naming, better diagnostics than one 325 KB blob compare.
    - `firestarter_app/tests/test_sdp_db_invariant.py:70-115, 337-370` — S-1 docstring shape, S-4
      offender-naming assertions, S-5 non-vacuity leg.
    - `148-PATTERNS.md` §"2. `tests/test_wire_dict_equivalence.py`" and §"S-1"…§"S-5".
  </read_first>
  <behavior>
    - Test 1: the live 746-chip capture equals `tests/golden/wire_dict_baseline.json`'s `records`
      exactly; on mismatch the message names added / removed / value-changed record keys
      **separately**, and for a value-changed record names the differing wire keys.
    - Test 2: the union of wire keys across the live capture is exactly the nine measured keys;
      the message names anything added or removed.
    - Test 3 (D-06): neither `vcc` nor `vpp_volts` appears in any live wire dict — this is the
      load-bearing claim that a VCC decode change cannot alter `write` behaviour.
    - Test 4 (non-vacuity, S-5): a synthetic record injected into a copy of the baseline makes the
      **same** comparison helper raise — proving the gate is capable of failing.
    - Test 5: the golden file exists; a missing golden is a loud failure, never a skip.
  </behavior>
  <action>
Working directory `/workspaces/firestarter_app`. Create `tests/test_wire_dict_equivalence.py`.

Module docstring per S-1: `Phase 148 Plan 01 -- DATA-03 (D-14, D-06)`, a "Defect class this closes"
paragraph (a decode or `_map_data` change that silently alters what reaches the firmware, which
GATE-03 cannot catch because it only asserts `vpp_mv` present / `vpp` absent), then a numbered
`Coverage:` list naming each test function.

Path resolution self-contained per S-2 — `_HERE = Path(__file__).resolve().parent`,
`_GOLDEN = _HERE / "golden" / "wire_dict_baseline.json"`. **Not** in `conftest.py`.

Build the live capture in a module-level helper `_capture_wire_dicts()` that constructs
`EpromDatabase(skip_local_override=True)` and reuses `tools/check_dispatch.py:368-370`'s shape
(`db.get_eprom(part)` -> `db.convert_to_programmer(mapped)`), keyed `f"{mfg}|{pn}|{i}"`. Capture
once at module level so the 0.31 s cost is paid once.

Write a `_describe_record_diff(recorded, live)` helper modelled on
`tests/test_chip_database_field_inventory.py:171-191`'s `_describe_counter_diff` — report
`added=`, `removed=`, `changed=` separately, and for changed records name the differing wire keys
rather than dumping both dicts.

The non-vacuity leg must drive `_describe_record_diff` (the same helper the real test calls), not a
parallel reimplementation: deep-copy the recorded records, mutate one record's `pulse-delay`, and
assert the helper reports exactly that one record as changed.

**Do not copy `test_audit_coverage_matrix.py`'s `pytest.skip` for a missing input.** That skip
exists only because its input lives outside the sub-repo; `wire_dict_baseline.json` lives inside
`tests/golden/`, so a skip here would make the phase's central proof invisible in CI. Assert
existence with a message naming the path and Plan 01.

Annotations: `target-version = "py39"`, so `list[str]` / `dict[str, int]` are fine unquoted but
`X | None` is not — add `from __future__ import annotations` if you need one (S-8).
  </action>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_wire_dict_equivalence.py -o addopts="" -q` passes with **5**
      tests collected and 0 failures, **on the unmodified pre-change tree**.
    - The module contains no `pytest.skip` **call** and no `skip` **decorator** — asserted by
      parsing the file with `ast`, not by a substring count. The action tells the executor to
      explain in prose why this module deliberately has no skip path, so a text count would flag
      the module's own rationale.
    - `grep -q "skip_local_override=True" tests/test_wire_dict_equivalence.py` succeeds.
    - The non-vacuity test proves the comparison helper raises / reports a diff on a mutated copy.
    - `python3 -m ruff check tests/test_wire_dict_equivalence.py` and
      `python3 -m ruff format --check tests/test_wire_dict_equivalence.py` both pass.
    - `git diff --quiet firestarter/ tools/` — the test added no source change.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_wire_dict_equivalence.py -o addopts="" -q && grep -q "skip_local_override=True" tests/test_wire_dict_equivalence.py && python3 -c "
import ast
bad=[]
for n in ast.walk(ast.parse(open('tests/test_wire_dict_equivalence.py').read())):
    if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='skip': bad.append(('call',n.lineno))
    if isinstance(n,(ast.FunctionDef,ast.ClassDef)):
        for d in n.decorator_list:
            if 'skip' in ast.dump(d): bad.append(('decorator',n.lineno))
assert not bad, f'skip path present at {bad}'
print('no skip path OK')
" && python3 -m ruff check tests/test_wire_dict_equivalence.py && python3 -m ruff format --check tests/test_wire_dict_equivalence.py && git diff --quiet firestarter/ tools/ && echo "PLAN-01-T2 OK"</automated>
  </verify>
  <done>The equivalence test is committed, green against the pre-change tree, contains no skip
  path, and its non-vacuity leg proves it can fail.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured pre-change state in the D-12 review artifact</name>
  <files>.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md</files>
  <read_first>
    - `148-CONTEXT.md` D-12 — what the artifact must carry.
    - `148-RESEARCH.md` §"Gates — commands, current state, and fail-open traps" — the four gate
      commands and their measured current output.
    - `148-RESEARCH.md` §"F-2" — why "744 changed chips" is the correct pre-change reading and
      "zero diff rows" is unachievable.
    - `.planning/PROJECT.md` §"Evidence ceiling — binding, not decorative".
  </read_first>
  <action>
Create `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md` (in the
**meta** repo, not the sub-repo) with a `## Before` section only — later plans append `## After`,
the mover list, the justification and the non-claim.

Run each command from `/workspaces/firestarter_app` and paste its **actual** output, not a
description:
  - `python3 tools/diff_db.py ; echo EXIT=$?` — expect the changed-chip total **744**, buckets
    `PGSZ_PAGE_SIZE` 2 and `PROV01_PROTECT_METADATA` 742, `NEW chips (0)`, `MISSING chips (0)`,
    `EXIT=0`.
  - `python3 tools/check_dispatch.py ; echo EXIT=$?` — expect 746 scanned, 736 supported,
    0 violations, `EXIT=0`.
  - `python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q` — expect
    8 passed.
  - `firestarter info AT28C256` — expect the `VCC:` row to read `4.0v` and the `VPP:` row `12.0v`.
    Run it with `FIRESTARTER_CONFIG_DIR` pointed at an empty scratch dir so no user override leaks.

Immediately under the `diff_db` block, add a short note stating **why 744 is the correct baseline**
— `tools/baseline/chip_database.baseline.json` predates Phase 136.1, so 744 of 746 chips already
diff and are already explained. Record that the baseline is **NOT re-pinned** by this phase
(**D-11**), and that a criterion phrased "diff_db reports zero changed chips" would be a false RED.

Add an `## Evidence Ceiling` section stating verbatim that this phase corrects data the generator
emits and makes **no claim** about AT28C silicon behaviour: `0x0D` stays `UNVERIFIED`, no
`support_status` changes, and gh#21 / #32 / #11 / #12 stay OPEN.

Commit this file to the **meta** repo, staging only this path — the meta working tree is dirty
(untracked `.claude/skills/`, `package*.json`, submodule gitlinks). Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `148-DB-DIFF.md` exists in the phase directory and contains a `## Before` heading.
    - It contains the literal strings `744`, `PGSZ_PAGE_SIZE`, `PROV01_PROTECT_METADATA`, `EXIT=0`,
      `0 violations` and `4.0v`.
    - It contains the string `NOT re-pinned` and cites `D-11`.
    - It contains an `## Evidence Ceiling` heading and the string `UNVERIFIED`.
    - It does **not** contain the phrase `zero changed chips` other than inside the note explaining
      why that phrasing is wrong.
    - `git -C /workspaces status --porcelain` shows the file staged/committed and no other
      `.planning/` path modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>F=/workspaces/.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md && test -f "$F" && grep -q '^## Before' "$F" && grep -q '744' "$F" && grep -q 'PGSZ_PAGE_SIZE' "$F" && grep -q 'PROV01_PROTECT_METADATA' "$F" && grep -q 'EXIT=0' "$F" && grep -q '0 violations' "$F" && grep -q '4\.0v' "$F" && grep -q 'NOT re-pinned' "$F" && grep -q 'D-11' "$F" && grep -q '^## Evidence Ceiling' "$F" && grep -q 'UNVERIFIED' "$F" && echo "PLAN-01-T3 OK"</automated>
  </verify>
  <done>The review artifact exists with a measured `## Before` section, the F-2 note explaining why
  744 is the correct baseline, the D-11 no-re-pinning statement, and the Evidence Ceiling
  non-claim.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols and paths created by **this plan** (exclude these from drift verification — they do not
exist upstream):

| Kind | Name / path |
|---|---|
| new file | `firestarter_app/tests/golden/wire_dict_baseline.json` |
| new file | `firestarter_app/tests/test_wire_dict_equivalence.py` |
| new file | `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-DB-DIFF.md` |
| new JSON keys | `meta.why_nine_keys`, top-level `records` (in `wire_dict_baseline.json`) |
| new test helpers | `_capture_wire_dicts`, `_describe_record_diff` |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware wire | `convert_to_programmer`'s output becomes bytes the firmware acts on; a wrong `vpp_mv` or `pulse-delay` energizes a rail against real silicon |
| committed golden -> CI gate | a golden that can be silently regenerated is a gate that can be silenced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Tampering | `tests/golden/wire_dict_baseline.json` | mitigate | `meta.how_to_update` states in-file that the capture must not be regenerated to silence a failure; Task 1 pins the exact byte count and SHA-256 so a re-capture is detectable |
| T-148-02 | Information disclosure | capture via `EpromDatabase()` | mitigate | `skip_local_override=True` is mandatory so a developer's `~/.firestarter/database.json` cannot leak a private chip row into a committed fixture |
| T-148-03 | Denial of service | `pytest.skip` on a missing golden | mitigate | no skip path — a missing golden is a loud failure naming the path (Task 2 acceptance criterion) |
| T-148-SC | Tampering | npm/pip/cargo installs | accept | **no package installs in this plan** — pytest, syrupy and ruff are already present and green; no dependency is added, so the legitimacy gate is not engaged |
</threat_model>

<verification>
Run from `/workspaces/firestarter_app`:

    python3 -m pytest tests/test_wire_dict_equivalence.py -o addopts="" -q
    python3 tools/diff_db.py ; echo EXIT=$?
    python3 tools/check_dispatch.py ; echo EXIT=$?
    git diff --quiet tools/check_dispatch.py && echo "GATE-03 file byte-unchanged"
    git diff --quiet tests/__snapshots__/test_characterization.ambr && echo "snapshots byte-unchanged"

All four gates must still read exactly as they did before this plan — this plan changes no
production code.
</verification>

<success_criteria>
- `tests/golden/wire_dict_baseline.json` committed: 746 records, canonical blob 332,716 bytes,
  SHA-256 `027a43a0dcef1085afa6a35d2500bd35556140dde4b838dfcd65bfae8cac7dab`.
- `tests/test_wire_dict_equivalence.py` committed and green pre-change, with a non-vacuity leg and
  no skip path.
- `148-DB-DIFF.md` carries the measured `## Before` state and the Evidence Ceiling non-claim.
- `git diff --quiet firestarter/ tools/` inside `firestarter_app` — zero production-code change.
</success_criteria>

<output>
Create `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-01-SUMMARY.md` when done.
</output>
