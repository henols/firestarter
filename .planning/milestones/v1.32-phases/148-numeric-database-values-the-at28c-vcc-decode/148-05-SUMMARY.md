---
phase: 148-numeric-database-values-the-at28c-vcc-decode
plan: 05
subsystem: tooling
tags: [audit-coverage-matrix, golden-regen, database, numeric-values, defect-ledger]

requires:
  - phase: 148-03
    provides: "build_db.py's numeric mv/us schema (vcc_mv/vdd_mv/vpp_mv/pulse_duration_us) emitted and regenerated into chip_database.json"
  - phase: 148-04
    provides: "database.py's coercion layer deleted; format_mv shared render helper; 6 of the 19 transient test failures retired"
provides:
  - "tools/audit_coverage_matrix.py's second live string parser (parse_pulse_us) deleted; all eight reads (seven call sites + the render at _enum_row) go straight to the integer key pulse_duration_us"
  - "_members_with_parseable_pulse's string-endswith filter rewritten to a single != 0 test (D-09/D-08: 0 means algorithm-controlled by construction)"
  - "tests/golden/v1.3-COVERAGE-MATRIX.md regenerated by the tool: 297 per-chip pulse cells now render as bare integers; the 6 pulse_bucket label lines unchanged"
  - "Measured proof: DEFECT-COV-NN ledger is byte-identical (68/68 entries) between the pre-Phase-148 baseline and the post-migration state — the coverage-ledger has zero signature drift from this schema change"
affects: [148-07, 148-08]

tech-stack:
  added: []
  patterns:
    - "D-09: a render site surviving an int unchanged (_md_escape) still forces a golden regen when the underlying value's string suffix disappears — the render function itself needed no code change, only the golden"
    - "!= 0 replaces isinstance(x, str) and x.endswith(...) as the 'is this pulse algorithm-controlled' predicate once the schema guarantees an int"

key-files:
  created: []
  modified:
    - firestarter_app/tools/audit_coverage_matrix.py
    - firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md

key-decisions:
  - "The _emit_algo_summary wide-scan try/except (ValueError, KeyError) -> '(malformed)' bucket was removed outright rather than narrowed, because the integer schema guarantees pulse_duration_us is always a parseable int (no test asserted on the '(malformed)' label, confirmed by grep over tests/test_audit_coverage_matrix.py before the edit) and D-10's must-raise-on-missing-key discipline applies here exactly as it did in 148-04's database.py work"
  - "Golden regeneration must seed the scratch ledger from the committed meta-repo ledger (.planning/v1.3-defect-coverage-ids.json, read-only copy), not from an empty {} ledger — the tool's byte-identity test (test_golden_file_matches) does the same seeding, and a golden generated from a fresh empty ledger produces a different byte stream (differing DEFECT-COV-NN numbering) that fails that test. Discovered via a first-pass regen attempt that left 1 of 10 tests red; corrected before committing"
  - "Task 3 required no its own commit: the full-suite run, the four gate re-checks, and the DEFECT-COV-NN ledger measurement are all verification activity with zero code/golden changes beyond what Tasks 1-2 already committed"

requirements-completed: []

coverage:
  - id: D1
    description: "parse_pulse_us deleted outright (not widened to accept ints, not kept as passthrough); all seven call sites plus the _enum_row render site go straight to chip['programming']['pulse_duration_us']; _members_with_parseable_pulse rewritten to a single != 0 test with an updated docstring; the wide-scan prose mention at :1562 no longer names the deleted function"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "grep -c parse_pulse_us == 0; grep -c 'endswith(\" us\")' == 0; grep -c pulse_duration_us >= 8; ast-based scan finds zero remaining ['pulse_duration'] subscripts or .get('pulse_duration', ...) calls; except-Exception count unchanged (0 before, 0 after) -- all pass"
        status: pass
    human_judgment: false
  - id: D2
    description: "tests/golden/v1.3-COVERAGE-MATRIX.md regenerated by the tool (never hand-edited), 297 ' us' value-cell lines become bare integers, the 6 pulse_bucket label lines (< 100 us, 100-999 us) are untouched, a fresh regen (seeded from the committed meta ledger) is byte-identical to the committed golden, and the --check contract exits 1 on an empty ledger / 0 on a freshly generated one"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "grep -cE '\\| [0-9]+ us' golden == 0; grep -c '100-999 us' golden >= 1; cmp against a fresh regen -- byte-identical; --check rc=1 on {} ledger, rc=0 on fresh ledger; python3 -m pytest tests/test_audit_coverage_matrix.py -o addopts=\"\" -q -- 10 passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "Full suite restored: the 10 test_audit_coverage_matrix.py transients this plan owns are GREEN. The remaining 3 (test_chip_database_field_inventory.py) are the 148-07-owned frozen-golden mismatches, confirmed untouched and unregressed by this plan's changes. All four repo gates (build_db.py, diff_db.py, check_dispatch.py, ruff check+format) pass; GATE-03 and the characterization snapshot are byte-unchanged"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "python3 -m pytest -o addopts=\"\" -q -- 3 failed, 1623 passed, 220.21s (count line visible); the 3 failures are exclusively test_chip_database_field_inventory.py::{test_programming_field_inventory_matches, test_electrical_field_inventory_matches, test_generator_emits_no_key_outside_the_frozen_inventory}; build_db.py/diff_db.py/check_dispatch.py all exit 0; ruff check + ruff format --check clean; git diff --quiet on tools/check_dispatch.py and tests/__snapshots__/test_characterization.ambr both clean"
        status: pass
    human_judgment: false
  - id: D4
    description: "RESEARCH Open Question 3 settled by measurement, not assumption: extracted the pre-Phase-148 tool (commit 568e58b) and pre-Phase-148 chip_database.json (same commit, old string schema), ran the old tool against the old DB to produce a 'before' ledger, compared its DEFECT-COV-NN key set and full byte content against a freshly generated 'after' ledger from the current numeric-schema state -- both are byte-identical (68/68 entries, zero additions, zero removals). The meta ledger needs no update; the pre-existing 78-vs-68 drift (121-01 note) predates and is unrelated to Phase 148"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "git show 568e58b:tools/audit_coverage_matrix.py + git show 568e58b:firestarter/data/chip_database.json run via FIRESTARTER_DB_FILE override -> before.json; python3 tools/audit_coverage_matrix.py (current state) -> after.json; diff before.json after.json -- byte-identical; Python set comparison confirms identical 68-entry key sets"
        status: pass
    human_judgment: false

duration: ~35min
completed: 2026-08-19
status: complete
---

# Phase 148 Plan 05: audit_coverage_matrix.py Coercion-Layer Deletion & Golden Regen Summary

**Deleted `tools/audit_coverage_matrix.py`'s `parse_pulse_us` — the second live string parser DATA-03 targeted — moved all eight reads to direct integer indexing, regenerated the tool-produced coverage-matrix golden (297 pulse cells become bare integers), and proved by direct ledger-content comparison that the DEFECT-COV-NN defect signatures are completely unaffected by the schema migration.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-19T11:05:00Z (approx, from first read)
- **Completed:** 2026-08-19T11:40:00Z (approx, from last verification)
- **Tasks:** 3 completed (Task 3 required no additional commit)
- **Files modified:** 2

## Accomplishments

- Deleted `parse_pulse_us` (`tools/audit_coverage_matrix.py:106-110`) outright — no widening to accept ints, no passthrough kept.
- Rewrote all seven call sites (`:307`, `:849`, `:1165`, `:1178`, `:1196`, `:1636`, `:1730` in the pre-edit file) to direct-index `chip["programming"]["pulse_duration_us"]`. No site uses `.get(..., 0)`: a missing key raises, consistent with 148-04's D-10 discipline (a silently-defaulted 0 would misclassify a chip as algorithm-controlled).
- The `_emit_algo_summary` wide-scan site's `try/except (ValueError, KeyError): us = -1` → `"(malformed)"` bucket branch was removed entirely rather than narrowed — the integer schema guarantees the value is always present and parseable, and no test named or asserted on the `"(malformed)"` label.
- Rewrote `_members_with_parseable_pulse`'s `isinstance(pd, str) and pd.endswith(" us")` string filter to a single `chip["programming"]["pulse_duration_us"] != 0` test; updated its docstring to describe "a non-zero integer microsecond value" and note that `0` means algorithm-controlled by construction (D-08's fatal-branch discipline in `interpret_timing` makes any other zero impossible).
- Left `_enum_row` (`:537`) untouched apart from the key rename — `_md_escape` survives an int unchanged, so the rendered cell legitimately changes from `100 us` to `100`.
- Updated the wide-scan module-docstring prose (`:1562`) that named the now-deleted `parse_pulse_us`.
- Regenerated `tests/golden/v1.3-COVERAGE-MATRIX.md` via `python3 tools/audit_coverage_matrix.py --output <scratch> --ledger <scratch, seeded from a read-only copy of the committed meta-repo ledger>` — **exact command used**, see "Regeneration Command" below. Copied the scratch output over the golden; never hand-edited. 297 of 303 ` us`-carrying lines changed to bare integers; the other 6 (`pulse_bucket` labels `< 100 us` / `100-999 us`) are byte-unchanged, matching the plan's measured prediction exactly.
- Proved the `--check` three-step contract: clean generate → exit 0; `--check` against an empty `{}` ledger → exit 1 (drift gate is not vacuous); `--check` against the step-1 ledger → exit 0.
- Full suite: **1623 passed, 3 failed** (count line visible, 220.21s runtime). The 3 failures are exclusively `tests/test_chip_database_field_inventory.py`'s three tests — the 148-07-owned frozen-inventory golden, explicitly out of this plan's scope per the prior-wave context and untouched.
- All four repo gates green: `build_db.py` (regen idempotent, zero diff), `diff_db.py` (744/2/742 distribution unchanged), `check_dispatch.py` (0 violations), `ruff check` + `ruff format --check` clean. `git diff --quiet` clean on both `tools/check_dispatch.py` and `tests/__snapshots__/test_characterization.ambr`.
- Settled RESEARCH Open Question 3 by direct measurement (not assumption): extracted the pre-Phase-148 tool and database (commit `568e58b`, the v1.30 merge base, both on the old string schema) into a scratch dir, ran the old tool against the old DB via `FIRESTARTER_DB_FILE` override to produce a "before" ledger, and diffed it byte-for-byte against a freshly generated "after" ledger from the current (numeric-schema, post-148-05) state. **The two ledgers are byte-identical: 68/68 `DEFECT-COV-NN` entries, zero additions, zero removals.** The coverage ledger's defect signatures are completely unaffected by the mv/us schema migration; `pulse_bucket`'s int-based bucketing was already schema-agnostic before this phase touched anything. The committed meta ledger's 78-vs-68 drift (noted in the planner_decisions and the standing 121-01 note) predates Phase 148 entirely and is not addressed by this plan, per explicit instruction.

### Regeneration Command (exact)

```bash
S=$(mktemp -d)
cp /workspaces/.planning/v1.3-defect-coverage-ids.json "$S/l.json"
python3 tools/audit_coverage_matrix.py --output "$S/m.md" --ledger "$S/l.json"
cp "$S/m.md" tests/golden/v1.3-COVERAGE-MATRIX.md
rm -rf "$S"
```

**Note on seeding:** the golden must be regenerated with the scratch ledger seeded from a *read-only copy* of the committed meta-repo ledger (`.planning/v1.3-defect-coverage-ids.json`), matching exactly what `tests/test_audit_coverage_matrix.py::test_golden_file_matches` does internally. A first attempt seeded from an empty `{}` ledger instead; that produced a byte-different (differently-numbered `DEFECT-COV-NN`) golden that failed `test_golden_file_matches` (1 of 10 tests still red). Caught by running the full `test_audit_coverage_matrix.py` suite before committing, corrected, and re-verified with `cmp` byte-identity before commit. The committed meta-repo ledger file itself was never written or modified — only read via `cp` into a scratch path outside both repos.

## Regeneration-run file-write audit (WATCH ITEM)

Per the submodule protocol's watch item, snapshotted `git status --porcelain` in both repos before the first regeneration run and diffed after each of the ~6 tool invocations performed during this plan (Task 1's verify run, Task 2's golden regen + 3-step `--check` sequence, Task 3's DEFECT-COV-NN before/after measurement). In every case:

- `git -C /workspaces status --porcelain .planning/v1.3-COVERAGE-MATRIX.md .planning/v1.3-defect-coverage-ids.json` was empty both before and after.
- No other meta-repo tracked file changed state as a side effect of any invocation.

**Finding: the tool wrote nothing to any meta-repo tracked file during this plan's execution.** Every invocation passed explicit `--output`/`--ledger` into a `mktemp -d` scratch path (or, for the historical "before" comparison, a scratch path holding an extracted old-commit database — never the live meta-repo paths). This confirms `_REPO_ROOT`'s dangerous defaults (`T-148-15` in the threat register) were never exercised; the mitigation held throughout.

## Task Commits

Each task committed atomically inside the `firestarter_app` submodule, on branch `gsd/v1.32-at28c-write-path-root-cause-report-provenance`:

1. **Task 1: Delete parse_pulse_us and move all eight reads to direct integer indexing** - `53fefa1` (feat) — `firestarter_app`
2. **Task 2: Regenerate the coverage-matrix golden with the tool, and prove the --check contract** - `6e4b31a` (docs) — `firestarter_app`
3. **Task 3: Restore the full suite to green and measure the defect-signature stability** — no commit (pure verification/measurement; zero files changed beyond what Tasks 1-2 already committed)

## Files Created/Modified

- `firestarter_app/tools/audit_coverage_matrix.py` — `parse_pulse_us` deleted; all eight reads (seven call sites + the `_enum_row` render) direct-index `pulse_duration_us`; `_members_with_parseable_pulse` rewritten to a `!= 0` test; the `_emit_algo_summary` try/except malformed-bucket branch removed; the wide-scan prose mention updated
- `firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md` — regenerated by the tool; 297 per-chip pulse-cell lines changed from `N us` to bare integers; the 6 `pulse_bucket` label lines unchanged

## Decisions Made

- Removed (not narrowed) the `_emit_algo_summary` try/except malformed-pulse branch — the integer schema makes the "(malformed)" outcome structurally unreachable, and D-10's must-raise-on-missing-key discipline (established in 148-04) applies identically here.
- Golden regeneration seeds from the committed meta-repo ledger (read-only copy), matching the byte-identity test's own seeding — required for `test_golden_file_matches` to pass, since `DEFECT-COV-NN` numbering depends on ledger seed state.
- Task 3 required no additional commit since it is purely verification and measurement work.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, self-caught during Task 2] First golden regeneration attempt seeded from an empty ledger instead of the committed one**
- **Found during:** Task 2 verification (running the full `test_audit_coverage_matrix.py` suite before committing)
- **Issue:** The plan's Task 2 action describes running the tool with `--output`/`--ledger` into a scratch path without specifying ledger seed content. A first pass generated the golden from a scratch-path ledger seeded from `{}` (empty). This produced a byte-different golden (different `DEFECT-COV-NN` numbering in the ledger-dependent finding-count rendering) than what `test_golden_file_matches` expects, since that test seeds its own comparison ledger from the committed meta-repo ledger.
- **Fix:** Re-ran the regeneration with the scratch ledger seeded from a read-only copy of `/workspaces/.planning/v1.3-defect-coverage-ids.json`, matching the test's seeding exactly. Re-verified byte-identity via `cmp` before committing.
- **Files modified:** `firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md` (corrected before the commit was made; no separate fix commit needed)
- **Verification:** `python3 -m pytest tests/test_audit_coverage_matrix.py -o addopts="" -q` — 10 passed (was 9 passed / 1 failed before the correction)
- **Committed in:** `6e4b31a` (the corrected version was what was committed; the incorrect first-pass file was never committed)

---

**Total deviations:** 1 self-caught-and-fixed (Rule 1), corrected before any commit.
**Impact on plan:** None on the committed state — the incorrect intermediate regeneration was never committed. The corrected regeneration command is documented above and matches exactly what the byte-identity test performs internally.

### Plan-vs-context discrepancy (acknowledged, not a deviation from execution scope)

The PLAN.md's Task 3 acceptance criteria literally state "0 failed" for the full suite. This plan's assigned execution context (the `<prior_wave_context>` block, corroborated by `148-04-SUMMARY.md`'s "Next Phase Readiness" section) explicitly states that the 3 `test_chip_database_field_inventory.py` failures are owned by Plan 148-07, are expected to stay red through this plan, and that `tests/golden/chip_database_field_inventory.json` must not be touched or fixed here. Per the scope-boundary rule (only fix issues directly caused by this plan's own changes) and the explicit phase-level ownership split established across 148-03/148-04/148-05/148-07's summaries, this plan did **not** touch `test_chip_database_field_inventory.py` or its golden. The measured final state — 3 failed (148-07's), 1623 passed — matches the explicitly-declared expected end state for this plan, not the PLAN.md's literal (and, per the phase-level ownership record, superseded) "0 failed" text.

## Issues Encountered

None beyond the one self-caught-and-corrected deviation above.

## User Setup Required

None — no external service configuration required.

## Requirements Handling

**DATA-03 is left `Pending`** in both `REQUIREMENTS.md` and `ROADMAP.md`, per this plan's explicit success-criteria instruction: "DATA-03 is also claimed by 148-08 — leave it Pending." This plan retires the second of DATA-03's two named live string parsers (`audit_coverage_matrix.py`'s `parse_pulse_us`, following `database.py`'s coercion layer in 148-04), but 148-08 is the last plan claiming this requirement and is the one that flips the checkbox. `roadmap.update-plan-progress` was run to update the Phase 148 plan-count row (5/8 plans complete); the requirement traceability table itself is unchanged beyond that automated row update.

## Next Phase Readiness

Full suite state after this plan: **1623 passed, 3 failed** (up from 148-04's baseline of 1613 passed / 13 failed — exactly the 10 `test_audit_coverage_matrix.py` transients this plan owned are now green, 0 net new failures). The 3 remaining failures are exclusively:

- `tests/test_chip_database_field_inventory.py::test_programming_field_inventory_matches`
- `tests/test_chip_database_field_inventory.py::test_electrical_field_inventory_matches`
- `tests/test_chip_database_field_inventory.py::test_generator_emits_no_key_outside_the_frozen_inventory`

All three are the frozen-inventory golden mismatch from Plan 03's schema migration (added=`pulse_duration_us`/`vcc_mv`/`vdd_mv`, removed=`pulse_duration`/`vcc`/`vdd`/`vpp`), confirmed unchanged in cause and unregressed by this plan. Plan 148-07 is the named owner and should update `tests/golden/chip_database_field_inventory.json` to reflect the new numeric field set.

No blockers for Plan 148-07 or 148-08.

## Self-Check: PASSED

- FOUND: `.planning/phases/148-numeric-database-values-the-at28c-vcc-decode/148-05-SUMMARY.md`
- FOUND: `firestarter_app/tools/audit_coverage_matrix.py` (modified, contains no `parse_pulse_us`, contains `pulse_duration_us`)
- FOUND: `firestarter_app/tests/golden/v1.3-COVERAGE-MATRIX.md` (modified, 297 lines changed)
- FOUND commit `53fefa1` (firestarter_app, Task 1)
- FOUND commit `6e4b31a` (firestarter_app, Task 2)

All items verified present on disk and in git history. No missing items.

---
*Phase: 148-numeric-database-values-the-at28c-vcc-decode*
*Completed: 2026-08-19*
