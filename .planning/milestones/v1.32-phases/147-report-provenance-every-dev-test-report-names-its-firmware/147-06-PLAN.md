---
phase: 147-report-provenance-every-dev-test-report-names-its-firmware
plan: 06
type: execute
wave: 4
depends_on: ["147-01", "147-03", "147-05"]
files_modified:
  - .claude/skills/devtest-triage/scripts/devtest_issues.py
  - .claude/skills/devtest-triage/SKILL.md
  - .claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md
  - .claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md
commits_land_in:
  - "/workspaces (meta repo) — ALL four files. This plan makes NO commit in firestarter_app and needs no gitlink bump."
autonomous: false
requirements: [PROV-05, PROV-06]
must_haves:
  truths:
    - "The devtest-triage skill's `show` render names the firmware identity, so the surface triage actually reaches for can attribute a report"
    - "The same render no longer prints a bare null for the hardware revision"
    - "When the identity is absent, the render says so and says the report is not attributable, with the action to take"
    - "SKILL.md's documented example matches what the script now prints"
  artifacts:
    - ".claude/skills/devtest-triage/scripts/devtest_issues.py — its own NOT_REPORTED and NOT_ATTRIBUTABLE literals plus the firmware line in cmd_show"
    - ".claude/skills/devtest-triage/SKILL.md — a regenerated §2 transcript"
    - "two committed fixture bodies (populated + null identity) that make the render verifiable offline and hermetically"
  key_links:
    - "The skill script may not import from firestarter_app (skills own their scripts) and lives in a different repo, so its marker literal is the third of three and its parity is carried by this plan's checkpoint, not by an automated test"
    - "An app-repo test subprocess-invoking /workspaces/.claude/ fails OPEN in standalone CI — that is why W-4 is a checkpoint and must not be converted to a test"
    - "The .gitignore un-ignore and the skill baseline commit from plan 147-01 are the precondition for committing anything here"
---

<objective>
Give the devtest-triage skill's `show` render the firmware identity, fix its bare-null hardware-revision
rendering, commit two fixture bodies that make the render verifiable offline, and have a human confirm
both the render and the attribution judgement.

Purpose: D-16 established that PROV-06 has **two** parser surfaces, not one. The named surface is the
app's, but the surface triage actually reaches for is this skill's — and RESEARCH reproduced its current
output: `host 3.0.0b21   hw None`, with no firmware identity anywhere in the render. Meanwhile
`SKILL.md` reproduces that render verbatim as its documented example, and a documented example that no
longer matches its script is a trust defect.

Output: the third and last marker literal, a firmware line in the render, a regenerated transcript, two
committed fixtures, and the two manual oracles VALIDATION.md identifies as manual **by necessity**.

**Evidence Ceiling (binding, PROJECT.md §Current Milestone: v1.32).** The not-attributable clause says
a report cannot be tied to a firmware version and asks for a fresh run. Nothing here may say or imply
the `0x0D` write path is proven, graduate `0x0D` out of `UNVERIFIED`, change any `support_status`, or be
phrased as closing gh#21/#32/#11/#12. This plan makes future reports attributable; it proves nothing
about the write path.

**No firmware change and no app-repo change.** Every file here is in the meta repo. `firestarter/` is
read-only reference; Phase 149 owns the only firmware-touching workstream.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-CONTEXT.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-RESEARCH.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-PATTERNS.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-VALIDATION.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-01-SUMMARY.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-05-SUMMARY.md
@/workspaces/.claude/skills/devtest-triage/SKILL.md
</context>

<artifacts_this_phase_produces>
Artifacts **created by this plan** (treat as new — none exists on disk before this plan):

- `NOT_REPORTED = "not reported"` — module constant in
  `.claude/skills/devtest-triage/scripts/devtest_issues.py`. Literal **#3 of 3**; identical in value to
  plan 147-03's and plan 147-05's, defined separately because this file is in a different repo and may
  not import from the app.
- `NOT_ATTRIBUTABLE` — module constant in the same file holding the D-14/D-17 clause:
  `"NOT attributable to a firmware version -- ask the reporter for a fresh dev test run on a current host build"`
- `.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md` — a frozen issue body
  whose `auto_capture.fw_board_identity` is `null` and whose `hw_revision` is populated.
- `.claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md` — a frozen issue body
  carrying `schema_version` `"1.4"` and `fw_board_identity` `"3.0.0b19:leonardo"`.
- A regenerated `## 2. Parse the report` transcript in `.claude/skills/devtest-triage/SKILL.md`.

Consumed from sibling plans, not created here: `NOT_REPORTED` in `firestarter/diagnostic_report.py` and
`SCHEMA_VERSION = "1.4"` (147-03); `NOT_REPORTED` and `_NOT_ATTRIBUTABLE` in
`tools/parse_devtest_issue.py` (147-05); the `.gitignore` un-ignore and the tracked skill baseline
(147-01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Commit two frozen fixture bodies and prove they parse on the unmodified script</name>
  <files>.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md, .claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md</files>
  <read_first>
    - `/workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py` — `parse_title`, `extract_report`, `fingerprint`, `MAX_BODY`, and `cmd_show`'s `--body-file` branch (which never shells out to `gh`, so the run is fully offline)
    - `/workspaces/.claude/skills/devtest-triage/SKILL.md` §2 — the transcript that shows exactly which fields the render reads
    - `/workspaces/firestarter_app/firestarter/submit.py` — `build_body` and `build_title`, the shape a real issue body has (read-only reference; **do not edit the app repo in this plan**)
    - `147-RESEARCH.md` §"Common Pitfalls" P-6 (the two verified offline commands and the measured current output) and §"Verified Findings" rows F-12, F-13, F-14
    - `147-VALIDATION.md` §"Wave 0 Requirements" item W-4 and §"Manual-Only Verifications"
  </read_first>
  <action>
    Hand-author two issue-body fixtures under a new `fixtures/` directory beside the skill's `scripts/`,
    each a realistic `[dev test]` body: the markdown `| Step | Verdict | Reason |` table followed by a
    fenced JSON code block (a triple-backtick fence tagged `json`), matching the shape
    `submit.py::build_body` produces. Detection requires both
    the `[dev test]` title marker (supplied on the command line via `--title`) and a fenced JSON object
    carrying a `schema_version` key.

    `dev-test-at28c256-null-identity.md` — model it on the real shape of the reports that motivated this
    milestone: `schema_version` `"1.2"`, `host_version` `"3.0.0b15"`, `fw_board_identity` **null**,
    `hw_revision` a populated coarse bucket string, `chip` `at28c256`, `protocol` `0x0D`, a
    `dedup_fingerprint`, a `db_diff` carrying a ladder state, a `voltage` block, and a `steps` list with
    at least one failing step. This is the fixture that exercises the marker and the not-attributable
    clause.

    `dev-test-at28c256-populated-identity.md` — the same body shape with `schema_version` `"1.4"`,
    `host_version` `"3.0.0b21"` and `fw_board_identity` `"3.0.0b19:leonardo"`. This is the fixture that
    exercises the labelled line and demonstrates what a report from a current host looks like.

    Put a comment line at the top of each `.md` file (outside the fenced JSON) stating that it is a
    **frozen** fixture for the skill-render verification and must never be regenerated from live
    `to_dict()` output — the null one in particular pins a shape a current host can no longer produce.

    Then prove both fixtures are valid inputs **before** any script change, by running the current,
    unmodified `show --body-file` on each. Both must parse (exit 0, a `#? at28c256 — FAIL` header, a
    fingerprint line, a `ROUTE:` line). Record the current output of both runs verbatim in the SUMMARY:
    that is the before-image the Task 3 checkpoint compares against, and it is what makes the
    checkpoint's judgement grounded rather than impressionistic. The null fixture's current output will
    contain a bare-null hardware-revision rendering and no firmware line at all — that is the defect,
    captured as evidence.

    Commit in the **meta** repo, staging only these two named paths. The meta tree is dirty (two
    submodule gitlinks, `package.json`, `package-lock.json`) — never `git add -A` or `git add .`, and do
    not stage either gitlink. Plan 147-01 already landed the `.gitignore` un-ignore that makes these
    paths addable at all; confirm that precondition holds rather than assuming it.
  </action>
  <acceptance_criteria>
    - `git ls-files .claude/skills/devtest-triage/fixtures` lists exactly the two `.md` paths
    - `git check-ignore -v .claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md` exits non-zero (the path is not ignored)
    - Behavior: `python3 /workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file /workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md --title '[dev test] at28c256 — FAIL'` exits 0 and its output contains `at28c256`, a `fingerprint ` line and a `ROUTE:` line
    - Behavior: the same command against the populated fixture exits 0 and its output contains `3.0.0b21` and a `ROUTE:` line
    - `grep -c '"fw_board_identity": null' fixtures/dev-test-at28c256-null-identity.md` prints `1`, and `grep -c '"fw_board_identity": "3.0.0b19:leonardo"' fixtures/dev-test-at28c256-populated-identity.md` prints `1` (relative to `/workspaces/.claude/skills/devtest-triage/`)
    - `grep -c '"schema_version": "1.2"' fixtures/dev-test-at28c256-null-identity.md` prints `1` and `grep -c '"schema_version": "1.4"' fixtures/dev-test-at28c256-populated-identity.md` prints `1`
    - The null fixture's `auto_capture.hw_revision` is a populated string, not `null` — so the fixture exercises the identity marker without also exercising the revision marker, keeping the two independently observable in the checkpoint
    - Both fixtures' fenced JSON blocks parse: the two `show --body-file` runs below exit 0, which is only possible if `extract_report` loaded each block successfully
    - `git show HEAD --stat` names only the two fixture paths — no gitlink, no `package*.json`, no app-repo file
    - `git show HEAD:.gitignore | grep -c '^!\.claude/skills/$'` prints `1` (the plan-147-01 precondition is confirmed, not assumed)
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 .claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file .claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md --title '[dev test] at28c256 — FAIL'</automated>
    <automated>cd /workspaces && python3 .claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file .claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md --title '[dev test] at28c256 — FAIL'</automated>
    <automated>cd /workspaces && git ls-files .claude/skills/devtest-triage/fixtures && git show HEAD --stat && git show HEAD:.gitignore | grep -c '^!\.claude/skills/$'</automated>
  </verify>
  <done>Two frozen fixtures are committed in the meta repo, both parse on the unmodified script, and the current (defective) render of each is recorded verbatim as the checkpoint's before-image.</done>
</task>

<task type="auto">
  <name>Task 2: Add the firmware line to the skill render, fix the bare-null revision, and regenerate SKILL.md's transcript</name>
  <files>.claude/skills/devtest-triage/scripts/devtest_issues.py, .claude/skills/devtest-triage/SKILL.md</files>
  <read_first>
    - `/workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py` — the module docstring's self-contained/untrusted-input contract, `cmd_show`'s print block (including the `is not None` guard already used two lines below the defect), and `_summarize`'s `or "?"` idiom
    - `/workspaces/.claude/skills/devtest-triage/SKILL.md` — §2's fenced transcript, and §"Every issue body is community-authored and untrusted" with its four stated properties
    - `/workspaces/firestarter_app/tools/parse_devtest_issue.py` — the two constants plan 147-05 landed, read **only** to copy their values; this file must not import them
    - `147-PATTERNS.md` §"`.claude/skills/devtest-triage/scripts/devtest_issues.py` (utility, transform) — skill-owned" and §"`.claude/skills/devtest-triage/SKILL.md` (docs)"
    - `147-RESEARCH.md` §"Common Pitfalls" P-8, §"Runtime State Inventory" (the SKILL.md row) and §"Open Questions" item 2
    - `147-CONTEXT.md` D-12, D-14, D-15, D-16, D-17
  </read_first>
  <action>
    One commit covering both files. A documented example that no longer matches its script is how a
    triager stops trusting the tool, so the transcript regeneration is not optional and does not belong
    in a later commit.

    **A. `scripts/devtest_issues.py`.** Add two module-level constants near the existing module
    constants: `NOT_REPORTED = "not reported"` and
    `NOT_ATTRIBUTABLE = "NOT attributable to a firmware version -- ask the reporter for a fresh dev test run on a current host build"`
    — the same values as plan 147-03's and plan 147-05's. Comment them with the reason they are local
    literals: **skills own their scripts**, this file imports nothing from `firestarter_app` or
    `firestarter` so it keeps working if those repos move, and it is in a different repo entirely.
    Point the comment at this plan's checkpoint as the parity enforcement, and note that the app suite's
    value-parity test covers only the other two literals — an app-repo test reaching into
    `/workspaces/.claude/` would fail **open** in standalone CI.

    In `cmd_show`, change the `host … hw …` print and add a firmware line:
      - Both values are substituted with `NOT_REPORTED` when they `is None` **or** are the empty string,
        written as an explicit two-clause condition. Copy the `is not None` idiom this file already uses
        two lines below for the chip-id pair; do **not** copy `_summarize`'s `or "?"` idiom, which also
        fires on `""` and would hide an empty-string transport fault (RESEARCH P-8). Leave
        `_summarize`'s existing `or "?"` alone — it belongs to the `list`/`fold` summary and is out of
        scope for D-16.
      - Add a `firmware` row printed in the same bare-`print(f"  label{spaces}{value}")` style as its
        neighbours, aligned with them. When the identity is present, print the value. When it is absent,
        print the marker followed by `NOT_ATTRIBUTABLE`. Place the firmware row directly after the
        `host … hw` row so provenance reads as one block.
    Keep the `hw` value on the host row and keep it rendering — D-15's "`hw_revision` stays out" governs
    what is **added** to a triage render, while D-16 governs fixing the bare-null rendering that is
    already there. Do not remove the `hw` field, and do not add a `hw_revision` row to the app parser.
    Add no `attributable` boolean (D-14) and no schema-version ordering logic (D-17).

    Preserve every property the module docstring states: stdlib only plus the `gh` CLI, no import from
    the app, body size-bounded before parsing, never `eval`/`exec`, never shelled out, never interpolated
    into a command. The `--body-file` branch must stay fully offline.

    **B. `SKILL.md`.** Regenerate §2's fenced transcript from **real** script output — run the null
    fixture through the edited script and paste what it prints, rather than hand-editing the old block.
    Keep it keyed to issue #32, whose real body genuinely carries no firmware identity: the resulting
    transcript then documents the not-attributable case on the exact issue that motivated this phase,
    which is honest, whereas showing a populated identity for #32 would fabricate output for a real
    report. Immediately after that block, add a short second snippet or a one-line note showing what a
    report from a current host renders instead (the populated fixture's firmware line), so a reader knows
    both shapes. Keep the addition small — §2 is a procedure section, not a reference.

    Add one sentence to §2 naming the two committed fixtures and the offline `show --body-file` form, so
    a future maintainer can reproduce the render without a live issue.

    `.claude/skills/` is not linted or type-checked by any CI job, so assert on **behaviour** here.
    Stage only the two named paths in the meta repo; no gitlink, no `package*.json`, no app-repo file.
  </action>
  <acceptance_criteria>
    - `python3 -c "import importlib.util as u; s=u.spec_from_file_location('d','/workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py'); m=u.module_from_spec(s); s.loader.exec_module(m); print(repr(m.NOT_REPORTED)); print(m.NOT_ATTRIBUTABLE)"` prints `'not reported'` and the clause text
    - Parity: the three literals are equal — `python3 -c` importing `NOT_REPORTED` from `firestarter.diagnostic_report`, from `tools.parse_devtest_issue`, and from the skill script by file path, printing that all three compare equal
    - `grep -cE '^\s*(from|import)\s+firestarter' .claude/skills/devtest-triage/scripts/devtest_issues.py` prints `0` — skills own their scripts
    - `grep -cE 'eval\(|exec\(|shell=True|os\.system' .claude/skills/devtest-triage/scripts/devtest_issues.py` prints `0` — the untrusted-input contract is intact
    - Behavior: `show --body-file` against the **null** fixture exits 0, prints a `firmware` line containing the marker and the not-attributable clause, and its `host` line shows the marker for the revision rather than a bare null rendering
    - Behavior: `show --body-file` against the **populated** fixture exits 0, prints `firmware` followed by `3.0.0b19:leonardo`, and prints the not-attributable clause zero times
    - Behavior: `_summarize`'s existing idiom is unchanged — `git diff -- .claude/skills/devtest-triage/scripts/devtest_issues.py` shows no change inside `_summarize`
    - The §2 transcript in `SKILL.md` matches the edited script's real output for the null fixture, verified by diffing the fenced block against a fresh run rather than by reading
    - `grep -c 'fixtures/dev-test-at28c256' .claude/skills/devtest-triage/SKILL.md` prints at least `1`
    - `grep -ciE 'verified fixed|confirmed working|now works|now proven|dev test proves|silicon' .claude/skills/devtest-triage/scripts/devtest_issues.py` prints `0`
    - `git show HEAD --stat` names exactly the two paths in this task's `files_modified` — no gitlink, no `package*.json`, no app-repo file
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && python3 -c "import importlib.util as u; s=u.spec_from_file_location('d','/workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py'); m=u.module_from_spec(s); s.loader.exec_module(m); print(repr(m.NOT_REPORTED)); print(m.NOT_ATTRIBUTABLE)"</automated>
    <automated>cd /workspaces/firestarter_app && python3 -c "
import importlib.util as u
from firestarter.diagnostic_report import NOT_REPORTED as A
from tools.parse_devtest_issue import NOT_REPORTED as B
s = u.spec_from_file_location('d', '/workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py')
m = u.module_from_spec(s); s.loader.exec_module(m)
C = m.NOT_REPORTED
assert A == B == C, (A, B, C)
print('all three marker literals equal:', repr(A))"</automated>
    <automated>cd /workspaces && OUT=$(python3 .claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file .claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md --title '[dev test] at28c256 — FAIL') && printf '%s\n' "$OUT" && printf '%s\n' "$OUT" | grep -c 'firmware' && printf '%s\n' "$OUT" | grep -c 'NOT attributable' && test "$(printf '%s\n' "$OUT" | grep -cE 'hw (None|null)$')" = "0" && echo "no bare null revision rendering"</automated>
    <automated>cd /workspaces && OUT=$(python3 .claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file .claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md --title '[dev test] at28c256 — FAIL') && printf '%s\n' "$OUT" && printf '%s\n' "$OUT" | grep -c '3.0.0b19:leonardo' && test "$(printf '%s\n' "$OUT" | grep -c 'NOT attributable')" = "0" && echo "populated case carries no not-attributable clause"</automated>
    <automated>cd /workspaces && test "$(grep -cE '^\s*(from|import)\s+firestarter' .claude/skills/devtest-triage/scripts/devtest_issues.py)" = "0" && test "$(grep -cE 'eval\(|exec\(|shell=True|os\.system' .claude/skills/devtest-triage/scripts/devtest_issues.py)" = "0" && echo "skill contracts intact"</automated>
  </verify>
  <done>The skill's `show` render names the firmware identity, marks an absent one not-attributable, no longer prints a bare null revision, and `SKILL.md`'s documented transcript is regenerated from real output in the same commit. All three marker literals are proven equal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human verification — the skill render (W-4) and the attribution judgement (criterion #5)</name>
  <files>.claude/skills/devtest-triage/scripts/devtest_issues.py, .claude/skills/devtest-triage/SKILL.md, .claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md, .claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md (all read-only at this task — no edit)</files>
  <read_first>
    - `147-VALIDATION.md` §"Manual-Only Verifications" — the two rows this task closes, and why each is manual by necessity
    - `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-06-SUMMARY.md` draft — the before-image of both renders recorded in Task 1
    - `.planning/ROADMAP.md` §"Phase 147" success criterion #5
    - `147-CONTEXT.md` D-14, D-15, D-17
  </read_first>
  <action>
    Do not edit any file in this task. The executor's job is to **produce the evidence and present it**,
    then stop at the gate and wait for a human verdict.

    Run all four render commands and paste their complete, unedited output into the checkpoint message,
    side by side with the before-image recorded in Task 1's SUMMARY entry:
      1. the skill's `show --body-file` against the **null**-identity fixture,
      2. the skill's `show --body-file` against the **populated**-identity fixture,
      3. the app parser `tools/parse_devtest_issue.py --title … --body-file …` against the **null** fixture,
      4. the same app parser against the **populated** fixture.
    All four are hermetic and offline: the skill's `--body-file` branch never shells out to `gh`, and the
    app parser reads only the file it is given.

    Also paste `SKILL.md` §2's fenced transcript beside command 1's output so the human can compare them
    line for line without opening a second file.

    Then present the two verdict questions from `<how-to-verify>` and **halt**. Do not self-approve, do
    not infer approval from the automated legs in Tasks 1–2, and do not record a verdict the human did
    not give. If the run is non-interactive, halt and surface the gate rather than passing it.
  </action>
  <what-built>
    Both triage surfaces now carry the firmware identity. The app's `render_diff` gained a labelled
    identity line plus a not-attributable clause (plan 147-05, automated tests green), and the
    devtest-triage skill's `show` render gained the same in Task 2 above, with its bare-null hardware
    revision fixed and `SKILL.md`'s documented transcript regenerated. Two frozen fixture bodies — one
    with a populated identity, one with `fw_board_identity: null` — are committed so both renders can be
    produced offline and hermetically.

    **These two oracles are manual by necessity, not by convenience.** VALIDATION.md §"Manual-Only
    Verifications" records why: the skill script has no test harness of any kind, and building one in the
    app repo would reach into `/workspaces/.claude/` and fail **OPEN** in standalone CI — the exact
    fail-open shape this project hit four times in Phase 117. And ROADMAP criterion #5 ("a triager can
    attribute the report to a firmware version without asking the reporter") is a judgement criterion;
    no assertion expresses "a human can answer this".

    **Do not auto-approve.** `--auto` / `--chain` auto-approves human-verify gates, and these two are
    the *only* proof for PROV-06's skill surface and for criterion #5. An auto-approval leaves both
    unproven while marking them done.
  </what-built>
  <how-to-verify>
    **Part A — the skill render is correct (W-4, PROV-05 / PROV-06).**

    1. Run the null-identity fixture:

       `python3 /workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file /workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md --title '[dev test] at28c256 — FAIL'`

       Expected: a `firmware` line reading `not reported` followed by the not-attributable clause; the
       `host` line showing `not reported` for the revision **only if** that fixture's revision is absent
       (this fixture's revision is populated, so it should show the real bucket string); no bare
       four-character rendering of a null value anywhere in the render; the `ROUTE:` line unchanged from
       the before-image recorded in the 147-06 Task 1 SUMMARY.

    2. Run the populated-identity fixture:

       `python3 /workspaces/.claude/skills/devtest-triage/scripts/devtest_issues.py show --body-file /workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md --title '[dev test] at28c256 — FAIL'`

       Expected: `firmware` followed by `3.0.0b19:leonardo`, and **no** not-attributable clause.

    3. Compare `SKILL.md` §2's fenced transcript against step 1's output. Every line of the documented
       example must match what the script actually printed. Confirm the added populated-case snippet or
       note is present and correct.

    4. Confirm the render still reads as one coherent block — the firmware line aligned with its
       neighbours, provenance grouped, nothing pushed off a normal terminal width.

    **Part B — attribution is answerable, and refusal is explicit (ROADMAP criterion #5, PROV-06).**

    5. From step 2's output alone, and from the app parser's render of the same body
       (`cd /workspaces/firestarter_app && python3 tools/parse_devtest_issue.py --title '[dev test] at28c256 — FAIL' --body-file /workspaces/.claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md`),
       answer: **which firmware version produced this report?** It must be answerable without asking the
       reporter anything.

    6. From step 1's output, and the app parser's render of the null body, answer the same question. The
       answer must be an **explicit refusal that names the next action** — not a blank, not a bare null,
       not silence. Confirm the clause tells you to ask for a fresh run on a current host build.

    7. Sanity-check the boundary the Evidence Ceiling draws: nothing in either render claims the `0x0D`
       write path works, is fixed, or is proven; nothing claims a support-status change; nothing reads as
       closing gh#21/#32/#11/#12. Both renders make a report *attributable*, and say so in those terms.
  </how-to-verify>
  <acceptance_criteria>
    - Operator-witnessed (Part A): the human confirms the skill's null-fixture render shows a `firmware`
      line carrying the marker plus the not-attributable clause, and shows no bare four-character
      rendering of a null value anywhere in its identity block.
    - Operator-witnessed (Part A): the human confirms the populated-fixture render shows
      `3.0.0b19:leonardo` on the `firmware` line and carries no not-attributable clause.
    - Operator-witnessed (Part A): the human confirms `SKILL.md` §2's fenced transcript matches command
      1's output line for line, and that the added populated-case snippet or note is present.
    - Operator-witnessed (Part B): the human states the firmware version that produced the populated
      report, sourced from the renders alone with no question asked of any reporter.
    - Operator-witnessed (Part B): the human confirms the absent case is an **explicit refusal naming
      the next action**, not a blank, a bare null, or silence.
    - Operator-witnessed (Part B): the human confirms neither render claims the `0x0D` write path works,
      is fixed or is proven; claims no support-status change; and does not read as closing
      gh#21/#32/#11/#12.
    - The four render commands' complete unedited output is recorded in `147-06-SUMMARY.md`, alongside
      Task 1's before-image, and the human's verdict on both parts is recorded verbatim.
  </acceptance_criteria>
  <verify>
    <human-check>
      A human runs or reads the four renders and returns an explicit verdict on Part A and Part B.
      **No automated oracle exists for either part, and none may be invented.** An app-repo test
      subprocess-invoking `/workspaces/.claude/` fails OPEN in standalone CI (RESEARCH P-6, the shape
      recorded four times in Phase 117), and criterion #5 is a judgement criterion no assertion can
      express. `--auto`/`--chain` auto-approves human-verify gates; if this gate is auto-approved,
      PROV-06's skill surface and ROADMAP criterion #5 are unproven and must be recorded as such rather
      than marked satisfied.
    </human-check>
  </verify>
  <resume-signal>
    Reply with a verdict on **each** part, e.g. `Part A approved, Part B approved` — or describe what is
    wrong. If Part A fails, name the line that differs. If Part B fails, say which of the two cases
    (populated / absent) could not be answered and what the render would need to say.
  </resume-signal>
  <done>A human has returned an explicit verdict on both parts, the four render outputs and that verdict are recorded verbatim in the plan SUMMARY, and no automated substitute was invented for either oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| community GitHub issue body → skill parser | Bodies are community-authored and hostile. The skill's docstring states the contract: size-bounded before parsing, never `eval`/`exec`d, never shelled out, never interpolated into a command, every extraction failing soft |
| skill render → maintainer decision | The `show` output is what triage acts on, and **no** claim gate scans this file |
| meta repo `git add` scope | The un-ignore admits `.claude/skills/` only; the rest of `.claude/` is local runtime state including a Discord token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-05 | Tampering / Denial of Service | the skill script's untrusted-body handling | medium | mitigate | Changes are confined to two constants and `cmd_show`'s print block. Task 2 asserts by grep that no `eval`/`exec`/`shell=True`/`os.system` exists and that no `firestarter` import was introduced, so the four stated properties are mechanically confirmed rather than assumed |
| T-147-18 | Repudiation | a fail-open harness for the skill render | high | mitigate | The verification is a blocking human-verify against two **committed** fixtures, explicitly **not** an app-repo test subprocess-invoking a meta-repo path. RESEARCH P-6 measured that such a test either errors for the wrong reason or skips — the fail-open shape recorded four times in Phase 117 |
| T-147-17 | Tampering | marker drift in the third literal | medium | mitigate | Task 2's parity leg imports the marker from all three modules — the report model, the app parser, and the skill script by file path — and asserts equality. That check runs from the app repo where both worlds are importable, so it does not create a meta-repo dependency inside the app test suite |
| T-147-15 | Tampering | the clause wording in a file no claim gate scans | medium | mitigate | Task 2 greps the skill script for the forbidden-claim vocabulary and for the word `silicon`; the checkpoint's step 7 is a human read of the same boundary. The gate's own target list is deliberately not widened — Phase 152 owns the outward-facing claim gate |
| T-147-07 | Information Disclosure | meta-repo `git add` scope | high | mitigate | Both tasks stage named paths only and assert `git show HEAD --stat` names exactly the expected files, so no `.claude/channels`, `.claude/settings*` or gitlink can ride along |
| T-147-04 | Spoofing / Repudiation | a board claiming an identity it does not run | low | accept | Both renders state what the board *claimed*. The clause states attributability, never authenticity, and the Evidence Ceiling forbids treating any report as proof — checked by the human in step 7 |
| T-147-SC | Tampering | npm/pip/cargo installs | high | accept | No install task exists; RESEARCH §"Package Legitimacy Audit" records zero proposed packages. Any new dependency is a scope escape that must stop the plan |
</threat_model>

<verification>
- Task 1 and Task 2 `<automated>` legs all exit 0, including the three-way marker parity leg.
- The blocking checkpoint is answered by a human with an explicit verdict on **both** parts. It must
  **not** be auto-approved: `--auto`/`--chain` auto-approves human-verify gates, and these are the only
  proof for PROV-06's skill surface and for ROADMAP criterion #5. If the run is automated, halt and
  surface the gate.
- `git log --oneline -3` in the meta repo shows two commits from this plan (fixtures, then script +
  `SKILL.md`) and no gitlink bump — this plan touches no app-repo file.
- `cd /workspaces/firestarter_app && git status --porcelain` prints no output: the app repo is untouched
  by this plan.
- `cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q` still exits 0 — the app
  suite is unaffected, which is itself the proof that no app↔meta coupling was introduced.
</verification>

<success_criteria>
- PROV-06 (skill surface): the `show` render names the firmware identity when present and explicitly
  refuses attribution with an action when absent; `SKILL.md`'s documented example matches real output.
- PROV-05 (skill surface): no bare rendering of a null value remains in the render's identity block;
  absent and empty values both show the explicit marker.
- All three marker literals are proven equal by an executed check, closing the gap D-11's
  architecturally-impossible single-sourcing leaves.
- ROADMAP criterion #5 is confirmed by a human on both fixtures: attribution answerable for the
  populated case, explicitly refused with a next action for the absent case.
- Evidence Ceiling intact: neither render claims anything about the `0x0D` write path, any
  support_status, or gh#21/#32/#11/#12.
</success_criteria>

<output>
Create `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-06-SUMMARY.md` when done. Record: the before-image of both fixture renders from Task 1, the after-image from Task 2, the three-way parity result, and the human's verdict on both checkpoint parts verbatim. State explicitly that the skill script's marker parity and the criterion-#5 judgement rest on the checkpoint, not on an automated test.
</output>
