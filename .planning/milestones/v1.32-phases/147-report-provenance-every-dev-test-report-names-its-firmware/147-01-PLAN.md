---
phase: 147-report-provenance-every-dev-test-report-names-its-firmware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .claude/skills/devtest-triage/SKILL.md
  - .claude/skills/devtest-triage/scripts/devtest_issues.py
commits_land_in:
  - "/workspaces (meta repo) — the .gitignore un-ignore hunk plus the first-ever tracking of .claude/skills/devtest-triage/ at its CURRENT content"
  - "firestarter_app (submodule) — NO commit; branch checkout only, no gitlink bump"
autonomous: true
requirements: []
must_haves:
  truths:
    - "firestarter_app HEAD is a v1.32 branch whose content equals origin/beta (app 3.0.0b21), not the v1.31 branch tip"
    - "The app test suite is green on that branch, with a recorded pass count that later plans can regress against"
    - ".claude/skills/devtest-triage/{SKILL.md,scripts/devtest_issues.py} are tracked in the meta repo, so plan 147-06's edits land as a reviewable diff instead of an untracked-file add"
  artifacts:
    - "firestarter_app branch gsd/v1.32-at28c-write-path-root-cause-report-provenance"
    - ".gitignore committed with .claude/* + !.claude/skills/"
  key_links:
    - "The .gitignore un-ignore must be committed BEFORE any commit that adds a .claude/skills/ file (F-14: git ls-files .claude is empty today and the un-ignore exists only in the working tree)"
    - "No code plan in this phase may run until the app repo is off gsd/v1.31-27c-programming-algorithm-fidelity"
---

<objective>
Establish the two repo-hygiene preconditions every other plan in Phase 147 depends on: move
`firestarter_app` onto a v1.32 branch forked off `origin/beta`, and make the devtest-triage skill
files committable-and-tracked in the meta repo.

Purpose: RESEARCH §"Sub-Repo Branch Precondition" measured that `firestarter_app` is still on
`gsd/v1.31-27c-programming-algorithm-fidelity`; the v1.32 charter requires sub-repo branches to fork
off their `beta` tips. Separately (F-14) `git ls-files .claude` is **empty** and the `.claude/*` /
`!.claude/skills/` un-ignore exists only in the working tree — so plan 147-06's skill-script edit
would be an untracked-file add with no reviewable diff, or silently unaddable if `.gitignore` were
ever reverted.

Output: an app repo on the v1.32 branch with a recorded green baseline, and two tracked skill files
in the meta repo. **Zero production-code change in this plan.**

**Requirement flips: none — `requirements:` is deliberately empty.** That array is not an "addresses"
label: `execute-plan.md`'s `update_requirements` step feeds it verbatim to
`gsd-tools query requirements.mark-complete`, which ticks each listed ID's checkbox in REQUIREMENTS.md
the moment this plan finishes. Naming any PROV ID here would mark it complete after nothing but a
branch checkout and a `.gitignore` hunk.

What this plan *advances*, for the verifier's benefit:
- **All of PROV-01…PROV-06** indirectly — it is the hard blocking precondition, since no code plan in
  this phase may run while `firestarter_app` is still on the v1.31 branch. Every flip is owned by a
  later plan.
- **PROV-06** specifically — tracking `.claude/skills/devtest-triage/scripts/devtest_issues.py` and
  `SKILL.md` is what lets plan `147-06` land its parser-surface edit as a reviewable diff rather than
  an untracked-file add. **PROV-06's flip is owned by `147-06`.**
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-CONTEXT.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-RESEARCH.md
@.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols and files **created** by Phase 147. The plan-review-convergence source-grounding pass must
treat every row as new — none of these exist on disk before this phase.

| Artifact | Kind | Created by plan | Notes |
|---|---|---|---|
| `ProgrammerIdentity` | `typing.NamedTuple` in `firestarter/hardware.py` | 147-02 | fields, in order: `hw_revision: Optional[str]`, `fw_board_identity: Optional[str]` |
| `read_programmer_identity` | method on `HardwareManager` | 147-02 | replaces `read_hardware_revision_value` (rename + widened return) |
| `_scrub_identity` | module-private function in `firestarter/hardware.py` | 147-02 | `Optional[str] -> Optional[str]` |
| `NOT_REPORTED = "not reported"` | module constant in `firestarter/diagnostic_report.py` | 147-03 | literal #1 of 3 |
| `_identity_cell` | module-private function in `firestarter/diagnostic_report.py` | 147-03 | render-only; never used in `to_dict()` |
| `SCHEMA_VERSION = "1.4"` | changed constant value in `firestarter/diagnostic_report.py` | 147-03 | was `"1.3"` |
| `NOT_REPORTED = "not reported"` | module constant in `tools/parse_devtest_issue.py` | 147-05 | literal #2 of 3 (stdlib-only module; no import) |
| `_NOT_ATTRIBUTABLE` | module constant in `tools/parse_devtest_issue.py` | 147-05 | the D-14/D-17 clause |
| `NOT_REPORTED` / `NOT_ATTRIBUTABLE` | module constants in `.claude/skills/devtest-triage/scripts/devtest_issues.py` | 147-06 | literal #3 of 3 (meta repo; not importable from the app) |
| `fw_board_identity` param on `make_hardware_manager` | new keyword arg, default `"3.0.0b19:leonardo"` | 147-02 | W-5 |
| `_NULL_IDENTITY_TITLE` / `_NULL_IDENTITY_BODY` | frozen fixture constants in `tests/test_parse_devtest_issue.py` | 147-05 | W-3 |
| `.claude/skills/devtest-triage/fixtures/dev-test-at28c256-null-identity.md` | new file (meta repo) | 147-06 | W-4 |
| `.claude/skills/devtest-triage/fixtures/dev-test-at28c256-populated-identity.md` | new file (meta repo) | 147-06 | W-4 |
| new test functions | `tests/test_hardware.py`, `tests/test_dev_test_cmd.py`, `tests/test_diagnostic_report.py`, `tests/test_parse_devtest_issue.py` | 147-02…05 | names listed in each plan's tasks |

<!-- planner-discipline-allow: read_hardware_revision_value -->
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move firestarter_app to the v1.32 branch off origin/beta and record the green baseline</name>
  <files>firestarter_app (branch state only — no file is edited, no commit is made in this repo)</files>
  <read_first>
    - `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-RESEARCH.md` §"Sub-Repo Branch Precondition" (the verified commands and the content oracle)
    - `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-VALIDATION.md` §"Test Infrastructure" (the exact full-suite command and its measured baseline)
    - `/workspaces/firestarter_app/CLAUDE.md`
  </read_first>
  <action>
    Working in `/workspaces/firestarter_app`. `git fetch origin`, then create and check out
    `gsd/v1.32-at28c-write-path-root-cause-report-provenance` **from `origin/beta`** (matching the
    meta repo's already-active branch name). `origin/beta` carries app **3.0.0b21**.

    Verify the base **by content, not by ancestry**: `git diff --stat` between
    `gsd/v1.31-27c-programming-algorithm-fidelity` and `origin/beta` must report exactly one changed
    file, `firestarter/__init__.py`, one insertion and one deletion — the version bump
    `3.0.0b20` -> `3.0.0b21`. RESEARCH F-15 measured that the app's v1.31 PR landed as a true merge
    (so `merge-base --is-ancestor` happens to succeed here), but the content check is the one that is
    immune to either merge shape, and it is the only one this task may rely on.

    The editable install at `/usr/local` needs **no** reinstall across a branch switch; confirm it
    picks the new version up by importing `firestarter` and printing `__version__`.

    Then close RESEARCH assumption A4 (which measured the suite green on the v1.31 tip, not on beta):
    run the full suite on the new branch and record the pass count in the plan SUMMARY. This is the
    number every later plan in this phase regresses against.

    Make **no commit** in `firestarter_app` and do **not** stage the meta repo's `firestarter_app`
    gitlink — there is no app commit to pin yet; the gitlink bumps belong to plans 147-02…05.
  </action>
  <acceptance_criteria>
    - `cd /workspaces/firestarter_app && git branch --show-current` prints `gsd/v1.32-at28c-write-path-root-cause-report-provenance`
    - `cd /workspaces/firestarter_app && git diff --stat gsd/v1.31-27c-programming-algorithm-fidelity origin/beta` prints exactly `1 file changed, 1 insertion(+), 1 deletion(-)` and names only `firestarter/__init__.py`
    - `cd /workspaces/firestarter_app && git diff --stat HEAD origin/beta` prints no output (HEAD content is identical to `origin/beta`)
    - `python3 -c "import firestarter; print(firestarter.__version__)"` prints `3.0.0b21`
    - The full suite command below exits 0 and its final line reports at least `1590 passed`; the exact count is copied verbatim into the SUMMARY as the phase baseline
    - `cd /workspaces/firestarter_app && git status --porcelain` prints no output (nothing was edited or left staged in the app repo)
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && git fetch origin && git branch --show-current && git diff --stat gsd/v1.31-27c-programming-algorithm-fidelity origin/beta && git diff --stat HEAD origin/beta && python3 -c "import firestarter; print(firestarter.__version__)" && git status --porcelain</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q</automated>
  </verify>
  <done>The app repo is on the v1.32 branch, its content equals `origin/beta` (3.0.0b21), the tree is clean, and a green full-suite pass count is recorded as the phase baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Land the meta .gitignore un-ignore and track the devtest-triage skill baseline</name>
  <files>.gitignore, .claude/skills/devtest-triage/SKILL.md, .claude/skills/devtest-triage/scripts/devtest_issues.py</files>
  <read_first>
    - `/workspaces/.gitignore` (and `git diff .gitignore` — the un-ignore hunk is already in the working tree, unstaged)
    - `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-PATTERNS.md` §"Trap 2 — the meta-repo `.gitignore` precondition (F-14, verified)"
    - `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-RESEARCH.md` §"Verified Findings" row F-14
    - `/workspaces/CLAUDE.md` (the meta repo tracks only `.planning/` and `.claude/`)
  </read_first>
  <action>
    Working in the **meta** repo `/workspaces`. Commit the already-present-but-unstaged `.gitignore`
    change (which replaces the blanket `.claude/` ignore with `.claude/*` plus `!.claude/skills/`,
    keeps `.claude/skills/*/scripts/__pycache__/` ignored, and keeps the marketplace-installed
    `.claude/skills/find-skills/` out) **together with** the two devtest-triage files at their
    current, unmodified content: `.claude/skills/devtest-triage/SKILL.md` and
    `.claude/skills/devtest-triage/scripts/devtest_issues.py`.

    Committing the baseline unmodified is the point: plan 147-06 then edits tracked files and its
    change is a reviewable diff, which an untracked-file add would not be.

    Stage **named paths only** — the meta tree is dirty (`.gitignore`, both submodule gitlinks,
    untracked `.claude/`, `package.json`, `package-lock.json`). Never `git add -A` or `git add .`.
    Do not stage the `firestarter` or `firestarter_app` gitlinks. Do not commit `package.json` /
    `package-lock.json`.

    Two expected side effects to state in the SUMMARY rather than "fix": (a) the same `.gitignore`
    working-tree change also carries an unrelated trailing `node_modules` line — it lands with this
    commit and is correct for the untracked `package*.json`; (b) the un-ignore makes the project's
    other hand-authored skills (`devtest-rootcause`, `skill-writer`) *committable*, but this task
    deliberately tracks only `devtest-triage`, the phase's edit target. Leave the others untracked.

    Commit message: `chore(147-01): un-ignore .claude/skills and track the devtest-triage baseline`.
  </action>
  <acceptance_criteria>
    - `git ls-files .claude` lists exactly two paths: `.claude/skills/devtest-triage/SKILL.md` and `.claude/skills/devtest-triage/scripts/devtest_issues.py`
    - `git show HEAD --stat` names `.gitignore` plus those two files, and names no `firestarter`/`firestarter_app` gitlink and no `package*.json`
    - `git show HEAD:.gitignore | grep -c '^!\.claude/skills/$'` prints `1`
    - `git diff HEAD -- .claude/skills/devtest-triage` prints no output (the baseline was committed byte-identical to the working tree)
    - `git status --porcelain .claude/skills/devtest-triage` prints no output
    - `git ls-files .claude/skills/find-skills` prints no output (the marketplace skill stayed out)
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && git ls-files .claude && git show HEAD --stat && git show HEAD:.gitignore | grep -c '^!\.claude/skills/$' && git diff HEAD -- .claude/skills/devtest-triage && git status --porcelain .claude/skills/devtest-triage && git ls-files .claude/skills/find-skills</automated>
  </verify>
  <done>`.gitignore`'s un-ignore and the two devtest-triage files are committed in the meta repo; nothing else was staged; plan 147-06 can now edit tracked files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index (meta repo) | A dirty multi-repo tree; an over-broad `git add` would publish local runtime state (`.claude/settings*`, `.claude/gsd-core`, `.claude/channels` incl. a Discord token) into a shared repo |
| `origin/beta` → local branch | Remote content is trusted (this project's own beta), but its *identity* must be proven by content rather than assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-07 | Information Disclosure | meta-repo `git add` scope in Task 2 | high | mitigate | Stage named paths only; the `.gitignore` hunk keeps `.claude/*` ignored and un-ignores **only** `.claude/skills/`. Acceptance criterion asserts `git ls-files .claude` contains exactly two paths, so any leak of `.claude/channels` or `.claude/settings*` fails the gate |
| T-147-08 | Tampering | branch base selection in Task 1 | medium | mitigate | Verify by `git diff --stat` content (one version line) rather than `merge-base --is-ancestor`, which RESEARCH F-15 shows is shape-dependent and has produced a false negative in this project before |
| T-147-SC | Tampering | npm/pip/cargo installs | high | accept | **No package-manager install task exists in this phase.** RESEARCH §"Package Legitimacy Audit" records zero proposed packages: `typing.NamedTuple` is stdlib and every test/lint tool is already pinned in `pyproject.toml`. Any new dependency proposed during execution is a scope escape and must stop the plan, not be installed |
</threat_model>

<verification>
- Both tasks' `<automated>` legs exit 0.
- `cd /workspaces/firestarter_app && git log --oneline -1` still shows `origin/beta`'s tip commit — this plan adds no app commit.
- No meta commit in this plan touches `.planning/` source code or either submodule gitlink.
</verification>

<success_criteria>
- `firestarter_app` is on `gsd/v1.32-at28c-write-path-root-cause-report-provenance` with content identical to `origin/beta` (3.0.0b21) and a clean tree.
- A green full-suite pass count from that branch is recorded in the SUMMARY as the Phase 147 baseline (closing RESEARCH assumption A4).
- `.gitignore`'s `.claude/*` + `!.claude/skills/` un-ignore is committed, and exactly the two devtest-triage files are tracked.
</success_criteria>

<output>
Create `.planning/phases/147-report-provenance-every-dev-test-report-names-its-firmware/147-01-SUMMARY.md` when done. Record the measured full-suite pass count verbatim.
</output>
