---
phase: 93-rca-root-cause-the-w29c040-page-0-write-fault
plan: 02
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/signature/
autonomous: false
requirements: [RCA-01, SAFE-01]
must_haves:
  truths:
    - "The W29C040 page-0 write fault reproduces N≥2 deterministically on the seated chip (Leonardo + Rev 2.0) with the recorded ERROR frame"
    - "The exact failing address/byte and the observed-byte are captured (baseline ERROR \"Timeout verifying 0xd7 at 0x0000ff (got 0x00)\" or equivalent)"
    - "The post-fail settled read of 0x0000ff is captured (distinguishes page-committed-but-poll-gave-up from page-never-committed)"
    - "Per standing bench discipline, controller: identity + live R1/R2 readback are recorded for this task"
  artifacts:
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md"
      provides: "RCA-01 reproduction section filled with N≥2 signature + post-fail settled read + bench discipline log row"
      contains: "RCA-01"
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/signature/"
      provides: "Captured raw serial ERROR frames + post-fail read-backs"
  key_links:
    - from: "evidence/93-RCA-FINDINGS.md RCA-01 section"
      to: "evidence/signature/ raw captures"
      via: "signature section cites the captured frame files"
      pattern: "signature/"
---

<objective>
Reproduce the W29C040 page-0 write fault on the operator-seated chip (Leonardo + RURP Rev
2.0) and capture the failure signature N≥2 deterministically — the pre-fix baseline for the
whole RCA. Record exactly which address/byte fails, the observed-byte at the failure, and
the post-fail settled read of the failing address (the free H4 fork: did the page commit
but the poll give up, or did the page never commit?). This is an operator-witnessed bench
plan; it cannot be fully autonomous.

Purpose: RCA-01 — a deterministic, recorded baseline is the precondition for a meaningful
differential (Plan 03) and a defensible named cause (Plan 04). N=1 conclusions are forbidden.
Output: evidence/93-RCA-FINDINGS.md RCA-01 section + raw captures under evidence/signature/.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bench pre-flight — confirm seated chip + port identity + R1/R2 + host build</name>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Reproduction & Signature Capture (RCA-01)" (repro command + the `-b` post-HARD-01 caveat) + § "Assumptions Log" (A1 chip-id 0xda46, A2 post-HARD-01 host, A4 FLAG_CAN_ERASE)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md (Plan 01 SAFE-01 verdict — must be clean before driving silicon)
    - /workspaces/firestarter_app/CLAUDE.md (serial protocol; HARD-01 `-b` decoupled from skip-erase)
  </read_first>
  <what-built>
    Plan 01 verified SAFE-01 (VPP-free write path, FLAG_CAN_ERASE absent, no escape hatch) and scaffolded the findings doc. This checkpoint confirms the physical bench matches the milestone lock before any write is driven into the seated chip.
  </what-built>
  <how-to-verify>
    1. Confirm the operator has seated the W29C040 on the Leonardo + RURP Rev 2.0 (do NOT remove the chip to sideload — Leonardo is chip-OUT-sideload-EXEMPT).
    2. Run `firestarter --version` and confirm the `controller:` identity reported on the seated port; record the port (e.g. /dev/ttyACM0). Numbers shuffle on replug — verify identity, do not trust a session-start mapping.
    3. Read back live R1/R2: confirm r1 ≈ 270000 (± 25%) for this board/shield. Record the exact readback.
    4. Confirm chip-id reads `0xda46` (W29C040): `firestarter info W29C040` or chip-id read — confirms A1 (same part class that failed in v1.15).
    5. Confirm the host build is post-HARD-01 (so `-b` does NOT skip erase) — `firestarter --version` / changelog.
  </how-to-verify>
  <acceptance_criteria>
    - The bench discipline log row in evidence/93-RCA-FINDINGS.md is filled: timestamp, controller: identity, port, R1/R2 readback (r1 within 270000 ± 25%), board=Leonardo, shield=Rev 2.0, chip-id=0xda46, host build = post-HARD-01.
    - SAFE-01-PREFLIGHT.md verdict is clean (FLAG_CAN_ERASE absent); if it recorded T-93-CANERASE, STOP — do not proceed.
  </acceptance_criteria>
  <resume-signal>Type "bench ready" once the discipline log row is recorded and SAFE-01 is clean, or describe the blocker (wrong port identity, R1 out of band, chip-id mismatch, pre-HARD-01 host).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Reproduce the fault N≥2 + capture signature + post-fail settled read</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md, .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/signature/</files>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Reproduction & Signature Capture (RCA-01)" (exact commands; the 5 things to record; the observed=0x00 interpretation hooks) + § "Open Questions" Q2 (settled read)
    - /workspaces/firestarter/src/proms/flash_type_4.cpp (flash4_wait_for_page_write packs [expected, A16, A8, A0, observed] into the MSG_ERR_FL4_VERIFY_TIMEOUT frame)
    - /workspaces/.planning/v1.15/bench/EVIDENCE.md (the prior recorded baseline to match)
  </read_first>
  <what-built>
    A deterministic reproduction harness driving the seated W29C040 through the normal 0x05 dispatch (no escape hatch), capturing the byte-tagged ERROR frame and the post-fail state of page 0.
  </what-built>
  <action>
    Generate a deterministic test image that crosses the 256/512/768 page boundaries (≥1024 B) with the repo's tools/gen_test_image.py (seed-fixed), save under evidence/signature/. Reproduce the historical signature: `firestarter write -b W29C040 <img>` and capture the full serial ERROR frame (expected byte, full failing address, observed byte) — expect `ERROR "Timeout verifying 0x.. at 0x0000ff (got 0x00)"`. Re-run for N≥2 determinism via `firestarter dev write-cycle W29C040 <img> --runs 2` and record the 0/1/2 verdict. Immediately after the fail, capture the post-fail state: `firestarter dev read W29C040 -a 0 -s 256` (is page 0 all 0x00, all 0xFF, partial, or pre-existing?) AND repeatedly read the failing address `firestarter dev read W29C040 -a 0xff -s 1` ×several to record whether 0x0000ff settles to the written 0xd7 (page committed, poll gave up → H4) or stays 0x00/0xFF (page never committed → H1/H3). Save all raw serial captures under evidence/signature/ and fill the RCA-01 section of evidence/93-RCA-FINDINGS.md with: the N≥2 ERROR frames, the failing address/byte, the observed-byte, the page-0 read-back, the settled read of 0x0000ff, and the bench discipline row.
  </action>
  <how-to-verify>
    1. The ERROR frame matches the baseline shape `Timeout verifying 0x.. at 0x0000ff (got 0x..)` on at least 2 runs.
    2. `dev write-cycle --runs 2` returns the same non-zero verdict both runs (deterministic).
    3. The post-fail read of page 0 and the repeated read of 0x0000ff are captured to files.
  </how-to-verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md RCA-01 section records the ERROR frame verbatim for N≥2 runs (with the decoded expected/address/observed bytes), the page-0 read-back contents, and the settled read of 0x0000ff with a stated verdict (settles-to-written vs stays-stale) — no subjective language; cite the captured filenames under evidence/signature/.
    - The reproduction used `firestarter write -b` / `dev write-cycle` through the normal 0x05 dispatch — no test-only escape hatch (SAFE-01 reaffirmed).
    - If the fault does NOT reproduce, that itself is recorded as an N≥2 result with the captured clean frames (and flags an assumption-A1/A2 mismatch for investigation) — do not declare repro on N=1.
  </acceptance_criteria>
  <resume-signal>Type "signature captured" with the observed-byte value and the settled-read verdict, or describe the deviation from the baseline.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (hardware-safety framing)

| Boundary | Description |
|----------|-------------|
| host CLI → seated W29C040 (serial write) | A write drives the real chip; a wrong flag/voltage could damage a 5V part |
| RCA harness → dispatch | Must remain the normal 0x05 path (no escape hatch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-CANERASE | Tampering (hardware) | flash4_erase_execute 12V on a 5V chip | mitigate | Task 1 gates on the Plan 01 SAFE-01 verdict (FLAG_CAN_ERASE absent) before any write; if violated, STOP |
| T-93-ESCAPE | Elevation of Privilege | repro path | accept-by-verify | Reproduction uses `firestarter write -b` / `dev write-cycle` only — normal dispatch; recorded in RCA-01 |
| T-93-PORTID | Spoofing (wrong device) | /dev/ttyACM* shuffle | mitigate | Task 1 verifies controller: identity + R1/R2 + chip-id 0xda46 per standing discipline before driving silicon |
| T-93-SC | Tampering | package installs | mitigate | No installs this phase |
</threat_model>

<verification>
- RCA-01 section of evidence/93-RCA-FINDINGS.md filled with N≥2 ERROR frames + post-fail reads + bench discipline row.
- Raw serial captures present under evidence/signature/.
- Reproduction flows through normal 0x05 dispatch (SAFE-01 reaffirmed).
</verification>

<success_criteria>
The page-0 write fault reproduces N≥2 deterministically on the seated chip with a recorded
failure signature (failing address/byte + observed-byte + post-fail settled read),
operator-witnessed on Leonardo + Rev 2.0 with the bench discipline row recorded — establishing
the pre-fix baseline.
</success_criteria>

<output>
Create `.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-02-SUMMARY.md` when done
</output>
