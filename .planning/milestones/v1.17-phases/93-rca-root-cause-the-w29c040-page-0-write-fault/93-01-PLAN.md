---
phase: 93-rca-root-cause-the-w29c040-page-0-write-fault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "flash4 write-execute path provably sets no VPP control bits (native test green)"
    - "FLAG_CAN_ERASE is confirmed absent from the W29C040 wire command so the 12V-asserting flash4_erase_execute never fires"
    - "The Phase-74 traps (missing SDP / wrong page size) are ruled out before bench: SDP emitted + 256B page derived for W29C040"
    - "W29C040 resolves through the normal supported dispatch with no --force / test-only escape hatch"
  artifacts:
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md"
      provides: "SAFE-01 verification checklist with recorded evidence (native test results + wire-flags inspection)"
      min_lines: 20
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md"
      provides: "Canonical RCA findings doc scaffold (sections for repro / differential / classification / safety)"
      min_lines: 30
  key_links:
    - from: "evidence/93-RCA-FINDINGS.md"
      to: "evidence/safety/SAFE-01-PREFLIGHT.md"
      via: "Safety section references the SAFE-01 preflight verdict"
      pattern: "SAFE-01"
---

<objective>
Establish the SAFE-01 non-bypass discipline and rule out the Phase-74 traps BEFORE any
silicon is touched. This is the autonomous (no-hardware) pre-flight that gates the entire
RCA: prove the flash4 write-execute path asserts no VPP bits, confirm FLAG_CAN_ERASE is
not set for the W29C040 (so the 12V-asserting flash4_erase_execute is dead code on this
chip), confirm the emitted SDP sequence + 256B page size are already correct (so the RCA
does not re-chase the silicon-ineffective Phase-74 hypotheses), and scaffold the canonical
evidence/93-RCA-FINDINGS.md that the bench plans (02–04) will fill.

Purpose: SAFE-01 must hold throughout the RCA (it recurs as a precondition through Phases
94–96). Ruling out the Phase-74 traps with native evidence prevents repeating the v1.15
mistake of naming an already-applied, silicon-ineffective fix as the cause.
Output: evidence/safety/SAFE-01-PREFLIGHT.md (recorded verdicts) + the scaffolded
evidence/93-RCA-FINDINGS.md.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-01 native + wire pre-flight verification</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md</files>
  <read_first>
    - /workspaces/firestarter/src/proms/flash_type_4.cpp (flash4_write_execute sets no VPP bits; flash4_erase_execute asserts CTRL_VPP_REGULATOR_ENABLE|CTRL_VPP_VPE_DROP_ENABLE|CTRL_VPE_ENABLE — only reachable when FLAG_CAN_ERASE set)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Security Domain" (SAFE-01 verification checklist) + Pitfall 3 (flash4_erase_execute VPP) + § "Code Examples"
    - /workspaces/firestarter/test/native/avr/test_val_flash4/test_val_flash4.cpp (test_flash4_write_execute_no_vpp; test_flash4_write_execute_emits_sdp; INV-04 page-boundary test)
    - /workspaces/firestarter_app/firestarter/data/chip_database.json (W29C040 entry near L14464–14473 — confirm algorithm 5, size 524288, and the flags it would emit)
    - /workspaces/firestarter_app/firestarter/chip_resolver.py (resolve_chip supported-gate; confirm no escape hatch)
  </read_first>
  <action>
    Run the native flash4 sanity suite from /workspaces/firestarter: `pio test -e native -f "*test_val_flash4*"` and record the pass/fail of test_flash4_write_execute_no_vpp (proves the write-execute path emits zero VPP control bits — SAFE-01), test_flash4_write_execute_emits_sdp (SDP emitted per page — rules out the Phase-74 "missing SDP" trap), and the INV-04 256B page-boundary assertion (page size correct for W29C040 — rules out the "wrong page size" trap). Capture the exact pio output lines.
    Inspect the W29C040 wire command flags: from firestarter_app, dump the JSON command that `firestarter write W29C040` would emit (use the host's command-build path / a dry-run / inspect chip_resolver + eprom_operations flag assembly) and confirm FLAG_CAN_ERASE (0x02) is NOT present in the `flags` field. If it IS present, STOP and record a HIGH-severity SAFE-01 finding (the 12V flash4_erase_execute would fire on a 5V chip — this is threat T-93-CANERASE).
    Confirm `chip_resolver.resolve_chip("W29C040")` resolves to a `supported` chip through the normal path with no `--force`-style bypass needed (record the resolved algorithm=5, pinout=DIP32_SST39SF040, vpp_mv).
    Write evidence/safety/SAFE-01-PREFLIGHT.md recording, per the RESEARCH § "Security Domain" checklist: (1) test_flash4_write_execute_no_vpp = GREEN/RED with output line; (2) FLAG_CAN_ERASE absent = confirmed/violated with the inspected flags value; (3) SDP emitted + 256B page = confirmed with the native test names; (4) resolve_chip normal path = confirmed with resolved fields. No escape hatch is introduced anywhere.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_val_flash4*" 2>&1 | grep -Eiq "PASSED|OK|[1-9][0-9]* Tests 0 Failures" && test -f /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md && grep -qi "FLAG_CAN_ERASE" /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md && echo PREFLIGHT_OK</automated>
  </verify>
  <acceptance_criteria>
    - SAFE-01-PREFLIGHT.md exists and records four recorded verdicts (each with the raw evidence line): test_flash4_write_execute_no_vpp result; FLAG_CAN_ERASE absent (with the inspected `flags` value, e.g. `flags=0x08` or whatever is emitted); SDP-emitted + 256B-page native test names+results; resolve_chip("W29C040") normal-path resolution fields.
    - If FLAG_CAN_ERASE is found set, the file records it as a HIGH-severity SAFE-01 violation (T-93-CANERASE) rather than a pass — no subjective "looks safe" language.
    - `pio test -e native -f "*test_val_flash4*"` exit is recorded verbatim (PASS or the failing test name).
  </acceptance_criteria>
  <done>The four SAFE-01 checklist items each carry a recorded verdict with raw evidence in SAFE-01-PREFLIGHT.md; no test-only escape hatch exists.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the canonical evidence/93-RCA-FINDINGS.md</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md</files>
  <read_first>
    - /workspaces/.planning/phases/44-bug-a-rca-modified-rev-0-upper-address-jitter/evidence/44-RCA-FINDINGS.md (the structure to mirror)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Recommended RCA artifact structure" + § "Root-Cause Classification (RCA-03)" (the 5 hypotheses H1–H5)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md § "Per-Task Verification Map"
  </read_first>
  <action>
    Create evidence/93-RCA-FINDINGS.md mirroring the Phase 44 evidence/44-RCA-FINDINGS.md structure, with these empty-but-headed sections the bench plans fill: (1) "## Bench Discipline Log" (per-task table: timestamp, controller: identity, port, R1/R2 readback, board=Leonardo, shield=Rev 2.0); (2) "## RCA-01 — Reproduction & Signature"; (3) "## RCA-02 — Differential vs W29C020"; (4) "## RCA-03 — Disconfirming-Test Matrix" with one stub row per hypothesis H1 (timing), H2 (addressing/A18), H3 (SDP re-arm), H4 (poll site/firmware-algorithm), H5 (silicon/wear), each with columns Verdict | Raw evidence | Classification; (5) "## RCA-03 — Named Root Cause (or Ranked Hypotheses)"; (6) "## SAFE-01 — Non-Bypass Confirmation" (link to evidence/safety/SAFE-01-PREFLIGHT.md). Create the evidence/signature/ and evidence/differential/ capture directories. Pre-populate the recorded prior baseline signature `ERROR "Timeout verifying 0xd7 at 0x0000ff (got 0x00)"` (Phase 82/84, N=2) under RCA-01 as the signature to reproduce.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md && test -d .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/signature && test -d .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/differential && grep -q "Timeout verifying 0xd7 at 0x0000ff" .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md && for h in H1 H2 H3 H4 H5; do grep -q "$h" .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md || exit 1; done && echo FINDINGS_SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md exists with all six named sections and the H1–H5 disconfirming-test matrix stub (each hypothesis row present with Verdict | Raw evidence | Classification columns).
    - The recorded prior baseline signature string `Timeout verifying 0xd7 at 0x0000ff (got 0x00)` is present under RCA-01.
    - evidence/signature/ and evidence/differential/ directories exist.
  </acceptance_criteria>
  <done>The findings doc scaffold + capture dirs exist with the H1–H5 matrix and the baseline signature pre-populated, ready for the bench plans to fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (hardware-safety framing)

| Boundary | Description |
|----------|-------------|
| host CLI → firmware (serial) | The `flags`/`algorithm`/`vpp_mv` wire command crosses here; a wrong flag can assert 12V on a 5V chip |
| firmware dispatch → flash4 handler | `configure_flash4` routes 0x05; the write path must set no VPP bits |
| host `chip_resolver.resolve_chip` gate | The supported-status gate that must never be bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-CANERASE | Tampering (hardware) | flash4_erase_execute 12V VPP assertion | mitigate | Task 1 confirms FLAG_CAN_ERASE absent in the W29C040 wire `flags`; if present, recorded as HIGH-severity SAFE-01 violation and the bench plans must NOT proceed until resolved |
| T-93-NOVPP | Tampering (hardware) | flash4_write_execute control-register writes | mitigate | Task 1 records test_flash4_write_execute_no_vpp green (write path provably VPP-free) |
| T-93-ESCAPE | Elevation of Privilege | RCA dispatch path | accept-by-verify | All RCA flows through normal 0x05 dispatch; DEBUG_ADDRESS is a passive trace; Task 1 confirms resolve_chip normal-path (no --force) |
| T-93-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH § Package Legitimacy Audit: N/A); slopcheck N/A |
</threat_model>

<verification>
- `pio test -e native -f "*test_val_flash4*"` recorded (no-VPP + SDP + 256B-page asserts).
- evidence/safety/SAFE-01-PREFLIGHT.md carries all four SAFE-01 checklist verdicts with raw evidence.
- evidence/93-RCA-FINDINGS.md scaffold + capture dirs exist with the H1–H5 matrix.
</verification>

<success_criteria>
SAFE-01 baseline is established with recorded evidence (VPP-free write path, FLAG_CAN_ERASE
absent, no escape hatch, normal resolve_chip path) AND the Phase-74 traps are ruled out
(SDP emitted + 256B page) BEFORE any bench task runs; the canonical findings doc is scaffolded.
</success_criteria>

<output>
Create `.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-01-SUMMARY.md` when done
</output>
