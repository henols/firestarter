---
phase: 93-rca-root-cause-the-w29c040-page-0-write-fault
plan: 04
type: execute
wave: 4
depends_on: [93-03]
files_modified:
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md
autonomous: true
requirements: [RCA-03, SAFE-01]
must_haves:
  truths:
    - "A single named root cause (or ranked hypotheses) is recorded for the page-0 fault, classified firmware-algorithm / timing / addressing / silicon"
    - "Every rejected hypothesis (of H1–H5) carries its disconfirming evidence — not 'untested'"
    - "The named cause is detailed enough to design the Phase 94 fix without further RCA (it cites the surviving disconfirming test + the silicon evidence)"
    - "SAFE-01 holds at close: VPP blocked, resolve_chip not bypassed, FLAG_CAN_ERASE unset, no escape hatch — confirmed across all RCA tasks"
  artifacts:
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md"
      provides: "Finalized named root cause + classification + per-hypothesis disconfirming evidence + SAFE-01 close-out + Phase-94 fix-design hand-off"
      contains: "Named Root Cause"
      min_lines: 80
  key_links:
    - from: "evidence/93-RCA-FINDINGS.md Named Root Cause section"
      to: "the H1–H5 disconfirming matrix + RCA-01 signature + RCA-02 differential"
      via: "the named cause cites the surviving test + the exonerating evidence for rejected hypotheses"
      pattern: "H[1-5]"
---

<objective>
Synthesize the recorded reproduction (Plan 02), differential, and disconfirming-matrix
(Plan 03) evidence into a NAMED root cause (or, if no single hypothesis survives uniquely,
a ranked set each carrying its disconfirming evidence), classified firmware-algorithm /
timing / addressing / silicon, and detailed enough that Phase 94 can design a targeted fix
with no further RCA. Close out SAFE-01 across the whole phase and mark the validation
sign-off. This is autonomous synthesis of evidence already captured — no new bench work.

Purpose: RCA-03 — the phase deliverable. A cause is named only when it survives its own
disconfirming test while competitors fail theirs (the Phase 44 D-07 causal bar). The output
governs the Phase 94 fix design.
Output: finalized evidence/93-RCA-FINDINGS.md + 93-VALIDATION.md sign-off.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Name + classify the root cause; ensure every rejected hypothesis carries disconfirming evidence</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md</files>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md (the RCA-01 signature, RCA-02 differential, and the filled H1–H5 matrix from Plans 02–03)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Root-Cause Classification (RCA-03)" (the 5 hypotheses + classifications) + § "Expected verdict shape" + Anti-Patterns ("re-asserting the Phase-74 hypothesis", "trusting native-only PASS as silicon proof")
    - /workspaces/.planning/phases/44-bug-a-rca-modified-rev-0-upper-address-jitter/evidence/44-RCA-FINDINGS.md (the named-cause + per-hypothesis-disconfirmer write-up style)
  </read_first>
  <action>
    From the recorded matrix verdicts, identify the surviving hypothesis (the one whose predicted knob/observation moved the failure while competitors' disconfirmers fired). Write the "## RCA-03 — Named Root Cause (or Ranked Hypotheses)" section: state the named cause in one sentence, assign its classification (firmware-algorithm / timing / addressing / silicon), and cite the surviving disconfirming test + its silicon evidence. For EACH of H1–H5 that is rejected, write its disconfirming evidence (the observation that killed it) — none may be left "untested". If no single hypothesis survives uniquely, record a RANKED set with each carrying its disconfirming evidence and the residual ambiguity stated. Explicitly note the Phase-74 traps are ruled out (SDP present + 256B page, per Plan 01 native evidence) so the cause is NOT re-asserting them, and that the cause cites silicon evidence (not a native-only PASS). Add a "## Hand-off to Phase 94" subsection: what the fix must change, the page-0+page-boundary cases it must cover, and any lockstep/wire implication the cause surfaces (e.g. if timing/H1 → a per-byte cadence or T_BLC concern; if poll-site/H4 → DQ7-mask vs whole-byte; if addressing/H2 → top-address packing) — enough that Phase 94 designs the fix with no further RCA.
  </action>
  <verify>
    <automated>cd /workspaces && F=.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md; grep -qi "Named Root Cause" "$F" && grep -qiE "firmware-algorithm|timing|addressing|silicon" "$F" && grep -qi "Hand-off to Phase 94" "$F" && for h in H1 H2 H3 H4 H5; do grep -q "$h" "$F" || exit 1; done && [ "$(grep -vc '^[[:space:]]*#' "$F")" -ge 80 ] && echo NAMED_CAUSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md contains a "Named Root Cause (or Ranked Hypotheses)" section stating the cause + one of the four classifications, citing the surviving disconfirming test + silicon evidence.
    - Each of H1–H5 either IS the named cause or carries its recorded disconfirming evidence — no hypothesis left "untested" (verifiable: each H-id appears with a confirms/disconfirms verdict).
    - A "Hand-off to Phase 94" subsection states what the fix must change + the cases it must cover.
    - The write-up explicitly notes the Phase-74 traps are ruled out and the cause rests on silicon evidence, not a native-only PASS.
  </acceptance_criteria>
  <done>The named root cause (or ranked set) is recorded + classified, every rejected hypothesis carries disconfirming evidence, and the Phase 94 hand-off is written — detailed enough to design the fix without further RCA.</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-01 phase-close confirmation + validation sign-off</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md, .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md</files>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/safety/SAFE-01-PREFLIGHT.md (Plan 01 SAFE-01 verdicts)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md § "Validation Sign-Off" + § "Per-Task Verification Map"
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Security Domain" (SAFE-01 checklist)
  </read_first>
  <action>
    Write the "## SAFE-01 — Non-Bypass Confirmation (phase close)" section in evidence/93-RCA-FINDINGS.md consolidating the SAFE-01 evidence across all RCA tasks: (1) the firmware VPP check stayed blocking + flash4 write path set no VPP bits (cite Plan 01 native test); (2) FLAG_CAN_ERASE confirmed absent for W29C040 (cite Plan 01 wire-flags inspection); (3) every bench operation flowed through the normal 0x05 dispatch with no test-only escape hatch (cite Plans 02–03 — `firestarter write`/`dev`/passive DEBUG_ADDRESS only); (4) `chip_resolver.resolve_chip("W29C040")` was never bypassed (no --force). State a single SAFE-01 = HELD/VIOLATED verdict. Then update 93-VALIDATION.md: tick the Per-Task Verification Map rows (repro/diff/classify/safety) to their final status, complete the "## Validation Sign-Off" checklist (every RCA requirement maps to recorded evidence; reproduction deterministic N≥2; every rejected hypothesis carries disconfirming evidence; SAFE-01 confirmed), and set `nyquist_compliant: true` + `status: complete` in the front-matter now the evidence map is complete.
  </action>
  <verify>
    <automated>cd /workspaces && F=.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md; V=.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md; grep -qi "SAFE-01 — Non-Bypass Confirmation" "$F" && grep -qiE "SAFE-01 = HELD|SAFE-01: HELD|SAFE-01 HELD" "$F" && grep -q "nyquist_compliant: true" "$V" && echo SAFE_SIGNOFF_OK</automated>
  </verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md "SAFE-01 — Non-Bypass Confirmation" section consolidates all four SAFE-01 checklist items with citations to Plan 01–03 evidence and states a single HELD/VIOLATED verdict.
    - 93-VALIDATION.md Per-Task Verification Map rows are updated to final status, the Validation Sign-Off checklist is complete, and front-matter has `nyquist_compliant: true` (and status complete).
    - If SAFE-01 is VIOLATED anywhere, it is recorded as such with the violating evidence (not rubber-stamped).
  </acceptance_criteria>
  <done>SAFE-01 is confirmed HELD (or recorded VIOLATED) across the phase with consolidated evidence, and 93-VALIDATION.md is signed off with nyquist_compliant set true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (hardware-safety framing)

| Boundary | Description |
|----------|-------------|
| RCA conclusion → Phase 94 fix design | A mis-named cause sends the fix in the wrong direction (re-chasing the Phase-74 traps) |
| SAFE-01 close-out | The consolidated safety verdict that Phases 94–96 inherit as a precondition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-PH74 | Repudiation (false cause) | RCA-03 conclusion | mitigate | Task 1 requires the named cause to rest on silicon evidence + cite the surviving disconfirmer, explicitly ruling out the already-applied silicon-ineffective Phase-74 hypotheses |
| T-93-ESCAPE | Elevation of Privilege | phase-wide | accept-by-verify | Task 2 consolidates that all RCA flowed through normal 0x05 dispatch with no escape hatch and resolve_chip was never bypassed |
| T-93-CANERASE | Tampering (hardware) | flash4_erase_execute | mitigate | Task 2 confirms FLAG_CAN_ERASE stayed absent across the phase (cite Plan 01) |
| T-93-SC | Tampering | package installs | mitigate | No installs this phase |
</threat_model>

<verification>
- evidence/93-RCA-FINDINGS.md: named cause + classification + per-hypothesis disconfirming evidence + Phase-94 hand-off + SAFE-01 close-out (≥80 non-comment lines).
- 93-VALIDATION.md signed off; nyquist_compliant: true.
</verification>

<success_criteria>
A named root cause (or ranked hypotheses each with disconfirming evidence) is recorded and
classified firmware-algorithm / timing / addressing / silicon, detailed enough to design the
Phase 94 fix without further RCA; SAFE-01 is confirmed held across the phase; the validation
contract is signed off.
</success_criteria>

<output>
Create `.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-04-SUMMARY.md` when done
</output>
