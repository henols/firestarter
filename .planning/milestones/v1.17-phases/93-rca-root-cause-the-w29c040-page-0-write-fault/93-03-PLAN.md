---
phase: 93-rca-root-cause-the-w29c040-page-0-write-fault
plan: 03
type: execute
wave: 3
depends_on: [93-02]
files_modified:
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
  - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/differential/
autonomous: false
requirements: [RCA-02, SAFE-01]
must_haves:
  truths:
    - "The passing sibling W29C020 writes→auto-erases→verifies clean on the identical bench + firmware build (the differential control)"
    - "The 4 candidate axes (SDP unlock, page-write polling/timing, A18 512KB addressing, page size) are each isolated or exonerated with recorded evidence"
    - "The cheapest-first disconfirming-test matrix is run: single-byte-to-page-0 (H1/H3 fork), DEBUG_ADDRESS top-address-stability trace (H2), non-page-0 page (H5), A18=1 page (H2 completeness)"
    - "Every axis carries a confirms/disconfirms verdict tied to a named hypothesis, not just 'untested'"
  artifacts:
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md"
      provides: "RCA-02 differential section + the H1–H5 disconfirming matrix rows filled with verdicts + raw evidence"
      contains: "RCA-02"
    - path: ".planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/differential/"
      provides: "Paired W29C040-vs-W29C020 captures + DEBUG_ADDRESS traces + per-test serial frames"
  key_links:
    - from: "evidence/93-RCA-FINDINGS.md RCA-03 matrix"
      to: "evidence/differential/ raw captures"
      via: "each matrix verdict cites its capture file"
      pattern: "differential/"
---

<objective>
Run the differential isolation against the passing sibling W29C020 and execute the
cheapest-first disconfirming-test matrix from RESEARCH § "Root-Cause Classification" so that
each candidate axis (SDP / timing / A18-addressing / page-size) and each hypothesis (H1–H5)
carries a confirms/disconfirms verdict with raw evidence. This is the experimental heart of
the RCA: hold everything constant except one axis, flip it, record whether the failure moves.
Operator-witnessed bench plan — cannot be fully autonomous.

Purpose: RCA-02 — isolate the differing variable(s) and exonerate the unchanged axes so
Plan 04 can name a defensible cause. The single-byte-to-page-0 test (which forks timing/SDP
from poll/silicon) runs early per the cheapest-first order.
Output: evidence/93-RCA-FINDINGS.md RCA-02 section + the filled H1–H5 matrix rows + raw
captures under evidence/differential/.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-VALIDATION.md
@.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: W29C020 control + single-byte page-0 fork (cheapest-first tests #1–#2)</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md, .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/differential/</files>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Differential Method (RCA-02)" (the axis table; both datasheets exonerate SDP-content/pinout/VPP) + § "Root-Cause Classification" H1/H3/H4 + § "Recommended disconfirming-test execution order" tests #1 (settled read) and #2 (single byte)
    - /workspaces/firestarter/src/proms/flash_type_4.cpp + /workspaces/firestarter/src/proms/primitives.cpp (poll_readback whole-byte compare — H4 clue)
    - /workspaces/firestarter/datasheets/0x05-FLASH-AMD-STD/W29C040.pdf §6.2/§6.7 and W29C020.pdf §6.2 (page-address bits, T_BLC, DQ7 complement-during-write)
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md (Plan 02 RCA-01 settled-read verdict — feeds test #1)
  </read_first>
  <what-built>
    Plan 02 captured the N≥2 baseline signature + post-fail settled read. This task establishes the W29C020 differential control on the identical bench/firmware build and runs the single-byte-to-page-0 fork that separates timing/SDP (H1/H3) from poll/silicon (H4/H5).
  </what-built>
  <action>
    Standing discipline FIRST: verify controller: identity + live R1/R2 readback for the seated port and record the bench discipline row (Leonardo + Rev 2.0). Differential control: with the operator's W29C020 seated, `firestarter write -b W29C020 <128K image>` and confirm PASS (write→auto-erase→verify clean) — this exonerates the handler/dispatch/SDP-content structure (both chips run identical 0x05 code). Record the W29C020 result + SHA. Re-seat the W29C040. Test #1 (H4 fork, free): if Plan 02 already captured the settled read of 0x0000ff, copy that verdict into the matrix; otherwise re-read 0x0000ff post-fail and record whether it settles to the written byte (→ H4 poll-site) or stays stale (→ H1/H3). Test #2 (H1/H3 fork): write a SINGLE byte to page 0 via the normal path (`firestarter write W29C040 -a 0 -s 1` with a 1-byte image, or `dev reg`/`dev` single-byte poke through the handle path) and poll/verify — if the single byte programs+verifies cleanly, per-byte mechanics + SDP-content are sound and the fault is multi-byte page-load timing (H1 indicated, H3 exonerated); if even one byte fails, H1 is disconfirmed. Save all raw captures under evidence/differential/ and fill the W29C020-control axis + the H1/H3/H4 matrix rows with verdicts + cited capture files.
  </action>
  <how-to-verify>
    1. W29C020 control write returns PASS with a recorded SHA on the same bench/build.
    2. The single-byte-to-page-0 result is captured (clean verify or fail) with the raw serial frame.
    3. The H1, H3, H4 matrix rows in evidence/93-RCA-FINDINGS.md each carry a confirms/disconfirms verdict citing a capture file.
  </how-to-verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md records: W29C020 control = PASS (with SHA) on identical bench/build; the single-byte-to-page-0 verdict (programmed+verified vs failed) with the raw frame; the settled-read H4 verdict. Each verdict cites a file under evidence/differential/.
    - H1 (timing), H3 (SDP re-arm), H4 (poll site) matrix rows are filled with confirms/disconfirms + classification — no "untested" left for these three.
    - All writes flowed through the normal 0x05 dispatch (no escape hatch); bench discipline row recorded.
  </acceptance_criteria>
  <resume-signal>Type "control+fork done" with the W29C020 verdict and the single-byte verdict, or describe the blocker (W29C020 unavailable → note the datasheet-only fallback per RESEARCH Environment Availability).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Addressing + silicon disconfirmers — DEBUG_ADDRESS trace + non-page-0 + A18=1 (tests #3–#5)</name>
  <files>.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md, .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/differential/</files>
  <read_first>
    - /workspaces/.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-RESEARCH.md § "Root-Cause Classification" H2 (A18/top-address) + H5 (silicon/boot-block §6.6) + § "Recommended disconfirming-test execution order" tests #3 (DEBUG_ADDRESS), #4 (non-page-0), #5 (A18=1) + Pitfall 5 (delayMicroseconds accuracy)
    - /workspaces/firestarter/platformio.ini (line ~25, the commented `-D DEBUG_ADDRESS` flag — passive trace build, SAFE-01-safe; emits DBG_TOP_MSB_LSB)
    - /workspaces/firestarter/src/proms/memory.cpp (mem_util_calculate_top_address_register — A16–A18 packing; both chips pin_count==32 so the pins<32 branch is not taken)
    - /workspaces/firestarter/include/rurp_pinout.h (CTRL_ADDRESS_LINE_18_REV2 == CTRL_VPP_P1_ENABLE_REV2 == 0x08 — A18 shares the P1 control bit)
  </read_first>
  <what-built>
    Task 1 forked H1/H3/H4. This task closes the addressing axis (H2) and the silicon/page-0-specificity axis (H5) so RCA-02 has every candidate axis isolated or exonerated.
  </what-built>
  <action>
    Verify controller: identity + R1/R2 and record the bench discipline row. Test #3 (H2 + H1 cadence): build a passive trace firmware with `-D DEBUG_ADDRESS` and sideload to the Leonardo (chip stays seated — Leonardo is chip-OUT-sideload-EXEMPT; this is a passive trace, NOT a dispatch change — SAFE-01-safe). Drive one full page-0 load and capture the per-byte DBG_TOP_MSB_LSB triplet stream; assess (a) whether the top-address byte is CONSTANT and correct (0x00) across all 256 bytes of page 0 — constant ⇒ H2 disconfirmed for page 0; and (b) the per-byte cadence/count for the H1 quantitative read (note delayMicroseconds<3µs inaccuracy — measure, don't sum constants). Re-flash the normal (non-trace) build afterward and note it. Test #4 (H5 / page-0-specificity): write a NON-page-0 page (e.g. `firestarter write W29C040 -a 0x1000 ...` a single mid-chip page) — if a middle page programs+verifies cleanly but page 0 never does, record page-0-specificity (boot-block §6.6 or A8-boundary addressing). Test #5 (H2 completeness): write a page where A18=1 (address 0x40000) and record whether it behaves differently from page 0 (A18=0). Save raw captures + the DEBUG_ADDRESS trace under evidence/differential/ and fill the H2 + H5 matrix rows + the RCA-02 axis-exoneration summary (SDP-content SAME/exonerated, pinout SAME/exonerated, VPP SAME/exonerated, page-size-value correct/exonerated; the surviving differing axes named).
  </action>
  <how-to-verify>
    1. The DEBUG_ADDRESS trace of one page-0 load is captured and the top-address-byte stability (constant vs changing) across the 256 bytes is stated.
    2. The non-page-0 page write and the A18=1 page write results are captured.
    3. The H2 and H5 matrix rows + the RCA-02 axis-exoneration summary are filled with verdicts citing capture files.
    4. The normal firmware build is re-flashed after the trace build (noted in the findings doc).
  </how-to-verify>
  <acceptance_criteria>
    - evidence/93-RCA-FINDINGS.md records: the DEBUG_ADDRESS top-address-stability verdict for page 0 (constant=0x00 ⇒ H2 disconfirmed-for-page-0, or changing ⇒ H2 indicated) with the captured trace cited; the non-page-0 page verdict (clean ⇒ page-0-specific); the A18=1 page verdict. Each cites a file under evidence/differential/.
    - RCA-02 axis-exoneration summary names which of the 4 candidate axes is isolated (differing) and which are exonerated, each with recorded evidence — no "untested" axis remains.
    - The trace build was passive (no dispatch change), the chip was NOT removed for sideload, and the normal build was re-flashed afterward (SAFE-01 + bench discipline reaffirmed).
  </acceptance_criteria>
  <resume-signal>Type "addressing+silicon done" with the H2 page-0 stability verdict and the non-page-0 verdict, or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (hardware-safety framing)

| Boundary | Description |
|----------|-------------|
| trace firmware build → seated chip | A DEBUG_ADDRESS build is sideloaded with the chip seated; must be a passive trace, not a dispatch/behavior change |
| differential writes → W29C040 / W29C020 | Real writes to 5V parts; no VPP assertion permitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-TRACE | Tampering (dispatch) | DEBUG_ADDRESS build | mitigate | The flag is passive (emits DBG_TOP_MSB_LSB only); Task 2 confirms W29C040 still routes through configure_flash4 (no dispatch change) and re-flashes the normal build after |
| T-93-CANERASE | Tampering (hardware) | flash4_erase_execute 12V | mitigate | Gated on the Plan 01 SAFE-01 verdict; no FLAG_CAN_ERASE on either chip's wire command |
| T-93-PORTID | Spoofing | /dev/ttyACM* shuffle across chip swap | mitigate | controller: identity + R1/R2 re-verified at each task (a W29C020↔W29C040 swap occurs here) |
| T-93-ESCAPE | Elevation of Privilege | disconfirming tests | accept-by-verify | All writes use normal `firestarter write` / `dev` through 0x05 dispatch |
| T-93-SC | Tampering | package installs | mitigate | No installs this phase |
</threat_model>

<verification>
- RCA-02 differential section of evidence/93-RCA-FINDINGS.md filled: W29C020 control PASS + per-axis exoneration summary.
- H1–H5 disconfirming matrix rows all carry confirms/disconfirms verdicts with cited captures under evidence/differential/.
- Trace build passive + normal build re-flashed; SAFE-01 reaffirmed.
</verification>

<success_criteria>
The W29C040 write path is differentially compared against the passing W29C020 across all 4
candidate axes with the differing variable(s) isolated and the unchanged axes exonerated,
and the cheapest-first H1–H5 disconfirming matrix is run with every hypothesis carrying a
recorded verdict — operator-witnessed on Leonardo + Rev 2.0.
</success_criteria>

<output>
Create `.planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/93-03-SUMMARY.md` when done
</output>
