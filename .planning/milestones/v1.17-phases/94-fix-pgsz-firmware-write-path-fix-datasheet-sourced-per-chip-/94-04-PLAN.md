---
phase: 94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-
plan: 04
type: execute
wave: 4
depends_on: ["94-01", "94-02", "94-03"]
files_modified:
  - .planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/SAFE-02-CI-PY311.md
  - .planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/WRITABLE-REGION-PROOF.md
autonomous: false
requirements: [SAFE-02, FIX-01]
user_setup: []

must_haves:
  truths:
    - "The full host CI step list runs green against the py3.11 target (ruff check + ruff format --check + mypy watermark + codegen drift gate + diff_db + check_dispatch via pytest), not just the devcontainer py3.12"
    - "constants.py <-> firestarter.h flag/CTRL parity stays green (no new flag added across the phase)"
    - "After the FIX-01a fix + new firmware sideloaded, a plain `firestarter write` of a fixed image into the writable region (>=0x4000) of the seated W29C040 reads back byte-exact (SHA match), N>=2, with no --skip-erase needed and no 12V asserted on the 5V chip"
    - "The page-0 (first-16K) limitation is recorded as hardware-blocked (Phase 93); no first-16K / full-image write is attempted"
  artifacts:
    - path: ".planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/SAFE-02-CI-PY311.md"
      provides: "py3.11 CI gate sign-off (each step + result)"
    - path: ".planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/WRITABLE-REGION-PROOF.md"
      provides: "bench write->read->verify SHA evidence on >=0x4000 (N>=2), port/R1-R2 identity captured"
  key_links:
    - from: "firestarter write (host CLI)"
      to: "seated W29C040 writable region >=0x4000"
      via: "Leonardo + Rev 2.0 /dev/ttyACM0 after FIX-01a + sideload"
      pattern: "sha256"
---

<objective>
Close SAFE-02 (host CI green against the real py3.11 target, constants parity green) and deliver
the FIX-01 honest demonstration: a bench write->read->verify byte-exact proof on the WRITABLE
region (>=0x4000) of the seated W29C040, after the FIX-01a fix lands and the new firmware is
sideloaded -- proving the flash4 page-write algorithm + commands are correct on the programmable
region with no --skip-erase and no 12V on the 5V chip.

Purpose: The devcontainer's py3.12 MASKS py3.11 CI failures (f-string backslashes; non-ruff-clean
codegen) -- SAFE-02 demands the gates pass on py3.11 specifically. And per the Phase 93 RCA, the
W29C040's first 16K is permanently locked (section 6.6 irreversible) so full-image graduation is
hardware-blocked (Phase 95, different sample); the honest Phase-94 demonstration is the
writable-region (>=0x4000) proof that the algorithm itself is correct.

Output: a py3.11 CI sign-off evidence file and a writable-region bench-proof evidence file (SHA
match, N>=2, port + R1/R2 identity), with the page-0 hardware-block documented, not faked.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-RESEARCH.md
@.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-VALIDATION.md
@.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-01-SUMMARY.md
@.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-02-SUMMARY.md
@.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (SAFE-02): run the full CI step list against py3.11 + constants parity; record sign-off</name>
  <files>.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/SAFE-02-CI-PY311.md</files>
  <read_first>
    - 94-RESEARCH.md "## SAFE-02 / CI -- py3.11 Validation Recipe" (the exact 9-step CI list from ci.yml; the py3.12-masks-py3.11 trap: f-string backslashes are a SyntaxError on 3.11; non-ruff-clean codegen) and Pitfall 4
    - firestarter_app/.github/workflows/ci.yml (the authoritative step list + the py3.11 pin)
    - firestarter_app/CLAUDE.md "Tooling gate (v1.8)" (ruff + ruff format --check + mypy strict on 8 modules + pytest --cov-fail-under=70)
    - firestarter_app/firestarter/constants.py + firestarter/include/firestarter.h (confirm NO new flag/constant was added across Plans 01-03 -- page-size is a wire STRING; FIX-01a changed a value not the field set)
  </read_first>
  <action>
    Obtain a real py3.11 interpreter (pyenv 3.11, `uv venv --python 3.11`, or a python:3.11 container -- do NOT trust the devcontainer's 3.12, per Pitfall 4). In a py3.11 env install `pip install -e '.[test]'` and run the ci.yml step list verbatim: (1) catalog validity `python3 tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check`; (2) codegen drift gate codegen.py then `git diff --exit-code firestarter/messages.py`; (3) vector codegen drift gate (frame_vectors.py); (4) install; (5) `ruff check firestarter/ tests/`; (6) `ruff format --check firestarter/ tests/`; (7) `python tools/check_mypy_watermark.py`; (8) `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70` (this is where test_diff_db_gate.py and test_check_dispatch_invariants.py run); (9) smoke `firestarter --help`. Fix any py3.11-only failure (f-string backslashes -> move the backslash out of the `{}` expression; any non-ruff-clean codegen -> fix at the emitter, never hand-normalize messages.py).

    Confirm constants parity: the constants.py <-> firestarter.h flag/CTRL parity test passes (page-size is a wire string, not a flag/constant; FIX-01a changed the flags VALUE not the field set -- so parity is unaffected; assert this explicitly).

    Write evidence/SAFE-02-CI-PY311.md recording the py3.11 interpreter used, each of the 9 steps with its result, and the constants-parity result. Treat py3.11 as the authority; do NOT mark SAFE-02 green from a py3.12-only run.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/ && python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check && git diff --exit-code firestarter/messages.py && pytest tests/test_diff_db_gate.py tests/test_check_dispatch_invariants.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - evidence/SAFE-02-CI-PY311.md exists and records the py3.11 interpreter version + all 9 ci.yml steps each with a PASS result + the constants-parity PASS.
    - On py3.11: `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` exit 0 (no f-string-backslash SyntaxError).
    - codegen drift gate clean: `python tools/catalog/codegen.py ... && git diff --exit-code firestarter/messages.py` exits 0.
    - `pytest tests/ --cov-fail-under=70` passes on py3.11 (includes diff_db + check_dispatch gates).
    - constants.py <-> firestarter.h flag/CTRL parity test passes; no NEW flag/constant was introduced across the phase (page-size is a wire string).
  </acceptance_criteria>
  <done>Full ci.yml step list green on a real py3.11 interpreter; constants parity green; SAFE-02 sign-off evidence written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (FIX-01 demonstration): bench write->read->verify on the W29C040 writable region (>=0x4000)</name>
  <files>.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/evidence/WRITABLE-REGION-PROOF.md</files>
  <read_first>
    - 94-RESEARCH.md "## FIX-01 Reframing" (page-0 is hardware-blocked; the writable-region >=0x4000 proof is the honest demonstration) and "## Runtime State Inventory" (sideload the new firmware: `pio run -t upload -e leonardo`; Leonardo is chip-OUT-sideload-EXEMPT -- do NOT remove the chip) and Pitfalls 7 (use plain `write`, not -b) and 8 (N>=2; verify controller identity + live R1/R2 270000 +/- 25% each task)
    - 94-VALIDATION.md "## Manual-Only Verifications" (writable-region write->verify; boot-block-locked diagnostic on real chip) and the BENCH-01 full-image NOT-validatable note
    - 93-RCA-FINDINGS.md (0x3F00 FAIL / 0x4000 PASS boundary -- write strictly >=0x4000)
    - STATE.md "Milestone Context (v1.17)" bench discipline (Leonardo + Rev 2.0 + seated W29C040 on /dev/ttyACM0)
  </read_first>
  <what-built>
    The FIX-01a fix (Plan 01 -- host omits FLAG_CAN_ERASE for 0x05 + firmware skips the 12V erase) and the new firmware (PGSZ consumption from Plan 02, optional boot-block detect from Plan 03) are committed and ready to sideload. This task drives a bench write->read->verify on the PROVEN-WRITABLE region (>=0x4000) of the seated W29C040 to demonstrate the algorithm + commands are correct, with no --skip-erase and no 12V on the 5V chip. The W29C040 is already operator-seated, so the bench is driven unattended; the human checkpoint is to confirm the captured SHA evidence + safety observations.
  </what-built>
  <action>
    Sideload the post-Plan-03 firmware: from /workspaces/firestarter run `pio run -t upload -e leonardo` (Leonardo is chip-OUT-sideload-EXEMPT -- do NOT remove the W29C040). Verify the controller identity + live R1/R2 (r1 ~ 270000 +/- 25%) on /dev/ttyACM0 before writing (port numbers shuffle on replug -- Pitfall 8). Build a fixed test image sized to fit the region 0x4000..end (or a bounded sub-range starting >=0x4000); compute its sha256. Run a PLAIN `firestarter write` of that image into the >=0x4000 region (NOT `-b`, NOT --skip-erase -- post-FIX-01a plain write no longer 12V-asserts; -b only skips blank-check, not erase, per Pitfall 7). Read the same region back and sha256-compare. Repeat N>=2 (deterministic match). DO NOT attempt any write at <0x4000 or a full-image write (first 16K is hardware-locked, Phase 93). Optionally, separately, attempt a single bounded write into <0x4000 ONLY to confirm the Plan-03 boot-block-locked diagnostic surfaces clearly (expected to fail with the hint, not a cryptic timeout) -- this is a diagnostic-confirmation, not a graduation.

    Record evidence/WRITABLE-REGION-PROOF.md: port identity, R1/R2 readback, the region written, the image sha256, the read-back sha256, the N>=2 results, confirmation that plain `write` was used (no --skip-erase), and an explicit note that page-0 / full-image is hardware-blocked (Phase 93) and intentionally NOT attempted.
  </action>
  <how-to-verify>
    1. Confirm evidence/WRITABLE-REGION-PROOF.md shows the image sha256 == read-back sha256 for the >=0x4000 region, for N>=2 runs.
    2. Confirm the write command used was plain `firestarter write` (no --skip-erase, no -b) -- proving FIX-01a removed the 12V-on-5V need.
    3. Confirm the controller identity + R1/R2 (270000 +/- 25%) were captured for the bench port.
    4. Confirm NO write was attempted at <0x4000 and NO full-image write -- and the page-0 hardware-block is documented.
    5. (If the optional <0x4000 diagnostic probe was run) confirm it surfaced the boot-block-locked hint, not a bare timeout.
  </how-to-verify>
  <resume-signal>Type "approved" once the SHA-match evidence + safety observations are confirmed, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| py3.12 local env vs py3.11 CI | a real py3.11-only failure can ship as "green" if only py3.12 is checked |
| host CLI -> 5V W29C040 | a write must not assert 12V on the 5V chip (post-FIX-01a) |
| bench port mapping | /dev/ttyACM* numbers shuffle on replug; wrong port = wrong board |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-PY312MASK | (process) | host CI gates | mitigate | Run the full ci.yml list on a REAL py3.11 interpreter; treat py3.11 as authority (Task 1, Pitfall 4). |
| T-93-CANERASE (verify) | Tampering / hardware destruction | bench write path | mitigate | Plain `write` (no 12V post-FIX-01a) on >=0x4000; record that no --skip-erase was needed; no 12V observed (Task 2). |
| T-94-N1 | Integrity (false result) | bench SHA proof | mitigate | N>=2 deterministic match; verify controller identity + live R1/R2 each task (Pitfall 8). |
| T-94-PAGE0FAKE | Integrity (faked graduation) | FIX-01 demonstration | mitigate | Write strictly >=0x4000; document page-0 / full-image as hardware-blocked (Phase 93); never attempt or claim it (Task 2). |
| T-94-NOBYPASS | Elevation | chip_resolver | accept | SAFE-01: no --force / resolve bypass; W29C040 stays on `supported`. No change in this plan. |
</threat_model>

<verification>
- SAFE-02: evidence/SAFE-02-CI-PY311.md shows all 9 ci.yml steps green on a real py3.11 interpreter + constants parity green.
- FIX-01 demo: evidence/WRITABLE-REGION-PROOF.md shows image sha256 == read-back sha256 on >=0x4000, N>=2, plain `write`, port + R1/R2 captured, page-0 hardware-block documented.
- No first-16K / full-image write attempted; page-0 limitation recorded as Phase-93 hardware-blocked, not faked.
</verification>

<success_criteria>
- Full host CI step list green on py3.11; constants parity green; SAFE-02 signed off.
- Writable-region (>=0x4000) write->read->verify SHA match on the seated W29C040, N>=2, plain `write`, no 12V on the 5V chip.
- Page-0 / full-image hardware-block documented (Phase 93), never faked.
- Evidence files committed under the phase evidence/ dir.
</success_criteria>

<output>
Create `.planning/phases/94-fix-pgsz-firmware-write-path-fix-datasheet-sourced-per-chip-/94-04-SUMMARY.md` when done.
</output>
