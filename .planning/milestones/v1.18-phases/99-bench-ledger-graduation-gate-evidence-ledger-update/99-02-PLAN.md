---
phase: 99-bench-ledger-graduation-gate-evidence-ledger-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.18/bench/AM27C020-graduation/imgA.bin
  - .planning/v1.18/bench/AM27C020-graduation/SHA256SUMS.txt
  - .planning/v1.18/bench/check_graduation.py
autonomous: true
requirements: [BENCH-02]
must_haves:
  truths:
    - "A deterministic 262144-byte write payload (imgA) exists with its SHA recorded in SHA256SUMS.txt"
    - "A Phase-99 EVIDENCE gate (check_graduation.py) exists that asserts the P99 AM27C020 cell fields are filled and SHA-self-consistent, refusing fabrication"
    - "The gate reports MISSING/incomplete while the P99 cell is absent/scaffold, and passes only after the bench cell is filled in plan 99-04"
  artifacts:
    - path: ".planning/v1.18/bench/AM27C020-graduation/imgA.bin"
      provides: "deterministic 262144-byte write image (gen_test_image seed 1)"
    - path: ".planning/v1.18/bench/AM27C020-graduation/SHA256SUMS.txt"
      provides: "annotated SHA record for the write image (readback line appended at the bench)"
    - path: ".planning/v1.18/bench/check_graduation.py"
      provides: "Phase-99 EVIDENCE-cell completeness + SHA-self-consistency gate"
      contains: "phase99"
  key_links:
    - from: ".planning/v1.18/bench/check_graduation.py"
      to: ".planning/v1.18/bench/EVIDENCE.json"
      via: "load + filter AM27C020 phase99 cell, assert filled + write==readback"
      pattern: "EVIDENCE.json"
---

<objective>
Do all the bench-independent preparation for the AM27C020 graduation so the operator bench
session (plan 99-03) is a pure execute-and-record step. Generate the deterministic write
payload (262144 bytes via the existing `gen_test_image.py`), lay down the `AM27C020-graduation/`
artifact directory with an annotated `SHA256SUMS.txt` header (readback line appended at the
bench), and add a Phase-99 EVIDENCE gate (`check_graduation.py`, sibling of the Phase-97
`check_signature.py`) that makes BENCH-02 automatically verifiable and anti-fabrication.

Purpose: The graduation oracle is `sha256(readback) == sha256(imgA)`. Pre-generating imgA +
its SHA and building the completeness gate ahead of time means the bench session records
values into a machine-checked schema, and nothing is fabricated. This plan does NOT edit the
EVIDENCE.json cells (that is the bench/finalize work) — it only creates the payload, the
artifact dir, and the gate.
Output: imgA.bin + SHA256SUMS.txt header + check_graduation.py.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-RESEARCH.md
@.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-PATTERNS.md
@.planning/v1.18/bench/check_signature.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the deterministic 262144-byte write image + annotated SHA256SUMS header</name>
  <read_first>
    - firestarter_app/tools/gen_test_image.py (the size/seed/out CLI contract; seed 1 = image A convention)
    - .planning/v1.16/ledger/bench/W27C512-fix/SHA256SUMS.txt (annotated-header convention: comment lines with firmware commit, controller, shield, method, verdict, then the sha256sum lines)
    - .planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-PATTERNS.md (the AM27C020-graduation/SHA256SUMS.txt section; the gen_test_image 262144 note)
  </read_first>
  <action>
    Create the directory `.planning/v1.18/bench/AM27C020-graduation/`. Run `python3 firestarter_app/tools/gen_test_image.py 262144 1 .planning/v1.18/bench/AM27C020-graduation/imgA.bin` (AM27C020 = 262144 bytes; seed 1 = image A per the project convention). Write `SHA256SUMS.txt` in that directory with a comment header
    (each line prefixed with the hash character) recording: firmware submodule commit under test `35706c2`
    (record the COMMIT, not the version string — the version-string caveat), controller `leonardo`, shield
    `Rev 2.0`, method `manual write -b -> read/verify -> host-side SHA compare` (UV EPROM: NO dev write-cycle,
    NO --skip-erase, NO --force), and a `verdict: PENDING BENCH` placeholder line. Then append the plain
    `sha256sum imgA.bin` output line (run sha256sum from inside the directory so the recorded path is the bare
    `imgA.bin`). Leave the readback SHA line to be appended at the bench (plan 99-03) — do NOT fabricate a
    readback line.
  </action>
  <verify>
    <automated>bash -c 'test "$(stat -c%s .planning/v1.18/bench/AM27C020-graduation/imgA.bin)" = 262144 && grep -q imgA.bin .planning/v1.18/bench/AM27C020-graduation/SHA256SUMS.txt && grep -q 35706c2 .planning/v1.18/bench/AM27C020-graduation/SHA256SUMS.txt && cd .planning/v1.18/bench/AM27C020-graduation && grep -v "^#" SHA256SUMS.txt | grep imgA.bin | sha256sum -c -'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `stat -c%s .../imgA.bin` equals `262144`.
    - CLI: `sha256sum -c` on the non-comment `imgA.bin` line (run from the artifact dir) prints `imgA.bin: OK`.
    - Source assertion: `SHA256SUMS.txt` header contains the firmware commit `35706c2`, controller `leonardo`, shield `Rev 2.0`, and a `PENDING BENCH` verdict line; no readback SHA line yet.
  </acceptance_criteria>
  <done>imgA.bin is exactly 262144 bytes, its SHA is recorded and self-verifies, and the SHA256SUMS header carries the bench-discipline provenance with a pending-bench verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase-99 EVIDENCE graduation gate (check_graduation.py)</name>
  <read_first>
    - .planning/v1.18/bench/check_signature.py (the Phase-97 sibling gate shape: load EVIDENCE.json, filter the AM27C020 cell, assert required fields have no TBD, return non-zero on any gap)
    - .planning/v1.18/bench/EVIDENCE.json (the existing cells; locked_columns at lines 9-19; the W27C512 PASS cell with write_image_sha256/readback_sha256; the AM27C020 Phase-97 cell with the bench-discipline fields)
    - .planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-PATTERNS.md (the check_graduation.py OPTIONAL section — the required-field list and the write==readback assertion)
  </read_first>
  <action>
    Create `.planning/v1.18/bench/check_graduation.py` mirroring `check_signature.py`. It loads
    `.planning/v1.18/bench/EVIDENCE.json` and selects the AM27C020 cell whose `op` starts with `phase99` (the
    Phase-99 graduation-or-defer cell created in plan 99-04 — do NOT read the Phase-97 `tier0_microprobe+rca01`
    cell). Behavior: (1) if no `phase99*` AM27C020 cell exists, print `MISSING: no phase99 AM27C020 cell yet`
    and return 1 (this is the expected pre-bench state); (2) assert the bench-discipline + signature fields are
    filled with real captures (no `TBD`): `controller`, `port`, `r1_readback`, `r2_readback`, `fw_commit`,
    `vpp_adc_mv`, `verdict`; (3) read the cell `verdict` string — if it starts with `PASS` (graduation), assert
    `write_image_sha256` and `readback_sha256` are both present AND EQUAL (the graduation oracle,
    self-consistency); if it starts with `DEFER` (deferral), assert `bits_flipped` and `post_read_sha256` are
    present (the failing-vs-fixed differential) and do NOT require write==readback. Return 0 only when the
    branch assertions hold; return 1 on any gap/contradiction. Reference SHAs from the cell fields only — never
    hardcode a raw SHA in the script. Keep it a plain stdlib script (no argv), sibling exit-code contract (0
    pass / 1 gap).
  </action>
  <verify>
    <automated>ruff check .planning/v1.18/bench/check_graduation.py && ruff format --check .planning/v1.18/bench/check_graduation.py && bash -c 'python3 .planning/v1.18/bench/check_graduation.py; rc=$?; echo "gate-exit=$rc"; [ "$rc" = 1 ]'</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `ruff check` + `ruff format --check` on `check_graduation.py` exit 0 (SAFE-02 hygiene).
    - CLI: running `check_graduation.py` with no `phase99*` cell present prints a MISSING message and exits 1 (the expected pre-bench state — it becomes green only after plan 99-04 fills the cell).
    - Source assertion: the script filters on `op` starting with `phase99` (not the Phase-97 `tier0_microprobe+rca01` cell) and branches PASS (require write==readback) vs DEFER (require the 0-bits signature), with no raw SHA literal in the script body.
  </acceptance_criteria>
  <done>check_graduation.py exists, is ruff-clean, correctly reports MISSING/exit-1 while the Phase-99 cell is absent, and encodes the PASS (write==readback) vs DEFER (0-bits) branch logic for later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-reported bench readings -> EVIDENCE.json cell | Untrusted human-reported values are recorded; check_graduation.py is the anti-fabrication check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02a | Tampering / Repudiation | fabricated graduation evidence (faked SHA) | mitigate | check_graduation.py asserts write_image_sha256 == readback_sha256 from the cell fields (independent host-side compare); the write SHA is pre-committed in SHA256SUMS.txt before the bench; N>=3 stability required in plan 99-03. |
| T-99-04a | Spoofing (of "the fix") | wrong/stale firmware build | mitigate | SHA256SUMS header records the firmware submodule COMMIT (35706c2), not the version string; plan 99-03 operator confirms `firestarter fw` before any spend. |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (gen_test_image.py + stdlib already present); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `stat -c%s imgA.bin` == 262144; `sha256sum -c` of the imgA line passes.
- `check_graduation.py` exits 1 (MISSING) pre-bench; ruff clean.
- `SHA256SUMS.txt` carries firmware commit + discipline provenance + pending verdict.
</verification>

<success_criteria>
The deterministic write payload and its recorded SHA exist, and a machine-checked EVIDENCE-cell gate is in place that will confirm BENCH-02 completeness + SHA self-consistency once the bench cell is authored — currently correctly reporting the pre-bench MISSING state.
</success_criteria>

<output>
Create `.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-02-SUMMARY.md` when done.
</output>
