---
phase: 99-bench-ledger-graduation-gate-evidence-ledger-update
plan: 04
type: execute
wave: 3
depends_on: [99-01, 99-02, 99-03]
files_modified:
  - .planning/v1.18/bench/EVIDENCE.json
  - .planning/v1.18/bench/EVIDENCE.md
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.json
  - .planning/v1.16/ledger/PROTOCOL-LEDGER.md
autonomous: true
requirements: [BENCH-01, BENCH-02]
must_haves:
  truths:
    - "A Phase-99 AM27C020 EVIDENCE cell (op starts with phase99) records the bench outcome; the Phase-97 RCA cell is preserved untouched"
    - "check_graduation.py passes against the filled Phase-99 cell (write==readback on graduate, or the failing signature on defer)"
    - "The PROTOCOL-LEDGER 0x08 row + FUT-06 reflect the outcome in JSON+MD lockstep, and check_ledger.py exits 0 (0 contradictions)"
    - "On graduation FUT-06 is removed from open_defects[]; on deferral FUT-06 is kept/re-described with status_changed:false — never flipped to true"
  artifacts:
    - path: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json"
      provides: "the 0x08 row updated to PASS (graduate) or re-recorded (defer) + FUT-06 disposition"
      contains: "0x08"
    - path: ".planning/v1.18/bench/EVIDENCE.json"
      provides: "the Phase-99 AM27C020 graduation-or-defer cell"
      contains: "phase99"
  key_links:
    - from: ".planning/v1.16/ledger/PROTOCOL-LEDGER.json (0x08 row)"
      to: ".planning/v1.18/bench/AM27C020-graduation/"
      via: "evidence.p90_artifacts path reference (no raw SHA — D-04)"
      pattern: "AM27C020-graduation"
---

<objective>
Transcribe the plan-99-03 bench outcome into the two canonical persisted records — the v1.18
EVIDENCE cell and the PROTOCOL-LEDGER `0x08` entry — driving both gates to green. This is the
branch-resolving finalize step: the SAME EVIDENCE-cell + ledger-update tasks author either the
graduation or the deferral, deterministically, from the BENCH-LOG.

GRADUATE branch: `0x08` row -> PASS (v1.18-native, using the gate extension from plan 99-01);
FUT-06 removed from open_defects[]; the graduated row cites `bench/AM27C020-graduation/` by
path (no raw SHA — D-04).
DEFER branch: `0x08` row stays `open-defect-carried` (or a documented residual status); FUT-06
kept and re-described (status_changed stays false) or renamed; the cell records the failing-vs-
fixed signature. No fabricated PASS.

Purpose: BENCH-01/02 land in the canonical ledger; ROADMAP SC#3 requires check_ledger.py at 0
contradictions and the JSON<->MD mirror in lockstep.
Output: updated EVIDENCE.{json,md} + PROTOCOL-LEDGER.{json,md}; both gates green.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-RESEARCH.md
@.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-PATTERNS.md
@.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md
@.planning/v1.18/bench/EVIDENCE.json
@.planning/v1.16/ledger/PROTOCOL-LEDGER.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase-99 AM27C020 EVIDENCE cell (json+md lockstep) from the bench outcome</name>
  <read_first>
    - .planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md (the operator-witnessed raw commands, RCs, readback SHA, VPP reading, verdict)
    - .planning/v1.18/bench/EVIDENCE.json (locked_columns at 9-19; the W27C512 PASS cell shape lines 50-64 with write_image_sha256/readback_sha256; the Phase-97 AM27C020 cell lines 23-49 with the bench-discipline + honest "not measured" fields)
    - .planning/v1.18/bench/EVIDENCE.md (the Cell A/B section style to mirror)
    - .planning/v1.18/bench/check_graduation.py (the required-field list + PASS/DEFER branch the cell must satisfy)
  </read_first>
  <action>
    Append a NEW Phase-99 cell to the `cells` array in `.planning/v1.18/bench/EVIDENCE.json` — do NOT overwrite
    the Phase-97 `tier0_microprobe+rca01` cell (check_signature.py / check_pre01.py still read it). Set
    `chip: "AM27C020"`, `family: "0x08 (EPROM_QUICK)"`, `board: "leonardo"`, `shield: "Rev 2.0"`, `op:
    "phase99_graduation"` (graduate) or `"phase99_deferral"` (defer), and the bench-discipline fields from the
    BENCH-LOG: `controller`, `port`, `r1_readback`, `r2_readback`, `fw_commit` (the actual flashed commit,
    `35706c2`), `jp4_position`, `vpp_adc_mv`, `dmm_pin1_v` (real value OR honest "not measured — <reason>").
    GRADUATE: set `write_image_sha256` + `readback_sha256` (EQUAL, from BENCH-LOG), `bits_flipped` (nonzero),
    `verdict` starting with `PASS`, and `post_read_sha256` = the stable programmed SHA. DEFER: set
    `bits_flipped: "0"`, `bad_bytes`, `retries`, `pre_read_sha256`==`post_read_sha256`, `verdict` starting with
    `DEFER`, and cite the failing-vs-fixed differential vs the Phase-97 cell. Mirror the new cell into
    `EVIDENCE.md` in the existing Cell-section table style. Values come ONLY from the BENCH-LOG — never
    fabricate.
  </action>
  <verify>
    <automated>python3 .planning/v1.18/bench/check_graduation.py && python3 .planning/v1.18/bench/check_signature.py && python3 .planning/v1.18/bench/check_pre01.py</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `python3 .planning/v1.18/bench/check_graduation.py` exits 0 against the newly-authored Phase-99 cell (graduate: write==readback; defer: 0-bits signature present).
    - Test-command: `check_signature.py` + `check_pre01.py` still exit 0 (the Phase-97 RCA cell was NOT overwritten).
    - Source assertion: the new cell `op` starts with `phase99`; `fw_commit` records `35706c2`; a tooling-blocked DMM value reads "not measured" with reason, never a fabricated number.
    - Source assertion: `EVIDENCE.md` carries the mirrored Phase-99 cell in lockstep with `EVIDENCE.json`.
  </acceptance_criteria>
  <done>The Phase-99 EVIDENCE cell exists in json+md lockstep, reflects the real bench outcome, and passes check_graduation.py while leaving the Phase-97 gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the PROTOCOL-LEDGER 0x08 row + FUT-06 (json+md lockstep) and drive check_ledger.py to exit 0</name>
  <read_first>
    - .planning/v1.16/ledger/PROTOCOL-LEDGER.json (the current 0x08 row lines 145-165; a PASS row shape — 0x05/0x07 rows with the evidence sub-object; the FUT-06 block in open_defects)
    - .planning/v1.16/ledger/PROTOCOL-LEDGER.md (the 0x08 bucket-table row line 25; the status legend lines 35-40; the FUT-06 Open-Defects block lines 64-70)
    - .planning/v1.16/ledger/tools/check_ledger.py (the extended D-09 constraint from plan 99-01; the D-04 no-copy guard; the status_changed invariant)
    - .planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-PATTERNS.md (the PROTOCOL-LEDGER.json / .md sections — graduate vs defer JSON shapes; Pitfall 3 FUT-06 retirement; Pitfall 4 D-04 no-copy)
  </read_first>
  <action>
    Edit BOTH `PROTOCOL-LEDGER.json` and `PROTOCOL-LEDGER.md` in lockstep from the Task-1 verdict.
    GRADUATE branch: set the `0x08` row `verification_status: "PASS"`, `oracle: "leonardo+Rev2.0"`,
    `on_hand_chip: "AM27C020"`, remove `defect_ref`, and set `evidence` to the v1.18-native shape the plan-99-01
    gate recognizes — `v1_18_writeverify_sha_selfconsistent: true`, `p90_read_sha_matches_v115: true`,
    `p90_artifacts: [".planning/v1.18/bench/AM27C020-graduation/", ".planning/v1.18/bench/EVIDENCE.json"]` (path
    references only — NO raw 64-hex SHA anywhere, D-04). REMOVE the FUT-06 block from `open_defects[]` entirely
    (retirement-by-removal; do NOT set status_changed:true — that trips LEDGER-03). Mirror in the .md: set the
    `0x08` table row On-Hand=AM27C020 / Status=PASS / Evidence=prose citation of `bench/AM27C020-graduation/` +
    method + firmware commit (no raw SHA); delete the FUT-06 Open-Defects block.
    DEFER branch: keep the `0x08` row `verification_status: "open-defect-carried"` (or a documented residual
    status already in `_VALID_STATUSES`), keep `evidence: null` or a path-only note; KEEP the FUT-06 block with
    `status_changed: false`, updating only its `disposition` to reflect "software fix landed (Phase 98), bench
    graduation deferred — <reason>"; mirror in the .md. No fabricated PASS.
    Run `check_ledger.py` after the edit; if it reports a violation, fix the JSON shape (do NOT weaken the gate).
  </action>
  <verify>
    <automated>python3 .planning/v1.16/ledger/tools/check_ledger.py</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `python3 .planning/v1.16/ledger/tools/check_ledger.py` exits 0 (0 contradictions) after the edit.
    - Source assertion: no raw 64-hex SHA appears in either PROTOCOL-LEDGER file (D-04); the `0x08` evidence cites `bench/AM27C020-graduation/` by path.
    - Source assertion (graduate): FUT-06 is ABSENT from `open_defects[]`; the `0x08` row is PASS with `v1_18_writeverify_sha_selfconsistent: true` and `oracle: "leonardo+Rev2.0"`.
    - Source assertion (defer): FUT-06 present with `status_changed: false` and an updated disposition; the `0x08` row is not PASS; no fabricated evidence.
    - Behavior assertion: `PROTOCOL-LEDGER.md` and `.json` carry the same `0x08` status + same FUT-06 disposition (lockstep).
  </acceptance_criteria>
  <done>The 0x08 ledger row + FUT-06 reflect the bench outcome in json+md lockstep, no raw SHA leaks into the ledger, the status_changed invariant holds, and check_ledger.py exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EVIDENCE/ledger JSON edits -> gate scripts | Untrusted status/evidence claims cross into the machine-checked gates (check_graduation.py + check_ledger.py), which are the tamper-evidence mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04b | Repudiation | ledger status silently changed (defect hidden) | mitigate | check_ledger.py `status_changed is False` invariant + D-09 PASS constraint; FUT-06 retired by REMOVAL on graduation, never by flipping status_changed. |
| T-99-04c | Tampering | raw SHA copied into the ledger (D-04) | mitigate | Evidence cited by artifact path + join key only; SHAs stay in EVIDENCE.json / SHA256SUMS.txt; the D-04 `_RAW_SHA_RE` guard blocks any 64-hex string. |
| T-99-04d | Tampering / Repudiation | fabricated graduation cell | mitigate | check_graduation.py asserts write==readback (graduate) or the 0-bits signature (defer) from the BENCH-LOG values; no PASS is authorable without the self-consistency proof. |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (stdlib gate scripts already present); no legitimacy checkpoint required. |
</threat_model>

<verification>
- `python3 .planning/v1.18/bench/check_graduation.py` exits 0 (Phase-99 cell filled + consistent).
- `python3 .planning/v1.18/bench/check_signature.py` + `check_pre01.py` still exit 0 (Phase-97 cell intact).
- `python3 .planning/v1.16/ledger/tools/check_ledger.py` exits 0 (0 contradictions).
- `python3 -m pytest .planning/v1.16/ledger/tools/test_check_ledger.py -q` still 8 passed.
- No raw 64-hex SHA in either PROTOCOL-LEDGER file; JSON<->MD lockstep for the 0x08 row + FUT-06.
</verification>

<success_criteria>
The bench outcome is faithfully recorded in the canonical EVIDENCE + PROTOCOL-LEDGER records (graduate: 0x08 PASS + FUT-06 retired; defer: documented residual + FUT-06 kept with status_changed:false), both gates pass at 0 contradictions, and no evidence is fabricated or SHA-leaked.
</success_criteria>

<output>
Create `.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-04-SUMMARY.md` when done.
</output>
