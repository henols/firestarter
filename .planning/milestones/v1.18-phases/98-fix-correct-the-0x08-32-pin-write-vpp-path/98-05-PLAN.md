---
phase: 98-fix-correct-the-0x08-32-pin-write-vpp-path
plan: 05
type: execute
wave: 3
depends_on: ["98-03", "98-04"]
files_modified:
  - firestarter/src/proms/memory.cpp
  - firestarter/include/firestarter.h
  - firestarter_app/firestarter/constants.py
  - firestarter_app/tests/test_revision_constants_parity.py
autonomous: true
requirements: [FIX-02, SAFE-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "IN-01 fixed: uint32_to_bytes (memory.cpp ~379-384) serializes the four bytes to explicit distinct indices (buffer[pos], buffer[pos+1], buffer[pos+2], buffer[pos+3] for >>24, >>16, >>8, >>0) instead of the stale-pos `buffer[pos]` then `buffer[pos++]` sequence that wrote the original index twice and dropped the >>0 byte. Latent under #ifdef RAW_DATA_PROGRESS (disabled) but now correct if ever enabled."
    - "IN-03 fixed: the double-evaluation `#define min(a,b) ((a)<(b)?(a):(b))` macro (memory.cpp ~58-60) is replaced with a single-evaluation form (inline function or std::min where available), so a future `min(f(), g())` cannot double-evaluate its arguments. The sole current call site (memory_read_execute ~219) still compiles and behaves identically."
    - "IN-02 firmware half: a named boundary constant (MAX_27C020_SIZE = 262144) exists in the firmware repo (firestarter/include/firestarter.h) with a comment cross-referencing the host constant (firestarter_app/tools/build_db.py MAX_27C020_SIZE from 98-03), so the ≤256K size boundary is a named symbol on both sides of the wire, not a bare literal. This plan is the SOLE owner of the firmware MAX_27C020_SIZE define (98-04 explicitly defers it here)."
    - "IN-02 parity: the constants parity test (firestarter_app/tests/test_revision_constants_parity.py) asserts the host boundary constant equals the firmware boundary constant (both 262144), so a future divergence — a hardware-damage A18 risk per the phase's own framing — FAILs at pytest time. The test skips cleanly (skipif) when the firmware sub-repo header is absent, matching the existing CTRL_*/FLAG_* parity pattern."
    - "CROSS-CUTTING INVARIANT (MUST NOT regress): pio test -e native stays fully green (including 98-04's WR-01 revision cases); the v1.16 golden traces (0x07/0x0B/chip-id) stay byte-identical (these cleanups do not touch the write/program path — IN-01 is behind a disabled ifdef, IN-03 is a semantics-preserving macro swap); host CI (ruff/format/mypy/parity) green on py3.11 (SAFE-02)."
  artifacts:
    - path: "firestarter/src/proms/memory.cpp"
      provides: "IN-01 uint32_to_bytes explicit-index fix; IN-03 single-evaluation min"
      contains: "uint32_to_bytes"
    - path: "firestarter/include/firestarter.h"
      provides: "MAX_27C020_SIZE named boundary constant (firmware half of IN-02)"
      contains: "MAX_27C020_SIZE"
    - path: "firestarter_app/tests/test_revision_constants_parity.py"
      provides: "host↔firmware MAX_27C020_SIZE parity assertion (IN-02)"
      contains: "MAX_27C020_SIZE"
  key_links:
    - from: "firestarter_app MAX_27C020_SIZE (host)"
      to: "firestarter.h MAX_27C020_SIZE (firmware)"
      via: "test_revision_constants_parity.py cross-repo equality assertion"
      pattern: "MAX_27C020_SIZE"
---

<objective>
Close the three INFO findings from 98-REVIEW.md — the latent code-quality items
in the reviewed files that are safe to fix alongside the CR-01 work: IN-01
(uint32_to_bytes stale-pos serialization bug), IN-03 (min macro double-evaluation
hazard), and IN-02 (extract the 262144 ≤256K boundary into a named constant in
both repos with a cross-repo parity test).

These are independent of the CR-01 write-path mechanism (IN-01 is behind a
disabled #ifdef; IN-03 is a semantics-preserving macro swap; IN-02 is a constant
extraction). This plan is Wave 3 (depends_on 98-04) because it shares
firestarter/src/proms/memory.cpp and firestarter/include/firestarter.h with 98-04
— exclusive file ownership requires it run AFTER 98-04 lands, not in parallel. The
edits are in disjoint regions (98-04: memory_set_data ~274-320; 98-05: the min
macro ~58-60 and uint32_to_bytes ~379-384), and 98-04 explicitly DEFERS the
firmware MAX_27C020_SIZE define to this plan, so there is no contested hunk — the
Wave-3 ordering just guarantees a clean sequential apply.

Purpose: retire the reviewed-file debt so the phase closes clean.
Output: IN-01/IN-03 fixes in memory.cpp, MAX_27C020_SIZE in firestarter.h, a
cross-repo parity assertion, green native + host CI.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-REVIEW.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-03-PLAN.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IN-01 uint32_to_bytes explicit-index fix + IN-03 single-evaluation min</name>
  <read_first>
    - firestarter/src/proms/memory.cpp lines 379-384 — uint32_to_bytes: the `buffer[pos] = >>24; buffer[pos++] = >>16; buffer[pos++] = >>8; buffer[pos++] = &0xFF;` sequence (the stale-pos bug: first two stores hit the same index, >>0 byte dropped). Note it is reachable only under #ifdef RAW_DATA_PROGRESS.
    - firestarter/src/proms/memory.cpp lines 58-60 — `#ifndef min / #define min(a,b) ((a)<(b)?(a):(b)) / #endif`.
    - firestarter/src/proms/memory.cpp line ~219 — memory_read_execute's `min(handle->mem_size - handle->address, DATA_BUFFER_SIZE)` (the sole current call site — operands are side-effect-free, must still behave identically).
    - grep for other `min(` / `RAW_DATA_PROGRESS` uses in src/ to confirm no other call site relies on the macro's current form.
  </read_first>
  <action>
    IN-01: rewrite uint32_to_bytes to write four explicit distinct indices — buffer[pos] = (value>>24)&0xFF; buffer[pos+1] = (value>>16)&0xFF; buffer[pos+2] = (value>>8)&0xFF; buffer[pos+3] = value&0xFF; (or an equivalent for-loop over the four shift amounts) — so all four bytes land in distinct positions and the >>0 byte is not dropped. Keep the signature (char* buffer, int pos, uint32_t value) unchanged. IN-03: replace the double-evaluation `#define min(a,b)` with a single-evaluation form — prefer a `static inline` function (e.g. a file-local `mem_min`) or `std::min` if the TU is C++ and includes <algorithm>; if a macro must remain for existing call syntax, guard it so each argument is evaluated once (statement-expression with typed temporaries) — and update the one call site accordingly if the name changes. Confirm the sole current call site (memory_read_execute ~219) compiles and yields the same value. Do NOT touch memory_set_data / the write-program path (98-04's region — already landed in Wave 2).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c "buffer\[pos + 1\]\|buffer\[pos+1\]" src/proms/memory.cpp</automated>
    <automated>cd /workspaces/firestarter && test "$(grep -c 'buffer\[pos++\]' src/proms/memory.cpp)" -eq 0 && echo "stale-pos buffer[pos++] sequence removed"</automated>
    <automated>cd /workspaces/firestarter && pio run -e uno && pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - uint32_to_bytes writes four distinct indices (buffer[pos]..buffer[pos+3]) with shifts >>24/>>16/>>8/>>0; no `buffer[pos++]` post-increment sequence remains.
    - The min double-evaluation macro is replaced with a single-evaluation form; the memory_read_execute call site compiles and returns the same value.
    - `pio run -e uno` and `pio run -e leonardo` compile clean.
    - memory_set_data / the write-program path is unchanged by this task (98-04 owns it).
  </acceptance_criteria>
  <done>IN-01 serialization corrected; IN-03 double-evaluation removed; firmware compiles for uno + leonardo.</done>
</task>

<task type="auto">
  <name>Task 2: IN-02 firmware MAX_27C020_SIZE constant + cross-repo parity assertion</name>
  <read_first>
    - firestarter/include/firestarter.h lines 1-40 — the `#define` style block (DATA_BUFFER_SIZE, CMD_*, TIMEOUT_MS) where a MAX_27C020_SIZE define belongs. Confirm 98-04 did NOT already add it (98-04 defers it here).
    - firestarter_app/tools/build_db.py — the MAX_27C020_SIZE = 262144 host constant added by 98-03 (the value the firmware side must match). CONFIRM the exact name and value from the 98-03 SUMMARY / build_db.py before writing the parity test.
    - firestarter_app/firestarter/constants.py lines 1-50 — the constants module style (where a mirrored MAX_27C020_SIZE would live if the parity test reads it from constants.py rather than build_db.py).
    - firestarter_app/tests/test_revision_constants_parity.py — the existing skipif-guarded cross-repo parity pattern (REVISION_*/COMMAND_*/FLAG_*/CTRL_* asserted against the firmware headers; the skipif keys on firestarter/include/firestarter.h existence).
  </read_first>
  <action>
    Add `#define MAX_27C020_SIZE 262144` to firestarter/include/firestarter.h with a comment cross-referencing the host constant (firestarter_app/tools/build_db.py MAX_27C020_SIZE — the ≤256K DIP32_27C020 size gate; divergence is a hardware-damage A18 risk). Decide where the host authoritative value lives: if 98-03 put MAX_27C020_SIZE only in build_db.py, either (a) mirror it into constants.py and have build_db.py import it, OR (b) have the parity test read the build_db.py value directly — pick the approach consistent with how the existing parity test sources host constants, and document the choice. Extend test_revision_constants_parity.py with a skipif-guarded function that parses MAX_27C020_SIZE from firestarter/include/firestarter.h (same header-parse approach the existing CTRL_* assertions use) and asserts it equals the host MAX_27C020_SIZE (262144). The skipif must key on the firestarter.h existence like the existing functions so it skips cleanly when the firmware sub-repo is absent.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c "define MAX_27C020_SIZE 262144\|define MAX_27C020_SIZE  *262144" include/firestarter.h</automated>
    <automated>cd /workspaces/firestarter_app && grep -c "MAX_27C020_SIZE" tests/test_revision_constants_parity.py</automated>
    <automated>cd /workspaces/firestarter_app && python -m pytest tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - firestarter/include/firestarter.h defines MAX_27C020_SIZE = 262144 with a host cross-reference comment.
    - test_revision_constants_parity.py has a skipif-guarded function asserting host MAX_27C020_SIZE == firmware MAX_27C020_SIZE (262144); it skips cleanly when firestarter.h is absent.
    - `pytest tests/test_revision_constants_parity.py` passes (or skips the new assertion cleanly if firmware absent) on the py3.11 target.
  </acceptance_criteria>
  <done>Firmware MAX_27C020_SIZE named; cross-repo parity assertion added and green (IN-02).</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate — native suite + host CI green (FIX-02, SAFE-02)</name>
  <read_first>
    - firestarter/platformio.ini lines 69-90 — the [env:native] suite list.
    - firestarter/test/native/avr/test_val_eprom/golden_eprom_0x07_write.inc, golden_eprom_0x0B_write.inc, golden_eprom_chip_id.inc — the protected traces (these cleanups must not change them).
    - firestarter_app/CLAUDE.md — the py3.11 test-env note (avoid the py3.12-masks-CI-3.11 trap).
  </read_first>
  <action>
    Run `pio test -e native` and confirm fully green (including 98-04's WR-01 revision cases already landed) with the 0x07/0x0B/chip-id golden traces byte-identical (these cleanups touch neither the program path nor the traces). Run the host CI gate on the py3.11 target: `ruff check .`, `ruff format --check .`, mypy, and `pytest tests/test_revision_constants_parity.py`. Record the explicit py3.11 invocations + exit codes and the native test count in the SUMMARY. Confirm the combined memory.cpp (98-04 + 98-05 edits) compiles and the full native suite is green.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(git diff --stat test/native/avr/test_val_eprom/golden_eprom_0x07_write.inc test/native/avr/test_val_eprom/golden_eprom_0x0B_write.inc test/native/avr/test_val_eprom/golden_eprom_chip_id.inc | wc -l)" -eq 0 && echo "golden traces byte-identical"</automated>
    <automated>cd /workspaces/firestarter && pio test -e native</automated>
    <automated>cd /workspaces/firestarter_app && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` fully green; 0x07/0x0B/chip-id golden traces byte-identical.
    - `ruff check .` + `ruff format --check .` + mypy + parity pass on the py3.11 target.
    - SUMMARY records py3.11 invocations, exit codes, and the native test count.
  </acceptance_criteria>
  <done>Native suite + host CI green on py3.11; golden traces byte-identical (FIX-02, SAFE-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host build_db size gate ↔ firmware size gate | The ≤256K (262144) boundary must be identical on both sides; a divergence is a hardware-damage A18 risk (a chip could be pinout-scoped one way host-side and gated another way firmware-side). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-18 | Tampering (silent size-boundary drift) | 262144 duplicated across repos | mitigate | IN-02: named MAX_27C020_SIZE in both repos + a cross-repo parity test that FAILs at pytest time on divergence (matches the existing CTRL_*/FLAG_* parity discipline). |
| T-98-19 | Info disclosure (latent serialization bug) | uint32_to_bytes (RAW_DATA_PROGRESS) | mitigate | IN-01: explicit-index write so a future RAW_DATA_PROGRESS enable emits correct bytes; verified by compile + code inspection (the ifdef is disabled, so no runtime test path). |
| T-98-20 | Tampering (macro double-evaluation footgun) | min macro | mitigate | IN-03: single-evaluation form; the sole call site is side-effect-free today, the fix prevents a future `min(f(),g())` hazard. |
| T-98-21 | Tampering (masked regression) | golden traces 0x07/0x0B/chip-id | mitigate | Task 3 asserts empty git diff on the protected traces — these cleanups must not touch the program path or traces. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package installs — existing PlatformIO + `.[test]` toolchains only. |
</threat_model>

<verification>
- IN-01 uint32_to_bytes writes four distinct indices; no buffer[pos++] sequence.
- IN-03 min is single-evaluation; call site unchanged in behavior.
- IN-02 MAX_27C020_SIZE named in firestarter.h; cross-repo parity assertion green.
- pio test -e native green; golden traces byte-identical; host CI green on py3.11.
</verification>

<success_criteria>
- IN-01, IN-02, IN-03 closed (FIX-02 test/quality posture; SAFE-02 parity).
- No regression: native suite + golden traces + host CI all green on py3.11.
</success_criteria>

<output>
Create `.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-05-SUMMARY.md` when done
</output>
