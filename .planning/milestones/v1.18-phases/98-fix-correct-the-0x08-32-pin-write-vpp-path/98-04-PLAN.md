---
phase: 98-fix-correct-the-0x08-32-pin-write-vpp-path
plan: 04
type: execute
wave: 2
depends_on: ["98-03"]
files_modified:
  - firestarter/src/proms/memory.cpp
  - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp
autonomous: true
requirements: [FIX-01, FIX-02, SAFE-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The inert Rev-2 fix is REVERTED: the gated `ctrl & ~CTRL_ADDRESS_LINE_18` clear that 98-02 added in memory_set_data (memory.cpp ~311-319) is removed. That clear was wrong on BOTH revisions — on Rev 2 it touched the P1/VPP physical bit 0x08 (CTRL_ADDRESS_LINE_18_REV2 == CTRL_VPP_P1_ENABLE_REV2 == 0x08, held HIGH for VPP) and NEVER pin 31 (physical no-op, CR-01); on Rev 0/1 it cleared a genuine A18 address line at physical 0x20."
    - "Pin 31 = /PGM is now held at the program-active VIL level by the EXISTING rw_line mechanism (mem_util_remap_address_bus applies read_write<<config.rw_line; WRITE_FLAG=0 → LOW), driven by 98-03's rw-pin:[31] on DIP32_27C020. config.rw_line=22 folds into the CONTROL register CTRL_READ_WRITE (0x40) bit via mem_util_calculate_top_address_register. The full-pulse P1-VPP hold in program_mismatched_bytes is unchanged (MED-5 no-op)."
    - "The corrected 0x08 32-pin write path is REVISION-AGNOSTIC: pin 31 = /PGM = RW = CTRL_READ_WRITE (physical 0x40) reaches the socket on legacy/Rev1/Rev2. CTRL_READ_WRITE=0x40 is revision-invariant (rurp_pinout.h:82/94/113/125) and passes straight through rurp_map_ctrl_reg_for_hardware_revision (it is in the passthrough mask at rurp_hw_rev_utils.h:23, NOT an OR-merged bit), so the fix supports Rev 0/1 for free. The 0x08 alias is Rev-2-only and does NOT touch CTRL_READ_WRITE."
    - "A PHYSICAL-remap native test (WR-01), PARAMETRIZED across hardware revisions (Rev 2 AND Rev 0/1 legacy), feeds a program-window CONTROL value through rurp_map_ctrl_reg_for_hardware_revision and asserts the physical pin-31 bit (0x40 / CTRL_READ_WRITE) is at the program-active level (LOW) for EACH revision. Rev-2 case additionally proves the 0x08 alias (CTRL_ADDRESS_LINE_18_REV2==CTRL_VPP_P1_ENABLE_REV2==0x08) does NOT defeat it (0x08 HIGH for VPP concurrently); Rev-0/1 case proves the no-alias path still reaches pin 31 at 0x40. BOTH cases are RED against 98-02's old logical-A18-clear (Rev-2 physical no-op via the 0x08 alias; Rev-0/1 clears a genuine A18 line at 0x20) and GREEN against the corrected rw_line approach — the missing RED state for CR-01."
    - "RC-98B docstring contradiction fixed (WR-02): the '4 writes' figure is corrected to '5 writes' to match the body, AND the assertion is tightened from TEST_ASSERT_LESS_OR_EQUAL(5, ...) to TEST_ASSERT_EQUAL(5, ctrl_writes) so the gate-exclusion baseline is PINNED, not `<=` (which would pass at 0..5 and mask a write-reducing regression)."
    - "WR-04 reconciled with the CR-01 revert: with the A18-clear gone the memory_set_data gate no longer performs a CONTROL-register mutation, so there is no stray-write hazard to guard — the now-dead gate is removed. If any 0x08/32-pin/≤256K-specific behavior is retained it additionally requires using_p1_as_vpp(handle) so it cannot fire on a non-P1 0x08/32-pin config. The RC-98A/RC-98C tests that asserted on the extra-CONTROL-write artifact are updated to the corrected rw_line reality (pin 31 driven via the address/rw path, not an extra CONTROL write)."
    - "The Phase-99 escape clause is REMOVED (memory.cpp ~298-303 HIGH-1 caveat): the '0 bits at addr 0 is consistent with the analysis' pre-excuse is deleted so a genuinely-still-broken Phase-99 bench result cannot be dismissed. The corrected fix makes a real physical change (pin 31 held VIL via CTRL_READ_WRITE) — the old 'addr-0 register byte-unchanged' caveat no longer applies."
    - "CROSS-CUTTING INVARIANT (D-05, MUST NOT regress): the v1.16 golden register traces stay byte-identical for the 0x07 and 0x0B write paths and the chip-id path; the 0x08 golden trace (golden_eprom_0x08_write.inc, pins=0 default → gate never fired) stays byte-identical. Where the 0x08 32-pin trace legitimately changes it is re-pinned with cited rationale. pio test -e native is fully green (new WR-01 test cases raise the count)."
  artifacts:
    - path: "firestarter/src/proms/memory.cpp"
      provides: "reverted inert A18-clear; rely on existing rw_line mechanism; escape clause removed; WR-04-reconciled gate"
      contains: "rw_line"
    - path: "firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp"
      provides: "WR-01 revision-parametrized physical-remap test (Rev 2 + Rev 0/1); WR-02 RC-98B fix; RC-98A/C updated to rw_line reality"
      contains: "rurp_map_ctrl_reg_for_hardware_revision"
  key_links:
    - from: "98-03 pinouts.json rw-pin:[31] → config.rw_line=22"
      to: "memory_set_data pin 31 held VIL on WRITE_FLAG"
      via: "mem_util_remap_address_bus read_write<<rw_line + top_address CTRL_READ_WRITE mask"
      pattern: "config.rw_line"
    - from: "program-window CONTROL value (per revision)"
      to: "physical pin-31 bit (0x40) LOW on legacy/Rev1/Rev2"
      via: "rurp_map_ctrl_reg_for_hardware_revision (WR-01 test asserts post-remap physical bits per rev)"
      pattern: "CTRL_READ_WRITE"
---

<objective>
Close the FIRMWARE core of the corrected CR-01 fix: revert 98-02's inert
A18-clear, rely on the existing rw_line mechanism (fed by 98-03's rw-pin:[31]),
add the missing PHYSICAL-remap native test (WR-01) parametrized across Rev 2 AND
Rev 0/1, fix WR-02, reconcile the gate per WR-04, and remove the Phase-99 escape
clause.

98-02 cleared logical CTRL_ADDRESS_LINE_18 to "hold PGM LOW." That was wrong on
BOTH revisions: on Rev 2 the logical bit is OR-aliased onto the SAME physical bit
as P1/VPP (0x08), held HIGH for VPP throughout programming — a physical no-op
(CR-01); on Rev 0/1 it cleared a genuine A18 address line (physical 0x20). The
corrected mechanism (operator-confirmed via schematic): pin 31 = /PGM is the RW
line, CTRL_READ_WRITE (physical 0x40), which is REVISION-INVARIANT (same bit on
legacy/Rev1/Rev2, rurp_pinout.h:82/94/113/125) and passes straight through
rurp_map_ctrl_reg_for_hardware_revision (passthrough mask, rurp_hw_rev_utils.h:23,
NOT OR-merged). So the fix is revision-agnostic and supports Rev 0/1 for free.
98-03 assigns pin 31 to the RW line (rw-pin:[31]); this plan makes the firmware
rely on the existing rw_line mechanism (mem_util_remap_address_bus + top_address
CTRL_READ_WRITE mask) that already drives pin 31 LOW on WRITE_FLAG=0. The WR-01
test is the RED state, run for BOTH Rev 2 and Rev 0/1: it feeds the CONTROL value
through the per-revision remap and asserts physical 0x40 is LOW — impossible under
98-02 on either rev, satisfied by the corrected path.

GOOD NEWS encoded: AM27C020 write is NOT unsupportable on Rev 2 — pin 31 reaches
RW fine; 98-02 simply asserted the wrong bit.

BENCH CAVEAT: this milestone's real-silicon proof (Phase 99) is LOCKED to
Leonardo + Rev 2.0. Rev 0/1 is covered by the revision-agnostic code path + the
WR-01 native test only — NOT bench-proven this milestone.

Purpose: make the firmware fix physically real on all revisions and CI-catchable.
Output: reverted memory.cpp, WR-01 revision-parametrized physical test, WR-02
fix, reconciled gate, escape clause removed, golden traces byte-identical, native
suite green.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-CONTEXT.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-REVIEW.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-02-SUMMARY.md
@.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-03-PLAN.md

# The mechanism to rely on + the remap to test against (READ BEFORE EDITING):
@firestarter/include/rurp_hw_rev_utils.h
@firestarter/include/rurp_pinout.h
@firestarter/test/native/avr/_shared/host_stubs_common.inc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the inert A18-clear, rely on rw_line, reconcile the gate (WR-04), remove the escape clause</name>
  <read_first>
    - firestarter/src/proms/memory.cpp lines 274-320 — memory_set_data: WRITE_FLAG remap call (~276), the RC-1 comment block (~281-303 incl. the HIGH-1 escape clause), and the gated `ctrl & ~CTRL_ADDRESS_LINE_18` clear (~311-319).
    - firestarter/src/proms/memory.cpp lines 349-370 — mem_util_remap_address_bus: the `config.rw_line != 0xFF` block (`reorg_address |= read_write << config.rw_line`) — the EXISTING mechanism that now drives pin 31.
    - firestarter/src/proms/memory.cpp lines 184-198 — mem_util_calculate_top_address_register masks CTRL_READ_WRITE (0x40) from (address>>16), so address bit 22 (rw_line) lands in CONTROL bit 0x40.
    - firestarter/include/rurp_hw_rev_utils.h lines 15-40 — rurp_map_ctrl_reg_for_hardware_revision: CTRL_READ_WRITE is in BOTH the Rev-2 passthrough mask (line 23) AND the Rev-0/1 `ctrl_reg = data` passthrough (survives at physical 0x40 on every revision); CTRL_ADDRESS_LINE_18 folds to CTRL_ADDRESS_LINE_18_REV2 == 0x08 only on Rev 2 (the aliased bit 98-02 wrongly cleared).
    - firestarter/include/rurp_pinout.h lines 72-130 — CTRL_READ_WRITE=0x40 (legacy AND Rev-2), CTRL_VPP_P1_ENABLE=0x08, and CTRL_ADDRESS_LINE_18_REV2==CTRL_VPP_P1_ENABLE_REV2==0x08 (~128).
    - firestarter/include/memory_utils.h lines 14-24 — WRITE_FLAG=0, READ_FLAG=1, using_p1_as_vpp(handle).
    - WORKING ANALOG: firestarter/src/proms/flash_utils.cpp lines 44-53 — how the SST39SF040 flash path uses CTRL_READ_WRITE directly (same physical strobe concept).
  </read_first>
  <action>
    Delete the gated PGM-hold clear block in memory_set_data — the `if (handle->protocol == 0x08 && handle->pins == 32 && handle->mem_size <= 262144)` gate plus its two-line rurp_read_from_register / rurp_write_to_register(..., ctrl & ~CTRL_ADDRESS_LINE_18) body. Rely on the existing rw_line mechanism: with 98-03's rw-pin:[31], config.rw_line=22, and the WRITE_FLAG=0 call to mem_util_remap_address_bus holds pin 31 (CONTROL bit CTRL_READ_WRITE 0x40) LOW = program-active VIL across the full CE-pulse window (rurp_chip_enable → delayMicroseconds(pulse_delay) → rurp_chip_disable, all after the address/CONTROL write). Rewrite the RC-1 comment block to describe the CORRECTED, REVISION-AGNOSTIC mechanism (pin 31 = RW / CTRL_READ_WRITE 0x40 held VIL via WRITE_FLAG; 0x40 is revision-invariant and passes through the remap on legacy/Rev1/Rev2; the old logical-A18-clear was wrong on both revs — Rev-2 physical no-op via the 0x08 alias, Rev-0/1 clears a genuine A18 line). WR-04 reconcile: the A18-clear was the only CONTROL mutation the gate performed, so remove the now-dead gate; if any 0x08/32-pin/≤256K-specific behavior is retained, it MUST additionally require using_p1_as_vpp(handle). Escape clause: delete the HIGH-1 caveat sentences (~298-303) that pre-excuse a Phase-99 "0 bits at addr 0 is consistent with the analysis" result. Do NOT modify primitives.cpp / eprom.cpp / the vpp_check_window over-voltage block (SAFE-02 — over-voltage stays HIGH→ERROR, no FLAG_FORCE relaxation). Add MAX_27C020_SIZE (=262144) to firestarter/include/firestarter.h ONLY if a size literal survives the revert; otherwise defer the IN-02 firmware constant to 98-05 and record that in the SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -v '^\s*\*' src/proms/memory.cpp | grep -v '^\s*//' | grep -c 'ctrl & ~CTRL_ADDRESS_LINE_18')" -eq 0 && echo "A18-clear reverted (no code-line clear remains)"</automated>
    <automated>cd /workspaces/firestarter && test "$(git diff --stat src/proms/primitives.cpp src/proms/eprom.cpp | wc -l)" -eq 0 && echo "primitives.cpp + eprom.cpp untouched (SAFE-02)"</automated>
  </verify>
  <acceptance_criteria>
    - No code line in memory.cpp performs `ctrl & ~CTRL_ADDRESS_LINE_18` (comment/header references filtered out); the size/protocol/pins gate that guarded it is removed (or, if retained for other behavior, additionally requires using_p1_as_vpp).
    - `git diff` on primitives.cpp and eprom.cpp is empty (over-voltage / VPP-routing untouched — SAFE-02).
    - The RC-1 comment block describes the CORRECTED revision-agnostic rw_line mechanism (CTRL_READ_WRITE 0x40 held VIL) and the HIGH-1 Phase-99 escape clause is deleted.
    - `pio run -e uno` and `pio run -e leonardo` compile clean (no reference to the removed gate/clear).
  </acceptance_criteria>
  <done>Inert A18-clear reverted; firmware relies on rw_line; gate reconciled (WR-04); escape clause removed; SAFE-02 files untouched; firmware compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the WR-01 revision-parametrized physical-remap test (Rev 2 + Rev 0/1) + fix WR-02 (RC-98B) + update RC-98A/C to the rw_line reality</name>
  <read_first>
    - firestarter/test/native/avr/test_val_eprom/test_val_eprom.cpp lines 705-960 — count_control_reg_writes (~708), recording_has_consecutive_control_writes (~724), and RC-98A/B/C (~758-943) incl. the RC-98B docstring "4 writes" (~835-836) vs body "5" (~860) vs the TEST_ASSERT_LESS_OR_EQUAL(5,...) (~866); the RUN_TEST registrations (~981-983).
    - firestarter/test/native/avr/_shared/host_stubs_common.inc lines 55-145 — the bus recording harness (rurp_write_to_register capture, recorded_reg/recorded_data, HOST_STUBS_CUSTOM_HW_REVISION toggle, and the default passthrough rurp_map_ctrl_reg_for_hardware_revision stub at ~138).
    - firestarter/include/rurp_hw_rev_utils.h lines 15-40 — the REAL remap the WR-01 test must replicate/exercise: the Rev-2 branch (passthrough mask incl. CTRL_READ_WRITE at line 23; the CTRL_ADDRESS_LINE_18 → CTRL_ADDRESS_LINE_18_REV2==0x08 fold) AND the Rev-0/1 branch (`ctrl_reg = data` — CTRL_READ_WRITE 0x40 and CTRL_ADDRESS_LINE_18 0x20 are DISTINCT there).
    - firestarter/include/rurp_pinout.h lines 72-130 — CTRL_READ_WRITE (0x40, revision-invariant), CTRL_VPP_P1_ENABLE (0x08), CTRL_ADDRESS_LINE_18 (0x20 legacy), the REV2 aliases.
  </read_first>
  <behavior>
    - WR-01 Rev-2 case (RED against 98-02, GREEN against 98-03/04): a program-window CONTROL value with pin 31 = RW held program-active (WRITE_FLAG=0 → CTRL_READ_WRITE 0x40 LOW) AND P1/VPP asserted (CTRL_VPP_P1_ENABLE 0x08 HIGH), fed through the Rev-2 remap, yields a physical byte where bit 0x40 is LOW (program-active pin 31) and bit 0x08 is HIGH (VPP). The old 98-02 value (drive logical CTRL_ADDRESS_LINE_18 → physical 0x08 only) leaves physical 0x40 unasserted → FAILS.
    - WR-01 Rev-0/1 case (RED against 98-02, GREEN against 98-03/04): the same corrected CONTROL value through the Rev-0/1 (legacy) remap yields physical 0x40 LOW (pin 31 program-active). The old 98-02 value cleared logical 0x20 (a genuine A18 line on legacy), leaving 0x40 unasserted → FAILS; the corrected value reaches pin 31 at 0x40. Proves the no-alias path still reaches pin 31.
    - WR-02 (RC-98B): docstring says "5 writes"; assertion is TEST_ASSERT_EQUAL(5, ctrl_writes) (exact, not `<=`).
    - RC-98A / RC-98C updated: pin 31's program-active hold arrives via the address/rw path (config.rw_line set from rw-pin:[31]), NOT an extra CONTROL write. Set the test bus_config rw_line to the resolved value (22) and assert the corrected artifact (the WRITE top_address CONTROL write carries CTRL_READ_WRITE=0), not "≥6 CONTROL writes."
  </behavior>
  <action>
    Add a revision-parametrized WR-01 test (e.g. test_wr01_pin31_pgm_low_across_revisions, or two named cases test_wr01_rev2_... and test_wr01_rev01_...): build a program-window CONTROL value (CTRL_VPP_P1_ENABLE HIGH for VPP; CTRL_READ_WRITE at the program-active LOW level for a write) and pass it through rurp_map_ctrl_reg_for_hardware_revision for EACH of REVISION_2_0 and a legacy revision (REVISION_0 or REVISION_1). Exercise the remap either by defining HOST_STUBS_CUSTOM_HW_REVISION and driving rurp_get_hardware_revision per case to call the real remap, OR by two local replicas mirroring rurp_hw_rev_utils.h lines 21-30 exactly (the Rev-2 passthrough-mask+fold branch and the Rev-0/1 `ctrl_reg = data` + DROP_ENABLE_REV1 branch) — choose the approach that compiles under [env:native] and document it in a header comment. For EACH revision assert `(physical & CTRL_READ_WRITE) == 0` (pin 31 program-active LOW). Additionally, for the Rev-2 case assert `(physical & CTRL_VPP_P1_ENABLE) != 0` (VPP asserted concurrently — proves the 0x08 alias does not defeat 0x40). Each assertion message must state it FAILS for the 98-02 logical-A18-clear value on that revision (Rev-2: alias no-op; Rev-0/1: cleared a genuine A18 line, never set 0x40). Register each case via RUN_TEST. WR-02: correct the RC-98B docstring "4 writes" → "5 writes" and change TEST_ASSERT_LESS_OR_EQUAL(5, ctrl_writes) → TEST_ASSERT_EQUAL(5, ctrl_writes). Update RC-98A and RC-98C: set h.bus_config.rw_line to the pin-31-resolved value (22) rather than 0xFF, and replace the "extra CONTROL write" / "≥6" / "≥63" artifact assertions with assertions on the corrected rw_line reality (WRITE-phase top_address CONTROL write has CTRL_READ_WRITE cleared). Keep RC-98B's D-04 exclusion intent for the 512K case (the A18 user must not get pin 31 pulled program-active in a way that corrupts A18) — express it against the corrected mechanism.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c "test_wr01" test/native/avr/test_val_eprom/test_val_eprom.cpp</automated>
    <automated>cd /workspaces/firestarter && grep -c "REVISION_2_0\|REVISION_0\|REVISION_1" test/native/avr/test_val_eprom/test_val_eprom.cpp</automated>
    <automated>cd /workspaces/firestarter && grep -c "TEST_ASSERT_EQUAL(5, ctrl_writes)\|TEST_ASSERT_EQUAL_MESSAGE(5, ctrl_writes" test/native/avr/test_val_eprom/test_val_eprom.cpp</automated>
    <automated>cd /workspaces/firestarter && test "$(grep -v '^\s*\*' test/native/avr/test_val_eprom/test_val_eprom.cpp | grep -c 'baseline (4 writes)')" -eq 0 && echo "RC-98B '4 writes' docstring corrected"</automated>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_val_eprom*"</automated>
  </verify>
  <acceptance_criteria>
    - The WR-01 test covers BOTH Rev 2 AND Rev 0/1 (legacy), each registered via RUN_TEST; each asserts physical CTRL_READ_WRITE (0x40) LOW after the per-revision remap; the Rev-2 case also asserts CTRL_VPP_P1_ENABLE (0x08) HIGH; messages document the FAIL against the 98-02 value on that revision.
    - RC-98B assertion is TEST_ASSERT_EQUAL(5, ...) (exact) and no "baseline (4 writes)" text remains.
    - RC-98A / RC-98C set rw_line=22 and assert the corrected rw_line artifact (WRITE top_address CTRL_READ_WRITE cleared), not the removed "extra CONTROL write" count.
    - `pio test -e native -f "*test_val_eprom*"` passes with all RC-98 + both WR-01 revision cases green.
  </acceptance_criteria>
  <done>WR-01 revision-parametrized physical test (Rev 2 + Rev 0/1) present + green; WR-02 pinned to EQUAL(5); RC-98A/C updated to the rw_line mechanism; eprom suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Golden-trace byte-identity + full native suite green (D-05 regression tripwire, FIX-02)</name>
  <read_first>
    - firestarter/test/native/avr/test_val_eprom/golden_eprom_0x07_write.inc, golden_eprom_0x0B_write.inc, golden_eprom_chip_id.inc, golden_eprom_0x08_write.inc — the pinned (reg,data) traces; the 0x08 trace header notes pins=0 default (gate never fired) so the revert must not change it.
    - firestarter/test/native/avr/_shared/golden_trace.h — the assert_golden_trace mechanism + GOLDEN_BLESS re-bless workflow.
    - firestarter/platformio.ini lines 69-90 — the [env:native] test suite list (all test_val_* families that must stay green).
  </read_first>
  <action>
    Run the full native suite (`pio test -e native`) and confirm the v1.16 golden traces for 0x07, 0x0B, and chip-id are byte-identical (no re-bless). The 0x08 golden trace (pins=0 default → the removed gate never fired even pre-revert) must also stay byte-identical — confirm no rows changed. If ANY 0x07/0x0B/chip-id golden trace changed, that is a D-05 regression: STOP and investigate (the revert must not touch those paths). If the 0x08 32-pin trace legitimately changes as a consequence of the rw_line reality, re-bless ONLY that trace via GOLDEN_BLESS and record the cited rationale (the CTRL_READ_WRITE=0 WRITE artifact) in the SUMMARY and in the .inc header. Confirm `git diff` on the three protected .inc files is empty. Record the total native test count (was 117 pre-WR-01; expect +2 for the two WR-01 revision cases) in the SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(git diff --stat test/native/avr/test_val_eprom/golden_eprom_0x07_write.inc test/native/avr/test_val_eprom/golden_eprom_0x0B_write.inc test/native/avr/test_val_eprom/golden_eprom_chip_id.inc | wc -l)" -eq 0 && echo "0x07 / 0x0B / chip-id golden traces byte-identical (D-05)"</automated>
    <automated>cd /workspaces/firestarter && pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - `git diff` on golden_eprom_0x07_write.inc, golden_eprom_0x0B_write.inc, golden_eprom_chip_id.inc is empty (byte-identical — D-05).
    - Any 0x08-trace change (if any) is re-blessed with cited rationale recorded in the SUMMARY + .inc header; otherwise the 0x08 trace is also unchanged.
    - `pio test -e native` is fully green; the SUMMARY records the total test count (prior 117 + the two WR-01 revision cases).
  </acceptance_criteria>
  <done>Protected golden traces byte-identical; full native suite green; test count recorded (FIX-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host wire bus_config → firmware control register | config.rw_line decides pin 31 is the /PGM write strobe. A wrong rw_line handling would put the wrong level on a physical socket pin (hardware damage). |
| program window → physical socket pins | The CE pulse drives pin 1 (VPP up to 13V) and pin 31 (/PGM). On Rev 2 the P1/VPP (0x08) and legacy-A18 (0x20→0x08) bits alias; the fix must drive pin 31 via CTRL_READ_WRITE (0x40), the DISTINCT, revision-invariant physical bit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-13 | Tampering (physical no-op / wrong bit — CR-01) | memory_set_data PGM assert | mitigate | Revert the logical-A18-clear (Rev-2 physical no-op via the 0x08 alias; Rev-0/1 cleared a genuine A18 line); rely on the rw_line mechanism that drives CTRL_READ_WRITE (0x40), revision-invariant and passthrough on all revs. WR-01 (Rev 2 + Rev 0/1) asserts the post-remap physical bit — the missing RED state. |
| T-98-14 | Tampering (hardware damage — D-04 alias) | 512K A18 user on the 0x08 path | mitigate | The removed gate no longer mutates CONTROL; RC-98B (D-04 exclusion, EQUAL(5)) confirms the 512K A18 user gets no program-active pin-31 pull that would corrupt A18. Any retained 0x08/32-pin behavior additionally requires using_p1_as_vpp (WR-04). |
| T-98-15 | Tampering (over-voltage) | vpp_check_window (primitives.cpp) | mitigate | Untouched — over-voltage stays HIGH→ERROR, no FLAG_FORCE relaxation (SAFE-02 / D-06); `git diff primitives.cpp` empty (Task 1 acceptance). |
| T-98-16 | Tampering (masked regression) | golden traces 0x07/0x0B/chip-id | mitigate | D-05 byte-identity tripwire — Task 3 asserts empty git diff on the three protected traces; only the 0x08 trace may be re-blessed with cited rationale; WR-01 + RC-98C mismatch case catch the correct-vs-incorrect fork (P89 CR-01 lesson). |
| T-98-17 | Repudiation (dismissible bench result) | Phase-99 escape clause | mitigate | Remove the memory.cpp HIGH-1 caveat pre-excusing "0 bits at addr 0"; the corrected fix makes a real physical change, so a still-broken Phase-99 result is a genuine signal, not a pre-dismissed one. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | No package installs — firmware native/AVR build via the existing PlatformIO toolchain only. |
</threat_model>

<verification>
- Inert A18-clear reverted; revision-agnostic rw_line mechanism relied on; gate reconciled (WR-04); escape clause removed.
- WR-01 asserts post-remap 0x40 LOW on BOTH Rev 2 (0x08 HIGH concurrently) and Rev 0/1 (RED vs 98-02 on both, GREEN vs corrected).
- WR-02: RC-98B docstring "5 writes" + TEST_ASSERT_EQUAL(5, ...).
- RC-98A/C updated to the rw_line reality.
- 0x07/0x0B/chip-id golden traces byte-identical (D-05); full native suite green.
- primitives.cpp + eprom.cpp untouched (SAFE-02).
</verification>

<success_criteria>
- CR-01 firmware core delivered: pin 31 = /PGM held VIL via CTRL_READ_WRITE
  (0x40) — physically real and revision-agnostic (legacy/Rev1/Rev2) (FIX-01).
- WR-01 revision-parametrized physical test is the CI-catchable RED state; WR-02
  pinned; RC-98A/C corrected; escape clause removed (FIX-02).
- Golden traces byte-identical; native suite green; SAFE-02 files untouched.
- BENCH CAVEAT recorded: Phase-99 real-silicon proof is Leonardo + Rev 2.0 only;
  Rev 0/1 covered by the code path + native tests, not bench this milestone.
</success_criteria>

<output>
Create `.planning/phases/98-fix-correct-the-0x08-32-pin-write-vpp-path/98-04-SUMMARY.md` when done
</output>
