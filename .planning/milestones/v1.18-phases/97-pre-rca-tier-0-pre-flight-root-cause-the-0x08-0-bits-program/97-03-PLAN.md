---
phase: 97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program
plan: 03
type: execute
wave: 3
depends_on: ["97-02"]
files_modified:
  - .planning/v1.18/bench/EVIDENCE.md
  - .planning/v1.18/bench/EVIDENCE.json
  - .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md
autonomous: false
requirements: [RCA-02, RCA-03]
user_setup: []
must_haves:
  truths:
    - "RCA-02: the passing 0x07 W27C512 control write (same session, same configure_eprom handler) exonerates the unchanged axes and isolates the differing variable(s) to the 32-pin / P1-VPP-delivery / pin-31-as-address region"
    - "RCA-03: RC-1 (PGM pin 31 modeled as an address line) AND RC-2 (P1 VPP routing/level at pin 1) EACH carry a recorded confirm-or-exonerate verdict backed by bench + code-analysis evidence (D-03)"
    - "RC-3 (JP4) and RC-4 (32-pin addressing collision) are pursued ONLY IF the resolved RC-1+RC-2 pair does not fully account for the 0-bits symptom (conditional, not unconditional)"
    - "A named root cause (or ranked hypotheses each with disconfirming evidence) is recorded and classified firmware-algorithm / host-pinout / VPP-routing / addressing / silicon — sufficient to design the Phase-98 fix without further RCA"
  artifacts:
    - path: ".planning/v1.18/bench/EVIDENCE.json"
      provides: "Filled W27C512 0x07 differential-control cell (write→read→verify PASS) alongside the 0x08 attempt row"
      contains: "W27C512"
    - path: ".planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md"
      provides: "Completed RCA verdict: differential matrix, RC-1..RC-5 disconfirmation table with verdicts, named root cause + classification, Phase-98 hand-off"
      contains: "Classification"
  key_links:
    - from: "the 0x07 W27C512 control write"
      to: "the RCA-02 differential matrix axes"
      via: "same-session same-handler 2xN isolation (passing sibling exonerates unchanged axes)"
      pattern: "configure_eprom"
    - from: "the held-rail pin-1/pin-31 readings (Plan 02) + code-analysis"
      to: "RC-1 and RC-2 verdicts"
      via: "disconfirmation logic (CONFIRM/EXONERATE per the RESEARCH table)"
      pattern: "RC-1|RC-2"
---

<objective>
Close the RCA: run the passing 0x07 W27C512 differential control in the same bench
session, then synthesize the verdict — resolving RC-1 (PGM pin 31 as address line) AND
RC-2 (P1 VPP routing/level) each individually (D-03), pursuing RC-3/RC-4 only if the pair
leaves the 0-bits symptom unexplained, and recording a named root cause (or ranked
hypotheses) classified sufficiently for Phase 98 to design the fix.

Purpose: The differential exonerates everything the passing 0x07 part shares with the
failing 0x08 part, collapsing the candidate space to the two converging 32-pin axes; the
Plan-02 held-rail pin-1/pin-31 readings plus the file:line code-analysis then settle each
axis. Phase 98's fix surface is whatever this verdict names.

Output: the filled W27C512 0x07 cell in .planning/v1.18/bench/EVIDENCE.{md,json}, and the
completed RCA-02 + RCA-03 sections of evidence/97-RCA-FINDINGS.md.

`autonomous: false` — the operator reseats W27C512 for the 0x07 control write and (if RC-3
triggers) toggles JP4 with a DMM re-read. Claude drives all `firestarter` commands and
authors the verdict synthesis.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-CONTEXT.md
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-RESEARCH.md
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Reseat W27C512 for the 0x07 differential control (same session)</name>
  <what-built>
    The 0x08 AM27C020 attempt + held-rail readings are captured (Plan 02). To run the
    RCA-02 differential in the SAME session with bench identity fixed, the operator must
    swap in the passing 0x07 control chip.
  </what-built>
  <read_first>
    - 97-RESEARCH.md §"0x07-vs-0x08 Differential Matrix (single-session, RCA-02)" (the sequence: 0x08 probe → seat W27C512 → 0x07 write→read→verify → record both rows)
    - 97-CONTEXT.md decision D-08 (re-confirm controller identity + R1/R2 after any swap; Leonardo chip-OUT-sideload-EXEMPT)
  </read_first>
  <how-to-verify>
    Operator removes the AM27C020 and seats the W27C512 in the correct socket position for
    the Rev 2.0 shield, then confirms "W27C512 seated? (yes/no)". Claude re-runs
    `firestarter hw` and re-confirms `controller:` identity + live R1/R2 (ACM port may have
    shuffled across the swap — D-08), recording a fresh Bench Discipline Log row.
  </how-to-verify>
  <acceptance_criteria>
    - Operator-confirmed W27C512 seated; `firestarter hw` re-run post-swap; controller
      identity + R1/R2 + port re-recorded into a new Bench Discipline Log row.
  </acceptance_criteria>
  <resume-signal>Operator confirms W27C512 seated; type "0x07 seated" or describe an issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: RCA-02 — 0x07 W27C512 differential control write + matrix</name>
  <files>.planning/v1.18/bench/EVIDENCE.json, .planning/v1.18/bench/EVIDENCE.md, .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md</files>
  <read_first>
    - 97-RESEARCH.md §"0x07-vs-0x08 Differential Matrix" (the 8-row axis table + the two converging differing axes: P1-VPP-delivery + pin-31-as-address)
    - 97-RESEARCH.md §"Architecture Patterns" Pattern 3 (2xN differential isolation)
    - firestarter/src/proms/memory.cpp:122 (configure_eprom — shared handler) and eprom.cpp:319-326 (using_p1_as_vpp rewrite, 0x08-only)
    - firestarter_app/firestarter/database.py:141 (pin_conversions[32][31]=22) and data/pinouts.json (DIP32_STD vs 28-pin W27C512)
  </read_first>
  <action>
    Claude drives the passing 0x07 control to exonerate the unchanged axes (RCA-02):

    (a) `firestarter info W27C512` → confirm 0x07 / 28-pin decode; record.
    (b) Run write→read→verify on a small deterministic known image (reuse the v1.15
    approach), capturing the result. Expect PASS (W27C512 wrote clean the same session in
    v1.15). Record write-image SHA, readback SHA, verdict, and the VPP ADC / DMM pin-1
    reading for the 0x07 part into the W27C512 cell of EVIDENCE.{md,json}.
    (c) Fill the RCA-02 Differential Matrix in 97-RCA-FINDINGS.md from the RESEARCH 8-row
    table, marking each axis Differs? yes/no with its file:line anchor: Handler
    (memory.cpp:122, same — exonerated), Pulse width (same), Program-pulse model (CE-only,
    same), VPP routing (0x07 VPE-drop bus vs 0x08 vpp_line=21 P1/pin-1 — DIFFERS),
    Program-enable bit (CTRL_VPE_ENABLE vs rewritten CTRL_VPP_P1_ENABLE — DIFFERS), Pin-31
    role (28-pin none vs 32-pin bus-line-22 address-driven — DIFFERS), Pin-1 role (DIFFERS).
    Conclude: the matrix collapses to the two converging differing axes — P1-VPP-delivery
    and pin-31-as-address — both 32-pin/0x08-only, both absent on the passing 0x07 part.
    NEVER fabricate the 0x07 result — if it does NOT pass, record the actual outcome and
    flag that the differential control is anomalous (which would change the analysis).
  </action>
  <verify>
    <automated>python3 .planning/v1.18/bench/check_diff07.py</automated>
    <human-check>Operator confirms the 0x07 W27C512 write→read→verify outcome recorded matches the bench result (PASS expected).</human-check>
  </verify>
  <acceptance_criteria>
    - W27C512 cell filled: info decode (0x07/28-pin), write-image SHA, readback SHA, VPP/pin-1
      reading, verdict (no "TBD").
    - The RCA-02 Differential Matrix is complete with per-axis Differs? + file:line anchors
      and the explicit two-axis-collapse conclusion.
    - If the 0x07 control did NOT pass, that anomaly is recorded (not glossed).
  </acceptance_criteria>
  <done>RCA-02 differential complete: the passing 0x07 control exonerates the shared axes and isolates the differing variables to the P1-VPP-delivery + pin-31-as-address pair.</done>
</task>

<task type="auto">
  <name>Task 3: RCA-03 — resolve RC-1 + RC-2, conditional RC-3/RC-4, name + classify root cause</name>
  <files>.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md</files>
  <read_first>
    - 97-RESEARCH.md §"RC-1..RC-5 Disconfirmation Logic" (the table CONFIRM/EXONERATE criteria + conditional RC-3/RC-4 trigger + the RC-1/RC-4 alias interaction)
    - 97-RESEARCH.md §"Held-Rail Static-Proxy Procedure" (what pin 31 / pin 1 readings mean per verdict)
    - 97-CONTEXT.md decisions D-03 (RC-1 AND RC-2 each get a verdict; RC-3/RC-4 conditional; RC-5 via Tier-0) and D-06 (deferral is a Phase-99 verdict, NEVER reached here)
    - evidence/97-RCA-FINDINGS.md RCA-01 + RCA-02 sections (captured signature, pin-1/pin-31 readings, differential)
    - Code anchors: memory.cpp:346-356 (CE-only pulse), memory.cpp:187-202 (top-address), database.py:141, pinouts.json DIP32_STD, eprom.cpp:319-326, rurp_pinout.h:~128 (0x08 firmware alias)
  </read_first>
  <action>
    Synthesize the verdict (analysis — no new bench unless RC-3 triggers):

    (a) RC-1 verdict (PGM pin 31 as address line): combine the Plan-02 held-rail pin-31 DMM
    reading with code-analysis (pin_conversions[32][31]=22; DIP32_STD pin 31 = 19th
    address-bus pin; memory_set_data strobes only CE; top-address derives line 22 from
    address>>16). Per the disconfirmation table: pin 31 VIH/float → RC-1 CONFIRMED; pin 31
    ~0V (VIL) AND code-driven-low at addr0 → RC-1 EXONERATED (coincidentally program-active
    at addr0 though still modeled as an address line — note this matters for non-zero
    addresses). Record verdict with both bench + code evidence.

    (b) RC-2 verdict (P1 VPP routing/level): pin-1 DMM vs `firestarter vpp` ADC node. pin 1
    = 12.5-13.0V AND ADC agrees → RC-2 EXONERATED; pin 1 ~0V/float while ADC ~13V → RC-2
    CONFIRMED (P1 routing); pin 1 < 12.5V → RC-2 CONFIRMED (level/pot). Record verdict with
    the divergence evidence (most decisive single measurement).

    (c) Conditional RC-3/RC-4 (D-03): pursue ONLY IF, after RC-1 + RC-2 each carry a verdict,
    the resolved pair does NOT fully account for the 0-bits symptom (e.g. both EXONERATED yet
    0 bits flipped). If triggered: RC-3 (JP4) = operator-action — surface a blocking
    checkpoint to toggle JP4 open↔closed (silkscreen meaning already captured Plan 02) and
    re-DMM pin 1; RC-4 (addressing collision) = code-analysis of the 0x08 alias
    (CTRL_VPP_P1_ENABLE == CTRL_ADDRESS_LINE_18, rurp_pinout.h:~128) + the Plan-02
    0x180-vs-0x188 readings (did pin 31 / pin 1 move together?), using read/dev inspection
    only — NO second destructive program (Pitfall 7). If NOT triggered, record RC-3/RC-4 as
    "not pursued — RC-1+RC-2 account for the symptom" with rationale, and RC-5 as
    "INDETERMINATE pre-fix (Tier-0), never triggers deferral (D-01/D-06)".

    (d) Name + classify the root cause (or ranked hypotheses each with disconfirming
    evidence); classification ∈ {firmware-algorithm, host-pinout, VPP-routing, addressing,
    silicon}. RC-1 AND RC-2 MUST each carry a verdict in the disconfirmation table (the D-03
    exit bar). Write the Phase-98 Hand-Off naming the implicated fix surfaces (eprom.cpp /
    memory.cpp / pinouts.json DIP32_27C020 / firmware alias), scoped so existing
    27C040/SST39SF040-family DIP32 users (pin 31 = A18/WE) are NOT broken. Set status
    COMPLETE. Do NOT record any deferral disposition — that is a Phase-99 verdict (D-06).
  </action>
  <verify>
    <automated>python3 .planning/v1.18/bench/check_verdict.py</automated>
  </verify>
  <acceptance_criteria>
    - RC-1 AND RC-2 each carry a recorded confirm-or-exonerate verdict with bench + code
      evidence (D-03 exit bar) — verified by check_verdict.py.
    - RC-3/RC-4 either pursued (with their evidence) or explicitly recorded "not pursued"
      with rationale; RC-5 recorded INDETERMINATE pre-fix (never triggers deferral).
    - A named root cause (or ranked hypotheses) + classification ∈ the five categories is
      recorded; Phase-98 Hand-Off names the fix surfaces scoped to the 0x08-UV-32-pin class.
    - 97-RCA-FINDINGS.md status = COMPLETE; no deferral disposition recorded (D-06).
  </acceptance_criteria>
  <done>RCA-03 closed: RC-1 + RC-2 each verdicted, conditional RC-3/RC-4 handled, root cause named + classified, Phase-98 fix surfaces handed off — sufficient to design the fix without further RCA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI → firmware (0x07 control write) | The differential write crosses into the firmware program pulse on the W27C512; uses plain dispatch, no guard relaxation. |
| operator JP4 toggle ↔ live VPP routing (RC-3, conditional) | Toggling JP4 the wrong way could mis-route ~13V; gated on the operator-stated silkscreen meaning (D-08) and only triggered if RC-1+RC-2 leave 0-bits unexplained. |
| RCA verdict → Phase-98 fix design | The named root cause governs which source surfaces Phase 98 edits; a wrong/under-evidenced verdict would misdirect the fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-SAFE-01 | Tampering | over-voltage VPP guard + host `resolve_chip` | mitigate | The 0x07 control write uses plain `firestarter write` — no --force, no -b-skip, no escape hatch; over-voltage stays ERROR-blocked (confirmed Plan 01). |
| T-97-JP4 | Tampering | JP4 / VPP routing (RC-3 conditional) | mitigate | JP4 toggled only if RC-1+RC-2 leave the symptom unexplained, only after the operator states the Rev 2.0 silkscreen meaning (D-08), with a re-DMM of pin 1 each position. |
| T-97-VERDICT | Spoofing (false root cause) | RCA-03 verdict | mitigate | check_verdict.py asserts RC-1 AND RC-2 each carry an explicit verdict + a named classification; the verdict pairs every bench reading with a code-analysis finding so it survives an ambiguous measurement. |
| T-97-PREMATURE-DEFER | Repudiation | deferral disposition | mitigate | D-01/D-06: a 0-flip is INDETERMINATE pre-fix and NEVER triggers deferral here; check_verdict.py + acceptance criteria forbid recording a deferral disposition in Phase 97. |
| T-97-FAB | Repudiation | 0x07 differential result | mitigate | The W27C512 outcome is recorded verbatim; a non-PASS is flagged as an anomaly, not glossed (D-02). |
</threat_model>

<verification>
- check_diff07.py passes: the W27C512 0x07 differential cell is filled with a recorded verdict.
- check_verdict.py passes: RC-1 AND RC-2 each carry a verdict, a classification is named, the
  0x07 differential cell is filled.
- 97-RCA-FINDINGS.md is COMPLETE: differential matrix + RC-1..RC-5 disconfirmation table with
  verdicts + named root cause + classification + Phase-98 hand-off; no deferral recorded (D-06).
</verification>

<success_criteria>
- RCA-02: the passing 0x07 control exonerates the shared axes; the candidate space collapses
  to the two converging 32-pin axes.
- RCA-03: a named root cause (or ranked hypotheses, RC-1 AND RC-2 each verdicted) classified
  sufficiently for Phase 98 to design the fix without further RCA.
</success_criteria>

<output>
Create `.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-03-SUMMARY.md` when done.
</output>
