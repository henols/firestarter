---
phase: 97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md
  - .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md
  - .planning/v1.18/bench/EVIDENCE.md
  - .planning/v1.18/bench/EVIDENCE.json
  - .planning/v1.18/bench/check_pre01.py
  - .planning/v1.18/bench/check_signature.py
  - .planning/v1.18/bench/check_diff07.py
  - .planning/v1.18/bench/check_verdict.py
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "SAFE-01: over-voltage HIGH path is ERROR-blocked and only relaxes under FLAG_FORCE — confirmed by file:line code-read, never triggered"
    - "SAFE-01: the Phase-97 bench procedure uses no --force / no -b-skip / no test-only escape hatch; chip_resolver.resolve_chip stays in the live write path"
    - "The v1.18 EVIDENCE record + RCA-FINDINGS doc + SAFE-01 note + PRE-01 result placeholder all exist, pre-populated with the bench-discipline schema, ready for the Wave-2/3 bench captures to fill (never fabricated)"
    - "The held-rail static-proxy control-register values (P1-route-on vs P1-route-off pair) are pinned against the live `dev reg -f` host bit map, with the host-vs-firmware bit-alias caveat documented"
    - "The four Wave-0 gate scripts (.planning/v1.18/bench/check_pre01.py, check_signature.py, check_diff07.py, check_verdict.py) are committed, git-tracked Wave-0 gate infrastructure that Plans 02/03 depend on"
  artifacts:
    - path: ".planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md"
      provides: "RCA verdict doc skeleton — bench-discipline log, RCA-01/02/03 sections, RC-1..RC-5 disconfirmation table (verdicts TBD), SAFE-01 close-out, Phase-98 hand-off"
      contains: "RC-1..RC-5"
    - path: ".planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md"
      provides: "SAFE-01 non-invasive confirmation note with file:line evidence"
      contains: "vpp_check_window"
    - path: ".planning/v1.18/bench/EVIDENCE.json"
      provides: "Machine-readable bench EVIDENCE record (v1.15 schema) with empty cells for the 0x08 attempt + 0x07 control"
      contains: "AM27C020"
    - path: ".planning/v1.18/bench/EVIDENCE.md"
      provides: "Human-readable bench EVIDENCE record mirroring the JSON"
      contains: "AM27C020"
    - path: ".planning/v1.18/bench/check_pre01.py"
      provides: "Wave-0 PRE-01 gate script consumed by Plan 02 Task 2 automated verify (committed, git-tracked)"
      contains: "pre_01_result"
    - path: ".planning/v1.18/bench/check_signature.py"
      provides: "Wave-0 RCA-01 signature gate script consumed by Plan 02 Task 3 automated verify (committed, git-tracked)"
      contains: "AM27C020"
    - path: ".planning/v1.18/bench/check_diff07.py"
      provides: "Wave-0 RCA-02 differential gate script consumed by Plan 03 Task 2 automated verify (committed, git-tracked)"
      contains: "W27C512"
    - path: ".planning/v1.18/bench/check_verdict.py"
      provides: "Wave-0 RCA-03 verdict gate script consumed by Plan 03 Task 3 automated verify (committed, git-tracked)"
      contains: "RC-1"
  key_links:
    - from: "evidence/SAFE-01-PREFLIGHT.md"
      to: "firestarter/src/proms/primitives.cpp"
      via: "file:line citation of vpp_check_window HIGH->ERROR / FLAG_FORCE-only relaxation / LOW->WARN"
      pattern: "primitives\\.cpp:1[0-9][0-9]"
    - from: "evidence/97-RCA-FINDINGS.md"
      to: "the held-rail proxy commands"
      via: "pinned dev reg -f control values cited against cli_handlers.py bit map"
      pattern: "dev reg 0 0 0x18"
    - from: "Plans 02 + 03 automated verify blocks"
      to: ".planning/v1.18/bench/check_pre01.py + check_signature.py + check_diff07.py + check_verdict.py"
      via: "python3 invocation of the committed Wave-0 gate scripts"
      pattern: "check_(pre01|signature|diff07|verdict)\\.py"
---

<objective>
Stand up the diagnostic scaffolding for the Phase-97 RCA and close the SAFE-01
non-invasive confirmation — entirely from the devcontainer with no hardware, so
the Wave-2/3 bench tasks land directly into ready-to-fill artifacts.

This plan delivers: (1) the SAFE-01 confirmation note (file:line evidence the
over-voltage ERROR path and host guard are intact and unbypassed — a *read*, never
a trigger); (2) the v1.18 bench EVIDENCE record (md+json, v1.15 schema) with empty
0x08-attempt and 0x07-control cells; (3) the 97-RCA-FINDINGS.md verdict-doc skeleton
mirroring v1.17 Phase-93; (4) the pinned held-rail static-proxy control-register
values verified against the live `dev reg -f` host bit map (resolving the
host-vs-firmware bit-alias question before the operator touches a DMM).

Wave-0 gate infrastructure (pre-staged, committed): the four gate scripts
.planning/v1.18/bench/check_pre01.py, check_signature.py, check_diff07.py and
check_verdict.py are committed, git-tracked Wave-0 infrastructure invoked by the
automated verify blocks of Plans 02 and 03. They are declared in this Wave-1 plan's
`files_modified` (and confirmed by Task 4) so their provenance is traceable to a task —
they are NOT recreated here (they already exist and are sound); Task 4 only asserts they
are present and tracked.

Purpose: A diagnostic phase produces defensible artifacts only if the capture
templates and the SAFE-01 invariant are locked *before* the single irreversible
bench session. This plan is the no-hardware foundation; Plans 02 and 03 fill it.

Output: evidence/SAFE-01-PREFLIGHT.md, evidence/97-RCA-FINDINGS.md (skeleton),
.planning/v1.18/bench/EVIDENCE.{md,json} (skeleton), and the recorded provenance of
the four committed Wave-0 gate scripts.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-CONTEXT.md
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-RESEARCH.md
@.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SAFE-01 non-invasive confirmation note (file:line code-read)</name>
  <files>.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md</files>
  <read_first>
    - firestarter/src/proms/primitives.cpp lines 90-160 (the full `vpp_check_window`: HIGH branch ~:118-128 sets RESPONSE_CODE_ERROR, relaxing to WARNING only under `is_flag_set(FLAG_FORCE)`; LOW branch ~:144-145 is WARNING/proceed-only)
    - firestarter_app/firestarter/chip_resolver.py (the `resolve_chip` guard, def at :16) — confirm it is the resolver the live `write` path calls
    - firestarter/src/proms/memory.cpp lines 118-125 (dispatch -> `configure_eprom`, protocol 0x08 has no special-case)
    - 97-RESEARCH.md §"SAFE-01 Non-Invasive Verification Steps" (the 4 confirmation steps) and §"User Constraints" D-07
    - 97-CONTEXT.md decision D-07 (the SAFE-01 invariant text)
  </read_first>
  <action>
    Confirm SAFE-01 by READING code only — never drive VPP high, never invoke a guard,
    never edit anything. Write evidence/SAFE-01-PREFLIGHT.md recording, per D-07, the
    four confirmations from 97-RESEARCH.md §"SAFE-01 Non-Invasive Verification Steps":

    (1) Over-voltage stays ERROR: cite the exact file:line in
    firestarter/src/proms/primitives.cpp where the VPP-HIGH branch sets
    RESPONSE_CODE_ERROR, and note that it relaxes to RESPONSE_CODE_WARNING ONLY when
    `is_flag_set(FLAG_FORCE)` is true. IMPORTANT NUANCE: the firmware DOES contain a
    FLAG_FORCE relaxation of the HIGH branch — so the SAFE-01 invariant is satisfied
    not by the firmware lacking relaxation, but by the Phase-97 procedure NEVER passing
    --force / FLAG_FORCE on the AM27C020 write attempt. State this explicitly. Cite the
    LOW branch (WARNING/proceed-only) and connect it to Pitfall 2 (a low VPP rail
    silently under-programs with no error — which is why the pin-1 DMM in Plan 02 is
    mandatory).

    (2) Host guard never bypassed: cite firestarter_app/firestarter/chip_resolver.py
    `resolve_chip` (:16) as the live-path guard, and state that the Plan-02/03 procedure
    uses plain `firestarter write` / `firestarter dev …` with NO test-only flag, NO `-b`
    skip-erase abuse, and NO escape-hatch argument.

    (3) Normal dispatch: cite firestarter/src/proms/memory.cpp:~122
    (dispatch -> `configure_eprom`) confirming protocol 0x08 has no special path; record
    that Phase 97 introduces ZERO firmware/host code edits (diagnostic only).

    (4) Manual-pot caveat: record that VPP magnitude is operator-set on the shield
    potentiometer (firmware enable/measure-only), so the pin-1 DMM (Plan 02) is the only
    proof the rail magnitude is correct — a SAFE-01-adjacent reason a measured rail is
    mandatory before any write verdict.

    Then run the bypass-command gate in <verify> to prove no Phase-97 plan AND no
    SAFE-01-PREFLIGHT.md text introduces an actual bypass *command invocation* (a
    `firestarter write|dev … --force` or `… -b`) for the AM27C020 write. The gate scans
    for real command invocations carrying a bypass flag — NOT free-text mentions of the
    words "--force"/"FLAG_FORCE"/"bypass" — so the plans' own legitimate SAFE-01 prose
    (which must discuss these terms to document the invariant) does not trip it, while a
    genuine bypass command anywhere in the three plans or this artifact still fails the
    gate. Use exact file:line anchors as they exist in the current tree (re-read; do not
    trust the RESEARCH line numbers blindly — the tree may have advanced past `bccd995`).
    No fenced code in the note beyond short inline `file:line` citations.
  </action>
  <verify>
    <automated>test -f .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md && grep -q "vpp_check_window" .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md && grep -q "resolve_chip" .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md && grep -q "FLAG_FORCE" .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md && ! grep -REns -- 'firestarter[[:space:]]+(write|dev)[^|]*[[:space:]](--force|-b)([[:space:]]|$|`)' .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-0*-PLAN.md .planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/SAFE-01-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - SAFE-01-PREFLIGHT.md records all four SAFE-01 confirmations, each with a concrete
      file:line citation against the CURRENT tree (verified by re-reading, not copied
      from RESEARCH).
    - The note explicitly states the firmware HIGH branch relaxes under FLAG_FORCE and
      that the invariant holds because the Phase-97 procedure never passes --force.
    - The note states the procedure uses no test-only flag / no -b skip / no escape hatch
      and that resolve_chip stays in the live path.
    - The bypass-command gate confirms no Phase-97 PLAN and no SAFE-01-PREFLIGHT.md text
      contains an actual `firestarter write|dev … --force`/`… -b` bypass *command*
      invocation for the AM27C020 write (free-text mentions of the terms do not trip it;
      a real bypass command does).
  </acceptance_criteria>
  <done>SAFE-01 is confirmed non-invasively with file:line evidence; no guard was triggered or relaxed; the procedure is proven bypass-free.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the v1.18 bench EVIDENCE record (md + json, v1.15 schema)</name>
  <files>.planning/v1.18/bench/EVIDENCE.md, .planning/v1.18/bench/EVIDENCE.json</files>
  <read_first>
    - .planning/v1.15/bench/EVIDENCE.json lines 1-60 (the schema: generated, phase, board, shield, locked_columns, cells[])
    - 97-RESEARCH.md §"Failure Signature Capture Schema" (the verbatim field list RCA-01 must capture)
    - 97-RESEARCH.md §"0x07-vs-0x08 Differential Matrix" (the two rows: 0x08 AM27C020 attempt + 0x07 W27C512 control)
    - 97-RESEARCH.md §"Artifacts the Phase Should Emit" (artifact 1 + 4)
    - 97-CONTEXT.md decision D-02 (PRE-01 = "writability indeterminate pre-fix", never fabricated) and D-08 (bench-discipline fields)
  </read_first>
  <action>
    Create .planning/v1.18/bench/EVIDENCE.json and a mirroring EVIDENCE.md, reusing the
    v1.15 EVIDENCE.{md,json} cell schema (DO NOT invent a new schema — Phase 99 + the
    PROTOCOL-LEDGER consume this shape; "Don't Hand-Roll" §EVIDENCE format).

    Top-level metadata: generated (ISO timestamp), milestone "v1.18", phase
    "97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program", board "leonardo",
    shield "Rev 2.0", note describing that cells are filled by Plans 02 (0x08 attempt)
    and 03 (0x07 control) — leave measured values as null/"TBD-bench" placeholders, NEVER
    fabricated.

    Create exactly TWO empty cells:
    - Cell A: chip "AM27C020", family "0x08 (EPROM_QUICK)", op "tier0_microprobe+rca01",
      with the full Failure Signature Capture Schema fields as keys (all "TBD-bench"):
      failing_addresses, bad_bytes, retries, bits_flipped, vpp_adc_mv, dmm_pin1_v,
      dmm_pin31_v, pre_read_sha256, post_read_sha256, controller, port, r1_readback,
      r2_readback, fw_commit, jp4_position, jp4_silkscreen_meaning, verdict.
    - Cell B: chip "W27C512", family "0x07 (EPROM_STD)", op "differential_control_write",
      with: write_image_sha256, readback_sha256, vpp_adc_mv, dmm_pin1_v, verdict — all
      "TBD-bench".

    Add a top-level "pre_01_result" field set to "TBD-bench (writability indeterminate
    pre-fix expected per D-01/D-02)" and a "blank_state_sha256": "TBD-bench" field — the
    PRE-01 result line (artifact 4). EVIDENCE.md mirrors the JSON as a readable table +
    the same bench-discipline columns from D-08.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.planning/v1.18/bench/EVIDENCE.json')); cells={c['chip'] for c in d['cells']}; assert {'AM27C020','W27C512'} <= cells, cells; assert 'pre_01_result' in d; assert any('TBD' in str(v) for c in d['cells'] for v in c.values()); print('EVIDENCE skeleton OK', cells)" && test -f .planning/v1.18/bench/EVIDENCE.md && grep -q "AM27C020" .planning/v1.18/bench/EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - EVIDENCE.json parses, contains exactly the AM27C020 (0x08) and W27C512 (0x07) cells,
      and reuses the v1.15 schema (locked_columns + cell keys).
    - The AM27C020 cell carries every Failure Signature Capture Schema field as a key,
      all set to "TBD-bench" (no fabricated measurements).
    - A top-level pre_01_result + blank_state_sha256 placeholder exists (PRE-01 line).
    - EVIDENCE.md mirrors the JSON as a human-readable table including the D-08
      bench-discipline columns.
  </acceptance_criteria>
  <done>The v1.18 bench EVIDENCE record exists in both formats with empty, schema-correct, never-fabricated cells ready for Plans 02/03 to fill.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 97-RCA-FINDINGS.md + pin the held-rail proxy control values</name>
  <files>.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md</files>
  <read_first>
    - .planning/phases/93-rca-root-cause-the-w29c040-page-0-write-fault/evidence/93-RCA-FINDINGS.md (the v1.17 template: frontmatter, Bench Discipline Log table, RCA-01/02/03 sections, disconfirmation table, SAFE-01 close-out, Phase-N hand-off)
    - 97-RESEARCH.md §"RC-1..RC-5 Disconfirmation Logic" (the 5-row table with CONFIRM/EXONERATE columns)
    - 97-RESEARCH.md §"Held-Rail Static-Proxy Procedure" (the 0x188 derivation + the 0x180-vs-0x188 alias experiment)
    - firestarter_app/firestarter/cli_handlers.py lines 1005-1024 (the live `dev reg -f` host bit map — the AUTHORITATIVE remapped values the operator/Claude will use)
    - firestarter/include/rurp_pinout.h lines 118-130 (the firmware Rev2 physical layout where CTRL_VPP_P1_ENABLE_REV2 == CTRL_ADDRESS_LINE_18_REV2 == 0x08)
    - 97-CONTEXT.md decisions D-03 (RC-1+RC-2 each get a verdict; RC-3/RC-4 conditional) and D-05 (held-rail proxy is the gating method; logic analyzer optional)
  </read_first>
  <action>
    Create evidence/97-RCA-FINDINGS.md mirroring the v1.17 93-RCA-FINDINGS.md structure,
    with frontmatter (artifact 97-RCA-FINDINGS, phase, milestone "v1.18 — AM27C020 0x08
    Write-Path RCA & Fix", requirements [PRE-01, RCA-01, RCA-02, RCA-03, SAFE-01],
    status "SCAFFOLD — bench captures pending Plans 02-03", operator_witnessed false until
    filled). Include these sections, all with "TBD — Plan 02/03 fills" placeholders where
    bench data goes (NEVER fabricated, D-02):

    - Bench Discipline Log table (D-08 columns: Plan, Timestamp, controller identity,
      Port, R1, R2, Board, Shield, JP4 position + silkscreen meaning, fw commit, Notes) —
      rows for Plan 02 and Plan 03, values TBD.
    - PRE-01 — Tier-0 Writability Pre-Flight (read-oracle N>=3, blank-state SHA, micro-probe
      result; record the expected outcome framing "writability INDETERMINATE pre-fix" per
      D-01/D-02 and that a 0-flip NEVER triggers deferral).
    - RCA-01 — Reproduction & Signature (the Failure Signature Capture Schema fields, TBD).
    - RCA-02 — 0x07-vs-0x08 Differential Matrix (paste the RESEARCH matrix; the two-axis
      collapse to P1-VPP-delivery + pin-31-as-address; verdicts TBD).
    - RCA-03 — RC-1..RC-5 Disconfirmation Table: paste the 5-row table verbatim with a
      "Verdict" column = TBD for each, and a callout that RC-1 AND RC-2 MUST each carry a
      verdict (D-03) and RC-3/RC-4 are pursued ONLY if the pair leaves 0-bits unexplained.
      Add a "Classification" placeholder (firmware-algorithm / host-pinout / VPP-routing /
      addressing / silicon).
    - SAFE-01 close-out (link to SAFE-01-PREFLIGHT.md).
    - Phase-98 Hand-Off (which fix surfaces the verdict implicates — TBD).

    PIN THE HELD-RAIL PROXY VALUES in a dedicated subsection, citing the live
    cli_handlers.py `dev reg -f` host bit map (re-read it; cite the current file:line). In
    the host `-f` namespace the bits are DISTINCT: 0x100=VPP_VPE_DROP_ENABLE,
    0x080=VPP_REGULATOR_ENABLE, 0x020=ADDRESS_LINE_18, 0x008=VPP_P1_ENABLE. Therefore:
      * Program-window held value (P1 route ON, A18/pin-31 bit clear) = 0x080 | 0x100 | 0x008 = 0x188.
      * Alias-probe pair: `dev reg 0 0 0x180 -f` (P1 OFF) vs `dev reg 0 0 0x188 -f` (P1 ON),
        which moves ONLY the P1_ENABLE bit (0x008) in host space — the decisive RC-1/RC-2/RC-4
        experiment from §"RC-1..RC-5".
    Document the host-vs-firmware caveat: in the FIRMWARE physical Rev2 layout
    (rurp_pinout.h:~128) CTRL_VPP_P1_ENABLE_REV2 and CTRL_ADDRESS_LINE_18_REV2 are the SAME
    bit (0x08), but the host `-f` remap presents them as distinct host bits — so the
    operator/Claude MUST use the host `-f` values above. Mark the held value 0x188 as
    [ASSUMED — confirm at first bench reading] per RESEARCH assumption A1. No fenced
    implementation code; short inline citations only.
  </action>
  <verify>
    <automated>F=.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/evidence/97-RCA-FINDINGS.md; test -f "$F" && grep -q "RC-1" "$F" && grep -q "RC-2" "$F" && grep -q "RC-5" "$F" && grep -q "dev reg 0 0 0x188" "$F" && grep -q "dev reg 0 0 0x180" "$F" && grep -qi "indeterminate" "$F" && grep -q "PRE-01" "$F" && grep -q "RCA-03" "$F"</automated>
  </verify>
  <acceptance_criteria>
    - 97-RCA-FINDINGS.md exists with frontmatter requirements [PRE-01, RCA-01, RCA-02,
      RCA-03, SAFE-01] and the full section set, all bench data marked TBD (never faked).
    - The RC-1..RC-5 disconfirmation table is present with a Verdict column and the D-03
      callout (RC-1 AND RC-2 each get a verdict; RC-3/RC-4 conditional).
    - The held-rail proxy subsection pins 0x188 (P1-on) and 0x180 (P1-off) in the host
      `dev reg -f` namespace, cites the live cli_handlers.py bit-map file:line, and
      documents the host-vs-firmware alias caveat.
    - PRE-01 framing records "writability indeterminate pre-fix" and that a 0-flip never
      triggers deferral (D-01/D-02).
  </acceptance_criteria>
  <done>The RCA verdict doc is scaffolded with all sections + the disconfirmation table, and the held-rail proxy commands are pinned against the live host bit map with the alias caveat documented.</done>
</task>

<task type="auto">
  <name>Task 4: Confirm provenance of the four committed Wave-0 gate scripts</name>
  <files>.planning/v1.18/bench/check_pre01.py, .planning/v1.18/bench/check_signature.py, .planning/v1.18/bench/check_diff07.py, .planning/v1.18/bench/check_verdict.py</files>
  <read_first>
    - .planning/v1.18/bench/check_pre01.py (Plan 02 Task 2 automated gate)
    - .planning/v1.18/bench/check_signature.py (Plan 02 Task 3 automated gate)
    - .planning/v1.18/bench/check_diff07.py (Plan 03 Task 2 automated gate)
    - .planning/v1.18/bench/check_verdict.py (Plan 03 Task 3 automated gate)
  </read_first>
  <action>
    Make the four Wave-0 gate scripts' provenance traceable to a task — do NOT recreate or
    rewrite them (they already exist, are committed, and are sound; the Plan-02/03
    automated verify blocks already invoke them). This task only ASSERTS their presence and
    git-tracking so a clean re-checkout cannot silently leave Plans 02/03 verifiers without
    their gates.

    Confirm all four scripts exist on disk, are git-tracked, and parse cleanly as Python
    (a `python3 -m py_compile` clean parse is sufficient — do NOT run them against bench
    data here; that happens in Plans 02/03 against filled cells). If any of the four is
    missing or untracked, STOP and flag it (the scripts are pre-staged Wave-0
    infrastructure; their absence is a blocker, not something to silently regenerate).
    Record the confirmation (file list + git-tracked status) in the Plan 01 SUMMARY so the
    dependency is documented.
  </action>
  <verify>
    <automated>for f in check_pre01 check_signature check_diff07 check_verdict; do P=.planning/v1.18/bench/$f.py; test -f "$P" || { echo "MISSING $P"; exit 1; }; git ls-files --error-unmatch "$P" >/dev/null 2>&1 || { echo "UNTRACKED $P"; exit 1; }; python3 -m py_compile "$P" || { echo "PARSE-FAIL $P"; exit 1; }; done; echo "all four Wave-0 gate scripts present, tracked, parse-clean"</automated>
  </verify>
  <acceptance_criteria>
    - All four .planning/v1.18/bench/check_*.py scripts exist, are git-tracked, and parse
      cleanly (py_compile).
    - The Plan 01 SUMMARY records that the four scripts are committed Wave-0 gate
      infrastructure on which Plans 02/03 automated verify blocks depend.
    - The scripts are NOT modified or regenerated by this task.
  </acceptance_criteria>
  <done>The four committed Wave-0 gate scripts are confirmed present, git-tracked, and parse-clean; their provenance is now traceable to this Wave-1 plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI -> firmware (serial) | The `write` command crosses into the firmware program pulse; SAFE-01 owns this seam (over-voltage block + host guard). NOT exercised in this plan (no hardware) — only code-read. |
| planning artifacts -> bench operator | The EVIDENCE/FINDINGS templates instruct the operator's irreversible bench actions; a template that encoded a bypass would route an unguarded ~13V rail at the seated chip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-SAFE-01 | Tampering | over-voltage VPP guard (`vpp_check_window` HIGH->ERROR) + host `chip_resolver.resolve_chip` | mitigate | Task 1 confirms by file:line read that the ERROR path is intact and relaxes only under FLAG_FORCE; the bypass-command gate proves no Phase-97 plan or artifact introduces a real `firestarter write|dev … --force`/`… -b` bypass command. Read-only — guard never triggered. |
| T-97-FAB | Repudiation | EVIDENCE / RCA-FINDINGS measured values | mitigate | All measured fields seeded as "TBD-bench" placeholders (Tasks 2,3); D-02 forbids fabricated results. Plans 02/03 fill only from real captures. |
| T-97-PROXY | Tampering | held-rail static-proxy control values | mitigate | Task 3 pins 0x188/0x180 against the LIVE host `dev reg -f` bit map (re-read, not copied) and documents the host-vs-firmware alias so the operator cannot mis-route VPP via a stale value. Held value marked [ASSUMED] pending bench confirm. |
| T-97-GATEPROV | Tampering | Wave-0 gate scripts (check_pre01/signature/diff07/verdict.py) | mitigate | Task 4 asserts the four scripts exist, are git-tracked, and parse-clean before Plans 02/03 rely on them; absence is a blocker, never silently regenerated. |
| T-97-NOCODE | Elevation | firmware / host source | accept | Phase 97 is diagnostic — no code edit. This plan writes only `.planning/**` files; no source path is in scope. |
</threat_model>

<verification>
- SAFE-01-PREFLIGHT.md cites the current-tree file:line for the VPP ERROR path, the
  FLAG_FORCE relaxation, and resolve_chip; the bypass-command gate confirms no real
  `firestarter write|dev … --force`/`… -b` invocation in any Phase-97 plan or the artifact.
- EVIDENCE.{md,json} parse, carry exactly the AM27C020 + W27C512 cells in the v1.15 schema,
  all measured fields "TBD-bench".
- 97-RCA-FINDINGS.md has the full section set + RC-1..RC-5 table + pinned 0x188/0x180 proxy
  values + the host-vs-firmware alias caveat.
- The four Wave-0 gate scripts are present, git-tracked, and parse-clean (Task 4).
- No firmware/host source file is modified (diagnostic phase).
</verification>

<success_criteria>
- SAFE-01 confirmed non-invasively with file:line evidence (SAFE-01 requirement satisfied for Phase 97).
- The three diagnostic artifacts exist, schema-correct, with zero fabricated bench data.
- The held-rail proxy commands are pinned to the live host bit map, ready for Plan 02's operator DMM.
- The four committed Wave-0 gate scripts' provenance is traceable to this Wave-1 plan.
</success_criteria>

<output>
Create `.planning/phases/97-pre-rca-tier-0-pre-flight-root-cause-the-0x08-0-bits-program/97-01-SUMMARY.md` when done.
</output>
