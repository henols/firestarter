---
phase: 145-bench-validation
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/145-bench-validation/145-BENCH-LOG.md
  - .planning/phases/145-bench-validation/145-02-SUMMARY.md
autonomous: false
requirements: [BENCH-02, BENCH-03]

must_haves:
  truths:
    - "Per D-15 BENCH-03 is proven by a machine-checked diff across the WHOLE v1.31 range from the app's branch base 4d18b645 to HEAD, not across this phase's commits, and the verbatim result is recorded"
    - "BENCH-03 is re-measured at the tip and recorded — it is not discovered for the first time here, and it is not a bench task"
    - "Per D-02 each of 0x08 and 0x0B gets a full disposition record naming the missing part, its last known bench state with numbers, and an explicit sentence that the disposition is NOT inferred from the 0x07 result"
    - "The 0x08 record carries 60/64, 0/64 and FUT-08, and states that under D-14's taxonomy that shape is a fail, not a qualified pass"
    - "The 0x0B record carries 22.4 V operator DMM and 23.9 V firmware VPE at max pot, and states the graduation is parked until a part is on hand"
    - "Per D-13 both BENCH-02 and BENCH-03 are complete before any silicon is touched, so a later halt cannot cost two requirements that never needed hardware"
    - "Per D-16 no file under firestarter/ or firestarter_app/ is created, edited or deleted — both sub-repos are read-only here"
  artifacts:
    - path: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      provides: "Gate 0's three completed evidence subsections: BENCH-03 invariance, the 0x08 disposition, the 0x0B disposition"
      contains: "NOT inferred from the"
  key_links:
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "the verbatim git diff over the whole v1.31 range plus the value histogram"
      pattern: "4d18b645"
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md"
      via: "the 0x08 prior-state numbers cited rather than re-derived"
      pattern: "60 ?/ ?64"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under /workspaces/firestarter or /workspaces/firestarter_app — D-16; both sub-repos are read-only measurement targets here"
    - "MUST NOT change any chip's support_status, and MUST NOT run tools/build_db.py or regenerate chip_database.json — the requirement is that nothing changed, and regenerating it would be the change"
    - "MUST NOT touch any serial port or run any firestarter CLI command against hardware — this plan is Gate 0, zero hardware"
    - "MUST NOT infer the 0x08 or 0x0B disposition from the 0x07 result, and MUST NOT wait for the 0x07 result before writing either record — BENCH-02's wording forbids the inference and D-13 requires these records to exist before the first spend"
    - "MUST NOT re-derive the 0x08 or 0x0B numbers by re-running anything — they are Phase 99's and Phase 79's, cited with their source"
    - "MUST NOT record either skip as rubber-stamped, and MUST NOT describe either as inconclusive or partial — D-14 admits only validated or skipped-with-reason"
    - "MUST NOT claim datasheet conformance anywhere, and MUST NOT make any comparative claim that v1.31 programs better than what preceded it (D-08)"
    - "MUST NOT run pytest while git -C /workspaces/firestarter status --porcelain is non-empty, and MUST NOT omit -o addopts="
---

<objective>
Complete both hardware-free requirements before the bench opens: BENCH-03's machine-checked proof that no
chip's `support_status` changed anywhere in v1.31, and BENCH-02's two full skip-with-reason disposition
records for `0x08` (AM27C020) and `0x0B` (M2716/M2732).

Purpose: D-13 halts this phase on the first genuine `0x07` failure. BENCH-03 is already provably true and
BENCH-02 is pure record-writing, so finishing both here means a halt costs neither. Per D-02 a skip is a
disposition record with numbers and a source, not a line — Phase 146's ledger will cite these rather than
re-derive them.

Output: three completed Gate 0 subsections in `145-BENCH-LOG.md`, and a Gate 0 verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-bench-validation/145-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
This plan produces no new file. It fills three subsections of `145-BENCH-LOG.md`, which `145-01` created, and
writes `145-02-SUMMARY.md`. The phase's full artifact inventory is listed in `145-01-PLAN.md`'s
`artifacts_this_phase_produces` section; every path there is new in this phase and must be excluded from any
drift check. No file in `firestarter/` or `firestarter_app/` is produced or modified (D-16).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure and record BENCH-03 — no support_status changed across the whole v1.31 range</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-6 in full — the four independent legs with their exact commands and expected results, the value histogram, the `chip_database.json` digest, and the honest caveat naming the three benign textual `support_status` mentions in the range.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` D-15 — why the diff is taken across the whole milestone range rather than this phase's commits, and why the proof composes with the existing AST lock rather than duplicating it.
    - `firestarter_app/tools/check_no_community_support_status_write.py` — read its header only, to state accurately in the record what it denies, which two files it scans, and that it fails closed if a scan target is missing.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the BENCH-03 subsection stubbed by `145-01`.
  </read_first>
  <action>
Run all four BENCH-03 legs from `/workspaces/firestarter_app` and capture each command's output verbatim.
Do not regenerate the database and do not run `tools/build_db.py` — the requirement is that nothing changed,
and regenerating would be the change.

Leg 1, D-15's mandated whole-milestone diff. Confirm the branch base rather than asserting it:
`git merge-base HEAD origin/beta` must print `4d18b645ab18a2d2465f0f623062e9249eb24132`. Then
`git diff 4d18b645..HEAD -- firestarter/data/chip_database.json` must produce zero bytes of output, and
`git diff --stat` over the same path must produce no rows. Record the commands and the empty result verbatim,
and record the confirmed merge-base so a reader can see the base was measured, not assumed.

Leg 2, the generator inputs. `git diff --stat 4d18b645..HEAD -- tools/build_db.py tools/extra_chips.json
tools/infoic.xml` must be empty. Record it, and record why it matters: `chip_database.json` is generated, so
an unchanged database with drifted generator inputs would be a latent change waiting for the next
regeneration. This leg closes that.

Leg 3, the mechanism lock. Run `python3 tools/check_no_community_support_status_write.py` and record its exit
status. Describe it accurately from its own header: an AST gate that denies any `support_status` assignment
target in `firestarter/diagnostic_report.py` and `tools/parse_devtest_issue.py`, fail-closed if either scan
target is missing, with `tools/build_db.py` as the sole sanctioned write locus. State that D-15's proof
composes with this lock rather than duplicating it, and that the lock runs automatically under `pytest tests/`
via `tests/test_check_no_community_support_status_write.py`.

Leg 4, the value histogram as a positive statement rather than an absence. Count `support_status` values
across every chip in `firestarter/data/chip_database.json` and record the result verbatim alongside the file's
SHA-256. Expected: 746 chips total, 736 `supported`, 9 `adapter-required`, 1 `protocol-not-implemented`, and
digest `3befbaad7bbb88307abd94db0447ad78e847c40f3c96be7751f5b87a1e913479`. If any figure differs from these,
that is a real finding — record the measured figures, do NOT overwrite them with the expected ones, and
stop-and-report rather than proceeding.

Then write the record's BENCH-03 subsection carrying all four legs verbatim, plus two honest additions. First,
the caveat: `git diff 4d18b645..HEAD` contains three textual mentions of `support_status` that are not value
changes, and the record must name them so a reader grepping the range is not alarmed — two in
`tests/golden/chip_database_field_inventory.json` (a per-key occurrence count of 746 and the key appearing in
a key list) and one in `tests/test_write_response_budget.py` (a docstring). Verify each of the three by
grepping the diff rather than restating RESEARCH, and record what you actually found. Second, the scope
statement: the firmware repo carries no `support_status` at all, so BENCH-03 is single-repo by construction —
verify that with a grep over `firestarter/src/`, `firestarter/include/` and `firestarter/scripts/` and record
the zero-hit result.

Close the subsection with a BENCH-03 verdict line in D-14's vocabulary: `validated`, with the four legs named.

Commit the record edit in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git merge-base HEAD origin/beta</automated>
    <automated>cd /workspaces/firestarter_app && git diff 4d18b645..HEAD -- firestarter/data/chip_database.json | wc -c</automated>
    <automated>cd /workspaces/firestarter_app && git diff --stat 4d18b645..HEAD -- tools/build_db.py tools/extra_chips.json tools/infoic.xml | wc -c</automated>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_no_community_support_status_write.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app && sha256sum firestarter/data/chip_database.json</automated>
    <automated>cd /workspaces/firestarter_app && python3 -c "import json,collections; d=json.load(open('firestarter/data/chip_database.json')); c=collections.Counter(ic.get('support_status') for ics in d.values() for ic in ics); print('total', sum(c.values())); [print(k, v) for k,v in c.most_common()]"</automated>
    <automated>cd /workspaces/firestarter && grep -rc "support_status" src/ include/ scripts/ 2>/dev/null | grep -v ":0$" | wc -l</automated>
    <automated>cd /workspaces && grep -q "4d18b645" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "3befbaad7bbb88307abd94db0447ad78e847c40f3c96be7751f5b87a1e913479" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "check_no_community_support_status_write.py" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "BENCH-03 legs recorded"</automated>
    <automated>cd /workspaces/firestarter_app && git status --porcelain firestarter/data/chip_database.json tools/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git merge-base HEAD origin/beta` in `firestarter_app` prints `4d18b645ab18a2d2465f0f623062e9249eb24132`, and that confirmed base appears in the record.
    - `git diff 4d18b645..HEAD -- firestarter/data/chip_database.json | wc -c` returns 0, and the record shows the command and its empty result verbatim.
    - `git diff --stat 4d18b645..HEAD -- tools/build_db.py tools/extra_chips.json tools/infoic.xml | wc -c` returns 0, recorded with the reason it matters.
    - `python3 tools/check_no_community_support_status_write.py` exits 0, and the record names both scan targets, the fail-closed behaviour, and `tools/build_db.py` as the sole sanctioned write locus.
    - The histogram prints `total 746` with `supported 736`, `adapter-required 9`, `protocol-not-implemented 1`, and the record carries those four figures plus the digest `3befbaad7bbb88307abd94db0447ad78e847c40f3c96be7751f5b87a1e913479`.
    - The record names all three benign textual `support_status` mentions in the range by file, each verified by grepping the diff rather than restated from RESEARCH.
    - The record states the firmware repo carries zero `support_status` hits across `src/`, `include/` and `scripts/`, with the grep result shown.
    - The BENCH-03 verdict line uses the word `validated` and no other outcome word.
    - `git -C /workspaces/firestarter_app status --porcelain firestarter/data/chip_database.json tools/` returns 0 lines — nothing in the sub-repo was modified.
  </acceptance_criteria>
  <done>BENCH-03 is re-measured at the tip across the whole v1.31 range on four independent legs, recorded verbatim with its caveat and its single-repo scope, and verdicted `validated`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 0x08 AM27C020 skipped-with-reason disposition record</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md` — the source of every `0x08` number this record cites: write #1's command line and `bad bytes: 4` with the 60 of 64 read-back, write #2's command line and `bad bytes: 64` with 0 of 64, the `dev consistency-check` PASS at N=3 with one distinct SHA, the idle VPP 12.9 to 13.0 V readings, the NOT-MEASURED program-window DMM entry with its blocking reason, and the DEFER verdict.
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-8's `0x08` block — the same numbers with their firmware commit `35706c2`, board and date, plus the sentence that under D-14 this shape is a fail rather than a qualified pass.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` D-02 and D-14 — what a disposition record must name, and the taxonomy it is judged under.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the `0x08` disposition subsection stubbed by `145-01`.
  </read_first>
  <action>
Fill the `0x08` AM27C020 disposition subsection of `145-BENCH-LOG.md`. This is a full record, not a line, and
every number is cited from Phase 99 rather than re-derived — no measurement is taken and no hardware is
touched.

The subsection must name, at minimum: that the part is AM27C020, protocol `0x08`, and that no AM27C020 is on
the bench this session, stated as the reason for the skip; the last known bench state with its numbers and its
source — Phase 99, 2026-07-01, Leonardo plus RURP Rev 2.0, firmware commit `35706c2` — specifically write #1
returning RC 1 with `bad bytes: 4` and a read-back of 60 of 64 bytes byte-exact where the failing bytes were
the first four at `0x1da00` through `0x1da03` which stayed `0xFF`, and write #2 returning RC 1 with
`bad bytes: 64` and a read-back of 0 of 64 where the entire region stayed `0xFF`; the read-stability result
between the two writes, `dev consistency-check AM27C020 --runs 3` giving PASS at N=3 with one distinct SHA, so
the partial state is real and stable rather than a read glitch; the idle VPP figures 12.9 to 13.0 V before and
after with Internal VCC 5.5 V, inside the 12.75 plus or minus 0.25 band; the explicitly NOT MEASURED entry —
program-window VPP at socket pin 1, blocked because the held-rail DMM proxy is defeated by DTR-reset-on-close
— with program-window droop under load named as the leading hypothesis that was never instrumented; and the
carry-forward id `FUT-08`.

Two sentences are mandatory and must appear as written here in substance. First, D-14's judgement: under this
phase's two-state taxonomy the Phase-99 shape (60 of 64 then 0 of 64) is a **fail**, not a qualified pass —
say so plainly, and note that the taxonomy was fixed before any run in this phase precisely so a partial
cannot be argued into the friendlier bucket. Second, D-02's denial: this disposition is NOT inferred from the
`0x07` result; no `0x08` measurement was taken this phase, and the numbers above are Phase 99's, cited, not
re-derived.

Close with a disposition verdict line using D-14's vocabulary: `skipped-with-reason`, naming the missing part.

Commit the record edit in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces && grep -q "AM27C020" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "FUT-08" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "part and carry-forward named"</automated>
    <automated>cd /workspaces && grep -qE "60 ?/ ?64|60 of 64" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -qE "0 ?/ ?64|0 of 64" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "both prior-state figures present"</automated>
    <automated>cd /workspaces && grep -q "35706c2" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "0x1da00" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "source commit and failing offset present"</automated>
    <automated>cd /workspaces && grep -qi "not inferred from the" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "D-02 denial sentence present"</automated>
    <automated>cd /workspaces && grep -qi "not measured" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -qi "DTR" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "not-measured entry with blocker present"</automated>
    <automated>cd /workspaces && grep -c "skipped-with-reason" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The `0x08` subsection names `AM27C020`, protocol `0x08`, and states that no AM27C020 is on the bench this session as the reason for the skip.
    - The subsection contains both `60` of `64` and `0` of `64` as the two write outcomes, plus `bad bytes: 4` and `bad bytes: 64`.
    - The subsection contains the firmware commit `35706c2`, the board (Leonardo, Rev 2.0), the date `2026-07-01`, and the failing offset `0x1da00`.
    - The subsection records `dev consistency-check` PASS at N=3 with 1 distinct SHA, and the idle VPP figures `12.9` and `13.0`.
    - The subsection contains an explicit `not measured` entry for program-window VPP at socket pin 1 naming the DTR-reset-on-close blocker and the un-instrumented droop hypothesis.
    - The subsection contains the carry-forward id `FUT-08`.
    - The subsection states plainly that under D-14's taxonomy this shape is a fail, not a qualified pass.
    - The subsection contains a sentence stating the disposition is NOT inferred from the `0x07` result and that the numbers are Phase 99's, cited, not re-derived.
    - The verdict line reads `skipped-with-reason` and names the missing part; the words `inconclusive` and `partial` do not appear as this record's outcome.
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0 and no sub-repo file changed.
  </acceptance_criteria>
  <done>The `0x08` disposition is a complete skipped-with-reason record carrying Phase 99's numbers with their source, the D-14 fail judgement, and the explicit not-inferred denial.</done>
</task>

<task type="auto">
  <name>Task 3: Write the 0x0B M2716/M2732 skipped-with-reason disposition record and close Gate 0</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/79-25v-nmos-ceiling-raise/79-01-SUMMARY.md` and `79-02-SUMMARY.md` — the source of every `0x0B` number: the rail-corrected VPE 22.4 V operator DMM against 23.9 V firmware reading at max pot, the VPP figures on the same run, R1/R2 at 270000 and 44000, the strict 25 V bar NOT CLEARED, the operator override retiring that bar, and the four NMOS chips graduating best-effort `supported`.
    - `.planning/phases/79-25v-nmos-ceiling-raise/79-03-PLAN.md` — the deferred definitive proof (a real write plus an independent read-back SHA), which is where the graduation is parked until a part is on hand.
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-8's `0x0B` block — the same numbers consolidated, including the caveat that the firmware ADC measures the regulator rail rather than the socket-delivered pin voltage.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the `0x0B` disposition subsection and the Gate 0 verdict line stubbed by `145-01`.
  </read_first>
  <action>
Fill the `0x0B` M2716/M2732 disposition subsection of `145-BENCH-LOG.md`. As with `0x08`, this is a full
record citing Phase 79 rather than re-deriving anything; no measurement is taken and no hardware is touched.

The subsection must name, at minimum: that the parts are M2716 and M2732, protocol `0x0B`, and that neither is
on the bench this session, stated as the reason for the skip; the last known bench state with its numbers and
its source — Phase 79, rail-corrected 2026-06-23, Leonardo plus RURP Rev 2.0, firmware `3.0.0b8`, chip OUT,
pot at maximum, R1/R2 at 270000 and 44000 — specifically VPE at 22.4 V by operator DMM, treated as
authoritative, against 23.9 V reported by `firestarter vpe`, roughly 90 % of the rated 25 V, and VPP on the
same run at roughly 15 to 19 V by DMM against 18.7 V reported by the firmware on the dropped path; that the
strict 25 V bar was NOT CLEARED and was then retired by operator override, after which the four NMOS chips
graduate `supported` best-effort with the firmware warning under-voltage at 22.4 V against the 23.75 V
threshold that is 95 % of 25 V, while over-voltage stays blocked; the caveat that the firmware ADC measures
the regulator rail at 23.9 V rather than the socket-delivered pin voltage at 22.4 V, so the two figures are
measuring different things and neither supersedes the other; and that the definitive proof — a real write plus
an independent read-back SHA — is Phase 79's plan `79-03`, deferred until a physical chip is on hand, which is
exactly where the graduation is parked.

The D-02 denial sentence is mandatory here too: this disposition is NOT inferred from the `0x07` result; no
`0x0B` measurement was taken this phase, and the numbers above are Phase 79's, cited, not re-derived.

Close the subsection with a disposition verdict line reading `skipped-with-reason` and naming the missing
parts.

Then close Gate 0. Fill the `Gate 0 verdict:` line with a one-sentence verdict naming each cleared item: the
instrument inventory and tripwile baseline from `145-01 Task 3`, the four images from `145-01 Task 2`,
BENCH-03 validated on four legs from Task 1 of this plan, and both BENCH-02 dispositions recorded
skipped-with-reason from Tasks 2 and 3. State explicitly in the verdict that Gate 0 completed with zero
hardware touched, so a D-13 halt at any later gate still leaves BENCH-02 and BENCH-03 complete.

Commit the record edit in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces && grep -q "M2716" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "M2732" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "both parts named"</automated>
    <automated>cd /workspaces && grep -q "22.4" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "23.9" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "both VPE figures present"</automated>
    <automated>cd /workspaces && grep -q "79-03" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -qi "parked" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "parked graduation recorded with its owner"</automated>
    <automated>cd /workspaces && grep -c -i "not inferred from the" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -c "skipped-with-reason" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -A3 "Gate 0 verdict" .planning/phases/145-bench-validation/145-BENCH-LOG.md | grep -qv "NOT YET RUN" && echo "Gate 0 verdict filled"</automated>
    <automated>cd /workspaces && grep -q "BENCH-02" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "BENCH-03" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "both requirement ids cited in the record"</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The `0x0B` subsection names `M2716` and `M2732`, protocol `0x0B`, and states that neither is on the bench this session as the reason for the skip.
    - The subsection contains `22.4` as the operator DMM VPE figure and `23.9` as the firmware figure, both at max pot, with the rail-versus-socket caveat stated.
    - The subsection contains `270000` and `44000` for R1/R2 and the source attribution to Phase 79.
    - The subsection states the strict 25 V bar was NOT CLEARED and then retired by operator override, and that the four NMOS chips graduate best-effort `supported`.
    - The subsection names `79-03` as the deferred definitive proof and states the graduation is parked until a part is on hand.
    - `grep -c -i "not inferred from the"` returns at least 2 — one denial in each of the two disposition records.
    - `grep -c "skipped-with-reason"` returns at least 2.
    - The `Gate 0 verdict:` line no longer reads `NOT YET RUN` and names all four cleared items plus the zero-hardware statement.
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0 and no sub-repo file changed.
  </acceptance_criteria>
  <done>The `0x0B` disposition is a complete skipped-with-reason record carrying Phase 79's numbers with their source and its parked graduation, and Gate 0 is closed with both hardware-free requirements complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prior phase record → this record | Numbers cited from Phase 99 and Phase 79 are trusted downstream; a mis-transcribed figure propagates into Phase 146's ledger |
| `0x07` result → `0x08`/`0x0B` disposition | BENCH-02 explicitly forbids inference across this boundary |
| generated database → the invariance claim | Regenerating `chip_database.json` would itself be the change BENCH-03 denies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-07 | Repudiation | A skip recorded as a line rather than a disposition, losing the evidence Phase 146 will cite | mitigate | D-02's required contents enumerated per record with literal-string acceptance criteria on `60`/`64`, `0`/`64`, `FUT-08`, `22.4`, `23.9`, `79-03` |
| T-145-08 | Spoofing | A `0x08` or `0x0B` disposition silently inferred from the `0x07` result | mitigate | Explicit denial sentence required in both records, grep-asserted at a count of at least 2, and both records written before the first spend |
| T-145-09 | Tampering | `chip_database.json` regenerated or edited while proving it did not change | mitigate | Prohibition against running `build_db.py`, plus a porcelain acceptance criterion over the database and `tools/` |
| T-145-10 | Repudiation | A benign textual `support_status` mention in the diff range read as a value change | mitigate | All three mentions named by file, each verified by grepping the actual diff rather than restating RESEARCH |
| T-145-11 | Repudiation | Phase 99's 60/64 then 0/64 shape softened into a qualified pass | mitigate | D-14's fail judgement is a mandatory recorded sentence; `inconclusive` and `partial` are prohibited as outcome words |
| T-145-12 | Tampering | A sub-repo file edited during a read-only measurement | mitigate | D-16 prohibition plus a firmware-porcelain acceptance criterion on every task |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and touches no manifest (D-16) |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && git diff 4d18b645..HEAD -- firestarter/data/chip_database.json | wc -c` — 0.
- `cd /workspaces/firestarter_app && python3 tools/check_no_community_support_status_write.py; echo $?` — 0.
- `cd /workspaces && grep -c -i "not inferred from the" .planning/phases/145-bench-validation/145-BENCH-LOG.md` — at least 2.
- `cd /workspaces && grep -c "skipped-with-reason" .planning/phases/145-bench-validation/145-BENCH-LOG.md` — at least 2.
- `cd /workspaces/firestarter && git status --porcelain` and `cd /workspaces/firestarter_app && git status --porcelain` — no tracked-file modification.
- The `Gate 0 verdict:` line is filled and no longer reads `NOT YET RUN`.
</verification>

<success_criteria>
- BENCH-03 is re-measured at the tip across the whole v1.31 range on four independent legs and recorded verbatim, with its caveat and its single-repo scope stated, verdict `validated`.
- Both BENCH-02 dispositions are full records naming the missing part, its prior bench state with numbers and source, and an explicit not-inferred sentence, verdict `skipped-with-reason`.
- The `0x08` record judges Phase 99's shape a fail under D-14 rather than a qualified pass.
- Gate 0 is closed with zero hardware touched, so a later D-13 halt cannot cost BENCH-02 or BENCH-03.
- Nothing in either sub-repo changed and no chip's `support_status` moved.
</success_criteria>

<output>
Create `.planning/phases/145-bench-validation/145-02-SUMMARY.md` when done. It MUST record: the confirmed
merge-base, the four BENCH-03 leg results verbatim including the histogram and the digest, the three benign
textual mentions as actually found in the diff, and a one-line index of where each of the three Gate 0
subsections lives in `145-BENCH-LOG.md`.
</output>
