---
phase: 145-bench-validation
plan: 09
type: execute
wave: 9
depends_on: ["145-08"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/145-bench-validation/145-09-SUMMARY.md
autonomous: false
requirements: [BENCH-01, BENCH-02, BENCH-03]

must_haves:
  truths:
    - "All three BENCH requirements are flipped exactly once, in both coverage documents, only after every piece of evidence exists and the operator has approved at a gate that was not auto-approved"
    - "The flip is a hand edit preceded by a snapshot and followed by a line-by-line diff — bulk requirements and roadmap rewrite verbs normalise the WHOLE file and have previously clobbered an unrelated phase's plan line"
    - "BENCH-01, BENCH-02 and BENCH-03 are also used by archived milestones in ROADMAP.md, so the edit is scoped to the v1.31 coverage table and the Phase 145 block only — no archived row moves"
    - "Per D-14 BENCH-01 is ticked only if the record's phase verdict says validated; a fail halts under D-13 and nothing is ticked"
    - "BENCH-02 is ticked on the strength of two skipped-with-reason disposition records, which is the requirement-sanctioned path, not a workaround"
    - "Per D-16 no file under firestarter/ or firestarter_app/ is created, edited or deleted"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BENCH-01, BENCH-02 and BENCH-03 ticked in the Bench Validation section"
      contains: "BENCH-03"
    - path: ".planning/ROADMAP.md"
      provides: "The v1.31 coverage table's three BENCH rows moved to Complete, the Phase 145 milestone checklist item ticked, and the Phase 145 plan checkboxes ticked"
      contains: "BENCH-01 | Phase 145 | Complete"
  key_links:
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the phase verdict answering each ROADMAP success criterion, which is what authorises each individual tick"
      pattern: "BENCH-0[1-3]"
  prohibitions:
    - "MUST NOT use any bulk requirements or roadmap rewrite verb — they normalise the whole file and have previously clobbered an unrelated phase's Plans line; hand-edit, then snapshot-and-diff"
    - "MUST NOT flip any requirement other than BENCH-01, BENCH-02 and BENCH-03 — do not touch a TEST-*, CLOSE-*, PREP-*, ISSUE-* or TABLE-* row"
    - "MUST NOT touch the archived BENCH-01/02/03 rows belonging to v1.2 and v1.3 Phase 12 and Phase 13 in ROADMAP.md — those are different requirements with the same ids and must remain byte-identical"
    - "MUST NOT change any requirement's text — only the checkbox character and the coverage-table Status cell move"
    - "MUST NOT tick anything if 145-BENCH-LOG.md's phase VERDICT is not validated for the corresponding criterion — a fail halts under D-13 and the ticking does not happen"
    - "MUST NOT auto-approve the operator checkpoint — this gate is blocking and is not auto-approvable even under auto-advance"
    - "MUST NOT edit either coverage document before the operator has answered"
    - "MUST NOT create, edit, rename or delete any file under /workspaces/firestarter or /workspaces/firestarter_app (D-16)"
    - "MUST NOT run any hardware command from this plan — the bench is closed"
---

<objective>
Flip BENCH-01, BENCH-02 and BENCH-03 in both coverage documents, behind a blocking operator gate, with a
snapshot-and-diff proving nothing else moved.

Purpose: requirement ticking is the phase's outward-facing claim, and it is centralised here so no executing
plan can tick early on partial evidence. The mechanical risk is equally real: the bulk roadmap and
requirements verbs reformat whole files, and `BENCH-01` through `BENCH-03` are ids that archived milestones
also use — so the edit is scoped by hand and audited line by line.

Output: three ticked checkboxes in `REQUIREMENTS.md`, three Complete rows plus the Phase 145 checklist item
and the plan checkboxes in `ROADMAP.md`, and a diff that contains nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/145-bench-validation/145-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan produces `145-09-SUMMARY.md` and modifies two existing meta-repo coverage documents. It creates no
binary and no new artifact. The phase's full artifact inventory is listed in `145-01-PLAN.md`; every path
there is new in this phase and must be excluded from any drift check. No file in `firestarter/` or
`firestarter_app/` is produced or modified (D-16).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator reviews the closed record and authorizes the requirement flip</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the complete record end to end; this is the artifact under review and the sole authority for each individual tick.
    - `.planning/REQUIREMENTS.md` §"Bench Validation" — BENCH-01, BENCH-02 and BENCH-03 in their exact wording, so the operator can check each against the evidence rather than against a summary.
    - `.planning/ROADMAP.md` §"Phase 145: Bench Validation" — the four success criteria the record's verdict answers.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` D-13 and D-14 — the halt route and the taxonomy, which decide whether ticking is permitted at all.
  </read_first>
  <action>
Read the record's phase `VERDICT:` section first. If BENCH-01's criterion is not `validated`, do NOT present a
flip request: record that the requirement is not ticked, state the reason, and stop-and-report per D-13.
Ticking on a fail is the exact overclaim this phase exists to avoid.

Otherwise present the summary below and STOP. Do not edit `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`
until the operator has answered. This gate is blocking and is NOT auto-approvable even under auto-advance:
flipping three requirements is an outward-facing claim the operator owns (D-20).

If the operator asks for changes to the record rather than approving, apply them to `145-BENCH-LOG.md` and
re-present. Do not proceed to Task 2 on a partial approval.
  </action>
  <what-built>
Phase 145 is complete pending your review. Across eight plans:

- **Gate 0, zero hardware.** The bench record skeleton with every gate stubbed and the two-state taxonomy fixed
  before any run; four address-attributable write images whose values decode back to a source address, with
  digests matching independently computed values; a frame-extraction instrument seen to pass on both a positive
  and a negative fixture; and a pre-bench tripwire baseline.
- **BENCH-03, four independent legs.** The whole-milestone `chip_database.json` diff from `4d18b645` empty, the
  generator-inputs diff empty, the AST write-locus checker exiting 0, and the 736 / 9 / 1 histogram over 746
  chips — plus an end-of-phase re-confirmation at the tip.
- **BENCH-02, two full disposition records.** `0x08` (AM27C020) with Phase 99's 60/64 then 0/64 and FUT-08;
  `0x0B` (M2716/M2732) with Phase 79's 22.4 V DMM against 23.9 V firmware at max pot and its parked
  graduation. Each names the missing part and each carries the explicit sentence that it is not inferred from
  the `0x07` result.
- **Gate 1.** The Rev 2.0 shield by your eyes-on reading, the Winbond W27C512 confirmed by chip-id `0xda08`,
  the v1.31 firmware flashed from a clean checkout and identified by commit plus the verified byte count, VPP
  in band by a single reading, `--force used? No`, and the erase capability settled on silicon.
- **Gate 2.** Three full 64 KiB write cycles on three distinct images, each byte-exact on both the firmware's
  own verdict and the independent host-side SHA compare, with per-cycle read stability PASS at N=3 and one
  distinct SHA.
- **Gate 3.** The `--pulse-us 4688` run above the 4687 µs threshold, with its provenance line recorded
  verbatim, plus your eyes-on statement of what the terminal actually did.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/145-bench-validation/145-BENCH-LOG.md` end to end. It is the whole output of this
   phase.
2. Check the four boundaries the record is obliged to state, and confirm each is present rather than implied:
   - **No comparative claim.** Nothing says v1.31 programs better than what preceded it. No control run was
     made, deliberately (D-08).
   - **No datasheet conformance.** The 6.25 V program-VCC ceiling is unreachable on this shield; that debt is
     the milestone's, not this phase's.
   - **Scope named.** One part (`W27C512`, `0xda08`), one controller (`leonardo`), one shield revision
     (Rev 2.0). Nothing extrapolates beyond that.
   - **Nothing dropped.** Every undischarged item is in the carry-forward section with `no v1.31 owner`
     stated, because Phase 146 is docs-and-claims only and cannot run a bench.
3. Check the two skips read as dispositions rather than as rubber stamps — each names the missing part, its
   prior numbers with a source, and the not-inferred sentence.
4. Spot-check the evidence yourself if you want to. From
   `/workspaces/.planning/phases/145-bench-validation/`:
   `sha256sum -c SHA256SUMS.txt` verifies every image, read-back and consistency-check run against its
   recorded digest.
5. On approval, exactly three checkboxes move: **BENCH-01**, **BENCH-02** and **BENCH-03** in
   `.planning/REQUIREMENTS.md`, plus their three rows in `.planning/ROADMAP.md`'s v1.31 coverage table, the
   Phase 145 milestone checklist item, and the Phase 145 plan checkboxes. No `TEST-*` or `CLOSE-*` row is
   touched, and the archived `BENCH-01/02/03` rows belonging to v1.2 and v1.3 stay byte-identical — they are
   different requirements that happen to share ids.
  </how-to-verify>
  <resume-signal>Type "approved" to flip BENCH-01, BENCH-02 and BENCH-03 — or describe what to change first.</resume-signal>
  <verify>
    <human-check>The operator has read the record and answered the resume signal. Neither coverage document was edited before that answer.</human-check>
    <automated>cd /workspaces && git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l</automated>
    <automated>cd /workspaces && grep -A4 "VERDICT" .planning/phases/145-bench-validation/145-BENCH-LOG.md | grep -qi "validated" && echo "phase verdict permits ticking"</automated>
  </verify>
  <acceptance_criteria>
    - The record's phase `VERDICT:` section was read before the gate was presented, and BENCH-01's criterion reads `validated`; if it did not, nothing was ticked and the task stopped and reported.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l` returns 0 at the moment the gate is presented — neither coverage document was pre-edited.
    - The operator's response is captured verbatim in `145-09-SUMMARY.md`.
    - If the operator asked for changes, the record was amended and re-presented, and Task 2 did not start until an explicit approval arrived.
  </acceptance_criteria>
  <done>The operator has reviewed the closed record and either approved the flip or named the changes required first.</done>
</task>

<task type="auto">
  <name>Task 2: Flip BENCH-01 through BENCH-03 in both coverage documents with a snapshot-and-diff</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` §"Bench Validation" — the three unchecked boxes, with the checked `TEST-*` block above and the unchecked `CLOSE-*` block below, so the edit's boundaries are unambiguous.
    - `.planning/ROADMAP.md` — four distinct edit sites: the v1.31 milestone phase checklist item for Phase 145; the "Phase 145: Bench Validation" block with its plan checkboxes; the v1.31 Coverage table's three `| BENCH-0N | Phase 145 | Pending |` rows; and, for contrast, the archived `| BENCH-0N | Phase 12 |` and `| Phase 13 |` rows that must NOT move.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the phase verdict's per-criterion answers, which are what authorise each individual tick.
  </read_first>
  <action>
Only after the operator has approved.

Snapshot first: copy `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to `/tmp/gsd-145/` so the post-edit
diff can be inspected line by line. Then hand-edit. Do NOT use a bulk requirements or roadmap rewrite verb —
they normalise the whole file, and one has previously clobbered an unrelated phase's `**Plans:**` line.

In `.planning/REQUIREMENTS.md`, change exactly three checkbox characters from unchecked to checked in the
`### Bench Validation` section: `BENCH-01`, `BENCH-02` and `BENCH-03`. Leave every `TEST-*` and `CLOSE-*` row
untouched, and leave each requirement's text byte-identical — only the checkbox character changes.

In `.planning/ROADMAP.md`, make exactly these edits and no others. Set the three v1.31 Coverage table rows
`| BENCH-01 | Phase 145 | Pending |`, `| BENCH-02 | Phase 145 | Pending |` and `| BENCH-03 | Phase 145 |
Pending |` to `Complete`. Tick the v1.31 milestone phase checklist item for Phase 145. Tick the Phase 145 plan
checkboxes for `145-01` through `145-09`. Do not touch any other phase's block, any other requirement row, or
Phase 145's goal and success-criteria text. Critically, do NOT touch the archived rows
`| BENCH-01 | Phase 12 |`, `| BENCH-02 | Phase 12 |`, `| BENCH-03 | Phase 13 |` or any other archived
milestone content — those are different requirements that happen to share ids, and a global substitution on
`BENCH-0` would corrupt them.

Diff both files against their snapshots and confirm the diff contains ONLY the intended lines. In
`REQUIREMENTS.md` expect exactly six changed diff lines — three removed and three added. In `ROADMAP.md`,
every changed line must mention either `BENCH-0` together with `Phase 145`, or `Phase 145: Bench Validation`,
or a `145-0N` plan id. Any other changed line — a reflowed paragraph, a moved heading, another phase's plan
line, an archived coverage row — must be reverted before committing.

Commit both coverage edits in the meta repo with a message naming the three flipped requirement ids and
`145-BENCH-LOG.md` as the evidence.
  </action>
  <verify>
    <automated>cd /workspaces && grep -cE "^- \[x\] \*\*BENCH-0[1-3]\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>cd /workspaces && grep -cE "^- \[ \] \*\*BENCH-0[1-3]\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>cd /workspaces && diff /tmp/gsd-145/REQUIREMENTS.md .planning/REQUIREMENTS.md | grep -c "^[<>]"</automated>
    <automated>cd /workspaces && diff /tmp/gsd-145/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -vcE "BENCH-0[1-3].*Phase 145|Phase 145: Bench Validation|145-0[1-9]"</automated>
    <automated>cd /workspaces && grep -cE "^\| BENCH-0[1-3] \| Phase 145 \| Complete \|" .planning/ROADMAP.md</automated>
    <automated>cd /workspaces && grep -cE "^\| BENCH-0[1-6] \| Phase 1[23] \|" .planning/ROADMAP.md</automated>
    <automated>cd /workspaces && diff /tmp/gsd-145/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -ciE "Phase 1[23]|TEST-0|CLOSE-0|PREP-0|ISSUE-0|TABLE-0"</automated>
    <automated>cd /workspaces && grep -cE "^- \[ \] \*\*CLOSE-0[1-5]\*\*" .planning/REQUIREMENTS.md</automated>
    <automated>cd /workspaces && git log --oneline -1 && git status --porcelain .planning/ | head</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*BENCH-0[1-3]\*\*" .planning/REQUIREMENTS.md` returns 3 and `grep -cE "^- \[ \] \*\*BENCH-0[1-3]\*\*"` returns 0.
    - The `REQUIREMENTS.md` diff against the snapshot contains exactly 6 changed lines (3 removed, 3 added) and nothing else; every requirement's text is byte-identical.
    - Every changed line in the `ROADMAP.md` diff mentions `BENCH-0N` together with `Phase 145`, or `Phase 145: Bench Validation`, or a `145-0N` plan id — the filtered count of other changed lines is 0.
    - No changed line in the `ROADMAP.md` diff mentions `Phase 12`, `Phase 13`, `TEST-0`, `CLOSE-0`, `PREP-0`, `ISSUE-0` or `TABLE-0` — the archived rows and every other milestone's coverage are byte-identical.
    - `grep -cE "^\| BENCH-0[1-3] \| Phase 145 \| Complete \|" .planning/ROADMAP.md` returns 3.
    - The archived `| BENCH-0N | Phase 12 |` and `| Phase 13 |` row count is unchanged from the snapshot.
    - All five `CLOSE-*` rows remain unchecked and no `TEST-*` row changed.
    - The commit contains only the two coverage documents and the plan summary; `git status --porcelain` in both sub-repos shows no tracked-file modification.
    - The commit message names all three flipped requirement ids and `145-BENCH-LOG.md`.
  </acceptance_criteria>
  <done>All three BENCH requirements are ticked in both coverage documents, with a diff proving no archived row, no other milestone's coverage and no unrelated line moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence set → requirement claim | A ticked checkbox is the phase's outward-facing claim and is read by the milestone close |
| tool-assisted doc edit → whole file | Bulk roadmap and requirements verbs reformat everything they touch, including unrelated phases |
| id namespace → milestone | `BENCH-01` through `BENCH-03` are reused by archived milestones, so a global substitution corrupts history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-54 | Repudiation | A requirement ticked on partial or failed evidence | mitigate | The phase verdict is read before the gate is presented; a non-`validated` BENCH-01 criterion halts under D-13 with nothing ticked; all ticking is centralised in this plan and prohibited in every other |
| T-145-55 | Tampering | A bulk doc verb reformatting the roadmap or clobbering another phase's plan line | mitigate | Snapshot-then-diff with an acceptance criterion that every changed line names `Phase 145` or a `145-0N` id; bulk verbs prohibited |
| T-145-56 | Tampering | A global `BENCH-0` substitution corrupting the archived v1.2 and v1.3 coverage rows | mitigate | The archived rows named explicitly as must-not-move, with a diff filter asserting no changed line mentions `Phase 12` or `Phase 13` and a row-count check |
| T-145-57 | Elevation of privilege | An automated agent approving an outward-facing claim on the operator's behalf | mitigate | The checkpoint is declared not auto-approvable, `autonomous: false`, and Task 2 asserts neither coverage document was pre-edited |
| T-145-58 | Repudiation | A `CLOSE-*` or `TEST-*` row moved as collateral | mitigate | Explicit prohibition plus acceptance criteria asserting all five `CLOSE-*` rows remain unchecked and no `TEST-*` line appears in the diff |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and writes documents only (D-16) |
</threat_model>

<verification>
- `cd /workspaces && grep -cE "^- \[x\] \*\*BENCH-0[1-3]\*\*" .planning/REQUIREMENTS.md` — 3.
- `cd /workspaces && diff /tmp/gsd-145/REQUIREMENTS.md .planning/REQUIREMENTS.md | grep -c "^[<>]"` — 6.
- `cd /workspaces && diff /tmp/gsd-145/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -vcE "BENCH-0[1-3].*Phase 145|Phase 145: Bench Validation|145-0[1-9]"` — 0.
- `cd /workspaces && diff /tmp/gsd-145/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -ciE "Phase 1[23]|TEST-0|CLOSE-0"` — 0.
- `cd /workspaces && grep -cE "^\| BENCH-0[1-3] \| Phase 145 \| Complete \|" .planning/ROADMAP.md` — 3.
- `cd /workspaces/firestarter && git status --porcelain` and `cd /workspaces/firestarter_app && git status --porcelain` — no tracked-file modification.
</verification>

<success_criteria>
- All three BENCH requirements are ticked in both coverage documents, only after the operator approved at a gate that was not auto-approved.
- The diff proves nothing else moved: six changed lines in `REQUIREMENTS.md`, and every changed `ROADMAP.md` line naming Phase 145 or a Phase 145 plan.
- No archived `BENCH-0N` row from v1.2 or v1.3 changed, and no `TEST-*` or `CLOSE-*` row moved.
- Nothing under either sub-repo changed.
</success_criteria>

<output>
Create `.planning/phases/145-bench-validation/145-09-SUMMARY.md` when done. It MUST record: the operator's
approval verbatim; both diffs' changed-line counts; the filtered other-lines count for `ROADMAP.md`; the
archived-row count before and after; and the commit sha with its message.
</output>
