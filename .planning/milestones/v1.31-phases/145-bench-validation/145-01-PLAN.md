---
phase: 145-bench-validation
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/145-bench-validation/145-BENCH-LOG.md
  - .planning/phases/145-bench-validation/images/gen_addr_image.py
  - .planning/phases/145-bench-validation/images/img1.bin
  - .planning/phases/145-bench-validation/images/img2.bin
  - .planning/phases/145-bench-validation/images/img3.bin
  - .planning/phases/145-bench-validation/images/img_4k_pulse.bin
  - .planning/phases/145-bench-validation/tools/extract_frames.py
  - .planning/phases/145-bench-validation/SHA256SUMS.txt
  - .planning/phases/145-bench-validation/145-01-SUMMARY.md
autonomous: false
requirements: [BENCH-01]

must_haves:
  truths:
    - "Per D-13 the phase can halt on the first genuine 0x07 failure, so the record skeleton, the four write images and the frame-extraction instrument all exist BEFORE any silicon is touched — a halt still lands a usable record"
    - "Per D-05 three distinct 64 KiB images exist whose byte values decode back to a source address, so a mismatch is attributable to an address rather than merely counted"
    - "Per D-16 every artifact this plan creates lives in the meta repo under .planning/phases/145-bench-validation/ — no file is created, edited or deleted inside firestarter/ or firestarter_app/"
    - "Per D-20 the record itself carries a line stating the dispatching command was issued without --auto and without --chain; autonomous:false alone is not self-protecting"
    - "Per D-10 the frame-extraction instrument has been SEEN to report INTRA-BLOCK on a synthetic fixture that contains one, and to report zero on a fixture that contains none — a pre-authored measurement leg proves nothing until both its outcomes are observed"
    - "Per D-14 the record's preamble fixes the two-state taxonomy before any run, so a partial result cannot later be argued into the friendlier bucket"
    - "Every row of 145-VALIDATION.md's per-task verification map is bound in the record to a concrete plan-and-task id"
    - "The RQ-9 tripwire baseline is recorded before the bench opens: firmware porcelain empty, firmware suite green, host sibling-porcelain subset green"
  artifacts:
    - path: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      provides: "The gated bench record in the 99-03-BENCH-LOG.md shape, with every gate section present and stubbed NOT YET RUN"
      min_lines: 120
      contains: "--force used?"
    - path: ".planning/phases/145-bench-validation/images/gen_addr_image.py"
      provides: "The word-stamped address image generator (bench tooling, meta repo only)"
      min_lines: 15
    - path: ".planning/phases/145-bench-validation/tools/extract_frames.py"
      provides: "Deterministic tqdm progress-frame extractor with last-bar-restart segment selection and a two-outcome self-test"
      min_lines: 40
    - path: ".planning/phases/145-bench-validation/SHA256SUMS.txt"
      provides: "One place for every image and read-back digest, so the narrative carries no inline hashes"
      contains: "img1.bin"
  key_links:
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/phases/145-bench-validation/145-VALIDATION.md"
      via: "the verification-map bindings table naming a concrete 145-NN Task M for every row"
      pattern: "145-0[1-9] Task [1-3]"
    - from: ".planning/phases/145-bench-validation/tools/extract_frames.py"
      to: ".planning/phases/145-bench-validation/logs/write_cycleN.stderr.raw"
      via: "raw tqdm stderr capture parsed after the last bar restart"
      pattern: "INTRA-BLOCK"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under /workspaces/firestarter or /workspaces/firestarter_app — D-16; one untracked file in the firmware checkout turns 9 tests RED (RQ-9)"
    - "MUST NOT add the image generator or the frame extractor to firestarter_app/tools/ — both are meta-repo bench tooling, not host source (D-16, RQ-2)"
    - "MUST NOT use firestarter_app/tools/gen_test_image.py as the image source — pseudo-random values are detectable but not decodable to a source address (D-05, RQ-2)"
    - "MUST NOT touch any serial port, run any firestarter CLI command against hardware, or assume a board is attached — this plan is Gate 0, zero hardware"
    - "MUST NOT name any evidence directory with a consistency-check- prefix, and MUST NOT use firestarter-runs/ or write-cycle-* — all three are gitignored in the meta repo and the evidence would silently never commit (Pitfall 5)"
    - "MUST NOT write any bench artifact into /workspaces/firestarter, including .bin files that happen to be gitignored today"
    - "MUST NOT run pytest while git -C /workspaces/firestarter status --porcelain is non-empty, and MUST NOT omit -o addopts= (the repos' addopts is -ra -q and doubling -q hides the count line)"
    - "MUST NOT pre-fill any Gate 1, Gate 2 or Gate 3 measurement with a predicted value — every such field is stubbed NOT YET RUN and filled only by the plan that measures it"
    - "MUST NOT record any comparative claim that v1.31 programs better than what preceded it — D-08 rejects a control run and the milestone claims fidelity, not improvement"
    - "MUST NOT use gawk strtonum in any extraction step — it is not portable here; parse hex in Python"
---

<objective>
Build every instrument this phase measures with, and the record it writes into, before a single byte of
silicon is spent. Three deliverables: the `145-BENCH-LOG.md` skeleton in the Phase-99 gate shape with all
gates stubbed and D-14's two-state taxonomy fixed up front; the four write images D-05 requires plus their
digests; and the frame-extraction instrument D-10's machine half depends on, seen to pass on both a positive
and a negative synthetic fixture.

Purpose: D-13 halts this phase on the first genuine `0x07` failure. If the record, the images and the
measuring instrument are still unwritten at that moment, a halt costs artifacts that never needed hardware at
all. This is Gate 0 — zero hardware, and it must complete first. Per D-16 every artifact is authored in the
meta repo; nothing is written into either sub-repo.

Output: `145-BENCH-LOG.md` skeleton, `images/gen_addr_image.py`, four `.bin` images, `SHA256SUMS.txt`,
`tools/extract_frames.py` with a passing two-outcome self-test, and a recorded pre-bench tripwire baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-bench-validation/145-CONTEXT.md
@.planning/phases/145-bench-validation/145-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Every path below is NEW and created by this phase. None exists at plan time; a source-grounding drift check
must exclude all of them.

- `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the phase record (this plan creates the skeleton;
  later plans fill the gates)
- `.planning/phases/145-bench-validation/images/gen_addr_image.py` — bench tooling, meta repo only
- `.planning/phases/145-bench-validation/images/img1.bin`, `img2.bin`, `img3.bin` — 65536 B each
- `.planning/phases/145-bench-validation/images/img_4k_pulse.bin` — 4096 B
- `.planning/phases/145-bench-validation/tools/extract_frames.py` — bench tooling, meta repo only
- `.planning/phases/145-bench-validation/SHA256SUMS.txt` — the single digest manifest
- `.planning/phases/145-bench-validation/readbacks/.gitkeep`, `runs/.gitkeep`, `logs/.gitkeep` — directory
  scaffolding for the artifacts later plans write
- `.planning/phases/145-bench-validation/145-01-SUMMARY.md`
- `/tmp/gsd-145/` — scratch for raw build and upload logs; NOT committed

Later plans additionally produce: `readbacks/prewrite.bin`, `readbacks/readback1.bin` through `readback3.bin`,
`runs/cycle1` through `runs/cycle3` each holding `run_01.bin` through `run_03.bin`, `logs/*.log`,
`logs/*.stderr.raw`, and `145-02-SUMMARY.md` through `145-09-SUMMARY.md`. No file in `firestarter/` or
`firestarter_app/` is produced or modified by any plan in this phase (D-16).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 145-BENCH-LOG.md skeleton and the artifact directory tree</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md, .planning/phases/145-bench-validation/readbacks/.gitkeep, .planning/phases/145-bench-validation/runs/.gitkeep, .planning/phases/145-bench-validation/logs/.gitkeep</files>
  <read_first>
    - `.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md` — the house template, all 116 lines. Copy its conventions exactly: an identity TABLE with a Source column, an operator authorization line quoted verbatim per spend, the command line used as the subsection heading, SHAs in a table rather than inline, a `--force used?` row, and every not-measured reading given its own line with its blocking reason.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` — D-01 through D-20; this skeleton is the shape those decisions get recorded in.
    - `.planning/phases/145-bench-validation/145-VALIDATION.md` — the per-task verification map whose rows this task binds to concrete plan-and-task ids.
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-8 (the extracted 99-03 skeleton), §"Recommended artifact layout", §"Pattern 2: Command line as evidence heading", and §"Pattern 3: Two-outcome discipline".
  </read_first>
  <action>
Create the artifact directory tree first under `.planning/phases/145-bench-validation/`: `readbacks/`, `runs/`,
`logs/`, `images/` and `tools/`, each with a `.gitkeep` so the empty directories commit. Do NOT name any of
them with a `consistency-check-` prefix and do NOT use `firestarter-runs/` or `write-cycle-*` — all three are
gitignored in the meta repo and the evidence would silently never commit. Confirm with `git check-ignore` that
none of the five paths is ignored. Also create `/tmp/gsd-145/` for scratch.

Then create `.planning/phases/145-bench-validation/145-BENCH-LOG.md`. Section order is yours; the following
content is mandatory, each item its own identifiable section or subsection heading, with every Gate 1, Gate 2
and Gate 3 measurement field present but carrying the literal placeholder `NOT YET RUN`.

1. Title and a one-paragraph honesty preamble in the 99-03 register: nothing here is fabricated, and a
tooling-blocked reading is recorded as `not measured` with its reason. State D-14's taxonomy explicitly in the
preamble — two outcome states only, `validated` or `skipped-with-reason`; anything that is not a clean pass is
a fail; there is no third state and the word `inconclusive` appears in this document only inside that
negation. Say that this taxonomy was fixed before any run, precisely so a partial result cannot be argued into
the friendlier bucket afterwards.

2. A header block with `Session start`, `Operator`, `Driver` and a `Dispatch mode` line. The dispatch-mode
line is D-20's: it must state that this phase was dispatched WITHOUT `--auto` and WITHOUT `--chain`, because
auto-modes auto-approve `human-verify` gates and `autonomous: false` alone is not self-protecting. Leave the
session-start timestamp as `NOT YET RUN`.

3. A `Verification map bindings` table with one row per row of `145-VALIDATION.md`'s per-task verification
map, in the same order, each bound to a concrete plan-and-task id. Use exactly these bindings —
`145-VALIDATION.md`'s own Plan column is a pre-planning guess and this table supersedes it. BENCH-03's
chip-database diff, generator-inputs diff, write-locus lock and histogram all bind to `145-02 Task 1`.
BENCH-02's `0x08` skip binds to `145-02 Task 2` and its `0x0B` skip to `145-02 Task 3`. The four images bind
to `145-01 Task 2`. Reflash and commit identity, zero flash growth, and controller and port identity all bind
to `145-03 Task 2`. Chip-id `0xda08` binds to `145-03 Task 3`. VPP in band and `--force used? No` bind to
`145-04 Task 1` and `145-04 Task 2`. Pre-write preservation binds to `145-04 Task 1`. D-03 settled on the
bench binds to `145-04 Task 3`. The four per-cycle rows — write completes, firmware verify, independent SHA
compare, and `dev consistency-check` — bind to `145-05 Task 2` for cycle 1 and to `145-06 Task 1` and
`145-06 Task 2` for cycles 2 and 3. Erase-fired corroboration, 3/3 on both oracles, and the no-`--force`
source assertion bind to `145-06 Task 3`. D-11 long-write survival and D-10 Claim A bind to `145-05 Task 3`.
D-10 Claim B and D-12's `--pulse-us` above 4687 µs bind to `145-07 Task 2`. D-12's A1 measurement binds to
`145-07 Task 3`. D-10's eyes-on half binds to `145-08 Task 1`.

4. A `## Gate 0 — Off-bench evidence (no hardware)` section with five subsections: instrument inventory and
tripwire baseline; write images; BENCH-03 `support_status` invariance; BENCH-02 `0x08` AM27C020 disposition;
BENCH-02 `0x0B` M2716/M2732 disposition. Then a `Gate 0 verdict:` line stubbed `NOT YET RUN`.

5. A `## Gate 1 — Identity, image under test, VPP, D-03 pre-flight` section opening with an identity table
carrying exactly these rows, each stubbed `NOT YET RUN`, each with a Source column: Controller identity; Port;
Hardware revision (reported); Shield silkscreen (operator eyes-on); Seated chip (operator confirmed); Part
expendable (operator confirmed); R1 readback; R2 readback; Firmware version string; Firmware commit under
test; Firmware working tree clean; Flash bytes measured; avrdude verified byte count; VPP target; VPP
confirmation read; `--force` used?; Dispatch mode. Next to the version-string row carry D-18's caveat in
prose: `3.0.0b17` is byte-identical to the v1.31 branch's own fork point `3085084` and reads older than
`origin/beta`'s `3.0.0b18`, so the version string identifies nothing — the commit plus the avrdude byte count
are the discriminators. Add subsections for the reflash proof, VPP, pre-write chip preservation, and the D-03
erase-capability pre-flight, then a `Gate 1 verdict:` line stubbed.

6. A `## Gate 2 — Three 64 KiB cycles (authorized spend)` section with an `Operator authorization:` line
stubbed for a verbatim quote; three cycle subsections stubbed, each to be re-titled with its exact command
line as the heading per Pattern 2; a progress-frame evidence subsection for D-10 Claim A; a D-09 re-seat
ledger subsection stating that at most one re-seat is allowed, that it must be attributable to a named
physical cause, and that a discarded failure and its re-run are BOTH recorded; and a `Gate 2 verdict:` line
stubbed.

7. A `## Gate 3 — --pulse-us 4688 (D-10 Claim B, D-12)` section with its own `Operator authorization:` line
stubbed, a run subsection, a Claim B subsection, an A1 per-pulse-overhead subsection, an operator eyes-on
subsection, and a `Gate 3 verdict:` line stubbed. State in this section that Gate 3 is REQUIRED conditional on
Gate 2 passing, and that if Gate 2 fails it is recorded as not-reached with the reason rather than omitted.

8. A `SHA manifest` pointer section naming `SHA256SUMS.txt` as the single home for every digest, so no hash is
written inline in the narrative.

9. A `Not measured` section, empty at this point, for readings that turn out tooling-blocked.

10. A `Carry-forward hand-offs with no v1.31 owner` section, empty at this point, stating that Phase 146 is
docs-and-claims only and cannot run a bench, so anything this phase does not discharge has no v1.31 owner and
is recorded rather than silently dropped.

11. A `VERDICT:` line stubbed `NOT YET RUN` and a `Session end:` line stubbed.

Commit the skeleton and the directory scaffolding in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/145-bench-validation/145-BENCH-LOG.md && wc -l .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -c -- "--force used?" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && for s in "Gate 0" "Gate 1" "Gate 2" "Gate 3" "Verification map bindings" "SHA manifest" "Not measured" "Carry-forward hand-offs" "Dispatch mode"; do grep -q "$s" .planning/phases/145-bench-validation/145-BENCH-LOG.md || { echo "MISSING: $s"; exit 1; }; done; echo "all sections present"</automated>
    <automated>cd /workspaces && grep -q -- "--auto" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q -- "--chain" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "D-20 dispatch line present"</automated>
    <automated>cd /workspaces && grep -oE "145-0[1-9] Task [1-3]" .planning/phases/145-bench-validation/145-BENCH-LOG.md | sort -u | wc -l</automated>
    <automated>cd /workspaces && grep -c "NOT YET RUN" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -q "3.0.0b17" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "3085084" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "3.0.0b18" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "D-18 version caveat present"</automated>
    <automated>cd /workspaces && for p in images tools runs logs readbacks; do git check-ignore -q ".planning/phases/145-bench-validation/$p" && { echo "IGNORED: $p"; exit 1; }; done; echo "no artifact dir is gitignored"</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` exists and `wc -l` reports at least 120.
    - `grep -c -- "--force used?"` returns exactly 1 — the identity-table row.
    - The section loop prints `all sections present`, covering `Gate 0`, `Gate 1`, `Gate 2`, `Gate 3`, `Verification map bindings`, `SHA manifest`, `Not measured`, `Carry-forward hand-offs`, `Dispatch mode`.
    - The dispatch-mode line contains both literals `--auto` and `--chain` and states the phase was dispatched without them.
    - `grep -oE "145-0[1-9] Task [1-3]" | sort -u | wc -l` returns at least 12, and every row of `145-VALIDATION.md`'s per-task verification map appears in the bindings table with a binding.
    - The preamble contains both `validated` and `skipped-with-reason`, and the only occurrence of `inconclusive` is inside the sentence that denies it exists.
    - `grep -c "NOT YET RUN"` returns at least 20.
    - The record contains the literals `3.0.0b17`, `3085084` and `3.0.0b18` in the version-string caveat.
    - `git check-ignore -q` returns non-zero for each of `images`, `tools`, `runs`, `logs`, `readbacks` under the phase directory.
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0, and `git -C /workspaces/firestarter_app status --porcelain` shows no new entry attributable to this task.
  </acceptance_criteria>
  <done>The record skeleton exists in the 99-03 shape with every gate stubbed NOT YET RUN, D-14's taxonomy fixed up front, every VALIDATION row bound to a concrete task id, and the artifact directories committable.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the four address-attributable write images and the digest manifest</name>
  <files>.planning/phases/145-bench-validation/images/gen_addr_image.py, .planning/phases/145-bench-validation/images/img1.bin, .planning/phases/145-bench-validation/images/img2.bin, .planning/phases/145-bench-validation/images/img3.bin, .planning/phases/145-bench-validation/images/img_4k_pulse.bin, .planning/phases/145-bench-validation/SHA256SUMS.txt, .planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-2 — the verified word-stamped generator, the three masks, the measured digests, the `0xFF` counts, the pairwise-distinctness figures, the `0` to `1` transition percentages, and the simulated A8-stuck-low decode walkthrough.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` §"Claude's Discretion" first bullet — the binding constraint that a mismatch must be attributable to an address, not merely counted, and the Phase-97 pin-31 precedent behind it.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — as created by Task 1; the write-images subsection this task fills.
  </read_first>
  <action>
Author `.planning/phases/145-bench-validation/images/gen_addr_image.py` in the meta repo. This is bench
tooling, NOT host source — it must not be added to `firestarter_app/tools/` and no file in either sub-repo may
be touched (D-16). Name that boundary in a comment at the top of the file so it stays legible to a later
reader.

The generator implements RQ-2's word-stamped address pattern: for a byte at offset `N`, the stamp is the low
byte of `N` when `N` is even and the high byte of `N` when `N` is odd, XORed with a per-image mask. Each
aligned two-byte word therefore literally stamps its own 16-bit address, which is what makes a mismatched
value decodable back to a source address — the property `firestarter_app/tools/gen_test_image.py` does not
have and the reason D-05 rejects it. Give the generator a small CLI taking size, mask and output path, and
have it print the SHA-256 of what it wrote.

Generate exactly four images into `.planning/phases/145-bench-validation/images/`: `img1.bin` at 65536 bytes
with mask `0x00`; `img2.bin` at 65536 bytes with mask `0xFF`; `img3.bin` at 65536 bytes with mask `0x5A`; and
`img_4k_pulse.bin` at 4096 bytes with mask `0x3C`.

Assert the three 64 KiB digests against RESEARCH's independently computed values and STOP if any differs — a
mismatch means the generator does not implement the verified recipe. Expected:
`img1` is `f72489604bfe917db7ee505e4d674576b2905a418e8dc55372b78dcab3e34e3a`;
`img2` is `b566c7a0319cc37051ec9c92bc1faef81f75e3740c7c6c8864778a549624fd96`;
`img3` is `74c359c8d8668fdc5778270d61cc3fbef55a1027999f20c5798a54bf0f6aea01`.

Write `.planning/phases/145-bench-validation/SHA256SUMS.txt` in `sha256sum` output format, one line per image,
paths relative to the phase directory so `sha256sum -c` works from there. This file is the single home for
digests for the whole phase; later plans append read-back digests to it and the narrative carries none inline.
Recompute the digests from the files on disk rather than copying the generator's printed output.

Then fill the write-images subsection of `145-BENCH-LOG.md` with, at minimum: the four filenames and sizes;
the generator's path plus the explicit statement that it is meta-repo bench tooling authored under
`.planning/phases/145-bench-validation/` and NOT inside `firestarter/` or `firestarter_app/` (D-16); the
measured pairwise distinctness, all three 64 KiB pairs differing in 65536 of 65536 bytes; the erase-oracle
figures — bytes needing at least one `0` to `1` transition are 65408 of 65536, 99.8 %, for cycle 1 to 2, and
59392 of 65536, 90.6 %, for cycle 2 to 3 — followed by the sentence that a clean cycle-2 PASS is therefore
positive proof the erase fired, since a silently no-op erase leaves those bytes unprogrammable and the write
fails with `MSG_ERR_MAX_PULSES`; the per-image `0xFF` byte counts 128, 384 and 128, with the honest statement
that the firmware skips a byte whose expected value is `0xFF` without issuing a pulse, so the record must not
imply all 65536 bytes were pulsed, while noting those bytes are still covered by the final full-block verify
pass; and one worked sentence showing how a mismatched value decodes to a source address, using RQ-2's
A8-stuck-low example — first mismatch at offset `0x0101`, observed byte `0x00`, un-masks to `0x00`, odd offset
means the high address byte, so the byte belongs to an address whose high byte is `0x00`, naming A8 as the
aliased line.

Commit the generator, the four images, the manifest and the record edit together in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/145-bench-validation && stat -c "%n %s" images/img1.bin images/img2.bin images/img3.bin images/img_4k_pulse.bin</automated>
    <automated>cd /workspaces/.planning/phases/145-bench-validation && sha256sum images/img1.bin images/img2.bin images/img3.bin | grep -cE "f72489604bfe917db7ee505e4d674576b2905a418e8dc55372b78dcab3e34e3a|b566c7a0319cc37051ec9c92bc1faef81f75e3740c7c6c8864778a549624fd96|74c359c8d8668fdc5778270d61cc3fbef55a1027999f20c5798a54bf0f6aea01"</automated>
    <automated>cd /workspaces/.planning/phases/145-bench-validation && sha256sum -c SHA256SUMS.txt</automated>
    <automated>cd /workspaces/.planning/phases/145-bench-validation && python3 -c "import pathlib; d=[pathlib.Path('images/img%d.bin'%i).read_bytes() for i in (1,2,3)]; print('pairs_identical=', sum(1 for a in range(3) for b in range(a+1,3) if d[a]==d[b]))"</automated>
    <automated>cd /workspaces && grep -q "99.8" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "90.6" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "65408" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "59392" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "erase-oracle figures recorded"</automated>
    <automated>cd /workspaces && grep -q "0x0101" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "A8" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "address decode walkthrough recorded"</automated>
    <automated>cd /workspaces && test ! -e firestarter_app/tools/gen_addr_image.py && test ! -e firestarter/tools/gen_addr_image.py && echo "generator not in either sub-repo"</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `stat` reports 65536 bytes for each of `img1.bin`, `img2.bin`, `img3.bin` and 4096 bytes for `img_4k_pulse.bin`.
    - The digest grep returns 3 — all three 64 KiB digests equal RESEARCH's values byte for byte.
    - `sha256sum -c SHA256SUMS.txt` exits 0 from the phase directory and the manifest lists all four images.
    - The pairwise check prints `pairs_identical= 0`.
    - `gen_addr_image.py` exists under `.planning/phases/145-bench-validation/images/` and does not exist anywhere under `firestarter/` or `firestarter_app/`; its header comment names the D-16 boundary.
    - The record's write-images subsection contains the literals `99.8`, `90.6`, `65408`, `59392`, the three `0xFF` counts `128`, `384`, `128`, and a sentence stating not all 65536 bytes are pulsed.
    - The record contains the A8 decode walkthrough naming offset `0x0101` and the line `A8`.
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0.
  </acceptance_criteria>
  <done>Four distinct address-attributable images exist with digests matching the research-verified values, the manifest verifies clean, and the record explains why a mismatch is decodable to an address.</done>
</task>

<task type="auto">
  <name>Task 3: Build the frame-extraction instrument, prove both its outcomes, and record the tripwire baseline</name>
  <files>.planning/phases/145-bench-validation/tools/extract_frames.py, .planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-4 in full — the emission site at `eprom.cpp:399-402`, the `#ifndef SERIAL_ON_IO` leonardo-only guard, the 1000 ms `EPROM_PROGRESS_EMIT_INTERVAL_MS` cadence with `last_emit_ms` re-initialised at the top of every block, the `bar_format` string `{n:#06x}/{total:#06x} bytes`, the tqdm non-TTY probe results, Claim A versus Claim B, and the two-bar discriminator (the INIT blank check emits at a 2048-byte step and `ClassProgressHandler.start()` closes and re-creates the bar, leaving a newline).
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-9 — the nine porcelain assertion sites by file and line, and the measured baselines: firmware 312 passed, host sibling-porcelain subset 38 passed.
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §"Common Pitfalls" items 1, 6 and 10.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — the instrument-inventory and tripwire-baseline subsection this task fills.
  </read_first>
  <action>
Author `.planning/phases/145-bench-validation/tools/extract_frames.py` — meta-repo bench tooling, Python 3, no
third-party imports. Name the D-16 boundary in its header comment. Do NOT reach for `awk`'s `strtonum`: it is
a gawk extension and is not portable here, so hex parsing happens in Python.

Behaviour, exactly. The script takes one argument, the path to a raw tqdm stderr capture, and reads it as
bytes decoded permissively. It splits the content on newline into segments — each newline marks a bar close
and re-create, so the LAST segment containing at least one progress frame is the write bar and every earlier
segment is the INIT-phase blank-check bar. Within the selected segment it splits on carriage return and
extracts every frame matching a position and total of the shape `0xNNNN/0xNNNN bytes`, converting positions
with base-16 integer parsing, then de-duplicates and sorts numerically. It prints to stdout, in this order and
format so the output is greppable: a `segments=<N>` line; a `selected_segment=<i>` line, one-based; a
`frames=<M>` line counting frames in the selected segment before de-duplication; one tab-separated line per
distinct position giving the decimal position, its block index computed as the position integer-divided by
1024, and the literal `boundary` when the position is an exact multiple of 1024 or the literal `INTRA-BLOCK`
when it is not; an `intra_block_frames=<count>` line; a `blocks_with_multiple_updates=<count>` line; then one
`block <b> has <c> updates` line for each block holding more than one distinct position; and finally a
`step_histogram=` line summarising the distinct deltas between consecutive positions, so a 2048-step INIT bar
is recognisable at a glance if segment selection ever misfires.

Give the script a `--selftest` flag that builds two synthetic fixtures in memory, runs the same parsing code
over each, checks every expected field, prints `SELFTEST: POSITIVE PASS` and `SELFTEST: NEGATIVE PASS` on
success, prints a field-by-field expected-versus-observed diff on failure, and exits non-zero unless both
legs pass. Both outcomes must be exercised — a pre-authored measurement leg proves nothing until each of its
outcomes has been seen.

The positive fixture is two segments. Segment one is an INIT-style bar with frames at `0x0000`, `0x0800` and
`0x1000` separated by carriage returns and terminated by a newline. Segment two is a write-style bar with
frames at `0x0000`, `0x0400`, `0x0500` and `0x0800` separated by carriage returns and terminated by a newline.
Expected and asserted: `segments=2`; `selected_segment=2`; exactly four distinct positions 0, 1024, 1280 and
2048; `intra_block_frames=1` for position 1280; `blocks_with_multiple_updates=1`; and the line
`block 1 has 2 updates`. Assert additionally that position 4096 does NOT appear, which is what proves segment
one's 2048-step INIT frames did not leak into the selection (Pitfall 6).

The negative fixture is the same two-segment shape with segment two carrying only boundary frames `0x0000`,
`0x0400`, `0x0800` and `0x0C00`. Expected and asserted: `intra_block_frames=0` and
`blocks_with_multiple_updates=0`.

If either leg fails, STOP and report — the instrument is not trustworthy and no bench spend may proceed on it.

Finally, record the pre-bench tripwire baseline. Assert `git -C /workspaces/firestarter status --porcelain` is
empty FIRST and stop-and-report if it is not: one untracked file in the firmware checkout turns 9 tests RED,
five in `firestarter` and four in `firestarter_app` (RQ-9). Then run the firmware suite and the host
sibling-porcelain subset, both with `-o addopts=` cleared, because the repos' `addopts` is `-ra -q` and
doubling `-q` suppresses the count line. The host subset is `tests/test_py32_flash_map_host.py`,
`tests/test_cap03_ack_layout_parity.py` and `tests/test_py32_asset_name_host.py`.

Record in the record's instrument-inventory subsection: the exact commands run; the firmware pass count
alongside RESEARCH's measured baseline of 312; the host subset pass count alongside 38; the porcelain
assertion result; both self-test verdicts with their expected-versus-observed field values; and
`extract_frames.py`'s path with its D-16 statement. State plainly in that subsection that this phase adds no
automated test, and that the suites are run as regression tripwires rather than as requirement evidence.

Commit the instrument and the record edit together in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "import ast; ast.parse(open('.planning/phases/145-bench-validation/tools/extract_frames.py').read()); print('parses')"</automated>
    <automated>cd /workspaces && python3 .planning/phases/145-bench-validation/tools/extract_frames.py --selftest</automated>
    <automated>cd /workspaces && python3 .planning/phases/145-bench-validation/tools/extract_frames.py --selftest | grep -c "PASS"</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts= 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_py32_flash_map_host.py tests/test_cap03_ack_layout_parity.py tests/test_py32_asset_name_host.py -q -o addopts= 2>&1 | tail -3</automated>
    <automated>cd /workspaces && grep -q "312" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "38" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "SELFTEST" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "tripwire baseline and selftest recorded"</automated>
    <automated>cd /workspaces && test ! -e firestarter_app/tools/extract_frames.py && test ! -e firestarter/tools/extract_frames.py && echo "extractor not in either sub-repo"</automated>
  </verify>
  <acceptance_criteria>
    - `extract_frames.py` parses as valid Python 3 and lives only under `.planning/phases/145-bench-validation/tools/`.
    - `python3 extract_frames.py --selftest` exits 0 and its output contains both `SELFTEST: POSITIVE PASS` and `SELFTEST: NEGATIVE PASS`; the `PASS` count is 2.
    - The positive leg asserts `segments=2`, `selected_segment=2`, the four positions 0/1024/1280/2048, `intra_block_frames=1`, `blocks_with_multiple_updates=1`, the line `block 1 has 2 updates`, and the absence of position 4096.
    - The negative leg asserts `intra_block_frames=0` and `blocks_with_multiple_updates=0`.
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0 immediately before both pytest invocations.
    - The firmware suite reports 0 failed with its pass count recorded in the record next to the baseline 312; the host subset reports 0 failed with its count recorded next to 38.
    - Both pytest invocations were issued with `-o addopts=` cleared, and the count line is visible in the captured output.
    - The record's instrument-inventory subsection contains `312`, `38`, `SELFTEST`, `extract_frames.py`, and the sentence that this phase adds no automated test and the suites are regression tripwires.
  </acceptance_criteria>
  <done>The frame-extraction instrument exists, both of its outcomes have been observed on synthetic fixtures, and the pre-bench tripwire baseline is recorded with the firmware porcelain proven empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta repo → sub-repo checkouts | Bench tooling authored on the wrong side of this line turns 9 tests RED and violates D-16 |
| synthetic fixture → real capture | The instrument's correctness is only as good as the outcomes actually observed before it is trusted |
| generated evidence → git | An artifact written into a gitignored path is silently never committed and the evidence disappears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | Bench tooling written into `firestarter/` or `firestarter_app/` | mitigate | D-16 prohibition plus acceptance criteria asserting both tool paths are absent from both sub-repos and firmware porcelain is empty after every task |
| T-145-02 | Repudiation | `extract_frames.py` trusted without either outcome observed | mitigate | `--selftest` exercises a positive and a negative fixture with full field assertions and exits non-zero unless both pass |
| T-145-03 | Information disclosure | Evidence written into `firestarter-runs/`, `consistency-check-*/` or `write-cycle-*/` and silently never committed | mitigate | Explicit non-matching directory names plus a `git check-ignore` acceptance criterion on all five artifact directories |
| T-145-04 | Spoofing | An image that detects a fault but cannot localise it, read as address-attributable evidence | mitigate | Word-stamped generator with three digests asserted against independently computed values; `gen_test_image.py` prohibited |
| T-145-05 | Repudiation | A record pre-filled with predicted values that later read as measurements | mitigate | Every Gate 1 to 3 field stubbed `NOT YET RUN` with a minimum-count acceptance criterion |
| T-145-06 | Elevation of privilege | An auto-mode approving this phase's operator gates | mitigate | `autonomous: false` on every plan plus a D-20 dispatch-mode line inside the record itself |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | This phase installs nothing and touches no manifest (D-16); RESEARCH's Package Legitimacy Audit is recorded as not applicable |
</threat_model>

<verification>
- `cd /workspaces && wc -l .planning/phases/145-bench-validation/145-BENCH-LOG.md` — at least 120 lines.
- `cd /workspaces && grep -c -- "--force used?" .planning/phases/145-bench-validation/145-BENCH-LOG.md` — exactly 1.
- `cd /workspaces/.planning/phases/145-bench-validation && sha256sum -c SHA256SUMS.txt` — exits 0.
- `cd /workspaces && python3 .planning/phases/145-bench-validation/tools/extract_frames.py --selftest` — exits 0, both legs PASS.
- `cd /workspaces/firestarter && git status --porcelain` — empty.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=` — 0 failed.
- No file under `/workspaces/firestarter` or `/workspaces/firestarter_app` was created, edited or deleted.
</verification>

<success_criteria>
- The bench record skeleton exists with every gate section present, every measurement field stubbed `NOT YET RUN`, D-14's taxonomy fixed before any run, D-20's dispatch line stated, and every `145-VALIDATION.md` row bound to a concrete plan-and-task id.
- Four distinct write images exist whose values decode back to a source address, with digests matching the research-verified figures and a manifest that verifies clean.
- The D-10 frame-extraction instrument exists and both of its outcomes have been observed before any silicon is spent.
- The pre-bench tripwire baseline is recorded with firmware porcelain proven empty and both suites green.
- Nothing in either sub-repo changed; no hardware was touched.
</success_criteria>

<output>
Create `.planning/phases/145-bench-validation/145-01-SUMMARY.md` when done. It MUST record: the four image
digests as recomputed from disk; both self-test verdicts with their observed field values; the two pytest
counts alongside their baselines 312 and 38; the `git check-ignore` result for all five artifact directories;
and a one-line index of where each mandatory skeleton section lives in `145-BENCH-LOG.md`.
</output>
