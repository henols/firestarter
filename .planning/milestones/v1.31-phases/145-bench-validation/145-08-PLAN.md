---
phase: 145-bench-validation
plan: 08
type: execute
wave: 8
depends_on: ["145-07"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/145-bench-validation/145-BENCH-LOG.md
  - .planning/phases/145-bench-validation/SHA256SUMS.txt
  - .planning/phases/145-bench-validation/145-08-SUMMARY.md
autonomous: false
requirements: [BENCH-01, BENCH-02, BENCH-03]

must_haves:
  truths:
    - "Per D-10 bar motion is discharged two ways and both halves are recorded: the machine-counted frame evidence and the operator's own verbatim statement of what the terminal actually did"
    - "Per D-14 every measurement in the record lands in exactly one of validated, skipped-with-reason or fail — no measurement is left as inconclusive or partial"
    - "Per D-12 anything this phase did not discharge is recorded as an open hand-off with NO v1.31 owner, because Phase 146 is docs-and-claims only and cannot run a bench — nothing is silently dropped"
    - "Per D-08 the record makes no comparative claim that v1.31 programs better than what preceded it, and per the milestone's 6.25 V ceiling it makes no datasheet-conformance claim"
    - "Per BENCH-03 no chip's support_status changed, re-confirmed at the end of the phase as well as at Gate 0"
    - "Per RQ-9 the firmware repo's porcelain is empty and both suites are green after every bench artifact has landed, proving no artifact leaked into the firmware checkout"
    - "Every artifact the phase produced is inventoried and every digest lives in SHA256SUMS.txt, so a reader never has to open a binary to check one"
    - "Per D-16 no file under firestarter/ or firestarter_app/ was created, edited or deleted at any point in this phase"
  artifacts:
    - path: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      provides: "The closed record: Gate 3 verdict, D-10 both halves, the not-measured section, the carry-forward hand-offs, the phase VERDICT and the session-end line"
      contains: "no v1.31 owner"
  key_links:
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/phases/145-bench-validation/SHA256SUMS.txt"
      via: "the artifact inventory pointing at the single digest manifest rather than inlining hashes"
      pattern: "SHA256SUMS.txt"
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/ROADMAP.md"
      via: "the four Phase 145 success criteria answered one by one in the phase verdict"
      pattern: "BENCH-0[1-3]"
  prohibitions:
    - "MUST NOT flip any requirement checkbox in .planning/REQUIREMENTS.md or .planning/ROADMAP.md — that is 145-09's, behind its own operator gate"
    - "MUST NOT describe any outcome as inconclusive or partial — D-14 admits only validated, skipped-with-reason or fail"
    - "MUST NOT claim datasheet conformance, algorithm correctness or datasheet-correct behaviour anywhere — the 6.25 V program-VCC ceiling is unreachable on this shield and that debt belongs to the milestone, not to this phase"
    - "MUST NOT make any comparative claim that v1.31 programs better, faster or more reliably than what preceded it (D-08) — no control run was made and none was intended"
    - "MUST NOT infer the 0x08 or 0x0B dispositions from the 0x07 result, and MUST NOT restate them as anything other than skipped-with-reason"
    - "MUST NOT change any chip's support_status, and MUST NOT run tools/build_db.py"
    - "MUST NOT silently drop any undischarged item — every one is recorded with the literal phrase no v1.31 owner"
    - "MUST NOT run pytest while git -C /workspaces/firestarter status --porcelain is non-empty, and MUST NOT omit -o addopts="
    - "MUST NOT create, edit, rename or delete any file under /workspaces/firestarter or /workspaces/firestarter_app (D-16)"
    - "MUST NOT extrapolate the 0x07 result to any other protocol, part or board revision — the bench was one part, one controller and one shield revision"
---

<objective>
Close the record. Capture D-10's eyes-on half from the operator while the run they watched is still fresh,
verdict Gate 3, then write the phase verdict in D-14's vocabulary with every not-measured reading named, every
undischarged item carried forward with no v1.31 owner, and every artifact inventoried. Finally re-run the
tripwire suites to prove no bench artifact leaked into the firmware checkout.

Purpose: the entire output of this phase IS its record. A verdict that softens a result, omits a reading or
drops a hand-off is the exact overclaim Phase 146's claim gate exists to catch — and it would be this
milestone committing its own anti-pattern.

Output: a closed `145-BENCH-LOG.md` with a Gate 3 verdict, both halves of D-10, a not-measured section, the
carry-forward hand-offs, the phase VERDICT and the session-end line, plus a post-bench tripwire result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-bench-validation/145-CONTEXT.md
@.planning/ROADMAP.md
</context>

<artifacts_this_phase_produces>
This plan produces no new committed artifact beyond the closed record and its own summary. It appends to
`SHA256SUMS.txt` only if the inventory finds a produced binary whose digest is missing. The phase's full
artifact inventory is listed in `145-01-PLAN.md`; every path there is new in this phase and must be excluded
from any drift check. No file in `firestarter/` or `firestarter_app/` is produced or modified (D-16).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture D-10's eyes-on half and close the Gate 3 verdict</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` D-10, D-19 and D-20 — the two-way discharge, the operator-only half, and why this gate is real rather than auto-approvable.
    - `.planning/phases/145-bench-validation/logs/frames_pulse4688.txt` and `.planning/phases/145-bench-validation/logs/frames_cycle1.txt` — the machine half's numbers, which are what the operator's statement is placed beside rather than replaced by.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — Gate 3's run subsections and the operator eyes-on subsection stubbed by `145-01`.
  </read_first>
  <action>
Present the machine half's measured numbers and the question below, then STOP. This gate is blocking and is
NOT auto-approvable even under auto-advance: only a human can say what the terminal looked like, and D-10
requires both halves (D-19, D-20).

Record the operator's answer verbatim — their words, not a paraphrase, and not a tidied-up version. If the
operator says the bar burst at the end rather than moving smoothly, record exactly that; a contradiction
between the machine half and the eyes-on half is a finding worth having, and D-06's principle applies here too
— a disagreement must be visible rather than averaged away.

Then fill the `Gate 3 verdict:` line. It must name: whether the 4688 µs run completed and its elapsed figure;
the Claim B verdict as measured; both D-12 mechanism items with their outcome; the A1 item's outcome, whether
derived or explicitly not discharged; and the eyes-on statement's outcome. Use D-14's vocabulary throughout —
`validated`, `skipped-with-reason` or `fail`, never anything softer.
  </action>
  <what-built>
Gate 3 is run. A 4096-byte write at `--pulse-us 4688` completed with its default-visible provenance line
recorded verbatim, and the frame extractor's output for that run is quoted above alongside the database-pulse
profile from cycle 1, so you can see both numbers side by side.

That run also carries the two D-12 mechanism items: `--pulse-us` exercised on real silicon, and the
above-4687 µs proof that the advertised CAP-03 budget — about 244 s, versus the old 120 s fallback — is what
carried the run.

One half of D-10 is machine-counted and is above. The other half is yours.
  </what-built>
  <how-to-verify>
Describe, in your own words, what the terminal actually did during the `--pulse-us 4688` write.

The specific thing being asked: did the progress bar **move smoothly** through the run, or did the frames
arrive as a **burst at the end**? Roughly how many visible updates did you see, and were they evenly spaced?

Why it matters: the machine half proves frames exist at particular byte positions, which is a checkable claim.
It cannot tell you whether a person watching the terminal experienced a live-progress display. Those are
different statements and the record keeps them apart.

Please answer honestly even if it contradicts the numbers above. A disagreement between the machine count and
what you saw is a genuine finding and will be recorded as one — it will not be smoothed over.

If you did not watch the run, say so. "Not observed" is a legitimate answer and gets recorded as a
not-measured reading with its reason, rather than being invented.
  </how-to-verify>
  <resume-signal>Describe what the terminal did during the 4688 us write — or say "not observed".</resume-signal>
  <verify>
    <human-check>The operator has described what the terminal did, or has said the run was not observed. The answer is recorded verbatim, including any contradiction with the machine count.</human-check>
    <automated>cd /workspaces && grep -A5 -i "eyes-on" .planning/phases/145-bench-validation/145-BENCH-LOG.md | grep -qv "NOT YET RUN" && echo "eyes-on subsection filled"</automated>
    <automated>cd /workspaces && grep -A4 "Gate 3 verdict" .planning/phases/145-bench-validation/145-BENCH-LOG.md | grep -qv "NOT YET RUN" && echo "Gate 3 verdict filled"</automated>
    <automated>cd /workspaces && grep -ciE "\binconclusive\b|\bpartial pass\b" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The operator eyes-on subsection contains the operator's statement verbatim, in quotation marks, and no longer reads `NOT YET RUN`; if the answer was "not observed", it is recorded as a not-measured reading with its reason.
    - If the eyes-on statement contradicts the machine count, the contradiction is stated explicitly in the record rather than reconciled away.
    - The `Gate 3 verdict:` line is filled and names all five required elements: run completion with elapsed, the Claim B verdict, both D-12 mechanism outcomes, the A1 outcome, and the eyes-on outcome.
    - The verdict uses only `validated`, `skipped-with-reason` or `fail`; `grep -ciE "\binconclusive\b|\bpartial pass\b"` over the record returns 0.
    - The operator's response is captured verbatim in `145-08-SUMMARY.md`.
  </acceptance_criteria>
  <done>D-10 carries both halves — machine-counted frames and the operator's own words — and Gate 3 is verdicted in D-14's vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Write the phase verdict, the not-measured section and the carry-forward hand-offs</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 145: Bench Validation" — the four success criteria this verdict must answer one by one, verbatim.
    - `.planning/REQUIREMENTS.md` §"Bench Validation" — BENCH-01, BENCH-02 and BENCH-03 in their exact wording.
    - `.planning/phases/145-bench-validation/145-CONTEXT.md` D-08, D-12, D-14 and the phase-boundary paragraph naming what is explicitly NOT this phase's.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` — every gate verdict written so far, which this verdict summarises rather than re-derives.
    - `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md` §10 H6 and H7 — the two hand-offs this phase inherited, so the record can say which are now discharged and which are not.
    - `.planning/phases/99-bench-ledger-graduation-gate-evidence-ledger-update/99-03-BENCH-LOG.md` — its VERDICT section shape, including the positive findings, residual defect and carry-forward structure.
  </read_first>
  <action>
Close the record's three remaining narrative sections.

First, the `Not measured` section. Walk every reading this phase could have taken and did not, and give each
its own line with its blocking reason. At minimum consider: any DMM reading (operator-only, and none was
required by this phase's gates unless one was taken); program-window VPP under load, which remains
un-instrumented and is `0x08`'s inherited FUT-08 hypothesis rather than a `0x07` question; the advertised
CAP-03 budget value itself, which nothing logs because the host decodes it silently; and any Gate 3 item that
was not run. If a reading in this list WAS taken, move it out of this section rather than listing it twice.
Do not pad this section with items that were never in scope.

Second, the `Carry-forward hand-offs with no v1.31 owner` section. Every item this phase did not discharge
goes here with the literal phrase `no v1.31 owner`, and the reason Phase 146 cannot take it: Phase 146 is
docs-and-claims only and cannot run a bench. Candidates, each included only if genuinely undischarged: D-12's
A1 measurement if the companion run was declined; D-10's machine half if Claim B did not hold; a true-UV
`0x07` data point on the TMS27C512, deliberately not spent under D-01 and reachable only by consuming an
irreversible part; `0x08` (AM27C020) bench validation, blocked on a part and carrying FUT-08's
un-instrumented program-window droop hypothesis; `0x0B` (M2716/M2732) bench validation with its graduation
parked at Phase 79's plan `79-03`; and the 6.25 V program-VCC ceiling, which is the milestone's accepted debt
and explicitly not this phase's to discharge. Name the owner as none for each, or name a real successor if one
genuinely exists.

Third, the phase `VERDICT:` section. Answer ROADMAP's four Phase 145 success criteria one at a time, each
quoted and each given a D-14 outcome word:

- Criterion 1, the `0x07` full write to read to verify on Leonardo with per-run evidence: cite the three cycles,
  both oracles, the per-cycle stability results and the artifact paths.
- Criterion 2, `0x08` either bench-validated or recorded skipped-with-reason naming the missing part: cite the
  disposition record and repeat that it is not inferred from the `0x07` result.
- Criterion 3, `0x0B` likewise.
- Criterion 4, no chip's `support_status` changed as a result of this milestone's bench runs: cite BENCH-03's
  four legs and add the end-of-phase re-confirmation from Task 3.

Then state the phase's positive findings and its boundaries in the 99-03 register. The boundaries are not
optional and each must appear: no comparative claim is made that v1.31 programs better than what preceded it,
because D-08 rejected a control run and this milestone claims fidelity, not improvement; no datasheet
conformance is claimed, because the 6.25 V program-VCC ceiling is unreachable on this shield and that debt
belongs to the milestone; the evidence covers exactly one part (Winbond W27C512, `0xda08`), one controller
(Leonardo) and one shield revision (Rev 2.0, operator eyes-on), and nothing here extrapolates to another
protocol, part or board; the intra-block progress emission is `leonardo`-only and structurally unavailable on
`SERIAL_ON_IO` targets; and `0x08` and `0x0B` remain unvalidated on hardware.

Finish with the `Session end:` line naming the operator as witness, and confirm the `Dispatch mode` line still
states the run was dispatched without `--auto` and without `--chain`.

Do NOT flip any requirement checkbox here.

Commit the record edit in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces && grep -A4 "VERDICT" .planning/phases/145-bench-validation/145-BENCH-LOG.md | grep -qv "NOT YET RUN" && echo "phase verdict filled"</automated>
    <automated>cd /workspaces && grep -c "no v1.31 owner" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -ciE "datasheet[- ]conforman|datasheet[- ]correct|algorithm[- ]accurate" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -qi "6.25" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -qi "fidelity, not improvement" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "both mandatory boundaries present"</automated>
    <automated>cd /workspaces && grep -cE "BENCH-0[1-3]" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -ciE "\binconclusive\b|\bpartial pass\b" .planning/phases/145-bench-validation/145-BENCH-LOG.md</automated>
    <automated>cd /workspaces && grep -qi "Session end" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q -- "--chain" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "session end and dispatch line present"</automated>
    <automated>cd /workspaces && git diff --name-only HEAD~1 -- .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The `Not measured` section lists every un-taken reading with its own blocking reason, and contains no item that was in fact measured.
    - The carry-forward section contains the literal phrase `no v1.31 owner` at least once per undischarged item, and states that Phase 146 is docs-and-claims only and cannot run a bench.
    - The `VERDICT:` section quotes all four ROADMAP success criteria and gives each a D-14 outcome word.
    - The record contains the literal `6.25` and a statement that no datasheet conformance is claimed; `grep -ciE "datasheet[- ]conforman|datasheet[- ]correct|algorithm[- ]accurate"` returns hits only inside explicit non-claim sentences, never as an unqualified claim.
    - The record contains the phrase `fidelity, not improvement` or an equivalent explicit denial of a comparative claim, and names D-08 as the reason no control run exists.
    - The record names the exact evidence scope — one part `W27C512` `0xda08`, one controller `leonardo`, one shield revision Rev 2.0 — and states that nothing extrapolates beyond it.
    - The record states that the intra-block emission is `leonardo`-only and unavailable on `SERIAL_ON_IO` targets.
    - `grep -ciE "\binconclusive\b|\bpartial pass\b"` returns 0.
    - The `Session end:` line is filled and the `Dispatch mode` line still names `--auto` and `--chain`.
    - `git diff --name-only HEAD~1 -- .planning/REQUIREMENTS.md .planning/ROADMAP.md | wc -l` returns 0 — no requirement was flipped by this plan.
  </acceptance_criteria>
  <done>The record carries a phase verdict answering all four ROADMAP criteria in D-14's vocabulary, every un-taken reading named with its blocker, every undischarged item carried forward with no v1.31 owner, and every mandatory boundary stated rather than implied.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the tripwire suites, re-confirm BENCH-03 at the tip, and inventory every artifact</name>
  <files>.planning/phases/145-bench-validation/145-BENCH-LOG.md, .planning/phases/145-bench-validation/SHA256SUMS.txt</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §RQ-9 — the nine porcelain assertion sites by file and line, the measured baselines (firmware 312 passed, host tripwire subset 38 passed), the safe and unsafe artifact locations table, and the two meta `.gitignore` rules to route around.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` §"Gate 0 instrument inventory" — the pre-bench baseline this run is compared against.
    - `.planning/phases/145-bench-validation/145-RESEARCH.md` §"Recommended artifact layout" — the full expected artifact tree, which the inventory is checked against.
    - `.planning/phases/145-bench-validation/SHA256SUMS.txt` — every digest recorded so far, which the inventory must find complete.
  </read_first>
  <action>
Prove the bench left no trace where it must not, and that the record's evidence is all present and hashed.

First, the RQ-9 tripwire. Assert `git -C /workspaces/firestarter status --porcelain` is EMPTY. A single
untracked file anywhere in the firmware checkout turns 9 tests RED — five in `firestarter` and four in
`firestarter_app` — so a non-empty result here means a bench artifact leaked into the wrong repo and must be
moved before anything else happens. Then run both suites with `-o addopts=` cleared, because the repos'
`addopts` is `-ra -q` and doubling `-q` hides the count line: the full firmware suite, and the full host
suite. Record both counts alongside the Gate 0 baselines of 312 and 38 (the host figure being the
sibling-porcelain subset; if you run the full host suite, record both its total and the subset's). Any
regression against the baseline is a finding to record and stop-and-report, not to explain away.

Second, re-confirm BENCH-03 at the tip now that the bench is complete, so criterion 4's wording — no chip's
`support_status` changes **as a result of this milestone's bench runs** — is answered at the end and not only
at Gate 0. Re-run the whole-milestone diff over `chip_database.json`, re-run the AST write-locus checker, and
re-compute the histogram and digest. Record the result verbatim in a short end-of-phase subsection and state
whether it matches Gate 0's figures exactly. Do not regenerate the database and do not run `tools/build_db.py`.

Third, the artifact inventory. List every artifact the phase produced, grouped as the record's layout section
describes: the four images plus the generator; the frame extractor; `prewrite.bin` and the three read-backs;
the nine `run_NN.bin` files across three cycle directories; every log; and `SHA256SUMS.txt` itself. For each
binary, confirm its digest is present in `SHA256SUMS.txt` — append any that is missing rather than leaving it
unhashed — and run `sha256sum -c SHA256SUMS.txt` from the phase directory as the completeness check. Confirm
with `git check-ignore` that no artifact path is ignored, and confirm none of the evidence directories starts
with `consistency-check-` or lives under `firestarter-runs/` or `write-cycle-*`. Confirm every artifact is
tracked by git rather than merely present on disk.

Record the inventory as a table in the record's artifact section: path, size, purpose, and whether its digest
is in the manifest. State the total artifact count and the total committed byte size so a reader knows what
the evidence set actually is.

Fourth, state the D-16 closing assertion: across the whole phase no file under `firestarter/` or
`firestarter_app/` was created, edited, renamed or deleted, the only write into the firmware checkout being
PlatformIO's gitignored `.pio/` build output. Back it with the porcelain results from both repos.

Commit the record edit and any manifest append in the meta repo.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts= 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -q -o addopts= 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && git diff 4d18b645..HEAD -- firestarter/data/chip_database.json | wc -c</automated>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_no_community_support_status_write.py; echo "exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app && sha256sum firestarter/data/chip_database.json</automated>
    <automated>cd /workspaces/.planning/phases/145-bench-validation && sha256sum -c SHA256SUMS.txt</automated>
    <automated>cd /workspaces && find .planning/phases/145-bench-validation -type f \( -name "*.bin" -o -name "*.raw" -o -name "*.log" -o -name "*.txt" -o -name "*.py" \) | sort | wc -l</automated>
    <automated>cd /workspaces && find .planning/phases/145-bench-validation -type f -print0 | xargs -0 git check-ignore 2>/dev/null | wc -l</automated>
    <automated>cd /workspaces && git ls-files .planning/phases/145-bench-validation | wc -l</automated>
    <automated>cd /workspaces && grep -qi "artifact" .planning/phases/145-bench-validation/145-BENCH-LOG.md && grep -q "SHA256SUMS.txt" .planning/phases/145-bench-validation/145-BENCH-LOG.md && echo "inventory recorded"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter status --porcelain | wc -l` returns 0, asserted before either pytest invocation.
    - The full firmware suite reports 0 failed with its count recorded against the Gate 0 baseline of 312; the host suite reports 0 failed with its count recorded, and the sibling-porcelain subset figure is recorded against 38.
    - Both pytest invocations used `-o addopts=` cleared and the count line is visible in the captured output.
    - The end-of-phase BENCH-03 re-confirmation shows a zero-byte `chip_database.json` diff over `4d18b645..HEAD`, the checker exiting 0, and a digest and histogram matching Gate 0's figures exactly — with any discrepancy recorded rather than smoothed.
    - `sha256sum -c SHA256SUMS.txt` exits 0 from the phase directory and covers every produced binary: the four images, `prewrite.bin`, the three read-backs and the nine `run_NN.bin` files.
    - `find ... | xargs git check-ignore | wc -l` returns 0 — no produced artifact is gitignored — and no evidence directory name starts with `consistency-check-`.
    - `git ls-files .planning/phases/145-bench-validation | wc -l` accounts for every produced file, so nothing is present on disk but untracked.
    - The record contains an artifact inventory table with path, size, purpose and manifest-presence per entry, plus a total count and total byte size.
    - The record contains the D-16 closing assertion backed by both repos' porcelain results.
  </acceptance_criteria>
  <done>Both suites are green with the firmware porcelain proven empty, BENCH-03 is re-confirmed at the tip, and every artifact is inventoried, hashed, committed and provably not gitignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| machine count → human experience | Frames at byte positions and a smoothly moving terminal are different statements; conflating them overclaims |
| undischarged item → the milestone | Phase 146 cannot run a bench, so an item dropped here is unrecoverable rather than merely deferred |
| bench artifacts → firmware checkout | One untracked file in `/workspaces/firestarter` turns 9 tests RED and falsifies any later suite-green claim |
| this phase's evidence → the milestone's claims | The record is what Phase 146's honesty ledger will cite; an overclaim here propagates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-47 | Repudiation | An eyes-on statement paraphrased, tidied, or reconciled with the machine count | mitigate | Verbatim capture required in quotation marks; a contradiction must be stated explicitly; "not observed" is an accepted answer recorded as not-measured |
| T-145-48 | Repudiation | An undischarged hand-off silently dropped at close | mitigate | The literal phrase `no v1.31 owner` required per item with a grep acceptance criterion, and the reason Phase 146 cannot take it stated |
| T-145-49 | Repudiation | A datasheet-conformance or comparative claim entering the record | mitigate | Both prohibited by name; the 6.25 V ceiling and the fidelity-not-improvement denial required as recorded literals with grep criteria |
| T-145-50 | Repudiation | The one-part, one-board result extrapolated to other protocols or revisions | mitigate | The evidence scope named explicitly — `W27C512` `0xda08`, `leonardo`, Rev 2.0 — with a no-extrapolation sentence |
| T-145-51 | Information disclosure | A produced artifact left untracked, unhashed or gitignored, so the evidence set is incomplete | mitigate | `sha256sum -c` over the whole manifest, `git check-ignore` over every produced file, and `git ls-files` reconciliation against the on-disk inventory |
| T-145-52 | Tampering | A bench artifact left inside the firmware checkout, falsifying a suite-green claim | mitigate | Porcelain asserted empty before both suite runs, with the nine assertion sites named and a stop-and-report on any non-empty result |
| T-145-53 | Elevation of privilege | An auto-mode supplying the eyes-on answer on the operator's behalf | mitigate | Gate declared not auto-approvable, `autonomous: false`, and the answer required verbatim in quotation marks |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing and touches no manifest (D-16) |
</threat_model>

<verification>
- `cd /workspaces/firestarter && git status --porcelain` — empty, asserted before both suite runs.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=` — 0 failed, count recorded against 312.
- `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q -o addopts=` — 0 failed.
- `cd /workspaces/firestarter_app && git diff 4d18b645..HEAD -- firestarter/data/chip_database.json | wc -c` — 0.
- `cd /workspaces/.planning/phases/145-bench-validation && sha256sum -c SHA256SUMS.txt` — exits 0 over every produced binary.
- `find .planning/phases/145-bench-validation -type f -print0 | xargs -0 git check-ignore | wc -l` — 0.
- `grep -c "no v1.31 owner" 145-BENCH-LOG.md` — at least 1 per undischarged item.
- `grep -ciE "\binconclusive\b|\bpartial pass\b" 145-BENCH-LOG.md` — 0.
- `git diff --name-only HEAD~1 -- .planning/REQUIREMENTS.md .planning/ROADMAP.md` — empty; no requirement flipped here.
</verification>

<success_criteria>
- D-10 carries both halves, with the operator's words verbatim and any contradiction with the machine count stated.
- Gate 3 and the phase are both verdicted in D-14's vocabulary, with all four ROADMAP criteria answered one by one.
- Every un-taken reading is named with its blocker, and every undischarged item carries forward with `no v1.31 owner`.
- No datasheet-conformance claim, no comparative claim, and no extrapolation beyond one part, one controller and one shield revision.
- Both suites are green with the firmware porcelain empty, BENCH-03 is re-confirmed at the tip, and every artifact is tracked, hashed and not gitignored.
- No requirement checkbox moved and nothing under either sub-repo changed.
</success_criteria>

<output>
Create `.planning/phases/145-bench-validation/145-08-SUMMARY.md` when done. It MUST record: the operator's
eyes-on statement verbatim; the `Gate 3 verdict:` and `VERDICT:` sentences verbatim; the complete
carry-forward list with each item's owner stated as none; both post-bench suite counts against their
baselines; the end-of-phase BENCH-03 figures against Gate 0's; and the artifact inventory totals with the
`sha256sum -c` and `git check-ignore` results.
</output>
