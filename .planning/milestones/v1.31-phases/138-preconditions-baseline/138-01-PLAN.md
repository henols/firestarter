---
phase: 138-preconditions-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PREP-01, PREP-02]
may_tick_requirements: [PREP-01, PREP-02]
commits_land_in: "meta (/workspaces) only — plus local-only branch refs created inside the firestarter and firestarter_app submodules (no submodule commit, no push)"

must_haves:
  truths:
    - "A reader can tell from 138-BRANCH-BASES.md whether v1.30's app work is present on origin/beta, and by which four independent oracles rather than by ancestry alone"
    - "Each of the three repos has a branch named gsd/v1.31-27c-programming-algorithm-fidelity whose base commit is named by full SHA in the artifact"
    - "The firmware fork-base decision (the decided base, not the live beta tip) is stated with its rationale and the recorded cost of the omitted drift"
    - "PREP-01's wording correction from ancestry to content-equivalence is recorded in REQUIREMENTS.md, not silently worked around"
  artifacts:
    - path: ".planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md"
      provides: "Four-oracle PREP-01 adjudication, the three verified base commits, and findings F-138-01/02/03"
      min_lines: 90
      contains: "F-138-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PREP-01 and PREP-02 ticked with evidence citations plus PREP-01's wording-correction annotation"
      contains: "PREP-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "138-BRANCH-BASES.md"
      via: "Evidence citation on the PREP-01 and PREP-02 rows"
      pattern: "138-BRANCH-BASES"
    - from: "138-BRANCH-BASES.md"
      to: "git refs in both submodules"
      via: "full base SHA named per repo, re-resolvable with git rev-parse"
      pattern: "gsd/v1\\.31-27c-programming-algorithm-fidelity"
---

<objective>
Discharge PREP-01 and PREP-02: establish by measurement, not restatement, where v1.30's app work
actually landed, and put a `gsd/v1.31-27c-programming-algorithm-fidelity` branch on a **named, verified**
base commit in all three repos (per **D-09**, one identical slug everywhere).

Purpose: every later plan in this phase, and every phase in v1.31, forks from what this plan names. The
research pass falsified four branch/ancestry facts CONTEXT.md recorded as verified, so this plan's first
job is to re-measure rather than inherit. Under operator decision **OD-1**, PREP-01 is discharged as a
**content-equivalence finding**, not as a merge: app PR #44 was already squash-merged, so **D-08**'s
"agent opens PR, operator merges" is a **no-op** and a re-merge provably conflicts.

Output: `138-BRANCH-BASES.md` (the adjudication, the three base commits, three named findings), two new
local branch refs, and PREP-01/PREP-02 ticked with citations.
</objective>

## Artifacts this phase produces

Newly-created symbols across all of Phase 138. None exists in any repo yet, so none is drift.

- **Finding IDs:** `F-138-01` PREP-01 squash-merge content equivalence · `F-138-02` firmware `beta` drift
  and MERGE-05 headroom · `F-138-03` meta submodule gitlinks left stale, deliberately · `F-138-04` size
  gate GREEN at the fork base and RED at the live tip · `F-138-05` `check_size_baseline.py` unknown-env
  `KeyError` versus its own exit-2 taxonomy, plus `NATIVE_ENVS` gate-blindness · `F-138-06`
  `host_stubs_common.inc`'s stale "14 pre-existing suites" count and its false "composes with" claim ·
  `F-138-07` `gen_sdp_bus_config.py` has no 27C row and rejects `static-high` pinouts · `F-138-08`
  write-path VPP-regulator disable observed only on the failure path (conditional; recorded only if the
  captured trace shows it).
- **New firmware guard macros:** `HOST_STUBS_RECORD_TIMING`, `HOST_STUBS_CUSTOM_READ_DATA_BUFFER`,
  `HOST_STUBS_MAX_TIMINGS`, `EPROM_V131_TRACE_DUMP`.
- **New firmware enum values:** `TIMING_KIND_DELAY_US`, `TIMING_KIND_DELAY_MS`.
- **New firmware functions:** `timing_push`, `clear_timings`, `timing_count`, `timing_overflowed`,
  `timing_kind`, `timing_us`, `timing_after_strobe`, `trace_readback_reset`, `trace_readback_seed`,
  `v131_merged_length`, `v131_merged_at`, `v131_first_divergence`, `v131_assert_stream_equals`,
  `v131_snapshot`, and a new per-suite copy of `reset_register_cache`.
- **New firmware types:** `timing_entry_t`, `v131_trace_entry_t`.
- **New fixture arrays:** `EPROM_V131_TRACE_PROTO_07`, `EPROM_V131_TRACE_PROTO_08`,
  `EPROM_V131_TRACE_PROTO_0B`, plus their `_LEN` macros.
- **New firmware files:** `test/native/avr/_shared/eprom_v131_expected.h`,
  `test/native/avr/test_trace_eprom_v131/host_stubs.cpp`,
  `test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp`,
  `tests/golden/eprom_v131_trace_inventory.json`,
  `tests/test_golden_trace_identity_eprom_v131.py`, `scripts/baseline/size_baseline_v131.json`.
- **New PlatformIO env:** `native_trace_v131`.
- **New JSON keys** in `size_baseline_v131.json`: `meta.fork_base`, `meta.measured_at_tree`,
  `meta.deltas_vs_size_baseline`, `meta.frozen_for`, `native_envs.native_trace_v131`.
- **New JSON keys** in `eprom_v131_trace_inventory.json`: `meta.frozen_for`,
  `meta.measured_entry_counts`, `meta.overflow_observed`.
- **New meta files:** `138-BRANCH-BASES.md`, `138-pulse-distribution.py`,
  `138-02-PULSE-DISTRIBUTION.md`, `138-04-HOST-BASELINE.md`, `138-06-FIRMWARE-MEASUREMENT.md`,
  `138-BASELINE.md`.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<repo_topology>
This is a meta-repo. `.planning/` lives in `/workspaces` (the meta git repo). `/workspaces/firestarter`
(firmware) and `/workspaces/firestarter_app` (host CLI) are **separate git repos** mounted as submodules.

**This plan's commits land in the meta repo only.** It creates branch *refs* inside the two submodules
(a git operation, not a commit), makes **no submodule commit**, and performs **no push**. Per **OD-3** it
does **not** advance the meta repo's submodule gitlinks.
</repo_topology>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-preconditions-baseline/138-CONTEXT.md
@.planning/phases/138-preconditions-baseline/138-RESEARCH.md
@.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the four PREP-01 oracles and both live tips, then write the adjudication</name>
  <files>.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md</files>
  <read_first>
    - `.planning/phases/138-preconditions-baseline/138-RESEARCH.md` sections "Branch and Ancestry Ground
      Truth" and "Common Pitfalls" Pitfall 1 — the four oracles, their measured values, and the
      squash-merge false-negative mechanism
    - `.planning/phases/138-preconditions-baseline/138-CONTEXT.md` section "Branch bases" — D-08, D-09,
      and the four now-falsified "verified during this discussion" bullets
    - `.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md` sections 5 and 8 — the F-NN
      finding style (mechanism, required actions, "read never compute") and the rule that the measured
      number wins with both values recorded and no reconciliation
    - `.planning/REQUIREMENTS.md` section "Preconditions and Baseline" — PREP-01 verbatim
    - `.planning/v1.30-PR-BODY.md` — the staged PR body D-08 was going to open
  </read_first>
  <action>
    Re-run all four PREP-01 oracles live and read-only in `/workspaces/firestarter_app`, plus the
    live-tip resolution for both submodules. RESEARCH.md's SHAs are ~3 days old at best and both `beta`
    branches moved twice in the 36 h before research, so nothing may be copied from it — re-measure,
    then compare, and treat any divergence as a finding in its own right (per D-06's evidence
    discipline).

    **Step 0, before any oracle runs and before the live-tip read: refresh the app repo's
    remote-tracking ref.** In `/workspaces/firestarter_app` run `git fetch origin beta`. This is neither
    optional nor cosmetic: the locally cached `origin/beta` lags the live tip, so without it Task 2's
    branch creation fails with `fatal: Not a valid object name` — `git branch NAME LIVE_SHA` cannot
    resolve an object the local repo does not hold. Record the cached `origin/beta` SHA **before** the
    fetch and again after, so the staleness the fetch corrects becomes part of section 3's evidence
    rather than being erased by the fix.

    The fetch also makes oracles 3 and 4 read the live target instead of a days-old cache. Oracles 1 and
    2 are robust to staleness either way — a squash merge's ancestry false negative is monotonic and the
    unmerged-commit count moves only in one direction — but there is no reason to run them against a
    stale ref once the fetch is free.

    **The firmware repo does not need a fetch to create its branch, and this plan says so explicitly
    rather than leaving it implicit:** per OD-2 the firmware forks at `3085084`, which is already a local
    object (it is the current local `beta` tip), so `git cat-file -e 3085084` succeeds without any
    network access. Still run `git fetch origin beta` in `/workspaces/firestarter` before reading its
    drift, so the drift compare is taken against a ref this repo actually holds. If that fetch is
    unavailable, fall back to the `gh api` compare call, which needs no local object, and state in
    section 3 which route was used.

    Oracles, all read-only:
    1. GitHub PR record: `gh pr view 44 --repo henols/firestarter_app --json state,mergedAt,mergeCommit`,
       then `git log -1 --format='%h parents=[%p]' MERGE_SHA` to show a single parent, i.e. a squash.
    2. Ancestry: `git merge-base --is-ancestor gsd/v1.30-sdp-surface-retirement origin/beta` with its
       exit code captured, and `git rev-list --count origin/beta..gsd/v1.30-sdp-surface-retirement`.
    3. Content equivalence: `comm -23` over the two sorted `git ls-tree -r --name-only` listings, v1.30
       branch minus `origin/beta`. An **empty** result is the load-bearing oracle. Record the reverse
       direction too.
    4. Attributable diff: `git diff --stat gsd/v1.30-sdp-surface-retirement origin/beta`, with every
       listed file attributed to a later beta PR or to the version bump.
    Supplementary: `git cherry origin/beta gsd/v1.30-sdp-surface-retirement` piped through
    `awk '{print $1}' | sort | uniq -c` (all `+` after a squash, because patch-ids differ), and
    `git merge-tree` on the merge base filtered for lines beginning `changed in both` or `added in both`
    to show a re-merge conflicts.

    Live tips: `gh api repos/henols/firestarter/git/refs/heads/beta --jq '.object.sha'` and the same for
    `firestarter_app`. Record each live tip beside the locally cached `git rev-parse origin/beta` so
    staleness is visible, and run `gh api repos/henols/firestarter/compare/BASE...LIVE` to enumerate the
    drift commits and the files each touches.

    Write `138-BRANCH-BASES.md` with a title line, an **Owner requirement** line naming PREP-01 and
    PREP-02, then:
    - **Section 1 Oracles** — a table with columns Oracle, Command, Result; one row per oracle; each
      command quoted literally as run.
    - **Section 2 F-138-01, PREP-01 discharged as content equivalence rather than ancestry** — state the
      mechanism (a GitHub squash merge creates a commit with none of the branch commits as ancestors, so
      `git merge-base --is-ancestor` is a **false negative**), the verdict (v1.30's app content **is** on
      `beta`), the requirement wording correction from ancestry to content equivalence per OD-1, and the
      refusal to re-merge because it provably conflicts — name the file reported as *added in both*.
      State explicitly that **D-08 is a no-op**: no PR is opened, no operator merge is requested, and
      D-08's "known and accepted consequence" of a new app pre-release **already happened**. Name the
      current `beta` version string, read live from `firestarter/__init__.py` on the live tip, and the
      `Apply automatic changes` auto-commits sitting on it. Note that `.planning/v1.30-PR-BODY.md`
      remains on disk as an unused draft and is **not** deleted by this phase.
    - **Section 3 Live tips versus local caches** — the table described above for both submodules.

    Tick no requirement in this task. Create, switch, push, merge, or delete no branch in this task — it
    is measurement only.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && git fetch origin beta && LIVE=$(gh api repos/henols/firestarter_app/git/refs/heads/beta --jq '.object.sha') && git cat-file -e "$LIVE" && test -z "$(comm -23 <(git ls-tree -r --name-only gsd/v1.30-sdp-surface-retirement | sort) <(git ls-tree -r --name-only origin/beta | sort))" && gh pr view 44 --repo henols/firestarter_app --json state --jq '.state' | grep -qx MERGED && grep -c 'F-138-01' /workspaces/.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md</automated>
  </verify>
  <acceptance_criteria>
    - `138-BRANCH-BASES.md` exists and contains the literal strings `F-138-01`, `PREP-01`, `PREP-02`,
      `content equivalence`, and `D-08`.
    - Section 1's table has at least 4 oracle rows, each carrying a command string containing `git ` or
      `gh `.
    - `gh pr view 44 --repo henols/firestarter_app --json state --jq '.state'` prints `MERGED`, and that
      value appears in section 1.
    - `comm -23` over the two `git ls-tree -r --name-only` listings prints zero lines, and section 1
      records that result as empty.
    - The file states the measured exit code of `git merge-base --is-ancestor` (as measured, not as
      expected) and explains it as a squash-merge false negative.
    - `git -C /workspaces/firestarter_app fetch origin beta` was run before any oracle command, and
      after it `git -C /workspaces/firestarter_app cat-file -e LIVE_TIP_SHA` succeeds — the live tip is a
      local object, which is the precondition Task 2's branch creation depends on.
    - Section 3 records the app repo's cached `origin/beta` SHA **before** the fetch alongside its value
      after, so the staleness is evidence rather than erased.
    - The artifact states explicitly whether the firmware repo was fetched, and if it was not, that
      `3085084` is already a local object and the drift figures came from the `gh api` compare call.
    - The file names the live `beta` tip SHA for both submodules and the cached `origin/beta` SHA for
      both. If a live tip differs from `138-RESEARCH.md`'s recorded value, both values are stated with
      no reconciliation.
    - Section 2 states D-08 is a no-op and that no PR was opened; `git -C /workspaces/firestarter_app log
      --oneline -1` shows no commit produced by this task.
    - No `gh pr create`, `gh pr merge`, or `git merge` command was executed. Naming such a command inside
      the "refused because" prose is acceptable; running one is not.
  </acceptance_criteria>
  <done>The four oracles are re-measured live, F-138-01 records the mechanism and the corrected criterion, and both repos' live-versus-cached tips are named.</done>
</task>

<task type="auto">
  <name>Task 2: Create and verify the v1.31 milestone branch in all three repos</name>
  <files>.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md</files>
  <read_first>
    - `.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md` — section 3's live-tip table from
      Task 1 is this task's input
    - `.planning/phases/138-preconditions-baseline/138-RESEARCH.md` sections "v1.31 branch existence" and
      "Working-tree state" — meta's branch already exists off `d0f0c6a0` with no upstream, and the two
      v1.30-named meta branches
    - `.planning/phases/138-preconditions-baseline/138-CONTEXT.md` — D-09, one identical slug in all
      three repos
    - `/workspaces/CLAUDE.md` — the meta repo tracks only `.planning/` and `.claude/`
  </read_first>
  <action>
    Put the branch `gsd/v1.31-27c-programming-algorithm-fidelity` — the identical slug in all three
    repos, per **D-09** — on a verified base, and name every base by full SHA.

    Bases, per **OD-2**; re-verify each, assume none:
    - **meta** (`/workspaces`): the branch already exists. Verify its base is the tip of
      `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`, expected `d0f0c6a0`, and **not**
      `gsd/v1.30-sdp-surface-retirement` at `00af5771`. The meta repo carries two v1.30-named branches
      and the v1.31 branch descends from the longer-named one; this is exactly the divergence D-09
      exists to prevent. Verify with `git rev-parse` on both v1.30 branches plus
      `git merge-base --is-ancestor BASE gsd/v1.31-27c-programming-algorithm-fidelity`.
    - **firmware** (`/workspaces/firestarter`): fork at the decided base **`3085084`**, not the live beta
      tip. Re-verify `3085084` resolves and is an ancestor of the live tip. Create the branch **and check
      it out**, because Wave 2 lands firmware code on it.
    - **app** (`/workspaces/firestarter_app`): fork at the **verified live `beta` tip** read in Task 1.
      Do not hardcode any SHA from RESEARCH.md. **Before creating the branch, re-assert that the tip is a
      local object** with `git cat-file -e LIVE_TIP_SHA`; Task 1's `git fetch origin beta` is what makes
      that true, and without it `git branch` fails with `fatal: Not a valid object name`. If the check
      fails, re-run the fetch rather than falling back to the stale cached `origin/beta`. Create the ref
      only; do not check it out — plan 04 does that.

    Before switching the firmware branch, assert the worktree carries no modified tracked files:
    `git status --porcelain | grep -v '^??'` must be empty. Untracked files are expected and fine. If a
    tracked modification exists, stop and record it as a finding; never stash or discard silently.

    Verify each branch with two commands and record both outputs: `git rev-parse BRANCH` and
    `git merge-base --is-ancestor BASE BRANCH` with its exit code (expect 0).

    Append **section 4, The three verified bases**: a table with columns Repo, Branch, Base commit (full
    SHA), How verified, plus one sentence per repo. State in the firmware row why the decided base was
    kept rather than the live tip: Phase 138 exists to define "before", a base whose own size gate
    arrives RED would make every downstream TEST-08 delta measure against a broken reference, and D-07
    forbids fixing it here.

    Append **section 5, F-138-02, firmware beta drift carried forward**: name the live tip, the drift
    commits from Task 1's compare output, the uniform `flash_used` delta and the MERGE-05 headroom
    figures as **research-measured** with their provenance (`138-RESEARCH.md`, 2026-08-08, cold
    freshly-extracted tree at the live tip) — this phase re-verifies the SHA-level facts live but does
    not re-build the live tip, and says so plainly rather than presenting a re-measurement it did not
    take. Assign owners: the flash-delta reconciliation to **Phase 144 / TEST-08**, the MERGE-05 band
    headroom to **Phase 143 / 144**, escalation to **henols**. Record, do not fix (D-07).

    Append **section 6, F-138-03, submodule gitlinks deliberately not advanced**: per **OD-3** the meta
    index's gitlinks stay as they are. Read them live with `git ls-files -s firestarter firestarter_app`,
    name both gitlink SHAs and both worktree SHAs, state that the resulting `M firestarter` and
    `M firestarter_app` lines in `git status` are expected rather than dirt, cite `CLAUDE.md`'s "neither
    sub-repo is committed here", and give the finding owner `henols`.

    Push no branch. Add or commit no submodule gitlink change.
  </action>
  <verify>
    <automated>for r in /workspaces /workspaces/firestarter /workspaces/firestarter_app; do git -C $r rev-parse --verify gsd/v1.31-27c-programming-algorithm-fidelity >/dev/null || exit 1; done; git -C /workspaces/firestarter symbolic-ref --short HEAD | grep -qx gsd/v1.31-27c-programming-algorithm-fidelity && git -C /workspaces/firestarter merge-base --is-ancestor 3085084 gsd/v1.31-27c-programming-algorithm-fidelity && grep -q 'F-138-02' /workspaces/.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md && grep -q 'F-138-03' /workspaces/.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app fetch origin beta` ran before the oracles, and `git cat-file -e`
  on the live tip succeeds locally.
- `git rev-parse --verify gsd/v1.31-27c-programming-algorithm-fidelity` succeeds in all three repos.
    - `git -C /workspaces/firestarter symbolic-ref --short HEAD` prints
      `gsd/v1.31-27c-programming-algorithm-fidelity`.
    - `git -C /workspaces/firestarter merge-base --is-ancestor 3085084
      gsd/v1.31-27c-programming-algorithm-fidelity` exits 0.
    - `git -C /workspaces/firestarter_app cat-file -e` on the live tip SHA succeeded **before** the
      branch was created, and the branch creation therefore did not fail on a missing object.
    - `git -C /workspaces/firestarter_app rev-parse gsd/v1.31-27c-programming-algorithm-fidelity` equals
      the live `beta` tip SHA recorded in section 3, and
      `git -C /workspaces/firestarter_app symbolic-ref --short HEAD` still prints the pre-existing branch
      name (the app was not checked out by this task).
    - `git -C /workspaces merge-base --is-ancestor` from the recorded meta base to the meta v1.31 branch
      exits 0, and section 4 names `d0f0c6a0` as the meta base and states that `00af5771` is not it.
    - Section 4's table has exactly 3 data rows and every Base commit cell holds a 40-character SHA.
    - `138-BRANCH-BASES.md` contains `F-138-02` and `F-138-03`, and the F-138-02 text contains the word
      `research-measured` and both an owner naming a phase and the owner `henols`.
    - `git ls-files -s firestarter firestarter_app` in `/workspaces` returns the same two SHAs before and
      after this task, and both are quoted in section 6.
    - `git -C /workspaces/firestarter log --oneline -1` and the same for the app show no commit created by
      this task; `git ls-remote --heads origin 'gsd/v1.31*'` is empty in both submodules (nothing pushed).
  </acceptance_criteria>
  <done>All three repos carry the identically-named v1.31 branch on a base verified two ways and named by full SHA; F-138-02 and F-138-03 are recorded with owners.</done>
</task>

<task type="auto">
  <name>Task 3: Tick PREP-01 and PREP-02 with the wording correction recorded</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `Preconditions and Baseline` section and the Traceability table
      rows for PREP-01 and PREP-02, so the edit matches the file's existing row shape exactly
    - `.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md` — the evidence this task cites
    - `.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md` section 8 — the "measured number
      wins, both recorded, no reconciliation" rule that governs the annotation's wording
  </read_first>
  <action>
    Tick **PREP-01** and **PREP-02** only. These are the sole requirement IDs this plan may mark
    Complete; PREP-03 and PREP-04 belong to later plans and must stay `[ ]` here.

    For PREP-01: change `- [ ]` to `- [x]` and append a dated annotation, in place, that (a) states the
    requirement's original premise — "its PR was staged and never opened" — is **false as measured**,
    (b) states the corrected criterion, content equivalence rather than `git merge-base --is-ancestor`
    exit 0, per **OD-1**, (c) cites `138-BRANCH-BASES.md` section 2 / `F-138-01` and names the four
    oracles by name, and (d) records that **D-08** is therefore a no-op and no PR was opened. Keep the
    original requirement text intact above the annotation — append a correction, never rewrite the
    historical record.

    For PREP-02: change `- [ ]` to `- [x]` and append an annotation citing `138-BRANCH-BASES.md`
    section 4 with all three base SHAs, and noting per **OD-2** that the firmware base is the decided
    `3085084` rather than the live beta tip, with `F-138-02` carrying the drift forward, and per
    **OD-3** that submodule gitlinks were deliberately not advanced (`F-138-03`).

    Update the Traceability table rows for PREP-01 and PREP-02 from `Pending` to `Complete` using the
    exact status token the file's other completed rows use.

    Finally append **section 7, Hand-off**, to `138-BRANCH-BASES.md`: name what later plans consume from
    this one (the firmware v1.31 branch checked out for Wave 2, the app v1.31 ref for plan 04, the three
    base SHAs for `138-BASELINE.md`), and state plainly what this plan did **not** establish — no push,
    no CI run, no gitlink bump, no re-build of the live firmware tip.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n '^- \[x\] \*\*PREP-01\*\*' .planning/REQUIREMENTS.md && grep -n '^- \[x\] \*\*PREP-02\*\*' .planning/REQUIREMENTS.md && grep -n '^- \[ \] \*\*PREP-03\*\*' .planning/REQUIREMENTS.md && grep -n '^- \[ \] \*\*PREP-04\*\*' .planning/REQUIREMENTS.md && grep -c '138-BRANCH-BASES' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*PREP-01\*\*' .planning/REQUIREMENTS.md` is 1 and the same for PREP-02.
    - PREP-03 and PREP-04 still match `^- \[ \] \*\*PREP-0[34]\*\*` — untouched by this plan.
    - The PREP-01 annotation contains all of: `content equivalence`, `F-138-01`, `D-08`, and the word
      `no-op`; and it contains the original premise quoted or paraphrased as false-as-measured.
    - The PREP-02 annotation contains three 40-character SHAs or explicit references to section 4 plus
      the literals `F-138-02` and `F-138-03`.
    - `grep -c '138-BRANCH-BASES' .planning/REQUIREMENTS.md` is at least 2 (one citation per requirement).
    - The Traceability rows for PREP-01 and PREP-02 no longer contain `Pending`.
    - `138-BRANCH-BASES.md` ends with a Hand-off section that names at least three consumers and at
      least three explicit non-establishments.
  </acceptance_criteria>
  <done>PREP-01 and PREP-02 are ticked with cited evidence, PREP-01 carries its wording correction in place, and the artifact hands off explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API to local record | Remote branch/PR state is read into a committed artifact; a stale read becomes a false provenance claim |
| local git refs to plan artifacts | Cached `origin/beta` refs may lag the live ref by days |
| agent to remote repos | Any write (push, merge, PR) would be an outward-facing action this plan is not authorised to take |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Spoofing (of provenance) | base commit read from a cached `origin/beta` | mitigate | Resolve every base against the live ref with `gh api .../git/refs/heads/beta`; record live and cached side by side (Task 1 acceptance criteria) |
| T-138-02 | Repudiation | a single-oracle ancestry check reported as proof | mitigate | Four independent oracles required, each with its literal command recorded; the empty `comm -23` is the load-bearing one |
| T-138-03 | Elevation of privilege | an unauthorised outward action (push, PR, merge) | mitigate | Task 1 and 2 forbid `gh pr create`, `gh pr merge`, `git merge`, and any push; Task 2's acceptance criteria assert `git ls-remote --heads origin 'gsd/v1.31*'` is empty |
| T-138-04 | Tampering | silently stashing or discarding a tracked submodule modification to switch branches | mitigate | Fail closed when `git status --porcelain` piped through `grep -v` of the untracked marker is non-empty, and record it as a finding |
| T-138-041 | Denial of service (self-inflicted) | branch creation against a SHA absent from the local object store | mitigate | Task 1 fetches `origin/beta` in the app repo before any oracle runs; Task 2 re-asserts with `git cat-file -e` before `git branch`, and re-fetches rather than silently falling back to the stale cache |
| T-138-05 | Tampering (of the record) | rewriting PREP-01's original text to make the premise look correct | mitigate | Task 3 appends a dated correction and keeps the original text intact |
| T-138-SC | Tampering | package-manager installs | accept | This plan installs no npm, pip, or cargo package; no `## Package Legitimacy Audit` is required because no install task exists |
</threat_model>

<verification>
- `git rev-parse --verify gsd/v1.31-27c-programming-algorithm-fidelity` succeeds in all three repos.
- `git -C /workspaces/firestarter symbolic-ref --short HEAD` is the v1.31 branch.
- `git ls-remote --heads origin 'gsd/v1.31*'` is empty in both submodules — nothing was pushed.
- `git ls-files -s firestarter firestarter_app` in `/workspaces` is unchanged from the pre-plan value.
- `138-BRANCH-BASES.md` contains `F-138-01`, `F-138-02`, `F-138-03` and a 3-row base table.
- PREP-01 and PREP-02 are `[x]`; PREP-03 and PREP-04 remain `[ ]`.
</verification>

<success_criteria>
- PREP-01 is discharged as a content-equivalence adjudication with four re-measured oracles, and the
  requirement's stale premise is corrected in place rather than force-satisfied.
- PREP-02 is discharged with three named, twice-verified base commits under one identical branch slug.
- No push, no PR, no merge, no submodule commit, and no gitlink change occurred.
- Three findings (`F-138-01`, `F-138-02`, `F-138-03`) are recorded with owners and explicitly not fixed.
</success_criteria>

<output>
Create `.planning/phases/138-preconditions-baseline/138-01-SUMMARY.md` when done.
</output>
