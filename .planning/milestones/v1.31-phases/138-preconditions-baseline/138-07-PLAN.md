---
phase: 138-preconditions-baseline
plan: 07
type: execute
wave: 5
depends_on: [138-01, 138-02, 138-03, 138-04, 138-05, 138-06]
files_modified:
  - .planning/phases/138-preconditions-baseline/138-BASELINE.md
  - .planning/phases/138-preconditions-baseline/138-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [PREP-03, PREP-04]
may_tick_requirements: [PREP-03, PREP-04]
commits_land_in: "meta (/workspaces) only. The operator's pushes move refs in all three repos; the agent makes no submodule commit and runs no push, no dispatch and no merge."

must_haves:
  truths:
    - "One narrative artifact holds the whole pre-change baseline: branch bases, the frozen trace, per-target flash and RAM, all four native suite counts, the host suite count and the pulse distribution, each figure naming the command and the tree it came from"
    - "CI evidence per repo is recorded read-only with run ids, keying terminality on conclusion rather than outcome, and the app-versus-firmware dispatch asymmetry is stated rather than assumed away"
    - "Every finding raised anywhere in the phase appears in one register with an owner and an explicit not-fixed-here disposition"
    - "PREP-03 and PREP-04 are ticked with citations, and what the baseline does not establish is stated as plainly as what it does"
  artifacts:
    - path: ".planning/phases/138-preconditions-baseline/138-BASELINE.md"
      provides: "The nine-section phase baseline narrative and the consolidated findings register"
      min_lines: 150
      contains: "F-138-01"
    - path: ".planning/phases/138-preconditions-baseline/138-VALIDATION.md"
      provides: "The per-task verification map filled in and signed off"
      contains: "nyquist_compliant"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PREP-03 and PREP-04 ticked with evidence citations"
      contains: "PREP-03"
  key_links:
    - from: ".planning/phases/138-preconditions-baseline/138-BASELINE.md"
      to: "the five per-plan artifacts"
      via: "each figure cited to its owning artifact by filename and section"
      pattern: "138-0[0-9]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/138-preconditions-baseline/138-BASELINE.md"
      via: "Evidence citation on the PREP-03 and PREP-04 rows"
      pattern: "138-BASELINE"
---

<objective>
Close Phase 138: assemble the whole pre-change baseline into one citable narrative in
`131-CI-BASELINE.md`'s shape (**D-05**), obtain and record the operator-gated CI evidence per repo
(**D-06**), consolidate every finding the phase raised into a single owned register, and tick PREP-03 and
PREP-04 with citations.

Purpose: Phases 139 through 146 cite this document. TEST-06 diffs against the frozen trace it names,
TEST-08 compares against the size freeze it names, ISSUE-01 quotes the pulse distribution it names, and
Phase 146's honesty ledger inherits its findings register. A baseline whose figures cannot each name their
command and their tree is not a baseline.

Output: `138-BASELINE.md`, a completed `138-VALIDATION.md` map, and two ticked requirements.

**This plan is not autonomous.** It carries a blocking operator gate: pushing branches and dispatching a
workflow are outward-facing actions, and `gh workflow run` is additionally blocked by the auto-mode
classifier. The agent reads runs **read-only** and records ids; it dispatches nothing and pushes nothing.
</objective>

## Artifacts this phase produces

Newly-created symbols across all of Phase 138. None exists in any repo yet, so none is drift.

- **Finding IDs:** `F-138-01` PREP-01 squash-merge content equivalence · `F-138-02` firmware `beta` drift
  and MERGE-05 headroom · `F-138-03` meta submodule gitlinks left stale, deliberately · `F-138-04` size
  gate GREEN at the fork base and RED at the live tip · `F-138-05` `check_size_baseline.py` unknown-env
  `KeyError` versus its own exit-2 taxonomy, plus `NATIVE_ENVS` gate-blindness · `F-138-06`
  `host_stubs_common.inc`'s stale "14 pre-existing suites" count and its false "composes with" claim ·
  `F-138-07` `gen_sdp_bus_config.py` has no 27C row and rejects `static-high` pinouts · `F-138-08`
  write-path VPP-regulator disable observed only on the failure path (conditional) · `F-138-09` CI
  evidence outstanding (conditional; recorded only if the operator holds the push or dispatch).
- **New firmware guard macros:** `HOST_STUBS_RECORD_TIMING`, `HOST_STUBS_CUSTOM_READ_DATA_BUFFER`,
  `HOST_STUBS_MAX_TIMINGS`, `EPROM_V131_TRACE_DUMP`.
- **New firmware enum values:** `TIMING_KIND_DELAY_US`, `TIMING_KIND_DELAY_MS`.
- **New firmware functions:** `timing_push`, `clear_timings`, `timing_count`, `timing_overflowed`,
  `timing_kind`, `timing_us`, `timing_after_strobe`, `trace_readback_reset`, `trace_readback_seed`,
  `v131_merged_length`, `v131_merged_at`, `v131_first_divergence`, `v131_assert_stream_equals`,
  `v131_snapshot`, and a new per-suite copy of `reset_register_cache`.
- **New firmware types:** `timing_entry_t`, `v131_trace_entry_t`.
- **New fixture arrays:** `EPROM_V131_TRACE_PROTO_07`, `EPROM_V131_TRACE_PROTO_08`,
  `EPROM_V131_TRACE_PROTO_0B`, plus their `_LEN` macros.
- **New firmware files:** `test/native/avr/_shared/eprom_v131_expected.h`,
  `test/native/avr/test_trace_eprom_v131/host_stubs.cpp`,
  `test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp`,
  `tests/golden/eprom_v131_trace_inventory.json`,
  `tests/test_golden_trace_identity_eprom_v131.py`, `scripts/baseline/size_baseline_v131.json`.
- **New PlatformIO env:** `native_trace_v131`.
- **New JSON keys** in `size_baseline_v131.json`: `meta.fork_base`, `meta.measured_at_tree`,
  `meta.deltas_vs_size_baseline`, `meta.frozen_for`, `native_envs.native_trace_v131`.
- **New JSON keys** in `eprom_v131_trace_inventory.json`: `meta.frozen_for`,
  `meta.measured_entry_counts`, `meta.overflow_observed`.
- **New meta files:** `138-BRANCH-BASES.md`, `138-pulse-distribution.py`,
  `138-02-PULSE-DISTRIBUTION.md`, `138-04-HOST-BASELINE.md`, `138-06-FIRMWARE-MEASUREMENT.md`,
  `138-BASELINE.md`.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<repo_topology>
This is a meta-repo. All three files this plan writes live in the **meta** repo and are committed there.
The operator's pushes move refs in `/workspaces`, `/workspaces/firestarter` and
`/workspaces/firestarter_app`; the agent creates no submodule commit and executes no push, no
`gh workflow run` and no merge.
</repo_topology>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-preconditions-baseline/138-CONTEXT.md
@.planning/phases/138-preconditions-baseline/138-RESEARCH.md
@.planning/phases/138-preconditions-baseline/138-VALIDATION.md
@.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md
@.planning/phases/138-preconditions-baseline/138-02-PULSE-DISTRIBUTION.md
@.planning/phases/138-preconditions-baseline/138-03-TRACE-CAPTURE.md
@.planning/phases/138-preconditions-baseline/138-04-HOST-BASELINE.md
@.planning/phases/138-preconditions-baseline/138-06-FIRMWARE-MEASUREMENT.md
@.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Operator gate — push the three v1.31 branches and dispatch the app CI run</name>
  <files>none — operator action only; no file is written by this task</files>
  <read_first>
    - `.planning/phases/138-preconditions-baseline/138-RESEARCH.md` section "CI Evidence (D-06)" — the
      measured trigger table: the firmware's `build.yml` has **no** `workflow_dispatch`, the app's
      `ci.yml` does, and `paths-ignore` on both excludes markdown-only pushes
    - `.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md` sections 3 and 4 — the branch
      names, base SHAs, and the recorded `beta` tips that must not move
  </read_first>
  <what-built>
    Everything measurable locally is done and committed on the v1.31 branch in each repo:
    `/workspaces/firestarter` carries the opt-in timing recorder, the read-back opt-out, the
    `native_trace_v131` env, the trace suite, the frozen fixture with its inventory and identity gate, and
    `scripts/baseline/size_baseline_v131.json`. `/workspaces/firestarter_app` carries no new commit; its
    v1.31 branch sits exactly on the verified `beta` fork base. `/workspaces` carries the phase's planning
    artifacts.

    Every local gate is green: both pinned native envs at 141 cases across 17 suites, the trace env
    passing, the firmware python gate suite passing in place, the host suite measured at CI parity, the new
    size freeze read back green through the checker's own seam, and the pulse-distribution script green
    with its failure mode demonstrated.
  </what-built>
  <how-to-verify>
    Two outward actions are needed and only you can take them. A push is outward-facing and
    `gh workflow run` is blocked by the auto-mode classifier, so the agent has done neither.

    1. **Push the firmware branch.** In `/workspaces/firestarter`, push
       `gsd/v1.31-27c-programming-algorithm-fidelity` to `origin` with upstream tracking. This fires
       `build.yml`, whose trigger covers every branch except `beta`. There is no `workflow_dispatch` on the
       firmware workflow, so a push is the only route to a firmware CI run. The branch carries real code,
       so the markdown-only `paths-ignore` exclusion does not apply.
    2. **Push the app branch, then dispatch its CI on that branch.** In `/workspaces/firestarter_app`,
       push the same branch name, then dispatch `ci.yml` against it. The app workflow does carry
       `workflow_dispatch`, which is exactly the shape `131-CI-BASELINE.md` recorded. The branch points at
       the current `beta` tip with no new commit, so the dispatch is the reliable route to a run whose head
       SHA is exactly the fork base.
    3. **Optionally push the meta branch** for backup. The meta repo has no CI workflow, so expect no run
       id from it.

    Neither push touches `beta` in either repo, so **no pre-release is cut** by this step.

    When the runs have finished, reply with the run ids.
  </how-to-verify>
  <resume-signal>
    Reply `pushed and dispatched` with the firmware run id and the app run id, or reply `hold`. On `hold`
    the phase continues: the local cold measurements stand on their own, and the missing CI evidence is
    recorded as finding `F-138-09` with an owner and the exact push and dispatch commands that close it. No
    run id is ever fabricated and the gap is never quietly dropped.
  </resume-signal>
  <action>
    Present the summary above, stop, and wait. Take no outward action. While waiting, take none of these:
    `git push`, `gh workflow run`, `gh pr create`, `gh pr merge`, `git merge`.

    After the operator replies, record the reply verbatim in the plan summary, including a `hold` reply.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git ls-remote --heads origin gsd/v1.31-27c-programming-algorithm-fidelity > /tmp/138-07-fwref.txt; cd /workspaces/firestarter_app && git ls-remote --heads origin gsd/v1.31-27c-programming-algorithm-fidelity > /tmp/138-07-appref.txt; if [ -s /tmp/138-07-fwref.txt ] || [ -s /tmp/138-07-appref.txt ]; then echo PUSHED; else echo "HOLD path - F-138-09 required"; fi</automated>
  </verify>
  <acceptance_criteria>
    - Either both remote refs exist, or the operator explicitly replied `hold` and that reply is quoted
      verbatim in the plan summary.
    - No agent-executed `git push`, `gh workflow run`, `gh pr create`, `gh pr merge` or `git merge` appears
      anywhere in this plan's command history.
    - `git ls-remote --heads origin beta` in each submodule returns the same SHA recorded in
      `138-BRANCH-BASES.md` section 3, or the difference is recorded as a finding rather than absorbed.
  </acceptance_criteria>
  <done>The operator has either pushed and dispatched with run ids supplied, or explicitly held with the hold recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Record the CI evidence read-only and assemble the phase baseline narrative</name>
  <files>.planning/phases/138-preconditions-baseline/138-BASELINE.md</files>
  <read_first>
    - `.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md` — all 187 lines. Copy the
      nine-section skeleton and the four discipline devices: the run table with its read-only attestation
      and the explicit naming of the stale run it is *not*; the six fail-closed preconditions read from a
      single json call; the verbatim-output convention quoting only the substantive lines in order; and
      the divergence rule
    - `.planning/phases/138-preconditions-baseline/138-BRANCH-BASES.md` — sections 1 through 7
    - `.planning/phases/138-preconditions-baseline/138-03-TRACE-CAPTURE.md` — entry counts, derived bus
      configs, the freeze record, and F-138-06, F-138-07 and F-138-08
    - `.planning/phases/138-preconditions-baseline/138-04-HOST-BASELINE.md` — host counts and interpreter
      provenance
    - `.planning/phases/138-preconditions-baseline/138-06-FIRMWARE-MEASUREMENT.md` — AVR, native and
      warning figures, the gate transcripts, and F-138-04 and F-138-05
    - `.planning/phases/138-preconditions-baseline/138-02-PULSE-DISTRIBUTION.md` — the distribution
    - `.planning/phases/138-preconditions-baseline/138-RESEARCH.md` section "CI Evidence (D-06)" — the
      measured dispatch asymmetry and the fact that neither firmware gate runs in CI
  </read_first>
  <action>
    Write `138-BASELINE.md`, the phase's single citable record, in `131-CI-BASELINE.md`'s nine-section
    shape adapted to a multi-repo, multi-source baseline.

    **CI evidence, read-only.** For each run id the operator supplied, make exactly one
    `gh run view` call requesting `event`, `headBranch`, `headSha`, `conclusion`, `url` and `createdAt`,
    and record the six fail-closed preconditions as a checklist with each marked pass or fail: the id is
    numeric; the view resolves it; `event` is the expected one for that repo; `headBranch` is the v1.31
    branch; `headSha` begins with the expected SHA; and **`conclusion` is terminal and non-null**. Key on
    `conclusion`, never `outcome` — `outcome` is the pre-`continue-on-error` result and can read a
    still-running or error-suppressed run as final. Name explicitly which earlier or unrelated run this is
    **not**.

    State per repo how the run was produced: the firmware's by **push**, because `build.yml` has no
    `workflow_dispatch`; the app's by **dispatch**, because `ci.yml` has one. Do not imply a symmetry that
    does not exist. State plainly that **neither firmware gate runs in CI** — `check_size_baseline.py` and
    `check_build_warnings.py` are local-run obligations — so a CI run cannot substitute for the cold local
    measurement. If the operator held, write `F-138-09` in place of the run tables: the CI leg is
    outstanding, with an owner, the exact push and dispatch commands that close it, and an explicit
    statement that no run id was fabricated.

    **The nine sections:**
    1. **The runs** — one field-and-value table per repo, plus the attestation that the operator took every
      outward action and that every command in this document is a read-only `gh run view` or `gh api` call.
    2. **Fail-closed preconditions** — the six-item checklist per run, each from one json call.
    3. **Branch bases** — the three verified base commits cited to `138-BRANCH-BASES.md` section 4, and the
      PREP-01 content-equivalence verdict in one paragraph citing F-138-01.
    4. **The frozen trace** — the fixture path, its committed blob SHA, the three array names with entry
      counts, the measured strobe, timing and merged counts per protocol, the recorder headroom, and the
      two mechanisms that pin it. Name Phase 144 / TEST-06 as the intended consumer and state that a future
      divergence is expected work, not a regression.
    5. **Size and suite counts** — the AVR table, the four-env native table, the warning watermarks with
      their build state, and the verbatim gate verdicts, cited to `138-06-FIRMWARE-MEASUREMENT.md`; plus
      the host suite counts with interpreter and tree, cited to `138-04-HOST-BASELINE.md`.
    6. **The pulse distribution** — the three per-protocol headline figures and the whole-database
      partition, cited to `138-02-PULSE-DISTRIBUTION.md`, with one sentence naming Phase 139 / ISSUE-01 as
      the consumer.
    7. **Findings register** — one table over every finding the phase raised: id, one-line mechanism,
      owner, disposition. Every disposition is *recorded, not fixed*. Cover F-138-01 through F-138-07,
      F-138-08's disposition (recorded, or not applicable with the reason), and F-138-09 if the operator
      held.
    8. **Divergence check against research** — a table of this phase's measured figures beside
      `138-RESEARCH.md`'s recorded figures for the branch tips, AVR sizes, native counts, host counts and
      the pulse distribution. State the rule explicitly: where they differ, **the measured number wins and
      both are recorded without reconciliation**. Adjust neither side.
    9. **What this baseline is — and is not** — it is the pre-change input Phases 139, 144 and 146 cite. It
      is **not** a claim that CI is green, **not** a claim of datasheet conformance, **not** a repair of any
      gate, **not** a measurement of any tree other than the SHAs it names, and **not** evidence about
      silicon behaviour. Add a short "not established" list: nothing about bench hardware, nothing about
      the new cadence (Phase 144 authors that trace), and nothing about the live firmware beta tip beyond
      what F-138-02 and F-138-04 carry with their provenance explicitly labelled research-measured.

    Close with a footer in the analog's form naming the phase, the plan and task, and the date.

    Every figure must cite its owning artifact by filename and section. A figure that cannot cite one does
    not belong in this document.
  </action>
  <verify>
    <automated>F=.planning/phases/138-preconditions-baseline/138-BASELINE.md && test -f $F && for id in F-138-01 F-138-02 F-138-03 F-138-04 F-138-05 F-138-06 F-138-07; do grep -q "$id" $F || exit 1; done && grep -q conclusion $F && grep -qi 'is not' $F && grep -c '138-BRANCH-BASES\|138-02-PULSE-DISTRIBUTION\|138-03-TRACE-CAPTURE\|138-04-HOST-BASELINE\|138-06-FIRMWARE-MEASUREMENT' $F</automated>
  </verify>
  <acceptance_criteria>
    - `138-BASELINE.md` contains all nine numbered sections in order.
    - It contains every finding id from `F-138-01` through `F-138-07`, each in the section-7 register with
      an owner and a disposition, and every disposition reads as recorded rather than fixed.
    - The CI section either carries a run table per repo with a non-null `conclusion` and the six-item
      precondition checklist, or carries `F-138-09` with an owner and the exact closing commands. It never
      carries an invented run id.
    - The word `outcome` appears only in the sentence explaining why `conclusion` is keyed on instead.
    - The document states that the firmware run was push-produced and the app run dispatch-produced, and
      that neither firmware gate runs in CI.
    - Every one of the five per-plan artifacts is cited by filename at least once.
    - Section 8's divergence table has one row per figure class and states the measured-number-wins rule
      verbatim; no figure has been altered to make two sources agree.
    - Section 9 explicitly denies a CI-green claim, a datasheet-conformance claim and a silicon claim, and
      the "not established" list has at least three entries.
    - No figure appears without a citation to its owning artifact.
  </acceptance_criteria>
  <done>One document holds the whole pre-change baseline, every figure cited, CI evidence recorded read-only or its absence owned, and the limits of the claim stated.</done>
</task>

<task type="auto">
  <name>Task 3: Tick PREP-03 and PREP-04, complete the validation map, and hand off</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/138-preconditions-baseline/138-VALIDATION.md, .planning/phases/138-preconditions-baseline/138-BASELINE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the `Preconditions and Baseline` section and the Traceability table, so
      the edit matches the existing row shape; PREP-01 and PREP-02 were already ticked by plan 138-01
    - `.planning/phases/138-preconditions-baseline/138-VALIDATION.md` — the front matter flags, the
      per-task verification map with its TBD columns, the Wave 0 checklist and the sign-off block
    - `.planning/ROADMAP.md` section "Phase 138: Preconditions and Baseline" — the four success criteria
      each tick must be defensible against
    - `.planning/phases/138-preconditions-baseline/138-BASELINE.md` — the evidence being cited
  </read_first>
  <action>
    Tick **PREP-03** and **PREP-04**, and only those two. PREP-01 and PREP-02 were ticked by plan 138-01
    and must be left as they are.

    For **PREP-03**, append a dated annotation citing `138-BASELINE.md` sections 4, 5 and the specific
    per-plan artifacts: the frozen golden trace with its blob SHA and array counts, the per-target flash and
    RAM figures, the four native env counts and the host suite count with its interpreter. State that the
    measurement was taken on the instrumented tree rather than the fork base, name both SHAs, and state
    why that ordering is deliberate — AVR builds exclude `test/`, so a green size gate on the instrumented
    tree is positive evidence that the instrumentation is AVR-invisible, and the pinned envs re-reading 141
    cases across 17 suites is the flag-off proof.

    For **PREP-04**, append a dated annotation citing `138-02-PULSE-DISTRIBUTION.md` and the committed
    script by path, naming the three per-protocol figures and the whole-database partition, and stating
    that the parser was imported rather than reimplemented and that the script was observed failing on a
    planted input before its pass was trusted.

    Update the Traceability rows for PREP-03 and PREP-04 from `Pending` to the same completed token the
    file's other completed rows use.

    Then complete `138-VALIDATION.md`:
    - fill the per-task verification map's Task ID, Plan, Wave and Status columns from the plans as
      executed, keeping the requirement-to-command mapping the research fixed, and mark each row green or
      red from its actual observed result rather than from its plan text
    - tick every Wave 0 checklist item that was delivered and leave un-delivered ones unticked with a note
      rather than ticking them optimistically
    - complete the sign-off block and set `nyquist_compliant: true` and `wave_0_complete: true` in the
      front matter **only if** every map row is green; if any row is red, leave the flags false, say which
      row and why, and record it as a finding in `138-BASELINE.md` section 7

    Finally append a **Hand-off** section to `138-BASELINE.md` naming, per downstream phase, exactly what
    it consumes from here: Phase 139 / ISSUE-01 the pulse distribution artifact; Phase 143 and 144 the
    MERGE-05 headroom warning in F-138-02; Phase 144 / TEST-06 the frozen fixture path, blob SHA and its
    identity gate; Phase 144 / TEST-08 `scripts/baseline/size_baseline_v131.json` and the seam it is read
    through; Phase 146 the findings register. Also state the three branch names and bases, and the fact
    that the app worktree is left on its v1.31 branch.
  </action>
  <verify>
    <automated>cd /workspaces && grep -qE '^- \[x\] \*\*PREP-03\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*PREP-04\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*PREP-01\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*PREP-02\*\*' .planning/REQUIREMENTS.md && grep -c '138-BASELINE' .planning/REQUIREMENTS.md && grep -q 'Hand-off' .planning/phases/138-preconditions-baseline/138-BASELINE.md && grep -qE 'nyquist_compliant: (true|false)' .planning/phases/138-preconditions-baseline/138-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All four of PREP-01, PREP-02, PREP-03 and PREP-04 match the ticked pattern in
      `.planning/REQUIREMENTS.md`, and no requirement outside this phase changed state.
    - The PREP-03 annotation cites `138-BASELINE.md` and names both the fork base SHA and the measured tree
      SHA, and explains why they differ.
    - The PREP-04 annotation cites `138-02-PULSE-DISTRIBUTION.md` and the script path, and states both the
      imported-parser fact and the observed-failure fact.
    - The Traceability rows for PREP-03 and PREP-04 no longer contain `Pending`.
    - `138-VALIDATION.md`'s per-task map has no remaining `TBD` in the Task ID, Plan or Wave columns, and
      every Status cell is a measured green or red, never left pending.
    - `nyquist_compliant` is `true` only if every map row is green; otherwise it is `false` with the red
      row named in the file and mirrored into `138-BASELINE.md` section 7.
    - `138-BASELINE.md` ends with a Hand-off section naming at least five downstream consumers, each with
      the exact artifact path or identifier it consumes.
    - `git -C /workspaces/firestarter status --porcelain` and the same for the app show no uncommitted work
      left behind by the phase.
  </acceptance_criteria>
  <done>PREP-03 and PREP-04 are ticked with cited evidence, the validation map reflects measured results rather than intentions, and every downstream consumer knows exactly what to cite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent to remote repos | Pushes, dispatches, merges and issue posts are outward-facing operator actions |
| CI run record to baseline claim | A run id read from the wrong run, or a non-terminal run, would be recorded as evidence |
| per-plan artifacts to the consolidated narrative | Figures are copied between documents and can drift or lose their provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-34 | Elevation of privilege | an agent-executed push, dispatch or merge | mitigate | Task 1 is a blocking human-action gate; the agent's own acceptance criteria assert no such command was executed, and the gate is not auto-approvable |
| T-138-35 | Spoofing (of provenance) | a stale or unrelated CI run recorded as this phase's evidence | mitigate | Six fail-closed preconditions from one json call per run, including a head-SHA prefix match, plus an explicit statement naming which run this is not |
| T-138-36 | Repudiation | a non-terminal run recorded as final by keying on `outcome` | mitigate | Terminality keys on `conclusion` and asserts it is non-null; `outcome` appears only in the sentence explaining the distinction |
| T-138-37 | Tampering (of the record) | inventing a run id when the operator holds | mitigate | The hold path writes F-138-09 with an owner and the exact closing commands; an acceptance criterion forbids an invented id |
| T-138-38 | Tampering | quietly reconciling a divergence between measured and researched figures | mitigate | Section 8 states the measured-number-wins rule and records both values; no figure may be altered to make two sources agree |
| T-138-39 | Repudiation | ticking a requirement whose evidence is not citable | mitigate | Each tick carries a dated annotation citing a named artifact and section; the validation map must show measured results, not plan text |
| T-138-40 | Elevation of privilege | a push accidentally targeting `beta` and cutting a pre-release | mitigate | The gate names the branch explicitly and the acceptance criteria re-check both `beta` refs against the recorded SHAs |
| T-138-SC | Tampering | package-manager installs | accept | No npm, pip, or cargo install occurs anywhere in this plan, so no `## Package Legitimacy Audit` is required |
</threat_model>

<verification>
- Either both submodules show the v1.31 branch on `origin`, or the operator's `hold` is quoted and
  F-138-09 is recorded.
- Both `beta` refs are unchanged from the SHAs recorded in `138-BRANCH-BASES.md`.
- `138-BASELINE.md` carries all nine sections, every finding id, and a citation for every figure.
- `138-VALIDATION.md`'s map is complete with measured statuses and its flags set consistently with them.
- All four PREP requirements are ticked; nothing outside the phase changed state.
- No agent-executed push, dispatch, PR or merge appears in the command history.
</verification>

<success_criteria>
- The whole pre-change baseline is readable from one document whose every figure names its command, its
  tree and its owning artifact.
- CI evidence is recorded read-only per repo with the dispatch asymmetry stated, or its absence is an owned
  finding rather than a silent gap.
- Every finding raised across the phase lives in one register with an owner and a not-fixed disposition.
- PREP-03 and PREP-04 are ticked with citations, and the document is as clear about what it does not
  establish as about what it does.
</success_criteria>

<output>
Create `.planning/phases/138-preconditions-baseline/138-07-SUMMARY.md` when done.
</output>
