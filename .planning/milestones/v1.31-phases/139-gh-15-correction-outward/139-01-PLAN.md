---
phase: 139-gh-15-correction-outward
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: .
files_modified:
  - .planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md
  - .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
autonomous: true
requirements: [ISSUE-01, ISSUE-02]
must_haves:
  truths:
    - "gh#15's acceptance-criteria block is captured verbatim from the live issue body, and the capture carries exactly NINE `- [ ]` boxes (D-03 maps item-for-item onto them; CONTEXT.md and ROADMAP both say seven, and both are wrong)"
    - "Every citation the comment will carry has been fetched at a pinned commit SHA, sliced to its pinned line range, and matched against expected content — not merely confirmed to return HTTP 200 (D-07)"
    - "Every source anchor records its ENCLOSING FUNCTION, so no reader following a link lands somewhere the comment did not mean (eprom.cpp:283 resolves and is the erase pulse)"
    - "The D-10 'before implementation' baseline is recorded as three-ref blob equality on eprom.cpp and memory.cpp, not as a git-status inference"
    - "gh#15's before-state (state, comment count, labels, updatedAt, body byte length) is recorded so any later change is provable"
  artifacts:
    - path: ".planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md"
      provides: "gh#15's original acceptance-criteria block, captured verbatim from the live body — the artifact D-01's `<details>` block and D-10's read-back assertion both consume"
      contains: "## Acceptance criteria"
    - path: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      provides: "The evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, and the D-10 blob baseline"
      min_lines: 60
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      to: "https://raw.githubusercontent.com/henols/firestarter/<pinned-sha>/src/proms/memory.cpp"
      via: "a content-verified anchor row for the PROGRAM pulse, replacing the erase-pulse anchor CONTEXT.md D-06 recorded"
      pattern: "memory\\.cpp"
    - from: ".planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md"
      to: "https://github.com/henols/firestarter_prom/issues/15"
      via: "a mechanical `gh issue view --json body` capture, never a hand transcription"
      pattern: "^- \\[ \\]"
  prohibitions:
    - "MUST NOT post, comment on, edit, label, close or otherwise mutate gh#15 — every `gh` call in this plan is read-only (`gh issue view`, `gh api repos/.../commits/...`)"
    - "MUST NOT run `git push` — no agent push anywhere in Phase 139 (F-06); the phase is designed so no push is needed"
    - "MUST NOT create or modify any file under `firestarter/` or `firestarter_app/` — both are read-only citation sources"
    - "MUST NOT record an anchor as verified on the strength of an HTTP status code alone"
    - "MUST NOT use `git submodule status` in any verification step — it fails unconditionally in this repo"
    - "MUST NOT tick ISSUE-01, ISSUE-02 or ISSUE-03 in REQUIREMENTS.md — this plan ticks NOTHING"
    - "MUST NOT edit `139-CONTEXT.md` or `139-RESEARCH.md` to reconcile a divergence — record the correction in `139-CITATIONS.md` and cite it"
---

<objective>
Build the evidence floor the gh#15 correction stands on: capture gh#15's original acceptance-criteria
block verbatim, and resolve every citation the comment will carry by CONTENT at a pinned commit SHA.

**This plan, and this phase, must NOT be executed under `--auto`/`--chain`.** Plan 139-05 below carries
a `checkpoint:human-action` gate on an irreversible public act. The `checkpoint:human-action` type is
never auto-approved by this project's own execution machinery — but the correct operational answer is:
do not pass either flag for this phase at all.

Purpose: a correction whose evidence 404s, or whose line anchor points at a different function than the
claim, reads as fabricated. That failure is worse than not posting at all. This plan makes every anchor
checkable before a word of the comment is drafted.

Output: `139-GH15-ORIGINAL-CRITERIA.md` (the nine boxes, verbatim) and `139-CITATIONS.md` (the evidence
register).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md
@.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md
@.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md
@.planning/phases/139-gh-15-correction-outward/139-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture gh#15's nine acceptance boxes verbatim from the live body</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` §"gh#15 Live State (read 2026-08-09)" — in particular the fenced block titled "The nine acceptance-criteria boxes, verbatim", which this task's capture is diffed against, and the two non-checkbox body directives named immediately below it.
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §6 (`139-GH15-ORIGINAL-CRITERIA.md` — verbatim preservation) and §S-1 (record-artifact file conventions, including the deliberate no-frontmatter/no-header exception this file falls under).
    - `/workspaces/CLAUDE.md` — repository structure; this repo tracks only `.planning/` and `.claude/`.
  </read_first>
  <action>
Capture, do not transcribe. Run `gh issue view 15 --repo henols/firestarter_prom --json body -q .body`
and pipe it through `awk '/^## Acceptance criteria/,0'`, writing the result to
`.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md`. This file is a raw
capture: no YAML frontmatter, no `#` title, no footer, no editorial line of any kind. It is the
`<details>` payload D-01's body amendment embeds and the target of D-10's post-edit read-back
assertion, so any added byte is corruption.

Then prove the capture is faithful and prove the count RESEARCH corrected:

1. `grep -c '^- \[ \]'` on the captured file MUST print `9`. RESEARCH finding F-01 is that gh#15
   carries NINE boxes; `139-CONTEXT.md` §canonical_refs and `.planning/ROADMAP.md` both say seven and
   both are wrong. If the live count is anything other than 9, STOP and record the divergence rather
   than proceeding — the box count is load-bearing for D-03's item-for-item mapping.
2. Diff the captured `- [ ]` lines against the nine lines inside the fenced block in
   `139-RESEARCH.md` §"The nine acceptance-criteria boxes, verbatim". They must match exactly. gh#15's
   `updatedAt == createdAt` (never edited since 2026-07-12T09:15:27Z) makes this diff a free,
   independent corroboration that the body has not changed since research ran.
3. Assert nothing has been posted: `gh issue view 15 --repo henols/firestarter_prom --json comments
   -q '.comments | length'` MUST print `0`. Record the value.

Every `gh` call in this task is read-only. Do not pass `--body`, `--body-file`, `--add-label`,
`--remove-label`, `--edit-last` or any other mutating flag to any `gh issue` subcommand, and do not
invoke `gh issue comment`, `gh issue edit` or `gh issue close` at any point.

Commit with an explicit path list (`git add <the one file> && git commit`), never `git add -A` and
never `git add .` — the working tree carries unrelated dirt (` M .gitignore`, ` M firestarter`,
` M firestarter_app`, untracked `.claude/`, `package.json`, `package-lock.json`) that must not travel.
Record any divergence from RESEARCH's recorded figures in the commit body and in the SUMMARY; do not
edit `139-RESEARCH.md` or `139-CONTEXT.md` to reconcile it.
  </action>
  <acceptance_criteria>
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` exists and its first line is `## Acceptance criteria`.
    - `grep -c '^- \[ \]' 139-GH15-ORIGINAL-CRITERIA.md` prints exactly `9`.
    - The file contains no `---` YAML delimiter on line 1 and no `# ` H1 heading anywhere: `head -1` is not `---`, and `grep -c '^# ' ` prints `0`.
    - The nine captured checkbox lines are byte-identical to the nine inside `139-RESEARCH.md` §"The nine acceptance-criteria boxes, verbatim" (after stripping that block's `> ` blockquote prefix). This is re-derived independently by this task's second automated verify leg, not self-reported: `diff <(grep '^- \[ \]' 139-GH15-ORIGINAL-CRITERIA.md) <(sed -n 's/^> \(- \[ \] .*\)$/\1/p' 139-RESEARCH.md)` exits 0 with empty output. Reproduce that command and its empty output in the SUMMARY.
    - `gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length'` printed `0`, and that value is recorded in the SUMMARY.
    - `git status --porcelain -- firestarter firestarter_app` shows at most the two pre-existing stale-gitlink ` M ` entries and nothing else; no file inside either sub-repo was written.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -c '^- \[ \]' .planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md)" = "9" && head -1 .planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md | grep -qF '## Acceptance criteria' && test "$(grep -c '^# ' .planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md)" = "0" && test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "0" && echo OK-T1</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && diff <(grep '^- \[ \]' 139-GH15-ORIGINAL-CRITERIA.md) <(sed -n 's/^> \(- \[ \] .*\)$/\1/p' 139-RESEARCH.md) && echo RESEARCH-DIFF-EMPTY-OK</automated>
  </verify>
  <done>gh#15's original acceptance-criteria block is captured verbatim, proven to carry nine boxes, proven identical to RESEARCH's independently-read copy, and committed — with gh#15 still at zero comments.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve every citation by content at a pinned SHA and write the evidence register</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` §"Citation Register — every citation resolved live" (the pinning table and all 14 resolution rows), §"F-02" (the corrected C3 anchor set), §"F-05" (the `verify_anchor` recipe and the PROJECT.md line-shift), §"F-06" (why `138-BASELINE.md` is dropped), §"F-09" (the three-ref blob check), §"Common Pitfalls" 1 and 2, and §"Code Examples" (the literal `verify_anchor` function and the two commit-resolution calls).
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §5 (`139-CITATIONS.md` — the evidence-register shape: the `138-BRANCH-BASES.md` header preamble, the three-column Oracles table plus the fourth column RESEARCH demands, the outward-act custody record, and the correction-by-appending convention) and §S-1, §S-4, §S-7.
    - `.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md` §canonical_refs "Evidence being cited" — the list this task deliberately diverges from in two named places.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` (Task 1's output).
  </read_first>
  <action>
Create `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` — the evidence register. Follow
`139-PATTERNS.md` §5a's header shape: `# Phase 139 — Citation Register`, then bolded metadata lines
(`**Owner requirement:**` ISSUE-01 / ISSUE-02, `**Measured:**` today's date, this session, live and
read-only), then a divergence clause stating that every command below was re-run fresh rather than
copied from `139-RESEARCH.md`, and that any divergence from research's recorded figures is stated
explicitly rather than reconciled. No YAML frontmatter.

Structure it in four numbered sections.

**§0 — gh#15 before-state.** Record `state`, comment count, `labels`, `updatedAt` and body byte length,
each with the literal command as run. Expected at time of writing: `OPEN`, `0`, `[]`,
`2026-07-12T09:15:27Z`, 5964 bytes — `updatedAt == createdAt` means the body has never been edited.
Record what you actually measure; if it differs, that is a finding, not a number to smooth over.

**§1 — Pinning strategy.** Pin to commit SHAs, never branch names (D-07). Three rows: meta
`henols/firestarter_prom` at the currently pushed tip `b6aa1dcb23ef9931105752ed6dd6badccf6719de`;
firmware `henols/firestarter` at `origin/beta` = `6fab4eafdcd0981d24fddc3ff177abc5c74e313c`; host
`henols/firestarter_app` at `origin/beta` = `4d18b645ab18a2d2465f0f623062e9249eb24132`. Re-derive each
with `git ls-remote --heads origin` / `git -C <repo> rev-parse origin/beta` rather than copying, and
state that all cited sub-repo blobs are byte-identical at `origin/beta` and at the milestone branch tip.
Record all three repos are PUBLIC (`gh repo view <repo> --json visibility`).

State two deliberate divergences from `139-CONTEXT.md` §canonical_refs, each with its reason, per the
project's append-a-correction-in-a-later-document convention:
- `138-BASELINE.md` is **DROPPED** from the citation set. It 404s at the pushed tip (it landed in the
  unpushed `baa131a3`), it appears nowhere in D-06's binding citation list, and everything it would
  prove is already in `138-02-PULSE-DISTRIBUTION.md`, which resolves. Dropping it means the phase needs
  no `git push`, and its only outward act becomes the post itself.
- `.planning/PROJECT.md` is **NOT cited publicly.** Its C1/C2/C3 table sits at lines 61-63 at the pushed
  tip and at lines 71-73 in the working tree; a permalink written from local line numbers returns HTTP
  200 and points at the wrong ten lines. PROJECT.md is the *source* of the comment's substance, not one
  of its citations, and D-06's binding list does not include it.

**§2 — Anchor verification table.** One row per anchor, four columns: numbered anchor · the literal
command as run · the sliced text actually returned · the enclosing function or section. Verify by
CONTENT: fetch the raw file at the pinned SHA, slice the pinned line range with `awk 'NR>=a && NR<=b'`,
and `grep -qF` for an expected substring. Use the `verify_anchor` shell function reproduced literally in
`139-RESEARCH.md` §"Code Examples"; an HTTP 200 is not verification and must never be recorded as one.
The anchors, with their expected substrings:
- `firestarter_app/doc/infoic-field-dictionary.md` 210-217 → `50000 µs (×100 wrong)` (C1's adjudication;
  L217 carries the BUG-2 / DEC-03 note naming 252 chips and the Phase 57 deferral)
- `firestarter_app/firestarter/database.py` 128-128 → `def _parse_pulse_duration`
- `firestarter/src/proms/eprom.cpp` 20-20 → `#define NUMBER_OF_RETRIES 20`
- `firestarter/src/proms/eprom.cpp` 69-77 → `case 0x0B: handle->pulse_delay = 500;` — note in the row
  that this range shows the firmware ALREADY defaults `0x0B` to 500 µs, independent corroboration of C1
  from inside the code gh#15 asks to change
- `firestarter/src/proms/eprom.cpp` 177-177 → `org_delay * retries / NUMBER_OF_RETRIES`
- `firestarter/src/proms/memory.cpp` 249-258 → `delayMicroseconds(handle->pulse_delay);` — enclosing
  function `memory_set_data()`, and this is **the program pulse**
- `firestarter/src/proms/eprom.cpp` 274-283 → `delayMicroseconds(handle->pulse_delay);` — enclosing
  function `eprom_internal_erase()`, and this is **the erase pulse**, recorded explicitly so the two are
  never confused again. `139-CONTEXT.md` D-06 lists `eprom.cpp:283` as C3's "the pulse" anchor; that is
  wrong and this register is where the correction is stated.
- `.planning/phases/138-preconditions-baseline/138-02-PULSE-DISTRIBUTION.md` 237-249 → `n = 170`, and
  separately confirm the `n = 127` and `n = 32` lines fall inside the same slice
- `.planning/phases/138-preconditions-baseline/138-pulse-distribution.py` — existence at the pinned meta
  SHA plus a content check on a distinctive line

Then the two C1 commits, resolved via `gh api repos/henols/firestarter_app/commits/<sha>` with
`--jq '.sha[0:7] + " | " + (.commit.message | split("\n")[0])'`:
`8de307f278370c07bfd3328aa9020248e66d0649` (expected subject: `feat(57-01): remove interpret_timing x100
multiplier and fix bare excepts (DEC-03)`) and `12286df86abaf02be1d8e719818b7ca76a4c00e9` (expected
subject: `feat(57-03): regenerate chip_database.json from corrected build_db.py`). Confirm each is
reachable from `origin/beta` with `git -C /workspaces/firestarter_app branch -r --contains <sha>`.

Then minipro, which lives on **GitLab**, not GitHub: `https://gitlab.com/DavidGriffith/minipro`, pinned
at `cae74c0607077d6260b24995f5e4c0d0b66a6a2e`. Fetch the single raw file
`/-/raw/cae74c06.../src/t48.c` and verify by content that line 255 is `msg[1] = device->protocol_id;`
and lines 266-267 carry `format_int(&(msg[12]), device->pulse_delay, 2,` — the two lines that carry the
whole "two orthogonal wire fields" claim, inside `t48_begin_transaction()` which opens at line 246. Also
verify `src/main.c` line 698 carries `Available write pulse[us]: 1-65535`, which is the source of the
65535 µs ceiling claim. If gitlab.com is unreachable from this environment, do NOT invent a result:
mark those rows `[CITED, not re-verified this session]`, reproduce the line contents recorded in
`139-RESEARCH.md` §"minipro — repo, SHA and the load-bearing lines", and say plainly in the row that the
verification was attempted and why it did not run.

**§3 — D-10 baseline: the correction precedes implementation.** Record the three-ref blob-equality
proof, not a `git status` inference. For `src/proms/eprom.cpp` and `src/proms/memory.cpp`, print the
blob at `HEAD`, at `origin/beta`, and at `3085084` (the Phase 138 frozen firmware base named in
PROJECT.md) with `git -C /workspaces/firestarter rev-parse <ref>:<path>`, and assert `sort -u | wc -l`
is `1`. Expected: `eprom.cpp` = `8dfa4cced460416fc1b0f73cf3f0e6f77965f962` at all three;
`memory.cpp` = `478fa1d35fed2abbefb27d4d2c54dcec02086687` (at `HEAD` and `origin/beta`; record whatever
`3085084` gives). Explain in prose why the meta repo's ` M firestarter` line is a stale gitlink pointer
and not an edit, and state that `git submodule status` is never used here because it fails
unconditionally in this repo on `.planning/v1.7/upstream-rurp`.

Close the file with the S-1 italic provenance footer naming the phase, the plan, and the three pinned
SHAs. Commit with an explicit path list.
  </action>
  <acceptance_criteria>
    - `139-CITATIONS.md` exists, carries no YAML frontmatter, and has the four numbered sections `## 0.`, `## 1.`, `## 2.`, `## 3.`.
    - Every anchor row in §2 carries all four columns populated, including the sliced text actually returned and the enclosing function/section — no row records only a status code.
    - The `verify_anchor` loop over all nine raw anchors exits 0 with every line printing `OK`, and the transcript is pasted into §2.
    - `grep -qF 'memory.cpp' 139-CITATIONS.md` succeeds, and the `memory.cpp` 249-258 row names `memory_set_data()` as the enclosing function and labels it the PROGRAM pulse.
    - The `eprom.cpp` 274-283 row names `eprom_internal_erase()` and labels it the ERASE pulse, and states in words that `139-CONTEXT.md` D-06's "the pulse" attribution for `eprom.cpp:283` is corrected here.
    - Both C1 commit rows print the expected 7-char SHA and first-line subject, and both record `branch -r --contains` showing `origin/beta`.
    - §1 states in words that `138-BASELINE.md` is dropped and that `PROJECT.md` is not cited publicly, each with its reason.
    - §3 records exactly one unique blob SHA across the three refs for `eprom.cpp`, with the literal `rev-parse` commands and their outputs.
    - `139-CONTEXT.md` and `139-RESEARCH.md` are byte-unchanged: `git status --porcelain -- .planning/phases/139-gh-15-correction-outward/139-CONTEXT.md .planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` is empty.
    - No file under `firestarter/` or `firestarter_app/` was written.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && C=.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md && test -f "$C" && head -1 "$C" | grep -qv '^---$' && grep -q '^## 0\.' "$C" && grep -q '^## 1\.' "$C" && grep -q '^## 2\.' "$C" && grep -q '^## 3\.' "$C" && grep -qF 'memory_set_data' "$C" && grep -qF 'eprom_internal_erase' "$C" && grep -qF '8de307f' "$C" && grep -qF '12286df' "$C" && grep -qF '138-02-PULSE-DISTRIBUTION.md' "$C" && test -z "$(git status --porcelain -- .planning/phases/139-gh-15-correction-outward/139-CONTEXT.md .planning/phases/139-gh-15-correction-outward/139-RESEARCH.md)" && echo OK-T2-STRUCT</automated>
    <automated>cd /workspaces && test "$(for r in HEAD origin/beta 3085084; do git -C /workspaces/firestarter rev-parse $r:src/proms/eprom.cpp; done | sort -u | wc -l)" = "1" && echo OK-T2-BLOB</automated>
    <automated>cd /workspaces && verify_anchor() { out=$(curl -sf "$1") || { echo "FAIL(fetch) $1"; return 1; }; printf '%s\n' "$out" | awk -v a="$2" -v b="$3" 'NR>=a && NR<=b' | grep -qF -- "$4" && echo "OK   $1#L$2-L$3" || { echo "FAIL(anchor) $1#L$2-L$3"; return 1; }; }; APP=https://raw.githubusercontent.com/henols/firestarter_app/4d18b645ab18a2d2465f0f623062e9249eb24132; FW=https://raw.githubusercontent.com/henols/firestarter/6fab4eafdcd0981d24fddc3ff177abc5c74e313c; META=https://raw.githubusercontent.com/henols/firestarter_prom/b6aa1dcb23ef9931105752ed6dd6badccf6719de; verify_anchor "$APP/doc/infoic-field-dictionary.md" 210 217 '50000 µs (×100 wrong)' && verify_anchor "$APP/firestarter/database.py" 128 128 'def _parse_pulse_duration' && verify_anchor "$FW/src/proms/eprom.cpp" 20 20 '#define NUMBER_OF_RETRIES 20' && verify_anchor "$FW/src/proms/eprom.cpp" 69 77 'case 0x0B: handle->pulse_delay = 500;' && verify_anchor "$FW/src/proms/eprom.cpp" 177 177 'org_delay * retries / NUMBER_OF_RETRIES' && verify_anchor "$FW/src/proms/memory.cpp" 249 258 'delayMicroseconds(handle->pulse_delay);' && verify_anchor "$META/.planning/phases/138-preconditions-baseline/138-02-PULSE-DISTRIBUTION.md" 237 249 'n = 170' && echo OK-T2-ANCHORS</automated>
  </verify>
  <done>`139-CITATIONS.md` records a content-verified transcript for every citation the comment will carry, names the corrected `memory.cpp` program-pulse anchor and the `eprom.cpp` erase-pulse anchor separately, records the two dropped/withheld citations with reasons, and pins the D-10 blob baseline at three refs.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Purpose |
|---|---|---|
| `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` | **139-01** | gh#15's nine acceptance boxes captured verbatim from the live body |
| `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` | **139-01** (created); 139-04 appends §4/§5; 139-05 appends §6 | evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, D-10 blob baseline, claim-gate runs, freeze values, delivery record |
| `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` | 139-02 | Phase-139-scoped forbidden-claim gate |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` | 139-03 (authored); 139-04 (gate corrections + freeze) | THE deliverable — the gh#15 comment body, byte for byte |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` | 139-03 (authored); 139-04 (gate corrections + freeze) | the optional, default-skipped issue-body replacement (D-01) |
| `.planning/v1.31-OPERATOR-BATCH.md` | 139-05 — **hold branch only** | the parked one-command follow-up (D-08) |

Symbols created in `139-check-claims.py` (139-02): `_HERE`, `_DEFAULT_TARGETS`,
`FIRESTARTER_CLAIMSCAN_TARGETS_V131`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
`_assert_default_targets_are_local()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`.

**No file in `firestarter/` or `firestarter_app/` is created or modified by any plan in this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pinned public SHA → the claim the comment makes about that line range | An HTTP 200 proves a file exists at a SHA, never that `#L71-L73` says what you claim. Fragments are client-side; no request validates them |
| firmware repo working tree + committed history → the "before implementation" claim | The meta repo's ` M firestarter` is a stale gitlink pointer, not an edit; a naive `git status` assertion false-reds, and a naive "ignore submodules" assertion false-greens |
| gh#15's live body (untrusted third-party state) → this repo's captured artifact | Consumed via `--json body -q .body`, JSON-decoded, never shell-interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-04 | Information disclosure / credibility | a citation that 404s or whose line range moved | high | mitigate | Content-based `verify_anchor` on every anchor at its pinned SHA — fetch raw, slice the range, `grep -qF` an expected substring. `138-BASELINE.md` dropped (404s at the pushed tip); `PROJECT.md` withheld (table moved by 10 lines) |
| T-139-04b | Tampering | an anchor that resolves but means something else | high | mitigate | Every source anchor row records its ENCLOSING FUNCTION. `eprom.cpp:283` recorded explicitly as `eprom_internal_erase()`, and `memory.cpp:321-330` as `memory_set_data()` — the program pulse |
| T-139-03 | Repudiation | claiming "before implementation" from phase order rather than from measurement | high | mitigate | Three-ref blob equality on `eprom.cpp` and `memory.cpp` at `HEAD`, `origin/beta` and `3085084` — cannot false-green on a gitlink pointer, an uncommitted edit, or a committed one. `git submodule status` never used (F-08) |
| T-139-01 | Repudiation | `--auto`/`--chain` supplying an approval that was never given | critical | mitigate | Inherits T-137-16. This plan is `autonomous: true` and posts nothing; the gate lives in 139-05 and is `checkpoint:human-action`, which this project's execution machinery never auto-approves. The objective states the phase must not be run under those flags |
| T-139-09 | Information disclosure | token leakage into a committed artifact | medium | mitigate | `gh auth token` is never invoked; no `--body "$(...)"` construction anywhere; artifacts are hand-authored markdown plus mechanical captures |
| T-139-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
- `139-GH15-ORIGINAL-CRITERIA.md`: nine `- [ ]` boxes, first line `## Acceptance criteria`, no frontmatter, no H1, byte-identical checkbox lines to RESEARCH's independently-read copy.
- `139-CITATIONS.md`: four numbered sections; every anchor row four-column complete; the `verify_anchor` loop exits 0; both C1 commits resolve with their expected subjects and are reachable from `origin/beta`.
- D-10 baseline: `eprom.cpp` blob identical at `HEAD`, `origin/beta`, `3085084` (exactly one unique SHA).
- gh#15 unchanged: `state=OPEN`, comment count `0`, `labels=[]`, `updatedAt` still equal to `createdAt`.
- Read-only sub-repo discipline: nothing written under `firestarter/` or `firestarter_app/`.
- Requirements ticked by this plan: **NONE.** ISSUE-01 and ISSUE-02 tick only in 139-05, on the operator's wording approval; ISSUE-03 only on a verified post.
</verification>

<success_criteria>
- Both artifacts exist and are committed to the meta repo on `gsd/v1.31-27c-programming-algorithm-fidelity`, each with an explicit `git add <path>` (never `-A`, never `.`).
- Every citation the gh#15 comment will carry is verified by content at a pinned commit SHA, with the sliced text and the enclosing function recorded.
- The two divergences from `139-CONTEXT.md` §canonical_refs (`138-BASELINE.md` dropped; `PROJECT.md` not cited publicly) are recorded with reasons, and neither `139-CONTEXT.md` nor `139-RESEARCH.md` is edited to reconcile them.
- No `git push`, no `gh issue comment`, no `gh issue edit`, no label mutation, no sub-repo write.
</success_criteria>

<output>
Create `.planning/phases/139-gh-15-correction-outward/139-01-SUMMARY.md` when done, following
`137-05-SUMMARY.md`'s shape (`139-PATTERNS.md` §8): frontmatter with `provides:` carrying the artifacts
inline, then a one-sentence bolded verdict, one `##` per task, `## Task Commits`, `## Files
Created/Modified`, `## Decisions Made`, `## Deviations from Plan`, `## Issues Encountered`, `## Next
Phase Readiness`, and `## Self-Check` with `FOUND:` lines naming every artifact and every commit.
Set `requirements-completed: []` — this plan ticks nothing.
</output>
