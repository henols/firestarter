---
phase: 139-gh-15-correction-outward
plan: 03
type: execute
wave: 2
depends_on: ["139-01", "139-02"]
commits_land_in: .
files_modified:
  - .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md
autonomous: true
requirements: [ISSUE-01, ISSUE-02]
must_haves:
  truths:
    - "The comment states C1 — 0x0B's pulse is 500 microseconds, and 50000 us is the fingerprint of the x100 interpret_timing() multiplier Phase 57 removed — cited by file:line AND by the two commits that removed it (D-06)"
    - "The comment states C2 — pulse width is a database datum, not a per-protocol constant — carrying all three per-protocol histogram lines inline and linking the full distribution and the runnable script (D-06)"
    - "The comment states C3 — the safe 32-bit delay helper is for the 75 ms overprogram pulse, not for any bare pulse — citing the PROGRAM pulse in memory.cpp, never the erase pulse in eprom.cpp"
    - "The ~6.25 V program-VCC ceiling gets its own paragraph, framed as a limit on what ANY implementation of gh#15 on this shield can claim, not as an excuse for this one (D-05)"
    - "The comment proposes an acceptance-criteria amendment mapped item-for-item onto ALL NINE of gh#15's original boxes, each marked kept/corrected/replaced with its reason, and names the two prose directives in the body that must also change (D-03)"
    - "Every numeric row of the proposed design shape is marked proposed — no per-value datasheet attribution is claimed, because that is Phase 140's TABLE-04 and it does not exist yet (D-04)"
    - "The comment closes with exactly one short, specific community ask naming the unanswerable 0x0B question and the parts this milestone cannot guarantee bench coverage for (D-11)"
    - "The voice is first-person self-correction carried forward from 137-GH12-COMMENT.md — I filed this, two numbers are wrong, one premise is inverted (D-02)"
  artifacts:
    - path: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      provides: "THE deliverable — the gh#15 correction comment body, byte for byte; no frontmatter, no title heading, no footer"
      min_lines: 45
      contains: "6.25"
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      to: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      via: "every permalink in the comment is a content-verified row of the citation register, pinned to a commit SHA and never to a branch name"
      pattern: "https://github\\.com/henols/"
    - from: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      to: ".planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md"
      via: "the amendment table's first column quotes each of the nine original boxes verbatim from the capture"
      pattern: "separate write handlers"
  prohibitions:
    - "MUST NOT carry the sentence all three gh#15 constants disagree with the modal value, in any wording — it is FALSE for 0x08, whose 100 us constant IS the mode (104 of 127), and it would sit next to a link to the distribution that disproves it"
    - "MUST NOT cite eprom.cpp:283 as the program pulse — that line is inside eprom_internal_erase() and is the ERASE pulse; the program pulse is memory.cpp:321-330 inside memory_set_data()"
    - "MUST NOT cite .planning/PROJECT.md or any planning file whose line anchors move — PROJECT.md's C1/C2/C3 table is at L61-63 at the pushed tip and L71-73 locally"
    - "MUST NOT cite 138-BASELINE.md — it 404s at the pushed tip and is deliberately dropped from the citation set"
    - "MUST NOT cite anything by branch name — every permalink pins a commit SHA (D-07)"
    - "MUST NOT claim datasheet conformance, datasheet correctness or algorithm accuracy, and MUST NOT use the word proven — the phase's own claim gate forbids all of them"
    - "MUST NOT present any proposed numeric value as datasheet-attributed — per-value datasheet citation is TABLE-04 in Phase 140 and does not exist yet"
    - "MUST NOT post, comment on, edit or label gh#15 — this plan writes a file and nothing else"
    - "MUST NOT add YAML frontmatter, a title heading, or a footer to 139-GH15-COMMENT.md — the file is the literal comment body and any added byte is corruption"
    - "MUST NOT tick ISSUE-01, ISSUE-02 or ISSUE-03 — this plan ticks NOTHING"
    - "MUST NOT run git push — no agent push anywhere in Phase 139 (F-06); the phase is designed so no push is needed"
    - "MUST NOT create or modify any file under firestarter/ or firestarter_app/"
---

<objective>
Author `139-GH15-COMMENT.md` — the gh#15 correction comment, byte for byte as it would be posted — and
prove mechanically that it says everything ISSUE-01 and ISSUE-02 require and none of what the claim gate
forbids.

**This plan, and this phase, must NOT be executed under `--auto`/`--chain`.** Plan 139-05 carries a
`checkpoint:human-action` gate on an irreversible public act. The `checkpoint:human-action` type is
never auto-approved by this project's own execution machinery — but the correct operational answer is:
do not pass either flag for this phase at all.

Purpose: the test this comment is written against is a single question — *if a stranger, or an agent,
read only gh#15 and its comments and started coding tomorrow, would they build the corrected design?*
Anything that does not move that answer toward yes is decoration. The `50000 us` number is the single
most important thing to kill, because it is the one a reader would copy straight into code.

Output: `139-GH15-COMMENT.md`, gate-clean and content-asserted, committed but **not yet frozen** —
plan 139-04 freezes it alongside the body amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md
@.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md
@.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md
@.planning/phases/139-gh-15-correction-outward/139-VALIDATION.md
@.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md
@.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 139-GH15-COMMENT.md — C1/C2/C3, the 6.25 V ceiling, the nine-box amendment, one ask</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-GH12-COMMENT.md` in full — the VOICE D-02 carries forward and the ask-shape D-11 mirrors. Note its file shape: no frontmatter, no title heading, bold paragraph headings rather than `##`, and the first character of line 1 is the first character the reader sees on GitHub.
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §3 in full (the prose skeleton: the four movements, the opening's two-sentence shape, the plain-spoken concession that sets the register, the bulleted bold-verdict-first split, and the community ask verbatim) and §S-1's deliberate no-header exception.
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` §"C1 / C2 / C3 Substance — reproduced verbatim, do not paraphrase" including the warning block naming the one sentence that must NOT be carried, §"C3, strengthened", §"The 6.25 V ceiling — three canonical statements, verbatim", §"gh#15 Live State" including the nine boxes and the disposition table, §"C2's three histogram lines — quote-ready", §"F-02", §"F-03", §"Citation Register", and §"Open Questions".
    - `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` — the pinned SHAs and the content-verified anchor rows every permalink in this comment is built from.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` — the nine boxes, verbatim, that the amendment table's first column quotes.
    - `.planning/PROJECT.md` §"Current Milestone: v1.31 27C Programming-Algorithm Fidelity (gh#15)" — the locked C1/C2/C3 table, the D-01 structural consequence, the D-02 energy cap, and the evidence ceiling. This is the comment's substance and is not re-decided here.
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — read the forbidden vocabulary before writing, not after.
  </read_first>
  <action>
Write `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md`. The file IS the comment body:
no YAML frontmatter, no title heading, no footer, no trailing metadata. Headings are bold paragraphs
such as `**What I got wrong**`, never `##` — `137-GH12-COMMENT.md` is the shape.

Voice, per D-02: first person, plain-spoken maintainer, no hedging, no corporate register. Framed
explicitly as self-correction — I filed this; two numbers in it are wrong and one premise is inverted.
gh#15's author is `henols`, the same account that will post, so this is a correction of one's own filing
and never a rebuttal of a contributor. Lead with the self-correction, not with the evidence: one short
opening paragraph stating the trigger and the change, evidence after. No greeting, no preamble.

Required content, in whatever section order reads best:

**C1 — the `50000 us` default.** State plainly that `0x0B`'s pulse is 500 microseconds, not `50000 us`,
and that `50000 = 500 x 100` is the fingerprint of a x100 multiplier `interpret_timing()` applied to
`0x07` and `0x0B`, affecting 252 chips, removed in Phase 57. This deserves its own named correction, not
a row in a table — it is the number a reader would copy straight into code. Cite the adjudication at
`firestarter_app/doc/infoic-field-dictionary.md` lines 210-217 as a permalink pinned to the host SHA
recorded in `139-CITATIONS.md`, and cite the removal by commit: `8de307f` (subject: feat(57-01): remove
interpret_timing x100 multiplier and fix bare excepts (DEC-03)) and `12286df` (subject: feat(57-03):
regenerate chip_database.json from corrected build_db.py). Include the cross-check that makes the x100
self-evidently wrong: `AT28C64B` reads 10000 us = 10 ms, its datasheet byte-write time, and `DS1225`
NVRAM reads 1 us — a x100 would make the 28C64 one second per byte. Add the corroboration that lands
hardest: the firmware gh#15 asks to change already defaults `0x0B` to 500 us today, at `eprom.cpp` lines
69-77.

**C2 — pulse width is data, not a per-protocol constant.** Quote the three histogram lines inline,
verbatim, because they are the payload and a reader can check the arithmetic from the quoted line alone:
`0x07` n = 170, histogram `100 us x113, 200x27, 1000x22, 500x4, 50x4`; `0x08` n = 127, histogram
`100 us x104, 50x11, 10x7, 200x2, 1000x2, 20x1`; `0x0B` n = 32, histogram `500 us x21, 1000x6, 200x5`.
Link the full distribution (`138-02-PULSE-DISTRIBUTION.md`) and the runnable script
(`138-pulse-distribution.py`) as meta permalinks pinned to the meta SHA — do not paste the full dump
inline, it would bury the correction it exists to support. Say that the distribution was measured against
the `chip_database.json` blob that actually ships.

Use this framing and no other: no protocol has a single pulse width — `0x07` takes 5 distinct values,
`0x08` takes 6, `0x0B` takes 3 — and under gh#15's three constants, **203 of 329 chips (61.7 %) would be
programmed at the wrong width** (`0x07` 148/170, `0x08` 23/127, `0x0B` 32/32). Say plainly that `0x08`'s
`100 us` happens to equal its modal value, and that getting the right answer from the wrong model is not
a defence of the model; and that even the corrected modal `0x0B` value of 500 us, used as a constant,
still mis-programs 11 of 32 parts. **Do not write, in any wording, that all three gh#15 constants
disagree with the modal value** — that sentence appears in this project's own internal summary, it is
false for `0x08`, and posting it beside a link to the distribution that disproves it is exactly the
credibility failure this correction exists to prevent.

Cite `firestarter_app/firestarter/database.py` line 128 (`_parse_pulse_duration`) for the string
`"100 us"` to integer-microseconds layer, and do not blur that layer. Cite minipro as the upstream
precedent that `protocol_id` and `pulse_delay` are two orthogonal wire fields: it lives on GitLab,
`https://gitlab.com/DavidGriffith/minipro`, pinned at `cae74c0`, `src/t48.c` line 255 (which assigns
`msg[1] = device->protocol_id;`) and lines 266-267 (which pack `device->pulse_delay` with `format_int`),
both inside `t48_begin_transaction()`; and that minipro exposes `-o pulse=N` per run as a uint16, so
65535 us is its hard ceiling (`src/main.c` line 698).

**C3 — the safe 32-bit delay helper.** State that the helper is still needed, but for the overprogram
pulse rather than for any bare pulse: `3 x 25 x 1000 us = 75 ms`, built from gh#15's own defaults
(maximum pulses 25, overpulse multiplier 3, maximum overpulse 75000 us) — the comment is not importing
an outside number to make its point. Give the AVR argument in its strong form, not the weak one:
`delayMicroseconds()`'s documented accurate ceiling is 16383 us, but on AVR its parameter is
`unsigned int`, which is 16-bit, so `delayMicroseconds(75000)` cannot represent 75000 at all — it
truncates to 75000 - 65536 = 9464 us, a 12.5 % pulse delivered silently. Cite the program pulse at
`firestarter/src/proms/memory.cpp` lines 249-258, inside `memory_set_data()`. Do not cite
`eprom.cpp:283` as the program pulse — that line is inside `eprom_internal_erase()` and is the erase
pulse. At most one sentence, framed as an observation rather than a defect claim, may note that
`handle->pulse_delay` currently does double duty as both the program pulse and the erase pulse. Cite
`eprom.cpp` line 20 (`NUMBER_OF_RETRIES 20`) and line 177 (the adaptive growth formula) where the
current behaviour needs naming.

**The ~6.25 V program-VCC ceiling — its own paragraph, not a footnote.** State that all four vendor
algorithms assume roughly 6.25 V program-VCC for threshold margin, and that this is unreachable on this
shield, which has no VCC-raise path on any revision. State what the work buys — timing, pulse-count and
verify fidelity — and what it explicitly does not: silicon-margin fidelity. Frame it as a limit on what
any implementation of gh#15 on this hardware can claim, not as an excuse for this one, and name which of
gh#15's criteria it makes unreachable as written (boxes 3, 4 and 5 imply datasheet algorithm fidelity).
gh#15 omits this entirely, which is precisely why its acceptance criteria imply a fidelity that is not
available here. Write it as a statement of the hardware limit, never as a disclaimer appended to satisfy
a checker — even though the phase's own claim gate does require it to be present.

**The corrected design, as shape only (D-04).** State the parameter table's columns — `max_pulses`,
`overprogram_factor`, `overprogram_cap_us`, `verify_mode`, `vpp_path` — and the no-pulse-column rule;
that `handle->pulse_delay` stays on every write path with protocol constants surviving only as
`pulse_delay == 0` fallbacks; that `0x0B` loops pulse-then-verify with a 50 ms accumulated-energy cap per
byte (100 x 500 us, the classic 2716 total programming time) and no overpulse; and that a byte which
exhausts `max_pulses` hard-fails the block, reporting the address and the pulse count. Mark every numeric
row **proposed** — per-value datasheet attribution is Phase 140's job and does not exist yet, so no
number here may be presented as datasheet-attributed.

**The acceptance-criteria amendment (D-03) — a table, not a checklist.** Render it as a markdown table
with one row per box, so the count of original checkbox lines stays cleanly assertable elsewhere.
**Nine rows, not seven** — `139-CONTEXT.md` and `.planning/ROADMAP.md` both say seven and both are wrong;
`139-GH15-ORIGINAL-CRITERIA.md` is the measured truth. Column 1 quotes each original box verbatim from
that capture, so the original survives inside the comment itself. Column 2 is the disposition — exactly
one of kept, corrected or replaced. Column 3 is the reason, stated publicly. The dispositions:

1. separate write handlers for `0x07`/`0x08`/`0x0B` — **replaced**: protocol owns shape, the database
   owns the pulse; one shared per-byte loop driven by a `const` table keyed by `protocol_id`; three
   state machines duplicate most of their body on a device with a hard AVR flash budget.
2. no new database algorithm flags — **kept**.
3. `EPROM_STD` per-byte fixed 1 ms pulse/verify plus a final overprogram — **corrected**: the per-byte
   loop and the overprogram pulse are gh#15's central and correct insight and are kept; the `1 ms` is
   wrong, because `0x07`'s modal value is 100 us and it spans 50 to 1000 us.
4. `EPROM_QUICK` uses its own fixed short-pulse handler — **corrected**: "its own handler" falls with
   row 1; "fixed" falls with C2, since `0x08` spans 10 to 1000 us across 6 distinct values.
5. `EPROM_LEGACY` uses a long fixed pulse rather than the adaptive loop — **corrected**: dropping the
   adaptive loop is kept; "long" is wrong, per C1.
6. block mismatch / adaptive pulse-growth removed — **kept**: gh#15's core diagnosis, endorsed.
7. VPP routing protocol-correct and disabled on all exits — **kept**.
8. native tests cover dispatch, pulse behaviour, verification, failure and cleanup — **kept**, with
   "dispatch" now meaning table-row selection rather than handler selection.
9. all firmware targets build — **kept**.

Then, in prose immediately after the table, name the two directives in gh#15's body that are not
checkboxes and must also change, quoting each verbatim: "each protocol must own its programming state
machine and timing constants" is **replaced** — D-03 names this one explicitly; and "Do not retain the
current generic 500 us legacy default" is **reversed**, because 500 us is the correct value and
`50000 us` is the bug. Say plainly why the second matters most: it pre-emptively forbids the right
answer, and a reader implementing from the body obeys it as readily as a checkbox.

**The ask (D-11) — four lines, at the end, and only one ask.** State that the `0x0B`
one-shot-versus-looped question is not answerable from source: minipro never runs the algorithm, it packs
`pulse_delay` into a `BEGIN_TRANS` message and hands it to closed TL866/T48/T56/T76 firmware — so the
50 ms energy cap is reasoned, not derived, and say exactly that. Invite a datasheet page or a
logic-analyzer capture that settles it. Name the parts this milestone cannot guarantee bench coverage for
— M2716 and M2732 for `0x0B`, AM27C020 for `0x08` — as evidence I lack, not as something the reader
owes. One clause may mention that a per-run pulse override (`--pulse-us`) is proposed, so a reader with a
logic analyzer can override the database value and report back; mark it proposed like every other shape
claim.

Every claim in the comment carries a `file:line` permalink or a commit SHA, built from
`139-CITATIONS.md`'s pinned SHAs. Never link by branch name. Do not cite `.planning/PROJECT.md` (its
table moved by ten lines between the pushed tip and the working tree) and do not cite `138-BASELINE.md`
(it 404s at the pushed tip).

Commit with an explicit `git add <the one file> && git commit` — never `git add -A` and never
`git add .`; the working tree carries unrelated dirt that must not travel.
  </action>
  <acceptance_criteria>
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` exists, its first line is not `---`, and it contains no line starting with `#`.
    - C1 present: the file contains `500`, `50000`, `8de307f`, `12286df`, `infoic-field-dictionary.md`, and a permalink fragment `#L210-L217`.
    - C2 present: the file contains `n = 170`, `n = 127`, `n = 32`, `203`, `329`, `61.7`, `138-02-PULSE-DISTRIBUTION.md`, `138-pulse-distribution.py`, `_parse_pulse_duration` and `gitlab.com/DavidGriffith/minipro`.
    - C2 negative: a case-insensitive search for `all three.*disagree.*modal` returns no match.
    - C3 present: the file contains `memory.cpp`, `memory_set_data`, `75 ms`, `16383` and `9464`; and no occurrence of `eprom.cpp#L283`, and no sentence attributing the program pulse to `eprom.cpp:283`.
    - ISSUE-02 ceiling: the file contains `6.25` and `silicon-margin`, and the ceiling occupies a paragraph of its own rather than a parenthetical or a footnote.
    - Amendment: a markdown table with exactly nine disposition rows exists; the disposition column uses only `kept`, `corrected` and `replaced`; a count of those three words returns at least 9; and every one of the nine original box texts in `139-GH15-ORIGINAL-CRITERIA.md` appears verbatim somewhere in the comment.
    - Both non-checkbox directives are quoted verbatim: `each protocol must own its programming state machine and timing constants` and `Do not retain the current generic 500 us legacy default`.
    - D-04 present: the file contains `max_pulses`, `overprogram_factor`, `overprogram_cap_us`, `verify_mode`, `vpp_path`, `pulse_delay == 0`, `50 ms` and the word `proposed`.
    - D-11 present: the file names `M2716`, `M2732` and `AM27C020`, and states that the 50 ms cap is reasoned rather than derived.
    - Every `github.com` / `gitlab.com` link other than the gh#15 issue URL itself embeds a 7-to-40 hex-character commit SHA; no link contains `/blob/beta/`, `/blob/main/` or `/blob/gsd/`.
    - No link in the file points at `PROJECT.md` or `138-BASELINE.md`.
    - `git status --porcelain -- firestarter firestarter_app` shows at most the two pre-existing stale-gitlink entries.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && F=139-GH15-COMMENT.md && test -f "$F" && test "$(head -1 "$F")" != "---" && test "$(grep -c '^#' "$F")" = "0" && for p in "500" "50000" "8de307f" "12286df" "infoic-field-dictionary.md" "#L210-L217" "n = 170" "n = 127" "n = 32" "203" "329" "61.7" "138-02-PULSE-DISTRIBUTION.md" "138-pulse-distribution.py" "_parse_pulse_duration" "gitlab.com/DavidGriffith/minipro" "memory.cpp" "memory_set_data" "75 ms" "16383" "9464" "6.25" "silicon-margin" "max_pulses" "overprogram_factor" "overprogram_cap_us" "verify_mode" "vpp_path" "pulse_delay == 0" "50 ms" "proposed" "M2716" "M2732" "AM27C020"; do grep -qF -- "$p" "$F" || { echo "MISSING: $p"; exit 1; }; done && echo CONTENT-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && F=139-GH15-COMMENT.md && ! grep -qiE 'all three.*disagree.*modal' "$F" && ! grep -qF 'eprom.cpp#L283' "$F" && ! grep -qF 'PROJECT.md' "$F" && ! grep -qF '138-BASELINE.md' "$F" && ! grep -qE '/blob/(beta|main|gsd)' "$F" && test "$(grep -oiE '\b(kept|corrected|replaced)\b' "$F" | wc -l)" -ge 9 && echo NEGATIVE-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && grep -qF 'each protocol must own its programming state machine and timing constants' 139-GH15-COMMENT.md && grep -qF 'Do not retain the current generic 500 us legacy default' 139-GH15-COMMENT.md && echo DIRECTIVES-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && MISS=$(sed -n 's/^- \[ \] //p' 139-GH15-ORIGINAL-CRITERIA.md | while IFS= read -r b; do grep -qF -- "${b%.}" 139-GH15-COMMENT.md || printf '%s\n' "$b"; done) && test -z "$MISS" && echo ORIGINAL-BOXES-QUOTED-OK || { echo "NOT QUOTED VERBATIM:"; printf '%s\n' "$MISS"; exit 1; }</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && BAD=$(grep -oE 'https://(github|gitlab)\.com/[^ )>"]+' 139-GH15-COMMENT.md | grep -v '/issues/' | grep -vE '/(blob|raw|commit|commits)/[0-9a-f]{7,40}' || true) && test -z "$BAD" && echo PERMALINKS-PINNED-OK || { echo "UNPINNED LINKS:"; printf '%s\n' "$BAD"; exit 1; }</automated>
  </verify>
  <done>`139-GH15-COMMENT.md` exists as a byte-exact comment body carrying C1, C2, C3, the 6.25 V ceiling paragraph, the nine-row amendment table quoting every original box verbatim, both corrected prose directives, the proposed design shape, and one community ask — with every permalink pinned to a commit SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Run the claim gate against the comment and record the result</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — its forbidden vocabulary and its two required caveat patterns, so a failure is understood rather than worked around.
    - `.planning/phases/139-gh-15-correction-outward/139-02-SUMMARY.md` §"Claim gate — non-vacuity record" — the four runs that established this gate can fail; this task's green is only meaningful because Run 1 was red.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` (Task 1's output).
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §3d's structural warning — the required caveat must read as a statement of the hardware limit, never as a disclaimer bolted on to satisfy a checker; if the gate forces a caveat in, weave it into the ceiling paragraph rather than appending a sentence.
  </read_first>
  <action>
Run the gate in argv mode against the comment alone: `python3 139-check-claims.py 139-GH15-COMMENT.md`
from inside the phase directory. Do not use default mode here — `139-GH15-BODY-AMENDMENT.md` does not
exist yet and default mode would correctly fail closed on a missing target, which is not the signal this
task wants.

If the gate reports a forbidden phrase, fix the comment's wording rather than the gate: reword to
"checkable", "measured", "shown", "what the database says" or a similarly falsifiable formulation. Never
add a forbidden pattern's negation, never weaken the pattern, and never add a suppression comment. If the
gate reports a missing required caveat, that is ISSUE-02's own content requirement surfacing — weave the
6.25 V ceiling and the silicon-margin narrowing into the ceiling paragraph so they read as the hardware
statement they are, not as an appended disclaimer.

Re-run every content assertion from Task 1 after any wording change, since a reword can silently drop a
required token. Record in the SUMMARY: the literal command, the literal stdout, the exit code, and — if
any wording changed — the before and after of each changed sentence with the label that forced it.

Finally, re-assert that nothing has been posted and that the correction still precedes implementation:
`gh issue view 15 --repo henols/firestarter_prom --json state,comments,labels` must still report
`OPEN`, zero comments and no labels; and the three-ref blob equality on
`firestarter/src/proms/eprom.cpp` at `HEAD`, `origin/beta` and `3085084` must still yield exactly one
unique blob SHA. Record both.

Commit any wording fix with an explicit path list.
  </action>
  <acceptance_criteria>
    - `python3 139-check-claims.py 139-GH15-COMMENT.md` exits 0 and its `PASS:` line names `139-GH15-COMMENT.md`.
    - The SUMMARY records the command, stdout and exit code verbatim, and states that this green is licensed by plan 139-02's Run 1 red.
    - If any sentence changed, the SUMMARY carries a before/after pair for each, naming the forbidden label or missing caveat that forced it; no forbidden pattern was weakened or removed from `139-check-claims.py` (its git blob SHA is unchanged from plan 139-02's commit, and that is recorded).
    - All Task 1 content assertions still pass after any wording change.
    - `gh issue view 15 --repo henols/firestarter_prom --json state,comments,labels` reports `OPEN`, comment count `0`, labels `[]`.
    - The three-ref blob equality on `src/proms/eprom.cpp` still yields exactly one unique SHA.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && out=$(python3 139-check-claims.py 139-GH15-COMMENT.md); rc=$?; printf '%s\n' "$out"; test "$rc" -eq 0 && printf '%s' "$out" | grep -qF 'PASS:' && printf '%s' "$out" | grep -qF '139-GH15-COMMENT.md' && echo GATE-GREEN-OK</automated>
    <automated>cd /workspaces && test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "0" && test "$(gh issue view 15 --repo henols/firestarter_prom --json state -q .state)" = "OPEN" && test "$(for r in HEAD origin/beta 3085084; do git -C /workspaces/firestarter rev-parse $r:src/proms/eprom.cpp; done | sort -u | wc -l)" = "1" && echo PRECONDITIONS-STILL-OK</automated>
    <automated>cd /workspaces && test -z "$(git status --porcelain -- .planning/phases/139-gh-15-correction-outward/139-check-claims.py)" && echo GATE-UNMODIFIED-OK</automated>
  </verify>
  <done>The comment passes the phase's own claim gate in argv mode with the gate itself unmodified, every Task 1 content assertion still holds, gh#15 is still OPEN with zero comments, and `eprom.cpp` is still byte-identical at all three refs.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Purpose |
|---|---|---|
| `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` | 139-01 | gh#15's nine acceptance boxes captured verbatim from the live body |
| `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` | 139-01 (created); 139-04 appends §4/§5; 139-05 appends §6 | evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, D-10 blob baseline, claim-gate runs, freeze values, delivery record |
| `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` | 139-02 | Phase-139-scoped forbidden-claim gate |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` | **139-03** (authored); 139-04 (gate corrections + freeze) | THE deliverable — the gh#15 comment body, byte for byte |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` | 139-04 (authored + frozen) | the optional, default-skipped issue-body replacement (D-01) |
| `.planning/v1.31-OPERATOR-BATCH.md` | 139-05 — **hold branch only** | the parked one-command follow-up (D-08) |

Symbols created in `139-check-claims.py` (139-02): `_HERE`, `_DEFAULT_TARGETS`,
`FIRESTARTER_CLAIMSCAN_TARGETS_V131`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
`_assert_default_targets_are_local()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`.

**No file in `firestarter/` or `firestarter_app/` is created or modified by any plan in this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this project's internal summary prose → a public, checkable claim | `PROJECT.md` and `STATE.md` both carry a C2 sentence that is false for `0x08`; copying internal prose into a public comment imports its errors |
| a recalled `file:line` anchor → the line a reader actually lands on | `eprom.cpp:283` resolves, is a real `delayMicroseconds(handle->pulse_delay)`, and is the erase pulse |
| a working-tree line number → a permalink pinned to a pushed SHA | The two differ by every unpushed commit touching the file; GitHub's `#Lnn` fragment is client-side and validates nothing |
| an execution session run under `--auto`/`--chain` → this phase's outward act | The gate in 139-05 is `checkpoint:human-action`; the `autonomous: false` frontmatter flag alone is not self-protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-10 | Information disclosure / credibility | publishing the false "all three constants disagree with the modal value" beside a link to the distribution that disproves it | critical | mitigate | Explicit negative assertion in this plan's verify block; the honest 203-of-329 (61.7 %) framing is mandated instead, and it does not depend on the mode at all |
| T-139-04b | Tampering | citing `eprom.cpp:283` as the program pulse | high | mitigate | `memory.cpp:321-330` / `memory_set_data()` mandated as C3's anchor; a negative assertion forbids the `eprom.cpp#L283` fragment |
| T-139-04 | Information disclosure / credibility | a permalink that resolves but points at the wrong lines | high | mitigate | Every link pinned to a commit SHA from `139-CITATIONS.md`; a verify leg rejects any `github.com`/`gitlab.com` link without a 7-40 hex SHA and any `/blob/beta|main|gsd/` form; `PROJECT.md` and `138-BASELINE.md` not cited at all |
| T-139-11 | Repudiation | half-correcting a public spec — the numbers fixed, the architecture criterion and the two prose directives left standing | high | mitigate | Nine-row disposition table asserted structurally; both non-checkbox directives asserted verbatim; every original box text asserted present verbatim in the comment |
| T-139-05 | Repudiation | the claim gate green-lighting an overclaim | critical | mitigate | The gate was seen red on a four-label planted violation in plan 139-02 before this plan's green; this plan asserts the gate's own blob is unmodified, so the green cannot have been bought by weakening the gate |
| T-139-01 | Repudiation | `--auto`/`--chain` supplying an approval that was never given | critical | mitigate | Inherits T-137-16. This plan writes a file and makes only read-only `gh` calls; the gate lives in 139-05 and is `checkpoint:human-action` |
| T-139-03 | Repudiation | drafting against an implementation that has already moved | high | mitigate | Three-ref blob equality on `eprom.cpp` re-asserted at the end of this plan, not inferred from phase order |
| T-139-09 | Information disclosure | token leakage into a committed artifact | medium | mitigate | `gh auth token` never invoked; no `--body "$(...)"` construction; the comment is hand-authored markdown |
| T-139-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
- Content: every C1/C2/C3 token, the 6.25 V ceiling, the five table column names, the ask's three part numbers, all asserted by literal-string grep.
- Negative content: the false modal sentence absent; `eprom.cpp#L283` absent; `PROJECT.md` and `138-BASELINE.md` absent; no branch-name permalink.
- Structure: nine disposition rows; every original box quoted verbatim; both prose directives quoted verbatim.
- Gate: `python3 139-check-claims.py 139-GH15-COMMENT.md` exits 0, licensed by plan 139-02's recorded Run 1 red, with the gate's own blob unmodified.
- Preconditions: gh#15 still `OPEN` with zero comments and no labels; `eprom.cpp` still one unique blob across `HEAD`, `origin/beta`, `3085084`.
- Requirements ticked by this plan: **NONE.** ISSUE-01 and ISSUE-02 tick in 139-05 on the operator's wording approval; ISSUE-03 only on a verified post.
</verification>

<success_criteria>
- `139-GH15-COMMENT.md` exists, is committed to the meta repo on `gsd/v1.31-27c-programming-algorithm-fidelity`, and would post as-is with no editing.
- A stranger reading only gh#15 plus this comment would build the corrected design, not the design as filed.
- Every factual claim in it survives a reader following the link: content-verified anchor, pinned SHA, correct enclosing function.
- No forbidden overclaim; the 6.25 V ceiling reads as a hardware limit rather than a disclaimer.
- No post, no push, no sub-repo write, no gate weakened.
</success_criteria>

<output>
Create `.planning/phases/139-gh-15-correction-outward/139-03-SUMMARY.md` when done, following
`137-05-SUMMARY.md`'s shape (`139-PATTERNS.md` §8), with `provides:` naming the comment and its byte
length inline, and a `## Claim gate` section carrying the literal command, stdout and exit code plus any
before/after wording pair. Set `requirements-completed: []` — this plan ticks nothing.
</output>
