---
phase: 139-gh-15-correction-outward
plan: 02
type: execute
wave: 1
depends_on: []
commits_land_in: .
files_modified:
  - .planning/phases/139-gh-15-correction-outward/139-check-claims.py
autonomous: true
requirements: [ISSUE-02]
must_haves:
  truths:
    - "A Phase-139-scoped forbidden-claim gate exists carrying v1.31 vocabulary — the v1.30 checker is proven vacuous on this phase's vocabulary and is not copied, forked or env-seamed (D-05)"
    - "The gate has been SEEN TO FAIL on a planted violation, naming the specific offending labels, before any pass of it is believed"
    - "The gate fails closed on zero resolved targets and on a target missing from disk — there is no exit-0-on-nothing-scanned path anywhere in it"
    - "The gate self-asserts that its own default targets resolve inside this phase's own directory and carry this phase's own 139- prefix, so a future cross-phase copy cannot silently scan nothing"
    - "The gate's required caveat IS the 6.25 V program-VCC ceiling ISSUE-02 requires, so the gate and the requirement are the same check"
  artifacts:
    - path: ".planning/phases/139-gh-15-correction-outward/139-check-claims.py"
      provides: "The Phase-139 forbidden-claim gate: argv/env/default target resolution, a hoisted never-vacuous guard, v1.31 forbidden vocabulary, and the 6.25 V ceiling as the required caveat"
      min_lines: 90
      contains: "_DEFAULT_TARGETS"
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-check-claims.py"
      to: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      via: "_DEFAULT_TARGETS, built exclusively from _HERE (this module's own directory, computed fresh from __file__) — never a sibling-directory string constant"
      pattern: "139-GH15-COMMENT\\.md"
  prohibitions:
    - "MUST NOT copy, fork, import, subclass or env-seam .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py — RESEARCH F-04 proved by five executed probes that it cannot do this job"
    - "MUST NOT carry a proximity/context window of any kind — RESEARCH probe E returned PASS exit 0 on a file with four forbidden overclaims because the v1.30 window required an at28c/sdp/0x0d token this phase's vocabulary never contains"
    - "MUST NOT carry the v1.30 required-caveat pattern (no AT28C silicon was tested) — a gh#15 comment about 27C EPROMs has no business asserting it"
    - "MUST NOT carry an UNARMED/arming branch or any other code path that returns 0 without scanning a file"
    - "MUST NOT build, arm or extend Phase 146's CLOSE-01 claim gate — that is a Deferred Idea; this script is COMPLIANCE with CLOSE-01's spirit, scanning exactly two files"
    - "MUST NOT reuse the bare env-var name FIRESTARTER_CLAIMSCAN_TARGETS — three prior checkers already use unsuffixed or _V130-suffixed names; this one is _V131"
    - "MUST NOT be believed on a pass that was not preceded by an observed, recorded failure"
    - "MUST NOT tick ISSUE-01, ISSUE-02 or ISSUE-03 — this plan ticks NOTHING"
    - "MUST NOT run git push — no agent push anywhere in Phase 139 (F-06); the phase is designed so no push is needed"
    - "MUST NOT create or modify any file under firestarter/ or firestarter_app/"
    - "MUST NOT commit any planted or control fixture file anywhere in the repository"
---

<objective>
Author the Phase-139-scoped forbidden-claim gate D-05 requires, and prove it can fail before anything
relies on it passing.

**This plan, and this phase, must NOT be executed under `--auto`/`--chain`.** Plan 139-05 carries a
`checkpoint:human-action` gate on an irreversible public act. The `checkpoint:human-action` type is
never auto-approved by this project's own execution machinery — but the correct operational answer is:
do not pass either flag for this phase at all.

Purpose: `139-CONTEXT.md` D-05 says to check the draft mechanically with `check_permitted_claims.py`,
and warns about one landmine. Research found three more, and proved by execution that the v1.30 checker
returns `PASS` exit 0 on a file containing four forbidden overclaims when the surrounding vocabulary is
`0x07`/`0x08`/`0x0B` instead of `AT28C`/`SDP`/`0x0D`. A gate that cannot fail is worse than no gate,
because it launders the claim it was supposed to catch. This plan writes a small, narrow replacement and
subjects it to this milestone's binding planted-failure discipline.

Output: `139-check-claims.py`, plus a recorded four-run non-vacuity transcript in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md
@.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md
@.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md
@.planning/phases/139-gh-15-correction-outward/139-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 139-check-claims.py with v1.31 vocabulary, no proximity gate and no arming branch</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-check-claims.py</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §4 in full. §4a, §4b and §4c are labelled CHANGE; §4d, §4e and §4f are labelled COPY; §4g carries the naming trap.
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` §"F-04" (the five executed probes and the four independent incompatibilities) and §"Code Examples → The D-05 gate, with its mandatory planted-failure run".
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py` — the MECHANICS donor only. Copy `_HERE`'s construction, `resolve_targets`'s precedence, the hoisted never-vacuous guard, the fail-closed missing-file branch, `_print_bucket`, and the PASS line's shape. Do not copy its vocabulary, its caveat, its proximity window, its self-verifying rule, or its arming branch.
    - `.planning/phases/138-preconditions-baseline/138-pulse-distribution.py` — the in-milestone naming and non-vacuity precedent (phase-prefixed hyphenated filename; a planted-failure run recorded before any pass is trusted).
    - `.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md` §Deferred Ideas — the boundary this script stays inside: Phase 146 owns CLOSE-01's real gate; this is compliance only.
  </read_first>
  <action>
Write `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — a standalone Python 3
script run as `python3 139-check-claims.py [paths...]`, exit 0 on pass and 1 on any failure. The
filename is deliberate: it matches Phase 138's phase-prefixed convention and is therefore not importable
as a module name, which costs nothing because nothing imports it.

Module docstring, carrying two explicit non-claims adapted from the donor's own docstring block:
a green run is the **mechanizable half only** — it cannot detect an implied overclaim, a misleading
omission, or wrong tone; that is ISSUE-03's blocking operator wording review in plan 139-05, and a green
run must never be reported anywhere as by itself satisfying the honesty discipline. And a **scope
non-claim**: this script is *compliance* with the spirit of Phase 146's CLOSE-01 claim gate, **not a
build of it** — CLOSE-01 is a Deferred Idea for this phase. It forbids and requires nothing beyond what
ISSUE-02 and D-05 already state, and it scans exactly two files.

Required module-level symbols and behaviour:

`_HERE` — `os.path.dirname(os.path.abspath(__file__))`. Copy the donor's construction verbatim,
including its comment explaining that this is the ONLY directory `_DEFAULT_TARGETS` is ever built from
and never a sibling-directory string constant. That construction is what stops the v1.23-class defect
where a copied checker resolved to a stale sibling phase directory and passed vacuously.

`_DEFAULT_TARGETS` — an explicit two-element list, never a wildcard expansion and never a recursive
directory walk: `os.path.join(_HERE, "139-GH15-COMMENT.md")` and
`os.path.join(_HERE, "139-GH15-BODY-AMENDMENT.md")`.

`_assert_default_targets_are_local()` — a startup self-check called first thing in `main()`, failing for
every `_DEFAULT_TARGETS` entry whose `os.path.dirname` is not `_HERE` or whose `os.path.basename` does
not start with `139-`. On any failure it prints a `FAIL:` line naming the offending entry and the exact
defect ("does not resolve inside this phase's own directory" / "does not carry this phase's own 139-
prefix"), and `main()` returns 1 without scanning. This moves the Phase 137 precedent's two mandatory
paired pytest legs inside the script itself, so a future cross-phase copy fails loudly at run time
rather than passing vacuously.

`FIRESTARTER_CLAIMSCAN_TARGETS_V131` — the env seam, read once at module level with `os.environ.get`.
The `_V131` suffix is mandatory: three prior checkers already use the bare or `_V130`-suffixed name, and
a collision would let one phase's seam silently retarget another phase's gate.

`resolve_targets(argv)` — copy the donor's precedence and its `is not None` subtlety verbatim: explicit
positional argv paths win; else the env seam **if the variable is present in `os.environ`** (checked via
`is not None`, not truthiness, so an explicitly empty value resolves to zero targets rather than
silently falling back to defaults); else `_DEFAULT_TARGETS`. Return the list plus a `used_defaults` flag.

`FORBIDDEN_PATTERNS` — a list of `(label, compiled_regex)` tuples, all `re.IGNORECASE`. Keep the donor's
tuple shape; replace its contents entirely. The v1.31 vocabulary, label by label:
`datasheet-conformant` matching `datasheet[-\s]conformant`; `datasheet-correct` matching
`datasheet[-\s]correct`; `algorithm-accurate` matching `algorithm[-\s]accurate`;
`datasheet-compound-unqualified` matching `datasheet[-\s](?:conforming|compliant|faithful|exact|perfect|true)`;
`verified-on-silicon` matching `verified\s+(?:on|against)\s+(?:real\s+)?silicon`; `silicon-verified`
matching `silicon[-\s]verified`; `confirmed-working` matching `confirmed\s+working`; `works-on-silicon`
matching `works?\s+on\s+(?:\w+\s+){0,2}silicon`; `proven-on-silicon` matching
`proven\s+on\s+(?:\w+\s+){0,2}silicon`; `proven-unqualified` matching `\bproven\b`; `now-works` matching
`now\s+works?\b`; `should-now-work` matching `should\s+now\s+work`. The last three are carry-forwards:
`now-works` and `should-now-work` come from the v1.30 list un-anchored to AT28C and cover the "this now
works" drift class at zero cost; `proven-unqualified` is deliberately broad — a correction comment has
no business using the word at all, and legitimate senses reword to "checkable", "measured" or "shown".

`REQUIRED_CAVEAT_PATTERNS` — a list of `(label, prose, compiled_regex)` triples, each of which must match
at least once in EACH scanned file. Two entries: `ceiling-voltage`, prose "the ~6.25 V program-VCC
ceiling", regex `6\.25\s*V`; and `ceiling-narrowing`, prose "the silicon-margin narrowing that ceiling
implies", regex `silicon[-\s]margin`. This is the design choice that makes the gate and ISSUE-02 the
same check: ISSUE-02 requires the ceiling stated plainly, so the gate requires it too.

`scan_text(text, path)` — split into lines and record a violation for **every** forbidden match, with no
window and no context condition of any kind. Return the forbidden hits as (label, matched text, 1-based
line number) plus the set of required-caveat labels absent from the file. Do not port
`_SDP_CONTEXT_TOKENS`, `_sdp_context_in_window`, `_emission_in_window`, `_caveat_in_window` or
`_SELF_VERIFYING_PATTERN` — all five are v1.30 SDP-domain machinery, and the window is exactly what made
the donor vacuous here.

`_print_bucket(label, violations)` — copy verbatim from the donor: prints `FAIL: {n} {label}:`, then up
to 20 violations one per line, then an `... and N more` line if truncated.

`main(argv)` — order of operations: run the default-targets self-check; resolve targets; the **hoisted
never-vacuous guard** copied verbatim from the donor (on an empty target list, print
`FAIL: no scan targets resolved -- the gate cannot vacuously pass with nothing scanned` and return 1);
partition targets into existing and missing; the **fail-closed missing-file branch** copied from the
donor (on any missing target, print
`FAIL: scan target(s) not found on disk -- the gate cannot vacuously pass with a target silently skipped:`
followed by the missing paths, and return 1) — and **no arming branch**: the donor's
`if used_defaults and len(missing) == len(targets)` path that prints `UNARMED` and returns 0 is deleted
entirely. Phase 139 authors its artifacts before this gate is ever run in default mode, so there is no
pre-authored window to protect and no reason to keep an exit-0-on-nothing-scanned path. Then scan every
existing target; if there are forbidden hits or missing caveats, print them through `_print_bucket` in
two buckets — `forbidden phrase match(es)` and `file(s) missing a required 6.25 V ceiling caveat` — and
return 1. Otherwise print a PASS line that **names every scanned file** by path relative to `_HERE`,
states how many carry both required caveats, and closes with the parenthetical non-claim that this PASS
is the mechanizable half of the ISSUE-02 / D-05 discipline only. Guard the entrypoint with
`if __name__ == "__main__": raise SystemExit(main(sys.argv[1:]))`.

Do not add a `--fix` mode, a config file, a JSON output mode, or any recursive directory walk. Commit
with an explicit `git add <the one file> && git commit` — never `git add -A` and never `git add .`; the
working tree carries unrelated dirt (` M .gitignore`, ` M firestarter`, ` M firestarter_app`, untracked
`.claude/`, `package.json`, `package-lock.json`) that must not travel.
  </action>
  <acceptance_criteria>
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` exists and parses: `python3 -c "import ast;ast.parse(open('139-check-claims.py').read())"` exits 0.
    - The file defines `_HERE`, `_DEFAULT_TARGETS`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`, `_assert_default_targets_are_local`, `resolve_targets`, `scan_text`, `_print_bucket` and `main` — each greppable by name.
    - A case-insensitive grep for `at28c`, `_SDP_CONTEXT_TOKENS`, `_sdp_context_in_window`, `SELF_VERIFYING` and `UNARMED` returns zero matches — none of the v1.30 domain machinery survives, and the absence of `UNARMED` is the proof the arming branch was deleted rather than renamed.
    - `_DEFAULT_TARGETS` has exactly two entries, both built with `os.path.join(_HERE, ...)`; no string literal in the file contains a `..` path segment or another phase's directory name.
    - All three of D-05's named phrases are present as forbidden patterns: the source contains `datasheet[-`, `conformant`, `correct` and `algorithm[-\s]accurate`.
    - The two required-caveat regexes are present verbatim in the source: `6\.25\s*V` and `silicon[-\s]margin`.
    - The docstring contains the string `mechanizable half` and a sentence naming Phase 146 / CLOSE-01 as the owner of the real gate.
    - No file under `firestarter/` or `firestarter_app/` was written.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && python3 -c "import ast;ast.parse(open('139-check-claims.py').read());print('AST-OK')" && for s in _HERE _DEFAULT_TARGETS FORBIDDEN_PATTERNS REQUIRED_CAVEAT_PATTERNS _assert_default_targets_are_local resolve_targets scan_text _print_bucket "def main"; do grep -qF "$s" 139-check-claims.py || { echo "MISSING $s"; exit 1; }; done && echo SYMBOLS-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && test "$(grep -c -iE 'at28c|_SDP_CONTEXT_TOKENS|_sdp_context_in_window|SELF_VERIFYING|UNARMED' 139-check-claims.py)" = "0" && grep -qF 'mechanizable half' 139-check-claims.py && grep -qF 'CLOSE-01' 139-check-claims.py && grep -qF '6\.25' 139-check-claims.py && grep -qF 'silicon[-' 139-check-claims.py && grep -qF 'FIRESTARTER_CLAIMSCAN_TARGETS_V131' 139-check-claims.py && echo VOCAB-OK</automated>
  </verify>
  <done>`139-check-claims.py` exists with v1.31 vocabulary, the 6.25 V ceiling as its required caveat, a local-defaults self-check, a hoisted never-vacuous guard, a fail-closed missing-target branch, no proximity window and no arming branch — committed to the meta repo.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate can fail — planted violation first, clean control second, both fail-closed paths third</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-check-claims.py</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` (Task 1's output) — read the code you are about to exercise, so a run that fails for the wrong reason is recognised as such rather than counted as a red.
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` §4i (fixture strategy — the two precedents and which one this phase takes) and §S-3 (a gate that has only ever passed is untrusted).
    - `.planning/phases/138-preconditions-baseline/138-02-PULSE-DISTRIBUTION.md` §"Run 1 — the non-vacuity proof (planted failure)" — the in-milestone recording shape this task copies: the planted input lives only in the session scratchpad, is deleted after the run, is never committed, and its exact content is reproduced verbatim in the record so the exercise stays re-creatable.
    - `.planning/ROADMAP.md` §"v1.31 — 27C Programming-Algorithm Fidelity (gh#15)" — the Must-not-do paragraph makes planted-failure non-vacuity binding on every phase in this milestone.
  </read_first>
  <action>
Exercise the gate in four runs, in this order, and record all four verbatim in the plan SUMMARY. Use the
session scratchpad directory for every planted or synthetic input — never `.planning/`, never
`firestarter/`, never `firestarter_app/` — and delete each one immediately after its run. Nothing
planted is committed. This is Phase 138's recorded house style, chosen deliberately over Phase 137's
committed-`fixtures/` precedent: a committed fixture set plus a paired pytest module is the shape of
Phase 146's CLOSE-01 gate, which is a Deferred Idea for this phase.

**Run 1 — non-vacuity, MUST FAIL.** Write a scratchpad file containing the required 6.25 V ceiling
caveat sentence and the `silicon-margin` narrowing (so the failure is attributable to the forbidden
phrases and not to a missing caveat), plus four planted overclaims covering four distinct labels: one
`datasheet-conformant`, one `algorithm-accurate`, one `verified on silicon`, one `confirmed working`.
Surround them with `0x07` / `0x08` / `0x0B` vocabulary — that is precisely the context in which the
v1.30 checker returned `PASS` exit 0, so this run doubles as the regression proof that the new gate does
not inherit that defect. Run `python3 139-check-claims.py <scratch file>`. It MUST exit 1, MUST print
`FAIL:`, and MUST name all four labels. An exit 1 that names the wrong reason is a failure of this run,
not a pass of it.

**Run 2 — clean control, MUST PASS.** Write a scratchpad file containing the 6.25 V ceiling sentence and
the `silicon-margin` narrowing, real `0x07`/`0x08`/`0x0B` prose, and no forbidden phrase. Run the
scanner against it. It MUST exit 0 and its `PASS:` line MUST name that file. Only after Run 1 is red is
this green meaningful.

**Run 3 — never-vacuous guard, MUST FAIL.** Invoke with the env seam set to an explicitly empty value —
`FIRESTARTER_CLAIMSCAN_TARGETS_V131=` with no positional arguments. It MUST exit 1 and print the
`no scan targets resolved` line. Assert `PASS:` does NOT appear in stdout.

**Run 4 — fail-closed on a missing target, MUST FAIL.** Invoke with no arguments at all, so
`_DEFAULT_TARGETS` resolves to `139-GH15-COMMENT.md` and `139-GH15-BODY-AMENDMENT.md`, neither of which
exists yet (plan 139-03 authors them). It MUST exit 1 and print the `scan target(s) not found on disk`
line naming both missing basenames. Assert that neither `PASS:` nor `UNARMED` appears in stdout — the
absence of `UNARMED` is the run-time proof that the donor's arming branch was actually deleted.

Record, for each of the four runs: the literal command as run, the literal stdout, and the exit code —
and, for Runs 1 and 2, the input file's exact content reproduced verbatim, so the exercise stays
re-creatable even though the files are gone. State explicitly in the SUMMARY that Run 1 preceded Run 2,
and that no pass of this gate is relied upon anywhere until Run 1 was observed red.

If any run produces an unexpected result, fix `139-check-claims.py` and re-run all four from the top —
never accept a partial pass, and never adjust the planted input to make the gate agree with itself.
Commit any fix with an explicit path list.
  </action>
  <acceptance_criteria>
    - Run 1 exited 1, printed `FAIL:`, and its stdout names all four of `datasheet-conformant`, `algorithm-accurate`, `verified-on-silicon`, `confirmed-working`.
    - Run 2 exited 0 and its `PASS:` line names the control file by name.
    - Run 3 exited 1 and printed `no scan targets resolved`; `PASS:` is absent from its stdout.
    - Run 4 exited 1 and printed `scan target(s) not found on disk` naming both `139-GH15-COMMENT.md` and `139-GH15-BODY-AMENDMENT.md`; both `PASS:` and `UNARMED` are absent from its stdout.
    - All four runs are reproduced in `139-02-SUMMARY.md` with command, stdout and exit code; Runs 1 and 2 additionally reproduce their input file's exact content.
    - No planted or control file survives anywhere under `.planning/`, `firestarter/` or `firestarter_app/`: `git status --porcelain -- .planning/phases/139-gh-15-correction-outward` lists no untracked file other than this plan's own artifacts.
    - The SUMMARY states in words that Run 1 preceded Run 2.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && T=$(mktemp -d) && printf 'Protocol 0x07 and 0x0B notes.\nThe ~6.25 V program-VCC rail is unreachable on this shield, so this buys timing fidelity and not silicon-margin fidelity.\nThis firmware is datasheet-conformant and algorithm-accurate.\nIt was verified on silicon and is confirmed working for 0x08.\n' > "$T/planted.md" && out=$(python3 139-check-claims.py "$T/planted.md"); rc=$?; printf '%s\n' "$out"; rm -rf "$T"; test "$rc" -eq 1 && printf '%s' "$out" | grep -qF 'FAIL:' && printf '%s' "$out" | grep -qF 'datasheet-conformant' && printf '%s' "$out" | grep -qF 'algorithm-accurate' && printf '%s' "$out" | grep -qF 'verified-on-silicon' && printf '%s' "$out" | grep -qF 'confirmed-working' && echo RUN1-RED-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && T=$(mktemp -d) && printf 'Protocol 0x0B programs each byte with a pulse width taken from the database.\nThe ~6.25 V program-VCC these algorithms assume is unreachable on this shield, so this buys timing, pulse-count and verify fidelity and not silicon-margin fidelity.\n' > "$T/clean.md" && out=$(python3 139-check-claims.py "$T/clean.md"); rc=$?; printf '%s\n' "$out"; rm -rf "$T"; test "$rc" -eq 0 && printf '%s' "$out" | grep -qF 'PASS:' && printf '%s' "$out" | grep -qF 'clean.md' && echo RUN2-GREEN-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && out=$(FIRESTARTER_CLAIMSCAN_TARGETS_V131= python3 139-check-claims.py); rc=$?; printf '%s\n' "$out"; test "$rc" -eq 1 && printf '%s' "$out" | grep -qF 'no scan targets resolved' && ! printf '%s' "$out" | grep -qF 'PASS:' && echo RUN3-FAILCLOSED-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward && out=$(python3 139-check-claims.py); rc=$?; printf '%s\n' "$out"; test "$rc" -eq 1 && printf '%s' "$out" | grep -qF 'not found on disk' && printf '%s' "$out" | grep -qF '139-GH15-COMMENT.md' && printf '%s' "$out" | grep -qF '139-GH15-BODY-AMENDMENT.md' && ! printf '%s' "$out" | grep -qF 'PASS:' && ! printf '%s' "$out" | grep -qF 'UNARMED' && echo RUN4-FAILCLOSED-OK</automated>
  </verify>
  <done>The gate has been observed RED on a planted four-label violation before any green was accepted, GREEN on a clean control, and RED on both fail-closed paths — all four runs recorded verbatim with command, stdout and exit code, and no fixture committed.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Purpose |
|---|---|---|
| `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` | 139-01 | gh#15's nine acceptance boxes captured verbatim from the live body |
| `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` | 139-01 (created); 139-04 appends §4/§5; 139-05 appends §6 | evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, D-10 blob baseline, claim-gate runs, freeze values, delivery record |
| `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` | **139-02** | Phase-139-scoped forbidden-claim gate |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` | 139-03 (authored); 139-04 (gate corrections + freeze) | THE deliverable — the gh#15 comment body, byte for byte |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` | 139-03 (authored); 139-04 (gate corrections + freeze) | the optional, default-skipped issue-body replacement (D-01) |
| `.planning/v1.31-OPERATOR-BATCH.md` | 139-05 — **hold branch only** | the parked one-command follow-up (D-08) |

Symbols created by **this plan** in `139-check-claims.py`: `_HERE`, `_DEFAULT_TARGETS`,
`FIRESTARTER_CLAIMSCAN_TARGETS_V131`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
`_assert_default_targets_are_local()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`.

**No file in `firestarter/` or `firestarter_app/` is created or modified by any plan in this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a gate's exit code → the belief that a public claim is safe | A green run that was never shown capable of red is indistinguishable from a gate that scans nothing |
| another milestone's checker vocabulary → this milestone's vocabulary | The v1.30 proximity window requires an `at28c`/`sdp`/`0x0d` token that `0x07`/`0x08`/`0x0B` prose never contains; importing it imports a vacuous green |
| a cross-phase copy of this script → the directory it believes it lives in | `_HERE`-relative defaults plus a startup self-check are what stop a future copy resolving to a stale sibling phase directory |
| an execution session run under `--auto`/`--chain` → this phase's outward act | The gate in 139-05 is `checkpoint:human-action`; the `autonomous: false` frontmatter flag alone is not self-protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-05 | Repudiation | a vacuous claim gate green-lighting an overclaim | critical | mitigate | No proximity window; argv/env/`_HERE`-defaults resolution with a hoisted never-vacuous guard; fail-closed on a missing target; no arming branch; and Task 2's four-run planted-failure-first discipline, recorded verbatim |
| T-139-05b | Spoofing | a future cross-phase copy of this script silently scanning nothing | high | mitigate | `_assert_default_targets_are_local()` runs first in `main()` and returns 1 if any default entry is outside `_HERE` or lacks the `139-` prefix |
| T-139-05c | Tampering | the gate failing for an irrelevant reason and being read as detection | high | mitigate | Run 1 asserts the four specific labels by name, not merely a non-zero exit; the v1.30 donor's AT28C caveat requirement is not ported |
| T-139-01 | Repudiation | `--auto`/`--chain` supplying an approval that was never given | critical | mitigate | Inherits T-137-16. This plan posts nothing; the gate lives in 139-05 and is `checkpoint:human-action`, which this project's execution machinery never auto-approves |
| T-139-09 | Information disclosure | token leakage into a committed artifact | medium | mitigate | `gh auth token` never invoked; this plan makes no network call at all |
| T-139-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in this plan's scope — the script is standard-library-only Python 3 |
</threat_model>

<verification>
- `139-check-claims.py` parses, defines all nine named symbols, and contains none of the v1.30 domain machinery (`at28c`, `_SDP_CONTEXT_TOKENS`, `_sdp_context_in_window`, `SELF_VERIFYING`, `UNARMED` all absent).
- Run 1 (planted, four labels) RED before Run 2 (clean control) GREEN — order recorded.
- Run 3 (empty env seam) and Run 4 (default targets absent) both RED with their distinct fail-closed messages; `PASS:` absent from both.
- Nothing planted committed; nothing written under `firestarter/` or `firestarter_app/`.
- Requirements ticked by this plan: **NONE.** ISSUE-02 ticks only in 139-05, on the operator's wording approval.
</verification>

<success_criteria>
- The gate exists, is committed to the meta repo on `gsd/v1.31-27c-programming-algorithm-fidelity` with an explicit `git add <path>`, and is exercisable by any later plan as `python3 139-check-claims.py [paths...]`.
- The gate has been seen to fail, for four attributable named reasons, before any pass of it is relied on.
- The gate's required caveat is the 6.25 V program-VCC ceiling, making the mechanical check and ISSUE-02 the same check.
- No `git push`, no `gh` call of any kind, no sub-repo write, no committed fixture.
</success_criteria>

<output>
Create `.planning/phases/139-gh-15-correction-outward/139-02-SUMMARY.md` when done, following
`137-05-SUMMARY.md`'s shape (`139-PATTERNS.md` §8). It must carry a `## Claim gate — non-vacuity record`
section reproducing all four runs (command, stdout, exit code) and the exact content of Runs 1 and 2's
inputs, in the style of `138-02-PULSE-DISTRIBUTION.md` §"Run 1". Set `requirements-completed: []` — this
plan ticks nothing.
</output>
