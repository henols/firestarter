---
phase: 139-gh-15-correction-outward
plan: 05
type: execute
wave: 4
depends_on: ["139-04"]
commits_land_in: .
files_modified:
  - .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md
  - .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md
  - .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
  - .planning/v1.31-OPERATOR-BATCH.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [ISSUE-01, ISSUE-02, ISSUE-03]
user_setup:
  - service: github
    why: "ISSUE-03's blocking operator wording review of the gh#15 correction, the separate explicit posting authorization, and the separately-authorized optional issue-body amendment"
    dashboard_config:
      - task: "Read the frozen draft and the frozen body amendment, then (1) explicitly approve the wording or name the exact sentences that must change, AND (2) state whether to post now or approve-but-hold, AND (3) only if posting was authorized, state whether to also apply the issue-body amendment (default is no)"
        location: "This plan's Task 1 checkpoint, presented in the execution transcript"
      - task: "If posting is authorized: either grant permission for the agent to run `gh issue comment` / `gh issue edit` (neither is in the settings allow list and state-changing `gh` is additionally gated by the auto-mode classifier), or run the recorded one-line command yourself and say so"
        location: "This plan's Task 2, immediately after the gate"
must_haves:
  truths:
    - "The gh#15 correction is never posted without a genuine, real-time operator wording approval — this task type is checkpoint:human-action specifically because --auto/--chain auto-approve checkpoint:human-verify gates but NEVER checkpoint:human-action gates (D-09)"
    - "Wording approval, posting authorization and the body-edit authorization are three separately-recorded answers, not one — the operator can stop at any of the three (D-01, D-08)"
    - "Posting requires BOTH the operator's explicit post now AND a freshly re-measured mechanical precondition that eprom.cpp and memory.cpp are still untouched — absent either signal the correction is frozen and reviewed but not posted (D-10)"
    - "The approve-but-hold branch unblocks Phase 140 under a recorded, named exception written into BOTH the phase record and the ROADMAP entry, leaving ISSUE-03 open with its exact one-command follow-up parked (D-08)"
    - "Any post is proven equal to the reviewed text by a fetch-back byte-diff under the single normalization named in advance, and gh#15's comment count is proven to increment by exactly one with state still OPEN and labels still empty"
  artifacts:
    - path: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      provides: "Extended with section 6, the delivery record: which branch was taken, who performed each outward act, the byte-diff result, the exact-increment state assertion, and the negative-flag audit"
      contains: "## 6."
    - path: ".planning/v1.31-OPERATOR-BATCH.md"
      provides: "HOLD BRANCH ONLY — the parked A-1 row carrying the approval date, the freeze values, the unambiguous posting state, the named precondition, and the exact single command with a full path and no ellipsis"
      contains: "gh issue comment 15"
  key_links:
    - from: ".planning/v1.31-OPERATOR-BATCH.md"
      to: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      via: "the A-1 row names the frozen file by full repo-relative path inside the exact one-line follow-up command"
      pattern: "139-GH15-COMMENT\\.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the hold branch's named exception on Phase 140's Depends on line and the in-place ISSUE-03 annotation are the same decision recorded in both places"
      pattern: "ISSUE-03"
  prohibitions:
    - "MUST NOT post, edit, label or close gh#15 before Task 1's gate has been answered by a real human in real time"
    - "MUST NOT treat auto_advance, --auto or --chain as an answer to any of the three questions — this task type is checkpoint:human-action precisely so no automated mode can supply them"
    - "MUST NOT infer posting authorization from wording approval, or the body-edit authorization from posting authorization — three answers, three records"
    - "MUST NOT apply the body amendment by default; it is offered only after posting is authorized and applied only on a separate explicit yes"
    - "MUST NOT post if the operator said post now but any mechanical precondition fails — fail closed to the hold branch and record why"
    - "MUST NOT attempt to self-unblock a permission denial: no edits to .claude/settings.local.json, no gh api --method POST workaround, no alternate transport"
    - "MUST NOT pass --add-label, --remove-label, --label, -l, --assignee, --milestone, --project, --web, --editor, --edit-last or --delete-last to any gh call, and MUST NOT use --body with an inline string or a heredoc — only --body-file with a committed path"
    - "MUST NOT run gh api on the path repos/OWNER/REPO/issues/15/comments/<id> — that form 404s and its error body would be diffed as if it were the comment"
    - "MUST NOT reword anything beyond the operator's named corrections; if a named correction cannot be mapped to a specific sentence, return to the gate rather than guess"
    - "MUST NOT tick ISSUE-03 on the hold branch — its text says posted, and it stays open with an in-place annotation"
    - "MUST NOT run phase.complete or any state tool to edit ROADMAP.md — it clobbers an unrelated phase's Plans line; amend by hand and diff"
    - "MUST NOT run git push, and MUST NOT create or modify any file under firestarter/ or firestarter_app/"
---

<objective>
Present the frozen correction to the operator, take exactly the branch they authorize, and prove
mechanically what happened either way.

**This plan, and this phase, must NOT be executed under `--auto`/`--chain`** (D-09, and this milestone's
own binding practice for outward-facing irreversible acts). Task 1 below is written so that even if a
future execution session runs under one of those flags anyway, the `checkpoint:human-action` type is
never auto-approved by this project's own execution machinery — but the correct operational answer is:
do not pass either flag for this phase at all. The `autonomous: false` frontmatter flag alone is **not**
self-protecting.

Purpose: the guarantee worth enforcing is that Phase 140 never starts against an **unreviewed** draft.
Posting is the default and expected outcome of the same gate, but "approved, hold" is a legitimate
distinct answer with its own fully-planned branch — not a failure, and never a silent skip.

Output: either a posted, byte-verified gh#15 comment (and optionally an amended body), or a frozen,
approved, parked follow-up with a named ROADMAP exception — recorded either way in `139-CITATIONS.md`
section 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md
@.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md
@.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md
@.planning/phases/139-gh-15-correction-outward/139-VALIDATION.md
@.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md
@.planning/v1.30-OPERATOR-BATCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: BLOCKING operator wording review, posting authorization, and the optional body-edit authorization</name>
  <files>none (gate only)</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` — the frozen comment, in full, presented to the operator in full.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` — the frozen body replacement, in full.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` — the nine boxes, so the operator can read the disposition mapping against the original.
    - `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` sections 2, 4 and 5 — the anchor transcript, the claim-gate runs, and the freeze values, which together are the review packet's evidence half.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-05-PLAN.md` Task 2 — the worked instance of this gate, whose shape this task copies with one extra question.
  </read_first>
  <what-built>
The gh#15 correction is drafted, cross-checked and frozen. `139-GH15-COMMENT.md` carries C1 (the 500 us
correction and the x100 BUG-2 fingerprint), C2 (pulse width as a database datum, with the three
per-protocol histogram lines and the honest 203-of-329 wrong-width count), C3 (the safe delay helper's
real purpose, anchored at the program pulse in `memory.cpp`, not the erase pulse), the approximately
6.25 V program-VCC ceiling in its own paragraph, a nine-row acceptance-criteria amendment quoting every
original box verbatim, both non-checkbox body directives named and corrected, and one community ask.
`139-GH15-BODY-AMENDMENT.md` is a complete, ready-to-apply replacement body preserving the original nine
boxes verbatim in a collapsed block.

Every citation was fetched at a pinned commit SHA, sliced to its pinned line range and matched by
content — not by status code — and each source anchor's enclosing function is recorded. The phase's own
claim gate was seen to FAIL on a planted violation immediately before it passed on these two files. Both
files are frozen: blob SHA, byte length and committing commit are recorded in `139-CITATIONS.md`
section 5. gh#15 currently has zero comments, state OPEN, no labels, and has never been edited.
  </what-built>
  <action>
Present the review packet — both frozen files in full, plus the freeze values, the claim-gate transcript
and the anchor-verification summary — and BLOCK until the operator answers the questions below. Do not
proceed on silence, and do not treat `auto_advance`, `--auto` or `--chain` as an answer to any of them;
this task type is `checkpoint:human-action` specifically so that no automated mode ever supplies these
answers (D-09).

1. **Wording.** Read `139-GH15-COMMENT.md`. Approve it as-is, or name the exact sentences that must
   change. Three things must never be true regardless of any correction: it must never claim datasheet
   conformance, datasheet correctness or algorithm accuracy; it must never present a proposed numeric
   value as datasheet-attributed, since per-value citation is Phase 140's work and does not exist yet;
   and it must never carry the claim that all three gh#15 constants disagree with the modal value, which
   is false for `0x08` and would sit beside a link to the distribution that disproves it.

2. **Posting authorization and timing.** State one of:
   - "Approved — post it now", or
   - "Approved, but hold — do not post until I say so", or
   - name corrections, in which case restate one of the two options above once the corrections are
     described.

   These are two different decisions. A wording approval is not a posting authorization and must never be
   read as one.

3. **The optional issue-body amendment (D-01) — ask this only if question 2 was answered "post it now".** State
   one of:
   - "Also apply the body amendment", or
   - "Comment only — do not touch the body" (**this is the default; if the answer is anything other than
     an explicit yes, the body is not edited**).

   Context for the decision: gh#15 was filed by this same account and has never been edited, so amending
   the body is available here in a way it is not on a stranger's issue — and a reader who implements
   gh#15 reads the body, not the comments. Against that: it is an outward act beyond what ISSUE-01
   through ISSUE-03 ask for, and it is effectively irreversible. The original nine boxes are preserved
   verbatim in a collapsed block either way.

Capture the operator's verdict **verbatim** in the plan SUMMARY, as three separately-numbered answers in
their own words. If they name corrections, apply exactly those corrections in Task 2 — no silent
auto-approval, no rewording beyond what was asked for, and if a named correction cannot be mapped to a
specific sentence, come back to this gate rather than guess.

Record the gh#15 comment count immediately before this task and again immediately after it; both must
read zero.
  </action>
  <acceptance_criteria>
    - The operator's verdict is recorded verbatim in the SUMMARY as three separately-numbered answers, including an unambiguous choice between "post now" and "hold".
    - Question 3 was asked only if question 2 was answered "post now"; if it was not asked, the SUMMARY says so and the body amendment is treated as not authorized.
    - If corrections were named, each is traceable to a line of the verbatim verdict.
    - Nothing was posted before or during this task: `gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length'` reads `0` both immediately before and immediately after, and both readings are recorded.
    - `gh issue view 15 --repo henols/firestarter_prom --json updatedAt -q .updatedAt` is unchanged from the value recorded in `139-CITATIONS.md` section 0 — the body was not edited during the gate.
  </acceptance_criteria>
  <verify>
    <human-check>The operator has typed an explicit wording approval (or named the exact sentences that must change), AND an explicit posting-timing choice ("post now" or "hold"), AND — only if posting was authorized — an explicit yes or no on the body amendment; all three recorded verbatim in the SUMMARY, with the gh#15 comment count recorded as zero both before and after.</human-check>
  </verify>
  <resume-signal>Type "approved, post now" / "approved, hold" / or name the sentences that must change plus your posting-timing choice. If you say "post now", you will also be asked one more question: whether to apply the issue-body amendment as well (default: no).</resume-signal>
  <done>The operator has explicitly answered questions 1 and 2, and question 3 if it applied; all answers are recorded verbatim; nothing has been posted and the body has not been edited.</done>
</task>

<task type="auto">
  <name>Task 2: Apply named corrections, take exactly the authorized branch, and prove what happened</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md, .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md, .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md, .planning/v1.31-OPERATOR-BATCH.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - The operator's verbatim verdict from Task 1.
    - `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` sections 3 and 5 — the D-10 blob baseline and the freeze values this task re-asserts before acting, plus the trailing-newline normalization named there in advance.
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` section 2c (the executed 122-DELIVERY byte-equality recipe), 2e (the exact-increment state assertion), 2f (the negative-flag audit table), section 7 (the `v1.30-OPERATOR-BATCH.md` A-1 row anatomy, all eight load-bearing elements, and the RUN STATUS section), and S-5 (in-place annotation, never silent ticking or silent rewriting).
    - `.planning/v1.30-OPERATOR-BATCH.md` — the shape the v1.31 file copies. Do **not** append a v1.31 row to it; it belongs to a closed milestone and its A-1 is already RESOLVED.
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` section "F-07" (the permission situation and the two available paths) and "Common Pitfalls" 5, 6 and 7.
    - `.planning/ROADMAP.md` — the Phase 140 block, whose `**Depends on**:` line the hold branch amends, and the Phase 139 block.
    - `.planning/REQUIREMENTS.md` section "gh#15 Correction" — the three requirement rows.
  </read_first>
  <action>
**Step 0 — apply exactly the operator's named corrections, if any.** Edit only the sentences they named.
Record a before/after pair for each. Then re-run `python3 139-check-claims.py` in default mode (must exit
0, naming both files), re-run every content assertion from plans 139-03 and 139-04, re-commit, and record
the **new** freeze triple (blob SHA, byte length, committing commit) for each changed file in
`139-CITATIONS.md` section 5 as an additional row — never by overwriting the original row. If no
corrections were named, say so explicitly and re-assert the existing freeze values instead.

**Step 1 — fail-closed preconditions, re-measured now, not reused.** All four must hold before any
outward call:
1. The operator's verdict says "post now" verbatim.
2. `firestarter/src/proms/eprom.cpp` and `firestarter/src/proms/memory.cpp` each yield exactly one unique
   blob SHA across `HEAD`, `origin/beta` and `3085084` — the D-10 "correction precedes implementation"
   assertion, re-measured in this task rather than carried forward from plan 139-01. Do not use
   `git status` on the meta repo for this (its ` M firestarter` line is a stale gitlink pointer, not an
   edit) and never use `git submodule status` (it fails unconditionally in this repo).
3. `git status --porcelain` is empty for both frozen artifacts, and each one's `git rev-parse HEAD:<path>`
   equals the freeze value recorded for it in `139-CITATIONS.md` section 5.
4. `gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length'` still reads
   `0`.

If any of the four fails, **do not post**. Take the hold branch below and record the failing precondition
by name as the reason. A "post now" plus a moved `eprom.cpp` blob does not post.

**Step 2A — POST BRANCH** (operator said post now, and all four preconditions hold).

Post with exactly: `gh issue comment 15 --repo henols/firestarter_prom --body-file
.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md`. Use `--body-file` with that committed
path and nothing else — no `--body`, no heredoc, no shell-constructed body, and none of the forbidden
flags listed in this plan's prohibitions.

If the call is refused by the permission layer, this is expected: `gh issue` is absent from the settings
allow list and state-changing `gh` is additionally gated by an auto-mode classifier. Do **not** attempt
to work around it. Offer the operator the two legitimate paths — grant the permission and the agent runs
the identical command, or the operator runs the recorded one-liner themselves — and record which path was
taken and **who performed the act**, in the custody style Phase 138's baseline used.

Immediately after any successful post:
- Capture the comment URL printed by the call.
- Fetch the comment back: `gh issue view 15 --repo henols/firestarter_prom --json comments -q
  '.comments[-1].body'`, piped through `tr -d '\r'`, written to a file under `$(mktemp -d)`. With gh#15
  starting at zero comments, `.comments[-1]` is unambiguously ours. If you prefer an ID-addressed fetch,
  the correct endpoint is `gh api repos/henols/firestarter_prom/issues/comments/<id>` — the form with the
  issue number in the path 404s, and a 404 body piped to a file without an exit-code check would be
  diffed as if it were the comment.
- Byte-diff under the single normalization named in advance:
  `diff <(sed -e '$a\' <frozen file>) <(sed -e '$a\' <fetched file>)` must be empty. A byte-length
  difference of exactly one, in the direction of GitHub having appended a trailing newline, is the
  expected and documented result — not a mismatch.
- Assert the exact increment and that nothing else changed:
  `gh issue view 15 --repo henols/firestarter_prom --json state,comments,labels -q
  '{state:.state,n:(.comments|length),labels:.labels}'` must yield state `OPEN`, `n` exactly `1`, and
  labels `[]`.
- Reproduce the negative-flag audit table: for each of `--label` / `--add-label` / `-l`, `--assignee`,
  `--milestone`, `--project`, `--web`, `--editor`, `--edit-last` / `--delete-last`, inline `--body`,
  heredoc or shell-piped body construction, `gh issue close`, and `gh auth token` — record "No" against
  each, with the literal argv of every call actually made recorded beside it, copy-pasted and not
  paraphrased.

**Step 2B — the body amendment**, only if question 3 was answered with an explicit yes AND the post
above succeeded and verified. Run `gh issue edit 15 --repo henols/firestarter_prom --body-file
.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md`, with no label flags. Then read
the body back with `gh issue view 15 --repo henols/firestarter_prom --json body -q .body | tr -d '\r'`
and assert: the nine original boxes survive verbatim inside the collapsed block (diff them against
`139-GH15-ORIGINAL-CRITERIA.md`); the count of lines matching `^- \[ \]` is exactly 9; `updatedAt` has
changed from `2026-07-12T09:15:27Z`; state is still `OPEN` and labels are still `[]`. Byte-diff the read
-back body against the frozen amendment under the same trailing-newline normalization. If question 3 was
not asked or was answered no, state plainly in the record that the body was **not** edited and that
`updatedAt` is unchanged — the default outcome, not an omission.

**Step 2C — HOLD BRANCH, D-08's named exception** (operator approved the wording but said hold, or any
precondition failed).

Post nothing. Assert the comment count is still `0` after this task and record it.

Create `.planning/v1.31-OPERATOR-BATCH.md` on the `v1.30-OPERATOR-BATCH.md` shape — a new file, never an
edit to the v1.30 one. Header naming the milestone and the standing operator instruction; a
`## A. Blocking` table with the columns Item, Why it cannot be delegated, Raised by; and an A-1 row
carrying all eight load-bearing elements in order: (1) the item, (2) who approved what and when, (3) the
freeze values — file, blob SHA, byte length, (4) the unambiguous posting state, stated as posting is HELD
and not authorized, (5) the independent mechanical evidence re-measured in this task, (6) any named
precondition for acting, (7) the exact single command with a full repo-relative path and no ellipsis —
`gh issue comment 15 --repo henols/firestarter_prom --body-file
.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` — and (8) provenance, Phase 139 /
ISSUE-03. Add the honest-arithmetic aside this phase needs: the fetch-back diff **expects** exactly one
appended trailing newline, so a bare note that the diff was non-empty would misread as a failure. Add a
`## RUN STATUS` section with the flat, unhedged inventory sentence in the v1.30 style: nothing has been
posted to GitHub, nothing has been pushed, the issue body has not been edited.

Amend `.planning/ROADMAP.md`'s Phase 140 block. Its `**Depends on**:` line currently reads
`**Depends on**: Phase 139 (the correction is public before this implementation phase lands).` Amend
exactly that line with the named exception: the correction is frozen and operator-approved for wording,
posting is held pending the operator's authorization, and Phase 140 proceeds under this recorded
exception so Phase 146's CLOSE-04 has something honest to reconcile against. Amend it **by hand**.
Snapshot `.planning/ROADMAP.md` before the edit and diff it after: a recorded project finding is that
`phase.complete` clobbers an unrelated phase's `**Plans:**` line, so no state tool may be used to make
this edit, and if any state tool touches ROADMAP during this plan the diff must be inspected.

**Step 3 — requirement ticking, both branches.** Name what is ticked and what is not, exhaustively.
- Tick **ISSUE-01** and **ISSUE-02** in `.planning/REQUIREMENTS.md`, on the operator's wording approval of
  the frozen draft, each with an Evidence line citing the frozen file, its blob SHA, and the specific
  content assertions that discharge it.
- Tick **ISSUE-03** only on the post branch, and only after the byte-diff and the exact-increment
  assertion both passed; cite the comment URL and the diff result.
- On the hold branch, **ISSUE-03 stays `[ ]`**. Annotate its row in place — original text preserved,
  annotation appended — explaining that the wording is approved and the text frozen, that posting is held
  by operator decision, and naming the exact single command that closes it. Do not rewrite the row and do
  not tick it. Add the matching Traceability-table note.

**Step 4 — record the branch in `139-CITATIONS.md` section 6, "Delivery".** Which branch was taken and
why; who performed each outward act, stated explicitly, in the custody style Phase 138's baseline used;
the literal argv of every call made; the byte-diff result; the before/after state assertion; the
negative-flag audit; and, on the hold branch, a pointer to the parked A-1 row. Append only — sections 0
through 5 are not rewritten.

Commit with explicit path lists throughout — never `git add -A` and never `git add .`. Never run
`git push`.
  </action>
  <acceptance_criteria>
    - The SUMMARY names which branch was taken and quotes the operator verdict line that selected it.
    - If corrections were named: each has a recorded before/after pair traceable to the verdict; the gate re-ran green in default mode; every content assertion from plans 139-03 and 139-04 re-ran green; a new freeze triple was appended for each changed file without overwriting the original row.
    - All four preconditions are recorded with their measured values, re-measured in this task rather than carried forward.
    - POST BRANCH: the comment URL is recorded; the fetch-back byte-diff under the `sed -e '$a\'` normalization is empty; `gh issue view 15 --json state,comments,labels` yields state `OPEN`, exactly `1` comment, labels `[]`; the negative-flag audit table records "No" for every listed flag with the literal argv beside it; ISSUE-01, ISSUE-02 and ISSUE-03 are ticked with evidence.
    - BODY-EDIT SUB-BRANCH (only if explicitly authorized): the read-back body's `^- \[ \]` count is exactly 9 and those nine lines are byte-identical to `139-GH15-ORIGINAL-CRITERIA.md`; `updatedAt` has changed; state `OPEN` and labels `[]` unchanged; the read-back body byte-diffs clean against the frozen amendment. If not authorized, the record states plainly that the body was not edited and `updatedAt` is unchanged.
    - HOLD BRANCH: gh#15's comment count is `0` after the task; `.planning/v1.31-OPERATOR-BATCH.md` exists carrying an A-1 row with all eight elements including the exact full-path one-line command and the trailing-newline aside; `.planning/ROADMAP.md`'s Phase 140 `**Depends on**:` line carries the named exception and the ROADMAP diff shows that change and no other unintended one; `REQUIREMENTS.md`'s ISSUE-03 row is annotated in place, still `[ ]`, with its original text preserved; ISSUE-01 and ISSUE-02 are ticked.
    - `139-CITATIONS.md` gains section `## 6.` and its sections 0 through 5 are unchanged.
    - `.planning/v1.30-OPERATOR-BATCH.md` is byte-unchanged.
    - No file under `firestarter/` or `firestarter_app/` was written; no `git push` was run.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces; for r in HEAD origin/beta 3085084; do git -C /workspaces/firestarter rev-parse $r:src/proms/eprom.cpp; done | sort -u | wc -l | grep -qx 1 && for r in HEAD origin/beta; do git -C /workspaces/firestarter rev-parse $r:src/proms/memory.cpp; done | sort -u | wc -l | grep -qx 1 && echo D10-PRECONDITION-OK</automated>
    <automated>cd /workspaces; test -z "$(git status --porcelain -- .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md)" && echo FROZEN-CLEAN-OK</automated>
    <automated>cd /workspaces; gh issue view 15 --repo henols/firestarter_prom --json state,comments,labels -q '{state:.state,n:(.comments|length),labels:.labels}'</automated>
    <automated>cd /workspaces; N=$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length'); S=$(gh issue view 15 --repo henols/firestarter_prom --json state -q .state); L=$(gh issue view 15 --repo henols/firestarter_prom --json labels -q '.labels | length'); echo "n=$N state=$S labels=$L"; test "$S" = "OPEN" && test "$L" = "0" && { test "$N" = "0" || test "$N" = "1"; } && echo GH15-STATE-OK</automated>
    <automated>cd /workspaces; if test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "1"; then T=$(mktemp -d); gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments[-1].body' | tr -d '\r' > "$T/posted.md"; diff <(sed -e '$a\' .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md) <(sed -e '$a\' "$T/posted.md") && echo POSTED-EQUALS-REVIEWED-OK; rm -rf "$T"; else echo HOLD-BRANCH-NOTHING-POSTED; fi</automated>
    <automated>cd /workspaces; if test -f .planning/v1.31-OPERATOR-BATCH.md; then grep -qF 'gh issue comment 15 --repo henols/firestarter_prom --body-file .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md' .planning/v1.31-OPERATOR-BATCH.md && grep -qF 'HELD' .planning/v1.31-OPERATOR-BATCH.md && grep -qiF 'trailing newline' .planning/v1.31-OPERATOR-BATCH.md && grep -qE '^\| *A-1' .planning/v1.31-OPERATOR-BATCH.md && echo OPERATOR-BATCH-OK; else echo POST-BRANCH-NO-BATCH-FILE; fi</automated>
    <automated>cd /workspaces; grep -q '^## 6\.' .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md && test -z "$(git status --porcelain -- .planning/v1.30-OPERATOR-BATCH.md)" && test -z "$(git status --porcelain -- firestarter firestarter_app | grep -v '^ M firestarter')" && echo RECORD-AND-DISCIPLINE-OK</automated>
    <automated>cd /workspaces; if test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "0"; then grep -A2 'ISSUE-03' .planning/REQUIREMENTS.md | grep -q '\[ \]' && echo ISSUE-03-CORRECTLY-OPEN; else grep -A2 'ISSUE-03' .planning/REQUIREMENTS.md | grep -q '\[x\]' && echo ISSUE-03-CORRECTLY-TICKED; fi</automated>
  </verify>
  <done>Exactly the authorized branch was taken and proven: either a posted comment byte-equal to the reviewed text with gh#15 at exactly one comment, still OPEN and unlabelled — or nothing posted, with the approval, the freeze values and the exact one-command follow-up parked in `.planning/v1.31-OPERATOR-BATCH.md`, Phase 140's ROADMAP dependency carrying the named exception, and ISSUE-03 annotated open in place.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Purpose |
|---|---|---|
| `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` | 139-01 | gh#15's nine acceptance boxes captured verbatim from the live body |
| `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` | 139-01 (created); 139-04 appends sections 4 and 5; **139-05** appends section 6 | evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, D-10 blob baseline, claim-gate runs, freeze values, delivery record |
| `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` | 139-02 | Phase-139-scoped forbidden-claim gate |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` | 139-03 (authored); 139-04 (freeze); **139-05** (operator-named corrections only) | THE deliverable — the gh#15 comment body, byte for byte |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` | 139-04 (authored + frozen); **139-05** (operator-named corrections only) | the optional, default-skipped issue-body replacement (D-01) |
| `.planning/v1.31-OPERATOR-BATCH.md` | **139-05 — hold branch only** | the parked one-command follow-up (D-08) |

Symbols created in `139-check-claims.py` (139-02): `_HERE`, `_DEFAULT_TARGETS`,
`FIRESTARTER_CLAIMSCAN_TARGETS_V131`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
`_assert_default_targets_are_local()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`.

**No file in `firestarter/` or `firestarter_app/` is created or modified by any plan in this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the operator's real-time wording judgement → a permanent public GitHub comment | The exact boundary ISSUE-03 exists to gate |
| an execution session run under `--auto`/`--chain` → this plan's checkpoint | The documented failure mode; `checkpoint:human-action` typing is the mitigation, not the `autonomous: false` frontmatter flag alone |
| "reviewed" text → "posted" text | The freeze (blob SHA, byte length, committing commit) plus a live byte-diff after any post is what proves the two are identical |
| a wording approval → a posting authorization → a body-edit authorization | Three distinct outward decisions; batching them lets one yes carry two acts the operator did not choose |
| the agent's execution permissions → a state-changing `gh` call | `gh issue` is not allowlisted and state-changing `gh` is additionally classifier-gated; the agent may not weaken either layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-16 | Repudiation | `--auto`/`--chain` silently supplying an approval that was never given | critical | mitigate | Task 1 is `checkpoint:human-action`, not `human-verify` — per this project's own established practice, only `human-action` is presented to the operator regardless of auto-mode |
| T-137-18 | Repudiation | a genuine follow-up silently dropped if posting is deferred | high | mitigate | The v1.31 operator-batch A-1 row is created with the exact follow-up command and the frozen file's blob SHA, in the same task that decides not to post |
| T-139-01 | Repudiation | an automated mode answering any of the three gate questions | critical | mitigate | Inherits T-137-16; the plan objective states the phase must not run under those flags, and question 3 is asked only after question 2 is a yes |
| T-139-02 | Tampering | posted text differing from reviewed text | critical | mitigate | Freeze values re-asserted immediately before the call; post-hoc `diff` under the single named `sed -e '$a\'` normalization; any operator-named correction re-gated, re-asserted and re-frozen with an appended row |
| T-139-03 | Repudiation | posting when implementation is not genuinely untouched, invalidating ROADMAP criterion 4 | high | mitigate | Three-ref blob equality on `eprom.cpp` **and** `memory.cpp` as a fail-closed precondition, re-measured in this task rather than inferred from phase order |
| T-139-06 | Tampering | a body edit destroying the public record | high | mitigate | Default SKIP; separate explicit authorization; read-back assertion that the original nine boxes survive verbatim with a `^- \[ \]` count of exactly 9 |
| T-139-07 | Repudiation | the hold branch evaporating into a gap | high | mitigate | The exact one-line command parked in `.planning/v1.31-OPERATOR-BATCH.md` A-1 **and** the named exception on ROADMAP's Phase 140 `Depends on:` line **and** the in-place ISSUE-03 annotation — three landing sites |
| T-139-08 | Repudiation | requirements ticked prematurely | high | mitigate | Ticking is named exhaustively per branch: ISSUE-01 and ISSUE-02 on wording approval; ISSUE-03 only after a verified post; on hold it stays `[ ]` annotated in place |
| T-139-14 | Elevation of privilege | an agent working around a permission refusal | critical | mitigate | Explicit prohibition on settings edits, `gh api --method POST` workarounds and alternate transports; the two legitimate paths are an operator grant or the operator running the recorded one-liner, and whoever performed the act is named in the record |
| T-139-13 | Repudiation | a correct post read as a failure because of GitHub's appended trailing newline | medium | mitigate | The normalization was named in `139-CITATIONS.md` section 5 before this task ran; a 1-byte difference in that direction is documented as expected |
| T-139-15 | Tampering | a 404 error body diffed as if it were the posted comment | medium | mitigate | Fetch-back uses `gh issue view --json comments -q '.comments[-1].body'`; if an ID-addressed fetch is used, only `repos/OWNER/REPO/issues/comments/<id>` is permitted and its exit code is checked |
| T-139-09 | Information disclosure | token leakage into a committed artifact | medium | mitigate | `gh auth token` never invoked; `--body-file` with a committed path only, never `--body "$(...)"` or a heredoc |
| T-139-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
- Gate: three separately-recorded verbatim answers; question 3 asked only after a "post now"; comment count recorded zero before and after Task 1.
- Preconditions before any outward call: verbatim "post now"; one unique blob SHA for `eprom.cpp` and for `memory.cpp` across the named refs; both frozen artifacts clean and matching their recorded freeze values; comment count still zero.
- Post branch: comment URL recorded; fetch-back byte-diff empty under the named normalization; state `OPEN`, exactly one comment, labels `[]`; negative-flag audit complete with literal argv.
- Body-edit sub-branch: only on an explicit third yes; nine original boxes byte-identical on read-back; `updatedAt` changed; labels and state unchanged. Otherwise recorded as deliberately not done.
- Hold branch: nothing posted; `v1.31-OPERATOR-BATCH.md` A-1 with all eight elements; ROADMAP Phase 140 `Depends on:` amended by hand with the diff inspected; ISSUE-03 annotated in place and still `[ ]`.
- `139-CITATIONS.md` section 6 written; sections 0 through 5 unchanged; `v1.30-OPERATOR-BATCH.md` byte-unchanged.
- Requirements ticked by this plan: **ISSUE-01 and ISSUE-02 on the operator's wording approval; ISSUE-03 only on a verified post.** No other requirement is touched.
</verification>

<success_criteria>
- The gh#15 correction was never posted without a real-time human wording approval, and never posted on a wording approval alone.
- Whatever branch was taken is provable from the record, and both branches leave Phase 140 able to start against reviewed text.
- If posted: what is public is byte-identical to what was reviewed, the issue gained exactly one comment, and it is still OPEN and unlabelled.
- If held: the approval, the freeze values and the exact single closing command are parked in three places, and ISSUE-03 is honestly open rather than quietly ticked.
- No `git push` by an agent, no permission workaround, no sub-repo write, no silent skip.
</success_criteria>

<output>
Create `.planning/phases/139-gh-15-correction-outward/139-05-SUMMARY.md` when done, following
`137-05-SUMMARY.md`'s shape (`139-PATTERNS.md` section 8), including the verbatim-verdict block —
numbered, one item per question asked, each in the operator's own words — followed by the mechanical
before/after proof of what was and was not posted. Set `requirements-completed` to the exact list this
plan ticked: `[ISSUE-01, ISSUE-02]` on the hold branch, `[ISSUE-01, ISSUE-02, ISSUE-03]` on the post
branch. Nothing else.
</output>
