---
phase: 139-gh-15-correction-outward
plan: 04
type: execute
wave: 3
depends_on: ["139-03"]
commits_land_in: .
files_modified:
  - .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md
  - .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md
  - .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
autonomous: true
requirements: [ISSUE-01, ISSUE-02, ISSUE-03]
must_haves:
  truths:
    - "A ready-to-apply gh#15 body replacement exists and is frozen, so the optional second action offered at the operator gate is reviewed text rather than improvised text (D-01)"
    - "gh#15's original acceptance-criteria list survives verbatim inside a collapsed details block — correcting a public spec by making the original unreadable is the failure mode being designed against"
    - "The two non-checkbox body directives are corrected in place by strike-through plus a dated note, never by deletion (D-03)"
    - "Both outward artifacts are frozen with three values each — git blob SHA, byte length, and the committing commit — before any operator gate is presented (D-08)"
    - "The claim gate was re-run RED on a planted violation immediately before the clean default-mode run that licenses the freeze, so the pass is adjacent in time to its own non-vacuity proof"
    - "The one-trailing-newline normalization GitHub applies is named in advance, in writing, before any post is attempted — not discovered afterwards"
  artifacts:
    - path: ".planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md"
      provides: "The complete replacement body for gh#15: a dated AMENDED block, the corrected criteria as a table, the original nine boxes verbatim in a details block, both prose directives struck through with dated corrections, and the 6.25 V ceiling qualifier"
      min_lines: 60
      contains: "details"
    - path: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      provides: "Extended with section 4 (claim-gate runs, planted-first) and section 5 (the freeze table, the named normalization, and the pre-flight re-assertion shape)"
      contains: "## 5."
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md"
      to: ".planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md"
      via: "the collapsed details block embeds the capture's nine checkbox lines byte-for-byte"
      pattern: "^- \\[ \\]"
    - from: ".planning/phases/139-gh-15-correction-outward/139-CITATIONS.md"
      to: ".planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md"
      via: "the freeze table records the comment's blob SHA, byte length and committing commit — the values 139-05 re-asserts immediately before any post"
      pattern: "blob"
  prohibitions:
    - "MUST NOT apply the body amendment to gh#15 — this plan authors and freezes it; gh issue edit is never invoked here, and the edit is default-SKIPPED even at the gate (D-01)"
    - "MUST NOT post a comment, add or remove a label, or close gh#15 — every gh call in this plan is read-only"
    - "MUST NOT hand-retype gh#15's body — fetch it with gh issue view --json body -q .body and edit the fetched text"
    - "MUST NOT delete, paraphrase or reflow either of the two prose directives being corrected — strike through and annotate, keeping the original readable"
    - "MUST NOT embed a comment URL or a substitutable placeholder in the amendment — the comment does not exist yet, and a later substitution would break the freeze-equals-posted guarantee"
    - "MUST NOT weaken, edit or re-target 139-check-claims.py to obtain a green — its git blob must be unchanged from plan 139-02's commit"
    - "MUST NOT accept a default-mode gate pass that was not preceded, in this same task, by an observed planted-violation red"
    - "MUST NOT tick ISSUE-01, ISSUE-02 or ISSUE-03 — this plan ticks NOTHING"
    - "MUST NOT create or modify any file under firestarter/ or firestarter_app/"
    - "MUST NOT run git push"
---

<objective>
Author the optional gh#15 body replacement, then freeze both outward artifacts so the operator gate in
plan 139-05 reviews text that cannot silently change between review and posting.

**This plan, and this phase, must NOT be executed under `--auto`/`--chain`.** Plan 139-05 carries a
`checkpoint:human-action` gate on an irreversible public act. The `checkpoint:human-action` type is
never auto-approved by this project's own execution machinery — but the correct operational answer is:
do not pass either flag for this phase at all.

Purpose: a stranger — or an agent — who implements gh#15 reads the **body**, not the comments. The body
edit is therefore the strongest available fix, and D-01 makes it available at the gate while keeping it
default-skipped, because an outward act beyond what ISSUE-01 through ISSUE-03 ask for must never happen
by default. Drafting it now means that if the operator says yes, they are approving reviewed text rather
than improvised text.

Output: `139-GH15-BODY-AMENDMENT.md`, plus `139-CITATIONS.md` section 4 (claim-gate runs) and section 5
(the freeze).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md
@.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md
@.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md
@.planning/phases/139-gh-15-correction-outward/139-VALIDATION.md
@.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md
@.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 139-GH15-BODY-AMENDMENT.md — the ready-to-apply, default-skipped body replacement</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` (plan 139-03's output) — the amendment must agree with it in every number and every disposition; it is the same correction rendered for a different reader.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` — the nine boxes whose bytes go inside the collapsed block unchanged.
    - `.planning/phases/139-gh-15-correction-outward/139-CONTEXT.md` D-01 and section "Specific Ideas" — the amendment's shape, and the rule that the original must survive verbatim wherever the amendment lands.
    - `.planning/phases/139-gh-15-correction-outward/139-RESEARCH.md` section "gh#15 Live State" (the nine boxes, the disposition table, and the two non-checkbox directives with their exact quoted text) and section "Code Examples", subsection "Optional body amendment".
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` section "No Analog Found" (this is the one artifact with no in-repo precedent — voice from `137-GH12-COMMENT.md`, structure from gh#15's own live body) and S-5 (in-place annotation; strike through rather than delete).
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — the forbidden vocabulary and the two required caveat patterns, both of which apply to this file too.
  </read_first>
  <action>
First fetch gh#15's live body with `gh issue view 15 --repo henols/firestarter_prom --json body -q .body`
and work from the fetched text. Do not hand-retype it. Record its byte length; it was 5964 bytes when
research measured it, and `updatedAt` equal to `createdAt` means it has never been edited.

Write `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` as a complete
replacement body for gh#15 — the exact bytes that would be passed to `gh issue edit --body-file`. No
YAML frontmatter and no planning-artifact header; the file is the payload. It will contain `##` headings
because gh#15's own body does, and those are preserved.

Structure, in this order:

1. A dated AMENDED block at the very top, before gh#15's own first heading, marked with a warning glyph
   and the literal word AMENDED. Two or three sentences: this issue was filed 2026-07-12 and is amended
   by its own author; two numbers in it are wrong and one premise is inverted; the corrections and their
   evidence are in the correction comment on this issue. Refer to the comment by description, never by
   URL — the comment does not exist yet, and embedding a placeholder that plan 139-05 would substitute
   after posting would break the guarantee that the frozen bytes are the posted bytes. Date the block
   with the date it is authored; plan 139-05 must not re-date it.
2. gh#15's own sections, in their original order and with their original headings, edited only where the
   correction requires it.
3. The two non-checkbox directives corrected in place by strike-through plus a dated note, never by
   deletion. In the "Required design" section, the sentence beginning "The handlers may share low-level
   electrical primitives, but each protocol must own its programming state machine and timing constants."
   is struck through and followed by a dated note stating it is replaced: protocol owns shape, the
   database owns the pulse, one shared per-byte pulse-then-verify loop driven by a `const` table keyed by
   `protocol_id`. In the `0x0B` section, the sentence "Do not retain the current generic 500 us legacy
   default." is struck through and followed by a dated note stating it is reversed: 500 us is the correct
   value and `50000 us` is the artifact of a x100 multiplier removed in Phase 57. Say plainly in that
   note that this sentence is the most dangerous line in the issue after the `pulse: 50000 us` default
   itself, because it pre-emptively forbids the right answer.
4. Correct the `0x0B` default itself wherever the body states `pulse: 50000 us`, with the same
   strike-through-plus-dated-note treatment, so a reader scanning the body cannot lift the wrong number.
5. The "Acceptance criteria" section replaced by the corrected list rendered as a markdown table — one
   row per box, columns: the original box quoted verbatim, the disposition (exactly one of kept,
   corrected or replaced), and the reason. Nine rows, matching the comment's table row for row and reason
   for reason. A table rather than a checklist is deliberate: it keeps the count of `- [ ]` lines in this
   file equal to the nine originals preserved below, so that count stays a clean assertion.
6. A collapsed block immediately after the table, opened with a `details` tag and a summary line such as
   "Original acceptance criteria, as filed 2026-07-12 (preserved verbatim)", containing the nine checkbox
   lines from `139-GH15-ORIGINAL-CRITERIA.md` byte for byte. Do not renumber, reword, reflow or
   re-punctuate them. This block is the whole reason a body edit is acceptable at all.
7. A short section carrying the approximately 6.25 V program-VCC ceiling: unreachable on this shield (no
   VCC-raise path on any revision); the work buys timing, pulse-count and verify fidelity and not
   silicon-margin fidelity; and boxes 3, 4 and 5 as originally written imply a fidelity that is not
   available on this hardware. This is ISSUE-02's requirement and it is also what the phase's claim gate
   requires present in every scanned file.

Every number in this file must match `139-GH15-COMMENT.md` exactly: 500 us, the x100 / 252-chip /
Phase 57 attribution, the three histogram lines, 203 of 329 (61.7 %), `3 x 25 x 1000 us = 75 ms`, 16383,
9464, the 50 ms `0x0B` energy cap, and the five table column names. Any permalink it carries is pinned to
a commit SHA taken from `139-CITATIONS.md`, never to a branch name. Do not cite `.planning/PROJECT.md`
and do not cite `138-BASELINE.md`.

Commit with an explicit `git add <the one file> && git commit` — never `git add -A` and never
`git add .`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` exists and its first line is not `---`.
    - The file opens with a block containing the literal word `AMENDED` and a date, and contains no `issuecomment-` fragment anywhere.
    - The file contains an opening and a closing `details` tag, and the checkbox lines between them are byte-identical to the nine in `139-GH15-ORIGINAL-CRITERIA.md`.
    - A count of lines matching `^- \[ \]` in the file returns exactly `9` — the corrected list is a table and contributes none.
    - Both original directives appear verbatim and struck through: the file contains `each protocol must own its programming state machine and timing constants` and `Do not retain the current generic 500 us legacy default`, and each occurrence sits inside a `~~` strike-through span.
    - A nine-row disposition table exists whose disposition column uses only `kept`, `corrected` and `replaced`: a case-insensitive count of those three words in the file returns at least `9` (mirroring the same assertion plan 139-03 makes on the comment), and the count of table rows whose **second column** carries one of the three tokens is exactly `9`. Both are asserted by this task's fourth automated verify leg.
    - Its rows match `139-GH15-COMMENT.md`'s dispositions one for one. This is re-derived independently in Task 2 by a box-anchored cross-check, not self-reported here.
    - The file contains `6.25` and `silicon-margin`.
    - The file contains `500`, `50000`, `203`, `329`, `61.7`, `75 ms`, `16383`, `9464`, `50 ms`, `max_pulses`, `overprogram_factor`, `overprogram_cap_us`, `verify_mode` and `vpp_path`.
    - No `github.com` or `gitlab.com` link in the file lacks a 7-to-40 hex commit SHA (the gh#15 issue URL itself excepted); no `/blob/beta/`, `/blob/main/` or `/blob/gsd/` form appears; `PROJECT.md` and `138-BASELINE.md` are absent.
    - `gh issue view 15` still reports state `OPEN`, comment count `0`, labels `[]`, and `updatedAt` unchanged from the value recorded in `139-CITATIONS.md` section 0 — nothing was applied.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; F=139-GH15-BODY-AMENDMENT.md; test -f "$F" && test "$(head -1 "$F")" != "---" && grep -qF 'AMENDED' "$F" && grep -qF 'details' "$F" && test "$(grep -c '^- \[ \]' "$F")" = "9" && ! grep -qF 'issuecomment-' "$F" && echo STRUCTURE-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; F=139-GH15-BODY-AMENDMENT.md; for p in "6.25" "silicon-margin" "500" "50000" "203" "329" "61.7" "75 ms" "16383" "9464" "50 ms" "max_pulses" "overprogram_factor" "overprogram_cap_us" "verify_mode" "vpp_path"; do grep -qF -- "$p" "$F" || { echo "MISSING: $p"; exit 1; }; done; echo CONTENT-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; F=139-GH15-BODY-AMENDMENT.md; test "$(grep -oiE '\b(kept|corrected|replaced)\b' "$F" | wc -l)" -ge 9 && test "$(awk -F'|' 'NF>=4 && /^[[:space:]]*\|/ {c=tolower($3); if (match(c,/kept|corrected|replaced/)) n++} END{print n+0}' "$F")" = "9" && echo AMENDMENT-DISPOSITIONS-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; F=139-GH15-BODY-AMENDMENT.md; grep -qF 'each protocol must own its programming state machine and timing constants' "$F" && grep -qF 'Do not retain the current generic 500 us legacy default' "$F" && grep -qF '~~' "$F" && ! grep -qF 'PROJECT.md' "$F" && ! grep -qF '138-BASELINE.md' "$F" && ! grep -qE '/blob/(beta|main|gsd)' "$F" && echo DIRECTIVES-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; MISS=$(sed -n 's/^- \[ \] //p' 139-GH15-ORIGINAL-CRITERIA.md | while IFS= read -r b; do grep -qF -- "$b" 139-GH15-BODY-AMENDMENT.md || printf '%s\n' "$b"; done); test -z "$MISS" && echo ORIGINALS-PRESERVED-OK || { echo "NOT PRESERVED VERBATIM:"; printf '%s\n' "$MISS"; exit 1; }</automated>
    <automated>cd /workspaces; test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "0" && test "$(gh issue view 15 --repo henols/firestarter_prom --json updatedAt -q .updatedAt)" = "2026-07-12T09:15:27Z" && echo GH15-UNTOUCHED-OK</automated>
  </verify>
  <done>`139-GH15-BODY-AMENDMENT.md` exists as a complete, ready-to-apply replacement body: dated AMENDED block, gh#15's structure preserved, both prose directives struck through with dated corrections, the wrong `0x0B` default corrected in place, a nine-row disposition table, the original nine boxes preserved byte-for-byte in a collapsed block, and the 6.25 V ceiling section — with gh#15 itself untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Planted-failure re-run, default-mode gate over both artifacts, structural cross-check, and the freeze</name>
  <files>.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md, .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md, .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` — sections 0 through 3 as written by plan 139-01; this task appends sections 4 and 5 in the same style and does not rewrite what is there.
    - `.planning/phases/139-gh-15-correction-outward/139-PATTERNS.md` section 2 in full — 2a (the freeze pair, and the fact that it is three values not two: blob SHA, byte length, and the committing commit), 2c (the executed 122-DELIVERY normalization: GitHub appends exactly one trailing newline; the `sed -e '$a\'` idiom), 2d (the pre-flight re-assertion table shape to copy into the register), and 2b (what to reject from 137-05's never-executed recipe).
    - `.planning/phases/139-gh-15-correction-outward/139-02-SUMMARY.md` section "Claim gate — non-vacuity record" — the four runs that established the gate can fail.
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — so a gate failure is understood rather than worked around.
    - `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` and `139-GH15-BODY-AMENDMENT.md` — the two files being cross-checked and frozen.
  </read_first>
  <action>
Three things, in this order: prove the gate can still fail, run it for real, then freeze.

**Step 1 — planted-failure re-run, MUST FAIL.** Copy `139-GH15-COMMENT.md` to the session scratchpad and
append a line containing two forbidden phrases, for example one asserting the firmware is
datasheet-conformant and algorithm-accurate. Run `python3 139-check-claims.py` against the scratch copy.
It MUST exit 1 and name both labels. Delete the scratch copy. Nothing planted is committed. This run
exists so that Step 2's green is adjacent in time to its own non-vacuity proof — plan 139-02's Run 1 is a
different session and a different file set.

**Step 2 — the real run, default mode.** From inside the phase directory run `python3
139-check-claims.py` with no arguments, so `_DEFAULT_TARGETS` resolves to both outward artifacts. It MUST
exit 0 and its PASS line MUST name both `139-GH15-COMMENT.md` and `139-GH15-BODY-AMENDMENT.md`. If it
fails, fix the artifact's wording — never the gate. Confirm the gate itself is unmodified: `git status
--porcelain -- 139-check-claims.py` empty, and its blob SHA equal to the value plan 139-02 recorded.

**Step 3 — structural cross-check between the two artifacts.** This is **not** a read-and-confirm step: the
row-for-row agreement is re-derived mechanically by this task's own automated verify leg, which walks the nine
boxes in `139-GH15-ORIGINAL-CRITERIA.md`, finds the table row quoting each one in each file, extracts the
**second column's** `kept`/`corrected`/`replaced` token from each, and fails naming the row number, the box
and both values on any mismatch, any missing row, or any row whose disposition cell carries no token. That
leg depends on the mandated three-column order (original box · disposition · reason) in both files, so do
not reorder the columns. Also assert, and record the result of each: every number listed in Task 1's action
appears in both files; the nine original boxes appear verbatim in both; and neither file carries the false
"all three constants disagree with the modal value" sentence in any wording. If the two disagree, the
comment is authoritative and the amendment is corrected to match — then re-run the leg until it is green.

**Step 4 — freeze both (D-08's precondition: the gate reviews frozen text).** Commit both artifacts,
then record three values per file — `git rev-parse
HEAD:<path>` for the blob SHA, `wc -c < <path>` for the byte length, and the committing commit's full
SHA — and assert `git status --porcelain -- <path>` is empty for each. Three values, not two; that is the
project's standing freeze record.

**Step 5 — extend `139-CITATIONS.md`.** Append two new numbered sections in the existing style, without
rewriting sections 0 through 3.

Section 4, "Claim gate": the planted-failure re-run and the real run, each with the literal command,
literal stdout and exit code, and the planted line's exact text reproduced so the exercise stays
re-creatable. State in words that the red preceded the green.

Section 5, "Freeze": a table in the `122-DELIVERY.md` pre-flight shape with columns File, Frozen blob
SHA, Byte length, Committing commit, and `git status --porcelain` (expected empty), one row per artifact.
Then a short paragraph naming, **in advance**, the single normalization that any post-hoc byte-diff will
apply: CRLF stripped, and both sides collapsed to exactly one trailing newline using the
`diff <(sed -e '$a\' A) <(sed -e '$a\' B)` idiom — because GitHub appends exactly one trailing newline
that the committed file does not already end with, measured across four outward calls in the v1.22
delivery record. State plainly that a byte-length difference of exactly 1 in that direction is the
expected result and not a mismatch, so nobody reads a correct post as a failure at the worst possible
moment. Also record that 137-05's stricter "must be identical" wording is deliberately rejected: it was
written but never executed, and it would fail on a correct post.

Close by re-asserting the two standing preconditions and recording both: gh#15 still `OPEN` with zero
comments and no labels, and `firestarter/src/proms/eprom.cpp` still one unique blob across `HEAD`,
`origin/beta` and `3085084`.

Commit with an explicit path list.
  </action>
  <acceptance_criteria>
    - The planted-failure re-run exited 1 and named both planted labels; its exact planted line is reproduced in `139-CITATIONS.md` section 4.
    - `python3 139-check-claims.py` with no arguments exits 0 and its PASS line names both `139-GH15-COMMENT.md` and `139-GH15-BODY-AMENDMENT.md`.
    - `git status --porcelain -- 139-check-claims.py` is empty and its blob SHA matches the value recorded by plan 139-02 — the green was not bought by weakening the gate.
    - `139-CITATIONS.md` gains sections `## 4.` and `## 5.` and its existing sections 0 through 3 are unchanged (a `git diff` of the previous commit shows additions only within the appended region).
    - Section 5's table carries, for each of the two artifacts, a blob SHA, a byte length, a committing commit SHA, and an empty `git status --porcelain` column.
    - Section 5 names the one-trailing-newline normalization and the `sed -e '$a\'` idiom in advance, states that a 1-byte difference in that direction is expected, and records the rejection of the never-executed "must be identical" wording.
    - Both artifacts are committed and clean: `git status --porcelain -- 139-GH15-COMMENT.md 139-GH15-BODY-AMENDMENT.md` is empty.
    - gh#15 still reports `OPEN`, comment count `0`, labels `[]`; `eprom.cpp` still yields exactly one unique blob SHA across the three refs.
    - Neither artifact carries the false modal sentence, in any wording.
    - The box-anchored disposition cross-check leg exits 0 printing `DISPOSITIONS-AGREE-OK`: for all nine original boxes, the disposition token in `139-GH15-COMMENT.md`'s table row equals the one in `139-GH15-BODY-AMENDMENT.md`'s, with nine rows matched in each. Its output is recorded in `139-CITATIONS.md` section 4 alongside the gate runs.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; T=$(mktemp -d); cp 139-GH15-COMMENT.md "$T/planted.md"; printf '\nThis firmware is datasheet-conformant and algorithm-accurate.\n' >> "$T/planted.md"; out=$(python3 139-check-claims.py "$T/planted.md"); rc=$?; printf '%s\n' "$out"; rm -rf "$T"; test "$rc" -eq 1 && printf '%s' "$out" | grep -qF 'datasheet-conformant' && printf '%s' "$out" | grep -qF 'algorithm-accurate' && echo REPLANTED-RED-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; out=$(python3 139-check-claims.py); rc=$?; printf '%s\n' "$out"; test "$rc" -eq 0 && printf '%s' "$out" | grep -qF 'PASS:' && printf '%s' "$out" | grep -qF '139-GH15-COMMENT.md' && printf '%s' "$out" | grep -qF '139-GH15-BODY-AMENDMENT.md' && echo DEFAULT-MODE-GREEN-OK</automated>
    <automated>cd /workspaces/.planning/phases/139-gh-15-correction-outward; d() { grep -F -- "$2" "$1" | grep -E '^[[:space:]]*\|' | head -1 | awk -F'|' '{c=tolower($3); if (match(c,/kept|corrected|replaced/)) print substr(c,RSTART,RLENGTH); else print "NONE"}'; }; N=0; BAD=0; while IFS= read -r b; do t=${b%.}; a=$(d 139-GH15-COMMENT.md "$t"); m=$(d 139-GH15-BODY-AMENDMENT.md "$t"); N=$((N+1)); if [ -z "$a" ] || [ -z "$m" ] || [ "$a" = NONE ] || [ "$a" != "$m" ]; then echo "ROW $N MISMATCH comment='$a' amendment='$m' :: $t"; BAD=1; fi; done < <(sed -n 's/^- \[ \] //p' 139-GH15-ORIGINAL-CRITERIA.md); [ "$N" = "9" ] && [ "$BAD" = "0" ] && echo DISPOSITIONS-AGREE-OK</automated>
    <automated>cd /workspaces; test -z "$(git status --porcelain -- .planning/phases/139-gh-15-correction-outward/139-check-claims.py .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md)" && echo FROZEN-CLEAN-OK</automated>
    <automated>cd /workspaces; C=.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md; grep -q '^## 4\.' "$C" && grep -q '^## 5\.' "$C" && grep -qF "sed -e '\$a" "$C" && grep -qiF 'trailing newline' "$C" && echo CITATIONS-EXTENDED-OK</automated>
    <automated>cd /workspaces; for f in 139-GH15-COMMENT.md 139-GH15-BODY-AMENDMENT.md; do p=.planning/phases/139-gh-15-correction-outward/$f; s=$(git rev-parse HEAD:"$p"); n=$(wc -c < "$p"); grep -qF "$s" .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md || { echo "blob $s for $f not recorded"; exit 1; }; grep -qF "$n" .planning/phases/139-gh-15-correction-outward/139-CITATIONS.md || { echo "byte length $n for $f not recorded"; exit 1; }; done; echo FREEZE-VALUES-RECORDED-OK</automated>
    <automated>cd /workspaces; test "$(gh issue view 15 --repo henols/firestarter_prom --json comments -q '.comments | length')" = "0" && test "$(for r in HEAD origin/beta 3085084; do git -C /workspaces/firestarter rev-parse $r:src/proms/eprom.cpp; done | sort -u | wc -l)" = "1" && ! grep -qiE 'all three.*disagree.*modal' .planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md .planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md && echo PRECONDITIONS-OK</automated>
  </verify>
  <done>Both outward artifacts are gate-clean in default mode — with the gate itself unmodified and freshly seen red on a planted violation first — proven mutually consistent by a box-anchored disposition cross-check rather than by self-report, frozen with blob SHA, byte length and committing commit each, and recorded in `139-CITATIONS.md` sections 4 and 5 alongside the normalization the post-hoc byte-diff will apply.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Owner plan | Purpose |
|---|---|---|
| `.planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` | 139-01 | gh#15's nine acceptance boxes captured verbatim from the live body |
| `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` | 139-01 (created); **139-04** appends sections 4 and 5; 139-05 appends section 6 | evidence register: gh#15 before-state, pinning strategy, per-anchor content-verified transcript, D-10 blob baseline, claim-gate runs, freeze values, delivery record |
| `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` | 139-02 | Phase-139-scoped forbidden-claim gate |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-COMMENT.md` | 139-03 (authored); **139-04** (gate cross-check + freeze) | THE deliverable — the gh#15 comment body, byte for byte |
| `.planning/phases/139-gh-15-correction-outward/139-GH15-BODY-AMENDMENT.md` | **139-04** (authored + frozen) | the optional, default-skipped issue-body replacement (D-01) |
| `.planning/v1.31-OPERATOR-BATCH.md` | 139-05 — **hold branch only** | the parked one-command follow-up (D-08) |

Symbols created in `139-check-claims.py` (139-02): `_HERE`, `_DEFAULT_TARGETS`,
`FIRESTARTER_CLAIMSCAN_TARGETS_V131`, `FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`,
`_assert_default_targets_are_local()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`.

**No file in `firestarter/` or `firestarter_app/` is created or modified by any plan in this phase.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| "reviewed" text → "posted" text | The freeze (blob SHA, byte length, committing commit) plus a post-hoc byte-diff under one named normalization is what proves the two are identical |
| a filed public spec → its amended replacement | Rewriting a spec without the original visible destroys the public record; the collapsed verbatim block is the mitigation |
| a gate's green in one session → the same gate's green in another | The gate is re-shown red on a planted violation in this task, adjacent in time to the pass that licenses the freeze |
| an execution session run under `--auto`/`--chain` → this phase's outward act | The gate in 139-05 is `checkpoint:human-action`; the `autonomous: false` frontmatter flag alone is not self-protecting |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-02 | Tampering | posted text differing from reviewed text | critical | mitigate | Blob SHA, byte length and committing commit recorded per artifact before any gate; 139-05 re-asserts them immediately before the call and byte-diffs afterwards |
| T-139-06 | Tampering | a body edit destroying the public record | high | mitigate | The nine original boxes preserved byte-for-byte in a collapsed block, asserted mechanically; both prose directives struck through rather than deleted; the edit stays default-SKIP |
| T-139-05 | Repudiation | a vacuous claim gate green-lighting an overclaim | critical | mitigate | Planted-failure re-run RED immediately before the default-mode green, with the gate's own blob asserted unchanged so the green cannot have been bought by weakening it |
| T-139-12 | Tampering | the comment and the amendment drifting apart, so the operator approves one and a different correction is published in the other | high | mitigate | Step 3's structural cross-check: same nine originals, same nine dispositions, same numbers in both; the comment is authoritative on conflict |
| T-139-13 | Repudiation | a correct post read as a failure because of GitHub's appended trailing newline | medium | mitigate | The normalization is named in `139-CITATIONS.md` section 5 in advance, with the `sed -e '$a\'` idiom and the explicit statement that a 1-byte difference in that direction is expected |
| T-139-03 | Repudiation | freezing against an implementation that has already moved | high | mitigate | Three-ref blob equality on `eprom.cpp` re-asserted at the end of this plan |
| T-139-01 | Repudiation | `--auto`/`--chain` supplying an approval that was never given | critical | mitigate | Inherits T-137-16. This plan makes only read-only `gh` calls; the gate lives in 139-05 and is `checkpoint:human-action` |
| T-139-09 | Information disclosure | token leakage into a committed artifact | medium | mitigate | `gh auth token` never invoked; body fetched with `--json body -q .body`, JSON-decoded, never shell-interpolated |
| T-139-SC | Tampering | npm/pip/cargo installs | high | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
- Amendment structure: dated AMENDED block; exactly nine `- [ ]` lines, all inside the collapsed block and byte-identical to the capture; both prose directives verbatim and struck through; nine-row disposition table matching the comment; no `issuecomment-` fragment.
- Amendment content: 6.25 and silicon-margin present; every shared number present; no `PROJECT.md`, no `138-BASELINE.md`, no branch-name permalink.
- Gate: planted re-run RED naming both labels, then default-mode run GREEN naming both files, with `139-check-claims.py` proven unmodified.
- Freeze: both artifacts committed and clean; blob SHA, byte length and committing commit recorded for each in `139-CITATIONS.md` section 5.
- Normalization named in advance, with the `sed -e '$a\'` idiom and the expected 1-byte direction.
- Preconditions: gh#15 `OPEN`, zero comments, `updatedAt` unchanged; `eprom.cpp` one unique blob across three refs.
- Requirements ticked by this plan: **NONE.**
</verification>

<success_criteria>
- Both outward artifacts exist, agree with each other, pass the phase's own claim gate in default mode, and are frozen with three recorded values each.
- The operator gate in 139-05 can be presented against text that is provably identical to what would be published.
- gh#15 is untouched: no comment, no edit, no label, `updatedAt` unchanged.
- No `git push`, no sub-repo write, no gate weakened, no fixture committed.
</success_criteria>

<output>
Create `.planning/phases/139-gh-15-correction-outward/139-04-SUMMARY.md` when done, following
`137-05-SUMMARY.md`'s shape (`139-PATTERNS.md` section 8). Its `provides:` block carries both artifacts'
freeze values inline (blob SHA and byte length), so a reader never has to open a file to know what was
frozen. Set `requirements-completed: []` — this plan ticks nothing.
</output>
