---
phase: 143-host-timeout-progress-pulse-override
plan: 10
type: execute
wave: 5
depends_on: ["143-01", "143-02", "143-03", "143-04", "143-05", "143-06", "143-07", "143-08", "143-09"]
commits_land_in: [meta, firestarter, firestarter_app]
files_modified:
  - /workspaces/firestarter/CLAUDE.md
  - /workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md
  - /workspaces/.planning/REQUIREMENTS.md
autonomous: false
requirements: [HOST-01, HOST-02, HOST-03, HOST-04, HOST-05]
must_haves:
  truths:
    - "The firmware's own algorithm-handler documentation carries D-06's two-dimension non-claim: intra-block write progress is emitted on the EPROM path only and delivered on leonardo only"
    - "Cold flash and RAM are measured on uno, uno328pb and leonardo and recorded with the delta versus the Phase 138 baseline, and leonardo still builds under its 28672 B ceiling (D-22)"
    - "The phase record states the honest headline, every non-claim, the three blocking-finding reconciliations, the findings register and the hand-offs"
    - "The roadmap's 'independent of Phases 140-142 (different repo)' framing is recorded as CORRECTED, and the correction is handed to Phase 146 / CLOSE-04 rather than applied to the roadmap prose here (D-01)"
    - "native_trace_v131 is recorded as RED by design with its values, and the record states this phase added ZERO frames to the frozen trace because that suite pins millis() to 0 (D-24)"
    - "All five HOST-* requirements are flipped in both the checkbox list and the coverage table, once, after every piece of evidence exists"
  artifacts:
    - path: "/workspaces/firestarter/CLAUDE.md"
      provides: "D-06's two-dimension non-claim on the 0x07/0x08/0x0B algorithm-handler rows, plus the --pulse-us and CAP-03 facts a firmware reader needs"
      contains: "leonardo"
    - path: "/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md"
      provides: "the phase record: honest headline, BF-1/BF-2/BF-3 reconciliations, the padding rule in prose, every non-claim, cold size figures, the D-25 inventory, the findings register and the hand-offs"
      min_lines: 200
    - path: "/workspaces/.planning/REQUIREMENTS.md"
      provides: "HOST-01..HOST-05 flipped in the Host checkbox list and in the coverage table"
      contains: "HOST-05"
  key_links:
    - from: "/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md"
      to: "/workspaces/.planning/REQUIREMENTS.md"
      via: "each HOST-* requirement's evidence named in the record before its checkbox is flipped"
      pattern: "HOST-0"
    - from: "/workspaces/firestarter/CLAUDE.md"
      to: "/workspaces/firestarter/src/proms/eprom.cpp"
      via: "the algorithm-handler rows describing the guarded emission the source actually contains"
      pattern: "SERIAL_ON_IO"
---

<objective>
Close the phase: reconcile the firmware's own documentation, measure cold size, write the record, and flip the
five `HOST-*` requirements once.

## The honest headline, and what it is not

**"A long write now reports what it is doing, and a failed byte now reports as a failed byte."** Two
user-visible truths, both provable off-hardware. **Not** "writes are faster" and **not** "writes are more
reliable" — this phase changed neither.

## Requirement ownership — this is the ONLY plan that flips anything

`requirements:` declares all five `HOST-*` IDs here and nowhere else. Plans 143-01 through 143-09 all carry
`requirements: []` on purpose, because `execute-plan.md`'s `update_requirements` step calls
`requirements.mark-complete` unconditionally with the executing plan's own frontmatter IDs and performs a real
regex flip. Executors in this project have marked multi-plan requirements Complete from a single plan four
times in one prior phase; concentrating the flip here is the standing countermeasure (the convention plans
141-05/141-09 and 142-01/142-07 used).

Which plan produced which evidence, for the record's requirement table:

| Requirement | Evidence lives in |
|-------------|-------------------|
| HOST-01 | 143-01 (firmware budget arithmetic + native cases), 143-02 (CAP-03 decode + byte-layout cases), 143-03 (the ack), 143-04 (the write-path timeout, the 120 s fallback, D-12's negative proof) |
| HOST-02 | 143-05 (the guarded firmware emission + cadence cases), 143-06 (the host DATA branch, offset, no-rebuild, no-rewind), 143-08 (the guard's source contract) |
| HOST-03 | 143-04 (the timeout that stopped the transport error firing first) **and** 143-09 (the render plus the hint). Neither alone satisfies it |
| HOST-04 | 143-04 (the `write_eprom` transport) **and** 143-07 (the CLI flag and the D-17 report line) |
| HOST-05 | 143-07 (parse-time refusal at exit 2, no port opened, the no-flag regression guard, the write-only scope) |

## D-01 — the roadmap correction is RECORDED here, not applied here

The roadmap's "Depends on: Phase 138 ... Independent of Phases 140-142 (different repo)" line and the
milestone's matching "can run in parallel with them" sequencing note are **factually wrong for the shipped
decision**: this phase depends on Phase 141's loop (the thing it emits from) and Phase 140's table (the thing
the budget is computed from), and it is **dual-repo**. Hand-off H2 predicted exactly this and required it be
named before planning rather than discovered during it. **Recording the correction is this phase's obligation;
amending `ROADMAP.md` / `PROJECT.md` milestone prose is Phase 146 / CLOSE-04's**, alongside the C3, F-140-05,
F-140-07 and H3 corrections already queued there. **Do not edit the roadmap's or PROJECT.md's prose in this
plan.**

**Roadmap-edit hazard, if any tool touches `ROADMAP.md` during this plan's completion:** the GSD roadmap and
requirements verbs re-normalise the **whole** file, and `phase.complete` has been observed clobbering an
**unrelated** phase's `**Plans:**` line. **Snapshot `ROADMAP.md` and `REQUIREMENTS.md` with
`git diff`/`git stash`-free copies before any such call and diff afterwards**, and revert any hunk outside the
intended lines by hand.

**Repo notes:** this plan **commits** in **two** repos and **reads** in a third. The
`firestarter/CLAUDE.md` change is a **docs-only commit inside the firmware submodule** (the measured Phase 142
house pattern — 142-07 landed the same file as its own docs-only commit, not folded into a code change). The
record and the requirement flips are **meta** commits. `commits_land_in` names both plus meta because a
worktree leaves submodules empty and `files_modified` alone under-detects.

`commits_land_in` **also names `firestarter_app`, read-only.** Task 2's whole-phase evidence sweep is run from
`/workspaces/firestarter_app` — the full host pytest suite with coverage, `ruff check`, `ruff format --check`
and `check_mypy_watermark.py` (L-3) — and those four figures are recorded in `143-HOST-RECORD.md` as **core
phase-closure evidence**, not as a courtesy. None of it runs against an empty submodule, and the record cannot
honestly be written without it. `143-CONTEXT.md`'s domain section states the rule verbatim: *a plan that only
reads a submodule still names it*. **Write nothing in `firestarter_app` and commit nothing there** — a
non-empty `git status --porcelain` in `/workspaces/firestarter_app` at the end of this plan is a defect, not an
expected state.

**L-1:** `tests/test_flash_path_record_sync.py` asserts the **whole firmware repo** is
`git status --porcelain` clean. Commit the `CLAUDE.md` change before running the full firmware pytest suite.

**L-1b — and the HOST suite has the same coupling, measured this session.** The host suite's
`tests/test_py32_flash_map_host.py::TestLinkerScriptParityFailsClosedOnBadInput::test_planted_mutated_config_origin_is_detected`
asserts `_git_porcelain(FW_ROOT) == ""` for the **sibling** `/workspaces/firestarter` (`tests/fw_presence.py`,
`@requires_fw`, and the firmware repo is present here so it runs rather than skips). An *untracked* file is
enough to turn it RED. Therefore **the `CLAUDE.md` commit comes before the host sweep as well as before the
firmware suite** — task 2 must not run the host figures with an uncommitted firmware tree. This plan is wave 5
and every firmware plan has committed by then, so `git -C /workspaces/firestarter status --porcelain` **must be
empty** here: unlike waves 1-4 there is no legitimate deselection, and if any wave-1-to-4 plan recorded a
deselection of that test, **this plan's un-deselected full-suite leg is the re-run that discharges it** — say
so explicitly in the record.

**L-1c — and the `1547` baseline itself is cross-repo, measured this session.** With the
`FIRESTARTER_FW_ROOT` seam pointed at an empty directory the full host suite reports
**`1504 passed, 43 skipped`** instead of **`1547 passed`**: 43 `@requires_fw`-gated tests across 11 modules
(`tests/fw_presence.py`) skip when the sibling firmware repo is absent. The host figures this plan records as
phase-closure evidence are therefore only meaningful with **both** submodules populated — which is the second
reason `commits_land_in` names `firestarter_app` *and* `firestarter` alongside `meta`. If the recorded count
lands 43 short, that is a populated-checkout problem, not a code problem, and the record must not absorb it as
a real regression.

**L-2:** every `pio` invocation MUST run with cwd `/workspaces/firestarter`. From `/workspaces` PlatformIO
aborts on the meta repo's untracked `platformio.ini` (duplicate `[platformio]` section) — measured.

**L-3:** host figures come from `/workspaces/firestarter_app/.venv/ci-replica/bin/python` (3.11.15) with
`-o addopts=""`.

**D-22:** the bar is **"it builds"**. `size_baseline.json` is **read-only**, MERGE-05 stays RED and is
Phase 144 / TEST-08's, there is **no** predictions artifact, and there is **no** shrink ladder unless a build
actually overruns. Measure **cold** and record.

**D-24:** `native_trace_v131` stays RED. Record its values and state that this phase added **zero** frames to
the frozen trace, because that suite pins `millis()` to `AlwaysReturn(0)` so the emission never fires there —
Phase 144 / TEST-06 will find zero D-02-attributable strobes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-host-timeout-progress-pulse-override/143-CONTEXT.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-RESEARCH.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-PATTERNS.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-VALIDATION.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-01-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-02-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-03-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-04-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-05-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-06-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-07-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-08-SUMMARY.md
@.planning/phases/143-host-timeout-progress-pulse-override/143-09-SUMMARY.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile firestarter/CLAUDE.md as a docs-only commit — D-06's two-dimension non-claim on the 27C rows</name>
  <files>/workspaces/firestarter/CLAUDE.md</files>
  <read_first>
    - `/workspaces/firestarter/CLAUDE.md` — the whole §"Algorithm Handlers" section, especially the
      `0x07`, `0x08` and `0x0B` rows. Note the voice each row already uses: the **"Honest headline:"** /
      **"Boundary:"** shape, the corrected `99998 us` energy-cap derivation on the `0x0B` row (F-141-10 — this
      file is the corrected source, **not** `141-CONTEXT.md`, whose `99999` figure is stale), the
      `command_done()`-is-a-source-contract-not-a-behavioural-claim sentence on all three rows, and the
      "proven only in the emitted control-register stream, never on a part" boundary on the `0x08` row. Also
      read §"Wire Protocol" and any section describing `MSG_OK_READY`'s ack shape.
    - `/workspaces/firestarter/src/proms/eprom.cpp` — plan 143-05's guarded emission as it actually shipped,
      so the documentation describes the code rather than the plan.
    - `/workspaces/firestarter/src/firestarter.cpp` — plan 143-03's pack block as it actually shipped, for the
      ack-shape sentence.
    - `/workspaces/firestarter/include/eprom_budget.h` — plan 143-01's padding rule in prose, so the
      `CLAUDE.md` sentence about the advertised budget matches the header rather than restating it loosely.
    - `/workspaces/.planning/phases/142-high-voltage-routing/142-07-SUMMARY.md` — the measured house pattern
      for this exact file: a **docs-only** commit, not folded into a code change. Follow it.
    - `/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-05-SUMMARY.md` and
      `143-08-SUMMARY.md` — the shipped non-claim wording and the source-contract framing, so the two
      documents agree.
  </read_first>
  <action>
    Edit `firestarter/CLAUDE.md` only, and commit it **alone** as a docs-only commit.

    **On the `0x07`, `0x08` and `0x0B` algorithm-handler rows**, add D-06's non-claim in the rows' existing
    **"Honest headline:" / "Boundary:"** voice, with **both** dimensions:
    - intra-block write progress (`MSG_DATA_PROGRESS`, `0xE0`) is emitted from inside the per-byte program
      loop on the **EPROM path only** — flash, EEPROM (`0x0D`), SRAM and every other family keep today's
      block-granularity progress and today's silent-stall behaviour;
    - and it is **delivered on `leonardo` only**. On `SERIAL_ON_IO` targets (`uno`, `uno328pb`) the emission is
      **compiled out**, because `rurp_set_programmer_mode()` tears the UART down for the whole
      programmer-mode window and `rurp_log_id` defers frames into a 4-slot buffer whose overflow is a silent
      drop — filling it would drop the subsequent `MSG_ERR_MAX_PULSES` frame and turn a program failure into a
      host transport timeout. This is **structural**, not a choice.
    - Add the boundary sentence in the file's own idiom: the guard is proven by a **source contract**
      (`tests/test_progress_emission_is_leonardo_only.py`), never by a behavioural test, because
      `src/boards/uno_rurp_shield.cpp` is compiled in no native environment and the native capture stub has no
      `com_mode` gate. Say that plainly, exactly as the rows already say it about `command_done()`.
    - Add the cadence fact: the emission is **time**-gated at `EPROM_PROGRESS_EMIT_INTERVAL_MS` (1000 ms),
      not byte-counted, and it emits an **absolute** chip address plus `handle->mem_size` — the same payload
      contract `mem_util_blank_check` uses, so `0xE0` has exactly one meaning.

    **In the wire-protocol / ack section**, record CAP-03 and the CAP-02 port in one short block:
    `MSG_OK_READY` now carries `[buffer_size u16 BE][hw_revision u8][ver_len u8][ver bytes][write_budget_s
    u16 BE]`; the budget is a **`uint16_t` of seconds**, computed by the firmware from the `eprom_params`
    table plus the live `pulse_delay`, and it is **already padded** — twice the ceil-rounded pulse-only worst
    case plus two seconds — because only the firmware knows `delay(500)`, the final verify pass, the per-pulse
    settle and the serial time; point at `include/eprom_budget.h` for the rule rather than restating it. Note
    that a non-EPROM protocol advertises **0**, which the host reads as "not advertised". Note that CAP-02 was
    **ported** into this branch from `origin/beta` commit `13eb350`, and that before that port a v1.31
    firmware build could not connect to the v1.31 host at all.

    **In the algorithm-handler rows or an adjacent note**, record the `--pulse-us` interaction as a firmware
    reader needs it: the host may now override `pulse-delay` per run within `1..65535` us (minipro parity,
    **not** a wire-type limit — `extract_long` parses the field into an unclamped `uint32_t`, which is
    Phase 146 / CLOSE-04's to reconcile), and `configure_eprom`'s `energy_cap_us`-keyed pre-flight
    `MSG_ERR_PULSE_TOO_WIDE` refusal is the **firmware-side backstop** that fires before any high voltage is
    enabled — which is why the host deliberately mirrors no table value.

    Correct nothing else in this file, and do **not** touch the `99998 us` derivation or any
    `command_done()` sentence. If a claim already present in the file is now false, name it in the SUMMARY as
    a finding rather than silently rewriting a locked derivation.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import pathlib, re
t = pathlib.Path('CLAUDE.md').read_text()
low = t.lower()
for needle, why in [
    ('serial_on_io', 'the compiled-out dimension of D-06 must be named'),
    ('leonardo', 'the delivered-on-leonardo-only dimension must be named'),
    ('eprom_progress_emit_interval_ms', 'the cadence constant must be named'),
    ('msg_data_progress', 'the emitted id must be named'),
    ('write_budget_s', 'CAP-03 must be documented in the ack layout'),
    ('eprom_budget.h', 'the padding rule must be pointed at, not restated'),
    ('13eb350', 'the ported CAP-02 provenance must be cited'),
    ('--pulse-us', 'the host override must be recorded for a firmware reader'),
    ('msg_err_pulse_too_wide', 'the firmware-side backstop must be named'),
    ('minipro', 'the bound provenance must be recorded as parity, not a type limit'),
    ('test_progress_emission_is_leonardo_only', 'the source-contract gate must be named'),
]:
    assert needle in low, (needle, why)
assert '99998' in t, 'the corrected energy-cap derivation must survive untouched'
print('OK CLAUDE.md carries every required fact')
" && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/CLAUDE.md` names **both** dimensions of D-06's non-claim on the `0x07`/`0x08`/`0x0B` rows,
      in the file's existing "Honest headline:" / "Boundary:" voice, and states that the `SERIAL_ON_IO`
      dimension is structural rather than a choice.
    - It names `tests/test_progress_emission_is_leonardo_only.py` as a **source contract** and states why a
      behavioural test is impossible (`src/boards/uno_rurp_shield.cpp` in no native `build_src_filter`; no
      `com_mode` gate in the native capture stub).
    - It records the cadence (`EPROM_PROGRESS_EMIT_INTERVAL_MS`, 1000 ms, time-gated not byte-counted) and the
      one-contract payload (absolute address plus `handle->mem_size`).
    - It records CAP-03's ack layout, that the budget is `uint16_t` seconds, that it is **already padded**, and
      that a non-EPROM protocol advertises `0`; it points at `include/eprom_budget.h` for the padding rule
      rather than restating it.
    - It cites `13eb350` as CAP-02's provenance and records that a pre-port v1.31 build could not connect at
      all.
    - It records `--pulse-us`'s range as **minipro parity, not a wire-type limit**, names the unclamped
      `extract_long` as Phase 146 / CLOSE-04's, and names `MSG_ERR_PULSE_TOO_WIDE` as the firmware-side
      backstop firing before any high voltage.
    - The `99998 us` derivation and every `command_done()` sentence are byte-unchanged.
    - `git log --stat -1` in `/workspaces/firestarter` shows **only** `CLAUDE.md` — a docs-only commit, the
      measured Phase 142 house pattern.
    - After committing, `cd /workspaces/firestarter && python3 -m pytest tests/ -o addopts="" -q` is green
      (whole-repo porcelain is clean because the commit came first — L-1).
  </acceptance_criteria>
  <done>`firestarter/CLAUDE.md` documents the guarded emission, both dimensions of its non-claim, the CAP-02 port and CAP-03's ack layout, and the `--pulse-us` interaction — as a docs-only commit that leaves every locked derivation untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Measure cold flash, RAM and warnings on all three AVR targets and record every gate verdict verbatim</name>
  <files>/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md</files>
  <read_first>
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — the `avr_targets` figures to compare
      against (`uno` flash 23954 / RAM 1573; `uno328pb` flash 24004 / RAM 1579; `leonardo` flash 26016 / RAM
      2014, ceiling 28672), the `native_envs` pins (`native` and `native_nodevtools` at 141 cases / 17
      suites / `all_passed`), the `warnings` block (all three AVR targets at `total: 0`; `native` and
      `native_nodevtools` at a 1166 watermark), the `policy` (`avr_rule: "== 0"`,
      `native_rule: "<= total_watermark"`), and `meta.warm_vs_cold_correction` (the watermark is the **COLD**
      figure; a warm re-run measures 998, and a prior baseline was silently a warm measurement).
      **Read-only this phase — do not edit it.**
    - `/workspaces/firestarter/scripts/check_size_baseline.py` and
      `/workspaces/firestarter/scripts/check_build_warnings.py` — how each is invoked and what each exit code
      means. Note that `NATIVE_ENVS` is hardcoded to `("native", "native_nodevtools")` and an unknown env name
      raises an **uncaught `KeyError`** (exit 1, a false regression signal) — so **never** pass any
      `native_*_v131` name to either script (F-138-05, inherited and accepted, owner `henols`).
    - `/workspaces/.planning/phases/142-high-voltage-routing/142-VPP-RECORD.md` — **F-142-08** (the `leonardo`
      headroom handed to this phase: **2130 B**, 142 B narrower than the Phase 141 tip's 2272 B) and §1.3.
      Also **H4** (`0xBF` still free).
    - `/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-01-SUMMARY.md`,
      `143-03-SUMMARY.md` and `143-05-SUMMARY.md` — the per-plan size figures already recorded, so this task
      reconciles rather than re-invents. RESEARCH's `[ASSUMED]` estimates were A2 (CAP-02 ~+34 B) and A3 (the
      emission 40-120 B on `leonardo`); compare the measured total against both and say whether each
      assumption held.
    - `/workspaces/.planning/phases/138-preconditions-baseline/138-04-HOST-BASELINE.md` — the host-suite
      baseline and the CI-parity interpreter constraint.
  </read_first>
  <action>
    Measure, then record. Create `143-HOST-RECORD.md` in this task (task 3 completes it) with a
    **§"Measured size and gate verdicts"** section.

    **Firmware, cold.** For each of `uno`, `uno328pb`, `leonardo`, run `pio run -t clean -e <env>` followed by
    `pio run -e <env>` from cwd `/workspaces/firestarter`, in one uninterrupted invocation per target, with the
    Bash timeout **raised above the default two minutes** — a cold toolchain build has been observed truncating
    at the default (a documented trap from 124-04). Record flash used / total / free and RAM used / total /
    free per target, the delta versus `size_baseline.json`, and for `leonardo` the remaining headroom against
    both the 28672 B ceiling and F-142-08's 2130 B starting figure. State plainly whether it **fits** — that
    is D-22's whole bar.

    **Warnings, cold.** `python3 scripts/check_build_warnings.py --rebuild`. Record the verdict and the
    per-target counts. All three AVR targets must be `total: 0` (`avr_rule: "== 0"`, stricter than the native
    rule); the native total must be at or below **1166**, which has **zero** headroom. If any figure moved,
    record the exact number and the file that caused it.

    **Native envs.** `pio test -e native` and `pio test -e native_nodevtools` — both must report
    **141 test cases: 141 succeeded** and 17 suites. Record both. Then `pio test -e native_loop_v131` and
    record its case and suite counts, noting that **no** gate asserts them and **no** CI leg of either repo
    runs it, so the record is the only place a later reader can find them. Then `pio test -e native_trace_v131`
    and record it as **RED by design** (D-24) with its failure values, plus the statement that this phase added
    **zero** frames to the frozen trace because that suite pins `millis()` to `AlwaysReturn(0)` — so
    Phase 144 / TEST-06 will find zero D-02-attributable strobes. Do **not** re-freeze it. Record
    `native_params_v131`'s counts too if the env is present, with the same no-CI-coverage caveat.

    **Baseline gate.** `python3 scripts/check_size_baseline.py` — expected **RED**. Capture the output
    verbatim and enumerate every reason it is red, then assert that each one is either MERGE-05 (already RED,
    Phase 144 / TEST-08's) or the CAP-02 `+34 B x 3 targets` drift that STATE.md's **OD-2** already recorded and
    the operator already accepted, or this phase's own measured code growth against a baseline that is
    deliberately **not** updated (D-22). **If a reason appears that is none of those, record it as a finding
    and say so loudly.** Do **not** edit `scripts/baseline/size_baseline.json`.

    **Firmware pytest.** `python3 -m pytest tests/ -o addopts="" -q` — record the count and confirm the two
    new gate modules (`test_ack_layout_source_contract_v143.py`,
    `test_progress_emission_is_leonardo_only.py`) are included and green, and that
    `test_protocol_branch_inventory.py`, `test_hv_routing_source_contract_v142.py`,
    `test_write_path_source_contract_v131.py`, both `test_golden_trace_identity*` modules and
    `test_flash_path_record_sync.py` are green.

    **Host.** From `/workspaces/firestarter_app`:
    `.venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-fail-under=70 -o addopts=""`,
    `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`, and
    `.venv/ci-replica/bin/python tools/check_mypy_watermark.py`. Record the test count against the **1547**
    phase-start baseline and account for every added test by plan. Record the mypy gate's exit code explicitly
    — exit 2 means the run cannot be trusted and is **not** a pass.

    Write every figure and verdict into the record as measured values with the command that produced them.
    Never write a number you did not measure in this session; if a figure is unavailable, say so and name why.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_build_warnings.py --rebuild; echo "WARNINGS-EXIT=$?"; pio test -e native 2>&1 | tail -4; pio test -e native_nodevtools 2>&1 | tail -4; python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -4; python3 scripts/check_size_baseline.py; echo "SIZEBASELINE-EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `143-HOST-RECORD.md` exists with a §"Measured size and gate verdicts" section containing, per AVR
      target, cold flash used/total/free and RAM used/total/free, the delta versus `size_baseline.json`, and
      the command that produced it.
    - `leonardo`'s remaining headroom is stated against **both** the 28672 B ceiling and F-142-08's 2130 B,
      and the record says plainly whether the build **fits**.
    - Every measurement is recorded as **cold** (`pio run -t clean -e <env>` then `pio run -e <env>`), and the
      record says so — the warm-versus-cold correction in `size_baseline.json`'s `meta` is cited.
    - RESEARCH's `[ASSUMED]` A2 (CAP-02 ~+34 B) and A3 (emission 40-120 B on `leonardo`) are each compared
      against the measured figures, with a verdict on whether the assumption held.
    - `check_build_warnings.py --rebuild` passes; all three AVR targets are recorded at `total: 0` and the
      native total at or below 1166.
    - `pio test -e native` and `pio test -e native_nodevtools` are both recorded at **141 test cases: 141
      succeeded** / 17 suites.
    - `native_loop_v131`'s case and suite counts are recorded, with the explicit note that no gate asserts
      them and no CI leg runs the env.
    - `native_trace_v131` is recorded as **RED by design** with its failure values, plus the zero-new-frames
      statement and the Phase 144 / TEST-06 hand-off. It is **not** re-frozen.
    - `check_size_baseline.py`'s output is captured **verbatim**, every RED reason is enumerated, and each is
      attributed to MERGE-05, the OD-2 CAP-02 drift, or this phase's measured growth against a deliberately
      un-updated baseline. Any other reason is recorded as a loud finding.
    - `git diff --exit-code -- scripts/baseline/size_baseline.json` in `/workspaces/firestarter` is clean.
    - The firmware pytest count is recorded and the two new gate modules are named as included and green.
    - The host suite count is recorded against the **1547** phase-start baseline with every added test
      attributed to its plan; ruff, ruff-format and the mypy watermark verdicts are recorded, the last with
      its exit code.
    - No `native_*_v131` env name was passed to `check_size_baseline.py` or `check_build_warnings.py`
      (F-138-05), and the record says so.
  </acceptance_criteria>
  <done>Cold flash, RAM and warnings are measured on all three AVR targets and every gate verdict — including the two expected REDs and their attributed reasons — is recorded verbatim with the command that produced it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Complete 143-HOST-RECORD.md, flip the five HOST requirements, and get operator sign-off on the phase's claims</name>
  <files>/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md, /workspaces/.planning/REQUIREMENTS.md</files>
  <read_first>
    - All nine prior SUMMARY files in this phase directory — the record is assembled from measured evidence,
      not from the plans' intentions. Read every D-25 transcript reference and every finding.
    - `/workspaces/.planning/phases/142-high-voltage-routing/142-VPP-RECORD.md` — the record's structural
      analog: the numbered sections, the qualified success-criterion treatment, the non-claims list, the
      D-15 inventory, the findings register with `F-142-NN` ids and owners, and the hand-offs table with
      `H1..Hn` ids and target phases. Follow that shape.
    - `/workspaces/.planning/REQUIREMENTS.md` — the `### Host` checkbox list (`HOST-01`..`HOST-05`) **and** the
      coverage table rows (`| HOST-01 | Phase 143 | Pending |` etc.). **Both** must be flipped; 142-07's
      precedent is one hand edit covering both tables.
    - `/workspaces/.planning/ROADMAP.md` — read the Phase 143 section, but **do not edit its prose**. D-01's
      correction is Phase 146 / CLOSE-04's.
    - `/workspaces/.planning/phases/143-host-timeout-progress-pulse-override/143-CONTEXT.md` — the full
      `<deferred>` list, so every deferred idea is carried forward with its named owner or its explicit
      no-owner status.
  </read_first>
  <action>
    **Part A — complete the record.** Finish `143-HOST-RECORD.md` with these sections, in this order:

    1. **Honest headline.** "A long write now reports what it is doing, and a failed byte now reports as a
       failed byte." State explicitly that this is **not** "writes are faster" and **not** "writes are more
       reliable", and that every proof in this phase is **off-hardware** (host suite plus native envs) — bench
       evidence is **Phase 145**'s.
    2. **Requirement evidence table.** One row per `HOST-01..05` naming which plans produced which evidence and
       which command proves it. Note the two split requirements: **HOST-03** needs plan 143-04's timeout **and**
       plan 143-09's render, and **HOST-04** needs plan 143-04's transport **and** plan 143-07's CLI.
    3. **Blocking-finding reconciliations.** One subsection each for BF-1, BF-2 and BF-3: which CONTEXT
       decision was affected, what the corrected behaviour is, why, and where it shipped. BF-1: CAP-02's
       firmware half was absent, so D-08 had nothing to append to and the two branches could not talk — ported
       in 143-03 from `13eb350`. BF-2: D-02's emission is undeliverable on `SERIAL_ON_IO` targets and a naive
       form would have **regressed HOST-03** there — compiled out in 143-05, pinned in 143-08. BF-3: D-11's
       formula under-estimates 2x at `--pulse-us 49999` on `0x0B` and 8.3x for a future factor-3 row —
       corrected in 143-01 with the `ceil` pulse count and a call to the shipped `eprom_overprogram_us`.
    4. **The padding rule, in prose.** D-09 requires this in the record because a host-side reader cannot see
       how conservative the firmware is: twice the ceil-rounded pulse-only worst case plus two seconds, floor
       one second, ceiling 65535; a multiplier and not an additive constant because per-pulse fixed overhead
       scales with pulse **count**; and the `[ASSUMED]` status of the ~20-60 us per-pulse figure (RESEARCH A1,
       not measured — Phase 145 may record the real number).
    5. **Non-claims.** At minimum: intra-block progress is EPROM-path only **and** `leonardo`-delivery only
       (D-06, both dimensions, with `SERIAL_ON_IO` named as the structural reason); no bench claim about any
       protocol is made here; the `#ifndef SERIAL_ON_IO` guard is proven by a **source contract**, never
       behaviourally, and why; `native_trace_v131` is RED by design and this phase added **zero** frames to it;
       the D-10 fallback's residual gap is `0x07`/`0x08` above ~**4687 us** on a Leonardo and ~**9375 us** on
       an Uno **when no budget is advertised**, and the realistic absent-advertisement case is a **released
       beta** firmware (CAP-02 without CAP-03), **not** a mid-milestone v1.31 build — which cannot connect at
       all (BF-1 corrects D-10's own wording here); `--pulse-us`'s `1..65535` bound is **minipro parity**, not
       a wire-type constraint, and `extract_long` parses the field into an unclamped `uint32_t` (H3, Phase 146
       / CLOSE-04); no host-side warning exists for a `--pulse-us` in `50001..65535` on `0x0B` (D-16 — the
       firmware's pre-flight refusal is the gate); `MSG_ERR_WRITE_FAILED` (`0xB1`) is a dead id on this family
       and nothing keys on it (D-20); and `0xBF` remains the sole free ERROR slot — this phase spent no new
       message id.
    6. **The D-01 correction, recorded.** State the roadmap's wrong framing verbatim, state the correct
       dependency (Phase 141's loop and Phase 140's table, dual-repo), and record that **amending the prose is
       Phase 146 / CLOSE-04's** alongside C3, F-140-05, F-140-07 and H3.
    7. **Measured size and gate verdicts.** Task 2's section, already written.
    8. **D-25 inventory.** A table of every new gate leg and native case authored in this phase, the plan that
       owns it, and a pointer to the SUMMARY holding its planted-RED and GREEN transcripts. Flag any leg that
       failed to go RED under its plant and the locator-only repair that followed.
    9. **Findings register.** `F-143-NN` ids with a one-line description, the owner, and the disposition.
       At minimum carry forward: the environment finding that `pio` aborts when run from `/workspaces` because
       the meta repo holds an untracked `platformio.ini` with a duplicate `[platformio]` section (owner
       `henols`, disposition: named, not fixed); F-141-11 (`test_flash_path_record_sync.py` asserts whole-repo
       porcelain — still orphaned, still bit this phase); F-138-05 (`check_size_baseline.py`'s uncaught
       `KeyError`, owner `henols`); the corrected D-15 rationale (Click's group callback runs before parameter
       conversion, so "before `AppContext` builds" is false while the guarantee holds); the corrected
       `start_time` reset citation (`serial_comm.py:502` for a binary id frame, not `:448`/`:513`); and the
       absence of any standing cross-repo wire-layout parity gate (RESEARCH Open Question 4 — hand to
       Phase 144 / TEST-07).
    10. **Hand-offs.** `H1..Hn` with target phases. At minimum: Phase 144 / TEST-06 owns the
       `native_trace_v131` re-freeze and the attributable diff, and will find **zero** D-02-attributable
       strobes; Phase 144 / TEST-07 should add a byte-layout parity assertion for CAP-03 (the gap BF-1 exposed);
       Phase 144 / TEST-08 owns MERGE-05 and the `size_baseline.json` update; Phase 145 owns all bench evidence
       including real bar motion and the per-pulse-overhead measurement (A1), and must **re-flash** before any
       bench check and record which image was on the board; Phase 146 / CLOSE-03 owns the `--pulse-us`
       documentation entry; Phase 146 / CLOSE-04 owns the roadmap/PROJECT.md prose correction, H3's unclamped
       `extract_long`, `DBG_PULSE_DELAY_MISMATCH`'s stale wording and `MSG_INFO_RETRIES`'s orphan status.
    11. **Deferred ideas carried forward.** The full `143-CONTEXT.md` `<deferred>` list with each item's owner
       or its explicit no-owner status — including `set_progress`'s rebuild-on-differing-total, intra-block
       progress for the non-EPROM families, the byte-count-OR-time cadence, the host-side over-cap warning,
       and the decided skip of `JSON_KEY_PULSE_DELAY`.

    **Part B — flip the requirements, carefully.** Snapshot `REQUIREMENTS.md` and `ROADMAP.md` first
    (`cp` to the scratch directory, or capture `git hash-object` for each). Then flip `HOST-01` through
    `HOST-05` in **both** places: the `### Host` checkbox list (`- [ ]` to `- [x]`) and the coverage table
    (`Pending` to `Complete`). Prefer a **hand edit** — the GSD requirements and roadmap verbs re-normalise the
    **whole** file, and `phase.complete` has been observed clobbering an unrelated phase's `**Plans:**` line.
    After any tooling call and after your own edits, `git diff` both files and **revert by hand any hunk
    outside the intended lines**, including whitespace-only reformatting. Record the diff of both files in the
    SUMMARY.

    Do **not** edit `ROADMAP.md`'s or `PROJECT.md`'s Phase 143 prose (D-01 — that is CLOSE-04's).

    **Part C — the operator checkpoint.** Present the following for sign-off before the phase is marked
    complete. `autonomous: false` is not self-protecting in this project: `--auto` and `--chain` auto-approve
    outward-facing gates, so if this plan is executed under either flag the presentation must still be
    produced in full and written into the SUMMARY even if approval is auto-granted.
  </action>
  <what-built>
    - **HOST-01**: the firmware computes a per-block worst-case write time from the `eprom_params` table plus
      the live pulse width and advertises it as two big-endian bytes on `MSG_OK_READY`; the host decodes it at
      a computed offset with a derived plausibility clamp and uses it verbatim as the write-path response
      timeout, falling back to a derived 120 s when none is advertised. CAP-02 was ported so the two branches
      can talk at all.
    - **HOST-02**: the firmware emits `MSG_DATA_PROGRESS` from inside the per-byte loop, time-gated at 1000 ms
      and **compiled out** on `uno`/`uno328pb`; the host renders it without acking, positions the bar relative
      to the write's start address, never rebuilds the bar, and latches so the bar cannot rewind.
    - **HOST-03**: a byte that fails at `max_pulses` now surfaces as an `EpromOperationError` naming the
      address, with a hint stating the abort's disposition and offering no retry — because the 10 s timeout no
      longer fires first.
    - **HOST-04/05**: `firestarter write --pulse-us N` overrides the database pulse through the existing
      `pulse-delay` wire field, bounded `1..65535` at Click parse time with exit 2 and no port opened, and
      always printing a provenance line naming both values.
    - Two new source-contract gates, eight new native cases, four new host test modules, and a re-derived
      protocol-branch golden.
  </what-built>
  <how-to-verify>
    1. Read §"Honest headline" and §"Non-claims" in
       `.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md`. Confirm the phase claims
       only what it proved: **no** bench claim, **no** "faster", **no** "more reliable", and the
       `leonardo`-only delivery limit stated plainly.
    2. Read §"Blocking-finding reconciliations". Confirm each of BF-1, BF-2 and BF-3 names the CONTEXT decision
       it corrects and that you agree with the deviation — in particular that **D-02 was scoped down** to
       `leonardo` delivery rather than shipped as written, and that **D-11's formula was replaced** rather than
       implemented literally.
    3. Read §"Measured size and gate verdicts". Confirm `leonardo` **fits** and that you accept the
       `check_size_baseline.py` RED for the enumerated reasons only (MERGE-05, the OD-2 CAP-02 `+34 B` drift,
       and this phase's measured growth against a baseline deliberately left un-updated).
    4. Confirm `native_trace_v131` being RED is expected (D-24) and that you accept the record's statement that
       this phase added **zero** frames to the frozen trace.
    5. Run, from `/workspaces/firestarter_app`:
       `.venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-fail-under=70 -o addopts=""` —
       expect green with coverage at or above 70 %.
    6. Run, from `/workspaces/firestarter`: `python3 -m pytest tests/ -o addopts="" -q` — expect green.
    7. Confirm the CAP-02 port is what you wanted: it is a **re-implementation** inside the CAP-03 pack block
       citing `13eb350` in the commit message, **not** a cherry-pick. RESEARCH Open Question 2 flagged this as
       a provenance question and this milestone cares about provenance.
    8. Confirm `git diff` on `.planning/REQUIREMENTS.md` touches only the five `HOST-*` checkboxes and their
       five coverage-table cells, and that `.planning/ROADMAP.md` and `.planning/PROJECT.md` are
       **byte-unchanged** (D-01's correction is Phase 146 / CLOSE-04's).
  </how-to-verify>
  <resume-signal>Type "approved" to mark the phase complete, or describe what to change.</resume-signal>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib, re
r = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
host_block = r[r.index('### Host'):]
host_block = host_block[:host_block.index('###', 5)] if '###' in host_block[5:] else host_block
for i in range(1, 6):
    rid = 'HOST-0%d' % i
    assert re.search(r'- \[x\] \*\*' + rid + r'\*\*', host_block), rid + ' checkbox not flipped in the Host list'
    assert re.search(r'\|\s*' + rid + r'\s*\|\s*Phase 143\s*\|\s*Complete\s*\|', r), rid + ' coverage-table cell not flipped'
rec = pathlib.Path('.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md').read_text()
low = rec.lower()
for needle, why in [
    ('bf-1', 'BF-1 reconciliation required'),
    ('bf-2', 'BF-2 reconciliation required'),
    ('bf-3', 'BF-3 reconciliation required'),
    ('non-claim', 'non-claims section required'),
    ('leonardo', 'the delivery non-claim must be stated'),
    ('serial_on_io', 'the structural reason must be named'),
    ('native_trace_v131', 'D-24 must be recorded'),
    ('4687', 'the D-10 residual threshold must be recorded'),
    ('9375', 'the Uno-side residual threshold must be recorded'),
    ('minipro', 'the bound provenance must be recorded'),
    ('0xbf', 'the unspent ERROR slot must be recorded'),
    ('close-04', 'the D-01 correction hand-off must be recorded'),
    ('test-06', 'the trace re-freeze hand-off must be recorded'),
    ('test-07', 'the wire-layout parity hand-off must be recorded'),
    ('test-08', 'the MERGE-05 hand-off must be recorded'),
    ('phase 145', 'bench evidence must be handed off'),
    ('f-143-', 'a findings register with F-143-NN ids is required'),
]:
    assert needle in low, (needle, why)
print('OK requirements flipped in both tables and the record carries every required section')
" && git -C /workspaces diff --stat -- .planning/ROADMAP.md .planning/PROJECT.md && echo "ROADMAP-AND-PROJECT-DIFF-MUST-BE-EMPTY-ABOVE"</automated>
  </verify>
  <acceptance_criteria>
    - `143-HOST-RECORD.md` contains all eleven sections listed in the action, in order, with the honest headline
      stated as written and explicitly disclaiming "faster" and "more reliable".
    - The requirement evidence table names the split ownership of **HOST-03** (143-04 plus 143-09) and
      **HOST-04** (143-04 plus 143-07).
    - Each of BF-1, BF-2 and BF-3 has its own subsection naming the affected CONTEXT decision, the corrected
      behaviour, the reason, and where it shipped.
    - The padding rule appears in **prose** with the multiplier-not-constant reason and A1's `[ASSUMED]` status.
    - The non-claims section carries every item listed in the action, including both dimensions of D-06, the
      source-contract framing, D-24's zero-new-frames statement, the corrected D-10 residual (4687 us Leonardo
      / 9375 us Uno, with a **released beta** firmware as the realistic absent-advertisement case), the
      minipro-parity provenance with H3, D-16's no-host-warning disposition, D-20's dead id, and `0xBF` still
      being free.
    - The D-01 correction is recorded with the roadmap's wrong framing quoted and the amendment handed to
      Phase 146 / CLOSE-04.
    - The D-25 inventory lists every new gate leg and native case with its owning plan and a pointer to the
      SUMMARY holding its transcripts, and flags any leg that needed a locator-only repair.
    - The findings register uses `F-143-NN` ids with owners and dispositions and carries forward the `pio`-from-
      `/workspaces` environment finding, F-141-11, F-138-05, the corrected D-15 rationale, the corrected
      `serial_comm.py:502` reset citation, and the missing cross-repo wire-layout parity gate.
    - The hand-offs table uses `H1..Hn` ids with target phases and covers TEST-06, TEST-07, TEST-08, Phase 145
      (including the re-flash obligation and A1's measurement), CLOSE-03 and CLOSE-04.
    - `.planning/REQUIREMENTS.md`: all five `HOST-*` checkboxes are `- [x]` **and** all five coverage-table
      cells read `Complete`. `git diff` on that file touches **only** those ten lines — any collateral
      reformatting was reverted by hand, and the diff is recorded in the SUMMARY.
    - `git -C /workspaces diff --stat -- .planning/ROADMAP.md .planning/PROJECT.md` is **empty**. If any tool
      touched either file, the hunks were reverted by hand and the episode is recorded as a finding.
    - The operator presentation (`<what-built>` / `<how-to-verify>`) is reproduced in the SUMMARY in full, with
      the operator's response recorded. If the plan ran under `--auto` or `--chain` and approval was
      auto-granted, the SUMMARY says so explicitly rather than implying a human read it.
  </acceptance_criteria>
  <done>The phase record states the honest headline, all three blocking-finding reconciliations, the padding rule, every non-claim, the measured figures, the D-25 inventory, the findings register and the hand-offs; the five HOST requirements are flipped in both tables with a clean diff; ROADMAP.md and PROJECT.md are byte-unchanged; and the operator has been presented with the phase's claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a phase record to every later reader | a record that overclaims becomes the basis for a bench decision, a release note or a milestone close; the D-09/F-120-02 incident is the precedent for a requirement passing its own verification while being false end to end |
| a GSD planning verb to a locked planning document | the requirements and roadmap verbs re-normalise the whole file, and `phase.complete` has been observed clobbering an unrelated phase's `**Plans:**` line |
| a requirement checkbox to the milestone's close | a requirement flipped without evidence propagates into the milestone record and cannot be un-flipped honestly later |
| a docs file inside a submodule to a whole-repo porcelain gate | an uncommitted docs edit turns `test_flash_path_record_sync.py` RED for an unrelated reason |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-143-OVERCLAIM2 | Repudiation | the phase record's claims | mitigate | the honest headline is fixed in advance and explicitly disclaims "faster" and "more reliable"; the non-claims section is enumerated in the plan so it cannot be quietly shortened; the operator checkpoint presents the claims for sign-off |
| T-143-NONCLAIMLOSS | Repudiation | D-06's second dimension | mitigate | both dimensions are required in `firestarter/CLAUDE.md` (task 1's gate asserts the tokens) **and** in the record (task 3's gate asserts them); the structural reason (`SERIAL_ON_IO`) must be named in both |
| T-143-REQFLIP | Tampering | `REQUIREMENTS.md` and `ROADMAP.md` | mitigate | snapshot before, hand edit preferred, `git diff` both files after any tooling call and revert collateral hunks by hand; an acceptance criterion requires `ROADMAP.md` and `PROJECT.md` to be byte-unchanged; the diff is recorded in the SUMMARY |
| T-143-EARLYFLIP | Repudiation | flipping a requirement before its evidence exists | mitigate | this is the only plan with a non-empty `requirements:`, it is in the last wave and depends on every evidence-producing plan, and the record's evidence table must name a command per requirement before the flip |
| T-143-BASELINEEDIT | Tampering | `scripts/baseline/size_baseline.json` | mitigate | read-only by D-22; an acceptance criterion requires `git diff --exit-code` on it to be clean, and every `check_size_baseline.py` RED reason must be attributed or raised as a finding |
| T-143-PORCELAIN | Denial of Service | the firmware repo's whole-repo porcelain gate | mitigate | the `CLAUDE.md` change is committed **alone** and **before** the full firmware pytest run (L-1) |
| T-143-AUTOAPPROVE | Repudiation | the operator checkpoint under `--auto` / `--chain` | mitigate | the plan requires the presentation to be produced and written into the SUMMARY in full even if approval is auto-granted, and requires the SUMMARY to say explicitly that it was auto-granted rather than implying a human read it |
| T-143-SC | Tampering | npm/pip/cargo installs | accept | this phase installs no external package; `143-RESEARCH.md` §"Package Legitimacy Audit" records the gate as not applicable, verified against the decision set. No plan added a dependency |
</threat_model>

<verification>
From `/workspaces/firestarter` (never from `/workspaces` for `pio` — L-2):
- `pio run -t clean -e uno && pio run -e uno` (and the same for `uno328pb`, `leonardo`) — cold, with the Bash
  timeout raised above two minutes. Record flash/RAM per target and the deltas.
- `python3 scripts/check_build_warnings.py --rebuild` — pass; three AVR targets at `total: 0`, native at or
  below 1166.
- `pio test -e native` and `pio test -e native_nodevtools` — 141 test cases: 141 succeeded, 17 suites each.
- `pio test -e native_loop_v131` — record counts (no gate asserts them, no CI leg runs the env).
- `pio test -e native_trace_v131` — expected RED (D-24); record values, do not re-freeze.
- `python3 scripts/check_size_baseline.py` — expected RED; capture verbatim and attribute every reason.
- `python3 -m pytest tests/ -o addopts="" -q` — green, including both new gate modules. **Commit
  `CLAUDE.md` first** (L-1).
- Never pass a `native_*_v131` env name to either script (F-138-05).

From `/workspaces/firestarter_app`:
- `.venv/ci-replica/bin/python -m pytest tests/ --cov=firestarter --cov-fail-under=70 -o addopts=""` — green,
  coverage at or above 70 %; record the count against the 1547 phase-start baseline.
- `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` — clean.
- `.venv/ci-replica/bin/python tools/check_mypy_watermark.py` — exit 0 (exit 2 is **not** a pass).

From `/workspaces`:
- `git diff -- .planning/REQUIREMENTS.md` — only the ten intended lines.
- `git diff --stat -- .planning/ROADMAP.md .planning/PROJECT.md` — **empty**.
</verification>

<success_criteria>
- `firestarter/CLAUDE.md` carries D-06's two-dimension non-claim, the cadence and payload facts, CAP-03's ack
  layout with the CAP-02 provenance, and the `--pulse-us` interaction — as a docs-only commit.
- Cold flash, RAM and warnings are measured on all three AVR targets, `leonardo` fits, and every gate verdict
  including the two expected REDs is recorded verbatim with its attributed reasons.
- `143-HOST-RECORD.md` carries the honest headline, the requirement evidence table with both split
  requirements named, all three blocking-finding reconciliations, the padding rule in prose, every non-claim,
  the D-01 correction, the measured figures, the D-25 inventory, an `F-143-NN` findings register and an
  `H1..Hn` hand-offs table.
- All five `HOST-*` requirements are flipped in both the checkbox list and the coverage table, with a diff
  touching only those ten lines; `ROADMAP.md` and `PROJECT.md` are byte-unchanged.
- The operator has been presented with the phase's claims and the two accepted REDs, and the response is
  recorded.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, file and name below is **created by Phase 143**. Treat each as new, not as drift against an
existing definition.

**Firmware (`/workspaces/firestarter`)**
- New files: `include/eprom_budget.h`, `src/proms/eprom_budget.cpp`
- New functions: `eprom_worst_pulses`, `eprom_per_byte_budget_us`, `eprom_block_budget_s`
- New macro (plan 143-05, `include/eprom.h`): `EPROM_PROGRESS_EMIT_INTERVAL_MS`
- New test-suite file-static (plan 143-05): `millis_counter` in `test_loop_eprom_v131.cpp`
- New pytest gate modules: `tests/test_ack_layout_source_contract_v143.py` (plan 143-03, env seam
  `FIRESTARTER_ACK_SCAN_DISPATCH_SOURCE`), `tests/test_progress_emission_is_leonardo_only.py` (plan 143-08,
  env seams `FIRESTARTER_PROGRESS_SCAN_EPROM_SOURCE` and `FIRESTARTER_PROGRESS_SCAN_PIO_CONFIG`)
- New native cases in `test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp`:
  `test_budget_uncapped_energy_cap_is_not_a_cap_at_zero`,
  `test_budget_pulse_count_ceils_because_the_loop_tests_after_it_increments`,
  `test_budget_0x0b_at_49999us_is_99998us_per_byte_not_50000`,
  `test_budget_overprogram_term_is_zero_for_factor_zero_and_clamped_for_factor_three`,
  `test_budget_zero_pulse_width_never_divides_by_zero`,
  `test_budget_block_seconds_matches_the_shipped_rows_and_is_padded`,
  `test_progress_emits_when_the_clock_advances_past_the_interval`,
  `test_progress_emits_nothing_when_the_clock_does_not_advance`
- Ported-not-invented firmware code: CAP-02's `MSG_OK_READY` pack block (upstream commit `13eb350` on
  `origin/beta`), extended in place with CAP-03 (plan 143-03)

**Host (`/workspaces/firestarter_app`)**
- New module constants: `WRITE_BUDGET_MAX_S` (`firestarter/serial_comm.py`),
  `WRITE_BLOCK_TIMEOUT_FALLBACK_S` and `_BUDGET_FAILURE_IDS` (`firestarter/eprom_operations.py`)
- **No** new `constants.py` entry. `JSON_KEY_PULSE_DELAY` was considered and **skipped** (a decided
  mechanical gray area, plan 143-07): both upstream documents call it cosmetic, and `143-PATTERNS.md`
  requires it be used at *both* sites or not at all — which would force an edit to `database.py`'s
  generated-database read path for zero behavioural gain. The `"pulse-delay"` literal stays.
- **No** new message id. `0xBF` remains the sole free `0xA0..0xBF` ERROR slot (D-02, F-141-05, H4)
- New `SerialCommunicator` attribute: `write_block_budget_s`
- New `EpromOperator` members: `_write_block_timeout`, `_apply_write_progress`
- New module-level function: `_budget_failure_hint_message` (`firestarter/eprom_operations.py`)
- New parameters on existing functions: `response_timeout` on `_main_phase_send_data`, `pulse_us` on
  `write_eprom`
- New CLI option: `--pulse-us` on `firestarter write` (dest `pulse_us`)
- New test modules: `tests/test_write_response_budget.py`, `tests/test_write_progress.py`,
  `tests/test_budget_failure_render.py`, `tests/test_pulse_us_override.py`
- New test helper: `_cap03_params` in `tests/test_hw_revision_gate.py`

**Meta (`/workspaces`)**
- New artifact: `.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/143-host-timeout-progress-pulse-override/143-10-SUMMARY.md` when done.
</output>
