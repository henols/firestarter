---
phase: 142-high-voltage-routing
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: [firestarter]
files_modified:
  - /workspaces/firestarter/include/rurp_pinout.h
  - /workspaces/firestarter/platformio.ini
  - /workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp
  - /workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - "One shared, header-level set of EPROM high-voltage composite masks exists beside the CTRL_* bits it is built from, and its values are correct in both build variants"
    - "A second suite runs inside the existing native_loop_v131 env without a seventh env, a baseline edit or a change to either pinned env"
    - "The new suite records the post-remap physical control byte below the register cache-compare elision, and can inject an arbitrary VPP reading"
    - "The new suite's read-back model can express a byte that converges inside the per-byte loop and then mismatches on the final verify pass"
  artifacts:
    - path: "/workspaces/firestarter/include/rurp_pinout.h"
      provides: "EPROM_HV_ROUTE_MASK and EPROM_HV_ALL_OFF_MASK composites after the per-variant CTRL_* arms"
      contains: "EPROM_HV_ALL_OFF_MASK"
    - path: "/workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp"
      provides: "strobe recorder + timing recorder + custom read-data-buffer + injectable VPP millivolts + logged-id capture + register cache reset"
      min_lines: 200
    - path: "/workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp"
      provides: "suite harness: four-timing mocks, unconditional revision reset, vpp-setpoint-carrying handle factory, init and write drivers, strobe accessors, harness self-check case"
      min_lines: 200
  key_links:
    - from: "/workspaces/firestarter/platformio.ini"
      to: "/workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/"
      via: "the [env:native_loop_v131] test_filter entry AND the matching -I include path (both required)"
      pattern: "test_vpp_eprom_v131"
    - from: "/workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp"
      to: "/workspaces/firestarter/include/rurp_register_utils.h"
      via: "HOST_STUBS_REAL_REGISTER_UTILS, the only recorder layer below rurp_write_to_register's cache-compare elision"
      pattern: "HOST_STUBS_REAL_REGISTER_UTILS"
---

<objective>
Lay the two prerequisites every behavioural leg in this phase depends on, and nothing else.

1. **The shared mask set (D-07).** Two composite `#define`s in `include/rurp_pinout.h`, beside the
   `CTRL_*` bits they are built from. D-07 is Claude's discretion and its recorded default is taken:
   header-only `#define`s cost 0 B until referenced (`rurp_pinout.h:63-64` records that rule), both
   `eprom.cpp` and `memory.cpp` already include the header (`eprom.cpp:17`, `memory.cpp:25`), and a
   composite defined next to its own bit definitions cannot drift from them. `eprom_params.h` was
   rejected by its own source comment (`eprom_params.h:43-45`); `eprom.h` was rejected because
   `memory.cpp` serves every protocol and would then need an EPROM-family header.
   **This is a new construct, not a followed pattern** — `142-PATTERNS.md` §E-1 found **zero**
   bitwise-OR composite `#define`s anywhere in `include/`. The nearest in-tree forms are three
   single-token aliases (`rurp_pinout.h:76`, `:116`, `:128`).

2. **The test seam (D-14).** A `test_vpp_eprom_v131` suite directory added to the **existing**
   `[env:native_loop_v131]` by exactly two lines — `test_filter` **and** the `-I` list, both required
   (Phase 119 D-04) — plus the suite's stub layer and harness. No seventh env, no
   `size_baseline.json` edit, no change to `[env:native]` or `[env:native_nodevtools]`.

**Requirement ownership: this plan may mark NO requirement Complete.** It contributes
infrastructure to VPP-01, VPP-02, VPP-03 and VPP-04. `requirements:` is deliberately empty and
**must not be repopulated** — `execute-plan.md`'s `update_requirements` step calls
`requirements.mark-complete` unconditionally with this plan's own frontmatter IDs and performs a real
regex flip of both the checkbox and the coverage-table cell. Plan **142-07** declares all four
`VPP-*` IDs and flips them in one hand edit after every piece of evidence exists. This is the exact
convention plan 141-05 used and 141-09 discharged.

**Repo notes:** firmware submodule only. Commit inside `/workspaces/firestarter` on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`. Do not create, switch or rename a branch.
`commits_land_in` names `firestarter` because a worktree leaves submodules empty and
`files_modified` alone under-detects the target repo.

**L-1 working rule, applies to every task here:** `tests/test_flash_path_record_sync.py:1247`
asserts the **whole firmware repo** is `git status --porcelain` clean. Commit before running the full
pytest suite. Do not fix that defect here (F-141-11, orphaned and unassigned).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-high-voltage-routing/142-CONTEXT.md
@.planning/phases/142-high-voltage-routing/142-RESEARCH.md
@.planning/phases/142-high-voltage-routing/142-PATTERNS.md
@.planning/phases/142-high-voltage-routing/142-VALIDATION.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two EPROM_HV_* composites to rurp_pinout.h (D-07)</name>
  <files>/workspaces/firestarter/include/rurp_pinout.h</files>
  <read_first>
    - /workspaces/firestarter/include/rurp_pinout.h — the whole file. Extract: the `#ifndef
      HARDWARE_REVISION` legacy arm at `:74-84` (note `:75` `CTRL_VPP_VPE_DROP_ENABLE 0x01` and `:76`
      `#define CTRL_ADDRESS_LINE_16 CTRL_VPP_VPE_DROP_ENABLE` — a genuine alias); the wide arm at
      `:85-97` (note `:88` `CTRL_ADDRESS_LINE_16 0x01`, `:91` `CTRL_VPP_P1_ENABLE 0x08`,
      `:93` `CTRL_ADDRESS_LINE_18 0x20`, `:95` `CTRL_VPP_REGULATOR_ENABLE 0x80`,
      `:96` `CTRL_VPP_VPE_DROP_ENABLE 0x100`); the `#endif` at `:97`; `:99`'s
      `CTRL_ADDRESS_LINE_13`; the recorded 0-B `#define` rule at `:63-64`; the `_REV1`/`_REV2`
      families at `:105-129` including `:128`'s `CTRL_ADDRESS_LINE_18_REV2 CTRL_VPP_P1_ENABLE_REV2`
      alias; and the `extern "C"` brackets at `:37-39` / `:131-133`.
    - /workspaces/.planning/phases/142-high-voltage-routing/142-PATTERNS.md §E (`:739-814`) — E-1's
      "NO PRECEDENT" grep, E-2's placement, E-3's re-run collision check.
    - /workspaces/.planning/phases/142-high-voltage-routing/142-RESEARCH.md §"Shared Mask Placement"
      (`:306-356`) — the per-variant value table, the "preserve/HOLD mask must NOT be a `#define`"
      determination, and the C-4 reason `CTRL_VPP_P1_ENABLE` is excluded.
    - /workspaces/firestarter/src/proms/eprom.cpp `:440-447` — `eprom_internal_set_control_register`'s
      `using_p1_as_vpp` VPE-to-P1 rewrite, the reason the all-off composite must name
      `CTRL_VPE_ENABLE` and must NOT name `CTRL_VPP_P1_ENABLE`.
  </read_first>
  <action>
    Insert two composite `#define`s **after** `rurp_pinout.h:97` (the close of the wide arm) and
    before `:99`'s `CTRL_ADDRESS_LINE_13`, inside the existing `extern "C"` brackets. A composite
    placed after `:97` expands at its use site and therefore picks up whichever variant arm is live,
    with no `#ifdef` duplication.

    Exact names and members:

    `EPROM_HV_ROUTE_MASK` — `CTRL_VPP_REGULATOR_ENABLE` OR `CTRL_VPP_VPE_DROP_ENABLE`. This is the
    drop-resistor route: regulator on, VPE dropped through the resistor to the VPP level.

    `EPROM_HV_ALL_OFF_MASK` — `CTRL_VPP_REGULATOR_ENABLE` OR `CTRL_VPP_VPE_DROP_ENABLE` OR
    `CTRL_VPP_A9_ENABLE` OR `CTRL_VPE_ENABLE`. Four members, in that order. Wrap each body in
    parentheses.

    `CTRL_VPP_P1_ENABLE` is **deliberately absent** from `EPROM_HV_ALL_OFF_MASK`. Two independent
    reasons, and the comment must state both: (a) `eprom_internal_set_control_register` strips
    `CTRL_VPE_ENABLE` from the mask and substitutes `CTRL_VPP_P1_ENABLE` whenever
    `using_p1_as_vpp(handle)` holds, so naming **both** would leave the physical VPE line never
    cleared; (b) on Rev 2-class, logical `CTRL_ADDRESS_LINE_18` and logical `CTRL_VPP_P1_ENABLE`
    collapse onto the same physical bit `0x08` (`rurp_pinout.h:127`), so naming P1 buys no physical
    guarantee anyway (correction C-4).

    Write a block comment above the pair that records, in words: the `EPROM_`-scoped naming and why
    (the native warning watermark is 1166 with **zero** headroom and all 1166 recorded warnings are
    macro redefinitions, so a generic name risks a collision that turns a live gate RED); that these
    are **logical** masks whose physical effect is produced by
    `rurp_map_ctrl_reg_for_hardware_revision`; the per-variant values
    (`EPROM_HV_ROUTE_MASK` is `0x81` legacy / `0x180` wide; `EPROM_HV_ALL_OFF_MASK` is `0x87` legacy
    / `0x186` wide); that the legacy arm aliases the drop bit onto A16 so the composite also clears
    A16 there, which is harmless on a **disable**; that a **preserve/HOLD** mask can NOT be a
    `#define` because its drop-bit membership depends on a runtime revision read and on
    `handle->pins` (that stays as the conditional inside `mem_util_calculate_top_address_register`);
    the 0-B-until-referenced rule this form rests on; and that no bitwise-OR composite `#define`
    existed in this header before — this establishes a form rather than following one.

    Do **not** convert `flash_5v_page.cpp` or `eeprom_28c.cpp`'s inline OR-lists. The composite is
    EPROM-family-scoped by C-4 and those files are out of scope.
    Change nothing else in the header: no bit value, no alias, no `_REV1`/`_REV2` entry.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import re, pathlib
t = pathlib.Path('include/rurp_pinout.h').read_text()
lines = t.splitlines()
def defline(name):
    idx = [i for i, l in enumerate(lines) if re.match(r'\s*#define\s+' + name + r'\b', l)]
    assert len(idx) == 1, (name, 'must be defined exactly once, found', len(idx))
    return idx[0], lines[idx[0]]
i_route, route = defline('EPROM_HV_ROUTE_MASK')
i_off, off = defline('EPROM_HV_ALL_OFF_MASK')
for tok in ['CTRL_VPP_REGULATOR_ENABLE', 'CTRL_VPP_VPE_DROP_ENABLE']:
    assert tok in route, ('EPROM_HV_ROUTE_MASK missing', tok)
for tok in ['CTRL_VPP_REGULATOR_ENABLE', 'CTRL_VPP_VPE_DROP_ENABLE', 'CTRL_VPP_A9_ENABLE', 'CTRL_VPE_ENABLE']:
    assert tok in off, ('EPROM_HV_ALL_OFF_MASK missing', tok)
assert 'CTRL_VPP_P1_ENABLE' not in off, 'EPROM_HV_ALL_OFF_MASK must NOT name CTRL_VPP_P1_ENABLE (C-4 + the VPE->P1 remap)'
endif = [i for i, l in enumerate(lines) if l.strip() == '#endif']
wide = [i for i, l in enumerate(lines) if re.match(r'\s*#define\s+CTRL_VPP_VPE_DROP_ENABLE\s+0x100', l)][0]
close = min(e for e in endif if e > wide)
a13 = [i for i, l in enumerate(lines) if re.match(r'\s*#define\s+CTRL_ADDRESS_LINE_13\b', l)][0]
assert close < i_route < a13 and close < i_off < a13, ('composites must sit between the wide arm #endif and CTRL_ADDRESS_LINE_13', close, i_route, i_off, a13)
print('OK composites present, ordered, P1-free')
"</automated>
  </verify>
  <acceptance_criteria>
    - `include/rurp_pinout.h` defines `EPROM_HV_ROUTE_MASK` exactly once and `EPROM_HV_ALL_OFF_MASK`
      exactly once, both positioned **after** the wide arm's `#endif` (the one closing the block that
      contains `#define CTRL_VPP_VPE_DROP_ENABLE 0x100`) and **before** `#define CTRL_ADDRESS_LINE_13`.
    - `EPROM_HV_ROUTE_MASK`'s body names `CTRL_VPP_REGULATOR_ENABLE` and `CTRL_VPP_VPE_DROP_ENABLE`
      and nothing else.
    - `EPROM_HV_ALL_OFF_MASK`'s body names exactly `CTRL_VPP_REGULATOR_ENABLE`,
      `CTRL_VPP_VPE_DROP_ENABLE`, `CTRL_VPP_A9_ENABLE`, `CTRL_VPE_ENABLE`, and the literal string
      `CTRL_VPP_P1_ENABLE` does **not** appear on that line.
    - The block comment states all seven recorded points: the `EPROM_` prefix rationale against the
      1166 zero-headroom watermark; logical-not-physical; the four per-variant values `0x81`/`0x180`
      and `0x87`/`0x186`; the legacy drop-equals-A16 aliasing being harmless on a disable; that a
      preserve/HOLD mask cannot be a `#define`; the 0-B-until-referenced rule; and that no composite
      `#define` precedent existed in this header.
    - `git diff -- include/rurp_pinout.h` adds only the comment and the two `#define` lines — no bit
      value, alias or `_REV1`/`_REV2` entry is touched.
    - `pio test -e native` reports **141 test cases: 141 succeeded** and 17 suites, and
      `pio test -e native_nodevtools` likewise — the pinned envs are unmoved (adding an unreferenced
      `#define` cannot change either count, and a macro-redefinition collision would surface here).
    - `python3 scripts/check_build_warnings.py --rebuild` passes — the native total is still at or
      below the 1166 watermark, proving the new names collided with nothing.
    - `pio run -e leonardo` links (the 28672 B ceiling is a build failure, not a gate).
  </acceptance_criteria>
  <done>Two `EPROM_HV_*` composites exist in `rurp_pinout.h` with the correct members, correct placement, a comment carrying the seven recorded points, and no movement in either pinned env or the warning watermark.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the new suite into [env:native_loop_v131] and author its stub layer (D-14)</name>
  <files>/workspaces/firestarter/platformio.ini, /workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp</files>
  <read_first>
    - /workspaces/firestarter/platformio.ini `:373-414` — the whole `[env:native_loop_v131]` block
      including its 31-line header comment. `test_loop_eprom_v131` appears in exactly two places,
      `:407` (`test_filter`) and `:410` (`-I`); indentation is a **hard tab**. Also read
      `[env:native_params_v131]` at `:361-371` (the identical two-list shape) and
      `${env:native.build_flags}` at `:120-141` (which supplies `-D HARDWARE_REVISION`, `-D DEV_TOOLS`,
      `-std=gnu++17`, `-I include` and all 17 existing suite `-I` paths).
    - /workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp — the whole file
      (281 lines). This is the analog; copy roughly 95 percent verbatim. Extract: the file banner
      `:1-48` including the two pitfall paragraphs at `:35-39` and `:41-47`; the `#include` block
      `:50-57`; the three opt-in `#define`s `:59-69`; the `.inc` include at `:71`; the
      `#include "rurp_register_utils.h"` **after** the `.inc` at `:73-80` with its explanatory
      comment; `reset_register_cache` at `:82-95`; the read-back model `:97-214`; the logged-id
      capture `:216-281`.
    - /workspaces/firestarter/test/native/avr/_shared/host_stubs_common.inc — `:22-25` (the
      guards-are-read-at-include-time pitfall), `:96-209` (the `HOST_STUBS_REAL_REGISTER_UTILS` arm,
      and note `:105` defines `HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`), `:211-231` and `:232-242`
      (`HOST_STUBS_RECORD_BUS`, which truncates to `uint8_t` and reads back 0 — unusable here),
      `:274-278` (the `HOST_STUBS_CUSTOM_VOLTAGE_MV` seam), `:288-305` (the revision stubs the
      recorder arm suppresses), `:306-312` (`s_host_config`, zero-initialised, so
      `hardware_revision == 0 == REVISION_0`).
    - /workspaces/firestarter/test/native/avr/test_flash_intel_vpp/host_stubs.cpp `:27-39` — the
      **only** thing to copy from that suite: the `#define HOST_STUBS_CUSTOM_VOLTAGE_MV` at `:30` and
      the `set_mock_vpp_mv` / `rurp_read_voltage_mv` pair at `:37-39`. Do **not** copy `:31`'s
      `HOST_STUBS_CUSTOM_HW_REVISION` or its `:41-47` block — both are structurally dead once
      `HOST_STUBS_REAL_REGISTER_UTILS` is on.
    - /workspaces/firestarter/include/rurp_shield.h `:145` — the exact signature
      `uint16_t rurp_read_voltage_mv()`, no parameters.
    - /workspaces/firestarter/include/rurp_register_utils.h `:12-14` and `:39-49` — the `0xff`-
      initialised globals, and the cache-compare elision that the recorder must sit **below**.
    - /workspaces/.planning/phases/142-high-voltage-routing/142-PATTERNS.md §A (`:33-186`) and §C
      (`:506-568`) — the block-by-block copy map and the voltage-seam contract.
  </read_first>
  <action>
    **Part A — `platformio.ini`, exactly two added lines.** Add `native/avr/test_vpp_eprom_v131` as a
    new hard-tab-indented line immediately after `:407`'s `native/avr/test_loop_eprom_v131` in
    `test_filter`, and `-I test/native/avr/test_vpp_eprom_v131` as a new hard-tab-indented line
    immediately after `:410`'s `-I test/native/avr/test_loop_eprom_v131` in `build_flags`. Both are
    required: a suite directory is invisible to `pio test` until it is in `test_filter`, and its
    headers are unreachable until a matching `-I` exists (Phase 119 D-04).

    Extend the env's header comment with a short paragraph naming Phase 142 / VPP-04 and D-14: this
    env now runs **two** suites, the new one exists because `native` and `native_nodevtools` are
    pinned at exactly 141 cases / 17 suites / `all_passed` by `check_size_baseline.py`'s
    `compare_native`, and the no-CI-coverage and never-feed-this-env-to-either-gate caveats already in
    that comment apply unchanged to the new suite.

    Add nothing else: no new `[env:...]`, no `default_envs` change, no `build_src_filter` change, no
    `size_baseline.json` edit. Do not add the suite to `[env:native]` or `[env:native_nodevtools]`.

    **Part B — `test/native/avr/test_vpp_eprom_v131/host_stubs.cpp`.** Copy the analog and change
    exactly five things.

    1. **Rewrite the file banner.** The analog's banner names Phase 141 plan 03 and lists **three**
       opt-in layers; this one names Phase 142 / VPP-01..VPP-04 and lists **four**. Carry both pitfall
       paragraphs forward in substance: the `0xff`-initialised-globals pitfall and the
       guards-must-precede-the-include pitfall. Add a paragraph naming the two reasons
       `HOST_STUBS_RECORD_BUS` is unusable for this suite (it stores `(uint8_t)data`, truncating the
       `0x100` drop bit to zero, and it records calls rather than strobes so cache elision is
       invisible — F-141-09), and a paragraph stating that because
       `HOST_STUBS_REAL_REGISTER_UTILS` defines `HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`, the **real**
       `rurp_get_hardware_revision()` is linked and returns `rurp_get_config()->hardware_revision`,
       which is `0` (`REVISION_0`) by default — so every case that cares about the drop bit or about
       reaching `eprom_check_vpp`'s voltage compare must override it per case.
    2. **Add the fourth opt-in guard** `HOST_STUBS_CUSTOM_VOLTAGE_MV` alongside the existing three
       (`HOST_STUBS_REAL_REGISTER_UTILS`, `HOST_STUBS_RECORD_TIMING`,
       `HOST_STUBS_CUSTOM_READ_DATA_BUFFER`), **before** the `#include "../_shared/host_stubs_common.inc"`.
       Ordering is load-bearing: the guards are read at include time.
    3. **Add the voltage mock**, after the `.inc` include: a TU-private `static uint16_t s_mock_vpp_mv`
       defaulting to `0`, `extern "C" void set_mock_vpp_mv(uint16_t mv)` which assigns it, and
       `extern "C" uint16_t rurp_read_voltage_mv()` which returns it — exact signature, no parameters.
    4. **Rename the read-back model's symbols from `loop_` to `vpp_`** — `vpp_readback_reset`,
       `vpp_readback_seed`, `vpp_readback_reads`, `vpp_readback_seeded_count`, and the internal
       `s_vpp_readback` / `vpp_readback_entry_t` / `VPP_READBACK_MAX_ENTRIES`. The rename is
       deliberate, not cosmetic: the model's semantics **diverge** from the analog's (point 5), and
       identical names would invite a reader to assume identical behaviour.
    5. **Extend the read-back model with a mismatch window.** The analog's rule is
       `result = (read_count < converge_after) ? 0xFF : target`, which has no upper bound — once a
       byte converges it matches on **every** later read, so the analog's model **cannot** express a
       byte that converges inside the per-byte loop and then mismatches on the final full-block verify
       pass. That exit (`eprom.cpp:311`, `MSG_ERR_VERIFY`) is VPP-02's headline gap and needs exactly
       that shape. Give `vpp_readback_seed` a fourth parameter `mismatch_from` and make the rule:
       return `0xFF` while `read_count < converge_after`; return `(uint8_t)(~target)` once
       `read_count >= mismatch_from`; return `target` otherwise. Increment `read_count` on every read,
       exactly as the analog does. Use `0xFFFF` as the "never mismatch" sentinel and state that in the
       comment. Keep the analog's documented read-count-to-pulse-count mapping comment
       (`reads == 1 + pulses`, `converge_after = N` means the byte matches on read `N+1`) and add the
       mismatch-window mapping beside it. Keep the unseeded-address behaviour verbatim: return `0xFF`
       and do **not** create an entry, so `vpp_readback_seeded_count()` stays a real negative control.

    Everything else is verbatim: the `#include` block, the `.inc` include, the
    `#include "rurp_register_utils.h"` **after** the `.inc` with its comment, `reset_register_cache`
    (signature `(uint8_t lsb, uint8_t msb, rurp_register_t ctrl)`), and the whole logged-id capture
    block including `rurp_log_id` and its `clear_logged_ids` / `logged_id_count` / `logged_id_at` /
    `logged_id_param_count` / `logged_id_param` / `logged_ids_overflowed` accessors. The logged-id
    capture is what makes "assert `MSG_ERR_VPP_HIGH` **by id**" possible and nothing else in the tree
    does it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import pathlib, re
ini = pathlib.Path('platformio.ini').read_text()
block = ini[ini.index('[env:native_loop_v131]'):]
block = block[:block.index('\n[env:') if '\n[env:' in block else len(block)]
assert block.count('native/avr/test_vpp_eprom_v131') >= 2, 'need BOTH the test_filter entry and the -I path'
assert '\t' + 'native/avr/test_vpp_eprom_v131' in block, 'test_filter entry must be hard-tab indented'
assert '\t-I test/native/avr/test_vpp_eprom_v131' in block, '-I entry must be hard-tab indented'
for env in ['[env:native]', '[env:native_nodevtools]']:
    seg = ini[ini.index(env):]
    seg = seg[:seg.index('\n[env:')]
    assert 'test_vpp_eprom_v131' not in seg, env + ' must NOT reference the new suite'
h = pathlib.Path('test/native/avr/test_vpp_eprom_v131/host_stubs.cpp').read_text()
inc = h.index('host_stubs_common.inc')
head = h[:inc]
for g in ['HOST_STUBS_REAL_REGISTER_UTILS','HOST_STUBS_RECORD_TIMING','HOST_STUBS_CUSTOM_READ_DATA_BUFFER','HOST_STUBS_CUSTOM_VOLTAGE_MV']:
    assert re.search(r'#define\s+' + g + r'\b', head), g + ' must be defined BEFORE the .inc include'
assert 'extern \"C\" uint16_t rurp_read_voltage_mv()' in h
assert 'extern \"C\" void set_mock_vpp_mv(uint16_t mv)' in h
assert 'HOST_STUBS_CUSTOM_HW_REVISION' not in h.replace('HOST_STUBS_CUSTOM_HW_REVISION_BLOCK',''), 'do not opt into the narrower revision guard'
for s in ['vpp_readback_reset','vpp_readback_seed','vpp_readback_reads','vpp_readback_seeded_count','mismatch_from','reset_register_cache','rurp_log_id','clear_logged_ids','logged_id_at','logged_id_param']:
    assert s in h, 'host_stubs missing ' + s
assert 'loop_readback' not in h, 'read-back symbols must be renamed to vpp_readback_*'
assert h.index('#include \"rurp_register_utils.h\"') > inc, 'rurp_register_utils.h must be included AFTER the .inc'
print('OK env wired with both lines; stub layer has four guards, the voltage mock and the extended readback model')
"</automated>
  </verify>
  <acceptance_criteria>
    - `[env:native_loop_v131]` contains a hard-tab-indented `native/avr/test_vpp_eprom_v131` line in
      `test_filter` **and** a hard-tab-indented `-I test/native/avr/test_vpp_eprom_v131` line in
      `build_flags`. `git diff -- platformio.ini` adds exactly those two lines plus the header-comment
      paragraph, and creates no new `[env:...]` section.
    - Neither `[env:native]` nor `[env:native_nodevtools]` mentions `test_vpp_eprom_v131`;
      `scripts/baseline/size_baseline.json` is byte-unchanged (`git diff --exit-code --
      scripts/baseline/size_baseline.json` is clean).
    - `host_stubs.cpp` defines all four opt-in guards **before** the `.inc` include, and
      `#include "rurp_register_utils.h"` appears **after** it.
    - `host_stubs.cpp` supplies `extern "C" uint16_t rurp_read_voltage_mv()` with no parameters and
      `extern "C" void set_mock_vpp_mv(uint16_t mv)`, and does **not** define
      `HOST_STUBS_CUSTOM_HW_REVISION`.
    - The read-back symbols are `vpp_readback_*`; no `loop_readback` identifier remains in the file.
    - `vpp_readback_seed` takes four parameters and its documented rule is: `0xFF` below
      `converge_after`, bitwise-NOT of `target` at or above `mismatch_from`, `target` in between —
      with `0xFFFF` named as the never-mismatch sentinel. **This is what makes VPP-02's X4 leg able to
      fail at all;** the analog's unbounded model would make a final-pass-mismatch assertion
      unreachable.
    - The file banner names Phase 142 and **four** opt-in layers, and carries: the `0xff`-globals
      pitfall, the guards-precede-the-include pitfall, the two reasons `HOST_STUBS_RECORD_BUS` is
      unusable, and the default-`REVISION_0` consequence.
    - `pio test -e native_loop_v131 -f "*test_vpp_eprom_v131*"` compiles and links (it will report no
      cases until task 3 lands `main()`); `pio test -e native` and `pio test -e native_nodevtools`
      each still report 141 cases / 17 suites.
  </acceptance_criteria>
  <done>The new suite directory is reachable from the existing env by both required lines, its stub layer records post-remap physical control bytes below the elision, injects VPP millivolts, and its read-back model can express a final-pass mismatch.</done>
</task>

<task type="auto">
  <name>Task 3: Author the suite harness and a self-check case that proves the composites and the recorders</name>
  <files>/workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp</files>
  <read_first>
    - /workspaces/firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp — targeted
      ranges: `:25-62` (includes and the locally restated kind constants, with the comment explaining
      why a TEST TU may pair `Arduino.h` with `ArduinoFake.h`); `:72-105` (the `extern "C"` seam
      declarations); `:111-141` (`setUp`, the four-timing-function mock block); `:143-151`
      (`tearDown`, the unconditional revision reset, with its longjmp explanation); `:164-176`
      (`make_loop_handle`); `:178-246` (the three `LOOP_BUS_CONFIG_*` constants and their derivation
      comments); `:275-288` (`drive_loop_write`) and `:256-267` (its never-`_init`,
      never-the-whole-command contract comment); `:301-308` (the harness self-check case);
      `:426-441` (`count_logged_id` / `find_logged_id`); `:1122-1173` (the three strobe accessors and
      the 3-entry group shape); `:1183-1193` (`first_genuine_pulse_strobe_index`); `:1511-1524` (the
      `REVISION_2_2` override idiom and its comment); `:1666-1723` (`main()`).
    - /workspaces/firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp — task 2's output, for
      the exact `extern "C"` names and the `vpp_readback_seed` four-parameter signature.
    - /workspaces/firestarter/include/rurp_pinout.h — task 1's output: the two composites, plus
      `:105-129`'s `_REV1`/`_REV2` values (`CTRL_VPP_VPE_DROP_ENABLE_REV2` is `0x01`,
      `CTRL_ADDRESS_LINE_16_REV2` is `0x20`).
    - /workspaces/firestarter/include/rurp_shield.h `:25-31` — the `REVISION_*` values
      (`REVISION_0` 0, `REVISION_1` 1, `REVISION_2_0` 2, `REVISION_2_1` 3, `REVISION_2_2` 4,
      `REVISION_2_3` 5, `REVISION_UNKNOWN` 0xFE).
    - /workspaces/firestarter/src/proms/eprom.cpp `:331-394` — `eprom_check_vpp`, so the harness's
      `_init` driver is aimed at code that actually exists; note the Rev-0 early return at `:337`.
    - /workspaces/firestarter/src/proms/eprom.cpp `:412-420` and `:43` — `eprom_generic_init` calls
      `eprom_check_vpp` at `:413`, and `configure_eprom:43` installs `eprom_generic_init` as the
      default `firestarter_operation_init`.
    - /workspaces/.planning/phases/142-high-voltage-routing/142-PATTERNS.md §B (`:187-503`) — the
      block-by-block harness map, and §B-5's **mandatory** handle-factory change.
  </read_first>
  <action>
    Author the suite's harness plus exactly one case. Behavioural legs land in plans 142-03, 142-05
    and 142-06; this task's only case is the harness self-check.

    **Includes and constants.** Copy `:25-62` verbatim in substance: `Arduino.h`, `ArduinoFake.h`,
    `unity.h`, `string.h`, `stdio.h`; then `extern "C" { #include "memory.h" }`, `firestarter.h`,
    `eprom.h`, `eprom_params.h`, `memory_utils.h`, `messages.h`. `memory_utils.h` is required — later
    plans call `mem_util_calculate_top_address_register` directly, and it is declared there at `:22`.
    `messages.h` is required for `MSG_ERR_VPP_HIGH` (0xB8) and `MSG_WARN_VPP_HIGH` (0x82); no other
    header pulls it in transitively. Restate the four kind constants
    (`STROBE_KIND_DATA` 1, `STROBE_KIND_PIN` 2, `TIMING_KIND_DELAY_US` 3, `TIMING_KIND_DELAY_MS` 4)
    locally with the same explanatory comment — `#define`s inside `host_stubs.cpp` are invisible in
    this separate translation unit.

    **`extern "C"` seam declarations.** Copy the analog's block and add `set_mock_vpp_mv`. Update the
    read-back declarations to the `vpp_readback_*` names and `vpp_readback_seed`'s four parameters.

    **`setUp`.** Copy `:111-141` verbatim in substance, including the four ArduinoFake mocks —
    `delay`, `delayMicroseconds`, `millis`, `micros`. All four are mandatory: ArduinoFake SIGABRTs on
    any unmocked call, and correction C-2 measured `test_flash_intel_vpp` aborting mid-run for exactly
    this reason (it mocks `delay` only). Carry that reason into the comment by name. `setUp` must also
    reset every recorder, the register cache and the read-back model, and call `set_mock_vpp_mv(0)`
    so no case inherits another's injected voltage.

    **`tearDown`.** Copy `:143-151` verbatim in substance: assign
    `rurp_get_config()->hardware_revision = 0` **unconditionally**, with the comment explaining that
    Unity calls `tearDown()` even when a case fails via `TEST_ASSERT`'s longjmp, so the reset always
    runs. Do not copy `test_flash_intel_vpp.cpp:67-68`'s empty `tearDown`.

    **Handle factory — the one mandatory divergence from the analog.** Author
    `make_vpp_handle(uint32_t protocol, uint8_t pins, uint32_t mem_size, uint32_t pulse_delay_us,
    uint16_t vpp_setpoint_mv, uint32_t extra_ctrl_flags, const bus_config_t& bus_config)`. It sets
    `protocol`, `pins`, `mem_size`, `pulse_delay`, `bus_config`, `cmd = CMD_WRITE`,
    `response_code = RESPONSE_CODE_OK`, `chip_id = 0` (so the chip-id branch is skipped), and
    `ctrl_flags = extra_ctrl_flags | FLAG_SKIP_BLANK_CHECK | FLAG_SKIP_ERASE`. **It must set
    `h.vpp_mv = vpp_setpoint_mv`.** The analog's `make_loop_handle` never sets `vpp_mv`, leaving it
    `0`, which makes `eprom_check_vpp`'s compares `0 > 0 + 500` (false) and `0 < 0 * 95 / 100`
    (false) — the identical vacuity trap D-13 found in `test_val_eprom.cpp:74`. Copying the analog
    as-is would make every VPP-04 leg vacuous. State that in the factory's comment.

    Copy `LOOP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B` from `:178-246` verbatim including their
    derivation comments, renamed `VPP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B`. A zeroed `bus_config` is
    degenerate, not identity — do not substitute one.

    **Two drivers.** `drive_vpp_write(h, base, block, n)` is `drive_loop_write` verbatim in substance:
    `configure_memory(h)` first, then `reset_register_cache(0x00, 0x00, 0x00)` **after** it (because
    `configure_memory` calls `mem_util_set_address(handle, 0)`), then the three `clear_*()` calls,
    then seed `address`/`data_size`/`data_buffer`, then `h->firestarter_operation_main(h)`.
    `drive_vpp_init(h)` is the same prologue but terminates in `h->firestarter_operation_init(h)` —
    the only way to reach `eprom_check_vpp`. Carry forward the half of the analog's contract comment
    that still holds ("never the whole command — `command_done()` zeroes `CONTROL_REGISTER` on every
    command exit, which would make any HV assertion vacuous") and explicitly invert the other half,
    stating that this suite deliberately DOES drive `_init` because VPP-04's gate lives inside
    `eprom_check_vpp`.

    **Accessors.** Copy verbatim: `control_write_count`, `control_write_strobe_index`,
    `control_write_value` (returning the Nth non-elided `CONTROL_REGISTER` write's **post-remap
    physical** byte, `-1` when not decodable — every assertion must check `v >= 0` first),
    `first_genuine_pulse_strobe_index`, `count_logged_id`, `find_logged_id`.

    **Revision override helper.** Provide a small helper or a documented idiom for
    `rurp_get_config()->hardware_revision = REVISION_2_2;` carrying the analog's `:1511-1523` comment
    in substance: on `REVISION_0`/`REVISION_1` the drop bit and `CTRL_ADDRESS_LINE_16` both remap onto
    physical `0x01`, so a drop-bit claim is undecidable there; on `REVISION_2_x` they are distinct
    (`CTRL_VPP_VPE_DROP_ENABLE_REV2` `0x01` versus `CTRL_ADDRESS_LINE_16_REV2` `0x20`). Add the second
    reason this suite needs it: on the default `REVISION_0`, `eprom_check_vpp` takes the early return
    at `eprom.cpp:337` and never reaches the over-voltage compare, so a VPP-04 case without the
    override is silently vacuous.

    **The one case, `test_setup_leaves_the_harness_clean_and_the_composites_correct`.** Three groups
    of assertions, each able to fail:
    (a) recorders start clean — `strobe_count() == 0`, `timing_count() == 0`,
    `logged_id_count() == 0`, `strobe_overflowed() == 0`, `vpp_readback_seeded_count() == 0`;
    (b) the composites are what task 1 wrote — `EPROM_HV_ROUTE_MASK` equals
    `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE`; `EPROM_HV_ALL_OFF_MASK` equals
    `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE | CTRL_VPP_A9_ENABLE | CTRL_VPE_ENABLE`;
    `(EPROM_HV_ALL_OFF_MASK & CTRL_VPP_P1_ENABLE) == 0` — this last one is the C-4 / VPE-to-P1-remap
    guard and would fail the moment someone "helpfully" adds P1 to the composite;
    (c) the read-back model's mismatch window works — seed one address with
    `converge_after = 1, mismatch_from = 2`, then drive three reads through
    `rurp_read_data_buffer()` after seeding the LSB/MSB latches to that key, and assert the sequence
    is `0xFF`, then `target`, then bitwise-NOT of `target`. Without (c) the whole VPP-02 X4 leg in
    plan 142-05 would rest on an unverified stub.

    **`main()`.** Copy the `:1666-1723` shape: `(void)argc; (void)argv;`, `UNITY_BEGIN()`, one
    `RUN_TEST` per case grouped under a comment header naming the plan and requirement, then
    `return UNITY_END();`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native_loop_v131 -f "*test_vpp_eprom_v131*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native_loop_v131 -f "*test_vpp_eprom_v131*"` reports **1 test case: 1 succeeded**
      and the suite `PASSED` — not `ERRORED` and not a SIGABRT (an unmocked ArduinoFake call would
      abort here; that is the C-2 failure mode this task is defending against).
    - `pio test -e native_loop_v131` (bare) reports **2 suites** and **40 test cases: 40 succeeded** —
      39 from `test_loop_eprom_v131` plus 1 new. Record that pair of figures in the SUMMARY; no gate
      asserts them, so the record is the only place a later reader can find them.
    - `setUp` installs `AlwaysReturn`/`AlwaysDo` mocks for all four of `delay`, `delayMicroseconds`,
      `millis` and `micros`, and its comment names C-2 / `test_flash_intel_vpp` as the reason.
    - `tearDown` assigns `rurp_get_config()->hardware_revision = 0` unconditionally.
    - `make_vpp_handle` assigns `h.vpp_mv` from its own parameter. Grepping the file for `vpp_mv`
      finds that assignment; a factory that omitted it would make every later VPP-04 assertion
      vacuously pass, which is D-13's named trap.
    - `drive_vpp_init` calls `h->firestarter_operation_init(h)` and `drive_vpp_write` calls
      `h->firestarter_operation_main(h)`; both call `configure_memory(h)` **before**
      `reset_register_cache(0x00, 0x00, 0x00)`.
    - The self-check case asserts `(EPROM_HV_ALL_OFF_MASK & CTRL_VPP_P1_ENABLE) == 0`.
    - The self-check case asserts the three-read mismatch-window sequence `0xFF`, `target`,
      `~target`. Delete the `mismatch_from` branch from `host_stubs.cpp` and this assertion fails —
      state in the SUMMARY that this was confirmed, or confirm it by the planted run below.
    - `pio test -e native` and `pio test -e native_nodevtools` each still report 141 cases / 17
      suites; `python3 scripts/check_build_warnings.py --rebuild` still passes.
    - After committing all three tasks' files, `python3 -m pytest tests/ -o addopts="" -q` reports
      **256 passed** (unchanged — this plan touches no gate's scan target). Commit first: the
      whole-repo porcelain assertion at `tests/test_flash_path_record_sync.py:1247` turns RED on any
      in-flight diff.
  </acceptance_criteria>
  <done>The suite compiles, links, runs one green case, and that case proves the recorders start clean, the composites are correct and P1-free, and the read-back mismatch window behaves as documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| header macro namespace to compiler | a new `#define` colliding with an ArduinoFake or `pgmspace` macro emits a redefinition warning against a watermark with zero headroom, turning a live gate RED |
| test stub layer to production register path | a recorder that sits above the cache-compare elision records calls hardware never sees, so a green assertion can be evidence of nothing |
| injected voltage to the code under test | a handle whose `vpp_mv` setpoint is 0 makes every over-voltage comparison structurally unreachable, so the gate passes vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-MACRO | Denial of Service | the two new `EPROM_HV_*` `#define`s in `rurp_pinout.h` | mitigate | `EPROM_`-prefixed names, collision-checked by grep across `*.h`/`*.cpp`/`*.c`/`*.inc`/`*.py`; `check_build_warnings.py --rebuild` run as an acceptance criterion so a collision fails loudly at once rather than at a later plan's commit |
| T-142-ELIDE | Repudiation | the strobe recorder layer choice | mitigate | `HOST_STUBS_REAL_REGISTER_UTILS` only, asserted present before the `.inc`; `HOST_STUBS_RECORD_BUS` is named in the banner as structurally incapable (it truncates the `0x100` drop bit and reads back 0) |
| T-142-VACUOUS | Repudiation | `make_vpp_handle` omitting the VPP setpoint | mitigate | the factory takes `vpp_setpoint_mv` as a required parameter and assigns `h.vpp_mv`; asserted by an acceptance criterion naming D-13's `test_val_eprom.cpp:74` precedent |
| T-142-WINDOW | Repudiation | the extended read-back model | mitigate | the self-check case drives three reads and asserts the exact `0xFF` / `target` / `~target` sequence, so VPP-02's X4 leg cannot later rest on an unverified stub |
| T-142-PINNED | Tampering | the two pinned native envs and `size_baseline.json` | mitigate | acceptance criteria assert `test_vpp_eprom_v131` appears in neither pinned env, that `size_baseline.json` is byte-unchanged, and that both envs still report 141 cases / 17 suites |
| T-142-SC | Tampering | package-manager installs | accept | no npm/pip/cargo install in this phase; `142-RESEARCH.md` records no Package Legitimacy Audit as not-applicable, and no plan in this phase installs a package |
</threat_model>

<verification>
Run from `/workspaces/firestarter`:
- `pio test -e native_loop_v131 -f "*test_vpp_eprom_v131*"` — 1 case, 1 succeeded, suite PASSED.
- `pio test -e native_loop_v131` — 2 suites, 40 cases, 40 succeeded.
- `pio test -e native` — 141 test cases: 141 succeeded, 17 suites.
- `pio test -e native_nodevtools` — 141 test cases: 141 succeeded, 17 suites.
- `python3 scripts/check_build_warnings.py --rebuild` — pass (native total at or below 1166).
- `pio run -e uno` / `pio run -e uno328pb` / `pio run -e leonardo` — all link; leonardo under 28672 B.
- `python3 -m pytest tests/ -o addopts="" -q` — 256 passed. **Commit first** (L-1).
- Never `python3 scripts/check_size_baseline.py ... native_loop_v131` and never
  `python3 scripts/check_build_warnings.py ... native_loop_v131` (F-138-05, uncaught `KeyError` / exit 2).
</verification>

<success_criteria>
- Two `EPROM_HV_*` composites exist in `rurp_pinout.h`, correctly placed, P1-free, documented with
  the per-variant values and the "a preserve mask cannot be a `#define`" determination.
- `[env:native_loop_v131]` runs a second suite via both required lines; no new env, no baseline edit,
  no change to either pinned env.
- The new suite compiles, links and runs one green case proving clean recorders, correct composites
  and a working read-back mismatch window.
- The warning watermark, both pinned env counts and the 256-test pytest suite are all unmoved.
</success_criteria>

<artifacts_this_phase_produces>
Every symbol, file and name below is **created by Phase 142**. Treat each as new, not as drift
against an existing definition.

- **Firmware macros (`include/rurp_pinout.h`):** `EPROM_HV_ROUTE_MASK`, `EPROM_HV_ALL_OFF_MASK`
- **Firmware function (plan 142-04, `include/eprom.h` + `src/proms/eprom.cpp`):**
  `eprom_hv_route_mask`
- **Firmware static inner functions (plan 142-04, `src/proms/eprom.cpp`):**
  `eprom_internal_write_execute_body`, `eprom_internal_write_init_body`
- **Firmware function DELETED (plan 142-04):** `eprom_internal_ensure_regulator_enabled`
- **New PlatformIO suite (no new env):** `test_vpp_eprom_v131`, inside the existing
  `[env:native_loop_v131]`
- **New test files:** `firestarter/test/native/avr/test_vpp_eprom_v131/host_stubs.cpp`,
  `firestarter/test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp`
- **New harness symbols:** `set_mock_vpp_mv`, `vpp_readback_reset`, `vpp_readback_seed`,
  `vpp_readback_reads`, `vpp_readback_seeded_count`, `make_vpp_handle`, `drive_vpp_init`,
  `drive_vpp_write`, `VPP_BUS_CONFIG_0x07`, `VPP_BUS_CONFIG_0x08`, `VPP_BUS_CONFIG_0x0B`
- **New pytest gate module (plan 142-06):**
  `firestarter/tests/test_hv_routing_source_contract_v142.py`, with env seams
  `FIRESTARTER_HV_SCAN_DISPATCH_SOURCE` and `FIRESTARTER_HV_SCAN_EPROM_SOURCE`
- **Renamed native case (plan 142-04):**
  `test_loop08_dip32_drop_bit_is_cleared_deliberately_before_the_first_pulse` becomes
  `test_vpp01_dip32_drop_bit_survives_the_block_on_rev2_class`
- **New meta artifact (plan 142-07):**
  `.planning/phases/142-high-voltage-routing/142-VPP-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/142-high-voltage-routing/142-01-SUMMARY.md` when done.
</output>
