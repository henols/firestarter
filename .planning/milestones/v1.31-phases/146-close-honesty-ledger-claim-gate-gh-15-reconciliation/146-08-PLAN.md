---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 08
type: execute
wave: 3
depends_on: ["146-01", "146-03", "146-05"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-08-SUMMARY.md
autonomous: true
requirements: [CLOSE-02]
must_haves:
  truths:
    - "Per CLOSE-02 every permitted claim is paired with its explicit non-claim in the same row, and the ledger leads with the ~6.25 V program-VCC ceiling and the asymmetric bench coverage. This is the single source of the permitted wording that 146-09 and 146-10 must match."
    - "Per D-03 all twelve of Phase 145's carry-forwards appear as negative-space rows with their Owner text reproduced verbatim from the bench record, and none gets a new backlog stub or a state-file block. Per D-14 no forbidden phrase is reproduced: boundary 2 at 145-BENCH-LOG.md:2707-2709 contains one of the forbidden literals, so it is cited by line range and paraphrased, never quoted; boundaries 1 and 3 measure clean and are quotable."
    - "MEASURED: the authoritative carry-forward table at 145-BENCH-LOG.md:2522-2565 has TWELVE rows, TEN of which carry the 'no v1.31 owner' Owner text. Two other documents give different counts; the ledger states the count as twelve and names both other readings rather than silently preferring one."
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md"
      provides: "The honesty ledger: identity header with live-measured submodule commits and an oracle line, the ceiling and asymmetric coverage as the leading section, evidence tiers weakest to strongest, a four-column claim table whose fourth column is the explicit non-claim, mechanism corrections pointing at the register, negative space covering all twelve carry-forwards, process failures, and what no test can close"
      contains: "Explicitly does NOT prove"
  key_links:
    - from: ".planning/phases/145-bench-validation/145-BENCH-LOG.md"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md"
      via: "the twelve-row carry-forward table and the nine numbered boundaries, cited by line and reproduced as negative-space rows rather than re-derived"
      pattern: "no v1.31 owner"
  prohibitions:
    - "MUST NOT quote boundary 2 (145-BENCH-LOG.md:2707-2709) — cite by line range and paraphrase (D-14, a measured live instance); MUST NOT use the bare unqualified claim word anywhere, including hyphenated compounds ending in it, since the gate's pattern matches after a hyphen."
    - "MUST NOT re-derive any figure a source record already measured — cite it; where a figure is re-measured live and agrees, state the agreement as a re-confirmation; where two readings exist, state both; MUST NOT re-derive the flash-band exemption wording, which was authored in STATE.md explicitly for verbatim quotation here."
    - "MUST NOT file a backlog stub or add a state-file block for any carry-forward (D-03) — ledger rows only; MUST NOT run a build, invoke the size-baseline script, or run any bench command; MUST NOT state or imply a comparative claim (fidelity not improvement, no control run exists); MUST NOT tick any CLOSE requirement (146-13 owns all five); MUST NOT touch firestarter/ or firestarter_app/ (D-06); MUST NOT push, merge, tag or dispatch a workflow (D-01)."
---
<objective>
Write `146-LEDGER.md`: the milestone's honesty ledger, pairing every permitted claim with its explicit non-claim, leading with the program-VCC ceiling and the asymmetric bench coverage (CLOSE-02).

This is the single source of the permitted wording — both release bodies and the public reconciliation must match it, which is why constraint 1 puts it first. Its raw material is already written and committed across five phase records; the ledger **cites** rather than re-measures. Two things make it delicate. The gate this phase built forbids twelve claim shapes with no proximity window and no exclusion mechanism, and the bench record it must cite contains one of those literals in its own boundary 2 — so citation discipline is mechanical, not stylistic. And three records disagree about how many carry-forwards there are; the ledger states all three readings and lets the authoritative table settle it. Output: `146-LEDGER.md`, green under the phase's own claim gate.
</objective>
<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CORRECTIONS.md
@.planning/REQUIREMENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Write the identity header and the leading ceiling-and-asymmetric-coverage section</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md:1-125` — the most recent instance: the identity header, the ceiling-quoted-verbatim section, the status key, the claim-class table header, and the composes-with block whose last entry asserts a referenced-but-unedited file's porcelain was confirmed empty. Note its discipline of capturing submodule commits live rather than reusing a prior document's citation. Then `122-LEDGER.md:1-60` for the status-key wording, whose forbidden-class entry states in the ledger's own voice that a forbidden claim appears only as a citation of what is not claimed.
    - `.planning/REQUIREMENTS.md` §"Evidence ceiling" — the ceiling in its authoritative wording, quoted verbatim including the not-behavior-preserving clause. Then `.planning/STATE.md` around `:105`, `:131` and `:182` — the flash-band exemption wording staged for verbatim quotation here, with the `+96 B` breach against the leonardo band and the named band constant; quote it, do not re-derive it, and record the commit it landed in.
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md` §"Boundaries" (`:2696-2735`) — the nine numbered boundaries. Boundary 3's scope sentence (one part `W27C512` chip-id `0xda08`, controller `leonardo`, shield `Rev 2.0` read off the silkscreen) measures clean and is quotable verbatim; boundary 1 is clean and carries the historical-figure caveat; **boundary 2 at `:2707-2709` is NOT quotable** — cite by line range and paraphrase. Then §"Not measured (phase close)" (`:2497-2521`) and the Gate 2/Gate 3 verdicts, and `146-ARM-BUILD-RECORD.md` for which arm `146-03` took plus its mandatory delta-not-CI-parity caveat.
  </read_first>
  <action>
Create `146-LEDGER.md` with an identity header, a status key and one leading section. Section order below the leading section is Tasks 2 and 3's; the leading section's content is fixed by CLOSE-02.

**Identity header.** Copy the most recent ledger's moves rather than inventing them: the milestone and its phase list; the repository scope; both submodule HEAD commits **captured live in this task** with the capturing command shown and an explicit statement that they were measured here rather than reused; an **Oracle:** line naming every gate and suite this close rests on with its count — the phase's claim gate at five targets, the fixture suite at its leg count, the documentation checker at four targets, the record gate with its exempt tally, and both sub-repo suites with their passed counts; a generated date; and a **Composes with (cross-reference only — no data copied)** block listing every record cited, whose last entry asserts that each referenced-but-unedited archived document's porcelain was confirmed clean. **Status key:** reproduce the prior ledgers' key, including the forbidden-class entry stating that a forbidden claim appears here only as a citation of what is *not* claimed.

**The leading section — the ceiling, then the asymmetric coverage.** *The ceiling:* quote the requirements document's evidence-ceiling statement verbatim including the not-behavior-preserving clause, cited by section name, then state its consequence in the ledger's own words — this milestone buys timing, pulse-count and verify fidelity and **not** silicon-margin fidelity, the debt is hardware-bound and tracked as a named future requirement, and gh#15 omits it entirely, which is why its acceptance criteria imply a fidelity unreachable on this hardware. Immediately after, quote the flash-band exemption **verbatim** from the staged STATE.md wording, cite the commit it landed in, and add the three facts that record states three ways: a named commit-attributed exemption constant, a decomposed pass-or-fail string that keeps the growth visible rather than laundering it into a moved anchor, and a negative control one byte past the exemption. Then state the two things a reader would otherwise have to infer: the originating finding was **never remediated**, and the earlier green came from the **anchor moving** rather than from growth shrinking. Re-derive no byte figure and do not run the size script — it cannot run without a build.

*The asymmetric coverage:* what was validated and on what — the required protocol via a full write-read-verify cycle, quoting boundary 3's scope sentence verbatim. Then the two skips as dispositions rather than gaps: each names the missing part, cites its last known state to the phase that measured it, and carries the explicit statement that it is **not inferred** from the validated protocol's result. Add the third deliberate non-spend, the true-UV data point that was not taken, and why. Then the two facts CONTEXT flags as load-bearing: the force flag was not used, and the re-seat allowance the bench decision permitted was not consumed. Use the bench record's own two-state taxonomy — *validated* and *skipped-with-reason* — throughout, and never the bare unqualified claim word; where you need to say something was not established, say *not established*, *not measured* or *remains open*. The ledger is one of the gate's caveat-required targets, so the ceiling voltage figure **and** a silicon-margin phrase must both be present; the leading section satisfies both naturally. Run the gate against the file as it stands at the end of this task and record the run. Do not commit yet — the file is committed once, at the end of Task 3.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-LEDGER.md > /tmp/gsd146/gate_ledger_t1.txt 2>&1; rc=$?; echo "claim_gate_rc=$rc"; cat /tmp/gsd146/gate_ledger_t1.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && mkdir -p /tmp/gsd146 && head -120 146-LEDGER.md > /tmp/gsd146/ledger_lead.txt; a=$(grep -c '6\.25' /tmp/gsd146/ledger_lead.txt); b=$(grep -ciE 'silicon[-_ ]margin' /tmp/gsd146/ledger_lead.txt); echo "lead_ceiling=$a lead_silicon_margin=$b"; test "$a" -ge 1 && test "$b" -ge 1</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && for tok in W27C512 0xda08 leonardo 'Rev 2.0' 'MERGE-05' '+96' 'Oracle' 'Composes with'; do grep -qF "$tok" 146-LEDGER.md || { echo "MISSING $tok"; exit 1; }; done; w=$(grep -oiE '\bpro[v]en\b' 146-LEDGER.md | wc -l); echo "scope_and_header_tokens=1 claim_word=$w"; test "$w" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The claim gate against `146-LEDGER.md` alone exits **0** with no forbidden hit and no missing-caveat complaint — reachable because the leading section carries both required caveats by construction. Negative control: append one probed forbidden phrase to a scratch copy outside the phase directory, confirm non-zero naming that label, discard the copy.
    - Both required caveats appear within the file's **first 120 lines**, which is the positional constraint CLOSE-02's leading-section rule implies; a whole-file grep would pass even with the ceiling buried at the end, so the slice is the oracle. Run the same slice over a copy with the leading section deleted and confirm the counts drop to 0.
    - The header carries both submodule HEAD commits captured in this task with the command shown and an explicit measured-here statement, an **Oracle:** line naming at least five gates or suites each with a count, a **Composes with** block whose final entry asserts referenced-but-unedited archived documents were confirmed clean, and the status key's forbidden-class entry.
    - The evidence-ceiling statement is quoted verbatim including the not-behavior-preserving clause and cited by section name. The flash-band exemption is quoted verbatim from the staged STATE.md wording and the quotation was compared character-for-character with the staged text — record the extraction commands for both sides. The exemption constant is named as it appears in that wording rather than from memory, the three-ways-of-saying-it facts are present, and both under-read facts are stated: never remediated, and the earlier green came from the anchor moving.
    - Boundary 3's scope sentence is quoted verbatim with the part identifier, chip-id, controller and shield revision all present. Boundary 2 appears **only** as a `file:line` citation with a paraphrase — the gate's exit 0 confirms none of its literals reached the file. Both skips are stated as dispositions with the not-inferred statement, the third deliberate non-spend is present, and the two load-bearing bench-posture facts (force flag unused, re-seat allowance unconsumed) are stated.
  </acceptance_criteria>
  <done>The ledger opens with a live-measured identity header, a status key carrying the citation discipline in the ledger's own voice, and a leading section stating the ceiling verbatim, quoting the admitted exemption verbatim with its two under-read facts, and stating the asymmetric coverage as one validation plus two dispositions plus one deliberate non-spend.</done>
</task>
<task type="auto">
  <name>Task 2: Write the evidence tiers, the four-column claim table and the mechanism corrections</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md</files>
  <read_first>
    - `130-LEDGER.md:53-102` — the evidence-tier grouping, weakest to strongest, each tier's membership stated rather than implied. Then `137-LEDGER.md:98-165` for the four-column claim table with its status token bolded into the class cell and the mechanism-corrections section that follows, and `137-LEDGER.md:71-76` and `:100-102` for the two disciplines to reuse: a live re-measurement that agrees with a record is stated as a **re-confirmation**, and where two readings exist both are stated.
    - `.planning/phases/145-bench-validation/145-08-SUMMARY.md` in full — measured clean of forbidden phrases and therefore quotable verbatim; its "what this plan did NOT prove" list is nearly a ready-made non-claim column and its suite results explain a count divergence rather than smoothing it. Then `144-TEST-RECORD.md` §10 for the hand-off items this ledger reports on rather than discharges, `141-LOOP-RECORD.md` §12 and §Findings for the two hand-offs and the operator-owned finding never remediated, and `143-HOST-RECORD.md` §§1, 4 and 5 for the honest headline, the padding rule with its assumed figure, and the non-claims about controller-only emission, the residual-gap threshold and the override's bound provenance.
    - `firestarter/src/proms/eprom.cpp` and `include/eprom_params.h` — the shipped write path and the table's column contract, read once, so the mechanism-corrections section describes what ships rather than what a record says ships. Then `146-CORRECTIONS.md` as `146-05` wrote it: the ledger **points at** it rather than duplicating it, because "what may be claimed" and "what we previously got wrong" are deliberately different files.
  </read_first>
  <action>
Append three sections.

**Evidence tiers, weakest to strongest,** one short table or list per tier with membership named explicitly. The tiers this milestone actually has: *never run* — neither repository's CI against any v1.31 code, and the ARM target's state as `146-03` observed it, carrying its mandatory scoping caveat; *structurally unreachable* — the program-VCC ceiling, the program-window rail behaviour under load, and progress delivery on the controller class where the emission is compiled out; *cited, not re-derived* — the two skipped protocols' last known states and the historical pre-milestone write figure, which is a recorded historical number and **not** a control measurement; *source contract only* — the operation-level high-voltage disable guarantee, the progress-emission guard, and the advertised-budget byte-layout parity; *native-simulated* — the milestone's own native environment counts, each stated with the fact that those environments run in no CI leg; *AVR-measured* — per-target flash and RAM at the tip and the admitted exemption; *bench-measured on one part* — the validated protocol's write-read-verify cycle, the override exercised on silicon, and the per-block claim that held. State the ordering rule in one sentence, so a reader knows a lower tier is a different kind of evidence rather than a weaker version of a higher one.

**The four-column claim table.** Reproduce the prior ledgers' header exactly — class, permitted wording, evidence with its measured source, and explicitly-does-not-prove — with a status token bolded into the class cell. Every row must have a **non-empty** fourth cell; that cell **is** CLOSE-02's explicit non-claim, and a row without one does not satisfy the requirement. Cover at least these classes, each with its own non-claim: the parameter-table dispatch; the per-byte pulse-to-verify loop as it ships, including that the bench-validated image contains a defect fix to the loop phase's own shipped code; the hard-fail-at-budget behaviour with its address and pulse-count reporting; the high-voltage routing consolidation and the error-exit-versus-successful-block asymmetry; the host-side long-block survival and intra-block progress with its controller-only boundary; the per-run pulse override with its bound-provenance narrowing; the test and build position including the native environment counts and the AVR sizes; the CI position — both suites pass locally at measured counts, and **no CI run has exercised any of it**; and the database position — no chip's support status changed and the database is byte-unchanged, cited to the phase that re-confirmed it.

Every byte, percentage or count figure must be cited to the record that measured it **at this milestone's tip**, and no figure may be carried from a record a later phase superseded; name each figure's source in the summary so the choice is auditable. Where a figure was re-measured live and agrees, state the agreement as a re-confirmation; where two readings exist, state both.

**Mechanism corrections.** A short section pointing at `146-CORRECTIONS.md` for the full register, naming in one line each the corrections that change what a reader should believe about the mechanism: the over-program factor the shipped table sets to zero against a throughput table that implies otherwise on two rows; the energy-cap value whose primary-datasheet basis is real while its published reason was wrong; the wire field parsed unclamped, delivered rather than truncated, and refused only on the row whose cap column is non-zero; and the shipped debug message that described a deleted loop until this phase's wording change. State explicitly that the unclamped field is **recorded and not clamped** and name the backlog item that owns the adjacent decision. Do not duplicate the register's rows. Run the gate again at task end and record the run. Do not commit yet.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-LEDGER.md > /tmp/gsd146/gate_ledger_t2.txt 2>&1; rc=$?; echo "claim_gate_rc=$rc"; test "$rc" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "import itertools;L=open('146-LEDGER.md',encoding='utf-8').read().splitlines();h=next(i for i,l in enumerate(L) if 'Explicitly does NOT prove' in l);R=list(itertools.takewhile(lambda l:l.lstrip().startswith('|'),L[h+2:]));C=[[x.strip() for x in l.strip().strip('|').split('|')] for l in R];rows=[c for c in C if len(c)>=4];empty=[h+3+i for i,c in enumerate(rows) if not c[3]];print('claim_rows='+str(len(rows))+' empty_non_claim_cell_lines='+repr(empty));assert len(rows)>=8,len(rows);assert not empty,empty"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && for tok in '146-CORRECTIONS.md' '999.31'; do grep -qF "$tok" 146-LEDGER.md || { echo "MISSING $tok"; exit 1; }; done; w=$(grep -oiE '\bpro[v]en\b' 146-LEDGER.md | wc -l); echo "register_pointer=1 backlog_owner=1 claim_word=$w"; test "$w" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The claim gate against the ledger still exits **0** after this task.
    - The four-column header appears verbatim and a row-wise parse finds **at least eight** rows with **zero** empty fourth cells, reporting offending line numbers rather than a boolean. Negative control: run the same parse over a copy whose fourth column is emptied on one row and confirm it names that line.
    - The evidence-tier section names all seven tiers with explicit membership and states the ordering rule in one sentence; the ARM tier carries `146-03`'s mandatory delta-not-CI-parity caveat. The CI class exists with permitted wording stating both suites pass locally at measured counts and a non-claim stating that no CI run has exercised any of it; the per-byte-loop class's evidence cell states that the bench-validated image contains a defect fix to the loop phase's own shipped code and its non-claim does not extrapolate beyond the one validated part; the pulse-override class's non-claim carries the bound-provenance narrowing.
    - Every byte, percentage and count figure is cited to the record that measured it at this milestone's tip, with each figure's source named in the summary, and no figure is carried from a superseded record. At least one figure is stated as a re-confirmation and at least one place where two readings exist states both.
    - The mechanism-corrections section points at `146-CORRECTIONS.md`, names the four mechanism-relevant corrections in one line each, states that the unclamped field is recorded and not clamped, names the owning backlog item, and does not reproduce the register's rows.
  </acceptance_criteria>
  <done>The ledger carries seven named evidence tiers, a claim table of at least eight rows with no empty non-claim cell, and a mechanism-corrections section that points at the register rather than duplicating it — with every figure cited to the record that measured it at this tip.</done>
</task>
<task type="auto">
  <name>Task 3: Write the negative space for all twelve carry-forwards, close with what no test can close, commit</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md</files>
  <read_first>
    - `.planning/phases/145-bench-validation/145-BENCH-LOG.md:2522-2565` — the authoritative carry-forward table. Measured for this plan: **twelve** rows, **ten** carrying the `no v1.31 owner` Owner text. Reproduce the Owner text verbatim, row by row. Then `145-08-SUMMARY.md` for its own prose count, which disagrees, and `146-CONTEXT.md` D-03 for the third reading and the instruction that each carry-forward gets a ledger row citing the bench record rather than re-deriving it.
    - `.planning/REQUIREMENTS.md` §"Future Requirements" and §"Out of Scope" — the named homes `FUT-08`, `FUT-VCC`, `FUT-PRESTO` and `FUT-MAXPULSE`. Then `.planning/ROADMAP.md` §"Phase 999.30" and §"Phase 999.31" — the two filed backlog items owning two of the carry-forwards.
    - `137-LEDGER.md:165-240` — the process-failures, negative-space and what-no-test-can-close sections in that order, with process failures first-class rather than folded in. Then `146-CITATIONS.md` §0.4 for the tracked-gitlink-versus-live-HEAD delta, which is recorded as a delta and explicitly **not** as a criterion.
  </read_first>
  <action>
Append three sections and commit.

**Negative space — all twelve carry-forwards.** Reproduce the authoritative table as twelve rows, each carrying the item as the bench record names it, the Owner text **verbatim** from that record's Owner column, where it lives now, and one line on what was not established. File no backlog stub and add no state-file block (D-03): several already have homes in the named future requirements or the two filed backlog items, and the genuinely homeless ones are named here and nowhere else, which is the point of a ledger. For the row the operator adjudicated, state that it is discharged rather than carried and point at the leading section where its wording is quoted. For the row that is the ceiling, state that this ledger is where it is **stated**, not where it is discharged.

**Then settle the counting disagreement explicitly.** State the count as twelve, because that is what the authoritative table has and what the phase verdict says, and that ten of the twelve carry the no-owner text. Then, in one short paragraph, record that two other documents give different numbers, name both by file, state that the table wins, and show the derivation — how many rows name no owner for this milestone and how many name a real one. Both readings stated, neither silently preferred; this is the discipline the prior ledger established and it applies here three times over.

**Process failures, first-class rather than folded in.** Each stated plainly and cited: the correction queue four phases each declined and routed forward, discharged only at close; the three inherited corrections that did not hold as stated, found only because they were re-measured; the acceptance locators in the prior phase that were false greens, one passing against a record with no content in it, which is why this phase's own gate carries two independent proofs; and the tracked submodule pointers in the meta repository, stale for the whole milestone. For the last, give the tracked and live commits from the citation register as a two-row table and state that re-pinning is handed to the milestone-close command rather than done here. **Write no criterion asserting the pointers match** — they do not and have not at any point this milestone.

**What no test, gate or review can close.** At minimum: the program-VCC ceiling, which is hardware and has no firmware answer; the root cause of the intermittent single-byte margin failure, mitigated by clean cycles rather than explained; the program-window rail behaviour under load, whose only available proxy is defeated by the port reset on close; whether the two skipped protocols work, which needs parts nobody has; and whether any of this is conformant in either direction — cite the bench record's boundary 2 by line range and paraphrase it, never quote it. End with the scanner-status paragraph: which gates this ledger has been run against with their exit statuses, and the explicit statement that a green gate means the forbidden vocabulary is absent and **not** that the prose is right — that judgement is the blocking operator wording review in `146-12`.

**Then run the full check and commit.** Run the claim gate against the ledger alone and confirm exit 0. Also run it with no argument and no environment override: at this wave it must exit non-zero on the artifacts that do not exist yet — the reconciliation and both release bodies — and that failure is recorded as **expected at this wave**, not as a red. Then commit `146-LEDGER.md` in the meta repository with a message naming CLOSE-02.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-LEDGER.md > /tmp/gsd146/gate_ledger_t3.txt 2>&1; rc=$?; echo "claim_gate_rc=$rc"; cat /tmp/gsd146/gate_ledger_t3.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -c 'no v1.31 owner' 146-LEDGER.md); echo "owner_text_rows=$n"; test "$n" -ge 10 && for tok in 999.30 999.31 FUT-08 FUT-VCC FUT-PRESTO FUT-MAXPULSE; do grep -qF "$tok" 146-LEDGER.md || { echo "MISSING home $tok"; exit 1; }; done; grep -qE 'twelve|12 carry' 146-LEDGER.md && grep -qiE '\bten\b|\beight\b' 146-LEDGER.md && echo "homes_cited=1 count_settled_with_other_readings=1"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py > /tmp/gsd146/gate_all_t3.txt 2>&1; rc=$?; m=$(grep -c 'not found on disk' /tmp/gsd146/gate_all_t3.txt); echo "all_targets_rc=$rc (non-zero expected at this wave) missing_target_lines=$m"; test "$rc" != "0" && test "$m" -ge 1; cd /workspaces && git show --stat --oneline HEAD | tail -3; f=$(git -C /workspaces/firestarter status --porcelain | wc -l); echo "fw_dirty=$f"; test "$f" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The claim gate against the ledger alone exits **0**. The all-target run exits non-zero naming the not-yet-existing artifacts, and that run is recorded as **expected at this wave** rather than as a failure — reachable and correct, because three of the gate's five targets are authored by later plans.
    - The negative-space section carries **twelve** rows; `grep -c 'no v1.31 owner'` returns **at least 10**, matching the ten Owner cells measured in the authoritative table at `145-BENCH-LOG.md:2522-2565`; and the rows naming real owners reproduce those owners verbatim. Every named home is cited: both filed backlog items and all four named future requirements appear.
    - The count is stated as twelve with ten no-owner rows, both other readings are named by file, and the derivation is shown; a ledger stating only one number fails this criterion even if the number is right. The adjudicated row is stated as discharged and points at the leading section, and the ceiling row states that this ledger is where it is stated rather than discharged.
    - The process-failures section exists as its own section with at least four items, including the stale submodule pointers as a two-row tracked-versus-live table with the re-pin handed onward — and **no** criterion anywhere in the ledger asserts that the pointers match.
    - The closing section carries at least five items nothing can close, cites boundary 2 by line range with a paraphrase rather than a quotation, and ends with a scanner-status paragraph naming every gate with its exit status and stating that a green gate is not a statement about the prose. `146-LEDGER.md` is committed in meta and the `firestarter` working tree is clean.
  </acceptance_criteria>
  <done>The ledger's negative space carries all twelve carry-forwards with verbatim Owner text and no new stub filed, settles the count by stating all three readings, treats process failures as first-class including the stale submodule pointers, closes with what nothing can close, and is committed green under its own gate.</done>
</task>
</tasks>
<verification>`146-LEDGER.md` committed and exit 0 under the phase's claim gate run against it alone; claim table parsed row-wise at eight or more rows with zero empty non-claim cells; twelve carry-forward rows with Owner text verbatim, no stub filed, no state-file block added; no forbidden literal reproduced and the bare claim-word count 0; the all-target run's failure recorded as expected at this wave; neither sub-repo touched; no `CLOSE-*` checkbox or coverage row moved.</verification>
<success_criteria>A reader who has seen none of this milestone's other records can learn from the ledger alone what v1.31 may be claimed to have done, what it explicitly does not claim, on what evidence and at what tier, what twelve things remain open and who owns each, and which items no amount of further testing on this hardware could settle. Plans `146-09` and `146-10` can source every permitted phrase from it without consulting another record.</success_criteria>
<output>Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-08-SUMMARY.md`. Record the three gate runs against the ledger, the all-target run as an expected failure, the row-wise claim-table parse output, the character-level comparison proving the exemption quotation is verbatim, and each figure's cited source.</output>
