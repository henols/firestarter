---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 10
type: execute
wave: 4
depends_on: ["146-08"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-app.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-10-SUMMARY.md
autonomous: true
requirements: [CLOSE-05]
must_haves:
  truths:
    - "Per D-01 both bodies are committed DRAFTS — nothing is pushed, merged, cut, tagged or dispatched here — and per D-02 both are version-agnostic: one explicit placeholder each, no literal prerelease version string, because the tag is only knowable after the cut"
    - "Per CLOSE-05 each body describes the programming-behaviour change and the per-run pulse override in terms a stranger can act on, with every claim traceable to a 146-LEDGER.md claim-table row"
    - "Per D-11 both bodies are caveat-required gate targets, so each carries the program-VCC voltage figure and a silicon-margin phrase; per D-14 no donor heading carrying the bare unqualified claim word is copied"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md"
      provides: "A version-agnostic firmware release body stating its bench, controller-class and ARM boundaries inside the headline section, with the ceiling as its safety fact and paired established / not-established sections"
      contains: "6.25"
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-app.md"
      provides: "A version-agnostic host release body: install line, the three user-visible changes with their boundaries at the point of claim, and paired established / not-established sections"
      contains: "--pulse-us"
  key_links:
    - from: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md"
      via: "the ledger's claim table as the single source of permitted wording, with every body claim traceable to a row in it"
      pattern: "Explicitly does NOT prove"
  prohibitions:
    - "MUST NOT write any literal prerelease version string or compute a predicted next tag (D-02), and MUST NOT copy a donor heading carrying the bare unqualified claim word"
    - "MUST NOT make a comparative claim, claim any continuous-integration result for this milestone's code, or claim anything the ledger's claim table does not permit — if a body needs a claim the table lacks, the body changes, never the ledger"
    - "MUST NOT assert any outcome for the gate's all-five default run or for the fixture suite's armed-against-real-files leg, and MUST NOT write to 146-CITATIONS.md — 146-09 runs in this same wave and 146-11 owns both"
    - "MUST NOT push, merge, tag, create a release, dispatch a workflow or publish to a package index (D-01); MUST NOT tick any CLOSE requirement (only 146-13 may); MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Draft both release bodies — firmware and host — version-agnostic, matched to the ledger's permitted wording, and written for a stranger rather than for someone who has read this milestone's records. CLOSE-05 asks that release notes *describe* the change; it asks for no cut, tag or push, and D-01 keeps all three out of this phase. D-02 makes both bodies version-agnostic because both repositories' CI derives the next version from a tag scan at push time. Two mechanical traps: donor section headings carry the bare claim word this phase's own gate forbids and must be renamed rather than copied, and both bodies are caveat-required gate targets.

Output: both bodies, each green under the claim gate run against it alone, both drafts behind the blocking operator wording review in plan 146-12.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-ARM-BUILD-RECORD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the firmware body with its boundaries stated inside the headline section</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md</files>
  <read_first>
    - `130-close-honesty-ledger-claim-gate-release-decision/130-RELEASE-NOTES-fw.md` — the heading spine and lines 1-25. Two moves to copy: a named headline section, and stating the boundary **immediately inside** it rather than saving it for the end. Two of its headings carry the bare claim word and must be renamed.
    - `122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-fw.md:1-10` — the asset-list opener and install-command sentence. And `146-LEDGER.md` in this directory — the claim table and evidence tiers; every claim in this body traces to a row and the non-claim column builds the not-established section.
    - `146-ARM-BUILD-RECORD.md` (this directory) — the observed ARM state, its mandatory scoping caveat, and the measured fact that neither repository's CI has run any of this milestone's code.
    - `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 and `firestarter/README.md` §Protocol Notes, as edited by plan 146-06 — the developer-facing and user-facing text this body is the release-page short form of. Do not contradict either.
  </read_first>
  <action>
Write the firmware body on the donor's spine with the renames applied.

**Self-identification and the placeholder.** Title it a firmware prerelease without naming a version. Exactly **one** placeholder token near the top, marked as filled in at cut time from the observed release listing and never computed. No literal prerelease version string anywhere. Then the asset sentence: what a reader downloads and the command that flashes it.

**The headline section, boundaries immediately inside it.** State the change in a stranger's terms — 27C programming now applies a fixed-width pulse per byte and verifies after each one, repeating to a per-protocol backstop, with the pulse width read from the chip database rather than baked in as a protocol constant, and a failure reporting the address and the pulse count instead of failing the block opaquely. Then, before anything else and in the same section, the boundaries: bench evidence exists for **one** of the three 27C protocols, on one named part, one controller and one shield revision, with the other two **skipped-with-reason** naming the part nobody had and neither inferred from the first; intra-block progress during long writes reaches one controller class only, and on the other the emission is compiled out **structurally** rather than by choice, with the reason in one clause; and the ARM target's state exactly as `146-ARM-BUILD-RECORD.md` observed it — if that build was green locally, say so **with** the scoping caveat in the same breath, that the local toolchain needed packages the CI install does not carry, so it establishes a delta against a target never before compiled against this milestone's code and is **not** CI parity. State once that neither repository's CI has run any of this milestone's code and that no board exists for the ARM target.

**The new capability, then the safety section.** The per-run pulse override in a firmware reader's terms: the host can override the per-run pulse width, the bound is what it is, and that bound is parity with another programmer's own integer width rather than a wire-type or hardware limit. Then the firmware-side reality: the only pre-flight refusal is keyed on a per-protocol energy cap, non-zero on one protocol and zero on the other two, so on those two there is no firmware-side upper bound today — name backlog item 999.31, which owns that decision, and do not present any of this as a fix. Then, under a *before you plug anything in* heading: the erase-skip option's warning, the force flag's meaning, and the hardware fact that matters most — the raised program-VCC the vendor algorithms assume for threshold margin is unreachable on this shield, so this is timing, pulse-count and verify fidelity and **not** silicon-margin fidelity. That sentence carries the voltage figure and a silicon-margin phrase and is what satisfies this file's caveat requirement.

**What is established** and **What this release does not establish** — both renamed off the donor's headings. Draw each established item from a ledger claim-table row's permitted wording, including the flash and RAM position with its admitted band exemption stated plainly rather than laundered; draw each not-established item from the same rows' non-claim column, and include all five of: no comparative claim, because no control run was made and the historical pre-milestone figure is not a control; no datasheet-conformance claim in either direction, paraphrased with a citation rather than quoted; the two unvalidated protocols; the unexplained intermittent single-byte margin failure, mitigated by clean cycles rather than root-caused; and the never-instrumented program-window rail behaviour under load.

Close with the capability boundary and a feedback ask naming what would actually help: the two missing parts, and a data point on the true-UV variant. Write in the phase's vocabulary throughout — *validated*, *established*, *measured*, *demonstrated*, *skipped-with-reason* — never the bare unqualified claim word, including in hyphenated compounds. Do not commit yet.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-RELEASE-NOTES-fw.md > /tmp/gsd146/gate_fw.txt 2>&1; rc=$?; echo "gate_rc=$rc"; cat /tmp/gsd146/gate_fw.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && v=$(grep -cE '[0-9]+\.[0-9]+\.[0-9]+b[0-9]*' 146-RELEASE-NOTES-fw.md || true); w=$(grep -oiE '\bproven\b' 146-RELEASE-NOTES-fw.md | wc -l); s=$(grep -cE '^## ' 146-RELEASE-NOTES-fw.md || true); echo "version_literals=$v claim_word=$w sections=$s"; test "$v" = "0" && test "$w" = "0" && test "$s" -ge 6</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && miss=0; for tok in '6.25' 'W27C512' 'leonardo' '--pulse-us' '999.31'; do grep -qF "$tok" 146-RELEASE-NOTES-fw.md || { echo "MISSING $tok"; miss=1; }; done; n=$(grep -ciE 'silicon[-[:space:]]margin' 146-RELEASE-NOTES-fw.md || true); awk '/^## /{c++} c<2' 146-RELEASE-NOTES-fw.md > /tmp/gsd146/fw_head.txt; h=$(grep -ciE 'skipped-with-reason|one part' /tmp/gsd146/fw_head.txt || true); echo "missing=$miss narrowing=$n headline_boundary=$h"; test "$miss" = "0" && test "$n" -ge 1 && test "$h" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - The gate run against the firmware body alone exits **0** with no forbidden hit and no missing-caveat complaint, its `PASS:` line recorded; and the second leg prints `version_literals=0`, `claim_word=0` and `sections` at six or more.
    - Exactly **one** placeholder token appears, marked as filled in at cut time from the observed release listing. Two or zero both fail; the count is stated in the summary because the token's spelling is the author's choice.
    - The third leg prints `missing=0`, `narrowing` at one or more, and `headline_boundary` at one or more — so the file carries the voltage figure, the validated part identifier, the controller name, the override flag spelling and backlog 999.31, and the bench boundary sits **inside** the headline section. Negative control: run the same `awk` slice over a copy with the headline's boundary sentences deleted and confirm the locator reports zero.
    - The controller-class boundary says the emission is compiled out structurally, making clear it is not a choice; the ARM state is stated from `146-ARM-BUILD-RECORD.md` with its scoping caveat in the same breath if the build was green, plus the no-CI-run and no-board statements.
    - The established and not-established sections each carry at least four items, every one traceable to a named ledger claim-table row with the mapping recorded in the summary one line per item, the not-established section carries all five named items, and no file under either sub-repo changed.
  </acceptance_criteria>
  <done>The firmware body is version-agnostic with one marked placeholder, states its bench, controller-class and ARM boundaries inside the headline section, carries the ceiling as its safety fact, and maps item by item onto ledger rows.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the host body for a CLI user, cross-check both bodies, gate them and commit</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-app.md, .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RELEASE-NOTES-fw.md</files>
  <read_first>
    - `137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md` — the leanest, most recent host body and the spine to follow: install line, a sentence on what the release page does and does not carry and where the matching firmware lives, per-change what-then-why, an also-in-this-release section, an honest-asymmetry section and an ask. Its asymmetry heading carries the bare claim word and must be renamed.
    - `firestarter_app/firestarter/cli_handlers.py:546-610` and `firestarter_app/README.md` as edited by plan 146-07 — the shipped option help text including the override's provenance sentence and the erase-skip warning. Take names and behaviour from here; the body must not describe an option differently from the README.
    - `143-host-timeout-progress-pulse-override/143-HOST-RECORD.md` §§1, 4, 5 — the honest headline, the padding rule, and the non-claims: controller-only emission, the board-specific residual-gap threshold, and the override's bound provenance.
    - `146-LEDGER.md` (this directory) — the claim-table rows this body maps onto, especially long-block survival with progress and the pulse override; and `146-RELEASE-NOTES-fw.md` as written in Task 1, which this body must agree with and must not duplicate at length.
  </read_first>
  <action>
Write the host body on the donor's leaner spine with its asymmetry heading renamed, then gate and commit both.

**Self-identification, install line, placeholder.** Title it a host prerelease without naming a version; exactly one placeholder token near the top, marked as filled in at cut time; the prerelease install command; and the donor's sentence on what the release page does and does not carry and where the matching firmware lives, because a user who upgrades one half without the other needs to know which half does what.

**What changed, in a user's terms** — per change, what then why. (1) *Long writes no longer time out, and you can see them progressing*: a write taking tens of seconds now reports progress from inside the block instead of appearing to hang — then the boundary immediately, that intra-block progress arrives on one controller class only and on the other the emission is compiled out of the firmware structurally, so the display falls back to block granularity; name both classes. State the cosmetic artefact honestly, because a user will see it: the write progress bar does not reach a full hundred percent, this is a display artefact, every write is verified byte-exact regardless, and the behaviour is filed as backlog 999.30. A user must not be able to read a partial bar as a partial write. (2) *A max-pulse failure now names the address and the pulse count*, where previously such a failure surfaced as a transport error — that is the diagnostic difference. (3) *A per-run pulse override*: name the flag, its bound, and in one clause that the bound is parity with another programmer's own integer width rather than a wire-type or hardware limit; say what it is for — trying a wider pulse on a marginal part for one run without editing the chip database — point at where the database value lives and how a permanent override is done, and state that it introduces no new wire field.

**Also in this release, then the two halves.** The write-option surface the documentation now describes correctly, including the two entries whose documented name and behaviour were both wrong before this release — describe the correction from the user's side, which is that the blank-check option's documented name and its erase behaviour were both wrong, and carry the erase-skip warning across at full strength rather than paraphrasing it milder. Then **what is established, and what is not** — renamed off the donor's heading, both halves, each item traceable to a ledger row. Established: one 27C protocol validated end to end on a named part, controller and shield revision; the override exercised on that part; both suites passing locally at measured counts. Not established: the other two protocols, skipped-with-reason naming the missing parts and not inferred from the first; no comparative claim, since no control run was made and the historical figure is not a control; no CI run against any of this milestone's code; the residual-gap threshold as a **board-specific** number rather than a universal one; and the raised program-VCC ceiling the vendor algorithms assume and this shield cannot reach, so this is timing, pulse-count and verify fidelity and **not** silicon-margin fidelity — that sentence carries both caveats and satisfies this file's caveat requirement. Close with an ask naming the two missing parts and reports from anyone running the two unvalidated protocols.

**Then cross-check, gate, freeze and commit.** Record three named cross-checks against the firmware body and the host README — the ceiling wording, the controller-class boundary, and the override's bound provenance — each with both texts quoted, and resolve any divergence by editing a body. Run the gate against **each body alone** in positional-argument mode and record both runs. Do **not** run or assert the all-five default run or the fixture suite's armed-against-real-files leg: plan 146-09 executes in this same wave, so the reconciliation may not exist yet, and plan 146-11 owns both of those observations. Show the caveat locator can fail by stripping the voltage figure and the silicon-margin phrase from a scratch copy and confirming the gate exits non-zero naming the missing labels. Record both bodies' `git hash-object` blob SHA and `wc -c` byte count in **this plan's SUMMARY** rather than in `146-CITATIONS.md`, which 146-11 owns and which a concurrent sibling must not append to. Commit both bodies in the meta repository with a message naming CLOSE-05, and state plainly in the summary that they remain **drafts** behind the blocking wording review and that nothing was pushed, cut, tagged or published.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && ok=0; for f in 146-RELEASE-NOTES-fw.md 146-RELEASE-NOTES-app.md; do python3 146-check-claims.py "$f" > /tmp/gsd146/gate_body.txt 2>&1; rc=$?; echo "--- $f rc=$rc"; cat /tmp/gsd146/gate_body.txt; test "$rc" = "0" && ok=$((ok+1)); done; echo "green_bodies=$ok"; test "$ok" = "2"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && v=$(grep -cE '[0-9]+\.[0-9]+\.[0-9]+b[0-9]*' 146-RELEASE-NOTES-app.md || true); w=$(grep -oiE '\bproven\b' 146-RELEASE-NOTES-app.md | wc -l); miss=0; for tok in '--pulse-us' '6.25' 'pip install' '999.30'; do grep -qF "$tok" 146-RELEASE-NOTES-app.md || { echo "MISSING $tok"; miss=1; }; done; b=$(grep -ciE 'byte-exact|byte exact' 146-RELEASE-NOTES-app.md || true); n=$(grep -ciE 'silicon[-[:space:]]margin' 146-RELEASE-NOTES-app.md || true); echo "version_literals=$v claim_word=$w missing=$miss byte_exact=$b narrowing=$n"; test "$v" = "0" && test "$w" = "0" && test "$miss" = "0" && test "$b" -ge 1 && test "$n" -ge 1</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && sed -E 's/6\.25//g; s/[Ss]ilicon[- ][Mm]argin//g' 146-RELEASE-NOTES-app.md > /tmp/gsd146/app_stripped.md && python3 146-check-claims.py /tmp/gsd146/app_stripped.md > /tmp/gsd146/gate_app_neg.txt 2>&1; rc=$?; echo "negative_control_rc=$rc"; grep -i caveat /tmp/gsd146/gate_app_neg.txt; for f in 146-RELEASE-NOTES-fw.md 146-RELEASE-NOTES-app.md; do echo "$f blob=$(git hash-object $f) bytes=$(wc -c < $f)"; done; echo "fw_porcelain=$(git -C /workspaces/firestarter status --porcelain | wc -l)"; test "$rc" != "0" && test "$(git -C /workspaces/firestarter status --porcelain | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The first leg prints `green_bodies=2`: each body gates green **individually** in positional-argument mode. No criterion in this plan asserts an outcome for the all-five default run or for the armed-against-real-files leg, because 146-09 executes in this same wave and 146-11 owns both.
    - The second leg prints `version_literals=0`, `claim_word=0`, `missing=0`, `byte_exact` at one or more and `narrowing` at one or more; and exactly one placeholder token appears, marked as filled in at cut time, with its count stated in the summary.
    - The progress-bar paragraph states all three of: the bar does not reach a full hundred percent, this is a display artefact, and every write is verified byte-exact — so a user cannot read a partial bar as a partial write.
    - The controller-class boundary names both classes and says the emission is compiled out on one of them rather than merely absent; the option-surface section names the corrected blank-check option and the separate erase-skip option and carries the erase-skip warning at the strength of the help text in `cli_handlers.py`.
    - Both halves of the established / not-established section carry at least four items each, every item mapped to a named ledger row in the summary, and the not-established half states the board-specific residual-gap threshold, the absence of any CI run against this milestone's code, and the no-comparative-claim position with the historical figure named as not a control.
    - Three cross-checks against the firmware body and the host README are recorded with both texts quoted — the ceiling wording, the controller-class boundary and the override's bound provenance — and any divergence was resolved by editing a body.
    - The third leg's `negative_control_rc` is **non-zero** with the gate naming the missing caveat labels; both blob SHAs and `wc -c` byte counts are recorded in this plan's SUMMARY rather than in `146-CITATIONS.md`; `fw_porcelain=0`; and both bodies are committed with the summary stating plainly that they are drafts behind the blocking wording review and that nothing was pushed, cut, tagged or published.
  </acceptance_criteria>
  <done>The host body is version-agnostic with one marked placeholder, describes the three user-visible changes with their boundaries at the point of the claim, refuses to let a partial progress bar read as a partial write, carries the ceiling, agrees with the firmware body on three cross-checked points, and both bodies gate green individually and are committed as drafts.</done>
</task>

</tasks>

<verification>
- Both release bodies committed, each exit 0 under the claim gate run against it alone; no assertion made about the all-five default run or the armed leg, which 146-11 owns. Version-agnosticism, claim-word absence, the caveat pair and every named content anchor measured by printed integers, with the caveat locator shown able to fail.
- Freeze values in this plan's SUMMARY with `146-CITATIONS.md` untouched; neither sub-repo modified; nothing pushed, cut, tagged or published; no `CLOSE-*` row moved.
</verification>

<success_criteria>
A stranger reading either body learns what changed, what to do about it, what the evidence covers and what it does not — with no version to trust, no comparative claim, and no way to mistake a partial progress bar for a partial write. Both remain drafts until the operator reviews the wording.
</success_criteria>

<output>
Create `146-10-SUMMARY.md` in this phase directory. Record both gate runs with their `PASS:` lines, the placeholder counts, the ledger-row mapping one line per item, the three cross-checks with quotations, the negative control with the labels the gate printed, and both freeze blob SHAs and byte counts.
</output>
