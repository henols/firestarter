---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 11
type: execute
wave: 5
depends_on: ["146-04", "146-07", "146-09", "146-10"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-11-SUMMARY.md
autonomous: true
requirements: [CLOSE-01]
must_haves:
  truths:
    - "Per D-12 CLOSE-01 makes two distinct claims — armed against the real files, and seen to fail on a planted violation — and neither proof covers both, so both exist and each is recorded separately"
    - "The plant lands in a REAL, tracked, committed closing artifact and is reverted to byte identity, proven by blob SHA and byte count on both sides rather than by eye; per D-11 the failure is observed through the no-argument no-environment defaults path, the only run that proves the default target list is wired to the files that ship; and every recorded exit status is the script's own, captured immediately and never a pipeline's"
    - "Every standing gate in the phase is green in one pass at the end, and both sub-repo suites run only after all three repositories are committed clean"
    - "This plan owns leg 9's GREEN — 146-04 recorded it RED as the gate's fail-closed missing-target branch, and it is reachable here for the first time because all five closing artifacts now exist"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md"
      provides: "Section 4: the armed default run, the plant-and-revert transcript with three captured exit statuses and both blob SHAs, leg 9's three observations, the all-gates-green pass, and the CLOSE-01 audit table"
      contains: "rc_planted"
  key_links:
    - from: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md"
      via: "the default target list, exercised through the no-argument no-environment path so the plant proves the list is wired to a file that ships"
      pattern: "_DEFAULT_TARGETS"
  prohibitions:
    - "MUST NOT plant into a fixture, via the environment seam, or via a positional argument — a fixture plant proves nothing about the default target list, which is the claim this transcript exists to discharge"
    - "MUST NOT leave the planted text in place: the revert is proven by blob SHA and byte count on both sides, and a non-matching SHA is a stop-and-report, never a re-edit"
    - "MUST NOT capture an exit status after a pipe; MUST NOT edit the gate, the suite, any fixture or the pattern table to make anything pass (D-14); MUST NOT run either sub-repo suite before the repositories are committed clean; MUST NOT tick any CLOSE requirement (only 146-13 may)"
    - "MUST NOT run git push, git merge, git tag, gh workflow run, gh release, gh issue edit or gh issue close (D-01), and MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Discharge CLOSE-01's second claim: arm the gate against the real files, record it being seen to fail on a planted violation in one of them, then bring every standing gate in the phase green in a single pass.

CLOSE-01 makes two claims that look like one. *Armed against the real files* is about the default target list; *seen to fail on a planted violation* is about the pattern table. A fixture suite proves the second and says nothing about the first — a checker with perfect fixtures and a stale default list is exactly the recorded defect this shape exists to catch, which is why D-12 requires both. This plan owns the proof the suite cannot give. Two measured cautions: an exit status taken after a pipe reports the pipe's last command, which in a recorded case printed zero for a script that had just failed; and the revert is proven by blob identity, not by reading the file. Output: `146-CITATIONS.md` §4, and every gate in the phase green in one recorded pass.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-VALIDATION.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plant, observe the failure through the defaults path, revert to byte identity, record §4</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md, .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md</files>
  <read_first>
    - `146-check-claims.py` (this directory) — the twelve pattern labels as implemented, and the exact strings the gate prints when it names a file, a line number and a label, so the transcript's assertions match text the gate actually emits.
    - `146-04-SUMMARY.md:125` (this directory) — the probed label used by the fixture suite's second leg. Reuse **that** label for the plant so its behaviour is already observed rather than assumed, and cite it by label id and by `file:line` rather than reproducing the phrase (D-14).
    - `146-CITATIONS.md` (this directory) — the register as earlier plans left it: §§0-2 from 146-01, §3 from 146-04. Section 4 is appended without disturbing any of them; plans 146-09 and 146-10 deliberately did **not** write here, so their freeze values live in their own SUMMARYs and are folded in from there. And `139-gh-15-correction-outward/139-CITATIONS.md` §4 — the two-run transcript shape to reproduce, including its sub-section recording a mid-task correction rather than smoothing it over.
  </read_first>
  <action>
Choose `146-LEDGER.md` as the plant target: it is tracked, committed by plan 146-08, and is the artifact whose integrity matters most. Before anything else confirm `git status --porcelain` on that path is empty. A plant against an uncommitted file cannot be reverted with a checkout — if that is the situation, stop and report rather than improvising a content restore.

**Step 0 — baseline.** Run the gate with **no argument and no environment override**; confirm exit 0 with a pass line naming all five artifacts, capturing the status immediately after the command and not after a pipe. Record the pass line verbatim. This is also the first time that run can succeed, since 146-09 and 146-10 only just landed the last three artifacts.

**Step 1 — identity before the plant.** Record `git hash-object` on the ledger and its byte count from `wc -c`.

**Step 2 — plant one forbidden phrase.** Append a single short line to the **real** ledger carrying the probed label's phrase, prefixed by an inline marker making it obviously a plant. Run the gate again with **no argument and no environment override**. It must exit **non-zero** and its output must name three things: the ledger's basename, the planted line's line number, and the specific label. Assert all three and record the output verbatim. If any of the three is missing, that is a defect in the gate's reporting and is recorded rather than worked around.

**Step 3 — leg 9's third observation, before the revert.** With the plant still in place, run the fixture suite's `test_armed_against_the_five_real_closing_artifacts` leg by name and confirm it **fails**. Record all three observations of that leg in one place, naming the plan that recorded each: RED before the artifacts existed (146-04, its named missing-target reason), GREEN once they existed (step 4 below, this plan), and RED again under perturbation (here). Sequence matters — this leg must run before the revert.

**Step 4 — revert and prove it by identity.** Restore the file with a checkout of that path, then re-record `git hash-object` and `wc -c`; both must equal step 1's values exactly. A mismatch is a **stop-and-report**: do not hand-edit to make the hash match, because the point of the assertion is that the revert mechanism worked. Then run the gate a third time through the defaults path — exit 0, with a pass line identical to step 0's — and run the full fixture suite, which must now be fifteen green including leg 9.

**Then append §4** to `146-CITATIONS.md` on the donor's two-run shape: a sub-section for the planted run that must fail and was observed failing, one for the default-mode run that must pass and was observed passing, and — if anything went differently from this plan — one recording the mid-task correction rather than smoothing it over. The transcript table carries these named fields: `rc_before`, `rc_planted`, `rc_after`, blob SHA before and after, byte count before and after, the planted label observed, and the line number the gate reported. Fold in the freeze values from 146-09's and 146-10's SUMMARYs as a three-row table so the register carries them in one place.

Commit `146-CITATIONS.md`. The ledger must appear in **no** diff.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py > /tmp/gsd146/gate_final.txt 2>&1; rc=$?; echo "gate_default_rc=$rc"; cat /tmp/gsd146/gate_final.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces && L=.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md; p=$(git -C /workspaces status --porcelain -- "$L" | wc -l); d=$(git -C /workspaces diff --numstat -- "$L" | wc -l); echo "ledger_porcelain=$p ledger_numstat_lines=$d blob=$(git hash-object "$L") bytes=$(wc -c < "$L")"; test "$p" = "0" && test "$d" = "0"</automated>
    <automated>cd /workspaces && C=.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md; miss=0; for s in 'rc_before' 'rc_planted' 'rc_after' '## 4.' '## 0.' '## 1.' '## 2.' '## 3.'; do grep -qF "$s" "$C" || { echo "MISSING $s"; miss=1; }; done; echo "missing_fields=$miss"; test "$miss" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The ledger was **committed and clean** before the plant, and the transcript records three exit statuses under the named fields — zero before the plant, **non-zero** with the plant, zero after the revert — each captured immediately after its command, with the record stating so; none came from after a pipe.
    - The planted run was invoked with **no argument and no environment override**. A plant routed through the environment seam or a positional argument does not satisfy this criterion, because it would not exercise the default target list.
    - The planted run's output names all three of the ledger's basename, the planted line's line number and the specific label, with the gate's output quoted verbatim; the label is one whose behaviour 146-04 already probed, cited by label id and `file:line` rather than reproduced.
    - The blob SHA and byte count before and after the revert are recorded and **equal**, the second leg prints `ledger_porcelain=0` and `ledger_numstat_lines=0`, the step-0 and step-4 pass lines are recorded and identical to each other, and the first leg prints `gate_default_rc=0`.
    - Leg 9 was run **with the plant in place** and failed, and all three of its observations are recorded in one place naming the plan that recorded each: RED before the artifacts existed, GREEN after, RED under perturbation. The full suite is fifteen green after the revert.
    - `146-CITATIONS.md` gains a `## 4.` section, still contains `## 0.` through `## 3.` unmodified, and carries the three-row freeze table folded in from 146-09's and 146-10's SUMMARYs. No file under either sub-repo changed.
  </acceptance_criteria>
  <done>The gate has been seen to fail on a planted violation in a real committed closing artifact through its own default target list, naming the file, the line and the label; the artifact is byte-identical afterwards by blob SHA and byte count; and leg 9's three-observation triple is recorded in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Bring every standing gate green in one pass, run both sub-repo suites, audit CLOSE-01's two claims</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md</files>
  <read_first>
    - `146-VALIDATION.md` §"Sampling Rate", the phase-gate row — the exact set that must be green together before verification: the claim gate on all five, the documentation checker, the record gate, both sub-repo suites at their measured baselines, and the plant-and-revert transcript.
    - `146-CITATIONS.md` §0 — the recorded baselines this task compares against: both sub-repo suite counts, the record gate's exempt tally, the three-repo porcelain, and the three-repo upstream-ahead counts (meta **233**, `firestarter` **61**, `firestarter_app` **16**).
    - `.planning/REQUIREMENTS.md:256-258` — CLOSE-01's text and its two distinct claims, which the audit table maps proof by proof.
    - `firestarter/tests/test_flash_path_record_sync.py` and `firestarter_app/tests/test_py32_flash_map_host.py` — the two whole-repo porcelain assertions that make the commit-before-run ordering mechanical; the host suite asserts the **firmware** repository's porcelain across the repository boundary.
    - `130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py` — the record gate, whose exempt tally must still match §0's baseline after all eight correction blocks landed. Note §0's baseline was **RED**, and 146-05 recorded and explained the flip to green; compare against the value 146-05 recorded as current, not against a green assumption.
  </read_first>
  <action>
Run everything in one pass and record each result with its own captured exit status.

**Precondition — commit first, everywhere.** Confirm all three repositories are committed clean apart from their recorded pre-existing untracked sets: the firmware repository's porcelain exactly zero lines, the host's equal to its recorded baseline, and the meta repository with nothing of this phase's uncommitted. Both sub-repo suites assert the firmware repository's whole-repo porcelain, so a mid-change run turns both red for a reason unrelated to any change in this phase.

**The five gates.** Run each and record its command, its own exit status and its evidence line: (1) the claim gate with no argument and no environment override — exit 0, pass line naming all five artifacts; (2) the fixture suite in full — exit 0, fifteen legs; (3) the documentation checker with no argument and no environment override — exit 0, pass line naming all four documentation targets; (4) the record gate — exit 0, with its exempt-hit tally compared bucket by bucket against the value 146-05 recorded as current, a moved bucket being a stop-and-report; (5) both sub-repo suites at their measured baselines, using the count-preserving invocation on the host whose own configuration already passes a quiet flag, since doubling it hides the count line and the count is the evidence. Record each suite's passed count, its snapshot count where reported, and its wall time; a count below baseline is a stop-and-report naming the failing test.

**The CLOSE-01 audit table.** One row per claim CLOSE-01 makes, one column per proof, every cell filled. The two claims are *armed against the real files* and *seen to fail on a planted violation*; the two proofs are the fixture suite and the plant-and-revert transcript. Beneath the table, in prose, state explicitly that the fixture suite does **not** discharge the arming claim and the plant does **not** discharge the pattern-table claim — that asymmetry is why D-12 required both, and a reader who does not see it stated will assume one proof covers everything. Cite each proof's location by `file:line` or by section.

**A no-push checkpoint, mid-phase.** Re-measure the upstream-ahead count in all three repositories and compare against §0's baselines. Every one must be **greater than or equal to** its baseline; the expected shape is the baseline plus this phase's own commits in each repository. A count that **dropped** means an upstream ref advanced, which means something was pushed — a stop-and-report, because D-01 has no mechanical enforcement in this project and this arithmetic is its only oracle.

**Then append the results to §4** as an all-gates-green sub-section, and commit. If any gate is red, do not proceed and do not adjust the gate: record the failure, read it, and fix the artifact it is complaining about. A gate edited to agree with an artifact is worth nothing.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py > /tmp/gsd146/g1.txt 2>&1; a=$?; python3 146-check-close03-docs.py > /tmp/gsd146/g2.txt 2>&1; b=$?; python3 /workspaces/.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py > /tmp/gsd146/g3.txt 2>&1; c=$?; echo "claim_gate_rc=$a doc_checker_rc=$b record_gate_rc=$c"; cat /tmp/gsd146/g1.txt /tmp/gsd146/g2.txt; grep -o 'exempt hits by verdict.*' /tmp/gsd146/g3.txt; test "$a" = "0" && test "$b" = "0" && test "$c" = "0"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && fwp=$(git -C /workspaces/firestarter status --porcelain | wc -l); echo "fw_porcelain=$fwp"; test "$fwp" = "0" || exit 1; python3 -m pytest .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py -o addopts="" -q > /tmp/gsd146/s0.txt 2>&1; s=$?; (cd /workspaces/firestarter && python3 -m pytest tests -o addopts="" -q > /tmp/gsd146/s1.txt 2>&1); f=$?; (cd /workspaces/firestarter_app && python3 -m pytest tests -o addopts="" -q > /tmp/gsd146/s2.txt 2>&1); h=$?; echo "fixture_suite_rc=$s fw_suite_rc=$f app_suite_rc=$h"; tail -2 /tmp/gsd146/s0.txt /tmp/gsd146/s1.txt /tmp/gsd146/s2.txt; test "$s" = "0" && test "$f" = "0" && test "$h" = "0"</automated>
    <automated>cd /workspaces && m=$(git -C /workspaces rev-list --count @{u}..HEAD); f=$(git -C /workspaces/firestarter rev-list --count @{u}..HEAD); a=$(git -C /workspaces/firestarter_app rev-list --count @{u}..HEAD); echo "ahead meta=$m firestarter=$f app=$a (baselines 233/61/16)"; C=.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md; x=$(grep -ciE 'armed against the real files' "$C" || true); y=$(grep -ciE 'seen to fail' "$C" || true); echo "audit_armed=$x audit_seen=$y"; test "$m" -ge 233 && test "$f" -ge 61 && test "$a" -ge 16 && test "$x" -ge 1 && test "$y" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - All three repositories were committed clean before either sub-repo suite ran: the second leg prints `fw_porcelain=0`, the host porcelain equals its recorded baseline, and the meta repository had nothing of this phase's uncommitted.
    - All five gates are green in one recorded pass, each with its own captured exit status: the first leg prints `claim_gate_rc=0`, `doc_checker_rc=0` and `record_gate_rc=0` with the claim gate's pass line naming five artifacts and the documentation checker's naming four targets; the second prints `fixture_suite_rc=0`, `fw_suite_rc=0` and `app_suite_rc=0` with counts at or above their recorded baselines.
    - The host suite's count line is visible in the recorded output, which requires the count-preserving invocation; and the record gate's exempt tally is compared bucket by bucket against the value **146-05 recorded as current** rather than against §0's RED baseline, with the comparison recorded and a moved bucket treated as stop-and-report.
    - The CLOSE-01 audit table exists with one row per claim and one column per proof, every cell filled, and prose beneath stating explicitly that neither proof covers both claims. A table without that prose fails the criterion, because the asymmetry is the point. Each proof's location is cited by `file:line` or by section.
    - The third leg prints all three ahead counts and each is **greater than or equal to** its §0 baseline of 233 / 61 / 16. A dropped count was treated as a stop-and-report indicating a push.
    - `146-CITATIONS.md` §4 gains an all-gates-green sub-section carrying all seven results with their commands and captured statuses, and is committed. No gate, suite, fixture or pattern table was edited in this task.
  </acceptance_criteria>
  <done>Every standing gate in the phase is green in one recorded pass with both sub-repo suites at or above baseline, the record gate's exempt tally is unchanged from the value 146-05 recorded, CLOSE-01's two claims are audited proof by proof with the asymmetry stated, and the three-repo no-push arithmetic holds.</done>
</task>

</tasks>

<verification>
- `146-CITATIONS.md` §4 committed with the plant-and-revert transcript, leg 9's three observations, the all-gates-green pass and the CLOSE-01 audit table; §§0-3 intact plus the folded three-row freeze table.
- `146-LEDGER.md` byte-identical: empty porcelain and empty numstat after the plant and revert, blob SHA and byte count equal on both sides. Five gates green in one pass; both sub-repo suites at or above baseline, run only after committing; three-repo ahead counts at or above 233 / 61 / 16; no `CLOSE-*` row moved.
</verification>

<success_criteria>
CLOSE-01's two claims are separately discharged and the separation stated rather than assumed: the gate is armed at the files that ship, it has been seen to fail on a planted violation in one of them through its own default target list, and the artifact planted into is provably byte-identical afterwards.
</success_criteria>

<output>
Create `146-11-SUMMARY.md` in this phase directory. Record the full transcript table, both pass lines, leg 9's three observations, all seven gate results with captured statuses, the record gate's bucket-by-bucket comparison, the three-repo ahead counts against baseline, and the CLOSE-01 audit table.
</output>
