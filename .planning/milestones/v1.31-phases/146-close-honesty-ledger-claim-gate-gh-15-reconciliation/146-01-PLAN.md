---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-01-SUMMARY.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "Per D-11 the claim gate resolves exactly five targets, all built from the script's own directory, and a partial set (1-4 of 5 present) is a hard failure rather than a skip"
    - "Per D-11 the required-caveat rule is per-file: four targets require both caveats, 146-CORRECTIONS.md requires neither, and an unknown basename gets the FULL set so the rule fails closed"
    - "Per D-14 the twelve forbidden patterns are transcribed verbatim from the donor and no proximity window, exclusion marker or heading quarantine is introduced anywhere"
    - "The phase's structural no-push baseline exists before any other plan runs: the upstream-ahead count in all three repos, the working-tree porcelain in all three repos, and the live gh#15 read-only state"
    - "The gate cannot exit 0 on nothing scanned and cannot exit 0 with a target silently skipped"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py"
      provides: "The D-11 all-or-nothing claim gate over this phase's five closing artifacts, with the per-file caveat map and the startup self-check"
      contains: "_CAVEAT_RULES"
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md"
      provides: "Sections 0-2: the phase-start before-state table, the pinning strategy, and the anchor verification table"
      contains: "rev-list --count"
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-check-claims.py"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py"
      via: "a 146-scoped sibling: same twelve patterns, same no-window design, same fail-closed order of operations, with the four mandatory renames applied"
      pattern: "_assert_default_targets_are_local"
  prohibitions:
    - "MUST NOT use glob, os.walk, Path.rglob or any wildcard anywhere near _DEFAULT_TARGETS — a 146-*.md default set sweeps in 146-CONTEXT.md (6 hits) and the fixtures directory"
    - "MUST NOT reuse the env-seam name FIRESTARTER_CLAIMSCAN_TARGETS_V131 — it is already live in Phase 139 this same milestone"
    - "MUST NOT copy the donor's explicit non-claim #2 at 139-check-claims.py:57-61 — it asserts that the script is NOT a build of CLOSE-01, which is the opposite of this plan's deliverable"
    - "MUST NOT add an UNARMED exit-0 branch — an exit-0-on-nothing-scanned path is a green that proves nothing"
    - "MUST NOT loosen, narrow or re-derive any of the twelve forbidden patterns, and MUST NOT add a proximity window or context condition (D-14)"
    - "MUST NOT tick CLOSE-01 or any other CLOSE requirement — only plan 146-13 may tick CLOSE-01 through CLOSE-05"
    - "MUST NOT run git push, git merge, git tag, gh workflow run, gh release, gh issue edit or gh issue close (D-01)"
    - "MUST NOT edit .claude/settings.local.json or add any permission-allowlist entry"
    - "MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Establish the phase's structural before-state, then author the D-11 claim gate as a 146-scoped sibling of
`139-check-claims.py`.

Purpose: two things must exist before anything else in this phase can be trusted. First, the no-push boundary
(D-01) has **no mechanical enforcement** in this project — `git push`, `gh workflow` and `gh release` are all
allowlisted — so the only available proof is a recorded before-state that plan 146-13 asserts against.
Second, per constraint 2 the claim gate must exist before any closing artifact is called final; per D-11 it
arms all-or-nothing on five files with per-file caveat rules, and per D-14 its vocabulary is a **writing
constraint** on every artifact this phase produces, so it has to be readable by the plans that come after.

Output: `146-CITATIONS.md` sections 0-2, and `146-check-claims.py` with its runtime self-check demonstrated.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_this_phase_produces>
The full phase inventory is repeated in every plan so the source-grounding pass can exclude every newly
created symbol from drift verification. **Every path and name below is new in Phase 146.**

**New scripts (meta repo, this phase directory):**
- `146-check-claims.py` — module globals `_HERE`, `_DEFAULT_TARGETS`, `FORBIDDEN_PATTERNS`,
  `REQUIRED_CAVEAT_PATTERNS`, `_ALL_CAVEAT_LABELS`, `_CAVEAT_RULES`,
  `FIRESTARTER_CLAIMSCAN_TARGETS_146`; functions `_assert_default_targets_are_local()`,
  `_required_caveats_for()`, `resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`
- `146-check-close03-docs.py` — `_find_repo_root()`, `_REPO_ROOT`, `_DEFAULT_TARGETS`,
  `_DOC_TARGET_ALLOWLIST`, `_REQUIRED_TOPICS_BY_FILE`, `REQUIRED_TOPIC_PATTERNS`,
  `_required_topics_for()`, `_assert_default_targets_are_allowlisted()`, `resolve_targets()`,
  `scan_text()`, `main()`, env seam `FIRESTARTER_DOCSCAN_TARGETS_146`
- `test_check_claims_v131.py` — `_HERE`, `_SCANNER`, `_run_scanner()`, `_import_scanner_module()`, and its
  thirteen `test_*` legs

**New fixtures:** `fixtures/clean_control.md`, `fixtures/clean_control_second.md`,
`fixtures/planted_forbidden_claim.md`, `fixtures/planted_proven_unqualified.md`,
`fixtures/planted_missing_caveat.md`

**New `.planning` artifacts:** `146-LEDGER.md`, `146-CORRECTIONS.md`, `146-GH15-RECONCILIATION.md`,
`146-RELEASE-NOTES-fw.md`, `146-RELEASE-NOTES-app.md`, `146-CITATIONS.md`, `146-ARM-BUILD-RECORD.md`,
`146-DOC-CHECK-RECORD.md`, `arm-build/` (raw build logs), and `146-0N-PLAN.md` / `146-0N-SUMMARY.md`

**New doc sections in existing files:** `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 replacement write-algorithm
text plus a pulse-override sub-item and a program-VCC-ceiling note; `firestarter/CLAUDE.md` a
`**⚠ CORRECTION (Phase 146 / CLOSE-04, origin F-144-01)**` block and a program-VCC-ceiling sentence;
`firestarter/README.md` a 27C paragraph under §Protocol Notes; `firestarter_app/README.md` four added
`write` option entries and a program-VCC-ceiling note; `⚠ CORRECTION` blocks in `.planning/ROADMAP.md`,
`.planning/PROJECT.md` and `.planning/REQUIREMENTS.md`

**New message text:** a replacement `format` string for `DBG_PULSE_DELAY_MISMATCH` in
`tools/catalog/messages.toml` (three copies) and its regenerated line in
`firestarter_app/firestarter/messages.py`

**New env-var names:** `FIRESTARTER_CLAIMSCAN_TARGETS_146`, `FIRESTARTER_DOCSCAN_TARGETS_146`

No new CLI flag is introduced — `--pulse-us` already ships from Phase 143.
</artifacts_this_phase_produces>

<!-- The three literals below are negative-greped by this plan's acceptance criteria over
     146-check-claims.py. They appear in task actions only as instructions about what that
     script must not contain; they are not written into any scanned file. -->
<!-- planner-discipline-allow: UNARMED -->
<!-- planner-discipline-allow: glob -->
<!-- planner-discipline-allow: FIRESTARTER_CLAIMSCAN_TARGETS_V131 -->

<tasks>

<task type="auto">
  <name>Task 1: Record the phase-start before-state as sections 0-2 of the citation register</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-CITATIONS.md` — read the heading spine and §0's `| Field | Command (as run) | Result |` table, §1's pinning-strategy paragraph, and §2's anchor-verification table. This is the register shape to reproduce; §§3-7 are written by later plans.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RESEARCH.md` §"gh#15 Live State" and §"Code Examples" → "The gh#15 read-only measurement block" — the four read-only commands and their expected values, and the recorded fact that `lastEditedAt` is GraphQL-only.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md` `<code_context>` → "Working-tree dirt to expect" — the pre-existing untracked and modified paths in the meta repo and in `firestarter_app`, so the porcelain snapshot does not read pre-existing dirt as this phase's damage.
  </read_first>
  <action>
Create `146-CITATIONS.md` with a `# Phase 146 — Citation Register` title and exactly three sections for now:
`## 0. Phase-start before-state`, `## 1. Pinning strategy`, `## 2. Anchor verification`. State in a header
sentence that later sections are appended by later plans (§3 the fixture suite, §4 the claim gate's armed run
and plant transcript, §5 the freeze, §6 delivery, §7 the close), and that **nothing in this register is copied
from RESEARCH** — every command was re-run here, and any divergence from research's recorded figure is stated
rather than reconciled by editing the earlier document.

**§0, one row per field, in a `| Field | Command (as run) | Result |` table.** Measure and record all of:

1. The **upstream-ahead count in all three repos** using `git rev-list --count @{u}..HEAD` — run in
   `/workspaces`, `/workspaces/firestarter` and `/workspaces/firestarter_app`. Measured at plan time these
   were meta **230**, `firestarter` **61**, `firestarter_app` **16**; re-measure and record what you observe.
   State explicitly in prose that this is the phase's structural no-push oracle: a `git push` makes the
   upstream tracking ref advance to HEAD and the count **drop**, so plan 146-13 asserts equality against these
   three numbers. State also that D-01 has no mechanical enforcement because `git push`, `gh workflow` and
   `gh release` are all in the permission allowlist, and that no plan in this phase may edit that allowlist.
2. The **branch name and HEAD SHA** in all three repos.
3. The **`git status --porcelain` line count** in all three repos, with the pre-existing entries enumerated by
   path so a later delta assertion has a baseline. Enumerate what you actually observe rather than inheriting a
   list. Measured immediately after this phase's plans were committed, the meta repo carried modified
   `.gitignore`, both gitlinks, and untracked `.claude/`, `.planning/VALIDATED-EPROMS.md`, `package.json`,
   `package-lock.json` — note that the pre-existing two-line Phase 146 heading rename in
   `.planning/ROADMAP.md` was **already folded into the plan-creation commit**, so that file is clean and is not
   part of the expected dirt. `firestarter_app` carried seven untracked paths and `firestarter` was clean.
4. The **tracked gitlink SHAs versus the live sub-repo HEADs** from `git ls-tree HEAD firestarter
   firestarter_app`. The gitlinks are stale by the whole milestone (they still point at v1.30's tips), so
   record the **delta** as a fact — do not write a criterion asserting they match, because they do not and
   have not at any point in v1.31.
5. The **gh#15 read-only state**, five oracles: `state`, `updatedAt`, `lastEditedAt`, comment `totalCount` and
   the single comment's `databaseId`, all from one `gh api graphql` query against `henols/firestarter_prom`
   issue 15; plus the body byte length via `gh issue view … --json body -q .body` piped to `wc -c`; plus the
   unticked-box count via a `grep -c` for the unchecked-checkbox line prefix over the body; plus a `diff` of
   the body's acceptance-criteria tail against `139-GH15-ORIGINAL-CRITERIA.md`. Record that
   `gh issue view --json lastEditedAt` **does not exist** (the field is rejected as an unknown JSON field) and
   that `updatedAt` **bumps on comment creation**, so it is not a body-edit oracle. Expected values from the
   research measurement: state `OPEN`, `lastEditedAt` null, comment count 1, comment id `5233463320`, body
   5964 bytes, 9 unticked boxes, empty diff, labels `[]`. Record what you observe; a divergence is a
   stop-and-report because `146-12`'s `1 → 2` assertion depends on it.
6. The **Phase 130 record-gate baseline**: run
   `python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py`
   and record the exit status and the full `exempt hits by verdict` tally verbatim. Baseline observed at plan
   time: exit 0, `{'block': 23, 'line-label': 4, 'inline-history': 6, 'inline-allow': 10, 'superseded': 12}`.
   State in prose that twelve `superseded` exemptions enumerate explicit 1-based line numbers, so any
   insertion above one of them silently orphans it — which is why plan 146-05 appends correction blocks
   after the text they correct and re-runs this gate after every insertion.

Capture each command's own exit status with `rc=$?` immediately after the command, never after a pipe — a
recorded case in this project printed `EXIT=0` for a script that had just printed a failure line, because the
status came from `tail`.

**§1** states the pinning strategy in one paragraph: citations pin to commit SHAs, never branch names; every
figure in this phase's artifacts is either measured in the plan that writes it or cited to a `file:line`.
**§2** is the anchor-verification table: for each source record this phase cites — `145-BENCH-LOG.md`,
`145-08-SUMMARY.md`, `144-TEST-RECORD.md`, `143-HOST-RECORD.md`, `141-LOOP-RECORD.md`,
`140-PARAM-TABLE-RECORD.md`, `139-GH15-COMMENT.md`, `139-GH15-ORIGINAL-CRITERIA.md`, `.planning/STATE.md` —
record its `git hash-object` blob SHA so a later plan can prove it cited an unmoved document.

Finally, commit **only** the paths in this plan's `files_modified`. Use explicit `git add` with those paths;
do not use `git add -A`, and if a helper commit verb is used, pass an explicit file list. Other Wave 1 plans
are running concurrently in this same repository.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md && grep -c 'rev-list --count' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md</automated>
    <automated>cd /workspaces && for s in '## 0.' '## 1.' '## 2.'; do grep -qF "$s" .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md || { echo "MISSING $s"; exit 1; }; done; echo "sections 0-2 present"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py > /tmp/gsd146/rec_baseline.txt 2>&1; rc=$?; echo "record_gate_rc=$rc"; test "$rc" = "0"</automated>
    <automated>cd /workspaces && grep -qF '5233463320' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md && grep -qF 'lastEditedAt' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md && echo "gh15 oracles recorded"</automated>
  </verify>
  <acceptance_criteria>
    - `146-CITATIONS.md` exists and contains the literal section headings `## 0.`, `## 1.` and `## 2.`.
    - `grep -c 'rev-list --count' 146-CITATIONS.md` returns **3 or more** — one recorded invocation per repository. Negative control: the same grep over the file before this task returns 0 (the file does not exist), which is recorded in the summary as the pre-task observation.
    - §0 names all three repositories (`/workspaces`, `firestarter`, `firestarter_app`) and gives an integer ahead-count for each. A missing repository is a task failure, not a formatting choice.
    - §0 records the observed gh#15 `state`, `lastEditedAt`, comment `totalCount` and comment `databaseId`, and the literal string `5233463320` appears in the file.
    - §0 records that `gh issue view --json lastEditedAt` is not a valid field and that `updatedAt` bumps on comment creation.
    - `python3 .planning/phases/130-.../check_record_corrections.py` exits **0** and its `exempt hits by verdict` tally is quoted verbatim in §0.
    - §2 carries a `git hash-object` blob SHA for each of the nine cited source records; a row with an empty SHA cell fails the task.
    - `git status --porcelain` in `/workspaces/firestarter` returns **0 lines** — this plan touched nothing in either sub-repo (D-06).
    - The commit's changed-file list, from `git show --stat --oneline HEAD`, contains only paths under this phase directory.
  </acceptance_criteria>
  <done>`146-CITATIONS.md` §§0-2 exist, carry the three-repo push-audit baseline, the five gh#15 oracles, the 130 record-gate baseline tally and nine anchor blob SHAs, and are committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author 146-check-claims.py as a 146-scoped sibling of the Phase 139 gate</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — **read all 331 lines.** This is the donor. Note in particular the module docstring's exit-code contract and five executed probes (`:1-62`), `_HERE` (`:73`), `_DEFAULT_TARGETS` (`:78-81`), the env seam (`:83-92`), the twelve `FORBIDDEN_PATTERNS` (`:98-128`), the two `REQUIRED_CAVEAT_PATTERNS` (`:134-145`), `_assert_default_targets_are_local()` (`:148-181`), `resolve_targets()` (`:184-202`), `scan_text()` (`:205-238`), `_print_bucket()` (`:241-246`) and `main()`'s order of operations (`:249-326`).
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md` §1 — the donor-line-to-146-element map, the six mandatory edits, and the explicit list of donor lines that must NOT be copied.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RESEARCH.md` §"Code Examples" → "The per-file caveat map" — the drafted `_CAVEAT_RULES` and `_required_caveats_for()`, the only genuinely new mechanism in this gate.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py` `:299-319` — the UNARMED branch. Read it **so as not to port it**: this gate has no exit-0-on-nothing-scanned path.
  </read_first>
  <action>
Write `146-check-claims.py` next to this plan. Stdlib only — `os`, `re`, `sys`, nothing else. Reproduce the
donor's structure element for element per `146-PATTERNS.md` §1's map, applying exactly these changes and no
others:

**1. `_DEFAULT_TARGETS` — five entries, all `os.path.join(_HERE, …)`:** `146-LEDGER.md`,
`146-CORRECTIONS.md`, `146-GH15-RECONCILIATION.md`, `146-RELEASE-NOTES-fw.md`,
`146-RELEASE-NOTES-app.md`. `_HERE` is `os.path.dirname(os.path.abspath(__file__))`, copied verbatim. No
glob, no walk, no wildcard, and no sibling-directory string constant anywhere in the file.

**2. The self-check prefix literal — `146-` in BOTH places.** `_assert_default_targets_are_local()` checks two
conditions per entry and prints every offending entry rather than the first: the entry's `dirname` must equal
`_HERE`, and its basename must start with this phase's number prefix. The donor's `startswith` call **and** the
printed failure message both carry the donor's own prefix; change both. Fixing only the call leaves a silent
documentation defect the fixture suite cannot see.

**3. A fresh env-seam name: `FIRESTARTER_CLAIMSCAN_TARGETS_146`.** The donor's suffixed name is already live
in Phase 139, this same milestone; reusing it would let one test suite aim two live checkers. Read it with
`os.environ.get(...)` and **no default**, so `None` (absent, use defaults) is distinguishable from `""`
(present but empty, resolve zero targets). Update the comment above it so it justifies the 146 name rather
than the donor's.

**4. The twelve forbidden patterns — verbatim.** Transcribe all twelve `(label, compiled_regex)` tuples from
the donor's table, unchanged, all `re.IGNORECASE`. D-14 forbids loosening, narrowing or re-deriving any of
them, and forbids adding a proximity window, a context condition, an exclusion-by-heading quarantine or an
inline allow-marker. Keep `scan_text`'s docstring statement that the scan carries no window of any kind, and
keep the measured justification — but attribute it as Phase 139's measurement, not as one made here.

**5. The two required-caveat patterns — verbatim** (`ceiling-voltage` and `ceiling-narrowing`), then the one
new mechanism: `_CAVEAT_RULES`, a dict keyed by **basename** mapping each of the five targets to its required
label set. Four targets require both labels; `146-CORRECTIONS.md` maps to `frozenset()` per D-11, because a
register of factual corrections must not be failed by a rule written for a release body. Add
`_ALL_CAVEAT_LABELS` and `_required_caveats_for(path)`, which returns `_CAVEAT_RULES.get(basename,
_ALL_CAVEAT_LABELS)` — an **unknown basename gets the FULL set**, so the rule fails closed and a future
rename cannot silently disable the caveat check for a real artifact. Pass the resolved rule set into
`scan_text` as a parameter and consume it where the donor iterated the caveat patterns unconditionally; do
not touch the forbidden-pattern loop. Update the `PASS:` line's caveat-count wording, because under D-11 one
target legitimately carries no caveat requirement and the donor's "N file(s) carry both required caveats"
sentence would misreport that.

**6. `main()`'s order of operations — verbatim:** self-check first (return non-zero if it fails), then
`resolve_targets`, then the hoisted never-vacuous guard (empty target list is a hard failure with an explicit
"cannot vacuously pass with nothing scanned" message), then the fail-closed missing-target branch (any target
absent from disk is a hard failure naming the missing paths). A **partial** default set therefore lands in the
missing-target branch and is a hard failure with no extra code — which is exactly D-11's arming contract. Do
**not** add an UNARMED branch. Keep `resolve_targets`' argv → env (`is not None`) → defaults precedence and
`_print_bucket`'s 20-entry display cap.

**7. Rewrite the module docstring.** It must state: the exit-code contract; that this is a 146-scoped sibling
of the Phase 139 gate rather than a copy, naming the four mandatory renames; that per D-11 it arms
all-or-nothing on five files with per-file caveat rules; that per D-14 it carries no window and no exclusion
mechanism; and — as its own explicit non-claim — that a green run is compliance with the pattern table only
and is **not** a substitute for the blocking operator wording review in plan 146-12. Do **not** copy the
donor's non-claim #2 (`139-check-claims.py:57-61`), which states that the donor is not a build of CLOSE-01;
this script **is** that build, and copying that paragraph would assert the opposite of the phase's deliverable.
Do not copy the donor's non-claim #1 either — it names Phase 139's own requirement ids and plan.

Then demonstrate the self-check and the two vacuity guards from the command line, before any fixture exists:
run the script with no argv and no env (expect a non-zero exit and a message naming the five missing
artifacts — none of them exists yet, so this is the fail-closed branch firing correctly); run it with the env
seam set to the empty string (expect a non-zero exit and the never-vacuous message, with neither `PASS:` nor
`UNARMED:` in stdout); and run it with a single positional argument pointing at a file that does not exist
(expect the fail-closed missing-target message). Record all three observations, with the captured exit status
of the script itself, in this plan's summary — these are the first evidence that the gate cannot pass
vacuously.

Commit only `146-check-claims.py`.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "import ast,sys; ast.parse(open('146-check-claims.py',encoding='utf-8').read()); print('parses')"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py > /tmp/gsd146/gate_noartifacts.txt 2>&1; rc=$?; echo "rc=$rc"; test "$rc" != "0" && grep -q 'not found on disk' /tmp/gsd146/gate_noartifacts.txt && echo "fail-closed on absent targets"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && FIRESTARTER_CLAIMSCAN_TARGETS_146="" python3 146-check-claims.py > /tmp/gsd146/gate_empty.txt 2>&1; rc=$?; echo "rc=$rc"; test "$rc" != "0" && grep -q 'no scan targets resolved' /tmp/gsd146/gate_empty.txt && ! grep -q 'PASS:' /tmp/gsd146/gate_empty.txt && ! grep -q 'UNARMED:' /tmp/gsd146/gate_empty.txt && echo "never-vacuous holds, no UNARMED path"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "
import importlib.util, os
s=importlib.util.spec_from_file_location('g','146-check-claims.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)
here=os.path.dirname(os.path.abspath('146-check-claims.py'))
assert len(m._DEFAULT_TARGETS)==5, m._DEFAULT_TARGETS
assert len(m.FORBIDDEN_PATTERNS)==12, len(m.FORBIDDEN_PATTERNS)
assert len(m.REQUIRED_CAVEAT_PATTERNS)==2
for t in m._DEFAULT_TARGETS:
    assert os.path.dirname(t)==here, t
    assert os.path.basename(t).startswith('146-'), t
    assert os.path.basename(t) in m._CAVEAT_RULES, t
assert m._required_caveats_for('/x/unknown-basename.md')==m._ALL_CAVEAT_LABELS
assert m._required_caveats_for('/x/146-CORRECTIONS.md')==frozenset() or len(m._required_caveats_for('/x/146-CORRECTIONS.md'))==0
print('introspection OK: 5 targets, 12 patterns, 2 caveats, rules complete, unknown fails closed')
"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -cE 'glob|os\.walk|rglob' 146-check-claims.py); echo "wildcard_hits=$n"; test "$n" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_V131' 146-check-claims.py); echo "stale_seam_hits=$n"; test "$n" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -c 'UNARMED' 146-check-claims.py); echo "unarmed_hits=$n"; test "$n" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -c '139-' 146-check-claims.py); echo "donor_prefix_hits=$n"</automated>
  </verify>
  <acceptance_criteria>
    - The file parses as Python 3 and imports with no side effects beyond module-level constant construction.
    - Introspection asserts, in one command: `len(_DEFAULT_TARGETS) == 5`; every entry's `dirname` equals the script's own directory; every basename starts with this phase's number prefix; every basename has a `_CAVEAT_RULES` entry; `len(FORBIDDEN_PATTERNS) == 12`; `len(REQUIRED_CAVEAT_PATTERNS) == 2`; `_required_caveats_for()` on an unknown basename returns the full label set; `_required_caveats_for('146-CORRECTIONS.md')` returns an empty set.
    - Run with no argv and no env, the script exits **non-zero** and stdout contains `not found on disk` naming all five absent artifacts. This is the pre-authored fail-closed branch seen to fire for the **named** reason — record the stdout verbatim.
    - Run with `FIRESTARTER_CLAIMSCAN_TARGETS_146=""`, the script exits **non-zero**, stdout contains `no scan targets resolved`, and stdout contains **neither** `PASS:` **nor** `UNARMED:`. Negative control for this leg: the same invocation with the seam pointing at any readable file exits with a report rather than the never-vacuous message.
    - `grep -cE 'glob|os\.walk|rglob'` over the file returns **0**.
    - `grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_V131'` over the file returns **0** — the donor's seam name is not reused.
    - `grep -c 'UNARMED'` over the file returns **0**.
    - Any occurrence of the donor's own phase-number prefix in the file appears **only** in a citation of the donor (a `Source:` or `139-check-claims.py:NN` reference), never inside the `startswith` call and never inside the self-check's printed message. Verify by reading every hit that `grep -n '139-'` reports.
    - The module docstring names CLOSE-01, names the four mandatory renames, and carries an explicit non-claim stating that a green run does not substitute for the blocking operator wording review.
    - Each of the three demonstration runs recorded its own exit status captured immediately after the command, never after a pipe.
  </acceptance_criteria>
  <done>`146-check-claims.py` exists, resolves exactly five `146-`-prefixed targets inside its own directory, carries the twelve donor patterns unchanged with no window and no exclusion mechanism, implements D-11's per-file caveat map failing closed on an unknown basename, has no UNARMED path, and has been seen to exit non-zero for the fail-closed and never-vacuous reasons by name.</done>
</task>

</tasks>

<verification>
- `146-CITATIONS.md` §§0-2 committed, with the three-repo `rev-list --count @{u}..HEAD` baseline, the five
  gh#15 read-only oracles, the Phase 130 record-gate tally and nine anchor blob SHAs.
- `146-check-claims.py` committed, introspection-verified, and seen to exit non-zero for two distinct named
  reasons.
- `git status --porcelain` in `/workspaces/firestarter` still 0 lines; the pre-existing `firestarter_app`
  untracked set unchanged in count.
- No `CLOSE-*` checkbox or coverage-table row moved: `git diff HEAD~2 -- .planning/REQUIREMENTS.md` is empty.
</verification>

<success_criteria>
The phase has a recorded structural before-state that plan 146-13 can assert against, and a claim gate that
is armed at exactly five files, cannot pass vacuously, and cannot pass with a target skipped. Both facts are
demonstrated by captured command output, not asserted.
</success_criteria>

<output>
Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-01-SUMMARY.md` when
done. Record the three demonstration runs verbatim, the observed gh#15 values against the expected ones, and
the three-repo ahead counts as the numbers 146-13 must reproduce.
</output>
