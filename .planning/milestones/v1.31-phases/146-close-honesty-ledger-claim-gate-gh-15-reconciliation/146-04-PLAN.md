---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 04
type: execute
wave: 2
depends_on: ["146-01"]
commits_land_in: meta
reads_repos: []
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/clean_control.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/clean_control_second.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/planted_forbidden_claim.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/planted_proven_unqualified.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/planted_missing_caveat.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-04-SUMMARY.md
autonomous: true
requirements: [CLOSE-01]

must_haves:
  truths:
    - "Per D-12 the first of CLOSE-01's two proofs is a pytest suite over a fixtures directory that exercises every forbidden pattern and every per-file caveat rule; the second proof is plan 146-11's real-file plant-and-revert"
    - "Every planted fixture's label set is PROBED before its assertion is written, because a recorded plant fired one pattern and not the second one a plan had assumed"
    - "Both clean controls carry the two required caveats, or the clean-control leg fails for the wrong reason"
    - "The pre-authored armed-against-the-real-files leg is observed RED here for the NAMED missing artifacts, not for a collection error, a missing fixture or a typo — a leg seen only red or only green is not evidence"
    - "The suite drives the gate as a subprocess for every behavioural leg and imports it by path only for the introspection legs"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py"
      provides: "The fifteen-leg fixture suite for the claim gate: forbidden patterns, per-file caveat rules, fail-closed, never-vacuous, argv precedence, multi-file anti-skip, the armed-against-real-files leg, and four introspection legs"
      contains: "def test_armed_against_the_five_real_closing_artifacts"
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/clean_control.md"
      provides: "A control document that carries both required caveats and zero forbidden phrases, so a green on it means the pattern table did not fire rather than that the file was skipped"
      contains: "6.25"
  key_links:
    - from: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/test_check_permitted_claims_v130.py"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py"
      via: "leg-for-leg reproduction of the subprocess-driven shape, with the relational-rule leg replaced by a second forbidden-pattern plant and two behavioural caveat-rule legs added"
      pattern: "_run_scanner"
  prohibitions:
    - "MUST NOT copy any donor fixture BODY — all five Phase 137 fixtures contain the bare unqualified claim word this milestone's pattern table forbids, so the donor's own clean control would fail this gate"
    - "MUST NOT write an assertion against a planted fixture's label until that fixture has been probed and the observed label set recorded"
    - "MUST NOT assert against the donor suite's v1.30 bucket strings or its relational rule label — this gate never prints either"
    - "MUST NOT add the fixtures directory to the gate's default target list, and MUST NOT make it reachable by any wildcard"
    - "MUST NOT edit 146-check-claims.py to make a leg pass — if a leg fails, fix the leg or the fixture and record why; a pattern-table change is forbidden by D-14"
    - "MUST NOT treat the armed-against-real-files leg's RED as a failure of this plan: it is expected until plan 146-11, and its expected reason must be verified rather than assumed"
    - "MUST NOT tick CLOSE-01 or any other CLOSE requirement — only plan 146-13 may tick CLOSE-01 through CLOSE-05"
    - "MUST NOT run git push, git merge, git tag, gh workflow run, gh release, gh issue edit or gh issue close (D-01)"
    - "MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Author the five fixtures and the fifteen-leg pytest suite that prove the claim gate's pattern table and its
per-file caveat rule fire — D-12's first proof.

Purpose: fixtures prove the table; they do **not** prove the gate is wired to the files that ship, which is why
D-12 demands a second, different proof and why plan 146-11 exists. What this plan uniquely owns is the negative
half: every forbidden pattern, every caveat rule, the fail-closed branch, the never-vacuous branch and the argv
precedence each seen to fire on a document authored to make it fire. The donor fixtures cannot be reused —
every one of them contains the bare claim word this milestone's table forbids, so the donor's own clean control
would fail here. And every plant must be probed before its assertion is written: a recorded plant fired one
pattern and not the second a plan had assumed it would.

Output: `fixtures/` (five files), `test_check_claims_v131.py`, and `146-CITATIONS.md` §3.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-VALIDATION.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
The full phase inventory is repeated in every plan so the source-grounding pass can exclude every newly
created symbol from drift verification. **Every path and name below is new in Phase 146.**

**New scripts (meta repo, this phase directory):** `146-check-claims.py` (`_HERE`, `_DEFAULT_TARGETS`,
`FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`, `_ALL_CAVEAT_LABELS`, `_CAVEAT_RULES`,
`FIRESTARTER_CLAIMSCAN_TARGETS_146`, `_assert_default_targets_are_local()`, `_required_caveats_for()`,
`resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`); `146-check-close03-docs.py`
(`_find_repo_root()`, `_REPO_ROOT`, `_DEFAULT_TARGETS`, `_DOC_TARGET_ALLOWLIST`,
`_REQUIRED_TOPICS_BY_FILE`, `REQUIRED_TOPIC_PATTERNS`, `_required_topics_for()`,
`_assert_default_targets_are_allowlisted()`, `resolve_targets()`, `scan_text()`, `main()`,
`FIRESTARTER_DOCSCAN_TARGETS_146`); `test_check_claims_v131.py` (`_HERE`, `_SCANNER`, `_run_scanner()`,
`_import_scanner_module()` and its fifteen `test_*` legs).

**New fixtures:** `fixtures/clean_control.md`, `fixtures/clean_control_second.md`,
`fixtures/planted_forbidden_claim.md`, `fixtures/planted_proven_unqualified.md`,
`fixtures/planted_missing_caveat.md`

**New `.planning` artifacts:** `146-LEDGER.md`, `146-CORRECTIONS.md`, `146-GH15-RECONCILIATION.md`,
`146-RELEASE-NOTES-fw.md`, `146-RELEASE-NOTES-app.md`, `146-CITATIONS.md`, `146-ARM-BUILD-RECORD.md`,
`146-DOC-CHECK-RECORD.md`, `arm-build/`, and `146-0N-PLAN.md` / `146-0N-SUMMARY.md`

**New doc sections in existing files:** `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 replacement write-algorithm
text plus a pulse-override sub-item and a program-VCC-ceiling note; `firestarter/CLAUDE.md` a
`**⚠ CORRECTION (Phase 146 / CLOSE-04, origin F-144-01)**` block and a program-VCC-ceiling sentence;
`firestarter/README.md` a 27C paragraph under §Protocol Notes; `firestarter_app/README.md` four added
`write` option entries and a program-VCC-ceiling note; `⚠ CORRECTION` blocks in `.planning/ROADMAP.md`,
`.planning/PROJECT.md` and `.planning/REQUIREMENTS.md`

**New message text:** a replacement `format` string for `DBG_PULSE_DELAY_MISMATCH` in
`tools/catalog/messages.toml` (three copies) and its regenerated line in
`firestarter_app/firestarter/messages.py`

**New env-var names:** `FIRESTARTER_CLAIMSCAN_TARGETS_146`, `FIRESTARTER_DOCSCAN_TARGETS_146`

No new CLI flag is introduced — `--pulse-us` already ships from Phase 143.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author five fixtures and probe each plant's label set before any assertion exists</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/fixtures/</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/fixtures/` — all five files. Read them for the **layout** convention only: 8-17 lines each, a self-labelling HTML comment on line 1 naming the fixture as test data and warning that it must never be added to the default target list, and a second comment on planted files naming the violation. **Their bodies are unusable** — every one contains the bare claim word this milestone's pattern table forbids.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md` §4 — the two-line comment convention to reproduce, the measured-safe substitute vocabulary, and the requirement that both clean controls carry the two caveats.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RESEARCH.md` §"Measured probes" — the twenty-plus probed candidate sentences with their verdicts and labels, including the two false negatives and the hyphen behaviour. This is the source of the safe vocabulary and of the candidate plants.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py` — the twelve pattern labels and the two caveat labels as actually implemented, plus `_required_caveats_for()`'s fail-closed default, which decides what a fixture with an unrecognised basename is held to.
  </read_first>
  <action>
Create the `fixtures/` directory next to this plan with exactly five files. Every file opens with the two-line
comment convention from the donor, retargeted at this phase's gate: line 1 declares the file as test data for
`146-check-claims.py`, states it is not a closing artifact, and states it must never appear in the default
target list; planted files add line 2 naming the label the plant is meant to trip.

**`clean_control.md` and `clean_control_second.md`** must each satisfy two things at once: **zero** hits from
any of the twelve patterns, and **both** required caveats present — the program-VCC voltage figure and a
silicon-margin phrase. The caveats are mandatory because a fixture's basename is not in `_CAVEAT_RULES`, so the
gate's fail-closed default holds it to the full caveat set; a clean control without them fails for the wrong
reason and the leg proves nothing. Write them in the measured-safe vocabulary this milestone already uses —
*validated*, *bench-validated*, *established*, *demonstrated*, *measured*, *shown*, *attested*,
*skipped-with-reason* — and give them different enough content that the two-file anti-skip leg can tell them
apart by name. Make each read like a miniature closing artifact rather than lorem ipsum, so a human reading a
failure can see what the control was controlling for.

**`planted_forbidden_claim.md`** carries **one** planted overclaim drawn from this milestone's real risk shape
plus both caveats, so its only failure is the forbidden phrase. **`planted_proven_unqualified.md`** carries a
plant that trips the bare-claim-word pattern specifically — this is the pattern most likely to bite the
closing artifacts, and it replaces the donor's relational-rule leg, for which this gate has no analog. Both
plants must also carry both caveats, so that each fails for exactly one reason.

**`planted_missing_caveat.md`** carries zero forbidden phrases and is **missing at least one** required caveat,
so its only failure is the caveat rule. State in its own comment which caveat is absent.

**Probe every plant before writing a single assertion.** Import `146-check-claims.py` by file path, call
`scan_text` on each fixture's text with the full caveat set, and record the exact label list and line numbers
returned. Then — and only then — is the assertion written against the labels that were **observed**. The
recorded reason this matters: a plant of the form *confirmed working on silicon* fires the confirmed-working
label but not the works-on-silicon label, because the second pattern's verb form does not match; a plan that
asserted both would have failed for a reason unrelated to the gate. If a probe returns a label set you did not
intend, change the fixture, not the pattern table — D-14 forbids touching the table.

Also confirm by probe that **neither clean control** returns any forbidden label and that **both** return an
empty missing-caveat list under the full caveat set. Record all five probe results in this plan's summary.

Confirm `git check-ignore` does not ignore the fixtures directory, and confirm the gate cannot reach it: run
the gate with no argv and no env and check that no fixture basename appears anywhere in its output.

Commit only the five fixture files. Use explicit `git add` with those paths; other Wave 2 plans are running
concurrently in this repository.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && ls fixtures/ | sort | tr '\n' ' '; echo; test "$(ls fixtures/*.md | wc -l)" = "5" && echo "five fixtures present"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "
import importlib.util, os, io
s=importlib.util.spec_from_file_location('g','146-check-claims.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)
full=m._ALL_CAVEAT_LABELS
for name in ['clean_control.md','clean_control_second.md']:
    t=open(os.path.join('fixtures',name),encoding='utf-8').read()
    hits,missing=m.scan_text(t,name,full)
    assert not hits, (name,hits)
    assert not missing, (name,missing)
    print('clean',name,'OK')
for name in ['planted_forbidden_claim.md','planted_proven_unqualified.md']:
    t=open(os.path.join('fixtures',name),encoding='utf-8').read()
    hits,missing=m.scan_text(t,name,full)
    assert hits, (name,'expected at least one forbidden hit')
    assert not missing, (name,'plant should carry both caveats so it fails for one reason only',missing)
    print('planted',name,sorted({h[0] for h in hits}))
t=open(os.path.join('fixtures','planted_missing_caveat.md'),encoding='utf-8').read()
hits,missing=m.scan_text(t,'planted_missing_caveat.md',full)
assert not hits, ('missing-caveat fixture must carry no forbidden phrase',hits)
assert missing, 'expected at least one missing caveat label'
print('planted missing-caveat labels', sorted(missing))
"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py > /tmp/gsd146/gate_fixtures_unreachable.txt 2>&1; for f in $(ls fixtures/); do if grep -q "$f" /tmp/gsd146/gate_fixtures_unreachable.txt; then echo "LEAK: $f reachable from defaults"; exit 1; fi; done; echo "no fixture is reachable from the default target list"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && for f in fixtures/*.md; do head -1 "$f" | grep -q 'test fixture' || { echo "MISSING self-label comment in $f"; exit 1; }; done; echo "all fixtures self-labelled"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five files exist under `fixtures/`, named `clean_control.md`, `clean_control_second.md`, `planted_forbidden_claim.md`, `planted_proven_unqualified.md`, `planted_missing_caveat.md`.
    - Every fixture's first line is a self-labelling comment declaring it test data for this phase's gate and warning that it must never be added to the default target list; every planted fixture carries a second comment naming the label it is meant to trip.
    - Probed with the gate's own `scan_text` under the full caveat set: both clean controls return **zero** forbidden hits **and** zero missing caveats. A clean control missing a caveat fails this criterion — the leg would then be red for the wrong reason.
    - `planted_forbidden_claim.md` and `planted_proven_unqualified.md` each return **at least one** forbidden hit and **zero** missing caveats, so each fails for exactly one reason. The observed label list for each is recorded in the summary before any assertion is written against it.
    - `planted_missing_caveat.md` returns **zero** forbidden hits and **at least one** missing caveat label, and its comment names which caveat is absent.
    - Running the gate with no argv and no env produces output in which **no** fixture basename appears — the fixtures are unreachable from the default target list.
    - The five probe results are recorded verbatim in `146-04-SUMMARY.md`, each with the label list actually returned rather than the label intended.
    - `146-check-claims.py` is byte-unchanged by this task: `git diff --numstat -- 146-check-claims.py` produces no output.
  </acceptance_criteria>
  <done>Five self-labelled fixtures exist, each probed against the gate's own scanner, with two clean controls that carry both caveats and zero forbidden hits, two single-reason forbidden plants and one single-reason caveat plant — and none of them reachable from the gate's defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Author the fifteen-leg suite and record the armed-against-real-files leg RED for its named reason</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py, .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/test_check_permitted_claims_v130.py` — **read all 350 lines.** The docstring's anti-hollow rationale and module-rename note (`:1-56`), the header and the two invocation idioms (`:58-100`), the eleven legs and their exact assertion styles, the multi-file `os.pathsep` idiom (`:235-239`), and the two introspection legs (`:314-349`).
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md` §3 — the leg-level adaptations, the note that the scanner filename is not a valid Python identifier and why that keeps the behavioural legs on subprocess, and the two donor strings that must not be copied.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-VALIDATION.md` §"Per-Task Verification Map" — the four CLOSE-01 `-k` selectors this suite's function names must satisfy.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-claims.py` — the exact strings the gate prints on each failure path, so every assertion matches text the gate actually emits.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md` — the register as plan 146-01 left it, so §3 is appended rather than overwriting §§0-2.
  </read_first>
  <action>
Write `test_check_claims_v131.py` next to this plan. The filename is deliberately distinct: two files named
`test_check_permitted_claims.py` and one named with the previous milestone's suffix already exist in sibling
phase directories, and pytest's default import mode collides on same-named modules for anyone running pytest
from the repository root.

**Header, copied from the donor with the seam name changed.** Reproduce `_HERE`, `_SCANNER` pointing at
`146-check-claims.py`, `_run_scanner(targets=None, argv=None)` driving the script via
`subprocess.run([sys.executable, str(_SCANNER), *argv])` with `cwd=_HERE`, `capture_output=True`, `text=True`
and a mutated environment, and `_import_scanner_module()` loading the script by file path for introspection
only. Set or pop `FIRESTARTER_CLAIMSCAN_TARGETS_146` — the seam name plan 146-01 chose — never the donor's.
Fixtures are addressed as relative paths, which works because `cwd` is the phase directory.

**Fifteen legs.** Every behavioural leg (1-9, 14, 15) must go through `_run_scanner`, never through an
in-process import — the scanner's filename is not a valid Python identifier, and the donor's own docstring
records that subprocess is what makes these legs non-hollow. Legs 10-13 import by path solely to introspect
module constants.

1. Clean control via the seam → exit 0, `PASS:` in stdout.
2. The single-overclaim plant → non-zero, `FAIL:` in stdout, **and the specific observed label** from Task 1's
   probe — not merely a non-zero exit.
3. The missing-caveat plant → non-zero, and stdout names the caveat bucket the gate actually prints.
4. The bare-claim-word plant → non-zero with **its** specific observed label. This replaces the donor's
   relational-rule leg; this gate has no relational rule and none is added.
5. A target path that does not exist → non-zero, stdout names it as not found on disk.
6. The seam set to the empty string → non-zero, stdout carries the never-vacuous message, and stdout contains
   **neither** a pass line **nor** an unarmed line. Keep both negative assertions even though this gate has no
   unarmed branch: the second one pins that absence.
7. Both clean controls in one run, joined with the platform path separator → exit 0 and a single pass line
   naming **both** basenames. This is the anti-skip leg.
8. Positional argument beats the seam: point the seam at a plant and the argument at a clean control, expect
   exit 0. The `is not None` check in the gate's target resolution is what this pins.
9. **No argument and no seam** → exit 0 with a pass line naming all five real closing-artifact basenames. This
   is the literal mechanical discharge of *armed against the real files* and it **cannot pass yet** — see the
   RED protocol below.
10. Introspection: every default target's directory equals this phase directory.
11. Introspection: every default target's basename carries this phase's number prefix.
12. Introspection: every default target's basename has an entry in the caveat-rule map — a missing entry is a
    typo, not a policy.
13. Introspection: an unrecognised basename resolves to the **full** caveat set, so the rule fails closed.
14. Behavioural: a document copied into pytest's `tmp_path` under the basename the caveat rule exempts, with
    **neither** caveat present, exits 0 — the per-file exemption proven behaviourally, not only by
    introspection.
15. Behavioural: the same exempt basename **with** a forbidden phrase still exits non-zero — the exemption is
    caveat-only and is not a blanket skip. Legs 14 and 15 together are the pair that makes the exemption safe.

**Function naming is load-bearing.** The four selector expressions in `146-VALIDATION.md` must each select a
non-empty set: `-k planted` must reach legs 2, 3 and 4; `-k caveat` must reach legs 3, 12, 13, 14 and 15;
`-k "closed or vacuous or precedence"` must reach legs 5, 6 and 8; `-k default_targets` must reach legs 10, 11
and 12. Name the functions so that holds, and verify it by running each selector and reading the collected
count — a selector that collects zero tests is a silent pass in the validation map.

**Do not copy** the donor's v1.30 missing-caveat bucket string or its relational-rule label; this gate prints
neither, and an assertion against a string the gate never emits is a leg that can only ever be red.

**The RED protocol for leg 9.** Run the suite. Leg 9 must fail, and you must verify it fails **for the named
reason**: its failure output must show the gate reporting the five closing artifacts as not found on disk. A
failure that is a collection error, an import error, a missing fixture, a wrong basename or a seam-name typo is
a defect in this plan, not the expected RED — read the failure text and fix the leg until the RED is the right
RED. Record, in `146-CITATIONS.md` §3, all three of: the full suite run with its pass/fail counts, leg 9's
failure output verbatim, and an explicit statement that leg 9's GREEN is plan 146-11's to record and its
second RED comes free from that plan's plant. Also record the four selector collection counts. Then run the
suite once more with leg 9 deselected and record that every other leg passes — that is what makes leg 9's RED
attributable.

Append §3 to `146-CITATIONS.md` after §2; do not touch §§0-2.

Commit only `test_check_claims_v131.py` and `146-CITATIONS.md`.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && python3 -m pytest .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py -o addopts="" -q > /tmp/gsd146/suite_all.txt 2>&1; rc=$?; echo "suite_rc=$rc"; tail -5 /tmp/gsd146/suite_all.txt</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && python3 -m pytest .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py -o addopts="" -q --deselect .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py::test_armed_against_the_five_real_closing_artifacts > /tmp/gsd146/suite_minus9.txt 2>&1; rc=$?; echo "minus9_rc=$rc"; tail -3 /tmp/gsd146/suite_minus9.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces && for k in planted caveat "closed or vacuous or precedence" default_targets; do n=$(python3 -m pytest .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py -o addopts="" -q --collect-only -k "$k" 2>&1 | grep -c '::test_'); echo "selector [$k] collects $n"; test "$n" -ge 2 || { echo "SELECTOR COLLECTS TOO FEW"; exit 1; }; done</automated>
    <automated>cd /workspaces && n=$(grep -c '^def test_' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py); echo "legs=$n"; test "$n" = "15"</automated>
    <automated>cd /workspaces && grep -qF '## 3.' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md && for s in '## 0.' '## 1.' '## 2.'; do grep -qF "$s" .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md || { echo "LOST $s"; exit 1; }; done; echo "section 3 appended, 0-2 intact"</automated>
    <automated>cd /workspaces && n=$(grep -c 'FIRESTARTER_CLAIMSCAN_TARGETS_146' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/test_check_claims_v131.py); echo "seam_refs=$n"; test "$n" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_'` over the suite returns exactly **15**.
    - The suite run with leg 9 deselected exits **0** — every other leg passes. This is the control that makes leg 9's failure attributable to leg 9.
    - The full suite run fails, and leg 9's captured failure output shows the gate reporting the five closing artifacts as **not found on disk**. A collection error, an import error, a missing-fixture error or a seam-name error is a defect to fix, not the expected RED, and the summary must state which of the two it observed.
    - Each of the four validation-map selectors collects **at least two** tests. A selector collecting zero would make its validation row a silent pass.
    - Legs 2 and 4 assert the **specific** label recorded by Task 1's probe, not merely a non-zero exit; legs 3 and 14/15 assert against strings the gate actually prints, verified by reading the gate's source.
    - Leg 6 asserts both that no pass line appears and that no unarmed line appears.
    - Leg 7's assertion names **both** clean-control basenames in a single pass line.
    - Legs 14 and 15 both exist and use the caveat-exempt basename: 14 exits 0 without either caveat, 15 exits non-zero when a forbidden phrase is added. Either leg alone would be insufficient.
    - `146-CITATIONS.md` gains a `## 3.` section and still contains `## 0.`, `## 1.` and `## 2.`.
    - The suite references the seam name `FIRESTARTER_CLAIMSCAN_TARGETS_146` at least twice (set and pop) and references the previous milestone's seam name zero times.
    - `146-check-claims.py` is byte-unchanged: `git diff --numstat -- 146-check-claims.py` produces no output. No leg was made to pass by editing the gate.
  </acceptance_criteria>
  <done>The fifteen-leg suite exists, every leg but the armed-against-real-files leg passes, that leg fails for the verified reason that the five closing artifacts do not yet exist, all four validation selectors collect tests, and §3 of the citation register records the run with its counts.</done>
</task>

</tasks>

<verification>
- Five fixtures and `test_check_claims_v131.py` committed; `146-CITATIONS.md` §3 appended with §§0-2 intact.
- Suite minus leg 9 exits 0; full suite fails only on leg 9, for the verified named reason.
- All four validation-map selectors collect at least two tests each.
- `146-check-claims.py` byte-unchanged; no fixture reachable from the gate's defaults.
- Neither sub-repo touched; no `CLOSE-*` checkbox or coverage row moved.
</verification>

<success_criteria>
Every forbidden pattern, both caveat labels, the per-file exemption in both directions, the fail-closed branch,
the never-vacuous branch and the argv precedence have each been seen to fire on a document authored to make
them fire — and the one leg that cannot pass yet has been seen to fail for the reason it is supposed to.
</success_criteria>

<output>
Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-04-SUMMARY.md` when
done. Record the five probe label lists verbatim, both suite runs with their counts, the four selector counts,
and leg 9's failure text.
</output>