---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 07
type: execute
wave: 3
depends_on: ["146-02", "146-06"]
commits_land_in: [meta, firestarter, firestarter_app]
reads_repos: [firestarter, firestarter_app]
files_modified:
  - firestarter_app/README.md
  - tools/catalog/messages.toml
  - firestarter/tools/catalog/messages.toml
  - firestarter_app/tools/catalog/messages.toml
  - firestarter/include/messages.h
  - firestarter_app/firestarter/messages.py
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-07-SUMMARY.md
autonomous: true
requirements: [CLOSE-03]
must_haves:
  truths:
    - "Per D-06 the debug-message wording is corrected through the catalog and regenerated — never by hand-editing either generated artifact — the parameter list is untouched so the change stays wording-only, and the orphaned message id is recorded rather than removed."
    - "The regen's effect is asserted from numstat output CAPTURED BEFORE ANY COMMIT: after committing, a working-tree diff is empty for both the changed and the unchanged file, so a post-commit numstat proves nothing. messages.h is identifier-only and takes a ZERO-line diff; messages.py takes exactly one changed line."
    - "Per CLOSE-03 the host README carries the complete shipped write-option surface, the database-supplied pulse, the pulse-override flag and the ~6.25 V accepted debt, and the two pre-existing defects in the exact lines the new entry inserts into are corrected as in-scope-by-adjacency."
    - "Per D-13 the documentation checker goes GREEN here — the same script seen RED in 146-02 — and the GREEN is recorded in a NEW §8, because §§5 and 6 already exist on disk and §6 itself routes this plan's GREEN to §8."
  artifacts:
    - path: "firestarter_app/README.md"
      provides: "The complete shipped write-option surface including the pulse override, the corrected blank-check name and behaviour, the separate skip-erase option with its warning carried across, the database-supplied pulse in user terms, and the program-VCC ceiling"
      contains: "--pulse-us"
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md"
      provides: "Section 8: the documentation checker GREEN across all four targets with its failure capability re-shown in the same session, the three-row regen diff shape, and both sub-repo suite counts"
      contains: "## 8."
  key_links:
    - from: "tools/catalog/messages.toml"
      to: "firestarter_app/firestarter/messages.py"
      via: "the sync-and-regenerate script, whose effect is proven by a pre-commit per-repository numstat rather than by the script's own self-comparing confirmation lines"
      pattern: "DBG_PULSE_DELAY_MISMATCH"
  prohibitions:
    - "MUST NOT hand-edit firestarter/include/messages.h or firestarter_app/firestarter/messages.py — both are codegen output and the host module is format-stable, so hand-normalising it is the defect this constraint prevents; MUST NOT change the message's id, name or parameter list; MUST NOT remove, renumber or reassign the orphaned message id."
    - "MUST NOT cite the sync script's regeneration confirmation lines as evidence (they are gated on a self-comparison) and MUST NOT treat the catalog validator as a drift check (it returns before any target comparison); MUST NOT touch the two scratch-worktree catalog copies — five copies exist on disk, three are in scope."
    - "MUST NOT edit 146-check-close03-docs.py or any §§1-7 of 146-DOC-CHECK-RECORD.md (if the checker fails, the documentation changes); MUST NOT run either sub-repo suite before committing (both assert the firmware repo's whole-repo porcelain); MUST NOT edit any chip database file or support-status value; MUST NOT tick any CLOSE requirement (146-13 owns all five); MUST NOT push, merge, tag or dispatch a workflow (D-01)."
---
<objective>
Complete CLOSE-03's host half, correct the shipped debug-message wording through the codegen path (D-06), and take the documentation checker from the RED plan `146-02` recorded to GREEN (D-13).

The host repository has zero documentation commits this milestone and the flag a user meets first appears nowhere in its README — a gap routed here verbatim by the host phase. Two further defects sit in the exact lines the new entry inserts into: the blank-check option is documented under a long name the shipped surface does not use, and its described behaviour claims it also skips erase, which a separate option now owns and which carries its own warning. On the firmware side one shipped debug message describes an adaptive loop that no longer exists; its **wording** is corrected through the catalog, because the generated header is identifier-only and a wording change there is a zero-line diff. Output: an edited host README, one catalog edit propagated to three copies, two regenerated artifacts with a pre-commit-captured diff shape, three commits, and `146-DOC-CHECK-RECORD.md` §8.
</objective>
<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md
@.planning/REQUIREMENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Complete the host README's write surface, correct its two adjacent defects, add the pulse and the ceiling</name>
  <files>firestarter_app/README.md</files>
  <read_first>
    - `firestarter_app/README.md:306-330` — the write section: an options list of three entries and a four-step description. Both the list and description steps one and two carry the two defects. Read the surrounding read and blank sections so the added entries match the file's own formatting. Then `:528-575` — the EEPROM-configuration section, which already documents the database pulse field, the user override path and a JSON example carrying that field: the style donor for the database-supplied-pulse topic and the place to extend rather than duplicate.
    - `firestarter_app/firestarter/cli_handlers.py:546-610` — the shipped `write` decorator block: seven options, the blank-check option's real long name, the separate skip-erase option with its warning text, and the pulse-override option's help text carrying the provenance narrowing verbatim. This is the authority for every name and description; take them from here, never from the README. Then `firestarter_app/firestarter/eprom_operations.py` around `:1860-1900` — how the override reaches the wire: it replaces the value the host already sends and introduces no new wire field.
    - `.planning/REQUIREMENTS.md` §"Evidence ceiling" for the user-facing ceiling wording, and `146-CORRECTIONS.md`'s two adjacency rows (written by `146-05`) so this edit lands exactly the corrected text the register already promises.
  </read_first>
  <action>
Bring the write section's options list to the shipped surface, taking every name and description from the decorator block. **Correct the blank-check entry:** replace the README's long name with the shipped short-and-long pair and correct its description — it skips the blank check **only**; it no longer skips erase. **Add the skip-erase entry** as a separate option and carry its warning across: skipping erase on a non-blank electrically-erasable chip leaves un-erased bits that cannot be reprogrammed. That is safety-relevant text and must not be paraphrased into something milder. **Add the pulse-override entry** with what it does, its bound, the provenance narrowing the shipped help text carries — the bound is parity with another programmer's own integer width and is **not** a wire-type or hardware limit — and the fact that it overrides the value the host already sends, introducing no new wire field. **Add the remaining two shipped options** (the VPE-as-VPP override and the SDP-unlock skip) with one-line descriptions from their help text. **Correct the description steps:** step one names the shipped blank-check option; step two stops attributing erase-skipping to it and names the separate skip-erase option instead.

Then add two short pieces of user-facing context. *The database-supplied pulse, in a user's terms:* extend the EEPROM-configuration section's existing treatment rather than duplicating it — the firmware applies this width per byte, the value comes from the chip database, a zero value means the protocol's own fallback, and the override flag changes it for one run only without editing the database; point at the user override path the section already documents. *The program-VCC ceiling:* one short paragraph in the requirements document's own terms — the raised program-VCC the vendor algorithms assume for threshold margin is unreachable on this shield, which has no VCC-raise path, so this is timing, pulse-count and verify fidelity and **not** silicon-margin fidelity, hardware-bound and recorded rather than attempted. Use the voltage figure in a grep-findable form and put it where a user reading about writing will meet it.

Write in the phase's vocabulary and avoid the unqualified claim forms the checker's pattern table catches; this file was measured clean of them, so keep it clean. No table, no datasheet citations, no firmware internals beyond what a CLI user needs. Then record the host porcelain line count, commit **inside** `firestarter_app` after confirming the milestone branch, staging exactly the README, and commit with a message naming CLOSE-03 and the two adjacency corrections. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && for opt in '--no-blank-check' '--skip-erase' '--vpe-as-vpp' '--skip-sdp-unlock' '--pulse-us'; do grep -q -- "$opt" firestarter_app/README.md || { echo "MISSING $opt"; exit 1; }; done; n=$(grep -c -- '--ignore-blank-check' firestarter_app/README.md); echo "shipped_surface=1 stale_long_name=$n"; test "$n" = "0"</automated>
    <automated>cd /workspaces && c=$(grep -c '6\.25' firestarter_app/README.md); w=$(grep -oiE '\bpro[v]en\b' firestarter_app/README.md | wc -l); echo "ceiling=$c claim_word=$w"; test "$c" -ge 1 && test "$w" = "0"</automated>
    <automated>cd /workspaces && git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD; f=$(git -C /workspaces/firestarter_app show --name-only --format= HEAD | grep -c .); s=$(git -C /workspaces/firestarter_app diff --numstat -- firestarter tools tests pyproject.toml | wc -l); echo "files_in_commit=$f host_source_changed=$s"; git -C /workspaces/firestarter_app show --name-only --format= HEAD; test "$f" = "1" && test "$s" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - All five option spellings are present — the correct blank-check long name, the skip-erase option, the VPE-as-VPP override, the SDP-unlock skip and the pulse override — and the stale blank-check long name prints **0**, down from the pre-edit count recorded in the summary.
    - The pulse-override entry states its bound, that the bound is parity with another programmer's integer width rather than a wire-type or hardware limit, and that it introduces no new wire field. The skip-erase entry carries the shipped warning about un-erased bits verbatim in substance, verified against `cli_handlers.py`, not a milder paraphrase. Description steps one and two name the shipped spellings, and step two no longer attributes erase-skipping to the blank-check option.
    - The ceiling figure appears at least once (up from a recorded **0**) with both halves stated, and the added pulse text states that the firmware applies the width per byte, that a zero value means the protocol fallback, and that the override is per run. The claim-word occurrence count is **0**.
    - The commit landed **inside** `firestarter_app` on the milestone branch, its file list is exactly the README, and the source/tool/test/packaging numstat prints no output. The host porcelain line count is recorded before and after and is unchanged — the pre-existing untracked host paths are still untracked.
  </acceptance_criteria>
  <done>The host README documents the complete shipped write surface with both adjacency defects corrected, carries the database-supplied pulse and the program-VCC ceiling, and is committed inside the host repository on the milestone branch with no source file touched.</done>
</task>
<task type="auto">
  <name>Task 2: Correct the debug-message wording in the canonical catalog, regenerate, capture the diff shape before committing</name>
  <files>tools/catalog/messages.toml, firestarter/tools/catalog/messages.toml, firestarter_app/tools/catalog/messages.toml, firestarter/include/messages.h, firestarter_app/firestarter/messages.py</files>
  <read_first>
    - `tools/catalog/messages.toml` around `:918-926` — the debug-message stanza to edit: its id, its name, its current format string describing an increasing pulse delay, and its two-parameter list. Also the info-message stanza around `:161-167` — the orphaned id that is recorded and **not** removed.
    - `tools/catalog/sync_to_subrepos.sh` in full — it copies the canonical catalog and the generator into both sub-repos, diffs each copy, asserts the two sub-repo catalogs are byte-identical, then runs the two emitters. Its two regeneration confirmation lines are gated on comparing a file against itself and prove nothing. Then `tools/catalog/codegen.py` around `:700-715` — the validator's early return, which is why there is no drift-detection mode and its success line is not evidence a target was regenerated.
    - `firestarter/include/messages.h` around `:48-54` and `:134-140` — the generated identifier definitions; identifier-only, which is why a wording change is a zero-line diff here. Then `firestarter_app/firestarter/messages.py` around `:60-70`, `:248-256`, `:838-846` and `:1066-1076` — the generated constants and entries including the single format line this change moves; the emitter's output is format-stable and must never be hand-normalised.
    - `.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md` §6 — both ids are unreferenced by firmware, the decision is to leave both assigned and unedited, and this message's wording actively contradicts shipped behaviour because the shipped loop never increases the pulse delay. Then `146-CORRECTIONS.md`'s register row for this correction, so the replacement text matches what the register states.
  </read_first>
  <action>
**Edit exactly one file:** the canonical catalog at the meta repository root. Replace the debug message's `format` string with wording that describes reality rather than the deleted adaptive loop — the id is held in reserve and has no firmware call site today; the shipped loop applies a fixed-width pulse per byte and never increases the pulse delay, so the previous wording described an algorithm the firmware no longer runs. Keep the id and name unchanged and keep the parameter list **exactly** as it is: changing it would make this a wire-format change rather than a wording change. Do not hand-edit the two sub-repo copies — the sync script propagates the canonical one, and a plan that edited only a sub-repo copy would have it overwritten by the next sync. Leave the orphaned info-message stanza byte-unchanged: recorded as orphaned, not removed, because deleting an id risks a later reuse collision.

**Propagate and regenerate** with one command: run the sync script from the meta repository root. Ignore its two regeneration confirmation lines entirely and do not run the catalog validator as though it were a drift check.

**Capture the diff shape BEFORE any commit — this ordering is load-bearing.** After a commit, `git diff` is empty for both the changed and the unchanged file, so a post-commit numstat cannot distinguish a successful regen from a missed one. Immediately after the regen, and before staging anything, write to `/tmp/gsd146/`: the canonical catalog's `git diff -U0` output; the canonical catalog's numstat; `git -C /workspaces/firestarter diff --numstat -- include/messages.h`; and `git -C /workspaces/firestarter_app diff --numstat -- firestarter/messages.py`. Expect the canonical catalog at exactly one changed line whose only altered content is the format line, both sub-repo catalog copies at one changed line each, all three copies at a single distinct sha256 digest, the generated firmware header at a **zero-line** diff, and the generated host module at exactly **one** changed line — read the hunk to confirm it is this message's format line. A non-zero header diff is a **stop-and-report**: it would mean an identifier moved, which is not a wording change.

**Then commit in three places, confirming the milestone branch in each first.** Commit each sub-repo's catalog copy plus any regenerated artifact **inside** its own repository — if the header diff is genuinely zero, the firmware commit carries only its catalog copy, which is the expected shape and should be stated in the message so a catalog-only commit does not surprise a reader. Commit the canonical catalog in meta. Push nothing. In the summary, cite the captured numstats and the digest comparison as the evidence, never the sync script's confirmation lines, and note that the host module's one changed line is the proof the emitter ran.
  </action>
  <verify>
    <automated>cd /workspaces && d=$(sha256sum tools/catalog/messages.toml firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml | awk '{print $1}' | sort -u | wc -l); s=$(grep -c 'Mismatch, retrying with increased pulse delay' tools/catalog/messages.toml); o=$(grep -c 'MSG_INFO_RETRIES' tools/catalog/messages.toml); echo "distinct_digests=$d stale_format=$s orphan_id=$o"; test "$d" = "1" && test "$s" = "0" && test "$o" -ge 1</automated>
    <automated>cd /workspaces && cd /workspaces && mkdir -p /tmp/gsd146 && test -s /tmp/gsd146/toml_diff.txt && test -s /tmp/gsd146/app_numstat.txt && h=$(wc -l < /tmp/gsd146/hdr_numstat.txt); echo "header_numstat_lines=$h app_numstat=$(cat /tmp/gsd146/app_numstat.txt)"; test "$h" = "0" && awk '{exit !($1==1 && $2==1)}' /tmp/gsd146/app_numstat.txt && python3 -c "import re;L=[l for l in open('/tmp/gsd146/toml_diff.txt',encoding='utf-8') if re.match(r'^[+-][^+-]',l)];print('toml_changed_lines='+str(len(L)));assert len(L)==2,L;assert all('format' in l for l in L),L;print('only_format_line_moved=1')"</automated>
    <automated>cd /workspaces && for r in /workspaces /workspaces/firestarter /workspaces/firestarter_app; do echo "$r branch=$(git -C $r rev-parse --abbrev-ref HEAD) ahead=$(git -C $r rev-list --count @{u}..HEAD)"; done; m=$(git -C /workspaces rev-list --count @{u}..HEAD); f=$(git -C /workspaces/firestarter rev-list --count @{u}..HEAD); a=$(git -C /workspaces/firestarter_app rev-list --count @{u}..HEAD); test "$m" -ge 233 && test "$f" -ge 61 && test "$a" -ge 16</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one catalog file was hand-edited (the canonical one); the two sub-repo copies were written by the sync script; all three yield **one** distinct sha256 digest; the stale format string prints **0**; the orphaned info-message id is still present and appears in no diff hunk.
    - The canonical catalog's captured `git diff -U0` shows exactly **two** changed lines (one removed, one added) and both are the format line — which is simultaneously the proof that the id, the name and the parameter list are unchanged, since any of those moving would add changed lines.
    - The captured header numstat is **zero lines** and the captured host-module numstat is exactly **one** changed line, both captured **before** any commit. Reading the host hunk confirms it is this message's format line. A non-zero header diff was treated as stop-and-report.
    - Three commits exist, one per repository, each on the milestone branch, and every ahead-count is at or above its `146-CITATIONS.md` §0.1 baseline (meta 233, `firestarter` 61, `firestarter_app` 16) — per §0.1 the counts may rise as this phase commits and may never fall, a fall being the push signal. The sync script's confirmation lines are cited nowhere as evidence, and no file outside this task's file list changed in any repository.
  </acceptance_criteria>
  <done>The debug message describes the loop that ships, the change propagated to all three catalog copies at one digest, the pre-commit capture shows a zero-line header diff and a one-line host-module diff at the format line, and three commits landed on the milestone branch with no push.</done>
</task>
<task type="auto">
  <name>Task 3: Take the documentation checker GREEN, re-show its failure capability, run both suites, record §8</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md</files>
  <read_first>
    - `146-DOC-CHECK-RECORD.md` in full as plans `146-02` and `146-06` left it: §1's pre-edit RED matrix, §2's locator REDs, §3's non-vacuity legs, §4's out-of-target findings, §5's working-tree readings, §6's routing (which assigns this plan **§8**) and §7's firmware-half flips. Sections are appended, never edited — and note that §§5 and 6 already exist, so a `## 6.` locator would pass vacuously.
    - `146-check-close03-docs.py` as `146-02` wrote it, including its env seam and `_REQUIRED_TOPICS_BY_FILE`. It must not be edited to make it pass; if it fails, the documentation changes.
    - `firestarter/tests/test_flash_path_record_sync.py` around `:1240-1255` and `firestarter_app/tests/test_py32_flash_map_host.py` around `:385-395` — the two assertions that make commit-before-run mechanical: both check the **firmware** repository's whole-repo porcelain, the second from the host suite. And `146-CITATIONS.md` §0.3 for the three-repo porcelain baselines; note §0 records **no** sub-repo suite baseline, so the firmware baseline is `146-06`'s recorded count and the host suite's count is recorded here for the first time.
  </read_first>
  <action>
**Run the checker with no argument and no environment override**, capturing its exit status immediately. It must exit 0 with a pass line naming all four scanned documentation files. If it fails, read which (file, topic) pair is unsatisfied and fix the **documentation** — editing the checker here would destroy the RED-to-GREEN pair that is the whole point.

**Then re-show that the GREEN can fail, in this session** rather than only in `146-02`'s record: run the checker with its environment override set to the empty string and confirm a non-zero exit with no pass line, and run it with the override naming one real target plus one path that does not exist and confirm a non-zero exit naming the missing path. A GREEN that has not been re-shown capable of failing is the false-GREEN shape this project has caught three times.

**Then confirm both sub-repo porcelains are at their recorded baselines and the firmware repository's is 0 lines, and only then run both suites.** Use `python3 -m pytest tests -o addopts="" -q` in each: the host repository's own configuration already passes a quiet flag and doubling it suppresses the count line that is the evidence, which the empty override prevents. The decisive oracle in each is **exit 0**. Compare the firmware count against the baseline `146-06` recorded; record the host count as the host baseline, since `146-CITATIONS.md` §0 carries no host suite figure to compare against. Record each count, each snapshot count where reported, and each wall time. A failure is a stop-and-report naming the failing test — a documentation and catalog-wording change should move neither.

**Then append §8** — not §6, which already exists — carrying: the checker's GREEN pass line verbatim with its captured exit status; the same per-file per-topic matrix as §1 but fully satisfied, so a reader can diff the two matrices; both re-shown non-vacuity legs; Task 2's pre-commit regen diff shape as a three-row table, one row per repository, with each numstat quoted; both suite counts with their comparison basis stated; and one closing sentence that the checker's GREEN means the five topics are **present**, not that the prose is correct — that judgement is the blocking operator wording review in plan `146-12`. Do not edit §§1-7. Commit `146-DOC-CHECK-RECORD.md` in the meta repository.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-close03-docs.py > /tmp/gsd146/doc_green.txt 2>&1; rc=$?; echo "doc_checker_rc=$rc"; cat /tmp/gsd146/doc_green.txt; s=$(grep -c '^## 8\.' 146-DOC-CHECK-RECORD.md); echo "record_section8=$s"; test "$rc" = "0" && test "$s" = "1"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && FIRESTARTER_DOCSCAN_TARGETS_146="" python3 146-check-close03-docs.py > /tmp/gsd146/doc_vac.txt 2>&1; rc=$?; p=$(grep -c 'PASS:' /tmp/gsd146/doc_vac.txt); echo "emptied_seam_rc=$rc pass_lines=$p"; test "$rc" != "0" && test "$p" = "0"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/firestarter && python3 -m pytest tests -o addopts="" -q > /tmp/gsd146/fw_suite_07.txt 2>&1; fw=$?; cd /workspaces/firestarter_app && python3 -m pytest tests -o addopts="" -q > /tmp/gsd146/app_suite_07.txt 2>&1; ap=$?; echo "fw_rc=$fw app_rc=$ap"; tail -2 /tmp/gsd146/fw_suite_07.txt; tail -2 /tmp/gsd146/app_suite_07.txt; test "$fw" = "0" && test "$ap" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The checker exits **0** with no argument and no environment override, and its pass line names all four scanned targets by path. Reachable at this wave because `146-06` landed the three firmware documents (including the claim-word clearance) and Task 1 landed the host document — the checker's four targets are all satisfied before it runs.
    - `146-check-close03-docs.py` is byte-unchanged by this plan: its numstat produces no output. No topic was satisfied by weakening the checker.
    - Both non-vacuity legs were re-run **in this task** and both exit non-zero: the emptied override with no pass line, and an override naming a nonexistent path with that path named in the output. Both sub-repo porcelains were at their recorded baselines before either suite ran, with the firmware repository's at exactly 0 lines, and both suites exit **0** with their count lines visible (which the empty-override invocation makes possible). The firmware count is compared against `146-06`'s recorded baseline; the host count is recorded as the host baseline rather than compared against a §0 figure that does not exist.
    - `146-DOC-CHECK-RECORD.md` gains exactly one `## 8.` section carrying the GREEN pass line with its exit status, a fully satisfied matrix comparable to §1's, both non-vacuity legs, the three-row pre-commit regen diff table, both suite counts with their comparison basis, and the presence-is-not-correctness sentence. §§1-7 appear in no diff hunk.
  </acceptance_criteria>
  <done>The checker is GREEN across all four targets with its pass line naming each and its ability to fail re-shown in the same session, both sub-repo suites pass after a clean commit, and §8 records all of it alongside the pre-commit regen diff shape.</done>
</task>
</tasks>
<verification>Host README completed and committed inside `firestarter_app`; three catalog copies at one digest; pre-commit capture showing a zero-line generated-header diff and a one-line generated-host-module diff at the format line; documentation checker GREEN over four targets with its failure capability re-shown; both sub-repo suites exit 0 after committing; four commits across three repositories, all on the milestone branch, none pushed; no `CLOSE-*` checkbox, coverage row, chip database or support-status value touched.</verification>
<success_criteria>CLOSE-03's five topics are present in the four documents a firmware developer and a CLI user actually read, the shipped debug message no longer describes a deleted algorithm, the regeneration is proven by pre-commit per-repository diffs rather than by a script's self-comparison, and the checker that was RED before the edits is GREEN after and has been re-shown capable of failing.</success_criteria>
<output>Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-07-SUMMARY.md`. Record the checker's GREEN pass line, the three-row pre-commit regen diff table, both suite counts and wall times, the four commit SHAs, and each repository's ahead-count before and after.</output>
