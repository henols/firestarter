---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 09
type: execute
wave: 4
depends_on: ["146-08"]
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-GH15-RECONCILIATION.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-09-SUMMARY.md
autonomous: true
requirements: [CLOSE-04]
must_haves:
  truths:
    - "Per D-08 the nine acceptance boxes are graded AS FILED with every Phase 139 correction named inline, and per CLOSE-04 each carries exactly one of three literal dispositions and no fourth token"
    - "Per D-09 the artifact carries the nine dispositions, the public datasheet-justification correction and ONE bench-boundary paragraph, and stops there"
    - "Per OD-A box 9 is graded on the ARM build 146-03 actually observed, and per D-14 the delta-not-CI-parity caveat travels with that grade while no forbidden literal is ever reproduced"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-GH15-RECONCILIATION.md"
      provides: "Nine original boxes reproduced verbatim, each graded with one literal disposition plus its named correction or reason; the public datasheet correction; one bench-boundary paragraph"
      contains: "not-reachable-on-this-hardware"
  key_links:
    - from: ".planning/phases/139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-GH15-RECONCILIATION.md"
      via: "the nine boxes as filed, already verified byte-identical to the live issue body's tail, so nothing is re-scraped"
      pattern: "Acceptance criteria"
  prohibitions:
    - "MUST NOT grade the amended criteria set (D-08), and MUST NOT grade the high-voltage-disable box or the all-targets-build box as a bare met"
    - "MUST NOT assert any outcome for the gate's all-five default run, and MUST NOT write to 146-CITATIONS.md — 146-10 executes in this same wave and 146-11 owns both that run and the register"
    - "MUST NOT post, comment, edit, close or label anything on gh#15 — plan 146-12 owns the phase's only outward act (D-01, D-07)"
    - "MUST NOT tick any CLOSE requirement (only 146-13 may) and MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Write `146-GH15-RECONCILIATION.md`: gh#15's nine acceptance boxes as originally filed, each graded with one of
CLOSE-04's three dispositions, plus the public correction to our own earlier comment and one bench-boundary
paragraph. CLOSE-04's vocabulary — *met-as-corrected, naming the correction* — only parses against the text as
filed, which is why D-08 grades the original nine. The decisive reason this gets posted is that one factual error
is already public **in our own comment**: it attributes the energy-cap value to a total programming time the vendor
datasheet gives as 100 seconds, when the value is the per-location typical width.

Output: `146-GH15-RECONCILIATION.md`, frozen and green under the claim gate run against it alone. Plan 146-12 posts
it behind a blocking gate; this plan posts nothing.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-LEDGER.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-ARM-BUILD-RECORD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the reconciliation — nine graded boxes, the public correction, one bench paragraph</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-GH15-RECONCILIATION.md</files>
  <read_first>
    - `139-gh-15-correction-outward/139-GH15-ORIGINAL-CRITERIA.md` — the nine boxes as filed, already verified byte-identical to the live issue body's tail and measured clean, so reproducible verbatim. D-08's grading subject. And `139-GH15-COMMENT.md` alongside it — its disposition table, and the sentence attributing the energy-cap value to a classic total programming time; also measured clean, so quotable verbatim.
    - `.planning/REQUIREMENTS.md:263-264` — CLOSE-04's three disposition tokens in their exact spelling.
    - `firestarter/src/proms/eprom.cpp`, `firestarter/src/proms/eprom_params.cpp`, `firestarter/include/eprom_params.h`, `firestarter/CLAUDE.md` §Algorithm Handlers — grade boxes 1, 2, 3, 5, 6 and 7 against shipped source, never against a record's summary.
    - `146-ARM-BUILD-RECORD.md` (this directory) — the observed ARM result and its mandatory scoping caveat, for box 9 under OD-A; and `144-tests-build-verification/144-TEST-RECORD.md` for box 8's counts and the register-stream boundary on one coverage claim.
    - `140-parameter-table/140-PARAM-TABLE-RECORD.md:259` — the F-140-07 fact: 100 seconds total for all bits, and 50 ms as `t_w(PR)` TYP (45/50/55). Also `145-bench-validation/145-BENCH-LOG.md` §"Boundaries — stated, not implied": boundaries 1 and 3 are quotable, boundary 2 carries a forbidden literal and must be cited by line range and paraphrased only.
  </read_first>
  <action>
Create the artifact in one pass. Open with a short orientation paragraph: this grades the criteria **as filed**, because met-as-corrected only parses against the original text, and a reader who never saw the earlier comment gets the whole story here.

**Reproduce the nine boxes verbatim** from the extracted-criteria file, numbered, before any grading appears — no paraphrase, no renumber, no merge. State that they are byte-identical to the issue body's tail and cite the file that established it.

**Then one disposition table**, nine data rows, three columns. Column one is the box number as a bare integer; column two is **exactly one** of CLOSE-04's three literals (bold permitted, nothing else — no hedging adverb, no parenthetical, no fourth token); column three carries the correction or the reason. Grade from the evidence you read, not from this plan:

- The four boxes whose premise the earlier comment replaced or corrected — the separate-handlers premise and the three pulse-constant premises — are *met-as-corrected*, quoting that comment's own correction wording in column three and stating both what the issue asked for and what shipped instead, with the reason.
- Boxes that shipped unchanged are *met*, column three stating what unchanged means concretely with a source citation. The test-coverage box keeps its two narrowings — dispatch now meaning table-row selection, and one coverage claim bounded to an emitted register stream — without weakening the disposition.
- The high-voltage-disable box is *met-as-corrected* with two narrowings named: every **error** exit disables every route through a single-exit wrapper, per block; the **operation**-level disable is a **source contract** rather than a behavioural result, because the file that owns it compiles in no native environment; and a **successful** block deliberately stays energised so the once-per-block settle is not re-paid.
- The all-targets-build box is *met-as-corrected*, graded on OD-A's observed ARM build: the three measured AVR targets with their admitted flash-band exemption, then the ARM target's observed state, and — in the same breath — that the local toolchain needed packages the CI install does not carry, so this is a **delta against a target never before compiled against this milestone's code, not CI parity**. State once that neither repository's CI has run any of this milestone's code.

**Then the public correction**, a short labelled section. Quote our own earlier sentence verbatim, then state the fact: the vendor datasheet gives total programming time for all bits as 100 seconds, and the 50 ms figure is the per-location typical pulse width `t_w(PR)` with its min/typ/max — so the **value** has a real primary-datasheet basis and the published **reason** did not. Cite `140-PARAM-TABLE-RECORD.md:259` by `file:line`. Add the nuance the firmware protocol reference already carries — the vendor specifies a single pulse per location, permits verification immediately after, and specifies neither a final full-array pass nor an over-program, while the firmware ships a looped pulse-verify under an accumulated cap — and name the box whose grading that qualifies. State that the firmware documentation's own copy was already corrected in place by Phase 140, cited by `file:line`, so this section corrects the **public** copy.

**Then one bench-boundary paragraph**, five facts and no more: (1) the required protocol validated on one part with its chip identifier, one controller, one shield revision read off the silkscreen because the stored revision byte cannot distinguish the candidates; (2) the other two protocols **skipped-with-reason**, each naming the missing part and each explicitly not inferred from the validated result; (3) the ceiling restated — the raised program-VCC the vendor algorithms assume for threshold margin is unreachable on this shield, so this is timing, pulse-count and verify fidelity and **not** silicon-margin fidelity; this one sentence satisfies both of the gate's caveat labels for this file, so it must carry the voltage figure and a silicon-margin phrase; (4) **no comparative claim** — fidelity not improvement, no control run by design, the historical pre-milestone write figure a recorded historical number rather than a control, with the forbidding boundary cited by line; (5) one **paraphrased** sentence on datasheet conformance, no claim in either direction, cited to the bench record's boundary by line range. Do not reproduce that boundary's sentence — quoting it in order to disclaim it is the self-reference trap that has fired five times this phase.

Close with a one-line pointer naming the ledger and the correction register by path. Per D-09 nothing else: no what-changed section, no install line, no asset list, no call to action. Do not commit yet.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -cE '^\| *[0-9]+ *\|' 146-GH15-RECONCILIATION.md || true); v=$(grep -oE '^\| *[0-9]+ *\| *\*{0,2}(not-reachable-on-this-hardware|met-as-corrected|met)\*{0,2} *\|' 146-GH15-RECONCILIATION.md | wc -l); echo "numbered_rows=$n valid_disposition_rows=$v"; test "$n" = "9" && test "$v" = "9"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && miss=0; for tok in '100 seconds' 't_w(PR)' '6.25' 'W27C512' 'leonardo' 'source contract'; do grep -qF "$tok" 146-GH15-RECONCILIATION.md || { echo "MISSING $tok"; miss=1; }; done; n=$(grep -ciE 'silicon[-[:space:]]margin' 146-GH15-RECONCILIATION.md || true); echo "narrowing_caveat=$n missing=$miss"; test "$miss" = "0" && test "$n" -ge 1</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && a=$(grep -oiE '\bproven\b' 146-GH15-RECONCILIATION.md | wc -l); b=$(grep -oiE 'datasheet[-[:space:]](conformant|correct)|algorithm[-[:space:]]accurate' 146-GH15-RECONCILIATION.md | wc -l); c=$(grep -ciE 'pip install|## The ask|Feedback wanted' 146-GH15-RECONCILIATION.md || true); echo "claim_word=$a headline_literals=$b release_apparatus=$c"; test "$a" = "0" && test "$b" = "0" && test "$c" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - All nine boxes are reproduced in filed order and wording before any grading appears — verified by diffing the reproduced list against `139-GH15-ORIGINAL-CRITERIA.md`'s box lines, where the only permitted differences are list-marker style and surrounding prose — and the first leg prints `numbered_rows=9` and `valid_disposition_rows=9`, so each of the nine rows carries exactly one of CLOSE-04's three literals and no other token.
    - Four boxes are *met-as-corrected* quoting the earlier comment's own correction wording, each stating what was asked and what shipped with the reason; the test-coverage box carries its two narrowings without weakening its disposition.
    - The high-voltage-disable box is not a bare *met*: column three names the per-block error-exit disable, the operation-level disable as a source contract with the reason it is not behavioural, and the deliberate energised state of a successful block.
    - The all-targets-build box is not a bare *met*: it names the three AVR targets with their admitted exemption, the ARM target's observed state from `146-ARM-BUILD-RECORD.md`, the delta-not-CI-parity caveat in the same breath, and states once that neither repository's CI has run this milestone's code.
    - The public-correction section quotes our own earlier sentence verbatim and carries the 100-second total, `t_w(PR)` identified as the per-location typical width, and the value-real / reason-wrong distinction; both `140-PARAM-TABLE-RECORD.md:259` and the already-landed in-place firmware-doc correction are cited by `file:line`.
    - The bench paragraph carries all five facts, and its ceiling sentence carries both the voltage figure and a silicon-margin phrase — which is what satisfies this file's caveat rule. The conformance sentence is a paraphrase with a line-range citation.
    - The third leg prints `claim_word=0`, `headline_literals=0` and `release_apparatus=0`: no forbidden literal was reproduced, the bench record's boundary sentence was not quoted, and no release-note apparatus leaked in.
  </acceptance_criteria>
  <done>The nine original boxes are reproduced verbatim and graded with exactly one of three literal dispositions each, neither the high-voltage-disable nor the all-targets-build box is a bare met, the public datasheet correction carries the fact and both citations, and the bench paragraph carries five facts and satisfies the caveat rule.</done>
</task>

<task type="auto">
  <name>Task 2: Show the CLOSE-04 locators can fail, run the gate green on this file alone, freeze and commit</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-GH15-RECONCILIATION.md</files>
  <read_first>
    - `146-check-claims.py` (this directory) — the positional-argument mode, and the caveat label names the gate prints when one is missing.
    - `145-bench-validation/145-08-SUMMARY.md` — three acceptance locators recorded as false greens, one of them passing against a record with no content in it. That is why every locator here carries a negative control.
  </read_first>
  <action>
**Run the gate against this artifact alone**, in positional-argument mode: exit 0, no forbidden hit, no missing caveat. Do **not** assert anything about the all-five default run — plan 146-10 executes in this same wave, so the two release bodies may or may not exist while this plan runs, and 146-11 owns that run. If you execute it for information, label the reading informational and draw no conclusion from it.

**Then show each locator can fail**, using scratch copies under `/tmp/gsd146` that are discarded afterwards: add a tenth numbered row and confirm the row count reports ten; change one disposition cell to an invented token and confirm the valid-row count drops to eight; delete the public-correction section and confirm its two anchors report missing; strip the voltage figure and the silicon-margin phrase and confirm the **gate** exits non-zero naming the missing caveat labels. Record all four readings and the label names the gate printed.

**Then check this artifact against the ledger for drift** — the ledger is the single source of permitted wording. Three named checks, each recorded with the ledger's corresponding statement quoted alongside this artifact's: the bench scope, the ceiling statement, and that nothing here claims what the ledger's claim table does not permit. A divergence is resolved by changing **this** artifact, never the ledger.

**Then freeze and commit.** Record the artifact's `git hash-object` blob SHA and its byte count from `wc -c` — a byte count, never a codepoint length — in **this plan's SUMMARY**, not in `146-CITATIONS.md`: plan 146-10 runs concurrently in this same wave and a second appender to that register would race it. Plan 146-11 owns the register's next section and plan 146-12 re-measures the freeze inside its own posting task, so nothing is lost by keeping these values here. Commit the artifact in the meta repository with a message naming CLOSE-04.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-GH15-RECONCILIATION.md > /tmp/gsd146/gate_recon.txt 2>&1; rc=$?; echo "gate_rc=$rc"; cat /tmp/gsd146/gate_recon.txt; test "$rc" = "0"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && sed -E 's/6\.25//g; s/[Ss]ilicon[- ][Mm]argin//g' 146-GH15-RECONCILIATION.md > /tmp/gsd146/recon_stripped.md && python3 146-check-claims.py /tmp/gsd146/recon_stripped.md > /tmp/gsd146/gate_recon_neg.txt 2>&1; rc=$?; echo "negative_control_rc=$rc"; grep -i caveat /tmp/gsd146/gate_recon_neg.txt; test "$rc" != "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && echo "blob=$(git hash-object 146-GH15-RECONCILIATION.md) bytes=$(wc -c < 146-GH15-RECONCILIATION.md) fw_porcelain=$(git -C /workspaces/firestarter status --porcelain | wc -l) citations_untouched=$(git -C /workspaces status --porcelain -- .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CITATIONS.md | wc -l)"; test "$(git -C /workspaces/firestarter status --porcelain | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The gate run against this artifact alone exits **0** with no forbidden hit and no missing-caveat complaint, and its `PASS:` line is recorded verbatim.
    - Four negative controls were run and recorded: the tenth-row copy reported ten rows; the invented-token copy dropped the valid-row count to eight; the correction-deleted copy reported its two anchors missing; and the caveat-stripped copy made the **gate** exit non-zero, with the label names it printed recorded in the summary. Every scratch copy was discarded.
    - Nothing in this plan asserts an outcome for the gate's all-five default run. If that run was executed at all, the summary labels it informational and states why: 146-10 executes in the same wave and 146-11 owns that run.
    - The three drift checks against the ledger are each recorded as checked, with the ledger's statement quoted alongside this artifact's, and any divergence was resolved by editing this artifact.
    - The freeze values — blob SHA and a `wc -c` byte count — are recorded in this plan's SUMMARY, and the third leg prints `fw_porcelain=0` and `citations_untouched=0`, so this wave-4 plan did not race its sibling for the citation register.
  </acceptance_criteria>
  <done>The gate passes against this artifact alone, every content locator has been shown able to fail with its reading recorded, the artifact has been checked against the ledger in three named places, and its freeze values are in the register.</done>
</task>

</tasks>

<verification>
- Reconciliation committed and exit 0 under the claim gate run against it alone; nine numbered rows, vocabulary constrained to three literals, four negative controls recorded.
- Public correction present with its fact and both `file:line` citations; bench paragraph carries five facts and satisfies the caveat rule; freeze values in the SUMMARY with `146-CITATIONS.md` untouched; neither sub-repo touched, nothing posted, no `CLOSE-*` row moved.
</verification>

<success_criteria>
gh#15's nine acceptance criteria are answered item by item against the text as filed, every earlier correction named inline, the error already public in our own comment corrected, and the artifact frozen and gate-green.
</success_criteria>

<output>
Create `146-09-SUMMARY.md` in this phase directory. Record the disposition-row readings, all four negative-control readings with the caveat labels the gate printed, the three drift checks with quotations, and the freeze blob SHA and byte count — the register is not written by this plan.
</output>
