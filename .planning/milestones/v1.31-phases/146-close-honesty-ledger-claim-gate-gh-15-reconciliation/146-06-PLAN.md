---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 06
type: execute
wave: 2
depends_on: ["146-03"]
commits_land_in: firestarter
reads_repos: [firestarter]
files_modified:
  - firestarter/doc/PROTOCOLS.md
  - firestarter/CLAUDE.md
  - firestarter/README.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-06-SUMMARY.md
autonomous: true
requirements: [CLOSE-03]
must_haves:
  truths:
    - "Per D-06 every edit here is documentation and wording only — no firmware source, header, CMake, script or test change — so the image Phase 145 bench-validated is provably the one that ships."
    - "Per CLOSE-03 the firmware documentation describes the shipped per-byte loop, the parameter table, the database-supplied pulse, the host pulse-override flag and the ~6.25 V accepted debt."
    - "Per D-04/D-05 the stale native-environment numerals are corrected in place AND a labelled block appended after the paragraph records what they said and why — but the block states the superseded figures in a form that does NOT reproduce the L3 locator string, so the correction and the locator flip are simultaneously satisfiable."
    - "The four locator REDs recorded in 146-DOC-CHECK-RECORD.md §2 flip here, and the flips are recorded in a NEW §7 — §5 and §6 already exist on disk, and §6 itself routes this plan's GREEN to §7."
  artifacts:
    - path: "firestarter/doc/PROTOCOLS.md"
      provides: "Sections 1.3-1.5 describing the shipped per-byte pulse-to-verify loop rather than the loop it replaced, plus the host pulse-override flag with its provenance narrowing and the program-VCC ceiling as accepted debt"
      contains: "6.25"
    - path: "firestarter/CLAUDE.md"
      provides: "Corrected native-environment totals with a labelled block preserving the superseded reading, the program-VCC ceiling, a discharged-at routing note, and zero occurrences of the unqualified claim word"
      contains: "CORRECTION (Phase 146"
  key_links:
    - from: "firestarter/src/proms/eprom.cpp"
      to: "firestarter/doc/PROTOCOLS.md"
      via: "the shipped per-byte inner loop, whose line range read from source in this plan replaces the stale reference to the loop it superseded"
      pattern: "eprom_internal_program_pulse"
  prohibitions:
    - "MUST NOT edit any firmware source, header, CMake file, script or test (D-06); MUST NOT edit tools/catalog/messages.toml, include/messages.h or firestarter/messages.py — the message wording is 146-07's, through the codegen path."
    - "MUST NOT re-derive or remove anything §1.3 already gets right (the zero-value pulse fallback, the modal value with its chip count, the max-pulses datasheet basis, the no-over-program citations, the named scoped divergence), and MUST NOT redo §1.5's in-place energy-cap correction or §1.4's contradiction note — both landed in the parameter-table phase."
    - "MUST NOT run any build, pass a milestone native environment name to the size-baseline or build-warning scripts, add a test to any pinned native environment (asserted at an exact case count), or run the firmware suite before committing (two suites assert the firmware repo's whole-repo porcelain); MUST NOT tick any CLOSE requirement (146-13 owns all five); MUST NOT push, merge, tag or dispatch a workflow (D-01)."
---
<objective>
Bring the firmware documentation into line with the firmware that shipped (CLOSE-03, D-06): replace `doc/PROTOCOLS.md` §1.3's stale write-algorithm clauses with the loop that actually runs, add the host pulse-override flag and the program-VCC ceiling, correct `CLAUDE.md`'s stale native-suite numerals behind a labelled block, and reword the four unqualified claim-word occurrences the checker's pattern 10 matches.

`doc/PROTOCOLS.md` has been stale since the parameter-table phase — §1.3 still calls the present loop retry escalation and promises a future replacement, five phases after that phase landed. `CLAUDE.md` carries four of CLOSE-03's five topics but lacks the ceiling and carries a numeral pair routed here in writing. `README.md` says nothing about 27C. Every edit is wording: the bench-validated image must remain the shipping image.

Output: three edited firmware documents, one labelled block, one commit inside `firestarter`, and `146-DOC-CHECK-RECORD.md` §7.
</objective>
<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md
@.planning/REQUIREMENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Rewrite doc/PROTOCOLS.md §§1.3-1.5 for the shipped loop, the pulse override and the ceiling</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - `firestarter/doc/PROTOCOLS.md:129-235` — §§1.3-1.5 in full: the five sub-heading shape, the per-item citation convention, §1.3's write-algorithm paragraph whose final two clauses are both false (locators **L1** and **L2** in `146-DOC-CHECK-RECORD.md` §2), and §1.5's existing in-place labelled correction, which is the style donor and must not be redone.
    - `firestarter/src/proms/eprom.cpp` — the write path: pre-flight refusals near `:90-110`, `eprom_internal_program_pulse()` near `:247-253` with its two settle constants and in-source comment, `eprom_hv_route_mask()` near `:284-299`, and the per-byte loop with its inner retry loop, budget checks and progress emission in the 400-500 range. Take the shipped inner-loop line range from what you read, never from a document. Then `include/eprom.h:160-170` for the two VPP settle constants.
    - `firestarter/src/proms/eprom_params.cpp:22-58` and `include/eprom_params.h:38-83` — the shipped table, its six columns in order, and the header comment stating the datasheets' raised-VCC verify margin is unreachable on this shield: the only place the ceiling currently lives in this repo, and the source for the ceiling wording. Then `firestarter_app/firestarter/cli_handlers.py:546-610` for the pulse-override option's help text, which states the provenance narrowing verbatim and is quotable, and `.planning/REQUIREMENTS.md` §"Evidence ceiling" for the authoritative ceiling wording including the not-behavior-preserving clause.
  </read_first>
  <action>
Edit §1.3's **Write algorithm** paragraph, keeping everything it already gets right and replacing **only** its final two clauses — the ones describing the present loop as retry escalation and promising a future replacement — with what actually ships: a per-byte pulse-to-verify loop applying a fixed-width pulse, verifying, and repeating up to the max-pulses backstop, with the program-voltage route asserted around every pulse, the two settle waits named by their constants, the verify mode taken from the table's own column (which on this row adds one final full-array pass), and the failure identity on budget exhaustion. Cite the shipped inner loop by an `eprom.cpp:NNN-NNN` range read from source in this task, cite the pulse helper by name, and leave no reference to the superseded range (L2).

Add two items to **each** of §§1.3, 1.4 and 1.5, in the existing sub-heading style with their own citation lines. **The host pulse-override flag:** the per-run pulse width can be overridden from the host; name the flag, its bound, and the provenance narrowing the shipped help text carries — the bound is parity with another programmer's own integer width and is **not** a wire-type or hardware limit. Then the firmware-side reality per row: the pre-flight refusal is keyed on the energy-cap column, so it is live where that column is non-zero and structurally unreachable on the two rows where it ships as zero. Cite the parse site and the refusal site by `file:line`. Do not clamp anything or propose clamping — that is recorded elsewhere and 999.31 owns the decision. **The program-VCC ceiling, as accepted debt:** in the requirements document's own terms — the raised program-VCC all four vendor algorithms assume for threshold margin is unreachable on this shield, which has no VCC-raise path; this milestone buys timing, pulse-count and verify fidelity and **not** silicon-margin fidelity; hardware-bound and recorded rather than attempted. Cite the parameter-table header comment and the requirements section by name, and use the voltage figure in a form a grep will find.

Leave §1.5's in-place correction and §1.4's contradiction note byte-unchanged, and leave the invariant-traceability section alone. Write in the phase's vocabulary — validated, established, measured, demonstrated, skipped-with-reason — and avoid the unqualified claim forms the checker's pattern table catches; this file is a checker target measured clean of them, so keep it clean. Do not commit: the sub-repo commit happens once, at the end of Task 3.
  </action>
  <verify>
    <automated>cd /workspaces && l1=$(grep -c 'Phase 141 replaces it' firestarter/doc/PROTOCOLS.md); l2=$(grep -c 'eprom.cpp:159-179' firestarter/doc/PROTOCOLS.md); echo "L1=$l1 L2=$l2"; test "$l1" = "0" && test "$l2" = "0"</automated>
    <automated>cd /workspaces && a=$(grep -c -- '--pulse-us' firestarter/doc/PROTOCOLS.md); b=$(grep -c '6\.25' firestarter/doc/PROTOCOLS.md); c=$(grep -ciE 'per[-_ ]byte' firestarter/doc/PROTOCOLS.md); echo "pulse_override=$a ceiling=$b per_byte=$c"; test "$a" -ge 3 && test "$b" -ge 3 && test "$c" -ge 3</automated>
    <automated>cd /workspaces && n=$(git -C /workspaces/firestarter diff --numstat -- src include lib platform scripts tests tools platformio.ini | wc -l); echo "source_files_changed=$n"; test "$n" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Locators **L1** and **L2** both print **0**, down from the **1** each recorded in `146-DOC-CHECK-RECORD.md` §2. Quote the RED and GREEN readings with identical command text in the summary — the pair is what makes it a proof. Note §2's own warning: these arrive with `grep` rc=1 because the printed count is 0, so the assertion is on the printed integer.
    - The replacement text cites the shipped inner loop by an `eprom.cpp:NNN-NNN` range read from source in this task, and opening that range confirms it is the inner retry loop.
    - `--pulse-us`, `6.25` and the hyphenated per-byte wording each appear **at least 3** times, one per protocol section; the ceiling count was **0** before this edit. The pulse-override text states the provenance narrowing and, per row, whether the pre-flight refusal is reachable, with the parse and refusal sites cited by `file:line`; the ceiling text states what the milestone buys and what it does not, citing both the parameter-table header comment and the requirements section.
    - Everything §1.3 already got right is still present, and §1.5's correction plus §1.4's note appear in no diff hunk. `git -C /workspaces/firestarter diff --numstat` over source, header, library, platform, script, tool, test and build-config paths prints **no output** (D-06) — reachable and non-vacuous because nothing is committed yet in this task, so a source edit would show.
  </acceptance_criteria>
  <done>§§1.3-1.5 describe the shipped per-byte loop with a source-read range, carry the pulse-override flag with its provenance narrowing and the ceiling as accepted debt, retain every citation they had, and no firmware source changed.</done>
</task>
<task type="auto">
  <name>Task 2: Correct CLAUDE.md's stale suite totals behind a labelled block, add the ceiling, clear the claim word</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    - `firestarter/CLAUDE.md:57-75` — the algorithm-handler table. The three 27C rows are single very long lines, each ending with a source-contract-only assertion using the bare claim word the checker's pattern 10 matches, with a second occurrence mid-row in the middle row's boundary clause: four occurrences at `:64` (one), `:65` (two), `:66` (one), measured and recorded in `146-DOC-CHECK-RECORD.md` §2.3. That section also records why `grep -c` reports 3 (it counts lines) and that the bracketed character-class form measures identically.
    - `firestarter/CLAUDE.md:103-146` — the operation-setup section's pulse-override paragraph, which already states the bound, the parity provenance, the unclamped wire parse and the reachable refusal, and ends by routing the reconciliation of that gap to this phase. Then `:270-282` — the native test-environment section with its pinned-environment exception and the paragraph carrying the two stale numerals (locators **L3** and **L4**).
    - `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md` around `:135-155` and `:518` — the measured suite counts at this milestone's tip and the finding that routed this correction here, recorded and not fixed at that time. Then `check_record_corrections.py:280-310` (Phase 130 dir) for the block opener the meta-repo blocks use, so one consistent opener spans the phase and the register's block column stays meaningful.
  </read_first>
  <action>
Three edits.

**Edit 1 — the stale numerals.** Rewrite the paragraph's two numerals to the measured ones (so L3 goes to 0 and L4 to at least 1), and append immediately **after** that paragraph a labelled block using the same recognised opener the meta-repo blocks use, naming this phase, the requirement and the origin finding id. Its content: the paragraph's numerals were updated in place; the superseded reading is preserved here; the measured counts are cited to `144-TEST-RECORD.md` by section and line, including the finding that named this correction and left it unfixed; one of the two suites grew after that paragraph was written and the growth was never folded back; and both figures share a boundary — the environment runs in **no** CI leg of either repository, so neither was ever a CI measurement. Only the numerals were wrong. **Critical mechanical constraint:** the block must state the superseded figures in a form that does **not** reproduce L3's locator string — spell the number in words, or cite it to `146-DOC-CHECK-RECORD.md` §2 where the exact prior string is already recorded. Reproducing it verbatim would re-arm L3 and make this task's own criteria mutually unsatisfiable.

**Edit 2 — the ceiling and the routing note.** Add one paragraph carrying CLOSE-03's fifth topic in the requirements document's own terms: the raised program-VCC the vendor algorithms assume for threshold margin is unreachable on this shield, which has no VCC-raise path; this milestone buys timing, pulse-count and verify fidelity and not silicon-margin fidelity; hardware-bound, recorded rather than attempted, tracked as a future requirement. Cite the parameter-table header comment and the requirements section by name, use the voltage figure in a grep-findable form, and place it where a reader of the algorithm-handler table will meet it. In the same edit, change the pulse-override paragraph's routing clause from naming this phase as future reconciler to stating it was discharged here, pointing at `146-CORRECTIONS.md`.

**Edit 3 — the claim word.** The four occurrences at `:64`, `:65` and `:66` are exactly the unqualified claim shape the evidence ceiling forbids, and this file is a checker target. Reword each to say the same thing in the phase's taxonomy — the status is *established as a source contract*, or *attested as a source contract*, never behaviourally — including the middle row's boundary clause. Change **only** the claim word and the minimum grammar around it: every other technical statement in those three rows stays byte-identical, including every message identifier, decision reference, test path and boundary sentence. This is claim hygiene rather than a factual correction, so it gets a register row pointing at `146-DOC-CHECK-RECORD.md` and **not** a correction block — nothing above it was false. Do not commit yet.
  </action>
  <verify>
    <automated>cd /workspaces && l3=$(grep -c '71 cases' firestarter/CLAUDE.md); l4=$(grep -c '79 cases' firestarter/CLAUDE.md); echo "L3=$l3 L4=$l4"; test "$l3" = "0" && test "$l4" -ge 1</automated>
    <automated>cd /workspaces && w=$(grep -oiE '\bpro[v]en\b' firestarter/CLAUDE.md | wc -l); b=$(grep -c 'CORRECTION (Phase 146' firestarter/CLAUDE.md); c=$(grep -c '6\.25' firestarter/CLAUDE.md); echo "claim_word_occurrences=$w blocks=$b ceiling=$c"; test "$w" = "0" && test "$b" = "1" && test "$c" -ge 1</automated>
    <automated>cd /workspaces && for tok in MSG_ERR_MAX_PULSES MSG_ERR_ENERGY_CAP MSG_ERR_PULSE_TOO_WIDE MSG_DATA_PROGRESS eprom_hv_route_mask command_done EPROM_PROGRESS_EMIT_INTERVAL_MS; do grep -q "$tok" firestarter/CLAUDE.md || { echo "LOST $tok"; exit 1; }; done; echo "identifiers_intact=1"</automated>
  </verify>
  <acceptance_criteria>
    - **L3** prints **0** and **L4** prints **at least 1**, against the **1** and **0** recorded in §2; both readings quoted with identical command text in the summary.
    - `grep -c 'CORRECTION (Phase 146' firestarter/CLAUDE.md` returns exactly **1**, its opener matches the meta-repo form, and it sits **after** the paragraph it corrects, verified by line number.
    - The block states the superseded figures without reproducing L3's locator string — spelled in words or cited to §2 — which is what makes L3's flip and the block's preservation duty simultaneously satisfiable. It also cites `144-TEST-RECORD.md` by section and line, names the origin finding, states that one suite grew after the paragraph was written, states the no-CI-leg boundary, and states that the numerals were updated in place.
    - The claim-word occurrence count is **0**, down from the recorded baseline of **4** measured as occurrences (not the misleading line count of 3), so the locator has been seen both non-zero and zero.
    - All seven technical identifiers in the reworded rows survive; the ceiling appears at least once and states both halves; the pulse-override routing clause reads as discharged and points at `146-CORRECTIONS.md`.
  </acceptance_criteria>
  <done>The stale totals are corrected in place with a labelled block that preserves the superseded reading without re-arming its own locator, the ceiling is present and findable, the routing clause reads as discharged, and the claim word is gone with every identifier intact.</done>
</task>
<task type="auto">
  <name>Task 3: Add README.md's 27C paragraph, commit once inside firestarter, run the suite, record §7</name>
  <files>firestarter/README.md, .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md</files>
  <read_first>
    - `firestarter/README.md:105-126` — the protocol-notes section, six lines entirely about the parallel-EEPROM protocol with a pointer into the protocol document, then the shield-revision section. This is where a 27C paragraph belongs and what it must sit alongside without disturbing.
    - `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 **as edited by Task 1** — the paragraph written here is the short user-facing form of it and must neither contradict it nor restate its citations. Then `.planning/REQUIREMENTS.md` §"Evidence ceiling" for the plainest ceiling wording.
    - `146-DOC-CHECK-RECORD.md` §§2 and 6 — §2 for the four locators' command text, §6 for the routing that makes this plan's GREEN **§7** (§5 and §6 already exist on disk, so a `## 5.` locator would pass vacuously). Then `firestarter/tests/test_flash_path_record_sync.py` around `:1240-1255` — the assertion that the firmware repo's whole-repo porcelain is empty, which is why commit-then-run is mechanical rather than stylistic.
  </read_first>
  <action>
**Edit.** Add a short paragraph to the protocol-notes section — five to eight lines, in the voice the existing parallel-EEPROM paragraph uses. Two things only. How programming works now: 27C programming applies a fixed-width pulse per byte and verifies after each one, repeating up to a per-protocol backstop, with the pulse width coming from the chip database rather than a protocol constant and overridable per run from the host flag, and a failure naming the address and the pulse count. And the one thing this shield cannot do: the raised program-VCC the vendor algorithms assume for threshold margin is unreachable here, so this is timing, pulse-count and verify fidelity and not silicon-margin fidelity — hardware-bound and recorded, not attempted. Point at the protocol document's sections in the style the existing paragraph uses. No table, no datasheet citations. Use the voltage figure and the hyphenated per-byte wording in grep-findable forms — both are required topics for this file in the checker's map.

**Then commit, once, inside the firmware repository.** Confirm `git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD` is the milestone branch, stage exactly the three documentation paths this plan edited and nothing else, verify with a porcelain listing that nothing else is pending, and commit **inside** `firestarter` — not in meta — with a message naming CLOSE-03 and the origin finding for the numeral correction. Do not push (D-01).

**Then, and only then, run the firmware suite** from `/workspaces/firestarter` with `python3 -m pytest tests -o addopts="" -q`. No suite baseline exists in `146-CITATIONS.md` §0, so do not compare against one: the decisive oracle is **exit 0**, and the printed passed count is **recorded here as the baseline** for plan `146-07` to compare against. Record the count and the wall time. A failure is a stop-and-report naming the failing test — a documentation edit should not move it, and if it did, a doc-content assertion in the suite must be read rather than worked around. Also record the `firestarter` upstream-ahead count: §0.1 records **61** at phase start and states the owed assertion is that it never **falls**, a fall being the signal that a push happened.

**Then append §7** to `146-DOC-CHECK-RECORD.md` — not §5, which already exists — carrying all four locator flips as before-and-after pairs with the identical command text §2 used, the claim-word occurrence count 4 to 0, the firmware suite count and wall time, and the statement that the checker as a whole is still RED because the host half has not landed; that GREEN is plan `146-07`'s, which §6 routes to §8. Do not edit §§1-6. Commit that record edit in the **meta** repository, separately from the sub-repo commit.
  </action>
  <verify>
    <automated>cd /workspaces && a=$(grep -c '6\.25' firestarter/README.md); b=$(grep -ciE 'per[-_ ]byte' firestarter/README.md); s=$(grep -c '^## 7\.' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md); echo "readme_ceiling=$a readme_per_byte=$b record_section7=$s"; test "$a" -ge 1 && test "$b" -ge 1 && test "$s" = "1"</automated>
    <automated>cd /workspaces && git -C /workspaces/firestarter rev-parse --abbrev-ref HEAD; n=$(git -C /workspaces/firestarter show --name-only --format= HEAD | grep -c .); p=$(git -C /workspaces/firestarter status --porcelain | wc -l); h=$(git -C /workspaces/firestarter rev-list --count @{u}..HEAD); echo "files_in_commit=$n fw_porcelain=$p ahead=$h"; git -C /workspaces/firestarter show --name-only --format= HEAD; test "$n" = "3" && test "$p" = "0" && test "$h" -ge 61</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/firestarter && python3 -m pytest tests -o addopts="" -q > /tmp/gsd146/fw_suite_06.txt 2>&1; rc=$?; echo "fw_suite_rc=$rc"; tail -3 /tmp/gsd146/fw_suite_06.txt; test "$rc" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/README.md` carries the ceiling figure and the hyphenated per-byte wording at least once each — both were **0** before this edit — and the paragraph names both halves: how programming works now, and that this is timing, pulse-count and verify fidelity rather than silicon-margin fidelity.
    - The commit landed **inside** `firestarter` on the milestone branch and its file list is exactly the three documentation paths; the firmware porcelain is **0** lines after the commit and the suite was not run before it.
    - The firmware suite exits **0**. Its passed count is recorded as this phase's firmware-suite baseline rather than compared against a §0 figure — no such baseline exists, so no criterion demands one.
    - The `firestarter` upstream-ahead count is **at least 61** and is recorded before and after, per §0.1's own assertion form: it may rise as this phase commits and may never fall.
    - `146-DOC-CHECK-RECORD.md` gains exactly one `## 7.` section carrying all four locator flips with identical command text, the claim-word count 4 to 0, the suite count and wall time, and the statement that the whole checker is still RED pending the host half. §§1-6 appear in no diff hunk.
  </acceptance_criteria>
  <done>The README carries a user-facing 27C paragraph with the ceiling stated plainly, one commit landed inside `firestarter` on the milestone branch containing exactly three documentation files, the firmware suite passes with its count recorded as the baseline, and §7 records all four flips with the commands that produced the RED.</done>
</task>
</tasks>
<verification>Three firmware documentation files edited; one commit inside `firestarter` on the milestone branch containing exactly those three paths; the source-path numstat empty at every point (D-06); all four §2 locators flipped and the claim-word count 4 to 0; firmware suite exit 0 with its count recorded as the new baseline; ahead-count never fell; no `CLOSE-*` checkbox or coverage row moved.</verification>
<success_criteria>A firmware developer reading `doc/PROTOCOLS.md` reads the algorithm that ships, an agent reading `CLAUDE.md` reads the measured suite totals and the accepted debt, and a user reading `README.md` learns both what changed and the one thing this shield cannot do — with no firmware behaviour changed and every claim in the phase's own vocabulary.</success_criteria>
<output>Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-06-SUMMARY.md`. Record the four locator before-and-after pairs, the claim-word occurrence pair, the firmware suite count and wall time, the sub-repo commit SHA, and the ahead-count before and after.</output>
