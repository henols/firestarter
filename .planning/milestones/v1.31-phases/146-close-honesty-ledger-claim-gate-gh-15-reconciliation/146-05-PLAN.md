---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 05
type: execute
wave: 2
depends_on: ["146-03"]
commits_land_in: meta
reads_repos: [firestarter]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CORRECTIONS.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-05-SUMMARY.md
autonomous: true
requirements: [CLOSE-04]
must_haves:
  truths:
    - "Per D-04/D-05/D-14 the inherited correction queue is discharged here: each correction lands as a labelled block APPENDED after the text it corrects plus a register row, and every false text is cited by file:line, never reproduced. OD-B adds an eighth: the comparative-claim sentence at .planning/PROJECT.md:216."
    - "MEASURED, superseding the inherited prose: the two ROADMAP blocks (:169, :396) and the 143 D-01 charter discharge (:394) are ALREADY COMMITTED at 8df5e564, and the Phase 130 record gate is ALREADY rc=0 at HEAD. This plan verifies both and re-lands neither."
    - "Three of D-04's seven do not hold as CONTEXT states them; each divergence is recorded as its own finding rather than reconciled."
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CORRECTIONS.md"
      provides: "Per correction: origin finding id, owning file:line, false text cited never quoted, corrected text stated positively, owning plan. Plus the three divergences, the record-gate flip, the block-versus-history rule and the two host-README adjacency findings."
      contains: "F-140-07"
  key_links:
    - from: ".planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py"
      to: ".planning/PROJECT.md"
      via: "the recognised block-label opener, which makes a Phase 146 block an exempt region rather than unlabeled prose"
      pattern: "CORRECTION"
  prohibitions:
    - "MUST NOT re-land, re-edit or revert the two ROADMAP blocks or the charter discharge (committed at 8df5e564); MUST NOT insert any block ABOVE the text it corrects; MUST NOT manufacture a correction where research measured none (the independence clause has no PROJECT.md site)."
    - "MUST NOT edit .planning/STATE.md, 146-04-SUMMARY.md, any dated footer, any decision-log entry or any archived milestone document — history gets a register row, never a block."
    - "MUST NOT clamp the unclamped wire-field parse — recorded only, 999.31 owns it (D-06); MUST NOT touch firestarter/ or firestarter_app/ (D-06); MUST NOT push, merge, tag or dispatch a workflow (D-01)."
    - "MUST NOT use a bulk roadmap/requirements rewrite verb, state.advance-plan, state.begin-phase, state.planned-phase or gsd-tools query commit — all recorded broken; MUST NOT tick any CLOSE requirement (146-13 owns all five)."
---
<objective>
Discharge the inherited correction queue (D-04, D-05): verify the ROADMAP half already landed, land the five still-owed blocks in `PROJECT.md` and `REQUIREMENTS.md` including OD-B's eighth, and write the register. Closing the milestone with its live planning record asserting what its own measurements disprove is the failure D-04 prevents. Measured state supersedes the inherited prose twice — an interrupted executor already committed the ROADMAP half at `8df5e564`, and the Phase 130 record gate is already **rc=0 at HEAD** because the RED at `.planning/STATE.md:11` closed when `last_activity_desc` was rewritten — so this plan verifies and records that flip rather than closing it.

Output: five correction blocks, one deferral note discharged, `146-CORRECTIONS.md`.
</objective>
<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>
<tasks>
<task type="auto">
  <name>Task 1: Verify the landed ROADMAP half and record the record-gate flip with its causation</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-05-SUMMARY.md</files>
  <read_first>
    - `.planning/ROADMAP.md:167-171` and `:380-397` — the two committed blocks and the charter's discharged-at clause; style donor for Task 2's openers.
    - `check_record_corrections.py:255-310` (Phase 130 dir) — the `arm-toolchain-absent` two-token lookahead needle, and the opener/label/closer regexes that make a block an exempt region extending **forward**.
    - `146-CITATIONS.md` §0.6 — the last-green tally, the bisection attributing the RED to `d2c212f1`, the hand-off naming this plan owner, and its measured note that **zero** active enumerated-line exemption markers exist in the three files this plan touches, so the line-shift orphaning hazard does not apply. Then `146-04-SUMMARY.md`'s record-gate report, which this task verifies as accurate.
  </read_first>
  <action>
This task edits no planning record; it measures and records. **Verify the landed ROADMAP half:** two blocks with the recognised opener, each after its subject (C-2's opener line > 167; C-1's > the charter paragraph's line), the charter clause reading as discharged here and pointing at `146-CORRECTIONS.md`, and an empty working-tree diff. Record the line numbers; do not edit the file.

**Verify the record-gate flip.** Run the Phase 130 gate on default targets, capturing its exit status immediately rather than after a pipe. Expect rc=0 and the tally `{'block': 23, 'line-label': 4, 'inline-history': 6, 'inline-allow': 10, 'superseded': 12}` — measured at HEAD for this plan, identical to §0.6's last-green reading. Record the `PASS:` line verbatim.

**Measure the causation rather than inheriting it.** Print the needle's current match lines in `.planning/STATE.md`, confirm 11 is not among them, then name the commit that removed the sentence from line 11 via `git log`. The attribution is settled and is `91a06604`, an interrupted `146-05` commit whose own message records the flip (`rc=1 -> rc=0`, `unlabeled 1 -> 0`). The replan brief's §4 credited `146-04` and was **wrong**, settled by testing the needle (`arm-none-eabi-gcc` and `absent` on one line) against each historical `STATE.md` blob: it sits on line 11 at `d2c212f1`, `083e4e5f`, and every `146-04` commit through its last, `0accb44e`, and is gone from line 11 at `91a06604`. A second match at line 1140 persists and is exempt, which is why the file scans rc=0 with it present. `146-04-SUMMARY.md`'s report of the gate as still RED is therefore **accurate** — do not record it as a misreport and do not edit that file. Re-measure rather than inheriting either reading, and record the brief's error as the divergence. A needle reappearing unlabelled is a stop-and-report naming the line. and carry the substantive point to Task 3 as a register row: the sentence asserted the ARM toolchain was absent, and `146-03` measured that false.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py > /tmp/gsd146/rec_05t1.txt 2>&1; rc=$?; echo "record_gate_rc=$rc"; grep -o "exempt hits by verdict.*" /tmp/gsd146/rec_05t1.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces && n=$(grep -c 'CORRECTION (Phase 146' .planning/ROADMAP.md); d=$(grep -c 'DISCHARGED AT Phase 146' .planning/ROADMAP.md); w=$(git diff --numstat -- .planning/ROADMAP.md | wc -l); echo "roadmap_blocks=$n charter_discharged=$d worktree_diff_lines=$w"; test "$n" = "2" && test "$d" -ge 1 && test "$w" = "0"</automated>
    <automated>cd /workspaces && python3 -c "import re;L=open('.planning/STATE.md',encoding='utf-8').read().splitlines();p=re.compile(r'(?=.*arm-none-eabi-gcc)(?=.*absent)',re.I);h=[i+1 for i,l in enumerate(L) if p.search(l)];print('needle_lines='+repr(h));assert 11 not in h,'needle back on line 11 -- stop and report'" && git log --oneline -8 -- .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The record gate exits **0** at HEAD with its tally recorded verbatim and equal to §0.6's last-green reading. Reachable because the RED is already closed: this task expects the gate GREEN and demands no value that only prints on a RED path.
    - `grep -c 'CORRECTION (Phase 146' .planning/ROADMAP.md` returns **2**, `DISCHARGED AT Phase 146` returns **≥1**, the working-tree diff is **0** lines, and each block's opener line is recorded as greater than its subject's.
    - The needle's match lines are printed and **11 is not among them**; the removing commit is named from `git log`; both prior written attributions are stated side by side, neither reconciled.
    - `146-04-SUMMARY.md`'s report is recorded as **accurate** with the settling command quoted, and the file is not edited. The needle sat on line 11 at every `146-04` commit through its last (`0accb44e`) and is absent from line 11 at `91a06604`, so nothing 146-04 wrote was an under-claim.
  </acceptance_criteria>
  <done>The landed ROADMAP half is verified committed and correctly placed, the gate is verified rc=0 with its tally recorded, the flip's causation is measured with both attributions stated, and 146-04's report is confirmed accurate.</done>
</task>
<task type="auto">
  <name>Task 2: Land the four PROJECT blocks and the one REQUIREMENTS block, including OD-B's eighth</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/PROJECT.md` — `:131` the deferral note that becomes discharged; `:151-160` the gh#15 corrections table (row **C3**) and its structural-consequence paragraph; `:176-181` the `0x0B` energy-budget bullet carrying the datasheet justification; `:210-216` the throughput table and, at `:216`, OD-B's comparative-claim sentence. Also `.planning/REQUIREMENTS.md:17-28` — the scoping table whose **D-02** rationale cell carries the same justification, and the `## Evidence ceiling` heading at `:28` that closes a block appended after the table.
    - `firestarter/src/json_parser.c:459-497`, `include/firestarter.h:197`, `src/proms/memory.cpp:32-34` and `:243-251`, `src/proms/eprom.cpp:90-110` — the wire field parsed unclamped into 32 bits, an over-ceiling value now **delivered** rather than truncated (split-delay helper), and the sole refusal gated on a cap shipping as zero on two of three rows. Then `src/proms/eprom_params.cpp:22-58` and `include/eprom_params.h:38-83` — the shipped table, its column order, and its own comment naming the contradiction with the throughput table; read the over-program value from source, not from any record.
    - `.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md:255-275` — total programming time for all bits (one hundred seconds) versus the per-location typical pulse width (fifty milliseconds); quote the fact, cite the record. And `146-RESEARCH.md` §"The Seven Inherited Corrections" items (3)-(6) for the measured verdicts, including the three that diverge from the inherited prose.
  </read_first>
  <action>
Snapshot both files. Work **bottom-up** — lowest-line block first — so no insertion shifts a later anchor. **Re-verify the non-finding first:** grep `PROJECT.md` for the independence clause carried at `.planning/ROADMAP.md:382`, taking the needle from that line rather than retyping it, and expect **zero** hits. If zero, that correction's PROJECT half has no false-statement site, so no block is written — `:131` carries a *true routing note*: update it from deferred-to-146 to discharged-at-146 pointing at `146-CORRECTIONS.md`, leave the dated footers as history, and hand the non-finding to Task 3 as a register row. A hit is a stop-and-report: the premise changed.

Then append four blocks to `PROJECT.md`, bottom-up, each with the opener used at `ROADMAP.md:169`, each naming its origin finding id, each **after** its subject. Content is the measured fact set from `<read_first>`, stated positively and cited by `file:line`, never by restating the false text:
- **C-8 (OD-B), after `:216`** — a comparative claim is forbidden: fidelity not improvement, no control run by deliberate choice, and the historical write figure that could read as a baseline is a recorded historical number cited to `145-BENCH-LOG.md` boundary 1 by line. Note the sentence is scoping-era intent prose and stands corrected anyway, because the next milestone's scoping pass reads it; attribute to OD-B alongside the boundary.
- **C-5, after the throughput table** — **both** the `0x07` and `0x08` rows imply an over-program factor of three while the shipped table sets that column to zero on both: two rows, not the one the inherited prose names, and the shipped source says so in its own comment. Their worst-case figures used the over-program pulse and so err conservatively. Third defect: the `0x0B` row's max-pulses cell states the energy cap rather than the shipped max-pulses value — give the shipped value. Affirm that the conditional prose above the table is correct and needs no correction.
- **C-6, after the energy-budget bullet** — the **value** has a primary-datasheet basis and the published **reason** does not; give the hundred-seconds-versus-per-location fact and cite the establishing record. Name the halves still owed (the public one `146-12` posts, plus the planning sites) and state that the firmware protocol document's copy was **already corrected in place** by the parameter-table phase, cited by `file:line`, so this phase does not redo it.
- **C-3, after the corrections table and its structural-consequence paragraph** — the three wire-field facts, each cited to firmware `file:line`, plus an explicit statement that this is **recorded and not clamped** (D-06) with 999.31 named as owner of the adjacent decision.

Then append **one** block to `REQUIREMENTS.md`, after the scoping table and before `## Evidence ceiling`, carrying the same energy-cap correction scoped to the **D-02** rationale cell above it; do not edit the cell, any requirement text, any checkbox or any traceability row. Re-run the Phase 130 gate after the PROJECT insertions and again at task end, capturing its exit status each time: it must exit **0** with the four non-`block` buckets at `line-label: 4`, `inline-history: 6`, `inline-allow: 10`, `superseded: 12`. The `block` bucket may rise if a new block's own text matches one of the gate's needles — correct behaviour, since a hit inside a labelled block is exempt by construction — so record the delta and the needle rather than treating it as failure. `REQUIREMENTS.md` is not one of the gate's targets. Prove the blast radius with `git diff --numstat`, never a piped grep over the diff: a recorded case reported zero changed lines over a genuinely changed markdown file because the diff marker collides with the list bullet. Commit both files in one commit naming the four origin ids plus OD-B.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces && python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py > /tmp/gsd146/rec_05t2.txt 2>&1; rc=$?; echo "record_gate_rc=$rc"; test "$rc" = "0" && python3 -c "import ast,re;d=ast.literal_eval(re.search(r'[{].*[}]',open('/tmp/gsd146/rec_05t2.txt',encoding='utf-8').read()).group(0));print('tally='+repr(d));assert [d.get(k) for k in ('line-label','inline-history','inline-allow','superseded')]==[4,6,10,12],d;print('non_block_unchanged=1 block='+str(d.get('block')))"</automated>
    <automated>cd /workspaces && p=$(grep -c 'CORRECTION (Phase 146' .planning/PROJECT.md); r=$(grep -c 'CORRECTION (Phase 146' .planning/REQUIREMENTS.md); echo "project_blocks=$p requirements_blocks=$r"; test "$p" = "4" && test "$r" = "1"</automated>
    <automated>cd /workspaces && t=$(grep -c '^- \[x\] \*\*CLOSE-' .planning/REQUIREMENTS.md); q=$(grep -c '| CLOSE-0[1-5] | Phase 146 | Pending |' .planning/REQUIREMENTS.md); s=$(git status --porcelain .planning/STATE.md | wc -l); f=$(git -C /workspaces/firestarter status --porcelain | wc -l); echo "close_ticked=$t pending_rows=$q state_dirty=$s fw_dirty=$f"; test "$t" = "0" && test "$q" = "5" && test "$s" = "0" && test "$f" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - The independence clause was re-measured against `PROJECT.md` and returned **zero** hits, with the command and reading recorded, and no block was written for it; `:131` reads as discharged and points at `146-CORRECTIONS.md`.
    - `grep -c 'CORRECTION (Phase 146'` returns exactly **4** over `PROJECT.md` and **1** over `REQUIREMENTS.md`; both were measured **0** before this task. Every block's opener line exceeds its subject's, recorded as a pair.
    - The gate exits **0** on both recorded runs with the four non-`block` buckets at 4/6/10/12. A moved `block` count is not by itself a failure; it is explained by naming the needle inside the new block.
    - The four blocks carry, respectively: the three wire-field facts with firmware `file:line` citations plus the recorded-not-clamped statement naming 999.31; **two** throughput rows with the over-program value read from source, the source's contradiction comment cited, the max-pulses cell as a third defect, and the conditional prose affirmed; the hundred-seconds-versus-per-location fact with its record cited and the already-corrected-in-place note; and OD-B's attribution with the historical figure named as not a control.
    - No block reproduces a forbidden phrase (D-14); no `CLOSE-*` checkbox is ticked; all five coverage rows read `Pending`; `.planning/STATE.md` and the `firestarter` working tree are clean.
  </acceptance_criteria>
  <done>Four recognised blocks are appended in the project record and one in the requirements record, all after their subjects; the non-finding is confirmed by re-measurement; the deferral note reads as discharged; the gate exits 0 with its non-block buckets unmoved.</done>
</task>
<task type="auto">
  <name>Task 3: Write 146-CORRECTIONS.md — the consolidated register, including the three divergences</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CORRECTIONS.md</files>
  <read_first>
    - `146-PATTERNS.md` §6 — the row shape, and that the citation column is the answer to the self-reference trap. Then `146-check-claims.py` `_CAVEAT_RULES`: confirm this basename maps to the **empty** set, so no program-VCC paragraph is pasted in to satisfy a rule this file is exempt from (D-11).
    - `146-RESEARCH.md` §"Correction-site summary table" and §"`firestarter_app/README.md` — and two defects beyond the missing flag" — the measured verdicts and the two adjacency findings. Then `firestarter_app/firestarter/cli_handlers.py:546-610` for the shipped `write` option surface the adjacency rows cite.
  </read_first>
  <action>
Write `146-CORRECTIONS.md`. Layout is discretionary; the fields are not. Use a table whose first column is a row id in the form `C-1`, `C-2`, … **at line start**, then origin finding id, owning `file:line`, the false text **cited never quoted**, the corrected text stated positively, and the owning plan — three corrections land in other plans and the register is where a reviewer reads that off. **Rows, at minimum:** the roadmap dependency prose and the sequencing spine (both landed at `8df5e564`, marked so); the wire-field parse; the energy-cap ceiling figure, cited *by location* to the phase context carrying the larger figure and deliberately not restating the numeral; the throughput table as **two** rows plus the third conflated cell; the energy-cap datasheet justification with all its sites including the public one `146-12` posts; the debug-message wording and the orphaned message id (`146-07`); the stale native-env numerals (`146-06`); OD-B's comparative claim; and the record-gate sentence that asserted the ARM toolchain absent, which `146-03` measured false.

**Four sections beyond the table.** (1) *Divergences* — the three findings, each with the command or `file:line` establishing it: the project half of the first correction has no false-statement site, so it was recorded rather than manufactured, and what the file carries instead; the throughput correction spans **two** rows and the shipped source names the contradiction itself; the energy-cap justification was **already corrected in place** in the firmware protocol document by the parameter-table phase. State the rule applied: where two readings exist, both are stated. (2) *Record gate* — the Phase 130 RED at `.planning/STATE.md:11` is closed; give Task 1's measured causation, note that `146-04-SUMMARY.md` reports it still RED, and state that this was **accurate at every 146-04 commit** — the RED closed at `91a06604`, an interrupted `146-05` commit — so it is not an under-claim; the divergence on record is the replan brief's §4, which credited 146-04. (3) *Block versus history* — the rule and its rationale (live prose gets a block because that is what warns the next milestone's scoping pass *in situ*; dated records get a row only, because history is not edited), then a list by `file:line` of which sites got a block and which a row only, with the dated footers and the decision-log entry in the row-only list. (4) *Adjacency* — the host README documents the blank-check option under a long name the shipped surface does not use and claims it also skips erase, which a separate option now owns and which carries its own warning; both sit in the exact lines the pulse-override entry inserts into, so both are in scope by adjacency and land in `146-07`. One row each, shipped surface cited to `cli_handlers.py:NNN`, plus why a documentation correction is inside the wording-only boundary.

Close with three explicit non-claims: this register does not fix the unclamped wire field; it edits no archived milestone document; and it is **not** the honesty ledger. Obey D-14 mechanically — no cell reproduces a phrase the claim gate forbids; where the false text contains one, the cell reads as a citation of the named clause at `path:line`. Run the claim gate against this file alone in positional-argument mode; expect exit 0. Prove the locator can fail: copy the file to a scratch path outside the phase directory, append one probed forbidden phrase, run the gate against the copy, confirm non-zero naming the label, discard the copy. Commit only this file.
  </action>
  <verify>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-claims.py 146-CORRECTIONS.md > /tmp/gsd146/gate_corr.txt 2>&1; rc=$?; echo "claim_gate_rc=$rc"; cat /tmp/gsd146/gate_corr.txt; test "$rc" = "0"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -cE '^\| *C-[0-9]' 146-CORRECTIONS.md); echo "register_rows=$n"; test "$n" -ge 9 && for tok in F-140-05 F-140-07 F-141-07 F-144-01 999.31; do grep -qF "$tok" 146-CORRECTIONS.md || { echo "MISSING $tok"; exit 1; }; done; echo "origin_ids_present=1"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -oE '[A-Za-z0-9_./-]+\.(md|cpp|c|h|py|toml):[0-9]+' 146-CORRECTIONS.md | wc -l); echo "file_line_citations=$n"; test "$n" -ge 12</automated>
  </verify>
  <acceptance_criteria>
    - The claim gate against `146-CORRECTIONS.md` alone exits **0** with a `PASS:` line and no missing-caveat complaint — reachable because `_CAVEAT_RULES` maps this basename to the empty set. The negative control on a scratch copy exited non-zero naming the planted label, and the copy was discarded.
    - `grep -cE '^\| *C-[0-9]'` returns **at least 9**, every row has all four D-05 fields populated plus its owning plan, the five origin/backlog tokens are present, and `file:line` citations number **at least 12**.
    - The divergences, record-gate, block-versus-history, adjacency and non-claims sections all exist; the block-versus-history list names sites by `file:line`; the record-gate section states both readings.
    - No cell reproduces a forbidden phrase, no program-VCC paragraph was added to satisfy a rule this file is exempt from, and `grep -c 'CORRECTION (Phase 146'` still returns 4 / 1 / 2 over `PROJECT.md` / `REQUIREMENTS.md` / `ROADMAP.md` — this task added no block.
  </acceptance_criteria>
  <done>The register exists, passes its own claim gate under D-11's exemption, carries at least nine populated rows, records the three divergences and the record-gate flip as findings, makes the block-versus-history choice auditable by `file:line`, and carries the adjacency findings and three non-claims.</done>
</task>
</tasks>
<verification>Two ROADMAP blocks verified landed and untouched; four PROJECT blocks and one REQUIREMENTS block appended after their subjects; record gate rc=0 on every run with the four non-`block` buckets at 4/6/10/12; `146-CORRECTIONS.md` committed and green under the claim gate with its locator shown able to fail; `.planning/STATE.md` untouched; neither sub-repo touched; no `CLOSE-*` checkbox or coverage row moved.</verification>
<success_criteria>The live planning record no longer asserts anything its own measurements disprove, every correction is readable in one place with its origin and owning plan, and the three places where the inherited correction list was itself wrong — plus the record-gate flip and the replan brief's §4 misattribution — are recorded as findings.</success_criteria>
<output>Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-05-SUMMARY.md`. Record every gate run with its tally, each block's opener line against its subject's line, the re-measured non-finding with its command, and the measured causation of the record-gate flip.</output>
