---
phase: 146-close-honesty-ledger-claim-gate-gh-15-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
commits_land_in: meta
reads_repos: [firestarter, firestarter_app]
files_modified:
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-close03-docs.py
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md
  - .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-02-SUMMARY.md
autonomous: true
requirements: [CLOSE-03]

must_haves:
  truths:
    - "Per D-13 a SECOND, differently-shaped phase-local script checks the CLOSE-03 sub-repo docs: five required topics plus zero forbidden phrases, and no blanket caveat rule anywhere in it"
    - "The checker is hosted in the phase directory, not in either sub-repo, so nothing about it is conditional on firmware presence — the recorded failure mode is that host-side gates scanning firmware source fail OPEN"
    - "The checker fails closed on a missing or renamed doc and exits non-zero on an emptied or repointed target list; it has no path that exits 0 having scanned nothing"
    - "The checker is seen RED for a NAMED reason before any doc is edited — two of the five topics are measured absent from every target today, so the RED needs no plant"
    - "The two content locators that are runnable today are recorded RED before the edit, which is the cheapest available proof that each locator is wired to the file it names"
  artifacts:
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-close03-docs.py"
      provides: "The D-13 five-topic-plus-forbidden-phrase checker over the four CLOSE-03 documentation targets, with a repo-root walk, a shape-asserting self-check, a never-vacuous guard and a fail-closed missing-target branch"
      contains: "_REQUIRED_TOPICS_BY_FILE"
    - path: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md"
      provides: "The pre-edit RED transcript per target file per topic, the two runnable-today locator REDs, the three non-vacuity legs, and the out-of-target-set findings recorded as decisions rather than omissions"
      contains: "RED (pre-edit)"
  key_links:
    - from: ".planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py"
      to: ".planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-close03-docs.py"
      via: "the repo-root upward walk, which is the only in-tree idiom for a phase-local checker whose targets live outside its own directory"
      pattern: "_find_repo_root"
  prohibitions:
    - "MUST NOT place this checker inside firestarter/ or firestarter_app/ — a host-side gate that scans firmware source breaks on renames and fails OPEN, which happened four times in Phase 117"
    - "MUST NOT add a required-caveat rule of any kind (D-13) — the sub-repo docs must not inherit a rule written for a release body"
    - "MUST NOT reuse the claim gate's env-seam name; this script gets its own distinct seam"
    - "MUST NOT resolve targets by walking upward with a fixed number of parent hops or by any wildcard — walk to the first ancestor containing a .planning directory and raise rather than fall back silently"
    - "MUST NOT drop or weaken the self-check: substituting a shape assertion for the prefix assertion is required, removing the self-check leg entirely is the fail-open shape"
    - "MUST NOT edit any documentation file in this plan — this plan writes the checker and records its RED; the edits are plans 146-06 and 146-07"
    - "MUST NOT tick CLOSE-03 or any other CLOSE requirement — only plan 146-13 may tick CLOSE-01 through CLOSE-05"
    - "MUST NOT run git push, git merge, git tag, gh workflow run, gh release, gh issue edit or gh issue close (D-01)"
    - "MUST NOT create, edit or delete any file under firestarter/ or firestarter_app/ (D-06)"
---

<objective>
Author the D-13 documentation checker and record it RED, per target and per topic, before any documentation is
edited.

Purpose: CLOSE-03 promises five topics in the firmware and host documentation. D-13 turns that prose promise
into a machine-checkable list, deliberately as a **second** script rather than a mode of the claim gate,
because D-11 rejected forcing a release-body caveat rule into a public README. The RED-first ordering is not
ceremony: this project has recorded that a pre-authored gate leg can be unreachable, and Phase 145 found three
acceptance locators that were false GREENs — one passing against a record with no content in it. Two of
CLOSE-03's five topics are measured absent from **every** target today, so this checker is a true RED before
the edits and a GREEN after, with no plant needed.

Output: `146-check-close03-docs.py`, and `146-DOC-CHECK-RECORD.md` carrying the pre-edit RED transcript.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-CONTEXT.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md
@.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-VALIDATION.md
</context>

<artifacts_this_phase_produces>
The full phase inventory is repeated in every plan so the source-grounding pass can exclude every newly
created symbol from drift verification. **Every path and name below is new in Phase 146.**

**New scripts (meta repo, this phase directory):** `146-check-claims.py` (`_HERE`, `_DEFAULT_TARGETS`,
`FORBIDDEN_PATTERNS`, `REQUIRED_CAVEAT_PATTERNS`, `_ALL_CAVEAT_LABELS`, `_CAVEAT_RULES`,
`FIRESTARTER_CLAIMSCAN_TARGETS_146`, `_assert_default_targets_are_local()`, `_required_caveats_for()`,
`resolve_targets()`, `scan_text()`, `_print_bucket()`, `main()`); `146-check-close03-docs.py`
(`_find_repo_root()`, `_REPO_ROOT`, `_DEFAULT_TARGETS`, `_DOC_TARGET_ALLOWLIST`,
`_REQUIRED_TOPICS_BY_FILE`, `REQUIRED_TOPIC_PATTERNS`, `_required_topics_for()`,
`_assert_default_targets_are_allowlisted()`, `resolve_targets()`, `scan_text()`, `main()`,
`FIRESTARTER_DOCSCAN_TARGETS_146`); `test_check_claims_v131.py` (`_HERE`, `_SCANNER`, `_run_scanner()`,
`_import_scanner_module()` and thirteen `test_*` legs).

**New fixtures:** `fixtures/clean_control.md`, `fixtures/clean_control_second.md`,
`fixtures/planted_forbidden_claim.md`, `fixtures/planted_proven_unqualified.md`,
`fixtures/planted_missing_caveat.md`

**New `.planning` artifacts:** `146-LEDGER.md`, `146-CORRECTIONS.md`, `146-GH15-RECONCILIATION.md`,
`146-RELEASE-NOTES-fw.md`, `146-RELEASE-NOTES-app.md`, `146-CITATIONS.md`, `146-ARM-BUILD-RECORD.md`,
`146-DOC-CHECK-RECORD.md`, `arm-build/`, and `146-0N-PLAN.md` / `146-0N-SUMMARY.md`

**New doc sections in existing files:** `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 replacement write-algorithm
text plus a pulse-override sub-item and a program-VCC-ceiling note; `firestarter/CLAUDE.md` a
`**⚠ CORRECTION (Phase 146 / CLOSE-04, origin F-144-01)**` block and a program-VCC-ceiling sentence;
`firestarter/README.md` a 27C paragraph under §Protocol Notes; `firestarter_app/README.md` four added
`write` option entries and a program-VCC-ceiling note; `⚠ CORRECTION` blocks in `.planning/ROADMAP.md`,
`.planning/PROJECT.md` and `.planning/REQUIREMENTS.md`

**New message text:** a replacement `format` string for `DBG_PULSE_DELAY_MISMATCH` in
`tools/catalog/messages.toml` (three copies) and its regenerated line in
`firestarter_app/firestarter/messages.py`

**New env-var names:** `FIRESTARTER_CLAIMSCAN_TARGETS_146`, `FIRESTARTER_DOCSCAN_TARGETS_146`

No new CLI flag is introduced — `--pulse-us` already ships from Phase 143.
</artifacts_this_phase_produces>

<!-- The literals below are negative-greped by this plan's acceptance criteria over sub-repo
     documentation and over 146-check-close03-docs.py. They appear in task actions only as the
     commands that measure them and as instructions about what must be removed or must not be
     introduced; this plan writes none of them into any scanned file. -->
<!-- planner-discipline-allow: Phase 141 replaces it -->
<!-- planner-discipline-allow: eprom.cpp:159-179 -->
<!-- planner-discipline-allow: 71 cases -->
<!-- planner-discipline-allow: proven -->
<!-- planner-discipline-allow: caveat -->

<tasks>

<task type="auto">
  <name>Task 1: Author 146-check-close03-docs.py over the four CLOSE-03 documentation targets</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-check-close03-docs.py</files>
  <read_first>
    - `.planning/phases/139-gh-15-correction-outward/139-check-claims.py` — the whole skeleton this script reuses: the module docstring's exit-code contract, `_HERE`, the startup self-check (`:148-181`), `resolve_targets()` (`:184-202`), the hoisted never-vacuous guard and the fail-closed missing-target branch (`:266-282`), `_print_bucket()` (`:241-246`), and `main()`'s order of operations. Transcribe the twelve `FORBIDDEN_PATTERNS` (`:98-128`) verbatim.
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py` `:120-172` — `_find_repo_root()`'s upward walk and the absolute-target-list shape. This is the only in-tree checker whose targets live outside its own phase directory, and its docstring states why it raises rather than falling back to its own directory.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-PATTERNS.md` §2 — the two-donor map, the four-target list, the four named mitigations for the fail-open pattern, and the instruction to replace the prefix assertion with a shape assertion.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RESEARCH.md` §"CLOSE-03 Documentation Targets" → "The five-topic × six-file matrix" — the measured per-file per-topic coverage this checker must reproduce, and the finding that topic 5 appears in no sub-repo document at all.
  </read_first>
  <action>
Write `146-check-close03-docs.py` next to this plan. Stdlib only.

**Target resolution.** Copy `_find_repo_root()` from the Phase 130 donor: walk upward from the script's own
directory and return the first ancestor containing a `.planning` subdirectory; raise `RuntimeError` if none
exists, never silently fall back to the script's own directory. Build `_DEFAULT_TARGETS` as four
`os.path.join(_REPO_ROOT, …)` entries, in this order: `firestarter/doc/PROTOCOLS.md`,
`firestarter/CLAUDE.md`, `firestarter/README.md`, `firestarter_app/README.md`. Do not use a fixed number of
`..` hops and do not use any wildcard.

**Why the script lives here, stated in the docstring.** This script scans firmware and host source-tree paths
from a phase-local location on purpose. The recorded pattern is that gates hosted inside `firestarter_app`
which scan `firestarter` source break on renames and fail **open** — four occurrences in Phase 117, and the
live instance is the app suite's firmware-presence gate, which `PROJECT.md:1181` disclosure 4 states fails
open across the repo boundary by design. Hosting the checker in the phase directory means nothing about it is
conditional on the other repository being present.

**The self-check.** Keep a startup self-check analogous to the claim gate's, running first in `main()` and
returning non-zero on failure. The claim gate's basename-prefix leg cannot apply here (the targets are
sub-repo docs), so **substitute** two legs rather than dropping one: every `_DEFAULT_TARGETS` entry must
resolve under `_REPO_ROOT`, and every entry must be a member of a literal four-element allowlist
`_DOC_TARGET_ALLOWLIST` declared in the module. A self-check with a leg removed and nothing substituted is
the fail-open shape this file exists to avoid; say so in the docstring.

**Forbidden phrases.** Transcribe the donor's twelve `(label, compiled_regex)` tuples verbatim, all
`re.IGNORECASE`, and apply them to **every** scanned file. No proximity window, no context condition, no
exclusion marker, no heading quarantine.

**Required topics, per file.** Declare `REQUIRED_TOPIC_PATTERNS` as five `(topic_id, prose, regex)` tuples:

| topic id | prose | regex |
|---|---|---|
| `per-byte-algorithm` | the shipped per-byte pulse-to-verify loop | matches the hyphenated, underscored and space-separated forms of "per byte", case-insensitive |
| `parameter-table` | the `protocol_id`-keyed parameter table | matches the table's source-file stem `eprom_params` |
| `database-supplied-pulse` | the pulse width as a database datum | matches the database field name in both its hyphenated and underscored spellings, case-insensitive |
| `pulse-override-flag` | the host's per-run pulse override | matches the literal long-option spelling of the `write` pulse-override flag |
| `program-vcc-ceiling` | the ~6.25 V program-VCC accepted debt | matches `6\.25\s*V` |

Then declare `_REQUIRED_TOPICS_BY_FILE`, keyed by the target's **repo-relative path** so the map cannot drift
from `_DEFAULT_TARGETS`' construction, with these sets — each chosen from what the document is for:

- `firestarter/doc/PROTOCOLS.md` → all five. It is the per-protocol reference a firmware developer reads and
  it is the document that went stale.
- `firestarter/CLAUDE.md` → all five. It is the living agent-facing reference and already carries four.
- `firestarter/README.md` → `per-byte-algorithm` and `program-vcc-ceiling`. It is the user-facing firmware
  surface; a parameter-table mention there would be noise.
- `firestarter_app/README.md` → `database-supplied-pulse`, `pulse-override-flag`, `program-vcc-ceiling`. It is
  where a CLI user meets the flag.

Add `_required_topics_for(path)` returning the mapped set, and — mirroring the claim gate's caveat rule — an
**unknown path returns the FULL five-topic set**, so an unrecognised or renamed target fails closed rather
than being waved through. The union of the four sets is all five topics, which is what CLOSE-03 asks for; add
an assertion in the self-check that the union equals the full topic set, so a future edit cannot quietly drop
a topic from every file at once. Declare **no** required-caveat rule of any kind (D-13).

**Order of operations in `main()`, copied from the donor:** self-check → `resolve_targets` (argv → the env
seam `FIRESTARTER_DOCSCAN_TARGETS_146`, read with `os.environ.get` and no default so `None` differs from `""`
→ defaults) → hoisted never-vacuous guard (empty target list is a hard failure naming that it cannot pass with
nothing scanned) → fail-closed missing-target branch (any absent path is a hard failure naming it) → scan →
bucketed report. The report must name **every scanned file** on the `PASS:` line, and on failure must name,
per file, each unsatisfied topic id and each forbidden-phrase hit with its line number. There must be no path
that exits 0 having scanned nothing.

**The exit-code contract in the docstring:** 0 only when every resolved target exists, carries zero
forbidden-phrase matches, and satisfies every topic in its own required set; non-zero in every other case,
including a self-check failure, an empty target list and a missing file. State as an explicit non-claim that a
green run means the five topics are **present**, not that the prose is correct — that judgement is the
blocking operator wording review in plan 146-12.

Commit only `146-check-close03-docs.py`. Use explicit `git add` with that path; other Wave 1 plans are running
concurrently in this repository.
  </action>
  <verify>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "import ast; ast.parse(open('146-check-close03-docs.py',encoding='utf-8').read()); print('parses')"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 -c "
import importlib.util, os
s=importlib.util.spec_from_file_location('d','146-check-close03-docs.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)
assert os.path.isdir(os.path.join(m._REPO_ROOT,'.planning')), m._REPO_ROOT
assert len(m._DEFAULT_TARGETS)==4, m._DEFAULT_TARGETS
assert len(m._DOC_TARGET_ALLOWLIST)==4
assert len(m.REQUIRED_TOPIC_PATTERNS)==5
assert len(m.FORBIDDEN_PATTERNS)==12
for t in m._DEFAULT_TARGETS:
    assert os.path.abspath(t).startswith(os.path.abspath(m._REPO_ROOT)+os.sep), t
union=set()
for v in m._REQUIRED_TOPICS_BY_FILE.values(): union |= set(v)
assert len(union)==5, union
full=set(tid for tid,_p,_r in m.REQUIRED_TOPIC_PATTERNS)
assert union==full, (union, full)
assert set(m._required_topics_for('some/unknown/doc.md'))==full
print('introspection OK: 4 targets under repo root, 5 topics, 12 patterns, union complete, unknown fails closed')
"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && FIRESTARTER_DOCSCAN_TARGETS_146="" python3 146-check-close03-docs.py > /tmp/gsd146/doc_empty.txt 2>&1; rc=$?; echo "rc=$rc"; test "$rc" != "0" && ! grep -q 'PASS:' /tmp/gsd146/doc_empty.txt && echo "never-vacuous holds"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && FIRESTARTER_DOCSCAN_TARGETS_146="/workspaces/firestarter/doc/NO-SUCH-DOC.md" python3 146-check-close03-docs.py > /tmp/gsd146/doc_missing.txt 2>&1; rc=$?; echo "rc=$rc"; test "$rc" != "0" && grep -qi 'not found' /tmp/gsd146/doc_missing.txt && echo "fail-closed on missing target"</automated>
    <automated>cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && n=$(grep -ciE 'caveat' 146-check-close03-docs.py); echo "caveat_mentions=$n"</automated>
    <automated>cd /workspaces && test "$(git -C firestarter status --porcelain | wc -l)" = "0" && echo "firmware repo untouched"</automated>
  </verify>
  <acceptance_criteria>
    - The file parses as Python 3 and imports by path with no side effect beyond constant construction.
    - Introspection asserts, in one command: `_REPO_ROOT` contains a `.planning` directory; `len(_DEFAULT_TARGETS) == 4`; `len(_DOC_TARGET_ALLOWLIST) == 4`; every target's absolute path is under `_REPO_ROOT`; `len(REQUIRED_TOPIC_PATTERNS) == 5`; `len(FORBIDDEN_PATTERNS) == 12`; the union of `_REQUIRED_TOPICS_BY_FILE`'s values equals the full five-topic id set; `_required_topics_for()` on an unrecognised path returns the full set.
    - With the env seam set to the empty string the script exits **non-zero** and stdout contains no `PASS:` line. Negative control: with the seam pointing at one readable target the script produces a report rather than the never-vacuous message.
    - With the env seam pointing at a path that does not exist the script exits **non-zero** and stdout names that path as not found.
    - `grep -ciE 'caveat'` over the file returns **0** — D-13 forbids a caveat rule here. If the count is non-zero the task has drifted into D-11's design.
    - The docstring states the exit-code contract, states why the checker is hosted in the phase directory rather than in either sub-repo, names the fail-open pattern it avoids, and carries the explicit non-claim that presence is not correctness.
    - `git -C firestarter status --porcelain | wc -l` returns **0** — no documentation was edited in this plan.
  </acceptance_criteria>
  <done>`146-check-close03-docs.py` exists, resolves four allowlisted documentation targets under a walked repo root, applies the twelve forbidden patterns to all of them and a per-file required-topic set whose union is all five topics, fails closed on a missing or unknown target, cannot pass with nothing scanned, and declares no caveat rule.</done>
</task>

<task type="auto">
  <name>Task 2: Record the pre-edit RED, per file and per topic, plus the two runnable-today locator REDs</name>
  <files>.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md</files>
  <read_first>
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-VALIDATION.md` §"Per-Task Verification Map" — the two CLOSE-03 rows marked *runnable today — true RED*, with their exact commands and expected counts. This plan records the RED half; plan 146-06 records the GREEN half.
    - `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-RESEARCH.md` §"Common Pitfalls" → Pitfall 4 and Pitfall 5 — the unreachable-leg trap (three recorded observations per leg, not one) and the reason a status must be captured immediately rather than after a pipe.
    - `firestarter/doc/PROTOCOLS.md` §§1.3-1.5 (lines 129-235) and `firestarter/CLAUDE.md` — the two files whose stale content the runnable-today locators point at. Read them so the recorded RED names what is actually there rather than what a document says is there.
    - `firestarter_app/CLAUDE.md` around line 46, and `firestarter_app/doc/protocol-id.md` / `protocol-flags.md` — the candidate documents that are deliberately **not** in the target set, so the omission is recorded as a decision.
  </read_first>
  <action>
Create `146-DOC-CHECK-RECORD.md`. It has four sections, all populated by running commands and pasting what
they printed — no figure is copied from RESEARCH.

**§1 — `RED (pre-edit)`: the checker's own failure, per file per topic.** Run
`python3 146-check-close03-docs.py` with no argv and no env, capture its exit status immediately (`rc=$?`,
never after a pipe), and paste the full stdout. Then present a matrix table with one row per target and one
column per topic id, marking each cell satisfied or unsatisfied **from the checker's own report**, not from a
separate grep. The measured expectation from research is that all four targets fail and that the
program-VCC-ceiling topic is unsatisfied in **every** one of them; if what you observe differs, record what
you observed and flag the divergence rather than reconciling it.

**§2 — the two runnable-today content locators, RED.** Run each of the four commands below in
`/workspaces`, record the command and its integer output, and label each as the RED half of a
before/after pair that plan 146-06 completes:

- `grep -c 'Phase 141 replaces it' firestarter/doc/PROTOCOLS.md` — expected **1** now, required **0** after
- `grep -c 'eprom.cpp:159-179' firestarter/doc/PROTOCOLS.md` — expected **1** now, required **0** after
- `grep -c '71 cases' firestarter/CLAUDE.md` — expected **1** now, required **0** after
- `grep -c '79 cases' firestarter/CLAUDE.md` — expected **0** now, required **at least 1** after

State in prose why this pair is worth recording: a locator that has only ever been seen green proves nothing
about the locator, and these four are the cheapest available proof that each one is wired to the file it
names. Also record the count of the bare unqualified claim word forbidden by the donor's pattern 10 in
`firestarter/CLAUDE.md`, measured as **occurrences** not lines —
`grep -oiE '\bproven\b' firestarter/CLAUDE.md | wc -l`, expected 4 now, required 0 after plan 146-06 — and
cite the four sites by location only, as `firestarter/CLAUDE.md:64` (one occurrence), `:65` (two) and `:66`
(one), without reproducing the sentences. Note that `grep -c` would report 3 here because it counts lines, and
that the occurrence count is the one the checker's behaviour follows.

**§3 — the three non-vacuity legs.** Record each with its command, its captured exit status and the relevant
stdout line: the emptied env seam, a repointed seam naming a path that does not exist, and a seam pointing at
a single readable target (the positive control that proves the first two failures were about vacuity and
absence rather than about the seam being unusable).

**§4 — out-of-target-set findings, recorded as decisions.** Two candidate documents were read and are
deliberately not scanned, and saying so is what makes the omission read as a decision rather than an
oversight. Record, by `file:line` and label only: `firestarter_app/CLAUDE.md:46` carries one hit of the
donor's `verified-on-silicon` pattern, and it is not a CLOSE-03 target — CLOSE-03 asks about the algorithm,
the table, the pulse, the override flag and the ceiling, none of which that file is the home for; and
`firestarter_app/doc/protocol-id.md` and `protocol-flags.md` measure zero forbidden hits and are the host-side
protocol reference pair, left out because the host half of CLOSE-03 lands in the README a user actually reads.
Record both as **found, out of scope for this phase, unedited** — not as passes.

Close the record with a one-line statement that plan 146-06 appends §5 (the firmware-doc GREEN) and plan
146-07 appends §6 (the host-doc GREEN and the full checker GREEN), and that this document is not a claim-gate
target.

Commit only `146-DOC-CHECK-RECORD.md`.
  </action>
  <verify>
    <automated>cd /workspaces && grep -qF 'RED (pre-edit)' .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md && echo "RED section present"</automated>
    <automated>cd /workspaces && test "$(grep -c 'Phase 141 replaces it' firestarter/doc/PROTOCOLS.md)" = "1" && test "$(grep -c 'eprom.cpp:159-179' firestarter/doc/PROTOCOLS.md)" = "1" && echo "PROTOCOLS locators still RED as recorded"</automated>
    <automated>cd /workspaces && test "$(grep -c '71 cases' firestarter/CLAUDE.md)" = "1" && test "$(grep -c '79 cases' firestarter/CLAUDE.md)" = "0" && echo "CLAUDE.md locators still RED as recorded"</automated>
    <automated>cd /workspaces && test "$(grep -oiE '\bproven\b' firestarter/CLAUDE.md | wc -l)" = "4" && echo "claim-word occurrence baseline 4 confirmed"</automated>
    <automated>mkdir -p /tmp/gsd146 && cd /workspaces/.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation && python3 146-check-close03-docs.py > /tmp/gsd146/doc_pre.txt 2>&1; rc=$?; echo "rc=$rc"; test "$rc" != "0" && grep -qi 'program-vcc-ceiling' /tmp/gsd146/doc_pre.txt && echo "RED for the named topic"</automated>
    <automated>cd /workspaces && for f in firestarter_app/CLAUDE.md 'protocol-id.md'; do grep -qF "$f" .planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-DOC-CHECK-RECORD.md || { echo "MISSING out-of-scope record for $f"; exit 1; }; done; echo "out-of-target-set findings recorded"</automated>
  </verify>
  <acceptance_criteria>
    - `146-DOC-CHECK-RECORD.md` contains the literal heading text `RED (pre-edit)` and a per-file per-topic matrix whose cells are transcribed from the checker's own report.
    - The checker's pre-edit run exits **non-zero** and its stdout names the `program-vcc-ceiling` topic as unsatisfied for all four targets. This is the RED-for-a-named-reason observation; a non-zero exit whose message is a traceback, an import error or a missing-file error instead is a task failure.
    - All four runnable-today locator counts are recorded with the command that produced them and match: `Phase 141 replaces it` → **1**, `eprom.cpp:159-179` → **1**, `71 cases` → **1**, `79 cases` → **0**.
    - The occurrence count of the donor's pattern-10 claim word in `firestarter/CLAUDE.md` is recorded as **4** using the occurrence-counting form, with a note that the line-counting form reports 3, and the four sites cited by `file:line` only.
    - §3 records three legs: emptied seam → non-zero with no `PASS:`; seam at a nonexistent path → non-zero naming it; seam at one readable target → a report rather than either failure message. The third leg is the positive control and its absence fails the task.
    - §4 names `firestarter_app/CLAUDE.md:46` with its pattern label and both `firestarter_app/doc/protocol-*.md` files, each marked found-and-unedited rather than passing.
    - Every recorded exit status was captured immediately after its command, never after a pipe; the record states this.
    - `git -C firestarter status --porcelain | wc -l` returns **0** and `git -C firestarter_app status --porcelain | wc -l` equals the baseline recorded in `146-CITATIONS.md` §0.
  </acceptance_criteria>
  <done>`146-DOC-CHECK-RECORD.md` carries a per-file per-topic RED matrix taken from the checker's own report, four recorded locator REDs with their commands, three non-vacuity legs including a positive control, and two out-of-target-set findings recorded as decisions.</done>
</task>

</tasks>

<verification>
- `146-check-close03-docs.py` committed; introspection green; both vacuity guards seen to fire.
- `146-DOC-CHECK-RECORD.md` committed with the pre-edit RED transcript.
- Neither sub-repo was modified: `git -C firestarter status --porcelain` returns 0 lines.
- No `CLOSE-*` checkbox or coverage row moved.
</verification>

<success_criteria>
CLOSE-03's five topics are a machine-checkable list before any documentation is edited, and the checker plus
its four content locators have each been seen to fail for a reason stated by name. Nothing in this plan claims
any topic is present.
</success_criteria>

<output>
Create `.planning/phases/146-close-honesty-ledger-claim-gate-gh-15-reconciliation/146-02-SUMMARY.md` when
done. Record the pre-edit matrix, the four locator counts and the three non-vacuity legs verbatim, and state
which later plan owns each GREEN half.
</output>
