---
phase: 141-per-byte-program-loop
plan: 03
type: execute
wave: 1
depends_on: []
commits_land_in: [firestarter]
files_modified:
  - /workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp
  - /workspaces/firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp
  - /workspaces/firestarter/platformio.ini
  - /workspaces/firestarter/CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A sixth native PlatformIO env compiles and runs a new suite by name, absent from default_envs and from both pinned envs"
    - "The suite records every register strobe through production's real cache-elision path"
    - "The suite records the argument of every mocked delay and delayMicroseconds call, in order, with strobe-interleave position"
    - "The suite models chip read-back per 16-bit latched address so a per-byte pulse count is observable without a strobe-stream cap"
    - "The suite captures every logged message id and its packed parameters"
    - "native and native_nodevtools still report exactly 141 cases across 17 suites"
  artifacts:
    - path: "/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp"
      provides: "three recorder layers, the 16-bit-keyed read-back model, and logged-id capture"
      contains: "HOST_STUBS_RECORD_TIMING"
      min_lines: 120
    - path: "/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp"
      provides: "setUp hooks, six harness non-vacuity cases, explicit RUN_TEST main"
      contains: "UNITY_BEGIN"
      min_lines: 150
    - path: "/workspaces/firestarter/platformio.ini"
      provides: "[env:native_loop_v131] with its HARD CONSTRAINT comment block"
      contains: "native_loop_v131"
  key_links:
    - from: "/workspaces/firestarter/platformio.ini"
      to: "/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/"
      via: "test_filter allowlist plus a matching -I entry"
      pattern: "native/avr/test_loop_eprom_v131"
    - from: "/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp"
      to: "/workspaces/firestarter/include/rurp_register_utils.h"
      via: "include AFTER the shared .inc so production's real elision runs"
      pattern: "#include \"rurp_register_utils.h\""
---

<objective>
Build the oracle this phase verifies itself with: a **sixth** native PlatformIO env plus a new suite
directory whose `host_stubs.cpp` composes all three recorder layers, models chip read-back per
16-bit latched address, and captures every logged message id.

Per **D-10** the pre-change `native_trace_v131` fixture goes RED in this phase and is deliberately not
re-frozen here, so it cannot verify the new loop. Phase 141 therefore authors its own suite, on the
`[env:native_params_v131]` precedent: it names only its own suite in `test_filter`, is never folded
into `native` or `native_nodevtools` (both pinned at exactly **141 cases / 17 suites** by the live
`scripts/baseline/size_baseline.json`), is never in `default_envs`, and is never passed to
`check_size_baseline.py` (unknown env raises an uncaught `KeyError`, exit 1 — F-138-05) or to
`check_build_warnings.py` (exit 2). It runs in **no CI leg of either repository** — a run-by-name
obligation recorded in the phase record, never implied as CI coverage.

CONTEXT.md's "two known blind spots in the native stubs" are **stale**, and this plan depends on them
being closed: `HOST_STUBS_RECORD_TIMING` (Phase 138) records every `delay` / `delayMicroseconds`
**argument** with strobe-interleave position, and ArduinoFake declares
`virtual void delayMicroseconds(unsigned int)` — 32-bit on a 64-bit Linux host — so an over-ceiling
value arrives **intact and visible** rather than truncated the way AVR's 16-bit `unsigned int` would
truncate it. `HOST_STUBS_REAL_REGISTER_UTILS` (Phase 116) closes the register-write-elision blind
spot. Build for the stronger oracle.

Purpose: every later plan's assertions rest on this harness, so its contract must be fixed and proven
before the loop rewrite lands.
Output: two new test files, one new env, one extended CLAUDE.md exception paragraph.

**Requirement ownership:** this plan **completes NO requirement.** It builds the oracle only. Do NOT
flip any `LOOP-*` checkbox in `.planning/REQUIREMENTS.md`. Every case added here is harness
self-verification and is deliberately **loop-independent**, so it stays true both before and after
plan 141-04's rewrite.

**Frontmatter `requirements:` is deliberately EMPTY — do not repopulate it.** `execute-plan.md`'s
`update_requirements` step calls the `requirements.mark-complete` verb unconditionally with *this*
plan's own frontmatter IDs, and that verb performs a **real** regex flip of both the `[ ]` checkbox
and the `Pending` coverage-table cell — it is not a log-only call, and unlike its sibling steps it
carries no documented skip for parallel or worktree execution. Partial IDs in frontmatter would mark
seven of the eight `LOOP-*` IDs `Complete` in wave 1, before the evidence exists (the exact failure mode this project hit
four times in Phase 116), and would also break plan 141-09's sixteen-line diff self-check. An empty
list makes `update_requirements`'s own "if no requirements field, skip" condition apply. Dimension-1
requirement coverage is carried by plan **141-09**, which declares all eight `LOOP-*` IDs and flips
them in one hand edit after every piece of evidence exists.

**Repo notes:** firmware submodule only. Commit inside `/workspaces/firestarter` on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-per-byte-program-loop/141-CONTEXT.md
@.planning/phases/141-per-byte-program-loop/141-RESEARCH.md
@.planning/phases/141-per-byte-program-loop/141-PATTERNS.md
@.planning/phases/141-per-byte-program-loop/141-VALIDATION.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author host_stubs.cpp — three recorder layers, a 16-bit-keyed read-back model, logged-id capture</name>
  <files>/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp</files>
  <read_first>
    - /workspaces/firestarter/test/native/avr/test_trace_eprom_v131/host_stubs.cpp — all 149 lines.
      The structural analog: guard composition at :52-78, the `reset_register_cache` seam at :89-93,
      the byte-index derivation comment at :104-121, the `trace_readback_*` model at :122-149. Do NOT
      use `test_eprom_params_v131/host_stubs.cpp` (39 lines) as the analog — it is the pure
      pass-through variant and lacks every recorder this suite needs.
    - /workspaces/firestarter/test/native/avr/_shared/host_stubs_common.inc — all 353 lines. Extract in
      one pass: the `#error` fail-closed guard at :92-94; the strobe read side (`clear_strobes`,
      `strobe_count`, `strobe_overflowed`, `strobe_kind`, `strobe_pin`, `strobe_value`) with
      `STROBE_KIND_DATA = 1` / `STROBE_KIND_PIN = 2`; the timing read side (`clear_timings`,
      `timing_count`, `timing_overflowed`, `timing_kind`, `timing_us`, `timing_after_strobe`),
      `timing_push`, `TIMING_KIND_DELAY_US = 3` / `TIMING_KIND_DELAY_MS = 4`; both caps at **512**; the
      two hook definitions (`rurp_write_data_buffer`, `rurp_set_control_pin`); and the
      `HOST_STUBS_CUSTOM_READ_DATA_BUFFER` opt-out.
    - /workspaces/firestarter/include/rurp_register_utils.h — the CONTROL cache-elision at **:39-42**,
      the `CTRL_VPP_P1_ENABLE` set-to-clear settle at **:57-59**, and `rurp_read_from_register` at
      **:91-100** (returns the cached logical value, not a hardware read-back). The three globals
      `lsb_address`, `msb_address`, `control_register` initialise to `0xff` at :12-14.
    - /workspaces/firestarter/src/boards/rurp_serial_utils.cpp lines 480-540 — `rurp_log_id` is
      `__attribute__((weak))` and every `rurp_log_id_u8` / `_u16` / `_u24` / `_u32` routes through it,
      so one strong definition here captures all of them.
  </read_first>
  <action>
    Create `test/native/avr/test_loop_eprom_v131/` and write `host_stubs.cpp`, opening with the project
    header block (MIT notice, then a line reading
    `Phase 141 Plan 03 (LOOP-01..LOOP-08, D-10) -- host stubs for the per-byte program loop suite`).

    Guard composition, in this exact order, all **before** the `#include` of the shared `.inc` — every
    guard is read at include time and a `#define` after the include silently does nothing:
    `HOST_STUBS_REAL_REGISTER_UTILS`, then `HOST_STUBS_RECORD_TIMING` (the `.inc` has a fail-closed
    `#error` if this appears without the first, because the timing recorder's sequence key is
    `s_strobe_count`), then `HOST_STUBS_CUSTOM_READ_DATA_BUFFER`. Then
    `#include "../_shared/host_stubs_common.inc"`, and only **after** that
    `#include "rurp_register_utils.h"` so production's real cache-compare elision and latch-strobe
    sequencing run rather than a replica.

    Do NOT also define `HOST_STUBS_CUSTOM_HW_REVISION`. `HOST_STUBS_REAL_REGISTER_UTILS` already
    defines `HOST_STUBS_CUSTOM_HW_REVISION_BLOCK`, so the hardware-revision stubs come from the real
    `rurp_hw_rev_utils.h`. `test_val_eprom/host_stubs.cpp` defines the narrower guard and must not be
    copied here.

    Copy `reset_register_cache(uint8_t lsb, uint8_t msb, rurp_register_t ctrl)` verbatim from the trace
    suite. Mandatory: the three globals default to `0xff` and persist across Unity cases in one binary,
    and `0xff` ORs `CTRL_VPP_REGULATOR_ENABLE` into the first address write of any case that forgets.

    Implement the read-back model **keyed on the full 16-bit latched address** — the deliberate
    departure from the trace suite's `& 0x03` index. State the derivation in a comment: for
    `V131_BUS_CONFIG_0x08` (the tree's only 32-pin config) `matching_lines` is 17 and
    `static_high_mask` is 0, so `mem_util_remap_address_bus` leaves bits 0-16 identity-mapped, and A16
    (bit 16) is carried in the CONTROL top-address register rather than in the LSB/MSB latches;
    therefore
    `rurp_read_from_register(LEAST_SIGNIFICANT_BYTE) | (rurp_read_from_register(MOST_SIGNIFICANT_BYTE) << 8)`
    equals `address & 0xFFFF` for every byte of a block, **including a block based anywhere other than
    address 0**. The trace suite's `& 0x03` derivation is valid only for a 4-byte block based at 0 and
    would silently collapse the D-09 A16-crossing case, which spans `0x00FFFE` to `0x010001`.

    Shape: a linear-scan seed table of 8 entries with fields
    `uint16_t addr16; uint8_t target; uint16_t converge_after; uint16_t read_count; uint8_t seeded;`.
    `converge_after` and `read_count` are **`uint16_t`, not `uint8_t`** — a `0x0B` energy-cap case needs
    251 reads at 200 us and a `uint8_t` counter would wrap. Provide, all `extern "C"`:
    `void loop_readback_reset(void)`;
    `void loop_readback_seed(uint16_t addr16, uint8_t target, uint16_t converge_after)`;
    `int loop_readback_reads(uint16_t addr16)` returning the observed read count or **-1** when the
    address was never seeded; `int loop_readback_seeded_count(void)`; and
    `uint8_t rurp_read_data_buffer(void)` — the model: build the 16-bit key from the two latched
    registers, linear-scan for a seeded entry, return `0xFF` while `read_count < converge_after` and the
    entry's `target` from then on, incrementing `read_count` on every call. An **unseeded** address must
    return `0xFF` and must not be silently created.

    Record the read-count-to-pulse-count mapping in a comment, because it is the single easiest place
    for this suite to be silently off by one: the new loop reads **first** (LOOP-06's skip check) and
    then pulses, so `converge_after = N` means the byte matches on read `N+1`, i.e. after exactly **N
    pulses**, and `loop_readback_reads(addr) == 1 + pulses`. A byte skipped by the `0xFF` rule is never
    read at all, so its read count is **0**; an already-matching byte seeded with `converge_after = 0`
    gets exactly **1** read and **0** pulses.

    Add logged-id capture: a strong
    `extern "C" void rurp_log_id(uint8_t id, const uint8_t* params, uint8_t param_count)` appending to a
    32-entry array (id, param_count, up to 8 param bytes) with an overflow flag beyond that, overriding
    the weak default in `src/boards/rurp_serial_utils.cpp`. Because every `rurp_log_id_u*` variant
    routes through `rurp_log_id`, this single override captures every logged frame **including**
    `LOG_DEBUG_ID_SUB`'s `MSG_DEBUG` entries — cases must filter by id rather than assume the array
    holds only their own frame. Expose `extern "C"`: `void clear_logged_ids(void)`,
    `int logged_id_count(void)`, `uint8_t logged_id_at(int i)`, `uint8_t logged_id_param_count(int i)`,
    `uint8_t logged_id_param(int i, int j)`, `int logged_ids_overflowed(void)`.

    Do NOT modify `_shared/host_stubs_common.inc`. Nothing in this phase adds a new `rurp_*` symbol, so
    the expected diff to that file is **zero bytes**, and its stability is load-bearing for 17 other
    suites.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -f test/native/avr/test_loop_eprom_v131/host_stubs.cpp && python3 -c "
import pathlib
p = pathlib.Path('test/native/avr/test_loop_eprom_v131/host_stubs.cpp')
t = p.read_text()
inc = t.index('#include \"../_shared/host_stubs_common.inc\"')
head = t[:inc]
for g in ['HOST_STUBS_REAL_REGISTER_UTILS', 'HOST_STUBS_RECORD_TIMING', 'HOST_STUBS_CUSTOM_READ_DATA_BUFFER']:
    assert ('#define ' + g) in head, g + ' must be defined BEFORE the .inc include'
assert 'HOST_STUBS_CUSTOM_HW_REVISION' not in t.replace('HOST_STUBS_CUSTOM_HW_REVISION_BLOCK',''), 'must not define the narrower hw-revision guard'
assert t.index('#include \"rurp_register_utils.h\"') > inc, 'rurp_register_utils.h must come AFTER the .inc'
for sym in ['loop_readback_reset','loop_readback_seed','loop_readback_reads','loop_readback_seeded_count','reset_register_cache','rurp_read_data_buffer','rurp_log_id','clear_logged_ids','logged_id_count','logged_id_at','logged_id_param_count','logged_id_param','logged_ids_overflowed']:
    assert sym in t, 'missing required symbol: ' + sym
assert 'uint16_t converge_after' in t and 'uint16_t read_count' in t, 'converge_after and read_count must be uint16_t'
assert 'MOST_SIGNIFICANT_BYTE' in t and 'LEAST_SIGNIFICANT_BYTE' in t, 'model must key on both latched bytes'
assert '0x03' not in t, 'the 4-entry and-0x03 index derivation must NOT be copied'
assert len(t.splitlines()) >= 120, len(t.splitlines())
print('OK host_stubs.cpp', len(t.splitlines()), 'lines')
" && test -z "$(git diff --stat -- test/native/avr/_shared/host_stubs_common.inc)" && echo "OK: shared .inc untouched"</automated>
  </verify>
  <acceptance_criteria>
    - All three `HOST_STUBS_*` guards appear **before** `#include "../_shared/host_stubs_common.inc"`,
      and `#include "rurp_register_utils.h"` appears **after** it.
    - `HOST_STUBS_CUSTOM_HW_REVISION` (the narrower guard) is not defined anywhere in the file.
    - All thirteen harness symbols listed in the action exist with `extern "C"` linkage.
    - The read-back key is built from **both** `LEAST_SIGNIFICANT_BYTE` and `MOST_SIGNIFICANT_BYTE`; the
      literal `0x03` appears nowhere in the file.
    - `converge_after` and `read_count` are `uint16_t`.
    - `loop_readback_reads` returns **-1** for an address that was never seeded.
    - A comment states the read-count-to-pulse-count mapping and a comment derives why the 16-bit
      latched key equals `address & 0xFFFF` for the 32-pin config, naming `matching_lines` 17 and
      `static_high_mask` 0.
    - `git diff --stat -- test/native/avr/_shared/host_stubs_common.inc` prints nothing.
  </acceptance_criteria>
  <done>`host_stubs.cpp` exists with the three recorder layers composed in the correct order, a 16-bit-keyed uncapped read-back counter, and a strong `rurp_log_id` capture, with the shared `.inc` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Author the suite skeleton — setUp hooks, the drive helper contract, six loop-independent harness cases</name>
  <files>/workspaces/firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp</files>
  <read_first>
    - /workspaces/firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp — the
      includes and `extern "C"` declarations at :34-66; the locally re-declared cap constants at
      :275-276 and their "separate translation unit" comment; `setUp()` at :72-98 with both `.AlwaysDo`
      delay hooks and the `millis` / `micros` mocks; the three derived `bus_config_t` literals at
      :171-213 with the derivation command at :140-148; `drive_v131_write` at :243-270.
    - /workspaces/firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp — the
      `make_handle()` fresh-zeroed-handle pattern at :70-85, the non-vacuity / negative-control comment
      at :113-135, and the explicit `RUN_TEST` `main()` shape at :203-220.
    - /workspaces/firestarter/platformio.ini lines 96-105 — the recorded rule that
      `TEST_IGNORE_MESSAGE` must never be used to park a RED case.
    - /workspaces/firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp — task 1's output, for
      the exact harness symbol names and signatures.
  </read_first>
  <action>
    Write `test_loop_eprom_v131.cpp` with the project header block naming
    `Phase 141 Plan 03 (LOOP-01..LOOP-08, D-10)`.

    Include `<Arduino.h>`, `<ArduinoFake.h>`, `<unity.h>`, `<string.h>`, `<stdio.h>`, then
    `extern "C" { #include "memory.h" }`, then `"firestarter.h"`, `"eprom.h"`, `"eprom_params.h"`,
    `"memory_utils.h"`. A **test** TU may pair `<Arduino.h>` with `<ArduinoFake.h>` — the
    14-macro-redefinition trap against the 1166 warning watermark with zero headroom bites
    **production** TUs that pair `<Arduino.h>` with the `avr/pgmspace.h` shim, not test TUs.

    Re-declare locally, because `#define`s inside `host_stubs.cpp`'s translation unit are invisible
    here: `STROBE_KIND_DATA = 1`, `STROBE_KIND_PIN = 2`, `TIMING_KIND_DELAY_US = 3`,
    `TIMING_KIND_DELAY_MS = 4`, and both 512 caps as `LOOP_STROBE_CAP` / `LOOP_TIMING_CAP`, carrying the
    same "restated because separate TU" comment the trace suite uses. Add `extern "C"` declarations for
    every harness symbol task 1 exposed.

    `setUp()` must do all of: `ArduinoFakeReset()`; mock both `Serial.write` overloads to return 1 and
    `Serial.flush()`; install the two `.AlwaysDo` hooks forwarding `delayMicroseconds(unsigned int)` and
    `delay(unsigned long)` into `timing_push(TIMING_KIND_DELAY_US, ...)` and
    `timing_push(TIMING_KIND_DELAY_MS, ...)` as capture-less lambdas; mock `millis` and `micros` even
    though unused, because ArduinoFake **SIGABRTs on any unmocked call**; then `clear_strobes()`,
    `clear_timings()`, `clear_logged_ids()`, `loop_readback_reset()` and
    `reset_register_cache(0x00, 0x00, 0x00)`.

    Add `make_loop_handle(...)` returning a **fresh, zero-initialised** `firestarter_handle_t h = {}`
    with `protocol`, `pins`, `mem_size`, `pulse_delay` and `bus_config` as parameters plus
    `cmd = CMD_WRITE`, `response_code = RESPONSE_CODE_OK`,
    `ctrl_flags = FLAG_SKIP_BLANK_CHECK | FLAG_SKIP_ERASE`. `json_parse()` never resets `pulse_delay`,
    `protocol`, `mem_size`, `vpp_mv` or `pins`, so a stale global handle leaks state between cases.

    Copy the three derived `bus_config_t` literals verbatim from the trace suite, comments included,
    renamed `LOOP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B`. **Do not invent new ones:** a zeroed
    `bus_config` is degenerate rather than an identity remap, because `mem_util_remap_address_bus`
    starts from `config.address_mask & address` and an `address_mask` of 0 collapses every address to 0.

    Add `drive_loop_write(firestarter_handle_t* h, uint32_t base, const uint8_t* block, uint8_t n)`
    modelled on `drive_v131_write`: call `configure_memory(h)` **first** (it writes address 0 itself),
    then `reset_register_cache(0x00, 0x00, 0x00)`, then `clear_strobes()`, `clear_timings()`,
    `clear_logged_ids()`; set `h->address = base` and `h->data_size = n`; copy the block into
    `h->data_buffer` with a `(char)` cast; finally call `h->firestarter_operation_main(h)` — **never
    `_init`, and never the whole command.** Driving the whole command makes a high-voltage assertion
    vacuous, because `command_done()` writes `CONTROL_REGISTER = 0x00` on every exit regardless of what
    `eprom_write_execute` did. Never re-assign `h->firestarter_get_data` or `h->firestarter_set_data`:
    keeping the real `memory_get_data` / `memory_set_data` in the path captures the verify read's own bus
    activity and is D-05 expressed as a test constraint. This helper is authored now and left unused by
    this plan's cases so plans 141-07 and 141-08 inherit a fixed contract; silence any unused-function
    warning the way the sibling suites do rather than by deleting it.

    Then add exactly these six **loop-independent** harness cases, each registered with `RUN_TEST` in an
    explicit `main()` that returns `UNITY_END()` (no auto-discovery):

    1. `test_setup_leaves_all_three_recorders_clean` — after `setUp`, `strobe_count()`,
       `timing_count()` and `logged_id_count()` are 0 and none of the three overflow flags is set.
    2. `test_timing_hook_records_both_delay_kinds_with_their_arguments` — call `delay(7)` then
       `delayMicroseconds(11)` directly; assert two entries in that order with kinds
       `TIMING_KIND_DELAY_MS` / `TIMING_KIND_DELAY_US` and `timing_us` values 7 and 11. This proves the
       hook LOOP-07's oracle depends on actually fires.
    3. `test_readback_model_returns_ff_until_converge_then_the_target` —
       `loop_readback_seed(0x1234, 0x5A, 2)`, `reset_register_cache(0x34, 0x12, 0x00)`, then three
       `rurp_read_data_buffer()` calls returning `0xFF`, `0xFF`, `0x5A`, and
       `loop_readback_reads(0x1234) == 3`.
    4. `test_readback_model_returns_ff_and_stays_unseeded_for_an_unknown_address` — the negative
       control: with only `0x1234` seeded, set the cache to lsb `0xFF` / msb `0x00`, assert
       `rurp_read_data_buffer()` returns `0xFF`, `loop_readback_reads(0x00FF) == -1`, and
       `loop_readback_seeded_count()` is still 1.
    5. `test_readback_model_distinguishes_two_addresses_across_an_a16_crossing` — seed `0xFFFE` and
       `0x0000` with different targets and different `converge_after` values, alternate the latched cache
       between them, and assert each keeps its own independent read counter. This is the exact property
       the D-09 case in plan 141-08 depends on.
    6. `test_logged_id_capture_records_the_id_and_its_packed_params` — call
       `rurp_log_id_u24(0xB1, 0x012345UL)` directly; assert `logged_id_count() == 1`,
       `logged_id_at(0) == 0xB1`, `logged_id_param_count(0) == 3`, and the three param bytes are `0x01`,
       `0x23`, `0x45` (big-endian, matching `rurp_log_id_u24`'s packing).

    Never use `TEST_IGNORE_MESSAGE` anywhere in this file.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import pathlib
t = pathlib.Path('test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp').read_text()
cases = ['test_setup_leaves_all_three_recorders_clean','test_timing_hook_records_both_delay_kinds_with_their_arguments','test_readback_model_returns_ff_until_converge_then_the_target','test_readback_model_returns_ff_and_stays_unseeded_for_an_unknown_address','test_readback_model_distinguishes_two_addresses_across_an_a16_crossing','test_logged_id_capture_records_the_id_and_its_packed_params']
for c in cases:
    assert t.count(c) >= 2, c + ' must be both defined and registered with RUN_TEST'
assert 'TEST_IGNORE_MESSAGE' not in t, 'TEST_IGNORE_MESSAGE is forbidden'
assert 'UNITY_BEGIN' in t and 'UNITY_END' in t
assert 'firestarter_operation_main' in t, 'the drive helper must call operation_main'
assert 'drive_loop_write' in t, 'the drive helper contract must exist for later plans'
assert 'LOOP_BUS_CONFIG_0x08' in t, 'the 32-pin bus config literal must be present'
assert 'millis' in t and 'micros' in t, 'ArduinoFake SIGABRTs on unmocked millis/micros'
assert len(t.splitlines()) >= 150, len(t.splitlines())
print('OK suite skeleton', len(t.splitlines()), 'lines,', len(cases), 'harness cases')
"</automated>
  </verify>
  <acceptance_criteria>
    - All six named cases are both defined and registered with `RUN_TEST` in an explicit `main()`.
    - `TEST_IGNORE_MESSAGE` appears nowhere in the file.
    - `drive_loop_write` exists, calls `configure_memory` then `reset_register_cache` then the three
      clear calls, and finally `h->firestarter_operation_main(h)` — it never calls
      `firestarter_operation_init` and never re-assigns `firestarter_get_data` or
      `firestarter_set_data`.
    - `LOOP_BUS_CONFIG_0x07`, `LOOP_BUS_CONFIG_0x08` and `LOOP_BUS_CONFIG_0x0B` are present and are
      byte-for-byte the trace suite's literals apart from the rename.
    - `setUp()` mocks `millis` and `micros` in addition to the two delays and both `Serial.write`
      overloads.
    - Case 4 asserts `loop_readback_reads(...) == -1` for the unseeded address (the negative control).
  </acceptance_criteria>
  <done>The suite compiles-ready skeleton exists with hooked recorders, a fixed drive-helper contract for later plans, and six loop-independent harness cases including two negative controls.</done>
</task>

<task type="auto">
  <name>Task 3: Add [env:native_loop_v131] and extend CLAUDE.md's pinned-env exception</name>
  <files>/workspaces/firestarter/platformio.ini, /workspaces/firestarter/CLAUDE.md</files>
  <read_first>
    - /workspaces/firestarter/platformio.ini — read the whole file (371 lines). Extract: `default_envs`
      at :16 (`uno, uno328pb, leonardo`); `-D HARDWARE_REVISION` in the shared `[env]` `build_flags` at
      :23; `[env:native]`'s 17-entry `test_filter` at :102-119 and its parallel `-I` list at :124-140;
      and `[env:native_params_v131]` at :331-371 in full — including its HARD CONSTRAINT comment block,
      which is the template to copy and retarget.
    - /workspaces/firestarter/scripts/baseline/size_baseline.json — the `native_envs` block pinning
      `native` and `native_nodevtools` at 141 cases / 17 suites each.
    - /workspaces/firestarter/CLAUDE.md lines 200-230 — §"Native (Host) Test Environment", the
      positive-allowlist paragraph and the **"Exception (Phase 140 D-11)"** paragraph that overrides it
      for `native_params_v131`.
  </read_first>
  <action>
    Append `[env:native_loop_v131]` to `platformio.ini`, immediately after `[env:native_params_v131]`.
    Copy `[env:native_params_v131]`'s comment block wholesale and retarget it: name this the **sixth**
    native env, name Phase 141 / D-10 as its origin, and state that its purpose is to verify the
    per-byte pulse-to-verify loop, which the frozen `native_trace_v131` fixture cannot do because that
    fixture goes RED in this phase by design and is not re-frozen here.

    The retargeted comment must keep stating all four constraints verbatim in substance:
    (1) it must **never** be folded into `[env:native]` or `[env:native_nodevtools]`'s `test_filter` or
    `-I` lists — both are pinned at exactly **141 cases / 17 suites** by
    `scripts/baseline/size_baseline.json` through `check_size_baseline.py`'s `compare_native`, so adding
    a case to either turns a live gate RED; (2) it is **not** added to `default_envs` — `pio run` would
    try to link a `main()`-less target; (3) it is **never** passed to `check_size_baseline.py` (whose
    `NATIVE_ENVS` is hardcoded to `("native", "native_nodevtools")`, so an unknown env raises an
    uncaught `KeyError` and exits 1 — a **false** regression signal, F-138-05, accepted and not fixed)
    nor to `check_build_warnings.py` (exit 2, no baseline entry); (4) it runs in **no CI leg of either
    repository** — neither `build.yml` nor `beta-build.yml` runs any `pio test` env beyond `native` and
    `native_nodevtools` — so its case and suite counts are a **run-by-name obligation** recorded in the
    Phase 141 phase record, never implied as CI coverage.

    Env body, matching the sibling envs exactly: `platform = native`; `test_framework = unity`;
    `test_filter` naming **one** entry, `native/avr/test_loop_eprom_v131`; `build_flags` starting with
    `${env:native.build_flags}` (which inherits `-D HARDWARE_REVISION`, `-std=gnu++17`, `-I include` and
    all sibling `-I` entries — dropping `HARDWARE_REVISION` would change which stubs compile) plus
    `-I test/native/avr/test_loop_eprom_v131`; `lib_deps = fabiobatsilva/ArduinoFake@^0.4.0`;
    `build_src_filter` **byte-identical** to the four sibling native envs
    (`+<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c> +<operation_utils.cpp>`); and
    `test_build_src = yes`.

    Then extend CLAUDE.md's **"Exception (Phase 140 D-11)"** paragraph in §"Native (Host) Test
    Environment" so it names **both** `native_params_v131` and `native_loop_v131`. Leaving it naming
    only `native_params_v131` makes the general "add a new suite to both pinned envs" instruction
    directly above read as still binding for the new suite, which would turn a live gate RED. State
    that `native_loop_v131` originates in Phase 141 / D-10 and carries the identical four constraints.
    Do not touch §"Algorithm Handlers" in this plan — plan 141-05 owns those rows.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import configparser
c = configparser.ConfigParser(strict=False)
c.read('platformio.ini')
assert 'env:native_loop_v131' in c, 'the sixth env is missing'
e = c['env:native_loop_v131']
tf = [l.strip() for l in e['test_filter'].splitlines() if l.strip()]
assert tf == ['native/avr/test_loop_eprom_v131'], tf
assert 'native_loop_v131' not in c['platformio']['default_envs'], 'must NOT be in default_envs'
sib = c['env:native_params_v131']['build_src_filter'].strip()
assert e['build_src_filter'].strip() == sib, 'build_src_filter must match the siblings byte-for-byte'
assert '\${env:native.build_flags}' in e['build_flags'], 'must inherit env:native build_flags'
assert '-I test/native/avr/test_loop_eprom_v131' in e['build_flags'], 'missing the -I entry'
for pinned in ('env:native','env:native_nodevtools'):
    assert 'test_loop_eprom_v131' not in c[pinned]['test_filter'], pinned + ' must not name the new suite'
    assert 'test_loop_eprom_v131' not in c[pinned]['build_flags'], pinned + ' must not -I the new suite'
print('OK sixth env wired, pinned envs untouched')
" && grep -q "native_loop_v131" CLAUDE.md && pio test -e native_loop_v131 && pio test -e native && pio test -e native_nodevtools && pio test -e native_trace_v131 && pio run -e uno && python3 -m pytest tests/ -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `[env:native_loop_v131]` exists with a `test_filter` of exactly one entry,
      `native/avr/test_loop_eprom_v131`.
    - `native_loop_v131` does **not** appear in `default_envs`.
    - Neither `[env:native]` nor `[env:native_nodevtools]` mentions `test_loop_eprom_v131` in
      `test_filter` or `build_flags`.
    - `build_src_filter` is byte-identical to `[env:native_params_v131]`'s.
    - `build_flags` begins with `${env:native.build_flags}` and adds
      `-I test/native/avr/test_loop_eprom_v131`.
    - The env's comment block states all four constraints (never folded into a pinned env; not in
      `default_envs`; never passed to either check script, naming the `KeyError`/exit-1 and exit-2
      behaviours; no CI coverage in either repository).
    - `pio test -e native_loop_v131` reports **6 test cases in 1 suite, all passing**.
    - `pio test -e native` and `pio test -e native_nodevtools` each still report **141 cases / 17
      suites**, all passing.
    - `pio test -e native_trace_v131` still passes.
    - `python3 -m pytest tests/ -q -o addopts=""` is fully green (244 passed).
    - `CLAUDE.md`'s "Exception (Phase 140 D-11)" paragraph names **both** `native_params_v131` and
      `native_loop_v131`, and §"Algorithm Handlers" is unchanged by this plan.
    - `check_size_baseline.py` and `check_build_warnings.py` are **not** invoked with the new env name
      anywhere in this plan's commands or in any committed script.
  </acceptance_criteria>
  <done>The sixth env runs the new suite by name with six passing harness cases, both pinned envs still report 141/17, and CLAUDE.md's exception paragraph covers the new env.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production register layer | the suite compiles against the real `rurp_register_utils.h`, so production's cache-elision is exercised rather than replicated |
| baseline JSON → live gate | `size_baseline.json` pins two envs by exact case and suite count; a new suite added to the wrong env turns a live gate RED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-GATE | Repudiation | a harness that silently observes nothing | mitigate | Six harness cases, two of them explicit negative controls (unseeded address returns `0xFF` with a read count of -1; per-address counters stay independent), plus overflow-flag assertions after `setUp` |
| T-141-ELIDE | Tampering | register-write elision invisible to a replica recorder | mitigate | `HOST_STUBS_REAL_REGISTER_UTILS` plus including `rurp_register_utils.h` **after** the shared `.inc`, so the real cache-compare runs — the bug class the FM1608 register-cache-elision defect documents |
| T-141-PIN | Denial of Service | the two pinned native envs' 141/17 counts | mitigate | The sixth env names only its own suite; `configparser`-based assertions prove neither pinned env references the new suite in `test_filter` or `build_flags` |
| T-141-CI | Repudiation | implying CI coverage that does not exist | mitigate | The env's own comment block states the no-CI-coverage fact, and plan 141-09 records the run-by-name obligation with the observed counts |
| T-141-SC | Tampering | package installs | accept | `ArduinoFake@^0.4.0` is a pre-existing `lib_deps` entry already resolved in `.pio/libdeps/` for five envs; no new dependency is added, so RESEARCH records the phase as not-applicable for a legitimacy audit |
</threat_model>

<verification>
- `cd /workspaces/firestarter && pio test -e native_loop_v131` — 6 cases / 1 suite, all passing.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` — 141 cases / 17
  suites each.
- `cd /workspaces/firestarter && pio test -e native_trace_v131` — still passing (nothing in this plan
  touches the write path).
- `cd /workspaces/firestarter && pio run -e uno` — SUCCESS with an unchanged flash figure (a test-only
  and config-only change costs 0 B in the AVR image).
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""` — fully green (244 passed).
</verification>

<success_criteria>
- A sixth native env `native_loop_v131` exists, runs by name, and is absent from `default_envs` and
  from both pinned envs' `test_filter` and `-I` lists.
- The new suite's `host_stubs.cpp` composes `HOST_STUBS_REAL_REGISTER_UTILS`,
  `HOST_STUBS_RECORD_TIMING` and `HOST_STUBS_CUSTOM_READ_DATA_BUFFER` in the correct order and includes
  the real `rurp_register_utils.h` afterwards.
- The read-back model keys on the full 16-bit latched address with `uint16_t` counters, so a block
  crossing `0x00FFFF` to `0x010000` and a 251-read energy-cap case are both representable.
- Six harness cases pass, including two negative controls.
- `_shared/host_stubs_common.inc` has a zero-byte diff.
- CLAUDE.md's pinned-env exception paragraph names the new env.
</success_criteria>

<artifacts_this_phase_produces>
New symbols, files and env names created by Phase 141 as a whole. Treat every entry as **new**, not
as drift against an existing definition:

- **Message IDs / names:** `MSG_ERR_PULSE_TOO_WIDE` (0xAE), `MSG_ERR_MAX_PULSES` (0xBD),
  `MSG_ERR_ENERGY_CAP` (0xBE)
- **Firmware functions:** `mem_util_delay_us`, `mem_util_split_delay`, `eprom_overprogram_us`,
  `eprom_internal_report_budget_failure`
- **Firmware macro:** `MEM_UTIL_DELAY_US_MAX`
- **New PlatformIO env:** `native_loop_v131`
- **New test files:** `firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp`,
  `firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp`
- **New harness symbols:** `loop_readback_reset`, `loop_readback_seed`, `loop_readback_reads`,
  `loop_readback_seeded_count`, `clear_logged_ids`, `logged_id_count`, `logged_id_at`,
  `logged_id_param_count`, `logged_id_param`, `logged_ids_overflowed`, `drive_loop_write`,
  `make_loop_handle`, `LOOP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B`, plus a per-suite
  `reset_register_cache` copy
- **New pytest gate module:** `firestarter/tests/test_write_path_source_contract_v131.py`
- **New meta artifacts:** `.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md`,
  `141-NEW-TRACE.md`, `141-LOOP-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/141-per-byte-program-loop/141-03-SUMMARY.md` when done.
</output>
