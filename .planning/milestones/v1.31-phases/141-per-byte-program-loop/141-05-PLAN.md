---
phase: 141-per-byte-program-loop
plan: 05
type: execute
wave: 3
depends_on: ["141-04"]
commits_land_in: [firestarter]
files_modified:
  - /workspaces/firestarter/tests/golden/protocol_branch_inventory.json
  - /workspaces/firestarter/tests/test_protocol_branch_inventory.py
  - /workspaces/firestarter/CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The protocol-branch inventory golden matches the rewritten write path, re-derived by its own scanner rather than hand-edited"
    - "The pinned tier-1 line list in the test module names the new line numbers and still holds exactly three entries"
    - "The full firmware gate suite is green again"
    - "The Algorithm Handlers rows describe what actually shipped, including the 0x08 drop-bit truth"
  artifacts:
    - path: "/workspaces/firestarter/tests/golden/protocol_branch_inventory.json"
      provides: "re-derived sites, hand-authored reasons, updated blob SHA and counts, frozen_for re-pointed at Phase 142"
      contains: "protocol_keyed_sites"
    - path: "/workspaces/firestarter/CLAUDE.md"
      provides: "Algorithm Handlers rows reconciled with the shipped loop"
      contains: "Algorithm Handlers"
  key_links:
    - from: "/workspaces/firestarter/tests/golden/protocol_branch_inventory.json"
      to: "/workspaces/firestarter/src/proms/eprom.cpp"
      via: "meta.blob_shas equality against git rev-parse HEAD:src/proms/eprom.cpp"
      pattern: "blob_shas"
    - from: "/workspaces/firestarter/tests/test_protocol_branch_inventory.py"
      to: "the three surviving tier-1 sites"
      via: "the pinned protocol_lines literal"
      pattern: "protocol_lines =="
---

<objective>
Bring the D-13 protocol-branch-inventory gate back to green **by re-derivation, never by hand-editing**,
and reconcile `firestarter/CLAUDE.md`'s Algorithm Handlers rows with what plan 141-04 actually shipped.

Per **D-11** this gate was always going to go RED — rewriting the write path moves the tier-2 inventory,
and that movement is exactly the visibility the gate was built for. Two mechanical facts make this
plan non-obvious and are the reason it is its own plan:

1. **The golden alone is insufficient.** `tests/test_protocol_branch_inventory.py:446` hard-codes
   `protocol_lines == [71, 145, 218]` **in the test module**, not in the JSON. Re-deriving the golden
   fixes two of the three broken tests and leaves the third failing. The line list *is* the locator, so
   updating it is "fix the locator, not the assertion" — and the count must stay **three**, because the
   failure text's own wording ("fewer than three means one of the pinned sites was removed without
   updating this inventory") would then be correct about a real regression.
2. **There is no re-derivation script.** `_extract_predicates` exists only inside the test module, and
   the golden's own `meta.how_to_update` names diffing the extractor's live output against the JSON as
   the only sanctioned route. Import it; never type a line number, tier or count by hand.

`pytest tests/ -v` runs in **both** firmware workflows (`build.yml:161`, `beta-build.yml:134`), so this
gate going RED is a CI failure once the branch reaches `main` or `beta`, not a local-only annoyance.

Purpose: restore the phase's only CI-visible gate and make the inventory movement the legible proof that
LOOP-02's removals happened.
Output: a re-derived golden, one edited pinned literal, three reconciled documentation rows.

**Requirement ownership:** contributes to LOOP-02 (the inventory movement is half its evidence; the
absence gate in plan 141-06 is the other half) and to LOOP-08 (the `0x08` row's corrected drop-bit
claim). **Completes neither.** Do NOT flip any `LOOP-*` checkbox here.

**Frontmatter `requirements:` is deliberately EMPTY — do not repopulate it.** `execute-plan.md`'s
`update_requirements` step calls the `requirements.mark-complete` verb unconditionally with *this*
plan's own frontmatter IDs, and that verb performs a **real** regex flip of both the `[ ]` checkbox
and the `Pending` coverage-table cell — it is not a log-only call, and unlike its sibling steps it
carries no documented skip for parallel or worktree execution. Partial IDs in frontmatter would mark
LOOP-02 / LOOP-08 `Complete` in wave 3, before the evidence exists (the exact failure mode this project hit
four times in Phase 116), and would also break plan 141-09's sixteen-line diff self-check. An empty
list makes `update_requirements`'s own "if no requirements field, skip" condition apply. Dimension-1
requirement coverage is carried by plan **141-09**, which declares all eight `LOOP-*` IDs and flips
them in one hand edit after every piece of evidence exists.

**Repo notes:** firmware submodule only. Commit inside `/workspaces/firestarter` on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`. Note `firestarter/CLAUDE.md` was also touched by plan
141-03 (a different section, §"Native (Host) Test Environment"); that plan is in an earlier wave, so
there is no conflict — but do not revert its paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-per-byte-program-loop/141-CONTEXT.md
@.planning/phases/141-per-byte-program-loop/141-RESEARCH.md
@.planning/phases/141-per-byte-program-loop/141-PATTERNS.md
@.planning/phases/141-per-byte-program-loop/141-04-SUMMARY.md
@firestarter/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the inventory golden with its own scanner and update its hand-authored fields</name>
  <files>/workspaces/firestarter/tests/golden/protocol_branch_inventory.json</files>
  <read_first>
    - /workspaces/firestarter/tests/test_protocol_branch_inventory.py — the whole module (565 lines).
      Extract: `_HERE` / `_REPO_ROOT` / `_EPROM_REL` / `_PARAMS_REL` / `_INVENTORY_JSON` and the
      `FIRESTARTER_BRANCH_SCAN_SOURCE` / `FIRESTARTER_BRANCH_SCAN_PARAMS_SOURCE` env seams at :88-104
      (they bind at **import** time); `_extract_predicates` at :277-368;
      `test_blob_shas_match_the_recorded_inventory` at :398; `test_branch_sites_match_the_recorded_inventory`
      at :417; `test_exactly_three_protocol_keyed_sites_at_the_pinned_lines` at :443 with the literal at
      :446; `test_inventory_is_non_vacuous` at :455 (it asserts every site has a **non-empty** `reason`,
      which `_extract_predicates` does not emit); `test_params_table_has_no_second_selector` at :487.
    - /workspaces/firestarter/tests/golden/protocol_branch_inventory.json — the whole file. Current
      state: `meta` with keys `sources`, `blob_shas`, `recorded_at_head`, `recorded_by`, `requirement`,
      `decision`, `why_two_checks`, `how_to_update`, `frozen_for`, `allowlist_rationale`; `sites` with 24
      entries; `counts` `{total_sites: 24, protocol_keyed_sites: 3, other_sites: 21}`; `params_table`.
      Note the `:328` entry's `reason` claims `eprom_internal_ensure_regulator_enabled` is "used by
      callers outside the write path".
    - /workspaces/firestarter/src/proms/eprom.cpp — the rewritten file, so each new site's
      hand-authored `reason` describes the predicate that is actually there.
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md — the tier-1 and tier-2
      counts predicted before the rewrite, for the record's before/after pairing.
  </read_first>
  <action>
    Re-derive `sites[]` by importing the extractor — the only sanctioned route:
    `cd /workspaces/firestarter` then a `python3 -c` that does
    `sys.path.insert(0, 'tests')`, `import test_protocol_branch_inventory as m`,
    `live = m._extract_predicates((m._REPO_ROOT / 'src/proms/eprom.cpp').read_text())`, and prints
    `json.dumps(live, indent=2)` plus the sorted tier-1 line list and the total and tier-1 counts.
    Write the printed `sites[]` tuples into the golden **verbatim**. Never type a `line`, `predicate`,
    `keyed_on` or `tier` value by hand — hand-editing a line number to make a surprise disappear is the
    exact failure this gate exists to prevent.

    Then update the four fields the extractor does **not** emit:

    (a) `sites[].reason` and `sites[].class` — hand-authored, and `test_inventory_is_non_vacuous`
    asserts every site has a non-empty `reason`. Carry forward each **surviving** site's existing reason
    verbatim, and author a new reason for each new site naming what the predicate is for (for example the
    byte-loop bound, the `0xFF` skip, the already-matching skip, the `pulses >= max_pulses` check, the
    `energy_cap_us` guard, the `pins >= 32` DIP32 branch, the `row == NULL` refusal, D-03's over-cap
    refusal, the `overprogram_factor` guard, the `verify_mode` check, and the final pass's own bound and
    comparison). **Correct the `eprom_internal_ensure_regulator_enabled` entry's reason** if that site
    survives: the claim that it is "used by callers outside the write path" is factually false — the
    function has zero callers anywhere in `src/`, `include/`, `test/` or `tests/`, and is retained only
    because `--gc-sections` already collects it so deleting it reclaims 0 B.

    (b) `meta.blob_shas["src/proms/eprom.cpp"]` — set it from `git hash-object src/proms/eprom.cpp`,
    which equals `git rev-parse HEAD:src/proms/eprom.cpp` for a file already committed by plan 141-04.
    Leave `eprom_params.cpp`'s SHA untouched — this phase changes no table value.

    (c) `counts.total_sites`, `counts.protocol_keyed_sites` and `counts.other_sites` — consistent with
    the re-derived `sites[]`. Also refresh `meta.recorded_at_head` and `meta.recorded_by` (asserted by no
    test, but stale values mislead).

    (d) `meta.frozen_for` — it currently anticipates **this** phase by name. Re-point it at **Phase 142**:
    state that Phase 141 removed the retry-escalation loop and that Phase 142 will rewrite the VPP
    branches into the table's `vpp_path` column, and keep the existing warning that an unchanged site
    count across either phase would itself be suspicious.

    Record the measured before/after pair in this plan's SUMMARY: before was 24 total / 3 tier-1 / 21
    tier-2; report the after triple and each site added and removed. **Do not assert or imply
    shrinkage** — a per-byte loop adds handle-field-keyed predicates, so tier-2 may legitimately grow.
    Only a **fourth tier-1 (protocol-keyed)** site is a violation.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import sys, json, subprocess, pathlib
sys.path.insert(0, 'tests')
import test_protocol_branch_inventory as m
live = m._extract_predicates((m._REPO_ROOT / 'src/proms/eprom.cpp').read_text())
g = json.loads(pathlib.Path('tests/golden/protocol_branch_inventory.json').read_text())
lk = [(s['line'], s['predicate'], tuple(s['keyed_on']), s['tier']) for s in live]
gk = [(s['line'], s['predicate'], tuple(s['keyed_on']), s['tier']) for s in g['sites']]
assert lk == gk, 'golden sites do not match the live re-derivation'
assert all(s.get('reason') for s in g['sites']), 'every site needs a non-empty hand-authored reason'
t1 = [s['line'] for s in g['sites'] if s['tier'] == 'protocol']
assert len(t1) == 3, ('tier-1 count must stay exactly 3, got', t1)
assert g['counts']['total_sites'] == len(g['sites'])
assert g['counts']['protocol_keyed_sites'] == 3
assert g['counts']['other_sites'] == len(g['sites']) - 3
sha = subprocess.run(['git','hash-object','src/proms/eprom.cpp'], capture_output=True, text=True).stdout.strip()
assert g['meta']['blob_shas']['src/proms/eprom.cpp'] == sha, 'blob SHA mismatch'
assert 'Phase 142' in g['meta']['frozen_for'], 'frozen_for must be re-pointed at Phase 142'
assert 'used by callers outside the write path' not in json.dumps(g), 'the false reason must be corrected'
print('OK golden re-derived: total', g['counts']['total_sites'], 'tier1', t1)
"</automated>
  </verify>
  <acceptance_criteria>
    - The golden's `sites[]` `(line, predicate, keyed_on, tier)` tuples are **exactly** the live
      `_extract_predicates` output, in the same order.
    - Every site has a non-empty `reason`, and the string
      `used by callers outside the write path` appears nowhere in the file.
    - `counts.protocol_keyed_sites` is **3**, and `counts.total_sites` and `counts.other_sites` are
      internally consistent with `sites[]`.
    - `meta.blob_shas["src/proms/eprom.cpp"]` equals `git hash-object src/proms/eprom.cpp`;
      `eprom_params.cpp`'s SHA is unchanged.
    - `meta.frozen_for` names **Phase 142** and retains the "an unchanged site count would itself be
      suspicious" warning.
    - The plan SUMMARY records the before triple (24 / 3 / 21) and the measured after triple, with each
      added and removed site named, and makes no shrinkage claim.
  </acceptance_criteria>
  <done>The golden matches the live scanner exactly, with hand-authored reasons, a correct blob SHA, consistent counts and `frozen_for` handed to Phase 142.</done>
</task>

<task type="auto">
  <name>Task 2: Update the pinned tier-1 line literal and prove the gate is armed, not merely quiet</name>
  <files>/workspaces/firestarter/tests/test_protocol_branch_inventory.py</files>
  <read_first>
    - /workspaces/firestarter/tests/test_protocol_branch_inventory.py lines 443-452 —
      `test_exactly_three_protocol_keyed_sites_at_the_pinned_lines`, whose `assert protocol_lines == [71, 145, 218]`
      at :446 is the single edit this task makes, and whose failure message wording constrains the fix.
    - /workspaces/firestarter/tests/test_protocol_branch_inventory.py lines 88-104 and 119-136 — the two
      env seams (which bind at **import**, so a planted-violation run must set them in a **child
      process**, never via monkeypatch) and `_resolve_git`'s fail-closed contract.
    - /workspaces/firestarter/tests/test_protocol_branch_inventory.py lines 512-543 —
      `test_default_targets_resolve_inside_this_repository`, the in-tree guard against the
      `_HERE`-resolves-to-the-wrong-directory class of defect.
    - /workspaces/firestarter/tests/golden/protocol_branch_inventory.json — task 1's output, for the
      authoritative new tier-1 line numbers.
  </read_first>
  <action>
    Edit **only** the list literal at :446, replacing `[71, 145, 218]` with the three tier-1 line
    numbers task 1 re-derived, and update the two line numbers quoted inside the failure message string
    so the message stays truthful. The count must remain **three**: `:71`'s `switch (handle->protocol)`
    fallback (Phase 140 D-03 keeps it), `eprom_write_execute`'s
    `handle->protocol == 0x0B || is_flag_set(FLAG_VPE_AS_VPP)` predicate (kept verbatim by plan 141-04;
    only its line moved), and `eprom_check_vpp`'s predicate (untouched; its line shifted only because the
    file above it shrank).

    Change nothing else in the module — not the extractor, not the non-vacuity guard, not the
    fail-closed `git` resolution, not the no-skip self-check.

    Then satisfy **D-15**: prove the leg is armed rather than merely quiet, in a **child process** so the
    import-time seam binding takes effect, and with the seam **unset afterwards**.
    Planted violation A: copy the live `src/proms/eprom.cpp` to a scratch file under
    `/tmp/claude-1000/-workspaces/*/scratchpad/`, insert a fourth protocol-keyed predicate into the copy
    (for example an `if (handle->protocol == 0x08)` branch), run
    `FIRESTARTER_BRANCH_SCAN_SOURCE=<copy> python3 -m pytest tests/test_protocol_branch_inventory.py::test_exactly_three_protocol_keyed_sites_at_the_pinned_lines -q -o addopts=""`
    in a child process, and confirm it goes **RED naming four tier-1 lines** — not RED with an import,
    decode or path error.
    Planted violation B: with the same copy but a **removed** tier-1 predicate, confirm the leg goes RED
    reporting two lines.
    Capture both transcripts into this plan's SUMMARY. Then `unset` both
    `FIRESTARTER_BRANCH_SCAN_SOURCE` and `FIRESTARTER_BRANCH_SCAN_PARAMS_SOURCE` and re-run the real
    module to confirm green. A stray seam left set makes later tests fail loudly — safe, but a wasted
    debugging cycle.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && unset FIRESTARTER_BRANCH_SCAN_SOURCE FIRESTARTER_BRANCH_SCAN_PARAMS_SOURCE && python3 -c "
import json, pathlib, re
g = json.loads(pathlib.Path('tests/golden/protocol_branch_inventory.json').read_text())
want = sorted(s['line'] for s in g['sites'] if s['tier'] == 'protocol')
src = pathlib.Path('tests/test_protocol_branch_inventory.py').read_text()
m = re.search(r'protocol_lines == (\[[0-9,\s]+\])', src)
assert m, 'the pinned literal was not found'
got = json.loads(m.group(1))
assert got == want, ('pinned literal', got, 'golden tier-1', want)
assert len(got) == 3, got
print('OK pinned literal matches the re-derived tier-1 lines:', got)
" && python3 -m pytest tests/test_protocol_branch_inventory.py -q -o addopts="" && python3 -m pytest tests/ -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - The `:446` literal equals the golden's sorted tier-1 line list and has exactly **three** entries.
    - The failure-message text in that test quotes the same three line numbers.
    - `git diff -- tests/test_protocol_branch_inventory.py` touches **only**
      `test_exactly_three_protocol_keyed_sites_at_the_pinned_lines` — no change to `_extract_predicates`,
      `test_inventory_is_non_vacuous`, `_resolve_git`, `test_default_targets_resolve_inside_this_repository`
      or `test_git_is_required_not_optional`.
    - `python3 -m pytest tests/test_protocol_branch_inventory.py -q -o addopts=""` reports **7 passed**.
    - `python3 -m pytest tests/ -q -o addopts=""` is fully green again (the whole firmware gate suite).
    - Both planted-violation transcripts are captured in the SUMMARY: the fourth-site run RED naming
      four tier-1 lines, and the removed-site run RED naming two — each failing on the **assertion**, not
      on an import, decode or path error.
    - `env | grep -c FIRESTARTER_BRANCH_SCAN` returns 0 after the planted runs.
  </acceptance_criteria>
  <done>The pinned locator names the new tier-1 lines, the whole firmware gate suite is green, and the leg has been seen RED in both directions for the right reason.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile CLAUDE.md's three Algorithm Handlers rows with what shipped</name>
  <files>/workspaces/firestarter/CLAUDE.md</files>
  <read_first>
    - /workspaces/firestarter/CLAUDE.md lines 57-71 — §"Algorithm Handlers" and the `0x07`, `0x08` and
      `0x0B` rows in full, including each row's VPP column and its "verify per pulse ... final
      full-array pass ... `max_pulses` ... overprogram" notes.
    - /workspaces/firestarter/src/proms/eprom.cpp — the rewritten write path, so each row describes the
      code as it now is.
    - /workspaces/firestarter/src/proms/eprom_params.cpp lines 49-53 — the shipped row values, which are
      the authority for `max_pulses`, `energy_cap_us`, `overprogram_factor`, `verify_mode` and `vpp_path`.
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-RESEARCH.md §"DIP32 / A16 truth table"
      — the source-verified statement that the drop bit does not survive the first `set_address` on any
      `pins >= 32` part.
  </read_first>
  <action>
    Update the three rows so each is true of the shipped loop. Concretely:

    `0x07` and `0x08`: keep the "verify per pulse + 1 final full-array pass" claim — plan 141-04 consumed
    `verify_mode` and both rows ship `VERIFY_PER_PULSE_PLUS_FINAL`, so the claim is now backed by an
    explicit `verify_mode == VERIFY_PER_PULSE_PLUS_FINAL` branch rather than by an accident of the block
    loop's structure. Say so, and name that a mismatch in that final pass emits `MSG_ERR_VERIFY`.

    `0x0B`: keep "verify per pulse, no final full-array pass" and the 50 ms per-byte accumulated-energy
    cap, and add the two facts the phase established: the cap divides evenly by **every** shipped `0x0B`
    width (200 / 500 / 1000 us give 250 / 100 / 50 pulses, `accumulated` landing on exactly 50000), so
    the "capped at 50 ms" wording is exactly true for shipped data; and with an arbitrary
    `--pulse-us` value the bound is `accumulated < energy_cap_us + w`, whose worst case under D-03's
    refusal is `2 * 50000 - 1` = 99999 us, reached at `w` = 49999. Do **not** repeat CONTEXT.md D-01's
    `50000 + 65535` figure of roughly 115 ms — that is the bound *without* D-03, and it is
    arithmetically unreachable now that D-03 refuses any `w` above the cap.

    All three rows: add that the two budget-exhaustion failures are separately named on the wire as
    `MSG_ERR_MAX_PULSES` (0xBD) and `MSG_ERR_ENERGY_CAP` (0xBE), each carrying the failing address and
    that byte's pulse count, and that a pulse wider than the row's energy cap is refused pre-flight with
    `MSG_ERR_PULSE_TOO_WIDE` (0xAE) before any high voltage is enabled.

    `0x08`'s VPP column: its current text, "13V via CTRL_VPP_VPE_DROP_ENABLE", is **already false today**
    and was false before this phase. On any `pins >= 32` part the drop bit is excluded from
    `mem_util_calculate_top_address_register`'s preserve mask, so it is cleared by the first
    `set_address` of the block, before the first pulse is emitted. Annotate the row with that fact,
    naming it a **pre-existing** defect that **Phase 142 / VPP-01 and VPP-03** own (route selection and
    mask consolidation), and naming Phase 141's contribution as the explicit `handle->pins >= 32` branch
    that makes the clearing deliberate and observable instead of incidental. Do not silently "fix" the
    row by deleting the claim, and do not silently keep it — naming it is the honest move D-09 asks for.

    Do not touch §"Native (Host) Test Environment" — plan 141-03 owns that paragraph and its
    `native_loop_v131` addition must survive.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -c "
import pathlib
t = pathlib.Path('CLAUDE.md').read_text()
s = t.index('### Algorithm Handlers')
sec = t[s:s+6000]
for tok in ['MSG_ERR_MAX_PULSES','MSG_ERR_ENERGY_CAP','MSG_ERR_PULSE_TOO_WIDE','verify_mode','Phase 142','pins >= 32','99999']:
    assert tok in sec, 'Algorithm Handlers section missing: ' + tok
assert '115' not in sec, 'the unreachable 115 ms figure must not appear'
assert 'native_loop_v131' in t, 'plan 141-03 native-env paragraph must survive'
print('OK Algorithm Handlers rows reconciled')
" && python3 -m pytest tests/ -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - The `0x07` and `0x08` rows attribute the final full-array pass to the
      `verify_mode == VERIFY_PER_PULSE_PLUS_FINAL` branch and name `MSG_ERR_VERIFY` as its failure.
    - The `0x0B` row states the exact-division fact for all three shipped widths (250 / 100 / 50 pulses)
      and the 99999 us worst case under D-03, and does **not** contain the roughly-115 ms figure.
    - All three rows name `MSG_ERR_MAX_PULSES` (0xBD), `MSG_ERR_ENERGY_CAP` (0xBE) and
      `MSG_ERR_PULSE_TOO_WIDE` (0xAE).
    - The `0x08` row explicitly states that the drop bit does not survive the first `set_address` on a
      32-pin part, calls it pre-existing, hands route selection to Phase 142 / VPP-01 and VPP-03, and
      names Phase 141's `handle->pins >= 32` branch as what makes it deliberate.
    - `native_loop_v131` still appears in CLAUDE.md (plan 141-03's paragraph is intact).
    - `python3 -m pytest tests/ -q -o addopts=""` fully green.
  </acceptance_criteria>
  <done>The three Algorithm Handlers rows describe the shipped loop, including the honest `0x08` drop-bit statement and the correct energy-cap arithmetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → committed golden | a golden that records a stale truth reports success while the tree has moved |
| env seam → gate scan target | `FIRESTARTER_BRANCH_SCAN_SOURCE` binds at import, so a mis-set seam can make the gate scan the wrong file and pass vacuously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-GATE | Repudiation | a re-derived golden that hides a real regression | mitigate | `sites[]` comes only from `_extract_predicates`; the tier-1 count is asserted to stay exactly three; and the leg is seen RED on a planted fourth site **and** on a planted removal, both in child processes so the import-time seam binds |
| T-141-SHA | Tampering | `meta.blob_shas` staleness | mitigate | Set from `git hash-object src/proms/eprom.cpp`, which equals `git rev-parse HEAD:<path>` for the already-committed file, and asserted equal in the verify leg |
| T-141-SEAM | Repudiation | a scan seam left set after a planted run | mitigate | Explicit `unset` plus an `env | grep -c` assertion of 0 after the planted runs |
| T-141-DOC | Repudiation | documentation that outlives the code it describes | mitigate | The `0x08` drop-bit claim is annotated as a pre-existing defect with a named owner rather than quietly deleted or quietly kept |
| T-141-SC | Tampering | package installs | accept | No package-manager install; RESEARCH records the phase as not-applicable |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/test_protocol_branch_inventory.py -q -o addopts=""` — 7 passed.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""` — fully green.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` — 141/17 each, unchanged.
- `cd /workspaces/firestarter && pio test -e native_trace_v131` — still RED on stream equality only (D-10; not this plan's to fix).
</verification>

<success_criteria>
- The golden's sites match the live scanner exactly; tier-1 is still three; the blob SHA matches;
  `frozen_for` points at Phase 142; the false `:328` reason is corrected.
- The pinned literal in the test module names the new tier-1 lines, with the count still three, and the
  leg has been seen RED in both directions for the right reason.
- The whole firmware gate suite is green again.
- The three Algorithm Handlers rows are true of the shipped loop, including the honest `0x08` statement.
</success_criteria>

<artifacts_this_phase_produces>
New symbols, files and env names created by Phase 141 as a whole. Treat every entry as **new**, not as
drift against an existing definition:

- **Message IDs / names:** `MSG_ERR_PULSE_TOO_WIDE` (0xAE), `MSG_ERR_MAX_PULSES` (0xBD),
  `MSG_ERR_ENERGY_CAP` (0xBE)
- **Firmware functions:** `mem_util_delay_us`, `mem_util_split_delay`, `eprom_overprogram_us`,
  `eprom_internal_report_budget_failure`
- **Firmware macro:** `MEM_UTIL_DELAY_US_MAX`
- **New PlatformIO env:** `native_loop_v131`
- **New test files:** `firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp`,
  `firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp`
- **New harness symbols:** `loop_readback_reset`, `loop_readback_seed`, `loop_readback_reads`,
  `loop_readback_seeded_count`, `clear_logged_ids`, `logged_id_count`, `logged_id_at`,
  `logged_id_param_count`, `logged_id_param`, `logged_ids_overflowed`, `drive_loop_write`,
  `make_loop_handle`, `LOOP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B`
- **New pytest gate module:** `firestarter/tests/test_write_path_source_contract_v131.py`
- **New meta artifacts:** `.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md`,
  `141-NEW-TRACE.md`, `141-LOOP-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/141-per-byte-program-loop/141-05-SUMMARY.md` when done.
</output>
