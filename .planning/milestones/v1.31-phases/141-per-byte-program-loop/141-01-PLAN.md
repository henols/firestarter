---
phase: 141-per-byte-program-loop
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: [meta, firestarter, firestarter_app]
files_modified:
  - /workspaces/tools/catalog/messages.toml
  - /workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md
  - /workspaces/firestarter/tools/catalog/messages.toml
  - /workspaces/firestarter/tools/catalog/codegen.py
  - /workspaces/firestarter/include/messages.h
  - /workspaces/firestarter_app/tools/catalog/messages.toml
  - /workspaces/firestarter_app/tools/catalog/codegen.py
  - /workspaces/firestarter_app/firestarter/messages.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Three new ERROR-band message IDs exist in the canonical meta catalog and in both sub-repos' generated output"
    - "The firmware header carries the three new IDs as ID-only #defines, produced by codegen and not hand-edited"
    - "The host message table carries the three new constants and their MessageDef entries, ruff-clean as emitted"
    - "A numeric flash/RAM delta prediction and a D-13 inventory-movement prediction are committed BEFORE any eprom.cpp byte changes"
  artifacts:
    - path: "/workspaces/tools/catalog/messages.toml"
      provides: "canonical [[messages]] blocks for 0xAE / 0xBD / 0xBE"
      contains: "MSG_ERR_ENERGY_CAP"
    - path: "/workspaces/firestarter/include/messages.h"
      provides: "generated ID-only #defines for the three new IDs"
      contains: "MSG_ERR_MAX_PULSES"
    - path: "/workspaces/firestarter_app/firestarter/messages.py"
      provides: "generated host constants + MessageDef entries"
      contains: "MSG_ERR_PULSE_TOO_WIDE"
    - path: "/workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md"
      provides: "pre-measurement flash/RAM and D-13 inventory predictions"
      min_lines: 40
  key_links:
    - from: "/workspaces/tools/catalog/messages.toml"
      to: "/workspaces/firestarter/include/messages.h"
      via: "tools/catalog/sync_to_subrepos.sh --language cpp"
      pattern: "MSG_ERR_ENERGY_CAP\\s+0xBE"
    - from: "/workspaces/tools/catalog/messages.toml"
      to: "/workspaces/firestarter_app/firestarter/messages.py"
      via: "tools/catalog/sync_to_subrepos.sh --language python"
      pattern: "MSG_ERR_ENERGY_CAP = 0xBE"
---

<objective>
Author the three new ERROR-band message IDs this phase's failure paths need, in the **canonical
meta-repo catalog**, sync + regenerate them into both sub-repos, and commit the phase's
pre-measurement predictions before any `src/proms/eprom.cpp` byte moves.

Per **D-04**, the two budget limits get **distinct** IDs so the host can tell `max_pulses`
exhaustion from energy-cap exhaustion apart without a second decode layer. **D-03** additionally
requires the pre-flight pulse-too-wide refusal to fail closed *with a named error*, which needs a
third ID — the ERROR band's remaining free slots are exactly `0xAE`, `0xBD`, `0xBE`, `0xBF`, so this
plan consumes three of four and must record that only `0xBF` remains.

Purpose: the loop rewrite (plan 141-04) cannot emit a named diagnostic that does not exist yet, and a
flash-delta measurement is only evidence if the prediction was committed first (Phase 140's
precedent).
Output: three catalog entries, two regenerated sub-repo artifacts, one committed predictions record.

**Requirement ownership:** this plan **contributes to** LOOP-04 and LOOP-05 (it supplies their
diagnostics) and **completes neither**. Do NOT flip any `LOOP-*` checkbox in
`.planning/REQUIREMENTS.md`. LOOP-04 and LOOP-05 are completed by plan 141-08.

**Frontmatter `requirements:` is deliberately EMPTY — do not repopulate it.** `execute-plan.md`'s
`update_requirements` step calls the `requirements.mark-complete` verb unconditionally with *this*
plan's own frontmatter IDs, and that verb performs a **real** regex flip of both the `[ ]` checkbox
and the `Pending` coverage-table cell — it is not a log-only call, and unlike its sibling steps it
carries no documented skip for parallel or worktree execution. Partial IDs in frontmatter would mark
LOOP-04 / LOOP-05 `Complete` in wave 1, before the evidence exists (the exact failure mode this project hit
four times in Phase 116), and would also break plan 141-09's sixteen-line diff self-check. An empty
list makes `update_requirements`'s own "if no requirements field, skip" condition apply. Dimension-1
requirement coverage is carried by plan **141-09**, which declares all eight `LOOP-*` IDs and flips
them in one hand edit after every piece of evidence exists.

**Repo notes (tri-repo, D-04):** the canonical edit is meta's `tools/catalog/messages.toml`. Both
sub-repos receive **synced + generated files only** — commit them in their own repos, on the
milestone branch `gsd/v1.31-27c-programming-algorithm-fidelity`. Never hand-edit a sub-repo copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-per-byte-program-loop/141-CONTEXT.md
@.planning/phases/141-per-byte-program-loop/141-RESEARCH.md
@.planning/phases/141-per-byte-program-loop/141-PATTERNS.md
@.planning/phases/141-per-byte-program-loop/141-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three new ERROR-band IDs in the canonical meta catalog</name>
  <files>/workspaces/tools/catalog/messages.toml</files>
  <read_first>
    - /workspaces/tools/catalog/messages.toml — read the header rules block (lines 1-11) and the
      `0xB0` `MSG_ERR_NOT_BLANK`, `0xB1` `MSG_ERR_WRITE_FAILED` and `0xBC`
      `MSG_ERR_FL4_BOOT_BLOCK_LOCKED` entries. `0xB0` is the exact `[u24 hex_addr, u8]` shape to copy;
      `0xBC` is the last-assigned ERROR slot the new blocks follow in id order.
    - /workspaces/tools/catalog/codegen.py — `validate_catalog()`. Rule 9 (printf-specifier count must
      equal the non-`bytes` param count) is the one that will reject a wrong shape at regen time.
    - /workspaces/firestarter/include/logging_id.h lines 105-110 — the `LOG_ERROR_ID_*` macros the
      firmware call sites will use, so the chosen param shapes must be emittable.
  </read_first>
  <action>
    Append three `[[messages]]` blocks. Do NOT reorder any existing entry — the file header states
    "DO NOT REORDER ENTRIES"; codegen sorts by id and the source order exists only for diff
    readability. Place `0xAE` immediately after the `0xAD` `MSG_ERR_CMD` block, and `0xBD` + `0xBE`
    immediately after the `0xBC` `MSG_ERR_FL4_BOOT_BLOCK_LOCKED` block.

    Entry 1 — D-03's pre-flight refusal (single param so the firmware can emit it with the existing
    `LOG_ERROR_ID_U32` helper and pay no packing code):
    id `0xAE`, name `MSG_ERR_PULSE_TOO_WIDE`, severity `ERROR`, wire_format `id_frame`,
    params one entry of type `u32`, format text naming that the requested pulse width exceeds this
    protocol's per-byte program-energy budget and using exactly one `%lu` specifier (match
    `MSG_ERR_MEM_SIZE_TOO_SMALL` 0xBA's `%lu`-for-`u32` precedent).

    Entry 2 — D-04's `max_pulses` exhaustion:
    id `0xBD`, name `MSG_ERR_MAX_PULSES`, severity `ERROR`, wire_format `id_frame`,
    params `[{ type = "u24", render = "hex_addr" }, { type = "u8" }]`, format text naming the address
    and the pulse count, with exactly two specifiers in the `0xB0` style (`0x%06x` for the address,
    `%d` for the count).

    Entry 3 — D-04's energy-cap exhaustion:
    id `0xBE`, name `MSG_ERR_ENERGY_CAP`, severity `ERROR`, wire_format `id_frame`, the same
    `[u24 hex_addr, u8]` param shape as entry 2, format text naming that the byte exhausted its
    per-byte program-energy budget at that address after that many pulses.

    Do NOT touch `MSG_INFO_RETRIES 0x51` or the debug entry `DBG_PULSE_DELAY_MISMATCH 0x15`. Both
    become caller-less when plan 141-04 removes the block loop; the decision for this phase is
    **leave both IDs assigned and unedited**, and record their now-unreferenced status in the phase
    record (plan 141-09), handing the wording question to Phase 146 / CLOSE-03. Deleting an id risks
    later reuse confusion for zero behavioural gain, and no orphan-ID gate exists.

    Do NOT add a `messages.h`-to-`messages.py` parity test — that is Phase 144 / TEST-04.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import tomllib
d = tomllib.load(open('tools/catalog/messages.toml','rb'))
by = {m['id']: m for m in d['messages']}
assert len(d['messages']) == 76, len(d['messages'])
for i, n in ((0xAE,'MSG_ERR_PULSE_TOO_WIDE'), (0xBD,'MSG_ERR_MAX_PULSES'), (0xBE,'MSG_ERR_ENERGY_CAP')):
    m = by[i]
    assert m['name'] == n, (hex(i), m['name'])
    assert m['severity'] == 'ERROR', m
    assert m['wire_format'] == 'id_frame', m
assert [p['type'] for p in by[0xAE]['params']] == ['u32'], by[0xAE]
assert [p['type'] for p in by[0xBD]['params']] == ['u24','u8'], by[0xBD]
assert [p['type'] for p in by[0xBE]['params']] == ['u24','u8'], by[0xBE]
free = [hex(x) for x in range(0xA0,0xC0) if x not in by]
assert free == ['0xbf'], free
print('OK 76 messages, three new ERROR ids, one free slot left:', free)
"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import tomllib; tomllib.load(open('/workspaces/tools/catalog/messages.toml','rb'))"` exits 0.
    - The catalog holds exactly **76** messages (73 before + 3).
    - `0xAE` is named `MSG_ERR_PULSE_TOO_WIDE` with exactly one `u32` param.
    - `0xBD` is named `MSG_ERR_MAX_PULSES` and `0xBE` `MSG_ERR_ENERGY_CAP`, both with params
      `[u24 hex_addr, u8]` and both `severity = "ERROR"`, `wire_format = "id_frame"`.
    - The only remaining free ERROR-band slot is `0xBF`.
    - `git -C /workspaces diff -- tools/catalog/messages.toml` shows **additions only** — zero
      deletions and zero moved lines outside the three new blocks.
    - `grep -c "MSG_INFO_RETRIES\|DBG_PULSE_DELAY_MISMATCH" /workspaces/tools/catalog/messages.toml`
      returns the same value before and after this task (both entries untouched).
  </acceptance_criteria>
  <done>Three new ERROR-band entries exist in the canonical catalog, parse under `tomllib`, and leave exactly one free ERROR slot.</done>
</task>

<task type="auto">
  <name>Task 2: Sync + regenerate into both sub-repos and verify the regen independently</name>
  <files>/workspaces/firestarter/tools/catalog/messages.toml, /workspaces/firestarter/tools/catalog/codegen.py, /workspaces/firestarter/include/messages.h, /workspaces/firestarter_app/tools/catalog/messages.toml, /workspaces/firestarter_app/tools/catalog/codegen.py, /workspaces/firestarter_app/firestarter/messages.py</files>
  <read_first>
    - /workspaces/tools/catalog/sync_to_subrepos.sh — read all 102 lines. Note lines 84 and 97 are
      `diff -q "$X" "$X"` (a file compared to itself): both are tautologies, so the
      `OK: … regenerated.` messages after steps 2 and 3 **prove nothing**. The genuine invariant
      checks are step 1's per-file `diff` and the cross-sub-repo comparison.
    - /workspaces/firestarter/include/messages.h — the DO-NOT-EDIT header and the aligned ID-only
      `#define` column, e.g. the `MSG_ERR_FL4_BOOT_BLOCK_LOCKED 0xBC` line.
    - /workspaces/firestarter_app/firestarter/messages.py — the `MSG_ERR_WRITE_FAILED = 0xB1`
      constant and its `MessageDef(...)` entry, so the expected generated shape is known before
      running the script.
  </read_first>
  <action>
    Run `bash /workspaces/tools/catalog/sync_to_subrepos.sh` exactly once. Do not hand-edit any file
    it writes: `messages.h` is generated ID-only `#define`s and `messages.py` is emitted ruff-clean
    and format-stable — never normalize either.

    Because the script's step-2 and step-3 success messages are tautological, verify the regeneration
    independently with the two `git diff --stat` invocations named in the verify block, and confirm:
    `firestarter/tools/catalog/codegen.py` and `firestarter_app/tools/catalog/codegen.py` show a
    **zero-byte diff** (all three copies were byte-identical before this change);
    `firestarter/include/messages.h` gains exactly three `#define` lines plus the changed
    `Total messages:` header comment (73 becomes 76 — no test asserts that count, so the comment
    moving is expected, not a gate); `firestarter_app/firestarter/messages.py` gains three constants
    and three `MessageDef` entries plus the same header-count change.

    Commit the meta-repo edit and each sub-repo's generated/synced files **in their own repositories**
    on branch `gsd/v1.31-27c-programming-algorithm-fidelity`. Do not advance the meta repo's
    submodule gitlinks.
  </action>
  <verify>
    <automated>cd /workspaces && bash tools/catalog/sync_to_subrepos.sh && git -C /workspaces/firestarter diff --stat -- include/messages.h tools/catalog/messages.toml tools/catalog/codegen.py && git -C /workspaces/firestarter_app diff --stat -- firestarter/messages.py tools/catalog/messages.toml tools/catalog/codegen.py && grep -E "MSG_ERR_(PULSE_TOO_WIDE|MAX_PULSES|ENERGY_CAP)" /workspaces/firestarter/include/messages.h && grep -E "MSG_ERR_(PULSE_TOO_WIDE|MAX_PULSES|ENERGY_CAP) = 0x" /workspaces/firestarter_app/firestarter/messages.py && diff -q /workspaces/tools/catalog/messages.toml /workspaces/firestarter/tools/catalog/messages.toml && diff -q /workspaces/tools/catalog/messages.toml /workspaces/firestarter_app/tools/catalog/messages.toml && cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""</automated>
  </verify>
  <acceptance_criteria>
    - `/workspaces/firestarter/include/messages.h` contains `MSG_ERR_PULSE_TOO_WIDE` `0xAE`,
      `MSG_ERR_MAX_PULSES` `0xBD` and `MSG_ERR_ENERGY_CAP` `0xBE` as ID-only `#define`s in the
      existing aligned column.
    - `/workspaces/firestarter_app/firestarter/messages.py` contains the three constants **and** three
      `MessageDef` entries with `param_bytes` 4, 4 and 4 respectively.
    - `diff -q` reports the meta `messages.toml` byte-identical to **both** sub-repo copies.
    - `git -C /workspaces/firestarter diff --stat -- tools/catalog/codegen.py` prints nothing
      (zero-byte diff), and the same for `firestarter_app`.
    - `git -C /workspaces/firestarter_app diff -- firestarter/messages.py` shows only additions plus
      the `Total messages:` header line; **no** reflowed or reformatted pre-existing entry.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""` is fully green
      (244 passed) — the D-13 inventory gate is still GREEN at this point because no
      `src/proms/eprom.cpp` byte has moved yet.
  </acceptance_criteria>
  <done>Both sub-repos carry the synced catalog and freshly generated artifacts, verified by independent git diffs rather than by the script's tautological messages, with the firmware gate suite still fully green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the pre-measurement flash/RAM and D-13 inventory predictions</name>
  <files>/workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md</files>
  <read_first>
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-RESEARCH.md §"Flash and RAM budget"
      — the measured branch-tip figures (`uno` 23954/1573, `uno328pb` 24004/1579,
      `leonardo` 26016/2014), the BASE-01 comparison table, and §"Ingredients for the flash-delta
      prediction" (the explicit adds and removes list).
    - /workspaces/firestarter/scripts/baseline/size_baseline_base01.json — the `avr_targets` block
      the merge05 policy compares against.
    - /workspaces/firestarter/tests/golden/protocol_branch_inventory.json — `counts`
      (`total_sites` 24, `protocol_keyed_sites` 3, `other_sites` 21) and `meta.frozen_for`.
    - /workspaces/.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md §6 — the precedent
      for stating a numeric prediction before measuring it.
  </read_first>
  <action>
    Write `141-PREDICTIONS.md` and commit it in the **meta** repo before any plan touches
    `src/proms/eprom.cpp`. It must contain, as numbers and not as prose hedges:

    (a) **Flash delta prediction, per target**, as a signed byte figure for `uno`, `uno328pb` and
    `leonardo`, plus the resulting predicted absolute flash figure against BASE-01's
    23932 / 23976 / 26072. State the growth budget each must respect: **42 B (`uno`), 36 B
    (`uno328pb`), 56 B (`leonardo` — must stay at or below 26072)**, with `uno328pb` named as the
    binding constraint. Record explicitly that F-138-02's 8 B / 2 B figure is an `origin/beta`-tip
    measurement that does **not** apply to this branch (`git merge-base --is-ancestor 6fab4ea HEAD`
    → NO) and re-verify that ancestry claim as part of writing the file.

    (b) **RAM delta prediction: exactly 0** on all three targets, with the note that
    `mismatch_bitmask[DATA_BUFFER_SIZE / 8]` is a **stack** array (64 B Uno-class, 128 B Leonardo) and
    therefore will not appear as a linker `ram_used` win — it is a peak-stack-depth improvement to be
    recorded as a non-metric safety note, not as a RAM delta.

    (c) The **adds/removes ledger** the prediction rests on: adds are the `EPROM_PARAMS[]`
    3 × 12 B PROGMEM plus `EPROM_PARAM_KEYS[]` 3 B (both currently `--gc-sections`-collected and
    landing for the first time because plan 141-04 is the first `src/` consumer), `eprom_params_for()`'s
    linear-scan body, six hoisted `pgm_read_*` reads, the per-byte loop's predicates,
    `eprom_overprogram_us`, `mem_util_split_delay` + `mem_util_delay_us` and their **two** call sites
    (`memory_set_data`'s pulse and `eprom_internal_erase`'s pulse — both pay, for LOOP-07's global
    claim), three new `LOG_ERROR_ID_*` call sites, the `pins >= 32` branch, and the `verify_mode`
    final full-block pass. Removes are `program_mismatched_bytes()`, `verify_and_update_mask()`, the
    `NUMBER_OF_RETRIES` block loop with its 32-bit multiply/divide growth arithmetic, the `memset`,
    and two `LOG_*` call sites. State that deleting `eprom_internal_ensure_regulator_enabled`
    reclaims **0 B** because it is already `--gc-sections`-collected — do not count it.

    (d) **D-13 inventory-movement prediction**: before = 24 total / 3 tier-1 / 21 tier-2. Name the
    tier-2 sites predicted to disappear (`eprom.cpp:119`, `:131`, `:132`, and `:144` only if the
    regulator idempotency guard is restructured) and the tier-2 predicates the new loop is predicted
    to add (the byte-loop bound, the `0xFF` skip, the already-matching skip, the `pulses >= max_pulses`
    check, the `energy_cap_us && accumulated >= energy_cap_us` check, the `pins >= 32` branch, the
    `row == NULL` refusal, D-03's `energy_cap_us > 0 && pulse_delay > energy_cap_us` refusal, the
    `overprogram_factor`/`op_us` guard, the `verify_mode == VERIFY_PER_PULSE_PLUS_FINAL` check and the
    final pass's own loop bound and comparison). Give a predicted after-count for tier-2 and state
    plainly that **the direction may be growth, not shrinkage** — CONTEXT.md D-11's "record the
    shrinkage" framing assumed removals dominate, and a per-byte loop adds handle-field-keyed
    predicates. Growth is not a violation; only a **fourth tier-1 (protocol-keyed) site** is. Predict
    tier-1 stays **exactly 3** at `:71`, the `eprom_write_execute` VPP predicate (kept verbatim, line
    moves) and the `eprom_check_vpp` predicate (untouched, line shifts).

    (e) A one-line statement that this file was committed **before** the first `eprom.cpp` byte moved,
    with the meta-repo commit SHA to be filled in by the measurement plan (141-09) when it quotes this
    file, so the ordering is auditable from git alone.
  </action>
  <verify>
    <automated>set -e
cd /workspaces
test -f .planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md
python3 - <<'PY'
import re, pathlib
t = pathlib.Path('.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md').read_text()
for token in ['uno328pb', '36 B', '42 B', '56 B', '23932', '23976', '26072', 'exactly 0',
              'mismatch_bitmask', 'gc-sections', '24', 'tier-1', 'tier-2', '6fab4ea',
              'eprom_overprogram_us', 'mem_util_delay_us', 'verify_mode']:
    assert token in t, f'missing required token: {token!r}'
signed = re.findall(r'[+-]\d+\s*B', t)
assert len(signed) >= 3, f'expected at least three signed byte predictions, found {signed}'
assert len(t.splitlines()) >= 40, len(t.splitlines())
print('OK predictions artifact:', len(t.splitlines()), 'lines,', len(signed), 'signed byte figures')
PY
cd /workspaces/firestarter
git cat-file -e '6fab4ea^{commit}'
if git merge-base --is-ancestor 6fab4ea HEAD; then echo "FAIL: 6fab4ea IS an ancestor of HEAD"; exit 1; fi
echo "OK: 6fab4ea is NOT an ancestor of HEAD (F-138-02 does not apply)"</automated>
  </verify>
  <acceptance_criteria>
    - `141-PREDICTIONS.md` exists, is at least 40 lines, and contains a **signed** byte prediction for
      each of `uno`, `uno328pb` and `leonardo` (at least three `+N B` / `-N B` figures).
    - It names the 42 / 36 / 56 B growth budgets and identifies `uno328pb` as binding.
    - It states RAM delta **exactly 0** and explains why removing `mismatch_bitmask` will not show as a
      RAM win.
    - It re-verifies and records that `git merge-base --is-ancestor 6fab4ea HEAD` exits non-zero, so
      F-138-02's 8 B / 2 B headroom does not apply to this branch.
    - It predicts tier-1 stays exactly 3 and gives a numeric tier-2 after-count, with an explicit
      statement that growth (not shrinkage) is a legitimate outcome and that only a fourth tier-1 site
      is a violation.
    - The file is committed in the meta repo, and `git -C /workspaces log --oneline -1 --
      .planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md` resolves to a commit that is an
      ancestor of every later commit in this phase.
  </acceptance_criteria>
  <done>A numeric, falsifiable prediction for flash, RAM and D-13 inventory movement is committed in the meta repo before any write-path byte changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host serial → firmware `json_parser.c` | `pulse-delay` arrives as an unclamped `uint32_t`; the diagnostics authored here are what make a refusal legible instead of silent |
| meta catalog → generated sub-repo artifacts | a hand-edited generated file diverges the moment codegen next runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-V5 | Tampering | wire `pulse-delay` reaching a VPE pulse | mitigate | Author `MSG_ERR_PULSE_TOO_WIDE 0xAE` so D-03's pre-flight refusal (plan 141-04) can name the offending value rather than reporting a misleading verify failure |
| T-141-DIAG | Repudiation | budget-exhaustion diagnosis | mitigate | Two distinct IDs (`0xBD`, `0xBE`) per D-04 — the host holds no copy of the parameter table and cannot otherwise tell 255-pulses-by-count from 250-pulses-by-energy apart |
| T-141-GEN | Tampering | generated `messages.h` / `messages.py` | mitigate | Author only in meta, run `sync_to_subrepos.sh`, verify with two independent `git diff --stat` calls because the script's own step-2/step-3 success messages are `diff -q "$X" "$X"` tautologies |
| T-141-SC | Tampering | package installs | accept | No `npm`/`pip`/`cargo` install occurs in any of the three repositories this phase touches; RESEARCH §"Package Legitimacy Audit" records the phase as not-applicable, so no legitimacy checkpoint exists to gate |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""` fully green (244 passed).
- `diff -q` proves all three `messages.toml` copies byte-identical.
- `cd /workspaces/firestarter && pio run -e uno` still succeeds (the header change is ID-only; a
  `#define` with no call site costs 0 B).
</verification>

<success_criteria>
- Exactly three new ERROR-band IDs (`0xAE`, `0xBD`, `0xBE`) exist canonically and in both sub-repos'
  generated output; `0xBF` is the only free ERROR slot left.
- No generated or synced file was hand-edited; `codegen.py` copies show a zero-byte diff.
- `141-PREDICTIONS.md` is committed in the meta repo with signed per-target flash figures, RAM
  delta 0, and a numeric D-13 inventory prediction — before any `eprom.cpp` byte moves.
- `MSG_INFO_RETRIES 0x51` and `DBG_PULSE_DELAY_MISMATCH 0x15` are untouched.
</success_criteria>

<artifacts_this_phase_produces>
New symbols, files and env names created by Phase 141 as a whole. Treat every entry as **new**, not
as drift against an existing definition:

- **Message IDs / names:** `MSG_ERR_PULSE_TOO_WIDE` (0xAE), `MSG_ERR_MAX_PULSES` (0xBD),
  `MSG_ERR_ENERGY_CAP` (0xBE)
- **Firmware functions:** `mem_util_delay_us`, `mem_util_split_delay`, `eprom_overprogram_us`,
  `eprom_internal_report_budget_failure`
- **Firmware macro:** `MEM_UTIL_DELAY_US_MAX`
- **New PlatformIO env:** `native_loop_v131`
- **New test files:** `firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp`,
  `firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp`
- **New harness symbols:** `loop_readback_reset`, `loop_readback_seed`, `loop_readback_reads`,
  `loop_readback_seeded_count`, `clear_logged_ids`, `logged_id_count`, `logged_id_at`,
  `logged_id_param_count`, `logged_id_param`, plus a per-suite `reset_register_cache` copy
- **New pytest gate module:** `firestarter/tests/test_write_path_source_contract_v131.py`
- **New meta artifacts:** `.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md`,
  `141-NEW-TRACE.md`, `141-LOOP-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/141-per-byte-program-loop/141-01-SUMMARY.md` when done.
</output>
