---
phase: 141-per-byte-program-loop
plan: 09
type: execute
wave: 5
depends_on: ["141-05", "141-06", "141-08"]
commits_land_in: [meta, firestarter]
files_modified:
  - /workspaces/.planning/phases/141-per-byte-program-loop/141-NEW-TRACE.md
  - /workspaces/.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md
  - /workspaces/.planning/REQUIREMENTS.md
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03, LOOP-04, LOOP-05, LOOP-06, LOOP-07, LOOP-08]
must_haves:
  truths:
    - "The post-change trace is captured as a committed artifact so Phase 144 has both sides of the diff"
    - "The native_trace_v131 RED is named explicitly as expected, with its determinism leg confirmed still passing"
    - "Per-target flash and RAM are measured against the prediction committed before the work started, and the merge05 policy verdict is recorded"
    - "The phase record states every non-claim, hand-off and orphaned artifact this phase created or discovered"
    - "All eight LOOP requirements are marked complete in one hand edit, after all evidence exists"
  artifacts:
    - path: "/workspaces/.planning/phases/141-per-byte-program-loop/141-NEW-TRACE.md"
      provides: "the post-change trace dump per protocol plus the exact build and run commands"
      min_lines: 40
    - path: "/workspaces/.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md"
      provides: "the phase record: measured deltas, inventory movement, non-claims, hand-offs, run-by-name obligation"
      min_lines: 120
  key_links:
    - from: "/workspaces/.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md"
      to: "/workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md"
      via: "measured-versus-predicted pairing with the prediction's commit SHA"
      pattern: "141-PREDICTIONS"
    - from: "/workspaces/.planning/REQUIREMENTS.md"
      to: "the eight LOOP requirements"
      via: "one hand edit flipping all eight plus their coverage rows"
      pattern: "LOOP-08"
---

<objective>
Close the phase: capture the post-change trace as a committed artifact, measure flash and RAM against the
prediction that was committed before any code moved, write the phase record, and flip all eight `LOOP-*`
requirements in a single hand edit.

Per **D-10** `native_trace_v131` goes RED in this phase and is **not** re-frozen here — Phase 144 /
TEST-06 owns the freeze and the diff, and a fixture re-frozen by the phase that breaks it stops being
evidence. This plan's job is to give Phase 144 **both sides**: the frozen pre-change trace already lives in
`_shared/eprom_v131_expected.h` (untouched, its blob SHA still pinned), and the post-change trace becomes
`141-NEW-TRACE.md`.

Per **D-12** this plan also records the hand-off findings, because the roadmap calls Phase 143
"independent of 140-142 (different repo)" while HOST-02's own named precedent — `mem_util_blank_check`'s
operation-in-progress plus `progress_data` pattern — is a **firmware** pattern. If HOST-02 needs
intra-block emission, part of Phase 143 lands in `firestarter/`. That must be named **before** Phase 143
plans, not after.

Purpose: make the phase's evidence auditable and its non-claims explicit, so `/gsd-verify-work` reads the
one expected RED as expected and Phase 144 and Phase 146 inherit findings rather than surprises.
Output: two meta artifacts and one requirements edit.

**Requirement ownership:** this plan owns **every** `LOOP-*` checkbox flip. Plans 141-01 through 141-08
deliberately flip nothing, so `/workspaces/.planning/REQUIREMENTS.md` is edited exactly once, by one plan,
after all eight requirements' evidence exists.

**Repo notes:** commits land in **meta** only. `firestarter` is named in `commits_land_in` because this
plan **builds and reads** the submodule (three AVR builds, four native envs, the trace dump binary) — a
plan that only reads a submodule still names it, because a worktree leaves submodules empty and a
`files_modified`-only detector under-detects. Do **not** commit anything inside `firestarter`, do not
modify `scripts/baseline/size_baseline.json` (Phase 144 / TEST-08 owns baseline reconciliation), and do
not advance the meta repo's submodule gitlinks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-per-byte-program-loop/141-CONTEXT.md
@.planning/phases/141-per-byte-program-loop/141-RESEARCH.md
@.planning/phases/141-per-byte-program-loop/141-PATTERNS.md
@.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md
@.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the post-change trace and confirm the RED's shape</name>
  <files>/workspaces/.planning/phases/141-per-byte-program-loop/141-NEW-TRACE.md</files>
  <read_first>
    - /workspaces/firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp lines
      340-397 — the `EPROM_V131_TRACE_DUMP` harness: `dump_v131_merged_ready_to_paste`, the
      `test_dump_v131_traces` case that drives all three protocols, the `RUN_TEST` registration under the
      same `#ifdef`, and the `##### <TAG> total=N strobe_overflow=0 timing_overflow=0` banner plus the
      per-entry `{kind, 0xPP pin, 0xVV value, NUL us}, /* index */` format. Note the comment: `pio test`
      swallows `printf`, so the dump must be produced by invoking the **built binary** directly.
    - /workspaces/firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp lines
      288-321 — `assert_v131_protocol_case`'s soundness and determinism wrapper, which drives each block
      **twice** and compares the two runs positionally. This is the leg that must still pass.
    - /workspaces/.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md §7 — the precedent for
      recording a non-CI env's case and suite counts in prose rather than in a baseline JSON.
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-08-SUMMARY.md — the observed
      `native_loop_v131` case and suite counts to record.
  </read_first>
  <action>
    Build `native_trace_v131` with the dump flag and run the binary directly. `pio` **must** run with cwd
    `/workspaces/firestarter` — the gitignored root `platformio.ini` carries two `[platformio]` sections and
    `pio -d <dir>` does not work around it, so every invocation needs a leading
    `cd /workspaces/firestarter &&`. Pass the dump flag through the environment
    (`PLATFORMIO_BUILD_FLAGS="-D EPROM_V131_TRACE_DUMP"`) rather than editing `platformio.ini` — the dump is
    permanently behind that `#ifdef` and no env defines it, and this plan must leave the firmware tree
    unmodified.

    Write `141-NEW-TRACE.md` containing, at minimum: the exact build and run command pair, verbatim and
    copy-pasteable; the full `##### EPROM_V131_TRACE_PROTO_07 / _08 / _0B` banner line for each protocol
    including its `total=`, `strobe_overflow=` and `timing_overflow=` values; and the complete entry list
    per protocol as printed. If a banner reports a non-zero overflow, record it as-is and note which
    protocol truncated — do not shorten the block to force a zero.

    Then confirm the RED's shape, which is **assumption A7** and the one thing RESEARCH flagged for early
    verification. Run `cd /workspaces/firestarter && pio test -e native_trace_v131` and record in the
    artifact: that the suite **compiled** (no build error); that its `RESPONSE_CODE_OK` assertions still
    pass; that the **determinism** assertion still passes — the second drive is positionally identical to
    the first; and that the **only** failure is `v131_assert_stream_equals`'s ordered positional comparison,
    naming the first divergent index. If determinism also fails, STOP and report it: a non-reproducible new
    cadence would poison Phase 144's diff and is **not** part of D-10's expected RED.

    Also record the pre-change-versus-post-change cadence walk for the fixture's own synthetic block
    `{0x3C, 0xFF, 0x55, 0xAA}` with `converge_after` `{0, 0, 2, 1}`, because it is the most legible summary
    of what changed: under the new loop byte 0 is skipped as already-matching (0 pulses), byte 1 is skipped
    by the `0xFF` rule (0 pulses, and **zero reads**), byte 2 takes 2 pulses and byte 3 takes 1 — where the
    old loop pulsed all four bytes on pass 1 and ran three passes. Note the pleasant side effect that the
    pre-change fixture's own data already contains a `0xFF` byte, so LOOP-06 is exercised by the fixture and
    the shrinkage will be legible in the diff Phase 144 reviews.

    Record the `native_loop_v131` case and suite counts observed in plan 141-08, alongside
    `native_trace_v131`'s 5/1 and `native_params_v131`'s 9/1, in prose — **never** in
    `size_baseline.json` or `size_baseline_v131.json`.

    Do **not** write any of this into `_shared/eprom_v131_expected.h`. Its blob SHA is pinned by
    `tests/test_golden_trace_identity_eprom_v131.py`, and re-freezing is Phase 144 / TEST-06's job.
  </action>
  <verify>
    <automated>set -e
cd /workspaces/firestarter
test -z "$(git status --porcelain)" || { echo "FAIL: firmware tree dirty before dump"; git status --porcelain; exit 1; }
echo "OK: firmware tree clean before dump"
PLATFORMIO_BUILD_FLAGS="-D EPROM_V131_TRACE_DUMP" pio test -e native_trace_v131 --without-testing
ls -l .pio/build/native_trace_v131/
cd /workspaces
python3 -c "
import pathlib
p = pathlib.Path('.planning/phases/141-per-byte-program-loop/141-NEW-TRACE.md')
t = p.read_text()
for tok in ['EPROM_V131_TRACE_PROTO_07','EPROM_V131_TRACE_PROTO_08','EPROM_V131_TRACE_PROTO_0B','PLATFORMIO_BUILD_FLAGS','strobe_overflow','determinism','native_loop_v131','native_params_v131','5/1','9/1']:
    assert tok in t, 'missing required content: ' + tok
assert 'eprom_v131_expected.h' in t, 'must state that the frozen fixture was NOT re-frozen'
assert len(t.splitlines()) >= 40, len(t.splitlines())
print('OK new-trace artifact:', len(t.splitlines()), 'lines')
"
cd /workspaces/firestarter
test -z "$(git status --porcelain)" || { echo "FAIL: firmware tree dirty after dump"; git status --porcelain; exit 1; }
echo "OK: firmware tree still clean after dump"</automated>
  </verify>
  <acceptance_criteria>
    - `141-NEW-TRACE.md` contains the verbatim build and run command pair, each `pio` invocation prefixed
      with `cd /workspaces/firestarter &&`, and passes the dump flag via `PLATFORMIO_BUILD_FLAGS` rather
      than by editing `platformio.ini`.
    - It contains all three `##### EPROM_V131_TRACE_PROTO_*` banners with their `total=`,
      `strobe_overflow=` and `timing_overflow=` values, and the full entry list per protocol.
    - It records that `native_trace_v131` **compiled**, that its determinism assertion still **passes**,
      and that the sole failure is `v131_assert_stream_equals`, naming the first divergent index.
    - It records the four-byte cadence walk (0, 0, 2, 1 pulses; byte 1 with **zero** reads) against the old
      loop's three passes over all four bytes.
    - It records `native_loop_v131`'s observed case and suite counts alongside `native_trace_v131` 5/1 and
      `native_params_v131` 9/1, in prose only.
    - It states explicitly that `_shared/eprom_v131_expected.h` was **not** re-frozen and that Phase 144 /
      TEST-06 owns it.
    - `git -C /workspaces/firestarter status --porcelain` is empty both before and after the dump — no
      firmware file was modified to produce it.
  </acceptance_criteria>
  <done>The post-change trace is a committed meta artifact, the expected RED is documented with its determinism leg confirmed passing, and the firmware tree is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Measure flash and RAM against the committed prediction and record the merge05 verdict</name>
  <files>/workspaces/.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md</files>
  <read_first>
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md — the signed per-target
      predictions committed by plan 141-01 before any code moved, and the adds/removes ledger they rest on.
    - /workspaces/firestarter/scripts/check_size_baseline.py — `NATIVE_ENVS` hardcoded to
      `("native", "native_nodevtools")` at :100; `MERGE05_UNO_CLASS_FLASH_BAND = 64` at :107; the
      never-vacuous guard that fails closed when zero envs would be compared; the RAM equality enforcement
      at :224-227; and the manual argv parser at :330-388 recognising `--baseline PATH`, repeated
      `--avr-log ENV=PATH`, repeated `--native-log ENV=PATH`, `--rebuild`, and `--policy merge05`.
    - /workspaces/firestarter/scripts/baseline/size_baseline_base01.json — the BASE-01 `avr_targets`
      figures the merge05 policy compares against (`uno` 23932, `uno328pb` 23976, `leonardo` 26072).
    - /workspaces/firestarter/scripts/baseline/size_baseline.json — the **live** baseline. Read it only to
      confirm you are not about to edit it; Phase 144 / TEST-08 owns reconciling it.
  </read_first>
  <action>
    Take clean, uncontaminated measurements: for each of `uno`, `uno328pb` and `leonardo`, run
    `cd /workspaces/firestarter && pio run -t clean -e <env>` then `cd /workspaces/firestarter && pio run -e <env>`
    as **one uninterrupted invocation per env**, capturing each build log to a file. The baseline's own
    `meta.note` records that a default two-minute timeout truncates a cold toolchain build and silently
    contaminates the measurement, so allow a long timeout and never interleave the three builds.

    Then run the judged verdict:
    `cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=<log> --avr-log uno328pb=<log> --avr-log leonardo=<log>`.
    **Never** pass `native_loop_v131`, `native_trace_v131` or `native_params_v131` to this script — an
    unknown env name raises an uncaught `KeyError` and exits 1, which is a **false** regression signal
    (F-138-05, inherited, accepted, not fixed by this phase). Never pass any of them to
    `check_build_warnings.py` either (exit 2). Also run
    `cd /workspaces/firestarter && python3 scripts/check_build_warnings.py` with only the two pinned native
    envs, to confirm the 1166 warning watermark still holds — it has **zero** headroom, and any new native
    warning turns it RED.

    Begin `141-LOOP-RECORD.md` with a **measured-versus-predicted** table: for each of the three targets,
    the prediction from `141-PREDICTIONS.md` (quoting that file's meta-repo commit SHA so the ordering is
    auditable from git alone), the measured absolute flash and RAM, the signed delta against BASE-01, the
    band, and the remaining headroom. State the RAM delta explicitly and, if it is 0, note that the removed
    `mismatch_bitmask[DATA_BUFFER_SIZE / 8]` stack array (64 B Uno-class, 128 B Leonardo) is a peak-stack
    improvement that deliberately does **not** appear in the linker's static figure. Record the
    `check_size_baseline.py --policy merge05` exit status and its printed verdict verbatim, and the
    `check_build_warnings.py` result.

    **If the merge05 verdict is RED**, do not weaken the policy, do not edit any baseline JSON, and do not
    quietly accept it. Work the reduction ladder in this order, re-measuring after each step, and record
    each step and its measured effect: (1) reduce `MSG_ERR_PULSE_TOO_WIDE`'s payload from `u32` to `u8` in
    the meta catalog and regenerate, if the diagnostic survives it; (2) collapse the `verify_mode` final
    pass to call `memory_verify_execute` directly instead of carrying its own loop, if the call is reachable
    from `eprom.cpp`; (3) delete `eprom_internal_ensure_regulator_enabled` **only** if the loop now uses it
    or it is genuinely dead — and record 0 B if the linker was already collecting it. If the verdict is
    still RED after the ladder, **STOP** and report a blocking finding naming the exact per-target overage,
    every lever tried with its measured effect, and the fact that Phase 144 / TEST-08 owns the baseline
    reconciliation. Do not mark the phase complete over an unexplained RED.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/claude-1000/-workspaces/*/scratchpad/uno.log --avr-log uno328pb=/tmp/claude-1000/-workspaces/*/scratchpad/uno328pb.log --avr-log leonardo=/tmp/claude-1000/-workspaces/*/scratchpad/leonardo.log && test -z "$(git status --porcelain scripts/baseline/)" && echo "OK: no baseline JSON was edited" && cd /workspaces && python3 -c "
import pathlib, re
t = pathlib.Path('.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md').read_text()
for tok in ['141-PREDICTIONS','uno328pb','leonardo','merge05','BASE-01','23932','23976','26072','mismatch_bitmask','check_build_warnings','1166']:
    assert tok in t, 'missing required content: ' + tok
assert re.search(r'RAM.*(delta|Delta).*0', t), 'the RAM delta must be stated explicitly'
print('OK measurement section present')
"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the three AVR targets was measured after `pio run -t clean -e <env>`, in one uninterrupted
      invocation per env, with the build log captured.
    - `141-LOOP-RECORD.md` carries a measured-versus-predicted table with, per target: the prediction, the
      `141-PREDICTIONS.md` commit SHA, the measured absolute flash and RAM, the signed delta against
      BASE-01, the band, and the remaining headroom.
    - The RAM delta is stated explicitly, with the `mismatch_bitmask` peak-stack note if it is 0.
    - `check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json` exits
      **0**, and its verdict text is recorded verbatim.
    - `check_build_warnings.py` result is recorded and the 1166 watermark still holds, with its zero
      headroom named.
    - Neither script was invoked with `native_loop_v131`, `native_trace_v131` or `native_params_v131`
      anywhere in this plan's commands or in any committed script.
    - `git -C /workspaces/firestarter status --porcelain scripts/baseline/` is empty — no baseline JSON was
      edited.
    - If the verdict was RED at any point, every ladder step attempted is recorded with its measured
      effect, and either the final verdict is green or the plan stopped with a blocking finding naming the
      exact per-target overage.
  </acceptance_criteria>
  <done>Flash and RAM are measured cleanly against a prediction committed before the work, the merge05 policy verdict is green and recorded verbatim, and no baseline was moved.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase record's non-claims and hand-offs, then flip all eight requirements</name>
  <files>/workspaces/.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md, /workspaces/.planning/REQUIREMENTS.md</files>
  <read_first>
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-05-SUMMARY.md — the measured D-13
      before-and-after inventory triple and the per-site added/removed list.
    - /workspaces/.planning/phases/141-per-byte-program-loop/141-06-SUMMARY.md,
      141-07-SUMMARY.md and 141-08-SUMMARY.md — the planted-RED transcript inventory and the observed
      `native_loop_v131` case and suite counts.
    - /workspaces/.planning/REQUIREMENTS.md lines 181-201 and the §"v1.31 Coverage" table — the eight
      `LOOP-*` entries and their coverage rows, so exactly sixteen lines change and nothing else.
    - /workspaces/.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md — the record's structure
      and §11's hand-off style, which this record mirrors for Phase 142, 143, 144 and 146.
  </read_first>
  <action>
    Complete `141-LOOP-RECORD.md` with these sections, each of which is a **stated obligation** rather than
    an optional nicety:

    **D-13 inventory movement.** The before triple (24 total / 3 tier-1 / 21 tier-2) and the measured after
    triple, with every site added and removed named. State that tier-1 stayed exactly **3** and that a
    fourth would have been a second algorithm selector. If tier-2 **grew**, say so plainly and correct
    CONTEXT.md D-11's "record the shrinkage" framing as a finding: a per-byte loop adds handle-field-keyed
    predicates, so growth is legitimate movement and only tier-1 growth is a violation.

    **LOOP-03's non-claim, stated plainly.** The end-to-end overprogram path is proven **only** in its
    arithmetic and its gating, never through the loop, because `overprogram_factor` is `0` on all three
    shipped rows and no live row can reach it. Name the pure function, the six boundary inputs it was
    proven at, and the separate assertion that all three live rows emit zero extra pulses. Hand this to
    **Phase 146 / CLOSE-04** alongside F-140-05.

    **What "hard-fails the block" actually does.** Do not let it quietly become "hard-fails the write".
    `eprom_write_execute` operates on one 512-byte (Uno-class) or 1024-byte (Leonardo) block and the host
    streams the rest. Record the traced chain: `response_code = RESPONSE_CODE_ERROR` →
    `_process_incoming_data` (`eprom_operations.cpp:115-117`) returns false immediately and does **not**
    advance `handle->address` → `op_execute_function` (`operation_utils.cpp:186-191`) → the command reports
    finished → `command_done()` (`firestarter.cpp:162-171`) zeroes the control register. Conclusion for
    the record: the firmware processes **no further blocks** — the streaming pump exits, it does not skip
    ahead, so "hard-fails the block" and "the write aborts" are the same event. The host observes the
    ERROR-severity id frame followed by normal command termination. **Phase 143 / HOST-03** has to render
    exactly this.

    **The three new message IDs and the band cost.** `MSG_ERR_PULSE_TOO_WIDE` 0xAE,
    `MSG_ERR_MAX_PULSES` 0xBD, `MSG_ERR_ENERGY_CAP` 0xBE. Record that the ERROR band `0xA0..0xBF` now has
    exactly **one** free slot, `0xBF` — a constraint Phase 142 and Phase 143 need to know before they
    claim one. Record that `MSG_ERR_WRITE_FAILED 0xB1`'s three-param shape
    (`u24 address, u8 retries, u16 bad bytes`) is now emitted by **nothing** on the 27C path, so Phase 143
    must not expect it.

    **The two orphaned catalog IDs.** `MSG_INFO_RETRIES 0x51` and the debug id
    `DBG_PULSE_DELAY_MISMATCH 0x15` are now unreferenced by firmware. The decision taken was to **leave
    both assigned and unedited** — no orphan-ID gate exists, and deleting an id risks later reuse confusion
    for zero behavioural gain. Note that `DBG_PULSE_DELAY_MISMATCH`'s wording ("retrying with increased
    pulse delay") now actively contradicts shipped behaviour and that a wording-only catalog change
    produces a **zero-byte** firmware diff because `messages.h` is ID-only — hand the wording question to
    **Phase 146 / CLOSE-03**.

    **The sixth env's run-by-name obligation.** `native_loop_v131` runs in **no CI leg of either
    repository** — neither `build.yml` nor `beta-build.yml` runs any `pio test` env beyond `native` and
    `native_nodevtools`. Record the exact command (`cd /workspaces/firestarter && pio test -e native_loop_v131`)
    and the observed case and suite counts. Never imply CI coverage.

    **The expected RED.** `native_trace_v131` is RED and deliberately not re-frozen (D-10). Name it here
    explicitly so `/gsd-verify-work` does not read it as a regression, cross-reference
    `141-NEW-TRACE.md`, and record that its determinism leg still passes.

    **Hand-offs, one line each with the owning phase named:**
    D-09's corrected mechanism — the drop bit dies via the `pins < 32` **preserve-mask** guard at
    `memory.cpp:162`, not via a bit collision (on every shipped build `CTRL_ADDRESS_LINE_16` is `0x01` and
    `CTRL_VPP_VPE_DROP_ENABLE` is `0x100`) — and that `0x08` is the row where it bites, with the route
    choice going to **Phase 142 / VPP-01 and VPP-03**, plus the note that the in-file comment stating the
    collision theory was corrected in this phase.
    D-12's finding that HOST-02's own named precedent is a **firmware** pattern
    (`mem_util_blank_check`'s operation-in-progress plus `progress_data`, at `memory.cpp:393-460` — CONTEXT.md's
    `:307-341` citation is stale), so part of **Phase 143** may land in `firestarter/` despite the
    roadmap's "different repo" framing. Name it before Phase 143 plans.
    The milestone **C3** correction: `pulse-delay` is parsed by `extract_long` into an **unclamped**
    `uint32_t` (`json_parser.c:503`), so an over-ceiling `delayMicroseconds` value is reachable **today**,
    before `--pulse-us` ships — C3's "no bare pulse comes near 16383 us" is true of database data (whose
    full set is 10/20/50/100/200/500/1000 us) and false of the wire. Hand to **Phase 146 / CLOSE-04**
    alongside F-140-05 and F-140-07.
    The honest energy-cap ceiling: exactly **50 ms** on every shipped `0x0B` width (200/500/1000 us give
    250/100/50 pulses, `accumulated` landing on exactly 50000), and a worst case of
    `2 x 50000 - 1` = **99999 us** for an arbitrary width under D-03's refusal. State that CONTEXT.md
    D-01's `50000 + 65535` figure of roughly 115 ms is the bound *without* D-03 and is arithmetically
    unreachable now, so the record must not repeat it.
    The two dispositions this phase decided rather than deferred: `verify_mode` is **consumed** as one
    final full-block pass on the two `PLUS_FINAL` rows, and `overprogram_cap_us == 0` yields **0 us**.
    Both are recorded with their reasoning so Phase 144 does not re-litigate them.
    The Phase 144 seam: TEST-01 owns the requirement flip and the consolidated accounting; this phase's
    suite is its **own** verification. Same split as 140-04 versus TEST-01 — record it so Phase 144 does
    not double-author.

    **D-15 evidence inventory.** Count and list every planted-RED transcript this phase produced across
    plans 141-05, 141-06 and 141-08, so the standard Phase 140 set (12 runs across three gates) is visibly
    matched or exceeded.

    Then edit `/workspaces/.planning/REQUIREMENTS.md` **by hand**: flip all eight `LOOP-01` through
    `LOOP-08` entries in §"Per-Byte Program Loop" from `[ ]` to `[x]`, and set all eight rows in
    §"v1.31 Coverage" from `Pending` to `Complete`. Snapshot the file before and diff it afterwards: the
    GSD requirement and roadmap verbs reformat the **whole** file, so this must be a hand edit and the diff
    must touch exactly **sixteen** lines — eight checkboxes and eight coverage rows — and reformat nothing
    else. Touch no `VPP-*`, `HOST-*`, `TEST-*`, `BENCH-*` or `CLOSE-*` row; they belong to Phases 142 to
    146.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import pathlib, re
r = pathlib.Path('.planning/phases/141-per-byte-program-loop/141-LOOP-RECORD.md').read_text()
for tok in ['tier-1','tier-2','overprogram','hard-fails the block','HOST-03','0xBF','MSG_ERR_WRITE_FAILED','MSG_INFO_RETRIES','DBG_PULSE_DELAY_MISMATCH','native_loop_v131','no CI leg','native_trace_v131','Phase 142','Phase 143','Phase 144','Phase 146','99999','verify_mode','planted']:
    assert tok in r, 'phase record missing: ' + tok
assert '115' not in r, 'the unreachable ~115 ms figure must not be repeated'
assert len(r.splitlines()) >= 120, len(r.splitlines())
t = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
sec = t[t.index('### Per-Byte Program Loop'):t.index('### High-Voltage Routing')]
done = sorted(re.findall(r'- \[x\] \*\*(LOOP-\d+)\*\*', sec))
pend = re.findall(r'- \[ \] \*\*(LOOP-\d+)\*\*', sec)
assert done == ['LOOP-01','LOOP-02','LOOP-03','LOOP-04','LOOP-05','LOOP-06','LOOP-07','LOOP-08'], done
assert pend == [], pend
for i in range(1, 9):
    row = '| LOOP-0%d | Phase 141 | Complete |' % i
    assert row in t, 'coverage row not updated: ' + row
assert '| VPP-01 | Phase 142 | Pending |' in t, 'other phases must stay Pending'
print('OK phase record complete;', len(done), 'LOOP requirements marked complete')
" && cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools && pio test -e native_loop_v131 && pio test -e native_params_v131 && python3 -m pytest tests/ -q -o addopts="" && pio run -e uno && pio run -e uno328pb && pio run -e leonardo</automated>
  </verify>
  <acceptance_criteria>
    - `141-LOOP-RECORD.md` is at least 120 lines and contains every named section: the D-13 before-and-after
      triple with per-site attribution; LOOP-03's non-claim; the traced "hard-fails the block" chain with
      the explicit statement that no further blocks are processed and that Phase 143 / HOST-03 renders it;
      the three new IDs with the one-free-slot band note and the `MSG_ERR_WRITE_FAILED` orphaning; the two
      orphaned catalog IDs with the leave-assigned decision; the run-by-name obligation with the exact
      command and observed counts; the expected `native_trace_v131` RED with its determinism leg passing;
      all named hand-offs to Phases 142, 143, 144 and 146; both decided dispositions; and the D-15
      planted-RED inventory.
    - The record states the honest energy-cap ceiling (exactly 50 ms on every shipped width, 99999 us worst
      case) and does **not** repeat the roughly-115 ms figure.
    - If tier-2 grew, the record says so plainly and corrects D-11's "shrinkage" framing as a finding.
    - `.planning/REQUIREMENTS.md` shows all eight `LOOP-*` entries `[x]` and all eight coverage rows
      `Complete`; the diff against the pre-edit snapshot touches exactly **sixteen** lines and reformats
      nothing else; no `VPP-*`, `HOST-*`, `TEST-*`, `BENCH-*` or `CLOSE-*` row changed.
    - Full-suite green at the end: `pio test -e native` and `-e native_nodevtools` at 141/17 each,
      `-e native_loop_v131` and `-e native_params_v131` passing, `pytest tests/` fully green, and all three
      AVR targets building.
    - `native_trace_v131` remains the **one** expected non-green, named as such in the record.
  </acceptance_criteria>
  <done>The phase record states every measurement, non-claim, hand-off and orphaned artifact, and all eight LOOP requirements are marked complete in a single sixteen-line hand edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build cache → recorded measurement | a warm or truncated build silently contaminates a figure that is then recorded as evidence |
| record prose → downstream phases | an unstated non-claim becomes an implied claim the next phase builds on |
| GSD requirement verbs → REQUIREMENTS.md | they reformat the whole file, so an automated flip can produce an unreviewable diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-MEASURE | Repudiation | flash and RAM figures recorded as evidence | mitigate | `pio run -t clean -e <env>` before each measurement, one uninterrupted invocation per env with a long timeout, and the verdict taken from `check_size_baseline.py --policy merge05` against BASE-01 rather than from a hand reading |
| T-141-BASELINE | Tampering | `size_baseline.json` | mitigate | Explicitly not edited; `git status --porcelain scripts/baseline/` asserted empty. Phase 144 / TEST-08 owns reconciliation |
| T-141-KEYERROR | Repudiation | a false regression signal | mitigate | The sixth env's name is never passed to `check_size_baseline.py` (uncaught `KeyError`, exit 1) or `check_build_warnings.py` (exit 2); its counts live in prose only |
| T-141-CLAIM | Repudiation | unstated non-claims | mitigate | LOOP-03's arithmetic-only proof, the block-versus-write abort semantics, the honest energy-cap ceiling, and the no-CI-coverage fact are each an explicit recorded section with a named downstream owner |
| T-141-REQFMT | Tampering | `REQUIREMENTS.md` whole-file reformat | mitigate | Hand edit only, snapshot-and-diff, with an exact sixteen-line change budget and an assertion that no other requirement family moved |
| T-141-SC | Tampering | package installs | accept | No package-manager install in any of the three repositories |
</threat_model>

<verification>
- `cd /workspaces/firestarter && pio run -t clean -e uno && pio run -e uno` (and the same for `uno328pb` and `leonardo`) — all SUCCESS, logs captured.
- `cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=<log> --avr-log uno328pb=<log> --avr-log leonardo=<log>` — exit 0.
- `cd /workspaces/firestarter && pio test -e native && pio test -e native_nodevtools` — 141/17 each.
- `cd /workspaces/firestarter && pio test -e native_loop_v131 && pio test -e native_params_v131` — both pass.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q -o addopts=""` — fully green.
- `cd /workspaces/firestarter && pio test -e native_trace_v131` — the **one** expected RED, on stream equality only.
- `cd /workspaces/firestarter && git status --porcelain` — empty (this plan commits nothing in the submodule).
</verification>

<success_criteria>
- `141-NEW-TRACE.md` gives Phase 144 the post-change side of the diff, with the frozen fixture untouched.
- Flash and RAM are measured cleanly against the pre-committed prediction, and merge05 is green with its
  verdict recorded verbatim; no baseline JSON was edited.
- `141-LOOP-RECORD.md` states every non-claim, hand-off, orphaned artifact and obligation named in
  CONTEXT.md and RESEARCH.md, including the honest energy-cap ceiling and the no-CI-coverage fact.
- All eight `LOOP-*` requirements are complete in one sixteen-line hand edit, with no other requirement
  family touched.
- The only non-green check in the whole phase is `native_trace_v131`, named in the record as expected.
</success_criteria>

<artifacts_this_phase_produces>
New symbols, files and env names created by Phase 141 as a whole. Treat every entry as **new**, not as
drift against an existing definition:

- **Message IDs / names:** `MSG_ERR_PULSE_TOO_WIDE` (0xAE), `MSG_ERR_MAX_PULSES` (0xBD),
  `MSG_ERR_ENERGY_CAP` (0xBE)
- **Firmware functions:** `mem_util_delay_us`, `mem_util_split_delay`, `eprom_overprogram_us`,
  `eprom_internal_report_budget_failure`
- **Firmware macro:** `MEM_UTIL_DELAY_US_MAX`
- **New PlatformIO env:** `native_loop_v131`
- **New test files:** `firestarter/test/native/avr/test_loop_eprom_v131/test_loop_eprom_v131.cpp`,
  `firestarter/test/native/avr/test_loop_eprom_v131/host_stubs.cpp`
- **New harness symbols:** `loop_readback_reset`, `loop_readback_seed`, `loop_readback_reads`,
  `loop_readback_seeded_count`, `clear_logged_ids`, `logged_id_count`, `logged_id_at`,
  `logged_id_param_count`, `logged_id_param`, `logged_ids_overflowed`, `drive_loop_write`,
  `make_loop_handle`, `LOOP_BUS_CONFIG_0x07` / `_0x08` / `_0x0B`
- **New pytest gate module:** `firestarter/tests/test_write_path_source_contract_v131.py`, with the env
  seam `FIRESTARTER_WRITE_PATH_SCAN_SOURCE`
- **New meta artifacts:** `.planning/phases/141-per-byte-program-loop/141-PREDICTIONS.md`,
  `141-NEW-TRACE.md`, `141-LOOP-RECORD.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/141-per-byte-program-loop/141-09-SUMMARY.md` when done.
</output>
