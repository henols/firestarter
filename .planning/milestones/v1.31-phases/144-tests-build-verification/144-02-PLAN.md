---
phase: 144-tests-build-verification
plan: 02
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter_app
reads_repos: [firestarter]
files_modified:
  - firestarter_app/tests/test_cap03_ack_layout_parity.py
  - firestarter_app/tests/fixtures/planted_cap03_literal_index.cpp
  - firestarter_app/tests/fixtures/planted_cap03_truncated_length.cpp
  - firestarter_app/tests/scan_paths.py
autonomous: true
requirements: [TEST-07]

must_haves:
  truths:
    - "The firmware MSG_OK_READY pack order and the host _decode_id_frame offsets are compared against each other by a committed gate — the comparison neither repo performed before (D-17)"
    - "The host is proven to read the CAP-03 budget at the COMPUTED ver_end, never at a fixed index"
    - "A one-sided wire-layout change (BF-1's shape) fails the gate with a message naming the literal index and the computed offset it should have been"
    - "A silent capability loss (budget bytes dropped from the emitted length) fails the gate loudly"
    - "The gate resolves its firmware scan target via fw_path so a firmware rename is a named MissingScanTargetError, never a silent skip"
  artifacts:
    - path: "firestarter_app/tests/test_cap03_ack_layout_parity.py"
      provides: "D-17's cross-repo CAP-03 byte-layout parity gate plus two planted-violation legs"
      contains: "FIRMWARE_ACK_SOURCE"
      min_lines: 250
    - path: "firestarter_app/tests/fixtures/planted_cap03_literal_index.cpp"
      provides: "D-18 plant: budget written at a literal _ready[13] instead of the computed offset"
    - path: "firestarter_app/tests/fixtures/planted_cap03_truncated_length.cpp"
      provides: "D-18 plant: emitted length omits the two budget bytes"
  key_links:
    - from: "firestarter_app/tests/test_cap03_ack_layout_parity.py"
      to: "firestarter/src/firestarter.cpp"
      via: "fw_path(\"src\", \"firestarter.cpp\") behind requires_fw"
      pattern: "fw_path\\(\\s*\"src\",\\s*\"firestarter\\.cpp\"\\s*\\)"
    - from: "firestarter_app/tests/test_cap03_ack_layout_parity.py"
      to: "firestarter_app/firestarter/serial_comm.py"
      via: "source read of _decode_id_frame's offsets"
      pattern: "ver_end"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under firestarter/src/ (D-04) — the firmware source is a READ-ONLY scan target here"
    - "MUST NOT run the full host suite, and MUST NOT run tests/test_py32_flash_map_host.py, in this plan — it asserts the SIBLING firmware repo's porcelain, and a firmware plan is running concurrently in wave 1 (Pitfall 7 / F-09)"
    - "MUST NOT invent a new env seam for this module — FIRESTARTER_FW_ROOT already overrides the ROOT ONLY, and fw_presence.py:66-76 states why the marker name stays hardcoded (S5)"
    - "MUST NOT use monkeypatch.setenv to reach fw_presence's names — they bind at import and skipif binds at collection; the planted legs use monkeypatch.setattr on THIS module's path constant (S6)"
    - "MUST NOT put requires_fw on the fixture-driven planted legs — that is what keeps them alive in D-16's absent-path run"
    - "MUST NOT use a bespoke `not path.exists()` presence proxy — tools/check_no_exists_proxy.py lints for that shape"
    - "MUST NOT lower tests/test_scan_paths_resolve.py's _FLOOR (6) or remove any existing CROSS_REPO_TEST_PATHS entry"
    - "MUST NOT stage the repo's 8 pre-existing untracked entries (.coverage, .planning/config.json, SECURITY.md, four datasheets/*.pdf, write_test_port.sh) — stage named paths only; never `git add -A`, never `git commit -a`, never a helper that stages everything"
    - "MUST NOT restate the CAP-03 budget arithmetic in Python — assert that eprom_block_budget_s is CALLED and that the index expressions are what they are"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
---

<objective>
Build D-17's CAP-03 byte-layout parity gate in the host repo: assert the firmware's `MSG_OK_READY` pack
order `[buffer u16 BE][hw_rev u8][ver_len u8][ver bytes][budget u16 BE]` against the host decoder's live
offsets, including the computed `ver_end` the budget is read at. Then prove it under D-18 with two
committed planted fixtures.

Purpose: BF-1 was a two-repo protocol with nothing comparing the sides. It went unnoticed for three
milestones and made the v1.31 app refuse every connection to a v1.31 firmware build. The firmware side is
already pinned as a source contract (`firestarter/tests/test_ack_layout_source_contract_v143.py`) and the
host side is already pinned behaviourally (`firestarter_app/tests/test_hw_revision_gate.py`), and BOTH
modules' docstrings hand the missing cross-repo comparison to Phase 144 / TEST-07 by name. This plan's
contribution is exactly that comparison — not a third copy of either side. Say so in the module docstring
or a reviewer will reasonably read it as duplication.

Output: `firestarter_app/tests/test_cap03_ack_layout_parity.py`, two `planted_cap03_*.cpp` fixtures, and
one new `CROSS_REPO_TEST_PATHS` entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and paths this plan creates (new — not drift):

- New file: `firestarter_app/tests/test_cap03_ack_layout_parity.py`
- New files: `firestarter_app/tests/fixtures/planted_cap03_literal_index.cpp`,
  `firestarter_app/tests/fixtures/planted_cap03_truncated_length.cpp`
- New module-level names: `FIRMWARE_ACK_SOURCE`, `HOST_DECODER_SOURCE`, `_FIXTURES_DIR`,
  `_FIXTURE_LITERAL_INDEX`, `_FIXTURE_TRUNCATED_LENGTH`, `_WIRE_LAYOUT_COMMENT`,
  `_extract_ready_pack_sites`, `_extract_host_offsets`, `_check_index_identity`,
  `_check_budget_offset_is_computed`, `_check_emitted_length_includes_budget`
- New test names: `test_firmware_pack_order_comment_matches_the_wire_layout`,
  `test_firmware_and_host_agree_on_indices_zero_through_three`,
  `test_budget_is_written_and_read_at_the_computed_offset`,
  `test_both_sides_use_big_endian_for_both_u16_fields`,
  `test_emitted_length_includes_the_two_budget_bytes`,
  `test_host_uses_no_bare_integer_index_above_three_to_reach_the_budget`,
  `test_scan_targets_are_non_vacuous`,
  `test_gate_fails_closed_on_an_unreadable_firmware_path`,
  `test_this_module_cannot_be_silently_skipped`,
  `test_planted_literal_index_is_detected`,
  `test_planted_truncated_emitted_length_is_detected`
- New `ScanPathEntry` in `firestarter_app/tests/scan_paths.py`: `"src/firestarter.cpp"` resolved by
  `("test_cap03_ack_layout_parity.py",)`

No existing symbol is renamed or removed by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the cross-repo CAP-03 layout parity gate and register its scan path</name>
  <files>firestarter_app/tests/test_cap03_ack_layout_parity.py, firestarter_app/tests/scan_paths.py</files>
  <read_first>
    - `firestarter_app/tests/test_revision_constants_parity.py` — THE analog (PATTERNS §3, match quality "exact"). Read: the `from tests.fw_presence import fw_path, requires_fw` import at :128 and the `FIRMWARE_HEADER` block at :130-148 (the path constant that doubles as the fixture-injection seam); the requires_fw / no-requires_fw split rationale at :713-725; the planted legs at :727-748; the fail-closed unreadable-path leg at :342-353 and :855-866.
    - `firestarter_app/tests/fw_presence.py` — read in full (141 lines): the import-time binding warning :35-45, the single seam :77-80, the `.git` marker probe :86-88, `requires_fw` :102, `MissingScanTargetError` :105, `fw_path` :117-140.
    - `firestarter_app/firestarter/serial_comm.py` :344-442 — `_decode_id_frame`: `params_bytes = body[1:-1]` (:388), the buffer unpack (:394-401), `hw_revision` (:408-409), `ver_end = 4 + params_bytes[3]` (:410), the identity slice (:411-414), the budget unpack at `params_bytes[ver_end : ver_end + 2]` (:430-441); and `WRITE_BUDGET_MAX_S = 14400` at :77.
    - `firestarter/src/firestarter.cpp` :160-212 — the `_ready` pack block inside `init_programmer_framed`, including the wire-layout comment at :168 and the emit at :208. READ ONLY (D-04).
    - `firestarter/tests/test_ack_layout_source_contract_v143.py` :28-35 (the docstring paragraph that hands this comparison to Phase 144 / TEST-07) and :372-386 (the firmware-side "no bare index above 3" assertion whose SHAPE to mirror on the host side — do not copy its `_ready[...]` regexes wholesale).
    - `firestarter_app/tests/test_hw_revision_gate.py` :174-188 — `_cap03_params`, the existing host-side behavioural pin whose docstring also hands the standing gate to Phase 144.
    - `firestarter_app/tests/scan_paths.py` :94-125 — the `ScanPathEntry` shape and the existing six `CROSS_REPO_TEST_PATHS` entries; `firestarter_app/tests/test_scan_paths_resolve.py` :47 (`_FLOOR = 6`, `>=` semantics).
    - `firestarter_app/pyproject.toml` :107-111, :174 — `addopts = "-ra -q"`, ruff `target-version = "py39"` and `line-length = 88`, the mypy watermark comment.
  </read_first>
  <action>
Create `firestarter_app/tests/test_cap03_ack_layout_parity.py`.

Plumbing, copied structurally from the analog: `from tests.fw_presence import fw_path, requires_fw`; ONE
firmware path constant `FIRMWARE_ACK_SOURCE = fw_path("src", "firestarter.cpp")`, with the comment
recording that it doubles as the fixture-injection seam the planted legs `monkeypatch.setattr`. Resolve
the host decoder as `HOST_DECODER_SOURCE = Path(firestarter.serial_comm.__file__)` (import the module and
take its `__file__`, so a host-side move is followed automatically) — and assert in the non-vacuity leg
that it resolves inside the app repo root. Do NOT add an env seam; do NOT write a
`not path.exists()` proxy.

Module docstring must state, in this order: (1) what this gate is — the cross-repo comparison neither
`test_ack_layout_source_contract_v143.py` (firmware side, source contract) nor `test_hw_revision_gate.py`
(host side, behavioural, 27 cases) performs, quoting the v143 module's own hand-off sentence; (2) that
`requires_fw` is the ONLY skip marker, keyed on `../firestarter/.git`, and that the fixture-driven
planted legs deliberately carry NO decorator so they stay live in an absent-firmware run (the property
`test_revision_constants_parity.py` documents at :713-725 and D-16 measures); (3) the honest asymmetry
non-claim from F-10 — the firmware clamps `_vlen` to `<= 32` and sizes `_ready[4 + 32 + 2]`, while the
host applies no upper bound on `params_bytes[3]` and relies only on `ver_end <= len(params_bytes)`; that
is safe, not a defect, so this gate proves the two sides agree on the LAYOUT, and must not be read as
proving they agree on the BOUNDS.

Extractors (comment-strip the firmware text first, using the same-shape-whitespace technique so a
reported line number is real):

`_extract_ready_pack_sites(text)` returns the firmware-side facts as a structured dict: the wire-layout
comment string, the `_ready` declaration's sizing expression, the index expression used for each of bytes
0, 1, 2, 3, the `memcpy` destination offset, the two budget index expressions, and the emitted-length
expression. The load-bearing forms to locate, as they appear at `firestarter.cpp:189-203`:
`uint8_t _ready[4 + 32 + 2];`, `_ready[0] = (DATA_BUFFER_SIZE >> 8) & 0xFF`,
`_ready[1] = DATA_BUFFER_SIZE & 0xFF`, `_ready[2] = rurp_get_hardware_revision()` with the
`#else`-arm `_ready[2] = 0xFE`, `_ready[3] = _vlen;`, `memcpy(_ready + 4, _ver, _vlen);`,
`_ready[4 + _vlen] = (_budget >> 8) & 0xFF`, `_ready[4 + _vlen + 1] = _budget & 0xFF`, and
`LOG_OK_ID_BYTES(MSG_OK_READY, _ready, (uint8_t)(4 + _vlen + 2));`. Build every negative-assertion
needle by concatenation from at least two literal pieces, and add the self-check leg proving no needle
appears verbatim in this module's own source.

`_extract_host_offsets(text)` returns the host-side facts: the `params_bytes = body[1:-1]` slice, the
`struct.unpack(">H", params_bytes[:2])` buffer read, the `params_bytes[2]` hw-revision read, the
`ver_end = 4 + params_bytes[3]` assignment, the `params_bytes[4:ver_end]` identity slice, and the
`struct.unpack(">H", params_bytes[ver_end : ver_end + 2])` budget read.

Legs to author:

1. `test_firmware_pack_order_comment_matches_the_wire_layout` — the firmware source contains the
   documented order string `[buffer_size u16 BE][hw_revision u8][ver_len u8][ver bytes][write_budget_s
   u16 BE]`, and the five pack sites appear in that same positional order in the text.
2. `test_firmware_and_host_agree_on_indices_zero_through_three` — index identity: firmware `_ready[k]`
   corresponds to host `params_bytes[k]` for k in 0,1,2,3, asserted pairwise with a message naming the
   index, the firmware expression and the host expression on any mismatch.
3. `test_budget_is_written_and_read_at_the_computed_offset` — the central assertion. The firmware writes
   the budget at `4 + _vlen` / `4 + _vlen + 1`, and the host reads it at `ver_end` where
   `ver_end = 4 + params_bytes[3]`. On failure the message must name BOTH the offending literal index and
   the computed offset it should have been.
4. `test_both_sides_use_big_endian_for_both_u16_fields` — firmware `>> 8` / `& 0xFF` pairs for buffer and
   budget; host `struct.unpack(">H", ...)` for both.
5. `test_emitted_length_includes_the_two_budget_bytes` — the emit expression is `(uint8_t)(4 + _vlen + 2)`;
   a `+ 2`-less form is a silent capability loss that leaves the host attribute `None` forever.
6. `test_host_uses_no_bare_integer_index_above_three_to_reach_the_budget` — mirror the SHAPE of
   `test_ack_layout_source_contract_v143.py:372-386` on the host side: within `_decode_id_frame`, no bare
   integer subscript greater than 3 may be used to reach the budget bytes.
7. `test_scan_targets_are_non_vacuous` — both sides. Assert `FIRMWARE_ACK_SOURCE` is a file and non-empty,
   `HOST_DECODER_SOURCE` resolves inside the app repo root, and both extractors return non-empty
   structures against the currently-resolved targets. A silently-empty extraction would make every
   negative assertion above pass vacuously.
8. `test_gate_fails_closed_on_an_unreadable_firmware_path` — `monkeypatch.setattr` this module's
   `FIRMWARE_ACK_SOURCE` to a non-existent `tmp_path` file and assert the extraction raises
   `AssertionError` with a message naming the missing path. Never a skip.
9. `test_this_module_cannot_be_silently_skipped` and the needle-hygiene twin — concatenation-needle form.

Decorate legs 1-6 with `@requires_fw` (they read the real firmware source); legs 7-9 need no marker
except where they read `FIRMWARE_ACK_SOURCE` (leg 7's firmware half gets `@requires_fw`; split it if
that is cleaner than one decorated leg).

Then add one entry to `firestarter_app/tests/scan_paths.py`'s `CROSS_REPO_TEST_PATHS`:
`ScanPathEntry("src/firestarter.cpp", ("test_cap03_ack_layout_parity.py",))`, appended in the existing
style. Do NOT change `_FLOOR` in `test_scan_paths_resolve.py` (it is `>=`, so 7 entries still satisfy 6),
and do not remove or reorder existing entries.

Write to ruff `py39` / `line-length = 88` from the start.

Commit named paths only: `git add tests/test_cap03_ack_layout_parity.py tests/scan_paths.py`. Never
`git add -A`, never `git commit -a`, and never a commit helper that stages everything — the repo carries
8 pre-existing untracked entries (four datasheet PDFs among them) that must stay untracked.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q -rs 2>&1 | tail -6</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_scan_paths_resolve.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check tests/test_cap03_ack_layout_parity.py tests/scan_paths.py && .venv/ci-replica/bin/ruff format --check tests/test_cap03_ack_layout_parity.py tests/scan_paths.py</automated>
    <automated>cd /workspaces/firestarter_app && grep -c "monkeypatch.setenv" tests/test_cap03_ack_layout_parity.py | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q -rs` reports 0 failed and 0 skipped (the sibling firmware repo is present), with at least 9 passed.
    - `.venv/ci-replica/bin/python -m pytest tests/test_scan_paths_resolve.py -o addopts="" -q` reports 0 failed with the new 7th entry present; `_FLOOR` in that module is still the literal `6`.
    - `ruff check` and `ruff format --check` both exit 0 on the two touched Python files.
    - The module contains `fw_path("src", "firestarter.cpp")` and contains no `os.environ` read and no `path.exists()` presence proxy.
    - `grep -c "monkeypatch.setenv" tests/test_cap03_ack_layout_parity.py` is 0.
    - `git -C /workspaces/firestarter status --porcelain -- src/` is empty and `git -C /workspaces/firestarter rev-parse HEAD:src/firestarter.cpp` is unchanged from the value it had before this task.
    - `git -C /workspaces/firestarter_app status --porcelain` still lists exactly the 8 pre-existing untracked entries and no staged additions beyond the two named files.
    - The module docstring quotes `test_ack_layout_source_contract_v143.py`'s hand-off sentence and states the bounds-vs-layout non-claim.
  </acceptance_criteria>
  <done>The cross-repo layout gate exists, passes unskipped on the replica interpreter, is ruff-clean, and `src/firestarter.cpp` is registered in the cross-repo scan inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Author both committed planted fixtures, prove RED and GREEN, and record the pair</name>
  <files>firestarter_app/tests/fixtures/planted_cap03_literal_index.cpp, firestarter_app/tests/fixtures/planted_cap03_truncated_length.cpp, firestarter_app/tests/test_cap03_ack_layout_parity.py</files>
  <read_first>
    - `firestarter_app/tests/fixtures/planted_constants_value_drift.h` :1-40 — the six-element fixture header comment to copy structurally (consuming module + plan; never compiled and in no build filter; the path constant it is injected through; exactly ONE planted change with both the planted and real values quoted; what it deliberately does NOT trip; the "do NOT fix this file" warning with the hollowing consequence).
    - `firestarter_app/tests/test_revision_constants_parity.py` :727-748 — the five-move planted-leg shape: assert the fixture `is_file()` first, `monkeypatch.setattr(sys.modules[__name__], "<PATH_CONST>", fixture)`, call the SAME helper the live leg calls, assert on message content naming specific values, assert the OTHER legs' phrases are absent (leg isolation).
    - `firestarter_app/tests/test_py32_flash_map_host.py` :214-249 — `_git_hash_object` / `_git_porcelain`, the host-repo twin of the fail-closed git helpers, for the "the plant never touched the real file" assertion.
    - `firestarter/src/firestarter.cpp` :190-209 — the exact region the two fixtures are faithful copies of, minus one planted change each. READ ONLY.
    - `.planning/phases/144-tests-build-verification/144-VALIDATION.md` § "Per-gate planted-violation specification" — rows 5 and 6 (D-17 CAP-03 layout, and D-17 second plant). Lift the RED/GREEN requirements verbatim.
  </read_first>
  <action>
Create two committed fixtures under `firestarter_app/tests/fixtures/`, each a minimal, standalone,
never-compiled `.cpp` carrying a faithful copy of `firestarter/src/firestarter.cpp`'s `_ready` pack region
(the wire-layout comment plus the block from the `_ready` declaration through the `LOG_OK_ID_BYTES` emit),
with exactly ONE planted change each:

- `planted_cap03_literal_index.cpp` — `_ready[4 + _vlen]` becomes the literal `_ready[13]` (and its
  sibling `_ready[4 + _vlen + 1]` becomes `_ready[14]`, since a single-sided edit would leave a
  self-contradictory pair). State in the header comment that the planted value is `13`/`14` and the real
  firmware writes `4 + _vlen` / `4 + _vlen + 1`. This is BF-1's shape: a wire layout changed on one side
  with nothing comparing the two.
- `planted_cap03_truncated_length.cpp` — the emit becomes `(uint8_t)(4 + _vlen)` instead of
  `(uint8_t)(4 + _vlen + 2)`, so the two budget bytes are packed but never emitted. State that this is the
  silent-capability-loss shape: the host's `write_block_budget_s` would stay `None` forever with no error
  anywhere.

Two fixtures, never one two-drift fixture — leg isolation is the reason, and each header comment must say
so explicitly (element 5 of the six).

Add the two planted legs to the gate module, with NO `requires_fw` decorator (they read committed
fixtures, so they stay live in an absent-firmware run):

- `test_planted_literal_index_is_detected` — assert the fixture `is_file()`, then
  `monkeypatch.setattr(sys.modules[__name__], "FIRMWARE_ACK_SOURCE", _FIXTURE_LITERAL_INDEX)`, then call
  the SAME helper `test_budget_is_written_and_read_at_the_computed_offset` calls (never a parallel
  reimplementation) inside `pytest.raises(AssertionError)`. Assert the message names the literal index
  `13` AND the computed offset `4 + _vlen`. Assert the truncated-length leg's phrase is ABSENT (leg
  isolation).
- `test_planted_truncated_emitted_length_is_detected` — same shape against
  `_FIXTURE_TRUNCATED_LENGTH` and `_check_emitted_length_includes_budget`; assert the message names the
  observed `(uint8_t)(4 + _vlen)` and the required `+ 2`; assert the literal-index leg's phrase is ABSENT.

Both legs must run the V12 ceremony against the REAL firmware source: capture `fw_path("src",
"firestarter.cpp")` BEFORE any monkeypatch, `git hash-object` it before and after, assert equality, and
assert `_git_porcelain(<firmware root>)` is empty at the end. (The firmware repo is expected clean; if it
is not, stop and report — a wave-1 firmware plan may still be mid-flight, which is exactly the coupling
Pitfall 7 names.)

Then capture the D-18 evidence pair for the SUMMARY, verbatim, both halves:
- RED: run each planted leg with `-q` and paste the output; then, separately, demonstrate the underlying
  failure directly by pointing the gate at the fixture and pasting the raised AssertionError text, so the
  RED transcript shows the message content (the literal index and the computed offset), not just a
  green planted leg.
- GREEN: the full-module run against the REAL firmware source, plus the non-vacuity leg's reported figures
  (the firmware pack sites found and the host offsets found), so the GREEN is attributed to a non-empty
  two-sided extraction rather than to an unreachable leg.

Commit named paths only (the two fixtures plus the gate module). Never `git add -A`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q -rs 2>&1 | tail -6</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py::test_planted_literal_index_is_detected tests/test_cap03_ack_layout_parity.py::test_planted_truncated_emitted_length_is_detected -o addopts="" -q 2>&1 | tail -4</automated>
    <automated>cd /workspaces/firestarter_app && ls tests/fixtures/planted_cap03_literal_index.cpp tests/fixtures/planted_cap03_truncated_length.cpp && grep -c "requires_fw" tests/fixtures/planted_cap03_literal_index.cpp | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0 && git rev-parse HEAD:src/firestarter.cpp</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check tests/test_cap03_ack_layout_parity.py && .venv/ci-replica/bin/ruff format --check tests/test_cap03_ack_layout_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - Both planted legs pass; the SUMMARY carries a verbatim RED transcript for each whose text contains, for plant 1, both `13` and `4 + _vlen`, and for plant 2, both `(uint8_t)(4 + _vlen)` and the required `+ 2`.
    - Each planted leg asserts the OTHER plant's message phrase is absent (leg isolation), and each calls the same helper its live counterpart calls.
    - Each fixture's header comment carries all six elements, including the "do NOT fix this file" warning and the explicit statement of what it deliberately does NOT trip.
    - `git -C /workspaces/firestarter rev-parse HEAD:src/firestarter.cpp` is identical before and after the planted runs, and `git -C /workspaces/firestarter status --porcelain` is empty.
    - The full module run on the replica reports 0 failed and 0 skipped, at least 11 passed.
    - The SUMMARY's GREEN half quotes the two-sided extraction figures from the non-vacuity leg.
    - `ruff check` / `ruff format --check` exit 0 on the module.
    - `git -C /workspaces/firestarter_app status --porcelain` still shows exactly the 8 pre-existing untracked entries.
  </acceptance_criteria>
  <done>Both plants are committed, seen RED with locating messages, and the gate is seen GREEN over a non-empty two-sided extraction, with the real firmware source provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware repo → host gate | The host gate reads firmware source text from a sibling repository it does not control |
| committed fixture → gate | The planted fixtures are inputs the gate must treat as untrusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-06 | Repudiation | The gate skipping silently when the sibling firmware repo is absent | mitigate | `requires_fw` produces a named skip with a canonical reason naming the probed `.git` marker; the two fixture-driven planted legs carry NO decorator so the failure modes stay exercised even with no checkout (F-11 measured 6 passed / 8 skipped on the analog) |
| T-144-07 | Repudiation | A firmware rename turning a PASS into a silent SKIP at exit 0 | mitigate | `fw_path` raises `MissingScanTargetError` naming the resolved path; `src/firestarter.cpp` registered in `CROSS_REPO_TEST_PATHS` so a rename is ONE named failure, not N anonymous skips |
| T-144-08 | Repudiation | Both extractors returning empty, making every negative assertion vacuous | mitigate | `test_scan_targets_are_non_vacuous` asserts both sides non-empty; `test_gate_fails_closed_on_an_unreadable_firmware_path` proves an unreadable path is an error, never a pass |
| T-144-09 | Tampering | A planted leg writing into the real firmware source | mitigate | Committed fixtures only; `monkeypatch.setattr` on a module attribute (never a write); `git hash-object` before/after plus sibling-repo porcelain asserted clean |
| T-144-10 | Spoofing | A layout change on one side of a two-repo protocol going unnoticed (BF-1) | mitigate | This gate IS the mitigation: index identity for k in 0..3, the computed-`ver_end` budget offset, big-endian on both sides, and the emitted length including both budget bytes |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan; RESEARCH § Package Legitimacy Audit records "not applicable" for the phase |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/test_cap03_ack_layout_parity.py tests/test_scan_paths_resolve.py -o addopts="" -q -rs` — 0 failed, 0 skipped.
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check tests/ firestarter/ && .venv/ci-replica/bin/ruff format --check tests/ firestarter/` — both exit 0 (CI-scoped form, `ci.yml` :81 / :84 — C-02).
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/python tools/check_mypy_watermark.py` — exit 0 (watermark 35; a below-watermark `INFO:` line is an invitation, not a pass to celebrate — do not lower it here).
- `cd /workspaces/firestarter && git status --porcelain` — empty (the firmware source stayed a read-only scan target).
- Do NOT run the full host suite or `tests/test_py32_flash_map_host.py` in this plan (Pitfall 7); plan 144-06 owns that, after every firmware commit has landed.
</verification>

<success_criteria>
- The cross-repo CAP-03 comparison exists as a committed gate, passes unskipped, and is ruff-clean at py39/88.
- Both named plants are seen RED with messages naming the literal index and the computed offset / the missing `+ 2`; the GREEN is attributed to a non-empty two-sided extraction.
- `src/firestarter.cpp` is registered in the cross-repo scan inventory without lowering `_FLOOR`.
- No firmware file changed; the app repo's 8 pre-existing untracked entries remain untracked.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-02-SUMMARY.md` when done. It MUST contain both
D-18 transcripts verbatim (two REDs with their message text, one GREEN full-module run with the
extraction figures quoted), and the bounds-vs-layout non-claim as written into the module docstring.
</output>
