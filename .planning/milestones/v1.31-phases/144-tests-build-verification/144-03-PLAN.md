---
phase: 144-tests-build-verification
plan: 03
type: execute
wave: 2
depends_on: ["144-01"]
commits_land_in: firestarter
files_modified:
  - firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h
  - firestarter/test/native/avr/_shared/eprom_v131_expected.h
  - firestarter/tests/golden/eprom_v131_trace_inventory.json
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "The pre-change golden trace survives as a historical artifact whose Phase-138 blob SHA ca3e09f164e6e1c541ecb63d15bbebf5bce41d70 still resolves after the rename (D-05)"
    - "A fresh trace is captured at THIS phase's tip from the shipped recorder — never hand-derived, never pasted from 141-NEW-TRACE.md's stale arrays (D-06)"
    - "The six-assertion identity gate is re-armed on the new fixture and reads GREEN (D-08)"
    - "native_trace_v131 runs 5 cases with 0 failed — the milestone's first standing RED is retired"
    - "The rename, the new capture and the inventory rewrite land in ONE commit, so the identity gate is never transiently RED for a reason that is really a git exit code (F-05)"
  artifacts:
    - path: "firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h"
      provides: "The frozen pre-change cadence (198/221/201), byte-untouched, included by nothing"
      contains: "EPROM_V131_TRACE_PROTO_07"
    - path: "firestarter/test/native/avr/_shared/eprom_v131_expected.h"
      provides: "The new post-v1.31 cadence captured at this phase's tip (91/115/59)"
      contains: "EPROM_V131_TRACE_PROTO_0B"
    - path: "firestarter/tests/golden/eprom_v131_trace_inventory.json"
      provides: "The re-pointed golden: new counts, new blob_sha, frozen_for v1.32"
      contains: "how_to_update"
  key_links:
    - from: "firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp"
      to: "firestarter/test/native/avr/_shared/eprom_v131_expected.h"
      via: "the #include at :45 — the NEW fixture must keep the OLD name"
      pattern: "_shared/eprom_v131_expected\\.h"
    - from: "firestarter/tests/golden/eprom_v131_trace_inventory.json"
      to: "firestarter/test/native/avr/_shared/eprom_v131_expected.h"
      via: "meta.blob_sha predicted with git hash-object BEFORE staging; recorded_at_head names the commit's PARENT"
      pattern: "blob_sha"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under firestarter/src/ (D-04) — the capture needs no src/ change; the #ifdef is already in the test TU and the macro comes from PLATFORMIO_BUILD_FLAGS"
    - "MUST NOT paste 141-NEW-TRACE.md section 5's arrays — stale at 91/119/59; a 0x08 total of 119 is positive proof of a stale paste (D-06, F-06)"
    - "MUST NOT hand-derive, hand-edit or hand-correct a single trace entry — every array in this repo is empirical"
    - "MUST NOT hand-edit a count or a blob SHA in the inventory JSON — meta.how_to_update binds an independent re-parse (D-08)"
    - "MUST NOT rename the NEW fixture — _FIXTURE_PATH, _CONSUMERS and test_trace_eprom_v131.cpp:45 all hardcode _shared/eprom_v131_expected.h (F-05)"
    - "MUST NOT let the renamed prechange header be #included by any TU — it must compile nowhere, or it can contribute a warning against the zero-headroom 1166 native watermark (D-05, D-23)"
    - "MUST NOT split D-05, D-06 and D-08 across commits — one commit, or the identity gate fails inside git's exit-code assert (F-05)"
    - "MUST NOT absorb a capture total other than 91 / 115 / 59 — stop and report (D-06)"
    - "MUST NOT add a seventh native env or edit platformio.ini"
    - "MUST NOT feed any *_v131 env name to check_size_baseline.py or check_build_warnings.py under any --baseline (D-22, unconditionally, per C-03)"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
---

<objective>
Retire the milestone's first standing RED. Freeze Phase 138's pre-change golden trace as a historical
artifact by pure rename, capture a fresh trace at this phase's tip from the shipped recorder, and re-point
the six-assertion identity gate's inventory at the new fixture — all in ONE commit — then confirm
`native_trace_v131` runs 5 cases with 0 failed.

Purpose: TEST-06 forbids a blanket snapshot update. The pre-change stream must survive intact so plan
144-04 can diff old against new and attribute every changed strobe; the new stream must be empirical so it
asserts what the code emits rather than what anyone believes it emits.

Output: `eprom_v131_expected_prechange.h` (renamed, byte-untouched), `eprom_v131_expected.h` (fresh capture
at the same path and name as before), and a re-derived `eprom_v131_trace_inventory.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- New path: `firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h` (renamed from
  `eprom_v131_expected.h`; byte-identical, blob `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70`)
- Rewritten content at the existing path `firestarter/test/native/avr/_shared/eprom_v131_expected.h`
  (three array bodies plus provenance banners replaced; typedef, kind macros, `v131_merged_length`,
  `v131_merged_at`, the assertion helpers and the sizeof-derived `_LEN` macros preserved verbatim)
- Rewritten `firestarter/tests/golden/eprom_v131_trace_inventory.json`: `meta.recorded_by`,
  `meta.requirement`, `meta.blob_sha`, `meta.recorded_at_head`, `meta.frozen_for`,
  `meta.measured_entry_counts`, `meta.overflow_observed`, `arrays[].entries` — all re-derived
- Scratch, not committed: `/tmp/gsd-144/trace_dump.txt` (raw dump) and `/tmp/gsd-144/count_arrays.py`
  (throwaway independent parser this plan writes and later plans reuse)

No symbol is removed. No new test module, no new env, no new seam.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Capture the new trace cold, rename the old fixture, write the new one</name>
  <files>firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h, firestarter/test/native/avr/_shared/eprom_v131_expected.h</files>
  <read_first>
    - `firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp` :341-396 — the `#ifdef EPROM_V131_TRACE_DUMP` block, `dump_v131_merged_ready_to_paste` at :351-361 (banner format :353, entry format :358), `test_dump_v131_traces` :363-375, `main()` registration :391-393. Also :45 (the `#include`) and :92 (`millis()` pinned to `AlwaysReturn(0)`).
    - `firestarter/test/native/avr/_shared/eprom_v131_expected.h` — the CURRENT fixture. Everything that is NOT an array body must survive byte-for-byte: header banner :1-38, typedef :77-82, kind macros :88-91, `v131_merged_length` :94, `v131_merged_at` and its splice rule :111-140, the assertion helpers through :220, each array's descriptive banner (:221-273, :368-410, :510-554), the three declarations (:274, :411, :555), the three `_LEN` macros (:366, :508, :647).
    - `.planning/phases/141-per-byte-program-loop/141-NEW-TRACE.md` — section 1 ONLY, for the capture command sequence and the cwd constraint. Do NOT read or use section 5's arrays.
    - `.planning/phases/144-tests-build-verification/144-RESEARCH.md` — F-06's three real-capture-vs-stale-paste discriminators.
  </read_first>
  <action>
Step 1 — capture BEFORE any rename. The suite `#include`s the fixture at its current name, so renaming
first breaks the build.

Every `pio` invocation runs with cwd `/workspaces/firestarter`: a gitignored root `platformio.ini` at
`/workspaces/platformio.ini` carries two `[platformio]` sections and `pio -d <dir>` does not work around it.
Give each build a long explicit timeout, at least 300000 ms — a default two-minute cap truncates the build
mid-compile.

Create `/tmp/gsd-144`. Remove `.pio/build/native_trace_v131` so the run is cold and the stale-binary
question is moot. Build with `PLATFORMIO_BUILD_FLAGS` set to `-D EPROM_V131_TRACE_DUMP` for
`pio test -e native_trace_v131 --without-testing`. Then run the built binary
`.pio/build/native_trace_v131/firestarter_native` DIRECTLY — `pio test` swallows `printf`, as the helper's
own comment at :346 states — redirecting stdout to `/tmp/gsd-144/trace_dump.txt`.

Step 2 — validate against all three F-06 discriminators before using a single line of the dump.
(a) The banners must read `total=91`, `total=115`, `total=59` for `EPROM_V131_TRACE_PROTO_07`, `_08`, `_0B`;
a `0x08` total of `119` is positive proof of a stale paste from `141-NEW-TRACE.md`. (b) Every banner must
carry `strobe_overflow=0 timing_overflow=0` (recorder caps are 512 each, so 115 is 22 percent of cap).
(c) The dump build's Unity summary reads `6 test cases` — the sixth being the dump case — whereas the plain
run reads `5 test cases`. Any deviation from 91 / 115 / 59 is STOP-AND-REPORT, not absorb.

Also write the throwaway independent parser `/tmp/gsd-144/count_arrays.py`: given a header path it
comment-strips each `static const v131_trace_entry_t <name>[] = { ... };` body and prints the per-array
entry counts space-separated, in declaration order. Validate the parser against the RENAMED prechange file
first — it must print `198 221 201`, which is what the existing inventory records — before trusting it on
the new file. That is the independent re-derivation `meta.how_to_update` binds, and validating it on a
known-answer input is what makes it trustworthy.

Record, as a prediction confirmed rather than assumed: Phase 143's `MSG_DATA_PROGRESS` emission adds ZERO
entries, for two independent reasons that both belong in the SUMMARY — `millis()` is pinned to
`AlwaysReturn(0)` so the 1000 ms interval guard can never fire, AND the recorders capture only register
strobes plus `delay`/`delayMicroseconds` timings and never `Serial` writes, so even an advancing clock could
not add one.

Step 3 — the rename (D-05). `git mv test/native/avr/_shared/eprom_v131_expected.h
test/native/avr/_shared/eprom_v131_expected_prechange.h`, content byte-untouched. Immediately confirm
`git hash-object` on the new path prints `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70` — a git blob SHA is
content-only and path-independent, so that single check is the whole of D-05's proof.

Step 4 — write the new fixture at the OLD name. Copy the renamed prechange file back to
`test/native/avr/_shared/eprom_v131_expected.h`, then replace exactly two classes of content and nothing
else: (i) the three array bodies, with the corresponding entry lines from `/tmp/gsd-144/trace_dump.txt`
pasted verbatim including their `/* N */` positional index comments; (ii) the file header banner and each
array's descriptive banner, rewritten to describe THIS capture — Phase 144 Plan 03, the exact dump command
used, the measured total, the two overflow flags, and a sentence naming
`eprom_v131_expected_prechange.h` plus its preserved blob `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70` as
where the pre-change cadence now lives.

Preserve byte-for-byte the include guard, the typedef, the four kind macros, `v131_merged_length`,
`v131_merged_at` and its splice rule, every assertion helper, and the three `_LEN` macros — they are
`sizeof`-derived, so no count literal exists in this file to update.

Do NOT reproduce the old fixture's rich per-segment entry comments. The recorder emits only `/* %d */`, so
descriptive per-entry comments here would be hand-authoring dressed as documentation — and plan 144-04's
segmentation is deliberately structural for exactly that reason.

Step 5 — remove `.pio/build/native_trace_v131` again and run `pio test -e native_trace_v131`: 5 test cases,
0 failed. That is the retirement of the milestone's first standing RED (3 of 5 were RED by design until this
capture landed). Do NOT commit yet — task 2 completes the one-commit unit.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -cE "^##### EPROM_V131_TRACE_PROTO_(07|08|0B) total=(91|115|59) strobe_overflow=0 timing_overflow=0" /tmp/gsd-144/trace_dump.txt | grep -x 3</automated>
    <automated>cd /workspaces/firestarter && git hash-object test/native/avr/_shared/eprom_v131_expected_prechange.h | grep -x ca3e09f164e6e1c541ecb63d15bbebf5bce41d70</automated>
    <automated>cd /workspaces/firestarter && python3 /tmp/gsd-144/count_arrays.py test/native/avr/_shared/eprom_v131_expected_prechange.h | grep -x "198 221 201" && python3 /tmp/gsd-144/count_arrays.py test/native/avr/_shared/eprom_v131_expected.h | grep -x "91 115 59"</automated>
    <automated>cd /workspaces/firestarter && pio test -e native_trace_v131 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/gsd-144/trace_dump.txt` carries exactly three banners, reading `total=91`, `total=115`, `total=59` in `_07`, `_08`, `_0B` order, each with `strobe_overflow=0 timing_overflow=0`. A `0x08` total of 119 is a stop-and-report.
    - `git hash-object test/native/avr/_shared/eprom_v131_expected_prechange.h` prints `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70`.
    - `git status --porcelain` shows the move as a staged rename (`R`), not a delete-plus-add pair.
    - `/tmp/gsd-144/count_arrays.py` prints `198 221 201` for the prechange header (the known-answer validation) and `91 115 59` for the new header.
    - `git show HEAD:test/native/avr/_shared/eprom_v131_expected.h` and the renamed prechange file are byte-identical (`diff` reports nothing).
    - The new `eprom_v131_expected.h` still contains `v131_merged_at`, `v131_merged_length`, all four `STROBE_KIND_`/`TIMING_KIND_` macros, and all three `_LEN` macros in `sizeof`-derived form — no integer literal appears in any `_LEN` definition.
    - `grep -rn "eprom_v131_expected_prechange" firestarter/ --include=*.cpp --include=*.h --include=*.inc | grep -c include` is 0 — the prechange header is `#include`d by nothing.
    - `pio test -e native_trace_v131` reports `5 test cases` and `0 failed`.
    - `git -C /workspaces/firestarter status --porcelain -- src/` is empty (D-04).
  </acceptance_criteria>
  <done>The fresh capture is validated against all three discriminators, the prechange header preserves its Phase-138 blob, the new fixture carries 91/115/59 with all non-array content intact, and the trace suite passes 5/5.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive the inventory, land all three paths in ONE commit, and re-arm the identity gate</name>
  <files>firestarter/tests/golden/eprom_v131_trace_inventory.json</files>
  <read_first>
    - `firestarter/tests/golden/eprom_v131_trace_inventory.json` — the current record in full: `meta.source`, `recorded_by`, `requirement`, `blob_sha`, `recorded_at_head`, `why_two_checks`, `how_to_update`, `frozen_for`, `measured_entry_counts` (strobes/timings/merged per array), `overflow_observed`, and the positional `arrays` list.
    - `firestarter/tests/test_golden_trace_identity_eprom_v131.py` — all six assertions and what each reads: `_FIXTURE_PATH` :78, `_INVENTORY_JSON` :79, `_CONSUMERS` :80-82, `_parse_arrays` :131-144, the `HEAD:` blob leg :155-165, the positional counts leg :177-195, `test_inventory_is_non_vacuous` :198-209, `test_consuming_suites_still_include_the_fixture` :212, `test_git_is_required_not_optional` :222.
    - `firestarter/tests/golden/protocol_branch_inventory.json` — `meta.recorded_by`'s verbatim paragraph documenting the one-commit `recorded_at_head`-names-the-parent offset. That register is what task 2's `recorded_by` must copy.
  </read_first>
  <action>
Rewrite `firestarter/tests/golden/eprom_v131_trace_inventory.json`, preserving its six-assertion-compatible
shape exactly (same top-level keys, same `meta` field names, same positional `arrays: [{name, entries}]`
list — the identity gate reads all of them, so a shape change breaks it):

- `meta.source` — unchanged: `test/native/avr/_shared/eprom_v131_expected.h`.
- `meta.recorded_by` — `Phase 144 Plan 03`, followed by the one-commit-offset paragraph in
  `protocol_branch_inventory.json`'s register: this file's `blob_sha` is the SHA of the working-tree fixture
  as it will read once committed (`git hash-object`, run BEFORE staging), and `recorded_at_head` therefore
  names this commit's PARENT rather than the commit that carries this file. Say explicitly that this is the
  deliberate one-commit offset and not a mistake.
- `meta.requirement` — `TEST-06`.
- `meta.blob_sha` — the output of `git hash-object test/native/avr/_shared/eprom_v131_expected.h` taken on
  the worktree file BEFORE staging.
- `meta.recorded_at_head` — the value of `git rev-parse HEAD` taken BEFORE this plan's commit.
- `meta.why_two_checks` and `meta.how_to_update` — preserved verbatim; they are binding.
- `meta.frozen_for` — repointed forward: this record now arms the identity gate for v1.32 drift detection,
  and a divergence from these three arrays is a regression to investigate rather than expected work. It must
  no longer name Phase 144 as the consumer that will diff it.
- `meta.measured_entry_counts` — per array `{strobes, timings, merged}`, re-derived by independent parse:
  strobes are the entries whose `kind` is 1 or 2, timings those whose `kind` is 3 or 4, merged the total.
  Validate that derivation rule on the prechange file first, where it must reproduce the recorded
  142+56=198, 157+64=221 and 142+59=201 exactly; only then apply it to the new arrays.
- `meta.overflow_observed` — restated from the new capture's banners (`strobe_overflow=0
  timing_overflow=0` on all three), naming this plan's dump as the source.
- `arrays` — `[{EPROM_V131_TRACE_PROTO_07, 91}, {EPROM_V131_TRACE_PROTO_08, 115},
  {EPROM_V131_TRACE_PROTO_0B, 59}]`, in that positional order, taken from `count_arrays.py`, never typed
  from memory.

Add one `meta` field recording D-08's named non-claim so it lives beside the record it qualifies: with a
single inventory record, nothing gate-asserts `eprom_v131_expected_prechange.h`; its preserved blob
`ca3e09f164e6e1c541ecb63d15bbebf5bce41d70` stays hand-verifiable via `git rev-parse HEAD:<path>` and is
cited in the phase record, but it is NOT machine-checked. Do not imply otherwise, and do not add a second
inventory record to close it — that is deferred work.

Then land ONE commit containing exactly three paths: the renamed prechange header, the new fixture, and this
inventory. The commit message must state which array changed and why, per `meta.how_to_update`: the three
arrays move 198→91, 221→115 and 201→59 because the per-byte program loop (Phase 141), the shared HV-route
resolver (Phase 142) and the parameter table (Phase 140) changed the emitted cadence; Phase 143 added zero
entries, for the two independent reasons task 1 recorded. Name the one-commit offset in the message too.

After committing — and only after, because `test_flash_path_record_sync.py` asserts the whole firmware
repo's `git status --porcelain` — run the verification legs below.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_golden_trace_identity_eprom_v131.py -q -rs 2>&1 | tail -4</automated>
    <automated>cd /workspaces/firestarter && git rev-parse HEAD:test/native/avr/_shared/eprom_v131_expected_prechange.h | grep -x ca3e09f164e6e1c541ecb63d15bbebf5bce41d70</automated>
    <automated>cd /workspaces/firestarter && python3 -c "import json;m=json.load(open('tests/golden/eprom_v131_trace_inventory.json'));import subprocess;h=subprocess.run(['git','rev-parse','HEAD:test/native/avr/_shared/eprom_v131_expected.h'],capture_output=True,text=True).stdout.strip();assert m['meta']['blob_sha']==h,(m['meta']['blob_sha'],h);assert [a['entries'] for a in m['arrays']]==[91,115,59];print('inventory agrees with HEAD')"</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && git show --stat --name-only HEAD | grep -c "^firestarter\|^src/" | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - The commit contains exactly three paths — `test/native/avr/_shared/eprom_v131_expected_prechange.h`, `test/native/avr/_shared/eprom_v131_expected.h`, `tests/golden/eprom_v131_trace_inventory.json` — and nothing else. `git show --name-only HEAD` lists no fourth file and nothing under `src/`.
    - `python3 -m pytest tests/test_golden_trace_identity_eprom_v131.py -q -rs` reports 6 passed, 0 failed, 0 skipped.
    - `meta.blob_sha` equals `git rev-parse HEAD:test/native/avr/_shared/eprom_v131_expected.h`, and `meta.recorded_at_head` equals `git rev-parse HEAD~1` (the deliberate parent offset).
    - `git rev-parse HEAD:test/native/avr/_shared/eprom_v131_expected_prechange.h` prints `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70`.
    - `arrays[].entries` reads `[91, 115, 59]` and `measured_entry_counts` per-array `strobes + timings == merged` for all three, with `merged` equal to the corresponding `arrays[].entries`.
    - `meta.how_to_update` and `meta.why_two_checks` are byte-identical to their pre-change text (`git show HEAD~1:tests/golden/eprom_v131_trace_inventory.json` compared field-by-field).
    - The commit message names all three array movements (198→91, 221→115, 201→59), their attributing phases, and the one-commit `recorded_at_head` offset.
    - `meta` carries D-08's non-claim: the prechange header is not machine-checked.
    - `python3 -m pytest tests/ -q` reports 0 failed with passed at least 292 plus the tests plan 144-01 added.
    - `git status --porcelain` is empty at task end.
  </acceptance_criteria>
  <done>All three paths are in one commit, the identity gate is GREEN on the new fixture, the prechange blob still resolves at HEAD, and the whole firmware pytest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built native binary → fixture content | The dump output becomes committed test data; a stale binary would produce a plausible-looking wrong capture |
| committed golden ↔ its gate | The inventory and the fixture must agree at `HEAD:`, not merely in the worktree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-11 | Tampering | A stale `.pio/build/native_trace_v131` binary yielding a wrong-but-plausible capture | mitigate | `rm -rf .pio/build/native_trace_v131` before the dump build; three independent stale-paste discriminators (banner totals, overflow flags, 6-vs-5 case summary) asserted before any line is pasted |
| T-144-12 | Repudiation | A hand-edited count or blob SHA making a surprise disappear | mitigate | Counts and `measured_entry_counts` come from `count_arrays.py`, itself validated against the prechange file's known 198/221/201; `blob_sha` comes from `git hash-object`; `meta.how_to_update` preserved verbatim and honoured in the commit message |
| T-144-13 | Repudiation | The identity gate transiently RED for a reason that is really a git exit code | mitigate | One commit for D-05 + D-06 + D-08 (F-05); the gate reads `HEAD:`, so the three paths must land together |
| T-144-14 | Repudiation | Implying the prechange header is machine-checked | mitigate | D-08's non-claim recorded in `meta` and in the SUMMARY; no second inventory record is added |
| T-144-15 | Tampering | An unincluded header becoming a warning source against the zero-headroom 1166 native watermark | accept | `_shared/` is reached only by relative `../_shared/…` includes and no TU includes the prechange header; asserted by the acceptance criterion that greps for an `#include` of it |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan; RESEARCH § Package Legitimacy Audit records "not applicable" for the phase |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/test_golden_trace_identity_eprom_v131.py -q -rs` — 6 passed, 0 skipped.
- `cd /workspaces/firestarter && pio test -e native_trace_v131 2>&1 | tail -6` — 5 test cases, 0 failed.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — 0 failed.
- `cd /workspaces/firestarter && python3 -m pytest tests/test_protocol_branch_inventory.py -q` — 0 failed, proving both `src/` blob pins (`cedc88dc…`, `5dffe841…`) still match (D-04 green throughout).
- `cd /workspaces/firestarter && git status --porcelain` — empty.
- Do NOT run `check_size_baseline.py` or `check_build_warnings.py` with any `*_v131` env name here; plan 144-05 owns the measurement, and an unknown env name raises an uncaught `KeyError` → exit 1, a FALSE regression signal (C-03).
</verification>

<success_criteria>
- The pre-change trace is preserved as a renamed artifact whose Phase-138 blob still resolves at `HEAD:`.
- The new trace is empirical, validated against three stale-paste discriminators, and totals 91 / 115 / 59.
- The identity gate is GREEN on the new fixture with all six assertions passing; `native_trace_v131` is 5/5.
- One commit, three paths, a message that names each array movement and its attributing phase.
- No `src/` file touched; no env or `platformio.ini` change; firmware repo clean at plan end.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-03-SUMMARY.md` when done. It MUST contain the
three capture banners verbatim, the two independent reasons Phase 143 added zero entries, the prechange
blob-SHA confirmation, the pre-staging `git hash-object` value alongside the post-commit
`git rev-parse HEAD:` value, the verbatim identity-gate and `native_trace_v131` transcripts, and D-08's
named non-claim (the prechange header is not machine-checked).
</output>
