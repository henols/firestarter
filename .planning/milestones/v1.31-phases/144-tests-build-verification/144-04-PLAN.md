---
phase: 144-tests-build-verification
plan: 04
type: execute
wave: 3
depends_on: ["144-03"]
commits_land_in: firestarter
files_modified:
  - firestarter/tests/test_trace_segment_exhaustiveness_v131.py
autonomous: true
requirements: [TEST-06]

must_haves:
  truths:
    - "All 885 trace entries (620 pre-change + 265 new) fall into exactly one named segment, proven by set equality over index ranges rather than by a count sum (D-07)"
    - "Every segment that appears in either stream carries a named attributing decision from Phases 140-143, so no delta is unattributed"
    - "An unattributed or unclassifiable entry fails the gate with a message naming its array, its positional index and its (kind, pin, value, us) tuple"
    - "The segmentation is structural — a state machine over kind/pin/value/us keyed on the OUTPUT_ENABLE toggle — never keyed on fixture comments, because the new capture carries positional index comments only"
    - "A count-only check cannot pass this gate: one entry deleted and one duplicated leaves the length unchanged and must still fail"
  artifacts:
    - path: "firestarter/tests/test_trace_segment_exhaustiveness_v131.py"
      provides: "D-07's exhaustiveness gate, its six-segment state machine, the attribution map and both planted-violation legs"
      contains: "FIRESTARTER_TRACE_SEGMENT_SCAN_NEW"
      min_lines: 300
  key_links:
    - from: "firestarter/tests/test_trace_segment_exhaustiveness_v131.py"
      to: "firestarter/test/native/avr/_shared/eprom_v131_expected.h"
      via: "import-time seam FIRESTARTER_TRACE_SEGMENT_SCAN_NEW"
      pattern: "FIRESTARTER_TRACE_SEGMENT_SCAN_NEW"
    - from: "firestarter/tests/test_trace_segment_exhaustiveness_v131.py"
      to: "firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h"
      via: "import-time seam FIRESTARTER_TRACE_SEGMENT_SCAN_PRECHANGE"
      pattern: "FIRESTARTER_TRACE_SEGMENT_SCAN_PRECHANGE"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under firestarter/src/ (D-04)"
    - "MUST NOT be authored as firestarter/scripts/check_*.py — FLOOR=6 and FIXTURE_FLOOR=15 sit at exactly the current counts, so a scripts/ checker obliges five coordinated same-commit edits including raising both floors (F-08)"
    - "MUST NOT key the segmentation on fixture comments — the new capture emits only /* N */, so a comment-keyed classifier would pass on all 620 pre-change entries and classify none of the 265 new ones (PATTERNS finding 2)"
    - "MUST NOT import _parse_arrays or any helper from test_golden_trace_identity_eprom_v131.py — that module's docstring requires two INDEPENDENT readings; re-implement the technique"
    - "MUST NOT write a segmentation rule against the 9-bit value 0x100 — the recorded control value is post-mapping and 8-bit (F-07)"
    - "MUST NOT satisfy the exhaustiveness assertion with a count sum — use set equality over range(len(array)) plus pairwise disjointness"
    - "MUST NOT widen the segment taxonomy to absorb an entry the state machine cannot classify — that is a stop-and-report"
    - "MUST NOT use monkeypatch.setenv for the planted runs — both seams bind at import, so a child process is mandatory (S6)"
    - "MUST NOT modify either fixture header — both are read-only inputs here; plants are scratch copies under tmp_path"
    - "MUST NOT run any suite before committing (D-20 / F-09)"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
---

<objective>
Author D-07's exhaustiveness gate: a pytest module that parses BOTH trace fixtures, walks each of the six
arrays with a structural state machine, partitions every entry into exactly one of six named segments, and
asserts that every segment present in either stream carries a named attributing decision from Phases
140-143. Then prove it under D-18 with two planted violations in a child process.

Purpose: TEST-06's "every changed strobe attributable to a named decision" is the requirement, and
"counts-plus-narrative" was explicitly rejected as a blanket snapshot update wearing a paragraph. This gate
makes the attribution machine-checked for completeness: 885 entries (620 pre-change + 265 new), every one in
exactly one attributed segment, an unattributed entry failing loudly and locatably.

Output: `firestarter/tests/test_trace_segment_exhaustiveness_v131.py`, plus its `__main__` per-segment
old-versus-new table, which is the evidence plan 144-07's record tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- New file: `firestarter/tests/test_trace_segment_exhaustiveness_v131.py`
- New env seam names: `FIRESTARTER_TRACE_SEGMENT_SCAN_NEW`,
  `FIRESTARTER_TRACE_SEGMENT_SCAN_PRECHANGE` (path-only overrides), and reuse of the child-process
  recursion guard name `FIRESTARTER_144_GATE_CHILD` introduced by plan 144-01
- New module-level names: `_HERE`, `_REPO_ROOT`, `_NEW_REL`, `_PRECHANGE_REL`, `_SCAN_NEW`,
  `_SCAN_PRECHANGE`, `_ARRAY_DECL_RE`, `_ENTRY_FIELDS_RE`, `_PIN_LSB`, `_PIN_MSB`, `_PIN_OE`,
  `_PIN_CTRL`, `_PIN_CE`, `_SEGMENTS`, `_SEGMENT_ATTRIBUTION`, `_TOTAL_ENTRY_FLOOR`,
  `_PRECHANGE_EXPECTED`, `_NEW_EXPECTED`, `_parse_arrays_with_fields`, `_segment_indices`,
  `_format_entry`
- New test names: `test_prechange_arrays_parse_to_the_recorded_lengths`,
  `test_new_arrays_parse_to_the_captured_lengths`,
  `test_every_entry_falls_in_exactly_one_segment`,
  `test_every_present_segment_has_a_named_attribution`,
  `test_pre_change_0x07_pulse_and_verify_counts_match_the_output_enable_toggles`,
  `test_total_attributed_entry_count_is_885`,
  `test_scan_targets_are_non_vacuous`,
  `test_this_module_cannot_be_silently_skipped`,
  `test_own_needles_do_not_appear_verbatim_in_this_module`,
  `test_planted_unclassifiable_entry_is_located`,
  `test_planted_delete_and_duplicate_defeats_a_count_only_check`
- A `__main__` block printing the per-segment old-versus-new count table

No existing symbol is renamed or removed.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the six-segment state machine, the partition assertion and the attribution map</name>
  <files>firestarter/tests/test_trace_segment_exhaustiveness_v131.py</files>
  <read_first>
    - `firestarter/tests/test_golden_trace_identity_eprom_v131.py` — the parse analog: `_ARRAY_DECL_RE` / `_ENTRY_RE` / `_parse_arrays` at :131-144 (copy the TECHNIQUE and the comment-strip-then-match order, never the import), the first-divergence failure-message shape at :177-195, `test_inventory_is_non_vacuous` at :198-209, and the fail-closed git helpers at :91-128.
    - `firestarter/tests/test_ack_layout_source_contract_v143.py` — the scaffolding analog: seam block :133-146, `_strip_comments` :148-182, `_line_of` :185-186, the two-half non-vacuity leg :477-525, the concatenation needles and both self-checks :528-568.
    - `firestarter/test/native/avr/_shared/eprom_v131_expected.h` (new, from plan 144-03) and `firestarter/test/native/avr/_shared/eprom_v131_expected_prechange.h` — the two streams. Read the typedef and kind macros (:77-91 in the prechange copy) and enough of each array body to see the 4-entry latch-group idiom.
    - `firestarter/include/rurp_shield.h` :53-57 — the five pin constants: `0x01` LSB, `0x02` MSB, `0x04` OUTPUT_ENABLE, `0x08` CONTROL_REGISTER, `0x20` CHIP_ENABLE.
    - `.planning/phases/144-tests-build-verification/144-RESEARCH.md` — F-07 in full: the field-capturing regex verified against all 620 entries, the primitive classification counts per array, the six-segment table, the `OUTPUT_ENABLE` discriminator arithmetic (7 pulses + 12 verify reads = the 19 `pin_OE` entries on 0x07), and the post-mapping 8-bit control-value caveat.
    - `.planning/phases/144-tests-build-verification/144-PATTERNS.md` section 2 — including the comment-keyed-segmentation trap and the "stop-and-report, never widen the taxonomy" rule.
  </read_first>
  <action>
Create `firestarter/tests/test_trace_segment_exhaustiveness_v131.py`. Stdlib plus pytest only.

Module scope: `_HERE`, `_REPO_ROOT`, `_NEW_REL = "test/native/avr/_shared/eprom_v131_expected.h"`,
`_PRECHANGE_REL = "test/native/avr/_shared/eprom_v131_expected_prechange.h"`, and TWO import-time seams —
`_SCAN_NEW` from `FIRESTARTER_TRACE_SEGMENT_SCAN_NEW` and `_SCAN_PRECHANGE` from
`FIRESTARTER_TRACE_SEGMENT_SCAN_PRECHANGE`, each defaulting to its `_REPO_ROOT`-relative path. Per S5 each
seam overrides a PATH ONLY: never a segment name, never an expected length, never the 885 denominator.
Document both seams, and the import-binding consequence, in an "Environment seams" docstring section — the
repo has no central env inventory, so that docstring is the only discoverable site.

Parse: re-implement (never import) the array-declaration regex
`static const v131_trace_entry_t\s+(\w+)\[\]\s*=\s*\{(.*?)\};` with `re.DOTALL`, comment-strip each body
first (block then line comments), and capture fields with the form RESEARCH verified against all 620
pre-change entries: `\{\s*(\d+)\s*,\s*(0x[0-9A-Fa-f]+)\s*,\s*(0x[0-9A-Fa-f]+)\s*,\s*(\d+)UL\s*\}`.
`_parse_arrays_with_fields(path)` returns an ordered list of `(name, [(kind, pin, value, us), ...])`.
Note in a comment that comment-stripping is what makes the new capture's `/* N */` index comments harmless
and is also why a comment-keyed classifier is impossible here.

Kind values (from the fixture's own macros): 1 STROBE_DATA, 2 STROBE_PIN, 3 DELAY_US, 4 DELAY_MS. Pin
constants as named module literals: `_PIN_LSB = 0x01`, `_PIN_MSB = 0x02`, `_PIN_OE = 0x04`,
`_PIN_CTRL = 0x08`, `_PIN_CE = 0x20`. Add a comment recording that the recorded control `value` is
post-mapping and 8-bit, so no rule may test for `0x100`.

`_segment_indices(entries)` — the state machine. It returns a dict mapping each of the six segment names to
a set of entry indices, and must raise a locating `AssertionError` for any entry it cannot classify. The six
segments and their entry conditions (F-07's table, which is the specification):
  - `init` — from index 0 through the first `DELAY_MS` whose `us` is 500: the one-time VPP-regulator enable
    latch group plus its settle.
  - `route_assert` — a CONTROL_REGISTER latch group (its data write, the LE-high strobe, the 1 us
    post-latch settle and the LE-low strobe) whose value changes HV route bits, plus a trailing `DELAY_MS`
    settle when present, and only while data or CE strobes still follow.
  - `address_set` — LSB/MSB latch groups (`pin` in `_PIN_LSB`, `_PIN_MSB`) with their data write and 1 us
    settle.
  - `pulse` — while `OUTPUT_ENABLE` last latched to 1: the entering `{2, 0x04, 0x01}` toggle group, the
    payload data write, and the CE-low, `DELAY_US`-width, CE-high window.
  - `verify_read` — while `OUTPUT_ENABLE` last latched to 0 after the first toggle: the entering
    `{2, 0x04, 0x00}` toggle group, and the CE-low, 3 us access, CE-high window.
  - `teardown` — the trailing CONTROL_REGISTER group or groups after the last data or CE strobe.
`_PIN_CE` alone cannot separate `pulse` from `verify_read` — it appears 38 times per array and spans both —
so the `OUTPUT_ENABLE` value is the discriminator. State that in a comment; it is the reason a per-entry
field lookup is provably insufficient.

`_SEGMENTS` — the frozen tuple of the six names, in stream order. `_SEGMENT_ATTRIBUTION` — a frozen dict
mapping each segment name to a non-empty tuple of attributing decisions, cited by phase and decision id
(for example the parameter table's per-row pulse width and verify mode from Phase 140, the per-byte
pulse-to-verify loop from Phase 141, the shared `eprom_hv_route_mask` resolver and the preserved drop bit
from Phase 142, and Phase 143's zero-entry contribution recorded explicitly as zero). Add a docstring
paragraph stating the honest boundary: this gate proves the attribution is COMPLETE (no segment is
unattributed and no entry is unsegmented), not that each attribution is CORRECT — correctness is a human
judgement made in the phase record.

`_PRECHANGE_EXPECTED = (198, 221, 201)`, `_NEW_EXPECTED = (91, 115, 59)` and
`_TOTAL_ENTRY_FLOOR = 885` as hardcoded literals, with a comment deriving 885 = 620 + 265 and naming what
each half is.

Legs to author:
1. `test_prechange_arrays_parse_to_the_recorded_lengths` — three arrays, lengths `(198, 221, 201)`,
   positional, with a first-divergence message naming index, expected and observed.
2. `test_new_arrays_parse_to_the_captured_lengths` — same shape against `(91, 115, 59)`.
3. `test_every_entry_falls_in_exactly_one_segment` — for each of the six arrays, assert the union of the
   six segment index sets equals `set(range(len(entries)))` AND that the sets are pairwise disjoint. Set
   equality and disjointness, never a count sum. On failure, name the array, every unclassified index, and
   for each one its `(kind, pin, value, us)` tuple through `_format_entry`.
4. `test_every_present_segment_has_a_named_attribution` — every segment name with at least one index in
   either stream has a non-empty `_SEGMENT_ATTRIBUTION` entry; the message names the orphan segment.
5. `test_pre_change_0x07_pulse_and_verify_counts_match_the_output_enable_toggles` — the F-07 known-answer
   self-test: on the pre-change `0x07` array the machine must find 7 pulse windows and 12 verify reads, and
   `7 + 12` must equal the count of `pin == _PIN_OE` strobes (19). This is what proves the state machine is
   not merely self-consistent.
6. `test_total_attributed_entry_count_is_885` — the six arrays' attributed totals sum to
   `_TOTAL_ENTRY_FLOOR`, with the per-stream subtotals (620 and 265) named in the message.
7. `test_scan_targets_are_non_vacuous` — the two-half pattern. Part (a) recomputes both default paths from
   `_REPO_ROOT` WITHOUT reading `os.environ` at all, asserts each `is_file()`, non-empty, and
   `resolve().is_relative_to(_REPO_ROOT)`. Part (b) runs the parser against whatever the two seams
   currently resolve to and asserts three arrays each with a non-empty entry list, total entries across both
   streams at least `_TOTAL_ENTRY_FLOOR`.
8. `test_this_module_cannot_be_silently_skipped` plus the needle-hygiene twin, concatenation-needle form.

Add an `if __name__ == "__main__":` block printing a per-segment old-versus-new table (segment, pre-change
count, new count, delta, attribution) for each protocol. `CHECKER_GLOB` never reaches `tests/`, so this
costs nothing (F-08) — and the table is the artifact plan 144-07's record pastes.

Commit the module before running the whole firmware suite (D-20).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_trace_segment_exhaustiveness_v131.py -q -rs 2>&1 | tail -5</automated>
    <automated>cd /workspaces/firestarter && python3 tests/test_trace_segment_exhaustiveness_v131.py | tee /tmp/gsd-144/segment_table.txt | tail -30</automated>
    <automated>cd /workspaces/firestarter && grep -c "from test_golden_trace_identity" tests/test_trace_segment_exhaustiveness_v131.py | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && grep -c "0x100" tests/test_trace_segment_exhaustiveness_v131.py | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_trace_segment_exhaustiveness_v131.py -q -rs` reports 0 failed, 0 skipped, at least 8 passed.
    - The partition leg asserts `union(segment_sets) == set(range(len(entries)))` AND pairwise disjointness for all six arrays — a count-sum-only assertion is a defect, not a shortcut.
    - The known-answer leg passes: pre-change `0x07` yields 7 pulse windows and 12 verify reads, summing to the 19 `pin == 0x04` strobes.
    - `_TOTAL_ENTRY_FLOOR` is the literal `885`, and a comment derives it as 620 (198+221+201) plus 265 (91+115+59).
    - `_SEGMENT_ATTRIBUTION` has one entry per segment name in `_SEGMENTS`, each a non-empty tuple naming a phase and decision; the module docstring states the complete-not-correct boundary.
    - The module contains no import from `test_golden_trace_identity_eprom_v131` and no occurrence of `0x100`.
    - `python3 tests/test_trace_segment_exhaustiveness_v131.py` prints a per-segment table for all three protocols with old, new and delta columns, saved to `/tmp/gsd-144/segment_table.txt`.
    - `git -C /workspaces/firestarter status --porcelain -- src/ test/` is empty — neither fixture was modified by this plan.
    - The module is committed before the full-suite run.
  </acceptance_criteria>
  <done>The exhaustiveness gate partitions all 885 entries by set equality, every present segment has a named attribution, the OUTPUT_ENABLE known-answer self-test passes, and the segment table is available as record evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Prove both D-18 planted violations in a child process, and record the RED/GREEN pair</name>
  <files>firestarter/tests/test_trace_segment_exhaustiveness_v131.py</files>
  <read_first>
    - `firestarter/tests/test_flash_path_record_sync.py` :269-305 — `_run_gate_in_subprocess` with its recursion guard; :1202-1250 — the six-move plant ceremony including `assert mutated_text != real_text`.
    - `firestarter/tests/test_trace_segment_exhaustiveness_v131.py` — the module as authored in task 1 (the legs the plants must trip, both seam names).
    - `.planning/phases/144-tests-build-verification/144-VALIDATION.md` § "Per-gate planted-violation specification" — rows 3 and 4 (D-07 exhaustiveness, and its second plant). Lift the RED/GREEN requirements verbatim.
  </read_first>
  <action>
Add a `_run_gate_in_subprocess(env_overrides, node_id=None)` helper copied structurally from
`test_flash_path_record_sync.py:269-305` — list-form argv `[sys.executable, "-m", "pytest", target, "-q",
"-rs"]`, `cwd=_REPO_ROOT`, environment copied and updated, and a plain-`assert` recursion guard on
`FIRESTARTER_144_GATE_CHILD`. A child process is MANDATORY: both seams bind at import, so
`monkeypatch.setenv` cannot reach them (S6).

Plant A — `test_planted_unclassifiable_entry_is_located`: copy the real NEW fixture to `tmp_path`, mutate
exactly one entry to an unclassifiable shape by replacing its text with `{2, 0x40, 0x01, 0UL}` (pin `0x40`
is none of the five known pins), and assert the mutated text differs from the real text before writing it —
a silently-unmatched replacement is a vacuous plant. Run the child with
`FIRESTARTER_TRACE_SEGMENT_SCAN_NEW` pointed at the scratch copy, restricted to the node id of
`test_every_entry_falls_in_exactly_one_segment`. Assert non-zero returncode and that the output names the
array name, the positional index of the mutated entry, and its `(kind, pin, value, us)` tuple including
`0x40`. A message that only reports a count is a defect: an unattributed entry must be locatable.

Plant B — `test_planted_delete_and_duplicate_defeats_a_count_only_check`: copy the real NEW fixture to
`tmp_path`, delete one entry and duplicate another inside the SAME array so the array length is unchanged,
assert the mutated text differs from the real text, and assert the mutated array's entry count equals the
original count (that equality is what makes the plant meaningful — it proves a count check would pass).
Run the child with the same seam pointed at the copy, restricted to the node ids of the partition leg and
the parse-lengths leg. Assert non-zero returncode, and assert the failure comes from the set-equality or
disjointness assertion — not from a length mismatch. Include the length-equality figure in the SUMMARY so a
reader can see the count check would have passed.

Both legs run the S2 ceremony against BOTH real fixtures: capture each real path before any patching,
`git hash-object` both before and after, assert equality, and assert the whole firmware repo's
`git status --porcelain` is empty at the end. Resolve `git` fail-closed via
`shutil.which(os.environ.get("GIT", "git"))` with a plain `assert`; list-form argv; never `shell=True`.

Then capture the D-18 evidence pair for the SUMMARY, verbatim, both halves:
- RED: both child-process transcripts, showing the located array name, index and tuple for plant A, and the
  set-equality failure with the unchanged-length figure for plant B.
- GREEN: the full-module run with every leg passing, plus the figures the non-vacuity leg and the
  known-answer leg report (three arrays per stream, 620 and 265 subtotals, 885 total, 7 pulses and 12 verify
  reads on pre-change `0x07`). A GREEN over an empty parse is the unreachable-leg trap this pair excludes.

Commit the module before running the whole firmware suite.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_trace_segment_exhaustiveness_v131.py -q -rs 2>&1 | tail -5</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_trace_segment_exhaustiveness_v131.py::test_planted_unclassifiable_entry_is_located tests/test_trace_segment_exhaustiveness_v131.py::test_planted_delete_and_duplicate_defeats_a_count_only_check -q 2>&1 | tail -4</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0 && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && grep -c "shell=True" tests/test_trace_segment_exhaustiveness_v131.py | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && git rev-parse HEAD:test/native/avr/_shared/eprom_v131_expected_prechange.h | grep -x ca3e09f164e6e1c541ecb63d15bbebf5bce41d70</automated>
  </verify>
  <acceptance_criteria>
    - Both planted legs pass, and the SUMMARY carries a verbatim child-process RED transcript for each.
    - Plant A's RED transcript names the array, the positional index and the tuple containing `0x40`.
    - Plant B's RED transcript shows the failure arising from set equality or disjointness, and the SUMMARY states the mutated array's length equals the original length (so a count-only check would have passed).
    - Both real fixture blobs are unchanged by `git hash-object` before and after; `git status --porcelain` is empty; `git rev-parse HEAD:...prechange.h` still prints `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70`.
    - The SUMMARY's GREEN half quotes the non-vacuity and known-answer figures (620 / 265 / 885, and 7 + 12 = 19).
    - `python3 -m pytest tests/ -q` reports 0 failed, with `test_golden_trace_identity_eprom_v131.py`'s six assertions still passing.
    - No `shell=True`; every subprocess uses list-form argv.
    - `git -C /workspaces/firestarter diff --stat HEAD -- src/` is empty.
  </acceptance_criteria>
  <done>Both plants are seen RED with locating messages in a child process, the gate is seen GREEN over a non-empty two-stream parse with its known-answer self-test passing, and both fixtures are provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture text → gate parse | Two committed C headers are parsed as data; a redirected or emptied seam is an attack on the gate's reach |
| env seam → scan target | Two path-only seams decide which streams are partitioned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-16 | Repudiation | The gate passing over an empty or partial parse | mitigate | Two-half non-vacuity leg (part (a) never reads `os.environ`); hardcoded lengths `(198,221,201)` / `(91,115,59)` and the `885` floor; plant B proves a length-preserving mutation still fails |
| T-144-17 | Repudiation | A count-sum assertion hiding a double-count paired with a drop | mitigate | Set equality over `range(len(array))` plus pairwise disjointness; plant B is exactly this defect and must be seen RED |
| T-144-18 | Repudiation | An entry silently absorbed into a widened taxonomy | mitigate | `_segment_indices` raises a locating `AssertionError` on any unclassifiable entry; the plan forbids widening the taxonomy and requires stop-and-report |
| T-144-19 | Tampering | A plant writing into a committed fixture | mitigate | Scratch copies under `tmp_path` only; `assert mutated_text != real_text`; `git hash-object` before/after on BOTH fixtures; whole-repo porcelain asserted clean |
| T-144-20 | Denial of service | Catastrophic backtracking on a 38 KB header | accept | `\{[^{}]*\}`-family patterns cannot nest and `(.*?)` is bounded by a literal `};`; no nested quantifiers introduced |
| T-144-21 | Repudiation | Implying the attribution is proven CORRECT rather than COMPLETE | mitigate | Docstring paragraph stating the boundary; the record repeats it |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs; RESEARCH § Package Legitimacy Audit records "not applicable" for the phase |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/test_trace_segment_exhaustiveness_v131.py -q -rs` — 0 failed, 0 skipped.
- `cd /workspaces/firestarter && python3 tests/test_trace_segment_exhaustiveness_v131.py` — prints the per-segment old/new/delta table for all three protocols.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — 0 failed.
- `cd /workspaces/firestarter && python3 -m pytest tests/test_golden_trace_identity_eprom_v131.py tests/test_protocol_branch_inventory.py tests/test_checker_convention.py -q` — 0 failed (the goldens and conventions this plan must leave undisturbed).
- `cd /workspaces/firestarter && git status --porcelain` — empty.
</verification>

<success_criteria>
- All 885 entries across both streams are partitioned into exactly one of six named segments, proven by set equality and disjointness.
- Every present segment carries a named attributing decision; the complete-not-correct boundary is stated.
- Both named plants are seen RED with locating messages; the GREEN is attributed to a non-empty two-stream parse with the OUTPUT_ENABLE known-answer self-test passing.
- The gate lives under `firestarter/tests/`, so no checker convention floor moves; no `src/` or fixture file changed.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-04-SUMMARY.md` when done. It MUST contain both
D-18 transcripts verbatim, the per-segment old-versus-new table with each delta's named attribution, the
620 / 265 / 885 subtotals, the `7 + 12 = 19` known-answer figures, and the explicit statement that the gate
proves attribution completeness rather than attribution correctness.
</output>
