---
phase: 144-tests-build-verification
plan: 06
type: execute
wave: 5
depends_on: ["144-05", "144-02"]
commits_land_in: meta
reads_repos: [firestarter_app, firestarter]
files_modified:
  - .planning/phases/144-tests-build-verification/144-06-SUMMARY.md
autonomous: true
requirements: [TEST-07]

must_haves:
  truths:
    - "Per D-19 this plan declares commits_land_in even though it writes no sub-repo file: it READS both sub-repos, and files_modified alone under-detects a submodule target while a worktree leaves submodules empty"
    - "Dual-repo constants parity is proven in BOTH directions: the present path passes verbatim, and the absent path skips cleanly rather than erroring (D-16)"
    - "The absent-path run is a child process with FIRESTARTER_FW_ROOT set in the child's environment, because fw_presence binds its names at import and skipif binds at collection"
    - "The absent-path evidence is a SKIP COUNT, not merely exit 0 — a stray .git inside the 'empty' directory would silently test the present path"
    - "The host suite, CI-scoped ruff, the mypy watermark and the coverage floor are all measured on the CI-parity 3.11 interpreter with addopts cleared (D-21)"
    - "The four CI-scoped commands are the ones ci.yml actually runs, at :81 / :84 / :87 / :90 (C-02)"
  artifacts:
    - path: ".planning/phases/144-tests-build-verification/144-06-SUMMARY.md"
      provides: "The verbatim host-half evidence set for TEST-07: both parity directions, the four CI legs, the suite and coverage figures"
      min_lines: 60
  key_links:
    - from: "firestarter_app/tests/fw_presence.py"
      to: "firestarter (sibling repo)"
      via: "FW_ROOT / '.git' existence probe, bound at import"
      pattern: "FIRESTARTER_FW_ROOT"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file in firestarter/ or firestarter_app/ — this plan is measurement-only; both repos are read-only here (the sole exception being the transient .coverage file the coverage run rewrites)"
    - "MUST NOT run any host suite while the firmware repo is dirty — test_py32_flash_map_host.py asserts _git_porcelain(FW_ROOT) == '' for the SIBLING firmware repo, so firmware work-in-progress turns the HOST suite RED (Pitfall 7 / F-09). Assert firmware porcelain is empty FIRST and stop-and-report if it is not"
    - "MUST NOT use monkeypatch.setenv for the absent-path run — FW_ROOT, FW_REPO_PRESENT and requires_fw bind at import and skipif binds at collection; a child process with the variable in its environment is the only mechanism that works (C-15 / F-11)"
    - "MUST NOT point FIRESTARTER_FW_ROOT at a directory containing a .git entry — FW_REPO_PRESENT probes .exists() only, so a stray marker silently tests the present path"
    - "MUST NOT assert only exit 0 on the absent-path run — assert the skip count"
    - "MUST NOT use the ambient Python 3.12 — it masks CI defects and makes the mypy watermark gate FAIL-OPEN; every host command uses .venv/ci-replica/bin/python (3.11.15)"
    - "MUST NOT omit -o addopts='' on any host pytest invocation — addopts is '-ra -q' and doubling -q suppresses the count line the record needs"
    - "MUST NOT lower the mypy watermark (35) or the coverage floor (70) — a below-watermark INFO line is a lowering invitation, not a pass to celebrate, and lowering it is out of scope here"
    - "MUST NOT commit or stage the app repo's pre-existing untracked entries (.coverage, .planning/config.json, SECURITY.md, four datasheets/*.pdf, write_test_port.sh)"
    - "MUST NOT imply CI covers the three *_v131 firmware envs, or that the app's CI runs the cross-repo parity gates — it does not; the requires_fw gates fail OPEN across the repo boundary by design (D-15, D-16)"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
---

<objective>
Close TEST-07's host half. Prove dual-repo constants parity in both directions, run the four CI-scoped
commands the app's workflow actually runs, and record the host suite and coverage figures on the CI-parity
interpreter — every verdict verbatim.

Purpose: this is the sweep that catches devcontainer-masked CI defects before a beta push. The ambient
interpreter is 3.12 while CI targets 3.11, and under the wrong interpreter the mypy watermark gate is
fail-open — a recorded hazard. The absent-path half exists because the cross-repo gates fail OPEN by design:
`requires_fw` skips when the sibling repo is absent, which is honest but means CI proves nothing about parity.
D-16 measures both directions instead of pretending otherwise.

Output: `144-06-SUMMARY.md` carrying the verbatim evidence set. No source file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-VALIDATION.md
</context>

<artifacts_this_phase_produces>
- `.planning/phases/144-tests-build-verification/144-06-SUMMARY.md` (the only file this plan writes)
- Scratch, not committed: `/tmp/gsd-144/host_present.log`, `/tmp/gsd-144/host_absent.log`,
  `/tmp/gsd-144/host_cov.log`, `/tmp/gsd-144/ruff.log`, `/tmp/gsd-144/mypy.log`, and an empty scratch
  directory created with `mktemp -d` for the absent-path run

No new symbol, no new module, no new seam. This plan measures only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: D-16's bidirectional constants-parity sweep, present path then absent path</name>
  <files>/tmp/gsd-144/host_present.log, /tmp/gsd-144/host_absent.log</files>
  <read_first>
    - `firestarter_app/tests/fw_presence.py` — read in full (141 lines). The import-time binding warning at :35-45 is the whole reason the absent run must be a child process; the single seam at :77-80; the `.git` marker probe at :86-88 (`.exists()`, deliberately, so a submodule or worktree `.git` FILE also counts); `requires_fw` at :102; `MissingScanTargetError` at :105; `fw_path` at :117-140.
    - `firestarter_app/tests/test_revision_constants_parity.py` — the bidirectional `CMD_*` / `FLAG_*` gate: `FIRMWARE_HEADER` at :148, the requires_fw / no-requires_fw split rationale at :713-725, the three fixture-driven planted legs at :727-786. Those are the 6 legs that stay live in the absent run.
    - `firestarter_app/tests/test_py32_flash_map_host.py` :351-395 — the sibling-repo porcelain assertion at :391 that makes this plan's ordering non-negotiable.
    - `.planning/phases/144-tests-build-verification/144-RESEARCH.md` — F-11 in full (both directions executed, with the verbatim transcripts and the 13-module `requires_fw` census) and F-12's command table.
  </read_first>
  <action>
Precondition, asserted before anything else: `git -C /workspaces/firestarter status --porcelain` must be
EMPTY. If it is not, stop and report — a dirty firmware repo turns `test_py32_flash_map_host.py` RED for
reasons that have nothing to do with the host, and mis-diagnosing that is the trap Pitfall 7 names. Record the
firmware HEAD sha alongside, so the SUMMARY says which firmware tree the parity was measured against.

Present path. From inside `/workspaces/firestarter_app`, using `.venv/ci-replica/bin/python` (3.11.15 — never
the ambient 3.12), run the parity module with `-o addopts="" -q`: expect 14 passed, and capture the output
verbatim. Then run the full suite with `-o addopts="" -rs -q`, teeing to `/tmp/gsd-144/host_present.log` —
that is both the present-direction sweep and the run establishing the suite total for task 2's comparison.
Record the total passed, and the skip list with reasons (`-rs` prints them), so the record states which gates
skipped in a repo-present environment and why.

Absent path. Create an empty scratch directory with `mktemp -d` and confirm it contains NO `.git` entry before
use — `FW_REPO_PRESENT` probes `FW_ROOT / ".git"` with `.exists()`, so a stray marker would silently test the
present path and the run would prove nothing. Then run the full suite in a CHILD process with
`FIRESTARTER_FW_ROOT` set to that directory in the child's environment, `-o addopts="" -rs -q`, teeing to
`/tmp/gsd-144/host_absent.log`. `monkeypatch.setenv` cannot substitute: `FW_ROOT`, `FW_REPO_PRESENT` and
`requires_fw` bind at import and `pytest.mark.skipif` binds at collection.

Assert on the absent run: exit code 0, ZERO errors, and a non-zero SKIP COUNT — the count, not merely the
exit code. Anchor it with a known answer by also running just `tests/test_revision_constants_parity.py` in the
absent child, which RESEARCH measured as 6 passed, 8 skipped with one canonical reason string naming the
probed marker path. Record both the whole-suite skip count and that module's 6-and-8 split, and confirm every
skip reason names the probed marker path rather than being anonymous.

Record explicitly, as the property D-16 exists to surface rather than as an oversight: the 6 legs that still
pass with no firmware checkout are the fixture-driven planted-violation legs, which read committed fixtures
under `tests/fixtures/` and therefore carry no `requires_fw`. That is what keeps the checker's failure modes
exercised in a host-only environment, and it is why plan 144-02's two CAP-03 plants were deliberately given
the same treatment. State the honest limit too: `requires_fw` fails OPEN across the repo boundary, so the
app's CI proves nothing about parity today; adding a firmware checkout to app CI is deferred, because it needs
a decision about which firmware ref to pin and `beta` and the v1.31 branch disagree.

State the sweep's scope in the record: which modules the absent-path run covered (the whole suite, of which 13
modules use `requires_fw`) and why the whole suite rather than the named subset — either satisfies D-16, but
the record must say which was done.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0 && git rev-parse HEAD</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python --version && .venv/ci-replica/bin/python -m pytest tests/test_revision_constants_parity.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -rs -q 2>&1 | tee /tmp/gsd-144/host_present.log | tail -5</automated>
    <automated>cd /workspaces/firestarter_app && EMPTY=$(mktemp -d) && ls -A "$EMPTY" | wc -l | grep -x 0 && FIRESTARTER_FW_ROOT="$EMPTY" .venv/ci-replica/bin/python -m pytest tests/test_revision_constants_parity.py -o addopts="" -rs -q 2>&1 | tail -4</automated>
    <automated>cd /workspaces/firestarter_app && EMPTY=$(mktemp -d) && FIRESTARTER_FW_ROOT="$EMPTY" .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -rs -q 2>&1 | tee /tmp/gsd-144/host_absent.log | tail -5</automated>
    <automated>cd /workspaces/firestarter_app && grep -cE "^(ERROR|E ) " /tmp/gsd-144/host_absent.log | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter status --porcelain` was empty before any host run, and the firmware HEAD sha is recorded in the SUMMARY.
    - `.venv/ci-replica/bin/python --version` prints Python 3.11.15; no host command used the ambient interpreter.
    - The present-path parity run reports 14 passed, captured verbatim.
    - The present-path full-suite run reports 0 failed with a passed count at least 1578 plus the tests plan 144-02 added (its new module contributes at least 11), and its skip list with reasons is recorded.
    - The absent-path scratch directory contained zero entries, including no `.git`, verified with `ls -A`.
    - The absent-path parity run reports exactly 6 passed and 8 skipped, with one canonical reason string naming the probed marker path; the whole-suite absent run exits 0 with zero errors and a recorded non-zero skip count.
    - The SUMMARY names the 6 surviving legs as the fixture-driven planted legs and states why they carry no `requires_fw`.
    - The SUMMARY states the sweep's scope (whole suite; 13 modules use `requires_fw`) and the honest fail-open limit across the repo boundary, without implying CI parity coverage.
    - No file in either sub-repo was modified (the transient `.coverage` rewrite aside), and nothing was staged or committed there.
  </acceptance_criteria>
  <done>Parity is proven in both directions on the CI-parity interpreter, with the absent path run as a child process and evidenced by skip counts rather than exit codes.</done>
</task>

<task type="auto">
  <name>Task 2: The four CI-scoped commands and the D-21 suite/coverage measurement</name>
  <files>/tmp/gsd-144/ruff.log, /tmp/gsd-144/mypy.log, /tmp/gsd-144/host_cov.log</files>
  <read_first>
    - `firestarter_app/.github/workflows/ci.yml` :81 / :84 / :87 / :90 — the four commands verbatim (`ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`, `python tools/check_mypy_watermark.py`, `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`). Per C-02 the coverage leg is at :90, outside the ":80-:87" range CONTEXT.md cites, so a plan quoting that range would omit the coverage floor from its own verification.
    - `firestarter_app/tools/check_mypy_watermark.py` — `get_watermark()`'s regex at :91-104 and `enforce_watermark` at :211-227; and `firestarter_app/pyproject.toml` :174 (`# mypy_error_watermark = 35`), :107 (`addopts = "-ra -q"`), :110-111 (ruff `py39`, line length 88).
    - `.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md` section 7.6 — the prior recorded host figures (1578 passed, 82.92 percent coverage) this run is compared against.
  </read_first>
  <action>
Run all four CI-scoped commands from inside `/workspaces/firestarter_app` on `.venv/ci-replica/bin/python`
(and that venv's `ruff`), capturing each verbatim:

1. `ruff check firestarter/ tests/` — the CI scope is those two directories, not the whole repo. Expect exit 0.
2. `ruff format --check firestarter/ tests/` — expect exit 0. A new module written to the wrong target version
   or line length goes RED here on arrival, which is why plan 144-02 wrote to py39 and 88.
3. `python tools/check_mypy_watermark.py` — expect exit 0 against the watermark of 35. If it prints an `INFO:`
   line because the error count is BELOW the watermark, record the line and note that a below-watermark result
   is a lowering invitation, not a pass to celebrate — and do not lower it here.
4. `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70` with `-o addopts=""`
   appended for D-21's record, teeing to `/tmp/gsd-144/host_cov.log`. Expect 0 failed, coverage above the 70
   floor, and a total at least 1578 plus plan 144-02's new tests.

Compare against the Phase 143 recorded state — 1578 passed at 82.92 percent — and record the delta with its
cause (plan 144-02's new cross-repo gate module and its two planted legs). If the coverage percentage moves,
say by how much and why; a new test module that adds no product code typically moves it only marginally.

Then assemble the SUMMARY's TEST-07 evidence set, all verbatim: the firmware-side figures already recorded by
plans 144-01 through 144-05 (referenced, not re-run here), both parity directions from task 1, and these four
legs. State plainly, in the record's own words, the two absences this phase does not close: the three
`*_v131` firmware envs run in no CI leg of either repository, and the app's CI has no firmware checkout so
every cross-repo parity gate skips there. Neither is presented as covered.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check firestarter/ tests/ 2>&1 | tee /tmp/gsd-144/ruff.log | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff format --check firestarter/ tests/ 2>&1 | tee -a /tmp/gsd-144/ruff.log | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python tools/check_mypy_watermark.py 2>&1 | tee /tmp/gsd-144/mypy.log | tail -5</automated>
    <automated>cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" --cov=firestarter --cov-report=term-missing --cov-fail-under=70 2>&1 | tee /tmp/gsd-144/host_cov.log | tail -6</automated>
    <automated>cd /workspaces/firestarter_app && git status --porcelain | grep -v "^?? " | wc -l | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - All four CI-scoped commands exit 0, each captured verbatim, and the SUMMARY cites them as `ci.yml` :81 / :84 / :87 / :90 (C-02, not ":80-:87").
    - The coverage run reports 0 failed, a coverage percentage above 70, and a passed total at least 1578 plus plan 144-02's added tests; the delta against Phase 143's 1578 / 82.92 percent is stated with its cause.
    - Every host command was run with `.venv/ci-replica/bin/python` or that venv's `ruff`, and every pytest invocation carried `-o addopts=""`.
    - If the mypy gate printed an `INFO:` below-watermark line, the SUMMARY quotes it and states that the watermark is deliberately not lowered here.
    - `git -C /workspaces/firestarter_app status --porcelain` shows no tracked-file modification (untracked entries may remain; `.coverage` may be rewritten).
    - `git -C /workspaces/firestarter status --porcelain` is still empty at plan end.
    - The SUMMARY restates both absences — no CI leg covers the three `*_v131` envs, and the app's CI has no firmware checkout — without implying coverage.
  </acceptance_criteria>
  <done>The four CI-scoped legs are green on the CI-parity interpreter, the suite and coverage figures are recorded with their delta and cause, and both standing absences are restated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sibling repo presence → gate reach | Whether the firmware checkout exists silently decides how much of the parity surface runs |
| env var → repo root | `FIRESTARTER_FW_ROOT` redirects the probe, and the probe is a bare `.exists()` on a `.git` marker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-28 | Repudiation | An absent-path run that silently tests the PRESENT path because the scratch dir held a `.git` entry | mitigate | `ls -A` asserted empty before use; the skip COUNT asserted, not merely exit 0; the parity module's 6-and-8 split used as a known answer |
| T-144-29 | Repudiation | A devcontainer-masked defect surviving to a beta push because the ambient 3.12 was used | mitigate | Every host command pinned to `.venv/ci-replica/bin/python` 3.11.15, with the version printed and recorded; the mypy gate is fail-open under the wrong interpreter, so the pin is asserted rather than assumed |
| T-144-30 | Repudiation | Claiming CI coverage that does not exist | mitigate | Both absences restated verbatim in the record: no CI leg runs the three `*_v131` firmware envs, and the app's CI has no firmware checkout so the cross-repo gates skip |
| T-144-31 | Repudiation | A host RED misattributed to the host when its cause is a dirty firmware repo | mitigate | Firmware porcelain asserted empty before any host suite run, with the firmware HEAD sha recorded; a dirty tree is stop-and-report |
| T-144-32 | Tampering | Quietly lowering the mypy watermark or the coverage floor to make a leg pass | mitigate | Both declared out of scope in the prohibitions; a below-watermark `INFO:` line must be recorded as an invitation rather than acted on |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs; both environments are already provisioned and RESEARCH § Package Legitimacy Audit records "not applicable" for the phase |
</threat_model>

<verification>
- `cd /workspaces/firestarter && git status --porcelain` — empty, checked BEFORE any host run.
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -rs -q` — 0 failed.
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/ruff check firestarter/ tests/ && .venv/ci-replica/bin/ruff format --check firestarter/ tests/` — both exit 0.
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/python tools/check_mypy_watermark.py` — exit 0.
- `cd /workspaces/firestarter_app && .venv/ci-replica/bin/python -m pytest tests/ -o addopts="" --cov=firestarter --cov-report=term-missing --cov-fail-under=70` — 0 failed, above the floor.
- Absent-path child run — exit 0, zero errors, non-zero skip count, and 6 passed / 8 skipped for the parity module.
</verification>

<success_criteria>
- Constants parity is proven in both directions with verbatim transcripts, the absent direction via a child process and evidenced by skip counts.
- All four CI-scoped commands pass on the 3.11 replica, cited at their real `ci.yml` line numbers.
- The host suite and coverage figures are recorded with their delta against Phase 143 and the cause of the delta.
- No tracked file in either sub-repo is modified; the firmware repo stays clean throughout.
- The record claims no CI coverage the tree does not have.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-06-SUMMARY.md` when done. It MUST contain: the
firmware HEAD sha and porcelain check that preceded the runs; the verbatim present-path parity output (14
passed); the verbatim absent-path output including the canonical skip reason string and the 6-and-8 split; the
whole-suite skip count for the absent run; all four CI-scoped commands' verbatim output with their `ci.yml`
line citations; the suite total and coverage percentage with the delta against 1578 / 82.92 percent and its
cause; and the two restated absences.
</output>
