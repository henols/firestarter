---
phase: 144-tests-build-verification
plan: 05
type: execute
wave: 4
depends_on: ["144-04"]
commits_land_in: firestarter
files_modified:
  - firestarter/scripts/baseline/size_baseline.json
  - firestarter/scripts/baseline/size_baseline_base01.json
  - firestarter/scripts/baseline/size_baseline_v131.json
  - firestarter/tests/fixtures/captured_build_uno.log
  - firestarter/tests/fixtures/captured_build_uno328pb.log
  - firestarter/tests/fixtures/captured_build_leonardo.log
  - firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log
  - firestarter/tests/fixtures/planted_size_baseline_policy_leonardo_growth.log
  - firestarter/tests/fixtures/planted_size_baseline_policy_ram_moved.log
  - firestarter/tests/fixtures/planted_size_baseline_flash_regression.log
  - firestarter/tests/test_check_size_baseline.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-07, TEST-08]

must_haves:
  truths:
    - "ONE cold consolidated run exercises every v131 env and both pinned envs against the FINAL tree, with every verdict recorded verbatim (D-02)"
    - "Per-target flash and RAM are measured against the PREP-03 anchor and the delta recorded: +870 / +870 / +890 B flash, RAM unmoved on all three (D-09, TEST-08)"
    - "The Leonardo ceiling is checked explicitly, not discovered: 26906 of 28672 B, 93.8 percent, 1766 B headroom"
    - "size_baseline.json, size_baseline_base01.json and size_baseline_v131.json are re-anchored to the v1.31 tip, and the everyday strict-identity gate reads GREEN again (D-10, D-11, D-12, D-13)"
    - "size_baseline_v131.json gains env records for native_params_v131 and native_loop_v131, which it never held (C-01)"
    - "Every planted-violation leg of test_check_size_baseline.py still fires after the re-anchor, each re-proven RED at its new figures (OD-01, D-18)"
    - "MERGE-05's green is disclosed as an anchor move, never as growth staying inside the band (D-14)"
  artifacts:
    - path: "firestarter/scripts/baseline/size_baseline.json"
      provides: "The PREP-03 anchor rewritten to the v1.31 tip"
      contains: "24824"
    - path: "firestarter/scripts/baseline/size_baseline_base01.json"
      provides: "MERGE-05's comparison record re-anchored; band literals untouched"
      contains: "26906"
    - path: "firestarter/scripts/baseline/size_baseline_v131.json"
      provides: "The running record of the envs no gate covers, with two env records added"
      contains: "native_loop_v131"
    - path: "firestarter/tests/fixtures/captured_build_leonardo.log"
      provides: "Re-captured cold build output at the v1.31 tip"
      contains: "26906"
  key_links:
    - from: "firestarter/tests/test_check_size_baseline.py"
      to: "firestarter/scripts/baseline/size_baseline.json"
      via: "subprocess invocation of the real checker against committed .log fixtures"
      pattern: "captured_build_"
    - from: "firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log"
      to: "firestarter/scripts/baseline/size_baseline_base01.json"
      via: "--policy merge05 band comparison; 24824 + 65 keeps delta=+65 against the new anchor"
      pattern: "24889"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under firestarter/src/ (D-04)"
    - "MUST NOT modify firestarter/scripts/check_size_baseline.py or check_build_warnings.py — the band literals and NATIVE_ENVS stay exactly as they are (D-11, D-22)"
    - "MUST NOT widen MERGE05_UNO_CLASS_FLASH_BAND (64) or the leonardo 0 B band"
    - "MUST NOT feed a *_v131 env name to check_size_baseline.py or check_build_warnings.py under ANY --baseline, including size_baseline_v131.json after this plan lands — an unknown env raises an uncaught KeyError, exit 1, a FALSE regression signal (D-22 restated unconditionally per C-03)"
    - "MUST NOT measure warm: every figure comes from `pio run -t clean -e <env>` then `pio run -e <env>`, or from `rm -rf .pio/build/<env>` then `pio test -e <env>`, one uninterrupted invocation per env with a long explicit timeout (Pitfall 6)"
    - "MUST NOT write a warm figure into any watermark field — 998 is the warm native figure against the 1166 cold watermark"
    - "MUST NOT change any native watermark or AVR warning rule — native stays `<= 1166` with zero headroom, AVR stays `== 0` (D-23)"
    - "MUST NOT change native_envs case/suite counts for native or native_nodevtools — they stay 141 / 17"
    - "MUST NOT preserve the v1.24 content of size_baseline_base01.json in-tree — git history is the record (D-12)"
    - "MUST NOT add a new planted fixture — modify the existing ones, so test_checker_convention.py's FIXTURE_FLOOR of 15 needs no edit"
    - "MUST NOT change the `delta=+65` or `delta=+1` assertions in test_check_size_baseline.py — preserving those offsets is the point of re-deriving the plants"
    - "MUST NOT leave any of the four re-derived plants unproven — a re-derived plant is a NEW plant and needs its own RED and GREEN (D-18)"
    - "MUST NOT run any suite before committing (D-20 / F-09)"
    - "MUST NOT claim anything about real silicon — no bench run happens here (Phase 145 owns that)"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
---

<objective>
Take the phase's ONE cold consolidated measurement against the final tree, then re-anchor all three size
baselines to the v1.31 tip and re-derive the seven captured/planted `.log` fixtures and two figure literals
that the re-anchor moves the ground under. Retire the milestone's second standing RED — MERGE-05's size band
— and disclose that retirement honestly.

Purpose: D-02 rejected citing the owning phases' recorded runs, because no single run has ever exercised all
88 existing v131 cases against Phases 140, 141, 142 and 143's landed code together; a cross-phase
interaction is exactly what this run exists to catch. And D-10's reasoning for rewriting the anchor is that a
gate which is RED for a known accepted reason can no longer report an unknown one.

Output: three re-anchored baseline JSONs, three re-captured build logs, four re-derived planted logs, two
updated figure literals, and a verbatim verdict set for every gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- Rewritten JSON records (existing paths): `scripts/baseline/size_baseline.json`,
  `scripts/baseline/size_baseline_base01.json`, `scripts/baseline/size_baseline_v131.json`
- NEW keys added to `size_baseline_v131.json`: `native_envs.native_params_v131`,
  `native_envs.native_loop_v131`, and (subject to OD-02's cold-measurement condition)
  `warnings.native.native_params_v131`, `warnings.native.native_loop_v131`
- Re-captured fixtures (existing paths): `tests/fixtures/captured_build_{uno,uno328pb,leonardo}.log`
- Re-derived planted fixtures (existing paths):
  `tests/fixtures/planted_size_baseline_policy_{uno_over_band,leonardo_growth,ram_moved}.log`,
  `tests/fixtures/planted_size_baseline_flash_regression.log`
- Two updated figure literals plus provenance docstrings in `tests/test_check_size_baseline.py`
- Scratch, not committed: `/tmp/gsd-144/build_{uno,uno328pb,leonardo}.log`,
  `/tmp/gsd-144/{native,native_nodevtools,native_params_v131,native_loop_v131,native_trace_v131}.log`,
  `/tmp/gsd-144/verdicts.txt`

No file is added and no file is deleted; no checker script is modified.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The ONE cold consolidated run, and the pre-rewrite gate verdicts</name>
  <files>/tmp/gsd-144/build_uno.log, /tmp/gsd-144/build_uno328pb.log, /tmp/gsd-144/build_leonardo.log, /tmp/gsd-144/native.log, /tmp/gsd-144/native_nodevtools.log, /tmp/gsd-144/native_params_v131.log, /tmp/gsd-144/native_loop_v131.log, /tmp/gsd-144/native_trace_v131.log, /tmp/gsd-144/verdicts.txt</files>
  <read_first>
    - `firestarter/scripts/check_size_baseline.py` — `AVR_ENVS` :99, `NATIVE_ENVS` :100, `MERGE05_UNO_CLASS_FLASH_BAND` :107, `SIZE_RE` :112, `CASES_RE` :119, `SUITE_RE` :124, `compare_avr` :183, `compare_avr_policy_merge05` :214, `compare_native`'s bare subscript, and `main()`'s never-vacuous guard and CLI (`--baseline`, `--avr-log ENV=PATH`, `--native-log ENV=PATH`, `--rebuild`, `--policy merge05`). READ ONLY — this script is not modified.
    - `firestarter/scripts/check_build_warnings.py` — `MACRO_REDEF_RE` :96, `WARNING_LINE_RE` :101, `check_env` :121-165, and `main()`'s `--log ENV=PATH` / `--rebuild` / `--baseline` argv and its never-vacuous guard. READ ONLY.
    - `firestarter/scripts/baseline/size_baseline.json` — `meta.note`'s measurement procedure and `meta.warm_vs_cold_correction`'s warm-versus-cold rule; the current `avr_targets`, `native_envs` and `warnings` blocks.
    - `firestarter/platformio.ini` — the caveat blocks at :293 (`native_trace_v131`), :331 (`native_params_v131`) and :373 (`native_loop_v131`), which are the authoritative statement of D-15 and D-22, plus each env's `test_filter`.
    - `.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md` — section 7.1's cold flash/RAM table and section 7.4's verbatim `check_size_baseline.py` output, for comparison against this run.
  </read_first>
  <action>
Run every command with cwd `/workspaces/firestarter` and a long explicit timeout (at least 600000 ms for
each AVR build): a default two-minute cap truncates the toolchain build mid-compile and silently
contaminates the measurement, a trap `size_baseline.json:meta.note` names explicitly. Tee every command's
combined output to the named scratch file under `/tmp/gsd-144` and append every gate verdict verbatim to
`/tmp/gsd-144/verdicts.txt`.

AVR, one uninterrupted invocation per env: `pio run -t clean -e <env>` then `pio run -e <env>` for `uno`,
`uno328pb`, `leonardo`. Expected, and a deviation is stop-and-report rather than absorb: uno flash 24824 of
32256 with RAM 1573 of 2048; uno328pb flash 24874 of 32384 with RAM 1579 of 2048; leonardo flash 26906 of
28672 with RAM 2014 of 2560. That is +870 / +870 / +890 B flash against the PREP-03 anchor's
23954 / 24004 / 26016, RAM unmoved on all three. Compute and record the Leonardo ceiling explicitly:
26906 of 28672 is 93.8 percent, leaving 1766 B — TEST-08 requires it watched, not discovered.

Native, `rm -rf .pio/build/<env>` then `pio test -e <env>`, for all five: `native` and `native_nodevtools`
(each expected at 141 cases / 17 suites — the pinned figures `compare_native` asserts), then the three v131
envs BY NAME: `native_params_v131` (9 cases, 1 suite), `native_loop_v131` (79 cases across its two suites,
47 in `test_loop_eprom_v131` plus 32 in `test_vpp_eprom_v131`), `native_trace_v131` (5 cases, 0 failed —
retired from RED by plan 144-03). Label every count with the env or suite that produced it: 88 is the
three-suite mapping denominator and 79 is `native_loop_v131`'s per-env figure; never add them.

The three v131 envs run in NO CI leg of either repository. Record that as a restated absence, not as an
implied coverage claim, and never pass any of their names to either checker script.

Warning counts, for the two envs no record holds (OD-02, RESEARCH Open Question 1): parse the cold
`native_params_v131` and `native_loop_v131` logs directly with the SAME two regexes
`check_build_warnings.py` uses — macro redefinitions match `warning:` followed by a quoted macro name and
the word `redefined`; the total is the count of lines containing `warning:`. Do this by grep on the captured
log, NOT by invoking `check_build_warnings.py` with a `*_v131` env name (D-22). Also re-measure
`native_trace_v131`'s cold counts, since plan 144-03 changed its fixture (the current record holds 140). If
a count genuinely cannot be obtained without violating D-22, record that gap explicitly for the phase record
rather than omitting the figure silently — either outcome is acceptable, a silent omission is not.

Pre-rewrite gate verdicts, captured verbatim as the "before" evidence D-14's disclosure refers to:
  - strict identity, default baseline: `python3 scripts/check_size_baseline.py` with
    `--avr-log uno=/tmp/gsd-144/build_uno.log`, `--avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log`,
    `--avr-log leonardo=/tmp/gsd-144/build_leonardo.log`. Expect a RED naming the three deltas.
  - band policy: the same three `--avr-log` arguments plus `--policy merge05 --baseline
    scripts/baseline/size_baseline_base01.json`. Expect a RED — +870 against a 64 B uno-class band and
    +890 against leonardo's 0 B band.
  - native compare: `--native-log native=/tmp/gsd-144/native.log --native-log
    native_nodevtools=/tmp/gsd-144/native_nodevtools.log`, default baseline. Expect PASS at 141 / 17.
  - warnings: `python3 scripts/check_build_warnings.py` with `--log` for `uno`, `uno328pb`, `leonardo`,
    `native` and `native_nodevtools` only. Expect PASS: AVR `== 0` on all three, native at or below the
    1166 cold watermark. A `Traceback` in a gate's output is never a working gate — if one appears, stop and
    report.

Change no tracked file in this task. The repo must still be clean at task end.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -hE "^(RAM|Flash):" /tmp/gsd-144/build_uno.log /tmp/gsd-144/build_uno328pb.log /tmp/gsd-144/build_leonardo.log</automated>
    <automated>cd /workspaces/firestarter && grep -hE "^=+ [0-9]+ test cases:" /tmp/gsd-144/native.log /tmp/gsd-144/native_nodevtools.log /tmp/gsd-144/native_params_v131.log /tmp/gsd-144/native_loop_v131.log /tmp/gsd-144/native_trace_v131.log</automated>
    <automated>cd /workspaces/firestarter && for f in native_params_v131 native_loop_v131 native_trace_v131; do printf "%s macro=" "$f"; grep -cE 'warning:[[:space:]]*"[^"]+"[[:space:]]+redefined' /tmp/gsd-144/$f.log; printf "%s total=" "$f"; grep -c "warning:" /tmp/gsd-144/$f.log; done</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_build_warnings.py --log uno=/tmp/gsd-144/build_uno.log --log uno328pb=/tmp/gsd-144/build_uno328pb.log --log leonardo=/tmp/gsd-144/build_leonardo.log --log native=/tmp/gsd-144/native.log --log native_nodevtools=/tmp/gsd-144/native_nodevtools.log 2>&1 | tee -a /tmp/gsd-144/verdicts.txt | tail -8</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - All eight scratch logs exist and each AVR log carries exactly one `Flash:` and one `RAM:` line from an uninterrupted post-clean build.
    - The three AVR figures read 24824 / 32256 and 1573 / 2048 (uno), 24874 / 32384 and 1579 / 2048 (uno328pb), 26906 / 28672 and 2014 / 2560 (leonardo). Any deviation is stop-and-report.
    - The recorded flash deltas against the PREP-03 anchor are exactly +870, +870, +890, and RAM deltas are 0, 0, 0.
    - The Leonardo ceiling is recorded as 93.8 percent with 1766 B headroom, computed from the measured figures rather than quoted.
    - `native` and `native_nodevtools` each report 141 test cases across 17 suites; `native_params_v131` 9; `native_loop_v131` 79 (labelled as 47 + 32 across two suites); `native_trace_v131` 5 with 0 failed.
    - Cold macro-redefinition and total warning counts exist for `native_params_v131`, `native_loop_v131` and `native_trace_v131`, obtained by grep on the cold logs — with no invocation of either checker naming a `*_v131` env.
    - `check_build_warnings.py` PASSes for the five permitted envs; `/tmp/gsd-144/verdicts.txt` contains its verbatim output and no `Traceback`.
    - The pre-rewrite strict-identity and `--policy merge05` verdicts are captured verbatim, both RED, each naming its deltas.
    - `git status --porcelain` in `firestarter` is empty — this task changed no tracked file.
  </acceptance_criteria>
  <done>One cold consolidated run has measured every AVR target and all five native envs against the final tree, with cold warning counts for the three v131 envs and verbatim pre-rewrite verdicts for every gate.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor all three baselines, re-capture and re-derive the seven fixtures, update the two literals — one commit</name>
  <files>firestarter/scripts/baseline/size_baseline.json, firestarter/scripts/baseline/size_baseline_base01.json, firestarter/scripts/baseline/size_baseline_v131.json, firestarter/tests/fixtures/captured_build_uno.log, firestarter/tests/fixtures/captured_build_uno328pb.log, firestarter/tests/fixtures/captured_build_leonardo.log, firestarter/tests/fixtures/planted_size_baseline_policy_uno_over_band.log, firestarter/tests/fixtures/planted_size_baseline_policy_leonardo_growth.log, firestarter/tests/fixtures/planted_size_baseline_policy_ram_moved.log, firestarter/tests/fixtures/planted_size_baseline_flash_regression.log, firestarter/tests/test_check_size_baseline.py</files>
  <read_first>
    - All three of `firestarter/scripts/baseline/size_baseline.json`, `size_baseline_base01.json`, `size_baseline_v131.json` — every `meta` field, `avr_targets`, `native_envs`, `envs_agree`, `envs_agree_note`, `warnings`. Note in particular `size_baseline.json`'s `meta.supersedes` (which asserts BASE-01 is immutable and remains MERGE-05's judged reference — a claim D-11 falsifies), its `meta.deltas_vs_base01` block, `size_baseline_v131.json`'s `meta.frozen_for`, `meta.consumed_by` and `meta.deltas_vs_size_baseline`, and the existing `native_envs.native_trace_v131` / `warnings.native.native_trace_v131` pair, which is the shape to replicate twice.
    - `firestarter/tests/test_check_size_baseline.py` in full — the module docstring's fixture inventory (:20-105), `test_clean_avr_all_three_envs_pass` :138-155, `test_clean_native_both_envs_pass` :158-171, `test_planted_flash_regression_flips_checker_to_failure` :174-190 (the two figure literals at :187-188), `test_policy_merge05_permits_the_measured_landing_deltas` :267-294 (including the Plan 124-10 re-capture precedent quoted at :272-279), `test_policy_merge05_fires_on_uno_class_over_band` :297-316 (the `delta=+65` assertion at :315), `test_policy_merge05_fires_on_leonardo_growth` :319-337 (`delta=+1` at :337), `test_policy_merge05_fires_on_ram_move` :340-357, and `test_default_mode_is_unchanged_by_the_new_flag` :360-377.
    - `firestarter/tests/fixtures/captured_build_uno.log` and `planted_size_baseline_policy_uno_over_band.log` — the exact `pio` log shape a fixture must keep (the `RAM:` and `Flash:` lines are what `SIZE_RE` reads).
    - `.planning/phases/144-tests-build-verification/144-PATTERNS.md` § "Collateral: D-10/D-11 break five legs of test_check_size_baseline.py" — the five affected legs, the survivor list, the silently-hollowed leg, and the recommended discharge.
  </read_first>
  <action>
This task is a single mechanical commit. Every target figure is given below; derive nothing from memory, and
take every measured figure from task 1's logs.

Baseline A — `scripts/baseline/size_baseline.json` (D-10). Rewrite `avr_targets` to: uno
`flash_used 24824, flash_total 32256, flash_free 7432, ram_used 1573, ram_total 2048, ram_free 475`;
uno328pb `24874, 32384, 7510, 1579, 2048, 469`; leonardo `26906, 28672, 1766, 2014, 2560, 546`. PRESERVE
`native_envs` exactly (native and native_nodevtools at 141/141/17/true, native_pinmap_provisional at
10/10/1/true) and PRESERVE the entire `warnings` block verbatim (native and native_nodevtools at 1166,
native_pinmap_provisional at 138, all three AVR envs at 0, both policy rules) — D-23 says they are unchanged,
and `check_build_warnings.py` plus `tests/test_check_build_warnings.py` read that same block. Update `meta`:
`generated` to today, `phase` to 144, `generated_by` naming this plan and task, `firmware_tree_sha` and
`host_app_tree_sha` from `git rev-parse HEAD^{tree}` in each repo, and a `note` stating the measurement
procedure actually used. Recompute `deltas_vs_base01` — after D-11 both files hold the same three figures, so
every delta is zero; rewrite each `merge05_clause` to say so AND to carry D-14's disclosure in the file
itself: the band now measures from the v1.31 anchor, so a zero delta means the anchor moved, not that growth
stayed inside v1.24's band. Correct `meta.supersedes`, which currently asserts BASE-01 is immutable and
remains MERGE-05's judged comparison reference: state that Phase 144 / D-11 re-anchored BASE-01 in place,
retiring its v1.24 semantics while keeping its forward band mechanism, and that the v1.24 figures remain
recoverable from git history (D-12).

Baseline B — `scripts/baseline/size_baseline_base01.json` (D-11, D-12). Overwrite `avr_targets` with the
same three tip figures and recomputed `flash_free` (7432 / 7510 / 1766); PRESERVE its `native_envs` (141/17
on both) and its whole `warnings` block (the 360 watermarks are a v1.23-era record and are not this phase's
to move). Add a `meta` field recording that the v1.24 semantics are retired and the forward mechanism kept,
that `MERGE05_UNO_CLASS_FLASH_BAND` stays 64 and the leonardo band stays 0, that `check_size_baseline.py` is
NOT modified, and that no in-tree copy of the v1.24 content is kept because git history is the record. Keep
the canonical invocation unchanged: `--policy merge05` still names this file explicitly via `--baseline`.

Baseline C — `scripts/baseline/size_baseline_v131.json` (D-13 + C-01). Rewrite `avr_targets` to the same tip
figures. In `native_envs`: keep `native`, `native_nodevtools` and `native_pinmap_provisional` as they are;
update `native_trace_v131` to task 1's measured `{cases, succeeded, suites, all_passed}` (expected
`5, 5, 1, true` — now genuinely all-passing rather than aspirational); and ADD two records that this file has
never held — `native_params_v131` at `9, 9, 1, true` and `native_loop_v131` at `79, 79, 2, true`. In
`warnings.native`: add `native_params_v131` and `native_loop_v131` using task 1's COLD grep-derived counts,
and update `native_trace_v131`'s counts if the re-measure moved them from 140. If any of those counts could
not be obtained cold, omit that field and record the gap in `meta` explicitly, naming which count is missing
and why — never write a warm figure into a watermark field. Recompute `deltas_vs_size_baseline` (all zero
again, both files now at the tip) and `deltas_vs_base01`. Update `meta.frozen_for` and `meta.consumed_by` to
past tense, and record RESEARCH Open Question 4's reading: `frozen_for`'s "TEST-08 must pass `--baseline
…_v131.json` explicitly" is a re-derivation instruction, not a gate leg — this file is a record, no code
reads it. Add a `meta` note recording C-01's policy change: `141-NEW-TRACE.md` section 6 held these two envs'
counts in prose only and explicitly "never in a baseline JSON"; D-13 changes that, and this is where.

Fixtures — re-capture (OD-01 step 1). Replace `tests/fixtures/captured_build_uno.log`,
`captured_build_uno328pb.log` and `captured_build_leonardo.log` with task 1's real cold `pio run` output for
the corresponding env. These are captured logs, so copy them verbatim rather than editing the old files.

Fixtures — re-derive the four plants (OD-01 step 2 and 3), each preserving its SINGLE cause so every literal
the tests assert stays valid:
  - `planted_size_baseline_policy_uno_over_band.log` — a uno log identical to the new capture except
    `Flash: used 24889 bytes from 32256 bytes` (24824 + 65, one byte outside the 64 B uno-class band, keeping
    `delta=+65`). RAM stays 1573.
  - `planted_size_baseline_policy_leonardo_growth.log` — a leonardo log identical to the new capture except
    `Flash: used 26907 bytes from 28672 bytes` (26906 + 1, keeping `delta=+1`). RAM stays 2014.
  - `planted_size_baseline_policy_ram_moved.log` — a uno log with flash at the anchor value 24824 (zero
    delta, so flash cannot be the cause) and `RAM: used 1574 bytes from 2048 bytes` (1573 + 1). Without this
    re-derivation the fixture would carry TWO deviations from the new anchor — an 892 B flash shrink and the
    RAM move — and its own "everything else identical" claim would be false.
  - `planted_size_baseline_flash_regression.log` — a leonardo log with
    `Flash: used 27418 bytes from 28672 bytes` (26906 + 512, the same +512 B offset the fixture has always
    used; 27418 is below the 28672 total, so it stays physically plausible). RAM stays 2014.
  The percentage bars in each log are cosmetic — `SIZE_RE` reads only the "used N bytes from M bytes" clause
  — but recompute each to one decimal so no fixture is self-contradictory on its face.

Test literals — `tests/test_check_size_baseline.py`. Change the two assertion literals in
`test_planted_flash_regression_flips_checker_to_failure`: the baseline figure 26016 becomes 26906 and the
observed figure 26528 becomes 27418. Leave the `delta=+65` and `delta=+1` assertions untouched — preserving
those offsets is precisely why the plants were re-derived. Update every docstring figure that names a moved
number (the module docstring's fixture inventory, the flash-regression leg's `+512 B` paragraph, the
`test_policy_merge05_permits_the_measured_landing_deltas` docstring, and the three policy plants' paragraphs)
so the prose matches the data. In that last docstring, extend the Plan 124-10 precedent paragraph with a
Phase 144 sentence in the same register: the fixtures were re-captured from the real, now-landed v1.31 tree,
so this leg now asserts exact identity at zero delta — and it does so because the anchor moved to v1.31, not
because growth stayed inside v1.24's band.

Then commit all eleven paths in ONE commit whose message states every delta and its attributing phase
(+870 / +870 / +890 B flash, RAM unmoved; the parameter table from Phase 140, the per-byte program loop from
Phase 141, the shared HV-route resolver from Phase 142, the host-facing progress and budget work from Phase
143), names the re-anchor of all three baselines, names the seven fixture re-derivations as collateral of the
re-anchor, and carries D-14's sentence. Only after committing, run the suite legs below.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && grep -hE "^Flash:" tests/fixtures/captured_build_uno.log tests/fixtures/captured_build_uno328pb.log tests/fixtures/captured_build_leonardo.log tests/fixtures/planted_size_baseline_policy_uno_over_band.log tests/fixtures/planted_size_baseline_policy_leonardo_growth.log tests/fixtures/planted_size_baseline_policy_ram_moved.log tests/fixtures/planted_size_baseline_flash_regression.log</automated>
    <automated>cd /workspaces/firestarter && python3 -c "import json; d=json.load(open('scripts/baseline/size_baseline.json')); a=d['avr_targets']; assert [a[e]['flash_used'] for e in ('uno','uno328pb','leonardo')]==[24824,24874,26906]; assert [a[e]['ram_used'] for e in ('uno','uno328pb','leonardo')]==[1573,1579,2014]; assert d['native_envs']['native']['cases']==141 and d['native_envs']['native']['suites']==17; assert d['warnings']['native']['native']['total_watermark']==1166; print('size_baseline OK')"</automated>
    <automated>cd /workspaces/firestarter && python3 -c "import json; d=json.load(open('scripts/baseline/size_baseline_v131.json')); n=d['native_envs']; assert n['native_params_v131']=={'cases':9,'succeeded':9,'suites':1,'all_passed':True}, n['native_params_v131']; assert n['native_loop_v131']['cases']==79 and n['native_loop_v131']['suites']==2; assert n['native_trace_v131']['all_passed'] is True; print('v131 record OK')"</automated>
    <automated>cd /workspaces/firestarter && git diff HEAD~1 --name-only | wc -l | grep -x 11 && git diff HEAD~1 --stat -- scripts/check_size_baseline.py scripts/check_build_warnings.py src/ | wc -l | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The commit contains exactly the eleven declared paths; `git diff HEAD~1 --name-only` lists no twelfth file, nothing under `src/`, and neither checker script.
    - `size_baseline.json` and `size_baseline_base01.json` both carry `flash_used` 24824 / 24874 / 26906 with `flash_free` 7432 / 7510 / 1766 and RAM 1573 / 1579 / 2014 unchanged.
    - `size_baseline.json`'s `warnings` block is byte-identical to its pre-change form (compare against `git show HEAD~1:scripts/baseline/size_baseline.json`), and `native_envs` still reads 141 cases / 17 suites for both pinned envs.
    - `size_baseline_v131.json` now has `native_envs` keys for `native_params_v131` (9/9/1/true) and `native_loop_v131` (79 cases, 2 suites) that it did not have before, and its `meta` records C-01's policy change naming `141-NEW-TRACE.md` section 6's prose-only predecessor.
    - Either both new `warnings.native` records exist with cold-measured counts, or `meta` names exactly which count is missing and why — no warm figure appears in any watermark field.
    - `size_baseline.json`'s `meta.supersedes` no longer claims BASE-01 is immutable, and states the D-11 re-anchor plus D-12's git-history-is-the-record position.
    - The seven fixture logs carry flash figures 24824 / 24874 / 26906 (captured) and 24889 / 26907 / 24824 / 27418 (planted), with RAM 1573 / 1579 / 2014 and the single 1574 in `ram_moved`.
    - `test_check_size_baseline.py` asserts `26906` and `27418` in the flash-regression leg, still asserts `delta=+65` and `delta=+1` unchanged, and its `test_policy_merge05_permits_the_measured_landing_deltas` docstring carries a Phase 144 sentence stating the re-anchor rather than growth-inside-band.
    - `python3 -m pytest tests/ -q` reports 0 failed — including all of `test_check_size_baseline.py` and `test_check_build_warnings.py`.
    - `git status --porcelain` is empty; the commit message states every delta with its attributing phase and carries D-14's sentence.
  </acceptance_criteria>
  <done>All three baselines are re-anchored to the v1.31 tip, the seven fixtures and two literals are re-derived in the same commit, and the whole firmware pytest suite is green with no checker script modified.</done>
</task>

<task type="auto">
  <name>Task 3: Post-rewrite gate verdicts and the four re-derived plants' RED/GREEN pairs</name>
  <files>/tmp/gsd-144/verdicts.txt</files>
  <read_first>
    - `firestarter/tests/test_check_size_baseline.py` — the four planted legs as updated in task 2, plus the survivor legs (`test_policy_merge05_fires_on_ram_move`, `test_clean_native_both_envs_pass`, `test_planted_unparseable_log_exits_exactly_2`, `test_planted_suites_errored_flips_checker_to_failure`, and the two `FIRESTARTER_SIZE_BASELINE` seam-precedence legs).
    - `firestarter/scripts/check_size_baseline.py` — `compare_avr_policy_merge05` :214 and its exact message strings, so the RED transcripts can be matched on content rather than on exit code alone.
    - `.planning/phases/144-tests-build-verification/144-VALIDATION.md` § "Per-gate planted-violation specification" and the § "Validation Sign-Off" checklist.
  </read_first>
  <action>
Post-rewrite verdicts, appended verbatim to `/tmp/gsd-144/verdicts.txt` and quoted in the SUMMARY:
  - strict identity, default baseline, the same three `--avr-log` arguments as task 1: now expect PASS at
    exact identity (zero delta on all six figures). The everyday drift gate is armed again, which is D-10's
    entire purpose.
  - band policy: the same three `--avr-log` arguments plus `--policy merge05 --baseline
    scripts/baseline/size_baseline_base01.json`: now expect PASS. This verdict MUST be recorded with D-14's
    sentence attached, in these terms — MERGE-05 reads green because its anchor moved to v1.31, not because
    growth stayed inside v1.24's band — together with F-141-01's operator acceptance of the overrun and the
    +204 B parameter-table mechanism. A sentence pairing MERGE-05 and green without the word anchor nearby is
    the overclaim this phase must not commit.
  - native compare and `check_build_warnings.py`: re-run with the five permitted env names only, expect PASS,
    and confirm no `Traceback` anywhere in the output.

The four re-derived plants, each a NEW plant needing its own D-18 pair. For each of
`planted_size_baseline_policy_uno_over_band.log`, `planted_size_baseline_policy_leonardo_growth.log`,
`planted_size_baseline_policy_ram_moved.log` and `planted_size_baseline_flash_regression.log`:
  - RED: invoke the real checker directly against the planted log with the invocation its paired test uses
    (the three policy plants take `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json`;
    the flash-regression plant takes the default baseline), capture the verbatim output, and confirm it exits
    non-zero AND names the expected cause — `delta=+65` for uno_over_band, `delta=+1` for leonardo_growth,
    the `ram_used` mismatch naming 1574 against 1573 for ram_moved, and both 26906 and 27418 for
    flash_regression. A plant that no longer fires is the exact defect OD-01 exists to prevent, and is a
    stop-and-report.
  - GREEN: the corresponding paired test passing, plus the clean-capture leg passing at exact identity, so
    each RED is attributable to the plant rather than to the fixture set as a whole.

Also record the honest note on the one leg that changes character rather than breaking:
`test_policy_merge05_permits_the_measured_landing_deltas` now asserts a zero-delta identity rather than
"growth stayed inside the band". Say that plainly; a silently-hollowed leg is worse than a RED one.

Finally, confirm the standing non-claims this plan is responsible for not eroding: no bench run happened and
nothing here is a claim about real silicon (Phase 145 owns that); TEST-04's "disables every high-voltage
route" remains proven in the emitted control-register stream only; and the three `*_v131` envs are covered by
no CI leg in either repository.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=/tmp/gsd-144/build_uno.log --avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log --avr-log leonardo=/tmp/gsd-144/build_leonardo.log 2>&1 | tee -a /tmp/gsd-144/verdicts.txt | tail -8</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/gsd-144/build_uno.log --avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log --avr-log leonardo=/tmp/gsd-144/build_leonardo.log 2>&1 | tee -a /tmp/gsd-144/verdicts.txt | tail -8</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=tests/fixtures/planted_size_baseline_policy_uno_over_band.log 2>&1 | tee -a /tmp/gsd-144/verdicts.txt | grep -E "delta=\+65|FAIL"; echo "exit=${PIPESTATUS[0]}"</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log leonardo=tests/fixtures/planted_size_baseline_flash_regression.log 2>&1 | tee -a /tmp/gsd-144/verdicts.txt | grep -E "26906|27418"</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_check_size_baseline.py tests/test_check_build_warnings.py -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && grep -c Traceback /tmp/gsd-144/verdicts.txt | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - The post-rewrite strict-identity verdict is PASS with zero delta on all six AVR figures, captured verbatim.
    - The post-rewrite `--policy merge05` verdict is PASS, captured verbatim, and the SUMMARY records it with the sentence "MERGE-05 reads green because its anchor moved to v1.31, not because growth stayed inside v1.24's band", plus F-141-01's operator acceptance and the +204 B parameter-table mechanism.
    - All four re-derived plants are shown RED with verbatim transcripts: `delta=+65`, `delta=+1`, a `ram_used` failure naming 1574 against 1573, and a flash regression naming both 26906 and 27418. Each exits non-zero.
    - Each plant's GREEN half is recorded: its paired test passes and the clean-capture identity leg passes.
    - `python3 -m pytest tests/test_check_size_baseline.py tests/test_check_build_warnings.py -q` reports 0 failed.
    - `/tmp/gsd-144/verdicts.txt` contains no `Traceback` and no invocation naming a `*_v131` env.
    - The SUMMARY states plainly that `test_policy_merge05_permits_the_measured_landing_deltas` now asserts zero-delta identity rather than growth-inside-band.
    - The SUMMARY restates the three standing non-claims this plan touches: no silicon claim, the register-stream-only boundary for TEST-04, and no CI coverage for the three `*_v131` envs.
  </acceptance_criteria>
  <done>Both TEST-08 gates read PASS with the anchor move disclosed in D-14's exact terms, and all four re-derived plants are proven to still fire with their asserted literals intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → committed record | Measured figures become the gate's own definition of correct; a warm or truncated build silently redefines it |
| baseline record ↔ its planted fixtures | Moving the anchor can make a planted violation stop firing without turning anything red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-22 | Repudiation | A warm-cache or timeout-truncated build contaminating the recorded figures | mitigate | `pio run -t clean` then `pio run`, one uninterrupted invocation per env, explicit 600000 ms timeouts; expected figures stated so a deviation is stop-and-report; no warm figure may enter a watermark field |
| T-144-23 | Repudiation | A planted leg passing silently because the anchor moved under it (OD-01) | mitigate | All four plants re-derived from the new anchor preserving their single cause and asserted literals, and each re-proven RED with a verbatim transcript before its GREEN is believed |
| T-144-24 | Repudiation | "MERGE-05 green" read as growth staying inside v1.24's band | mitigate | D-14's constrained sentence recorded in the SUMMARY, in `size_baseline.json`'s own `deltas_vs_base01` prose, and in the affected test docstring |
| T-144-25 | Repudiation | A `*_v131` env name fed to a checker producing exit 1 — the regression code — from an uncaught `KeyError` | mitigate | Unconditional prohibition restated under any `--baseline`; warning counts for those envs obtained by grep on cold logs; verdict file asserted to contain no `Traceback` |
| T-144-26 | Tampering | Silently widening a band or a watermark to make a gate pass | mitigate | Neither checker script is in `files_modified`; the commit is asserted to touch neither; watermark and `native_envs` blocks asserted byte-identical to their pre-change form |
| T-144-27 | Repudiation | Losing the v1.24 comparison without saying so | accept | D-11 retires the v1.24 semantics deliberately; D-12 keeps the figures recoverable from git history and `138-BASELINE.md` section 5 records them in prose; both facts are written into the affected files' `meta` |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs; RESEARCH § Package Legitimacy Audit records "not applicable" for the phase |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — 0 failed.
- `cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=/tmp/gsd-144/build_uno.log --avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log --avr-log leonardo=/tmp/gsd-144/build_leonardo.log` — PASS, zero delta.
- `cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/gsd-144/build_uno.log --avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log --avr-log leonardo=/tmp/gsd-144/build_leonardo.log` — PASS, recorded with D-14's sentence.
- `cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --native-log native=/tmp/gsd-144/native.log --native-log native_nodevtools=/tmp/gsd-144/native_nodevtools.log` — PASS at 141 / 17.
- `cd /workspaces/firestarter && python3 scripts/check_build_warnings.py --log uno=/tmp/gsd-144/build_uno.log --log uno328pb=/tmp/gsd-144/build_uno328pb.log --log leonardo=/tmp/gsd-144/build_leonardo.log --log native=/tmp/gsd-144/native.log --log native_nodevtools=/tmp/gsd-144/native_nodevtools.log` — PASS.
- `cd /workspaces/firestarter && git status --porcelain` — empty.
</verification>

<success_criteria>
- One cold consolidated run covers three AVR targets and five native envs against the final tree, every verdict verbatim.
- Flash deltas +870 / +870 / +890 with RAM unmoved are recorded against the PREP-03 anchor, and the Leonardo ceiling is stated as 93.8 percent with 1766 B headroom.
- All three baselines are re-anchored; `size_baseline_v131.json` gains the two env records it never had.
- Both TEST-08 gates read PASS, with the re-anchor disclosed in D-14's exact terms.
- All four re-derived plants still fire, each with a verbatim RED and an attributable GREEN.
- No `src/` file, no checker script, no band literal and no watermark changed; the firmware suite is green.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-05-SUMMARY.md` when done. It MUST contain: the
verbatim cold flash/RAM table for all three AVR targets with deltas; the Leonardo ceiling arithmetic; the
per-env case and suite counts each labelled with the env that produced it; the cold warning counts for the
three `*_v131` envs (or the explicit gap statement); the verbatim pre- and post-rewrite verdicts for both
`check_size_baseline.py` modes and `check_build_warnings.py`; D-14's sentence; the four re-derived plants'
RED and GREEN halves; the note that `test_policy_merge05_permits_the_measured_landing_deltas` now asserts
zero-delta identity; and the restated absences (no CI leg covers the three `*_v131` envs, no bench claim).
</output>
