---
phase: 144-tests-build-verification
plan: 07
type: execute
wave: 6
depends_on: ["144-05", "144-06"]
commits_land_in: meta
files_modified:
  - .planning/phases/144-tests-build-verification/144-TEST-RECORD.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08]

must_haves:
  truths:
    - "One phase record carries every verbatim verdict, every measured figure labelled with the env that produced it, and every non-claim this phase is obliged to state"
    - "D-14's disclosure appears in its constrained terms: green because the anchor moved to v1.31, never because growth stayed inside v1.24's band"
    - "D-03's non-claim is stated: the overprogram arithmetic is proven, the in-loop wiring on a live row is not, because no shipped row sets overprogram_factor"
    - "D-08's gap is stated: nothing gate-asserts eprom_v131_expected_prechange.h; its preserved blob is hand-verifiable, not machine-checked"
    - "D-15's absence is stated: the three *_v131 envs run in no CI leg of either repository"
    - "All eight TEST requirements are flipped exactly once, in both coverage tables, only after every piece of evidence exists and the operator has approved"
  artifacts:
    - path: ".planning/phases/144-tests-build-verification/144-TEST-RECORD.md"
      provides: "The phase record: verdicts, figures, segment attribution table, D-18 inventory, non-claims, findings, hand-offs"
      min_lines: 200
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-01 through TEST-08 ticked"
      contains: "TEST-08"
  key_links:
    - from: ".planning/phases/144-tests-build-verification/144-TEST-RECORD.md"
      to: ".planning/ROADMAP.md"
      via: "the v1.31 coverage table's TEST-01..08 rows moving to Complete"
      pattern: "TEST-0[1-8]"
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file in firestarter/ or firestarter_app/ — this plan writes only meta-repo documents"
    - "MUST NOT flip any requirement other than TEST-01..TEST-08 — do not touch a BENCH-* or CLOSE-* row"
    - "MUST NOT use a bulk requirements or roadmap rewrite verb — they normalise the WHOLE file and have previously clobbered an unrelated phase's Plans line; hand-edit, then snapshot-and-diff"
    - "MUST NOT pair the words MERGE-05 and green without the word anchor nearby (Pitfall 4)"
    - "MUST NOT claim any bench result, any real-silicon behaviour, or any support_status change — Phase 145 owns bench evidence and BENCH-03 forbids the status change"
    - "MUST NOT claim CI covers the three *_v131 envs, nor that the app's CI exercises the cross-repo parity gates"
    - "MUST NOT narrow TEST-04's boundary: 'disables every high-voltage route' is proven in the emitted control-register stream only"
    - "MUST NOT author the honesty ledger, the claim gate, gh#15 reconciliation, or Phase 143's deferred ROADMAP prose correction — all four are Phase 146's"
    - "MUST NOT quote firestarter/CLAUDE.md's stale '71 cases total' for native_loop_v131 — the measured figure is 79 (47 + 32)"
    - "MUST NOT print a bare 79 or 88 — 88 is the three-suite mapping denominator, 79 is native_loop_v131's per-env figure, and they are never added"
    - "MUST NOT auto-approve the operator checkpoint — this gate is blocking and is not auto-approvable even under auto-advance"
---

<objective>
Write the phase record and flip the eight TEST requirements. The record makes exactly the claims the evidence
supports: every gate verdict verbatim, every figure labelled with the env that produced it, the segment
attribution table, the full D-18 planted-violation inventory, and each mandatory non-claim stated rather than
implied.

Purpose: an undisclosed re-anchor is precisely the overclaim Phase 146's claim gate exists to catch — it would
be this milestone committing its own anti-pattern. Stating the facts belongs here; the honesty ledger itself
belongs to Phase 146 / CLOSE-02.

Output: `144-TEST-RECORD.md`, plus TEST-01…TEST-08 ticked in both coverage tables behind a blocking operator
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- New file: `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md`
- Modified: `.planning/REQUIREMENTS.md` (eight checkboxes), `.planning/ROADMAP.md` (eight coverage rows plus
  the Phase 144 plan checkboxes)
- New finding ids introduced by this record: `F-144-01` onwards, numbered in the record itself
- No source file in either sub-repo is touched.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author 144-TEST-RECORD.md</name>
  <files>.planning/phases/144-tests-build-verification/144-TEST-RECORD.md</files>
  <read_first>
    - `.planning/phases/143-host-timeout-progress-pulse-override/143-HOST-RECORD.md` — the structural analog: section 7.1's cold flash/RAM table, 7.3's zero-added-frames non-claim, 7.4's verbatim checker output, the numbered non-claims section, section 15's `F-143-NN` findings register, section 12's hand-offs.
    - Every SUMMARY this phase produced, `144-01-SUMMARY.md` through `144-06-SUMMARY.md`. All transcripts, figures and non-claims are lifted from them — nothing here may be re-measured or recalled from memory.
    - `.planning/phases/144-tests-build-verification/144-CONTEXT.md` — the decisions this record discharges, especially D-03, D-08, D-14, D-15.
    - `.planning/phases/144-tests-build-verification/144-RESEARCH.md` — C-01 and C-04 (the corrections the record surfaces), F-01 (the 88-versus-79 labelling rule), and F-141-01's operator acceptance as cited there.
    - `/tmp/gsd-144/segment_table.txt` (plan 144-04's per-segment table) and `/tmp/gsd-144/verdicts.txt` (plan 144-05's verdict capture).
  </read_first>
  <action>
Create `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md`. Section ordering is Claude's
discretion; the following ten contents are mandatory, each as its own identifiable section or subsection.

1. Requirement-to-evidence map, TEST-01 through TEST-08. For TEST-01…05 the named native cases plus the
mapping gate; for TEST-06 the rename, the fresh capture, the identity gate and the exhaustiveness gate; for
TEST-07 the builds, the pinned and v131 env runs, both parity directions and the four CI-scoped legs; for
TEST-08 the measured deltas, the re-anchor and both gate verdicts. Cite case names, not just counts.

2. Cold measurement tables. Per-target flash and RAM with deltas against the PREP-03 anchor
(+870 / +870 / +890 B flash, RAM unmoved), and the Leonardo ceiling stated explicitly as 26906 of 28672 B,
93.8 percent, 1766 B headroom. Per-env case and suite counts each labelled with its env: `native` 141/17,
`native_nodevtools` 141/17, `native_params_v131` 9, `native_loop_v131` 79 as 47 + 32 across two suites,
`native_trace_v131` 5. State the labelling rule: 88 is the three-suite mapping denominator, 79 is a per-env
figure, and they are never added.

3. Verbatim gate verdicts: both `check_size_baseline.py` modes before and after the re-anchor,
`check_build_warnings.py`, the firmware pytest total, the identity gate, both new firmware gates, the new host
gate, both parity directions, and all four CI-scoped commands with their `ci.yml` citations
(:81 / :84 / :87 / :90 — C-02). Verbatim transcripts, never paraphrased verdicts.

4. D-14's re-anchor disclosure as its own subsection, containing this sentence verbatim: "MERGE-05 reads green
because its anchor moved to v1.31, not because growth stayed inside v1.24's band." Cite alongside it
F-141-01's operator acceptance of the overrun and the +204 B parameter-table mechanism, and state that
MERGE-05's v1.24 semantics are retired while its forward band mechanism is kept with
`MERGE05_UNO_CLASS_FLASH_BAND` still 64.

5. The trace diff: plan 144-04's per-segment old-versus-new table, every delta attributed to a named decision
from Phases 140-143, the 620 / 265 / 885 subtotals, and Phase 143's zero-entry contribution with both
independent reasons. State the gate's boundary: it proves attribution COMPLETENESS, not attribution
CORRECTNESS.

6. A numbered non-claims section, including each of these as its own numbered item:
- D-03: the overprogram arithmetic is proven by five `test_loop03_*` cases; the in-loop wiring on a live row is
  NOT proven, because no shipped row sets `overprogram_factor` — it is 0 on all three rows at
  `eprom_params.cpp:46-48`, structurally unreachable on live data, as
  `test_loop04_no_live_row_emits_an_overprogram_pulse` witnesses. The wording is Claude's; the content is not
  optional.
- D-08: nothing gate-asserts `eprom_v131_expected_prechange.h`. Its preserved blob
  `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70` is cited here and stays verifiable by hand via
  `git rev-parse HEAD:<path>`, but it is NOT machine-checked. A second inventory record would close the gap
  and is deferred.
- D-15: the three `*_v131` envs are wired into NO CI leg of either repository — a local run-by-name
  obligation. Never imply CI covers them.
- No bench run happened and nothing here is a claim about real silicon (Phase 145 owns that); TEST-04's
  "disables every high-voltage route" is proven in the emitted control-register stream only; no chip's
  `support_status` changed.
- The app's CI has no firmware checkout, so every cross-repo parity gate skips there; `requires_fw` fails OPEN
  across the repo boundary by design.
- If OD-02's cold warning counts for `native_params_v131` or `native_loop_v131` could not be obtained without
  violating D-22, the gap is named here explicitly rather than omitted.

7. The corrections this phase surfaced. C-04: CONTEXT.md's own prose nominated "the two fallback cases" for
TEST-05, which names no existing pair — there are six, in two families of three — and that is exactly the
defect class D-01's mapping gate exists to catch, arriving in the phase's own input. C-01:
`size_baseline_v131.json` recorded only `native_trace_v131`, so D-13's refresh ADDED two env records to a file
whose predecessor deliberately held those counts in prose only, never in a baseline JSON; this record is where
that policy change is stated.

8. The D-18 planted-violation inventory: ten plants with their owning plan and both halves of each evidence
pair — two for the mapping gate, two for the exhaustiveness gate, two for the CAP-03 layout gate, four
re-derived size-baseline plants. For each, what turned RED and why the GREEN is attributable. State the
principle: a pre-authored leg can be unreachable, so RED alone proves nothing.

9. A findings register `F-144-01` onward, and a hand-offs section naming what Phases 145 and 146 inherit —
including that Phase 146 owns the honesty ledger, the claim gate, gh#15's item-by-item reconciliation, Phase
143's deferred ROADMAP prose correction, and reconciling `firestarter/CLAUDE.md`'s stale "71 cases total" for
`native_loop_v131` against the measured 79.

10. The gate-state summary D-04 makes possible for the first time this milestone: both
`protocol_branch_inventory.json` pins (`cedc88dc…` for `src/proms/eprom.cpp`, `5dffe841…` for
`src/proms/eprom_params.cpp`) matched `HEAD` at every point in the phase, because no file under
`firestarter/src/` was touched. Say it plainly and cite the check that proves it.
  </action>
  <verify>
    <automated>cd /workspaces && wc -l .planning/phases/144-tests-build-verification/144-TEST-RECORD.md</automated>
    <automated>cd /workspaces && grep -c "MERGE-05 reads green because its anchor moved to v1.31, not because growth stayed inside v1.24's band" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md | grep -x 1</automated>
    <automated>cd /workspaces && grep -q "overprogram_factor" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && grep -q "eprom_params.cpp:46-48" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && echo "D-03 non-claim present"</automated>
    <automated>cd /workspaces && grep -q "ca3e09f164e6e1c541ecb63d15bbebf5bce41d70" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && grep -qi "not machine-checked" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && echo "D-08 gap present"</automated>
    <automated>cd /workspaces && grep -qi "no CI leg" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && grep -q "native_params_v131" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md && echo "D-15 absence present"</automated>
    <automated>cd /workspaces && grep -cE "\b71 cases\b" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md | grep -x 0</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain | wc -l | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - The record contains the D-14 sentence verbatim exactly once, with F-141-01's operator acceptance and the +204 B parameter-table mechanism cited nearby.
    - The record contains D-03's non-claim naming `overprogram_factor`, its value 0 on all three rows, and `eprom_params.cpp:46-48`.
    - The record contains D-08's gap naming `eprom_v131_expected_prechange.h`, the blob `ca3e09f164e6e1c541ecb63d15bbebf5bce41d70`, and the words "not machine-checked".
    - The record states that the three `*_v131` envs run in no CI leg of either repository, naming all three envs.
    - The record contains the no-bench / register-stream-only boundary for TEST-04 and states that no `support_status` changed.
    - The record contains C-04's phantom-pair observation and C-01's two-added-env-records policy change.
    - Every per-env count is labelled with its env, the 88-versus-79 rule is stated, and `grep -E "\b71 cases\b"` returns zero hits.
    - The record contains the per-segment old/new/delta table with a named attribution for every delta, the 620 / 265 / 885 subtotals, and the completeness-not-correctness boundary.
    - The record contains the ten-plant D-18 inventory, each entry naming its owning plan and both evidence halves.
    - The record contains an `F-144-` findings register and a hand-offs section naming Phase 145's and Phase 146's inheritances, including the stale "71 cases total" doc reconciliation.
    - The record states both `src/` blob pins matched `HEAD` throughout, citing the check that proves it.
    - `git -C /workspaces/firestarter status --porcelain` and `git -C /workspaces/firestarter_app status --porcelain` show no tracked-file modification.
  </acceptance_criteria>
  <done>The phase record exists, carries every verbatim verdict and measured figure, and states all four mandatory disclosures plus both corrections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator review of the record before any requirement is flipped</name>
  <files>.planning/phases/144-tests-build-verification/144-TEST-RECORD.md</files>
  <read_first>
    - `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md` — as written by task 1; the artifact under review.
    - `.planning/phases/144-tests-build-verification/144-CONTEXT.md` — D-11's stated trade-off and D-08's deliberate gap, the two judgement calls presented below.
  </read_first>
  <action>
Present the record for operator review and STOP. Do not edit either coverage document until the operator has
answered. This gate is blocking and is not auto-approvable even under auto-advance: flipping eight
requirements and disclosing a re-anchor are exactly the kind of outward-facing claims the operator owns.

Post the what-built and how-to-verify content below, then wait for the resume signal. If the operator responds
with changes rather than approval, apply them to `144-TEST-RECORD.md` and re-present — do not proceed to task 3
on a partial approval.
  </action>
  <what-built>
Phase 144 is complete pending review. Across seven plans:

- Two new firmware gates: the machine-checked TEST-01…05 requirement-to-case mapping
  (`tests/test_requirement_case_mapping_v131.py`) and the trace-segment exhaustiveness gate over all 885
  entries (`tests/test_trace_segment_exhaustiveness_v131.py`).
- One new host gate: the cross-repo CAP-03 byte-layout parity comparison neither repo performed before
  (`firestarter_app/tests/test_cap03_ack_layout_parity.py`), with two committed planted fixtures.
- The trace freeze: the pre-change golden renamed to `eprom_v131_expected_prechange.h` with its Phase-138 blob
  intact, a fresh capture at this phase's tip totalling 91 / 115 / 59, and the identity gate re-pointed — all in
  one commit. `native_trace_v131` now runs 5 cases with 0 failed: the milestone's first standing RED is retired.
- One cold consolidated measurement run over three AVR targets and five native envs; all three size baselines
  re-anchored to the v1.31 tip; the seven `.log` fixtures and two figure literals re-derived so every planted leg
  still fires. MERGE-05's band is retired as a v1.24 comparison and repurposed as a forward tripwire: the second
  standing RED.
- The host sweep: constants parity in both directions, and four CI-scoped commands green on the 3.11 replica.
- `144-TEST-RECORD.md`, carrying every verbatim verdict, the segment attribution table, the ten-plant D-18
  inventory, and four disclosures stated rather than implied.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md` end to end. The four disclosures to
   check for: D-14's re-anchor sentence (MERGE-05 green because the ANCHOR moved, not because growth stayed
   inside the band); D-03's non-claim (overprogram arithmetic proven, in-loop wiring on a live row not, because
   no shipped row sets the factor); D-08's gap (the prechange header is not machine-checked); D-15's absence
   (the three `*_v131` envs run in no CI leg of either repository).
2. Confirm the record makes no bench or real-silicon claim, and that TEST-04's high-voltage-route clause stays
   bounded to the emitted control-register stream.
3. Spot-check the figures. From `/workspaces/firestarter`:
   `python3 scripts/check_size_baseline.py --avr-log uno=/tmp/gsd-144/build_uno.log --avr-log uno328pb=/tmp/gsd-144/build_uno328pb.log --avr-log leonardo=/tmp/gsd-144/build_leonardo.log`
   should PASS at zero delta, and the same command with
   `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json` should also PASS — green because
   the anchor moved.
4. Confirm the headroom you are being handed: leonardo at 26906 of 28672 B, 93.8 percent, 1766 B remaining.
   That band is now armed at 0 B growth for Phases 145 and 146, which is D-11's stated intent.
5. Confirm the two judgement calls this phase deliberately leaves with you: (a) the re-anchor ends MERGE-05's
   ability to make its original v1.24 comparison — the trade you took as D-11; (b)
   `eprom_v131_expected_prechange.h` stays un-gated by choice — the named gap, rather than a second inventory
   record now.
6. On approval, the eight TEST requirements are flipped in `.planning/REQUIREMENTS.md` and in
   `.planning/ROADMAP.md`'s v1.31 coverage table. No other requirement row is touched.
  </how-to-verify>
  <resume-signal>Type "approved" to flip TEST-01 through TEST-08, or describe what to change first.</resume-signal>
  <verify>
    <human-check>The operator has read the record and answered the resume signal. No coverage document was edited before that answer.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator's response is captured verbatim in the plan SUMMARY.
    - `git -C /workspaces status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md` is empty at the moment the gate is presented — neither coverage document was pre-edited.
    - If the operator asked for changes, the record was amended and re-presented, and task 3 did not start until an explicit approval arrived.
  </acceptance_criteria>
  <done>The operator has reviewed the record and either approved the flip or named the changes required first.</done>
</task>

<task type="auto">
  <name>Task 3: Flip TEST-01 through TEST-08 in both coverage tables</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` :226-243 — the "Tests & Build" section's eight unchecked boxes, with the checked `HOST-*` block above and the unchecked `BENCH-*` block below, so the edit's boundaries are unambiguous.
    - `.planning/ROADMAP.md` — the "v1.31 Coverage" table and the "Phase 144: Tests & Build Verification" block including its plan list.
    - `.planning/phases/144-tests-build-verification/144-TEST-RECORD.md` — the requirement-to-evidence map, which is what authorises each individual flip.
  </read_first>
  <action>
Only after the operator has approved.

Snapshot first: copy `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` to `/tmp/gsd-144/` so the post-edit
diff can be inspected line by line. Then hand-edit. Do NOT use a bulk requirements or roadmap rewrite verb —
they normalise the whole file and have previously clobbered an unrelated phase's plan line.

In `.planning/REQUIREMENTS.md`, change exactly eight checkbox characters from unchecked to checked: TEST-01,
TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08. Leave every `BENCH-*` and `CLOSE-*` row
untouched and leave each requirement's text byte-identical — only the checkbox character changes.

In `.planning/ROADMAP.md`, set the eight corresponding rows of the v1.31 coverage table to Complete and tick
the Phase 144 plan checkboxes for `144-01` through `144-07`. Do not edit any other phase's block, any other
requirement row, or Phase 144's goal and success-criteria text.

Diff both files against their snapshots and confirm the diff contains ONLY the intended lines: eight checkbox
characters in REQUIREMENTS.md, and in ROADMAP.md the eight coverage rows plus the seven plan checkboxes. Any
other changed line — a reflowed paragraph, a moved heading, another phase's plan line — must be reverted before
committing.

Commit the record and both coverage edits together in the meta repo, with a message naming the eight flipped
requirement ids and the record filename.
  </action>
  <verify>
    <automated>cd /workspaces && grep -cE "^- \[x\] \*\*TEST-0[1-8]\*\*" .planning/REQUIREMENTS.md | grep -x 8</automated>
    <automated>cd /workspaces && diff /tmp/gsd-144/REQUIREMENTS.md .planning/REQUIREMENTS.md | grep -c "^[<>]" | grep -x 16</automated>
    <automated>cd /workspaces && diff /tmp/gsd-144/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -vcE "TEST-0[1-8]|144-0[1-7]" | grep -x 0</automated>
    <automated>cd /workspaces && grep -cE "^- \[ \] \*\*BENCH-0[1-3]\*\*" .planning/REQUIREMENTS.md | grep -x 3</automated>
    <automated>cd /workspaces && git log --oneline -1 && git status --porcelain .planning/ | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*TEST-0[1-8]\*\*" .planning/REQUIREMENTS.md` returns 8, and no `TEST-0N` row remains unchecked.
    - The REQUIREMENTS.md diff against the snapshot contains exactly 16 changed lines (8 removed, 8 added) and nothing else.
    - Every changed line in the ROADMAP.md diff mentions either a `TEST-0N` id or a `144-0N` plan id — zero other lines changed, so no unrelated phase's plan line moved and no paragraph reflowed.
    - All three `BENCH-*` rows remain unchecked and no `CLOSE-*` row changed.
    - The commit contains only the record and the two coverage documents; `git status --porcelain` in both sub-repos shows no tracked-file modification.
    - The commit message names all eight flipped requirement ids and `144-TEST-RECORD.md`.
  </acceptance_criteria>
  <done>All eight TEST requirements are ticked in both coverage tables, with a diff proving nothing else moved, committed alongside the phase record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence set → record claims | The record is what downstream phases and readers trust; an overclaim here propagates into the milestone close |
| tool-assisted doc edit → whole file | Bulk roadmap/requirements verbs reformat everything they touch, including unrelated phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-33 | Repudiation | An undisclosed re-anchor read as "growth stayed inside the band" | mitigate | D-14's constrained sentence required verbatim and grepped by an acceptance criterion; pairing MERGE-05 with green without the word anchor is prohibited |
| T-144-34 | Repudiation | A requirement flipped without evidence, or flipped early by an earlier plan | mitigate | All eight flips centralised here behind a blocking operator gate; every other plan carries an explicit prohibition against touching either coverage document; the allowed id list is enumerated |
| T-144-35 | Repudiation | Implying CI coverage, bench evidence or a machine-checked prechange file | mitigate | Three non-claims mandated as numbered items with grep-able acceptance criteria (D-15, the bench boundary, D-08) |
| T-144-36 | Tampering | A bulk doc verb silently reformatting the roadmap or clobbering another phase's plan line | mitigate | Snapshot-then-diff with an acceptance criterion that every changed line names a `TEST-0N` or `144-0N` id; bulk verbs prohibited |
| T-144-37 | Repudiation | Quoting a stale figure (CLAUDE.md's 71 cases) as this phase's measurement | mitigate | Prohibition plus a zero-hit grep criterion; the measured 79 is used and labelled with its env |
| T-144-38 | Elevation of privilege | An automated agent approving an outward-facing claim gate on the operator's behalf | mitigate | The checkpoint is declared not auto-approvable, `autonomous: false`, and task 3 asserts neither coverage document was pre-edited |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs; this plan writes documents only |
</threat_model>

<verification>
- `cd /workspaces && grep -c "MERGE-05 reads green because its anchor moved to v1.31, not because growth stayed inside v1.24's band" .planning/phases/144-tests-build-verification/144-TEST-RECORD.md` — exactly 1.
- `cd /workspaces && grep -cE "^- \[x\] \*\*TEST-0[1-8]\*\*" .planning/REQUIREMENTS.md` — 8.
- `cd /workspaces && diff /tmp/gsd-144/ROADMAP.md .planning/ROADMAP.md | grep "^[<>]" | grep -vcE "TEST-0[1-8]|144-0[1-7]"` — 0.
- `cd /workspaces/firestarter && git status --porcelain` and `cd /workspaces/firestarter_app && git status --porcelain` — no tracked-file modification.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — 0 failed, as a final phase-level confirmation.
</verification>

<success_criteria>
- The phase record states every verdict, figure and non-claim the phase is obliged to carry, with the four mandatory disclosures present and grep-verified.
- All eight TEST requirements are ticked in both coverage tables, with a diff proving no unrelated line moved.
- Nothing in either sub-repo changed; no bench, CI-coverage or real-silicon claim is made anywhere.
- The operator approved before the flip, at a gate that was not auto-approved.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-07-SUMMARY.md` when done. It MUST record the
operator's approval verbatim, both diffs' changed-line counts, and a one-line index of where each of the four
mandatory disclosures lives in `144-TEST-RECORD.md`.
</output>
