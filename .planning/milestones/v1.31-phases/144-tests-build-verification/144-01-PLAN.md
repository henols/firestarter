---
phase: 144-tests-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
commits_land_in: firestarter
files_modified:
  - firestarter/tests/test_requirement_case_mapping_v131.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "Every case name TEST-01…TEST-05 are flipped against is proven to exist in the v131 suite sources by a machine-checked parse (D-01)"
    - "A renamed or deleted native case fails the gate with a message naming both the missing case and the requirement that lost it"
    - "An emptied or misdirected scan root fails the gate's non-vacuity leg instead of making every membership check pass over an empty set"
    - "The gate scopes to the three TEST-mapped suites and says so, because test_trace_eprom_v131's sixth RUN_TEST is #ifdef-guarded (C-05)"
  artifacts:
    - path: "firestarter/tests/test_requirement_case_mapping_v131.py"
      provides: "D-01's requirement→case mapping gate plus its two planted-violation legs"
      contains: "FIRESTARTER_CASE_MAP_SCAN_ROOT"
      min_lines: 250
  key_links:
    - from: "firestarter/tests/test_requirement_case_mapping_v131.py"
      to: "firestarter/test/native/avr/test_{loop,vpp,eprom_params}_eprom_v131/*.cpp"
      via: "import-time env seam FIRESTARTER_CASE_MAP_SCAN_ROOT resolving to test/native/avr"
      pattern: "os\\.environ\\.get\\(\"FIRESTARTER_CASE_MAP_SCAN_ROOT\""
  prohibitions:
    - "MUST NOT create, edit, rename or delete any file under firestarter/src/ (D-04) — stop and report if one seems needed"
    - "MUST NOT be authored as firestarter/scripts/check_*.py — test_checker_convention.py's FLOOR=6 and FIXTURE_FLOOR=15 sit at exactly the current counts (F-08)"
    - "MUST NOT add a conftest.py anywhere in the firmware repo (house rule, verified zero present)"
    - "MUST NOT add or rename any RUN_TEST case in any native suite — the pinned envs are asserted at 141 cases / 17 suites"
    - "MUST NOT include test_trace_eprom_v131 in the mapped-suite set (its 6th RUN_TEST is #ifdef-guarded, C-05)"
    - "MUST NOT derive the requirement→case map from the suites — a derived map can never detect a rename"
    - "MUST NOT use monkeypatch.setenv for the planted runs — the seam binds at import, so a child process is mandatory (S6)"
    - "MUST NOT run any suite before committing — test_flash_path_record_sync.py asserts the WHOLE firmware repo's git status --porcelain (D-20 / F-09)"
    - "MUST NOT tick any requirement in REQUIREMENTS.md or ROADMAP.md — plan 144-07 owns all eight flips"
    - "MUST NOT lower any floor literal to quiet a red gate"
---

<objective>
Author D-01's machine-checked requirement→case mapping gate: a pytest module under `firestarter/tests/`
that parses the three v131 Unity suites, extracts every `RUN_TEST` name, and asserts that each of
TEST-01…TEST-05 names cases which actually exist. Then prove it under D-18 — seen RED on two named
planted violations in a child process, and seen GREEN for the right reason.

Purpose: the specific risk this removes is a requirement flipped against a case that was later renamed
or deleted. CONTEXT.md's own prose already exhibits the defect (its "two fallback cases" for TEST-05
names no existing pair — there are six; RESEARCH C-04), which is the strongest possible argument for
machine-checking the map rather than writing it in prose.

Output: `firestarter/tests/test_requirement_case_mapping_v131.py` with the frozen map literal, the
extractor, five per-requirement legs, the two-half non-vacuity leg, the no-skip/needle self-checks and
two planted-violation legs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/144-tests-build-verification/144-CONTEXT.md
@.planning/phases/144-tests-build-verification/144-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and paths this plan creates (new — not drift):

- New file: `firestarter/tests/test_requirement_case_mapping_v131.py`
- New env seam name: `FIRESTARTER_CASE_MAP_SCAN_ROOT` (path-only override of the suite scan root)
- New env seam name: `FIRESTARTER_144_GATE_CHILD` (child-process recursion guard)
- New module-level names in that file: `_HERE`, `_REPO_ROOT`, `_SUITES_REL`, `_SCAN_SUITES`,
  `_MAPPED_SUITES`, `_SUITE_FLOORS`, `_TOTAL_FLOOR`, `_REQUIREMENT_CASES`, `_RUN_TEST_RE`,
  `_NEEDLE_SKIP_CALL`, `_NEEDLE_SKIPIF_MARKER`, `_NEEDLE_DEPENDENCY_SKIP_CALL`,
  `_ALL_SELF_CHECK_NEEDLES`, `_strip_comments`, `_line_of`, `_extract_run_test_names`,
  `_run_gate_in_subprocess`
- New test names in that file: `test_every_mapped_requirement_names_only_existing_cases`,
  `test_each_mapped_suite_meets_its_hardcoded_case_floor`,
  `test_extracted_case_names_are_unique`,
  `test_trace_suite_is_deliberately_out_of_scope`,
  `test_scan_targets_are_non_vacuous`,
  `test_this_module_cannot_be_silently_skipped`,
  `test_own_needles_do_not_appear_verbatim_in_this_module`,
  `test_planted_renamed_case_is_detected`,
  `test_planted_emptied_scan_root_fails_the_non_vacuity_leg`

No existing symbol is renamed or removed by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the mapping gate with its frozen map, extractor and self-protection scaffolding</name>
  <files>firestarter/tests/test_requirement_case_mapping_v131.py</files>
  <read_first>
    - `firestarter/tests/test_ack_layout_source_contract_v143.py` — THE analog (PATTERNS §1, adjudicated winner over the v142 twin). Read in full: the module docstring's "Environment seams" section, the `_HERE`/`_REPO_ROOT`/seam block at :133-146, `_strip_comments` at :148-182, `_line_of` at :185-186, the two-half non-vacuity leg `test_scan_targets_are_non_vacuous` at :477-525, and the concatenation-built needles plus both self-checks at :528-568.
    - `firestarter/tests/test_golden_trace_identity_eprom_v131.py` — the hardcoded-floor precedent (`test_inventory_is_non_vacuous`, :198-209) and the fail-closed `git`/list-form-argv reference implementation (:91-128) in case a later leg needs git.
    - `firestarter/tests/test_checker_convention.py` :113-133 — why this module must NOT live in `scripts/` (`CHECKER_GLOB`, `FLOOR = 6`, `FIXTURE_FLOOR = 15`, all at zero headroom).
    - `firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp` — the `main()` RUN_TEST block (all nine sites, one per line, unguarded).
    - `firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp` :378-396 — the `#ifdef EPROM_V131_TRACE_DUMP`-guarded sixth `RUN_TEST` that is the reason this gate scopes to three suites (C-05).
    - `firestarter_app/tests/test_revision_constants_parity.py` :172-175 — the wording precedent for "a second hand-maintained site is correct here".
  </read_first>
  <action>
Create `firestarter/tests/test_requirement_case_mapping_v131.py`. Stdlib + pytest only (`os`, `re`,
`subprocess`, `sys`, `pathlib`); no `conftest.py`, no imports from any other test module.

Module scope, copying the analog's shape exactly: `_HERE = Path(__file__).resolve().parent`,
`_REPO_ROOT = _HERE.parent`, `_SUITES_REL = "test/native/avr"`, and ONE import-time env seam
`_SCAN_SUITES = Path(os.environ.get("FIRESTARTER_CASE_MAP_SCAN_ROOT", str(_REPO_ROOT / _SUITES_REL)))`.
Per S5 the seam overrides the PATH ONLY — never a floor literal, never a case name, never the mapped-suite
set. Document the seam, and the fact that it binds at import so a planted run must be a child process, in
a dedicated "Environment seams" section of the module docstring: neither repo has a central
environment-variable inventory, so that docstring is the only discoverable site.

Mapped suites (`_MAPPED_SUITES`), exactly three, each named as
`<dirname>/<dirname>.cpp` beneath the scan root: `test_loop_eprom_v131`, `test_vpp_eprom_v131`,
`test_eprom_params_v131`. Hardcoded per-suite floors (`_SUITE_FLOORS`) 47 / 32 / 9 and
`_TOTAL_FLOOR = 88`, all as literal ints with a comment recording that 88 is the three-suite MAPPING
denominator and must never be confused with `native_loop_v131`'s per-env figure of 79 (47+32) — the
Pitfall-2 labelling rule. `test_trace_eprom_v131` is deliberately EXCLUDED; the docstring must state the
reason (its case set is build-flag dependent — the sixth `RUN_TEST` sits inside
`#ifdef EPROM_V131_TRACE_DUMP`, which no env defines — and it proves TEST-06, not TEST-01…05; C-05,
RESEARCH Open Question 3).

`_strip_comments(text)`: copy the analog's function body verbatim, including the same-shape-whitespace
property (a stripped newline stays a newline, everything else becomes one space) so `_line_of` reports
real file line numbers in failure messages.

`_RUN_TEST_RE = re.compile(r"RUN_TEST\(\s*([A-Za-z0-9_]+)\s*\)")` — the tolerant form from F-03, so a
future macro line-wrap cannot silently drop a case. `_extract_run_test_names(path)` reads the file,
comment-strips, and returns the ordered list of captured names.

`_REQUIREMENT_CASES`: a frozen module-level dict literal mapping requirement id → tuple of case names.
It is deliberately NOT derived from the suites (a derived map could never detect a rename) — carry a
comment saying exactly that, in the register of `test_revision_constants_parity.py:172-175`. The five
entries, verified on disk this session as existing exactly once each:

TEST-01 (3): test_each_protocol_resolves_to_its_own_distinct_row, test_unknown_protocol_returns_null,
test_row_values_match_the_frozen_table.

TEST-02 (4): test_loop01_pulse_width_never_grows_between_attempts,
test_loop01_each_byte_gets_exactly_the_seeded_number_of_fixed_width_pulses,
test_loop01_verify_read_follows_every_pulse,
test_loop01_a_byte_that_converges_on_its_last_permitted_pulse_succeeds.

TEST-03 (6): test_loop03_overprogram_duration_is_three_times_the_pulse_count_times_the_width,
test_loop03_overprogram_is_zero_when_the_factor_is_zero,
test_loop03_overprogram_clamps_at_the_cap_rather_than_refusing,
test_loop03_overprogram_is_32_bit_safe_at_the_uint16_ceiling,
test_loop03_a_zero_cap_yields_no_overprogram_pulse,
test_loop04_no_live_row_emits_an_overprogram_pulse.

TEST-04 (6): test_loop05_a_byte_that_misses_within_max_pulses_aborts_the_block,
test_loop05_the_loops_own_strobes_disable_the_high_voltage_route,
test_loop05_a_successful_block_does_not_disable_the_route,
test_vpp02_x3_the_energy_cap_exit_disables_the_route,
test_vpp02_x4_the_final_pass_verify_failure_disables_the_route,
test_vpp02_e1_write_init_error_exit_leaves_no_route_asserted.

TEST-05 (10): test_loop06_an_ff_target_byte_is_never_read_and_never_pulsed,
test_loop06_an_already_matching_byte_is_read_once_and_never_pulsed,
test_loop06_a_block_of_only_skipped_bytes_emits_no_pulse_at_all,
test_loop06_the_ff_rule_does_not_suppress_the_final_verify_pass,
test_0x07_zero_pulse_delay_takes_the_1000us_fallback,
test_0x08_zero_pulse_delay_takes_the_100us_fallback,
test_0x0B_zero_pulse_delay_takes_the_500us_fallback,
test_0x07_nonzero_pulse_delay_is_left_alone,
test_0x08_nonzero_pulse_delay_is_left_alone,
test_0x0B_nonzero_pulse_delay_is_left_alone.

Carry a comment on the TEST-05 entry recording C-04: CONTEXT.md nominated "the two fallback cases",
which names no existing pair — the real shape is two families of three, and the `nonzero_pulse_delay`
family is the non-vacuity half (a fallback that fired unconditionally would pass the three `zero_`
cases and fail these three). Carry a comment on TEST-04 recording that the "reports the address" clause
is satisfied by the `u24 address + u8 pulse count` payload asserted INSIDE
`test_loop05_a_byte_that_misses_within_max_pulses_aborts_the_block`, and a comment on TEST-03 recording
D-03's non-claim: the arithmetic is proven, the in-loop wiring on a live row is not, because no shipped
row sets `overprogram_factor` (`eprom_params.cpp:46-48`), which is what
`test_loop04_no_live_row_emits_an_overprogram_pulse` witnesses.

Legs to author:

1. `test_every_mapped_requirement_names_only_existing_cases` — union the extracted names across the three
   mapped suites, then for each requirement id assert every mapped case name is present. On failure raise
   an AssertionError naming the requirement id, the missing case name, and the mapped-suite files that
   were scanned. Never a bare "lists differ" (the failure-message discipline at
   `test_golden_trace_identity_eprom_v131.py:177-195`).
2. `test_each_mapped_suite_meets_its_hardcoded_case_floor` — per-suite extracted count `>=` its floor
   (47 / 32 / 9), message naming the suite, the floor and the observed count.
3. `test_extracted_case_names_are_unique` — `len(set(names)) == len(names)` across the union; a duplicate
   would inflate a count.
4. `test_trace_suite_is_deliberately_out_of_scope` — assert `test_trace_eprom_v131` is absent from
   `_MAPPED_SUITES`, and assert (against the seam-aware scan root, guarded so it is skipped-free when the
   directory is absent under a planted root by asserting only when the file exists) that the trace suite's
   source contains the `#ifdef` guard token built by concatenation, so the exclusion reason stays true
   rather than becoming folklore.
5. `test_scan_targets_are_non_vacuous` — the two-half pattern, copied structurally from the analog's
   Coverage 8. Part (a): recompute `_REPO_ROOT / _SUITES_REL` fresh WITHOUT reading `os.environ` at all;
   assert it `is_dir()`, that each mapped suite file under it `is_file()` and is non-empty, and that it
   `resolve().is_relative_to(_REPO_ROOT)`. Part (b): run `_extract_run_test_names` against whatever
   `_SCAN_SUITES` currently resolves to and assert the union size `>= _TOTAL_FLOOR` (88). Part (b) is the
   half an emptied scratch root turns RED.
6. `test_this_module_cannot_be_silently_skipped` and
   `test_own_needles_do_not_appear_verbatim_in_this_module` — copy the analog's concatenation-needle form
   (`_NEEDLE_SKIP_CALL`, `_NEEDLE_SKIPIF_MARKER`, `_NEEDLE_DEPENDENCY_SKIP_CALL`,
   `_ALL_SELF_CHECK_NEEDLES`). Do not mix in the line-prefix `startswith` form.

Module docstring must also carry the honest CI-framing paragraph (F-15 item 6): `pytest tests/ -v` runs
at `firestarter/.github/workflows/build.yml:161` and `beta-build.yml:134`, so this module WILL run in CI
once the branch reaches `main`/`beta`, but runs in NO CI leg on this milestone branch — state it that way,
do not imply current coverage.

Style: no fenced-code cleverness, no `pytest.skip`, no `@pytest.mark.skipif`, no `shell=True`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_requirement_case_mapping_v131.py -q 2>&1 | tail -5</automated>
    <automated>cd /workspaces/firestarter && python3 -c "import pathlib,re; t=pathlib.Path('tests/test_requirement_case_mapping_v131.py').read_text(); assert 'FIRESTARTER_CASE_MAP_SCAN_ROOT' in t; assert 'test_trace_eprom_v131' in t; print('seam+exclusion present')"</automated>
    <automated>cd /workspaces/firestarter && ls scripts/check_*.py | wc -l | grep -x 6 && ls -d tests/fixtures/planted_* | wc -l | grep -x 15</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_requirement_case_mapping_v131.py -q` reports 0 failed and at least 7 passed.
    - The module contains the literal string `FIRESTARTER_CASE_MAP_SCAN_ROOT` exactly once as an `os.environ.get` key, and no other `os.environ` read.
    - `_TOTAL_FLOOR` is the literal `88` and `_SUITE_FLOORS` carries the literals `47`, `32`, `9`; a comment in the module states that 88 is the three-suite mapping denominator and 79 is `native_loop_v131`'s per-env figure, never to be added.
    - `_REQUIREMENT_CASES` has exactly five keys (`TEST-01`…`TEST-05`) with 3 / 4 / 6 / 6 / 10 case names respectively, and every name appears exactly once in `grep -rhoE "RUN_TEST\(<name>\)"` across the three mapped suite files.
    - The module's `_MAPPED_SUITES` does NOT contain `test_trace_eprom_v131`, and the docstring states the exclusion reason (build-flag-dependent case set; proves TEST-06 not TEST-01…05).
    - `grep -c 'firestarter/scripts/check_' tests/test_requirement_case_mapping_v131.py` is 0 and `ls firestarter/scripts/check_*.py | wc -l` is still 6 — no checker was added, so `FLOOR`/`FIXTURE_FLOOR` need no edit.
    - `git -C /workspaces/firestarter status --porcelain -- src/` is empty (D-04 invariant).
    - The file is committed before any suite run beyond this module's own (D-20).
  </acceptance_criteria>
  <done>The mapping gate exists, passes, scans the three mapped suites through a single documented import-time seam, and carries hardcoded floors plus both self-check legs.</done>
</task>

<task type="auto">
  <name>Task 2: Prove both D-18 planted violations in a child process, and record the RED/GREEN pair</name>
  <files>firestarter/tests/test_requirement_case_mapping_v131.py</files>
  <read_first>
    - `firestarter/tests/test_flash_path_record_sync.py` :269-305 — `_run_gate_in_subprocess`, including the `FIRESTARTER_129_GATE_CHILD` recursion guard; and :1202-1250 — the six-move "the plant never touched the source of truth" ceremony (capture the real path BEFORE any patch, hash it, assert the mutation actually differs, write under `tmp_path`, assert the blob unchanged after, assert porcelain clean).
    - `firestarter/tests/test_requirement_case_mapping_v131.py` — the module as authored in Task 1 (the legs the plants must trip, and the seam name).
    - `.planning/phases/144-tests-build-verification/144-VALIDATION.md` § "Per-gate planted-violation specification" — rows 1 and 2 (D-01 mapping, and D-01 mapping second plant). Lift the RED/GREEN requirements verbatim.
  </read_first>
  <action>
Add two planted-violation legs to the same module, plus a `_run_gate_in_subprocess(env_overrides,
node_id=None)` helper copied structurally from `test_flash_path_record_sync.py:269-305` — list-form argv
`[sys.executable, "-m", "pytest", target, "-q", "-rs"]`, `cwd=_REPO_ROOT`, `env` a copy of `os.environ`
updated with the overrides, and a recursion guard that refuses (plain `assert`) when
`FIRESTARTER_144_GATE_CHILD` is already set in the current process. A child process is MANDATORY here
because `_SCAN_SUITES` binds at import and `monkeypatch.setenv` cannot reach it (S6).

Plant A — `test_planted_renamed_case_is_detected`: build a scratch scan root under `tmp_path` mirroring
`test/native/avr/<suite>/<suite>.cpp` for all three mapped suites; copy each real suite source; in the
`test_loop_eprom_v131` copy replace `test_loop01_pulse_width_never_grows_between_attempts` with
`test_loop01_pulse_width_never_grows` (both occurrences — the function definition and the `RUN_TEST`
site) and assert the mutated text differs from the real text before writing it (a silently-unmatched
`.replace()` is a vacuous plant). Run the child with `FIRESTARTER_CASE_MAP_SCAN_ROOT` pointed at the
scratch root, restricted to the node id of `test_every_mapped_requirement_names_only_existing_cases`.
Assert the child's returncode is non-zero, that its output contains the missing case name
`test_loop01_pulse_width_never_grows_between_attempts` AND the requirement id `TEST-02`, and that it does
NOT contain the phrases the OTHER legs emit (leg isolation — the non-vacuity leg's floor message must be
absent, since 88 names are still extractable from the scratch root minus one rename... assert instead
that the message names exactly the one missing case, not a count-only complaint).

Plant B — `test_planted_emptied_scan_root_fails_the_non_vacuity_leg`: create an EMPTY directory under
`tmp_path` (no `.cpp` anywhere) and run the child with the seam pointed at it, restricted to the node id
of `test_scan_targets_are_non_vacuous`. Assert non-zero returncode and that the output names the floor
literal 88 and the observed count 0 — proving an empty parse fails closed rather than making every
membership check pass vacuously. Also assert the child output does not report the run as skipped
(`-rs` is passed precisely so a skip would be visible).

Both legs, before finishing, must execute the S2 ceremony against the REAL suite sources: capture each
real path first, `git hash-object` each before and after, assert equality, and assert
`git status --porcelain` for the whole firmware repo is empty at the end. Use a fail-closed
`shutil.which(os.environ.get("GIT", "git"))` resolution with a plain `assert` (never a skip), list-form
argv, `shell=False`.

Then capture the D-18 evidence pair for the SUMMARY, verbatim, both halves:
- RED: the child-process transcripts from both plants (run the two plant node ids and paste the full
  output, including the failure message text).
- GREEN: the full-module run showing every leg passing, plus the non-vacuity leg's own reported figures
  (union size, per-suite 47 / 32 / 9) so the GREEN is attributable to a non-empty extraction rather than
  to an unreachable leg. A GREEN over an empty set is the trap this pair exists to exclude.

Commit the module (all legs) before running the full firmware suite.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_requirement_case_mapping_v131.py -q -rs 2>&1 | tail -8</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_requirement_case_mapping_v131.py::test_planted_renamed_case_is_detected tests/test_requirement_case_mapping_v131.py::test_planted_emptied_scan_root_fails_the_non_vacuity_leg -q 2>&1 | tail -5</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && grep -n "shell=True" tests/test_requirement_case_mapping_v131.py | wc -l | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - Both planted legs pass, and the SUMMARY carries the verbatim child-process RED transcript for each.
    - The Plant A RED transcript contains both the string `test_loop01_pulse_width_never_grows_between_attempts` and the string `TEST-02`.
    - The Plant B RED transcript names the floor `88` and an observed count of `0`, and shows no skipped outcome.
    - The SUMMARY's GREEN half quotes the non-vacuity leg's observed figures (union `>= 88`; per-suite 47 / 32 / 9), so the GREEN is attributed to a non-empty extraction.
    - `git -C /workspaces/firestarter status --porcelain` is empty after the planted runs, and `git hash-object` for each of the three real suite sources is unchanged from before them.
    - `python3 -m pytest tests/ -q` in `firestarter/` reports 0 failed with a passed count `>= 292` (292 is the pre-phase figure; this plan adds tests, so the total grows — a drop below 292 pre-existing passes is a regression).
    - No `shell=True` anywhere in the module; every subprocess call uses list-form argv.
    - `git -C /workspaces/firestarter diff --stat HEAD -- src/` is empty.
  </acceptance_criteria>
  <done>Both plants are seen RED with locating messages in a child process, the gate is seen GREEN over a non-empty extraction, the real sources are provably untouched, and the whole firmware pytest suite is green with no pre-existing test lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → gate parse | The gate parses C source text whose content is in-principle untrusted (a future edit, a planted fixture, an env-redirected scratch root) |
| env var → scan target | `FIRESTARTER_CASE_MAP_SCAN_ROOT` redirects what the gate reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-01 | Repudiation | `test_requirement_case_mapping_v131.py` reporting exit 0 over an empty parse | mitigate | Two-half non-vacuity leg with hardcoded floors 88 / 47 / 32 / 9; part (a) recomputes the default root without reading `os.environ`; Plant B proves it fails closed |
| T-144-02 | Repudiation | The module silently skipping (missing dependency, absent path) | mitigate | `test_this_module_cannot_be_silently_skipped` plus needle-hygiene leg; no `pytest.skip`, no `@pytest.mark.skipif`, no `importorskip` |
| T-144-03 | Tampering | A planted run writing into the real tree | mitigate | S2 six-move ceremony: real paths captured first, `git hash-object` before/after, plant written only under `tmp_path`, whole-repo porcelain asserted clean |
| T-144-04 | Tampering / EoP | `subprocess` invoked through a shell with an interpolated path | mitigate | List-form argv only, `shell=False` (default), git resolved via `shutil.which`; asserted by the verify leg that greps for `shell=True` |
| T-144-05 | Denial of service | Catastrophic regex backtracking on the 88-site sources | accept | `RUN_TEST\(\s*([A-Za-z0-9_]+)\s*\)` is linear with no nested quantifiers; the comment-strip regexes are the repo's existing bounded forms |
| T-144-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages; RESEARCH § Package Legitimacy Audit records "not applicable — this phase installs zero external packages". A plan that proposes a dependency is a scope deviation to report, not absorb |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/test_requirement_case_mapping_v131.py -q -rs` — 0 failed, no skips.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` — 0 failed, passed `>= 292`.
- `cd /workspaces/firestarter && git status --porcelain` — empty (committed, D-20).
- `cd /workspaces/firestarter && git diff --stat HEAD~1 -- src/ | wc -l` — 0 (D-04).
- `cd /workspaces/firestarter && python3 -m pytest tests/test_protocol_branch_inventory.py tests/test_golden_trace_identity_eprom_v131.py tests/test_checker_convention.py -q` — 0 failed (the three goldens/conventions this plan must leave undisturbed).
</verification>

<success_criteria>
- TEST-01…TEST-05 each name only cases that provably exist, checked by a parse rather than by prose.
- Both named planted violations produce a locating RED in a child process; the GREEN is attributed to a non-empty extraction with its figures quoted.
- No file under `firestarter/src/` changed; `FLOOR`/`FIXTURE_FLOOR` untouched at 6 / 15; no new native env, no new `RUN_TEST`.
- The firmware repo is clean and its pytest suite green at plan end.
</success_criteria>

<output>
Create `.planning/phases/144-tests-build-verification/144-01-SUMMARY.md` when done. It MUST contain both
D-18 transcripts verbatim (two RED child-process runs, one GREEN full-module run with the non-vacuity
figures quoted), the observed per-suite extraction counts, and a note that C-04's phantom pair was
corrected in the frozen map rather than propagated.
</output>
