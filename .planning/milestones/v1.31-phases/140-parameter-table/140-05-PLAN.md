---
phase: 140-parameter-table
plan: 05
type: execute
wave: 2
depends_on: [140-01]
files_modified:
  - firestarter/tests/golden/eprom_params_citations.json
  - firestarter/tests/test_eprom_params_citations.py
commits_land_in:
  - firestarter/          # submodule — tests/ and tests/golden/
autonomous: true
requirements: [TABLE-04, TABLE-01, TABLE-02]
must_haves:
  truths:
    - "Every one of the 18 (row, column) cells carries exactly one citation — no unattributed number ships"
    - "No citation exists for a cell that does not exist in the table"
    - "Every datasheet citation names family, part, document number, revision/date and section, and carries the D-09 scope clause"
    - "Every non-datasheet value carries the literal 'no datasheet basis — reasoned from ' prefix"
    - "A value cannot change without its citation following — the sidecar's value is compared against the live table"
    - "D-07: on the 0x0B row both limits are attributed — energy_cap_us = 50000 cites the 2716-class datasheet (TI TMS 2516 t_w(PR)) and max_pulses = 255 carries the 'reasoned from: termination bound' form, per D-07's attribution requirement. D-07's other half — that both limits BIND at runtime, whichever trips first, as one LOOP-05 hard-fail reporting which limit tripped — is loop behaviour and is Phase 141's, deferred here by D-10 (this phase ships DATA + GATES + TEST only; eprom.cpp is byte-unchanged)"
    - "The struct's field-name list is exactly the frozen six and none of them is a pulse-width column"
    - "The gate has been SEEN RED on planted violations before its GREEN is believed"
  artifacts:
    - path: "firestarter/tests/golden/eprom_params_citations.json"
      provides: "the D-14 machine-readable citation sidecar — one entry per (row, column) cell, 18 total"
      contains: "no datasheet basis"
      min_lines: 120
    - path: "firestarter/tests/test_eprom_params_citations.py"
      provides: "the TABLE-04 coverage gate plus the TABLE-01/02 structural assertions: 10 tests"
      contains: "def test_"
      min_lines: 200
  key_links:
    - from: "firestarter/tests/test_eprom_params_citations.py"
      to: "firestarter/include/eprom_params.h"
      via: "live re-parse of the struct's ordered field names"
      pattern: "eprom_params\\.h"
    - from: "firestarter/tests/test_eprom_params_citations.py"
      to: "firestarter/src/proms/eprom_params.cpp"
      via: "live re-parse of EPROM_PARAM_KEYS and the three row initialisers"
      pattern: "eprom_params\\.cpp"
    - from: "firestarter/tests/golden/eprom_params_citations.json"
      to: "firestarter/src/proms/eprom_params.cpp"
      via: "per-cell value equality — a value that moves without its citation fails the gate"
      pattern: "cells"
---

<objective>
Ship TABLE-04: a machine-readable citation sidecar carrying one entry per `(row, column)` cell of
the parameter table (D-14), and a committed gate asserting the entry set **exactly** covers the
table's cells — every value cited, and no citation for a value that does not exist. That turns "no
unattributed number ships" into an exit code rather than an inspection. The same module carries the
TABLE-01 field-name freeze and the TABLE-02 no-pulse-column assertion, because both are structural
readings of the same two files.

Purpose: TABLE-04's guarantee cannot rest on inline C comments (inspection) or on a prose markdown
document (nothing detects a value changing without its citation following). D-14 requires the
sidecar plus the gate.

Output: `firestarter/tests/golden/eprom_params_citations.json`,
`firestarter/tests/test_eprom_params_citations.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-parameter-table/140-CONTEXT.md
@.planning/phases/140-parameter-table/140-PATTERNS.md
@.planning/phases/140-parameter-table/140-VALIDATION.md
</context>

<repository>
This is a meta-repo. `firestarter/` is a git submodule that is NOT committed to the meta-repo.
Both new files are committed **inside** `/workspaces/firestarter`, on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`. Verify with
`git -C /workspaces/firestarter branch --show-current` before committing.

**Datasheet PDFs are NOT committed** (locked decision; Open Question 3). No `datasheets/` tree
resolves on this branch — `doc/PROTOCOLS.md` already cites paths that do not exist here (F-140-08).
Citations are therefore self-describing (vendor, part, document number, revision/date,
section/figure), with the recovery command recorded in the sidecar's `meta` header.

**Wave 2 runs three plans concurrently inside this same submodule** (140-02, 140-04, 140-05). Their
`files_modified` sets do not overlap, but the git index is shared. Stage and commit **only** the
paths named in this plan's `files_modified` — never `git add -A`, never `git commit -a`, and never a
commit helper that stages everything, or you will sweep a sibling plan's in-flight work into your
commit. If `git status --porcelain` shows a path this plan does not own, leave it alone.
</repository>

<citation_content>
The eighteen cells, with the basis each must record. Row chip counts for the D-09 scope clause:
`0x07` = 170, `0x08` = 127, `0x0B` = 32.

**Row `0x07` — family: Intel Intelligent Programming / Winbond SMART Programming Algorithm 2 /
ST PRESTO IIB (28-pin 27C family). Representative part: Winbond W27C512 (the part Phase 145's
required bench coverage uses).**

| Column | Value | Basis |
|---|---|---|
| `max_pulses` | 25 | **datasheet** — Winbond W27C512 Data Sheet, Rev A4, published November 1999, "SMART PROGRAMMING ALGORITHM 2" flowchart decision box `X = 25?`; corroborated by ST M27C512, Rev 3, May 2007, Fig. 4 `++n = 25`. **Mandatory note:** Microchip 27C512A, DS11173G, 2004, §1.6 specifies **10**; 25 is the maximum across the row's cited datasheets, chosen so no compliant part is refused. |
| `overprogram_factor` | 0 | **datasheet + operator decision (2026-08-09)** — all three `0x07` datasheets read apply no overprogram: Winbond W27C512 Rev A4 (no overprogram step in either of its algorithms), ST M27C512 Rev 3 §2.6 verbatim *"No overprogram pulses are applied since the verify in MARGIN MODE provides the necessary margin"*, Microchip 27C512A DS11173G §1.6. Those cover 113 of the row's 170 chips. Also behaviour-preserving: `src/proms/eprom.cpp:161-178` is retry escalation of `pulse_delay`, not an Intel 3N margin pulse, so `3` would be an unvalidated behaviour change to all 170 chips. **Named divergence (F-140-05):** `PROJECT.md`'s throughput table implies `3`, and the 22 Intel-family 1 ms parts on this row (Intel 2764 / 2764A / 27128 / 27128A / 27512, TI TMS2764, NEC UPD2764, ST M2764A and the rest of the 1000 µs sub-population) genuinely do want `3 x N`. Serving them correctly requires splitting `0x07`, which TABLE-05's no-second-dispatch-key constraint forbids — recorded as a Phase 146 follow-up candidate, not silently dropped. |
| `overprogram_cap_us` | 75000 | **reasoned** — Intel Intelligent worst case at the row's largest database pulse (1000 µs) times the datasheet max-pulse count (25) = 75 ms; also the exact figure published in the gh#15 correction. Explicitly **inert** on this row because `overprogram_factor` is 0. |
| `verify_mode` | `VERIFY_PER_PULSE_PLUS_FINAL` | **datasheet** — W27C512 "Compare All Bytes to Original Data" after the last address; ST M27C512 Fig. 4 "CHECK ALL BYTES"; Microchip 27C512A Fig. 1-3 "All bytes = original data?". **Mandatory D-02 clause:** this column encodes WHEN to verify and never at what VCC. The final pass's specified VCC levels (6 V / 4.2 V on ST; 4.5 V / 5.5 V on Microchip) are unreachable on the RURP — there is no VCC-raise path, the ~6.25 V program-VCC ceiling is this milestone's evidence ceiling — so a verify-VCC column could only record a value the firmware cannot act on. |
| `vpp_path` | `VPP_PATH_DROP_RESISTOR` | **datasheet (voltage) + code (route)** — ST M27C512 Rev 3 VPP 12.75 V +/- 0.25; Winbond W27C512 Rev A4 VPP 11.75 / 12.0 / 12.25 V. Route matches `src/proms/eprom.cpp:150` `CTRL_VPP_REGULATOR_ENABLE | CTRL_VPP_VPE_DROP_ENABLE`. |
| `energy_cap_us` | 0 | **reasoned** — no 28-pin datasheet read specifies a per-byte energy budget; 0 is the sentinel for "no cap". |

**Row `0x08` — family: Intel Quick-Pulse / AMD Flashrite / ST PRESTO II (32-pin 27C family).
Representative part: AMD Am27C020 (FINAL).**

| Column | Value | Basis |
|---|---|---|
| `max_pulses` | 25 | **datasheet** — Winbond W27C020 (Preliminary), "SMART PROGRAMMING ALGORITHM" flowchart `X = 25?`; ST M27C1001 Fig. 5 `++n = 25`. **Mandatory note:** the representative part (AMD Am27C020) says *"until it verifies or the maximum is reached"* **without stating a number**. |
| `overprogram_factor` | 0 | **datasheet — D-06 RESOLVED** — ST M27C1001 §2.6 verbatim *"No overprogram pulse is applied since the verify in Margin mode provides necessary margin to each programmed cell"*; AMD Am27C020's Flashrite description contains no overprogram step; Winbond W27C020's flowchart has none. Three independent vendors, including the 27C010-class part D-06 names. **Named contradiction:** this agrees with `PROJECT.md`'s prose and **contradicts `PROJECT.md`'s own throughput table**, which gives `0x08` a `3 x N x pulse` overpulse. The contradiction is named here rather than silently resolved. |
| `overprogram_cap_us` | 75000 | **reasoned** — uniform ceiling with `0x07`; explicitly **inert** on this row because `overprogram_factor` is 0. D-08's phrase "both overprogramming rows" presupposed two such rows; research found none. |
| `verify_mode` | `VERIFY_PER_PULSE_PLUS_FINAL` | **datasheet** — AMD Am27C020 *"After the final address is completed, the entire EPROM memory is verified at VCC = VPP = 5.25 V"*; Winbond W27C020 "Compare All Bytes to Original Data"; ST M27C1001 "CHECK ALL BYTES". Same D-02 clause: AMD programs at VCC = 6.25 V and verifies at 5.25 V, and neither rail is reachable on this shield. |
| `vpp_path` | `VPP_PATH_DROP_RESISTOR` | **datasheet (voltage) + code (route)** — Intel 27C010 VPP 12.5 / 12.75 / 13.0 V; AMD Am27C020 VPP 12.75 V +/- 0.25; Winbond W27C020 Vpp 12 V. Route `src/proms/eprom.cpp:150`; the DIP32 pin-1 routing is handled separately by `using_p1_as_vpp` (`eprom.cpp:320`), which is pin routing, not an algorithm axis. |
| `energy_cap_us` | 0 | **reasoned** — same wording as `0x07`. |

**Row `0x0B` — family: classic 2716 / 2732 50 ms single-pulse programming. Representative part:
TI TMS 2516-25/35/45 JL, December 1979, revised May 1982.**

| Column | Value | Basis |
|---|---|---|
| `max_pulses` | 255 | **reasoned** — termination bound. F-140-06: the floor is **250** — reaching the 50 ms energy cap needs 250 pulses at 200 µs (5 of 32 chips), 100 at 500 µs (21 chips) and 50 at 1000 µs (6 chips), so a backstop below 250 pre-empts the energy cap on the 200 µs sub-population and silently under-programs 5 chips. 255 is the smallest `uint8_t`-representable value clearing the floor. |
| `overprogram_factor` | 0 | **datasheet** — TI TMS 2516 specifies a single 50 ms pulse per location; no overprogram step appears anywhere in the document. |
| `overprogram_cap_us` | 75000 | **reasoned** — uniform ceiling with `0x07` and `0x08`; explicitly **inert** on this row because `overprogram_factor` is 0. |
| `verify_mode` | `VERIFY_PER_PULSE` | **datasheet** — TMS 2516 p.139: a verification *"can be done on each location immediately after that location is programmed"*; no final full-array pass is specified anywhere in the document. |
| `vpp_path` | `VPP_PATH_DIRECT_VPE` | **datasheet + code** — TMS 2516 mode table: Start Programming VPP = **+25 V**; Program Verification VPP = +25 V (or +5 V), corroborating both the direct rail and VPE held across verify. Route `src/proms/eprom.cpp:147`. |
| `energy_cap_us` | 50000 | **datasheet** — TI TMS 2516 AC "recommended timing requirements for programming": `t_w(PR)` pulse width, program pulse = MIN 45 / TYP **50** / MAX 55 ms; and *"a 50-millisecond TTL high-level pulse should be applied to the PGM pin at each address location to be programmed. Maximum pulse width is 55 milliseconds."* **Correction that must be recorded, not applied here (F-140-07):** the justification published on gh#15 and carried in `PROJECT.md` — that 50 ms "is exactly the classic 2716 *total* programming time", derived as `100 x 500 µs` — is factually wrong. That datasheet states total programming time for all bits is **100 seconds**; 50 ms is the per-location pulse. The **value** has a genuine primary basis; the **reason** published for it does not. Phase 146 / CLOSE-04 reconciles the posted text; this phase does not edit it. |

**The D-09 scope clause** attaches to every `basis == "datasheet"` entry, verbatim except for the
substituted count: `representative of this row; not asserted true of all N chips carrying this
protocol_id.` with N = 170 / 127 / 32 for `0x07` / `0x08` / `0x0B`.

**The reasoned prefix** is the exact literal string `no datasheet basis — reasoned from ` (em dash,
trailing space) at the start of every `basis == "reasoned"` entry's `reasoned_from` field.
</citation_content>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/golden/eprom_params_citations.json — 18 cells, one citation each</name>
  <files>firestarter/tests/golden/eprom_params_citations.json</files>
  <read_first>
    - `/workspaces/firestarter/tests/golden/eprom_v131_trace_inventory.json` — the `meta`-block key
      set to copy (`source`, `recorded_by`, `requirement`, `blob_sha`, `recorded_at_head`,
      `why_two_checks`, `how_to_update`, `frozen_for`)
    - `/workspaces/firestarter/include/eprom_params.h` and
      `/workspaces/firestarter/src/proms/eprom_params.cpp` — written by plan 140-01; the field
      names, the three keys and the eighteen live values this sidecar must mirror exactly
    - `/workspaces/.planning/phases/140-parameter-table/140-RESEARCH.md` § Datasheet Attribution
      Matrix — the verbatim datasheet quotations behind every cell, and § Code Examples for the
      `git show v1.16-protocol-first-architecture-rebuild:datasheets/...` recovery command
    - `/workspaces/.planning/phases/140-parameter-table/140-CONTEXT.md` § D-02, D-09, D-14
  </read_first>
  <action>
Create `firestarter/tests/golden/eprom_params_citations.json`.

`meta` block:
- `sources`: the two relative paths `include/eprom_params.h` and `src/proms/eprom_params.cpp`.
- `blob_shas`: a map of those paths to `git rev-parse HEAD:<path>` output.
- `recorded_at_head`, `recorded_by` ("Phase 140 Plan 05"), `requirement` ("TABLE-04"),
  `decision` ("D-09, D-14").
- `scope_clause_template`: the literal D-09 sentence with `{N}` where the chip count goes.
- `reasoned_prefix`: the literal string `no datasheet basis — reasoned from `.
- `row_chip_counts`: `{"0x07": 170, "0x08": 127, "0x0B": 32}`.
- `datasheets_not_committed`: one paragraph stating that no `datasheets/` tree resolves on this
  branch (F-140-08), that citations are therefore self-describing, and giving the recovery command
  `git -C firestarter show v1.16-protocol-first-architecture-rebuild:datasheets/<slug>/<file>.pdf`
  together with the note that the TI TMS 2516 PDF has no text layer and must be rendered
  (`pdftoppm -png -r 170`).
- `why_two_checks`: a citation that can drift from its value is not a citation — the gate compares
  each recorded `value` against the live table as well as checking coverage.
- `how_to_update`: if a value changes, its citation changes in the same commit; never edit a value
  here to match a surprise in the source.
- `evidence_ceiling`: one paragraph carrying the D-02 constraint — `verify_mode` encodes WHEN to
  verify and never at what VCC, because the datasheets' raised-VCC verify passes are unreachable on
  this shield.
- `named_divergences`: an array of the three items that must stay visible — (1) `0x07`'s
  Intel-family `3 x N` divergence from `PROJECT.md`'s throughput table (F-140-05); (2) `0x08`'s
  contradiction between `PROJECT.md`'s prose and `PROJECT.md`'s own throughput table (D-06); (3)
  F-140-07's wrong published justification for `energy_cap_us = 50000`. Each with a one-line
  statement and a Phase 146 hand-off note.

`cells`: an array of exactly **18** objects, ordered row-major (`0x07` then `0x08` then `0x0B`;
within each row the six columns in the struct's declaration order). Each object carries:
`row`, `column`, `value`, `basis` (either `"datasheet"` or `"reasoned"`), and then —
- for `basis == "datasheet"`: `family`, `part`, `document`, `revision`, `section`, `quote`
  (the verbatim clause), `scope` (the D-09 sentence with the row's count substituted), and
  optionally `notes`;
- for `basis == "reasoned"`: `reasoned_from`, beginning with the exact literal prefix
  `no datasheet basis — reasoned from `, and optionally `notes`.

Populate every cell from this plan's `<citation_content>` tables. `value` must be recorded in the
form the live table uses: integers for the four numeric columns, and the enum identifier strings
`VERIFY_PER_PULSE` / `VERIFY_PER_PULSE_PLUS_FINAL` / `VPP_PATH_DROP_RESISTOR` /
`VPP_PATH_DIRECT_VPE` for `verify_mode` and `vpp_path`.

Do NOT cite any datasheet by repository path — no such path resolves on this branch.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 - <<'PY'
import json, re
g = json.load(open("tests/golden/eprom_params_citations.json"))
cells = g["cells"]
assert len(cells) == 18, len(cells)
rows = ["0x07", "0x08", "0x0B"]
cols = ["overprogram_cap_us", "energy_cap_us", "max_pulses",
        "overprogram_factor", "verify_mode", "vpp_path"]
assert {(c["row"], c["column"]) for c in cells} == {(r, k) for r in rows for k in cols}
pref = g["meta"]["reasoned_prefix"]
assert pref == "no datasheet basis — reasoned from ", repr(pref)
scope_re = re.compile(
    r"^representative of this row; not asserted true of all \d+ chips "
    r"carrying this protocol_id\.$")
counts = g["meta"]["row_chip_counts"]
for c in cells:
    if c["basis"] == "datasheet":
        for k in ("family", "part", "document", "revision", "section", "quote", "scope"):
            assert c.get(k), (c["row"], c["column"], k)
        assert scope_re.match(c["scope"]), c["scope"]
        assert str(counts[c["row"]]) in c["scope"], (c["row"], c["scope"])
    else:
        assert c["basis"] == "reasoned", c["basis"]
        assert c["reasoned_from"].startswith(pref), c["reasoned_from"][:60]
assert len(g["meta"]["named_divergences"]) == 3
print("SIDECAR_OK cells=%d" % len(cells))
PY</automated>
    <automated>cd /workspaces/firestarter && ! grep -q 'datasheets/0x0' tests/golden/eprom_params_citations.json || (echo "FAIL: cited a repo path that does not resolve on this branch"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/tests/golden/eprom_params_citations.json` parses as JSON and `cells` has exactly
      **18** entries.
    - The `(row, column)` set equals the cross-product of `["0x07","0x08","0x0B"]` and the six
      column names, with no duplicates.
    - `meta.reasoned_prefix` is exactly `no datasheet basis — reasoned from ` (em dash, trailing
      space), and every `basis == "reasoned"` cell's `reasoned_from` starts with it.
    - Every `basis == "datasheet"` cell has non-empty `family`, `part`, `document`, `revision`,
      `section`, `quote` and `scope`, and `scope` matches
      `^representative of this row; not asserted true of all \d+ chips carrying this protocol_id\.$`
      with the count equal to `meta.row_chip_counts[row]`.
    - `meta.named_divergences` has exactly 3 entries covering F-140-05, D-06's `PROJECT.md`
      contradiction and F-140-07.
    - `meta.evidence_ceiling` states the D-02 constraint (`verify_mode` never encodes a verify VCC).
    - No cell or `meta` field cites a datasheet by repository path (no `datasheets/0x0...` string).
    - `meta.blob_shas` matches the live `git rev-parse HEAD:<path>` for both source files.
  </acceptance_criteria>
  <done>Every value in every row is attributed, in a machine-readable form, with the divergences and
  the evidence ceiling recorded in the same file as the numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/test_eprom_params_citations.py — the 10-test TABLE-04/01/02 gate</name>
  <files>firestarter/tests/test_eprom_params_citations.py</files>
  <read_first>
    - `/workspaces/firestarter/tests/test_golden_trace_identity_eprom_v131.py` (whole file) — the
      pattern to COPY, not import: self-contained path resolution, fail-closed `_resolve_git()` /
      `_git()`, comment-stripping independent re-parse, first-divergence reporting, non-vacuity
      floor, startswith-based skip-bypass self-scan
    - `/workspaces/firestarter/include/eprom_params.h` — the struct whose ordered field names this
      module re-parses
    - `/workspaces/firestarter/src/proms/eprom_params.cpp` — `EPROM_PARAM_KEYS` and the three row
      initialisers this module re-parses
    - `/workspaces/firestarter/tests/golden/eprom_params_citations.json` — written in Task 1
    - `/workspaces/firestarter/tests/test_checker_convention.py` lines 120-135 — why this module
      must NOT be a `scripts/check_*.py`
    - `/workspaces/.planning/phases/140-parameter-table/140-PATTERNS.md` § 6, § 7, § S1
  </read_first>
  <action>
Create `firestarter/tests/test_eprom_params_citations.py`. Stdlib and pytest only; no `conftest.py`
(the firmware repo has none anywhere under `tests/`).

Module docstring in the § S1 Python shape: banner,
`Phase 140 Plan 05 -- TABLE-04 (+ TABLE-01, TABLE-02) (D-09, D-14)`,
`Requirements: TABLE-04, TABLE-01, TABLE-02`, a `Defect class this closes:` line ("a table value
changing without its citation following, a cell shipping with no attribution at all, a citation for
a cell that does not exist, and a pulse-width column entering the table under any name"), a numbered
`Coverage:` list naming all ten tests, and an `Environment seams:` section documenting
`FIRESTARTER_PARAMS_HEADER`, `FIRESTARTER_PARAMS_SOURCE` and `FIRESTARTER_PARAMS_CITATIONS`, that
they bind at import, that they exist only for planted-violation runs, and that tests 1 and 9 ignore
them by construction.

Module-level resolution: `_HERE` / `_REPO_ROOT` from `__file__`;
`_HEADER_REL = "include/eprom_params.h"`; `_SOURCE_REL = "src/proms/eprom_params.cpp"`;
`_CITATIONS_DEFAULT = _HERE / "golden" / "eprom_params_citations.json"`; and the three env-seam
overrides layered on top of those defaults.

Parsers (each comment- and string-stripped first, line numbers irrelevant here):
- `_struct_field_names(header_text)` — the ordered field names inside the
  `typedef struct { ... } eprom_params_t;` body.
- `_row_keys(source_text)` — the ordered literals inside `EPROM_PARAM_KEYS[] PROGMEM = { ... }`,
  normalised to lowercase `0x` hex strings (`0x07`, `0x08`, `0x0b` -> compare case-insensitively
  against the sidecar's `0x0B`).
- `_row_values(source_text)` — for each brace-initialised row inside
  `EPROM_PARAMS[] PROGMEM = { ... }`, the ordered initialiser tokens, with `UL`/`U` suffixes
  stripped from integers and enum identifiers kept as strings.

The ten tests:
1. `test_blob_shas_match_the_recorded_sources` — `git rev-parse HEAD:<path>` (run with
   `cwd=_REPO_ROOT`, never an env-seam path) for both sources vs `meta.blob_shas`.
2. `test_struct_field_names_are_exactly_the_frozen_six_in_order` — the live ordered field list
   equals `["overprogram_cap_us", "energy_cap_us", "max_pulses", "overprogram_factor",
   "verify_mode", "vpp_path"]`, reporting the first divergence by index.
3. `test_no_pulse_width_column_exists` — TABLE-02. No field name matches
   `(?i)(pulse_(width|delay|us)|fallback_pulse)`, and the only field whose name contains the
   substring `pulse` is exactly `max_pulses`. State in the message that a naive substring test is
   fooled by `max_pulses` — that is the trap this test exists to avoid.
4. `test_rows_are_exactly_the_three_27c_protocols` — `_row_keys` yields exactly `0x07`, `0x08`,
   `0x0B` in that order, and `_row_values` yields exactly three rows of six initialisers each.
5. `test_citations_cover_every_cell_exactly_once` — the bijection. The set of `(row, column)` in
   `cells` equals the cross-product of the live row keys and the live field names; there are no
   duplicates; and `len(cells) == 18`. The message must separately name missing cells (a value with
   no citation) and extra cells (a citation for a value that does not exist).
6. `test_every_citation_is_well_formed` — for `basis == "datasheet"`: `family`, `part`, `document`,
   `revision`, `section`, `quote` and `scope` all non-empty, and `scope` matches
   `^representative of this row; not asserted true of all (\d+) chips carrying this protocol_id\.$`
   with the captured count equal to `meta.row_chip_counts[row]`. For `basis == "reasoned"`:
   `reasoned_from` starts with `meta.reasoned_prefix`, and that prefix is itself asserted to be the
   literal `no datasheet basis — reasoned from `. Any other `basis` value fails.
7. `test_recorded_values_match_the_live_table` — for each cell, the recorded `value` equals the
   live initialiser at that row and column position, comparing integers as integers and enum
   identifiers as strings. This is what stops a value drifting away from its citation. Report the
   first mismatch naming row, column, recorded and live.
8. `test_inventory_is_non_vacuous` — `cells_scanned == 18` and `> 0`; both source files resolve,
   exist and are non-empty; the live parse returned 6 field names and 3 rows; every cell object is
   non-empty. Report the scanned counts in the messages so a zero-scan can never read as a pass.
9. `test_default_targets_resolve_inside_this_repository` — recompute all three default paths from
   `_REPO_ROOT` / `_HERE` **without** consulting the environment; assert each exists, is non-empty
   and lies inside this repository.
10. `test_git_is_required_not_optional` — copy the analog's startswith-based self-scan verbatim.

Do NOT place this module under `scripts/`, do NOT name it `check_*.py`, and do NOT add anything
under `tests/fixtures/`.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_eprom_params_citations.py -v 2>&1 | tail -18</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_checker_convention.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_eprom_params_citations.py -v` reports **10 passed**, with the
      ten test names matching those listed in the action.
    - `python3 -m pytest tests/ -q` reports **0 failed** (the absolute total depends on whether plan
      140-02's module has landed yet — assert zero failures here, not a total; plan 140-07 pins the
      final total).
    - `python3 -m pytest tests/test_checker_convention.py -q` still passes — nothing was added under
      `scripts/` or `tests/fixtures/`.
    - Test 3 asserts BOTH that no field matches `(?i)(pulse_(width|delay|us)|fallback_pulse)` AND
      that the only `pulse`-containing field name is exactly `max_pulses`.
    - Test 5's failure message distinguishes missing cells from extra cells.
    - Test 7 compares recorded values against the live initialisers, not against a hardcoded list.
    - The module imports only stdlib and pytest, and contains no line starting with `pytest.skip`
      or `@pytest.mark.skipif`.
  </acceptance_criteria>
  <done>The citation-coverage bijection, the field-name freeze, the no-pulse-column assertion and
  the value-drift check all run as exit codes.</done>
</task>

<task type="auto">
  <name>Task 3: Plant five violations, see the gate RED, capture verbatim, then see it GREEN</name>
  <files>firestarter/tests/test_eprom_params_citations.py</files>
  <read_first>
    - `/workspaces/.planning/phases/139-gh-15-correction-outward/139-02-SUMMARY.md` lines 120-200 —
      the planted-run transcript discipline
    - `/workspaces/firestarter/tests/test_eprom_params_citations.py` — written in Task 2
    - `/workspaces/.planning/phases/140-parameter-table/140-CONTEXT.md` § D-15
  </read_first>
  <action>
Prove the gate can fail, five different ways, before its pass is believed (D-15). Every planted
input lives in a `mktemp -d` scratch directory outside both repositories and is deleted immediately
after its run. No planted input may survive anywhere under `/workspaces`, and neither
`include/eprom_params.h`, `src/proms/eprom_params.cpp` nor the sidecar may be edited in place.

Run A — **a value with no citation**. Copy the sidecar to scratch, delete one cell, run with
`FIRESTARTER_PARAMS_CITATIONS` pointed at the copy. Expected: RED on test 5 naming the missing
`(row, column)`, and on test 8 (`cells_scanned` 17, not 18).

Run B — **a citation for a value that does not exist**. Copy the sidecar and append a cell for
`("0x07", "fallback_pulse_us")`. Expected: RED on test 5 naming it as an extra cell.

Run C — **a datasheet citation with the D-09 scope clause removed**. Copy the sidecar and blank the
`scope` of **exactly one** `basis == "datasheet"` cell — the first in file order, selected with
`next(...)`. Do **not** use `[c.update(scope='') for c in ... ][:1]`: the `[:1]` slices the
comprehension's *output* list after every `.update()` side effect has already run, so it blanks the
scope of every datasheet cell, not one. The run prints `PLANTED_CELL=<row>/<column>` to stdout
before pytest starts, so the captured transcript names the single cell that was blanked.
Expected: RED on test 6 naming that one cell.

Run D — **a value that drifted from its citation**. Copy the sidecar and change one cell's `value`
(for example `0x0B`'s `max_pulses` from 255 to 100). Expected: RED on test 7 naming row, column,
recorded and live.

Run E — **a pulse-width column added to the header**. Copy `include/eprom_params.h` to scratch,
insert a `uint32_t fallback_pulse_us;` field, and run with `FIRESTARTER_PARAMS_HEADER` pointed at
the copy. Expected: RED on test 2 (field list) and test 3 (TABLE-02).

Run F — **the real tree, no env seams set**: all 10 tests pass.

If any RED run fails for the wrong reason — a JSON decode error, an import error, a path error
rather than the planted violation — the leg was unreachable. Fix the **locator**, never the
assertion (D-15 trap 2), and re-run all six.

Record all six runs, with the exact command line and the verbatim stdout, in this plan's SUMMARY.
Then confirm the tree is clean: `git -C /workspaces/firestarter status --porcelain` lists only the
two intended new files, and
`git -C /workspaces/firestarter diff --quiet -- include/eprom_params.h src/proms/eprom_params.cpp`
exits 0.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && T=$(mktemp -d) && python3 -c "import json,sys; g=json.load(open('tests/golden/eprom_params_citations.json')); g['cells']=g['cells'][1:]; json.dump(g, open(sys.argv[1],'w'))" "$T/c.json" && FIRESTARTER_PARAMS_CITATIONS="$T/c.json" python3 -m pytest tests/test_eprom_params_citations.py -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_A_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter && T=$(mktemp -d) && python3 -c "import json,sys; g=json.load(open('tests/golden/eprom_params_citations.json')); e=dict(g['cells'][0]); e['column']='fallback_pulse_us'; g['cells'].append(e); json.dump(g, open(sys.argv[1],'w'))" "$T/c.json" && FIRESTARTER_PARAMS_CITATIONS="$T/c.json" python3 -m pytest tests/test_eprom_params_citations.py -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_B_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter && T=$(mktemp -d) && python3 -c "import json,sys; g=json.load(open('tests/golden/eprom_params_citations.json')); t=next(c for c in g['cells'] if c['basis']=='datasheet'); t['scope']=''; print('PLANTED_CELL=%s/%s' % (t['row'], t['column'])); json.dump(g, open(sys.argv[1],'w'))" "$T/c.json" && FIRESTARTER_PARAMS_CITATIONS="$T/c.json" python3 -m pytest tests/test_eprom_params_citations.py -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_C_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter && T=$(mktemp -d) && python3 -c "import json,sys; g=json.load(open('tests/golden/eprom_params_citations.json')); [c.update(value=100) for c in g['cells'] if c['row']=='0x0B' and c['column']=='max_pulses']; json.dump(g, open(sys.argv[1],'w'))" "$T/c.json" && FIRESTARTER_PARAMS_CITATIONS="$T/c.json" python3 -m pytest tests/test_eprom_params_citations.py -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_D_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter && T=$(mktemp -d) && sed 's/^\( *\)uint8_t  *vpp_path;/\1uint32_t fallback_pulse_us;\n\1uint8_t vpp_path;/' include/eprom_params.h > "$T/eprom_params.h" && FIRESTARTER_PARAMS_HEADER="$T/eprom_params.h" python3 -m pytest tests/test_eprom_params_citations.py -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_E_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_eprom_params_citations.py -q 2>&1 | tail -3 && git diff --quiet -- include/eprom_params.h src/proms/eprom_params.cpp && echo "RUN_F_GREEN_AND_SOURCES_UNCHANGED"</automated>
  </verify>
  <acceptance_criteria>
    - Runs A, B, C, D and E each exit **non-zero**, and the captured stdout names the planted
      violation as the reason — not a JSON-decode, import or path error.
    - Run A's output names the missing `(row, column)`; Run B's names the extra cell; Run C's
      transcript opens with exactly one `PLANTED_CELL=<row>/<column>` line and its RED names **that
      one cell and no other** — a Run C transcript reporting a blank scope clause on several cells
      means the mutation over-applied, which makes the run's breadth disagree with this plan, and
      the mutation must be fixed before its RED is believed; Run D's names row, column, recorded and
      live; Run E's fails on both test 2 and test 3.
    - Run F exits 0 with 10 passed.
    - The SUMMARY reproduces all six runs verbatim: exact command line and exact stdout.
    - `git -C /workspaces/firestarter status --porcelain` lists only the two intended new files;
      `git -C /workspaces/firestarter diff --quiet -- include/eprom_params.h
      src/proms/eprom_params.cpp tests/golden/eprom_params_citations.json` exits 0.
  </acceptance_criteria>
  <done>The citation gate has been seen RED five ways for the right reasons and GREEN once, and the
  RED output is on the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan owns:
- `firestarter/tests/golden/eprom_params_citations.json` — keys `meta` (with `sources`,
  `blob_shas`, `scope_clause_template`, `reasoned_prefix`, `row_chip_counts`,
  `datasheets_not_committed`, `why_two_checks`, `how_to_update`, `evidence_ceiling`,
  `named_divergences`) and `cells` (18 entries)
- `firestarter/tests/test_eprom_params_citations.py` — 10 pytest cases; env seams
  `FIRESTARTER_PARAMS_HEADER`, `FIRESTARTER_PARAMS_SOURCE`, `FIRESTARTER_PARAMS_CITATIONS`

Full Phase 140 artifact list: see `140-01-PLAN.md` § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<requirement_completion>
This plan **contributes to** TABLE-04, TABLE-01 and TABLE-02.

**Requirement IDs this plan may mark Complete: NONE.**

TABLE-01 spans plans 140-01, 140-04 and 140-05; TABLE-02 spans 140-01 and 140-05; TABLE-04 spans
140-05 and 140-06. Only plan 140-07 may flip any TABLE-* checkbox in `.planning/REQUIREMENTS.md` or
`.planning/ROADMAP.md`.
</requirement_completion>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| table value → citation | a value that can change without its citation following is unattributed in practice |
| gate → CI verdict | a bijection gate that scans an empty cell list exits 0 and manufactures false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-18 | Repudiation | an unattributed number shipping | mitigate | The bijection test asserts the `(row, column)` cell set equals the live cross-product, so a value with no citation and a citation with no value both fail. Planted Runs A and B prove both directions. |
| T-140-19 | Repudiation | citation drift | mitigate | Test 7 compares each recorded `value` against the live initialiser. Planted Run D proves it fires. |
| T-140-20 | Repudiation | an overclaiming citation | mitigate | Every datasheet citation carries the D-09 scope clause with the row's real chip count; test 6 enforces it literally. Planted Run C proves it fires. |
| T-140-21 | Tampering | a pulse-width column entering the table | mitigate | Test 3 checks both the negative regex and the positive "only `max_pulses` contains `pulse`" assertion. Planted Run E proves it fires. |
| T-140-22 | Information disclosure | a knowingly-inert verify-VCC field | mitigate | D-02 forbids the column; `meta.evidence_ceiling` records why in the same file as the numbers, rather than only in Phase 146's ledger. |
| T-140-23 | Tampering | a stray `FIRESTARTER_PARAMS_*` env var in CI | mitigate | Tests 1 and 9 recompute their targets from `_REPO_ROOT` ignoring the environment, so a redirect turns the gate RED rather than green. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages in either repository (`140-RESEARCH.md` § Package Legitimacy Audit). |
</threat_model>

<verification>
- `python3 -m pytest tests/test_eprom_params_citations.py -q` → 10 passed.
- `python3 -m pytest tests/ -q` → 0 failed.
- `python3 -m pytest tests/test_checker_convention.py -q` still passes.
- Five planted RED runs and one GREEN run, all six captured verbatim in the SUMMARY.
- `include/eprom_params.h` and `src/proms/eprom_params.cpp` are both byte-unchanged by this plan.
</verification>

<success_criteria>
- Every one of the 18 cells is attributed, machine-readably, in a form a gate can check.
- The three named divergences (F-140-05, D-06's `PROJECT.md` contradiction, F-140-07) are recorded
  in the sidecar rather than smoothed away.
- The gate has been seen RED on five distinct planted violations before any pass was believed.
</success_criteria>

<output>
Create `.planning/phases/140-parameter-table/140-05-SUMMARY.md` when done. It MUST reproduce, for
each of Runs A/B/C/D/E/F, the exact command line and the verbatim stdout, and state the cell count
(18) and the three named divergences.
</output>
