---
phase: 140-parameter-table
plan: 07
type: execute
wave: 4
depends_on: [140-01, 140-02, 140-03, 140-04, 140-05, 140-06]
files_modified:
  - .planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
commits_land_in:
  - .planning/            # meta repo — the phase record and the requirement checkboxes (COMMITS)
  - firestarter/          # submodule — READ-ONLY: cold build/test capture, no commit lands here
  - firestarter_app/      # submodule — READ-ONLY: suite re-run, no commit lands here
autonomous: true
requirements: [TABLE-01, TABLE-02, TABLE-03, TABLE-04, TABLE-05]
must_haves:
  truths:
    - "The predicted ~0 AVR flash delta is reconciled against a cold measurement taken after the prediction was committed"
    - "AVR RAM delta is exactly 0 and the MERGE-05 band run passes"
    - "Both pinned native envs are still at 141 cases / 17 suites and the native warning watermark is still exactly 1166"
    - "The two non-CI native envs' counts are recorded as run-by-name obligations, with the absence of CI coverage stated"
    - "The 0x08 PROJECT.md contradiction and the 0x07 Intel-family divergence are named in the phase record, not smoothed"
    - "All five TABLE requirements are marked Complete exactly once, here"
  artifacts:
    - path: ".planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md"
      provides: "the Phase 140 record: measurements, reconciliation, named divergences, gate evidence index, Phase 141/144/146 hand-offs"
      contains: "Intel"
      min_lines: 120
    - path: ".planning/REQUIREMENTS.md"
      provides: "TABLE-01..05 marked complete with their traceability rows updated"
      contains: "TABLE-05"
  key_links:
    - from: ".planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md"
      to: ".planning/phases/140-parameter-table/140-PREDICTIONS.md"
      via: "reconciliation against the prediction commit SHA recorded before any measurement"
      pattern: "140-PREDICTIONS"
    - from: ".planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md"
      to: "firestarter/tests/golden/eprom_params_citations.json"
      via: "the phase record points at the machine-readable attribution rather than restating it"
      pattern: "eprom_params_citations"
---

<objective>
Close Phase 140: take the cold firmware measurement the phase predicted, reconcile it against the
prediction committed in plan 140-01, record the two named divergences and the gate evidence, and
mark TABLE-01 through TABLE-05 complete — once, here, and nowhere else.

Purpose: a ~0 AVR flash delta from a garbage-collected table looks identical to "we forgot to add
the table". The prediction was committed first precisely so this measurement is evidence rather than
a surprise, and Phase 144 / TEST-08 reconciles a prediction rather than discovering one.

Output: `.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md`, plus the requirement
checkboxes in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-parameter-table/140-CONTEXT.md
@.planning/phases/140-parameter-table/140-VALIDATION.md
@.planning/phases/138-preconditions-baseline/138-BASELINE.md
</context>

<repository>
This is a meta-repo with two submodules that are NOT committed to it. All three repos are on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`.

- **Commits land in `/workspaces` (the meta repo) only.** The record and the requirement checkboxes
  are meta-repo artifacts.
- `firestarter/` is entered **read-only** in this plan, for the cold build and test capture. No
  commit may land there. `git -C /workspaces/firestarter status --porcelain` must be empty at the
  end (build output lives under the git-ignored `.pio/`).
- `firestarter_app/` is entered **read-only**, for the suite re-run. No commit may land there.
</repository>

<tasks>

<task type="auto">
  <name>Task 1: Cold AVR + native capture, both gate scripts, and the full regression pin set</name>
  <files>.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md</files>
  <read_first>
    - `/workspaces/.planning/phases/138-preconditions-baseline/138-06-FIRMWARE-MEASUREMENT.md`
      lines 55-200 — the exact cold-capture sequence and the verbatim gate invocations and PASS-line
      shapes to reproduce
    - `/workspaces/.planning/phases/140-parameter-table/140-PREDICTIONS.md` — written and committed
      by plan 140-01; the predictions P1-P5 this task reconciles against
    - `/workspaces/firestarter/scripts/baseline/size_baseline_v131.json` —
      `meta.deltas_vs_base01` carries the operative MERGE-05 headroom (+22/64 B `uno`,
      +28/64 B `uno328pb`, -56 B `leonardo` must-not-grow, RAM delta exactly 0)
    - `/workspaces/.planning/phases/140-parameter-table/140-VALIDATION.md` § Baselines that must not
      move, and § Manual-Only Verifications
  </read_first>
  <action>
Take the full cold measurement for Phase 140 and run every gate this phase must leave unmoved.

**Cold discipline (mandatory):** for each env, `rm -rf .pio/build/<env>` then a **single**
uninterrupted invocation at a `>= 540 s` timeout, logging to a distinct file. Warm builds under-count
badly — native warm 998 warnings versus cold 1166 — and produce a false pass.

Capture, in this order:
1. `pio run -e uno`, `pio run -e uno328pb`, `pio run -e leonardo` — cold, one log each.
2. `pio test -e native`, `pio test -e native_nodevtools` — cold, one log each.
3. `pio test -e native_trace_v131`, `pio test -e native_params_v131` — cold, one log each.

Then run:
- `check_size_baseline.py` on the **default seam** with the three AVR logs and the two pinned native
  logs (no `--baseline` override) — this is the load-bearing AVR-invisibility run.
- `check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json`
  with the three AVR logs — the MERGE-05 band run. RAM delta must be exactly 0 on all three targets.
- `check_build_warnings.py` with the two pinned native logs — expect exactly 1166 each.
- `check_build_warnings.py` with the three AVR logs — expect `macro_redefinition=0` on each.
- Both repositories' Python suites.

**Never pass `native_params_v131` or `native_trace_v131` to either check script.**
`check_size_baseline.py` hardcodes `NATIVE_ENVS = ("native", "native_nodevtools")` and
`compare_native` does a bare dict lookup, so an unknown env raises an uncaught `KeyError` (exit 1) —
a false regression signal, not the documented exit-2 (F-138-05). `check_build_warnings.py` exits 2
for want of a baseline entry. Both live gates are blind to those two envs by construction; their
counts are recorded by hand instead.

**Never modify `scripts/baseline/size_baseline.json` or `size_baseline_v131.json`.** Phase 138
created the `_v131` sibling precisely so nothing moves the live baseline before Phase 144.

Record every command line and its verbatim output in a scratch note for Task 2 to fold into the
record. If any measurement contradicts a prediction in `140-PREDICTIONS.md`, record the
contradiction verbatim and STOP — do not adjust the prediction to match.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && for e in uno uno328pb leonardo; do rm -rf ".pio/build/$e"; timeout 900 pio run -e "$e" > "/tmp/140-07-$e.log" 2>&1; echo "$e exit=$?"; done</automated>
    <automated>cd /workspaces/firestarter && for e in native native_nodevtools native_trace_v131 native_params_v131; do rm -rf ".pio/build/$e"; timeout 900 pio test -e "$e" > "/tmp/140-07-$e.log" 2>&1; echo "$e exit=$?"; grep -Eo '[0-9]+ test cases: [0-9]+ succeeded' "/tmp/140-07-$e.log" | tail -1; done</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --avr-log uno=/tmp/140-07-uno.log --avr-log uno328pb=/tmp/140-07-uno328pb.log --avr-log leonardo=/tmp/140-07-leonardo.log --native-log native=/tmp/140-07-native.log --native-log native_nodevtools=/tmp/140-07-native_nodevtools.log; echo "default_seam_exit=$?"</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/140-07-uno.log --avr-log uno328pb=/tmp/140-07-uno328pb.log --avr-log leonardo=/tmp/140-07-leonardo.log; echo "merge05_exit=$?"</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_build_warnings.py --log native=/tmp/140-07-native.log --log native_nodevtools=/tmp/140-07-native_nodevtools.log; echo "warn_native_exit=$?"; python3 scripts/check_build_warnings.py --log uno=/tmp/140-07-uno.log --log uno328pb=/tmp/140-07-uno328pb.log --log leonardo=/tmp/140-07-leonardo.log; echo "warn_avr_exit=$?"</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | tail -3; cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain; git diff --quiet -- scripts/baseline/ src/proms/eprom.cpp && echo BASELINES_AND_EPROM_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - All three `pio run` AVR builds exit 0; all four `pio test` envs exit 0.
    - `pio test -e native` and `pio test -e native_nodevtools`: **141 test cases, 141 succeeded,
      17 test suites** each.
    - `pio test -e native_trace_v131`: **5 / 5 / 1**. `pio test -e native_params_v131`: **9 / 9 / 1**.
    - `check_size_baseline.py` default seam exits **0** with a PASS line naming all five envs.
    - `check_size_baseline.py --policy merge05 --baseline .../size_baseline_base01.json` exits **0**,
      and its PASS line shows **RAM delta `[=]` on all three AVR targets** and flash inside the
      64 B band on `uno`/`uno328pb` and non-positive on `leonardo`.
    - `check_build_warnings.py` on the two pinned native envs exits 0 with `total warnings=1166` for
      both; on the three AVR envs it exits 0 with `macro_redefinition=0` for each.
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` reports **244 passed**
      (227 baseline + 7 from plan 140-02 + 10 from plan 140-05).
    - `cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q` reports
      **1547 passed** (1539 baseline + 8 from plan 140-03).
    - Neither check script was invoked with `native_params_v131` or `native_trace_v131`.
    - `git -C /workspaces/firestarter diff --quiet -- scripts/baseline/ src/proms/eprom.cpp` exits 0,
      and `git -C /workspaces/firestarter status --porcelain` is empty.
  </acceptance_criteria>
  <done>Every pinned baseline is measured cold and proved unmoved, and the AVR flash/RAM figures
  needed to reconcile the phase's prediction are captured.</done>
</task>

<task type="auto">
  <name>Task 2: Write 140-PARAM-TABLE-RECORD.md — reconciliation, divergences, gate evidence, hand-offs</name>
  <files>.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md</files>
  <read_first>
    - `/workspaces/.planning/phases/138-preconditions-baseline/138-BASELINE.md` — the house shape
      for a phase record: numbered sections, a findings register with owners, an explicit
      "what this is and is not" section, and a hand-off table
    - `/workspaces/.planning/phases/140-parameter-table/140-PREDICTIONS.md` — the predictions and
      their commit SHA
    - `/workspaces/.planning/phases/140-parameter-table/140-01-SUMMARY.md` through `140-06-SUMMARY.md`
      — the six plan summaries whose evidence this record indexes
    - `/workspaces/firestarter/tests/golden/eprom_params_citations.json` — pointed at, not restated
    - `/workspaces/.planning/phases/140-parameter-table/140-CONTEXT.md` § specifics — the four things
      CONTEXT requires this record to say
  </read_first>
  <action>
Write `.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md`. Sections, in order:

1. **What shipped** — the six-column, three-row table; the accessor's fail-closed contract; the
   fifth native env; the three new gates; the two corrected documents. One line each, each with its
   file path.
2. **The row values as shipped** — the 3x6 grid, plus a pointer to
   `firestarter/tests/golden/eprom_params_citations.json` for the per-cell attribution. Do not
   restate the citations; the sidecar is the machine-readable authority.
3. **Named divergence 1 — `0x07 overprogram_factor = 0`.** State the operator decision of
   2026-08-09 and its three bases (behaviour-preserving against `eprom.cpp:161-178`; all three
   `0x07` datasheets read specify no overprogram, covering 113 of 170 chips; D-06's precedent that a
   primary datasheet beats `PROJECT.md`'s derived throughput table). Then state the **scoped
   divergence** plainly: `PROJECT.md`'s throughput table implies `3`, and the 22 Intel-family 1 ms
   parts on this row genuinely want a `3 x N` margin pulse. Serving them correctly requires
   splitting `0x07`, which TABLE-05's single-dispatch-key constraint forbids. Record it as a
   **Phase 146 follow-up candidate** with F-140-05 as its evidence.
4. **Named contradiction — `0x08 overprogram_factor = 0`.** State plainly that `PROJECT.md`'s prose
   and `PROJECT.md`'s own throughput table disagreed, which one won and why: three independent
   vendors (ST M27C1001 §2.6 verbatim, AMD Am27C020 Flashrite, Winbond W27C020) specify no
   overprogram, so the prose wins under D-06's tie-break. Naming the contradiction is the
   requirement; smoothing it over is the failure mode.
5. **The `overprogram_cap_us` column is inert on every row.** 75000 uniformly; TABLE-01 names the
   column so it ships; it is cited as reasoned and explicitly inert. Note that D-08's phrase "both
   overprogramming rows" presupposed two such rows and research found none.
6. **Prediction versus measurement.** Quote each of P1-P5 from `140-PREDICTIONS.md`, name that
   file's commit SHA and commit timestamp, and put the cold measurement beside it. State explicitly
   that the AVR flash delta is ~0 **because the table is unreferenced by `src/` and the AVR link
   passes `-ffunction-sections -fdata-sections -Wl,--gc-sections`** (F-140-02), that the real flash
   cost — 3 rows x 12 B PROGMEM plus the accessor — lands in Phase 141 funded by LOOP-02's removals,
   and that a ~0 delta here is the predicted outcome rather than a missing table.
7. **Suite and env counts** — a table of every env and suite with its count:
   `native` 141/17, `native_nodevtools` 141/17, `native_trace_v131` 5/1, `native_params_v131` 9/1,
   firmware `pytest tests/` 227 -> 244, app `pytest tests/` 1539 -> 1547. State explicitly that
   `native_params_v131` and `native_trace_v131` run in **no CI leg of either repository**
   (F-140-11), so their counts are local run-by-name obligations (D-11) and not CI coverage.
8. **Gate evidence index** — for each of the three new gates, its path, its test count, and a
   pointer to the plan SUMMARY carrying its verbatim planted-RED transcripts. State the total number
   of RED runs recorded (3 + 4 + 5 = 12) and that no gate's GREEN was believed before its RED.
9. **What is NOT verified by this phase — stated, not implied.**
   - No bench oracle exists for TABLE-03: 0 of 329 shipped 27C chips yields `pulse_delay == 0`
     (F-140-04), so no bench run can reach the fallback. The native suite is the only oracle, and
     Phase 145's bench work covers the loop, not this branch.
   - `0x07`'s `overprogram_factor` is not validatable by any test in this phase — the table is
     unreferenced by `src/` (D-10). It is a data-correctness question settled by attribution and an
     operator decision, not by an assertion.
   - The ~6.25 V program-VCC and the datasheets' raised-VCC verify passes are unreachable on this
     hardware. This is the milestone's evidence ceiling and is why D-02 forbids a verify-VCC column.
   - `check_size_baseline.py` and `check_build_warnings.py` are blind to `native_params_v131`
     (F-138-05, inherited and accepted, owner `henols`).
10. **Findings register** — F-140-05 (the `0x07` family split), F-140-07 (the wrong published
    `energy_cap_us` justification: the value has a primary basis, the reason does not; the TI TMS
    2516 total for all bits is 100 seconds, not 50 ms), F-140-09 (`doc/PROTOCOLS.md` contradicted
    the datasheets it cited — corrected in plan 140-06), and the correction this phase made to
    `140-RESEARCH.md` Pitfall 5's three-site branch inventory (measured at roughly twenty
    pre-existing handle-field predicates, which is why D-13's gate pins a two-tier inventory rather
    than forbidding a class of branch). Each with an owner and a consuming phase.
11. **Hand-offs** — Phase 141 (the loop that reads all six columns; the flash cost lands there),
    Phase 142 (`vpp_path`'s consumers and the duplicated VPP branch at `eprom.cpp:145`/`:218`),
    Phase 144 / TEST-08 (reconciles the total flash delta against section 6's prediction; TEST-06
    diffs the frozen trace), Phase 146 / CLOSE-04 (reconciles F-140-07 against the posted gh#15
    text — this phase did not edit it).
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md && grep -c 'F-140-05\|F-140-07\|F-140-09\|F-138-05\|F-140-11\|F-140-04\|F-140-02' .planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md</automated>
    <automated>cd /workspaces && for s in 'Intel' 'throughput table' 'native_params_v131' '140-PREDICTIONS' 'eprom_params_citations' '100 seconds' 'no CI leg'; do printf '%s: ' "$s"; grep -c "$s" .planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md; done</automated>
    <automated>cd /workspaces && grep -Ec '^## ' .planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md</automated>
  </verify>
  <acceptance_criteria>
    - The record exists with all eleven numbered sections present as `## ` headings.
    - Section 3 names the 22 Intel-family parts, states that `PROJECT.md`'s throughput table implies
      `3`, and records the split as a Phase 146 follow-up candidate — the divergence is visible, not
      dropped.
    - Section 4 states in words that `PROJECT.md`'s prose and its own throughput table disagreed,
      and which won and why.
    - Section 6 quotes P1-P5, names `140-PREDICTIONS.md`'s commit SHA, puts the cold measurement
      beside each, and explains the ~0 flash delta mechanically (`--gc-sections`, F-140-02).
    - Section 7's table carries all six counts and the explicit no-CI-coverage statement for the two
      non-CI envs.
    - Section 8 accounts for **12** planted RED runs across the three gates and points at the plan
      SUMMARY holding each verbatim transcript.
    - Section 9 lists all four not-verified items explicitly, including "no bench oracle exists for
      TABLE-03".
    - Section 10 includes F-140-07's "100 seconds" correction and the Pitfall-5 branch-inventory
      correction.
    - The record points at `firestarter/tests/golden/eprom_params_citations.json` rather than
      restating the citations.
  </acceptance_criteria>
  <done>The phase record names both divergences, reconciles the prediction against the measurement,
  indexes the gate evidence, and states what this phase does not verify.</done>
</task>

<task type="auto">
  <name>Task 3: Mark TABLE-01..05 complete — once, here — with snapshot-and-diff discipline</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `/workspaces/.planning/REQUIREMENTS.md` lines 167-180 (the Parameter Table checkboxes) and
      lines 300-312 (the Traceability table rows for TABLE-01..05)
    - `/workspaces/.planning/ROADMAP.md` lines 175-185 (the phase checklist) and lines 360-370 (the
      Traceability table rows)
    - `/workspaces/.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md` — written in
      Task 2; the evidence each checkbox rests on
    - `/workspaces/.planning/phases/139-gh-15-correction-outward/139-05-SUMMARY.md` — how the prior
      phase recorded its requirement closure
  </read_first>
  <action>
Mark TABLE-01, TABLE-02, TABLE-03, TABLE-04 and TABLE-05 complete. **This is the only plan in
Phase 140 permitted to do so** — plans 140-01 through 140-06 each state "may mark Complete: NONE".

Before editing, snapshot both files (`cp` to the scratch directory). After editing, diff against the
snapshots and confirm the change set is exactly: five `- [ ]` to `- [x]` flips in
`REQUIREMENTS.md` § "Parameter Table"; five `Pending` to `Complete` flips in `REQUIREMENTS.md`'s
Traceability table; five `Pending` to `Complete` flips in `ROADMAP.md`'s Traceability table; and one
`- [ ] **Phase 140: Parameter Table**` to `- [x]` with a `(completed YYYY-MM-DD)` suffix in
`ROADMAP.md`'s phase checklist. Nothing else may move — in particular, no other phase's
`**Plans:**` line and no other requirement's row. A prior tool in this project has been observed
clobbering an unrelated phase's `**Plans:**` line, so diff rather than trust.

For each of the five, append the evidence pointer used, in one line, to the plan SUMMARY (not to
`REQUIREMENTS.md`): TABLE-01 — the header/TU plus `native_params_v131` cases 7 and 9 plus the
citations gate's field-name freeze; TABLE-02 — the citations gate's no-pulse-column test plus
`native_trace_v131` still GREEN; TABLE-03 — `native_params_v131` cases 1-6, with the explicit note
that no bench oracle exists; TABLE-04 — the 18-cell sidecar and its coverage gate, with five planted
RED runs; TABLE-05 — the two split gates, with three and four planted RED runs respectively.

Do NOT touch any `LOOP-*`, `VPP-*`, `HOST-*`, `TEST-*`, `BENCH-*` or `CLOSE-*` row. In particular
**TEST-01 belongs to Phase 144** even though `native_params_v131` case 9 proves part of its content.

Do NOT edit `.planning/PROJECT.md` — its contradiction is named in the phase record, not rewritten.
  </action>
  <verify>
    <automated>cd /workspaces && grep -n 'TABLE-0' .planning/REQUIREMENTS.md | head -20</automated>
    <automated>cd /workspaces && grep -c '^- \[x\] \*\*TABLE-0' .planning/REQUIREMENTS.md; grep -Ec '\| TABLE-0[1-5] \| Phase 140 \| Complete \|' .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
    <automated>cd /workspaces && git diff --stat -- .planning/REQUIREMENTS.md .planning/ROADMAP.md && git diff -- .planning/ROADMAP.md | grep -E '^[-+].*\*\*Plans:\*\*' | head</automated>
    <automated>cd /workspaces && git diff -- .planning/REQUIREMENTS.md .planning/ROADMAP.md | grep -Ec '^[-+]' </automated>
  </verify>
  <acceptance_criteria>
    - All five `TABLE-0N` checkboxes in `REQUIREMENTS.md` § "Parameter Table" read `- [x]`.
    - All five TABLE rows read `Complete` in **both** `REQUIREMENTS.md`'s and `ROADMAP.md`'s
      Traceability tables.
    - `ROADMAP.md`'s Phase 140 checklist entry reads `- [x]` with a `(completed YYYY-MM-DD)` suffix.
    - `git diff -- .planning/ROADMAP.md | grep -E '^[-+].*\*\*Plans:\*\*'` produces output for the
      Phase 140 entry **only** — no other phase's `**Plans:**` line moved.
    - The combined diff of the two files touches only the changed checkbox/status lines and the
      Phase 140 checklist line; no `LOOP-*`, `VPP-*`, `HOST-*`, `TEST-*`, `BENCH-*` or `CLOSE-*` row
      appears in the diff.
    - `.planning/PROJECT.md` is byte-unchanged (`git diff --quiet -- .planning/PROJECT.md` exits 0).
    - `git -C /workspaces/firestarter status --porcelain` and
      `git -C /workspaces/firestarter_app status --porcelain` are both empty — no commit landed in
      either submodule from this plan.
  </acceptance_criteria>
  <done>The five phase requirements are marked complete exactly once, with the evidence for each
  recorded and the surrounding roadmap provably untouched.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan owns:
- `.planning/phases/140-parameter-table/140-PARAM-TABLE-RECORD.md` — the Phase 140 record
- The TABLE-01..05 completion flips in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`

Full Phase 140 artifact list: see `140-01-PLAN.md` § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<requirement_completion>
**Requirement IDs this plan MAY mark Complete: TABLE-01, TABLE-02, TABLE-03, TABLE-04, TABLE-05 —
all five, and only these five.**

No other plan in Phase 140 may flip a checkbox. No `LOOP-*`, `VPP-*`, `HOST-*`, `TEST-*`, `BENCH-*`
or `CLOSE-*` requirement may be touched: they belong to Phases 141-146. TEST-01 in particular is
Phase 144's, even though `native_params_v131` case 9 proves part of its content.
</requirement_completion>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prediction → measurement | a measurement taken before its prediction is recorded is not evidence |
| plan → `.planning/` records | an automated edit can clobber an unrelated phase's roadmap line |
| meta repo → submodules | a plan that only reads a submodule must leave no commit there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-28 | Repudiation | a ~0 flash delta read as a missing table | mitigate | `140-PREDICTIONS.md` is committed by plan 140-01 before any measurement; section 6 of the record quotes its commit SHA beside the cold figures and gives the mechanical reason (`--gc-sections`, F-140-02). |
| T-140-29 | Tampering | `check_size_baseline.py` given an unknown env | mitigate | Neither check script is invoked with `native_params_v131` or `native_trace_v131` (F-138-05: uncaught `KeyError`, exit 1 — a false regression signal). Their counts are recorded by hand. |
| T-140-30 | Tampering | a roadmap edit clobbering an unrelated phase | mitigate | Snapshot both `.planning` files before editing and diff after; an acceptance criterion asserts that only the Phase 140 `**Plans:**` line appears in the diff. |
| T-140-31 | Repudiation | a requirement marked complete twice, or early | mitigate | Every other plan in this phase states "may mark Complete: NONE"; this plan names the five allowed IDs explicitly and forbids every other requirement family. |
| T-140-32 | Tampering | an accidental commit inside a read-only submodule | mitigate | Acceptance criteria assert `git status --porcelain` is empty in both submodules at the end of the plan. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages in either repository (`140-RESEARCH.md` § Package Legitimacy Audit). |
</threat_model>

<verification>
- `pio test -e native` / `native_nodevtools` → 141 cases / 17 suites each; `native_trace_v131` → 5/1;
  `native_params_v131` → 9/1.
- `check_size_baseline.py` default seam → exit 0; `--policy merge05 --baseline size_baseline_base01`
  → exit 0 with RAM delta `[=]` on all three AVR targets.
- `check_build_warnings.py` → 1166 on both pinned native envs; `macro_redefinition=0` on all three
  AVR envs.
- firmware `pytest tests/ -q` → 244 passed; app `pytest tests/ -o addopts="" -q` → 1547 passed.
- `git -C /workspaces/firestarter diff --quiet -- scripts/baseline/ src/proms/eprom.cpp` exits 0.
- Both submodules' `git status --porcelain` are empty.
</verification>

<success_criteria>
- The predicted AVR flash and RAM deltas are reconciled against a cold measurement taken after the
  prediction was committed, with the mechanical reason stated.
- Both named divergences — `0x07`'s Intel-family scope and `0x08`'s `PROJECT.md` contradiction — are
  visible in the phase record.
- Twelve planted RED runs across three gates are indexed, and nothing this phase does not verify is
  left implied.
- TABLE-01 through TABLE-05 are marked complete exactly once, with the rest of the roadmap provably
  untouched.
</success_criteria>

<output>
Create `.planning/phases/140-parameter-table/140-07-SUMMARY.md` when done. It MUST carry the full
gate transcript (both `check_size_baseline.py` runs and both `check_build_warnings.py` runs,
verbatim), the six suite/env counts, the prediction-versus-measurement table, and the five
requirement evidence pointers.
</output>
