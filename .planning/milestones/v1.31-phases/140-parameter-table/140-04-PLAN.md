---
phase: 140-parameter-table
plan: 04
type: execute
wave: 2
depends_on: [140-01]
files_modified:
  - firestarter/platformio.ini
  - firestarter/test/native/avr/test_eprom_params_v131/host_stubs.cpp
  - firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp
commits_land_in:
  - firestarter/          # submodule — platformio.ini and test/native/avr/
autonomous: true
requirements: [TABLE-03, TABLE-01]
must_haves:
  truths:
    - "The pulse_delay == 0 fallback is EXERCISED by a running test, not asserted in prose"
    - "pulse_delay == 0 yields 1000 us on 0x07, 100 us on 0x08 and 500 us on 0x0B"
    - "A non-zero pulse_delay survives configure_memory untouched — the negative control that stops the positive cases passing vacuously"
    - "Each of 0x07, 0x08 and 0x0B resolves to its own distinct table row, and an unknown protocol resolves to NULL"
    - "Neither pinned native env moved: native and native_nodevtools stay at 141 cases / 17 suites"
  artifacts:
    - path: "firestarter/platformio.ini"
      provides: "[env:native_params_v131] — the fifth native env, naming only its own suite"
      contains: "native_params_v131"
    - path: "firestarter/test/native/avr/test_eprom_params_v131/host_stubs.cpp"
      provides: "pure pass-through host stub TU (no opt-in recorder guards)"
      min_lines: 20
    - path: "firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp"
      provides: "9 Unity cases: 3 fallback positives, 3 negative controls, row resolution, NULL row, frozen row values"
      contains: "RUN_TEST"
      min_lines: 120
  key_links:
    - from: "firestarter/platformio.ini"
      to: "firestarter/test/native/avr/test_eprom_params_v131"
      via: "positive test_filter allowlist plus a matching -I entry — the suite is invisible without BOTH"
      pattern: "native/avr/test_eprom_params_v131"
    - from: "firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp"
      to: "firestarter/src/proms/eprom.cpp"
      via: "configure_memory -> configure_eprom -> the pulse_delay == 0 fallback switch at :71-76"
      pattern: "configure_memory"
---

<objective>
Exercise TABLE-03: prove by a running test that a protocol's constant pulse is consulted **only**
when `handle->pulse_delay == 0`. The test runs in a FIFTH dedicated PlatformIO native environment
(D-11) so neither pinned env's 141-cases/17-suites pin moves, and it also proves TABLE-01's row
resolution behaviourally.

Purpose: F-140-04 measured that **0 of the 329 shipped 27C chips** yield `pulse_delay == 0` — the
fallback is unreachable on the bench, so this native suite is the **only possible oracle** for
TABLE-03. No bench run in Phase 145 can cover it, and claiming otherwise would be false.

Output: `[env:native_params_v131]` in `platformio.ini`, plus the
`test/native/avr/test_eprom_params_v131/` suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-parameter-table/140-CONTEXT.md
@.planning/phases/140-parameter-table/140-PATTERNS.md
@.planning/phases/140-parameter-table/140-VALIDATION.md
@firestarter/CLAUDE.md
</context>

<repository>
This is a meta-repo. `firestarter/` is a git submodule that is NOT committed to the meta-repo.
All three files are committed **inside** `/workspaces/firestarter`, on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`. Verify with
`git -C /workspaces/firestarter branch --show-current` before committing.

**Wave 2 runs three plans concurrently inside this same submodule** (140-02, 140-04, 140-05). Their
`files_modified` sets do not overlap, but the git index is shared. Stage and commit **only** the
paths named in this plan's `files_modified` — never `git add -A`, never `git commit -a`, and never a
commit helper that stages everything, or you will sweep a sibling plan's in-flight work into your
commit. If `git status --porcelain` shows a path this plan does not own, leave it alone.
</repository>

<hard_constraints>
1. **Never fold this suite into `[env:native]` or `[env:native_nodevtools]`.** Both are asserted at
   **exactly 141 cases / 17 suites** by the live `scripts/baseline/size_baseline.json` through
   `check_size_baseline.py`'s `compare_native`, whose default mode is strict byte-identity. Adding
   one case to either turns a live gate RED. `firestarter/CLAUDE.md` § "Reuse pattern for future
   native tests" actively instructs adding a new suite to **both** pinned envs — **D-11 overrides
   that instruction here**, and plan 140-06 records the exception in `CLAUDE.md`.
2. **Never add `native_params_v131` to `default_envs`** (`platformio.ini:16`). `pio run` would try
   to link a `main()`-less target.
3. **Never pass `native_params_v131` to `check_size_baseline.py`** — `NATIVE_ENVS` is hardcoded and
   `compare_native` does a bare dict lookup, so an unknown env raises an **uncaught KeyError,
   exit 1** — a false regression signal, not the documented exit-2 (F-138-05). **Never pass it to
   `check_build_warnings.py`** either — it handles the case cleanly (exit 2) but has no baseline
   entry.
4. **`pio test -e native_params_v131` runs in NO CI leg of either repository.** `build.yml:142,155`
   and `beta-build.yml:122,128` run only `native` and `native_nodevtools` (F-140-11). This suite is
   a **run-by-name local obligation**; its pass/fail counts must be recorded in this plan's SUMMARY
   and again in the phase record (D-11). Do not write anything implying CI covers it.
5. **`src/proms/eprom.cpp` stays byte-unchanged** (D-10). This plan calls into it; it does not edit
   it.
6. A `void test_*(void)` function that is not named in a `RUN_TEST(...)` line **never runs and never
   fails**. The reported case count must equal the number of `RUN_TEST` lines.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add [env:native_params_v131] and the pass-through host stub TU</name>
  <files>firestarter/platformio.ini, firestarter/test/native/avr/test_eprom_params_v131/host_stubs.cpp</files>
  <read_first>
    - `/workspaces/firestarter/platformio.ini` lines 293-329 — `[env:native_trace_v131]`, the
      complete worked fourth-env instance to copy **including its HARD CONSTRAINT comment block**;
      note the continuation lines are **tab**-indented, not space-indented
    - `/workspaces/firestarter/platformio.ini` lines 11-16 — `default_envs`, which must NOT change
    - `/workspaces/firestarter/platformio.ini` lines 102-141 — `[env:native]`'s 17-entry
      `test_filter` and its `-I` list, both of which must NOT change
    - `/workspaces/firestarter/test/native/avr/test_not_implemented/host_stubs.cpp` (whole file) —
      the pure pass-through stub TU to copy; Pitfall 6 says this suite needs none of the three
      opt-in recorder layers
    - `/workspaces/firestarter/test/native/avr/_shared/host_stubs_common.inc` lines 22-32 and 50-60
      — the opt-in guard documentation, for reference only; do NOT define any guard here
    - `/workspaces/.planning/phases/140-parameter-table/140-PATTERNS.md` § 3 and § 5
  </read_first>
  <action>
Append `[env:native_params_v131]` to `firestarter/platformio.ini`, immediately after
`[env:native_trace_v131]`, as a structural copy of that env with these substitutions:
`test_filter` names **only** `native/avr/test_eprom_params_v131` (one entry, not eighteen);
`build_flags` is `${env:native.build_flags}` plus `-I test/native/avr/test_eprom_params_v131`;
`lib_deps` is `fabiobatsilva/ArduinoFake@^0.4.0`; `build_src_filter` and `test_build_src` are copied
unchanged; `platform = native` and `test_framework = unity`. Use tab-indented continuation lines to
match the file.

Copy the HARD CONSTRAINT comment block from `[env:native_trace_v131]` and adapt it, keeping all four
of its warnings intact and re-stated for this env: never folded into either pinned env's
`test_filter`; never added to `default_envs`; never fed to `check_size_baseline.py` (uncaught
KeyError, exit 1, F-138-05); never fed to `check_build_warnings.py` (exit 2, no baseline entry).
Add one further line stating that this env runs in **no CI leg of either repository** (F-140-11), so
its counts are a run-by-name obligation recorded in the phase record (D-11). The comment block is
not decoration — it is what stops a later phase folding this env into a pinned one.

`${env:native.build_flags}` inherits `-std=gnu++17`, `-I include` and every existing suite `-I`
entry, so `include/eprom_params.h` is already on the include path via `-I include`.

Create `firestarter/test/native/avr/test_eprom_params_v131/host_stubs.cpp` as a copy of
`test/native/avr/test_not_implemented/host_stubs.cpp`: the house banner (updated to
`Phase 140 Plan 04 -- host stub TU for the test_eprom_params_v131 suite`), the three stdlib
includes, the `extern "C"` block including `rurp_shield.h` and `rurp_types.h`, and a single
`#include "../_shared/host_stubs_common.inc"`. Define **no** `HOST_STUBS_*` opt-in guard — the
suite calls `configure_memory` / `configure_eprom` only, which perform no hardware I/O beyond
`mem_util_set_address(handle, 0)`.

Do not touch `[env:native]`, `[env:native_nodevtools]`, `[env:native_pinmap_provisional]`,
`[env:native_trace_v131]`, `default_envs`, or any AVR env.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -q '^\[env:native_params_v131\]' platformio.ini && grep -A40 '^\[env:native_params_v131\]' platformio.ini | grep -q 'native/avr/test_eprom_params_v131' && grep -c 'test_eprom_params_v131' platformio.ini</automated>
    <automated>cd /workspaces/firestarter && grep -n 'default_envs' platformio.ini | grep -q 'default_envs = uno, uno328pb, leonardo' && ! sed -n '/^\[env:native\]/,/^\[env:native_nodevtools\]/p' platformio.ini | grep -q 'test_eprom_params_v131' && ! sed -n '/^\[env:native_nodevtools\]/,/^\[env:native_pinmap_provisional\]/p' platformio.ini | grep -q 'test_eprom_params_v131' && echo PINNED_ENVS_UNTOUCHED</automated>
    <automated>cd /workspaces/firestarter && test -f test/native/avr/test_eprom_params_v131/host_stubs.cpp && ! grep -q 'HOST_STUBS_' test/native/avr/test_eprom_params_v131/host_stubs.cpp && grep -q '_shared/host_stubs_common.inc' test/native/avr/test_eprom_params_v131/host_stubs.cpp && echo STUB_IS_PASSTHROUGH</automated>
    <automated>cd /workspaces/firestarter && git diff --stat -- platformio.ini</automated>
  </verify>
  <acceptance_criteria>
    - `platformio.ini` contains a `[env:native_params_v131]` section whose `test_filter` has exactly
      one entry, `native/avr/test_eprom_params_v131`.
    - Its `build_flags` contains `${env:native.build_flags}` and
      `-I test/native/avr/test_eprom_params_v131`.
    - The section carries a comment block naming all five constraints: not in either pinned env's
      `test_filter`; not in `default_envs`; not passed to `check_size_baseline.py`; not passed to
      `check_build_warnings.py`; and not run by any CI leg.
    - `default_envs` is still exactly `uno, uno328pb, leonardo`.
    - The string `test_eprom_params_v131` appears in `platformio.ini` **only** inside the new
      section (`grep -c` equals the number of occurrences within that section; it appears zero times
      between `[env:native]` and `[env:native_nodevtools]`, and zero times between
      `[env:native_nodevtools]` and `[env:native_pinmap_provisional]`).
    - `test/native/avr/test_eprom_params_v131/host_stubs.cpp` exists, defines no `HOST_STUBS_*`
      guard, and includes `../_shared/host_stubs_common.inc`.
    - `git diff` on `platformio.ini` shows additions only — no line inside any pre-existing env was
      modified or deleted.
  </acceptance_criteria>
  <done>The fifth env exists as a positive allowlist naming only its own suite, both pinned envs are
  untouched, and the pass-through stub TU is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 9-case Unity suite exercising the fallback and the row resolution</name>
  <files>firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp</files>
  <read_first>
    - `/workspaces/firestarter/test/native/avr/test_trace_eprom_v131/test_trace_eprom_v131.cpp`
      lines 34-47 (include block), 72-100 (`setUp`/`tearDown` ArduinoFake preamble), 215-229
      (the fresh-handle factory), 378-395 (the hand-written Unity `main()` with explicit
      `RUN_TEST` calls) — the exact shapes to copy
    - `/workspaces/firestarter/src/proms/eprom.cpp` lines 41-77 — `configure_eprom` and the
      `pulse_delay == 0` fallback switch; note `0x07` reaches 1000 us through `default:`, not a
      `case 0x07:` label. READ-ONLY this phase.
    - `/workspaces/firestarter/src/proms/memory.cpp` lines 88-140 — `configure_memory`, the only
      call site that reaches `configure_eprom`, and its `mem_util_set_address(handle, 0)` bus touch
    - `/workspaces/firestarter/src/json_parser.c` lines 81-89 — proof that `json_parse` does NOT
      reset `pulse_delay`, `protocol`, `mem_size`, `vpp_mv` or `pins` (Pitfall 4: build a fresh
      zero-initialised handle per case)
    - `/workspaces/firestarter/include/eprom_params.h` and
      `/workspaces/firestarter/src/proms/eprom_params.cpp` — written by plan 140-01; the row values
      this suite asserts
    - `/workspaces/firestarter/include/firestarter.h` — `firestarter_handle_t`, `CMD_WRITE`,
      `RESPONSE_CODE_OK`, `FLAG_SKIP_BLANK_CHECK`, `FLAG_SKIP_ERASE`
  </read_first>
  <action>
Create `firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp`.

Include block copied from the trace suite: `<Arduino.h>`, `<ArduinoFake.h>`, `<unity.h>`, an
`extern "C" { #include "memory.h" }` block, `"firestarter.h"`, and `"eprom_params.h"`;
`using namespace fakeit;`. `<Arduino.h>` in a **test** TU is fine — Pitfall 1's watermark constraint
applies to `src/proms/*.cpp`, which every native env compiles; this TU is compiled only by the env
whose `test_filter` names it.

`setUp()` calls `ArduinoFakeReset()` and stubs the `Serial` write overloads, `Serial.flush`,
`delay`, `delayMicroseconds`, `millis` and `micros` — `delay` and `delayMicroseconds` are defined by
ArduinoFake rather than by the shared `.inc`, so every suite that could reach them mocks them in its
own `setUp()`. `tearDown()` is empty.

Static helper `make_handle(uint32_t protocol, uint32_t pulse_delay_us)` returning a
`firestarter_handle_t h = {};` with `h.protocol`, `h.cmd = CMD_WRITE`,
`h.response_code = RESPONSE_CODE_OK`, `h.mem_size = 2048`,
`h.ctrl_flags = FLAG_SKIP_BLANK_CHECK | FLAG_SKIP_ERASE`, and `h.pulse_delay` set from the argument.
A fresh handle per case is mandatory — the production handle is a file-scope global and
`json_parse` never resets `pulse_delay` (Pitfall 4).

Write exactly these nine cases, and name all nine in a hand-written `main()` with explicit
`RUN_TEST` calls:

1. `test_0x07_zero_pulse_delay_takes_the_1000us_fallback` — `make_handle(0x07, 0)`,
   `configure_memory(&h)`, assert `h.response_code != RESPONSE_CODE_ERROR` and
   `h.pulse_delay == 1000`.
2. `test_0x08_zero_pulse_delay_takes_the_100us_fallback` — same shape, expect `100`.
3. `test_0x0B_zero_pulse_delay_takes_the_500us_fallback` — same shape, expect `500`.
4. `test_0x07_nonzero_pulse_delay_is_left_alone` — `make_handle(0x07, 777)`, `configure_memory(&h)`,
   assert `h.pulse_delay == 777`. **This is the non-vacuity control**: without it the positive
   cases could pass on a handle that was never touched.
5. `test_0x08_nonzero_pulse_delay_is_left_alone` — same, `777`.
6. `test_0x0B_nonzero_pulse_delay_is_left_alone` — same, `777`.
7. `test_each_protocol_resolves_to_its_own_distinct_row` — `eprom_params_for` for `0x07`, `0x08`
   and `0x0B` each returns non-NULL, and the three returned pointers are pairwise distinct.
8. `test_unknown_protocol_returns_null` — `eprom_params_for(0x0C)` and `eprom_params_for(0)` both
   return NULL (D-05 fail-closed; never the `0x07` row).
9. `test_row_values_match_the_frozen_table` — read all six columns of all three rows through
   `pgm_read_byte` / `pgm_read_dword` on `&row->member` and assert the 18 values from plan 140-01's
   locked table: `0x07` = (75000, 0, 25, 0, VERIFY_PER_PULSE_PLUS_FINAL, VPP_PATH_DROP_RESISTOR);
   `0x08` = (75000, 0, 25, 0, VERIFY_PER_PULSE_PLUS_FINAL, VPP_PATH_DROP_RESISTOR);
   `0x0B` = (75000, 50000, 255, 0, VERIFY_PER_PULSE, VPP_PATH_DIRECT_VPE). Never dereference a
   PROGMEM member directly — it compiles and silently reads RAM garbage on AVR.

Use the `_MESSAGE` Unity variants with a context string naming the protocol, so a failure says which
row broke.

Add a file-header comment stating: this suite is the ONLY oracle for TABLE-03 because 0 of 329
shipped 27C chips yields `pulse_delay == 0` (F-140-04), so no bench run can reach this branch; and
that case 9 proves TEST-01's *content* but **TEST-01 is assigned to Phase 144 — do not mark it
complete here**.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/native_params_v131 && timeout 900 pio test -e native_params_v131 2>&1 | tee /tmp/140-04-params.log | tail -20</automated>
    <automated>cd /workspaces/firestarter && grep -c 'RUN_TEST(' test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp</automated>
    <automated>cd /workspaces/firestarter && grep -Eo '[0-9]+ test cases: [0-9]+ succeeded' /tmp/140-04-params.log | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RUN_TEST('` on the suite returns **9**, and the nine names are exactly those listed
      in the action.
    - `pio test -e native_params_v131` reports **9 test cases, 9 succeeded, 1 test suite**, exit 0.
      The reported case count equals the `RUN_TEST` count — a `test_*` function omitted from
      `main()` would silently never run.
    - Cases 1-3 assert `h.pulse_delay` equals 1000 / 100 / 500 after `configure_memory` on a handle
      constructed with `pulse_delay = 0`.
    - Cases 4-6 assert `h.pulse_delay == 777` — the fallback did not fire.
    - Case 8 asserts NULL for both `0x0C` and `0`.
    - Case 9 asserts all 18 cell values through `pgm_read_*` accessors, never a direct dereference.
    - `git -C /workspaces/firestarter diff --quiet -- src/proms/eprom.cpp` exits 0.
  </acceptance_criteria>
  <done>The `pulse_delay == 0` fallback is exercised, not asserted, with negative controls that make
  the positive cases non-vacuous, and the frozen row values are proved at runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the pinned envs did not move, and record the run-by-name counts</name>
  <files>firestarter/platformio.ini</files>
  <read_first>
    - `/workspaces/.planning/phases/138-preconditions-baseline/138-06-FIRMWARE-MEASUREMENT.md`
      lines 55-110 — the cold-run discipline and the case/suite figures for every env
    - `/workspaces/.planning/phases/140-parameter-table/140-VALIDATION.md` § Baselines that must not
      move, and § Manual-Only Verifications
    - `/workspaces/firestarter/scripts/baseline/size_baseline.json` — the live baseline asserting
      141/17 on both pinned envs (read only; never modify)
  </read_first>
  <action>
Prove that adding a fifth env moved nothing that was pinned, and record the counts D-11 requires.

Run each of the four native envs **cold** — `rm -rf .pio/build/<env>` then a single uninterrupted
`pio test -e <env>` at a >= 540 s timeout. Warm builds under-count badly (native warm 998 warnings
vs cold 1166) and produce a false pass.

Capture, for each env, the reported case count, success count and suite count:
`native` (expect 141 / 141 / 17), `native_nodevtools` (expect 141 / 141 / 17),
`native_trace_v131` (expect 5 / 5 / 1, GREEN — D-10's frozen fixture), and `native_params_v131`
(expect 9 / 9 / 1).

Then run `check_build_warnings.py` on the two pinned envs only and confirm exactly 1166 each.
**Do not pass `native_params_v131` or `native_trace_v131` to that script, and do not run
`check_size_baseline.py` in this plan at all** — plan 140-07 owns the AVR cold capture and the
MERGE-05 band run.

Record all four env results in this plan's SUMMARY as a table, together with the explicit statement
that `native_params_v131` and `native_trace_v131` run in **no CI leg of either repository**, so
these are local, run-by-name obligations and not CI coverage (D-11, F-140-11).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/native && timeout 900 pio test -e native > /tmp/140-04-native.log 2>&1; grep -Eo '[0-9]+ test cases: [0-9]+ succeeded' /tmp/140-04-native.log | tail -1; grep -Eo '[0-9]+ test suites' /tmp/140-04-native.log | tail -1</automated>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/native_nodevtools && timeout 900 pio test -e native_nodevtools > /tmp/140-04-nodev.log 2>&1; grep -Eo '[0-9]+ test cases: [0-9]+ succeeded' /tmp/140-04-nodev.log | tail -1</automated>
    <automated>cd /workspaces/firestarter && rm -rf .pio/build/native_trace_v131 && timeout 900 pio test -e native_trace_v131 > /tmp/140-04-trace.log 2>&1; grep -Eo '[0-9]+ test cases: [0-9]+ succeeded' /tmp/140-04-trace.log | tail -1</automated>
    <automated>cd /workspaces/firestarter && python3 scripts/check_build_warnings.py --log native=/tmp/140-04-native.log --log native_nodevtools=/tmp/140-04-nodev.log; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` and `pio test -e native_nodevtools` each report **141 test cases, 141
      succeeded, 17 test suites**, exit 0.
    - `pio test -e native_trace_v131` reports **5 test cases, 5 succeeded, 1 test suite**, exit 0.
    - `pio test -e native_params_v131` reports **9 test cases, 9 succeeded, 1 test suite**, exit 0.
    - `check_build_warnings.py --log native=... --log native_nodevtools=...` exits 0 with
      `total warnings=1166` for both envs.
    - `check_size_baseline.py` was NOT invoked in this plan, and neither check script was given
      `native_params_v131` or `native_trace_v131`.
    - `scripts/baseline/size_baseline.json` and `scripts/baseline/size_baseline_v131.json` are both
      byte-unchanged (`git diff --quiet -- scripts/baseline/` exits 0).
    - The SUMMARY carries a four-row table of env / cases / succeeded / suites, plus the explicit
      no-CI-coverage statement.
  </acceptance_criteria>
  <done>Both pinned envs are proved unmoved, the frozen trace suite is proved still GREEN, and the
  two non-CI envs' counts are on the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan owns:
- `firestarter/platformio.ini` → new PlatformIO env **`native_params_v131`** (`test_filter` =
  `native/avr/test_eprom_params_v131` only)
- `firestarter/test/native/avr/test_eprom_params_v131/host_stubs.cpp`
- `firestarter/test/native/avr/test_eprom_params_v131/test_eprom_params_v131.cpp` — 9 Unity cases:
  `test_0x07_zero_pulse_delay_takes_the_1000us_fallback`,
  `test_0x08_zero_pulse_delay_takes_the_100us_fallback`,
  `test_0x0B_zero_pulse_delay_takes_the_500us_fallback`,
  `test_0x07_nonzero_pulse_delay_is_left_alone`,
  `test_0x08_nonzero_pulse_delay_is_left_alone`,
  `test_0x0B_nonzero_pulse_delay_is_left_alone`,
  `test_each_protocol_resolves_to_its_own_distinct_row`,
  `test_unknown_protocol_returns_null`,
  `test_row_values_match_the_frozen_table`

Full Phase 140 artifact list: see `140-01-PLAN.md` § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<requirement_completion>
This plan **contributes to** TABLE-03 and TABLE-01.

**Requirement IDs this plan may mark Complete: NONE.**

TABLE-01 spans plans 140-01, 140-04 and 140-05. TABLE-03 is verified here, but its completion is
recorded together with the phase-wide regression evidence in plan 140-07 — only 140-07 may flip any
TABLE-* checkbox in `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`.

**TEST-01 belongs to Phase 144.** Case 9 proves part of its content; do not mark TEST-01, or any
other `TEST-*`, `LOOP-*` or `VPP-*` requirement, as touched by this plan.
</requirement_completion>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host JSON → `handle->pulse_delay` | a 0 does reach the firmware (`database.py:555` emits the field unconditionally) |
| test env → pinned baselines | a new suite added to the wrong env silently breaks a live gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-13 | Repudiation | a fallback test that passes vacuously | mitigate | Three negative controls (`pulse_delay = 777` survives) paired 1:1 with the three positives; a fresh zero-initialised handle per case because `json_parse` never resets `pulse_delay` (Pitfall 4). |
| T-140-14 | Repudiation | a `test_*` function omitted from `main()` | mitigate | Acceptance criterion compares the reported case count against `grep -c 'RUN_TEST('` — exactly 9. |
| T-140-15 | Tampering | the new suite folded into a pinned env | mitigate | `test_filter` names one entry; acceptance criteria assert `test_eprom_params_v131` appears zero times between the pinned env section markers; the HARD CONSTRAINT comment block records why. |
| T-140-16 | Denial of service | false regression from a blind gate | mitigate | Neither check script is given the new env name (F-138-05: uncaught `KeyError`, exit 1). |
| T-140-17 | Tampering / damage to physical asset | unknown protocol resolving to a row | mitigate | `test_unknown_protocol_returns_null` covers `0x0C` and `0`; a `0x07` fallback row would route 13 V through the drop resistor for an unknown part. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages in either repository (`140-RESEARCH.md` § Package Legitimacy Audit). Unity 2.6.1 and ArduinoFake 0.4.0 are pre-existing pinned `lib_deps` already in the frozen baseline. |
</threat_model>

<verification>
- `pio test -e native_params_v131` → 9 cases / 9 succeeded / 1 suite.
- `pio test -e native` → 141 / 141 / 17; `pio test -e native_nodevtools` → 141 / 141 / 17.
- `pio test -e native_trace_v131` → 5 / 5 / 1, GREEN.
- `check_build_warnings.py` on the two pinned envs → exactly 1166 each, exit 0.
- `git -C /workspaces/firestarter diff --quiet -- src/proms/eprom.cpp scripts/baseline/` exits 0.
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` unchanged by this plan.
</verification>

<success_criteria>
- TABLE-03's fallback is exercised by a running test in a fifth env that leaves every pinned count
  where it was.
- The suite's counts are recorded as a local run-by-name obligation, with the absence of CI coverage
  stated rather than implied.
</success_criteria>

<output>
Create `.planning/phases/140-parameter-table/140-04-SUMMARY.md` when done. It MUST carry the
four-env case/suite table, the `RUN_TEST` count, the `check_build_warnings.py` PASS line verbatim,
and the explicit statement that `native_params_v131` and `native_trace_v131` run in no CI leg.
</output>
