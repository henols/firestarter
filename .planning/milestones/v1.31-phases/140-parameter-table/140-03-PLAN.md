---
phase: 140-parameter-table
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/golden/chip_database_field_inventory.json
  - firestarter_app/tests/test_chip_database_field_inventory.py
commits_land_in:
  - firestarter_app/      # submodule — tests/ and tests/golden/
autonomous: true
requirements: [TABLE-05]
must_haves:
  truths:
    - "A committed gate, not inspection, proves chip_database.json gained no new field"
    - "The gate pins per-key OCCURRENCE COUNTS, not just key names, so a field added to a subset cannot slip through"
    - "The gate also reads the GENERATOR (tools/build_db.py), because chip_database.json is generated and never hand-edited"
    - "The gate cannot be silenced by regenerating tools/baseline/chip_database.baseline.json"
    - "The gate has been SEEN RED on planted violations before its GREEN is believed"
  artifacts:
    - path: "firestarter_app/tests/golden/chip_database_field_inventory.json"
      provides: "the frozen three-level key inventory with occurrence counts, the 27C protocol counts, and the generator's emitted-key set"
      contains: "generator_emitted_chip_entry_keys"
      min_lines: 50
    - path: "firestarter_app/tests/test_chip_database_field_inventory.py"
      provides: "the D-12 DB-half gate: 8 tests, two-level traversal, ast-based generator scan, explicit target resolution"
      contains: "def test_"
      min_lines: 170
  key_links:
    - from: "firestarter_app/tests/test_chip_database_field_inventory.py"
      to: "firestarter_app/firestarter/data/chip_database.json"
      via: "two-level traversal over {manufacturer: [chip, ...]}"
      pattern: "chip_database\\.json"
    - from: "firestarter_app/tests/test_chip_database_field_inventory.py"
      to: "firestarter_app/tools/build_db.py"
      via: "ast walk over the chip_entry dict literal and its subscript assignments"
      pattern: "build_db\\.py"
---

<objective>
Ship the `chip_database.json` half of TABLE-05 (D-12): a committed pytest gate in
`firestarter_app/tests/` that freezes the database's field inventory — names **and** per-key
occurrence counts — plus an `ast`-based check of the generator that produces it, and prove the gate
can fail before believing that it passes (D-15).

Purpose: D-12 splits TABLE-05 so each half lives in the repo it can actually see. An app-side gate
scanning firmware source is the shape that failed open four times in Phase 117; a meta-repo script
reading both working trees runs in no CI leg of either sub-repo. This half runs in
`firestarter_app`'s own CI leg (`pytest tests/ --cov=firestarter ...`).

Output: `firestarter_app/tests/golden/chip_database_field_inventory.json`,
`firestarter_app/tests/test_chip_database_field_inventory.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-parameter-table/140-CONTEXT.md
@.planning/phases/140-parameter-table/140-PATTERNS.md
@.planning/phases/140-parameter-table/140-VALIDATION.md
@firestarter_app/CLAUDE.md
</context>

<repository>
This is a meta-repo. `firestarter_app/` is a git submodule that is NOT committed to the meta-repo.
Both new files are committed **inside** `/workspaces/firestarter_app`, on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`. Verify with
`git -C /workspaces/firestarter_app branch --show-current` before committing.

This plan is **read-only** with respect to `firestarter/` and must not touch it. It must not modify
`firestarter/data/chip_database.json` — that file is GENERATED from `infoic.xml` and is never
hand-edited. Reading it to freeze its inventory is not a licence to add a column to it.
</repository>

<frozen_inventory>
Measured live this session against `firestarter_app/firestarter/data/chip_database.json`
(59 manufacturers, **746** chips). These are the exact figures the golden file must carry. If the
executor's own derivation disagrees with any of them, STOP and report — do not adjust the table.

**Top level** (11 keys): `electrical` 746, `part_number` 746, `pinout` 746, `programming` 746,
`support_status` 746, `unsupported_reason` 10, `datasheet` 2, `provenance` 2, `source` 2,
`verification_note` 2, `verification_status` 2

**`programming`** (8 keys): `algorithm` 746, `chip_id_check` 746, `chip_id_value` 746,
`pulse_duration` 746, `infoic_page_size_raw` 744, `protect_off_before` 744, `protect_on_after` 744,
`page_size` 2

**`electrical`** (7 keys): `pin_count` 746, `size_bytes` 746, `type` 746, `vcc` 746, `vdd` 746,
`vpp` 746, `vpp_mv` 746

**27C protocol chip counts:** `programming.algorithm` 7 → **170**, 8 → **127**, 11 → **32**

**Why counts and not just names:** `page_size` (2 of 746) and the five sparse top-level keys are
already subset-only fields. A names-only assertion is satisfied by a new field added to a single
chip; an occurrence-count assertion is not.

**Two-level traversal is mandatory:** the DB shape is `{manufacturer: [chip, ...]}` with 59
top-level keys. `for key in db:` enumerates **manufacturers**, not chips, and makes every downstream
assertion pass vacuously. Copy `tests/test_sdp_db_invariant.py` lines 86-99 (`_select_0x0d_chips`).
</frozen_inventory>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/golden/chip_database_field_inventory.json — the frozen inventory</name>
  <files>firestarter_app/tests/golden/chip_database_field_inventory.json</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_sdp_db_invariant.py` lines 60-110 and 260-290 — the
      DB path resolution, the two-level `{manufacturer: [chip, ...]}` traversal that stops the gate
      being vacuous, and the count-assertion message shape that explains the consequence
    - `/workspaces/firestarter/tests/golden/eprom_v131_trace_inventory.json` — the cross-repo
      `meta`-block shape to copy (`firestarter_app/tests/golden/` holds no JSON today)
    - `/workspaces/firestarter_app/tools/build_db.py` lines 700-830 — the `chip_entry = {...}`
      dict literal, the `chip_entry["unsupported_reason"] = ...` subscript assignment, the
      `chip_entry["electrical"]["vcc"] = ...` nested subscript assignment, and the
      `**({...} if ... else {})` conditional spread that carries `page_size`
    - `/workspaces/firestarter_app/firestarter/data/chip_database.json` — read enough of one chip
      object to confirm the three-level shape; do NOT modify it
  </read_first>
  <action>
Create `firestarter_app/tests/golden/chip_database_field_inventory.json`.

Derive the inventory yourself with a throwaway script in a scratch directory (never committed) that
walks `{manufacturer: [chip, ...]}` two levels deep and counts key occurrences at the top level, in
`programming` and in `electrical`. Compare against this plan's `<frozen_inventory>` table; they must
agree exactly.

Shape:
- `meta`: `source` (`firestarter/data/chip_database.json`), `generator` (`tools/build_db.py`),
  `recorded_by` ("Phase 140 Plan 03"), `requirement` ("TABLE-05"), `decision` ("D-12"),
  `recorded_at_head`,
  `why_counts_not_names` — a field added to a subset, as `page_size` and the five sparse top-level
  keys already are, slips past a names-only assertion;
  `why_not_diff_db` — `tools/diff_db.py`'s GATE-02 already unions both key sets and would see a new
  field, but it compares against `tools/baseline/chip_database.baseline.json`, which regenerating
  silences; this inventory is the non-silenceable half and is deliberately NOT derived from that
  baseline;
  `how_to_update` — re-derive with an independent traversal, never hand-edit a count to make a
  surprise disappear, and state in the commit message which key changed and why;
  `db_is_generated` — one sentence stating that `chip_database.json` is produced by
  `tools/build_db.py` from `infoic.xml` and is never hand-edited, which is why this gate also reads
  the generator.
- `totals`: `manufacturers` 59, `chips` 746.
- `levels`: an object with three entries `top`, `programming`, `electrical`, each mapping key name
  to occurrence count, exactly as in `<frozen_inventory>`.
- `protocol_chip_counts`: `{"7": 170, "8": 127, "11": 32}`.
- `generator_emitted_chip_entry_keys`: the sorted union of every chip-entry key
  `tools/build_db.py` can emit — derive it with an `ast` walk (see Task 2's rule), review it by
  hand, and confirm it is exactly the union of the three `levels` key sets (26 names). If the walk
  finds a name the database does not carry, or misses one it does, record the discrepancy and STOP.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 - <<'PY'
import collections, json
g = json.load(open("tests/golden/chip_database_field_inventory.json"))
db = json.load(open("firestarter/data/chip_database.json"))
top = collections.Counter(); prog = collections.Counter()
elec = collections.Counter(); alg = collections.Counter(); n = 0
for _m, chips in db.items():
    for c in chips:
        n += 1
        top.update(c.keys()); prog.update(c["programming"].keys())
        elec.update(c["electrical"].keys()); alg[c["programming"]["algorithm"]] += 1
assert g["totals"] == {"manufacturers": len(db), "chips": n}, g["totals"]
assert g["levels"]["top"] == dict(top), "top mismatch"
assert g["levels"]["programming"] == dict(prog), "programming mismatch"
assert g["levels"]["electrical"] == dict(elec), "electrical mismatch"
assert g["protocol_chip_counts"] == {"7": 170, "8": 127, "11": 32}, g["protocol_chip_counts"]
names = set(g["levels"]["top"]) | set(g["levels"]["programming"]) | set(g["levels"]["electrical"])
assert set(g["generator_emitted_chip_entry_keys"]) == names, sorted(
    names ^ set(g["generator_emitted_chip_entry_keys"]))
print("GOLDEN_OK chips=%d keys=%d" % (n, len(names)))
PY</automated>
    <automated>cd /workspaces/firestarter_app && git diff --quiet -- firestarter/data/chip_database.json && echo DB_UNMODIFIED</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tests/golden/chip_database_field_inventory.json` parses as JSON.
    - `totals` is `{"manufacturers": 59, "chips": 746}`.
    - `levels.top`, `levels.programming` and `levels.electrical` equal the live per-key occurrence
      counters exactly, key for key and count for count (11 / 8 / 7 keys respectively).
    - `protocol_chip_counts` is `{"7": 170, "8": 127, "11": 32}`.
    - `generator_emitted_chip_entry_keys` is a sorted list of exactly the 26 names in the union of
      the three levels.
    - `meta` carries `why_counts_not_names`, `why_not_diff_db`, `how_to_update` and
      `db_is_generated`, each non-empty.
    - `git -C /workspaces/firestarter_app diff --quiet -- firestarter/data/chip_database.json`
      exits 0 — the database itself was not touched.
  </acceptance_criteria>
  <done>The frozen field inventory is committed with per-key occurrence counts, the 27C protocol
  counts, and the generator's emitted-key set.</done>
</task>

<task type="auto">
  <name>Task 2: Author tests/test_chip_database_field_inventory.py — the 8-test DB-half gate</name>
  <files>firestarter_app/tests/test_chip_database_field_inventory.py</files>
  <read_first>
    - `/workspaces/firestarter_app/tests/test_sdp_db_invariant.py` — the two-level traversal and
      the count-assertion message shape that explains the consequence of a change
    - `/workspaces/firestarter/tests/test_golden_trace_identity_eprom_v131.py` — the cross-repo
      gate pattern: explicit target resolution, non-vacuity floor, first-divergence reporting,
      startswith-based skip-bypass self-scan
    - `/workspaces/firestarter_app/tests/conftest.py` lines 70-105 — `collect_ignore` is ARMED
      (pyusb only); Assumption A6 is why this module must prove it is collected
    - `/workspaces/firestarter_app/pyproject.toml` lines 100-125 — `target-version = "py39"`,
      `line-length = 88`, `addopts = "-ra -q"`, `testpaths = ["tests"]`
    - `/workspaces/firestarter_app/.github/workflows/ci.yml` lines 75-95 — `ruff check`,
      `ruff format --check` and `check_mypy_watermark.py` all cover `tests/`
    - `/workspaces/firestarter_app/tools/check_mypy_watermark.py` lines 40-60 and 110-120 — mypy
      runs on `firestarter/ tests/`, so this module IS type-checked
    - `/workspaces/firestarter_app/tools/build_db.py` lines 700-830 — the ast targets
  </read_first>
  <action>
Create `firestarter_app/tests/test_chip_database_field_inventory.py`. Stdlib and pytest only.
Python 3.9-compatible syntax, 88-column lines, fully type-annotated (mypy runs on `tests/`).

Module docstring in the house shape: banner, `Phase 140 Plan 03 -- TABLE-05 (D-12)`,
`Requirements: TABLE-05`, a `Defect class this closes:` line naming "a new `chip_database.json`
field entering through the generator or through a subset of chips, invisible to a names-only or
regenerate-to-silence check", a numbered `Coverage:` list naming all eight tests, and an
`Environment seams:` section documenting `FIRESTARTER_CHIP_DB_JSON` and
`FIRESTARTER_BUILD_DB_SOURCE`, that they bind at import, that they exist only for planted-violation
runs, and that test 7 ignores them by construction.

Module-level resolution:
`_HERE = Path(__file__).resolve().parent`; `_APP_ROOT = _HERE.parent`;
`_DB_REL = "firestarter/data/chip_database.json"`; `_GEN_REL = "tools/build_db.py"`;
`_GOLDEN = _HERE / "golden" / "chip_database_field_inventory.json"`;
`_DB_PATH = Path(os.environ.get("FIRESTARTER_CHIP_DB_JSON", _APP_ROOT / _DB_REL))`;
`_GEN_PATH = Path(os.environ.get("FIRESTARTER_BUILD_DB_SOURCE", _APP_ROOT / _GEN_REL))`.

Helper `_walk(db)` returns manufacturer count, chip count, and the top / `programming` /
`electrical` / `algorithm` counters using the two-level traversal. It must raise — never return
empty counters silently — if the loaded object is not a mapping of manufacturer name to a list of
chip objects.

Helper `_generator_chip_entry_keys(source_text)` — `ast.parse` the generator, then collect
chip-entry key literals from: (a) every `ast.Assign` whose target is a `Name` called `chip_entry`,
recursing into its `Dict` value including nested `Dict` values **and** into both branches of any
`ast.IfExp` reached through a `**` spread (a `Dict` key of `None`); and (b) every `ast.Assign`
whose target is a `Subscript` chain rooted at a `Name` called `chip_entry`, taking each constant
subscript index. This must handle the `page_size` conditional spread and the
`chip_entry["electrical"]["vcc"]` nested subscript — both exist in the current generator and a walk
that misses them under-collects.

The eight tests:
1. `test_top_level_field_inventory_matches` — live top-level counter equals `levels.top`; on
   mismatch the message names added, removed and count-changed keys separately.
2. `test_programming_field_inventory_matches` — same for `levels.programming`.
3. `test_electrical_field_inventory_matches` — same for `levels.electrical`.
4. `test_27c_protocol_chip_counts_match` — `programming.algorithm` counts for 7, 8 and 11 equal
   170, 127 and 32; the message states that a count change invalidates every per-protocol figure
   this milestone cites (F-140-04's pulse distribution and the D-09 scope clauses).
5. `test_inventory_is_non_vacuous` — 59 manufacturers, 746 chips, chip count `> 0`, every recorded
   count `>= 1`, and the loaded database is a non-empty mapping. Report the scanned counts in the
   messages so a zero-scan can never read as a pass.
6. `test_generator_emits_no_key_outside_the_frozen_inventory` — `_generator_chip_entry_keys` over
   `_GEN_PATH` equals `generator_emitted_chip_entry_keys` exactly. The message states that
   `chip_database.json` is generated, so a new key in the generator becomes a new database field
   the moment anyone regenerates.
7. `test_default_targets_resolve_inside_this_repository` — recompute the default database and
   generator paths from `_APP_ROOT` **without** consulting the environment; assert both exist, are
   non-empty and are located under `_APP_ROOT`; assert `_GOLDEN` resolves under `_HERE / "golden"`.
8. `test_this_module_is_collected_and_never_skipped` — read `tests/conftest.py`'s source and assert
   this module's filename does not appear in it (Assumption A6: `collect_ignore` is armed); and
   self-scan this module's own source for any line starting with `pytest.skip` or
   `@pytest.mark.skipif`.

Do NOT delegate any assertion to `tools/diff_db.py` — it is corroboration, not the gate, because it
is silenceable by regenerating `tools/baseline/chip_database.baseline.json` (F-140-10). Do NOT read
anything under `/workspaces/firestarter` — the app repo has its own codified rule against that
(`tests/test_dev_gate_reads_no_firmware_source.py`).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -v 2>&1 | tail -15</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && ruff check tests/test_chip_database_field_inventory.py && ruff format --check tests/test_chip_database_field_inventory.py && echo LINT_OK</automated>
    <automated>cd /workspaces/firestarter_app && python3 tools/check_mypy_watermark.py; echo "mypy_gate_exit=$?"</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" --collect-only -q 2>&1 | grep -c 'test_chip_database_field_inventory'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -v` reports
      **8 passed**, with the eight test names matching those named in the action.
    - `python3 -m pytest tests/ -o addopts="" -q` reports **1547 passed** (1539 baseline + 8). The
      `-o addopts=""` is required — the doubled `-q` otherwise suppresses the count line.
    - `ruff check` and `ruff format --check` both pass on the new module.
    - `python3 tools/check_mypy_watermark.py` exits 0, and its `checked N source files` line is
      recorded in the SUMMARY. The SUMMARY must note that this devcontainer runs py3.12 while CI
      runs py3.9/3.11, so a local pass is weaker evidence than a CI pass.
    - `pytest --collect-only` lists at least one item from
      `tests/test_chip_database_field_inventory.py` (Assumption A6 disproved for this module).
    - The module reads nothing under `/workspaces/firestarter` and imports nothing from
      `firestarter_app/tools/`.
    - `git -C /workspaces/firestarter_app diff --quiet -- firestarter/data/chip_database.json`
      exits 0.
  </acceptance_criteria>
  <done>The DB-half gate exists, passes for the right reason on the real tree, is lint- and
  type-clean, and is proved collectable.</done>
</task>

<task type="auto">
  <name>Task 3: Plant four violations, see the gate RED, capture verbatim, then see it GREEN</name>
  <files>firestarter_app/tests/test_chip_database_field_inventory.py</files>
  <read_first>
    - `/workspaces/.planning/phases/139-gh-15-correction-outward/139-02-SUMMARY.md` lines 120-200 —
      the planted-run transcript discipline: planted input lives only in a scratch directory, is
      deleted immediately after the run, and the RED output is reproduced verbatim
    - `/workspaces/firestarter_app/tests/test_chip_database_field_inventory.py` — written in Task 2
    - `/workspaces/.planning/phases/140-parameter-table/140-CONTEXT.md` § D-15
  </read_first>
  <action>
Prove the gate can fail, four different ways, before its pass is believed (D-15). Every planted
input lives in a `mktemp -d` scratch directory outside both repositories and is deleted immediately
after its run. No planted input may survive anywhere under `/workspaces`, and neither
`firestarter/data/chip_database.json` nor `tools/build_db.py` may be edited in place at any point.

Run A — **a new field on ONE chip**. Copy the database to scratch, add `"foo": 1` to the
`programming` object of a single chip, run with `FIRESTARTER_CHIP_DB_JSON` pointed at the copy.
Expected: RED on test 2 naming `foo` as an added key with count 1.

Run B — **a count change with no new name**. Copy the database to scratch and delete one chip from
one manufacturer. Expected: RED on tests 1/2/3 (counts drop from 746) and on test 5 (745 chips, not
746). This is the leg that proves counts-not-names matters — a names-only gate passes Run B.

Run C — **a vacuous target**. Point `FIRESTARTER_CHIP_DB_JSON` at a file containing `{}`. Expected:
RED on test 5 (non-vacuity), NOT a silent pass.

Run D — **a new key in the GENERATOR only**. Copy `tools/build_db.py` to scratch and add a
`"foo": 1,` entry inside its `chip_entry = {` dict literal. Run with `FIRESTARTER_BUILD_DB_SOURCE`
pointed at the copy. Expected: RED on test 6 naming `foo`. This is the hazard-8 leg: the database is
generated, so a generator-only change is a latent new field.

Run E — **the real tree, no env seams set**: all 8 tests pass.

If any RED run fails for the wrong reason — an import error, a JSON decode error at the wrong point,
a path error rather than the planted violation — the leg was unreachable. Fix the **locator**, never
the assertion (D-15 trap 2), and re-run all five.

Record all five runs, with the exact command line and the verbatim stdout, in this plan's SUMMARY.
Then confirm the tree is clean: `git -C /workspaces/firestarter_app status --porcelain` shows only
the two intended new files.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && T=$(mktemp -d) && python3 -c "import json,sys; db=json.load(open('firestarter/data/chip_database.json')); db[sorted(db)[0]][0]['programming']['foo']=1; json.dump(db, open(sys.argv[1],'w'))" "$T/db.json" && FIRESTARTER_CHIP_DB_JSON="$T/db.json" python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_A_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter_app && T=$(mktemp -d) && python3 -c "import json,sys; db=json.load(open('firestarter/data/chip_database.json')); k=sorted(db)[0]; db[k]=db[k][1:]; json.dump(db, open(sys.argv[1],'w'))" "$T/db.json" && FIRESTARTER_CHIP_DB_JSON="$T/db.json" python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_B_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter_app && T=$(mktemp -d) && printf '{}' > "$T/db.json" && FIRESTARTER_CHIP_DB_JSON="$T/db.json" python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_C_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter_app && T=$(mktemp -d) && sed 's/^\( *\)"support_status": _support_status,/\1"foo": 1,\n\1"support_status": _support_status,/' tools/build_db.py > "$T/build_db.py" && FIRESTARTER_BUILD_DB_SOURCE="$T/build_db.py" python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q; rc=$?; rm -rf "$T"; test $rc -ne 0 && echo "RUN_D_RED_AS_REQUIRED rc=$rc"</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q 2>&1 | tail -3 && git status --porcelain && echo RUN_E_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Runs A, B, C and D each exit **non-zero**, and the captured stdout names the planted violation
      (an added `foo` key, a dropped count, the non-vacuity failure, the generator key) as the
      reason — not an import, path or JSON-decode error.
    - Run B fails on a COUNT assertion with no new key name present — this is the explicit proof
      that pinning counts rather than names was necessary.
    - Run E exits 0 with 8 passed.
    - The SUMMARY reproduces all five runs verbatim: exact command line and exact stdout.
    - `git -C /workspaces/firestarter_app status --porcelain` lists only the two intended new files;
      `git -C /workspaces/firestarter_app diff --quiet -- firestarter/data/chip_database.json
      tools/build_db.py` exits 0.
  </acceptance_criteria>
  <done>The DB-half gate has been seen RED four ways for the right reasons and GREEN once, and the
  RED output is on the record.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan owns:
- `firestarter_app/tests/golden/chip_database_field_inventory.json` — keys `meta`, `totals`,
  `levels` (`top` / `programming` / `electrical`), `protocol_chip_counts`,
  `generator_emitted_chip_entry_keys`
- `firestarter_app/tests/test_chip_database_field_inventory.py` — 8 pytest cases; env seams
  `FIRESTARTER_CHIP_DB_JSON`, `FIRESTARTER_BUILD_DB_SOURCE`

Full Phase 140 artifact list: see `140-01-PLAN.md` § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<requirement_completion>
This plan **contributes to** TABLE-05 (the `chip_database.json` half of D-12's split gate).

**Requirement IDs this plan may mark Complete: NONE.**

TABLE-05 spans plans 140-02 (firmware half) and 140-03 (database half). Only plan 140-07 may flip
any TABLE-* checkbox in `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`.
</requirement_completion>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `infoic.xml` → `tools/build_db.py` → `chip_database.json` | the only supported path by which a database field can appear |
| generated artifact → gate | a gate that reads only the artifact misses a generator-only change |
| gate → CI verdict | a gate silenced by regenerating its own baseline manufactures false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-08 | Tampering | a gate silenced by regenerating a baseline | mitigate | The inventory is committed under `tests/golden/`, is derived independently of `tools/baseline/chip_database.baseline.json`, and `tools/diff_db.py` is explicitly NOT consulted (F-140-10). |
| T-140-09 | Repudiation | a names-only field check | mitigate | Per-key occurrence counts are pinned; planted Run B is a count change with no new name and must be RED. |
| T-140-10 | Tampering | a generator-only new field | mitigate | `ast`-based scan of `tools/build_db.py`'s `chip_entry` construction; planted Run D proves it fires. |
| T-140-11 | Repudiation | a vacuous scan (top-level `for key in db`) | mitigate | Two-level traversal, a `_walk` that raises on a wrong shape, and a non-vacuity test asserting 59 manufacturers and 746 chips; planted Run C proves it fires. |
| T-140-12 | Tampering | a stray `FIRESTARTER_*` env var in CI | mitigate | Test 7 recomputes the default targets from `_APP_ROOT` ignoring the environment, so a redirect turns the gate RED rather than green. Both seams documented in the module docstring. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages in either repository (`140-RESEARCH.md` § Package Legitimacy Audit). |
</threat_model>

<verification>
- `python3 -m pytest tests/test_chip_database_field_inventory.py -o addopts="" -q` → 8 passed.
- `python3 -m pytest tests/ -o addopts="" -q` → 1547 passed.
- `ruff check tests/` and `ruff format --check tests/` pass; `tools/check_mypy_watermark.py` exits 0.
- Four planted RED runs and one GREEN run, all five captured verbatim in the SUMMARY.
- `firestarter/data/chip_database.json` and `tools/build_db.py` are both byte-unchanged.
</verification>

<success_criteria>
- The database half of TABLE-05's gate is committed in the repo that owns the file, runs in that
  repo's CI leg, and has no cross-repo path seam.
- It pins occurrence counts, covers the generator, and is not silenceable by regenerating any
  existing baseline.
- It has been seen RED on four distinct planted violations before any pass was believed.
</success_criteria>

<output>
Create `.planning/phases/140-parameter-table/140-03-SUMMARY.md` when done. It MUST reproduce, for
each of Runs A/B/C/D/E, the exact command line and the verbatim stdout, and record the app suite
count (1539 → 1547) and the `check_mypy_watermark.py` output line.
</output>
