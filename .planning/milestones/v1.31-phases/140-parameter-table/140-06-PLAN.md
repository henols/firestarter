---
phase: 140-parameter-table
plan: 06
type: execute
wave: 3
depends_on: [140-01, 140-05]
files_modified:
  - firestarter/doc/PROTOCOLS.md
  - firestarter/CLAUDE.md
commits_land_in:
  - firestarter/          # submodule — doc/ and CLAUDE.md
autonomous: true
requirements: [TABLE-04, TABLE-01]
must_haves:
  truths:
    - "No firmware document in the repo contradicts the citations this phase commits"
    - "doc/PROTOCOLS.md no longer claims a 3× overpulse or a 1 ms protocol-constant pulse on 0x07"
    - "CLAUDE.md's Algorithm Handlers table no longer claims DQ7 verify on 0x07 or 12-18V on 0x0B"
    - "CLAUDE.md records the D-11 exception to its own 'add a new suite to both pinned envs' rule"
    - "Every corrected claim points at tests/golden/eprom_params_citations.json"
  artifacts:
    - path: "firestarter/doc/PROTOCOLS.md"
      provides: "corrected 27C write-algorithm claims in 1.3/1.4/1.5 plus the F-140-08 datasheet-path note"
      contains: "eprom_params_citations.json"
    - path: "firestarter/CLAUDE.md"
      provides: "corrected Algorithm Handlers rows for 0x07/0x08/0x0B plus the D-11 native-env exception"
      contains: "native_params_v131"
  key_links:
    - from: "firestarter/doc/PROTOCOLS.md"
      to: "firestarter/tests/golden/eprom_params_citations.json"
      via: "per-value attribution pointer in each corrected section"
      pattern: "eprom_params_citations\\.json"
    - from: "firestarter/CLAUDE.md"
      to: "firestarter/platformio.ini"
      via: "the D-11 exception naming native_params_v131 and why it is in neither pinned env"
      pattern: "native_params_v131"
---

<objective>
Reconcile the two firmware documents whose claims this phase's committed evidence contradicts, so
the repository does not ship a cited parameter table alongside prose asserting the opposite.

Purpose: F-140-09 found `doc/PROTOCOLS.md` §1.3 describing "JEDEC Intelligent Programming (1 ms
pulse × N + 3× overpulse)" and citing `W27C512.pdf p.7 §6.2 Programming Algorithm` — a section that
does not exist in a document that actually describes SMART PROGRAMMING with 100 µs pulses, `X = 25`
and no overpulse. §1.4 calls `0x08` "Same Intelligent Programming algorithm" while citing AMD's
Quick-Pulse/Flashrite text. `firestarter/CLAUDE.md` calls `0x07` "1ms pulse, DQ7 verify" (DQ7
polling is a flash-family mechanism) and `0x0B` "12-18V direct" (those NMOS parts are 25 V, and the
database carries `vpp` up to 25 V on that row). CONTEXT flags `CLAUDE.md`; F-140-09 adds
`PROTOCOLS.md`, which CONTEXT does not list.

Output: corrected `firestarter/doc/PROTOCOLS.md` and `firestarter/CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-parameter-table/140-CONTEXT.md
@.planning/phases/140-parameter-table/140-VALIDATION.md
@firestarter/CLAUDE.md
</context>

<repository>
This is a meta-repo. `firestarter/` is a git submodule that is NOT committed to the meta-repo. Both
files are committed **inside** `/workspaces/firestarter`, on branch
`gsd/v1.31-27c-programming-algorithm-fidelity`.
</repository>

<scope_boundary>
**In scope:** only claims this phase's committed evidence directly contradicts — the 27C
**write-algorithm** paragraphs and their citation lines in `doc/PROTOCOLS.md` §§1.3/1.4/1.5, one
added datasheet-path note under `## 1. Real Protocol Buckets`, the three 27C rows of `CLAUDE.md`'s
Algorithm Handlers table, and the D-11 exception in `CLAUDE.md` § "Reuse pattern for future native
tests".

**Out of scope — do NOT touch:** the "Erase model", "VPP behavior" and "Pin roles" paragraphs of
§§1.3-1.5; §0's protocol tables; §§1.1, 1.2, 1.6-1.12; §2; §3; the Cross-links section; `README.md`;
`doc/SHIELD-REVISIONS.md`; `platform/py32f071/FLASH-PATH-AND-PCB.md`; and every
`[SHARED:S1]`-`[SHARED:S5]` marker section of `CLAUDE.md`. Broader documentation work is
Phase 146 / CLOSE-03.

**Do NOT edit the posted gh#15 text or `139-GH15-COMMENT.md`.** F-140-07's correction to the
published `energy_cap_us` justification is recorded, never applied — Phase 146 / CLOSE-04 owns that.

**Do NOT edit `.planning/PROJECT.md`.** Its prose-versus-throughput-table contradiction is named in
the phase record (plan 140-07), not silently rewritten.
</scope_boundary>

<gate_hazards>
Two committed gates read these files; both must still pass afterwards.
1. `firestarter_app/tests/test_dispatch_mirror.py` reads `firestarter/doc/PROTOCOLS.md` across the
   repository boundary and parses its **§0** two-table layout (`_BUCKET_ROW_RE`, `_FAMILY_ROW_RE`).
   §0 is out of scope here, but the **app** suite must be re-run to prove it.
   `tests/scan_paths.py` and `tests/test_fw_presence.py` also resolve that path.
2. `firestarter/tests/test_flash_path_record_sync.py` reads `firestarter/CLAUDE.md` and syncs its
   `[SHARED:S1]`-`[SHARED:S5]` markers against the meta record. Those sections are out of scope,
   but the **firmware** suite must be re-run to prove it.
</gate_hazards>

<tasks>

<task type="auto">
  <name>Task 1: Correct doc/PROTOCOLS.md 1.3/1.4/1.5 and add the datasheet-path note</name>
  <files>firestarter/doc/PROTOCOLS.md</files>
  <read_first>
    - `/workspaces/firestarter/doc/PROTOCOLS.md` lines 74-80 (`## 1. Real Protocol Buckets`, where
      the note goes) and lines 120-178 (§§1.3, 1.4, 1.5 in full)
    - `/workspaces/firestarter/tests/golden/eprom_params_citations.json` — written by plan 140-05;
      the authority every corrected claim points at
    - `/workspaces/firestarter/src/proms/eprom.cpp` lines 69-77 and 159-179 — the actual firmware
      behaviour (fallback switch; retry escalation, not a margin pulse). READ-ONLY.
    - `/workspaces/firestarter_app/tests/test_dispatch_mirror.py` lines 90-170 — what it parses out
      of this file, so the edit stays clear of it
    - `/workspaces/.planning/phases/140-parameter-table/140-RESEARCH.md` § State of the Art and
      findings F-140-05, F-140-07, F-140-08, F-140-09
  </read_first>
  <action>
Edit `firestarter/doc/PROTOCOLS.md` in place. Four changes, nothing else.

**Change 1 — datasheet-path note.** Immediately under the `## 1. Real Protocol Buckets` heading, add
a note stating that the `datasheets/<slug>/<file>.pdf` paths cited throughout §1 do NOT resolve on
this branch (that tree exists only on `v1.16-protocol-first-architecture-rebuild`), giving the
recovery command `git show v1.16-protocol-first-architecture-rebuild:datasheets/<slug>/<file>.pdf`,
and stating that per-value attribution for the three 27C rows is machine-readable at
`tests/golden/eprom_params_citations.json` (Phase 140 / TABLE-04). Cite F-140-08 by name.

**Change 2 — §1.3 (`0x07`) write-algorithm paragraph and its citation line.** Remove the
"JEDEC Intelligent Programming (1 ms pulse × N + 3× overpulse) ... apply overpulse = 3 ×
retry_count pulses (max ~25 ms total)" claim and the `W27C512.pdf p.7 §6.2 Programming Algorithm`
citation. Both quoted fragments use the U+00D7 multiplication sign `×` in the file, **not** an
ASCII `x` — search for the `×` bytes or the edit will silently miss them. The replacement states
all five of:
  (a) pulse width is a **database datum** read from `handle->pulse_delay` on every write path, not a
      protocol constant; 1000 µs is only the `pulse_delay == 0` fallback (`eprom.cpp:71-76`) and the
      row's modal database value is 100 µs across 113 of its 170 chips;
  (b) **no overprogram pulse is applied on this row** (`overprogram_factor = 0`) per Winbond W27C512
      Rev A4 (SMART PROGRAMMING ALGORITHM 2, no overprogram step), ST M27C512 Rev 3 §2.6 ("No
      overprogram pulses are applied since the verify in MARGIN MODE provides the necessary
      margin") and Microchip 27C512A DS11173G §1.6;
  (c) `max_pulses = 25` from the Winbond and ST flowcharts, noting Microchip specifies 10 and 25 is
      the maximum across the row's cited datasheets;
  (d) the **named, scoped divergence**: the 22 Intel-family 1 ms parts on this row genuinely want a
      `3 x N` margin pulse; serving them correctly requires splitting `0x07`, which TABLE-05's
      single-dispatch-key constraint forbids; recorded as a Phase 146 follow-up (F-140-05);
  (e) the firmware's present loop at `eprom.cpp:159-179` is **retry escalation of `pulse_delay`**,
      not an Intel 3N margin pulse — Phase 141 replaces it.
Rewrite the citation line in the self-describing form (vendor, part, document number,
revision/date, section or figure) and point at `tests/golden/eprom_params_citations.json`. Do not
invent a section number: the Winbond document's algorithm is titled "SMART PROGRAMMING ALGORITHM 2"
and has no §6.2.

**Change 3 — §1.4 (`0x08`) write-algorithm paragraph and its citation line.** Remove "Same
Intelligent Programming algorithm as 0x07". The replacement states: the family is Intel Quick-Pulse
/ AMD Flashrite / ST PRESTO II; **no overprogram pulse is applied** (`overprogram_factor = 0`)
resolved from three independent vendors (ST M27C1001 §2.6 verbatim, AMD Am27C020 Flashrite, Winbond
W27C020), which agrees with `PROJECT.md`'s prose and contradicts `PROJECT.md`'s own throughput table
(D-06, named in the Phase 140 record); `max_pulses = 25` from Winbond W27C020 and ST M27C1001,
noting AMD Am27C020 says "until it verifies or the maximum is reached" **without a number**; and
100 µs is the modal database value (104 of 127 chips), not a protocol constant — the firmware
constant is only the `pulse_delay == 0` fallback. Point at the citations sidecar.

**Change 4 — §1.5 (`0x0B`) write-algorithm paragraph and its citation line.** Remove "Same
Intelligent Programming pulse algorithm". The replacement states: TI TMS 2516-25/35/45 JL (December
1979, revised May 1982) specifies a **single 50 ms pulse per location** (`t_w(PR)` = 45 / 50 / 55
ms), permits verification immediately after each location, and specifies **no final full-array
pass** and **no overprogram**; the firmware ships a looped pulse-verify with a per-byte
accumulated-energy cap of 50 ms (`energy_cap_us`), satisfying both readings (milestone D-02); 500 µs
is only the `pulse_delay == 0` fallback; and — recorded, not applied here — the justification
published for the 50 ms figure ("100 x 500 µs is the classic 2716 *total* programming time") is
factually wrong, because that datasheet states total programming time for all bits is **100
seconds** and 50 ms is the per-location pulse. The **value** has a genuine primary basis; the
**reason** published for it does not (F-140-07), and Phase 146 / CLOSE-04 reconciles the posted
text. Point at the citations sidecar.

Leave the "Erase model", "VPP behavior" and "Pin roles" paragraphs of all three sections
byte-unchanged, and leave §0, §2, §3 and every other bucket section byte-unchanged.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'eprom_params_citations.json' doc/PROTOCOLS.md && ! grep -q '3× overpulse' doc/PROTOCOLS.md && ! grep -q '3 × retry_count' doc/PROTOCOLS.md && ! grep -q 'Programming Algorithm; `datasheets' doc/PROTOCOLS.md && ! grep -q 'Same Intelligent Programming' doc/PROTOCOLS.md && echo PROTOCOLS_CORRECTED</automated>
    <automated>cd /workspaces/firestarter && git diff -- doc/PROTOCOLS.md | grep -q '^-.*3× overpulse' && git diff -- doc/PROTOCOLS.md | grep -q '^-.*3 × retry_count' && git diff -- doc/PROTOCOLS.md | grep -q '^-.*Same Intelligent Programming' && echo REMOVALS_PROVEN_IN_DIFF</automated>
    <automated>cd /workspaces/firestarter && grep -c 'v1.16-protocol-first-architecture-rebuild' doc/PROTOCOLS.md</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_dispatch_mirror.py tests/test_fw_presence.py -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && git diff --stat -- doc/PROTOCOLS.md</automated>
  </verify>
  <acceptance_criteria>
    - `doc/PROTOCOLS.md` contains `eprom_params_citations.json` at least 4 times (the §1 note plus
      each of §§1.3/1.4/1.5).
    - The strings `3× overpulse`, `3 × retry_count` and `Same Intelligent Programming` no longer
      appear anywhere in the file, and no citation line claims a `§6.2 Programming Algorithm` in the
      W27C512 document. Match the **U+00D7 multiplication sign `×`, never an ASCII `x`**: on the
      pre-edit file `grep -c '3× overpulse' doc/PROTOCOLS.md` is 1 and `grep -c '3x overpulse'` is 0,
      so an ASCII-`x` pattern matches nothing before *or* after the edit and the assertion would be
      vacuous.
    - The removal is proven **positively**, not only asserted negatively: `git diff -- doc/PROTOCOLS.md`
      carries `-` lines containing `3× overpulse`, `3 × retry_count` and `Same Intelligent
      Programming`. Run that leg **before committing** — the same working-tree precondition the
      `git diff --stat` leg below already relies on.
    - The §1 note names `v1.16-protocol-first-architecture-rebuild`, the `git show` recovery command
      and F-140-08.
    - §1.3's replacement contains all five points (a)-(e), including the 22-part Intel-family
      divergence and the 113-of-170 modal-pulse figure.
    - §1.4's replacement names the `PROJECT.md` throughput-table contradiction; §1.5's names the
      100-seconds correction (F-140-07) and states that Phase 146 / CLOSE-04 reconciles the posted
      text.
    - `git diff --stat` lists `doc/PROTOCOLS.md` only, and `git diff` shows no change inside any
      "Erase model", "VPP behavior" or "Pin roles" paragraph, nor in §0, §2 or §3.
    - `pytest tests/test_dispatch_mirror.py tests/test_fw_presence.py` in `firestarter_app` still
      passes.
  </acceptance_criteria>
  <done>The three 27C write-algorithm descriptions match the datasheets this phase actually read,
  and each points at the machine-readable citation sidecar.</done>
</task>

<task type="auto">
  <name>Task 2: Correct CLAUDE.md's Algorithm Handlers rows and record the D-11 native-env exception</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    - `/workspaces/firestarter/CLAUDE.md` lines 57-75 (the Algorithm Handlers table) and lines
      200-215 (§ "Reuse pattern for future native tests")
    - `/workspaces/firestarter/platformio.ini` — the `[env:native_params_v131]` section added by
      plan 140-04, whose HARD CONSTRAINT comment this section must agree with
    - `/workspaces/firestarter/tests/golden/eprom_params_citations.json` — written by plan 140-05
    - `/workspaces/firestarter/tests/test_flash_path_record_sync.py` — the gate that reads this
      file; confirm the `[SHARED:S1]`-`[SHARED:S5]` marker sections are untouched
    - `/workspaces/.planning/phases/140-parameter-table/140-RESEARCH.md` § State of the Art
      ("Deprecated / outdated") and § Common Pitfalls, Pitfall 7
  </read_first>
  <action>
Edit `firestarter/CLAUDE.md` in place. Four changes, nothing else.

**Change 1 — Algorithm Handlers row `0x07`.** Replace the Notes cell "1ms pulse, DQ7 verify". The
replacement states: pulse width comes from the database `pulse-delay` field (modal 100 µs, 113 of
170 chips); 1000 µs is only the `pulse_delay == 0` fallback; verify after every pulse plus one final
full-array pass; `max_pulses` 25; no overprogram. Add that **DQ7 polling is a flash-family mechanism
and is not used on this row**. Point at `tests/golden/eprom_params_citations.json`.

**Change 2 — Algorithm Handlers row `0x08`.** Replace the Notes cell "100µs pulse" with: pulse width
from the database (modal 100 µs, 104 of 127 chips); 100 µs is only the `pulse_delay == 0` fallback;
verify per pulse plus a final full-array pass; `max_pulses` 25; no overprogram (D-06 resolved from
three vendors). Point at the sidecar.

**Change 3 — Algorithm Handlers row `0x0B`.** Replace the VPP cell "12–18V direct" with the correct
range — the database carries `vpp` values up to 25 V on this row and TI TMS 2516 specifies +25 V —
and replace the Notes cell "500µs pulse, 24-pin" with: 24-pin; pulse width from the database (modal
500 µs, 21 of 32 chips); 500 µs is only the `pulse_delay == 0` fallback; verify per pulse with no
final full-array pass; per-byte accumulated-energy cap 50 ms; no overprogram. Point at the sidecar.

**Change 4 — § "Reuse pattern for future native tests".** Append a clearly-labelled exception
paragraph: the section's instruction to add a new suite to **both** `[env:native]` and
`[env:native_nodevtools]` is **overridden for `native_params_v131`** by Phase 140 / D-11, because
both pinned envs are asserted at exactly 141 cases / 17 suites by
`scripts/baseline/size_baseline.json` through `check_size_baseline.py`'s `compare_native`, so adding
a case to either turns a live gate RED. State that `native_params_v131` follows the
`native_trace_v131` precedent: its `test_filter` names only its own suite, it is not in
`default_envs`, it is never passed to `check_size_baseline.py` (uncaught `KeyError`, exit 1 —
F-138-05) nor to `check_build_warnings.py` (exit 2, no baseline entry), and it runs in **no CI leg**
of either repository (F-140-11), so its counts are a run-by-name obligation recorded in the phase
record.

Do not touch any `[SHARED:S1]`-`[SHARED:S5]` marker section, the Protocol Dispatch section, the
Hardware Revision Documentation section, or the PY32F071 section.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -c 'eprom_params_citations.json' CLAUDE.md && grep -q 'native_params_v131' CLAUDE.md && ! grep -q '1ms pulse, DQ7 verify' CLAUDE.md && ! grep -q '12–18V direct' CLAUDE.md && ! grep -q '500µs pulse, 24-pin' CLAUDE.md && echo CLAUDE_CORRECTED</automated>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_flash_path_record_sync.py -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && git diff --stat -- CLAUDE.md && git diff -- CLAUDE.md | grep -c 'SHARED:S'</automated>
  </verify>
  <acceptance_criteria>
    - The strings `1ms pulse, DQ7 verify`, `12–18V direct` and `500µs pulse, 24-pin` no longer
      appear in `CLAUDE.md`.
    - `CLAUDE.md` contains `eprom_params_citations.json` at least 3 times (one per 27C row) and
      `native_params_v131` at least once.
    - The 0x07 Notes cell explicitly states that DQ7 polling is a flash-family mechanism not used on
      that row.
    - The 0x0B VPP cell names 25 V and the Notes cell names the 50 ms per-byte energy cap.
    - The "Reuse pattern for future native tests" section carries a labelled D-11 exception naming
      all five constraints (neither pinned env, not `default_envs`, not `check_size_baseline.py`,
      not `check_build_warnings.py`, no CI leg).
    - `git diff -- CLAUDE.md | grep -c 'SHARED:S'` returns **0** — no marker section was touched.
    - `pytest tests/test_flash_path_record_sync.py -q` still passes.
  </acceptance_criteria>
  <done>The firmware's own guidance file no longer contradicts the citations shipped beside it, and
  the D-11 override of its native-test reuse instruction is recorded where a future executor reads
  it.</done>
</task>

<task type="auto">
  <name>Task 3: Prove both repositories' suites are still green after the doc edits</name>
  <files>firestarter/doc/PROTOCOLS.md, firestarter/CLAUDE.md</files>
  <read_first>
    - `/workspaces/.planning/phases/140-parameter-table/140-06-PLAN.md` § gate_hazards (this file)
    - `/workspaces/firestarter/tests/test_flash_path_record_sync.py` — the CLAUDE.md consumer
    - `/workspaces/firestarter_app/tests/scan_paths.py` — the app repo's explicit cross-repo path
      inventory, which resolves `firestarter/doc/PROTOCOLS.md`
  </read_first>
  <action>
Run both repositories' Python suites in full and confirm zero failures, then confirm that only the
two intended documentation files changed in this plan.

Documentation edits in this repository are load-bearing: one committed gate in each repository reads
one of these two files. A green run here is the evidence that the corrections did not break a
parser. Record both suite results in this plan's SUMMARY, together with the `git diff --stat` output
showing exactly two changed files.

Do not run any `pio` env in this plan — no C/C++ source changed, and plan 140-07 owns the cold
firmware capture.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/ -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q 2>&1 | tail -3</automated>
    <automated>cd /workspaces/firestarter && git status --porcelain && git diff --stat</automated>
    <automated>cd /workspaces/firestarter && git diff --quiet -- src/ include/ platformio.ini tests/ && echo ONLY_DOCS_CHANGED</automated>
  </verify>
  <acceptance_criteria>
    - `cd /workspaces/firestarter && python3 -m pytest tests/ -q` reports **0 failed**.
    - `cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q` reports
      **0 failed** (baseline 1539 plus plan 140-03's 8 = 1547).
    - `git -C /workspaces/firestarter diff --quiet -- src/ include/ platformio.ini tests/` exits 0 —
      this plan changed documentation only.
    - `git -C /workspaces/firestarter diff --stat` lists exactly two files: `doc/PROTOCOLS.md` and
      `CLAUDE.md`.
    - `git -C /workspaces/firestarter diff --quiet -- src/proms/eprom.cpp` exits 0 (D-10 still
      holds).
  </acceptance_criteria>
  <done>Both suites are green after the documentation corrections, and nothing outside the two
  documents moved.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan owns (modifications, not new files):
- `firestarter/doc/PROTOCOLS.md` — a datasheet-path note under `## 1. Real Protocol Buckets`, plus
  corrected write-algorithm paragraphs and citation lines in §§1.3, 1.4 and 1.5
- `firestarter/CLAUDE.md` — corrected Algorithm Handlers Notes/VPP cells for 0x07, 0x08 and 0x0B,
  plus the D-11 exception in § "Reuse pattern for future native tests"

Full Phase 140 artifact list: see `140-01-PLAN.md` § "Artifacts this phase produces".
</artifacts_this_phase_produces>

<requirement_completion>
This plan **contributes to** TABLE-04 and TABLE-01.

**Requirement IDs this plan may mark Complete: NONE.**

TABLE-04 spans plans 140-05 and 140-06; TABLE-01 spans 140-01, 140-04 and 140-05. Only plan 140-07
may flip any TABLE-* checkbox in `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`.
CLOSE-03 and CLOSE-04 belong to Phase 146 and must not be touched here.
</requirement_completion>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | a doc that contradicts the shipped citations misleads the next implementer |
| `doc/PROTOCOLS.md` → cross-repo parser | the app repo parses §0 of this file across the repository boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-24 | Repudiation | contradictory in-repo documentation | mitigate | Correct only the claims this phase's evidence contradicts and point each at `tests/golden/eprom_params_citations.json`, so the doc and the gate cannot drift apart silently. |
| T-140-25 | Tampering | breaking `test_dispatch_mirror.py`'s cross-repo §0 parse | mitigate | §0 is explicitly out of scope; Task 1 re-runs that module and Task 3 re-runs the whole app suite. |
| T-140-26 | Tampering | breaking `test_flash_path_record_sync.py`'s CLAUDE.md marker sync | mitigate | `[SHARED:S1]`-`[SHARED:S5]` are out of scope; an acceptance criterion asserts `git diff` touches no `SHARED:S` line, and Task 2 re-runs that module. |
| T-140-27 | Repudiation | silently re-wording a publicly posted claim | mitigate | F-140-07's correction is recorded in-repo and handed to Phase 146 / CLOSE-04; the posted gh#15 text and `139-GH15-COMMENT.md` are explicitly out of scope. |
| T-140-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no external packages in either repository (`140-RESEARCH.md` § Package Legitimacy Audit). |
</threat_model>

<verification>
- `cd /workspaces/firestarter && python3 -m pytest tests/ -q` → 0 failed.
- `cd /workspaces/firestarter_app && python3 -m pytest tests/ -o addopts="" -q` → 0 failed.
- `git -C /workspaces/firestarter diff --stat` lists exactly `doc/PROTOCOLS.md` and `CLAUDE.md`.
- `git -C /workspaces/firestarter diff -- CLAUDE.md | grep -c 'SHARED:S'` returns 0.
- `git -C /workspaces/firestarter diff --quiet -- src/proms/eprom.cpp` exits 0.
</verification>

<success_criteria>
- The three 27C write-algorithm descriptions in `doc/PROTOCOLS.md` and the three Algorithm Handlers
  rows in `CLAUDE.md` agree with the citations this phase commits.
- The D-11 exception is recorded in the very section whose instruction it overrides.
- No gate in either repository broke, and nothing outside the two documents changed.
</success_criteria>

<output>
Create `.planning/phases/140-parameter-table/140-06-SUMMARY.md` when done. It MUST record both suite
results, the `git diff --stat` output, and a short list of each corrected claim with its replacement.
</output>
