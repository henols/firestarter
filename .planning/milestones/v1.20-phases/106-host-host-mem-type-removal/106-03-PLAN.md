---
phase: 106-host-host-mem-type-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/chip_resolver.py
  - firestarter_app/tests/test_chip_resolver.py
autonomous: true
requirements: [HOST-04]
must_haves:
  truths:
    - "A chip entry (built-in or user-override) lacking a usable `algorithm` (absent OR 0) is rejected with a clear, actionable error BEFORE any serial byte is sent — no silent fallback dispatch (SC#4)."
    - "The rejection reuses `ChipNotImplementedError` and fires in `resolve_chip` alongside the existing `support_status` refusal, upstream of `convert_to_programmer` and all serial I/O."
    - "No supported chip with a valid non-zero `algorithm` is newly refused (no false-positive regression)."
  artifacts:
    - "firestarter_app/firestarter/chip_resolver.py (edited — algorithm-presence guard added, fully type-annotated)"
    - "firestarter_app/tests/test_chip_resolver.py (edited — `:43` required-keys inversion + net-new D-06 HOST-04 test)"
  key_links:
    - "`resolve_chip` is the single pre-serial chokepoint; the guard reads `algorithm` from the un-mapped `raw_config` (`raw_config['programming']['algorithm']`), the same object the `support_status` guard reads — NOT the mapped `protocol-id` key."
    - "`convert_to_programmer` must NOT be called when the guard fires — proven by `patch.object(...).assert_not_called()` (no wire dict, no serial byte)."
---

<objective>
Add the HOST-04 algorithm-presence guard to `chip_resolver.resolve_chip` (D-01/D-02) — reject any chip whose `algorithm` is absent or `0` with a clear, actionable `ChipNotImplementedError` BEFORE any wire dict is built — and prove it with a net-new test exercising a deliberately-broken user-override (D-06, SC#4). Also invert the `test_chip_resolver.py:43` required-keys assertion (removing `"type"`), owned here to avoid a write-conflict with Plan 01.

Purpose: Complete the fail-closed dispatch contract in-host, exactly mirroring the firmware `protocol == 0 → 0xBB` fail-close (Phase 105 D-04/D-06). Pre-v1.20 or hand-crafted user-override DB entries lacking `algorithm` must no longer silently dispatch — the intended breaking change. The guard's placement in `resolve_chip` (upstream of `convert_to_programmer` and all serial I/O) satisfies "before any serial byte" structurally.

Output: `chip_resolver.py` with a fully-typed algorithm-presence guard beside the `support_status` refusal; `test_chip_resolver.py` with the `:43` inversion + a D-06 test proving refusal with `convert_to_programmer` never called.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-host-host-mem-type-removal/106-CONTEXT.md
@.planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md
@.planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md
@firestarter_app/CLAUDE.md

# The exception being reused (do NOT add a new type)
@firestarter_app/firestarter/exceptions.py
</context>

<artifacts_this_phase_produces>
Primarily ADDITIVE (a guard + a test) plus one test inversion.

Net-new symbols:
- one new test function in `tests/test_chip_resolver.py` (the D-06 HOST-04 broken-override test). Suggested name: `test_resolve_chip_refuses_missing_algorithm_before_convert_to_programmer` (mirrors the existing `test_resolve_chip_guard_fires_before_convert_to_programmer` naming).
- a small guard block inside the existing `resolve_chip` function (no new module-level symbol).

Removed symbol:
- the `"type"` element of the required-keys tuple in `test_chip_resolver.py:43`.

Reused (NOT new): `ChipNotImplementedError` (already imported at `chip_resolver.py:13`).

OUT OF SCOPE (do NOT add): no `KNOWN_PROTOCOLS` membership gate (D-01 — a non-zero-but-unknown protocol falls through to the firmware's fail-closed 0xBB, preserving "trust the wire's real protocol"); no new exception type.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the algorithm-presence guard to resolve_chip (D-01/D-02, HOST-04)</name>
  <files>firestarter_app/firestarter/chip_resolver.py</files>
  <read_first>
    - firestarter_app/firestarter/chip_resolver.py (edit target — read the full `resolve_chip` body :16–61, especially the `support_status` guard :43–57 and the `convert_to_programmer` call :59–60)
    - firestarter_app/firestarter/exceptions.py (`ChipNotImplementedError` :49–64 — the exception to reuse; note its refuse-before-serial docstring)
    - .planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md § "firestarter/chip_resolver.py" (the analog `support_status` guard + exact copy-from directions)
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Pitfall 3: HOST-04 guard placement" (placement (a): read `raw_config['programming']['algorithm']`, before `get_eprom`) and § "Pitfall 5: mypy strict-island scope" (`chip_resolver.py` IS strict — fully annotate)
  </read_first>
  <behavior>
    - A supported chip with a non-zero `algorithm` (e.g. `W27C512`) resolves normally — no new refusal (regression guard).
    - A `support_status == "supported"` entry whose `programming.algorithm` is absent → `ChipNotImplementedError` raised, message names the chip and states a protocol/`algorithm` is required.
    - A `support_status == "supported"` entry whose `programming.algorithm == 0` → `ChipNotImplementedError` raised (0 is not usable — mirrors firmware `protocol == 0 → 0xBB`).
    - The guard fires BEFORE `db.get_eprom` / `db.convert_to_programmer` are called (no wire dict, no serial byte).
    - A non-zero-but-unknown `algorithm` (e.g. 0x99) is NOT refused by the host (D-01 — falls through to firmware fail-close).
  </behavior>
  <action>
    Add the HOST-04 guard inside `resolve_chip`, placed AFTER the existing `support_status` block (`:57`) and BEFORE the `full = db.get_eprom(name)` line (`:59`) — per D-02 "alongside the existing support_status refusal ... BEFORE convert_to_programmer builds any wire dict" and Pitfall 3 placement (a).
    Read the algorithm from the UN-mapped `raw_config` (the same object the support_status guard reads), path `raw_config.get("programming", {}).get("algorithm", 0)`. Per D-01, treat the value as unusable when it is falsy (absent OR `0`) — a plain `if not <algorithm>:` truthiness check covers both (0 and missing→default-0). Do NOT add a `KNOWN_PROTOCOLS` membership check; a non-zero-but-unknown protocol must pass through (D-01).
    On the unusable case, raise `ChipNotImplementedError` (already imported at `:13` — reuse it, add no new type) with a clear, actionable message in the same `f"{name}: ..."` shape as the support_status analog — name the chip and state that a usable protocol/`algorithm` is required (e.g. `f"{name}: no usable programming algorithm (protocol) defined — a chip entry must specify a non-zero 'algorithm' to be programmed"`).
    Keep the guard fully type-annotated (`chip_resolver.py` is in the 8-module mypy strict island — `disallow_untyped_defs`). The function signature is unchanged; only a typed local read + a raise are added. Use only py3.9-compatible idioms consistent with the existing file style (no `match`, no new union sugar).
    Add a short comment tying the guard to HOST-04 / D-01 / D-02 and the firmware `protocol==0 → 0xBB` mirror, matching the neighboring comment style. Do NOT write the exact literal error-message string into any negative-grep target elsewhere; it lives only in this raise.
  </action>
  <acceptance_criteria>
    - `grep -n 'algorithm' firestarter/chip_resolver.py` shows the new read from `raw_config`'s `programming` sub-dict (not the mapped `protocol-id` key).
    - `grep -c 'class ChipNotImplementedError\|raise ChipNotImplementedError' firestarter/chip_resolver.py` shows the guard reuses the existing exception (no new `class` definition in this file).
    - `cd firestarter_app && mypy firestarter/main.py firestarter/cli_handlers.py firestarter/chip_resolver.py firestarter/frame_parser.py firestarter/codec.py firestarter/address_parser.py firestarter/exceptions.py firestarter/serial_comm.py` reports success for `chip_resolver.py` (the mypy 2.1.0 "python_version 3.9 not supported" line is a WARNING, not a failure — do not mistake it).
    - `cd firestarter_app && python -c "from firestarter.chip_resolver import resolve_chip; from firestarter.database import EpromDatabase; d=EpromDatabase(skip_local_override=True); print('OK supported:', resolve_chip('W27C512', db=d).get('algorithm'))"` exits 0 (regression: a real supported chip still resolves).
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && mypy firestarter/chip_resolver.py firestarter/exceptions.py firestarter/database.py firestarter/serial_comm.py firestarter/codec.py firestarter/frame_parser.py firestarter/address_parser.py firestarter/main.py firestarter/cli_handlers.py && python -c "from firestarter.chip_resolver import resolve_chip; from firestarter.database import EpromDatabase; d=EpromDatabase(skip_local_override=True); assert resolve_chip('W27C512', db=d).get('algorithm'); print('OK')"</automated>
  </verify>
  <done>`resolve_chip` refuses any `support_status=="supported"` entry whose `programming.algorithm` is absent or 0 with a reused `ChipNotImplementedError` (clear, chip-named message), placed before `get_eprom`/`convert_to_programmer`; a real supported chip still resolves; `chip_resolver.py` passes mypy strict.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-06 HOST-04 test + invert the :43 required-keys assertion</name>
  <files>firestarter_app/tests/test_chip_resolver.py</files>
  <read_first>
    - firestarter_app/tests/test_chip_resolver.py (edit target — the `db` fixture :28–31, the required-keys tuple :43, and the analog `test_resolve_chip_guard_fires_before_convert_to_programmer` :122–132)
    - .planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md § "tests/test_chip_resolver.py" (Analog A for the D-06 test's patch-and-assert-not-called structure; Analog B for the `:43` inversion)
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Open Questions #3" (D-06 construction: monkeypatch `get_eprom_config` to return a synthetic supported-but-algorithm-less raw record)
  </read_first>
  <action>
    (1) `:43` required-keys inversion (D-05 discipline, forced by HOST-01): in `test_resolve_chip_hit_has_required_programmer_keys`, REMOVE the `"type"` element from the tuple `("memory-size", "type", "algorithm", "pin-count", "vpp_mv", "flags")` — `type` is no longer a resolved-dict key.
    (2) Add the net-new D-06 test (SC#4). Copy the patch-and-assert-not-called structure from `test_resolve_chip_guard_fires_before_convert_to_programmer` (`:122–132`). Construct a deliberately-broken entry that is `support_status == "supported"` yet has `programming.algorithm` absent/`0`, so the NEW algorithm guard (not the support_status guard) is what fires: use `unittest.mock.patch.object(db, "get_eprom_config")` to return a synthetic `(raw_config, manufacturer)` tuple where `raw_config = {"support_status": "supported", "programming": {}}` (algorithm absent) — and optionally a second parametrized case with `"programming": {"algorithm": 0}`. Simultaneously `patch.object(db, "convert_to_programmer")` and assert it is NEVER called. Assert `pytest.raises(ChipNotImplementedError)` wraps the `resolve_chip(<name>, db=db)` call and then `mock_convert.assert_not_called()` — proving no wire dict / no serial byte. Name it `test_resolve_chip_refuses_missing_algorithm_before_convert_to_programmer`.
    (3) Optionally add a NON-refusal assertion in the same or a sibling test: a synthetic `support_status=="supported"` entry with a non-zero-but-unknown `algorithm` (e.g. `0x99`) must NOT raise from the algorithm guard (D-01 pass-through) — but only if it can be exercised without tripping downstream `convert_to_programmer` on a synthetic record; if that requires over-mocking, document the D-01 pass-through in a comment instead and rely on the real-chip regression check in Task 1.
    Reuse the existing `db` fixture (`EpromDatabase(skip_local_override=True)`), the existing `ChipNotImplementedError` import, and `unittest.mock.patch`.
  </action>
  <acceptance_criteria>
    - `grep -c '"type"' firestarter_app/tests/test_chip_resolver.py` returns 0 (the `type` element removed from the required-keys tuple).
    - `grep -c 'assert_not_called' firestarter_app/tests/test_chip_resolver.py` is ≥2 (the existing guard test + the new D-06 test).
    - `grep -c 'def test_resolve_chip_refuses_missing_algorithm' firestarter_app/tests/test_chip_resolver.py` returns 1.
    - `cd firestarter_app && python -m pytest tests/test_chip_resolver.py -q` exits 0 (all resolver tests, including the new D-06 test, pass).
    - `cd firestarter_app && python -m pytest tests/test_chip_resolver.py -k "refuses_missing_algorithm" -q` runs ≥1 test and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && ! grep -q '"type"' tests/test_chip_resolver.py && python -m pytest tests/test_chip_resolver.py -q && python -m pytest tests/test_chip_resolver.py -k "refuses_missing_algorithm" -q</automated>
  </verify>
  <done>`test_chip_resolver.py`'s required-keys tuple no longer lists `"type"`; a net-new D-06 test proves a supported-but-algorithm-less entry raises `ChipNotImplementedError` with `convert_to_programmer` never called (no serial byte); the resolver suite passes green.</done>
</task>

<task type="auto">
  <name>Task 3: Full host suite + py3.11 static gates + non-regression gates (wave-close)</name>
  <files>firestarter_app/firestarter/chip_resolver.py, firestarter_app/tests/test_chip_resolver.py</files>
  <read_first>
    - firestarter_app/CLAUDE.md § tooling gate + non-regression gates
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Non-Regression Gates (must stay green)" and § "Sampling Rate → Per wave merge"
  </read_first>
  <action>
    This task is the wave-close integration gate for the whole phase (all three plans land in Wave 1; run this after Plans 01 and 02 are also complete — the executor sequences it last). Run the full CI-equivalent gate set from `firestarter_app/`:
    (1) full host suite: `python -m pytest` (all tests green, coverage ≥70% floor unchanged — deletions should not drop coverage below floor).
    (2) py3.11-target static gates on the milestone-touched files: `ruff check firestarter/ tests/ tools/`, `ruff format --check firestarter/ tests/`, and `mypy` on the 8 strict modules (confirm `chip_resolver.py` clean).
    (3) non-regression gates that must stay green (GATE-01 predecessors, re-verified at Phase 107 but must not break here): `python tools/check_dispatch.py` (exit 0, 0 violations), `python -m pytest tests/test_dispatch_mirror.py`, and confirm `chip_database.json` is UNCHANGED by this phase (code-only) — `git -C . diff --stat -- firestarter/data/chip_database.json` shows no change (this phase edits code, NOT the generated DB; do NOT run `build_db.py`).
    If any gate is red, fix within the owning file before declaring done. If `python3.11` binary is absent in the devcontainer (per the Phase-98 precedent), record the py3.11 leg as CI-PENDING/structurally-green under py3.12.13 and confirm ruff/mypy/pytest all pass under the pinned analysis target (py39 config) — never fabricate a py3.11 PASS for an absent interpreter.
  </action>
  <acceptance_criteria>
    - `cd firestarter_app && python -m pytest -q` exits 0 (full suite, coverage floor held).
    - `cd firestarter_app && ruff check firestarter/ tests/ tools/` prints "All checks passed!".
    - `cd firestarter_app && ruff format --check firestarter/ tests/` reports already-formatted.
    - `cd firestarter_app && python tools/check_dispatch.py; echo "exit=$?"` prints `exit=0` (0 violations — the tool-leg `_ALGO_MEM_TYPE` deadness-simulation is intentionally UNTOUCHED and stays green, per the documented OUT-of-scope decision).
    - `cd firestarter_app && python -m pytest tests/test_dispatch_mirror.py -q` exits 0.
    - `cd firestarter_app && git diff --stat -- firestarter/data/chip_database.json` shows no change (DB untouched).
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && python -m pytest -q && ruff check firestarter/ tests/ tools/ && ruff format --check firestarter/ tests/ && python tools/check_dispatch.py && python -m pytest tests/test_dispatch_mirror.py -q && test -z "$(git diff --name-only -- firestarter/data/chip_database.json)"</automated>
  </verify>
  <done>Full host suite green with coverage floor held; ruff/ruff-format/mypy clean against the py3.11 analysis target; `check_dispatch.py` reports 0 violations and `test_dispatch_mirror.py` passes (tool-leg deliberately untouched); `chip_database.json` is byte-unchanged (code-only phase).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI dispatch → DB lookup/conversion | `resolve_chip` is the single pre-serial chokepoint; a chip entry crossing here without a usable `algorithm` must be refused before any wire dict/serial byte |
| user-override DB entry → dispatch | `~/.firestarter/database.json` entries are untrusted hand-crafted input; a legacy/broken entry lacking `algorithm` is the intended breaking-change refusal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-05 | Elevation of privilege / Tampering | hand-crafted / legacy user-override DB entry lacking `algorithm` | medium | mitigate | HOST-04 guard in `resolve_chip` refuses missing/zero `algorithm` in-host BEFORE serial I/O, mirroring firmware `protocol==0 → 0xBB` fail-close (D-01). Prevents an unusable entry silently reaching a 12V-VPP hazard path. Proven by the D-06 `assert_not_called()` test. |
| T-106-06 | Denial of service | over-strict guard refusing valid chips | low | mitigate | D-01 uses present-and-non-zero only (no `KNOWN_PROTOCOLS` gate), so a non-zero-but-unknown protocol still passes through to firmware. Regression test (Task 1) confirms a real supported chip (`W27C512`) still resolves. |
| T-106-07 | Tampering | over-voltage / VPP path | low | accept | Removing `type` + adding the guard does not alter any voltage path; firmware VPP ceiling untouched. SAFE-01 re-confirmed at Phase 107. |

No package installs in this plan → no supply-chain arm.
</threat_model>

<verification>
- `resolve_chip` refuses absent/0 `algorithm` via reused `ChipNotImplementedError` before `convert_to_programmer` (SC#4); D-06 test proves `assert_not_called()`.
- Real supported chip still resolves (no false-positive regression).
- `chip_resolver.py` mypy-strict clean.
- Full host suite green; ruff/ruff-format clean; `check_dispatch.py` 0 violations; `test_dispatch_mirror.py` green; `chip_database.json` unchanged.
</verification>

<success_criteria>
- SC#4 (full): a chip entry lacking a usable `algorithm` is rejected with a clear, actionable error before any serial byte — verified by a host test on a deliberately-broken user-override, with `convert_to_programmer` never called.
- All host tests green; py3.11-target ruff/mypy clean; non-regression gates (`check_dispatch.py`, `test_dispatch_mirror.py`, DB identity) hold.
</success_criteria>

<output>
Create `.planning/phases/106-host-host-mem-type-removal/106-03-SUMMARY.md` when done.
</output>
