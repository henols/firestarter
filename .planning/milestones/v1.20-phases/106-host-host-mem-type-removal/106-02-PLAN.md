---
phase: 106-host-host-mem-type-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/firestarter/ic_layout.py
  - firestarter_app/firestarter/eprom_info.py
  - firestarter_app/tests/test_ic_layout.py
autonomous: true
requirements: [HOST-03]
must_haves:
  truths:
    - "`ic_layout.py` and `eprom_info.py` contain no `mem_type`-keyed numeric display-label fallback (`type_map`) and no `type_int`/`chip_type_int` parameter (SC#3)."
    - "`info`/`list`/`search` derive labels solely from `electrical.type` (tier 1) then protocol name (tier 2), landing on the bare string `\"Unknown\"` when neither resolves."
    - "No display-label regression: any chip that already resolved via `electrical.type` or protocol renders the identical label."
  artifacts:
    - "firestarter_app/firestarter/ic_layout.py (edited — `get_chip_type_string` + `resolve_type_label` signatures shrunk, `type_map` deleted)"
    - "firestarter_app/firestarter/eprom_info.py (edited — caller drops the dead positional arg)"
    - "firestarter_app/tests/test_ic_layout.py (edited — positional-call ripple updated)"
  key_links:
    - "`resolve_type_label` is the single shared label helper feeding BOTH `build_specifications` (info) and `print_eprom_list_table` (list/search) — editing it once fixes both views."
    - "Tiers `_ELECTRICAL_TYPE_LABEL` (`electrical.type` ground truth) and `_PROTOCOL_DISPLAY_NAME` (protocol) STAY; only the numeric third tier + its param are removed."
---

<objective>
Remove the `mem_type`-keyed numeric display-label fallback (HOST-03, D-03) from the shared label helper in `ic_layout.py` and clean the now-dead positional argument at every caller (`ic_layout.build_specifications`, `eprom_info.print_eprom_list_table`, and the in-file `__main__` self-test), then update the one positional test call that the signature change forces.

Purpose: `info`/`list`/`search` labels must derive from `electrical.type` and protocol only — the numeric `type_map` tier is the last vestige of the removed `mem_type` axis on the display side. Dropping the `type_int`/`chip_type_int` parameter removes the axis fully rather than leaving a vestigial param.

Output: `get_chip_type_string` and `resolve_type_label` with the numeric param dropped and `type_map` deleted (landing on `"Unknown"`); two runtime callers + one self-test block + one test call updated. No behavior regression for any chip that already resolved via `electrical.type`/protocol.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-host-host-mem-type-removal/106-CONTEXT.md
@.planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md
@.planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md
@firestarter_app/CLAUDE.md
</context>

<artifacts_this_phase_produces>
DELETION-focused. Removed symbols (drift verification MUST exclude these — intentionally gone):
- `type_map` (local dict, `ic_layout.py:222`)
- the `chip_type_int` parameter of `get_chip_type_string` (`ic_layout.py:204`)
- the `type_int` parameter of `resolve_type_label` (`ic_layout.py:502`) + its docstring `type_int:` Arg line (`:524`)
- the `f"Unknown ({chip_type_int})"` formatted fallback → replaced by the bare string `"Unknown"`
- the dead `.get("type", 0)` / literal positional args at all callers

Net-new symbols: NONE (label helpers keep their names; only signatures shrink).

Surviving tiers (MUST NOT be touched): `_ELECTRICAL_TYPE_LABEL` (`ic_layout.py:491–500`), `_PROTOCOL_DISPLAY_NAME` (`ic_layout.py:469–485`).

OUT OF SCOPE (do NOT touch — Pitfall 2 / Claude's-discretion confirmed): `eprom_info.py:69`'s `"type": "unknown"` is a STRING-typed raw-JSON display field in `_clean_config`'s `key_map`, a DIFFERENT axis from the numeric `mem_type`. Removing it would regress the `info`/`id` Type line. LEAVE it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Drop the numeric tier + param from the shared label helper in ic_layout.py</name>
  <files>firestarter_app/firestarter/ic_layout.py</files>
  <read_first>
    - firestarter_app/firestarter/ic_layout.py (edit target — read `get_chip_type_string` :203–223, `resolve_type_label` :502–534, the `build_specifications` caller :559–566, and the `__main__` self-test :707–709)
    - .planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md § "firestarter/ic_layout.py" (exact drop-last-tier + param directions, with the rename note: source names the param `chip_type_int` on `get_chip_type_string` and `type_int` on `resolve_type_label`)
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Verified Edit-Site Blast Radius" (line anchors) and § "Confirmed NON-targets" (the surviving tiers)
  </read_first>
  <action>
    Apply D-03 in `ic_layout.py`:
    (1) `get_chip_type_string` (`:203–223`): DROP the `chip_type_int` parameter so the signature becomes `def get_chip_type_string(self, protocol_id: int | None = None) -> str:`. KEEP the tier-2 protocol lookup (`if protocol_id is not None: if protocol_id in self._PROTOCOL_DISPLAY_NAME: return ...`). DELETE the `type_map = {1: "EPROM", 2: "Flash type 2", 3: "Flash type 3", 4: "SRAM"}` line and its return; REPLACE the final return with `return "Unknown"` (bare string — replaces the old `f"Unknown ({chip_type_int})"`).
    (2) `resolve_type_label` (`:502–534`): DROP the `type_int: int = 0` parameter so the signature is `resolve_type_label(self, electrical_type, protocol_id=None)` (keep the existing `Optional[str]` / `Optional[int]` annotations + `# noqa` markers as-is). KEEP the tier-1 `_ELECTRICAL_TYPE_LABEL` check. Update the delegating final line to `return self.get_chip_type_string(protocol_id)` (drop the `type_int` arg). REMOVE the `type_int:` line from the docstring Args (`:524`).
    (3) `build_specifications` caller (`:562–566`): DROP the dead positional `eprom_data.get("type", 0),` middle argument so the call is `self.resolve_type_label(electrical_type, eprom_data.get("protocol-id"))`.
    (4) `__main__` self-test block (`:708–709`): the two calls `get_chip_type_string(1)` and `get_chip_type_string(5)` pass the now-removed first positional param. UPDATE both — since these were exercising the deleted numeric tier, change them to protocol-based calls: use `get_chip_type_string(protocol_id=<a value present in _PROTOCOL_DISPLAY_NAME>)` for one and a value NOT present (to show the `"Unknown"` path) for the other, updating the surrounding log labels to match. This is a compile-forcing ripple, not a behavior contract — keep it minimal and self-consistent.
    Do NOT touch `_ELECTRICAL_TYPE_LABEL` or `_PROTOCOL_DISPLAY_NAME`.
  </action>
  <acceptance_criteria>
    - `grep -nE 'type_map|chip_type_int|type_int' firestarter/ic_layout.py` returns 0 matches.
    - `grep -n 'def get_chip_type_string' firestarter/ic_layout.py` shows a signature with `protocol_id` as the only value param (no `chip_type_int`).
    - `grep -n 'def resolve_type_label' firestarter/ic_layout.py` shows `electrical_type` + `protocol_id` params only (no `type_int`).
    - `grep -n '_ELECTRICAL_TYPE_LABEL\|_PROTOCOL_DISPLAY_NAME' firestarter/ic_layout.py` still shows both surviving tiers.
    - `cd firestarter_app && python -c "from firestarter.ic_layout import SpecBuilder" 2>/dev/null || python -c "import firestarter.ic_layout as m; import inspect; sb=[c for _,c in inspect.getmembers(m, inspect.isclass) if hasattr(c,'resolve_type_label')][0]; s=sb(); assert s.resolve_type_label('EPROM')=='EPROM'; assert s.resolve_type_label(None, 999)=='Unknown'; print('OK', s.resolve_type_label('EPROM'), s.resolve_type_label(None,999))"` exits 0 (tier-1 resolves, unresolved → "Unknown"). If the class name is not `SpecBuilder`, use the introspection fallback shown.
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && ! grep -qE 'type_map|chip_type_int|type_int' firestarter/ic_layout.py && python -c "import firestarter.ic_layout as m; import inspect; sb=[c for _,c in inspect.getmembers(m, inspect.isclass) if hasattr(c,'resolve_type_label')][0]; s=sb(); assert s.resolve_type_label('EPROM')=='EPROM'; assert s.resolve_type_label(None, 999)=='Unknown'; print('OK')"</automated>
  </verify>
  <done>`ic_layout.py` has no `type_map`, no `chip_type_int`/`type_int` param; `get_chip_type_string`/`resolve_type_label` derive labels from `electrical.type` → protocol → `"Unknown"`; `build_specifications` + the `__main__` self-test no longer pass the dead numeric arg; both surviving tiers intact.</done>
</task>

<task type="auto">
  <name>Task 2: Drop the dead positional arg at the eprom_info.py list/search caller + fix the test ripple</name>
  <files>firestarter_app/firestarter/eprom_info.py, firestarter_app/tests/test_ic_layout.py</files>
  <read_first>
    - firestarter_app/firestarter/eprom_info.py (edit target — the `print_eprom_list_table` caller at :405–410; and confirm :69 `"type": "unknown"` is the string field to LEAVE)
    - firestarter_app/tests/test_ic_layout.py (the positional `get_chip_type_string(0, pid)` call at :180 + surrounding single-source coverage test :168–188)
    - .planning/phases/106-host-host-mem-type-removal/106-PATTERNS.md § "firestarter/eprom_info.py" and § "tests/test_ic_layout.py"
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Pitfall 2: eprom_info.py:69 false-positive"
  </read_first>
  <action>
    (1) In `eprom_info.py` `print_eprom_list_table` (`:405–410`, the D-04 shared-helper Type-column call): DROP the dead positional `ic.get("type", 0),` middle argument so the call becomes `spec_builder.resolve_type_label(ic.get("electrical-type"), ic.get("protocol-id"))`. Leave the surrounding comment as-is (or trim the `mem_type` mention if present).
    (2) Do NOT touch `eprom_info.py:69` — `"type": "unknown"` is a string-typed raw-JSON display field in `_clean_config`'s `key_map`, unrelated to the numeric axis (Pitfall 2). Removing it would regress the `info`/`id` Type line.
    (3) In `tests/test_ic_layout.py` (`:180`): the call `spec_builder.get_chip_type_string(0, pid)` passes `0` as the now-removed first param. UPDATE it to `spec_builder.get_chip_type_string(pid)` — this test asserts the `_get_protocol_info_structured` type field equals the `get_chip_type_string` protocol fallback, so after the signature change `pid` must be the sole positional arg. This is a compile-forcing ripple (D-03 signature change); the assertion semantics are unchanged (protocol-based label equality).
  </action>
  <acceptance_criteria>
    - `grep -n 'resolve_type_label' firestarter/eprom_info.py` shows the caller with exactly two args (`electrical-type`, `protocol-id`) — no `.get("type"` middle arg.
    - `grep -n '"type": "unknown"' firestarter/eprom_info.py` STILL returns its match at ~:69 (string field left intact).
    - `grep -n 'get_chip_type_string(0' firestarter/tests/test_ic_layout.py` OR `grep -n 'get_chip_type_string(0' tests/test_ic_layout.py` returns 0 matches (the dead `0` positional dropped).
    - `cd firestarter_app && python -m pytest tests/test_ic_layout.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && ! grep -q 'get_chip_type_string(0' tests/test_ic_layout.py && grep -q '"type": "unknown"' firestarter/eprom_info.py && python -m pytest tests/test_ic_layout.py -q</automated>
  </verify>
  <done>`eprom_info.py`'s list/search Type-column call passes only `electrical-type` + `protocol-id`; the string-typed `"type": "unknown"` field is untouched; `test_ic_layout.py:180` calls `get_chip_type_string(pid)` positionally; `test_ic_layout.py` passes green with no label regression.</done>
</task>

<task type="auto">
  <name>Task 3: py3.11-target static gates on the touched display files</name>
  <files>firestarter_app/firestarter/ic_layout.py, firestarter_app/firestarter/eprom_info.py</files>
  <read_first>
    - firestarter_app/CLAUDE.md § tooling gate
    - .planning/phases/106-host-host-mem-type-removal/106-RESEARCH.md § "Pitfall 1" (py3.12-masks-CI-3.11) and § "Pitfall 5" (`ic_layout.py`/`eprom_info.py` are NOT in the mypy strict island)
  </read_first>
  <action>
    Run the CI-equivalent static gates against the py3.9/py3.11 analysis target on the edited display files. `ic_layout.py` and `eprom_info.py` are in the non-strict `follow_imports = "silent"` mypy list (NOT strict-gated — the strict `chip_resolver.py` is Plan 03's concern), so mypy is not the primary gate here, but `ruff check` + `ruff format --check` MUST be clean. If format flags a now-shrunk signature or empty region after deletion, run `ruff format` on the files and re-check. No py3.10+ idioms.
  </action>
  <acceptance_criteria>
    - `cd firestarter_app && ruff check firestarter/ic_layout.py firestarter/eprom_info.py tests/test_ic_layout.py` prints "All checks passed!".
    - `cd firestarter_app && ruff format --check firestarter/ic_layout.py firestarter/eprom_info.py tests/test_ic_layout.py` reports already-formatted.
  </acceptance_criteria>
  <verify>
    <automated>cd firestarter_app && ruff check firestarter/ic_layout.py firestarter/eprom_info.py tests/test_ic_layout.py && ruff format --check firestarter/ic_layout.py firestarter/eprom_info.py tests/test_ic_layout.py</automated>
  </verify>
  <done>`ruff check` + `ruff format --check` pass clean on the touched display files against the py3.11 analysis target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB record → display label | `resolve_type_label`/`get_chip_type_string` render the human label for `info`/`list`/`search`; no wire/dispatch impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-03 | Information disclosure | display label fallback | low | accept | Display-only change; the guard-relevant dispatch path is untouched. Worst case is a broken user-override renders `"Unknown"` instead of a numeric-derived label — a graceful degradation, not a leak. `info`/`list`/`search` bypass `resolve_chip` by design. |
| T-106-04 | Tampering | none | low | accept | No wire/dispatch key touched in this plan; `diff_db.py` unaffected (code-only, no DB change). |

No package installs in this plan → no supply-chain arm.
</threat_model>

<verification>
- `grep -nE 'type_map|chip_type_int|type_int' firestarter/ic_layout.py` → 0 (SC#3).
- Surviving tiers `_ELECTRICAL_TYPE_LABEL` + `_PROTOCOL_DISPLAY_NAME` present; `eprom_info.py:69` string field intact.
- `resolve_type_label('EPROM')=='EPROM'` and `resolve_type_label(None, 999)=='Unknown'` (tier-1 resolves; unresolved → bare "Unknown").
- `test_ic_layout.py` green (no label regression, positional ripple fixed).
- ruff/ruff-format clean on the py3.11 target.
</verification>

<success_criteria>
- SC#3 (full): `ic_layout.py`/`eprom_info.py` contain no numeric `type_map`/`type_int` fallback; labels derive from `electrical.type`/protocol only, landing on `"Unknown"`; no behavior regression for any chip that already resolved correctly.
- All touched tests green; ruff/ruff-format clean on the py3.11 target.
</success_criteria>

<output>
Create `.planning/phases/106-host-host-mem-type-removal/106-02-SUMMARY.md` when done.
</output>
