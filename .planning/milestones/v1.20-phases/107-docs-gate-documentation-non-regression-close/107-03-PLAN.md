---
phase: 107-docs-gate-documentation-non-regression-close
plan: 03
type: execute
wave: 2
depends_on: ["107-01", "107-02"]
files_modified: []
autonomous: true
requirements: [GATE-01, GATE-02, SAFE-01]

must_haves:
  truths:
    - "v1.16 golden/frame vectors + dispatch-mirror guard re-run GREEN; check_dispatch.py reports 0 violations (746 chips); diff_db.py shows no chip_database.json value change for any real chip (GATE-01)"
    - "Full native suite (pio test -e native) passes; host pytest introduces NO NEW failure vs the beta baseline; dual-repo constants parity holds; py3.11-target ruff/ruff-format clean-or-matches-pre-existing-baseline (GATE-02, D-07)"
    - "Over-voltage stays blocked at the firmware VPP check AND every currently-dispatchable DB chip routes to its identical handler via protocol alone — proven by the existing native dispatch tests + check_dispatch.py, no new test added (SAFE-01)"
    - "Any REAL new regression (diff_db value change on a real chip, check_dispatch violation, golden/dispatch-mirror mismatch, or a NEW failing test attributable to Phase 105/106/107) is surfaced as a BLOCKING finding, never auto-fixed or baseline-regenerated (D-04, D-05)"
    - "The documented pre-existing baseline (test_golden_file_matches fail + ruff/format dirt in v1.20-untouched files) is asserted as prior debt, NOT fixed here (D-07)"
  artifacts:
    - "107-03-SUMMARY.md capturing each gate command + its GREEN (or matches-baseline) output as the non-regression evidence record"
  key_links:
    - "This gate sweep runs AFTER the doc scrub (107-01) and 0xAE codegen removal (107-02) land, so it verifies the fully-applied state"
    - "GATE-02 pass bar is 'no NEW regression vs git diff beta..HEAD scope', not absolute green (D-07)"
    - "check_dispatch.py's own _ALGO_MEM_TYPE is an intentional verification-tool artifact — RUN it, do NOT flag/remove it (D-05)"
---

<objective>
Re-run every non-regression gate at close and assert each is green (or matches the documented pre-existing baseline), proving Phases 105/106/107 changed no dispatch outcome and over-voltage stays blocked.

Purpose: The entire point of this milestone-closing phase is to PROVE the removed mem_type fallback was dead code for every real chip and that no regression was introduced (GATE-01, GATE-02, SAFE-01). This plan runs the full gate surface after the doc + codegen changes land. It adds NO new tests — SAFE-01 is fully covered by the existing native dispatch tests + check_dispatch.py + dispatch-mirror.

Output: An evidence record (in the SUMMARY) of each gate command and its green-or-matches-baseline result. Any REAL new regression STOPS the phase as a blocking finding (D-04) — never auto-fixed, never baseline-regenerated (D-05).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-CONTEXT.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-RESEARCH.md
@.planning/phases/106-host-host-mem-type-removal/106-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the GATE-01 + SAFE-01 dispatch-non-regression gates (native suite, dispatch-mirror, check_dispatch, diff_db)</name>
  <files>(no files modified — verification run only)</files>
  <read_first>
    - 107-RESEARCH.md "## Gate Command Inventory (GATE-01, GATE-02)" table (exact commands + expected results: native 80/80, dispatch-mirror 2 tests PASS, check_dispatch 0 violations/746 chips, diff_db 0 unexplained)
    - 107-RESEARCH.md "### v1.16 golden register traces — naming clarification" (golden = frame_vectors + dispatch suite inside pio test -e native; do NOT hunt for a separate register-trace harness)
    - 107-RESEARCH.md Pitfall #5 (check_dispatch.py's _ALGO_MEM_TYPE is a verification artifact — RUN, do not touch — D-05)
    - 107-RESEARCH.md "## SAFE-01 Verification Approach" (SAFE-01 covered by these same gates + native test_val_* handler suites; no new test)
  </read_first>
  <action>
    Run the dispatch non-regression gates in sequence and capture each command's exit code + summary line for the SUMMARY evidence record. Do NOT regenerate any baseline, golden fixture, frame vector, or the dispatch-mirror tool model (D-05).
    1. Native suite (firmware, host-compiled — GATE-01/GATE-02/SAFE-01): `cd /workspaces/firestarter && pio test -e native` — expect ~80/80 PASS (includes test_frame_vectors golden, test_dispatch/test_configure_memory dispatch arms, configure_not_implemented fail-closed, and the test_val_* handler-validation suites that exercise SAFE-01's over-voltage/VPP-regulator paths).
    2. Dispatch-mirror guard (GATE-01): `cd /workspaces/firestarter_app && python -m pytest tests/test_dispatch_mirror.py -q` — expect PASS (2 tests) — the three-way doc/tool/firmware bind.
    3. Dispatch scanner (GATE-01 + SAFE-01 identical-handler routing): `cd /workspaces/firestarter_app && python tools/check_dispatch.py` — expect exit 0, 746 chips, 0 non_supported_dispatchable, 0 dispatch regressions, 0 consistency violations.
    4. DB value-diff (GATE-01): `cd /workspaces/firestarter_app && python tools/diff_db.py` — expect exit 0, 0 unexplained changes (the 2 pre-explained W29C020/W29C040 page_size changes from Phase 94 are acceptable; 0 NEW/removed/unexplained).
    D-04 disposition: if native fails, or dispatch-mirror fails, or check_dispatch reports any violation, or diff_db reports an unexplained/new real-chip value change — STOP and surface it as a BLOCKING finding attributable to 105/106/107. Do NOT auto-fix, silence, or regenerate baselines.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python tools/check_dispatch.py && python tools/diff_db.py && python -m pytest tests/test_dispatch_mirror.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` (in firestarter/) exits 0 with ~80/80 PASS (no failed tests; includes frame-vector golden + dispatch + test_val_* handler suites)
    - `python -m pytest tests/test_dispatch_mirror.py -q` (in firestarter_app/) passes (2 tests)
    - `python tools/check_dispatch.py` exits 0: 746 chips, 0 non_supported_dispatchable, 0 dispatch regressions, 0 consistency violations
    - `python tools/diff_db.py` exits 0: 0 unexplained / 0 new / 0 removed real-chip value changes (only the pre-explained Phase-94 page_size deltas may appear)
    - No baseline / golden fixture / frame_vectors / dispatch-mirror tool model was modified during this task
  </acceptance_criteria>
  <done>All four GATE-01/SAFE-01 dispatch gates re-run green with no baseline touched, proving the mem_type-axis removal changed no dispatch outcome and over-voltage stays blocked (fail-closed dispatch + no chip routes to configure_eprom on a vpp-less pinout).</done>
</task>

<task type="auto">
  <name>Task 2: Run the GATE-02 full-suite gates (native + host pytest + constants parity + ruff/ruff-format/mypy) against the beta baseline</name>
  <files>(no files modified — verification run only)</files>
  <read_first>
    - 107-RESEARCH.md Pitfall #1 (the exact pre-existing red baseline: test_audit_coverage_matrix.py::test_golden_file_matches FAILS 186034 vs 184631 bytes; ruff check 4 errors in tools/catalog/codegen_vectors.py + tools/audit_coverage_matrix.py; ruff format 1 file tests/test_validate_family_cmd.py — ALL in files NOT in git diff beta..HEAD)
    - 107-RESEARCH.md Pitfall #3 (py3.12-masks-CI-py3.11: run ruff check + ruff format --check explicitly — ruff pins target-version from pyproject.toml; python3.11 binary absent; record py3.12-analysis disposition)
    - 107-RESEARCH.md Pitfall #4 (firestarter_app test env; use /usr/local python; restore with pip install -e '.[test]' if wiped; verify mypy actually runs — the hardened gate prints OK even when mypy is missing)
    - 106-03-SUMMARY.md "py3.11 static gate" note (the disposition precedent for the absent py3.11 binary)
  </read_first>
  <action>
    Run the GATE-02 full-suite gates and assert NO NEW regression versus the beta baseline (D-07 pass bar = `git diff beta..HEAD` scope, NOT absolute green). Capture each command's summary for the SUMMARY evidence record.
    1. Native full suite (GATE-02): already covered by Task 1's `pio test -e native` (80/80) — cite it, no re-run needed.
    2. Host suite + constants parity (GATE-02): `cd /workspaces/firestarter_app && python -m pytest -q` — expect the documented baseline: all pass EXCEPT the single pre-existing `tests/test_audit_coverage_matrix.py::test_golden_file_matches` failure (byte-size golden drift, unrelated to mem_type, reproduced pre-105). The constants-parity test (tests/test_revision_constants_parity.py, reads firestarter/include/*.h) must be among the passing set. If the ONLY failure is test_golden_file_matches -> GATE-02 PASSES (pre-existing debt per D-07). If ANY OTHER test fails, or test_golden_file_matches now fails for a NEW reason attributable to 105/106/107 -> STOP, blocking finding (D-04).
    3. ruff lint (GATE-02): `cd /workspaces/firestarter_app && ruff check firestarter/ tests/ tools/` — expect exactly the 4 pre-existing errors in tools/catalog/codegen_vectors.py + tools/audit_coverage_matrix.py (files NOT in git diff beta..HEAD). No NEW ruff error in any v1.20-touched file (notably messages.py must be clean — it is codegen-emitted ruff-clean).
    4. ruff format (GATE-02): `cd /workspaces/firestarter_app && ruff format --check firestarter/ tests/` — expect exactly the 1 pre-existing file (tests/test_validate_family_cmd.py) would reformat. No NEW format drift in any v1.20-touched file.
    5. mypy (GATE-02): run mypy per firestarter_app/CLAUDE.md's tooling gate; first CONFIRM mypy is actually installed/runs (`mypy --version` -> expect 2.1.0 at ~/.local/bin/mypy) — do NOT trust a bare "OK" (Pitfall #4). Expect clean on the strict-typed modules; record the py3.12-analysis / py3.11-binary-absent disposition (Pitfall #3, matching 106-03).
    To scope "new vs pre-existing", run `git diff --name-only beta..HEAD` in each sub-repo and confirm every failing/dirty file is OUTSIDE that diff (pre-existing) — a failing file INSIDE the diff is a NEW regression (D-04 blocker).
    Do NOT fix any pre-existing failure or lint here — that is out-of-scope code change; log it as documented prior debt (D-07).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python -m pytest -q -k "revision_constants_parity" && ruff check firestarter/ | tail -5 && mypy --version</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest -q` (in firestarter_app/) shows the documented baseline: only `test_audit_coverage_matrix.py::test_golden_file_matches` fails; every other test (incl. test_revision_constants_parity) passes
    - No failing test file is inside `git diff --name-only beta..HEAD` (i.e. no NEW regression attributable to v1.20)
    - `ruff check firestarter/ tests/ tools/` reports only the 4 pre-existing errors (in codegen_vectors.py + audit_coverage_matrix.py); messages.py and all v1.20-touched files are ruff-clean
    - `ruff format --check firestarter/ tests/` reports only the 1 pre-existing file (test_validate_family_cmd.py)
    - `mypy --version` confirms mypy is actually installed (not a phantom OK); mypy run is clean or matches the documented py3.11-binary-absent disposition
    - No pre-existing failure/lint was "fixed" (working tree for those files unchanged)
  </acceptance_criteria>
  <done>The GATE-02 full-suite gates confirm no NEW regression vs the beta baseline: native 80/80, host pytest green modulo the single documented pre-existing test_golden_file_matches failure, constants parity holds, and ruff/ruff-format/mypy show only the documented pre-existing dirt — all recorded as prior debt per D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host->firmware dispatch | Every dispatchable chip must route to its identical handler via protocol alone (no mem_type fallback); over-voltage must stay blocked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-05 | Tampering / Denial | over-voltage (12V VPP on a 5V part) | critical | mitigate | SAFE-01 re-verified: fail-closed dispatch (protocol==0/unknown -> configure_not_implemented, no regulator engagement) + check_dispatch structural guard (no chip routes to configure_eprom on a vpp-less pinout) + native test_val_* handler suites — all asserted GREEN this run |
| T-107-06 | Repudiation (hidden regression) | pre-existing red baseline mistaken for a v1.20 regression | medium | mitigate | Baseline the documented pre-existing failures; scope pass bar to git diff beta..HEAD; a failing file inside that diff is a D-04 blocker, never auto-fixed |
</threat_model>

<verification>
- `pio test -e native` (firestarter/): 80/80 PASS
- `python tools/check_dispatch.py`: exit 0, 0 violations, 746 chips
- `python tools/diff_db.py`: exit 0, 0 unexplained/new/removed real-chip changes
- `python -m pytest tests/test_dispatch_mirror.py -q`: PASS (2 tests)
- `python -m pytest -q` (firestarter_app/): only the pre-existing test_golden_file_matches fails (D-07 baseline)
- `ruff check` / `ruff format --check`: only the documented pre-existing dirt; no NEW drift in v1.20-touched files
- `mypy --version` confirms mypy runs; type gate clean-or-documented-disposition
- No baseline, golden fixture, frame vector, or pre-existing failure was modified/regenerated/fixed (D-04, D-05, D-07)
</verification>

<success_criteria>
Every non-regression gate re-runs green (or matches the documented pre-existing beta baseline): dispatch outcomes unchanged (GATE-01), full native + host suites + parity + lint show no NEW regression (GATE-02), and over-voltage stays blocked with every dispatchable chip routing via protocol alone (SAFE-01) — with no baseline touched and any real regression surfaced as a blocking finding.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces NO code symbols and modifies NO source files — it is a verification-only gate sweep. It produces:
- EVIDENCE RECORD: 107-03-SUMMARY.md documenting each gate command + its green (or matches-baseline) result, serving as the milestone-close non-regression proof
- DOCUMENTED BASELINE: the pre-existing red set (test_golden_file_matches + ruff/format dirt) recorded as prior debt (D-07), NOT fixed
- PRESERVED (explicitly not touched): all golden fixtures, frame vectors, tools/baseline/*.json, and check_dispatch.py's _ALGO_MEM_TYPE verification model (D-05)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/107-docs-gate-documentation-non-regression-close/107-03-SUMMARY.md` when done
</output>
