---
phase: 107-docs-gate-documentation-non-regression-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/catalog/messages.toml
  - firestarter/tools/catalog/messages.toml
  - firestarter_app/tools/catalog/messages.toml
  - firestarter_app/firestarter/messages.py
  - firestarter/include/messages.h
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "The retired MSG_ERR_MEM_TYPE_UNSUPPORTED (0xAE) constant is removed from the canonical messages.toml (meta + both vendored sub-repo copies) (D-06)"
    - "messages.py and messages.h are REGENERATED via codegen.py (never hand-edited) and no longer define 0xAE / MSG_ERR_MEM_TYPE_UNSUPPORTED (D-06)"
    - "The codegen drift/check gate is clean after the edit (canonical toml <-> generated artifacts in sync) (D-06)"
    - "The change is scoped STRICTLY to removing the dead 0xAE message — no other message id, format, or param is altered (D-06)"
  artifacts:
    - "Regenerated firestarter_app/firestarter/messages.py (0xAE-free)"
    - "Regenerated firestarter/include/messages.h (already 0xAE-free; confirm unchanged-or-clean after regen)"
  key_links:
    - "sync_to_subrepos.sh is the canonical regen path: edit meta canonical toml -> script copies to both sub-repos + regenerates messages.h/messages.py; both vendored toml copies must stay byte-identical"
    - "codegen.py --check must exit 0 after regen (drift gate)"
---

<objective>
Remove the dead, retired MSG_ERR_MEM_TYPE_UNSUPPORTED (0xAE) message constant from the canonical catalog and regenerate the host/firmware artifacts, so v1.20 closes with no `mem_type` residue anywhere.

Purpose: Phase 105 removed 0xAE from the firmware `messages.h`/`messages.toml` but left the meta canonical `tools/catalog/messages.toml` (line ~450) and the host copies (`firestarter_app/tools/catalog/messages.toml` + generated `messages.py`) stale — a cross-repo codegen desync (FW-03 near-miss). The operator authorized reconciling it in this phase (D-06). This is the ONLY sanctioned code/codegen change in this docs+gate phase, scoped strictly to the dead 0xAE constant.

Output: The `[[messages]]` block for 0xAE removed from the canonical messages.toml, propagated to both sub-repo vendored copies, and messages.py/messages.h regenerated via codegen.py. Codegen drift gate clean.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-CONTEXT.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-RESEARCH.md
@.planning/phases/105-fw-firmware-mem-type-removal/105-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the 0xAE [[messages]] block from the canonical messages.toml and regenerate via sync_to_subrepos.sh</name>
  <files>tools/catalog/messages.toml, firestarter/tools/catalog/messages.toml, firestarter_app/tools/catalog/messages.toml, firestarter_app/firestarter/messages.py, firestarter/include/messages.h</files>
  <read_first>
    - tools/catalog/messages.toml (the meta canonical catalog — read lines 445-465: the 0xAE `[[messages]]` block sits between the 0x?? "Command error" block and the 0xAF MSG_ERR_VERIFY block)
    - tools/catalog/sync_to_subrepos.sh (the canonical regen path — copies meta toml to both sub-repos then regenerates messages.h + messages.py; run it, do not hand-edit generated files)
    - firestarter_app/firestarter/messages.py (header confirms "DO NOT EDIT -- generated by codegen.py" — NEVER hand-edit; 0xAE currently at lines 99 + 538-540)
    - .planning/phases/105-fw-firmware-mem-type-removal/105-01-SUMMARY.md (firmware already retired 0xAE — this reconciles the host + canonical copies)
  </read_first>
  <action>
    Delete the entire `[[messages]]` block defining id = 0xAE / name = "MSG_ERR_MEM_TYPE_UNSUPPORTED" (severity ERROR, format "Memory type 0x%02x not supported", the u8 hex_byte param, wire_format id_frame) from the meta canonical tools/catalog/messages.toml (~lines 449-455). Delete only that one block — leave the surrounding "Command error" block and the 0xAF MSG_ERR_VERIFY block byte-identical (D-06, strictly scoped to 0xAE).
    Then run the canonical regen path from the meta catalog dir:
      cd /workspaces && bash tools/catalog/sync_to_subrepos.sh
    This copies the edited canonical toml into both sub-repos' tools/catalog/messages.toml (asserting they are byte-identical to each other), and regenerates firestarter/include/messages.h (cpp) and firestarter_app/firestarter/messages.py (python) via codegen.py. Do NOT hand-edit messages.py or messages.h — the sync script regenerates them. If sync_to_subrepos.sh is unavailable, fall back to: edit the canonical toml, cp it to both sub-repo tools/catalog/, then run codegen.py --catalog <toml> --language python --target firestarter_app/firestarter/messages.py and --language cpp --target firestarter/include/messages.h.
    Note: firestarter/include/messages.h is already 0xAE-free (Phase 105); regeneration should leave it functionally clean/unchanged — do NOT treat a no-op there as an error.
  </action>
  <verify>
    <automated>cd /workspaces && ! grep -rqn 'MSG_ERR_MEM_TYPE_UNSUPPORTED\|0xAE\|0xae' tools/catalog/messages.toml firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml firestarter_app/firestarter/messages.py && echo "0xAE_REMOVED_ALL" || echo "RESIDUAL_0xAE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'MSG_ERR_MEM_TYPE_UNSUPPORTED' tools/catalog/messages.toml firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml firestarter_app/firestarter/messages.py firestarter/include/messages.h` returns 0 hits across all five files
    - `grep -rn '0xAE\|0xae' firestarter_app/firestarter/messages.py` returns 0 hits
    - `diff -q firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml` reports the two vendored copies are identical (sync invariant)
    - The 0xAF MSG_ERR_VERIFY block still present and unchanged in the canonical toml (`grep -c 'MSG_ERR_VERIFY' tools/catalog/messages.toml` returns 1)
  </acceptance_criteria>
  <done>The 0xAE MSG_ERR_MEM_TYPE_UNSUPPORTED message is gone from the canonical + both vendored messages.toml, and messages.py/messages.h are regenerated 0xAE-free with no other message altered.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the codegen drift gate is clean after the 0xAE removal</name>
  <files>firestarter_app/firestarter/messages.py</files>
  <read_first>
    - firestarter_app/tools/catalog/codegen.py (read lines 671-720: the argparser — `--check` validates the catalog and exits 0/1; regen with `--language`/`--target`)
    - 107-RESEARCH.md "Runtime State Inventory" (the 0xAE desync row) + "State of the Art" table
  </read_first>
  <action>
    Verify the regenerated artifacts are in sync with the edited canonical catalog (D-06 drift gate). Run codegen in check mode against the vendored host catalog:
      cd /workspaces/firestarter_app && python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check
    (expect exit 0 / "OK: catalog valid").
    Then confirm regeneration is idempotent (no drift) by regenerating messages.py to a temp target and diffing against the committed file:
      cd /workspaces/firestarter_app && python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --language python --target /tmp/claude-1000/messages_regen.py && diff -q /tmp/claude-1000/messages_regen.py firestarter/messages.py
    A clean diff proves messages.py is a faithful regeneration (no hand-edit drift). Do the equivalent cpp check if the firmware messages.h is in scope of the local drift gate. Do NOT edit any generated file to force the diff clean — if the diff is non-empty, that is a real drift finding to surface (D-04-adjacent), STOP and report.
    Do NOT run ruff-fix or reformat here — messages.py is codegen-emitted and already ruff-clean per the emitter (v1.12 Phase 63). Any incidental lint on messages.py is a codegen-emitter concern, not a hand-fix.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check && echo "CATALOG_VALID"</automated>
  </verify>
  <acceptance_criteria>
    - `python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0 and prints "OK: catalog valid (…)"
    - Regenerating messages.py to a temp target and diffing against firestarter/messages.py produces an EMPTY diff (no drift — the committed file is a faithful regen)
    - No generated file was hand-edited to achieve the clean state
  </acceptance_criteria>
  <done>The codegen check gate exits 0 and messages.py is a byte-faithful regeneration of the 0xAE-free canonical catalog — the cross-repo codegen desync is reconciled with no drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical catalog -> generated artifacts | codegen.py must faithfully regenerate; hand-edits create drift the CI gate catches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-03 | Tampering (codegen drift) | messages.py / messages.h vs canonical toml | medium | mitigate | Regenerate via sync_to_subrepos.sh / codegen.py; never hand-edit generated files; verify --check exits 0 and regen diff is empty |
| T-107-04 | Denial (accidental over-scope) | messages.toml (removing more than 0xAE) | low | mitigate | Scope edit strictly to the single 0xAE [[messages]] block; assert 0xAF and neighbors unchanged via grep |
</threat_model>

<verification>
- `grep -rn 'MSG_ERR_MEM_TYPE_UNSUPPORTED\|0xAE' tools/catalog/messages.toml firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml firestarter_app/firestarter/messages.py firestarter/include/messages.h` returns 0 hits
- `diff -q firestarter/tools/catalog/messages.toml firestarter_app/tools/catalog/messages.toml` is clean (sync invariant)
- `python tools/catalog/codegen.py --catalog tools/catalog/messages.toml --check` exits 0
- Regen-to-temp diff of messages.py is empty (no hand-edit drift)
</verification>

<success_criteria>
The retired 0xAE MSG_ERR_MEM_TYPE_UNSUPPORTED constant is removed from the canonical catalog and every generated/vendored copy, artifacts are regenerated (not hand-edited) via the canonical codegen path, and the drift gate is clean — with no other message definition altered.
</success_criteria>

<artifacts_this_phase_produces>
This plan REMOVES a symbol; it produces no new symbols:
- REMOVED constant: `MSG_ERR_MEM_TYPE_UNSUPPORTED = 0xAE` (from canonical + both vendored messages.toml, and regenerated out of messages.py; firmware messages.h already clean)
- REGENERATED artifact: firestarter_app/firestarter/messages.py (0xAE-free)
- REGENERATED artifact: firestarter/include/messages.h (confirmed 0xAE-free)
- PRESERVED: all other message ids including 0xAF MSG_ERR_VERIFY and 0xBB MSG_ERR_PROTOCOL_NOT_IMPLEMENTED (unchanged)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/107-docs-gate-documentation-non-regression-close/107-02-SUMMARY.md` when done
</output>
