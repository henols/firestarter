---
phase: 107-docs-gate-documentation-non-regression-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter/CLAUDE.md
  - firestarter/README.md
  - firestarter_app/CLAUDE.md
  - firestarter_app/README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "firestarter/CLAUDE.md dispatch narrative no longer describes the removed steps 7-11 mem_type fallback chain; protocol==0 and any unrecognized protocol fail-close to configure_not_implemented() / MSG_ERR_PROTOCOL_NOT_IMPLEMENTED (0xBB) (D-02, D-03)"
    - "The legacy `type` wire-field bullet is gone from firestarter/CLAUDE.md JSON Wire Protocol key-field list (D-02)"
    - "The stale `\"type\": 1` line is removed from the firestarter_app/CLAUDE.md Wire Protocol example (D-08)"
    - "Both sub-repo READMEs carry a new `## Breaking Changes (v1.20)` section recording: type removed from the wire, every chip entry now requires a usable `algorithm`, pre-v1.20 hosts emitting a stray `type` stay safe (D-01, D-09)"
    - "The v1.16 electrical.type STRING semantics and infoic `type=N` identity tuples are PRESERVED untouched (D-03)"
  artifacts:
    - "firestarter/README.md `## Breaking Changes (v1.20)` section"
    - "firestarter_app/README.md `## Breaking Changes (v1.20)` section"
  key_links:
    - "Scrubbed dispatch narrative must match shipped firestarter/src/proms/memory.cpp reality (single terminal configure_not_implemented) per Phase 105 SUMMARY"
    - "Breaking-change wording must mirror the existing `## Breaking Changes (v1.10)` template shape (### subsystem + Upgrade: line)"
---

<objective>
Scrub the now-false `type`/`mem_type` wire/dispatch references out of the firmware and host agent docs, and record the v1.20 breaking wire-contract change in both sub-repo READMEs.

Purpose: Phases 105 (firmware) and 106 (host) removed the `mem_type`/`type` dispatch axis. The docs still describe the deleted steps 7-11 fallback chain and list `type` as a wire field — this makes the agent-facing docs actively misleading. This plan brings the prose in line with the shipped fail-closed reality and records the breaking change for future readers and pre-v1.20 hosts (DOC-01).

Output: Edited firestarter/CLAUDE.md, firestarter_app/CLAUDE.md, and a new `## Breaking Changes (v1.20)` section in both READMEs. NO code/codegen change in this plan (0xAE removal is plan 02). NO edit to firestarter/doc/PROTOCOLS.md (verify-only — its `type=N` tokens are out-of-scope infoic identity tuples).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-CONTEXT.md
@.planning/phases/107-docs-gate-documentation-non-regression-close/107-RESEARCH.md
@.planning/phases/105-fw-firmware-mem-type-removal/105-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub the mem_type dispatch narrative + legacy type wire bullet from firestarter/CLAUDE.md</name>
  <files>firestarter/CLAUDE.md</files>
  <read_first>
    - firestarter/CLAUDE.md (the file being edited — read lines 16-100: the "### Protocol Dispatch" section and "### JSON Wire Protocol" key-field list)
    - .planning/phases/105-fw-firmware-mem-type-removal/105-01-SUMMARY.md (authoritative record of what firmware removed: configure_memory() now has a single terminal configure_not_implemented(handle); mem_type field + TYPE_* defines removed; json_parser.c no longer parses `type`)
    - 107-RESEARCH.md "## Doc Edit Sites (DOC-01)" table (exact line-by-line edit instructions for lines 20-24, 28-31, 49-53, 55-57, 59-63, 90)
  </read_first>
  <action>
    Rewrite the "### Protocol Dispatch" section of firestarter/CLAUDE.md so it reflects single-axis protocol-only dispatch (D-02, D-03):
    - Lines ~20-24: Remove the "**before** dispatching on handle->mem_type" clause and the `mem_type=1`/TYPE_EPROM examples. State the firmware dispatches SOLELY on handle->protocol (populated from the `algorithm` JSON field). Preserve the 12V-VPP-on-5V-part hazard note but rephrase it in protocol terms: SRAM protocols 0x0E/0x27/0x28/0x29 route to configure_sram (never configure_eprom).
    - Lines ~28-31: Delete the "The `mem_type` chain is retained as a backward-compatibility fallback…" sentence entirely. Replace with a statement that there is NO mem_type fallback: a command with no recognized protocol (including protocol == 0) fail-closes.
    - Lines ~49-53: DELETE dispatch steps 7-11 outright (the `protocol == 0` mem_type arms and the "Memory type 0x%02x not supported" error). Renumber the list so the terminal arm is: protocol == 0 (and any unrecognized protocol) reaches configure_not_implemented() returning MSG_ERR_PROTOCOL_NOT_IMPLEMENTED (0xBB). This must match shipped memory.cpp (single terminal unconditional configure_not_implemented per Phase 105 SUMMARY).
    - Lines ~55-57: DELETE the "There is no mem_type == 2 dispatch case … reaches step 11" paragraph — step 11 no longer exists; algorithm == 0 now reaches configure_not_implemented().
    - Lines ~59-63: Keep the "Fail-closed invariant (Phase 64)" statement (arms 6a/6b remain shipped), but delete all "…never falls through to the mem_type chain / mem_type fallback (steps 7-11) is unreachable" language — there is no mem_type chain to fall through to anymore.
    - Line ~90: DELETE the `- type — legacy mem_type integer (fallback when algorithm is absent)` bullet from the "### JSON Wire Protocol → Key fields" list. Add a one-line note that a legacy `type` key is no longer parsed (silently skipped as an unknown JSON field by json_parser.c).
    Do NOT touch the "### Algorithm Handlers" PROTO_ table (lines ~65-82) or any electrical.type reference. Do NOT touch the arms 1-6/6a/6b dispatch descriptions except renumbering after step-7-11 deletion.
    Do NOT introduce a fenced code block; edit the existing prose/list in place.
  </action>
  <verify>
    <automated>cd /workspaces && grep -nE 'mem_type|Memory type 0x|steps 7' firestarter/CLAUDE.md | grep -v '^[0-9]*: *#' ; test -z "$(grep -nE 'mem_type|steps 7-11|Memory type 0x' firestarter/CLAUDE.md)" && echo "SCRUB_CLEAN" || echo "RESIDUAL_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'mem_type' firestarter/CLAUDE.md` returns 0 hits (the mem_type dispatch chain and legacy type bullet are gone)
    - `grep -n 'Memory type 0x%02x not supported' firestarter/CLAUDE.md` returns 0 hits (step 11 error text deleted)
    - `grep -n 'legacy' firestarter/CLAUDE.md` does not describe `type` as a fallback wire field
    - `grep -n 'configure_not_implemented' firestarter/CLAUDE.md` still present (terminal fail-closed arm preserved/renumbered)
    - The "### Algorithm Handlers" PROTO_ table and all electrical.type references are byte-unchanged
  </acceptance_criteria>
  <done>firestarter/CLAUDE.md dispatch narrative describes protocol-only dispatch with a fail-closed terminal arm, contains no mem_type/steps-7-11 references, and the legacy `type` wire bullet is removed; electrical.type semantics and the PROTO_ handler table are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the stale "type": 1 wire example line from firestarter_app/CLAUDE.md</name>
  <files>firestarter_app/CLAUDE.md</files>
  <read_first>
    - firestarter_app/CLAUDE.md (the file being edited — read lines 52-72: the "### Wire Protocol" example write command JSON block)
    - 107-RESEARCH.md "Open Questions" #1 (the live `"type": 1` wire example at line ~58 is the numeric wire axis, NOT electrical.type — scrub it per D-08)
  </read_first>
  <action>
    In firestarter_app/CLAUDE.md "### Wire Protocol" section, delete the single `"type": 1,` line inside the example write-command JSON block (~line 58) per D-08. This is the numeric wire `type` (= removed mem_type), which the firmware no longer parses — leaving it makes the example misleading. Do NOT touch the `"algorithm": 7,` line or any other field in the example. Do NOT touch any `electrical.type` reference elsewhere in the file. The JSON block must remain valid (no dangling trailing comma issue — the removed line already ends in a comma and the following `"algorithm"` line remains, so the block stays valid).
  </action>
  <verify>
    <automated>cd /workspaces && grep -nE '"type"' firestarter_app/CLAUDE.md; test -z "$(grep -nE '\"type\"[[:space:]]*:[[:space:]]*[0-9]' firestarter_app/CLAUDE.md)" && echo "TYPE_EXAMPLE_CLEAN" || echo "RESIDUAL_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"type"[[:space:]]*:[[:space:]]*[0-9]' firestarter_app/CLAUDE.md` returns 0 hits (numeric wire type example removed)
    - The `"algorithm": 7,` line in the same JSON example is still present and unchanged
    - Any `electrical.type` string reference in the file is byte-unchanged
  </acceptance_criteria>
  <done>The example write-command JSON in firestarter_app/CLAUDE.md no longer carries a numeric `"type"` wire field; the algorithm-based dispatch example is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add ## Breaking Changes (v1.20) section to both sub-repo READMEs</name>
  <files>firestarter/README.md, firestarter_app/README.md</files>
  <read_first>
    - firestarter/README.md (read the existing `## Breaking Changes (v1.10)` section starting at line 17 — the template shape to mirror: ### subsystem heading + Upgrade: line)
    - firestarter_app/README.md (read the existing `## Breaking Changes (v1.10)` section starting at line 61 — same template shape)
    - 107-RESEARCH.md "## Breaking-Change Record Wording (D-01)" (the four points the record must convey)
  </read_first>
  <action>
    Add a new `## Breaking Changes (v1.20)` section to BOTH README files (D-01, D-09), placed immediately before the existing `## Breaking Changes (v1.10)` section (newest-first ordering) or in the same relative position the repo uses — mirror the existing v1.10 section's exact shape (a `### <subsystem>` subheading plus a bolded `**Upgrade:**` line). The section MUST convey these points (wording is your discretion, prose only, no fenced JSON):
    1. The `type` field is removed from the host->firmware wire. `algorithm` (upstream minipro protocol_id) is now the sole dispatch key.
    2. Every chip entry (built-in or user-override) now requires a usable non-zero `algorithm`; a chip lacking one is refused in-host (chip_resolver raises before any serial byte — HOST-04, Phase 106) — there is no silent mem_type-derived fallback dispatch.
    3. Pre-v1.20 hosts emitting a stray `type` key remain SAFE: the firmware silently skips unknown JSON fields, so `type` simply no longer does anything (not a crash).
    4. Upgrade guidance: upgrade both firmware and host together (mirror the v1.10 "upgrade both" framing); the only behavioral loss is the mem_type fallback, which was dead code for every real chip.
    Cite the decision anchor per D-01 in prose is not required, but the section heading must read exactly `## Breaking Changes (v1.20)`.
  </action>
  <verify>
    <automated>cd /workspaces && grep -c '^## Breaking Changes (v1.20)' firestarter/README.md firestarter_app/README.md && grep -l 'algorithm' firestarter/README.md firestarter_app/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Breaking Changes (v1.20)' firestarter/README.md` returns 1
    - `grep -c '^## Breaking Changes (v1.20)' firestarter_app/README.md` returns 1
    - Each new section mentions the `type` field removal AND the `algorithm`-required rule AND an Upgrade line (grep for `algorithm`, `Upgrade` within the new section)
    - The existing `## Breaking Changes (v1.10)` sections in both files are byte-unchanged
  </acceptance_criteria>
  <done>Both READMEs carry a `## Breaking Changes (v1.20)` section recording the wire-contract break, the algorithm-required requirement, and the pre-v1.20-host-safe note, styled to match the existing v1.10 breaking-change template.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-v1.20 host -> firmware | A stale host may emit a `type` key; firmware must ignore it safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-01 | Information (misleading docs) | firestarter/CLAUDE.md dispatch narrative | medium | mitigate | Scrub false steps-7-11 fallback prose so agents/readers don't hand-craft `type`-based commands expecting fallback dispatch |
| T-107-02 | Tampering | stray `type` from pre-v1.20 host | low | accept | Documented as safe (json_parser.c unknown-field-skip); this plan records the safety note in the README, no code change |
</threat_model>

<verification>
- `grep -nE 'mem_type|Memory type 0x' firestarter/CLAUDE.md` returns 0 hits
- `grep -nE '"type"[[:space:]]*:[[:space:]]*[0-9]' firestarter_app/CLAUDE.md` returns 0 hits
- `grep -c '^## Breaking Changes (v1.20)'` returns 1 in each README
- No electrical.type / infoic `type=N` reference altered anywhere (firestarter/doc/PROTOCOLS.md is NOT edited by this plan)
</verification>

<success_criteria>
Firmware and host agent docs describe protocol-only dispatch with a fail-closed terminal arm, carry no numeric `type`/`mem_type` wire references, and both READMEs record the v1.20 breaking wire-contract change with the algorithm-required rule.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces NO new code symbols. It creates documentation artifacts and removes stale prose:
- NEW section anchor: `## Breaking Changes (v1.20)` in firestarter/README.md
- NEW section anchor: `## Breaking Changes (v1.20)` in firestarter_app/README.md
- REMOVED prose: firestarter/CLAUDE.md dispatch steps 7-11, mem_type fallback narrative, legacy `type` wire bullet
- REMOVED prose: firestarter_app/CLAUDE.md `"type": 1` wire example line
- PRESERVED (explicitly not touched): all electrical.type string semantics, infoic `type=N` identity tuples, firestarter/doc/PROTOCOLS.md
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/107-docs-gate-documentation-non-regression-close/107-01-SUMMARY.md` when done
</output>
