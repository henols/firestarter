---
phase: 159-citation-remap-milestone-close
plan: "04"
type: execute
wave: 4
depends_on: ["159-03"]
files_modified:
  - .planning/v1.33/159-remap-exceptions.jsonl
  - .planning/v1.33/159-corpus-overlay.json
  - .planning/v1.33/tools/rehearse_citation_remap.py
  - .planning/v1.33/tools/test_rehearse_citation_remap.py
  - .planning/v1.33/159-rehearsal-record.json
autonomous: true
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "Every Plan-03 record decision and dirty-overlap authorization is transcribed by exact stable ID/hash; no unreviewed or stale overlay row remains."
    - "The production-shaped live dry run exits 0 with all actionable counts/open-ID sets zero before rehearsal."
    - "Rehearsal materializes every production affected-document byte and approved live topology, including approved dirty overlays; a detached committed checkout alone is insufficient."
    - "A disposable injected mid-batch failure replaces at least one file, restores the full corpus/topology from recovery, and cannot be resumed as apply."
    - "A separate disposable transaction applies once, dry-runs to zero/zero, preserves post-apply hashes, proves the real 4-to-3 range shrink, and leaves archive PASS/12 unchanged."
  artifacts:
    - path: .planning/v1.33/159-remap-exceptions.jsonl
      provides: "Zero-open reviewed exception ledger"
    - path: .planning/v1.33/159-corpus-overlay.json
      provides: "Approved, hash-pinned production topology and staging dispositions"
    - path: .planning/v1.33/tools/rehearse_citation_remap.py
      provides: "Exact-live-corpus recovery/apply/no-op/index/archive rehearsal harness"
    - path: .planning/v1.33/tools/test_rehearse_citation_remap.py
      provides: "Harness safety and overlay/recovery/index isolation tests"
    - path: .planning/v1.33/159-rehearsal-record.json
      provides: "Machine-readable production-equivalence evidence"
  key_links:
    - from: .planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md
      to: .planning/v1.33/159-remap-exceptions.jsonl
      via: "exact stable-ID transcription plus target/anchor/location oracle"
      pattern: "review_source"
    - from: .planning/v1.33/159-corpus-overlay.json
      to: .planning/v1.33/159-rehearsal-record.json
      via: "same path/topology/SHA-256 fingerprint as the prospective production corpus"
      pattern: "corpus_fingerprint"
---

<objective>
Close every reviewed exception and rehearse the exact approved live corpus, recovery path, sole apply path, dry fixed point, index isolation, range oracle, and archive gate outside production.

Purpose: a detached HEAD omits current STATE bytes and the deleted-old/untracked-new COBS topology. Production readiness exists only if the disposable root reproduces the actual approved bytes/topology and demonstrates batch recovery plus unrelated-hunk preservation.

Output: zero-open ledger/overlay data, a tested rehearsal harness, and a non-vacuous rehearsal record whose corpus fingerprint can be compared byte-for-byte at production preflight.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/159-citation-remap-milestone-close/159-01-SUMMARY.md
@.planning/phases/159-citation-remap-milestone-close/159-02-SUMMARY.md
@.planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md
@.planning/v1.33/159-late-citation-manifest.jsonl
@.planning/v1.33/159-remap-exceptions.jsonl
@.planning/v1.33/159-corpus-overlay.json
@.planning/v1.33/tools/remap_citations.py
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py
</context>

## Artifacts this phase produces

- Updated ledger fields `status: reviewed`, review-kind-specific `chosen_*`, `rationale`, and `review_source: 159-03` for exactly the approved record IDs.
- Updated overlay fields `approval_status`, `decision`, `authorization_scope`, `authorization_source: 159-03`, `approved_preapply_sha256`, `expected_postapply_sha256`, and final `staging_strategy` for exactly the approved overlap IDs.
- New `.planning/v1.33/tools/rehearse_citation_remap.py` symbols `materialize_live_corpus`, `exercise_recovery`, `simulate_index_stage`, `snapshot_hashes`, and `run_archive_gate`.
- New rehearsal flags: positional `repo_root`; repeatable `--manifest` and `--pre-sweep-sha`; `--exceptions`; `--corpus-overlay`; `--planning-base-sha`; `--firmware-head`; `--app-head`; and `--output`.
- New `.planning/v1.33/tools/test_rehearse_citation_remap.py`.
- New `.planning/v1.33/159-rehearsal-record.json` fields `status`, `root_class`, `input_hashes`, `topology_digest`, `corpus_fingerprint`, `recovery`, `apply`, `second_dry_run`, `post_apply_hashes`, `index_isolation`, `range_proofs`, and `archive_gate`.
- Executor-created `.planning/phases/159-citation-remap-milestone-close/159-04-SUMMARY.md` with closed review counts, overlay authorizations, and rehearsal/recovery results.
- No production receipt or production recovery bundle is created; no live citing document or marker is changed.

<tasks>

<task type="auto">
  <name>Task 1: Transcribe every approved decision and prove the live corpus dry-runs with zero open outcomes</name>
  <files>.planning/v1.33/159-remap-exceptions.jsonl, .planning/v1.33/159-corpus-overlay.json</files>
  <read_first>
    - `.planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md` — sole approval authority; parse every Decision/Overlap section
    - `.planning/v1.33/159-remap-exceptions.jsonl` — update exactly the pre-existing pending record-ID set
    - `.planning/v1.33/159-corpus-overlay.json` — update exactly the pending authorization-ID set and re-hash live paths first
    - every source/history/document context named by the summary — independently reproduce the approved evidence
    - `.planning/v1.33/tools/prepare_citation_remap.py` — deterministic JSONL/JSON writer and self-check path
    - `.planning/v1.33/tools/remap_citations.py` — production-shaped dry-run/report interface
  </read_first>
  <action>
Before mutation, derive pending record and authorization sets and require exact equality with the Plan-03 summary. Re-hash every overlay path and re-run Git-state/topology detection; any path/hash/status change invalidates approval and stops for a new review rather than updating evidence silently.

For every record ID, copy only its review-kind-specific approved values, set `status: reviewed`, and set `review_source: 159-03`. Reproduce selected current endpoint text, selected Git-blob endpoint text, or selected planning location/ordinal before closing the row. Require at least 110 reviewed IDs with exactly five `known_post154_non_survivor` and 105 `ordinary_original_non_survivor` classifications.

For each overlap authorization, transcribe the exact decision/scope/rationale and set `authorization_source: 159-03`. `preserve_unstaged` requires `citation_only_blob`; `authorize_include` records the precise pre-existing path/topology change permitted into the commit; no `stop`, `needs_review`, or unapproved changed hash may remain. Record the approved current hash as `approved_preapply_sha256`; after the zero-exception render, record its deterministic `expected_postapply_sha256` so the same immutable overlay accepts exactly the pre-apply or fixed-point post-apply state and no third byte state. Regenerate both files to temporary outputs and require byte identity.

Run the remapper dry on `/workspaces` with both manifests, the closed ledger, approved overlay, planning base, correct original SHAs, a JSON report, and an index plan. Require exit 0; nonzero examined records/documents and planned rewrites/documents; every actionable count zero; every open-ID set empty; and every dirty `preserve_unstaged` entry represented by a citation-only index blob while each `authorize_include` entry names its authorization ID.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; python3 -c "import json; rows=[json.loads(x) for x in open('.planning/v1.33/159-remap-exceptions.jsonl') if '\"_schema\"' not in x]; assert not any(r['status'] in {'needs_review','ambiguous'} for r in rows); reviewed=[r for r in rows if r.get('review_source')=='159-03']; assert len(reviewed)&gt;=110; assert sum(r['classification']=='known_post154_non_survivor' for r in reviewed)==5; assert sum(r['classification']=='ordinary_original_non_survivor' for r in reviewed)==105; overlay=json.load(open('.planning/v1.33/159-corpus-overlay.json')); dirty=[r for r in overlay['documents'] if r['dirty_overlap']]; assert all(r['approval_status']=='approved' and r['decision'] in {'preserve_unstaged','authorize_include'} and r['approved_preapply_sha256']==r['preapply_sha256'] and r['expected_postapply_sha256'] for r in dirty); assert all(r['staging_strategy']=='citation_only_blob' for r in dirty if r['decision']=='preserve_unstaged')" &amp;&amp; python3 .planning/v1.33/tools/remap_citations.py /workspaces --manifest .planning/v1.33/sweep-citation-manifest.jsonl --manifest .planning/v1.33/159-late-citation-manifest.jsonl --exceptions .planning/v1.33/159-remap-exceptions.jsonl --corpus-overlay .planning/v1.33/159-corpus-overlay.json --planning-base-sha 9a78bc6dc8b31087265f13c684ae850223806772 --pre-sweep-sha firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a --pre-sweep-sha firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a --report-json /tmp/gsd-159-plan04-dry.json --index-plan /tmp/gsd-159-plan04-index.json --quiet-notes &amp;&amp; python3 -c "import json; r=json.load(open('/tmp/gsd-159-plan04-dry.json')); assert all(v==0 for v in r['actionable_counts'].values()); assert all(not v for v in r['open_ids'].values()); assert all(r['totals'][k]&gt;0 for k in ('examined_records','examined_documents','planned_rewrites','planned_documents'))"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly the Plan-03 ID sets change; every record/authorization value reproduces against unchanged evidence and deterministic serialization.
    - At least 110 records are reviewed, including exact five/105 mandatory classifications; no ledger row remains open or ambiguous.
    - Every dirty overlap is approved explicitly, hash-current, and either citation-only-stageable or precisely authorized for inclusion; no `stop` remains.
    - Live production-shaped dry run exits 0 with nonzero work and zero in every actionable/open class.
    - Dry run writes no citing document; original manifest and marker remain byte-identical.
  </acceptance_criteria>
  <done>All human decisions and authorities are mechanically closed, and the exact live corpus has a zero-exception dry plan plus an index-safe staging description.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize the approved live overlay and rehearse recovery, apply, no-op, index isolation, range, and archive gates</name>
  <files>.planning/v1.33/tools/rehearse_citation_remap.py, .planning/v1.33/tools/test_rehearse_citation_remap.py, .planning/v1.33/159-rehearsal-record.json</files>
  <read_first>
    - `.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md` `rehearse_citation_remap.py` and stored-data sections
    - `.planning/v1.33/159-corpus-overlay.json` — approved path/topology/byte authority
    - `.planning/v1.33/tools/test_remap_citations.py` recovery, idempotency, dirty-index, and live-root-refusal tests
    - `.planning/v1.33/tools/remap_citations.py` — invoke this CLI; do not implement a second renderer
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py` — archive baseline, allow at least 90 seconds
  </read_first>
  <behavior>
    - Refuses `/workspaces`, its canonical equivalent, or any non-disposable apply root.
    - Copies every affected current document byte and exact approved path topology from live root into a disposable meta worktree, not only dirty paths.
    - Re-hashes the disposable corpus and requires exact equality with the prospective production `corpus_fingerprint` and `topology_digest`.
    - Exercises an injected failure after at least one replacement in a dedicated disposable copy; receipt records progress/FAILED, rollback is COMPLETE, and all preimage hashes/topology are restored.
    - Runs a separate single successful disposable apply, then a dry second run with zero rewrites/documents and byte-identical post-first/post-second hashes.
    - Simulates receipt index staging and proves `preserve_unstaged` bytes remain outside staged blobs while authorized-inclusion paths equal their recorded scope.
    - Preserves archive PASS with `superseded: 12` and proves `json_parser.c:128-131 -> 316-318`, span 4 -> 3, with exact endpoint texts.
  </behavior>
  <action>
Create the harness/tests. Use registered detached worktrees at the current meta execution commit, firmware final `2ccda8d43c8161a34fb5f83b9ab12c37a443bf22`, and app final `38f0d839a1984fa71cb16ea98afa4d8a4e6bcfe2`. After creating the meta worktree, materialize every affected document from live bytes: copy current paths, delete historical paths for approved relocations, retain modes, and reject symlinks/out-of-root destinations. This step must recreate the approved COBS topology if present rather than restoring its old committed path.

In a first disposable corpus, invoke the real remapper with disposable receipt/recovery bundle and `--inject-write-failure-after 1` (or a larger positive value below planned-document count). Require at least one recorded replacement, `FAILED`, verified `rollback_status: COMPLETE`, byte/topology equality with preimages, and failure of any apply replay; test recovery-only handling of an APPLYING receipt separately.

In a fresh disposable corpus with the same fingerprint, run one apply through the real CLI without a production receipt classification, require nonzero changed documents and zero actionable outcomes, snapshot all affected hashes, then run without `--apply` and require zero rewritten/zero documents-would-change plus identical hashes. Generate and simulate the index plan: staged path set must equal citation-changed tracked documents plus explicitly authorized inclusion paths; every unrelated dirty patch digest must remain byte-identical and unstaged.

Run archive correction before and after, require PASS and `superseded: 12`. Assert structured real-range proof exactly old 128/131, new 316/318, spans 4/3, with start text `if (jsoneq_(json, key_token, key) == 0) {` and end text `    token_idx += 2; // Skip key and simple value` byte-equal at both old/new endpoints. Write `159-rehearsal-record.json` atomically with all locked fields, exact dynamic row/review/exclusion/document counts, live/disposable fingerprint equality, recovery evidence, index-isolation evidence, and `root_class: disposable`. Always remove/prune only the exact temporary worktrees in `finally`; install nothing and use no network.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_rehearse_citation_remap.py .planning/v1.33/tools/test_remap_citations.py &amp;&amp; python3 .planning/v1.33/tools/rehearse_citation_remap.py /workspaces --manifest .planning/v1.33/sweep-citation-manifest.jsonl --manifest .planning/v1.33/159-late-citation-manifest.jsonl --exceptions .planning/v1.33/159-remap-exceptions.jsonl --corpus-overlay .planning/v1.33/159-corpus-overlay.json --planning-base-sha 9a78bc6dc8b31087265f13c684ae850223806772 --pre-sweep-sha firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a --pre-sweep-sha firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a --firmware-head 2ccda8d43c8161a34fb5f83b9ab12c37a443bf22 --app-head 38f0d839a1984fa71cb16ea98afa4d8a4e6bcfe2 --output .planning/v1.33/159-rehearsal-record.json</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests pass with the py-utils interpreter and prove live-root refusal plus approved untracked-relocation materialization.
    - Rehearsal input bytes/topology equal the approved prospective production fingerprint for every affected document, not only committed or dirty files.
    - Injected failure occurs after at least one replacement, restores every preimage/topology, records FAILED/COMPLETE recovery, and cannot resume/replay apply.
    - Separate rehearsal apply changes nonzero documents once; dry run two exits 0 with zero/zero and identical post-first/post-second affected-corpus hashes.
    - Index simulation leaves every unrelated dirty hunk unstaged and includes a pre-existing change only when its exact authorization ID/scope says `authorize_include`.
    - Archive result is PASS/12 before and after; real range is exactly 128-131 -> 316-318, span 4 -> 3, endpoint texts equal.
    - `159-rehearsal-record.json` is non-vacuous, self-consistent, labels all apply events disposable, and contains no production event.
    - Live citing documents and marker are unchanged; temporary worktrees are removed.
  </acceptance_criteria>
  <done>The exact approved live corpus is recovery-proven, index-isolated, range-correct, archive-safe, and byte-idempotent in disposable roots without consuming production apply.</done>
</task>

</tasks>

<verification>
- Ledger and overlay contain zero open/unapproved rows and reproduce Plan-03 evidence exactly.
- Live dry run is non-vacuous and zero-exception.
- Rehearsal record proves exact production corpus/topology, mid-batch recovery, one disposable apply, dry zero/zero fixed point, index isolation, range 4->3, and archive PASS/12.
- No production receipt exists and `CITATIONS-STALE.md` remains present.
</verification>

<success_criteria>
All review decisions are oracle-backed, the rehearsal uses the actual approved live corpus rather than a clean approximation, recovery restores a partially written batch, unrelated dirty bytes remain outside the staged payload, and the exact production engine proves REMAP-02/03/05 before production authorization.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-04-SUMMARY.md` when done.
</output>
