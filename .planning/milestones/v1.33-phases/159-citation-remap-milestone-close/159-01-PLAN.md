---
phase: 159-citation-remap-milestone-close
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/tools/remap_citations.py
  - .planning/v1.33/tools/test_remap_citations.py
autonomous: true
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "The legacy baseline is measured with the proven py-utils interpreter as 20 passed / 1 obsolete real-tree guard failure; a missing pytest installation can never count as RED."
    - "The current raw dry run is recorded as failing first on the pending-to-completed citing-document rename, never as the older location-restored 2238/6908/904/3486/156 diagnostic."
    - "Every unmatched, not-at-recorded, dynamic-retarget, unreviewed-retarget, missing-target, location/anchor ambiguity, and source/destination oracle outcome blocks before the first write."
    - "A batch interrupted after one or more replacements restores every preimage or leaves a FAILED/APPLYING receipt that permits recovery only, never an automatic apply retry."
    - "Original app rows use 6bfa6453d1bac232eb81ab35fa7f14b50b0b291a; bc9d59293b9a08b16d6d7eb16eaf6c6f53e88e65 is valid only for Phase-154 hand-retarget advancement."
  artifacts:
    - path: .planning/v1.33/tools/remap_citations.py
      provides: "One fail-closed, multi-anchor, overlay-aware, recoverable batch transaction"
    - path: .planning/v1.33/tools/test_remap_citations.py
      provides: "RED/GREEN coverage for location, anchor, review, recovery, range, index-plan, and one-shot contracts"
  key_links:
    - from: .planning/v1.33/tools/remap_citations.py
      to: .planning/v1.33/tools/build_citation_manifest.py
      via: "imports `_CITATION_RE` and `_spans_from_match`; no second citation grammar"
      pattern: "import build_citation_manifest as bcm"
    - from: .planning/v1.33/tools/remap_citations.py
      to: .planning/v1.33/tools/citation_paths.py
      via: "uses the Phase-154 resolver for every original and supplemental row"
      pattern: "import citation_paths"
---

<objective>
Harden the existing Phase-154 remapper before any Phase-159 census or corpus mutation.

Purpose: the committed CLI currently stops on a renamed citing document, and its location-restored diagnostic would still tolerate 156 unmatched rows and 103 visible ordinary dynamic retargets. REMAP-01/02/05 require one engine that is corpus-complete, fail-closed, batch-recoverable, and auditable before production.

Output: the existing remapper extended with stable identities, per-record historical anchors, current-worktree topology overlays, reviewed targets, structured reports, recovery receipts, index-safe staging plans, and a real-range assertion.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-04-SUMMARY.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-05-SUMMARY.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-12-SUMMARY.md
@.planning/v1.33/tools/remap_citations.py
@.planning/v1.33/tools/test_remap_citations.py
@.planning/v1.33/tools/build_citation_manifest.py
@.planning/v1.33/tools/citation_paths.py
</context>

## Artifacts this phase produces

- Modified `.planning/v1.33/tools/remap_citations.py` with new symbols `stable_record_id`, `LocationResolver`, `BatchTransaction`, `build_index_stage_plan`, and `recover_failed_receipt`.
- Modified `.planning/v1.33/tools/test_remap_citations.py`; no second test harness or fixture file is created.
- New/extended CLI surface: repeatable `--manifest`; `--planning-base-sha`; `--exceptions`; `--corpus-overlay`; `--report-json`; `--production-receipt`; `--recovery-bundle`; `--recover-receipt`; `--index-plan`; and test-only `--inject-write-failure-after`, which must reject `/workspaces` as its root.
- Structured report fields: `totals`, `actionable_counts`, `open_ids`, `affected_documents`, `corpus_fingerprint`, `topology_digest`, and `range_proofs`.
- Receipt/recovery fields: `status`, `production_apply_events`, `event_id`, `input_fingerprint`, `planned_documents`, `replaced_documents`, `preimage_hashes`, `recovery_bundle_sha256`, `rollback_status`, and `failure`.
- Index-plan fields: `path`, `index_mode`, `base_index_blob`, `citation_only_blob`, `live_preimage_sha256`, `live_postimage_sha256`, `staging_strategy`, and `authorization_id`.
- Executor-created `.planning/phases/159-citation-remap-milestone-close/159-01-SUMMARY.md` with the measured 20/1 baseline, raw rename failure, GREEN test count, and new interface/recovery evidence.
- This plan creates no manifest, performs no real-corpus apply, and leaves `.planning/v1.33/CITATIONS-STALE.md` present.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the truthful baseline, retire the obsolete guard, and add Phase-159 RED legs</name>
  <files>.planning/v1.33/tools/test_remap_citations.py</files>
  <read_first>
    - `.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md` sections `Executive Answer`, `Findings Changed by This Forced Refresh`, `Exact Current Commands`, `Complete exception set`, and `Validation Architecture`
    - `.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md` rows `159-01-01` and `159-01-02`
    - `.planning/v1.33/tools/test_remap_citations.py` `Harness`, `_record`, rollback/oracle tests, and `test_the_tool_is_not_applied_to_any_real_planning_document`
    - `.planning/v1.33/tools/remap_citations.py` `LineMap`, `decide`, `_associate`, `remap_document`, `atomic_write`, and `main`
  </read_first>
  <action>
First prove `/usr/local/py-utils/venvs/pytest/bin/python` imports pytest `9.1.1`. Run the untouched focused suite with that interpreter and record exactly `20 passed, 1 failed`; require the sole failure to be `test_the_tool_is_not_applied_to_any_real_planning_document`, naming `.planning/STATE.md` and `.planning/v1.9-COBS-DECISION.md`. The system interpreter's missing-pytest error is infrastructure failure and cannot count as RED.

Run the committed raw dry command with original SHAs `firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a` and `firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a`; require exit 1 and first error `citing document does not exist: .planning/todos/pending/2026-08-22-sweep-gsd-provenance-comments-from-firmware-and-host-source.md`. Record the older 2238/6908/904/3486/156/508 figures only as a location-restored diagnostic, not current output.

Replace the obsolete real-tree mutation guard with receipt/apply-event assertions. Extend the existing `Harness` and `_record`; add RED tests for: correct versus wrong app anchor (wrong `bc9d592...` produces 787 real-corpus oracle violations in the research diagnostic); tracked rename; shifted planning line; duplicate same-path/variant occurrence; untracked relocation; two source SHAs for one target; all actionable fail-closed classes; all 105 ordinary original composite non-survivors remaining individually visible even when duplicate groups exist; full-window coverage failing on a planted citation outside Phase 155-158 directories; reviewed-retarget current-text oracle/fixed point; late validation failure after hundreds of planned edits; injected failure after at least one replacement with full rollback; interrupted-receipt recovery without apply replay; dirty affected document producing a citation-only index blob; receipt replay; and the real `json_parser.c:128-131 -> 316-318` range using the actual Git blobs at `8695ee52...` and `2ccda8d43...`. The range test must assert start text `if (jsoneq_(json, key_token, key) == 0) {` and end text `    token_idx += 2; // Skip key and simple value` at both old and new endpoints.

Every negative test must assert the expected exit class, stable record ID(s), and byte/hash equality for all candidate documents. The injected failure flag must be accepted only for a disposable root. Keep the original Phase-154 synthetic range, fixture, traversal, and idempotency tests.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -c 'import pytest; assert pytest.__version__ == "9.1.1"' &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest --collect-only -q .planning/v1.33/tools/test_remap_citations.py | grep -q 'test_injected_mid_batch_failure_restores_every_preimage' &amp;&amp; set +e; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_remap_citations.py &gt;/tmp/gsd-159-01-red.log 2&gt;&amp;1; rc=$?; set -e; test "$rc" = 1 &amp;&amp; grep -q 'test_wrong_original_app_anchor_is_rejected' /tmp/gsd-159-01-red.log &amp;&amp; grep -q 'test_injected_mid_batch_failure_restores_every_preimage' /tmp/gsd-159-01-red.log &amp;&amp; ! grep -q 'No module named pytest' /tmp/gsd-159-01-red.log</automated>
  </verify>
  <acceptance_criteria>
    - The pre-edit baseline is recorded as `20 passed / 1 obsolete real-tree guard failure` under pytest 9.1.1, and the raw dry run is recorded as the renamed-document exit-1 result.
    - The obsolete guard is narrowed/replaced; ordinary STATE bookkeeping and a user COBS relocation are not treated as evidence that production apply occurred.
    - The expanded module collects at least 35 tests and contains every named Phase-159 RED leg, including the 105-row visibility, outside-directory coverage, recovery, dirty-index, and real-range legs.
    - The RED execution collected tests successfully and fails on named unimplemented behaviors, never on pytest import or collection infrastructure.
    - No test invokes `--apply` on `/workspaces`, and the original manifest hash remains `ecdd0fc84be1627f893e30f6369c0b9eedf2a69ce3ec351064828d82e72d992e`.
  </acceptance_criteria>
  <done>The actual legacy/raw state is recorded truthfully, and every refreshed Phase-159 safety gap is an executable RED contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the fail-closed multi-anchor transaction, batch recovery, and index-plan surface</name>
  <files>.planning/v1.33/tools/remap_citations.py, .planning/v1.33/tools/test_remap_citations.py</files>
  <read_first>
    - `.planning/v1.33/tools/test_remap_citations.py` — Task 1 tests are the binding contract
    - `.planning/v1.33/tools/remap_citations.py` — extend the existing engine; retain path-safety and independent endpoint mapping
    - `.planning/v1.33/tools/build_citation_manifest.py` `_CITATION_RE`, `_spans_from_match`, `_dump`, text-status constants, and fixed key ordering
    - `.planning/v1.33/tools/citation_paths.py` `CandidateIndex`, fixture handling, and containment rules
    - `.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md` sections `Explicit historical identity`, `Fail closed`, and `Determinism and fixed point`
  </read_first>
  <action>
Implement the locked symbols, CLI flags, and structured fields listed in `Artifacts this phase produces` inside the one existing engine.

Normalize original rows to deterministic `record_id` values without editing the immutable manifest. Require supplemental rows to supply a stable ID and explicit `source_root` plus either one `source_sha` or a nonempty `source_sha_candidates`; a candidate set remains blocking until a reviewed ledger row chooses a contained SHA. Cache maps by `(target_file_resolved, source_sha)`. Homogeneous original rows fall back only to firmware `8695ee52...` or app `6bfa6453...`; Phase-154 hand choices advance from `2ad5b322...` or `bc9d592...` through explicit `retarget_source_sha`.

Resolve citing-document path, line, citation group, variant, and ordinal from the planning base before positional association. Apply only approved `--corpus-overlay` mappings whose recorded path/Git state equal live topology and whose bytes equal either the approved `preapply_sha256` or the deterministic `expected_postapply_sha256`; reject every third state. Missing, changed, ambiguous, or unapproved overlays are exit-1 outcomes. Recognize destination text as a fixed point before interpreting a current integer as an old coordinate. Reviewed semantic targets must match `chosen_current_text` and range end text; reviewed anchors/locations must be contained in their serialized candidates.

Build every updated document and the complete structured report before the first replacement. Treat every unmatched, not-at-recorded, ordinary dynamic-retarget, unreviewed retarget, open review, missing/ambiguous location, unresolved anchor, missing target, fixture collision, and old/destination/current-target oracle failure as exit 1 with zero writes. Reserve exit 2 for argv/schema/root/SHA/blob/zero-input infrastructure failures. Reports must be atomic, deterministic, non-vacuous, and list stable IDs per actionable class.

Implement `BatchTransaction`: render temporary outputs; create the exclusive receipt and recoverable preimage bundle; transition `PREPARED -> APPLYING -> APPLIED` while recording each replaced path. On caught write failure, restore every replaced path and topology from the bundle, verify preimage hashes, set `FAILED` plus `rollback_status: COMPLETE`, and exit without retry. If a process dies in APPLYING, `--recover-receipt` performs recovery only and transitions to a recovered terminal state; it never resumes apply. A pre-existing receipt blocks a new apply. `--inject-write-failure-after N` must refuse the canonical live root and exist only to prove this contract in disposable tests.

Implement `build_index_stage_plan` from current index blobs plus only the exact citation span edits. A tracked dirty file's `citation_only_blob` must exclude unrelated worktree bytes; an untracked/renamed path must report `staging_strategy: requires_authorization` rather than silently stage a whole file. Preserve `SequenceMatcher(..., autojunk=False)`, independent range endpoints, shared grammar/resolver, traversal/containment/symlink refusal, and same-directory `os.replace`.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_remap_citations.py</automated>
  </verify>
  <acceptance_criteria>
    - At least 35 focused tests pass under `/usr/local/py-utils/venvs/pytest/bin/python`, including every Task-1 RED leg and every retained Phase-154 leg.
    - Correct app anchor passes and the wrong original anchor fails; maps for the same target at two SHAs remain distinct.
    - Every actionable data class exits 1 before writes, every infrastructure class exits 2, and structured stable-ID sets/counts are deterministic and non-vacuous.
    - A late validation failure writes zero files; an injected mid-write failure restores all bytes/topology, records replaced progress and verified rollback, and cannot be replayed as apply.
    - A dirty tracked-file fixture yields an index blob containing citation edits but excluding its unrelated edit; an untracked relocation requires explicit authorization.
    - The real range proof is exactly old `128-131`, new `316-318`, inclusive span `4 -> 3`, with both endpoint texts equal.
    - The original manifest remains byte-identical and no production receipt/recovery bundle exists in the live tree.
  </acceptance_criteria>
  <done>The remapper is one deterministic, fail-closed, recoverable transaction that supports historical anchors and live overlays, proves the real shrinking range, and can describe citation-only index staging without touching production.</done>
</task>

</tasks>

<verification>
- The proven pytest interpreter reports 9.1.1 and the focused suite is green.
- The raw pre-hardening result and 20/1 legacy baseline are preserved in the summary as measurements, not future expectations.
- Original manifest is 13,693 physical lines and has SHA-256 `ecdd0fc84be1627f893e30f6369c0b9eedf2a69ce3ec351064828d82e72d992e`.
- `test -e .planning/v1.33/CITATIONS-STALE.md` remains true; no live-root apply receipt exists.
</verification>

<success_criteria>
One existing remap engine now makes partial-green results impossible, distinguishes data failures from infrastructure failures, reconciles historical/current topology under explicit overlays, restores a partially written batch, emits citation-only index objects for dirty files, enforces a single production receipt, and retains the exact independent-endpoint range behavior required by REMAP-03.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-01-SUMMARY.md` when done.
</output>
