---
phase: 159-citation-remap-milestone-close
plan: "05"
type: execute
wave: 5
depends_on: ["159-04"]
files_modified:
  - .planning/v1.33/159-production-preflight.json
  - .planning/v1.33/159-index-stage-plan.json
  - .planning/v1.33/159-production-apply.json
  - .planning/v1.33/159-remap-record.md
  - "manifest-selected .planning documents enumerated by 159-production-preflight.json"
autonomous: true
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "Production stops unless current affected-document bytes/topology exactly equal rehearsal, approved dirty overlaps are unchanged, and no new dirty overlap or staged user path exists."
    - "Exactly one live-root command contains --apply; it creates an exclusive receipt plus recoverable preimages and is never automatically retried after failure."
    - "The post-apply proof is dry only, reports zero rewrites/documents/actionable outcomes, and leaves every affected byte unchanged."
    - "Index staging uses citation-only blobs for tracked dirty paths and includes pre-existing content only under an explicit Plan-03 authorization ID; whole changed paths are never staged by convenience."
    - "Unrelated dirty worktree bytes, app untracked files, source repositories, and the drifted firmware gitlink remain outside the Phase-159 commit."
  artifacts:
    - path: .planning/v1.33/159-production-preflight.json
      provides: "READY gate with exact rehearsal/input/corpus/dirty/index invariants"
    - path: .planning/v1.33/159-index-stage-plan.json
      provides: "Per-path index blobs and authorization-backed staging strategies"
    - path: .planning/v1.33/159-production-apply.json
      provides: "Exclusive one-event production receipt with replacement/recovery progress"
    - path: .planning/v1.33/159-remap-record.md
      provides: "Authoritative measured apply/no-op/range/archive/index evidence"
  key_links:
    - from: .planning/v1.33/159-rehearsal-record.json
      to: .planning/v1.33/159-production-preflight.json
      via: "exact input hashes, source SHAs, corpus fingerprint, and topology digest"
      pattern: "corpus_fingerprint"
    - from: .planning/v1.33/159-index-stage-plan.json
      to: .planning/v1.33/159-production-apply.json
      via: "receipt-listed span edits generate only the precomputed citation-only index blobs"
      pattern: "citation_only_blob"
---

<objective>
Run the one authorized production transaction, prove the real corpus is a byte-stable fixed point, and commit only citation edits plus explicitly authorized overlap changes.

Purpose: this wave is the irreversible center of Phase 159. Recovery protects a partial filesystem batch; the dirty-overlap and index-object gates protect unrelated user work from being absorbed into the remap commit.

Output: READY/index plans, one production receipt, migrated citation spans, dry no-op/hash/range/archive evidence, a scoped commit, and an authoritative record. The staleness marker remains for Plan 159-06.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/159-citation-remap-milestone-close/159-04-SUMMARY.md
@.planning/v1.33/159-rehearsal-record.json
@.planning/v1.33/159-late-citation-manifest.jsonl
@.planning/v1.33/159-remap-exceptions.jsonl
@.planning/v1.33/159-corpus-overlay.json
@.planning/v1.33/tools/remap_citations.py
@.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py
</context>

## Artifacts this phase produces

- New `.planning/v1.33/159-production-preflight.json` fields: `status`, `input_hashes`, `source_heads`, `source_porcelain`, `original_manifest_sha256`, `supplemental_counts`, `review_counts`, `affected_documents`, `corpus_fingerprint`, `topology_digest`, `approved_dirty_overlaps`, `preexisting_index_paths`, `unrelated_patch_digests`, `rehearsal_match`, `archive_gate`, and `production_apply_events`.
- New `.planning/v1.33/159-index-stage-plan.json` fields: `path`, `mode`, `base_index_blob`, `citation_only_blob`, `live_preimage_sha256`, `predicted_postimage_sha256`, `staging_strategy`, `authorization_id`, and `unrelated_patch_digest`.
- New `.planning/v1.33/159-production-apply.json` receipt fields from Plan 01, ending `status: APPLIED` and `production_apply_events: 1` on success.
- Transient `.planning/v1.33/159-production-recovery.tar`, containing complete pre-apply bytes/topology for every affected document. It exists from PREPARED through successful commit, is never staged, and is deleted only after that commit; its SHA-256 remains recorded in the receipt/record.
- New `.planning/v1.33/159-remap-record.md` frontmatter and measured sections for inputs, dynamic populations, sole apply, recovery, second dry run, range proof, archive proof, dirty/index proof, and marker state.
- Citation-span-only changes to the exact affected documents listed by the receipt/preflight. ROADMAP/REQUIREMENTS may receive citation-span edits here but no status/prose normalization; Plan 06 owns scoped closure fields.
- Executor-created `.planning/phases/159-citation-remap-milestone-close/159-05-SUMMARY.md` with production commit, receipt, recovery-bundle disposition, fixed-point, and preserved-dirty evidence.
- No new CLI flag or code symbol is created in this plan; it consumes Plan-01/04 surfaces.

<tasks>

<task type="auto">
  <name>Task 1: Freeze exact production bytes/topology, dirty overlaps, and citation-only index objects</name>
  <files>.planning/v1.33/159-production-preflight.json, .planning/v1.33/159-index-stage-plan.json</files>
  <read_first>
    - `.planning/v1.33/159-rehearsal-record.json` — must be PASS, disposable, recovery-proven, index-isolated, archive-safe, and exact-live-corpus matched
    - `.planning/v1.33/159-corpus-overlay.json` — every approved live path/hash/topology/authorization
    - `.planning/v1.33/159-late-citation-manifest.jsonl` and `.planning/v1.33/159-remap-exceptions.jsonl` — dynamic final counts and zero-open inputs
    - `.planning/v1.33/tools/remap_citations.py` and all three focused test modules — production dry/index interfaces
    - `.planning/v1.33/CITATIONS-STALE.md` — must remain present
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-07-SUMMARY.md` `The gitlinks` — firmware gitlink drift is pre-existing and must stay unstaged
  </read_first>
  <action>
Assert source repository HEADs exactly firmware `2ccda8d43c8161a34fb5f83b9ab12c37a443bf22` and app `38f0d839a1984fa71cb16ea98afa4d8a4e6bcfe2`; require tracked worktree and index clean in both. Inventory but do not delete/stage app untracked `.planning/config.json`, `SECURITY.md`, four PDFs, and `write_test_port.sh` (or their freshly measured replacement set). Record meta gitlinks and prove neither is staged.

Require original manifest SHA-256 `ecdd0fc84be1627f893e30f6369c0b9eedf2a69ce3ec351064828d82e72d992e`; supplemental count dynamically measured and at least 881; exact 642 subset/127-184-225-106; review count at least 110 with exact 5/105 mandatory classes; zero open ledger/overlay rows; marker present; receipt and recovery bundle absent; and rehearsal PASS.

Recompute the complete affected-document list, every current path/hash/mode, and topology. Require exact equality with `159-rehearsal-record.json`'s `corpus_fingerprint`/`topology_digest`; if Plan-04 execution bookkeeping changed any affected byte (especially STATE), stop and rerun Plan 04 rehearsal rather than blessing new bytes. Recompute `affected_documents ∩ preexisting_dirty_paths`; require exact equality with approved overlay authorization IDs. Any new/changed overlap stops. Require the meta index has zero staged paths; a pre-staged user change requires direction and cannot be hidden by a temporary index.

Run all focused tests, full dry/report, index-plan generation, and archive gate. Require zero actionable/open outcomes, nonzero planned work, range proof exact, archive PASS/12. Validate each index entry:

- clean tracked path: `citation_only_blob` equals the predicted live postimage;
- `preserve_unstaged`: blob is computed from the current index version plus citation edits only, excludes the recorded unrelated patch, and retains its digest for post-commit comparison;
- `authorize_include`: path/topology/scope exactly names the Plan-03 authorization;
- untracked/renamed path without `authorize_include`: fail.

Write both artifacts atomically. Preflight status is `READY`, rehearsal match true, production event count zero, and all exact commands/results/hashes are recorded. This task invokes no `--apply`, creates no apply receipt/recovery bundle, stages nothing, and changes no citing document.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_remap_citations.py .planning/v1.33/tools/test_prepare_citation_remap.py .planning/v1.33/tools/test_rehearse_citation_remap.py &amp;&amp; python3 -c "import json; p=json.load(open('.planning/v1.33/159-production-preflight.json')); i=json.load(open('.planning/v1.33/159-index-stage-plan.json')); assert p['status']=='READY' and p['production_apply_events']==0 and p['rehearsal_match']; assert p['supplemental_counts']['total']&gt;=881 and p['supplemental_counts']['phase_155_158']==642; assert p['review_counts']['total']&gt;=110 and p['review_counts']['known_post154_non_survivor']==5 and p['review_counts']['ordinary_original_non_survivor']==105; assert p['affected_documents'] and len(i['documents'])==len(p['affected_documents']); assert not p['preexisting_index_paths']; assert all(v==0 for v in p['actionable_exception_counts'].values()); assert all(d['staging_strategy'] in {'citation_only_blob','authorized_include'} for d in i['documents'])" &amp;&amp; test ! -e .planning/v1.33/159-production-apply.json &amp;&amp; test ! -e .planning/v1.33/159-production-recovery.tar &amp;&amp; test -e .planning/v1.33/CITATIONS-STALE.md</automated>
  </verify>
  <acceptance_criteria>
    - Source HEADs/cleanliness and original manifest hash are exact; dynamic supplemental/review counts satisfy the 881 and 110 lower bounds plus exact 642 and 5/105 subcounts.
    - Current complete corpus fingerprint/topology equals rehearsal; any changed byte/topology causes stop/rehearsal rerun.
    - Dirty intersection equals the approved set, all approval hashes remain current, no new dirty/staged path exists, and gitlinks/app untracked files remain untouched.
    - Every index-plan entry is citation-only or explicitly authorized; unrelated patch digests are pinned for post-commit proof.
    - Tests, dry run, range proof, and archive PASS/12 are green; preflight is READY with zero production events.
    - No apply command, receipt, recovery bundle, document write, or staging occurs.
  </acceptance_criteria>
  <done>Production inputs, live bytes/topology, dirty authority, and exact index objects are frozen and equivalent to rehearsal, with no apply event consumed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply once with recovery, prove dry fixed point, stage index-safely, and commit authoritative evidence</name>
  <files>.planning/v1.33/159-production-apply.json, .planning/v1.33/159-remap-record.md, manifest-selected paths listed by .planning/v1.33/159-production-preflight.json</files>
  <read_first>
    - `.planning/v1.33/159-production-preflight.json` and `.planning/v1.33/159-index-stage-plan.json` — every current input/hash/index precondition must still match
    - `.planning/v1.33/159-rehearsal-record.json` — expected recovery/apply/no-op/range/archive/index evidence shape
    - `.planning/v1.33/tools/remap_citations.py` — exact receipt/recovery semantics and production CLI
    - `.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md` `159-remap-record.md` section
    - `.planning/v1.33/sweep-outcome-record.md` — measured-command evidence style
    - `.planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py`
  </read_first>
  <action>
Immediately revalidate every preflight/input/corpus/index/source hash and marker presence. Abort before receipt creation if any differs.

Execute exactly one live-root apply command, once, without a shell retry loop:

`python3 .planning/v1.33/tools/remap_citations.py /workspaces --manifest .planning/v1.33/sweep-citation-manifest.jsonl --manifest .planning/v1.33/159-late-citation-manifest.jsonl --exceptions .planning/v1.33/159-remap-exceptions.jsonl --corpus-overlay .planning/v1.33/159-corpus-overlay.json --planning-base-sha 9a78bc6dc8b31087265f13c684ae850223806772 --pre-sweep-sha firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a --pre-sweep-sha firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a --report-json /tmp/gsd-159-production-apply-report.json --index-plan /tmp/gsd-159-production-index-confirm.json --production-receipt .planning/v1.33/159-production-apply.json --recovery-bundle .planning/v1.33/159-production-recovery.tar --apply --quiet-notes`

If it exits nonzero, do not delete the receipt/bundle and do not rerun apply. If receipt is `FAILED` with rollback COMPLETE, verify preimage equality and stop for diagnosis. If it is `APPLYING`, run only `--recover-receipt .planning/v1.33/159-production-apply.json --recovery-bundle .planning/v1.33/159-production-recovery.tar`, verify recovery, and stop. No failed production event is laundered into another attempt.

On success require `APPLIED`, production event count 1, nonzero changed documents contained in preflight, recovery bundle hash/preimages recorded, and zero actionable outcomes. Snapshot every affected postimage. Run the same transaction without `--apply`, receipt, or recovery flags, with a fresh report. Require exit 0, exactly zero rewritten and zero documents-would-change, all actionable/open values zero, and byte equality for all affected postimages. Run archive gate PASS/12 and assert range 128-131 -> 316-318, span 4 -> 3, exact endpoints.

Create `159-remap-record.md` with authoritative frontmatter and exact dynamic counts, SHAs/hashes, the single command above, receipt/recovery transitions, changed-document/oracle totals, dry zero/zero/hash result, real range texts, archive before/after, dirty-overlap authorization table, index staging proof, and `CITATIONS-STALE.md: PRESENT — Plan 159-06 close-blocks on removal`. Label all Plan-04 applies disposable; do not claim milestone completion.

Stage without `git add` on receipt-listed dirty documents. First require the real index still has zero staged paths. For each `citation_only_blob`, verify its Git object and use explicit `git update-index --cacheinfo &lt;mode&gt;,&lt;blob&gt;,&lt;path&gt;`; for each `authorized_include`, stage only the exact old/new path set named by its authorization; stage the production receipt and record as Phase-owned files. Include preflight/index-plan only if this task owns their still-uncommitted bytes. Do not stage the recovery tar, source/gitlink/app-untracked/package/marker/closure files, or any path outside the computed allowlist.

Verify staged path set and blob IDs exactly equal the index plan plus owned artifacts; diff every outside path to zero. Commit without a pathspec (to avoid the known gitlink/pathspec trap) using `docs(159-05): apply citation remap once and prove real-corpus no-op`. After commit, verify every `preserve_unstaged` unrelated patch digest is unchanged in the worktree, every authorized-inclusion scope is exactly represented, and source/gitlink/app-untracked inventories are unchanged. Only then delete the untracked recovery tar; record already retains its hash. Do not push or run milestone completion.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; python3 -c "import json; r=json.load(open('.planning/v1.33/159-production-apply.json')); assert r['status']=='APPLIED'; assert r['production_apply_events']==1; assert r['changed_document_count']&gt;0; assert len(r['changed_documents'])==r['changed_document_count']; assert r['rollback_status'] in {None,'NOT_NEEDED'}" &amp;&amp; python3 .planning/v1.33/tools/remap_citations.py /workspaces --manifest .planning/v1.33/sweep-citation-manifest.jsonl --manifest .planning/v1.33/159-late-citation-manifest.jsonl --exceptions .planning/v1.33/159-remap-exceptions.jsonl --corpus-overlay .planning/v1.33/159-corpus-overlay.json --planning-base-sha 9a78bc6dc8b31087265f13c684ae850223806772 --pre-sweep-sha firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a --pre-sweep-sha firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a --report-json /tmp/gsd-159-post-apply-dry.json --quiet-notes &amp;&amp; python3 -c "import json; r=json.load(open('/tmp/gsd-159-post-apply-dry.json')); assert r['totals']['rewritten']==0 and r['totals']['planned_documents']==0; assert all(v==0 for v in r['actionable_counts'].values()); assert all(not v for v in r['open_ids'].values())" &amp;&amp; python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py | grep -q "'superseded': 12" &amp;&amp; test ! -e .planning/v1.33/159-production-recovery.tar &amp;&amp; test -e .planning/v1.33/CITATIONS-STALE.md &amp;&amp; test "$(git log -1 --format=%s | grep -c '^docs(159-05):')" = 1</automated>
  </verify>
  <acceptance_criteria>
    - The authoritative record contains exactly one production-root command with `--apply`; receipt is APPLIED with one event and nonzero changed set.
    - Failure handling is stop/recover-only/no-retry; on the successful path, complete preimages exist through commit and the recovery tar is deleted only afterward without staging.
    - Post-apply invocation is dry, exits 0, reports zero rewritten/zero documents/actionable outcomes, and preserves all affected hashes.
    - Real range and endpoint texts are exact; archive remains PASS/12; source/gitlink/app-untracked inventories are invariant.
    - Index blobs stage only citation edits for preserved dirty paths; unrelated patch digests remain in the worktree unchanged; pre-existing changes appear in the commit only under exact `authorize_include` IDs.
    - Commit staged set contains only computed remap paths plus Phase-owned evidence, uses no pathspec, and leaves marker present.
    - No ROADMAP/REQUIREMENTS status closure, STATE normalization, push, PR, release, archive, or milestone-completion action occurs.
  </acceptance_criteria>
  <done>The composite remap has one auditable production event, a recoverable batch history, a byte-proven dry fixed point, and an index-safe commit that preserves unrelated user work while the close marker remains active.</done>
</task>

</tasks>

<verification>
- READY inputs and corpus match rehearsal exactly; dirty and staged intersections are approved/empty as required.
- Receipt records one successful production apply and complete preimages; recovery bundle is removed only after the scoped commit.
- Dry second run and affected-corpus hashes prove REMAP-05 without a second apply.
- Range, archive, source/gitlink, dirty-worktree, and index-staging gates pass; marker remains present.
</verification>

<success_criteria>
The immutable original plus complete dynamic supplement is applied once over the real composite diff, every oracle and real range holds, recovery makes partial writes safe, run two is a dry byte no-op, and the commit contains only citation edits or explicitly authorized overlap changes—never unrelated user work.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-05-SUMMARY.md` when done.
</output>
