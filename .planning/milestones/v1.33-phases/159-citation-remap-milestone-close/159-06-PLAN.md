---
phase: 159-citation-remap-milestone-close
plan: "06"
type: execute
wave: 6
depends_on: ["159-05"]
files_modified:
  - .planning/v1.33/159-close-readiness.json
  - .planning/v1.33/159-remap-record.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/v1.33/CITATIONS-STALE.md
autonomous: true
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "Closure cannot mutate records until production receipt=APPLIED/1, dry run=0/0, corpus hashes, range 4->3, archive PASS/12, index isolation, and source invariance all re-pass."
    - "Only the five REMAP bullets/traceability rows and the existing Phase-159 ROADMAP region are status/prose-edited; hand-authored files are never globally regenerated."
    - "Pre-existing STATE and other unrelated dirty bytes remain hash-identical and unstaged; a dirty closure-target file or nonempty index stops closure."
    - "CITATIONS-STALE.md remains present through readiness, scoped edits, and every pre-delete gate; its deletion is the final implementation-file mutation."
    - "The phase leaves v1.33 ready for /gsd-complete-milestone but performs no archive, release, push, PR, merge, or milestone-completion action."
  artifacts:
    - path: .planning/v1.33/159-close-readiness.json
      provides: "All-green gate results, preserved-dirty hashes, and exact licensed closure payload"
    - path: .planning/v1.33/159-remap-record.md
      provides: "Final scoped-close and marker-transition evidence"
    - path: .planning/REQUIREMENTS.md
      provides: "Exactly REMAP-01..05 checked and five traceability rows completed"
    - path: .planning/ROADMAP.md
      provides: "Exactly six Phase-159 plans checked and five criteria closed with measured facts"
    - path: .planning/v1.33/CITATIONS-STALE.md
      provides: "Close-blocking marker deleted as the final implementation mutation"
  key_links:
    - from: .planning/v1.33/159-production-apply.json
      to: .planning/v1.33/159-close-readiness.json
      via: "one event plus post-apply fixed-point/recovery/index evidence"
      pattern: "production_apply_events"
    - from: .planning/v1.33/159-close-readiness.json
      to: .planning/v1.33/CITATIONS-STALE.md
      via: "READY_TO_REMOVE_MARKER is required before final deletion"
      pattern: "READY_TO_REMOVE_MARKER"
---

<objective>
Close the bounded citation-staleness window by scoped hand-authored record edits and final marker removal, while preserving all unrelated work.

Purpose: REMAP evidence is incomplete until REQUIREMENTS/ROADMAP reflect measured results and the close-blocking marker is absent. This phase closes Phase 159 only; actual milestone completion remains a separate operator workflow.

Output: readiness receipt, final authoritative record, five REMAP closures, six checked Phase-159 plans, marker deletion last, and a handoff to `/gsd-complete-milestone`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/159-citation-remap-milestone-close/159-05-SUMMARY.md
@.planning/v1.33/159-production-preflight.json
@.planning/v1.33/159-production-apply.json
@.planning/v1.33/159-remap-record.md
@.planning/v1.33/CITATIONS-STALE.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-07-PLAN.md
</context>

## Artifacts this phase produces

- New `.planning/v1.33/159-close-readiness.json` fields: `status`, `production_apply_events`, `post_apply_rewrites`, `post_apply_documents_would_change`, `affected_corpus_hashes_match`, `range`, `archive_superseded_count`, `source_heads`, `marker_present`, `preserved_dirty_hashes`, `closure_target_hashes`, `allowed_regions`, `replacement_payload_sha256`, `state_mutation_authorized`, and `milestone_completion_authorized`.
- Updated `.planning/v1.33/159-remap-record.md` section `Scoped closure readiness and final marker transition`; no production facts are rewritten.
- Scoped `.planning/REQUIREMENTS.md` mutations: five REMAP checkboxes, five discharge sentences, five traceability statuses only.
- Scoped `.planning/ROADMAP.md` mutations: six Phase-159 plan checkboxes and measured additions to five existing criteria only; Goal/Depends on/Requirements remain byte-identical.
- Deleted `.planning/v1.33/CITATIONS-STALE.md`; no replacement marker is created.
- Executor-created `.planning/phases/159-citation-remap-milestone-close/159-06-SUMMARY.md` after the final commit, reporting the already-completed final marker mutation and read-only close checks; it does not alter the implementation/closure payload.
- No new code symbol or CLI flag is created.

<tasks>

<task type="auto">
  <name>Task 1: Re-run every close gate and freeze exact scoped edits while marker remains present</name>
  <files>.planning/v1.33/159-close-readiness.json</files>
  <read_first>
    - `.planning/v1.33/159-production-apply.json` — APPLIED/1, changed-document set, preimage/recovery state
    - `.planning/v1.33/159-remap-record.md` — production/no-op/range/archive/index evidence
    - `.planning/phases/159-citation-remap-milestone-close/159-05-SUMMARY.md` — commit and preserved-dirty outcome
    - `.planning/REQUIREMENTS.md` §6 and its REMAP traceability rows — only licensed requirements regions
    - `.planning/ROADMAP.md` Phase 159 — only licensed roadmap region
    - `.planning/v1.33/CITATIONS-STALE.md` — must still exist throughout this task
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-07-PLAN.md` scoped closure/count/hash precedent
  </read_first>
  <action>
Re-run the three focused test modules with `/usr/local/py-utils/venvs/pytest/bin/python`; the production-shaped dry run; affected-corpus hash comparison; real range assertion; archive correction gate; receipt/recovery state; source HEAD/clean-tree/gitlink inventory; original-manifest hash; dynamic supplemental/review subcounts; and zero-open ledger/overlay checks. Require APPLIED/1, no recovery tar, dry rewritten/documents 0/0, zero actionable/open IDs, range 128-131 -> 316-318/span 4->3, archive PASS/12, source HEADs exact, and marker present.

Require the real meta index has zero staged paths. Snapshot every pre-existing dirty path and patch/hash, especially STATE, and require equality with the preserved post-Plan-05 inventory. Require all closure targets (`REQUIREMENTS.md`, `ROADMAP.md`, `159-remap-record.md`, marker) are tracked and free of unrelated unstaged modifications relative to the Plan-05 commit; otherwise stop for review instead of staging whole files.

Snapshot REQUIREMENTS, ROADMAP, STATE, remap record, and marker: SHA-256, byte/line counts, heading/bullet counts, and exact licensed section extracts. Construct an exact replacement payload in memory:

- REQUIREMENTS: tick REMAP-01..05; append measured discharge sentences naming the applicable plans/record; change exactly five traceability rows to `Complete (..., closed 159-06)`.
- ROADMAP Phase 159: preserve Goal/Depends on/Requirements and all original criterion text; check exactly 159-01..06; append measured facts to five criteria using actual dynamic original/supplemental/review/exclusion/document totals, exact source SHAs, one apply event, dry 0/0, real range 4->3, archive PASS/12, and marker gate.
- Record: append readiness commands, scoped-diff expectations, preserved-dirty hashes, marker-present precondition, and `Next action: /gsd-complete-milestone`. Name SWEEP-13's intentionally open one-meta-commit clause without ticking or rewriting it.

Write only `159-close-readiness.json`, with `status: READY_TO_REMOVE_MARKER`, exact allowed region hashes and replacement payload digest. Set `state_mutation_authorized: false`, `milestone_completion_authorized: false`, and prohibitions on archive/release/push/PR/merge. REQUIREMENTS, ROADMAP, STATE, record, and marker must remain byte-identical during this task.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_remap_citations.py .planning/v1.33/tools/test_prepare_citation_remap.py .planning/v1.33/tools/test_rehearse_citation_remap.py &amp;&amp; python3 -c "import json; p=json.load(open('.planning/v1.33/159-close-readiness.json')); assert p['status']=='READY_TO_REMOVE_MARKER'; assert p['production_apply_events']==1; assert p['post_apply_rewrites']==0 and p['post_apply_documents_would_change']==0; assert p['affected_corpus_hashes_match']; assert p['archive_superseded_count']==12; assert p['range']=={'old_start':128,'old_end':131,'new_start':316,'new_end':318,'old_span':4,'new_span':3}; assert all(p['gates'].values()); assert p['state_mutation_authorized'] is False and p['milestone_completion_authorized'] is False" &amp;&amp; test -e .planning/v1.33/CITATIONS-STALE.md &amp;&amp; test "$(grep -cE '^\| REMAP-0[1-5] \| Phase 159 \| Pending \|' .planning/REQUIREMENTS.md)" = 5</automated>
  </verify>
  <acceptance_criteria>
    - All close gates freshly pass with the exact py-utils interpreter and measured 1/0/0, 4->3, PASS/12, source/hash, dynamic-count, recovery/index invariants.
    - Real index is empty; every unrelated dirty path/patch equals the preserved Plan-05 state; closure targets have no unrelated worktree overlap.
    - Readiness records exact snapshots and a digest of the licensed replacement payload; SWEEP-13 remains intentionally open and unchanged.
    - REQUIREMENTS, ROADMAP, STATE, remap record, and marker remain byte-identical; only readiness is created.
    - Readiness explicitly denies STATE mutation, milestone completion, archive/release, push, PR, and merge actions.
  </acceptance_criteria>
  <done>Every close gate and preservation invariant is freshly green, and exact scoped edits are frozen while the marker still blocks closure.</done>
</task>

<task type="auto">
  <name>Task 2: Apply scoped REMAP closure, recheck, and delete the marker as the final implementation mutation</name>
  <files>.planning/v1.33/159-remap-record.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/v1.33/CITATIONS-STALE.md</files>
  <read_first>
    - `.planning/v1.33/159-close-readiness.json` — every snapshot/current hash must still match
    - `.planning/REQUIREMENTS.md` §6 and five REMAP traceability rows
    - `.planning/ROADMAP.md` Phase 159, including six sequential plan entries
    - `.planning/v1.33/159-remap-record.md` — append only the frozen closure section
    - `.planning/v1.33/CITATIONS-STALE.md` — read last immediately before final deletion
  </read_first>
  <action>
Revalidate readiness and every current snapshot/index/dirty hash. Use `apply_patch` only for the three text files; never use a whole-file writer or GSD roadmap/requirements mutation helper.

First apply the exact REQUIREMENTS payload. Change only five REMAP checkboxes, append five measured discharge clauses, and change only five REMAP traceability rows. Preserve every SWEEP/DEAD/DEDUP/DECODE/LAND bullet/row and SWEEP-13's open state.

Second apply the exact ROADMAP Phase-159 payload. Preserve Goal/Depends on/Requirements verbatim, keep six plans/six strictly sequential waves, check exactly six plan lines, and append measured closure text to each of the five original criteria. Do not describe v1.33 as archived, released, shipped, or completed.

Third append the frozen record section with scope-diff checks, preserved-dirty hashes, marker-present pre-delete state, the intended final deletion, and separate `/gsd-complete-milestone` handoff. State no archive/release/push/PR/merge/completion action occurred.

Before marker deletion, run all in-memory/diff/read-only gates: readiness hashes for untouched regions; unchanged heading/bullet counts; REQUIREMENTS/ROADMAP line counts not lower; exactly five REMAP bullets/rows complete; exactly six plan lines checked; zero diff outside licensed regions; Goal/Depends on/Requirements unchanged; STATE and every unrelated dirty patch hash unchanged; dry no-op still 0/0; archive PASS/12; index still empty; marker still present.

Only then delete `.planning/v1.33/CITATIONS-STALE.md` with `apply_patch`. This deletion is the final implementation-file mutation: after it, make no further file edits or generators. Perform read-only negative/scope/hash checks, stage exactly readiness, record, REQUIREMENTS, ROADMAP, and marker deletion, and verify staged paths/blobs. Do not stage STATE or any preserved dirty/source/gitlink/manifest/ledger/package path. Commit without a pathspec as `docs(159-06): close citation staleness window and record milestone readiness`. Do not run milestone completion or any external action.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; test ! -e .planning/v1.33/CITATIONS-STALE.md &amp;&amp; test "$(grep -cE '^- \[x\] \*\*REMAP-0[1-5]\*\*' .planning/REQUIREMENTS.md)" = 5 &amp;&amp; test "$(grep -cE '^\| REMAP-0[1-5] \| Phase 159 \| Complete' .planning/REQUIREMENTS.md)" = 5 &amp;&amp; test "$(grep -cE '^- \[x\] 159-0[1-6]-PLAN\.md' .planning/ROADMAP.md)" = 6 &amp;&amp; python3 -c "import hashlib,json,pathlib; p=json.load(open('.planning/v1.33/159-close-readiness.json')); state=pathlib.Path('.planning/STATE.md').read_bytes(); assert hashlib.sha256(state).hexdigest()==p['preserved_dirty_hashes']['.planning/STATE.md']" &amp;&amp; test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" = "$(git show HEAD~1:.planning/ROADMAP.md | grep -c '^### Phase ')" &amp;&amp; test "$(grep -c '^- \[' .planning/REQUIREMENTS.md)" = "$(git show HEAD~1:.planning/REQUIREMENTS.md | grep -c '^- \[')" &amp;&amp; python3 .planning/phases/130-close-honesty-ledger-claim-gate-release-decision/check_record_corrections.py | grep -q "'superseded': 12" &amp;&amp; test "$(git log -1 --format=%s | grep -c '^docs(159-06):')" = 1 &amp;&amp; git show --name-status --format= HEAD | grep -q '^D[[:space:]].planning/v1.33/CITATIONS-STALE.md$'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five REMAP bullets and five traceability rows close; no other requirement family or SWEEP-13 changes.
    - Exactly six Phase-159 plan lines are checked and all five criteria gain measured closure facts; Goal/Depends on/Requirements and every other phase remain unchanged.
    - Hand-authored heading/bullet counts are unchanged, line counts do not fall, and no whole-file normalization occurs.
    - STATE and every unrelated dirty patch remain byte-identical/unstaged; staged set contains exactly the five closure paths and no source/gitlink/manifest/ledger/package path.
    - Marker is present for every pre-delete gate, deleted by the final implementation mutation, and absent afterward.
    - Commit and final record hand off to `/gsd-complete-milestone` without invoking it; no archive/release/push/PR/merge action occurs.
  </acceptance_criteria>
  <done>REMAP-01..05 and Phase 159 close against measured evidence, unrelated work remains intact, the marker is removed last and proven absent, and v1.33 is ready—but not actioned—for milestone completion.</done>
</task>

</tasks>

<verification>
- Receipt/apply/no-op/recovery/index/range/archive/source gates remain green at closure.
- Scoped diff proves only five REMAP rows/bullets, six Phase-159 plan rows, five Phase-159 criteria, and the record/marker transition changed.
- Pre-existing dirty bytes and STATE remain hash-identical and unstaged.
- `test ! -e .planning/v1.33/CITATIONS-STALE.md` passes after the final mutation.
- No milestone or external action is performed.
</verification>

<success_criteria>
All five REMAP requirements are closed without collateral normalization, the authoritative evidence and exact dirty-preservation state are recorded, and the close-blocking marker is the final implementation mutation and absent—leaving the separate milestone completion workflow to the operator.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-06-SUMMARY.md` when done.
</output>
