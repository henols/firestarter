---
phase: 159-citation-remap-milestone-close
plan: "02"
type: execute
wave: 2
depends_on: ["159-01"]
files_modified:
  - .planning/v1.33/tools/prepare_citation_remap.py
  - .planning/v1.33/tools/test_prepare_citation_remap.py
  - .planning/v1.33/159-late-citation-manifest.jsonl
  - .planning/v1.33/159-remap-exceptions.jsonl
  - .planning/v1.33/159-retarget-review.md
  - .planning/v1.33/159-corpus-overlay.json
autonomous: true
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "The original 13,692-record manifest remains byte-identical; the supplemental manifest is derived from the whole bounded temporal planning delta, not from a final-tree rescan."
    - "The 642 Phase-155-158-directory records remain an exact required subtotal (127/184/225/106), while the complete supplemental row count is dynamically reconciled and is at least the verified 881 lower bound."
    - "The manual review set contains all five re-deleted Phase-154 hand choices and all 105 ordinary original composite non-survivors, so its known floor is 110 before supplemental/anchor/location/dirty-overlap additions."
    - "Every live path move and every pre-existing dirty affected document is explicit, hash-pinned, and pending approval; no clean detached checkout is accepted as a substitute for live topology."
    - "The only nonzero actionable class after preparation is the exact dynamic needs-review stable-ID set; unmatched and hidden duplicate-group rows are zero."
  artifacts:
    - path: .planning/v1.33/tools/prepare_citation_remap.py
      provides: "Whole-window historical census, anchor/location reconciliation, review-set construction, and overlay inventory"
    - path: .planning/v1.33/tools/test_prepare_citation_remap.py
      provides: "Coverage for full-window scope, deterministic JSONL, lower bounds, and planted outside-directory omissions"
    - path: .planning/v1.33/159-late-citation-manifest.jsonl
      provides: "Dynamically sized complete post-manifest bounded-window record set"
    - path: .planning/v1.33/159-remap-exceptions.jsonl
      provides: "Exhaustive stable-ID exception and review ledger"
    - path: .planning/v1.33/159-retarget-review.md
      provides: "Evidence packet for the complete dynamic manual set"
    - path: .planning/v1.33/159-corpus-overlay.json
      provides: "Current citing-document topology, bytes, dirty overlaps, and staging feasibility"
  key_links:
    - from: .planning/v1.33/tools/prepare_citation_remap.py
      to: .planning/v1.33/tools/build_citation_manifest.py
      via: "imports the only citation grammar and JSONL conventions"
      pattern: "import build_citation_manifest as bcm"
    - from: .planning/v1.33/159-remap-exceptions.jsonl
      to: .planning/v1.33/159-retarget-review.md
      via: "exact equality of `status: needs_review` stable IDs and packet headings"
      pattern: "record_id"
---

<objective>
Build the complete historical supplemental corpus and expose every non-mechanical target, anchor, location, and dirty-overlap decision before human review.

Purpose: 642 records cover only four phase directories. The bounded window also contains 125 post-manifest Phase-154 records, 77 added v1.33 evidence records, and newly authored occurrences in modified global documents; research proves a lower bound of 881 but requires the exact total to be reconciled dynamically. Separately, five prior hand choices plus 105 ordinary original records require review.

Output: a deterministic preparer with tests, a complete dynamic supplemental manifest, an exhaustive ledger/review packet, and a hash-pinned live-corpus overlay inventory.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/159-citation-remap-milestone-close/159-01-SUMMARY.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-04-SUMMARY.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-12-SUMMARY.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/
@.planning/v1.33/sweep-citation-manifest.jsonl
@.planning/v1.33/CITATIONS-STALE.md
@.planning/v1.33/tools/build_citation_manifest.py
@.planning/v1.33/tools/test_build_citation_manifest.py
</context>

## Artifacts this phase produces

- New `.planning/v1.33/tools/prepare_citation_remap.py` symbols: `WindowCensus`, `HistoricalAnchor`, `reconcile_planning_locations`, `build_review_set`, and `write_corpus_overlay`.
- New preparer flags: `--repo-root`, `--original-manifest`, `--window-start-sha`, `--window-end-sha`, `--firmware-root`, `--app-root`, `--late-output`, `--exceptions-output`, `--review-output`, `--overlay-output`, and `--check-existing`.
- New `.planning/v1.33/tools/test_prepare_citation_remap.py`.
- New `.planning/v1.33/159-late-citation-manifest.jsonl` fields: `record_id`, `origin`, `historical_planning_file`, `historical_planning_line`, `citation_ordinal`, `source_root`, `source_sha`, `source_sha_candidates`, `source_text`, `source_text_end`, `text_status`, and `text_status_end`.
- New `.planning/v1.33/159-remap-exceptions.jsonl` fields: `record_id`, `classification`, `review_kind`, `status`, `candidate_evidence`, `chosen_source_sha`, `chosen_planning_file`, `chosen_current_start`, `chosen_current_end`, `chosen_current_text`, `chosen_current_text_end`, `rationale`, and `review_source`.
- New `.planning/v1.33/159-corpus-overlay.json` fields: `historical_path`, `current_path`, `git_state`, `preapply_sha256`, `expected_postapply_sha256`, `topology_action`, `dirty_overlap`, `approval_status`, `staging_strategy`, and `authorization_id`.
- New `.planning/v1.33/159-retarget-review.md`; it contains one `## Record <stable-id>` section per pending record and one `## Dirty overlap <authorization-id>` section per pending affected overlap.
- Executor-created `.planning/phases/159-citation-remap-milestone-close/159-02-SUMMARY.md` with exact dynamic supplemental/review/overlay counts and the zero-other-exception dry result.
- The immutable `.planning/v1.33/sweep-citation-manifest.jsonl` and all manifest-selected citing documents remain unmodified.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and test the whole-window historical preparer</name>
  <files>.planning/v1.33/tools/prepare_citation_remap.py, .planning/v1.33/tools/test_prepare_citation_remap.py</files>
  <read_first>
    - `.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md` sections `Post-manifest inventory`, `Record identity and anchors`, `Complete exception set`, and `Current dirty state and preservation obligations`
    - `.planning/phases/159-citation-remap-milestone-close/159-PATTERNS.md` sections for `159-late-citation-manifest.jsonl` and `159-remap-exceptions.jsonl`
    - `.planning/v1.33/tools/build_citation_manifest.py` `_CITATION_RE`, `_spans_from_match`, `_dump`, `_ordered`, and serialize-then-read checks
    - `.planning/v1.33/tools/test_build_citation_manifest.py` byte-identical regeneration tests
    - Phase 154-158 PLAN/SUMMARY commit and dependency evidence, especially final summaries `154-12`, `155-06`, `156-07`, `157-07`, and `158-07`
    - `git log --name-status 9a78bc6dc8b31087265f13c684ae850223806772..048a0394f3bf2cefb9c284f9bc7212d1958b8ca1 -- .planning` — the bounded temporal file delta, including renames and modified global documents
  </read_first>
  <action>
Create the preparer and focused test module. Reuse `build_citation_manifest` grammar/span/text-status helpers and `citation_paths.CandidateIndex`; do not copy the regex, resolver, or final-tree text into a historical field.

Set the current verified window start to manifest commit `9a78bc6dc8b31087265f13c684ae850223806772` and audit history to verify cutoff `048a0394f3bf2cefb9c284f9bc7212d1958b8ca1` is the Phase-158 completion/evolution boundary. Scan every non-tool `.planning` file added or modified in that Git interval. For added files, parse their first committed blob; for modified pre-existing files, reconcile citation occurrences positionally across blobs so newly authored records are distinguished from moved/deleted pre-manifest occurrences. Every supplemental record must carry the historically justified source SHA from artifact commit/dependency evidence or a nonempty candidate set routed to review. `git show &lt;source_sha&gt;:&lt;target&gt;` must reproduce both endpoint texts for a mechanically closed row.

Implement an independent coverage census over all changed paths/commits and compare its stable occurrence identities to manifest output. Tests must plant a citation in an added v1.33 record and in a modified global document outside Phase 155-158 directories; omitting either must make coverage RED. Assert the four directory subtotal is exactly 642 with phase record counts 127/184/225/106, added-file classes are 125 Phase-154 + 642 Phase-155-158 + 77 v1.33 = 844, and full output is at least 881 without fixing its final cardinality.

Normalize original IDs externally. Preserve the 815 Phase-154 hand choices and classify exactly 717 same-coordinate, 93 verbatim advanced, and five selected-text non-survivors using post-154 anchors `2ad5b322...`/`bc9d592...`. Independently enumerate exactly 105 readable ordinary original-manifest composite non-survivors across 46 documents/10 firmware targets; ensure the two currently hidden by duplicate groups have distinct ledger IDs. Keep all 267 non-read original rows as named exclusions.

Reconcile pending-to-completed tracked renames and untracked relocations. Inventory `git status --porcelain=v2` and intersect it with the affected citing-document set. Record current bytes/topology for every affected document, including the known modified `.planning/STATE.md` and deleted-old/untracked-new COBS relocation if still present. A topology change from research is not auto-approved: emit a `planning_location` or `dirty_overlap` review row. For a tracked dirty path, set `staging_strategy: citation_only_blob`; for a deletion/untracked relocation, set `requires_authorization` unless its inclusion was already explicitly authorized. Tests require deterministic JSON/JSONL output, duplicate-ID refusal, final-tree-oracle rejection, and byte-identical regeneration.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; /usr/local/py-utils/venvs/pytest/bin/python -m pytest -q .planning/v1.33/tools/test_prepare_citation_remap.py .planning/v1.33/tools/test_build_citation_manifest.py .planning/v1.33/tools/test_remap_citations.py</automated>
  </verify>
  <acceptance_criteria>
    - All three modules pass with the py-utils interpreter; planted citations outside the four phase directories make the coverage gate fail when omitted.
    - Two independent temporary runs emit byte-identical manifests, ledgers, review packets, and overlay inventories.
    - The 642 subtotal and 127/184/225/106 phase partition are exact; 844 added-file records reconcile as 125/642/77; the full supplemental result is dynamically measured and at least 881.
    - Exactly 105 ordinary original non-survivors remain individually ledgered, including the two duplicate-group-hidden rows; exactly five hand-choice non-survivors and 267 exclusions remain distinct.
    - Historical endpoint text comes from explicit Git blobs; a fixture that can pass only against final-tree text fails.
    - Original manifest SHA-256 remains `ecdd0fc84be1627f893e30f6369c0b9eedf2a69ce3ec351064828d82e72d992e`.
  </acceptance_criteria>
  <done>The preparer can prove complete bounded-window coverage and expose every target/anchor/location/dirty decision without guessing or mutating the original oracle.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize the dynamic supplemental corpus, ledger, review packet, and live overlay inventory</name>
  <files>.planning/v1.33/159-late-citation-manifest.jsonl, .planning/v1.33/159-remap-exceptions.jsonl, .planning/v1.33/159-retarget-review.md, .planning/v1.33/159-corpus-overlay.json</files>
  <read_first>
    - `.planning/v1.33/tools/prepare_citation_remap.py` and `.planning/v1.33/tools/test_prepare_citation_remap.py`
    - `.planning/v1.33/sweep-citation-manifest.jsonl` header plus all `retarget: true` rows
    - `.planning/v1.33/CITATIONS-STALE.md` sections `What the closer needs` and `Three hard requirements on the closer`
    - `.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md` tables `Original-manifest inventory` and `Post-manifest inventory`
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-07-SUMMARY.md` `The four residuals handed to Phase 159`
  </read_first>
  <action>
Run the preparer with explicit roots, window SHAs, and all four output paths. Write compact deterministic JSONL/JSON atomically, one schema header first for JSONL, UTF-8 LF, no blank lines, and serialize-then-read validation.

Require the late manifest's dynamic row count to be at least 881 and prove set equality with the independent whole-window census. Report the exact final count rather than substituting 881. Separately require the Phase-155-158 subset exactly 642/127/184/225/106 and the added-file subtotal exactly 844/125/642/77. Report newly authored modified-global rows as a measured exact count, while retaining research's net `+37` only as the verified lower-bound evidence that triggered positional reconciliation.

Define `review_ids` as the exact union of: five `known_post154_non_survivor`; all 105 `ordinary_original_non_survivor`; every supplemental endpoint non-survivor; every non-unique historical anchor; and every ambiguous/moved planning-location record. Require at least 110 record IDs, exactly five and 105 in the first two classes, and no upper bound. Repeated semantic decisions may share context but each stable ID gets its own row. Define affected dirty/topology approvals separately as `overlap_authorization_ids`; they join the blocking review packet but cannot reduce or inflate the record-ID floor.

Build `159-retarget-review.md` with complete evidence and blank decision fields. Semantic entries include old/post-154/current contexts, exact candidate endpoints, and both range endpoints. Anchor/location entries include every candidate SHA/path/ordinal and evidence. Dirty-overlap entries include live path/status/hash, pre-existing patch or topology change, whether citation-only staging is feasible, and blank choice among `preserve_unstaged`, `authorize_include`, or `stop`. Do not preselect.

Run the hardened remapper dry with both manifests, ledger, overlay, planning base, and correct original SHAs. Require exit 1 solely because `open_ids.needs_review` equals the complete pending record-ID set plus its explicitly keyed overlay-authorization set. Every other actionable counter and open-ID collection must be zero; examined records/documents and planned rewrites/documents must be nonzero. This dry run must now pass the pending-to-completed rename and fail only on explicit review, demonstrating the raw renamed-document blocker was actually reconciled.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; python3 .planning/v1.33/tools/prepare_citation_remap.py --repo-root /workspaces --original-manifest .planning/v1.33/sweep-citation-manifest.jsonl --window-start-sha 9a78bc6dc8b31087265f13c684ae850223806772 --window-end-sha 048a0394f3bf2cefb9c284f9bc7212d1958b8ca1 --firmware-root firestarter --app-root firestarter_app --late-output .planning/v1.33/159-late-citation-manifest.jsonl --exceptions-output .planning/v1.33/159-remap-exceptions.jsonl --review-output .planning/v1.33/159-retarget-review.md --overlay-output .planning/v1.33/159-corpus-overlay.json --check-existing &amp;&amp; set +e; python3 .planning/v1.33/tools/remap_citations.py /workspaces --manifest .planning/v1.33/sweep-citation-manifest.jsonl --manifest .planning/v1.33/159-late-citation-manifest.jsonl --exceptions .planning/v1.33/159-remap-exceptions.jsonl --corpus-overlay .planning/v1.33/159-corpus-overlay.json --planning-base-sha 9a78bc6dc8b31087265f13c684ae850223806772 --pre-sweep-sha firestarter=8695ee52c27a4bee4387c5c489afd5f3d7275e8a --pre-sweep-sha firestarter_app=6bfa6453d1bac232eb81ab35fa7f14b50b0b291a --report-json /tmp/gsd-159-plan02-dry.json --quiet-notes; rc=$?; set -e; test "$rc" = 1 &amp;&amp; python3 -c "import json; from collections import Counter; late=[json.loads(x) for x in open('.planning/v1.33/159-late-citation-manifest.jsonl') if '\"_schema\"' not in x]; assert len(late)&gt;=881; phase=Counter(r.get('phase') for r in late if r.get('phase') in {'155','156','157','158'}); assert phase=={'155':127,'156':184,'157':225,'158':106}; rows=[json.loads(x) for x in open('.planning/v1.33/159-remap-exceptions.jsonl') if '\"_schema\"' not in x]; pending=[r for r in rows if r['status']=='needs_review']; assert sum(r['classification']=='known_post154_non_survivor' for r in pending)==5; assert sum(r['classification']=='ordinary_original_non_survivor' for r in pending)==105; assert len({r['record_id'] for r in pending})&gt;=110; report=json.load(open('/tmp/gsd-159-plan02-dry.json')); assert report['open_ids']['needs_review']; assert all(v==0 for k,v in report['actionable_counts'].items() if k!='needs_review'); assert all(not v for k,v in report['open_ids'].items() if k!='needs_review'); assert all(report['totals'][k]&gt;0 for k in ('examined_records','examined_documents','planned_rewrites','planned_documents'))" &amp;&amp; sha256sum .planning/v1.33/sweep-citation-manifest.jsonl | grep -q '^ecdd0fc84be1627f893e30f6369c0b9eedf2a69ce3ec351064828d82e72d992e '</automated>
  </verify>
  <acceptance_criteria>
    - Supplemental cardinality is reported as a measured exact result and is at least 881; no plan artifact describes 642 or 881 as the fixed final total.
    - The 642 phase-directory subset, its 127/184/225/106 partition, and the 844 added-file 125/642/77 partition reconcile exactly.
    - Review record IDs include exactly 5 hand-choice non-survivors and 105 ordinary original non-survivors, with total at least 110 plus all dynamic supplemental/anchor/location cases.
    - Every affected dirty/topology overlap appears in both the overlay inventory and review packet with a staging-feasibility decision pending; no user byte is approved or staged by this plan.
    - The production-shaped dry run gets past the tracked rename and exits 1 solely for the exact review/authorization set; all other actionable classes are zero.
    - No manifest-selected planning document, original manifest, source repo, gitlink, or marker is changed.
  </acceptance_criteria>
  <done>The complete bounded-window corpus and known-minimum-110 review population are materialized, and live dirty topology is explicit and fail-closed before the manual checkpoint.</done>
</task>

</tasks>

<verification>
- All preparer, generator, and remapper tests pass with the py-utils interpreter.
- Full supplemental count is dynamic and at least 881; 642 remains only the exact four-directory subset.
- Review count is dynamic and at least 110, with exact 5/105 mandatory classes.
- Dry run exits 1 only on the exact pending stable-ID/authorization set and writes zero bytes.
- Original manifest hash and staleness marker remain unchanged.
</verification>

<success_criteria>
Every original and bounded-window citation has a stable historical identity or an explicit blocking review row; all 105 ordinary non-survivors and five re-deleted hand choices are visible; the complete supplement is proven beyond the 642 subset; and current dirty bytes/topology are captured without authorizing their inclusion.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-02-SUMMARY.md` when done.
</output>
