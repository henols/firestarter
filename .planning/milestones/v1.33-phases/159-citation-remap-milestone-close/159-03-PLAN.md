---
phase: 159-citation-remap-milestone-close
plan: "03"
type: execute
wave: 3
depends_on: ["159-02"]
files_modified:
  - .planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md
autonomous: false
requirements: [REMAP-01, REMAP-02, REMAP-03, REMAP-04, REMAP-05]
must_haves:
  truths:
    - "A human reviews the exact dynamically measured record-ID set, including exactly five Phase-154 hand-choice non-survivors and all 105 ordinary original non-survivors; the record-ID floor is at least 110."
    - "Every supplemental semantic deletion, non-unique historical anchor, and ambiguous planning location has one evidence-backed decision or the checkpoint remains blocked."
    - "Every dirty affected path has an explicit preservation/inclusion/stop decision; an untracked relocation is never treated as citation-only-stageable."
    - "Repeated semantic decisions may be reviewed together only when every stable record ID retains an individual chosen endpoint/text/rationale."
    - "The manual checkpoint produces a durable 159-03-SUMMARY.md even though it changes no manifest, ledger, source, or citing document."
  artifacts:
    - path: .planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md
      provides: "Complete stable-ID and dirty-overlap approval handoff for deterministic Plan-04 transcription"
  key_links:
    - from: .planning/v1.33/159-retarget-review.md
      to: .planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md
      via: "one `Decision` section for every pending record and one `Overlap` section for every pending authorization"
      pattern: "record_id"
---

<objective>
Settle every semantic target, historical anchor, planning location, and dirty-overlap choice that automation cannot make without inventing meaning or authority.

Purpose: no Phase-159 CONTEXT.md exists because the operator chose no discussion phase, but REMAP-02 still requires human judgment for at least 110 stable records and explicit authority for dirty affected bytes/topology. This is the phase's only manual checkpoint.

Output: `159-03-SUMMARY.md`, containing one complete decision per pending stable record ID and one authorization decision per dirty overlap. No corpus or ledger mutation occurs here.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-citation-remap-milestone-close/159-RESEARCH.md
@.planning/phases/159-citation-remap-milestone-close/159-VALIDATION.md
@.planning/phases/159-citation-remap-milestone-close/159-02-SUMMARY.md
@.planning/v1.33/159-retarget-review.md
@.planning/v1.33/159-remap-exceptions.jsonl
@.planning/v1.33/159-corpus-overlay.json
</context>

## Artifacts this phase produces

- New `.planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md`.
- Each `## Decision <record_id>` section records `review_kind`, selected source SHA or planning location or final start/end, exact endpoint text(s), rationale, and evidence reference.
- Each `## Overlap <authorization_id>` section records `current_path`, reviewed pre-apply SHA-256, `decision` (`preserve_unstaged`, `authorize_include`, or `stop`), staging strategy, scope of authorization, and rationale.
- No symbol, CLI flag, manifest field, ledger row, corpus byte, ROADMAP/REQUIREMENTS/STATE field, source file, or marker is changed by this checkpoint.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Review the complete dynamic record set and every dirty-overlap authorization</name>
  <files>.planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md</files>
  <read_first>
    - `.planning/v1.33/159-retarget-review.md` — complete evidence packet; do not review a subset or a hard-coded first five
    - `.planning/v1.33/159-remap-exceptions.jsonl` — authoritative `status: needs_review` record-ID set and classification counts
    - `.planning/v1.33/159-corpus-overlay.json` — authoritative dirty/topology authorization set, live hashes, and staging feasibility
    - `.planning/phases/159-citation-remap-milestone-close/159-02-SUMMARY.md` — exact supplemental cardinality, review total, and zero-other-exception proof
    - every source Git object/current context/document location named by a packet — inspect surrounding evidence, not just suggested coordinates
  </read_first>
  <decision>Choose one evidence-backed resolution for every pending record ID and one authorized handling for every affected dirty/topology overlap, or stop on a named evidence/authority defect.</decision>
  <action>
Before pausing, run the automated census and print: total pending record IDs; classification/review-kind breakdown; the exact five `known_post154_non_survivor` IDs; all 105 `ordinary_original_non_survivor` IDs; all additional IDs; and every pending overlay authorization ID/path/hash/staging strategy. Require record-ID count at least 110, exactly five/105 mandatory classes, no duplicates, exact equality with `## Record` review-packet sections, and exact equality between pending overlay rows and `## Dirty overlap` packet sections.

Present all packets without selecting a preferred answer. For `semantic_target`, require chosen final start/end, exact current endpoint text(s), and semantic continuity rationale. For `historical_anchor`, require a chosen source root/SHA contained in the candidates plus endpoint-text and artifact-authoring evidence. For `planning_location`, require current path/group/variant/ordinal and relocation evidence. A repeated semantic resolution may be approved in one batch statement only if the summary still expands it to one complete section per stable ID.

For each dirty overlap, require one decision:

- `preserve_unstaged` is valid only when the overlay says `citation_only_blob`; the unrelated pre-existing patch remains unstaged and its before/after digest must be preserved.
- `authorize_include` must name the exact pre-existing path/topology change being deliberately included; this is required if an untracked relocation must enter the Phase-159 commit.
- `stop` blocks resumption until the user separately commits/reverts/relocates the change; do not continue Plan 04.

The known deleted-old/untracked-new COBS topology, if still present, cannot use `preserve_unstaged`; it requires `authorize_include` or `stop`. A changed SHA/path since Plan 02 is an evidence defect and also blocks.

After approval, create `159-03-SUMMARY.md` with measured totals/breakdowns, one complete `## Decision` section per record ID, one complete `## Overlap` section per authorization ID, and a statement that no ledger/manifest/source/citing document was modified. Parse the completed summary and require exact ID equality with both pending sets before marking the checkpoint complete.
  </action>
  <verify>
    <automated>cd /workspaces &amp;&amp; python3 -c "import json; from collections import Counter; rows=[json.loads(x) for x in open('.planning/v1.33/159-remap-exceptions.jsonl') if '\"_schema\"' not in x]; pending=sorted((r for r in rows if r['status']=='needs_review'), key=lambda r:r['record_id']); ids=[r['record_id'] for r in pending]; assert len(ids)&gt;=110 and len(ids)==len(set(ids)); assert sum(r['classification']=='known_post154_non_survivor' for r in pending)==5; assert sum(r['classification']=='ordinary_original_non_survivor' for r in pending)==105; assert all(r['review_kind'] in {'semantic_target','historical_anchor','planning_location'} for r in pending); packet=open('.planning/v1.33/159-retarget-review.md').read(); packet_ids=sorted(line.split(maxsplit=2)[2] for line in packet.splitlines() if line.startswith('## Record ')); assert packet_ids==ids; overlay=json.load(open('.planning/v1.33/159-corpus-overlay.json')); overlaps=sorted(r['authorization_id'] for r in overlay['documents'] if r['dirty_overlap'] and r['approval_status']=='needs_review'); packet_overlaps=sorted(line.split(maxsplit=3)[3] for line in packet.splitlines() if line.startswith('## Dirty overlap ')); assert packet_overlaps==overlaps; print('records',len(ids),dict(Counter(r['classification'] for r in pending)),dict(Counter(r['review_kind'] for r in pending))); print('overlaps',len(overlaps)); print('\n'.join(ids+overlaps))"</automated>
    <human-check>Review every printed record and overlap packet. Provide all required evidence fields; any `stop` decision or changed live hash keeps the checkpoint blocked.</human-check>
  </verify>
  <acceptance_criteria>
    - The reviewed record set is dynamic, contains at least 110 unique IDs, and contains exactly five Phase-154 hand-choice plus 105 ordinary-original non-survivor IDs before additions.
    - Every record decision names exact evidence appropriate to `semantic_target`, `historical_anchor`, or `planning_location`; no pending ID is omitted.
    - Every dirty overlap has one exact-path/hash decision; `preserve_unstaged` appears only for citation-only-stageable tracked paths, and every included pre-existing change is explicitly authorized by ID.
    - No `stop` decision remains and no reviewed live hash/topology has changed before summary creation.
    - `159-03-SUMMARY.md` declares the measured totals and its decision/authorization sets exactly equal the two pending input sets.
    - Only `159-03-SUMMARY.md` is created; the ledger, overlay inventory, manifests, source, citing documents, hand-authored closure files, and marker remain unchanged.
  </acceptance_criteria>
  <done>Every non-mechanical record and dirty-overlap authority question has a complete evidence-backed decision, durably recorded for deterministic transcription.</done>
</task>

</tasks>

<verification>
- Pre-checkpoint census proves the known record floor is 110 with exact 5/105 mandatory classes.
- Completed summary contains exactly one decision per pending record ID and one authorization per pending dirty overlap.
- No `stop`, missing evidence, stale live hash, extra ID, or omitted ID remains.
- No repository artifact other than `159-03-SUMMARY.md` is changed by the checkpoint.
</verification>

<success_criteria>
The complete dynamic semantic/anchor/location set and every dirty affected path have explicit human decisions, no authority is inferred from the absence of CONTEXT.md, and Plan 04 can apply those decisions mechanically by stable ID without absorbing an unrelated change by accident.
</success_criteria>

<output>
Create `.planning/phases/159-citation-remap-milestone-close/159-03-SUMMARY.md` when done.
</output>
