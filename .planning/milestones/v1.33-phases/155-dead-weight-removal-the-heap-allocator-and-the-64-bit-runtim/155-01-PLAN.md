---
phase: 155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/155-before-figures.md
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03]
must_haves:
  truths:
    - "A committed record exists holding every pre-change figure this phase's savings are measured against, each carrying the verbatim command that produced it."
    - "The record proves malloc and free have exactly one caller (mem_util_blank_check) and that the five 64-bit helpers referenced from user code have exactly one caller (rurp_read_voltage_mv), by disassembly attribution rather than by reading source."
    - "The record carries the full 11-symbol 64-bit table with per-symbol sizes, so the 438 B named-subset figure and the 528 B contiguous-blob figure are both provable from it."
    - "The record carries per-target handle and token sizes so DEAD-02's RAM headroom sentence can be derived rather than quoted."
    - "Every figure in the record is labelled WARM or COLD."
  artifacts:
    - .planning/v1.33/155-before-figures.md
  key_links:
    - "155-before-figures.md is the ONLY source for this phase's before half; Phases 156, 157 and 158 each invalidate the measurements it captures, so no later plan can re-derive them."
    - "The three flash/RAM pairs recorded here are the denominators for every delta plan 06 computes; the baseline file is +478 B stale and must never be used as the before position."
  prohibitions:
    - "Must NOT edit any file under firestarter/src, firestarter/include or firestarter/test -- this plan runs entirely before the first source edit and its whole value is that the tree is unchanged."
    - "Must NOT re-anchor or write scripts/baseline/size_baseline.json (LAND-01 / Phase 158 owns the cold re-record)."
    - "Must NOT widen build_src_filter in platformio.ini."
    - "Must NOT touch include/memory_utils.h (Phase 156)."
    - "Must NOT change anything in firestarter_app, and must NOT git add the firestarter_app gitlink in the meta repo."
    - "Must NOT author or imply a bench/hardware criterion (D-02)."
    - "Must NOT run the Phase 154 citation remap tool or repair any file:LINE citation (Phase 159 / D-01)."
---

<objective>
Capture and commit every pre-change figure Phase 155's claims are measured against,
before the first byte of firmware source is edited.

Purpose: these figures are irrecoverable. The pre-change symbol table, the
disassembly-derived sole-caller attribution, the three flash/RAM pairs, and the
per-target `handle` sizes cease to exist the moment plan 04 or plan 05 lands, and
Phases 156, 157 and 158 each move the same numbers again. `scripts/baseline/size_baseline.json`
cannot stand in for them: it is already +478 B stale against this tree by its own
`meta` block's admission, so "delta vs baseline" is not "delta from this phase".

Output: `.planning/v1.33/155-before-figures.md`, an AUTHORITATIVE record in the
meta repo, following the `.planning/v1.33/baseline-pre-sweep.md` convention --
every number carries the verbatim command that produced it, nothing is quoted from
`155-RESEARCH.md`, and every figure is labelled WARM or COLD.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-PATTERNS.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-VALIDATION.md
</context>

## Multi-Source Coverage Audit (phase-wide, recorded once here)

| Source | Item | Covered by |
|---|---|---|
| GOAL | Delete two whole libraries that one call site each was dragging in | 04 (64-bit runtime), 05 (heap), 02 + 06 (proof) |
| GOAL | The allocator's removal closes a latent unchecked-allocation dereference | 05 (code), 01 + 06 (record) |
| REQ | DEAD-01 | 01 (before), 02 (gate), 05 (change), 06 (landing) |
| REQ | DEAD-02 | 01 (pre-state + RAM derivation), 05 (change), 06 (latent-defect record) |
| REQ | DEAD-03 | 01 (before, 11 symbols), 02 (gate), 04 (change), 06 (landing) |
| REQ | DEAD-04 | 04 (oracle + reformulation), 06 (landing) |
| REQ | DEAD-05 | 03 (phrasing gate), 04 (ceiling in source + oracle docstring), 06 (corpus run) |
| REQ | DEAD-06 | 05 (both suites + 4 comments, same commit), 06 (172/172 confirmation) |
| RESEARCH | C-1 the `k` guard is a live 2 B decision | 04 (ships 4194303UL), 06 (records the 2 B delta) |
| RESEARCH | C-2 the -650/-714 split does not decompose | 06 (marks the split UNVERIFIED, quotes the total) |
| RESEARCH | C-3 the RAM sentence double-counts 512 B | 01 (measures 603/512/1115), 05 (comment), 06 (record) |
| RESEARCH | C-4 the 64-bit list is undercounted by 90 B | 01 (11-symbol table), 02 (gate asserts 11), 06 (both figures) |
| RESEARCH | C-5 "the same statement" is false | 05 (three comment blocks corrected), 06 (record) |
| RESEARCH | Sole-caller status proven by disassembly, not by reading source | 01 |
| RESEARCH | The oracle must be a pytest in `firestarter/tests/`, not a native suite | 04 |
| RESEARCH | Neither overflow guard fires anywhere in the nominal grid | 04 (4 boundary + 2 sentinel cases) |
| RESEARCH | The tolerance window is asymmetric, not +/-5 % | 04 (source comment), 06 (record) |
| RESEARCH | The DEAD-06 change is compiler-forced; header + tests in one commit | 05 |
| RESEARCH | The preserved reference will not cherry-pick; it is semantic only | 04, 05 |
| RESEARCH | Pitfall 3 -- the host suite asserts whole-repo porcelain | 02, 04, 05, 06 (all run pytest only after committing) |
| RESEARCH | Pitfall 8 -- a bare `grep -c` gate inverts on zero matches | 02 (the gate parses in Python, never greps) |
| PATTERNS | The new gate is FOUR files, not one (checker convention) | 02 |
| PATTERNS | `include/memory_utils.h` is out of scope (belongs to Phase 156) | prohibitions in every plan; named in 05 |
| PATTERNS | `build_src_filter` sits at platformio.ini :205/:294/:332/:370/:412/:468 | 04 (cited by symbol, not by line) |
| PATTERNS | Four defects in the reference's `rurp_common.cpp` hunk | 04 |
| PATTERNS | The C-5 error appears in THREE comment blocks, not one | 05 |
| PATTERNS | `test_config_storage_dualslot.py` names the oracle's shape as hollow | 04 (answered head-on) |
| CONTEXT | OQ-1 `k` = 4194303, phase records -1366 B | 04, 06 |
| CONTEXT | OQ-2 the gate asserts all 11 symbols; record 438 B and 528 B | 01, 02, 06 |
| CONTEXT | OQ-3 correct the tolerance statement | 04, 06 |
| CONTEXT | OQ-4 both per-target RAM figures, "shared heap-and-stack headroom" | 01, 05, 06 |
| CONTEXT | OQ-5 DEAD-05 mechanised as a two-halved phrasing gate | 03, 06 |
| CONTEXT | OQ-6 `memory_utils.h` out of scope; trust PATTERNS over RESEARCH | prohibitions in every plan |

No source item is MISSING. Excluded and not planned, with reasons: `include/memory_utils.h`,
`json_parser.c`, `eprom.cpp`, `flash_intel.cpp`, `flash_utils.cpp`, `eeprom_28c.cpp` (Phases
156/157/158 per the RESEARCH scope fence); the `size_baseline.json` cold re-record and the
BASE-01 native case-count mismatch (LAND-01 / LAND-03, Phase 158); the citation remap
(REMAP-*, Phase 159); any bench criterion (D-02).

## Artifacts this phase produces

Every symbol, file and value Phase 155 creates. Repeated verbatim in every plan so a
source-grounding pass can exclude newly-created symbols from drift verification.

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/155-before-figures.md` | 01 | meta |
| `firestarter/scripts/check_no_heap_or_64bit_symbols.py` | 02 | fw |
| `firestarter/tests/test_check_no_heap_or_64bit_symbols.py` | 02 | fw |
| `firestarter/tests/fixtures/planted_no_heap_or_64bit_symbols_prechange_uno/README.md` | 02 | fw |
| `firestarter/tests/fixtures/planted_no_heap_or_64bit_symbols_prechange_uno/avr-nm-uno.txt` | 02 | fw |
| Gate script symbols: `HEAP_SYMBOLS`, `DI64_SYMBOLS`, `REQUIRED_ANCHORS`, `FIRESTARTER_AVR_NM`, `_resolve_avr_nm`, `_listing_for_target`, `_scan_listing`, `_print_pass`, `_print_fail`, `_parse_argv`, `main` | 02 | fw |
| `FLOOR` 6 -> 7 and `FIXTURE_FLOOR` 15 -> 16 in `firestarter/tests/test_checker_convention.py` | 02 | fw |
| `.planning/v1.33/tools/check_dead05_phrasing.py` | 03 | meta |
| `.planning/v1.33/tools/test_check_dead05_phrasing.py` | 03 | meta |
| `.planning/v1.33/tools/fixtures/planted_dead05_phrasing_violation.md` | 03 | meta |
| `.planning/v1.33/155-dead05-phrasing-corpus.md` | 03 | meta |
| `firestarter/tests/test_voltage_reformulation_oracle.py` | 04 | fw |
| Oracle symbols: `_RURP_COMMON_REL`, `_SCAN_RURP_COMMON`, `_SHIELD_HEADER_REL`, `_strip_comments`, `_line_of`, `_extract_function_body`, `_v64`, `_v32`, `_SCALE_NUMERATOR` (1100), `_GUARD_SUM_MAX` (3900000), `_GUARD_K_MAX` (4194303) | 04 | fw |
| New locals in `rurp_read_voltage_mv`: `sum`, `k`, `bg`; guard expressions `sum > 3900000UL` and `k > 4194303UL` | 04 | fw |
| File-scope static `blank_check_saved_address` in `firestarter/src/proms/memory.cpp` | 05 | fw |
| `firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/README.md` + `avr-nm-uno.txt` | 06 | fw |
| New leg `test_real_postchange_listing_exits_zero` in `test_check_no_heap_or_64bit_symbols.py` | 06 | fw |
| `.planning/v1.33/155-after-figures.md` (the phase record) | 06 | meta |

<tasks>

<task type="auto">
  <name>Task 1: Assert the tree is unchanged, then capture every irrecoverable pre-change measurement</name>
  <files>
    (no file is edited by this task -- it only builds, measures and records values for task 2;
    the build products under firestarter/.pio/build/ are gitignored)
  </files>
  <read_first>
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md` -- sections "Measured Figures", "DEAD-01 / DEAD-03: the mechanical symbol-absence check", "DEAD-02: the latent defect", "Common Pitfalls" (all eight), "Validation Architecture"
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-VALIDATION.md` -- "Measured baseline", "Wave 0 Requirements"
    - `.planning/v1.33/baseline-pre-sweep.md` -- the record convention this task's output copies (frontmatter shape, "each number carries the verbatim command that produced it", "nothing here is quoted from research")
    - `firestarter/src/proms/memory.cpp` lines 388-425 -- the current allocator code being measured
    - `firestarter/src/boards/rurp_common.cpp` lines 45-75 -- the current 64-bit arithmetic being measured
    - `firestarter/include/firestarter.h` lines 218-232 -- the `progress_data` member being measured
    - `firestarter/scripts/baseline/size_baseline.json` -- its `meta` block, to quote the +478 B staleness admission
    - `/workspaces/CLAUDE.md` and `/workspaces/firestarter/CLAUDE.md`
  </read_first>
  <action>
Run in `/workspaces/firestarter` unless a step says otherwise. Nothing in this task edits
a tracked file. If any assertion in step 1 fails, STOP and report -- every figure below is
only meaningful against an unchanged tree.

Step 1 -- assert and record the anchor state. Record `FW_PRE_SHA` from
`git -C firestarter rev-parse HEAD`. Assert `git -C firestarter branch --show-current` is
`gsd/v1.33-source-hygiene-firmware-size-reduction`. Assert `git -C firestarter status --porcelain`
is empty. Assert `git -C firestarter diff --name-only HEAD` is empty. Separately record the
meta repo's HEAD sha and note that `firestarter_app`'s gitlink shows as modified in the meta
repo -- that is pre-existing Phase 154 drift, it is operator-gated, and no task in this phase
may stage or re-pin it.

Step 2 -- warm builds, all three AVR targets. Run `pio run -e uno`, `pio run -e uno328pb` and
`pio run -e leonardo`, capturing each build log to a scratch file under the session scratch
directory. From each log record the `Flash:` and `RAM:` byte figures and the `flash_total` /
`ram_total` capacities. Expected (assert, do not merely record): uno 26026 flash / 1575 RAM,
uno328pb 26074 / 1581, leonardo 28170 / 2016; capacities 32768 flash on all three and 2048 /
2048 / 2560 RAM. Label these figures WARM -- the `.pio/build` trees are warm and LAND-01 /
Phase 158 owns the cold re-record. If a figure differs from the expected value, do not
proceed: report the difference, because it means the tree or toolchain moved since research.

Step 3 -- full symbol tables. Resolve `avr-nm` at
`$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm` and assert it is executable. For
each of the three ELFs at `.pio/build/<env>/firestarter_<env>.elf`, run
`avr-nm --print-size --size-sort -C <elf>` and save the complete listing to the scratch
directory. From each listing extract and record:
  (a) the heap set -- `malloc`, `free`, `realloc`, `calloc`, `__brkval`, `__flp`,
      `__malloc_heap_start`, `__malloc_heap_end`, `__malloc_margin` -- with the address, size
      and section of every one that is PRESENT, and an explicit note naming the ones that are
      ABSENT (`realloc` and `calloc` are expected absent; record that, do not omit it).
  (b) the full ELEVEN-symbol 64-bit set -- `__muldi3`, `__muldi3_6`, `__umulsidi3`,
      `__umulsidi3_helper`, `__umoddi3`, `__udivdi3`, `__udivdi3_umoddi3`, `__udivmod64`,
      `__ashrdi3`, `__lshrdi3`, `__adddi3` -- each with address and size.
  (c) `rurp_read_voltage_mv` (expected 0x1b2 = 434 B), `mem_util_blank_check` (expected
      0x1fe = 510 B), `handle` in .bss (expected 603 B on uno and uno328pb, 1115 B on
      leonardo) and the jsmn `tokens` object (expected 512 B).
  (d) the two counts the plan-02 gate will later assert are zero: the heap-set match count
      (expected 7 on each ELF) and the 64-bit-set match count (expected 11 on each ELF).
Compute and record the two contiguity spans on uno from the addresses and sizes: the 64-bit
blob and the heap blob, and whether they abut. Expected: 64-bit `0x6036`-`0x6246` = 528 B,
heap `0x6246`-`0x6490` = 586 B, contiguous. Also record the arithmetic sum of the eight
symbols DEAD-03 names by name (expected exactly 438 B) alongside the eleven-symbol total
(expected 528 B) -- OQ-2 requires both figures on the record.

Step 4 -- sole-caller attribution by disassembly, not by reading source. Run
`avr-objdump -d .pio/build/uno/firestarter_uno.elf` into a scratch file, then run a short
throwaway Python script (written under the session scratch directory, NOT committed) that
walks the disassembly, tracks the enclosing function from the `<name>:` header lines,
and for every `call`/`jmp` line attributes each referenced symbol to that enclosing function.
Report, for each of `malloc`, `free` and the eleven 64-bit symbols, the exact set of
attributed callers. Expected: `malloc` and `free` each have exactly one caller,
`mem_util_blank_check`; `__muldi3`, `__adddi3`, `__lshrdi3`, `__udivdi3` and `__umulsidi3`
each have exactly one user-code caller, `rurp_read_voltage_mv`; the remaining six are reached
only from inside the blob. Record the raw attribution output verbatim -- this is the strongest
available evidence for DEAD-01 criterion 1 and DEAD-03's "alone pulled them in", and it cannot
be reproduced after the change.

Step 5 -- test and gate baselines on the clean committed tree. Run `pio test -e native` and
`pio test -e native_nodevtools` one at a time (never concurrently -- D-04 records that this
suite's failures correlate with run duration, not tree content) and record cases, suites and
wall time for each; expected 172 cases / 172 succeeded / 17 suites on both. Run
`python3 -m pytest tests/ -q` (system python3, not the PlatformIO penv, which has no pytest)
and record the pass count; expected 323 passed. Run
`python3 scripts/check_build_warnings.py --log uno=<uno build log>` and record its verdict.
Do NOT run `check_size_baseline.py --rebuild` and do not pass it any flag that writes: the
baseline must not move in this phase.

Step 6 -- record the baseline-staleness arithmetic that makes this record necessary. From
`scripts/baseline/size_baseline.json` read the recorded `flash_used` for each AVR target and
subtract it from this task's measured figure; expected +478 / +476 / +540. Quote the `meta`
block sentence that records this as deliberate. State the consequence in one line: after the
change, `check_size_baseline.py` will report uno as roughly -888 B against the baseline, and
that number is correct for the MERGE-05 policy run and WRONG as this phase's saving.

Hold every value from steps 1-6 for task 2 to write into the record.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && NM="$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm" && test -x "$NM" && for e in uno uno328pb leonardo; do test -f ".pio/build/$e/firestarter_$e.elf" || exit 1; H=$("$NM" ".pio/build/$e/firestarter_$e.elf" | grep -cE ' (T|t|B|b|D|d) (malloc|free|realloc|calloc|__brkval|__flp|__malloc_heap_start|__malloc_heap_end|__malloc_margin)$' || true); D=$("$NM" ".pio/build/$e/firestarter_$e.elf" | grep -cE '__(muldi3|muldi3_6|udivmod64|lshrdi3|udivdi3|udivdi3_umoddi3|umoddi3|adddi3|umulsidi3|umulsidi3_helper|ashrdi3)$' || true); echo "$e heap=$H di=$D"; test "$H" -eq 7 || exit 1; test "$D" -eq 11 || exit 1; done && echo PRECHANGE_STATE_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter status --porcelain` is empty at both the start and the end of the task.
    - `git -C firestarter branch --show-current` prints `gsd/v1.33-source-hygiene-firmware-size-reduction`.
    - `pio run` reports flash/RAM of exactly 26026/1575 (uno), 26074/1581 (uno328pb), 28170/2016 (leonardo).
    - `avr-nm --print-size` reports `mem_util_blank_check` with size `000001fe` and `rurp_read_voltage_mv` with size `000001b2` on the uno ELF.
    - The heap-set match count is exactly 7 and the 64-bit-set match count is exactly 11 on each of the three ELFs.
    - `realloc` and `calloc` are recorded as ABSENT by name, not silently omitted.
    - The eight symbols DEAD-03 names sum to exactly 438 B and the eleven-symbol set sums to exactly 528 B, both computed from the captured listing.
    - The disassembly attribution output shows `malloc <- {mem_util_blank_check}` and `free <- {mem_util_blank_check}` -- exactly one caller each, no others.
    - The disassembly attribution output shows exactly one user-code caller, `rurp_read_voltage_mv`, for each of `__muldi3`, `__adddi3`, `__lshrdi3`, `__udivdi3`, `__umulsidi3`.
    - `pio test -e native` and `pio test -e native_nodevtools` each report `172 test cases: 172 succeeded` across 17 suites; the two runs were executed sequentially, never in parallel.
    - `python3 -m pytest tests/ -q` reports 323 passed, 0 failed.
    - No command run by this task wrote to `scripts/baseline/size_baseline.json`; `git -C firestarter diff --name-only -- scripts/baseline/` is empty.
  </acceptance_criteria>
  <done>Every pre-change figure DEAD-01, DEAD-02 and DEAD-03 depend on has been measured on the unchanged tree and is held for task 2, including the two figures that cannot be recovered later: the sole-caller attribution and the full 11-symbol size table.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the AUTHORITATIVE before-figures record, including the derivations the ROADMAP got wrong</name>
  <files>.planning/v1.33/155-before-figures.md</files>
  <read_first>
    - `.planning/v1.33/baseline-pre-sweep.md` lines 1-40 -- the frontmatter and opening-paragraph shape to copy exactly (title / phase / plan / measured / status / supersedes / requirements keys, then the "Git anchors" table)
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md` -- corrections C-2, C-3, C-4 and C-5, to be restated as this record's own derivations from task 1's measurements
    - `.planning/REQUIREMENTS.md` section 2 -- the DEAD-01/02/03 text whose figures this record corrects
    - `.planning/ROADMAP.md` Phase 155 entry -- the `**Measured: -1364 B**` header and success criteria 1-6
  </read_first>
  <action>
Create `.planning/v1.33/155-before-figures.md` in the meta repo. Copy
`.planning/v1.33/baseline-pre-sweep.md`'s conventions exactly: YAML frontmatter with
`title`, `phase`, `plan: "01"`, `measured`, `status`, `supersedes` and `requirements` keys;
then an opening paragraph stating that every number was measured on a clean tree, that each
number carries the verbatim command that produced it, and that nothing is quoted from
`155-RESEARCH.md`. Set `status` to name this file as authoritative for every before/after
comparison in Phase 155. Use ` -- ` (double hyphen) rather than an em dash throughout, per
house style in this directory.

Write these sections, in order.

1. Git anchors -- `FW_PRE_SHA`, the firmware branch name, the empty-porcelain assertion, the
   meta HEAD sha, and an explicit note that the `firestarter_app` gitlink drift is
   pre-existing and operator-gated and is not touched by this phase.

2. Flash and RAM, all three targets -- a table of measured flash/RAM plus capacities, every
   row labelled **WARM**, with the `pio run -e <env>` command quoted. Add one line stating
   that LAND-01 / Phase 158 owns the cold re-record and that these figures are deliberately
   not cold.

3. The heap set -- the per-symbol address/size/section table for the seven present symbols,
   the two absent ones named explicitly, the contiguous span and its byte total, and the
   quoted `avr-nm` command.

4. The 64-bit runtime set -- all ELEVEN symbols with address and size, the contiguous span,
   and BOTH totals stated side by side: the 438 B across the eight symbols DEAD-03 names,
   and the 528 B across the full contiguous blob. State the consequence in the record's own
   words: an absence check over only the eight named symbols could pass while 90 B of 64-bit
   runtime is still linked, because `__umulsidi3` (2 B), `__umulsidi3_helper` (84 B) and
   `__ashrdi3` (4 B) are omitted from the requirement's list. Note that plan 02's gate
   therefore asserts all eleven.

5. Sole-caller attribution -- the verbatim output of task 1 step 4, with a sentence naming
   the method (disassembly attribution over every call/jmp reference) and why source reading
   was rejected (reading source missed three of the eleven symbols; the disassembly did not).

6. Function and object sizes -- `rurp_read_voltage_mv` 434 B, `mem_util_blank_check` 510 B,
   `handle` per target, jsmn `tokens` 512 B, with the `avr-nm --print-size` command quoted.

7. DEAD-02's RAM headroom, derived not quoted. State the corrected arithmetic per target:
   on `uno`, `handle` 603 B plus the jsmn token array 512 B together consume 1115 B of the
   2048 B SRAM, leaving 473 B; on `leonardo`, `handle` 1115 B plus tokens 512 B consume
   1627 B of 2560 B, leaving 544 B; `uno328pb` measures 467 B free. Then record the
   correction explicitly: REQUIREMENTS DEAD-02 and ROADMAP criterion 2 both say "roughly 470 B
   of free RAM once handle (1115 B) and the jsmn token array (512 B) are accounted for", and
   1115 B is `handle` plus tokens together on `uno` -- so that sentence adds the 512 B twice.
   The ~470 B conclusion survives; its derivation does not. Use the phrase **"shared
   heap-and-stack headroom"** and state why: `ram_used` counts .data and .bss only, the stack
   grows down into that 473 B during every operation, so the true margin at the allocation
   site is LESS than 473 B. Do not present 473 B as available to the allocator.

8. The pre-change defect, quoted from source with line anchors as they stand today: the
   `malloc` at `memory.cpp:502`, the unchecked write through the result at `memory.cpp:502`
   with no NULL test between them, and `sizeof(blank_check_progress_data_t)` = 4 bytes for a
   struct holding one `uint32_t`. Record it as a latent defect that exists at this sha, so
   plan 06 can record its closure against a measured pre-state.

9. The "same statement" correction. Record that ROADMAP criterion 6 and REQUIREMENTS DEAD-06
   both say the surviving `is_operation_in_progress` assertion is set by *the same statement*
   as the allocation, quote `memory.cpp:500` and `memory.cpp:502` to show they are two
   distinct statements, and give the corrected, stronger and true formulation: they are
   unconditionally adjacent statements in the same then-branch of the same `if`, with no
   intervening control flow, early return or condition -- so a false `is_operation_in_progress`
   strictly implies the branch never executed, which strictly implies the allocation never
   executed. Note that three comment blocks in the two native suites carry the wrong wording
   and that plan 05 corrects all three.

10. The -650 / -714 split, marked UNVERIFIED. Show that the measured components (heap blob
    586 B, 64-bit blob 528 B, the two function bodies) decompose to roughly -644 / -732 under
    the natural attribution and therefore do not reproduce -650 / -714. State that the TOTAL
    is the reliable figure and that plan 06 quotes the total.

11. Baseline staleness -- the per-target +478 / +476 / +540 arithmetic from task 1 step 6, the
    quoted `meta` block admission, and the one-line consequence about the roughly -888 B the
    policy run will report after the change.

12. Test and gate baselines -- the two native legs with cases/suites/time, the pytest count,
    and the `check_build_warnings.py` verdict, each with its command.

Finally commit to the meta repo only. Stage exactly `.planning/v1.33/155-before-figures.md`
and nothing else -- do not use `git add -A` or `git add .`, and never stage the
`firestarter_app` gitlink. Commit subject: `docs(155-01): before-figures record -- symbol
tables, sole-caller attribution and the corrected RAM derivation`.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/155-before-figures.md && for n in 26026 1575 26074 1581 28170 2016 434 510 603 1115 512 586 528 438 473 544 467 478 172 323; do grep -q "$n" .planning/v1.33/155-before-figures.md || { echo "MISSING FIGURE $n"; exit 1; }; done && grep -qi 'shared heap-and-stack headroom' .planning/v1.33/155-before-figures.md && grep -qi 'WARM' .planning/v1.33/155-before-figures.md && grep -q 'mem_util_blank_check' .planning/v1.33/155-before-figures.md && grep -q '__umulsidi3_helper' .planning/v1.33/155-before-figures.md && test -z "$(git status --porcelain .planning/v1.33/155-before-figures.md)" && echo RECORD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/155-before-figures.md` exists, is committed, and `git status --porcelain` reports it clean.
    - The file carries YAML frontmatter with `title`, `phase`, `plan`, `measured`, `status`, `supersedes` and `requirements` keys, matching `.planning/v1.33/baseline-pre-sweep.md`'s shape.
    - The file contains the literal string `shared heap-and-stack headroom`.
    - The file contains all three per-target free-RAM figures 473, 467 and 544, and states that 1115 B is `handle` plus tokens together on `uno`, not `handle` alone.
    - The file contains both 64-bit totals, 438 and 528, and names `__umulsidi3`, `__umulsidi3_helper` and `__ashrdi3` as the three symbols the requirement's list omits.
    - The file names `realloc` and `calloc` as ABSENT from the pre-change image.
    - The file contains the verbatim sole-caller attribution output showing exactly one caller for `malloc` and one for `free`.
    - Every flash/RAM figure in the file is labelled `WARM`.
    - The file states that the -650 / -714 split is UNVERIFIED and that the total is the reliable figure.
    - The file states the corrected "unconditionally adjacent statements in the same then-branch" formulation and records the ROADMAP/REQUIREMENTS "same statement" wording as corrected.
    - `git -C /workspaces log -1 --name-only` for this commit lists exactly one path, `.planning/v1.33/155-before-figures.md`.
    - `git -C /workspaces diff --cached --name-only` is empty after the commit, and the `firestarter_app` gitlink is still unstaged and still shows as modified.
  </acceptance_criteria>
  <done>An authoritative, committed before-figures record exists in the meta repo carrying every measurement Phases 156-158 will destroy, plus the four derivations the ROADMAP and REQUIREMENTS got wrong, each stated as a correction with its measured basis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host CLI -> firmware wire | `r1` / `r2` calibration values arrive over serial and are persisted to Arduino EEPROM; unchanged by this plan, measured by it |
| linked ELF -> claims made about it | the record asserts properties of a binary; a mis-measured figure silently weakens every later criterion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-01 | Repudiation | `.planning/v1.33/155-before-figures.md` | high | mitigate | Every figure carries the verbatim command that produced it and is re-measured rather than copied from `155-RESEARCH.md`; the automated verify re-runs the two symbol counts independently of the record's prose. |
| T-155-02 | Tampering | `scripts/baseline/size_baseline.json` | medium | mitigate | No command in this plan may write the baseline; the acceptance criteria assert `git diff --name-only -- scripts/baseline/` is empty. |
| T-155-03 | Information disclosure | measurement scratch files | low | accept | Symbol tables and disassembly of an open-source firmware image carry no secret; scratch files are written under the session scratch directory and never committed. |
| T-155-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs ZERO packages -- `155-RESEARCH.md`'s Package Legitimacy Audit records "not applicable, this phase installs zero external packages". Any install attempt is out of scope and must stop the task. |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty before and after the plan; no file under `firestarter/src`, `firestarter/include` or `firestarter/test` differs from `FW_PRE_SHA`.
- `.planning/v1.33/155-before-figures.md` committed as exactly one meta-repo commit touching exactly one path.
- The two independent symbol counts (7 heap, 11 64-bit) reproduce on all three ELFs from the automated verify, not only from the record's prose.
- `scripts/baseline/size_baseline.json` byte-unchanged.
</verification>

<success_criteria>
- Every item on `155-VALIDATION.md`'s Wave 0 bullet "Pre-change before-figures, captured and committed before any edit" is present in the record: the symbol table, the sole-caller attribution, the three flash/RAM pairs, `rurp_read_voltage_mv` = 434 B, `mem_util_blank_check` = 510 B, `handle` = 603/603/1115 B.
- OQ-2's two figures (438 B named subset, 528 B contiguous blob) and OQ-4's two per-target RAM derivations with the phrase "shared heap-and-stack headroom" are on the record.
- The firmware tree is provably unedited by this plan.
</success_criteria>

<output>
Create `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-01-SUMMARY.md` when done.
</output>

