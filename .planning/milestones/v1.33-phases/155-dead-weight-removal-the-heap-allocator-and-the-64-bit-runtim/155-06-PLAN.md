---
phase: 155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim
plan: 06
type: execute
wave: 5
depends_on: ["155-01", "155-02", "155-03", "155-04", "155-05"]
files_modified:
  - firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/README.md
  - firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt
  - firestarter/tests/test_check_no_heap_or_64bit_symbols.py
  - .planning/v1.33/155-after-figures.md
autonomous: true
requirements: [DEAD-01, DEAD-02, DEAD-03, DEAD-04, DEAD-05, DEAD-06]
must_haves:
  truths:
    - "The phase's saving is stated as before-minus-after on the same tree, on all three AVR targets, and is never quoted as a delta against the stale baseline file."
    - "The symbol gate is proven RED once more from a post-change throwaway worktree in which one allocation call is reinstated, so its non-hollowness does not rest solely on a pre-change listing."
    - "A REAL post-change avr-nm listing is committed as the gate's clean control, replacing the synthetic one the paired pytest derived at test time."
    - "All eight phase-gate legs are green and each is recorded with its measured figure."
    - "The DEAD-05 phrasing gate runs over the real, complete corpus and is green in both halves."
    - "The phase record corrects every figure the ROADMAP and REQUIREMENTS got wrong, and records the 2 B delta against the ROADMAP's measured header together with its cause."
    - "The size-baseline policy run is recorded as ONE-SIDED and the baseline file is byte-unchanged."
  artifacts:
    - firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/README.md
    - firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt
    - .planning/v1.33/155-after-figures.md
  key_links:
    - "The before-figures record is the only valid denominator for this phase's saving; the baseline file is +478 B stale and its delta is a different quantity."
    - "The post-change throwaway-worktree RED plus the committed pre-change planted listing together close the hollow-gate question from both directions."
    - "Every source line this phase shifted is Phase 159's input; this phase must not remap a single citation."
  prohibitions:
    - "Must NOT re-anchor, rebuild or write scripts/baseline/size_baseline.json in any way -- LAND-01 / Phase 158 owns the cold re-record, and re-anchoring is known to strand fixture legs."
    - "Must NOT author an exemption for the size reduction -- the policy comparison is one-sided, so a shrink passes with no exemption, and that must be RECORDED as one-sided rather than read as 'nothing moved'."
    - "Must NOT fix the pre-existing native case-count mismatch on the canonical policy invocation -- that is Phase 158's LAND-03."
    - "Must NOT run the Phase 154 citation remap tool or repair any file:LINE citation (Phase 159 / D-01)."
    - "Must NOT edit any firmware source file under src/ or include/, and must NOT edit either native suite -- plans 04 and 05 own those and their commits are final."
    - "Must NOT widen build_src_filter, add a native case, or add a PlatformIO env."
    - "Must NOT touch include/memory_utils.h (Phase 156)."
    - "Must NOT leave a throwaway git worktree or branch behind, and must NOT delete or force-update the preserved wip reference."
    - "Must NOT change anything in firestarter_app, and must NOT git add the firestarter_app gitlink in the meta repo."
    - "Must NOT author or imply a bench/hardware criterion (D-02), and must NOT use any of the six coverage phrasings enumerated in 155-VALIDATION.md item 5 about the voltage function."
    - "Must NOT quote the roughly -888 B baseline delta as this phase's saving."
---

<objective>
Land the phase: measure the after position against the before-figures record, close the
hollow-gate question from the post-change direction, run all eight phase-gate legs, run the
DEAD-05 phrasing gate over the real corpus, and write the phase record that corrects every
figure the ROADMAP and REQUIREMENTS got wrong.

Purpose: the two removals are already committed. What remains is the part that makes them
defensible -- an attributable delta measured against the same tree rather than against a
baseline file that is already +478 B stale, a gate whose non-hollowness is proven from both
directions, and a record that states its corrections publicly instead of quietly shipping
figures that do not reconcile.

Output: the gate's real post-change clean control plus its paired leg, and
`.planning/v1.33/155-after-figures.md` -- the phase record.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-PATTERNS.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-VALIDATION.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-01-SUMMARY.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-02-SUMMARY.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-03-SUMMARY.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-04-SUMMARY.md
@.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-05-SUMMARY.md
@.planning/v1.33/155-before-figures.md
@.planning/v1.33/155-dead05-phrasing-corpus.md
</context>

## Multi-Source Coverage Audit

The phase-wide audit is recorded once, in `155-01-PLAN.md`. This plan carries the landing
half of all six DEAD requirements plus CONTEXT OQ-1 through OQ-5, RESEARCH corrections C-1
through C-5, and milestone decisions D-02, D-03 and D-04. No item assigned to this plan is
MISSING.

## The coverage ceiling, carried in the mandated terms

`src/boards/rurp_common.cpp` is compiled by NO native environment, so `rurp_read_voltage_mv`
is **proven by a committed host-side numerical oracle over a stated input grid, bound to the
shipped C by a source-contract scan; no native and no bench coverage exists.** This phase
created neither. The named residual risk, stated and NOT presented as covered: avr-gcc
miscompiling the 32-bit multiply/divide, unmitigated by any artefact of this phase and
mitigated only by that being AVR's most-exercised path and by the change reducing codegen
complexity.

## Artifacts this phase produces

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/155-before-figures.md` | 01 | meta |
| `firestarter/scripts/check_no_heap_or_64bit_symbols.py` | 02 | fw |
| `firestarter/tests/test_check_no_heap_or_64bit_symbols.py` | 02 | fw |
| `firestarter/tests/fixtures/planted_no_heap_or_64bit_symbols_prechange_uno/README.md` | 02 | fw |
| `firestarter/tests/fixtures/planted_no_heap_or_64bit_symbols_prechange_uno/avr-nm-uno.txt` | 02 | fw |
| Gate script symbols: `HEAP_SYMBOLS`, `DI64_SYMBOLS`, `REQUIRED_ANCHORS`, `NM_ARGS`, `FIRESTARTER_AVR_NM`, `_resolve_avr_nm`, `_listing_for_target`, `_scan_listing`, `_print_pass`, `_print_fail`, `_parse_argv`, `main` | 02 | fw |
| `FLOOR` 6 -> 7 and `FIXTURE_FLOOR` 15 -> 16 in `firestarter/tests/test_checker_convention.py` | 02 | fw |
| `.planning/v1.33/tools/check_dead05_phrasing.py` | 03 | meta |
| Phrasing-tool symbols: `CORPUS_GLOBS`, `NAMED_EXCLUSIONS`, `TRIGGER_TOKENS`, `PARAGRAPH_FLOOR`, `REQUIRED_POSITIVE_TARGETS`, `_forbidden_needles`, `_normalise`, `_paragraphs`, `_scan_file`, `main` | 03 | meta |
| `.planning/v1.33/tools/test_check_dead05_phrasing.py` | 03 | meta |
| `.planning/v1.33/tools/fixtures/planted_dead05_phrasing_violation.md` | 03 | meta |
| `.planning/v1.33/155-dead05-phrasing-corpus.md` | 03 | meta |
| `firestarter/tests/test_voltage_reformulation_oracle.py` | 04 | fw |
| Oracle symbols: `_RURP_COMMON_REL`, `_SCAN_RURP_COMMON`, `_SHIELD_HEADER_REL`, `_strip_comments`, `_line_of`, `_extract_function_body`, `_v64`, `_v32`, `_SCALE_NUMERATOR` (1100), `_GUARD_SUM_MAX` (3900000), `_GUARD_K_MAX` (4194303) | 04 | fw |
| New locals in `rurp_read_voltage_mv`: `sum`, `k`, `bg`; guard expressions `sum > 3900000UL` and `k > 4194303UL` | 04 | fw |
| File-scope static `blank_check_saved_address` in `firestarter/src/proms/memory.cpp` | 05 | fw |
| `firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/README.md` + `avr-nm-uno.txt` | 06 | fw |
| New leg `test_real_postchange_listing_exits_zero` in `test_check_no_heap_or_64bit_symbols.py` | 06 | fw |
| `.planning/v1.33/155-after-figures.md` (the phase record) | 06 | meta |

<!-- planner-discipline-allow: malloc -->
<!-- planner-discipline-allow: progress_data -->
<!-- planner-discipline-allow: DEAD-0 -->
<!-- planner-discipline-allow: Phase 155 -->
<!-- planner-discipline-allow: OQ- -->

<tasks>

<task type="auto">
  <name>Task 1: Capture the after position, commit the real clean control, and prove the gate RED from the post-change side</name>
  <files>firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/README.md, firestarter/tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt, firestarter/tests/test_check_no_heap_or_64bit_symbols.py</files>
  <read_first>
    - `.planning/v1.33/155-before-figures.md` -- every figure this task computes a delta against
    - `firestarter/tests/test_check_no_heap_or_64bit_symbols.py` -- the existing legs, and in particular the synthetic-clean-control leg whose docstring promises this task's real control
    - `firestarter/tests/fixtures/clean_release_assets_all_three/` -- the `clean_` fixture naming and README idiom to copy (a clean control is named `clean_`, not `planted_`, so it does not inflate the planted count)
    - `firestarter/tests/fixtures/planted_no_heap_or_64bit_symbols_prechange_uno/README.md` -- the sibling README whose shape this one mirrors
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-VALIDATION.md` -- the Wave 0 bullet requiring a throwaway-worktree planted negative
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md` -- the flash and RAM ledgers, including the +10 B unattributed residual
    - `firestarter/CLAUDE.md` and `/workspaces/CLAUDE.md`
  </read_first>
  <action>
Step 1 -- assert the starting state. `git -C firestarter status --porcelain` must be empty and
plans 04 and 05 must both be in the history. Record the post-change firmware sha as
`FW_POST_SHA`.

Step 2 -- warm rebuild and after-figures capture. Run `pio run -e uno`, `-e uno328pb` and
`-e leonardo` one at a time and record flash/RAM. Expected 24660/1567, 24708/1573,
26804/2008. Label WARM, and state that LAND-01 / Phase 158 owns the cold re-record. Then run
`avr-nm --print-size --size-sort -C` on each ELF and record: the heap-set match count
(expected 0 on each), the 64-bit-set match count (expected 0 on each), the voltage function's
new size (expected 0xe6 = 230 B), the blank-check function's new size (expected 0x1c4 =
452 B), the handle object's new size (expected 601 / 601 / 1113 B), the new static's size
(expected 4 B), and the token array (expected unchanged at 512 B). Compute every delta
against `155-before-figures.md`, never against the baseline file.

Step 3 -- reconcile both ledgers, and state the residual rather than absorbing it.
  RAM: the five allocator globals leaving, the removed handle member, and the new 4 B static
  must sum to exactly -8 B; show the per-item table and the net.
  FLASH: the heap blob, the 64-bit blob and the two shrunk function bodies sum to a larger
  reduction than the measured whole-image delta. Compute the residual and record it as
  unattributed growth elsewhere, most plausibly call-site register allocation and inlining.
  Do NOT fold it into a component to make the arithmetic look clean. If the residual is not
  the +10 B research measured, record the measured value.

Step 4 -- commit the REAL clean control. Write the exact stdout of
`avr-nm --print-size --size-sort -C .pio/build/uno/firestarter_uno.elf` to
`tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt`, unedited.
Assert on the captured file before proceeding: 0 heap-set matches, 0 sixty-four-bit-set
matches, and both non-vacuity anchors present. Write the sibling `README.md` in the same shape
as the planted fixture's: what it is, the exact command, `FW_POST_SHA`, the three asserted
properties, why it is committed as text rather than as an ELF, and that it is the real
post-change control replacing the synthetic one the paired pytest derived at test time.
Then add ONE leg to `tests/test_check_no_heap_or_64bit_symbols.py`, named exactly
`test_real_postchange_listing_exits_zero`: run the gate against this committed listing and
assert exit 0, `PASS:` in stdout, and the target name in stdout. Update the synthetic leg's
docstring so it now points at this real control instead of promising it. Change nothing else
in that module.

Step 5 -- the post-change planted negative, from a throwaway worktree. This is the direction
the pre-change listing cannot cover: it proves the gate still fires against a tree that is
otherwise the shipped tree. Create a throwaway worktree under the session scratch directory
from `FW_POST_SHA` with `git -C firestarter worktree add`. In it, reinstate exactly ONE
allocation call inside the blank check -- the minimum edit that links the allocator again --
build `-e uno` in that worktree, and run the gate against that worktree's build root with
`--build-root`. It MUST exit 1 and its FAIL report MUST name the allocator entry points.
Record the verbatim stdout. Then remove the worktree with `git -C firestarter worktree remove`
and prune, and assert `git -C firestarter worktree list` shows only the main tree and that
`git -C firestarter status --porcelain` is still empty. Do not create a branch for this, do not
commit inside the worktree, and do not touch the preserved wip reference.

Step 6 -- commit the three firmware paths, staging exactly those and nothing else. Subject:
`test(155-06): real post-change clean control for the symbol gate and its exit-zero leg`.
Only after the commit lands, run `python3 -m pytest tests/test_check_no_heap_or_64bit_symbols.py tests/test_checker_convention.py -q`
and require 0 failed.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -f tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt && grep -q 'test_real_postchange_listing_exits_zero' tests/test_check_no_heap_or_64bit_symbols.py && python3 scripts/check_no_heap_or_64bit_symbols.py --nm-output uno=tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt && python3 scripts/check_no_heap_or_64bit_symbols.py && test -z "$(git status --porcelain)" && test "$(git worktree list | wc -l)" -eq 1 && python3 -m pytest tests/test_check_no_heap_or_64bit_symbols.py tests/test_checker_convention.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pio run` reports flash/RAM of exactly 24660/1567 (uno), 24708/1573 (uno328pb), 26804/2008 (leonardo); every figure is labelled WARM.
    - The per-target flash delta against `155-before-figures.md` is exactly -1366 B and the RAM delta exactly -8 B on all three targets.
    - `avr-nm --print-size` reports the voltage function at `000000e6` and the blank-check function at `000001c4` on the uno ELF, and the handle object at 601 B.
    - The RAM ledger table sums to exactly -8 and is shown item by item.
    - The flash residual is computed and recorded as unattributed, with its measured value, and is not folded into any component.
    - `python3 scripts/check_no_heap_or_64bit_symbols.py` exits 0 and its PASS line names all three env keys with both counts 0 and two anchors found.
    - `python3 scripts/check_no_heap_or_64bit_symbols.py --nm-output uno=tests/fixtures/clean_no_heap_or_64bit_symbols_postchange_uno/avr-nm-uno.txt` exits 0.
    - The committed clean-control listing contains 0 heap-set matches, 0 sixty-four-bit-set matches, and both anchors.
    - The throwaway-worktree run exits **1** with a FAIL report naming the allocator entry points, and its verbatim stdout is quoted in the SUMMARY.
    - `git -C firestarter worktree list` shows exactly one entry after cleanup, and `git -C firestarter status --porcelain` is empty.
    - `git -C firestarter rev-parse --verify wip/v1.33-size-reduction-survey-preserved` still resolves to the same sha it had before this task.
    - `python3 -m pytest tests/test_check_no_heap_or_64bit_symbols.py tests/test_checker_convention.py -q` reports 0 failed, with one more test collected from the gate module than before.
    - `git -C firestarter diff HEAD~1 HEAD --name-only` lists exactly the three paths in this task's `files`; `scripts/`, `src/`, `include/`, `test/` and `platformio.ini` are byte-unchanged by this commit.
  </acceptance_criteria>
  <done>The after position is measured against the before-figures record on all three targets, both ledgers reconcile with the residual stated rather than absorbed, the gate now has a real post-change clean control with its own exit-zero leg, and the gate is proven RED from the post-change direction with the worktree cleaned up.</done>
</task>

<task type="auto">
  <name>Task 2: Run all eight phase-gate legs and the one-sided size-baseline policy comparison</name>
  <files>
    (no file is edited by this task -- its deliverable is the eight recorded verdicts)
  </files>
  <read_first>
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-VALIDATION.md` -- the eight phase-gate legs, verbatim, and the sampling-rate rules
    - `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-RESEARCH.md` -- pitfalls 1, 2, 3, 5, 6 and 7, each of which names a way this task can misreport
    - `firestarter/scripts/check_size_baseline.py` -- the docstring's two modes, its `Usage:` block including the canonical policy invocation, the one-sided flash comparison, and the two exact-equality capacity comparisons
    - `firestarter/scripts/check_build_warnings.py` -- its `Usage:` block
    - `.planning/v1.33/155-before-figures.md` -- the pre-change test baselines and the baseline-staleness arithmetic
  </read_first>
  <action>
Run the eight legs in order and record each with its measured figure. Never run two `pio`
invocations concurrently: the native suite's failures correlate with run duration rather than
with tree content, and no conclusion may be drawn from a single red run -- if a leg is red,
re-run it once on an otherwise idle machine and record both runs before reporting anything.

  1. `pio test -e native` -- require 172 cases, 172 succeeded, 17 suites. This is the leg that
     verifies rather than assumes that removing an assertion did not remove a case; the count
     is an exact input to the size script's native comparison.
  2. `pio test -e native_nodevtools` -- same requirement, run after leg 1, not alongside it.
  3. `python3 -m pytest tests/ -q` -- require 0 failed and record the exact count. Run this
     only on a clean tree: one module asserts whole-repo git porcelain, so an uncommitted file
     produces exactly one failure naming porcelain, and that failure is the uncommitted file,
     not this phase.
  4. The heap half of the symbol gate -- 0 on all three ELFs.
  5. The 64-bit half of the symbol gate over all ELEVEN symbols -- 0 on all three ELFs.
  6. `pio run` for all three targets -- record flash/RAM and compute the delta against the
     before-figures record. State the delta as before-minus-after on the same tree.
  7. `python3 scripts/check_size_baseline.py` -- run the comparison WITHOUT any flag that
     writes, and record three things. First, the verdict. Second, that the flash comparison is
     ONE-SIDED: it fires only when the delta exceeds an allowance, so a reduction passes with
     no exemption authored, and this is the first size movement in this project's history that
     does not need one -- record the pass AS one-sided so no future reader mistakes green for
     "nothing moved". Third, that the only exact-equality comparisons in that script are the
     two capacity figures, and both are unchanged (32768 flash on all three targets, and
     2048 / 2048 / 2560 RAM), so a reduction of this size trips no exact-size assertion
     anywhere. Also run the canonical policy invocation named in the script's own `Usage:`
     block and record its result -- if it exits non-zero on a frozen native case count from an
     earlier phase, record that as pre-existing and as Phase 158's item, and note that it
     exits before it ever reports flash. Do NOT pass `--rebuild` and do NOT edit the baseline.
  8. `python3 scripts/check_build_warnings.py --log uno=<uno build log>` -- require no new
     warning attributable to the two edited firmware source files or the header.

Then record the two caveats that must travel with these figures. The baseline file is
+478 / +476 / +540 B stale, so the delta the size script reports against it is a different
quantity from this phase's saving -- both must appear, labelled, and the baseline-relative
figure must never be called the saving. And the size script runs in NO CI workflow at all, so
every figure in leg 7 is a local-run obligation; state that plainly rather than implying
automation.

Make no file edit and no commit in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && pio test -e native 2>&1 | tail -3 | grep -qE '172 test cases: 172 succeeded' && pio test -e native_nodevtools 2>&1 | tail -3 | grep -qE '172 test cases: 172 succeeded' && python3 -m pytest tests/ -q && python3 scripts/check_no_heap_or_64bit_symbols.py && git diff --quiet -- scripts/baseline/size_baseline.json && echo GATE_LEGS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Legs 1 and 2 each report `172 test cases: 172 succeeded` across 17 suites, run sequentially.
    - Leg 3 reports 0 failed, and its exact count is recorded and reconciled against the pre-change 323 plus the cases plans 02, 04 and 06 added.
    - Legs 4 and 5 both report 0 on all three ELFs, with leg 5 covering all eleven symbols.
    - Leg 6 records flash/RAM for all three targets and states the delta as -1366 B flash and -8 B RAM computed against `155-before-figures.md`.
    - Leg 7's verdict is recorded together with the explicit statement that the flash comparison is one-sided and that no exemption was authored; and with the statement that both capacity figures are exact-equality comparisons and both are unchanged.
    - Leg 7's canonical policy invocation result is recorded, and any non-zero exit is attributed to the pre-existing frozen native case count and named as Phase 158's item.
    - `git -C firestarter diff --quiet -- scripts/baseline/size_baseline.json` exits 0 -- the baseline file is byte-unchanged.
    - Leg 8 reports no new warning attributable to the two edited firmware source files or the header.
    - The SUMMARY records the +478 / +476 / +540 B baseline staleness and states that the baseline-relative delta is not this phase's saving.
    - The SUMMARY states that the size script runs in no CI workflow and that leg 7 is therefore a local-run obligation.
    - `git -C firestarter status --porcelain` is empty and this task added no commit.
  </acceptance_criteria>
  <done>All eight phase-gate legs are green with their measured figures recorded, the policy comparison is recorded as one-sided with no exemption authored, the baseline file is byte-unchanged, and both caveats -- the staleness and the absence of CI automation -- travel with the figures.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase record, then run the DEAD-05 phrasing gate over the real corpus</name>
  <files>.planning/v1.33/155-after-figures.md</files>
  <read_first>
    - `.planning/v1.33/155-before-figures.md` IN FULL -- every figure this record computes a delta against, and the corrections it already carries
    - `.planning/v1.33/155-dead05-phrasing-corpus.md` -- the corpus, the three named exclusions, the paragraph floor and the four required-positive targets, one of which is the file this task writes
    - `.planning/v1.33/sweep-outcome-record.md` -- Phase 154's outcome-record shape, which this record copies
    - `.planning/ROADMAP.md` Phase 155's entry -- the measured header and the six success criteria, each of which this record must answer with a measured figure
    - `.planning/REQUIREMENTS.md` section 2 -- DEAD-01 through DEAD-06
    - `.planning/v1.33/CITATIONS-STALE.md` -- the Phase 154 staleness marker this record must hand its own line shifts forward to
    - all five prior `155-0N-SUMMARY.md` files -- the measured evidence to consolidate
  </read_first>
  <action>
Write `.planning/v1.33/155-after-figures.md` in the meta repo, in the shape of
`.planning/v1.33/sweep-outcome-record.md` with the frontmatter convention used in that
directory (`title`, `phase`, `plan: "06"`, `measured`, `status`, `requirements`). Use ` -- `
rather than an em dash. Sections, in order:

  1. Git anchors -- the pre-change and post-change firmware shas, the commit count between
     them, the branch name, and an explicit note that nothing is pushed and that pushing is
     the operator's call.
  2. The measured result -- the three flash/RAM pairs before and after, the per-target delta,
     all labelled WARM, with the statement that LAND-01 / Phase 158 owns the cold re-record.
  3. **OQ-1, stated not buried.** The shipped guard constant is 4194303, which is 0x3FFFFF
     and therefore compiles to a shift test, is the tighter bound, and is 2 B cheaper than the
     decimal alternative the preserved reference used. Consequently the measured saving is
     -1366 B, which is 2 B MORE than the ROADMAP's measured header states. Record the 2 B
     delta, record its cause as the guard constant, and record that the cheaper-looking
     constant was NOT shipped in order to make the header's figure come out. State that the
     ROADMAP's header and its own success criterion could not both be true and that the
     criterion won.
  4. **OQ-2.** Both 64-bit figures: the eight symbols the requirement names total 438 B, and
     the full contiguous eleven-symbol blob totals 528 B. Record that the gate asserts all
     eleven and why -- a gate over eight could have passed with 90 B still linked.
  5. **C-2.** The per-half flash split from the ROADMAP does not decompose from the measured
     symbol sizes. Mark the split UNVERIFIED, show what the natural attribution actually
     gives, and quote the TOTAL as the reliable figure.
  6. The RAM ledger, item by item, summing to exactly -8, and the flash ledger with its
     residual recorded as unattributed.
  7. **DEAD-02, recorded as a latent defect CLOSED.** An unchecked-allocation dereference,
     present since the blank-check feature landed, on a part with under 473 B of shared
     heap-and-stack headroom, closed by removing the allocation entirely rather than by adding
     a null test -- because the allocation had no reason to exist. Carry **OQ-4** here: on uno
     the handle is 603 B and the token array 512 B, together 1115 B of 2048 B, leaving 473 B;
     on leonardo the handle is 1115 B and the tokens 512 B, together 1627 B of 2560 B, leaving
     544 B; uno328pb leaves 467 B. Record the correction that the requirement's own sentence
     adds the 512 B twice, that the ~470 B conclusion survives and its derivation does not,
     and that the accounting excludes the stack so the true margin was smaller still.
  8. **DEAD-06.** Both suites updated with their comments and the third stale comment, in one
     commit because the change is compiler-forced. The native case count stayed 172 across 17
     suites -- verified, not assumed. **C-5:** the requirement's "same statement" claim is
     false and is replaced by the true, stronger formulation about unconditionally adjacent
     statements in the same then-branch with no intervening control flow. The rejected
     alternative -- retaining the dead member so the two assertions compile unchanged -- is
     recorded as considered and rejected with its measured 2 B per-target cost and with the
     reason: the retained member would make both assertions vacuous, this project's own named
     hollow-gate failure mode.
  9. **DEAD-04.** The oracle's readings, each with its measured value: the exact scale factor
     at the shipped calibration, the named single reading agreeing in both forms, the
     bit-identity counts, the exactly-5 mV worst deviation with its argmax, the one-directional
     result, both guard boundaries with the exact input pairs used and the scale factors they
     produce, and both zero sentinels. State that the nominal grid reaches NEITHER guard, which
     is why the four dedicated boundary cases exist.
 10. **OQ-3, the corrected tolerance statement.** The consumers' window is asymmetric: the low
     edge is a -5 % relative bound, which is -600 mV at 12 V, and the high edge is a fixed
     +500 mV absolute. So the 5 mV bound is 1.0 % of the tighter 500 mV window and 0.83 % of
     the 600 mV edge. Then the directional argument: because the reformulation only ever
     under-reads, it can never suppress the high-side error, which fires on an over-read
     condition; its only possible effect is a spurious low-side warning for a true voltage
     within 5 mV of the low edge. Record the symmetric claim in the ROADMAP and REQUIREMENTS as
     corrected.
 11. **DEAD-05, the coverage ceiling**, in the mandated wording quoted verbatim in this plan's
     ceiling section, naming `rurp_read_voltage_mv`, plus the named residual risk stated as
     unmitigated. Also record the phrasing gate's verdict from step 2 below.
 12. **D-03.** The size comparison passed and the pass is recorded AS one-sided, with no
     exemption authored, and the baseline was NOT re-anchored. Record the baseline staleness
     figures and the statement that the baseline-relative delta is a different quantity from
     this phase's saving. Record that the size script runs in no CI workflow.
 13. Phase 159's input. This phase shifted source lines in five files -- name them -- and
     remapped ZERO citations, by D-01. Point at the Phase 154 staleness marker and state that
     the composite pre-154-to-post-158 diff is what Phase 159 maps.
 14. Scope fence honoured. Record that `include/memory_utils.h` was NOT touched because
     PATTERNS verified it holds no reference to the removed member and its reference hunk
     belongs to Phase 156; and that the five other source files the preserved reference
     touches carry no Phase 155 content. Record that the reference was used semantically and
     never applied as a patch, and that four defects in its in-scope hunks were corrected
     rather than carried.
 15. The eight phase-gate verdicts from task 2, each with its figure.

THEN, and only after the record file exists, run the DEAD-05 phrasing gate over the real
corpus: `python3 .planning/v1.33/tools/check_dead05_phrasing.py`. It must exit 0. Record its
verbatim PASS line -- including the per-file in-scope paragraph counts, the total against the
floor, and each required-positive target confirmed -- into section 11 of the record. If it
exits 1, fix the offending prose rather than adding an exclusion; the corpus record states
that no fourth exclusion may be added to turn a red run green.

Commit to the meta repo, staging exactly `.planning/v1.33/155-after-figures.md`. Subject:
`docs(155-06): phase record -- measured deltas, five corrections and the one-sided policy pass`.

Finally, note in the plan SUMMARY that this plan's own SUMMARY file is authored after the
gate ran, so the phrasing gate must be re-run once more at phase verification with every
SUMMARY present -- and that this is a `<verification>` obligation of this plan, not an
optional extra.
  </action>
  <verify>
    <automated>test -f .planning/v1.33/155-after-figures.md && for n in 24660 1567 24708 1573 26804 2008 1366 438 528 473 544 467 601 1113 230 452 7850 35691 4194303 3900000 172; do grep -q "$n" .planning/v1.33/155-after-figures.md || { echo "MISSING FIGURE $n"; exit 1; }; done && grep -qi 'shared heap-and-stack headroom' .planning/v1.33/155-after-figures.md && grep -qi 'no native and no bench coverage exists' .planning/v1.33/155-after-figures.md && grep -qi 'one-sided' .planning/v1.33/155-after-figures.md && python3 .planning/v1.33/tools/check_dead05_phrasing.py && test -z "$(git status --porcelain .planning/v1.33/155-after-figures.md)" && echo PHASE_RECORD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/155-after-figures.md` exists, is committed, and `git status --porcelain` reports it clean.
    - The record contains every figure in the automated verify's list, and states the per-target delta as -1366 B flash and -8 B RAM.
    - Section 3 records the 2 B delta against the ROADMAP's measured header, names the guard constant as its cause, and states that the cheaper-looking constant was not shipped to make the header's figure come out.
    - Section 4 contains both 438 and 528 and explains why the gate asserts eleven symbols.
    - Section 5 marks the per-half flash split UNVERIFIED and quotes the total as the reliable figure.
    - Section 6's RAM ledger sums to exactly -8 item by item, and the flash residual is recorded as unattributed with its measured value.
    - Section 7 contains the literal `shared heap-and-stack headroom`, all three per-target free-RAM figures, and the correction that the requirement's sentence adds the 512 B twice.
    - Section 8 records the native count as 172 across 17 suites verified rather than assumed, states the corrected adjacent-statements formulation, and records the rejected alternative with its measured 2 B cost and the hollow-gate reason.
    - Section 9 records both guard boundary input pairs and the scale factors 4194303 and 4194304, and states that the nominal grid reaches neither guard.
    - Section 10 states both window edges -- a relative low edge and a fixed absolute high edge -- and contains no symmetric-window claim.
    - Section 11 contains the mandated ceiling sentence verbatim, names the residual risk as unmitigated, and quotes the phrasing gate's PASS line including the paragraph counts.
    - Section 12 contains the literal `one-sided`, states that no exemption was authored, and states that the baseline was not re-anchored and that the size script runs in no CI workflow.
    - Section 13 names all five source files whose lines shifted and states that zero citations were remapped.
    - Section 14 states that `include/memory_utils.h` was not touched and why, and that four defects in the reference's in-scope hunks were corrected rather than carried.
    - `python3 .planning/v1.33/tools/check_dead05_phrasing.py` exits 0, with the total in-scope paragraph count at or above the recorded floor, and no exclusion was added to achieve it -- `git diff` on the corpus record shows it unchanged by this task.
    - `git -C /workspaces log -1 --name-only` lists exactly one path for this commit, and the `firestarter_app` gitlink is still unstaged and still modified.
  </acceptance_criteria>
  <done>The phase record exists and answers each of the ROADMAP's six success criteria with a measured figure, records five corrections publicly with their causes, records the policy pass as one-sided with the baseline untouched, hands the line shifts forward to Phase 159, and carries a green two-halved DEAD-05 phrasing verdict with its paragraph counts quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured figures -> the phase record | the record is what a future reader and Phase 158's re-anchor trust; a figure quoted against the wrong denominator misleads permanently |
| gate verdict -> success criterion | the gate's exit code is the evidence for two criteria, so its non-hollowness must be proven from both directions |
| this phase's line shifts -> Phase 159 | the composite diff is Phase 159's only input; a citation repaired here would be remapped twice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-22 | Repudiation | the phase record's saving figure | high | mitigate | The saving is computed as before-minus-after on the same tree from the committed before-figures record; the baseline-relative figure is recorded separately and labelled as a different quantity, with the staleness figures given. |
| T-155-23 | Repudiation | a gate proven RED from only one direction | high | mitigate | Two negatives: the committed pre-change listing and a post-change throwaway worktree in which one allocation call is reinstated. A real post-change clean control proves exit 0 is reachable. |
| T-155-24 | Tampering | `scripts/baseline/size_baseline.json` | high | mitigate | No writing flag is passed anywhere; an acceptance criterion asserts the file is byte-unchanged; re-anchoring is explicitly LAND-01's and is known to strand fixture legs. |
| T-155-25 | Repudiation | over-claiming coverage in the phase record | high | mitigate | The DEAD-05 phrasing gate runs over the real corpus including this record, in both halves, and its PASS line with paragraph counts is quoted into the record itself. |
| T-155-26 | Tampering | a leftover throwaway worktree or branch | medium | mitigate | The worktree is removed and pruned, and acceptance criteria assert a single worktree entry, an empty porcelain, and that the preserved wip reference still resolves to its prior sha. |
| T-155-27 | Repudiation | attributing a pre-existing red to this phase | medium | mitigate | The canonical policy invocation's non-zero exit is recorded as pre-existing, caused by a frozen native case count from an earlier phase, exiting before flash is reported, and named as Phase 158's item. A single red native run is never attributed on N=1. |
| T-155-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed anywhere in this phase. |
</threat_model>

<verification>
- All eight phase-gate legs green with recorded figures: both native legs 172/172 across 17 suites, the host suite 0 failed, both symbol halves 0 on all three ELFs, the three builds' figures, the one-sided policy comparison, and the warnings gate.
- The symbol gate proven RED from both directions and GREEN against a real post-change listing.
- `python3 .planning/v1.33/tools/check_dead05_phrasing.py` exits 0 over the real corpus, with no exclusion added.
- **Re-run obligation:** the phrasing gate must be re-run once more after every `155-0N-SUMMARY.md` exists, including this plan's own, because SUMMARY files are in the corpus and this plan's is authored after the in-task run. The phase is not complete until that re-run is green.
- `scripts/baseline/size_baseline.json` byte-unchanged; no citation remapped; `git worktree list` shows one entry; the preserved wip reference unchanged.
- `git -C firestarter status --porcelain` and the meta repo's porcelain both clean apart from the pre-existing `firestarter_app` gitlink drift.
</verification>

<success_criteria>
- Each of the ROADMAP's six success criteria for this phase is answered in the phase record with a measured figure rather than a restatement.
- All of OQ-1 through OQ-5 are visible in the record: the guard constant with its 2 B delta and cause, both 64-bit figures, the corrected asymmetric window, both per-target RAM derivations with the mandated phrase, and the two-halved phrasing verdict.
- DEAD-01 through DEAD-06 are all discharged with mechanical evidence, and the two that are documentation obligations -- DEAD-02's latent-defect record and DEAD-05's honest ceiling -- are discharged as records with a gate over them.
- The phase leaves `size_baseline.json`, `platformio.ini`, `include/memory_utils.h`, every Phase 156/157/158 source file, and everything in `firestarter_app` byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/155-dead-weight-removal-the-heap-allocator-and-the-64-bit-runtim/155-06-SUMMARY.md` when done.
</output>
