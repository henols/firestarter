---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/157-before-figures.md
autonomous: true
requirements: [DECODE-01, DECODE-02, DECODE-03, DECODE-04, DECODE-05, DECODE-06, DECODE-07]

must_haves:
  truths:
    - "Every pre-change figure this phase's seven requirements are measured against exists in one committed file, captured on a tree proven unchanged, before any source edit."
    - "The eleven-stub ledger sums to exactly 1012 B and the five directly-called siblings are proven ABSENT from the uno symbol table -- the controlled experiment the codebase ran on itself, and DECODE-01's whole proof."
    - "The key-string duplication is recorded as an offset-resolved block comparison, not an exact-string count, because `strings` glues the preceding byte on and reports `Uflags` (C-3)."
    - "`sizeof(firestarter_handle_t)` is re-derived from the real compiler flags captured by `pio run -v` and recorded as ONE number with its method, reconciling the 600-vs-601 discrepancy (OD-7)."
    - "The sixteen research corrections C-1..C-16 plus the three planner-found corrections C-17..C-19 are stated as superseding the ROADMAP and REQUIREMENTS prose, so a later reader cannot re-inherit a stale figure by reading the ROADMAP alone."
    - "All seven operator/orchestrator decisions OD-1..OD-7 are recorded with the alternative each declined and that alternative's cost."
  artifacts:
    - .planning/v1.33/157-before-figures.md
  key_links:
    - "The three flash/RAM pairs are the ONLY before side for DECODE-01's -890 B table figure and DECODE-04's -258 B / -5 B narrowing figure; plans 02 and 03 each measure their own half against them."
    - "The two 118 B key-string vaddr blocks are the only before side for DECODE-02's single-storage claim, and that claim is a LINK-time property source inspection cannot see."
    - "The AVR struct-offset table (3-37, data_buffer at 38) is what makes the uint8_t offset column provably safe, and the native table (4-42, data_buffer at 44) is what proves a hand-written offset would be wrong on one of the two architectures."
    - "The recorded `test_read_timing` case count of 9 and native total of 172 are the denominators of Phase 158 / LAND-01's case-count handoff."
  prohibitions:
    - "Must NOT edit any file under firestarter/ -- this plan measures and records only. A single source edit makes every figure it captures irrecoverable."
    - "Must NOT edit any file under firestarter_app/ (OD-2 keeps this phase firmware-only, with zero host commits)."
    - "Must NOT `git apply` any hunk of `.planning/notes/firmware-size-reduction-measured.patch` -- only `--check` / `--dry-run` are permitted, and hunk #3 is expected to FAIL at every -C level (C-11, ceiling 10)."
    - "Must NOT restate the superseded ROADMAP figures as current: -976/-172, 19, 45, `ten of eleven`, 3440 B, 86-110 B, 25696/25678 (C-2, C-3, C-4, C-5, C-10, C-13)."
    - "Must NOT count ELF strings with an exact-match filter (`awk '$2==\"flags\"'` reports 1 where the truth is 2) nor with a substring `grep -c` (reports 4 on leonardo) -- C-3."
    - "Must NOT pin a `.constprop.NN` suffix; `get_cmd.constprop.31` moved to `.32` between the research's before and after trees (Pitfall 13)."
    - "Must NOT re-anchor `scripts/baseline/size_baseline.json` nor repair the BASE-01 native case-count mismatch (Phase 158 / LAND-01, LAND-03)."
    - "Must NOT run the Phase 154 citation remap tool nor repair any `.planning/` file:LINE citation (Phase 159 / REMAP-01..05, milestone decision D-01)."
    - "Must NOT borrow Phase 155's coverage-gap phrasing: `src/json_parser.c` IS inside the native build (ceiling 1). Writing a Phase-155-style gap sentence here would be a false ceiling."
    - "Must NOT author or imply any bench/hardware criterion (milestone decision D-02)."
    - "Must NOT propose or prepare any step toward the binary command protocol (v1.28 / Backlog 999.35)."
    - "Must NOT stage or re-pin the `firestarter_app` gitlink in the meta repo -- it is pre-existing Phase 154 drift and operator-gated."
    - "Must NOT push any branch."
---

<objective>
Capture every pre-change measurement Phase 157's seven requirements are claimed against, on a
tree proven unchanged, and commit it as the authoritative before-half record.

Purpose: five classes of figure here become irrecoverable the moment `src/json_parser.c` is
edited, and Phase 158's cold baseline re-record invalidates them again. The three flash/RAM
pairs are the only before side for both size claims. The eleven-stub ledger summing to exactly
1012 B against five identical siblings costing zero is DECODE-01's entire proof. The two
key-string vaddr blocks are the only before side for DECODE-02, and that property is a
link-time outcome no source read can establish. And the ROADMAP carries sixteen figures the
research measured wrong -- if they are not corrected in a file that says it supersedes the
ROADMAP, the next reader re-inherits them.

Output: `.planning/v1.33/157-before-figures.md`, committed in the meta repo, every number
carrying the verbatim command that produced it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/156-before-figures.md
@.planning/v1.33/156-after-figures.md
</context>

## Multi-Source Coverage Audit (phase-wide, recorded once here)

Four source types audited. Every item is COVERED by a plan; nothing is MISSING.

**GOAL** -- ROADMAP.md §Phase 157: *"Finish the half-done refactor in `json_parser.c` -- the
key table that matched every wire key twice -- and narrow two handle fields that were four
bytes wide for byte-sized values, closing a fail-closed hole in the process."*

| Goal clause | Plan | Note |
|---|---|---|
| Finish the half-done refactor (the double-match table) | 02 | the `{key, clamp, offset, width}` table + `store_field` |
| the key table that matched every wire key twice | 02, 07 | OD-3 makes single storage a source property; 07 re-measures per target |
| narrow two handle fields | 03 | `protocol` u32->u8, `ctrl_flags` u32->u16 |
| closing a fail-closed hole | 04 | cases S1-S5, S2 load-bearing |
| (implicit) the clamp survives | 02 (mechanism), 05 (proof) | |
| (implicit) the rejected alternative is recorded | 06 | OD-4 re-measures at this position |

**REQ** -- phase_req_ids `DECODE-01 .. DECODE-07`:

| ID | Plans | Note |
|---|---|---|
| DECODE-01 | 01 (before ledger), 02 (the table, -890 B), 07 (after ledger) | C-4: the split is -890/-258, not -976/-172 |
| DECODE-02 | 01, 02 (OD-3), 07 (per-target re-measure) | C-3: ELEVEN of eleven were doubled; C-1: `get_flags`' two sites are in two DIFFERENT functions |
| DECODE-03 | 01 (offsets, both arches), 02 (twelve `_Static_assert`s + planted negative), 05 (six round-trip cases, OD-5) | C-9: the reference guards only `page_size`; all eleven rows need a guard |
| DECODE-04 | 01, 03 (the narrowing, -258 B / -5 B, OD-6, OD-7) | C-5: 18/20 protocol sites and 40/59 `is_flag_set` uses, never 19/45 |
| DECODE-05 | 01, 02 (`store_field` policy), 03 (mask becomes active), 04 (S1-S5 + probes A/B) | F-1/C-7/OD-1: `ctrl_flags` MASKS |
| DECODE-06 | 01, 02 (hoist + `clamp` column), 05 (strobe cap case + `==` tightening) | C-8: a strobe cap test does not exist today |
| DECODE-07 | 01, 06 (OD-4 re-measurement), 07 (the record that discharges it) | record-only; C-10: the absolutes are stale by ~1.4 KB |

**RESEARCH** -- `157-RESEARCH.md` items:

| Item | Plan |
|---|---|
| F-1 (`ctrl_flags` saturation is a safety defect) | 02, 03, 04, 07 |
| F-2 (the phase is not firmware-only unless `key_parsers` is kept) | 02 (OD-2 shape constraint), 07 (host gate run) |
| F-3 (`json_parser.c` IS in the native build) | ceiling 1, every plan |
| F-4 (the suite is blind -- PROVEN) | 04 |
| F-5 (eleven of eleven doubled, one after) | 01, 07 |
| F-6 (the pre-existing BASE-01 RED masks nothing) | 01, 07 |
| C-1 .. C-16 | 01 records them; 07 closes each out |
| C-11 / ceiling 10 (the patch is a hand-port, not an applier) | 01 (re-checked), 02 |
| Prior art: `wip/v1.33-size-reduction-survey-preserved` @ `a6b46f8` | 01 |
| The eleven-stub ledger, the string blocks, the struct offsets | 01, 07 |
| Pitfalls 1-17 | distributed; 5 (dirty sibling) in 02-07, 8 (worktree name) in 02/04/05, 9 (avr-nm PATH) in 01/07, 10 (load flakiness, D-04) in every test-running plan, 11-14 in 01/07 |
| Security V5/V4 (the phase's core surface) | 02, 03, 04 threat models |
| Runtime state inventory: nothing persisted, no migration | 03 |
| OQ-1 -> OD-7 (`sizeof` 600 vs 601) | 01 |
| OQ-2 -> OD-3 (`get_flags` -> `key_flags`) | 02 |
| OQ-3 -> OD-1 (saturate/mask/reject) | 02, 03, 04 |
| OQ-4 -> OD-2 (keep `key_parsers`) | 02, 07 |
| OQ-5 -> OD-4 (re-measure the `switch`) | 06 |
| OQ-6 -> OD-5 (six round-trip cases) | 05 |
| OQ-7 -> OD-6 (run both check scripts) | 03, 07 |
| A1-A10 | A2/A3/A4 stated in 07's record; A6 removed by OD-3 in 02; A7 removed by OD-4 in 06; A8 removed by OD-6; A9 removed by OD-7 in 01 |

**CONTEXT** -- there is **no** `157-CONTEXT.md`; Phases 155 and 156 of this milestone were also
planned without one. The authoritative scope is ROADMAP.md §Phase 157 plus REQUIREMENTS.md §4,
as corrected by C-1..C-16. The seven settled decisions **OD-1 .. OD-7** stand in for a decision
record and every one of them is cited in a plan: OD-1 in 02/03/04/07, OD-2 in 02/07, OD-3 in 02,
OD-4 in 06, OD-5 in 05, OD-6 in 03/07, OD-7 in 01. Milestone decisions D-01..D-05 appear as
scope fences in every plan's prohibitions. **No plan may invent a D-NN label** -- the only D-NN
labels that exist are the milestone framing's five.

**Exclusions, not gaps:** converting the if-chain to a `switch` (DECODE-07 rejects it -- the
measurement is the requirement), re-anchoring `size_baseline.json` (Phase 158 / LAND-01), the
BASE-01 case-count mismatch (Phase 158 / LAND-03), `jsmntok_t` 8->6 B and `NUMBER_JSNM_TOKENS`
(Phase 158 / LAND-05, LAND-07), narrowing `eprom_params_for(uint32_t)` /
`eprom_block_budget_s(uint32_t, ...)` signatures (a lead recorded and not taken -- not required
by DECODE-04 and it widens the blast radius into two headers and two v131 suites), the
`.planning/` citation remap (Phase 159 / REMAP-01..05), and the binary command protocol
(v1.28 / Backlog 999.35, operator-ruled out 2026-08-22).

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by **no** test unless case S2 is written. Record it as
   contingent.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that, and only for the fields
   they exercise (today `read_settling_us`, `read_strobe_us`, `page_size`; after this phase also
   `protocol`, `ctrl_flags` and the six round-trip fields OD-5 takes).
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation is a hand-port, and the
    patch is evidence, not a shortcut.

## Artifacts this phase produces

Every symbol, file and record Phase 157 creates or deletes. Repeated verbatim in every plan so
a source-grounding pass can exclude newly-created symbols and not flag deleted ones as drift.

**CREATED (firmware, `src/json_parser.c` unless stated):**

| Artifact | Plan |
|---|---|
| `field_desc_t` -- the PROGMEM row typedef `{PGM_P key; uint16_t clamp; uint8_t offset; uint8_t width;}`, replacing `key_parser_t` | 02 |
| `FIELD(k, member, cl)` -- saturate-policy row macro (`offsetof` + `sizeof(((firestarter_handle_t*)0)->member)`) | 02 |
| `FIELD_MASK(k, member)` -- mask-policy row macro (OD-1; the `ctrl_flags` row only) | 02 |
| `FIELD_WIDTH_MASK` (`0x0F`) and `FIELD_POLICY_MASK` (`0x80`) | 02 |
| `store_field(firestarter_handle_t*, const field_desc_t*, uint32_t)` -- `static`, expected to inline to 0 B | 02 |
| twelve `_Static_assert` lines: eleven per-member `offsetof(...) < 256 && sizeof(...) <= 4` guards + one `key_parsers` row-count guard | 02 |
| the hoisted `#define READ_TIMING_MAX_US 1000UL` and its comment block, moved from `src/json_parser.c:60` to above the table | 02 |
| `#include <stddef.h>` and `#include <string.h>` (these two lines shift every citation in the file -- expected, Phase 159 remaps) | 02 |
| `key_parsers[]` -- **identifier KEPT** (OD-2), contents replaced by eleven `{key, clamp, offset, width}` rows | 02 |
| `extern "C" { #include "memory.h" }` in `test/native/avr/test_read_timing/test_read_timing_params.cpp` | 04 |
| test cases `test_out_of_range_algorithm_saturates_not_truncates` (S1), `test_out_of_range_algorithm_dispatch_fail_closes` (S2), `test_in_range_algorithm_still_dispatches` (S3), `test_out_of_range_flags_masks_never_sets_every_flag` (S4), `test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size` (S5) | 04 |
| test case `test_read_strobe_us_capped_at_max` | 05 |
| test cases `test_memory_size_round_trips_through_the_field_table`, `test_address_round_trips_through_the_field_table`, `test_pulse_delay_round_trips_through_the_field_table`, `test_chip_id_round_trips_through_the_field_table`, `test_vpp_mv_round_trips_through_the_field_table`, `test_pin_count_round_trips_through_the_field_table` | 05 |
| `.planning/v1.33/157-before-figures.md` | 01 |
| `.planning/v1.33/157-after-figures.md` | 07 |

**DELETED (all in `src/json_parser.c`):**

| Artifact | Plan |
|---|---|
| `key_parser_t` (the typedef, and its `parser_func` member) | 02 |
| ten stubs and their forward declarations: `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`, `get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`, `get_page_size` | 02 |
| the `pgm_read_ptr(&key_parsers[j].parser_func)` indirect-call site inside `json_parse`'s loop | 02 |
| the `get_page_size` comment justifying the non-clamp form (its substance -- validation lives in `eeprom28c_page_mask` -- migrates to the `page-size` table row) | 02 |

**SURVIVES, deliberately, and must not be deleted:** `get_flags` (the DECODE-02 exception -- two
direct call sites in two *different* functions, `json_parse_config` and `json_get_cmd`, per C-1;
its body changes to reference `key_flags` directly per OD-3). Also untouched: the five zero-cost
siblings `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`; `get_cmd`;
`parse_bus_config`; `jsoneq_`; `simple_strtoul`; `extract_num` / `extract_long` / `extract_int`.
**`include/json_parser.h` is NOT edited** -- the eleven `get_*` are file-local forward
declarations, and the header exports only `json_get_cmd`, `json_parse`, `json_parse_config` and
`NUMBER_JSNM_TOKENS` (PATTERNS §3).

<tasks>

<task type="auto">
  <name>Task 1: Assert the tree is unchanged, then measure every irrecoverable pre-change figure</name>
  <files>
    (no tracked file is edited by this task -- it builds, tests and measures, then hands values
    to task 2; build products under firestarter/.pio/build/ are gitignored, and scratch probe
    TUs live under the session scratch directory)
  </files>
  <read_first>
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- sections `Measured Figures` (git anchors, the before position, the decomposition, both per-symbol ledgers, struct offsets, native and gate results), `Corrections to ROADMAP / REQUIREMENTS` (C-1 through C-16, verbatim), `Prior Art`, `DECODE-02` (the oracle and its trap), `Common Pitfalls` (all seventeen), `Code Examples`, `Environment Availability`
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- `Test Infrastructure`, the measured baseline table, `Wave 0 Requirements`, the ten coverage ceilings
    - `.planning/v1.33/156-before-figures.md` -- the record convention this task's output copies: frontmatter keys, the `| Field | Value |` git-anchor table, WARM labelling, and the rule that every number carries the verbatim command that produced it
    - `.planning/v1.33/155-before-figures.md` -- specifically its recorded `sizeof(firestarter_handle_t)` figure, which is the 601 B side of OD-7's discrepancy
    - `firestarter/src/json_parser.c` -- the whole file (391 lines): the eleven forward declarations at `:17-27`, the eleven `const char key_*[] PROGMEM` declarations at `:51-66`, `key_parser_t` at `:68-71`, `key_parsers[]` at `:73-81`, the dispatch loop at `:124-134`, the eleven stub definitions, the five siblings, and `#define READ_TIMING_MAX_US` at `:360`
    - `firestarter/include/firestarter.h` -- the `firestarter_handle_t` definition at `:205-232` and the nine `FLAG_*` defines at `:172-189`
    - `/workspaces/CLAUDE.md` and `/workspaces/firestarter/CLAUDE.md`
  </read_first>
  <action>
Run everything in `/workspaces/firestarter` unless a step says otherwise. This task edits no
tracked file. If any assertion in step 1 fails, STOP and report -- every figure below is only
meaningful against an unchanged tree.

STEP 1 -- anchor the state. Record `FW_PRE_SHA` as the full sha from `git rev-parse HEAD`
(expected to abbreviate to `1151dc4`). Assert `git branch --show-current` is
`gsd/v1.33-source-hygiene-firmware-size-reduction`. Assert `git status --porcelain` is empty and
`git diff --name-only HEAD` is empty. Record the meta repo HEAD sha separately, and note that
`firestarter_app`'s gitlink shows modified in the meta repo: pre-existing Phase 154 drift,
operator-gated, and no task in this phase may stage or re-pin it. Also record `git worktree
list` so a later planted-negative probe worktree is visibly additive.

STEP 2 -- warm AVR builds, all three targets. Run `pio run -e uno`, `pio run -e uno328pb`,
`pio run -e leonardo`, capturing each log under the session scratch directory. Record the
`Flash:` and `RAM:` byte figures per target. Expected: 24234/1567, 24282/1573, 26378/2008.
Record Leonardo Caterina headroom against the 28672 B cliff (expected 2294 B). Label every
figure **WARM** and state that the cold re-record is LAND-01 / Phase 158's obligation, not this
phase's -- `size_baseline.json`'s own documented convention is `rm -rf .pio/build/<env>` then
exactly one `pio run -e <env>`.

STEP 3 -- the eleven-stub ledger, `uno`. `avr-nm` and `avr-objdump` are NOT on PATH; invoke them
as `$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm` and `...avr-objdump`. Run
`avr-nm --print-size --size-sort --radix=d .pio/build/uno/firestarter_uno.elf` and record, per
symbol: `get_read_settling` 110, `get_read_strobe` 110, `get_address` 90, `get_algorithm` 90,
`get_delay` 90, `get_flags` 90, `get_memory_size` 90, `get_chip_id` 86, `get_page_size` 86,
`get_vpp_mv` 86, `get_pin_count` 84 -- and their sum, which must be exactly **1012**. Also
record `key_parsers` 44 (11 x 4 B), `jsoneq_` 108, `simple_strtoul` 68, and the `get_cmd`
constprop clone at 102 **without pinning its numeric suffix** (Pitfall 13). Then record the
load-bearing negative: `get_r1`, `get_r2`, `get_rev`, `get_rw_pin` and `get_vpp_pin` are
**ABSENT** from the symbol table -- structurally identical bodies, called directly with a
literal key, costing zero because `extract_num` expands at the call site. State the C-2
correction: the per-stub range is **84-110 B**, not the ROADMAP's 86-110, because
`get_pin_count` measures 84.

STEP 4 -- the key-string duplication ledger. Use the offset-resolved block form, never an
exact-string count. `.text` begins at file offset 0x94 = 148, so vaddr = fileoff - 148. Run
`strings -a -n 2 -t d .pio/build/uno/firestarter_uno.elf` and record the two blocks: vaddr
**104-221** (118 B, the named `key_*` PROGMEM arrays -- `key_page_size` 104, `key_read_strobe`
114, `key_read_settling` 129, `key_algorithm` 149, `key_vpp_mv` 159, `key_pulse_delay` 166,
`key_pin_count` 178, `key_chip_id` 188, `key_flags` 196, `key_address` 202, `key_mem_size` 210)
and vaddr **226-343** (118 B, the anonymous `PSTR` duplicates emitted inside the eleven stubs,
including `flags` at 226). Cross-key the first block against
`avr-nm --print-size --radix=d ... | grep -E ' key_'`. Repeat the block dump on
`firestarter_leonardo.elf`. Record the C-3 correction explicitly: **eleven** of eleven wire keys
are stored twice, not ten of eleven, and the reason a naive count misses `flags` is that the
byte before the second copy is 0x55 so `strings` emits the token as `Uflags` -- an exact-match
filter drops it and a substring `grep -c` over-reports. Name both wrong forms as forbidden.

STEP 5 -- struct offsets, both architectures, and OD-7. Build the `offsetof` probe TU described
in RESEARCH §Code Examples under the session scratch directory, compiled twice: once with
`avr-gcc -mmcu=atmega328p` and once with host `gcc`. Record all seventeen member offsets on both
architectures. Expected AVR: `cmd` 0, `operation_state` 1, `response_code` 2, `protocol` 3,
`pins` 7, `mem_size` 8, `address` 12, `vpp_mv` 16, `pulse_delay` 18, `read_settling_us` 22,
`read_strobe_us` 26, `ctrl_flags` 30, `chip_id` 34, `page_size` 36, `data_buffer` 38,
`data_size` 550, `bus_config` 554. Expected native: `protocol` 4, `pins` 8, `mem_size` 12,
`address` 16, `vpp_mv` 20, `pulse_delay` 24, `read_settling_us` 28, `read_strobe_us` 32,
`ctrl_flags` 36, `chip_id` 40, `page_size` 42, `data_buffer` 44, `data_size` 556, `bus_config`
560. Confirm the ROADMAP's "all eleven fields sit at offsets 3-37, below `data_buffer` at 38" is
exact on AVR, and state that the native table differs at every field from `protocol` down --
which is why a hand-written offset column would be wrong on one of the two architectures and why
`offsetof` derivation is a correctness requirement, not a style preference.

Then discharge **OD-7**: capture the REAL AVR compiler flags for `src/json_parser.c` from
`pio run -v -e uno` (grep the `avr-g++`/`avr-gcc` invocation line for that TU), re-derive
`sizeof(firestarter_handle_t)` with exactly those `-D` flags and that `-mmcu`, and record **ONE**
number together with the full command line that produced it. Note the discrepancy being closed:
the research's probe measured 600 while `155-after-figures.md` records 601, and state that the
-5 B delta is independently confirmed by `ram_used` (1567 -> 1562 on `uno`), so nothing
downstream depends on the absolute -- but the record must not carry two unreconciled numbers.

STEP 6 -- test and gate baselines, on the clean committed tree. Run, one at a time:
`pio test -e native`, `pio test -e native_nodevtools`, and -- in `/workspaces/firestarter_app`
with system python3 -- `python3 -m pytest tests/ -q -o addopts=""`. Record for each the case
count, the suite count, pass/fail/skip and wall time. Expected: 172 cases / 17 suites / 172
succeeded; 172 / 17 / 172; and 1976 passed / 0 failed / 0 skipped with 32 syrupy snapshots.
Record `test_read_timing`'s own case count as **9** (`RUN_TEST` entries at
`test/native/avr/test_read_timing/test_read_timing_params.cpp:544-553`) and state the lexical
trap: `grep -ro "RUN_TEST(" ` across the seventeen filtered suites totals 173 while the runner
reports 172 -- **trust the runner, never the grep** (Pitfall 7). Record raw AVR `warning:` counts
as 0/0/0 across the three build logs, and note the AVR warnings policy is `== 0`, i.e. no slack.
Record that the four CI legs are exhaustively `pio test -e native`,
`pio test -e native_nodevtools`, `pytest tests/` and `pio run` -- confirm by
`grep -rn 'pio test' .github/workflows/` and `grep -rn check_size_baseline .github/` (the latter
must return nothing). Also record the D-04 flakiness caveat: three 172/172 runs in the research
session took 19.8 s, 25.3 s and 54.6 s; duration varies 2.8x with load and the result did not.

STEP 7 -- both size-gate modes. Run
`python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --rebuild`
and record its output verbatim. Expected: FAIL with **exactly two lines**, both native case
counts (`native: cases baseline=141 observed=172` and the `native_nodevtools` twin) -- and **no
AVR flash or RAM leg**, which is what proves the pre-existing BASE-01 RED masks nothing (F-6).
Record `size_baseline.json`'s `native_envs.native.cases` as 172 and
`size_baseline_base01.json`'s as 141 (frozen at Phase 124). State D-03: `:697` is
`if flash_delta > allowance` and `:709` is `if ram_delta > ram_tolerance`, so this phase's
reduction passes with **no named exemption**, and the pass must be recorded as **one-sided** so
nobody later reads a green run as "no size change". Hand both facts forward to Phase 158 --
LAND-01 owns the re-record and LAND-03 owns the case-count mismatch; neither is this phase's to
fix.

STEP 8 -- reference-carrier applicability, re-checked at plan time (C-11 / ceiling 10). Assert
`git rev-parse wip/v1.33-size-reduction-survey-preserved` abbreviates to `a6b46f8`. Extract this
phase's two subsets from `.planning/notes/firmware-size-reduction-measured.patch` per RESEARCH
§Code Examples -- lines 1-30 (`include/firestarter.h`; hunks 1 and 2 are Phase 157, hunk 3 is
Phase 155 and already landed) and lines 98-311 (`src/json_parser.c`) -- into the session scratch
directory. Run `git apply --check -C0`, `-C1`, `-C2` and `-C3` on the json subset and record that
**all four FAIL** at `src/json_parser.c:76`, then `patch -p1 --dry-run -F3` and record that hunk
**#3 alone** fails while #4-#7 succeed with offsets. Record WHY: hunk #3's removal lines include
two provenance comments Phase 154's sweep deleted, and `-C` reduces context lines but cannot
reconcile a `-` line that no longer exists. **Do NOT apply anything** -- `--check` and
`--dry-run` only -- and re-assert `git status --porcelain` is empty afterwards, deleting any
`.rej`/`.orig` file if one appears. Conclude, for the record, that plan 02's implementation is a
**hand-port** and the patch is evidence, not a shortcut.

Hand every value to task 2. Do not write the record file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git rev-parse --short HEAD)" = "1151dc4" && test "$(git rev-parse --abbrev-ref HEAD)" = "gsd/v1.33-source-hygiene-firmware-size-reduction" && NM=$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm && pio run -e uno -e uno328pb -e leonardo > /tmp/157-before-build.log 2>&1 && grep -q 'used 24234 bytes' /tmp/157-before-build.log && grep -q 'used 24282 bytes' /tmp/157-before-build.log && grep -q 'used 26378 bytes' /tmp/157-before-build.log && grep -q 'used 1567 bytes' /tmp/157-before-build.log && grep -q 'used 2008 bytes' /tmp/157-before-build.log && test "$($NM --print-size --size-sort --radix=d .pio/build/uno/firestarter_uno.elf | awk '$4 ~ /^get_(flags|memory_size|address|chip_id|pin_count|delay|vpp_mv|algorithm|read_settling|read_strobe|page_size)$/ {s+=$2} END {print s+0}')" = "1012" && test "$($NM --print-size --radix=d .pio/build/uno/firestarter_uno.elf | grep -cE ' (get_r1|get_r2|get_rev|get_rw_pin|get_vpp_pin)$')" = "0" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "9" && test -z "$(grep -rn check_size_baseline .github/ 2>/dev/null)" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter status --porcelain` is empty at the START and at the END of this task, and `git -C firestarter rev-parse --short HEAD` prints `1151dc4` on branch `gsd/v1.33-source-hygiene-firmware-size-reduction`.
    - `pio run` reports Flash/RAM of exactly `24234`/`1567` (`uno`), `24282`/`1573` (`uno328pb`), `26378`/`2008` (`leonardo`). Any deviation STOPS the plan rather than being recorded as the new baseline.
    - The eleven-stub sum from `avr-nm --print-size --size-sort --radix=d` on the `uno` ELF is exactly `1012`, and each of the eleven per-symbol sizes is recorded individually with `get_pin_count` at `84` (the C-2 correction) and `get_read_settling`/`get_read_strobe` at `110`.
    - `get_r1`, `get_r2`, `get_rev`, `get_rw_pin` and `get_vpp_pin` produce ZERO matches in the `uno` symbol table -- recorded as the measured proof that the opacity, not the logic, was the cost.
    - `key_parsers` is recorded at `44` B, `jsoneq_` at `108`, `simple_strtoul` at `68`, and the `get_cmd` clone at `102` with its numeric `.constprop` suffix explicitly NOT pinned.
    - The `strings -a -n 2 -t d` block dump records TWO blocks on `uno`, vaddr `104-221` and `226-343`, each `118` B, with all eleven `key_*` addresses listed and `flags` present at `226`; the same two-block structure is recorded for `leonardo`.
    - The record states that `awk '$2=="flags"'` reports 1 where the truth is 2 (the `Uflags` artifact) and that a substring `grep -c flags` reports 4 on `leonardo` -- both named as forbidden oracles.
    - All seventeen member offsets are recorded for BOTH architectures, with AVR `protocol` 3 / `page_size` 36 / `data_buffer` 38 and native `protocol` 4 / `page_size` 42 / `data_buffer` 44.
    - Exactly ONE `sizeof(firestarter_handle_t)` figure is recorded, accompanied by the full compiler command line captured from `pio run -v -e uno`, plus one sentence naming the 600-vs-601 discrepancy it closes and the `ram_used` 1567->1562 independent confirmation of the -5 B delta (OD-7).
    - `pio test -e native` reports `172 test cases: 172 succeeded` over 17 suites; `pio test -e native_nodevtools` the same; `python3 -m pytest tests/ -q -o addopts=""` in `firestarter_app` reports `1976 passed`, `0 failed`, `0 skipped`.
    - `python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --rebuild` output is captured verbatim and contains exactly two FAIL lines, both `cases baseline=141 observed=172`, and NO line naming a flash or RAM leg.
    - `grep -rn check_size_baseline .github/` produces empty output, and the four CI legs are enumerated with their workflow line references.
    - `git rev-parse wip/v1.33-size-reduction-survey-preserved` abbreviates to `a6b46f8`; `git apply --check` on the `src/json_parser.c` subset FAILS at all four of `-C0`, `-C1`, `-C2`, `-C3`; `patch -p1 --dry-run -F3` reports hunk `#3` failed and `1 out of 7 hunks FAILED`.
    - No path under `firestarter/` outside `.pio/build/` differs from HEAD when the task ends, and no `.rej` or `.orig` file remains anywhere in the repo.
  </acceptance_criteria>
  <done>Every irrecoverable pre-change figure is measured on a tree proven unchanged before and after; the eleven-stub ledger closes at exactly 1012 B against five siblings at zero; the two key-string blocks are recorded by offset; both architectures' struct offsets are compiler-derived; OD-7 yields one `sizeof` number with its method; the pre-existing size-gate RED is proven to mask nothing; and the reference patch is proven not to apply.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the before-figures record</name>
  <files>.planning/v1.33/157-before-figures.md</files>
  <read_first>
    - `.planning/v1.33/156-before-figures.md` -- copy its frontmatter keys, its `## 1. Git anchors` table shape, its WARM labelling, and its per-figure "verbatim command" discipline. Note `phase:` is the full directory slug
    - `.planning/v1.33/156-after-figures.md` -- how a v1.33 record states a correction so it SUPERSEDES the ROADMAP rather than merely disagreeing with it (see its `supersedes:` block and its `## 9. The corrections ledger`)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- `Corrections to ROADMAP / REQUIREMENTS` C-1 through C-16 verbatim, so no correction is paraphrased into vagueness; and `The 999.35 / v1.28 overlap`
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md` -- §8 (the ledger format map, including the two sections 156's skeleton has no slot for) and §5 (the `_Static_assert` idiom's newness, which the record must state)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- the ten coverage ceilings and OD-1 / OD-2, both of which this record must carry
    - `.planning/ROADMAP.md` §Phase 157 (criteria 1-7) and `.planning/REQUIREMENTS.md` §4 (DECODE-01..07) -- the exact stale sentences being superseded, so the record can quote what it replaces
  </read_first>
  <action>
Create `.planning/v1.33/157-before-figures.md` in the meta repo. Filenames in that directory are
lowercase-hyphenated with the phase number prefixed; follow that.

Frontmatter keys, matching `156-before-figures.md`: `title`, `phase`
(`157-command-decode-table-handle-type-narrowing-firmware-only`), `plan` (`"01"`), `measured`
(today's date), `status` (state that this file is the ONLY source for Phase 157's before-half
figures, and that Phase 158 invalidates them -- LAND-01 re-anchors `size_baseline.json` and
cold-rebuilds), `supersedes` (name ROADMAP.md §Phase 157 criteria 1, 2, 3, 4, 5, 6 and 7 and
REQUIREMENTS.md DECODE-01 through DECODE-07 prose wherever they state a figure this file
corrects -- C-1 through C-16 plus the three planner-found corrections below), and
`requirements: [DECODE-01, DECODE-02, DECODE-03, DECODE-04, DECODE-05, DECODE-06, DECODE-07]`.

Sections, in this order (the 156 skeleton, remapped onto 157's content per PATTERNS §8):

1. **Git anchors** -- a `| Field | Value |` table: `FW_PRE_SHA` full and abbreviated, branch,
   `git status --porcelain` empty stated as measured both before AND after task 1,
   `git worktree list`, the meta repo HEAD, and the `firestarter_app` gitlink drift note (copy
   156's -- it is still true and still operator-gated).
2. **AVR image figures, WARM** -- the three flash/RAM pairs with the verbatim `pio run` command,
   plus Leonardo Caterina headroom against 28672 (2294 B). State that WARM matches the 155/156
   convention and that the cold re-record is LAND-01 / Phase 158's.
3. **The eleven-stub ledger, `uno`** -- all eleven per-symbol sizes, the sum 1012, `key_parsers`
   44, `jsoneq_` 108, `simple_strtoul` 68, the `get_cmd` clone 102 with its suffix unpinned, and
   the five siblings' ABSENCE. Add the C-2 paragraph (the floor is 84 B, not 86 B). Add one
   sentence explaining WHY, so a later reader does not re-derive it: `parser_func` is read with
   `pgm_read_ptr` and called through, so gcc can neither inline the callee nor constant-propagate
   the literal key into `jsoneq_`, and must emit each stub with a full four-argument AVR ABI
   prologue for one `simple_strtoul` and one store.
4. **The string-duplication ledger** -- the two 118 B vaddr blocks with every `key_*` address,
   the vaddr = fileoff - 148 derivation, the `leonardo` confirmation, the C-3 correction
   (**eleven** of eleven, not ten), and both forbidden oracle forms named with the numbers they
   wrongly report.
5. **Test and gate baselines, on the clean committed tree** -- both native envs' case and suite
   counts, `test_read_timing`'s 9, the host suite's 1976/0/0, AVR warnings 0/0/0 with the `== 0`
   policy, the exhaustive four-item CI list with workflow line references, the
   `grep -rn check_size_baseline .github/` empty result, and the D-04 duration/result caveat.
6. **Struct offsets, both architectures** -- the full seventeen-member table for AVR and native
   side by side, plus the OD-7 subsection: one `sizeof` number, the full captured compiler
   command line, and the 600-vs-601 reconciliation with the `ram_used` cross-check.
7. **Reference carriers** -- the patch subset line ranges and what each belongs to,
   `wip/v1.33-size-reduction-survey-preserved` @ `a6b46f8` recorded as the carrier while the
   branch named in ROADMAP §v1.33 (`size-reduction-survey`) is not, the four failing
   `git apply --check -C{0,1,2,3}` runs and the `patch -F3` hunk-by-hunk result, the reason
   (Phase 154's sweep deleted two removal-side comment lines), and the conclusion that plan 02
   is a hand-port. State also that Phase 156's research finding "the subset applies with
   `git apply -C1`" does **not** generalise here (C-11).
8. **The one-sided size gate, and the pre-existing red** -- D-03's two source line references
   quoted, the statement that a reduction needs no named exemption, the verbatim two-line FAIL
   with both native case counts and the explicit absence of any flash or RAM leg (F-6), and both
   facts handed to Phase 158 (LAND-01, LAND-03).
9. **The honest coverage ceilings -- stated, not implied** -- all TEN, in the terms this plan
   states them. Ceiling 1 is a NEGATIVE ceiling and must be written as such: `src/json_parser.c`
   IS inside the native build, so this phase has no coverage gap of Phase 155's kind and this
   record must not borrow that phrasing.
10. **The corrections index** -- a table with one row per correction: `C-N | what the ROADMAP or
    REQUIREMENTS says | what is measured | which plan carries the corrected figure forward`.
    Rows C-1 through C-16 from the research, plus these three found at plan time:
    - **C-17 (citation drift inside the research itself).** `#define READ_TIMING_MAX_US 1000UL`
      is at `src/json_parser.c:60`, not `:352` as RESEARCH §DECODE-06 and C-9 both state.
      RESEARCH's DECODE-01 table also lists the first seven `key_*` PROGMEM declarations one line
      high: measured, `memory-size` is at `:51`, `address` `:52`, `flags` `:53`, `chip-id` `:54`,
      `pin-count` `:55`, `pulse-delay` `:56`, `vpp_mv` `:57`; `algorithm` `:58`,
      `read-settling-delay` `:60`, `read-strobe-us` `:61` and `page-size` `:66` are correct as
      stated. Measured by `grep -n 'PROGMEM = "' src/json_parser.c` and
      `grep -n '#define READ_TIMING_MAX_US' src/json_parser.c` at plan time. The hoist C-9 calls
      for is still required -- only its source line moves.
    - **C-18 (S4's RED probe is the wrong one in VALIDATION).** `157-VALIDATION.md`'s Wave-0 row
      says the RED-first capture is "narrowing applied with saturation/mask deleted -> S1/S2/S4
      FAIL". That is wrong for S4. With `ctrl_flags` narrowed to `uint16_t` and the saturation
      branch deleted, a wire `flags: 65536` is truncated to `0` by the width-limited store, so
      S4's `ctrl_flags == 0` assertion **passes vacuously** against that probe. S4's only
      non-vacuous negative is a tree where `ctrl_flags` **saturates** -- the reference patch
      verbatim -- which stores `0xFFFF`. Two distinct probes are therefore required, and plan 04
      builds both. Note the second direction that keeps S4 honest: on TODAY's un-narrowed tree
      `ctrl_flags` is `uint32_t` and stores `65536`, so S4 is RED there too.
    - **C-19 (the -890 / -1148 figures predate OD-1's policy column).** The reference
      implementation the -890 B and -1148 B figures were measured on has a single global
      saturate rule and no per-row policy. OD-1 requires a per-row mask-vs-saturate policy,
      which is a column the reference does not have. Plans 02 and 03 must therefore record
      whatever they measure and, if it is not exactly -890 / -1148, state the byte difference and
      name OD-1's policy encoding as its cause. A figure that still reads exactly -1148 after
      adding a discriminator the reference lacked is the SUSPICIOUS outcome, not the expected
      one, and must not be produced by adjusting the code to chase the number.
11. **The seven settled decisions** -- OD-1 through OD-7, each with: what was decided, what was
    declined, and the declined alternative's cost. In particular: OD-1's `reject` semantics is
    declined because it needs a new message id, which means editing `tools/catalog/messages.toml`
    in the **meta** repo and regenerating `include/messages.h` (codegen-generated, never
    hand-edited) -- a cross-repo codegen step that would break the firmware-only property. And
    OD-2's kept identifier `key_parsers` is now slightly stale -- it becomes a data table of
    `{key, clamp, offset, width}`, not a table of parsers -- and the record must say so and say
    why it was kept anyway (renaming it turns
    `firestarter_app/tests/test_json_key_parity.py::test_page_size_key_string_matches_constants_py`
    RED and makes `::test_every_dispatched_identifier_has_a_declared_key_string` pass vacuously
    on an empty regex match, measured at 3 failures against a 24-passed baseline).
12. **The 999.35 / v1.28 non-additivity warning** -- DECODE-01's field table is SUPERSEDED if the
    binary command protocol ever lands, so this phase's -890 B and 999.35's -3728 B / -512 B on
    `leonardo` are **not additive**, and 999.35 must be re-measured from the post-v1.33 position
    before anyone quotes a combined saving. The operator ruled the binary protocol OUT of v1.33
    on 2026-08-22; this phase proposes no step toward it. State this here because a reader of the
    figures records will not necessarily reach the backlog entry that carries it.
13. **Summary of what this record proves.**

State once, near the top, that this document's own `file:LINE` citations were measured against
the current post-Phase-154 tree and will themselves be remapped by Phase 159 (REMAP-01..04), and
that plan 02 adds two `#include` lines which shift every line number in `src/json_parser.c` by
+2 -- expected, accounted for by D-01, and not a defect.

Write no planning-provenance token into any firmware source file -- this task writes only into
`.planning/`. Use ` -- ` rather than an em dash.

Then commit in the META repo, staging exactly `.planning/v1.33/157-before-figures.md` and nothing
else -- never `git add -A`, never `git add .`. Subject:
`docs(157-01): capture the pre-change before-figures and the nineteen ROADMAP corrections`. The
body must state that no firmware file was edited and that every AVR figure is WARM. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/157-before-figures.md && for n in 24234 1567 24282 1573 26378 2008 2294 1012 84 110 44 172 1976 141 118 655 a6b46f8 C-16 C-17 C-18 C-19 OD-7 999.35; do grep -q "$n" .planning/v1.33/157-before-figures.md || { echo "MISSING $n"; exit 1; }; done && grep -q 'key_parsers' .planning/v1.33/157-before-figures.md && grep -q 'Uflags' .planning/v1.33/157-before-figures.md && test "$(git log --oneline -1 --format=%s -- .planning/v1.33/157-before-figures.md | grep -c '^docs(157-01):')" = "1" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" = "1" && test -z "$(git status --porcelain .planning/)" && test -z "$(git -C firestarter status --porcelain)" && test "$(git -C firestarter rev-parse --short HEAD)" = "1151dc4"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/157-before-figures.md` exists with frontmatter carrying `title`, `phase`, `plan`, `measured`, `status`, `supersedes` and `requirements`, and `requirements` lists all seven DECODE ids.
    - The file contains the literals `24234`, `1567`, `24282`, `1573`, `26378`, `2008`, `2294`, `1012`, `84`, `110`, `44`, `108`, `68`, `102`, `118`, `104`, `221`, `226`, `343`, `172`, `17`, `9`, `1976`, `141`, `655`, `a6b46f8`, `1151dc4` and `28672`.
    - The file records exactly ONE `sizeof(firestarter_handle_t)` value together with the compiler command line captured from `pio run -v -e uno`, and one sentence naming the 600-vs-601 discrepancy and the `ram_used` cross-check. Source assertion: the file contains no place where two different unreconciled `sizeof` numbers are both presented as current.
    - The file names both forbidden `strings` oracles and contains the literal `Uflags`.
    - The file records the eleven-stub sum as `1012` and states the per-stub floor as `84`, and explicitly does NOT restate `86-110`, `-976`, `-172`, `19`, `45`, `ten of eleven`, `3440` or `25696` as current figures.
    - The corrections index has one row per correction from `C-1` through `C-19`, and rows `C-17`, `C-18` and `C-19` carry the plan-time measurement or reasoning stated in the action.
    - The file carries all TEN coverage ceilings, and ceiling 1 is written as a NEGATIVE ceiling stating that `src/json_parser.c` IS natively covered and that Phase 155's coverage-gap phrasing must not be borrowed.
    - The file carries a section on `OD-1` through `OD-7`, each with its declined alternative and that alternative's cost, including the `messages.toml` + codegen cost of `reject` and the staleness of the kept identifier `key_parsers`.
    - The file carries the 999.35 / v1.28 non-additivity warning naming both `-890` and `-3728`.
    - The file states that its own `file:LINE` citations will be remapped by Phase 159 and that plan 02's two new `#include` lines shift `src/json_parser.c` by +2.
    - `git -C /workspaces log --oneline -1 -- .planning/v1.33/157-before-figures.md` shows a commit whose subject begins `docs(157-01):`, and `git -C /workspaces show --stat HEAD` lists exactly one path.
    - `git -C firestarter status --porcelain` is empty and `git -C firestarter rev-parse --short HEAD` is still `1151dc4` -- no firmware commit was made by this plan.
    - No path under `firestarter_app/` is staged, and the `firestarter_app` gitlink is not in the commit's file list.
  </acceptance_criteria>
  <done>The before-half record is committed in the meta repo, every figure carries its command, all nineteen corrections and all seven OD decisions are stated as superseding the ROADMAP, the ten ceilings are carried verbatim, and no firmware file has been touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | Untrusted JSON commands cross here; unchanged by this plan (measurement only) |
| planning record -> future reader | A wrong figure recorded here is inherited silently by plans 02-07 and by Phases 158 and 159 |
| reference patch -> working tree | A patch that partially applies would silently mix Phase 155/156 work into this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-08 | Repudiation | `.planning/v1.33/157-before-figures.md` | high | mitigate | Every figure carries the verbatim command that produced it, and `git status --porcelain` is asserted empty at task start AND task end, so no number can be a post-hoc reconstruction |
| T-157-11 | Repudiation | the nineteen stale ROADMAP / REQUIREMENTS figures | medium | mitigate | A corrections index plus a `supersedes:` frontmatter key, so a stale figure cannot be re-inherited by reading the ROADMAP alone |
| T-157-12 | Repudiation | a WARM figure quoted as a cold baseline | medium | mitigate | Every AVR figure labelled WARM in the record and in the commit body; the cold re-record is explicitly handed to LAND-01 / Phase 158 |
| T-157-13 | Tampering | accidental patch application during the `git apply --check` step | high | mitigate | Only `--check` and `--dry-run` are run; `git status --porcelain` is re-asserted empty afterwards and any `.rej`/`.orig` file is deleted |
| T-157-14 | Repudiation | the DECODE-02 oracle "proven" by a broken script | high | mitigate | The record names both forbidden `strings` forms with the wrong numbers they report, and specifies the offset-resolved block comparison as the only valid oracle |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This phase installs ZERO packages -- no `pip install`, no `platformio lib install`, no `lib_deps` change. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty, HEAD still `1151dc4`, branch unchanged.
- `git -C /workspaces log --oneline -1` subject begins `docs(157-01):` and touches exactly one path.
- Every literal listed in task 2's acceptance criteria present in `.planning/v1.33/157-before-figures.md`.
- No `.rej` or `.orig` file anywhere under `/workspaces/firestarter`.
</verification>

<success_criteria>
The before-half record exists and is committed; the tree is provably unchanged before and after
every measurement; the eleven-stub ledger closes at exactly 1012 B against five siblings at
zero; the key-string duplication is recorded by offset block on two targets; both
architectures' struct offsets are compiler-derived; OD-7 yields one `sizeof` number with its
method; the reference patch is proven NOT to apply; and all nineteen corrections plus all seven
OD decisions are recorded in a file whose frontmatter says it supersedes the ROADMAP prose.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-01-SUMMARY.md` when done.
</output>
