---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 03
type: execute
wave: 3
depends_on: ["157-02"]
files_modified:
  - firestarter/include/firestarter.h
  - firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp
  - firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp
autonomous: true
requirements: [DECODE-04]

must_haves:
  truths:
    - "`handle->protocol` is `uint8_t` and `handle->ctrl_flags` is `uint16_t`, and every one of the 18 protocol-keyed dispatch sites and 40 `is_flag_set` uses keeps identical truth values by integer promotion."
    - "The narrowing is provably NOT wire-visible and NOT EEPROM-visible: neither field is serialised into any response frame, and `rurp_configuration_t` -- the only persisted firmware struct -- contains neither."
    - "The -5 B RAM saving is real on all three AVR targets and is measured two independent ways: `ram_used` from the build, and `sizeof(firestarter_handle_t)` re-derived from the real compiler flags."
    - "OD-1's mask policy on the `ctrl_flags` row is now LOAD-BEARING: with `ctrl_flags` two bytes wide, the saturate branch would produce `0xFFFF` and the row's policy bit is what stops it."
    - "Both local check scripts were RUN, not assumed: `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` (OD-6)."
    - "The measured site counts are recorded as 18 protocol-keyed sites (20 total `handle->protocol` reads) and 40 textual `is_flag_set` uses (59 post-preprocessor), never as the ROADMAP's 19 and 45."
  artifacts:
    - firestarter/include/firestarter.h
  key_links:
    - "`is_flag_set(flag)` is `((handle->ctrl_flags & flag) == flag)` -- a macro capturing `handle` from the enclosing scope, so NO call site names a type. That is why 40 call sites need zero edits, and it is also why the narrowing's correctness rests entirely on promotion semantics rather than on any per-site change."
    - "The nine `FLAG_*` values max out at 0x100, and `firestarter_app/tests/test_revision_constants_parity.py` asserts BIDIRECTIONALLY that both sides have exactly nine flags with maximum 0x100 -- so `uint16_t` is provably sufficient AND provably parity-checked, and a future tenth flag above 0xFFFF trips that gate first."
    - "`eprom_params_for(uint32_t protocol)` and `eprom_block_budget_s(uint32_t protocol, ...)` keep 32-bit parameters; the narrowed field promotes at each call site. Narrowing those signatures is a recorded lead, NOT taken."
    - "Both dispatch suites declare `make_handle(uint32_t protocol, ...)` and assign it to `h.protocol`; after this change that is an implicit narrowing in C++, and the native warnings watermark has zero headroom."
  prohibitions:
    - "Must NOT narrow `eprom_params_for(uint32_t protocol)` or `eprom_block_budget_s(uint32_t protocol, ...)`. DECODE-04 does not ask for it and it widens the blast radius into `include/eprom_params.h`, `include/eprom_budget.h`, `src/proms/eprom_params.cpp`, `src/proms/eprom_budget.cpp` and the `native_params_v131` / `native_loop_v131` suites. It is recorded as a lead, not taken."
    - "Must NOT restate the ROADMAP's `19 protocol comparisons` or `45 is_flag_set call sites`. Neither number is reproducible by any counting rule; use 18 (or 20) and 40 (or 59), each with its stated derivation (C-5)."
    - "Must NOT add, remove or renumber any `#define` in `include/firestarter.h`. `firestarter_app/firestarter/constants.py` duplicates the FLAG values, and the CLAUDE.md directive to change both together is NOT triggered because this plan changes field TYPES only."
    - "Must NOT change the `ctrl_flags` row's policy from mask to saturate (OD-1 / F-1 / C-7); this plan is the point at which that policy becomes load-bearing."
    - "Must NOT edit `src/json_parser.c` except to fix a compile failure this narrowing surfaces, and never to change the table's policy or shape."
    - "Must NOT edit `src/proms/memory.cpp` or convert its if-chain to a `switch` (DECODE-07 rejects it; `firestarter/CLAUDE.md` pins the dispatch order as a line-for-line contract)."
    - "Must NOT edit `include/messages.h` or `tools/catalog/messages.toml`."
    - "Must NOT raise the native macro-redefinition watermark or the AVR warning policy to make a gate pass; fix the cause instead."
    - "Must NOT adjust code to make the measured flash delta equal -258 B or the composed total equal -1148 B. Record what is measured and reconcile it against OD-1's policy column (C-19)."
    - "Must NOT quote `3440 B` for Leonardo's post-phase Caterina headroom; the measured value is 3442 B (C-13)."
    - "Must NOT edit any file under `firestarter_app/`; must NOT re-anchor `scripts/baseline/size_baseline.json`; must NOT repair the BASE-01 case-count mismatch; must NOT run the Phase 154 citation remap tool or repair any `.planning/` file:LINE citation."
    - "Must NOT run `pytest tests/` in `firestarter_app` before this plan's firmware commit lands (Pitfall 5)."
    - "Must NOT author or imply any bench/hardware criterion (D-02); must NOT propose any step toward the binary command protocol (999.35); must NOT push any branch."
---

<objective>
Narrow `firestarter_handle_t.protocol` to `uint8_t` and `firestarter_handle_t.ctrl_flags` to
`uint16_t`, audit every consumer surface one at a time, and measure the narrowing's own flash and
RAM delta separately from the table's.

Purpose: DECODE-01's and DECODE-04's byte figures are only attributable if they are measured at
two different commits -- the ROADMAP's -976 / -172 split is wrong at this position and the
measured split is -890 / -258 (C-4), so guessing is not an option. Narrowing is also the moment
OD-1's mask policy stops being inert: with `ctrl_flags` two bytes wide, the saturate branch that
the reference implementation applied globally would store `0xFFFF` and turn on every control flag.

Output: a two-line type change in `firestarter/include/firestarter.h`, the consumer-surface audit
recorded with its measured counts, the narrowing's own delta on all three AVR targets, and both
local check scripts run rather than assumed.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/157-before-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by **no** test unless case S2 is written. Record it as
   contingent.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that, and only for the fields
   they exercise (today `read_settling_us`, `read_strobe_us`, `page_size`; after this phase also
   `protocol`, `ctrl_flags` and the six round-trip fields OD-5 takes).
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation is a hand-port, and the
    patch is evidence, not a shortcut.

## Artifacts this phase produces

**CREATED / CHANGED by this plan:** `firestarter_handle_t.protocol` becomes `uint8_t` and
`firestarter_handle_t.ctrl_flags` becomes `uint16_t` in `firestarter/include/firestarter.h`;
conditionally, the `make_handle` protocol parameter in
`test/native/avr/test_dispatch/test_configure_memory.cpp` and
`test/native/avr/test_not_implemented/test_not_implemented.cpp` narrows to `uint8_t` (only if
`check_build_warnings.py` reports a new diagnostic).

**CREATED by other plans of this phase:** in `src/json_parser.c` -- `field_desc_t`, `FIELD`,
`FIELD_MASK`, `FIELD_WIDTH_MASK`, `FIELD_POLICY_MASK`, `store_field`, twelve `_Static_assert`
lines, the hoisted `#define READ_TIMING_MAX_US`, `#include <stddef.h>`, `#include <string.h>`,
and the eleven-row `key_parsers[]` body (plan 02). In
`test/native/avr/test_read_timing/test_read_timing_params.cpp` -- `extern "C" { #include
"memory.h" }` plus cases `test_out_of_range_algorithm_saturates_not_truncates`,
`test_out_of_range_algorithm_dispatch_fail_closes`, `test_in_range_algorithm_still_dispatches`,
`test_out_of_range_flags_masks_never_sets_every_flag`,
`test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size` (plan 04), and
`test_read_strobe_us_capped_at_max` plus the six `*_round_trips_through_the_field_table` cases
(plan 05). Records: `.planning/v1.33/157-before-figures.md` (plan 01),
`.planning/v1.33/157-after-figures.md` (plan 07).

**DELETED by plan 02 (all in `src/json_parser.c`), listed so drift verification does not flag
their absence:** the `key_parser_t` typedef and its function-pointer member; the ten stubs and
their forward declarations `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`,
`get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`,
`get_page_size`; the indirect call through the function-pointer column; and the page-size
non-clamp justification comment. **`get_flags` survives** as the deliberate DECODE-02 exception,
along with the five siblings `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`.
**`include/json_parser.h` is NOT edited.**

<!-- planner-discipline-allow: uint32_t protocol; -->
<!-- planner-discipline-allow: uint32_t ctrl_flags; -->

<tasks>

<task type="auto">
  <name>Task 1: Narrow the two handle fields and audit every consumer surface, one at a time</name>
  <files>firestarter/include/firestarter.h, firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp, firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp</files>
  <read_first>
    - `firestarter/include/firestarter.h` -- `firestarter_handle_t` at `:205-232` (the two members to narrow and their neighbours), the nine `FLAG_*` defines at `:172-189` (maximum `FLAG_SKIP_SDP_UNLOCK` = `0x100`), and `is_flag_set` at `:191-192`
    - `firestarter/include/proto_constants.h` -- the full `PROTO_*` set; the largest dispatched value is `PROTO_PHANTOM_0x39` = `0x39`
    - `firestarter/src/proms/memory.cpp` -- `configure_memory` at `:95-145`: the seventeen `handle->protocol ==` comparisons on nine lines, the four named infeasibility arms, and the generic fail-closed tail
    - `firestarter/src/proms/eprom.cpp` -- `:70` (the one `switch (handle->protocol)`), `:85-90` (`eprom_params_for` returning NULL as `configure_eprom`'s own refusal), and `:87` (the already-`(uint8_t)`-cast log payload)
    - `firestarter/src/proms/not_implemented.cpp` -- `:17` (the other already-cast log payload, and the zero-side-effect refusal)
    - `firestarter/src/firestarter.cpp` -- `:59` and `:134` (the two `ctrl_flags = 0x80` verbose defaults), `:99-111` (the seven `LOG_DEBUG_ID_SUB_U8` lines that pass `is_flag_set`'s BOOL result, never the field), and `:242` (`eprom_block_budget_s(handle->protocol, ...)`)
    - `firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp` -- `:19-22` (why a success case must NOT assert an operation pointer) and `:56-63` (`make_handle(uint32_t protocol, ...)`)
    - `firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp` -- `:6-14` (the header comment) and `:42-48` (the second `make_handle(uint32_t protocol, ...)`)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §DECODE-04 in full (the value-range justification and the seven consumer surfaces checked one at a time), §Runtime State Inventory, correction C-5, and §Common Pitfalls 12 and 14
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md` -- §7 (the `is_flag_set` / protocol call-site shapes and the measured conclusion that the classic varargs break does not exist here) and §6c (the two test-side `make_handle` narrowings, the only real hazard found)
    - `.planning/v1.33/157-before-figures.md` -- §2 and §6 (the flash/RAM pairs and the struct-offset tables, both architectures) and correction C-19
  </read_first>
  <action>
Work in `/workspaces/firestarter`. Assert `git status --porcelain` is empty and HEAD's subject
begins `refactor(157-02):` before starting.

STEP 1 -- the narrowing. In `include/firestarter.h`, change `firestarter_handle_t`'s `protocol`
member from a 32-bit unsigned to `uint8_t` and its `ctrl_flags` member from a 32-bit unsigned to
`uint16_t`. Change nothing else in the struct: do not reorder members, do not touch
`data_buffer`, and do not add or remove a field. Add a short comment on each narrowed member
stating the bound that justifies it -- `protocol`'s largest dispatched value is `0x39`
(`PROTO_PHANTOM_0x39`, `include/proto_constants.h`), and `ctrl_flags`' largest flag is `0x100`
(`FLAG_SKIP_SDP_UNLOCK`), which
`firestarter_app/tests/test_revision_constants_parity.py` asserts bidirectionally as exactly nine
flags with maximum `0x100` on both sides, so a future tenth flag above `0xFFFF` would trip that
gate before it could overflow this field.

STEP 2 -- audit the site counts and record the MEASURED numbers. Run and record, verbatim:
`grep -ro "handle->protocol ==" src/ | wc -l` (expect 17), `grep -rn "switch (handle->protocol)"
src/` (expect one hit, in `src/proms/eprom.cpp`), and `grep -rn -- "->protocol" src/ include/`
(expect 20 total reads across `src/`). Record the result as **18 protocol-keyed dispatch sites**
(17 equality comparisons plus one `switch`) out of **20 total `handle->protocol` reads**, the
other two being the `eprom_params_for` / `eprom_block_budget_s` arguments and the two
already-cast log payloads. Then run a per-file `grep -o is_flag_set <file> | wc -l` across `src/`
and record the total as **40 textual uses**, plus `grep -rno "LOG_INFO_ID[A-Z_0-9]*" src/ | wc -l`
(expect 19) to derive the **59 post-preprocessor** figure, since each `LOG_INFO_ID*` macro body
contains one `is_flag_set(FLAG_VERBOSE)`. State explicitly that the ROADMAP's 19 and 45 are not
reproducible by any counting rule and are superseded (C-5); state also that
`include/firestarter.h`'s `is_flag_set` definition and `include/memory_utils.h`'s prose mention
are neither of them call sites.

STEP 3 -- the promotion audit, surface by surface, recorded as findings rather than assumed.
Confirm and record each of the following:
  - **Comparison semantics.** Every protocol comparison is against an unsigned constant no
    greater than `0x39`; a `uint8_t` promotes to `int`, so every `handle->protocol == PROTO_*`
    keeps an identical truth value. `is_flag_set` is `((handle->ctrl_flags & flag) == flag)`;
    `uint16_t & int` promotes to `int`, and all nine flags fit in `int` on AVR (16-bit `int`,
    largest flag `0x100`). No semantic change.
  - **Log payloads.** `handle->protocol` reaches logging only through two sites that already cast
    to `uint8_t` explicitly, so the emitted byte is unchanged. `ctrl_flags` is never logged --
    the seven debug lines in `src/firestarter.cpp` pass `is_flag_set`'s `bool` result, not the
    field. The project's logging is id-and-typed-params, so there is no printf format specifier
    anywhere that could desync.
  - **Wire visibility.** Neither field is serialised into any response frame. `protocol` is
    written only by the `algorithm` key and read only for dispatch, for the two `uint32_t`
    parameter surfaces, and for the two cast error payloads. `ctrl_flags` is written only by the
    `flags` key and the two verbose defaults, and read only by `is_flag_set`. The ROADMAP's "no
    wire change" claim holds -- state it as verified, not assumed.
  - **Persistence.** `rurp_configuration_t` -- the only EEPROM-persisted firmware struct, holding
    `r1`, `r2` and `hardware_revision` -- contains neither field, and `sizeof(firestarter_handle_t)`
    appears in no wire frame and no EEPROM record. Therefore **no data migration exists in this
    phase**: the only runtime system holding the old field widths is a flashed board, the change
    is invisible across the wire, and nothing needs migrating. Record that conclusion explicitly
    rather than skipping the question.
  - **The `uint32_t` parameter surfaces.** `eprom_params_for` and `eprom_block_budget_s` keep
    32-bit parameters and the narrowed field promotes at each call site; the 4-byte compare
    survives *inside* those functions. Record narrowing them as a **lead, not taken**, naming the
    five files and two v131 suites it would reach and stating that DECODE-04 does not ask for it.

STEP 4 -- OD-1 becomes load-bearing; say so. With `ctrl_flags` two bytes wide, `store_field`'s
saturate branch would compute a maximum of `0xFFFF` and, on an out-of-range wire `flags`, store
every one of the nine flags including `FLAG_FORCE`, `FLAG_SKIP_ERASE` and
`FLAG_SKIP_BLANK_CHECK`. The `key_flags` row's `FIELD_MASK` policy bit is what prevents that.
Confirm by inspection that the row still uses the mask macro and record that this plan is the
commit at which the policy stopped being inert. Also record the consistency note: `get_flags`
assigns `simple_strtoul`'s 32-bit result straight into the now-16-bit member on the
`json_parse_config` and `json_get_cmd` paths, which truncates -- the same observable result as the
table's mask row, so all three `flags` paths agree.

STEP 5 -- build all three AVR targets and check for the test-side narrowing hazard. Run
`pio run -e uno`, `pio run -e uno328pb`, `pio run -e leonardo`, then `pio test -e native` and
`pio test -e native_nodevtools`, and then `python3 scripts/check_build_warnings.py`. Both dispatch
suites declare `make_handle` with a 32-bit `protocol` parameter and assign it to `h.protocol`,
which is now an implicit narrowing conversion in C++. No `-Wconversion`, `-Wnarrowing` or
`-Werror` appears in any `build_flags`, so it should compile silently -- but the native
macro-redefinition watermark is 1166 with a `<=` policy and near-zero headroom, so **do not
assume**. If `check_build_warnings.py` reports any new diagnostic attributable to this change, fix
it by narrowing the `make_handle` protocol parameter to `uint8_t` in BOTH
`test/native/avr/test_dispatch/test_configure_memory.cpp` and
`test/native/avr/test_not_implemented/test_not_implemented.cpp` -- both are only ever called with
values no greater than `0x39` -- and never by raising a watermark. If it reports nothing, leave
both files byte-unchanged and record that fact.

Do not edit `src/json_parser.c` in this task unless the narrowing surfaces a compile failure
there, and if it does, fix only that failure.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -c 'uint32_t protocol;' include/firestarter.h)" = "0" && test "$(grep -c 'uint32_t ctrl_flags;' include/firestarter.h)" = "0" && test "$(grep -c 'uint8_t protocol;' include/firestarter.h)" = "1" && test "$(grep -c 'uint16_t ctrl_flags;' include/firestarter.h)" = "1" && test "$(grep -cE '^#define FLAG_' include/firestarter.h)" = "9" && test "$(grep -c 'char data_buffer\[DATA_BUFFER_SIZE\];' include/firestarter.h)" = "1" && test "$(grep -ro 'handle->protocol ==' src/ | wc -l)" = "17" && grep -q 'FIELD_MASK(key_flags' src/json_parser.c && pio run -e uno -e uno328pb -e leonardo > /tmp/157-03-build.log 2>&1 && test "$(grep -c 'warning:' /tmp/157-03-build.log)" = "0" && grep -q 'used 1562 bytes' /tmp/157-03-build.log && grep -q 'used 1568 bytes' /tmp/157-03-build.log && grep -q 'used 2003 bytes' /tmp/157-03-build.log && pio test -e native 2>&1 | grep -q '172 test cases: 172 succeeded' && python3 scripts/check_build_warnings.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'uint8_t protocol;' include/firestarter.h` prints `1` and `grep -c 'uint16_t ctrl_flags;' include/firestarter.h` prints `1`; the 32-bit forms of both members produce zero matches.
    - `grep -cE '^#define FLAG_' include/firestarter.h` prints `9` -- no `#define` was added, removed or renumbered, so the `constants.py` parity surface is untouched.
    - The struct's member ORDER is unchanged and `char data_buffer[DATA_BUFFER_SIZE];` is still present exactly once; no member was reordered, added or removed.
    - `grep -ro "handle->protocol ==" src/ | wc -l` prints `17`, `grep -rn "switch (handle->protocol)" src/` yields exactly one hit in `src/proms/eprom.cpp`, and the SUMMARY records **18** protocol-keyed sites out of **20** total reads with both derivations shown.
    - The SUMMARY records `is_flag_set` at **40** textual uses in `src/` with the per-file breakdown, and **59** post-preprocessor uses derived from **19** `LOG_INFO_ID*` call sites. Source assertion: the SUMMARY contains neither `19 protocol` nor `45 is_flag_set` as a current figure.
    - The SUMMARY records all five audit findings from step 3 individually -- comparison semantics, log payloads, wire visibility, persistence (including the explicit statement that no data migration exists in this phase), and the `uint32_t` parameter surfaces recorded as a lead not taken with its five files and two v131 suites named.
    - `src/json_parser.c` still contains `FIELD_MASK(key_flags`, and the SUMMARY states that this plan is the commit at which OD-1's mask policy stopped being inert, with the `0xFFFF`-sets-all-nine-flags consequence spelled out.
    - `pio run` reports RAM of exactly `1562` (`uno`), `1568` (`uno328pb`), `2003` (`leonardo`) -- the -5 B saving on all three.
    - `grep -c 'warning:'` over the three build logs prints `0`, and `python3 scripts/check_build_warnings.py` exits 0 (OD-6). If it did not exit 0 on first run, the SUMMARY records the diagnostic and the `make_handle` parameter narrowing that fixed it in BOTH dispatch suites -- never a watermark change.
    - `pio test -e native` and `pio test -e native_nodevtools` each report `172 test cases: 172 succeeded`.
  </acceptance_criteria>
  <done>Both handle fields are narrowed with their bounds documented in place; all five consumer surfaces are audited and recorded with measured counts rather than the ROADMAP's unreproducible 19 and 45; the persistence question is answered explicitly with "no migration"; OD-1's mask policy is confirmed load-bearing; RAM falls by 5 B on all three targets; and the build is warning-free with `check_build_warnings.py` run rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the narrowing's own delta, re-derive sizeof, run the remaining gates, and commit</name>
  <files>firestarter/include/firestarter.h</files>
  <read_first>
    - `.planning/v1.33/157-before-figures.md` -- §2 (the three WARM before pairs), §6 (the struct-offset tables and the OD-7 `sizeof` figure with its captured command line), §8 (the one-sided size gate and the pre-existing red), and correction C-13 (Leonardo headroom is 3442 B, not 3440)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-SUMMARY-of-plan-02` if present, otherwise `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-02-SUMMARY.md` -- the table-only figures this task's delta is subtracted from, and the recorded OD-1 policy cost
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §Measured Figures (the after-narrowing absolutes 23086 / 23134 / 25230 and RAM 1562 / 1568 / 2003, and the struct-offset table's after column: AVR 3-32 with `data_buffer` at 33, `sizeof` 595; native unchanged at 655), §Gate blast radius, and §Common Pitfalls 14
    - `firestarter/scripts/check_no_heap_or_64bit_symbols.py` -- its policy, so a non-zero exit is read correctly
  </read_first>
  <action>
Still in `/workspaces/firestarter`.

STEP 1 -- the narrowing's own delta. Record flash and RAM for all three targets and subtract the
table-only figures plan 02 recorded. Expected direction: flash falls by a further amount that is
the same on all three, RAM falls by exactly 5 B on each. The research's after-narrowing absolutes
were `uno` 23086, `uno328pb` 23134, `leonardo` 25230 with RAM 1562 / 1568 / 2003, giving a
narrowing half of -258 B. Record the ACTUAL numbers. Then reconcile the composed total against
the before-record: the composed flash change from `1151dc4` should be about -1148 B on each
target, and if it is not exactly -1148 the SUMMARY must state the byte difference and attribute it
to OD-1's per-row policy column -- a discriminator the reference implementation the -1148 was
measured on does not have (C-19). **Never adjust code to hit -258 or -1148.** Also re-derive
Leonardo's Caterina headroom against 28672 and record it as the measured value; the ROADMAP's
3440 B is superseded by 3442 B (C-13), so quote whichever number the measurement gives and say
which ROADMAP figure it replaces. Label every figure WARM and hand the cold re-record to LAND-01 /
Phase 158.

STEP 2 -- re-derive `sizeof(firestarter_handle_t)` after the narrowing, using the SAME method and
the SAME captured compiler flags the before-record used for OD-7, on both architectures. Expected
AVR: the eleven table members at offsets 3-32 with `data_buffer` at 33, and `sizeof` five bytes
smaller than the before figure. Expected native: `sizeof` **unchanged at 655**, because the five
bytes are absorbed by the alignment padding before the function-pointer block. Record both, and
state ceiling 5 explicitly: the -5 B RAM saving is **AVR-only and unobservable natively**, so a
native test asserting it would be vacuously false and none is written. The -5 B is confirmed two
independent ways -- `ram_used` from the build and this `sizeof` re-derivation -- which is what
makes the OD-7 absolute non-load-bearing.

STEP 3 -- the remaining local gates. Run `python3 scripts/check_no_heap_or_64bit_symbols.py` and
record its exit code and output; the narrowing adds no allocation and no 64-bit arithmetic, and
`store_field`'s shift is 32-bit, so it should stay green -- but OD-6 says run it, not assume it
(it was UNVERIFIED in the research session). Then run
`python3 scripts/check_size_baseline.py --policy merge05 --baseline
scripts/baseline/size_baseline_base01.json --rebuild` and record its output verbatim. Expect the
same two-line FAIL on native case counts and **no** AVR flash or RAM line -- which is what proves
the flash and RAM legs are informative and that this reduction passes them. Record the pass as
**one-sided**, quoting `check_size_baseline.py:697` (`if flash_delta > allowance`) and `:709`
(`if ram_delta > ram_tolerance`) from source, so nobody later reads a green run as "no size
change" (D-03). Author **no** exemption constant.

STEP 4 -- commit inside the submodule. Stage exactly `include/firestarter.h`, plus the two
dispatch test files ONLY if step 5 of task 1 required narrowing their `make_handle` parameter.
Never `git add -A`, never `git add .`. Subject:
`refactor(157-03): narrow handle protocol to uint8_t and ctrl_flags to uint16_t`. The body must
state: the two bounds that justify the widths (`0x39` and `0x100`) and the host parity gate that
pins the flag maximum; that the change is neither wire-visible nor EEPROM-visible and therefore
carries no migration; the measured flash and RAM delta per target with any divergence from -258 /
-1148 attributed to OD-1's policy column; that Leonardo's Caterina headroom is the measured value
and the ROADMAP's 3440 B is superseded; the measured site counts 18/20 and 40/59, explicitly
replacing 19 and 45; and that OD-1's mask policy on the `ctrl_flags` row is now load-bearing. Do
not push.

STEP 5 -- the host gates, on the now-committed tree. In `/workspaces/firestarter_app`, run
`python3 -m pytest tests/test_revision_constants_parity.py tests/test_json_key_parity.py -q -o
addopts=""`. Expected **24 passed**: the revision-constants module scans `#define` lines only and
the field-type change is invisible to it, and the wire-key module is unaffected because OD-2 kept
the table shape. Run only after the commit -- two legs assert sibling porcelain (Pitfall 5).
Delete any `.rej` or `.orig` file first.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && pio run -e uno -e uno328pb -e leonardo > /tmp/157-03b-build.log 2>&1 && grep -q 'used 1562 bytes' /tmp/157-03b-build.log && grep -q 'used 1568 bytes' /tmp/157-03b-build.log && grep -q 'used 2003 bytes' /tmp/157-03b-build.log && test "$(grep -c 'warning:' /tmp/157-03b-build.log)" = "0" && pio test -e native 2>&1 | grep -q '172 test cases: 172 succeeded' && pio test -e native_nodevtools 2>&1 | grep -q '172 test cases: 172 succeeded' && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --rebuild > /tmp/157-03-size.log 2>&1; test "$(grep -c 'cases baseline=141 observed=172' /tmp/157-03-size.log)" = "2" && test "$(grep -ciE '(flash|ram)_(used|delta)' /tmp/157-03-size.log)" = "0" && cd /workspaces/firestarter && test "$(git log --oneline -1 --format=%s | grep -c '^refactor(157-03):')" = "1" && cd /workspaces/firestarter_app && python3 -m pytest tests/test_revision_constants_parity.py tests/test_json_key_parity.py -q -o addopts="" 2>&1 | grep -q '24 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `pio run` reports RAM of exactly `1562` (`uno`), `1568` (`uno328pb`), `2003` (`leonardo`), i.e. -5 B on every target, and the flash figure for each target is recorded together with BOTH deltas: the narrowing half (against plan 02's table-only figures) and the composed total (against `.planning/v1.33/157-before-figures.md` §2).
    - If either delta is not exactly `-258` / `-1148`, the SUMMARY states the byte difference and attributes it to OD-1's per-row policy column (C-19). Source assertion: the SUMMARY contains no sentence claiming a figure was brought into line with -258 or -1148 by editing code.
    - `sizeof(firestarter_handle_t)` is re-derived after the change with the SAME method and captured flags as the before-record's OD-7 figure, on BOTH architectures, and recorded: the AVR value is exactly 5 B smaller than the before figure with the eleven table members at offsets `3`-`32` and `data_buffer` at `33`; the native value is `655`, unchanged.
    - The SUMMARY states ceiling 5 explicitly -- the -5 B RAM saving is AVR-only and unobservable natively, so no native test asserts it -- and states that the -5 B is confirmed two independent ways (`ram_used` and `sizeof`).
    - Leonardo's Caterina headroom against `28672` is recorded as measured, with one sentence naming which ROADMAP figure it supersedes. Source assertion: the SUMMARY does not present `3440` as the current value.
    - `python3 scripts/check_build_warnings.py` and `python3 scripts/check_no_heap_or_64bit_symbols.py` both exit 0 with their output recorded (OD-6, both UNVERIFIED before this phase).
    - `python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --rebuild` output is captured verbatim, contains exactly two `cases baseline=141 observed=172` lines and NO flash or RAM leg line, and the SUMMARY records the pass as ONE-SIDED quoting `:697` and `:709` from source (D-03). No exemption constant was authored.
    - `pio test -e native` and `pio test -e native_nodevtools` each report `172 test cases: 172 succeeded` -- the count still has not moved; plans 04 and 05 move it.
    - `git -C firestarter log --oneline -1` subject begins `refactor(157-03):`; the commit's file list is exactly `include/firestarter.h`, plus the two dispatch test files if and only if `check_build_warnings.py` required their `make_handle` parameter narrowed; `git status --porcelain` is empty afterwards with no `.rej` or `.orig` file anywhere.
    - `python3 -m pytest tests/test_revision_constants_parity.py tests/test_json_key_parity.py -q -o addopts=""` in `firestarter_app` reports `24 passed`, run on the committed firmware tree, and `git -C firestarter_app status --porcelain` shows no modification attributable to this plan.
  </acceptance_criteria>
  <done>The narrowing's own flash delta and the composed total are measured on all three targets and reconciled against the before-record with any divergence attributed rather than engineered away; RAM is 5 B smaller on every target and confirmed two independent ways; `sizeof` is re-derived on both architectures with the AVR-only ceiling stated; both check scripts and the one-sided size gate were run rather than assumed; the change is committed as a minimal-path commit inside the submodule; and both host parity modules report 24 passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | The wire `algorithm` value now lands in a one-byte dispatch key; truncation at this boundary is the phase's central risk |
| host -> firmware serial line | The wire `flags` value now lands in a two-byte bitmask; a saturating clamp here would grant every privilege |
| firmware -> AVR EEPROM | Checked and found EMPTY: `rurp_configuration_t` contains neither narrowed field, so no persisted layout changes and no migration exists |
| firmware -> host response frames | Checked and found EMPTY: neither field is serialised, so the ROADMAP's "no wire change" claim is verified rather than assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-02 | Elevation of Privilege | `handle->protocol` narrowed to `uint8_t` at the wire boundary | critical | mitigate | `store_field` saturates `protocol` to `0xFF`, which appears in no arm of `configure_memory`'s chain and therefore reaches `configure_not_implemented` -- `MSG_ERR_PROTOCOL_NOT_IMPLEMENTED` with ZERO hardware side effects. Without it, `0x105` truncates to `0x05` and dispatches into the 5 V page-write handler, and `0x107` would truncate to `0x07` and reach `configure_eprom`, which enables the 12/13 V VPP route. Pinned by cases S1 and S2 in plan 04 |
| T-157-01 | Elevation of Privilege | `handle->ctrl_flags` narrowed to `uint16_t` | critical | mitigate | The `key_flags` row keeps `FIELD_MASK`; this plan is the commit at which that policy becomes load-bearing, because a saturate to `0xFFFF` would set all nine flags including `FLAG_FORCE`, `FLAG_SKIP_ERASE` and `FLAG_SKIP_BLANK_CHECK`, and `is_flag_set` would then read true at every one of the 40 call sites (OD-1 / F-1 / C-7). Pinned by case S4 in plan 04 |
| T-157-18 | Tampering | a promotion or comparison changing truth value at one of the 18 protocol sites or 40 flag sites | high | mitigate | Audited surface by surface in task 1 step 3 and recorded as findings: all comparisons are against unsigned constants no greater than `0x39`, `uint8_t` and `uint16_t` both promote to `int`, and all nine flags fit in AVR's 16-bit `int`. Backed by both native envs staying at 172/172 and by three clean AVR builds |
| T-157-19 | Tampering | a log payload or format specifier desyncing on the narrowed field | medium | mitigate | Verified: both `protocol` log sites already cast to `uint8_t` explicitly; `ctrl_flags` is never logged (the seven debug lines pass `is_flag_set`'s `bool`); the project's logging is id-and-typed-params so no format string exists to desync |
| T-157-07 | Tampering | `pins` saturating to 255 flips which arm `mem_util_calculate_top_address_register` takes, where today's truncation to 0 takes the opposite arm | low | accept | A behaviour change on invalid input only, in the safer direction for VPP-drop, and `is_vpp_pin_present` rejects 255. Accepted and STATED in the record rather than discovered later (research A4) |
| T-157-12 | Repudiation | a WARM figure quoted as a cold baseline, or a green one-sided gate read as "nothing moved" | medium | mitigate | Every figure labelled WARM with the cold re-record handed to LAND-01; the size-gate pass recorded as one-sided with `:697` and `:709` quoted from source (D-03) |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages and changes no `lib_deps`. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty; HEAD subject begins `refactor(157-03):`.
- `include/firestarter.h` has `uint8_t protocol;` and `uint16_t ctrl_flags;` exactly once each, nine `FLAG_*` defines, and an unchanged member order.
- `pio run -e uno -e uno328pb -e leonardo` green, zero `warning:` lines, RAM 1562 / 1568 / 2003.
- `pio test -e native` and `pio test -e native_nodevtools` both `172 test cases: 172 succeeded`.
- `check_build_warnings.py`, `check_no_heap_or_64bit_symbols.py` exit 0; the merge05 size gate fails on exactly the two pre-existing native case-count lines and no flash or RAM leg.
- `pytest tests/test_revision_constants_parity.py tests/test_json_key_parity.py` in `firestarter_app`: 24 passed.
</verification>

<success_criteria>
`handle->protocol` is one byte and `handle->ctrl_flags` is two, justified in place by the `0x39`
and `0x100` bounds and by the bidirectional host parity gate that pins the flag maximum; all five
consumer surfaces are audited and recorded with measured counts of 18/20 and 40/59 rather than the
ROADMAP's unreproducible 19 and 45; the change is proven neither wire-visible nor EEPROM-visible
so no migration exists; RAM falls by exactly 5 B on all three targets, confirmed two independent
ways, with the AVR-only ceiling stated; OD-1's mask policy is confirmed load-bearing at this
commit; the narrowing's own flash delta and the composed total are recorded with any divergence
from -258 / -1148 attributed to OD-1's policy column; and every local gate was run rather than
assumed.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-03-SUMMARY.md` when done.
</output>