---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 04
type: execute
wave: 4
depends_on: ["157-03"]
files_modified:
  - firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp
autonomous: true
requirements: [DECODE-05]

must_haves:
  truths:
    - "An out-of-range wire `algorithm` cannot reach a real algorithm handler: it saturates to 0xFF, which appears in no arm of `configure_memory`'s chain, so the command reaches `configure_not_implemented` with `RESPONSE_CODE_ERROR` and all three operation pointers NULL."
    - "The DISPATCH outcome is asserted, not merely the stored byte -- case S2 is the case that would have caught the defect, and it is the load-bearing one."
    - "An out-of-range wire `flags` cannot turn on `FLAG_FORCE`, `FLAG_SKIP_ERASE` or `FLAG_SKIP_BLANK_CHECK`: the bitmask row masks and never saturates."
    - "A valid algorithm still dispatches, so S1 and S2 cannot be satisfied by breaking every algorithm."
    - "An out-of-range wire `page-size` no longer lands on a plausible VALID power of two; it saturates to 0xFFFF, which fails both of the consumer's guards."
    - "Every new assertion has been SEEN to fail against a tree where the property it asserts is absent, and the two probes are distinct: the saturation-deleted probe reddens S1, S2 and S5, while only a saturating-bitmask probe reddens S4."
  artifacts:
    - firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp
  key_links:
    - "`configure_memory`'s fail-closed tail is what turns a saturated 0xFF into a refusal with zero hardware side effects; S2 is the only thing in the repository that pins that link, and the link is CONTINGENT on 0xFF being unmapped in the dispatch chain (ceiling 6)."
    - "`test_read_timing`'s existing `setUp` already stubs `Serial.write` and `Serial.flush`, which is what lets the refusal path's `LOG_ERROR_ID_*` run without aborting -- do not remove it."
    - "`store_field`'s value parameter is `uint32_t`, so the saturation branch behaves identically on AVR and x86-64; that is what makes these native cases valid oracles for AVR behaviour."
    - "Adding cases moves the native count off 172, which reddens the `cases` leg of both `check_size_baseline.py` modes. That is a HANDOFF to Phase 158 / LAND-01, not a defect of this phase."
  prohibitions:
    - "Must NOT assert that the selected operation is 'not `flash_5v_page`'s'. No suite in the tree compares against a named handler's function pointer and that symbol is not exported to tests. Use `test_not_implemented.cpp`'s idiom -- `RESPONSE_CODE_ERROR` plus all three operation pointers NULL -- which is strictly stronger and is local convention (PATTERNS finding 4)."
    - "Must NOT assert an operation pointer on the SUCCESS case S3. `test_configure_memory.cpp:19-22` documents why: `configure_sram` is a stub that leaves `firestarter_operation_init` NULL, so pointer-set assertions spuriously fail. `response_code` is the robust dispatch-success signal."
    - "Must NOT use any input value at or above 2^32. `simple_strtoul` returns `unsigned long`, 32-bit on AVR and 64-bit on native, so a value that large is not a valid cross-architecture oracle (Pitfall 12)."
    - "Must NOT hard-code a numeric struct offset in any test. AVR and native offsets differ at every member from `protocol` down; an offset literal is wrong on one of the two."
    - "Must NOT claim case S4 went RED against the saturation-deleted probe. With the saturation branch removed, a wire `flags` of 65536 truncates to 0 and S4 passes VACUOUSLY. S4's RED requires the saturating-bitmask probe (C-18)."
    - "Must NOT call `eeprom28c_page_mask` from a test -- it is `static` in `src/proms/eeprom_28c.cpp` and unreachable. S5 asserts the parse-level saturation only, and the consumer-side fallback is recorded as source-level evidence (C-20)."
    - "Must NOT create a new test suite. A new suite needs BOTH native `test_filter` lists AND both `-I` lists updated in lockstep and would move the suite count as well as the case count."
    - "Must NOT edit `platformio.ini`."
    - "Must NOT remove or weaken the suite's `setUp` Serial stubs, and must NOT remove the suite's local `#define READ_TIMING_MAX_US`."
    - "Must NOT edit `src/json_parser.c`, `include/firestarter.h` or `src/proms/memory.cpp` in this plan; a case that fails is either a wrong case or a real defect, and a real defect is reported, not patched around by loosening the assertion."
    - "Must NOT re-anchor `scripts/baseline/size_baseline.json` or repair the BASE-01 case-count mismatch to absorb the new case count -- that is Phase 158 / LAND-01 and LAND-03."
    - "Must NOT attribute a native-suite failure to this change on N=1; re-run on an idle machine first (D-04)."
    - "Must NOT leave a probe worktree, or any commit inside one, behind. Must NOT edit any file under `firestarter_app/`. Must NOT push any branch."
---

<objective>
Author the five DECODE-05 safety cases in the existing `test_read_timing` suite, prove each one
fails against a tree where the property it asserts is absent, and land them green.

Purpose: this is the milestone's single safety requirement. `json_parser.c` applied no range check
of any kind before this phase, and the narrowing turns that into a live hole -- a wire
`algorithm: 261` would truncate to `0x05` and dispatch into the 5 V page-write handler, and
`0x107` would truncate to `0x07` and reach `configure_eprom`, which enables the 12/13 V VPP route.
The existing suite is provably blind to it: with the narrowing applied and the saturation deleted,
the research ran `pio test -e native` and got 172 of 172 green. So a new case that goes RED against
that tree is a real oracle rather than a tautology, and the RED must be captured before the case
is trusted.

Output: five new cases in
`firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp`, each with its captured
RED, committed inside the submodule, with the native case count's movement handed forward to
Phase 158.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/157-before-figures.md
</context>

## Why these cases land AFTER the implementation, and how RED-first is still discharged

A case asserting a post-fix property cannot be committed on the pre-fix tree without leaving both
CI native legs RED for two commits. Phase 156's plan 01 used exactly this reasoning to move three
of its own Wave-0 items into later waves, each time with the cause stated. The obligation is
therefore discharged **per case** rather than by commit order: every new assertion is authored
against a probe tree in which the property it asserts is absent, seen to FAIL there, and only then
landed against the real tree and seen to pass. Two distinct probes are required and the assignment
is not interchangeable:

| Probe | What is broken in it | Cases that must go RED | Cases that must stay GREEN |
|---|---|---|---|
| **A** -- saturation deleted | the `width < sizeof(uint32_t)` saturation branch removed from `store_field` | S1, S2, S5 | S3, S4 (S4 passes VACUOUSLY here -- C-18) |
| **B** -- saturating bitmask | the `key_flags` row switched from the mask macro to the saturate macro | S4 | S1, S2, S3, S5 |

Probe A is the tree the research already ran (172/172 green), which is the published proof that
the pre-existing suite cannot see the hole. Probe B is the reference implementation's own verbatim
behaviour, which is the fail-open defect OD-1 exists to reject.

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by **no** test unless case S2 is written. Record it as
   contingent.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that, and only for the fields
   they exercise (today `read_settling_us`, `read_strobe_us`, `page_size`; after this phase also
   `protocol`, `ctrl_flags` and the six round-trip fields OD-5 takes).
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation is a hand-port, and the
    patch is evidence, not a shortcut.

**Plus one ceiling this plan adds, C-20:** `eeprom28c_page_mask` is `static` in
`src/proms/eeprom_28c.cpp:628` and therefore **unreachable from any test**. The research's S5
spec and `157-VALIDATION.md` both say the case proves the consumer "falls back"; it cannot. S5's
assertable half is the parse-level saturation to `0xFFFF`. The consumer half is **source-level
evidence only**: `0xFFFF` exceeds `AT28C_PAGE_SIZE_MAX` (512) **and** fails the
`(requested & (requested - 1)) == 0` power-of-two test, so both guards reject it and the function
returns `AT28C_PAGE_SIZE_FALLBACK - 1`, i.e. the mask `63`, not the size `64`. Record it that way.

## Artifacts this phase produces

**CREATED by this plan** (all in
`firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp`):
`extern "C" { #include "memory.h" }`; and five cases plus their `RUN_TEST` entries --
`test_out_of_range_algorithm_saturates_not_truncates` (S1),
`test_out_of_range_algorithm_dispatch_fail_closes` (S2),
`test_in_range_algorithm_still_dispatches` (S3),
`test_out_of_range_flags_masks_never_sets_every_flag` (S4),
`test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size` (S5).

**CREATED by other plans of this phase:** in `src/json_parser.c` -- `field_desc_t`, `FIELD`,
`FIELD_MASK`, `FIELD_WIDTH_MASK`, `FIELD_POLICY_MASK`, `store_field`, twelve `_Static_assert`
lines, the hoisted `#define READ_TIMING_MAX_US`, `#include <stddef.h>`, `#include <string.h>`, the
eleven-row `key_parsers[]` body (plan 02); the narrowed `protocol` and `ctrl_flags` members of
`firestarter_handle_t` in `include/firestarter.h` (plan 03); `test_read_strobe_us_capped_at_max`
and the six `*_round_trips_through_the_field_table` cases in the same test file (plan 05);
`.planning/v1.33/157-before-figures.md` (plan 01) and `.planning/v1.33/157-after-figures.md`
(plan 07).

**DELETED by plan 02 (all in `src/json_parser.c`), listed so drift verification does not flag
their absence:** the `key_parser_t` typedef and its function-pointer member; the ten stubs and
their forward declarations `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`,
`get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`,
`get_page_size`; the indirect call through the function-pointer column; and the page-size
non-clamp justification comment. **`get_flags` survives** as the deliberate DECODE-02 exception,
along with `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`. **`include/json_parser.h`
is NOT edited.**

<tasks>

<task type="auto">
  <name>Task 1: Author the five safety cases in a probe tree and capture each RED</name>
  <files>
    (all authoring happens inside a throwaway git worktree; nothing in
    /workspaces/firestarter is edited by this task, and the case bodies are carried forward to
    task 2)
  </files>
  <read_first>
    - `firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp` -- the WHOLE file (195 lines): the include block `:24-32`, the local `#define READ_TIMING_MAX_US` `:36-38`, `setUp` / `tearDown` `:40-51`, `make_handle` and `parse_json` `:53-75`, the nine existing cases `:78-183`, and the `main` / `RUN_TEST` block `:181-195`. Case T7 at `:142-156` is the in-file model for a non-vacuous message-bearing assertion -- copy that style, not the bare `TEST_ASSERT_TRUE` at `:105-116`
    - `firestarter/test/native/avr/test_not_implemented/test_not_implemented.cpp` -- `:6-14` (the header comment explaining WHY pointer assertions are safe for a refusal), `:27-34` (the Serial stubs), `:42-48` (`make_handle`), and `:52-59` (the exact case shape S2 must copy: `RESPONSE_CODE_ERROR` plus all three operation pointers NULL, and the `extern "C" { #include "memory.h" }` include form)
    - `firestarter/test/native/avr/test_dispatch/test_configure_memory.cpp` -- `:19-22` (why a SUCCESS case must not assert an operation pointer) and `:73-77` (the exact case shape S3 must copy)
    - `firestarter/src/json_parser.c` -- `store_field` and the `key_parsers[]` rows as landed by plan 02, so the probes break exactly the right thing; and `json_parse`'s reset block, so each case knows which members are zeroed per command (`address`, `ctrl_flags`, `chip_id`, `page_size` are; `protocol`, `pins`, `mem_size`, `pulse_delay` and the two read-timing knobs are not, and `make_handle` zero-initialises the whole handle)
    - `firestarter/src/proms/memory.cpp` -- `configure_memory` `:95-145`: which values reach which handler, the four named infeasibility arms, and the generic fail-closed tail. This is where S1's `0xFF` and S3's `0x05` land
    - `firestarter/src/proms/not_implemented.cpp` -- the refusal's response code, its `LOG_ERROR_ID_U8` payload and its zero-side-effect property
    - `firestarter/include/firestarter.h` -- `RESPONSE_CODE_*`, `CMD_READ` (1) and `CMD_WRITE` (2), and the nine `FLAG_*` values S4 asserts against individually
    - `firestarter/src/proms/eeprom_28c.cpp` -- `:625-651` (`eeprom28c_page_mask`, its `static` linkage, `AT28C_PAGE_SIZE_MAX` 512 and `AT28C_PAGE_SIZE_FALLBACK` 64, and the fact that it returns the FALLBACK minus one as a mask)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §DECODE-05 in full: where the truncation happens, what `0x05` then does, the fail-closed tail, the blindness proof F-4, the per-field saturate/mask/reject analysis table, the AVR/native divergence, the test spec and the anti-tautology instruction
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md` -- §6a, §6b, §6c and §Shared Patterns (non-vacuous message-bearing Unity assertions, and the fail-closed comment register)
  </read_first>
  <action>
Assert `git status --porcelain` is empty in `/workspaces/firestarter` and HEAD's subject begins
`refactor(157-03):`.

STEP 1 -- the probe worktree. `git worktree add --detach /tmp/157-s-probe/firestarter HEAD`. The
leaf directory name **must be exactly `firestarter`**:
`firestarter_app/tests/test_checker_convention.py::test_scope_is_firmware_only` hard-codes it.
Record `git worktree list` before and after.

STEP 2 -- author all five cases in the probe tree's copy of
`test/native/avr/test_read_timing/test_read_timing_params.cpp`. Add
`extern "C" { #include "memory.h" }` to the include block using the same form
`test_not_implemented.cpp` uses -- this is an include line only; the TU already links
`src/proms/memory.cpp` because every native env's `build_src_filter` carries `+<proms/>`, so no
`platformio.ini` or `-I` change is needed. Use the existing `make_handle` and `parse_json` helpers
unchanged. Every assertion must be the message-bearing form
(`TEST_ASSERT_EQUAL_*_MESSAGE` / `TEST_ASSERT_NULL_MESSAGE`) whose message states the defect the
case exists to catch, in case T7's register. Append the five `RUN_TEST` entries to `main` in
declaration order.

  - **S1 `test_out_of_range_algorithm_saturates_not_truncates`.** Parse
    `{"cmd":1,"algorithm":261}` with `CMD_READ`, assert `parse_json` returned 0, and assert
    `h.protocol` equals `0xFF`. The message must say that 261 must not become 5, naming the
    truncation and the handler it would have reached.
  - **S2 `test_out_of_range_algorithm_dispatch_fail_closes`** -- the load-bearing case. Parse the
    same input, then call `configure_memory(&h)`, then assert `h.response_code` equals
    `RESPONSE_CODE_ERROR` **and** that `h.firestarter_operation_init`,
    `h.firestarter_operation_main` and `h.firestarter_operation_end` are all NULL. Do **not**
    compare against a named handler's function pointer -- no suite in the tree does that and the
    symbol is not exported to tests; the all-three-NULL plus `RESPONSE_CODE_ERROR` form is
    `test_not_implemented.cpp`'s local idiom and is strictly stronger. The message must say this
    is the case that would have caught the defect: the stored byte being right is not the same
    claim as the dispatch fail-closing.
  - **S3 `test_in_range_algorithm_still_dispatches`** -- the non-regression guard. Parse
    `{"cmd":1,"algorithm":5}`, assert `h.protocol` equals 5, call `configure_memory(&h)`, and
    assert `h.response_code` is **not** `RESPONSE_CODE_ERROR`. Do **not** assert any operation
    pointer here: `test_configure_memory.cpp:19-22` documents that `configure_sram` is a stub
    leaving `firestarter_operation_init` NULL, so pointer-set assertions on a success path
    spuriously fail. The message must say this case exists so S1 and S2 cannot be satisfied by
    breaking every algorithm.
  - **S4 `test_out_of_range_flags_masks_never_sets_every_flag`.** Parse
    `{"cmd":2,"flags":65536}` with `CMD_WRITE` and assert `h.ctrl_flags` equals 0. Then add three
    further assertions, one per dangerous flag, that `h.ctrl_flags` bitwise-AND `FLAG_FORCE`,
    `FLAG_SKIP_ERASE` and `FLAG_SKIP_BLANK_CHECK` are each zero -- so the case reads as what it
    prevents, not merely as an equality. The message must state the fail-open it rejects:
    saturating a bitmask to its type maximum turns on all nine flags at once, and `is_flag_set`
    would then read true at every one of the 40 call sites. Do not write the macro `is_flag_set`
    in the test -- it captures `handle` from the enclosing scope and no call site names a type.
  - **S5 `test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size`.** Parse
    `{"cmd":2,"page-size":65600}` and assert `h.page_size` equals `0xFFFF`. The message must say
    that today's truncation of that input yields **64** -- a perfectly valid page size, which is
    what makes the hole silent -- while `0xFFFF` is rejected by the consumer. Add a comment, not
    an assertion, recording the consumer-side half as source-level evidence: `eeprom28c_page_mask`
    is `static` in `src/proms/eeprom_28c.cpp` and unreachable from any test, and `0xFFFF` fails
    BOTH its guards -- it exceeds `AT28C_PAGE_SIZE_MAX` (512) and it fails the power-of-two test
    -- so the function returns `AT28C_PAGE_SIZE_FALLBACK - 1`, a mask, not a size (C-20).

Keep every input value below 2^32: `simple_strtoul` returns `unsigned long`, 32-bit on AVR and
64-bit on native, so a larger input is not a valid cross-architecture oracle. Do not hard-code any
struct offset. Do not touch `setUp`, `tearDown`, `make_handle`, `parse_json`, the local
`#define READ_TIMING_MAX_US`, or any of the nine existing cases.

STEP 3 -- probe A, and capture the RED. In the probe tree, delete the saturation branch from
`store_field` -- the `width < sizeof(uint32_t)` block that computes the member maximum and
saturates -- leaving the clamp step and the `memcpy` intact. Run
`pio test -e native -f "*test_read_timing*"` and capture the output verbatim. Required outcome:
**S1, S2 and S5 FAIL; S3 and S4 pass; all nine pre-existing cases pass.** If S1, S2 or S5 passes
here, the case is not an oracle and must be rewritten before proceeding. Then run the full
`pio test -e native` on probe A and record the count: the nine pre-existing cases plus S3 and S4
pass while S1, S2 and S5 fail, which is the direct extension of the research's F-4 result (that
tree was 172 of 172 green before these cases existed).

STEP 4 -- probe B, and capture S4's RED. Restore `store_field` in the probe tree
(`git checkout -- src/json_parser.c`), then change the `key_flags` row from the mask macro to the
saturate macro -- the reference implementation's own verbatim behaviour. Run
`pio test -e native -f "*test_read_timing*"` and capture the output verbatim. Required outcome:
**S4 FAILS with `h.ctrl_flags` observed as `0xFFFF`; S1, S2, S3 and S5 pass; all nine pre-existing
cases pass.** Record the observed value, because `0xFFFF` is precisely the fail-open OD-1 rejects.

Record explicitly, in the SUMMARY, why two probes were needed rather than one: with the saturation
branch deleted, a wire `flags` of 65536 is truncated to 0 by the width-limited store, so S4's
equality holds and the case passes VACUOUSLY against probe A.
`157-VALIDATION.md`'s Wave-0 row claims a single saturation-deleted probe reddens S1, S2 and S4;
that is wrong for S4 and is correction C-18. Note the second direction that keeps S4 honest: on
the pre-phase tree at `1151dc4`, `ctrl_flags` was 32 bits wide and stored 65536, so S4 was RED
there too.

STEP 5 -- discard the probe and prove nothing landed. `git checkout -- .` in the probe tree, then
`git worktree remove --force /tmp/157-s-probe/firestarter` and `git worktree prune`. Assert `git
worktree list` in `/workspaces/firestarter` matches its pre-probe output, `git status --porcelain`
there is empty, `git branch --list` gained nothing, and `git rev-list --count HEAD` is unchanged
from the value recorded at the start of this task -- record both numbers rather than comparing
`HEAD` against `HEAD~1`, which is vacuously 1. Carry the five finished case bodies forward to
task 2.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | grep -c 157-s-probe)" = "0" && test ! -d /tmp/157-s-probe/firestarter && test "$(git log --oneline -1 --format=%s | grep -c '^refactor(157-03):')" = "1" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "9" && pio test -e native 2>&1 | grep -q '172 test cases: 172 succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - The probe worktree's leaf directory was named exactly `firestarter`, and `git worktree list` in `/workspaces/firestarter` is byte-identical before and after this task.
    - Probe A (`store_field`'s saturation branch deleted) produces a captured `pio test -e native -f "*test_read_timing*"` transcript in which `test_out_of_range_algorithm_saturates_not_truncates`, `test_out_of_range_algorithm_dispatch_fail_closes` and `test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size` all FAIL, while `test_in_range_algorithm_still_dispatches`, `test_out_of_range_flags_masks_never_sets_every_flag` and all nine pre-existing cases pass. A pass by any of the three that must fail is a FAILED task.
    - Probe B (the `key_flags` row switched to the saturate macro) produces a captured transcript in which `test_out_of_range_flags_masks_never_sets_every_flag` FAILS with the observed `ctrl_flags` value `0xFFFF` recorded, while the other four new cases and all nine pre-existing cases pass.
    - Both transcripts are transcribed verbatim into the SUMMARY, not paraphrased, and each names which probe produced it.
    - The SUMMARY states why two probes were needed, records C-18 as a correction to `157-VALIDATION.md`'s Wave-0 row, and records the second RED direction (S4 was also RED on the pre-phase tree because `ctrl_flags` was 32 bits wide there). Source assertion: the SUMMARY contains no sentence asserting that S4 went red against the saturation-deleted probe.
    - S2's assertions are `RESPONSE_CODE_ERROR` plus all three operation pointers NULL. Source assertion: no case body in the authored file references a named algorithm handler's function pointer.
    - S3 asserts only that `response_code` is not `RESPONSE_CODE_ERROR` after `configure_memory`, and asserts no operation pointer, with the `configure_sram`-is-a-stub reason recorded in a comment or the SUMMARY.
    - S4 contains, in addition to the equality, three separate assertions that the `FLAG_FORCE`, `FLAG_SKIP_ERASE` and `FLAG_SKIP_BLANK_CHECK` bits are each clear.
    - Every new input value is below 2^32, and no case body contains a numeric struct offset literal.
    - S5 asserts the parse-level value only, and the consumer-side fallback is recorded as a comment naming `eeprom28c_page_mask`'s `static` linkage, `AT28C_PAGE_SIZE_MAX` 512, the power-of-two test, and the fact that the function returns the fallback MINUS ONE (C-20).
    - `/tmp/157-s-probe/firestarter` does not exist when the task ends; `git worktree prune` has been run; `git branch --list` gained no entry; `git rev-list --count HEAD` is recorded at task start and task end and the two numbers are equal.
    - `/workspaces/firestarter` is untouched: `git status --porcelain` empty, HEAD still the `refactor(157-03):` commit, the test file still at nine `RUN_TEST` entries, and `pio test -e native` still `172 test cases: 172 succeeded`.
  </acceptance_criteria>
  <done>All five safety cases are written in local idiom, each has been seen to FAIL against a tree in which the property it asserts is absent, the two probes are distinguished with C-18 recorded, the observed fail-open value 0xFFFF is captured from probe B, the C-20 ceiling on S5's consumer half is stated, and no trace of either probe remains.</done>
</task>

<task type="auto">
  <name>Task 2: Land the five cases green, and hand the case-count movement to Phase 158</name>
  <files>firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp</files>
  <read_first>
    - `firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp` -- the current file, so the five cases are inserted without disturbing the nine existing ones or the helpers
    - `.planning/v1.33/157-before-figures.md` -- §5 (the recorded baselines: `test_read_timing` at 9 cases, both native envs at 172/17/172) and §8 (the one-sided size gate and the pre-existing BASE-01 red)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- correction C-15 (adding native cases moves the count off 172 and reddens both baseline gates' count legs; that is a handoff to LAND-01, not a defect) and §Common Pitfalls 6, 7 and 10
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- §Sampling Rate and the DECODE-05 rows of the Per-Task Verification Map
  </read_first>
  <action>
Work in `/workspaces/firestarter`. Edit exactly one file:
`test/native/avr/test_read_timing/test_read_timing_params.cpp`.

STEP 1 -- land the five cases exactly as authored and proven in task 1, with their five `RUN_TEST`
entries appended to `main` in declaration order and the `extern "C" { #include "memory.h" }`
include added. Change nothing else in the file: `setUp`, `tearDown`, `make_handle`, `parse_json`,
the local `#define READ_TIMING_MAX_US` and the nine existing cases stay byte-identical.

STEP 2 -- run the suite and both envs. `pio test -e native -f "*test_read_timing*"` must report
**14 cases, 14 succeeded**. Then `pio test -e native` and `pio test -e native_nodevtools` must
each report **177 cases, 177 succeeded** over 17 suites -- the two envs must stay in lockstep.
If any leg fails, re-run on an idle machine before attributing it to this change (D-04): the
research observed three 172/172 runs at 19.8 s, 25.3 s and 54.6 s, a 2.8x duration spread with a
constant result. Trust the runner's count, never `grep -c "RUN_TEST("` -- the lexical count across
the seventeen filtered suites is one higher than the executed count (Pitfall 7).

STEP 3 -- confirm nothing else moved. `pio run -e uno` must still succeed with zero `warning:`
lines and the SAME flash and RAM figures plan 03 recorded, because a native test case cannot
change an AVR image. Record that identity explicitly: it is the cheapest available proof that this
plan added no production code. Then run `python3 scripts/check_build_warnings.py` and
`python3 scripts/check_no_heap_or_64bit_symbols.py` and record both exit codes (OD-6).

STEP 4 -- the case-count handoff. Run
`python3 scripts/check_size_baseline.py --policy merge05 --baseline
scripts/baseline/size_baseline_base01.json --rebuild` and record its output verbatim. The two
native `cases` lines will now read `observed=177` instead of `observed=172`; the leg was already
RED against BASE-01's frozen 141 and remains RED, and still **no** AVR flash or RAM leg fails.
Then record, as an explicit handoff to **Phase 158 / LAND-01**: the native case count moved from
172 to 177 on BOTH `native` and `native_nodevtools`, plan 05 will move it again, and
`scripts/baseline/size_baseline.json` records 172 today. **Do not edit either baseline file.**
State that this is expected movement, not a defect of this phase, and that leaving Phase 158 to
discover it would be the failure mode this handoff prevents.

STEP 5 -- commit inside the submodule. Stage exactly
`test/native/avr/test_read_timing/test_read_timing_params.cpp` -- never `git add -A`, never
`git add .`. Subject: `test(157-04): prove an out-of-range algorithm and flags fail closed`. The
body must state: which case is load-bearing and why (S2 asserts the dispatch outcome, not the
stored byte); that S1, S2 and S5 were seen RED against a saturation-deleted probe and S4 against a
saturating-bitmask probe, with C-18 named; that ceiling 6 stands -- saturation-as-fail-closed is
contingent on `0xFF` being unmapped in `configure_memory`'s chain, a property of the dispatch table
rather than of `store_field`; that C-20 stands -- S5's consumer half is source-level evidence
because `eeprom28c_page_mask` is `static`; and the 172 to 177 case-count movement as a handoff to
LAND-01. Write no planning-provenance token into the test file. Do not push.

STEP 6 -- the host gate, on the now-committed tree. In `/workspaces/firestarter_app`, run
`python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o
addopts=""` and expect **24 passed**. This plan touches no scanned firmware source, so the only
way these legs move is the sibling-porcelain assertion -- which is why they run after the commit
(Pitfall 5). Delete any `.rej` or `.orig` file first.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "14" && for t in test_out_of_range_algorithm_saturates_not_truncates test_out_of_range_algorithm_dispatch_fail_closes test_in_range_algorithm_still_dispatches test_out_of_range_flags_masks_never_sets_every_flag test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size; do test "$(grep -c "$t" test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "2" || { echo "MISSING $t"; exit 1; }; done && grep -q '#include "memory.h"' test/native/avr/test_read_timing/test_read_timing_params.cpp && test "$(grep -c 'FLAG_SKIP_BLANK_CHECK' test/native/avr/test_read_timing/test_read_timing_params.cpp)" -ge "1" && pio test -e native -f "*test_read_timing*" 2>&1 | grep -q '14 test cases: 14 succeeded' && pio test -e native 2>&1 | grep -q '177 test cases: 177 succeeded' && pio test -e native_nodevtools 2>&1 | grep -q '177 test cases: 177 succeeded' && pio run -e uno > /tmp/157-04-build.log 2>&1 && test "$(grep -c 'warning:' /tmp/157-04-build.log)" = "0" && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && test "$(git log --oneline -1 --format=%s | grep -c '^test(157-04):')" = "1" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" = "1" && cd /workspaces/firestarter_app && python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o addopts="" 2>&1 | grep -q '24 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp` prints `14`, and each of the five new case names appears exactly twice in the file (its definition and its `RUN_TEST` entry).
    - `#include "memory.h"` is present in the test file, inside an `extern "C"` block, and `platformio.ini` is byte-unchanged.
    - `pio test -e native -f "*test_read_timing*"` reports `14 test cases: 14 succeeded`; `pio test -e native` and `pio test -e native_nodevtools` each report `177 test cases: 177 succeeded` over 17 suites.
    - `setUp`, `tearDown`, `make_handle`, `parse_json`, the local `#define READ_TIMING_MAX_US` and all nine pre-existing case bodies are byte-identical to their pre-plan state. Source assertion: `git diff HEAD~1 -- test/native/avr/test_read_timing/test_read_timing_params.cpp` shows only additions plus the one include line, and no deletion inside any pre-existing case body.
    - `pio run -e uno` exits 0 with zero `warning:` lines and reports the SAME flash and RAM figures plan 03 recorded, and the SUMMARY states that identity as the proof that this plan added no production code.
    - `python3 scripts/check_build_warnings.py` and `python3 scripts/check_no_heap_or_64bit_symbols.py` both exit 0 (OD-6).
    - The `check_size_baseline.py --policy merge05` output is captured verbatim, its two native `cases` lines read `observed=177`, and no flash or RAM leg line appears. Neither baseline file was edited.
    - The SUMMARY records the 172-to-177 movement on BOTH native envs as an explicit handoff to Phase 158 / LAND-01, states that plan 05 moves it again, and states that `size_baseline.json` records 172 today.
    - The SUMMARY carries both probe transcripts from task 1, names S2 as the load-bearing case, and restates ceilings 6 and the new C-20 in the terms this plan states them.
    - `git -C firestarter log --oneline -1` subject begins `test(157-04):`, `git show --stat HEAD` lists exactly one path, `git status --porcelain` is empty afterwards, and no `.rej` or `.orig` file exists.
    - `python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o addopts=""` in `firestarter_app` reports `24 passed`, run on the committed firmware tree, with zero host files changed.
  </acceptance_criteria>
  <done>The five safety cases are committed and green; the read-timing suite is at 14 cases and both native envs at 177 in lockstep; the AVR image is byte-for-byte the same as plan 03's, proving no production code was added; the case-count movement is handed to LAND-01 rather than absorbed; and both host parity modules still report 24 passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | The wire `algorithm` and `flags` integers are attacker- or bug-controlled and land in a dispatch key and a privilege bitmask; these cases are the only executing evidence that the boundary holds |
| stored field -> dispatch decision | A correct stored byte is a different claim from a correct dispatch; S2 is the only thing in the repository that pins the second |
| test assertion -> evidence | An assertion never seen to fail is not evidence; the two probes are what convert these cases from description into oracle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-02 | Elevation of Privilege | out-of-range wire `algorithm` narrowing into a valid protocol | critical | mitigate | Cases S1 and S2 pin saturation to `0xFF` AND the resulting refusal (`RESPONSE_CODE_ERROR`, all three operation pointers NULL, zero hardware side effects). Both were seen RED against a saturation-deleted tree. Without them the suite is blind: the research measured 172 of 172 green on exactly that tree |
| T-157-01 | Elevation of Privilege | out-of-range wire `flags` saturating to all-flags-set | critical | mitigate | Case S4 pins the mask, asserts the equality AND the three dangerous bits individually, and was seen RED against a saturating-bitmask probe with the observed value `0xFFFF` captured -- the reference implementation's own verbatim fail-open |
| T-157-05 | Tampering | out-of-range wire `page-size` truncating to a plausible VALID power of two | medium | mitigate | Case S5 pins saturation to `0xFFFF`; the consumer's rejection is source-level evidence because `eeprom28c_page_mask` is `static` (C-20) |
| T-157-20 | Repudiation | a new case that is a tautology rather than an oracle | high | mitigate | Per-case planted-negative probes with verbatim captured transcripts, and an acceptance criterion that a pass by a case that must fail is a FAILED task |
| T-157-21 | Repudiation | claiming S4's RED against the wrong probe | high | mitigate | C-18 recorded; the two probes' expected RED/GREEN split is tabulated in the plan; a source assertion forbids the wrong claim in the SUMMARY |
| T-157-22 | Tampering | absorbing the native case-count movement by editing a baseline file | medium | mitigate | Both baseline files are prohibited from edit; the movement is recorded as a handoff to Phase 158 / LAND-01 with the old and new numbers |
| T-157-06 | Information disclosure | the refusal reports `MSG_ERR_PROTOCOL_NOT_IMPLEMENTED` and cannot distinguish "you sent 261" from "you sent 0x34" | low | accept | A distinguishing id needs meta-repo `messages.toml` plus regenerated `include/messages.h` -- a cross-repo codegen step that breaks the firmware-only property (OD-1). Accepted and stated |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages and changes no `lib_deps`. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty; HEAD subject begins `test(157-04):`; exactly one path in the commit.
- `pio test -e native -f "*test_read_timing*"`: 14 of 14. `pio test -e native` and `pio test -e native_nodevtools`: 177 of 177 each.
- `pio run -e uno` green, zero `warning:` lines, flash and RAM identical to plan 03's figures.
- `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` exit 0.
- No worktree named `157-s-probe` remains; `git worktree list` matches its pre-task output.
- `pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py` in `firestarter_app`: 24 passed.
</verification>

<success_criteria>
Five executing native cases pin the phase's safety requirement: an out-of-range `algorithm`
saturates to `0xFF` AND the dispatch fail-closes with all three operation pointers NULL; a valid
algorithm still dispatches; an out-of-range `flags` masks to zero with the three dangerous flag
bits individually asserted clear; and an out-of-range `page-size` no longer lands on a valid power
of two. Each case has been seen to fail against a tree in which its property is absent, using the
correct probe for that case, with both transcripts captured verbatim. The AVR image is unchanged
from plan 03's, both native envs are at 177 in lockstep, and the case-count movement is handed
forward to Phase 158 rather than absorbed.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-04-SUMMARY.md` when done.
</output>
