---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 06
type: execute
wave: 6
depends_on: ["157-05"]
files_modified: []
autonomous: true
requirements: [DECODE-07]

must_haves:
  truths:
    - "The rejected `switch` alternative is measured at THIS phase's post-change position, on all three AVR targets, as a first-party number rather than a stale third-party one."
    - "The original survey pair 25696 / 25678 is recorded with its provenance and marked SUPERSEDED as absolutes, and the fresh figure is recorded beside it -- both, so nobody reads either alone."
    - "The if-chain in `src/proms/memory.cpp` is byte-unchanged by this phase, and the `switch` variant exists in no commit anywhere."
    - "The second reason the rejection is right -- `firestarter/CLAUDE.md` pins `configure_memory`'s dispatch order as a source-of-truth contract, so a `switch` would require rewriting that document too -- is recorded, because the ROADMAP criterion does not give it."
    - "The branch-inventory golden that was Phase 156's main red is proven GREEN here, because Phase 157 touches neither `src/proms/eprom.cpp` nor `src/proms/memory.cpp`."
  artifacts: []
  key_links:
    - "The `switch` delta is measured on the NARROWED field: gcc's choice between a jump table and a comparison chain depends on value density, and `switch (uint8_t)` is not the same decision as `switch (uint32_t)` -- which is exactly why a pre-narrowing figure cannot answer the question."
    - "DECODE-07 is discharged by a RECORD, not by code. The measurement produced here is the raw material for `.planning/v1.33/157-after-figures.md`'s DECODE-07 section, which plan 07 writes."
  prohibitions:
    - "Must NOT commit the `switch` variant, on any branch, in any repository. It is built once in a throwaway worktree, measured, and discarded."
    - "Must NOT edit `src/proms/memory.cpp` in `/workspaces/firestarter`. DECODE-07 REJECTS the conversion; recording the measurement is the whole requirement."
    - "Must NOT edit `firestarter/CLAUDE.md`. The if-chain is retained precisely so that document stays valid with zero edits; an edit here would invert the argument."
    - "Must NOT restate the survey's `25696` / `25678` as current absolutes. They predate Phases 155 and 156 and the flash-ceiling quick task, and are stale by roughly 1.4 to 2.6 KB (C-10)."
    - "Must NOT label the fresh delta as confirming or refuting `+18 B` unless the measurement says so; report the measured number and let it stand. If the measurement is not taken for any reason, the `+18 B` must be labelled `[UNVERIFIED at this position]` with the survey cited as provenance -- never restated as measured."
    - "Must NOT re-derive, hand-edit or regenerate `firestarter/tests/golden/protocol_branch_inventory.json`. This phase touches neither file it pins, so the gate is verified GREEN, not updated."
    - "Must NOT re-anchor `scripts/baseline/size_baseline.json` or repair the BASE-01 case-count mismatch (Phase 158 / LAND-01, LAND-03)."
    - "Must NOT run the Phase 154 citation remap tool or repair any `.planning/` file:LINE citation (Phase 159, D-01)."
    - "Must NOT author or imply any bench/hardware criterion (D-02); must NOT propose any step toward the binary command protocol (999.35)."
    - "Must NOT leave a probe worktree, or any commit inside one, behind. Must NOT edit any file under `firestarter_app/`. Must NOT push any branch."
---

<objective>
Measure the rejected `switch`-instead-of-if-chain alternative at this phase's post-change position,
on all three AVR targets, then discard the variant -- converting a stale third-party number into a
first-party one taken where the decision actually applies.

Purpose: DECODE-07 is the one requirement in this phase that changes no code. Its criterion cites
`uno` 25696 versus 25678 for a `+18 B` penalty, but current `uno` is 24234 before this phase and
roughly 23000 after, so both absolutes are stale by 1.4 to 2.6 KB and the delta was never verified
at this position (C-10). Whether the direction still holds is genuinely unknown: gcc's choice
between a jump table and a comparison chain depends on value density over the dispatched set, and
this phase changed the switched expression's type from 32 bits to 8. OD-4 settles it -- build the
variant once, record the delta, discard the variant.

Output: no source change anywhere. A SUMMARY carrying the fresh three-target measurement, the
original figures with their provenance and staleness, and the second rejection argument the
criterion omits -- all of it raw material for `157-after-figures.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/157-before-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by **no** test unless case S2 is written. Record it as
   contingent.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that; plan 05 closed this for
   all eleven rows.
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation is a hand-port, and the
    patch is evidence, not a shortcut.

**Plus C-20 and C-21 from plans 04 and 05:** `eeprom28c_page_mask` is `static` and unreachable
from any test, so S5's consumer half is source-level evidence only; and the two
`READ_TIMING_MAX_US` definitions are an unremovable duplicate with a named drift risk.

**Plus this plan's own ceiling, C-22:** the `switch` delta is a **single-configuration
measurement** on one compiler at one optimisation level. It states what avr-gcc 7.3.0 emitted for
this dispatched value set at this tree position; it is not a general claim about `switch` versus
if-chain, and a different toolchain version could decide differently. Record it as such.

## Artifacts this phase produces

**CREATED by this plan:** nothing. DECODE-07 is record-only; the artifact that discharges it is
`.planning/v1.33/157-after-figures.md`, written by plan 07, and this plan supplies its raw
material. The `switch` variant exists only inside a throwaway worktree and is discarded.

**CREATED by other plans of this phase:** in `src/json_parser.c` -- `field_desc_t`, `FIELD`,
`FIELD_MASK`, `FIELD_WIDTH_MASK`, `FIELD_POLICY_MASK`, `store_field`, twelve `_Static_assert`
lines, the hoisted `#define READ_TIMING_MAX_US`, `#include <stddef.h>`, `#include <string.h>`, the
eleven-row `key_parsers[]` body (plan 02); the narrowed `protocol` and `ctrl_flags` members of
`firestarter_handle_t` in `include/firestarter.h` (plan 03); `extern "C" { #include "memory.h" }`
plus twelve cases in `test/native/avr/test_read_timing/test_read_timing_params.cpp` (plans 04 and
05, taking that suite from 9 cases to 21 and both native envs from 172 to 184);
`.planning/v1.33/157-before-figures.md` (plan 01); `.planning/v1.33/157-after-figures.md`
(plan 07).

**DELETED by plan 02 (all in `src/json_parser.c`), listed so drift verification does not flag
their absence:** the `key_parser_t` typedef and its function-pointer member; the ten stubs and
their forward declarations `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`,
`get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`,
`get_page_size`; the indirect call through the function-pointer column; and the page-size
non-clamp justification comment. **`get_flags` survives** as the deliberate DECODE-02 exception,
along with `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`. **`include/json_parser.h`
is NOT edited, and `src/proms/memory.cpp` is not edited by any plan in this phase.**

<tasks>

<task type="auto">
  <name>Task 1: Build the switch variant once at this position, measure all three targets, discard it</name>
  <files>
    (no tracked file is edited -- the variant is built inside a throwaway git worktree that is
    removed before the task ends)
  </files>
  <read_first>
    - `firestarter/src/proms/memory.cpp` -- `configure_memory` at `:95-145` in full: the seventeen `handle->protocol ==` comparisons across seven arms, the four named infeasibility values in the fifth-from-last arm, and the generic fail-closed tail with its comment. Enumerate the dispatched value set from THIS source rather than from any document
    - `firestarter/include/proto_constants.h` -- every `PROTO_*` value the arms reference, so the density of the dispatched set over its range can be stated
    - `firestarter/CLAUDE.md` -- §Protocol Dispatch (the dispatch-order contract, the KNOWN_PROTOCOLS list, the no-legacy-fallback-axis statement, and the fail-closed claim) and §Algorithm Handlers
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §DECODE-07 in full, correction C-10, and §DECODE-04's note that keeping the if-chain keeps `firestarter/CLAUDE.md` valid with zero edits
    - `.planning/v1.33/157-before-figures.md` -- §2 (the before flash figures) and the corrections index row for C-10
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-05-SUMMARY.md` -- the post-change flash figures per target that the `switch` variant is compared against
  </read_first>
  <action>
Assert `git status --porcelain` is empty in `/workspaces/firestarter` and HEAD's subject begins
`test(157-05):`.

STEP 1 -- record the if-chain baseline at this position. Run `pio run -e uno`,
`pio run -e uno328pb` and `pio run -e leonardo` in `/workspaces/firestarter` and record the flash
figure per target. These must equal the figures plan 05 recorded; if they do not, stop and report,
because the comparison is meaningless against a moving baseline. Label them WARM.

STEP 2 -- enumerate the dispatched value set from source. From `configure_memory`'s arms, list
every protocol value that reaches a handler, resolving each `PROTO_*` name to its numeric value
from `include/proto_constants.h`, and note which arms group several values. Record the count, the
lowest and highest value, and the resulting density over that span -- this is the property gcc's
jump-table-versus-comparison-chain decision turns on, and it is why a pre-narrowing figure cannot
answer the question: the switched expression is now 8 bits wide, not 32.

STEP 3 -- the probe worktree. `git worktree add --detach /tmp/157-sw-probe/firestarter HEAD`. The
leaf directory name **must be exactly `firestarter`**
(`firestarter_app/tests/test_checker_convention.py::test_scope_is_firmware_only` hard-codes it).
Record `git worktree list` before and after.

STEP 4 -- build the variant. In the PROBE tree only, convert `configure_memory`'s protocol
if-chain into a single `switch (handle->protocol)` with one `case` label per dispatched value,
grouped exactly as the arms group them today, falling through to the same handler call per group,
and with a `default` that calls the same fail-closed handler the generic tail calls. Preserve the
dispatch ORDER, preserve every handler call, preserve the `mem_util_set_address(handle, 0)` and
the function-pointer assignments above the chain, and change nothing else in the file. The variant
must be behaviourally identical -- a variant that is not is not a measurement of the alternative.
Then run `pio run -e uno`, `pio run -e uno328pb` and `pio run -e leonardo` in the probe tree and
record the flash figure per target. Also run `pio test -e native` in the probe tree as a sanity
check that the variant is behaviourally equivalent, and record the case count and result; a
behavioural difference invalidates the size comparison and must be reported rather than absorbed.

STEP 5 -- the delta, per target. For each of `uno`, `uno328pb` and `leonardo`, record
`switch: <n>` / `if-chain: <m>` / `delta <sign><k> B`, measured at Phase 157's post-change
position, with the exact `pio run` command quoted. State whether the direction matches the
survey's `+18 B` claim, in the terms the measurement actually supports -- do not label the survey
confirmed or refuted beyond what the three numbers show, and do not generalise: this is one
compiler (avr-gcc 7.3.0) at one optimisation level on one dispatched value set (C-22).

STEP 6 -- record the original figures with their provenance. State that the criterion's `uno`
`25696` (switch) and `25678` (if-chain) come from
`.planning/notes/firmware-size-reduction-survey.md`, that they predate Phases 155 and 156 and the
flash-ceiling quick task, that current `uno` is `24234` before this phase per the before-record,
and that they are therefore **superseded as absolutes**. Record both the original pair and the
fresh pair so a later reader cannot mistake one for the other.

STEP 7 -- discard, and prove nothing landed. `git checkout -- .` in the probe tree, then
`git worktree remove --force /tmp/157-sw-probe/firestarter` and `git worktree prune`. Assert `git
worktree list` in `/workspaces/firestarter` matches its pre-probe output, `git status --porcelain`
there is empty, `git branch --list` gained nothing, and `git rev-list --count HEAD` matches the
value recorded at task start -- record both numbers rather than comparing `HEAD` against `HEAD~1`,
which is vacuously 1. Then assert that `src/proms/memory.cpp` in `/workspaces/firestarter` is
byte-identical to its state at the start of this phase: compare `git hash-object
src/proms/memory.cpp` against the same value computed at `1151dc4`
(`git show 1151dc4:src/proms/memory.cpp | git hash-object --stdin`). They must match -- no plan in
this phase edits that file.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | grep -c 157-sw-probe)" = "0" && test ! -d /tmp/157-sw-probe/firestarter && test "$(git log --oneline -1 --format=%s | grep -c '^test(157-05):')" = "1" && test "$(git hash-object src/proms/memory.cpp)" = "$(git show 1151dc4:src/proms/memory.cpp | git hash-object --stdin)" && test "$(grep -c 'switch (handle->protocol)' src/proms/memory.cpp)" = "0" && test "$(grep -ro 'handle->protocol ==' src/proms/memory.cpp | wc -l)" = "17" && pio run -e uno -e uno328pb -e leonardo > /tmp/157-06-build.log 2>&1 && test "$(grep -c 'warning:' /tmp/157-06-build.log)" = "0" && pio test -e native 2>&1 | grep -q '184 test cases: 184 succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - The if-chain baseline flash figures recorded in step 1 equal, exactly, the post-change figures plan 05 recorded for all three targets.
    - The dispatched value set is enumerated from `src/proms/memory.cpp` with each `PROTO_*` name resolved to its numeric value, and the count, range and density are recorded.
    - The probe worktree's leaf directory was named exactly `firestarter`, and `git worktree list` in `/workspaces/firestarter` is byte-identical before and after this task.
    - The `switch` variant's flash figure is recorded for ALL THREE targets, each with the verbatim `pio run` command, and each accompanied by a `switch` / `if-chain` / delta triple.
    - `pio test -e native` was run in the probe tree and its result recorded; if the case count or result differs from `184 of 184`, the SUMMARY reports that the variant was not behaviourally equivalent and the size comparison is therefore not usable, rather than reporting the delta anyway.
    - The survey's `25696` and `25678` are recorded with `.planning/notes/firmware-size-reduction-survey.md` named as their provenance, marked SUPERSEDED as absolutes, and stated alongside -- never in place of -- the fresh figures. Source assertion: the SUMMARY does not present either survey number as a current measurement.
    - The SUMMARY records the delta in the terms the three measurements support and does not claim the `+18 B` figure was confirmed or refuted beyond that, and it states C-22: one compiler, one optimisation level, one dispatched value set.
    - `git hash-object src/proms/memory.cpp` equals the blob hash of that file at `1151dc4`, and `grep -c 'switch (handle->protocol)' src/proms/memory.cpp` prints `0` -- the variant exists in no commit and the if-chain is byte-unchanged by this phase.
    - `grep -ro 'handle->protocol ==' src/proms/memory.cpp | wc -l` prints `17`, matching the count plan 03 recorded.
    - `/tmp/157-sw-probe/firestarter` does not exist when the task ends; `git worktree prune` has been run; `git branch --list` gained no entry; `git rev-list --count HEAD` is recorded at task start and end and the two numbers are equal.
  </acceptance_criteria>
  <done>The rejected alternative is measured as a first-party number on all three targets at the position where the decision applies, the stale survey pair is recorded with its provenance and superseded rather than restated, the variant exists in no commit, and `src/proms/memory.cpp` is proven byte-identical to its pre-phase state.</done>
</task>

<task type="auto">
  <name>Task 2: Record the second rejection argument and prove the dispatch-pinning gates are green</name>
  <files>
    (no file is edited -- this task verifies and records)
  </files>
  <read_first>
    - `firestarter/CLAUDE.md` -- §Protocol Dispatch and §Algorithm Handlers in full: the claim that dispatch is solely on `handle->protocol`, the KNOWN_PROTOCOLS list, the no-legacy-fallback-axis statement, and the fail-closed-to-`configure_not_implemented` claim
    - `firestarter/src/proms/memory.cpp` -- `configure_memory` `:95-145`, to check each CLAUDE.md claim against the source as it stands after this phase
    - `firestarter/tests/golden/protocol_branch_inventory.json` -- its `meta` block (`sources`, `blob_shas`, `recorded_at_head`, `recorded_by`, `how_to_update`) and its `counts`
    - `firestarter_app/tests/test_dispatch_mirror.py` -- what it actually scans (`doc/PROTOCOLS.md` and a firmware TEST file, not `memory.cpp`), so its greenness is reported for the right reason
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §Gate blast radius (the `protocol_branch_inventory.json` row: Phase 157 touches neither `eprom.cpp` nor `memory.cpp`, so this gate is GREEN and was Phase 156's main red, not this phase's) and §DECODE-07's second rejection argument
    - `.planning/v1.33/156-after-figures.md` -- how Phase 156 recorded its golden re-derivation, so this plan's "verified green, not updated" framing is unmistakably different from it
  </read_first>
  <action>
No file is edited by this task. Verify and record.

STEP 1 -- the second rejection argument, which the ROADMAP criterion does not give. Read
`firestarter/CLAUDE.md` §Protocol Dispatch and check each of its claims against
`src/proms/memory.cpp` as it stands after this phase: that dispatch is solely on
`handle->protocol` with no secondary axis; that the protocol-prefix chain covers the
KNOWN_PROTOCOLS set; that there is no legacy-integer fallback axis; and that an unrecognized
value, `0` included, fail-closes to `configure_not_implemented`. Record each as still TRUE of the
source, or record precisely which claim is not and stop rather than editing either side. Then
record the argument itself: `firestarter/CLAUDE.md` documents the dispatch order as a
source-of-truth contract, so converting the chain to a `switch` would require rewriting that
section -- the if-chain is not merely cheaper, it is the shape the project's own documentation
pins. **Do not edit `firestarter/CLAUDE.md`.** Its needing no edit is the point.

STEP 2 -- prove the branch-inventory golden is GREEN, and say why. Run `git hash-object
src/proms/eprom.cpp` and compare it against `blob_shas['src/proms/eprom.cpp']` in
`firestarter/tests/golden/protocol_branch_inventory.json`; they must match. Then, from
`/workspaces/firestarter_app`, run `python3 -m pytest tests/test_protocol_branch_inventory.py -q
-o addopts=""` and record the result. Record the reason it is green: Phase 157 touches neither
`src/proms/eprom.cpp` nor `src/proms/memory.cpp`, so the gate that was Phase 156's main red is
not this phase's business. **Do not re-derive, hand-edit or regenerate the golden.** State
explicitly that this is a "verified green" outcome, not the "re-derived inside the same commit"
outcome Phase 156 recorded -- the two read alike in a summary and mean opposite things.

STEP 3 -- confirm the neighbouring scanning gates. Run, from `/workspaces/firestarter_app`,
`python3 -m pytest tests/test_dispatch_mirror.py tests/test_parse_gate_admission.py -q -o
addopts=""` and record the result. Record why each is unaffected: `test_dispatch_mirror.py` scans
`doc/PROTOCOLS.md` and a firmware test file rather than `memory.cpp`, so DECODE-07's if-chain is
not its business; and `test_parse_gate_admission.py` scans `src/firestarter.cpp`, which this phase
does not touch. Run these only on the committed firmware tree.

STEP 4 -- assemble DECODE-07's discharge material for plan 07, in one place in the SUMMARY: the
fresh three-target `switch` / if-chain / delta triples with their commands; the survey's original
pair with its provenance and its supersession as absolutes; the dispatched value set with its
range and density; the two rejection arguments (the measured size penalty, and the CLAUDE.md
contract); the C-22 single-configuration ceiling; and the statement that the requirement is
discharged by the record in `.planning/v1.33/157-after-figures.md` and by no code change.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git hash-object src/proms/eprom.cpp)" = "$(python3 -c "import json;print(json.load(open('tests/golden/protocol_branch_inventory.json'))['meta']['blob_shas']['src/proms/eprom.cpp'])")" && test "$(git hash-object src/proms/memory.cpp)" = "$(git show 1151dc4:src/proms/memory.cpp | git hash-object --stdin)" && test "$(git diff --name-only 1151dc4 HEAD -- CLAUDE.md | wc -l)" = "0" && cd /workspaces/firestarter_app && python3 -m pytest tests/test_protocol_branch_inventory.py tests/test_dispatch_mirror.py tests/test_parse_gate_admission.py -q -o addopts="" 2>&1 | tail -3 | grep -qE '[0-9]+ passed' && python3 -m pytest tests/test_protocol_branch_inventory.py tests/test_dispatch_mirror.py tests/test_parse_gate_admission.py -q -o addopts="" 2>&1 | grep -qv 'failed'</automated>
  </verify>
  <acceptance_criteria>
    - Each of `firestarter/CLAUDE.md` §Protocol Dispatch's four claims is checked against `src/proms/memory.cpp` and recorded individually as still true, or the failing claim is named and the task stops.
    - `git diff --name-only 1151dc4 HEAD -- CLAUDE.md` produces empty output -- `firestarter/CLAUDE.md` is byte-unchanged by this phase, which is the argument, not an oversight.
    - `git hash-object src/proms/eprom.cpp` equals `meta.blob_shas['src/proms/eprom.cpp']` in `firestarter/tests/golden/protocol_branch_inventory.json`, and `python3 -m pytest tests/test_protocol_branch_inventory.py -q -o addopts=""` passes.
    - The golden file is byte-unchanged. Source assertion: `git status --porcelain tests/golden/protocol_branch_inventory.json` is empty and `git diff --name-only HEAD~1 HEAD` does not list it.
    - The SUMMARY states that the branch-inventory outcome is "verified green because this phase touches neither pinned file", explicitly distinguishing it from Phase 156's "re-derived inside the same commit" outcome.
    - `python3 -m pytest tests/test_dispatch_mirror.py tests/test_parse_gate_admission.py -q -o addopts=""` passes, and the SUMMARY records the scan target of each as the reason it is unaffected.
    - `git hash-object src/proms/memory.cpp` still equals its blob hash at `1151dc4`.
    - The SUMMARY contains a single consolidated DECODE-07 discharge section carrying all six items listed in step 4, ready for plan 07 to transcribe into `.planning/v1.33/157-after-figures.md`.
  </acceptance_criteria>
  <done>Both rejection arguments are recorded -- the freshly measured size penalty and the CLAUDE.md dispatch-order contract that a `switch` would invalidate -- the branch-inventory golden and both neighbouring scanning gates are proven green for their correct reasons without touching the golden, and DECODE-07's discharge material is assembled in one place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe worktree -> committed history | A `switch` variant that escaped into a commit would silently reverse DECODE-07's decision; the boundary is enforced by blob-hash comparison against `1151dc4` |
| survey figure -> phase record | A stale absolute restated as current is how a superseded number gets re-inherited; both pairs are recorded side by side with provenance |
| firmware source -> host scanning gates | Three host modules scan firmware source or docs near this territory; each is verified green for its own stated reason rather than assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-26 | Tampering | the `switch` variant escaping the probe worktree into a commit | high | mitigate | Built only in a detached throwaway worktree; `git hash-object src/proms/memory.cpp` compared against its blob at `1151dc4`; a negative source assertion confirms the `switch` form is absent; the worktree is removed and pruned with `git worktree list` compared before and after |
| T-157-27 | Repudiation | restating the survey's stale absolutes as a current measurement | medium | mitigate | Both pairs recorded side by side, the survey pair named with its file provenance and marked superseded, and a source assertion forbidding either survey number from appearing as a current figure |
| T-157-28 | Repudiation | reporting a size delta from a variant that is not behaviourally equivalent | high | mitigate | `pio test -e native` is run in the probe tree and its result recorded; a behavioural difference invalidates the comparison and must be reported rather than absorbed |
| T-157-29 | Repudiation | overgeneralising a single-configuration measurement into a claim about `switch` versus if-chain | low | mitigate | C-22 recorded: one compiler version, one optimisation level, one dispatched value set, at one tree position |
| T-157-30 | Tampering | regenerating the branch-inventory golden when it merely needed verifying | medium | mitigate | Prohibited explicitly; verified by blob-hash equality and a passing gate run; the SUMMARY must distinguish "verified green" from Phase 156's "re-derived in the same commit" |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages and changes no `lib_deps`. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty; HEAD still the `test(157-05):` commit; no new commit created by this plan.
- `git hash-object src/proms/memory.cpp` equals its blob hash at `1151dc4`; `firestarter/CLAUDE.md` byte-unchanged since `1151dc4`.
- No worktree named `157-sw-probe` remains; `git worktree list` matches its pre-task output.
- `pio run -e uno -e uno328pb -e leonardo` green with zero `warning:` lines; `pio test -e native` 184 of 184.
- `pytest tests/test_protocol_branch_inventory.py tests/test_dispatch_mirror.py tests/test_parse_gate_admission.py` in `firestarter_app`: all passed.
</verification>

<success_criteria>
The rejected `switch` alternative is measured on all three AVR targets at Phase 157's own
post-change position, with a behavioural-equivalence check on the variant, and the variant exists
in no commit; the survey's stale absolutes are recorded with their provenance and superseded rather
than restated; the dispatched value set, its density and the single-configuration ceiling are
recorded; the second rejection argument -- that `firestarter/CLAUDE.md` pins the dispatch order,
so a `switch` would require rewriting it -- is recorded with that document proven byte-unchanged;
and the branch-inventory golden plus both neighbouring scanning gates are proven green for their
correct reasons without the golden being touched.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-06-SUMMARY.md` when done.
</output>
