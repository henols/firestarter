---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 05
type: execute
wave: 5
depends_on: ["157-04"]
files_modified:
  - firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp
autonomous: true
requirements: [DECODE-06, DECODE-03]

must_haves:
  truths:
    - "Both read-timing knobs clamp to EXACTLY 1000 microseconds, proven by two executing tests rather than by inspection -- and the `read-strobe-us` half did not exist before this plan."
    - "The equality assertions catch a clamp-to-zero regression that the previous inequality form would have passed, and that has been SEEN: a probe that clamps to zero reddens the equality form while the inequality form is green."
    - "Every one of the eleven table rows now has an executing round-trip test proving it writes the member its `offsetof` names, closing the ceiling that the compile-time guards cannot reach."
    - "A wrong member in one row is LOCALISED: a planted row-member swap reddens exactly the affected round-trip case and leaves the other ten green."
    - "The two copies of `READ_TIMING_MAX_US` -- production and test-local -- are recorded as an unremovable duplicate with the drift risk named, not silently left."
  artifacts:
    - firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp
  key_links:
    - "The table's `clamp` column is the only thing standing between a wire value and `delayMicroseconds()` in the read loop; before this plan only one of the two knobs had any executing proof of it, and that proof was an inequality that a clamp-to-zero regression would pass."
    - "The six round-trip cases plus the three fields already exercised (`read_settling_us`, `read_strobe_us`, `page_size`) plus the two exercised by plan 04 (`protocol`, `ctrl_flags`) cover all eleven rows -- which is exactly what closes ceiling 7, since `_Static_assert` cannot prove a row names the right member."
    - "Zero is the semantically loaded value for both knobs (`read_settling_us == 0` means no settling delay, `read_strobe_us == 0` means use the firmware default of 3 microseconds), which is precisely why the assertion must be equality and not an upper bound."
  prohibitions:
    - "Must NOT remove the test file's local `#define READ_TIMING_MAX_US 1000UL`. The production constant is a file-scope `#define` inside a `.c` translation unit, not a header export, so the test cannot reach it. Keep the duplicate and name the drift risk in a comment (C-8)."
    - "Must NOT weaken either cap assertion back to an upper bound. Zero passes an upper bound and zero is the loaded value for both knobs."
    - "Must NOT use any input value at or above 2^32 in any case (Pitfall 12)."
    - "Must NOT hard-code a numeric struct offset in any test; AVR and native offsets differ at every member from `protocol` down."
    - "Must NOT use a hyphen in the `vpp_mv` wire key. The declared PROGMEM string is the UNDERSCORE form, and a test written against a hyphen would silently never match -- the same trap `key_page_size`'s own comment documents in the opposite direction."
    - "Must NOT create a new test suite or edit `platformio.ini`."
    - "Must NOT edit `src/json_parser.c`, `include/firestarter.h` or `src/proms/memory.cpp`; a case that fails is either a wrong case or a real defect, and a real defect is reported, not absorbed by loosening an assertion."
    - "Must NOT modify the five cases plan 04 landed, nor `setUp`, `tearDown`, `make_handle` or `parse_json`. The ONLY pre-existing case body this plan may change is the `read-settling-delay` cap case, and only to tighten its assertion."
    - "Must NOT re-anchor `scripts/baseline/size_baseline.json` or repair the BASE-01 case-count mismatch to absorb the new case count -- Phase 158 / LAND-01 and LAND-03."
    - "Must NOT attribute a native-suite failure to this change on N=1; re-run on an idle machine first (D-04)."
    - "Must NOT leave a probe worktree, or any commit inside one, behind. Must NOT edit any file under `firestarter_app/`. Must NOT push any branch."
---

<objective>
Add the missing `read-strobe-us` cap case, tighten both cap assertions from an upper bound to an
exact equality, and add one store-round-trip case per otherwise-untested table field -- each with
its RED captured against a planted negative.

Purpose: DECODE-06 asks for the T-44-01 clamp to be proven by a test rather than by inspection,
and only half of it was. A `read-settling-delay` cap case exists; a `read-strobe-us` cap case does
not exist at all, and both would have been written against an upper bound that a clamped-to-zero
regression passes -- with zero being the semantically loaded value for both knobs. Separately, a
wrong `offsetof` in one table row is this refactor's most plausible silent defect, and the
compile-time guards cannot see it: they prove an offset FITS the column, not that it names the
right member. Six round-trip cases close that ceiling for the last six rows (OD-5).

Output: seven new cases and one tightened assertion in
`firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp`, committed inside the
submodule, with the final native case count handed forward to Phase 158.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/157-before-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by **no** test unless case S2 is written. Record it as
   contingent.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that, and only for the fields
   they exercise (today `read_settling_us`, `read_strobe_us`, `page_size`; after this phase also
   `protocol`, `ctrl_flags` and the six round-trip fields OD-5 takes). **This plan is what
   closes it**, and the SUMMARY must say so in those terms -- all eleven rows covered, by
   execution, not by assertion.
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation is a hand-port, and the
    patch is evidence, not a shortcut.

**Plus C-20, from plan 04:** `eeprom28c_page_mask` is `static` in `src/proms/eeprom_28c.cpp` and
unreachable from any test, so case S5's consumer-side half is source-level evidence only.

**Plus C-21, this plan's own residual:** the two `READ_TIMING_MAX_US` definitions -- the hoisted
production `#define` in `src/json_parser.c` and the test-local one in the suite -- are an
**unremovable duplicate**. The production constant is a file-scope `#define` in a `.c` translation
unit, not a header export, so the test cannot reference it. They can drift silently in either
direction and nothing gates that. Recorded, not fixed.

## Artifacts this phase produces

**CREATED by this plan** (all in
`firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp`):
`test_read_strobe_us_capped_at_max`;
`test_memory_size_round_trips_through_the_field_table`;
`test_address_round_trips_through_the_field_table`;
`test_pulse_delay_round_trips_through_the_field_table`;
`test_chip_id_round_trips_through_the_field_table`;
`test_vpp_mv_round_trips_through_the_field_table`;
`test_pin_count_round_trips_through_the_field_table`; plus their seven `RUN_TEST` entries.
**CHANGED by this plan:** the assertion inside the pre-existing
`test_read_settling_us_capped_at_max` tightens from an upper bound to an exact equality, and a
comment is added above the test-local `#define READ_TIMING_MAX_US` recording the duplicate and its
drift risk.

**CREATED by other plans of this phase:** in `src/json_parser.c` -- `field_desc_t`, `FIELD`,
`FIELD_MASK`, `FIELD_WIDTH_MASK`, `FIELD_POLICY_MASK`, `store_field`, twelve `_Static_assert`
lines, the hoisted `#define READ_TIMING_MAX_US`, `#include <stddef.h>`, `#include <string.h>`, the
eleven-row `key_parsers[]` body (plan 02); the narrowed `protocol` and `ctrl_flags` members of
`firestarter_handle_t` in `include/firestarter.h` (plan 03); `extern "C" { #include "memory.h" }`
and the five cases `test_out_of_range_algorithm_saturates_not_truncates`,
`test_out_of_range_algorithm_dispatch_fail_closes`, `test_in_range_algorithm_still_dispatches`,
`test_out_of_range_flags_masks_never_sets_every_flag`,
`test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size` in the same test file
(plan 04); `.planning/v1.33/157-before-figures.md` (plan 01) and
`.planning/v1.33/157-after-figures.md` (plan 07).

**DELETED by plan 02 (all in `src/json_parser.c`), listed so drift verification does not flag
their absence:** the `key_parser_t` typedef and its function-pointer member; the ten stubs and
their forward declarations `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`,
`get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`,
`get_page_size`; the indirect call through the function-pointer column; and the page-size
non-clamp justification comment. **`get_flags` survives** as the deliberate DECODE-02 exception,
along with `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`. **`include/json_parser.h`
is NOT edited.**

<tasks>

<task type="auto">
  <name>Task 1: Author the strobe cap case, tighten both cap assertions, and prove both against a planted clamp break</name>
  <files>
    (authoring happens inside a throwaway git worktree; nothing in /workspaces/firestarter is
    edited by this task, and the finished bodies are carried forward to task 3)
  </files>
  <read_first>
    - `firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp` -- the current file as plan 04 left it: the include block, the local `#define READ_TIMING_MAX_US` at the top, `setUp` / `tearDown`, `make_handle` / `parse_json`, the existing `read-settling-delay` cap case with its upper-bound assertion, the existing `read-strobe-us` parse case (which proves the key parses but says nothing about the cap), and the `RUN_TEST` block
    - `firestarter/src/json_parser.c` -- the hoisted `#define READ_TIMING_MAX_US` and its comment block, the `key_read_settling` and `key_read_strobe` rows with their `clamp` arguments, and `store_field`'s clamp step, so the probe breaks exactly the right thing
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §DECODE-06 in full and correction C-8 (all three gaps: no strobe test exists, the assertion is an upper bound not an equality, and the suite re-defines the constant locally)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md` -- §6a (the existing cap assertion verbatim, and case T7 as the message-bearing model) and §Shared Patterns
  </read_first>
  <action>
Assert `git status --porcelain` is empty in `/workspaces/firestarter` and HEAD's subject begins
`test(157-04):`.

STEP 1 -- the probe worktree. `git worktree add --detach /tmp/157-c-probe/firestarter HEAD`. The
leaf directory name **must be exactly `firestarter`**
(`firestarter_app/tests/test_checker_convention.py::test_scope_is_firmware_only` hard-codes it).
Record `git worktree list` before and after.

STEP 2 -- in the probe tree, author `test_read_strobe_us_capped_at_max`: parse
`{"cmd":1,"read-strobe-us":9999}` with `CMD_READ`, assert `parse_json` returned 0, and assert
`h.read_strobe_us` is EQUAL to `READ_TIMING_MAX_US`. Use the message-bearing equality form and
state in the message that this cap is T-44-01's mitigation -- an absurd JSON value must not reach
`delayMicroseconds()` in the read loop -- and that the assertion is an equality rather than an
upper bound because zero passes an upper bound and zero is this knob's loaded value ("use the
firmware default of 3 microseconds").

STEP 3 -- in the probe tree, tighten the pre-existing `read-settling-delay` cap case: replace its
upper-bound assertion with the same message-bearing equality against `READ_TIMING_MAX_US`. Change
nothing else about that case -- its name, its input `9999`, and its `parse_json` return assertion
all stay. Extend its message with the same zero-is-loaded reasoning ("no settling delay" is the
explicit test point for this knob).

STEP 4 -- add the drift note. Above the test-local `#define READ_TIMING_MAX_US 1000UL`, add a
comment recording: the production constant is now hoisted above the field table in
`src/json_parser.c`; it is a file-scope `#define` in a `.c` translation unit and not a header
export, so this test cannot reference it; the two copies can therefore drift silently in either
direction and nothing gates that; and the duplicate is kept deliberately rather than removed
(C-8, C-21). Do not delete the local definition.

STEP 5 -- probe C1: the clamp column removed. In the probe tree, set the `clamp` argument of BOTH
read-timing rows to 0 -- which means "no clamp". Run `pio test -e native -f "*test_read_timing*"`
and capture the output verbatim. Required outcome: **both cap cases FAIL**, each reporting the
observed value `9999`. This proves both cases see the clamp column at all.

STEP 6 -- probe C2: the clamp-to-zero regression, which is the whole reason for the equality. In
the probe tree, restore the rows and instead break `store_field`'s clamp step so that an
over-limit value is stored as **0** rather than as the clamp value. Run
`pio test -e native -f "*test_read_timing*"` and capture the output verbatim. Required outcome:
**both cap cases FAIL with the observed value 0.** Then, in the same probe tree, temporarily
restore ONE of the two assertions to its old upper-bound form, re-run, and capture that it
**PASSES** against the same break. That contrast -- equality RED, upper bound GREEN, same broken
tree -- is the measured evidence that the tightening added coverage rather than restyling an
assertion. Restore the equality form before leaving the probe.

STEP 7 -- discard and prove nothing landed. `git checkout -- .` in the probe tree, then
`git worktree remove --force /tmp/157-c-probe/firestarter` and `git worktree prune`. Assert `git
worktree list` in `/workspaces/firestarter` matches its pre-probe output, `git status --porcelain`
there is empty, `git branch --list` gained nothing, and `git rev-list --count HEAD` matches the
value recorded at task start -- record both numbers rather than comparing `HEAD` against `HEAD~1`,
which is vacuously 1. Carry the finished case body, the tightened assertion and the drift comment
forward to task 3.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | grep -c 157-c-probe)" = "0" && test ! -d /tmp/157-c-probe/firestarter && test "$(git log --oneline -1 --format=%s | grep -c '^test(157-04):')" = "1" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "14" && pio test -e native 2>&1 | grep -q '177 test cases: 177 succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - The probe worktree's leaf directory was named exactly `firestarter`, and `git worktree list` in `/workspaces/firestarter` is byte-identical before and after this task.
    - Probe C1 (both read-timing `clamp` arguments set to 0) produces a captured transcript in which BOTH cap cases FAIL, each reporting the observed value `9999`.
    - Probe C2 (the clamp step made to store 0) produces a captured transcript in which BOTH cap cases FAIL reporting the observed value `0`, AND a second captured transcript from the same broken tree in which the temporarily-restored upper-bound form PASSES. Both transcripts are transcribed verbatim.
    - The SUMMARY states the equality-RED / upper-bound-GREEN contrast on the same broken tree as the measured justification for the tightening, and names zero as the loaded value for both knobs with each knob's meaning given.
    - The authored strobe case parses the exact input `{"cmd":1,"read-strobe-us":9999}` and asserts an equality against `READ_TIMING_MAX_US`, in the message-bearing form.
    - The pre-existing settling case keeps its name, its `9999` input and its `parse_json` return assertion; only its comparison changed.
    - A comment above the test-local `#define READ_TIMING_MAX_US` records the unremovable duplicate and the drift risk, and the local definition is still present.
    - `/tmp/157-c-probe/firestarter` does not exist when the task ends; `git worktree prune` has been run; `git branch --list` gained no entry; `git rev-list --count HEAD` is recorded at task start and end and the two numbers are equal.
    - `/workspaces/firestarter` is untouched: `git status --porcelain` empty, HEAD still the `test(157-04):` commit, the test file still at 14 `RUN_TEST` entries, and `pio test -e native` still `177 test cases: 177 succeeded`.
  </acceptance_criteria>
  <done>The missing strobe cap case exists, both cap assertions are equalities, both have been seen RED against two distinct clamp breaks, and the equality-versus-upper-bound contrast on the clamp-to-zero break is captured as the evidence that the tightening added real coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Author the six store-round-trip cases and prove a planted row-member swap localises to one case</name>
  <files>
    (authoring happens inside a throwaway git worktree; nothing in /workspaces/firestarter is
    edited by this task, and the finished bodies are carried forward to task 3)
  </files>
  <read_first>
    - `firestarter/src/json_parser.c` -- the eleven `const char key_*[] PROGMEM` declarations with their EXACT wire strings (note `vpp_mv` uses an UNDERSCORE while the other multi-word keys use hyphens), the eleven `key_parsers[]` rows and which member each names, and `json_parse`'s reset block -- which zeroes `address`, `ctrl_flags`, `chip_id` and `page_size` per command and deliberately does NOT zero `protocol`, `pins`, `mem_size`, `pulse_delay` or the two read-timing knobs
    - `firestarter/include/firestarter.h` -- `firestarter_handle_t` at `:205-232`: each of the eleven members' types, so each round-trip value fits and each assertion uses the right Unity width
    - `firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp` -- `make_handle` (which zero-initialises the whole handle, so every non-target table member starts at 0) and case T7's message-bearing style
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- OQ-6 and the §Validation Architecture ceiling 9 entry (a wrong `offsetof` in one row is this refactor's most plausible silent defect, and only three of eleven fields had any native parse test)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- the OD-5 Wave-0 item and the DECODE-03 rows of the Per-Task Verification Map
  </read_first>
  <action>
STEP 1 -- the probe worktree, again. `git worktree add --detach /tmp/157-d-probe/firestarter HEAD`,
leaf directory named exactly `firestarter`. Record `git worktree list` before and after.

STEP 2 -- author six cases in the probe tree, one per table field that no test exercises yet.
Each case parses a single key on a freshly built handle and then makes TWO kinds of assertion:
the target member equals the value, AND every other one of the eleven table-written members is
still 0. The second half is the actual offset oracle -- a row whose `offsetof` names the wrong
member writes into a neighbour, and only a neighbour assertion catches that. `make_handle`
zero-initialises the whole handle, so the neighbour assertions are well-founded.

  - `test_memory_size_round_trips_through_the_field_table` -- key `memory-size`, value 65536,
    target `mem_size`.
  - `test_address_round_trips_through_the_field_table` -- key `address`, value 4096, target
    `address`.
  - `test_pulse_delay_round_trips_through_the_field_table` -- key `pulse-delay`, value 1000,
    target `pulse_delay`.
  - `test_chip_id_round_trips_through_the_field_table` -- key `chip-id`, value 4660, target
    `chip_id`.
  - `test_vpp_mv_round_trips_through_the_field_table` -- key `vpp_mv` (**underscore**, as
    declared; a hyphen would silently never match), value 12000, target `vpp_mv`.
  - `test_pin_count_round_trips_through_the_field_table` -- key `pin-count`, value 28, target
    `pins`.

Use `CMD_READ` or `CMD_WRITE` as appropriate and keep every value below 2^32 and within the
target member's range, so the case tests the offset and width rather than the saturation path
plan 04 already covers. Use the Unity assertion width that matches each member's type. Each
message must state that the case exists to catch a wrong `offsetof` in that row, and that the
compile-time guards cannot see it because they prove an offset fits the column, not that it names
the right member.

STEP 3 -- probe D: plant a wrong member in ONE row. In the probe tree, change the `key_vpp_mv`
row so its member argument names `chip_id` instead of `vpp_mv` -- a plausible transcription slip
between two adjacent 16-bit members, and exactly the class of defect these cases exist for. Run
`pio test -e native -f "*test_read_timing*"` and capture the output verbatim. Required outcome:
`test_vpp_mv_round_trips_through_the_field_table` FAILS -- and it must fail on BOTH halves, the
target member left at 0 and the neighbour `chip_id` carrying the value -- while the other five
round-trip cases, the two cap cases and every case plan 04 landed all pass. That localisation is
the point: a wrong row reddens its own case and nothing else.

STEP 4 -- probe D confirmation on a second row, to show the neighbour half is not incidental.
Restore the tree, then change the `key_pin_count` row's member argument to name `page_size`. Run
the suite again and capture: `test_pin_count_round_trips_through_the_field_table` must FAIL, and
so must plan 04's page-size saturation case if the width change makes it observable -- record
whichever cases move, exactly as observed, without predicting them into the record.

STEP 5 -- discard and prove nothing landed. `git checkout -- .`,
`git worktree remove --force /tmp/157-d-probe/firestarter`, `git worktree prune`. Assert `git
worktree list` matches its pre-probe output, `git status --porcelain` in
`/workspaces/firestarter` is empty, `git branch --list` gained nothing, and
`git rev-list --count HEAD` matches the value recorded at task start. Carry the six finished
bodies forward to task 3.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | grep -c 157-d-probe)" = "0" && test ! -d /tmp/157-d-probe/firestarter && test "$(git log --oneline -1 --format=%s | grep -c '^test(157-04):')" = "1" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "14" && grep -q 'PROGMEM = "vpp_mv"' src/json_parser.c && pio test -e native 2>&1 | grep -q '177 test cases: 177 succeeded'</automated>
  </verify>
  <acceptance_criteria>
    - Six cases are authored with the exact names listed in the action, each parsing exactly one wire key.
    - The `vpp_mv` case's JSON key is the UNDERSCORE form, matching `const char key_vpp_mv[] PROGMEM = "vpp_mv";`. Source assertion: no authored case body contains a hyphenated spelling of that key.
    - Each case asserts BOTH the target member's value AND that every other one of the eleven table-written members is still 0, using the Unity assertion width matching each member's declared type.
    - Every input value is below 2^32 and within its target member's range, so no case exercises the saturation path.
    - Probe D on the `key_vpp_mv` row produces a captured transcript in which `test_vpp_mv_round_trips_through_the_field_table` FAILS on both halves -- target left at 0 and `chip_id` carrying 12000 -- while the other five round-trip cases, both cap cases and all five of plan 04's cases pass. A localisation failure (more than the affected case reddening, other than as recorded in step 4) is a FAILED task and must be investigated, not narrated away.
    - Probe D on the `key_pin_count` row produces a second captured transcript recording exactly which cases moved, as observed, with no predicted-but-unobserved case listed.
    - Both transcripts are transcribed verbatim into the SUMMARY.
    - `/tmp/157-d-probe/firestarter` does not exist when the task ends; `git worktree prune` has been run; `git branch --list` gained no entry; `git rev-list --count HEAD` is recorded at task start and end and the two numbers are equal.
    - `/workspaces/firestarter` is untouched: `git status --porcelain` empty, HEAD still the `test(157-04):` commit, the test file still at 14 `RUN_TEST` entries.
  </acceptance_criteria>
  <done>Six round-trip cases exist, each asserting its own member AND the ten neighbours, and a planted wrong-member row has been seen to redden exactly the affected case -- which is the localisation property that makes them an offset oracle rather than a value check.</done>
</task>

<task type="auto">
  <name>Task 3: Land the seven cases and the tightening green, and hand the final case count to Phase 158</name>
  <files>firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp</files>
  <read_first>
    - `firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp` -- the current file, so the seven cases and the one tightened assertion land without disturbing anything else
    - `.planning/v1.33/157-before-figures.md` -- §5 (the baselines: `test_read_timing` at 9 cases, both native envs at 172) and §8 (the one-sided size gate and the pre-existing BASE-01 red)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-04-SUMMARY.md` -- the 172-to-177 handoff this plan extends, so the final number is stated once and consistently
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- correction C-15 and §Common Pitfalls 6, 7 and 10
  </read_first>
  <action>
Work in `/workspaces/firestarter`. Edit exactly one file:
`test/native/avr/test_read_timing/test_read_timing_params.cpp`.

STEP 1 -- land, from tasks 1 and 2: the strobe cap case, the tightened settling assertion, the
drift comment above the test-local `#define`, and the six round-trip cases, with their seven new
`RUN_TEST` entries appended to `main` in declaration order. Change nothing else: `setUp`,
`tearDown`, `make_handle`, `parse_json`, plan 04's five cases and the eight untouched pre-existing
cases all stay byte-identical.

STEP 2 -- run the suite and both envs. `pio test -e native -f "*test_read_timing*"` must report
**21 cases, 21 succeeded**. Then `pio test -e native` and `pio test -e native_nodevtools` must each
report **184 cases, 184 succeeded** over 17 suites -- the two envs must stay in lockstep, and the
suite count must still be 17 because no new suite was created. If any leg fails, re-run on an idle
machine before attributing it to this change (D-04). Trust the runner's count, never
`grep -c "RUN_TEST("`: the lexical count across the seventeen filtered suites is one higher than
the executed count (Pitfall 7).

STEP 3 -- confirm the AVR image did not move. `pio run -e uno`, `pio run -e uno328pb` and
`pio run -e leonardo` must all still report the SAME flash and RAM figures plan 03 recorded, with
zero `warning:` lines, because native test cases cannot change an AVR image. Record that identity
explicitly as the cheapest available proof that this plan added no production code. Then run
`python3 scripts/check_build_warnings.py` and `python3 scripts/check_no_heap_or_64bit_symbols.py`
and record both exit codes (OD-6).

STEP 4 -- the final case-count handoff. Run
`python3 scripts/check_size_baseline.py --policy merge05 --baseline
scripts/baseline/size_baseline_base01.json --rebuild` and record its output verbatim. The two
native `cases` lines now read `observed=184`; the leg was already RED against BASE-01's frozen 141
and remains RED, and still **no** AVR flash or RAM leg fails. Record, as the **final** handoff to
**Phase 158 / LAND-01**: the native case count moved 172 -> 177 (plan 04) -> **184** (this plan) on
BOTH `native` and `native_nodevtools`; the suite count is unchanged at 17;
`scripts/baseline/size_baseline.json` records 172 today and LAND-01 owns the re-record; and
BASE-01's 141 is LAND-03's, frozen at Phase 124. **Do not edit either baseline file.**

STEP 5 -- commit inside the submodule. Stage exactly
`test/native/avr/test_read_timing/test_read_timing_params.cpp`. Subject:
`test(157-05): cap read-strobe-us, tighten both cap assertions, round-trip every table row`. The
body must state: that a `read-strobe-us` cap test did not exist before this commit (C-8); that both
cap assertions are now equalities and why (zero passes an upper bound and zero is the loaded value
for both knobs), with the equality-RED / upper-bound-GREEN contrast from probe C2 cited as the
evidence; that all eleven table rows now have an executing round-trip test, which is what closes
ceiling 7; that a planted wrong-member row was seen to redden exactly its own case; that the two
`READ_TIMING_MAX_US` copies are an unremovable duplicate with a named drift risk (C-21); and the
final 184 case count as the LAND-01 handoff. Write no planning-provenance token into the test file.
Do not push.

STEP 6 -- the host gate, on the now-committed tree. In `/workspaces/firestarter_app`, run
`python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o
addopts=""` and expect **24 passed**. Delete any `.rej` or `.orig` file first (Pitfall 5).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "21" && for t in test_read_strobe_us_capped_at_max test_memory_size_round_trips_through_the_field_table test_address_round_trips_through_the_field_table test_pulse_delay_round_trips_through_the_field_table test_chip_id_round_trips_through_the_field_table test_vpp_mv_round_trips_through_the_field_table test_pin_count_round_trips_through_the_field_table; do test "$(grep -c "$t" test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "2" || { echo "MISSING $t"; exit 1; }; done && test "$(grep -c 'read_settling_us <= READ_TIMING_MAX_US' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "0" && test "$(grep -c '#define READ_TIMING_MAX_US 1000UL' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "1" && test "$(grep -c 'vpp-mv' test/native/avr/test_read_timing/test_read_timing_params.cpp)" = "0" && pio test -e native -f "*test_read_timing*" 2>&1 | grep -q '21 test cases: 21 succeeded' && pio test -e native 2>&1 | grep -q '184 test cases: 184 succeeded' && pio test -e native_nodevtools 2>&1 | grep -q '184 test cases: 184 succeeded' && pio run -e uno -e uno328pb -e leonardo > /tmp/157-05-build.log 2>&1 && test "$(grep -c 'warning:' /tmp/157-05-build.log)" = "0" && grep -q 'used 1562 bytes' /tmp/157-05-build.log && grep -q 'used 2003 bytes' /tmp/157-05-build.log && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && test "$(git log --oneline -1 --format=%s | grep -c '^test(157-05):')" = "1" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" = "1" && cd /workspaces/firestarter_app && python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o addopts="" 2>&1 | grep -q '24 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RUN_TEST(' test/native/avr/test_read_timing/test_read_timing_params.cpp` prints `21`, and each of the seven new case names appears exactly twice in the file.
    - The settling cap case's upper-bound comparison against the constant produces zero matches, and both cap cases assert an equality against `READ_TIMING_MAX_US`.
    - The test-local `#define READ_TIMING_MAX_US 1000UL` is still present exactly once, with the drift-risk comment above it.
    - The file contains no hyphenated spelling of the `vpp_mv` wire key.
    - `pio test -e native -f "*test_read_timing*"` reports `21 test cases: 21 succeeded`; `pio test -e native` and `pio test -e native_nodevtools` each report `184 test cases: 184 succeeded` over `17` suites.
    - `setUp`, `tearDown`, `make_handle`, `parse_json`, plan 04's five cases and the eight untouched pre-existing cases are byte-identical to their pre-plan state. Source assertion: `git diff HEAD~1 -- test/native/avr/test_read_timing/test_read_timing_params.cpp` shows deletions only inside the settling cap case's assertion.
    - `pio run -e uno -e uno328pb -e leonardo` exits 0 with zero `warning:` lines and the SAME flash and RAM figures plan 03 recorded (RAM `1562` / `1568` / `2003`), and the SUMMARY states that identity as the proof that this plan added no production code.
    - `python3 scripts/check_build_warnings.py` and `python3 scripts/check_no_heap_or_64bit_symbols.py` both exit 0 (OD-6).
    - The `check_size_baseline.py --policy merge05` output is captured verbatim, its two native `cases` lines read `observed=184`, and no flash or RAM leg line appears. Neither baseline file was edited.
    - The SUMMARY records the full case-count trajectory `172 -> 177 -> 184` on both native envs, the suite count unchanged at `17`, and names LAND-01 as the owner of the `size_baseline.json` re-record and LAND-03 as the owner of BASE-01's frozen `141`.
    - The SUMMARY carries all four probe transcripts from tasks 1 and 2, states ceiling 7 as CLOSED with all eleven rows covered by execution, and records C-21.
    - `git -C firestarter log --oneline -1` subject begins `test(157-05):`, `git show --stat HEAD` lists exactly one path, `git status --porcelain` is empty afterwards, and no `.rej` or `.orig` file exists.
    - `python3 -m pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py -q -o addopts=""` in `firestarter_app` reports `24 passed`, run on the committed firmware tree, with zero host files changed.
  </acceptance_criteria>
  <done>Both read-timing knobs are capped by executing equality assertions, the missing strobe case exists, all eleven table rows have a round-trip test that localises a wrong member to its own case, the AVR image is unchanged from plan 03's on all three targets, and the final native case count of 184 is handed to Phase 158 rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | A wire-supplied timing value reaches `delayMicroseconds()` in the read loop; the table's `clamp` column is the only bound on it |
| table row -> handle member | A row's `offsetof` decides which member a wire value lands in; a wrong member is a silent write to a neighbour that compiles cleanly and passes every compile-time guard |
| assertion form -> coverage | An upper-bound assertion and an equality assertion look alike and differ exactly on the value a regression is most likely to produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-04 | Denial of Service | wire-supplied `read-settling-delay` / `read-strobe-us` reaching `delayMicroseconds()` | high | mitigate | Both knobs now have an executing cap case asserting EXACTLY 1000, and both were seen RED against a removed clamp column and against a clamp-to-zero break. Before this plan the strobe knob had no cap test at all (C-8) |
| T-157-23 | Tampering | a clamp regression that stores 0 instead of the cap, passing an upper-bound assertion | high | mitigate | Both assertions tightened to equality, with the equality-RED / upper-bound-GREEN contrast captured on the same broken tree. Zero is the loaded value for both knobs, which is why the weaker form was dangerous rather than merely loose |
| T-157-24 | Tampering | a wrong member in one table row silently writing a wire value into a neighbouring handle field | high | mitigate | Six round-trip cases, each asserting its target AND all ten neighbours still zero; a planted wrong-member row seen to redden exactly its own case. This closes ceiling 7, which the compile-time guards cannot reach |
| T-157-25 | Repudiation | the two `READ_TIMING_MAX_US` copies drifting silently | low | accept | Unremovable: the production constant is a file-scope `#define` in a `.c` TU, not a header export. Recorded as C-21 with the drift risk named in a comment at the duplicate site, not fixed |
| T-157-22 | Tampering | absorbing the native case-count movement by editing a baseline file | medium | mitigate | Both baseline files prohibited from edit; the full `172 -> 177 -> 184` trajectory recorded as a handoff to LAND-01 with LAND-03 named as the owner of BASE-01's frozen 141 |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages and changes no `lib_deps`. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty; HEAD subject begins `test(157-05):`; exactly one path in the commit.
- `pio test -e native -f "*test_read_timing*"`: 21 of 21. `pio test -e native` and `pio test -e native_nodevtools`: 184 of 184 each, 17 suites.
- `pio run -e uno -e uno328pb -e leonardo` green, zero `warning:` lines, RAM 1562 / 1568 / 2003 -- identical to plan 03's figures.
- `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` exit 0.
- No worktree named `157-c-probe` or `157-d-probe` remains; `git worktree list` matches its pre-task output.
- `pytest tests/test_json_key_parity.py tests/test_revision_constants_parity.py` in `firestarter_app`: 24 passed.
</verification>

<success_criteria>
The `read-strobe-us` cap has an executing test for the first time; both cap assertions are exact
equalities, each seen RED against a removed clamp and against a clamp-to-zero break, with the
upper-bound form seen GREEN on that same break as the measured justification; all eleven field
table rows have an executing round-trip test that asserts its own member and all ten neighbours,
and a planted wrong-member row reddens exactly its own case; the AVR image is unchanged from plan
03's on all three targets; and the final native case count of 184 on both envs is handed forward
to Phase 158 rather than absorbed into a baseline.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-05-SUMMARY.md` when done.
</output>
