---
phase: 157-command-decode-table-handle-type-narrowing-firmware-only
plan: 07
type: execute
wave: 7
depends_on: ["157-06"]
files_modified:
  - .planning/v1.33/157-after-figures.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DECODE-01, DECODE-02, DECODE-03, DECODE-04, DECODE-05, DECODE-06, DECODE-07]

must_haves:
  truths:
    - "Every phase-gate leg is run on the final committed tree and recorded with its verbatim output -- including the three legs no CI workflow invokes."
    - "The headline size figures are COLD on both sides of the comparison: the pre-phase position is cold-rebuilt in a throwaway worktree so the delta is not a warm-to-cold mixture."
    - "The record supersedes the ROADMAP and REQUIREMENTS prose wherever they disagree, and every correction C-1 through C-22 is closed out with the value that replaces it."
    - "DECODE-07 -- the phase's one requirement that changes no code -- is discharged by a named section of the record, with a first-party measurement and the superseded absolutes both present."
    - "All seven decisions OD-1 through OD-7 are recorded with the alternative each declined and that alternative's cost, including the identifier that was deliberately left slightly stale."
    - "The 999.35 / v1.28 non-additivity warning is stated in the record itself, because a reader of the figures file will not necessarily reach the backlog entry that carries it."
    - "Phase 158 receives the native case-count movement, the cold-versus-warm status of every figure, and the two baseline facts it owns, as explicit handoffs rather than as things to discover."
  artifacts:
    - .planning/v1.33/157-after-figures.md
  key_links:
    - "The cold-rebuilt pre-phase figures are what make the composed delta a like-for-like comparison; without them the phase's headline number is a warm-before against a cold-after."
    - "The after-record is the ONLY artifact that discharges DECODE-07, and the only place DECODE-01's and DECODE-02's measurements are recorded at all -- neither has an automated gate (ceiling 4)."
    - "The ROADMAP's `**Measured**` line for Phase 157 carries the superseded -976 / -172 split; leaving it is how the wrong split gets re-inherited by the next reader."
  prohibitions:
    - "Must NOT use `Write` on `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`. Use scoped `Edit` replacements only -- a whole-file write destroys every phase and requirement entry outside the diff window."
    - "Must NOT use the GSD requirements or roadmap mutation verbs on these two files; they reformat the whole document. Hand-scoped edits only."
    - "Must NOT re-anchor `scripts/baseline/size_baseline.json`, and must NOT repair the BASE-01 native case-count mismatch. Both are Phase 158's (LAND-01, LAND-03) and both are handed forward, not fixed."
    - "Must NOT author any MERGE-05 exemption constant. The gate is growth-only, so a reduction passes with none, and the pass is recorded as ONE-SIDED (D-03)."
    - "Must NOT run the Phase 154 citation remap tool or repair any `.planning/` file:LINE citation. Every citation this record writes is measured against the CURRENT tree and will be remapped once, by Phase 159 (D-01, D-05)."
    - "Must NOT borrow Phase 155's coverage-gap phrasing. Ceiling 1 is a NEGATIVE ceiling: `src/json_parser.c` IS inside the native build, so this phase has no gap of that kind, and writing one would be a false ceiling."
    - "Must NOT restate the superseded figures as current: -976 / -172, 19, 45, `ten of eleven`, 3440 B, 86-110 B, 25696 / 25678 (C-2, C-3, C-4, C-5, C-10, C-13)."
    - "Must NOT pin a `.constprop.NN` suffix anywhere in the record (Pitfall 13)."
    - "Must NOT count ELF strings with an exact-match filter or a substring `grep -c`; the DECODE-02 oracle is an offset-resolved block comparison (C-3)."
    - "Must NOT describe DECODE-01 or DECODE-02 as gated. They are recorded measurements with no automated check (ceiling 4)."
    - "Must NOT claim any bench or hardware verification (D-02); must NOT propose any step toward the binary command protocol (999.35)."
    - "Must NOT edit any file under `firestarter/` or `firestarter_app/`. This plan measures, records and updates `.planning/` only."
    - "Must NOT stage or re-pin the `firestarter_app` gitlink in the meta repo -- pre-existing Phase 154 drift, operator-gated."
    - "Must NOT mark the phase complete in STATE.md or ROADMAP by any automated verb that rewrites unrelated phases' `**Plans:**` lines; snapshot and diff ROADMAP.md before and after any edit."
    - "Must NOT leave a probe worktree, or any commit inside one, behind. Must NOT push any branch."
---

<objective>
Run every phase-gate leg on the final committed tree, write the after-figures record, and close out
the ROADMAP and REQUIREMENTS prose this phase supersedes.

Purpose: three of this phase's legs run in no CI workflow, so a green CI run is not evidence they
passed. Two of its seven requirements have no automated gate at all -- DECODE-01's stub ledger and
DECODE-02's single-key-storage are measurements, and this record is the only place they exist.
DECODE-07 changes no code and this record is the only thing that discharges it. And nineteen
figures in the ROADMAP and REQUIREMENTS are wrong; if the record does not say it supersedes them,
the next reader inherits them from the ROADMAP alone.

Output: `.planning/v1.33/157-after-figures.md`, plus scoped corrections to `.planning/ROADMAP.md`
§Phase 157 and `.planning/REQUIREMENTS.md` §4, committed in the meta repo.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md
@.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md
@.planning/v1.33/157-before-figures.md
@.planning/v1.33/156-after-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All ten appear in every plan of this phase, in every SUMMARY, and in the phase record.

1. **`src/json_parser.c` IS natively covered** (F-3) -- `build_src_filter` includes
   `+<json_parser.c>`, and `test_read_timing` already drives `json_parse` against a real
   `jsmn_parse`. Every behavioural criterion in this phase is reachable by a native test **that
   CI runs**. **This phase has no coverage gap of Phase 155's kind, and its record must NOT
   borrow that phrasing.** This is the opposite of `rurp_common.cpp`'s situation and saying
   otherwise would be a false ceiling.
2. **`src/firestarter.cpp` and `src/eprom_operations.cpp` are OUTSIDE the native `src_filter`.**
   Between them they hold 8 of the 40 `is_flag_set` uses and the `eprom_block_budget_s` call.
   The narrowing's effect there is proven **only by compilation**, never by execution.
3. **`src/dev_tools.cpp` is outside too** -- 9 `is_flag_set` uses plus 7 `LOG_INFO_ID*`
   expansions, the single largest concentration. Compile-only coverage.
4. **DECODE-01 and DECODE-02 have NO automated gate.** They are measurements recorded in
   `157-after-figures.md`. No test asserts that the ten deleted stubs stayed deleted or that a
   key is stored once. A future phase could silently reintroduce either. **Do not describe them
   as gated.**
5. **The -5 B RAM saving is unobservable natively** (`sizeof` is 655 either way). AVR-only.
6. **Saturation-as-fail-closed is CONTINGENT on `0xFF` being unmapped** in `configure_memory`'s
   dispatch chain. That is a property of the **dispatch table**, not of `store_field`, it is
   true only today, and it is pinned by case S2 and by nothing else.
7. **`_Static_assert` proves the offsets fit `uint8_t` at build time -- it does NOT prove the
   table writes the right member.** Only the native parse tests do that; plan 05 closed this for
   all eleven rows, and the record must say the closure came from execution, not from assertion.
8. **No bench coverage, by design** (D-02). No criterion needs silicon; nothing here is claimed
   of real hardware.
9. **`check_size_baseline.py` and `check_build_warnings.py` are in NO CI workflow.** Every gate
   in this phase beyond the four CI legs (`pio test -e native`, `pio test -e native_nodevtools`,
   `pytest tests/`, `pio run`) is a **local-run obligation**. A green CI run is not evidence
   that the size gate passed.
10. **The reference patch does not apply cleanly** (C-11): hunk #3 fails at every `-C` level
    because Phase 154's sweep changed its context. The implementation was a hand-port, and the
    patch is evidence, not a shortcut.

**Plus the three ceilings the phase's own plans added:** C-20 (`eeprom28c_page_mask` is `static`
and unreachable from any test, so case S5's consumer half is source-level evidence only); C-21 (the
two `READ_TIMING_MAX_US` definitions are an unremovable duplicate with an ungated drift risk); and
C-22 (the `switch` delta is a single-configuration measurement -- one compiler version, one
optimisation level, one dispatched value set, one tree position).

## Artifacts this phase produces

**CREATED by this plan:** `.planning/v1.33/157-after-figures.md`. **CHANGED by this plan:** scoped
edits to `.planning/ROADMAP.md` §Phase 157 (the `**Measured**` line's superseded split, the
`**Plans:**` count and list, the seven plan checkboxes) and `.planning/REQUIREMENTS.md` §4
(DECODE-01 through DECODE-07 checkboxes and correction notes) plus its traceability rows.

**CREATED by earlier plans of this phase, in `firestarter/src/json_parser.c`:** `field_desc_t`;
`FIELD(k, member, cl)`; `FIELD_MASK(k, member)`; `FIELD_WIDTH_MASK`; `FIELD_POLICY_MASK`;
`store_field(firestarter_handle_t*, const field_desc_t*, uint32_t)`; twelve `_Static_assert` lines;
the hoisted `#define READ_TIMING_MAX_US 1000UL`; `#include <stddef.h>`; `#include <string.h>`; and
the eleven-row replacement body of `key_parsers[]` (identifier KEPT per OD-2). **In
`firestarter/include/firestarter.h`:** `protocol` narrowed to `uint8_t` and `ctrl_flags` narrowed
to `uint16_t`. **In
`firestarter/test/native/avr/test_read_timing/test_read_timing_params.cpp`:**
`extern "C" { #include "memory.h" }`; `test_out_of_range_algorithm_saturates_not_truncates`;
`test_out_of_range_algorithm_dispatch_fail_closes`; `test_in_range_algorithm_still_dispatches`;
`test_out_of_range_flags_masks_never_sets_every_flag`;
`test_out_of_range_page_size_saturates_not_truncates_to_a_valid_size`;
`test_read_strobe_us_capped_at_max`;
`test_memory_size_round_trips_through_the_field_table`;
`test_address_round_trips_through_the_field_table`;
`test_pulse_delay_round_trips_through_the_field_table`;
`test_chip_id_round_trips_through_the_field_table`;
`test_vpp_mv_round_trips_through_the_field_table`;
`test_pin_count_round_trips_through_the_field_table`; a tightened assertion inside the pre-existing
`test_read_settling_us_capped_at_max`; and a drift-risk comment above the test-local
`#define READ_TIMING_MAX_US`. **Records:** `.planning/v1.33/157-before-figures.md` (plan 01).

**DELETED by plan 02 (all in `firestarter/src/json_parser.c`), listed so drift verification does
not flag their absence:** the `key_parser_t` typedef and its function-pointer member; the ten stubs
and their forward declarations `get_memory_size`, `get_address`, `get_chip_id`, `get_pin_count`,
`get_delay`, `get_vpp_mv`, `get_algorithm`, `get_read_settling`, `get_read_strobe`,
`get_page_size`; the indirect call through the function-pointer column; and the page-size
non-clamp justification comment. **`get_flags` survives** as the deliberate DECODE-02 exception,
with its two call sites in two DIFFERENT functions (`json_parse_config` and `json_get_cmd`, C-1),
alongside the five zero-cost siblings `get_r1`, `get_r2`, `get_rev`, `get_rw_pin`, `get_vpp_pin`.
**`include/json_parser.h` and `src/proms/memory.cpp` are edited by no plan in this phase.**

<tasks>

<task type="auto">
  <name>Task 1: Run every phase-gate leg on the final tree, cold on both sides of the comparison</name>
  <files>
    (no tracked file is edited -- this task measures; the cold pre-phase build happens in a
    throwaway git worktree that is removed before the task ends)
  </files>
  <read_first>
    - `.planning/v1.33/157-before-figures.md` -- every section: the WARM before pairs, the eleven-stub ledger, the two key-string blocks, the struct offsets and the OD-7 `sizeof` figure with its captured command line, the test and gate baselines, the reference-carrier result, the one-sided gate, the ten ceilings, the corrections index C-1..C-19, and the OD-1..OD-7 section
    - all five prior SUMMARY files in `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/` (`157-01` through `157-06`) -- every measured figure, every probe transcript, and the corrections each added (C-20, C-21, C-22)
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- §Sampling Rate's phase-gate leg list, and the Per-Task Verification Map
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-RESEARCH.md` -- §Code Examples (the verbatim `avr-nm`, `strings` and size-gate invocations), §Gate blast radius (both repos, exhaustively), and §Common Pitfalls 5, 7, 9, 10, 13, 14, 15
    - `firestarter/scripts/check_size_baseline.py` -- lines `697` and `709`, to quote the one-sidedness from source rather than from a paraphrase
  </read_first>
  <action>
Assert `git -C firestarter status --porcelain` is empty and its HEAD subject begins
`test(157-05):` -- plan 06 created no commit. Record the final firmware sha as `FW_POST_SHA` and
the meta repo HEAD.

STEP 1 -- the cold before side. The before-record's figures are WARM; a record whose headline delta
compares a warm before against a cold after is a mixed-convention number.
`git worktree add --detach /tmp/157-cold-probe/firestarter 1151dc4` -- leaf directory named exactly
`firestarter`. In that worktree, for each of `uno`, `uno328pb` and `leonardo`, `rm -rf
.pio/build/<env>` then run exactly ONE `pio run -e <env>`, per `size_baseline.json`'s own
documented cold convention, and record flash and RAM. Then compare against the before-record's WARM
figures and record whether they agree. Remove and prune the worktree, and assert `git worktree
list` in `/workspaces/firestarter` matches its pre-probe output.

STEP 2 -- the cold after side. In `/workspaces/firestarter`, for each of the three targets,
`rm -rf .pio/build/<env>` then exactly ONE `pio run -e <env>`, and record flash and RAM. Compute
the composed delta **cold to cold** per target, and also record Leonardo's Caterina headroom
against 28672. Record the per-half attribution from plans 02 and 03 alongside, and reconcile any
difference from the research's -890 / -258 / -1148 by naming OD-1's per-row policy column as the
cause (C-19) -- never by adjusting anything.

STEP 3 -- DECODE-01's ledger at the final position. Using the full toolchain path
(`$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm`, which is not on PATH), run
`avr-nm --print-size --size-sort --radix=d` on the `uno` ELF and record: the ten deleted stubs
ABSENT; `store_field` ABSENT because it inlined; the five siblings still ABSENT; `key_parsers`'
new byte size against the before value 44; `get_flags`' surviving clone size with its numeric
suffix NOT pinned; and `jsoneq_` and `simple_strtoul` unchanged. State that this ledger will NOT
close arithmetically against the image delta and say why -- symbols that stop existing are inlined
into their callers, so the image figure is the phase total and the ledger is corroboration only.

STEP 4 -- DECODE-02's oracle at the final position, all three targets. Run the offset-resolved
`strings -a -n 2 -t d` block dump on all three ELFs, cross-keyed against
`avr-nm ... | grep -E ' key_'`, and record that exactly ONE key-string block survives on each,
with all eleven keys at count one. Use the block comparison only. State that OD-3 made this a
SOURCE property -- `get_flags` references `key_flags` directly -- and that it was nonetheless
re-measured per target rather than assumed, because the before-state duplicate removal was a
link-time outcome the research could not explain.

STEP 5 -- the eight gate legs, each recorded with its verbatim output. In
`/workspaces/firestarter`: (1) the three cold `pio run` builds from step 2 with their `warning:`
counts; (2) `pio test -e native`; (3) `pio test -e native_nodevtools`; (4)
`python3 scripts/check_build_warnings.py`; (5)
`python3 scripts/check_no_heap_or_64bit_symbols.py`; (6)
`python3 scripts/check_size_baseline.py --policy merge05 --baseline
scripts/baseline/size_baseline_base01.json --rebuild`; (7) the same script in DEFAULT mode against
`scripts/baseline/size_baseline.json`. Then in `/workspaces/firestarter_app`: (8) the full host
suite, `python3 -m pytest tests/ -q -o addopts=""` -- the doubled `-q` in `addopts` hides the count
line, hence the override -- run on the COMMITTED firmware tree with any `.rej` or `.orig` file
deleted first. For each leg record the command, the exit status and the salient output. For legs
6 and 7, record which lines fail and which do not: the expected shape is failure on the native
`cases` lines only, with no AVR flash or RAM leg, plus -- in default mode -- the pre-existing
`flash_used` byte-identity failures Phases 155 and 156 already caused. Mark legs 4, 5, 6 and 7 as
running in NO CI workflow.

STEP 6 -- the one-sidedness, quoted from source. Read `scripts/check_size_baseline.py` at lines
697 and 709 and quote both comparisons verbatim. Record that they are growth-only, so this phase's
reduction passes with no named exemption, and that the pass must therefore be recorded as
ONE-SIDED so nobody later reads a green run as "no size change" (D-03). Author no exemption
constant.

STEP 7 -- re-derive `sizeof(firestarter_handle_t)` one final time, on both architectures, with the
same method and the same captured flags the before-record used for OD-7, and record the offsets of
all seventeen members after the change. Confirm the AVR value is exactly 5 B below the before
figure with the eleven table members at offsets 3-32 and `data_buffer` at 33, and that the native
value is 655 -- unchanged, which is ceiling 5.

STEP 8 -- assemble the handoffs for Phase 158, as a list with numbers: the native case count moved
172 -> 177 -> 184 on BOTH `native` and `native_nodevtools`, with the suite count unchanged at 17;
`scripts/baseline/size_baseline.json` records 172 today and LAND-01 owns the re-record;
BASE-01 records 141, frozen at Phase 124, and LAND-03 owns it; every headline figure in this record
is COLD while every intermediate plan figure was WARM; and the MERGE-05 pass is one-sided.

Hand every value to task 2. Write no file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | grep -c 157-cold-probe)" = "0" && NM=$HOME/.platformio/packages/toolchain-atmelavr/bin/avr-nm && for e in uno uno328pb leonardo; do rm -rf .pio/build/$e; done && pio run -e uno -e uno328pb -e leonardo > /tmp/157-07-cold.log 2>&1 && test "$(grep -c 'warning:' /tmp/157-07-cold.log)" = "0" && grep -q 'used 1562 bytes' /tmp/157-07-cold.log && grep -q 'used 1568 bytes' /tmp/157-07-cold.log && grep -q 'used 2003 bytes' /tmp/157-07-cold.log && test "$($NM --print-size --radix=d .pio/build/uno/firestarter_uno.elf | grep -cE ' (get_memory_size|get_address|get_chip_id|get_pin_count|get_delay|get_vpp_mv|get_algorithm|get_read_settling|get_read_strobe|get_page_size|store_field|get_r1|get_r2|get_rev|get_rw_pin|get_vpp_pin)$')" = "0" && test "$($NM --print-size --radix=d .pio/build/uno/firestarter_uno.elf | grep -c ' key_parsers$')" = "1" && pio test -e native 2>&1 | grep -q '184 test cases: 184 succeeded' && pio test -e native_nodevtools 2>&1 | grep -q '184 test cases: 184 succeeded' && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && sed -n '697p;709p' scripts/check_size_baseline.py | grep -c 'delta >' | grep -q 2 && cd /workspaces/firestarter_app && python3 -m pytest tests/ -q -o addopts="" 2>&1 | tail -5 | grep -q '1976 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `FW_POST_SHA` and the meta HEAD are recorded, `git -C firestarter status --porcelain` is empty, and the firmware HEAD subject begins `test(157-05):` -- confirming plan 06 created no commit.
    - Cold pre-phase figures are recorded for all three targets from a throwaway worktree detached at `1151dc4`, each from `rm -rf .pio/build/<env>` plus exactly one `pio run -e <env>`, and compared against the before-record's WARM pairs with agreement or disagreement stated per target.
    - Cold post-phase figures are recorded for all three targets by the same method, RAM reads exactly `1562` / `1568` / `2003`, and the composed flash delta is computed COLD to COLD per target and stated together with the per-half attribution from plans 02 and 03.
    - Any difference between the measured deltas and the research's `-890` / `-258` / `-1148` is stated in bytes and attributed to OD-1's per-row policy column (C-19).
    - Leonardo's Caterina headroom against `28672` is recorded as measured, with the ROADMAP figure it supersedes named. Source assertion: `3440` does not appear as a current value.
    - `avr-nm --print-size --radix=d` on the `uno` ELF yields ZERO matches for the alternation of the ten deleted stub names, `store_field`, and the five sibling names; `key_parsers` appears exactly once with its new size recorded against the before value `44`; and `get_flags`' clone is recorded without its numeric suffix pinned.
    - The record states that the symbol ledger does not close arithmetically against the image delta, and gives the inlining reason.
    - The offset-resolved `strings` block dump on all THREE ELFs shows exactly ONE key-string block each, with the eleven keys at count one, and the evidence contains no exact-string count and no substring `grep -c`.
    - All eight gate legs are recorded with command, exit status and salient output. Legs 4, 5, 6 and 7 are marked as running in no CI workflow. The full host suite reports `1976 passed`, run on the committed firmware tree.
    - `pio test -e native` and `pio test -e native_nodevtools` each report `184 test cases: 184 succeeded` over `17` suites.
    - `scripts/check_size_baseline.py` lines `697` and `709` are quoted verbatim, the pass is recorded as ONE-SIDED, and no exemption constant was authored.
    - `sizeof(firestarter_handle_t)` is re-derived on both architectures with the OD-7 method: the AVR value is exactly 5 B below the before figure with the eleven table members at offsets `3`-`32` and `data_buffer` at `33`, and the native value is `655`.
    - The Phase 158 handoff list carries the `172 -> 177 -> 184` trajectory on both envs, the suite count `17`, `size_baseline.json`'s recorded `172`, BASE-01's frozen `141`, the cold-versus-warm status of every figure, and the one-sidedness.
    - `/tmp/157-cold-probe/firestarter` does not exist when the task ends; `git worktree prune` has been run; `git worktree list` in `/workspaces/firestarter` matches its pre-probe output; no `.rej` or `.orig` file exists in either repo.
  </acceptance_criteria>
  <done>Every phase-gate leg is run on the final committed tree with its verbatim output captured, the headline delta is cold on both sides, DECODE-01's ledger and DECODE-02's per-target block dumps are taken at the final position, the size-gate pass is recorded as one-sided with its source lines quoted, `sizeof` is re-derived on both architectures, and Phase 158's handoffs are assembled with numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the after-figures record</name>
  <files>.planning/v1.33/157-after-figures.md</files>
  <read_first>
    - `.planning/v1.33/156-after-figures.md` -- its frontmatter (`title`, `phase`, `plan`, `measured`, `status`, `supersedes`, `requirements`), its ten-section skeleton plus the closing self-verification section, and specifically how its `## 9. The corrections ledger` closes each correction out rather than merely restating it
    - `.planning/v1.33/157-before-figures.md` -- every section, since this record closes out its corrections index and its OD-1..OD-7 section
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-PATTERNS.md` -- §8, including the two sections 156's skeleton has no slot for and which this record must add: DECODE-07's record-only section, and the 999.35 non-additivity warning
    - all five prior SUMMARY files (`157-01` through `157-06`) -- every probe transcript and every measured figure, so nothing is transcribed from memory
    - `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-VALIDATION.md` -- the ten coverage ceilings and the OD-1 / OD-2 statements
    - `.planning/ROADMAP.md` §Phase 157 (its `**Measured**` line and criteria 1-7) and `.planning/REQUIREMENTS.md` §4 -- the exact stale sentences this record supersedes, so it can quote what it replaces
  </read_first>
  <action>
Create `.planning/v1.33/157-after-figures.md` in the meta repo, following the 155/156 naming and
frontmatter convention.

Frontmatter: `title`; `phase`
(`157-command-decode-table-handle-type-narrowing-firmware-only`); `plan` (`"07"`); `measured`
(today's date); `status` -- state that this file is the phase's outcome record, re-measured against
the committed tree at `FW_POST_SHA` rather than transcribed from an earlier plan's SUMMARY, and
that Phase 158 invalidates its size figures (LAND-01 re-anchors `size_baseline.json` and
cold-rebuilds); `supersedes` -- name ROADMAP.md §Phase 157's `**Measured**` line and criteria 1
through 7, and REQUIREMENTS.md DECODE-01 through DECODE-07 prose, wherever they state a figure this
file corrects, listing C-1 through C-22; and
`requirements: [DECODE-01, DECODE-02, DECODE-03, DECODE-04, DECODE-05, DECODE-06, DECODE-07]`.

Sections, in this order:

1. **Git anchors** -- `FW_POST_SHA` full and abbreviated, the branch, the four firmware commit
   subjects this phase produced (`refactor(157-02)`, `refactor(157-03)`, `test(157-04)`,
   `test(157-05)`) with the statement that plan 06 produced none, porcelain asserted, `git worktree
   list`, the meta HEAD, and the `firestarter_app` gitlink drift note.
2. **The phase ledger -- flash and RAM, before versus after, per target** -- the COLD pairs on both
   sides, the cold-to-cold composed delta, the per-half attribution (-890-class table half from
   plan 02, -258-class narrowing half from plan 03), the WARM figures alongside with the convention
   difference stated, and Leonardo's Caterina headroom. Every figure labelled COLD or WARM
   explicitly.
3. **DECODE-01's mechanical criteria** -- the final symbol ledger, the ten stubs and `store_field`
   absent, the five siblings still absent, `key_parsers` 44 B before and its new size after, the
   surviving `get_flags` clone with its suffix unpinned, the non-closing-ledger explanation, and
   the before-side eleven-stub total of exactly 1012 B with its 84-110 B range (C-2).
4. **DECODE-02's evidence** -- the two-blocks-to-one result on all three targets by offset block
   comparison, the eleven keys at count one, the C-3 correction that ELEVEN of eleven were doubled,
   both forbidden oracle forms named with the wrong numbers they report, and OD-3's three-line
   change recorded as what made single storage a source property. State `get_flags`' exception
   properly: two direct call sites in two DIFFERENT functions, `json_parse_config` and
   `json_get_cmd` (C-1) -- a record saying `json_parse_config` calls it twice is factually wrong.
5. **DECODE-03's evidence** -- the twelve compile-time guards, the two planted-negative diagnostics
   from plan 02 verbatim, the struct-offset tables for both architectures before and after, the
   OD-7 `sizeof` figure with its method and the 600-versus-601 reconciliation, and the six
   round-trip cases plus the localisation probe from plan 05 recorded as what closes ceiling 7 by
   execution rather than by assertion. State that `_Static_assert` in a C translation unit is a NEW
   idiom for this repository, the only pre-existing static assert being the C++-guarded one in
   `include/eprom_params.h`.
6. **DECODE-04's evidence** -- the two narrowed members with their `0x39` and `0x100` bounds and
   the bidirectional host parity gate that pins the flag maximum; the measured site counts 18
   protocol-keyed of 20 total reads and 40 textual `is_flag_set` uses of 59 post-preprocessor, each
   with its derivation, explicitly superseding 19 and 45 (C-5); the five audited surfaces; the
   not-wire-visible and not-EEPROM-visible findings and the resulting "no migration exists"
   conclusion; and the -5 B RAM confirmed two independent ways with ceiling 5 stated.
7. **DECODE-05's evidence -- the safety requirement** -- the five cases with what each proves, S2
   named as load-bearing, both probe transcripts verbatim, C-18 (S4's RED needs the
   saturating-bitmask probe, not the saturation-deleted one, and why), C-20 (S5's consumer half is
   source-level because `eeprom28c_page_mask` is `static`), and ceiling 6 (saturation-as-fail-closed
   is contingent on `0xFF` being unmapped -- a property of the dispatch table, not of
   `store_field`).
8. **DECODE-06's evidence** -- the hoisted constant, the `clamp` column, the strobe cap case that
   did not exist before (C-8), both assertions tightened to equality with the equality-RED /
   upper-bound-GREEN contrast from probe C2 as the justification, and C-21's unremovable duplicate.
9. **DECODE-07 -- the rejected alternative, recorded** (the section 156's skeleton has no slot
   for) -- the fresh three-target `switch` / if-chain / delta triples with their commands and their
   position; the survey's `25696` / `25678` pair with `.planning/notes/firmware-size-reduction-survey.md`
   named as provenance and marked SUPERSEDED as absolutes; the dispatched value set with its range
   and density; the behavioural-equivalence check on the variant; the second rejection argument
   (`firestarter/CLAUDE.md` pins the dispatch order, proven byte-unchanged); C-22's
   single-configuration ceiling; and the plain statement that this section is what discharges the
   requirement, since no code changed.
10. **The gate ledger -- all eight legs** -- each with its command, exit status and salient output,
    with legs 4 through 7 marked as running in NO CI workflow, the two failing size-gate lines
    identified as the pre-existing native `cases` legs with no AVR flash or RAM leg among them, the
    default-mode `flash_used` byte-identity failures attributed to Phases 155 and 156, the
    one-sidedness quoted from `check_size_baseline.py:697` and `:709`, and the host suite at 1976
    passed on a committed tree.
11. **The coverage ceilings -- final form** -- all ten plus C-20, C-21 and C-22. Ceiling 1 written
    as the NEGATIVE ceiling it is.
12. **The corrections ledger -- every row closed out** -- one row per correction C-1 through C-22:
    what the source document said, what was measured, and the outcome. Every one of C-1..C-16 from
    the research and C-17..C-19 from the before-record must appear with a closing verdict, and
    C-20, C-21 and C-22 with the plan that found them.
13. **The seven decisions OD-1 through OD-7** -- each with what was decided, what was declined and
    the declined alternative's cost. State OD-1's declined `reject` semantics with its meta-repo
    `messages.toml` plus codegen cost. State OD-2's kept identifier `key_parsers` as now slightly
    stale -- it is a data table of `{key, clamp, offset, width}`, not a table of parsers -- and why
    it was kept anyway, with the measured 3-failures-against-24-passed alternative.
14. **The 999.35 / v1.28 non-additivity warning** (the second section 156's skeleton has no slot
    for) -- this phase's table-half saving and 999.35's -3728 B / -512 B on `leonardo` are **not
    additive**, because DECODE-01's field table is superseded if the binary command protocol ever
    lands; 999.35 must be re-measured from the post-v1.33 position before anyone quotes a combined
    saving; and the operator ruled the binary protocol OUT of v1.33 on 2026-08-22, so this phase
    proposes no step toward it.
15. **Handoffs** -- Phase 158: the `172 -> 177 -> 184` case-count trajectory on both native envs
    with the suite count unchanged at 17, `size_baseline.json`'s recorded 172 for LAND-01, BASE-01's
    frozen 141 for LAND-03, and the cold-versus-warm status of every figure. Phase 159: every
    `file:LINE` citation in this record and in the before-record was measured against the current
    post-Phase-154 tree and will be remapped once, over the composite diff (D-01, D-05); plan 02's
    two new `#include` lines shifted `src/json_parser.c` by +2 and that is expected, not a defect.
16. **Self-verification of this record** -- how each figure above can be re-derived, and which
    commands to run.

Use ` -- ` rather than an em dash. Do not write any planning-provenance token into a firmware
source file; this task writes only into `.planning/`.

Then commit in the META repo, staging exactly `.planning/v1.33/157-after-figures.md`. Subject:
`docs(157-07): record the phase outcome, the gate ledger and the twenty-two corrections`. The body
must state that every headline figure is COLD, that DECODE-07 is discharged by this record and by
no code change, and that four firmware commits landed. Do not push.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/157-after-figures.md && for n in 1012 44 172 177 184 1562 1568 2003 655 28672 1976 141 C-22 OD-7 999.35 25696 key_parsers get_flags json_get_cmd; do grep -q "$n" .planning/v1.33/157-after-figures.md || { echo "MISSING $n"; exit 1; }; done && test "$(grep -c 'COLD' .planning/v1.33/157-after-figures.md)" -ge "3" && test "$(grep -c 'ONE-SIDED\|one-sided' .planning/v1.33/157-after-figures.md)" -ge "1" && test "$(grep -cE '^\| *C-([1-9]|1[0-9]|2[0-2]) ' .planning/v1.33/157-after-figures.md)" = "22" && test "$(git log --oneline -1 --format=%s -- .planning/v1.33/157-after-figures.md | grep -c '^docs(157-07):')" = "1" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" = "1" && test -z "$(git -C firestarter status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/157-after-figures.md` exists with all seven frontmatter keys, `requirements` listing all seven DECODE ids, and a `supersedes` block naming ROADMAP §Phase 157's `**Measured**` line and criteria 1 through 7 plus REQUIREMENTS §4's DECODE-01 through DECODE-07.
    - The file has all sixteen sections listed in the action, including the two 156's skeleton has no slot for: DECODE-07's record-only section and the 999.35 non-additivity warning.
    - Every headline flash and RAM figure is labelled COLD, and the WARM intermediate figures appear alongside with the convention difference stated. RAM reads `1562` / `1568` / `2003`.
    - The corrections ledger has exactly 22 rows, `C-1` through `C-22`, each with what the source document said, what was measured, and a closing verdict.
    - The file records the eleven-stub before total as `1012` with the `84`-`110` B range, `key_parsers` at `44` B before with its new size after, and the `get_flags` clone WITHOUT its numeric `.constprop` suffix. Source assertion: no `.constprop.` followed by digits appears anywhere in the file.
    - The file states `get_flags`' exception with BOTH function names, `json_parse_config` and `json_get_cmd`. Source assertion: the file contains no sentence claiming `json_parse_config` calls it at two sites.
    - The file records the measured site counts 18 of 20 and 40 of 59 with their derivations, and does not present `19` or `45` as current figures.
    - The file carries both planted-negative diagnostics from plan 02, both probe transcripts from plan 04, and the four probe transcripts from plan 05, verbatim.
    - The file carries all ten coverage ceilings plus C-20, C-21 and C-22, with ceiling 1 written as a NEGATIVE ceiling that forbids borrowing Phase 155's coverage-gap phrasing.
    - The file carries an OD-1..OD-7 section with each declined alternative and its cost, including `messages.toml` plus codegen for `reject`, and the deliberate staleness of the kept identifier `key_parsers`.
    - The gate ledger has all eight legs, marks legs 4 through 7 as running in no CI workflow, quotes `check_size_baseline.py:697` and `:709`, records the pass as one-sided, and records `1976 passed`.
    - The handoff section carries the `172` / `177` / `184` trajectory, the suite count `17`, `size_baseline.json`'s `172`, BASE-01's `141`, and the Phase 159 citation note including the `+2` line shift.
    - `git -C /workspaces log --oneline -1 -- .planning/v1.33/157-after-figures.md` shows a commit whose subject begins `docs(157-07):`, and `git show --stat HEAD` lists exactly one path.
    - `git -C firestarter status --porcelain` is empty and no firmware commit was created by this plan.
  </acceptance_criteria>
  <done>The after-figures record exists and is committed, every figure is cold-labelled and re-derivable from a quoted command, all twenty-two corrections are closed out, all seven OD decisions carry their declined alternative's cost, DECODE-07 is discharged by its own named section, the 999.35 non-additivity warning is stated in the record itself, and Phase 158 and Phase 159 both have explicit handoffs.</done>
</task>

<task type="auto">
  <name>Task 3: Close out the ROADMAP and REQUIREMENTS prose this phase supersedes</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 157 -- the `**Measured**` line, criteria 1 through 7, the `**Plans:**` line and the plan checkbox list, so each edit is scoped to the exact text it replaces
    - `.planning/REQUIREMENTS.md` §4 -- the DECODE-01 through DECODE-07 bullets and their checkboxes -- and the traceability table rows for those seven ids
    - `.planning/REQUIREMENTS.md` §2 and §3 -- how Phases 155 and 156 appended their corrections to a satisfied requirement bullet (the `**Discharged ... by plan NNN-NN against ...**` plus `**Correction (C-N):**` shape), so this phase's closures read the same way
    - `.planning/v1.33/157-after-figures.md` -- the record just written, which every closure must cite as the authority
    - `/workspaces/CLAUDE.md`
  </read_first>
  <action>
Edit `.planning/` only. Use scoped `Edit` replacements exclusively -- **never `Write`** on either
file, and never a GSD roadmap or requirements mutation verb, both of which reformat the whole
document. Before the first edit, snapshot both files (`git stash`-free: just copy them to the
session scratch directory) so the diff can be checked for collateral change afterwards.

STEP 1 -- ROADMAP.md §Phase 157. Make four scoped edits:
  a. The `**Measured**` line currently states the split as field table -976 and narrowing plus
     saturation -172. Replace it with the measured total and the measured split from
     `157-after-figures.md` §2, and add a parenthetical naming the after-record as the authority
     and C-4 as the correction.
  b. The `**Plans:**` line: replace `TBD at /gsd-plan-phase 157` with the plan count.
  c. The plan checkbox list: replace the placeholder with one ticked entry per plan, each naming
     its file and a one-line objective.
  d. Criteria 1 through 7: append to each a short closure sentence naming the plan that discharged
     it and the after-record section that carries the evidence, plus its correction where one
     applies -- criterion 1's `86-110 B` becomes `84-110 B` (C-2); criterion 2's `Ten of eleven`
     becomes eleven of eleven and its `json_parse_config calls it directly at two sites` becomes
     one call there and one in `json_get_cmd` (C-3, C-1); criterion 4's `19` and `45` become 18 of
     20 and 40 of 59 (C-5); criterion 6 gains the note that the `read-strobe-us` half did not
     exist and was written (C-8); criterion 7's `25696 vs 25678` is marked superseded as absolutes
     with the fresh delta cited (C-10). Do not delete a criterion; append to it, the way Phases 155
     and 156 did.

STEP 2 -- REQUIREMENTS.md §4. For each of DECODE-01 through DECODE-07: tick its checkbox, and
append a `**Discharged <date> by plan 157-NN against [157-after-figures.md](v1.33/157-after-figures.md).**`
sentence plus the corrections that bear on that requirement, in the same shape §2's DEAD entries
and §3's DEDUP entries use. Then update the traceability table rows for those seven ids from
`Pending` to `Complete (157-NN)` with the closing plan named, matching the format the DEAD and
DEDUP rows already use.

STEP 3 -- verify no collateral change. Diff both files against the scratch snapshots and confirm
that every changed line falls inside §Phase 157 of the ROADMAP or §4 and the seven traceability
rows of REQUIREMENTS. Any change outside those regions is collateral damage from a too-broad edit
and must be reverted. Specifically assert that no OTHER phase's `**Plans:**` line, `**Goal**` line
or criteria changed, and that the total line count of each file did not fall.

STEP 4 -- commit in the META repo, staging exactly `.planning/ROADMAP.md` and
`.planning/REQUIREMENTS.md`. Subject:
`docs(157-07): close out DECODE-01..07 and supersede the stale ROADMAP figures`. The body must
name the seven requirements, the corrections applied to each criterion, and the after-record as
the authority. Do not push. Do not touch STATE.md in this task -- phase-completion state is the
orchestrator's, and the `phase.complete` verb is known to rewrite unrelated phases' `**Plans:**`
lines and to corrupt STATE.md frontmatter, so it is not invoked here.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(grep -cE '^- \[x\] \*\*DECODE-0[1-7]\*\*' .planning/REQUIREMENTS.md)" = "7" && test "$(grep -cE '^\| DECODE-0[1-7] \| Phase 157 \| Complete' .planning/REQUIREMENTS.md)" = "7" && test "$(grep -c 'field table −976' .planning/ROADMAP.md)" = "0" && test "$(grep -c '157-after-figures.md' .planning/ROADMAP.md)" -ge "1" && test "$(grep -c '157-after-figures.md' .planning/REQUIREMENTS.md)" -ge "1" && test "$(grep -cE '^- \[x\] 157-0[1-7]-PLAN.md' .planning/ROADMAP.md)" = "7" && test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" = "$(git show HEAD~1:.planning/ROADMAP.md | grep -c '^### Phase ')" && test "$(git log --oneline -1 --format=%s | grep -c '^docs(157-07):')" = "1" && test -z "$(git -C firestarter status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - All seven `DECODE-0N` bullets in `.planning/REQUIREMENTS.md` §4 are ticked, and each carries a `**Discharged ... by plan 157-NN against [157-after-figures.md](v1.33/157-after-figures.md).**` sentence plus the corrections bearing on it, in the same shape the DEAD and DEDUP entries use.
    - All seven traceability rows for `DECODE-01` through `DECODE-07` read `Complete` with the closing plan named.
    - ROADMAP §Phase 157's `**Measured**` line no longer states the `-976` / `-172` split and instead carries the measured total and split with the after-record named as authority.
    - ROADMAP §Phase 157's `**Plans:**` line states the plan count, and the plan list has seven ticked entries `157-01-PLAN.md` through `157-07-PLAN.md`, each with a one-line objective.
    - Each of criteria 1 through 7 carries a closure sentence naming its discharging plan and the after-record section, with the applicable correction appended -- `84-110 B` for criterion 1, eleven-of-eleven plus the two `get_flags` call-site functions for criterion 2, 18-of-20 and 40-of-59 for criterion 4, the written `read-strobe-us` half for criterion 6, and the superseded absolutes plus the fresh delta for criterion 7.
    - The count of `^### Phase ` headings in `.planning/ROADMAP.md` is identical before and after this task, and no other phase's `**Goal**`, `**Plans:**` or criteria lines changed. Source assertion: the diff against the pre-task snapshot touches only lines inside §Phase 157.
    - Neither file's total line count fell, and neither was rewritten by `Write` or by a GSD mutation verb.
    - `git -C /workspaces log --oneline -1` subject begins `docs(157-07):` and `git show --stat HEAD` lists exactly the two paths `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
    - `git -C firestarter status --porcelain` is empty, and no path under `firestarter_app/` is staged; the `firestarter_app` gitlink is not in the commit's file list.
    - `.planning/STATE.md` is unchanged by this task.
  </acceptance_criteria>
  <done>All seven DECODE requirements are ticked and closed against the after-record with their corrections appended; ROADMAP §Phase 157 carries the measured split, the plan list and seven per-criterion closures; every stale figure named by a correction is superseded in place; and the diff is provably confined to §Phase 157 and §4 with no other phase's entry disturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final tree -> phase record | Every claim this phase makes ends up here; a figure transcribed from an earlier SUMMARY rather than re-measured is unfalsifiable |
| record -> Phase 158 and Phase 159 | The case-count movement, the cold/warm status and the citation staleness are handed forward; anything omitted becomes something a later phase must rediscover |
| scoped edit -> whole planning document | A whole-file write on ROADMAP.md or REQUIREMENTS.md destroys every entry outside the diff window; the boundary is enforced by heading-count and snapshot-diff assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-157-31 | Tampering | a whole-file `Write` or a GSD mutation verb on `ROADMAP.md` / `REQUIREMENTS.md` | high | mitigate | Prohibited explicitly; scoped `Edit` only; pre-task snapshots diffed afterwards; the `^### Phase ` heading count asserted identical before and after; a line-count-did-not-fall check |
| T-157-32 | Repudiation | a headline delta computed from a WARM before against a COLD after | high | mitigate | The pre-phase position is cold-rebuilt in a throwaway worktree detached at `1151dc4`, so the composed delta is cold to cold; the WARM figures are recorded alongside with the convention difference stated |
| T-157-33 | Repudiation | a figure transcribed from an earlier SUMMARY rather than re-measured on the final tree | high | mitigate | Every headline figure is re-measured in task 1 on the committed tree, and the record's closing section states how each can be re-derived and with which command |
| T-157-34 | Repudiation | reading a green one-sided size gate as "no size change", or a green CI run as evidence the size gate passed | medium | mitigate | `check_size_baseline.py:697` and `:709` quoted from source; the pass recorded as one-sided (D-03); legs 4 through 7 marked as running in NO CI workflow (ceiling 9) |
| T-157-35 | Repudiation | describing DECODE-01 or DECODE-02 as gated, or borrowing Phase 155's coverage-gap phrasing | medium | mitigate | Ceiling 4 and the negative ceiling 1 are both restated in the record and both are enforced by prohibitions and source assertions |
| T-157-36 | Tampering | absorbing the native case-count movement into a baseline file instead of handing it forward | medium | mitigate | Both baseline files prohibited from edit; the `172 -> 177 -> 184` trajectory and both owning requirements (LAND-01, LAND-03) recorded as explicit handoffs |
| T-157-09 | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages. RESEARCH's Package Legitimacy Audit records zero packages for the whole phase, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty; the firmware HEAD subject still begins `test(157-05):`; four firmware commits total for this phase.
- Cold `pio run` on all three targets: zero `warning:` lines, RAM 1562 / 1568 / 2003.
- `pio test -e native` and `pio test -e native_nodevtools`: 184 of 184 each, 17 suites.
- `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` exit 0; both size-gate modes' outputs captured with their failing lines identified.
- Host `pytest tests/ -q -o addopts=""`: 1976 passed, on the committed firmware tree.
- `.planning/v1.33/157-after-figures.md` exists with 22 correction rows and sixteen sections.
- `.planning/REQUIREMENTS.md`: seven ticked DECODE bullets and seven `Complete` traceability rows.
- `.planning/ROADMAP.md`: `^### Phase ` heading count unchanged; seven ticked plan entries under §Phase 157.
- No worktree named `157-cold-probe` remains; `git worktree list` matches its pre-task output.
</verification>

<success_criteria>
Every phase-gate leg -- including the four no CI workflow invokes -- is run on the final committed
tree and recorded with its verbatim output; the headline size figures are cold on both sides of the
comparison; DECODE-01's symbol ledger and DECODE-02's per-target block dumps exist in the one place
they can exist, with their absence of any automated gate stated; DECODE-07 is discharged by its own
named section carrying a first-party three-target measurement and the superseded absolutes;
all twenty-two corrections are closed out and all seven OD decisions carry their declined
alternative's cost; the 999.35 non-additivity warning is stated in the record itself; Phase 158 and
Phase 159 both receive explicit numbered handoffs; and the ROADMAP and REQUIREMENTS closures are
provably confined to §Phase 157 and §4 with no other phase's entry disturbed.
</success_criteria>

<output>
Create `.planning/phases/157-command-decode-table-handle-type-narrowing-firmware-only/157-07-SUMMARY.md` when done.
</output>
