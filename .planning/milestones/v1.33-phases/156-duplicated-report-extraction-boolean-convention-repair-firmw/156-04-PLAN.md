---
phase: 156-duplicated-report-extraction-boolean-convention-repair-firmw
plan: 04
type: execute
wave: 4
depends_on: ["156-03"]
files_modified:
  - firestarter/src/proms/memory.cpp
  - firestarter/include/memory_utils.h
  - firestarter/src/proms/flash_utils.cpp
  - firestarter/src/proms/flash_intel.cpp
  - firestarter/src/proms/eprom.cpp
  - firestarter/src/proms/eeprom_28c.cpp
  - firestarter/tests/golden/protocol_branch_inventory.json
autonomous: true
requirements: [DEDUP-02, DEDUP-03]

must_haves:
  truths:
    - "One mem_util_report_chip_id() replaces all four chip-ID mismatch blocks, in flash_utils.cpp, flash_intel.cpp, eprom.cpp and eeprom_28c.cpp."
    - "The four copies had drifted six ways, every divergence is enumerated with its resolution and reasoning, and the resolved single semantic is STATED rather than silently chosen -- that statement is itself the deliverable criterion 2 asks for."
    - "The standalone CMD_CHECK_CHIP_ID path still refuses unconditionally, independent of FLAG_FORCE. Severity is a caller-supplied parameter precisely so this one caller's policy is not silently changed."
    - "The chip-ID severity fork is still pinned in both directions, by response_code AND by message id, in CI -- the id half added by plan 02 against the pre-refactor code, now re-run against the helper."
    - "The committed branch-inventory golden is re-derived by its own extractor inside the SAME commit, and its note records that the removed predicate MOVED into memory.cpp rather than vanishing."
    - "The already-stale in-source citation at eeprom_28c.cpp:265 is repaired as an incidental fix inside a comment this plan touches anyway, and recorded as incidental rather than as scope creep."
  artifacts:
    - firestarter/src/proms/memory.cpp
    - firestarter/include/memory_utils.h
    - firestarter/src/proms/flash_utils.cpp
    - firestarter/src/proms/flash_intel.cpp
    - firestarter/src/proms/eprom.cpp
    - firestarter/src/proms/eeprom_28c.cpp
    - firestarter/tests/golden/protocol_branch_inventory.json
  key_links:
    - "warn_only as a PARAMETER is the link that keeps standalone CMD_CHECK_CHIP_ID refusing unconditionally. eprom.cpp has two callers of the same block with different policies -- eprom_check_chip_id_execute passes ERROR unconditionally while eprom_generic_init passes the FLAG_FORCE-derived value -- so folding is_flag_set(FLAG_FORCE) into the helper would silently make --force apply where it does not today."
    - "The helper derives BOTH the id and the response_code from one boolean, which makes a transposition on that axis impossible by construction -- structurally safer than mem_util_report_voltage's two independent parameters, and worth stating as the reason the two helpers differ in shape."
    - "Hoisting the mismatch guard into the helper's early return is what buys most of the saving and is also what removes the second site from eprom.cpp's pinned branch inventory -- the predicate MOVES into a file the gate does not scan, which is exactly the pattern the sibling HV-routing gate exists to catch."
  prohibitions:
    - "Must NOT fold is_flag_set(FLAG_FORCE) into mem_util_report_chip_id. Divergence 1 must be PARAMETERISED, never collapsed -- collapsing it makes the standalone CMD_CHECK_CHIP_ID command honour --force, which it does not today, and criterion 2 forbids a behaviour change."
    - "Must NOT hand-edit tests/golden/protocol_branch_inventory.json, and must NOT replace its recorded_by field -- append only."
    - "Must NOT record the removed chip-ID predicate as a deleted safety branch. It MOVED into mem_util_report_chip_id's early return in src/proms/memory.cpp, a file this gate does not scan, and the note must say so."
    - "Must NOT change the linkage of any of the four functions -- eeprom28c_check_chip_id stays static, the other three stay external. Divergence 5 resolves to unchanged."
    - "Must NOT touch any VPP block or mem_util_report_voltage -- plan 03 owns those and they are already committed."
    - "Must NOT touch src/eprom_operations.cpp, src/operation_utils.cpp or include/operation_utils.h -- those are plan 05's."
    - "Must NOT edit include/messages.h, meta's tools/catalog/messages.toml, or run codegen.py."
    - "Must NOT remap any .planning/ file:LINE citation. The single #include added to flash_utils.cpp shifts all 97 of that file's planning citations; that staleness is EXPECTED and close-blocked by REMAP-04 (D-05). The ONLY citation repaired here is the in-source one at eeprom_28c.cpp:265, which is already stale and sits in a comment this plan edits."
    - "Must NOT re-anchor scripts/baseline/size_baseline.json (Phase 158 / LAND-01) or repair the BASE-01 mismatch (Phase 158 / LAND-03)."
    - "Must NOT run pytest tests/ before committing -- the dirty-tree run gives spurious failures and one false GREEN."
    - "Must NOT author or imply a bench/hardware criterion (D-02), and must NOT invent D-NN labels (OD-1)."
    - "Must NOT add a GSD provenance comment of the // Phase NNN (REQ-NN): form to any firmware source file."
    - "Must NOT push any branch."
---

<!-- Comment-text discipline: these literals appear in task actions AND in negative-grep acceptance criteria scoped to firmware files. They are named here because naming them precisely is this phase's evidence discipline. -->
<!-- planner-discipline-allow: DEDUP-01 -->
<!-- planner-discipline-allow: DEDUP-02 -->
<!-- planner-discipline-allow: Phase 156 -->
<!-- planner-discipline-allow: OD-1 -->
<!-- planner-discipline-allow: OD-2 -->
<!-- planner-discipline-allow: is_flag_set(FLAG_FORCE) -->
<!-- planner-discipline-allow: flash_intel.cpp:112-121 -->
<!-- planner-discipline-allow: (uint16_t)actual -->
<!-- planner-discipline-allow: (uint16_t)handle->chip_id -->

<objective>
Replace the four chip-ID mismatch blocks with one shared helper, resolving six measured
divergences without changing behaviour at any of the four sites, and state the resolved single
semantic rather than choosing it silently.

Purpose: the four copies had already drifted. Three test `is_flag_set(FLAG_FORCE)` inline while
`eprom.cpp`'s takes an `error_code` parameter; `eeprom_28c.cpp`'s carries redundant casts and a
superfluous brace level. Criterion 2 requires the resolved semantic be **stated**, which makes
the statement itself a deliverable. And one divergence must NOT be collapsed: `eprom.cpp` has
two callers of the same block with different policies, so folding the force test into the helper
would silently make the standalone `CMD_CHECK_CHIP_ID` command honour `--force` -- a behaviour
change criterion 2 forbids, and one for which no oracle exists.

Output: `mem_util_report_chip_id` in `src/proms/memory.cpp` with its declaration in
`include/memory_utils.h`, four call sites collapsed, one `#include` added to
`flash_utils.cpp`, the stale in-source citation at `eeprom_28c.cpp:265` repaired, and the golden
re-derived from 22 sites to 21 -- all in one commit, measured on all three AVR targets.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-PATTERNS.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-02-SUMMARY.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-03-SUMMARY.md
@.planning/v1.33/156-before-figures.md
</context>

## Multi-Source Coverage Audit (this plan's slice)

The phase-wide audit is recorded once, in `156-01-PLAN.md`. This plan covers REQ **DEDUP-02**
in full plus DEDUP-03's chip-ID fork half; GOAL clause "collapse the chip-ID report block";
RESEARCH's six enumerated divergences and the resolved single semantic; RESEARCH C-3 option (a)
(measure DEDUP-02's own half in its own commit); RESEARCH open question 2 (two commits, the
golden riding each) and open question 5 (the `eeprom_28c.cpp:265` citation, repaired as
incidental per the recommendation); the Runtime State Inventory's golden entry; and the one
`#include` ROADMAP D-01 counts among the milestone's four citation-shifting include additions.
No item assigned to this plan is MISSING.

## The honest coverage ceilings -- stated, not implied

All seven are carried verbatim from `156-01-PLAN.md`. The ones bearing on this plan:

- **Ceiling 2 / the divergence-1 gap.** There is **no oracle anywhere** for the claim that the
  standalone `CMD_CHECK_CHIP_ID` path still refuses unconditionally. `test_case7...` and
  `test_migrated_mismatching_chip_id_errors` exercise the mismatch fork, not that specific
  caller's policy. This is the one place divergence 1 could silently regress, and its evidence
  is source-level: the caller keeps passing an unconditional `RESPONSE_CODE_ERROR` and the
  helper does not consult `FLAG_FORCE`. Say that, do not imply a test covers it.
- **Ceiling 5.** `check_size_baseline.py` is in no CI workflow; every size figure here is a
  local-run obligation.
- **Ceiling 6.** No bench claim.
- **Ceiling 7.** Nothing in this plan makes an image-identity claim.

## Artifacts this phase produces

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/156-before-figures.md` | 01 | meta |
| New case(s) in `test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp` | 02 | fw |
| New assertions inside `test_case7_mismatching_chip_id_with_force_warns` | 02 | fw |
| `mem_util_report_voltage` -- definition in `src/proms/memory.cpp`, declaration in `include/memory_utils.h` | 03 | fw |
| Local `force` at the four VPP call sites | 03 | fw |
| `mem_util_report_chip_id` -- definition in `src/proms/memory.cpp`, declaration in `include/memory_utils.h` | 04 | fw |
| `#include "memory_utils.h"` added to `src/proms/flash_utils.cpp` | 04 | fw |
| Re-derived `tests/golden/protocol_branch_inventory.json`: `counts` 23/1/22 -> 22/1/21 (plan 03) -> 21/1/20 (plan 04) | 03, 04 | fw |
| `tests/test_boolean_convention_source_contract_v133.py` and its module symbols | 06 | fw |
| `.planning/v1.33/156-after-figures.md` (the phase record) | 07 | meta |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author mem_util_report_chip_id, and state the resolved single semantic</name>
  <files>firestarter/src/proms/memory.cpp, firestarter/include/memory_utils.h</files>
  <behavior>
    - `pio run -e uno` compiles the helper with no new warning on `memory.cpp`.
    - `avr-nm --print-size` reports `mem_util_report_chip_id` at approximately 90 bytes (`0x5a`), the figure research measured against this signature.
    - The helper emits nothing at all when the ids match: it takes an early return before the payload is packed. That early return is the hoisted guard from all four call sites.
    - `pio test -e native` still reports 172 cases succeeding -- the helper is compiled into the native envs and adding it changes nothing until the call sites move.
  </behavior>
  <read_first>
    - `firestarter/src/proms/memory.cpp` -- the region around `mem_util_report_voltage` as committed by plan 03, so the second helper sits immediately beside its sibling and matches its comment style
    - `firestarter/include/memory_utils.h` -- the current state after plan 03, including the new block comment and the `extern "C"` guard boundaries at :11-13 and its close. A `bool` parameter in a declaration inside that guard compiles in both C and C++ TUs because `firestarter.h` is included at :10 -- confirm that before writing the signature
    - `firestarter/src/proms/flash_utils.cpp` lines 100-125 -- Site A, `flash_util_check_chip_id_execute` at :103, the cast-free canonical form whose four byte assignments the helper reproduces, with its `is_flag_set(FLAG_FORCE)` fork at :111-117
    - `firestarter/src/proms/eeprom_28c.cpp` lines 260-310 -- Site C: the stale in-source citation at :264, `eeprom28c_check_chip_id` at :268, `chip_id`'s declaration at :288 proving it already `uint16_t`, the redundant `(uint16_t)` casts, the superfluous brace level, and the fork at :296-304
    - `firestarter/src/proms/eprom.cpp` lines 783-815 -- Site D: `eprom_generic_init` at :792 passing a FLAG_FORCE-derived value at :791, and `eprom_internal_check_chip_id` at :795 keying severity on its `error_code` parameter at :804
    - `firestarter/src/proms/eprom.cpp` around `eprom_check_chip_id_execute` (the standalone `CMD_CHECK_CHIP_ID` entry, near :119) -- the OTHER caller of Site D, which passes `RESPONSE_CODE_ERROR` unconditionally. This is divergence 1's whole reason
    - `firestarter/src/proms/flash_intel.cpp` lines 185-210 -- Site B, `flash_intel_check_chip_id` at :187, identical to Site A
    - `firestarter/include/firestarter.h` around the handle's `chip_id` member -- proof it is `uint16_t`, which makes Site C's casts provable no-ops
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §DEDUP-02 -- all four sites verbatim, the six-divergence table with its per-divergence reasoning, and the resolved-semantic comment block quoted in full
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-PATTERNS.md` -- Pattern 1's verbatim reference implementation of this helper and the note on the two helpers' structural asymmetry
  </read_first>
  <action>
Two files in this task: the definition and the declaration. No commit -- task 3 commits.

Take the CODE from the reference at `wip/v1.33-size-reduction-survey-preserved` @ `a6b46f8`
(`git show wip/v1.33-size-reduction-survey-preserved:src/proms/memory.cpp`), or from the
`memory.cpp` hunk of the reconstructed subset patch. That branch cannot be cherry-picked; use it
as a semantic reference. Its comment for this helper needs no correction -- unlike
`mem_util_report_voltage`'s, which plan 03 fixed.

DEFINITION -- `src/proms/memory.cpp`, immediately beside `mem_util_report_voltage`.

Signature, exactly:
`void mem_util_report_chip_id(firestarter_handle_t* handle, uint16_t actual, bool warn_only)`.

Body: an early `return` when `actual == handle->chip_id` -- this is the mismatch guard hoisted
out of all four call sites, and hoisting it is what collapses each site to one line and buys
most of the saving. Then a `uint8_t` array of 4 with the four big-endian assignments: high then
low byte of `actual`, then high then low byte of `handle->chip_id`. Then one
`LOG_ID_BYTES(...)` call whose id argument is a ternary on `warn_only` selecting
`MSG_WARN_CHIP_ID_MISMATCH` or `MSG_ERR_CHIP_ID_MISMATCH`, with the count 4. Then
`handle->response_code = warn_only ? RESPONSE_CODE_WARNING : RESPONSE_CODE_ERROR;`.

Write the four assignments cast-free, matching Site A's canonical form. Site C's extra
`(uint16_t)` casts are provable no-ops -- `chip_id` is already `uint16_t` at
`eeprom_28c.cpp:288` and `handle->chip_id` is `uint16_t` in `firestarter.h` -- so they are
dropped, which is divergence 2's resolution.

THE RESOLVED SINGLE SEMANTIC -- this is the deliverable, not decoration. Write it as the helper's
leading comment, and it must say, in prose:
  - what the function does: reports a chip-ID mismatch, or returns without emitting anything if
    the ids match;
  - that severity is the CALLER's decision, passed as `warn_only`, **because the four former
    copies did not agree on how to derive it and one of them must not agree**: `eprom.cpp`'s
    standalone `CMD_CHECK_CHIP_ID` path refuses unconditionally, independent of `FLAG_FORCE`;
  - that the helper therefore unifies the COMPARISON and the PAYLOAD and deliberately does NOT
    unify the POLICY;
  - that both the id and the `response_code` are derived from the same single boolean, so a
    transposition between them is impossible by construction on this helper -- deliberately
    unlike `mem_util_report_voltage`, which takes them as two independent parameters because its
    under-voltage arm needs a pairing its over-voltage arm does not. That asymmetry is stated,
    not accidental;
  - that severity rides entirely in the message id, because every `LOG_{WARN,ERROR}_ID_BYTES`
    macro is the same alias of `LOG_ID_BYTES`.
Use ` -- ` rather than an em dash. Add no phase number, no requirement id in GSD provenance
form, no decision label.

DECLARATION -- `include/memory_utils.h`, immediately below `mem_util_report_voltage`'s
prototype, inside the `extern "C"` guard, sharing or extending plan 03's block comment. If
extending, add one sentence naming the duplication this second declaration retires: four
chip-ID mismatch blocks across four translation units.

ALSO IN THIS TASK -- write the six-divergence resolution table into the plan SUMMARY (not into
source). One row per divergence with the sites it affects, the resolution, and the reasoning:
  1. severity keyed on `is_flag_set(FLAG_FORCE)` inline (A, B, C) versus on an `error_code`
     parameter (D) -- resolved by the `bool warn_only` parameter, A/B/C passing the force test
     and D passing its parameter compared against `RESPONSE_CODE_WARNING`. Must NOT be
     collapsed, with the two-callers-different-policies reason spelled out.
  2. redundant `(uint16_t)` casts (C only) -- dropped, provable no-ops, zero behaviour and zero
     size delta.
  3. superfluous extra brace level (C only) -- dropped, lexical only.
  4. the mismatch guard living at the call site (all four) -- moved into the helper as an early
     return; byte-identical at all four sites; consequence tracked in the golden.
  5. linkage (C is static, A/B/D external) -- UNCHANGED, out of scope.
  6. `#include "memory_utils.h"` absent from A -- one include added, with the note that this
     single line shifts all 97 of `flash_utils.cpp`'s planning citations, that this is one of
     the four include additions ROADMAP D-01 measures as causing 41 % of the milestone's
     citation rework, and that the staleness is expected and close-blocked by REMAP-04.
State explicitly that **no divergence requires a behaviour change to resolve**: 1 is preserved
by parameterisation, 2 and 3 are provable no-ops, 4 is a pure relocation, 5 is untouched, 6 is
additive.

Build check: `pio run -e uno` succeeds with no new `memory.cpp` warning, and
`avr-nm --print-size --size-sort -C .pio/build/uno/firestarter_uno.elf` lists
`mem_util_report_chip_id`. Use the full toolchain paths -- `avr-nm` is not on PATH.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -q 'void mem_util_report_chip_id(firestarter_handle_t\* handle, uint16_t actual, bool warn_only)' src/proms/memory.cpp && grep -q 'mem_util_report_chip_id' include/memory_utils.h && test "$(grep -c 'is_flag_set(FLAG_FORCE)' src/proms/memory.cpp)" -eq 0 && pio run -e uno 2>&1 | grep -E '^(RAM|Flash):' && ~/.platformio/packages/toolchain-atmelavr/bin/avr-nm --print-size --size-sort -C .pio/build/uno/firestarter_uno.elf | grep -q 'mem_util_report_chip_id' && pio test -e native 2>&1 | tee /tmp/n4.log | tail -3 && grep -qE '172 test cases: 172 succeeded' /tmp/n4.log</automated>
  </verify>
  <acceptance_criteria>
    - `src/proms/memory.cpp` defines `mem_util_report_chip_id` with exactly the three-parameter signature named in the action, and `include/memory_utils.h` declares it identically inside the `extern "C"` guard.
    - Source assertion: `grep -c 'is_flag_set(FLAG_FORCE)' src/proms/memory.cpp` is 0. The helper does not consult the force flag -- divergence 1 is parameterised, not collapsed.
    - Source assertion: `grep -c '(uint16_t)actual' src/proms/memory.cpp` is 0 and `grep -c '(uint16_t)handle->chip_id' src/proms/memory.cpp` is 0. The redundant casts were dropped, not carried over.
    - The helper's body contains exactly one `LOG_ID_BYTES` call, with a ternary on `warn_only` selecting between `MSG_WARN_CHIP_ID_MISMATCH` and `MSG_ERR_CHIP_ID_MISMATCH`, and a count of 4.
    - The helper contains an early `return` on the ids matching, before any payload assignment.
    - `avr-nm --print-size --size-sort -C` lists `mem_util_report_chip_id` at `0x5a` (90 bytes). A materially different size means the signature or body diverged and must be re-checked before proceeding.
    - The helper's leading comment states all five points listed in the action, including the explicit sentence that the standalone `CMD_CHECK_CHIP_ID` path refuses unconditionally independent of `FLAG_FORCE`, and the explicit statement of why this helper's shape differs from `mem_util_report_voltage`'s.
    - The SUMMARY carries the six-divergence table with per-divergence resolution AND reasoning, and the sentence that no divergence requires a behaviour change to resolve.
    - `pio run -e uno` succeeds; `python3 scripts/check_build_warnings.py` reports no new warning on `src/proms/memory.cpp`.
    - `pio test -e native` still reports `172 test cases: 172 succeeded`.
    - `include/messages.h` is byte-unchanged.
    - Source assertion: `grep -cE 'DEDUP-0[0-9]|Phase 156|OD-[0-9]' src/proms/memory.cpp include/memory_utils.h` is 0.
  </acceptance_criteria>
  <done>The chip-ID helper exists with the transposition-proof single-boolean shape, its comment states the resolved semantic including the one divergence that must not be collapsed, and the six-divergence resolution is recorded with reasoning rather than asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse the four chip-ID call sites, add the one include, repair the stale in-source citation</name>
  <files>firestarter/src/proms/flash_utils.cpp, firestarter/src/proms/flash_intel.cpp, firestarter/src/proms/eprom.cpp, firestarter/src/proms/eeprom_28c.cpp</files>
  <behavior>
    - `pio test -e native` still reports 172 cases succeeding over 17 suites, including `test_case7_mismatching_chip_id_with_force_warns` with plan 02's id assertions in both directions and `test_migrated_mismatching_chip_id_errors` in `test_sdp_harness`.
    - Under `FLAG_FORCE` a mismatch still warns; without it a mismatch still errors; and the standalone `CMD_CHECK_CHIP_ID` entry still passes an unconditional error severity so it refuses regardless of `--force`.
    - Matching ids still emit nothing at all at every one of the four sites.
  </behavior>
  <read_first>
    - `firestarter/src/proms/flash_utils.cpp` lines 1-20 and 100-125 -- the include block (which has `flash_utils.h`, `Arduino.h`, `rurp_shield.h`, `rurp_pinout.h`, `logging_id.h`, `stdio.h` and NO `memory_utils.h`) and Site A in full
    - `firestarter/src/proms/flash_intel.cpp` lines 10-20 and 185-210 -- proof it already includes `memory_utils.h` at :14, and Site B in full
    - `firestarter/src/proms/eprom.cpp` lines 110-125 and 783-815 -- `eprom_check_chip_id_execute` and its unconditional severity argument, `eprom_generic_init` and its FLAG_FORCE-derived argument at :791, and `eprom_internal_check_chip_id` at :795 in full
    - `firestarter/src/proms/eeprom_28c.cpp` lines 255-315 and 330-345 and 545-560 -- Site C in full including the stale citation at :264, plus the two other places in this file that reference `eeprom28c_check_chip_id` (around :338 and :551) so its linkage and call shape are known before editing
    - `firestarter/src/proms/memory.cpp` -- the helper authored in task 1, to confirm the parameter order before writing four call sites
    - `firestarter/tests/test_check_erase_no_vpp.py` and `firestarter/scripts/check_erase_no_vpp.py` -- both scan `eeprom_28c.cpp`; read enough to confirm this edit's region is outside what they pin
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §DEDUP-02 -- the per-site verbatim quotes and the divergence table, and open question 5's recommendation on the stale citation
  </read_first>
  <action>
Edit four source files. Still no commit -- task 3 commits.

SITE A -- `src/proms/flash_utils.cpp`, `flash_util_check_chip_id_execute`. Replace the guard,
the 4-byte payload and the fork with one call passing `handle`, the local chip id just read, and
`is_flag_set(FLAG_FORCE)` as `warn_only`. ADD `#include "memory_utils.h"` in the file's existing
include ordering -- it is currently absent and the declaration is required. Record in the
SUMMARY that this single added line shifts all 97 of this file's `.planning/` citations, that
it is one of the four include additions ROADMAP D-01 measures as causing 41 % of the milestone's
citation rework, and that the resulting staleness is expected and close-blocked by REMAP-04. Do
not remap anything.

SITE B -- `src/proms/flash_intel.cpp`, `flash_intel_check_chip_id`. Same replacement, same
`warn_only` derivation. No include needed; it is already at :14.

SITE C -- `src/proms/eeprom_28c.cpp`, `eeprom28c_check_chip_id`. Same replacement. Drop the
redundant `(uint16_t)` casts and the superfluous inner brace level along with the block --
divergences 2 and 3. Keep the function `static`; divergence 5 resolves to unchanged. While
inside the comment block at :260-267, repair the stale in-source citation at :264: it points at
`flash_intel.cpp:112-121` for a function that now lives at `flash_intel.cpp:153`. Per research
open question 5's recommendation, repair it here because it is one line inside a comment this
plan is editing anyway. Prefer naming the symbol (`flash_intel_check_chip_id`) over a new line
number, because Phases 157 and 158 will move that file again and Phase 159 remaps the composite
diff -- a symbol name cannot go stale. Record this in the SUMMARY as an INCIDENTAL fix with its
before and after text, explicitly not as scope creep and explicitly not as a `.planning/` remap.

SITE D -- `src/proms/eprom.cpp`, `eprom_internal_check_chip_id(handle, uint8_t error_code)`.
Keep the function's own signature exactly as it is, including the `error_code` parameter, and
keep both callers exactly as they are. Replace only the guard, payload and fork with one call
passing `handle`, the local chip id, and `error_code == RESPONSE_CODE_WARNING` as `warn_only`.
This is divergence 1's resolution and it is load-bearing: `eprom_check_chip_id_execute` (the
standalone `CMD_CHECK_CHIP_ID` entry near :119) passes `RESPONSE_CODE_ERROR` unconditionally
while `eprom_generic_init` at :791 passes the `FLAG_FORCE`-derived value, so the two callers have
different policies and the parameter is what preserves both. Do NOT simplify by having the
helper consult the flag.

Add a one-line comment at Site D recording why the `error_code` parameter survives -- that this
call site's two callers disagree on policy by design and the standalone chip-ID check refuses
regardless of `--force`. This is the only place a future reader would be tempted to "finish the
de-duplication" and break it, and there is no test that would catch them: state, in the SUMMARY,
that this claim's evidence is source-level -- the caller keeps passing an unconditional error
severity and the helper does not consult the flag -- and that no oracle exists for it.

Then run, recording every figure: `pio run -e uno`, `pio run -e uno328pb`,
`pio run -e leonardo`; `pio test -e native`; `pio test -e native_nodevtools`;
`pio test -e native_loop_v131`. Do NOT run `pytest tests/` yet -- the tree is dirty.

Record DEDUP-02's own flash and RAM delta per target against the figures plan 03 recorded. This
is what makes DEDUP-02's figure evidence rather than inheritance. Research measured -426 B for
DEDUP-01 and DEDUP-02 together and labels the -268 / -158 split UNVERIFIED at this position;
whatever this task measures IS DEDUP-02's verified figure. State whether it confirms -158 or
supersedes it, and state the combined total against `156-before-figures.md` -- expected
24234 / 24282 / 26378, i.e. -426 on all three targets with RAM unchanged. If the combined total
does not land there, stop and report rather than adjusting the claim.

Also record: the new `__udivmodhi4` count (research measured 13 after both halves), and the new
sizes of `flash_util_check_chip_id_execute`, `flash_intel_check_chip_id`, `eeprom28c_write_init`
and `eprom_check_chip_id_execute`, noting that `eprom_internal_check_chip_id` is expected to
disappear from the symbol table entirely because it gets inlined into its two callers. Expect
the per-symbol ledger NOT to close against the image delta -- research measured -624 B of symbol
deltas against -426 B, because that inlining bills 260 B into `main`. Do not hunt for a bug and
do not fudge the difference; record it as LTO redistribution with the cause named.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(grep -c 'mem_util_report_chip_id' src/proms/flash_utils.cpp src/proms/flash_intel.cpp src/proms/eprom.cpp src/proms/eeprom_28c.cpp | awk -F: '{s+=$2} END {print s}')" -eq 4 && grep -q '#include "memory_utils.h"' src/proms/flash_utils.cpp && test "$(grep -c 'uint8_t _b\[4\]' src/proms/flash_utils.cpp src/proms/flash_intel.cpp src/proms/eprom.cpp src/proms/eeprom_28c.cpp | awk -F: '{s+=$2} END {print s}')" -eq 0 && grep -q 'uint8_t error_code' src/proms/eprom.cpp && test "$(grep -c 'flash_intel.cpp:112-121' src/proms/eeprom_28c.cpp)" -eq 0 && pio run -e uno -e uno328pb -e leonardo 2>&1 | grep -E '^(RAM|Flash):' && pio test -e native 2>&1 | tee /tmp/n4b.log | tail -3 && grep -qE '172 test cases: 172 succeeded' /tmp/n4b.log && pio test -e native_loop_v131 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `mem_util_report_chip_id` is called exactly once in each of `src/proms/flash_utils.cpp`, `src/proms/flash_intel.cpp`, `src/proms/eprom.cpp` and `src/proms/eeprom_28c.cpp` -- four calls, one per site.
    - Source assertion: the 4-byte payload array declaration appears zero times across those four files. All four blocks are gone, not merely shortened.
    - `src/proms/flash_utils.cpp` now contains `#include "memory_utils.h"`, and its include ordering is otherwise unchanged.
    - `eprom_internal_check_chip_id` still takes its `uint8_t error_code` parameter, both of its callers are byte-unchanged (`eprom_check_chip_id_execute` still passing an unconditional error severity, `eprom_generic_init` still passing the `FLAG_FORCE`-derived value), and a comment at that site records why the parameter survives.
    - Source assertion: `grep -c 'flash_intel.cpp:112-121' src/proms/eeprom_28c.cpp` is 0, and the repaired comment names `flash_intel_check_chip_id` by symbol rather than by a new line number. The before and after text is quoted in the SUMMARY, labelled incidental.
    - `eeprom28c_check_chip_id` is still `static`, and the two other references to it in that file are byte-unchanged.
    - `pio run` reports flash of exactly `24234` (uno), `24282` (uno328pb), `26378` (leonardo) with RAM `1567` / `1573` / `2008` -- the combined -426 B on all three targets with RAM unchanged, matching `156-before-figures.md`. A deviation stops the plan.
    - `avr-objdump -d .pio/build/uno/firestarter_uno.elf | grep -cE '(r?call|jmp).*__udivmodhi4'` prints `13`, down from the recorded 31, and the `__udivmodsi4` count is 0.
    - `avr-nm --print-size --size-sort -C` no longer lists `eprom_internal_check_chip_id`, and the SUMMARY records the ledger as not closing (-624 B of symbol deltas against -426 B of image) with LTO inlining into `main` named as the cause.
    - `pio test -e native` and `pio test -e native_nodevtools` each report `172 test cases: 172 succeeded` over 17 suites, and the SUMMARY names `test_case7_mismatching_chip_id_with_force_warns` (with both of plan 02's id legs) and `test_migrated_mismatching_chip_id_errors` as having run and passed.
    - `pio test -e native_loop_v131` reports all cases succeeding.
    - `python3 scripts/check_build_warnings.py` reports no new warning on any of the four edited files.
    - `git -C firestarter diff --name-only` lists at most the six source/header files in `files_modified` plus the golden -- no test file, no `platformio.ini`, no `scripts/`, no `firestarter_app` path.
    - Source assertion: `grep -cE 'DEDUP-0[0-9]|Phase 156|OD-[0-9]' src/proms/flash_utils.cpp src/proms/flash_intel.cpp src/proms/eprom.cpp src/proms/eeprom_28c.cpp` is 0.
  </acceptance_criteria>
  <done>All four chip-ID blocks are gone, divergence 1 is preserved by parameterisation with its reason recorded at the site, the stale in-source citation is repaired incidentally, and the combined phase total of -426 B on all three targets is measured.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the golden for the second removed site, then commit once</name>
  <files>firestarter/tests/golden/protocol_branch_inventory.json</files>
  <read_first>
    - `firestarter/tests/golden/protocol_branch_inventory.json` -- its current state as plan 03 left it: `counts` at 22/1/21, the 22-site array, and the `recorded_by` list now five entries long including plan 03's
    - `firestarter/tests/test_protocol_branch_inventory.py` -- `_extract_predicates` at :277 and the four assertion legs at :398, :417, :443, :463
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-03-SUMMARY.md` -- the 22-site extraction plan 03 recorded, which is this task's before side
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §"Gate blast radius" -- the measured final target of 21/1/20, the measured line shifts of the survivors, and the explicit warning that this second predicate MOVED into `memory.cpp` and must not be recorded as deleted
    - `firestarter/tests/test_hv_routing_source_contract_v142.py` -- read its purpose statement, because "duplication merely relocated somewhere else" is precisely what that sibling gate exists to catch and the `recorded_by` prose must make the distinction it would look for
  </read_first>
  <action>
Re-derive the golden for the one site DEDUP-02 removes, then make the single commit for this
whole plan.

RE-DERIVE, never hand-edit and never arithmetic. Import
`tests/test_protocol_branch_inventory.py` by file location, run its `_extract_predicates`
against the edited `src/proms/eprom.cpp`, and write the result into `sites`. Set `counts` from
the extraction: expect `total_sites` 21, `protocol_keyed_sites` 1 (unchanged -- the
`switch (handle->protocol)` site at :70 is TABLE-05 and must stay at exactly one),
`other_sites` 20. If the extraction disagrees with 21, STOP and report. Compare the surviving
entries field by field against the 22-site extraction plan 03 recorded and assert that only
`line` moved; record which sites shifted and by how much. Research measured the final survivor
shifts as `:713` unchanged, `:736` to `:720`, and `:787`, `:790`, `:791`, `:815` moving to
`:758`, `:761`, `:762`, `:773` -- use them as a sanity check on the extraction, never as a
substitute for it, and record any divergence rather than forcing agreement.

METADATA, same four conventions as plan 03: `blob_shas['src/proms/eprom.cpp']` from
`git hash-object` on the WORKING TREE before staging; `recorded_at_head` set to this commit's
PARENT sha; `recorded_by` APPENDED to, never replaced; and every other `meta` key untouched,
including `blob_shas['src/proms/eprom_params.cpp']`.

THE NEW `recorded_by` ENTRY -- this is where the one genuinely dangerous sentence in this phase
gets written correctly. The removed predicate is the mismatch guard inside
`eprom_internal_check_chip_id`. It did NOT disappear: it was **relocated into
`mem_util_report_chip_id`'s early return in `src/proms/memory.cpp`, a file this gate does not
scan.** The prose must say that explicitly, and must NOT read as "a `chip_id` safety branch was
deleted from `eprom.cpp`", which would be materially misleading. State also that no site was
added, that nothing was re-keyed or re-tiered, that the surviving fields were machine-compared
as identical apart from line numbers, and that this closes the phase's two-commit sequence begun
by plan 03. Note in one sentence that the same relocation pattern is what the sibling gate
`tests/test_hv_routing_source_contract_v142.py` exists to catch, which is why the relocation is
named rather than glossed.

THEN COMMIT -- once, for the whole plan. Stage exactly `src/proms/memory.cpp`,
`include/memory_utils.h`, `src/proms/flash_utils.cpp`, `src/proms/flash_intel.cpp`,
`src/proms/eprom.cpp`, `src/proms/eeprom_28c.cpp` and
`tests/golden/protocol_branch_inventory.json`. Never `git add -A`, never `git add .`. Record the
pre-commit sha first and afterwards assert `git rev-list --count <PRE_SHA>..HEAD` equals 1,
anchored to that recorded sha and never to `HEAD~1`. Subject:
`refactor(156-04): one shared chip-ID mismatch report replaces four drifted copies`. The body
must state the measured per-target flash delta for DEDUP-02 and the combined -426 B total; that
severity stays a caller parameter so the standalone chip-ID check still refuses regardless of
`--force`; that the removed inventory predicate moved into `memory.cpp` rather than vanishing;
and that the `eeprom_28c.cpp` citation repair is incidental.

AFTER committing, run `python3 -m pytest tests/ -q` (at least 313 passed, 0 failed) --
this is the first point at which that suite is meaningful for this plan. Then
`python3 scripts/check_size_baseline.py --policy merge05`, recording its result and its
one-sidedness (D-03), and `python3 scripts/check_build_warnings.py`. If
`tests/test_checker_convention.py::test_scope_is_firmware_only` fails, check the directory name
before believing it.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test "$(python3 -c "import json;print(json.load(open('tests/golden/protocol_branch_inventory.json'))['counts']['total_sites'])")" -eq 21 && test "$(python3 -c "import json;print(json.load(open('tests/golden/protocol_branch_inventory.json'))['counts']['protocol_keyed_sites'])")" -eq 1 && test "$(python3 -c "import json;print(json.load(open('tests/golden/protocol_branch_inventory.json'))['counts']['other_sites'])")" -eq 20 && test "$(python3 -c "import json;print(len(json.load(open('tests/golden/protocol_branch_inventory.json'))['sites']))")" -eq 21 && test "$(python3 -c "import json;print(json.load(open('tests/golden/protocol_branch_inventory.json'))['meta']['blob_shas']['src/proms/eprom.cpp'])")" = "$(git hash-object src/proms/eprom.cpp)" && python3 -m pytest tests/ -q 2>&1 | tail -3 && test -z "$(git status --porcelain)" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" -eq 7</automated>
  </verify>
  <acceptance_criteria>
    - `tests/golden/protocol_branch_inventory.json` reads `total_sites` 21, `protocol_keyed_sites` 1, `other_sites` 20, with exactly 21 entries in `sites`, all produced by the module's own `_extract_predicates`.
    - `blob_shas['src/proms/eprom.cpp']` equals `git hash-object src/proms/eprom.cpp` on the committed file; `blob_shas['src/proms/eprom_params.cpp']` is byte-unchanged.
    - `recorded_at_head` equals this commit's PARENT sha.
    - `recorded_by` has exactly one new entry appended and every prior entry -- including plan 03's -- byte-unchanged. The new entry contains the words that the predicate was relocated into `mem_util_report_chip_id` in `src/proms/memory.cpp`, names that file as one this gate does not scan, and does NOT describe the predicate as deleted or removed from the codebase.
    - `meta.sources`, `meta.why_two_checks`, `meta.how_to_update`, `meta.frozen_for`, `meta.allowlist_rationale` and `meta.sweep_note` are byte-unchanged across both plan 03's and this plan's commits.
    - `python3 -m pytest tests/test_protocol_branch_inventory.py -q` passes all legs.
    - `python3 -m pytest tests/ -q`, run AFTER the commit, reports at least 313 passed and 0 failed.
    - `git rev-list --count <PRE_SHA>..HEAD` equals 1, anchored to the sha recorded before this plan's first edit.
    - `git show --stat HEAD` lists exactly seven paths, all of them in `files_modified`. No test file, no `platformio.ini`, no `scripts/`, no `firestarter_app` path, no `.planning/` path.
    - The commit subject begins `refactor(156-04):` and the body states the four things named in the action.
    - `python3 scripts/check_size_baseline.py --policy merge05` run and recorded with its one-sidedness stated; `scripts/baseline/size_baseline.json` byte-unchanged.
    - `git -C firestarter status --porcelain` empty after the commit.
  </acceptance_criteria>
  <done>The golden is re-derived from 22 sites to 21 with the relocation recorded as a relocation, and the whole of DEDUP-02 lands in exactly one commit that leaves every gate green and the phase at its measured -426 B total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | Untrusted JSON supplies the expected chip id the comparison uses, and the `--force` flag that selects severity |
| firmware -> host log frames | The 4-byte payload and its severity-bearing id cross here; both must be identical at all four sites |
| caller -> helper policy boundary | `warn_only` is where the divergent per-caller policy is preserved. Folding it inward silently changes one command's refusal semantics |
| source shape -> committed golden | The mismatch guard moving into `memory.cpp` removes a predicate from a gate that scans only `eprom.cpp` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-22 | Elevation of privilege | the standalone `CMD_CHECK_CHIP_ID` path | critical | mitigate | Folding `is_flag_set(FLAG_FORCE)` into the helper would make a command that refuses unconditionally today start honouring `--force`, letting a user program a part whose identity does not match. Prohibited outright, negative-grepped in the acceptance criteria, and a comment at Site D records why the parameter survives. Evidence is source-level -- no oracle exists, and that is stated |
| T-156-23 | Tampering | the chip-ID severity fork | critical | mitigate | Plan 02's id assertions in both directions plus the two pre-existing `response_code` cases are re-run and named. Both halves are required because the WARN and ERROR macros are the same alias |
| T-156-24 | Repudiation | recording the removed predicate as deleted | high | mitigate | The `recorded_by` prose must name the relocation and the destination file, an acceptance criterion checks for the relocation wording and against deletion wording, and the sibling HV-routing gate's purpose is cited as the reason |
| T-156-25 | Tampering | the redundant-cast removal changing behaviour | low | accept | Both operands are already `uint16_t`, verified at `eeprom_28c.cpp:288` and in `firestarter.h`, so the casts are provable no-ops with zero behaviour and zero size delta |
| T-156-26 | Repudiation | the per-symbol ledger not closing | low | accept | Expected under `-flto`: -624 B of symbol deltas against a -426 B image because `eprom_internal_check_chip_id` is inlined into `main`. Recorded with its cause, never fudged |
| T-156-27 | Repudiation | quoting -158 B as measured when it is not | medium | mitigate | This plan measures DEDUP-02's own delta in its own commit and states whether it confirms or supersedes the figure |
| T-156-28 | Tampering | the added `#include` shifting 97 planning citations | low | accept | Expected and measured; close-blocked by REMAP-04 (D-05). Explicitly not remapped here. The one in-source citation repaired is a pre-existing staleness in a comment this plan edits anyway |
| T-156-29 | Tampering | npm/pip/cargo installs | high | accept | No package installed; `lib_deps` untouched. RESEARCH's Package Legitimacy Audit records zero packages |
</threat_model>

<verification>
- `pio run -e uno -e uno328pb -e leonardo`: flash exactly 24234 / 24282 / 26378, RAM 1567 / 1573 / 2008.
- `avr-objdump -d` on the uno ELF: `__udivmodhi4` count exactly 13; `__udivmodsi4` count 0.
- `pio test -e native` and `pio test -e native_nodevtools`: 172/172 over 17 suites each.
- `pio test -e native_loop_v131`: all cases pass.
- `python3 -m pytest tests/ -q` AFTER the commit: at least 313 passed, 0 failed.
- `python3 scripts/check_build_warnings.py`: no new warning on the four edited source files.
- `python3 scripts/check_size_baseline.py --policy merge05`: run, recorded, one-sidedness recorded.
- Exactly one commit, exactly seven paths, `git rev-list --count <PRE_SHA>..HEAD` equals 1.
</verification>

<success_criteria>
One helper replaces four drifted blocks. All six divergences are enumerated with resolution and
reasoning, and the resolved single semantic is stated in the helper's own comment -- which is
what criterion 2 asks for. Divergence 1 is preserved by parameterisation so the standalone
chip-ID check still refuses regardless of `--force`, with the absence of an oracle for that
stated rather than papered over. The golden is re-derived to 21 sites with the relocation
recorded as a relocation. The phase total measures -426 B flash with RAM unchanged on all three
AVR targets, and `__udivmodhi4` is down to 13.
</success_criteria>

<output>
Create `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-04-SUMMARY.md` when done.
</output>