---
phase: 156-duplicated-report-extraction-boolean-convention-repair-firmw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/156-before-figures.md
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04]

must_haves:
  truths:
    - "Every pre-change figure this phase's four requirements are measured against exists in one committed file, captured on a tree proven unchanged, before any source edit."
    - "The record carries the seven corrections research made to ROADMAP.md and REQUIREMENTS.md, each with the measured value that replaces the stale one, so a later reader cannot reintroduce the stale figure."
    - "The reference carrier is named correctly: the branch the ROADMAP names does not hold this work, and the one that does is recorded with its sha and its measured applicability."
    - "Every number carries the verbatim command that produced it, so it can be re-derived rather than trusted."
    - "The four native/pytest baselines are recorded as counts AND as suite counts, so a later count movement is attributable rather than mysterious."
  artifacts:
    - .planning/v1.33/156-before-figures.md
  key_links:
    - "The eprom.cpp blob SHA recorded here is what proves tests/golden/protocol_branch_inventory.json is GREEN on arrival, which is what makes this phase's edit -- not some earlier drift -- the cause of the two RED legs plans 03 and 04 must clear."
    - "The __udivmodhi4 count of 31 is the denominator of DEDUP-01's only mechanical criterion; the ROADMAP's 30 would make the post-change 13 look like a 17-site fall instead of the measured 18."
    - "The three flash/RAM pairs are the ONLY baseline for DEDUP-04's size-identity oracle; without them the flip's zero-byte claim has no before side."
  prohibitions:
    - "Must NOT edit any file in firestarter/ -- this plan measures and records only. A single source edit invalidates every figure it captures."
    - "Must NOT re-anchor scripts/baseline/size_baseline.json (LAND-01 / Phase 158)."
    - "Must NOT repair the pre-existing BASE-01 native case-count mismatch (cases baseline=141 observed=172) that makes the canonical check_size_baseline.py --policy merge05 invocation exit 1 (LAND-03 / Phase 158)."
    - "Must NOT run the Phase 154 citation remap tool or repair any .planning/ file:LINE citation (REMAP-01..05 / Phase 159, milestone decision D-01)."
    - "Must NOT stage or re-pin the firestarter_app gitlink in the meta repo -- it shows modified from pre-existing Phase 154 drift and is operator-gated."
    - "Must NOT invent D-NN decision labels -- there is no 156-CONTEXT.md by operator decision OD-1, so no decision record exists to cite."
    - "Must NOT claim a cause for the 31st __udivmodhi4 call site unless it is measured (research assumption A1 is an inference, not a measurement)."
    - "Must NOT author or imply any bench/hardware criterion (milestone decision D-02)."
    - "Must NOT push any branch."
---

<objective>
Capture every pre-change measurement this phase's claims are made against, on a tree proven
unchanged, and commit it as the authoritative before-half record.

Purpose: five classes of figure here are irrecoverable once the first edit lands, and Phases
157 and 158 each invalidate them again. The three flash/RAM pairs are DEDUP-04's only before
side. The `__udivmodhi4` count is DEDUP-01's only mechanical criterion. The `eprom.cpp` blob
SHA is what proves the branch-inventory gate is green on arrival, which is what makes plans
03/04 the cause of its two RED legs rather than some earlier drift. And the ROADMAP carries
seven figures research measured wrong -- if they are not corrected in a file that says it
supersedes the ROADMAP, the next reader re-inherits them.

Output: `.planning/v1.33/156-before-figures.md`, committed in the meta repo, with every
number carrying the verbatim command that produced it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-PATTERNS.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md
@.planning/v1.33/155-before-figures.md
@.planning/v1.33/155-after-figures.md
</context>

## Multi-Source Coverage Audit (phase-wide, recorded once here)

Four source types audited. Every item is COVERED by a plan; nothing is MISSING.

**GOAL** -- ROADMAP.md §Phase 156: *"Collapse two report blocks that were copy-pasted four
times each -- and, at zero byte cost, remove the inverted-return convention that needed a
ten-line comment to defend itself."*

| Goal clause | Plan | Note |
|---|---|---|
| Collapse the VPP report block (4 copies) | 03 | |
| Collapse the chip-ID report block (4 copies) | 04 | |
| Remove the inverted-return convention | 05 | OD-2 resolves it toward removal |
| "at zero byte cost" | 05 | Corrected by C-4: size-identical on all three targets, image NOT identical |
| "a ten-line comment" | 05 | Corrected by C-6: only 3 of the 11 lines at `eprom_operations.cpp:57-63` are about the `!` |

**REQ** -- phase_req_ids `DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04`:

| ID | Plans | Note |
|---|---|---|
| DEDUP-01 | 01 (before-figures), 03 (the change), 07 (after-figures) | `__udivmodhi4` 31 (not 30) -> 13 |
| DEDUP-02 | 01, 04, 07 | six divergences enumerated, resolved semantic stated in 04 |
| DEDUP-03 | 02 (both blind spots), 03/04 (the fork survives), 07 (probes re-run post-change) | |
| DEDUP-04 | 01, 05 (the flip + both native cases), 06 (source-scan gate), 07 | |

**RESEARCH** -- `156-RESEARCH.md` items:

| Item | Plan |
|---|---|
| C-1 (blocks are in `flash_intel_check_vpp`, not `flash_intel_write_init`) | 01, 03 |
| C-2 (`__udivmodhi4` 31, not 30) | 01, 03, 07 |
| C-3 (the -268/-158 split is UNVERIFIED at this position) | 03, 04 measure each half; 07 records the outcome |
| C-4 (size-identical, NOT image-identical) | 05, 07 |
| C-5 (`.constprop.42`, not `.44`; never pin a clone suffix) | 05, 06 |
| C-6 (only `:65-67` of the comment is dead) | 05 |
| C-7 (DEAD-06's "only requirement touching a test file" is false) | 05, 07 |
| Prior art: `size-reduction-survey` does NOT carry this work; use `wip/v1.33-size-reduction-survey-preserved` @ `a6b46f8`; `git apply -C1` | 01 (re-check), 03, 04 |
| Six DEDUP-02 divergences + the resolved single semantic | 04 |
| Four planted probes A-D; blind spots 1 and 2 | 02 (close both), 07 (re-run post-change) |
| Golden re-derivation, `total_sites` 23 -> 21 | 03 (one site), 04 (one site) |
| The seven coverage ceilings | every plan |
| Pitfalls 1-12 | distributed; 7 (worktree name) in 02/06, 8 (dirty tree) in 03-07, 9 (flakiness) in 07 |
| Security V7 / severity-transposition threat | 02, 03, 04 threat models |
| Runtime state inventory: the golden is the only state this phase invalidates | 03, 04 |
| A1 (31st site attribution) | 01 -- confirmed or left unclaimed, never asserted |
| A3 (adding v131 cases trips no gate) | 02 -- confirmed empirically |
| A4 (id-capture facility exists) | RESOLVED by PATTERNS before planning; 02 uses it |
| OQ-2 (one commit or two) | 03/04 -- two, and the golden rides each |
| OQ-3 (DEDUP-04 last, alone) | 05 |
| OQ-4 (source-scan gate: yes) | 06 |
| OQ-5 (`eeprom_28c.cpp:265` stale citation) | 04 -- repaired as incidental |

**CONTEXT** -- there is no `156-CONTEXT.md`, by operator decision OD-1. The operator's two
binding decisions are covered: OD-1 (no invented D-NN labels) in every plan's prohibitions;
OD-2 (DEDUP-04 is REMOVE, the decline branch is dead) in plan 05. Milestone decisions D-01
through D-05 appear as scope fences in every plan's prohibitions.

**Exclusions, not gaps:** re-anchoring `size_baseline.json` (Phase 158 / LAND-01), the BASE-01
case-count mismatch (Phase 158 / LAND-03), `json_parser.c`'s missing `algorithm` range check
(Phase 157 / DECODE-05), the `.planning/` citation remap (Phase 159 / REMAP-01..05), shipping
raw mV over the wire (a wire change, out of scope by criterion 1), and the binary command
protocol (v1.28 / Backlog 999.35).

## Wave 0 disposition -- where each MISSING reference landed, and why two moved

`156-VALIDATION.md` lists six required Wave 0 items plus two optional. All six required items are
covered and both optional items are dispositioned. Three of them deliberately do NOT sit in an
early wave, and the reason is the same in every case: placing them earlier would leave a CI leg
red at an intermediate commit.

| VALIDATION Wave 0 item | Plan | Wave | Disposition |
|---|---|---|---|
| Pre-change before-figures, before any edit | 01 | 1 | As specified. Irrecoverable afterwards |
| `test_vpp_eprom_v131` under-voltage pairing (blind spot 1) | 02 | 2 | As specified, and deliberately BEFORE the refactor so the assertion is regression evidence rather than a description of the new code |
| `test_eeprom28c_sdp` chip-ID message id (blind spot 2) | 02 | 2 | As specified, same reasoning. The id-capture facility exists (open question 1 resolved by PATTERNS), so this lands in CI with zero new infrastructure |
| Case 24's polarity flip | 05 | 5 | **MOVED, with cause.** Case 24 fails by construction only AFTER the flip. Flipping its assertion in an earlier wave would make it fail BEFORE the flip, leaving both CI native legs red for three commits. It must ride the same commit as the six engine returns |
| Case 25's de-vacuuming | 05 | 5 | **MOVED, with cause.** Same: the loop's polarity is only wrong after the flip, and the four-call assertion is what stops the case going vacuous at exactly that moment |
| `protocol_branch_inventory.json` re-derivation | 03 and 04 | 3, 4 | **MOVED and SPLIT, with cause.** The golden's own `meta.recorded_by` requires the source change and the golden to land in the same commit. DEDUP-01 removes exactly one site and DEDUP-02 exactly one other, so each plan re-derives its own site inside its own commit. This is what lets research open question 2's two-commit recommendation coexist with the one-commit property -- the gate is green at every commit AND each requirement keeps its own measured figure |
| *(optional)* a source-scan gate for DEDUP-04 | 06 | 6 | **TAKEN**, per open question 4's recommendation, with the mandatory non-vacuity legs. It cannot precede plan 05 because it asserts the post-flip state |
| *(optional)* a payload-**value** oracle | -- | -- | **NOT TAKEN.** No criterion requires it. The gap stays visible as coverage ceiling 4 in every plan of this phase and in the phase record, which is exactly what VALIDATION asked for |

## The honest coverage ceilings -- stated, not implied

These seven appear in every plan of this phase and in every SUMMARY, in these terms.

1. **`src/eprom_operations.cpp` compiles in NO native environment.** All native envs share
   `build_src_filter = +<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c>
   +<operation_utils.cpp>`. The nine dropped negations therefore have **no behavioural
   oracle** -- only the size-identity build and source inspection. `src/operation_utils.cpp`
   **is** in the filter, so the six flipped engine returns ARE covered -- by Cases 24 and 25
   and nothing else.
2. **`test_vpp_eprom_v131` and `test_flash_intel_vpp` are in no CI leg.** The former runs only
   under `native_loop_v131`, whose own env comment says NO CI COVERAGE. The latter is in no
   env's `test_filter` at all. So `flash_intel.cpp`'s VPP path -- two of DEDUP-01's four
   blocks -- has zero executing coverage today; its regression evidence is the `eprom.cpp`
   twin's plus the source-level identity of the two blocks. (Plan 02 attempts to narrow this;
   whichever way that lands, plan 07 records the resulting wording.)
3. **The AVR 16-bit promotion is unobservable natively.** Native `int` is 32-bit, so no native
   test can attest the AVR arithmetic or its wrap above 65485 mV. Identical before and after;
   not a regression; **not covered**.
4. **The 8-byte payload's byte VALUES have no oracle anywhere.** `test_vpp04_a` asserts the
   payload LENGTH is 8. Criterion 1's "the emitted 8-byte payload is unchanged" is established
   by source-level identity of the arithmetic (extracted expressions character-identical to
   the four originals, parameter types identical) plus that length assertion. Say so.
5. **`scripts/check_size_baseline.py` runs in no CI workflow at all** (LAND-04). Every size
   gate in this phase is a local-run obligation.
6. **No bench claim** (D-02). Nothing in this phase is attested on silicon.
7. **DEDUP-04's zero-byte result is a SIZE claim, not an IMAGE claim.** The `.hex` SHA changes
   on all three targets and `avr-objdump` differs on 5450 lines (a uniform +2 B relocation
   plus branch-polarity swaps). The build was proven reproducible first, so this is a real
   negative result. An oracle asserting image identity would go RED. The claim that may be
   made is size-identity on all three targets.

## Artifacts this phase produces

Every symbol, file and record Phase 156 creates. Repeated verbatim in every plan so a
source-grounding pass can exclude newly-created symbols from drift verification.

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/156-before-figures.md` | 01 | meta |
| New case(s) in `test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp`: `test_vpp04_e_undervoltage_warning_pairing_fires_by_id_with_payload_shape` and (if reachable) `test_vpp04_f_flash_intel_undervoltage_warning_pairing` | 02 | fw |
| New assertions inside `test_case7_mismatching_chip_id_with_force_warns` (id legs + an ERROR-direction anti-hollow re-drive) | 02 | fw |
| `mem_util_report_voltage` -- definition in `src/proms/memory.cpp`, declaration in `include/memory_utils.h` | 03 | fw |
| Local `force` at the four VPP call sites (`eprom.cpp` x2, `flash_intel.cpp` x2) | 03 | fw |
| `mem_util_report_chip_id` -- definition in `src/proms/memory.cpp`, declaration in `include/memory_utils.h` | 04 | fw |
| `#include "memory_utils.h"` added to `src/proms/flash_utils.cpp` | 04 | fw |
| Re-derived `tests/golden/protocol_branch_inventory.json`: `counts` 23/1/22 -> 22/1/21 (plan 03) -> 21/1/20 (plan 04), refreshed `blob_shas['src/proms/eprom.cpp']`, `recorded_at_head`, and two appended `recorded_by` entries | 03, 04 | fw |
| `tests/test_boolean_convention_source_contract_v133.py` and its module symbols | 06 | fw |
| `.planning/v1.33/156-after-figures.md` (the phase record) | 07 | meta |

<tasks>

<task type="auto">
  <name>Task 1: Assert the tree is unchanged, then measure every irrecoverable pre-change figure</name>
  <files>
    (no file is edited by this task -- it builds, tests and measures, then hands values to
    task 2; build products under firestarter/.pio/build/ are gitignored)
  </files>
  <read_first>
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` -- sections "Measured Figures" (all sub-sections), "Corrections to ROADMAP / REQUIREMENTS" (C-1 through C-7), "Prior Art", "Common Pitfalls" (all twelve), "Environment Availability"
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md` -- "Measured baseline at adf1a31", "Wave 0 Requirements", the eight phase-gate legs
    - `.planning/v1.33/155-before-figures.md` -- the record convention this task's output copies: frontmatter shape, the `| Field | Value |` git-anchor table, WARM labelling, and the rule that each number carries the verbatim command that produced it
    - `firestarter/src/proms/eprom.cpp` lines 705-760 -- the two VPP blocks being measured, and the trailing `EPROM_HV_ALL_OFF_MASK` clear that must not be disturbed later
    - `firestarter/src/proms/flash_intel.cpp` lines 26-80 -- proof of C-1: the two blocks are lexically inside `static void flash_intel_check_vpp`, which begins at :26, NOT inside `flash_intel_write_init` at :106
    - `firestarter/tests/golden/protocol_branch_inventory.json` -- its `meta` block (`sources`, `blob_shas`, `recorded_at_head`, `recorded_by`, `how_to_update`) and its `counts`
    - `/workspaces/CLAUDE.md` and `/workspaces/firestarter/CLAUDE.md`
  </read_first>
  <action>
Run everything in `/workspaces/firestarter` unless a step says otherwise. This task edits no
tracked file. If any assertion in step 1 fails, STOP and report -- every figure below is only
meaningful against an unchanged tree.

STEP 1 -- anchor the state. Record `FW_PRE_SHA` as the full sha from `git rev-parse HEAD`
(expected to abbreviate to `adf1a31`). Assert `git branch --show-current` is
`gsd/v1.33-source-hygiene-firmware-size-reduction`. Assert `git status --porcelain` is empty
and `git diff --name-only HEAD` is empty. Separately record the meta repo's HEAD sha, and
note that `firestarter_app`'s gitlink shows modified in the meta repo: that is pre-existing
Phase 154 drift, it is operator-gated, and no task in this phase may stage or re-pin it.

STEP 2 -- warm AVR builds, all three targets. Run `pio run -e uno`, `pio run -e uno328pb`,
`pio run -e leonardo`, capturing each log under the session scratch directory. Record the
`Flash:` and `RAM:` byte figures per target. Expected: 24660/1567, 24708/1573, 26804/2008.
Also record Leonardo Caterina headroom against the 28672 B cliff (expected 1868 B). Label
every figure WARM and state that the cold re-record is LAND-01 / Phase 158's, not this
phase's.

STEP 3 -- per-symbol sizes. `avr-nm` and `avr-objdump` are NOT on PATH; invoke them as
`~/.platformio/packages/toolchain-atmelavr/bin/avr-nm` and `...avr-objdump`. Run
`avr-nm --print-size --size-sort -C .pio/build/uno/firestarter_uno.elf` and record the sizes
of `eprom_check_vpp` (expected 524, `0x20c`), `flash_intel_write_init` (expected 562,
`0x232`), `flash_util_check_chip_id_execute` (expected 192), `flash_intel_check_chip_id`
(expected 220), `eeprom28c_write_init` (expected 430), `eprom_internal_check_chip_id`
(expected 260), `eprom_check_chip_id_execute` (expected 6), and
`op_execute_stateful_operation.constprop.42` (expected 216, `0xd8`). Record the C-1 nuance
explicitly: `flash_intel_check_vpp` is `static` and fully inlined, so it has no symbol and
its bytes are billed to `flash_intel_write_init` -- the symbol-table attribution and the
lexical location are two different true facts, and the requirement text conflates them.
Record the C-5 nuance: the clone suffix is `.constprop.42` today, and no gate in this phase
may pin a clone suffix.

STEP 4 -- the `__udivmodhi4` call-site count. Run
`avr-objdump -d .pio/build/uno/firestarter_uno.elf | grep -cE '(r?call|jmp).*__udivmodhi4'`.
Expected **31**. Record it as the corrected figure and state in one sentence that ROADMAP
criterion 1 and REQUIREMENTS DEDUP-01 both say 30, that this record supersedes them (C-2),
and that the derived claim "those four blocks held 24 of them" is unaffected. Do NOT assert a
cause for the 31st site: research assumption A1 attributes it to Phase 155's 32-bit voltage
reformulation but that is an inference. Either measure it (count the same way against
`46dd574`'s parent) and record the result, or record the count with no cause claimed. Both
are acceptable; asserting an unmeasured cause is not.

STEP 5 -- test baselines. Run, one at a time, `pio test -e native`,
`pio test -e native_nodevtools`, `pio test -e native_loop_v131`, and
`python3 -m pytest tests/ -q` (system python3, not the pio penv). Record for each: the case
count, the suite count, pass/fail/skip, and wall time. Expected: 172/172 over 17 suites;
172/172 over 17 suites; 80/80 over 2 suites with the per-suite split
(`test_loop_eprom_v131` 47, `test_vpp_eprom_v131` 33); and 313 passed / 0 failed / 32 skipped.
Record that `native_loop_v131` is in NO CI leg (its env comment says so) and that the four CI
legs are exhaustively `pio test -e native`, `pio test -e native_nodevtools`,
`pytest tests/ -v`, `pio run`.

STEP 6 -- the golden's arrival state. Run `git hash-object src/proms/eprom.cpp` and compare
against `blob_shas['src/proms/eprom.cpp']` in `tests/golden/protocol_branch_inventory.json`.
Expected: both `838aca47986103969be4caca3cef71a033bac069`, i.e. the gate is GREEN on arrival.
Record the golden's `counts` (expected `total_sites` 23, `protocol_keyed_sites` 1,
`other_sites` 22) and, separately, the live extraction: import
`tests/test_protocol_branch_inventory.py` by file location, run its own `_extract_predicates`
against `src/proms/eprom.cpp`, and record the site count and each site's line and predicate.
This is the pre-change inventory plans 03 and 04 will each re-derive one site out of. Never
hand-edit the golden and never hand-count.

STEP 7 -- reference-carrier applicability (research A5, re-checked at plan time). Assert
`git rev-parse wip/v1.33-size-reduction-survey-preserved` abbreviates to `a6b46f8`. Assert
that `git diff HEAD size-reduction-survey -- src/proms/eprom.cpp src/proms/flash_intel.cpp
include/memory_utils.h` is EMPTY, which is the measured proof that the branch named in
ROADMAP §v1.33 does not carry this work. Then rebuild the six-file Phase-156 subset patch
from `.planning/notes/firmware-size-reduction-measured.patch` using the extraction script in
RESEARCH §Code Examples (keep `include/memory_utils.h`, `src/proms/eeprom_28c.cpp`,
`src/proms/eprom.cpp`, `src/proms/flash_intel.cpp`, `src/proms/flash_utils.cpp`, and only the
`@@ -230,6 +230,52 @@` hunk of `src/proms/memory.cpp`) and record the results of
`git apply --check` (expected: fails on `src/proms/eeprom_28c.cpp` only, on one swept
trailing context line) and `git apply --check -C1` (expected: clean). Do NOT apply the patch
in this plan. Leave the tree clean and re-assert `git status --porcelain` is empty at the end.
Note that neither carrier holds DEDUP-04 -- it exists nowhere and plan 05 authors it -- and
that neither can be cherry-picked, because both fork `8695ee5`, before Phase 154's sweep.

STEP 8 -- record the one-sided size gate, without running it as a phase gate yet. State in
one sentence that `check_size_baseline.py:697` is `if flash_delta > allowance` and `:709` is
`if ram_delta > ram_tolerance`, so a reduction passes with no named exemption (D-03), and that
the canonical `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json`
invocation is ALREADY exiting non-zero on `beta` for the unrelated pre-existing reason
`native: cases baseline=141 observed=172` (BASE-01 frozen at Phase 124) -- Phase 158 /
LAND-03's problem, not this phase's, and not to be "fixed" here.

Hand every value to task 2. Do not write the record file in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git rev-parse --abbrev-ref HEAD)" = "gsd/v1.33-source-hygiene-firmware-size-reduction" && NM=~/.platformio/packages/toolchain-atmelavr/bin/avr-nm && OD=~/.platformio/packages/toolchain-atmelavr/bin/avr-objdump && pio run -e uno -e uno328pb -e leonardo 2>&1 | grep -E '^(RAM|Flash):' && test "$($OD -d .pio/build/uno/firestarter_uno.elf | grep -cE '(r?call|jmp).*__udivmodhi4')" -eq 31 && test "$(git hash-object src/proms/eprom.cpp)" = "838aca47986103969be4caca3cef71a033bac069" && $NM --print-size --size-sort -C .pio/build/uno/firestarter_uno.elf | grep -E 'eprom_check_vpp|flash_intel_write_init' && git apply --check -C1 /tmp/dedup156.patch && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter status --porcelain` is empty at the start AND at the end of this task, and `git -C firestarter rev-parse HEAD` abbreviates to `adf1a31`.
    - `pio run` reports Flash/RAM of exactly `24660`/`1567` (uno), `24708`/`1573` (uno328pb), `26804`/`2008` (leonardo). Any deviation stops the plan rather than being recorded as the new baseline.
    - `avr-objdump -d .pio/build/uno/firestarter_uno.elf | grep -cE '(r?call|jmp).*__udivmodhi4'` prints exactly `31`.
    - `avr-nm --print-size --size-sort -C` on the uno ELF reports `eprom_check_vpp` at `0x20c` (524) and `flash_intel_write_init` at `0x232` (562); `flash_intel_check_vpp` is ABSENT from the symbol table (it is static and inlined -- the C-1 proof).
    - Exactly one `op_execute_stateful_operation.constprop.NN` clone exists and its suffix is recorded as `42`, together with the statement that no gate in this phase pins a clone suffix.
    - `git hash-object src/proms/eprom.cpp` equals `838aca47986103969be4caca3cef71a033bac069`, which equals the golden's recorded `blob_shas['src/proms/eprom.cpp']` -- the gate is proven GREEN on arrival.
    - The golden's `counts` read `total_sites` 23, `protocol_keyed_sites` 1, `other_sites` 22, and the live run of the module's own `_extract_predicates` yields 23 sites whose lines and predicates are recorded individually.
    - `pio test -e native` reports `172 test cases: 172 succeeded` over 17 suites; `pio test -e native_nodevtools` the same; `pio test -e native_loop_v131` reports 80/80 over 2 suites with the 47 + 33 per-suite split; `python3 -m pytest tests/ -q` reports 313 passed, 0 failed, 32 skipped.
    - `git -C firestarter diff HEAD size-reduction-survey -- src/proms/eprom.cpp src/proms/flash_intel.cpp include/memory_utils.h` produces empty output, and `git rev-parse wip/v1.33-size-reduction-survey-preserved` abbreviates to `a6b46f8`.
    - `git apply --check` on the rebuilt six-file subset fails naming `src/proms/eeprom_28c.cpp` and no other file; `git apply --check -C1` on the same subset exits 0. Neither is applied.
    - No file under `firestarter/` outside `.pio/build/` differs from HEAD when the task ends.
  </acceptance_criteria>
  <done>Every irrecoverable pre-change figure is measured on a tree proven unchanged before and after, the golden is proven green on arrival, the reference carrier is proven applicable with `-C1`, and nothing has been edited.</done>
</task>

<task type="auto">
  <name>Task 2: Write and commit the before-figures record</name>
  <files>.planning/v1.33/156-before-figures.md</files>
  <read_first>
    - `.planning/v1.33/155-before-figures.md` -- copy its frontmatter keys, its `## 1. Git anchors` table shape, its WARM labelling, and its per-figure "verbatim command" discipline
    - `.planning/v1.33/155-after-figures.md` -- how a v1.33 record states a correction so it supersedes the ROADMAP rather than merely disagreeing with it
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` -- C-1 through C-7, verbatim, so no correction is paraphrased into vagueness
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md` -- the seven coverage ceilings and the forbidden phrasings, both of which this record must respect
    - `.planning/ROADMAP.md` §Phase 156 and `.planning/REQUIREMENTS.md` §3 -- the exact stale sentences being superseded, so the record can quote what it replaces
  </read_first>
  <action>
Create `.planning/v1.33/156-before-figures.md` in the meta repo. Filenames in this directory
are lowercase-hyphenated with the phase number prefixed; follow that.

Frontmatter keys, matching `155-before-figures.md`: `title`, `phase`
(`156-duplicated-report-extraction-boolean-convention-repair-firmw`), `plan` (`"01"`),
`measured` (today's date), `status` (state that this file is the ONLY source for Phase 156's
before-half figures and that Phases 157 and 158 each invalidate the measurements captured
here), `supersedes` (name ROADMAP.md §Phase 156 criteria 1 and 4 and REQUIREMENTS.md
DEDUP-01/DEDUP-04 prose wherever they state a figure this file corrects, C-1 through C-7),
and `requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04]`.

Sections, in this order:

1. **Git anchors** -- a `| Field | Value |` table: `FW_PRE_SHA` (full sha), branch,
   `git status --porcelain` empty (stated as measured, both before and after task 1),
   `git worktree list`, the meta repo HEAD, and the note that the `firestarter_app` gitlink
   shows modified from pre-existing Phase 154 drift and is not this phase's to touch.
2. **AVR image figures, WARM** -- the three flash/RAM pairs with the verbatim `pio run`
   command, plus Leonardo Caterina headroom against 28672. State that WARM matches the
   Phase 155 convention and that the cold re-record is LAND-01 / Phase 158's.
3. **Per-symbol ledger** -- the eight symbol sizes from task 1 step 3, with the verbatim
   `avr-nm` invocation including its full toolchain path. Add the C-1 paragraph: the two
   `flash_intel.cpp` VPP blocks are lexically inside `static void flash_intel_check_vpp`
   (which begins at `src/proms/flash_intel.cpp:26` and has no symbol because it is inlined),
   NOT inside `flash_intel_write_init` as criterion 1 and DEDUP-01 both say; the symbol-table
   attribution and the lexical location are two different true facts and the requirement
   conflates them. Add the C-5 paragraph: exactly one clone exists, its suffix is
   `.constprop.42` not `.44`, and no gate in this phase pins a clone suffix. Add the
   LTO warning: the ledger will NOT close after the change (research measured -624 B of
   symbol deltas against a -426 B image, because `eprom_internal_check_chip_id` stops
   existing as a symbol and is inlined into `main`), so the image figure is the phase total
   and the ledger is corroboration only.
4. **`__udivmodhi4` call sites** -- the figure 31, the verbatim `avr-objdump` command, and the
   C-2 correction stated as superseding both the ROADMAP and REQUIREMENTS, with the surviving
   derived claim ("those four blocks held 24 of them") named as unaffected. Record whether the
   31st site's cause was measured or left unclaimed -- never assert A1's inference as fact.
5. **Test and gate baselines** -- the four legs with counts, suite counts and wall times; the
   statement that `native_loop_v131` is in NO CI leg; and the exhaustive four-item CI list
   with its workflow line references.
6. **The golden's arrival state** -- the live blob SHA, the recorded blob SHA, the fact they
   match so the gate is GREEN on arrival and this phase's edit is what breaks it, the recorded
   `counts`, and the 23-site live extraction with each site's line and predicate. State the
   one-commit property the golden's own `meta.recorded_by` documents, and state the plan-level
   resolution: because DEDUP-01 removes exactly one site and DEDUP-02 removes exactly one
   other, each of plans 03 and 04 re-derives the golden inside its own commit, so the gate is
   green at every commit AND each requirement keeps its own measured figure.
7. **Reference carriers** -- `size-reduction-survey` proven by empty diff NOT to carry this
   work; `wip/v1.33-size-reduction-survey-preserved` @ `a6b46f8` recorded as the one that
   does; neither carries DEDUP-04; neither can be cherry-picked (both fork `8695ee5`, before
   Phase 154's sweep) so both are semantic references only; and the measured
   `git apply --check` / `--check -C1` results with the single obstructing file named.
8. **The one-sided size gate and the pre-existing red** -- D-03's two source line references,
   the statement that a reduction needs no named exemption, and the BASE-01 pre-existing
   non-zero exit with its cause, explicitly handed to Phase 158 / LAND-03.
9. **The seven coverage ceilings**, verbatim in the terms this plan states them.
10. **The corrections index** -- a table of C-1 through C-7: what the ROADMAP or REQUIREMENTS
    says, what is measured, and which plan carries the corrected figure forward. Add a row
    for the planner-found eighth item: `src/operation_utils.cpp:58` carries the same
    `@return true if the operation is still ongoing ... false when fully completed` doxygen
    text as `include/operation_utils.h:73`, so DEDUP-04's comment blast radius is SEVEN
    locations, not the six research enumerated. Label it as found at plan time, measured by
    `grep -n '@return' src/operation_utils.cpp include/operation_utils.h`.

Write no planning-provenance token into any firmware source file -- this task writes only
into `.planning/`. Use ` -- ` rather than an em dash. Do not use any of the phrasings
`156-VALIDATION.md` forbids: this record must not describe the DEDUP-04 flip as producing an
identical image, must not say the payload bytes were compared, must not describe anything
living only in `native_loop_v131` as covered by CI, and must not use "tested",
"verified on hardware" or "bench-verified" for the nine wrapper negations.

Then commit in the META repo, staging exactly `.planning/v1.33/156-before-figures.md` and
nothing else -- never `git add -A`, never `git add .`. Subject:
`docs(156-01): capture the pre-change before-figures and the seven ROADMAP corrections`.
The body must state that no firmware file was edited and that the figures are WARM. Do not
push.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/156-before-figures.md && grep -q '24660' .planning/v1.33/156-before-figures.md && grep -q '24708' .planning/v1.33/156-before-figures.md && grep -q '26804' .planning/v1.33/156-before-figures.md && grep -q '838aca47986103969be4caca3cef71a033bac069' .planning/v1.33/156-before-figures.md && grep -q 'a6b46f8' .planning/v1.33/156-before-figures.md && grep -q 'constprop.42' .planning/v1.33/156-before-figures.md && grep -q 'flash_intel_check_vpp' .planning/v1.33/156-before-figures.md && test "$(git log --oneline -1 -- .planning/v1.33/156-before-figures.md | wc -l)" -eq 1 && test -z "$(git status --porcelain .planning/)" && test -z "$(git -C firestarter status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/156-before-figures.md` exists, has frontmatter carrying all seven keys named in the action, and its `requirements` key lists all four DEDUP ids.
    - The file contains the literals `24660`, `1567`, `24708`, `1573`, `26804`, `2008`, `1868`, `524`, `562`, `31`, `13`, `24`, `172`, `17`, `80`, `47`, `33`, `313`, `32`, `23`, `22`, `838aca47986103969be4caca3cef71a033bac069`, `a6b46f8`, `8695ee5` and `constprop.42`.
    - The file names `flash_intel_check_vpp` as the lexical home of the two blocks and states that `flash_intel_write_init` is the symbol-table attribution -- both, as two different true facts.
    - The file states the `__udivmodhi4` count as 31 and explicitly says the ROADMAP and REQUIREMENTS say 30 and are superseded; the derived "24 of them" claim is named as unaffected.
    - The file states either a measured cause for the 31st site or that no cause is claimed. Source assertion: the file contains no sentence attributing the 31st site to Phase 155 without an accompanying measurement.
    - The file records the golden as GREEN on arrival with both SHAs quoted, and states the per-commit re-derivation resolution (one site in plan 03, one in plan 04) as the reason the gate is green at every commit.
    - The file states the reference-branch correction with its empty-diff proof, records `a6b46f8` as the carrier, and states that neither carrier holds DEDUP-04 and neither is cherry-pickable.
    - The file carries all seven coverage ceilings and a corrections table with rows C-1 through C-7 plus the planner-found seventh comment location (`src/operation_utils.cpp:58`).
    - `git -C /workspaces log --oneline -1 -- .planning/v1.33/156-before-figures.md` shows a commit whose subject begins `docs(156-01):`, and `git -C /workspaces show --stat HEAD` lists exactly one path.
    - `git -C firestarter status --porcelain` is empty and `git -C firestarter rev-parse HEAD` still abbreviates to `adf1a31` -- no firmware commit was made by this plan.
    - No path under `firestarter_app/` is staged, and the `firestarter_app` gitlink is not in the commit's file list.
  </acceptance_criteria>
  <done>The before-half record is committed in the meta repo, every figure carries its command, all seven ROADMAP corrections plus the planner-found eighth are stated as superseding, and no firmware file has been touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host -> firmware serial line | Untrusted JSON commands cross here; unchanged by this plan (measurement only) |
| firmware -> host log frames | Severity-bearing message ids cross here; this plan records the baseline that later plans are compared against |
| planning record -> future reader | A wrong figure recorded here is inherited silently by Phases 157, 158 and 159 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-01 | Repudiation | `.planning/v1.33/156-before-figures.md` | high | mitigate | Every figure carries the verbatim command that produced it, and the tree is proven clean before AND after measurement, so no number can be a post-hoc reconstruction |
| T-156-02 | Tampering | measurement on a dirty tree | high | mitigate | `git status --porcelain` asserted empty at task start and task end; any non-empty result stops the plan rather than being recorded |
| T-156-03 | Repudiation | the stale ROADMAP figures (30 sites, `flash_intel_write_init`, `.constprop.44`, byte-identical image) | medium | mitigate | A corrections table with `supersedes` frontmatter, so the stale figure cannot be re-inherited by reading the ROADMAP alone |
| T-156-04 | Repudiation | asserting A1's inferred cause for the 31st `__udivmodhi4` site as measured fact | low | mitigate | The action forbids it explicitly and an acceptance criterion negative-checks for it; either measure the cause or claim none |
| T-156-05 | Tampering | accidental patch application during the `git apply --check` step | medium | mitigate | Only `--check` is run, and `git status --porcelain` is re-asserted empty afterwards |
| T-156-06 | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this phase; `platformio.ini` `lib_deps` is untouched. RESEARCH's Package Legitimacy Audit records zero packages, so no `[ASSUMED]`/`[SUS]` checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty, HEAD still `adf1a31`, branch unchanged.
- `git -C /workspaces log --oneline -1` subject begins `docs(156-01):` and touches exactly one path.
- Every figure in the acceptance criteria present in `.planning/v1.33/156-before-figures.md`.
- The golden gate still green: `python3 -m pytest tests/test_protocol_branch_inventory.py -q` passes (nothing was edited).
</verification>

<success_criteria>
The before-half record exists and is committed; the tree is provably unchanged; the golden is
provably green on arrival; the reference carrier is provably applicable with `-C1`; and the
seven ROADMAP corrections plus the planner-found seventh comment location are recorded in a
file whose frontmatter says it supersedes the ROADMAP prose.
</success_criteria>

<output>
Create `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-01-SUMMARY.md` when done.
</output>
