---
phase: 156-duplicated-report-extraction-boolean-convention-repair-firmw
plan: 07
type: execute
wave: 7
depends_on: ["156-05", "156-06"]
files_modified:
  - .planning/v1.33/156-after-figures.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04]

must_haves:
  truths:
    - "All eight phase-gate legs are run on the final tree and their results recorded, including the two that are local-run obligations because no CI workflow invokes them."
    - "The four planted severity transpositions are re-run against the POST-refactor tree, so DEDUP-03's evidence covers the shipped code and not only the code the tests were written against."
    - "Every figure this phase claims is measured on the final tree and recorded against the before-figures, and every ROADMAP or REQUIREMENTS figure the measurements correct is named as superseded."
    - "The -268 / -158 split is resolved one way or the other by the two per-requirement measurements plans 03 and 04 made, and is never quoted as measured if it was not."
    - "The seven coverage ceilings are restated in their final form, including whichever way plan 02's flash_intel reachability attempt landed."
    - "All four requirement checkboxes are flipped with their discharge evidence cited, and the traceability table matches."
  artifacts:
    - .planning/v1.33/156-after-figures.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - "The post-refactor probe re-run is what closes the gap between 'the tests were written against the old code and pass against the new' and 'the tests can see a transposition in the new code'. Without it plan 02's evidence is about a tree that no longer exists."
    - "The after-figures record is the file Phases 157, 158 and 159 read. Every correction not recorded there gets re-inherited from the ROADMAP, which is what happened to the 30-site figure this phase had to correct."
    - "check_size_baseline.py runs in no CI workflow at all, so recording the gate run AND its one-sidedness is what stops a later reader treating a green run as evidence that nothing moved."
  prohibitions:
    - "Must NOT edit any file inside firestarter/. This plan measures the committed tree and writes only into .planning/."
    - "Must NOT re-anchor scripts/baseline/size_baseline.json (Phase 158 / LAND-01), and must NOT repair the BASE-01 native case-count mismatch (Phase 158 / LAND-03). Run the gate, record the pre-existing failure with its cause and its owner."
    - "Must NOT fix json_parser.c's missing algorithm range check (Phase 157 / DECODE-05)."
    - "Must NOT run the citation remap tool or repair any .planning/ file:LINE citation (Phase 159 / REMAP-01..05, D-01). The staleness this phase created is expected and close-blocked by REMAP-04."
    - "Must NOT remove or alter .planning/v1.33/CITATIONS-STALE.md -- REMAP-04 makes it close-blocking and Phase 159 owns its removal."
    - "Must NOT write that the DEDUP-04 flip produced an identical image. The claim is size-identity on all three targets, with the .hex SHA divergence recorded (C-4)."
    - "Must NOT write that the 8-byte payload's bytes were compared. No value oracle exists; the claim is source-level identity of the arithmetic plus the length assertion (ceiling 4)."
    - "Must NOT describe anything living only in native_loop_v131 as covered by CI, and must NOT use tested, verified on hardware or bench-verified for the nine wrapper call sites."
    - "Must NOT mark a requirement Complete without citing the plan and the artifact that discharged it."
    - "Must NOT author or imply a bench/hardware criterion (D-02), and must NOT invent D-NN labels (OD-1)."
    - "Must NOT regenerate ROADMAP.md or REQUIREMENTS.md with a tool -- this milestone's ROADMAP and REQUIREMENTS are hand-authored and the GSD requirements and roadmap verbs reformat the whole file. Hand-edit the specific lines."
    - "Must NOT rewrite STATE.md wholesale, and must NOT let its Current Position become multi-line -- the state scraper reads the body, not the frontmatter."
    - "Must NOT push any branch."
---

<!-- Comment-text discipline: these literals appear in task actions AND in negative-grep acceptance criteria. -->
<!-- planner-discipline-allow: DEDUP-01 -->
<!-- planner-discipline-allow: DEDUP-02 -->
<!-- planner-discipline-allow: DEDUP-03 -->
<!-- planner-discipline-allow: DEDUP-04 -->
<!-- planner-discipline-allow: Phase 156 -->
<!-- planner-discipline-allow: OD-1 -->
<!-- planner-discipline-allow: OD-2 -->
<!-- planner-discipline-allow: the payload bytes were compared -->
<!-- planner-discipline-allow: byte-for-byte identical image -->

<objective>
Run the phase gate on the final tree, re-prove DEDUP-03's evidence against the code that actually
shipped, and write the record Phases 157 to 159 will read.

Purpose: three things would otherwise be lost. Plan 02's blind-spot assertions were written and
proven against the PRE-refactor code -- deliberately, so they are regression evidence -- but
DEDUP-03's claim is about the shipped code, which means the four planted transpositions must be
re-run against the post-refactor tree where two of them now live inside the shared helpers. The
eight phase-gate legs include two that no CI workflow invokes, so if they are not run and recorded
here they are not run at all. And the ROADMAP carries figures this phase measured wrong -- the
same failure mode that left "30 `__udivmodhi4` call sites" in a requirement this phase had to
correct.

Output: `.planning/v1.33/156-after-figures.md`, the four requirement checkboxes flipped with
evidence, the ROADMAP Coverage rows synced, and STATE.md advanced.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-03-SUMMARY.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-04-SUMMARY.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-05-SUMMARY.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-06-SUMMARY.md
@.planning/v1.33/156-before-figures.md
@.planning/v1.33/155-after-figures.md
</context>

## Multi-Source Coverage Audit (this plan's slice)

The phase-wide audit is recorded once, in `156-01-PLAN.md`. This plan closes the recording half
of all four requirements: the eight VALIDATION phase-gate legs, DEDUP-03's post-refactor probe
re-run, the C-1 through C-7 corrections plus the planner-found seventh comment location, the
C-3 split resolution, milestone decisions D-02 through D-05 as recorded facts, and the final form
of the seven coverage ceilings. No item assigned to this plan is MISSING, and nothing assigned to
Phases 157, 158 or 159 is done here.

## The honest coverage ceilings -- final form, stated not implied

All seven are carried from `156-01-PLAN.md` and must appear in the after-figures record. Two need
their final wording decided by measurement rather than copied:

- **Ceiling 2** depends on how plan 02's `flash_intel` reachability attempt landed. If the
  `flash_intel` under-voltage case was kept and reaches the arm, the ceiling is **narrowed**: the
  under-voltage arm has one executing case in `native_loop_v131` (still NOT CI-visible) while the
  over-voltage arm and the rest of the path remain uncovered. If the case was deleted, the ceiling
  stands unchanged and the blocking mechanism is recorded. Read `156-02-SUMMARY.md` and write
  whichever is true -- do not guess, and do not copy the pre-phase wording if it stopped being
  accurate.
- **Ceiling 7** is now measured twice: once by research and once by plan 05. Record plan 05's own
  three `.hex` SHA pairs, not research's.

## Artifacts this phase produces

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/156-before-figures.md` | 01 | meta |
| New case(s) in `test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp` | 02 | fw |
| New assertions inside `test_case7_mismatching_chip_id_with_force_warns` | 02 | fw |
| `mem_util_report_voltage` in `src/proms/memory.cpp` + `include/memory_utils.h` | 03 | fw |
| Local `force` at the four VPP call sites | 03 | fw |
| `mem_util_report_chip_id` in `src/proms/memory.cpp` + `include/memory_utils.h` | 04 | fw |
| `#include "memory_utils.h"` added to `src/proms/flash_utils.cpp` | 04 | fw |
| Re-derived `tests/golden/protocol_branch_inventory.json` (23/1/22 -> 22/1/21 -> 21/1/20) | 03, 04 | fw |
| New local `finished` and the `calls` count assertion in Case 25 of `test_eeprom28c_sdp.cpp` | 05 | fw |
| `tests/test_boolean_convention_source_contract_v133.py` and its module symbols | 06 | fw |
| `.planning/v1.33/156-after-figures.md` (the phase record) | 07 | meta |

<tasks>

<task type="auto">
  <name>Task 1: Run the eight-leg phase gate and re-prove DEDUP-03 against the shipped code</name>
  <files>
    (no file is edited by this task -- it measures the committed tree and hands results to task 2)
  </files>
  <read_first>
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md` -- the eight phase-gate legs verbatim, the sampling-rate section, and the forbidden phrasings
    - `.planning/v1.33/156-before-figures.md` -- every before figure this task compares against
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §"The four DEDUP-03 planted transpositions" -- the four substitutions verbatim, together with what research measured each one catching BEFORE plan 02 closed the two blind spots
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-02-SUMMARY.md` -- which cases plan 02 added or strengthened, whether the `flash_intel` case survived, and the pre-refactor RED/GREEN pairs it recorded
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-05-SUMMARY.md` -- plan 05's three `.hex` SHA pairs and its size-identity result
    - `firestarter/scripts/check_size_baseline.py` around the flash and RAM comparison branches -- to quote the one-sidedness from source rather than from the ROADMAP
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §"The throwaway-worktree trap" and §"Common Pitfalls" 8 and 9 -- the worktree naming requirement, the dirty-tree rule, and the flakiness rule
  </read_first>
  <action>
Run in `/workspaces/firestarter` unless a step says otherwise. This task edits nothing.

STEP 1 -- assert the anchor. Record `FW_POST_SHA` from `git rev-parse HEAD`. Assert
`git status --porcelain` is empty. Assert the branch is still
`gsd/v1.33-source-hygiene-firmware-size-reduction`. Count the commits this phase made inside
`firestarter/` by anchoring to the `FW_PRE_SHA` recorded in `156-before-figures.md`:
`git rev-list --count <FW_PRE_SHA>..HEAD`. Expect 5 -- two from plan 02, one each from plans 03,
04, 05, and one from plan 06, which is 6; reconcile the actual number against the per-plan
commits rather than against an expectation, list them by subject, and record the reconciliation.
Never assert a count you have not derived from the log.

STEP 2 -- the eight phase-gate legs, each run and each recorded with its result and wall time:
  1. `pio test -e native` -- expect 172 cases, 172 succeeded, 17 suites. This count is an
     exact-equality input to `check_size_baseline.py`'s native comparison, so it must be 172, not
     "at least".
  2. `pio test -e native_nodevtools` -- same expectation.
  3. `python3 -m pytest tests/ -q` -- expect at least 314 passed (313 before this phase plus plan
     06's module) and 0 failed. The tree is committed, so this run is meaningful.
  4. `pio test -e native_loop_v131` -- expect at least 81 succeeding over 2 suites, up from 80.
     Its per-suite gate is a floor, so growth is fine. Record that this env is in NO CI leg.
  5. `pio run -e uno -e uno328pb -e leonardo` -- expect flash 24234 / 24282 / 26378 and RAM
     1567 / 1573 / 2008. Record the delta against `156-before-figures.md`: -426 flash on all
     three, 0 RAM on all three. Also record Leonardo Caterina headroom against the 28672 B cliff
     (expect 2294 B, up from 1868 B).
  6. The DEDUP-03 both-directions evidence -- step 3 below.
  7. `python3 scripts/check_size_baseline.py --policy merge05` -- run it, record the result, and
     record its one-sidedness quoted from source: the flash leg is a greater-than comparison
     against the allowance and the RAM leg is a greater-than comparison against the tolerance, so
     a reduction passes with **no named exemption**. Record that explicitly (D-03) so nobody
     later reads the green run as "nothing moved" -- this is the first size movement in this
     project's history that needs no exemption. Assert
     `git diff --quiet -- scripts/baseline/size_baseline.json`, i.e. the baseline is
     byte-unchanged: the re-anchor is Phase 158 / LAND-01's. If you also run the canonical
     `--baseline scripts/baseline/size_baseline_base01.json` form, record its non-zero exit with
     the cause `native: cases baseline=141 observed=172` (BASE-01 frozen at Phase 124) and its
     owner (Phase 158 / LAND-03), stated plainly as pre-existing and NOT this phase's failure.
  8. `python3 scripts/check_build_warnings.py` -- record no new warning on any of
     `src/proms/eprom.cpp`, `src/proms/flash_intel.cpp`, `src/proms/flash_utils.cpp`,
     `src/proms/eeprom_28c.cpp`, `src/proms/memory.cpp`, `src/eprom_operations.cpp`,
     `src/operation_utils.cpp`.

Run leg 1 at least three times and record every result. The native suite is load-flaky -- 172/172
has been observed at 21 to 35 seconds while 171/172 and 158-cases-with-2-errored have been
observed at 1:13 and 1:44, tracking run duration rather than tree content. Never conclude from a
single run, and never read the count alone: a real failure inflates the reported total, so a
"173" reading means read the failure line.

STEP 3 -- re-run the four planted transpositions against the POST-refactor tree. This is the
step that makes DEDUP-03 a claim about the shipped code. Plan 02 proved its assertions RED
against transpositions planted in the PRE-refactor code, which was correct then; two of the four
mutation points have since MOVED into the shared helpers, so the same evidence must be
re-established where the code now lives.

Use a throwaway git worktree per probe, each directory named `firestarter` -- for example
`/tmp/probe156g/firestarter`. Any other name makes
`tests/test_checker_convention.py::test_scope_is_firmware_only` fail spuriously and that failure
reads like a real red. After each probe, remove and prune the worktree and re-assert the real
tree clean.

  Probe A -- in `src/proms/eprom.cpp`, transpose the over-voltage arm's severity ternary so
  forced maps to error and unforced to warning. Research measured this RED in
  `native_loop_v131` (three cases) and BLIND in `native`. Expect the same, and record that the
  blindness of the CI leg is unchanged and is coverage ceiling 2, not a regression.

  Probe B -- transpose the under-voltage pairing's response code in BOTH `src/proms/eprom.cpp`
  and `src/proms/flash_intel.cpp`. Research measured this BLIND EVERYWHERE. Expect it now RED in
  `native_loop_v131`, from plan 02's new case. That flip from blind to red is blind spot 1's
  closure, re-proven against the shipped code.

  Probe C -- in `src/proms/memory.cpp`, transpose `mem_util_report_chip_id`'s response-code
  ternary. Research measured this RED in `native` (two cases). Expect the same. Note that the
  mutation point has moved: pre-refactor it lived in four separate blocks, and now one edit to
  the helper reaches all four call sites, which is itself worth recording as a property of the
  de-duplication.

  Probe D -- in `src/proms/memory.cpp`, transpose `mem_util_report_chip_id`'s message-id ternary
  while leaving the response code correct. Research measured this BLIND EVERYWHERE. Expect it now
  RED in `native`, from plan 02's id assertions. That flip from blind to red is blind spot 2's
  closure, in CI, re-proven against the shipped code.

For each probe record: the substitution, the worktree path, which envs were run, which case names
failed, and the assertion messages. Then record the GREEN control -- the real committed tree at
`FW_POST_SHA`, all legs passing -- described as the real unedited tree, not as a synthetic
derivative. Build the four-probe before-and-after table: what each probe caught before plan 02
and what it catches now. Two cells flip from blind to red; that table is DEDUP-03's discharge
evidence.

STEP 4 -- final measurements for the record. Record the post-change `__udivmodhi4` count (expect
13) and the `__udivmodsi4` count (expect 0). Record the post-change sizes of `eprom_check_vpp`,
`flash_intel_write_init`, `mem_util_report_voltage`, `mem_util_report_chip_id`,
`flash_util_check_chip_id_execute`, `flash_intel_check_chip_id`, `eeprom28c_write_init` and
`eprom_check_chip_id_execute`, and record that `eprom_internal_check_chip_id` is absent from the
symbol table. Compute the symbol-delta sum against the before-figures and record that the ledger
does NOT close -- expect roughly -624 B of symbol deltas against a -426 B image -- with LTO
inlining into `main` named as the cause. Do not fudge the difference and do not hunt for a bug.
`avr-nm` and `avr-objdump` are not on PATH; use
`~/.platformio/packages/toolchain-atmelavr/bin/avr-{nm,objdump}`.

Leave the tree clean. Re-assert `git status --porcelain` empty and `git worktree list` showing
only the primary tree.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git worktree list | wc -l)" -eq 1 && pio run -e uno -e uno328pb -e leonardo 2>&1 | grep -E '^(RAM|Flash):' | tee /tmp/sz7.log && grep -q '24234' /tmp/sz7.log && grep -q '24282' /tmp/sz7.log && grep -q '26378' /tmp/sz7.log && pio test -e native 2>&1 | tee /tmp/n7.log | tail -3 && grep -qE '172 test cases: 172 succeeded' /tmp/n7.log && pio test -e native_nodevtools 2>&1 | tail -3 && pio test -e native_loop_v131 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -3 && test "$(~/.platformio/packages/toolchain-atmelavr/bin/avr-objdump -d .pio/build/uno/firestarter_uno.elf | grep -cE '(r?call|jmp).*__udivmodhi4')" -eq 13 && git diff --quiet -- scripts/baseline/size_baseline.json && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter status --porcelain` empty at the start and at the end; `git worktree list` shows only the primary tree at the end.
    - The commit count from `FW_PRE_SHA` to `HEAD` is derived from `git rev-list --count` and reconciled against the per-plan commits, each listed by subject. No count is asserted without being derived.
    - Leg 1 run at least three times, every result recorded, each reporting `172 test cases: 172 succeeded` over 17 suites. Leg 2 the same.
    - Leg 3 reports at least 314 passed and 0 failed.
    - Leg 4 reports at least 81 cases all succeeding over 2 suites, and the record states that this env is in no CI leg.
    - Leg 5 reports flash `24234` / `24282` / `26378` and RAM `1567` / `1573` / `2008`, with the deltas recorded as -426 flash and 0 RAM on all three targets, and Leonardo Caterina headroom recorded as `2294` up from `1868`.
    - Leg 7 run; its one-sidedness quoted from `check_size_baseline.py` source, not from the ROADMAP; and `git diff --quiet -- scripts/baseline/size_baseline.json` exits 0. If the canonical baseline form was run, its pre-existing `native: cases baseline=141 observed=172` failure is recorded with cause and owner and explicitly not attributed to this phase.
    - Leg 8 run; no new warning recorded on any of the seven edited source files.
    - All four probes run in worktrees whose paths end in `/firestarter`, with substitution, envs, failing case names and assertion messages recorded for each.
    - Probe B is recorded RED in `native_loop_v131` where research measured it blind everywhere, and probe D is recorded RED in `native` where research measured it blind everywhere. Those two flips are the discharge evidence for DEDUP-03's two blind spots.
    - Probe A is recorded as still blind in `native` and red in `native_loop_v131`, and that blindness is attributed to coverage ceiling 2 rather than presented as a regression.
    - The four-probe before-and-after table is recorded, with the GREEN control described as the real, committed, unedited tree at `FW_POST_SHA`.
    - `avr-objdump -d` reports `__udivmodhi4` exactly 13 and `__udivmodsi4` exactly 0.
    - `eprom_internal_check_chip_id` is absent from `avr-nm` output, the symbol-delta sum is recorded, and the ledger's failure to close is recorded with LTO inlining into `main` named as the cause.
    - No file under `firestarter/` outside `.pio/build/` differs from HEAD when the task ends.
  </acceptance_criteria>
  <done>All eight gate legs are run and recorded on the final tree, including the two no CI workflow invokes, and DEDUP-03's evidence is re-established against the code that actually shipped, with two probes flipping from blind to red.</done>
</task>

<task type="auto">
  <name>Task 2: Write the after-figures record with every correction stated as superseding</name>
  <files>.planning/v1.33/156-after-figures.md</files>
  <read_first>
    - `.planning/v1.33/155-after-figures.md` -- the record convention this file copies: frontmatter keys including `supersedes`, numbered sections, per-figure verbatim commands, and how a correction is stated so it supersedes the ROADMAP rather than merely disagreeing with it
    - `.planning/v1.33/156-before-figures.md` -- the before side of every figure and the corrections index this file must close out
    - all four of `156-02-SUMMARY.md`, `156-03-SUMMARY.md`, `156-04-SUMMARY.md`, `156-05-SUMMARY.md`, `156-06-SUMMARY.md` -- the per-plan measured figures, planted-negative results, and the six-divergence table
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md` -- the eight legs, the seven ceilings, and the forbidden phrasings this record must not use
    - `.planning/ROADMAP.md` §Phase 156 and `.planning/REQUIREMENTS.md` §3 -- the exact sentences being superseded, so the record can quote what it replaces
  </read_first>
  <action>
Create `.planning/v1.33/156-after-figures.md` in the meta repo. Lowercase-hyphenated filename with
the phase number prefixed, matching the directory's convention.

Frontmatter keys, matching `155-after-figures.md`: `title`, `phase`, `plan` (`"07"`), `measured`,
`status` (this file is the AUTHORITATIVE record of Phase 156's outcome; Phases 157 and 158 each
invalidate the size figures captured here), `supersedes` (ROADMAP.md §Phase 156 criteria 1, 2 and
4 and REQUIREMENTS.md DEDUP-01 through DEDUP-04 prose wherever they state a figure this file
corrects), and `requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04]`.

Sections, in this order:

1. **Git anchors** -- `FW_PRE_SHA`, `FW_POST_SHA`, branch, the derived commit count with each
   commit listed by subject, clean-tree assertions before and after, and the meta repo HEAD.
   Note again that the `firestarter_app` gitlink shows modified from pre-existing Phase 154 drift
   and was not touched.
2. **The phase ledger** -- flash and RAM per target before and after with the delta, plus the
   per-requirement split: DEDUP-01's own measured delta from plan 03 and DEDUP-02's from plan 04,
   summing to the combined figure. Then resolve C-3 explicitly: research labelled the -268 / -158
   split UNVERIFIED at this position, and these two commits measured each half. State whether the
   measurements CONFIRM the ROADMAP's -268 and -158, or SUPERSEDE them with the values actually
   measured. Do not present an inherited figure as measured. Record Leonardo Caterina headroom
   before and after.
3. **DEDUP-04's size-identity result** -- the three flash/RAM pairs identical across plan 05's
   commit, and the three `.hex` SHA pairs showing the image DID change, with the cause: a uniform
   two-byte relocation plus branch-polarity swaps, on a build proven reproducible. Use plan 05's
   own measured SHAs, not research's. State the claim in the permitted form -- size-identical on
   all three AVR targets -- and state that an oracle asserting image identity would have gone red.
   Also record the nine-to-one negation reduction and why site 4 keeps its negation.
4. **The mechanical criteria** -- `__udivmodhi4` 31 to 13 with the derived arithmetic (the four
   blocks held 24, the helper adds 6, net 18), `__udivmodsi4` 0, and the per-symbol ledger with
   its explicit non-closure and the LTO cause.
5. **The gate ledger** -- all eight legs with their results, wall times, and for legs 4 and 7 the
   statement that no CI workflow invokes them. Leg 1's three-or-more runs listed individually
   (D-04). Leg 7's one-sidedness stated in the D-03 terms, with the baseline asserted
   byte-unchanged and the pre-existing BASE-01 failure recorded with cause and owner.
6. **DEDUP-03's evidence** -- the four-probe before-and-after table from task 1, the two
   blind-to-red flips called out as the discharge, the pre-refactor RED/GREEN pairs plan 02
   recorded, and the mechanism sentence: severity rides entirely in the message id because every
   `LOG_{WARN,ERROR}_ID_BYTES` macro is the same alias of `LOG_ID_BYTES`, so a `response_code`
   assertion cannot see a transposed id and an id assertion cannot see a swapped `response_code`.
   Both halves are required, per path.
7. **DEDUP-02's resolved single semantic** -- the six-divergence table with resolution and
   reasoning, the sentence that no divergence required a behaviour change to resolve, and the
   named gap: there is NO oracle anywhere for the claim that the standalone chip-ID check still
   refuses regardless of `--force`; its evidence is source-level. Say so.
8. **The seven coverage ceilings, final form** -- with ceiling 2 written according to how plan
   02's `flash_intel` attempt actually landed (narrowed, or unchanged with the blocking mechanism
   recorded) and ceiling 7 carrying plan 05's own SHAs.
9. **The corrections ledger** -- close out every row of the before-figures corrections index:
   C-1 through C-7 plus the planner-found seventh comment location at
   `src/operation_utils.cpp:58`, each with what the ROADMAP or REQUIREMENTS says, what was
   measured, and where the corrected figure now lives. Add C-7's consequence explicitly:
   REQUIREMENTS DEAD-06's sentence that it is the only requirement in Phases 155 to 158 touching
   a test file is FALSE, because DEDUP-04 touched `test_eeprom28c_sdp.cpp`.
10. **Handoffs** -- what this phase deliberately did not do and who owns it: the
    `size_baseline.json` re-anchor and the BASE-01 case-count mismatch (Phase 158 / LAND-01 and
    LAND-03), `json_parser.c`'s missing `algorithm` range check (Phase 157 / DECODE-05), and the
    citation staleness this phase created -- name the one added `#include` in
    `src/proms/flash_utils.cpp` shifting all 97 of that file's planning citations, plus the shifts
    in `eprom.cpp`, `flash_intel.cpp`, `eeprom_28c.cpp` and `memory.cpp` -- all owned by Phase 159
    / REMAP-01 through REMAP-05 and close-blocked by REMAP-04. State that
    `.planning/v1.33/CITATIONS-STALE.md` was not touched. Record separately the ONE in-source
    citation this phase did repair, at `src/proms/eeprom_28c.cpp:265`, as incidental.

Every figure carries the verbatim command that produced it. Use ` -- ` rather than an em dash.

Respect the forbidden phrasings absolutely. This record must not say the DEDUP-04 flip produced
an identical image; must not say the payload bytes were compared; must not describe anything
living only in `native_loop_v131` as covered by CI; and must not use "tested", "verified on
hardware" or "bench-verified" for the nine wrapper call sites -- for those, the honest statement
is that `src/eprom_operations.cpp` compiles in no native environment, so their evidence is plan
06's source contract plus plan 05's size-identity build and source inspection.

Do not commit in this task -- task 3 commits all four meta files together.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/156-after-figures.md && for t in 24234 24282 26378 1567 1573 2008 2294 426 13 31 24 172 314 21 20; do grep -q "$t" .planning/v1.33/156-after-figures.md || { echo "MISSING $t"; exit 1; }; done && grep -q 'size-identical' .planning/v1.33/156-after-figures.md && grep -q 'NO CI COVERAGE\|no CI workflow' .planning/v1.33/156-after-figures.md && grep -q 'REMAP-04' .planning/v1.33/156-after-figures.md && grep -q 'operation_utils.cpp:58' .planning/v1.33/156-after-figures.md && test "$(grep -c 'byte-for-byte identical image' .planning/v1.33/156-after-figures.md)" -eq 0 && test "$(grep -c 'the payload bytes were compared' .planning/v1.33/156-after-figures.md)" -eq 0 && echo RECORD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/156-after-figures.md` exists with all seven frontmatter keys named in the action, including `supersedes`, and `requirements` listing all four ids.
    - The file contains the literals `24234`, `24282`, `26378`, `1567`, `1573`, `2008`, `2294`, `1868`, `426`, `31`, `13`, `24`, `18`, `172`, `17`, `21`, `20` and `314`.
    - Section 2 resolves C-3 explicitly, stating whether the two per-requirement measurements confirm or supersede -268 and -158, and quoting the measured values. No inherited figure is presented as measured.
    - Section 3 records three `.hex` SHA pairs from plan 05's own measurement and states the claim as size-identity on all three targets. Source assertion: `grep -c 'byte-for-byte identical image' .planning/v1.33/156-after-figures.md` is 0.
    - Section 3 records the nine-to-one negation reduction and the reason site 4 retains its negation.
    - Section 5 lists all eight legs with results, lists leg 1's three-or-more runs individually, marks legs 4 and 7 as invoked by no CI workflow, states leg 7's one-sidedness in the D-03 terms, and records the baseline as byte-unchanged plus the pre-existing BASE-01 failure with its cause and owner.
    - Section 6 contains the four-probe before-and-after table with the two blind-to-red flips called out, and the macro-aliasing mechanism sentence.
    - Section 7 contains the six-divergence table and the explicit statement that no oracle exists for the standalone chip-ID refusal claim.
    - Section 8 carries all seven ceilings, with ceiling 2 written to match what plan 02 actually achieved rather than copied from the pre-phase wording.
    - Section 9 closes out C-1 through C-7 plus the planner-found `src/operation_utils.cpp:58` location, and states the C-7 consequence for DEAD-06's sentence.
    - Section 10 names all four handoffs with their owning phase and requirement ids, names the `flash_utils.cpp` include and its 97-citation shift, and states that `.planning/v1.33/CITATIONS-STALE.md` was not touched. The `eeprom_28c.cpp:265` repair is recorded separately as incidental.
    - Source assertion: `grep -c 'the payload bytes were compared' .planning/v1.33/156-after-figures.md` is 0, and the file instead states that the payload claim rests on source-level identity of the arithmetic plus the length assertion.
    - Source assertion: the file contains no sentence applying the words "tested", "verified on hardware" or "bench-verified" to the nine wrapper call sites.
    - `git -C firestarter status --porcelain` is empty -- this task edited nothing under `firestarter/`.
  </acceptance_criteria>
  <done>The authoritative phase record exists, every figure carries its command, every correction is stated as superseding the ROADMAP, both named gaps are visible rather than smoothed over, and no forbidden phrasing appears.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the four requirement checkboxes, sync the ROADMAP, advance STATE.md, commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` -- §3's four DEDUP entries at their current unchecked state, the traceability table rows for DEDUP-01 through DEDUP-04, and §2's DEAD entries as the format model for how this milestone writes a discharge (the checkbox, the discharging plan, the artifact cited, and any correction appended)
    - `.planning/ROADMAP.md` §Phase 156 -- the `**Plans:** TBD` line, the `Plans:` list with its single `TBD` entry, and the four success criteria whose figures the after-figures record now supersedes
    - `.planning/ROADMAP.md` §Phase 155's completed entry -- the format model for a per-plan done annotation in this milestone
    - `.planning/STATE.md` -- its frontmatter, the `## Session` block the state scraper reads for `stopped_at`, and the Current Position line. Read only what you need; the file is large
    - `.planning/v1.33/156-after-figures.md` as task 2 wrote it -- the evidence each checkbox cites
    - all five plan SUMMARYs -- to cite the discharging plan per requirement accurately
  </read_first>
  <action>
Hand-edit three `.planning/` files, then commit all four meta files from this plan together.

Do NOT regenerate `ROADMAP.md` or `REQUIREMENTS.md` with any tool. This milestone's ROADMAP and
REQUIREMENTS are hand-authored, and the GSD requirements and roadmap verbs reformat the whole
file. Edit the specific lines.

REQUIREMENTS.md -- §3, the four DEDUP entries. Flip each checkbox and append the discharge to the
existing prose, following the format §2's DEAD entries use: which plan discharged it, the artifact
that proves it, and any correction the measurement forced. Specifically:
  - DEDUP-01: discharged by plan 03 against `.planning/v1.33/156-after-figures.md`. Append the
    C-2 correction (`__udivmodhi4` was 31 at this phase's start, not 30; it fell to 13, and the
    "24 of them" claim is unaffected), the C-1 correction (the two copies are lexically inside
    `flash_intel_check_vpp`, and `flash_intel_write_init` is the symbol-table attribution), and
    the C-3 resolution for the -268 figure -- confirmed or superseded, with the measured value.
    Also append coverage ceiling 4 in one clause: the payload claim rests on source-level identity
    of the arithmetic plus the length assertion, and no value oracle exists.
  - DEDUP-02: discharged by plan 04. Append that the six divergences are enumerated with
    resolution and reasoning in the after-figures record, that the resolved semantic is stated in
    the helper's own comment, that divergence 1 is parameterised so the standalone chip-ID check
    still refuses regardless of `--force`, and that no oracle exists for that last claim -- its
    evidence is source-level. Append the C-3 resolution for the -158 figure.
  - DEDUP-03: discharged by plan 02, re-proven against the shipped code by plan 07. Append the
    four-probe before-and-after result: two blind spots closed, one in CI and one not, each seen
    RED against a planted transposition and GREEN against the real tree. Append that the VPP
    over-voltage fork's only oracle remains a suite no CI workflow runs.
  - DEDUP-04: discharged by plans 05 and 06. Append that the criterion's decline branch is dead
    by operator decision OD-2, the C-4 correction (size-identical on all three targets -- one
    more than the survey claimed -- while the image is NOT identical, `.hex` SHA changing on all
    three), the C-5 correction (the clone suffix is `.constprop.42`, not `.44`, and no gate pins
    it), the C-6 correction (only three of the eleven comment lines were dead), and the honest
    nine-to-one framing rather than elimination.

Then update the traceability table rows for DEDUP-01 through DEDUP-04 from `Pending` to the
completed form this file uses, each naming its discharging plan, matching how the DEAD rows read.

Also append the C-7 correction to DEAD-06's entry in §2: its claim to be the only requirement in
Phases 155 to 158 touching a test file is now false, because DEDUP-04 touched
`test_eeprom28c_sdp.cpp`. Do not silently leave that sentence standing. Change nothing else in
§2.

ROADMAP.md -- §Phase 156 only. Replace the `**Plans:** TBD at /gsd-plan-phase 156` line with the
plan count, and replace the single `TBD` list entry with one line per plan, each naming its
objective and carrying a done annotation in the format Phase 155's entries use, with the headline
measured result. Add a short note under the success criteria that the figures in criteria 1, 2 and
4 are superseded by `.planning/v1.33/156-after-figures.md` where they disagree, listing which
corrections apply -- exactly as Phase 155's entry does for its own five corrections. Do NOT edit
any other phase's section, and after editing, diff the file against its pre-edit state to confirm
no other phase's `**Plans:**` line or Coverage row moved: this project has seen a state writer
clobber an unrelated phase's plans line, so verify rather than assume.

Also sync the ROADMAP v1.33 Coverage rows for DEDUP-01 through DEDUP-04 if that section carries
per-requirement status, so REQUIREMENTS.md and ROADMAP.md do not diverge -- a divergence of
exactly this kind was a blocker earlier in this project.

STATE.md -- advance it. Set the current phase's status to completed and write the `stopped_at`
narrative in the `## Session` block, which is where the state scraper reads it from -- not the
frontmatter. Keep the Current Position line SINGLE-LINE; the scraper reads the body. Summarise:
the four requirements discharged and by which plans; the measured -426 B flash on all three AVR
targets with RAM unchanged; `__udivmodhi4` 31 to 13; the golden re-derived twice, one site per
commit, so no gate was red at any commit; both DEDUP-03 blind spots closed with two probes
flipping from blind to red; DEDUP-04 size-identical on all three targets with the image
divergence recorded; Case 25 de-vacuumed with a four-call assertion after being measured passing
at one; the new source-contract gate proven red four ways; and the handoffs to Phases 157, 158 and
159. Note that nothing was pushed. Do not rewrite unrelated sections and do not reflow the file.

THEN COMMIT -- once, in the META repo, staging exactly `.planning/v1.33/156-after-figures.md`,
`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and `.planning/STATE.md`. Never
`git add -A`, never `git add .`. Do not stage the `firestarter_app` gitlink and do not stage any
`firestarter/` path. Subject:
`docs(156-07): close out Phase 156 -- the measured ledger, the gate results and the corrections`.
The body must state the phase total, that all four requirements are discharged with their
evidence cited, and that nothing was pushed.

Then verify the record's own integrity: re-read the four DEDUP entries and confirm each checkbox
is ticked and each cites a plan and an artifact; re-read the traceability rows and confirm they
match; and confirm `git -C firestarter status --porcelain` is still empty and
`git -C firestarter rev-parse HEAD` still equals the `FW_POST_SHA` task 1 recorded -- this plan
must not have moved the firmware repo at all.
  </action>
  <verify>
    <automated>cd /workspaces && test "$(grep -cE '^- \[x\] \*\*DEDUP-0[1-4]\*\*' .planning/REQUIREMENTS.md)" -eq 4 && test "$(grep -cE '^- \[ \] \*\*DEDUP-0[1-4]\*\*' .planning/REQUIREMENTS.md)" -eq 0 && test "$(grep -cE '\| DEDUP-0[1-4] \| Phase 156 \| Pending' .planning/REQUIREMENTS.md)" -eq 0 && grep -q '156-after-figures' .planning/REQUIREMENTS.md && grep -qE '156-0[1-7]-PLAN.md' .planning/ROADMAP.md && test "$(grep -c 'TBD at ' .planning/ROADMAP.md | head -1)" -ge 0 && test -z "$(git -C firestarter status --porcelain)" && git log -1 --pretty=%s | grep -q '^docs(156-07):' && test "$(git show --stat --name-only --format= HEAD | grep -c .)" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - All four DEDUP entries in `.planning/REQUIREMENTS.md` §3 are ticked, and none remains unticked. Each cites its discharging plan and `.planning/v1.33/156-after-figures.md`.
    - DEDUP-01's entry carries the C-2 and C-1 corrections and the C-3 resolution for -268 plus the ceiling-4 payload clause. DEDUP-02's carries the C-3 resolution for -158, the six-divergence reference, and the no-oracle statement for the standalone chip-ID refusal. DEDUP-03's carries the four-probe result and the statement that the over-voltage fork's only oracle is a suite no CI workflow runs. DEDUP-04's carries C-4, C-5, C-6, OD-2 and the nine-to-one framing.
    - The traceability table rows for DEDUP-01 through DEDUP-04 no longer read `Pending` and each names its discharging plan, matching the format the DEAD rows use.
    - DEAD-06's §2 entry carries the appended C-7 correction, and nothing else in §2 changed: `git -C /workspaces diff HEAD~1 HEAD -- .planning/REQUIREMENTS.md` shows changes confined to the four DEDUP entries, the four traceability rows, and DEAD-06's appended sentence.
    - `.planning/ROADMAP.md` §Phase 156 has a real plan count, one list entry per plan with an objective and a done annotation, and a note that criteria 1, 2 and 4's figures are superseded by the after-figures record with the applicable corrections listed.
    - A pre-edit versus post-edit diff of `.planning/ROADMAP.md` confirms no other phase's `**Plans:**` line, plan list or Coverage row changed. The verification is recorded, not assumed.
    - The v1.33 Coverage rows for the four DEDUP ids in `.planning/ROADMAP.md` agree with `.planning/REQUIREMENTS.md` -- zero divergence.
    - `.planning/STATE.md` records the phase as completed with the `stopped_at` narrative in the `## Session` block, the Current Position line still single-line, and every item enumerated in the action present. No unrelated section was rewritten or reflowed.
    - `git -C /workspaces show --stat HEAD` lists exactly four paths, all under `.planning/`. No `firestarter/` path, no `firestarter_app` gitlink, no `.planning/v1.33/CITATIONS-STALE.md`.
    - The commit subject begins `docs(156-07):` and the body states the phase total, the four discharges, and that nothing was pushed.
    - `git -C firestarter status --porcelain` is empty and `git -C firestarter rev-parse HEAD` equals the `FW_POST_SHA` recorded in task 1 -- this plan moved the firmware repo not at all.
    - `.planning/v1.33/CITATIONS-STALE.md` is byte-unchanged.
  </acceptance_criteria>
  <done>All four requirements are marked complete with their evidence cited, the ROADMAP and REQUIREMENTS agree and carry the corrections, STATE.md is advanced, and the whole close-out is one meta-repo commit of four files with the firmware repo provably untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped firmware -> the phase record | A figure recorded wrong here is inherited by Phases 157, 158 and 159, exactly as the 30-site figure was inherited into this phase's own requirement |
| pre-refactor test evidence -> post-refactor code | Plan 02's probes were run against code that no longer exists. Without a re-run, DEDUP-03's claim is about the wrong tree |
| a gate that no CI workflow invokes -> the record | If the size and v131 legs are not run here, they are not run at all |
| state writer -> unrelated ROADMAP sections | A whole-file rewrite of ROADMAP.md or REQUIREMENTS.md silently damages other phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-48 | Repudiation | DEDUP-03's evidence being about the pre-refactor tree | high | mitigate | All four probes are re-run against the post-refactor tree, where two mutation points have moved into the shared helpers, and the before-and-after table records the two blind-to-red flips |
| T-156-49 | Repudiation | quoting an unmeasured split as measured | high | mitigate | C-3 is resolved explicitly from plans 03 and 04's per-commit measurements, and the record must state confirm-or-supersede with the measured values |
| T-156-50 | Repudiation | a green one-sided size gate read as "nothing moved" | medium | mitigate | The gate's one-sidedness is quoted from source and recorded in the D-03 terms alongside the measured -426 B |
| T-156-51 | Repudiation | the pre-existing BASE-01 failure read as this phase's | medium | mitigate | Recorded with its cause, its frozen-at-Phase-124 origin, and its Phase 158 / LAND-03 owner, explicitly not attributed here |
| T-156-52 | Tampering | a whole-file regeneration of ROADMAP.md or REQUIREMENTS.md | high | mitigate | Prohibited; hand edits only; and a pre-edit versus post-edit diff must confirm no other phase's plans line or Coverage row moved. This project has seen exactly that clobber happen |
| T-156-53 | Tampering | STATE.md frontmatter corruption or a multi-line Current Position | medium | mitigate | The narrative goes in the `## Session` block where the scraper reads it, the Current Position line stays single-line, and no unrelated section is reflowed |
| T-156-54 | Repudiation | forbidden phrasing entering the authoritative record | high | mitigate | Three negative source assertions on the after-figures file plus a positive requirement that the permitted claim forms appear |
| T-156-55 | Repudiation | closing the phase while doing Phase 157/158/159 work | medium | mitigate | Section 10 names every handoff with its owning phase and requirement ids; `CITATIONS-STALE.md` is asserted byte-unchanged; the baseline is asserted byte-unchanged |
| T-156-56 | Denial of service | native-suite load flakiness (D-04) | low | accept | Leg 1 is run at least three times with each result listed individually; a real failure inflates the reported total, so failure lines are read rather than counts |
| T-156-57 | Tampering | npm/pip/cargo installs | high | accept | No package installed. RESEARCH's Package Legitimacy Audit records zero packages |
</threat_model>

<verification>
- All eight phase-gate legs green and recorded, with legs 4 and 7 marked as invoked by no CI workflow.
- `pio run`: flash 24234 / 24282 / 26378, RAM 1567 / 1573 / 2008 -- `-426` / `0` against the before-figures on all three targets.
- `pio test -e native` 172/172 over 17 suites on three or more runs; `native_nodevtools` 172/172; `native_loop_v131` at least 81/81; `pytest tests/ -q` at least 314 passed, 0 failed.
- `__udivmodhi4` exactly 13, `__udivmodsi4` exactly 0.
- Four probes re-run post-refactor, each in a worktree named `firestarter`, results tabulated against research's pre-plan-02 measurements.
- `scripts/baseline/size_baseline.json` and `.planning/v1.33/CITATIONS-STALE.md` byte-unchanged.
- One meta-repo commit of exactly four `.planning/` paths; `firestarter/` HEAD unchanged from `FW_POST_SHA`; nothing pushed.
</verification>

<success_criteria>
Phase 156 is closed on measurement rather than on assertion. All eight gate legs are run and
recorded, including the two nothing else runs. DEDUP-03's evidence is re-established against the
code that shipped, with two probes flipping from blind to red. The -426 B total and both
per-requirement halves are recorded with the C-3 split resolved honestly. Every ROADMAP and
REQUIREMENTS figure the measurements correct is named as superseded, including the planner-found
seventh comment location and the falsification of DEAD-06's test-file claim. Both named gaps --
the payload's absent value oracle and the standalone chip-ID refusal's absent oracle -- are
visible in the record. And every handoff to Phases 157, 158 and 159 is named with its owner.
</success_criteria>

<output>
Create `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-07-SUMMARY.md` when done.
</output>