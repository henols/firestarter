---
phase: 156-duplicated-report-extraction-boolean-convention-repair-firmw
plan: 06
type: execute
wave: 6
depends_on: ["156-05"]
files_modified:
  - firestarter/tests/test_boolean_convention_source_contract_v133.py
autonomous: true
requirements: [DEDUP-04]

must_haves:
  truths:
    - "The nine wrapper call sites now have a mechanical check that runs in CI, which is the only kind possible for a translation unit that compiles in no native environment."
    - "The gate cannot pass vacuously: a deleted, emptied or filtered scan target fails it loudly, and a positive count leg asserts the nine forwarding calls are actually present rather than merely un-negated."
    - "The gate is proven RED against a planted violation in a throwaway worktree and GREEN against the real tree, both recorded."
    - "The gate's own forbidden token is built by concatenation, so the module does not match itself and can actually pass."
    - "The gate's docstring states plainly that it is a source contract and not a behavioural oracle, and that no native or bench coverage of these nine call sites exists."
  artifacts:
    - firestarter/tests/test_boolean_convention_source_contract_v133.py
  key_links:
    - "A zero-match grep passes trivially against a deleted or emptied file. The non-vacuity legs -- target exists, is non-empty, resolves inside the repository root, and its comment-stripped text is non-empty -- are what turn an absence assertion into evidence. This project has been bitten by exactly this shape before."
    - "The positive counterpart matters as much as the absence leg: asserting the nine forwarding calls ARE present is what makes a deleted call fail loudly instead of satisfying the absence assertion."
    - "resolve().is_relative_to(_REPO_ROOT) on the default targets closes the landmine where a checker's own directory anchor resolves to the wrong place and the module scans nothing while exiting zero."
  prohibitions:
    - "Must NOT write the forbidden token as a single literal in the module. Build it by concatenation, following the existing sibling gate's idiom -- a gate that quotes its own forbidden token verbatim matches itself and can never pass."
    - "Must NOT ship the absence legs without the non-vacuity legs. A zero-match grep against a deleted file passes trivially."
    - "Must NOT add a scripts/check_*.py checker. tests/test_checker_convention.py requires every such script to have a paired test module AND a planted fixture, and would need its FLOOR and FIXTURE_FLOOR bumped. A pytest module under tests/ trips neither, which is why the sibling source-contract gates are shaped that way."
    - "Must NOT pin any line number in the gate. Phases 157 and 158 move these files again and Phase 159 remaps the composite diff -- assert on symbols and counts, never on positions."
    - "Must NOT pin a .constprop.NN clone suffix or any AVR symbol name -- this gate reads source text, not build output."
    - "Must NOT name a throwaway worktree anything but firestarter. tests/test_checker_convention.py::test_scope_is_firmware_only hard-codes the directory name."
    - "Must NOT edit any firmware source file, any other test module, or platformio.ini. This plan adds exactly one new file."
    - "Must NOT claim this gate provides behavioural or native coverage of the nine call sites. It is a source contract; the translation unit compiles in no native environment."
    - "Must NOT re-anchor scripts/baseline/size_baseline.json (Phase 158 / LAND-01) or repair the BASE-01 mismatch (Phase 158 / LAND-03)."
    - "Must NOT run the citation remap tool or repair any .planning/ file:LINE citation (Phase 159, D-01)."
    - "Must NOT author or imply a bench/hardware criterion (D-02), and must NOT invent D-NN labels (OD-1)."
    - "Must NOT push any branch."
---

<!-- Comment-text discipline: these literals appear in task actions AND in negative-grep acceptance criteria. They are named here because a gate that cannot name its own forbidden token cannot be reviewed. -->
<!-- planner-discipline-allow: DEDUP-04 -->
<!-- planner-discipline-allow: Phase 156 -->
<!-- planner-discipline-allow: OD-1 -->
<!-- planner-discipline-allow: OD-2 -->
<!-- planner-discipline-allow: return !op_execute_ -->

<objective>
Add the one mechanical check that is possible on `src/eprom_operations.cpp`: a committed
source-contract gate that runs in CI leg 3 and pins the boolean convention this phase repaired.

Purpose: that translation unit compiles in no native environment, so the nine wrapper edits have
no behavioural oracle at all -- only plan 05's size-identity build and source inspection.
Research open question 4 recommends closing that with a source scan, and the repo has three
precedents for exactly this shape. The trap is equally well documented: a zero-match grep passes
trivially against a deleted or emptied file, and this project has been bitten by precisely that.
So the absence legs are the easy half; the non-vacuity legs and the positive counterpart are what
make the gate evidence rather than decoration.

Output: `tests/test_boolean_convention_source_contract_v133.py`, following
`tests/test_write_path_source_contract_v131.py`'s established idiom, proven RED against a planted
violation and GREEN against the real tree.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-PATTERNS.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-VALIDATION.md
@.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-05-SUMMARY.md
@.planning/v1.33/156-before-figures.md
</context>

## Multi-Source Coverage Audit (this plan's slice)

The phase-wide audit is recorded once, in `156-01-PLAN.md`. This plan covers RESEARCH open
question 4 (answered yes, with the mandatory non-vacuity leg), the VALIDATION Wave 0 optional
high-value item "a source-scan gate for DEDUP-04", and DEDUP-04's mechanical half. It closes no
new requirement of its own -- DEDUP-04's code change landed in plan 05 -- and it is the only
mechanical check available for coverage ceiling 1. No item assigned to this plan is MISSING.

## The honest coverage ceilings -- stated, not implied

All seven are carried verbatim from `156-01-PLAN.md`. The one this plan exists because of:

- **Ceiling 1.** `src/eprom_operations.cpp` compiles in **NO** native environment. All native
  envs share `build_src_filter = +<proms/> +<boards/rurp_serial_utils.cpp> +<json_parser.c>
  +<operation_utils.cpp>`. This gate is a **source contract**, not a behavioural oracle: it
  proves the shipped text says what the requirement says, and nothing about what the code does
  at runtime. `src/operation_utils.cpp` **is** in the native filter, so the six flipped engine
  returns are covered by Cases 24 and 25; the nine wrapper call sites are covered by this gate
  and by plan 05's size-identity build, and by nothing else. The gate's own docstring must say
  this.

Also relevant: **ceiling 5** -- `pytest tests/ -v` DOES run in CI at `build.yml:161` and
`beta-build.yml:134`, so unlike the size gates this one is genuinely CI-visible. But CI runs on
the branches those workflows trigger for, not on this milestone branch, so the honest framing is
"lands in a CI leg" rather than "is running in CI today". Write that.

## Artifacts this phase produces

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/156-before-figures.md` | 01 | meta |
| New case(s) in `test/native/avr/test_vpp_eprom_v131/test_vpp_eprom_v131.cpp` | 02 | fw |
| New assertions inside `test_case7_mismatching_chip_id_with_force_warns` | 02 | fw |
| `mem_util_report_voltage` in `src/proms/memory.cpp` + `include/memory_utils.h` | 03 | fw |
| Local `force` at the four VPP call sites | 03 | fw |
| `mem_util_report_chip_id` in `src/proms/memory.cpp` + `include/memory_utils.h` | 04 | fw |
| `#include "memory_utils.h"` added to `src/proms/flash_utils.cpp` | 04 | fw |
| Re-derived `tests/golden/protocol_branch_inventory.json` (23/1/22 -> 22/1/21 -> 21/1/20) | 03, 04 | fw |
| New local `finished` and the `calls` count assertion in Case 25 of `test_eeprom28c_sdp.cpp` | 05 | fw |
| `tests/test_boolean_convention_source_contract_v133.py` -- module symbols `_HERE`, `_REPO_ROOT`, `_WRAPPERS_REL`, `_ENGINE_REL`, `_SCAN_WRAPPERS`, `_SCAN_ENGINE`, `_strip_comments`, `_assert_identifier_absent`, `_NEEDLE_INVERTED_CALL`, `_FORWARD_STATEFUL`, `_FORWARD_SIMPLE`, `_EXPECTED_FORWARD_TOTAL` (9), and its test legs | 06 | fw |
| `.planning/v1.33/156-after-figures.md` (the phase record) | 07 | meta |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the source-contract gate, absence legs and non-vacuity legs together</name>
  <files>firestarter/tests/test_boolean_convention_source_contract_v133.py</files>
  <behavior>
    - `python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -q` passes every leg against the real post-plan-05 tree.
    - The absence leg fails when the negated call form is reintroduced anywhere in the comment-stripped wrapper file.
    - The positive count leg fails when a forwarding call is deleted, so the absence leg cannot be satisfied by an empty or truncated file.
    - The non-vacuity legs fail when either scan target is missing, empty, or resolves outside the repository root.
    - The module's own needles do not appear verbatim inside the module, so the gate does not match itself.
  </behavior>
  <read_first>
    - `firestarter/tests/test_write_path_source_contract_v131.py` -- the analog, in four specific places: the root and environment-seam header at :146-159 (no fixture, no conftest -- the module locates firmware source itself and the seam binds at IMPORT time); `_strip_comments` at :222-255, which replaces each stripped span with whitespace of the SAME SHAPE so line numbers survive; `_assert_identifier_absent` at :302-315 together with the concatenation-built needle discipline at :167-172; and four legs to copy in spirit -- `test_the_per_byte_loop_constructs_are_present` at :394-425 (the positive counterpart), `test_scan_targets_are_non_vacuous` at :641-669, `test_this_module_cannot_be_silently_skipped` at :671-698, and `test_own_needles_do_not_appear_verbatim_in_this_module` at :700-715
    - `firestarter/src/eprom_operations.cpp` as plan 05 left it -- the nine forwarding call sites, the two early-return literals that must NOT be counted as violations, and the surviving comment block. Read it to confirm what the gate should count.
    - `firestarter/src/operation_utils.cpp` as plan 05 left it -- the six flipped returns and the ONE surviving negation at the callback delegation, which the gate must permit rather than forbid
    - `firestarter/tests/test_protocol_branch_inventory.py` lines 159-200 -- its comment-and-literal stripper, so you can judge whether comment-only stripping is sufficient for the two new scan targets
    - `firestarter/tests/test_checker_convention.py` lines 140-250 -- `FLOOR` and `FIXTURE_FLOOR` and the four legs that consume them, to confirm that adding a pytest module under `tests/` (rather than a `scripts/check_*.py`) trips none of them, and `test_scope_is_firmware_only` at :304 which hard-codes the directory name
    - `firestarter/.github/workflows/build.yml` around line 161 and `beta-build.yml` around line 134 -- the `pytest tests/ -v` invocation, so the docstring's CI framing paragraph is accurate rather than assumed
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-PATTERNS.md` -- the section on this optional gate, which quotes all four things to copy verbatim and flags the literal-stripping question that must be re-confirmed
  </read_first>
  <action>
Create exactly one new file, `tests/test_boolean_convention_source_contract_v133.py`. Edit no
other file. Do not commit yet -- task 2 proves it and commits.

SHAPE -- copy `tests/test_write_path_source_contract_v131.py`'s idiom, not just its intent.

(a) ROOT AND SEAM HEADER. Resolve `_HERE` from `__file__` and `_REPO_ROOT` as its parent, then
define two relative paths -- the wrapper file `src/eprom_operations.cpp` and the engine file
`src/operation_utils.cpp` -- and two module-level scan paths, each defaulting to
`_REPO_ROOT / <relative path>` but overridable from a single named environment variable. Follow
the sibling's convention of one seam per module. Record in the docstring that the seam binds at
IMPORT time, which is why a planted-violation run must set it in a CHILD PROCESS -- setting it
inside an already-imported test does nothing.

(b) `_strip_comments`. Copy the sibling's implementation at :222-255 verbatim, including its
docstring's explanation that each stripped span is replaced with whitespace of the same shape so
every line number in the result matches the original file exactly. Then RE-CONFIRM the
justification that sibling gives for skipping literal-stripping: it asserts its own two scan
targets contain no string or character literal outside a comment or an `#include`. That was
confirmed for `eprom.cpp` and `memory.cpp`, NOT for `src/eprom_operations.cpp` and
`src/operation_utils.cpp`. Check both new targets yourself. If either contains a string or
character literal outside a comment or include, either extend the stripper to handle literals
(borrowing from `test_protocol_branch_inventory.py`'s stripper, which does both) or narrow the
scan region -- and say in the docstring which you did and why. Do not copy the justification
without re-checking it.

(c) NEEDLES, built by concatenation. The forbidden token is the negated op-layer call in a return
statement. Build it by concatenating two fragments so the literal never appears whole in this
module -- the sibling does exactly this at :167-172, and the reason is mechanical: a gate that
quotes its own forbidden token verbatim matches itself and can never pass. Build the two
permitted forwarding forms the same way.

(d) ABSENCE LEG. Copy `_assert_identifier_absent` at :302-315: find all word-boundary matches of
the needle in the comment-stripped target and assert the match list is empty, with a message
that reports the count and names the target. Apply it to the wrapper file. Do NOT apply it to
the engine file -- one negation legitimately survives there, at the callback delegation, and a
gate that forbade it would be wrong. Instead add a separate leg asserting that the engine
contains EXACTLY ONE negated call, and state in its message that this is plan 05's honest
nine-to-one reduction rather than an elimination, so a future reader who "finishes the job" by
flipping the callbacks trips a gate that explains itself.

(e) POSITIVE COUNTERPART -- the leg that makes the absence leg mean something. In the shape of
the sibling's `test_the_per_byte_loop_constructs_are_present` at :394-425, assert that the two
forwarding forms together occur exactly nine times in the comment-stripped wrapper file. Define
the expected total as a single named module constant so the number lives in one place. A deleted
file, a truncated file or a deleted call now fails loudly rather than satisfying the absence
assertion.

Add one more positive leg: assert the two early-return literals are still present in the wrapper
file -- these are the `FLAG_CAN_ERASE` refusal and the zero-chip-id refusal, both of which are
correct under both conventions and both of which a careless mechanical substitution would flip.
Assert on the enclosing function names and the returned literal, never on line numbers.

(f) NON-VACUITY LEGS, all mandatory. Copy `test_scan_targets_are_non_vacuous` at :641-669 in
spirit: for each DEFAULT target assert it `is_file()`, that its byte length is non-zero, that
`resolve().is_relative_to(_REPO_ROOT)` holds, and that its comment-stripped text is non-empty.
The `is_relative_to` leg is not decoration -- it closes by construction the landmine where a
checker's own directory anchor resolves somewhere unintended, so the module scans nothing and
exits zero. Assert against the DEFAULT paths, never through the seam, exactly as the sibling
does. Then copy `test_this_module_cannot_be_silently_skipped` at :671-698 and
`test_own_needles_do_not_appear_verbatim_in_this_module` at :700-715.

(g) DOCSTRING. This module's docstring is the only place the environment seam is inventoried, so
write it in full, following the sibling's structure: project and copyright header matching the
repo's other test modules; the requirement this gate pins; a "Defect class this closes" paragraph
saying that the wrapper translation unit compiles in NO native environment so this source
contract and the size-identity build are the only mechanical checks on those nine call sites; a
numbered `Coverage:` list, one entry per leg; an `Environment seams:` section naming the variable
and stating that it binds at import time so a planted-violation run needs a child process; and an
honest `CI framing` paragraph recording that `pytest tests/ -v` runs at `build.yml:161` and
`beta-build.yml:134` but that those workflows do not trigger on this milestone branch, so the gate
lands in a CI leg without being exercised by CI today.

DO NOT add a `scripts/check_*.py` companion. `tests/test_checker_convention.py` requires every
such script to have both a paired test module and a planted fixture, and would need its `FLOOR`
and `FIXTURE_FLOOR` raised. A pytest module under `tests/` trips neither, which is exactly why
the three sibling source-contract gates are shaped this way. Confirm that by running
`python3 -m pytest tests/test_checker_convention.py -q` after creating the file.

Pin NO line numbers anywhere in the gate. Phases 157 and 158 move both scan targets again and
Phase 159 remaps the composite diff, so every assertion must be on a symbol, a token or a count.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -f tests/test_boolean_convention_source_contract_v133.py && python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -v 2>&1 | tail -20 && python3 -m pytest tests/test_checker_convention.py -q 2>&1 | tail -3 && test "$(grep -c 'return !op_execute_' tests/test_boolean_convention_source_contract_v133.py)" -eq 0 && test "$(grep -cE 'eprom_operations\.cpp:[0-9]+|operation_utils\.cpp:[0-9]+' tests/test_boolean_convention_source_contract_v133.py)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -v` passes every leg, and the leg names cover: the wrapper absence assertion, the engine exactly-one-negation assertion, the nine-forwarding-call positive count, the two surviving early-return literals, target non-vacuity, the cannot-be-silently-skipped leg, and the own-needles leg.
    - Source assertion: `grep -c 'return !op_execute_' tests/test_boolean_convention_source_contract_v133.py` is 0. The needle is built by concatenation and the module does not match itself.
    - Source assertion: `grep -cE 'eprom_operations\.cpp:[0-9]+|operation_utils\.cpp:[0-9]+' tests/test_boolean_convention_source_contract_v133.py` is 0 -- no line number is pinned anywhere in the gate.
    - Source assertion: `grep -c 'constprop' tests/test_boolean_convention_source_contract_v133.py` is 0.
    - The non-vacuity leg asserts, for each of the two DEFAULT targets: `is_file()`, non-zero length, `resolve().is_relative_to(_REPO_ROOT)`, and non-empty comment-stripped text. It asserts against the default paths, not through the environment seam.
    - The expected forwarding-call total lives in exactly one named module constant whose value is 9, and the positive leg reads that constant rather than an inline number.
    - The engine leg asserts EXACTLY ONE surviving negated call and its message states the nine-to-one framing.
    - The literal-stripping question is answered explicitly in the docstring: either both new scan targets were confirmed to contain no string or character literal outside a comment or include, or the stripper was extended, or the scan region was narrowed -- with which one, and why, written down.
    - The docstring contains all six sections named in the action, including the CI framing paragraph with both workflow line references and the statement that those workflows do not trigger on this milestone branch.
    - The docstring states that `src/eprom_operations.cpp` compiles in no native environment and that this gate is a source contract, not a behavioural oracle.
    - `python3 -m pytest tests/test_checker_convention.py -q` passes with `FLOOR` and `FIXTURE_FLOOR` unchanged -- no `scripts/check_*.py` companion was added.
    - `git -C firestarter status --porcelain` shows exactly one untracked file, `tests/test_boolean_convention_source_contract_v133.py`, and nothing modified.
  </acceptance_criteria>
  <done>The gate exists, every leg passes on the real tree, the absence assertions are backed by positive count legs and four non-vacuity legs, no line number or clone suffix is pinned, and the docstring states what the gate does and does not prove.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate RED against a planted violation, then commit</name>
  <files>
    (no file is edited by this task -- it plants violations in throwaway worktrees, records the
    results, and commits the module task 1 created)
  </files>
  <read_first>
    - `firestarter/tests/test_boolean_convention_source_contract_v133.py` as task 1 wrote it -- specifically the environment-seam variable name and the docstring's statement that it binds at import time
    - `firestarter/tests/test_check_no_heap_or_64bit_symbols.py` lines 150-350 -- Phase 155's own gate, whose leg structure is this project's template for armed-negative evidence: an armed negative that MUST fail asserting a specific exit code and that the output NAMES the offender, a synthetic clean control derived at test time, a real clean control captured at a named sha, a fail-closed taxonomy separating a violation from a usage error, and a non-vacuity leg asserting the fixture contains at least one of each forbidden class
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-RESEARCH.md` §"The throwaway-worktree trap" -- the measured failure mode and the directory-name requirement
    - `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-05-SUMMARY.md` -- the post-flip source state the gate is asserting against
  </read_first>
  <action>
Prove the gate can fail, then commit it. No source edit.

FOUR PLANTED VIOLATIONS, each in a throwaway git worktree whose directory is named
`firestarter` -- for example `/tmp/probe156f/firestarter`. Any other directory name makes
`tests/test_checker_convention.py::test_scope_is_firmware_only` fail spuriously, because it
hard-codes the directory name, and that failure reads exactly like a real red. Research measured
this. In each worktree, copy in the new gate module, plant the violation, run
`python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -v`, record which legs
fail and their assertion messages, then remove and prune the worktree and re-assert the real tree
clean.

  Violation 1 -- REINTRODUCE the negation. Restore the leading logical-not on one of the nine
  wrapper return statements. Expected: the wrapper absence leg fails and names the count and the
  target. This is the armed negative the gate exists for.

  Violation 2 -- DELETE a forwarding call. Remove one of the nine wrapper bodies' return
  statements (or the whole wrapper). Expected: the positive count leg fails because the total is
  8, not 9. This is the leg that stops the absence assertion being satisfiable by deletion.

  Violation 3 -- EMPTY the scan target. Truncate `src/eprom_operations.cpp` to zero bytes.
  Expected: the non-vacuity leg fails on the non-zero-length or non-empty-stripped-text check,
  AND the positive count leg fails. Record both. This is the exact vacuity shape the project has
  been bitten by, and the point of the demonstration is that the absence leg alone would have
  passed here.

  Violation 4 -- FLIP an early-return literal. Change the `FLAG_CAN_ERASE` refusal literal in
  `eprom_erase`. Expected: the surviving-literals leg fails. This is the one mistake a careless
  mechanical substitution over the whole file would actually make, and the gate should catch it.

Also run the SEAM demonstration, in a child process, to prove the docstring's import-time claim:
point the environment variable at a file that does not exist and confirm the non-vacuity leg's
behaviour is what the docstring says it is. Record the exact invocation. Setting the variable
inside an already-imported test would prove nothing, which is why this is a child process.

RECORD THE EVIDENCE in the SUMMARY as a table: violation, worktree path (each ending in
`/firestarter`), the failing leg name, and the assertion message. Then the GREEN control: the
real tree, every leg passing, quoted. Follow the discipline the Phase 155 gate's own real-control
docstring sets -- name it as the real, committed, unedited tree at a named sha, not as a synthetic
derivative.

THEN COMMIT -- stage exactly `tests/test_boolean_convention_source_contract_v133.py`. Never
`git add -A`, never `git add .`. Record the pre-commit sha and afterwards assert
`git rev-list --count <PRE_SHA>..HEAD` equals 1, anchored to that recorded sha and never to
`HEAD~1`. Subject:
`test(156-06): pin the boolean convention with a non-vacuous source contract`. The body must
state that the wrapper translation unit compiles in no native environment so this is the only
mechanical check available; that the gate is proven RED against four distinct planted violations
including an emptied target; and that the engine deliberately retains exactly one negation, which
the gate asserts rather than forbids.

AFTER committing, run `python3 -m pytest tests/ -q` -- at least 314 passed (the pre-existing 313
plus this module's legs) and 0 failed. Also re-run `pio test -e native` once to confirm nothing
about adding a pytest module perturbed the native count from 172.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -q 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -3 && test -z "$(git status --porcelain)" && test "$(git worktree list | wc -l)" -eq 1 && test "$(git show --stat --name-only --format= HEAD | grep -c .)" -eq 1 && git log -1 --pretty=%s | grep -q '^test(156-06):'</automated>
  </verify>
  <acceptance_criteria>
    - Four planted violations are recorded in the SUMMARY, each with its worktree path ending in `/firestarter`, the failing leg name, and the verbatim assertion message. All four produced a genuine failure; none passed.
    - Violation 3 (emptied target) is recorded as failing BOTH the non-vacuity leg and the positive count leg, with the explicit note that the absence leg alone would have passed -- which is the whole reason the non-vacuity legs exist.
    - Violation 4 (flipped early-return literal) is recorded as failing the surviving-literals leg.
    - The seam demonstration was run in a CHILD PROCESS and its exact invocation is recorded, proving the docstring's import-time claim rather than asserting it.
    - The GREEN control is recorded against the real, committed, unedited tree at a named sha, described as such and not as a synthetic derivative.
    - `python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -q` passes on the real tree.
    - `python3 -m pytest tests/ -q` reports at least 314 passed and 0 failed.
    - `pio test -e native` still reports `172 test cases: 172 succeeded` over 17 suites.
    - `git worktree list` shows only the primary tree -- every throwaway worktree was removed and pruned.
    - `git rev-list --count <PRE_SHA>..HEAD` equals 1, anchored to the recorded pre-task sha.
    - `git show --stat HEAD` lists exactly one path, `tests/test_boolean_convention_source_contract_v133.py`. No firmware source file, no other test module, no `platformio.ini`, no `.planning/` path.
    - The commit subject begins `test(156-06):` and the body contains all three statements named in the action.
    - `git -C firestarter status --porcelain` empty after the commit.
  </acceptance_criteria>
  <done>The gate is proven able to fail four different ways including the vacuity shape this project has been bitten by, proven green on the real tree, and committed as one file with the evidence recorded in both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped source text -> committed gate | The gate's only input is source text. If it resolves the wrong path, or the path is empty, it asserts nothing while exiting zero |
| gate module -> its own forbidden token | A gate that contains its own needle verbatim matches itself and can never pass |
| planted-violation worktree -> real tree | A violation left behind, or a worktree misnamed, contaminates the result or fakes a red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-39 | Repudiation | a zero-match absence grep against a deleted or emptied file | high | mitigate | Four non-vacuity legs (exists, non-empty, resolves inside the repo root, non-empty stripped text) plus a positive count leg pinning nine forwarding calls. Planted violation 3 empties the target and is recorded as failing both, with the note that the absence leg alone would have passed |
| T-156-40 | Repudiation | a gate whose own needle makes it self-matching | medium | mitigate | Needles built by concatenation, plus a dedicated leg asserting the module's own needles do not appear verbatim in it, copied from the sibling gate |
| T-156-41 | Tampering | a directory anchor resolving outside the repository so the module scans nothing | high | mitigate | `resolve().is_relative_to(_REPO_ROOT)` asserted on both DEFAULT targets. This closes by construction the landmine where a checker's own path anchor points at the wrong place and the scan silently covers nothing |
| T-156-42 | Elevation of privilege | a future mechanical substitution flipping an early-return refusal literal | high | mitigate | A dedicated positive leg asserts both surviving refusal literals, and planted violation 4 proves that leg fires |
| T-156-43 | Repudiation | forbidding the one legitimate surviving negation | medium | mitigate | The engine leg asserts EXACTLY ONE rather than zero, and its message states the nine-to-one framing so a future reader who flips the callbacks meets a gate that explains itself |
| T-156-44 | Repudiation | pinning a line number that Phases 157-159 invalidate | medium | mitigate | Prohibited, and negative-grepped in the acceptance criteria. Every assertion is on a symbol, token or count |
| T-156-45 | Repudiation | a throwaway worktree named anything but `firestarter` | medium | mitigate | Required in the action and checked in the acceptance criteria; the sibling convention gate hard-codes the directory name |
| T-156-46 | Repudiation | claiming this gate is behavioural coverage | medium | mitigate | Stated in the docstring, in the plan, in the commit body and in an acceptance criterion: it is a source contract, and the translation unit compiles in no native environment |
| T-156-47 | Tampering | npm/pip/cargo installs | high | accept | No package installed; the gate is stdlib-plus-pytest only. RESEARCH's Package Legitimacy Audit records zero packages |
</threat_model>

<verification>
- `python3 -m pytest tests/test_boolean_convention_source_contract_v133.py -v`: every leg passes on the real tree.
- Four planted violations, each in a worktree named `firestarter`, each producing a recorded failure.
- The seam demonstration run in a child process, invocation recorded.
- `python3 -m pytest tests/test_checker_convention.py -q`: passes, `FLOOR` and `FIXTURE_FLOOR` unchanged.
- `python3 -m pytest tests/ -q` after the commit: at least 314 passed, 0 failed.
- `pio test -e native`: still 172/172 over 17 suites.
- `git worktree list`: primary tree only. Exactly one commit, exactly one path.
</verification>

<success_criteria>
The nine wrapper call sites have the only mechanical check their translation unit permits, and it
is a non-vacuous one: absence assertions backed by a positive nine-call count, four non-vacuity
legs, and a leg pinning the two refusal literals a careless substitution would break. The gate is
proven RED four different ways -- including against an emptied scan target, where the absence leg
alone would have passed -- and GREEN against the real tree. It pins no line number and no clone
suffix, and its docstring states plainly that it proves what the source says, not what the code
does.
</success_criteria>

<output>
Create `.planning/phases/156-duplicated-report-extraction-boolean-convention-repair-firmw/156-06-SUMMARY.md` when done.
</output>