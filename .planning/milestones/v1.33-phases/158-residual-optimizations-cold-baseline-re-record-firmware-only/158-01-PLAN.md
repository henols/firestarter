---
phase: 158-residual-optimizations-cold-baseline-re-record-firmware-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/158-before-figures.md
autonomous: true
requirements: [LAND-04, LAND-07, LAND-08]

must_haves:
  truths:
    - "The pre-phase position of every figure this phase moves is captured COLD, from `rm -rf .pio/build/<env>` plus exactly one `pio run -e <env>` per env, before any source edit exists."
    - "LAND-04 is recorded with BOTH clauses: no `.github/` workflow invokes `check_size_baseline.py` as a size gate, AND the checker is nonetheless executed in CI as a subprocess by its own paired pytest at `build.yml:161`, whose trigger is `push: branches: ['**','!beta']`."
    - "LAND-07's three token bounds are re-derived by a script a reader can re-run against `pinouts.json` and the real chip database, and the criterion's `57 tokens / 7 headroom` is stated as reproducible by none of the three counting rules."
    - "LAND-07's conclusion rests on the unknown-key forward-compatibility budget, never on arithmetic impossibility -- because `64 -> 56` is arithmetically available and is declined on the budget."
    - "LAND-08's flakiness record carries both the prior recorded data points and this plan's own three timed same-tree `pio test -e native` runs, with the prohibition on quoting a wall time as evidence stated in the record itself."
    - "`check_size_baseline.py` default mode is recorded RED at this position, with its failing lines verbatim, so LAND-01's later polarity flip to GREEN is falsifiable rather than assumed."
    - "The four legs that redden on a re-record are named with their file:line and their remedy, cited to the research that observed them, and distinguished in the record from the four porcelain legs that redden for any dirty file."
  artifacts:
    - .planning/v1.33/158-before-figures.md
  key_links:
    - "The three cold pre-phase figures are the only like-for-like `before` side of this phase's headline delta; a warm before against a cold after is a mixed-convention number."
    - "The default-mode RED shape recorded here is what makes LAND-01's GREEN in plan 04 evidence of a discharge rather than an unfalsifiable claim."
    - "LAND-08's record is what stops plan 02 from attributing a load-flaky native failure to the `jsmntok_t` narrowing."
  prohibitions:
    - "Must NOT edit any file under `firestarter/`. This plan measures and writes into `.planning/` only; `git -C firestarter status --porcelain` must be empty at start and at end."
    - "Must NOT edit `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`. Every figure correction lives in this record (S-3, the Phase 155/156/157 convention); plan 07 alone closes those two documents out, with scoped `Edit` replacements (OD-9 as amended)."
    - "Must NOT remove, edit or reference-as-resolvable `.planning/v1.33/CITATIONS-STALE.md`. REMAP-04 makes it close-blocking and Phase 159 owns its removal."
    - "Must NOT run the Phase 154 citation remap tool and must NOT repair any `file:LINE` citation (D-01, D-05)."
    - "Must NOT transcribe any figure from `check_size_baseline.py --rebuild`. `_rebuild_avr` uses `pio run -t clean -e <env>`, not the mandated `rm -rf .pio/build/<env>` (Pitfall 6). `--rebuild` is for checking, never for recording."
    - "Must NOT run `pytest tests/` from a `/tmp` worktree for any figure that enters the record -- `tests/meta_presence.py` silently skips 32 cross-repo legs there (F-12, S-6)."
    - "Must NOT quote a wall-clock time as evidence of anything (D-04)."
    - "Must NOT author any bench, hardware or silicon criterion (D-02)."
    - "Must NOT author or widen any MERGE-05 exemption constant. A reduction needs none (D-03)."
    - "Must NOT propose any step toward the binary command protocol; LAND-07 points at v1.28 / Backlog 999.35 and stops there."
    - "Must NOT stage or re-pin the `firestarter` or `firestarter_app` gitlink -- pre-existing drift since Phase 154, operator-gated (OD-10)."
    - "Must NOT use `Write` on any pre-existing `.planning/` file; `158-before-figures.md` is net-new, so `Write` is correct for it and for nothing else here."
---

<objective>
Capture the pre-phase position of every figure Phase 158 moves, and discharge the phase's three
purely record-only criteria -- LAND-04, LAND-07 and LAND-08 -- into
`.planning/v1.33/158-before-figures.md`.

Purpose: three of the eight criteria are records, and a record-only criterion still needs a real
acceptance criterion -- the reproducible command and its exact expected output, not "a record
exists". Two of them carry figures the ROADMAP and REQUIREMENTS get wrong (LAND-07's `57 tokens /
7 headroom` is reproducible by no counting rule; LAND-03's `observed=172` is stale), and one
(LAND-04) is true only with a second clause the criterion does not state. Beyond that, LAND-01's
own discharge in plan 04 is a **polarity flip** -- `check_size_baseline.py` default mode has been
RED since Phase 155 and must be GREEN afterwards -- and a polarity flip is only evidence if the RED
shape was written down first.

Output: `.planning/v1.33/158-before-figures.md`, committed in the meta repo. No file under
`firestarter/` is touched.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-RESEARCH.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-PATTERNS.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-VALIDATION.md
@.planning/v1.33/157-before-figures.md
@.planning/v1.33/157-after-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All twelve appear in every plan of this phase, in every SUMMARY, and in both phase records.

1. **`check_size_baseline.py`, `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` are invoked by NO CI workflow.** Every size gate this phase leans on is a **local-run obligation**. A green CI run is not evidence that any of them passed. This is LAND-04's whole content.
2. **But the checker IS executed in CI by its own paired pytest.** `build.yml:161` runs `pytest tests/ -v`, ungated by any `if:`, and `build.yml`'s trigger is `push: branches: ['**','!beta']` -- it fires on this milestone branch. LAND-04's honest statement has **two clauses**.
3. **The ARM half of LAND-05 is unverified locally unless the toolchain install succeeds.** `arm-none-eabi-gcc` and `cmake` are both absent from this devcontainer; `platform/py32f071/CMakeLists.txt` compiles the same `lib/jsmn/src/jsmn.c`, and `py32f071.yml` is the loud gate at push time. **Never claim ARM coverage that was not built.**
4. **LAND-06's runtime half is unmeasurable in this milestone.** D-02 forbids a bench criterion, so the decline rests on a size measurement plus a coverage gap and never on a runtime number.
5. **The algorithm-5 page-boundary path has ZERO behavioural native coverage.** The only case in `test/native/avr/test_val_5v_page/test_val_5v_page.cpp` that executes `flash_5v_page_write_execute` drives a 4-byte write, which never crosses a 64-byte boundary.
6. **`sizeof(jsmntok_t)` cannot be asserted in a native test.** AVR gives 6 B, the host gives 12 B. The RAM saving is witnessed by the linker's `RAM: used N` line and by `avr-nm`, never by a native `sizeof`.
7. **The native suite is load-flaky (D-04).** No suite failure may be attributed to a tree change on N=1, and wall time carries no signal on its own.
8. **A `/tmp` worktree run of `pytest tests/` silently skips 32 cross-repo legs** (355 versus 323+32, both measured). Gate-purpose runs happen from `/workspaces/firestarter`.
9. **`lib/jsmn/src/jsmn.h` carries a dead duplicate implementation** under `#ifndef JSMN_HEADER` while `#define JSMN_HEADER` precedes it -- it compiles in no translation unit but is live text to a naive grep.
10. **LAND-07's conclusion is a budget argument, not an impossibility argument.** The derived headroom is 13 tokens, so `64 -> 56` is arithmetically available; it is declined because the unknown-key skip's forward-compatibility budget is load-bearing.
11. **Every `file:LINE` citation this phase writes will be newly stale.** Phase 159 remaps them exactly once over the composite diff (D-01, D-05). This phase runs no remap and repairs no citation.
12. **The `firestarter` gitlink in the meta repo is drifted** (meta tracks `2ad5b322`, the submodule HEAD is `785e644` or later) -- pre-existing since Phase 154, operator-gated, and **not re-pinned by this phase**.

## The ten phase decisions -- OD-1 through OD-10

Locked before planning. No plan re-opens one, plans the alternative, or adds a task that evaluates one.

- **OD-1 -- LAND-05 TAKEN.** `jsmntok_t` narrows 8 -> 6 B with layout `uint8_t type; uint8_t size; int start; int end;`. `start`/`end` stay `int` (twelve `-1` sentinel field references on six lines of `jsmn.c`).
- **OD-2 -- LAND-06 DECLINED.** `src/proms/flash_5v_page.cpp` is not edited and no `test_val_5v_page` case is added. The criterion is discharged by recording the measurement, the two `call __udivmodsi4` sites, and the zero behavioural native coverage as the stated reason.
- **OD-3 -- LAND-03 FIXED, not carried**, on the axis-split argument: a frozen test-case count is a **test-inventory** axis, a third axis alongside the frozen growth axis and the licensed board-identity axis.
- **OD-4 -- the two false CI-coverage docstrings are CORRECTED** (comment-only, no assertion change).
- **OD-5 -- the `tests/test_checker_convention.py` FLOOR carry-forward is CLOSED**, in the same commit that reconciles it.
- **OD-6 -- `jsmn.h`'s dead duplicate implementation is LEFT UNEDITED**, and the source-contract leg's region slice is the machine-checked reason that is safe.
- **OD-7 -- the ARM half is verified locally if the toolchain installs once**, otherwise the ceiling is recorded and `py32f071.yml` witnesses it at push time.
- **OD-8 -- the `*_v153*` fixture family is retired in place and KEPT.** Only 4 new fixtures plus 2 updated in place are authored; Groups 2 and 3 are deliberately **not** needed.
- **OD-9 (as amended by the orchestrator, 2026-08-24) -- `ROADMAP.md` and `REQUIREMENTS.md` are never REGENERATED, and are edited by exactly one plan.** Both files are hand-authored for v1.33, so the GSD requirements and roadmap mutation verbs -- which reformat the whole document -- must never touch them, and neither may a whole-file `Write`. Plans 01 through 06 edit neither file; every *figure correction* lives in the two `*-figures.md` records (S-3). **Plan 07 alone** closes them out with scoped `Edit` replacements, exactly as `157-07-PLAN.md` did: it ticks the eight LAND bullets and their traceability rows, fills the `**Measured**` and `**Plans:**` lines, and scope-corrects the three stale figures **in place beside** the numbers they supersede, so the ROADMAP stops being a source of superseded figures. The declined alternative -- leaving both documents untouched -- ships a milestone whose requirement set reads half-open and whose criteria still carry the numbers this phase disproved.
- **OD-10 -- neither the `firestarter` nor the `firestarter_app` gitlink is re-pinned.** The drift predates Phase 154, Phase 157 recorded rather than re-pinned it, and milestone close (Phase 159) owns it; it is recorded with both sha pairs and handed forward. **Operational note for whoever does re-pin:** `git commit -- <path>` **discards** a gitlink `update-index`, so the re-pin must drop the pathspec and commit the staged index directly -- recorded here so Phase 159 does not rediscover it.

## The corrections ledger -- C-1 through C-13

Established here; closed out in `158-after-figures.md` by plan 06. The numbering is fixed and no plan renumbers it.

| ID | Source document's claim | What replaces it |
|----|-------------------------|------------------|
| C-1 | ROADMAP criterion 3 / REQUIREMENTS LAND-03: `cases baseline=141 observed=172` | `observed` is the re-measured current count (184-class at the research position), not 172 |
| C-2 | ROADMAP criterion 5 / REQUIREMENTS LAND-05: `−128 B RAM for +30 B flash` | Flash is a **win**: measured `−138 / −138 / −136 B` on `uno` / `uno328pb` / `leonardo`, alongside the `−128 B` RAM. `+30 B` is reproducible on no layout tried |
| C-3 | ROADMAP criterion 6 / REQUIREMENTS LAND-06: masking costs `+22 B flash` (flat) | `+22 / +24 / +22 B` -- right on `uno` and `leonardo`, **2 B low on `uno328pb`** |
| C-4 | ROADMAP criterion 7 / REQUIREMENTS LAND-07: `57 tokens`, `7 tokens of headroom` | Three bounds, each with its own scope: **50 / 14** observed over the real chip database, **51 / 13** over any real pin map with every optional scalar, **55 / 9** field-wise-maximum synthetic. `57` is reproducible by none of the three |
| C-5 | LAND-07's implied argument that 64 is not reducible arithmetically | It **is** arithmetically reducible (`64 -> 56` clears the real maximum by 5). It is declined on the unknown-key forward-compatibility budget |
| C-6 | LAND-04 as a single clause | Two clauses: no workflow invokes the checker as a size gate, **and** the checker is executed in CI by its own pytest at `build.yml:161` |
| C-7 | `size_baseline.json` `meta.consumed_by` names two consumers | There are **three**; `scripts/check_release_assets.py` derives its required asset set from `avr_targets`' keys and runs in CI at `beta-build.yml:327` |
| C-8 | `size_baseline.json` `envs_agree_note` quotes `{cases: 151, suites: 17}` | Stale; already flagged by `meta.native_case_count_revision_260822` in the same file |
| C-9 | `tests/test_check_size_baseline.py` and `tests/meta_presence.py` both claim no CI leg runs `pytest tests/` on this branch | Both false since `build.yml`'s trigger widened to `push: branches: ['**','!beta']`, documented in that workflow's own header comment |
| C-10 | `tests/test_checker_convention.py`: `FLOOR = 7` described as "the number actually shipped"; `FIXTURE_FLOOR = 16` | 8 checkers ship; 30 `planted_*` fixtures ship before this phase's new plant. Both floors are `>=`, so the gate is loose, not broken |
| C-11 | Every prior generation's severance docstring documents "the same four groups", 13 files | This generation authors **4 new files plus 2 updated in place**. Groups 2 and 3 are not needed because **no MERGE-05 exemption is authored for a reduction**, so the allowance is unchanged and all three `planted_size_baseline_policy_*_v153.log` legs stay valid |
| C-12 | LAND-03: the canonical invocation exits 1 "before it ever reports flash" | True of the **report**, not of the comparison. The AVR loop runs first and **passes**; the native loop then appends two failures and `_print_fail` returns 1, so `_print_pass` -- the line that would have carried the flash figures -- is never reached |
| C-13 | `157-after-figures.md` §2's Leonardo Caterina headroom `3438 B` | Correct at `785e644`; moves once LAND-05 lands. Re-measured at this phase's final position |

## Artifacts this phase produces

**CREATED by this plan:** `.planning/v1.33/158-before-figures.md`.

**CREATED by later plans of this phase** -- listed so drift verification does not flag them as
unacknowledged new symbols:

- `firestarter/tests/test_jsmn_token_layout_source_contract_v158.py` (plan 02), containing
  `_HERE`, `_REPO_ROOT`, `_JSMN_HEADER_REL`, `_SCAN_JSMN`, `_LIVE_STRUCT_END_MARKER`,
  `_strip_comments`, `_assert_identifier_absent`, `_live_header_region`,
  `test_token_start_and_end_remain_signed_int`, `test_token_type_and_size_are_uint8`,
  `test_scan_target_is_non_vacuous`, `test_this_module_cannot_be_silently_skipped`,
  `test_own_needles_do_not_appear_verbatim_in_this_module`, and the import-time environment seam
  `FIRESTARTER_JSMN_TOKEN_LAYOUT_SCAN_SOURCE`.
- `firestarter/tests/fixtures/captured_build_v158_uno.log`,
  `captured_build_v158_uno328pb.log`, `captured_build_v158_leonardo.log`, and
  `planted_size_baseline_flash_regression_v158.log` (plan 04).
- `.planning/v1.33/158-after-figures.md` (plan 06).

**CHANGED by later plans:** `firestarter/lib/jsmn/src/jsmn.h` (`jsmntok_t.type` becomes `uint8_t`,
`jsmntok_t.size` becomes `uint8_t`, field order becomes `type, size, start, end`, `#include
<stdint.h>` added) -- plan 02. `firestarter/scripts/baseline/size_baseline.json` (all twelve
`avr_targets` figures, the four `native_envs` figures for `native` and `native_nodevtools`, a new
`meta.cold_rerecord_phase158` note key, plus the `meta.consumed_by` and `envs_agree_note` repairs)
and `firestarter/tests/fixtures/captured_test_native_summary.log` /
`captured_test_native_nodevtools_summary.log` (re-captured in place) and
`firestarter/tests/test_check_size_baseline.py` (three legs' fixture tuples repointed, two figure
literals and one case-count literal updated) -- plan 04.
`firestarter/scripts/baseline/size_baseline_base01.json` (four `native_envs` integers plus a new
`meta.native_inventory_axis_phase158` note key), `firestarter/tests/test_checker_convention.py`
(`FLOOR` 7 -> 8, `FIXTURE_FLOOR` 16 -> the re-counted value, plus its docstring's carry-forward
paragraph), and the CI-coverage paragraphs of
`firestarter/tests/test_check_size_baseline.py` and `firestarter/tests/meta_presence.py` -- plan 05.

**CHANGED by plan 07, the only plan that touches either document:** `.planning/ROADMAP.md` §Phase
158 -- a new `**Measured**` line, the `**Plans:**` line, seven ticked plan checkboxes, and appended
closures plus in-place scope corrections on criteria 1 through 8; and `.planning/REQUIREMENTS.md` §5
-- the eight `LAND-0N` checkboxes with their discharge sentences and corrections -- plus the eight
`LAND-0N` traceability rows. Scoped `Edit` replacements only; neither file is ever regenerated
(OD-9 as amended).

**NOT created and NOT edited by any plan of this phase:** `src/proms/flash_5v_page.cpp`,
anything under `test/native/avr/test_val_5v_page/`, `lib/jsmn/src/jsmn.c`,
`include/json_parser.h` (`NUMBER_JSNM_TOKENS` stays 64), any `firestarter_app/` file,
`.planning/v1.33/CITATIONS-STALE.md`, `.planning/STATE.md`, and every Phase 159 entry in either
planning document.

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-phase position -- cold builds, both native envs, the full pytest suite, and every gate leg's current shape</name>
  <files>
    (no tracked file is edited -- this task measures. Scratch output goes to `/tmp/gsd-158/`.)
  </files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` -- `meta.generated_by`, `meta.note`, `meta.warm_vs_cold_correction`, `meta.consumed_by`, the full `avr_targets` and `native_envs` blocks, `envs_agree_note`, and the whole `warnings` block, so the pre-phase recorded values are read from the file rather than from a research table
    - `firestarter/scripts/baseline/size_baseline_base01.json` -- the `native_envs` block (141/141/17 on both envs) and `meta.re_anchor_note`
    - `firestarter/scripts/check_size_baseline.py` -- the Usage block's canonical invocation, `AVR_ENVS` and `NATIVE_ENVS`, `compare_native`, `compare_avr_policy_merge05`, lines `697` and `709`, `main()`'s AVR-loop-then-native-loop-then-`_print_fail` ordering, and `_rebuild_avr`
    - `firestarter/.github/workflows/build.yml` -- the `on:` block and its header comment, the `pytest tests/ -v` step, and the two `pio test` steps
    - `.planning/v1.33/157-after-figures.md` -- §2 (the cold ledger and the Caterina headroom), §10 (the eight-leg gate ledger, including the default-mode failing lines), §11 (the one-sidedness), §16 (the handoffs to this phase)
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-RESEARCH.md` -- F-1, F-2, F-4, F-8, F-12, § Code Examples, and Pitfalls 1 through 9
  </read_first>
  <action>
Work from `/workspaces/firestarter` throughout. Create `/tmp/gsd-158/` for scratch logs. Assert
`git -C /workspaces/firestarter status --porcelain` is empty before starting and record the HEAD sha
as `FW_PRE_SHA` together with its subject line; also record the meta repo HEAD. If the tree is
dirty, stop and report rather than measuring a dirty tree.

STEP 1 -- the cold AVR position. For each of `uno`, `uno328pb`, `leonardo`, in that order, run
`rm -rf .pio/build/<env>` and then **exactly one** `pio run -e <env>`, teeing the full output to
`/tmp/gsd-158/pre-cold-<env>.log`. This is the recipe `size_baseline.json`'s own
`meta.generated_by` mandates; do not substitute `pio run -t clean`, do not batch the three envs
into one invocation, and do not use `check_size_baseline.py --rebuild`. Record, per env, the
`Flash: ... used N bytes` figure, the `RAM: ... used N bytes` figure and the count of `warning:`
lines (AVR policy is `== 0`). Label every figure COLD. Compute Leonardo's Caterina headroom against
the `28672` bootloader cliff and note that it supersedes `157-after-figures.md` §2's `3438 B` only
if it differs (C-13).

STEP 2 -- the native position and LAND-08's three timed runs. Run `pio test -e native` **three
times**, teeing each to `/tmp/gsd-158/pre-native-run{1,2,3}.log`, and record for each run the
`N test cases: N succeeded` figure, the suite count, and the wall-clock duration taken from the
run's own trailing `in HH:MM:SS.mmm` field -- never from an external stopwatch. Then run
`pio test -e native_nodevtools` once, teed to `/tmp/gsd-158/pre-native-nodevtools.log`, and record
the same three fields. These four runs are LAND-08's own new same-tree data points. If any run's
case count differs from the others, **re-run before concluding anything** (D-04) and record every
observation including the outlier; a count that returns to the majority value on re-run is a flake,
and the record must say so in those words.

STEP 3 -- the full host-adjacent gate suite, from the canonical checkout. Run
`python3 -m pytest tests/ -q -o addopts=""` from `/workspaces/firestarter` -- the `-o addopts=""`
override is required because the project's `addopts` is `-ra -q` and doubling `-q` suppresses the
count line, which is the evidence here. Record the count line verbatim. Assert the word `skipped`
does **not** appear: its absence is the proof that the 32 cross-repo legs of
`tests/test_flash_path_record_sync.py` actually ran (F-12). Also run
`python3 -m pytest tests/test_check_size_baseline.py -q -o addopts=""` and record its count.

STEP 4 -- every gate leg's current shape, stated before it is run. For each leg below, write down
the expected shape first, then run it and record the command, the exit status and the salient
output, marking whether any CI workflow invokes it:
  (a) `python3 scripts/check_build_warnings.py` -- expected exit 0.
  (b) `python3 scripts/check_no_heap_or_64bit_symbols.py` -- expected exit 0.
  (c) `python3 scripts/check_size_baseline.py --policy merge05 --baseline
      scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/gsd-158/pre-cold-uno.log
      --avr-log uno328pb=/tmp/gsd-158/pre-cold-uno328pb.log --avr-log
      leonardo=/tmp/gsd-158/pre-cold-leonardo.log` -- expected **exit 0**, with a `PASS:` line whose
      three flash deltas are **negative** against positive allowances. Capture that PASS line
      verbatim: it **is** LAND-02's one-sidedness evidence, and it belongs in the record as a quote
      rather than as a paraphrase of D-03.
  (d) the same script with `--policy merge05 --baseline scripts/baseline/size_baseline_base01.json
      --rebuild` -- expected **exit 1** with **exactly two** failure lines, both of the shape
      `cases baseline=141 observed=<N>`, and **no** AVR `flash_used` or `ram_used` line. Any third
      line, or any AVR line, is new and must be reported rather than absorbed. This is LAND-03's
      pre-fix state and C-12's mechanism.
  (e) the same script in **default mode** against `scripts/baseline/size_baseline.json`, using the
      three `--avr-log` paths from step 1 -- expected **RED**, because default mode is byte-identity
      and the recorded `avr_targets` figures have been stale since Phase 155. Record every failing
      line verbatim. **This RED shape is what makes plan 04's GREEN a discharge rather than a
      claim.**
  (f) `sed -n '697p;709p' scripts/check_size_baseline.py` -- quote both comparisons verbatim as the
      one-sidedness mechanism in source, alongside (c)'s PASS line as its observed consequence.

STEP 5 -- the four legs that will redden, named from research and distinguished from the four that
will not. Record `test_clean_avr_all_three_envs_pass`, `test_clean_native_both_envs_pass`,
`test_planted_flash_regression_flips_checker_to_failure` and
`test_default_mode_is_unchanged_by_the_new_flag`, each with its `tests/test_check_size_baseline.py`
line number located by reading the file **now** rather than copied from the research (line numbers
shift), the fixture family each currently reads, and its remedy. Cite them as **observed** by
`158-RESEARCH.md` F-2's simulated re-record, not as predicted. Separately record the four porcelain
legs -- two in `tests/test_requirement_case_mapping_v131.py` and two in
`tests/test_trace_segment_exhaustiveness_v131.py` -- which assert `git status --porcelain` is empty
after their own planted mutation and therefore redden for **any** dirty file. The record must state
that these two sets have different causes and different remedies, and that conflating them is
Pitfall 4.

STEP 6 -- confirm the surviving plants. Record that
`planted_size_baseline_policy_uno_over_band_v153.log`,
`planted_size_baseline_policy_leonardo_growth_v153.log` and
`planted_size_baseline_policy_ram_moved_v153.log` all still back **green** legs at this position,
and that no MERGE-05 exemption is authored by this phase, so their derivation basis
(`allowance + 1`) is unchanged and they need no re-plant (C-11, OD-8). Also record the six MERGE-05
literals and their line numbers, read from the checker's source, as the values that must be
byte-unchanged at the end of the phase.

Hand every measured value to tasks 2 and 3. Write no file in this task. Leave `/tmp/gsd-158/`
in place: plan 04 re-captures its own logs, but this plan's records must remain re-readable within
the session.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && mkdir -p /tmp/gsd-158 && for e in uno uno328pb leonardo; do rm -rf .pio/build/$e; pio run -e $e > /tmp/gsd-158/pre-cold-$e.log 2>&1 || exit 1; grep -qE 'Flash:.*used [0-9]+ bytes' /tmp/gsd-158/pre-cold-$e.log || exit 1; grep -qE 'RAM:.*used [0-9]+ bytes' /tmp/gsd-158/pre-cold-$e.log || exit 1; test "$(grep -c 'warning:' /tmp/gsd-158/pre-cold-$e.log)" = "0" || exit 1; done && pio test -e native > /tmp/gsd-158/pre-native-run1.log 2>&1 && grep -qE '[0-9]+ test cases: [0-9]+ succeeded' /tmp/gsd-158/pre-native-run1.log && python3 -m pytest tests/ -q -o addopts="" > /tmp/gsd-158/pre-pytest.log 2>&1 && grep -qE '[0-9]+ passed' /tmp/gsd-158/pre-pytest.log && test "$(grep -c skipped /tmp/gsd-158/pre-pytest.log)" = "0" && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=/tmp/gsd-158/pre-cold-uno.log --avr-log uno328pb=/tmp/gsd-158/pre-cold-uno328pb.log --avr-log leonardo=/tmp/gsd-158/pre-cold-leonardo.log > /tmp/gsd-158/pre-merge05.log 2>&1 && grep -q 'PASS:' /tmp/gsd-158/pre-merge05.log && test "$(grep -oE 'flash=[0-9]+/[0-9]+\[-[0-9]+' /tmp/gsd-158/pre-merge05.log | wc -l)" = "3" && sed -n '697p;709p' scripts/check_size_baseline.py | grep -c 'delta >' | grep -q 2 && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `FW_PRE_SHA` and its commit subject are recorded, the meta HEAD is recorded, and `git -C /workspaces/firestarter status --porcelain` is empty both before and after the task.
    - Three cold AVR logs exist at `/tmp/gsd-158/pre-cold-{uno,uno328pb,leonardo}.log`, each produced by `rm -rf .pio/build/<env>` followed by exactly one `pio run -e <env>`; each carries a `Flash: ... used N bytes` line and a `RAM: ... used N bytes` line, and each has a `warning:` count of exactly `0`.
    - Every AVR figure recorded in the SUMMARY is labelled COLD, and Leonardo's Caterina headroom against `28672` is recorded as measured with the `157-after-figures.md` `3438 B` figure named as the value it either confirms or supersedes.
    - Three `pio test -e native` runs and one `pio test -e native_nodevtools` run are recorded, each with its case count, its succeeded count, its suite count and its own trailing duration field. If any count disagrees, the outlier and the re-run are both recorded and the flake is named as such per D-04.
    - `python3 -m pytest tests/ -q -o addopts=""` was run from `/workspaces/firestarter`, its count line is recorded verbatim, and the word `skipped` is absent from its output -- proving the 32 cross-repo legs ran.
    - The canonical MERGE-05 `--avr-log` invocation exited `0`, and its verbatim `PASS:` line is captured with all three flash deltas printed as **negative** numbers against positive allowances.
    - The canonical MERGE-05 `--rebuild` invocation exited `1` with **exactly two** failure lines, both matching `cases baseline=141 observed=`, and **zero** lines naming `flash_used` or `ram_used`. Any deviation is reported, not absorbed.
    - Default mode against `scripts/baseline/size_baseline.json` is recorded RED with every failing line verbatim, and the SUMMARY states in advance that plan 04 must flip this leg to GREEN.
    - `scripts/check_size_baseline.py` lines `697` and `709` are quoted verbatim and both are growth-only comparisons.
    - The four reddening legs are each recorded with a line number located by reading `tests/test_check_size_baseline.py` in this session, the fixture family it reads, and its remedy; and the four porcelain legs are recorded separately with their distinct cause. Source assertion: the SUMMARY contains no sentence that treats the two sets as one.
    - The three surviving `planted_size_baseline_policy_*_v153.log` legs are recorded as green at this position, and the six MERGE-05 literals with their line numbers are recorded as the values that must be byte-unchanged at phase end.
    - No file under `firestarter/` was created, modified or deleted by this task.
  </acceptance_criteria>
  <done>The pre-phase position is measured cold on all three AVR targets with zero warnings, both native envs are measured with four timed runs, the full `pytest tests/` suite is measured from the canonical checkout with no skips, every gate leg's exit status and salient output are captured with its expected shape stated in advance, the default-mode RED shape is written down, and the four reddening legs are enumerated by reading the current source and kept distinct from the four porcelain legs.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive LAND-07's token arithmetic and prove LAND-04's two clauses</name>
  <files>
    (no tracked file is edited -- this task derives. Scratch scripts go to `/tmp/gsd-158/`.)
  </files>
  <read_first>
    - `firestarter/include/json_parser.h` -- the `NUMBER_JSNM_TOKENS` definition and its surrounding comment
    - `firestarter/src/firestarter.cpp` -- the `jsmntok_t tokens[NUMBER_JSNM_TOKENS]` declaration and the `jsmn_parse` call site
    - `firestarter/lib/jsmn/src/jsmn.c` -- `jsmn_alloc_token`'s `NULL` return and the `JSMN_ERROR_NOMEM` path, plus `jsmn_parse`'s own token-counting loop, which is the counting rule the derivation must reproduce
    - `firestarter/src/json_parser.c` -- the wire-key table and its PROGMEM strings, `parse_bus_config`'s four inner keys, and the unknown-key skip that advances two tokens at a time
    - `firestarter_app/firestarter/database.py` -- `convert_to_programmer` and `get_bus_config`, including the branch that drops `vpp-pin` when it resolves to a chip-enable or output-enable pin
    - `firestarter_app/firestarter/eprom_operations.py` -- the sites that add `cmd`, `flags`, the optional `address`, and the optional read-timing keys to the wire dict
    - `firestarter/.github/workflows/build.yml`, `beta-build.yml` and `py32f071.yml` -- every `run:` step and every `on:` trigger
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-RESEARCH.md` -- F-4 and F-7 in full, including the `jsmn_count` snippet in § Code Examples and the three-bounds table
  </read_first>
  <action>
STEP 1 -- LAND-04, clause 1. Run each of these and record the command, its output and its exit
status: `grep -rn "check_size_baseline" .github/` inside `/workspaces/firestarter` (expected: no
output, exit 1); the same grep inside `/workspaces` and inside `/workspaces/firestarter_app`
(expected: no output); and
`grep -rn "scripts/check_" .github/workflows/*.yml` filtered to `run:` lines. Record that of the
eight `scripts/check_*.py` files in the firmware repo, **exactly one** -- `check_release_assets.py`
-- is invoked by any workflow, and name the other seven as local-run obligations. Verify the count
of eight by `ls scripts/check_*.py` rather than by transcription.

STEP 2 -- LAND-04, clause 2. Record `grep -n "pytest tests/" .github/workflows/build.yml` and the
step's line number, that the step is ungated by any `if:` and sits above the publish boundary, and
`sed -n` over `build.yml`'s `on:` block showing the `push: branches` filter that includes `'**'`
and excludes `beta`. Record the sibling leg in `beta-build.yml`. Then state the consequence in one
sentence: re-recording `size_baseline.json` without severing the fixtures turns CI red on this
branch. Enumerate the CI legs on this branch exhaustively -- the two `pio test` legs, the `pytest
tests/ -v` leg, the `pio run` leg, and the separate `py32f071.yml` ARM build -- and state that there
is nothing else.

STEP 3 -- LAND-07's counting rule, made reproducible. Write a short script to
`/tmp/gsd-158/land07_tokens.py` implementing jsmn's own counting rule (one token per `{`, per `[`,
per string, per primitive), modelled on the `jsmn_count` function in `158-RESEARCH.md` § Code
Examples. The script must print, each with its own label and derivation:
  (a) the **observed maximum over the real chip database** -- sweep every entry of
      `firestarter_app/firestarter/data/chip_database.json` through the host's own
      `convert_to_programmer`, add the runtime keys `eprom_operations.py` merges in, serialise, and
      count. Record the maximum and the chip family that hits it. The host package is importable
      from this environment (`import firestarter` resolves into `/workspaces/firestarter_app`); if
      the import fails, record that the bound is `[CITED: 158-RESEARCH.md F-7]` rather than
      re-derived, and say so plainly instead of implying a first-party measurement.
  (b) the **maximum over any real pin map, with every optional scalar present** -- iterate the 15
      records of `firestarter_app/firestarter/data/pinouts.json`, take each record's
      address-bus-pin count and static-high-pin count, add the optional `rw-pin` and `vpp-pin`
      where the record has them, plus all twelve top-level scalars, and count. Record the maximum
      and the pin map that hits it.
  (c) the **field-wise-maximum synthetic bound** -- each field independently at its own maximum
      across all 15 records (the largest address-bus-pin count AND a static-high entry AND
      `rw-pin` AND `vpp-pin`), a combination that exists in no real record. Record the value and
      say so.
Also print the number of `pinouts.json` records, the maximum address-bus-pin count and the maximum
static-high-pin count, so a reader can check the inputs. Record `NUMBER_JSNM_TOKENS` read from
`include/json_parser.h` and compute the headroom for each of the three bounds. Then record that the
criterion's `57 tokens` / `7 tokens of headroom` is reproducible by **none** of the three rules,
that even the loosest synthetic composition the criterion itself describes yields the (c) value,
and that the most likely origin of the gap is counting `state` alongside `cmd` -- flagged as an
unverified explanation whose failure would not disturb the three derived bounds.

STEP 4 -- LAND-07's actual argument. Record the overflow behaviour from source: `jsmn_alloc_token`
returns `NULL` at budget exhaustion, which becomes `JSMN_ERROR_NOMEM` and rejects the **whole**
command -- a fail-closed whole-command rejection, not a partial parse. Record the unknown-key skip
in `src/json_parser.c` with its line number, read now, as the mechanism by which the host can add a
wire field without a firmware release. Then state the conclusion in the form the evidence supports:
`NUMBER_JSNM_TOKENS` is **not reducible without spending the forward-compatibility budget the
unknown-key skip depends on** -- explicitly **not** "arithmetically impossible", because `64 -> 56`
would clear the real maximum and save `64 B` today (or `48 B` after LAND-05). Translate the derived
headroom into future host-added scalar keys at two tokens each, and record that `pinouts.json` is
host data that grows. Close by naming the only two paths that shrink the array -- LAND-05, and
v1.28 / Backlog 999.35 -- and propose no step toward the latter.

Hand every value to task 3. Write no tracked file. Keep `/tmp/gsd-158/land07_tokens.py` so the
record can name it and quote its output.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && ! grep -rq "check_size_baseline" .github/ && test "$(ls scripts/check_*.py | wc -l)" = "8" && grep -q "pytest tests/" .github/workflows/build.yml && grep -qE "'\*\*'" .github/workflows/build.yml && test -f /tmp/gsd-158/land07_tokens.py && python3 /tmp/gsd-158/land07_tokens.py > /tmp/gsd-158/land07.out 2>&1 && test "$(python3 -c "import json; print(len(json.load(open('/workspaces/firestarter_app/firestarter/data/pinouts.json'))))")" = "15" && grep -qE '^#define NUMBER_JSNM_TOKENS 64' include/json_parser.h && test "$(grep -cE '\b(50|51|55)\b' /tmp/gsd-158/land07.out)" -ge "3" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "check_size_baseline" .github/` produces no output and exits `1` in all three repositories, and the outputs and exit statuses are recorded.
    - `ls scripts/check_*.py` yields exactly `8` files; `check_release_assets.py` is recorded as the only one invoked by any workflow, with the invoking file and line; the other seven are named individually as local-run obligations.
    - The `pytest tests/ -v` step in `build.yml` is recorded with its line number, its absence of an `if:` gate, and `build.yml`'s `on: push: branches` filter showing `'**'` present and `beta` excluded. The sibling leg in `beta-build.yml` is recorded.
    - The CI legs on this branch are enumerated exhaustively -- two `pio test` legs, `pytest tests/ -v`, `pio run`, and `py32f071.yml` -- with an explicit statement that there is nothing else.
    - `/tmp/gsd-158/land07_tokens.py` exists, runs to completion, and prints all three bounds with their labels, the 15-record count of `pinouts.json`, the maximum address-bus-pin count, the maximum static-high-pin count, and the headroom of each bound against `NUMBER_JSNM_TOKENS` read from `include/json_parser.h`.
    - Each of the three bounds is recorded with the specific input that produces it: the chip family for the chip-database maximum, the pin-map name for the real-pin-map maximum, and an explicit statement that the field-wise-maximum composition exists in no real record.
    - If the host package import failed, the chip-database bound is marked `[CITED: 158-RESEARCH.md F-7]` rather than presented as a first-party measurement. If it succeeded, the sweep's own output is recorded.
    - The record states that `57` / `7` is reproducible by none of the three counting rules, names the loosest synthetic composition's actual value, and flags the `state`-alongside-`cmd` explanation as unverified.
    - The overflow path is recorded from source as a fail-closed whole-command rejection via `JSMN_ERROR_NOMEM`, and the unknown-key two-token skip is recorded with the line number read this session.
    - The LAND-07 conclusion is stated as a forward-compatibility-budget argument and explicitly **not** as arithmetic impossibility, with `64 -> 56`'s arithmetic availability and its byte saving both stated. Source assertion: no sentence claims the array cannot arithmetically be smaller.
    - No step toward a binary command protocol is proposed; v1.28 / Backlog 999.35 is named as the only other shrink path.
    - No file under `firestarter/` or `firestarter_app/` was modified.
  </acceptance_criteria>
  <done>LAND-04's two clauses are proven by command output with the eight-checker inventory verified by `ls`, and LAND-07's three token bounds are re-derived by a script a reader can re-run, with the criterion's `57` / `7` refuted, the conclusion resting on the forward-compatibility budget rather than on impossibility, and the fail-closed overflow path quoted from source.</done>
</task>

<task type="auto">
  <name>Task 3: Write and commit the before-figures record</name>
  <files>.planning/v1.33/158-before-figures.md</files>
  <read_first>
    - `.planning/v1.33/157-before-figures.md` -- the full frontmatter block (`title`, `phase`, `plan`, `measured`, `status`, `supersedes`, `requirements`) as the template to retarget, and the numbered `##` section layout in which every figure carries the verbatim command that produced it
    - `.planning/v1.33/157-after-figures.md` -- its `## 10` gate ledger, `## 11` one-sidedness section, corrections-ledger row shape, and `## 16` handoffs, since this record continues that series
    - `.planning/v1.33/156-before-figures.md` -- the LAND-04 confirmation it already carries, so this record extends rather than contradicts it
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-PATTERNS.md` -- §10, the frontmatter shape and the required sections for this phase's records
    - `.planning/ROADMAP.md` §Phase 158 and `.planning/REQUIREMENTS.md` §5 -- the exact stale sentences this record supersedes, so it can quote what it replaces without editing either document
    - `.planning/v1.33/CITATIONS-STALE.md` -- read only, to confirm what Phase 159 owns and to avoid duplicating or resolving it
  </read_first>
  <action>
Create `.planning/v1.33/158-before-figures.md` with the `Write` tool. It is net-new, so `Write` is
correct here; no other file in this plan may be written.

Frontmatter, following `157-before-figures.md` exactly and retargeted: `title` naming the
before-figures record for milestone v1.33 Phase 158 (firmware-only); `phase`
(`158-residual-optimizations-cold-baseline-re-record-firmware-only`); `plan` (`"01"`); `measured`
(today's date); `status: AUTHORITATIVE` -- state that this file is the only source for Phase 158's
before-half figures, that every AVR figure in it is COLD, and that the figures it captures are
invalidated by this phase's own plan 04 (which re-records `size_baseline.json`), so no later plan
re-derives them from this position; `supersedes` -- name ROADMAP §Phase 158 criteria 3, 5, 6 and 7
and REQUIREMENTS LAND-03, LAND-05, LAND-06 and LAND-07 prose, wherever they state a figure this
file corrects, listing C-1 through C-13, and state that neither document is edited by this phase
(OD-9); `requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05, LAND-06, LAND-07, LAND-08]`.

Sections, numbered `##`, each carrying the verbatim command that produced every number in it:

1. **Git anchors** -- `FW_PRE_SHA` full and abbreviated with its subject, the branch name, the meta
   HEAD, `git status --porcelain` asserted empty before and after, and the gitlink drift recorded as
   pre-existing and not re-pinned (ceiling 12, OD-10).
2. **The pre-phase cold ledger** -- the three AVR flash and RAM pairs with their `warning:` counts,
   every figure labelled COLD and each carrying its `rm -rf` plus single-`pio run` command; the
   recorded `size_baseline.json` values alongside for contrast, with the staleness attributed to
   Phases 155-157; and Leonardo's Caterina headroom against `28672`.
3. **The native position and LAND-08's evidence** -- this plan's four timed runs as (env, cases,
   succeeded, suites, duration) rows; the prior recorded data points from the milestone's own
   records; and the prohibitions in plain words: never attribute a suite failure to the tree on
   N=1, never quote a wall time as evidence, never treat a single case-count mismatch as a
   regression without a re-run. State the honest form: duration is a necessary-but-not-sufficient
   correlate of the observed failures, not a predictor.
4. **The gate ledger at this position, all legs** -- each with command, exit status and salient
   output; each marked as running in a CI workflow or not; the default-mode RED recorded with every
   failing line verbatim and a statement that plan 04 must flip it GREEN; and the `--rebuild` form's
   exactly-two-native-failure-lines shape recorded as LAND-03's pre-fix state.
5. **The one-sidedness, quoted from source and from the gate** -- `check_size_baseline.py` lines
   `697` and `709` verbatim as the mechanism, and the canonical `--avr-log` invocation's verbatim
   `PASS:` line with its negative deltas against positive allowances as the observed consequence.
   State that no exemption is authored (D-03) and list the six MERGE-05 literals with their line
   numbers as the values that must be byte-unchanged at phase end.
6. **The four legs that redden, and the four that do not** -- the reddening four with their line
   numbers as read this session, the fixture family each reads, and each one's remedy; the surviving
   policy plants and `test_base01_is_not_re_anchored_by_the_new_exemption` named as legs that read
   BASE-01 or their own baseline copy and therefore do not move; and the four porcelain legs
   recorded separately with their distinct cause and remedy (commit first).
7. **The severance plan, and why it is 4 plus 2 rather than 13** -- the `*_v158*` membership, the
   in-place disposition of the two native summary fixtures with its licence quoted from the leg's
   own docstring, the retire-in-place-and-KEEP disposition of `*_v153*`, and an explicit statement
   that Groups 2 and 3 are **not needed** because no exemption is authored (C-11, OD-8).
8. **LAND-04, both clauses** -- the greps with their exit statuses, the one-of-eight checker
   inventory verified by `ls`, the seven local-run obligations named individually, the
   `pytest tests/ -v` step with its line number and its trigger, the exhaustive CI-leg list, and the
   consequence sentence.
9. **LAND-07, the token arithmetic** -- the three bounds with their derivations and their headroom,
   the inputs (record count, maximum address-bus-pin count, maximum static-high-pin count), the
   script path and its verbatim output, the refutation of `57` / `7`, the fail-closed overflow path,
   the unknown-key skip with its line number, and the conclusion stated as a budget argument.
10. **The coverage ceilings** -- all twelve, verbatim from this plan's header.
11. **The decisions** -- OD-1 through OD-10, each with what was decided and what was declined.
12. **The corrections index** -- C-1 through C-13 as a table, one row each, exactly as this plan's
    header states them, each with the source document's claim and what replaces it. Rows whose
    replacement value is only measurable later (C-2, C-3, C-13) are marked as opened here and
    closed in `158-after-figures.md`.
13. **Handoffs** -- to plan 02: the pre-edit native baseline and the D-04 re-run rule. To plan 04:
    the cold recipe, the default-mode RED shape it must flip, the four legs and their remedies, and
    the `*_v158*` membership. To plan 05: BASE-01's four `141` integers, the two false CI-coverage
    paragraphs, and the current `FLOOR` / `FIXTURE_FLOOR` values with the shipped counts. To plan
    07: the three ROADMAP and REQUIREMENTS figures it must scope-correct in place, named with the
    correction id that replaces each. To Phase 159: every `file:LINE` citation in this record was
    measured against the current tree and will be remapped once over the composite diff.
14. **Self-verification of this record** -- how each figure can be re-derived, and with which
    command.

Use ` -- ` rather than an em dash. Write no planning-provenance token into any firmware source
file; this task writes into `.planning/` only.

Then commit in the **meta** repo, staging exactly `.planning/v1.33/158-before-figures.md`. Subject:
`docs(158-01): record the pre-phase position, LAND-04/07/08 and the four red legs`. The body must
state that every AVR figure is COLD, that LAND-04's statement has two clauses, that LAND-07's `57` /
`7` is refuted with three derived bounds, and that the default-mode RED shape is recorded so plan
04's GREEN is falsifiable. Do not push. Do not touch `.planning/STATE.md`, `.planning/ROADMAP.md`
or `.planning/REQUIREMENTS.md` in this task.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/158-before-figures.md && head -1 .planning/v1.33/158-before-figures.md | grep -q '^---$' && for k in "title:" "phase: 158-residual-optimizations" "plan: \"01\"" "measured:" "status: AUTHORITATIVE" "supersedes:" "requirements: \[LAND-01"; do grep -q "$k" .planning/v1.33/158-before-figures.md || { echo "MISSING FRONTMATTER $k"; exit 1; }; done && test "$(grep -cE '^\| C-([1-9]|1[0-3]) ' .planning/v1.33/158-before-figures.md)" = "13" && test "$(grep -cE '^## ' .planning/v1.33/158-before-figures.md)" -ge "14" && for t in COLD "697" "709" "PASS:" "28672" "build.yml" "OD-10" "JSMN_ERROR_NOMEM" "NUMBER_JSNM_TOKENS" "test_clean_avr_all_three_envs_pass" "test_clean_native_both_envs_pass" "test_planted_flash_regression_flips_checker_to_failure" "test_default_mode_is_unchanged_by_the_new_flag" "captured_build_v158" "check_release_assets.py"; do grep -q "$t" .planning/v1.33/158-before-figures.md || { echo "MISSING $t"; exit 1; }; done && test "$(grep -cE '^- \*\*OD-([1-9]|10)' .planning/v1.33/158-before-figures.md)" = "10" && test "$(git log -1 --format=%s -- .planning/v1.33/158-before-figures.md | grep -c '^docs(158-01):')" = "1" && test "$(git show --stat --name-only --format= HEAD | grep -c .)" = "1" && test -z "$(git -C firestarter status --porcelain)" && git diff --quiet HEAD -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/v1.33/CITATIONS-STALE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/158-before-figures.md` exists with all seven frontmatter keys, `plan: "01"`, `status: AUTHORITATIVE`, `requirements` listing all eight LAND ids, and a `supersedes` block naming ROADMAP §Phase 158 criteria 3, 5, 6 and 7 plus REQUIREMENTS LAND-03/05/06/07, and stating that neither document is edited.
    - The file has at least the fourteen numbered `##` sections listed in the action, and every figure in it is accompanied by the verbatim command that produced it.
    - Every AVR flash and RAM figure is labelled COLD, and the stale `size_baseline.json` values appear alongside as contrast with their staleness attributed to Phases 155-157.
    - Section 3 carries this plan's four timed runs as (env, cases, succeeded, suites, duration) rows plus the prior recorded data points, and states the three prohibitions in plain words including that duration is a necessary-but-not-sufficient correlate rather than a predictor.
    - Section 4 records every gate leg with command, exit status and salient output; each is marked as CI-invoked or not; the default-mode failing lines appear verbatim with an explicit statement that plan 04 must flip the leg GREEN; and the `--rebuild` form's two native failure lines are recorded with no AVR line present.
    - Section 5 quotes `check_size_baseline.py:697` and `:709` verbatim, quotes the canonical `--avr-log` `PASS:` line verbatim with three negative deltas, states that no exemption is authored, and lists the six MERGE-05 literals with their line numbers.
    - Section 6 names all four reddening legs with line numbers read this session, the fixture family each reads and its remedy; names the legs that do not move with the reason; and records the four porcelain legs separately. Source assertion: the file contains no sentence that treats the two sets of four as one set.
    - Section 7 states the severance is 4 new files plus 2 updated in place, names all six, states the in-place licence for the native summary pair, states the retire-in-place-and-KEEP disposition of `*_v153*`, and states explicitly that Groups 2 and 3 are not needed and why.
    - Section 8 carries both LAND-04 clauses with their commands and exit statuses, the `8` checker count verified by `ls`, the seven local-run obligations named individually, and the exhaustive CI-leg list.
    - Section 9 carries all three token bounds with their derivations, headroom, inputs, the script path and its verbatim output, the refutation of `57` / `7`, and the budget-not-impossibility conclusion.
    - Sections 10, 11 and 12 carry all twelve ceilings, all ten OD decisions as bullets matching `- **OD-N`, and exactly thirteen correction rows `C-1` through `C-13`, with C-2, C-3 and C-13 marked as opened here and closed later.
    - `git -C /workspaces log -1 -- .planning/v1.33/158-before-figures.md` has a subject beginning `docs(158-01):` and `git show --stat HEAD` lists exactly one path.
    - `git -C firestarter status --porcelain` is empty, and `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and `.planning/v1.33/CITATIONS-STALE.md` are byte-unchanged against HEAD.
  </acceptance_criteria>
  <done>The before-figures record exists and is committed as a single-path commit; every figure carries its producing command and is labelled COLD where applicable; LAND-04's two clauses, LAND-07's three derived bounds and LAND-08's timed evidence are all discharged inside it; the default-mode RED shape, the four reddening legs and the severance membership are handed forward with numbers; and all twelve ceilings, ten decisions and thirteen corrections are stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> recorded figure | A figure that is computed rather than transcribed from a captured log is unfalsifiable; the record is the only place these numbers will exist |
| local gate -> CI gate | Four of the gates measured here run in no workflow while the checker's own pytest does; conflating the two is how a red CI run gets shipped |
| `.planning/` record -> later plans and Phase 159 | The default-mode RED shape, the four legs, the severance membership and the citation staleness are all handed forward; anything omitted becomes something a later plan must rediscover |
| host data files -> firmware token budget | LAND-07's bounds are derived from `firestarter_app` data read-only; a derivation that mutates or assumes host data would break the firmware-only fence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-01 | Repudiation | a recorded figure produced by arithmetic on another record rather than transcribed from a captured `Flash:` / `RAM:` line | high | mitigate | Every AVR figure comes from a tee'd cold log kept at `/tmp/gsd-158/`; the record names the command per figure; `--rebuild` is prohibited as a transcription source |
| T-158-02 | Repudiation | LAND-04 recorded as one clause, implying the checker is untouched by CI | high | mitigate | Both clauses are separately proven by command output and both are required by the acceptance criteria; the exhaustive CI-leg list is recorded |
| T-158-03 | Repudiation | LAND-07 recorded on the criterion's unreproducible `57` / `7`, or as arithmetic impossibility | high | mitigate | Three bounds re-derived by a kept script with its inputs printed; the conclusion is required to be a budget argument, with `64 -> 56`'s availability stated |
| T-158-04 | Repudiation | a load-flaky native failure recorded as a tree property (D-04) | medium | mitigate | Four timed runs recorded; the re-run-before-concluding rule and the no-wall-time-as-evidence prohibition are written into the record itself |
| T-158-05 | Tampering | a `/tmp` worktree measurement of `pytest tests/` reported as the suite, silently 32 legs short | medium | mitigate | The run happens from `/workspaces/firestarter` and the absence of the word `skipped` is asserted mechanically |
| T-158-06 | Tampering | a whole-file write or an automated mutation verb touching `ROADMAP.md`, `REQUIREMENTS.md` or `CITATIONS-STALE.md` | high | mitigate | All three prohibited; `git diff --quiet HEAD` over the three paths is a verify leg; the commit is asserted to list exactly one path |
| T-158-07 | Repudiation | claiming ARM or bench coverage that was not built | high | mitigate | Ceilings 3 and 4 are restated in the record; no bench criterion is authored anywhere in this phase |
| T-158-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages. RESEARCH's Package Legitimacy Audit records zero packages for the whole phase, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- `git -C firestarter status --porcelain` empty before and after; HEAD unchanged; no firmware commit.
- Three cold `pio run` logs exist with a `Flash:` and a `RAM:` line each and a `warning:` count of 0.
- `pio test -e native` recorded three times plus `native_nodevtools` once, each with its own duration field.
- `python3 -m pytest tests/ -q -o addopts=""` from `/workspaces/firestarter`: exit 0, count line recorded, the word `skipped` absent.
- Canonical MERGE-05 `--avr-log` form: exit 0 with three negative flash deltas in the `PASS:` line.
- Canonical MERGE-05 `--rebuild` form: exit 1 with exactly two `cases baseline=141 observed=` lines and no AVR line.
- Default mode: RED, every failing line captured verbatim.
- `grep -rn "check_size_baseline" .github/` empty in all three repos; `ls scripts/check_*.py` yields 8.
- `/tmp/gsd-158/land07_tokens.py` runs and prints all three bounds with their inputs.
- `.planning/v1.33/158-before-figures.md` exists with 13 correction rows, 10 OD bullets and at least 14 numbered sections; committed as a one-path commit.
- `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` and `.planning/v1.33/CITATIONS-STALE.md` byte-unchanged.
</verification>

<success_criteria>
The pre-phase position of every figure Phase 158 moves is captured COLD, with the producing command
recorded per figure and nothing transcribed from `--rebuild`; LAND-04 is discharged with both
clauses proven by command output; LAND-07 is discharged with three re-derived bounds, its inputs,
its script, the refutation of the criterion's own numbers, and a conclusion that rests on the
forward-compatibility budget rather than on arithmetic impossibility; LAND-08 is discharged with
four new timed same-tree runs plus the prior corpus and the three prohibitions stated in the
record; the default-mode gate is recorded RED with its failing lines verbatim so plan 04's GREEN is
falsifiable; the four reddening legs are enumerated by reading current source and kept distinct from
the four porcelain legs; and no file under `firestarter/`, no `ROADMAP.md`, no `REQUIREMENTS.md` and
no `CITATIONS-STALE.md` is touched.
</success_criteria>

<output>
Create `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-01-SUMMARY.md` when done.
</output>
