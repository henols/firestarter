---
phase: 158-residual-optimizations-cold-baseline-re-record-firmware-only
plan: 04
type: execute
wave: 4
depends_on: ["158-03"]
files_modified:
  - firestarter/scripts/baseline/size_baseline.json
  - firestarter/tests/fixtures/captured_build_v158_uno.log
  - firestarter/tests/fixtures/captured_build_v158_uno328pb.log
  - firestarter/tests/fixtures/captured_build_v158_leonardo.log
  - firestarter/tests/fixtures/planted_size_baseline_flash_regression_v158.log
  - firestarter/tests/fixtures/captured_test_native_summary.log
  - firestarter/tests/fixtures/captured_test_native_nodevtools_summary.log
  - firestarter/tests/test_check_size_baseline.py
autonomous: true
requirements: [LAND-01, LAND-02]

must_haves:
  truths:
    - "`scripts/baseline/size_baseline.json`'s twelve `avr_targets` figures and four `native_envs` figures for the two pinned envs are re-recorded from COLD builds produced by `rm -rf .pio/build/<env>` plus exactly one `pio run -e <env>` per env, transcribed from the captured logs and never computed."
    - "The re-record and the fixture severance are in ONE commit, so no commit on this branch is CI-red at `build.yml:161`."
    - "`check_size_baseline.py` in DEFAULT mode goes from RED to GREEN at this commit -- the polarity flip is LAND-01's own discharge evidence, and the RED shape it flips was written down in plan 01 before the flip."
    - "The canonical MERGE-05 policy invocation exits 0 and prints three NEGATIVE flash deltas against positive allowances, and that verbatim PASS line is captured as LAND-02's one-sidedness evidence."
    - "No MERGE-05 exemption constant is authored, added or widened; all six literals are byte-unchanged."
    - "BASE-01's growth axis is untouched: `flash_used`, `ram_used` and `flash_total` are byte-unchanged in `size_baseline_base01.json` by this plan."
    - "The severance is 4 new fixtures plus 2 updated in place, and the three surviving `planted_size_baseline_policy_*_v153.log` legs are asserted green rather than re-planted."
    - "The two native summary fixtures are genuine captures of real runs at this tree position, never hand-edited counts."
  artifacts:
    - firestarter/tests/fixtures/captured_build_v158_uno.log
    - firestarter/tests/fixtures/captured_build_v158_uno328pb.log
    - firestarter/tests/fixtures/captured_build_v158_leonardo.log
    - firestarter/tests/fixtures/planted_size_baseline_flash_regression_v158.log
  key_links:
    - "The three legs that read `captured_build_v153_*` compare a FIXTURE against the LIVE baseline in byte-identity mode, which is why moving the baseline without repointing them turns CI red -- the fixture is the observed side, so the failure reads exactly like a real regression."
    - "The two native summary fixtures are read by exactly one leg, which is the licence for updating them in place rather than severing them."
    - "The new plant must be derived from the new leonardo capture, or the checker fails for TWO reasons instead of the one the leg names -- the false-cause pattern the severance discipline exists to prevent."
  prohibitions:
    - "Must NOT split the re-record and the severance across commits. One commit, or `build.yml:161` `pytest tests/ -v` is red at the intermediate commit on this branch (S-4, Pitfall 1)."
    - "Must NOT transcribe any figure from `check_size_baseline.py --rebuild`; its rebuild path uses `pio run -t clean -e <env>`, not the mandated `rm -rf .pio/build/<env>` (Pitfall 6, S-2)."
    - "Must NOT compute any recorded figure by arithmetic on another record. Every number is transcribed from a `Flash:`, `RAM:` or `test cases:` line in a committed capture."
    - "Must NOT edit `scripts/baseline/size_baseline_base01.json`. Plan 05 owns it; the growth axis is never re-anchored (LAND-01)."
    - "Must NOT author, add, widen or renumber any MERGE-05 exemption constant. A reduction passes with none (D-03)."
    - "Must NOT modify, repoint or delete any `*_fullflash*`, `*_v132*`, `*_v151*` or `*_v153*` fixture. They are retired in place and KEPT (S-1, OD-8)."
    - "Must NOT author the Group 2 anchor trio or a Group 3 exemption-admission trio. Neither is needed this generation, and the record must say so explicitly (C-11)."
    - "Must NOT change any assertion's MEANING in the three severed legs -- only the fixture names, the two flash figure literals in the planted-regression leg, and the case-count literal in the native leg."
    - "Must NOT touch the `warnings` block, `flash_total`, `ram_total`, `suites`, or the `native_pinmap_provisional` entry in `size_baseline.json`."
    - "Must NOT add the three `*_v131` native envs to `size_baseline.json`; the recorded three-env set is deliberate per that file's own note."
    - "Must NOT verify fixture presence with `git add`'s exit code. Use `git ls-files` -- git stages nothing at exit 0 for some paths."
    - "Must NOT run `pytest tests/` for gate purposes on an uncommitted tree (S-5) or from a `/tmp` worktree (S-6)."
    - "Must NOT edit `lib/jsmn/src/jsmn.h`, `src/proms/flash_5v_page.cpp`, `tests/test_checker_convention.py`, `tests/meta_presence.py`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` or `.planning/v1.33/CITATIONS-STALE.md`."
    - "Must NOT attribute a native case-count anomaly to this plan on N=1 (D-04); re-run first, and record every run."
---

<objective>
Re-record `scripts/baseline/size_baseline.json` from cold builds of the final tree and sever the
fixture corpus onto a new `*_v158*` family -- **in one commit**.

Purpose: this is the phase's central structural constraint and the one no success criterion states.
Three legs of `tests/test_check_size_baseline.py` compare a committed fixture against the **live**
baseline in byte-identity mode, and a fourth reads the two native summary fixtures against
`native_envs`. Moving the baseline without moving them reddens exactly four legs -- observed, not
predicted -- and `pytest tests/ -v` runs at `build.yml:161` on `push: branches: ['**','!beta']`,
which fires on this milestone branch. So a split commit is not a local inconvenience; it is a red CI
run at that commit. The same three cold logs serve twice: they are LAND-01's transcription source
and the severance's Group 1 fixtures. Capture once.

Output: one firmware commit carrying the re-recorded baseline, three new captures, one new plant,
two in-place native summary re-captures and the three severed legs; plus the LAND-02 evidence -- a
default-mode gate that has flipped RED to GREEN and a verbatim one-sided PASS line.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-RESEARCH.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-PATTERNS.md
@.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-VALIDATION.md
@.planning/v1.33/158-before-figures.md
</context>

## The honest coverage ceilings -- stated, not implied

All twelve appear in every plan of this phase, in every SUMMARY, and in both phase records.

1. **`check_size_baseline.py`, `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` are invoked by NO CI workflow** -- every size gate this phase leans on is a **local-run obligation**. A green CI run is not evidence any of them passed.
2. **But the checker IS executed in CI by its own paired pytest** -- `build.yml:161` `pytest tests/ -v`, ungated, on `push: branches: ['**','!beta']`. **This is the ceiling that makes this plan's one-commit rule mandatory.**
3. **The ARM half of LAND-05 is unverified locally unless plan 02's toolchain install succeeded.** Never claim ARM coverage that was not built.
4. **LAND-06's runtime half is unmeasurable in this milestone** (D-02); its decline rests on a size measurement plus a coverage gap.
5. **The algorithm-5 page-boundary path has ZERO behavioural native coverage.**
6. **`sizeof(jsmntok_t)` cannot be asserted in a native test** -- AVR gives 6 B, the host gives 12 B.
7. **The native suite is load-flaky (D-04).** No failure may be attributed to a tree change on N=1, and wall time carries no signal on its own.
8. **A `/tmp` worktree run of `pytest tests/` silently skips 32 cross-repo legs.** Gate-purpose runs happen from `/workspaces/firestarter`.
9. **`lib/jsmn/src/jsmn.h` carries a dead duplicate implementation** that compiles in no translation unit but is live text to a naive grep.
10. **LAND-07's conclusion is a budget argument, not an impossibility argument.**
11. **Every `file:LINE` citation this phase writes will be newly stale.** Phase 159 remaps them exactly once (D-01, D-05).
12. **The `firestarter` gitlink in the meta repo is drifted** -- pre-existing since Phase 154, operator-gated, not re-pinned by this phase.

## The decisions this plan executes

- **OD-8 -- the `*_v153*` family is retired in place and KEPT**, never deleted or repointed. This generation authors **4 new files plus 2 updated in place**, not the thirteen every prior generation used, because **no MERGE-05 exemption is authored for a reduction** -- so the allowance is unchanged and all three policy plants remain valid. **Groups 2 and 3 are deliberately not needed**, and the record must say so explicitly: every prior generation's docstring documents "the same four groups", and a reader will otherwise think two were forgotten (C-11).
- **The one-commit rule (S-4)** is not a preference. It is derived from ceiling 2.
- **`size_baseline.json`'s two stale prose fields are repaired in the same commit** -- `meta.consumed_by` names two consumers where there are three, and `envs_agree_note` quotes a stale case count already flagged by a sibling `meta` key (C-7, C-8). Both are on this plan's own subject.

## Artifacts this phase produces

**CREATED by this plan:** `firestarter/tests/fixtures/captured_build_v158_uno.log`,
`captured_build_v158_uno328pb.log`, `captured_build_v158_leonardo.log`, and
`planted_size_baseline_flash_regression_v158.log`.

**CHANGED by this plan:** `firestarter/scripts/baseline/size_baseline.json` -- the twelve
`avr_targets` figures, the four `native_envs` figures for `native` and `native_nodevtools`, a **new**
`meta.cold_rerecord_phase158` note key, plus the `meta.consumed_by` and `envs_agree_note` prose
repairs. `firestarter/tests/fixtures/captured_test_native_summary.log` and
`captured_test_native_nodevtools_summary.log` -- re-captured in place.
`firestarter/tests/test_check_size_baseline.py` -- the fixture tuples in
`test_clean_avr_all_three_envs_pass` and `test_default_mode_is_unchanged_by_the_new_flag`, the
fixture name and two flash figure literals in `test_planted_flash_regression_flips_checker_to_failure`,
the case-count literal in `test_clean_native_both_envs_pass`, and each of those four legs' docstring
severance record.

**CREATED by other plans of this phase:** `.planning/v1.33/158-before-figures.md` (plan 01);
`firestarter/tests/test_jsmn_token_layout_source_contract_v158.py` with its five `test_` functions,
its helpers `_strip_comments`, `_assert_identifier_absent`, `_live_header_region`,
`_LIVE_STRUCT_END_MARKER`, `_SCAN_JSMN`, `_JSMN_HEADER_REL`, and its environment seam
`FIRESTARTER_JSMN_TOKEN_LAYOUT_SCAN_SOURCE` (plan 02); `.planning/v1.33/158-after-figures.md`
(plan 06). **CHANGED by other plans:** `firestarter/lib/jsmn/src/jsmn.h` (plan 02);
`firestarter/scripts/baseline/size_baseline_base01.json` plus its new
`meta.native_inventory_axis_phase158` key, `firestarter/tests/test_checker_convention.py`'s `FLOOR`
and `FIXTURE_FLOOR`, and the CI-coverage paragraphs of `tests/test_check_size_baseline.py` and
`tests/meta_presence.py` (plan 05).

**NOT created and NOT edited by any plan:** `src/proms/flash_5v_page.cpp`, anything under
`test/native/avr/test_val_5v_page/`, `lib/jsmn/src/jsmn.c`, `include/json_parser.h`, any
`firestarter_app/` file, any `*_fullflash*` / `*_v132*` / `*_v151*` / `*_v153*` fixture,
`.planning/v1.33/CITATIONS-STALE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`.

<tasks>

<task type="auto">
  <name>Task 1: Capture the final position once -- three cold AVR logs and two native summary captures</name>
  <files>
    (no tracked file is edited -- this task captures. Logs go to `/tmp/gsd-158/final/`.)
  </files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` -- `meta.generated_by`, `meta.note` and `meta.warm_vs_cold_correction`, which are the recipe this task must follow verbatim; plus the `avr_targets` and `native_envs` blocks as they stand
    - `firestarter/tests/fixtures/captured_build_v153_uno.log` and `captured_build_v153_leonardo.log` -- the normative fixture format: verbatim `pio run` output, committed unedited, including its line count and its trailing environment-status block
    - `firestarter/tests/fixtures/captured_test_native_summary.log` and `captured_test_native_nodevtools_summary.log` -- the exact window shape to reproduce: the run's SUMMARY banner line, the two-line table header, one row per suite, and the trailing total line
    - `firestarter/tests/fixtures/README.md` -- the three-prefix naming discipline, the rule that a `captured_*` truncation window must be a stated fact in the producing plan's SUMMARY, and the `git ls-files` presence rule
    - `.planning/v1.33/158-before-figures.md` -- §2 and §3, the pre-phase cold and native positions this capture is compared against
    - the SUMMARY of plan 158-02 -- its post-narrowing cold figures, which this capture must either reproduce or explain
  </read_first>
  <action>
Work in `/workspaces/firestarter`. Assert `git status --porcelain` is empty and record HEAD as
`FW_FINAL_SHA` with its subject -- it must be plan 02's `test(158-02)` commit, since plan 03 created
none. Create `/tmp/gsd-158/final/`.

STEP 1 -- the three cold AVR captures. For each of `uno`, `uno328pb`, `leonardo`, in that order:
`rm -rf .pio/build/<env>`, then **exactly one** `pio run -e <env>`, teeing the complete output --
first line to last, no trimming, no reflow, no ANSI stripping -- to
`/tmp/gsd-158/final/captured_build_v158_<env>.log`. Do not batch envs into one invocation: the
fixture format is single-env `pio run` output and a batched run produces a different trailing block.
Record, per env, the `Flash: ... used N bytes` figure, the `RAM: ... used N bytes` figure, the
`warning:` count (must be 0), and the log's line count. Compare each log's shape against the v153
analog -- same banner, same `Checking size` line, same `RAM:`/`Flash:` pair, same trailing
environment-status block -- and record any structural difference rather than editing it away.

STEP 2 -- the two native captures. Run `pio test -e native` and `pio test -e native_nodevtools`,
teeing each full run to `/tmp/gsd-158/final/full-native.log` and
`/tmp/gsd-158/final/full-native_nodevtools.log`. From each, extract the contiguous window that the
existing fixture pair uses: from the run's SUMMARY banner line through the trailing
`N test cases: N succeeded ...` line inclusive, byte-for-byte, no other edit. Write those windows to
`/tmp/gsd-158/final/captured_test_native_summary.log` and
`/tmp/gsd-158/final/captured_test_native_nodevtools_summary.log`. Record the case count, the
succeeded count, the suite count and the line count for each, and record the truncation window as an
explicit stated fact -- the fixture README requires a truncated `captured_*` file's window to be
stated in the producing plan's SUMMARY, never left as an invisible edit.

STEP 3 -- the D-04 protocol. If either native run's case count differs from plan 02's, **re-run
that env before concluding anything**. A count that returns to plan 02's value is a flake and the
record says so; a stable difference must be explained by naming what changed the inventory --
recalling that plan 02 added a `tests/` module, which does **not** move the native count, and plan
03 added no case at all. Whatever the final count is, **it is the number that gets recorded**:
Pitfall 9 forbids reusing a figure from the research document. Record every run.

STEP 4 -- the plant's derivation inputs. Read
`tests/fixtures/planted_size_baseline_flash_regression_v153.log` and diff it against
`tests/fixtures/captured_build_v153_leonardo.log` to establish the derivation rule by observation:
the two differ on **exactly one line**, the `Flash:` line, whose used figure is the capture's plus
512 and whose percentage and progress-bar rendering were re-derived for the new figure. Record the
observed diff verbatim as the rule this plan will replicate, together with the target used figure
for the new plant -- the new leonardo capture's used figure plus 512.

STEP 5 -- hand off the transcription table. Assemble, as a table, every value task 2 must write into
`size_baseline.json`: for each AVR target the four fields (used and free for both flash and RAM),
with `free` derived as the untouched total minus the transcribed used figure and the totals named as
untouched; and for `native` and `native_nodevtools` the case and succeeded counts. Mark
`flash_total`, `ram_total`, `suites`, `native_pinmap_provisional` and the entire `warnings` block as
UNTOUCHED. Write no tracked file in this task, and create no commit.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && mkdir -p /tmp/gsd-158/final && for e in uno uno328pb leonardo; do rm -rf .pio/build/$e; pio run -e $e > /tmp/gsd-158/final/captured_build_v158_$e.log 2>&1 || exit 1; grep -qE 'Flash:.*used [0-9]+ bytes' /tmp/gsd-158/final/captured_build_v158_$e.log || exit 1; grep -qE 'RAM:.*used [0-9]+ bytes' /tmp/gsd-158/final/captured_build_v158_$e.log || exit 1; test "$(grep -c 'warning:' /tmp/gsd-158/final/captured_build_v158_$e.log)" = "0" || exit 1; test "$(grep -c 'SUCCESS' /tmp/gsd-158/final/captured_build_v158_$e.log)" -ge "1" || exit 1; done && pio test -e native > /tmp/gsd-158/final/full-native.log 2>&1 && pio test -e native_nodevtools > /tmp/gsd-158/final/full-native_nodevtools.log 2>&1 && for f in native native_nodevtools; do awk '/^=+ SUMMARY =+$/,0' /tmp/gsd-158/final/full-$f.log | grep -qE '[0-9]+ test cases: [0-9]+ succeeded' || exit 1; done && python3 -c "
import re,sys
ok=True
for f in ['native','native_nodevtools']:
    t=open('/tmp/gsd-158/final/full-%s.log'%f).read()
    m=re.search('([0-9]+) test cases: ([0-9]+) succeeded',t)
    if not m or m.group(1)!=m.group(2): ok=False; print('MISMATCH',f)
sys.exit(0 if ok else 1)" && diff tests/fixtures/captured_build_v153_leonardo.log tests/fixtures/planted_size_baseline_flash_regression_v153.log | grep -c '^[<>]' | grep -q '^2$' && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - `FW_FINAL_SHA` is recorded with a subject beginning `test(158-02):`, confirming plan 03 created no commit, and `git status --porcelain` is empty before and after.
    - Three logs exist at `/tmp/gsd-158/final/captured_build_v158_{uno,uno328pb,leonardo}.log`, each produced by `rm -rf .pio/build/<env>` followed by exactly one single-env `pio run -e <env>`, each containing a `Flash:` used line, a `RAM:` used line, a success marker and a trailing environment-status block, and each with a `warning:` count of `0`.
    - Each capture's line count and structural comparison against the v153 analog are recorded, with any structural difference reported rather than edited away.
    - Two window files exist at `/tmp/gsd-158/final/captured_test_native{,_nodevtools}_summary.log`, each spanning the SUMMARY banner line through the trailing `N test cases: N succeeded` line inclusive and byte-identical to that region of its full run log. The window is recorded in the SUMMARY as an explicit stated truncation.
    - Case count equals succeeded count on both native envs; the suite count and line count are recorded for each; and any count anomaly has its re-run recorded with the flake named per D-04.
    - The final native case count recorded is the one measured this session -- the SUMMARY states explicitly that no figure was reused from the research document (Pitfall 9).
    - The v153 plant's derivation rule is established by an observed diff against its own capture: exactly one differing line, the `Flash:` line, at capture-plus-512 with the percentage and progress bar re-rendered. The diff is recorded verbatim and the new plant's target used figure is stated.
    - A transcription table is handed to task 2 covering all sixteen moving fields, with `free` values derived from the untouched totals and with `flash_total`, `ram_total`, `suites`, `native_pinmap_provisional` and the whole `warnings` block explicitly marked UNTOUCHED.
    - No tracked file was created or modified and no commit was created by this task.
  </acceptance_criteria>
  <done>The final tree position is captured once: three single-env cold `pio run` logs with zero warnings and verified structure, and two native summary windows extracted byte-for-byte from real runs with their truncation stated; the native counts are re-measured rather than reused; the plant's derivation rule is established by observing the prior generation's own diff; and a complete sixteen-field transcription table is handed forward with the untouched fields named.</done>
</task>

<task type="auto">
  <name>Task 2: One commit -- re-record the baseline, sever the fixtures onto v158, repoint the four legs</name>
  <files>firestarter/scripts/baseline/size_baseline.json, firestarter/tests/fixtures/captured_build_v158_uno.log, firestarter/tests/fixtures/captured_build_v158_uno328pb.log, firestarter/tests/fixtures/captured_build_v158_leonardo.log, firestarter/tests/fixtures/planted_size_baseline_flash_regression_v158.log, firestarter/tests/fixtures/captured_test_native_summary.log, firestarter/tests/fixtures/captured_test_native_nodevtools_summary.log, firestarter/tests/test_check_size_baseline.py</files>
  <read_first>
    - `firestarter/scripts/baseline/size_baseline.json` -- the whole file, so the `meta` key order is preserved and the new note key is appended in the established position; specifically `meta.consumed_by`, `meta.note`, `meta.native_case_count_revision_260822`, `meta.flash_ceiling_move_260820_a7w` (the two precedents for a dated note key), `envs_agree`, `envs_agree_note`, and the whole `warnings` block
    - `firestarter/tests/test_check_size_baseline.py` -- the module docstring's fixture-generation disposition table; `_run_checker`; and the full body plus docstring of `test_clean_avr_all_three_envs_pass`, `test_clean_native_both_envs_pass`, `test_planted_flash_regression_flips_checker_to_failure` and `test_default_mode_is_unchanged_by_the_new_flag`, including each one's fixture tuple, its literal figure assertions, and how the previous generation recorded its own severance in the docstring
    - `firestarter/tests/test_check_size_baseline.py` -- also `test_base01_is_not_re_anchored_by_the_new_exemption`, `test_policy_merge05_fires_on_uno_class_over_band`, `test_policy_merge05_fires_on_leonardo_growth`, `test_policy_merge05_fires_on_ram_move`, `test_policy_merge05_permits_the_measured_landing_deltas` and every arm of `test_policy_merge05_admits_the_documented_defect_fix`, to confirm by reading which legs read BASE-01 or their own baseline copy and therefore must NOT be touched
    - `firestarter/scripts/check_size_baseline.py` -- `compare_native`'s strict equality, `compare_avr_policy_merge05`, the six MERGE-05 literals with their line numbers, and default mode's byte-identity comparison
    - `firestarter/tests/fixtures/README.md` -- the `captured_*` / `planted_*` / `clean_*` discipline and the `git ls-files` presence rule
    - `firestarter/tests/fixtures/planted_size_baseline_flash_regression_v153.log` and `captured_build_v153_leonardo.log` -- the plant and its capture, as the byte-level template
    - `.planning/v1.33/158-before-figures.md` -- §4 (the default-mode RED shape this commit must flip), §6 (the four legs and their remedies) and §7 (the severance membership)
    - the SUMMARY of plan 158-04 task 1 -- the transcription table and the plant's target figure
  </read_first>
  <action>
Everything below lands in **ONE** firmware commit. Stage nothing until every edit is done and the
suite is green locally; then commit once.

STEP 1 -- install the four new fixtures. Copy the three cold logs from
`/tmp/gsd-158/final/captured_build_v158_<env>.log` into `tests/fixtures/` under the same names,
unedited. Then create `tests/fixtures/planted_size_baseline_flash_regression_v158.log` as a
byte-for-byte copy of `tests/fixtures/captured_build_v158_leonardo.log` with **exactly one line
changed**: the `Flash:` line, whose used figure becomes the capture's used figure plus 512, with the
percentage and the progress-bar rendering re-derived for the new figure by the same rule the v153
pair demonstrates (percentage as used over total to one decimal place with standard rounding; bar as
that percentage divided by ten, rounded to the nearest whole number of bar characters, left-aligned
in the same fixed-width field). The `RAM:` line, every percentage other than the flash one, and every
other byte are the capture's. Verify by diffing the plant against its capture: exactly one changed
line, and it is the `Flash:` line. Verify all four files are tracked with `git ls-files
tests/fixtures/`, never with `git add`'s exit code.

STEP 2 -- update the two native summary fixtures IN PLACE. Overwrite
`tests/fixtures/captured_test_native_summary.log` and
`tests/fixtures/captured_test_native_nodevtools_summary.log` with the two window files task 1
extracted from real runs. These are genuine captures at this tree position, not hand-edited counts --
the consuming leg's own docstring states that requirement in as many words. Do **not** sever this
pair: exactly one leg reads either file, which is the licence for the in-place convention.

STEP 3 -- re-record `scripts/baseline/size_baseline.json`, transcribing only. Write the sixteen
moving figures from task 1's transcription table: for each of the three AVR targets, `flash_used`
and `ram_used` transcribed from that target's committed capture, and `flash_free` / `ram_free` as the
untouched totals minus those figures; and for `native` and `native_nodevtools`, `cases` and
`succeeded` transcribed from the committed native summary captures. Leave `flash_total`, `ram_total`,
`suites`, the `native_pinmap_provisional` entry, `envs_agree` and the entire `warnings` block
untouched. Preserve the existing `meta` key order and append a **new** dated note key
`meta.cold_rerecord_phase158` -- following the precedent of the two existing dated note keys rather
than rewriting either -- recording: the cold recipe used, that the figures are transcribed from the
three committed `captured_build_v158_*` logs and from the two native summary captures, the phase and
plan, the firmware tree sha, and that BASE-01 is deliberately not re-anchored. Also repair the two
stale prose fields in the same commit: `meta.consumed_by` must name **three** consumers, adding the
release-asset checker with the note that it derives its required set from the `avr_targets` **keys**
and therefore cannot be affected by a value re-record, and that it runs in CI (C-7); and
`envs_agree_note`'s stale case-count quotation must be updated to the current recorded count while
keeping the note's meaning -- that the third native env is recorded only so the two consuming
scripts do not raise on that env name, never to widen the two-env policy scope (C-8). Validate the
file parses as JSON after editing.

STEP 4 -- repoint the four legs in `tests/test_check_size_baseline.py`, changing fixtures and
figures but never meanings. In `test_clean_avr_all_three_envs_pass` and
`test_default_mode_is_unchanged_by_the_new_flag`, change the three fixture names in each leg's tuple
from the v153 family to the v158 family and nothing else -- the exit-status, PASS-substring,
env-name and band-substring assertions are exactly as they were. In
`test_planted_flash_regression_flips_checker_to_failure`, change the fixture name to the v158 plant
and update the **two** literal figures it asserts appear in the FAIL text: the baseline figure
becomes the newly recorded leonardo `flash_used`, and the observed figure becomes that plus 512. In
`test_clean_native_both_envs_pass`, update the case-count literal to the newly recorded count; the
suite-count literal and both fixture names stay as they are. In each of the four legs, append a
severance paragraph to the docstring in the shape the prior generations used: which figure moved,
why the previous family no longer matches, that the previous family is retired in place and not
repointed, and -- for the plant leg -- that a leg which fails for a different reason than the one it
names is the false-cause pattern the severance exists to prevent. Touch **no other leg**: every leg
that reads BASE-01 or tampers with its own baseline copy must be byte-unchanged, and the six
MERGE-05 literals in the checker's source must be byte-unchanged.

STEP 5 -- prove it locally, then commit once. Run
`python3 -m pytest tests/test_check_size_baseline.py -q -o addopts=""` and require the full leg count
green with zero failures. Run
`python3 -m pytest tests/test_check_size_baseline.py tests/test_check_build_warnings.py -q -o
addopts=""` and require the combined count green -- this is the leg that catches an incomplete
severance. Only then commit in the **firmware** repo, staging exactly the eight paths this plan owns:
the baseline JSON, the four new fixtures, the two in-place native summaries, and the test module.
Subject: `test(158-04): re-record size_baseline.json from cold builds and sever fixtures onto v158`.
The body must state: the one-commit rule and why (the CI leg and its trigger); the three per-target
flash and RAM figures with the cold recipe named; the native counts; the severance inventory as 4
new plus 2 in place; that Groups 2 and 3 are not needed because no exemption is authored; that the
v153 family is retired in place and kept; and that BASE-01 is untouched. Do not push. **Then** run
`python3 -m pytest tests/ -q -o addopts=""` from `/workspaces/firestarter` on the now-committed tree
and record the count line verbatim, asserting the word `skipped` is absent -- running it before the
commit would redden four porcelain legs for a reason that has nothing to do with this work (S-5).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git log -1 --format=%s | grep -c '^test(158-04):')" = "1" && test "$(git diff --name-only HEAD~1 HEAD | wc -l)" = "8" && for p in scripts/baseline/size_baseline.json tests/fixtures/captured_build_v158_uno.log tests/fixtures/captured_build_v158_uno328pb.log tests/fixtures/captured_build_v158_leonardo.log tests/fixtures/planted_size_baseline_flash_regression_v158.log tests/fixtures/captured_test_native_summary.log tests/fixtures/captured_test_native_nodevtools_summary.log tests/test_check_size_baseline.py; do git diff --name-only HEAD~1 HEAD | grep -qx "$p" || { echo "MISSING PATH $p"; exit 1; }; done && test "$(git ls-files tests/fixtures/ | grep -c v158)" = "4" && test "$(git diff HEAD~1 HEAD -- scripts/baseline/size_baseline_base01.json | wc -l)" = "0" && test "$(diff tests/fixtures/captured_build_v158_leonardo.log tests/fixtures/planted_size_baseline_flash_regression_v158.log | grep -c '^[<>]')" = "2" && test "$(diff tests/fixtures/captured_build_v158_leonardo.log tests/fixtures/planted_size_baseline_flash_regression_v158.log | grep -c '^[<>] Flash:')" = "2" && python3 -c "
import json,re,sys
d=json.load(open('scripts/baseline/size_baseline.json'))
g=lambda e,k: int(re.search(k+r':.*used ([0-9]+) bytes', open('tests/fixtures/captured_build_v158_'+e+'.log').read()).group(1))
for e in ['uno','uno328pb','leonardo']:
    a=d['avr_targets'][e]; f=g(e,'Flash'); r=g(e,'RAM')
    assert a['flash_used']==f, (e,'flash_used',a['flash_used'],f)
    assert a['ram_used']==r, (e,'ram_used',a['ram_used'],r)
    assert a['flash_free']==a['flash_total']-f, (e,'flash_free')
    assert a['ram_free']==a['ram_total']-r, (e,'ram_free')
n=lambda p: [int(x) for x in re.search('([0-9]+) test cases: ([0-9]+) succeeded', open(p).read()).groups()]
for e,p in [('native','tests/fixtures/captured_test_native_summary.log'),('native_nodevtools','tests/fixtures/captured_test_native_nodevtools_summary.log')]:
    c,s=n(p); assert d['native_envs'][e]['cases']==c and d['native_envs'][e]['succeeded']==s, e
    assert d['native_envs'][e]['suites']==17, e
assert d['avr_targets']['uno']['flash_total']==32768 and d['avr_targets']['leonardo']['ram_total']==2560
assert 'cold_rerecord_phase158' in d['meta']
assert set(d['native_envs'].keys())=={'native','native_nodevtools','native_pinmap_provisional'}
print('BASELINE-FIXTURE-CONSISTENT')
sys.exit(0)" && for e in uno uno328pb leonardo; do test "$(grep -c "\"captured_build_v158_$e.log\"" tests/test_check_size_baseline.py)" = "2" || { echo "TUPLE NOT REPOINTED $e"; exit 1; }; test "$(grep -c "\"captured_build_v153_$e.log\"" tests/test_check_size_baseline.py)" = "0" || { echo "STALE TUPLE ENTRY $e"; exit 1; }; done && test "$(grep -c "'planted_size_baseline_flash_regression_v158.log'" tests/test_check_size_baseline.py)" = "1" && test "$(grep -c "'planted_size_baseline_flash_regression_v153.log'" tests/test_check_size_baseline.py)" = "0" && test "$(git diff HEAD~1 HEAD -- scripts/check_size_baseline.py | wc -l)" = "0" && python3 -m pytest tests/test_check_size_baseline.py tests/test_check_build_warnings.py -q -o addopts="" > /tmp/gsd-158/final/sever.log 2>&1 && grep -qE '[0-9]+ passed' /tmp/gsd-158/final/sever.log && test "$(grep -c 'failed' /tmp/gsd-158/final/sever.log)" = "0" && python3 -m pytest tests/ -q -o addopts="" > /tmp/gsd-158/final/pytest-post.log 2>&1 && test "$(grep -c skipped /tmp/gsd-158/final/pytest-post.log)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD~1 HEAD` lists exactly the eight paths this plan owns and nothing else, in ONE commit whose subject begins `test(158-04):`. No intermediate commit exists between plan 02's `test(158-02)` and this one.
    - `git ls-files tests/fixtures/` shows exactly four `v158` entries -- the three captures and the one plant -- with presence established through the index, never through `git add`'s exit code.
    - Each `captured_build_v158_<env>.log` is byte-identical to the corresponding log in `/tmp/gsd-158/final/`, is single-env `pio run` output, and contains no hand-edited number.
    - `diff tests/fixtures/captured_build_v158_leonardo.log tests/fixtures/planted_size_baseline_flash_regression_v158.log` reports **exactly one** changed line and that line is the `Flash:` line; the plant's used figure equals the capture's plus `512`; the `RAM:` line is byte-identical.
    - The two native summary fixtures are byte-identical to the windows task 1 extracted from real `pio test` runs, and the SUMMARY states the truncation window explicitly. Neither is severed.
    - Baseline-to-fixture consistency holds mechanically: for each AVR target, `flash_used` and `ram_used` in `size_baseline.json` equal the figures parsed from that target's committed capture, and `flash_free` / `ram_free` equal the untouched totals minus those figures; for both pinned native envs, `cases` and `succeeded` equal the figures parsed from the committed native summary captures.
    - `flash_total` (32768 on all three), `ram_total` (2048/2048/2560), `suites` (17), the `native_pinmap_provisional` entry, `envs_agree` and the entire `warnings` block are byte-unchanged; `native_envs` still has exactly the three recorded env keys and no `*_v131` env was added.
    - `size_baseline.json` parses as JSON, the `meta` key order is preserved, and a new `meta.cold_rerecord_phase158` key exists recording the cold recipe, the transcription source, the phase and plan, the tree sha, and that BASE-01 is deliberately not re-anchored.
    - `meta.consumed_by` names three consumers including the release-asset checker, with the keys-not-values note and the CI note; `envs_agree_note` no longer quotes the stale case count and keeps its original meaning about the third env's recorded-only status.
    - `scripts/baseline/size_baseline_base01.json` is byte-unchanged by this commit, and `scripts/check_size_baseline.py` is byte-unchanged -- so all six MERGE-05 literals are untouched.
    - `tests/test_check_size_baseline.py`'s **fixture-tuple entries** are fully repointed: the double-quoted form of each v158 capture name appears exactly twice (once in each of the two AVR legs' tuples), the double-quoted form of each v153 capture name appears zero times, the single-quoted v158 plant name appears exactly once, and the single-quoted v153 plant name appears zero times. Prose references to the retired family inside docstrings are expected and are not counted -- the quoted forms are the discriminator.
    - The planted-regression leg asserts two figures: the newly recorded leonardo `flash_used` and that figure plus 512. The native leg asserts the newly recorded case count. No assertion's meaning changed in any of the four legs, and each of the four carries a new docstring severance paragraph naming what moved, why the old family no longer matches, and that the old family is retired in place.
    - Every other leg in `tests/test_check_size_baseline.py` is byte-unchanged -- in particular the not-re-anchored leg, the three policy-fires legs, the measured-landing-deltas leg and all arms of the defect-fix admission leg.
    - `python3 -m pytest tests/test_check_size_baseline.py tests/test_check_build_warnings.py -q -o addopts=""` is green with zero failures -- the leg that catches an incomplete severance.
    - `python3 -m pytest tests/ -q -o addopts=""` was run from `/workspaces/firestarter` **after** the commit, is green, and contains no `skipped`.
  </acceptance_criteria>
  <done>One commit carries the cold re-record, the three new captures, the derived plant, the two in-place native re-captures and the four repointed legs; every recorded figure is mechanically equal to a figure parsed from a committed capture rather than computed; the untouched fields, BASE-01 and the checker source are all byte-unchanged; the v153 family is retired in place; and both the severance-detecting pytest pair and the full suite are green on the committed tree with no skips.</done>
</task>

<task type="auto">
  <name>Task 3: Discharge LAND-01 and LAND-02 through the gate ledger -- the polarity flip and the one-sided PASS line</name>
  <files>
    (no tracked file is edited -- this task runs gates and records their output)
  </files>
  <read_first>
    - `.planning/v1.33/158-before-figures.md` -- §4's default-mode RED shape with its verbatim failing lines, §5's one-sidedness section, and §6's list of legs that must not have moved. This is the before side of every polarity claim this task makes
    - `firestarter/scripts/check_size_baseline.py` -- the Usage block's canonical invocations, lines `697` and `709`, `compare_native`'s strict equality in both modes, `_merge05_flash_allowance` and `_merge05_ram_allowance` with the six literals they resolve, and `main()`'s ordering
    - `firestarter/scripts/baseline/size_baseline_base01.json` -- read only, to confirm the growth axis is unmoved and to know that its native case count is still the pre-fix value that plan 05 owns
    - `firestarter/tests/test_check_size_baseline.py` -- the three surviving policy-plant legs and the not-re-anchored leg, to name them in the evidence rather than describe them
    - `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-RESEARCH.md` -- F-2 in full, and Pitfalls 1, 2 and 6
  </read_first>
  <action>
Every leg below has its **expected shape stated before it is run**, so a green run cannot be
mistaken for a vacuous one. Record the command, the exit status and the salient output for each, and
mark each as CI-invoked or a local-run obligation.

LEG 1 -- the polarity flip, which is LAND-01's own discharge. Run `check_size_baseline.py` in
DEFAULT mode against `scripts/baseline/size_baseline.json`, passing the three committed
`tests/fixtures/captured_build_v158_*` logs as `--avr-log` inputs and the two committed native
summary captures as `--native-log` inputs. Expected: **exit 0 with a `PASS:` line** -- this leg has
been RED since Phase 155 and this is the commit that turns it GREEN. Quote plan 01's recorded RED
lines alongside the new PASS output so the flip is visible in one place, and state that this
polarity change is the discharge evidence rather than an incidental side effect.

LEG 2 -- the same default mode, but against a freshly built tree rather than the fixtures. Run
`check_size_baseline.py --rebuild` in default mode. Expected: the AVR comparison passes byte-identity
against the just-committed baseline, and the two native `cases` lines still fail because BASE-01 is
not the baseline here -- so this leg's expected shape depends on which baseline is passed. Record
which baseline was used, and record the exact shape observed. Note that `--rebuild` is being used
here purely as a **check**, never as a transcription source (Pitfall 6).

LEG 3 -- LAND-02's one-sidedness, from the gate. Run the canonical policy invocation:
`--policy merge05 --baseline scripts/baseline/size_baseline_base01.json` with the three
`--avr-log` inputs pointing at the committed v158 captures. Expected: **exit 0**, with a `PASS:`
line printing three **negative** flash deltas against positive allowances, and negative RAM deltas
against a small positive RAM tolerance. Capture that PASS line **verbatim** -- it is LAND-02's
one-sidedness evidence and it is strictly better than restating the policy in prose, because a
reader can see with their own eyes that the comparison admitted a negative delta with no exemption
authored for it.

LEG 4 -- the one-sidedness, from source. `sed -n '697p;709p' scripts/check_size_baseline.py`. Quote
both comparisons verbatim as the mechanism behind leg 3's observed behaviour, and record that both
are strict growth-only comparisons so a reduction of any magnitude passes. Confirm the six MERGE-05
literals are byte-unchanged by diffing the checker's source against the pre-phase position.

LEG 5 -- BASE-01's growth axis, unmoved. Run
`python3 -m pytest tests/test_check_size_baseline.py::test_base01_is_not_re_anchored_by_the_new_exemption
-q -o addopts=""`. Expected: green. Also assert by diff that `scripts/baseline/size_baseline_base01.json`
is byte-unchanged across this whole plan. This is LAND-01's "BASE-01 is NOT re-anchored" clause,
discharged by a leg that runs in CI rather than by an assurance.

LEG 6 -- the tripwire is still armed. Run the three surviving policy-plant legs by name --
`test_policy_merge05_fires_on_uno_class_over_band`,
`test_policy_merge05_fires_on_leonardo_growth`, `test_policy_merge05_fires_on_ram_move` -- and
require all three green. Then **assert the fact rather than re-planting**: because no exemption was
authored, the allowance those plants were derived against (`allowance + 1`) is unchanged, so they
remain valid evidence that the gate still fires on a real growth. Record this as the reason the
severance is 4 plus 2 rather than 13 (C-11), and state explicitly that Groups 2 and 3 are not needed
and were not authored.

LEG 7 -- the other two local-run gates. `python3 scripts/check_build_warnings.py` and
`python3 scripts/check_no_heap_or_64bit_symbols.py`, both expected exit 0. Record that the
`warnings` block was untouched by this plan, which is why the first is expected unchanged.

LEG 8 -- the full suite, from the canonical checkout. `python3 -m pytest tests/ -q -o addopts=""`
from `/workspaces/firestarter` on the committed tree. Record the count line verbatim and assert the
word `skipped` is absent. Compare the count against plan 02's post-commit count and account for any
difference.

Then assemble the LAND-01 and LAND-02 discharge record for plan 06: the sixteen re-recorded figures
with their transcription source; the polarity flip with both shapes; the verbatim one-sided PASS
line; the source quote of both comparisons; the severance inventory as 4 plus 2 with the
Groups-2-and-3 statement; the three surviving plants asserted green; and BASE-01 confirmed
byte-unchanged. Write no tracked file and create no commit in this task.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && test -z "$(git status --porcelain)" && test "$(git log -1 --format=%s | grep -c '^test(158-04):')" = "1" && python3 scripts/check_size_baseline.py --avr-log uno=tests/fixtures/captured_build_v158_uno.log --avr-log uno328pb=tests/fixtures/captured_build_v158_uno328pb.log --avr-log leonardo=tests/fixtures/captured_build_v158_leonardo.log --native-log native=tests/fixtures/captured_test_native_summary.log --native-log native_nodevtools=tests/fixtures/captured_test_native_nodevtools_summary.log > /tmp/gsd-158/final/default-mode.log 2>&1 && grep -q 'PASS:' /tmp/gsd-158/final/default-mode.log && python3 scripts/check_size_baseline.py --policy merge05 --baseline scripts/baseline/size_baseline_base01.json --avr-log uno=tests/fixtures/captured_build_v158_uno.log --avr-log uno328pb=tests/fixtures/captured_build_v158_uno328pb.log --avr-log leonardo=tests/fixtures/captured_build_v158_leonardo.log > /tmp/gsd-158/final/merge05.log 2>&1 && grep -q 'PASS:' /tmp/gsd-158/final/merge05.log && test "$(grep -oE 'flash=[0-9]+/[0-9]+\[-[0-9]+' /tmp/gsd-158/final/merge05.log | wc -l)" = "3" && sed -n '697p;709p' scripts/check_size_baseline.py | grep -c 'delta >' | grep -q 2 && python3 -m pytest "tests/test_check_size_baseline.py::test_base01_is_not_re_anchored_by_the_new_exemption" "tests/test_check_size_baseline.py::test_policy_merge05_fires_on_uno_class_over_band" "tests/test_check_size_baseline.py::test_policy_merge05_fires_on_leonardo_growth" "tests/test_check_size_baseline.py::test_policy_merge05_fires_on_ram_move" -q -o addopts="" 2>&1 | grep -qE '4 passed' && python3 scripts/check_build_warnings.py && python3 scripts/check_no_heap_or_64bit_symbols.py && python3 -m pytest tests/ -q -o addopts="" > /tmp/gsd-158/final/pytest-ledger.log 2>&1 && test "$(grep -c skipped /tmp/gsd-158/final/pytest-ledger.log)" = "0" && test -z "$(git status --porcelain)"</automated>
  </verify>
  <acceptance_criteria>
    - Every leg is recorded with its expected shape stated in advance, then its command, exit status and salient output, and a CI-invoked or local-run-obligation marker.
    - LEG 1: default mode against the re-recorded baseline exits `0` and prints `PASS:`. Plan 01's recorded RED lines are quoted alongside, and the SUMMARY states that this RED-to-GREEN flip is LAND-01's discharge evidence rather than a side effect.
    - LEG 2: the `--rebuild` check is recorded with the baseline it used and the exact shape observed, plus an explicit note that `--rebuild` was used as a check and never as a transcription source.
    - LEG 3: the canonical MERGE-05 invocation exits `0` and its `PASS:` line is captured verbatim, showing **three** negative flash deltas against positive allowances and negative RAM deltas against the RAM tolerance.
    - LEG 4: `check_size_baseline.py` lines `697` and `709` are quoted verbatim as growth-only comparisons, and the checker's source is confirmed byte-unchanged across the phase so all six MERGE-05 literals are untouched.
    - LEG 5: `test_base01_is_not_re_anchored_by_the_new_exemption` is green, and `scripts/baseline/size_baseline_base01.json` is byte-unchanged across this plan.
    - LEG 6: all three surviving policy-plant legs are green by name; the SUMMARY asserts their continued validity from the unchanged allowance rather than re-planting them, and states explicitly that Groups 2 and 3 are not needed and were not authored (C-11).
    - LEG 7: `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` both exit `0`, with the untouched `warnings` block named as the reason the first is expected unchanged.
    - LEG 8: `python3 -m pytest tests/ -q -o addopts=""` from `/workspaces/firestarter` is green with no `skipped`, and its count is reconciled against plan 02's post-commit count.
    - The LAND-01 / LAND-02 discharge record for plan 06 is assembled with all seven items listed in the action.
    - `git status --porcelain` is empty, HEAD still points at task 2's single commit, and no commit was created by this task.
  </acceptance_criteria>
  <done>LAND-01 is discharged by a default-mode gate that has flipped from the RED shape plan 01 recorded to a `PASS:` line, plus a BASE-01 not-re-anchored leg that runs in CI and a byte-unchanged BASE-01 file; LAND-02 is discharged by a verbatim one-sided `PASS:` line printing three negative deltas against positive allowances, the two growth-only comparisons quoted from source, six untouched exemption literals, three surviving plants asserted green rather than re-planted, and a severance inventory of 4 plus 2 with the Groups-2-and-3 omission stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build log -> recorded figure | A figure not transcribed from a committed capture cannot be audited; the mechanical baseline-to-fixture equality check is what turns the convention into a gate |
| committed fixture -> live baseline | Three legs compare a fixture against the live baseline in byte-identity mode, so the fixture is the observed side and a mismatch reads exactly like a real regression |
| this commit -> CI at `build.yml:161` | The checker's own pytest runs on this branch; any intermediate commit that moves the baseline without the fixtures is red |
| live baseline -> frozen anchor | BASE-01's growth axis must never move; re-anchoring it would erase a reduction exactly as it would erase a growth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-21 | Tampering | splitting the re-record and the severance across commits, leaving a CI-red commit on the branch | critical | mitigate | The one-commit rule is a prohibition; the verify asserts a single commit whose file list is exactly the eight owned paths, and that no intermediate commit exists between plan 02's and this one |
| T-158-22 | Repudiation | a recorded figure laundered by arithmetic rather than transcribed from a captured log | high | mitigate | A mechanical consistency check parses each committed capture and asserts equality with the JSON, including the derived `free` fields against untouched totals. `--rebuild` is prohibited as a transcription source |
| T-158-23 | Tampering | re-anchoring BASE-01's growth axis to make a gate green | critical | mitigate | BASE-01 is prohibited from edit in this plan; its byte-unchanged status is asserted by diff, and the not-re-anchored leg is run by name |
| T-158-24 | Tampering | authoring or widening a MERGE-05 exemption so the reduction "passes" | high | mitigate | Prohibited; `scripts/check_size_baseline.py` is asserted byte-unchanged, which pins all six literals; the pass is recorded as ONE-SIDED with both comparisons quoted from source |
| T-158-25 | Repudiation | a plant derived from the wrong generation's capture, so the leg fails for two reasons instead of the one it names | high | mitigate | The plant is a one-line derivation from the same generation's leonardo capture; the verify asserts exactly one differing line and that it is the `Flash:` line at capture-plus-512 |
| T-158-26 | Repudiation | repointing or editing a retired fixture family instead of severing onto a new one | medium | mitigate | All prior families prohibited from modification; the verify asserts zero v153 capture references remain in the test module while the files themselves stay tracked and unmodified |
| T-158-27 | Repudiation | a native summary fixture with a hand-edited count | high | mitigate | Both are byte-identical windows extracted from real `pio test` runs in task 1; the truncation window is a stated fact in the SUMMARY; the mechanical check ties the JSON counts to the fixture text |
| T-158-28 | Repudiation | reading a green one-sided gate as "nothing moved", or a green CI run as evidence the size gate passed | medium | mitigate | The PASS line's negative deltas are quoted verbatim; ceilings 1 and 2 are restated; every leg is marked CI-invoked or local-run |
| T-158-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs ZERO packages. RESEARCH's Package Legitimacy Audit records zero packages for the whole phase, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required |
</threat_model>

<verification>
- Exactly ONE commit, subject `test(158-04):`, whose file list is exactly the eight owned paths.
- `git ls-files tests/fixtures/` shows four `v158` entries; every `*_fullflash*`, `*_v132*`, `*_v151*` and `*_v153*` fixture is unmodified.
- The plant differs from its capture on exactly one line, the `Flash:` line, at capture-plus-512, with an identical `RAM:` line.
- Mechanical baseline-to-fixture consistency passes for all sixteen moving fields, with `free` values derived from untouched totals.
- `flash_total`, `ram_total`, `suites`, `native_pinmap_provisional`, `envs_agree` and the whole `warnings` block byte-unchanged; `native_envs` still has exactly three env keys.
- `size_baseline.json` parses; `meta.cold_rerecord_phase158` exists; `meta.consumed_by` names three consumers; `envs_agree_note`'s stale count is gone.
- `scripts/baseline/size_baseline_base01.json` and `scripts/check_size_baseline.py` byte-unchanged.
- `tests/test_check_size_baseline.py`: zero v153 capture references, six v158 capture references, one v158 plant reference; every other leg byte-unchanged.
- Default mode: exit 0 with `PASS:` -- the RED-to-GREEN flip against plan 01's recorded shape.
- Canonical MERGE-05: exit 0 with three negative flash deltas in the `PASS:` line.
- `sed -n '697p;709p'`: two growth-only comparisons.
- Four named legs green: the not-re-anchored leg plus the three policy-plant legs.
- `check_build_warnings.py` and `check_no_heap_or_64bit_symbols.py` exit 0.
- `python3 -m pytest tests/ -q -o addopts=""` from `/workspaces/firestarter`: green, no `skipped`.
</verification>

<success_criteria>
`size_baseline.json` is re-recorded from cold single-env builds of the final tree with every figure
mechanically equal to a figure parsed from a committed capture; the re-record, the three new
captures, the one-line-derived plant, the two in-place native re-captures and the four repointed
legs are in a single commit, so no commit on this branch is CI-red; the default-mode gate has flipped
from the RED shape plan 01 recorded to a `PASS:` line; the canonical MERGE-05 run exits 0 with three
negative deltas quoted verbatim as the one-sidedness evidence, with both growth-only comparisons
quoted from source and all six exemption literals byte-unchanged; BASE-01's growth axis is
byte-unchanged and proven so by a CI-run leg; the three surviving policy plants are asserted green
rather than re-planted; and the severance is recorded as 4 new plus 2 in place with the deliberate
omission of Groups 2 and 3 stated explicitly.
</success_criteria>

<output>
Create `.planning/phases/158-residual-optimizations-cold-baseline-re-record-firmware-only/158-04-SUMMARY.md` when done.
</output>