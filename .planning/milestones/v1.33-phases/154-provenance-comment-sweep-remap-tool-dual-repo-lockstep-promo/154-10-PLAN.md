---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 10
type: execute
wave: 3
depends_on: ["154-02"]
files_modified:
  # All hit-carrying files under firestarter_app/tools.
  # Worklist authority: survey_provenance.py --group app-tools --file-table.
  - firestarter_app/tools/**
autonomous: true
requirements: [SWEEP-01, SWEEP-03]
must_haves:
  truths:
    - "All 43 provenance hits across the 9 hit-carrying files under firestarter_app/tools are triaged under D-01."
    - "No generated artifact's output is edited: build_db.py and gen_sdp_bus_config.py are generators and only their own comments are touched, never their outputs."
    - "The tools' own gate modules still pass, and the audit-coverage-matrix ledger is not mutated by this plan."
  artifacts: []
  key_links:
    - "tools/build_db.py generates chip_database.json and tools/gen_sdp_bus_config.py generates a header measured at 0 provenance hits — editing either generator's comments must not change its output byte-for-byte."
    - "tools/audit_coverage_matrix.py MUTATES a committed ledger when run; a comment edit here must not be validated by running it."
---

<objective>
Sweep `firestarter_app/tools` — 43 hit lines across 9 files — under D-01.

Purpose: these are host-side developer tools, not test files, so D-01 applies in full
(keep-reflowed / delete / delete) and D-03 applies in its strip direction. The group is
small but it contains generators, and the project's standing rule is that a generated
artifact is never edited — fixed at the generator or left alone. A comment edit inside a
generator must therefore leave its output byte-identical, which is a checkable property
rather than a hope.

Output: `firestarter_app/tools` swept, with each generator's output proven unchanged.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/v1.33/sweep-corpus-baseline.md
@.planning/v1.33/sweep-gate-dispositions.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces no new artifact; it modifies existing files under `firestarter_app/tools` only.

<tasks>

<task type="auto">
  <name>Task 1: Sweep the three densest tool files (SWEEP-01, SWEEP-03, D-01, D-03)</name>
  <files>firestarter_app/tools/diff_db.py, firestarter_app/tools/check_dispatch.py, firestarter_app/tools/build_db.py</files>
  <read_first>
    - `.planning/v1.33/sweep-corpus-baseline.md` — the `app-tools` per-file hit table, D-01's procedure and token list, D-03's strip direction
    - `firestarter_app/tools/diff_db.py` (17 hits)
    - `firestarter_app/tools/check_dispatch.py` (11 hits)
    - `firestarter_app/tools/build_db.py` (8 hits) — the `chip_database.json` generator
    - `/workspaces/CLAUDE.md` — the standing rule that the EPROM database is generated by `build_db.py` and never hand-edited
  </read_first>
  <action>
Sweep `tools/diff_db.py`, `tools/check_dispatch.py` and `tools/build_db.py` — 36 of the group's
43 hits. These are developer tools rather than test files, so apply D-01 in full: strip the
tokens, then keep the sentence reflowed where it describes code that exists, and delete the whole
comment where nothing but connective punctuation or a tombstone remains. D-03's strip direction
applies: requirement and decision IDs are stripped here.

Step 3's guard binds. In `build_db.py` in particular, any comment stating a decode rule, a field
derivation, or a reason a field is *not* emitted is a non-obvious invariant — the project's
standing rule is that the chip database is **generated** and that a wrong field is fixed at the
generator, so a comment explaining why the generator does what it does is load-bearing. Keep and
reword rather than delete.

**`build_db.py` is a generator and its output must not change.** Capture a hash of
`firestarter/data/chip_database.json` (the committed generated artifact) before and after this
plan's edits and assert it is unchanged. This plan edits only comments, so the artifact cannot
change — but that is exactly the kind of claim worth asserting rather than assuming, because the
consequence of being wrong is a silently regenerated 746-row database.

Do not run `tools/audit_coverage_matrix.py` as a verification step. It **mutates** a committed
ledger when run, so using it to check a comment edit would create a spurious diff attributed to
this phase.

Touch no code line and no docstring line.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group app-tools && test "$(git -C firestarter_app diff -U0 -- tools | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)')" -eq 0 && git -C firestarter_app diff --quiet -- firestarter/data/chip_database.json</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group app-tools --json` reports 0 hits for `tools/diff_db.py`, `tools/check_dispatch.py` and `tools/build_db.py`.
    - `git -C firestarter_app diff -U0 -- tools | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)'` == 0 — every added and removed line is a `#` comment or blank.
    - `git -C firestarter_app diff --quiet -- firestarter/data/chip_database.json` exits 0 — the generated artifact is untouched.
    - `sha256sum firestarter_app/firestarter/data/chip_database.json` equals the value captured at task start.
    - Source assertion: no docstring line modified — `git -C firestarter_app diff -U0 -- tools | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -cE '^[+-][[:space:]]*"""'` == 0.
    - `tools/audit_coverage_matrix.py` was **not** run: the SUMMARY states so, and `git -C firestarter_app status --porcelain` shows no ledger file modified.
    - Manual-only, named here rather than laundered into an automated leg: whether a `build_db.py` decode-rule comment was correctly judged load-bearing. The reviewable artifact is `git -C firestarter_app diff -- tools/build_db.py`.
  </acceptance_criteria>
  <done>The three densest tool files are swept, the generated database is provably untouched, and the ledger-mutating tool was not run.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the remaining six tool files and run the tools' own gates (SWEEP-01, SWEEP-03, D-01)</name>
  <files>firestarter_app/tools/gen_sdp_bus_config.py, firestarter_app/tools/check_is_memory_cmd_no_ifdef.py, firestarter_app/tools/parse_devtest_issue.py, firestarter_app/tools/check_devtest_orchestrator.py, firestarter_app/tools/catalog/codegen_vectors.py, firestarter_app/tools/audit_coverage_matrix.py</files>
  <read_first>
    - `.planning/v1.33/sweep-corpus-baseline.md` — the `app-tools` per-file hit table for the six remaining files
    - `firestarter_app/tools/gen_sdp_bus_config.py` (1 hit) — the generator for `firestarter/test/native/avr/_shared/sdp_bus_config.h`, which plan 02 recorded at a **measured 0** provenance hits
    - `firestarter_app/tools/check_is_memory_cmd_no_ifdef.py` (1 hit) — the checker `test_check_is_memory_cmd_no_ifdef.py` drives, which does comment stripping in the tool
    - `firestarter_app/tools/parse_devtest_issue.py`, `tools/check_devtest_orchestrator.py`, `tools/catalog/codegen_vectors.py`, `tools/audit_coverage_matrix.py`
    - `.planning/v1.33/sweep-gate-dispositions.md` §A — the generated-header dispositions
  </read_first>
  <action>
Sweep the remaining six hit-carrying files in the group — 7 hits total, one or two each. Same
D-01 treatment and same D-03 strip direction as task 1.

**Generated-output rule, restated because two generators are in this list.**
`tools/gen_sdp_bus_config.py` produces `firestarter/test/native/avr/_shared/sdp_bus_config.h`,
and `tools/gen_validation_header.py` produces `validation_matrix.h`. Plan 02 recorded both
outputs at a **measured 0** provenance hits, which is precisely what discharges SWEEP-06's "fixed
at their generators or shown to need no fix". So: sweep the generator's own comment, never its
output, and prove the output did not change by regenerating and diffing, or — if regeneration is
not safely runnable here — by asserting the committed output's hash is unchanged. Whichever route
is taken, record it.

`tools/check_is_memory_cmd_no_ifdef.py` is the checker behind a gate that plan 02 classified safe
because the tool itself strips comments. Sweeping its own comments cannot affect that, but run its
gate afterwards to confirm.

Then run the host gates that name any file this plan touched, plus the two SWEEP-07 gates. Do not
run the full 235 s host suite here — it belongs at plan 12's phase gate, after both sub-repo
commits land, because 9 modules assert git porcelain and this plan's edits are uncommitted.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group app-tools && git -C firestarter diff --quiet -- test/native/avr/_shared/sdp_bus_config.h test/native/avr/_shared/validation_matrix.h && cd firestarter_app && FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group app-tools --json` reports **0** hits for the whole group, with no residual left unattributed.
    - `git -C firestarter diff --quiet -- test/native/avr/_shared/sdp_bus_config.h test/native/avr/_shared/validation_matrix.h` exits 0 — neither generated header changed. Note these live in the *firmware* repo while their generators live in the *app* repo, which is why the assertion is on `firestarter`, not `firestarter_app`.
    - `pytest tests/test_check_is_memory_cmd_no_ifdef.py tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q` exits 0.
    - Every added and removed diff line under `tools` is a `#` comment or blank; zero docstring lines.
    - The chosen route for proving the generated outputs unchanged (regenerate-and-diff, or committed-hash comparison) is named in the SUMMARY with its result.
    - The full host suite was **not** run in this plan, and the SUMMARY says so with the reason (9 porcelain-asserting modules, uncommitted edits, D-11 ordering).
  </acceptance_criteria>
  <done>The whole app-tools group reads 0 hits, both generated headers are provably unchanged, and the tools' own gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator source → generated artifact | editing a generator's comment must not change its output |
| tool source → committed ledger | `audit_coverage_matrix.py` mutates a ledger when run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-39 | Tampering | a generated artifact changing as a side effect of a comment edit or of a verification run | high | mitigate | `chip_database.json` hash asserted unchanged; both generated headers asserted unchanged by `git diff --quiet` in the firmware repo; the regeneration route recorded. |
| T-154-40 | Tampering | running `audit_coverage_matrix.py` as a check and mutating its committed ledger, creating a diff wrongly attributed to this phase | medium | mitigate | Explicitly forbidden as a verification step; asserted by a porcelain check and a SUMMARY statement. |
| T-154-41 | Denial of Service (of future correctness) | deleting a `build_db.py` decode-rule comment, where the standing project rule is that a wrong database field is fixed at the generator | high | mitigate | D-01 step 3 keep-and-reword instruction called out for `build_db.py` specifically; the diff named as the review artifact. |
</threat_model>

<verification>
- `survey_provenance.py --group app-tools` reports 0 hits for the group.
- `chip_database.json` hash unchanged; both generated headers unchanged in the firmware repo.
- Every added/removed diff line under `tools` is a `#` comment or blank; zero docstring lines.
- `test_check_is_memory_cmd_no_ifdef.py` and the two SWEEP-07 gates pass.
</verification>

<success_criteria>
- The app-tools group carries no planning provenance.
- No generated artifact changed, and the assertion is mechanical rather than argued.
- No committed ledger was mutated by a verification step.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-10-SUMMARY.md` when done.
</output>
