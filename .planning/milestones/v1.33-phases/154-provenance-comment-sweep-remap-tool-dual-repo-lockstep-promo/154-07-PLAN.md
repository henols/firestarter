---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 07
type: execute
wave: 3
depends_on: ["154-02"]
files_modified:
  # All hit-carrying files under firestarter/src and firestarter/include EXCEPT
  # src/proms/eeprom_28c.cpp (plan 06) and the four Ruling B exempted paths
  # (include/eprom_params.h, src/proms/eprom.cpp,
  # test/native/avr/_shared/{eprom_v131_expected,sdp_expected}.h).
  # Worklist authority: survey_provenance.py --group fw-src --group fw-include.
  - firestarter/src/**
  - firestarter/include/**
  - firestarter/tests/golden/eprom_params_citations.json
  - firestarter/tests/golden/protocol_branch_inventory.json
autonomous: true
requirements: [SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-05, SWEEP-06]
must_haves:
  truths:
    - "Every provenance hit in firestarter/src and firestarter/include is triaged under D-01, except the D-02 no-touch region and the four Ruling B exempted files."
    - "firestarter/src/firestarter.cpp:177-195 is byte-identical to its pre-sweep state and test_cap03_ack_layout_parity.py stays green including its planted legs."
    - "CAP-0N survives everywhere it appears, as cross-repo wire-protocol vocabulary."
    - "eprom_params.cpp is swept and BOTH sidecars pinning its blob SHA are re-derived in the same working-tree state."
    - "The four exempted pinned files are untouched, verified by an empty git diff on each."
    - "The uno, uno328pb and leonardo artifacts are byte-identical to the pre-sweep record."
  artifacts:
    - firestarter/tests/golden/eprom_params_citations.json
    - firestarter/tests/golden/protocol_branch_inventory.json
  key_links:
    - "eprom_params.cpp is pinned by TWO sidecars, not one. Updating only eprom_params_citations.json leaves test_protocol_branch_inventory.py RED."
    - "The blob SHA is content-addressed, so `git hash-object <file>` on the swept working-tree content equals what `git rev-parse HEAD:<path>` reports once the commit lands — that is the re-derivation the gate's own failure message asks for."
---

<objective>
Sweep the shipped firmware source — `firestarter/src` and `firestarter/include` —
under D-01, minus the D-02 no-touch region and minus the four blob-SHA-pinned files
Ruling B exempts, and re-derive the two sidecars that pin the one pinned file this
plan does sweep.

Purpose: this is the 20% of the corpus the strongest oracle actually covers. The
`uno` byte-identity oracle covers 129 of 635 hit lines and they all live here, so
this is the only group where "nothing executable changed" is mechanically provable
rather than argued. It is also where the D-02 no-touch region sits: the
CAP-01/CAP-02/CAP-03 wire-layout block is a gate fixture that happens to be spelled
as a comment, pinned verbatim in **raw, un-stripped** text by
`test_cap03_ack_layout_parity.py`.

Output: the shipped-firmware sweep, `eprom_params_citations.json` and
`protocol_branch_inventory.json` re-derived, and the byte-identity oracle run for
all three AVR targets.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md
@.planning/v1.33/sweep-corpus-baseline.md
@.planning/v1.33/sweep-gate-dispositions.md
@.planning/v1.33/baseline-pre-sweep.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces the two re-derived sidecars
`firestarter/tests/golden/eprom_params_citations.json` and
`firestarter/tests/golden/protocol_branch_inventory.json`.

<tasks>

<task type="auto">
  <name>Task 1: Sweep firestarter/src and firestarter/include under D-01, honouring the D-02 no-touch region and the Ruling B exemptions (SWEEP-01, SWEEP-02, SWEEP-03, SWEEP-06)</name>
  <files>firestarter/src/** and firestarter/include/** — every hit-carrying file per `survey_provenance.py --group fw-src --group fw-include --file-table`, EXCLUDING src/proms/eeprom_28c.cpp (plan 06), src/firestarter.cpp lines 182-200 (D-02 no-touch), include/eprom_params.h, src/proms/eprom.cpp (Ruling B)</files>
  <read_first>
    - `.planning/v1.33/sweep-corpus-baseline.md` — D-01's procedure verbatim with its token list, the unit-of-edit rule, the step-3 guard, and the per-file hit table for `fw-src` and `fw-include`
    - `.planning/v1.33/sweep-gate-dispositions.md` §C — the four exempted files and the double-pin consequence
    - `firestarter/src/firestarter.cpp` lines 175-215 — the no-touch region and its boundaries
    - `firestarter/src/json_parser.c` — the C-comment delimiter form and the reset-invariant comment near line 151
    - `firestarter/include/firestarter.h` — the `CAP-01` mention near line 53 and the slot-allocation invariant near line 94
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md` lines 532-577 — the worked before/after for `firestarter.cpp`, `json_parser.c` and `firestarter.h`
  </read_first>
  <action>
Sweep every file in `firestarter/src` and `firestarter/include` carrying a provenance hit,
using `survey_provenance.py --group fw-src --group fw-include --file-table` as the worklist.
Per plan 02's measurement the groups hold roughly 102 and 27 hit lines respectively across
about 34 files; work from the tool's table, not from a remembered list.

**Exclusions, each of which must be verifiable as untouched afterwards:**

1. `firestarter/src/proms/eeprom_28c.cpp` — swept by plan 06, not here.
2. `firestarter/src/firestarter.cpp` lines 182-200 — the D-02 **no-touch region**. It carries
   the CAP-01/CAP-02/CAP-03 wire-layout block, and `test_cap03_ack_layout_parity.py` pins
   `_WIRE_LAYOUT_COMMENT` verbatim against the **raw, un-stripped** text of this file with the
   pinned string at line 192. It is a gate fixture that happens to be spelled as a comment.
   The rest of `firestarter.cpp`'s 8 hits are in scope.
3. The four **Ruling B exempted** files: `include/eprom_params.h`, `src/proms/eprom.cpp`,
   `test/native/avr/_shared/eprom_v131_expected.h`, `test/native/avr/_shared/sdp_expected.h`.
   Each is blob-SHA-pinned by a golden sidecar with no regeneration tool, and the exemption is
   recorded with its cause in `sweep-gate-dispositions.md` §C. Leave all four byte-identical.

**D-02's exemption applies everywhere:** `CAP-0N` is live cross-repo wire-protocol vocabulary,
present in shipped host source and referenced by 13 host test modules, so it survives every
comment it appears in — not only inside the no-touch region. Strip the surrounding phase label
and keep the `CAP-0N` token. Apply the generalised both-repos exemption test before stripping
any token not on D-01's list: a token appearing in both repos' shipped source is vocabulary,
not provenance.

**D-03 applies here in its strip direction:** these are shipped-source files, so requirement
and decision IDs are stripped. In shipped source an ID resolves only against `.planning/`,
which is the coupling this phase exists to remove.

Named step-3 keeps in this group, from PATTERNS.md's worked examples:

- `src/json_parser.c`'s reset-invariant comment near line 151 — it is the only statement of
  the invariant that prevents the read-timing-knobs bug class. Strip the ID, keep and reword
  the sentence so it stands alone. Note the file uses C `/* */` delimiters, so the reflow must
  preserve the delimiter form.
- `include/firestarter.h`'s slot-allocation invariant near line 94 — the statement about which
  integer is next unused is a live invariant; strip the phase and requirement labels and keep it.
- `src/boards/uno_rurp_shield.cpp` and `src/proms/flash_5v_page.cpp` each hold one of the five
  keep-examples the writeup names, and both land on keep-reflowed. Show the surviving sentence
  for each in the SUMMARY — SWEEP-01 requires all five keep-examples to be *shown* to land on
  keep-reflowed, and three of the five (`uno_rurp_shield.cpp`, `flash_5v_page.cpp`,
  `json_parser.c`) are in this plan's scope. `eprom_params.cpp` is task 2's. The fifth,
  `database.py`, is plan 09's.
- Tombstones delete outright: a comment describing code that is not there, such as one
  recording that a bootstrap call was deleted, is a whole-comment delete under step 2.

Do not touch any code line anywhere.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group fw-src --group fw-include && git -C firestarter diff --quiet -- include/eprom_params.h src/proms/eprom.cpp test/native/avr/_shared/eprom_v131_expected.h test/native/avr/_shared/sdp_expected.h && test "$(git -C firestarter diff -U0 -- src/firestarter.cpp | grep -c 'buffer_size u16 BE')" -eq 0 && test "$(git -C firestarter diff -U0 -- src include | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(//|\*|/\*|$)')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group fw-src --group fw-include --json` reports 0 hits for every file except the four Ruling B exempted paths, whose recorded hit counts are unchanged (1, 20, 4, 3), and except the D-02-exempt `CAP-0` lines which survive by design — the tool's `--json` output must be pasted into the SUMMARY so the residual is visible and attributed rather than merely asserted zero.
    - `git -C firestarter diff --quiet -- include/eprom_params.h src/proms/eprom.cpp test/native/avr/_shared/eprom_v131_expected.h test/native/avr/_shared/sdp_expected.h` exits 0 — all four exempted files are byte-identical.
    - `git -C firestarter diff -U0 -- src/firestarter.cpp | grep -c 'buffer_size u16 BE'` == 0 — the pinned wire-layout string was neither added nor removed, so the no-touch region is untouched. The grep is scoped to the **diff**, not the file, so it cannot be invalidated by the string legitimately existing in the file.
    - `sed -n '182,200p' firestarter/src/firestarter.cpp` is byte-identical to `git -C firestarter show <FW_PRE_SHA>:src/firestarter.cpp | sed -n '182,200p'` — assert with `diff` on the two extracts, allowing for the region's line numbers having shifted only if the shift is recorded and the content compared by search rather than by line range.
    - Every added and removed diff line under `src` and `include` is a comment line or blank: the `git diff -U0` filtered count == 0.
    - `pytest tests/test_cap03_ack_layout_parity.py -o addopts="" -q` from `firestarter_app` exits 0 including its two pre-existing planted legs, proving the cap03 gate is green **and** still able to fail (SWEEP-02).
    - `grep -c 'CAP-0' firestarter/src/firestarter.cpp firestarter/include/firestarter.h` is unchanged from the pre-sweep count captured at task start — the D-02 exemption held.
    - Manual-only, named here rather than laundered into an automated leg: whether D-01 step 3's guard was honoured is a review judgment. The reviewable artifact is `git -C firestarter diff -- src include`, and the SUMMARY must quote the surviving sentence for each of the three keep-examples in this plan's scope.
  </acceptance_criteria>
  <done>Shipped firmware source carries no provenance except the D-02 exemption and the four recorded Ruling B exemptions; the no-touch region is provably untouched; no code line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep eprom_params.cpp and re-derive BOTH sidecars pinning it (Ruling B, SWEEP-01, SWEEP-06)</name>
  <files>firestarter/src/proms/eprom_params.cpp, firestarter/tests/golden/eprom_params_citations.json, firestarter/tests/golden/protocol_branch_inventory.json</files>
  <read_first>
    - `firestarter/src/proms/eprom_params.cpp` — both hits: the file-header block and the fail-closed-return comment near line 61
    - `.planning/v1.33/sweep-gate-dispositions.md` §C — the regeneration ruling and the double-pin consequence
    - `firestarter/tests/golden/eprom_params_citations.json` — `meta.blob_shas` and `meta.sources`
    - `firestarter/tests/golden/protocol_branch_inventory.json` — `meta.blob_shas` and `meta.sources`
    - `firestarter/tests/test_eprom_params_citations.py` lines 293-311 — the blob-SHA gate and its "never hand-edit the SHA" instruction
    - `firestarter/tests/test_protocol_branch_inventory.py` lines 398-410 — the sibling blob-SHA gate
  </read_first>
  <action>
Sweep `firestarter/src/proms/eprom_params.cpp`'s two hits. The one near line 61 — the
fail-closed-return rationale, stating that the function returns a null pointer with zero
hardware side effects rather than the first table entry — is one of SWEEP-01's five **named**
keep-and-reflow examples, and delivering it is exactly why Ruling B chose to regenerate rather
than exempt. Strip the decision ID, keep and reword the sentence so it stands alone. The
file-header block's phase/plan/requirement labels strip; whatever it says about the table being
const and PROGMEM-resident is a description of code that exists and is kept, reflowed.

Then re-derive the blob SHAs. The gates' own failure messages instruct re-deriving the sidecar
rather than hand-editing the SHA; a blob SHA is content-addressed, so `git -C firestarter
hash-object src/proms/eprom_params.cpp` on the swept working-tree content is exactly what
`git rev-parse HEAD:src/proms/eprom_params.cpp` will report once the sweep commit lands. That
is the re-derivation, not a hand-invented value.

**Update the `src/proms/eprom_params.cpp` entry in `meta.blob_shas` of BOTH sidecars** —
`tests/golden/eprom_params_citations.json` and `tests/golden/protocol_branch_inventory.json`.
This double-pin is the consequence Ruling B does not name and plan 02 recorded: updating only
the first leaves `test_protocol_branch_inventory.py::test_blob_shas_match_the_recorded_inventory`
RED.

**Leave every other blob-SHA entry alone.** In particular `protocol_branch_inventory.json`'s
`src/proms/eprom.cpp` entry and `eprom_params_citations.json`'s `include/eprom_params.h` entry
must not change, because both of those files are Ruling B exempted and therefore unswept.

Add, to each sidecar's `meta`, a short note key recording that the `eprom_params.cpp` blob SHA
was re-derived by the provenance comment sweep with no change to any cell value, any recorded
site, or any table content — and naming SWEEP-01 and SWEEP-06 rather than a phase or plan
label. Neither gate asserts an exact `meta` key set (verified), so an added note key is safe;
do not remove or rename any existing key, and do not touch `meta.recorded_at_head`, which
records the head at original recording rather than a live value.

Two verified facts bound the regeneration and must be restated in the SUMMARY so a reviewer
does not have to re-derive them: `eprom_params_citations.json`'s `cells` carry
`row`/`column`/`value`/`basis`/`notes`/`reasoned_from` and **no line-number field**, and
`protocol_branch_inventory.json`'s line-bearing `sites` array is extracted from
`src/proms/eprom.cpp` only, while its `params_table` scan of `eprom_params.cpp` is
**comment-stripped** and therefore immune to this edit. So the only thing this sweep can
invalidate in either sidecar is the one blob SHA.
  </action>
  <verify>
    <automated>cd firestarter && test "$(python3 -c "
import json,subprocess
live=subprocess.run(['git','-C','.','hash-object','src/proms/eprom_params.cpp'],capture_output=True,text=True).stdout.strip()
a=json.load(open('tests/golden/eprom_params_citations.json'))['meta']['blob_shas']
b=json.load(open('tests/golden/protocol_branch_inventory.json'))['meta']['blob_shas']
ok = a['src/proms/eprom_params.cpp']==live and b['src/proms/eprom_params.cpp']==live
print('OK' if ok else 'MISMATCH')
")" = "OK" && git diff --quiet -- src/proms/eprom.cpp include/eprom_params.h && python3 -c "
import json
b=json.load(open('tests/golden/protocol_branch_inventory.json'))['meta']['blob_shas']
assert b['src/proms/eprom.cpp']=='838aca47986103969be4caca3cef71a033bac069', 'eprom.cpp pin must not change'
a=json.load(open('tests/golden/eprom_params_citations.json'))['meta']['blob_shas']
assert a['include/eprom_params.h']=='b04c788b02c1be51200d22a876f03f4de876cd9e', 'eprom_params.h pin must not change'
print('exempt pins intact')"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter hash-object src/proms/eprom_params.cpp` equals the `src/proms/eprom_params.cpp` value in `meta.blob_shas` of **both** sidecars.
    - `protocol_branch_inventory.json`'s `src/proms/eprom.cpp` pin still equals `838aca47986103969be4caca3cef71a033bac069`, and `eprom_params_citations.json`'s `include/eprom_params.h` pin still equals `b04c788b02c1be51200d22a876f03f4de876cd9e` — both exempted files' pins are untouched.
    - `git -C firestarter diff --quiet -- src/proms/eprom.cpp include/eprom_params.h` exits 0.
    - `survey_provenance.py` reports 0 hits for `src/proms/eprom_params.cpp`.
    - Both sidecars still parse as JSON and no pre-existing `meta` key was removed or renamed: assert the pre-task `meta` key set is a subset of the post-task one for each file.
    - After the sweep commit lands in plan 12, `pytest tests/test_eprom_params_citations.py tests/test_protocol_branch_inventory.py` must exit 0. Because those gates read `git rev-parse HEAD:<path>` rather than the working tree, they are **expected RED until the commit lands** — record that expectation here explicitly so plan 12 does not diagnose it as a defect, and record the actual pre-commit result observed.
    - The SUMMARY quotes the surviving keep-example sentence from near line 61 in full (SWEEP-01's "shown to land on keep, reflowed") and restates the two verified facts that bound the regeneration.
  </acceptance_criteria>
  <done>The one blob-SHA-pinned file this phase sweeps is swept, both sidecars pinning it are re-derived by content hash, and the four exempted files' pins are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Byte-identity oracle on all three AVR targets, plus the native and firmware gate suites (SWEEP-05, Ruling E)</name>
  <files>(verification only — no file is modified; builds write to firestarter/.pio/build/{uno,uno328pb,leonardo}, gitignored)</files>
  <read_first>
    - `.planning/v1.33/baseline-pre-sweep.md` — the pre-sweep hash and size pairs for `uno`, `uno328pb` and `leonardo`, and the recorded suite baselines
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md` §`R5` lines 208-232 — why the `.elf` is primary, the `.hex` corroborating and the size summary weakest
  </read_first>
  <action>
Run the byte-identity oracle per file swept in this plan, at the 1.5 s per-file cadence — it
localises a regression to one file instead of to a batch. After the whole plan's edits, run the
recorded form for all three AVR targets: `rm -rf .pio/build/<env>` then `pio run -e <env>`, then
`sha256sum` the `.elf` and the `.hex` and read the `Flash:`/`RAM:` lines.

Per Ruling E, record the hash pair and the size pair for each of `uno`, `uno328pb` and
`leonardo`. The `.elf` is primary; the `.hex` is corroboration only, because `avr-objcopy -O ihex`
drops non-loadable sections so a `.hex` match would mask a debug-section change; and the
`pio run` size summary alone is the weakest form and must not be the primary criterion, because
a size-neutral code change would slip past it.

SWEEP-05's rule on any delta is explicit: **reverted, not explained.** If a target's `.elf`
sha256 differs, bisect by reverting file by file — the per-file cadence makes this cheap —
identify the file, revert it, re-measure to show the baseline restored, and report the file and
the divergence.

Then run the firmware suites and compare against plan 01's baseline: `pio test -e native`
(172 cases expected) and `pytest tests/` from `firestarter`. Expect
`test_eprom_params_citations.py` and `test_protocol_branch_inventory.py` to be RED at this point
because their blob-SHA legs read `git rev-parse HEAD:<path>` and this plan's edits are
uncommitted; record that as an expected, attributed failure with the reason, not as an
unexplained regression. Every other module's result must be no worse than baseline.

Do not commit. The firmware repo takes exactly one commit for this phase, created in plan 12.
  </action>
  <verify>
    <automated>cd firestarter && for e in uno uno328pb leonardo; do rm -rf .pio/build/$e && pio run -e $e || exit 1; sha256sum .pio/build/$e/firestarter_$e.elf .pio/build/$e/firestarter_$e.hex; done && pio test -e native</automated>
  </verify>
  <acceptance_criteria>
    - For each of `uno`, `uno328pb` and `leonardo`: the post-sweep `.elf` sha256 **equals** the pre-sweep value in `.planning/v1.33/baseline-pre-sweep.md`, character for character.
    - For each of the three: the `.hex` sha256 and the `Flash:`/`RAM:` figures also match, recorded as corroboration.
    - `pio test -e native` reports the same pass/fail counts as the plan 01 baseline.
    - `pytest tests/` from `firestarter` shows no failure other than the two blob-SHA modules, and each of those is recorded with the reason "blob-SHA leg reads `HEAD:<path>`; this plan's edits are uncommitted; plan 12's commit resolves it".
    - If any hash diverges, the offending file is named, its edit reverted, and a second oracle run shows the baseline restored — recorded, not explained away.
  </acceptance_criteria>
  <done>All three AVR artifacts are byte-identical by hash and by size, the native suite matches baseline, and the only firmware gate failures are the two blob-SHA modules with their cause recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment text → `test_cap03_ack_layout_parity.py`'s raw-text pin | the no-touch region; a reflow here breaks a gate fixture |
| working-tree content → committed blob SHA | the sidecar pins live on this boundary |
| comment edit → compiled artifact | the boundary the byte-identity oracle guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-26 | Tampering | editing `firestarter.cpp:177-195`, breaking the verbatim `_WIRE_LAYOUT_COMMENT` pin | high | mitigate | Diff-scoped grep asserting the pinned string appears on no added or removed line; region extract compared against `FW_PRE_SHA`; `test_cap03_ack_layout_parity.py` green including its planted legs. |
| T-154-27 | Tampering | sweeping a blob-SHA-pinned file without updating every sidecar that pins it | high | mitigate | Ruling B exemptions verified by `git diff --quiet`; the double-pin handled by asserting both sidecars carry the live `hash-object` value; the exempt pins asserted unchanged by literal comparison. |
| T-154-28 | Tampering | stripping `CAP-0N`, destroying vocabulary shared by both repos and the wire protocol | high | mitigate | D-02 exemption applied everywhere, not only in the no-touch region; the pre/post `grep -c 'CAP-0'` count is asserted unchanged. |
| T-154-29 | Tampering | a code change hidden inside a comment sweep | critical | mitigate | Diff-line-class assertion (every added/removed line is a comment or blank) plus a three-target `.elf` sha256 equality; any delta is reverted, not explained. |
| T-154-30 | Denial of Service (of future correctness) | losing the json_parser reset invariant or the slot-allocation invariant | high | mitigate | Both named as explicit step-3 keeps; the surviving sentence for each keep-example is quoted in the SUMMARY for review. |
</threat_model>

<verification>
- `survey_provenance.py --group fw-src --group fw-include --json` residual is only the D-02 `CAP-0` lines and the four exempted files' recorded counts, pasted into the SUMMARY.
- The four exempted files have an empty `git diff`; their sidecar pins are unchanged literals.
- Both sidecars carry the live `git hash-object` value for `eprom_params.cpp`.
- Three AVR targets match the pre-sweep hash and size pairs.
- `pio test -e native` matches baseline; only the two blob-SHA gate modules are RED, with cause.
</verification>

<success_criteria>
- The only group covered by a strong oracle is swept and proven executable-identical on three targets.
- The no-touch region and the four exemptions are provably intact.
- The double-pin consequence is handled, so plan 12's commit lands with all four F3 gates able to go green.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-07-SUMMARY.md` when done.
</output>
