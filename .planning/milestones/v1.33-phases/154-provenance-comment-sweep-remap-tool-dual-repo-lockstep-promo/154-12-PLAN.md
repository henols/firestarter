---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 12
type: execute
wave: 5
depends_on: ["154-05", "154-06", "154-07", "154-08", "154-09", "154-10", "154-11"]
files_modified:
  - .planning/v1.33/CITATIONS-STALE.md
  - .planning/v1.33/sweep-outcome-record.md
  - .planning/v1.33/sweep-citation-manifest.jsonl
autonomous: true
requirements: [SWEEP-05, SWEEP-07, SWEEP-10, SWEEP-12, SWEEP-13]
must_haves:
  truths:
    - "The post-sweep byte-identity pair for all three AVR targets equals the pre-sweep pair, recorded as hashes AND as Flash:/RAM: figures."
    - "All five SWEEP-07 planted controls are re-run after the sweep and behave as they did before it."
    - "The D-08 retarget subset is settled against the actual post-sweep diff, its rows are flagged retarget: true with the original cited text preserved, and its COUNT is reported."
    - "The staleness marker exists, names the actually-swept files, states the citations are knowingly stale, and points at Phase 159 / REMAP-04 as the close-blocking closer."
    - "Exactly one commit in firestarter and exactly one in firestarter_app carry this phase's source changes, and both land BEFORE the full host suite runs."
    - "The archived-milestones hazard is recorded either way: absent, with its cause."
  artifacts:
    - .planning/v1.33/CITATIONS-STALE.md
    - .planning/v1.33/sweep-outcome-record.md
  key_links:
    - "The staleness marker is the interface to REMAP-04's close block. Without it, D-01's deliberate staleness window becomes a promise instead of a structural guarantee."
    - "9 modules assert git porcelain — 5 in the firmware repo including test_flash_path_record_sync.py:1247, and 4 in the app repo asserting on the firmware repo. Both sub-repo commits MUST land before the full host suite runs, or 7+ modules go RED on the sweep's own dirt."
---

<objective>
Land the phase: record the post-sweep measurements, settle the D-08 retarget subset,
plant the staleness marker, produce the three commits in D-11's order, and run the
phase gate after the sub-repo commits — not before.

Purpose: three of this phase's requirements are discharged only at the end.
SWEEP-05's "after" half needs the whole sweep in place. SWEEP-10's retarget count is
unknowable until the diff exists — CONTEXT.md names it as a *deliverable*, not a
prediction, and as the only manual work in the whole repair. And SWEEP-13's commit
granularity and ordering is not "commit eventually": research measured **9** modules
asserting git porcelain, not the 1 D-11 cites, four of them in the app repo asserting
on the *firmware* repo's cleanliness, so firmware-commit then app-commit then host
suite is a hard sequence.

Output: `sweep-outcome-record.md`, `CITATIONS-STALE.md`, the manifest's retarget rows,
the three commits, and the phase gate results.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md
@.planning/v1.33/baseline-pre-sweep.md
@.planning/v1.33/sweep-corpus-baseline.md
@.planning/v1.33/sweep-gate-dispositions.md
@.planning/v1.33/sweep-citation-manifest-report.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces `.planning/v1.33/CITATIONS-STALE.md` (the SWEEP-12 staleness marker) and
`.planning/v1.33/sweep-outcome-record.md`, and updates
`.planning/v1.33/sweep-citation-manifest.jsonl` with the `retarget: true` subset.

<tasks>

<task type="auto">
  <name>Task 1: Record the post-sweep outcome — byte-identity after-pair, the actual swept set, the survey re-run, and the SWEEP-13 archived-milestones record (SWEEP-05, SWEEP-13, Rulings D, E, G)</name>
  <files>.planning/v1.33/sweep-outcome-record.md</files>
  <read_first>
    - `.planning/v1.33/baseline-pre-sweep.md` — every pre-sweep number to compare against, and the recorded git anchors
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md` §`R6 — Two Known-Hazard Confirmations` (lines 1285-1326) — the archived-milestones finding and the record-gate runtime update
    - `.planning/v1.33/sweep-gate-dispositions.md` — the 22-gate exposure table, for the follow-on note
    - The SUMMARY of each of plans 06 through 11 — the per-file results and every recorded abstention
  </read_first>
  <action>
Create `.planning/v1.33/sweep-outcome-record.md` with, in this order:

1. **The SWEEP-05 after-pair.** Cold-build all three AVR targets and record `sha256sum` of the
   `.elf` and the `.hex` plus the `Flash:`/`RAM:` figures, side by side with plan 01's pre-sweep
   values. Per Ruling E, record the hash pair (strictly stronger, and proven comment-immune) **and**
   the size pair, so SWEEP-05's literal "measured pair of numbers" is satisfied and strengthened
   rather than substituted. State the artifact name `firestarter_uno.elf` explicitly, because
   `name_firmware.py` rewrites `PROGNAME` and any oracle hard-coding `firmware.elf` silently
   measures nothing. SWEEP-05's rule if a hash differs is explicit: **reverted, not explained** —
   bisect by file, revert, re-measure, and report.

2. **The actual swept set** versus the pre-sweep candidate set. SWEEP-12 requires the marker to
   name the swept files, and research established the candidate set (files under the globs with at
   least one hit) is a strict superset of the actual swept set because D-02's exemption and D-01's
   delete-the-whole-comment outcomes can leave a candidate untouched. Produce the actual set from
   `git -C firestarter diff --name-only <FW_PRE_SHA>` and
   `git -C firestarter_app diff --name-only <APP_PRE_SHA>`. Report both sizes and the difference —
   research calls that difference itself a reportable number.

3. **The post-sweep survey re-run**, and the SWEEP-03 assertion it exists to make:
   `survey_provenance.py --assert-tokens-zero D-# --group fw-src --group fw-include` must now exit
   **0**, where plan 02 proved it exited 1 before the sweep. That before/after pair is what makes
   the green evidence rather than an assertion. Also report the test-file groups' ID counts as
   unchanged, which is D-03's retention side.

4. **The per-group residual, fully attributed.** For every group, every remaining hit must be
   classified as: a D-02-exempt `CAP-0` line, a retained ID in a test file (D-03), one of the four
   Ruling B exempted files, or a recorded narrow-treatment abstention. Collect the abstention lists
   from plans 08 and 11. A residual left unattributed is a defect in this record, not a rounding
   error.

5. **The Ruling D follow-on note.** Restate that SWEEP-06's requirement text was not expanded, that
   the 22 non-comment-stripping firmware-repo gates are recorded as a named exposure rather than
   controlled, and that "22 firmware-repo gates need planted controls" is filed as a follow-on
   phase. Cross-check plan 02's overlap column against the actual swept set produced in item 2 — a
   gate recorded `no-overlap` against the *candidate* set must be re-checked against the *actual*
   set, and any that turns out to overlap is upgraded to `control` or `EXPOSURE` here.

6. **The SWEEP-13 archived-`milestones/` record, discharged by recording.** Research §R6 established
   this phase edits **nothing** under `.planning/milestones/`: the remap is deferred to Phase 159
   per D-01 and D-10, and this phase's only `.planning/` writes are new files under
   `.planning/v1.33/` plus the marker. The manifest *records* 1,302 citations that live in
   `milestones/`, but recording is a read. So the honest record is the collision's **absence, with
   cause**: "No archived record was edited — the citation repair is deferred to Phase 159 per D-01,
   so the archived-record hazard belongs to REMAP-01, not to this phase." Verify the claim rather
   than restating it: `git -C /workspaces diff --name-only <meta pre-sweep sha> -- .planning/milestones`
   must be empty. Carry the hazard forward as a note on REMAP-01, where 1,302 `milestones/`
   citations do get rewritten.

7. **A correction to the inherited record-gate timeout folklore.** Research measured `STATE.md`'s
   longest line at 2,965 characters (the remembered 52k-character line is gone) and found no
   `.planning`-level record-gate script — the `check-claims` scripts are phase-scoped under
   individual phase directories, and this phase authors no phase record an existing gate would
   scan. So the 300 s guidance does not apply to anything this phase runs; the slowest measured leg
   is the 235 s host suite, and 600 s is the timeout sized by measurement. Record this so the next
   phase does not inherit a stale number.
  </action>
  <verify>
    <automated>cd firestarter && for e in uno uno328pb leonardo; do rm -rf .pio/build/$e && pio run -e $e || exit 1; sha256sum .pio/build/$e/firestarter_$e.elf; done && cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --assert-tokens-zero D-# --group fw-src --group fw-include && test -f .planning/v1.33/sweep-outcome-record.md && for t in firestarter_uno.elf REMAP-01 'milestones' 600; do grep -q "$t" .planning/v1.33/sweep-outcome-record.md || { echo "MISSING $t"; exit 1; }; done && test -z "$(git -C /workspaces diff --name-only -- .planning/milestones)"</automated>
  </verify>
  <acceptance_criteria>
    - For each of `uno`, `uno328pb`, `leonardo`: the post-sweep `.elf` sha256 equals plan 01's pre-sweep value, character for character, and the `.hex` sha256 and `Flash:`/`RAM:` figures match too. All values appear side by side in the record.
    - `survey_provenance.py --assert-tokens-zero D-# --group fw-src --group fw-include` exits **0**, and the record states that plan 02 proved the same invocation exited 1 before the sweep.
    - The record contains the actual swept set (from `git diff --name-only` against `FW_PRE_SHA` and `APP_PRE_SHA`), its size, the candidate-set size, and the difference.
    - Every group's residual hits are individually attributed to one of the four permitted classes; the record contains no unattributed residual.
    - `git -C /workspaces diff --name-only -- .planning/milestones` is empty, and the record states the SWEEP-13 archived-record finding as an absence with its cause and carries it forward as a REMAP-01 note.
    - The record contains the literal `600` as the measurement-sized timeout and the literal `2,965` as the corrected `STATE.md` longest-line figure.
    - The Ruling D overlap column is re-checked against the actual swept set, and any upgraded disposition is named.
  </acceptance_criteria>
  <done>Every post-sweep measurement is recorded beside its pre-sweep value, the SWEEP-03 before/after pair makes the green meaningful, every residual hit is attributed, and the SWEEP-13 archived-record clause is discharged by a verified absence with cause.</done>
</task>

<task type="auto">
  <name>Task 2: Settle the D-08 retarget subset against the actual diff and report its count (SWEEP-10, D-08)</name>
  <files>.planning/v1.33/sweep-citation-manifest.jsonl</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<decisions>` D-08 — the retarget rule and the statement that its size is a deliverable
    - `.planning/v1.33/sweep-citation-manifest-report.md` — the pre-sweep row counts and the per-resolution breakdown
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md` §`The three cases the requirement names` (lines 283-319) — why a `retarget` record's new target is hand-chosen and why `replace` is non-surviving
    - `.planning/v1.33/tools/remap_citations.py` — the `map_point`/`map_range` clamp that produces the retarget flag
  </read_first>
  <action>
Compute the retarget subset: for every manifest record whose resolved target file is in the actual
swept set, build the old-to-new line map with `remap_citations.py`'s map builder in **dry-run**
mode and identify the records whose target line did not survive verbatim. Both `delete` and
`replace` count as non-surviving — a reflowed comment is a `replace`, so its recorded `source_text`
can no longer match at the destination and the record cannot round-trip. Mapping such a record
positionally would manufacture false green, which is why it is flagged rather than renumbered.

For each identified record: set `retarget: true`, **preserve the original cited text** in
`source_text` unchanged, and set the new target to the first surviving code line the deleted
comment described. D-08 is explicit that this new target is **hand-chosen** — the tool's clamp is a
starting suggestion for the human, not the answer, and it is the only manual work in the whole
repair. Record the chosen target and a one-line reason per record.

**None is silently dropped.** A record that cannot be retargeted at all — because the comment it
cited described nothing that survives — keeps `retarget: true` with a null new target and an
explicit recorded reason, so Phase 159's oracle can skip it **by name** rather than fail open on it.

Then **report the count** in `sweep-outcome-record.md`: the number of `retarget: true` rows, broken
down by group and by cause (deleted comment versus reflowed comment), and the number with a null
new target. SWEEP-10 requires the count as a deliverable; CONTEXT.md's `<decisions>` section lists
this and the per-file keep/delete ratio as the two things that were genuinely unknown until the diff
existed. Report the per-file keep/delete ratio too, since it is the sibling deliverable.

Write the updated manifest atomically (temp plus rename) and re-run the generator's schema
self-check afterwards so a hand edit cannot leave the file invalid.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c "
import json
rows=[json.loads(l) for l in open('.planning/v1.33/sweep-citation-manifest.jsonl') if l.strip()]
hdr, recs = rows[0], rows[1:]
rt=[r for r in recs if r.get('retarget') is True]
print('total', len(recs), 'retarget', len(rt))
assert all('source_text' in r and r['source_text'] is not None for r in rt), 'retarget row lost its original cited text'
nulls=[r for r in rt if r.get('target_line') is None]
assert all(r.get('retarget_reason') for r in nulls), 'null-target retarget row without a recorded reason'
" && grep -qE 'retarget' .planning/v1.33/sweep-outcome-record.md && grep -qE 'keep/delete|keep-delete' .planning/v1.33/sweep-outcome-record.md</automated>
  </verify>
  <acceptance_criteria>
    - Every line of the updated manifest still parses as JSON and the generator's schema self-check passes on it.
    - Every `retarget: true` row retains a non-null `source_text` equal to its pre-sweep value — assert by comparing against the pre-update manifest captured at task start.
    - Every `retarget: true` row with a null new target carries a non-empty `retarget_reason`.
    - `sweep-outcome-record.md` reports the `retarget: true` **count**, broken down by group and by cause (deleted versus reflowed), and the null-target count.
    - `sweep-outcome-record.md` reports the per-file keep/delete ratio.
    - No manifest row was removed: the total row count of the updated manifest equals the pre-update count.
    - Manual-only, named here rather than laundered into an automated leg: whether each hand-chosen new target is the *right* first surviving code line is a per-citation judgment (VALIDATION.md lists this as one of the six manual-only items). The reviewable artifact is the `retarget: true` row set with its per-row reason, and the record must present it as a table, not only as a count.
  </acceptance_criteria>
  <done>The retarget subset is settled against the real diff with original texts preserved and per-row reasons recorded, nothing is dropped, and both post-diff deliverables — the retarget count and the keep/delete ratio — are reported.</done>
</task>

<task type="auto">
  <name>Task 3: Plant the staleness marker (SWEEP-12, D-01, D-10)</name>
  <files>.planning/v1.33/CITATIONS-STALE.md</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<requirements>` SWEEP-12, and §`<decisions>` D-01 and D-10 for why the staleness window exists at all
    - `.planning/REQUIREMENTS.md` §6 — REMAP-04's exact wording: the marker's removal is close-blocking
    - `.planning/v1.33/sweep-outcome-record.md` (task 1) — the actual swept set to name
    - `.planning/notes/firmware-size-reduction-survey.md` — the closest shape precedent: a committed note another phase consumes
  </read_first>
  <action>
Create `.planning/v1.33/CITATIONS-STALE.md`. It is the interface to REMAP-04's close block, and it
is what turns D-01's deliberate staleness from a promise into a structural guarantee. No *marker*
precedent exists in this project — `.planning/notes/*.md` are design notes and the phase-scoped
`check-claims` scripts are gates — so the shape is new; keep it short and unambiguous.

It must state, each as its own clearly headed section:

1. **What is stale.** That `.planning/` citations targeting the files named below are knowingly
   stale as of this phase, by deliberate decision (D-01), not by oversight.
2. **Which files.** The **actual** swept set from task 1, listed in full — SWEEP-12 requires the
   marker to name the swept files. Include the candidate-set size and the actual-set size so a
   reader can see the difference is intentional.
3. **How many citations.** The manifest's row count and the shifting subset count, and a pointer to
   `.planning/v1.33/sweep-citation-manifest.jsonl` as the record and to
   `.planning/v1.33/sweep-citation-manifest-report.md` for the reconciliation.
4. **Who closes it, and that the close is blocking.** Phase 159 / **REMAP-04** — the marker is
   removed there and its removal is close-blocking, so the milestone cannot close while this file
   exists. Name the literal `REMAP-04` and the literal `Phase 159`. State why the window is safe:
   D-10 records that composing four successive mappings would create a range-shrinking hazard that
   one composite mapping avoids, and that 723 citations would otherwise be remapped twice with 41%
   of that rework caused by four added `#include` lines.
5. **What the closer needs.** The manifest, the remap tool at
   `.planning/v1.33/tools/remap_citations.py`, and the `retarget: true` row set that Phase 159's
   oracle must **skip by name** rather than round-trip.
6. **The known hazard being handed forward.** Research §R6: this phase edited nothing under
   `.planning/milestones/`, but Phase 159 rewrites 1,302 citations that live there, so the
   archived-record gate hazard belongs to REMAP-01.

Do not add a mechanical close-block here — REMAP-04 owns that. VALIDATION.md specifies the check
against this file as the literal `REMAP-04` plus at least one swept path, and that is deliberately
weak because the enforcement lives downstream.
  </action>
  <verify>
    <automated>cd /workspaces && test -f .planning/v1.33/CITATIONS-STALE.md && for t in REMAP-04 'Phase 159' 'remap_citations.py' 'sweep-citation-manifest.jsonl' 'retarget'; do grep -q "$t" .planning/v1.33/CITATIONS-STALE.md || { echo "MISSING $t"; exit 1; }; done && test "$(grep -cE '(firestarter|firestarter_app)/(src|include|test|tests|tools|firestarter)/' .planning/v1.33/CITATIONS-STALE.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/CITATIONS-STALE.md` exists and contains the literals `REMAP-04`, `Phase 159`, `remap_citations.py`, `sweep-citation-manifest.jsonl` and `retarget`.
    - It lists at least one swept repo-relative source path, and the count of listed paths equals the actual swept-set size recorded in `sweep-outcome-record.md`.
    - It states the manifest row count and the shifting-subset count, matching the figures in `sweep-citation-manifest-report.md` and `sweep-outcome-record.md`.
    - It states that the marker's removal is close-blocking and that the milestone cannot close while the file exists.
    - It carries the REMAP-01 archived-record hazard forward with the `1,302` figure.
    - It contains all six headed sections listed in the action.
  </acceptance_criteria>
  <done>The staleness marker exists, names the actual swept files and the citation counts, and points at Phase 159 / REMAP-04 as the close-blocking closer, with the downstream hazard handed forward.</done>
</task>

<task type="auto">
  <name>Task 4: Produce the three commits in D-11 order and run the phase gate AFTER both sub-repo commits (SWEEP-13, SWEEP-05, SWEEP-07, D-11)</name>
  <files>firestarter/ (one commit), firestarter_app/ (one commit), .planning/v1.33/** (one meta commit)</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<decisions>` D-11 — the commit granularity and the ordering constraint
    - `.planning/v1.33/baseline-pre-sweep.md` — `FW_PRE_SHA` and `APP_PRE_SHA`, the anchors every commit-count criterion is measured against
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md` §`Pitfall 7` (lines 950-956) and §`F7` (line 132) — the 9 porcelain-asserting modules
    - `firestarter/tests/test_flash_path_record_sync.py` line 1247 — the whole-repo porcelain assertion on the firmware repo
  </read_first>
  <action>
Produce exactly three commits, in this order, and measure each against a **pre-task sha captured
at task start** rather than against `HEAD~1` — `git rev-list --count HEAD ^HEAD~1` is a tautology
that always prints 1 and proves nothing.

**Step 1 — the firmware commit.** Capture `FW_TIP_NOW = git -C firestarter rev-parse HEAD` for the
reflog record. If plans 06-08 left the tree uncommitted, stage the phase's firmware paths
explicitly (`src`, `include`, `test`, `tests/golden`) — never `git add -A` — and commit once. If
those plans' executors already committed, squash the range non-destructively:
`git -C firestarter reset --soft $FW_PRE_SHA` then one `git commit`. `reset --soft` preserves all
content in the index, so no work can be lost, and `FW_TIP_NOW` plus the reflog is the recovery
path. Record which route was taken. Assert afterwards:
`git -C firestarter rev-list --count $FW_PRE_SHA..HEAD` == 1 and
`git -C firestarter status --porcelain` is empty.

**Step 2 — the app commit.** Same shape with `APP_PRE_SHA`, staging `firestarter`, `tests` and
`tools` explicitly. **Never `git add -A` or `git add .` here**: plan 02 recorded that the app repo
carries three pre-existing untracked items (`SECURITY.md`, `datasheets/*.pdf`,
`.planning/config.json`) that are not this phase's work and must not enter the commit. Assert
`git -C firestarter_app rev-list --count $APP_PRE_SHA..HEAD` == 1, and assert those three untracked
items are **still untracked** afterwards.

**Step 3 — the phase gate, and only now.** With both sub-repo commits landed, run:
  - `pytest tests/` from `firestarter` — all four F3 blob-sha gates must now be green, because their
    legs read `git rev-parse HEAD:<path>` and the sweep commit has landed. Plan 07 recorded them as
    expected-RED pre-commit; this is where that expectation is discharged.
  - `pio test -e native` and the `native_nodevtools` leg — those two plus `pytest tests/` are the
    firmware CI legs, and they are the only three.
  - The full host suite:
    `FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/ -o addopts="" -q`
    from `firestarter_app`, with `-o addopts=""` because the project's `addopts = "-ra -q"` plus a
    command-line `-q` suppresses the count line. Allow 600 s.
  - The five SWEEP-07 legs individually with `-k` selectors — this is SWEEP-07's **RED-after** half,
    and it must be compared leg by leg against plan 03's recorded before-results.
  - The byte-identity after-pair for all three AVR targets, already recorded in task 1, restated in
    the commit message body for `firestarter`.

**Step 4 — the meta commit.** Commit `.planning/v1.33/` once, carrying the tools, the manifest, the
reports, the baseline, the dispositions and the marker. Plans 02, 04 and 05 were instructed to
leave these uncommitted precisely so this is one commit. Measure the criterion path-scoped:
`git -C /workspaces log --oneline <meta pre-sweep sha>..HEAD -- .planning/v1.33 | wc -l` == 1. GSD's
own `docs(154):` PLAN/SUMMARY commits are bookkeeping and are excluded by the path scope, which is
why the criterion is scoped rather than a bare commit count. If an earlier plan's executor
committed `.planning/v1.33/` files anyway, do **not** rewrite meta history — it contains GSD doc
commits — and instead record the actual count with its cause. SWEEP-13 requires the outcome recorded
either way, and a non-destructive honest record beats a history rewrite in a chained run.

Commit message subjects: `refactor(154): strip planning provenance from firmware source` for
`firestarter`, `refactor(154): strip planning provenance from host source` for `firestarter_app`,
and `docs(154): citation manifest, remap tool and staleness marker` for the meta repo. Each body
must name the requirements it discharges and, for the firmware one, carry the three-target hash and
size pairs.
  </action>
  <verify>
    <automated>cd /workspaces && test -z "$(git -C firestarter status --porcelain)" && test -z "$(git -C firestarter_app status --porcelain --untracked-files=no)" && cd firestarter && pytest tests/ && pio test -e native && cd /workspaces/firestarter_app && FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/ -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter rev-list --count <FW_PRE_SHA>..HEAD` == 1, with `FW_PRE_SHA` read from `.planning/v1.33/baseline-pre-sweep.md` — never `HEAD~1`.
    - `git -C firestarter_app rev-list --count <APP_PRE_SHA>..HEAD` == 1, same anchor rule.
    - `git -C firestarter status --porcelain` is empty; `git -C firestarter_app status --porcelain --untracked-files=no` is empty, and the three pre-existing untracked app items are still untracked (`git -C firestarter_app status --porcelain` still lists them as `??`).
    - Both sub-repo commits are timestamped **before** the full host suite run — evidenced by the SUMMARY recording the commit shas and then the suite output, in that order.
    - `pytest tests/` from `firestarter` exits 0, including all four F3 blob-sha modules (`test_eprom_params_citations.py`, `test_protocol_branch_inventory.py`, `test_golden_trace_identity.py`, `test_golden_trace_identity_eprom_v131.py`).
    - `pio test -e native` and the `native_nodevtools` leg both pass at the plan 01 baseline counts.
    - The full host suite exits 0 with a count line at least as high as plan 01's baseline; every failure, if any, is named and attributed.
    - All five SWEEP-07 legs pass individually and match plan 03's recorded before-results in direction — this is the RED-after half.
    - `git -C /workspaces log --oneline <meta pre-sweep sha>..HEAD -- .planning/v1.33 | wc -l` == 1, or the actual count is recorded with its cause and no meta history was rewritten.
    - The firmware commit body contains the three-target `.elf` sha256 values.
    - If the squash route was used in either sub-repo, the pre-squash tip sha is recorded so the reflog is a usable recovery path.
  </acceptance_criteria>
  <done>Exactly one source commit per sub-repo, both landed before the full host suite, all four blob-sha gates green, all three firmware CI legs and the host suite at or above baseline, the five SWEEP-07 controls re-proven, and the meta commit count recorded honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uncommitted working tree → the 9 porcelain-asserting gate modules | crossing it in the wrong order produces 7+ false failures |
| `git add` glob → the commit contents | three pre-existing untracked app items must not enter the commit |
| this phase's marker → REMAP-04's close block | the only structural guarantee that the staleness window closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-47 | Repudiation | running the full host suite before the sub-repo commits land, producing 7+ porcelain failures that mask a real regression | high | mitigate | Strict step order in task 4; the criterion requires the commit shas to be recorded before the suite output; 9 porcelain-asserting modules named. |
| T-154-48 | Denial of Service | `git reset --soft` losing work in the squash route | high | mitigate | `reset --soft` preserves the index by construction; the pre-squash tip sha is captured and recorded so the reflog is a usable recovery path; no `reset --hard` anywhere. |
| T-154-49 | Tampering | `git add -A` sweeping three unrelated untracked app items into the SWEEP-13 commit | medium | mitigate | Path-scoped staging only; the three items asserted still untracked after the commit. |
| T-154-50 | Repudiation | a commit-count criterion anchored to `HEAD~1`, which is a tautology that always reads 1 | medium | mitigate | Both criteria anchored to `FW_PRE_SHA` / `APP_PRE_SHA` recorded in plan 01. |
| T-154-51 | Repudiation | the staleness window leaking past the milestone because the marker is missing or unfindable | high | mitigate | Marker planted with the literal `REMAP-04`, the literal `Phase 159`, the full swept-file list and the citation counts; REMAP-04 owns the mechanical close block. |
| T-154-52 | Repudiation | a retarget row silently dropped, leaving Phase 159's oracle to fail open on it | high | mitigate | Row count asserted unchanged; every `retarget: true` row keeps its original `source_text`; null-target rows carry a recorded reason so the oracle can skip them by name. |
| T-154-53 | Tampering | rewriting meta history to force a single commit, entangling GSD's own doc commits | medium | accept | Explicitly declined: the criterion is path-scoped to `.planning/v1.33`, and a deviation is recorded with its cause rather than rewritten. SWEEP-13 asks for the outcome recorded either way. |
</threat_model>

<verification>
- Three AVR targets' post-sweep hash and size pairs equal plan 01's pre-sweep values.
- `--assert-tokens-zero D-#` over shipped firmware source exits 0 where plan 02 proved it exited 1.
- The retarget subset is flagged, texted, reasoned and counted; the manifest row count is unchanged.
- `CITATIONS-STALE.md` carries `REMAP-04`, `Phase 159`, the swept-file list and the counts.
- One commit per sub-repo anchored to the plan 01 shas; both landed before the host suite.
- All three firmware CI legs and the full host suite at or above baseline; four F3 gates green; five SWEEP-07 legs re-proven.
</verification>

<success_criteria>
- SWEEP-05 is discharged as a measured pair, strengthened to a hash pair, on three targets.
- SWEEP-07's RED-after half is proven leg by leg against the recorded before-results.
- SWEEP-10's retarget count and the per-file keep/delete ratio — the phase's two genuinely-unknown deliverables — are reported.
- SWEEP-12's marker exists and points at its close-blocking closer.
- SWEEP-13's commit granularity and ordering are mechanically proven, and the archived-milestones clause is discharged by a verified absence with cause.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-12-SUMMARY.md` when done.
</output>
