---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 11
type: execute
wave: 4
depends_on: ["154-02", "154-03", "154-09", "154-10"]
files_modified:
  # All hit-carrying files under firestarter_app/tests, including the two gate
  # modules plan 03 added legs to. Worklist authority:
  # survey_provenance.py --group app-tests --file-table.
  - firestarter_app/tests/**
autonomous: true
requirements: [SWEEP-03, SWEEP-04]
must_haves:
  truths:
    - "firestarter_app/tests receives the NARROW treatment only: tombstone deletion, label-only-comment deletion, and narrative-prefix stripping where a sentence follows."
    - "Requirement and decision IDs are RETAINED throughout, because in a test file the ID is the case's traceability key."
    - "scan_paths.py is kept in full — it carries zero regex hits, and that is recorded as the reason rather than skipped silently."
    - "The five SWEEP-07 legs plan 03 added still behave as specified after this group is swept."
  artifacts: []
  key_links:
    - "This is the last group swept and it touches the two modules plan 03 edited, which is why it is a later wave than plan 03 rather than a sibling."
    - "Wave 4 rather than wave 3 is load-bearing and is why 154-09 and 154-10 are declared dependencies: this group sweeps the gate modules under firestarter_app/tests that scan the very source 09 and 10 are sweeping. Run concurrently, a gate re-run here could observe a half-swept firestarter_app/firestarter or tools tree and report a state neither plan produced. The dependency is on observation order, not on a shared file path — 09, 10 and 11 write disjoint paths."
    - "scan_paths.py's docstring is the only written statement of the firestarter name-collision trap that the remap tool's path handling was checked against. It carries no regex hit, so keeping it in full requires no edit at all."
---

<objective>
Sweep `firestarter_app/tests` — the last group — under D-04's narrow treatment only.

Purpose: this group completes D-04's 331 test-file hits and it is deliberately last,
because it touches `test_sdp_table_parity.py` and `test_dispatch_mirror.py`, the two
modules plan 03 added the SWEEP-07 controls to. Sweeping them in the same wave as plan
03 would put two plans on the same files; sweeping them after means the controls exist,
are proven, and can be re-run against the swept text.

It is also where D-04's named keep-in-full case lives, and the honest answer there is
that it needs no edit: `scan_paths.py` carries **zero** regex hits, because the survey
regex requires a provenance token immediately after a comment opener and that file's
labels sit inside its module docstring. Plan 02 recorded that with its measured size
and cause. "Keep in full" is therefore satisfied by not editing the file, and that is
recorded rather than skipped silently.

Output: `firestarter_app/tests` swept narrowly, and the five SWEEP-07 legs re-run.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md
@.planning/v1.33/sweep-corpus-baseline.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces no new artifact; it modifies existing files under `firestarter_app/tests` only.

<tasks>

<task type="auto">
  <name>Task 1: Narrow sweep of firestarter_app/tests, IDs retained (SWEEP-03, SWEEP-04, D-01, D-03, D-04)</name>
  <files>firestarter_app/tests/** — every hit-carrying file per `survey_provenance.py --group app-tests --file-table`, EXCLUDING tests/scan_paths.py (measured zero hits, keep in full) and the four plan-03 fixtures</files>
  <read_first>
    - `.planning/v1.33/sweep-corpus-baseline.md` — D-04's narrow-treatment definition, D-03's retain-in-tests rule, the `app-tests` per-file hit table, and the `CORPUS DEFINITION` exclusion that explains why `scan_paths.py` reads zero
    - `firestarter_app/tests/test_submit.py` (13 hits), `tests/test_chip_test.py` (12 hits), `tests/test_eprom_operations.py` (8 hits), `tests/conftest.py` (8 hits), `tests/test_dev_test_cmd.py` (7 hits)
    - `firestarter_app/tests/scan_paths.py` — read the module docstring in full to confirm it carries no regex hit and to confirm the name-collision statement is intact
    - `firestarter_app/tests/test_dispatch_mirror.py` — the single hit near line 53
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md` §`firestarter_app/tests/scan_paths.py` (lines 596-613)
  </read_first>
  <action>
Sweep every hit-carrying file under `firestarter_app/tests` using
`survey_provenance.py --group app-tests --file-table` as the worklist, with **narrow treatment
only** — exactly the same three operations as plan 08 and no others:

1. Tombstone deletion — a comment describing code that is not there is deleted whole.
2. Label-only-comment deletion — a comment that, after removing D-01's tokens and their
   enclosing punctuation, contains nothing but connective punctuation is deleted whole.
3. Narrative-prefix stripping where a sentence follows — remove the `Phase N` / `Plan N` /
   `Task N` / `PNNN` / `<NNN>-CONTEXT.md` prefix and leave the following sentence as it stands.

**No reflowing, rewording or condensing of substantive test commentary.** D-04 forbids it because
no oracle covers this group. Where a hit's enclosing comment cannot be handled by one of the three
operations without rewriting prose, abstain and record the file and line in the SUMMARY. An
abstention is a correct outcome.

**Requirement and decision IDs are RETAINED (D-03)** — in a test file the ID is the traceability
key REQUIREMENTS.md runs on, and deleting it would sever an edge no gate would notice.

**`scan_paths.py`: keep in full by not editing it.** Confirm by running the survey tool restricted
to that path that it carries zero regex hits, then record in the SUMMARY that D-04's named
keep-in-full case is discharged by a measured zero rather than by a judgment call, and that
PATTERNS.md's suggested reword of that file is therefore deliberately **not performed** — the
labels it names sit inside the module docstring, which `sweep-corpus-baseline.md`'s `CORPUS
DEFINITION` section places outside the corpus with its measured size and cause. State that the
name-collision statement (one `..` from `tools/` lands in the app's own Python package, two reach
the sibling firmware repo) is intact and unaltered, since that is the sentence the remap tool's
path handling was checked against.

**Do not sweep the four fixtures plan 03 created.** They were authored keyed on `SWEEP-07` and
carry no provenance. Do sweep pre-existing fixtures that carry hits — plan 02's per-file table
lists them, for example the planted JSON-parser fixture with 4 hits — under the same narrow
treatment, and note that a fixture header naming the gate and the seam is substantive, so
label-only deletion applies to the phase/plan prefix and not to the rest.

`test_dispatch_mirror.py`'s single hit near line 53 describes a restructuring of a bucket table.
Judge it under step 2: if it describes the table as it exists, strip the prefix and keep; if it
describes the pre-restructuring shape, it is a tombstone.

Touch no code line and no docstring line.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group app-tests && test "$(git -C firestarter_app diff -U0 -- tests | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|//|\*|/\*|$)')" -eq 0 && git -C firestarter_app diff --quiet -- tests/scan_paths.py</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group app-tests --json` residual is fully attributed in the SUMMARY: every remaining hit is a retained ID (D-03), a D-02-exempt `CAP-0` line (research measured 4 in this group), or a listed abstention.
    - `git -C firestarter_app diff --quiet -- tests/scan_paths.py` exits 0 — the named keep-in-full case is untouched, and the SUMMARY records that the reason is a measured zero regex hits.
    - Every added and removed diff line under `tests` is a comment line or blank: the filtered `git diff -U0` count == 0. The filter admits `#`, `//`, `*` and `/*` openers because this group contains both Python modules and `.c`/`.cpp` fixtures.
    - Narrow-treatment discipline, measured: `git -C firestarter_app diff --numstat -- tests` insertions do not exceed deletions per file; any file violating this is named with the reason.
    - Requirement/decision IDs retained: the count of `D-` ID occurrences under `tests` after the sweep is unchanged from the pre-task count captured at task start.
    - The four plan-03 fixtures are untouched: `git -C firestarter_app diff --quiet -- tests/fixtures/planted_sdp_comment_misanchor.cpp tests/fixtures/planted_sdp_comment_brace.cpp tests/fixtures/planted_dispatch_comment_only_hex.cpp tests/fixtures/planted_dispatch_missing_hex.cpp` exits 0.
    - Source assertion: zero docstring lines modified — `git -C firestarter_app diff -U0 -- tests | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -cE '^[+-][[:space:]]*"""'` == 0.
    - Manual-only, named here rather than laundered into an automated leg: whether each comment was correctly classified tombstone versus substantive, in a group with no covering oracle. The reviewable artifact is `git -C firestarter_app diff -- tests`, and the SUMMARY must list every abstention and every whole-comment deletion with its classification.
  </acceptance_criteria>
  <done>The app-tests group is swept narrowly with IDs retained, `scan_paths.py` is untouched with the measured reason recorded, plan 03's fixtures are untouched, and no code or docstring line changed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the five SWEEP-07 legs and the targeted host gates against the swept text (SWEEP-07 continuity, SWEEP-04)</name>
  <files>(verification only — no file is modified)</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-03-SUMMARY.md` — the recorded RED-before / GREEN-before results for all five legs
    - `.planning/v1.33/baseline-pre-sweep.md` — the pre-sweep host suite baseline and the venv path
    - `firestarter_app/tests/test_sdp_table_parity.py` and `firestarter_app/tests/test_dispatch_mirror.py` — as swept
  </read_first>
  <action>
This plan is the only one that edits the two modules carrying the SWEEP-07 legs, so re-run all
five and compare against plan 03's recorded before-results, leg by leg:
`pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q`. All eight
sdp legs and the whole dispatch-mirror module must be green, which means: the two planted-comment
sdp controls still detect their plants, the anchoring leg still finds the slice inside the real
declaration's span, the dispatch RED control still detects its plant, and the dispatch fail-open
control is still GREEN.

Then run the targeted host gates for every module this plan touched. For each, record the result
per module rather than as an aggregate, so a regression is attributable to a file.

Do **not** run the full 235 s host suite here. It belongs at plan 12's phase gate, after both
sub-repo commits land: research measured **9** modules asserting git porcelain — 5 in the firmware
repo including `test_flash_path_record_sync.py` at line 1247, and 4 in the app repo asserting on
the *firmware* repo via `_git_porcelain(FW_ROOT)` — and every one of them is RED against
uncommitted edits. Running the suite now would produce 7-plus failures caused by the sweep's own
dirt and would mask a real regression. Record this reasoning in the SUMMARY so plan 12's ordering
reads as a requirement rather than a preference.

Do not commit. The app repo takes exactly one commit for this phase, created in plan 12.
  </action>
  <verify>
    <automated>cd firestarter_app && FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py -o addopts="" -q` exits 0, and the count line reports the same totals as plan 03's recorded post-addition run.
    - Each of the five SWEEP-07 legs is reported individually as pass/fail with `-k` selectors, and each matches plan 03's recorded before-result in direction: the four RED-detecting legs pass, and the fail-open leg passes while asserting GREEN.
    - Targeted gates for every module this plan touched are recorded per module, not as an aggregate.
    - The SUMMARY states that the full host suite was deliberately not run, names the 9 porcelain-asserting modules as the reason, and points at plan 12's phase gate.
    - The SUMMARY records the `app-tests` group's post-sweep hit count from `survey_provenance.py` and confirms the D-04 total across both test groups: the pre-sweep 331 and the post-sweep residual, with the residual attributed.
  </acceptance_criteria>
  <done>All five SWEEP-07 legs behave as recorded before the sweep, per-module targeted gates are recorded, and the full-suite deferral is justified in writing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-file comment text → REQUIREMENTS.md traceability | an ID in a test file is a traceability edge |
| swept gate module → the SWEEP-07 controls living in it | this is the only plan that edits the modules carrying the controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-42 | Tampering | sweeping the two gate modules and disturbing the SWEEP-07 legs added in plan 03 | high | mitigate | Later wave than plan 03 so the controls exist and are proven; all five legs re-run leg by leg and compared against plan 03's recorded before-results. |
| T-154-43 | Denial of Service (of future correctness) | stripping a requirement ID from a test case and severing traceability no gate would notice | high | mitigate | D-03 retention; pre/post `D-` occurrence count asserted unchanged under `tests`. |
| T-154-44 | Repudiation | 115 hit lines with no covering oracle | high | mitigate | Narrow treatment only; abstention permitted and listed; insertions-no-greater-than-deletions as a mechanical proxy for "only removed or shortened"; per-module gate results recorded. |
| T-154-45 | Repudiation | editing `scan_paths.py` on the strength of PATTERNS.md's suggested reword and silently expanding the corpus into docstrings | medium | mitigate | `git diff --quiet` on that path; the measured-zero reason recorded; the `CORPUS DEFINITION` exclusion cited. |
| T-154-46 | Repudiation | running the full host suite before the commits land and masking a real regression behind 7-plus porcelain failures | medium | mitigate | Deferral to plan 12's phase gate, with the 9 porcelain-asserting modules named as the cause in the SUMMARY. |
</threat_model>

<verification>
- `survey_provenance.py --group app-tests` residual fully attributed.
- `git diff --quiet` on `tests/scan_paths.py` and on the four plan-03 fixtures.
- Every added/removed diff line under `tests` is a comment or blank; zero docstring lines; insertions do not exceed deletions.
- `D-` occurrence count under `tests` unchanged.
- All five SWEEP-07 legs green, reported individually.
</verification>

<success_criteria>
- D-04's 331 test-file hits are fully accounted for across plans 08 and 11.
- The named keep-in-full case is kept in full, with the measured reason recorded rather than skipped.
- The SWEEP-07 controls survive the sweep of the modules that host them.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-11-SUMMARY.md` when done.
</output>
