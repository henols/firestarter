---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.33/baseline-pre-sweep.md
autonomous: true
requirements: [SWEEP-05, SWEEP-13]
must_haves:
  truths:
    - "The 11 modified firestarter files are recoverable from a named committed ref, verified byte-identical to .planning/notes/firmware-size-reduction-measured.patch, BEFORE the working tree stops being dirty."
    - "Both sub-repos are on a gsd/v1.33-* branch forked from beta, with clean working trees."
    - "A pre-sweep byte-identity record exists for uno, uno328pb and leonardo: sha256(.elf), sha256(.hex), Flash: and RAM: figures."
    - "A pre-sweep test baseline exists for all three firmware CI legs and the full host suite, taken on the clean tree."
  artifacts:
    - .planning/v1.33/baseline-pre-sweep.md
  key_links:
    - "The preserved ref name + sha recorded in baseline-pre-sweep.md is the ONLY recovery path for Phases 155-158's implemented work."
    - "The pre-sweep sha of each sub-repo branch recorded in baseline-pre-sweep.md is the anchor every SWEEP-13 commit-count criterion in plan 12 measures against."
---

<objective>
Discharge D-12's two blocking preconditions in a way that cannot lose work, and
take the pre-sweep baselines that every "before/after" claim in this phase is
measured against.

Purpose: research finding F8 established that **no baseline measurement is
meaningful until the `firestarter` working tree is clean** — measured against the
dirty tree the host suite is 1963 pass / 7 fail and the firmware gates are 317
pass / 6 fail, all attributable to the dirt. And this run is chained (`--auto`),
so D-12's "operator's call at execution time" gate would be auto-approved and
protect nothing. Therefore the reset is made **safe by construction**: the dirt is
**committed onto a named side branch and verified** before the tree stops being
dirty, which reduces the "reset" to a plain branch switch. No destructive git
command runs at all.

Output: a named preserved ref in the `firestarter` sub-repo, a
`gsd/v1.33-source-hygiene-firmware-size-reduction` branch in both sub-repos, and
`.planning/v1.33/baseline-pre-sweep.md` carrying every pre-sweep number this phase
will later compare against.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-VALIDATION.md
</context>

## Multi-Source Coverage Audit (phase-wide, recorded once here)

| Source | Item | Covered by |
|---|---|---|
| GOAL | Remove planning provenance from shipped source, condense load-bearing rationale, build (not apply) the remap tool | Plans 06-11 (sweep), 05 (tool), 12 (landing) |
| REQ | SWEEP-01 | 02 (procedure stated), 06, 07, 09, 10 |
| REQ | SWEEP-02 | 02, 07, 09 |
| REQ | SWEEP-03 | 02, 07, 08, 09, 10, 11 |
| REQ | SWEEP-04 | 02, 08, 11 |
| REQ | SWEEP-05 | **01 (before)**, 06, 07, 12 (after) |
| REQ | SWEEP-06 | 02, 07 |
| REQ | SWEEP-07 | 03, 12 |
| REQ | SWEEP-08 | 06 |
| REQ | SWEEP-09 | 04 |
| REQ | SWEEP-10 | 04, 12 |
| REQ | SWEEP-11 | 05 |
| REQ | SWEEP-12 | 12 |
| REQ | SWEEP-13 | **01 (branches + anchors)**, 12 (commits) |
| RESEARCH | F1 byte-identity is sha256(firestarter_uno.elf) | 01, 12 (Ruling E) |
| RESEARCH | F2 sdp gate can go silently green | 03 |
| RESEARCH | F3 four blob-sha sidecars | 02 (record), 07 (execute) |
| RESEARCH | F4 22 firmware-repo gates | 02 (Ruling D) |
| RESEARCH | F5 665 ambiguous basenames | 04 |
| RESEARCH | F6 SWEEP-07 needs new test code | 03 (Ruling A) |
| RESEARCH | F7 9 modules assert porcelain | 01, 12 |
| RESEARCH | F8 no baseline until the tree is clean | **01** |
| RESEARCH | R1 remap algorithm | 05 |
| RESEARCH | R2 citation extraction + ordering | 04 |
| RESEARCH | R6 archived-milestones hazard is absent | 12 |
| CONTEXT | D-01 | 02, 05, 06, 07, 08, 09, 10, 11 |
| CONTEXT | D-02 | 02, 07, 09 |
| CONTEXT | D-03 | 02, 07, 08, 11 |
| CONTEXT | D-04 | 02, 08, 11 |
| CONTEXT | D-05 | 02 |
| CONTEXT | D-06 | 03, 06 |
| CONTEXT | D-07 | 04 |
| CONTEXT | D-08 | 04, 12 |
| CONTEXT | D-09 | 04, 05 |
| CONTEXT | D-10 | 05 |
| CONTEXT | D-11 | **01**, 12 |
| CONTEXT | D-12 | **01** |

No source item is MISSING. Excluded by CONTEXT.md `<deferred>` and therefore not
planned: the Phase 157 `_KEY_PARSERS_TABLE_RE` / DECODE-01 flag, a global citation
gate, and a recurrence lint.

## Artifacts this phase produces

Created across the whole phase (this section is repeated in every plan so no plan
can lose sight of the set):

| Artifact | Plan | Repo |
|---|---|---|
| `.planning/v1.33/baseline-pre-sweep.md` | 01 | meta |
| `.planning/v1.33/tools/survey_provenance.py` | 02 | meta |
| `.planning/v1.33/sweep-corpus-baseline.md` | 02 | meta |
| `.planning/v1.33/sweep-gate-dispositions.md` | 02 | meta |
| `firestarter_app/tests/fixtures/planted_sdp_comment_misanchor.cpp` | 03 | app |
| `firestarter_app/tests/fixtures/planted_sdp_comment_brace.cpp` | 03 | app |
| `firestarter_app/tests/fixtures/planted_dispatch_comment_only_hex.cpp` | 03 | app |
| `firestarter_app/tests/fixtures/planted_dispatch_missing_hex.cpp` | 03 | app |
| `test_planted_comment_misanchor_is_detected` (leg) | 03 | app |
| `test_planted_comment_brace_break_is_detected` (leg) | 03 | app |
| `test_extracted_slice_is_anchored_on_the_real_declaration` (leg) | 03 | app |
| `test_planted_missing_hex_is_detected` (leg) | 03 | app |
| `test_planted_comment_only_hex_is_NOT_detected` (leg) | 03 | app |
| `.planning/v1.33/tools/citation_paths.py` | 04 | meta |
| `.planning/v1.33/tools/build_citation_manifest.py` | 04 | meta |
| `.planning/v1.33/tools/test_build_citation_manifest.py` | 04 | meta |
| `.planning/v1.33/sweep-citation-manifest.jsonl` | 04 | meta |
| `.planning/v1.33/sweep-citation-manifest-report.md` | 04 | meta |
| `.planning/v1.33/tools/remap_citations.py` | 05 | meta |
| `.planning/v1.33/tools/test_remap_citations.py` | 05 | meta |
| `.planning/v1.33/tools/fixtures/` (5 files) | 05 | meta |
| `.planning/v1.33/CITATIONS-STALE.md` (the SWEEP-12 staleness marker) | 12 | meta |
| `.planning/v1.33/sweep-outcome-record.md` | 12 | meta |
| Regenerated `firestarter/tests/golden/eprom_params_citations.json` | 07 | fw |
| Regenerated `firestarter/tests/golden/protocol_branch_inventory.json` | 07 | fw |

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Preserve the dirty firestarter tree on a named ref and verify it before anything resets (D-12 precondition 1, Ruling C)</name>
  <files>firestarter/ (git refs only — branch wip/v1.33-size-reduction-survey-preserved; no file content is edited)</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<decisions>` D-12 — both preconditions, verbatim
    - `.planning/notes/firmware-size-reduction-survey.md` — what the 11 modified files are and why they exist
    - `.planning/notes/firmware-size-reduction-measured.patch` — head of the file only, to confirm it is a `git diff` patch against `beta`
    - `firestarter/CLAUDE.md`
  </read_first>
  <action>
This task is BLOCKING and its failure mode is unrecoverable loss of Phases 155-158's
already-implemented work, so it is made safe by construction: the dirt is COMMITTED,
not discarded. No `git checkout --`, `git reset --hard` or `git clean` may run in this
task or anywhere in this phase.

Step 1 — capture and assert the starting state. Record `PRE_DIRTY_SHA` from
`git -C firestarter rev-parse HEAD`. Assert `git -C firestarter branch --show-current`
is `size-reduction-survey` and `git -C firestarter rev-list --left-right --count HEAD...beta`
is `0	0`. Assert `git -C firestarter status --porcelain` has exactly 11 lines and every
line starts with ` M` (no untracked, no staged, no deletions). If any assertion fails,
STOP and report — the recovery record was verified against exactly this state and a
different state invalidates the oracle.

Step 2 — preserve. Create the side branch `wip/v1.33-size-reduction-survey-preserved`
at the current HEAD carrying the dirty tree along (`git -C firestarter checkout -b`),
stage tracked modifications only with `git -C firestarter add -u` (never `add -A`, never
`add .`), assert `git -C firestarter diff --cached --name-only` lists exactly the same 11
paths, then commit with subject
`wip(v1.33): preserve size-reduction-survey working tree before the provenance sweep`
and a body naming `.planning/notes/firmware-size-reduction-measured.patch` as the
duplicate record. Record `PRESERVED_SHA`.

Step 3 — verify the preservation against the recorded patch, using a tree comparison
rather than a patch-text comparison (patch texts differ in `index` lines even when the
content is identical). Build two scratch trees under `/tmp/gsd-154-preserve/`: `beta_plus`
from `git -C firestarter archive beta | tar -x` with
`.planning/notes/firmware-size-reduction-measured.patch` applied by `git apply` run with
`beta_plus` as the working directory; and `preserved` from
`git -C firestarter archive $PRESERVED_SHA | tar -x`. Then `diff -r` the two trees. The
recursive diff MUST be empty. Also record `git -C firestarter diff --shortstat beta..$PRESERVED_SHA`
and confirm it reports 11 files, 229 insertions, 231 deletions.

Step 4 — and ONLY if step 3's `diff -r` was empty — leave the preserved branch. Because
step 2 committed the dirt, the working tree is already clean, so this is a plain
`git -C firestarter checkout beta` with no discard semantics. Assert
`git -C firestarter status --porcelain` is now empty.

Do not delete or force-update `wip/v1.33-size-reduction-survey-preserved` anywhere in
this phase. Record `PRE_DIRTY_SHA`, `PRESERVED_SHA` and the branch name in this task's
output for task 3 to write into the baseline record.
  </action>
  <verify>
    <automated>test -z "$(git -C firestarter status --porcelain)" && git -C firestarter rev-parse --verify wip/v1.33-size-reduction-survey-preserved && test "$(git -C firestarter diff --name-only beta..wip/v1.33-size-reduction-survey-preserved | wc -l)" -eq 11 && diff -r /tmp/gsd-154-preserve/beta_plus /tmp/gsd-154-preserve/preserved</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter rev-parse --verify wip/v1.33-size-reduction-survey-preserved` exits 0 and prints a sha.
    - `diff -r /tmp/gsd-154-preserve/beta_plus /tmp/gsd-154-preserve/preserved` produces **no output** and exits 0 — the preserved commit's tree is byte-identical to clean `beta` plus the recorded patch.
    - `git -C firestarter diff --name-only beta..wip/v1.33-size-reduction-survey-preserved | wc -l` == 11.
    - `git -C firestarter diff --shortstat beta..wip/v1.33-size-reduction-survey-preserved` reports `229 insertions` and `231 deletions`.
    - `git -C firestarter status --porcelain` is empty.
    - Source assertion: `git -C firestarter reflog --date=iso | head -40` contains no `reset --hard`, `checkout --` or `clean` entry created by this task; the SUMMARY states this explicitly.
    - `PRE_DIRTY_SHA` and `PRESERVED_SHA` are both recorded verbatim in the task output.
  </acceptance_criteria>
  <done>The 11 modified firestarter files exist on a named committed ref, proven byte-identical to `beta` + the recorded recovery patch by a recursive tree diff, and the working tree is clean without any destructive git command having run.</done>
</task>

<task type="auto">
  <name>Task 2: Fork the milestone branch off beta in both sub-repos (D-12 precondition 2, D-11)</name>
  <files>firestarter/, firestarter_app/ (git refs only — branch gsd/v1.33-source-hygiene-firmware-size-reduction; no file content is edited)</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<decisions>` D-11 and D-12 precondition 2
    - `firestarter_app/CLAUDE.md`
    - `/workspaces/CLAUDE.md`
  </read_first>
  <action>
Per D-12 precondition 2 and the project's branching rule, fork
`gsd/v1.33-source-hygiene-firmware-size-reduction` off `beta` in BOTH sub-repos. The meta
repo is already on that branch. Assert first that no `gsd/v1.33-*` branch already exists
in either sub-repo (`git branch --list 'gsd/v1.33-*'` must be empty in both); if one does,
STOP and report rather than reusing it.

In `firestarter`: `git checkout -b gsd/v1.33-source-hygiene-firmware-size-reduction beta`.
In `firestarter_app`: same command. Record each new branch's tip sha as
`FW_PRE_SHA` and `APP_PRE_SHA`.

Note and record a pre-existing condition that changes how plan 12 must commit: the
`firestarter_app` working tree carries untracked files that are NOT this phase's work
(`SECURITY.md`, `datasheets/*.pdf`, `.planning/config.json`). Research finding F7 measured
9 modules asserting git porcelain and **all 9 assert on the FIRMWARE repo**
(`_git_porcelain(FW_ROOT)` in the 4 app modules; `_git_porcelain(_FW_REPO_ROOT)` at
`firestarter/tests/test_flash_path_record_sync.py:1247` in the firmware ones), so app-repo
untracked files are harmless to the gates — but every `git add` in the app repo throughout
this phase MUST be path-scoped and MUST NOT use `git add -A` or `git add .`, or those three
unrelated items get swept into the SWEEP-13 commit. Write this constraint into the task
output.
  </action>
  <verify>
    <automated>test "$(git -C firestarter branch --show-current)" = "gsd/v1.33-source-hygiene-firmware-size-reduction" && test "$(git -C firestarter_app branch --show-current)" = "gsd/v1.33-source-hygiene-firmware-size-reduction" && test -z "$(git -C firestarter status --porcelain)" && git -C firestarter rev-parse HEAD && git -C firestarter_app rev-parse HEAD</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter branch --show-current` == `gsd/v1.33-source-hygiene-firmware-size-reduction`.
    - `git -C firestarter_app branch --show-current` == `gsd/v1.33-source-hygiene-firmware-size-reduction`.
    - `git -C firestarter rev-list --left-right --count HEAD...beta` prints `0	0` (the branch is at beta's tip, nothing added yet).
    - Same for `firestarter_app`.
    - `git -C firestarter status --porcelain` is empty.
    - `FW_PRE_SHA` and `APP_PRE_SHA` are recorded verbatim in the task output — plan 12's SWEEP-13 commit-count criteria are anchored to these, never to `HEAD~1` (`git rev-list --count HEAD ^HEAD~1` is a tautology that always prints 1).
  </acceptance_criteria>
  <done>Both sub-repos are on the milestone branch forked from `beta` with clean or known-benign working trees, and both pre-sweep anchor shas are recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Record the pre-sweep byte-identity pair for three AVR targets and the pre-sweep suite baselines on the clean tree (SWEEP-05 before-half, Ruling E)</name>
  <files>.planning/v1.33/baseline-pre-sweep.md</files>
  <read_first>
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-RESEARCH.md` §`R5 — The Byte-Identity Oracle` (lines 153-232) and §`Environment Availability` (lines 1351-1381)
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-VALIDATION.md` §`Test Infrastructure`
    - `firestarter/platformio.ini`
    - `firestarter_app/pyproject.toml` (the `addopts` line)
  </read_first>
  <action>
Per Ruling E, record BOTH the hash pair (strictly stronger, and proven comment-immune) and
the `Flash:`/`RAM:` figures, so SWEEP-05's literal "measured pair of numbers" is satisfied
and strengthened rather than substituted. The artifact is `firestarter_uno.elf`, NOT
`firmware.elf` — `name_firmware.py` rewrites `PROGNAME` via `env.Replace`.

For each of the three AVR environments `uno`, `uno328pb`, `leonardo`: cold-build with
`rm -rf .pio/build/<env>` then `pio run -e <env>`, and record `sha256sum` of
`.pio/build/<env>/firestarter_<env>.elf` and `.../firestarter_<env>.hex` plus the `Flash:`
and `RAM:` lines from the build summary. Confirm the `uno` figures match research's
`RAM: 1562 / 2048` and `Flash: 23088 / 32768`; if they do not, record the delta rather than
adjusting anything — the research numbers were taken against the dirty tree and this is the
first clean measurement. Research assumption A2 flags that the `-g`-absent compile line was
verified for `uno` only; before trusting the hash oracle on `uno328pb` and `leonardo`,
confirm with `pio run -v -e <env> 2>&1 | grep -c ' -g '` that the project-source compile
lines carry no `-g`, and record the result per env.

Then take the three pre-sweep suite baselines on the now-clean tree, because research F8
established none of research's own baseline numbers are meaningful (they were taken against
the dirty tree: host 1963 pass / 7 fail, firmware gates 317 pass / 6 fail, all attributable
to the dirt):
  1. `cd firestarter && pio test -e native` — record passed/failed (expected 172 cases).
  2. `cd firestarter && pytest tests/` — record passed/failed.
  3. Host suite on CPython 3.11 ONLY (the devcontainer's 3.12 masks app CI): export
     `UV_CACHE_DIR=/tmp/gsd-154-uvcache`, `uv venv --python 3.11 /tmp/gsd-154-venv311`,
     `uv pip install --python /tmp/gsd-154-venv311/bin/python -e '.[test]'` from
     `firestarter_app`, then
     `FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/ -o addopts="" -q`.
     `-o addopts=""` is mandatory: `pyproject.toml`'s `addopts = "-ra -q"` plus a
     command-line `-q` suppresses the count line. Allow 600 s. Record passed/failed
     (expected ~1970 tests).
  4. Also record the four blob-sha gates named by research F3 —
     `pytest tests/test_eprom_params_citations.py tests/test_protocol_branch_inventory.py tests/test_golden_trace_identity.py tests/test_golden_trace_identity_eprom_v131.py`
     from `firestarter` — as green, so plan 07's regeneration has a proven green starting
     point.

Write everything into `.planning/v1.33/baseline-pre-sweep.md`, creating the
`.planning/v1.33/` tree (research verified it does not exist yet). The file must also carry,
as named fields: `PRE_DIRTY_SHA`, `PRESERVED_SHA`, the preserved branch name, `FW_PRE_SHA`,
`APP_PRE_SHA`, the venv path `/tmp/gsd-154-venv311`, the `UV_CACHE_DIR` path, and the
verbatim commands that produced every number. Do NOT commit this file to the meta repo in
this plan — per D-11 the meta deliverables are committed once, by plan 12; note that in the
SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/v1.33/baseline-pre-sweep.md && grep -c 'firestarter_uno.elf' .planning/v1.33/baseline-pre-sweep.md && test "$(grep -cE '^\| *(uno|uno328pb|leonardo) *\|' .planning/v1.33/baseline-pre-sweep.md)" -ge 3 && grep -qE 'FW_PRE_SHA.*[0-9a-f]{7}' .planning/v1.33/baseline-pre-sweep.md && grep -qE 'APP_PRE_SHA.*[0-9a-f]{7}' .planning/v1.33/baseline-pre-sweep.md && grep -q 'PRESERVED_SHA' .planning/v1.33/baseline-pre-sweep.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/v1.33/baseline-pre-sweep.md` exists and contains a row per AVR target for each of `uno`, `uno328pb`, `leonardo` with a 64-hex-char `.elf` sha256, a 64-hex-char `.hex` sha256, a `Flash:` figure and a `RAM:` figure.
    - The file contains the literal string `firestarter_uno.elf` and does NOT contain the string `firmware.elf` as the measured artifact.
    - `PRE_DIRTY_SHA`, `PRESERVED_SHA`, `wip/v1.33-size-reduction-survey-preserved`, `FW_PRE_SHA` and `APP_PRE_SHA` each appear in the file with a resolvable sha or ref.
    - Three suite baselines are recorded as `<passed> passed / <failed> failed` with the verbatim command beside each; the host row's command contains `-o addopts=""` and `--python 3.11` or the `/tmp/gsd-154-venv311` interpreter path.
    - The four F3 blob-sha gates are recorded green: `cd firestarter && pytest tests/test_eprom_params_citations.py tests/test_protocol_branch_inventory.py tests/test_golden_trace_identity.py tests/test_golden_trace_identity_eprom_v131.py` exits 0.
    - Per-env `-g` absence is recorded for all three envs as a count (`0` expected), not asserted from research.
  </acceptance_criteria>
  <done>Every "before" number this phase will compare against exists in one committed-intent file, measured on the clean tree, with its producing command beside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | The only crossing where information can be destroyed; task 1 crosses it in the safe direction (commit, never discard) |
| `pyproject.toml` extras → local venv | `uv pip install -e '.[test]'` resolves the app's already-committed dependency set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-01 | Denial of Service | the 11 modified `firestarter` files (Phases 155-158's implemented work) | critical | mitigate | Commit-then-verify-then-switch (Ruling C). Recursive tree diff against `beta` + the recorded patch must be empty BEFORE the tree stops being dirty. No `reset --hard`/`clean`/`checkout --` anywhere. |
| T-154-02 | Repudiation | a baseline taken against the dirty tree | high | mitigate | Task 3 runs strictly after task 1; research F8's dirty-tree numbers are recorded as superseded, not reused. |
| T-154-03 | Tampering | `git add -A` in `firestarter_app` sweeping the 3 pre-existing untracked items into the SWEEP-13 commit | medium | mitigate | Path-scoped `git add` only, recorded as a phase-wide constraint in task 2's output. |
| T-154-SC | Tampering | `uv pip install -e '.[test]'` | low | accept | No new package is introduced: `pyproject.toml` is unchanged, the extras set is already committed, and the remap tooling uses stdlib `difflib` only (research §R1). RESEARCH.md therefore has no `## Package Legitimacy Audit` and none is required — this is the same install CI already performs. Recorded here so the absence is deliberate, not silent. |
</threat_model>

<verification>
- `git -C firestarter rev-parse --verify wip/v1.33-size-reduction-survey-preserved` succeeds.
- `diff -r /tmp/gsd-154-preserve/beta_plus /tmp/gsd-154-preserve/preserved` is empty.
- Both sub-repos on `gsd/v1.33-source-hygiene-firmware-size-reduction`, forked from `beta`.
- `.planning/v1.33/baseline-pre-sweep.md` carries 3 AVR hash+size rows, 3 suite baselines, and 5 named git anchors.
</verification>

<success_criteria>
- No destructive git command ran; the dirty tree's content is recoverable from a named ref proven equal to the committed recovery patch.
- Both sub-repos are branched per D-12 precondition 2 and the project's fork-off-`beta` rule.
- Every downstream "before/after" comparison in this phase has a recorded, command-attributed "before" value taken on a clean tree.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-01-SUMMARY.md` when done.
</output>
