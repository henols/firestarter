---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 06
type: execute
wave: 3
depends_on: ["154-02", "154-03"]
files_modified:
  - firestarter/src/proms/eeprom_28c.cpp
autonomous: true
requirements: [SWEEP-01, SWEEP-03, SWEEP-05, SWEEP-08]
must_haves:
  truths:
    - "All 33 provenance hits in eeprom_28c.cpp are triaged by D-01's procedure and the file's provenance hit count reaches 0."
    - "The AT28C datasheet citation of record survives verbatim: Atmel doc0270 rev 0270L-PEEPR-2/09 section 19 note 2 and Microchip DS20006432B section 6.18 note 2."
    - "The no-data-write-follows safety invariant survives in full, stated so it stands alone."
    - "No brace-wrapped hex pair form survives in any comment in the file, so the _PAIR_RE collision is removed rather than depending on ordering."
    - "The uno build is byte-identical after this file is swept: sha256 of the .elf matches the pre-sweep record."
    - "Both SWEEP-07 controls still go RED and the sdp gate's anchoring leg still passes."
  artifacts:
    - firestarter/src/proms/eeprom_28c.cpp
  key_links:
    - "This file is where the highest comment value and the highest gate risk coincide: the datasheet citation of record and the safety invariant sit in the same block as three _PAIR_RE-shaped literals that could be pulled into the gate's brace slice by a reflow."
    - "The per-file byte-identity oracle costs 1.5 s, so it runs on this one file alone and localises any regression to it."
---

<objective>
Sweep `firestarter/src/proms/eeprom_28c.cpp` — 33 hits, the densest file in the
corpus — as its own unit, per SWEEP-08.

Purpose: SWEEP-08 exists because three things land in this one file. Both
comment-blind gate mechanisms are reachable here: `test_sdp_table_parity.py`'s
`_PAIR_RE` pair counter and its comment-blind brace slice, and the file **already
contains three `_PAIR_RE`-shaped literals inside a comment**, currently harmless only
because they sit above the declaration the gate anchors on. The AT28C datasheet
citation of record lives here. And the D-10-labelled safety property — that the only
thing separating "lock the chip" from "prefix a byte write" is that no data write
follows the sequence — is stated here and nowhere else in a form a reader would find.
Batching this file with others would put the highest-value comments in the repo behind
the same review pass as thirty one-hit files.

Output: `eeprom_28c.cpp` swept, with the collision removed by construction rather
than by ordering (Ruling H), and the byte-identity oracle plus both SWEEP-07 controls
re-run on this file alone.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md
@.planning/v1.33/sweep-corpus-baseline.md
@.planning/v1.33/sweep-gate-dispositions.md
@.planning/v1.33/baseline-pre-sweep.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces no new artifact; it modifies `firestarter/src/proms/eeprom_28c.cpp` only.

<tasks>

<task type="auto">
  <name>Task 1: Triage all 33 hits in eeprom_28c.cpp under D-01, preserving the datasheet citation and the safety invariant (SWEEP-01, SWEEP-03, SWEEP-08, D-01, D-06)</name>
  <files>firestarter/src/proms/eeprom_28c.cpp</files>
  <read_first>
    - `firestarter/src/proms/eeprom_28c.cpp` — the whole file, and lines 176-230 with particular care: the datasheet citation block, the `extern` linkage explanation, the block carrying the safety property, and the real `EEPROM_SDP_ENABLE` declaration at 220 with its initializer at 221-225
    - `.planning/v1.33/sweep-corpus-baseline.md` — D-01's procedure verbatim with its full token list, the unit-of-edit rule, and the step-3 guard
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md` §`firestarter/src/proms/eeprom_28c.cpp (33 hits — own plan, SWEEP-08)` (lines 481-530) — the worked before/after for the `extern` block, the collapse of one tombstone-shaped block into another, and the concrete de-shaping recommendation
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<specifics>` — the sentence stating that a citation is not provenance
    - `firestarter_app/tests/test_sdp_table_parity.py` lines 120 and 141-158 — the two comment-blind mechanisms this file's comments can reach
  </read_first>
  <action>
Apply D-01's procedure to every one of the 33 hits, hit by hit, with the enclosing comment
block as the unit of edit. The three outcomes are keep-reflowed, delete-the-whole-comment
(connective punctuation only), and delete-the-whole-comment (tombstone describing code that
is not there). Step 3's guard binds throughout: never delete the only statement of a
non-obvious invariant, trap, or fail-closed rationale — if stripping leaves it too terse to
stand alone, reword it to stand alone.

Four specific dispositions, all of which PATTERNS.md works out concretely:

1. **The datasheet citation of record survives verbatim** — Atmel doc0270 rev
   0270L-PEEPR-2/09 section 19 note 2, corroborated by Microchip DS20006432B section 6.18
   note 2. A citation is not provenance. Where a decision-ID token appears *inside* the
   citation's prose, strip the token and keep the clause: the phrase about the standalone
   lock op issuing no data write and no read after it must survive as a plain sentence.

2. **The `extern` linkage explanation is a keep-reflowed** — the fact that a namespace-scope
   `const` array has internal linkage in C++ unless a prior declaration with external
   linkage is visible, and that the cross-guard must be able to pin the production array
   rather than a transcribed test-local copy, is a non-obvious invariant. Strip the plan
   label and the requirement ID and the parenthetical that only resolves against
   `.planning/`; keep the sentence.

3. **The tombstone-shaped cross-reference block collapses rather than vanishing.** Most of
   it deletes under step 2, but its operative fact — that this table is deliberately not
   deduped against `flash_utils.h` — is the same fact the safety block states, so fold it
   into that block instead of losing it.

4. **The safety block keeps its full force.** The statement that the absence of a payload is
   a hard safety invariant rather than a convenience, the reason the `flash_utils.h`
   duplication is preserved rather than deduped (the array NAME is the only discriminator
   once the bytes match, so deduping would destroy real semantics), and the reason that
   absence cannot be asserted by comparing tables but has to be asserted on the emitted
   stream — all three survive, reworded to stand alone without their labels.

**Ruling H — de-shape the pair literals rather than preserving an ordering dependency.** The
safety block currently spells three pair literals in the exact brace-wrapped hex-pair form
that `test_sdp_table_parity.py`'s `_PAIR_RE` counts, and it names the arrays. They are
outside the gate's slice today only because the anchor sits below them, and reflowing the
block is precisely the operation that could move them in. Rewrite them in a form that carries
the same information without the brace-wrapped pair shape — the byte values and their target
addresses written as a slash-separated correspondence, as PATTERNS.md lines 527-530 spell out
— so that **no** brace-wrapped hex pair form survives in any comment anywhere in this file.
Removing the collision beats relying on those literals staying above the anchor. The byte
values and the "byte-identical to the protected-write prefix" claim must still be legible to
a reader; do not drop the values, change their shape.

Do not touch any code line. Do not change the real `EEPROM_SDP_ENABLE` or
`EEPROM_SDP_DISABLE` declarations, their initializers, or the `extern` declarations
themselves.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group fw-src | python3 -c "import json,sys; d=json.load(sys.stdin); f=[x for x in d['fw-src']['files'] if 'eeprom_28c.cpp' in x['path']]; print(f); assert not f or f[0]['hits']==0, 'provenance hits remain'" && grep -c '0270L-PEEPR-2/09' firestarter/src/proms/eeprom_28c.cpp && grep -c 'DS20006432B' firestarter/src/proms/eeprom_28c.cpp && test "$(grep -nE '^[[:space:]]*(//|\*|/\*)' firestarter/src/proms/eeprom_28c.cpp | grep -cE '\{[[:space:]]*0x[0-9A-Fa-f]+[[:space:]]*,[[:space:]]*0x[0-9A-Fa-f]+[[:space:]]*\}')" -eq 0 && git -C firestarter diff --numstat -- src/proms/eeprom_28c.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group fw-src --json` reports **0** provenance hits for `src/proms/eeprom_28c.cpp` (down from a measured 33).
    - `grep -c '0270L-PEEPR-2/09' firestarter/src/proms/eeprom_28c.cpp` is at least 1 and `grep -c 'DS20006432B' firestarter/src/proms/eeprom_28c.cpp` is at least 1 — the datasheet citation of record survives.
    - The file still contains a sentence asserting that no data write follows the lock sequence: `grep -ic 'no data write' firestarter/src/proms/eeprom_28c.cpp` is at least 1.
    - **Ruling H gate:** restricting the search to comment lines only, the count of brace-wrapped hex-pair forms is **0**. Measured as `grep -nE '^[[:space:]]*(//|\*|/\*)' firestarter/src/proms/eeprom_28c.cpp` piped to a `grep -c` for the pair shape. The scope restriction to comment lines is deliberate and required: the real initializer at lines 221-225 legitimately uses that form on **code** lines and a file-wide count would be self-invalidating.
    - `git -C firestarter diff --numstat -- src/proms/eeprom_28c.cpp` shows a non-empty diff confined to this one path.
    - Source assertion: `git -C firestarter diff -U0 -- src/proms/eeprom_28c.cpp | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(//|\*|/\*|$)'` == 0 — every added and removed line is a comment line or blank. No code line changed.
    - Manual-only, named here rather than laundered into an automated leg: whether the datasheet citation is **unaltered** and whether the safety invariant still carries its full force are reader judgments. The reviewable artifact is `git -C firestarter diff -- src/proms/eeprom_28c.cpp`, and the SUMMARY must quote the surviving citation text and the surviving safety sentence in full so a reviewer does not have to reconstruct them.
  </acceptance_criteria>
  <done>All 33 hits are triaged, the datasheet citation and the safety invariant survive in full, no code line changed, and no brace-wrapped hex pair form survives on any comment line.</done>
</task>

<task type="auto">
  <name>Task 2: Prove nothing executable changed and both comment-blind gates are still armed (SWEEP-05, SWEEP-07, D-06, Ruling E)</name>
  <files>(verification only — no file is modified; builds write to firestarter/.pio/build/uno, which is gitignored)</files>
  <read_first>
    - `.planning/v1.33/baseline-pre-sweep.md` — the pre-sweep `uno` sha256 pair and the `Flash:`/`RAM:` figures, and the venv path
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-VALIDATION.md` §`Sampling Rate` — why the oracle runs per file rather than per wave
    - `firestarter_app/tests/test_sdp_table_parity.py` — the eight legs, including the three added by plan 03
  </read_first>
  <action>
Run the byte-identity oracle on this one file's change, at the per-file cadence VALIDATION.md
specifies: a cold build costs 1.5 s, so running it per file localises any regression to one
file instead of to a batch. `rm -rf .pio/build/uno && pio run -e uno`, then `sha256sum` the
`.elf` and the `.hex` and read the `Flash:`/`RAM:` lines. Record all four and compare against
plan 01's pre-sweep record. Per Ruling E, record the hash pair **and** the size pair — the
hash is strictly stronger and the sizes satisfy SWEEP-05's literal wording. If the `.elf`
sha256 differs, SWEEP-05's rule is explicit: **the delta is reverted, not explained.** Revert
this file's edit, re-measure to confirm the baseline is restored, and report the divergence
rather than rationalising it.

Then re-run the SWEEP-07 controls against the swept file, since this is the file whose
comments can reach both comment-blind mechanisms:
`pytest tests/test_sdp_table_parity.py -o addopts="" -q` must be green across all eight legs,
including the two planted-comment controls (which read committed fixtures, so they are
unaffected by the sweep and must stay RED-detecting) and the anchoring leg (which reads the
**real, now-swept** file and must still find the slice inside the real declaration's span).
The anchoring leg is the one that could legitimately change behaviour as a result of this
sweep, and its passing is the evidence that the Ruling H de-shaping worked.

Also run the firmware native suite for this file's subsystem and the firmware gate suite:
`pio test -e native` and `pytest tests/` from `firestarter`, comparing pass/fail against plan
01's baseline. Note that the four F3 blob-sha gates are expected green here because
`eeprom_28c.cpp` is not blob-sha-pinned — only `eprom_params.cpp`, `eprom.cpp`,
`eprom_params.h`, `eprom_v131_expected.h` and `sdp_expected.h` are, and those are plan 07's
concern.

Do not commit. The firmware repo takes exactly one commit for this whole phase, created in
plan 12; leaving this plan's edit uncommitted is intentional, and if the executor's own
workflow commits it anyway, plan 12 squashes the range. Record which happened.
  </action>
  <verify>
    <automated>cd firestarter && rm -rf .pio/build/uno && pio run -e uno && sha256sum .pio/build/uno/firestarter_uno.elf .pio/build/uno/firestarter_uno.hex && cd /workspaces/firestarter_app && FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/test_sdp_table_parity.py -o addopts="" -q && cd /workspaces/firestarter && pio test -e native && pytest tests/</automated>
  </verify>
  <acceptance_criteria>
    - `sha256sum .pio/build/uno/firestarter_uno.elf` after the sweep **equals** the value recorded in `.planning/v1.33/baseline-pre-sweep.md`, character for character.
    - `sha256sum .pio/build/uno/firestarter_uno.hex` also matches, recorded as corroboration.
    - The `Flash:` and `RAM:` figures match the pre-sweep record exactly.
    - `pytest tests/test_sdp_table_parity.py -o addopts="" -q` exits 0 with 8 tests, including the anchoring leg passing against the **swept** real file.
    - `pio test -e native` reports the same pass/fail counts as plan 01's baseline (172 cases expected).
    - `pytest tests/` from `firestarter` reports pass/fail counts no worse than plan 01's baseline; any new failure is named and attributed.
    - If any of the above diverges, the file's edit is reverted and the baseline is shown restored by a second oracle run — the divergence is reported, not explained away.
  </acceptance_criteria>
  <done>The uno artifact hash and size pair are unchanged, both comment-blind gates are still armed, the anchoring leg passes against the swept file, and the native and gate suites are no worse than baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comment text → gate extraction slice | the boundary the `_PAIR_RE` collision and the brace slice both cross |
| comment text → future reader's safety understanding | the AT28C no-payload invariant has no other statement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-22 | Tampering | reflowing the safety block pulls its three brace-wrapped hex pairs into `_PAIR_RE`'s slice, or a comment brace mis-anchors the depth walk | critical | mitigate | Ruling H: de-shape the pair literals so no brace-wrapped pair form survives on any comment line, asserted by a comment-line-scoped grep. Plus the anchoring leg from plan 03 run against the swept file. |
| T-154-23 | Denial of Service (of future correctness) | the AT28C no-payload safety invariant losing its only statement | critical | mitigate | D-01 step 3's guard; explicit keep-and-reword instruction; a `grep -ic 'no data write'` criterion; the SUMMARY quotes the surviving sentence in full for review. |
| T-154-24 | Repudiation | the datasheet citation of record altered while looking preserved | high | mitigate | Two literal grep criteria on the document numbers, plus the manual review artifact named and quoted in the SUMMARY. |
| T-154-25 | Tampering | a code line changed under cover of a comment sweep | high | mitigate | The `git diff -U0` criterion asserting every added and removed line is a comment or blank, plus the `.elf` sha256 equality. |
</threat_model>

<verification>
- `survey_provenance.py` reports 0 hits for `src/proms/eeprom_28c.cpp`.
- The comment-line-scoped brace-wrapped-pair count is 0.
- `sha256sum` of the `uno` `.elf` and `.hex` and the `Flash:`/`RAM:` pair all match plan 01's record.
- `pytest tests/test_sdp_table_parity.py` is green across 8 legs; `pio test -e native` and `pytest tests/` are no worse than baseline.
- Every added and removed diff line is a comment or blank line.
</verification>

<success_criteria>
- The densest and highest-risk file in the corpus is swept in isolation, with its two irreplaceable comments intact.
- The `_PAIR_RE` collision is removed by construction, not by ordering.
- Nothing executable changed, proven by a hash pair and corroborated by a size pair.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-06-SUMMARY.md` when done.
</output>
