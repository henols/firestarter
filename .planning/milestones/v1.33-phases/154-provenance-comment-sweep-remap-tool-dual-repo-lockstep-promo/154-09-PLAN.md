---
phase: 154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo
plan: 09
type: execute
wave: 3
depends_on: ["154-02"]
files_modified:
  # All hit-carrying files under firestarter_app/firestarter (the shipped Python
  # package). Worklist authority: survey_provenance.py --group app-pkg --file-table.
  - firestarter_app/firestarter/**
autonomous: true
requirements: [SWEEP-01, SWEEP-02, SWEEP-03]
must_haves:
  truths:
    - "Every provenance hit in the shipped host package is triaged under D-01, with requirement and decision IDs stripped because this is shipped source."
    - "CAP-0N survives everywhere in the host package, as live cross-repo wire-protocol vocabulary present in shipped host source."
    - "The database.py reversal-record block is CONDENSED, not compressed: it still states that the earlier policy was right and its premise was wrong, and the hardware-hazard argument keeps its full force."
    - "The full host suite is no worse than the pre-sweep baseline once the sub-repo commits land."
  artifacts: []
  key_links:
    - "The host repo has NO size or byte-identity oracle at all. 132 hit lines here have no mechanical did-this-change-behaviour check beyond the host suite itself."
    - "CAP-0N lives in serial_comm.py, hardware.py and firmware.py and is referenced by 13 host test modules; stripping it would destroy vocabulary the wire protocol shares with the firmware."
---

<objective>
Sweep the shipped host Python package `firestarter_app/firestarter` — 132 hit lines
across 20 files — under D-01, with IDs stripped and `CAP-0N` exempt.

Purpose: this is shipped source on the host side, so D-03's strip direction applies
and D-02's exemption is at its most load-bearing here — `CAP-0N` is *defined* in this
package (`serial_comm.py`, `hardware.py`, `firmware.py`) and referenced by 13 host
test modules. It is also where the single highest-value comment block in either repo
lives: the reversal record in `database.py` around lines 580-630, which the writeup
calls out by name and which must survive condensation with both halves of its
reversal intact, or the reversal gets re-reversed by a future reader.

There is no byte-identity oracle on this side at all, so the host suite is the only
mechanical check, and the review of the comment diff carries proportionally more of
the weight.

Output: the host package swept, with the three densest files and the `database.py`
block handled explicitly.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md
@.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md
@.planning/v1.33/sweep-corpus-baseline.md
@.planning/v1.33/baseline-pre-sweep.md
</context>

## Artifacts this phase produces

See plan 01 §"Artifacts this phase produces" for the full 24-row inventory. This plan
produces no new artifact; it modifies existing files under
`firestarter_app/firestarter` only.

<tasks>

<task type="auto">
  <name>Task 1: Condense the database.py reversal record without re-reversing it (SWEEP-01, D-01 step 3)</name>
  <files>firestarter_app/firestarter/database.py</files>
  <read_first>
    - `firestarter_app/firestarter/database.py` lines 570-645 — the whole reversal-record block including the hardware-hazard paragraph
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-CONTEXT.md` §`<specifics>` — the requirement that the condensed form still say the policy was right and the premise was wrong
    - `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-PATTERNS.md` §`firestarter_app/firestarter/database.py:580-630` (lines 578-594) — which paragraphs are step-3 protected and which are the condensable part, with the worked before/after for the load-bearing sentence
    - `.planning/v1.33/sweep-corpus-baseline.md` — D-01's procedure and step-3 guard
  </read_first>
  <action>
This block is ~50 lines and the writeup calls it "the single highest-value comment block in the
repo". Under D-01 it is keep-and-reflow, and under step 3 it must still say that the earlier
decision's **policy** was right and its **premise** was wrong. **Condense; do not compress to a
one-liner.** If the condensed form loses either half of that statement, a future reader has
grounds to re-reverse the reversal, which is the specific failure this comment exists to prevent.

Concretely:

- The load-bearing sentence — that the earlier policy was correct given its premise and only the
  premise changed, that this is recorded as mechanism-corrected and intent-satisfied rather than
  as failed, and that the honest resolution was to make the firmware do more rather than make the
  host claim less — keeps all four of those clauses. Strip the decision-ID token; the sentence
  stays.
- The **hardware-hazard argument** (the paragraph about the algorithm-5 / flash4 path putting 12 V
  on a 5 V part, around lines 583-592) is step-3 protected and **keeps its full force**. Do not
  shorten a safety argument to make a comment tidier.
- The **plan-shape and blast-radius paragraphs** (around lines 625-635) are the condensable part.
  These are the paragraphs that describe how the work was organised rather than what the code
  does, so under step 2 they either strip to a sentence about the code or delete.

Strip every D-01 token throughout the block. Leave the code untouched.

Quote the full condensed block in the SUMMARY. This is one of the six manual-only verifications
VALIDATION.md names, and the reviewable artifact must be in the SUMMARY rather than requiring a
reviewer to reconstruct it from a diff.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group app-pkg && test "$(git -C firestarter_app diff -U0 -- firestarter/database.py | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)')" -eq 0 && grep -qi 'premise' firestarter_app/firestarter/database.py && grep -qiE '12 ?V|12v' firestarter_app/firestarter/database.py</automated>
  </verify>
  <acceptance_criteria>
    - `git -C firestarter_app diff -U0 -- firestarter/database.py | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)'` == 0 — every added and removed line is a `#` comment or blank. No code line changed.
    - The surviving block contains a statement of **both** halves of the reversal: `grep -ic 'premise' firestarter_app/firestarter/database.py` is at least 1, and the surviving text asserts the policy was correct while the premise changed.
    - The hardware-hazard paragraph survives: the surviving text still names the voltage hazard (a 12 V rail reaching a 5 V part) — assert by grep for the voltage figure and by quoting the paragraph in the SUMMARY.
    - The block is condensed, not compressed: `git -C firestarter_app diff --numstat -- firestarter/database.py` shows deletions exceeding insertions, and the surviving block is at least 8 comment lines long (`awk`/`grep -c` over the block's range) — a one-liner fails this criterion.
    - `survey_provenance.py --group app-pkg --json` reports 0 hits for `firestarter/database.py`.
    - Manual-only, named here rather than laundered into an automated leg: whether the condensed block still carries the reversal's meaning is a reading judgment. The reviewable artifact is the block quoted in full in the SUMMARY.
  </acceptance_criteria>
  <done>The reversal record is condensed with both halves intact, the hardware-hazard argument at full force, no code line touched, and the condensed block quoted in the SUMMARY for review.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the rest of the shipped host package, honouring the CAP-0N exemption (SWEEP-01, SWEEP-02, SWEEP-03, D-01, D-02, D-03)</name>
  <files>firestarter_app/firestarter/** — every hit-carrying file per `survey_provenance.py --group app-pkg --file-table` EXCEPT database.py; densest are cli_handlers.py, serial_comm.py, chip_test.py</files>
  <read_first>
    - `.planning/v1.33/sweep-corpus-baseline.md` — the `app-pkg` per-file hit table, D-02's exemption test, D-03's strip direction
    - `firestarter_app/firestarter/cli_handlers.py` (26 hits)
    - `firestarter_app/firestarter/serial_comm.py` (22 hits) — lines 67-156 define `CAP-0N`; the exemption is at its most load-bearing here
    - `firestarter_app/firestarter/chip_test.py` (20 hits)
    - `firestarter_app/firestarter/hardware.py` and `firestarter_app/firestarter/firmware.py` — further `CAP-0N` sites
    - `firestarter_app/CLAUDE.md`
  </read_first>
  <action>
Sweep every hit-carrying file under `firestarter_app/firestarter` other than `database.py`, using
`survey_provenance.py --group app-pkg --file-table` as the worklist. The three densest files hold
68 of the group's 132 hits (`cli_handlers.py` 26, `serial_comm.py` 22, `chip_test.py` 20); the
remaining ~16 files hold the rest, mostly in ones to nines.

This is **shipped source**, so D-03 applies in its strip direction: requirement and decision IDs
are stripped, because here an ID resolves only against `.planning/`.

**D-02's exemption is at its strongest in this group and must be applied before any strip.**
`CAP-0N` is live cross-repo wire-protocol vocabulary: it names a protocol capability generation
in this very package (`serial_comm.py` lines 67-156, `hardware.py` lines 39 and 153,
`firmware.py` line 180) and is referenced by 13 host test modules. Research measured 13 of the 20
`CAP-0`-only hit lines as living in this group. Strip the surrounding phase or plan label and
**keep the `CAP-0N` token**. Apply the generalised exemption test to any token not on D-01's
list: a token appearing in both repos' shipped source is vocabulary, not provenance, and is
exempt. `CLAUDE.md`'s standing instruction that serial-protocol changes be kept in sync between
`serial_comm.py` and `firestarter/src/firestarter.cpp` is the reason this matters.

Step-3 keeps to expect in this group: anything stating a wire-protocol invariant, a
length-discrimination rule, a fail-closed branch's rationale, or a hardware-hazard argument. When
in doubt, keep and reword rather than delete — D-01 step 3 makes that the default, not the
exception.

Touch no code line. In particular do not touch a comment that is inside a string literal or a
docstring: the corpus is `#`-comment lines only, and `sweep-corpus-baseline.md`'s `CORPUS
DEFINITION` section records that docstring-borne provenance is deliberately out of scope with its
measured size and cause.

Then run the targeted host gates for the files touched, plus the CAP-0N regression check, plus
the two SWEEP-07 gates. The full 235 s host suite belongs at the phase gate in plan 12, after
both sub-repo commits land — running it now would redden the 9 porcelain-asserting modules on
this plan's own uncommitted edits.
  </action>
  <verify>
    <automated>cd /workspaces && python3 .planning/v1.33/tools/survey_provenance.py /workspaces/firestarter /workspaces/firestarter_app --json --group app-pkg && test "$(git -C firestarter_app diff -U0 -- firestarter | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)')" -eq 0 && cd firestarter_app && FIRESTARTER_FW_ROOT=/workspaces/firestarter /tmp/gsd-154-venv311/bin/python -m pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py tests/test_serial_comm.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `survey_provenance.py --group app-pkg --json` residual is fully attributed in the SUMMARY: every remaining hit is a D-02-exempt `CAP-0` line, and the count of those matches the pre-sweep `CAP-0`-only measurement for this group (13).
    - `git -C firestarter_app diff -U0 -- firestarter | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -vcE '^[+-][[:space:]]*(#|$)'` == 0 — every added and removed line is a `#` comment or blank.
    - CAP-0N survived: the total `grep -c 'CAP-0'` across `firestarter_app/firestarter/` after the sweep equals the pre-task count captured at task start.
    - `pytest tests/test_sdp_table_parity.py tests/test_dispatch_mirror.py tests/test_serial_comm.py -o addopts="" -q` exits 0.
    - Targeted gates for each touched module pass: for every file swept, run any host test module whose name or content names it, and record the result per file.
    - Source assertion: no docstring line was modified — `git -C firestarter_app diff -U0 -- firestarter | grep -E '^[+-]' | grep -vE '^[+-]{3}' | grep -cE '^[+-][[:space:]]*"""'` == 0.
    - Manual-only, named here rather than laundered into an automated leg: whether step 3's guard was honoured across 132 hit lines with **no** byte-identity oracle on this side. The reviewable artifact is `git -C firestarter_app diff -- firestarter`, and the SUMMARY must name every hit whose enclosing comment was deleted whole, with its classification (label-only or tombstone).
  </acceptance_criteria>
  <done>The shipped host package carries no provenance except the D-02-exempt CAP-0N lines, no code or docstring line changed, and every deleted comment is classified in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host comment text → cross-repo wire-protocol vocabulary | `CAP-0N` is shared with the firmware and the wire protocol; stripping it is a cross-repo break |
| host comment text → future maintainer's decision record | the `database.py` reversal record is the only statement of why a prior decision was reversed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-35 | Tampering | stripping `CAP-0N` from `serial_comm.py`/`hardware.py`/`firmware.py`, destroying vocabulary 13 host test modules and the firmware share | high | mitigate | D-02 exemption applied before any strip; pre/post `grep -c 'CAP-0'` count asserted unchanged for the group. |
| T-154-36 | Denial of Service (of future correctness) | over-compressing the `database.py` reversal record so a future reader re-reverses it | critical | mitigate | Both halves asserted present; the hardware-hazard paragraph kept at full force; the block asserted at least 8 comment lines and quoted in full in the SUMMARY. |
| T-154-37 | Repudiation | 132 hit lines with no byte-identity oracle on this side | high | mitigate | Diff-line-class assertions (comment-or-blank only, no docstring lines), per-file targeted gates, and a named review artifact listing every whole-comment deletion with its classification. |
| T-154-38 | Repudiation | running the full host suite before the sub-repo commits land, reddening 9 porcelain-asserting modules and masking a real regression | medium | mitigate | Targeted subset here; the full suite is deferred to plan 12's phase gate, after both commits, per D-11. |
</threat_model>

<verification>
- `survey_provenance.py --group app-pkg` residual is exactly the 13 D-02-exempt `CAP-0` lines.
- Every added/removed diff line under `firestarter_app/firestarter` is a `#` comment or blank; zero docstring lines.
- `grep -c 'CAP-0'` unchanged for the group.
- The two SWEEP-07 gates plus `test_serial_comm.py` pass; per-file targeted gates recorded.
- The condensed `database.py` block is quoted in the SUMMARY and is at least 8 comment lines.
</verification>

<success_criteria>
- Shipped host source carries no planning provenance except the deliberate CAP-0N exemption.
- The highest-value comment block in either repo is condensed with both halves of its reversal intact.
- Every whole-comment deletion in a group with no oracle is classified and reviewable.
</success_criteria>

<output>
Create `.planning/phases/154-provenance-comment-sweep-remap-tool-dual-repo-lockstep-promo/154-09-SUMMARY.md` when done.
</output>
