# gh#12 follow-up reply — DRAFT, awaiting operator wording review

**Status: NOT POSTED. Do not post from this file.** This discharges the authoring half of CLOSE-06;
the posting half is blocked on the operator's wording review (batch item A-1). Drafted 2026-08-05
while compute was degraded, so the close is not waiting on it.

**Two preconditions before this may be posted:**

1. **The removal must have actually shipped.** Phase 132 deleted `dev sdp` on the milestone branch,
   but this thread must not name a command as gone until the release carrying that removal is real.
   Posting early makes the thread describe a command that still exists for anyone on b15.
2. **Operator wording review.** Outward-facing, going to a person who built a separate Arduino to work
   around this. Never auto-approved — `--auto`/`--chain` would approve exactly this gate, which is why
   Phase 137 must not run under either.

**Checked against the todo's four required points and its amendment:** `write --sdp-relock` is NOT
named as available anywhere below (Phase 135 was deferred to Backlog 999.28; naming it would repeat
the v1.22 C-5 overclaim class *in the very thread this reply exists to be honest with*).

---

## Draft comment

> Following up here, because `firestarter dev sdp` was named in this thread and in the `3.0.0b14`
> release notes, and it's being removed.
>
> **What's changing**
>
> `dev sdp <chip> enable|disable` is gone. The two halves don't survive equally, and I'd rather be
> plain about that than let the release notes imply otherwise:
>
> - **`disable`'s behaviour survives, and you no longer need a command for it.** Unlocking is already
>   what `write` does by default on every protocol-`0x0D` part — it auto-unlocks unless you pass
>   `--skip-sdp-unlock`. So `dev sdp disable` was genuinely redundant, not merely dropped.
> - **`enable` is withdrawn, with no replacement in this release.** If you want a part deliberately
>   left protected, there is currently no supported way to do it. The design for one is settled and the
>   work is queued, but it is not in this release and I'm not going to promise a version for it.
>
> This isn't the "enable/disable" you asked for. You asked for both, and what you get is one of them
> automatically and none of the other. There's a second limitation worth stating: the protection bit on
> these parts can't be read back, so even when a part is protected, nothing can show you that it is.
>
> **What did get better**
>
> The lock is now *checkable*, which it never was before. `dev test` on an SDP-capable part derives a
> leg that writes a known pattern, locks the part, attempts a write without unlocking, and reads back
> to see whether the contents changed — reporting `HELD` / `NOT-HELD` / `NOT-RUN` with a reason. If a
> lock silently fails to hold, that leg is built to surface it rather than let the run look clean.
>
> **This is where I need help, and it's the honest reason `dev test` exists**
>
> I don't have most of these parts. No AT28C silicon was tested during this milestone — I can't buy
> one of everything, and a lot of what the database
> claims about a chip has never been checked against the actual silicon — including the AT28C family in
> this thread. That's not a gap I can close on my own bench.
>
> `dev test` is built for exactly that. If you have a part — the AT28C from this thread, or anything
> else:
>
> ```
> firestarter dev test <chip>
> ```
>
> It derives what that chip's protocol supports, runs each operation as an independent step, and writes
> a diagnostic report you can file straight back to this repo. Non-destructive by default; add
> `--destructive` only on a chip you're willing to risk, and it'll tell you what it skipped without it.
>
> One run from a part in someone's hand tells us more than anything I can derive from the database. If
> the SDP lock doesn't actually hold on real AT28C silicon, a report is how we find out.

---

## Notes for the reviewer (not part of the comment)

- **⚠ Revision history, 2026-08-05 — I got this wrong once, and the correction matters.** On a first
  pass I read the operator's "it does not matter if it has been tested or run with `dev test`" as
  *remove the ask*, and cut the todo's point 4 entirely. That was backwards. The operator's actual
  point: **`dev test` exists to test whether EPROMs work, and they need community help because they
  don't have all the EPROMs on hand.** The ask is not a footnote to this reply — it is the most
  load-bearing thing in it. Restored and expanded into its own section, framed around the real reason
  (no bench has one of everything) rather than as a polite postscript.
- **What "it does not matter if it has been tested" now reads as, in the draft:** the reply does not
  dwell on the milestone's internal verification status or recite `0x0D`'s `UNVERIFIED` marker as a
  disclaimer. Where the silicon gap appears, it appears as the *reason to help* — "if the SDP lock
  doesn't actually hold on real AT28C silicon, a report is how we find out" — not as hedging.
- **What is not negotiable:** the reply still never states or implies the lock is *proven* to work. It
  says "checkable", "built to surface it" — never "verified" or "works". Asserting a result nothing has
  demonstrated is the v1.22 C-5 overclaim class, and Phase 137's claim gate exists to catch exactly
  that. If the wording is pushed toward "proven", expect CLOSE-01 to fail — correctly.
- **Tone:** this reporter built separate hardware to work around the gap. The draft leads with what
  they lose, and only then asks for help — and the ask is honest about why it's needed rather than
  dressed up as an invitation to contribute.
- **Check before posting:** `--destructive` is described as "add only on a chip you're willing to
  risk". Confirm that matches the shipped banner wording, since the reply is now actively instructing a
  stranger to run a command against their hardware.
- **The `3.0.0b14` release notes stay as published** — they are historical. The correction belongs in
  the *next* release notes, whose "Removed" section must read `dev sdp disable` → `write` (automatic)
  and `dev sdp enable` → withdrawn, no replacement, tracked as Backlog 999.28. That is CLOSE-05, not
  this reply.
- If you want it shorter, the "second limitation" sentence about the unreadable protection bit is the
  most cuttable — but it is also the most honest line in the draft, so I'd keep it.
