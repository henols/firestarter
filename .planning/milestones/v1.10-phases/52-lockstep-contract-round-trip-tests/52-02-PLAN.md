---
phase: 52-lockstep-contract-round-trip-tests
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp
  - firestarter/test/native/avr/test_frame_vectors/host_stubs.cpp
  - firestarter/test/native/avr/test_frame_vectors/serial_read_mock.h
  - firestarter/platformio.ini
autonomous: true
requirements: [LOCK-01, LOCK-02]
must_haves:
  truths:
    - "The firmware Unity suite asserts encode(payload)==frozen frame for every golden vector (leg 1)"
    - "The firmware Unity suite asserts rurp_communication_read_data(frame)==payload for every <=511-byte vector (leg 2)"
    - "512/1024-byte data-block vectors are encoder-only on the decode leg (the cmd decoder caps at 511 B, CR-01)"
    - "A CRC8 known-answer test pins poly 0x07 (CRC8([0x01])==0x07) independently of the production PROGMEM table"
    - "The new suite is registered in both test_filter AND build_flags -I so pio test -e native runs it"
  artifacts:
    - path: "firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp"
      provides: "Unity both-legs vector assertions + CRC8 KAT against frame_vectors.h"
      contains: "FRAME_VECTORS"
    - path: "firestarter/test/native/avr/test_frame_vectors/host_stubs.cpp"
      provides: "Native link stubs via _shared/host_stubs_common.inc"
      contains: "host_stubs_common.inc"
    - path: "firestarter/test/native/avr/test_frame_vectors/serial_read_mock.h"
      provides: "Finite-stream Serial.read/available mock for the decode leg"
      contains: "setup_serial_read_mock"
  key_links:
    - from: "firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp"
      to: "firestarter/include/frame_vectors.h"
      via: "#include + memcpy_P over FRAME_VECTORS"
      pattern: "FRAME_VECTORS"
    - from: "firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp"
      to: "rurp_communication_read_data"
      via: "decode-leg assertion fed by the serial_read_mock"
      pattern: "rurp_communication_read_data"
    - from: "firestarter/platformio.ini"
      to: "firestarter/test/native/avr/test_frame_vectors"
      via: "test_filter allowlist + build_flags -I (positive-allowlist gotcha)"
      pattern: "test_frame_vectors"
---

<objective>
Add the firmware-side Unity vector suite that pins the COBS frame contract against the frozen
golden vectors generated in Plan 01. Each vector drives BOTH legs (D-02): encode(payload) ==
frozen frame bytes, and rurp_communication_read_data(frame) == payload. A CRC8 known-answer
test pins poly 0x07 (D-06/SC4) independently of the production PROGMEM table. The new suite is
registered in the `[env:native]` positive allowlist so `pio test -e native` actually runs it.

Purpose: Prove the firmware decode/encode is a byte-exact inverse of the frozen contract
(LOCK-01) and pin it in the Unity layer (LOCK-02 SC2). This is the firmware half of the
shared-golden-vector cross-repo proof.
Output: a new `test_frame_vectors/` Unity suite + platformio.ini registration; the suite runs
green under `pio test -e native -f "*test_frame_vectors*"`.

Runs in parallel with Plan 03 (host) — zero file overlap (firmware repo only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-CONTEXT.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-RESEARCH.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-PATTERNS.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold test_frame_vectors/ suite + register in platformio.ini</name>
  <files>firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp, firestarter/test/native/avr/test_frame_vectors/host_stubs.cpp, firestarter/test/native/avr/test_frame_vectors/serial_read_mock.h, firestarter/platformio.ini</files>
  <read_first>
    - firestarter/test/native/avr/test_cobs_cmd_frame/host_stubs.cpp (analog — copy with updated phase comment; per 52-PATTERNS.md)
    - firestarter/test/native/avr/test_cobs_cmd_frame/serial_read_mock.h (analog — copy verbatim with updated header comment; finite-stream semantics required)
    - firestarter/test/native/avr/_shared/host_stubs_common.inc (the shared include host_stubs.cpp pulls in)
    - firestarter/platformio.ini (analog: existing [env:native] test_filter + build_flags -I entries for the cobs suites; per 52-PATTERNS.md)
  </read_first>
  <action>
    Create `firestarter/test/native/avr/test_frame_vectors/`:
    - `host_stubs.cpp` — copy `test_cobs_cmd_frame/host_stubs.cpp` verbatim, update the phase
      comment to "Phase 52 Plan 02 — host stub TU for test_frame_vectors"; it `#include`s
      `../_shared/host_stubs_common.inc` (no suite-specific overrides needed).
    - `serial_read_mock.h` — copy `test_cobs_cmd_frame/serial_read_mock.h` verbatim, update the
      header comment to the Phase-52 reference; preserve `setup_serial_read_mock()` exactly
      (finite-stream: returns -1 after queue exhaustion).
    - `test_frame_vectors.cpp` — a Wave-0 scaffold: includes (Arduino.h, ArduinoFake.h, unity.h,
      cstdint/cstddef/cstring/vector, "serial_read_mock.h", the extern "C" rurp_shield.h +
      firestarter.h block, and "frame_vectors.h"), empty `setUp`/`tearDown` mirroring
      test_cobs_cmd_frame, and a `main()` with `UNITY_BEGIN(); UNITY_END();` (no RUN_TEST yet —
      compiles + links cleanly).

    Register the new suite in `firestarter/platformio.ini` `[env:native]` (Pitfall 3 — positive
    allowlist; BOTH entries mandatory or the suite is silently skipped): add
    `native/avr/test_frame_vectors` to `test_filter` AND `-I test/native/avr/test_frame_vectors`
    to `build_flags`. No `build_src_filter` change — `rurp_serial_utils.cpp` is already included
    there, giving the suite `rurp_communication_read_data()`.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_frame_vectors*" 2>&1 | grep -Eq 'test_frame_vectors|PASSED|IGNORE'</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "*test_frame_vectors*"` compiles and links (output references the test_frame_vectors suite, not "no tests")
    - `platformio.ini` `[env:native]` lists `native/avr/test_frame_vectors` in `test_filter`
    - `platformio.ini` `[env:native]` lists `-I test/native/avr/test_frame_vectors` in `build_flags`
    - `host_stubs.cpp` includes `../_shared/host_stubs_common.inc`; `serial_read_mock.h` defines `setup_serial_read_mock`
  </acceptance_criteria>
  <done>
    The test_frame_vectors suite exists, is registered in both platformio.ini sections, and
    compiles/links under `pio test -e native`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement both-legs vector assertions + CRC8 KAT</name>
  <files>firestarter/test/native/avr/test_frame_vectors/test_frame_vectors.cpp</files>
  <read_first>
    - firestarter/test/native/avr/test_cobs_cmd_frame/test_cobs_cmd_frame.cpp (primary analog — ref_crc8, test_cobs_encode, build_cobs_frame_bytes, setUp stub chain, main() RUN_TEST shape; per 52-PATTERNS.md, copy the helpers verbatim)
    - firestarter/include/frame_vectors.h (the generated PROGMEM FRAME_VECTORS array this suite asserts against)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-PATTERNS.md "both-legs assertion shape" + "KAT (D-06) pattern" blocks
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-RESEARCH.md Pitfall 5 (511-byte cap) + KAT values
  </read_first>
  <action>
    In `test_frame_vectors.cpp`, copy verbatim from `test_cobs_cmd_frame.cpp`: `ref_crc8` (the
    table-free poly-0x07 reference — keeps the KAT independent of the production CRC8_TABLE),
    `test_cobs_encode` (the test-side COBS encoder), and `build_cobs_frame_bytes` (assembles
    COBS(payload+CRC8)+0x00). Replicate its `setUp` stub chain (ArduinoFakeReset, flush stub,
    write stub, millis stub).

    Add three Unity tests and wire them into `main()` via RUN_TEST:
    - `test_crc8_known_answer` (D-06/SC4): assert `ref_crc8({0x01},1) == 0x07` and
      `ref_crc8(NULL,0) == 0x00` — pins poly 0x07, seed 0x00 independently of the PROGMEM table.
    - `test_vector_encode_leg` (leg 1, LOCK-01): loop `i` over `FRAME_VECTOR_COUNT`, `memcpy_P`
      each `frame_vector_t` out of PROGMEM, run `build_cobs_frame_bytes(vec.payload,
      vec.payload_len, built)`, assert `built.size() == vec.frame_len` and
      `TEST_ASSERT_EQUAL_MEMORY(vec.frame, built.data(), vec.frame_len)` — for EVERY vector incl.
      the 512/1024-byte ones (the encoder leg has no cap).
    - `test_vector_decode_leg` (leg 2, LOCK-01): loop over vectors; SKIP any with
      `payload_len > DATA_BUFFER_SIZE - 1` (encoder-only — the cmd-channel decoder caps at 511 B
      per CR-01, RESEARCH Pitfall 5); for the rest, load `vec.frame` into the rx_queue via
      `setup_serial_read_mock`, call `rurp_communication_read_data(data_buffer)`, assert the
      return `>= 0`, `== vec.payload_len`, and `TEST_ASSERT_EQUAL_MEMORY(vec.payload, data_buffer,
      vec.payload_len)`.

    Do NOT add negative cases (corrupt-CRC reject / truncated-frame recovery) — those stay in the
    existing test_cobs_cmd_frame suite (D-03, valid-payload-only golden set).
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native -f "*test_frame_vectors*" 2>&1 | tail -5 | grep -Eq '([1-9][0-9]* Tests 0 Failures|PASSED)'</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native -f "*test_frame_vectors*"` runs and reports 0 failures with >=3 tests executed
    - `test_crc8_known_answer` asserts `ref_crc8({0x01},1)==0x07` and `ref_crc8(NULL,0)==0x00`
    - `test_vector_encode_leg` asserts all FRAME_VECTOR_COUNT vectors (incl. 512/1024-byte) byte-for-byte
    - `test_vector_decode_leg` skips payloads >511 bytes and asserts payload round-trip for the rest
    - No corrupt-CRC or truncated-frame assertions appear in this suite (D-03)
    - `ref_crc8` / `test_cobs_encode` are table-free copies, not the production PROGMEM table
  </acceptance_criteria>
  <done>
    The firmware vector suite proves both legs of the frozen contract for the full corpus and
    pins CRC8 poly 0x07 with a KAT; `pio test -e native` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen vector bytes -> firmware codec | The Unity suite is the firmware-side checkpoint that the production decoder/encoder matches the frozen contract |

## STRIDE Threat Register

Honest L1 framing: test-only firmware suite; no production code changes. It exercises the
already-shipped `rurp_communication_read_data()` (Phase 50/51) against frozen vectors.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Tampering | firmware codec drifts from the frozen contract | mitigate | both-legs vector assertions fail if encode or decode diverges by a single byte (Task 2); the 253/254/255-run vectors specifically guard the CR-01 byte-drop class |
| T-52-05 | Tampering | new suite silently skipped (platformio.ini positive allowlist) | mitigate | register in BOTH test_filter AND build_flags -I (Task 1, Pitfall 3); verify output references the suite |
| T-52-06 | Spoofing | KAT passing against a wrong-polynomial table | mitigate | KAT uses the table-free `ref_crc8`, independent of the production PROGMEM CRC8_TABLE (Task 2, D-06) |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (existing PlatformIO + ArduinoFake toolchain only) |
</threat_model>

<verification>
- `pio test -e native -f "*test_frame_vectors*"` runs the new suite (not skipped) with 0 failures.
- `pio test -e native` full suite stays green (existing cobs/messages suites unperturbed).
- Decode leg correctly skips >511-byte vectors (encoder-only).
</verification>

<success_criteria>
- Both legs asserted for every golden vector (decode leg skips >511 B per CR-01).
- CRC8 KAT pins poly 0x07 / seed 0x00 via the table-free reference (SC4).
- Suite registered in both platformio.ini sections and runs under `pio test -e native`.
- No negative cases pulled in (D-03 valid-only).
</success_criteria>

<output>
Create `.planning/phases/52-lockstep-contract-round-trip-tests/52-02-SUMMARY.md` when done
</output>
