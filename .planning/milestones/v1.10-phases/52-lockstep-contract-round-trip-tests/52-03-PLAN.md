---
phase: 52-lockstep-contract-round-trip-tests
plan: 03
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - firestarter_app/tests/test_frame_vectors.py
  - firestarter_app/tests/test_revision_constants_parity.py
autonomous: true
requirements: [LOCK-01, LOCK-02]
must_haves:
  truths:
    - "The host pytest suite asserts encode(payload)==frozen frame for every golden vector (leg 1)"
    - "The host pytest suite asserts cobs_decode(frame)==payload (+CRC match) for every <=511-byte vector (leg 2)"
    - "512/1024-byte vectors are encoder-only on the decode leg (consistent with the firmware 511 B cap)"
    - "A CRC8 known-answer test pins poly 0x07 (CRC8([0x01])==0x07) via the table-free reference"
    - "test_revision_constants_parity.py asserts host CMD_FRAME_MAX==512 (firmware Uno DATA_BUFFER_SIZE floor), skipif-guarded on firmware-header presence"
  artifacts:
    - path: "firestarter_app/tests/test_frame_vectors.py"
      provides: "pytest both-legs vector assertions + CRC8 KAT against firestarter.frame_vectors.FRAME_VECTORS"
      contains: "FRAME_VECTORS"
    - path: "firestarter_app/tests/test_revision_constants_parity.py"
      provides: "CMD_FRAME_MAX parity assertion (D-07) extending the existing skipif-guarded file"
      contains: "test_cmd_frame_max_parity"
  key_links:
    - from: "firestarter_app/tests/test_frame_vectors.py"
      to: "firestarter.frame_vectors.FRAME_VECTORS"
      via: "import + per-vector both-legs assertion"
      pattern: "from firestarter.frame_vectors import FRAME_VECTORS"
    - from: "firestarter_app/tests/test_frame_vectors.py"
      to: "firestarter.frame_parser cobs_encode/cobs_decode/_crc8_ccitt"
      via: "exercised codec under test"
      pattern: "cobs_encode"
    - from: "firestarter_app/tests/test_revision_constants_parity.py"
      to: "firestarter.constants.CMD_FRAME_MAX"
      via: "parity assertion against firmware Uno floor 512"
      pattern: "CMD_FRAME_MAX == 512"
---

<objective>
Add the host-side pytest vector suite that pins the COBS frame contract against the frozen
golden vectors generated in Plan 01, and extend the constant-parity test to cover
CMD_FRAME_MAX (D-07). Each vector drives BOTH legs (D-02): encode(payload) == frozen frame
bytes, and cobs_decode(frame)[:-1] == payload (with CRC byte match). A CRC8 known-answer test
pins poly 0x07 (D-06/SC4) via the table-free reference.

Purpose: Prove the host codec is a byte-exact inverse of the frozen contract (LOCK-01), pin it
in the pytest layer, and complete the transport-constant parity guard (LOCK-02 SC2/SC3). This
is the host half of the shared-golden-vector cross-repo proof.
Output: a new tests/test_frame_vectors.py + a test_cmd_frame_max_parity function appended to
the existing parity test; both green under pytest.

Runs in parallel with Plan 02 (firmware) — zero file overlap (host repo only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-CONTEXT.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-RESEARCH.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-PATTERNS.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/test_frame_vectors.py — both-legs vector assertions + CRC8 KAT</name>
  <files>firestarter_app/tests/test_frame_vectors.py</files>
  <read_first>
    - firestarter_app/tests/test_cobs.py (primary analog — imports from frame_parser, _ref_crc8_ccitt usage, build_cobs_frame helper, test class shapes, TestCobsFullBuffer; per 52-PATTERNS.md)
    - firestarter_app/tests/conftest.py (the module-level table-free _ref_crc8_ccitt reference; import or inline it)
    - firestarter_app/firestarter/frame_vectors.py (the generated FRAME_VECTORS list this suite asserts against — from Plan 01)
    - firestarter_app/firestarter/frame_parser.py (the exercised codec: cobs_encode, cobs_decode, _crc8_ccitt)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-PATTERNS.md "both-legs assertion shape (D-02)" + "KAT pattern" blocks
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-RESEARCH.md Pitfall 5 (511-byte decode cap) + KAT values
  </read_first>
  <action>
    Create firestarter_app/tests/test_frame_vectors.py. Import cobs_encode, cobs_decode,
    _crc8_ccitt from firestarter.frame_parser, FRAME_VECTORS from firestarter.frame_vectors, and
    the table-free _ref_crc8_ccitt (import from conftest or inline it as test_cobs.py does —
    table-free independence is what matters).

    Add three test groups:
    - TestFrameVectorsEncodeLeg (leg 1, LOCK-01): for every vec in FRAME_VECTORS, compute
      crc = _crc8_ccitt(vec.payload), encoded = cobs_encode(vec.payload + bytes([crc])), and
      assert encoded + b"\x00" == vec.frame — for EVERY vector incl. 512/1024-byte (encoder has
      no cap). Message names the failing vector.
    - TestFrameVectorsDecodeLeg (leg 2, LOCK-01): for every vec, SKIP len(vec.payload) > 511
      (encoder-only, mirrors the firmware CR-01 511 B cap — RESEARCH Pitfall 5); for the rest,
      body = vec.frame[:-1] (strip the 0x00 delimiter), decoded = cobs_decode(body), assert
      decoded[:-1] == vec.payload and decoded[-1] == _crc8_ccitt(vec.payload).
    - TestCrc8KnownAnswer (D-06/SC4): assert _crc8_ccitt(bytes([0x01])) == 0x07 (poly value
      itself) and _crc8_ccitt(b"") == 0x00 (seed).

    Cross-check the KAT against the table-free _ref_crc8_ccitt too (a typo in the production
    _crc8_ccitt table would diverge from the reference). Do NOT add negative cases — those stay
    in test_cobs.py (D-03, valid-payload-only golden set).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_frame_vectors.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_frame_vectors.py -x -q passes (0 failures) with encode-leg, decode-leg, and KAT tests collected
    - Encode-leg asserts every FRAME_VECTORS entry (incl. 512/1024-byte) with encoded + b"\x00" == vec.frame
    - Decode-leg skips payloads >511 bytes and asserts decoded[:-1] == vec.payload + CRC match for the rest
    - KAT: _crc8_ccitt(bytes([0x01])) == 0x07 and _crc8_ccitt(b"") == 0x00; cross-checked against _ref_crc8_ccitt
    - No corrupt-CRC / truncated-frame negative cases added (D-03)
  </acceptance_criteria>
  <done>
    The host vector suite proves both legs of the frozen contract for the full corpus (decode leg
    capped at 511 B) and pins CRC8 poly 0x07 with a KAT.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend test_revision_constants_parity.py with CMD_FRAME_MAX parity</name>
  <files>firestarter_app/tests/test_revision_constants_parity.py</files>
  <read_first>
    - firestarter_app/tests/test_revision_constants_parity.py (the file to extend — FIRMWARE_HEADER / FW_ABSENT defs + @pytest.mark.skipif pattern + local-import-in-body style; per 52-PATTERNS.md)
    - firestarter_app/firestarter/constants.py (CMD_FRAME_MAX = 512 — the host side of the assertion)
    - firestarter/include/firestarter.h (the firmware #define CMD_FRAME_MAX DATA_BUFFER_SIZE; DATA_BUFFER_SIZE default 512 on Uno/uno328pb — the parity subject, read-only)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-PATTERNS.md "test_revision_constants_parity.py" extension block (Example 5)
  </read_first>
  <action>
    Append a test_cmd_frame_max_parity function to
    firestarter_app/tests/test_revision_constants_parity.py, reusing the module-level FW_ABSENT
    skipif guard already defined in the file
    (@pytest.mark.skipif(FW_ABSENT, reason="firestarter firmware checkout absent")). The test does
    a local from firestarter.constants import CMD_FRAME_MAX and asserts CMD_FRAME_MAX == 512.
    Include a docstring documenting D-07: firmware uses board-parameterized
    #define CMD_FRAME_MAX DATA_BUFFER_SIZE (512 on Uno/uno328pb, possibly 1024 on Leonardo), and
    512 is the binding minimum the host must not exceed; the host hardcoding 512 is ACCEPTED for
    v1.10 (NOT a bug to fix). Do not add a firmware-header byte-parse — the 512 floor is the
    pinned invariant per D-07; the skipif keys on firmware-header presence only.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_revision_constants_parity.py -k cmd_frame_max -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_revision_constants_parity.py -k cmd_frame_max -q passes (or skips cleanly if firmware checkout absent — FW_ABSENT guard)
    - The new function is decorated with @pytest.mark.skipif(FW_ABSENT, ...) reusing the existing module-level guard
    - It asserts host CMD_FRAME_MAX == 512 (the Uno DATA_BUFFER_SIZE floor)
    - Docstring records the D-07 acceptance (host hardcodes 512; board-variant DATA_BUFFER_SIZE; not a bug to fix)
  </acceptance_criteria>
  <done>
    The parity test now guards CMD_FRAME_MAX against the firmware Uno floor (512), completing the
    transport-constant parity coverage (LOCK-02 SC3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen vector bytes -> host codec | The pytest suite is the host-side checkpoint that the production cobs_encode/decode matches the frozen contract |
| host constants <-> firmware header | CMD_FRAME_MAX is duplicated across repos; the parity test guards the duplication |

## STRIDE Threat Register

Honest L1 framing: test-only host suite + a parity assertion; no production code changes. It
exercises the already-shipped frame_parser codec (Phase 50/51) against frozen vectors.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-07 | Tampering | host codec drifts from the frozen contract | mitigate | both-legs vector assertions fail if encode/decode diverges by a single byte (Task 1); 253/254/255-run vectors guard the CR-01 byte-drop class |
| T-52-08 | Tampering | CMD_FRAME_MAX silently diverges from the firmware floor | mitigate | test_cmd_frame_max_parity pins host==512 against the firmware Uno DATA_BUFFER_SIZE floor (Task 2, D-07) |
| T-52-09 | Spoofing | KAT passing against a wrong-polynomial table | mitigate | KAT cross-checked against the table-free _ref_crc8_ccitt, independent of the production _crc8_ccitt table (Task 1, D-06) |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (existing .[test] toolchain only) |
</threat_model>

<verification>
- pytest tests/test_frame_vectors.py -x passes; both legs + KAT covered.
- pytest tests/test_revision_constants_parity.py -k cmd_frame_max passes (or skips on FW_ABSENT).
- Decode leg correctly skips >511-byte vectors (encoder-only).
- Full host suite stays green at the 70% coverage floor (the new tests add coverage, not risk).
</verification>

<success_criteria>
- Both legs asserted for every golden vector (decode leg capped at 511 B).
- CRC8 KAT pins poly 0x07 / seed 0x00 via the table-free reference (SC4).
- CMD_FRAME_MAX parity asserted host==512 vs firmware Uno floor (SC3, D-07).
- No negative cases pulled in (D-03 valid-only).
</success_criteria>

<output>
Create `.planning/phases/52-lockstep-contract-round-trip-tests/52-03-SUMMARY.md` when done
</output>
