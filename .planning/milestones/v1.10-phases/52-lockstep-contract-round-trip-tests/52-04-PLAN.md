---
phase: 52-lockstep-contract-round-trip-tests
plan: 04
type: execute
wave: 3
depends_on: ["52-01", "52-02", "52-03"]
files_modified: []
autonomous: true
requirements: [LOCK-01, LOCK-02]
must_haves:
  truths:
    - "The two vendored frame-vectors.toml copies are byte-identical (diff empty) — the shared anchor is provably shared (D-09)"
    - "The two vendored codegen_vectors.py copies are byte-identical (diff empty)"
    - "The firmware full native suite is green (pio test -e native)"
    - "The host full suite is green at the 70% coverage floor (pytest tests/ --cov)"
    - "Both repos' codegen drift gates are clean (regen && git diff --exit-code) for the vector artifacts"
  artifacts: []
  key_links:
    - from: "firestarter/tools/catalog/frame-vectors.toml"
      to: "firestarter_app/tools/catalog/frame-vectors.toml"
      via: "diff --exit-code (byte-identity merge gate, D-09)"
      pattern: "VEC_RUN_254"
    - from: "firestarter/include/frame_vectors.h"
      to: "firestarter/tools/catalog/frame-vectors.toml"
      via: "regen && git diff --exit-code (no drift)"
      pattern: "codegen_vectors.py"
---

<objective>
Merge gate for Phase 52. With both halves of the shared-golden-vector proof in place (firmware
Plan 02, host Plan 03), this plan asserts the load-bearing cross-repo invariant that makes the
"shared" vectors actually shared: the two vendored `frame-vectors.toml` + `codegen_vectors.py`
copies are byte-identical (D-09). It then confirms both repos' full suites are green and both
codegen drift gates are clean — the "CI green across both repos" condition of LOCK-02 (D-08).

This is the explicit pin for the one risk no per-repo CI structurally catches (catalog drift
between sub-repos, D-09): the byte-identity is asserted here as a concrete check, not assumed.

Purpose: Close LOCK-01 + LOCK-02 by proving the cross-repo contract holds end to end and is
drift-free in both directions. Depends on Plans 01/02/03 (the catalog, both suites must exist).
Output: a verification SUMMARY recording the byte-identity diffs, both full-suite results, and
both drift-gate results. No files modified (verification-only plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-CONTEXT.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-VALIDATION.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-01-SUMMARY.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-02-SUMMARY.md
@.planning/phases/52-lockstep-contract-round-trip-tests/52-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert cross-repo byte-identity + both codegen drift gates clean</name>
  <files>(verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-VALIDATION.md "Manual-Only Verifications" (the D-09 byte-identity check + the dual-CI-green condition)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-01-SUMMARY.md (the catalog + codegen + generated-artifact paths produced in Plan 01)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-CONTEXT.md D-09 (accepted cross-repo drift risk; paired-commit mitigation)
  </read_first>
  <action>
    Assert the two vendored copies are byte-identical (D-09): run
    `diff firestarter/tools/catalog/frame-vectors.toml firestarter_app/tools/catalog/frame-vectors.toml`
    and `diff firestarter/tools/catalog/codegen_vectors.py firestarter_app/tools/catalog/codegen_vectors.py`
    — both MUST be empty. If either diffs, the lockstep is broken; reconcile by re-vendoring from
    the canonical firmware copy (the catalog/codegen authored in Plan 01) and re-running the
    affected repo's codegen before proceeding.

    Re-run each repo's vector codegen and assert no drift (the per-repo drift gate, D-04):
    in firestarter, regenerate include/frame_vectors.h via cpp-vectors then
    `git diff --exit-code include/frame_vectors.h`; in firestarter_app, regenerate
    firestarter/frame_vectors.py via python-vectors then
    `git diff --exit-code firestarter/frame_vectors.py`. Both must be clean.

    Record the diff results (empty) and drift-gate results (clean) in the plan SUMMARY as the
    explicit D-09 byte-identity pin.
  </action>
  <verify>
    <automated>diff firestarter/tools/catalog/frame-vectors.toml firestarter_app/tools/catalog/frame-vectors.toml && diff firestarter/tools/catalog/codegen_vectors.py firestarter_app/tools/catalog/codegen_vectors.py && cd firestarter && python3 tools/catalog/codegen_vectors.py --catalog tools/catalog/frame-vectors.toml --target include/frame_vectors.h --language cpp-vectors && git diff --exit-code include/frame_vectors.h && cd ../firestarter_app && python3 tools/catalog/codegen_vectors.py --catalog tools/catalog/frame-vectors.toml --target firestarter/frame_vectors.py --language python-vectors && git diff --exit-code firestarter/frame_vectors.py</automated>
  </verify>
  <acceptance_criteria>
    - `diff firestarter/tools/catalog/frame-vectors.toml firestarter_app/tools/catalog/frame-vectors.toml` is empty (D-09)
    - `diff firestarter/tools/catalog/codegen_vectors.py firestarter_app/tools/catalog/codegen_vectors.py` is empty (D-09)
    - Regenerating include/frame_vectors.h leaves `git diff --exit-code` clean
    - Regenerating firestarter/frame_vectors.py leaves `git diff --exit-code` clean
    - SUMMARY records the byte-identity diffs and drift-gate results explicitly
  </acceptance_criteria>
  <done>
    Cross-repo byte-identity is proven (both diffs empty) and both vector artifacts are drift-free;
    the D-09 risk is pinned by an explicit check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dual-repo full-suite green gate</name>
  <files>(verification-only — no files modified)</files>
  <read_first>
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-VALIDATION.md "Test Infrastructure" + "Sampling Rate" (the full-suite commands + 70% coverage floor)
    - .planning/phases/52-lockstep-contract-round-trip-tests/52-02-SUMMARY.md + 52-03-SUMMARY.md (what each suite added)
  </read_first>
  <action>
    Run the full suite in each repo and confirm green (the LOCK-02 / D-08 "CI green across both
    repos" condition, verified locally before the per-repo CI runs on the branch):
    - firestarter: `pio test -e native` — full native suite (the new test_frame_vectors suite plus
      the unperturbed existing cobs/messages/dispatch suites) must report 0 failures.
    - firestarter_app: `pytest tests/ --cov=firestarter --cov-fail-under=70` — full host suite incl.
      the new test_frame_vectors.py + the extended parity test; must pass and stay at/above the 70%
      coverage floor (the new tests add coverage, not risk, per RESEARCH).

    Record both full-suite results (test counts, coverage %) in the SUMMARY. Note for the operator
    that "CI green across both repos" (D-08) is finally confirmed by the two sub-repo CI pipelines
    running on `v1.10-serial-transport-hardening` after the paired commits land — this local green
    is the pre-CI gate.
  </action>
  <verify>
    <automated>cd firestarter && pio test -e native 2>&1 | tail -3 | grep -Eq '0 Failures|PASSED' && cd ../firestarter_app && python -m pytest tests/ --cov=firestarter --cov-fail-under=70 -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` (firmware) reports 0 failures across all registered suites
    - `pytest tests/ --cov=firestarter --cov-fail-under=70` (host) passes and meets the 70% floor
    - SUMMARY records both full-suite results (counts + coverage) and notes the per-repo CI as the final D-08 confirmation
  </acceptance_criteria>
  <done>
    Both repos' full suites are green locally; LOCK-01 + LOCK-02 are satisfied pending the final
    per-repo CI runs on the branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical catalog -> two vendored copies | The merge gate is where cross-repo byte-identity is explicitly asserted (D-09) — the one drift class no per-repo CI catches |

## STRIDE Threat Register

Honest L1 framing: verification-only plan; no files modified. It pins the accepted D-09 drift
risk with a concrete byte-identity check and confirms both full suites green.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Tampering | the two vendored catalog/codegen copies diverging (D-09 accepted risk) | mitigate-at-gate | explicit `diff --exit-code` on both files at this merge gate (Task 1) turns the accepted risk into a checked invariant at phase close; paired-commit discipline holds it forward |
| T-52-10 | Tampering | a generated artifact left stale (regen skipped after a catalog edit) | mitigate | re-run both codegen emits + `git diff --exit-code` at the gate (Task 1) |
| T-52-11 | Denial of Service | a new vector regressing the existing suites' green baseline | mitigate | full-suite run in both repos (Task 2) catches any perturbation to the Phase 50/51 GREEN baselines |
| T-52-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (verification-only) |
</threat_model>

<verification>
- Both vendored copies byte-identical (diffs empty).
- Both vector artifacts drift-free (regen && git diff --exit-code clean).
- Firmware full native suite: 0 failures.
- Host full suite: pass at >=70% coverage.
</verification>

<success_criteria>
- Cross-repo byte-identity of frame-vectors.toml + codegen_vectors.py proven (D-09 pinned).
- Both per-repo codegen drift gates clean for the vector artifacts.
- Both full suites green locally (LOCK-01 + LOCK-02), with per-repo CI as the final D-08 confirmation.
</success_criteria>

<output>
Create `.planning/phases/52-lockstep-contract-round-trip-tests/52-04-SUMMARY.md` when done
</output>
