---
phase: 51-command-channel-framing-migration-breaking-wire-change
plan: 03
type: execute
wave: 2
depends_on: ["51-01", "51-02"]
files_modified:
  - firestarter/README.md
  - firestarter_app/README.md
autonomous: true
requirements: [FRAME-05]
must_haves:
  truths:
    - "Both sub-repo READMEs document that the host→fw command channel is now a COBS-framed CRC8-verified wire protocol — a BREAKING change with no mixed-version interop; firmware and host must be upgraded in lockstep"
    - "The dual-repo full test suite is green: firmware `pio test -e native` AND host `python -m pytest --cov-fail-under=70` both pass against the merged Wave-1 changes"
    - "CMD_FRAME_MAX is defined identically (512) in firestarter.h and constants.py (firmware/host constant parity holds)"
  artifacts:
    - path: "firestarter/README.md"
      provides: "breaking wire-change note (firmware side) for the beta cut"
      contains: "breaking"
    - path: "firestarter_app/README.md"
      provides: "breaking wire-change note (host side) for the beta cut"
      contains: "breaking"
  key_links:
    - from: "firestarter/include/firestarter.h CMD_FRAME_MAX"
      to: "firestarter_app/firestarter/constants.py CMD_FRAME_MAX"
      via: "constant parity (CLAUDE.md)"
      pattern: "CMD_FRAME_MAX"
---

<objective>
Close Phase 51 SC3 (breaking-change documentation) and prove the dual-repo gate. D-01 removes all
interop machinery (the framed protocol is the ONLY supported protocol — no capability negotiation,
no fallback), so D-02 makes DOCUMENTATION the SC3 "guard equivalent": both sub-repo READMEs must state
that the host→fw command channel is now COBS-framed + CRC8-verified, that this is a BREAKING wire
change with no mixed-version interop, and that firmware and host must be upgraded in lockstep (cf. the
v1.2 Message-ID rework). Then run the full dual-repo test suite green against the merged Wave-1 work
and confirm firmware/host CMD_FRAME_MAX constant parity.

Purpose: satisfies FRAME-05 SC3; locks in the breaking-change record for the v1.10 beta cut.
Output: README breaking-change notes in both sub-repos + a verified dual-repo green gate.

This plan depends on 51-01 (firmware) and 51-02 (host) — both Wave-1 framing changes must exist before
the gate is meaningful.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Dual-repo: README edits commit INSIDE each respective submodule (`firestarter/` and `firestarter_app/`)
on branch `v1.10-serial-transport-hardening` — NOT in the meta-repo. The MILESTONES.md v1.10 entry is the
milestone-close artifact (deferred to the v1.10 close phase) — do NOT write it here; the README notes are
the per-repo beta-cut record D-02 requires now.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/51-command-channel-framing-migration-breaking-wire-change/51-CONTEXT.md
@.planning/phases/51-command-channel-framing-migration-breaking-wire-change/51-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the breaking command-channel wire change in both sub-repo READMEs (D-02 / SC3)</name>
  <files>firestarter/README.md, firestarter_app/README.md</files>
  <read_first>
    - firestarter/README.md (find the protocol / firmware-version / changelog-style section to anchor the note; if none, add a short "Breaking changes (v1.10)" subsection near the top of the protocol/usage area)
    - firestarter_app/README.md (same — anchor the host-side note where serial/protocol or version is described)
    - .planning/phases/51-command-channel-framing-migration-breaking-wire-change/51-CONTEXT.md (D-01 only-supported-protocol stance; D-02 documentation-as-guard; D-04 the version probe is framed too)
    - .planning/REQUIREMENTS.md (FRAME-05 wording — "breaking wire-protocol change: firmware and host upgrade lockstep, no mixed-version interop")
  </read_first>
  <action>
    Add a concise BREAKING-CHANGE note to BOTH sub-repo READMEs (wording adapted per repo voice, same
    substance). The note MUST state, in plain operator language:
      - The host→firmware JSON command channel is now wrapped in a COBS frame with a CRC8-CCITT integrity
        byte (the same framing the data-block path already uses since v1.10 Phase 50). The firmware verifies
        CRC8 BEFORE parsing the command — the command channel previously had no checksum.
      - This is a BREAKING wire-protocol change. There is NO mixed-version interop: a new host cannot drive
        old (unframed) firmware and vice-versa. Upgrade BOTH the firmware and the host CLI together (lockstep),
        exactly like the v1.2 Message-ID rework. A mismatched pair simply fails (timeout / decode error).
      - The legacy `{`-peek plaintext command path has been removed; ALL commands — including the firmware
        version probe — are framed (D-04). There is no plaintext command escape hatch.
      - This change is beta-only; nothing is promoted to stable without operator authorization.
    Use the word "breaking" explicitly in each note (the acceptance gate greps for it). Keep it to a short
    subsection (a few sentences + a one-line "Upgrade: reflash firmware AND upgrade the CLI together").
    Do NOT touch MILESTONES.md (deferred to milestone close). Do NOT add any interop/negotiation machinery —
    documentation IS the guard per D-01/D-02.
  </action>
  <verify>
    <automated>grep -in "breaking" /workspaces/firestarter/README.md /workspaces/firestarter_app/README.md && grep -il "cobs\|crc8\|lockstep\|frame" /workspaces/firestarter/README.md /workspaces/firestarter_app/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "breaking" firestarter/README.md` returns the command-channel note
    - `grep -i "breaking" firestarter_app/README.md` returns the command-channel note
    - Each README note mentions COBS/CRC8 framing AND lockstep upgrade (no mixed-version interop)
    - Each README note states the change is beta-only / stable promotion is operator-gated
    - `grep -ri "negotiat\|fallback\|interop guard\|dual-protocol" firestarter/README.md firestarter_app/README.md` returns nothing in the new notes (D-01 — documentation is the only guard, no machinery)
  </acceptance_criteria>
  <done>Both sub-repo READMEs carry a breaking-change note covering COBS/CRC8 framing, the lockstep no-interop upgrade requirement, the removal of the plaintext command path, and beta-only status.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-repo green gate + firmware/host constant-parity check</name>
  <files>firestarter/README.md, firestarter_app/README.md</files>
  <read_first>
    - firestarter/include/firestarter.h (CMD_FRAME_MAX define — must match host)
    - firestarter_app/firestarter/constants.py (CMD_FRAME_MAX = 512 — must match firmware)
    - .planning/phases/51-command-channel-framing-migration-breaking-wire-change/51-RESEARCH.md (§Validation Architecture — full-suite commands and the Phase-50 28/28 fw + 408/408 host green baseline)
  </read_first>
  <action>
    This is a verification gate — no production-code edits (the README edits in Task 1 are already committed;
    no further file writes are expected unless a regression surfaces a doc-only fix). Run the full dual-repo
    test suite and confirm everything merged from Wave 1 (51-01 firmware + 51-02 host) is green together:
      - Firmware: `pio test -e native` from `/workspaces/firestarter` — full native suite including the new
        `test_cobs_cmd_frame` and the Phase-50 `test_cobs_data_frame` regression.
      - Host: `python -m pytest --cov-fail-under=70` from `/workspaces/firestarter_app` — full suite incl. the
        new framed `test_serial_comm.py` cases and the Phase-50 `test_cobs.py` regression, coverage floor held.
    Confirm firmware/host constant parity: `CMD_FRAME_MAX` resolves to 512 in BOTH `firestarter.h`
    (= DATA_BUFFER_SIZE) and `constants.py` (= 512). If either suite is red or the constants disagree, the
    gate FAILS — surface the failure for a follow-up fix rather than papering over it. Record the green
    counts (fw test count, host test count + coverage %) in the SUMMARY as the Phase-51 gate evidence that
    Phase 52 (round-trip/lockstep contract) builds on.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -15; echo "===HOST==="; cd /workspaces/firestarter_app && python -m pytest --cov-fail-under=70 2>&1 | tail -15; echo "===PARITY==="; grep -H "CMD_FRAME_MAX" /workspaces/firestarter/include/firestarter.h /workspaces/firestarter_app/firestarter/constants.py</automated>
  </verify>
  <acceptance_criteria>
    - behavior: `pio test -e native` exits 0 (full firmware native suite green, incl. test_cobs_cmd_frame + test_cobs_data_frame)
    - behavior: `python -m pytest --cov-fail-under=70` exits 0 (full host suite green at the 70% coverage floor)
    - `CMD_FRAME_MAX` resolves to 512 in both firestarter.h (via DATA_BUFFER_SIZE) and constants.py — constant parity holds
    - SUMMARY records the firmware test count, host test count, and coverage % as the dual-repo gate evidence
  </acceptance_criteria>
  <done>The merged Wave-1 firmware + host changes pass the full dual-repo suite together; firmware/host CMD_FRAME_MAX parity is confirmed; gate evidence is recorded for Phase 52.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → upgrade procedure | A mismatched firmware/host pair (one framed, one unframed) is unsupported under D-01; the only guard is the documented lockstep-upgrade requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Repudiation / Misconfiguration | mismatched-version host↔firmware pair | mitigate | D-01 accepts that a mismatched pair fails (timeout / decode error) — no interop machinery. The breaking-change README notes (D-02) are the SC3 guard equivalent: the operator is told to upgrade both repos in lockstep. Asserted by the `grep -i breaking` README gate. |
| T-51-08 | Tampering | firmware/host constant drift | mitigate | `CMD_FRAME_MAX` parity check (firestarter.h ↔ constants.py both = 512) in the gate prevents silent divergence of the command-frame size cap across repos (CLAUDE.md constant-parity rule). |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — documentation + test-run gate only. No legitimacy gate required. |

No HIGH-severity threat is left without an in-plan mitigation. (The load-bearing CRC8-before-parse / DoS / SAFE-01 threats are mitigated and asserted in plans 51-01 and 51-02; this plan documents the breaking change and gates the merge.)
</threat_model>

<verification>
- `grep -i breaking` finds the command-channel note in both sub-repo READMEs
- `pio test -e native` exits 0 (firmware)
- `python -m pytest --cov-fail-under=70` exits 0 (host)
- CMD_FRAME_MAX = 512 in both firestarter.h and constants.py
</verification>

<success_criteria>
- FRAME-05 SC3: breaking lockstep wire change documented for the beta cut (D-02 documentation-as-guard)
- Dual-repo gate green: both suites pass together against the merged framing changes
- Constant parity: CMD_FRAME_MAX identical across firmware and host
</success_criteria>

<output>
Create `.planning/phases/51-command-channel-framing-migration-breaking-wire-change/51-03-SUMMARY.md` when done.
Commit README edits inside each respective submodule on branch `v1.10-serial-transport-hardening`.
</output>
