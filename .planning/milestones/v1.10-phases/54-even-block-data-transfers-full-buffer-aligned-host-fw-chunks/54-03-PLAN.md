---
phase: 54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks
plan: 03
type: execute
wave: 2
depends_on: [54-01, 54-02]
files_modified: []
autonomous: true
requirements: [EVEN-01]

must_haves:
  truths:
    - "Uno SRAM after the firmware change stays under the ~545 B free-RAM ceiling (DATA used <= 1503 bytes)"
    - "uno328pb SRAM after the firmware change stays under the ceiling"
    - "The Leonardo build remains clean"
    - "Both repos' full test suites are green at the new full-buffer block size (lockstep contract intact)"
    - "The frame-vectors codegen drift gate is clean in both repos"
  artifacts:
    - path: ".planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-03-SUMMARY.md"
      provides: "Captured RAM reports + dual-repo green gate evidence"
  key_links:
    - from: "pio run -e uno"
      to: "~545 B free-RAM ceiling"
      via: "DATA used grep assertion"
      pattern: "DATA.*used"
---

<objective>
Close Phase 54 with the hard gates: capture the post-change `pio run -e uno` and
`pio run -e uno328pb` RAM reports and assert each stays under the ~545 B free-RAM ceiling
(`DATA used <= 1503` bytes — the Phase 50 baseline), even though Candidate A is zero-growth
(D-08 is a HARD close gate regardless). Then prove the lockstep contract stayed green at the
new full-buffer block size: run both repos' full test suites and confirm the `frame-vectors`
codegen drift gate is clean in both repos (D-07 — the existing 512/1024 vectors now decode on
the MAIN path; no TOML edit was needed, so the drift gate must pass without a regen).

Purpose: SC3 (RAM-fit confirmed on Uno + uno328pb) and SC4 (Phase 52 lockstep contract +
round-trip tests stay green) are the two phase-close criteria that gate `/gsd-verify-work`.
This plan is the integration gate after the firmware (54-01) and host (54-02) halves land.

Output: A SUMMARY capturing the RAM numbers, the dual-repo full-suite results, and the
drift-gate status — the evidence record for SC3/SC4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Firmware builds run from `/workspaces/firestarter`; host tests from `/workspaces/firestarter_app`.
Both on branch `v1.10-serial-transport-hardening`. This plan modifies no source files — it runs
gates and records evidence. Per the devcontainer note: `node` is not on PATH for the SDK; the
codegen drift gate uses each repo's `codegen_vectors.py --check` (Python), not the SDK.
</execution_context>

<context>
@.planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-CONTEXT.md
@.planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-RESEARCH.md
@.planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-01-SUMMARY.md
@.planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RAM gate (Uno + uno328pb) + Leonardo clean build</name>
  <files>(no source files — build + capture only)</files>
  <read_first>
    - 54-RESEARCH.md § "D-08: RAM Gate Specification" (exact commands + PASS criterion `DATA used <= 1503`)
    - 54-CONTEXT.md D-08 (hard phase-close gate, even for zero-growth Candidate A)
    - .planning/STATE.md line 93 (Uno RAM baseline 1503/2048 used, 545 B free)
    - firestarter/platformio.ini (env names: uno, uno328pb, leonardo)
  </read_first>
  <action>
    Build the firmware for Uno and capture the SRAM line:
    `cd /workspaces/firestarter && pio run -e uno 2>&1 | grep -Ei "RAM|DATA|bytes"`.
    Repeat for uno328pb: `pio run -e uno328pb 2>&1 | grep -Ei "RAM|DATA|bytes"`.
    Assert the `DATA used` (static SRAM) value is `<= 1503` bytes on BOTH envs — the Phase 50
    baseline / ~545 B free-RAM ceiling. Candidate A is zero-growth (the `cap` parameter lives in a
    register / 2-byte stack slot already in the call frame), so the gate should pass with the same
    or near-identical number as the baseline; if `DATA used` exceeds 1503 on either env, STOP and
    flag — the mechanism's zero-growth claim is violated and the phase cannot close. Also run
    `pio run -e leonardo` and confirm it builds cleanly (no RAM ceiling assertion required on
    Leonardo's 2.5 KB SRAM, but the build must succeed). Record the three build outputs verbatim in
    the SUMMARY (board, DATA used bytes, % SRAM, PASS/FAIL).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio run -e uno 2>&1 | grep -Ei "RAM|DATA.*used" && pio run -e uno328pb 2>&1 | grep -Ei "RAM|DATA.*used" && pio run -e leonardo 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pio run -e uno` reports `DATA used` <= 1503 bytes (under the ~545 B free-RAM ceiling).
    - `pio run -e uno328pb` reports `DATA used` <= 1503 bytes.
    - `pio run -e leonardo` completes successfully (clean build).
    - All three RAM/build outputs are recorded verbatim in the SUMMARY with explicit PASS/FAIL per board.
  </acceptance_criteria>
  <done>D-08 hard RAM gate passes on Uno and uno328pb; Leonardo builds clean; numbers captured.</done>
</task>

<task type="auto">
  <name>Task 2: Dual-repo full-suite green + frame-vectors drift gate (lockstep contract, SC4)</name>
  <files>(no source files — full suites + drift gate only)</files>
  <read_first>
    - 54-RESEARCH.md § "D-07: Regression Mechanism" (existing 512/1024 vectors serve as the even-block corpus; TOML unchanged → drift gate clean without regen) and § "Validation Architecture" (full-suite commands)
    - 54-CONTEXT.md § "Lockstep / regression mechanism (D-07)" (both repos' frame-vectors.toml byte-identical; `codegen.py` + `git diff --exit-code` drift gate)
    - 54-PATTERNS.md § "Drift Gate Reminder (D-07)"
    - firestarter/tools/catalog/frame-vectors.toml and firestarter_app/tools/catalog/frame-vectors.toml (confirm byte-identity if any vector was added by 54-01/54-02)
  </read_first>
  <action>
    Run the firmware full native suite: `cd /workspaces/firestarter && pio test -e native` — all
    suites (test_messages, test_cobs_data_frame, test_cobs_cmd_frame, test_frame_vectors) must be
    green. Run the host full suite: `cd /workspaces/firestarter_app && pytest --cov=firestarter
    --cov-fail-under=70` — must pass with coverage at or above the 70% floor (note: the floor has
    ~0 headroom per project history; if the new `test_even_block.py` tests nudge coverage, confirm
    it stays >= 70). Run the frame-vectors codegen drift gate in BOTH repos (each repo's
    `codegen_vectors.py --check`, or the project's `<regen> && git diff --exit-code` step) and
    confirm it is clean — per RESEARCH the TOML was NOT modified (the existing 512/1024 vectors
    already cover the even block), so the gate passes without a regen step. If 54-01/54-02 added any
    new named vectors, instead run the regen and assert `git diff --exit-code` is empty for both
    repos' `frame-vectors.toml` + generated `frame_vectors.h` / `frame_vectors.py` (byte-identity,
    D-09). Confirm `CMD_FRAME_MAX` parity (firmware `firestarter.h` == host `constants.py` == 512)
    is unbroken. Record all results in the SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native 2>&1 | tail -10 && cd /workspaces/firestarter_app && pytest --cov=firestarter --cov-fail-under=70 -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pio test -e native` is fully green (all four allowlisted suites pass).
    - `pytest --cov=firestarter --cov-fail-under=70` passes in `firestarter_app` (coverage >= 70%).
    - The frame-vectors codegen drift gate is clean in both repos (either no-regen-needed with TOML unchanged, or `git diff --exit-code` empty after regen if vectors were added).
    - `CMD_FRAME_MAX` parity holds (firmware == host == 512).
    - SUMMARY records firmware suite pass count, host suite pass count + coverage %, and drift-gate status.
  </acceptance_criteria>
  <done>Both repos' full suites are green at the new full-buffer block size and the lockstep/drift gate is clean — SC4 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/CI gate | No runtime data boundary — this plan runs builds and tests; no new code or wire surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Tampering | frame-vectors lockstep drift between repos | mitigate | The `codegen_vectors.py --check` / `git diff --exit-code` drift gate in BOTH repos is run as an explicit acceptance criterion (Task 2); divergence between the two vendored `frame-vectors.toml` copies fails the gate rather than silently shipping a contract mismatch. |
| T-54-SC | Tampering | pip/pio installs | accept | No new packages installed; this plan only runs existing build + test tooling. |

No HIGH-severity threats: this is a build/test gate plan with no source changes.
</threat_model>

<verification>
- Uno + uno328pb `DATA used` <= 1503 bytes; Leonardo clean.
- `pio test -e native` green; `pytest --cov-fail-under=70` green.
- Drift gate clean in both repos; `CMD_FRAME_MAX` parity holds.
</verification>

<success_criteria>
- EVEN-01 SC3: RAM-fit confirmed on Uno (2 KB) and uno328pb under the ceiling (D-08 hard gate).
- EVEN-01 SC4: Phase 52 lockstep contract + round-trip tests green at the new block size; drift gate clean.
- Phase 54 ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/54-even-block-data-transfers-full-buffer-aligned-host-fw-chunks/54-03-SUMMARY.md` when done — include the verbatim RAM reports, dual-repo suite results, and drift-gate status.
</output>
