---
phase: 55-relocate-buffer-size-advertisement-operation-ok-ack
plan: 04
type: execute
wave: 3
depends_on: ["55-02", "55-03"]
files_modified: []
autonomous: false
requirements: [CAP-01]
must_haves:
  truths:
    - "messages.toml is byte-identical across the meta-repo and both sub-repos (drift gate clean)"
    - "Firmware native suite green and host full suite green at >=70% coverage in their respective repos"
    - "EVEN-01 preserved: Phase 52 lockstep round-trip / golden-vector tests stay green at the new contract"
    - "firmware/host constant parity preserved (CMD_FRAME_MAX still 512)"
  artifacts: []
  key_links:
    - from: "tools/catalog/sync_to_subrepos.sh"
      to: "firestarter/include/messages.h + firestarter_app/firestarter/messages.py"
      via: "byte-identity assertion + git diff --exit-code in CI drift gate"
      pattern: "messages.toml"
---

<objective>
Tie the firmware (Plan 02) and host (Plan 03) halves together and prove the dual-repo
contract: messages.toml byte-identical across all repos, both full suites green, EVEN-01
and Phase 52 lockstep preserved, and constant parity intact. This is the SC5 integration gate
and runs last (Wave 3) after both repo plans land.

This plan writes no source — it is a pure verification gate that re-runs the drift gate, the
full firmware native suite, the full host suite with coverage, and the EVEN-01 / Phase 52
lockstep regressions. If any gate fails, the SUMMARY records the failure for gap-closure rather
than declaring the phase done.

Purpose: Close CAP-01 SC5 (and confirm SC4) — the bench (Phase 53) must verify the FINAL
transport, so this gate ensures the catalog and both repos are coherent before bench.
Output: A verification SUMMARY recording drift-gate, suite, parity, and RAM results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-RESEARCH.md
@.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the drift gate + dual-repo full suites + EVEN-01/parity regressions</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - /workspaces/tools/catalog/sync_to_subrepos.sh (the byte-identity assertion this gate re-runs)
    - /workspaces/firestarter/.github/workflows/build.yml (firmware CI drift gate: codegen --check + --language cpp + git diff --exit-code include/messages.h)
    - /workspaces/firestarter_app/.github/workflows/ci.yml (host CI drift gate + ruff + mypy strict-8 + pytest --cov-fail-under=70)
    - /workspaces/firestarter_app/tests/test_revision_constants_parity.py (CMD_FRAME_MAX / CTRL_* / REVISION_* parity — must stay green; CMD_FRAME_MAX unchanged at 512)
    - /workspaces/firestarter_app/tests/test_frame_vectors.py and the firmware test_frame_vectors / test_cobs_* Unity suites (Phase 52 lockstep round-trip / golden-vector regressions — must stay green at the new MSG_OK_READY shape)
    - .planning/phases/.../55-RESEARCH.md §5 (drift + parity procedure) and §6 (tests that must stay green); 55-VALIDATION.md "Sampling Rate" / "Before /gsd-verify-work"
  </read_first>
  <action>
    Re-run `bash /workspaces/tools/catalog/sync_to_subrepos.sh` and confirm it exits 0 with no
    file changes (idempotent — Plan 01 already synced; this proves no drift crept in from the repo
    plans). Run `python3 firestarter/tools/catalog/codegen.py --catalog tools/catalog/messages.toml
    --check` and, in each sub-repo, confirm the regenerated artifact matches the committed one
    (`git diff --exit-code` on firestarter/include/messages.h and
    firestarter_app/firestarter/messages.py — the same assertion CI runs). Run the full firmware
    native suite (`cd /workspaces/firestarter && pio test -e native`) and the full host suite
    (`cd /workspaces/firestarter_app && pytest --cov=firestarter --cov-fail-under=70`). Explicitly
    confirm the EVEN-01 / Phase 52 lockstep regressions are in the green set
    (test_frame_vectors.py host + the firmware test_frame_vectors / test_cobs_* suites) and that
    test_revision_constants_parity.py passes (CMD_FRAME_MAX still 512 — no parity change). Record the
    Plan-02 Uno RAM line in the SUMMARY as the D-08-spirit close gate. If any gate fails, do NOT mark
    the phase done — record the failing gate and the exact command output in the SUMMARY for
    gap-closure.
  </action>
  <verify>
    <automated>cd /workspaces && bash tools/catalog/sync_to_subrepos.sh && git -C firestarter diff --exit-code include/messages.h && git -C firestarter_app diff --exit-code firestarter/messages.py && (cd firestarter && pio test -e native) && (cd firestarter_app && pytest --cov=firestarter --cov-fail-under=70)</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/catalog/sync_to_subrepos.sh` exits 0 and leaves no uncommitted changes to messages.toml in either sub-repo (byte-identity proven).
    - `git -C firestarter diff --exit-code include/messages.h` and `git -C firestarter_app diff --exit-code firestarter/messages.py` both exit 0 (regenerated == committed).
    - `pio test -e native` (firmware) exits 0 — all native suites including test_messages, test_frame_vectors, test_cobs_* pass.
    - `pytest --cov=firestarter --cov-fail-under=70` (host) exits 0 — full suite green at >=70% coverage, including TestCapSafeDefault, test_serial_comm, test_even_block, test_frame_vectors, and test_revision_constants_parity.
    - The Plan-02 Uno RAM line is recorded in the SUMMARY and is under the ~545 B free ceiling.
  </acceptance_criteria>
  <done>Drift gate clean, both full suites green, EVEN-01 + Phase 52 lockstep + parity preserved, RAM under ceiling — CAP-01 SC4/SC5 closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The relocated buffer-size advertisement: FW identity reverted to `<version>:<board>`, capacity on MSG_OK_READY u16 param, host safe-512 default. All automated gates (drift, dual-repo suites, parity, RAM) are green from Task 1.</what-built>
  <how-to-verify>
    1. Run `firestarter fw` against connected firmware (or the mocked-firmware path) and confirm the printed FW string is `<version>:<board>` only — NO trailing `:512` / `:1024` / maxchunk field (CAP-01 SC1).
    2. (Hardware, optional per operator) On the Leonardo (ACM0 — NOT uno328pb, which browns out on program per project memory): run a full write+verify of a known EPROM image and confirm chunks size to 1024 (full buffer, whole-block transfer, byte-exact), exercising the ack-sourced buffer size end to end (CAP-01 SC4).
  </how-to-verify>
  <resume-signal>Type "approved" once `firestarter fw` shows version:board only (and, if bench-tested, the Leonardo write+verify is byte-exact), or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta-repo catalog -> both sub-repos | The drift gate is the integrity boundary ensuring both repos decode MSG_OK_READY identically; divergence would silently break the ack contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-08 | Tampering | messages.toml drift between repos | mitigate | `sync_to_subrepos.sh` byte-identity assertion + `git diff --exit-code` on both generated artifacts (the same checks CI enforces) run as the gate; any drift fails the task. |
| T-55-09 | Spoofing | constant-parity divergence (firmware vs host) | mitigate | test_revision_constants_parity.py is in the required green set; CMD_FRAME_MAX confirmed unchanged at 512. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package installs — verification-only plan; no dependency surface. |
</threat_model>

<verification>
- Drift gate: `sync_to_subrepos.sh` + `git diff --exit-code` on both generated artifacts → clean.
- Firmware: `pio test -e native` → all suites pass.
- Host: `pytest --cov=firestarter --cov-fail-under=70` → green at >=70%.
- Parity: `test_revision_constants_parity.py` green; CMD_FRAME_MAX == 512.
- Manual: `firestarter fw` prints version:board only (checkpoint).
</verification>

<success_criteria>
- CAP-01 SC4: EVEN-01 preserved — full-buffer chunks, whole-block transfer, Phase 52 lockstep + round-trip green.
- CAP-01 SC5: messages.toml byte-identical across repos; both full suites green; constant parity preserved.
- SC1 confirmed end-to-end via the `firestarter fw` checkpoint.
</success_criteria>

<output>
Create `.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-04-SUMMARY.md` when done. Record drift-gate, suite, parity, and RAM results.
</output>
