---
phase: 55-relocate-buffer-size-advertisement-operation-ok-ack
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - firestarter/include/firestarter.h
  - firestarter/src/firestarter.cpp
  - firestarter/src/hardware_operations.cpp
  - firestarter/src/dev_tools.cpp
  - firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp
autonomous: true
requirements: [CAP-01]
must_haves:
  truths:
    - "FW_VERSION emits <version>:<board> only — no :512:512 buffer/maxchunk suffix"
    - "All 4 MSG_OK_READY emit sites carry DATA_BUFFER_SIZE as a 2-byte param"
    - "Uno RAM after the change stays under the ~545 B free ceiling (post-Phase-54 baseline 496 B free)"
  artifacts:
    - path: "firestarter/include/firestarter.h"
      provides: "Reverted FW_VERSION macro (version:board only)"
      contains: "#define FW_VERSION VERSION"
    - path: "firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp"
      provides: "Unity test asserting MSG_OK_READY emits a 2-byte param frame"
      contains: "MSG_OK_READY"
  key_links:
    - from: "firestarter/src/firestarter.cpp (init_programmer)"
      to: "rurp_log_id_u16 (DATA_BUFFER_SIZE big-endian)"
      via: "LOG_OK_ID_U16(MSG_OK_READY, (uint16_t)DATA_BUFFER_SIZE)"
      pattern: "LOG_OK_ID_U16\\(MSG_OK_READY"
---

<objective>
Revert the firmware identity string to pure `<version>:<board>` and emit the board's
`DATA_BUFFER_SIZE` as a 2-byte param on every `MSG_OK_READY` ack instead. Extend the Unity
message test to pin the new 2-byte param frame.

This is the firmware half of the dual-repo change (SC1 + SC2 firmware side). It depends on
Plan 01 (the regenerated catalog must be in place — the `bytes` param declaration and the
synced `messages.h`). It runs in Wave 2 in parallel with the host plan (03) because the two
plans touch disjoint repos with zero file overlap.

Purpose: Separate firmware *version* from transport *capability* on the wire — version string
clean, capacity advertised per-operation.
Output: Reverted FW_VERSION macro, 4 updated emit sites, and a Unity test asserting the
2-byte param.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-RESEARCH.md
@.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert FW_VERSION macro and update all 4 MSG_OK_READY emit sites</name>
  <files>firestarter/include/firestarter.h, firestarter/src/firestarter.cpp, firestarter/src/hardware_operations.cpp, firestarter/src/dev_tools.cpp</files>
  <read_first>
    - /workspaces/firestarter/include/firestarter.h (lines 16-40: DATA_BUFFER_SIZE define, the Phase 54 comment block at 26-37 documenting the maxchunk field, and the FW_VERSION macro at line 40 currently `VERSION ":" RURP_BOARD_NAME ":" FS_STRINGIFY(DATA_BUFFER_SIZE) ":" FS_STRINGIFY(DATA_BUFFER_SIZE)`)
    - /workspaces/firestarter/include/logging_id.h (lines 123-128: LOG_OK_ID and LOG_OK_ID_U16 macros — LOG_OK_ID_U16 already exists)
    - /workspaces/firestarter/src/boards/rurp_serial_utils.cpp (lines 484-489: rurp_log_id_u16 — packs big-endian 2-byte param; already implemented, no change)
    - /workspaces/firestarter/src/firestarter.cpp (line 142: `LOG_OK_ID(MSG_OK_READY);` operation-setup ack)
    - /workspaces/firestarter/src/hardware_operations.cpp (line 43: VPP/VPE probe ack)
    - /workspaces/firestarter/src/dev_tools.cpp (lines 107 and 153: two dev-tool acks)
    - .planning/phases/.../55-RESEARCH.md §2 (emit-site inventory), §3 (FW_VERSION revert diff)
  </read_first>
  <action>
    In firestarter/include/firestarter.h line 40, change the FW_VERSION macro to
    `VERSION ":" RURP_BOARD_NAME` — strip BOTH `":" FS_STRINGIFY(DATA_BUFFER_SIZE)` suffixes so it
    emits e.g. "3.0.0b8:uno". Replace the Phase 54 comment block at lines ~26-37 (which documents
    the `<maxchunk>` identity field) with a short note that buffer capacity is now advertised as a
    u16 bytes param on every MSG_OK_READY ack (Phase 55 / CAP-01).
    At all 4 emit sites — firestarter.cpp:138, hardware_operations.cpp:43, dev_tools.cpp:107, and
    dev_tools.cpp:153 — change `LOG_OK_ID(MSG_OK_READY);` to
    `LOG_OK_ID_U16(MSG_OK_READY, (uint16_t)DATA_BUFFER_SIZE);`. Preserve the existing trailing
    comments on the dev_tools.cpp sites. Do NOT add any new global buffer — rurp_log_id_u16 uses a
    2-byte local stack array that already exists; no new infrastructure.
    After the edits, run a Uno build and capture the RAM line to confirm it holds under the ceiling
    (baseline 496 B free post-Phase-54; ~8 bytes Flash added, ~0 SRAM).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && grep -q '#define FW_VERSION VERSION ":" RURP_BOARD_NAME$' include/firestarter.h && [ "$(grep -h 'LOG_OK_ID_U16(MSG_OK_READY' src/firestarter.cpp src/hardware_operations.cpp src/dev_tools.cpp | wc -l)" = "4" ] && ! grep -rq 'LOG_OK_ID(MSG_OK_READY)' src/ && pio run -e uno</automated>
  </verify>
  <acceptance_criteria>
    - `include/firestarter.h` line 40 is exactly `#define FW_VERSION VERSION ":" RURP_BOARD_NAME` (no FS_STRINGIFY suffix).
    - Exactly 4 occurrences of `LOG_OK_ID_U16(MSG_OK_READY` across firestarter.cpp, hardware_operations.cpp, dev_tools.cpp.
    - Zero remaining `LOG_OK_ID(MSG_OK_READY)` (the old zero-param form) anywhere under src/.
    - `pio run -e uno` builds successfully; the RAM report line is captured in the SUMMARY and shows free RAM >= 480 B (held under the ~545 B ceiling; regression below baseline 496 B must be flagged, not silently accepted).
  </acceptance_criteria>
  <done>FW_VERSION is version:board only, all 4 emit sites carry the u16 param, and the Uno build passes with RAM under ceiling.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Unity message test to assert MSG_OK_READY emits a 2-byte param frame</name>
  <files>firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp</files>
  <read_first>
    - /workspaces/firestarter/test/native/avr/test_messages/test_rurp_log_id.cpp (test_zero_param_frame at line 87 — the 9-byte zero-param frame template; test_u32_param_frame at line 111 — the param-bearing frame template with len/crc/anchor asserts and ref_crc8 usage; and the RUN_TEST registration block at the bottom)
    - /workspaces/firestarter/src/boards/rurp_serial_utils.cpp (lines 484-489: rurp_log_id_u16 big-endian packing — MSB first)
    - .planning/phases/.../55-VALIDATION.md "Wave 0 Requirements" (the firmware Unity extension item)
  </read_first>
  <action>
    Add a Unity test (e.g. `test_ok_ready_u16_param_frame`) to test_rurp_log_id.cpp that calls
    `rurp_log_id_u16(MSG_OK_READY, 512)` (or invokes via the same helper used by the other param
    tests) and asserts the captured frame is the expected 11-byte sequence: 4 magic bytes
    (0xAA 0x55 0xAA 0x55), len MSB/LSB (len LSB = 1 id + 2 params + 1 crc = 0x04), id byte = 0x01
    (MSG_OK_READY), the 2 big-endian param bytes (0x02 0x00 for 512), the CRC8 over the
    [id][params] body via `ref_crc8(body, 3)`, and the 0x0A re-sync anchor — mirroring the byte-level
    assertion style of test_u32_param_frame. Register the new test with RUN_TEST in the test runner
    block. This pins SC2's "MSG_OK_READY emits 2-byte param correctly" contract.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter && pio test -e native -f "*test_messages*"</automated>
  </verify>
  <acceptance_criteria>
    - `test_rurp_log_id.cpp` contains a new test function asserting a MSG_OK_READY frame with 2 param bytes (0x02 0x00 for 512) and the correct CRC8 over the 3-byte body.
    - The new test is registered with `RUN_TEST(...)` in the runner.
    - `pio test -e native -f "*test_messages*"` exits 0 (all message frame tests pass, including the new one).
  </acceptance_criteria>
  <done>The Unity suite asserts the MSG_OK_READY 2-byte param frame and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware -> host (serial ack) | The firmware emits the MSG_OK_READY param body; a wrong byte order or wrong length would mislead host chunk sizing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Tampering | u16 param byte order on the wire | mitigate | Reuse the already-tested `rurp_log_id_u16` helper (MSB-first); the new Unity test pins the exact byte sequence (0x02 0x00 for 512) and CRC8 so a regression in packing fails CI. |
| T-55-04 | Information Disclosure | over-large advertised capacity (e.g. host sizing for >1024) | accept | The firmware only ever emits the compile-time `DATA_BUFFER_SIZE` (512 Uno / 1024 Leonardo) — it cannot emit a hostile value; the host-side clamp/floor (Plan 03) handles any garbled wire value. |
| T-55-SC | Tampering | npm/pip/cargo installs | accept | No package installs — firmware source + Unity test edits only; no dependency surface. |
</threat_model>

<verification>
- `pio run -e uno` builds; RAM held under the ~545 B free ceiling (captured in SUMMARY).
- `pio test -e native -f "*test_messages*"` passes including the new MSG_OK_READY 2-byte param test.
- `grep` confirms 4 `LOG_OK_ID_U16(MSG_OK_READY` sites and 0 legacy `LOG_OK_ID(MSG_OK_READY)` sites.
- `firestarter fw` against firmware emits `<version>:<board>` only (manual / Plan 04 integration).
</verification>

<success_criteria>
- CAP-01 SC1: FW_VERSION is `<version>:<board>` only.
- CAP-01 SC2 (firmware side): all 4 MSG_OK_READY emit sites carry DATA_BUFFER_SIZE as a u16; the firmware Unity test pins the 2-byte param frame.
- Uno RAM holds under ceiling (FRAME-03 / D-08 spirit preserved).
</success_criteria>

<output>
Create `.planning/phases/55-relocate-buffer-size-advertisement-operation-ok-ack/55-02-SUMMARY.md` when done. Record the Uno RAM line.
</output>
