---
phase: 50-data-path-framing-layer-automatic-resync-dual-repo-lockstep
plan: 04
type: execute
wave: 3
depends_on: ["50-02", "50-03"]
files_modified:
  - firestarter/platformio.ini
  - .planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-RAM-REPORT.md
autonomous: false
requirements: [FRAME-03, FRAME-04]
must_haves:
  truths:
    - "A post-change pio run -e uno RAM report shows free RAM >= ~545 B (no second ~512 B encode buffer materialized) — proven by the Plan-01 check_uno_ram.sh gate passing against the rewritten firmware"
    - "The full firmware native suite and the full host pytest suite are both green after the dual-repo lockstep change"
    - "The Leonardo DATA_BUFFER_SIZE A/B-test pin (currently 512, a TEMP override of the FRAME-04 nominal 1024) is surfaced for operator decision — either restored to 1024 or explicitly documented as a deliberate A/B pin, not silently shipped"
    - "The 512 B (Uno) and 1024 B (Leonardo) full-buffer payloads frame without operator-visible re-chunking (COBS is size-agnostic)"
  artifacts:
    - path: ".planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-RAM-REPORT.md"
      provides: "Post-change Uno RAM figures vs the Phase-49 baseline (FRAME-03 evidence)"
      contains: "545"
  key_links:
    - from: "firestarter/scripts/check_uno_ram.sh"
      to: "pio run -e uno"
      via: "RAM-ceiling gate run post-change"
      pattern: "pio run -e uno"
---

<objective>
Close Phase 50 with the integration gates that require BOTH sub-repos to be implemented: run the
post-change Uno RAM report (FRAME-03 proof — no second ~512 B buffer, ≥545 B free), confirm the full
firmware native suite and full host pytest suite are green together (dual-repo lockstep), surface the
Leonardo `DATA_BUFFER_SIZE=512` A/B-test pin for an operator decision (FRAME-04 "1024 B Leonardo"),
and capture the RAM evidence artifact.

Purpose: FRAME-03 is a build-report property that can only be proven against the rewritten firmware
(Plan 02), and the lockstep contract requires both repos green at once. The Leonardo buffer pin is a
known watch-item (currently a 512 B TEMP override of the nominal 1024) that must not ship silently.

Output: a RAM-report artifact, both suites green, and a resolved Leonardo buffer disposition (restored
to 1024 OR documented A/B pin) — operator-gated.

Per D-03, NO interim version/interop guard is added in Phase 50 — this is accepted as a breaking
beta lockstep upgrade (no mixed-version interop, cf. v1.2); the breaking nature is documented in the
RAM-report/summary, and the version/handshake guard lands in Phase 51. Nothing is promoted to stable
without operator authorization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.10-FRAMING-DECISION.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-RESEARCH.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-VALIDATION.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-02-PLAN.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-change Uno RAM proof + dual-repo full-suite green gate</name>
  <files>.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-RAM-REPORT.md</files>
  <read_first>
    - firestarter/scripts/check_uno_ram.sh (the Plan-01 gate this runs post-change; the 545 B floor variable)
    - .planning/phases/.../50-RESEARCH.md ("Baseline RAM report captured this session" — 1503/2048 B used, 545 B free, Flash 22492/32256; the comparison target)
    - .planning/v1.10-FRAMING-DECISION.md §4.5 (no second ~512 B buffer; ~6 B stack; ~545 B ceiling)
    - .planning/phases/.../50-VALIDATION.md (Sampling Rate "Per wave merge" + "Phase gate" — both full suites green AND RAM under ceiling before /gsd-verify-work)
  </read_first>
  <action>
    Run `cd firestarter && bash scripts/check_uno_ram.sh` against the Plan-02 rewritten firmware; it must
    exit 0 (free RAM ≥ 545 B, no second ~512 B buffer). Run the full firmware native suite
    `pio test -e native` (all green, including the Plan-01 COBS suite and the unchanged log/telemetry suite).
    Run the full host suite `cd firestarter_app && python -m pytest --cov-fail-under=70` (all green, coverage
    floor held). Capture the post-change Uno RAM `used/free` figures and the Flash figure into
    `50-RAM-REPORT.md`, tabulated against the Phase-49 baseline (1503/2048 B used → 545 B free), with an
    explicit statement that no second ~512 B static buffer was materialized (cite the COBS state cost ~6 B
    stack). If the RAM gate fails, STOP and report — do not paper over a regression.
  </action>
  <verify>
    <automated>cd firestarter && bash scripts/check_uno_ram.sh && pio test -e native && cd ../firestarter_app && python -m pytest --cov-fail-under=70</automated>
  </verify>
  <acceptance_criteria>
    - `bash firestarter/scripts/check_uno_ram.sh` exits 0 (free RAM ≥ 545 B post-change)
    - `pio test -e native` (full firmware suite) exits 0
    - `python -m pytest --cov-fail-under=70` (full host suite) exits 0
    - `50-RAM-REPORT.md` exists, contains the post-change used/free figures, the baseline comparison (545), and the no-second-buffer statement (grep `545` and `second` / `no second`)
  </acceptance_criteria>
  <done>The post-change Uno RAM gate passes (≥545 B free, no second buffer), both full suites are green, and the RAM evidence is captured.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Leonardo DATA_BUFFER_SIZE A/B-pin disposition (FRAME-04)</name>
  <read_first>
    - firestarter/platformio.ini (`[env:leonardo]` at ~line 57; the `-D DATA_BUFFER_SIZE=512` pin at ~line 65 with the "TEMP: 512 to match Uno for buffer-size A/B test (was 1024)" comment)
    - .planning/phases/.../50-RESEARCH.md (Project Constraints — the Leonardo 512 B A/B pin vs FRAME-04 "1024 B Leonardo" flag; COBS is size-agnostic so this does not block Phase 50)
    - .planning/REQUIREMENTS.md (FRAME-04 — "512 B (Uno) and 1024 B (Leonardo) transfers complete through the new framing transparently")
  </read_first>
  <decision>
    The live `[env:leonardo]` build pins `DATA_BUFFER_SIZE=512` as a TEMP A/B-test override of the
    nominal 1024. FRAME-04 specifies "1024 B Leonardo." COBS is size-agnostic, so this does not block the
    framing change — but the Leonardo build must not silently ship at 512 B without an explicit operator call.
  </decision>
  <context>
    The 512 B pin was set as a buffer-size A/B test (linked to the v1.9 read-bug investigation). Restoring
    1024 changes the Leonardo data-buffer back to its nominal capacity; keeping 512 preserves the A/B
    condition but means FRAME-04's "1024 B Leonardo" is exercised only by changing the define. This is a
    deliberate decision the operator owns (it intersects the paused v1.9 RCA), not a planner call.
  </context>
  <options>
    <option id="restore-1024">
      <name>Restore DATA_BUFFER_SIZE=1024 for Leonardo</name>
      <pros>Matches FRAME-04 nominal; Leonardo ships at full buffer; the 1024 B framing path is the shipped path.</pros>
      <cons>Ends the in-progress buffer-size A/B condition tied to the v1.9 read-bug investigation.</cons>
    </option>
    <option id="keep-512-documented">
      <name>Keep 512 but document it as a deliberate A/B pin in the phase summary</name>
      <pros>Preserves the v1.9 A/B condition; COBS still frames correctly at 512.</pros>
      <cons>FRAME-04's "1024 B Leonardo" remains exercised only by flipping the define; the shipped Leonardo build is 512 B.</cons>
    </option>
  </options>
  <action>
    Present both options to the operator. If `restore-1024` is chosen, change the `[env:leonardo]`
    `-D DATA_BUFFER_SIZE=512` to `1024` (update/remove the TEMP comment) and re-run `pio run -e leonardo`
    to confirm it builds. If `keep-512-documented` is chosen, leave `platformio.ini` unchanged and record
    the deliberate-pin disposition (with the v1.9 A/B rationale) in `50-RAM-REPORT.md` / the plan summary.
    Either way, note the 1024 B COBS framing path is validated by the size-agnostic round-trip tests
    (Plan 01/02/03), independent of the shipped Leonardo define.
  </action>
  <verify>
    <automated>cd firestarter && grep -n "DATA_BUFFER_SIZE" platformio.ini</automated>
  </verify>
  <resume-signal>Select: restore-1024 or keep-512-documented</resume-signal>
  <acceptance_criteria>
    - The chosen disposition is reflected: either `platformio.ini` `[env:leonardo]` shows `DATA_BUFFER_SIZE=1024` and `pio run -e leonardo` builds clean, OR the 512 B pin is left intact AND the deliberate-A/B-pin rationale is recorded in `50-RAM-REPORT.md` / the summary
    - FRAME-04's 1024 B path is noted as validated by the size-agnostic COBS round-trip tests regardless of the shipped Leonardo define
  </acceptance_criteria>
  <done>The Leonardo buffer-size disposition is operator-decided and either applied (1024 restored, builds clean) or explicitly documented (512 A/B pin), with the 1024 B framing path noted as test-validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build artifact → AVR RAM | The FRAME-03 RAM gate verifies the rewritten decoder/encoder did not materialize a second ~512 B buffer that would overrun the ~545 B free-RAM ceiling on the Uno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | DoS | a second ~512 B encode/decode buffer slipping in and exhausting Uno RAM | mitigate | `check_uno_ram.sh` gate fails the phase if free RAM < 545 B; the RAM-report artifact records the post-change figures (acceptance: gate exits 0, report cites 545) |
| T-50-06 | Tampering | shipping a 512 B Leonardo build that diverges from the FRAME-04 1024 B contract without an explicit decision | accept (operator-gated) | Blocking decision checkpoint surfaces the A/B pin; disposition recorded, not silently shipped |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `cd firestarter && bash scripts/check_uno_ram.sh` exits 0 against the rewritten firmware (FRAME-03).
- `cd firestarter && pio test -e native` and `cd firestarter_app && python -m pytest --cov-fail-under=70` both green (dual-repo lockstep gate).
- `50-RAM-REPORT.md` records post-change RAM vs the 545 B baseline + the no-second-buffer statement.
- Leonardo buffer disposition applied or documented per the operator decision.
</verification>

<success_criteria>
FRAME-03 is proven by a passing post-change Uno RAM gate (≥545 B free, no second buffer) with the
evidence captured; both full suites are green together (firmware native + host pytest); FRAME-04's
512/1024 B full-buffer framing is validated by the size-agnostic round-trip tests and the Leonardo
buffer-size pin is resolved by an explicit operator decision (restored to 1024 or documented A/B pin).
</success_criteria>

<output>
Create `.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-04-SUMMARY.md` when done
</output>
