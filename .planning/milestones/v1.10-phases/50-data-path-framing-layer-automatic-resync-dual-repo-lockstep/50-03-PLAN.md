---
phase: 50-data-path-framing-layer-automatic-resync-dual-repo-lockstep
plan: 03
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - firestarter_app/firestarter/frame_parser.py
  - firestarter_app/firestarter/eprom_operations.py
autonomous: true
requirements: [FRAME-01, FRAME-02, FRAME-04, CRC-01]
must_haves:
  truths:
    - "frame_parser.py exposes cobs_encode and cobs_decode, reusing the existing _crc8_ccitt/_build_crc8_table unchanged (no new CRC routine, no polynomial swap)"
    - "cobs_decode raises a clean ValueError on a 0x00 inside the body or a run that exceeds the buffer, enabling bounded resync (drain to next 0x00, never silently accept)"
    - "_main_phase_send_data builds the data frame as b'#' + cobs_encode(chunk + bytes([crc8])) + b'\\x00' as ONE bytes object passed to send_bytes in a single call (atomic-write mandate)"
    - "The len_u16 prefix and XOR checksum are removed from the host data-block send path"
    - "_read_and_parse_lines (the GATE-1.8d ring-fenced RX path) and the MSG_DATA_CHUNK read path are UNTOUCHED — reads stay on the magic-preamble frame"
  artifacts:
    - path: "firestarter_app/firestarter/frame_parser.py"
      provides: "cobs_encode / cobs_decode helpers; CRC8 reused unchanged"
      contains: "def cobs_decode("
    - path: "firestarter_app/firestarter/eprom_operations.py"
      provides: "COBS-framed data-block send (write path) — contents-only change to _main_phase_send_data"
      contains: "cobs_encode"
  key_links:
    - from: "firestarter_app/firestarter/eprom_operations.py"
      to: "firestarter.frame_parser.cobs_encode"
      via: "import + frame build"
      pattern: "cobs_encode\\(data_chunk"
    - from: "firestarter_app/firestarter/eprom_operations.py"
      to: "self.comm.send_bytes"
      via: "single atomic send of b'#' + body + b'\\x00'"
      pattern: "send_bytes\\(frame\\)"
---

<objective>
Implement the host half of the Phase-50 data-block framing in lockstep with the firmware (Plan 02):
add `cobs_encode`/`cobs_decode` to `frame_parser.py` (reusing the existing CRC8 unchanged) and change
the WRITE-path frame CONTENTS in `eprom_operations.py:_main_phase_send_data` to
`b"#" + cobs_encode(chunk + bytes([crc8])) + b"\x00"`, assembled as one `bytes` object and sent in a
single `send_bytes()` call. Greens the Wave-0 host pytest from Plan 01.

Purpose: the host must emit the exact `[COBS(payload+CRC8)][0x00]` frame the firmware decoder (Plan 02)
consumes — dual-repo lockstep. The read RX path is explicitly NOT touched (D-06 Option A: reads stay on
the frozen `MSG_DATA_CHUNK` magic-preamble frame; `_read_and_parse_lines` is GATE-1.8d ring-fenced).

Output: COBS codec helpers (stdlib-only, mypy-strict clean) and a contents-only change to the atomic
write-send path. No host-side COBS data-frame RX path is added (none exists on the read path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.10-FRAMING-DECISION.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-RESEARCH.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-PATTERNS.md
@.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cobs_encode / cobs_decode to frame_parser.py (reuse CRC8 unchanged)</name>
  <files>firestarter_app/firestarter/frame_parser.py</files>
  <read_first>
    - firestarter_app/firestarter/frame_parser.py (the existing leaf byte-loop functions: `_build_crc8_table` ~lines 28-44, `_crc8_ccitt` ~lines 50-55, `_decode_param` ~lines 58-109 — match their pure-transform style and raise-`ValueError`-on-malformed convention; reuse `_crc8_ccitt` UNCHANGED)
    - .planning/phases/.../50-PATTERNS.md ("frame_parser.py — add cobs_encode/cobs_decode" — the decode reference with implicit-zero / 254-run edges and the ValueError-on-0x00-in-body rule)
    - .planning/phases/.../50-RESEARCH.md (Code Examples cobs_decode reference; Pattern 1 encode; Pitfall 2 254-run/phantom-zero)
    - .planning/v1.10-FRAMING-DECISION.md §4.1/§4.3 (COBS scheme over [payload + CRC8]; encoded body contains no 0x00 by construction)
    - firestarter_app/tests/test_cobs.py (the Plan-01 contract this code must satisfy — the exact round-trip / resync / CRC expectations)
  </read_first>
  <behavior>
    - `cobs_decode(cobs_encode(p)) == p` for empty, single-byte, 0x00-only, 0xFF-only, 300-byte mixed, and 512 B all-zero / random payloads
    - `cobs_encode` output contains no `0x00` byte; `cobs_decode` raises `ValueError` on a `0x00` inside the body or a run length that exceeds the input
    - The framed body is `cobs_encode(payload + bytes([_crc8_ccitt(payload)]))` and the trailing `0x00` is the caller's delimiter (helpers operate on the body only, NOT the delimiter)
  </behavior>
  <action>
    Add `cobs_encode(payload: bytes) -> bytes` and `cobs_decode(encoded: bytes) -> bytes` as pure leaf
    transforms beside `_decode_param`. `cobs_decode` is the reference from PATTERNS.md: walk run-length
    codes, append implicit zero except after a 254-run or at stream end, raise `ValueError` on a `0x00`
    inside the body or a run that overruns the input (the bounded-decode V5 control). `cobs_encode` is the
    inverse: scan runs of ≤254 non-zero bytes, emit a `run_len + 1` code byte then the run bytes, handle the
    phantom-zero / 254-run edges exactly (Pitfall 2). Reuse `_crc8_ccitt`/`_build_crc8_table` UNCHANGED —
    do NOT define a new CRC routine or change the polynomial (D-05/CRC-01). The helpers take/return the COBS
    BODY only (no trailing `0x00` delimiter — the caller appends it). Add full type hints + docstrings
    (`frame_parser.py` is in the strict-8 mypy set). Optionally re-export via `__all__` if the module uses one.
    Do NOT modify `_decode_id_frame`, `Response`, `LogMessage`, `MAGIC_PREAMBLE`, or any magic-preamble parse.
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_cobs.py -x -k "cobs_roundtrip or cobs_full_buffer or crc8_data_payload"</automated>
  </verify>
  <acceptance_criteria>
    - `frame_parser.py` contains `def cobs_encode(` and `def cobs_decode(` (grep both)
    - `cobs_decode`/`cobs_encode` reuse `_crc8_ccitt` is NOT required inside them, but the file defines NO new CRC table or routine beyond the existing `_build_crc8_table`/`_crc8_ccitt` (`grep -cE 'def _.*crc|CRC.*TABLE' frame_parser.py` unchanged from baseline count)
    - `cobs_decode` raises `ValueError` on a `0x00` in the body and on a run overrun (grep `raise ValueError`)
    - The round-trip / full-buffer / CRC pytest cases pass (`pytest tests/test_cobs.py -k "cobs_roundtrip or cobs_full_buffer or crc8_data_payload"` exits 0)
    - `ruff check firestarter_app/firestarter/frame_parser.py` and `mypy firestarter_app/firestarter/frame_parser.py` exit 0
    - `_decode_id_frame` / `MAGIC_PREAMBLE` regions are byte-unchanged (`git diff` of those regions empty)
  </acceptance_criteria>
  <done>cobs_encode/cobs_decode exist in frame_parser.py, round-trip and reject malformed input, reuse the existing CRC8, pass ruff+mypy, and leave the magic-preamble parsing untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch _main_phase_send_data to the COBS frame contents (atomic write)</name>
  <files>firestarter_app/firestarter/eprom_operations.py</files>
  <read_first>
    - firestarter_app/firestarter/eprom_operations.py (the CURRENT `_main_phase_send_data` ~lines 357-391: the `checksum = functools.reduce(operator.xor, ...)` + `header = b"#" + len(...).to_bytes(2,"big") + checksum.to_bytes(1)` to REPLACE; the single `self.comm.send_bytes(header + data_chunk)` atomic call ~line 390 to PRESERVE; the stale `[len_u16][xor]` comment block ~lines 384-389 to update; the `functools`/`operator` imports ~lines 10/13)
    - firestarter_app/firestarter/serial_comm.py (`send_bytes` ~lines 134-148 — already atomic write+flush; do NOT change; do NOT touch `_read_and_parse_lines` ~lines 224-336, ring-fenced GATE-1.8d)
    - .planning/phases/.../50-PATTERNS.md ("eprom_operations.py — _main_phase_send_data" — NEW contents; the import note; the functools/operator unused-import check)
    - .planning/phases/.../50-RESEARCH.md (Pattern 4 atomic-write already satisfied; Trace Target 3 — there is NO host read-path [len][xor] decoder to migrate; the read RX path stays on MSG_DATA_CHUNK)
    - .planning/v1.10-FRAMING-DECISION.md §4.1 (atomic-write mandate: full frame incl. trailing 0x00 as one bytes object, single call)
    - firestarter_app/tests/test_cobs.py (the resync expectation, and any send-path expectation if present)
  </read_first>
  <behavior>
    - For each data chunk, the host builds `crc = _crc8_ccitt(data_chunk)`, `body = cobs_encode(data_chunk + bytes([crc]))`, `frame = b"#" + body + b"\x00"`, and calls `self.comm.send_bytes(frame)` exactly once (no split-write of the delimiter)
    - The 512 B (Uno) and 1024 B (Leonardo) full-buffer chunks frame without re-chunking (FRAME-04 — COBS is size-agnostic; the chunk is whatever `buffer_size` the caller passes)
    - No `len_u16` length prefix and no XOR checksum remain on the send path
  </behavior>
  <action>
    In `_main_phase_send_data`, replace the `[len_u16][xor]` header construction with the COBS frame:
    `crc = _crc8_ccitt(data_chunk)`, `body = cobs_encode(data_chunk + bytes([crc]))`,
    `frame = b"#" + body + b"\x00"`, then `self.comm.send_bytes(frame)` — keep it as ONE `bytes` object and
    ONE `send_bytes` call (atomic-write mandate, §4.1); NEVER split the `0x00` delimiter into a second write.
    Add `from firestarter.frame_parser import cobs_encode, _crc8_ccitt` (or a public re-export). Remove the
    now-unused `functools`/`operator` imports ONLY if no other caller uses them (ruff will flag — verify with
    grep before removing). Update the stale comment block (~lines 384-389) that describes the old
    `[len_u16][xor]` header to describe the COBS frame and name the firmware counterpart
    `rurp_communication_read_data`. Do NOT modify `_main_phase_read_data`, `_read_and_parse_lines`, or any
    `MSG_DATA_CHUNK` handling — reads stay on the frozen frame (D-06 Option A); there is no host read-path
    COBS decoder to add (RESEARCH Trace Target 3).
  </action>
  <verify>
    <automated>cd firestarter_app && python -m pytest tests/test_cobs.py tests/test_eprom_operations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_main_phase_send_data` builds `b"#" + ... + b"\x00"` as one bytes object and calls `send_bytes` once (`grep -E 'cobs_encode\(data_chunk' eprom_operations.py` and `grep -E 'b"#".*b"\\x00"|frame = b"#"' eprom_operations.py` match; exactly one `send_bytes` call in the function)
    - No `len_u16` / XOR remains on the send path (`grep -c 'to_bytes(2, "big")' eprom_operations.py` in that function returns 0; no `operator.xor` reduce remains)
    - `cobs_encode` / `_crc8_ccitt` are imported from `firestarter.frame_parser` (grep the import)
    - `_main_phase_read_data` / `_read_and_parse_lines` / `MSG_DATA_CHUNK` handling are byte-unchanged (`git diff` of `_main_phase_read_data` empty; `serial_comm.py` unchanged: `git -C firestarter_app diff --stat firestarter/serial_comm.py` empty)
    - `python -m pytest tests/test_cobs.py tests/test_eprom_operations.py` exits 0
    - `ruff check firestarter_app/firestarter/eprom_operations.py` exits 0 (no unused-import warnings) and full host `pytest --cov-fail-under=70` stays green
  </acceptance_criteria>
  <done>_main_phase_send_data emits the COBS frame atomically in one send_bytes call, len_u16/XOR removed, imports clean, read RX path and serial_comm untouched, host tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host build → fw wire | The host constructs the data-block frame the firmware will parse; a split-write of the `0x00` delimiter would reopen the SAFE-01 timing window |
| fw wire → host decode | `cobs_decode` is exercised by tests on attacker-shaped bytes; on the live read path the host stays on the frozen MSG_DATA_CHUNK frame (no host COBS RX decode in scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02 | Tampering | `cobs_decode` accepting a corrupted/oversized body | mitigate | `cobs_decode` raises `ValueError` on `0x00`-in-body and run overrun; callers treat as resync, never silently accept (acceptance: grep `raise ValueError`; `cobs_resync` test green) |
| T-50-05 | Tampering / DoS | split-write of the `0x00` delimiter (re-opening the mode-transition aliasing window) | mitigate | Frame assembled as one `bytes` object, single `send_bytes` call (acceptance: one `send_bytes` call, `b"#"+...+b"\x00"` literal) |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — COBS hand-rolled; pyserial already present, unchanged; RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `cd firestarter_app && python -m pytest tests/test_cobs.py` — all COBS cases (round-trip, full-buffer, CRC, resync) GREEN.
- `cd firestarter_app && python -m pytest --cov-fail-under=70` — full host suite green, coverage floor held.
- `ruff check` + `mypy` clean on `frame_parser.py` and `eprom_operations.py` (strict-8 set).
- `git -C firestarter_app diff --stat firestarter/serial_comm.py` empty; `_main_phase_read_data`/`_read_and_parse_lines` unchanged (scope guard).
</verification>

<success_criteria>
The host data-block send path emits `[COBS(payload+CRC8)][0x00]` atomically in one `send_bytes` call;
`cobs_encode`/`cobs_decode` exist in `frame_parser.py` reusing the unchanged CRC8 and rejecting malformed
input; the `len_u16`/XOR header is removed; the Wave-0 host pytest is green; ruff+mypy+coverage gates pass;
the GATE-1.8d ring-fenced RX path and the MSG_DATA_CHUNK read path are untouched.
</success_criteria>

<output>
Create `.planning/phases/50-data-path-framing-layer-automatic-resync-dual-repo-lockstep/50-03-SUMMARY.md` when done
</output>
