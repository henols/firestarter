---
phase: 53-byte-exact-bench-verification-hardware-gated
plan: 05
type: execute
wave: 3
depends_on: ["53-02"]
files_modified:
  - .planning/v1.10/bench-verification/uno328pb/
autonomous: false
requirements: [XACT-03]
must_haves:
  truths:
    - "The uno328pb consistency-check read is re-run N=5 on the HARDENED firmware only (no re-flash of old firmware)"
    - "Timeouts are LOGGED and retried, never aborted; a timeout maps to verdict 2 (hw-error), never collapsed to verdict 1"
    - "The hardened 'after' failure shape (timeout count / 0xff-drift %) is captured and compared against the documented v1.6 'before' shape"
    - "A structured exoneration verdict block states this is transport-exoneration per v1.9-COBS-DECISION §2.0, NOT a per-shield hardware fix; RCA stays deferred to v1.9 Phase 45+"
  artifacts:
    - path: ".planning/v1.10/bench-verification/uno328pb/timeout-retry-log.txt"
      provides: "raw timeout + retry log across the N=5 attempt(s)"
    - path: ".planning/v1.10/bench-verification/uno328pb/exoneration-verdict.txt"
      provides: "D-10 structured before/after + exoneration verdict block"
  key_links:
    - from: "dev consistency-check --runs 5 (uno328pb)"
      to: ".planning/v1.10/bench-verification/uno328pb/"
      via: "per-run binaries (however many complete) + timeout-retry log"
      pattern: "consistency-check"
    - from: "exoneration-verdict.txt"
      to: ".planning/v1.6-EVIDENCE.md + .planning/v1.9-COBS-DECISION.md §2.0"
      via: "cited before-shape + verbatim exoneration line"
      pattern: "transport-exoneration"
---

<objective>
XACT-03 / SC3: Re-test the uno328pb on the hardened transport and record a STRUCTURED transport-exoneration
verdict. Operator-witnessed.

The uno328pb is where the timeout + ~99% 0xff-drift instability bites hardest. Re-run
`dev consistency-check W27C512 --runs 5` on the HARDENED firmware ONLY — do NOT re-flash old firmware
(D-09); the pre-hardening "before" shape is already documented in .planning/v1.6-EVIDENCE.md (Plan 27-04:
~99.4% 0xff floating bus, 100% positions unstable across N=5, 4x N=5 timeouts, 0.47% pairwise divergence
run_01 vs run_02). LOG raw timeouts and retries rather than aborting (D-08; never trust N=1). A timeout
maps to verdict 2 (hw-error), never collapsed to verdict 1 — the v1.6 RCA diagnostic depends on this.

Record a structured exoneration verdict block (D-10) stating: (1) the observed hardened "after" shape,
(2) whether the shape changed vs the documented before-shape, and (3) the explicit line: "transport-
exoneration per v1.9-COBS-DECISION §2.0 — NOT a per-shield hardware fix; the actual RCA stays deferred to
v1.9 Phase 45+." A green OR changed result is still exoneration, not a fix — the verdict block makes that
misread impossible for the resumed RCA.

This phase adds NO transport behavior. The uno328pb sits on /dev/ttyUSB0 (urclock bootloader); verify the
controller reports uno328pb before any read.
Output: timeout-retry log + structured exoneration verdict under .planning/v1.10/bench-verification/uno328pb/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md
@.planning/v1.6-EVIDENCE.md
@.planning/v1.9-COBS-DECISION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bench preconditions — uno328pb hardened firmware + identity verification</name>
  <what-built>Hardened firmware for the uno328pb target + the 53-02 dev consistency-check harness.</what-built>
  <action>Operator-witnessed bench setup. Execute the preconditions in <how-to-verify> (chip-out before sideloading the HARDENED uno328pb firmware only — never re-flash old firmware per D-09; verify controller=uno328pb on the port; record shield rev). No autonomous code execution.</action>
  <read_first>
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md (Question 5 bench protocol; Open Question 2 uno328pb port)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md (D-09: do NOT re-flash old firmware)
  </read_first>
  <how-to-verify>
    MANDATORY operator steps:
    1. CHIP OUT before sideloading the HARDENED firmware (feedback_chip_out_before_sideload). Sideload only
       the hardened build: `cd firestarter && pio run -e uno328pb -t upload --upload-port /dev/ttyUSB0`
       (urclock bootloader). Do NOT re-flash any pre-hardening firmware (D-09).
    2. Verify controller identity at THIS task start: `firestarter -p /dev/ttyUSB0 fw` -> confirm
       `controller:` reports `uno328pb` (ACM/USB numbers shuffle; /dev/ttyUSB0 is historical, re-confirm).
    3. ASK the operator the silkscreen shield rev on the bench and record it.
    4. Re-seat the W27C512 and verify `firestarter id W27C512` (or note if id read itself times out — that
       is itself part of the failure shape to log).
  </how-to-verify>
  <resume-signal>Type confirmed port, controller=uno328pb, shield rev, then "approved".</resume-signal>
  <acceptance_criteria>
    - uno328pb controller identity confirmed on the port at this task start.
    - HARDENED firmware sideloaded chip-out; no pre-hardening firmware flashed (D-09).
    - Silkscreen shield rev recorded.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: N=5 consistency-check with timeout-retry logging + structured exoneration verdict</name>
  <what-built>dev consistency-check (53-02) with the 3-way verdict; timeout -> verdict 2.</what-built>
  <action>Operator runs dev consistency-check --runs 5 on the uno328pb with timeout-retry logging (D-08; never abort, never collapse timeout to verdict 1) and writes the D-10 structured exoneration verdict block citing the v1.6 before-shape and the verbatim v1.9-COBS-DECISION §2.0 line, exactly as scripted in <how-to-verify>. No autonomous code execution.</action>
  <read_first>
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md (D-08 timeout-retry logging; D-09 cite before-shape; D-10 structured verdict block)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md (Question 3 before-shape metrics table + D-10 verdict-block template; Pitfall 3 timeout->verdict 2)
    - .planning/v1.6-EVIDENCE.md (Plan 27-04 uno328pb pre-hardening shape — cite verbatim)
    - .planning/v1.9-COBS-DECISION.md (§2.0 transport-exoneration scope — the verdict line wording)
  </read_first>
  <how-to-verify>
    1. `firestarter dev consistency-check W27C512 --runs 5 --output-dir
       .planning/v1.10/bench-verification/uno328pb/`. Capture every run that completes.
    2. On any timeout: LOG it to timeout-retry-log.txt (run index, timeout, retry attempt) and RETRY the
       run slot — do NOT abort and do NOT count an incomplete run as a divergent run (Pitfall 3). A
       fully-timed-out attempt maps to verdict 2 (hw-error), NEVER verdict 1.
    3. Record the hardened "after" shape: number of completed runs, timeout count, retry count, per-run
       0xff% / 0x00% distribution, pairwise divergence if >=2 runs completed.
    4. Write exoneration-verdict.txt using the D-10 template (53-RESEARCH.md Question 3): cite the v1.6
       before-shape verbatim (~99.4% 0xff, 100% unstable, 4x N=5 timeouts, 0.47% pairwise), fill the
       observed after-shape, state whether the shape changed (YES/NO/PARTIAL), and include the exact line:
       "transport-exoneration per v1.9-COBS-DECISION §2.0 — NOT a per-shield hardware fix; the actual RCA
       stays deferred to v1.9 Phase 45+." Add the clarifying paragraph that a green OR changed result is
       identical in conclusion: it characterizes whether COBS hardening affected the failure mode; it does
       not diagnose the hardware cause (Bug B / Rev 2.0 timing / ATmega328PB read protocol).
  </how-to-verify>
  <resume-signal>Type completed-run count, timeout/retry count, after-shape summary, shape-changed verdict, then "approved".</resume-signal>
  <acceptance_criteria>
    - uno328pb/timeout-retry-log.txt records every timeout + retry; no abort; no timeout counted as a
      divergent (verdict-1) run.
    - uno328pb/exoneration-verdict.txt contains: (1) the cited v1.6 before-shape, (2) the observed
      hardened after-shape, (3) shape-changed = YES/NO/PARTIAL, (4) the verbatim transport-exoneration
      line per v1.9-COBS-DECISION §2.0 with RCA deferred to v1.9 Phase 45+.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware sideload -> chip in socket | uno328pb sideload via urclock drives the bus; chip-out required |
| resumed v1.9 RCA -> this artifact | A misread of a green/changed uno328pb result as a "fix" would corrupt the deferred RCA's premise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-12 | Repudiation | uno328pb result misread as a hardware fix by the resumed RCA | mitigate | Structured D-10 exoneration verdict block with the verbatim §2.0 line + the "green OR changed = still exoneration" clarifying paragraph (Task 2 step 4) |
| T-53-13 | Tampering | timeout collapsed to verdict 1 (FAIL) instead of verdict 2 (hw-error) | mitigate | Pitfall 3 handling: incomplete runs logged + retried, never counted as divergent; verdict 2 preserved |
| T-53-14 | Tampering | physical chip during sideload | mitigate | Mandatory chip-out-before-sideload (Task 1 step 1) |
| T-53-15 | Spoofing | wrong board on a shuffled USB port | mitigate | controller=uno328pb verified on the port at task start (Task 1 step 2) |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
- uno328pb re-run on hardened firmware only (no old-firmware re-flash).
- Timeouts logged + retried; verdict 2 never collapsed to 1.
- exoneration-verdict.txt has the cited before-shape, observed after-shape, shape-changed verdict, and the
  verbatim §2.0 transport-exoneration line.
- No firmware-repo source edits (sideload only).
</verification>

<success_criteria>
- XACT-03 recorded: uno328pb hardened re-test with timeout-retry logging + a structured exoneration
  verdict that the resumed v1.9 RCA cannot misread as a hardware fix (D-08/D-09/D-10).
</success_criteria>

<output>
Create `.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-05-SUMMARY.md` when done.
</output>
