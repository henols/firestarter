---
phase: 53-byte-exact-bench-verification-hardware-gated
plan: 06
type: execute
wave: 4
depends_on: ["53-03", "53-04", "53-05"]
files_modified:
  - .planning/v1.10/bench-verification/SUMMARY.md
autonomous: false
requirements: [XACT-01, XACT-02, XACT-03]
must_haves:
  truths:
    - "A SUMMARY.md aggregates every read/write SHA-256 (clean Uno + Leonardo), the fault-injection log (both directions), and the uno328pb before/after + exoneration verdict"
    - "An operator-witness attestation records which boards, the confirmed silkscreen shield rev per board (D-07), and the date"
    - "The artifact explicitly states whether the GATE-1.8d baseline hash-match (strong form) or self-consistency-only was achieved"
    - "The artifact states the milestone claim: the serial transport is a settled, byte-exact variable for the resumed v1.9 RCA"
  artifacts:
    - path: ".planning/v1.10/bench-verification/SUMMARY.md"
      provides: "milestone evidence artifact (SC4) — full SHA table, fault log, exoneration verdict, attestation"
      contains: "Operator attestation"
  key_links:
    - from: ".planning/v1.10/bench-verification/SUMMARY.md"
      to: "clean-board-*/, fault-injection/, uno328pb/ artifacts"
      via: "aggregated SHA table + log references + exoneration verdict"
      pattern: "SUMMARY"
---

<objective>
SC4: Assemble the milestone evidence artifact under .planning/v1.10/bench-verification/ — the direct
hand-off to the resumed v1.9 Phase 45+ RCA, sufficient for it to treat the serial transport as a settled,
byte-exact variable. Operator-witnessed close.

This phase adds NO transport behavior and NO new harness code. It composes the bench outputs from
53-03 (clean-board read+write), 53-04 (fault injection both directions), and 53-05 (uno328pb re-test +
exoneration verdict) into a single SUMMARY.md mirroring the .planning/v1.6/consistency-check-runs/ layout
(D-11). The exact filenames/sub-structure are planner-refined per the 53-RESEARCH.md Question 4 layout.

Output: .planning/v1.10/bench-verification/SUMMARY.md containing — operator attestation (boards, confirmed
shield revs per D-07, date); SHA-256 table for every read/write run (clean boards + uno328pb); the
GATE-1.8d baseline hash-match result (strong-form MATCH or self-consistency-only per D-05); the
fault-injection summary (both directions, error latency, recovery confirmed); the uno328pb before/after
shape comparison + structured exoneration verdict (D-10); and the explicit milestone claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Compose SUMMARY.md + operator attestation</name>
  <what-built>The aggregated bench evidence from 53-03/04/05.</what-built>
  <action>Operator-witnessed close. Compose .planning/v1.10/bench-verification/SUMMARY.md from the per-leg outputs exactly as scripted in <how-to-verify> (operator attestation with boards/confirmed revs/date, full read+write SHA table, GATE-1.8d hash-match outcome, both-direction fault-injection summary, uno328pb before/after + exoneration verdict, milestone settled-variable claim); operator signs off the attestation.</action>
  <read_first>
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md (Question 4 artifact layout + SUMMARY.md contents list)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md (D-05 strong-form vs self-consistency; D-10 exoneration verdict; D-11 artifact layout)
    - .planning/v1.10/bench-verification/clean-board-uno/, clean-board-leonardo/, fault-injection/, uno328pb/ (the per-leg outputs to aggregate)
    - .planning/v1.6/consistency-check-runs/ (the layout being mirrored)
  </read_first>
  <how-to-verify>
    1. Confirm the per-leg artifacts exist: clean-board-uno/read-leg + clean-board-leonardo/read-leg +
       clean-board-<board>/write-leg sha256sums; fault-injection/fault-inject-outgoing-log.txt +
       fault-inject-incoming-log.txt; uno328pb/timeout-retry-log.txt + exoneration-verdict.txt.
    2. Write .planning/v1.10/bench-verification/SUMMARY.md containing:
       - Operator attestation: which boards were used, the operator-confirmed silkscreen shield rev per
         board (D-07 target Rev 2.0 — record the actual declared rev), and the date.
       - SHA-256 table for every read run (Uno + Leonardo) and every write read-back cycle vs source SHA.
       - GATE-1.8d baseline hash-match result: strong-form MATCH (run_01 vs Rev 2.0 canonical 19710f6e...)
         or "self-consistency-only" (original chip absent), per D-05 — stated explicitly.
       - Fault-injection summary: both directions, both fault forms, error latency (sub-second / no 2 s
         cascade), recovery byte-exact confirmed.
       - uno328pb before/after shape comparison + the structured exoneration verdict (transport-exoneration
         per v1.9-COBS-DECISION §2.0, NOT a hardware fix; RCA deferred to v1.9 Phase 45+).
       - The explicit milestone claim: "The serial transport is a settled, byte-exact variable for the
         resumed v1.9 read-bug RCA."
    3. Operator reviews and signs off the attestation (boards/revs/date are correct).
  </how-to-verify>
  <resume-signal>Confirm the SUMMARY.md attestation is accurate (boards, revs, date) and complete, then "approved".</resume-signal>
  <acceptance_criteria>
    - .planning/v1.10/bench-verification/SUMMARY.md exists and contains: operator attestation, the full
      read+write SHA-256 table, the GATE-1.8d hash-match result (strong-form or self-consistency-only),
      the both-direction fault-injection summary, and the uno328pb before/after + exoneration verdict.
    - The strong-form vs self-consistency outcome (D-05) and the operator-confirmed shield rev (D-07) are
      both explicit.
    - The verbatim transport-exoneration line and the milestone settled-variable claim are present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Artifact completeness check</name>
  <files>.planning/v1.10/bench-verification/SUMMARY.md</files>
  <read_first>
    - .planning/v1.10/bench-verification/SUMMARY.md (the artifact just composed)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-RESEARCH.md (Validation Architecture: the post-bench semi-automatable completeness check)
  </read_first>
  <action>
    Run a completeness check over .planning/v1.10/bench-verification/: confirm the expected per-run
    binaries exist for each completed leg (clean-board read-leg run_01..run_05 per board; write-leg
    cycle_*_readback per board; uno328pb runs as captured), that sha256sums.txt files are non-empty, that
    fault-injection logs for both directions exist, that uno328pb exoneration-verdict.txt contains the
    "transport-exoneration per v1.9-COBS-DECISION" string, and that SUMMARY.md contains the operator
    attestation heading and the milestone settled-variable claim. Report any missing file as a gap (do NOT
    fabricate bench data). Use ls, wc -c, and grep for the checks.
  </action>
  <verify>
    <automated>test -f .planning/v1.10/bench-verification/SUMMARY.md && grep -q "transport-exoneration per v1.9-COBS-DECISION" .planning/v1.10/bench-verification/uno328pb/exoneration-verdict.txt && grep -qi "operator attestation" .planning/v1.10/bench-verification/SUMMARY.md && test -s .planning/v1.10/bench-verification/clean-board-uno/read-leg/run_05.bin && test -s .planning/v1.10/bench-verification/clean-board-leonardo/read-leg/run_05.bin && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints OK (SUMMARY.md present with the attestation heading and milestone claim;
      uno328pb exoneration verdict present with the §2.0 line).
    - Any missing per-leg artifact is reported as an explicit gap, not silently passed.
  </acceptance_criteria>
  <done>SUMMARY.md and all referenced per-leg artifacts are present and internally consistent; completeness check prints OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench output -> milestone artifact | The artifact is the settled-variable hand-off to v1.9 RCA; fabricated or incomplete data would mislead the resumed RCA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-16 | Repudiation | unattested or fabricated bench evidence | mitigate | Operator-witness attestation (boards/revs/date) signed off in Task 1; Task 2 completeness check reports gaps rather than fabricating |
| T-53-17 | Information disclosure | incomplete artifact misleads the resumed v1.9 RCA | mitigate | Completeness check (Task 2) asserts every per-leg artifact + the exoneration verdict string are present |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
- .planning/v1.10/bench-verification/SUMMARY.md exists with operator attestation, full SHA table, GATE-1.8d
  hash-match outcome, both-direction fault-injection summary, uno328pb before/after + exoneration verdict,
  and the milestone settled-variable claim.
- Task 2 completeness check prints OK.
</verification>

<success_criteria>
- SC4 satisfied: a complete, operator-attested milestone evidence artifact under
  .planning/v1.10/bench-verification/ that lets the resumed v1.9 RCA treat the serial transport as a
  settled, byte-exact variable.
</success_criteria>

<output>
Create `.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-06-SUMMARY.md` when done.
</output>
