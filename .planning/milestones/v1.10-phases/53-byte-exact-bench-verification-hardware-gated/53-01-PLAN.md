---
phase: 53-byte-exact-bench-verification-hardware-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_eprom_operations.py
  - firestarter_app/tests/test_serial_comm.py
  - firestarter_app/tests/test_cli_handlers.py
autonomous: true
requirements: [XACT-01, XACT-02]
must_haves:
  truths:
    - "Failing tests exist for write_cycle_eprom 3-way verdict (PASS/mismatch/hw-error)"
    - "Failing tests exist for the outgoing fault-inject hook (None no-op, corrupt-crc8, drop-delimiter)"
    - "Failing test exists for FaultInjectingSerialCommunicator one-shot incoming CRC8 flip"
    - "Failing smoke tests exist for the dev write-cycle and dev fault-inject subcommands"
    - "A ring-fence compliance test asserts the _read_and_parse_lines body source text is unchanged"
  artifacts:
    - path: "firestarter_app/tests/test_eprom_operations.py"
      provides: "write_cycle_eprom verdict tests (RED)"
      contains: "write_cycle"
    - path: "firestarter_app/tests/test_serial_comm.py"
      provides: "fault-inject hook + subclass tests + ring-fence assertion (RED)"
      contains: "fault_inject"
    - path: "firestarter_app/tests/test_cli_handlers.py"
      provides: "dev write-cycle / dev fault-inject invocation smoke tests (RED)"
      contains: "write_cycle"
  key_links:
    - from: "tests/test_eprom_operations.py"
      to: "EpromOperator.write_cycle_eprom"
      via: "monkeypatched _operation_context + _run_state_machine"
      pattern: "write_cycle_eprom"
    - from: "tests/test_serial_comm.py"
      to: "SerialCommunicator._fault_inject_outgoing / FaultInjectingSerialCommunicator"
      via: "send_json_command frame interception"
      pattern: "_fault_inject_outgoing"
---

<objective>
Wave 0 failing-test scaffold for the Phase 53 software harness. Author RED unit tests (and a
ring-fence compliance assertion) for every software-verifiable behavior the harness will deliver in
Wave 1, per the Nyquist split in 53-VALIDATION.md.

This phase adds NO transport behavior. The COBS frame contract, codec, CRC8, and resync posture are
FROZEN (Phases 49-52) and ring-fenced. These tests pin only the new harness surfaces:
`write_cycle_eprom()` (write->read-back->compare N-cycle), the outgoing fault-inject hook in
`send_json_command()`, the `FaultInjectingSerialCommunicator` subclass, and the new `dev write-cycle`
/ `dev fault-inject` subcommands.

Purpose: Establish the test-first contract so Wave 1 implementation has a green target, and lock the
GATE-1.8d ring-fence with an automated assertion that `_read_and_parse_lines` body source is unchanged.
Output: Three test files with new RED test cases that fail until 53-02 lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-CONTEXT.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-PATTERNS.md
@.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-VALIDATION.md
@firestarter_app/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for write_cycle_eprom 3-way verdict</name>
  <files>firestarter_app/tests/test_eprom_operations.py</files>
  <read_first>
    - firestarter_app/tests/test_consistency_check.py (the monkeypatch-of-operator-internals analog: `_make_fake_ctx`, `_make_fake_state_machine_with_payloads`, the `monkeypatch.setattr(EpromOperator, "_operation_context", ...)` usage at lines 76-147)
    - firestarter_app/firestarter/eprom_operations.py (lines 497-696 consistency_check_eprom; 838-872 write_eprom; 910-933 erase_eprom — the methods write_cycle_eprom will call)
    - firestarter_app/tests/test_eprom_operations.py (the file being modified — existing fixtures/imports)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-PATTERNS.md (the new-test adaptations block for write_cycle_eprom)
  </read_first>
  <behavior>
    - test_write_cycle_eprom_pass: monkeypatch erase_eprom->True, write_eprom->True, and a fake _operation_context + _run_state_machine that feeds a payload whose bytes equal the source image bytes; assert return value 0.
    - test_write_cycle_eprom_mismatch: read-back payload differs from the source image bytes; assert return value 1.
    - test_write_cycle_eprom_hw_error: _run_state_machine returns (False, "timeout") on the read-back; assert return value 2 (NOT collapsed to 1).
    - test_write_cycle_eprom_erase_fail: erase_eprom returns False; assert return value 2.
  </behavior>
  <action>
    Add a test class/functions targeting `EpromOperator.write_cycle_eprom` (which does not yet exist —
    these MUST fail at collection/call time). Mirror `test_consistency_check.py`: monkeypatch
    `EpromOperator._operation_context` with a `@contextmanager` fake yielding
    `({"address": 0, "memory-size": N}, 512, "READ")`, and monkeypatch `_run_state_machine` with a fake
    that invokes `process_data_chunk_callback(0, payload)` and returns `(True, None)`. Monkeypatch
    `EpromOperator.erase_eprom` and `EpromOperator.write_eprom` to return booleans per case. Write the
    source image to `tmp_path` with known bytes and pass its path as `source_image_path`. Assert the int
    return value matches the 3-way verdict contract (0=all cycles match source / 1=mismatch / 2=hw-error).
    Use the source-image SHA-256 = read-back SHA-256 semantics from D-06 (independent host-side compare,
    NOT firmware verify). Do NOT implement write_cycle_eprom here.
  </action>
  <verify>
    <automated>cd firestarter_app && pytest tests/test_eprom_operations.py -k write_cycle -x 2>&1 | grep -Eq "error|Error|failed|AttributeError|no attribute"</automated>
  </verify>
  <acceptance_criteria>
    - test_eprom_operations.py contains the four `write_cycle` test functions named above.
    - `pytest tests/test_eprom_operations.py -k write_cycle -x` collects and RUNS (fails red because
      `write_cycle_eprom` is absent), not a collection-only ImportError on the test module itself.
    - No production code is modified by this task.
  </acceptance_criteria>
  <done>Four RED write_cycle tests exist and fail because EpromOperator.write_cycle_eprom is not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for fault-inject hooks + ring-fence compliance assertion</name>
  <files>firestarter_app/tests/test_serial_comm.py</files>
  <read_first>
    - firestarter_app/tests/test_serial_comm.py (the file being modified; the make_comm/__new__ constructor-bypass pattern)
    - firestarter_app/tests/conftest.py (lines 65-145: _FakeSerial, make_comm factory, feed())
    - firestarter_app/firestarter/serial_comm.py (lines 156-175 send_json_command frame assembly; lines 225-238 _decode_id_frame wrapper + GATE-1.8d ring-fence comment; the _read_and_parse_lines generator body)
    - firestarter_app/firestarter/frame_parser.py (lines 28-128: _crc8_ccitt, cobs_encode, cobs_decode — used by the tests to verify frame mutation)
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-PATTERNS.md (the new-tests block for send_json_command hook + FaultInjectingSerialCommunicator)
  </read_first>
  <behavior>
    - test_fault_inject_outgoing_none: with `_fault_inject_outgoing = None`, send_json_command emits an
      unmodified frame: trailing byte is 0x00, and cobs_decode(frame[:-1]) yields payload+CRC where
      CRC == _crc8_ccitt(payload).
    - test_fault_inject_outgoing_corrupt_crc8: with a hook flipping frame[-2], the decoded CRC no longer
      matches _crc8_ccitt(payload).
    - test_fault_inject_outgoing_drop_delimiter: with a hook returning frame[:-1], the sent bytes have no
      trailing 0x00 delimiter.
    - test_fault_inject_incoming_subclass: FaultInjectingSerialCommunicator(corrupt_incoming_once=True)
      flips the last body byte exactly once in _decode_id_frame, then passes subsequent frames through
      unmodified (one-shot semantics).
    - test_read_and_parse_lines_ringfence_unchanged: assert the source text of the `_read_and_parse_lines`
      generator body is byte-identical to a pinned snapshot (the GATE-1.8d ring-fence guard).
  </behavior>
  <action>
    Add tests targeting `SerialCommunicator._fault_inject_outgoing` (a new instance attribute defaulting
    to None) and a new `FaultInjectingSerialCommunicator` subclass (neither exists yet — these MUST fail).
    Build a communicator via the `__new__` constructor-bypass pattern from conftest; monkeypatch
    `send_bytes` to capture the emitted frame. Verify frame integrity via `cobs_decode` + `_crc8_ccitt`
    from frame_parser. For the corrupt-crc8 hook use `frame[:-2] + bytes([frame[-2] ^ 0x01]) + b"\x00"`;
    for drop-delimiter use `frame[:-1]`. For the incoming subclass, assert the one-shot flip on
    `body[-1] ^ 0x01` fires once via `_decode_id_frame`. For the ring-fence test, extract the
    `_read_and_parse_lines` source with `inspect.getsource` and assert it equals a verbatim pinned string
    (or assert its SHA-256 equals a pinned hex digest) — capture the CURRENT body text into the pinned
    snapshot so the test is GREEN today and only goes red if the body changes. Do NOT implement the hook,
    the attribute, or the subclass in production code here.
  </action>
  <verify>
    <automated>cd firestarter_app && pytest tests/test_serial_comm.py -k "fault_inject or ringfence" -x 2>&1 | grep -Eq "fault_inject|ringfence"</automated>
  </verify>
  <acceptance_criteria>
    - test_serial_comm.py contains the five test functions named above.
    - The four `fault_inject` tests RUN and fail red (attribute/subclass absent); the ringfence test is
      GREEN (snapshot captured from current body).
    - The ringfence test uses inspect.getsource against `_read_and_parse_lines` and pins the body.
    - No production code is modified by this task.
  </acceptance_criteria>
  <done>Four RED fault-inject tests + one GREEN ring-fence compliance test exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED smoke tests for dev write-cycle and dev fault-inject subcommands</name>
  <files>firestarter_app/tests/test_cli_handlers.py</files>
  <read_first>
    - firestarter_app/tests/test_cli_handlers.py (the file being modified; the dev-group CliRunner pattern at lines 566-595, make_app_context, Mock(spec=EpromOperator))
    - firestarter_app/firestarter/cli_handlers.py (lines 1030-1117 dev_consistency_check handler: Click decorators, @map_typed_errors, @click.pass_obj, sys.exit(verdict_int))
    - .planning/phases/53-byte-exact-bench-verification-hardware-gated/53-PATTERNS.md (the dev write-cycle / dev fault-inject adaptation blocks and the new CliRunner tests)
  </read_first>
  <behavior>
    - test_dev_write_cycle_pass: Mock(spec=EpromOperator).write_cycle_eprom returns 0; invoke
      ["dev","write-cycle","W27C512",<source>]; assert exit_code 0.
    - test_dev_write_cycle_hardware_error: write_cycle_eprom returns 2; assert exit_code 2 (NOT 1).
    - test_dev_fault_inject_pass: Mock fault_inject_cycle returns True; invoke
      ["dev","fault-inject","W27C512"]; assert exit_code 0.
    - test_dev_fault_inject_fail: fault_inject_cycle returns False; assert exit_code 1.
  </behavior>
  <action>
    Add CliRunner-based smoke tests for the not-yet-existing `dev write-cycle` and `dev fault-inject`
    subcommands (these MUST fail — Click reports "No such command" until 53-02 registers them). Follow the
    `dev_consistency_check` test analog: build `make_app_context(eprom_operator=Mock(spec=EpromOperator))`,
    set the relevant operator method return value, and assert the verdict is passed through verbatim via
    `sys.exit(verdict_int)` (the 0/1/2 contract is NOT collapsed by a bool-to-int wrap). For write-cycle,
    write a source file into tmp_path so the `click.Path(exists=True)` argument resolves. Do NOT register
    the subcommands here.
  </action>
  <verify>
    <automated>cd firestarter_app && pytest tests/test_cli_handlers.py -k "write_cycle or fault_inject" -x 2>&1 | grep -Eq "write_cycle|fault_inject|No such command"</automated>
  </verify>
  <acceptance_criteria>
    - test_cli_handlers.py contains the four subcommand smoke tests named above.
    - The tests RUN and fail red ("No such command" / non-zero unexpected exit) until the subcommands exist.
    - No production code is modified by this task.
  </acceptance_criteria>
  <done>Four RED dev-subcommand smoke tests exist and fail because the subcommands are unregistered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production source | Tests import production modules but MUST NOT mutate them; risk is a test that monkeypatches production state and leaves it altered for other tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | _read_and_parse_lines ring-fence | mitigate | Ring-fence compliance test (Task 2) pins the body source so any future edit fails a test rather than silently breaking the GATE-1.8d baseline lineage |
| T-53-02 | Tampering | monkeypatched EpromOperator/SerialCommunicator class attributes | accept | pytest monkeypatch fixture auto-reverts; no persistent state mutation; low risk in a local dev test suite |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (53-RESEARCH.md Standard Stack: no new dependencies); package legitimacy gate N/A |
</threat_model>

<verification>
- `cd firestarter_app && pytest tests/ -x -q --no-header` runs (new write_cycle/fault_inject/subcommand
  tests RED; ringfence test GREEN; pre-existing suite unaffected).
- No production module under firestarter_app/firestarter/ is modified by this plan.
</verification>

<success_criteria>
- RED tests exist for write_cycle_eprom (4), outgoing fault-inject hook + incoming subclass (4), and the
  two new dev subcommands (4).
- One GREEN ring-fence compliance test pins the `_read_and_parse_lines` body.
- All new RED tests reference identifiers that 53-02 will create; none are implemented in production yet.
</success_criteria>

<output>
Create `.planning/phases/53-byte-exact-bench-verification-hardware-gated/53-01-SUMMARY.md` when done.
</output>
