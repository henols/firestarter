---
phase: 131-gate-hardening-ci-parity
plan: 06
type: execute
wave: 3
depends_on: [131-02, 131-03, 131-04]
files_modified:
  - firestarter_app/tools/ci_parity.sh
  - .planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md
autonomous: true
requirements: [GATE-09]
must_haves:
  truths:
    - "tools/ci_parity.sh exists, is executable, and runs four labelled legs to completion even when an earlier leg fails"
    - "The script exits non-zero if any leg failed, and prints a final summary naming each leg's exit code"
    - "Leg 1 sets FIRESTARTER_FW_ROOT in the pytest process environment, pointing at an empty directory"
    - "Leg 3 mirrors ci.yml's ruff path set exactly - firestarter/ tests/, neither wider nor narrower"
    - "The summary stamps BOARD-ATTACHED with the enumerated serial devices, or none"
    - "At least one recorded run carries BOARD-ATTACHED: none"
    - "check_no_exists_proxy.py was run once and its PASS recorded, without becoming a recipe leg"
  artifacts:
    - path: "firestarter_app/tools/ci_parity.sh"
      provides: "The runnable CI-parity recipe every later v1.30 phase reuses as an acceptance leg"
      contains: "BOARD-ATTACHED"
      min_lines: 60
    - path: ".planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md"
      provides: "A recorded no-board run of the recipe, its per-leg exit codes, and the D-10 proxy-checker PASS"
      contains: "BOARD-ATTACHED: none"
  key_links:
    - from: "firestarter_app/tools/ci_parity.sh"
      to: "firestarter_app/tests/fw_presence.py"
      via: "leg 1 sets FIRESTARTER_FW_ROOT in the child pytest process environment, which fw_presence.py reads at module scope"
      pattern: "FIRESTARTER_FW_ROOT"
    - from: "firestarter_app/tools/ci_parity.sh"
      to: "firestarter_app/.github/workflows/ci.yml"
      via: "legs 3 and 4 reproduce the ruff steps and the watermark gate step verbatim"
      pattern: "ruff (check|format)"
  prohibitions:
    - statement: "The script does not exit on the first failing leg - all four legs always run"
      status: required
      verification: "grep -c 'set -e' tools/ci_parity.sh equals 0, and a forced-failure rehearsal shows four leg banners"
    - statement: "check_no_exists_proxy.py is not a leg of the recipe - ci.yml runs no such step"
      status: required
      verification: "grep -c 'check_no_exists_proxy' tools/ci_parity.sh equals 0"
    - statement: "No outward-facing command appears in the script or in any automated block - no gh workflow run, git push, git merge, git tag, gh release, twine upload"
      status: required
      verification: "grep -Ec 'gh workflow run|git push|git merge|git tag|gh release|twine upload' tools/ci_parity.sh equals 0"
    - statement: "No new pytest skip reason is introduced and no test file is edited"
      status: required
      verification: "git diff --name-only lists no file under tests/"
    - statement: "131-CI-PARITY.md contains no claim that the primary ci job is green"
      status: required
      verification: "the document states leg 4's local exit 2 is the hardened gate working, and that ci stays RED until Phase 132"
---

<!-- planner-discipline-allow: set -e -->
<!-- planner-discipline-allow: check_no_exists_proxy -->

<objective>
Author the CI-parity recipe every later v1.30 phase reuses as an acceptance leg, and record one run
of it with no board attached.

Purpose: this devcontainer cannot see three whole classes of defect the milestone can ship — it runs
Python 3.12 against CI's 3.11, it **has** the `../firestarter` sibling that standalone CI lacks, and
a live board on `/dev/ttyACM*` beats a `comports=[]` patch. Three CI-only sibling-checkout defects
fired simultaneously on the real b15 push and were invisible locally. A recipe that is documentation
only is a recipe every later phase silently skips.

Output: `firestarter_app/tools/ci_parity.sh` (runnable, four legs, aggregate exit, board stamp) and
`131-CI-PARITY.md` (a recorded no-board run plus the D-10 confirmation).

Implements GATE-09 and decisions D-07, D-08, D-09, D-10.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md
@.planning/phases/131-gate-hardening-ci-parity/131-PATTERNS.md
@.planning/phases/131-gate-hardening-ci-parity/131-02-SUMMARY.md
@/workspaces/firestarter_app/CLAUDE.md

Read before task 1: `.planning/research/PITFALLS.md` §P-18 (line 647) — the recipe verbatim, the
three defect classes this devcontainer cannot see, and the note that `FIRESTARTER_FW_ROOT` is read
at **module scope** and must therefore be set in the pytest **process** environment.
**⚠ P-18 item 4 is corrected by D-17 — read D-17 before acting on it. Do not act on item 4.**
</context>

## Working directory

Every `automated` command runs with **`/workspaces/firestarter_app`** as the working directory
unless it names an absolute path. `tools/ci_parity.sh` is committed inside the submodule on the
milestone branch; `131-CI-PARITY.md` is committed in the meta repo. Two separate commits, each with
an explicit file list — never `git add -A`. The five pre-existing untracked/modified paths
(`.coverage`, `.planning/config.json`, `SECURITY.md`, `write_test_port.sh`, `.gitignore`) must never
enter a commit.

`firestarter/` (firmware) is out of scope and must end this plan with an empty `git status`.

## Requirement IDs this plan may mark Complete

**This plan alone may mark Complete: `GATE-09`.** It is delivered entirely here.

**This plan may NOT mark Complete any other GATE ID.** GATE-01/02/03/04/06 belong to 131-02, GATE-05
to 131-01, GATE-07 to 131-07, GATE-08 to 131-03, GATE-10 to 131-04. A requirement spanning multiple
plans may be marked Complete only by the last plan in the span. Executors prematurely marked
multi-plan requirements Complete **4× in Phase 116** — do not repeat it.

**Requirement ticking protocol.** Tick only the `- [ ] GATE-09` line in `.planning/REQUIREMENTS.md`
and its single Traceability row, with line-scoped `Edit` calls — never a whole-file `Write`. Give the
tick an evidence clause naming `tools/ci_parity.sh` and the recorded run.

## Corrections that amend locked decisions

The phase's six corrections (F-01…F-06) are tabled in full in `131-01-PLAN.md`. None of them changes
this plan's work, but **one measured fact does**, and it is the single thing most likely to be
mistaken for a defect — so it is stated here as a first-class expectation rather than left to be
discovered:

**Leg 4 is EXPECTED to fail in this devcontainer, and that failure is the deliverable working.**
After 131-01's hardening, an ambient numpy PEP-695 stub truncates mypy, mypy exits 2, and the
hardened gate correctly exits **2**. Measured today, before hardening: legs 1, 2 and 3 all pass
(full suite green with an empty sibling root; full suite green with the sibling present; ruff clean
at CI's exact path set), and leg 4 exits 0 **because it is fail-open**. After hardening, leg 4 exits
2 and the aggregate exit is non-zero. **Phase acceptance does not require a zero aggregate exit.** It
requires that all four legs ran, that each printed its own exit code, that the board stamp says
`none`, and that the only failing leg is leg 4 for the stated reason. Do **not** weaken a guard, add
a `|| true`, or exclude leg 4 to make the aggregate green.

## Artifacts this phase produces

See `131-01-PLAN.md` for the full phase-wide list. The ones **this plan** creates:
`firestarter_app/tools/ci_parity.sh` and `131-CI-PARITY.md`.

<tasks>

<task type="auto">
  <name>Task 1: Author tools/ci_parity.sh — four legs, per-leg status, aggregate exit, board stamp</name>
  <files>firestarter_app/tools/ci_parity.sh</files>
  <read_first>
    - /workspaces/firestarter_app/.github/workflows/ci.yml — `:60-73` verbatim. Legs 3 and 4 must reproduce those steps exactly. Re-measure the line numbers; do not inherit them. Note also which steps this recipe deliberately does NOT mirror, so the header can say so.
    - /workspaces/firestarter_app/tests/fw_presence.py — `:35-45` (the import-time-binding warning; CONTEXT.md's `:37-42` is stale) and `:77-88` (`FW_ROOT`, `FIRESTARTER_FW_ROOT` at `:80`, `FW_REPO_MARKER`, `FW_REPO_PRESENT`, all evaluated once at import). This is why leg 1's env var must be in the child process's environment and never monkeypatched.
    - /workspaces/firestarter_app/firestarter_test.sh — `:90-113` and `:171`. The only reusable convention in the repo: the `---------------------------------` banner and the trailing summary line. **Do not copy its structure otherwise** — it exits 1 on the first failure, which violates D-08.
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md — decisions D-07, D-08, D-09, D-10.
  </read_first>
  <action>
Create `firestarter_app/tools/ci_parity.sh` and make it executable (`chmod +x`, and verify the mode
bit lands in git as `100755`).

**No analog exists.** `tools/` contains zero `.sh` files; the three repo-root scripts are hardware
integration drivers with no `set -e`, no per-leg status and no aggregate exit code. Take only the
banner and trailing-summary convention from `firestarter_test.sh`; author the leg-aggregation logic
fresh.

**Structure.** Do **not** use `set -e` — it would abort at the first failing leg, and D-08 requires
all four legs to run and a final summary. Use `set -u` if you want undefined-variable safety. Run
each leg, capture `$?` immediately into a per-leg variable, and print that leg's own status line.
Resolve the repo root from the script's own location so it behaves identically from any caller's
working directory — the same anchoring discipline `check_mypy_watermark.py` uses — and `cd` there
before running the legs. Create leg 1's empty directory with `mktemp -d` and remove it with a `trap`
on `EXIT`.

**Header comment** must state:
  - what the recipe is for: it mirrors `ci.yml`'s gate steps so a developer can see, locally, the
    three defect classes this devcontainer otherwise hides (py3.12-vs-3.11, the sibling-repo layout
    standalone CI lacks, and a live board beating a `comports=[]` patch);
  - what it deliberately does **not** mirror, so the boundary is visible rather than assumed: the two
    codegen drift gates, the vector-catalog checks, `pytest`'s `--cov-fail-under=70` coverage gate,
    and the entry-point smoke test. Naming the omissions is what keeps the recipe an honest mirror
    rather than an implied full-CI claim;
  - that `tools/check_no_exists_proxy.py` is deliberately **not** a leg (D-10): the recipe's contract
    is CI parity, `ci.yml` runs no such step, and the checker's behaviour is already covered by
    `tests/test_check_no_exists_proxy.py`, which legs 1 and 2 run. Adding it would make the mirror
    unfaithful;
  - the leg-4 expectation from this plan's corrections section: in any environment where an ambient
    PEP-695 stub truncates mypy — this devcontainer included — leg 4 exits 2, and that is the
    hardened gate working correctly, not a script defect. State it in the script so the next reader
    does not "fix" it.

**The four legs, in this order**, each preceded by a `---------------------------------` banner
naming the leg number, its command, and what it proves:

  1. `FIRESTARTER_FW_ROOT="$TMPROOT" python3 -m pytest tests/ -q` where `TMPROOT` is the `mktemp -d`
     directory. The variable must be set in the **child process's environment** — as a command
     prefix or an exported variable — because `tests/fw_presence.py:80` reads it at module scope and
     an in-process `monkeypatch.setenv` runs after import and has no effect. Proves: the suite passes
     with the firmware sibling absent, the standalone-CI condition.
  2. `python3 -m pytest tests/ -q`. Proves: the suite passes with the sibling present.
  3. `ruff check firestarter/ tests/` then `ruff format --check firestarter/ tests/`, as two commands
     in one leg with a combined status. The path set is **exactly** `firestarter/ tests/`, neither
     wider nor narrower — ruff *is* correctly CI-scoped today; the failure mode is running it locally
     at a different scope.
  4. `python3 tools/check_mypy_watermark.py`. Proves: the hardened watermark gate reaches a legible
     terminal state.

**Board stamp (D-09).** The no-board check is **evidence metadata, not a fifth leg** — a script
cannot detach hardware, and requiring physical detachment on every invocation would make the recipe
unused. Enumerate `/dev/ttyACM*` and `/dev/ttyUSB*` with a plain glob or `ls`; never open a serial
port. Emit into the final summary either `BOARD-ATTACHED: <space-separated device list>` or
`BOARD-ATTACHED: none`. A run with a board attached is still valid — it simply cannot claim the
no-board leg.

**Final summary.** After all four legs, print a summary block listing each leg's number, its short
name and its numeric exit code; the `BOARD-ATTACHED:` line; the interpreter version (`python3 -V`);
and a single `CI-PARITY: PASS` or `CI-PARITY: FAIL (legs: N, M)` line. Then `exit` non-zero if any
leg failed, zero otherwise. The script must not swallow leg failures.
  </action>
  <acceptance_criteria>
    - `test -x tools/ci_parity.sh` succeeds and `git ls-files -s tools/ci_parity.sh` shows mode `100755`.
    - `grep -c 'set -e' tools/ci_parity.sh` equals 0.
    - `grep -c 'check_no_exists_proxy' tools/ci_parity.sh` equals 0 — D-10 keeps it out of the recipe.
    - `grep -Ec 'gh workflow run|git push|git merge|git tag|gh release|twine upload' tools/ci_parity.sh` equals 0.
    - `grep -c 'ruff check firestarter/ tests/' tools/ci_parity.sh` equals 1 and `grep -c 'ruff format --check firestarter/ tests/' tools/ci_parity.sh` equals 1 — CI's exact path set, neither wider nor narrower.
    - `grep -c 'FIRESTARTER_FW_ROOT' tools/ci_parity.sh` is at least 1 and the occurrence is a process-environment assignment on the pytest invocation, not an argument to pytest.
    - `grep -c 'BOARD-ATTACHED' tools/ci_parity.sh` is at least 1.
    - Running `bash tools/ci_parity.sh` from `/tmp` produces the same four leg banners as running it from the repo root — proving the script anchors on its own location.
    - The run prints four distinct leg banners and four numeric exit codes even though leg 4 fails — proving failures are not swallowed.
    - The header comment names all four deliberately-unmirrored CI steps and states the leg-4 expectation.
    - `git diff --name-only` lists exactly `tools/ci_parity.sh`; nothing under `tests/` or `firestarter/` changed.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && test -x tools/ci_parity.sh && [ "$(grep -c 'set -e' tools/ci_parity.sh)" = 0 ] && [ "$(grep -c 'check_no_exists_proxy' tools/ci_parity.sh)" = 0 ] && [ "$(grep -Ec '(^|[^a-zA-Z])(gh|git|twine)[[:space:]]' tools/ci_parity.sh)" = 0 ] && [ "$(grep -c 'ruff check firestarter/ tests/' tools/ci_parity.sh)" = 1 ] && [ "$(grep -c 'ruff format --check firestarter/ tests/' tools/ci_parity.sh)" = 1 ] && grep -q 'FIRESTARTER_FW_ROOT' tools/ci_parity.sh && grep -q 'BOARD-ATTACHED' tools/ci_parity.sh && echo GATE-OK</automated>
  </verify>
  <done>A runnable, location-anchored, four-leg script exists that never aborts early, prints a per-leg exit code and a labelled summary, stamps the attached-board state as evidence metadata, mirrors CI's ruff path set exactly, and documents both its deliberate omissions and the expected local leg-4 exit 2.</done>
</task>

<task type="auto">
  <name>Task 2: Record a no-board run, and record the D-10 proxy-checker confirmation</name>
  <files>.planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md</files>
  <read_first>
    - /workspaces/firestarter_app/tools/ci_parity.sh — as written by task 1, so the transcript is read against the script that produced it.
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md — decisions D-09 (the board stamp and the at-least-one-no-board-run acceptance), D-10 (the one-time proxy-checker confirmation), D-17 and D-18 (record-only, no action).
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-02-SUMMARY.md and `131-03-SUMMARY.md`, `131-04-SUMMARY.md` — the new test modules and legs whose presence under leg 1 this run is what mechanically discharges D-18's negative criterion.
  </read_first>
  <action>
**Confirm no board is attached first.** Enumerate `/dev/ttyACM*` and `/dev/ttyUSB*`. Measured at
plan time: **none present**. If a board is attached, ask the operator to detach it before the
recording run — this phase's acceptance requires **at least one recorded run carrying
`BOARD-ATTACHED: none`**, and a run with a board attached cannot claim it. Do not fabricate the
stamp.

**Run the recipe once** and capture its complete stdout and stderr.

**Run `python3 tools/check_no_exists_proxy.py` once, separately** (D-10). It is a one-time recorded
confirmation, **not** a recipe leg. Capture its exit code and its `PASS:` line. This discharges
STATE.md's standing note that the `_FW_ABSENT` idiom was fixed for the host suite in Phase 123 but
*"six modules shared it — worth confirming none survive"* — for free, without making the recipe an
unfaithful mirror of a `ci.yml` that runs no such step.

**Write `131-CI-PARITY.md`** carrying:
  - the date, the interpreter version, and the branch and HEAD SHA of `firestarter_app`;
  - the full transcript, or its four leg banners plus the summary block verbatim;
  - a per-leg table: leg number, command, exit code, and one sentence on what it proves;
  - the `BOARD-ATTACHED: none` line quoted from the summary;
  - an **expected-failure** section: leg 4 exited 2 because an ambient numpy PEP-695 stub truncates
    mypy in this devcontainer and the hardened gate correctly refuses to report a count for an
    incomplete run. State plainly that this is the Phase 131 deliverable working, that the same leg
    is expected to exit **1** in CI (69 errors against a watermark of 35), and that
    `firestarter_app`'s primary `ci` job **stays RED until Phase 132**. Any sentence implying CI is
    green is an overclaim;
  - the D-10 result: `tools/check_no_exists_proxy.py` → `PASS`, with its exit code and the count of
    files it names, and an explicit note that it is a one-time confirmation and deliberately not a
    recipe leg;
  - a short **record-only** section carrying D-17 and D-18, neither of which is work:
      - **D-17.** The research record's claim about `test_present_root_with_missing_target_raises_not_skips`
        is wrong on repo, on commit and on substance. The test is at
        `firestarter/tests/test_flash_path_record_sync.py:694` — the **firmware** repo, which this
        milestone does not touch — so it is not in `firestarter_app` and no agent should hunt for it
        there. The softening is firmware `1c511e8`, not app `5934a54` (which touched
        `tests/test_py32_flash_map_host.py` and `tests/test_scan_paths_resolve.py`). And it is
        **premise-scoped, not weakened**: the gate's own subject — that a missing scan target raises
        rather than skips — is still hard-asserted wherever the premise holds. Record the correction;
        do not act on it; do not edit the firmware repo.
      - **D-18.** `81fa53c` is present in the app repo's history. `main` has never been merged in any
        of the three repos, so the carry stays latent and acting now would be work against a merge
        that is not happening. Its criterion is the **negative** one — any test this phase adds must
        pass under recipe leg 1 — and leg 1 of this very run discharges it **mechanically**, over the
        new modules from 131-02, 131-03 and 131-04. Name those modules and state that leg 1 covered
        them.

Commit `tools/ci_parity.sh` inside the submodule and `131-CI-PARITY.md` in the meta repo, as two
commits, each with an explicit file list.
  </action>
  <acceptance_criteria>
    - `131-CI-PARITY.md` exists and contains the exact string `BOARD-ATTACHED: none`.
    - It contains four leg entries with four numeric exit codes, and the per-leg table names what each leg proves.
    - Legs 1, 2 and 3 are recorded as exit 0. If any of them is non-zero, the document explains it and the plan reports it as a finding rather than absorbing it — legs 1, 2 and 3 were all measured green at plan time.
    - Leg 4 is recorded with its exit code and an explanation naming the ambient numpy PEP-695 stub, the truncated mypy run, and the fact that this is the hardened gate working.
    - It contains an explicit sentence stating `firestarter_app`'s primary `ci` job stays RED until Phase 132, and contains no sentence claiming CI is green.
    - It records `tools/check_no_exists_proxy.py` exiting 0 with a `PASS:` line, and states explicitly that it is a one-time confirmation and not a recipe leg.
    - It carries the D-17 correction with all three of its wrongnesses (repo, commit, substance) and the instruction not to act.
    - It carries the D-18 record, names the three new/extended test modules, and states that leg 1 covered them.
    - `grep -c 'gh workflow run' 131-CI-PARITY.md` equals 0.
    - `git -C /workspaces/firestarter status --short` is empty and its HEAD is unchanged — the firmware repo was read about, never touched.
  </acceptance_criteria>
  <verify>
    <automated>D=/workspaces/.planning/phases/131-gate-hardening-ci-parity; test -f $D/131-CI-PARITY.md && grep -q 'BOARD-ATTACHED: none' $D/131-CI-PARITY.md && grep -q 'check_no_exists_proxy' $D/131-CI-PARITY.md && grep -q 'D-17' $D/131-CI-PARITY.md && grep -q 'D-18' $D/131-CI-PARITY.md && grep -qi 'RED until Phase 132' $D/131-CI-PARITY.md && [ -z "$(git -C /workspaces/firestarter status --short)" ] && echo GATE-OK</automated>
  </verify>
  <done>One run of the recipe is recorded with no board attached, its four per-leg exit codes are stated, leg 4's exit 2 is explained as the deliverable working rather than a defect, the D-10 confirmation is recorded, and D-17 and D-18 are carried as corrections rather than acted on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devcontainer → CI | The local environment has a sibling firmware checkout, Python 3.12 and an ambient numpy that CI has none of; a locally-green claim can be false in CI. |
| host `/dev` → test suite | A live board on `/dev/ttyACM*` beats a `comports=[]` patch and can turn a port-discovery test green locally and red in CI, or worse, green in both while having opened a real port. |
| script exit code → later phases' acceptance | Every later v1.30 phase reuses this script as its acceptance leg; a script that swallows a failure launders it into six phases' evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-34 | Spoofing | a locally-green suite standing in for CI | high | mitigate | Leg 1 runs the suite with `FIRESTARTER_FW_ROOT` pointed at an empty directory, in the child process's environment, reproducing standalone CI's missing sibling — the exact condition that hid three defects until the b15 push. |
| T-131-35 | Tampering | a swallowed leg failure | critical | mitigate | No `set -e`; each leg's exit code is captured and printed; the aggregate exit is non-zero if any leg failed. Asserted by an acceptance criterion that four banners and four codes appear even though leg 4 fails. |
| T-131-36 | Repudiation | an unclaimable no-board run | medium | mitigate | D-09's board stamp is written into the summary from a live enumeration, and phase acceptance requires at least one recorded run stamped `none`. Fabricating the stamp is explicitly forbidden. |
| T-131-37 | Elevation of Privilege | a recipe that drifts wider than CI | medium | mitigate | Leg 3's path set is asserted to be exactly `firestarter/ tests/` by an equality-count criterion; the header names every CI step deliberately not mirrored. |
| T-131-38 | Denial of Service | opening a real serial port during the board check | medium | mitigate | The board check is a plain glob or `ls` over `/dev/ttyACM*` and `/dev/ttyUSB*`; it never opens a port. |
| T-131-39 | Information Disclosure | device paths in a committed transcript | low | accept | `/dev/tty*` paths are local device names, already routine in this project's bench records. No secret. |
| T-131-40 | Tampering | npm/pip/cargo installs | high | mitigate | The script installs nothing — it runs `pytest`, `ruff` and the watermark gate from the already-installed environment. No package is added by this plan; no legitimacy audit is owed. |
</threat_model>

<verification>
- `bash tools/ci_parity.sh` runs all four legs from both the repo root and a foreign working directory, printing four banners, four exit codes, a `BOARD-ATTACHED:` line and a final `CI-PARITY:` line.
- Legs 1, 2 and 3 exit 0; leg 4 exits 2 in this devcontainer and the aggregate exit is non-zero, which is the expected and correct post-hardening state.
- `python3 tools/check_no_exists_proxy.py` exits 0 and its PASS line names the new `tests/test_check_mypy_watermark.py`.
- `131-CI-PARITY.md` carries `BOARD-ATTACHED: none`, the per-leg table, the leg-4 explanation, the D-10 result, and the D-17 / D-18 record-only corrections.
- `git ls-files -s tools/ci_parity.sh` shows mode `100755`.
- `git -C /workspaces/firestarter status --short` is empty.
</verification>

<success_criteria>
- The CI-parity recipe is **runnable**, not documentation: a committed, executable, location-anchored script with four labelled legs, per-leg exit codes, an aggregate exit and a final summary.
- Leg 1 sets `FIRESTARTER_FW_ROOT` in the pytest process environment; leg 3 mirrors `ci.yml`'s ruff path set exactly.
- The script never aborts early and never swallows a leg failure.
- One recorded run carries `BOARD-ATTACHED: none`, and leg 4's local exit 2 is explained as the hardened gate working rather than absorbed or worked around.
- `check_no_exists_proxy.py` PASS is recorded once, and is deliberately not a recipe leg.
- D-17 and D-18 are recorded as corrections with their reasons, and neither is acted on.
- GATE-09 is ticked in REQUIREMENTS.md with an evidence clause naming the script and the recorded run.
</success_criteria>

<output>
Create `.planning/phases/131-gate-hardening-ci-parity/131-06-SUMMARY.md` when done.

It must state the four per-leg exit codes, the `BOARD-ATTACHED: none` stamp, and — in its own words
— that leg 4's non-zero exit is the Phase 131 deliverable working, that this phase **set no
watermark, deleted nothing and fixed no mypy errors**, and that `firestarter_app`'s primary `ci` job
is **RED before and after this phase, by design**. Any sentence implying CI is green is the v1.22
C-5 overclaim class.
</output>
