---
phase: 131-gate-hardening-ci-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md
  - .planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md
autonomous: false
requirements: [GATE-07]
must_haves:
  truths:
    - "One real ci.yml dispatch has been made on the fork base beta at 16a313a, by the operator, and its run id and URL are recorded"
    - "The recorded error count is READ verbatim from the CI log, never computed locally"
    - "131-CI-BASELINE.md states explicitly that the number is an input to Phase 132's watermark, not a Phase 131 claim"
    - "If the measured number differs from research's 69, the measured number wins and the divergence is recorded rather than reconciled away"
    - "No agent-executed command in this plan dispatches a workflow, pushes, merges, tags, or publishes"
  artifacts:
    - path: ".planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md"
      provides: "The operator-run dispatch procedure as prose, with the exact commands and the safety argument"
      contains: "workflow_dispatch"
    - path: ".planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md"
      provides: "Run id, URL, step statuses, the verbatim mypy summary line, mypy's resolved version, and the Python version"
      contains: "checked"
  key_links:
    - from: ".planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md"
      to: "firestarter_app .github/workflows/ci.yml"
      via: "the recorded run is a workflow_dispatch of ci.yml on ref beta, whose gate step is `mypy type check (watermark gate)`"
      pattern: "ci\\.yml"
  prohibitions:
    - statement: "No automated block in this plan contains gh workflow run, git push, git merge, git tag, gh release, or twine upload - the dispatch is operator-run prose"
      status: required
      verification: "reviewer scan of every automated body in this file; only read-only gh run view / gh run list appear"
    - statement: "No workflow file is edited - ci.yml already carries workflow_dispatch at :25"
      status: required
      verification: "git -C /workspaces/firestarter_app diff --name-only lists nothing under .github/"
    - statement: "The error count is read from the CI log, never recomputed locally - no local mypy invocation contributes a number to 131-CI-BASELINE.md"
      status: required
      verification: "131-CI-BASELINE.md's count line is quoted verbatim with its run id and log line reference"
    - statement: "131-CI-BASELINE.md contains no claim that the ci job is green or will be green after this phase"
      status: required
      verification: "the document states the job is RED and that Phase 132 turns it green"
---

<!-- planner-discipline-allow: gh workflow run -->

<objective>
Obtain and record one real, current, post-fork CI error count on the exact fork base, before any
later phase's number depends on it.

Purpose: 69 is well established, but the watermark must be set from a **current** run on the exact
commit the milestone forked from. A count measured with the broken gate is meaningless — which is why
this measurement targets the **fork base**, not the hardened branch, and depends on nothing else in
this phase. It can be requested first or in parallel.

Output: `131-HANDOFF.md` (the operator procedure) and `131-CI-BASELINE.md` (the recorded number and
its provenance).

Implements GATE-07 and decisions D-11, D-12.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md
@.planning/phases/131-gate-hardening-ci-parity/131-PATTERNS.md

Read before task 1: `.planning/research/SUMMARY.md` §A-1 (line 244) — the CI-redness adjudication,
the prior run `30708836339` (Host CI, `workflow_dispatch`, 2026-08-01), and the exact dispatch
commands. §"Gaps to Address" (line 919) — the current post-fork count is this plan's job.
</context>

## Working directory

This plan writes only meta-repo files under
`.planning/phases/131-gate-hardening-ci-parity/`. It creates **no** branch, edits **no** submodule
file, and does not depend on 131-01. Read-only `gh run view` / `gh run list` calls may run from any
directory. Commit in the meta repo, naming files explicitly.

`firestarter/` (firmware) is out of scope and must end this plan with an empty `git status`.

## The standing rule this plan is built around

**No `automated` block in any plan may contain `gh workflow run`, `git push`, `git merge` into
`beta`, `git tag`, `gh release …`, or `twine upload`.** The dispatch therefore lives as **prose in
`131-HANDOFF.md`** and is run by the operator. No workflow edit is needed: `ci.yml` already carries
`workflow_dispatch:` at `:25` (added by Phase 127 D-01).

Confirmed safe, and this must be stated in the handoff: `ci.yml`'s `push` trigger is
`branches: [main]` only, so a dispatch on `beta` fires `ci.yml` and nothing else. No branch push is
required — the ref already exists.

`gh run view` and `gh run list` are **read-only** and are permitted in `automated` blocks.

## Requirement IDs this plan may mark Complete

**This plan may mark NOTHING Complete.**

`GATE-07` is *delivered* here, but its tick is deliberately deferred to **131-07**, which verifies
that `131-CI-BASELINE.md` exists, carries a real run id, and carries a verbatim count line before
ticking. This keeps two wave-1 plans from writing `.planning/REQUIREMENTS.md` concurrently, and it
means the requirement is ticked only after its evidence has been independently re-read.

Do not tick `GATE-07`. Do not tick anything else. Executors prematurely marked multi-plan
requirements Complete **4× in Phase 116** — this is the discipline that prevents it.

## Corrections that amend locked decisions

The phase's six corrections (F-01…F-06) are tabled in full in `131-01-PLAN.md`. None of them changes
this plan's work. Read them anyway before writing `131-HANDOFF.md`, because the handoff is the
document the operator reads.

## Artifacts this phase produces

See `131-01-PLAN.md` for the full phase-wide list. The ones **this plan** creates:
`131-HANDOFF.md` and `131-CI-BASELINE.md`.

<tasks>

<task type="auto">
  <name>Task 1: Author 131-HANDOFF.md — the operator dispatch procedure</name>
  <files>.planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md</files>
  <read_first>
    - /workspaces/firestarter_app/.github/workflows/ci.yml — the whole file. Confirm by reading, not by inheriting: `workflow_dispatch:` at `:25`, `push: branches: [main]` at `:9-11`, `Set up Python 3.11` at `:33-36`, the ruff steps at `:63-67`, and the gate step `mypy type check (watermark gate)` at `:69-70`. Re-measure every line number.
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md — decisions D-11 (exactly one dispatch, on the fork base, operator-run, and why not two) and D-12 (what the record must carry).
    - /workspaces/.planning/research/SUMMARY.md lines 244–271 — §A-1, including the prior run id `30708836339` and the exact command shapes.
  </read_first>
  <action>
Write `131-HANDOFF.md`. It is an **operator procedure**, so every privileged command appears as
copy-pasteable prose in a fenced block that the operator runs, never as an agent-executed step.

Contents, in this order:

1. **What is being asked and why.** One dispatch of `ci.yml` on ref `beta`, whose tip is the fork
   base `16a313a`. The purpose is not to re-establish that the job is red — run `30708836339`
   already did that on 2026-08-01 — but to obtain a **current** post-fork error count on the exact
   commit this milestone forked from, because that count is the number Phase 132's watermark is set
   from.

2. **Why exactly one dispatch, not two.** A second, post-hardening dispatch would show the hardened
   gate at `exit 1` on a 69-error tree — the same red, for the same reason. It buys nothing and costs
   an operator round-trip. If a later phase needs the hardened gate proven red-for-the-right-reason
   in CI, Phase 132 gets it for free: it is the phase that turns the gate green, so its own dispatch
   is the proof. Record the residual: **if Phase 132 is replanned without a dispatch, this phase owes
   a second run.**

3. **Why the operator runs it.** The standing rule: no `automated` block in any plan may contain
   `gh workflow run`, `git push`, `git merge` into `beta`, `git tag`, `gh release …`, or
   `twine upload`.

4. **Safety.** `ci.yml`'s `push` trigger is `branches: [main]` only, so dispatching on `beta` fires
   `ci.yml` and nothing else. No branch push is required — `beta` already exists at `16a313a`. Name
   the two workflows that do **not** fire and why.

5. **The commands**, in a fenced block, in order: the dispatch
   (`gh workflow run ci.yml --repo henols/firestarter_app --ref beta`); then
   `gh run list --workflow=ci.yml --repo henols/firestarter_app --limit 3` to obtain the run id; then
   the read-only follow-ups the agent will run — `gh run view <id> --repo henols/firestarter_app` for
   step statuses and `gh run view <id> --repo henols/firestarter_app --log` for the log the count is
   read from.

6. **What to hand back.** Exactly one thing: the **run id**. Everything else the agent reads for
   itself. State that a fabricated or placeholder id will be caught by task 3's precondition.

7. **What this run does NOT establish.** The `ci` job is expected to be **RED** on the mypy gate
   step, and steps after it (`pytest`, the entry-point smoke test) will not run. That is the current
   state of the fork base and it stays that way after Phase 131 — this phase hardens the mechanism
   and fixes zero errors. Phase 132 turns it green. **Any artifact claiming otherwise is an
   overclaim.**

Commit in the meta repo with an explicit file list.
  </action>
  <acceptance_criteria>
    - `131-HANDOFF.md` exists and contains the literal string `gh workflow run ci.yml` inside a fenced operator-procedure block, and nowhere else in the phase's plan files.
    - It names the ref `beta`, the fork base `16a313a`, and the repository `henols/firestarter_app`.
    - It cites the prior run `30708836339` and states why this dispatch is nevertheless needed.
    - It states the one-dispatch rationale AND the residual: if Phase 132 is replanned without a dispatch, this phase owes a second run.
    - It states that `ci.yml`'s `push` trigger is `branches: [main]` only and that no branch push is required.
    - It states the expected outcome is a RED `ci` job on the mypy gate step, and that Phase 131 leaves it RED by design.
    - It asks the operator to hand back exactly one thing: the run id.
    - `git -C /workspaces/firestarter_app diff --name-only HEAD` lists nothing under `.github/` — no workflow file was edited.
    - `git -C /workspaces/firestarter status --short` is empty.
  </acceptance_criteria>
  <verify>
    <automated>D=/workspaces/.planning/phases/131-gate-hardening-ci-parity; test -f $D/131-HANDOFF.md && grep -q 'ci.yml' $D/131-HANDOFF.md && grep -q '16a313a' $D/131-HANDOFF.md && grep -q '30708836339' $D/131-HANDOFF.md && grep -q 'henols/firestarter_app' $D/131-HANDOFF.md && grep -qi 'red' $D/131-HANDOFF.md && [ -z "$(git -C /workspaces/firestarter status --short)" ] && echo GATE-OK</automated>
  </verify>
  <done>The operator has a self-contained procedure naming the exact commands, the safety argument, the one-dispatch rationale with its residual, and the single thing to hand back — and it states up front that a RED result is the expected, correct outcome.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator runs the ci.yml dispatch on the fork base</name>
  <files>.planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md</files>
  <read_first>
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md — as written by task 1. Present its command block to the operator verbatim.
  </read_first>
  <action>
Halt and hand off. Print `131-HANDOFF.md`'s command block to the operator verbatim, together with the
one-line statement of what to hand back: the numeric run id, nothing else. Do not run the dispatch —
it is a privileged, outward-facing action reserved to the operator by standing rule, and no
`automated` block in any plan of this phase may contain it. Do not substitute a prior run id, do
not infer an id from `gh run list`, and do not proceed on an empty or non-numeric reply: task 3 opens
with a fail-closed precondition that resolves whatever is supplied against the real run before a
single file is written, so an auto-approved or mistaken resume produces a visible stop rather than a
phantom baseline.
  </action>
  <what-built>
    `131-HANDOFF.md`, containing the full dispatch procedure: the exact `gh` commands, the safety
    argument (`ci.yml`'s `push` trigger is `branches: [main]` only, so nothing else fires), the
    one-dispatch rationale, and the statement that a RED `ci` job is the expected outcome.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md`.
    2. Run, from any directory:
       `gh workflow run ci.yml --repo henols/firestarter_app --ref beta`
    3. Wait roughly a minute, then run:
       `gh run list --workflow=ci.yml --repo henols/firestarter_app --limit 3`
    4. Identify the run whose event is `workflow_dispatch` and whose branch is `beta`, and wait for
       it to reach a terminal conclusion (it is expected to be `failure` — the mypy gate step is the
       only red step, and `pytest` and the entry-point smoke test will not have run).
    5. Reply with the **run id** only — a numeric id, e.g. `30708836339`.

    This is a human-only step by standing rule: no agent may run `gh workflow run`. Nothing else is
    asked of you; the agent reads the step statuses, the log, mypy's resolved version and the Python
    version for itself.
  </how-to-verify>
  <acceptance_criteria>
    - The operator supplies a numeric run id.
    - The id is not a placeholder, not the prior run `30708836339` re-used, and not an id whose event is anything other than `workflow_dispatch` on `beta`. Task 3 verifies all three mechanically before writing anything.
    - If the checkpoint is resumed without a run id, task 3's fail-closed precondition halts the plan rather than producing a phantom baseline.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human-action checkpoint; the mechanical verification is task 3's fail-closed precondition, which resolves the supplied id via `gh run view` before any file is written.</automated>
  </verify>
  <resume-signal>Reply with the numeric run id, or describe what went wrong.</resume-signal>
  <done>The operator has dispatched `ci.yml` on `beta` and supplied a numeric run id, which task 3 resolves and validates before writing anything.</done>
</task>

<task type="auto">
  <name>Task 3: Fail-closed precondition, then read the run and write 131-CI-BASELINE.md</name>
  <files>.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md</files>
  <read_first>
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md — decision D-12, which enumerates exactly what the record must carry.
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-HANDOFF.md — the read-only follow-up commands named in step 5.
    - /workspaces/.planning/research/SUMMARY.md lines 272–301 — §A-2, the 69-error arithmetic and its 25-in-`firestarter/` / 44-in-`tests/` split, so a divergence from the measured number is recognisable as a divergence rather than mistaken for a transcription error.
  </read_first>
  <action>
**Fail-closed precondition, first, before anything is written.** An auto-approved checkpoint must
produce a visible stop, not a phantom baseline (the discipline Phase 130 plan 15 established). Halt
the plan with an explicit message if **any** of these is true:
  - no run id was supplied, or it is not numeric;
  - `gh run view <id> --repo henols/firestarter_app` does not resolve;
  - the run's `event` is not `workflow_dispatch`, or its `headBranch` is not `beta`;
  - the run's head SHA does not begin `16a313a`;
  - the supplied id equals `30708836339` (the prior 2026-08-01 run — a re-used id is not a current
    measurement);
  - the run has not reached a terminal `conclusion`.
On halt, write nothing and report which condition failed. Note the v1.23 lesson that a run's
`outcome` and its `conclusion` are different fields — key on `conclusion`.

**Then read, never compute.** Using read-only `gh` calls only:
  - `gh run view <id> --repo henols/firestarter_app` → per-step statuses for the `ci` job;
  - `gh run view <id> --repo henols/firestarter_app --log` → the raw log, from which you extract, by
    grep and **verbatim copy**:
      - the `Found N errors in M files (checked K source files)` line emitted by the
        `mypy type check (watermark gate)` step;
      - mypy's resolved version, from the `Install package + test deps` step's
        `Successfully installed …` line;
      - the Python version, from the `Set up Python 3.11` step.
  Do **not** run mypy locally and do **not** derive, adjust or round any number. The count is
  **read**, never computed — this is the v1.22 C-5 discipline.

**Write `131-CI-BASELINE.md`** carrying all of:
  - run id, full run URL, event, head branch, head SHA, and terminal conclusion;
  - the `gh run view <id>` step statuses for the `ci` job, showing which step failed and that the
    steps after it did not run;
  - the **verbatim** `Found N errors in M files (checked K source files)` line, quoted, with the log
    step it came from;
  - mypy's resolved version and the Python version, both read from the log;
  - an explicit statement, in its own section, that **this number is an input to Phase 132's
    watermark, not a Phase 131 claim**. Phase 131 sets no watermark, deletes nothing and fixes no
    mypy errors; `firestarter_app`'s primary `ci` job is RED before this phase and RED after it, by
    design, and Phase 132 turns it green;
  - a divergence section: research measured **69** at py3.11 in a numpy-free CI-replica venv, split
    25 in `firestarter/` and 44 in `tests/`, across 17 files with 120 checked. If the measured number
    differs, **the measured number wins** — record both, state the divergence plainly, and do not
    reconcile it away. If it agrees, say so and say it agrees;
  - a note that the `(checked K source files)` value in this line is the measurement
    `MIN_CHECKED_SOURCE_FILES = 120` was set from, so a later change to K is a signal about the tree,
    not about the gate.

Commit in the meta repo with an explicit file list.
  </action>
  <acceptance_criteria>
    - `131-CI-BASELINE.md` exists and contains a numeric run id, a full `https://github.com/henols/firestarter_app/actions/runs/<id>` URL, the event `workflow_dispatch`, the branch `beta`, and a head SHA beginning `16a313a`.
    - It contains a verbatim-quoted line matching `mypy errors: [0-9]+ \(watermark: [0-9]+\)` — **amended by correction F-07** (see `131-01-PLAN.md`'s corrections table): the fork-base (pre-131-01) checker never emits mypy's raw `Found N errors in M files (checked K source files)` summary to CI's log, because it captures mypy's stdout and prints only its own two-line summary. The original criterion (`Found … checked … source files`) is unreachable against real CI output and must not be satisfied by fabrication or local recomputation.
    - It names mypy's resolved version and the Python version, each attributed to the log step it was read from.
    - It contains the `ci` job's per-step statuses and shows that steps after the mypy gate did not run.
    - It contains an explicit sentence stating the number is an input to Phase 132's watermark and **not** a Phase 131 claim.
    - It contains an explicit sentence stating the `ci` job is RED and stays RED after Phase 131, by design.
    - It contains a divergence section that either records agreement with 69 or records the divergence with both numbers.
    - `grep -c 'gh workflow run' 131-CI-BASELINE.md` equals 0 — the privileged command lives only in the handoff.
    - No local mypy invocation contributed a number: the count line is attributed to the CI run id.
    - `git -C /workspaces/firestarter status --short` is empty.
  </acceptance_criteria>
  <verify>
    <automated>D=/workspaces/.planning/phases/131-gate-hardening-ci-parity; test -f $D/131-CI-BASELINE.md && grep -Eq 'mypy errors: [0-9]+ \(watermark: [0-9]+\)' $D/131-CI-BASELINE.md && grep -q 'workflow_dispatch' $D/131-CI-BASELINE.md && grep -q '16a313a' $D/131-CI-BASELINE.md && grep -q 'actions/runs/' $D/131-CI-BASELINE.md && grep -qi 'not a Phase 131 claim' $D/131-CI-BASELINE.md && grep -q 'F-07' $D/131-CI-BASELINE.md && [ -z "$(git -C /workspaces/firestarter status --short)" ] && echo GATE-OK</automated>
  </verify>
  <done>A real, current, fork-base CI run is recorded with its id, URL, step statuses and verbatim summary line; the count is read rather than computed; the document says plainly that the number belongs to Phase 132 and that the job is RED by design.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → agent (run id) | An unverified id lets an auto-approved or mistaken checkpoint produce a confident-looking baseline for a run that never happened. |
| CI log → recorded number | The number Phase 132's watermark is set from crosses from an external log into a committed record; a transcription or a local recomputation would substitute a different measurement. |
| agent → GitHub Actions | Dispatching a workflow is a privileged, outward-facing action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-27 | Spoofing | a phantom or re-used run id | critical | mitigate | Task 3's fail-closed precondition resolves the id via `gh run view` and rejects a non-numeric id, a non-`workflow_dispatch` event, a non-`beta` branch, a head SHA that is not the fork base, the prior run `30708836339`, and a run with no terminal `conclusion` — writing nothing on any failure. |
| T-131-28 | Elevation of Privilege | an agent dispatching a workflow | high | mitigate | Structural, not flag-based: `gh workflow run` appears only as prose in `131-HANDOFF.md`. Every `automated` block here uses read-only `gh run view` / `gh run list`. |
| T-131-29 | Tampering | recomputing the count locally | high | mitigate | D-12: the count is READ verbatim from the CI log with its run id attached. The devcontainer is py3.12 with an ambient numpy that truncates mypy, so a local number would be a different measurement wearing the same name. |
| T-131-30 | Repudiation | reconciling a divergence away | high | mitigate | If the measured number differs from research's 69, the measured number wins and both are recorded. Asserted as an acceptance criterion. |
| T-131-31 | Information Disclosure | a public CI log quoted into the record | low | accept | The repository and its Actions logs are already public. No secret crosses the boundary. |
| T-131-32 | Denial of Service | dispatching the wrong workflow | medium | mitigate | The handoff records the measured trigger topology: `ci.yml`'s `push` trigger is `branches: [main]` only, so a dispatch on `beta` fires `ci.yml` and nothing else, and no branch push is required. |
| T-131-33 | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed or added by this plan. No legitimacy audit is owed. |
</threat_model>

<verification>
- `131-HANDOFF.md` exists, carries the operator commands as prose, and states the RED expectation.
- The supplied run id resolves via `gh run view`, is a `workflow_dispatch` on `beta`, has a head SHA beginning `16a313a`, is not `30708836339`, and has a terminal conclusion.
- `131-CI-BASELINE.md` carries the run id, the URL, the step statuses, the verbatim summary line, mypy's resolved version and the Python version.
- `131-CI-BASELINE.md` states the number is an input to Phase 132's watermark and not a Phase 131 claim, and that the `ci` job stays RED after this phase.
- No `automated` block in this plan contains a dispatch, push, merge, tag, release or upload command.
- `git -C /workspaces/firestarter_app diff --name-only HEAD` lists nothing under `.github/`.
- `git -C /workspaces/firestarter status --short` is empty.
</verification>

<success_criteria>
- One real `gh workflow run ci.yml` dispatch has been made by the operator on the fork base `beta` @ `16a313a`, and is recorded with its id and URL.
- The error count is quoted verbatim from the CI log, attributed to that run, and never recomputed locally.
- The record states explicitly that the number is Phase 132's input, that Phase 131 sets no watermark, and that the `ci` job is RED by design before and after this phase.
- Any divergence from research's 69 is recorded with both numbers and the measured one taken as authoritative.
- Nothing is ticked in `.planning/REQUIREMENTS.md` by this plan — 131-07 ticks GATE-07 after re-reading this evidence.
</success_criteria>

<output>
Create `.planning/phases/131-gate-hardening-ci-parity/131-05-SUMMARY.md` when done.

It must state the run id, the measured count, and — in its own words — that the count is **an input
to Phase 132's watermark, not a Phase 131 achievement**, and that `firestarter_app`'s primary `ci`
job is RED before and after this phase by design. Reporting the error count as something this phase
accomplished is the v1.22 C-5 overclaim class.
</output>
