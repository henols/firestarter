---
phase: 133-sdp-leg-mechanism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tests/test_chip_test_sdp_leg.py
  - .planning/phases/133-sdp-leg-mechanism/133-BASELINE.md
autonomous: true
requirements: [LEG-11]
must_haves:
  truths:
    - "Criterion 4's evidence exists BEFORE `chip_test.py` is touched: a frozen `_PRE_EDIT_PRECEDENCE_MATRIX` (nine exception classes -> exact `(escapes?, verdict, error_code)`) and a frozen derived-op-sequence literal, both measured against the unmodified engine and both GREEN at this commit."
    - "The precedence matrix is derived by driving the real `run_plan`, never transcribed by hand -- and a non-vacuity leg proves the deriver can fail."
    - "Every later `chip_test.py` edit is forced to declare its behavioural delta: `_INTENDED_PRECEDENCE_DELTA` starts EMPTY, so any row that changes without being named in that constant turns the suite RED."
    - "The pre-edit half of the CI-parity recipe is recorded with a REAL mypy count from `tools/ci_replica_venv.sh` -- research assumption A1 (a new plain test module contributes 0) is measured, not inherited."
    - "No production source file is modified by this plan at all."
  artifacts:
    - firestarter_app/tests/test_chip_test_sdp_leg.py
    - .planning/phases/133-sdp-leg-mechanism/133-BASELINE.md
  key_links:
    - "`_PRE_EDIT_PRECEDENCE_MATRIX` (frozen forever) vs `_EXPECTED_PRECEDENCE_MATRIX` (edited by later plans) vs `_INTENDED_PRECEDENCE_DELTA` (the named row set) -- the three-constant triple IS criterion 4's mechanism; drop any one and the criterion becomes an assertion about a diff nobody captured."
    - "`_OPERATOR_METHODS` in this new module must extend `tests/test_chip_test.py`'s six-name `Mock(spec=[...])` allow-list with `sdp_lock` and `sdp_unlock`, or every later plan's dispatch test raises AttributeError against the double."
---

<objective>
Capture the two pre-edit baselines that are ROADMAP criterion 4's only real evidence, in a new
`tests/test_chip_test_sdp_leg.py`, and record the pre-edit half of the CI-parity recipe. Zero
production edits.

Purpose: 133-VALIDATION.md's Wave 0 requirement is explicit — the `exception_precedence_matrix` and
`shipped_ops_sequence_unchanged` baselines are "worthless if written afterward". Research Pitfall 4
adds the reason: D-08 (plan 133-02) inserts two clauses into a four-clause `except` chain where
Python matches the FIRST matching class, and no existing test pins which handler wins for any class.
Written after the edit, the matrix records the new behaviour and proves nothing. Written here, it
becomes a frozen before-image that every later plan must diff against by name.

Output: `firestarter_app/tests/test_chip_test_sdp_leg.py` (new, D-15) carrying the operator-double
harness, the D-13a op-sequence literal, and the three-constant precedence triple;
`.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md` (new) carrying the measured pre-edit numbers.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-sdp-leg-mechanism/133-CONTEXT.md
@.planning/phases/133-sdp-leg-mechanism/133-PATTERNS.md
@.planning/phases/133-sdp-leg-mechanism/133-VALIDATION.md
</context>

<repo_topology>
**All code in this plan is committed INSIDE the `firestarter_app` submodule**, on branch
`gsd/v1.30-sdp-surface-retirement` (verified live at plan time: `firestarter_app` HEAD `42a1971`).
`133-BASELINE.md` is committed in the **meta repo** (`/workspaces`) on branch
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`. **The two branch names deliberately
diverge — that is not a defect to fix.** Do not create, rename, or switch branches. Two git repos
cannot share a commit, so this plan produces one commit per repo.

`firestarter/` (firmware) is not touched by this phase at all: no lockstep, no `.hex` re-cut, no
`constants.py`/`firestarter.h` mirroring.
</repo_topology>

<requirement_fence>
**Requirement IDs this plan may mark Complete in `.planning/REQUIREMENTS.md`: NONE.**

This plan must not edit `.planning/REQUIREMENTS.md`. LEG-11 appears in this plan's `requirements`
field because this plan builds LEG-11's evidence foundation (the precedence matrix), but **plan
133-07 is the only plan in this phase permitted to tick any requirement box**, and it ticks all four
(LEG-09, LEG-10, LEG-11, LEG-15) at once against named green tests. This project's executors marked
multi-plan requirements Complete prematurely 4x in a single phase (Phase 116); centralising the tick
in 133-07 is the mitigation.

**LEG-01…08, LEG-12, LEG-13, LEG-14, LEG-16, LEG-17, LEG-18 belong to Phase 134.** No task in this
plan may reference them as its own work.
</requirement_fence>

<artifacts_this_phase_produces>
Symbols this **phase** creates (none of them exist at this plan's start; the drift-verification pass
must exclude them from "symbol not found in source" findings). This plan creates only the starred
ones.

| Symbol | Created in | Location |
|---|---|---|
| `_OPERATOR_METHODS`, `_mock_operator`, `_plan_with_steps`, `_result`, `_REAL_DB` (module-local copies) * | 133-01 | `tests/test_chip_test_sdp_leg.py` |
| `_PRE_EDIT_PRECEDENCE_MATRIX`, `_EXPECTED_PRECEDENCE_MATRIX`, `_INTENDED_PRECEDENCE_DELTA`, `_derive_precedence_row`, `_SHIPPED_OPS_SEQUENCE` * | 133-01 | `tests/test_chip_test_sdp_leg.py` |
| `OP_SDP_LOCK`, `OP_SDP_UNLOCK`, `_SDP_OPS`, `_dispatch_sdp` | 133-03 | `firestarter/chip_test.py` |
| cleanup-registry local `cleanup` + `_UNLOCK_CLEANUP_SWALLOWED` | 133-04 | `firestarter/chip_test.py` |
| `visit_ExceptHandler`, `visit_FunctionDef`, `_BROAD_EXCEPT_NAMES`, `broad_except_violations`, `_BROAD_EXCEPT_EXEMPTIONS`, `_validate_exemption_table` | 133-05 | `tools/check_devtest_orchestrator.py` |
| `_POLICED_REGISTRIES`, `_POLICED_REGISTRY_COUNT`, `_DECLARED_NON_REGISTRIES`, `_DECLARED_NON_REGISTRY_COUNT`, `_OP_REGISTRY_EXEMPTIONS`, `_assert_op_parity`, `_op_names_referenced_in` | 133-06 | `tests/test_op_registration_parity.py` |
| `tests/test_chip_test_sdp_leg.py`, `tests/test_op_registration_parity.py` | 133-01 / 133-06 | new files |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_chip_test_sdp_leg.py with the operator-double harness and the D-13a frozen op-sequence literal</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/tests/test_chip_test.py` lines 270-300 and 780-900 — the source of truth for
      the harness being copied: `_REAL_DB = EpromDatabase(skip_local_override=True)`, the
      `_OPERATOR_METHODS` `Mock(spec=[...])` allow-list, `_mock_operator(**returns)`,
      `_plan_with_steps(*steps)`, `_result(results, op)`. Re-measure the line numbers; anchor on the
      names. **Copy these helpers; do not import them** (they are module-private in a 1958-line module).
    - `firestarter_app/firestarter/chip_test.py` — the module under test. Read `derive_plan`
      (currently `:394`, `Step(op=...)` construction sites currently `:473-572`), `run_plan`
      (currently `:709-794`), `Plan`/`Step`/`StepResult` dataclasses, the seven `OP_*` constants
      (currently `:289-295`), and the verdict constants (currently `:620-624`).
    - `firestarter_app/tests/test_sdp_table_parity.py` lines 295-355 — the house non-vacuity idiom
      (fixture-setup assertion, then `try/except AssertionError: pass / else: raise`). Task 2 reuses it.
    - `.planning/phases/133-sdp-leg-mechanism/133-PATTERNS.md` §`tests/test_chip_test_sdp_leg.py`
      (NEW, test, request-response) — the verbatim excerpts of every helper above.
  </read_first>
  <action>
Create `firestarter_app/tests/test_chip_test_sdp_leg.py`. It is a new module per D-15; its stated
cost is that together with `tests/test_op_registration_parity.py` (plan 133-06) it consumes **both**
slots of the measured `checked 122 source files` vs `MIN_CHECKED_SOURCE_FILES = 120` margin, exactly.
**Do not create any third new source file anywhere in this phase** — the floor accounting has no
further slack.

Module docstring: follow the house convention of a numbered `Coverage:` list. State explicitly that
this module is the SDP-leg unit (D-15), that it carries **no** skip marker and **no** `@requires_fw`
(it is host-and-engine-local and must run in standalone CI), and that no syrupy snapshot is added
(syrupy 5.5.3 fails the whole session on unused snapshots; 30 live snapshots measured — Phase 132
D-13).

Copy the four harness helpers from `tests/test_chip_test.py` (names above) with one mandatory change:
`_OPERATOR_METHODS` must extend the existing six-name list with `"sdp_lock"` and `"sdp_unlock"`.
Measured constraint: `tests/test_chip_test.py`'s `Mock(spec=[...])` allow-list omits both, so without
this extension every later plan's dispatch test raises `AttributeError` against the double rather
than exercising the arm. Adding the two names here is inert at this commit (nothing calls them yet)
and is why this task, not a later one, owns the harness. Use `"M8720"` as the chip — it is the house
chip (protocol `0x08`, EEPROM, resolves for every step against `_REAL_DB`); do not pick a new one.

Add `_SHIPPED_OPS_SEQUENCE`: a module constant holding, as an **in-test literal**, the exact result of
running the shipped engine against `"M8720"` at this commit — the derived op-string sequence from
`derive_plan("M8720", _REAL_DB)` and the per-step `(verdict, run_count)` pair list from
`run_plan(plan, _mock_operator(), _REAL_DB)`. **Measure both by running the real code and transcribe
what you measure**; do not predict the sequence and do not reuse any figure from a planning document.
If `derive_plan`'s default `write_scope` yields no write/verify/erase step, record the sequence you
actually get — D-13b's sentinel (plan 133-03) is what covers the remaining shipped op strings, so
this literal does not need to reach all seven.

Add `test_shipped_ops_sequence_unchanged` (the `-k shipped_ops_sequence_unchanged` selector from
133-VALIDATION.md): derive the plan and run it exactly as above, and assert equality against
`_SHIPPED_OPS_SEQUENCE` for (a) the op-string sequence, (b) the per-step `(verdict, run_count)` list,
and (c) `len(results)`. Criterion (c) is not redundant — it is what makes a silently *added* step
fail rather than pass a prefix comparison.

Write the assertion failure messages to say what breaks if they fire: this is criterion 4's
before-image, and a mismatch means a shipped op's behaviour changed. Follow the house comment style
(`133-PATTERNS.md` §"Comments that carry the safety argument, not a description") — record what
breaks, with the phase/decision id, not a restatement of the code.

Scope discipline for this task: **no file under `firestarter_app/firestarter/` or
`firestarter_app/tools/` may be modified.** This plan is evidence capture only.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_chip_test_sdp_leg.py -k shipped_ops_sequence_unchanged -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter_app/tests/test_chip_test_sdp_leg.py` exists and `python3 -m pytest tests/test_chip_test_sdp_leg.py -k shipped_ops_sequence_unchanged -x -q` exits 0.
    - `git -C /workspaces/firestarter_app diff --name-only HEAD` lists **only** `tests/test_chip_test_sdp_leg.py` — zero files under `firestarter/` or `tools/`.
    - `grep -c 'sdp_lock' firestarter_app/tests/test_chip_test_sdp_leg.py` is >= 1 and `grep -c 'sdp_unlock' firestarter_app/tests/test_chip_test_sdp_leg.py` is >= 1, both inside `_OPERATOR_METHODS`.
    - `grep -c 'requires_fw' firestarter_app/tests/test_chip_test_sdp_leg.py` is 0, and `grep -c 'snapshot' firestarter_app/tests/test_chip_test_sdp_leg.py` is 0.
    - Deliberately mutating the recorded op-string sequence literal in the working tree makes `pytest tests/test_chip_test_sdp_leg.py -k shipped_ops_sequence_unchanged` FAIL; revert the mutation and it passes again. Record the observed failure message in the task notes — an unproven baseline is not a baseline.
    - `cd /workspaces/firestarter_app && ruff check tests/test_chip_test_sdp_leg.py && ruff format --check tests/test_chip_test_sdp_leg.py` both exit 0.
    - `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q` is green with the same test count as the recorded pre-edit baseline plus this task's new tests.
  </acceptance_criteria>
  <done>The new test module exists with the SDP-extended operator double and a frozen, mutation-proven derived-op-sequence literal; no production file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the three-constant exception-precedence triple, derived from the real run_plan, with a non-vacuity leg</name>
  <files>firestarter_app/tests/test_chip_test_sdp_leg.py</files>
  <read_first>
    - `firestarter_app/firestarter/exceptions.py` — the full hierarchy in one pass. Measured shape:
      `SerialError(Exception)` with exactly three subclasses (`SerialTimeoutError`,
      `ProgrammerNotFoundError`, `FirmwareOutdatedError`); `EpromOperationError(Exception)` carrying
      `.error_code`, with `ProtocolNotImplementedError` and `ChipNotImplementedError`;
      `HardwareOperationError(Exception)`, `FirmwareOperationError(Exception)` and
      `ChipNotFoundError(Exception)` as **siblings of `Exception`, not of `EpromOperationError`**.
    - `firestarter_app/firestarter/chip_test.py` `_run_step` (currently `:863-898`) — the existing
      two-clause `except` chain (`EpromOperationError` first, then
      `(ChipNotImplementedError, ChipNotFoundError)`) and the fact that `_resolve_or_none` sits
      OUTSIDE the `try`.
    - `.planning/phases/133-sdp-leg-mechanism/133-RESEARCH.md` §Pitfall 4 (Exception-clause ordering)
      and the `⚠ Refines locked decision D-08` note in §Measured Anchors — the already-latent
      `ChipNotImplementedError` precedence finding this matrix must capture as it currently is.
    - `firestarter_app/tests/test_sdp_table_parity.py` lines 295-355 — the non-vacuity idiom.
  </read_first>
  <action>
Append to `tests/test_chip_test_sdp_leg.py` the mechanism that makes ROADMAP criterion 4 provable
rather than asserted.

Add `_derive_precedence_row(exc)`: a helper that injects `exc` as
`operator.check_eprom_blank.side_effect` on a fresh `_mock_operator()`, runs
`run_plan(_plan_with_steps(Step(op=OP_BLANK_CHECK, supported=True, reason="")), operator, _REAL_DB)`,
and returns the observed triple `(escaped, verdict, error_code)` — where `escaped` is the escaping
exception's class name (or `None` when `run_plan` returned), and `verdict`/`error_code` are read off
the blank-check `StepResult` (or `None` when it escaped). Use `OP_BLANK_CHECK` deliberately: its arm
is inside `_run_step`'s `try` and calls the operator directly, which is the exact position the
deliberate `AssertionError` at `_dispatch_multi_run`'s terminal `else` occupies relative to the
handler chain. Derive every row by **running the engine**; transcribe nothing by hand.

Add `_PRE_EDIT_PRECEDENCE_MATRIX`: a mapping from exception-class name to that triple, covering
exactly these nine classes — `SerialError`, `SerialTimeoutError`, `ProgrammerNotFoundError`,
`FirmwareOutdatedError`, `EpromOperationError`, `ChipNotImplementedError`, `ChipNotFoundError`,
`HardwareOperationError`, `AssertionError`. Populate it from a real measured run at this commit.
Mark it in a comment as **FROZEN: never edited after plan 133-01** — it is the before-image, and a
later plan editing it destroys the only evidence criterion 4 has. Include the phase/decision id in
that comment.

Add `_EXPECTED_PRECEDENCE_MATRIX`: at this commit, byte-identical in value to
`_PRE_EDIT_PRECEDENCE_MATRIX`, and marked in a comment as the **current** expectation that plan
133-02 edits for exactly the rows D-08 intends to change.

Add `_INTENDED_PRECEDENCE_DELTA`: a `frozenset` of exception-class names whose row a later plan
deliberately changes. **It starts EMPTY.** Comment it with what it enforces: a later
`chip_test.py` edit that changes any row it did not name here turns the suite RED, which is how the
seven shipped ops' behaviour is proven unchanged rather than assumed.

Add three tests, using the exact `-k` selectors from 133-VALIDATION.md:

1. `test_exception_precedence_matrix` — derive all nine rows live and assert equality with
   `_EXPECTED_PRECEDENCE_MATRIX`, one row at a time so the failure names the class.
2. `test_precedence_matrix_delta_is_exactly_intended` — compute
   `{name for name in _PRE_EDIT_PRECEDENCE_MATRIX if _PRE_EDIT_PRECEDENCE_MATRIX[name] != _EXPECTED_PRECEDENCE_MATRIX[name]}`
   and assert it equals `_INTENDED_PRECEDENCE_DELTA`. Also assert both mappings have identical key
   sets, so a row cannot be dropped instead of changed. At this commit both sides are empty.
3. `test_precedence_matrix_deriver_is_non_vacuous` — build an in-memory copy of
   `_EXPECTED_PRECEDENCE_MATRIX` with exactly one row's verdict altered, assert the copy differs from
   the original (the house fixture-setup assertion, so a stale edit cannot make the leg vacuous),
   then run the same comparison helper the real leg uses and assert it raises `AssertionError`
   (`try/except AssertionError: pass / else: raise` per the house idiom). Factor the row comparison
   into a helper both this leg and leg 1 call, so the non-vacuity leg exercises the real comparison
   rather than a re-implementation — the `_referenced_underscore_helpers_in_dev_test` precedent.

Record in a comment, as measured fact rather than prediction, whichever precedence subtleties the
run actually shows — in particular whether `ChipNotImplementedError` currently lands on the
`EpromOperationError` clause (recorded `BAD`) rather than the narrower SKIPPED clause. If the measured
behaviour disagrees with research's reading, **the measurement wins**; say so in the comment and in
the SUMMARY.

Still no production file may be modified.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app && python3 -m pytest tests/test_chip_test_sdp_leg.py -k "exception_precedence_matrix or precedence_matrix_delta or precedence_matrix_deriver" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_chip_test_sdp_leg.py -k exception_precedence_matrix -x` exits 0, and the matrix has exactly nine keys, one per class named in the action.
    - `pytest tests/test_chip_test_sdp_leg.py -k precedence_matrix_delta -x` exits 0 with `_INTENDED_PRECEDENCE_DELTA` empty and the computed delta set empty.
    - `pytest tests/test_chip_test_sdp_leg.py -k precedence_matrix_deriver -x` exits 0 — the non-vacuity leg is GREEN, meaning an altered row DOES make the comparison fail.
    - Adding one name to `_INTENDED_PRECEDENCE_DELTA` in the working tree (with both matrices unchanged) makes `test_precedence_matrix_delta_is_exactly_intended` FAIL; revert and it passes. Record the observed failure text.
    - `_PRE_EDIT_PRECEDENCE_MATRIX` and `_EXPECTED_PRECEDENCE_MATRIX` are equal in value at the end of this task, verified by `pytest -k precedence_matrix_delta` passing with an empty `_INTENDED_PRECEDENCE_DELTA`.
    - Every row's triple was produced by executing `run_plan`; the SUMMARY quotes the measured matrix verbatim, including whichever class escapes today.
    - `git -C /workspaces/firestarter_app diff --name-only HEAD` still lists only `tests/test_chip_test_sdp_leg.py`.
    - `ruff check tests/test_chip_test_sdp_leg.py && ruff format --check tests/test_chip_test_sdp_leg.py` exit 0.
  </acceptance_criteria>
  <done>The frozen before-image, the current expectation, and the empty intended-delta set all exist, all three tests are green, and the delta gate has been seen to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Record 133-BASELINE.md — the pre-edit half of the CI-parity recipe with a REAL mypy count</name>
  <files>.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md</files>
  <read_first>
    - `firestarter_app/tools/ci_parity.sh` — the four legs and their banner lines. Confirm by reading
      that there is **no discrete board leg**: the board dimension is an ambient condition of legs 1
      and 2, named in the script header as a defect class.
    - `firestarter_app/tools/ci_replica_venv.sh` — the numpy-free replica venv; the only local path to
      a real mypy count (the devcontainer's own py3.12 + numpy 2.5.1 run makes `ci_parity.sh` leg 4
      exit 2, which is correct behaviour of a hardened gate refusing a truncated run).
    - `firestarter_app/tools/check_mypy_watermark.py` — read `MIN_CHECKED_SOURCE_FILES` (measured
      `120`) and the exit-code table. Do NOT edit this file.
    - `firestarter_app/pyproject.toml` `[tool.mypy]` — the watermark comment (measured `35`).
      **Out of scope: do not change the watermark.**
    - `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-GREEN.md` — the certifying
      line `mypy errors: 32 (watermark: 35)` and `checked 122 source files`. This is the headroom this
      phase spends against: **errors headroom 3, checked-files margin 2.**
  </read_first>
  <action>
Run the pre-edit half of the recipe and write `.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md`
in the meta repo.

Measure and record, each as a verbatim command plus its verbatim output tail:

1. `ls /dev/ttyACM* /dev/ttyUSB*` — assert **no board is attached** and record that fact. This is the
   correct state: a live board beats the `comports=[]` patch and turns the
   `test_no_programmer_found_*` characterization tests RED. State plainly that the ROADMAP's
   cross-cutting instruction to "run the CI-parity recipe with the no-board leg emphasized"
   **cannot be followed literally** — `ci_parity.sh` has no such leg — and that it is discharged
   here as "legs 1 and 2 were run in the no-board condition, and that condition was asserted and
   recorded". Do not invent a leg name.
2. `cd /workspaces/firestarter_app && bash tools/ci_parity.sh` — record the aggregate result and each
   leg. Record leg 4's exit 2 as **expected locally**, with the reason.
3. `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q` — record the collected test count
   and the pass/skip line.
4. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` — record the **real** mypy error
   count and the `checked N source files` figure. This is mandatory, not optional: research assumption
   A1 ("a new plain test module contributes 0 mypy errors") is flagged LOW confidence and explicitly
   unmeasured, and it is the one number in the research document that must not be trusted. With
   headroom 3, a wrong A1 fails the watermark gate on an otherwise successful phase.
5. `git -C /workspaces/firestarter_app rev-parse --short HEAD` and `git -C /workspaces/firestarter_app branch --show-current`.

Then compute and record the accounting for this phase explicitly: with `133-01`'s new module already
on disk, state the measured `checked` count and the remaining margin against `MIN_CHECKED_SOURCE_FILES`,
and state that plan `133-06`'s `tests/test_op_registration_parity.py` is the **second and last** new
source file this phase may add. If the measured `checked` count or error count leaves less headroom
than 132-CI-GREEN.md implies, say so as a finding — do not smooth it toward the expected number.

Also copy into `133-BASELINE.md` the measured `_PRE_EDIT_PRECEDENCE_MATRIX` and
`_SHIPPED_OPS_SEQUENCE` values from tasks 1-2 verbatim, so the before-image survives independently of
the test file a later phase might refactor.

Commit `133-BASELINE.md` in the **meta** repo. Tasks 1-2 committed inside the submodule. Note in the
commit message that the two halves cannot share a commit because they are two git repos, and
cross-cite the sibling commit hash.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md && grep -qi "no board" /workspaces/.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md && grep -qE "watermark|checked" /workspaces/.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md` exists and contains, each as a verbatim command + output tail: the board check, the `ci_parity.sh` aggregate + per-leg result, the full-suite collected count, and the `ci_replica_venv.sh` mypy error count AND `checked N source files` figure.
    - The recorded mypy error count is `<= 35` and the recorded `checked` count is `>= 120`. If either fails, the task STOPS and reports it as a blocking finding rather than adjusting `pyproject.toml` or `check_mypy_watermark.py` — both are out of scope.
    - `133-BASELINE.md` states in its own words that `ci_parity.sh` has **no discrete no-board leg**, that the board dimension is an ambient condition of legs 1/2, and that no board was attached when the recipe ran.
    - `133-BASELINE.md` reproduces the measured `_PRE_EDIT_PRECEDENCE_MATRIX` (nine rows) and `_SHIPPED_OPS_SEQUENCE` verbatim.
    - `133-BASELINE.md` states the remaining `MIN_CHECKED_SOURCE_FILES` margin after this plan's one new file, and names `tests/test_op_registration_parity.py` (133-06) as the second and last new source file this phase may add.
    - `git -C /workspaces diff --name-only HEAD` for the meta repo lists only `.planning/` paths; `git -C /workspaces/firestarter_app status --porcelain` is clean (tasks 1-2 already committed).
    - `pyproject.toml` and `tools/check_mypy_watermark.py` are byte-unchanged: `git -C /workspaces/firestarter_app diff --stat HEAD -- pyproject.toml tools/check_mypy_watermark.py` produces no output.
  </acceptance_criteria>
  <done>The pre-edit CI-parity half is recorded with a real, in-range mypy count; A1 is measured rather than assumed; the before-image survives outside the test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test double -> engine | A `Mock(spec=[...])` operator stands in for `EpromOperator`. The `spec` allow-list is the boundary: a name absent from it raises `AttributeError` instead of silently returning a truthy `Mock`, which is why extending it with `sdp_lock`/`sdp_unlock` is a correctness act and not a convenience. |
| planning record -> executor | `133-BASELINE.md` becomes the phase's before-image. A number copied from a planning document rather than measured would be a fabricated boundary. |
| local toolchain -> CI | The devcontainer runs py3.12 + numpy 2.5.1; CI runs py3.9/3.11 and mypy targets 3.10. `ci_replica_venv.sh` is the only local crossing that yields a CI-comparable mypy count. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-01 | Tampering | `_PRE_EDIT_PRECEDENCE_MATRIX` | high | mitigate | Marked FROZEN in a comment naming plan 133-01 as its only author; plan 133-02's acceptance criteria assert it is byte-unchanged, so a later edit is caught by a gate rather than by review. |
| T-133-02 | Repudiation | the pre-edit baseline | high | mitigate | Task 3 records the baseline in the meta repo with verbatim commands and output tails, and reproduces the matrix outside the test file, so the evidence cannot be silently rewritten by a later refactor of the test module. |
| T-133-03 | Information disclosure | `_REAL_DB = EpromDatabase(skip_local_override=True)` | low | accept | The `skip_local_override` seam is exactly what stops the test reading `~/.firestarter/database.json`; no user data or secret is read. Accepted with that rationale. |
| T-133-04 | Denial of service | attached programmer board | medium | mitigate | Task 3 asserts no board is attached before running the recipe; a live board turns `test_no_programmer_found_*` RED and would corrupt the baseline. Recorded as an asserted ambient condition. |
| T-133-05 | Elevation of privilege | `ci_replica_venv.sh` | low | accept | Creates a local throwaway venv from the repo's own pins; installs nothing outside it and touches no system path. Accepted. |
| T-133-SC | Tampering | npm/pip/cargo installs | high | accept | **No package-manager install task exists in this phase.** `133-RESEARCH.md` §"Package Legitimacy Audit" records zero external packages, zero new `dependencies` entries, and zero new third-party imports; every mechanism is stdlib or an already-pinned dev tool. The row is retained rather than deleted so a later plan that adds an install cannot inherit silence. |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && python3 -m pytest tests/test_chip_test_sdp_leg.py -q` — all tests in the new module green.
- `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q` — full suite green (measured pre-edit baseline: 1297 tests / 88 test files / 30 syrupy snapshots; re-measure, do not inherit).
- `cd /workspaces/firestarter_app && ruff check firestarter/ tests/ && ruff format --check firestarter/ tests/` — both exit 0.
- `cd /workspaces/firestarter_app && python3 -m pytest tests/test_skip_census.py -x -q` — no new skip reason was introduced (this module adds none; it carries no skip marker at all).
- `git -C /workspaces/firestarter_app diff --stat HEAD~1 -- firestarter/ tools/` — empty. No production file was touched by this plan.
</verification>

<success_criteria>
- `tests/test_chip_test_sdp_leg.py` exists with the SDP-extended operator double, and both pre-edit baselines are GREEN against the unmodified engine.
- `_PRE_EDIT_PRECEDENCE_MATRIX` has nine measured rows; `_EXPECTED_PRECEDENCE_MATRIX` equals it; `_INTENDED_PRECEDENCE_DELTA` is empty.
- The delta gate and the sequence gate have each been SEEN to fail on a deliberate working-tree mutation and then pass on revert — a pre-authored gate proves nothing until it is seen to fail and then pass.
- `133-BASELINE.md` records the no-board condition, the four-leg recipe result, and a real in-range mypy count with its `checked` figure.
- Zero files under `firestarter_app/firestarter/` or `firestarter_app/tools/` were modified.
</success_criteria>

<output>
Create `.planning/phases/133-sdp-leg-mechanism/133-01-SUMMARY.md` when done. It must quote the measured
nine-row precedence matrix and the derived op sequence verbatim, and state the measured mypy error
count and `checked` figure.
</output>
