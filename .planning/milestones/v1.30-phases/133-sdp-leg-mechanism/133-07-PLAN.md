---
phase: 133-sdp-leg-mechanism
plan: 07
type: execute
wave: 7
depends_on: ["133-06"]
files_modified:
  - .planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md
  - .planning/phases/133-sdp-leg-mechanism/133-RECORD.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LEG-09, LEG-10, LEG-11, LEG-15]
must_haves:
  truths:
    - "The after-half of the CI-parity recipe is measured and recorded, with the no-board condition asserted, leg 4's local exit 2 recorded as expected, and a REAL mypy count from `tools/ci_replica_venv.sh` proving `errors <= 35` and `checked >= 120` — research assumption A1 discharged by measurement, not assumption."
    - "LEG-09, LEG-10, LEG-11 and LEG-15 are ticked in `REQUIREMENTS.md` — each with an evidence clause naming the plan and the green test — and NO other LEG requirement is touched."
    - "All five ROADMAP success criteria are discharged with named evidence, and criterion 4's `group=None` clause is recorded as satisfied VACUOUSLY (there is no `group` field) with its intent met by a different mechanism — its literal words are never restated as tested."
    - "Criterion 5's 'eight previously fail-open registries' is recorded as a measured-wrong count, with the measured breakdown in its place."
    - "The Evidence Ceiling split is stated plainly: this phase proves the mechanism cannot strand a chip or lose a report and that the op registries fail closed; it proves NOTHING about SDP behaviour on silicon."
    - "The three honest residuals (D-07's forfeited report, D-16's invisible failed unlock, D-11's forward-protection qualifier) and the D-10/D-16 reconciliation are carried as record content, not footnotes."
  artifacts:
    - .planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md
    - .planning/phases/133-sdp-leg-mechanism/133-RECORD.md
  key_links:
    - "This is the ONLY plan in the phase permitted to edit `.planning/REQUIREMENTS.md`. Plans 133-01…06 each state that in their own requirement fence; centralising the tick is the mitigation for this project's 4x-in-one-phase premature-Complete failure mode."
    - "`133-RECORD.md` consumes the SUMMARY content of plans 133-01…06 verbatim — the observed deliberate-mutation failure messages, the measured registry census, and the residual paragraphs. It must not paraphrase a measurement it did not take."
---

<objective>
Close the phase: measure the after-half of the CI-parity recipe, tick the four requirements against named
green tests, and write the honesty record that keeps this phase's claim inside what it actually proved.

Purpose: this project closes phases against an honesty ledger, and this phase has more claim-shaped
hazards than most. Two of the five ROADMAP criteria are the ones most likely to be quietly
mis-satisfied — criterion 4 because D-05 makes its `group=None` wording vacuous and the temptation is to
restate it as though tested, and criterion 5 because an exemption table is the classic way a fail-closed
gate becomes fail-open. Both are handled here explicitly rather than by omission. And the phase's own
scope makes an overclaim especially cheap: nothing it ships is user-visible, so nothing pushes back on a
record that says more than the tests support. That is the v1.22 C-5 overclaim class, and the Evidence
Ceiling exists to make refusing it mechanical.

Output: `133-CI-PARITY.md` (the measured before/after recipe with a real mypy count) and `133-RECORD.md`
(requirement accounting, decision coverage for D-01…D-16, five criteria discharged, corrections,
residuals, and the Evidence Ceiling statement); plus four ticked boxes in `REQUIREMENTS.md`.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/133-sdp-leg-mechanism/133-CONTEXT.md
@.planning/phases/133-sdp-leg-mechanism/133-VALIDATION.md
@.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md
@.planning/phases/133-sdp-leg-mechanism/133-01-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-02-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-03-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-04-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-05-SUMMARY.md
@.planning/phases/133-sdp-leg-mechanism/133-06-SUMMARY.md
</context>

<repo_topology>
Every file this plan writes is in the **meta repo** (`/workspaces`) on branch
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`. The submodule stays on
`gsd/v1.30-sdp-surface-retirement`; **the divergence is deliberate.** This plan makes **no code change
inside `firestarter_app`** — it only reads it and runs its tooling. Firmware untouched.

**Do not push, do not merge into `beta`, do not tag, do not dispatch CI, do not create a release.** Those
are operator-gated milestone-close actions (Phase 137's), and no `<automated>` block in this plan may
contain `git push`, `git merge`, `git tag`, `gh workflow run`, `gh release`, or `twine upload`. A real CI
dispatch also requires an operator turn in this environment (`gh workflow run` is denied by the auto-mode
classifier independently of the project allowlist), and this phase does not need one: it is host-only and
its gates all run locally.
</repo_topology>

<requirement_fence>
**Requirement IDs this plan may mark Complete in `.planning/REQUIREMENTS.md`: LEG-09, LEG-10, LEG-11,
LEG-15 — all four, and NOTHING else.**

This is the **only** plan in Phase 133 permitted to edit `.planning/REQUIREMENTS.md`. Plans 133-01…06 each
carry a fence stating they may tick nothing; that centralisation is the mitigation for this project's
recorded failure mode (executors marked multi-plan requirements Complete prematurely 4x in a single
phase).

**LEG-01, LEG-02, LEG-03, LEG-04, LEG-05, LEG-06, LEG-07, LEG-08, LEG-12, LEG-13, LEG-14, LEG-16, LEG-17
and LEG-18 belong to Phase 134.** Their checkboxes must remain `[ ]` and their text must remain
byte-unchanged. Do not tick them, do not annotate them, do not add evidence clauses to them.

Each of the four ticks must carry an evidence clause naming the plan that delivered it and at least one
green test by its `pytest -k` selector. A tick without evidence is the shape this fence exists to prevent.
</requirement_fence>

<artifacts_this_phase_produces>
Created by THIS plan: `.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md` and
`.planning/phases/133-sdp-leg-mechanism/133-RECORD.md`.

The full phase symbol inventory (for the drift-verification pass — none of these existed before this
phase): `OP_SDP_LOCK`, `OP_SDP_UNLOCK`, `_SDP_OPS`, `_dispatch_sdp`, the `cleanup` registry local and
`_UNLOCK_CLEANUP_SWALLOWED` in `firestarter/chip_test.py`; `visit_ExceptHandler`, `visit_FunctionDef`,
`_BROAD_EXCEPT_NAMES`, `broad_except_violations`, `_BROAD_EXCEPT_EXEMPTIONS`, `_validate_exemption_table`
in `tools/check_devtest_orchestrator.py`; `_PRE_EDIT_PRECEDENCE_MATRIX`, `_EXPECTED_PRECEDENCE_MATRIX`,
`_INTENDED_PRECEDENCE_DELTA`, `_SHIPPED_OPS_SEQUENCE` and the SDP-leg harness in
`tests/test_chip_test_sdp_leg.py`; `_POLICED_REGISTRIES`, `_POLICED_REGISTRY_COUNT`,
`_DECLARED_NON_REGISTRIES`, `_DECLARED_NON_REGISTRY_COUNT`, `_OP_REGISTRY_EXEMPTIONS`,
`_assert_op_parity`, `_op_names_referenced_in` in `tests/test_op_registration_parity.py`. Both test
modules are new files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Measure and record the after-half of the CI-parity recipe with a real mypy count</name>
  <files>.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md</files>
  <read_first>
    - `.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md` — the before-half measured by plan 133-01.
      Every after-figure is reported as a delta against it.
    - `firestarter_app/tools/ci_parity.sh` — the four legs; confirm again by reading that there is no
      discrete board leg.
    - `firestarter_app/tools/ci_replica_venv.sh` — the only local path to a real mypy count.
    - `firestarter_app/tools/check_mypy_watermark.py` — `MIN_CHECKED_SOURCE_FILES` and the exit-code
      table. **Do not edit.**
    - `firestarter_app/pyproject.toml` `[tool.mypy]` — the watermark comment. **Do not edit; moving the
      watermark is explicitly out of scope for this phase.**
    - `.planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md` — the house shape for this document.
  </read_first>
  <action>
Run the after-half and write `.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md`.

Measure and record, each as a verbatim command plus its verbatim output tail, and each as an explicit
delta against `133-BASELINE.md`:

1. `ls /dev/ttyACM* /dev/ttyUSB*` — assert **no board attached** and record it. Restate, as this phase's
   discharge of the ROADMAP's cross-cutting instruction, that `ci_parity.sh` has **no discrete no-board
   leg** — the board dimension is an ambient condition of legs 1 and 2 — so the instruction is satisfied
   by having run legs 1/2 in the no-board condition and having asserted and recorded that condition. Do
   not invent a leg name, and do not claim a leg was run that does not exist.
2. `cd /workspaces/firestarter_app && bash tools/ci_parity.sh` — aggregate plus each leg. Record leg 4's
   local exit 2 as expected, with the reason (the hardened watermark gate correctly refusing a truncated
   mypy run under the devcontainer's py3.12 + numpy 2.5.1).
3. `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q` — collected count and the pass line.
   Report the delta in test count against the baseline and attribute it to the plans that added tests.
4. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` — the **real** mypy error count and
   the `checked N source files` figure. Assert `errors <= 35` and `checked >= 120` and record both
   numbers. State whether research assumption A1 (a new plain test module contributes 0 mypy errors) held,
   with the measured contribution — this is A1's discharge, and A1 was the one figure in the research
   flagged LOW confidence and explicitly unmeasured.
5. `cd /workspaces/firestarter_app && python3 tools/check_devtest_orchestrator.py; echo "exit=$?"` —
   exit 0 with the four-counter PASS line.
6. `cd /workspaces/firestarter_app && ruff check firestarter/ tools/ tests/` and
   `ruff format --check firestarter/ tools/ tests/`.
7. `cd /workspaces/firestarter_app && python3 -m pytest tests/test_skip_census.py -x -q` — no new skip
   reason was introduced anywhere in the phase.
8. `git -C /workspaces/firestarter_app rev-parse --short HEAD`, `git -C /workspaces/firestarter_app branch --show-current`,
   and `git -C /workspaces/firestarter_app status --porcelain` (must be clean).

Then record the file-count accounting: the measured `checked` figure, the margin against
`MIN_CHECKED_SOURCE_FILES`, and the confirmation that exactly **two** new source files were added across
the whole phase (`tests/test_chip_test_sdp_leg.py`, `tests/test_op_registration_parity.py`) — spending
both slots of the measured margin exactly, as D-15 stated it would.

**If any figure is out of range, stop and report it as a blocking finding.** Do not edit
`pyproject.toml`'s watermark or `check_mypy_watermark.py`'s floor to make a number fit — both are out of
scope, and ratcheting the watermark to the measured count is a separate, still-unowned deferral.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md && grep -qi "no board" /workspaces/.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md && grep -qE "checked" /workspaces/.planning/phases/133-sdp-leg-mechanism/133-CI-PARITY.md && cd /workspaces/firestarter_app && python3 -m pytest tests/ -q && python3 tools/check_devtest_orchestrator.py</automated>
  </verify>
  <acceptance_criteria>
    - `133-CI-PARITY.md` exists and records all eight measurements above, each with its verbatim command and output tail, and each after-figure expressed as a delta against `133-BASELINE.md`.
    - The recorded mypy error count is `<= 35` and the recorded `checked` count is `>= 120`, and A1's measured contribution is stated explicitly (not assumed to be zero).
    - `133-CI-PARITY.md` states that `ci_parity.sh` has no discrete no-board leg and describes the discharge in those terms; it does not name a leg that does not exist.
    - `133-CI-PARITY.md` records leg 4's exit 2 as expected-locally with its reason.
    - `133-CI-PARITY.md` confirms exactly two new source files across the whole phase and states the remaining `MIN_CHECKED_SOURCE_FILES` margin.
    - `python3 tools/check_devtest_orchestrator.py` exits 0 with a four-counter PASS line, recorded verbatim.
    - `pytest tests/ -q` full suite green and `pytest tests/test_skip_census.py -x` exits 0, both recorded.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty, and `git -C /workspaces/firestarter_app diff --stat HEAD -- pyproject.toml tools/check_mypy_watermark.py` produces no output.
    - No `<automated>` command in this task contains `git push`, `git merge`, `git tag`, `gh workflow run`, `gh release`, or `twine upload`.
  </acceptance_criteria>
  <done>The after-half of the recipe is measured and recorded with a real, in-range mypy count, A1 is discharged by measurement, and no gate threshold was moved to make a number fit.</done>
</task>

<task type="auto">
  <name>Task 2: Write 133-RECORD.md — requirement accounting, decision coverage, five criteria, corrections, residuals, Evidence Ceiling</name>
  <files>.planning/phases/133-sdp-leg-mechanism/133-RECORD.md</files>
  <read_first>
    - All six plan SUMMARYs (`133-01-SUMMARY.md` … `133-06-SUMMARY.md`) — the source of every measured
      figure and every observed deliberate-mutation failure message. **Quote them; do not paraphrase a
      measurement you did not take.**
    - `.planning/phases/133-sdp-leg-mechanism/133-BASELINE.md` and `133-CI-PARITY.md`.
    - `.planning/ROADMAP.md` §"Phase 133: SDP Leg Mechanism" — the goal and the five success criteria,
      verbatim, including criterion 4's `group=None` clause and criterion 5's "eight previously fail-open
      registries".
    - `.planning/REQUIREMENTS.md` §"⚠ Evidence Ceiling" (lines 14-40) and §LEG (lines 186-247) — the
      ceiling must not be smoothed over, and LEG-09/10/11/15's exact requirement text is what the ticks
      are measured against.
    - `.planning/phases/133-sdp-leg-mechanism/133-CONTEXT.md` §Implementation Decisions — D-01…D-16 plus
      the three measured corrections at the top of the section.
    - `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-RECORD.md` — the house shape for
      this document (requirements accounted, decisions honoured including any non-literal ones,
      corrections, residuals).
  </read_first>
  <action>
Write `.planning/phases/133-sdp-leg-mechanism/133-RECORD.md`. Six sections, all mandatory.

**1. Requirement accounting.** One row per requirement, quoting LEG-09/10/11/15's requirement text and
naming: the plan that delivered it, the green tests by `pytest -k` selector, and any honest qualifier.
Then state explicitly that **no other LEG requirement was touched**, and name the fourteen that belong to
Phase 134, so a later reader can verify the fence held rather than assume it.

**2. Decision coverage, D-01 … D-16.** One row each: honoured / honoured-non-literally / refined by
measurement, with the evidence. Flag every non-literal honouring rather than smoothing it:
  - **D-05** — no `Step.group` field; the arm keys on `_SDP_OPS` membership.
  - **D-06** — record that the registry shipped **generic** (a list of callables drained in one
    `try/finally` around the whole step loop) rather than as a hardcoded lock-to-unlock window, and that
    an empty registry is a proven no-op for every currently-shipping run.
  - **D-07** — the drain's record does not go into `results` and there is no logger; "recorded on the
    exception (or logged)" is honoured as test-observability, per D-16.
  - **D-10 / D-16 reconciliation** — D-10 asks that a failed cleanup be "recorded"; no in-module recorder
    was created, and why (no logger in a module documented as emitting nothing; `add_note` is 3.11+ against
    a 3.9 floor; the drain must not touch `results`; a list read by nobody would be a dead surface).
  - **D-12** — refined: the declared count was split into policed registries and declared
    non-registries, plus an inversion guard, because three of P-23's ten rows have no op vocabulary at all.
  - Also record the plan-level derivation neither CONTEXT.md nor RESEARCH.md stated in so many words:
    **`OP_SDP_LOCK` was made a MEMBER of `_DESTRUCTIVE_OPS`** while `OP_SDP_UNLOCK` was kept out. D-11's
    own mechanism requires it (gate-closed-from-the-start ⇒ `sdp_lock` SKIPPED is only reachable if a lock
    can be gated), and the frozenset's stated purpose ("ops that mutate the chip") requires it. Record it
    as a derivation with its evidence, not as an inherited instruction.

**3. The five ROADMAP success criteria, discharged with named evidence.** One section each, quoting the
criterion, then the evidence, then the honest caveat. Three of the five carry caveats that must appear:
  - **Criterion 1** — the drain runs on `KeyboardInterrupt`/`SystemExit` and they propagate. Caveat
    (D-07): on that path the **report is honestly forfeited** — the production caller's
    `results = run_plan(...)` assignment never completes, so after a Ctrl-C mid-leg the chip has an unlock
    *attempted* and the user sees **no `dev test` report at all**.
  - **Criterion 2** — the two degrade classes, the two re-raise classes, the still-loud `AssertionError`,
    and the build-time gate. Caveat: bare `except:` was **already** gated by ruff E722; the phase's genuinely
    new coverage is `except Exception:` / `except BaseException:`, and the gate is GREEN on real source only
    because of one reasoned exemption for the pre-existing sampler swallow.
  - **Criterion 3** — the two cases, proven non-vacuously with the OPEN-gate mirror. Caveat (D-11): in
    Phase 133 `OP_SDP_UNLOCK`'s absence from `_DESTRUCTIVE_OPS` is **forward-protection for Phase 134**,
    where the unlock becomes step 4 of the derived leg. It does not gate a live 133 path, because 133
    derives no SDP step. Do not imply otherwise.
  - **Criterion 4** — evidence: the frozen nine-row before-image, the three-row named delta, the frozen
    op-sequence literal, and the seven-op sentinel that has been seen to fail under a deliberate
    arm-reorder. **Mandatory caveat:** the criterion's clause *"an op with `group=None` takes the exact
    pre-existing dispatch path"* is satisfied **VACUOUSLY** — there is no `group` field, so no op has
    `group=None`. State that the criterion's *intent* (shipped ops behaviourally unchanged at zero added
    branching cost) was met by a different mechanism, and **do not restate the criterion's literal words as
    though they were tested.** Also scope any file-level claim to *assertions-and-behaviour unchanged* —
    never "the file did not change", a criterion shape this project has already been bitten by.
  - **Criterion 5** — the parity test, its four guards, the inversion guard and the non-vacuity leg.
    **Mandatory correction:** the criterion's "converting **eight** previously fail-open registries into
    one fail-closed gate" is a **measured-wrong count**. Report the measured breakdown from
    `133-06-SUMMARY.md` in its place — how many are real policed op-keyed registries, how many are declared
    non-registries with no op vocabulary at all, and how many are keyed on a different axis. Say that the
    number was measured, not inherited.

**4. Corrections carried forward.** Every place a measurement overturned a written record, with both
readings. At minimum: research P-20 prevention #2's "wide enough to catch `BaseException`" is unnecessary
and self-defeating (a bare `finally` suffices and an `except BaseException:` would violate criterion 2 and
trip the phase's own new deny-rule); P-23's registry table's three phantom rows; `chip_test.py`'s
`# noqa: BLE001` is inert because `BLE` is not in ruff's `select`; `_run_step`'s docstring over-claimed
that it wrapped the entire step body; and any anchor or count that differed from `133-PATTERNS.md` or
`133-RESEARCH.md` when re-measured at execute time.

**5. Residuals, carried unresolved by design.** Each with a named owner or a stated reason for having
none: D-07's forfeited report; D-16's failed unlock not user-visible until Phase 134's `HELD`/`NOT-RUN`
field (LEG-12); research assumption A2 (a `SerialError` is not currently reachable from the resolver, so
`_run_step`'s resolve half remains outside the `try` — a latent-robustness gap, now documented in the
function's own docstring rather than contradicted by it); the still-unowned watermark ratchet to the
measured count; adding either new test module to the mypy test strict-island (deliberately not spent, with
only three slots of headroom); and the stale `.planning/codebase/TESTING.md` (a map-refresh task, not this
phase's work, but it will mislead any agent that reads it).

**6. The Evidence Ceiling — the honest claim, stated plainly.** This phase proves that *the mechanism
cannot strand a chip or lose a report to a transport error, and that the op registries fail closed.* It
proves **nothing** about SDP behaviour on silicon. Enumerate why, from `REQUIREMENTS.md` §"Evidence
Ceiling": a locked die is unrepresentable in either repo's stubs (both model the bus, never the die's
protection state), so no test in this phase can simulate inhibition; the Phase 116 ground-truth trace
harness is unreachable from the host (a PlatformIO native Unity binary in the firmware repo), and this
phase does not even reach a scripted wire — it asserts on an `EpromOperator` **double**, one layer above
it; protection state is not readable on this family; `0x0D` stays `UNVERIFIED`; and **no AT28C part has
ever been in operator inventory**. State that any artifact claiming more is the v1.22 C-5 overclaim class.

Then tick the four boxes in `.planning/REQUIREMENTS.md` — LEG-09, LEG-10, LEG-11, LEG-15 — each with an
`Evidence:` clause naming the delivering plan and at least one green `pytest -k` selector, in the style
the already-ticked GATE and RETIRE rows use. Change nothing else in that file: no other checkbox, no other
requirement's text, no section heading. Snapshot the file before editing and diff after, so the change is
provably confined to those four lines.
  </action>
  <verify>
    <automated>cd /workspaces && python3 -c 'import re; r=open(".planning/REQUIREMENTS.md").read(); done=sorted(set(re.findall(r"\[x\] \*\*(LEG-\d+)\*\*", r))); openrows=sorted(set(re.findall(r"\[ \] \*\*(LEG-\d+)\*\*", r))); want=["LEG-09","LEG-10","LEG-11","LEG-15"]; assert done==want, "ticked %s expected %s" % (done, want); assert len(openrows)==14, "%d LEG rows open, expected 14" % len(openrows); print("REQ_FENCE_OK", done)'</automated>
  </verify>
  <acceptance_criteria>
    - `133-RECORD.md` exists with all six numbered sections present and non-empty.
    - Section 1 quotes LEG-09/10/11/15's requirement text, names the delivering plan and a green `pytest -k` selector for each, and names the fourteen Phase-134 requirements as untouched.
    - Section 2 has a row for every one of D-01 … D-16, and explicitly flags D-05, D-07, the D-10/D-16 reconciliation and D-12's refinement as non-literal or refined; it records the `OP_SDP_LOCK` ∈ `_DESTRUCTIVE_OPS` derivation with its evidence.
    - Section 3 discharges all five criteria with named evidence, and criterion 4's section contains the word "vacuous" (or "vacuously") applied to the `group=None` clause and does **not** assert that clause as tested; criterion 5's section states the "eight" count is measured-wrong and gives the measured breakdown.
    - Section 5 lists at least the six residuals named in the action, each with an owner or a stated reason for having none.
    - Section 6 contains all five Evidence-Ceiling clauses (locked die unrepresentable; Phase 116 harness unreachable and this phase asserts on a double one layer above the wire; protection state unreadable on this family; `0x0D` stays `UNVERIFIED`; no AT28C part has ever been in operator inventory) and names the v1.22 C-5 overclaim class.
    - The inline python check in `<verify>` prints `REQ_FENCE_OK` with exactly the four expected IDs ticked and exactly fourteen LEG rows still open.
    - `git -C /workspaces diff -- .planning/REQUIREMENTS.md` touches only the four LEG lines being ticked (and their evidence clauses) — no other checkbox, no other requirement's text, no heading. Verified from the diff, not asserted.
    - Every measured figure in `133-RECORD.md` is traceable to a named SUMMARY, `133-BASELINE.md` or `133-CI-PARITY.md`; no figure appears that was not measured by a plan in this phase.
    - No `<automated>` command in this task contains `git push`, `git merge`, `git tag`, `gh workflow run`, `gh release`, or `twine upload`.
  </acceptance_criteria>
  <done>The honesty record exists with all six sections, the four requirements are ticked with evidence and nothing else in `REQUIREMENTS.md` moved, and both mis-satisfiable criteria carry their mandatory corrections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record -> future reader | The record is what a stranger, an auditor, or Phase 137's ledger reads. An unsupported claim crossing this boundary is the v1.22 C-5 overclaim class, and nothing user-visible pushes back on it. |
| `REQUIREMENTS.md` -> milestone accounting | A ticked box is a durable claim. This project has a recorded 4x-in-one-phase premature-tick failure mode, which is why every other plan in the phase is forbidden from touching this file. |
| local toolchain -> CI | The devcontainer's py3.12 + numpy 2.5.1 cannot produce a mypy count; `ci_replica_venv.sh` is the only local crossing that yields a CI-comparable figure. |
| this phase -> publish/release surface | Push, merge-to-`beta`, tag, CI dispatch and release are operator-gated milestone-close actions. This plan must not reach them, structurally rather than by intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-35 | Repudiation | an overclaim about SDP behaviour on silicon | critical | mitigate | Section 6 states the honest claim and enumerates all five Evidence-Ceiling clauses; the acceptance criteria assert each clause's presence, so the ceiling is mechanically checked rather than aspirational. |
| T-133-36 | Spoofing | criterion 4's `group=None` clause restated as tested | high | mitigate | The record must contain the word "vacuous" applied to that clause and must not assert it as tested; asserted as an acceptance criterion. D-05 predicted this exact temptation. |
| T-133-37 | Spoofing | criterion 5's inherited "eight registries" figure | high | mitigate | The record reports the measured breakdown from `133-06-SUMMARY.md` and states the criterion's number is measured-wrong; asserted as an acceptance criterion. |
| T-133-38 | Elevation of privilege | a premature or over-broad requirement tick | critical | mitigate | Only this plan may edit `REQUIREMENTS.md`; the `<verify>` block asserts exactly four ticked LEG rows and exactly fourteen still open; the diff is asserted confined to those four lines. |
| T-133-39 | Tampering | moving a gate threshold to make a number fit | high | mitigate | Task 1 stops and reports a blocking finding instead; the acceptance criteria assert `pyproject.toml` and `check_mypy_watermark.py` are byte-unchanged. |
| T-133-40 | Elevation of privilege | an accidental push, merge, tag, dispatch or release | critical | mitigate | Structurally excluded: no `<automated>` block in this plan contains any of those commands, and both tasks assert that as an acceptance criterion. Those actions are operator-gated and belong to Phase 137. |
| T-133-41 | Repudiation | a paraphrased measurement the record did not take | medium | mitigate | Every figure must be traceable to a named SUMMARY, `133-BASELINE.md` or `133-CI-PARITY.md`; asserted as an acceptance criterion. |
| T-133-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase (`133-RESEARCH.md` §"Package Legitimacy Audit": zero external packages). `ci_replica_venv.sh` creates a throwaway venv from the repo's own existing pins and installs nothing outside it. Row retained so a later plan adding an install cannot inherit silence. |
</threat_model>

<verification>
- `cd /workspaces/firestarter_app && python3 -m pytest tests/ -q` — full suite green.
- `cd /workspaces/firestarter_app && python3 tools/check_devtest_orchestrator.py` — exit 0, four-counter PASS line.
- `cd /workspaces/firestarter_app && bash tools/ci_parity.sh` — legs 1-3 green, leg 4 exit 2 recorded as expected-locally.
- `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` — `errors <= 35` and `checked >= 120`.
- `cd /workspaces && git diff -- .planning/REQUIREMENTS.md` — confined to the four LEG lines.
- `grep -c 'git push\|git merge\|git tag\|gh workflow run\|gh release\|twine upload' /workspaces/.planning/phases/133-sdp-leg-mechanism/133-07-PLAN.md` inside `<automated>` blocks — zero.
</verification>

<success_criteria>
- `133-CI-PARITY.md` records the after-half with a real in-range mypy count and A1 discharged by measurement.
- `133-RECORD.md` has all six sections, discharges all five criteria with named evidence, and carries criterion 4's vacuity correction and criterion 5's measured-count correction.
- LEG-09, LEG-10, LEG-11 and LEG-15 are ticked with evidence clauses; exactly fourteen LEG rows remain open; nothing else in `REQUIREMENTS.md` changed.
- The three honest residuals and the D-10/D-16 reconciliation are record content.
- The Evidence Ceiling's five clauses are present and the v1.22 C-5 overclaim class is named.
- No push, merge, tag, CI dispatch or release occurred, and no gate threshold was moved.
</success_criteria>

<output>
Create `.planning/phases/133-sdp-leg-mechanism/133-07-SUMMARY.md` when done. It must state the four ticked
requirement IDs, the measured mypy error and `checked` figures, the measured registry breakdown that
replaces criterion 5's "eight", and the one-sentence honest claim this phase supports.
</output>
