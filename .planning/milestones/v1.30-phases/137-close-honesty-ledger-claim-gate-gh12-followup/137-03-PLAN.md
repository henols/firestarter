---
phase: 137-close-honesty-ledger-claim-gate-gh12-followup
plan: 03
type: execute
wave: 3
depends_on: ["137-01", "137-02"]
files_modified:
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md
autonomous: true
requirements: [CLOSE-04]
must_haves:
  truths:
    - "Every permitted claim this milestone may make is paired with its explicit non-claim, including the auto-unlock coupled-decision tripwire (Phase 132 RETIRE-07) and both narrowings of the Evidence Ceiling (the native trace harness being unreachable from the host, and a locked die being unrepresentable in either repo's stubs)"
    - "The milestone's own measured-wrong corrections reach this ledger, not only their individual phase records: the six-step (not four-step) SDP leg, the marginal-outranked-BAD exit-code bug (fixed by D-14/134-05), the seven (not six) laundering routes, the chip-ID gate's structural vacuity for all 43 ALLOW chips, LEG-02's 703-chip (not 41-chip) population, and PROV-05's premise being already satisfied by Phase 121 commit c3c9424"
    - "Every figure in this ledger is re-measured fresh in this plan's own execution, not merely copied from a cited SUMMARY or RECORD"
    - "Operator-batch items C-1 (build_db_diff's ladder_state regression) and C-3 (the Evidence Ceiling) are both restated here, not only in the operator batch file"
  artifacts:
    - path: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md"
      provides: "v1.30 honesty ledger: claim classes, corrections carried forward, negative space, Evidence Ceiling"
      contains: "no AT28C silicon was tested"
  key_links:
    - from: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md"
      to: ".planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md"
      via: "the six corrections named in this plan's must_haves are sourced from 134-RECORD.md §4 and re-measured, not re-derived from scratch"
      pattern: "134-RECORD"
---

<objective>
Author `137-LEDGER.md`: the honesty ledger pairing every claim v1.30 is permitted to make with its
explicit non-claim, carrying forward the milestone's own measured-wrong corrections (from Phase 134 and
Phase 136.1's closing records) so they reach a single outward-visible document, not only their
individual phase records that a future reader may never open.

Purpose: CLOSE-04. This is the artifact `check_permitted_claims.py` (plan 137-01) is armed against and
the one a future reader (including the operator, and including a stranger who follows the gh#12
citation trail) will actually read to understand what v1.30 proved and did not prove.

Output: `137-LEDGER.md`, one of the claim gate's four default scan targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.30-OPERATOR-BATCH.md
</context>

<read_first_shared>
Read once, extract fully, do not re-read:
- `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-LEDGER.md` — the SHAPE
  to fork: header block, "the ceiling quoted verbatim" section, "status/claim key" legend, a
  claim-classes table (permitted wording | evidence with source | what it explicitly does NOT prove),
  a "mechanism corrections recorded here, not in REQUIREMENTS.md" section, a "what this milestone chose
  not to prove" negative-space section, and a closing "what no test, gate or review in this phase can
  close" three-way split. v1.30's ledger follows this same seven-section shape.
- `.planning/REQUIREMENTS.md` §"⚠ Evidence Ceiling" (top of file, lines 14-40) — the exact permitted/
  not-provable wording, including both named narrowings.
- `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md` — §3, §4, §5 (incl. the
  seventh laundering route named in prose beneath the table), §6, §7.
- `.planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md` — Finding 1 (PROV-05 already fixed
  by Phase 121 `c3c9424`) and Finding 5 (the "committed NOTHING" process failure).
- `firestarter_app/tests/test_write_skip_sdp_unlock.py` lines ~320-395 — the P-21 tripwire test
  `test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on`.
- `.planning/v1.30-OPERATOR-BATCH.md` §C — items C-1 and C-3.
</read_first_shared>

<tasks>

<task type="auto">
  <name>Task 1: Draft the seven-section honesty ledger, citing every figure to a named source</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md</files>
  <action>
Write `137-LEDGER.md` following `122-LEDGER.md`'s seven-section shape, scoped to v1.30:

**Header block:** milestone name and dates; **submodule commit** captured live via
`git -C /workspaces/firestarter_app rev-parse HEAD` (never reuse a SHA cited in a prior document
without re-confirming it live); oracle statement (software-only — native register trace unreachable
from host per Evidence Ceiling narrowing 1, host pytest, source-scan gates including both claim gates
from 137-01/137-02, zero AT28C silicon); "composes with" cross-references to `REQUIREMENTS.md`
§Evidence Ceiling, `134-RECORD.md`, `136.1-RECORD.md`, `.planning/v1.16/ledger/PROTOCOL-LEDGER.md`
(`0x0D` stays `UNVERIFIED` — reference only, never edit, matching v1.22's D-09 discipline).

**"The ceiling, quoted verbatim" section:** reproduce the "Provable this milestone" /
"NOT provable this milestone" paragraphs from `REQUIREMENTS.md`'s Evidence Ceiling, plus both named
narrowings verbatim (native trace harness unreachable from host; locked die unrepresentable in either
repo's stubs).

**Status/claim key:** reuse v1.22's four labels (`PERMITTED`, `CONTEXT-ONLY`, `COMMUNITY-CORROBORATED`,
`FORBIDDEN`) unchanged.

**Claim classes table.** At minimum these rows, each with permitted wording | measured evidence with a
named source | explicit non-claim:
1. Plan derivation is provable for the full 84-chip 0x0D population — 43 ALLOW get the six-step leg,
   41 REFUSE get six NA steps carrying a reason. `PERMITTED`. Evidence: `134-RECORD.md` §1, re-measured
   live this plan (Task 2). Does NOT prove: the leg holds on real silicon.
2. **The SDP leg is SIX steps, not four** (`134-RECORD.md` §3 Criterion 1 / §4 correction 1):
   `write-baseline-b` · `write-baseline-a` · `sdp-lock` · `write-inhibited` · `sdp-unlock` ·
   `write-restored`. State explicitly that the ROADMAP's own inherited "four-step" wording (and
   REQUIREMENTS.md's own prior LEG-01/LEG-02 text) predates LEG-04's two-transition-direction mandate
   and omits `write-restored` — the only step proving the part was left writable.
3. The oracle is read-back equality, never an exit code; an unexpected write success reports `BAD`,
   never `SKIPPED`/`NA`/`OK`. `PERMITTED`. Evidence: `134-RECORD.md` §3 Criterion 2, re-measured
   (`test_leaked_lock_exits_1`).
4. **The exit-code precedence bug is fixed, and named** (`134-RECORD.md` §4 correction 2): before D-14
   landed, `marginal` (exit 2) numerically outranked `BAD` (exit 1) via a naive `max()`, so a run with
   both a leaked lock and any marginal step exited 2 — laundering the milestone's headline finding into
   the inconclusive code. `_EXIT_CODE_PRECEDENCE = (1, 2, 0)` fixes it. State this was a real, shipped
   defect this milestone caught in its own code.
5. **Every one of the SEVEN known routes to a non-running oracle is tested, not six.** R1-R6 are
   research's own laundering routes; the SEVENTH is the baseline gate itself (D-08/D-20,
   `134-RECORD.md` §5) — named so a reader does not mistake "six laundering-route tests" for exhaustive
   coverage.
6. **The chip-ID destructive gate is structurally vacuous for the entire SDP-ALLOW population** — all
   43 measured ALLOW chips have `chip-id == 0` (re-measured live). R1/R2 are driven through a synthetic
   nonzero-chip-id fixture BECAUSE the real gate is unreachable in production today. No artifact may
   ever say "the leg is gated by chip ID" (`134-RECORD.md` §4 correction 7 / D-17).
7. **LEG-02's tested REFUSE population is 703 chips, a superset of the 41 protocol-0x0D REFUSE chips
   the ROADMAP names** (`134-RECORD.md` §4 correction 8, found by an independent verifier). The
   behavior is proven MORE broadly than originally claimed, but the earlier "41" numeral was wrong.
8. **The auto-unlock coupled-decision tripwire (P-21)** — `dev sdp disable`'s removal is safe ONLY
   because `write`'s auto-unlock stays default-on (`skip_sdp_unlock: bool = False` in
   `cli_handlers.py`'s `_build_op_flags`); recorded as the named test
   `test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on`
   (`tests/test_write_skip_sdp_unlock.py`), not merely a sentence in a note. Explicit non-claim: if
   that default is ever revisited, this decision must be revisited with it.
9. `0x0D` stays `UNVERIFIED` — zero chips changed `support_status`, zero AT28C silicon on this
   milestone's bench. Cross-reference `.planning/v1.16/ledger/PROTOCOL-LEDGER.md`'s `0x0D` row.
10. **PROV-05's premise was already satisfied — by an earlier phase, in an earlier milestone.**
    `doc/lockable-proms.md`'s AT28C16/64/256 distinction was corrected by Phase 121 plan `121-13`,
    commit `c3c9424`, five phases and one milestone earlier (`136.1-RECORD.md` Finding 1). State as a
    genuine finding, not an embarrassment: Phase 136.1's contribution was the durable automated gate
    (`test_lockable_proms_doc_claims.py`) that did not exist before, not the correction itself.

Cite every figure to its source; re-measure the ones Task 2 names (do not claim re-measurement you did
not perform).

**Mechanism corrections section:** restate corrections 3-6 from `134-RECORD.md` §4 in condensed
one-sentence-each form, citing `134-RECORD.md` §4 for the full text — do not re-derive.

**Negative space:**
- The causal claim "the lock inhibited the write" — not provable this milestone (Evidence Ceiling).
- `build_db_diff`'s `ladder_state` regression (operator batch C-1) — a real, previously-undocumented
  finding from Phase 134, still without a disposition as of this ledger's drafting. State: "disposition
  recorded in `137-DECISION.md`, authored later in this same phase" (this plan is wave 3; the
  disposition plan is wave 4) — do not invent a disposition this plan cannot yet see.
- gh#20's underlying AT28C256 write-path defect — filed as backlog `999.29`, `Owner: henols`
  (`134-RECORD.md` §6 residual 6).
- RELOCK-01…06 — deferred to Backlog 999.28; v1.30 withdraws the deliberate-protection surface and
  ships no replacement — a withdrawal, never a migration.
- The mypy watermark ratchet remains unowned (headroom 2, unmoved across Phases 132-136.1).

**Closing three-way split:** mechanically checkable (every claim-gate sub-claim, both suites' green
output) vs. requires the CLOSE-06 blocking operator review (whether the prose is honest, not merely
unbanned) vs. inherently unverifiable in-phase at a sampling rate of zero, permanently, until an AT28C
part is on the bench (the causal claim; `tBLC` accepted by a real die; `0x0D` graduating).
  </action>
  <acceptance_criteria>
    - `137-LEDGER.md` exists with all seven sections present.
    - The literal caveat sentence `no AT28C silicon was tested` appears at least once.
    - All six hard-requirement-6 corrections (six-step leg, exit-code precedence bug, seven laundering routes, chip-ID vacuity, 703-chip population, PROV-05 premise) each appear, citing `134-RECORD.md` or `136.1-RECORD.md` by name.
    - The P-21 row cites the exact test name `test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on`.
    - Operator-batch items C-1 and C-3 both appear, C-1 explicitly deferring its disposition to `137-DECISION.md`.
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md && test -f "$F" && grep -q "no AT28C silicon was tested" "$F" && grep -qi "six.step\|SIX steps\|six-step" "$F" && grep -q "test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on" "$F" && grep -q "703" "$F" && grep -q "c3c9424" "$F" && echo LEDGER-SECTIONS-PRESENT</automated>
  </verify>
  <done>137-LEDGER.md exists with all seven sections, all six mandated corrections, the P-21 tripwire test name, and both operator-batch cross-references.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure every re-measurable figure fresh; sanity-scan the ledger alone against the claim gate</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md</files>
  <read_first>
    - `137-LEDGER.md` (Task 1) — every figure marked for re-measurement.
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py`
      (plan 137-01) — the env-override seam, to scan this ledger ALONE (the other three artifacts do
      not exist yet, so a no-argument run hits the fail-closed partial-set branch, not the real scan).
  </read_first>
  <action>
Re-run, live, from `/workspaces/firestarter_app` using `tools/ci_replica_venv.sh`'s venv (never the
devcontainer python), the following, and reconcile against what Task 1 wrote (correcting the ledger
text in place if a fresh number disagrees, and recording the correction plainly):
- `git rev-parse HEAD` — confirm the header's cited commit is the CURRENT tip.
- The named `derive_plan` ALLOW/REFUSE population tests — confirm 43/41.
- The chip-id-zero test for all 43 ALLOW chips.
- `tools/ci_replica_venv.sh` full run — confirm mypy 33/35 (headroom 2), checked-files 132 or higher
  (plans 137-01/02 already added two test files), full suite count reconciled against the 1504
  baseline plus this phase's own additions so far — report the exact delta, do not assert 1504
  unchanged.
- `grep -c '^- \[x\] \*\*LEG-' .planning/REQUIREMENTS.md` → 18 (unchanged from Phase 134).

Then run the claim gate against `137-LEDGER.md` ALONE, via the env seam, from the phase directory:
`FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-LEDGER.md python3 check_permitted_claims.py`. This must exit
**0** with a `PASS:` line naming `137-LEDGER.md` and reporting the caveat present. If it exits 1, read
the FAIL bucket, reword the flagged sentence to avoid the forbidden phrase while keeping the same
factual content (never delete the caveat, never soften a real finding to dodge the gate), and re-run
until green.

Record every re-measurement's fresh output verbatim in the SUMMARY, per this milestone's "don't trust
a citation, measure" discipline already applied repeatedly in `134-RECORD.md` and `136.1-RECORD.md`.
  </action>
  <acceptance_criteria>
    - Every re-measurement bullet was actually run this plan, with its output recorded verbatim in the SUMMARY.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-LEDGER.md python3 check_permitted_claims.py` (from the phase directory) exits **0** with `PASS:` naming `137-LEDGER.md`.
    - Any figure that disagreed with a fresh measurement was corrected in place, noted in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup && FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-LEDGER.md python3 check_permitted_claims.py | grep -q 'PASS:' && echo LEDGER-CLAIM-SCAN-GREEN</automated>
  </verify>
  <done>All named figures re-measured fresh and recorded verbatim; the ledger scans clean (PASS) alone against the claim gate; any disagreement was corrected in place, not silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| individual phase RECORD.md files → one outward-visible ledger | A finding buried in `134-RECORD.md` §6 is invisible to a reader who only ever opens the ledger; this plan's job is to make sure it does not stay buried |
| a cited figure → a re-measured figure | The milestone's own repeated pattern (independent verifiers catching wrong citations in 134, 136.1) makes "cited but not re-measured" a real, demonstrated risk class in THIS project, not a hypothetical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-09 | Repudiation | a milestone-wide finding staying buried in one phase's own RECORD.md | mitigate | This ledger explicitly restates all six mandated corrections plus the P-21 tripwire and the two operator-batch items, each with a named source citation |
| T-137-10 | Tampering | a cited figure silently drifting from the truth (the project's own demonstrated failure mode — 134-RECORD's own §4 correction 8 was found by an independent verifier, not the authoring plan) | mitigate | Task 2 re-measures every re-measurable figure live and corrects in place on disagreement, recording the correction |
| T-137-11 | Information Disclosure | the ledger itself carrying a forbidden phrase | mitigate | Task 2's env-seam sanity scan against the claim gate (137-01), scanning the ledger alone before the other three artifacts exist |
| T-137-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
1. `137-LEDGER.md` exists with all seven sections and all six mandated corrections.
2. `FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-LEDGER.md python3 check_permitted_claims.py` exits 0.
3. Every re-measurement in Task 2 was actually run, with real output recorded in the SUMMARY.

**Requirement ticking:** this plan ticks **CLOSE-04 only**.
</verification>

<success_criteria>
- `137-LEDGER.md` exists, follows the seven-section shape, and passes the claim gate alone.
- Every mandated correction (six-step leg, exit-code bug, seven routes, chip-ID vacuity, 703 chips,
  PROV-05 premise, P-21 tripwire) is present and cited by name.
- Every re-measurable figure was re-measured fresh this plan, not merely copied.
</success_criteria>

<output>
Create `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-03-SUMMARY.md` when
done. Record: every re-measurement's verbatim output, any in-place correction made, and the claim
gate's PASS line for the ledger scanned alone.
</output>
