---
phase: 137-close-honesty-ledger-claim-gate-gh12-followup
plan: 06
type: execute
wave: 6
depends_on: ["137-01", "137-02", "137-03", "137-04", "137-05"]
files_modified:
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/test_check_permitted_claims_v130.py
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-CI-PARITY.md
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RECORD.md
  - .planning/v1.30-OPERATOR-BATCH.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLOSE-01]
must_haves:
  truths:
    - "The claim gate, run with NO arguments (its real default targets), is ARMED and GREEN, printing a PASS line naming all four of this milestone's own closing artifacts -- the literal discharge of CLOSE-01, only possible now that plans 137-03/04/05 have authored all four"
    - "The now-stale UNARMED-expecting test from plan 137-01 is updated to assert the armed-and-green state, not left contradicting reality"
    - "The CI-parity recipe is run one final time over the WHOLE milestone diff (ROADMAP's cross-cutting instruction), not merely this phase's own delta"
    - "The operator batch presents everything accumulated across phases 136 -> 136.1 -> 137 as ONE list, with every item's disposition stated, before /gsd-complete-milestone"
    - "A phase-closing record (137-RECORD.md) exists with the standard sections this milestone's other phases used: requirement accounting, corrections, Evidence Ceiling restated, residuals, hand-off"
  artifacts:
    - path: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-CI-PARITY.md"
      provides: "Before/after whole-milestone-diff CI-parity measurement"
    - path: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RECORD.md"
      provides: "Phase 137's closing record: all 7 requirements accounted for, corrections, residuals, hand-off to /gsd-complete-milestone"
  key_links:
    - from: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py"
      to: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-LEDGER.md"
      via: "the real, no-argument default-target run scans all four real files for the first time in this phase"
      pattern: "PASS:"
---

<objective>
Close out Phase 137, and this milestone's final phase: arm the claim gate for real (CLOSE-01), run the
milestone-wide CI-parity recipe one last time, finalize the operator's single consolidated batch, and
author the phase's own closing record.

This plan can only run after 137-03 (`137-LEDGER.md`), 137-04 (`137-DECISION.md`,
`137-RELEASE-NOTES-app.md`), and 137-05 (`137-GH12-COMMENT.md`) — all four of the claim gate's default
targets must exist before an unarmed/PASS distinction is even meaningful.

Output: `137-CI-PARITY.md`, `137-RECORD.md`, an updated `.planning/v1.30-OPERATOR-BATCH.md`, CLOSE-01
ticked, and the claim-gate test suite updated to reflect the now-armed reality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/v1.30-OPERATOR-BATCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the now-stale UNARMED test to ARMED, and run the claim gate for real — the literal discharge of CLOSE-01</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/test_check_permitted_claims_v130.py</files>
  <read_first>
    - `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/test_check_permitted_claims_v130.py`
      (plan 137-01) — leg 9, `test_unarmed_when_zero_of_four_default_targets_exist`, whose own docstring
      already anticipates this exact edit ("stays green until 137-06 makes all four exist").
    - The four real artifacts now on disk: `137-LEDGER.md`, `137-DECISION.md`,
      `137-RELEASE-NOTES-app.md`, `137-GH12-COMMENT.md`.
  </read_first>
  <action>
Replace `test_unarmed_when_zero_of_four_default_targets_exist` with
`test_armed_and_green_against_the_four_real_artifacts`: invoke the scanner with no argv and no env
override (the real defaults) and assert returncode **0**, `PASS:` present, and all four basenames
(`137-LEDGER.md`, `137-DECISION.md`, `137-RELEASE-NOTES-app.md`, `137-GH12-COMMENT.md`) present in
stdout. Update the module docstring's numbered coverage list to match (still 11 legs total — this is a
content change to leg 9, not a removal).

Run the full paired suite; it must still report 11 passed. Then run the scanner directly with no
arguments from the phase directory — this is the literal, first-ever real-defaults run of this gate in
this milestone, and it must print `PASS:` naming all four files plus the caveat-present count.

If it does NOT exit 0 (a real forbidden-phrase or missing-caveat violation in one of the four
authored artifacts), do not weaken the gate — read the FAIL bucket, go fix the flagged artifact
(re-open the owning plan's file: `137-LEDGER.md` from 137-03, `137-DECISION.md`/
`137-RELEASE-NOTES-app.md` from 137-04, `137-GH12-COMMENT.md` from 137-05), re-run until green, and
record which artifact needed a correction and why in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `python3 -m pytest test_check_permitted_claims_v130.py -o addopts="" -q` reports 11 passed.
    - `python3 check_permitted_claims.py` (no arguments, from the phase directory) exits **0** and its `PASS:` line names all four real files.
    - `grep -c 'test_unarmed_when_zero_of_four_default_targets_exist' test_check_permitted_claims_v130.py` returns 0 (fully replaced, not left dangling alongside the new one).
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup && python3 -m pytest test_check_permitted_claims_v130.py -o addopts="" -q 2>&1 | tail -3 | grep -qE '11 passed' && OUT=$(python3 check_permitted_claims.py) && echo "$OUT" | grep -q 'PASS:' && for f in 137-LEDGER.md 137-DECISION.md 137-RELEASE-NOTES-app.md 137-GH12-COMMENT.md; do echo "$OUT" | grep -q "$f" || { echo "PASS line omits $f"; exit 1; }; done && echo CLOSE-01-ARMED-AND-GREEN</automated>
  </verify>
  <done>The claim gate, run with its real default targets, is armed and green, naming all four artifacts; the test suite reflects the armed reality with 11 passed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm the host-side gate, then run the final milestone-wide CI-parity recipe</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-CI-PARITY.md</files>
  <read_first>
    - `.planning/phases/136.1-sdp-partition-provenance/136.1-CI-PARITY.md`'s `## After (phase close)` —
      the shape and metric set to reproduce one final time, plus the Phase 136.1 baseline numbers
      (mypy 33/35, checked 132, suite 1504, coverage 82.14%) this task diffs against.
    - `.planning/ROADMAP.md`'s Phase 137 `**Cross-cutting**` line: "Run the CI-parity recipe one final
      time over the whole milestone diff before closing."
  </read_first>
  <action>
From `/workspaces/firestarter_app`, using `tools/ci_replica_venv.sh`'s venv (never the devcontainer
python):

1. Re-run `python3 tools/check_diagnostic_report_claims.py` (plan 137-02) — must still exit 0 (nothing
   in plans 137-03/04/05 touches `diagnostic_report.py`, so this is a confirmation, not expected to
   change; record it anyway per "measure, don't assume").
2. Run `tools/ci_parity.sh` (all legs) and `tools/ci_replica_venv.sh` — record mypy errors/watermark/
   headroom, checked-source-file count, full suite pass count, coverage percentage, ruff status.
   **Do NOT move the watermark of 35** regardless of the measured error count.
3. Diff every metric against the Phase 136.1 baseline (33/35 mypy, headroom 2, 132 checked, 1504
   suite, 82.14% coverage) — the suite count and checked-file count SHOULD have grown by exactly the
   test files plans 137-01/137-02 added (report the exact delta and reconcile it; do not just assert
   "unchanged" if it changed).
4. Confirm the SDP partition is unchanged: 43 ALLOW / 41 REFUSE / 84 total (this phase touches no
   database file, so this should be a trivial re-confirmation).
5. Confirm no file under `/workspaces/firestarter/` (the firmware submodule) was touched anywhere in
   this phase's five plans (`git -C /workspaces/firestarter status --porcelain` shows only pre-existing
   dirt, nothing new), and no new pip/npm/cargo package was installed.

Write `137-CI-PARITY.md` with `## Before (phase entry)` (the Phase 136.1 baseline, cited) and
`## After (whole-milestone, phase close)` sections, matching the shape of every prior phase's own
CI-PARITY doc in this milestone.
  </action>
  <acceptance_criteria>
    - `tools/ci_replica_venv.sh` reports `CI-REPLICA: PASS`, mypy watermark still 35 (not moved), the measured error count and headroom recorded exactly.
    - The full suite count and checked-file-count deltas are reconciled explicitly against 1504/132, not silently asserted unchanged.
    - The SDP partition is confirmed 43/41/84.
    - `137-CI-PARITY.md` exists with both Before and After sections.
    - Firmware submodule untouched; zero new package installs.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh 2>&1 | tail -20 | grep -q 'CI-REPLICA: PASS' && test -f /workspaces/.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-CI-PARITY.md && echo FINAL-CI-PARITY-RECORDED</automated>
  </verify>
  <done>The host-side gate is reconfirmed green; the whole-milestone CI-parity recipe ran once more with every metric reconciled against the Phase 136.1 baseline; 137-CI-PARITY.md records both.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize the operator batch, author the phase's closing record, and tick CLOSE-01</name>
  <files>.planning/v1.30-OPERATOR-BATCH.md, .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RECORD.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md` and
      `.planning/phases/136.1-sdp-partition-provenance/136.1-RECORD.md` — the seven/six-section shape
      to follow (requirement accounting table, decisions honoured, ROADMAP success criteria discharged
      with evidence, corrections with both readings, non-vacuity obligations, residuals, Evidence
      Ceiling restated).
    - Every SUMMARY from plans 137-01 through 137-05 in this phase.
    - `.planning/v1.30-OPERATOR-BATCH.md` in full — this task's job is to make it accurately reflect
      the final state of every item, not to re-author it.
  </read_first>
  <action>
**Update `.planning/v1.30-OPERATOR-BATCH.md`:**
- Confirm item A-1's row reflects plan 137-05's actual outcome (post-now-and-verified, or
  hold-with-follow-up-command — whichever branch 137-05 actually took).
- Confirm item C-1's row reflects plan 137-04's disposition (defer-with-owner, todo filed).
- Add a final line noting Phase 137 is now complete and this file is ready to be presented to the
  operator as one list immediately before `/gsd-complete-milestone`, per its own standing header
  instruction.
- Do not alter items B-1, B-2, C-2, C-3, D-1, D-2, E-1, E-2 — they are unrelated to Phase 137's scope.

**Author `137-RECORD.md`** with these sections:
1. **Requirement accounting** — a table with all seven of this phase's own requirements
   (CLOSE-01…CLOSE-06, RELOCK-07), which plan ticked each, and the evidence (test names / file paths /
   the claim-gate PASS line). Cross-check: `grep -c '^- \[ \] \*\*CLOSE-\|^- \[ \] \*\*RELOCK-'
   .planning/REQUIREMENTS.md` must return **0** after this task's own CLOSE-01 tick — confirm and state
   the number.
2. **The ROADMAP's six success criteria, discharged with named evidence** — quote each of Phase 137's
   own six numbered success criteria from ROADMAP.md and cite the plan/evidence that discharges it,
   the same shape `134-RECORD.md` §3 used.
3. **Corrections carried forward** — restate, in one line each with a citation, the six mandated
   corrections this ledger (137-LEDGER.md) already carries, PLUS this phase's own: the RELOCK-07 fifth
   citation drift found and fixed (137-04), and the CLOSE-06 posting-precondition timing issue (this
   plan's own design choice, recorded plainly: CLOSE-06 was discharged via review+approval+freeze,
   with the mechanical post itself sequenced to after the beta ships — state explicitly whether 137-05
   actually posted or held).
4. **Residuals, carried not closed** — the mypy watermark ratchet (still unowned); C-1's now-filed
   todo; gh#20's AT28C256 defect (`Owner: henols`, unrelated to this phase); any residual left open by
   137-05 if the "hold" branch was taken (the actual `gh issue comment` call, with its exact command).
5. **The Evidence Ceiling, restated plainly** — verbatim from `134-RECORD.md` §7, since it governs this
   phase's own claims identically: this phase proves the claim gate mechanism works, that the ledger
   correctly pairs claims with non-claims, and that the outward-facing text is reviewed — it proves
   NOTHING new about SDP behaviour on silicon.
6. **Hand-off** — this is the last of v1.30's seven phases (131, 132, 133, 134, 136, 136.1, 137 — the
   135 slot stayed vacant). State the project-wide requirement count (should now read 56/56 ticked, 0
   open — confirm via `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` against the total requirement
   count, and state the number found). Name the two remaining operator actions
   (`.planning/v1.30-OPERATOR-BATCH.md` A-1 if held, A-2) as the only things standing between this
   phase's completion and `/gsd-complete-milestone`.

**Tick CLOSE-01** in `.planning/REQUIREMENTS.md`: checkbox `[ ]` → `[x]`, traceability row `Pending` →
`Complete`, citing Task 1's `PASS:` line as evidence.
  </action>
  <acceptance_criteria>
    - `.planning/v1.30-OPERATOR-BATCH.md` accurately reflects 137-05's A-1 outcome and 137-04's C-1 disposition.
    - `137-RECORD.md` exists with all six sections.
    - `grep -c '^- \[ \] \*\*CLOSE-\|^- \[ \] \*\*RELOCK-' .planning/REQUIREMENTS.md` returns **0**.
    - `137-RECORD.md` states the project-wide ticked/open count, freshly measured, not assumed.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test "$(grep -c '^- \[ \] \*\*CLOSE-\|^- \[ \] \*\*RELOCK-' .planning/REQUIREMENTS.md)" = "0" && test -f .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RECORD.md && grep -q '\[x\] \*\*CLOSE-01' .planning/REQUIREMENTS.md && echo PHASE-137-RECORD-COMPLETE</automated>
  </verify>
  <done>The operator batch is finalized and accurate; 137-RECORD.md exists with all six sections and a freshly-measured project-wide requirement count; CLOSE-01 is ticked; zero open CLOSE-*/RELOCK-* requirements remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's own claim gate → the artifacts it scans | The first genuinely-armed run of this gate in this milestone; a false PASS here would be the exact P-11 failure mode this phase exists to prevent |
| this phase's closing record → the operator's next action (`/gsd-complete-milestone`) | The hand-off must be accurate, since it is the last thing read before an irreversible milestone-close act |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-19 | Tampering | a real forbidden phrase in one of the four authored artifacts, first discovered only now | mitigate | Task 1 requires genuine exit 0 against the real targets; on failure, fix the flagged artifact and re-run — never weaken the gate to force a pass |
| T-137-20 | Repudiation | the operator batch or phase record misstating what 137-05 actually did (posted vs. held) | mitigate | Task 3 reads 137-05's actual SUMMARY and states the real branch taken, not an assumption |
| T-137-21 | Tampering | the mypy watermark moved to paper over a regression | mitigate | Task 2 explicitly forbids moving the watermark of 35 regardless of measured count |
| T-137-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
1. `python3 check_permitted_claims.py` (no arguments) exits 0, naming all four real artifacts.
2. `tools/ci_replica_venv.sh` reports PASS with the watermark unmoved at 35.
3. `137-RECORD.md` exists with all six sections and a freshly-measured 56/56 requirement count.
4. Zero `- [ ] **CLOSE-` or `- [ ] **RELOCK-` rows remain in REQUIREMENTS.md.

**Requirement ticking:** this plan ticks **CLOSE-01 only** — the last of the seven requirements this
phase owns.
</verification>

<success_criteria>
- The claim gate is armed and green against this milestone's own four real closing artifacts.
- The CI-parity recipe ran one final time over the whole milestone diff, every metric reconciled.
- The operator batch and the phase's closing record both accurately reflect what actually happened.
- Zero open CLOSE-*/RELOCK-* requirements remain; v1.30 is ready for `/gsd-complete-milestone`.
</success_criteria>

<output>
Create `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-06-SUMMARY.md` when
done. Record: the claim gate's real PASS line, the final CI-parity numbers and their reconciliation,
the operator batch's final state, and the project-wide requirement count.
</output>
