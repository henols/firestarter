<!-- test fixture for check_permitted_claims.py — NOT a closing artifact — never add to _DEFAULT_TARGETS -->

Phase 137's honesty ledger states plainly what this milestone can and cannot show. The
plan-derivation step is measurable today with zero hardware: 43 chips resolve to ALLOW and 41
resolve to REFUSE, each NA step carrying an explicit reason. Every degenerate read-back case for
the SDP oracle -- empty, short, all-zero, all-0xFF -- is exercised in native envs, not on a bench.
Command emission over the scripted wire is observable by the host's own test doubles. As the
evidence ceiling requires: no AT28C silicon was tested.
