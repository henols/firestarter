---
phase: 137-close-honesty-ledger-claim-gate-gh12-followup
plan: 04
type: execute
wave: 4
depends_on: ["137-03"]
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/notes/sdp-surface-retirement-and-behavioral-proof.md
  - .planning/ROADMAP.md
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md
  - .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-DECISION.md
  - .planning/v1.30-OPERATOR-BATCH.md
  - .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md
autonomous: true
requirements: [CLOSE-05, RELOCK-07]
must_haves:
  truths:
    - "The stale '--sdp-relock -> v1.23+' label is corrected, at every currently-live occurrence, to name Backlog 999.28 instead of a milestone version that never carried the flag"
    - "All four citation sites that reference the stale label's line numbers (REQUIREMENTS.md's own RELOCK-07 text, PROJECT.md's own self-referential paragraph, the design note, and ROADMAP.md's v1.30 milestone-list entry) are corrected to the numbers freshly measured THIS plan, not any previously-recorded number"
    - "137-RELEASE-NOTES-app.md's Removed section maps dev sdp disable -> write (automatic) and dev sdp enable -> nothing in this release, withdrawn, tracked as Backlog 999.28 -- never write --sdp-relock"
    - "Operator-batch item C-1 (build_db_diff's ladder_state regression) receives an explicit disposition, filed with a named owner, not silently dropped"
  artifacts:
    - path: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md"
      provides: "Hand-written next-release notes body carrying the honest Removed section"
      contains: "Backlog 999.28"
    - path: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-DECISION.md"
      provides: "RELOCK-07 fix confirmation + C-1 disposition + beta-only close recommendation"
  key_links:
    - from: ".planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md"
      to: ".planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md"
      via: "both describe the same withdrawal, in the same words, per the amended todo's own instruction"
      pattern: "withdrawn|Backlog 999.28"
---

<objective>
Three closely-related documentation fixes, all light (no code touched), grouped into one plan:

1. **RELOCK-07** — fresh-measure and correct the stale `--sdp-relock` "v1.23+" deferral label
   everywhere it currently lives, AND fix the four citation sites that reference its line numbers
   (which have already drifted three times — do not trust any number recorded before this plan runs,
   including any number in this plan's own text).
2. **CLOSE-05** — write `137-RELEASE-NOTES-app.md`, the next release's notes, whose "Removed" section
   states a withdrawal (`dev sdp enable` -> nothing in this release, Backlog 999.28), never a migration
   to a command that does not exist in the release being announced.
3. **`137-DECISION.md`** — the fourth claim-gate artifact: confirms the RELOCK-07 fix, gives operator
   -batch item C-1 an explicit disposition (fix / defer-with-owner / accept — it must not be silently
   dropped), and records this phase's pre-flight recommendation (beta-only close; nothing is stable
   until the operator says so).

Purpose: CLOSE-05's honesty depends on RELOCK-07 already being correct in the documents CLOSE-05 itself
cross-references (a release note correctly describing a withdrawal, sitting next to a still-mislabeled
deferral elsewhere in the tree, is an inconsistency this milestone's own honesty discipline would
otherwise leave behind).

Output: `137-RELEASE-NOTES-app.md`, `137-DECISION.md` (two of the claim gate's four default targets),
corrected STATE.md/PROJECT.md/REQUIREMENTS.md/the design note/ROADMAP.md, and one new backlog todo for
C-1 if the disposition is defer-with-owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/v1.30-OPERATOR-BATCH.md
@.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md
@.planning/todos/pending/write-sdp-relock-deferred.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RELOCK-07 — fresh-measure and fix the stale label at every live occurrence, and all four citation sites</name>
  <files>.planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/notes/sdp-surface-retirement-and-behavioral-proof.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md`'s RELOCK-07 text in full — it names four citation sites and warns
      "do not trust any citation in the record, including this one." Treat that warning as binding on
      this plan too: every line number below is what planning measured on 2026-08-05; re-measure fresh
      at execution time regardless of what this plan says.
    - `.planning/PROJECT.md` lines ~130-158 — the "Stale labels this milestone fixes" paragraph plus
      its "AMENDED 2026-08-03" blockquote, which is itself a chain of three prior, each-superseded
      line-number citations. This plan's edit is the FOURTH and, being the milestone's own close phase,
      should be the terminal one — state the final measured numbers plainly, without adding a fifth
      speculative "these might drift again" hedge beyond one factual sentence.
    - `.planning/notes/sdp-surface-retirement-and-behavioral-proof.md` §8 (and its own top-of-file
      AMENDED blockquote) — the design note's citation, already partially self-corrected once.
  </read_first>
  <action>
**Step 1 — fresh-measure, trusting nothing cited before this plan:**
Run `grep -n "v1.23+" .planning/STATE.md .planning/PROJECT.md` and confirm the exact current line
numbers of the two literal mislabeled occurrences (a table row in STATE.md; a decision-log table row
in PROJECT.md). Do NOT use any previously-recorded line number (634/823, 538/823, 532/705, 154/671, or
any number appearing in this plan's own text) without confirming it against this fresh grep — they are
each already known-stale by at least one, and in most cases several, generations.

**Step 2 — fix the two live labels:**
- In `.planning/STATE.md`'s table row, change `--sdp-relock\` → v1.23+` to
  `--sdp-relock\` → Backlog 999.28` (keep the rest of the row's prose unchanged — this is a one-phrase
  edit, not a rewrite).
- In `.planning/PROJECT.md`'s decision-log table row, change `--sdp-relock\` deferred to v1.23+` to
  `--sdp-relock\` deferred to Backlog 999.28` (again, one-phrase edit only).

**Step 3 — fix the four citation sites**, each naming the freshly-measured line numbers from Step 1:
1. `.planning/REQUIREMENTS.md`'s RELOCK-07 requirement text itself — replace the stale
   "Measured 2026-08-03: `.planning/STATE.md:634` and `.planning/PROJECT.md:823`" sentence with the
   freshly measured pair, dated to THIS plan's execution date, and add one sentence: "this is the
   terminal correction for this requirement — RELOCK-07 is now ticked Complete." Leave the historical
   "four separate places cite this same pair and no two agree" narrative intact as a record of the
   drift, appending (not replacing) a note that a fifth citation was found to have drifted again before
   this plan ran, confirming the pattern one more time.
2. `.planning/PROJECT.md`'s own "Stale labels this milestone fixes" + "AMENDED 2026-08-03" paragraph
   (~lines 145-158) — append a final, clearly-marked correction stating the freshly-measured live
   lines and that this is the terminal fix (Phase 137, milestone close). Do not delete the prior
   AMENDED blockquote — append below it, matching this project's own append-only-correction
   convention used throughout `134-RECORD.md`/`136.1-RECORD.md`.
3. `.planning/notes/sdp-surface-retirement-and-behavioral-proof.md` §8 — add a short correction
   sentence beneath the existing paragraph (do not rewrite the section) naming the final measured
   lines, cross-referencing RELOCK-07's now-Complete status.
4. `.planning/ROADMAP.md`'s v1.30 milestone-list entry (the long paragraph beginning "v1.30 SDP Surface
   Retirement...", containing the sentence "the `STATE.md:154` / `PROJECT.md:671` citations... are a
   third, separately-drifted pair") — append one clause to that same parenthetical noting that Phase
   137 found and fixed a fourth drift and recorded the terminal values in RELOCK-07's own text (point
   there rather than re-stating the numbers a fifth time in this already-dense paragraph).

**Step 4 — tick the requirement:**
In `.planning/REQUIREMENTS.md`, change RELOCK-07's checkbox from `- [ ]` to `- [x]`, and in the
traceability table change its row from `Pending (retained, re-homed 2026-08-03)` to `Complete`.
  </action>
  <acceptance_criteria>
    - `grep -rn "v1.23+" .planning/STATE.md .planning/PROJECT.md` returns **zero** matches referring to `--sdp-relock` (a `grep -c` on the exact old row text returns 0 in both files).
    - `grep -c "Backlog 999.28" .planning/STATE.md` ≥ 1 and `grep -c "Backlog 999.28" .planning/PROJECT.md` ≥ 1 (in the corrected rows).
    - All four citation sites (REQUIREMENTS.md RELOCK-07, PROJECT.md's own paragraph, the design note §8, ROADMAP.md's milestone-list entry) contain a line-number pair matching what Step 1's fresh grep actually found, not any previously-cited pair.
    - `grep -n '\[x\] \*\*RELOCK-07' .planning/REQUIREMENTS.md` returns one match.
    - The traceability table's RELOCK-07 row reads `Complete`.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && ! grep -q '`--sdp-relock`.*v1\.23+' .planning/STATE.md && ! grep -q '`--sdp-relock`.*v1\.23+' .planning/PROJECT.md && grep -q '\[x\] \*\*RELOCK-07' .planning/REQUIREMENTS.md && echo RELOCK-07-FIXED</automated>
  </verify>
  <done>The two live stale labels now read Backlog 999.28; all four citation sites state the freshly-measured, terminal line numbers; RELOCK-07 is ticked Complete.</done>
</task>

<task type="auto">
  <name>Task 2: Author `137-RELEASE-NOTES-app.md` — the "Removed" section states a withdrawal, never a migration</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-RELEASE-NOTES-app.md</files>
  <read_first>
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-RELEASE-NOTES-app.md`
      — the shape to follow (hand-written prerelease body, because CI writes an empty body and an
      empty body still lets a reader infer capability).
    - `.planning/todos/pending/gh12-followup-after-dev-sdp-retirement.md` — constraint 4: "the 'Removed'
      mapping in the next release notes must read `dev sdp disable` -> `write` (automatic) and
      `dev sdp enable` -> withdrawn, no replacement, tracked as Backlog 999.28. Do not map it to
      `write --sdp-relock`."
    - `.planning/REQUIREMENTS.md` §Evidence Ceiling — the exact provable/not-provable wording, for the
      SDP-leg description below.
  </read_first>
  <action>
Write `137-RELEASE-NOTES-app.md`, a hand-written next-release body (this milestone's parts do not yet
have an observed cut tag — write it version-agnostic, "this release" rather than a guessed version
string, matching how `122-RELEASE-NOTES-app.md` handled the same uncertainty before its own cut).

**"Removed" section (the mandatory, mechanically-checked part):**
- `firestarter dev sdp <chip> enable|disable` is gone.
- `disable`'s behaviour is absorbed: `write` already auto-unlocks on every protocol-`0x0D` write by
  default (declinable via `--skip-sdp-unlock`), so `dev sdp disable` was genuinely redundant.
- `enable` is withdrawn, **with no replacement in this release**. State plainly: there is currently no
  supported way to deliberately protect an SDP part, and the protection bit cannot be read back to
  check the result even if there were. The design for a replacement (`write --sdp-relock`) is settled
  and tracked as Backlog 999.28 — name it as queued, never as shipped, and do not promise a version.

**Other sections, briefer:** the new `dev test` SDP leg (plan-derived for the 43 SDP-capable chips;
locks, attempts an inhibited write, reads back for equality; reports `HELD`/`NOT-HELD`/`NOT-RUN(reason)`
— this is the first oracle this feature has ever had; explicitly state it remains untested on real
AT28C silicon and `0x0D` stays `UNVERIFIED`); the stable-channel `dev` group now exposes only `read`
and `test` (Phase 136, if relevant to this release's scope — check `136-RECORD.md`/`136-VALIDATION.md`
for its actual shipped state before describing it as shipping in this same release).

Run this file's prose past the 14-label forbidden vocabulary mentally before finalizing (Task 3 of
plan 137-06 re-scans it mechanically as part of arming the claim gate) — do not write "now proven",
"the lock inhibited the write", or "write --sdp-relock" as available anywhere in this file.
  </action>
  <acceptance_criteria>
    - `137-RELEASE-NOTES-app.md` exists with a `## Removed` (or equivalently named) section.
    - `grep -c 'write --sdp-relock' 137-RELEASE-NOTES-app.md` returns **0** (never named as available).
    - `grep -c 'Backlog 999.28' 137-RELEASE-NOTES-app.md` ≥ 1.
    - `grep -c 'withdrawn' 137-RELEASE-NOTES-app.md` ≥ 1.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-RELEASE-NOTES-app.md python3 .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/check_permitted_claims.py` (run from the phase directory with the relative filename) exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces/.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup && test "$(grep -c 'write --sdp-relock' 137-RELEASE-NOTES-app.md)" = "0" && grep -q 'Backlog 999.28' 137-RELEASE-NOTES-app.md && FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-RELEASE-NOTES-app.md python3 check_permitted_claims.py | grep -q 'PASS:' && echo RELEASE-NOTES-OK</automated>
  </verify>
  <done>137-RELEASE-NOTES-app.md exists, its Removed section states a withdrawal (never a migration), never names write --sdp-relock as available, and passes the claim gate alone.</done>
</task>

<task type="auto">
  <name>Task 3: Author `137-DECISION.md` — RELOCK-07 confirmation, C-1's disposition, and the pre-flight beta-only recommendation</name>
  <files>.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-DECISION.md, .planning/v1.30-OPERATOR-BATCH.md, .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md</files>
  <read_first>
    - `.planning/phases/134-the-plan-derived-sdp-oracle-in-dev-test/134-RECORD.md` §6 residual 4 (the
      `ladder_state` finding in full: `build_db_diff`'s `ladder_state` no longer reaches
      `community-reported` for a genuinely-passing ALLOW chip, because an all-OK run attaches an
      `"indeterminate"`-classified `Fingerprint` on the baseline/restored steps, which
      `classify_fingerprint`'s four-bucket design has no "perfect match" bucket for).
    - `.planning/v1.30-OPERATOR-BATCH.md` §C, item C-1.
    - `.planning/phases/122-close-honesty-ledger-community-ask-release-decision/122-DECISION.md` — the
      shape (pre-flight measurement, explicit accept/avoid/cleanup framing) to follow for the beta-only
      recommendation section.
  </read_first>
  <action>
Write `137-DECISION.md` with three sections:

**1. RELOCK-07 confirmation.** One paragraph: the stale label is fixed (cite the freshly-measured
lines from Task 1), all four citation sites agree, the requirement is ticked Complete.

**2. Operator-batch C-1 disposition.** `build_db_diff`'s `ladder_state` regression touches
`diagnostic_report.py`/`classify_fingerprint`, both **outside every Phase 137 plan's declared file
scope** — fixing it here would be an undeclared scope expansion in the milestone's own honesty-close
phase, the opposite of this phase's purpose. Disposition: **defer-with-owner**. File
`.planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md` (front-matter:
`title`, `date` today, `priority: low`, `blocked_by: nothing technical`, `resolves_phase: none`),
describing the finding verbatim from `134-RECORD.md` §6 residual 4, with `Owner: henols` (matching this
project's established convention for genuinely-unowned findings that need a human decision, e.g.
`at28c256-write-path-failure-gh20.md`). Then update `.planning/v1.30-OPERATOR-BATCH.md`'s C-1 row: change
"needs a disposition before close" to "Disposition: defer-with-owner, filed as
`.planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md`, Owner: henols
— Phase 137, plan 137-04". Do not fix the underlying `classify_fingerprint`/`ladder_state` code in this
plan.

**3. Pre-flight recommendation.** State plainly, per the standing operator instruction ("nothing is
stable until I say so" — beta-only at milestone close, matching every prior milestone in this project):
this phase recommends the milestone stays on the `--pre`/beta channel at close; no stable-channel
promotion is implied or requested by anything in Phase 137. Note the two items still requiring the
operator directly (cross-reference `.planning/v1.30-OPERATOR-BATCH.md` A-1, A-2) and that this
decision doc records a recommendation, not an action — it pushes, merges, and publishes nothing itself.
  </action>
  <acceptance_criteria>
    - `137-DECISION.md` exists with all three numbered sections.
    - `.planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md` exists, cites `134-RECORD.md` §6 residual 4, and names `Owner: henols`.
    - `.planning/v1.30-OPERATOR-BATCH.md`'s C-1 row no longer reads "needs a disposition before close" — it names the defer-with-owner disposition and the new todo's path.
    - `FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-DECISION.md python3 check_permitted_claims.py` (from the phase directory) exits **0**.
  </acceptance_criteria>
  <verify>
    <automated>cd /workspaces && test -f .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-DECISION.md && test -f .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md && grep -q "Owner: henols" .planning/todos/pending/build-db-diff-ladder-state-community-reported-regression.md && ! grep -q "needs a disposition before close" .planning/v1.30-OPERATOR-BATCH.md && cd .planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup && FIRESTARTER_CLAIMSCAN_TARGETS_V130=137-DECISION.md python3 check_permitted_claims.py | grep -q 'PASS:' && echo DECISION-DOC-OK</automated>
  </verify>
  <done>137-DECISION.md exists with all three sections; C-1 has an explicit defer-with-owner disposition filed as a named-owner todo; the operator batch reflects the disposition; the file passes the claim gate alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a documentation-only correction → four independently-drifting citation sites | RELOCK-07's own history (three prior drifts) proves this boundary is real, not theoretical, in this exact project |
| a known-open finding (C-1) → silent disappearance at milestone close | The hard requirement this task exists to prevent — "the close must not silently drop it" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-12 | Tampering | a fifth line-number drift on RELOCK-07's citations | mitigate | Fresh `grep` at execution time, not any previously-recorded number; all four citation sites edited in the same task |
| T-137-13 | Information Disclosure | release notes implying a migration to a command that does not exist | mitigate | Explicit "withdrawn, no replacement" wording; `grep -c 'write --sdp-relock'` returns 0; claim-gate scan alone |
| T-137-14 | Repudiation | operator-batch C-1 silently dropped at close | mitigate | Explicit defer-with-owner disposition, named-owner todo filed, operator batch row updated in the same task |
| T-137-SC | Tampering | npm/pip/cargo installs | accept | Zero installs in this plan's scope |
</threat_model>

<verification>
1. RELOCK-07's stale label is gone from both live occurrences; all four citation sites agree on the
   freshly-measured lines; the requirement is ticked Complete.
2. `137-RELEASE-NOTES-app.md` states a withdrawal, never names `write --sdp-relock` as available, and
   passes the claim gate alone.
3. `137-DECISION.md` exists with an explicit C-1 disposition, a filed named-owner todo, and an updated
   operator batch; passes the claim gate alone.

**Requirement ticking:** this plan ticks **CLOSE-05 and RELOCK-07**.
</verification>

<success_criteria>
- RELOCK-07 is genuinely, terminally fixed — not a fourth entry in the drift chain.
- The next release's notes are honest about a withdrawal.
- C-1 has a real, named-owner disposition instead of silent disappearance.
</success_criteria>

<output>
Create `.planning/phases/137-close-honesty-ledger-claim-gate-gh12-followup/137-04-SUMMARY.md` when
done. Record: the freshly-measured line numbers, the exact wording change in STATE.md/PROJECT.md, the
C-1 disposition and its filed todo path, and both claim-gate PASS lines (release notes alone, decision
doc alone).
</output>
