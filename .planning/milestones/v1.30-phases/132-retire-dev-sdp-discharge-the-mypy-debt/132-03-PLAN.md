---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 03
type: execute
wave: 3
depends_on: ["132-02"]
files_modified:
  - firestarter_app/tests/test_sdp_honesty.py
  - firestarter_app/tools/check_no_exists_proxy.py
  - firestarter_app/tests/test_write_skip_sdp_unlock.py
  - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PRUNE-LEDGER.md
autonomous: true
requirements: [RETIRE-02, RETIRE-03]
must_haves:
  truths:
    - "The fail-closed target list and the file move land in ONE commit, so the absence-proxy gate is never red for even one commit (RETIRE-02, D-03) -- and that gate has been SEEN to go red and then green, not merely observed exiting 0."
    - "All four honesty assertions still exist somewhere under tests/ after the move, retargeted onto firestarter/sdp_honesty.py, proven by four greps (RETIRE-03, D-01)."
    - "Everything pruned in the move is counted and named in the phase record, because a ~550-line deletion inside a file move is exactly the diff shape that hides a real loss (D-04)."
  artifacts:
    - firestarter_app/tests/test_sdp_honesty.py
    - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PRUNE-LEDGER.md
  key_links:
    - "tools/check_no_exists_proxy.py's _DEFAULT_TARGETS is a literal, non-glob, alphabetically sorted list -- the rename is a delete plus an insert at a new position, not an in-place string edit."
    - "tests/test_write_skip_sdp_unlock.py cites the old module by name in two docstrings; the move stales both, so they are corrected in the same commit."
---

<!-- planner-discipline-allow: dev sdp -->
<!-- planner-discipline-allow: test_dev_sdp_cmd.py -->

<objective>
Move the dev-sdp test module to `tests/test_sdp_honesty.py` in the same commit as the gate's
target-list edit, retarget the four honesty assertions onto the helper authored in plan 132-02, and
prune the rest with a counted, named ledger entry.

Purpose: 132-CONTEXT.md's Specific-Ideas block states the phase's real deliverable plainly -- the
deletion is ~126 lines and one snapshot line, but what can actually be *lost* here is four assertions
that exist nowhere else in the tree. This plan is where that loss is either prevented or silently
taken. RETIRE-03 protects four tests out of 558 lines, and D-04 requires the other ~550 lines be
accounted rather than quietly dropped inside a rename.

Output: the moved and rewritten test module, the one-line gate edit, two corrected docstring
citations, and `132-PRUNE-LEDGER.md`.

May mark Complete: **RETIRE-02 and RETIRE-03.** Both are fully discharged here -- RETIRE-02 by the
single same-commit proof in task 1, RETIRE-03 by the four surviving assertions plus the four-grep
criterion in task 2. Plan 132-02 was explicitly forbidden from ticking RETIRE-03; this plan owns it.
May NOT mark Complete: RETIRE-01 (owned by 132-04), RETIRE-04 (132-08), RETIRE-05 (132-05),
RETIRE-06 (132-09), RETIRE-07 (132-07), RETIRE-08 (132-08).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-02-SUMMARY.md
</context>

<repo_map>
Tasks 1 and 2 and the code half of task 3 are in the **`firestarter_app` submodule**
(`/workspaces/firestarter_app/`, branch `gsd/v1.30-sdp-surface-retirement`) and commit there.
`132-PRUNE-LEDGER.md` is a **meta-repo** file (`/workspaces/`, branch
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`) and commits there.
The firmware repo is not touched.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files** -- `firestarter_app/firestarter/sdp_honesty.py` (132-02),
`firestarter_app/tools/ci_replica_venv.sh` (132-01), `firestarter_app/tests/test_sdp_honesty.py`
(**this plan**, from the file move)

**New functions in `firestarter/sdp_honesty.py`** (132-02) -- `unreadable_state_caveat`,
`emission_summary`, `map_unknown_cmd_to_outdated`

**New symbols in `tests/conftest.py`** (132-05) -- `make_app_context(...)`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` in
`tests/test_sdp_honesty.py` (**this plan**);
`test_command_names_dereferences_both_sdp_commands` (132-08);
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` (132-07)

**New config entries** -- `"firestarter.sdp_honesty"` in the Phase-42 strict island (132-02)

**New constants:** none in `constants.py`.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01),
`132-PRUNE-LEDGER.md` (**this plan**), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Re-measured at plan time against `firestarter_app` @ `8caf77f`.

- **Exactly three in-tree references** to the old module name exist, and the move stales all three:
  `tools/check_no_exists_proxy.py:157` (the gate's literal target), and
  `tests/test_write_skip_sdp_unlock.py:42` and `:58` (two docstring citations). Nothing else in the
  tree -- no `.toml`, no `.yml`, no `.md`, no other `.py` -- names it. `tests/test_audit_coverage_matrix.py`
  and `tests/test_coverage_floor_v18.py` do **not** enumerate test module names, so no golden or
  ledger is staled by the rename.
- `tools/check_no_exists_proxy.py:157` confirmed exact. `_DEFAULT_TARGETS` at `:125-157+` is a
  comprehension over a literal tuple of relative paths, `_HERE`/`_APP_ROOT`-anchored at `:113-116`,
  and **alphabetically sorted**. The new name sorts after `tests/test_scan_paths_resolve.py`-class
  entries and before `tests/test_serial_*`, so the edit is a delete plus an insert at a different
  position. The recorded `_HERE`-resolves-to-the-wrong-directory trap does **not** apply to this
  checker -- its anchoring resolves locally, verified.
- The four survivors, re-measured in the pre-move file: `:395`
  `test_summary_line_carries_the_unreadable_state_caveat_on_both_directions`, `:423`
  `test_summary_line_carries_no_duration_figure`, `:453`
  `test_no_fabricated_lock_state_boolean_in_the_report`, `:513`
  `test_firmware_too_old_is_reported_when_unknown_cmd_comes_back`.
- The module is 558 lines and currently collects **26** tests. The pre-move file's local
  `make_app_context` at `:80` carries **6** of the phase's mypy errors (`:94-99`, all `[arg-type]`
  `object` into `AppContext`) -- removing that factory entirely is the measured 69 -> 63 step.
- The line-selection idiom the first three survivors share is at `:445-450` and `:471-473`: the
  assertions are scoped to the host's own summary line via a `next(...)` generator over
  `result.output.splitlines()`, never the whole capture. Retargeting onto a helper that returns
  exactly that line preserves the scoping **by construction**, which is strictly stronger.
- The D-14 survivor's mock shape is at `:521-536`: `Mock(spec=EpromOperator)` with
  `sdp_lock.side_effect = EpromOperationError("Unknown command: 9", error_code=MSG_ERR_UNKNOWN_CMD)`.
- `_off_tty()` / `_on_tty()` are at `:110-117`. `_ALLOWED_CHIP = "AT28C256"` is the chip the
  survivors use. `_ABSENT_CHIP`, `_FRAM_CHIP`, `_PRESDP_DIP2816_CHIP`, `_NON_0X0D_CHIP` and the
  nine-element `_ADAPTER_REQUIRED_CHIPS` list serve only pruned cases.
- Coverage that makes the prune safe, verified present: `tests/test_sdp_db_invariant.py` (Phase
  131's 43/41/84 partition gate, seven named tests including two non-vacuity legs) and
  `tests/test_check_sdp_capability.py`. Between them the capability partition and its non-vacuity are
  already fully covered, which is why D-04 forbids retargeting the chip-resolution and
  capability-refusal cases onto `sdp_capability()` -- that would be duplicate coverage dressed as
  preservation.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: The single same-commit move -- file rename, gate target list, and both stale citations (RETIRE-02, D-03)</name>
  <files>firestarter_app/tests/test_sdp_honesty.py, firestarter_app/tools/check_no_exists_proxy.py, firestarter_app/tests/test_write_skip_sdp_unlock.py</files>
  <read_first>
    - /workspaces/firestarter_app/tools/check_no_exists_proxy.py lines 40-165 (the docstring's explicit-non-pattern-target-list rationale, the `_HERE`/`_APP_ROOT` anchoring, and the full alphabetically-sorted literal tuple)
    - /workspaces/firestarter_app/tests/test_check_no_exists_proxy.py (the paired pytest module that runs the checker over its default targets, and its fail-closed legs -- this is what makes the gate's redness observable from the suite)
    - /workspaces/firestarter_app/tests/test_write_skip_sdp_unlock.py lines 38-62 (the two docstring citations at :42 and :58 that the move stales)
  </read_first>
  <action>
Make one commit containing exactly four things: the file rename, the gate's target-list edit, and the
two corrected docstring citations.

Use `git mv tests/test_dev_sdp_cmd.py tests/test_sdp_honesty.py` so the rename is recorded as a rename
rather than a delete plus an add -- the diff shape matters for the record, and D-04's accounting reads
that diff.

In `tools/check_no_exists_proxy.py`, remove the old relative-path entry from `_DEFAULT_TARGETS` and
insert `"tests/test_sdp_honesty.py"` at its correct alphabetical position. The list's sortedness is a
maintained property, so do not simply overwrite the string in place at its old index. Change nothing
else in that file -- not the docstring, not the guard ordering, not the env seam.

In `tests/test_write_skip_sdp_unlock.py`, correct the two docstring citations that name the old module:
the module-level comment near `:42` about mirroring its fixture chips, and the factory docstring near
`:58` about mirroring its factory shape. Point both at `tests/test_sdp_honesty.py`. Do not touch that
file's code, its factory signature, or any test in it -- plan 132-05 retypes that factory and plan
132-07 adds a test to it, and both must see an otherwise-untouched file.

Then prove the RETIRE-02 property the way the verification-honesty bar requires: a gate that has never
been seen to fail proves nothing. Perform an explicit RED-then-GREEN demonstration and record the
failure text:
  1. After committing, temporarily revert only the one-line target-list edit in the working tree (leave
     the rename in place), run `python3 tools/check_no_exists_proxy.py`, and capture the non-zero exit
     and the verbatim missing-target message. Read the failure reason -- do not merely note that it
     exited non-zero.
  2. Restore the edit with `git checkout -- tools/check_no_exists_proxy.py`, re-run, and capture the
     zero exit.
  3. Also run `python -m pytest tests/test_check_no_exists_proxy.py -q` and confirm it passes, since
     that paired module -- not any CI step -- is what carries this gate inside the suite. The gate is
     deliberately **not** a `ci.yml` step and deliberately **not** a leg of `tools/ci_parity.sh`;
     `ci_parity.sh`'s own header explains why.
Record both captured outputs in the summary. Confirm and state that the demonstration ended with the
working tree clean, so the RED half left nothing behind.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python3 tools/check_no_exists_proxy.py; python -m pytest tests/test_check_no_exists_proxy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app log -1 --name-status` shows a rename (`R`) from the old test path to `tests/test_sdp_honesty.py`, plus modifications to `tools/check_no_exists_proxy.py` and `tests/test_write_skip_sdp_unlock.py`, and nothing else -- one commit, all four changes.
    - `grep -rn "test_dev_sdp_cmd" /workspaces/firestarter_app --include='*.py' --include='*.toml' --include='*.yml' --include='*.md'` returns no matches.
    - `grep -c "tests/test_sdp_honesty.py" /workspaces/firestarter_app/tools/check_no_exists_proxy.py` returns 1.
    - `python -c "import re; s=open('/workspaces/firestarter_app/tools/check_no_exists_proxy.py').read(); m=re.search(r'_DEFAULT_TARGETS = \[(.*?)\]', s, re.S).group(1); ts=re.findall(r'\"(tests/[^\"]+)\"', m); assert ts==sorted(ts), [t for i,t in enumerate(ts) if i and t<ts[i-1]]; print('SORTED OK', len(ts))"` exits 0 -- the list is still alphabetically sorted after the insert.
    - `python3 tools/check_no_exists_proxy.py` exits 0, and `python -m pytest tests/test_check_no_exists_proxy.py -q` reports zero failures.
    - The RED demonstration is recorded in the summary with the checker's verbatim missing-target message and its non-zero exit code, and the GREEN re-run's exit 0. A recorded exit-0 observation alone does not satisfy this criterion.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty after the demonstration.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/` produces no output -- no production file changed in this commit.
  </acceptance_criteria>
  <done>One commit carries the rename, the correctly-positioned target-list entry, and both corrected citations; the gate has been seen to go red for the missing target and then green, with the failure text read and recorded; the tree is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget the four honesty assertions onto the helper, and add the import-purity test (RETIRE-03, D-01)</name>
  <files>firestarter_app/tests/test_sdp_honesty.py</files>
  <read_first>
    - /workspaces/firestarter_app/tests/test_sdp_honesty.py (the moved file in full -- the module docstring's FALSE-GREEN TRAP paragraph, the four survivors at their post-move positions, the shared summary-line-selection idiom, and everything task 3 prunes)
    - /workspaces/firestarter_app/firestarter/sdp_honesty.py (the three function signatures the assertions retarget onto)
    - /workspaces/firestarter_app/tests/test_sdp_db_invariant.py (Phase 131's partition gate -- read enough to confirm the capability partition really is covered elsewhere, so task 3's prune is justified rather than assumed)
  </read_first>
  <behavior>
    - The unlock-direction test asserts the caveat token appears for BOTH `enable` and `disable`, calling the helper twice.
    - The no-duration test asserts the helper's returned line matches no digit-plus-time-unit pattern, using the same regex as before.
    - The no-fabricated-lock-state test asserts all three tokens on the helper's returned line, using the same three literals as before.
    - The firmware-too-old test asserts the mapper returns a FirmwareOutdatedError carrying the install remediation, driven by an EpromOperationError whose error_code is MSG_ERR_UNKNOWN_CMD -- the same mock-free equivalent of the mock shape it used before.
    - A new purity test parses the helper module by AST and asserts its top-level import set is within the declared invariant; it fails if `click` is planted as a top-level import.
  </behavior>
  <action>
Rewrite the moved module so its four surviving tests exercise `firestarter/sdp_honesty.py` directly
instead of driving the subcommand through CliRunner. Keep all four **test function names byte-identical**
-- the names are the record, RETIRE-03 and ROADMAP criterion 2 both key on them, and plan 132-09's phase
record cites them.

For each survivor, preserve the assertion literals exactly and preserve each docstring's substance,
adding one sentence to each recording what changed and why: the SUT is now the shared helper, because
the subcommand that was the wording's only production carrier is retired in this phase and the
replacement leg is Phase 134's. Do not soften any assertion, and do not convert any of the four into a
source-string-presence check -- D-01 rejected that explicitly as converting four behavioural
assertions into a scanning gate, the fail-open class this milestone exists to close.

Retarget concretely:
- The unlock-direction test calls `emission_summary` for `enable` and for `disable` and asserts
  `cannot be read back` in each. Note in its docstring that the symmetry it pins is now unconditional
  by construction, since one function serves both directions.
- The no-duration test calls `emission_summary` once and applies the existing regex for a digit
  followed by an optional space and a time unit, case-insensitively, asserting no match. State in the
  docstring that the previous `next(...)` line-selection is no longer needed because the helper returns
  exactly the host's own line -- the scoping is now structural rather than selected.
- The no-fabricated-lock-state test calls `emission_summary` and asserts all three original literals on
  the returned string.
- The firmware-too-old test constructs an `EpromOperationError` with `error_code` set to
  `MSG_ERR_UNKNOWN_CMD`, passes it to `map_unknown_cmd_to_outdated`, and asserts the returned exception
  is not None, that its string contains the install remediation, and that it contains `does not
  implement` -- the same two assertions as before. Add a negative leg in the same test or an adjacent
  one asserting a different `error_code` maps to None, so the test proves the mapper discriminates
  rather than always mapping.

Add one new test named `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` that parses
`firestarter/sdp_honesty.py` with `ast` and asserts its top-level import set -- top-level statements
only, so a future `TYPE_CHECKING` block is not mistaken for a runtime import -- lies within the
invariant the module declares in its own comment. This is the enforcement that turns that comment into
a checked property, and it protects the forward contract: Phase 134's report layer cannot import a
module that pulls in `click`.

Prove the purity test is not unreachable-green: temporarily add a top-level `import click` to the
helper, run the test, read the failure, then revert. A pre-authored assertion that has never been seen
to fail proves nothing, and this project has a recorded instance of a gate that scanned nothing and
exited 0.

Rewrite the module docstring: keep the FALSE-GREEN TRAP paragraph's lesson but restate it for the new
SUT -- an exit-code-only assertion was the known false-green when the SUT was a CLI surface with four
identically-exiting refusals; with a pure wording helper the equivalent false-green is asserting that a
function exists rather than what it returns. Drop the `_is_interactive`-patching rationale paragraph,
because no test in the rewritten module patches it. State plainly in the docstring what these four
tests now do and do not prove: they pin the **wording**, and between this phase and Phase 134 that
wording has no user-reachable carrier at all (D-05's residual) -- the delivery path was proven once, in
plan 132-02's equivalence run, and cannot be re-proven from this module.

Update the module's imports to only what remains in use. Do not leave an unused import behind for ruff
to find later; `ruff check` with the `F` rules is the proof.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_sdp_honesty.py -q; ruff check firestarter/ tests/; ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - All four greps return exactly 1 each, in `tests/test_sdp_honesty.py`: `grep -c "def test_summary_line_carries_the_unreadable_state_caveat_on_both_directions"`, `grep -c "def test_summary_line_carries_no_duration_figure"`, `grep -c "def test_no_fabricated_lock_state_boolean_in_the_report"`, `grep -c "def test_firmware_too_old_is_reported_when_unknown_cmd_comes_back"`. This is ROADMAP criterion 2's grep proof, satisfied by name.
    - `grep -rc "cannot be read back" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns at least 3, and `grep -c "not a claim about the chip's actual state" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns at least 1 -- the assertion literals survived, not just the names.
    - `grep -c "firestarter fw --install" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns at least 1.
    - `grep -c "def test_sdp_honesty_module_imports_only_leaf_firestarter_modules" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns 1.
    - `python -m pytest tests/test_sdp_honesty.py -q` reports 0 failures and 0 errors, and the collected count is recorded in the summary.
    - `grep -c "CliRunner" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns 0 -- the module no longer drives a CLI surface, so it cannot be silently re-coupled to the subcommand being deleted in plan 132-04.
    - The purity test's RED demonstration is recorded in the summary: the planted top-level `click` import, the test's verbatim failure output, and confirmation that the plant was reverted and `git status --porcelain` is clean.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0 -- no unused import survived the rewrite.
    - `python -m pytest tests/ -q` reports zero failures across the whole suite.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output.
  </acceptance_criteria>
  <done>The four assertions live in the moved module under their original names with their original literals, retargeted onto the helper; a purity test enforces the module's declared import invariant and has been seen to fail on a planted `click`; nothing drives the CLI from this module any more.</done>
</task>

<task type="auto">
  <name>Task 3: Prune the dead cases, and write 132-PRUNE-LEDGER.md -- counted and named (D-04)</name>
  <files>firestarter_app/tests/test_sdp_honesty.py, .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PRUNE-LEDGER.md</files>
  <read_first>
    - /workspaces/firestarter_app/tests/test_sdp_honesty.py (the state after task 2 -- every remaining case, helper and module constant, so the prune is enumerated from the file rather than from memory)
    - /workspaces/firestarter_app/tests/test_check_sdp_capability.py (the capability-predicate gate tests, so the ledger's justification cites real coverage)
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md the D-04 paragraph (the exact standard: "no net loss" must be a measured claim about the four honesty assertions PLUS an accounted loss elsewhere)
  </read_first>
  <action>
Remove from the moved module every case whose subject dies with the subcommand, and every helper and
module constant that only those cases used -- then account for the removal.

Prune the surface-shape case, the gate-ordering cases (absent chip, capability refusal, the
nine-part adapter-required parametrisation, the confirm prompt, the assume-yes bypass, the off-TTY
refusal), the warning-band case, and the exit-code-contract case. Also prune the now-dead
off-TTY/on-TTY context-manager helpers, the local `make_app_context` factory, and the chip-name
constants that only pruned cases referenced -- keep only the allowed-chip constant the survivors use.
Removing the local factory is the measured six-error step of this phase's mypy arithmetic; the
module must end with no local `AppContext` construction at all.

Do **not** retarget the chip-resolution or capability-refusal cases onto `sdp_capability()`. D-04
rejects it explicitly: `tests/test_sdp_db_invariant.py` and `tests/test_check_sdp_capability.py`
already cover the partition and its non-vacuity, so re-authoring them elsewhere is duplicate coverage
dressed as preservation.

Then write `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PRUNE-LEDGER.md`. This
file is the whole point of the task: a ~550-line deletion inside a rename is exactly the diff shape
that hides a real loss, so the ledger must make the loss legible to a reader who was not here.

It must contain:
- A `## 1. What survived` table naming the four test functions, their pre-move line numbers, the
  assertion literals each keys on, and their new SUT.
- A `## 2. What was pruned` table with one row per removed test function and per removed helper or
  constant, giving its name, its pre-move line number, what it asserted, and the disposition: either
  `gate dies with the command` (the subject no longer exists) or `covered elsewhere` naming the exact
  file and test that covers it.
- A `## 3. Counts` section with the arithmetic: tests collected before the move, tests collected
  after, the number pruned, the number retargeted, the number newly added, and the file's line count
  before and after. Every number read from a command, and the command named.
- A `## 4. The accounted loss` section stating, without hedging, what this phase gives up. Name at
  least these: the gate-ordering *ordering* proof -- that the capability gate outranks the
  support-status gate, which mattered on all nine adapter-required parts and is asserted nowhere else
  after this commit; the off-TTY refusal behaviour; the binary exit-code contract; and the fact that
  the four survivors now pin wording rather than delivery. For each, say whether it is a loss of
  coverage for code that still exists (a real reduction) or coverage for code being deleted in plan
  132-04 (not a loss at all). Be explicit about which is which -- that distinction is the entire
  value of the ledger.
- A `## 5. Why "no net loss" is defensible` section tying it together: the four honesty assertions
  survive and are measured to pass; the capability partition is covered by Phase 131's gate; and the
  losses in section 4 that concern still-existing code are enumerated rather than discovered later.

Commit the module prune in the submodule and the ledger in the meta-repo, and say which commit is
which in the summary.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_sdp_honesty.py -q; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "make_app_context" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns 0 -- no local factory remains, which is the six-error reduction this phase's arithmetic depends on.
    - `grep -cE "^_[A-Z][A-Z_]* = " /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns 1 -- exactly one module-level private constant survives (the allowed-chip name). Unused module constants are invisible to ruff, so this count is the only mechanical guard against leaving dead ones behind.
    - `grep -c "_off_tty\|_on_tty" /workspaces/firestarter_app/tests/test_sdp_honesty.py` returns 0.
    - `wc -l < /workspaces/firestarter_app/tests/test_sdp_honesty.py` is under 200, and both the before (558) and after values appear in 132-PRUNE-LEDGER.md section 3.
    - 132-PRUNE-LEDGER.md exists and contains all five section headings (`## 1.` through `## 5.`).
    - Section 2's table has at least 8 rows, and every row carries a disposition of either `gate dies with the command` or `covered elsewhere` naming a file and test.
    - Section 3's numbers are each attributed to the command that produced them, and the after-count matches the collected count from `python -m pytest tests/test_sdp_honesty.py -q`.
    - Section 4 explicitly distinguishes losses of coverage for still-existing code from losses of coverage for code plan 132-04 deletes, and names the capability-before-support-status ordering proof as one of the enumerated items.
    - `python -m pytest tests/ -q` reports zero failures.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - The submodule commit and the meta-repo commit are separate and each is named in the summary with its sha.
  </acceptance_criteria>
  <done>The module contains only the four survivors plus the purity test; every removal is named in the ledger with a disposition; the accounted loss is stated without hedging and separates real reductions from coverage of soon-deleted code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the fail-closed gate → its own target list | `check_no_exists_proxy.py` decides what it scans from a literal list; a stale entry is the difference between a scanning gate and a vacuous one. |
| the phase record → a future reader | `132-PRUNE-LEDGER.md` is the only artifact that will tell a later reader what a ~550-line deletion removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-07 | Tampering | `_DEFAULT_TARGETS` after the rename | high | mitigate | The target-list edit and the rename are one commit (RETIRE-02/D-03), and the property is proven by a RED-then-GREEN demonstration with the missing-target message read verbatim -- not by observing exit 0, which is the failure mode that let a prior outward-facing gate scan nothing and pass. A sortedness assertion additionally prevents the entry being appended at a position that a future maintainer's alphabetical insert would duplicate. |
| T-132-08 | Repudiation | the four honesty assertions | high | mitigate | Four name greps plus three literal greps, and the assertions are behavioural (they call the helper and inspect its return) rather than source-scanning. Any weakening to a presence check violates an explicit criterion. |
| T-132-09 | Denial of service | the purity test's non-vacuity | medium | mitigate | The AST test is proven to fail on a planted top-level `click` import before being trusted, and it reads only top-level statements so a future `TYPE_CHECKING` block cannot silently widen what passes. |
| T-132-10 | Information disclosure | the prune's legibility | medium | mitigate | `132-PRUNE-LEDGER.md` enumerates every removal with a disposition and separates real coverage reductions from coverage of code deleted one plan later, so the loss is stated rather than absorbed into a rename diff. |
| T-132-SC | Tampering | package installs | high | accept | No package is installed or added in this plan; `pyproject.toml` is not touched. The Package Legitimacy Gate's trigger condition is not met. |
</threat_model>

<verification>
1. `git -C /workspaces/firestarter_app log --oneline -3` shows the same-commit move, then the
   retarget, then the prune -- with the move commit's `--name-status` showing an `R` rename plus the
   two other files.
2. `cd /workspaces/firestarter_app && python3 tools/check_no_exists_proxy.py` exits 0, and the RED
   demonstration's captured failure text is in the summary.
3. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
4. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
5. All four survivor-name greps and the three literal greps return their expected counts.
6. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` is empty.
7. `132-PRUNE-LEDGER.md` has all five sections and section 2 has at least 8 dispositioned rows.
</verification>

<success_criteria>
- One commit carries the rename, the correctly-positioned target-list entry, and both corrected
  citations; the gate was seen red then green with the failure reason read.
- The four honesty assertions exist under their original names in `tests/test_sdp_honesty.py`, keyed
  on their original literals, exercising `firestarter/sdp_honesty.py`.
- A purity test enforces the helper's declared import invariant and has been seen to fail.
- Every pruned case and helper is named in `132-PRUNE-LEDGER.md` with a disposition, and the
  accounted loss is stated plainly.
- RETIRE-02 and RETIRE-03 are marked Complete. No other RETIRE id is touched.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-03-SUMMARY.md` when done.
State in it: the move commit sha and its `--name-status` output, the checker's verbatim RED message
and the GREEN re-run, the purity test's RED output on the planted import, the collected test count
before and after, and an explicit line reading "Requirements marked Complete: RETIRE-02, RETIRE-03".
</output>
