---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 06
type: execute
wave: 6
depends_on: ["132-05"]
files_modified:
  - firestarter_app/firestarter/config.py
  - firestarter_app/firestarter/database.py
  - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md
autonomous: true
requirements: [RETIRE-06]
must_haves:
  truths:
    - "The six unannotated-collection errors are annotated, and the resulting error count is MEASURED in the numpy-free replica venv and recorded with the mypy line that proves each one is gone."
    - "The count reaches at or below the existing watermark of 35 without editing the ring-fenced operations module (RETIRE-06, D-07) and without touching the watermark (D-09)."
    - "The measured true count is recorded as a named input to a later phase's ratchet, and the resulting silent headroom is stated as an accepted cost rather than quietly enjoyed (D-09)."
  artifacts:
    - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md
  key_links:
    - "All six targets sit in the follow-imports ring-fence block, but that setting only suppresses errors for modules reached BY IMPORT; the gate names both directories on mypy's command line, so these errors do surface and are countable."
    - "The singleton instance registry in config.py is one of the six targets, and the config manager's singleton behaviour is load-bearing for a test-fixture behaviour plan 132-05 preserved -- so the annotation must not change the runtime type of that mapping."
---

<objective>
Annotate the six bare-collection declarations mypy cannot infer, measure the resulting count in the
replica venv, and record the number that a later phase's watermark ratchet will consume.

Purpose: this is the last of the three subtractions in P-13's ordering -- delete, then the typed fixture,
then the mock-typing migration, then these six annotations, then measure, then certify. It is a small,
mechanical change with a large consequence: it is what brings the count under the existing watermark
without opening the ring-fence, which is precisely what makes RETIRE-06 achievable as worded and makes
the operator's out-of-scope decision on the ring-fence hold rather than quietly reverse.

Output: six annotations across two modules, and `132-MYPY-LEDGER.md` section 2 carrying the measured
post-fix count.

May mark Complete: **NOTHING.** This plan touches RETIRE-06 and gets the local count to green, but a
locally-measured count is not a green `ci` job -- plan 132-09 owns RETIRE-06's Complete transition after
the certifying dispatch. Explicitly forbidden here: ticking RETIRE-06 because the local number is at or
below 35. That is the exact overclaim class this project has recorded before. Also may not touch
RETIRE-01/02/03/05 (already Complete), RETIRE-04, RETIRE-07, RETIRE-08.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-05-SUMMARY.md
</context>

<repo_map>
The two code files are in the **`firestarter_app` submodule** (`/workspaces/firestarter_app/`, branch
`gsd/v1.30-sdp-surface-retirement`) and commit there. The ledger append is a **meta-repo** file
(`/workspaces/`, branch `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`) and commits there.
The firmware repo is not touched.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files** -- `firestarter_app/firestarter/sdp_honesty.py` (132-02),
`firestarter_app/tools/ci_replica_venv.sh` (132-01), `firestarter_app/tests/test_sdp_honesty.py` (132-03)

**New functions in `firestarter/sdp_honesty.py`** (132-02) -- `unreadable_state_caveat`,
`emission_summary`, `map_unknown_cmd_to_outdated`

**New symbols in `tests/conftest.py`** (132-05) -- `make_app_context`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` (132-03),
`test_command_names_dereferences_both_sdp_commands` (132-08),
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` (132-07)

**New config entries** -- `"firestarter.sdp_honesty"` in the Phase-42 strict island (132-02).
**This plan adds no new symbol** -- it annotates six existing declarations.

**New constants:** none in `constants.py`.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01, appended **here**),
`132-PRUNE-LEDGER.md` (132-03), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Measured at plan time in the numpy-free Python 3.11.15 replica venv with mypy 2.3.0, against
`firestarter_app` @ `8caf77f`. **This block corrects 132-PATTERNS.md, which named the wrong file.**

The six errors, each with its verbatim mypy text:

| # | Location | Verbatim mypy line |
|---|----------|--------------------|
| 1 | `firestarter/config.py:84` | `Need type annotation for "_instances" (hint: "_instances: dict[<type>, <type>] = ...")` |
| 2 | `firestarter/config.py:85` | `Need type annotation for "_initialized_configs" (hint: "_initialized_configs: dict[<type>, <type>] = ...")` |
| 3 | `firestarter/config.py:102` | `Need type annotation for "_config" (hint: "_config: dict[<type>, <type>] = ...")` |
| 4 | `firestarter/database.py:174` | `Need type annotation for "proms" (hint: "proms: dict[<type>, <type>] = ...")` |
| 5 | `firestarter/database.py:175` | `Need type annotation for "pin_maps" (hint: "pin_maps: dict[<type>, <type>] = ...")` |
| 6 | `firestarter/database.py:325` | `Need type annotation for "pin_signals" (hint: "pin_signals: dict[<type>, <type>] = ...")` |

**Corrections the executor must not re-derive:**
- 132-PATTERNS.md listed `firestarter/ic_layout.py:233` as one of the targets. It is **not**. That line
  produces no error at all. `ic_layout.py` has exactly two errors and neither is of this class:
  `:447` an attribute error on a sequence, and `:630` an argument-type error. Both stay; neither is cheap
  and neither is needed to reach the watermark.
- 132-PATTERNS.md gave the database targets as `:173/:174`; the measured lines are **`:174/:175`**, which
  is what CONTEXT.md said. mypy's own output is the authority here.
- `firestarter/config.py` was not named as a target by either prior artifact, yet it carries **three of
  the six**. Do not skip it.
- All six modules sit in the follow-imports ring-fence block of `pyproject.toml`. That setting suppresses
  errors only for modules reached **by import**; the gate invokes mypy with both directories named on the
  command line, so these errors surface and count. Verified: they appear in the measured 69.
- `firestarter/database.py` also carries three errors this plan does **not** touch -- one assignment error
  at `:295` and two at `:383` and `:388` -- and `firestarter/config.py` has none besides the three above.
  Leaving those three is deliberate: they are inside the watermark and are not of this class.
- The ring-fenced operations module carries 10 errors of one root cause, dispositioned to a future item by
  operator decision. It is **not** opened here and its diff must stay empty.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Annotate the three collections in firestarter/config.py</name>
  <files>firestarter_app/firestarter/config.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/config.py lines 70-115 (the class-level singleton registries at :84 and :85, the instance-level configuration mapping at :102, and the surrounding singleton machinery that tells you what the key and value types actually are)
    - /workspaces/firestarter_app/firestarter/config.py the read/write accessors (whatever reads and writes the instance mapping, so the value type is derived from real usage rather than guessed)
    - /workspaces/firestarter_app/pyproject.toml lines 190-210 (the follow-imports block that includes this module, so the executor understands why these errors count despite the ring-fence)
  </read_first>
  <action>
Add the three missing annotations mypy asks for, deriving each type from how the collection is actually
used rather than from the hint's placeholder.

For the two class-level singleton registries: read how they are keyed and what they store. The registry
that maps a class to its single instance wants a mapping from a type to the instance type; the registry
tracking which configurations have been initialised wants a mapping from the same key type to a boolean or
whatever the code actually stores. Derive both from the code, not from the names.

For the instance-level configuration mapping: it stores heterogeneous configuration values read from and
written to a JSON file, so a mapping from string keys to an unconstrained value type is the honest
annotation. Do not invent a narrower value type that the JSON round-trip cannot guarantee -- a
too-narrow annotation here would create new assignment errors elsewhere in the module and turn a
six-error win into a net loss.

Critical constraint: the singleton behaviour is load-bearing outside this module. Plan 132-05 preserved an
explicit port-clearing step in a test factory *because* the config manager is a singleton and a port set
by an earlier test leaks otherwise. An annotation must not change any runtime behaviour, so add only the
annotation -- do not restructure the registries, do not change their initialisation site, and do not
convert them to a different container type.

After the edit, run the replica script and confirm all three of the named mypy lines are gone, by name,
from the output -- not merely that the total dropped by three. A total that dropped by three while one of
these persisted and something else disappeared would be a silent substitution.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_instances\s*:" /workspaces/firestarter_app/firestarter/config.py` returns a line, and the same for `_initialized_configs` and `_config` -- each of the three declarations now carries an annotation.
    - The replica script's mypy output contains **zero** lines matching `config\.py:(84|85|102).*Need type annotation`, confirmed by grepping the captured output and recording the grep count of 0.
    - The replica script's `mypy errors:` count is exactly 3 lower than plan 132-05's recorded count, or the divergence is recorded together with the mypy lines that account for it.
    - No new error appears in `firestarter/config.py`: the per-file error count for that module in the captured output is 0, recorded.
    - `python -m pytest tests/ -q` reports zero failures -- in particular the config and validate-family suites, since the singleton behaviour is load-bearing for the latter.
    - `python -c "from firestarter.config import ConfigManager as C; a=C(); b=C(); assert a is b, 'singleton behaviour changed'; print('SINGLETON OK')"` exits 0.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py pyproject.toml` produces no output.
  </acceptance_criteria>
  <done>All three declarations are annotated with types derived from real usage, the three named mypy lines are gone by name, no new error appeared in the module, and the singleton behaviour is unchanged and asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the three collections in firestarter/database.py</name>
  <files>firestarter_app/firestarter/database.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/database.py lines 165-185 (the constructor and the two mappings at :174 and :175, plus the loader call that populates them, which is where their real key and value types are visible)
    - /workspaces/firestarter_app/firestarter/database.py lines 315-345 (the local mapping at :325 and the nested assignment helper that fills it)
    - /workspaces/firestarter_app/firestarter/database.py lines 285-300 and 375-395 (the three assignment errors this plan deliberately does NOT fix, so the executor recognises them and leaves them alone)
  </read_first>
  <action>
Add the three missing annotations, again deriving each type from how the collection is populated and read.

The two constructor-level mappings hold the parsed chip database and the pin-map table. Read the loader
that populates them to determine the key type and the value type; the values are parsed JSON structures,
so an unconstrained value type is the honest annotation unless the code demonstrably narrows it.

The local mapping in the pin-signal routine is populated by a nested assignment helper. Derive its types
from that helper's parameters and from what the routine returns.

Do not attempt the three assignment errors in this same module -- one where an integer is assigned to a
list-typed target and two where a float is assigned to an integer-typed variable. They are a different
class, they are inside the watermark, and each is a real behavioural question rather than a missing
annotation. Fixing them here would expand this plan into semantic changes in the database loader.
Record in the summary that they were seen and deliberately left, with their line numbers, so a later
reader knows they are carried rather than missed.

Then run the replica script and take the phase's post-fix measurement. Confirm each of the three named
mypy lines is gone by name. Then evaluate the total against the watermark of 35, and do exactly one of two
things:
- If the count is at or below 35: record it, and state plainly that the remaining headroom is silent
  headroom that this phase does **not** ratchet away, per D-09.
- If the count is above 35: do **not** touch the watermark and do **not** open the ring-fenced operations
  module. Record the actual number, list the remaining errors by file and code from the captured output,
  and report the shortfall. Reaching the watermark by widening a suppression or by opening the ring-fence
  would reverse an operator decision, which no plan in this phase has the authority to do.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; bash tools/ci_replica_venv.sh 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "proms\s*:" /workspaces/firestarter_app/firestarter/database.py` returns a line, and the same for `pin_maps` and `pin_signals`.
    - The replica script's mypy output contains **zero** lines matching `database\.py:(174|175|325).*Need type annotation`, with the grep count of 0 recorded.
    - The replica script's mypy output contains **zero** lines matching `Need type annotation` anywhere -- all six of this class are gone.
    - The per-file error count for `firestarter/database.py` in the captured output is exactly 3, and those three are the assignment errors at the recorded line numbers -- no new error was introduced.
    - The replica script's `mypy errors: N (watermark: 35)` line is recorded with N stated, and the arithmetic from the pre-change 69 through each plan's subtraction to N is written out.
    - The script's output contains a `Found N errors in M files (checked K source files)` line and `checked` is at least 120.
    - `grep -c "mypy_error_watermark = 35" /workspaces/firestarter_app/pyproject.toml` returns 1.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py pyproject.toml` produces no output.
    - `python -m pytest tests/ -q` reports zero failures, and `ruff check firestarter/ tests/` plus `ruff format --check firestarter/ tests/` both exit 0.
    - The summary records the three deliberately-unfixed assignment errors with their line numbers.
  </acceptance_criteria>
  <done>All six unannotated-collection errors are gone by name, no new error appeared in either module, the count is measured against the watermark, and the three carried assignment errors are named rather than silently left.</done>
</task>

<task type="auto">
  <name>Task 3: Append the measured post-fix reading to 132-MYPY-LEDGER.md</name>
  <files>.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md</files>
  <read_first>
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md (sections 1 through 5 as plan 132-01 wrote them, including the `132-06 appends here` placeholder and the labelled projection in section 4)
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md the D-04 and D-09-adjacent reasoning (why the watermark's meaning must not be corrupted, and why there is no canary)
  </read_first>
  <action>
Replace the `132-06 appends here` placeholder with a `## 6. Measured post-fix reading (plan 132-06)`
section. Every number in it must be read from the captured replica-script output and attributed to the
command that produced it.

The section must contain:
- The gate's `mypy errors: N (watermark: 35)` line verbatim, and mypy's own
  `Found N errors in M files (checked K source files)` line verbatim.
- The interpreter, mypy version and numpy-absence stamps verbatim.
- The submodule HEAD sha at the time of the reading.
- A subtraction table walking from the pre-change 69 to N, one row per plan, each row naming the plan, what
  it removed, the expected count and the observed count. Where expected and observed differ, the row says
  so and the difference is explained from the mypy output rather than reconciled away.
- The remaining error population by file and by code, so a later phase's ratchet has the inventory it needs
  and so the 10 ring-fenced errors are visible as a named, dispositioned carry rather than an unexplained
  remainder.
- A comparison of the projection in section 4 against the measured result, saying explicitly which was
  right. Research projected 33; the plan-time projection was 32 on account of a seventh factory error; the
  measured number is whatever it is. Do not retro-edit section 4 -- the value of a recorded projection is
  that it can be wrong.

Then a `## 7. The number a later phase ratchets to, and the cost of not doing it here` section. State: the
measured true count N; that this phase deliberately does **not** set the watermark to N (D-09); the two
rejected alternatives and why -- setting the watermark before the single certifying dispatch risks a
local-versus-CI divergence reddening the gate and costing a second operator turn, and this project's own
record contains an instance of local and CI disagreeing on exactly this number; and green-at-35-then-ratchet
would cost two operator push-and-dispatch turns in one phase. Then state the accepted cost without
softening it: 35 minus N of silent headroom persists in what Phase 131 D-04 called the milestone's central
honesty artifact, and the actual defence against new errors of the discharged pattern is plan 132-05's typed
factory, not a tight watermark. Name N as the input a later phase's ratchet consumes, and note that
CONTEXT.md's Deferred block requires that ratchet be filed as its own backlog item with a named owner or it
becomes a seventh consecutive acknowledgement.

Finally a `## 8. Still not established` section: a locally-measured count is not a green `ci` job. The `ci`
job's state is unknown until plan 132-09's dispatch reads it. Nothing in this section is a claim that CI is
green.

Commit in the meta-repo.
  </action>
  <verify>
    <automated>grep -cE "Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)" /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - 132-MYPY-LEDGER.md contains section headings `## 6.`, `## 7.` and `## 8.`, and no longer contains the literal placeholder text for this plan's append point.
    - Section 6 contains a line matching `mypy errors: [0-9]+ \(watermark: 35\)` and a line matching `Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)`.
    - Section 6's subtraction table has one row per contributing plan (132-03, 132-05, 132-06) with both an expected and an observed column.
    - Section 6 lists the remaining error population by file, and the ring-fenced operations module's 10 errors appear there labelled as a dispositioned carry.
    - Section 6 states explicitly whether the section-4 projection was right, and section 4 itself is unchanged: `git diff` on the ledger shows no deletion inside section 4.
    - Section 7 names the measured count as the input to a later ratchet, states the headroom arithmetic against 35, names both rejected alternatives, and says the typed factory is the real defence.
    - Section 8 states that a local count is not a green ci job.
    - `git -C /workspaces log -1 --name-only` lists only paths under `.planning/`.
    - The `132-09 appends here` placeholder is still present and untouched.
  </acceptance_criteria>
  <done>The ledger carries the measured post-fix reading with a per-plan expected-versus-observed subtraction table, the remaining inventory including the dispositioned ring-fence carry, an honest projection post-mortem, the named ratchet input with its stated headroom cost, and an explicit statement that nothing here says CI is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON chip database → the parsed in-memory mappings | The two constructor mappings hold externally-supplied parsed data; an over-narrow annotation would assert a shape the input cannot guarantee. |
| the watermark gate → the phase's honesty | The count this plan produces is the number a later phase ratchets to; corrupting its meaning corrupts every later "green suite" claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-19 | Tampering | reaching the watermark by the wrong means | critical | mitigate | Explicit prohibition plus criteria: the watermark comment must still read 35, and the ring-fenced module's diff must be empty. If the count lands above 35 the plan reports a shortfall rather than widening a suppression -- both would reverse an operator decision this plan has no authority to reverse. |
| T-132-20 | Spoofing | a total that fell by the right amount for the wrong reason | high | mitigate | Each of the six errors must be proven gone **by name** from the captured output, and each module's per-file error count is asserted, so a silent substitution (one target fixed, an unrelated error vanished) cannot pass as success. |
| T-132-21 | Tampering | an over-narrow annotation on JSON-backed data | medium | mitigate | Annotations are derived from the populating code rather than from mypy's placeholder hint, and the acceptance criteria require zero new errors in each edited module -- a too-narrow value type would surface immediately as new assignment errors. |
| T-132-22 | Denial of service | the config singleton's behaviour | high | mitigate | Annotation-only edits, no restructuring, plus a runtime identity assertion that two constructions return the same instance, plus a full-suite run -- because plan 132-05 preserved a test-fixture behaviour that depends on exactly this singleton semantics. |
| T-132-23 | Repudiation | the silent headroom | medium | accept | The headroom is measured, named and recorded as an accepted cost with its two rejected alternatives, and the measured count is handed forward as a named ratchet input. Per D-09 this phase does not ratchet; per CONTEXT.md's Deferred block the ratchet needs a named owner or it recurs. |
| T-132-SC | Tampering | package installs | high | accept | No package is installed or added; `pyproject.toml` is untouched and a criterion asserts it. The Package Legitimacy Gate's trigger condition is not met. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` output contains zero
   `Need type annotation` lines.
2. The `mypy errors:` count is recorded, and the full 69-to-N arithmetic appears in the ledger with
   expected-versus-observed columns.
3. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
4. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
5. The config singleton identity assertion exits 0.
6. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py pyproject.toml` is
   empty, and the watermark still reads 35.
</verification>

<success_criteria>
- All six unannotated-collection errors are annotated and proven gone by name.
- No new error was introduced in either edited module; the three carried assignment errors are named.
- The count is measured in the replica venv and recorded with the per-plan subtraction table.
- The watermark still reads 35 and the ring-fenced module is untouched.
- The measured count is recorded as a named input to a later ratchet, with its headroom cost stated.
- No RETIRE requirement is marked Complete.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-06-SUMMARY.md` when done.
State in it: the six annotations as written, the grep count of 0 for `Need type annotation`, the measured
count against the watermark, the full 69-to-N arithmetic, the three deliberately-carried assignment errors
with line numbers, and an explicit line reading "Requirements marked Complete: none (RETIRE-06 is owned by
plan 132-09)".
</output>
