---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 04
type: execute
wave: 4
depends_on: ["132-03"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/tests/__snapshots__/test_characterization.ambr
autonomous: true
requirements: [RETIRE-01]
must_haves:
  truths:
    - "The subcommand no longer exists as an invokable command, and the four gates only it exercised are gone with it (RETIRE-01, ROADMAP criterion 1)."
    - "The dev group's subcommand roster is exactly eight, proven by enumerating the registered command names, not by a grep for an absent string."
    - "The help snapshot changed by exactly one deleted line, updated scoped to the test_help_dev node id only, never by a broad snapshot regeneration (D-13)."
    - "No honesty caveat is added to the write auto-unlock path, and the resulting residual -- that between this phase and Phase 134 the caveat has no user-reachable carrier -- is stated plainly rather than engineered around (D-05)."
  artifacts:
    - firestarter_app/firestarter/cli_handlers.py
  key_links:
    - "The MSG_ERR_UNKNOWN_CMD import becomes the only remaining reference to that name in cli_handlers.py once the span is gone, so it must be removed in the same commit or ruff's F rules fail the ci job."
    - "Confirm, FirmwareOutdatedError, EpromOperationError, sdp_capability and _is_interactive all have other users in the file and must NOT be removed."
---

<!-- planner-discipline-allow: dev sdp -->
<!-- planner-discipline-allow: MSG_ERR_UNKNOWN_CMD -->

<objective>
Delete the subcommand, clean up the one import that its removal orphans, and update the one help
snapshot line.

Purpose: this is RETIRE-01 and ROADMAP criterion 1. It is deliberately the *fourth* plan, not the
first: the wording had exactly one production carrier and four assertions keyed on it, so plan 132-02
relocated the wording and plan 132-03 retargeted the assertions before anything was removed. By the
time this plan runs, deleting the span costs nothing that has not already been preserved -- which is
the whole point of the ordering.

Output: a ~126-line deletion in `cli_handlers.py`, one import removal, one snapshot line removed.

May mark Complete: **RETIRE-01 only.** May NOT mark Complete: RETIRE-02 and RETIRE-03 (already
completed by 132-03 -- do not re-tick, do not un-tick), RETIRE-04 (owned by 132-08), RETIRE-05
(132-05), RETIRE-06 (132-09), RETIRE-07 (132-07), RETIRE-08 (132-08). In particular: do not tick
RETIRE-07 because this plan touches the auto-unlock neighbourhood in the file -- it does not add the
tripwire, and 132-07 owns it.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-03-SUMMARY.md
</context>

<repo_map>
Both files in this plan are in the **`firestarter_app` submodule**
(`/workspaces/firestarter_app/`, branch `gsd/v1.30-sdp-surface-retirement`) and both commits land
there. This plan writes nothing in the meta-repo except its own SUMMARY. The firmware repo is not
touched.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files** -- `firestarter_app/firestarter/sdp_honesty.py` (132-02),
`firestarter_app/tools/ci_replica_venv.sh` (132-01), `firestarter_app/tests/test_sdp_honesty.py` (132-03)

**New functions in `firestarter/sdp_honesty.py`** (132-02) -- `unreadable_state_caveat`,
`emission_summary`, `map_unknown_cmd_to_outdated`

**New symbols in `tests/conftest.py`** (132-05) -- `make_app_context(...)`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` (132-03),
`test_command_names_dereferences_both_sdp_commands` (132-08),
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` (132-07)

**New config entries** -- `"firestarter.sdp_honesty"` in the Phase-42 strict island (132-02)

**New constants:** none in `constants.py`. **This plan creates no new symbol at all** -- it only removes.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01),
`132-PRUNE-LEDGER.md` (132-03), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Re-measured at plan time against `firestarter_app` @ `8caf77f`. Note plan 132-02 edits two spans
inside this range, so re-read the file before cutting rather than trusting these offsets.

- Deletion boundary: the `@dev.command` registration decorator at `:2196`, the function definition at
  `:2213`, body running to `:2321` which is **EOF**. It is the last function in the file, so the
  deletion is a tail truncation plus the decorator block above it.
- The `-y/--yes` option decorator occupies `:2199-2210`.
- The four gates deleted with it: absent-chip at `:2235`, capability at `:2244`, support-status at
  `:2250`, consent at `:2261-2281` including the confirm call at **`:2273`** (132-CONTEXT.md cited
  `:2267`, which is inside the prompt *string*; the call is at `:2273`) and the off-TTY refusal at
  `:2277-2281`.
- Full `dev` subcommand roster before the deletion, nine entries at: `:1180 read`, `:1211 reg`,
  `:1273 addr`, `:1310 consistency-check`, `:1400 write-cycle`, `:1453 fault-inject`,
  `:1680 validate-family`, `:2055 test`, `:2196 sdp`. **Post-deletion must be eight.**
- Import survival, measured by counting occurrences inside versus outside the span: `Confirm`
  (import `:32`, live use `:1990`), `FirmwareOutdatedError` (import `:65`, live use `:189`),
  `EpromOperationError` (import `:63`, live use `:210`), `sdp_capability` (live use `:625`),
  `_is_interactive` (live uses outside the span), `resolve_chip` (17 references outside),
  `ChipNotFoundError` (live uses outside) -- **all survive**. The `firestarter.messages` import at
  `:74` for the unknown-command message id has **no** other user in this file and must be removed.
  If plan 132-02's rewire already removed it, that plan's summary says so.
- Snapshot: the `test_help_dev` entry occupies `tests/__snapshots__/test_characterization.ambr:124-146`
  and the line to remove is `:141`. `test_help_dev` is defined at `tests/test_characterization.py:328`
  and drives a **real subprocess** via `run_firestarter("dev", "--help")` -- not `CliRunner`, whose
  `isolation()` forces an 80-column width that wraps differently from a real subprocess's unforced 78.
- The `Commands:` block's name-column padding is driven by the longest surviving name,
  `consistency-check` at 17 characters, which is unaffected by removing a 3-character name. So the
  expected diff really is exactly one deleted line with no realignment of any other line.
- `addopts` is `"-ra -q"` with no unused-snapshot warning flag, and the `test_help_dev` entry stays
  *used* after the edit, so syrupy's unused-snapshot session failure does not fire in this phase. That
  trap arrives when the entry is renamed or parametrised, which is Phase 136's channel split -- do not
  engineer around it here (D-13).
- The only in-tree references to the subcommand token were in the test module plan 132-03 rewrote and
  the registration decorator this plan deletes. Zero markdown files under `firestarter_app` mention the
  command -- re-verified at plan time, and task 3 re-runs it as an acceptance leg rather than
  inheriting it.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Delete the subcommand span and the one orphaned import (RETIRE-01)</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 2190-2321 (the full span as it stands AFTER plan 132-02's rewire -- re-read rather than trusting the pre-rewire offsets)
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 55-80 (the import block, to identify precisely which names lose their last reference)
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 2050-2195 (the preceding subcommand, so the deletion's upper boundary is placed at the end of that function and not one line early or late)
  </read_first>
  <action>
Remove the subcommand entirely: its `@dev.command` registration decorator, its `click.argument` and
`click.option` decorators including the assume-yes flag, its `pass_obj` and error-mapping decorators,
its definition line, its docstring, and its whole body through end of file. The span is the last
function in the module, so the file ends where the preceding subcommand's body ends.

All four gates go with it, and that is the requirement rather than a side effect: the absent-chip
hard-fail, the capability refusal, the support-status resolution, and the consent gate with both its
on-TTY confirm branch and its off-TTY refusal branch. Do not preserve any of them elsewhere -- plan
132-03's ledger already accounted for the coverage those gates carried.

Then fix the import block. Remove the `firestarter.messages` import of the unknown-command message id,
which after this deletion has no other reference in this file (the mapping now lives in
`firestarter/sdp_honesty.py`, which imports it itself). Do **not** remove the rich confirm import, the
outdated-firmware exception, the operation-error exception, the capability predicate, the chip
resolver, the chip-not-found exception, or the interactivity helper -- every one of them has a live
user elsewhere in the file, measured. If plan 132-02's rewire already removed the messages import,
record that in the summary and skip this step rather than reporting a discrepancy.

Also remove the `firestarter.sdp_honesty` import if and only if the deletion leaves it with no other
user in this file -- which it will, since the only two call sites were inside the deleted span. This is
expected and correct: the helper becomes a library module with no production caller until Phase 134.
That is exactly what D-01 chose over the rejected alternative of pre-staging it on Phase 135's path,
and it is not a defect: the module has tests, so it is exercised, and ruff's unused-import rules apply
to imports, not to modules.

Do not touch the auto-unlock decision block in this file. Do not add any output to the write path --
D-05 forbids adding an honesty caveat there, on the reasoning that the caveat exists to qualify a
*claim* and the auto-unlock makes no claim about lock state, so there is no dishonest claim to caveat.
Record the residual in the summary in one plain sentence: between this phase and Phase 134 the caveat
has no user-reachable carrier. State it; do not close it early.

Run the whole suite and both ruff gates before committing. Commit in the submodule with a message
naming RETIRE-01.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -c "from firestarter.cli_handlers import dev; names=sorted(dev.commands); assert names==['addr','consistency-check','fault-inject','read','reg','test','validate-family','write-cycle'], names; print('ROSTER OK', len(names))"; ruff check firestarter/ tests/; ruff format --check firestarter/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from firestarter.cli_handlers import dev; names=sorted(dev.commands); assert names==['addr','consistency-check','fault-inject','read','reg','test','validate-family','write-cycle'], names"` exits 0 -- the roster is exactly eight, enumerated positively rather than proven by an absence grep.
    - `python -c "import subprocess,sys; r=subprocess.run(['firestarter','dev','sdp','--help'],capture_output=True,text=True); assert r.returncode!=0, r.stdout; assert 'No such command' in (r.stderr+r.stdout), (r.stderr,r.stdout); print('REFUSED OK')"` exits 0 -- invoking it produces a Click no-such-command failure, which is behavioural proof of removal.
    - `ruff check firestarter/ tests/` exits 0 -- this is the proof that no import was orphaned and none that is still needed was removed.
    - `ruff format --check firestarter/ tests/` exits 0.
    - `python -m pytest tests/ -q` reports zero failures except, possibly, `tests/test_characterization.py::test_help_dev`, whose snapshot task 2 updates. Any other failure is a defect in this task.
    - `python -c "from firestarter.cli_handlers import write; print('write import OK')"` exits 0 and the write path emits no new line: `git diff HEAD~1 -- firestarter/cli_handlers.py | grep -c '^+.*click.echo'` returns 0, enforcing D-05.
    - `git -C /workspaces/firestarter_app diff --stat HEAD~1 -- firestarter/eprom_operations.py firestarter/constants.py pyproject.toml` produces no output.
    - `git -C /workspaces/firestarter_app log -1 --name-only` lists `firestarter/cli_handlers.py` and nothing else.
    - The summary states the D-05 residual in one plain sentence.
  </acceptance_criteria>
  <done>The subcommand and its four gates are gone, the roster enumerates to exactly eight, invoking it fails with a no-such-command error, both ruff gates pass proving the import block is exactly right, and the D-05 residual is stated.</done>
</task>

<task type="auto">
  <name>Task 2: Update the help snapshot, scoped to the test_help_dev node id only (D-13)</name>
  <files>firestarter_app/tests/__snapshots__/test_characterization.ambr</files>
  <read_first>
    - /workspaces/firestarter_app/tests/__snapshots__/test_characterization.ambr lines 118-150 (the `test_help_dev` entry, its neighbours, and the exact indentation and padding of the commands block)
    - /workspaces/firestarter_app/tests/test_characterization.py lines 320-340 (the `test_help_dev` definition and its real-subprocess driver)
    - /workspaces/firestarter_app/tests/test_characterization.py lines 90-140 (the scrubber applied to subprocess output before any snapshot comparison, so an unexpected diff can be attributed correctly)
  </read_first>
  <action>
Bring the committed snapshot into line with the eight-command roster, changing exactly one line.

Run the update scoped to the single node id -- `pytest "tests/test_characterization.py::test_help_dev"
--snapshot-update` -- and nothing wider. A broad update over the module or the suite regenerates every
snapshot in the selection and would silently bless drift in the top-level help, the known-chip info
output, or the list output. That is the failure D-13 forbids.

Then inspect the result against a named expected shape before accepting it: the only change is the
removal of the one command line from the `test_help_dev` entry, and `git diff --stat` on the snapshot
file shows one file changed, zero insertions, one deletion. Record the `git diff --stat` output
verbatim in the summary.

If the diff is anything other than that -- extra deletions, any insertion, or a change to a
neighbouring entry -- do not accept it. Restore the file with `git checkout --
tests/__snapshots__/test_characterization.ambr`, then make the single-line removal by hand and prove it
by running `pytest "tests/test_characterization.py::test_help_dev"` **without** the update flag and
requiring it to pass. A passing snapshot assertion is a stronger proof than a regeneration, because
the assertion is the thing that will guard the file afterwards. Either route must end at the same
provable state: one deleted line, and the node-scoped test green without an update flag.

Do not add, rename, or parametrise any snapshot entry. The entry stays used, so syrupy's
unused-snapshot session failure does not fire; that hazard belongs to Phase 136's channel split and
pre-solving it here would be engineering around the wrong phase's problem (D-13).

Note for the record why exactly one line is expected: the commands column's padding is set by the
longest surviving command name, which is unchanged, so no other line realigns.

After the snapshot is correct, run the full characterization module and then the whole suite. Commit in
the submodule.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest "tests/test_characterization.py::test_help_dev" -q; python -m pytest tests/test_characterization.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app diff --stat HEAD~1 -- tests/__snapshots__/test_characterization.ambr` shows exactly `1 file changed, 1 deletion(-)` with no insertions, and that output is quoted verbatim in the summary.
    - `git -C /workspaces/firestarter_app diff HEAD~1 -- tests/__snapshots__/test_characterization.ambr | grep -c '^+[^+]'` returns 0 -- no added line anywhere in the file.
    - `python -m pytest "tests/test_characterization.py::test_help_dev" -q` passes with no update flag.
    - `python -m pytest tests/test_characterization.py -q` reports zero failures -- proving no neighbouring snapshot was disturbed.
    - `python -c "import re; s=open('/workspaces/firestarter_app/tests/__snapshots__/test_characterization.ambr').read(); b=re.search(r'# name: test_help_dev\n(.*?)\n# ---', s, re.S).group(1); cmds=[l.strip().split()[0] for l in b.splitlines() if l.startswith('    ') and l.strip() and not l.strip().startswith(chr(39))]; print(cmds)"` shows a command list of length 8 whose entries match the roster asserted in task 1.
    - `python -m pytest tests/ -q` reports zero failures across the whole suite.
    - `git -C /workspaces/firestarter_app log -1 --name-only` lists only `tests/__snapshots__/test_characterization.ambr`.
    - The summary states which route was used (node-scoped update, or restore-and-hand-edit) and why.
  </acceptance_criteria>
  <done>The snapshot lost exactly one line and gained none, the node-scoped test passes without an update flag, every neighbouring snapshot is untouched, and the route taken is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Post-deletion sweep -- prove nothing in the tree still references the retired surface</name>
  <files>none -- read-only sweep and measurement; findings go in this plan's SUMMARY</files>
  <read_first>
    - /workspaces/firestarter_app/README.md and /workspaces/firestarter_app/autocomplete.md (the two user-facing markdown files most likely to document a subcommand)
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md the Deferred block (so the sweep does not stray into Phase 137's outward-facing publication work)
  </read_first>
  <action>
Run the trace sweep as a real acceptance leg rather than inheriting the research claim that it is
already clean.

Re-run the markdown sweep across `firestarter_app` for prose references to the retired subcommand.
Research recorded zero hits; that was re-confirmed at plan time; confirm it again here after the
deletion, and record the command and its count. If a hit appears, do not fix outward-facing
publication text -- the release-notes mapping, the issue reply, and the deferral-label cleanup are
Phase 137's CLOSE-05/CLOSE-06 behind a blocking operator wording-review gate. Record the hit and its
location in the summary, and leave it. In-tree code or test references are different and must be
fixed here.

Then confirm the sweep is not vacuous. A grep over a file set that happens to be empty returns zero
hits and proves nothing, which is the fail-open shape this milestone exists to close. Prove the file
set is non-empty by counting the markdown files the sweep covered, and prove the pattern works by
searching the same file set for a token that certainly is present.

Finally re-run the CI-replica script from plan 132-01 and record the mypy count. The expected movement
is the measured six-error reduction from plan 132-03's removal of that module's local factory, taking
the count from the pre-change reading to six below it. Record the observed number against the expected
one. Do not fix any newly-visible error here -- plans 132-05 and 132-06 own the remaining fixes, and a
divergence at this point is information those plans need, not a defect to patch quietly.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; bash tools/ci_replica_venv.sh 2>&amp;1 | grep -E "mypy errors:|Found [0-9]+ errors|CI-REPLICA:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "dev sdp" /workspaces/firestarter_app --include='*.md' | wc -l` returns 0, and the command and its output are recorded in the summary.
    - The sweep is proven non-vacuous: `find /workspaces/firestarter_app -name '*.md' -not -path '*/.git/*' | wc -l` returns at least 5, and `grep -rl "firestarter" /workspaces/firestarter_app --include='*.md' | wc -l` returns at least 1 -- so the file set is real and the pattern mechanism works over it.
    - `grep -rn "dev sdp" /workspaces/firestarter_app --include='*.py' | wc -l` returns 0.
    - The replica script's `mypy errors: N (watermark: 35)` line is recorded, together with the pre-change N from `132-MYPY-LEDGER.md` section 1 and the arithmetic difference, and a statement of whether the difference matches the expected six.
    - The script's output still contains a line matching `Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)` and the `checked` value is at least 120 -- the floor still holds after a rename and two added files.
    - `python -m pytest tests/ -q` reports zero failures.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty.
  </acceptance_criteria>
  <done>The markdown and code sweeps return zero hits and are proven non-vacuous, the mypy count has moved by the expected amount and is recorded against the pre-change reading, and the checked-files floor still holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI input → the dev command group | Removing a registered subcommand changes what the group accepts; a partial removal could leave a callable path with its gates deleted. |
| committed snapshot → the help-output contract | The `.ambr` file is the pinned record of user-visible help text; a broad regeneration silently rewrites contracts unrelated to this change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-11 | Elevation of privilege | a partially-deleted subcommand | critical | mitigate | The deletion removes the registration decorator and the body together, and the proof is a positive enumeration of the eight surviving command names plus a behavioural invocation that must fail with a no-such-command error. A body-only or decorator-only deletion cannot satisfy both. Crucially, the four gates cannot be left behind attached to a still-callable path, because there is no callable path left. |
| T-132-12 | Tampering | broad snapshot regeneration | high | mitigate | The update is scoped to one node id; acceptance requires zero insertions anywhere in the snapshot file and a green run of the whole characterization module, so drift in a neighbouring snapshot cannot ride along. A restore-and-hand-edit fallback with a no-update-flag passing assertion is specified for any other diff shape. |
| T-132-13 | Repudiation | the D-05 residual | medium | accept | No caveat is added to the write auto-unlock path, and the residual -- no user-reachable carrier for the caveat between this phase and Phase 134 -- is stated in the summary and again in the phase record. Accepted deliberately: the auto-unlock makes no claim about lock state, so there is no dishonest claim to qualify, and adding output would change the production write path's behaviour inside a retirement phase. Enforced by a criterion that no echo line was added. |
| T-132-14 | Denial of service | orphaned or over-eager import removal | medium | mitigate | Import survival was measured name by name, inside versus outside the span, and the proof is `ruff check` over the CI path set, which fails on both an unused import and an undefined name. |
| T-132-SC | Tampering | package installs | high | accept | No package is installed or added; `pyproject.toml` is untouched by this plan. The Package Legitimacy Gate's trigger condition is not met. |
</threat_model>

<verification>
1. The eight-name roster assertion exits 0, and the behavioural invocation fails with a no-such-command
   error.
2. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
3. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
4. `git -C /workspaces/firestarter_app diff --stat HEAD~2 -- tests/__snapshots__/test_characterization.ambr`
   shows one deletion and no insertions.
5. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` is empty.
6. The markdown and code sweeps return zero and are proven non-vacuous.
7. The replica script's mypy count is recorded against plan 132-01's reading with the arithmetic shown.
</verification>

<success_criteria>
- The subcommand and its four gates no longer exist; the roster is exactly eight; invoking it fails.
- Exactly one import was removed and every still-needed name survived, proven by ruff over CI's path set.
- The snapshot lost exactly one line, updated scoped to one node id, with no insertion anywhere.
- No output was added to the write path, and the D-05 residual is stated plainly.
- RETIRE-01 is marked Complete. No other RETIRE id is ticked or un-ticked.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-04-SUMMARY.md` when done.
State in it: the eight-name roster as asserted, the verbatim `git diff --stat` for the snapshot, which
snapshot route was used and why, the D-05 residual sentence, the sweep commands with their counts and
their non-vacuity evidence, the mypy count before and after with the arithmetic, and an explicit line
reading "Requirements marked Complete: RETIRE-01".
</output>
