---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 02
type: execute
wave: 2
depends_on: ["132-01"]
files_modified:
  - firestarter_app/firestarter/sdp_honesty.py
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/pyproject.toml
  - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md
autonomous: true
requirements: [RETIRE-03]
must_haves:
  truths:
    - "The honesty caveat wording and the unknown-command mapping live in a shared production module authored in this phase, so the four honesty assertions have a real SUT with no coverage window (D-01)."
    - "That module is firestarter/sdp_honesty.py, registered in pyproject.toml's strict-island override list so it is type-checked from birth (D-02)."
    - "Before anything is deleted, the command emits its summary line and its outdated-firmware error THROUGH the helper and the full unmodified 26-test suite still passes -- that green run is the behavioural-equivalence proof, captured here because plan 132-04 makes it unreachable forever."
  artifacts:
    - firestarter_app/firestarter/sdp_honesty.py
  key_links:
    - "sdp_honesty.py must not import click: Phase 134's leg-report rows and Phase 135's relock path import this module, and a click dependency would make it unusable from the report layer. The caller does the echo."
    - "The three greppable tokens the surviving tests key on must all remain inside the string the helper returns, or the four assertions go RED."
---

<!-- planner-discipline-allow: dev sdp -->

<objective>
Author `firestarter/sdp_honesty.py`, rewire the still-live subcommand to emit through it, and
register it in the mypy strict island.

Purpose: correction 1 in 132-CONTEXT.md's decisions block is the load-bearing fact of this phase --
all three honesty strings and the unknown-command mapping live in exactly one place, inside the span
about to be deleted, and `eprom_operations.py`'s `sdp_lock`/`sdp_unlock` structurally *cannot* carry
them (`get_response()` filters the entire INFO band at `serial_comm.py:424`, so the operation layer
never sees the firmware's `0x5F`/`0x61` frame). RETIRE-03's "retargeted onto the new leg" is not
executable in phase 132 because the leg is Phase 134. D-01/D-02 resolve it: relocate the survivors
into a shared production helper authored here.

This plan does the relocation **while the subcommand still exists**, so the existing 26-test suite
becomes a one-time equivalence proof: the same tests, unchanged, passing against a command that now
composes its output from the helper. After plan 132-04 that proof is unreachable forever, which is
exactly why it is taken now and recorded.

Output: one new production module, a narrow rewire of the command body, one `pyproject.toml`
override-list insertion, and an appended equivalence-proof note in the ledger.

May mark Complete: **NOTHING.** This plan touches RETIRE-03 but must NOT tick it -- plan 132-03 owns
RETIRE-03's Complete transition, because RETIRE-03's wording requires the file move and the grep
proof of no net loss, neither of which happens here. Explicitly forbidden: ticking RETIRE-03 because
"the survivors now have a home".
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-01-SUMMARY.md
</context>

<repo_map>
All three code paths in this plan are in the **`firestarter_app` submodule**
(`/workspaces/firestarter_app/`, branch `gsd/v1.30-sdp-surface-retirement`) and commit there.
The single `.planning/` edit (the ledger append in task 3) commits in the **meta-repo**
(`/workspaces/`, branch `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`).
The firmware repo `/workspaces/firestarter/` is not touched by this phase at all.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files**
- `firestarter_app/firestarter/sdp_honesty.py` -- **this plan**
- `firestarter_app/tools/ci_replica_venv.sh` -- plan 132-01
- `firestarter_app/tests/test_sdp_honesty.py` -- plan 132-03 (file move of the dev-sdp test module)

**New functions in `firestarter/sdp_honesty.py`** -- **this plan**
- `unreadable_state_caveat() -> str`
- `emission_summary(mode: str, chip_name: str) -> str`
- `map_unknown_cmd_to_outdated(exc, mode, chip_name) -> FirmwareOutdatedError | None`

**New symbols in `tests/conftest.py`** -- plan 132-05: `make_app_context(...)`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` (132-03),
`test_command_names_dereferences_both_sdp_commands` (132-08),
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` (132-07)

**New config entries** -- `"firestarter.sdp_honesty"` in `pyproject.toml`'s Phase-42 strict-island
`module` list (**this plan**)

**New constants:** none in `constants.py`.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01),
`132-PRUNE-LEDGER.md` (132-03), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Re-measured at plan time against `firestarter_app` @ `8caf77f`. Where these differ from
132-CONTEXT.md, the measured value wins (D-11's own instruction).

- The honesty `click.echo` is at `cli_handlers.py:2312-2316` (CONTEXT.md said `:2316-2318`), inside
  the `if ok:` arm, preceded by the D-10 rationale comment at `:2304-2314`.
- The string composed there, over three source lines: `SDP {mode} sequence for {chip_upper} was
  emitted. The ` / `resulting protection state cannot be read back on this chip ` / `family, so this
  is not a claim about the chip's actual state.`
- The D-14 arm is at `:2283-2302`: rationale comment `:2283-2290`, `try` `:2291`, `except
  EpromOperationError as e` `:2295`, the `error_code` test `:2296`, the raise `:2297-2301`, bare
  `raise` `:2302`. Its message, over three source lines: `{chip_upper}: attached firmware does not
  implement SDP ` / `{mode} (unknown command) -- upgrade with ` / `'firestarter fw --install'.`
- `firestarter/exceptions.py` has **zero** top-level imports -- a true leaf. `FirmwareOutdatedError`
  is at `:31` (subclass of `SerialError`), `EpromOperationError` at `:37` (carries `error_code`).
- `firestarter/messages.py` imports only `dataclasses` -- also a leaf. `MSG_ERR_UNKNOWN_CMD` lives
  there and is imported into `cli_handlers.py` at `:74`.
- `pyproject.toml`'s Phase-42 production strict island is the `[[tool.mypy.overrides]]` block whose
  comment reads "strict-island for the 8 modules touched in v1.8", listing in this
  non-alphabetical order: `firestarter.main`, `firestarter.cli_handlers`,
  `firestarter.chip_resolver`, `firestarter.frame_parser`, `firestarter.codec`,
  `firestarter.address_parser`, `firestarter.exceptions`, `firestarter.serial_comm`, with
  `disallow_untyped_defs = true` and `check_untyped_defs = true`. **`firestarter.sdp_capability` is
  NOT in it** -- confirmed. So joining the island is a deliberate strengthening, not a copy of the
  analog's treatment.
- The watermark comment is at `pyproject.toml:159` and is **not touched** (D-09).
- `tests/test_dev_sdp_cmd.py` currently collects and passes **26** tests (14 top-level test
  functions, one parametrised over nine adapter-required parts).
- Post-deletion symbol survival in `cli_handlers.py`, measured: `Confirm` (import `:32`, other use
  `:1990`), `FirmwareOutdatedError` (import `:65`, other use `:189`), `EpromOperationError` (import
  `:63`, other use `:210`), `sdp_capability` (other use `:625`) and `_is_interactive` all survive the
  later deletion. `MSG_ERR_UNKNOWN_CMD` (`:74`) does **not** -- but its removal belongs to plan
  132-04, not here, because this plan's rewire still uses it indirectly through the helper.
</measured_anchors>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create firestarter/sdp_honesty.py -- the shared honesty carrier (D-01, D-02)</name>
  <files>firestarter_app/firestarter/sdp_honesty.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 2280-2321 (the exact wording being relocated: the D-14 rationale comment and arm, the D-10 rationale comment, and the summary echo)
    - /workspaces/firestarter_app/firestarter/sdp_capability.py lines 1-80 (the shape to imitate: lettered provenance docstring, then an explicit import-set-invariant comment, then module constants; and its thin-wrapper-over-pure-core convention)
    - /workspaces/firestarter_app/firestarter/exceptions.py (confirm the zero-import leaf property, FirmwareOutdatedError's base and constructor, and EpromOperationError.error_code)
    - /workspaces/firestarter_app/tests/test_dev_sdp_cmd.py lines 390-560 (the four surviving tests, so the helper's return values satisfy their assertions verbatim rather than approximately)
  </read_first>
  <behavior>
    - `unreadable_state_caveat()` returns a single-line string containing both `cannot be read back` and `not a claim about the chip's actual state`.
    - `emission_summary("enable", "AT28C256")` returns a single-line string containing `was emitted`, `cannot be read back` and `not a claim about the chip's actual state`, and containing no newline character.
    - `emission_summary("disable", "AT28C256")` also contains `cannot be read back` -- the caveat is unconditional on direction, never attached to one mode.
    - For both modes, `re.search(r"\d+\s*(us|ms|s)\b", result, re.IGNORECASE)` is None -- no digit-plus-time-unit token.
    - `emission_summary("enable", "at28c256")` uppercases the chip name itself, so a Phase 134/135 caller cannot forget to.
    - `map_unknown_cmd_to_outdated` given an EpromOperationError whose error_code is MSG_ERR_UNKNOWN_CMD returns a FirmwareOutdatedError whose message contains `firestarter fw --install` and `does not implement`.
    - `map_unknown_cmd_to_outdated` given an EpromOperationError with any other error_code returns None, so the caller re-raises the original rather than mis-reporting a firmware-age problem.
    - `map_unknown_cmd_to_outdated` given an EpromOperationError whose error_code is None returns None.
  </behavior>
  <action>
Create `firestarter/sdp_honesty.py`. Name the module and its functions for **what they carry**, not
for the subcommand being retired: 132-CONTEXT.md's Integration-points block records that Phase 134's
leg-report rows and Phase 135's relock path import this module, so its API shape is a forward
contract, not an internal detail.

Module docstring: a provenance paragraph naming phase 132, RETIRE-03, D-01 and D-02, and stating in
one sentence why the module exists -- the wording it carries had exactly one carrier, inside the
retired subcommand, and the operation layer cannot carry it because `get_response()` filters the
whole INFO band at `serial_comm.py:424`, making the firmware's duration frame structurally invisible
below the CLI. Also state the forward contract naming Phase 134's leg rows and Phase 135's relock
path as the intended future callers.

Immediately after `from __future__ import annotations`, add an explicit import-set-invariant comment
in the style of `sdp_capability.py`'s purity comment, declaring this module's own invariant: the
top-level import set is a subset of `{__future__, firestarter.exceptions, firestarter.messages}`,
both of which are leaf modules (`exceptions.py` has zero imports, `messages.py` imports only
`dataclasses`), and specifically no `click`, because the caller performs the echo and a `click`
dependency would make this module unusable from Phase 134's report layer. Plan 132-03 adds an
AST-based test enforcing this invariant, so the comment states a checked property rather than a hope.

Define three public functions, each fully annotated -- the module joins a strict island in task 3
where `disallow_untyped_defs = true` makes a missing return annotation an error.

`unreadable_state_caveat() -> str` returns the caveat clause alone, byte-identical in wording to the
second and third fragments currently composed at `cli_handlers.py:2313-2315`: the sentence beginning
`The resulting protection state cannot be read back on this chip family,` and ending `so this is not
a claim about the chip's actual state.` It is exposed separately because Phase 134's report rows need
the clause without the emission preamble.

`emission_summary(mode: str, chip_name: str) -> str` returns the full single-line summary: the text
`SDP `, then mode, then ` sequence for `, then the uppercased chip name, then ` was emitted. `, then
the caveat clause. Compose it by calling `unreadable_state_caveat()` rather than duplicating the
wording, so the two can never drift. Uppercase `chip_name` inside the function. Do not append a
newline -- the caller echoes. Carry the D-10 rationale across in substance: the claim is that the
sequence was EMITTED, never that the resulting state was verified; no duration figure appears, and
that is mechanically enforced rather than a discipline, because the INFO-band filter makes the
duration frame invisible at and below the operation layer; and no lock or unlock state boolean
appears either, which is HOST-05's honesty floor.

`map_unknown_cmd_to_outdated(exc: EpromOperationError, mode: str, chip_name: str) ->
FirmwareOutdatedError | None` returns a constructed (not raised) `FirmwareOutdatedError` when
`exc.error_code` equals `MSG_ERR_UNKNOWN_CMD`, and `None` otherwise. Returning rather than raising
keeps the caller in control of exception chaining. Its message must contain `does not implement SDP `
followed by the mode and ` (unknown command)`, and the remediation text `'firestarter fw --install'`,
matching the current wording so the surviving test's two assertions hold. Carry the D-14 rationale
across: this exploits the one real asymmetry in the wire surface (HOST-06) -- an unknown COMMAND
produces an error and is therefore detectable after the fact, whereas an unknown flag BIT produces
silence -- and it is keyed on the message id, never on the message text.

Do not add a class. Do not add logging. Do not import `firestarter.constants`,
`firestarter.serial_comm` or `firestarter.eprom_operations`: none is needed and each would break the
declared invariant.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -c "import ast; t=ast.parse(open('firestarter/sdp_honesty.py').read()); fns=sorted(n.name for n in t.body if isinstance(n,ast.FunctionDef)); assert fns==['emission_summary','map_unknown_cmd_to_outdated','unreadable_state_caveat'], fns; print('FUNCS OK')"; ruff check firestarter/sdp_honesty.py; ruff format --check firestarter/sdp_honesty.py</automated>
  </verify>
  <acceptance_criteria>
    - `firestarter/sdp_honesty.py` exists and its top-level function names are exactly `emission_summary`, `map_unknown_cmd_to_outdated`, `unreadable_state_caveat` (asserted by the AST check in the verify block).
    - `python -c "from firestarter.sdp_honesty import emission_summary as e; s=e('enable','at28c256'); assert 'AT28C256' in s; assert '\n' not in s; assert 'was emitted' in s; assert 'cannot be read back' in s; assert \"not a claim about the chip's actual state\" in s; print(s)"` exits 0.
    - `python -c "import re; from firestarter.sdp_honesty import emission_summary as e; [__import__('sys').exit(1) for m in ['enable','disable'] if re.search(r'\d+\s*(us|ms|s)\b', e(m,'AT28C256'), re.IGNORECASE)]"` exits 0 for both modes.
    - `python -c "from firestarter.sdp_honesty import emission_summary as e; assert 'cannot be read back' in e('disable','AT28C256')"` exits 0.
    - `python -c "from firestarter.exceptions import EpromOperationError; from firestarter.messages import MSG_ERR_UNKNOWN_CMD; from firestarter.sdp_honesty import map_unknown_cmd_to_outdated as m; r=m(EpromOperationError('x', error_code=MSG_ERR_UNKNOWN_CMD),'enable','AT28C256'); assert r is not None; assert 'firestarter fw --install' in str(r); assert 'does not implement' in str(r); assert m(EpromOperationError('y', error_code=0x01),'enable','AT28C256') is None; print('MAPPER OK')"` exits 0.
    - `python -c "import ast; t=ast.parse(open('firestarter/sdp_honesty.py').read()); mods=set(); [mods.add(a.name.split('.')[0]) for n in t.body if isinstance(n,ast.Import) for a in n.names]; [mods.add((n.module or '').split('.')[0]) for n in t.body if isinstance(n,ast.ImportFrom)]; assert mods <= {'__future__','firestarter'}, mods; print('PURITY OK', mods)"` exits 0.
    - `ruff check firestarter/sdp_honesty.py` and `ruff format --check firestarter/sdp_honesty.py` both exit 0.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output. Nothing in this phase edits that file at any point, so the emptiness check is safe rather than a hostage to later conditional edits.
  </acceptance_criteria>
  <done>The module exists with exactly the three named functions, all fully annotated, satisfying every behaviour bullet, importing nothing outside the declared invariant, and passing both ruff gates.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the live subcommand through the helper -- the one-time equivalence proof</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 2283-2321 (the two spans being rewired, and the surrounding `if ok:` / `sys.exit` structure that must not change)
    - /workspaces/firestarter_app/firestarter/cli_handlers.py lines 55-80 (the existing import block, so the new helper import is placed in the established grouping)
    - /workspaces/firestarter_app/firestarter/sdp_honesty.py (as created in task 1)
  </read_first>
  <action>
Change the retired-subcommand body so it obtains both pieces of wording from the helper instead of
composing them inline. This is the last commit in which the command and the helper coexist, and that
coexistence is the point.

Two edits inside the subcommand body, and nothing else in the file:

First, replace the inline construction of the outdated-firmware error in the `except
EpromOperationError` arm with a call to `map_unknown_cmd_to_outdated`, passing the caught exception,
the mode, and the uppercased chip name. When the helper returns a value, raise it with `from` the
caught exception so the chain is preserved exactly as today; when it returns `None`, re-raise bare, as
today. Keep the existing rationale comment in place but shorten it to a pointer at
`firestarter/sdp_honesty.py`, since the full reasoning now lives in the helper's docstring -- do not
delete the reasoning from the tree, relocate it.

Second, replace the inline three-fragment summary string in the `if ok:` arm with a single
`click.echo(emission_summary(mode, chip_upper))`. Keep `click.echo` rather than a logger call, for
the reason the existing comment gives: it always reaches the user's console and the CliRunner capture
regardless of log-level or handler wiring. Again shorten the D-10 comment to a pointer at the helper
rather than deleting it.

Add the helper import to the existing `firestarter.*` import grouping. Do **not** remove the
`MSG_ERR_UNKNOWN_CMD` import in this commit even though this file no longer references that name
directly -- if ruff flags it as unused, that is a signal the rewire is complete, and the import
removal belongs to plan 132-04's deletion commit where it can be attributed to the deletion. If ruff
does flag it here, remove it here and record in the summary that the removal moved forward by one
commit, so plan 132-04's own criteria can be adjusted rather than silently failing.

Then run the full unmodified dev-sdp test module and record the result. This is the deliverable of
this task, not a side effect: **26 tests, unchanged, must pass against the rewired command.** They
exercise the wording end to end through CliRunner -- the delivery path from the helper's return
value, through `click.echo`, to the user's captured console. After plan 132-04 removes the command,
no test in the tree can prove that delivery path again, so this run is the phase's only
behavioural-equivalence evidence and must be captured with its exact collected count.

Do not touch any test file in this commit. Do not touch `pyproject.toml` in this commit.
Commit in the submodule with a message naming RETIRE-03 and D-01.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_dev_sdp_cmd.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_dev_sdp_cmd.py -q` reports 26 passed, 0 failed, 0 skipped, 0 errors.
    - `git -C /workspaces/firestarter_app diff --stat tests/` produces no output -- no test file changed in this commit.
    - `git -C /workspaces/firestarter_app diff --stat pyproject.toml` produces no output.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output.
    - `grep -c "sdp_honesty" firestarter/cli_handlers.py` returns at least 3 (the import plus both call sites).
    - `python -c "import re,sys; s=open('firestarter/cli_handlers.py').read(); sys.exit(0 if s.count('resulting protection state cannot be read back')==0 else 1)"` exits 0 -- the caveat wording is no longer duplicated in the controller, it is obtained from the helper. <!-- planner-discipline-allow: resulting protection state cannot be read back -->
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `python -m pytest tests/ -q` reports zero failures across the whole suite.
    - `git -C /workspaces/firestarter_app log -1 --name-only` lists `firestarter/cli_handlers.py` and nothing else.
  </acceptance_criteria>
  <done>The subcommand composes both its summary line and its outdated-firmware error from the helper, and the unmodified 26-test module passes against it -- the equivalence proof is taken while it is still takeable.</done>
</task>

<task type="auto">
  <name>Task 3: Register the module in the strict island (D-02), then append the equivalence proof to the ledger</name>
  <files>firestarter_app/pyproject.toml, .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md</files>
  <read_first>
    - /workspaces/firestarter_app/pyproject.toml lines 138-210 (the `[tool.mypy]` global block with the watermark comment, the Phase-36 test strict island, the Phase-42 production strict island D-02 extends, and the follow-imports ring-fence block)
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md (as written by plan 132-01, including its `132-06 appends here` and `132-09 appends here` placeholders)
  </read_first>
  <action>
Two edits, in two repositories, in this order.

In `firestarter_app/pyproject.toml`: append `"firestarter.sdp_honesty"` to the Phase-42 production
strict-island `[[tool.mypy.overrides]]` `module` list -- the block carrying
`disallow_untyped_defs = true` and `check_untyped_defs = true`, whose comment currently says
"strict-island for the 8 modules touched in v1.8". Append at the end of the list; the list is
deliberately not alphabetical, so do not reorder it. **Update that block's own header comment in the
same edit** to record that the list is now nine modules and that the ninth is a phase-132 D-02
addition -- otherwise this edit creates a freshly stale comment claiming eight, which is precisely the
defect class RETIRE-08 exists to correct elsewhere in this same phase. Leave the block's existing note
that `eprom_operations.py` is deliberately excluded per D-07 exactly as it stands: that exclusion is
still in force and this phase does not touch it.

Touch nothing else in `pyproject.toml`. In particular the watermark comment stays at 35 (D-09) and no
`dependencies` or `optional-dependencies` entry is added or changed -- the latter is the criterion that
keeps threat T-132-SC's disposition honest.

Then re-run the CI-replica script from plan 132-01 and confirm the new module type-checks under
`disallow_untyped_defs`: the error count must not have gone UP relative to the count 132-01 recorded.
Record the observed count. It is expected to be unchanged from the pre-change reading at this point in
the phase: nothing has been deleted yet and no factory has been retyped, so the only movement possible
is a regression introduced by this plan. If the count rose, the new module has an unannotated
definition or the rewire introduced a type error -- fix it here rather than carrying it forward.

In the meta-repo, append a `## 1a. Behavioural-equivalence proof (plan 132-02)` subsection to
`132-MYPY-LEDGER.md`, above the `132-06 appends here` placeholder, recording: the collected/passed
test count from task 2's run (26), the submodule commit sha of the rewire, the statement that this run
exercised the wording end to end through CliRunner and `click.echo`, and the honest scope limit -- that
after plan 132-04 the four surviving assertions guard the **wording**, not its delivery, because
between this phase and Phase 134 the caveat has no user-reachable carrier at all (D-05's stated
residual). Say that plainly here so the phase record does not have to discover it later. Also record
the post-registration mypy count and its comparison against 132-01's reading.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; bash tools/ci_replica_venv.sh 2>&amp;1 | grep -E "mypy errors:|Found [0-9]+ errors|CI-REPLICA:"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "firestarter.sdp_honesty" /workspaces/firestarter_app/pyproject.toml` returns 1.
    - `python -c "import tomllib,sys; d=tomllib.load(open('/workspaces/firestarter_app/pyproject.toml','rb')); ovs=[o for o in d['tool']['mypy']['overrides'] if o.get('disallow_untyped_defs')]; assert len(ovs)==1; m=ovs[0]['module']; assert 'firestarter.sdp_honesty' in m; assert 'firestarter.eprom_operations' not in m; assert len(m)==9, len(m); print('ISLAND OK', len(m))"` exits 0.
    - The strict-island block's header comment names nine modules and cites phase 132 / D-02, verifiable by `grep -n "D-02" /workspaces/firestarter_app/pyproject.toml` returning a line inside that block.
    - `grep -c "mypy_error_watermark = 35" /workspaces/firestarter_app/pyproject.toml` returns 1 -- the watermark is untouched.
    - `git -C /workspaces/firestarter_app diff HEAD~1 -- pyproject.toml | grep -cE "^[+-].*(^|[^-])dependencies"` returns 0 -- no dependency line was added or changed, keeping T-132-SC's `accept` disposition truthful.
    - The replica script's `mypy errors: N (watermark: 35)` line shows an N no greater than the N recorded in 132-MYPY-LEDGER.md section 1, and that comparison is written into the ledger.
    - `132-MYPY-LEDGER.md` contains a `## 1a.` section stating the passed-test count `26`, the rewire commit sha, and a sentence stating that the four assertions guard the wording rather than its delivery once the subcommand is gone, citing D-05.
    - `git -C /workspaces log -1 --name-only` lists only paths under `.planning/`.
  </acceptance_criteria>
  <done>The new module is in the nine-module strict island with a non-stale header comment, the watermark and dependency set are untouched, the mypy count has not regressed, and the equivalence proof plus its honest scope limit are recorded in the ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware → host error path | An `EpromOperationError` carrying a firmware-supplied `error_code` crosses into `map_unknown_cmd_to_outdated`, which decides what the user is told about their firmware's age. |
| helper → future callers (Phase 134/135) | `sdp_honesty.py`'s API is a forward contract consumed by code that does not exist yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-04 | Spoofing | `map_unknown_cmd_to_outdated`'s decision input | medium | mitigate | The mapper keys strictly on `exc.error_code == MSG_ERR_UNKNOWN_CMD` -- the message **id**, never the message text (D-14's own rule, carried across verbatim). A behaviour bullet and an acceptance criterion both require `None` for any other `error_code` and for `None`, so a differently-worded firmware error can never be reported as an outdated firmware. |
| T-132-05 | Repudiation | the honesty caveat's integrity | high | mitigate | The caveat is composed once in `unreadable_state_caveat()` and reused by `emission_summary`, so the summary line cannot claim a verified protection state while the standalone clause disclaims one. The three greppable tokens are pinned by acceptance criteria, and plan 132-03 retargets four behavioural assertions onto them. |
| T-132-06 | Tampering | `pyproject.toml` edit scope | medium | mitigate | Acceptance criteria assert the watermark comment is unchanged, no dependency line moved, `eprom_operations` is absent from the strict island, and the island is exactly nine modules -- so a widened override or a dependency injection cannot ride along in this edit. |
| T-132-SC | Tampering | package installs | high | accept | No new package is introduced anywhere in phase 132; the only `pyproject.toml` change is the mypy override-list insertion above, enforced by the no-dependency-line criterion. The Package Legitimacy Gate's trigger condition is not met, so no legitimacy checkpoint is required. Recorded rather than silently skipped. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && python -m pytest tests/test_dev_sdp_cmd.py -q` reports 26
   passed -- the equivalence proof.
2. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
3. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
4. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` reports a mypy count no higher
   than plan 132-01's recorded count.
5. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` is empty.
6. The strict island contains exactly nine modules, `eprom_operations` not among them.
</verification>

<success_criteria>
- `firestarter/sdp_honesty.py` exists with the three named, fully-annotated functions and the
  declared import invariant, and is type-checked as a strict-island member.
- The still-live subcommand emits both its summary line and its outdated-firmware error through the
  helper, and the unmodified 26-test module passes against it.
- The watermark stays at 35; the dependency closure is unchanged; `eprom_operations.py` is untouched.
- `132-MYPY-LEDGER.md` carries the equivalence proof and the D-05 delivery-residual statement.
- No RETIRE requirement is marked Complete.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-02-SUMMARY.md` when done.
State in it: the three function signatures as authored, the passed-test count from the equivalence
run, the post-registration mypy count versus 132-01's, whether the unused-import removal moved forward
from plan 132-04, and an explicit line reading "Requirements marked Complete: none (RETIRE-03 is owned
by plan 132-03)".
</output>
