---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 08
type: execute
wave: 8
depends_on: ["132-07"]
files_modified:
  - firestarter_app/tests/test_revision_constants_parity.py
  - firestarter_app/firestarter/constants.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RETIRE-04, RETIRE-08]
must_haves:
  truths:
    - "Both SDP command constants and their command-name entries survive the retirement, and a test dereferences both, so a future edit dropping an entry fails a test rather than surfacing as a lookup error at operation setup (RETIRE-04, ROADMAP criterion 3)."
    - "All five stale dereference references are corrected -- not the three the requirement claims -- with function names first and the corrected line numbers alongside (RETIRE-08, D-11)."
    - "RETIRE-08's own text is corrected in-phase with the measured evidence clause, rather than being ticked while known to be wrong (D-12)."
  artifacts:
    - .planning/REQUIREMENTS.md
  key_links:
    - "The command-name entries are load-bearing rather than cosmetic: they are dereferenced at two points during operation setup, and a missing entry is a lookup error at setup, not a display gap. That is exactly what the new test pins."
    - "The corrected references must not point at the ring-fenced operations module's line numbers alone -- function names are the durable anchor, because this milestone already holds the receipt that line numbers re-stale."
---

<objective>
Add the dereference test that keeps the two SDP command constants alive, correct all five stale
dereference references, and correct RETIRE-08's own wording in the same breath.

Purpose: two requirements converge on one pair of files. RETIRE-04 exists because the retirement removes
the only *host-surface* user of the two SDP command constants, which leaves their command-name entries
looking cosmetic when in fact the operation layer dereferences them twice during setup -- so a future
tidy-up that drops an entry would surface as a runtime lookup error rather than a failing test.
RETIRE-08 exists because the comments that document that very fact cite line numbers that moved.

D-12 makes the requirement-text correction part of this phase rather than a Phase 137 cleanup: fixing five
satisfies "three" a fortiori, but RETIRE-08 is this phase's own requirement, and ticking it while knowing
its text is wrong is the shape that closes as "three corrected" with two left behind.

Output: one new test, five corrected references across two files, and a corrected requirement.

May mark Complete: **RETIRE-04 and RETIRE-08.** Both are fully discharged here. May NOT mark Complete:
RETIRE-01/02/03/05/07 (already Complete -- do not re-tick, do not un-tick), RETIRE-06 (owned by 132-09).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-07-SUMMARY.md
</context>

<repo_map>
This plan spans **both repositories**, and the split is load-bearing rather than incidental:
- `firestarter_app/tests/test_revision_constants_parity.py` and
  `firestarter_app/firestarter/constants.py` are in the **submodule**
  (`/workspaces/firestarter_app/`, branch `gsd/v1.30-sdp-surface-retirement`). Tasks 1 and 2 commit there.
- `.planning/REQUIREMENTS.md` is in the **meta-repo** (`/workspaces/`, branch
  `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`). Task 3 commits there.

**A constraint that cannot be met as literally worded, and how it is honoured instead.** D-12 requires
RETIRE-08's text be corrected "in the same commit as the fixes". The fixes are in the submodule and the
requirement text is in the meta-repo, so a single commit containing both is impossible -- two git
repositories cannot share a commit. The binding is therefore honoured as **same plan, same task sequence,
adjacent commits, with each commit naming the other**: task 2's submodule commit message cites the
meta-repo correction as its companion, and task 3's meta-repo commit message cites the submodule commit's
sha. Record this in the summary explicitly rather than letting a later reader think "same commit" was met.
The firmware repo is not touched.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files** -- `firestarter_app/firestarter/sdp_honesty.py` (132-02),
`firestarter_app/tools/ci_replica_venv.sh` (132-01), `firestarter_app/tests/test_sdp_honesty.py` (132-03)

**New functions in `firestarter/sdp_honesty.py`** (132-02) -- `unreadable_state_caveat`,
`emission_summary`, `map_unknown_cmd_to_outdated`

**New symbols in `tests/conftest.py`** (132-05) -- `make_app_context`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` (132-03),
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` (132-07), and
`test_command_names_dereferences_both_sdp_commands` in
`tests/test_revision_constants_parity.py` -- **this plan**

**New config entries** -- `"firestarter.sdp_honesty"` in the Phase-42 strict island (132-02).

**New constants:** none. This plan corrects a comment above two existing constants and adds no name.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01),
`132-PRUNE-LEDGER.md` (132-03), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Re-measured at plan time against `firestarter_app` @ `8caf77f`. Plan 132-07 has since appended to
`constants.py`'s flag block, so re-read that file; the blocks are non-adjacent and that edit does not move
these anchors, but verify rather than assume.

**The constants that must survive, and their entries.** `COMMAND_SDP_UNLOCK = 9` is at
`firestarter/constants.py:73` and `COMMAND_SDP_LOCK = 10` at `:74` -- 132-CONTEXT.md said `:72`/`:73`; the
measured values are `:73`/`:74`. Their command-name mapping entries are at `:90` and `:91`, confirmed exact,
mapping to the names `SDP_UNLOCK` and `SDP_LOCK`.

**The correct dereference anchors, re-measured and confirmed in the ring-fenced operations module:**
`_setup_operation` begins at `:315` with its command-name dereference at **`:329`**; `_operation_context`
begins at `:376` with its dereference at **`:405`**. The stale comments cite `301` and `377`.

**All five stale references -- the measured evidence clause D-12 requires.** RETIRE-08's text says
"three"; there are five, across two files. **Zero stale tokens exist inside the operations module itself**
-- that file needs no edit at all, which is what makes its ring-fence trivially provable here.

| # | File | Line | The stale citation |
|---|------|------|--------------------|
| 1 | `firestarter/constants.py` | `:69-70` | a two-line comment citing the dereference at `301` and again at `377`, naming both functions |
| 2 | `tests/test_revision_constants_parity.py` | `:71-72` | the same citation in a module or class docstring |
| 3 | `tests/test_revision_constants_parity.py` | `:527` | a short inline citation of both numbers |
| 4 | `tests/test_revision_constants_parity.py` | `:549` | **inside an assertion message string** -- correcting it changes a test's failure text, not its pass/fail behaviour |
| 5 | `tests/test_revision_constants_parity.py` | `:585-586` | the same citation again, naming both functions |

**Other measured facts:**
- `tests.test_revision_constants_parity` is in the Phase-36 test strict island, so edits there are
  body-checked. It carries 3 mypy errors today, none of them of a class this phase fixes; leaving them is
  deliberate and they are inside the watermark.
- The constants comment block at `:67-72` explains *why* the entries are load-bearing: a missing entry is a
  lookup failure at operation setup rather than a cosmetic display gap. That reasoning is what RETIRE-04's
  test mechanises, so the corrected comment and the new test should point at each other.
- The operations module is ring-fenced by operator decision and is dispositioned to a future item. This
  plan reads it to confirm the anchors and changes nothing in it.
</measured_anchors>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dereference test that keeps both command-name entries alive (RETIRE-04)</name>
  <files>firestarter_app/tests/test_revision_constants_parity.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/constants.py lines 60-95 (both SDP command constants, the full command-name mapping, and the comment block explaining why the entries are load-bearing)
    - /workspaces/firestarter_app/firestarter/eprom_operations.py lines 310-335 and 370-410 (the two dereference sites -- read to confirm the anchors and to see exactly what a missing entry would do at setup; change nothing here)
    - /workspaces/firestarter_app/tests/test_revision_constants_parity.py in full (its existing assertion style, its class structure, and the three places it cites the dereference so the new test sits with them rather than apart)
  </read_first>
  <behavior>
    - Given the command-name mapping, indexing it with each of the two SDP command constants returns a non-empty string -- both dereferences succeed, mirroring what operation setup does.
    - The test dereferences both constants, not one: dropping either entry fails it.
    - The test asserts the constants themselves still exist and are importable, so a future edit that removes a constant rather than an entry also fails it.
    - The test's failure message names operation setup as the consequence, so a developer who breaks it learns what would have happened at runtime rather than only that a mapping lacks a key.
    - The test does not assert the mapping's exact string values beyond non-emptiness in a way that would make an intentional display rename a failure -- the requirement is that the dereference succeeds, not that the label is frozen.
  </behavior>
  <action>
Add a test named `test_command_names_dereferences_both_sdp_commands` to
`tests/test_revision_constants_parity.py`, placed with that module's existing SDP-related assertions rather
than appended at the end.

What it must do: import both SDP command constants and the command-name mapping, then dereference the
mapping with each constant in turn -- performing the same lookup operation setup performs -- and assert each
lookup yields a non-empty string. Assert against both constants explicitly, in two separate assertions or a
loop over both, so that dropping either entry fails. Also assert both constants exist with their expected
integer values, so removing a constant fails too rather than silently making the lookup vacuous.

Its docstring must state the requirement id and the mechanism: the retirement removed the only host-surface
user of these two constants, which makes their entries *look* cosmetic; they are not, because the operation
layer dereferences the mapping at two points during setup, so a missing entry is a lookup failure at setup
rather than a display gap. Name both dereferencing functions -- the setup routine and the operation-context
routine -- as the durable anchors, with their corrected line numbers alongside, matching the convention
task 2 applies to the comments (D-11). Point at the corrected comment in the constants module so the two
halves reference each other.

Give the assertions explicit messages naming operation setup as the consequence. A bare assertion failure
tells a developer a key is missing; a message naming the setup failure tells them why the key exists.

Then prove the test is a real gate, not an unreachable green. Temporarily remove one of the two mapping
entries, run the test, read the failure message, and confirm it names the missing constant and the setup
consequence. Then restore the entry, remove the *other* one, and confirm the test fails again -- this second
half is what proves the test really dereferences **both** rather than passing on the strength of one. Then
revert and confirm green and a clean tree. Record both failure outputs verbatim in the summary. A test that
has only ever been seen to pass proves nothing, and a two-key test that has only been proven against one key
is half a test.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_command_names_dereferences_both_sdp_commands" /workspaces/firestarter_app/tests/test_revision_constants_parity.py` returns 1.
    - `python -m pytest "tests/test_revision_constants_parity.py::test_command_names_dereferences_both_sdp_commands" -q` passes, or the equivalent class-qualified node id passes if the test is placed inside a class.
    - Both constants and both entries are still present: `python -c "from firestarter.constants import COMMAND_SDP_LOCK, COMMAND_SDP_UNLOCK, COMMAND_NAMES; assert COMMAND_SDP_UNLOCK == 9 and COMMAND_SDP_LOCK == 10; assert COMMAND_NAMES[COMMAND_SDP_UNLOCK] and COMMAND_NAMES[COMMAND_SDP_LOCK]; print('DEREF OK', COMMAND_NAMES[COMMAND_SDP_UNLOCK], COMMAND_NAMES[COMMAND_SDP_LOCK])"` exits 0.
    - The test body references both constants: `grep -c "COMMAND_SDP_UNLOCK" /workspaces/firestarter_app/tests/test_revision_constants_parity.py` and `grep -c "COMMAND_SDP_LOCK"` each return at least 2 (the pre-existing citations plus the new test's use).
    - The docstring names RETIRE-04 and both dereferencing function names: `grep -c "_setup_operation" /workspaces/firestarter_app/tests/test_revision_constants_parity.py` and `grep -c "_operation_context"` each return at least 1.
    - **Both** RED demonstrations are recorded in the summary -- one per removed entry -- each with the test's verbatim failure output, and confirmation that the message names the missing constant and the setup consequence. A single demonstration does not satisfy this criterion.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty after the demonstrations.
    - `python -m pytest tests/ -q` reports zero failures, and `ruff check firestarter/ tests/` plus `ruff format --check firestarter/ tests/` both exit 0.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output.
  </acceptance_criteria>
  <done>The named test dereferences both constants with messages naming the setup consequence, has been seen to fail once per entry removed, and both constants and entries are asserted present.</done>
</task>

<task type="auto">
  <name>Task 2: Correct all five stale dereference references, function names first (RETIRE-08, D-11)</name>
  <files>firestarter_app/firestarter/constants.py, firestarter_app/tests/test_revision_constants_parity.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/constants.py lines 60-95 (stale reference 1, and note plan 132-07 appended to a different block further down -- do not disturb that)
    - /workspaces/firestarter_app/tests/test_revision_constants_parity.py lines 60-80, 515-560, 575-595 (stale references 2 through 5, one of which is inside an assertion message)
    - /workspaces/firestarter_app/firestarter/eprom_operations.py lines 310-335 and 370-410 (the ground truth the corrected references must match -- read only)
  </read_first>
  <action>
Correct all five stale references. Not three. Five is the measured count, and each one is enumerated in this
plan's measured-anchors table with its file and line.

Apply D-11's convention to every one: **name the function first, with the corrected line number
alongside** as a reader convenience. Function names are the durable anchor and the corrected numbers earn
their place because the two dereference points are 76 lines apart in a large module -- but the names come
first, because this milestone already holds the receipt that line numbers re-stale (a prior milestone's
insertion staled 11 of 12 anchors). So the corrected form reads as the setup routine's name with its
dereference line, and the operation-context routine's name with its dereference line.

The five sites, each requiring the same substantive correction:
1. In `constants.py`, the two-line comment above the SDP command constants. Preserve everything else the
   block says -- particularly the sentence explaining that a missing entry is a lookup failure at operation
   setup rather than a cosmetic display gap, which is the reasoning task 1's test mechanises. Add a pointer
   to the new test's name, so the comment and the test reference each other.
2. In the parity test module, the docstring citation near the top.
3. In the same module, the short inline citation.
4. In the same module, the citation **inside an assertion message string**. Correcting it changes that
   test's failure text and nothing about its pass/fail behaviour -- state that in the summary so a reviewer
   reading the diff does not mistake it for a semantic change.
5. In the same module, the final citation naming both functions.

Change nothing in the operations module. It contains **zero** stale tokens -- so its diff must be empty,
and that emptiness is a safe criterion rather than a hostage to later conditional edits, because no task in
this entire phase edits that file at all.

Commit in the submodule. The commit message must name RETIRE-08, state that five references were corrected
across two files, and cite the meta-repo companion commit from task 3 by intent (the sha will not exist
yet, so name the file and the requirement; task 3's message closes the loop with this commit's sha).
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - No stale token remains: `grep -rn "eprom_operations.py:301" /workspaces/firestarter_app` returns nothing, and `grep -rn ":377" /workspaces/firestarter_app --include='*.py'` returns nothing.
    - The corrected anchors are present: `grep -rc "329" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1 and `grep -rc "405" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1; and in the parity module, `grep -c "329" /workspaces/firestarter_app/tests/test_revision_constants_parity.py` returns at least 4 and `grep -c "405"` returns at least 4 -- one per corrected reference in that file.
    - Function names lead in every corrected site: `grep -c "_setup_operation" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1, and in the parity module at least 4.
    - The load-bearing reasoning survived in the constants comment: `grep -ci "not a cosmetic" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1, or the equivalent sentence is confirmed present in the summary by quotation.
    - The constants comment names the new test: `grep -c "test_command_names_dereferences_both_sdp_commands" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output. This phase edits that file in no task, so the emptiness check is safe.
    - Plan 132-07's flag-block note is undisturbed: `grep -c "D-14" /workspaces/firestarter_app/firestarter/constants.py` still returns at least 1.
    - `python -m pytest tests/ -q` reports zero failures -- confirming that correcting the assertion-message citation changed failure text only.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `bash tools/ci_replica_venv.sh` reports a `mypy errors:` count no higher than plan 132-06's recorded count.
    - The summary enumerates all five corrected sites with their file and line, and states that site 4 changed a failure message rather than behaviour.
  </acceptance_criteria>
  <done>All five references name the function first with the corrected line alongside, the load-bearing reasoning and plan 132-07's note both survive, the ring-fenced module's diff is empty, and the assertion-message change is identified as failure-text-only.</done>
</task>

<task type="auto">
  <name>Task 3: Correct RETIRE-08's own text with the measured evidence clause (D-12)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /workspaces/.planning/REQUIREMENTS.md lines 118-145 (the RETIRE block, so the correction matches the file's existing requirement voice and checkbox convention)
    - /workspaces/.planning/REQUIREMENTS.md lines 14-40 (the Evidence Ceiling, which must not be smoothed over by any edit to this file)
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md the D-12 paragraph (the requirement that the correction carry the measured evidence clause, enumerated)
  </read_first>
  <action>
Correct RETIRE-08's text in the meta-repo's requirements file, in this plan and not in a later phase.

Replace the count with the measured one and attach the evidence clause: five references, across two files,
enumerated. Use the measured-anchors table from this plan as the enumeration -- one reference in the
constants module and four in the parity test module, with the note that one of the four sits inside an
assertion message. Also record that the ring-fenced operations module itself contains **zero** stale tokens,
because that fact is what makes the correction's scope surprising and is the single most likely thing for a
future reader to get wrong.

Keep D-11's convention in the corrected requirement text itself: name the two dereferencing functions, with
the corrected line numbers alongside. A requirement that cites only numbers re-stales, which is the defect
being corrected.

Mark RETIRE-08's checkbox complete in the same edit, and RETIRE-04's, since both are discharged by this
plan. Do not touch any other checkbox in the file -- in particular do not tick RETIRE-06, whose Complete
transition belongs to plan 132-09 after the certifying CI run.

Do not soften, reflow or abbreviate the Evidence Ceiling section, and do not touch the out-of-scope row that
records the operations-module ring-fence as an operator decision. Both are load-bearing records that other
phases cite, and this file has a recorded history of edits that damaged neighbouring content.

State the reason for the in-phase correction in the requirement's own text or in an adjacent bracketed note,
following the file's existing convention for corrections: fixing five satisfies "three" a fortiori, but
ticking a requirement while knowing its text is wrong is the shape that closes as "three corrected" with two
left behind, and deferring the wording would leave a wrong number in this file for five phases.

Commit in the meta-repo. The commit message must cite task 2's submodule commit sha, closing the loop the
repo-map section describes, and must state plainly that D-12's "same commit" binding is met as adjacent
cross-repository commits because a single commit spanning two repositories is impossible.
  </action>
  <verify>
    <automated>grep -n "RETIRE-08" /workspaces/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - RETIRE-08's text no longer claims three: `grep -c "three in-tree stale" /workspaces/.planning/REQUIREMENTS.md` returns 0.
    - RETIRE-08's text carries the measured count and the enumeration: its line or lines contain the word `five`, the phrase identifying two files, and both corrected anchors, verifiable by `grep -A4 "RETIRE-08" /workspaces/.planning/REQUIREMENTS.md` showing `five`, `329` and `405`.
    - RETIRE-08's text names both dereferencing functions: the same grep window shows `_setup_operation` and `_operation_context`.
    - RETIRE-08's text records that the operations module itself contains zero stale tokens.
    - Both checkboxes are ticked and no other changed: `git -C /workspaces diff -- .planning/REQUIREMENTS.md | grep -cE "^\+.*\[x\].*\*\*RETIRE-(04|08)\*\*"` returns 2, and `git -C /workspaces diff -- .planning/REQUIREMENTS.md | grep -cE "^\+.*\[x\].*\*\*RETIRE-06\*\*"` returns 0.
    - The Evidence Ceiling is untouched: `git -C /workspaces diff -- .planning/REQUIREMENTS.md | grep -cE "^-.*(Evidence Ceiling|NOT provable|UNVERIFIED)"` returns 0.
    - The out-of-scope ring-fence row is untouched: `git -C /workspaces diff -- .planning/REQUIREMENTS.md | grep -cE "^-.*FUT-MYPY-02"` returns 0.
    - The meta-repo commit message contains task 2's submodule commit sha and the explicit statement about the cross-repository "same commit" binding.
    - `git -C /workspaces log -1 --name-only` lists only paths under `.planning/`.
  </acceptance_criteria>
  <done>RETIRE-08 reads five with the enumerated two-file evidence clause and both function-name anchors, RETIRE-04 and RETIRE-08 are ticked and nothing else is, the Evidence Ceiling and the ring-fence row are intact, and the cross-repository commit binding is stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command-name mapping → operation setup | Operation setup dereferences the mapping twice; a missing entry is a runtime lookup failure on a real device operation, not a cosmetic gap. |
| requirement text → every later phase's reader | The requirements file is the milestone contract; a wrong number in it propagates into five later phases' artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-29 | Denial of service | a dropped command-name entry | high | mitigate | A test dereferences **both** constants with messages naming the setup consequence, and it has been proven to fail once per entry removed -- so it cannot pass on the strength of one key. Both constants' values are asserted too, so removing a constant rather than an entry also fails. |
| T-132-30 | Spoofing | a half-proven two-key test | high | mitigate | Two separate RED demonstrations are required, one per entry, each with its verbatim failure output recorded. A single demonstration is explicitly insufficient. |
| T-132-31 | Tampering | collateral damage to the requirements file | high | mitigate | Criteria assert the Evidence Ceiling and the ring-fence out-of-scope row are unchanged, that exactly two checkboxes moved, and that RETIRE-06's did not. This file has a recorded history of edits damaging neighbouring content. |
| T-132-32 | Repudiation | corrections that re-stale | medium | mitigate | Every corrected reference names the function first with the number alongside (D-11), and the requirement's own text does the same, so the next insertion into the operations module does not invalidate the correction's meaning. |
| T-132-33 | Tampering | straying into the ring-fenced module | high | mitigate | That module contains zero stale tokens and needs no edit; its empty diff is asserted, and the emptiness check is safe because no task in the whole phase edits that file. |
| T-132-34 | Repudiation | the impossible "same commit" binding | medium | accept | D-12's literal wording cannot be met across two repositories. Honoured as adjacent commits that cite each other, with the impossibility stated in the commit message and the summary rather than papered over. |
| T-132-SC | Tampering | package installs | high | accept | No package is installed or added; `pyproject.toml` is untouched. The Package Legitimacy Gate's trigger condition is not met. |
</threat_model>

<verification>
1. The named test passes, and both per-entry RED demonstrations are recorded verbatim.
2. No stale token remains anywhere in the submodule, and the corrected anchors appear in all five sites with
   function names leading.
3. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
4. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
5. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` reports a count no higher than plan
   132-06's.
6. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` is empty.
7. RETIRE-08's corrected text and exactly two moved checkboxes are confirmed by the diff greps.
</verification>

<success_criteria>
- `test_command_names_dereferences_both_sdp_commands` exists, dereferences both constants, and has been seen
  to fail once for each entry removed.
- All five stale references are corrected with function names first and the corrected numbers alongside.
- RETIRE-08's own text reads five with the enumerated two-file evidence clause and both function anchors.
- The Evidence Ceiling, the ring-fence out-of-scope row, and plan 132-07's flag-block note are all intact.
- The ring-fenced module's diff is empty; the watermark and the mypy count are unmoved.
- RETIRE-04 and RETIRE-08 are marked Complete. No other RETIRE id is ticked.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-08-SUMMARY.md` when done.
State in it: the two verbatim RED outputs from the per-entry demonstrations, the five corrected sites with
file and line, the note that the assertion-message correction changed failure text only, RETIRE-08's before
and after text, the cross-repository "same commit" statement with both commit shas, and an explicit line
reading "Requirements marked Complete: RETIRE-04, RETIRE-08".
</output>
