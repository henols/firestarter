---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 07
type: execute
wave: 7
depends_on: ["132-06"]
files_modified:
  - firestarter_app/firestarter/cli_handlers.py
  - firestarter_app/firestarter/constants.py
  - firestarter_app/tests/test_write_skip_sdp_unlock.py
autonomous: true
requirements: [RETIRE-07]
must_haves:
  truths:
    - "The removal-safety dependency is recorded as a tripwire, not a sentence in a note: a comment at the host auto-unlock DECISION site plus a test named for the dependency (RETIRE-07, ROADMAP criterion 5, D-14)."
    - "The tripwire is placed so that a developer changing the auto-unlock default must read it by construction -- the criterion is reachability from the edit, not a line number."
    - "No output is added to the write auto-unlock path while placing the comment (D-05): the tripwire is a comment and a test, never a user-visible message."
  artifacts:
    - firestarter_app/tests/test_write_skip_sdp_unlock.py
  key_links:
    - "The record's original coordinate pointed at the AUDIT site in the operations module, which is mis-attributed and also ring-fenced. The DECISION site is in the CLI handler: the flag's defaults and the auto-set block."
    - "The named test extends an existing module that plan 132-05 already retyped, so the file must be re-read after that plan rather than assumed."
---

<!-- planner-discipline-allow: dev sdp -->

<objective>
Place the tripwire that makes this removal's safety argument self-enforcing: a comment at the host
auto-unlock decision site, a matching comment at the wire flag's definition, and a test whose name and
docstring *are* the record.

Purpose: the whole safety argument for deleting the subcommand is that the host already unlocks
protected parts automatically, by default, on every write. That argument is currently a sentence in a
design note. RETIRE-07's point is that a sentence rots invisibly, whereas a comment at the decision site
plus a test named for the dependency forces the argument to be revisited if the default ever changes.
D-14 additionally corrects where the tripwire goes: the record's coordinate pointed at the *audit* site
in the ring-fenced operations module, which is both mis-attributed and off-limits. The *decision* lives
in the CLI handler.

Output: three comment sites and one named test.

May mark Complete: **RETIRE-07 only.** May NOT mark Complete: RETIRE-01/02/03/05 (already Complete --
do not re-tick, do not un-tick), RETIRE-04 and RETIRE-08 (owned by plan 132-08 -- this plan edits the
same constants file, but only at the flag definition, and must not touch the stale comment block that
132-08 corrects), RETIRE-06 (owned by 132-09).
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-06-SUMMARY.md
</context>

<repo_map>
All three files are in the **`firestarter_app` submodule** (`/workspaces/firestarter_app/`, branch
`gsd/v1.30-sdp-surface-retirement`) and every commit lands there. Nothing in the meta-repo changes
except this plan's SUMMARY. The firmware repo is not touched -- in particular, the firmware-side flag
handling is out of scope and no dual-repo lockstep applies.
</repo_map>

<artifacts_this_phase_produces>
**New modules / files** -- `firestarter_app/firestarter/sdp_honesty.py` (132-02),
`firestarter_app/tools/ci_replica_venv.sh` (132-01), `firestarter_app/tests/test_sdp_honesty.py` (132-03)

**New functions in `firestarter/sdp_honesty.py`** (132-02) -- `unreadable_state_caveat`,
`emission_summary`, `map_unknown_cmd_to_outdated`

**New symbols in `tests/conftest.py`** (132-05) -- `make_app_context`, `app_context` fixture

**New tests** -- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` (132-03),
`test_command_names_dereferences_both_sdp_commands` (132-08), and
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` in
`tests/test_write_skip_sdp_unlock.py` -- **this plan**

**New config entries** -- `"firestarter.sdp_honesty"` in the Phase-42 strict island (132-02).

**New constants:** none. This plan adds comments beside an existing flag constant, not a new one.

**New meta-repo records** -- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (132-01),
`132-PRUNE-LEDGER.md` (132-03), `132-CI-GREEN.md`, `132-RECORD.md` (132-09)
</artifacts_this_phase_produces>

<measured_anchors>
Re-measured at plan time against `firestarter_app` @ `8caf77f`. Plans 132-02, 132-04 and 132-05 have all
edited these files by the time this plan runs, so **re-read each file and re-locate each site by
identifier** rather than trusting the offsets below. D-14's own instruction is that the criterion is
reachability, not coordinates.

- **The decision site.** The auto-set and flag-independence block occupies
  `firestarter/cli_handlers.py:622-645` in the write handler. Its shape: the chip entry is fetched, a
  protocol-identity boolean is computed by comparing the entry's protocol key against the SDP protocol
  constant, the capability predicate is called, and then the decision itself -- when the part is on the
  SDP protocol **and** capability-refused **and** the user did not pass the skip flag, the flag is
  auto-set to true and an unconditional report line is echoed. The following branch is the
  warn-and-proceed arm for a non-SDP-protocol part that was given the flag anyway. **The tripwire comment
  goes adjacent to the auto-set condition**, in the existing rationale-in-prose, decision-ID-tagged
  comment style that block already uses.
- **The two default sites.** The flag parameter appears twice with different shapes, and
  132-CONTEXT.md conflated them: at `cli_handlers.py:302` it is a `= False` **default** inside the
  op-flags builder (which begins at `:295`), while at `:579` it is a **required, non-defaulted parameter**
  on the write handler's signature. The Click-level default lives on the option decorator above the write
  handler -- locate it and treat it as the third comment site, because that decorator is where a developer
  would actually flip the default.
- **The flag constant.** `FLAG_SKIP_SDP_UNLOCK` is at `firestarter/constants.py:121`, inside a comment
  block running from `:110`. That block already carries a firmware-sync caveat, so **append** to it rather
  than displacing what is there.
- **The test host.** `tests/test_write_skip_sdp_unlock.py` already covers this flag with six tests at
  `:157`, `:179`, `:199` (parametrised over two refused parts), `:218`, `:243` and `:263`. Its naming
  convention is long and criterion-shaped -- copy it. Its write-driver helper is at `:86` and drives a
  full successful write through a real operator wired to a fake serial port, capturing the composed wire
  command dict at the point it would cross onto the wire; the tripwire test should reuse it rather than
  re-authoring a runner invocation.
- **Three concerns land in that one file across this phase**, and they are deliberately sequenced across
  waves so they do not collide: plan 132-03 corrected two stale docstring citations in it, plan 132-05
  retyped its local factory into a typed delegate that keeps a **real** operator as the default, and this
  plan adds the named test. Read the file as it now stands.
- `constants.py` is also edited by plan 132-08, at the stale-comment block near `:67-72`. That block and
  the flag block near `:110-121` are distinct and non-adjacent; this plan touches only the latter.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: The tripwire comment at the host auto-unlock decision site (D-14, RETIRE-07)</name>
  <files>firestarter_app/firestarter/cli_handlers.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/cli_handlers.py the auto-set and flag-independence block near :622-645 (the decision condition, the report line, and the warn-and-proceed arm -- and the existing comment style to mirror)
    - /workspaces/firestarter_app/firestarter/cli_handlers.py the op-flags builder near :295-310 (the `= False` default) and the write handler's signature near :575-585 plus the Click option decorator above it (the third site)
    - /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md the D-14 paragraph (the criterion: a developer changing the auto-unlock default must read the tripwire by construction)
  </read_first>
  <action>
Add the tripwire comment at the decision site and short pointer comments at the two default sites.

The decision-site comment is the substantive one. It must state, in the block's existing prose style and
tagged with the decision id, the complete dependency in a form a developer cannot misread: the host's
retired standalone SDP subcommand was removed in phase 132 (RETIRE-01) on the argument that no user needs
a manual unlock surface, **because** this auto-unlock is default-on for capability-refused parts on the
SDP protocol; therefore flipping that default, or narrowing the condition, or making the flag default to
skipping, invalidates the removal argument and requires the removal decision to be revisited with it.
Name the test that carries the same record so the reader can find it, and name the requirement id.

Keep it a comment. Do not add a message, do not add a warning, do not change the condition, and do not
change what is echoed. D-05 is explicit that no honesty caveat is added to this path in this phase, and
the reasoning is that the auto-unlock makes no claim about lock state, so there is no dishonest claim to
qualify -- and a retirement phase is the wrong place to change the production write path's user-visible
behaviour. The tripwire is a comment and a test, full stop.

At the two default sites, add one short comment each pointing at the decision-site comment: one beside the
op-flags builder's `= False` default, one beside the Click option decorator that establishes the
user-facing default. These are the two places a developer would actually type when flipping the default, so
each needs a pointer even though the reasoning lives in one place. Note that the write handler's own
parameter is required rather than defaulted, so it needs no pointer -- do not add one there and do not
"fix" the signature to give it a default.

Prove the placement satisfies D-14's criterion rather than merely existing. Identify, from the code, every
edit a developer would have to make to turn auto-unlock off -- changing the builder default, changing the
Click option's default, or altering the auto-set condition -- and confirm each of those lines is within a
few lines of a tripwire comment. Record that enumeration in the summary. A comment 200 lines from the edit
satisfies a grep and fails the criterion.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; ruff check firestarter/ tests/; ruff format --check firestarter/ tests/; python -m pytest tests/test_write_skip_sdp_unlock.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RETIRE-01" /workspaces/firestarter_app/firestarter/cli_handlers.py` returns at least 1 -- the decision-site comment names the requirement whose safety argument it protects.
    - `grep -c "test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on" /workspaces/firestarter_app/firestarter/cli_handlers.py` returns at least 1 -- the comment names the test that carries the same record, so the two halves of the tripwire point at each other.
    - `grep -c "D-14" /workspaces/firestarter_app/firestarter/cli_handlers.py` returns at least 3 -- the decision site plus the two default-site pointers.
    - Reachability is proven and recorded: the summary enumerates each line a developer would edit to disable auto-unlock (the builder default, the Click option default, the auto-set condition) with its line number and the line number of the nearest tripwire comment, and every distance is under 10 lines.
    - No user-visible output was added: `git -C /workspaces/firestarter_app diff HEAD~1 -- firestarter/cli_handlers.py | grep -cE "^\+[^+].*(click\.echo|logger\.)"` returns 0, enforcing D-05.
    - The decision logic is unchanged: `git -C /workspaces/firestarter_app diff HEAD~1 -- firestarter/cli_handlers.py | grep -E "^[+-][^+-]" | grep -vcE "^[+-]\s*#"` returns 0 -- every added or removed line is a comment line, so no statement moved.
    - `python -m pytest tests/ -q` reports zero failures.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py` produces no output -- the mis-attributed audit-site coordinate was correctly not followed.
  </acceptance_criteria>
  <done>The decision site carries a comment naming the requirement and the test, both default sites carry pointers, every line a developer would edit to disable auto-unlock is within 10 lines of a tripwire, the diff is comment-only, and no output was added.</done>
</task>

<task type="auto">
  <name>Task 2: Append the tripwire note at the wire flag's definition in constants.py</name>
  <files>firestarter_app/firestarter/constants.py</files>
  <read_first>
    - /workspaces/firestarter_app/firestarter/constants.py lines 105-125 (the flag's existing comment block with its firmware-sync caveat, and the flag definition itself -- so the new note appends rather than displaces)
    - /workspaces/firestarter_app/firestarter/constants.py lines 60-95 (the stale-comment block and the command-name entries that plan 132-08 edits -- read so this task can stay clear of them)
  </read_first>
  <action>
Append a short note to the existing comment block above the skip-SDP-unlock wire flag.

The note states the same dependency in one or two sentences, from the constant's point of view: this flag's
default-off state is what makes the host's auto-unlock effective by default, which is the argument on which
phase 132 removed the standalone SDP subcommand; changing the semantics or the default of this bit
invalidates that argument. Point at the decision-site comment in the CLI handler and name the test, so all
three tripwire locations reference one another rather than each carrying a partial version of the reasoning.

Append -- do not restructure or displace the existing firmware-sync caveat about the firmware's flag block
ending at this bit. That caveat is load-bearing for a different reason and must survive verbatim.

Do not change the constant's value. Do not add a new constant. Do not touch the stale-comment block near
the top of the file that carries the wrong dereference coordinates -- that is plan 132-08's edit, it is a
different block, and touching it here would split RETIRE-08's correction across two plans and two commits,
which D-12 requires be one commit with the evidence clause.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_revision_constants_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-14" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1.
    - `grep -c "test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on" /workspaces/firestarter_app/firestarter/constants.py` returns at least 1.
    - The existing firmware-sync caveat survives verbatim: `git -C /workspaces/firestarter_app diff HEAD~1 -- firestarter/constants.py | grep -cE "^-[^-]"` returns 0 -- the edit is pure addition, nothing was removed or reflowed.
    - The flag's value is unchanged: `python -c "from firestarter.constants import FLAG_SKIP_SDP_UNLOCK as f; assert f == 0x100, hex(f); print('FLAG VALUE OK')"` exits 0.
    - The stale-comment block is untouched by this commit: `git -C /workspaces/firestarter_app diff HEAD~1 -- firestarter/constants.py` shows changed lines only in the flag block's line range, and no changed line contains a dereference coordinate.
    - `python -m pytest tests/test_revision_constants_parity.py -q` reports zero failures -- that module asserts constant parity and is in a mypy strict island, so it is the natural early warning for a constants edit.
    - `python -m pytest tests/ -q` reports zero failures.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
  </acceptance_criteria>
  <done>The flag's comment block gained a pointer note naming the decision site and the test, nothing existing was removed or reflowed, the value is unchanged, and the block plan 132-08 owns was not touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The named test -- the half of the tripwire that fails when the default changes (RETIRE-07)</name>
  <files>firestarter_app/tests/test_write_skip_sdp_unlock.py</files>
  <read_first>
    - /workspaces/firestarter_app/tests/test_write_skip_sdp_unlock.py in full as it now stands (after plan 132-03's citation fixes and plan 132-05's typed delegate) -- its six existing tests, its long criterion-shaped naming convention, its chip constants, and its write-driver helper at :86 with its documented frame sequence
    - /workspaces/firestarter_app/firestarter/cli_handlers.py the auto-set block and the tripwire comment from task 1 (the behaviour the test pins)
    - /workspaces/firestarter_app/firestarter/constants.py the flag definition (the bit value the test asserts on the wire)
  </read_first>
  <behavior>
    - For a capability-refused part on the SDP protocol, a plain write with no flag passed results in the skip bit being set on the composed wire command dict, because the host auto-set it -- this is the dependency, asserted as behaviour.
    - The same write emits the unconditional report line announcing the auto-set, so the user is told.
    - For a capability-allowed part on the SDP protocol, a plain write does NOT set the skip bit -- so the test proves the auto-set is conditional rather than blanket, which is what makes it a discriminating test rather than a tautology.
    - The test's name and docstring state the removal-safety dependency explicitly, so the failure message a future developer sees names the decision they are about to invalidate.
  </behavior>
  <action>
Add one test to `tests/test_write_skip_sdp_unlock.py` named exactly
`test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on`. The name is not decoration -- it is
the record, and RETIRE-07 and ROADMAP criterion 5 both key on there being a test *named for the
dependency*. Do not shorten or rephrase it.

Its docstring is the second half of the record and must state four things: that phase 132 removed the
standalone SDP subcommand (RETIRE-01); that the removal was safe **because** the host auto-unlocks
capability-refused SDP-protocol parts by default on every write, so no user needs a manual unlock surface;
that this test pins that default-on behaviour, so if the default is ever flipped this test fails and the
removal decision must be revisited with it; and where the companion comments live (the decision site in the
CLI handler and the flag definition in the constants module). Cite D-14 and RETIRE-07.

Implementation: reuse the module's existing write-driver helper rather than re-authoring a runner
invocation -- it already drives a full successful write through a real operator on a fake serial port and
captures the composed wire command dict, and it already feeds the firmware acknowledgement frame the
skip-bit path requires. Drive a write for a capability-refused SDP-protocol part with **no** flag argument,
then assert the skip bit is set in the captured command dict's flags value and that the auto-set report line
appears in the output.

Then add the discriminating leg in the same test: drive the same helper for a capability-**allowed**
SDP-protocol part, again with no flag, and assert the bit is **not** set. Without this leg the test would
pass even if the host set the bit unconditionally, which would be a different and worse behaviour -- so the
leg is what makes the test prove the *conditional* default rather than merely a set bit. Use the module's
existing chip constants for both parts; do not introduce new ones.

Then prove the test is a real tripwire rather than an unreachable green. Temporarily invert the auto-set
condition in the CLI handler -- for instance make it require the flag to already be set, or negate the
capability check -- run the test, read the failure message, and confirm the failure text makes the
invalidated decision legible to someone who did not write it. If the message is opaque, improve the
assertion message before reverting. Then revert the plant and confirm the test passes and the tree is
clean. Record the verbatim failure output in the summary. This project has a recorded case of a
pre-authored gate that could never go red; a tripwire that has never been seen to fire is not a tripwire.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app; python -m pytest tests/test_write_skip_sdp_unlock.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on" /workspaces/firestarter_app/tests/test_write_skip_sdp_unlock.py` returns 1, with the name byte-exact.
    - The docstring names RETIRE-01, RETIRE-07 and D-14: `python -c "import re; s=open('/workspaces/firestarter_app/tests/test_write_skip_sdp_unlock.py').read(); d=re.search(r'def test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on.*?\"\"\"(.*?)\"\"\"', s, re.S).group(1); assert 'RETIRE-01' in d and 'RETIRE-07' in d and 'D-14' in d, d[:200]; print('DOCSTRING OK')"` exits 0.
    - `python -m pytest "tests/test_write_skip_sdp_unlock.py::test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on" -q` passes.
    - The test contains both legs: a refused-part assertion that the bit IS set and an allowed-part assertion that it is NOT, verifiable by the test body referencing both of the module's existing chip constants and asserting in both directions.
    - The RED demonstration is recorded in the summary: the exact condition inversion planted in the CLI handler, the test's verbatim failure output, a note on whether the failure message made the invalidated decision legible, and confirmation that `git status --porcelain` is clean after the revert.
    - `python -m pytest tests/ -q` reports zero failures.
    - `ruff check firestarter/ tests/` and `ruff format --check firestarter/ tests/` both exit 0.
    - `bash tools/ci_replica_venv.sh` reports a `mypy errors:` count no higher than plan 132-06's recorded count -- the new test did not add an error, which is the first live demonstration that plan 132-05's typed factory does its job.
    - `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py pyproject.toml` produces no output.
  </acceptance_criteria>
  <done>The named test exists with its byte-exact name, its docstring carries the full record, it asserts both the set bit for a refused part and the unset bit for an allowed part, it has been seen to fail on an inverted condition with a legible message, and it added no mypy error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host write path → the chip's protection state | The auto-unlock decision determines whether a protected part is unlocked before a write; changing it silently is the hazard the tripwire exists to catch. |
| a future developer's edit → this phase's removal argument | The tripwire is the only mechanism by which a later default change surfaces the invalidated removal decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-24 | Repudiation | the removal-safety argument | high | mitigate | The argument is recorded in three code locations that reference one another -- the decision site, the flag definition, and a test whose name and docstring carry it -- and the test has been seen to fail on an inverted condition. A sentence in a design note, which is what exists today, cannot fail. |
| T-132-25 | Tampering | comment placement that satisfies a grep but not the criterion | high | mitigate | Reachability is proven by enumerating every line a developer would edit to disable auto-unlock and recording its distance to the nearest tripwire comment, with a 10-line bound. D-14's criterion is reachability by construction, not the existence of a comment somewhere in the file. |
| T-132-26 | Elevation of privilege | accidental behaviour change on the write path | critical | mitigate | The comment diff is asserted to be comment-only -- every added or removed line must be a comment line -- and no echo or logger line may be added, enforcing D-05. The auto-set condition itself is unchanged, and the full suite passes. |
| T-132-27 | Spoofing | a tautological tripwire test | high | mitigate | The test carries a discriminating leg: an allowed part must NOT get the bit. Without it the test would pass under a blanket unconditional set, which is a different and worse behaviour. The RED demonstration additionally proves the test responds to the condition rather than to the bit's mere presence. |
| T-132-28 | Tampering | straying into the ring-fenced audit site | high | mitigate | The record's original coordinate pointed at the operations module, which is both mis-attributed and ring-fenced. An acceptance criterion asserts that module's diff is empty, so following the stale coordinate cannot pass. |
| T-132-SC | Tampering | package installs | high | accept | No package is installed or added; `pyproject.toml` is untouched and a criterion asserts it. The Package Legitimacy Gate's trigger condition is not met. |
</threat_model>

<verification>
1. All three comment-site greps return their expected counts, and the reachability enumeration with
   distances is recorded in the summary.
2. The comment-only diff assertion on the CLI handler returns 0 non-comment changed lines.
3. `cd /workspaces/firestarter_app && python -m pytest tests/ -q` reports zero failures.
4. The named test's RED demonstration output is recorded verbatim.
5. `cd /workspaces/firestarter_app && ruff check firestarter/ tests/` and
   `ruff format --check firestarter/ tests/` both exit 0.
6. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` reports a count no higher than plan
   132-06's.
7. `git -C /workspaces/firestarter_app diff --stat firestarter/eprom_operations.py pyproject.toml` is
   empty.
</verification>

<success_criteria>
- A tripwire comment sits at the host auto-unlock decision site, naming RETIRE-01 and the companion test;
  pointer comments sit at both default sites; a note is appended at the flag definition.
- Every line a developer would edit to disable auto-unlock is within 10 lines of a tripwire comment, and
  that enumeration is recorded.
- The named test exists with its byte-exact name, asserts both directions, and has been seen to fail on an
  inverted condition with a legible message.
- The diff on the CLI handler is comment-only; no output was added to the write path; the constants edit is
  pure addition.
- The mypy count did not rise, and `eprom_operations.py` and `pyproject.toml` are untouched.
- RETIRE-07 is marked Complete. No other RETIRE id is ticked or un-ticked.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-07-SUMMARY.md` when done.
State in it: the three comment sites with their line numbers, the reachability enumeration with distances,
the named test's verbatim RED output on the inverted condition and whether the message was legible, the
mypy count against plan 132-06's, and an explicit line reading "Requirements marked Complete: RETIRE-07".
</output>
