---
phase: 132-retire-dev-sdp-discharge-the-mypy-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firestarter_app/tools/ci_replica_venv.sh
  - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md
  - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md
autonomous: true
requirements: [RETIRE-06]
must_haves:
  truths:
    - "A committed, numpy-free CI-replica venv script exists in firestarter_app/tools/, separate from tools/ci_parity.sh (D-07), and any later plan can obtain a trustworthy mypy count by running it."
    - "The pre-change CI-parity baseline is recorded before any deletion or type fix lands (ROADMAP cross-cutting), so the after-run in plan 132-09 is a comparison rather than an assertion."
    - "The pre-change mypy count is a number this phase MEASURED today, not a number inherited from 131-CI-BASELINE.md (D-06, D-09)."
  artifacts:
    - firestarter_app/tools/ci_replica_venv.sh
    - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md
    - .planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md
  key_links:
    - "ci_replica_venv.sh must call tools/check_mypy_watermark.py (not a hand-rolled mypy invocation) so the count it reports is the same gate CI runs — D-06's whole premise."
    - "The venv lives under .venv/ci-replica, inside the already-gitignored .venv/, so a run never leaves untracked files in firestarter_app."
---

<!-- planner-discipline-allow: dev sdp -->
<!-- planner-discipline-allow: test_dev_sdp_cmd.py -->

<objective>
Build the measuring instrument and take the pre-change readings, before a single line of
`dev sdp` or a single mypy fix moves.

Purpose: every later plan in this phase asserts a mypy error count. Those assertions are only
worth anything if the count comes from an environment that can actually produce one. The
devcontainer's ambient `numpy 2.5.1` PEP-695 stub makes `tools/check_mypy_watermark.py` exit 2
(correctly refusing to report a count for a truncated run — Phase 131 GATE-01/02), so a
numpy-free Python 3.11 replica venv is the only local path to a number. D-07 requires that
recipe committed as a script rather than left as prose, because Phases 133–136 hit the same wall.
The ROADMAP's cross-cutting rule additionally requires the CI-parity recipe run *before* the
deletion+discharge, not only after.

Output: `firestarter_app/tools/ci_replica_venv.sh` (new, committed in the submodule), plus two
meta-repo record files carrying the pre-change readings.

May mark Complete: **NOTHING.** This plan touches RETIRE-06 but must not tick it — plan 132-09
owns RETIRE-06's Complete transition, after the certifying CI run. Explicitly forbidden here:
ticking RETIRE-06 because "the count was measured". A measured local count is not a green CI job.
</objective>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CONTEXT.md
@.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-PATTERNS.md
@.planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md
@.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md
</context>

<repo_map>
This is a meta-repo. Two git repositories are in play and **each commit lands in exactly one of
them**:

- `/workspaces/firestarter_app/` — the Python host CLI submodule. Branch MUST be
  `gsd/v1.30-sdp-surface-retirement` (note: NOT the meta-repo's longer branch name, and not
  `beta`, not `main`). Every path written `firestarter/…`, `tests/…`, `tools/…`, `pyproject.toml`
  in this plan is relative to this directory. **Task 2's commit lands here.**
- `/workspaces/` — the meta-repo, branch `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`.
  Holds only `.planning/` and `.claude/`. **Task 1's and task 3's record commits land here.**
- `/workspaces/firestarter/` — the Arduino firmware repo. **This phase does not touch it at all.**
</repo_map>

<artifacts_this_phase_produces>
Symbols and files created by phase 132 as a whole (the source-grounding pass excludes these from
drift verification — they do not exist yet at plan time):

**New modules / files**
- `firestarter_app/firestarter/sdp_honesty.py` — new production module (plan 132-02)
- `firestarter_app/tools/ci_replica_venv.sh` — new tooling script (this plan)
- `firestarter_app/tests/test_sdp_honesty.py` — from `git mv` of the old dev-sdp test module (plan 132-03)

**New functions in `firestarter/sdp_honesty.py`** (plan 132-02)
- `unreadable_state_caveat() -> str`
- `emission_summary(mode: str, chip_name: str) -> str`
- `map_unknown_cmd_to_outdated(exc, mode, chip_name) -> FirmwareOutdatedError | None`

**New symbols in `tests/conftest.py`** (plan 132-05)
- `make_app_context(...) -> AppContext` — typed keyword factory
- `app_context` — thin pytest fixture wrapping it

**New tests**
- `test_sdp_honesty_module_imports_only_leaf_firestarter_modules` in `tests/test_sdp_honesty.py` (plan 132-03)
- `test_command_names_dereferences_both_sdp_commands` in `tests/test_revision_constants_parity.py` (plan 132-08)
- `test_dev_sdp_removal_is_safe_only_because_auto_unlock_is_default_on` in `tests/test_write_skip_sdp_unlock.py` (plan 132-07)

**New config entries**
- `"firestarter.sdp_honesty"` appended to `pyproject.toml`'s Phase-42 strict-island `module` list (plan 132-02)

**New constants:** none. `constants.py` gains no new name in this phase.

**New meta-repo records** (all under `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/`)
- `132-CI-PARITY.md`, `132-MYPY-LEDGER.md` (this plan; appended by 132-06 and 132-09)
- `132-PRUNE-LEDGER.md` (plan 132-03)
- `132-CI-GREEN.md`, `132-RECORD.md` (plan 132-09)
</artifacts_this_phase_produces>

<measured_ground_truth>
Measured at plan time, 2026-08-03, against `firestarter_app` @ `8caf77f` on branch
`gsd/v1.30-sdp-surface-retirement`, in a throwaway numpy-free Python 3.11.15 venv with
mypy 2.3.0. These are **planner measurements, restated here so the executor can confirm rather
than discover**. Where they differ from CONTEXT.md or 132-PATTERNS.md, the measured value wins
(D-11's own instruction).

- `tools/check_mypy_watermark.py` in the replica venv prints, verbatim:
  `checked 121 source files` / `mypy errors: 69 (watermark: 35)` /
  `FAIL: 69 errors exceeds watermark 35. New errors introduced.`
- Raw mypy summary line: `Found 69 errors in 17 files (checked 121 source files)`.
- **121, not 120.** `MIN_CHECKED_SOURCE_FILES = 120` (check_mypy_watermark.py:48) is therefore
  satisfied with one file of margin, and this phase ADDS two files (net +2), so the floor rises
  away from the boundary rather than toward it. Phase 131 D-05's "verify, do not assume" is
  discharged: **no floor edit is needed or permitted in this phase.**
- 69 agrees exactly with 131-CI-BASELINE.md's CI reading — no divergence to reconcile.
- Error population by file: `firestarter/eprom_operations.py` 10 (**ring-fenced**),
  `tests/test_dev_test_cmd.py` 9, `tests/test_write_skip_sdp_unlock.py` 7,
  `tests/test_write_skip_erase_0x0d.py` 6, `tests/test_validate_family_cmd.py` 6,
  `tests/test_dev_sdp_cmd.py` 6, `firestarter/database.py` 6, `tests/test_serial_comm.py` 3,
  `tests/test_revision_constants_parity.py` 3, `firestarter/firmware.py` 3,
  `firestarter/config.py` 3, `firestarter/ic_layout.py` 2, and one each in
  `tests/test_provenance.py`, `tests/test_protocol_not_implemented_production_path.py`,
  `tests/test_eprom_database.py`, `tests/test_characterization.py`, `firestarter/submit.py`.
  Sum = 69.
- By error code: 39 `[arg-type]`, 10 `[union-attr]`, 7 `[assignment]`, 6 `[var-annotated]`,
  4 `[attr-defined]`, 3 `[func-returns-value]`. The 28 `[annotation-unchecked]` lines mypy also
  emits are **notes, not errors**, and are excluded from the 69.
- **PATTERNS.md correction:** the six `[var-annotated]` errors are at `firestarter/config.py:84`,
  `:85`, `:102` and `firestarter/database.py:174`, `:175`, `:325`. They are **not** in
  `ic_layout.py` — `ic_layout.py:233` produces no error at all, and `ic_layout.py`'s two errors
  are `:447 [attr-defined]` and `:630 [arg-type]`, neither cheap. CONTEXT.md's `174/175` was right
  and PATTERNS.md's `173/174` was wrong.
- Projected path to green (each step measured or arithmetic from measured values):
  69 − 6 (the retargeted module needs no `AppContext` at all) − 25 (the four surviving factories:
  6 + 6 + 6 + **7** — `tests/test_write_skip_sdp_unlock.py:72` carries a seventh error,
  `Argument 1 to "EpromOperator" has incompatible type "object"`) − 6 (`[var-annotated]`)
  = **32**, versus watermark 35 → 3 of headroom. Research projected 33; the measured seventh error
  makes it 32. **This is a projection, not a claim.** Plan 132-06 measures the real post-fix count
  and plan 132-09 reads CI's.
</measured_ground_truth>

<tasks>

<task type="auto">
  <name>Task 1: Preconditions, then record the pre-change CI-parity baseline</name>
  <files>.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md</files>
  <read_first>
    - /workspaces/firestarter_app/tools/ci_parity.sh (all 162 lines — the four legs, the leg-4 expected-exit-2 header, the banner/summary/aggregate-exit shape task 2 mirrors)
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CI-PARITY.md (what the four legs prove, and the recorded local exit-2 explanation)
    - /workspaces/firestarter_app/.github/workflows/ci.yml (the `ci` job's step list — the thing being mirrored)
  </read_first>
  <action>
Establish the preconditions and take the "before" reading.

Preconditions, each verified and recorded, and each a hard stop on failure:
(a) `git -C /workspaces/firestarter_app branch --show-current` is `gsd/v1.30-sdp-surface-retirement`.
    If it reports `beta` or `main`, STOP and report — do not check out anything.
(b) `git -C /workspaces branch --show-current` is
    `gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof`.
(c) `git -C /workspaces/firestarter_app status --porcelain` is empty (a dirty tree makes a
    baseline meaningless).
(d) Record whether a programmer board is attached, because it changes what leg 1/2 prove: the
    recorded trap is that `test_no_programmer_found_*` go RED when a live `/dev/ttyACM*` beats the
    `comports=[]` patch. `tools/ci_parity.sh` already stamps `BOARD-ATTACHED:` — read that stamp
    from its output rather than probing separately.

Then run `bash tools/ci_parity.sh` from the app root, capture the complete output, and write
`.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md` with a
`## 1. Before (pre-deletion, pre-discharge)` section containing: the four `Leg N exit code:` lines
verbatim, the `BOARD-ATTACHED:` and `Python:` stamps verbatim, the aggregate `CI-PARITY:` line,
the app-repo HEAD sha, and a plain statement of which legs passed and which did not.

Leg 4 is expected to exit 2 in this devcontainer, and that is CORRECT behaviour, not a defect:
the hardened watermark gate refuses to report a count for a numpy-truncated run. Do not weaken a
guard, add `|| true`, or drop leg 4 to force a zero aggregate exit. Record the non-zero aggregate
exit as the expected pre-change shape and say why, quoting `ci_parity.sh`'s own leg-4 header
reasoning. Leave `tools/ci_parity.sh` byte-unchanged — D-07 forbids folding the venv recipe into
it, and this plan's contract is to run it, not edit it.

Also record, in the same file, the one gap the parity recipe does NOT cover and that this phase
must therefore close by other means: `ci.yml` runs pytest as
`pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`, whereas parity
legs 1 and 2 run `pytest tests/ -q` with no coverage gate. The 70% floor is a real `ci` step that
this phase can move (it deletes ~126 lines of well-covered production code plus ~550 lines of its
tests), and nothing in the existing recipe would catch a drop below it. Task 2's script closes
that gap.

Commit the record in the **meta-repo**.
  </action>
  <verify>
    <automated>test -f /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md &amp;&amp; grep -c "Leg 4 exit code:" /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-CI-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /workspaces/firestarter_app branch --show-current` printed `gsd/v1.30-sdp-surface-retirement`, and that exact string is quoted in 132-CI-PARITY.md.
    - 132-CI-PARITY.md contains a `## 1. Before` section with four distinct lines matching `Leg [1-4] exit code: [0-9]+`, copied verbatim from the run.
    - 132-CI-PARITY.md contains the run's `BOARD-ATTACHED:` line and `Python:` line verbatim, and the aggregate line matching `CI-PARITY: (PASS|FAIL)`.
    - 132-CI-PARITY.md states the app-repo HEAD sha the baseline was taken at, and that sha matches `git -C /workspaces/firestarter_app rev-parse --short HEAD` at the time of the run.
    - 132-CI-PARITY.md names the coverage-gate gap explicitly, quoting `--cov-fail-under=70` and stating that parity legs 1 and 2 do not include it.
    - `git -C /workspaces/firestarter_app diff --stat tools/ci_parity.sh` produces no output (the recipe was run, not edited).
    - The record commit is in the meta-repo: `git -C /workspaces log -1 --name-only` lists only paths under `.planning/`.
  </acceptance_criteria>
  <done>The pre-change parity reading is on disk in the meta-repo, with all four leg exits, both stamps, the aggregate exit, the HEAD sha, and the named coverage-gate gap. `tools/ci_parity.sh` is unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author and commit tools/ci_replica_venv.sh — the numpy-free CI replica (D-07)</name>
  <files>firestarter_app/tools/ci_replica_venv.sh</files>
  <read_first>
    - /workspaces/firestarter_app/tools/ci_parity.sh (the header sections `WHAT THIS IS FOR` / `WHAT THIS DELIBERATELY DOES NOT MIRROR` / `Exit codes:`, the `set -u`-not-`set -e` choice, `BASH_SOURCE`-anchored `REPO_ROOT`, `mktemp -d` + `trap cleanup EXIT`, the `banner()` function, the non-swallowing `LEGn_EXIT=$?` capture, and the summary/stamp/aggregate-exit block)
    - /workspaces/firestarter_app/tools/check_mypy_watermark.py (`REPO_ROOT`, `MIN_CHECKED_SOURCE_FILES`, `get_watermark`, `mypy_argv`, `run_mypy`, `classify_mypy_result`, `enforce_watermark`, `main` — the script must invoke this gate, not re-implement it)
    - /workspaces/firestarter_app/.github/workflows/ci.yml (the `ci` job: Python 3.11, `pip install -e .[test]`, `ruff check firestarter/ tests/`, `ruff format --check firestarter/ tests/`, `python tools/check_mypy_watermark.py`, `pytest tests/ --cov=firestarter --cov-report=term-missing --cov-fail-under=70`)
    - /workspaces/firestarter_app/.gitignore (confirm `.venv/` is ignored — line 4)
  </read_first>
  <behavior>
    - Given no venv exists, the script creates one at `.venv/ci-replica` and exits 0 with all legs passing on a clean tree.
    - Given the venv already exists, a second run reuses it (no reinstall unless `--refresh` is passed) so iteration over ~30 mypy errors costs seconds, not minutes.
    - Given `python3.11` is unavailable, the script still runs but prints an explicit `INTERPRETER-DIVERGENCE:` line naming the interpreter actually used versus CI's 3.11 — it never silently substitutes.
    - Given `numpy` is importable inside the venv, the script fails that leg loudly rather than reporting a mypy count, because a count obtained with numpy present is the truncated-run shape the whole script exists to avoid.
    - Given a leg fails, every other leg still runs and the aggregate exit names the failing legs.
  </behavior>
  <action>
Create `tools/ci_replica_venv.sh` in `firestarter_app`, a new script **separate from**
`tools/ci_parity.sh` (D-07: that script's contract is "faithful CI mirror, neither wider nor
narrower", and a local venv substitute is not a CI step; it also shipped 9 commits ago and its
leg-4 header documents the local exit 2 as correct).

Concrete requirements:

Header block, mirroring `ci_parity.sh:1-56`'s style: a first line of the form
`# tools/ci_replica_venv.sh -- <purpose> (RETIRE-06, phase 132, D-06/D-07).`, then all-caps
sections: `WHAT THIS IS FOR`, `WHY THIS IS NOT A LEG OF ci_parity.sh`, `WHAT THIS DELIBERATELY
DOES NOT MIRROR`, and a closing `Exit codes:` contract. In `WHAT THIS IS FOR`, state the measured
fact that motivates it: in this devcontainer the ambient numpy PEP-695 stub makes mypy exit 2 and
the hardened gate correctly refuses to report a count, so a numpy-free interpreter is the only way
to obtain one locally. In `WHAT THIS DELIBERATELY DOES NOT MIRROR`, name the four codegen/catalog
`ci` steps and the `pip install -e . && firestarter --help` smoke step, with the reason: they need
no venv and no phase-132 change can affect them (this phase edits no catalog and no generated
file).

Shell shape: `set -u` and deliberately NOT `set -e` (a failing leg must not abort the run);
`SCRIPT_DIR` from `BASH_SOURCE`; `REPO_ROOT` one parent up; `cd "$REPO_ROOT"` with a FATAL guard.

Venv location: default `VENV_DIR="${REPO_ROOT}/.venv/ci-replica"`, overridable by the
`CI_REPLICA_VENV` environment variable read with an `is set` test (`${CI_REPLICA_VENV:-}` then a
`-n` check), never a truthiness test. `.venv/` is already in `.gitignore`, so nothing a run
creates can appear as untracked — assert that with a `git status --porcelain` check inside the
script and fail the leg if it is non-empty at exit.

Interpreter selection: prefer the first of `python3.11` on PATH, then
`/home/vscode/.local/bin/python3.11`, then `python3`. Print `INTERPRETER: <path> <version>` always,
and additionally print `INTERPRETER-DIVERGENCE: using <ver>, CI uses 3.11` when the resolved minor
version is not 3.11. State in the header why the divergence matters little but is still stamped:
mypy's error population is driven by `python_version = "3.10"` in `pyproject.toml`, which is a
*target*, not the running interpreter — but dependency resolution can still differ, so the stamp
is required rather than a reassurance.

Cache: export `PIP_CACHE_DIR` to a directory inside the script's `mktemp -d` scratch root before
any pip call, because the recorded devcontainer failure mode is a cache write to an unwritable
`$HOME/.cache` (`Permission denied`) aborting the install.

Legs, each with a `banner N "<command>" "<what it proves>"` and a non-swallowing `LEGn_EXIT=$?`:
1. Create-or-reuse the venv, then `"$VENV_DIR/bin/python" -m pip install -e '.[test]'` (skipped
   with a printed `REUSED` line when the venv exists and `--refresh` was not passed).
2. Prove `numpy` is absent: run `importlib.util.find_spec("numpy")` inside the venv and require
   `None`. A present numpy fails this leg and the script says why in one sentence.
3. `"$VENV_DIR/bin/ruff" check firestarter/ tests/` then
   `"$VENV_DIR/bin/ruff" format --check firestarter/ tests/` — CI's exact two path arguments,
   neither wider nor narrower.
4. `"$VENV_DIR/bin/python" tools/check_mypy_watermark.py` — the gate itself, so the number
   reported is the number CI's gate would report. Do not call mypy directly for the count.
5. `"$VENV_DIR/bin/python" -m pytest tests/ --cov=firestarter --cov-report=term-missing
   --cov-fail-under=70` — CI's exact pytest invocation, closing the coverage-floor gap task 1
   recorded.

Stamps in the summary block, mirroring `ci_parity.sh:124-162`: `INTERPRETER:`,
`MYPY-VERSION:` (resolved from `python -m mypy --version` inside the venv),
`NUMPY-PRESENT: no|yes`, `Python:`, then per-leg statuses and an aggregate
`CI-REPLICA: PASS` / `CI-REPLICA: FAIL (legs:...)` with a non-zero exit on failure.

Also print, unconditionally and clearly labelled, the two lines a later plan must quote:
the gate's `mypy errors: N (watermark: M)` line and mypy's own
`Found N errors in M files (checked K source files)` line. To surface the second without
re-running mypy or switching to `--output json` (STACK §1 forbids JSON mode: it emits no summary
line and would destroy the `checked K` clause D-08 depends on), have the script run the gate's
pure seam — import `run_mypy` and `classify_mypy_result` from `tools/check_mypy_watermark.py` in a
short inline `python -c` and echo the raw summary line — rather than duplicating mypy's argv.

Commit in the **submodule** with a message naming RETIRE-06 and D-07, e.g.
`feat(132-01): numpy-free CI-replica venv script (RETIRE-06, D-07)`. Do NOT touch
`pyproject.toml`, `.gitignore`, or `tools/ci_parity.sh` in this commit.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; bash tools/ci_replica_venv.sh 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test -x /workspaces/firestarter_app/tools/ci_replica_venv.sh` succeeds, or the file is present and runs under `bash tools/ci_replica_venv.sh`.
    - The script's output contains a line matching `NUMPY-PRESENT: no`.
    - The script's output contains a line matching `mypy errors: [0-9]+ \(watermark: 35\)`.
    - The script's output contains a line matching `Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)` — the `checked K` clause is present, not absent (Phase 131 F-07's failure shape must not recur).
    - The script's output contains a line matching `INTERPRETER: ` and a line matching `MYPY-VERSION: `.
    - The script's output contains five `Leg [1-5]` banners and five `Leg [1-5] exit code:` lines, and a final line matching `CI-REPLICA: (PASS|FAIL)`.
    - A second consecutive run without `--refresh` prints a `REUSED` line and completes without reinstalling (observable as a materially shorter wall time and no `Successfully installed` line).
    - `grep -c "output json" /workspaces/firestarter_app/tools/ci_replica_venv.sh` returns 0 — the script never switches mypy to JSON mode.
    - `git -C /workspaces/firestarter_app status --porcelain` is empty after a run — the venv left no untracked files.
    - `git -C /workspaces/firestarter_app diff --stat tools/ci_parity.sh pyproject.toml .gitignore` produces no output.
    - `git -C /workspaces/firestarter_app log -1 --name-only` lists `tools/ci_replica_venv.sh` and nothing else.
  </acceptance_criteria>
  <done>The script is committed in the submodule, runs five legs, proves numpy absent, reports the watermark gate's count AND mypy's `checked K` clause, stamps interpreter and mypy version, and leaves the tree clean.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured pre-change mypy baseline in 132-MYPY-LEDGER.md</name>
  <files>.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md</files>
  <read_first>
    - /workspaces/.planning/phases/131-gate-hardening-ci-parity/131-CI-BASELINE.md (§4 the verbatim CI gate output, §5 the F-07 absent-`checked`-clause correction, §6 mypy 2.3.0 / Python 3.11.15, §7 "this number is an input to Phase 132")
    - /workspaces/firestarter_app/tools/check_mypy_watermark.py (`MIN_CHECKED_SOURCE_FILES = 120` and its comment: lowered only in the same commit as a legitimate deletion, with the new measured number)
  </read_first>
  <action>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md` — the
single file this phase appends its three mypy readings to (this plan's pre-change reading, plan
132-06's post-fix reading, plan 132-09's certifying CI reading). Write section 1 only; leave
explicit `<!-- 132-06 appends here -->` and `<!-- 132-09 appends here -->` placeholders.

Section 1 must contain, all read from task 2's run and none of it computed:
- The gate's `mypy errors: N (watermark: M)` line verbatim.
- The gate's `checked K source files` line verbatim, and mypy's own
  `Found N errors in M files (checked K source files)` line verbatim.
- `INTERPRETER:`, `MYPY-VERSION:`, `NUMPY-PRESENT:` stamps verbatim.
- The app-repo HEAD sha and branch name.
- The per-file error distribution and the per-code distribution, as counted from the run's output.
- An explicit statement that the 28 `[annotation-unchecked]` lines are mypy **notes**, not errors,
  and are excluded from the count — so a later reader does not "find" 97 and think the ledger is wrong.

Then a `## 2. Divergence check against Phase 131's inherited 69` section: state whether this
phase's measured count agrees with 131-CI-BASELINE.md's CI reading of 69. If it agrees, say so and
say there is nothing to reconcile. If it does not, **the measured number wins**, and both are
recorded without reconciliation (Phase 131 D-12's rule).

Then a `## 3. The checked-source-files floor` section: record the measured `checked K` value
against `MIN_CHECKED_SOURCE_FILES = 120`, and state the conclusion for this phase explicitly —
this phase adds two files (`firestarter/sdp_honesty.py`, `tests/test_sdp_honesty.py`) and net-removes
none, so the checked count rises and **no floor edit is needed or permitted**. This discharges
Phase 131 D-05's "a `git mv` holds the count at 120 — verify" as a verified fact rather than an
assumption.

Then a `## 4. Projected path to the watermark (a projection, not a claim)` section: the arithmetic
from the measured distribution to the projected post-fix count, with each subtraction attributed to
the plan that performs it (132-03 the retargeted module's factory; 132-05 the four surviving
factories; 132-06 the six `[var-annotated]`). Label it unambiguously as a projection and name
plan 132-06 as the plan that measures the real number and 132-09 as the plan that reads CI's.

Finally a `## 5. What this document does not establish` section, in the register of
131-CI-BASELINE.md §7 and §9: a locally-measured count is not a green CI job; `firestarter_app`'s
primary `ci` job is RED at the start of this phase by design; nothing here is a claim that anything
is green. Also state D-09's accepted cost plainly: the watermark stays at 35 and is not ratcheted
in this phase, so once the count lands at the projected 32 there are 3 of silent headroom in what
Phase 131 D-04 called the milestone's central honesty artifact — an accepted, stated cost whose
actual defence is plan 132-05's typed factory, not a tight watermark.

Commit in the **meta-repo**.
  </action>
  <verify>
    <automated>grep -cE "Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)" /workspaces/.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-MYPY-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - 132-MYPY-LEDGER.md exists and contains a line matching `mypy errors: [0-9]+ \(watermark: 35\)`.
    - 132-MYPY-LEDGER.md contains a line matching `Found [0-9]+ errors in [0-9]+ files \(checked [0-9]+ source files\)`.
    - 132-MYPY-LEDGER.md contains the literal string `annotation-unchecked` together with the word `notes`, in a sentence stating they are excluded from the count.
    - 132-MYPY-LEDGER.md contains both placeholder comments `132-06 appends here` and `132-09 appends here`.
    - 132-MYPY-LEDGER.md states a `checked` value and compares it against `120`, and concludes in words that no floor edit is needed.
    - 132-MYPY-LEDGER.md contains the word `projection` (or `projected`) in the heading or first sentence of the arithmetic section, and names plans 132-06 and 132-09 as the measuring/reading owners.
    - 132-MYPY-LEDGER.md states that the watermark stays at 35 and is not ratcheted in this phase.
    - `git -C /workspaces/firestarter_app diff --stat pyproject.toml` produces no output — the watermark comment is untouched.
    - `git -C /workspaces log -1 --name-only` lists only paths under `.planning/`.
  </acceptance_criteria>
  <done>132-MYPY-LEDGER.md section 1 carries the measured pre-change reading with both summary lines verbatim, the divergence check against 69, the verified `checked`-floor conclusion, a clearly-labelled projection, and the stated D-09 headroom cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → the replica venv | `pip install -e '.[test]'` inside the new script resolves and executes third-party package code (setuptools, mypy, ruff, pytest, coverage) in this environment. |
| Local filesystem → the script | `CI_REPLICA_VENV` is an environment-controlled path the script creates and deletes into. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-SC | Tampering | `pip install -e '.[test]'` in `tools/ci_replica_venv.sh` | high | accept | **No new package is introduced by this phase.** The `[test]` extra's dependency closure is unchanged: the only `pyproject.toml` edit in phase 132 is a `[[tool.mypy.overrides]]` module-list insertion (plan 132-02), and the criterion `git diff pyproject.toml` touching no `dependencies` / `optional-dependencies` line is enforced there. The Package Legitimacy Gate's trigger condition — a package of unverified provenance being added — is therefore not met, and no `[ASSUMED]`/`[SUS]` human-verify checkpoint is required. Every package installed is one CI already installs on every run. Recorded rather than silently skipped. |
| T-132-01 | Tampering | `CI_REPLICA_VENV` env override + `rm -rf` cleanup | medium | mitigate | Cleanup `rm -rf` is scoped to the script's own `mktemp -d` scratch root, never to `VENV_DIR`; the venv is removed only on an explicit `--refresh`, and only after confirming the path resolves under `REPO_ROOT`. The env seam is read with an `is set` test, never truthiness. |
| T-132-02 | Information disclosure | the run's captured output pasted into `.planning/` records | low | accept | The output carries interpreter paths and package versions only. No credential, no token, and no `gh` authentication material is read by any leg. |
| T-132-03 | Spoofing | which `mypy` binary produces the count | medium | mitigate | Leg 4 invokes `tools/check_mypy_watermark.py` with the venv's own interpreter, and that script's `mypy_argv()` uses `sys.executable -m mypy` (Phase 131 GATE-04) — never a bare `mypy` from PATH. The resolved version is stamped as `MYPY-VERSION:` so the count is attributable. |
</threat_model>

<verification>
1. `cd /workspaces/firestarter_app && bash tools/ci_replica_venv.sh` exits 0 on the clean
   pre-change tree, or exits non-zero naming only leg 4 (the watermark gate, RED at 69 > 35 by
   design at this point in the phase). Any other failing leg is a defect in this plan's work.
2. `cd /workspaces/firestarter_app && bash tools/ci_parity.sh` reproduces the four leg exits
   recorded in 132-CI-PARITY.md §1.
3. `git -C /workspaces/firestarter_app status --porcelain` is empty.
4. `git -C /workspaces/firestarter_app log --oneline -1` shows exactly one new commit touching
   only `tools/ci_replica_venv.sh`.
5. Both meta-repo records exist and satisfy their acceptance criteria greps.
</verification>

<success_criteria>
- `firestarter_app/tools/ci_replica_venv.sh` is committed in the submodule, on branch
  `gsd/v1.30-sdp-surface-retirement`, and produces a trustworthy mypy count with the
  `checked K source files` clause present.
- `tools/ci_parity.sh`, `pyproject.toml` and `.gitignore` are byte-unchanged.
- `132-CI-PARITY.md` §1 and `132-MYPY-LEDGER.md` §1–§5 are committed in the meta-repo.
- No RETIRE requirement is marked Complete by this plan.
</success_criteria>

<output>
Create `.planning/phases/132-retire-dev-sdp-discharge-the-mypy-debt/132-01-SUMMARY.md` when done.
State in it: the four parity leg exits, the measured mypy count and `checked` value, the resolved
interpreter and mypy version, and an explicit line reading "Requirements marked Complete: none
(RETIRE-06 is owned by plan 132-09)".
</output>
