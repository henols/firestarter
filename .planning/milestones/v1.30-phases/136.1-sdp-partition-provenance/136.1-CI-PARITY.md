# Phase 136.1 — CI-Parity / mypy-Watermark Record

Three-section record (Before / After-PROV-01 / After-phase-close), same shape as
`136-CI-PARITY.md`. This phase INHERITS a starting point (unlike Phase 136, which had
none) — every section below still measures fresh rather than copying an inherited number
uncritically.

---

## Before (pre-edit)

Measured 2026-08-05, from `/workspaces/firestarter_app`, on branch
`gsd/v1.30-sdp-surface-retirement`, before any file this plan owns was edited.

### 1. `tools/ci_parity.sh` — every leg's exit status, verbatim

```
CI-parity recipe (GATE-09) -- repo root: /workspaces/firestarter_app

Leg 1 (pytest, empty sibling root):  exit 0
Leg 2 (pytest, sibling present):     exit 0
Leg 3 (ruff check + format --check): exit 0
Leg 4 (mypy watermark gate):         exit 2
BOARD-ATTACHED: none
Python: Python 3.12.13
CI-PARITY: FAIL (legs:4)
```

Leg 4's local exit 2 is the documented, expected devcontainer shape (ambient numpy
PEP-695 stub truncates mypy at Python 3.12) — `ci_parity.sh`'s own leg-4 banner and
`131-CI-PARITY.md` both name this as the hardened gate correctly refusing to report a
count for an incomplete run, not a script defect. `tools/ci_replica_venv.sh` (below) is
the only local path to a real mypy count.

### 2. `tools/ci_replica_venv.sh` — all five legs + the two certifying lines

```
INTERPRETER: /home/vscode/.local/bin/python3.11 Python 3.11.15
Leg 1 (venv create-or-reuse + install): exit 0
Leg 2 (numpy absent):                   exit 0
Leg 3 (ruff check + format --check):    exit 0
Leg 4 (mypy watermark gate):             exit 0
Leg 5 (pytest --cov, CI's exact args):   exit 0
CI-REPLICA: PASS
```

**Certifying lines, verbatim:**

```
Found 33 errors in 13 files (checked 130 source files)
checked 130 source files
mypy errors: 33 (watermark: 35)
```

Leg 5's full suite: **1494 passed, 1 warning** in 250.31s, coverage **82.14%** against the
70% floor (30 snapshots passed).

### 3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — tail -5

```
--------------------------- snapshot report summary ----------------------------
30 snapshots passed.
1494 passed, 1 warning in 214.61s (0:03:34)
```

### Comparison against `136-CI-PARITY.md`'s own "## After" (mypy 33/35, checked 130,
1494 passed, coverage 82.14%)

**Matches exactly, on every axis measured** — mypy errors 33 (watermark 35, headroom 2),
checked 130 source files, full suite 1494 passed / 0 failed, 30 snapshots passed,
coverage 82.14%. No delta between Phase 136's own close-of-phase measurement and this
phase's independently re-measured baseline. This is expected (no file changed between
136-04's own commit and this task's measurement) but is stated here as a **measured**
fact, not assumed uncritically from the inherited record — per this task's own
instruction to re-measure fresh.

### Observed board state

`ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null` → no matches (no board attached). `ci_parity.sh`
has no no-board leg; this is an ambient observation, never a claimed leg.

### Python-version note

The devcontainer runs Python 3.12.13 (see `tools/ci_parity.sh`'s own "Python:" stamp
above); CI runs 3.9/3.11. Only the `ci_replica_venv.sh` numbers (measured from a real
Python 3.11.15 interpreter, numpy-absent) are quotable as a CI-comparable mypy/lint
count; `ci_parity.sh`'s own leg 4 in this devcontainer is structurally unable to produce
one (exit 2, not a count).

---

## After PROV-01 (DB regen only)

Measured 2026-08-05, from `/workspaces/firestarter_app`, after commits `f294821` (feat:
build_db.py decode + chip_database.json regeneration) and `8fccb47` (fix: GATE-02
`diff_db.py` PROV01_PROTECT_METADATA rule — see this task's own deviation note below).

**Expectation, stated explicitly and grounded in `ci.yml:63-70`'s own path scoping:**
this task's production edits are inside `tools/` only (`build_db.py`, `diff_db.py`) plus
the data file `firestarter/data/chip_database.json` — none of which mypy
(`mypy firestarter/ tests/`, GATE-04) or ruff (`ruff check firestarter/ tests/`) scan in
CI. The mypy count and the `checked N source files` count are therefore expected to be
**IDENTICAL** to "## Before". This task adds and edits zero test files, so the full
suite's passed count is expected to be **EXACTLY** the "## Before" count (0 delta), not
merely "greater than or equal."

### 1. `tools/ci_parity.sh` — every leg's exit status, compared against "## Before"

```
Leg 1 (pytest, empty sibling root):  exit 0
Leg 2 (pytest, sibling present):     exit 0
Leg 3 (ruff check + format --check): exit 0
Leg 4 (mypy watermark gate):         exit 2
BOARD-ATTACHED: none
Python: Python 3.12.13
CI-PARITY: FAIL (legs:4)
```

**Identical to "## Before"** on every leg. (An interim run taken between the regen commit
and the `diff_db.py` fix commit briefly showed legs 1/2 at exit 1 — `test_diff_db_gate.py`
red, a real regression this task caused and fixed in the same task, documented in the
Deviations section below. This is the post-fix, final state.)

### 2. `tools/ci_replica_venv.sh` — all five legs + the two certifying lines

```
Leg 1 (venv create-or-reuse + install): exit 0
Leg 2 (numpy absent):                   exit 0
Leg 3 (ruff check + format --check):    exit 0
Leg 4 (mypy watermark gate):            exit 0
Leg 5 (pytest --cov, CI's exact args):  exit 0
CI-REPLICA: PASS
```

**Certifying lines, verbatim:**

```
Found 33 errors in 13 files (checked 130 source files)
checked 130 source files
mypy errors: 33 (watermark: 35)
```

**Confirmed by a real run, not assumed a priori:** mypy errors 33 (watermark 35),
checked 130 source files — **IDENTICAL** to "## Before". `tools/` (including the two
files this task edited) is outside mypy's and ruff's CI-scanned paths, exactly as
expected.

Leg 5's full suite: **1494 passed, 1 warning** in 224.79s, coverage **82.14%** (30
snapshots passed) — **identical to "## Before"**, 0 delta.

### 3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — tail -5

```
--------------------------- snapshot report summary ----------------------------
30 snapshots passed.
1494 passed, 1 warning in 236.92s (0:03:56)
```

**1494 passed — EXACTLY the "## Before" count, 0 delta**, as expected (this task adds
and edits zero test files).

### 4. Board state and firmware-submodule confirmation

`ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null` → no matches (unchanged from "## Before"; no
no-board leg exists, ambient observation only).

`git -C /workspaces/firestarter status --porcelain` → empty. No file under
`/workspaces/firestarter/` (the firmware submodule) was touched at any point in this
task — v1.30 is host-only. No new pip/npm/cargo package was installed at any point in
this task.

### Headroom delta against "## Before"

**0.** `## Before` measured `mypy errors: 33 (watermark: 35)`, headroom 2; this
`## After PROV-01` measurement is the identical `33 (watermark: 35)`, headroom 2 — flat,
as reasoned above from `tools/`'s CI-scope exclusion.

### Summary table

| Metric | Before | After PROV-01 | Delta |
|---|---|---|---|
| mypy errors (watermark 35) | 33 | 33 | **0** |
| checked source files | 130 | 130 | **0** |
| full suite (`pytest tests/`) | 1494 passed | 1494 passed | **0** |
| coverage | 82.14% | 82.14% | **0.00 pp** |
| ruff (check + format) | clean | clean | **0** |

### Deviation this task's regeneration caused, fixed in the same task

`tests/test_diff_db_gate.py::TestDiffDbGate::test_diff_db_identity_pass` (GATE-02, an
existing per-chip diff gate comparing `chip_database.json` against a pinned baseline)
went RED immediately after the regeneration commit, reporting all 744 upstream-decoded
chips as "unexplained diffs" — it had no root-cause rule recognizing the three new
`protect_off_before`/`protect_on_after`/`infoic_page_size_raw` keys. Fixed (Rule 1 —
auto-fix, directly caused by this task's own change) by adding a `PROV01_PROTECT_METADATA`
rule to `tools/diff_db.py`, following the file's own established pattern (the same shape
as `PGSZ_PAGE_SIZE`, `RC1_DIP32_27C020`, etc. added by prior phases). `tools/diff_db.py`
is NOT in this plan's `files_modified` frontmatter list — it is a genuine, measured blast-
radius consequence of the chip_database.json regeneration (hard requirement 8's "measure,
don't assume" instruction), not a file this plan set out to touch. See
`136.1-01-SUMMARY.md`'s own Deviations section for the full account and commit hash.

---

## After (phase close)

Measured 2026-08-05, from `/workspaces/firestarter_app`, on branch
`gsd/v1.30-sdp-surface-retirement`, HEAD `31b5d74` (Plan `136.1-03`'s last submodule commit — this
plan, `136.1-04`, makes no submodule commit at all, per its own `<repo_topology>`: pure verification
capstone). This section closes the phase: every plan's own file-scoped edits (`136.1-01`'s
`build_db.py`/`chip_database.json`/`diff_db.py`, `136.1-02`'s `test_sdp_db_invariant.py`/
`sdp_capability.py`/`derive_sdp_partition.py`, `136.1-03`'s two new test modules) are now landed, and
this is the first CI-parity measurement taken with **all of them present at once**.

### 1. `tools/ci_parity.sh` — every leg's exit status, verbatim

```
CI-parity recipe (GATE-09) -- repo root: /workspaces/firestarter_app

Leg 1 (pytest, empty sibling root):  exit 0
Leg 2 (pytest, sibling present):     exit 0
Leg 3 (ruff check + format --check): exit 0
Leg 4 (mypy watermark gate):         exit 2
BOARD-ATTACHED: none
Python: Python 3.12.13
CI-PARITY: FAIL (legs:4)
```

**Identical in shape to both `## Before` and `## After PROV-01`** — legs 1-3 exit 0, leg 4 exits 2 for
the same documented reason (the devcontainer's ambient Python 3.12 numpy stub truncates mypy at a
PEP-695 syntax error before it can report a count; `tools/ci_replica_venv.sh` below is the only local
path to a real number). No leg's exit code moved across the whole phase.

### 2. `tools/ci_replica_venv.sh` — all five legs + the two certifying lines

```
INTERPRETER: /home/vscode/.local/bin/python3.11 Python 3.11.15
MYPY-VERSION: mypy 2.3.0 (compiled: yes)
NUMPY-PRESENT: no
Leg 1 (venv create-or-reuse + install): exit 0
Leg 2 (numpy absent):                   exit 0
Leg 3 (ruff check + format --check):    exit 0
Leg 4 (mypy watermark gate):            exit 0
Leg 5 (pytest --cov, CI's exact args):  exit 0
CI-REPLICA: PASS
```

**Certifying lines, verbatim:**

```
Found 33 errors in 13 files (checked 132 source files)
checked 132 source files
mypy errors: 33 (watermark: 35)
```

Leg 5's full suite: **1504 passed, 1 warning** in 251.70s, coverage **82.14%** against the 70% floor
(30 snapshots passed). `ruff check`/`ruff format --check`: clean (126 files already formatted).

### 3. `.venv/ci-replica/bin/python -m pytest tests/ -o addopts="" -q` — tail

```
--------------------------- snapshot report summary ----------------------------
30 snapshots passed.
1504 passed, 1 warning in 219.77s (0:03:39)
```

**1504 passed** — matches Leg 5 above exactly (same interpreter, same test set, run independently a
second time as this task's own step 3).

### 4. `tests/test_sdp_db_invariant.py -o addopts="" -q` (explicit, standalone re-run)

```
9 passed in 0.08s
```

Verbose re-run (`-v`) confirms all 9 by name, including **both** partition-comparison legs:
`test_sdp_partition_matches_committed_allow_list_element_wise` (the original hand-curated-snapshot
check, Phase 131) and `test_sdp_partition_matches_infoic_derived_field_element_wise` (the new
infoic-derived-DB-field check, Plan `136.1-02`) — both green, both independently agreeing the
partition is **43 ALLOW / 41 REFUSE / 84 total**.

### 5. Byte-for-byte diff against the ORIGINAL `120-sdp-partition.json`

Independently re-measured this task (not read from any prior plan's own record): loaded
`firestarter/data/chip_database.json` directly, re-derived the ALLOW set via the production
`sdp_capability_for_entry` predicate over all 84 `algorithm==13` entries, and compared the resulting
43-entry `{mfr}/{part_number}` key set against
`.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json`'s
own committed `allow` array (also 43 entries, keyed identically), sorted on both sides.

```
Original committed allow count: 43
Current 0x0D total: 84
Current measured allow count: 43
Only in original (missing from current): []
Only in current (new, not in original): []
BYTE-FOR-BYTE MATCH: True
```

**Zero entries differ, in either direction.** This is the same 43-chip ALLOW list that was true
before Phase 136.1 started — indeed before Phase 120 itself was superseded by later plans; the file
being diffed against predates this entire milestone.

### 6. `tools/derive_sdp_partition.py` — closing, independent, from-scratch re-run

Run against the cached, previously-verified pinned-commit XML copy (`INFOIC_XML_PATH` pointed at the
scratchpad copy, per this task's own instruction to avoid a redundant live fetch when a verified cache
already exists):

```
====================================================================================================
SDP-capability partition derived from a freshly-loaded infoic.xml
minipro @ a8efaedc236c1d9718bd28299dfbb99536b010ff (INFOIC2PLUS, exact-token keying, parens NOT stripped)
====================================================================================================
ALLOW   : 43
REFUSE  : 41
MIXED   : 0
NOMATCH : 0
TOTAL   : 84  (must be 84)

PASS: fresh XML derivation measures 43 ALLOW / 41 REFUSE / 84 total.
PASS: fresh XML derivation agrees with sdp_capability_for_entry on all 43 ALLOW entries.
PASS: fresh XML derivation agrees with the committed protect_on_after field on all 43 ALLOW entries.

PASS: 43/41/84, zero disagreement against both comparison targets (sdp_capability_for_entry and protect_on_after).
```

Exit 0. This is the phase's **third** independent confirmation of 43/41/84 this task alone performs
(steps 4, 5, 6 above), on top of the three plans' own prior measurements (`136.1-01`'s regression
floor, `136.1-02`'s GATE-08 leg 1 + this same script's first run, `136.1-03`'s untouched-partition
confirmation) — six independent measurements across the whole phase, all agreeing.

### 7. The phase's own opening promise, re-confirmed

**"The split stays 43 ALLOW / 41 REFUSE / 84 -- this phase changed provenance, never verdicts."**
Confirmed one final time by steps 4-6 above: the production transcription, the committed
`protect_on_after` DB field, and a from-scratch fresh XML re-derivation all agree at 43/41/84 with
zero disagreement among them, and the measured ALLOW set is byte-for-byte identical to the
pre-Phase-136.1 (indeed pre-v1.30) committed snapshot. No chip moved between ALLOW and REFUSE at any
point in this phase's four plans.

### 8. Firmware submodule and package-manager confirmation

```
$ git -C /workspaces/firestarter status --porcelain
(empty)
```

No file under `/workspaces/firestarter/` (the firmware submodule) was touched at any point across
this whole phase — v1.30 is host-only, matching every plan-level confirmation already recorded in
`136.1-01-SUMMARY.md`/`136.1-02-SUMMARY.md`/`136.1-03-SUMMARY.md`. `git -C firestarter_app log
--diff-filter=A --name-only f294821^..HEAD -- '*.txt' pyproject.toml setup.py` (the phase's full
commit range, oldest to newest) returns nothing — no `requirements*.txt`, `pyproject.toml`, or
`setup.py` was ever touched, confirming no new pip/npm/cargo package was installed anywhere in this
phase.

### Whole-phase summary table

| Metric | Before | After PROV-01 | After (phase close) | Phase-wide delta |
|---|---|---|---|---|
| mypy errors (watermark 35) | 33 | 33 | 33 | **0** |
| mypy headroom | 2 | 2 | 2 | **0** |
| checked source files | 130 | 130 | 132 | **+2** (two new test files added by Plan `136.1-03`: `test_lockable_proms_doc_claims.py`, `test_b15_page_size_corroboration.py` — both inside `tests/`, which IS mypy's CI-scanned path, unlike `tools/`) |
| full suite (`pytest tests/`) | 1494 passed | 1494 passed | 1504 passed | **+10** (+2 from Plan `136.1-02`'s two new GATE-08 tests, +8 from Plan `136.1-03`'s two new eight-test modules; zero deletions across the phase) |
| coverage | 82.14% | 82.14% | 82.14% | **0.00 pp** |
| ruff (check + format) | clean | clean | clean | **0** |
| SDP partition (ALLOW/REFUSE/TOTAL) | 43/41/84 | 43/41/84 | 43/41/84 | **0** |

**Whole-phase mypy/lint/test-count delta, stated explicitly, never estimated:** mypy headroom is
**flat at 2** across the entire phase (33 errors against an unmoved 35 watermark) despite the checked
source-file count growing by 2 (the two new test files this phase's Plan `136.1-03` added, which sit
inside mypy's CI-scanned `tests/` path — unlike Plan `136.1-01`'s `tools/`-only edit, this delta was
measured, not assumed zero, exactly as this task's own instruction required). The full suite grew by
exactly 10 tests, matching the two test-adding plans' own commit-level counts with 0 unrelated
deletions or regressions. Coverage and ruff cleanliness are unchanged. The SDP partition itself never
moved.

**Phase 137's honesty ledger can read this table directly: this phase's real, measured cost is 0 mypy
headroom spent, +2 mypy-scanned files, +10 tests, 0.00 pp coverage change — never an estimate.**
