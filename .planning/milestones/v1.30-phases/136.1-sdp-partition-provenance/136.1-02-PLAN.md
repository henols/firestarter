---
phase: 136.1-sdp-partition-provenance
plan: 02
type: execute
wave: 2
depends_on: ["136.1-01"]
files_modified:
  - firestarter_app/tests/test_sdp_db_invariant.py
  - firestarter_app/firestarter/sdp_capability.py
  - firestarter_app/tools/derive_sdp_partition.py
  - .planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md
autonomous: true
requirements: [PROV-02, PROV-03, PROV-04]
must_haves:
  truths:
    - "GATE-08 is re-pointed at a genuinely infoic.xml-derived source: a NEW test computes the SDP partition from chip_database.json's own protect_on_after field (shipped by Plan 136.1-01) and asserts it equal, element-wise, to the production sdp_capability_for_entry answer -- closing the gap test_sdp_db_invariant.py's own committed-snapshot comment has named since Phase 131 ('leg 1... not implementable... chip_database.json contains ZERO occurrences of the string flags')."
    - "SDP_CAPABLE_TOKENS stays a static frozenset literal, untouched, and tools/check_sdp_capability_invariants.py's Class-2(b) AST gate (which forbids anything but a literal frozenset-of-string-literals binding) still passes -- the transcription SURVIVES per PROV-02's 'or' branch; this plan does not make production ALLOW/REFUSE reads a mutable DB field, which would defeat the fail-closed guarantee a user-supplied ~/.firestarter/database.json entry currently cannot escape."
    - "The new gate has been SEEN to fail: a real, planted single-chip re-bucketing on the actual committed chip_database.json produces a FAIL naming that exact chip, captured verbatim, then the file is restored byte-identically before commit (PROV-03, hard requirement 7)."
    - "tools/derive_sdp_partition.py is committed in firestarter_app (not left stranded in the archived meta-repo 120-* directory), pinned to minipro a8efaedc236c1d9718bd28299dfbb99536b010ff, documented as requiring a fetch (tools/infoic*.xml is gitignored and absent), and preserves Phase 120's exact token-matching rules -- exact part_number token, strip only the @PACKAGE suffix, do NOT strip parentheticals."
    - "Running tools/derive_sdp_partition.py today (against the cached infoic-a8efaed.xml or a fresh live fetch) reproduces 43 ALLOW / 41 REFUSE / 84 total with zero disagreement against BOTH sdp_capability_for_entry and chip_database.json's own protect_on_after field."
  artifacts:
    - path: "firestarter_app/tests/test_sdp_db_invariant.py"
      provides: "the re-pointed GATE-08 comparison (production transcription vs infoic-derived DB field) plus its non-vacuity proof, ADDED alongside the existing committed-snapshot tests (both kept, neither removed)"
    - path: "firestarter_app/tools/derive_sdp_partition.py"
      provides: "the committed, reproducible, fetch-based independent re-derivation script (PROV-04)"
    - path: ".planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md"
      provides: "verbatim record of the live seen-to-fail demonstration and the byte-identical restore"
  key_links:
    - "This plan reads chip_database.json's protect_on_after field, which only exists once Plan 136.1-01 has run -- a hard data dependency, not merely a file-overlap one."
    - "tools/check_sdp_capability_invariants.py (untouched by this plan) is the reason sdp_capability.py's runtime logic is NOT changed here -- its Class 2(b) rule is the concrete, mechanical reason PROV-02 takes the 'transcription survives, gated' branch rather than the 'derive at runtime' branch."
---

<objective>
Re-point GATE-08 at a genuinely infoic.xml-derived source now that Plan 136.1-01 has shipped a committed
per-chip `protect_on_after` field, prove that gate SEEN to fail under a real planted single-chip
re-bucketing, and commit a reproducible, fetch-based independent verification script into
`firestarter_app/tools/` -- while leaving the static, fail-closed `SDP_CAPABLE_TOKENS` transcription
completely untouched, because an existing AST gate (`tools/check_sdp_capability_invariants.py`) makes any
other shape of it mechanically fail.

Purpose: PROV-02's own text offers two branches -- derive at runtime, or keep the transcription gated by
an equality proof. The runtime-derive branch is unavailable here without reopening the exact
permit-by-default / widenable-allow-set hole `check_sdp_capability_invariants.py` was built to close (a
DB field readable from a user's local override merge must never become an ALLOW vote). PROV-03 supplies
the equality proof that branch needs, and requires it be SEEN to fail, not merely asserted fail-closed.
PROV-04 requires the underlying Phase 120 derivation to be reproducible from a clean checkout, in-repo,
not stranded in an archived meta-repo phase directory.
Output: a re-pointed GATE-08 (both directions kept, the new comparison ADDED), a verbatim seen-to-fail
demonstration, and a committed, fetch-capable re-derivation script proven to reproduce 43/41/84 today.
</objective>

<repo_topology>
All code in this plan lives inside the `firestarter_app` git submodule -- `cd /workspaces/firestarter_app`
and commit THERE, on `gsd/v1.30-sdp-surface-retirement` (the meta repo is on
`gsd/v1.30-sdp-surface-retirement-behavioral-lock-proof` -- the names deliberately diverge). Verify with
`git -C /workspaces/firestarter_app rev-parse --abbrev-ref HEAD` before the first edit, and confirm Plan
`136.1-01`'s commits (the `chip_database.json` regeneration) are present on this branch before starting --
`grep -c protect_on_after firestarter/data/chip_database.json` must be non-zero.

`.planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md` is a **meta-repo** artifact,
committed in `/workspaces`.

The top-level `/workspaces/firestarter/` directory is the **Arduino firmware submodule and is not touched
by this plan at all.**
</repo_topology>

<execution_context>
@/workspaces/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-VALIDATION.md
@.planning/phases/136.1-sdp-partition-provenance/136.1-01-SUMMARY.md
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-derive-sdp-allowset.py
@.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-sdp-partition.json
</context>

## Requirements this plan may mark Complete

**MAY tick:** PROV-02, PROV-03, PROV-04.

**Contributes to but MUST NOT tick:** none -- all three are fully discharged by this plan's tasks (the
re-pointed gate, its seen-to-fail proof, and the committed reproduction script are each requirement's
entire ask).

**MUST NOT touch, re-tick, or re-evidence:** PROV-01 (already `[x]` by Plan `136.1-01`), PROV-05, PROV-06
(Plan `136.1-03`'s), and none of `GATE-*`, `RETIRE-*`, `LEG-*`, `RELOCK-*`, `CHAN-*`, or `CLOSE-*`. This
plan must NOT edit `tools/check_sdp_capability_invariants.py`, must NOT delete or narrow
`_COMMITTED_SDP_ALLOW_ENTRIES`, and must NOT change `SDP_CAPABLE_TOKENS`'s literal contents or binding
shape.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-point GATE-08 -- add the infoic-derived-field comparison, keep the existing snapshot check</name>
  <files>firestarter_app/tests/test_sdp_db_invariant.py, firestarter_app/firestarter/sdp_capability.py</files>
  <read_first>
    - firestarter_app/tests/test_sdp_db_invariant.py (full file) -- `_select_0x0d_chips`, `_partition_0x0d`,
      `_assert_partition_matches_committed`, `_COMMITTED_SDP_ALLOW_ENTRIES`'s own comment block (`:165-195`)
      which states leg 1 ("recompute from the DB field") was "not implementable... measured 2026-08-03" --
      that measurement is now stale; this task implements leg 1 for the first time.
    - firestarter_app/tools/check_sdp_capability_invariants.py (full file) -- Class 2(b)'s exact permitted
      shape (`_is_clean_frozenset_of_literals_call`): a direct `frozenset(...)` call over a string-literal
      display only, never a comprehension, call, or name reference. This is WHY `SDP_CAPABLE_TOKENS` is
      not switched to read `protect_on_after` at runtime in this task.
    - firestarter_app/firestarter/sdp_capability.py `:58-70` -- the comment block immediately above
      `SDP_CAPABLE_TOKENS`'s definition, the one line this task adds to.
  </read_first>
  <behavior>
    - `_partition_from_protect_on_after_field(db)` selects every algorithm==13 chip and reads
      `chip["programming"]["protect_on_after"]` -- raising `KeyError` (never defaulting) if the field is
      absent, so a chip_database.json that regresses to missing the field fails loudly rather than
      silently reading as REFUSE.
    - `test_sdp_partition_matches_infoic_derived_field_element_wise` passes against the real,
      Plan-136.1-01-regenerated chip_database.json.
    - A synthetic single-chip re-bucketing (in-memory dict, never the real file) makes the new
      non-vacuity test raise, naming the moved chip and not an untouched control chip.
    - The existing `_COMMITTED_SDP_ALLOW_ENTRIES`-based tests (5/6/7, unmodified) still pass unchanged.
  </behavior>
  <action>
In `tests/test_sdp_db_invariant.py`, add a short module-docstring paragraph explaining WHY this task adds a
second, independent comparison rather than replacing the first: two independent proofs (the hand-curated
43-entry snapshot, and the freshly-derived-from-the-DB-field partition) are strictly stronger than
replacing one with the other, and the snapshot is cheap insurance that costs nothing to keep. State the
Phase-131 gap this closes: `_COMMITTED_SDP_ALLOW_ENTRIES`'s own comment (`:165-195`) said leg 1 was "not
implementable" because `chip_database.json` had zero `flags`-derived fields -- Plan `136.1-01` changed that
measurement; this is leg 1, implemented for the first time.

Add `_partition_from_protect_on_after_field(db)`: mirrors `_partition_0x0d`'s selection and
`"MFR/PART_NUMBER"` key convention exactly, but reads `chip["programming"]["protect_on_after"]` directly
(no `.get()` default -- a missing key must raise) instead of calling `sdp_capability_for_entry`. Returns
sorted `(allow, refuse)` lists.

Add `_assert_two_partitions_match(allow_a, allow_b, label_a, label_b)`: a generalization of
`_assert_partition_matches_committed` for comparing two MEASURED sources (neither a "committed literal"),
naming both directions of the symmetric difference and both labels in the raised message. Do NOT modify
`_assert_partition_matches_committed` or any of its three existing callers (tests 5/6/7) -- add this as a
new, separate helper.

Add `test_sdp_partition_matches_infoic_derived_field_element_wise()`: computes `_partition_0x0d(db)`
(production, `SDP_CAPABLE_TOKENS`-based) and `_partition_from_protect_on_after_field(db)` (the new,
Plan-136.1-01-shipped-field-based source); asserts equal via `_assert_two_partitions_match`; additionally
asserts both partitions independently measure 43 ALLOW / 41 REFUSE / 84 total.

Add `test_partition_flags_a_moved_chip_via_db_field_non_vacuous()`: mirrors test 7's synthetic-DB shape
(`AT28C256` as the ALLOW chip that moves, the `AT28C16,AT28HC16,AT28HC16L` control that does not), but
this time the synthetic in-memory dicts carry an explicit `protect_on_after` field (not a part_number token
trick) -- flip the moving chip's field from `True` to `False` between the "before" and "after" synthetic
DBs, and prove `_assert_two_partitions_match` raises naming the moved chip and not the control.

In `firestarter/sdp_capability.py`, add ONE sentence to the existing comment block above
`SDP_CAPABLE_TOKENS` (`:58-70`): this static transcription is no longer merely asserted equal to a
hand-curated snapshot -- `tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise`
now proves it equal, element-wise, to `chip_database.json`'s own `protect_on_after` field (PROV-02/03,
Phase 136.1) -- and state explicitly that this frozenset still cannot itself read that field at runtime,
by design: `tools/check_sdp_capability_invariants.py`'s Class 2(b) gate forbids any binding shape other
than a literal `frozenset(...)` of string literals, unchanged and untouched by this phase.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_sdp_db_invariant.py -o addopts="" -q &amp;&amp; python3 tools/check_sdp_capability_invariants.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` collects 9 tests (the 7 existing plus the
      2 new), all pass.
    - `git -C /workspaces/firestarter_app diff -- tests/test_sdp_db_invariant.py` shows
      `_COMMITTED_SDP_ALLOW_ENTRIES` and `_assert_partition_matches_committed` byte-identical to before
      this task (additions only, no edits to existing lines within them).
    - `python3 tools/check_sdp_capability_invariants.py` still prints a `PASS:` line and exits 0 -- proving
      the docstring-only `sdp_capability.py` edit did not trip the AST gate.
    - `git -C /workspaces/firestarter_app diff --stat -- tools/check_sdp_capability_invariants.py` is
      empty -- this task never touches that file.
    - `ruff check tests/ firestarter/` and `ruff format --check tests/ firestarter/` both exit 0.
  </acceptance_criteria>
  <done>GATE-08 now compares the production transcription against a genuinely infoic-derived DB field, in
  addition to (never instead of) its existing hand-curated snapshot check -- closing the gap
  `_COMMITTED_SDP_ALLOW_ENTRIES`'s own comment has named since Phase 131.</done>
</task>

<task type="auto">
  <name>Task 2: Live seen-to-fail demonstration on the real committed file, then byte-identical restore</name>
  <files>.planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md</files>
  <read_first>
    - Task 1's new test, `test_sdp_partition_matches_infoic_derived_field_element_wise`.
    - `.planning/phases/131-gate-hardening-ci-parity/131-CONTEXT.md`'s D-03 RED-preserving-revert
      precedent (cited in GATE-01's REQUIREMENTS.md evidence) -- the exact "observed RED once, then
      restored byte-identically" discipline this project applies everywhere; this task is that discipline
      applied to a real committed data file rather than a source line.
    - `.planning/phases/134-.../134-*-SUMMARY.md` for the recorded shape of a similar live demonstration
      (any one plan's non-vacuity obligation section is a fine model for the write-up).
  </read_first>
  <action>
Pick ONE real ALLOW chip already present in the committed `chip_database.json`:
`ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L` -- the same part this
milestone's own `dev test` SDP leg exercises elsewhere (LEG-06's evidence), a recognizable, meaningful
choice rather than an arbitrary one.

Edit the REAL, committed `firestarter/data/chip_database.json` in place: locate that ATMEL entry, flip its
`programming.protect_on_after` from `true` to `false` -- this is the "planted single-chip re-bucketing"
PROV-03 requires be SEEN to fail, not merely asserted capable of it.

Run
`pytest tests/test_sdp_db_invariant.py::test_sdp_partition_matches_infoic_derived_field_element_wise -o addopts="" -q`
and confirm it FAILS. Capture the FULL raised assertion message VERBATIM -- it must name
`ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L` explicitly as having left
ALLOW (or the equivalent wording `_assert_two_partitions_match`'s message actually uses).

Revert the edit: `git -C /workspaces/firestarter_app checkout -- firestarter/data/chip_database.json` and
confirm `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/chip_database.json` prints
nothing. Re-run the same test and confirm it passes again.

Record the entire before/after transcript verbatim -- the FAIL output naming the exact chip key, the
confirmation of the clean revert, and the re-pass -- into
`.planning/phases/136.1-sdp-partition-provenance/136.1-02-SEEN-TO-FAIL.md`, and reference it from this
plan's own SUMMARY.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; git diff --stat -- firestarter/data/chip_database.json &amp;&amp; .venv/ci-replica/bin/python -m pytest tests/test_sdp_db_invariant.py -o addopts="" -q</automated>
  </verify>
  <acceptance_criteria>
    - `136.1-02-SEEN-TO-FAIL.md` exists and contains verbatim FAIL output naming the exact chip key
      `ATMEL/AT28C256,AT28C256E,AT28C256F,AT28HC256,AT28HC256E,AT28HC256F,AT28HC256L`.
    - Post-revert `git -C /workspaces/firestarter_app diff --stat -- firestarter/data/chip_database.json`
      is empty (the automated verify command above proves this directly).
    - Post-revert, `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` is fully green again (all 9
      tests).
  </acceptance_criteria>
  <done>PROV-03's fail-closed gate has been SEEN to fail under a real, planted single-chip re-bucketing on
  the committed file, with the failure message recorded verbatim, and the file restored byte-identically
  before commit.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the reproducible, fetch-based independent re-derivation script (PROV-04)</name>
  <files>firestarter_app/tools/derive_sdp_partition.py</files>
  <read_first>
    - `.planning/phases/120-host-cli-surface-wire-emission-capability-refusal/120-derive-sdp-allowset.py`
      (full file, already the origin of every rule this task preserves).
    - `firestarter_app/firestarter/sdp_capability.py`'s `split_part_number_tokens` (`:187-198`) -- the
      exact token rule to cross-reference (comma-split, uppercase, strip only `@PACKAGE`, keep
      parentheticals).
    - `firestarter_app/tools/build_db.py`'s `MINIPRO_XML_URL` constant (`:17-20`) -- the pinned commit URL
      this task's script duplicates verbatim.
  </read_first>
  <action>
Author `tools/derive_sdp_partition.py`: a standalone, network-capable script, never imported by production
code or by the pytest suite -- this is the manual/occasional reproducibility recipe PROV-04 asks for,
analogous to `build_db.py` itself, not a CI-wired gate (CI must never depend on a live network fetch).

Constants: duplicate `MINIPRO_XML_URL` from `build_db.py` verbatim, with a comment stating the two
constants must be kept in lockstep (the same discipline this project already applies to
`constants.py`/`firestarter.h`). Read an `INFOIC_XML_PATH` env var: if set, read the XML from that local
file path instead of fetching (so a cached copy -- today's planning-scratchpad copy, or any future one --
can be used WITHOUT this script's source ever depending on that path existing); if unset, fetch live via
`requests.get`, mirroring `build_db.py`'s own default behavior exactly.

Preserve Phase 120's exact matching rules verbatim, citing `120-derive-sdp-allowset.py` as the origin in a
comment: key on the EXACT `part_number` token (comma-split, uppercased), strip ONLY the `@PACKAGE` suffix,
do NOT strip parentheticals (`AT28C64B(Non-Standard)` stays its own token, never collapsed onto
`AT28C64B`).

Read `firestarter/data/chip_database.json`'s own algorithm==13 entries (duplicate the selection logic
locally with a one-line comment noting the deliberate duplication -- this script must not import from
`tests/`). For each entry, cross-reference the freshly-loaded XML's `&lt;ic&gt;` elements via the exact
token rule, derive ALLOW/REFUSE from bit 15 (`0x8000`), and independently compute counts and a MIXED/
NO-MATCH accounting (mirroring Phase 120's own script structure).

Compare the freshly-derived partition against BOTH (a) `firestarter.sdp_capability.sdp_capability_for_entry`
(import from the installed package) and (b) `chip_database.json`'s own committed `protect_on_after` field.
Print a PASS/FAIL summary for both comparisons plus the 43/41/84 triple, and exit non-zero if either
comparison disagrees, naming every disagreeing chip.

Module docstring states: pinned to minipro `a8efaedc236c1d9718bd28299dfbb99536b010ff`; `tools/infoic*.xml`
is gitignored and absent from a clean checkout; this script fetches it live by default (an explicit network
dependency, stated plainly, mirroring `build_db.py`'s own), or reads `INFOIC_XML_PATH` if set.

Run it ONCE now, with `INFOIC_XML_PATH` pointed at the cached scratchpad copy
(`/tmp/claude-1000/-workspaces/567ad46d-dc13-4977-a55b-4d782f814e87/scratchpad/infoic-a8efaed.xml`) to
avoid a redundant live fetch when a verified-fresh copy from today already exists -- record the full PASS
output verbatim in this plan's SUMMARY, explicitly confirming 43/41/84 and zero disagreement against both
comparison targets. Confirm the script's own source contains NO hardcoded reference to that scratchpad
path anywhere -- the cached copy is a caller convenience via the env var only.
  </action>
  <verify>
    <automated>cd /workspaces/firestarter_app &amp;&amp; INFOIC_XML_PATH=/tmp/claude-1000/-workspaces/567ad46d-dc13-4977-a55b-4d782f814e87/scratchpad/infoic-a8efaed.xml python3 tools/derive_sdp_partition.py</automated>
  </verify>
  <acceptance_criteria>
    - Running the script (via `INFOIC_XML_PATH` against the cached file) prints ALLOW=43, REFUSE=41,
      TOTAL=84, and zero disagreements against both `sdp_capability_for_entry` and the committed
      `protect_on_after` field.
    - `grep -c scratchpad tools/derive_sdp_partition.py` returns 0 -- no hardcoded reference to the caller
      convenience path.
    - `ruff check tools/derive_sdp_partition.py` and `ruff format --check tools/derive_sdp_partition.py`
      pass (best-effort local hygiene -- note in the SUMMARY that `ci.yml`'s ruff step does not scope to
      `tools/` at all, so this is not itself a CI-enforced claim).
    - The script's module docstring names the pinned commit and states the fetch-vs-local-override
      behavior explicitly.
  </acceptance_criteria>
  <done>The Phase 120 derivation is committed, reproducible from a clean checkout via a real fetch (or a
  caller-supplied local copy), and independently re-verified today against both the static transcription
  and the new DB field -- zero disagreement, 43/41/84.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip_database.json (data) → sdp_capability.py (runtime ALLOW/REFUSE decision) | Deliberately NOT crossed by production code in this plan -- the decision stays sourced from a static, literal frozenset, never a file read |
| tools/derive_sdp_partition.py → gitlab.com (unauthenticated HTTPS fetch) | Same class as Plan 136.1-01's build_db.py fetch; a manual/occasional recipe, never CI-wired |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136.1-05 | Elevation of Privilege | `sdp_capability_for_entry`'s ALLOW/REFUSE decision made readable-from-a-mutable-file, defeating the fail-closed guarantee against a user-supplied `~/.firestarter/database.json` entry | critical | mitigate | Explicitly NOT done -- `SDP_CAPABLE_TOKENS` stays a static literal; `tools/check_sdp_capability_invariants.py` untouched and re-confirmed passing (Task 1's verify) |
| T-136.1-06 | Tampering | A single chip silently re-bucketed between ALLOW/REFUSE without detection | high | mitigate | The new gate (Task 1) plus the live seen-to-fail proof (Task 2) |
| T-136.1-07 | Repudiation | Claiming the gate is "fail-closed" without ever observing it fail | high | mitigate | Task 2's live demonstration on the real file, verbatim message recorded, byte-identical restore proven |
| T-136.1-08 | Tampering | `tools/derive_sdp_partition.py`'s unauthenticated HTTPS fetch | low | accept | Same rationale as T-136.1-01; unchanged, pinned-commit mechanism |
| T-136.1-SC | Tampering | package-manager installs | n/a | accept | No new npm/pip/cargo package installed this plan |
</threat_model>

<verification>
- `pytest tests/test_sdp_db_invariant.py -o addopts="" -q` -- 9/9 green.
- `python3 tools/check_sdp_capability_invariants.py` -- PASS, untouched.
- `136.1-02-SEEN-TO-FAIL.md` records a real FAIL, naming the planted chip, then a clean, verified restore.
- `tools/derive_sdp_partition.py` -- run once against the cached XML, records PASS at 43/41/84, zero
  disagreement against both comparison targets.
- `git -C /workspaces/firestarter_app status --porcelain -- firestarter/data/chip_database.json` is empty
  after Task 2 (the plant was reverted, never committed in the flipped state).
</verification>

<success_criteria>
- GATE-08 compares production transcription against the infoic-derived DB field, in addition to its
  existing snapshot check.
- The new gate has been SEEN to fail on the real file, verbatim, then restored byte-identically.
- `tools/derive_sdp_partition.py` is committed, documented, fetch-capable, and proven today to reproduce
  43/41/84 with zero disagreement.
</success_criteria>

<output>
Create `.planning/phases/136.1-sdp-partition-provenance/136.1-02-SUMMARY.md` when done.
</output>
